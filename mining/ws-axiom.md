# ws-axiom 
 
# Bad smells
I found 2296 bad smells with 257 repairable:
| ruleID | number | fixable |
| --- | --- | --- |
| JUnitMalformedDeclaration | 719 | false |
| JavadocDeclaration | 316 | false |
| SimplifiableAssertion | 232 | false |
| UnnecessaryModifier | 222 | true |
| Deprecation | 92 | false |
| DuplicatedCode | 85 | false |
| SpringXmlModelInspection | 84 | false |
| JavadocReference | 72 | false |
| NullableProblems | 57 | false |
| FieldMayBeFinal | 32 | false |
| DataFlowIssue | 29 | false |
| UnusedAssignment | 24 | false |
| MarkedForRemoval | 18 | false |
| ToArrayCallWithZeroLengthArrayArgument | 17 | true |
| EmptyStatementBody | 17 | false |
| StringOperationCanBeSimplified | 17 | false |
| DeprecatedIsStillUsed | 16 | false |
| RedundantCast | 14 | false |
| IgnoreResultOfCall | 13 | false |
| ConstantValue | 13 | false |
| UnparsedCustomBeanInspection | 12 | false |
| DuplicateExpressions | 10 | false |
| StaticInitializerReferencesSubClass | 9 | false |
| FinalMethodInFinalClass | 9 | false |
| RedundantTypeArguments | 9 | false |
| IntegerMultiplicationImplicitCastToLong | 9 | false |
| UNCHECKED_WARNING | 8 | false |
| SpringPlaceholdersInspection | 8 | false |
| CStyleArrayDeclaration | 6 | false |
| FieldCanBeLocal | 6 | false |
| ProtectedMemberInFinalClass | 6 | true |
| CommentedOutCode | 5 | false |
| JavadocLinkAsPlainText | 5 | false |
| IfStatementWithIdenticalBranches | 5 | false |
| StringEquality | 4 | false |
| StringBufferReplaceableByString | 4 | false |
| InnerClassMayBeStatic | 4 | true |
| ConditionCoveredByFurtherCondition | 4 | false |
| MismatchedArrayReadWrite | 3 | false |
| SimplifiableConditionalExpression | 3 | false |
| ThrowFromFinallyBlock | 3 | false |
| UnnecessarySemicolon | 3 | false |
| WebProperties | 3 | false |
| RedundantCollectionOperation | 3 | false |
| UnnecessaryToStringCall | 3 | true |
| DanglingJavadoc | 3 | false |
| CharsetObjectCanBeUsed | 3 | false |
| CaughtExceptionImmediatelyRethrown | 3 | false |
| DuplicatedBeanNamesInspection | 3 | false |
| UnnecessaryStringEscape | 2 | true |
| UnnecessaryLabelOnContinueStatement | 2 | false |
| SpringBeanConstructorArgInspection | 2 | false |
| FinalPrivateMethod | 2 | false |
| RegExpSimplifiable | 2 | false |
| UNUSED_IMPORT | 2 | false |
| MismatchedCollectionQueryUpdate | 2 | false |
| UnnecessaryInitCause | 2 | false |
| AutoCloseableResource | 2 | false |
| ServletWithoutMappingInspection | 2 | false |
| CachedNumberConstructorCall | 2 | false |
| UnnecessaryContinue | 2 | false |
| SynchronizationOnLocalVariableOrMethodParameter | 2 | false |
| RegExpRedundantEscape | 1 | false |
| DuplicateBranchesInSwitch | 1 | false |
| UnnecessaryReturn | 1 | true |
| FinalStaticMethod | 1 | false |
| ExplicitArrayFilling | 1 | false |
| SpringInjectionValueStyleInspection | 1 | false |
| PointlessBitwiseExpression | 1 | false |
| SlowAbstractSetRemoveAll | 1 | false |
| PointlessNullCheck | 1 | false |
| StringConcatenationInLoops | 1 | false |
| OptionalGetWithoutIsPresent | 1 | false |
| ManualArrayToCollectionCopy | 1 | false |
| MissingSerialAnnotation | 1 | false |
| UnnecessaryCallToStringValueOf | 1 | false |
| CollectionContainsUrl | 1 | false |
| CollectionAddAllCanBeReplacedWithConstructor | 1 | false |
| TrivialIf | 1 | false |
| NonStrictComparisonCanBeEquality | 1 | true |
| EqualsHashCodeCalledOnUrl | 1 | false |
| RegExpSingleCharAlternation | 1 | false |
| SpringXmlAutowireExplicitlyInspection | 1 | false |
| UnnecessaryLocalVariable | 1 | true |
| CopyConstructorMissesField | 1 | false |
| UseBulkOperation | 1 | false |
| CastCanBeRemovedNarrowingVariableType | 1 | false |
## RuleId[id=ToArrayCallWithZeroLengthArrayArgument]
### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Class\[ifaces.size()\]'
in `testing/testutils/src/main/java/org/apache/axiom/testutils/InvocationCounter.java`
#### Snippet
```java
        return Proxy.newProxyInstance(
                InvocationCounter.class.getClassLoader(),
                ifaces.toArray(new Class<?>[ifaces.size()]),
                new InvocationHandlerImpl(target));
    }
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new String\[parameters.size()\]'
in `axiom-api/src/main/java/org/apache/axiom/mime/ContentType.java`
#### Snippet
```java
            parameters.add(tokenizer.requireTokenOrQuotedString());
        }
        this.parameters = parameters.toArray(new String[parameters.size()]);
    }
    
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new String\[clazzes.size()\]'
in `axiom-api/src/main/java/org/apache/axiom/locator/OSGiOMMetaFactoryLocator.java`
#### Snippet
```java
                    properties.put("feature", feature.getName());
                    properties.put(Constants.SERVICE_RANKING, feature.getPriority());
                    ServiceRegistration<?> registration = bundle.getBundleContext().registerService(clazzes.toArray(new String[clazzes.size()]), implementation.getMetaFactory(), properties);
                    registrations.add(registration);
                    ServiceReference<?> reference = registration.getReference();
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Class\[extensionInterfaces.size()\]'
in `axiom-api/src/main/java/org/apache/axiom/locator/ImplementationFactory.java`
#### Snippet
```java
        }
        try {
            return new Feature(name, Integer.parseInt(priority), extensionInterfaces.toArray(new Class<?>[extensionInterfaces.size()]));
        } catch (NumberFormatException ex) {
            log.error("Invalid priority value '" + priority + "'; must be an integer");
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Feature\[features.size()\]'
in `axiom-api/src/main/java/org/apache/axiom/locator/ImplementationFactory.java`
#### Snippet
```java
            child = child.getNextSibling();
        }
        return new Implementation(name, metaFactory, features.toArray(new Feature[features.size()]));
    }

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new String\[cids.size()\]'
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/Attachments.java`
#### Snippet
```java
    public String[] getAllContentIDs() {
        Set<String> cids = delegate.getContentIDs(true);
        return cids.toArray(new String[cids.size()]);
    }

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new SOAPElementType\[childTypes.size()\]'
in `testing/soap-testsuite/src/main/java/org/apache/axiom/ts/soap/SOAPElementType.java`
#### Snippet
```java

    public SOAPElementType[] getChildTypes() {
        return childTypes.toArray(new SOAPElementType[childTypes.size()]);
    }
}
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new SOAPElementType\[parentTypes.size()\]'
in `testing/soap-testsuite/src/main/java/org/apache/axiom/ts/soap/SOAPElementType.java`
#### Snippet
```java

    public SOAPElementType[] getParentTypes() {
        return parentTypes.toArray(new SOAPElementType[parentTypes.size()]);
    }

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new SOAPSample\[instances.size()\]'
in `testing/soap-testsuite/src/main/java/org/apache/axiom/ts/soap/SOAPSample.java`
#### Snippet
```java
            }
        }
        return instances.toArray(new SOAPSample[instances.size()]);
    }

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new String\[result.size()\]'
in `testing/soap-testsuite/src/main/java/org/apache/axiom/ts/soap/SOAPSpec.java`
#### Snippet
```java
            result.add("TRUE");
        }
        return result.toArray(new String[result.size()]);
    }

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new BuilderFactory\[instances.size()\]'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/dimension/BuilderFactory.java`
#### Snippet
```java
            instances.add(new SAXBuilderFactory(implementation));
        }
        return instances.toArray(new BuilderFactory[instances.size()]);
    }

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new StAXImplementation\[instances.size()\]'
in `testing/jaxp-testsuite/src/main/java/org/apache/axiom/ts/jaxp/stax/StAXImplementation.java`
#### Snippet
```java
            }
        }
        return instances.toArray(new StAXImplementation[instances.size()]);
    }

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new XMLSample\[result.size()\]'
in `testing/xml-testsuite/src/main/java/org/apache/axiom/ts/xml/XMLSample.java`
#### Snippet
```java
        }
        in.close();
        return result.toArray(new XMLSample[result.size()]);
    }

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new ClassDefinition\[result.size()\]'
in `axiom-weaver/src/main/java/org/apache/axiom/weaver/Weaver.java`
#### Snippet
```java
            result.addAll(node.toClassDefinitions(context));
        }
        return result.toArray(new ClassDefinition[result.size()]);
    }

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new MixinMethod\[methodMap.size()\]'
in `axiom-weaver/src/main/java/org/apache/axiom/weaver/ImplementationClassDefinition.java`
#### Snippet
```java
            }
        }
        methods = methodMap.values().toArray(new MixinMethod[methodMap.size()]);
    }

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new String\[ifaceNames.size()\]'
in `axiom-weaver/src/main/java/org/apache/axiom/weaver/ImplementationNode.java`
#### Snippet
```java
                        access,
                        parents.isEmpty() ? null : parents.iterator().next().getClassName(),
                        ifaceNames.toArray(new String[ifaceNames.size()]),
                        primaryInterface.isSingleton(),
                        mixins.toArray(new Mixin[mixins.size()])));
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Mixin\[mixins.size()\]'
in `axiom-weaver/src/main/java/org/apache/axiom/weaver/ImplementationNode.java`
#### Snippet
```java
                        ifaceNames.toArray(new String[ifaceNames.size()]),
                        primaryInterface.isSingleton(),
                        mixins.toArray(new Mixin[mixins.size()])));
        return classDefinitions;
    }
```

## RuleId[id=StringEquality]
### StringEquality
String values are compared using `==`, not 'equals()'
in `axiom-api/src/main/java/org/apache/axiom/util/stax/dialect/SJSXPNamespaceContextWrapper.java`
#### Snippet
```java
            String prefix = (String)it.next();
            String actualNamespaceURI = parent.getNamespaceURI(prefix);
            if (namespaceURI == actualNamespaceURI
                    || namespaceURI != null && namespaceURI.equals(actualNamespaceURI)) {
                prefixes.add(prefix);
```

### StringEquality
String values are compared using `!=`, not 'equals()'
in `axiom-compat/src/main/java/org/apache/axiom/util/stax/AbstractXMLStreamReader.java`
#### Snippet
```java
            }
            String actualLocalName = getLocalName();
            if (actualLocalName != localName && !actualLocalName.equals(localName)) {
                throw new XMLStreamException("Required local name '" + localName +
                        "'; current local name '" + actualLocalName + "'.");
```

### StringEquality
String values are compared using `==`, not 'equals()'
in `mixins/om-mixins/src/main/java/org/apache/axiom/om/impl/stream/XsiTypeFilterHandler.java`
#### Snippet
```java
                boolean bound = false;
                for (int i = 0; i < prefixCount; i++) {
                    if (prefixes[i] == prefix) {
                        bound = true;
                        break;
```

### StringEquality
String values are compared using `==`, not 'equals()'
in `testing/jaxen-testsuite/src/main/java/org/jaxen/test/ExprComparator.java`
#### Snippet
```java
	{
		int cmp;
		if (s1 == s2)
		{
			cmp = 0;
```

## RuleId[id=MismatchedArrayReadWrite]
### MismatchedArrayReadWrite
Contents of array `weavablePackages` are read, but never written to
in `buildutils/axiom-weaver-maven-plugin/src/main/java/org/apache/axiom/weaver/maven/WeaveMojo.java`
#### Snippet
```java

    @Parameter(required = true)
    private String[] weavablePackages;

    @Parameter(required = true)
```

### MismatchedArrayReadWrite
Contents of array `packageMappings` are read, but never written to
in `buildutils/axiom-weaver-maven-plugin/src/main/java/org/apache/axiom/weaver/maven/WeaveMojo.java`
#### Snippet
```java

    @Parameter(required = true)
    private PackageMapping[] packageMappings;

    @Parameter(required = true)
```

### MismatchedArrayReadWrite
Contents of array `interfaces` are read, but never written to
in `buildutils/axiom-weaver-maven-plugin/src/main/java/org/apache/axiom/weaver/maven/WeaveMojo.java`
#### Snippet
```java

    @Parameter(required = true)
    private String[] interfaces;

    @Override
```

## RuleId[id=UnnecessaryModifier]
### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/om/OMDocument.java`
#### Snippet
```java

    /** Field XML_11 XML Version 1.1 */
    final static String XML_11 = "1.1";

    /**
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/om/OMDocument.java`
#### Snippet
```java

    /** Field XML_11 XML Version 1.1 */
    final static String XML_11 = "1.1";

    /**
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/om/OMDocument.java`
#### Snippet
```java

    /** Field XML_10 XML Version 1.0 */
    final static String XML_10 = "1.0";

    /** Field XML_11 XML Version 1.1 */
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/om/OMDocument.java`
#### Snippet
```java

    /** Field XML_10 XML Version 1.0 */
    final static String XML_10 = "1.0";

    /** Field XML_11 XML Version 1.1 */
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `axiom-api/src/main/java/org/apache/axiom/om/OMAttachmentAccessor.java`
#### Snippet
```java
     *         <code>null</code> if the MIME part referred by the content ID does not exist
     */
    public Blob getBlob(String contentID);
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `axiom-api/src/main/java/org/apache/axiom/om/OMFactory.java`
#### Snippet
```java
     * @return TODO
     */
    public OMText createOMText(OMContainer parent, OMText source);
    
    /**
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/om/OMNode.java`
#### Snippet
```java
     * @see #getType()
     */
    static final short SPACE_NODE = XMLStreamConstants.SPACE;

    /**
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/om/OMNode.java`
#### Snippet
```java
     * @see #getType()
     */
    static final short SPACE_NODE = XMLStreamConstants.SPACE;

    /**
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/om/OMNode.java`
#### Snippet
```java
     * @see #getType()
     */
    static final short ENTITY_REFERENCE_NODE = XMLStreamConstants.ENTITY_REFERENCE;

    /**
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/om/OMNode.java`
#### Snippet
```java
     * @see #getType()
     */
    static final short ENTITY_REFERENCE_NODE = XMLStreamConstants.ENTITY_REFERENCE;

    /**
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/om/OMNode.java`
#### Snippet
```java
     * @see #getType()
     */
    static final short COMMENT_NODE = XMLStreamConstants.COMMENT;

    /**
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/om/OMNode.java`
#### Snippet
```java
     * @see #getType()
     */
    static final short COMMENT_NODE = XMLStreamConstants.COMMENT;

    /**
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/om/OMNode.java`
#### Snippet
```java
     * @see #getType()
     */
    static final short DTD_NODE = XMLStreamConstants.DTD;

    /**
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/om/OMNode.java`
#### Snippet
```java
     * @see #getType()
     */
    static final short DTD_NODE = XMLStreamConstants.DTD;

    /**
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/om/OMNode.java`
#### Snippet
```java
     * @see #getType()
     */
    static final short PI_NODE = XMLStreamConstants.PROCESSING_INSTRUCTION;

    /**
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/om/OMNode.java`
#### Snippet
```java
     * @see #getType()
     */
    static final short PI_NODE = XMLStreamConstants.PROCESSING_INSTRUCTION;

    /**
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/om/OMNode.java`
#### Snippet
```java
     * @see #getType()
     */
    static final short TEXT_NODE = XMLStreamConstants.CHARACTERS;

    /**
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/om/OMNode.java`
#### Snippet
```java
     * @see #getType()
     */
    static final short TEXT_NODE = XMLStreamConstants.CHARACTERS;

    /**
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/om/OMNode.java`
#### Snippet
```java
     * @see #getType()
     */
    static final short CDATA_SECTION_NODE = XMLStreamConstants.CDATA;

    /**
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/om/OMNode.java`
#### Snippet
```java
     * @see #getType()
     */
    static final short CDATA_SECTION_NODE = XMLStreamConstants.CDATA;

    /**
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/om/OMNode.java`
#### Snippet
```java
     * @see #getType()
     */
    static final short ELEMENT_NODE = 1;

    /**
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/om/OMNode.java`
#### Snippet
```java
     * @see #getType()
     */
    static final short ELEMENT_NODE = 1;

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `axiom-api/src/main/java/org/apache/axiom/om/impl/MTOMConstants.java`
#### Snippet
```java
public interface MTOMConstants {
    public static final String XOP_INCLUDE = "Include";
    public static final String XOP_NAMESPACE_URI = "http://www.w3.org/2004/08/xop/include";

    /** If the Message is MTOM optimised then <code>MTOM_TYPE</code> */
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/om/impl/MTOMConstants.java`
#### Snippet
```java
public interface MTOMConstants {
    public static final String XOP_INCLUDE = "Include";
    public static final String XOP_NAMESPACE_URI = "http://www.w3.org/2004/08/xop/include";

    /** If the Message is MTOM optimised then <code>MTOM_TYPE</code> */
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/om/impl/MTOMConstants.java`
#### Snippet
```java
public interface MTOMConstants {
    public static final String XOP_INCLUDE = "Include";
    public static final String XOP_NAMESPACE_URI = "http://www.w3.org/2004/08/xop/include";

    /** If the Message is MTOM optimised then <code>MTOM_TYPE</code> */
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `axiom-api/src/main/java/org/apache/axiom/om/impl/MTOMConstants.java`
#### Snippet
```java

public interface MTOMConstants {
    public static final String XOP_INCLUDE = "Include";
    public static final String XOP_NAMESPACE_URI = "http://www.w3.org/2004/08/xop/include";

```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/om/impl/MTOMConstants.java`
#### Snippet
```java

public interface MTOMConstants {
    public static final String XOP_INCLUDE = "Include";
    public static final String XOP_NAMESPACE_URI = "http://www.w3.org/2004/08/xop/include";

```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/om/impl/MTOMConstants.java`
#### Snippet
```java

public interface MTOMConstants {
    public static final String XOP_INCLUDE = "Include";
    public static final String XOP_NAMESPACE_URI = "http://www.w3.org/2004/08/xop/include";

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `axiom-api/src/main/java/org/apache/axiom/om/ds/custombuilder/CustomBuilder.java`
#### Snippet
```java
     * implemented by the {@link CustomBuilder} itself.
     */
    public interface Selector {
        /**
         * Selects the message payload element. For plain XML documents, that is the document
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `axiom-api/src/main/java/org/apache/axiom/om/ds/custombuilder/CustomBuilder.java`
#### Snippet
```java
     * @return an {@link OMDataSource} with content that is equivalent to the original element
     */
    public OMDataSource create(OMElement element) throws OMException;
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAPEnvelope.java`
#### Snippet
```java
     * @return local name of first element in the body or null
     */
    public String getSOAPBodyFirstElementLocalName();
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAPEnvelope.java`
#### Snippet
```java
     * has no namespace or the body is empty
     */
    public OMNamespace getSOAPBodyFirstElementNS();
    
    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAPHeaderBlock.java`
#### Snippet
```java
     * the value of the ROLE/ACTOR, RELAY or MUST_UNDERSTAND setting.
     */
    public String ROLE_PROPERTY = "org.apache.axiom.soap.SOAPHeader.ROLE";
    public String RELAY_PROPERTY = "org.apache.axiom.soap.SOAPHeader.RELAY";
    public String MUST_UNDERSTAND_PROPERTY = "org.apache.axiom.soap.SOAPHeader.MUST_UNDERSTAND";
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAPHeaderBlock.java`
#### Snippet
```java
    public String ROLE_PROPERTY = "org.apache.axiom.soap.SOAPHeader.ROLE";
    public String RELAY_PROPERTY = "org.apache.axiom.soap.SOAPHeader.RELAY";
    public String MUST_UNDERSTAND_PROPERTY = "org.apache.axiom.soap.SOAPHeader.MUST_UNDERSTAND";
    
    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAPHeaderBlock.java`
#### Snippet
```java
     */
    public String ROLE_PROPERTY = "org.apache.axiom.soap.SOAPHeader.ROLE";
    public String RELAY_PROPERTY = "org.apache.axiom.soap.SOAPHeader.RELAY";
    public String MUST_UNDERSTAND_PROPERTY = "org.apache.axiom.soap.SOAPHeader.MUST_UNDERSTAND";
    
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java

    // QNames
    static final QName QNAME_ROLE = new QName(SOAP_ENVELOPE_NAMESPACE_URI, SOAP_ROLE);
    static final QName QNAME_RELAY = new QName(SOAP_ENVELOPE_NAMESPACE_URI, SOAP_RELAY);
    static final QName QNAME_MU_FAULTCODE = new QName(SOAP_ENVELOPE_NAMESPACE_URI,
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java

    // QNames
    static final QName QNAME_ROLE = new QName(SOAP_ENVELOPE_NAMESPACE_URI, SOAP_ROLE);
    static final QName QNAME_RELAY = new QName(SOAP_ENVELOPE_NAMESPACE_URI, SOAP_RELAY);
    static final QName QNAME_MU_FAULTCODE = new QName(SOAP_ENVELOPE_NAMESPACE_URI,
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java
    public static final String SOAP_FAULT_REASON_LOCAL_NAME = "Reason";
    public static final String SOAP_FAULT_TEXT_LOCAL_NAME = "Text";
    public static final String SOAP_FAULT_TEXT_LANG_ATTR_LOCAL_NAME = "lang";
    public static final String SOAP_FAULT_TEXT_LANG_ATTR_NS_URI =
            "http://www.w3.org/XML/1998/namespace";
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java
    public static final String SOAP_FAULT_REASON_LOCAL_NAME = "Reason";
    public static final String SOAP_FAULT_TEXT_LOCAL_NAME = "Text";
    public static final String SOAP_FAULT_TEXT_LANG_ATTR_LOCAL_NAME = "lang";
    public static final String SOAP_FAULT_TEXT_LANG_ATTR_NS_URI =
            "http://www.w3.org/XML/1998/namespace";
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java
    public static final String SOAP_FAULT_REASON_LOCAL_NAME = "Reason";
    public static final String SOAP_FAULT_TEXT_LOCAL_NAME = "Text";
    public static final String SOAP_FAULT_TEXT_LANG_ATTR_LOCAL_NAME = "lang";
    public static final String SOAP_FAULT_TEXT_LANG_ATTR_NS_URI =
            "http://www.w3.org/XML/1998/namespace";
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java
                                                            FAULT_CODE_RECEIVER);

    static final QName QNAME_FAULT_REASON = new QName(SOAP_ENVELOPE_NAMESPACE_URI,
                                                      SOAP_FAULT_REASON_LOCAL_NAME);
    static final QName QNAME_FAULT_CODE = new QName(SOAP_ENVELOPE_NAMESPACE_URI,
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java
                                                            FAULT_CODE_RECEIVER);

    static final QName QNAME_FAULT_REASON = new QName(SOAP_ENVELOPE_NAMESPACE_URI,
                                                      SOAP_FAULT_REASON_LOCAL_NAME);
    static final QName QNAME_FAULT_CODE = new QName(SOAP_ENVELOPE_NAMESPACE_URI,
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java

    public static final String SOAP_ROLE = "role";
    public static final String SOAP_RELAY = "relay";

    // SOAP Fault Code
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java

    public static final String SOAP_ROLE = "role";
    public static final String SOAP_RELAY = "relay";

    // SOAP Fault Code
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java

    public static final String SOAP_ROLE = "role";
    public static final String SOAP_RELAY = "relay";

    // SOAP Fault Code
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java
    // SOAP Fault Codes
    public static final String SOAP_FAULT_VALUE_VERSION_MISMATCH = "VersionMismatch";
    public static final String SOAP_FAULT_VALUE_MUST_UNDERSTAND = "MustUnderstand";
    public static final String SOAP_FAULT_VALUE_DATA_ENCODING_UKNOWN = "DataEncodingUnknown";
    public static final String SOAP_FAULT_VALUE_SENDER = "Sender";
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java
    // SOAP Fault Codes
    public static final String SOAP_FAULT_VALUE_VERSION_MISMATCH = "VersionMismatch";
    public static final String SOAP_FAULT_VALUE_MUST_UNDERSTAND = "MustUnderstand";
    public static final String SOAP_FAULT_VALUE_DATA_ENCODING_UKNOWN = "DataEncodingUnknown";
    public static final String SOAP_FAULT_VALUE_SENDER = "Sender";
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java
    // SOAP Fault Codes
    public static final String SOAP_FAULT_VALUE_VERSION_MISMATCH = "VersionMismatch";
    public static final String SOAP_FAULT_VALUE_MUST_UNDERSTAND = "MustUnderstand";
    public static final String SOAP_FAULT_VALUE_DATA_ENCODING_UKNOWN = "DataEncodingUnknown";
    public static final String SOAP_FAULT_VALUE_SENDER = "Sender";
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java
    static final QName QNAME_FAULT_SUBCODE = new QName(SOAP_ENVELOPE_NAMESPACE_URI,
                                                       SOAP_FAULT_SUB_CODE_LOCAL_NAME);
    static final QName QNAME_FAULT_TEXT = new QName(SOAP_ENVELOPE_NAMESPACE_URI,
                                                    SOAP_FAULT_TEXT_LOCAL_NAME);
}
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java
    static final QName QNAME_FAULT_SUBCODE = new QName(SOAP_ENVELOPE_NAMESPACE_URI,
                                                       SOAP_FAULT_SUB_CODE_LOCAL_NAME);
    static final QName QNAME_FAULT_TEXT = new QName(SOAP_ENVELOPE_NAMESPACE_URI,
                                                    SOAP_FAULT_TEXT_LOCAL_NAME);
}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java

public interface SOAP12Constants extends SOAPConstants {
    public String SOAP_ENVELOPE_NAMESPACE_URI =
            "http://www.w3.org/2003/05/soap-envelope";
    public String SOAP_ENCODING_NAMESPACE_URI =
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java
    static final QName QNAME_SENDER_FAULTCODE = new QName(SOAP_ENVELOPE_NAMESPACE_URI,
                                                          FAULT_CODE_SENDER);
    static final QName QNAME_RECEIVER_FAULTCODE = new QName(SOAP_ENVELOPE_NAMESPACE_URI,
                                                            FAULT_CODE_RECEIVER);

```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java
    static final QName QNAME_SENDER_FAULTCODE = new QName(SOAP_ENVELOPE_NAMESPACE_URI,
                                                          FAULT_CODE_SENDER);
    static final QName QNAME_RECEIVER_FAULTCODE = new QName(SOAP_ENVELOPE_NAMESPACE_URI,
                                                            FAULT_CODE_RECEIVER);

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java

    // SOAP Fault Reason
    public static final String SOAP_FAULT_REASON_LOCAL_NAME = "Reason";
    public static final String SOAP_FAULT_TEXT_LOCAL_NAME = "Text";
    public static final String SOAP_FAULT_TEXT_LANG_ATTR_LOCAL_NAME = "lang";
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java

    // SOAP Fault Reason
    public static final String SOAP_FAULT_REASON_LOCAL_NAME = "Reason";
    public static final String SOAP_FAULT_TEXT_LOCAL_NAME = "Text";
    public static final String SOAP_FAULT_TEXT_LANG_ATTR_LOCAL_NAME = "lang";
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java

    // SOAP Fault Reason
    public static final String SOAP_FAULT_REASON_LOCAL_NAME = "Reason";
    public static final String SOAP_FAULT_TEXT_LOCAL_NAME = "Text";
    public static final String SOAP_FAULT_TEXT_LANG_ATTR_LOCAL_NAME = "lang";
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java
    public static final String SOAP_FAULT_VALUE_MUST_UNDERSTAND = "MustUnderstand";
    public static final String SOAP_FAULT_VALUE_DATA_ENCODING_UKNOWN = "DataEncodingUnknown";
    public static final String SOAP_FAULT_VALUE_SENDER = "Sender";
    public static final String SOAP_FAULT_VALUE_RECEIVER = "Receiver";

```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java
    public static final String SOAP_FAULT_VALUE_MUST_UNDERSTAND = "MustUnderstand";
    public static final String SOAP_FAULT_VALUE_DATA_ENCODING_UKNOWN = "DataEncodingUnknown";
    public static final String SOAP_FAULT_VALUE_SENDER = "Sender";
    public static final String SOAP_FAULT_VALUE_RECEIVER = "Receiver";

```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java
    public static final String SOAP_FAULT_VALUE_MUST_UNDERSTAND = "MustUnderstand";
    public static final String SOAP_FAULT_VALUE_DATA_ENCODING_UKNOWN = "DataEncodingUnknown";
    public static final String SOAP_FAULT_VALUE_SENDER = "Sender";
    public static final String SOAP_FAULT_VALUE_RECEIVER = "Receiver";

```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java
    static final QName QNAME_FAULT_DETAIL = new QName(SOAP_ENVELOPE_NAMESPACE_URI,
                                                      SOAP_FAULT_DETAIL_LOCAL_NAME);
    static final QName QNAME_FAULT_ROLE = new QName(SOAP_ENVELOPE_NAMESPACE_URI,
                                                    SOAP_FAULT_ROLE_LOCAL_NAME);
    static final QName QNAME_FAULT_VALUE = new QName(SOAP_ENVELOPE_NAMESPACE_URI,
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java
    static final QName QNAME_FAULT_DETAIL = new QName(SOAP_ENVELOPE_NAMESPACE_URI,
                                                      SOAP_FAULT_DETAIL_LOCAL_NAME);
    static final QName QNAME_FAULT_ROLE = new QName(SOAP_ENVELOPE_NAMESPACE_URI,
                                                    SOAP_FAULT_ROLE_LOCAL_NAME);
    static final QName QNAME_FAULT_VALUE = new QName(SOAP_ENVELOPE_NAMESPACE_URI,
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java

    // SOAP Fault Node
    public static final String SOAP_FAULT_NODE_LOCAL_NAME = "Node";

    // SOAP Fault Detail
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java

    // SOAP Fault Node
    public static final String SOAP_FAULT_NODE_LOCAL_NAME = "Node";

    // SOAP Fault Detail
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java

    // SOAP Fault Node
    public static final String SOAP_FAULT_NODE_LOCAL_NAME = "Node";

    // SOAP Fault Detail
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java
    public static final String SOAP_FAULT_CODE_LOCAL_NAME = "Code";
    public static final String SOAP_FAULT_SUB_CODE_LOCAL_NAME = "Subcode";
    public static final String SOAP_FAULT_VALUE_LOCAL_NAME = "Value";

    // SOAP Fault Codes
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java
    public static final String SOAP_FAULT_CODE_LOCAL_NAME = "Code";
    public static final String SOAP_FAULT_SUB_CODE_LOCAL_NAME = "Subcode";
    public static final String SOAP_FAULT_VALUE_LOCAL_NAME = "Value";

    // SOAP Fault Codes
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java
    public static final String SOAP_FAULT_CODE_LOCAL_NAME = "Code";
    public static final String SOAP_FAULT_SUB_CODE_LOCAL_NAME = "Subcode";
    public static final String SOAP_FAULT_VALUE_LOCAL_NAME = "Value";

    // SOAP Fault Codes
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java
    // QNames
    static final QName QNAME_ROLE = new QName(SOAP_ENVELOPE_NAMESPACE_URI, SOAP_ROLE);
    static final QName QNAME_RELAY = new QName(SOAP_ENVELOPE_NAMESPACE_URI, SOAP_RELAY);
    static final QName QNAME_MU_FAULTCODE = new QName(SOAP_ENVELOPE_NAMESPACE_URI,
                                                      FAULT_CODE_MUST_UNDERSTAND);
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java
    // QNames
    static final QName QNAME_ROLE = new QName(SOAP_ENVELOPE_NAMESPACE_URI, SOAP_ROLE);
    static final QName QNAME_RELAY = new QName(SOAP_ENVELOPE_NAMESPACE_URI, SOAP_RELAY);
    static final QName QNAME_MU_FAULTCODE = new QName(SOAP_ENVELOPE_NAMESPACE_URI,
                                                      FAULT_CODE_MUST_UNDERSTAND);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java

    // SOAP Fault Detail
    public static final String SOAP_FAULT_DETAIL_LOCAL_NAME = "Detail";

    // SOAP Fault Role
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java

    // SOAP Fault Detail
    public static final String SOAP_FAULT_DETAIL_LOCAL_NAME = "Detail";

    // SOAP Fault Role
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java

    // SOAP Fault Detail
    public static final String SOAP_FAULT_DETAIL_LOCAL_NAME = "Detail";

    // SOAP Fault Role
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java
    public String SOAP_ENVELOPE_NAMESPACE_URI =
            "http://www.w3.org/2003/05/soap-envelope";
    public String SOAP_ENCODING_NAMESPACE_URI =
            "http://www.w3.org/2003/05/soap-encoding";

```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java
    static final QName QNAME_FAULT_CODE = new QName(SOAP_ENVELOPE_NAMESPACE_URI,
                                                    SOAP_FAULT_CODE_LOCAL_NAME);
    static final QName QNAME_FAULT_NODE = new QName(SOAP_ENVELOPE_NAMESPACE_URI,
                                                    SOAP_FAULT_NODE_LOCAL_NAME);
    static final QName QNAME_FAULT_DETAIL = new QName(SOAP_ENVELOPE_NAMESPACE_URI,
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java
    static final QName QNAME_FAULT_CODE = new QName(SOAP_ENVELOPE_NAMESPACE_URI,
                                                    SOAP_FAULT_CODE_LOCAL_NAME);
    static final QName QNAME_FAULT_NODE = new QName(SOAP_ENVELOPE_NAMESPACE_URI,
                                                    SOAP_FAULT_NODE_LOCAL_NAME);
    static final QName QNAME_FAULT_DETAIL = new QName(SOAP_ENVELOPE_NAMESPACE_URI,
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java
    // SOAP Fault Reason
    public static final String SOAP_FAULT_REASON_LOCAL_NAME = "Reason";
    public static final String SOAP_FAULT_TEXT_LOCAL_NAME = "Text";
    public static final String SOAP_FAULT_TEXT_LANG_ATTR_LOCAL_NAME = "lang";
    public static final String SOAP_FAULT_TEXT_LANG_ATTR_NS_URI =
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java
    // SOAP Fault Reason
    public static final String SOAP_FAULT_REASON_LOCAL_NAME = "Reason";
    public static final String SOAP_FAULT_TEXT_LOCAL_NAME = "Text";
    public static final String SOAP_FAULT_TEXT_LANG_ATTR_LOCAL_NAME = "lang";
    public static final String SOAP_FAULT_TEXT_LANG_ATTR_NS_URI =
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java
    // SOAP Fault Reason
    public static final String SOAP_FAULT_REASON_LOCAL_NAME = "Reason";
    public static final String SOAP_FAULT_TEXT_LOCAL_NAME = "Text";
    public static final String SOAP_FAULT_TEXT_LANG_ATTR_LOCAL_NAME = "lang";
    public static final String SOAP_FAULT_TEXT_LANG_ATTR_NS_URI =
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java
    public static final String FAULT_CODE_RECEIVER = "Receiver";

    public static final String SOAP_ROLE_NEXT = "http://www.w3.org/2003/05/soap-envelope/role/next";
    public static final String SOAP_ROLE_NONE = "http://www.w3.org/2003/05/soap-envelope/role/none";
    public static final String SOAP_ROLE_ULTIMATE_RECEIVER =
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java
    public static final String FAULT_CODE_RECEIVER = "Receiver";

    public static final String SOAP_ROLE_NEXT = "http://www.w3.org/2003/05/soap-envelope/role/next";
    public static final String SOAP_ROLE_NONE = "http://www.w3.org/2003/05/soap-envelope/role/none";
    public static final String SOAP_ROLE_ULTIMATE_RECEIVER =
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java
    public static final String FAULT_CODE_RECEIVER = "Receiver";

    public static final String SOAP_ROLE_NEXT = "http://www.w3.org/2003/05/soap-envelope/role/next";
    public static final String SOAP_ROLE_NONE = "http://www.w3.org/2003/05/soap-envelope/role/none";
    public static final String SOAP_ROLE_ULTIMATE_RECEIVER =
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java
    public static final String SOAP_FAULT_VALUE_VERSION_MISMATCH = "VersionMismatch";
    public static final String SOAP_FAULT_VALUE_MUST_UNDERSTAND = "MustUnderstand";
    public static final String SOAP_FAULT_VALUE_DATA_ENCODING_UKNOWN = "DataEncodingUnknown";
    public static final String SOAP_FAULT_VALUE_SENDER = "Sender";
    public static final String SOAP_FAULT_VALUE_RECEIVER = "Receiver";
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java
    public static final String SOAP_FAULT_VALUE_VERSION_MISMATCH = "VersionMismatch";
    public static final String SOAP_FAULT_VALUE_MUST_UNDERSTAND = "MustUnderstand";
    public static final String SOAP_FAULT_VALUE_DATA_ENCODING_UKNOWN = "DataEncodingUnknown";
    public static final String SOAP_FAULT_VALUE_SENDER = "Sender";
    public static final String SOAP_FAULT_VALUE_RECEIVER = "Receiver";
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java
    public static final String SOAP_FAULT_VALUE_VERSION_MISMATCH = "VersionMismatch";
    public static final String SOAP_FAULT_VALUE_MUST_UNDERSTAND = "MustUnderstand";
    public static final String SOAP_FAULT_VALUE_DATA_ENCODING_UKNOWN = "DataEncodingUnknown";
    public static final String SOAP_FAULT_VALUE_SENDER = "Sender";
    public static final String SOAP_FAULT_VALUE_RECEIVER = "Receiver";
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java
    static final QName QNAME_FAULT_NODE = new QName(SOAP_ENVELOPE_NAMESPACE_URI,
                                                    SOAP_FAULT_NODE_LOCAL_NAME);
    static final QName QNAME_FAULT_DETAIL = new QName(SOAP_ENVELOPE_NAMESPACE_URI,
                                                      SOAP_FAULT_DETAIL_LOCAL_NAME);
    static final QName QNAME_FAULT_ROLE = new QName(SOAP_ENVELOPE_NAMESPACE_URI,
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java
    static final QName QNAME_FAULT_NODE = new QName(SOAP_ENVELOPE_NAMESPACE_URI,
                                                    SOAP_FAULT_NODE_LOCAL_NAME);
    static final QName QNAME_FAULT_DETAIL = new QName(SOAP_ENVELOPE_NAMESPACE_URI,
                                                      SOAP_FAULT_DETAIL_LOCAL_NAME);
    static final QName QNAME_FAULT_ROLE = new QName(SOAP_ENVELOPE_NAMESPACE_URI,
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java
    static final QName QNAME_MU_FAULTCODE = new QName(SOAP_ENVELOPE_NAMESPACE_URI,
                                                      FAULT_CODE_MUST_UNDERSTAND);
    static final QName QNAME_SENDER_FAULTCODE = new QName(SOAP_ENVELOPE_NAMESPACE_URI,
                                                          FAULT_CODE_SENDER);
    static final QName QNAME_RECEIVER_FAULTCODE = new QName(SOAP_ENVELOPE_NAMESPACE_URI,
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java
    static final QName QNAME_MU_FAULTCODE = new QName(SOAP_ENVELOPE_NAMESPACE_URI,
                                                      FAULT_CODE_MUST_UNDERSTAND);
    static final QName QNAME_SENDER_FAULTCODE = new QName(SOAP_ENVELOPE_NAMESPACE_URI,
                                                          FAULT_CODE_SENDER);
    static final QName QNAME_RECEIVER_FAULTCODE = new QName(SOAP_ENVELOPE_NAMESPACE_URI,
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java
    static final QName QNAME_ROLE = new QName(SOAP_ENVELOPE_NAMESPACE_URI, SOAP_ROLE);
    static final QName QNAME_RELAY = new QName(SOAP_ENVELOPE_NAMESPACE_URI, SOAP_RELAY);
    static final QName QNAME_MU_FAULTCODE = new QName(SOAP_ENVELOPE_NAMESPACE_URI,
                                                      FAULT_CODE_MUST_UNDERSTAND);
    static final QName QNAME_SENDER_FAULTCODE = new QName(SOAP_ENVELOPE_NAMESPACE_URI,
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java
    static final QName QNAME_ROLE = new QName(SOAP_ENVELOPE_NAMESPACE_URI, SOAP_ROLE);
    static final QName QNAME_RELAY = new QName(SOAP_ENVELOPE_NAMESPACE_URI, SOAP_RELAY);
    static final QName QNAME_MU_FAULTCODE = new QName(SOAP_ENVELOPE_NAMESPACE_URI,
                                                      FAULT_CODE_MUST_UNDERSTAND);
    static final QName QNAME_SENDER_FAULTCODE = new QName(SOAP_ENVELOPE_NAMESPACE_URI,
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAPBody.java`
#### Snippet
```java
     * has no namespace or the body is empty
     */
    public OMNamespace getFirstElementNS();
    
    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAPBody.java`
#### Snippet
```java
     * @return local name of first element in the body, or <code>null</code> if the body is empty
     */
    public String getFirstElementLocalName();
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java

    // -------- SOAP Fault Codes ------------------------------
    public static final String FAULT_CODE_SENDER = "Sender";
    public static final String FAULT_CODE_RECEIVER = "Receiver";

```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java

    // -------- SOAP Fault Codes ------------------------------
    public static final String FAULT_CODE_SENDER = "Sender";
    public static final String FAULT_CODE_RECEIVER = "Receiver";

```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java

    // -------- SOAP Fault Codes ------------------------------
    public static final String FAULT_CODE_SENDER = "Sender";
    public static final String FAULT_CODE_RECEIVER = "Receiver";

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java

    // SOAP Fault Role
    public static final String SOAP_FAULT_ROLE_LOCAL_NAME = "Role";

    //SOAP 1.2 Content Type
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java

    // SOAP Fault Role
    public static final String SOAP_FAULT_ROLE_LOCAL_NAME = "Role";

    //SOAP 1.2 Content Type
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java

    // SOAP Fault Role
    public static final String SOAP_FAULT_ROLE_LOCAL_NAME = "Role";

    //SOAP 1.2 Content Type
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java
    static final QName QNAME_FAULT_VALUE = new QName(SOAP_ENVELOPE_NAMESPACE_URI,
                                                     SOAP_FAULT_VALUE_LOCAL_NAME);
    static final QName QNAME_FAULT_SUBCODE = new QName(SOAP_ENVELOPE_NAMESPACE_URI,
                                                       SOAP_FAULT_SUB_CODE_LOCAL_NAME);
    static final QName QNAME_FAULT_TEXT = new QName(SOAP_ENVELOPE_NAMESPACE_URI,
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java
    static final QName QNAME_FAULT_VALUE = new QName(SOAP_ENVELOPE_NAMESPACE_URI,
                                                     SOAP_FAULT_VALUE_LOCAL_NAME);
    static final QName QNAME_FAULT_SUBCODE = new QName(SOAP_ENVELOPE_NAMESPACE_URI,
                                                       SOAP_FAULT_SUB_CODE_LOCAL_NAME);
    static final QName QNAME_FAULT_TEXT = new QName(SOAP_ENVELOPE_NAMESPACE_URI,
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java
     * @deprecated Use {@link SOAPVersion#getMediaType()} instead.
     */
    public static final String SOAP_12_CONTENT_TYPE = "application/soap+xml";

    // -------- SOAP Fault Codes ------------------------------
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java
     * @deprecated Use {@link SOAPVersion#getMediaType()} instead.
     */
    public static final String SOAP_12_CONTENT_TYPE = "application/soap+xml";

    // -------- SOAP Fault Codes ------------------------------
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java
     * @deprecated Use {@link SOAPVersion#getMediaType()} instead.
     */
    public static final String SOAP_12_CONTENT_TYPE = "application/soap+xml";

    // -------- SOAP Fault Codes ------------------------------
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java

    public static final String SOAP_ROLE_NEXT = "http://www.w3.org/2003/05/soap-envelope/role/next";
    public static final String SOAP_ROLE_NONE = "http://www.w3.org/2003/05/soap-envelope/role/none";
    public static final String SOAP_ROLE_ULTIMATE_RECEIVER =
            "http://www.w3.org/2003/05/soap-envelope/role/ultimateReceiver";
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java

    public static final String SOAP_ROLE_NEXT = "http://www.w3.org/2003/05/soap-envelope/role/next";
    public static final String SOAP_ROLE_NONE = "http://www.w3.org/2003/05/soap-envelope/role/none";
    public static final String SOAP_ROLE_ULTIMATE_RECEIVER =
            "http://www.w3.org/2003/05/soap-envelope/role/ultimateReceiver";
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java

    public static final String SOAP_ROLE_NEXT = "http://www.w3.org/2003/05/soap-envelope/role/next";
    public static final String SOAP_ROLE_NONE = "http://www.w3.org/2003/05/soap-envelope/role/none";
    public static final String SOAP_ROLE_ULTIMATE_RECEIVER =
            "http://www.w3.org/2003/05/soap-envelope/role/ultimateReceiver";
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java

    // SOAP Fault Code
    public static final String SOAP_FAULT_CODE_LOCAL_NAME = "Code";
    public static final String SOAP_FAULT_SUB_CODE_LOCAL_NAME = "Subcode";
    public static final String SOAP_FAULT_VALUE_LOCAL_NAME = "Value";
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java

    // SOAP Fault Code
    public static final String SOAP_FAULT_CODE_LOCAL_NAME = "Code";
    public static final String SOAP_FAULT_SUB_CODE_LOCAL_NAME = "Subcode";
    public static final String SOAP_FAULT_VALUE_LOCAL_NAME = "Value";
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java

    // SOAP Fault Code
    public static final String SOAP_FAULT_CODE_LOCAL_NAME = "Code";
    public static final String SOAP_FAULT_SUB_CODE_LOCAL_NAME = "Subcode";
    public static final String SOAP_FAULT_VALUE_LOCAL_NAME = "Value";
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java
    public static final String SOAP_FAULT_TEXT_LANG_ATTR_NS_URI =
            "http://www.w3.org/XML/1998/namespace";
    public static final String SOAP_FAULT_TEXT_LANG_ATTR_NS_PREFIX = "xml";

    // SOAP Fault Node
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java
    public static final String SOAP_FAULT_TEXT_LANG_ATTR_NS_URI =
            "http://www.w3.org/XML/1998/namespace";
    public static final String SOAP_FAULT_TEXT_LANG_ATTR_NS_PREFIX = "xml";

    // SOAP Fault Node
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java
    public static final String SOAP_FAULT_TEXT_LANG_ATTR_NS_URI =
            "http://www.w3.org/XML/1998/namespace";
    public static final String SOAP_FAULT_TEXT_LANG_ATTR_NS_PREFIX = "xml";

    // SOAP Fault Node
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java
    static final QName QNAME_FAULT_ROLE = new QName(SOAP_ENVELOPE_NAMESPACE_URI,
                                                    SOAP_FAULT_ROLE_LOCAL_NAME);
    static final QName QNAME_FAULT_VALUE = new QName(SOAP_ENVELOPE_NAMESPACE_URI,
                                                     SOAP_FAULT_VALUE_LOCAL_NAME);
    static final QName QNAME_FAULT_SUBCODE = new QName(SOAP_ENVELOPE_NAMESPACE_URI,
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java
    static final QName QNAME_FAULT_ROLE = new QName(SOAP_ENVELOPE_NAMESPACE_URI,
                                                    SOAP_FAULT_ROLE_LOCAL_NAME);
    static final QName QNAME_FAULT_VALUE = new QName(SOAP_ENVELOPE_NAMESPACE_URI,
                                                     SOAP_FAULT_VALUE_LOCAL_NAME);
    static final QName QNAME_FAULT_SUBCODE = new QName(SOAP_ENVELOPE_NAMESPACE_URI,
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java
    public static final String SOAP_ROLE_NEXT = "http://www.w3.org/2003/05/soap-envelope/role/next";
    public static final String SOAP_ROLE_NONE = "http://www.w3.org/2003/05/soap-envelope/role/none";
    public static final String SOAP_ROLE_ULTIMATE_RECEIVER =
            "http://www.w3.org/2003/05/soap-envelope/role/ultimateReceiver";

```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java
    public static final String SOAP_ROLE_NEXT = "http://www.w3.org/2003/05/soap-envelope/role/next";
    public static final String SOAP_ROLE_NONE = "http://www.w3.org/2003/05/soap-envelope/role/none";
    public static final String SOAP_ROLE_ULTIMATE_RECEIVER =
            "http://www.w3.org/2003/05/soap-envelope/role/ultimateReceiver";

```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java
    public static final String SOAP_ROLE_NEXT = "http://www.w3.org/2003/05/soap-envelope/role/next";
    public static final String SOAP_ROLE_NONE = "http://www.w3.org/2003/05/soap-envelope/role/none";
    public static final String SOAP_ROLE_ULTIMATE_RECEIVER =
            "http://www.w3.org/2003/05/soap-envelope/role/ultimateReceiver";

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java
    public static final String SOAP_FAULT_TEXT_LOCAL_NAME = "Text";
    public static final String SOAP_FAULT_TEXT_LANG_ATTR_LOCAL_NAME = "lang";
    public static final String SOAP_FAULT_TEXT_LANG_ATTR_NS_URI =
            "http://www.w3.org/XML/1998/namespace";
    public static final String SOAP_FAULT_TEXT_LANG_ATTR_NS_PREFIX = "xml";
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java
    public static final String SOAP_FAULT_TEXT_LOCAL_NAME = "Text";
    public static final String SOAP_FAULT_TEXT_LANG_ATTR_LOCAL_NAME = "lang";
    public static final String SOAP_FAULT_TEXT_LANG_ATTR_NS_URI =
            "http://www.w3.org/XML/1998/namespace";
    public static final String SOAP_FAULT_TEXT_LANG_ATTR_NS_PREFIX = "xml";
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java
    public static final String SOAP_FAULT_TEXT_LOCAL_NAME = "Text";
    public static final String SOAP_FAULT_TEXT_LANG_ATTR_LOCAL_NAME = "lang";
    public static final String SOAP_FAULT_TEXT_LANG_ATTR_NS_URI =
            "http://www.w3.org/XML/1998/namespace";
    public static final String SOAP_FAULT_TEXT_LANG_ATTR_NS_PREFIX = "xml";
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java
            "http://www.w3.org/2003/05/soap-encoding";

    public static final String SOAP_ROLE = "role";
    public static final String SOAP_RELAY = "relay";

```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java
            "http://www.w3.org/2003/05/soap-encoding";

    public static final String SOAP_ROLE = "role";
    public static final String SOAP_RELAY = "relay";

```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java
            "http://www.w3.org/2003/05/soap-encoding";

    public static final String SOAP_ROLE = "role";
    public static final String SOAP_RELAY = "relay";

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java
    public static final String SOAP_FAULT_VALUE_DATA_ENCODING_UKNOWN = "DataEncodingUnknown";
    public static final String SOAP_FAULT_VALUE_SENDER = "Sender";
    public static final String SOAP_FAULT_VALUE_RECEIVER = "Receiver";

    // SOAP Fault Reason
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java
    public static final String SOAP_FAULT_VALUE_DATA_ENCODING_UKNOWN = "DataEncodingUnknown";
    public static final String SOAP_FAULT_VALUE_SENDER = "Sender";
    public static final String SOAP_FAULT_VALUE_RECEIVER = "Receiver";

    // SOAP Fault Reason
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java
    public static final String SOAP_FAULT_VALUE_DATA_ENCODING_UKNOWN = "DataEncodingUnknown";
    public static final String SOAP_FAULT_VALUE_SENDER = "Sender";
    public static final String SOAP_FAULT_VALUE_RECEIVER = "Receiver";

    // SOAP Fault Reason
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java
    // SOAP Fault Code
    public static final String SOAP_FAULT_CODE_LOCAL_NAME = "Code";
    public static final String SOAP_FAULT_SUB_CODE_LOCAL_NAME = "Subcode";
    public static final String SOAP_FAULT_VALUE_LOCAL_NAME = "Value";

```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java
    // SOAP Fault Code
    public static final String SOAP_FAULT_CODE_LOCAL_NAME = "Code";
    public static final String SOAP_FAULT_SUB_CODE_LOCAL_NAME = "Subcode";
    public static final String SOAP_FAULT_VALUE_LOCAL_NAME = "Value";

```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java
    // SOAP Fault Code
    public static final String SOAP_FAULT_CODE_LOCAL_NAME = "Code";
    public static final String SOAP_FAULT_SUB_CODE_LOCAL_NAME = "Subcode";
    public static final String SOAP_FAULT_VALUE_LOCAL_NAME = "Value";

```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java
    static final QName QNAME_FAULT_REASON = new QName(SOAP_ENVELOPE_NAMESPACE_URI,
                                                      SOAP_FAULT_REASON_LOCAL_NAME);
    static final QName QNAME_FAULT_CODE = new QName(SOAP_ENVELOPE_NAMESPACE_URI,
                                                    SOAP_FAULT_CODE_LOCAL_NAME);
    static final QName QNAME_FAULT_NODE = new QName(SOAP_ENVELOPE_NAMESPACE_URI,
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java
    static final QName QNAME_FAULT_REASON = new QName(SOAP_ENVELOPE_NAMESPACE_URI,
                                                      SOAP_FAULT_REASON_LOCAL_NAME);
    static final QName QNAME_FAULT_CODE = new QName(SOAP_ENVELOPE_NAMESPACE_URI,
                                                    SOAP_FAULT_CODE_LOCAL_NAME);
    static final QName QNAME_FAULT_NODE = new QName(SOAP_ENVELOPE_NAMESPACE_URI,
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java

    // SOAP Fault Codes
    public static final String SOAP_FAULT_VALUE_VERSION_MISMATCH = "VersionMismatch";
    public static final String SOAP_FAULT_VALUE_MUST_UNDERSTAND = "MustUnderstand";
    public static final String SOAP_FAULT_VALUE_DATA_ENCODING_UKNOWN = "DataEncodingUnknown";
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java

    // SOAP Fault Codes
    public static final String SOAP_FAULT_VALUE_VERSION_MISMATCH = "VersionMismatch";
    public static final String SOAP_FAULT_VALUE_MUST_UNDERSTAND = "MustUnderstand";
    public static final String SOAP_FAULT_VALUE_DATA_ENCODING_UKNOWN = "DataEncodingUnknown";
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java

    // SOAP Fault Codes
    public static final String SOAP_FAULT_VALUE_VERSION_MISMATCH = "VersionMismatch";
    public static final String SOAP_FAULT_VALUE_MUST_UNDERSTAND = "MustUnderstand";
    public static final String SOAP_FAULT_VALUE_DATA_ENCODING_UKNOWN = "DataEncodingUnknown";
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java
    // -------- SOAP Fault Codes ------------------------------
    public static final String FAULT_CODE_SENDER = "Sender";
    public static final String FAULT_CODE_RECEIVER = "Receiver";

    public static final String SOAP_ROLE_NEXT = "http://www.w3.org/2003/05/soap-envelope/role/next";
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java
    // -------- SOAP Fault Codes ------------------------------
    public static final String FAULT_CODE_SENDER = "Sender";
    public static final String FAULT_CODE_RECEIVER = "Receiver";

    public static final String SOAP_ROLE_NEXT = "http://www.w3.org/2003/05/soap-envelope/role/next";
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP12Constants.java`
#### Snippet
```java
    // -------- SOAP Fault Codes ------------------------------
    public static final String FAULT_CODE_SENDER = "Sender";
    public static final String FAULT_CODE_RECEIVER = "Receiver";

    public static final String SOAP_ROLE_NEXT = "http://www.w3.org/2003/05/soap-envelope/role/next";
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP11Constants.java`
#### Snippet
```java
    static final String SOAP_FAULT_ACTOR_LOCAL_NAME = "faultactor";

    static final String SOAP_FAULT_DETAIL_LOCAL_NAME = "detail";

    //SOAP 1.2 Content Type
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP11Constants.java`
#### Snippet
```java
    static final String SOAP_FAULT_ACTOR_LOCAL_NAME = "faultactor";

    static final String SOAP_FAULT_DETAIL_LOCAL_NAME = "detail";

    //SOAP 1.2 Content Type
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP11Constants.java`
#### Snippet
```java

    /** Field SOAP_FAULT_CODE_LOCAL_NAME */
    static final String SOAP_FAULT_CODE_LOCAL_NAME = "faultcode";
    /** Field SOAP_FAULT_STRING_LOCAL_NAME */
    static final String SOAP_FAULT_STRING_LOCAL_NAME = "faultstring";
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP11Constants.java`
#### Snippet
```java

    /** Field SOAP_FAULT_CODE_LOCAL_NAME */
    static final String SOAP_FAULT_CODE_LOCAL_NAME = "faultcode";
    /** Field SOAP_FAULT_STRING_LOCAL_NAME */
    static final String SOAP_FAULT_STRING_LOCAL_NAME = "faultstring";
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP11Constants.java`
#### Snippet
```java
    static final QName QNAME_FAULT_CODE = new QName(SOAP_FAULT_CODE_LOCAL_NAME);
    static final QName QNAME_FAULT_DETAIL = new QName(SOAP_FAULT_DETAIL_LOCAL_NAME);
    static final QName QNAME_FAULT_ROLE = new QName(SOAP_FAULT_ACTOR_LOCAL_NAME);
}

```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP11Constants.java`
#### Snippet
```java
    static final QName QNAME_FAULT_CODE = new QName(SOAP_FAULT_CODE_LOCAL_NAME);
    static final QName QNAME_FAULT_DETAIL = new QName(SOAP_FAULT_DETAIL_LOCAL_NAME);
    static final QName QNAME_FAULT_ROLE = new QName(SOAP_FAULT_ACTOR_LOCAL_NAME);
}

```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP11Constants.java`
#### Snippet
```java
    static final QName QNAME_SENDER_FAULTCODE = new QName(SOAP_ENVELOPE_NAMESPACE_URI,
                                                          FAULT_CODE_SENDER);
    static final QName QNAME_RECEIVER_FAULTCODE = new QName(SOAP_ENVELOPE_NAMESPACE_URI,
                                                            FAULT_CODE_RECEIVER);

```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP11Constants.java`
#### Snippet
```java
    static final QName QNAME_SENDER_FAULTCODE = new QName(SOAP_ENVELOPE_NAMESPACE_URI,
                                                          FAULT_CODE_SENDER);
    static final QName QNAME_RECEIVER_FAULTCODE = new QName(SOAP_ENVELOPE_NAMESPACE_URI,
                                                            FAULT_CODE_RECEIVER);

```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP11Constants.java`
#### Snippet
```java
    static final String SOAP_FAULT_STRING_LOCAL_NAME = "faultstring";
    /** Field SOAP_FAULT_ACTOR_LOCAL_NAME */
    static final String SOAP_FAULT_ACTOR_LOCAL_NAME = "faultactor";

    static final String SOAP_FAULT_DETAIL_LOCAL_NAME = "detail";
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP11Constants.java`
#### Snippet
```java
    static final String SOAP_FAULT_STRING_LOCAL_NAME = "faultstring";
    /** Field SOAP_FAULT_ACTOR_LOCAL_NAME */
    static final String SOAP_FAULT_ACTOR_LOCAL_NAME = "faultactor";

    static final String SOAP_FAULT_DETAIL_LOCAL_NAME = "detail";
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP11Constants.java`
#### Snippet
```java
    static final String SOAP_ENVELOPE_NAMESPACE_URI =
            "http://schemas.xmlsoap.org/soap/envelope/";
    static final String SOAP_ENCODING_NAMESPACE_URI =
            "http://schemas.xmlsoap.org/soap/encoding/";

```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP11Constants.java`
#### Snippet
```java
    static final String SOAP_ENVELOPE_NAMESPACE_URI =
            "http://schemas.xmlsoap.org/soap/envelope/";
    static final String SOAP_ENCODING_NAMESPACE_URI =
            "http://schemas.xmlsoap.org/soap/encoding/";

```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP11Constants.java`
#### Snippet
```java
    static final String SOAP_FAULT_CODE_LOCAL_NAME = "faultcode";
    /** Field SOAP_FAULT_STRING_LOCAL_NAME */
    static final String SOAP_FAULT_STRING_LOCAL_NAME = "faultstring";
    /** Field SOAP_FAULT_ACTOR_LOCAL_NAME */
    static final String SOAP_FAULT_ACTOR_LOCAL_NAME = "faultactor";
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP11Constants.java`
#### Snippet
```java
    static final String SOAP_FAULT_CODE_LOCAL_NAME = "faultcode";
    /** Field SOAP_FAULT_STRING_LOCAL_NAME */
    static final String SOAP_FAULT_STRING_LOCAL_NAME = "faultstring";
    /** Field SOAP_FAULT_ACTOR_LOCAL_NAME */
    static final String SOAP_FAULT_ACTOR_LOCAL_NAME = "faultactor";
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP11Constants.java`
#### Snippet
```java
    static final QName QNAME_FAULT_REASON = new QName(SOAP_FAULT_STRING_LOCAL_NAME);
    static final QName QNAME_FAULT_CODE = new QName(SOAP_FAULT_CODE_LOCAL_NAME);
    static final QName QNAME_FAULT_DETAIL = new QName(SOAP_FAULT_DETAIL_LOCAL_NAME);
    static final QName QNAME_FAULT_ROLE = new QName(SOAP_FAULT_ACTOR_LOCAL_NAME);
}
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP11Constants.java`
#### Snippet
```java
    static final QName QNAME_FAULT_REASON = new QName(SOAP_FAULT_STRING_LOCAL_NAME);
    static final QName QNAME_FAULT_CODE = new QName(SOAP_FAULT_CODE_LOCAL_NAME);
    static final QName QNAME_FAULT_DETAIL = new QName(SOAP_FAULT_DETAIL_LOCAL_NAME);
    static final QName QNAME_FAULT_ROLE = new QName(SOAP_FAULT_ACTOR_LOCAL_NAME);
}
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP11Constants.java`
#### Snippet
```java

    // QNames
    static final QName QNAME_ACTOR = new QName(SOAP_ENVELOPE_NAMESPACE_URI, ATTR_ACTOR);
    
    static final QName QNAME_MU_FAULTCODE = new QName(SOAP_ENVELOPE_NAMESPACE_URI,
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP11Constants.java`
#### Snippet
```java

    // QNames
    static final QName QNAME_ACTOR = new QName(SOAP_ENVELOPE_NAMESPACE_URI, ATTR_ACTOR);
    
    static final QName QNAME_MU_FAULTCODE = new QName(SOAP_ENVELOPE_NAMESPACE_URI,
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP11Constants.java`
#### Snippet
```java

    /** Field ATTR_ACTOR */
    static final String ATTR_ACTOR = "actor";

    /** Field SOAP_FAULT_CODE_LOCAL_NAME */
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP11Constants.java`
#### Snippet
```java

    /** Field ATTR_ACTOR */
    static final String ATTR_ACTOR = "actor";

    /** Field SOAP_FAULT_CODE_LOCAL_NAME */
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP11Constants.java`
#### Snippet
```java

    // -------- SOAP Fault Codes ------------------------------
    static final String FAULT_CODE_SENDER = "Client";
    static final String FAULT_CODE_RECEIVER = "Server";

```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP11Constants.java`
#### Snippet
```java

    // -------- SOAP Fault Codes ------------------------------
    static final String FAULT_CODE_SENDER = "Client";
    static final String FAULT_CODE_RECEIVER = "Server";

```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP11Constants.java`
#### Snippet
```java
                                                            FAULT_CODE_RECEIVER);

    static final QName QNAME_FAULT_REASON = new QName(SOAP_FAULT_STRING_LOCAL_NAME);
    static final QName QNAME_FAULT_CODE = new QName(SOAP_FAULT_CODE_LOCAL_NAME);
    static final QName QNAME_FAULT_DETAIL = new QName(SOAP_FAULT_DETAIL_LOCAL_NAME);
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP11Constants.java`
#### Snippet
```java
                                                            FAULT_CODE_RECEIVER);

    static final QName QNAME_FAULT_REASON = new QName(SOAP_FAULT_STRING_LOCAL_NAME);
    static final QName QNAME_FAULT_CODE = new QName(SOAP_FAULT_CODE_LOCAL_NAME);
    static final QName QNAME_FAULT_DETAIL = new QName(SOAP_FAULT_DETAIL_LOCAL_NAME);
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP11Constants.java`
#### Snippet
```java
    static final QName QNAME_ACTOR = new QName(SOAP_ENVELOPE_NAMESPACE_URI, ATTR_ACTOR);
    
    static final QName QNAME_MU_FAULTCODE = new QName(SOAP_ENVELOPE_NAMESPACE_URI,
                                                      FAULT_CODE_MUST_UNDERSTAND);
    static final QName QNAME_SENDER_FAULTCODE = new QName(SOAP_ENVELOPE_NAMESPACE_URI,
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP11Constants.java`
#### Snippet
```java
    static final QName QNAME_ACTOR = new QName(SOAP_ENVELOPE_NAMESPACE_URI, ATTR_ACTOR);
    
    static final QName QNAME_MU_FAULTCODE = new QName(SOAP_ENVELOPE_NAMESPACE_URI,
                                                      FAULT_CODE_MUST_UNDERSTAND);
    static final QName QNAME_SENDER_FAULTCODE = new QName(SOAP_ENVELOPE_NAMESPACE_URI,
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP11Constants.java`
#### Snippet
```java
    // -------- SOAP Fault Codes ------------------------------
    static final String FAULT_CODE_SENDER = "Client";
    static final String FAULT_CODE_RECEIVER = "Server";

    static final String SOAP_ACTOR_NEXT = "http://schemas.xmlsoap.org/soap/actor/next";
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP11Constants.java`
#### Snippet
```java
    // -------- SOAP Fault Codes ------------------------------
    static final String FAULT_CODE_SENDER = "Client";
    static final String FAULT_CODE_RECEIVER = "Server";

    static final String SOAP_ACTOR_NEXT = "http://schemas.xmlsoap.org/soap/actor/next";
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP11Constants.java`
#### Snippet
```java
     * @deprecated Use {@link SOAPVersion#getMediaType()} instead.
     */
    static final String SOAP_11_CONTENT_TYPE = "text/xml";

    // -------- SOAP Fault Codes ------------------------------
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP11Constants.java`
#### Snippet
```java
     * @deprecated Use {@link SOAPVersion#getMediaType()} instead.
     */
    static final String SOAP_11_CONTENT_TYPE = "text/xml";

    // -------- SOAP Fault Codes ------------------------------
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP11Constants.java`
#### Snippet
```java

public interface SOAP11Constants extends SOAPConstants {
    static final String SOAP_ENVELOPE_NAMESPACE_URI =
            "http://schemas.xmlsoap.org/soap/envelope/";
    static final String SOAP_ENCODING_NAMESPACE_URI =
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP11Constants.java`
#### Snippet
```java

public interface SOAP11Constants extends SOAPConstants {
    static final String SOAP_ENVELOPE_NAMESPACE_URI =
            "http://schemas.xmlsoap.org/soap/envelope/";
    static final String SOAP_ENCODING_NAMESPACE_URI =
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP11Constants.java`
#### Snippet
```java

    static final QName QNAME_FAULT_REASON = new QName(SOAP_FAULT_STRING_LOCAL_NAME);
    static final QName QNAME_FAULT_CODE = new QName(SOAP_FAULT_CODE_LOCAL_NAME);
    static final QName QNAME_FAULT_DETAIL = new QName(SOAP_FAULT_DETAIL_LOCAL_NAME);
    static final QName QNAME_FAULT_ROLE = new QName(SOAP_FAULT_ACTOR_LOCAL_NAME);
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP11Constants.java`
#### Snippet
```java

    static final QName QNAME_FAULT_REASON = new QName(SOAP_FAULT_STRING_LOCAL_NAME);
    static final QName QNAME_FAULT_CODE = new QName(SOAP_FAULT_CODE_LOCAL_NAME);
    static final QName QNAME_FAULT_DETAIL = new QName(SOAP_FAULT_DETAIL_LOCAL_NAME);
    static final QName QNAME_FAULT_ROLE = new QName(SOAP_FAULT_ACTOR_LOCAL_NAME);
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP11Constants.java`
#### Snippet
```java
    static final QName QNAME_MU_FAULTCODE = new QName(SOAP_ENVELOPE_NAMESPACE_URI,
                                                      FAULT_CODE_MUST_UNDERSTAND);
    static final QName QNAME_SENDER_FAULTCODE = new QName(SOAP_ENVELOPE_NAMESPACE_URI,
                                                          FAULT_CODE_SENDER);
    static final QName QNAME_RECEIVER_FAULTCODE = new QName(SOAP_ENVELOPE_NAMESPACE_URI,
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP11Constants.java`
#### Snippet
```java
    static final QName QNAME_MU_FAULTCODE = new QName(SOAP_ENVELOPE_NAMESPACE_URI,
                                                      FAULT_CODE_MUST_UNDERSTAND);
    static final QName QNAME_SENDER_FAULTCODE = new QName(SOAP_ENVELOPE_NAMESPACE_URI,
                                                          FAULT_CODE_SENDER);
    static final QName QNAME_RECEIVER_FAULTCODE = new QName(SOAP_ENVELOPE_NAMESPACE_URI,
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP11Constants.java`
#### Snippet
```java
    static final String FAULT_CODE_RECEIVER = "Server";

    static final String SOAP_ACTOR_NEXT = "http://schemas.xmlsoap.org/soap/actor/next";

    // QNames
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAP11Constants.java`
#### Snippet
```java
    static final String FAULT_CODE_RECEIVER = "Server";

    static final String SOAP_ACTOR_NEXT = "http://schemas.xmlsoap.org/soap/actor/next";

    // QNames
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAPConstants.java`
#### Snippet
```java
    static final String BODY_LOCAL_NAME = "Body";
    /** Field BODY_NAMESPACE_PREFIX */
    static final String BODY_NAMESPACE_PREFIX =
            SOAP_DEFAULT_NAMESPACE_PREFIX;
    /** Field BODY_FAULT_LOCAL_NAME */
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAPConstants.java`
#### Snippet
```java
    static final String BODY_LOCAL_NAME = "Body";
    /** Field BODY_NAMESPACE_PREFIX */
    static final String BODY_NAMESPACE_PREFIX =
            SOAP_DEFAULT_NAMESPACE_PREFIX;
    /** Field BODY_FAULT_LOCAL_NAME */
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAPConstants.java`
#### Snippet
```java

    /** Field BODY_LOCAL_NAME */
    static final String BODY_LOCAL_NAME = "Body";
    /** Field BODY_NAMESPACE_PREFIX */
    static final String BODY_NAMESPACE_PREFIX =
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAPConstants.java`
#### Snippet
```java

    /** Field BODY_LOCAL_NAME */
    static final String BODY_LOCAL_NAME = "Body";
    /** Field BODY_NAMESPACE_PREFIX */
    static final String BODY_NAMESPACE_PREFIX =
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAPConstants.java`
#### Snippet
```java
    static final String SOAP_DEFAULT_NAMESPACE_PREFIX = "soapenv";
    /** Field SOAPENVELOPE_LOCAL_NAME */
    static final String SOAPENVELOPE_LOCAL_NAME = "Envelope";

    /** Field HEADER_LOCAL_NAME */
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAPConstants.java`
#### Snippet
```java
    static final String SOAP_DEFAULT_NAMESPACE_PREFIX = "soapenv";
    /** Field SOAPENVELOPE_LOCAL_NAME */
    static final String SOAPENVELOPE_LOCAL_NAME = "Envelope";

    /** Field HEADER_LOCAL_NAME */
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAPConstants.java`
#### Snippet
```java
public interface SOAPConstants {
    /** Field SOAP_DEFAULT_NAMESPACE_PREFIX */
    static final String SOAP_DEFAULT_NAMESPACE_PREFIX = "soapenv";
    /** Field SOAPENVELOPE_LOCAL_NAME */
    static final String SOAPENVELOPE_LOCAL_NAME = "Envelope";
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAPConstants.java`
#### Snippet
```java
public interface SOAPConstants {
    /** Field SOAP_DEFAULT_NAMESPACE_PREFIX */
    static final String SOAP_DEFAULT_NAMESPACE_PREFIX = "soapenv";
    /** Field SOAPENVELOPE_LOCAL_NAME */
    static final String SOAPENVELOPE_LOCAL_NAME = "Envelope";
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAPConstants.java`
#### Snippet
```java

    // -------- SOAP Fault Codes ------------------------------
    static final String FAULT_CODE_VERSION_MISMATCH = "VersionMismatch";
    static final String FAULT_CODE_MUST_UNDERSTAND = "MustUnderstand";
    static final String FAULT_CODE_DATA_ENCODING_UNKNOWN = "DataEncodingUnknown";
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAPConstants.java`
#### Snippet
```java

    // -------- SOAP Fault Codes ------------------------------
    static final String FAULT_CODE_VERSION_MISMATCH = "VersionMismatch";
    static final String FAULT_CODE_MUST_UNDERSTAND = "MustUnderstand";
    static final String FAULT_CODE_DATA_ENCODING_UNKNOWN = "DataEncodingUnknown";
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAPConstants.java`
#### Snippet
```java
    static final String ATTR_MUSTUNDERSTAND_1 = "1";
    /** Field SOAPFAULT_LOCAL_NAME */
    static final String SOAPFAULT_LOCAL_NAME = "Fault";
    /** Field SOAPFAULT_DETAIL_LOCAL_NAME */
    static final String SOAPFAULT_DETAIL_LOCAL_NAME = "detail";
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAPConstants.java`
#### Snippet
```java
    static final String ATTR_MUSTUNDERSTAND_1 = "1";
    /** Field SOAPFAULT_LOCAL_NAME */
    static final String SOAPFAULT_LOCAL_NAME = "Fault";
    /** Field SOAPFAULT_DETAIL_LOCAL_NAME */
    static final String SOAPFAULT_DETAIL_LOCAL_NAME = "detail";
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAPConstants.java`
#### Snippet
```java
            SOAP_DEFAULT_NAMESPACE_PREFIX;
    /** Field BODY_FAULT_LOCAL_NAME */
    static final String BODY_FAULT_LOCAL_NAME = "Fault";

    /** Field ATTR_MUSTUNDERSTAND */
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAPConstants.java`
#### Snippet
```java
            SOAP_DEFAULT_NAMESPACE_PREFIX;
    /** Field BODY_FAULT_LOCAL_NAME */
    static final String BODY_FAULT_LOCAL_NAME = "Fault";

    /** Field ATTR_MUSTUNDERSTAND */
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAPConstants.java`
#### Snippet
```java
    static final String ATTR_MUSTUNDERSTAND_TRUE = "true";
    static final String ATTR_MUSTUNDERSTAND_FALSE = "false";
    static final String ATTR_MUSTUNDERSTAND_0 = "0";
    static final String ATTR_MUSTUNDERSTAND_1 = "1";
    /** Field SOAPFAULT_LOCAL_NAME */
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAPConstants.java`
#### Snippet
```java
    static final String ATTR_MUSTUNDERSTAND_TRUE = "true";
    static final String ATTR_MUSTUNDERSTAND_FALSE = "false";
    static final String ATTR_MUSTUNDERSTAND_0 = "0";
    static final String ATTR_MUSTUNDERSTAND_1 = "1";
    /** Field SOAPFAULT_LOCAL_NAME */
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAPConstants.java`
#### Snippet
```java
    static final String SOAPFAULT_LOCAL_NAME = "Fault";
    /** Field SOAPFAULT_DETAIL_LOCAL_NAME */
    static final String SOAPFAULT_DETAIL_LOCAL_NAME = "detail";

    static final String SOAP_FAULT_DETAIL_EXCEPTION_ENTRY = "Exception";
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAPConstants.java`
#### Snippet
```java
    static final String SOAPFAULT_LOCAL_NAME = "Fault";
    /** Field SOAPFAULT_DETAIL_LOCAL_NAME */
    static final String SOAPFAULT_DETAIL_LOCAL_NAME = "detail";

    static final String SOAP_FAULT_DETAIL_EXCEPTION_ENTRY = "Exception";
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAPConstants.java`
#### Snippet
```java

    // Followings are different in SOAP 1.1 and 1.2 specifications
    static final String FAULT_CODE_SENDER = "";
    static final String FAULT_CODE_RECEIVER = "";
}
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAPConstants.java`
#### Snippet
```java

    // Followings are different in SOAP 1.1 and 1.2 specifications
    static final String FAULT_CODE_SENDER = "";
    static final String FAULT_CODE_RECEIVER = "";
}
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAPConstants.java`
#### Snippet
```java
    // -------- SOAP Fault Codes ------------------------------
    static final String FAULT_CODE_VERSION_MISMATCH = "VersionMismatch";
    static final String FAULT_CODE_MUST_UNDERSTAND = "MustUnderstand";
    static final String FAULT_CODE_DATA_ENCODING_UNKNOWN = "DataEncodingUnknown";

```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAPConstants.java`
#### Snippet
```java
    // -------- SOAP Fault Codes ------------------------------
    static final String FAULT_CODE_VERSION_MISMATCH = "VersionMismatch";
    static final String FAULT_CODE_MUST_UNDERSTAND = "MustUnderstand";
    static final String FAULT_CODE_DATA_ENCODING_UNKNOWN = "DataEncodingUnknown";

```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAPConstants.java`
#### Snippet
```java
    static final String ATTR_MUSTUNDERSTAND_FALSE = "false";
    static final String ATTR_MUSTUNDERSTAND_0 = "0";
    static final String ATTR_MUSTUNDERSTAND_1 = "1";
    /** Field SOAPFAULT_LOCAL_NAME */
    static final String SOAPFAULT_LOCAL_NAME = "Fault";
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAPConstants.java`
#### Snippet
```java
    static final String ATTR_MUSTUNDERSTAND_FALSE = "false";
    static final String ATTR_MUSTUNDERSTAND_0 = "0";
    static final String ATTR_MUSTUNDERSTAND_1 = "1";
    /** Field SOAPFAULT_LOCAL_NAME */
    static final String SOAPFAULT_LOCAL_NAME = "Fault";
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAPConstants.java`
#### Snippet
```java
    static final String ATTR_MUSTUNDERSTAND = "mustUnderstand";
    static final String ATTR_MUSTUNDERSTAND_TRUE = "true";
    static final String ATTR_MUSTUNDERSTAND_FALSE = "false";
    static final String ATTR_MUSTUNDERSTAND_0 = "0";
    static final String ATTR_MUSTUNDERSTAND_1 = "1";
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAPConstants.java`
#### Snippet
```java
    static final String ATTR_MUSTUNDERSTAND = "mustUnderstand";
    static final String ATTR_MUSTUNDERSTAND_TRUE = "true";
    static final String ATTR_MUSTUNDERSTAND_FALSE = "false";
    static final String ATTR_MUSTUNDERSTAND_0 = "0";
    static final String ATTR_MUSTUNDERSTAND_1 = "1";
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAPConstants.java`
#### Snippet
```java
    static final String SOAPFAULT_DETAIL_LOCAL_NAME = "detail";

    static final String SOAP_FAULT_DETAIL_EXCEPTION_ENTRY = "Exception";

    // -------- SOAP Fault Codes ------------------------------
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAPConstants.java`
#### Snippet
```java
    static final String SOAPFAULT_DETAIL_LOCAL_NAME = "detail";

    static final String SOAP_FAULT_DETAIL_EXCEPTION_ENTRY = "Exception";

    // -------- SOAP Fault Codes ------------------------------
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAPConstants.java`
#### Snippet
```java
    static final String FAULT_CODE_VERSION_MISMATCH = "VersionMismatch";
    static final String FAULT_CODE_MUST_UNDERSTAND = "MustUnderstand";
    static final String FAULT_CODE_DATA_ENCODING_UNKNOWN = "DataEncodingUnknown";

    // Followings are different in SOAP 1.1 and 1.2 specifications
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAPConstants.java`
#### Snippet
```java
    static final String FAULT_CODE_VERSION_MISMATCH = "VersionMismatch";
    static final String FAULT_CODE_MUST_UNDERSTAND = "MustUnderstand";
    static final String FAULT_CODE_DATA_ENCODING_UNKNOWN = "DataEncodingUnknown";

    // Followings are different in SOAP 1.1 and 1.2 specifications
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAPConstants.java`
#### Snippet
```java

    /** Field HEADER_LOCAL_NAME */
    static final String HEADER_LOCAL_NAME = "Header";

    /** Field BODY_LOCAL_NAME */
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAPConstants.java`
#### Snippet
```java

    /** Field HEADER_LOCAL_NAME */
    static final String HEADER_LOCAL_NAME = "Header";

    /** Field BODY_LOCAL_NAME */
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAPConstants.java`
#### Snippet
```java

    /** Field ATTR_MUSTUNDERSTAND */
    static final String ATTR_MUSTUNDERSTAND = "mustUnderstand";
    static final String ATTR_MUSTUNDERSTAND_TRUE = "true";
    static final String ATTR_MUSTUNDERSTAND_FALSE = "false";
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAPConstants.java`
#### Snippet
```java

    /** Field ATTR_MUSTUNDERSTAND */
    static final String ATTR_MUSTUNDERSTAND = "mustUnderstand";
    static final String ATTR_MUSTUNDERSTAND_TRUE = "true";
    static final String ATTR_MUSTUNDERSTAND_FALSE = "false";
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAPConstants.java`
#### Snippet
```java
    // Followings are different in SOAP 1.1 and 1.2 specifications
    static final String FAULT_CODE_SENDER = "";
    static final String FAULT_CODE_RECEIVER = "";
}

```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAPConstants.java`
#### Snippet
```java
    // Followings are different in SOAP 1.1 and 1.2 specifications
    static final String FAULT_CODE_SENDER = "";
    static final String FAULT_CODE_RECEIVER = "";
}

```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAPConstants.java`
#### Snippet
```java
    /** Field ATTR_MUSTUNDERSTAND */
    static final String ATTR_MUSTUNDERSTAND = "mustUnderstand";
    static final String ATTR_MUSTUNDERSTAND_TRUE = "true";
    static final String ATTR_MUSTUNDERSTAND_FALSE = "false";
    static final String ATTR_MUSTUNDERSTAND_0 = "0";
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAPConstants.java`
#### Snippet
```java
    /** Field ATTR_MUSTUNDERSTAND */
    static final String ATTR_MUSTUNDERSTAND = "mustUnderstand";
    static final String ATTR_MUSTUNDERSTAND_TRUE = "true";
    static final String ATTR_MUSTUNDERSTAND_FALSE = "false";
    static final String ATTR_MUSTUNDERSTAND_0 = "0";
```

### UnnecessaryModifier
Modifier `private` is redundant for enum constructors
in `mixins/core-mixins/src/main/java/org/apache/axiom/core/NodeType.java`
#### Snippet
```java
    private EnumSet<NodeType> allowedChildTypes;

    private NodeType(Class<? extends CoreNode> iface) {
        this.iface = iface;
    }
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/lifecycle/DataHandlerExt.java`
#### Snippet
```java
	 * to purge the file. 
	 */
	public void purgeDataSource() throws IOException;
	
	/**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/lifecycle/DataHandlerExt.java`
#### Snippet
```java
	 * @deprecated Use {@link #readOnce()} or {@link #purgeDataSource()} instead.
	 */
	public void deleteWhenReadOnce() throws IOException;
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/lifecycle/LifecycleManager.java`
#### Snippet
```java
     * @throws IOException
     */
    public void deleteOnTimeInterval(int interval, File file) throws IOException;
    
    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/lifecycle/LifecycleManager.java`
#### Snippet
```java
     * @throws IOException
     */
    public FileAccessor getFileAccessor(String file) throws IOException;
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/lifecycle/LifecycleManager.java`
#### Snippet
```java
     * @throws IOException
     */
    public void delete(File file) throws IOException;
    
    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/lifecycle/LifecycleManager.java`
#### Snippet
```java
     * @throws IOException
     */
    public void deleteOnExit(File file) throws IOException;
    
    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/lifecycle/LifecycleManager.java`
#### Snippet
```java
     * @throws IOException
     */    
    public FileAccessor create(String attachmentDir) throws IOException;
    
    /** 
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/lifecycle/impl/LifecycleEventHandler.java`
#### Snippet
```java
     * @param eventId
     */
    public void handleEvent(int eventId) throws IOException;
}

```

## RuleId[id=MarkedForRemoval]
### MarkedForRemoval
'Integer(int)' is deprecated and marked for removal
in `testing/testutils/src/main/java/org/apache/axiom/testutils/stax/XMLStreamReaderComparator.java`
#### Snippet
```java
            for (int i = 0; i < (attributeCount == null ? 1 : attributeCount.intValue()); i++) {
                Class<?>[] paramTypes = {Integer.TYPE};
                Object[] args = {new Integer(i)};
                assertSameResult(String.class, "getAttributeLocalName", paramTypes, args);
                assertSameResult(QName.class, "getAttributeName", paramTypes, args);
```

### MarkedForRemoval
'java.security.AccessController' is deprecated and marked for removal
in `axiom-api/src/main/java/org/apache/axiom/om/util/StAXUtils.java`
#### Snippet
```java
            final StAXParserConfiguration configuration) {
        
        return AccessController.doPrivileged(new PrivilegedAction<XMLInputFactory>() {
            @Override
            public XMLInputFactory run() {
```

### MarkedForRemoval
'java.security.AccessController' is deprecated and marked for removal
in `axiom-api/src/main/java/org/apache/axiom/om/util/StAXUtils.java`
#### Snippet
```java
            final StAXParserConfiguration configuration) {
        
        return AccessController.doPrivileged(new PrivilegedAction<XMLInputFactory>() {
            @Override
            public XMLInputFactory run() {
```

### MarkedForRemoval
'java.security.AccessController' is deprecated and marked for removal
in `axiom-api/src/main/java/org/apache/axiom/om/util/StAXUtils.java`
#### Snippet
```java
    private static XMLOutputFactory newXMLOutputFactory(final ClassLoader classLoader,
            final StAXWriterConfiguration configuration) {
        return AccessController.doPrivileged(new PrivilegedAction<XMLOutputFactory>() {
            @Override
            public XMLOutputFactory run() {
```

### MarkedForRemoval
'java.security.AccessController' is deprecated and marked for removal
in `axiom-api/src/main/java/org/apache/axiom/om/util/StAXUtils.java`
#### Snippet
```java
    private static XMLOutputFactory newXMLOutputFactory(final ClassLoader classLoader,
            final StAXWriterConfiguration configuration) {
        return AccessController.doPrivileged(new PrivilegedAction<XMLOutputFactory>() {
            @Override
            public XMLOutputFactory run() {
```

### MarkedForRemoval
'java.security.AccessController' is deprecated and marked for removal
in `axiom-api/src/main/java/org/apache/axiom/om/util/StAXUtils.java`
#### Snippet
```java
import java.io.Reader;
import java.io.Writer;
import java.security.AccessController;
import java.security.PrivilegedAction;
import java.util.Collections;
```

### MarkedForRemoval
'java.security.AccessController' is deprecated and marked for removal
in `axiom-api/src/main/java/org/apache/axiom/util/stax/dialect/JBossFactoryUnwrapper.java`
#### Snippet
```java
        try {
            actual = wrapperClass.getDeclaredField("actual");
            AccessController.doPrivileged(new PrivilegedAction<Void>() {
                @Override
                public Void run() {
```

### MarkedForRemoval
'java.security.AccessController' is deprecated and marked for removal
in `axiom-api/src/main/java/org/apache/axiom/util/stax/dialect/JBossFactoryUnwrapper.java`
#### Snippet
```java
        try {
            actual = wrapperClass.getDeclaredField("actual");
            AccessController.doPrivileged(new PrivilegedAction<Void>() {
                @Override
                public Void run() {
```

### MarkedForRemoval
'java.security.AccessController' is deprecated and marked for removal
in `axiom-api/src/main/java/org/apache/axiom/util/stax/dialect/JBossFactoryUnwrapper.java`
#### Snippet
```java

import java.lang.reflect.Field;
import java.security.AccessController;
import java.security.PrivilegedAction;

```

### MarkedForRemoval
'java.security.AccessController' is deprecated and marked for removal
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/AttachmentCacheMonitor.java`
#### Snippet
```java
import java.io.File;

import java.security.AccessController;
import java.security.PrivilegedAction;

```

### MarkedForRemoval
'java.security.AccessController' is deprecated and marked for removal
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/AttachmentCacheMonitor.java`
#### Snippet
```java

    private boolean deleteFile(final String fileName ) {
        return AccessController.doPrivileged(new PrivilegedAction<Boolean>() {
                @Override
                public Boolean run() {
```

### MarkedForRemoval
'java.security.AccessController' is deprecated and marked for removal
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/AttachmentCacheMonitor.java`
#### Snippet
```java

    private boolean deleteFile(final String fileName ) {
        return AccessController.doPrivileged(new PrivilegedAction<Boolean>() {
                @Override
                public Boolean run() {
```

### MarkedForRemoval
'Long(long)' is deprecated and marked for removal
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/AttachmentCacheMonitor.java`
#### Snippet
```java

    private Long getTime() {
        return new Long(System.currentTimeMillis());
    }

```

### MarkedForRemoval
'java.security.AccessController' is deprecated and marked for removal
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/lifecycle/impl/LifecycleManagerImpl.java`
#### Snippet
```java
        }
        try{
            hook = AccessController.doPrivileged(new PrivilegedExceptionAction<VMShutdownHook>() {
                @Override
                public VMShutdownHook run() throws SecurityException, IllegalStateException, IllegalArgumentException {
```

### MarkedForRemoval
'java.security.AccessController' is deprecated and marked for removal
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/lifecycle/impl/LifecycleManagerImpl.java`
#### Snippet
```java
        }
        try{
            hook = AccessController.doPrivileged(new PrivilegedExceptionAction<VMShutdownHook>() {
                @Override
                public VMShutdownHook run() throws SecurityException, IllegalStateException, IllegalArgumentException {
```

### MarkedForRemoval
'java.security.AccessController' is deprecated and marked for removal
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/lifecycle/impl/LifecycleManagerImpl.java`
#### Snippet
```java
import java.io.File;
import java.io.IOException;
import java.security.AccessController;
import java.security.PrivilegedActionException;
import java.security.PrivilegedExceptionAction;
```

### MarkedForRemoval
'Double(double)' is deprecated and marked for removal
in `testing/jaxen-testsuite/src/main/java/org/jaxen/test/ExprComparator.java`
#### Snippet
```java
					NumberExpr numberExpr1 = (NumberExpr)o1;
					NumberExpr numberExpr2 = (NumberExpr)o2;
					cmp = new Double(numberExpr1.getNumber().doubleValue()).compareTo(new Double(numberExpr2.getNumber().doubleValue()));
					break;
				case TYPE_PATH_EXPR:
```

### MarkedForRemoval
'Double(double)' is deprecated and marked for removal
in `testing/jaxen-testsuite/src/main/java/org/jaxen/test/ExprComparator.java`
#### Snippet
```java
					NumberExpr numberExpr1 = (NumberExpr)o1;
					NumberExpr numberExpr2 = (NumberExpr)o2;
					cmp = new Double(numberExpr1.getNumber().doubleValue()).compareTo(new Double(numberExpr2.getNumber().doubleValue()));
					break;
				case TYPE_PATH_EXPR:
```

## RuleId[id=EmptyStatementBody]
### EmptyStatementBody
`while` statement has empty body
in `testing/xml-truth/src/main/java/org/apache/axiom/truth/xml/XMLSubject.java`
#### Snippet
```java
                        public Event next() throws TraverserException {
                            Event event;
                            while (ignoredEvents.contains(event = super.next())) {
                                // loop
                            }
```

### EmptyStatementBody
`for` statement has empty body
in `axiom-api/src/main/java/org/apache/axiom/mime/ContentTypeTokenizer.java`
#### Snippet
```java
        skipWhiteSpace();
        int begin = index;
        for (int len = s.length(); index < len && tspecials.indexOf(s.charAt(index)) == -1; index++) {
            // Just loop
        }
```

### EmptyStatementBody
`for` statement has empty body
in `axiom-api/src/main/java/org/apache/axiom/mime/ContentTypeTokenizer.java`
#### Snippet
```java
        }
        int end = index;
        for (; end > begin && whitespace.indexOf(s.charAt(end-1)) != -1; end--) {
            // Just loop
        }
```

### EmptyStatementBody
`for` statement has empty body
in `axiom-api/src/main/java/org/apache/axiom/mime/ContentTypeTokenizer.java`
#### Snippet
```java

    private void skipWhiteSpace() {
        for (int len = s.length(); index < len && whitespace.indexOf(s.charAt(index)) != -1; index++) {
            // Just loop
        }
```

### EmptyStatementBody
`while` statement has empty body
in `axiom-api/src/main/java/org/apache/axiom/mime/PartImpl.java`
#### Snippet
```java
            EntityState state = parser.next();
            if (state == EntityState.T_EPILOGUE) {
                while (parser.next() != EntityState.T_END_MULTIPART) {
                    // Just loop
                }
```

### EmptyStatementBody
`while` statement has empty body
in `axiom-api/src/main/java/org/apache/axiom/mime/MultipartBody.java`
#### Snippet
```java

    public void detach() {
        while (getNextPart() != null) {
            // Just loop
        }
```

### EmptyStatementBody
`if` statement has empty body
in `axiom-api/src/main/java/org/apache/axiom/util/base64/Base64Utils.java`
#### Snippet
```java
            char ch = data.charAt(i);

            if (ch == Base64Constants.S_BASE64PAD || ch < Base64Constants.S_DECODETABLE.length
                    && Base64Constants.S_DECODETABLE[ch] >= 0) {
                //valid character.Do nothing
```

### EmptyStatementBody
`if` statement has empty body
in `axiom-api/src/main/java/org/apache/axiom/util/base64/Base64Utils.java`
#### Snippet
```java
                    && Base64Constants.S_DECODETABLE[ch] >= 0) {
                //valid character.Do nothing
            } else if (ch == '\r' || ch == '\n') {
                //do nothing
            } else {
```

### EmptyStatementBody
`if` statement has empty body
in `mixins/core-mixins/src/main/java/org/apache/axiom/core/impl/mixin/CoreAttributeMixin.java`
#### Snippet
```java
    @Override
    public final void coreSetOwnerDocument(CoreDocument document) {
        if (owner instanceof CoreElement) {
            // TODO
            //            throw new IllegalStateException();
```

### EmptyStatementBody
`while` statement has empty body
in `mixins/core-mixins/src/main/java/org/apache/axiom/core/impl/mixin/CoreParentNodeMixin.java`
#### Snippet
```java
        try {
            XmlReader reader = coreGetReader(handler, cache, false);
            while (!reader.proceed()) {
                // Just loop
            }
```

### EmptyStatementBody
`else` statement has empty body
in `mixins/dom-mixins/src/main/java/org/apache/axiom/dom/DOMConfigurationImpl.java`
#### Snippet
```java
                        params &= ~mask.intValue();
                    }
                } else {
                    // TODO
                }
```

### EmptyStatementBody
`else` statement has empty body
in `mixins/dom-mixins/src/main/java/org/apache/axiom/dom/DOMConfigurationImpl.java`
#### Snippet
```java
                    // TODO
                }
            } else {
                // TODO
            }
```

### EmptyStatementBody
`else` statement has empty body
in `mixins/dom-mixins/src/main/java/org/apache/axiom/dom/DOMConfigurationImpl.java`
#### Snippet
```java
                // TODO
            }
        } else {
            // TODO
        }
```

### EmptyStatementBody
`while` statement has empty body
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/MultipartBodyAdapter.java`
#### Snippet
```java

    private void fetchAll() {
        while (fetchNext()) {
            // Just loop
        }
```

### EmptyStatementBody
`if` statement has empty body
in `axiom-compat/src/main/java/org/apache/axiom/util/stax/AbstractXMLStreamReader.java`
#### Snippet
```java
                    || eventType == XMLStreamConstants.ENTITY_REFERENCE) {
                content.append(getText());
            } else if (eventType == XMLStreamConstants.PROCESSING_INSTRUCTION
                    || eventType == XMLStreamConstants.COMMENT) {
                // skipping
```

### EmptyStatementBody
`while` statement has empty body
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/document/sr/TestDTDReaderFromParser.java`
#### Snippet
```java
        DTDReader dtdReader = (DTDReader) reader.getProperty(DTDReader.PROPERTY);
        assertNotNull(dtdReader);
        while (reader.next() != XMLStreamReader.DTD) {
            // Just loop
        }
```

### EmptyStatementBody
`else` statement has empty body
in `mixins/om-mixins/src/main/java/org/apache/axiom/om/impl/mixin/AxiomNamedInformationItemMixin.java`
#### Snippet
```java
            if (prefix.length() > 0) {
                throw new OMException("Cannot set prefix on an information item without namespace");
            } else {
                // No need to set a new OMNamespace in this case
            }
```

## RuleId[id=UnnecessaryStringEscape]
### UnnecessaryStringEscape
`\'` is unnecessarily escaped
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestChildReDeclaringGrandParentsDefaultNSWithPrefix.java`
#### Snippet
```java
        String xml = elem.toString();
        assertTrue(
                "Binarysecret element should have \'wst\' ns prefix",
                xml.indexOf("<wst:Binarysecret") != -1);
    }
```

### UnnecessaryStringEscape
`\'` is unnecessarily escaped
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestChildReDeclaringGrandParentsDefaultNSWithPrefix.java`
#### Snippet
```java
        String xml = elem.toString();
        assertTrue(
                "Binarysecret element should have \'wst\' ns prefix",
                xml.indexOf("<wst:Binarysecret") != -1);
    }
```

## RuleId[id=StaticInitializerReferencesSubClass]
### StaticInitializerReferencesSubClass
Referencing subclass SimpleMessageFactoryConfigurator from superclass MessageFactoryConfigurator initializer might lead to class loading deadlock
in `testing/spring-ws-testsuite/src/main/java/org/apache/axiom/ts/springws/MessageFactoryConfigurator.java`
#### Snippet
```java
public abstract class MessageFactoryConfigurator {
    public static final MessageFactoryConfigurator SAAJ =
            new SimpleMessageFactoryConfigurator(
                    "saaj",
                    new ClassPathResource(
```

### StaticInitializerReferencesSubClass
Referencing subclass Identity from superclass ContentTransferEncoding initializer might lead to class loading deadlock
in `axiom-api/src/main/java/org/apache/axiom/mime/ContentTransferEncoding.java`
#### Snippet
```java
     * The {@code 8bit} content transfer encoding.
     */
    public static final ContentTransferEncoding EIGHT_BIT = new Identity("8bit");

    /**
```

### StaticInitializerReferencesSubClass
Referencing subclass Identity from superclass ContentTransferEncoding initializer might lead to class loading deadlock
in `axiom-api/src/main/java/org/apache/axiom/mime/ContentTransferEncoding.java`
#### Snippet
```java
     * The {@code binary} content transfer encoding.
     */
    public static final ContentTransferEncoding BINARY = new Identity("binary");

    /**
```

### StaticInitializerReferencesSubClass
Referencing subclass SOAPFaultChild from superclass SOAPElementType initializer might lead to class loading deadlock
in `testing/soap-testsuite/src/main/java/org/apache/axiom/ts/soap/SOAPElementType.java`
#### Snippet
```java
        SOAPFaultChild.NODE,
        SOAPFaultChild.ROLE,
        SOAPFaultChild.DETAIL,
    };

```

### StaticInitializerReferencesSubClass
Referencing subclass SOAPFaultChild from superclass SOAPElementType initializer might lead to class loading deadlock
in `testing/soap-testsuite/src/main/java/org/apache/axiom/ts/soap/SOAPElementType.java`
#### Snippet
```java
        addRelation(FAULT, SOAPFaultChild.NODE);
        addRelation(FAULT, SOAPFaultChild.ROLE);
        addRelation(FAULT, SOAPFaultChild.DETAIL);
    }

```

### StaticInitializerReferencesSubClass
Referencing subclass SimpleSOAPSample from superclass SOAPSample initializer might lead to class loading deadlock
in `testing/soap-testsuite/src/main/java/org/apache/axiom/ts/soap/SOAPSample.java`
#### Snippet
```java
    /** A SOAP fault response that uses some SOAP 1.2 specific features. */
    public static final SOAPSample SOAP12_FAULT =
            new SimpleSOAPSample(
                    SOAPSpec.SOAP12, "test-message/soap12/fault.xml", "soap12/fault.xml");

```

### StaticInitializerReferencesSubClass
Referencing subclass SimpleSOAPSample from superclass SOAPSample initializer might lead to class loading deadlock
in `testing/soap-testsuite/src/main/java/org/apache/axiom/ts/soap/SOAPSample.java`
#### Snippet
```java
    /** A SOAP 1.2 message that uses the relay attribute. */
    public static final SOAPSample SOAP12_RELAY =
            new SimpleSOAPSample(
                    SOAPSpec.SOAP12, "test-message/soap12/relay.xml", "soap12/relay.xml");

```

### StaticInitializerReferencesSubClass
Referencing subclass OMElementExtractor from superclass OMContainerExtractor initializer might lead to class loading deadlock
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/container/OMContainerExtractor.java`
#### Snippet
```java

    public static final OMElementExtractor ELEMENT = new OMElementExtractor(false);
    public static final OMElementExtractor ELEMENT_DETACHED = new OMElementExtractor(true);

    OMContainerExtractor() {}
```

### StaticInitializerReferencesSubClass
Referencing subclass OMElementExtractor from superclass OMContainerExtractor initializer might lead to class loading deadlock
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/container/OMContainerExtractor.java`
#### Snippet
```java
            };

    public static final OMElementExtractor ELEMENT = new OMElementExtractor(false);
    public static final OMElementExtractor ELEMENT_DETACHED = new OMElementExtractor(true);

```

## RuleId[id=CStyleArrayDeclaration]
### CStyleArrayDeclaration
C-style array declaration of parameter `ch`
in `axiom-compat/src/main/java/org/apache/axiom/om/impl/serialize/StreamWriterToContentHandlerConverter.java`
#### Snippet
```java
     */
    @Override
    public void characters(char ch[], int start, int length)
            throws SAXException {
        try {
```

### CStyleArrayDeclaration
C-style array declaration of parameter `ch`
in `axiom-compat/src/main/java/org/apache/axiom/om/impl/serialize/StreamWriterToContentHandlerConverter.java`
#### Snippet
```java
     */
    @Override
    public void ignorableWhitespace(char ch[], int start, int length)
            throws SAXException {

```

### CStyleArrayDeclaration
C-style array declaration of local variable `control`
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/container/TestSerialize.java`
#### Snippet
```java
            // We need to clone the InputSource objects so that we can dump their contents
            // if the test fails
            InputSource control[] =
                    duplicateInputSource(containerExtractor.getControl(file.getInputStream()));
            XML actual = serializationStrategy.serialize(container);
```

### CStyleArrayDeclaration
C-style array declaration of local variable `roles`
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/header/TestGetHeadersToProcessWithParser.java`
#### Snippet
```java
        SOAPHeader soapHeader = envelope.getHeader();

        String roles[] = {"http://example.org/myCustomRole"};
        RolePlayer rp = new MyRolePlayer(true, roles);

```

### CStyleArrayDeclaration
C-style array declaration of parameter `chars`
in `components/core-streams/src/main/java/org/apache/axiom/core/stream/serializer/writer/XmlWriter.java`
#### Snippet
```java
    public abstract void write(String s) throws IOException;

    public abstract void write(char chars[], int start, int length) throws IOException;

    public abstract AbstractBase64EncodingOutputStream getBase64EncodingOutputStream();
```

### CStyleArrayDeclaration
C-style array declaration of parameter `chars`
in `components/core-streams/src/main/java/org/apache/axiom/core/stream/serializer/Serializer.java`
#### Snippet
```java
     * @throws StreamException
     */
    public void characters(char chars[], int start, int length) throws StreamException {
        // It does not make sense to continue with rest of the method if the number of
        // characters to read from array is 0.
```

## RuleId[id=CommentedOutCode]
### CommentedOutCode
Commented out code (7 lines)
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/ConfigurableDataHandler.java`
#### Snippet
```java
    }

//	public String getContentID() {
//		return contentID;
//	}
```

### CommentedOutCode
Commented out code (2 lines)
in `implementations/axiom-dom/src/main/java/org/apache/axiom/om/impl/dom/factory/DOOMDocumentBuilderFactory.java`
#### Snippet
```java
    public void setAttribute(String name, Object value)
            throws IllegalArgumentException {
        // // TODO
        // throw new UnsupportedOperationException("TODO");
    }
```

### CommentedOutCode
Commented out code (4 lines)
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap11/builder/TestBuilder.java`
#### Snippet
```java
                        metaFactory, new StringReader(soap11Message));
        SOAPEnvelope soap11Envelope = (SOAPEnvelope) soap11Builder.getDocumentElement();
        //            soap11Envelope.build();
        //            writer = XMLOutputFactory.newInstance().createXMLStreamWriter(System.out);
        //            soap11Envelope.internalSerializeAndConsume(writer);
```

### CommentedOutCode
Commented out code (2 lines)
in `axiom-weaver/src/main/java/org/apache/axiom/weaver/Weaver.java`
#### Snippet
```java
        dump("Graph after applying transitive reduction:\n", false);

        //        compact();
        //        dump("Graph after compaction:\n", false);

```

### CommentedOutCode
Commented out code (2 lines)
in `components/core-streams/src/main/java/org/apache/axiom/core/stream/serializer/Serializer.java`
#### Snippet
```java
                writer.write(valueDefault);
            }
            // writer.write(" ");
            // writer.write(value);
            writer.write(">\n");
```

## RuleId[id=UnnecessaryLabelOnContinueStatement]
### UnnecessaryLabelOnContinueStatement
Unnecessary label `loop` on continue statement
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/container/DocumentElementExtractor.java`
#### Snippet
```java
                        break loop;
                    } else {
                        continue loop;
                    }
            }
```

### UnnecessaryLabelOnContinueStatement
Unnecessary label `loop` on continue statement
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/container/RootWhitespaceFilter.java`
#### Snippet
```java
                        break loop;
                    } else {
                        continue loop;
                    }
                default:
```

## RuleId[id=RegExpRedundantEscape]
### RegExpRedundantEscape
Redundant character escape `\\.` in RegExp
in `axiom-api/src/main/java/org/apache/axiom/util/stax/dialect/Version.java`
#### Snippet
```java

final class Version {
    private static final Pattern pattern = Pattern.compile("([0-9]+(\\.[0-9]+)*)([\\.-].*)?");
    
    private final int[] components;
```

## RuleId[id=UnparsedCustomBeanInspection]
### UnparsedCustomBeanInspection
Unsupported tag 'sws:annotation-driven' for namespace '', please file issue (Help\|Submit a Bug Report...)
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/jdom/server.xml`
#### Snippet
```java
         http://www.springframework.org/schema/web-services http://www.springframework.org/schema/web-services/web-services-2.0.xsd">
    <context:component-scan base-package="org.apache.axiom.ts.springws.scenario.jdom"/>
    <sws:annotation-driven/>
</beans>

```

### UnparsedCustomBeanInspection
Unsupported tag 'sws:annotation-driven' for namespace '', please file issue (Help\|Submit a Bug Report...)
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/jaxb2/server.xml`
#### Snippet
```java
         http://www.springframework.org/schema/web-services http://www.springframework.org/schema/web-services/web-services-2.0.xsd">
    <context:component-scan base-package="org.apache.axiom.ts.springws.scenario.jaxb2"/>
    <sws:annotation-driven/>
</beans>

```

### UnparsedCustomBeanInspection
Unsupported tag 'sws:annotation-driven' for namespace '', please file issue (Help\|Submit a Bug Report...)
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/wsadom/server.xml`
#### Snippet
```java
         http://www.springframework.org/schema/web-services http://www.springframework.org/schema/web-services/web-services-2.0.xsd">
    <context:component-scan base-package="org.apache.axiom.ts.springws.scenario.wsadom"/>
    <sws:annotation-driven/>
</beans>

```

### UnparsedCustomBeanInspection
Unsupported tag 'oxm:jaxb2-marshaller' for namespace '', please file issue (Help\|Submit a Bug Report...)
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/jaxb2/client.xml`
#### Snippet
```java
         http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.0.xsd
         http://www.springframework.org/schema/oxm http://www.springframework.org/schema/oxm/spring-oxm-4.0.xsd">
    <oxm:jaxb2-marshaller id="marshaller">
        <oxm:class-to-be-bound name="org.apache.axiom.ts.springws.scenario.jaxb2.GetQuoteRequest"/>
        <oxm:class-to-be-bound name="org.apache.axiom.ts.springws.scenario.jaxb2.GetQuoteResponse"/>
```

### UnparsedCustomBeanInspection
Unsupported tag 'sws:annotation-driven' for namespace '', please file issue (Help\|Submit a Bug Report...)
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/broker/server.xml`
#### Snippet
```java
    <bean id="customerService" class="org.apache.axiom.ts.springws.scenario.broker.MockCustomerService"/>
    <context:component-scan base-package="org.apache.axiom.ts.springws.scenario.broker"/>
    <sws:annotation-driven/>
    <bean id="exceptionResolver" class="org.springframework.ws.soap.server.endpoint.SoapFaultMappingExceptionResolver">
        <property name="order" value="-1"/>
```

### UnparsedCustomBeanInspection
Unsupported tag 'sws:annotation-driven' for namespace '', please file issue (Help\|Submit a Bug Report...)
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/soapaction/server.xml`
#### Snippet
```java
         http://www.springframework.org/schema/web-services http://www.springframework.org/schema/web-services/web-services-2.0.xsd">
    <context:component-scan base-package="org.apache.axiom.ts.springws.scenario.soapaction"/>
    <sws:annotation-driven/>
</beans>

```

### UnparsedCustomBeanInspection
Unsupported tag 'sws:annotation-driven' for namespace '', please file issue (Help\|Submit a Bug Report...)
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/secureecho/server.xml`
#### Snippet
```java
         http://www.springframework.org/schema/web-services http://www.springframework.org/schema/web-services/web-services-2.0.xsd">
    <context:component-scan base-package="org.apache.axiom.ts.springws.scenario.secureecho"/>
    <sws:annotation-driven/>
    <sws:interceptors>
        <bean class="org.springframework.ws.soap.security.wss4j2.Wss4jSecurityInterceptor">
```

### UnparsedCustomBeanInspection
Unsupported tag 'sws:interceptors' for namespace '', please file issue (Help\|Submit a Bug Report...)
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/secureecho/server.xml`
#### Snippet
```java
    <context:component-scan base-package="org.apache.axiom.ts.springws.scenario.secureecho"/>
    <sws:annotation-driven/>
    <sws:interceptors>
        <bean class="org.springframework.ws.soap.security.wss4j2.Wss4jSecurityInterceptor">
            <property name="validationActions" value="UsernameToken"/>
```

### UnparsedCustomBeanInspection
Unsupported tag 'oxm:jaxb2-marshaller' for namespace '', please file issue (Help\|Submit a Bug Report...)
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/broker/client.xml`
#### Snippet
```java
        http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.0.xsd
        http://www.springframework.org/schema/oxm http://www.springframework.org/schema/oxm/spring-oxm-4.0.xsd">
    <oxm:jaxb2-marshaller id="marshaller">
        <oxm:class-to-be-bound name="org.apache.axiom.ts.springws.scenario.broker.Order"/>
        <oxm:class-to-be-bound name="org.apache.axiom.ts.springws.scenario.broker.OrderStatus"/>
```

### UnparsedCustomBeanInspection
Unsupported tag 'oxm:jaxb2-marshaller' for namespace '', please file issue (Help\|Submit a Bug Report...)
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/validation/client.xml`
#### Snippet
```java
         http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.0.xsd
         http://www.springframework.org/schema/oxm http://www.springframework.org/schema/oxm/spring-oxm-4.0.xsd">
    <oxm:jaxb2-marshaller id="marshaller" context-path="org.apache.axiom.ts.springws.scenario.validation"/>
    <bean class="org.apache.axiom.ts.springws.scenario.validation.StockQuoteClient">
        <constructor-arg>
```

### UnparsedCustomBeanInspection
Unsupported tag 'sws:annotation-driven' for namespace '', please file issue (Help\|Submit a Bug Report...)
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/validation/server.xml`
#### Snippet
```java
         http://www.springframework.org/schema/web-services http://www.springframework.org/schema/web-services/web-services-2.0.xsd">
    <context:component-scan base-package="org.apache.axiom.ts.springws.scenario.validation"/>
    <sws:annotation-driven/>
    <sws:interceptors>
        <bean class="org.springframework.ws.soap.server.endpoint.interceptor.PayloadValidatingInterceptor">
```

### UnparsedCustomBeanInspection
Unsupported tag 'sws:interceptors' for namespace '', please file issue (Help\|Submit a Bug Report...)
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/validation/server.xml`
#### Snippet
```java
    <context:component-scan base-package="org.apache.axiom.ts.springws.scenario.validation"/>
    <sws:annotation-driven/>
    <sws:interceptors>
        <bean class="org.springframework.ws.soap.server.endpoint.interceptor.PayloadValidatingInterceptor">
            <property name="schema" value="classpath:org/apache/axiom/ts/springws/scenario/validation/schema/stockquote.xsd"/>
```

## RuleId[id=SpringBeanConstructorArgInspection]
### SpringBeanConstructorArgInspection
No matching constructor found in class 'BrokerClient'#treeend

*** ** * ** ***

|---------------------------------------|---|-----------|
| **BrokerClient(...):**                |   | **Bean:** |
| WebServiceTemplate webServiceTemplate |   | **???**   |
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/broker/client.xml`
#### Snippet
```java
        <oxm:class-to-be-bound name="org.apache.axiom.ts.springws.scenario.broker.RetrieveNextOrder"/>
    </oxm:jaxb2-marshaller>
    <bean class="org.apache.axiom.ts.springws.scenario.broker.BrokerClient">
        <constructor-arg>
            <bean class="org.springframework.ws.client.core.WebServiceTemplate">
```

### SpringBeanConstructorArgInspection
No matching constructor found in class 'StockQuoteClient'#treeend

*** ** * ** ***

|---------------------------------------|---|-----------|
| **StockQuoteClient(...):**            |   | **Bean:** |
| WebServiceTemplate webServiceTemplate |   | **???**   |
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/validation/client.xml`
#### Snippet
```java
         http://www.springframework.org/schema/oxm http://www.springframework.org/schema/oxm/spring-oxm-4.0.xsd">
    <oxm:jaxb2-marshaller id="marshaller" context-path="org.apache.axiom.ts.springws.scenario.validation"/>
    <bean class="org.apache.axiom.ts.springws.scenario.validation.StockQuoteClient">
        <constructor-arg>
            <bean class="org.springframework.ws.client.core.WebServiceTemplate">
```

## RuleId[id=FinalMethodInFinalClass]
### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `testing/spring-ws-testsuite/src/main/java/org/apache/axiom/ts/springws/scenario/ScenarioConfig.java`
#### Snippet
```java
    }

    public final MessageFactoryConfigurator getClientMessageFactoryConfigurator() {
        return clientMessageFactoryConfigurator;
    }
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `testing/spring-ws-testsuite/src/main/java/org/apache/axiom/ts/springws/scenario/ScenarioConfig.java`
#### Snippet
```java
    }

    public final MessageFactoryConfigurator getServerMessageFactoryConfigurator() {
        return serverMessageFactoryConfigurator;
    }
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `mixins/core-mixins/src/main/java/org/apache/axiom/core/impl/ElementsIterator.java`
#### Snippet
```java

    @Override
    protected final boolean matches(T node) throws CoreModelException {
        return matcher.matches(node, namespaceURI, name);
    }
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `mixins/core-mixins/src/main/java/org/apache/axiom/core/impl/AttributeIterator.java`
#### Snippet
```java
    @Override
    @SuppressWarnings("ReturnValueIgnored")
    public final void remove() {
        if (currentAttribute == null) {
            throw new IllegalStateException();
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `mixins/core-mixins/src/main/java/org/apache/axiom/core/impl/AttributeIterator.java`
#### Snippet
```java

    @Override
    public final boolean hasNext() {
        if (!nextAttributeSet) {
            CoreAttribute attribute = currentAttribute;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `mixins/core-mixins/src/main/java/org/apache/axiom/core/impl/AttributeIterator.java`
#### Snippet
```java

    @Override
    public final S next() {
        if (hasNext()) {
            CoreAttribute attribute = nextAttribute;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/IncomingAttachmentStreams.java`
#### Snippet
```java

    /** @return True if the next stream can be read, false otherwise. */
    public final boolean isReadyToGetNextStream() {
        return readyToGetNextStream;
    }
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `components/core-streams/src/main/java/org/apache/axiom/core/stream/stax/push/input/XmlHandlerStreamWriter.java`
#### Snippet
```java

    @Override
    public final String getNamespaceURI(String prefix) {
        if (prefix == null) {
            throw new IllegalArgumentException("prefix can't be null");
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `components/core-streams/src/main/java/org/apache/axiom/core/stream/stax/push/input/XmlHandlerStreamWriter.java`
#### Snippet
```java

    @Override
    public final Iterator<String> getPrefixes(final String namespaceURI) {
        if (namespaceURI == null) {
            throw new IllegalArgumentException("namespaceURI can't be null");
```

## RuleId[id=DuplicateBranchesInSwitch]
### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `testing/jaxen-testsuite/src/main/java/org/jaxen/test/ExprComparator.java`
#### Snippet
```java
					break;
				case TYPE_UNION_EXPR:
					if (true)
						throw new RuntimeException("Not yet implemented!");
					break;
				case TYPE_VARIABLE_REFERENCE_EXPR:
```

## RuleId[id=FinalPrivateMethod]
### FinalPrivateMethod
'private' method declared `final`
in `components/core-streams/src/main/java/org/apache/axiom/core/stream/serializer/writer/ASCIICompatibleXmlWriter.java`
#### Snippet
```java
    }

    private final void internalWrite(char c) throws IOException {
        if (highSurrogate != 0) {
            if (Character.isLowSurrogate(c)) {
```

### FinalPrivateMethod
'private' method declared `final`
in `testing/xml-testsuite/src/main/java/org/apache/axiom/ts/xml/MIMESample.java`
#### Snippet
```java
    }

    private final synchronized Multipart getMultipart() {
        if (multipart == null) {
            try {
```

## RuleId[id=RegExpSimplifiable]
### RegExpSimplifiable
`{1}` is redundant
in `testing/axiom-testsuite/src/main/schema/xmlvend.xsd`
#### Snippet
```java
		</annotation>
		<restriction base="string">
			<pattern value="(\d){1,3}\.(\d){1}"/>
		</restriction>
	</simpleType>
```

### RegExpSimplifiable
`{1}` is redundant
in `testing/axiom-testsuite/src/main/schema/xmlvend.xsd`
#### Snippet
```java
		</annotation>
		<restriction base="string">
			<pattern value="(\d){1}"/>
		</restriction>
	</simpleType>
```

## RuleId[id=DuplicateExpressions]
### DuplicateExpressions
Multiple occurrences of `b0 << 2 & 0xfc | b1 >> 4 & 0x3`
in `axiom-api/src/main/java/org/apache/axiom/util/base64/Base64Utils.java`
#### Snippet
```java
        switch (outlen) {
            case 1:
                obuf[wp] = (byte) (b0 << 2 & 0xfc | b1 >> 4 & 0x3);
                return 1;
            case 2:
```

### DuplicateExpressions
Multiple occurrences of `b0 << 2 & 0xfc | b1 >> 4 & 0x3`
in `axiom-api/src/main/java/org/apache/axiom/util/base64/Base64Utils.java`
#### Snippet
```java
                return 1;
            case 2:
                obuf[wp++] = (byte) (b0 << 2 & 0xfc | b1 >> 4 & 0x3);
                obuf[wp] = (byte) (b1 << 4 & 0xf0 | b2 >> 2 & 0xf);
                return 2;
```

### DuplicateExpressions
Multiple occurrences of `b1 << 4 & 0xf0 | b2 >> 2 & 0xf`
in `axiom-api/src/main/java/org/apache/axiom/util/base64/Base64Utils.java`
#### Snippet
```java
            case 2:
                obuf[wp++] = (byte) (b0 << 2 & 0xfc | b1 >> 4 & 0x3);
                obuf[wp] = (byte) (b1 << 4 & 0xf0 | b2 >> 2 & 0xf);
                return 2;
            case 3:
```

### DuplicateExpressions
Multiple occurrences of `b0 << 2 & 0xfc | b1 >> 4 & 0x3`
in `axiom-api/src/main/java/org/apache/axiom/util/base64/Base64Utils.java`
#### Snippet
```java
                return 2;
            case 3:
                obuf[wp++] = (byte) (b0 << 2 & 0xfc | b1 >> 4 & 0x3);
                obuf[wp++] = (byte) (b1 << 4 & 0xf0 | b2 >> 2 & 0xf);
                obuf[wp] = (byte) (b2 << 6 & 0xc0 | b3 & 0x3f);
```

### DuplicateExpressions
Multiple occurrences of `b1 << 4 & 0xf0 | b2 >> 2 & 0xf`
in `axiom-api/src/main/java/org/apache/axiom/util/base64/Base64Utils.java`
#### Snippet
```java
            case 3:
                obuf[wp++] = (byte) (b0 << 2 & 0xfc | b1 >> 4 & 0x3);
                obuf[wp++] = (byte) (b1 << 4 & 0xf0 | b2 >> 2 & 0xf);
                obuf[wp] = (byte) (b2 << 6 & 0xc0 | b3 & 0x3f);
                return 3;
```

### DuplicateExpressions
Multiple occurrences of `b0 << 2 & 0xfc | b1 >> 4 & 0x3`
in `components/base64-utils/src/main/java/org/apache/axiom/util/base64/AbstractBase64DecodingWriter.java`
#### Snippet
```java
        switch (outlen) {
            case 1:
                out[0] = (byte) (b0 << 2 & 0xfc | b1 >> 4 & 0x3);
                break;
            case 2:
```

### DuplicateExpressions
Multiple occurrences of `b0 << 2 & 0xfc | b1 >> 4 & 0x3`
in `components/base64-utils/src/main/java/org/apache/axiom/util/base64/AbstractBase64DecodingWriter.java`
#### Snippet
```java
                break;
            case 2:
                out[0] = (byte) (b0 << 2 & 0xfc | b1 >> 4 & 0x3);
                out[1] = (byte) (b1 << 4 & 0xf0 | b2 >> 2 & 0xf);
                break;
```

### DuplicateExpressions
Multiple occurrences of `b1 << 4 & 0xf0 | b2 >> 2 & 0xf`
in `components/base64-utils/src/main/java/org/apache/axiom/util/base64/AbstractBase64DecodingWriter.java`
#### Snippet
```java
            case 2:
                out[0] = (byte) (b0 << 2 & 0xfc | b1 >> 4 & 0x3);
                out[1] = (byte) (b1 << 4 & 0xf0 | b2 >> 2 & 0xf);
                break;
            case 3:
```

### DuplicateExpressions
Multiple occurrences of `b0 << 2 & 0xfc | b1 >> 4 & 0x3`
in `components/base64-utils/src/main/java/org/apache/axiom/util/base64/AbstractBase64DecodingWriter.java`
#### Snippet
```java
                break;
            case 3:
                out[0] = (byte) (b0 << 2 & 0xfc | b1 >> 4 & 0x3);
                out[1] = (byte) (b1 << 4 & 0xf0 | b2 >> 2 & 0xf);
                out[2] = (byte) (b2 << 6 & 0xc0 | b3 & 0x3f);
```

### DuplicateExpressions
Multiple occurrences of `b1 << 4 & 0xf0 | b2 >> 2 & 0xf`
in `components/base64-utils/src/main/java/org/apache/axiom/util/base64/AbstractBase64DecodingWriter.java`
#### Snippet
```java
            case 3:
                out[0] = (byte) (b0 << 2 & 0xfc | b1 >> 4 & 0x3);
                out[1] = (byte) (b1 << 4 & 0xf0 | b2 >> 2 & 0xf);
                out[2] = (byte) (b2 << 6 & 0xc0 | b3 & 0x3f);
        }
```

## RuleId[id=StringBufferReplaceableByString]
### StringBufferReplaceableByString
`StringBuffer sb` can be replaced with 'String'
in `axiom-api/src/main/java/org/apache/axiom/om/OMOutputFormat.java`
#### Snippet
```java

    public String getContentTypeForSwA(String SOAPContentType) {
        StringBuffer sb = new StringBuffer();
        sb.append("multipart/related");
        sb.append("; ");
```

### StringBufferReplaceableByString
`StringBuffer sb` can be replaced with 'String'
in `axiom-api/src/main/java/org/apache/axiom/om/OMOutputFormat.java`
#### Snippet
```java
        }
        
        StringBuffer sb = new StringBuffer();
        sb.append("multipart/related");
        sb.append("; ");
```

### StringBufferReplaceableByString
`StringBuffer sb` can be replaced with 'String'
in `axiom-api/src/main/java/org/apache/axiom/om/OMOutputFormat.java`
#### Snippet
```java
    @Override
    public String toString() {
        StringBuffer sb = new StringBuffer();
        sb.append("OMOutputFormat [");
        
```

### StringBufferReplaceableByString
`StringBuffer sb` can be replaced with 'String'
in `axiom-compat/src/main/java/org/apache/axiom/om/util/UUIDGenerator.java`
#### Snippet
```java
            sid = Thread.currentThread().getName();
        }
        StringBuffer sb = new StringBuffer();
        sb.append(sid);
        sb.append(":");
```

## RuleId[id=Deprecation]
### Deprecation
Overrides deprecated method in 'java.lang.Object'
in `components/blob/src/main/java/org/apache/axiom/blob/TempFileBlobImpl.java`
#### Snippet
```java

    @Override
    protected void finalize() throws Throwable {
        if (file != null) {
            log.warn("Cleaning up unreleased temporary file " + file);
```

### Deprecation
'serialize(java.io.Writer)' is deprecated
in `axiom-api/src/main/java/org/apache/axiom/om/OMElement.java`
#### Snippet
```java
     * after invoking this method.
     * <p>
     * This method produces the same result as {@link OMContainer#serialize(Writer)}. In particular,
     * the element is always serialized as plain XML and {@link OMText} nodes containing optimized
     * binary data are always inlined using base64 encoding. Since the output is accumulated into a
```

### Deprecation
'getTextAsQName()' is deprecated
in `axiom-api/src/main/java/org/apache/axiom/om/OMElement.java`
#### Snippet
```java
 * <li>If namespace well-formedness is lost, the string to {@link QName} resolution for attribute
 * values and element content may be inconsistent, i.e. {@link #resolveQName(String)},
 * {@link #getTextAsQName()} and {@link OMText#getTextAsQName()} may return incorrect results.
 * However, it should be noted that these methods are most relevant for object model instances that
 * have been loaded from existing documents or messages. These object models are guaranteed to be
```

### Deprecation
'serializeAndConsume(java.io.Writer, org.apache.axiom.om.OMOutputFormat)' is deprecated
in `axiom-api/src/main/java/org/apache/axiom/om/ds/AbstractOMDataSource.java`
#### Snippet
```java
    @Override
    public final void serialize(Writer writer, OMOutputFormat format) throws XMLStreamException {
        OMAbstractFactory.getOMFactory().createOMElement(this).serializeAndConsume(writer, format);
    }

```

### Deprecation
'serializeAndConsume(java.io.OutputStream, org.apache.axiom.om.OMOutputFormat)' is deprecated
in `axiom-api/src/main/java/org/apache/axiom/om/ds/AbstractOMDataSource.java`
#### Snippet
```java
    @Override
    public final void serialize(OutputStream out, OMOutputFormat format) throws XMLStreamException {
        OMAbstractFactory.getOMFactory().createOMElement(this).serializeAndConsume(out, format);
    }

```

### Deprecation
'serializeAndConsume(java.io.OutputStream)' is deprecated
in `axiom-api/src/main/java/org/apache/axiom/om/ds/custombuilder/BlobOMDataSourceCustomBuilder.java`
#### Snippet
```java
            OutputStream out = blob.getOutputStream();
            try {
                element.serializeAndConsume(out);
            } finally {
                out.close();
```

### Deprecation
'org.apache.axiom.om.util.StAXWriterConfiguration' is deprecated
in `axiom-api/src/main/java/org/apache/axiom/om/util/StAXParserConfiguration.java`
#### Snippet
```java
 * cache keys.
 * 
 * @see StAXWriterConfiguration
 */
public interface StAXParserConfiguration {
```

### Deprecation
'USE_CTE_BASE64_FOR_NON_TEXTUAL_ATTACHMENTS' is deprecated
in `axiom-api/src/main/java/org/apache/axiom/om/impl/OMMultipartWriter.java`
#### Snippet
```java
        ContentTransferEncodingPolicy contentTransferEncodingPolicy = format.getContentTransferEncodingPolicy();
        if (format != null && Boolean.TRUE.equals(
                format.getProperty(OMOutputFormat.USE_CTE_BASE64_FOR_NON_TEXTUAL_ATTACHMENTS))) {
            if (contentTransferEncodingPolicy == null) {
                contentTransferEncodingPolicy = ContentTransferEncodingPolicy.USE_BASE64_FOR_NON_TEXTUAL_PARTS;
```

### Deprecation
Overrides deprecated method in 'java.lang.Object'
in `axiom-api/src/main/java/org/apache/axiom/util/stax/debug/UnclosedReaderDetector.java`
#### Snippet
```java
    
        @Override
        protected void finalize() throws Throwable {
            if (!isClosed) {
                log.warn("Detected unclosed XMLStreamReader.", stackTrace);
```

### Deprecation
'org.apache.axiom.ext.stax.DelegatingXMLStreamReader' is deprecated
in `axiom-api/src/main/java/org/apache/axiom/util/stax/wrapper/XMLStreamReaderWrapper.java`
#### Snippet
```java
     * Get the parent stream reader. This method is declared as protected because it should only be
     * used by subclasses. However, stream reader wrappers that can safely be unwrapped may
     * implement the {@link org.apache.axiom.ext.stax.DelegatingXMLStreamReader} interface to make
     * this a public method. Note that a corresponding <code>setParent</code> method is
     * intentionally omitted because {@link XMLStreamReaderWrapper} is immutable.
```

### Deprecation
'org.apache.axiom.attachments.SizeAwareDataSource' is deprecated
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/Attachments.java`
#### Snippet
```java
     * instance MAY implement {@link DataHandlerExt} in which case the caller can use that API to
     * stream the content of the part. In addition, the {@link DataSource} linked to the returned
     * {@link DataHandler} MAY be of type {@link SizeAwareDataSource} in which case the caller can
     * use that interface to determine the size of the MIME part.
     * 
```

### Deprecation
'java.util.Observable' is deprecated
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/lifecycle/impl/FileAccessor.java`
#### Snippet
```java
 *
 */
public class FileAccessor extends Observable{
    private static final Log log = LogFactory.getLog(FileAccessor.class);
    File file = null;
```

### Deprecation
Overrides deprecated method in 'java.lang.Object'
in `axiom-compat/src/main/java/org/apache/axiom/util/blob/OverflowBlob.java`
#### Snippet
```java

    @Override
    protected void finalize() throws Throwable {
        if (temporaryFile != null) {
            log.warn("Cleaning up unreleased temporary file " + temporaryFile);
```

### Deprecation
'serialize(java.io.OutputStream)' is deprecated
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/AxiomTestCase.java`
#### Snippet
```java
        boolean isConsumed;
        try {
            container.serialize(NullOutputStream.NULL_OUTPUT_STREAM);
            isConsumed = false;
        } catch (Exception ex) {
```

### Deprecation
'serialize(java.io.OutputStream, org.apache.axiom.om.OMOutputFormat)' is deprecated
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/xop/TestSetOptimize.java`
#### Snippet
```java
            OMOutputFormat format = new OMOutputFormat();
            format.setDoOptimize(true);
            document.serialize(out, format);

            Multipart mp =
```

### Deprecation
'USE_CTE_BASE64_FOR_NON_TEXTUAL_ATTACHMENTS' is deprecated
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/xop/TestSerialize.java`
#### Snippet
```java
            oof.setContentTypeProvider(blob -> new ContentType(new MediaType("image", "jpeg")));
            oof.setProperty(
                    OMOutputFormat.USE_CTE_BASE64_FOR_NON_TEXTUAL_ATTACHMENTS, Boolean.TRUE);
        }

```

### Deprecation
'serialize(java.io.OutputStream, org.apache.axiom.om.OMOutputFormat)' is deprecated
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/xop/TestSerialize.java`
#### Snippet
```java
                        metaFactory.getOMFactory(), StAXParserConfiguration.DEFAULT, mb);
        OMElement om = builder.getDocumentElement();
        om.serialize(baos, oof);
        om.close(false);
        String out = baos.toString();
```

### Deprecation
'getNamespace()' is deprecated
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/text/TestGetNamespaceNoNamespace.java`
#### Snippet
```java

/**
 * Tests the behavior of {@link OMText#getNamespace()} for an unprefixed QName and no default
 * namespace in scope.
 */
```

### Deprecation
'getNamespace()' is deprecated
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/text/TestGetNamespaceNoNamespace.java`
#### Snippet
```java
        OMElement element = factory.createOMElement("TestElement", null);
        OMText text = factory.createOMText(element, "value");
        assertNull(text.getNamespace());
    }
}
```

### Deprecation
'serialize(java.io.OutputStream)' is deprecated
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/text/TestBase64StreamingWithSerialize.java`
#### Snippet
```java
                factory.createOMText(new TestBlob('A', Runtime.getRuntime().maxMemory()), false);
        elem.addChild(text);
        elem.serialize(NullOutputStream.NULL_OUTPUT_STREAM);
    }
}
```

### Deprecation
'getNamespace()' is deprecated
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/text/TestGetNamespace.java`
#### Snippet
```java
import org.apache.axiom.ts.AxiomTestCase;

/** Tests the behavior of {@link OMText#getNamespace()} for a prefixed QName. */
public class TestGetNamespace extends AxiomTestCase {
    public TestGetNamespace(OMMetaFactory metaFactory) {
```

### Deprecation
'getNamespace()' is deprecated
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/text/TestGetNamespace.java`
#### Snippet
```java
        element.declareNamespace("urn:ns", "p");
        OMText text = factory.createOMText(element, "p:value");
        OMNamespace ns = text.getNamespace();
        assertEquals("urn:ns", ns.getNamespaceURI());
        assertEquals("p", ns.getPrefix());
```

### Deprecation
'ByteStreamComparator(java.io.InputStream)' is deprecated
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/text/TestBase64StreamingWithGetSAXSource.java`
#### Snippet
```java

        public Base64Comparator(InputStream expected) {
            out = new Base64DecodingOutputStreamWriter(new ByteStreamComparator(expected));
        }

```

### Deprecation
'getTextCharacters()' is deprecated
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/text/TestGetTextCharactersFromDataHandler.java`
#### Snippet
```java

/**
 * Tests that {@link OMText#getTextCharacters()} returns the expected result for an {@link OMText}
 * node backed by a {@link Blob}. This is a regression test for <a
 * href="https://issues.apache.org/jira/browse/AXIOM-442">AXIOM-442</a>.
```

### Deprecation
'getTextCharacters()' is deprecated
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/text/TestGetTextCharactersFromDataHandler.java`
#### Snippet
```java
        Blob blob = new TextBlob("test content", StandardCharsets.UTF_8);
        OMText text = metaFactory.getOMFactory().createOMText(blob, true);
        char[] chars = text.getTextCharacters();
        byte[] decoded = Base64.decodeBase64(new String(chars));
        assertEquals("test content", new String(decoded, StandardCharsets.UTF_8));
```

### Deprecation
'serialize(java.io.OutputStream)' is deprecated
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/builder/TestCreateOMBuilderFromSAXSource.java`
#### Snippet
```java
        }
        ByteArrayOutputStream baos = new ByteArrayOutputStream();
        builder.getDocument().serialize(baos);
        InputSource actual = new InputSource();
        actual.setByteStream(new ByteArrayInputStream(baos.toByteArray()));
```

### Deprecation
'serialize(java.io.OutputStream, org.apache.axiom.om.OMOutputFormat)' is deprecated
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/builder/TestRootPartStreaming.java`
#### Snippet
```java
        format.setDoOptimize(true);
        ByteArrayOutputStream baos = new ByteArrayOutputStream();
        orgRoot.serialize(baos, format);

        // Parse the message and monitor the number of bytes read
```

### Deprecation
'serialize(java.io.OutputStream)' is deprecated
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/builder/TestCreateOMBuilderFromDOM.java`
#### Snippet
```java
        }
        ByteArrayOutputStream baos = new ByteArrayOutputStream();
        builder.getDocument().serialize(baos);
        InputSource actual = new InputSource();
        actual.setByteStream(new ByteArrayInputStream(baos.toByteArray()));
```

### Deprecation
'serialize(java.io.OutputStream)' is deprecated
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/builder/TestMalformedDocument.java`
#### Snippet
```java
                        .getDocument();
        try {
            document.serialize(new ByteArrayOutputStream());
            fail("Expected exception");
        } catch (Exception ex) {
```

### Deprecation
'serialize(java.io.OutputStream, org.apache.axiom.om.OMOutputFormat)' is deprecated
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/builder/TestReadAttachmentBeforeRootPartComplete.java`
#### Snippet
```java
        MemoryBlob xop = Blobs.createMemoryBlob();
        OutputStream out = xop.getOutputStream();
        orgRoot.serialize(out, format);
        out.close();

```

### Deprecation
'compareStreams(java.io.InputStream, java.io.InputStream)' is deprecated
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/builder/TestReadAttachmentBeforeRootPartComplete.java`
#### Snippet
```java
        // Access the Blob
        Blob blob = text.getBlob();
        IOTestUtils.compareStreams(orgBlob.getInputStream(), blob.getInputStream());
        OMElement child2 = (OMElement) child1.getNextOMSibling();
        assertFalse(child2.isComplete());
```

### Deprecation
'serializeAndConsume(java.io.Writer)' is deprecated
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestSerializeAndConsumePartiallyBuilt.java`
#### Snippet
```java

        StringWriter out = new StringWriter();
        root.serializeAndConsume(out);
        assertEquals(xml, out.toString());
    }
```

### Deprecation
'serializeAndConsume(java.io.Writer)' is deprecated
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestSerializeAndConsumePartiallyBuilt.java`
#### Snippet
```java

/**
 * Tests that {@link OMElement#serializeAndConsume(java.io.Writer)} correctly serializes an object
 * model tree that has been partially built. This is a regression test for AXIOM-151.
 */
```

### Deprecation
'compareStreams(java.io.InputStream, java.io.InputStream)' is deprecated
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestGetTextBinary.java`
#### Snippet
```java
            element.build();
        }
        IOTestUtils.compareStreams(
                blob.getInputStream(),
                new ByteArrayInputStream(Base64.decodeBase64(element.getText())));
```

### Deprecation
'serializeAndConsume(java.io.Writer)' is deprecated
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestSerializeAndConsumeWithIncompleteDescendant.java`
#### Snippet
```java
        child.addChild(incompleteElement);
        StringWriter out = new StringWriter();
        root.serializeAndConsume(out);
        assertEquals("<root><child><elem>text</elem></child></root>", out.toString());
        assertConsumed(incompleteElement);
```

### Deprecation
'serializeAndConsume(java.io.Writer)' is deprecated
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestSerializeAndConsumeWithIncompleteDescendant.java`
#### Snippet
```java

/**
 * Tests that {@link OMElement#serializeAndConsume(java.io.Writer)} consumes incomplete descendants,
 * even if the element itself is complete (more precisely, created programmatically). This situation
 * may occur when an element obtained from {@link
```

### Deprecation
'compareStreams(java.io.InputStream, java.io.InputStream)' is deprecated
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/sr/TestGetBlobFromElement.java`
#### Snippet
```java
        assertEquals(XMLStreamReader.START_ELEMENT, reader.next());
        Blob blob = XMLStreamReaderUtils.getBlobFromElement(reader);
        IOTestUtils.compareStreams(orgBlob.getInputStream(), blob.getInputStream());
    }
}
```

### Deprecation
'serialize(java.io.OutputStream)' is deprecated
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/document/TestGetSAXResultSAXParser.java`
#### Snippet
```java
        reader.parse(new InputSource(file.getUrl().toString()));
        ByteArrayOutputStream baos = new ByteArrayOutputStream();
        document.serialize(baos);
        InputSource actual = new InputSource();
        actual.setByteStream(new ByteArrayInputStream(baos.toByteArray()));
```

### Deprecation
'serializeAndConsume(java.io.OutputStream, org.apache.axiom.om.OMOutputFormat)' is deprecated
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/document/TestSerializeWithIgnoreXMLDeclaration.java`
#### Snippet
```java
        OMOutputFormat format = new OMOutputFormat();
        format.setIgnoreXMLDeclaration(true);
        document.serializeAndConsume(baos, format);

        String xmlDocument = new String(baos.toByteArray(), StandardCharsets.UTF_8);
```

### Deprecation
'serializeAndConsume(java.io.OutputStream)' is deprecated
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/document/TestSerializeXMLDeclaration.java`
#### Snippet
```java
    protected void runTest(OMDocument document) throws Throwable {
        ByteArrayOutputStream baos = new ByteArrayOutputStream();
        document.serializeAndConsume(baos);

        String xmlDocument = new String(baos.toByteArray(), StandardCharsets.UTF_8);
```

### Deprecation
'serializeAndConsume(java.io.OutputStream)' is deprecated
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/document/TestSerializeXML11.java`
#### Snippet
```java
        ByteArrayOutputStream baos = new ByteArrayOutputStream();
        document.setXMLVersion("1.1");
        document.serializeAndConsume(baos);

        String xmlDocument = new String(baos.toByteArray(), StandardCharsets.UTF_8);
```

### Deprecation
'serializeAndConsume(java.io.OutputStream)' is deprecated
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/document/TestSerializeAndConsume.java`
#### Snippet
```java
/**
 * Tests the behavior of {@link
 * org.apache.axiom.om.OMContainer#serializeAndConsume(java.io.OutputStream)} when invoked on an
 * {@link OMDocument} instance. This is a regression test for <a
 * href="https://issues.apache.org/jira/browse/AXIOM-24">AXIOM-24</a>.
```

### Deprecation
'serializeAndConsume(java.io.OutputStream)' is deprecated
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/document/TestSerializeAndConsume.java`
#### Snippet
```java
                OMXMLBuilderFactory.createOMBuilder(factory, new StringReader("<elem>text</elem>"))
                        .getDocument();
        document.serializeAndConsume(NullOutputStream.NULL_OUTPUT_STREAM);
        assertConsumed(document);
    }
```

### Deprecation
'serializeAndConsume(java.io.OutputStream, org.apache.axiom.om.OMOutputFormat)' is deprecated
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/document/TestSerializeUTF16.java`
#### Snippet
```java
        OMOutputFormat format = new OMOutputFormat();
        format.setCharSetEncoding("UTF-16");
        document.serializeAndConsume(baos, format);

        String xmlDocument = new String(baos.toByteArray(), StandardCharsets.UTF_16);
```

### Deprecation
'serializeAndConsume(java.io.Writer)' is deprecated
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/document/TestSerializeAndConsumeWithIncompleteDescendant.java`
#### Snippet
```java
        root.addChild(incompleteElement);
        StringWriter out = new StringWriter();
        document.serializeAndConsume(out);
        assertAbout(xml()).that(out.toString()).hasSameContentAs("<root><elem>text</elem></root>");
        assertConsumed(incompleteElement);
```

### Deprecation
'serializeAndConsume(java.io.Writer)' is deprecated
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/document/TestSerializeAndConsumeWithIncompleteDescendant.java`
#### Snippet
```java

/**
 * Tests that {@link OMDocument#serializeAndConsume(java.io.Writer)} consumes incomplete
 * descendants, even if the document itself is complete (more precisely, created programmatically).
 * This situation may occur when an element obtained from {@link
```

### Deprecation
'CharacterStreamComparator(java.io.Reader)' is deprecated
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestWriteTextToWithNonDestructiveOMDataSource.java`
#### Snippet
```java
                        qname);
        Reader in = new InputStreamReader(blob.getInputStream(), StandardCharsets.US_ASCII);
        Writer out = new CharacterStreamComparator(in);
        element.writeTextTo(out, true); // cache doesn't matter here
        out.close();
```

### Deprecation
'serialize(java.io.Writer)' is deprecated
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName1DefaultPrefix.java`
#### Snippet
```java
        // the testDocument string
        StringWriter writer = new StringWriter();
        root.serialize(writer);
        String result = writer.toString();

```

### Deprecation
'serialize(java.io.Writer)' is deprecated
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName1DefaultPrefix.java`
#### Snippet
```java
        // Serialize again
        writer = new StringWriter();
        root.serialize(writer);
        result = writer.toString();

```

### Deprecation
'serialize(java.io.Writer)' is deprecated
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName3QualifiedPrefix.java`
#### Snippet
```java

        try {
            root.serialize(writer);
        } catch (Exception e) {
            // Current Behavior
```

### Deprecation
'serialize(java.io.Writer)' is deprecated
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName3QualifiedPrefix.java`
#### Snippet
```java
        // Serialize again
        writer = new StringWriter();
        root.serialize(writer);
        result = writer.toString();

```

### Deprecation
'serialize(java.io.Writer)' is deprecated
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestStringOMDataSource.java`
#### Snippet
```java
        // of the OMSourcedElement.
        StringWriter out = new StringWriter();
        parent.serialize(out);
        //        System.out.println(output);
        assertTrue(
```

### Deprecation
'serializeAndConsume(java.io.Writer)' is deprecated
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName2QualifiedPrefix.java`
#### Snippet
```java
        // the name of the element.
        StringWriter writer = new StringWriter();
        root.serializeAndConsume(writer);
        String result = writer.toString();

```

### Deprecation
'serializeAndConsume(java.io.Writer)' is deprecated
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName2DefaultPrefix.java`
#### Snippet
```java
        // the name of the element.
        StringWriter writer = new StringWriter();
        root.serializeAndConsume(writer);
        String result = writer.toString();

```

### Deprecation
'serialize(java.io.Writer)' is deprecated
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestSerializeModifiedOMSEWithNonDestructiveDataSource.java`
#### Snippet
```java

        StringWriter sw = new StringWriter();
        element.serialize(sw);
        assertTrue(sw.toString().indexOf("TEST") != -1);

```

### Deprecation
'serialize(java.io.OutputStream)' is deprecated
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestSerializeModifiedOMSEWithNonDestructiveDataSource.java`
#### Snippet
```java

        ByteArrayOutputStream baos = new ByteArrayOutputStream();
        element.serialize(baos);
        assertTrue(new String(baos.toByteArray(), StandardCharsets.UTF_8).indexOf("TEST") != -1);

```

### Deprecation
'compareStreams(java.io.Reader, java.io.Reader)' is deprecated
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestGetTextAsStreamWithNonDestructiveOMDataSource.java`
#### Snippet
```java
        Reader in = element.getTextAsStream(true);
        assertFalse(in instanceof StringReader);
        IOTestUtils.compareStreams(new InputStreamReader(blob.getInputStream(), cs), in);
        assertFalse(element.isExpanded());
    }
```

### Deprecation
'serializeAndConsume(java.io.Writer)' is deprecated
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName4QualifiedPrefix.java`
#### Snippet
```java
        // the name of the element.
        StringWriter writer = new StringWriter();
        root.serializeAndConsume(writer);
        String result = writer.toString();

```

### Deprecation
'serialize(java.io.Writer)' is deprecated
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName1Unqualified.java`
#### Snippet
```java
        // testDocument string
        StringWriter writer = new StringWriter();
        root.serialize(writer);
        String result = writer.toString();

```

### Deprecation
'serialize(java.io.Writer)' is deprecated
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName1Unqualified.java`
#### Snippet
```java
        // Serialize again
        writer = new StringWriter();
        root.serialize(writer);
        result = writer.toString();

```

### Deprecation
'serializeAndConsume(java.io.Writer)' is deprecated
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName4DefaultPrefix.java`
#### Snippet
```java
        // the name of the element.
        StringWriter writer = new StringWriter();
        root.serializeAndConsume(writer);
        String result = writer.toString();

```

### Deprecation
'serializeAndConsume(java.io.Writer)' is deprecated
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName4Unqualified.java`
#### Snippet
```java
        // the name of the element.
        StringWriter writer = new StringWriter();
        root.serializeAndConsume(writer);
        String result = writer.toString();

```

### Deprecation
'serialize(java.io.Writer)' is deprecated
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName3Unqualified.java`
#### Snippet
```java

        try {
            root.serialize(writer);
        } catch (Exception e) {
            // Current Behavior
```

### Deprecation
'serialize(java.io.Writer)' is deprecated
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName3Unqualified.java`
#### Snippet
```java
        // Serialize again
        writer = new StringWriter();
        root.serialize(writer);
        result = writer.toString();

```

### Deprecation
'serializeAndConsume(java.io.Writer)' is deprecated
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName2Unqualified.java`
#### Snippet
```java
        // the name of the element.
        StringWriter writer = new StringWriter();
        root.serializeAndConsume(writer);
        String result = writer.toString();

```

### Deprecation
'serialize(java.io.Writer)' is deprecated
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName1QualifiedPrefix.java`
#### Snippet
```java
        // testDocument string
        StringWriter writer = new StringWriter();
        root.serialize(writer);
        String result = writer.toString();

```

### Deprecation
'serialize(java.io.Writer)' is deprecated
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName1QualifiedPrefix.java`
#### Snippet
```java
        // Serialize again
        writer = new StringWriter();
        root.serialize(writer);
        result = writer.toString();

```

### Deprecation
'compareStreams(java.io.InputStream, java.io.InputStream)' is deprecated
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/push/WriteBlobScenario.java`
#### Snippet
```java
            // TODO: this will only work if a single text node was created
            child.setBinary(true);
            IOTestUtils.compareStreams(blob.getInputStream(), child.getBlob().getInputStream());
        }
    }
```

### Deprecation
'serialize(java.io.Writer)' is deprecated
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName3DefaultPrefix.java`
#### Snippet
```java

        try {
            root.serialize(writer);
        } catch (Exception e) {
            // Current Behavior
```

### Deprecation
'serialize(java.io.Writer)' is deprecated
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName3DefaultPrefix.java`
#### Snippet
```java
        // Serialize again
        writer = new StringWriter();
        root.serialize(writer);
        result = writer.toString();

```

### Deprecation
'compareStreams(java.io.InputStream, java.io.InputStream)' is deprecated
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/push/WriteBlobProviderScenario.java`
#### Snippet
```java
        } else {
            child.setBinary(true);
            IOTestUtils.compareStreams(blob.getInputStream(), child.getBlob().getInputStream());
        }
    }
```

### Deprecation
'getHeaderBlocksWithNSURI(java.lang.String)' is deprecated
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/header/TestGetHeaderBlocksWithNSURIWithParser.java`
#### Snippet
```java
    protected void runTest(SOAPEnvelope envelope) throws Throwable {
        ArrayList<SOAPHeaderBlock> headerBlocks =
                envelope.getHeader().getHeaderBlocksWithNSURI("urn:ns2");
        assertThat(headerBlocks).hasSize(2);
        assertThat(headerBlocks.get(0).getQName()).isEqualTo(new QName("urn:ns2", "h4"));
```

### Deprecation
'getHeaderBlocksWithNSURI(java.lang.String)' is deprecated
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/header/TestGetHeaderBlocksWithNSURI.java`
#### Snippet
```java
                "echoOk2", soapFactory.createOMNamespace("http://www.test1.org", "test1"));
        ArrayList<SOAPHeaderBlock> arrayList =
                soapHeader.getHeaderBlocksWithNSURI("http://www.test1.org");
        assertEquals(
                "SOAP Header Test : - getHeaderBlocksWithNSURI returns an arrayList of incorrect size",
```

### Deprecation
'getHeaderBlocksWithNSURI(java.lang.String)' is deprecated
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/builder/TestRegisterCustomBuilder.java`
#### Snippet
```java
                        new BlobOMDataSourceCustomBuilder(MemoryBlob.FACTORY, "utf-8"));
        SOAPHeader header = builder.getSOAPEnvelope().getHeader();
        ArrayList al = header.getHeaderBlocksWithNSURI("http://www.w3.org/2005/08/addressing");
        assertEquals(al.size(), 4);
        for (int i = 0; i < al.size(); i++) {
```

### Deprecation
'serialize(java.io.OutputStream, org.apache.axiom.om.OMOutputFormat)' is deprecated
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/message/TestGetCharsetEncodingWithParser.java`
#### Snippet
```java
        OMOutputFormat format = new OMOutputFormat();
        format.setCharSetEncoding(encoding);
        orgEnvelope.serialize(baos, format);
        SOAPMessage message =
                OMXMLBuilderFactory.createSOAPModelBuilder(
```

### Deprecation
'serialize(java.io.OutputStream)' is deprecated
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/envelope/TestSerializeAsChild.java`
#### Snippet
```java
        MemoryBlob blob = Blobs.createMemoryBlob();
        OutputStream out = blob.getOutputStream();
        envelope.serialize(out);
        out.close();
        assertAbout(xml())
```

### Deprecation
'serialize(java.io.Writer, org.apache.axiom.om.OMOutputFormat)' is deprecated
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/envelope/TestSerializeAndConsumeWithOMSEInBody.java`
#### Snippet
```java

        /**
         * @see org.apache.axiom.om.OMDataSource#serialize(java.io.Writer,
         *     org.apache.axiom.om.OMOutputFormat)
         */
```

### Deprecation
'serializeAndConsume(java.io.Writer)' is deprecated
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/envelope/TestSerializeAndConsumeWithOMSEInBody.java`
#### Snippet
```java
        body.addChild(payload);
        StringWriter writer = new StringWriter();
        envelope.serializeAndConsume(writer);
        //        System.out.println(writer);
    }
```

### Deprecation
'serialize(java.io.OutputStream, org.apache.axiom.om.OMOutputFormat)' is deprecated
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/envelope/TestSerializeAndConsumeWithOMSEInBody.java`
#### Snippet
```java

        /**
         * @see org.apache.axiom.om.OMDataSource#serialize(java.io.OutputStream,
         *     org.apache.axiom.om.OMOutputFormat)
         */
```

### Deprecation
'serialize(java.io.Writer)' is deprecated
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/faultdetail/TestSerialization.java`
#### Snippet
```java
        soapFaultDetail.addDetailEntry(soapFactory.createOMElement("DetailEntry2", omNamespace));
        StringWriter out = new StringWriter();
        soapFaultDetail.serialize(out);
        String msg = out.toString();
        assertTrue(msg.indexOf("DetailEntry1") != -1);
```

### Deprecation
'compareStreams(java.io.InputStream, java.io.InputStream)' is deprecated
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/omdom/text/TestGetNodeValueBinary.java`
#### Snippet
```java
        String nodeValue = text.getNodeValue();
        assertThat(nodeValue).isNotNull();
        IOTestUtils.compareStreams(
                blob.getInputStream(), new ByteArrayInputStream(Base64.decodeBase64(nodeValue)));
    }
```

### Deprecation
'compareStreams(java.io.InputStream, java.io.InputStream)' is deprecated
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/mtom/TestBuilderDetach.java`
#### Snippet
```java
                OMText text = (OMText) node;
                if (text.isBinary()) {
                    IOTestUtils.compareStreams(
                            sample.getPart(text.getContentID()), text.getBlob().getInputStream());
                    binaryCount++;
```

### Deprecation
'compareStreams(java.io.InputStream, java.io.InputStream)' is deprecated
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/envelope/TestBuildWithAttachments.java`
#### Snippet
```java
        OMElement image2 = it.next();

        IOTestUtils.compareStreams(
                ((OMText) image1.getFirstOMChild()).getBlob().getInputStream(), sample.getPart(1));

```

### Deprecation
'compareStreams(java.io.InputStream, java.io.InputStream)' is deprecated
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/envelope/TestBuildWithAttachments.java`
#### Snippet
```java
                ((OMText) image1.getFirstOMChild()).getBlob().getInputStream(), sample.getPart(1));

        IOTestUtils.compareStreams(
                ((OMText) image2.getFirstOMChild()).getBlob().getInputStream(), sample.getPart(2));
    }
```

### Deprecation
'serialize(java.io.OutputStream, org.apache.axiom.om.OMOutputFormat)' is deprecated
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/envelope/TestMTOMForwardStreaming.java`
#### Snippet
```java
                            try {
                                try {
                                    orgEnvelope.serialize(pipe1Out, format);
                                } finally {
                                    pipe1Out.close();
```

### Deprecation
'serializeAndConsume(java.io.OutputStream, org.apache.axiom.om.OMOutputFormat)' is deprecated
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/envelope/TestMTOMForwardStreaming.java`
#### Snippet
```java
                                    }
                                    // Usage of serializeAndConsume should enable streaming
                                    envelope.serializeAndConsume(pipe2Out, format);
                                } finally {
                                    pipe2Out.close();
```

### Deprecation
'compareStreams(java.io.InputStream, java.io.InputStream)' is deprecated
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/envelope/TestMTOMForwardStreaming.java`
#### Snippet
```java
            OMElement data2 = it.next();

            IOTestUtils.compareStreams(
                    blob1.getInputStream(),
                    ((PartBlob) ((OMText) data1.getFirstOMChild()).getBlob())
```

### Deprecation
'compareStreams(java.io.InputStream, java.io.InputStream)' is deprecated
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/envelope/TestMTOMForwardStreaming.java`
#### Snippet
```java
                            .getPart()
                            .getInputStream(false));
            IOTestUtils.compareStreams(
                    blob2.getInputStream(),
                    ((PartBlob) ((OMText) data2.getFirstOMChild()).getBlob())
```

### Deprecation
'serializeAndConsume(java.io.OutputStream, org.apache.axiom.om.OMOutputFormat)' is deprecated
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/envelope/TestMTOMForwardStreaming.java`
#### Snippet
```java
/**
 * Tests that attachments are streamed (i.e. not read entirely into memory) if the original message
 * was read from an input stream and {@link OMElement#serializeAndConsume(java.io.OutputStream,
 * OMOutputFormat)} is used to serialize the message. This feature is important because it allows
 * projects such as Synapse to forward MTOM messages very efficiently.
```

### Deprecation
'serialize(java.io.Writer)' is deprecated
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/dimension/serialization/SerializeFromXMLStreamReader.java`
#### Snippet
```java
                OMXMLBuilderFactory.createStAXOMBuilder(
                        container.getOMFactory(), container.getXMLStreamReader(cache));
        builder.getDocument().serialize(sw);
        builder.close();
        return new XMLAsString(sw.toString());
```

### Deprecation
'serialize(java.io.Writer)' is deprecated
in `mixins/om-mixins/src/main/java/org/apache/axiom/om/impl/mixin/AxiomElementMixin.java`
#### Snippet
```java
        StringWriter sw = new StringWriter();
        try {
            serialize(sw);
        } catch (XMLStreamException ex) {
            throw new OMException("Failed to serialize node", ex);
```

### Deprecation
'serializeAndConsume(java.io.Writer)' is deprecated
in `mixins/om-mixins/src/main/java/org/apache/axiom/om/impl/mixin/AxiomElementMixin.java`
#### Snippet
```java
    public final String toStringWithConsume() throws XMLStreamException {
        StringWriter sw = new StringWriter();
        serializeAndConsume(sw);
        return sw.toString();
    }
```

## RuleId[id=UnnecessaryReturn]
### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/builder/BadInputTest.java`
#### Snippet
```java
            fail("this must failed gracefully with SOAPProcessingException");
        } catch (SOAPProcessingException e) {
            return;
        }
    }
```

## RuleId[id=FinalStaticMethod]
### FinalStaticMethod
'static' method declared `final`
in `axiom-compat/src/main/java/org/apache/axiom/om/util/CommonUtils.java`
#### Snippet
```java
     * @return replacement string
     */
    public static final String replace(String name,
                                       String oldT, String newT) {

```

## RuleId[id=SimplifiableConditionalExpression]
### SimplifiableConditionalExpression
`super.getEventType() == SPACE ? true : super.isCharacters()` can be simplified to 'super.getEventType() == SPACE \|\| super.isCharacters()'
in `testing/testutils/src/main/java/org/apache/axiom/testutils/stax/SpaceAsCharactersXMLStreamReaderFilter.java`
#### Snippet
```java
    @Override
    public boolean isCharacters() {
        return super.getEventType() == SPACE ? true : super.isCharacters();
    }

```

### SimplifiableConditionalExpression
`expandEntityReferences == null
? false
: expandEntityReferences.booleanValue()` can be simplified to 'expandEntityReferences!=null \&\& expandEntityReferences.booleanValue()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/builder/TestCreateOMBuilderFromSAXSource.java`
#### Snippet
```java
                .ignoringWhitespaceInPrologAndEpilog()
                .expandingEntityReferences(
                        expandEntityReferences == null
                                ? false
                                : expandEntityReferences.booleanValue())
```

### SimplifiableConditionalExpression
`(body == null) ? false : body.hasFault()` can be simplified to 'body!=null \&\& body.hasFault()'
in `mixins/om-mixins/src/main/java/org/apache/axiom/soap/impl/mixin/AxiomSOAPEnvelopeMixin.java`
#### Snippet
```java
    public final boolean hasFault() {
        SOAPBody body = getBody();
        return (body == null) ? false : body.hasFault();
    }

```

## RuleId[id=RedundantTypeArguments]
### RedundantTypeArguments
Explicit type arguments can be inferred
in `mixins/dom-mixins/src/main/java/org/apache/axiom/dom/impl/mixin/DOMDocumentMixin.java`
#### Snippet
```java
                                    Axis.DESCENDANTS,
                                    DOMElement.class,
                                    Mappers.<DOMElement>identity(),
                                    DOMSemantics.INSTANCE);
                    it.hasNext(); ) {
```

### RedundantTypeArguments
Explicit type arguments can be inferred
in `mixins/om-mixins/src/main/java/org/apache/axiom/om/impl/mixin/AxiomDocumentMixin.java`
#### Snippet
```java
                includeSelf ? Axis.DESCENDANTS_OR_SELF : Axis.DESCENDANTS,
                AxiomSerializable.class,
                Mappers.<OMSerializable>identity(),
                AxiomSemantics.INSTANCE);
    }
```

### RedundantTypeArguments
Explicit type arguments can be inferred
in `mixins/om-mixins/src/main/java/org/apache/axiom/om/impl/mixin/AxiomElementMixin.java`
#### Snippet
```java
                null,
                null,
                Mappers.<OMElement>identity(),
                AxiomSemantics.INSTANCE);
    }
```

### RedundantTypeArguments
Explicit type arguments can be inferred
in `mixins/om-mixins/src/main/java/org/apache/axiom/om/impl/mixin/AxiomElementMixin.java`
#### Snippet
```java
    public final Iterator<OMAttribute> getAllAttributes() {
        return coreGetAttributesByType(
                AxiomAttribute.class, Mappers.<OMAttribute>identity(), AxiomSemantics.INSTANCE);
    }

```

### RedundantTypeArguments
Explicit type arguments can be inferred
in `mixins/om-mixins/src/main/java/org/apache/axiom/om/impl/mixin/AxiomElementMixin.java`
#### Snippet
```java
                includeSelf ? Axis.DESCENDANTS_OR_SELF : Axis.DESCENDANTS,
                AxiomChildNode.class,
                Mappers.<OMNode>identity(),
                AxiomSemantics.INSTANCE);
    }
```

### RedundantTypeArguments
Explicit type arguments can be inferred
in `mixins/om-mixins/src/main/java/org/apache/axiom/om/impl/mixin/AxiomContainerMixin.java`
#### Snippet
```java
                null,
                localName,
                Mappers.<OMElement>identity(),
                AxiomSemantics.INSTANCE);
    }
```

### RedundantTypeArguments
Explicit type arguments can be inferred
in `mixins/om-mixins/src/main/java/org/apache/axiom/om/impl/mixin/AxiomContainerMixin.java`
#### Snippet
```java
                Axis.CHILDREN,
                AxiomChildNode.class,
                Mappers.<OMNode>identity(),
                AxiomSemantics.INSTANCE);
    }
```

### RedundantTypeArguments
Explicit type arguments can be inferred
in `mixins/om-mixins/src/main/java/org/apache/axiom/om/impl/mixin/AxiomContainerMixin.java`
#### Snippet
```java
                name.getNamespaceURI(),
                name.getLocalPart(),
                Mappers.<OMElement>identity(),
                AxiomSemantics.INSTANCE);
    }
```

### RedundantTypeArguments
Explicit type arguments can be inferred
in `mixins/om-mixins/src/main/java/org/apache/axiom/om/impl/mixin/AxiomContainerMixin.java`
#### Snippet
```java
                uri,
                null,
                Mappers.<OMElement>identity(),
                AxiomSemantics.INSTANCE);
    }
```

## RuleId[id=NullableProblems]
### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `components/blob/src/main/java/org/apache/axiom/blob/MemoryBlobOutputStream.java`
#### Snippet
```java

    @Override
    public abstract void write(byte[] b);

    @Override
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `components/blob/src/main/java/org/apache/axiom/blob/MemoryBlobOutputStream.java`
#### Snippet
```java

    @Override
    public abstract void write(byte[] b, int off, int len);

    @Override
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `components/blob/src/main/java/org/apache/axiom/blob/MemoryBlobInputStream.java`
#### Snippet
```java

    @Override
    public abstract int read(byte[] b);

    @Override
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `components/blob/src/main/java/org/apache/axiom/blob/MemoryBlobInputStream.java`
#### Snippet
```java

    @Override
    public abstract int read(byte[] b, int off, int len);

    @Override
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `testing/testutils/src/main/java/org/apache/axiom/testutils/io/ByteStreamComparator.java`
#### Snippet
```java

    @Override
    public void write(byte[] buffer, int off, int len) throws IOException {
        while (len > 0) {
            int c = in.read(compareBuffer, 0, Math.min(compareBuffer.length, len));
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `testing/testutils/src/main/java/org/apache/axiom/testutils/io/CharacterStreamComparator.java`
#### Snippet
```java

    @Override
    public void write(char[] buffer, int off, int len) throws IOException {
        while (len > 0) {
            int c = in.read(compareBuffer, 0, Math.min(compareBuffer.length, len));
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `components/blob/src/main/java/org/apache/axiom/blob/OverflowableBlobImpl.java`
#### Snippet
```java

        @Override
        public void write(byte[] b) throws IOException {
            write(b, 0, b.length);
        }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `components/blob/src/main/java/org/apache/axiom/blob/OverflowableBlobImpl.java`
#### Snippet
```java
    class OutputStreamImpl extends OutputStream implements ReadFromSupport {
        @Override
        public void write(byte[] b, int off, int len) throws IOException {
            if (state != State.UNCOMMITTED) {
                throw new IllegalStateException();
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `components/blob/src/main/java/org/apache/axiom/blob/OverflowableBlobImpl.java`
#### Snippet
```java

        @Override
        public int read(byte[] b) throws IOException {
            return read(b, 0, b.length);
        }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `components/blob/src/main/java/org/apache/axiom/blob/OverflowableBlobImpl.java`
#### Snippet
```java

        @Override
        public int read(byte[] b, int off, int len) throws IOException {

            if (len == 0) {
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `axiom-api/src/main/java/org/apache/axiom/mime/MultipartBodyWriter.java`
#### Snippet
```java

        @Override
        public void write(byte[] b, int off, int len) throws IOException {
            parent.write(b, off, len);
        }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `axiom-api/src/main/java/org/apache/axiom/mime/MultipartBodyWriter.java`
#### Snippet
```java

        @Override
        public void write(byte[] b) throws IOException {
            parent.write(b);
        }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `axiom-api/src/main/java/org/apache/axiom/mime/DebugInputStream.java`
#### Snippet
```java

    @Override
    public int read(byte[] b, int off, int len) throws IOException {
        int c;
        try {
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `axiom-api/src/main/java/org/apache/axiom/mime/PartInputStream.java`
#### Snippet
```java

    @Override
    public int read(byte[] b) throws IOException {
        if (in == null) {
            return -1;
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `axiom-api/src/main/java/org/apache/axiom/mime/PartInputStream.java`
#### Snippet
```java

    @Override
    public int read(byte[] b, int off, int len) throws IOException {
        if (in == null) {
            return -1;
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `axiom-api/src/main/java/org/apache/axiom/mime/MultipartBody.java`
#### Snippet
```java

    @Override
    public Iterator<Part> iterator() {
        return new PartIterator(this);
    }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `axiom-api/src/main/java/org/apache/axiom/util/stax/XMLStreamWriterWriter.java`
#### Snippet
```java

    @Override
    public void write(String str) throws IOException {
        try {
            writer.writeCharacters(str);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `axiom-api/src/main/java/org/apache/axiom/util/stax/XMLStreamWriterWriter.java`
#### Snippet
```java

    @Override
    public void write(char[] cbuf, int off, int len) throws IOException {
        try {
            writer.writeCharacters(cbuf, off, len);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `axiom-api/src/main/java/org/apache/axiom/util/stax/TextFromElementReader.java`
#### Snippet
```java

    @Override
    public int read(char[] cbuf, int off, int len) throws IOException {
        if (endOfStream) {
            return -1;
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `axiom-api/src/main/java/org/apache/axiom/util/stax/dialect/CloseShieldReader.java`
#### Snippet
```java

    @Override
    public int read(char[] cbuf) throws IOException {
        return parent.read(cbuf);
    }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `axiom-api/src/main/java/org/apache/axiom/util/stax/dialect/CloseShieldReader.java`
#### Snippet
```java

    @Override
    public int read(char[] cbuf, int off, int len) throws IOException {
        return parent.read(cbuf, off, len);
    }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `axiom-api/src/main/java/org/apache/axiom/util/stax/dialect/CloseShieldReader.java`
#### Snippet
```java

    @Override
    public int read(CharBuffer target) throws IOException {
        return parent.read(target);
    }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `axiom-api/src/main/java/org/apache/axiom/util/stax/dialect/CloseShieldInputStream.java`
#### Snippet
```java

    @Override
    public int read(byte[] b) throws IOException {
        return parent.read(b);
    }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `axiom-api/src/main/java/org/apache/axiom/util/stax/dialect/CloseShieldInputStream.java`
#### Snippet
```java

    @Override
    public int read(byte[] b, int off, int len) throws IOException {
        return parent.read(b, off, len);
    }
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `mixins/dom-mixins/src/main/java/org/apache/axiom/dom/impl/mixin/DOMCDATASectionMixin.java`
#### Snippet
```java

    @Override
    public final String getNodeName() {
        return "#cdata-section";
    }
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `mixins/dom-mixins/src/main/java/org/apache/axiom/dom/impl/mixin/DOMCommentMixin.java`
#### Snippet
```java
public abstract class DOMCommentMixin implements DOMComment {
    @Override
    public final String getNodeName() {
        return "#comment";
    }
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `mixins/dom-mixins/src/main/java/org/apache/axiom/dom/impl/mixin/DOMDocumentFragmentMixin.java`
#### Snippet
```java

    @Override
    public final String getNodeName() {
        return "#document-fragment";
    }
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `mixins/dom-mixins/src/main/java/org/apache/axiom/dom/impl/mixin/DOMTextMixin.java`
#### Snippet
```java
public abstract class DOMTextMixin implements DOMText {
    @Override
    public final String getNodeName() {
        return "#text";
    }
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `mixins/dom-mixins/src/main/java/org/apache/axiom/dom/impl/mixin/DOMElementMixin.java`
#### Snippet
```java

    @Override
    public final NodeList getElementsByTagName(String tagname) {
        return new ElementsByTagName(this, tagname);
    }
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `mixins/dom-mixins/src/main/java/org/apache/axiom/dom/impl/mixin/DOMElementMixin.java`
#### Snippet
```java

    @Override
    public final NodeList getElementsByTagNameNS(String namespaceURI, String localName) {
        return new ElementsByTagNameNS(this, namespaceURI, localName);
    }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `components/base64-utils/src/main/java/org/apache/axiom/util/base64/AbstractBase64DecodingWriter.java`
#### Snippet
```java

    @Override
    public final void write(String str, int off, int len) throws IOException {
        while (len > 0) {
            write(str.charAt(off));
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `components/base64-utils/src/main/java/org/apache/axiom/util/base64/AbstractBase64DecodingWriter.java`
#### Snippet
```java
    
    @Override
    public final void write(char[] cbuf, int off, int len) throws IOException {
        while (len > 0) {
            write(cbuf[off]);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/CountingInputStream.java`
#### Snippet
```java

    @Override
    public int read(byte[] b) throws IOException {
        int read = parent.read(b);
        if (read != -1) {
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/CountingInputStream.java`
#### Snippet
```java

    @Override
    public int read(byte[] b, int off, int len) throws IOException {
        int read = parent.read(b, off, len);
        if (read != -1) {
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `components/base64-utils/src/main/java/org/apache/axiom/util/base64/AbstractBase64EncodingOutputStream.java`
#### Snippet
```java
    
    @Override
    public final void write(byte[] b, int off, int len) throws IOException {
        if (completed) {
            throw new IOException("Attempt to write data after base64 encoding has been completed");
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/IncomingAttachmentInputStream.java`
#### Snippet
```java

    @Override
    public int read(byte[] b, int off, int len) throws IOException {
        int retval = _stream.read(b, off, len);
        parentContainer.setReadyToGetNextStream(retval == -1);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/IncomingAttachmentInputStream.java`
#### Snippet
```java

    @Override
    public int read(byte[] b) throws IOException {
        int retval = _stream.read(b);
        parentContainer.setReadyToGetNextStream(retval == -1);
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `mixins/dom-mixins/src/main/java/org/apache/axiom/dom/impl/mixin/DOMDocumentMixin.java`
#### Snippet
```java

    @Override
    public final String getNodeName() {
        return "#document";
    }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `axiom-compat/src/main/java/org/apache/axiom/om/impl/serialize/ContentHandlerWriter.java`
#### Snippet
```java

    @Override
    public void write(char[] cbuf, int off, int len) throws IOException {
        try {
            contentHandler.characters(cbuf, off, len);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `axiom-compat/src/main/java/org/apache/axiom/om/util/LogOutputStream.java`
#### Snippet
```java

    @Override
    public void write(byte[] b) throws IOException {
        this.write(b, 0, b.length);
    }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `axiom-compat/src/main/java/org/apache/axiom/om/util/LogOutputStream.java`
#### Snippet
```java

    @Override
    public void write(byte[] b, int off, int len) throws IOException {
       
        // Adjust total count 
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `axiom-compat/src/main/java/org/apache/axiom/util/blob/MemoryBlob.java`
#### Snippet
```java

        @Override
        public int read(byte[] b, int off, int len) throws IOException {
            int size = (int)getLength();
            int total = 0;
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `axiom-compat/src/main/java/org/apache/axiom/util/blob/MemoryBlob.java`
#### Snippet
```java

        @Override
        public void write(byte[] b) throws IOException {
            this.write(b, 0, b.length);
        }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `axiom-compat/src/main/java/org/apache/axiom/util/blob/MemoryBlob.java`
#### Snippet
```java

        @Override
        public int read(byte[] b) throws IOException {
            return this.read(b, 0, b.length);
        }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `axiom-compat/src/main/java/org/apache/axiom/util/blob/MemoryBlob.java`
#### Snippet
```java

        @Override
        public void write(byte[] b, int off, int len) throws IOException {
           int total = 0;
           while (total < len) {
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `axiom-compat/src/main/java/org/apache/axiom/util/blob/OverflowBlob.java`
#### Snippet
```java

        @Override
        public void write(byte[] b, int off, int len) throws IOException {

            if (fileOutputStream != null) {
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `axiom-compat/src/main/java/org/apache/axiom/util/blob/OverflowBlob.java`
#### Snippet
```java

        @Override
        public int read(byte[] b, int off, int len) throws IOException {

            if (len == 0) {
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `axiom-compat/src/main/java/org/apache/axiom/util/blob/OverflowBlob.java`
#### Snippet
```java

        @Override
        public void write(byte[] b) throws IOException {
            write(b, 0, b.length);
        }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `axiom-compat/src/main/java/org/apache/axiom/util/blob/OverflowBlob.java`
#### Snippet
```java

        @Override
        public int read(byte[] b) throws IOException {
            return read(b, 0, b.length);
        }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `mixins/om-mixins/src/main/java/org/apache/axiom/om/impl/common/factory/meta/DetachableReader.java`
#### Snippet
```java

    @Override
    public int read(char[] cbuf, int off, int len) throws IOException {
        return target.read(cbuf, off, len);
    }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `mixins/om-mixins/src/main/java/org/apache/axiom/om/impl/common/factory/meta/DetachableReader.java`
#### Snippet
```java

    @Override
    public int read(char[] cbuf) throws IOException {
        return target.read(cbuf);
    }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `mixins/om-mixins/src/main/java/org/apache/axiom/om/impl/common/factory/meta/DetachableInputStream.java`
#### Snippet
```java

    @Override
    public int read(byte[] b) throws IOException {
        return target.read(b);
    }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `mixins/om-mixins/src/main/java/org/apache/axiom/om/impl/common/factory/meta/DetachableInputStream.java`
#### Snippet
```java

    @Override
    public int read(byte[] b, int off, int len) throws IOException {
        return target.read(b, off, len);
    }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `mixins/om-mixins/src/main/java/org/apache/axiom/om/impl/stream/xop/CountingOutputStream.java`
#### Snippet
```java

    @Override
    public void write(byte[] b, int off, int len) throws IOException {
        size += len;
        checkSize();
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `components/core-streams/src/main/java/org/apache/axiom/core/stream/sax/output/ContentHandlerWriter.java`
#### Snippet
```java

    @Override
    public void write(char[] cbuf, int off, int len) throws IOException {
        try {
            contentHandler.characters(cbuf, off, len);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `components/core-streams/src/main/java/org/apache/axiom/core/stream/serializer/SerializerWriter.java`
#### Snippet
```java

    @Override
    public void write(char[] cbuf, int off, int len) throws IOException {
        try {
            serializer.characters(cbuf, off, len);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `components/core-streams/src/main/java/org/apache/axiom/core/stream/serializer/SerializerWriter.java`
#### Snippet
```java

    @Override
    public void write(String str, int off, int len) throws IOException {
        try {
            serializer.characters(str, off, len);
```

## RuleId[id=ExplicitArrayFilling]
### ExplicitArrayFilling
Can be replaced with single 'Arrays.fill()' method call
in `components/base64-utils/src/main/java/org/apache/axiom/util/base64/Base64Constants.java`
#### Snippet
```java
    
    static {
        for (int i = 0; i < S_DECODETABLE.length; i++) {
            S_DECODETABLE[i] = INVALID;
        }
```

## RuleId[id=JavadocLinkAsPlainText]
### JavadocLinkAsPlainText
Link specified as plain text
in `axiom-api/src/main/java/org/apache/axiom/om/OMXMLStreamReaderConfiguration.java`
#### Snippet
```java
     * 
     * <pre>
     * &lt;soapenv:Envelope xmlns:soapenv="http://schemas.xmlsoap.org/soap/envelope/"
     *                   xmlns:xsd="http://www.w3.org/2001/XMLSchema"
     *                   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"&gt;
```

### JavadocLinkAsPlainText
Link specified as plain text
in `axiom-api/src/main/java/org/apache/axiom/om/OMXMLStreamReaderConfiguration.java`
#### Snippet
```java
     * <pre>
     * &lt;soapenv:Envelope xmlns:soapenv="http://schemas.xmlsoap.org/soap/envelope/"
     *                   xmlns:xsd="http://www.w3.org/2001/XMLSchema"
     *                   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"&gt;
     *   &lt;soapenv:Body&gt;
```

### JavadocLinkAsPlainText
Link specified as plain text
in `axiom-api/src/main/java/org/apache/axiom/om/OMXMLStreamReaderConfiguration.java`
#### Snippet
```java
     * &lt;soapenv:Envelope xmlns:soapenv="http://schemas.xmlsoap.org/soap/envelope/"
     *                   xmlns:xsd="http://www.w3.org/2001/XMLSchema"
     *                   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"&gt;
     *   &lt;soapenv:Body&gt;
     *     &lt;ns:echo xmlns:ns="urn:test"&gt;
```

### JavadocLinkAsPlainText
Link specified as plain text
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAPFaultRole.java`
#### Snippet
```java
 * <p>
 * The Role element information item has: A [local name] of Role . A [namespace name] of
 * http://www.w3.org/2003/05/soap-envelope .
 */
public interface SOAPFaultRole extends OMElement {
```

### JavadocLinkAsPlainText
Link specified as plain text
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAPFaultDetail.java`
#### Snippet
```java
 * <p>
 * The Detail element information item has: A [local name] of Detail . A [namespace name] of
 * http://www.w3.org/2003/05/soap-envelope . Zero or more attribute information items in its
 * [attributes] property. Zero or more child element information items in its [children] property.
 */
```

## RuleId[id=FieldCanBeLocal]
### FieldCanBeLocal
Field can be converted to a local variable
in `axiom-api/src/main/java/org/apache/axiom/util/stax/debug/XMLStreamReaderValidator.java`
#### Snippet
```java

    private static final Log log = LogFactory.getLog(XMLStreamReaderValidator.class);
    private static boolean IS_ADV_DEBUG_ENABLED = false;  // Turn this on to trace every event
    
    private boolean throwExceptions = false;   // Indicates whether OMException should be thrown if errors are disovered
```

### FieldCanBeLocal
Field can be converted to a local variable
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/CachedFileDataSource.java`
#### Snippet
```java
    
    // Represents the absolute pathname of cached attachment file
    private String cachedFileName = null;

    public CachedFileDataSource(File file) {
```

### FieldCanBeLocal
Field can be converted to a local variable
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/lifecycle/impl/FileAccessor.java`
#### Snippet
```java
    File file = null;
    LifecycleManager manager;
    private int accessCount = 0;
    public FileAccessor(LifecycleManager manager, File file) {
        super();
```

### FieldCanBeLocal
Field can be converted to a local variable
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/envelope/TestSerializeAndConsumeWithOMSEInBody.java`
#### Snippet
```java
        private XMLStreamReader reader;
        // private String xml = "<?xml version='1.0'?><d:dummy name='1' xmlns:d='http://ns1'/>";
        private String xml =
                "<?xml version='1.0'?><d:dummy name='1' xmlns:d='http://ns1'>hello<mixed/>world</d:dummy>";

```

### FieldCanBeLocal
Field can be converted to a local variable
in `mixins/om-mixins/src/main/java/org/apache/axiom/soap/impl/common/builder/SOAP11BuilderHelper.java`
#### Snippet
```java
public class SOAP11BuilderHelper extends SOAPBuilderHelper implements SOAP11Constants {
    private boolean faultcodePresent = false;
    private boolean faultstringPresent = false;

    @Override
```

### FieldCanBeLocal
Field can be converted to a local variable
in `mixins/om-mixins/src/main/java/org/apache/axiom/soap/impl/common/builder/SOAP11BuilderHelper.java`
#### Snippet
```java

public class SOAP11BuilderHelper extends SOAPBuilderHelper implements SOAP11Constants {
    private boolean faultcodePresent = false;
    private boolean faultstringPresent = false;

```

## RuleId[id=SpringInjectionValueStyleInspection]
### SpringInjectionValueStyleInspection
Usage of explicit element
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/broker/server.xml`
#### Snippet
```java
        <property name="defaultFault" value="SERVER"/>
        <property name="exceptionMappings">
            <value>
                org.apache.axiom.ts.springws.scenario.broker.UnknownCustomerException=CLIENT
            </value>
        </property>
    </bean>
```

## RuleId[id=IgnoreResultOfCall]
### IgnoreResultOfCall
Result of `File.delete()` is ignored
in `components/blob/src/main/java/org/apache/axiom/blob/TempFileBlobImpl.java`
#### Snippet
```java
                log.debug("Blob was created here", trace);
            }
            file.delete();
        }
    }
```

### IgnoreResultOfCall
Result of `AttributeIterator.hasNext()` is ignored
in `mixins/core-mixins/src/main/java/org/apache/axiom/core/impl/AttributeIterator.java`
#### Snippet
```java
        } else {
            // Ensure that the next attribute is known before we remove the current one.
            hasNext();
            currentAttribute.coreRemove(semantics);
            currentAttribute = null;
```

### IgnoreResultOfCall
Result of `File.delete()` is ignored
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/lifecycle/impl/VMShutdownHook.java`
#### Snippet
```java
                log.debug("Deleting File from Shutdown Hook Collection"+file.getAbsolutePath());
            }    		
            file.delete();
        }    
        if(log.isDebugEnabled()){
```

### IgnoreResultOfCall
Result of `File.delete()` is ignored
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/lifecycle/impl/LifecycleManagerImpl.java`
#### Snippet
```java
                if(_file.exists()){
                    table.remove(_file.getName());
                    _file.delete();
                }
            }catch(InterruptedException e){
```

### IgnoreResultOfCall
Result of `File.mkdirs()` is ignored
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/lifecycle/impl/LifecycleManagerImpl.java`
#### Snippet
```java
            dir = new File(attachmentDir);
            if (!dir.exists()) {
                dir.mkdirs();
            }
        }
```

### IgnoreResultOfCall
Result of `OMAbstractIterator.hasNext()` is ignored
in `axiom-compat/src/main/java/org/apache/axiom/om/impl/traverse/OMAbstractIterator.java`
#### Snippet
```java
        }
        // Make sure that we know the next node before removing the current one
        hasNext();
        currentNode.detach();
        nextCalled = false;
```

### IgnoreResultOfCall
Result of `File.mkdirs()` is ignored
in `buildutils/buildutils-maven-plugin/src/main/java/org/apache/axiom/buildutils/pom/CreateDependencyReducedPomMojo.java`
#### Snippet
```java
            }
        }
        dependencyReducedPomLocation.getParentFile().mkdirs();
        MavenXpp3Writer pomWriter = new MavenXpp3Writer();
        try (OutputStream out = new FileOutputStream(dependencyReducedPomLocation)) {
```

### IgnoreResultOfCall
Result of `File.delete()` is ignored
in `axiom-compat/src/main/java/org/apache/axiom/util/blob/OverflowBlob.java`
#### Snippet
```java
                log.debug("Deleting temporary file " + temporaryFile);
            }
            temporaryFile.delete();
        }
    }
```

### IgnoreResultOfCall
Result of `File.delete()` is ignored
in `axiom-compat/src/main/java/org/apache/axiom/util/blob/OverflowBlob.java`
#### Snippet
```java
        if (temporaryFile != null) {
            log.warn("Cleaning up unreleased temporary file " + temporaryFile);
            temporaryFile.delete();
        }
    }
```

### IgnoreResultOfCall
Result of `File.delete()` is ignored
in `buildutils/buildutils-maven-plugin/src/main/java/org/apache/axiom/buildutils/sources/PostProcessMojo.java`
#### Snippet
```java
            throw new MojoExecutionException("Error writing jar: " + ex.getMessage(), ex);
        }
        sourcesJar.delete();
        postProcessedSourcesJar.renameTo(sourcesJar);
    }
```

### IgnoreResultOfCall
Result of `File.renameTo()` is ignored
in `buildutils/buildutils-maven-plugin/src/main/java/org/apache/axiom/buildutils/sources/PostProcessMojo.java`
#### Snippet
```java
        }
        sourcesJar.delete();
        postProcessedSourcesJar.renameTo(sourcesJar);
    }

```

### IgnoreResultOfCall
Result of `File.delete()` is ignored
in `testing/jaxp-testsuite/src/main/java/org/apache/axiom/ts/jaxp/stax/StAXImplementation.java`
#### Snippet
```java
                                        () -> {
                                            IOUtils.closeQuietly(classLoader);
                                            jar.delete();
                                        }));
                final Properties props = loadProperties(jarName);
```

### IgnoreResultOfCall
Result of `File.mkdirs()` is ignored
in `buildutils/axiom-weaver-maven-plugin/src/main/java/org/apache/axiom/weaver/maven/WeaveMojo.java`
#### Snippet
```java
                    log.debug(String.format("Generating %s:\n%s", outputFile, sw));
                }
                outputFile.getParentFile().mkdirs();
                try (FileOutputStream out = new FileOutputStream(outputFile)) {
                    out.write(classDefinition.toByteArray());
```

## RuleId[id=IntegerMultiplicationImplicitCastToLong]
### IntegerMultiplicationImplicitCastToLong
chunkIndex \* chunkSize: integer multiplication implicitly cast to long
in `components/blob/src/main/java/org/apache/axiom/blob/OverflowableBlobImpl.java`
#### Snippet
```java
            return overflowBlob.getSize();
        } else {
            return chunkIndex * chunkSize + chunkOffset;
        }
    }
```

### IntegerMultiplicationImplicitCastToLong
thresholdSecs\*1000: integer multiplication implicitly cast to long
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/AttachmentCacheMonitor.java`
#### Snippet
```java
        long elapse = newTimeMillis.longValue() -
            oldTimeMillis.longValue();
        return (elapse > (thresholdSecs*1000));
    }

```

### IntegerMultiplicationImplicitCastToLong
refreshSeconds \* 1000: integer multiplication implicitly cast to long
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/AttachmentCacheMonitor.java`
#### Snippet
```java
            timer = new Timer( true );
            timer.schedule( new CleanupFilesTask(), 
                    refreshSeconds * 1000, 
                    refreshSeconds * 1000 );
        }
```

### IntegerMultiplicationImplicitCastToLong
refreshSeconds \* 1000: integer multiplication implicitly cast to long
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/AttachmentCacheMonitor.java`
#### Snippet
```java
            timer.schedule( new CleanupFilesTask(), 
                    refreshSeconds * 1000, 
                    refreshSeconds * 1000 );
        }
    }
```

### IntegerMultiplicationImplicitCastToLong
refreshSeconds \* 1000: integer multiplication implicitly cast to long
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/AttachmentCacheMonitor.java`
#### Snippet
```java
        	timer = new Timer( true );
            timer.schedule( new CleanupFilesTask(), 
                    refreshSeconds * 1000, 
                    refreshSeconds * 1000 );
        }
```

### IntegerMultiplicationImplicitCastToLong
refreshSeconds \* 1000: integer multiplication implicitly cast to long
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/AttachmentCacheMonitor.java`
#### Snippet
```java
            timer.schedule( new CleanupFilesTask(), 
                    refreshSeconds * 1000, 
                    refreshSeconds * 1000 );
        }
        
```

### IntegerMultiplicationImplicitCastToLong
interval\*1000: integer multiplication implicitly cast to long
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/lifecycle/impl/LifecycleManagerImpl.java`
#### Snippet
```java
        public void run() {
            try{
                Thread.sleep(interval*1000);
                if(_file.exists()){
                    table.remove(_file.getName());
```

### IntegerMultiplicationImplicitCastToLong
BUFFER_SIZE \* (data.size()-1): integer multiplication implicitly cast to long
in `axiom-compat/src/main/java/org/apache/axiom/util/blob/MemoryBlob.java`
#### Snippet
```java
            return 0;
        } else {
            return (BUFFER_SIZE * (data.size()-1)) + index;
        }
    }
```

### IntegerMultiplicationImplicitCastToLong
chunkIndex\*chunkSize: integer multiplication implicitly cast to long
in `axiom-compat/src/main/java/org/apache/axiom/util/blob/OverflowBlob.java`
#### Snippet
```java
            return temporaryFile.length();
        } else {
            return chunkIndex*chunkSize + chunkOffset;
        }
    }
```

## RuleId[id=ThrowFromFinallyBlock]
### ThrowFromFinallyBlock
`throw` inside 'finally' block
in `components/blob/src/main/java/org/apache/axiom/blob/AbstractWritableBlob.java`
#### Snippet
```java
                out.close();
            } catch (IOException ex) {
                throw new StreamCopyException(StreamCopyException.WRITE, ex);
            }
        }
```

### ThrowFromFinallyBlock
`throw` inside 'finally' block
in `components/blob/src/main/java/org/apache/axiom/blob/AbstractWritableBlob.java`
#### Snippet
```java
                in.close();
            } catch (IOException ex) {
                throw new StreamCopyException(StreamCopyException.READ, ex);
            }
        }
```

### ThrowFromFinallyBlock
`throw` inside 'finally' block
in `axiom-compat/src/main/java/org/apache/axiom/util/blob/OverflowBlob.java`
#### Snippet
```java
                    in.close();
                } catch (IOException ex) {
                    throw new StreamCopyException(StreamCopyException.READ, ex);
                }
            }
```

## RuleId[id=IfStatementWithIdenticalBranches]
### IfStatementWithIdenticalBranches
Common parts with variables can be extracted from 'if'
in `testing/xml-truth/src/main/java/org/apache/axiom/truth/xml/CoalescingFilter.java`
#### Snippet
```java
    public Event next() throws TraverserException {
        savedText = null;
        if (savedEvent != null) {
            Event event = savedEvent;
            savedEvent = null;
```

### IfStatementWithIdenticalBranches
Common part can be extracted removing branch
in `axiom-api/src/main/java/org/apache/axiom/util/stax/dialect/StAXDialectDetector.java`
#### Snippet
```java
    private static StAXDialect getDialect(ClassLoader classLoader, URL rootUrl) {
        StAXDialect dialect = dialectByUrl.get(rootUrl);
        if (dialect != null) {
            return dialect;
        } else {
```

### IfStatementWithIdenticalBranches
Common part can be extracted from 'if'
in `mixins/om-mixins/src/main/java/org/apache/axiom/om/impl/mixin/AxiomSourcedElementMixin.java`
#### Snippet
```java
                String namespaceURI = ((QNameAwareOMDataSource) dataSource).getNamespaceURI();
                if (namespaceURI != null) {
                    if (namespaceURI.length() == 0) {
                        // No namespace case. definedNamespace is already null, so we only need
                        // to set definedNamespaceSet to true. Note that we don't need to retrieve
```

### IfStatementWithIdenticalBranches
Common part can be extracted removing branch
in `mixins/om-mixins/src/main/java/org/apache/axiom/om/impl/mixin/AxiomSourcedElementMixin.java`
#### Snippet
```java
    public OMDataSource setDataSource(OMDataSource dataSource) {
        try {
            if (!isExpanded()) {
                OMDataSource oldDS = this.dataSource;
                this.dataSource = dataSource;
```

### IfStatementWithIdenticalBranches
Common part can be extracted removing branch
in `mixins/om-mixins/src/main/java/org/apache/axiom/om/impl/stream/xop/OptimizationPolicyImpl.java`
#### Snippet
```java
            return false;
        } catch (StreamCopyException ex) {
            if (ex.getOperation() == StreamCopyException.WRITE) {
                return true;
            } else {
```

## RuleId[id=UNUSED_IMPORT]
### UNUSED_IMPORT
Unused import `import org.apache.axiom.blob.WritableBlob;`
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/Attachments.java`
#### Snippet
```java
import org.apache.axiom.blob.Blobs;
import org.apache.axiom.blob.MemoryBlob;
import org.apache.axiom.blob.WritableBlob;
import org.apache.axiom.blob.WritableBlobFactory;
import org.apache.axiom.ext.activation.SizeAwareDataSource;
```

### UNUSED_IMPORT
Unused import `import static com.google.common.truth.Truth.assertThat;`
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/headerblock/TestSetRoleWithoutExistingNamespaceDecl.java`
#### Snippet
```java
package org.apache.axiom.ts.soap.headerblock;

import static com.google.common.truth.Truth.assertThat;
import static org.apache.axiom.truth.AxiomTruth.assertThat;

```

## RuleId[id=JUnitMalformedDeclaration]
### JUnitMalformedDeclaration
Test class `TestCreateWebServiceMessageFromInputStreamMTOM` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/spring-ws-testsuite/src/main/java/org/apache/axiom/ts/springws/soap/messagefactory/TestCreateWebServiceMessageFromInputStreamMTOM.java`
#### Snippet
```java
 * SoapMessageFactory#createWebServiceMessage(InputStream)}.
 */
public class TestCreateWebServiceMessageFromInputStreamMTOM extends SimpleTestCase {
    public TestCreateWebServiceMessageFromInputStreamMTOM(MessageFactoryConfigurator mfc) {
        super(mfc, SOAPSpec.SOAP12);
```

### JUnitMalformedDeclaration
Test class `TestCreateWebServiceMessage` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/spring-ws-testsuite/src/main/java/org/apache/axiom/ts/springws/soap/messagefactory/TestCreateWebServiceMessage.java`
#### Snippet
```java
 * SoapMessageFactory#createWebServiceMessage()}.
 */
public class TestCreateWebServiceMessage extends SimpleTestCase {
    public TestCreateWebServiceMessage(MessageFactoryConfigurator mfc, SOAPSpec spec) {
        super(mfc, spec);
```

### JUnitMalformedDeclaration
Test class `TestCreateWebServiceMessageFromInputStream` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/spring-ws-testsuite/src/main/java/org/apache/axiom/ts/springws/soap/messagefactory/TestCreateWebServiceMessageFromInputStream.java`
#### Snippet
```java
 * SoapMessageFactory#createWebServiceMessage(InputStream)}.
 */
public class TestCreateWebServiceMessageFromInputStream extends SimpleTestCase {
    public TestCreateWebServiceMessageFromInputStream(
            MessageFactoryConfigurator mfc, SOAPSpec spec) {
```

### JUnitMalformedDeclaration
Test class `TestCreateWebServiceMessageFromInputStreamVersionMismatch` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/spring-ws-testsuite/src/main/java/org/apache/axiom/ts/springws/soap/messagefactory/TestCreateWebServiceMessageFromInputStreamVersionMismatch.java`
#### Snippet
```java
 * content type and the actual SOAP version used by the message.
 */
public class TestCreateWebServiceMessageFromInputStreamVersionMismatch extends SimpleTestCase {
    public TestCreateWebServiceMessageFromInputStreamVersionMismatch(
            MessageFactoryConfigurator mfc, SOAPSpec spec) {
```

### JUnitMalformedDeclaration
Test class `JAXB2Test` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/spring-ws-testsuite/src/main/java/org/apache/axiom/ts/springws/scenario/jaxb2/JAXB2Test.java`
#### Snippet
```java
import org.springframework.ws.client.core.WebServiceTemplate;

public class JAXB2Test extends ScenarioTestCase {
    public JAXB2Test(ScenarioConfig config, SOAPSpec spec) {
        super(config, spec);
```

### JUnitMalformedDeclaration
Test class `ClientServerTest` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/spring-ws-testsuite/src/main/java/org/apache/axiom/ts/springws/scenario/jdom/ClientServerTest.java`
#### Snippet
```java
import org.springframework.ws.client.core.WebServiceTemplate;

public class ClientServerTest extends ScenarioTestCase {
    public ClientServerTest(ScenarioConfig config, SOAPSpec spec) {
        super(config, spec);
```

### JUnitMalformedDeclaration
Test class `BrokerScenarioTest` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/spring-ws-testsuite/src/main/java/org/apache/axiom/ts/springws/scenario/broker/BrokerScenarioTest.java`
#### Snippet
```java
 * </ul>
 */
public class BrokerScenarioTest extends ScenarioTestCase {
    public BrokerScenarioTest(ScenarioConfig config, SOAPSpec spec) {
        super(config, spec);
```

### JUnitMalformedDeclaration
Test class `SecureEchoTest` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/spring-ws-testsuite/src/main/java/org/apache/axiom/ts/springws/scenario/secureecho/SecureEchoTest.java`
#### Snippet
```java
import org.w3c.dom.Element;

public class SecureEchoTest extends ScenarioTestCase {
    public SecureEchoTest(ScenarioConfig config, SOAPSpec spec) {
        super(config, spec);
```

### JUnitMalformedDeclaration
Test class `SoapActionTest` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/spring-ws-testsuite/src/main/java/org/apache/axiom/ts/springws/scenario/soapaction/SoapActionTest.java`
#### Snippet
```java
import org.w3c.dom.Element;

public class SoapActionTest extends ScenarioTestCase {
    public SoapActionTest(ScenarioConfig config, SOAPSpec spec) {
        super(config, spec);
```

### JUnitMalformedDeclaration
Test class `WSAddressingDOMTest` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/spring-ws-testsuite/src/main/java/org/apache/axiom/ts/springws/scenario/wsadom/WSAddressingDOMTest.java`
#### Snippet
```java
import org.w3c.dom.Element;

public class WSAddressingDOMTest extends ScenarioTestCase {
    public WSAddressingDOMTest(ScenarioConfig config, SOAPSpec spec) {
        super(config, spec);
```

### JUnitMalformedDeclaration
Test class `ValidationTest` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/spring-ws-testsuite/src/main/java/org/apache/axiom/ts/springws/scenario/validation/ValidationTest.java`
#### Snippet
```java
import org.springframework.ws.soap.client.SoapFaultClientException;

public class ValidationTest extends ScenarioTestCase {
    public ValidationTest(ScenarioConfig config, SOAPSpec spec) {
        super(config, spec);
```

### JUnitMalformedDeclaration
Test class `W3CTestCase` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/w3c/W3CTestCase.java`
#### Snippet
```java
import org.w3c.domts.DOMTestCase;

public final class W3CTestCase extends MatrixTestCase {
    private DOMTestCase test;

```

### JUnitMalformedDeclaration
Test class `TestSetPrefixNotNullWithNamespace` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/attr/TestSetPrefixNotNullWithNamespace.java`
#### Snippet
```java
 * is not null (and not an empty string) and the attribute has a namespace.
 */
public class TestSetPrefixNotNullWithNamespace extends DOMTestCase {
    public TestSetPrefixNotNullWithNamespace(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestLookupNamespaceURIWithoutOwnerElement` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/attr/TestLookupNamespaceURIWithoutOwnerElement.java`
#### Snippet
```java
 * owner element.
 */
public class TestLookupNamespaceURIWithoutOwnerElement extends DOMTestCase {
    public TestLookupNamespaceURIWithoutOwnerElement(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestSetPrefixNotNullWithoutNamespace` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/attr/TestSetPrefixNotNullWithoutNamespace.java`
#### Snippet
```java
 * prefix on an {@link Attr} that has no namespace.
 */
public class TestSetPrefixNotNullWithoutNamespace extends DOMTestCase {
    public TestSetPrefixNotNullWithoutNamespace(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestSetPrefixNullWithNamespace` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/attr/TestSetPrefixNullWithNamespace.java`
#### Snippet
```java
 * namespaces, no exception is thrown.
 */
public class TestSetPrefixNullWithNamespace extends DOMTestCase {
    public TestSetPrefixNullWithNamespace(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestGetFirstChild` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/attr/TestGetFirstChild.java`
#### Snippet
```java
import org.w3c.dom.Text;

public class TestGetFirstChild extends DOMTestCase {
    public TestGetFirstChild(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestGetChildNodes` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/attr/TestGetChildNodes.java`
#### Snippet
```java
import org.w3c.dom.Text;

public class TestGetChildNodes extends DOMTestCase {
    public TestGetChildNodes(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestGetValueWithMultipleChildren` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/attr/TestGetValueWithMultipleChildren.java`
#### Snippet
```java
import org.w3c.dom.Document;

public class TestGetValueWithMultipleChildren extends DOMTestCase {
    public TestGetValueWithMultipleChildren(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestGetNamespaceURIWithNoNamespace` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/attr/TestGetNamespaceURIWithNoNamespace.java`
#### Snippet
```java
import org.w3c.dom.Document;

public class TestGetNamespaceURIWithNoNamespace extends DOMTestCase {
    public TestGetNamespaceURIWithNoNamespace(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestLookupNamespaceURIWithoutParent` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/text/TestLookupNamespaceURIWithoutParent.java`
#### Snippet
```java
 * parent.
 */
public class TestLookupNamespaceURIWithoutParent extends DOMTestCase {
    public TestLookupNamespaceURIWithoutParent(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestGetWholeTextWithCDATASection` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/text/TestGetWholeTextWithCDATASection.java`
#### Snippet
```java
import org.w3c.dom.Text;

public class TestGetWholeTextWithCDATASection extends DOMTestCase {
    public TestGetWholeTextWithCDATASection(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestSplitText` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/text/TestSplitText.java`
#### Snippet
```java
import org.w3c.dom.Text;

public class TestSplitText extends DOMTestCase {
    public TestSplitText(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestGetChildNodes` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/text/TestGetChildNodes.java`
#### Snippet
```java
import org.w3c.dom.Text;

public class TestGetChildNodes extends DOMTestCase {
    public TestGetChildNodes(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestReplaceDataAppend` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/text/TestReplaceDataAppend.java`
#### Snippet
```java
import org.w3c.dom.Text;

public class TestReplaceDataAppend extends DOMTestCase {
    public TestReplaceDataAppend(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestAppendData` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/text/TestAppendData.java`
#### Snippet
```java
import org.w3c.dom.Text;

public class TestAppendData extends DOMTestCase {
    public TestAppendData(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestCloneNode` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/attr/TestCloneNode.java`
#### Snippet
```java
import org.w3c.dom.Node;

public class TestCloneNode extends DOMTestCase {
    private final boolean deep;

```

### JUnitMalformedDeclaration
Test class `TestGetLength` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/text/TestGetLength.java`
#### Snippet
```java

/** Tests that {@link CharacterData#getLength()} returns the expected result. */
public class TestGetLength extends DOMTestCase {
    public TestGetLength(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestSplitTextWithoutParent` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/text/TestSplitTextWithoutParent.java`
#### Snippet
```java

/** Tests the behavior of {@link Text#splitText(int)} when the node has no parent. */
public class TestSplitTextWithoutParent extends DOMTestCase {
    public TestSplitTextWithoutParent(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestSetPrefix` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/text/TestSetPrefix.java`
#### Snippet
```java

/** Tests that calling {@link Node#setPrefix(String)} on a text node results in an exception. */
public class TestSetPrefix extends DOMTestCase {
    public TestSetPrefix(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestGetWholeTextWithComment` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/text/TestGetWholeTextWithComment.java`
#### Snippet
```java
import org.w3c.dom.Text;

public class TestGetWholeTextWithComment extends DOMTestCase {
    public TestGetWholeTextWithComment(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestGetWholeText` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/text/TestGetWholeText.java`
#### Snippet
```java
import org.w3c.dom.Text;

public class TestGetWholeText extends DOMTestCase {
    public TestGetWholeText(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestRemoveLastChild` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/element/TestRemoveLastChild.java`
#### Snippet
```java
import org.xml.sax.InputSource;

public class TestRemoveLastChild extends DOMTestCase {
    public TestRemoveLastChild(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestWhitespaceAroundDocumentElement` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/builder/TestWhitespaceAroundDocumentElement.java`
#### Snippet
```java
 * doesn't allow text nodes as children of a document.
 */
public class TestWhitespaceAroundDocumentElement extends DOMTestCase {
    public TestWhitespaceAroundDocumentElement(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestInsertBefore` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/element/TestInsertBefore.java`
#### Snippet
```java
import org.w3c.dom.NodeList;

public class TestInsertBefore extends DOMTestCase {
    public TestInsertBefore(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestReplaceChildLastWithDocumentFragment` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/element/TestReplaceChildLastWithDocumentFragment.java`
#### Snippet
```java
 * (which uses a different code path in DOOM).
 */
public class TestReplaceChildLastWithDocumentFragment extends DOMTestCase {
    public TestReplaceChildLastWithDocumentFragment(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestParseURI` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/builder/TestParseURI.java`
#### Snippet
```java

/** Tests {@link DocumentBuilder#parse(String)}. */
public class TestParseURI extends DOMTestCase {
    public TestParseURI(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestImportNode` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/element/TestImportNode.java`
#### Snippet
```java
import org.xml.sax.InputSource;

public class TestImportNode extends DOMTestCase {
    private final XMLSample file;
    private final DOMImplementation from;
```

### JUnitMalformedDeclaration
Test class `TestReplaceChildNotFound` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/element/TestReplaceChildNotFound.java`
#### Snippet
```java
 * child.
 */
public class TestReplaceChildNotFound extends DOMTestCase {
    public TestReplaceChildNotFound(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestSetAttributeNS` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/element/TestSetAttributeNS.java`
#### Snippet
```java
import org.w3c.dom.NamedNodeMap;

public class TestSetAttributeNS extends DOMTestCase {
    private final QName qname;
    private final String value;
```

### JUnitMalformedDeclaration
Test class `TestRemoveSingleChild` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/element/TestRemoveSingleChild.java`
#### Snippet
```java
import org.xml.sax.InputSource;

public class TestRemoveSingleChild extends DOMTestCase {
    public TestRemoveSingleChild(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestSetPrefixNotNullWithNamespace` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/element/TestSetPrefixNotNullWithNamespace.java`
#### Snippet
```java
 * prefix is not null (and not an empty string) and the element has a namespace.
 */
public class TestSetPrefixNotNullWithNamespace extends DOMTestCase {
    public TestSetPrefixNotNullWithNamespace(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestReplaceChildSingle` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/element/TestReplaceChildSingle.java`
#### Snippet
```java
 * child being replaced is the only child.
 */
public class TestReplaceChildSingle extends ReplaceChildTestCase {
    public TestReplaceChildSingle(DocumentBuilderFactory dbf, boolean newChildHasSiblings) {
        super(dbf, newChildHasSiblings);
```

### JUnitMalformedDeclaration
Test class `TestLookupNamespaceURIDefaultBindings` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/element/TestLookupNamespaceURIDefaultBindings.java`
#### Snippet
```java
 * bindings for the {@code xml} and {@code xmlns} prefixes.
 */
public class TestLookupNamespaceURIDefaultBindings extends DOMTestCase {
    public TestLookupNamespaceURIDefaultBindings(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestAppendChildCyclic` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/element/TestAppendChildCyclic.java`
#### Snippet
```java
 * which is appended.
 */
public class TestAppendChildCyclic extends DOMTestCase {
    public TestAppendChildCyclic(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestReplaceChildCyclic` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/element/TestReplaceChildCyclic.java`
#### Snippet
```java
 * child with an ancestor of the node, i.e. if the replacement would create a cycle.
 */
public class TestReplaceChildCyclic extends DOMTestCase {
    public TestReplaceChildCyclic(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestLookupPrefixImplicitMasked` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/element/TestLookupPrefixImplicitMasked.java`
#### Snippet
```java
 * masked by a child element (of the element that has the matching prefix).
 */
public class TestLookupPrefixImplicitMasked extends DOMTestCase {
    public TestLookupPrefixImplicitMasked(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestLookupNamespaceURIExplicit` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/element/TestLookupNamespaceURIExplicit.java`
#### Snippet
```java
 * namespaces defined explicitly by attributes representing namespace declarations.
 */
public class TestLookupNamespaceURIExplicit extends DOMTestCase {
    public TestLookupNamespaceURIExplicit(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestCloneNodeWithAttributeHavingMultipleChildren` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/element/TestCloneNodeWithAttributeHavingMultipleChildren.java`
#### Snippet
```java
import org.w3c.dom.NodeList;

public class TestCloneNodeWithAttributeHavingMultipleChildren extends DOMTestCase {
    public TestCloneNodeWithAttributeHavingMultipleChildren(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestGetElementsByTagNameWithNamespaces` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/element/TestGetElementsByTagNameWithNamespaces.java`
#### Snippet
```java
import org.w3c.dom.Element;

public class TestGetElementsByTagNameWithNamespaces extends DOMTestCase {
    public TestGetElementsByTagNameWithNamespaces(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestGetPrefixWithDefaultNamespace` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/element/TestGetPrefixWithDefaultNamespace.java`
#### Snippet
```java
import org.w3c.dom.Element;

public class TestGetPrefixWithDefaultNamespace extends DOMTestCase {
    public TestGetPrefixWithDefaultNamespace(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestReplaceChildMiddleWithDocumentFragment` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/element/TestReplaceChildMiddleWithDocumentFragment.java`
#### Snippet
```java
 * nor the last child.
 */
public class TestReplaceChildMiddleWithDocumentFragment extends DOMTestCase {
    public TestReplaceChildMiddleWithDocumentFragment(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestSetAttributeNodeNSReplace` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/element/TestSetAttributeNodeNSReplace.java`
#### Snippet
```java
 * attribute.
 */
public class TestSetAttributeNodeNSReplace extends DOMTestCase {
    public TestSetAttributeNodeNSReplace(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestRemoveAttributeNodeNotOwner` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/element/TestRemoveAttributeNodeNotOwner.java`
#### Snippet
```java
 * owned by the element.
 */
public class TestRemoveAttributeNodeNotOwner extends DOMTestCase {
    public TestRemoveAttributeNodeNotOwner(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestReplaceChildFirst` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/element/TestReplaceChildFirst.java`
#### Snippet
```java
 * child being replaced is the first child (which uses a different code path in DOOM).
 */
public class TestReplaceChildFirst extends ReplaceChildTestCase {
    public TestReplaceChildFirst(DocumentBuilderFactory dbf, boolean newChildHasSiblings) {
        super(dbf, newChildHasSiblings);
```

### JUnitMalformedDeclaration
Test class `TestReplaceChildLast` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/element/TestReplaceChildLast.java`
#### Snippet
```java
 * child being replaced is the last child (which uses a different code path in DOOM).
 */
public class TestReplaceChildLast extends ReplaceChildTestCase {
    public TestReplaceChildLast(DocumentBuilderFactory dbf, boolean newChildHasSiblings) {
        super(dbf, newChildHasSiblings);
```

### JUnitMalformedDeclaration
Test class `TestAttributes2` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/element/TestAttributes2.java`
#### Snippet
```java
import org.xml.sax.InputSource;

public class TestAttributes2 extends DOMTestCase {
    public TestAttributes2(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestRemoveAttributeNodeForeignImplementation` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/element/TestRemoveAttributeNodeForeignImplementation.java`
#### Snippet
```java
import org.w3c.dom.Element;

public class TestRemoveAttributeNodeForeignImplementation extends DOMTestCase {
    public TestRemoveAttributeNodeForeignImplementation(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestSetAttributeNSExistingDefaultNamespaceDeclaration` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/element/TestSetAttributeNSExistingDefaultNamespaceDeclaration.java`
#### Snippet
```java
 * a namespace declaration for the default namespace.
 */
public class TestSetAttributeNSExistingDefaultNamespaceDeclaration extends DOMTestCase {
    public TestSetAttributeNSExistingDefaultNamespaceDeclaration(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestAppendChildWrongDocument` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/element/TestAppendChildWrongDocument.java`
#### Snippet
```java
 * to append a child that has a different owner document.
 */
public class TestAppendChildWrongDocument extends DOMTestCase {
    public TestAppendChildWrongDocument(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestSetAttributeNodeNSForeignImplementation` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/element/TestSetAttributeNodeNSForeignImplementation.java`
#### Snippet
```java
import org.w3c.dom.Element;

public class TestSetAttributeNodeNSForeignImplementation extends DOMTestCase {
    public TestSetAttributeNodeNSForeignImplementation(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestInsertBeforeWithDocumentFragment` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/element/TestInsertBeforeWithDocumentFragment.java`
#### Snippet
```java
import org.w3c.dom.NodeList;

public class TestInsertBeforeWithDocumentFragment extends DOMTestCase {
    public TestInsertBeforeWithDocumentFragment(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestReplaceChildMiddle` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/element/TestReplaceChildMiddle.java`
#### Snippet
```java
 * child being replaced is neither the first nor the last child.
 */
public class TestReplaceChildMiddle extends ReplaceChildTestCase {
    public TestReplaceChildMiddle(DocumentBuilderFactory dbf, boolean newChildHasSiblings) {
        super(dbf, newChildHasSiblings);
```

### JUnitMalformedDeclaration
Test class `TestSetPrefixNull` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/element/TestSetPrefixNull.java`
#### Snippet
```java
 * prefix is null.
 */
public class TestSetPrefixNull extends DOMTestCase {
    public TestSetPrefixNull(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestReplaceChildWrongDocument` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/element/TestReplaceChildWrongDocument.java`
#### Snippet
```java
import org.w3c.dom.Element;

public class TestReplaceChildWrongDocument extends DOMTestCase {
    public TestReplaceChildWrongDocument(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestRemoveFirstChild` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/element/TestRemoveFirstChild.java`
#### Snippet
```java

// Regression test for AXIOM-169
public class TestRemoveFirstChild extends DOMTestCase {
    public TestRemoveFirstChild(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestLookupPrefixEmptyNamespace` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/element/TestLookupPrefixEmptyNamespace.java`
#### Snippet
```java
 * namespace URI is the empty string.
 */
public class TestLookupPrefixEmptyNamespace extends DOMTestCase {
    public TestLookupPrefixEmptyNamespace(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestSetAttributeNodeNSInUse` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/element/TestSetAttributeNodeNSInUse.java`
#### Snippet
```java
import org.w3c.dom.Element;

public class TestSetAttributeNodeNSInUse extends DOMTestCase {
    public TestSetAttributeNodeNSInUse(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestAttributes3` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/element/TestAttributes3.java`
#### Snippet
```java
import org.xml.sax.InputSource;

public class TestAttributes3 extends DOMTestCase {
    public TestAttributes3(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestLookupPrefixDefaultBindings` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/element/TestLookupPrefixDefaultBindings.java`
#### Snippet
```java
 * the {@code xml} and {@code xmlns} prefixes.
 */
public class TestLookupPrefixDefaultBindings extends DOMTestCase {
    public TestLookupPrefixDefaultBindings(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestGetNamespaceURIWithNoNamespace` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/element/TestGetNamespaceURIWithNoNamespace.java`
#### Snippet
```java
import org.w3c.dom.Element;

public class TestGetNamespaceURIWithNoNamespace extends DOMTestCase {
    public TestGetNamespaceURIWithNoNamespace(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestAppendChildSelf` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/element/TestAppendChildSelf.java`
#### Snippet
```java
 * to add a node as a child of itself.
 */
public class TestAppendChildSelf extends DOMTestCase {
    public TestAppendChildSelf(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestGetElementsByTagName` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/element/TestGetElementsByTagName.java`
#### Snippet
```java

/** Testing the NodeList returned with the elements's children */
public class TestGetElementsByTagName extends DOMTestCase {
    public TestGetElementsByTagName(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestAttributes4` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/element/TestAttributes4.java`
#### Snippet
```java
import org.xml.sax.InputSource;

public class TestAttributes4 extends DOMTestCase {
    public TestAttributes4(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestReplaceChildNullNewChild` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/element/TestReplaceChildNullNewChild.java`
#### Snippet
```java
 * exception to throw; Xerces throws a {@link NullPointerException}.
 */
public class TestReplaceChildNullNewChild extends DOMTestCase {
    public TestReplaceChildNullNewChild(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestGetElementsByTagNameRecursive` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/element/TestGetElementsByTagNameRecursive.java`
#### Snippet
```java
import org.w3c.dom.NodeList;

public class TestGetElementsByTagNameRecursive extends DOMTestCase {
    public TestGetElementsByTagNameRecursive(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestGetTextContent` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/element/TestGetTextContent.java`
#### Snippet
```java
import org.xml.sax.InputSource;

public class TestGetTextContent extends DOMTestCase {
    public TestGetTextContent(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestGetElementsByTagNameNS` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/element/TestGetElementsByTagNameNS.java`
#### Snippet
```java
import org.w3c.dom.NodeList;

public class TestGetElementsByTagNameNS extends DOMTestCase {
    public TestGetElementsByTagNameNS(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestSetAttributeNodeWrongDocument` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/element/TestSetAttributeNodeWrongDocument.java`
#### Snippet
```java
import org.w3c.dom.Element;

public class TestSetAttributeNodeWrongDocument extends DOMTestCase {
    public TestSetAttributeNodeWrongDocument(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestCloneNodeWithAttributes` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/element/TestCloneNodeWithAttributes.java`
#### Snippet
```java

/** Tests that {@link Node#cloneNode(boolean)} correctly clones the attributes of an element. */
public class TestCloneNodeWithAttributes extends DOMTestCase {
    private final boolean deep;

```

### JUnitMalformedDeclaration
Test class `TestAppendChild` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/element/TestAppendChild.java`
#### Snippet
```java
import org.w3c.dom.Text;

public class TestAppendChild extends DOMTestCase {
    public TestAppendChild(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestSetAttributeNSExisting` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/element/TestSetAttributeNSExisting.java`
#### Snippet
```java
 * already has an attribute with the same namespace URI and local name.
 */
public class TestSetAttributeNSExisting extends DOMTestCase {
    public TestSetAttributeNSExisting(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestLookupNamespaceURINamespaceDeclarationAsNSUnawareAttribute` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/element/TestLookupNamespaceURINamespaceDeclarationAsNSUnawareAttribute.java`
#### Snippet
```java
 * declaration added with {@link Element#setAttribute(String, String)}.
 */
public class TestLookupNamespaceURINamespaceDeclarationAsNSUnawareAttribute extends DOMTestCase {
    public TestLookupNamespaceURINamespaceDeclarationAsNSUnawareAttribute(
            DocumentBuilderFactory dbf) {
```

### JUnitMalformedDeclaration
Test class `TestAttributes` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/element/TestAttributes.java`
#### Snippet
```java
import org.w3c.dom.NamedNodeMap;

public class TestAttributes extends DOMTestCase {
    public TestAttributes(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestReplaceChildFirstWithDocumentFragment` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/element/TestReplaceChildFirstWithDocumentFragment.java`
#### Snippet
```java
 * (which uses a different code path in DOOM).
 */
public class TestReplaceChildFirstWithDocumentFragment extends DOMTestCase {
    public TestReplaceChildFirstWithDocumentFragment(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestSetAttributeNSInvalid` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/element/TestSetAttributeNSInvalid.java`
#### Snippet
```java
import org.w3c.dom.Element;

public class TestSetAttributeNSInvalid extends DOMTestCase {
    private final QName qname;

```

### JUnitMalformedDeclaration
Test class `TestGetElementsByTagNameWithWildcard` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/element/TestGetElementsByTagNameWithWildcard.java`
#### Snippet
```java
import org.w3c.dom.Element;

public class TestGetElementsByTagNameWithWildcard extends DOMTestCase {
    public TestGetElementsByTagNameWithWildcard(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestLookupPrefixExplicitMasked` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/element/TestLookupPrefixExplicitMasked.java`
#### Snippet
```java
 * redeclared.
 */
public class TestLookupPrefixExplicitMasked extends DOMTestCase {
    public TestLookupPrefixExplicitMasked(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestCloneNode` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/element/TestCloneNode.java`
#### Snippet
```java
import org.w3c.dom.Element;

public class TestCloneNode extends DOMTestCase {
    public TestCloneNode(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestSetPrefixNotNullWithoutNamespace` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/element/TestSetPrefixNotNullWithoutNamespace.java`
#### Snippet
```java
 * prefix on an {@link Element} that has no namespace.
 */
public class TestSetPrefixNotNullWithoutNamespace extends DOMTestCase {
    public TestSetPrefixNotNullWithoutNamespace(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestSetTextContent` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/element/TestSetTextContent.java`
#### Snippet
```java
import org.xml.sax.InputSource;

public class TestSetTextContent extends DOMTestCase {
    public TestSetTextContent(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestSetAttributeNodeNSWrongDocument` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/element/TestSetAttributeNodeNSWrongDocument.java`
#### Snippet
```java
import org.w3c.dom.Element;

public class TestSetAttributeNodeNSWrongDocument extends DOMTestCase {
    public TestSetAttributeNodeNSWrongDocument(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestLookupNamespaceURIXercesJ1586` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/element/TestLookupNamespaceURIXercesJ1586.java`
#### Snippet
```java
 * href="https://issues.apache.org/jira/browse/XERCESJ-1586">XERCESJ-1586</a>.
 */
public class TestLookupNamespaceURIXercesJ1586 extends DOMTestCase {
    public TestLookupNamespaceURIXercesJ1586(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestSetNamedItemNSWrongDocument` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/element/attributes/TestSetNamedItemNSWrongDocument.java`
#### Snippet
```java
import org.w3c.dom.Element;

public class TestSetNamedItemNSWrongDocument extends DOMTestCase {
    public TestSetNamedItemNSWrongDocument(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestSetNamedItemWrongDocument` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/element/attributes/TestSetNamedItemWrongDocument.java`
#### Snippet
```java
import org.w3c.dom.Element;

public class TestSetNamedItemWrongDocument extends DOMTestCase {
    public TestSetNamedItemWrongDocument(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestCreateCDATASection` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/document/TestCreateCDATASection.java`
#### Snippet
```java
import org.w3c.dom.Document;

public class TestCreateCDATASection extends DOMTestCase {
    public TestCreateCDATASection(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestTransformerWithIdentityStylesheet` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/document/TestTransformerWithIdentityStylesheet.java`
#### Snippet
```java
// This test failed with Saxon 8.9 because NodeImpl#compareDocumentPosition
// threw an UnsupportedOperationException instead of a DOMException.
public class TestTransformerWithIdentityStylesheet extends TransformerTestCase {
    public TestTransformerWithIdentityStylesheet(
            DocumentBuilderFactory dbf, XSLTImplementation xsltImplementation) {
```

### JUnitMalformedDeclaration
Test class `TestAppendChildForeignImplementation` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/document/TestAppendChildForeignImplementation.java`
#### Snippet
```java
import org.w3c.dom.Element;

public class TestAppendChildForeignImplementation extends DOMTestCase {
    public TestAppendChildForeignImplementation(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestCreateAttribute` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/document/TestCreateAttribute.java`
#### Snippet
```java
import org.w3c.dom.Document;

public class TestCreateAttribute extends DOMTestCase {
    public TestCreateAttribute(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestCreateElementNSWithInvalidName` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/document/TestCreateElementNSWithInvalidName.java`
#### Snippet
```java
import org.w3c.dom.Document;

public class TestCreateElementNSWithInvalidName extends DOMTestCase {
    public TestCreateElementNSWithInvalidName(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestLookupNamespaceURIImplicit` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/element/TestLookupNamespaceURIImplicit.java`
#### Snippet
```java
 * for namespaces not defined explicitly by attributes representing namespace declarations.
 */
public class TestLookupNamespaceURIImplicit extends DOMTestCase {

    public TestLookupNamespaceURIImplicit(DocumentBuilderFactory dbf) {
```

### JUnitMalformedDeclaration
Test class `TestCreateEntityReference` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/document/TestCreateEntityReference.java`
#### Snippet
```java
import org.w3c.dom.Node;

public class TestCreateEntityReference extends DOMTestCase {
    public TestCreateEntityReference(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestCreateAttributeNSWithoutNamespace` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/document/TestCreateAttributeNSWithoutNamespace.java`
#### Snippet
```java
import org.w3c.dom.Document;

public class TestCreateAttributeNSWithoutNamespace extends DOMTestCase {
    public TestCreateAttributeNSWithoutNamespace(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestCreateAttributeNSInvalid` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/document/TestCreateAttributeNSInvalid.java`
#### Snippet
```java
import org.w3c.dom.Document;

public class TestCreateAttributeNSInvalid extends DOMTestCase {
    private final QName qname;

```

### JUnitMalformedDeclaration
Test class `TestAdoptNodeWithParent` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/document/TestAdoptNodeWithParent.java`
#### Snippet
```java
 * case, the method must detach the node from its parent first.
 */
public class TestAdoptNodeWithParent extends DOMTestCase {
    public TestAdoptNodeWithParent(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestCreateText` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/document/TestCreateText.java`
#### Snippet
```java
import org.w3c.dom.Text;

public class TestCreateText extends DOMTestCase {
    public TestCreateText(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestLookupNamespaceURIWithEmptyDocument` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/document/TestLookupNamespaceURIWithEmptyDocument.java`
#### Snippet
```java

/** Tests the behavior of {@link Node#lookupNamespaceURI(String)} on an empty {@link Document}. */
public class TestLookupNamespaceURIWithEmptyDocument extends DOMTestCase {
    public TestLookupNamespaceURIWithEmptyDocument(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestLookupPrefixWithEmptyDocument` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/document/TestLookupPrefixWithEmptyDocument.java`
#### Snippet
```java

/** Tests the behavior of {@link Node#lookupPrefix(String)} on an empty {@link Document}. */
public class TestLookupPrefixWithEmptyDocument extends DOMTestCase {
    public TestLookupPrefixWithEmptyDocument(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestCreateElementNS` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/document/TestCreateElementNS.java`
#### Snippet
```java
import org.w3c.dom.Element;

public class TestCreateElementNS extends DOMTestCase {
    public TestCreateElementNS(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestAllowedChildren` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/document/TestAllowedChildren.java`
#### Snippet
```java
import org.w3c.dom.Document;

public class TestAllowedChildren extends DOMTestCase {
    public TestAllowedChildren(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestTransformerWithStylesheet` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/document/TestTransformerWithStylesheet.java`
#### Snippet
```java
import org.w3c.dom.Document;

public class TestTransformerWithStylesheet extends TransformerTestCase {
    public TestTransformerWithStylesheet(
            DocumentBuilderFactory dbf, XSLTImplementation xsltImplementation) {
```

### JUnitMalformedDeclaration
Test class `TestValidator` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/document/TestValidator.java`
#### Snippet
```java
import org.apache.xerces.jaxp.validation.XMLSchemaFactory;

public class TestValidator extends DOMTestCase {
    public TestValidator(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestAppendChildWrongDocument` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/document/TestAppendChildWrongDocument.java`
#### Snippet
```java
 * to append a child that has a different owner document.
 */
public class TestAppendChildWrongDocument extends DOMTestCase {
    public TestAppendChildWrongDocument(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestNormalizeDocumentNamespace` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/document/TestNormalizeDocumentNamespace.java`
#### Snippet
```java
import org.w3c.dom.NamedNodeMap;

public class TestNormalizeDocumentNamespace extends DOMTestCase {
    public TestNormalizeDocumentNamespace(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestCreateAttributeNS` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/document/TestCreateAttributeNS.java`
#### Snippet
```java
import org.w3c.dom.Document;

public class TestCreateAttributeNS extends DOMTestCase {
    private final QName qname;

```

### JUnitMalformedDeclaration
Test class `TestGetOwnerDocument` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/document/TestGetOwnerDocument.java`
#### Snippet
```java
 * Document}.
 */
public class TestGetOwnerDocument extends DOMTestCase {
    public TestGetOwnerDocument(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestLookupNamespaceURI` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/document/TestLookupNamespaceURI.java`
#### Snippet
```java
 * specification.
 */
public class TestLookupNamespaceURI extends DOMTestCase {
    public TestLookupNamespaceURI(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestCloneNode` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/document/TestCloneNode.java`
#### Snippet
```java
import org.w3c.dom.Document;

public class TestCloneNode extends DOMTestCase {
    private final XMLSample file;

```

### JUnitMalformedDeclaration
Test class `TestCreateElementNSWithoutNamespace` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/document/TestCreateElementNSWithoutNamespace.java`
#### Snippet
```java
import org.w3c.dom.Element;

public class TestCreateElementNSWithoutNamespace extends DOMTestCase {
    public TestCreateElementNSWithoutNamespace(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestCreateElement` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/document/TestCreateElement.java`
#### Snippet
```java
import org.w3c.dom.Element;

public class TestCreateElement extends DOMTestCase {
    public TestCreateElement(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestDocumentSiblings` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/document/TestDocumentSiblings.java`
#### Snippet
```java
import org.w3c.dom.Node;

public class TestDocumentSiblings extends DOMTestCase {
    public TestDocumentSiblings(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestGetXmlVersionFromParsedDocumentWithoutDeclaration` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/document/TestGetXmlVersionFromParsedDocumentWithoutDeclaration.java`
#### Snippet
```java
import org.xml.sax.InputSource;

public class TestGetXmlVersionFromParsedDocumentWithoutDeclaration extends DOMTestCase {
    public TestGetXmlVersionFromParsedDocumentWithoutDeclaration(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestAdoptNodeToSameDocument` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/document/TestAdoptNodeToSameDocument.java`
#### Snippet
```java
 * document. In this case, the method is still expected to detach the node from its parent.
 */
public class TestAdoptNodeToSameDocument extends DOMTestCase {
    public TestAdoptNodeToSameDocument(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestAdoptNode` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/document/TestAdoptNode.java`
#### Snippet
```java

/** Tests the behavior of {@link Document#adoptNode(Node)}. */
public class TestAdoptNode extends DOMTestCase {
    public TestAdoptNode(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestGetDomConfigDefaults` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/document/TestGetDomConfigDefaults.java`
#### Snippet
```java
 * initialized with the correct default values as defined by the DOM specification.
 */
public class TestGetDomConfigDefaults extends DOMTestCase {
    public TestGetDomConfigDefaults(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestWithParser1` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/documenttype/TestWithParser1.java`
#### Snippet
```java
import org.w3c.dom.DocumentType;

public class TestWithParser1 extends DOMTestCase {
    public TestWithParser1(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestLookupPrefix` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/documentfragment/TestLookupPrefix.java`
#### Snippet
```java
 * matching namespace declaration.
 */
public class TestLookupPrefix extends DOMTestCase {
    public TestLookupPrefix(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestWithParser2` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/documenttype/TestWithParser2.java`
#### Snippet
```java
import org.w3c.dom.DocumentType;

public class TestWithParser2 extends DOMTestCase {
    public TestWithParser2(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestLookupNamespaceURI` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/documentfragment/TestLookupNamespaceURI.java`
#### Snippet
```java
 * matching namespace declaration.
 */
public class TestLookupNamespaceURI extends DOMTestCase {
    public TestLookupNamespaceURI(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestCloneNodeDeep` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/documentfragment/TestCloneNodeDeep.java`
#### Snippet
```java

/** Tests {@link Node#cloneNode(boolean)} with <code>deep</code> set to <code>true</code>. */
public class TestCloneNodeDeep extends DOMTestCase {
    public TestCloneNodeDeep(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestCloneNodeShallow` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/documentfragment/TestCloneNodeShallow.java`
#### Snippet
```java

/** Tests {@link Node#cloneNode(boolean)} with <code>deep</code> set to <code>false</code>. */
public class TestCloneNodeShallow extends DOMTestCase {
    public TestCloneNodeShallow(DocumentBuilderFactory dbf) {
        super(dbf);
```

### JUnitMalformedDeclaration
Test class `TestAddChildElementReification` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/saaj-testsuite/src/main/java/org/apache/axiom/ts/saaj/body/TestAddChildElementReification.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SOAPSpec;

public class TestAddChildElementReification extends SAAJTestCase {
    public TestAddChildElementReification(SAAJImplementation saajImplementation, SOAPSpec spec) {
        super(saajImplementation, spec);
```

### JUnitMalformedDeclaration
Test class `TestGetOwnerDocument` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/saaj-testsuite/src/main/java/org/apache/axiom/ts/saaj/element/TestGetOwnerDocument.java`
#### Snippet
```java
 * SOAPElement} as well as the properties of the returned document.
 */
public class TestGetOwnerDocument extends SAAJTestCase {
    public TestGetOwnerDocument(SAAJImplementation saajImplementation, SOAPSpec spec) {
        super(saajImplementation, spec);
```

### JUnitMalformedDeclaration
Test class `TestExamineMustUnderstandHeaderElements` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/saaj-testsuite/src/main/java/org/apache/axiom/ts/saaj/header/TestExamineMustUnderstandHeaderElements.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SOAPSampleSet;

public class TestExamineMustUnderstandHeaderElements extends SAAJTestCase {
    public TestExamineMustUnderstandHeaderElements(
            SAAJImplementation saajImplementation, SOAPSpec spec) {
```

### JUnitMalformedDeclaration
Test class `TestSetParentElement` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/saaj-testsuite/src/main/java/org/apache/axiom/ts/saaj/element/TestSetParentElement.java`
#### Snippet
```java
import org.w3c.dom.NodeList;

public class TestSetParentElement extends SAAJTestCase {
    public TestSetParentElement(SAAJImplementation saajImplementation, SOAPSpec spec) {
        super(saajImplementation, spec);
```

### JUnitMalformedDeclaration
Test class `TestAddChildElementLocalName` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/saaj-testsuite/src/main/java/org/apache/axiom/ts/saaj/element/TestAddChildElementLocalName.java`
#### Snippet
```java

/** Tests the behavior of {@link SOAPElement#addChildElement(String)}. */
public class TestAddChildElementLocalName extends SAAJTestCase {
    public TestAddChildElementLocalName(SAAJImplementation saajImplementation, SOAPSpec spec) {
        super(saajImplementation, spec);
```

### JUnitMalformedDeclaration
Test class `TestAddChildElementLocalNamePrefixAndURI` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/saaj-testsuite/src/main/java/org/apache/axiom/ts/saaj/element/TestAddChildElementLocalNamePrefixAndURI.java`
#### Snippet
```java

/** Tests the behavior of {@link SOAPElement#addChildElement(String, String, String)}. */
public class TestAddChildElementLocalNamePrefixAndURI extends SAAJTestCase {
    public TestAddChildElementLocalNamePrefixAndURI(
            SAAJImplementation saajImplementation, SOAPSpec spec) {
```

### JUnitMalformedDeclaration
Test class `TestSerialize` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/pi/TestSerialize.java`
#### Snippet
```java
import org.apache.axiom.ts.AxiomTestCase;

public class TestSerialize extends AxiomTestCase {
    public TestSerialize(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestDigest` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/pi/TestDigest.java`
#### Snippet
```java
import org.apache.axiom.ts.om.DigestTestCase;

public class TestDigest extends DigestTestCase {
    public TestDigest(OMMetaFactory metaFactory) {
        super(metaFactory, "MD5", "a7066a04f134fc8b62c1745da10dcd26");
```

### JUnitMalformedDeclaration
Test class `TestAxiom95` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/misc/TestAxiom95.java`
#### Snippet
```java

/** Regression test for <a href="https://issues.apache.org/jira/browse/AXIOM-95">AXIOM-95</a>. */
public class TestAxiom95 extends AxiomTestCase {
    public TestAxiom95(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestSetOptimizePlainOMText` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/xop/TestSetOptimizePlainOMText.java`
#### Snippet
```java
 * href="https://issues.apache.org/jira/browse/AXIOM-519">AXIOM-519</a>.
 */
public class TestSetOptimizePlainOMText extends AxiomTestCase {
    public TestSetOptimizePlainOMText(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `XOPRoundtripTest` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/xop/XOPRoundtripTest.java`
#### Snippet
```java
import org.apache.axiom.ts.AxiomTestCase;

public class XOPRoundtripTest extends AxiomTestCase {
    public XOPRoundtripTest(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestSetOptimize` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/xop/TestSetOptimize.java`
#### Snippet
```java
import jakarta.mail.util.ByteArrayDataSource;

public class TestSetOptimize extends AxiomTestCase {
    private final boolean optimize;

```

### JUnitMalformedDeclaration
Test class `TestInsertSiblingBeforeOnOrphan` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/node/TestInsertSiblingBeforeOnOrphan.java`
#### Snippet
```java

/** Tests that {@link OMNode#insertSiblingAfter(OMNode)} fails if the node doesn't have a parent. */
public class TestInsertSiblingBeforeOnOrphan extends AxiomTestCase {
    public TestInsertSiblingBeforeOnOrphan(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestSerialize` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/xop/TestSerialize.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.MTOMSample;

public class TestSerialize extends AxiomTestCase {
    private final boolean base64;

```

### JUnitMalformedDeclaration
Test class `TestDetachAfterBuilderClose` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/node/TestDetachAfterBuilderClose.java`
#### Snippet
```java
import org.apache.axiom.ts.AxiomTestCase;

public class TestDetachAfterBuilderClose extends AxiomTestCase {
    public TestDetachAfterBuilderClose(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestDetachFirstChild` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/node/TestDetachFirstChild.java`
#### Snippet
```java

/** Tests the behavior of {@link OMNode#detach()}. */
public class TestDetachFirstChild extends AxiomTestCase {
    private final boolean build;

```

### JUnitMalformedDeclaration
Test class `TestInsertSiblingBeforeSameParent` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/node/TestInsertSiblingBeforeSameParent.java`
#### Snippet
```java
 * sibling.
 */
public class TestInsertSiblingBeforeSameParent extends AxiomTestCase {
    public TestInsertSiblingBeforeSameParent(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestInsertSiblingBefore` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/node/TestInsertSiblingBefore.java`
#### Snippet
```java

/** Tests the behavior of {@link OMNode#insertSiblingBefore(OMNode)}. */
public class TestInsertSiblingBefore extends AxiomTestCase {
    public TestInsertSiblingBefore(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestInsertSiblingAfterSameParent` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/node/TestInsertSiblingAfterSameParent.java`
#### Snippet
```java
 * Tests the behavior of {@link OMNode#insertSiblingAfter(OMNode)} if the node is already a sibling.
 */
public class TestInsertSiblingAfterSameParent extends AxiomTestCase {
    public TestInsertSiblingAfterSameParent(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestInsertSiblingAfter` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/node/TestInsertSiblingAfter.java`
#### Snippet
```java

/** Tests the behavior of {@link OMNode#insertSiblingAfter(OMNode)}. */
public class TestInsertSiblingAfter extends AxiomTestCase {
    public TestInsertSiblingAfter(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestDetach` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/node/TestDetach.java`
#### Snippet
```java

/** Tests the behavior of {@link OMNode#detach()}. */
public class TestDetach extends AxiomTestCase {
    private final boolean document;
    private final boolean build;
```

### JUnitMalformedDeclaration
Test class `TestInsertSiblingBeforeOnChild` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/node/TestInsertSiblingBeforeOnChild.java`
#### Snippet
```java
import org.apache.axiom.ts.AxiomTestCase;

public class TestInsertSiblingBeforeOnChild extends AxiomTestCase {
    public TestInsertSiblingBeforeOnChild(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestInsertSiblingBeforeOnSelf` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/node/TestInsertSiblingBeforeOnSelf.java`
#### Snippet
```java
import org.apache.axiom.ts.AxiomTestCase;

public class TestInsertSiblingBeforeOnSelf extends AxiomTestCase {
    public TestInsertSiblingBeforeOnSelf(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestInsertSiblingAfterLastChild` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/node/TestInsertSiblingAfterLastChild.java`
#### Snippet
```java

/** Regression test for <a href="https://issues.apache.org/jira/browse/AXIOM-153">AXIOM-153</a>. */
public class TestInsertSiblingAfterLastChild extends AxiomTestCase {
    public TestInsertSiblingAfterLastChild(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetNextOMSiblingAfterDiscard` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/node/TestGetNextOMSiblingAfterDiscard.java`
#### Snippet
```java
import org.apache.axiom.ts.AxiomTestCase;

public class TestGetNextOMSiblingAfterDiscard extends AxiomTestCase {
    public TestGetNextOMSiblingAfterDiscard(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestInsertSiblingAfterOnOrphan` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/node/TestInsertSiblingAfterOnOrphan.java`
#### Snippet
```java
 * Tests that {@link OMNode#insertSiblingBefore(OMNode)} fails if the node doesn't have a parent.
 */
public class TestInsertSiblingAfterOnOrphan extends AxiomTestCase {
    public TestInsertSiblingAfterOnOrphan(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestInsertSiblingAfterOnSelf` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/node/TestInsertSiblingAfterOnSelf.java`
#### Snippet
```java
import org.apache.axiom.ts.AxiomTestCase;

public class TestInsertSiblingAfterOnSelf extends AxiomTestCase {
    public TestInsertSiblingAfterOnSelf(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetNamespaceNoNamespace` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/text/TestGetNamespaceNoNamespace.java`
#### Snippet
```java
 * namespace in scope.
 */
public class TestGetNamespaceNoNamespace extends AxiomTestCase {
    public TestGetNamespaceNoNamespace(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestInsertSiblingAfterOnChild` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/node/TestInsertSiblingAfterOnChild.java`
#### Snippet
```java
import org.apache.axiom.ts.AxiomTestCase;

public class TestInsertSiblingAfterOnChild extends AxiomTestCase {
    public TestInsertSiblingAfterOnChild(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestDigest` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/text/TestDigest.java`
#### Snippet
```java
import org.apache.axiom.ts.om.DigestTestCase;

public class TestDigest extends DigestTestCase {
    public TestDigest(OMMetaFactory metaFactory) {
        super(metaFactory, "SHA1", "91b802187b7ae7c2c858f4aa5ae5470fc50d0306");
```

### JUnitMalformedDeclaration
Test class `TestSerialize` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/text/TestSerialize.java`
#### Snippet
```java
import org.apache.axiom.util.xml.stream.XMLEventUtils;

public class TestSerialize extends AxiomTestCase {
    private final int type;

```

### JUnitMalformedDeclaration
Test class `TestBase64StreamingWithSerialize` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/text/TestBase64StreamingWithSerialize.java`
#### Snippet
```java
 * <p>Regression test for <a href="https://issues.apache.org/jira/browse/AXIOM-236">AXIOM-236</a>.
 */
public class TestBase64StreamingWithSerialize extends AxiomTestCase {
    public TestBase64StreamingWithSerialize(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetNamespace` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/text/TestGetNamespace.java`
#### Snippet
```java

/** Tests the behavior of {@link OMText#getNamespace()} for a prefixed QName. */
public class TestGetNamespace extends AxiomTestCase {
    public TestGetNamespace(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestBase64StreamingWithGetSAXSource` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/text/TestBase64StreamingWithGetSAXSource.java`
#### Snippet
```java
 * <p>Regression test for <a href="https://issues.apache.org/jira/browse/AXIOM-442">AXIOM-442</a>.
 */
public class TestBase64StreamingWithGetSAXSource extends AxiomTestCase {
    private static class Base64Comparator extends DefaultHandler {
        private final Writer out;
```

### JUnitMalformedDeclaration
Test class `TestGetTextCharactersFromDataHandler` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/text/TestGetTextCharactersFromDataHandler.java`
#### Snippet
```java
 * href="https://issues.apache.org/jira/browse/AXIOM-442">AXIOM-442</a>.
 */
public class TestGetTextCharactersFromDataHandler extends AxiomTestCase {
    public TestGetTextCharactersFromDataHandler(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestImportInformationItem` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/cross/TestImportInformationItem.java`
#### Snippet
```java
import org.apache.axiom.ts.xml.XMLSample;

public class TestImportInformationItem extends CrossOMTestCase {
    private final XMLSample file;

```

### JUnitMalformedDeclaration
Test class `TestCloneBinary` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/text/TestCloneBinary.java`
#### Snippet
```java
import org.apache.axiom.ts.AxiomTestCase;

public class TestCloneBinary extends AxiomTestCase {
    private boolean fetch;

```

### JUnitMalformedDeclaration
Test class `TestAXIOMXPath` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/xpath/TestAXIOMXPath.java`
#### Snippet
```java
import org.apache.axiom.ts.AxiomTestCase;

public class TestAXIOMXPath extends AxiomTestCase {
    private final AXIOMXPathTestCase test;

```

### JUnitMalformedDeclaration
Test class `TestInsertSibling` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/cross/TestInsertSibling.java`
#### Snippet
```java
import org.apache.axiom.om.OMText;

public class TestInsertSibling extends CrossOMTestCase {
    private final boolean before;

```

### JUnitMalformedDeclaration
Test class `TestGetAttributeQName` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/xpath/TestGetAttributeQName.java`
#### Snippet
```java
 * attribute with namespace.
 */
public class TestGetAttributeQName extends AxiomTestCase {
    public TestGetAttributeQName(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestAddNamespaces` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/xpath/TestAddNamespaces.java`
#### Snippet
```java
import org.apache.axiom.ts.AxiomTestCase;

public class TestAddNamespaces extends AxiomTestCase {
    public TestAddNamespaces(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestSerialize` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/entref/TestSerialize.java`
#### Snippet
```java
import org.apache.axiom.ts.AxiomTestCase;

public class TestSerialize extends AxiomTestCase {
    public TestSerialize(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestAddNamespaces2` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/xpath/TestAddNamespaces2.java`
#### Snippet
```java
import org.apache.axiom.ts.AxiomTestCase;

public class TestAddNamespaces2 extends AxiomTestCase {
    public TestAddNamespaces2(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `AXIOMXPathTestCase` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/xpath/AXIOMXPathTestCase.java`
#### Snippet
```java
import org.jaxen.Navigator;

public class AXIOMXPathTestCase extends JaxenXPathTestBase {
    private final OMMetaFactory metaFactory;

```

### JUnitMalformedDeclaration
Test class `TestAddChild` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/cross/TestAddChild.java`
#### Snippet
```java
import org.apache.axiom.om.OMMetaFactory;

public class TestAddChild extends CrossOMTestCase {
    public TestAddChild(OMMetaFactory metaFactory, OMMetaFactory altMetaFactory) {
        super(metaFactory, altMetaFactory);
```

### JUnitMalformedDeclaration
Test class `TestDetachWithStream` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/builder/TestDetachWithStream.java`
#### Snippet
```java
import org.apache.axiom.ts.xml.XMLSample;

public class TestDetachWithStream extends AxiomTestCase {
    private final StreamType streamType;
    private final boolean useStreamSource;
```

### JUnitMalformedDeclaration
Test class `TestCreateOMBuilderXOP` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/builder/TestCreateOMBuilderXOP.java`
#### Snippet
```java
import org.apache.axiom.ts.xml.XOPSample;

public class TestCreateOMBuilderXOP extends AxiomTestCase {
    private final XOPSample sample;
    private final boolean build;
```

### JUnitMalformedDeclaration
Test class `TestCreateOMBuilderFromSAXSource` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/builder/TestCreateOMBuilderFromSAXSource.java`
#### Snippet
```java
import org.xml.sax.InputSource;

public class TestCreateOMBuilderFromSAXSource extends ConformanceTestCase {
    private final SAXImplementation implementation;
    private final Boolean expandEntityReferences;
```

### JUnitMalformedDeclaration
Test class `TestCreateStAXOMBuilderNamespaceRepairing2` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/builder/TestCreateStAXOMBuilderNamespaceRepairing2.java`
#### Snippet
```java
 * href="https://issues.apache.org/jira/browse/AXIOM-408">AXIOM-408</a>.
 */
public class TestCreateStAXOMBuilderNamespaceRepairing2 extends AxiomTestCase {
    public TestCreateStAXOMBuilderNamespaceRepairing2(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestCreateStAXOMBuilderIncorrectState` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/builder/TestCreateStAXOMBuilderIncorrectState.java`
#### Snippet
```java
 * XMLStreamConstants#START_DOCUMENT} or {@link XMLStreamConstants#END_DOCUMENT} event.
 */
public class TestCreateStAXOMBuilderIncorrectState extends AxiomTestCase {
    public TestCreateStAXOMBuilderIncorrectState(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetDocumentElement` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/builder/TestGetDocumentElement.java`
#### Snippet
```java
 * OMXMLParserWrapper#getDocumentElement(boolean)}.
 */
public class TestGetDocumentElement extends AxiomTestCase {
    private final BuilderFactory builderFactory;
    private final Boolean discardDocument;
```

### JUnitMalformedDeclaration
Test class `TestCreateStAXOMBuilderNamespaceRepairing` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/builder/TestCreateStAXOMBuilderNamespaceRepairing.java`
#### Snippet
```java
 * repairing.
 */
public class TestCreateStAXOMBuilderNamespaceRepairing extends AxiomTestCase {
    public TestCreateStAXOMBuilderNamespaceRepairing(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestInvalidXML` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/builder/TestInvalidXML.java`
#### Snippet
```java
import org.apache.axiom.ts.AxiomTestCase;

public class TestInvalidXML extends AxiomTestCase {
    public TestInvalidXML(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestCloseWithXMLStreamReader` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/builder/TestCloseWithXMLStreamReader.java`
#### Snippet
```java
import org.apache.axiom.ts.xml.XMLSample;

public class TestCloseWithXMLStreamReader extends AxiomTestCase {
    public TestCloseWithXMLStreamReader(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetDocumentElementWithIllFormedDocument` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/builder/TestGetDocumentElementWithIllFormedDocument.java`
#### Snippet
```java
 * returning <code>null</code>) if the input document has no document element.
 */
public class TestGetDocumentElementWithIllFormedDocument extends AxiomTestCase {
    public TestGetDocumentElementWithIllFormedDocument(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestStandaloneConfiguration` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/builder/TestStandaloneConfiguration.java`
#### Snippet
```java
 * InputStream)} with {@link StAXParserConfiguration#STANDALONE}.
 */
public class TestStandaloneConfiguration extends AxiomTestCase {
    public TestStandaloneConfiguration(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestRootPartStreaming` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/builder/TestRootPartStreaming.java`
#### Snippet
```java
 * href="https://issues.apache.org/jira/browse/AXIOM-403">AXIOM-403</a>.
 */
public class TestRootPartStreaming extends AxiomTestCase {
    public TestRootPartStreaming(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestCreateOMBuilderFromDOM` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/builder/TestCreateOMBuilderFromDOM.java`
#### Snippet
```java
import org.xml.sax.InputSource;

public class TestCreateOMBuilderFromDOM extends ConformanceTestCase {
    private final DOMImplementation implementation;
    private final Boolean expandEntityReferences;
```

### JUnitMalformedDeclaration
Test class `TestDetachWithDOM` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/builder/TestDetachWithDOM.java`
#### Snippet
```java
import org.w3c.dom.Element;

public class TestDetachWithDOM extends AxiomTestCase {
    private final boolean useDOMSource;

```

### JUnitMalformedDeclaration
Test class `TestMalformedDocument` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/builder/TestMalformedDocument.java`
#### Snippet
```java

/** Test that a document that is not well formed triggers an appropriate error. */
public class TestMalformedDocument extends AxiomTestCase {
    public TestMalformedDocument(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestCreateOMBuilderFromDOMWithNSUnawareUnprefixedAttribute` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/builder/TestCreateOMBuilderFromDOMWithNSUnawareUnprefixedAttribute.java`
#### Snippet
```java
import org.w3c.dom.Element;

public class TestCreateOMBuilderFromDOMWithNSUnawareUnprefixedAttribute extends AxiomTestCase {
    public TestCreateOMBuilderFromDOMWithNSUnawareUnprefixedAttribute(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestCloseWithStream` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/builder/TestCloseWithStream.java`
#### Snippet
```java
import org.apache.axiom.ts.xml.XMLSample;

public class TestCloseWithStream extends AxiomTestCase {
    private final StreamType streamType;

```

### JUnitMalformedDeclaration
Test class `TestCreateStAXOMBuilderFromXmlBeansPullParser` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/builder/TestCreateStAXOMBuilderFromXmlBeansPullParser.java`
#### Snippet
```java
import za.co.eskom.nrs.xmlvend.base.x20.schema.AdviceReqDocument;

public class TestCreateStAXOMBuilderFromXmlBeansPullParser extends AxiomTestCase {
    public TestCreateStAXOMBuilderFromXmlBeansPullParser(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestCreateStAXOMBuilderFromFragment` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/builder/TestCreateStAXOMBuilderFromFragment.java`
#### Snippet
```java
 * XMLStreamConstants#START_ELEMENT} event.
 */
public class TestCreateStAXOMBuilderFromFragment extends AxiomTestCase {
    public TestCreateStAXOMBuilderFromFragment(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestIOExceptionInGetText` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/builder/TestIOExceptionInGetText.java`
#### Snippet
```java
 * longer attempts to access the parser.
 */
public class TestIOExceptionInGetText extends AxiomTestCase {
    public TestIOExceptionInGetText(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetDocumentElementWithDiscardDocumentIllFormedEpilog` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/builder/TestGetDocumentElementWithDiscardDocumentIllFormedEpilog.java`
#### Snippet
```java
 * well formed. This situation should be detected.
 */
public class TestGetDocumentElementWithDiscardDocumentIllFormedEpilog extends AxiomTestCase {
    public TestGetDocumentElementWithDiscardDocumentIllFormedEpilog(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestDetachWithSAXSource` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/builder/TestDetachWithSAXSource.java`
#### Snippet
```java
import org.xml.sax.InputSource;

public class TestDetachWithSAXSource extends AxiomTestCase {
    public TestDetachWithSAXSource(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestCreateOMBuilderFromDOMWithNSUnawarePrefixedAttribute` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/builder/TestCreateOMBuilderFromDOMWithNSUnawarePrefixedAttribute.java`
#### Snippet
```java
import org.w3c.dom.Element;

public class TestCreateOMBuilderFromDOMWithNSUnawarePrefixedAttribute extends AxiomTestCase {
    public TestCreateOMBuilderFromDOMWithNSUnawarePrefixedAttribute(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestCloseWithSystemId` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/builder/TestCloseWithSystemId.java`
#### Snippet
```java
import org.apache.axiom.ts.xml.XMLSample;

public class TestCloseWithSystemId extends AxiomTestCase {
    public TestCloseWithSystemId(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestSerialize` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/comment/TestSerialize.java`
#### Snippet
```java
import org.apache.axiom.ts.AxiomTestCase;

public class TestSerialize extends AxiomTestCase {
    public TestSerialize(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestSerialize` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/doctype/TestSerialize.java`
#### Snippet
```java
import org.apache.axiom.ts.AxiomTestCase;

public class TestSerialize extends AxiomTestCase {
    public TestSerialize(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestCreateOMBuilderFromDOMElement` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/builder/TestCreateOMBuilderFromDOMElement.java`
#### Snippet
```java
 * defined by that element.
 */
public class TestCreateOMBuilderFromDOMElement extends AxiomTestCase {
    public TestCreateOMBuilderFromDOMElement(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestFindNamespaceByPrefix` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestFindNamespaceByPrefix.java`
#### Snippet
```java
import org.apache.axiom.ts.AxiomTestCase;

public class TestFindNamespaceByPrefix extends AxiomTestCase {
    public TestFindNamespaceByPrefix(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetChildrenRemove2` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestGetChildrenRemove2.java`
#### Snippet
```java

/** test the remove exception behavior, consecutive remove calls */
public class TestGetChildrenRemove2 extends AxiomTestCase {
    public TestGetChildrenRemove2(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestCreateOMBuilderFromDOMWithNSUnawareNamespaceDeclaration` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/builder/TestCreateOMBuilderFromDOMWithNSUnawareNamespaceDeclaration.java`
#### Snippet
```java
import org.w3c.dom.Element;

public class TestCreateOMBuilderFromDOMWithNSUnawareNamespaceDeclaration extends AxiomTestCase {
    private final String prefix;

```

### JUnitMalformedDeclaration
Test class `TestFindNamespaceCaseSensitivity` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestFindNamespaceCaseSensitivity.java`
#### Snippet
```java
import org.apache.axiom.ts.AxiomTestCase;

public class TestFindNamespaceCaseSensitivity extends AxiomTestCase {
    public TestFindNamespaceCaseSensitivity(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestReadAttachmentBeforeRootPartComplete` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/builder/TestReadAttachmentBeforeRootPartComplete.java`
#### Snippet
```java
 * </ul>
 */
public class TestReadAttachmentBeforeRootPartComplete extends AxiomTestCase {
    public TestReadAttachmentBeforeRootPartComplete(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestDetachWithDifferentBuilder` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestDetachWithDifferentBuilder.java`
#### Snippet
```java
import org.apache.axiom.ts.AxiomTestCase;

public class TestDetachWithDifferentBuilder extends AxiomTestCase {
    public TestDetachWithDifferentBuilder(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestWriteTextToWithNonTextNodes` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestWriteTextToWithNonTextNodes.java`
#### Snippet
```java
import org.apache.axiom.ts.AxiomTestCase;

public class TestWriteTextToWithNonTextNodes extends AxiomTestCase {
    public TestWriteTextToWithNonTextNodes(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestHasNameWithNamespace` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestHasNameWithNamespace.java`
#### Snippet
```java
import javax.xml.namespace.QName;

public class TestHasNameWithNamespace extends AxiomTestCase {
    public TestHasNameWithNamespace(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetChildren` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestGetChildren.java`
#### Snippet
```java

/** Test the plain iterator which includes all the children (including the texts) */
public class TestGetChildren extends AxiomTestCase {
    public TestGetChildren(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetQNameWithoutNamespace` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestGetQNameWithoutNamespace.java`
#### Snippet
```java
import org.apache.axiom.ts.AxiomTestCase;

public class TestGetQNameWithoutNamespace extends AxiomTestCase {
    public TestGetQNameWithoutNamespace(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetTextAsStreamWithNonTextChildren` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestGetTextAsStreamWithNonTextChildren.java`
#### Snippet
```java
import org.apache.commons.io.IOUtils;

public class TestGetTextAsStreamWithNonTextChildren extends AxiomTestCase {
    public TestGetTextAsStreamWithNonTextChildren(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetChildrenRemove3` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestGetChildrenRemove3.java`
#### Snippet
```java

/** Remove all! */
public class TestGetChildrenRemove3 extends AxiomTestCase {
    public TestGetChildrenRemove3(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetFirstChildWithName` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestGetFirstChildWithName.java`
#### Snippet
```java

/** Tests the behavior of {@link OMElement#getFirstChildWithName(QName)}. */
public class TestGetFirstChildWithName extends AxiomTestCase {
    public TestGetFirstChildWithName(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestRemoveAttribute` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestRemoveAttribute.java`
#### Snippet
```java

/** Tests the behavior of {@link OMElement#removeAttribute(OMAttribute)}. */
public class TestRemoveAttribute extends AxiomTestCase {
    public TestRemoveAttribute(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetAllAttributes1` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestGetAllAttributes1.java`
#### Snippet
```java

/** Test {@link OMElement#getAllAttributes()} on a programmatically created document. */
public class TestGetAllAttributes1 extends AxiomTestCase {
    public TestGetAllAttributes1(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetFirstOMChildAfterDiscard` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestGetFirstOMChildAfterDiscard.java`
#### Snippet
```java
 * created.
 */
public class TestGetFirstOMChildAfterDiscard extends AxiomTestCase {
    public TestGetFirstOMChildAfterDiscard(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestSetTextQNameWithEmptyPrefix` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestSetTextQNameWithEmptyPrefix.java`
#### Snippet
```java
 * but an empty prefix. In this case, the method is expected to generate a prefix.
 */
public class TestSetTextQNameWithEmptyPrefix extends AxiomTestCase {
    public TestSetTextQNameWithEmptyPrefix(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetChildrenWithNameNextWithoutHasNext` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestGetChildrenWithNameNextWithoutHasNext.java`
#### Snippet
```java
 * href="https://issues.apache.org/jira/browse/AXIOM-172">AXIOM-172</a>.
 */
public class TestGetChildrenWithNameNextWithoutHasNext extends AxiomTestCase {
    public TestGetChildrenWithNameNextWithoutHasNext(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestAddChildWithParent` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestAddChildWithParent.java`
#### Snippet
```java
 */
// TODO: update Javadoc of OMContainer
public class TestAddChildWithParent extends AxiomTestCase {
    public TestAddChildWithParent(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestRemoveAttributeNotOwner` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestRemoveAttributeNotOwner.java`
#### Snippet
```java
 * not owned by the element.
 */
public class TestRemoveAttributeNotOwner extends AxiomTestCase {
    public TestRemoveAttributeNotOwner(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestDigestWithNamespace` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestDigestWithNamespace.java`
#### Snippet
```java
import org.apache.axiom.ts.om.DigestTestCase;

public class TestDigestWithNamespace extends DigestTestCase {
    public TestDigestWithNamespace(OMMetaFactory metaFactory) {
        super(metaFactory, "MD5", "76e696f6b648837de0fa32e2ad8f1250");
```

### JUnitMalformedDeclaration
Test class `TestGetDescendants` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestGetDescendants.java`
#### Snippet
```java
import org.apache.axiom.ts.AxiomTestCase;

public class TestGetDescendants extends AxiomTestCase {
    private final boolean includeSelf;

```

### JUnitMalformedDeclaration
Test class `TestGetChildrenConcurrentModification` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestGetChildrenConcurrentModification.java`
#### Snippet
```java
 * Iterator#remove()}.
 */
public class TestGetChildrenConcurrentModification extends AxiomTestCase {
    public TestGetChildrenConcurrentModification(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetNamespaceContext` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestGetNamespaceContext.java`
#### Snippet
```java

/** Tests {@link OMElement#getNamespaceContext(boolean)}. */
public class TestGetNamespaceContext extends AxiomTestCase {
    private final boolean detached;

```

### JUnitMalformedDeclaration
Test class `TestSerializationWithTwoNonBuiltOMElements` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestSerializationWithTwoNonBuiltOMElements.java`
#### Snippet
```java

/** Regression test for <a href="https://issues.apache.org/jira/browse/AXIOM-65">AXIOM-65</a>. */
public class TestSerializationWithTwoNonBuiltOMElements extends AxiomTestCase {
    public TestSerializationWithTwoNonBuiltOMElements(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestAddChild2` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestAddChild2.java`
#### Snippet
```java
import org.apache.axiom.ts.AxiomTestCase;

public class TestAddChild2 extends AxiomTestCase {
    public TestAddChild2(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetPrefixWithDefaultNamespace` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestGetPrefixWithDefaultNamespace.java`
#### Snippet
```java
 * an {@link OMElement} that has a namespace without a prefix.
 */
public class TestGetPrefixWithDefaultNamespace extends AxiomTestCase {
    public TestGetPrefixWithDefaultNamespace(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestDeclareNamespaceWithEmptyPrefix` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestDeclareNamespaceWithEmptyPrefix.java`
#### Snippet
```java
 * Tests the behavior of {@link OMElement#declareNamespace(String, String)} with an empty prefix.
 */
public class TestDeclareNamespaceWithEmptyPrefix extends AxiomTestCase {
    public TestDeclareNamespaceWithEmptyPrefix(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetSAXSourceIdentityTransform` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestGetSAXSourceIdentityTransform.java`
#### Snippet
```java
import org.apache.axiom.ts.jaxp.xslt.XSLTImplementation;

public class TestGetSAXSourceIdentityTransform extends AxiomTestCase {
    private final XSLTImplementation xsltImplementation;
    private final boolean cache;
```

### JUnitMalformedDeclaration
Test class `TestGetChildrenWithName2` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestGetChildrenWithName2.java`
#### Snippet
```java
import org.apache.axiom.ts.AxiomTestCase;

public class TestGetChildrenWithName2 extends AxiomTestCase {
    public TestGetChildrenWithName2(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestDeclareDefaultNamespaceConflict1` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestDeclareDefaultNamespaceConflict1.java`
#### Snippet
```java
 * href="https://issues.apache.org/jira/browse/AXIOM-376">AXIOM-376</a>.
 */
public class TestDeclareDefaultNamespaceConflict1 extends AxiomTestCase {
    public TestDeclareDefaultNamespaceConflict1(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestSetTextQName` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestSetTextQName.java`
#### Snippet
```java
 * declaration if necessary.
 */
public class TestSetTextQName extends AxiomTestCase {
    public TestSetTextQName(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetXMLStreamReaderWithCaching` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestGetXMLStreamReaderWithCaching.java`
#### Snippet
```java
 * OMContainer#getXMLStreamReader()}.
 */
public class TestGetXMLStreamReaderWithCaching extends AxiomTestCase {
    public TestGetXMLStreamReaderWithCaching(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestChildReDeclaringParentsDefaultNSWithPrefix` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestChildReDeclaringParentsDefaultNSWithPrefix.java`
#### Snippet
```java
import org.apache.axiom.ts.AxiomTestCase;

public class TestChildReDeclaringParentsDefaultNSWithPrefix extends AxiomTestCase {
    public TestChildReDeclaringParentsDefaultNSWithPrefix(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestIsCompleteAfterAddingIncompleteChild` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestIsCompleteAfterAddingIncompleteChild.java`
#### Snippet
```java
 * regression test for <a href="https://issues.apache.org/jira/browse/AXIOM-315">AXIOM-315</a>.
 */
public class TestIsCompleteAfterAddingIncompleteChild extends AxiomTestCase {
    public TestIsCompleteAfterAddingIncompleteChild(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetXMLStreamReaderOnNonRootElement` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestGetXMLStreamReaderOnNonRootElement.java`
#### Snippet
```java
 * href="https://issues.apache.org/jira/browse/AXIOM-288">AXIOM-288</a>.
 */
public class TestGetXMLStreamReaderOnNonRootElement extends AxiomTestCase {
    private final boolean cache;

```

### JUnitMalformedDeclaration
Test class `TestGetDescendantsRemoveSubtree` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestGetDescendantsRemoveSubtree.java`
#### Snippet
```java
 * OMContainer#getDescendants(boolean)} when used to remove an element with child nodes.
 */
public class TestGetDescendantsRemoveSubtree extends AxiomTestCase {
    public TestGetDescendantsRemoveSubtree(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetAllAttributes2` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestGetAllAttributes2.java`
#### Snippet
```java
 * doesn't attempt to return namespace declarations.
 */
public class TestGetAllAttributes2 extends AxiomTestCase {
    public TestGetAllAttributes2(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestDeclareNamespaceWithGeneratedPrefix3` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestDeclareNamespaceWithGeneratedPrefix3.java`
#### Snippet
```java
 * specified prefix is <code>null</code>.
 */
public class TestDeclareNamespaceWithGeneratedPrefix3 extends AxiomTestCase {
    public TestDeclareNamespaceWithGeneratedPrefix3(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestSerializeAndConsumeConsumed` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestSerializeAndConsumeConsumed.java`
#### Snippet
```java
// TODO: in this scenario we should trigger a NodeUnavailableException as well; fix this with
// AXIOM-288
public class TestSerializeAndConsumeConsumed extends AxiomTestCase {
    public TestSerializeAndConsumeConsumed(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestSerializeAndConsumePartiallyBuilt` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestSerializeAndConsumePartiallyBuilt.java`
#### Snippet
```java
 * model tree that has been partially built. This is a regression test for AXIOM-151.
 */
public class TestSerializeAndConsumePartiallyBuilt extends AxiomTestCase {
    public TestSerializeAndConsumePartiallyBuilt(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestAddChildWithSameParent` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestAddChildWithSameParent.java`
#### Snippet
```java
 * of the list of children.
 */
public class TestAddChildWithSameParent extends AxiomTestCase {
    private final boolean build;

```

### JUnitMalformedDeclaration
Test class `TestAddAttributeWithoutNamespace` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestAddAttributeWithoutNamespace.java`
#### Snippet
```java
 * namespace.
 */
public class TestAddAttributeWithoutNamespace extends AxiomTestCase {
    private final AddAttributeStrategy addAttributeStrategy;
    private final NoNamespaceStrategy noNamespaceStrategy;
```

### JUnitMalformedDeclaration
Test class `TestResolveQNameWithUnboundPrefix` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestResolveQNameWithUnboundPrefix.java`
#### Snippet
```java
 * bound in the namespace context of the element.
 */
public class TestResolveQNameWithUnboundPrefix extends AxiomTestCase {
    public TestResolveQNameWithUnboundPrefix(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetTextBinary` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestGetTextBinary.java`
#### Snippet
```java
 * an element that has an {@link OMText} child constructed from a {@link Blob}.
 */
public class TestGetTextBinary extends AxiomTestCase {
    private final boolean compact;

```

### JUnitMalformedDeclaration
Test class `TestGetTextAsStreamWithSingleTextNode` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestGetTextAsStreamWithSingleTextNode.java`
#### Snippet
```java
import org.apache.commons.io.IOUtils;

public class TestGetTextAsStreamWithSingleTextNode extends AxiomTestCase {
    public TestGetTextAsStreamWithSingleTextNode(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetChildrenWithNamespaceURI` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestGetChildrenWithNamespaceURI.java`
#### Snippet
```java

/** Tests the behavior of {@link OMContainer#getChildrenWithNamespaceURI(String)}. */
public class TestGetChildrenWithNamespaceURI extends AxiomTestCase {
    public TestGetChildrenWithNamespaceURI(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestCloneOMElement2` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestCloneOMElement2.java`
#### Snippet
```java
import org.apache.axiom.ts.xml.XMLSample;

public class TestCloneOMElement2 extends ConformanceTestCase {
    public TestCloneOMElement2(OMMetaFactory metaFactory, XMLSample file) {
        super(metaFactory, file);
```

### JUnitMalformedDeclaration
Test class `TestGetAllDeclaredNamespacesRemove` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestGetAllDeclaredNamespacesRemove.java`
#### Snippet
```java
 * OMElement#getAllDeclaredNamespaces()} to remove a namespace declaration.
 */
public class TestGetAllDeclaredNamespacesRemove extends AxiomTestCase {
    public TestGetAllDeclaredNamespacesRemove(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetAllDeclaredNamespaces` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestGetAllDeclaredNamespaces.java`
#### Snippet
```java
import org.apache.axiom.ts.AxiomTestCase;

public class TestGetAllDeclaredNamespaces extends AxiomTestCase {
    public TestGetAllDeclaredNamespaces(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetText` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestGetText.java`
#### Snippet
```java
 * of type {@link OMText}.
 */
public class TestGetText extends AxiomTestCase {
    public TestGetText(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetAttributeValueNonExisting` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestGetAttributeValueNonExisting.java`
#### Snippet
```java
 * with the given QName.
 */
public class TestGetAttributeValueNonExisting extends AxiomTestCase {
    public TestGetAttributeValueNonExisting(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetTextWithCDATASectionChild` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestGetTextWithCDATASectionChild.java`
#### Snippet
```java
 * text node and a CDATA section.
 */
public class TestGetTextWithCDATASectionChild extends AxiomTestCase {
    public TestGetTextWithCDATASectionChild(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetTextAsQNameNoNamespace` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestGetTextAsQNameNoNamespace.java`
#### Snippet
```java
 * namespace in scope.
 */
public class TestGetTextAsQNameNoNamespace extends AxiomTestCase {
    public TestGetTextAsQNameNoNamespace(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestSetTextEmptyString` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestSetTextEmptyString.java`
#### Snippet
```java
 * parameter.
 */
public class TestSetTextEmptyString extends AxiomTestCase {
    public TestSetTextEmptyString(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetAttributeWithXmlPrefix1` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestGetAttributeWithXmlPrefix1.java`
#### Snippet
```java
 * href="https://issues.apache.org/jira/browse/AXIS2-329">AXIS2-329</a>.
 */
public class TestGetAttributeWithXmlPrefix1 extends AxiomTestCase {
    public TestGetAttributeWithXmlPrefix1(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestBuildDiscarded` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestBuildDiscarded.java`
#### Snippet
```java
 * discarded. In this case the method is expected to throw a {@link NodeUnavailableException}.
 */
public class TestBuildDiscarded extends AxiomTestCase {
    public TestBuildDiscarded(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestDeclareNamespace1` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestDeclareNamespace1.java`
#### Snippet
```java
import org.apache.axiom.ts.AxiomTestCase;

public class TestDeclareNamespace1 extends AxiomTestCase {
    public TestDeclareNamespace1(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestAddAttributeReuseExistingPrefix` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestAddAttributeReuseExistingPrefix.java`
#### Snippet
```java
 * namespace URI is in scope, the method reuses the existing prefix instead of generating one.
 */
public class TestAddAttributeReuseExistingPrefix extends AxiomTestCase {
    public TestAddAttributeReuseExistingPrefix(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestAddAttributeAlreadyOwnedByElement` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestAddAttributeAlreadyOwnedByElement.java`
#### Snippet
```java
 * owned by the element is a no-op.
 */
public class TestAddAttributeAlreadyOwnedByElement extends AxiomTestCase {
    public TestAddAttributeAlreadyOwnedByElement(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestDiscardDocumentElement` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestDiscardDocumentElement.java`
#### Snippet
```java
 * document element, i.e. the parent of which is an {@link OMDocument}.
 */
public class TestDiscardDocumentElement extends AxiomTestCase {
    public TestDiscardDocumentElement(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestAddAttributeWithExistingNamespaceDeclarationInScope` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestAddAttributeWithExistingNamespaceDeclarationInScope.java`
#### Snippet
```java
 * corresponding declaration is already in scope.
 */
public class TestAddAttributeWithExistingNamespaceDeclarationInScope extends AxiomTestCase {
    private final AddAttributeStrategy strategy;

```

### JUnitMalformedDeclaration
Test class `TestGetQNameWithNamespace` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestGetQNameWithNamespace.java`
#### Snippet
```java
import javax.xml.namespace.QName;

public class TestGetQNameWithNamespace extends AxiomTestCase {
    public TestGetQNameWithNamespace(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestWriteTextTo` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestWriteTextTo.java`
#### Snippet
```java
 * Writer#close()}.
 */
public class TestWriteTextTo extends AxiomTestCase {
    public TestWriteTextTo(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetFirstOMChildAfterConsume` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestGetFirstOMChildAfterConsume.java`
#### Snippet
```java
 * OMContainer#getXMLStreamReaderWithoutCaching()}.
 */
public class TestGetFirstOMChildAfterConsume extends AxiomTestCase {
    public TestGetFirstOMChildAfterConsume(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestAddAttributeWithoutExistingNamespaceDeclaration` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestAddAttributeWithoutExistingNamespaceDeclaration.java`
#### Snippet
```java
 * and a corresponding namespace declaration doesn't exist yet.
 */
public class TestAddAttributeWithoutExistingNamespaceDeclaration extends AxiomTestCase {
    private final AddAttributeStrategy strategy;

```

### JUnitMalformedDeclaration
Test class `TestSetNamespace` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestSetNamespace.java`
#### Snippet
```java
 * OMNamedInformationItem#setNamespace(OMNamespace, boolean)}.
 */
public class TestSetNamespace extends SetNamespaceTestCase {
    private final Boolean declare;

```

### JUnitMalformedDeclaration
Test class `TestGetTextAsQNameEmpty` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestGetTextAsQNameEmpty.java`
#### Snippet
```java

/** Tests the behavior of {@link OMElement#getTextAsQName()} if the element is empty. */
public class TestGetTextAsQNameEmpty extends AxiomTestCase {
    public TestGetTextAsQNameEmpty(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetChildrenRemove4` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestGetChildrenRemove4.java`
#### Snippet
```java

/** test whether the children count reduces. */
public class TestGetChildrenRemove4 extends AxiomTestCase {
    public TestGetChildrenRemove4(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetChildrenRemove1` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestGetChildrenRemove1.java`
#### Snippet
```java

/** test the remove exception behavior */
public class TestGetChildrenRemove1 extends AxiomTestCase {
    public TestGetChildrenRemove1(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestAddAttributeReplace` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestAddAttributeReplace.java`
#### Snippet
```java
 * namespace URI already exists.
 */
public class TestAddAttributeReplace extends AxiomTestCase {
    private final AddAttributeStrategy strategy;

```

### JUnitMalformedDeclaration
Test class `TestGetXMLStreamReaderWithoutCachingPartiallyBuiltModified` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestGetXMLStreamReaderWithoutCachingPartiallyBuiltModified.java`
#### Snippet
```java
 * pull through mode.
 */
public class TestGetXMLStreamReaderWithoutCachingPartiallyBuiltModified extends AxiomTestCase {
    public TestGetXMLStreamReaderWithoutCachingPartiallyBuiltModified(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetXMLStreamReaderWithPreserveNamespaceContext2` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestGetXMLStreamReaderWithPreserveNamespaceContext2.java`
#### Snippet
```java
 * href="https://issues.apache.org/jira/browse/AXIOM-485">AXIOM-485</a>.
 */
public class TestGetXMLStreamReaderWithPreserveNamespaceContext2 extends AxiomTestCase {
    public TestGetXMLStreamReaderWithPreserveNamespaceContext2(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetAttributeValueWithXmlPrefix1` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestGetAttributeValueWithXmlPrefix1.java`
#### Snippet
```java
 * <a href="https://issues.apache.org/jira/browse/AXIS2-329">AXIS2-329</a>.
 */
public class TestGetAttributeValueWithXmlPrefix1 extends AxiomTestCase {
    public TestGetAttributeValueWithXmlPrefix1(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetTextAsStreamWithoutCaching` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestGetTextAsStreamWithoutCaching.java`
#### Snippet
```java
import org.apache.axiom.ts.AxiomTestCase;

public class TestGetTextAsStreamWithoutCaching extends AxiomTestCase {
    public TestGetTextAsStreamWithoutCaching(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestAddAttributeWithInvalidNamespace1` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestAddAttributeWithInvalidNamespace1.java`
#### Snippet
```java
 * @see TestCreateOMAttributeWithInvalidNamespace1
 */
public class TestAddAttributeWithInvalidNamespace1 extends AxiomTestCase {
    public TestAddAttributeWithInvalidNamespace1(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestAddAttributeAlreadyOwnedByOtherElement` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestAddAttributeAlreadyOwnedByOtherElement.java`
#### Snippet
```java
 * owned by another element will clone the attribute.
 */
public class TestAddAttributeAlreadyOwnedByOtherElement extends AxiomTestCase {
    public TestAddAttributeAlreadyOwnedByOtherElement(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestAddAttributeWithExistingNamespaceDeclarationOnSameElement` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestAddAttributeWithExistingNamespaceDeclarationOnSameElement.java`
#### Snippet
```java
 * corresponding declaration already exists on the element.
 */
public class TestAddAttributeWithExistingNamespaceDeclarationOnSameElement extends AxiomTestCase {
    private final AddAttributeStrategy strategy;

```

### JUnitMalformedDeclaration
Test class `TestGetNamespacesInScopeWithMaskedNamespace` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestGetNamespacesInScopeWithMaskedNamespace.java`
#### Snippet
```java
import org.apache.axiom.ts.AxiomTestCase;

public class TestGetNamespacesInScopeWithMaskedNamespace extends AxiomTestCase {
    public TestGetNamespacesInScopeWithMaskedNamespace(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetPrefixWithNamespace` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestGetPrefixWithNamespace.java`
#### Snippet
```java
 * {@link OMElement} that has a namespace with a prefix.
 */
public class TestGetPrefixWithNamespace extends AxiomTestCase {
    public TestGetPrefixWithNamespace(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetXMLStreamReaderOnNonRootElementPartiallyBuilt` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestGetXMLStreamReaderOnNonRootElementPartiallyBuilt.java`
#### Snippet
```java
 * built.
 */
public class TestGetXMLStreamReaderOnNonRootElementPartiallyBuilt extends AxiomTestCase {
    private final boolean cache;
    private final int build;
```

### JUnitMalformedDeclaration
Test class `TestResolveQNameWithNonDefaultNamespace` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestResolveQNameWithNonDefaultNamespace.java`
#### Snippet
```java
import org.apache.axiom.ts.AxiomTestCase;

public class TestResolveQNameWithNonDefaultNamespace extends AxiomTestCase {
    public TestResolveQNameWithNonDefaultNamespace(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestAddChildDiscarded` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestAddChildDiscarded.java`
#### Snippet
```java
 * this case the method is expected to throw a {@link NodeUnavailableException}.
 */
public class TestAddChildDiscarded extends AxiomTestCase {
    public TestAddChildDiscarded(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetSAXSourceIdentityTransformOnFragment` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestGetSAXSourceIdentityTransformOnFragment.java`
#### Snippet
```java
 * href="https://issues.apache.org/jira/browse/SYNAPSE-501">SYNAPSE-501</a>).
 */
public class TestGetSAXSourceIdentityTransformOnFragment extends AxiomTestCase {
    private final XSLTImplementation xsltImplementation;
    private final boolean cache;
```

### JUnitMalformedDeclaration
Test class `TestGetChildElementsConsumed` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestGetChildElementsConsumed.java`
#### Snippet
```java
 * already been consumed by {@link OMContainer#getXMLStreamReaderWithoutCaching()}.
 */
public class TestGetChildElementsConsumed extends AxiomTestCase {
    public TestGetChildElementsConsumed(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetChildrenWithName4` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestGetChildrenWithName4.java`
#### Snippet
```java
import org.apache.axiom.ts.AxiomTestCase;

public class TestGetChildrenWithName4 extends AxiomTestCase {
    private static final String NS_A = "urn://a";
    private static final String NS_B = "urn://b";
```

### JUnitMalformedDeclaration
Test class `TestRemoveChildren` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestRemoveChildren.java`
#### Snippet
```java

/** Tests {@link OMContainer#removeChildren()} on an {@link OMElement}. */
public class TestRemoveChildren extends AxiomTestCase {
    private final boolean complete;

```

### JUnitMalformedDeclaration
Test class `TestGetNamespaceURIWithoutNamespace` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestGetNamespaceURIWithoutNamespace.java`
#### Snippet
```java
 * invoked on an {@link OMElement} that has no namespace.
 */
public class TestGetNamespaceURIWithoutNamespace extends AxiomTestCase {
    public TestGetNamespaceURIWithoutNamespace(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetNamespaceNormalized` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestGetNamespaceNormalized.java`
#### Snippet
```java
 * href="https://issues.apache.org/jira/browse/AXIOM-398">AXIOM-398</a>.
 */
public class TestGetNamespaceNormalized extends AxiomTestCase {
    private final boolean useNull;

```

### JUnitMalformedDeclaration
Test class `TestGetXMLStreamReaderWithoutCachingPartiallyBuilt` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestGetXMLStreamReaderWithoutCachingPartiallyBuilt.java`
#### Snippet
```java
 * an element that has been partially built. This is a regression test for AXIOM-393.
 */
public class TestGetXMLStreamReaderWithoutCachingPartiallyBuilt extends AxiomTestCase {
    public TestGetXMLStreamReaderWithoutCachingPartiallyBuilt(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestHasNameWithoutNamespace` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestHasNameWithoutNamespace.java`
#### Snippet
```java
import org.apache.axiom.ts.AxiomTestCase;

public class TestHasNameWithoutNamespace extends AxiomTestCase {
    public TestHasNameWithoutNamespace(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestFindNamespaceByNamespaceURIMasked` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestFindNamespaceByNamespaceURIMasked.java`
#### Snippet
```java
 * corresponding prefix is redeclared.
 */
public class TestFindNamespaceByNamespaceURIMasked extends AxiomTestCase {
    public TestFindNamespaceByNamespaceURIMasked(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetNamespaceNormalizedWithParser` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestGetNamespaceNormalizedWithParser.java`
#### Snippet
```java
 * regression test for <a href="https://issues.apache.org/jira/browse/AXIOM-398">AXIOM-398</a>.
 */
public class TestGetNamespaceNormalizedWithParser extends AxiomTestCase {
    public TestGetNamespaceNormalizedWithParser(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetSAXResultWithDTD` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestGetSAXResultWithDTD.java`
#### Snippet
```java
 * {@link OMContainer#getSAXResult()} is silently ignored.
 */
public class TestGetSAXResultWithDTD extends AxiomTestCase {
    public TestGetSAXResultWithDTD(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestAddChild` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestAddChild.java`
#### Snippet
```java
import org.apache.axiom.ts.AxiomTestCase;

public class TestAddChild extends AxiomTestCase {
    public TestAddChild(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetXMLStreamReaderCDATAEventFromElement` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestGetXMLStreamReaderCDATAEventFromElement.java`
#### Snippet
```java
import org.apache.axiom.ts.AxiomTestCase;

public class TestGetXMLStreamReaderCDATAEventFromElement extends AxiomTestCase {
    public TestGetXMLStreamReaderCDATAEventFromElement(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetAttributeValueWithXmlPrefix2` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestGetAttributeValueWithXmlPrefix2.java`
#### Snippet
```java
 * <a href="https://issues.apache.org/jira/browse/AXIS2-329">AXIS2-329</a>.
 */
public class TestGetAttributeValueWithXmlPrefix2 extends AxiomTestCase {
    public TestGetAttributeValueWithXmlPrefix2(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestAddAttributeMultiple` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestAddAttributeMultiple.java`
#### Snippet
```java
 * declaration is generated for each of them.
 */
public class TestAddAttributeMultiple extends AxiomTestCase {
    private final AddAttributeStrategy strategy;

```

### JUnitMalformedDeclaration
Test class `TestResolveQNameWithoutNamespace` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestResolveQNameWithoutNamespace.java`
#### Snippet
```java
import org.apache.axiom.ts.AxiomTestCase;

public class TestResolveQNameWithoutNamespace extends AxiomTestCase {
    public TestResolveQNameWithoutNamespace(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetChildrenWithLocalName` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestGetChildrenWithLocalName.java`
#### Snippet
```java
import org.apache.axiom.ts.xml.XMLSample;

public class TestGetChildrenWithLocalName extends AxiomTestCase {
    public TestGetChildrenWithLocalName(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetXMLStreamReaderWithNamespaceURIInterning` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestGetXMLStreamReaderWithNamespaceURIInterning.java`
#### Snippet
```java
 * OMXMLStreamReaderConfiguration#isNamespaceURIInterning()} set to <code>true</code>.
 */
public class TestGetXMLStreamReaderWithNamespaceURIInterning extends AxiomTestCase {
    public TestGetXMLStreamReaderWithNamespaceURIInterning(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestMultipleDefaultNS` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestMultipleDefaultNS.java`
#### Snippet
```java
import org.apache.axiom.ts.AxiomTestCase;

public class TestMultipleDefaultNS extends AxiomTestCase {
    public TestMultipleDefaultNS(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestSetTextQNameWithExistingChildren` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestSetTextQNameWithExistingChildren.java`
#### Snippet
```java
 * children.
 */
public class TestSetTextQNameWithExistingChildren extends AxiomTestCase {
    public TestSetTextQNameWithExistingChildren(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestResolveQNameWithDefaultNamespace` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestResolveQNameWithDefaultNamespace.java`
#### Snippet
```java
import org.apache.axiom.ts.AxiomTestCase;

public class TestResolveQNameWithDefaultNamespace extends AxiomTestCase {
    public TestResolveQNameWithDefaultNamespace(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetAttributeWithXmlPrefix2` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestGetAttributeWithXmlPrefix2.java`
#### Snippet
```java
 * href="https://issues.apache.org/jira/browse/AXIS2-329">AXIS2-329</a>.
 */
public class TestGetAttributeWithXmlPrefix2 extends AxiomTestCase {
    public TestGetAttributeWithXmlPrefix2(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestSetTextNull` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestSetTextNull.java`
#### Snippet
```java
 * parameter.
 */
public class TestSetTextNull extends AxiomTestCase {
    public TestSetTextNull(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetXMLStreamReaderCDATAEventFromParser` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestGetXMLStreamReaderCDATAEventFromParser.java`
#### Snippet
```java

// Regression test for AXIOM-144 and AXIOM-146
public class TestGetXMLStreamReaderCDATAEventFromParser extends AxiomTestCase {
    public TestGetXMLStreamReaderCDATAEventFromParser(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestIsCompleteWithParser` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestIsCompleteWithParser.java`
#### Snippet
```java
 * parsing is in effect.
 */
public class TestIsCompleteWithParser extends AxiomTestCase {
    public TestIsCompleteWithParser(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestAddChildIncomplete` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestAddChildIncomplete.java`
#### Snippet
```java
 * @see org.apache.axiom.ts.om.document.TestAddChildIncomplete
 */
public class TestAddChildIncomplete extends AxiomTestCase {
    public TestAddChildIncomplete(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetNamespacesInScope` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestGetNamespacesInScope.java`
#### Snippet
```java
import org.apache.axiom.ts.AxiomTestCase;

public class TestGetNamespacesInScope extends AxiomTestCase {
    public TestGetNamespacesInScope(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestDeclareNamespaceInvalid2` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestDeclareNamespaceInvalid2.java`
#### Snippet
```java
 * XML 1.0 and XML 1.1.
 */
public class TestDeclareNamespaceInvalid2 extends AxiomTestCase {
    public TestDeclareNamespaceInvalid2(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetDefaultNamespace2` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestGetDefaultNamespace2.java`
#### Snippet
```java
 * href="https://issues.apache.org/jira/browse/AXIOM-400">AXIOM-400</a>.
 */
public class TestGetDefaultNamespace2 extends AxiomTestCase {
    public TestGetDefaultNamespace2(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetTextWithMixedOMTextChildren` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestGetTextWithMixedOMTextChildren.java`
#### Snippet
```java
 * CDATA section.
 */
public class TestGetTextWithMixedOMTextChildren extends AxiomTestCase {
    public TestGetTextWithMixedOMTextChildren(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestAddAttributeWithInvalidNamespace2` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestAddAttributeWithInvalidNamespace2.java`
#### Snippet
```java
 * @see TestCreateOMAttributeWithInvalidNamespace2
 */
public class TestAddAttributeWithInvalidNamespace2 extends AxiomTestCase {
    public TestAddAttributeWithInvalidNamespace2(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestChildReDeclaringGrandParentsDefaultNSWithPrefix` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestChildReDeclaringGrandParentsDefaultNSWithPrefix.java`
#### Snippet
```java
import org.apache.axiom.ts.AxiomTestCase;

public class TestChildReDeclaringGrandParentsDefaultNSWithPrefix extends AxiomTestCase {
    public TestChildReDeclaringGrandParentsDefaultNSWithPrefix(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetAllDeclaredNamespacesNoSuchElementException` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestGetAllDeclaredNamespacesNoSuchElementException.java`
#### Snippet
```java
 * {@link Iterator} contract with respect to throwing {@link NoSuchElementException}.
 */
public class TestGetAllDeclaredNamespacesNoSuchElementException extends AxiomTestCase {
    public TestGetAllDeclaredNamespacesNoSuchElementException(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetNamespaceNormalizedWithSAXSource` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestGetNamespaceNormalizedWithSAXSource.java`
#### Snippet
```java
 * href="https://issues.apache.org/jira/browse/AXIOM-398">AXIOM-398</a>.
 */
public class TestGetNamespaceNormalizedWithSAXSource extends AxiomTestCase {
    public TestGetNamespaceNormalizedWithSAXSource(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestClose` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestClose.java`
#### Snippet
```java
import org.apache.axiom.ts.xml.XMLSample;

public class TestClose extends AxiomTestCase {
    public TestClose(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetChildElements` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestGetChildElements.java`
#### Snippet
```java

/** Test the element iterator */
public class TestGetChildElements extends AxiomTestCase {
    public TestGetChildElements(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetNamespacesInScopeWithMaskedDefaultNamespace` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestGetNamespacesInScopeWithMaskedDefaultNamespace.java`
#### Snippet
```java
import org.apache.axiom.ts.AxiomTestCase;

public class TestGetNamespacesInScopeWithMaskedDefaultNamespace extends AxiomTestCase {
    public TestGetNamespacesInScopeWithMaskedDefaultNamespace(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestSetText` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestSetText.java`
#### Snippet
```java

/** Tests the behavior of {@link OMElement#setText(String)} when invoked on an empty element. */
public class TestSetText extends AxiomTestCase {
    public TestSetText(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestDeclareDefaultNamespace2` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestDeclareDefaultNamespace2.java`
#### Snippet
```java
import org.apache.axiom.ts.AxiomTestCase;

public class TestDeclareDefaultNamespace2 extends AxiomTestCase {
    public TestDeclareDefaultNamespace2(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestSetTextQNameNull` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestSetTextQNameNull.java`
#### Snippet
```java
 * parameter.
 */
public class TestSetTextQNameNull extends AxiomTestCase {
    public TestSetTextQNameNull(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestSerialization` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestSerialization.java`
#### Snippet
```java
 * </ul>
 */
public class TestSerialization extends AxiomTestCase {
    private static final String NS = "urn:ns";
    private static final String PREFIX = "p";
```

### JUnitMalformedDeclaration
Test class `TestFindNamespaceURIWithPrefixUndeclaring` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestFindNamespaceURIWithPrefixUndeclaring.java`
#### Snippet
```java
import org.apache.axiom.ts.AxiomTestCase;

public class TestFindNamespaceURIWithPrefixUndeclaring extends AxiomTestCase {
    public TestFindNamespaceURIWithPrefixUndeclaring(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetPrefixWithoutNamespace` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestGetPrefixWithoutNamespace.java`
#### Snippet
```java
 * an {@link OMElement} that has no namespace.
 */
public class TestGetPrefixWithoutNamespace extends AxiomTestCase {
    public TestGetPrefixWithoutNamespace(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetTextAsQNameWithExtraWhitespace` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestGetTextAsQNameWithExtraWhitespace.java`
#### Snippet
```java
 * surrounding whitespace.
 */
public class TestGetTextAsQNameWithExtraWhitespace extends AxiomTestCase {
    public TestGetTextAsQNameWithExtraWhitespace(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetNamespacesInScopeWithDefaultNamespace` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestGetNamespacesInScopeWithDefaultNamespace.java`
#### Snippet
```java
import org.apache.axiom.ts.AxiomTestCase;

public class TestGetNamespacesInScopeWithDefaultNamespace extends AxiomTestCase {
    public TestGetNamespacesInScopeWithDefaultNamespace(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetChildrenWithName3` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestGetChildrenWithName3.java`
#### Snippet
```java

/** Regression test for <a href="https://issues.apache.org/jira/browse/AXIOM-166">AXIOM-166</a>. */
public class TestGetChildrenWithName3 extends AxiomTestCase {
    public TestGetChildrenWithName3(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestDeclareDefaultNamespace1` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestDeclareDefaultNamespace1.java`
#### Snippet
```java
import org.apache.axiom.ts.AxiomTestCase;

public class TestDeclareDefaultNamespace1 extends AxiomTestCase {
    public TestDeclareDefaultNamespace1(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetDefaultNamespace` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestGetDefaultNamespace.java`
#### Snippet
```java
import org.apache.axiom.ts.AxiomTestCase;

public class TestGetDefaultNamespace extends AxiomTestCase {
    public TestGetDefaultNamespace(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetFirstChildWithNameOnIncompleteElement` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestGetFirstChildWithNameOnIncompleteElement.java`
#### Snippet
```java
 * href="https://issues.apache.org/jira/browse/AXIOM-333">AXIOM-333</a>.
 */
public class TestGetFirstChildWithNameOnIncompleteElement extends AxiomTestCase {
    public TestGetFirstChildWithNameOnIncompleteElement(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetChildElementsConcurrentModification` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestGetChildElementsConcurrentModification.java`
#### Snippet
```java
 * href="https://issues.apache.org/jira/browse/AXIOM-391">AXIOM-391</a>.
 */
public class TestGetChildElementsConcurrentModification extends AxiomTestCase {
    public TestGetChildElementsConcurrentModification(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetTextAsQName` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestGetTextAsQName.java`
#### Snippet
```java

/** Tests the behavior of {@link OMElement#getTextAsQName()} for a prefixed QName. */
public class TestGetTextAsQName extends AxiomTestCase {
    public TestGetTextAsQName(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestAddAttributeWithMaskedNamespaceDeclaration` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestAddAttributeWithMaskedNamespaceDeclaration.java`
#### Snippet
```java
 * <p>Note that because of WSTX-202, Axiom will not be able to serialize the resulting XML.
 */
public class TestAddAttributeWithMaskedNamespaceDeclaration extends AxiomTestCase {
    private final AddAttributeStrategy strategy;

```

### JUnitMalformedDeclaration
Test class `TestDiscardPartiallyBuilt` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestDiscardPartiallyBuilt.java`
#### Snippet
```java
 * child of the element.
 */
public class TestDiscardPartiallyBuilt extends AxiomTestCase {
    public TestDiscardPartiallyBuilt(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestSetTextQNameWithoutNamespace` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestSetTextQNameWithoutNamespace.java`
#### Snippet
```java
 * the empty namespace name).
 */
public class TestSetTextQNameWithoutNamespace extends AxiomTestCase {
    public TestSetTextQNameWithoutNamespace(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestDeclareNamespaceWithGeneratedPrefix1` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestDeclareNamespaceWithGeneratedPrefix1.java`
#### Snippet
```java
 * specified in the {@link OMNamespace} object is <code>null</code>.
 */
public class TestDeclareNamespaceWithGeneratedPrefix1 extends AxiomTestCase {
    public TestDeclareNamespaceWithGeneratedPrefix1(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetChildrenWithName` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestGetChildrenWithName.java`
#### Snippet
```java

/** Test the element iterator */
public class TestGetChildrenWithName extends AxiomTestCase {
    public TestGetChildrenWithName(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestAddAttributeGeneratedPrefix` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestAddAttributeGeneratedPrefix.java`
#### Snippet
```java
 * namespace URI is in scope, the method generates a prefix.
 */
public class TestAddAttributeGeneratedPrefix extends AxiomTestCase {
    private final boolean defaultNamespaceInScope;

```

### JUnitMalformedDeclaration
Test class `TestGetXMLStreamReaderWithIncompleteDescendant` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestGetXMLStreamReaderWithIncompleteDescendant.java`
#### Snippet
```java
 * href="https://issues.apache.org/jira/browse/AXIOM-431">AXIOM-431</a>.
 */
public class TestGetXMLStreamReaderWithIncompleteDescendant extends AxiomTestCase {
    private final boolean cache;

```

### JUnitMalformedDeclaration
Test class `TestDiscardIncomplete` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestDiscardIncomplete.java`
#### Snippet
```java

/** Test the discard method */
public class TestDiscardIncomplete extends AxiomTestCase {
    public TestDiscardIncomplete(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestDeclareNamespaceInvalid1` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestDeclareNamespaceInvalid1.java`
#### Snippet
```java
 * XML 1.0 and XML 1.1.
 */
public class TestDeclareNamespaceInvalid1 extends AxiomTestCase {
    public TestDeclareNamespaceInvalid1(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestUndeclarePrefix` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestUndeclarePrefix.java`
#### Snippet
```java
import org.apache.axiom.ts.AxiomTestCase;

public class TestUndeclarePrefix extends AxiomTestCase {
    public TestUndeclarePrefix(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetNamespaceURIWithNamespace` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestGetNamespaceURIWithNamespace.java`
#### Snippet
```java
 * invoked on an {@link OMElement} that has a namespace.
 */
public class TestGetNamespaceURIWithNamespace extends AxiomTestCase {
    public TestGetNamespaceURIWithNamespace(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestDigestWithoutNamespace` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestDigestWithoutNamespace.java`
#### Snippet
```java

// Regression test for AXIOM-203
public class TestDigestWithoutNamespace extends DigestTestCase {
    public TestDigestWithoutNamespace(OMMetaFactory metaFactory) {
        super(metaFactory, "MD5", "0730b7dda59f628a10b53b5502682ceb");
```

### JUnitMalformedDeclaration
Test class `TestDeclareDefaultNamespaceConflict2` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestDeclareDefaultNamespaceConflict2.java`
#### Snippet
```java
 * href="https://issues.apache.org/jira/browse/AXIOM-376">AXIOM-376</a>.
 */
public class TestDeclareDefaultNamespaceConflict2 extends AxiomTestCase {
    public TestDeclareDefaultNamespaceConflict2(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestCloneOMElementNamespaceRepairing` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestCloneOMElementNamespaceRepairing.java`
#### Snippet
```java
/** Tests that {@link OMElement#cloneOMElement()} performs namespace repairing. */
// TODO: this is actually undocumented; in addition, it should probably made configurable
public class TestCloneOMElementNamespaceRepairing extends AxiomTestCase {
    public TestCloneOMElementNamespaceRepairing(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestSetTextWithExistingChildren` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestSetTextWithExistingChildren.java`
#### Snippet
```java
 * children.
 */
public class TestSetTextWithExistingChildren extends AxiomTestCase {
    public TestSetTextWithExistingChildren(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestSerializeAndConsumeWithIncompleteDescendant` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestSerializeAndConsumeWithIncompleteDescendant.java`
#### Snippet
```java
 * </code> set to true) is added to an existing tree.
 */
public class TestSerializeAndConsumeWithIncompleteDescendant extends AxiomTestCase {
    public TestSerializeAndConsumeWithIncompleteDescendant(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetElementText` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/sr/TestGetElementText.java`
#### Snippet
```java
import org.apache.axiom.ts.AxiomTestCase;

public class TestGetElementText extends AxiomTestCase {
    public TestGetElementText(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestFactoryIsSingleton` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/factory/TestFactoryIsSingleton.java`
#### Snippet
```java
 * same instance.
 */
public class TestFactoryIsSingleton extends AxiomTestCase {
    public TestFactoryIsSingleton(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetElementTextFromParser` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/sr/TestGetElementTextFromParser.java`
#### Snippet
```java
import org.xml.sax.InputSource;

public class TestGetElementTextFromParser extends AxiomTestCase {
    private final BuilderFactory builderFactory;
    private final boolean cache;
```

### JUnitMalformedDeclaration
Test class `TestNextTag` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/sr/TestNextTag.java`
#### Snippet
```java
import org.apache.axiom.ts.AxiomTestCase;

public class TestNextTag extends AxiomTestCase {
    public TestNextTag(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetNamespaceContext` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/sr/TestGetNamespaceContext.java`
#### Snippet
```java
import org.apache.axiom.ts.AxiomTestCase;

public class TestGetNamespaceContext extends AxiomTestCase {
    private final boolean cache;

```

### JUnitMalformedDeclaration
Test class `TestGetXMLStreamReaderWithPreserveNamespaceContext` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestGetXMLStreamReaderWithPreserveNamespaceContext.java`
#### Snippet
```java
 * OMXMLStreamReaderConfiguration#isPreserveNamespaceContext()}.
 */
public class TestGetXMLStreamReaderWithPreserveNamespaceContext extends AxiomTestCase {
    private final boolean preserveNamespaceContext;
    private final boolean cache;
```

### JUnitMalformedDeclaration
Test class `TestGetBlobFromElement` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/sr/TestGetBlobFromElement.java`
#### Snippet
```java
 * XMLStreamReader} returned by {@link OMElement#getXMLStreamReader(boolean)}.
 */
public class TestGetBlobFromElement extends AxiomTestCase {
    private final boolean cache;

```

### JUnitMalformedDeclaration
Test class `TestCloseAndContinueBuilding` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/sr/TestCloseAndContinueBuilding.java`
#### Snippet
```java
 * href="https://issues.apache.org/jira/browse/AXIOM-288">AXIOM-288</a>.
 */
public class TestCloseAndContinueBuilding extends AxiomTestCase {
    private final boolean cache;

```

### JUnitMalformedDeclaration
Test class `TestCreateOMText` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/factory/TestCreateOMText.java`
#### Snippet
```java
import org.apache.axiom.ts.AxiomTestCase;

public class TestCreateOMText extends AxiomTestCase {
    public TestCreateOMText(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestCreateOMDocument` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/factory/TestCreateOMDocument.java`
#### Snippet
```java
import org.apache.axiom.ts.AxiomTestCase;

public class TestCreateOMDocument extends AxiomTestCase {
    public TestCreateOMDocument(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestCreateOMNamespace` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/factory/TestCreateOMNamespace.java`
#### Snippet
```java
import org.apache.axiom.ts.AxiomTestCase;

public class TestCreateOMNamespace extends AxiomTestCase {
    public TestCreateOMNamespace(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestCommentEvent` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/sr/TestCommentEvent.java`
#### Snippet
```java
import org.xml.sax.InputSource;

public class TestCommentEvent extends AxiomTestCase {
    private final BuilderFactory builderFactory;
    private final boolean cache;
```

### JUnitMalformedDeclaration
Test class `TestCreateOMCommentWithoutParent` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/factory/TestCreateOMCommentWithoutParent.java`
#### Snippet
```java
import org.apache.axiom.ts.AxiomTestCase;

public class TestCreateOMCommentWithoutParent extends AxiomTestCase {
    public TestCreateOMCommentWithoutParent(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestCreateOMElementWithNullOMDataSource1` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/factory/TestCreateOMElementWithNullOMDataSource1.java`
#### Snippet
```java
 * data source is <code>null</code>.
 */
public class TestCreateOMElementWithNullOMDataSource1 extends AxiomTestCase {
    public TestCreateOMElementWithNullOMDataSource1(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestCreateOMAttributeWithInvalidNamespace2` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/factory/TestCreateOMAttributeWithInvalidNamespace2.java`
#### Snippet
```java
 * non-empty namespace but no prefix.
 */
public class TestCreateOMAttributeWithInvalidNamespace2 extends AxiomTestCase {
    public TestCreateOMAttributeWithInvalidNamespace2(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestCreateOMElementWithNamespaceInScope1` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/factory/TestCreateOMElementWithNamespaceInScope1.java`
#### Snippet
```java
 * specified for the {@link OMElement} to be created.
 */
public class TestCreateOMElementWithNamespaceInScope1 extends CreateOMElementTestCase {
    public TestCreateOMElementWithNamespaceInScope1(
            OMMetaFactory metaFactory, CreateOMElementVariant variant) {
```

### JUnitMalformedDeclaration
Test class `TestCreateOMElementWithNamespaceInScope4` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/factory/TestCreateOMElementWithNamespaceInScope4.java`
#### Snippet
```java
 * null</code>.
 */
public class TestCreateOMElementWithNamespaceInScope4 extends CreateOMElementTestCase {
    public TestCreateOMElementWithNamespaceInScope4(
            OMMetaFactory metaFactory, CreateOMElementVariant variant) {
```

### JUnitMalformedDeclaration
Test class `TestCreateOMElementWithoutNamespaceNullPrefix` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/factory/TestCreateOMElementWithoutNamespaceNullPrefix.java`
#### Snippet
```java
 * for an element without namespace this is not possible and an empty prefix must be used instead.
 */
public class TestCreateOMElementWithoutNamespaceNullPrefix extends CreateOMElementTestCase {
    public TestCreateOMElementWithoutNamespaceNullPrefix(
            OMMetaFactory metaFactory,
```

### JUnitMalformedDeclaration
Test class `TestCreateOMDocTypeWithoutParent` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/factory/TestCreateOMDocTypeWithoutParent.java`
#### Snippet
```java
import org.apache.axiom.ts.AxiomTestCase;

public class TestCreateOMDocTypeWithoutParent extends AxiomTestCase {
    public TestCreateOMDocTypeWithoutParent(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestCreateOMElementWithoutNamespace3` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/factory/TestCreateOMElementWithoutNamespace3.java`
#### Snippet
```java
 * declaration. In this case, no namespace declaration is added to the created element.
 */
public class TestCreateOMElementWithoutNamespace3 extends CreateOMElementTestCase {
    public TestCreateOMElementWithoutNamespace3(
            OMMetaFactory metaFactory, CreateOMElementVariant variant) {
```

### JUnitMalformedDeclaration
Test class `TestCreateOMAttributeWithInvalidNamespace1` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/factory/TestCreateOMAttributeWithInvalidNamespace1.java`
#### Snippet
```java
 * for an empty namespace.
 */
public class TestCreateOMAttributeWithInvalidNamespace1 extends AxiomTestCase {
    public TestCreateOMAttributeWithInvalidNamespace1(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestCreateOMAttributeInterfaces` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/factory/TestCreateOMAttributeInterfaces.java`
#### Snippet
```java
 * and {@link EntityReference} nodes).
 */
public class TestCreateOMAttributeInterfaces extends AxiomTestCase {
    public TestCreateOMAttributeInterfaces(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestCreateOMProcessingInstructionWithoutParent` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/factory/TestCreateOMProcessingInstructionWithoutParent.java`
#### Snippet
```java
import org.apache.axiom.ts.AxiomTestCase;

public class TestCreateOMProcessingInstructionWithoutParent extends AxiomTestCase {
    public TestCreateOMProcessingInstructionWithoutParent(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestCreateOMElementWithNullURIAndPrefix` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/factory/TestCreateOMElementWithNullURIAndPrefix.java`
#### Snippet
```java
import org.apache.axiom.ts.AxiomTestCase;

public class TestCreateOMElementWithNullURIAndPrefix extends AxiomTestCase {
    public TestCreateOMElementWithNullURIAndPrefix(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestCreateOMElementWithNamespaceInScope2` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/factory/TestCreateOMElementWithNamespaceInScope2.java`
#### Snippet
```java
 * not the existing prefix.
 */
public class TestCreateOMElementWithNamespaceInScope2 extends CreateOMElementTestCase {
    public TestCreateOMElementWithNamespaceInScope2(
            OMMetaFactory metaFactory, CreateOMElementVariant variant) {
```

### JUnitMalformedDeclaration
Test class `TestCreateOMTextFromBlobProvider` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/factory/TestCreateOMTextFromBlobProvider.java`
#### Snippet
```java
import org.apache.axiom.util.UIDGenerator;

public class TestCreateOMTextFromBlobProvider extends AxiomTestCase {
    static class TestBlobProvider implements BlobProvider {
        private Blob blob;
```

### JUnitMalformedDeclaration
Test class `TestCreateOMAttributeGeneratedPrefix` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/factory/TestCreateOMAttributeGeneratedPrefix.java`
#### Snippet
```java
 * an {@link OMNamespace} object with a null prefix and a non empty namespace URI is given.
 */
public class TestCreateOMAttributeGeneratedPrefix extends AxiomTestCase {
    public TestCreateOMAttributeGeneratedPrefix(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestCreateOMAttributeNullPrefixNoNamespace` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/factory/TestCreateOMAttributeNullPrefixNoNamespace.java`
#### Snippet
```java
 * namespace, this should give the same result as specifying an empty prefix.
 */
public class TestCreateOMAttributeNullPrefixNoNamespace extends AxiomTestCase {
    public TestCreateOMAttributeNullPrefixNoNamespace(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestCreateOMElementWithGeneratedPrefix` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/factory/TestCreateOMElementWithGeneratedPrefix.java`
#### Snippet
```java
 * generate a prefix.
 */
public class TestCreateOMElementWithGeneratedPrefix extends CreateOMElementTestCase {
    public TestCreateOMElementWithGeneratedPrefix(
            OMMetaFactory metaFactory,
```

### JUnitMalformedDeclaration
Test class `TestCreateOMEntityReferenceWithNullParent` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/factory/TestCreateOMEntityReferenceWithNullParent.java`
#### Snippet
```java
import org.apache.axiom.ts.AxiomTestCase;

public class TestCreateOMEntityReferenceWithNullParent extends AxiomTestCase {
    public TestCreateOMEntityReferenceWithNullParent(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestCreateOMTextFromOMText` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/factory/TestCreateOMTextFromOMText.java`
#### Snippet
```java

/** Tests {@link OMFactory#createOMText(OMContainer, OMText)}. */
public class TestCreateOMTextFromOMText extends AxiomTestCase {
    public TestCreateOMTextFromOMText(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestCreateOMElementWithInvalidNamespace` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/factory/TestCreateOMElementWithInvalidNamespace.java`
#### Snippet
```java
 * Neither XML 1.0 nor XML 1.1 allow binding a prefix to the empty namespace name.
 */
public class TestCreateOMElementWithInvalidNamespace extends CreateOMElementTestCase {
    public TestCreateOMElementWithInvalidNamespace(
            OMMetaFactory metaFactory,
```

### JUnitMalformedDeclaration
Test class `TestCreateOMElementWithoutNamespace` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/factory/TestCreateOMElementWithoutNamespace.java`
#### Snippet
```java
 * to the created element.
 */
public class TestCreateOMElementWithoutNamespace extends CreateOMElementTestCase {
    public TestCreateOMElementWithoutNamespace(
            OMMetaFactory metaFactory,
```

### JUnitMalformedDeclaration
Test class `TestCreateOMElementWithDefaultNamespace` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/factory/TestCreateOMElementWithDefaultNamespace.java`
#### Snippet
```java
import org.apache.axiom.om.OMNamespace;

public class TestCreateOMElementWithDefaultNamespace extends CreateOMElementTestCase {
    public TestCreateOMElementWithDefaultNamespace(
            OMMetaFactory metaFactory,
```

### JUnitMalformedDeclaration
Test class `TestCreateOMElementWithNullOMDataSource2` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/factory/TestCreateOMElementWithNullOMDataSource2.java`
#### Snippet
```java
 * is <code>null</code>.
 */
public class TestCreateOMElementWithNullOMDataSource2 extends AxiomTestCase {
    public TestCreateOMElementWithNullOMDataSource2(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestCreateOMNamespaceWithNullURI` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/factory/TestCreateOMNamespaceWithNullURI.java`
#### Snippet
```java
import org.apache.axiom.ts.AxiomTestCase;

public class TestCreateOMNamespaceWithNullURI extends AxiomTestCase {
    public TestCreateOMNamespaceWithNullURI(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetMetaFactory` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/factory/TestGetMetaFactory.java`
#### Snippet
```java
 * from which the reference to the {@link OMFactory} was obtained.
 */
public class TestGetMetaFactory extends AxiomTestCase {
    public TestGetMetaFactory(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestCreateOMEntityReference` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/factory/TestCreateOMEntityReference.java`
#### Snippet
```java
import org.apache.axiom.ts.AxiomTestCase;

public class TestCreateOMEntityReference extends AxiomTestCase {
    public TestCreateOMEntityReference(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestCreateOMElementWithNamespaceInScope3` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/factory/TestCreateOMElementWithNamespaceInScope3.java`
#### Snippet
```java
 * the existing prefix.
 */
public class TestCreateOMElementWithNamespaceInScope3 extends CreateOMElementTestCase {
    public TestCreateOMElementWithNamespaceInScope3(
            OMMetaFactory metaFactory, CreateOMElementVariant variant) {
```

### JUnitMalformedDeclaration
Test class `TestCreateOMElementWithoutNamespace2` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/factory/TestCreateOMElementWithoutNamespace2.java`
#### Snippet
```java
 * href="https://issues.apache.org/jira/browse/AXIOM-400">AXIOM-400</a>.
 */
public class TestCreateOMElementWithoutNamespace2 extends CreateOMElementTestCase {
    public TestCreateOMElementWithoutNamespace2(
            OMMetaFactory metaFactory, CreateOMElementVariant variant) {
```

### JUnitMalformedDeclaration
Test class `TestBuild` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/document/TestBuild.java`
#### Snippet
```java
import org.apache.axiom.ts.xml.XMLSample;

public class TestBuild extends AxiomTestCase {
    public TestBuild(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestClone` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/document/TestClone.java`
#### Snippet
```java
import org.apache.axiom.ts.xml.XMLSample;

public class TestClone extends ConformanceTestCase {
    public TestClone(OMMetaFactory metaFactory, XMLSample file) {
        super(metaFactory, file);
```

### JUnitMalformedDeclaration
Test class `TestSetOMDocumentElementReplaceSame` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/document/TestSetOMDocumentElementReplaceSame.java`
#### Snippet
```java
 * already the document element.
 */
public class TestSetOMDocumentElementReplaceSame extends AxiomTestCase {
    public TestSetOMDocumentElementReplaceSame(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestCreateOMTextWithNullParent` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/factory/TestCreateOMTextWithNullParent.java`
#### Snippet
```java
 * node by setting <code>parent</code> to <code>null</code>.
 */
public class TestCreateOMTextWithNullParent extends AxiomTestCase {
    public TestCreateOMTextWithNullParent(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestDigest` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/document/TestDigest.java`
#### Snippet
```java
import org.apache.axiom.ts.om.DigestTestCase;

public class TestDigest extends DigestTestCase {
    private final String file;

```

### JUnitMalformedDeclaration
Test class `TestCreateOMElementWithNonDefaultNamespace` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/factory/TestCreateOMElementWithNonDefaultNamespace.java`
#### Snippet
```java
import org.apache.axiom.om.OMNamespace;

public class TestCreateOMElementWithNonDefaultNamespace extends CreateOMElementTestCase {
    public TestCreateOMElementWithNonDefaultNamespace(
            OMMetaFactory metaFactory,
```

### JUnitMalformedDeclaration
Test class `TestRemoveChildren` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/document/TestRemoveChildren.java`
#### Snippet
```java

/** Tests {@link OMContainer#removeChildren()} on an {@link OMContainer}. */
public class TestRemoveChildren extends AxiomTestCase {
    private final boolean complete;
    private final boolean accessDocumentElement;
```

### JUnitMalformedDeclaration
Test class `TestGetOMDocumentElementWithParser` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/document/TestGetOMDocumentElementWithParser.java`
#### Snippet
```java
 * constructed from a stream.
 */
public class TestGetOMDocumentElementWithParser extends AxiomTestCase {
    public TestGetOMDocumentElementWithParser(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetOMDocumentElementAfterDetach` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/document/TestGetOMDocumentElementAfterDetach.java`
#### Snippet
```java
 * href="https://issues.apache.org/jira/browse/AXIOM-361">AXIOM-361</a>.
 */
public class TestGetOMDocumentElementAfterDetach extends AxiomTestCase {
    public TestGetOMDocumentElementAfterDetach(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestAddChildWithExistingDocumentElement` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/document/TestAddChildWithExistingDocumentElement.java`
#### Snippet
```java
 * an {@link OMDocument} that already has a document element results in an exception.
 */
public class TestAddChildWithExistingDocumentElement extends AxiomTestCase {
    public TestAddChildWithExistingDocumentElement(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestSetOMDocumentElementReplace` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/document/TestSetOMDocumentElementReplace.java`
#### Snippet
```java
 * has a document element.
 */
public class TestSetOMDocumentElementReplace extends AxiomTestCase {
    public TestSetOMDocumentElementReplace(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetOMDocumentElement` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/document/TestGetOMDocumentElement.java`
#### Snippet
```java
 * empty document.
 */
public class TestGetOMDocumentElement extends AxiomTestCase {
    public TestGetOMDocumentElement(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetSAXResult` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/document/TestGetSAXResult.java`
#### Snippet
```java
import org.apache.axiom.ts.xml.XMLSample;

public class TestGetSAXResult extends ConformanceTestCase {
    private final XSLTImplementation xsltImplementation;

```

### JUnitMalformedDeclaration
Test class `TestGetSAXResultSAXParser` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/document/TestGetSAXResultSAXParser.java`
#### Snippet
```java
import org.xml.sax.XMLReader;

public class TestGetSAXResultSAXParser extends AxiomTestCase {
    private final SAXImplementation saxImplementation;
    private final XMLSample file;
```

### JUnitMalformedDeclaration
Test class `TestSerializeWithIgnoreXMLDeclaration` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/document/TestSerializeWithIgnoreXMLDeclaration.java`
#### Snippet
```java
import org.apache.axiom.om.OMOutputFormat;

public class TestSerializeWithIgnoreXMLDeclaration extends XMLDeclarationSerializationTestCase {
    public TestSerializeWithIgnoreXMLDeclaration(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestSerializeXMLDeclaration` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/document/TestSerializeXMLDeclaration.java`
#### Snippet
```java
import org.apache.axiom.om.OMMetaFactory;

public class TestSerializeXMLDeclaration extends XMLDeclarationSerializationTestCase {
    public TestSerializeXMLDeclaration(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestSerializeXML11` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/document/TestSerializeXML11.java`
#### Snippet
```java
import org.apache.axiom.om.OMMetaFactory;

public class TestSerializeXML11 extends XMLDeclarationSerializationTestCase {
    public TestSerializeXML11(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestIsCompleteAfterAddingIncompleteChild` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/document/TestIsCompleteAfterAddingIncompleteChild.java`
#### Snippet
```java
 * regression test for <a href="https://issues.apache.org/jira/browse/AXIOM-315">AXIOM-315</a>.
 */
public class TestIsCompleteAfterAddingIncompleteChild extends AxiomTestCase {
    public TestIsCompleteAfterAddingIncompleteChild(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestSerializeAndConsume` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/document/TestSerializeAndConsume.java`
#### Snippet
```java
 * href="https://issues.apache.org/jira/browse/AXIOM-24">AXIOM-24</a>.
 */
public class TestSerializeAndConsume extends AxiomTestCase {
    public TestSerializeAndConsume(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestSetOMDocumentElementNew` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/document/TestSetOMDocumentElementNew.java`
#### Snippet
```java
 * document element.
 */
public class TestSetOMDocumentElementNew extends AxiomTestCase {
    public TestSetOMDocumentElementNew(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestAddChildIncomplete` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/document/TestAddChildIncomplete.java`
#### Snippet
```java
 * @see org.apache.axiom.ts.om.element.TestAddChildIncomplete
 */
public class TestAddChildIncomplete extends AxiomTestCase {
    public TestAddChildIncomplete(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetSAXResultXMLBeans` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/document/TestGetSAXResultXMLBeans.java`
#### Snippet
```java
import org.xml.sax.ext.LexicalHandler;

public class TestGetSAXResultXMLBeans extends AxiomTestCase {
    public TestGetSAXResultXMLBeans(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestSetOMDocumentElementNull` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/document/TestSetOMDocumentElementNull.java`
#### Snippet
```java
 * parameter.
 */
public class TestSetOMDocumentElementNull extends AxiomTestCase {
    public TestSetOMDocumentElementNull(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestSerializeUTF16` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/document/TestSerializeUTF16.java`
#### Snippet
```java
import org.apache.axiom.om.OMOutputFormat;

public class TestSerializeUTF16 extends XMLDeclarationSerializationTestCase {
    public TestSerializeUTF16(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestSerializeAndConsumeWithIncompleteDescendant` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/document/TestSerializeAndConsumeWithIncompleteDescendant.java`
#### Snippet
```java
 * </code> set to true) is added to an existing document.
 */
public class TestSerializeAndConsumeWithIncompleteDescendant extends AxiomTestCase {
    public TestSerializeAndConsumeWithIncompleteDescendant(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestDTDReaderFromParser` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/document/sr/TestDTDReaderFromParser.java`
#### Snippet
```java
 * an OM tree created by a builder correctly implements the {@link DTDReader} extension.
 */
public class TestDTDReaderFromParser extends AxiomTestCase {
    private final boolean build;
    private final boolean cache;
```

### JUnitMalformedDeclaration
Test class `TestGetPrefixWithoutNamespace` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/attribute/TestGetPrefixWithoutNamespace.java`
#### Snippet
```java
 * an {@link OMAttribute} that has no namespace.
 */
public class TestGetPrefixWithoutNamespace extends AxiomTestCase {
    public TestGetPrefixWithoutNamespace(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetNamespaceNormalized` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/attribute/TestGetNamespaceNormalized.java`
#### Snippet
```java
 * href="https://issues.apache.org/jira/browse/AXIOM-398">AXIOM-398</a>.
 */
public class TestGetNamespaceNormalized extends AxiomTestCase {
    public TestGetNamespaceNormalized(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestDTDReader` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/document/sr/TestDTDReader.java`
#### Snippet
```java
 * programmatically created OM tree correctly implements the {@link DTDReader} extension.
 */
public class TestDTDReader extends AxiomTestCase {
    public TestDTDReader(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestCharacterDataReaderFromParser` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/document/sr/TestCharacterDataReaderFromParser.java`
#### Snippet
```java
 * for an OM tree created by a builder correctly implements the {@link DTDReader} extension.
 */
public class TestCharacterDataReaderFromParser extends AxiomTestCase {
    private final boolean cache;

```

### JUnitMalformedDeclaration
Test class `TestCloseWithoutCaching` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/document/sr/TestCloseWithoutCaching.java`
#### Snippet
```java
 * rest of the document.
 */
public class TestCloseWithoutCaching extends AxiomTestCase {
    public TestCloseWithoutCaching(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestDigestWithNamespace` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/attribute/TestDigestWithNamespace.java`
#### Snippet
```java
import org.apache.axiom.ts.om.DigestTestCase;

public class TestDigestWithNamespace extends DigestTestCase {
    public TestDigestWithNamespace(OMMetaFactory metaFactory) {
        super(metaFactory, "MD5", "0403b2798bee0397c2755c01d560092e");
```

### JUnitMalformedDeclaration
Test class `TestGetAttributeTypeDefault` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/attribute/TestGetAttributeTypeDefault.java`
#### Snippet
```java
 * Tests that the default attribute type for programmatically created attributes is {@code CDATA}.
 */
public class TestGetAttributeTypeDefault extends AxiomTestCase {
    public TestGetAttributeTypeDefault(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestHasNameWithNamespace` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/attribute/TestHasNameWithNamespace.java`
#### Snippet
```java
 * namespace) created by {@link OMFactory#createOMAttribute(String, OMNamespace, String)}.
 */
public class TestHasNameWithNamespace extends AxiomTestCase {
    public TestHasNameWithNamespace(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestSetNamespace` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/attribute/TestSetNamespace.java`
#### Snippet
```java
 * {@link OMAttribute}.
 */
public class TestSetNamespace extends SetNamespaceTestCase {
    private final boolean declare;
    private final boolean owner;
```

### JUnitMalformedDeclaration
Test class `TestGetNamespaceURIWithoutNamespace` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/attribute/TestGetNamespaceURIWithoutNamespace.java`
#### Snippet
```java
 * invoked on an {@link OMAttribute} that has no namespace.
 */
public class TestGetNamespaceURIWithoutNamespace extends AxiomTestCase {
    public TestGetNamespaceURIWithoutNamespace(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestHasNameWithoutNamespace` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/attribute/TestHasNameWithoutNamespace.java`
#### Snippet
```java
 * namespace) created by {@link OMFactory#createOMAttribute(String, OMNamespace, String)}.
 */
public class TestHasNameWithoutNamespace extends AxiomTestCase {
    public TestHasNameWithoutNamespace(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetQNameWithoutNamespace` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/attribute/TestGetQNameWithoutNamespace.java`
#### Snippet
```java
 * namespace) created by {@link OMFactory#createOMAttribute(String, OMNamespace, String)}.
 */
public class TestGetQNameWithoutNamespace extends AxiomTestCase {
    public TestGetQNameWithoutNamespace(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestSetLocalName` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/attribute/TestSetLocalName.java`
#### Snippet
```java
import org.apache.axiom.ts.AxiomTestCase;

public class TestSetLocalName extends AxiomTestCase {
    public TestSetLocalName(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetNamespaceURIWithNamespace` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/attribute/TestGetNamespaceURIWithNamespace.java`
#### Snippet
```java
 * invoked on an {@link OMAttribute} that has a namespace.
 */
public class TestGetNamespaceURIWithNamespace extends AxiomTestCase {
    public TestGetNamespaceURIWithNamespace(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetPrefixWithNamespace` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/attribute/TestGetPrefixWithNamespace.java`
#### Snippet
```java
 * {@link OMAttribute} that has a namespace.
 */
public class TestGetPrefixWithNamespace extends AxiomTestCase {
    public TestGetPrefixWithNamespace(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestDigestWithoutNamespace` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/attribute/TestDigestWithoutNamespace.java`
#### Snippet
```java

// Regression test for AXIOM-203
public class TestDigestWithoutNamespace extends DigestTestCase {
    public TestDigestWithoutNamespace(OMMetaFactory metaFactory) {
        super(metaFactory, "MD5", "78f0b570fedc6e01679f501b7b75d069");
```

### JUnitMalformedDeclaration
Test class `TestGetQNameWithNamespace` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/attribute/TestGetQNameWithNamespace.java`
#### Snippet
```java
 * namespace) created by {@link OMFactory#createOMAttribute(String, OMNamespace, String)}.
 */
public class TestGetQNameWithNamespace extends AxiomTestCase {
    public TestGetQNameWithNamespace(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetBuilderNull` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/container/TestGetBuilderNull.java`
#### Snippet
```java
 * node.
 */
public class TestGetBuilderNull extends AxiomTestCase {
    private final OMContainerFactory containerFactory;

```

### JUnitMalformedDeclaration
Test class `TestAddChildWithIncompleteSibling` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/container/TestAddChildWithIncompleteSibling.java`
#### Snippet
```java
 * OMContainer#build()}) method would fail on {@link OMDocument} instances.
 */
public class TestAddChildWithIncompleteSibling extends AxiomTestCase {
    private final OMContainerFactory containerFactory;

```

### JUnitMalformedDeclaration
Test class `TestHashCode` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/namespace/TestHashCode.java`
#### Snippet
```java

/** Tests that the {@link OMNamespace} overrides {@link Object#hashCode()}. */
public class TestHashCode extends AxiomTestCase {
    public TestHashCode(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetPrefix` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/namespace/TestGetPrefix.java`
#### Snippet
```java

/** Tests the behavior of {@link OMNamespace#getPrefix()}. */
public class TestGetPrefix extends AxiomTestCase {
    public TestGetPrefix(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestSerialize` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/container/TestSerialize.java`
#### Snippet
```java
import org.xml.sax.InputSource;

public class TestSerialize extends ConformanceTestCase {
    private final OMContainerExtractor containerExtractor;
    private final SerializationStrategy serializationStrategy;
```

### JUnitMalformedDeclaration
Test class `TestEqualsWithNullPrefix` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/namespace/TestEqualsWithNullPrefix.java`
#### Snippet
```java
 * instance with non prefix.
 */
public class TestEqualsWithNullPrefix extends AxiomTestCase {
    public TestEqualsWithNullPrefix(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetDescendants` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/container/TestGetDescendants.java`
#### Snippet
```java
import org.apache.axiom.ts.AxiomTestCase;

public class TestGetDescendants extends AxiomTestCase {
    private final OMContainerFactory containerFactory;
    private final boolean includeSelf;
```

### JUnitMalformedDeclaration
Test class `TestGetXMLStreamReader` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/container/TestGetXMLStreamReader.java`
#### Snippet
```java
 * native StAX parser.
 */
public class TestGetXMLStreamReader extends ConformanceTestCase {
    private final BuilderFactory builderFactory;
    private final OMContainerExtractor containerExtractor;
```

### JUnitMalformedDeclaration
Test class `TestObjectEqualsWithDifferentPrefixes` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/namespace/TestObjectEqualsWithDifferentPrefixes.java`
#### Snippet
```java
 * different prefixes.
 */
public class TestObjectEqualsWithDifferentPrefixes extends AxiomTestCase {
    public TestObjectEqualsWithDifferentPrefixes(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestObjectEqualsWithDifferentURIs` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/namespace/TestObjectEqualsWithDifferentURIs.java`
#### Snippet
```java
 * different namespace URIs.
 */
public class TestObjectEqualsWithDifferentURIs extends AxiomTestCase {
    public TestObjectEqualsWithDifferentURIs(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetNamespaceURI` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/namespace/TestGetNamespaceURI.java`
#### Snippet
```java

/** Tests the behavior of {@link OMNamespace#getNamespaceURI()}. */
public class TestGetNamespaceURI extends AxiomTestCase {
    public TestGetNamespaceURI(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestObjectEquals` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/namespace/TestObjectEquals.java`
#### Snippet
```java
 * namespace URI and prefix.
 */
public class TestObjectEquals extends AxiomTestCase {
    public TestObjectEquals(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetNamespaceNormalized` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestGetNamespaceNormalized.java`
#### Snippet
```java
 * href="https://issues.apache.org/jira/browse/AXIOM-398">AXIOM-398</a>.
 */
public class TestGetNamespaceNormalized extends AxiomTestCase {
    public TestGetNamespaceNormalized(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestWrappedTextNodeOMDataSourceFromReader` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestWrappedTextNodeOMDataSourceFromReader.java`
#### Snippet
```java
import org.apache.axiom.ts.AxiomTestCase;

public class TestWrappedTextNodeOMDataSourceFromReader extends AxiomTestCase {
    public TestWrappedTextNodeOMDataSourceFromReader(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestEquals` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/namespace/TestEquals.java`
#### Snippet
```java
 * instance with non null prefix.
 */
public class TestEquals extends AxiomTestCase {
    public TestEquals(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetDocumentFromBuilder` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestGetDocumentFromBuilder.java`
#### Snippet
```java
 * on the builder associated with an {@link OMSourcedElement}.
 */
public class TestGetDocumentFromBuilder extends AxiomTestCase {
    public TestGetDocumentFromBuilder(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestWriteTextToWithNonDestructiveOMDataSource` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestWriteTextToWithNonDestructiveOMDataSource.java`
#### Snippet
```java
import org.apache.axiom.ts.AxiomTestCase;

public class TestWriteTextToWithNonDestructiveOMDataSource extends AxiomTestCase {
    public TestWriteTextToWithNonDestructiveOMDataSource(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestBlobOMDataSource` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestBlobOMDataSource.java`
#### Snippet
```java

/** Tests functionality of BlobOMDataSource */
public class TestBlobOMDataSource extends AxiomTestCase {
    public TestBlobOMDataSource(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestName1DefaultPrefix` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName1DefaultPrefix.java`
#### Snippet
```java
 * Serialize and cache Prefix test
 */
public class TestName1DefaultPrefix extends AxiomTestCase {
    public TestName1DefaultPrefix(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestAddAttribute` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestAddAttribute.java`
#### Snippet
```java
 * that may be produced during expansion.
 */
public class TestAddAttribute extends AxiomTestCase {
    private final AddAttributeStrategy strategy;

```

### JUnitMalformedDeclaration
Test class `TestGetNamespace` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestGetNamespace.java`
#### Snippet
```java
import org.apache.axiom.om.OMSourcedElement;

public class TestGetNamespace extends LazyNameTestCase {
    public TestGetNamespace(
            OMMetaFactory metaFactory, OMSourcedElementVariant variant, QName qname) {
```

### JUnitMalformedDeclaration
Test class `TestGetReaderException` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestGetReaderException.java`
#### Snippet
```java
 * propagate the original exception (wrapped in an {@link OMException}).
 */
public class TestGetReaderException extends AxiomTestCase {
    public TestGetReaderException(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestName3QualifiedPrefix` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName3QualifiedPrefix.java`
#### Snippet
```java
 * Serialize and cache Tests attempt to rename namespace and localpart, which is not allowed
 */
public class TestName3QualifiedPrefix extends AxiomTestCase {
    public TestName3QualifiedPrefix(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestStringOMDataSource` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestStringOMDataSource.java`
#### Snippet
```java

/** Tests functionality of ByteArrayDataSource */
public class TestStringOMDataSource extends AxiomTestCase {
    public TestStringOMDataSource(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestCloseOnComplete` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestCloseOnComplete.java`
#### Snippet
```java
 * built.
 */
public class TestCloseOnComplete extends AxiomTestCase {
    public TestCloseOnComplete(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestName2QualifiedPrefix` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName2QualifiedPrefix.java`
#### Snippet
```java
 * Serialize and consume Tests update of prefix
 */
public class TestName2QualifiedPrefix extends AxiomTestCase {
    public TestName2QualifiedPrefix(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetNamespaceNormalized2` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestGetNamespaceNormalized2.java`
#### Snippet
```java
 * href="https://issues.apache.org/jira/browse/AXIOM-398">AXIOM-398</a>.
 */
public class TestGetNamespaceNormalized2 extends AxiomTestCase {
    public TestGetNamespaceNormalized2(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetAllAttributes` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestGetAllAttributes.java`
#### Snippet
```java
 * instances.
 */
public class TestGetAllAttributes extends AxiomTestCase {
    public TestGetAllAttributes(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestName2DefaultPrefix` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName2DefaultPrefix.java`
#### Snippet
```java
 * Serialize and consume Tests update of prefix
 */
public class TestName2DefaultPrefix extends AxiomTestCase {
    public TestName2DefaultPrefix(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestSetLocalName` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestSetLocalName.java`
#### Snippet
```java
 * of the root element returned by the data source.
 */
public class TestSetLocalName extends AxiomTestCase {
    private boolean expand;

```

### JUnitMalformedDeclaration
Test class `TestGetObject` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestGetObject.java`
#### Snippet
```java
import org.apache.axiom.ts.AxiomTestCase;

public class TestGetObject extends AxiomTestCase {
    public TestGetObject(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetNextOMSiblingIncomplete` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestGetNextOMSiblingIncomplete.java`
#### Snippet
```java
 * OMSourcedElement} that is expanded but not complete.
 */
public class TestGetNextOMSiblingIncomplete extends AxiomTestCase {
    public TestGetNextOMSiblingIncomplete(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestExpand` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestExpand.java`
#### Snippet
```java

/** Make sure the expanded OMSourcedElement behaves like a normal OMElement. */
public class TestExpand extends AxiomTestCase {
    public TestExpand(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestHasName` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestHasName.java`
#### Snippet
```java
import org.apache.axiom.om.OMSourcedElement;

public class TestHasName extends LazyNameTestCase {
    public TestHasName(OMMetaFactory metaFactory, OMSourcedElementVariant variant, QName qname) {
        super(metaFactory, variant, qname);
```

### JUnitMalformedDeclaration
Test class `TestSerializeModifiedOMSEWithNonDestructiveDataSource` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestSerializeModifiedOMSEWithNonDestructiveDataSource.java`
#### Snippet
```java
import org.apache.axiom.ts.AxiomTestCase;

public class TestSerializeModifiedOMSEWithNonDestructiveDataSource extends AxiomTestCase {
    public TestSerializeModifiedOMSEWithNonDestructiveDataSource(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestName4QualifiedPrefix` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName4QualifiedPrefix.java`
#### Snippet
```java
 * Serialize and cache Tests attempt to rename namespace and localpart, which is not allowed
 */
public class TestName4QualifiedPrefix extends AxiomTestCase {
    public TestName4QualifiedPrefix(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetTextAsStreamWithNonDestructiveOMDataSource` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestGetTextAsStreamWithNonDestructiveOMDataSource.java`
#### Snippet
```java
import org.apache.axiom.ts.AxiomTestCase;

public class TestGetTextAsStreamWithNonDestructiveOMDataSource extends AxiomTestCase {
    public TestGetTextAsStreamWithNonDestructiveOMDataSource(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestSetDataSourceOnAlreadyExpandedElement` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestSetDataSourceOnAlreadyExpandedElement.java`
#### Snippet
```java

@SuppressWarnings("deprecation")
public class TestSetDataSourceOnAlreadyExpandedElement extends AxiomTestCase {
    public TestSetDataSourceOnAlreadyExpandedElement(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestDetach` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestDetach.java`
#### Snippet
```java
import org.apache.axiom.ts.om.sourcedelement.util.PullOMDataSource;

public class TestDetach extends AxiomTestCase {
    private final ExpansionStrategy expansionStrategy;

```

### JUnitMalformedDeclaration
Test class `TestSetDataSource` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestSetDataSource.java`
#### Snippet
```java
/** Verifies that a OMDataSource can be replaced with another one */
@SuppressWarnings("deprecation")
public class TestSetDataSource extends AxiomTestCase {
    public TestSetDataSource(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestCloneNonDestructive` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestCloneNonDestructive.java`
#### Snippet
```java
import org.apache.axiom.ts.AxiomTestCase;

public class TestCloneNonDestructive extends AxiomTestCase {
    private final boolean copyOMDataSources;

```

### JUnitMalformedDeclaration
Test class `TestGetPrefix` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestGetPrefix.java`
#### Snippet
```java
import org.apache.axiom.om.OMSourcedElement;

public class TestGetPrefix extends LazyNameTestCase {
    public TestGetPrefix(OMMetaFactory metaFactory, OMSourcedElementVariant variant, QName qname) {
        super(metaFactory, variant, qname);
```

### JUnitMalformedDeclaration
Test class `TestName1Unqualified` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName1Unqualified.java`
#### Snippet
```java
 * Serialize and cache Prefix test
 */
public class TestName1Unqualified extends AxiomTestCase {
    public TestName1Unqualified(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestName4DefaultPrefix` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName4DefaultPrefix.java`
#### Snippet
```java
 * serializeAndConsume
 */
public class TestName4DefaultPrefix extends AxiomTestCase {
    public TestName4DefaultPrefix(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetAllDeclaredNamespaces` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestGetAllDeclaredNamespaces.java`
#### Snippet
```java
 * OMSourcedElement} instances.
 */
public class TestGetAllDeclaredNamespaces extends AxiomTestCase {
    public TestGetAllDeclaredNamespaces(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetLocalName` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestGetLocalName.java`
#### Snippet
```java
import org.apache.axiom.om.OMSourcedElement;

public class TestGetLocalName extends LazyNameTestCase {
    public TestGetLocalName(
            OMMetaFactory metaFactory, OMSourcedElementVariant variant, QName qname) {
```

### JUnitMalformedDeclaration
Test class `TestName4Unqualified` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName4Unqualified.java`
#### Snippet
```java
 * Serialize and cache Tests attempt to rename namespace and localpart, which is not allowed
 */
public class TestName4Unqualified extends AxiomTestCase {
    public TestName4Unqualified(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestDiscard` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestDiscard.java`
#### Snippet
```java
import org.apache.axiom.ts.om.sourcedelement.util.PullOMDataSource;

public class TestDiscard extends AxiomTestCase {
    private final ExpansionStrategy expansionStrategy;

```

### JUnitMalformedDeclaration
Test class `TestGetAttributeValue` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestGetAttributeValue.java`
#### Snippet
```java
 * OMSourcedElement} instances.
 */
public class TestGetAttributeValue extends AxiomTestCase {
    public TestGetAttributeValue(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestName3Unqualified` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName3Unqualified.java`
#### Snippet
```java
 * Serialize and cache Tests attempt to rename namespace and localpart, which is not allowed
 */
public class TestName3Unqualified extends AxiomTestCase {
    public TestName3Unqualified(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestRemoveChildrenUnexpanded` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestRemoveChildrenUnexpanded.java`
#### Snippet
```java
 * element and to ensure that the information about the name of the element is complete.
 */
public class TestRemoveChildrenUnexpanded extends AxiomTestCase {
    public TestRemoveChildrenUnexpanded(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetNamespaceURI` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestGetNamespaceURI.java`
#### Snippet
```java
import org.apache.axiom.om.OMSourcedElement;

public class TestGetNamespaceURI extends LazyNameTestCase {
    public TestGetNamespaceURI(
            OMMetaFactory metaFactory, OMSourcedElementVariant variant, QName qname) {
```

### JUnitMalformedDeclaration
Test class `TestSerializeOMDataSourceWritingToOutputStream` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestSerializeOMDataSourceWritingToOutputStream.java`
#### Snippet
```java
 * MTOMXMLStreamWriter#getOutputStream()}.
 */
public class TestSerializeOMDataSourceWritingToOutputStream extends AxiomTestCase {
    private static final class OMDataSourceImpl extends AbstractPushOMDataSource {
        private boolean outputStreamUsed;
```

### JUnitMalformedDeclaration
Test class `TestPushOMDataSourceExpansion` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestPushOMDataSourceExpansion.java`
#### Snippet
```java
 * Tests the expansion of an {@link OMSourcedElement} backed by an {@link AbstractPushOMDataSource}.
 */
public class TestPushOMDataSourceExpansion extends AxiomTestCase {
    class PushOMDataSource extends AbstractPushOMDataSource {
        private boolean expanded;
```

### JUnitMalformedDeclaration
Test class `TestName2Unqualified` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName2Unqualified.java`
#### Snippet
```java
 * Serialize and consume Tests update of prefix
 */
public class TestName2Unqualified extends AxiomTestCase {
    public TestName2Unqualified(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestDeclareNamespace` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestDeclareNamespace.java`
#### Snippet
```java
 * namespace declaration that may be produced during expansion.
 */
public class TestDeclareNamespace extends AxiomTestCase {
    public TestDeclareNamespace(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestComplete` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestComplete.java`
#### Snippet
```java

/** Make sure that the incomplete setting of an OMSE is not propogated to the root */
public class TestComplete extends AxiomTestCase {
    public TestComplete(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetAttribute` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestGetAttribute.java`
#### Snippet
```java
 * instances.
 */
public class TestGetAttribute extends AxiomTestCase {
    public TestGetAttribute(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestName1QualifiedPrefix` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName1QualifiedPrefix.java`
#### Snippet
```java
 * Serialize and cache Prefix test
 */
public class TestName1QualifiedPrefix extends AxiomTestCase {
    public TestName1QualifiedPrefix(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetSAXSourceWithPushOMDataSourceThrowingException` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestGetSAXSourceWithPushOMDataSourceThrowingException.java`
#### Snippet
```java
import org.xml.sax.XMLReader;

public class TestGetSAXSourceWithPushOMDataSourceThrowingException extends AxiomTestCase {
    public TestGetSAXSourceWithPushOMDataSourceThrowingException(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestCloneUnknownName` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestCloneUnknownName.java`
#### Snippet
```java
 * the original {@link OMSourcedElement}.
 */
public class TestCloneUnknownName extends AxiomTestCase {
    public TestCloneUnknownName(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetName` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/sr/TestGetName.java`
#### Snippet
```java
 * even if the prefix is not known in advance.
 */
public class TestGetName extends AxiomTestCase {
    public TestGetName(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestCloseWithoutCaching` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/sr/TestCloseWithoutCaching.java`
#### Snippet
```java
 * AbstractPullOMDataSource} closes all readers requested from the data source.
 */
public class TestCloseWithoutCaching extends AxiomTestCase {
    private final int events;

```

### JUnitMalformedDeclaration
Test class `TestGetSAXSourceWithPushOMDataSource` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestGetSAXSourceWithPushOMDataSource.java`
#### Snippet
```java
 * serializes an {@link OMSourcedElement} backed by an {@link AbstractPushOMDataSource}.
 */
public class TestGetSAXSourceWithPushOMDataSource extends AxiomTestCase {
    private final PushOMDataSourceScenario scenario;
    private boolean serializeParent;
```

### JUnitMalformedDeclaration
Test class `TestSerialize` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestSerialize.java`
#### Snippet
```java

/** Tests various ways to serialize an {@link OMSourcedElement}. */
public class TestSerialize extends AxiomTestCase {
    private final boolean push;
    private final boolean destructive;
```

### JUnitMalformedDeclaration
Test class `TestName3DefaultPrefix` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName3DefaultPrefix.java`
#### Snippet
```java
 * Serialize and cache Tests attempt to rename namespace and localpart, which is not allowed
 */
public class TestName3DefaultPrefix extends AxiomTestCase {
    public TestName3DefaultPrefix(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetFaultFakeFault` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/body/TestGetFaultFakeFault.java`
#### Snippet
```java
 * return <code>null</code>.
 */
public class TestGetFaultFakeFault extends SOAPTestCase {
    public TestGetFaultFakeFault(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec);
```

### JUnitMalformedDeclaration
Test class `TestGetFirstElementNSEmptyBody` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/body/TestGetFirstElementNSEmptyBody.java`
#### Snippet
```java
 * Tests that {@link SOAPBody#getFirstElementNS()} returns <code>null</code> if the body is empty.
 */
public class TestGetFirstElementNSEmptyBody extends SOAPTestCase {
    public TestGetFirstElementNSEmptyBody(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec);
```

### JUnitMalformedDeclaration
Test class `TestHasFaultWithOMSEUnknownName` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/body/TestHasFaultWithOMSEUnknownName.java`
#### Snippet
```java
 * OMSourcedElement} with an unknown name that is the first child of the SOAP body.
 */
public class TestHasFaultWithOMSEUnknownName extends SOAPTestCase {
    public TestHasFaultWithOMSEUnknownName(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec);
```

### JUnitMalformedDeclaration
Test class `TestGetFaultWithParserNoFault` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/body/TestGetFaultWithParserNoFault.java`
#### Snippet
```java

/** Tests {@link SOAPBody#getFault()} in the case where the body doesn't contain a SOAP fault. */
public class TestGetFaultWithParserNoFault extends FirstElementNameWithParserTestCase {
    public TestGetFaultWithParserNoFault(OMMetaFactory metaFactory, SOAPSpec spec, QName qname) {
        super(metaFactory, spec, qname, false);
```

### JUnitMalformedDeclaration
Test class `TestGetFirstElementLocalNameWithParser2` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/body/TestGetFirstElementLocalNameWithParser2.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SampleBasedSOAPTestCase;

public class TestGetFirstElementLocalNameWithParser2 extends SampleBasedSOAPTestCase {
    private final boolean buildPayload;

```

### JUnitMalformedDeclaration
Test class `TestHasFaultAfterReplace` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/body/TestHasFaultAfterReplace.java`
#### Snippet
```java
 * updated correctly in all situations. This is a regression test for this issue.
 */
public class TestHasFaultAfterReplace extends SOAPTestCase {
    public TestHasFaultAfterReplace(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec);
```

### JUnitMalformedDeclaration
Test class `TestGetFaultWithParser` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/body/TestGetFaultWithParser.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SampleBasedSOAPTestCase;

public class TestGetFaultWithParser extends SampleBasedSOAPTestCase {
    public TestGetFaultWithParser(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec, SOAPSampleSet.SIMPLE_FAULT);
```

### JUnitMalformedDeclaration
Test class `TestAddFault2` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/body/TestAddFault2.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SOAPTestCase;

public class TestAddFault2 extends SOAPTestCase {
    public TestAddFault2(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec);
```

### JUnitMalformedDeclaration
Test class `TestHasFaultNoFault` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/body/TestHasFaultNoFault.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SOAPTestCase;

public class TestHasFaultNoFault extends SOAPTestCase {
    private final QName qname;

```

### JUnitMalformedDeclaration
Test class `TestHasFault` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/body/TestHasFault.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SOAPTestCase;

public class TestHasFault extends SOAPTestCase {
    public TestHasFault(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec);
```

### JUnitMalformedDeclaration
Test class `TestGetFirstElementNSWithParser` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/body/TestGetFirstElementNSWithParser.java`
#### Snippet
```java
 * href="https://issues.apache.org/jira/browse/AXIOM-282">AXIOM-282</a>.
 */
public class TestGetFirstElementNSWithParser extends FirstElementNameWithParserTestCase {
    public TestGetFirstElementNSWithParser(OMMetaFactory metaFactory, SOAPSpec spec, QName qname) {
        super(metaFactory, spec, qname, true);
```

### JUnitMalformedDeclaration
Test class `TestGetFaultNoFault` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/body/TestGetFaultNoFault.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SOAPTestCase;

public class TestGetFaultNoFault extends SOAPTestCase {
    private final QName qname;

```

### JUnitMalformedDeclaration
Test class `TestGetFault` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/body/TestGetFault.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SOAPTestCase;

public class TestGetFault extends SOAPTestCase {
    public TestGetFault(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec);
```

### JUnitMalformedDeclaration
Test class `TestSerializeWithXSITypeAttribute` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/body/TestSerializeWithXSITypeAttribute.java`
#### Snippet
```java

/** Regression test for <a href="https://issues.apache.org/jira/browse/AXIOM-107">AXIOM-107</a>. */
public class TestSerializeWithXSITypeAttribute extends SampleBasedSOAPTestCase {
    private final SerializationStrategy serializationStrategy;

```

### JUnitMalformedDeclaration
Test class `TestHasFaultWithParserNoFault` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/body/TestHasFaultWithParserNoFault.java`
#### Snippet
```java
 * href="https://issues.apache.org/jira/browse/AXIOM-282">AXIOM-282</a>.
 */
public class TestHasFaultWithParserNoFault extends FirstElementNameWithParserTestCase {
    public TestHasFaultWithParserNoFault(OMMetaFactory metaFactory, SOAPSpec spec, QName qname) {
        super(metaFactory, spec, qname, true);
```

### JUnitMalformedDeclaration
Test class `TestAddFault1` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/body/TestAddFault1.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SOAPTestCase;

public class TestAddFault1 extends SOAPTestCase {
    public TestAddFault1(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec);
```

### JUnitMalformedDeclaration
Test class `TestHasFaultWithParser` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/body/TestHasFaultWithParser.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SampleBasedSOAPTestCase;

public class TestHasFaultWithParser extends SampleBasedSOAPTestCase {
    public TestHasFaultWithParser(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec, SOAPSampleSet.SIMPLE_FAULT);
```

### JUnitMalformedDeclaration
Test class `TestGetFirstElementLocalNameWithParser` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/body/TestGetFirstElementLocalNameWithParser.java`
#### Snippet
```java
 * href="https://issues.apache.org/jira/browse/AXIOM-282">AXIOM-282</a>.
 */
public class TestGetFirstElementLocalNameWithParser extends FirstElementNameWithParserTestCase {
    public TestGetFirstElementLocalNameWithParser(
            OMMetaFactory metaFactory, SOAPSpec spec, QName qname) {
```

### JUnitMalformedDeclaration
Test class `TestGetFirstElementLocalNameEmptyBody` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/body/TestGetFirstElementLocalNameEmptyBody.java`
#### Snippet
```java
 * empty.
 */
public class TestGetFirstElementLocalNameEmptyBody extends SOAPTestCase {
    public TestGetFirstElementLocalNameEmptyBody(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec);
```

### JUnitMalformedDeclaration
Test class `TestHasFaultFakeFault` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/body/TestHasFaultFakeFault.java`
#### Snippet
```java
 * return <code>false</code>.
 */
public class TestHasFaultFakeFault extends SOAPTestCase {
    public TestHasFaultFakeFault(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec);
```

### JUnitMalformedDeclaration
Test class `TestGetFirstElementLocalNameWithParserNoLookahead` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/body/TestGetFirstElementLocalNameWithParserNoLookahead.java`
#### Snippet
```java
 * href="https://issues.apache.org/jira/browse/AXIOM-282">AXIOM-282</a> is no longer applicable.
 */
public class TestGetFirstElementLocalNameWithParserNoLookahead extends SOAPTestCase {
    public TestGetFirstElementLocalNameWithParserNoLookahead(
            OMMetaFactory metaFactory, SOAPSpec spec) {
```

### JUnitMalformedDeclaration
Test class `TestGetChild` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/misc/TestGetChild.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SOAPSpec;

public class TestGetChild extends GetSetChildTestCase {
    public TestGetChild(
            OMMetaFactory metaFactory,
```

### JUnitMalformedDeclaration
Test class `TestCloneOMElement` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/body/TestCloneOMElement.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SampleBasedSOAPTestCase;

public class TestCloneOMElement extends SampleBasedSOAPTestCase {
    public TestCloneOMElement(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec, SOAPSampleSet.WSA);
```

### JUnitMalformedDeclaration
Test class `TestSetChild` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/misc/TestSetChild.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SOAPSpec;

public class TestSetChild extends GetSetChildTestCase {
    public TestSetChild(
            OMMetaFactory metaFactory,
```

### JUnitMalformedDeclaration
Test class `TestSetChildVersionMismatch` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/misc/TestSetChildVersionMismatch.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SOAPSpec;

public class TestSetChildVersionMismatch extends GetSetChildTestCase {
    public TestSetChildVersionMismatch(
            OMMetaFactory metaFactory,
```

### JUnitMalformedDeclaration
Test class `TestGetRoleWithParser` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/fault/TestGetRoleWithParser.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SampleBasedSOAPTestCase;

public class TestGetRoleWithParser extends SampleBasedSOAPTestCase {
    public TestGetRoleWithParser(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec, SOAPSampleSet.CUSTOM_ROLE_FAULT);
```

### JUnitMalformedDeclaration
Test class `TestGetException` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/fault/TestGetException.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SOAPTestCase;

public class TestGetException extends SOAPTestCase {
    public TestGetException(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec);
```

### JUnitMalformedDeclaration
Test class `TestWrongParent1` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/fault/TestWrongParent1.java`
#### Snippet
```java
 * a {@link SOAPFault} to a SOAP element other than {@link SOAPBody}.
 */
public class TestWrongParent1 extends SOAPTestCase {
    public TestWrongParent1(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec);
```

### JUnitMalformedDeclaration
Test class `TestGetDetailWithParser` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/fault/TestGetDetailWithParser.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SampleBasedSOAPTestCase;

public class TestGetDetailWithParser extends SampleBasedSOAPTestCase {
    public TestGetDetailWithParser(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec, SOAPSampleSet.SIMPLE_FAULT);
```

### JUnitMalformedDeclaration
Test class `TestWrongParent2` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/fault/TestWrongParent2.java`
#### Snippet
```java
 * add a {@link SOAPFault} as a child of a SOAP element other than {@link SOAPBody}.
 */
public class TestWrongParent2 extends SOAPTestCase {
    public TestWrongParent2(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec);
```

### JUnitMalformedDeclaration
Test class `TestGetReasonWithParser` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/fault/TestGetReasonWithParser.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SampleBasedSOAPTestCase;

public class TestGetReasonWithParser extends SampleBasedSOAPTestCase {
    public TestGetReasonWithParser(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec, SOAPSampleSet.SIMPLE_FAULT);
```

### JUnitMalformedDeclaration
Test class `TestWrongParent3` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/fault/TestWrongParent3.java`
#### Snippet
```java
 * to add a {@link SOAPFault} as a child of a SOAP element other than {@link SOAPBody}.
 */
public class TestWrongParent3 extends SOAPTestCase {
    public TestWrongParent3(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec);
```

### JUnitMalformedDeclaration
Test class `TestGetCodeWithParser` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/fault/TestGetCodeWithParser.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SampleBasedSOAPTestCase;

public class TestGetCodeWithParser extends SampleBasedSOAPTestCase {
    public TestGetCodeWithParser(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec, SOAPSampleSet.SIMPLE_FAULT);
```

### JUnitMalformedDeclaration
Test class `TestSetException` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/fault/TestSetException.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SOAPTestCase;

public class TestSetException extends SOAPTestCase {
    public TestSetException(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec);
```

### JUnitMalformedDeclaration
Test class `TestDiscardPartiallyBuilt` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/header/TestDiscardPartiallyBuilt.java`
#### Snippet
```java
 * child of the header.
 */
public class TestDiscardPartiallyBuilt extends SampleBasedSOAPTestCase {
    public TestDiscardPartiallyBuilt(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec, SOAPSampleSet.WSA);
```

### JUnitMalformedDeclaration
Test class `TestXPathAppliedToSOAPEnvelope` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/xpath/TestXPathAppliedToSOAPEnvelope.java`
#### Snippet
```java

/** Regression test for <a href="https://issues.apache.org/jira/browse/AXIOM-141">AXIOM-141</a>. */
public class TestXPathAppliedToSOAPEnvelope extends SOAPTestCase {
    private boolean createDocument;

```

### JUnitMalformedDeclaration
Test class `TestChildOrder` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/fault/TestChildOrder.java`
#### Snippet
```java
 * <p>Regression test for <a href="https://issues.apache.org/jira/browse/AXIOM-392">AXIOM-392</a>.
 */
public class TestChildOrder extends SOAPTestCase {
    private final SOAPFaultChild[] inputOrder;
    private final SerializationStrategy serializationStrategy;
```

### JUnitMalformedDeclaration
Test class `TestExtractAllHeaderBlocks` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/header/TestExtractAllHeaderBlocks.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SOAPTestCase;

public class TestExtractAllHeaderBlocks extends SOAPTestCase {
    public TestExtractAllHeaderBlocks(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec);
```

### JUnitMalformedDeclaration
Test class `TestAddHeaderBlockWithoutNamespace2` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/header/TestAddHeaderBlockWithoutNamespace2.java`
#### Snippet
```java
 * {@link OMNamespace} object with an empty namespace URI.
 */
public class TestAddHeaderBlockWithoutNamespace2 extends SOAPTestCase {
    public TestAddHeaderBlockWithoutNamespace2(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec);
```

### JUnitMalformedDeclaration
Test class `TestGetHeadersToProcessWithNamespace` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/header/TestGetHeadersToProcessWithNamespace.java`
#### Snippet
```java

/** Tests the behavior of {@link SOAPHeader#getHeadersToProcess(RolePlayer, String)} */
public class TestGetHeadersToProcessWithNamespace extends SOAPTestCase {
    public TestGetHeadersToProcessWithNamespace(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec);
```

### JUnitMalformedDeclaration
Test class `TestExamineMustUnderstandHeaderBlocksWithParser` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/header/TestExamineMustUnderstandHeaderBlocksWithParser.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SampleBasedSOAPTestCase;

public class TestExamineMustUnderstandHeaderBlocksWithParser extends SampleBasedSOAPTestCase {
    public TestExamineMustUnderstandHeaderBlocksWithParser(
            OMMetaFactory metaFactory, SOAPSpec spec) {
```

### JUnitMalformedDeclaration
Test class `TestGetHeaderBlocksWithNSURIWithParser` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/header/TestGetHeaderBlocksWithNSURIWithParser.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SampleBasedSOAPTestCase;

public class TestGetHeaderBlocksWithNSURIWithParser extends SampleBasedSOAPTestCase {
    public TestGetHeaderBlocksWithNSURIWithParser(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec, SOAPSampleSet.HEADERS);
```

### JUnitMalformedDeclaration
Test class `TestExamineAllHeaderBlocks` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/header/TestExamineAllHeaderBlocks.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SOAPTestCase;

public class TestExamineAllHeaderBlocks extends SOAPTestCase {
    public TestExamineAllHeaderBlocks(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec);
```

### JUnitMalformedDeclaration
Test class `TestAddHeaderBlock` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/header/TestAddHeaderBlock.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SOAPTestCase;

public class TestAddHeaderBlock extends SOAPTestCase {
    public TestAddHeaderBlock(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec);
```

### JUnitMalformedDeclaration
Test class `TestGetHeaderBlocksWithName` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/header/TestGetHeaderBlocksWithName.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SampleBasedSOAPTestCase;

public class TestGetHeaderBlocksWithName extends SampleBasedSOAPTestCase {
    public TestGetHeaderBlocksWithName(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec, SOAPSampleSet.HEADERS);
```

### JUnitMalformedDeclaration
Test class `TestDiscardIncomplete` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/header/TestDiscardIncomplete.java`
#### Snippet
```java

// Regression test for AXIOM-223 (see r567512)
public class TestDiscardIncomplete extends SampleBasedSOAPTestCase {
    public TestDiscardIncomplete(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec, SOAPSampleSet.WSA);
```

### JUnitMalformedDeclaration
Test class `TestExamineHeaderBlocks` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/header/TestExamineHeaderBlocks.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SOAPTestCase;

public class TestExamineHeaderBlocks extends SOAPTestCase {
    public TestExamineHeaderBlocks(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec);
```

### JUnitMalformedDeclaration
Test class `TestAddHeaderBlockFromQName` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/header/TestAddHeaderBlockFromQName.java`
#### Snippet
```java

/** Tests the behavior of {@link SOAPHeader#addHeaderBlock(QName)}. */
public class TestAddHeaderBlockFromQName extends SOAPTestCase {
    public TestAddHeaderBlockFromQName(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec);
```

### JUnitMalformedDeclaration
Test class `TestGetHeadersToProcessWithParser` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/header/TestGetHeadersToProcessWithParser.java`
#### Snippet
```java
import org.apache.axiom.om.OMMetaFactory;

public class TestGetHeadersToProcessWithParser extends SampleBasedSOAPTestCase {
    public TestGetHeadersToProcessWithParser(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec, SOAPSampleSet.HEADERS);
```

### JUnitMalformedDeclaration
Test class `TestAddHeaderBlockWithoutNamespace1` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/header/TestAddHeaderBlockWithoutNamespace1.java`
#### Snippet
```java
 * <code>null</code> value for the {@link OMNamespace} parameter.
 */
public class TestAddHeaderBlockWithoutNamespace1 extends SOAPTestCase {
    public TestAddHeaderBlockWithoutNamespace1(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec);
```

### JUnitMalformedDeclaration
Test class `TestAddChildWithPlainOMElement` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/header/TestAddChildWithPlainOMElement.java`
#### Snippet
```java

/** Regression test for AXIOM-512. */
public class TestAddChildWithPlainOMElement extends SOAPTestCase {
    public TestAddChildWithPlainOMElement(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec);
```

### JUnitMalformedDeclaration
Test class `TestAddHeaderBlockFromQNameWithoutNamespace` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/header/TestAddHeaderBlockFromQNameWithoutNamespace.java`
#### Snippet
```java
 * no namespace.
 */
public class TestAddHeaderBlockFromQNameWithoutNamespace extends SOAPTestCase {
    public TestAddHeaderBlockFromQNameWithoutNamespace(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec);
```

### JUnitMalformedDeclaration
Test class `TestExamineHeaderBlocksWithParser` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/header/TestExamineHeaderBlocksWithParser.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SampleBasedSOAPTestCase;

public class TestExamineHeaderBlocksWithParser extends SampleBasedSOAPTestCase {
    public TestExamineHeaderBlocksWithParser(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec, SOAPSampleSet.HEADERS);
```

### JUnitMalformedDeclaration
Test class `TestGetHeaderBlocksWithNSURI` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/header/TestGetHeaderBlocksWithNSURI.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SOAPTestCase;

public class TestGetHeaderBlocksWithNSURI extends SOAPTestCase {
    public TestGetHeaderBlocksWithNSURI(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec);
```

### JUnitMalformedDeclaration
Test class `BadInputTest` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/builder/BadInputTest.java`
#### Snippet
```java
import org.apache.axiom.ts.AxiomTestCase;

public class BadInputTest extends AxiomTestCase {
    private final String file;

```

### JUnitMalformedDeclaration
Test class `TestExamineAllHeaderBlocksWithParser` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/header/TestExamineAllHeaderBlocksWithParser.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SampleBasedSOAPTestCase;

public class TestExamineAllHeaderBlocksWithParser extends SampleBasedSOAPTestCase {
    public TestExamineAllHeaderBlocksWithParser(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec, SOAPSampleSet.WSA);
```

### JUnitMalformedDeclaration
Test class `MessageTest` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/builder/MessageTest.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SOAPSample;

public class MessageTest extends AxiomTestCase {
    private final SOAPSample message;

```

### JUnitMalformedDeclaration
Test class `TestRegisterCustomBuilderForPayloadAfterSOAPFaultCheck` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/builder/TestRegisterCustomBuilderForPayloadAfterSOAPFaultCheck.java`
#### Snippet
```java
 * href="https://issues.apache.org/jira/browse/AXIOM-282">AXIOM-282</a>.
 */
public class TestRegisterCustomBuilderForPayloadAfterSOAPFaultCheck
        extends SampleBasedSOAPTestCase {
    public TestRegisterCustomBuilderForPayloadAfterSOAPFaultCheck(
```

### JUnitMalformedDeclaration
Test class `TestDTD` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/builder/TestDTD.java`
#### Snippet
```java
 * the methods taking a stream as input will generally reject DTDs at a much lower level.
 */
public class TestDTD extends SOAPTestCase {
    public TestDTD(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec);
```

### JUnitMalformedDeclaration
Test class `TestCommentInProlog` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/builder/TestCommentInProlog.java`
#### Snippet
```java

/** Tests that the SOAP builder creates {@link OMComment} nodes for comments in the XML prolog. */
public class TestCommentInProlog extends SOAPTestCase {
    public TestCommentInProlog(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec);
```

### JUnitMalformedDeclaration
Test class `TestCommentInEpilog` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/builder/TestCommentInEpilog.java`
#### Snippet
```java
 * document element.
 */
public class TestCommentInEpilog extends SOAPTestCase {
    public TestCommentInEpilog(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec);
```

### JUnitMalformedDeclaration
Test class `TestCreateSOAPModelBuilderFromSAXSource` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/builder/TestCreateSOAPModelBuilderFromSAXSource.java`
#### Snippet
```java
import org.xml.sax.XMLReader;

public class TestCreateSOAPModelBuilderFromSAXSource extends SOAPTestCase {
    public TestCreateSOAPModelBuilderFromSAXSource(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec);
```

### JUnitMalformedDeclaration
Test class `TestFactoryIsSingleton` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/factory/TestFactoryIsSingleton.java`
#### Snippet
```java
 * {@link OMMetaFactory#getSOAP12Factory()} return the same instances.
 */
public class TestFactoryIsSingleton extends SOAPTestCase {
    public TestFactoryIsSingleton(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec);
```

### JUnitMalformedDeclaration
Test class `TestCreateSOAPModelBuilderMTOMContentTypeMismatch` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/builder/TestCreateSOAPModelBuilderMTOMContentTypeMismatch.java`
#### Snippet
```java
 * message.
 */
public class TestCreateSOAPModelBuilderMTOMContentTypeMismatch extends SOAPTestCase {
    public TestCreateSOAPModelBuilderMTOMContentTypeMismatch(
            OMMetaFactory metaFactory, SOAPSpec spec) {
```

### JUnitMalformedDeclaration
Test class `TestCreateSOAPModelBuilderFromDOMSource` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/builder/TestCreateSOAPModelBuilderFromDOMSource.java`
#### Snippet
```java
import org.xml.sax.InputSource;

public class TestCreateSOAPModelBuilderFromDOMSource extends SOAPTestCase {
    public TestCreateSOAPModelBuilderFromDOMSource(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec);
```

### JUnitMalformedDeclaration
Test class `TestCreateSOAPElementWithNullParent` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/factory/TestCreateSOAPElementWithNullParent.java`
#### Snippet
```java
 * and {@link SOAPFactory#createSOAPFaultDetail(SOAPFault)} with a null parent.
 */
public class TestCreateSOAPElementWithNullParent extends CreateSOAPElementWithoutParentTestCase {
    private final SOAPElementType parentType;

```

### JUnitMalformedDeclaration
Test class `TestRegisterCustomBuilder` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/builder/TestRegisterCustomBuilder.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SOAPSampleSet;

public class TestRegisterCustomBuilder extends SOAPTestCase {
    public TestRegisterCustomBuilder(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec);
```

### JUnitMalformedDeclaration
Test class `TestGetNamespace` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/factory/TestGetNamespace.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SOAPTestCase;

public class TestGetNamespace extends SOAPTestCase {
    public TestGetNamespace(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec);
```

### JUnitMalformedDeclaration
Test class `TestGetDefaultFaultEnvelope` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/factory/TestGetDefaultFaultEnvelope.java`
#### Snippet
```java

/** Tests the behavior of {@link org.apache.axiom.soap.SOAPFactory#getDefaultFaultEnvelope()}. */
public class TestGetDefaultFaultEnvelope extends SOAPTestCase {
    public TestGetDefaultFaultEnvelope(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec);
```

### JUnitMalformedDeclaration
Test class `TestCreateSOAPElement` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/factory/TestCreateSOAPElement.java`
#### Snippet
```java
 * SOAPFactory#createSOAPFaultRole()} and {@link SOAPFactory#createSOAPFaultDetail()}.
 */
public class TestCreateSOAPElement extends CreateSOAPElementWithoutParentTestCase {
    public TestCreateSOAPElement(OMMetaFactory metaFactory, SOAPSpec spec, SOAPElementType type) {
        super(metaFactory, spec, type);
```

### JUnitMalformedDeclaration
Test class `TestCreateSOAPFaultWithException` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/factory/TestCreateSOAPFaultWithException.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SOAPTestCase;

public class TestCreateSOAPFaultWithException extends SOAPTestCase {
    private final boolean withParent;

```

### JUnitMalformedDeclaration
Test class `TestRegisterCustomBuilderForPayload` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/builder/TestRegisterCustomBuilderForPayload.java`
#### Snippet
```java
import org.xml.sax.InputSource;

public class TestRegisterCustomBuilderForPayload extends AxiomTestCase {
    private final SOAPSample message;

```

### JUnitMalformedDeclaration
Test class `TestGetMetaFactory` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/factory/TestGetMetaFactory.java`
#### Snippet
```java
 * from which the reference to the {@link OMFactory} was obtained.
 */
public class TestGetMetaFactory extends SOAPTestCase {
    public TestGetMetaFactory(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec);
```

### JUnitMalformedDeclaration
Test class `TestCreateSOAPEnvelopeWithCustomPrefix` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/factory/TestCreateSOAPEnvelopeWithCustomPrefix.java`
#### Snippet
```java

/** Tests the behavior of {@link SOAPFactory#createSOAPEnvelope(OMNamespace)}. */
public class TestCreateSOAPEnvelopeWithCustomPrefix extends SOAPTestCase {
    public TestCreateSOAPEnvelopeWithCustomPrefix(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec);
```

### JUnitMalformedDeclaration
Test class `TestGetDefaultEnvelope` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/factory/TestGetDefaultEnvelope.java`
#### Snippet
```java

/** Checks the content of the SOAP envelope returned by {@link SOAPFactory#getDefaultEnvelope()}. */
public class TestGetDefaultEnvelope extends SOAPTestCase {
    public TestGetDefaultEnvelope(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec);
```

### JUnitMalformedDeclaration
Test class `TestCreateSOAPHeaderBlockFromOMElement` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/factory/TestCreateSOAPHeaderBlockFromOMElement.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SOAPTestCase;

public class TestCreateSOAPHeaderBlockFromOMElement extends SOAPTestCase {
    public TestCreateSOAPHeaderBlockFromOMElement(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec);
```

### JUnitMalformedDeclaration
Test class `TestSerialize` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/message/TestSerialize.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SOAPSample;

public class TestSerialize extends AxiomTestCase {
    private final SOAPSample message;
    private final ExpansionStrategy expansionStrategy;
```

### JUnitMalformedDeclaration
Test class `TestCreateDefaultSOAPMessage` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/factory/TestCreateDefaultSOAPMessage.java`
#### Snippet
```java
 * SOAPFactory#createDefaultSOAPMessage()}.
 */
public class TestCreateDefaultSOAPMessage extends SOAPTestCase {
    public TestCreateDefaultSOAPMessage(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec);
```

### JUnitMalformedDeclaration
Test class `TestCreateSOAPElementWithParent` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/factory/TestCreateSOAPElementWithParent.java`
#### Snippet
```java
 * and {@link SOAPFactory#createSOAPFaultDetail(SOAPFault)} with a non null parent.
 */
public class TestCreateSOAPElementWithParent extends SOAPTestCase {
    private final SOAPElementType type;
    private final SOAPElementType parentType;
```

### JUnitMalformedDeclaration
Test class `TestSetOMDocumentElementNonSOAPEnvelope` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/message/TestSetOMDocumentElementNonSOAPEnvelope.java`
#### Snippet
```java
 * SOAPMessage}. In this case, an exception should be thrown.
 */
public class TestSetOMDocumentElementNonSOAPEnvelope extends SOAPTestCase {
    public TestSetOMDocumentElementNonSOAPEnvelope(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec);
```

### JUnitMalformedDeclaration
Test class `TestGetCharsetEncodingWithParser` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/message/TestGetCharsetEncodingWithParser.java`
#### Snippet
```java

/** Tests {@link OMDocument#getCharsetEncoding()} on a {@link SOAPMessage} created by a builder. */
public class TestGetCharsetEncodingWithParser extends SOAPTestCase {
    public TestGetCharsetEncodingWithParser(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec);
```

### JUnitMalformedDeclaration
Test class `TestSetOMDocumentElement` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/message/TestSetOMDocumentElement.java`
#### Snippet
```java
 * {@link SOAPEnvelope} as the root element of a {@link SOAPMessage}.
 */
public class TestSetOMDocumentElement extends SOAPTestCase {
    public TestSetOMDocumentElement(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec);
```

### JUnitMalformedDeclaration
Test class `TestCloneIncomplete` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/message/TestCloneIncomplete.java`
#### Snippet
```java
 * returns the correct factory (which is determined lazily) on the clone.
 */
public class TestCloneIncomplete extends SOAPTestCase {
    private final boolean preserveModel;

```

### JUnitMalformedDeclaration
Test class `TestClone` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/message/TestClone.java`
#### Snippet
```java
 * OMCloneOptions#isPreserveModel()} is <code>true</code>.
 */
public class TestClone extends SOAPTestCase {
    private final boolean preserveModel;

```

### JUnitMalformedDeclaration
Test class `TestGetOMFactoryWithParser` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/message/TestGetOMFactoryWithParser.java`
#### Snippet
```java
 * factory is auto-detected based on the namespace URI of the SOAP envelope.
 */
public class TestGetOMFactoryWithParser extends SOAPTestCase {
    public TestGetOMFactoryWithParser(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec);
```

### JUnitMalformedDeclaration
Test class `TestCloneWithSourcedElement1` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/envelope/TestCloneWithSourcedElement1.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SOAPSpec;

public class TestCloneWithSourcedElement1 extends CloneTestCase {
    public TestCloneWithSourcedElement1(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec);
```

### JUnitMalformedDeclaration
Test class `TestBodyHeaderOrder` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/envelope/TestBodyHeaderOrder.java`
#### Snippet
```java

// Make sure order of header/body creation doesn't matter
public class TestBodyHeaderOrder extends SOAPTestCase {
    public TestBodyHeaderOrder(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec);
```

### JUnitMalformedDeclaration
Test class `TestGetXMLStreamReaderWithoutCachingWithPartiallyBuiltHeaderBlock` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/envelope/TestGetXMLStreamReaderWithoutCachingWithPartiallyBuiltHeaderBlock.java`
#### Snippet
```java
 * means that consuming the reader should not build the {@link SOAPHeaderBlock}.
 */
public class TestGetXMLStreamReaderWithoutCachingWithPartiallyBuiltHeaderBlock
        extends SampleBasedSOAPTestCase {
    public TestGetXMLStreamReaderWithoutCachingWithPartiallyBuiltHeaderBlock(
```

### JUnitMalformedDeclaration
Test class `TestClone` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/envelope/TestClone.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SOAPSampleAdapter;

public class TestClone extends CloneTestCase {
    private final SOAPSample message;

```

### JUnitMalformedDeclaration
Test class `TestGetOrCreateHeaderWithParserNoHeader` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/envelope/TestGetOrCreateHeaderWithParserNoHeader.java`
#### Snippet
```java
 * SOAPBody}.
 */
public class TestGetOrCreateHeaderWithParserNoHeader extends SampleBasedSOAPTestCase {
    public TestGetOrCreateHeaderWithParserNoHeader(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec, SOAPSampleSet.NO_HEADER);
```

### JUnitMalformedDeclaration
Test class `TestSerializeAsChild` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/envelope/TestSerializeAsChild.java`
#### Snippet
```java

/** Regression test for <a href="https://issues.apache.org/jira/browse/AXIOM-474">AXIOM-474</a>. */
public class TestSerializeAsChild extends SOAPTestCase {
    public TestSerializeAsChild(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec);
```

### JUnitMalformedDeclaration
Test class `TestGetHeaderWithParser` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/envelope/TestGetHeaderWithParser.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SampleBasedSOAPTestCase;

public class TestGetHeaderWithParser extends SampleBasedSOAPTestCase {
    public TestGetHeaderWithParser(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec, SOAPSampleSet.WSA);
```

### JUnitMalformedDeclaration
Test class `TestGetBody` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/envelope/TestGetBody.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SOAPTestCase;

public class TestGetBody extends SOAPTestCase {
    public TestGetBody(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec);
```

### JUnitMalformedDeclaration
Test class `TestGetOrCreateHeader` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/envelope/TestGetOrCreateHeader.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SOAPTestCase;

public class TestGetOrCreateHeader extends SOAPTestCase {
    public TestGetOrCreateHeader(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec);
```

### JUnitMalformedDeclaration
Test class `TestAddHeaderToIncompleteEnvelope` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/envelope/TestAddHeaderToIncompleteEnvelope.java`
#### Snippet
```java
 * This is a regression test for AXIOM-127.
 */
public class TestAddHeaderToIncompleteEnvelope extends SampleBasedSOAPTestCase {
    public TestAddHeaderToIncompleteEnvelope(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec, SOAPSampleSet.NO_HEADER);
```

### JUnitMalformedDeclaration
Test class `TestAddElementAfterBody` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/envelope/TestAddElementAfterBody.java`
#### Snippet
```java
 * allowed in SOAP 1.1, but not in SOAP 1.2.
 */
public class TestAddElementAfterBody extends SOAPTestCase {
    private final boolean header;

```

### JUnitMalformedDeclaration
Test class `TestCloneWithSourcedElement2` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/envelope/TestCloneWithSourcedElement2.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SOAPSpec;

public class TestCloneWithSourcedElement2 extends CloneTestCase {
    public TestCloneWithSourcedElement2(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec);
```

### JUnitMalformedDeclaration
Test class `TestGetHeaderWithParserNoHeader` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/envelope/TestGetHeaderWithParserNoHeader.java`
#### Snippet
```java
 * particular it tests that the call doesn't build the {@link SOAPBody}.
 */
public class TestGetHeaderWithParserNoHeader extends SampleBasedSOAPTestCase {
    public TestGetHeaderWithParserNoHeader(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec, SOAPSampleSet.NO_HEADER);
```

### JUnitMalformedDeclaration
Test class `TestDetach` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/envelope/TestDetach.java`
#### Snippet
```java
 * envelope from its containing document.
 */
public class TestDetach extends SampleBasedSOAPTestCase {
    public TestDetach(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec, SOAPSampleSet.NO_HEADER);
```

### JUnitMalformedDeclaration
Test class `TestGetHeader` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/envelope/TestGetHeader.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SOAPTestCase;

public class TestGetHeader extends SOAPTestCase {
    public TestGetHeader(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec);
```

### JUnitMalformedDeclaration
Test class `TestGetBodyWithParser` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/envelope/TestGetBodyWithParser.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SampleBasedSOAPTestCase;

public class TestGetBodyWithParser extends SampleBasedSOAPTestCase {
    public TestGetBodyWithParser(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec, SOAPSampleSet.NO_HEADER);
```

### JUnitMalformedDeclaration
Test class `TestHasFault` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/envelope/TestHasFault.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SOAPTestCase;

public class TestHasFault extends SOAPTestCase {
    public TestHasFault(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec);
```

### JUnitMalformedDeclaration
Test class `TestGetBodyOnEmptyEnvelope` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/envelope/TestGetBodyOnEmptyEnvelope.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SOAPTestCase;

public class TestGetBodyOnEmptyEnvelope extends SOAPTestCase {
    public TestGetBodyOnEmptyEnvelope(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec);
```

### JUnitMalformedDeclaration
Test class `TestGetSOAPBodyFirstElementLocalNameAndNS` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/envelope/TestGetSOAPBodyFirstElementLocalNameAndNS.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SOAPTestCase;

public class TestGetSOAPBodyFirstElementLocalNameAndNS extends SOAPTestCase {
    private final QName qname;

```

### JUnitMalformedDeclaration
Test class `TestHasFaultWithParser` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/envelope/TestHasFaultWithParser.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SampleBasedSOAPTestCase;

public class TestHasFaultWithParser extends SampleBasedSOAPTestCase {
    public TestHasFaultWithParser(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec, SOAPSampleSet.SIMPLE_FAULT);
```

### JUnitMalformedDeclaration
Test class `TestGetValueAsQNameWithParser` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/faultcode/TestGetValueAsQNameWithParser.java`
#### Snippet
```java
 * Tests the behavior of {@link SOAPFaultClassifier#getValueAsQName()} on a {@link SOAPFaultCode}.
 */
public class TestGetValueAsQNameWithParser extends SampleBasedSOAPTestCase {
    public TestGetValueAsQNameWithParser(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec, SOAPSampleSet.SIMPLE_FAULT);
```

### JUnitMalformedDeclaration
Test class `TestGetValueAsQName` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/faultcode/TestGetValueAsQName.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SOAPTestCase;

public class TestGetValueAsQName extends SOAPTestCase {
    public TestGetValueAsQName(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec);
```

### JUnitMalformedDeclaration
Test class `TestSetFaultNodeValue` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/faultnode/TestSetFaultNodeValue.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SOAPTestCase;

public class TestSetFaultNodeValue extends SOAPTestCase {
    public TestSetFaultNodeValue(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec);
```

### JUnitMalformedDeclaration
Test class `TestGetBodyOnEnvelopeWithHeaderOnly` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/envelope/TestGetBodyOnEnvelopeWithHeaderOnly.java`
#### Snippet
```java

// Regression test for AXIOM-218
public class TestGetBodyOnEnvelopeWithHeaderOnly extends SOAPTestCase {
    public TestGetBodyOnEnvelopeWithHeaderOnly(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec);
```

### JUnitMalformedDeclaration
Test class `TestSerialize` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/envelope/TestSerialize.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SOAPSample;

public class TestSerialize extends AxiomTestCase {
    private final SOAPSample message;
    private final ExpansionStrategy expansionStrategy;
```

### JUnitMalformedDeclaration
Test class `TestSerializeAndConsumeWithOMSEInBody` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/envelope/TestSerializeAndConsumeWithOMSEInBody.java`
#### Snippet
```java

/** Regression test for WSCOMMONS-226. */
public class TestSerializeAndConsumeWithOMSEInBody extends SOAPTestCase {
    public TestSerializeAndConsumeWithOMSEInBody(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec);
```

### JUnitMalformedDeclaration
Test class `TestGetFaultNodeValueWithParser` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/faultnode/TestGetFaultNodeValueWithParser.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SampleBasedSOAPTestCase;

public class TestGetFaultNodeValueWithParser extends SampleBasedSOAPTestCase {
    public TestGetFaultNodeValueWithParser(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec, SOAPSampleSet.CUSTOM_ROLE_FAULT);
```

### JUnitMalformedDeclaration
Test class `TestGetFaultNodeValue` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/faultnode/TestGetFaultNodeValue.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SOAPTestCase;

public class TestGetFaultNodeValue extends SOAPTestCase {
    public TestGetFaultNodeValue(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec);
```

### JUnitMalformedDeclaration
Test class `TestGetLangFromParser` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/faulttext/TestGetLangFromParser.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SampleBasedSOAPTestCase;

public class TestGetLangFromParser extends SampleBasedSOAPTestCase {
    public TestGetLangFromParser(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec, SOAPSampleSet.SIMPLE_FAULT);
```

### JUnitMalformedDeclaration
Test class `TestGetRoleValueWithParser` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/faultrole/TestGetRoleValueWithParser.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SampleBasedSOAPTestCase;

public class TestGetRoleValueWithParser extends SampleBasedSOAPTestCase {
    public TestGetRoleValueWithParser(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec, SOAPSampleSet.CUSTOM_ROLE_FAULT);
```

### JUnitMalformedDeclaration
Test class `TestSetRoleValue` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/faultrole/TestSetRoleValue.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SOAPTestCase;

public class TestSetRoleValue extends SOAPTestCase {
    public TestSetRoleValue(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec);
```

### JUnitMalformedDeclaration
Test class `TestGetRoleValue` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/faultrole/TestGetRoleValue.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SOAPTestCase;

public class TestGetRoleValue extends SOAPTestCase {
    public TestGetRoleValue(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec);
```

### JUnitMalformedDeclaration
Test class `TestGetSOAPBodyFirstElementLocalNameAndNSWithParser` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/envelope/TestGetSOAPBodyFirstElementLocalNameAndNSWithParser.java`
#### Snippet
```java
 * of the element without actually instantiating the corresponding {@link OMElement}.
 */
public class TestGetSOAPBodyFirstElementLocalNameAndNSWithParser extends SOAPTestCase {
    private final QName qname;

```

### JUnitMalformedDeclaration
Test class `TestSetLang` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/faulttext/TestSetLang.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SOAPTestCase;

public class TestSetLang extends SOAPTestCase {
    public TestSetLang(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec);
```

### JUnitMalformedDeclaration
Test class `TestGetLang` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/faulttext/TestGetLang.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SOAPTestCase;

public class TestGetLang extends SOAPTestCase {
    public TestGetLang(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec);
```

### JUnitMalformedDeclaration
Test class `TestWSCommons202` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/faultdetail/TestWSCommons202.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SOAPTestCase;

public class TestWSCommons202 extends SOAPTestCase {
    public TestWSCommons202(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec);
```

### JUnitMalformedDeclaration
Test class `TestDetailEntriesUsingDefaultNamespaceWithParser` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/faultdetail/TestDetailEntriesUsingDefaultNamespaceWithParser.java`
#### Snippet
```java

/** Tests parsing of a SOAP fault that has detail entries that use a default namespace. */
public class TestDetailEntriesUsingDefaultNamespaceWithParser extends SampleBasedSOAPTestCase {
    public TestDetailEntriesUsingDefaultNamespaceWithParser(
            OMMetaFactory metaFactory, SOAPSpec spec) {
```

### JUnitMalformedDeclaration
Test class `TestSerialization` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/faultdetail/TestSerialization.java`
#### Snippet
```java

// Regression test for AXIOM-196
public class TestSerialization extends SOAPTestCase {
    public TestSerialization(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec);
```

### JUnitMalformedDeclaration
Test class `TestGetAllDetailEntries` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/faultdetail/TestGetAllDetailEntries.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SOAPTestCase;

public class TestGetAllDetailEntries extends SOAPTestCase {
    public TestGetAllDetailEntries(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec);
```

### JUnitMalformedDeclaration
Test class `TestSetRole` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/headerblock/TestSetRole.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SOAPTestCase;

public class TestSetRole extends SOAPTestCase {
    public TestSetRole(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec);
```

### JUnitMalformedDeclaration
Test class `TestGetFaultReasonText` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/faultreason/TestGetFaultReasonText.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SampleBasedSOAPTestCase;

public class TestGetFaultReasonText extends SampleBasedSOAPTestCase {
    public TestGetFaultReasonText(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec, SOAPSampleSet.SIMPLE_FAULT);
```

### JUnitMalformedDeclaration
Test class `TestGetRoleWithParser` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/headerblock/TestGetRoleWithParser.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SampleBasedSOAPTestCase;

public class TestGetRoleWithParser extends SampleBasedSOAPTestCase {
    public TestGetRoleWithParser(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec, SOAPSampleSet.HEADERS);
```

### JUnitMalformedDeclaration
Test class `TestGetRole` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/headerblock/TestGetRole.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SOAPTestCase;

public class TestGetRole extends SOAPTestCase {
    public TestGetRole(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec);
```

### JUnitMalformedDeclaration
Test class `TestGetMustUnderstandWithParser` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/headerblock/TestGetMustUnderstandWithParser.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SampleBasedSOAPTestCase;

public class TestGetMustUnderstandWithParser extends SampleBasedSOAPTestCase {
    public TestGetMustUnderstandWithParser(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec, SOAPSampleSet.HEADERS);
```

### JUnitMalformedDeclaration
Test class `TestGetAllDetailEntriesWithParser` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/faultdetail/TestGetAllDetailEntriesWithParser.java`
#### Snippet
```java

// SOAP Fault Detail Test (With Parser)
public class TestGetAllDetailEntriesWithParser extends SampleBasedSOAPTestCase {
    public TestGetAllDetailEntriesWithParser(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec, SOAPSampleSet.SIMPLE_FAULT);
```

### JUnitMalformedDeclaration
Test class `TestAddDetailEntry` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/faultdetail/TestAddDetailEntry.java`
#### Snippet
```java

// SOAP Fault Detail Test (Programaticaly Created)
public class TestAddDetailEntry extends SOAPTestCase {
    public TestAddDetailEntry(OMMetaFactory omMetaFactory, SOAPSpec spec) {
        super(omMetaFactory, spec);
```

### JUnitMalformedDeclaration
Test class `TestWrongParent2` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/headerblock/TestWrongParent2.java`
#### Snippet
```java
 * add a {@link SOAPHeaderBlock} as a child of a SOAP element other than {@link SOAPHeader}.
 */
public class TestWrongParent2 extends SOAPTestCase {
    public TestWrongParent2(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec);
```

### JUnitMalformedDeclaration
Test class `TestWrongParent1` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/headerblock/TestWrongParent1.java`
#### Snippet
```java
 * a {@link SOAPHeaderBlock} to a SOAP element other than {@link SOAPHeader}.
 */
public class TestWrongParent1 extends SOAPTestCase {
    public TestWrongParent1(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec);
```

### JUnitMalformedDeclaration
Test class `TestCloneProcessedWithoutPreservingModel` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/headerblock/TestCloneProcessedWithoutPreservingModel.java`
#### Snippet
```java
 * <p>This is a regression test for an issue in older Axiom versions.
 */
public class TestCloneProcessedWithoutPreservingModel extends SOAPTestCase {
    public TestCloneProcessedWithoutPreservingModel(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec);
```

### JUnitMalformedDeclaration
Test class `TestClone` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/headerblock/TestClone.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SOAPTestCase;

public class TestClone extends SOAPTestCase {
    private final Boolean processed;

```

### JUnitMalformedDeclaration
Test class `TestGetVersion` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/headerblock/TestGetVersion.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SOAPTestCase;

public class TestGetVersion extends SOAPTestCase {
    public TestGetVersion(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec);
```

### JUnitMalformedDeclaration
Test class `TestSetRoleWithoutExistingNamespaceDecl` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/headerblock/TestSetRoleWithoutExistingNamespaceDecl.java`
#### Snippet
```java

/** Tests that {@link SOAPHeaderBlock#setRole(String)} adds a namespace declaration if necessary. */
public class TestSetRoleWithoutExistingNamespaceDecl extends SOAPTestCase {
    public TestSetRoleWithoutExistingNamespaceDecl(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec);
```

### JUnitMalformedDeclaration
Test class `TestSetBooleanAttribute` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/headerblock/TestSetBooleanAttribute.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SOAPSpec;

public class TestSetBooleanAttribute extends BooleanAttributeTestCase {
    private final boolean value;

```

### JUnitMalformedDeclaration
Test class `TestBlobOMDataSource` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/headerblock/TestBlobOMDataSource.java`
#### Snippet
```java

/** Tests functionality of BlobOMDataSource */
public class TestBlobOMDataSource extends SOAPTestCase {
    public TestBlobOMDataSource(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec);
```

### JUnitMalformedDeclaration
Test class `TestGetBooleanAttribute` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/headerblock/TestGetBooleanAttribute.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SOAPSpec;

public class TestGetBooleanAttribute extends BooleanAttributeTestCase {
    private final BooleanLiteral literal;

```

### JUnitMalformedDeclaration
Test class `TestGetBooleanAttributeInvalid` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/headerblock/TestGetBooleanAttributeInvalid.java`
#### Snippet
```java
 * attribute is present but has an invalid value.
 */
public class TestGetBooleanAttributeInvalid extends BooleanAttributeTestCase {
    private final String value;

```

### JUnitMalformedDeclaration
Test class `TestGetBooleanAttributeUnspported` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/headerblock/TestGetBooleanAttributeUnspported.java`
#### Snippet
```java
 * SOAP 1.1 messages.
 */
public class TestGetBooleanAttributeUnspported extends BooleanAttributeTestCase {
    public TestGetBooleanAttributeUnspported(
            OMMetaFactory metaFactory, SOAPSpec spec, HeaderBlockAttribute attribute) {
```

### JUnitMalformedDeclaration
Test class `TestGetBooleanAttributeDefault` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/headerblock/TestGetBooleanAttributeDefault.java`
#### Snippet
```java
 * absent.
 */
public class TestGetBooleanAttributeDefault extends BooleanAttributeTestCase {
    public TestGetBooleanAttributeDefault(
            OMMetaFactory metaFactory, SOAPSpec spec, HeaderBlockAttribute attribute) {
```

### JUnitMalformedDeclaration
Test class `TestWrongParent3` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/headerblock/TestWrongParent3.java`
#### Snippet
```java
 * to add a {@link SOAPHeaderBlock} as a child of a SOAP element other than {@link SOAPHeader}.
 */
public class TestWrongParent3 extends SOAPTestCase {
    public TestWrongParent3(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec);
```

### JUnitMalformedDeclaration
Test class `TestSetBooleanAttributeUnsupported` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/headerblock/TestSetBooleanAttributeUnsupported.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SOAPSpec;

public class TestSetBooleanAttributeUnsupported extends BooleanAttributeTestCase {
    public TestSetBooleanAttributeUnsupported(
            OMMetaFactory metaFactory, SOAPSpec spec, HeaderBlockAttribute attribute) {
```

### JUnitMalformedDeclaration
Test class `TestGetNamespaceNormalized` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/omdom/attr/TestGetNamespaceNormalized.java`
#### Snippet
```java
 * normalized for attributes created using {@link Document#createAttributeNS(String, String)}.
 */
public class TestGetNamespaceNormalized extends AxiomTestCase {
    public TestGetNamespaceNormalized(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestCloneNodeBinary` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/omdom/text/TestCloneNodeBinary.java`
#### Snippet
```java
import org.w3c.dom.Text;

public class TestCloneNodeBinary extends AxiomTestCase {
    public TestCloneNodeBinary(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestSetValueOnNamespaceDeclaration` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/omdom/attr/TestSetValueOnNamespaceDeclaration.java`
#### Snippet
```java
 * namespace URI is reflected in the result of {@link OMElement#getAllDeclaredNamespaces()}.
 */
public class TestSetValueOnNamespaceDeclaration extends AxiomTestCase {
    public TestSetValueOnNamespaceDeclaration(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetNamespaceNormalized` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/omdom/element/TestGetNamespaceNormalized.java`
#### Snippet
```java
 * normalized for elements created using {@link Document#createElementNS(String, String)}.
 */
public class TestGetNamespaceNormalized extends AxiomTestCase {
    public TestGetNamespaceNormalized(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetNodeValueBinary` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/omdom/text/TestGetNodeValueBinary.java`
#### Snippet
```java
import org.w3c.dom.Text;

public class TestGetNodeValueBinary extends AxiomTestCase {
    public TestGetNodeValueBinary(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestInsertSiblingAfterFromForeignDocument` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/omdom/node/TestInsertSiblingAfterFromForeignDocument.java`
#### Snippet
```java
 * the sibling if it doesn't have the same owner document.
 */
public class TestInsertSiblingAfterFromForeignDocument extends AxiomTestCase {
    public TestInsertSiblingAfterFromForeignDocument(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestInsertSiblingBeforeFromForeignDocument` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/omdom/node/TestInsertSiblingBeforeFromForeignDocument.java`
#### Snippet
```java
 * the sibling if it doesn't have the same owner document.
 */
public class TestInsertSiblingBeforeFromForeignDocument extends AxiomTestCase {
    public TestInsertSiblingBeforeFromForeignDocument(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestAddChildFromForeignDocument` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/omdom/element/TestAddChildFromForeignDocument.java`
#### Snippet
```java
 * child node if it doesn't have the same owner document.
 */
public class TestAddChildFromForeignDocument extends AxiomTestCase {
    public TestAddChildFromForeignDocument(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestDetach` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/omdom/element/TestDetach.java`
#### Snippet
```java
 * have a new owner document.
 */
public class TestDetach extends AxiomTestCase {
    public TestDetach(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestAppendChildIncomplete` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/omdom/element/TestAppendChildIncomplete.java`
#### Snippet
```java
 * @see TestInsertBeforeIncomplete
 */
public class TestAppendChildIncomplete extends AxiomTestCase {
    public TestAppendChildIncomplete(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestRemoveAttribute` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/omdom/element/TestRemoveAttribute.java`
#### Snippet
```java
 * OMElement#removeAttribute(OMAttribute)}.
 */
public class TestRemoveAttribute extends AxiomTestCase {
    public TestRemoveAttribute(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestRemoveChildIncomplete` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/omdom/element/TestRemoveChildIncomplete.java`
#### Snippet
```java
 * completely.
 */
public class TestRemoveChildIncomplete extends AxiomTestCase {
    public TestRemoveChildIncomplete(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestCloneOMElement` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/omdom/element/TestCloneOMElement.java`
#### Snippet
```java
 * document than the original element.
 */
public class TestCloneOMElement extends AxiomTestCase {
    public TestCloneOMElement(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestAddAttributeReplace` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/omdom/element/TestAddAttributeReplace.java`
#### Snippet
```java
 * a side effect of {@link OMElement#addAttribute(OMAttribute)}.
 */
public class TestAddAttributeReplace extends AxiomTestCase {
    public TestAddAttributeReplace(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestReplaceChildFirstIncomplete` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/omdom/element/TestReplaceChildFirstIncomplete.java`
#### Snippet
```java
 * completely. This test covers the case where the child being replaced is the first child.
 */
public class TestReplaceChildFirstIncomplete extends AxiomTestCase {
    public TestReplaceChildFirstIncomplete(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestRemoveAttributeNode` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/omdom/element/TestRemoveAttributeNode.java`
#### Snippet
```java
 * using the Axiom API, i.e. where the owner document is created lazily.
 */
public class TestRemoveAttributeNode extends AxiomTestCase {
    public TestRemoveAttributeNode(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestRemoveAttributeNSNamespaceDeclaration` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/omdom/element/TestRemoveAttributeNSNamespaceDeclaration.java`
#### Snippet
```java
 * declaration.
 */
public class TestRemoveAttributeNSNamespaceDeclaration extends AxiomTestCase {
    public TestRemoveAttributeNSNamespaceDeclaration(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestInsertBeforeIncomplete` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/omdom/element/TestInsertBeforeIncomplete.java`
#### Snippet
```java
 * @see TestAppendChildIncomplete
 */
public class TestInsertBeforeIncomplete extends AxiomTestCase {
    public TestInsertBeforeIncomplete(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestCloneNodeIncomplete` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/omdom/element/TestCloneNodeIncomplete.java`
#### Snippet
```java

/** Tests {@link Node#cloneNode(boolean)} on an element that is not completely built. */
public class TestCloneNodeIncomplete extends AxiomTestCase {
    public TestCloneNodeIncomplete(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestCreateOMAttribute` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/omdom/factory/TestCreateOMAttribute.java`
#### Snippet
```java
import org.w3c.dom.Attr;

public class TestCreateOMAttribute extends AxiomTestCase {
    public TestCreateOMAttribute(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestReplaceChildMiddleIncomplete` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/omdom/element/TestReplaceChildMiddleIncomplete.java`
#### Snippet
```java
 * href="https://issues.apache.org/jira/browse/AXIOM-432">AXIOM-432</a>.
 */
public class TestReplaceChildMiddleIncomplete extends AxiomTestCase {
    public TestReplaceChildMiddleIncomplete(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestCreateOMTextCDATASection` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/omdom/factory/TestCreateOMTextCDATASection.java`
#### Snippet
```java
 * CDATASection} if the specified type is {@link OMNode#CDATA_SECTION_NODE}.
 */
public class TestCreateOMTextCDATASection extends AxiomTestCase {
    public TestCreateOMTextCDATASection(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetOMFactory1` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/omdom/document/TestGetOMFactory1.java`
#### Snippet
```java
 * Document} created with the {@link DocumentBuilder#newDocument()}.
 */
public class TestGetOMFactory1 extends AxiomTestCase {
    public TestGetOMFactory1(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestCreateOMTextCDATASectionWithParent` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/omdom/factory/TestCreateOMTextCDATASectionWithParent.java`
#### Snippet
```java
 * implements {@link CDATASection} if the specified type is {@link OMNode#CDATA_SECTION_NODE}.
 */
public class TestCreateOMTextCDATASectionWithParent extends AxiomTestCase {
    public TestCreateOMTextCDATASectionWithParent(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestImportNode` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/omdom/document/TestImportNode.java`
#### Snippet
```java
import org.xml.sax.InputSource;

public class TestImportNode extends ConformanceTestCase {
    public TestImportNode(OMMetaFactory metaFactory, XMLSample file) {
        super(metaFactory, file);
```

### JUnitMalformedDeclaration
Test class `TestGetOMFactory2` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/omdom/document/TestGetOMFactory2.java`
#### Snippet
```java
 * DocumentType)}.
 */
public class TestGetOMFactory2 extends AxiomTestCase {
    public TestGetOMFactory2(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestSetNode` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap11/fault/TestSetNode.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SOAPTestCase;

public class TestSetNode extends SOAPTestCase {
    public TestSetNode(OMMetaFactory metaFactory) {
        super(metaFactory, SOAPSpec.SOAP11);
```

### JUnitMalformedDeclaration
Test class `TestCreateDocumentFragmentInterfaces` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/omdom/document/TestCreateDocumentFragmentInterfaces.java`
#### Snippet
```java
 * Document#createDocumentFragment()} don't implement any Axiom interfaces.
 */
public class TestCreateDocumentFragmentInterfaces extends AxiomTestCase {
    public TestCreateDocumentFragmentInterfaces(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestAppendChildForbidden` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/omdom/document/TestAppendChildForbidden.java`
#### Snippet
```java
import org.w3c.dom.Document;

public class TestAppendChildForbidden extends AxiomTestCase {
    private final boolean build;

```

### JUnitMalformedDeclaration
Test class `TestGetNode` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap11/fault/TestGetNode.java`
#### Snippet
```java
 * UnsupportedOperationException}) for SOAP 1.1 faults.
 */
public class TestGetNode extends SOAPTestCase {
    public TestGetNode(OMMetaFactory metaFactory) {
        super(metaFactory, SOAPSpec.SOAP11);
```

### JUnitMalformedDeclaration
Test class `TestInsertBeforeForbidden` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/omdom/document/TestInsertBeforeForbidden.java`
#### Snippet
```java
import org.w3c.dom.Document;

public class TestInsertBeforeForbidden extends AxiomTestCase {
    private final boolean build;

```

### JUnitMalformedDeclaration
Test class `TestSetValueFromQName` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap11/faultcode/TestSetValueFromQName.java`
#### Snippet
```java
 * {@link SOAPFaultCode}.
 */
public class TestSetValueFromQName extends SOAPTestCase {
    public TestSetValueFromQName(OMMetaFactory metaFactory) {
        super(metaFactory, SOAPSpec.SOAP11);
```

### JUnitMalformedDeclaration
Test class `TestGetValueWithParser` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap11/faultcode/TestGetValueWithParser.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SampleBasedSOAPTestCase;

public class TestGetValueWithParser extends SampleBasedSOAPTestCase {
    public TestGetValueWithParser(OMMetaFactory metaFactory) {
        super(metaFactory, SOAPSpec.SOAP11, SOAPSampleSet.SIMPLE_FAULT);
```

### JUnitMalformedDeclaration
Test class `TestGetFirstSOAPText` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap11/faultreason/TestGetFirstSOAPText.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SOAPTestCase;

public class TestGetFirstSOAPText extends SOAPTestCase {
    public TestGetFirstSOAPText(OMMetaFactory metaFactory) {
        super(metaFactory, SOAPSpec.SOAP11);
```

### JUnitMalformedDeclaration
Test class `TestAddSOAPText` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap11/faultreason/TestAddSOAPText.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SOAPTestCase;

public class TestAddSOAPText extends SOAPTestCase {
    public TestAddSOAPText(OMMetaFactory metaFactory) {
        super(metaFactory, SOAPSpec.SOAP11);
```

### JUnitMalformedDeclaration
Test class `TestGetValue` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap11/faultcode/TestGetValue.java`
#### Snippet
```java
 * SOAP 1.1 {@link SOAPFaultCode}.
 */
public class TestGetValue extends SOAPTestCase {
    public TestGetValue(OMMetaFactory metaFactory) {
        super(metaFactory, SOAPSpec.SOAP11);
```

### JUnitMalformedDeclaration
Test class `TestGetNodeWithParser` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/fault/TestGetNodeWithParser.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SampleBasedSOAPTestCase;

public class TestGetNodeWithParser extends SampleBasedSOAPTestCase {
    public TestGetNodeWithParser(OMMetaFactory metaFactory) {
        super(metaFactory, SOAPSample.SOAP12_FAULT);
```

### JUnitMalformedDeclaration
Test class `TestGetNode` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/fault/TestGetNode.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SOAPTestCase;

public class TestGetNode extends SOAPTestCase {
    public TestGetNode(OMMetaFactory metaFactory) {
        super(metaFactory, SOAPSpec.SOAP12);
```

### JUnitMalformedDeclaration
Test class `TestSetNode` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/fault/TestSetNode.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SOAPTestCase;

public class TestSetNode extends SOAPTestCase {
    public TestSetNode(OMMetaFactory metaFactory) {
        super(metaFactory, SOAPSpec.SOAP12);
```

### JUnitMalformedDeclaration
Test class `TestGetTextWithCDATA` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap11/faultreason/TestGetTextWithCDATA.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SOAPTestCase;

public class TestGetTextWithCDATA extends SOAPTestCase {
    public TestGetTextWithCDATA(OMMetaFactory metaFactory) {
        super(metaFactory, SOAPSpec.SOAP11);
```

### JUnitMalformedDeclaration
Test class `TestBuilderDetach` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/mtom/TestBuilderDetach.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.MTOMSample;

public class TestBuilderDetach extends AxiomTestCase {
    public TestBuilderDetach(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestExamineMustUnderstandHeaderBlocks` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/header/TestExamineMustUnderstandHeaderBlocks.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SOAPTestCase;

public class TestExamineMustUnderstandHeaderBlocks extends SOAPTestCase {
    public TestExamineMustUnderstandHeaderBlocks(OMMetaFactory metaFactory) {
        super(metaFactory, SOAPSpec.SOAP12);
```

### JUnitMalformedDeclaration
Test class `TestMoreChildrenAddition` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/fault/TestMoreChildrenAddition.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SOAPTestCase;

public class TestMoreChildrenAddition extends SOAPTestCase {
    public TestMoreChildrenAddition(OMMetaFactory metaFactory) {
        super(metaFactory, SOAPSpec.SOAP12);
```

### JUnitMalformedDeclaration
Test class `TestSetValueFromQName` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/faultcode/TestSetValueFromQName.java`
#### Snippet
```java
 * 1.2 {@link SOAPFaultCode}.
 */
public class TestSetValueFromQName extends SOAPTestCase {
    public TestSetValueFromQName(OMMetaFactory metaFactory) {
        super(metaFactory, SOAPSpec.SOAP12);
```

### JUnitMalformedDeclaration
Test class `TestBuildWithAttachments` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/envelope/TestBuildWithAttachments.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.MTOMSample;

public class TestBuildWithAttachments extends AxiomTestCase {
    public TestBuildWithAttachments(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetXMLStreamReaderMTOMEncoded` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/mtom/TestGetXMLStreamReaderMTOMEncoded.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.MTOMSample;

public class TestGetXMLStreamReaderMTOMEncoded extends AxiomTestCase {
    private static final QName XOP_INCLUDE =
            new QName("http://www.w3.org/2004/08/xop/include", "Include");
```

### JUnitMalformedDeclaration
Test class `TestCreateSOAPFaultSubCode` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/factory/TestCreateSOAPFaultSubCode.java`
#### Snippet
```java
 * SOAPFaultCode} that has already a {@link SOAPFaultValue} child.
 */
public class TestCreateSOAPFaultSubCode extends SOAPTestCase {
    public TestCreateSOAPFaultSubCode(OMMetaFactory metaFactory) {
        super(metaFactory, SOAPSpec.SOAP12);
```

### JUnitMalformedDeclaration
Test class `TestBuilder` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap11/builder/TestBuilder.java`
#### Snippet
```java
import org.apache.axiom.ts.AxiomTestCase;

public class TestBuilder extends AxiomTestCase {
    public TestBuilder(OMMetaFactory metaFactory) {
        super(metaFactory);
```

### JUnitMalformedDeclaration
Test class `TestGetValueWithParser` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/faultcode/TestGetValueWithParser.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SampleBasedSOAPTestCase;

public class TestGetValueWithParser extends SampleBasedSOAPTestCase {
    public TestGetValueWithParser(OMMetaFactory metaFactory) {
        super(metaFactory, SOAPSpec.SOAP12, SOAPSampleSet.SIMPLE_FAULT);
```

### JUnitMalformedDeclaration
Test class `TestSetValueFromQNameWithExistingValue` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/faultcode/TestSetValueFromQNameWithExistingValue.java`
#### Snippet
```java
 * {@link SOAPFaultCode} that already has a {@link SOAPFaultValue} child.
 */
public class TestSetValueFromQNameWithExistingValue extends SOAPTestCase {
    public TestSetValueFromQNameWithExistingValue(OMMetaFactory metaFactory) {
        super(metaFactory, SOAPSpec.SOAP12);
```

### JUnitMalformedDeclaration
Test class `TestGetFaultReasonTextCaseSensitivity` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/faultreason/TestGetFaultReasonTextCaseSensitivity.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SOAPTestCase;

public class TestGetFaultReasonTextCaseSensitivity extends SOAPTestCase {
    public TestGetFaultReasonTextCaseSensitivity(OMMetaFactory metaFactory) {
        super(metaFactory, SOAPSpec.SOAP12);
```

### JUnitMalformedDeclaration
Test class `TestGetFaultReasonTextWithoutLangAttribute` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/faultreason/TestGetFaultReasonTextWithoutLangAttribute.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SOAPTestCase;

public class TestGetFaultReasonTextWithoutLangAttribute extends SOAPTestCase {
    public TestGetFaultReasonTextWithoutLangAttribute(OMMetaFactory metaFactory) {
        super(metaFactory, SOAPSpec.SOAP12);
```

### JUnitMalformedDeclaration
Test class `TestAddSOAPText` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/faultreason/TestAddSOAPText.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SOAPTestCase;

public class TestAddSOAPText extends SOAPTestCase {
    public TestAddSOAPText(OMMetaFactory metaFactory) {
        super(metaFactory, SOAPSpec.SOAP12);
```

### JUnitMalformedDeclaration
Test class `TestGetSubCodeWithParser` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/faultcode/TestGetSubCodeWithParser.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SampleBasedSOAPTestCase;

public class TestGetSubCodeWithParser extends SampleBasedSOAPTestCase {
    public TestGetSubCodeWithParser(OMMetaFactory metaFactory) {
        super(metaFactory, SOAPSample.SOAP12_FAULT);
```

### JUnitMalformedDeclaration
Test class `TestGetFirstSOAPText` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/faultreason/TestGetFirstSOAPText.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SOAPTestCase;

public class TestGetFirstSOAPText extends SOAPTestCase {
    public TestGetFirstSOAPText(OMMetaFactory metaFactory) {
        super(metaFactory, SOAPSpec.SOAP12);
```

### JUnitMalformedDeclaration
Test class `TestGetFirstSOAPTextWithParser` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/faultreason/TestGetFirstSOAPTextWithParser.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SampleBasedSOAPTestCase;

public class TestGetFirstSOAPTextWithParser extends SampleBasedSOAPTestCase {
    public TestGetFirstSOAPTextWithParser(OMMetaFactory metaFactory) {
        super(metaFactory, SOAPSpec.SOAP12, SOAPSampleSet.SIMPLE_FAULT);
```

### JUnitMalformedDeclaration
Test class `TestAddSOAPTextWithSOAPVersionMismatch` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/faultreason/TestAddSOAPTextWithSOAPVersionMismatch.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SOAPTestCase;

public class TestAddSOAPTextWithSOAPVersionMismatch extends SOAPTestCase {
    public TestAddSOAPTextWithSOAPVersionMismatch(OMMetaFactory metaFactory) {
        super(metaFactory, SOAPSpec.SOAP12);
```

### JUnitMalformedDeclaration
Test class `TestGetRelayWithParser` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/headerblock/TestGetRelayWithParser.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SampleBasedSOAPTestCase;

public class TestGetRelayWithParser extends SampleBasedSOAPTestCase {
    public TestGetRelayWithParser(OMMetaFactory metaFactory) {
        super(metaFactory, SOAPSample.SOAP12_RELAY);
```

### JUnitMalformedDeclaration
Test class `TestGetFaultReasonText` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/faultreason/TestGetFaultReasonText.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SampleBasedSOAPTestCase;

public class TestGetFaultReasonText extends SampleBasedSOAPTestCase {
    public TestGetFaultReasonText(OMMetaFactory metaFactory) {
        super(metaFactory, SOAPSample.SOAP12_FAULT);
```

### JUnitMalformedDeclaration
Test class `TestMTOMForwardStreaming` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/envelope/TestMTOMForwardStreaming.java`
#### Snippet
```java
 * projects such as Synapse to forward MTOM messages very efficiently.
 */
public class TestMTOMForwardStreaming extends AxiomTestCase {
    private final boolean buildSOAPPart;

```

### JUnitMalformedDeclaration
Test class `TestAddSOAPTextMultiple` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/faultreason/TestAddSOAPTextMultiple.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SOAPTestCase;

public class TestAddSOAPTextMultiple extends SOAPTestCase {
    public TestAddSOAPTextMultiple(OMMetaFactory metaFactory) {
        super(metaFactory, SOAPSpec.SOAP12);
```

### JUnitMalformedDeclaration
Test class `TestGetLangWithParser` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/faulttext/TestGetLangWithParser.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SampleBasedSOAPTestCase;

public class TestGetLangWithParser extends SampleBasedSOAPTestCase {
    public TestGetLangWithParser(OMMetaFactory metaFactory) {
        super(metaFactory, SOAPSpec.SOAP12, SOAPSampleSet.SIMPLE_FAULT);
```

### JUnitMalformedDeclaration
Test class `TestGetAllSoapTextsWithParser` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/faultreason/TestGetAllSoapTextsWithParser.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SampleBasedSOAPTestCase;

public class TestGetAllSoapTextsWithParser extends SampleBasedSOAPTestCase {
    public TestGetAllSoapTextsWithParser(OMMetaFactory metaFactory) {
        super(metaFactory, SOAPSample.SOAP12_FAULT);
```

### JUnitMalformedDeclaration
Test class `TestGetValueNestedWithParser` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/faultsubcode/TestGetValueNestedWithParser.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SampleBasedSOAPTestCase;

public class TestGetValueNestedWithParser extends SampleBasedSOAPTestCase {
    public TestGetValueNestedWithParser(OMMetaFactory metaFactory) {
        super(metaFactory, SOAPSample.SOAP12_FAULT);
```

### JUnitMalformedDeclaration
Test class `TestGetSubCodeNestedWithParser` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/faultsubcode/TestGetSubCodeNestedWithParser.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SampleBasedSOAPTestCase;

public class TestGetSubCodeNestedWithParser extends SampleBasedSOAPTestCase {
    public TestGetSubCodeNestedWithParser(OMMetaFactory metaFactory) {
        super(metaFactory, SOAPSample.SOAP12_FAULT);
```

### JUnitMalformedDeclaration
Test class `TestGetValueWithParser` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/faultsubcode/TestGetValueWithParser.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SampleBasedSOAPTestCase;

public class TestGetValueWithParser extends SampleBasedSOAPTestCase {
    public TestGetValueWithParser(OMMetaFactory metaFactory) {
        super(metaFactory, SOAPSample.SOAP12_FAULT);
```

### JUnitMalformedDeclaration
Test class `TestGetSubCodeWithParser` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/faultsubcode/TestGetSubCodeWithParser.java`
#### Snippet
```java
import org.apache.axiom.ts.soap.SampleBasedSOAPTestCase;

public class TestGetSubCodeWithParser extends SampleBasedSOAPTestCase {
    public TestGetSubCodeWithParser(OMMetaFactory metaFactory) {
        super(metaFactory, SOAPSample.SOAP12_FAULT);
```

### JUnitMalformedDeclaration
Test class `TestGetValueAsQNameWithParser` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/faultsubcode/TestGetValueAsQNameWithParser.java`
#### Snippet
```java
 * SOAPFaultSubCode}.
 */
public class TestGetValueAsQNameWithParser extends SampleBasedSOAPTestCase {
    public TestGetValueAsQNameWithParser(OMMetaFactory metaFactory) {
        super(metaFactory, SOAPSample.SOAP12_FAULT);
```

### JUnitMalformedDeclaration
Test class `TestLazySOAPFactorySelection` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soapdom/message/TestLazySOAPFactorySelection.java`
#### Snippet
```java
import org.w3c.dom.Document;

public class TestLazySOAPFactorySelection extends SOAPTestCase {
    public TestLazySOAPFactorySelection(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec);
```

### JUnitMalformedDeclaration
Test class `TestExamineMustUnderstandHeaderBlocks` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soapdom/header/TestExamineMustUnderstandHeaderBlocks.java`
#### Snippet
```java
import org.w3c.dom.Element;

public class TestExamineMustUnderstandHeaderBlocks extends SOAPTestCase {
    public TestExamineMustUnderstandHeaderBlocks(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec);
```

### JUnitMalformedDeclaration
Test class `TestExamineAllHeaderBlocks` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soapdom/header/TestExamineAllHeaderBlocks.java`
#### Snippet
```java
 * SOAPHeader#examineAllHeaderBlocks()}.
 */
public class TestExamineAllHeaderBlocks extends SOAPTestCase {
    public TestExamineAllHeaderBlocks(OMMetaFactory metaFactory, SOAPSpec spec) {
        super(metaFactory, spec);
```

### JUnitMalformedDeclaration
Test class `TestBuilder` is not constructable because it does not have a 'public' no-arg or single 'String' parameter constructor
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/builder/TestBuilder.java`
#### Snippet
```java
import org.apache.axiom.ts.AxiomTestCase;

public class TestBuilder extends AxiomTestCase {
    public TestBuilder(OMMetaFactory metaFactory) {
        super(metaFactory);
```

## RuleId[id=MismatchedCollectionQueryUpdate]
### MismatchedCollectionQueryUpdate
Contents of collection `excludedArtifacts` are queried, but never updated
in `buildutils/buildutils-maven-plugin/src/main/java/org/apache/axiom/buildutils/pom/CreateDependencyReducedPomMojo.java`
#### Snippet
```java
    private MavenProject project;

    @Parameter private Set<String> excludedArtifacts;

    @Parameter(defaultValue = "${project.build.directory}/dependency-reduced-pom.xml")
```

### MismatchedCollectionQueryUpdate
Contents of collection `implementationNodes` are updated, but never queried
in `axiom-weaver/src/main/java/org/apache/axiom/weaver/Weaver.java`
#### Snippet
```java
    private final ImplementationClassNameMapper implementationClassNameMapper;
    private final Map<Class<?>, InterfaceNode> interfaceNodes = new HashMap<>();
    private final Map<Class<?>, ImplementationNode> implementationNodes = new HashMap<>();
    private final Map<Class<?>, Set<Mixin>> mixinsByInterface = new HashMap<>();
    private final MutableReferences<ImplementationNode> nodes =
```

## RuleId[id=PointlessBitwiseExpression]
### PointlessBitwiseExpression
`1 << 0` can be replaced with '1'
in `mixins/dom-mixins/src/main/java/org/apache/axiom/dom/DOMConfigurationImpl.java`
#### Snippet
```java
    private static final String WELLFORMED_PARAM = "well-formed";

    public static final int CANONICAL_FORM = 1 << 0;
    public static final int CDATA_SECTIONS = 1 << 1;
    public static final int CHECK_CHAR_NORMALIZATION = 1 << 2;
```

## RuleId[id=SlowAbstractSetRemoveAll]
### SlowAbstractSetRemoveAll
Call to 'set.removeAll(list)' may work slowly
in `testing/soap-testsuite/src/main/java/org/apache/axiom/ts/soap/SOAPSpec.java`
#### Snippet
```java
        result.addAll(getReps(getAltSpec().booleanLiterals));
        List<String> valid = getReps(booleanLiterals);
        result.removeAll(valid);
        result.add("invalid");
        if (valid.contains("true")) {
```

## RuleId[id=PointlessNullCheck]
### PointlessNullCheck
Unnecessary 'null' check before 'isInstance()' call
in `mixins/om-mixins/src/main/java/org/apache/axiom/om/impl/mixin/AxiomSourcedElementMixin.java`
#### Snippet
```java
    @Override
    public Object getObject(Class<? extends OMDataSourceExt> dataSourceClass) {
        if (dataSource == null || isExpanded || !dataSourceClass.isInstance(dataSource)) {
            return null;
        } else {
```

## RuleId[id=StringConcatenationInLoops]
### StringConcatenationInLoops
String concatenation `+=` in loop
in `axiom-api/src/main/java/org/apache/axiom/om/util/DigestGenerator.java`
#### Snippet
```java
    public String getStringRepresentation(byte[] array) {
        String str = "";
        for (int i = 0; i < array.length; i++) str += array[i];
        return str;
    }
```

## RuleId[id=UnusedAssignment]
### UnusedAssignment
Variable `digest` initializer `new byte[0]` is redundant
in `axiom-api/src/main/java/org/apache/axiom/om/util/DigestGenerator.java`
#### Snippet
```java
     */
    public byte[] getDigest(OMElement element, String digestAlgorithm) throws OMException {
        byte[] digest = new byte[0];
        try {
            MessageDigest md = MessageDigest.getInstance(digestAlgorithm);
```

### UnusedAssignment
Variable `digest` initializer `new byte[0]` is redundant
in `axiom-api/src/main/java/org/apache/axiom/om/util/DigestGenerator.java`
#### Snippet
```java
     */
    public byte[] getDigest(OMText text, String digestAlgorithm) throws OMException {
        byte[] digest = new byte[0];
        try {
            MessageDigest md = MessageDigest.getInstance(digestAlgorithm);
```

### UnusedAssignment
Variable `digest` initializer `new byte[0]` is redundant
in `axiom-api/src/main/java/org/apache/axiom/om/util/DigestGenerator.java`
#### Snippet
```java
     */
    public byte[] getDigest(OMProcessingInstruction pi, String digestAlgorithm) throws OMException {
        byte[] digest = new byte[0];
        try {
            MessageDigest md = MessageDigest.getInstance(digestAlgorithm);
```

### UnusedAssignment
Variable `digest` initializer `new byte[0]` is redundant
in `axiom-api/src/main/java/org/apache/axiom/om/util/DigestGenerator.java`
#### Snippet
```java
     */
    public byte[] getDigest(OMDocument document, String digestAlgorithm) throws OMException {
        byte[] digest = new byte[0];
        try {
            MessageDigest md = MessageDigest.getInstance(digestAlgorithm);
```

### UnusedAssignment
Variable `throwExceptions` initializer `false` is redundant
in `axiom-api/src/main/java/org/apache/axiom/util/stax/debug/XMLStreamReaderValidator.java`
#### Snippet
```java
    private static boolean IS_ADV_DEBUG_ENABLED = false;  // Turn this on to trace every event
    
    private boolean throwExceptions = false;   // Indicates whether OMException should be thrown if errors are disovered
    private Stack<QName> stack = new Stack<QName>(); // Stack keeps track of the nested element QName
    
```

### UnusedAssignment
Variable `ct` initializer `null` is redundant
in `axiom-api/src/main/java/org/apache/axiom/om/OMOutputFormat.java`
#### Snippet
```java
    public String getContentType() {
       
        String ct = null;
        if (log.isDebugEnabled()) {
            log.debug("Start getContentType: " + toString());
```

### UnusedAssignment
Variable `header` initializer `null` is redundant
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/IncomingAttachmentInputStream.java`
#### Snippet
```java
     */
    public String getHeader(String name) {
        Object header = null;
        if (_headersLowerCase == null || (header = _headersLowerCase.get(name.toLowerCase())) == null) {
            return null;
```

### UnusedAssignment
Variable `_stream` initializer `null` is redundant
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/IncomingAttachmentInputStream.java`
#### Snippet
```java
    private Map<String,String> _headersLowerCase = null;
    
    private InputStream _stream = null;
    private IncomingAttachmentStreams parentContainer;

```

### UnusedAssignment
Variable `file` initializer `null` is redundant
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/lifecycle/impl/FileAccessor.java`
#### Snippet
```java
public class FileAccessor extends Observable{
    private static final Log log = LogFactory.getLog(FileAccessor.class);
    File file = null;
    LifecycleManager manager;
    private int accessCount = 0;
```

### UnusedAssignment
Variable `newNode` initializer `null` is redundant
in `mixins/dom-mixins/src/main/java/org/apache/axiom/dom/impl/mixin/DOMDocumentMixin.java`
#### Snippet
```java

        short type = importedNode.getNodeType();
        Node newNode = null;
        switch (type) {
            case Node.ELEMENT_NODE:
```

### UnusedAssignment
Variable `refreshSeconds` initializer `0` is redundant
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/AttachmentCacheMonitor.java`
#### Snippet
```java
    // The suggested value is 300 seconds
    private int attachmentTimeoutSeconds = 0;  // Default is 0 (forever)
    private int refreshSeconds = 0;
    public static final String ATTACHMENT_TIMEOUT_PROPERTY = "org.apache.axiom.attachments.tempfile.expiration";

```

### UnusedAssignment
Variable `file` initializer `null` is redundant
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/lifecycle/impl/LifecycleManagerImpl.java`
#### Snippet
```java
            log.debug("Start Create()");
        }
        File file = null;
        File dir = null;
        if (attachmentDir != null) {
```

### UnusedAssignment
Variable `chars` initializer `null` is redundant
in `axiom-compat/src/main/java/org/apache/axiom/om/ds/CharArrayDataSource.java`
#### Snippet
```java
public class CharArrayDataSource extends OMDataSourceExtBase {

    char[] chars = null;
   
    /**
```

### UnusedAssignment
Variable `byteArray` initializer `null` is redundant
in `axiom-compat/src/main/java/org/apache/axiom/om/ds/ByteArrayDataSource.java`
#### Snippet
```java
    private static final Log log = LogFactory.getLog(ByteArrayDataSource.class);
	
    ByteArray byteArray = null;
    
    /**
```

### UnusedAssignment
Variable `data` initializer `null` is redundant
in `axiom-compat/src/main/java/org/apache/axiom/om/ds/InputStreamDataSource.java`
#### Snippet
```java
public class InputStreamDataSource extends OMDataSourceExtBase {

    Data data = null;
    private static final int BUFFER_LEN = 4096;
    
```

### UnusedAssignment
Variable `encoding` initializer `null` is redundant
in `axiom-compat/src/main/java/org/apache/axiom/om/ds/custombuilder/ByteArrayCustomBuilder.java`
#### Snippet
```java
 */
public class ByteArrayCustomBuilder implements CustomBuilder {
    private String encoding = null;
    
    /**
```

### UnusedAssignment
Variable `isDebugEnabled` initializer `false` is redundant
in `axiom-compat/src/main/java/org/apache/axiom/om/util/LogOutputStream.java`
#### Snippet
```java

    private byte[] temp = new byte[1];
    private boolean isDebugEnabled = false;
    private long count = 0;
    private Log log;
```

### UnusedAssignment
Variable `md5` initializer `null` is redundant
in `axiom-compat/src/main/java/org/apache/axiom/om/util/UUIDGenerator.java`
#### Snippet
```java
        sb.append(":");
        sb.append(Long.toString(rand));
        MessageDigest md5 = null;
        try {
            md5 = MessageDigest.getInstance("MD5");
```

### UnusedAssignment
Variable `state` initializer `null` is redundant
in `axiom-compat/src/main/java/org/apache/axiom/util/stax/XMLEventUtils.java`
#### Snippet
```java
     */
    public static String getEventTypeString(int event) {
        String state = null;
        switch(event) {
        case XMLStreamConstants.START_ELEMENT:
```

### UnusedAssignment
Variable `k` initializer `0` is redundant
in `axiom-compat/src/main/java/org/apache/axiom/attachments/utils/ByteSearch.java`
#### Snippet
```java
        
        if (direction) {
            int k = 0;
            for (k = start + patternLength - 1; 
                k < end; // end is exclusive
```

### UnusedAssignment
Variable `state` initializer `null` is redundant
in `components/xml-utils/src/main/java/org/apache/axiom/util/xml/stream/XMLEventUtils.java`
#### Snippet
```java
     */
    public static String getEventTypeString(int event) {
        String state = null;
        switch(event) {
        case XMLStreamConstants.START_ELEMENT:
```

### UnusedAssignment
The value `null` assigned to `reader` is never used
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/builder/TestCloseWithXMLStreamReader.java`
#### Snippet
```java
            WeakReference<XMLStreamReader> readerWeakRef =
                    new WeakReference<XMLStreamReader>(reader);
            reader = null;
            builder.getDocument().build();
            builder.close();
```

### UnusedAssignment
Variable `documentElement` initializer `null` is redundant
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestDiscardIncomplete.java`
#### Snippet
```java
    @Override
    protected void runTest() throws Throwable {
        OMElement documentElement = null;

        // first build the OM tree without caching and see whether we can discard
```

### UnusedAssignment
Variable `parentTagName` initializer `""` is redundant
in `mixins/om-mixins/src/main/java/org/apache/axiom/soap/impl/common/builder/SOAP11BuilderHelper.java`
#### Snippet
```java
        } else if (elementLevel == 5) {

            String parentTagName = "";
            if (parent instanceof Element) {
                parentTagName = ((Element) parent).getTagName();
```

## RuleId[id=OptionalGetWithoutIsPresent]
### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `testing/jaxp-testsuite/src/main/java/org/apache/axiom/ts/jaxp/stax/StAXImplementation.java`
#### Snippet
```java
                                    }
                                }
                                return ServiceLoader.load(type, classLoader).findFirst().get();
                            }

```

## RuleId[id=ConstantValue]
### ConstantValue
Condition `format != null` is always `true`
in `axiom-api/src/main/java/org/apache/axiom/om/impl/OMMultipartWriter.java`
#### Snippet
```java
        
        ContentTransferEncodingPolicy contentTransferEncodingPolicy = format.getContentTransferEncodingPolicy();
        if (format != null && Boolean.TRUE.equals(
                format.getProperty(OMOutputFormat.USE_CTE_BASE64_FOR_NON_TEXTUAL_ATTACHMENTS))) {
            if (contentTransferEncodingPolicy == null) {
```

### ConstantValue
Condition `i==len` is always `false` when reached
in `axiom-api/src/main/java/org/apache/axiom/util/stax/dialect/BEAStreamReaderWrapper.java`
#### Snippet
```java
            for (int i=0; i<len; i++) {
                char c = text.charAt(i);
                if (c == '\r' && (i==len || text.charAt(i+1) == '\n')) {
                    if (buffer == null) {
                        buffer = new StringBuffer(len-1);
```

### ConstantValue
Condition `elementTwo != null` is always `true` when reached
in `axiom-compat/src/main/java/org/apache/axiom/om/impl/llom/util/XMLComparator.java`
#### Snippet
```java
            return true;
        }
        if (elementOne == null && elementTwo != null) {
            throw new XMLComparisonException(
                    "Element One is null and Element Two is not null");
```

### ConstantValue
Condition `elementOne != null` is always `true`
in `axiom-compat/src/main/java/org/apache/axiom/om/impl/llom/util/XMLComparator.java`
#### Snippet
```java
                    "Element One is null and Element Two is not null");
        }
        if (elementOne != null && elementTwo == null) {
            throw new XMLComparisonException(
                    "Element Two is null and Element One is not null");
```

### ConstantValue
Condition `two != null` is always `true` when reached
in `axiom-compat/src/main/java/org/apache/axiom/om/impl/llom/util/XMLComparator.java`
#### Snippet
```java
            throw new XMLComparisonException(
                    "First Namespace is NOT null. But the second is null");
        } else if (one == null && two != null) {
            throw new XMLComparisonException(
                    "First Namespace is null. But the second is NOT null");
```

### ConstantValue
Condition `0x10000 <= c` is always `false`
in `components/xml-utils/src/main/java/org/apache/axiom/util/xml/XMLChar.java`
#### Snippet
```java
                || 0x2070 <= c && c <= 0x218F || 0x2C00 <= c && c <= 0x2FEF
                || 0x3001 <= c && c <= 0xD7FF || 0xF900 <= c && c <= 0xFDCF
                || 0xFDF0 <= c && c <= 0xFFFD || 0x10000 <= c && c <= 0xEFFFF;
    }
    
```

### ConstantValue
Condition `0x10000 <= c && c <= 0xEFFFF` is always `false`
in `components/xml-utils/src/main/java/org/apache/axiom/util/xml/XMLChar.java`
#### Snippet
```java
                || 0x2070 <= c && c <= 0x218F || 0x2C00 <= c && c <= 0x2FEF
                || 0x3001 <= c && c <= 0xD7FF || 0xF900 <= c && c <= 0xFDCF
                || 0xFDF0 <= c && c <= 0xFFFD || 0x10000 <= c && c <= 0xEFFFF;
    }
    
```

### ConstantValue
Value `optimize` is always 'true'
in `mixins/om-mixins/src/main/java/org/apache/axiom/om/impl/stream/xop/OptimizationPolicyImpl.java`
#### Snippet
```java
            return true;
        } else {
            return isOptimized(blobProvider.getBlob(), optimize);
        }
    }
```

### ConstantValue
Value `hasDTD` is always 'false'
in `testing/xml-testsuite/src/main/java/org/apache/axiom/ts/xml/XMLSampleProperties.java`
#### Snippet
```java
            throw new Error("Unable to parse " + sample.getUrl(), ex);
        }
        this.hasDTD = hasDTD;
        this.hasExternalSubset = hasExternalSubset;
        this.hasInternalSubset = hasInternalSubset;
```

### ConstantValue
Value `hasExternalSubset` is always 'false'
in `testing/xml-testsuite/src/main/java/org/apache/axiom/ts/xml/XMLSampleProperties.java`
#### Snippet
```java
        }
        this.hasDTD = hasDTD;
        this.hasExternalSubset = hasExternalSubset;
        this.hasInternalSubset = hasInternalSubset;
        this.hasEntityReferences = hasEntityReferences;
```

### ConstantValue
Value `hasInternalSubset` is always 'false'
in `testing/xml-testsuite/src/main/java/org/apache/axiom/ts/xml/XMLSampleProperties.java`
#### Snippet
```java
        this.hasDTD = hasDTD;
        this.hasExternalSubset = hasExternalSubset;
        this.hasInternalSubset = hasInternalSubset;
        this.hasEntityReferences = hasEntityReferences;
    }
```

### ConstantValue
Condition is always true
in `testing/jaxen-testsuite/src/main/java/org/jaxen/test/ExprComparator.java`
#### Snippet
```java
					break;
				case TYPE_FILTER_EXPR:
					if (true)
						throw new RuntimeException("Not yet implemented!");
					break;
```

### ConstantValue
Condition is always true
in `testing/jaxen-testsuite/src/main/java/org/jaxen/test/ExprComparator.java`
#### Snippet
```java
					break;
				case TYPE_UNION_EXPR:
					if (true)
						throw new RuntimeException("Not yet implemented!");
					break;
```

## RuleId[id=UNCHECKED_WARNING]
### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Collection' to 'java.util.Collection'
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/w3c/JUnitTestFramework.java`
#### Snippet
```java
            DOMTestCase test, String assertID, Collection expected, Collection actual) {
        Assert.assertArrayEquals(
                assertID, toArray(expected, true, true), toArray(actual, true, true));
    }

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Collection' to 'java.util.Collection'
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/w3c/JUnitTestFramework.java`
#### Snippet
```java
            DOMTestCase test, String assertID, Collection expected, Collection actual) {
        Assert.assertArrayEquals(
                assertID, toArray(expected, true, true), toArray(actual, true, true));
    }

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Collection' to 'java.util.Collection'
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/w3c/JUnitTestFramework.java`
#### Snippet
```java
            DOMTestCase test, String assertID, Collection expected, Collection actual) {
        Assert.assertArrayEquals(
                assertID, toArray(expected, false, true), toArray(actual, false, true));
    }

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Collection' to 'java.util.Collection'
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/w3c/JUnitTestFramework.java`
#### Snippet
```java
            DOMTestCase test, String assertID, Collection expected, Collection actual) {
        Assert.assertArrayEquals(
                assertID, toArray(expected, false, true), toArray(actual, false, true));
    }

```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.Hashtable'
in `mixins/dom-mixins/src/main/java/org/apache/axiom/dom/impl/mixin/DOMNodeMixin.java`
#### Snippet
```java
            userData = new Hashtable();
        }
        return userData.put(key, value);
    }

```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `testing/jaxen-testsuite/src/main/java/org/apache/axiom/test/jaxen/JaxenXPathTestBase.java`
#### Snippet
```java
                    URL url = new URL(TESTS_ROOT + uri);
                    Object document = loadDocument(url.openStream());
                    documents.add(document);
                    return document;
                } catch (Exception ex) {
```

### UNCHECKED_WARNING
Unchecked call to 'compare(T, T)' as a member of raw type 'java.util.Comparator'
in `testing/jaxen-testsuite/src/main/java/org/jaxen/test/XPathTestBase.java`
#### Snippet
```java
    private void assertExprGetTextIdempotent(BaseXPath xpath) throws JaxenException
    {
    	assertEquals(0, ExprComparator.EXPR_COMPARATOR.compare(xpath.getRootExpr(), 
          new BaseXPath(xpath.getRootExpr().getText(), null).getRootExpr()));
    }
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `testing/jaxen-testsuite/src/main/java/org/jaxen/test/XPathTestBase.java`
#### Snippet
```java
        Context context = new Context(getContextSupport());
        List list = new ArrayList(1);
        list.add(contextNode);
        context.setNodeSet(list);
        return context;
```

## RuleId[id=SpringPlaceholdersInspection]
### SpringPlaceholdersInspection
Cannot resolve property key
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/saaj-message-factory.xml`
#### Snippet
```java
    <bean name="messageFactory" class="org.springframework.ws.soap.saaj.SaajSoapMessageFactory">
        <property name="soapVersion">
            <util:constant static-field="org.springframework.ws.soap.SoapVersion.${soapVersion}"/>
        </property>
    </bean>
```

### SpringPlaceholdersInspection
Cannot resolve property key
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/wsadom/client.xml`
#### Snippet
```java
    <bean class="org.springframework.ws.client.core.WebServiceTemplate">
        <constructor-arg ref="messageFactory"/>
        <property name="defaultUri" value="http://localhost:${port}/"/>
    </bean>
</beans>
```

### SpringPlaceholdersInspection
Cannot resolve property key
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/jdom/client.xml`
#### Snippet
```java
    <bean class="org.springframework.ws.client.core.WebServiceTemplate">
        <constructor-arg ref="messageFactory"/>
        <property name="defaultUri" value="http://localhost:${port}/"/>
    </bean>
</beans>
```

### SpringPlaceholdersInspection
Cannot resolve property key
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/jaxb2/client.xml`
#### Snippet
```java
        <property name="marshaller" ref="marshaller"/>
        <property name="unmarshaller" ref="marshaller"/>
        <property name="defaultUri" value="http://localhost:${port}/"/>
    </bean>
</beans>
```

### SpringPlaceholdersInspection
Cannot resolve property key
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/soapaction/client.xml`
#### Snippet
```java
    <bean class="org.springframework.ws.client.core.WebServiceTemplate">
        <constructor-arg ref="messageFactory"/>
        <property name="defaultUri" value="http://localhost:${port}/"/>
    </bean>
</beans>
```

### SpringPlaceholdersInspection
Cannot resolve property key
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/secureecho/client.xml`
#### Snippet
```java
    <bean class="org.springframework.ws.client.core.WebServiceTemplate">
        <constructor-arg ref="messageFactory"/>
        <property name="defaultUri" value="http://localhost:${port}/"/>
        <property name="interceptors">
            <bean class="org.springframework.ws.soap.security.wss4j2.Wss4jSecurityInterceptor">
```

### SpringPlaceholdersInspection
Cannot resolve property key
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/broker/client.xml`
#### Snippet
```java
                <property name="marshaller" ref="marshaller"/>
                <property name="unmarshaller" ref="marshaller"/>
                <property name="defaultUri" value="http://localhost:${port}/"/>
            </bean>
        </constructor-arg>
```

### SpringPlaceholdersInspection
Cannot resolve property key
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/validation/client.xml`
#### Snippet
```java
                <property name="marshaller" ref="marshaller"/>
                <property name="unmarshaller" ref="marshaller"/>
                <property name="defaultUri" value="http://localhost:${port}/"/>
            </bean>
        </constructor-arg>
```

## RuleId[id=JavadocReference]
### JavadocReference
Cannot resolve symbol `SoapMessageFactory`
in `testing/spring-ws-testsuite/src/main/java/org/apache/axiom/ts/springws/MessageFactoryConfigurator.java`
#### Snippet
```java

    /**
     * Configure a {@link SoapMessageFactory} in the given application context. The method must
     *
     * <ul>
```

### JavadocReference
Cannot resolve symbol `SoapMessageFactory`
in `testing/spring-ws-testsuite/src/main/java/org/apache/axiom/ts/springws/MessageFactoryConfigurator.java`
#### Snippet
```java
     *
     * <ul>
     *   <li>add a bean of type {@link SoapMessageFactory} and name {@code messageFactory} to the
     *       context;
     *   <li>configure that {@link SoapMessageFactory} to use the SOAP version specified by the
```

### JavadocReference
Cannot resolve symbol `SoapMessageFactory`
in `testing/spring-ws-testsuite/src/main/java/org/apache/axiom/ts/springws/MessageFactoryConfigurator.java`
#### Snippet
```java
     *   <li>add a bean of type {@link SoapMessageFactory} and name {@code messageFactory} to the
     *       context;
     *   <li>configure that {@link SoapMessageFactory} to use the SOAP version specified by the
     *       {@code soapVersion} property (which specifies the name of one of the constants defined
     *       in {@link SoapVersion}).
```

### JavadocReference
Cannot resolve symbol `SoapVersion`
in `testing/spring-ws-testsuite/src/main/java/org/apache/axiom/ts/springws/MessageFactoryConfigurator.java`
#### Snippet
```java
     *   <li>configure that {@link SoapMessageFactory} to use the SOAP version specified by the
     *       {@code soapVersion} property (which specifies the name of one of the constants defined
     *       in {@link SoapVersion}).
     * </ul>
     *
```

### JavadocReference
Cannot resolve symbol `SoapMessageFactory`
in `testing/spring-ws-testsuite/src/main/java/org/apache/axiom/ts/springws/MessageFactoryConfigurator.java`
#### Snippet
```java
import org.springframework.ws.soap.SoapVersion;

/** Defines a particular {@link SoapMessageFactory} configuration. */
public abstract class MessageFactoryConfigurator {
    public static final MessageFactoryConfigurator SAAJ =
```

### JavadocReference
Cannot resolve symbol `SoapMessage`
in `testing/spring-ws-testsuite/src/main/java/org/apache/axiom/ts/springws/soap/messagefactory/TestCreateWebServiceMessageFromInputStreamMTOM.java`
#### Snippet
```java

/**
 * Tests the characteristics of the {@link SoapMessage} returned by {@link
 * SoapMessageFactory#createWebServiceMessage(InputStream)}.
 */
```

### JavadocReference
Cannot resolve symbol `SoapMessageFactory`
in `testing/spring-ws-testsuite/src/main/java/org/apache/axiom/ts/springws/soap/messagefactory/TestCreateWebServiceMessageFromInputStreamMTOM.java`
#### Snippet
```java
/**
 * Tests the characteristics of the {@link SoapMessage} returned by {@link
 * SoapMessageFactory#createWebServiceMessage(InputStream)}.
 */
public class TestCreateWebServiceMessageFromInputStreamMTOM extends SimpleTestCase {
```

### JavadocReference
Cannot resolve symbol `createWebServiceMessage(InputStream)`
in `testing/spring-ws-testsuite/src/main/java/org/apache/axiom/ts/springws/soap/messagefactory/TestCreateWebServiceMessageFromInputStreamMTOM.java`
#### Snippet
```java
/**
 * Tests the characteristics of the {@link SoapMessage} returned by {@link
 * SoapMessageFactory#createWebServiceMessage(InputStream)}.
 */
public class TestCreateWebServiceMessageFromInputStreamMTOM extends SimpleTestCase {
```

### JavadocReference
Cannot resolve symbol `SoapMessage`
in `testing/spring-ws-testsuite/src/main/java/org/apache/axiom/ts/springws/soap/messagefactory/TestCreateWebServiceMessage.java`
#### Snippet
```java

/**
 * Tests the characteristics of the {@link SoapMessage} returned by {@link
 * SoapMessageFactory#createWebServiceMessage()}.
 */
```

### JavadocReference
Cannot resolve symbol `SoapMessageFactory`
in `testing/spring-ws-testsuite/src/main/java/org/apache/axiom/ts/springws/soap/messagefactory/TestCreateWebServiceMessage.java`
#### Snippet
```java
/**
 * Tests the characteristics of the {@link SoapMessage} returned by {@link
 * SoapMessageFactory#createWebServiceMessage()}.
 */
public class TestCreateWebServiceMessage extends SimpleTestCase {
```

### JavadocReference
Cannot resolve symbol `createWebServiceMessage()`
in `testing/spring-ws-testsuite/src/main/java/org/apache/axiom/ts/springws/soap/messagefactory/TestCreateWebServiceMessage.java`
#### Snippet
```java
/**
 * Tests the characteristics of the {@link SoapMessage} returned by {@link
 * SoapMessageFactory#createWebServiceMessage()}.
 */
public class TestCreateWebServiceMessage extends SimpleTestCase {
```

### JavadocReference
Cannot resolve symbol `SoapMessage`
in `testing/spring-ws-testsuite/src/main/java/org/apache/axiom/ts/springws/soap/messagefactory/TestCreateWebServiceMessageFromInputStream.java`
#### Snippet
```java

/**
 * Tests the characteristics of the {@link SoapMessage} returned by {@link
 * SoapMessageFactory#createWebServiceMessage(InputStream)}.
 */
```

### JavadocReference
Cannot resolve symbol `SoapMessageFactory`
in `testing/spring-ws-testsuite/src/main/java/org/apache/axiom/ts/springws/soap/messagefactory/TestCreateWebServiceMessageFromInputStream.java`
#### Snippet
```java
/**
 * Tests the characteristics of the {@link SoapMessage} returned by {@link
 * SoapMessageFactory#createWebServiceMessage(InputStream)}.
 */
public class TestCreateWebServiceMessageFromInputStream extends SimpleTestCase {
```

### JavadocReference
Cannot resolve symbol `createWebServiceMessage(InputStream)`
in `testing/spring-ws-testsuite/src/main/java/org/apache/axiom/ts/springws/soap/messagefactory/TestCreateWebServiceMessageFromInputStream.java`
#### Snippet
```java
/**
 * Tests the characteristics of the {@link SoapMessage} returned by {@link
 * SoapMessageFactory#createWebServiceMessage(InputStream)}.
 */
public class TestCreateWebServiceMessageFromInputStream extends SimpleTestCase {
```

### JavadocReference
Cannot resolve symbol `SoapMessageFactory`
in `testing/spring-ws-testsuite/src/main/java/org/apache/axiom/ts/springws/soap/messagefactory/TestCreateWebServiceMessageFromInputStreamVersionMismatch.java`
#### Snippet
```java

/**
 * Tests that {@link SoapMessageFactory#createWebServiceMessage(InputStream)} throws {@link
 * SoapMessageCreationException} if there is a mismatch between the SOAP version implied by the
 * content type and the actual SOAP version used by the message.
```

### JavadocReference
Cannot resolve symbol `createWebServiceMessage(InputStream)`
in `testing/spring-ws-testsuite/src/main/java/org/apache/axiom/ts/springws/soap/messagefactory/TestCreateWebServiceMessageFromInputStreamVersionMismatch.java`
#### Snippet
```java

/**
 * Tests that {@link SoapMessageFactory#createWebServiceMessage(InputStream)} throws {@link
 * SoapMessageCreationException} if there is a mismatch between the SOAP version implied by the
 * content type and the actual SOAP version used by the message.
```

### JavadocReference
Cannot resolve symbol `SoapMessageCreationException`
in `testing/spring-ws-testsuite/src/main/java/org/apache/axiom/ts/springws/soap/messagefactory/TestCreateWebServiceMessageFromInputStreamVersionMismatch.java`
#### Snippet
```java
/**
 * Tests that {@link SoapMessageFactory#createWebServiceMessage(InputStream)} throws {@link
 * SoapMessageCreationException} if there is a mismatch between the SOAP version implied by the
 * content type and the actual SOAP version used by the message.
 */
```

### JavadocReference
Cannot resolve symbol `XPathParam`
in `testing/spring-ws-testsuite/src/main/java/org/apache/axiom/ts/springws/scenario/broker/BrokerScenarioTest.java`
#### Snippet
```java
 *
 * <ul>
 *   <li>{@link XPathParam} parameters.
 *   <li>{@link Source} parameters and return values.
 *   <li>Endpoint methods with multiple parameters that require procession the payload.
```

### JavadocReference
Symbol `javax.activation.DataHandler` is inaccessible from here
in `axiom-api/src/main/java/org/apache/axiom/om/OMText.java`
#### Snippet
```java
 * <p>
 * An {@link OMText} node stores the character data as {@link String},
 * <code>char[]</code> or a {@link javax.activation.DataHandler}. The latter is
 * used for base64 encoded binary data.
 */
```

### JavadocReference
Cannot resolve symbol `EntityState`
in `axiom-api/src/main/java/org/apache/axiom/mime/PartImpl.java`
#### Snippet
```java
    /**
     * The part has not been read yet. In this case the parser is in state
     * {@link EntityState#T_BODY}.
     */
    private static final int STATE_UNREAD = 0;
```

### JavadocReference
Cannot resolve symbol `T_BODY`
in `axiom-api/src/main/java/org/apache/axiom/mime/PartImpl.java`
#### Snippet
```java
    /**
     * The part has not been read yet. In this case the parser is in state
     * {@link EntityState#T_BODY}.
     */
    private static final int STATE_UNREAD = 0;
```

### JavadocReference
Symbol `org.apache.axiom.om.util.ElementHelper` is inaccessible from here
in `axiom-api/src/main/java/org/apache/axiom/util/stax/TextFromElementReader.java`
#### Snippet
```java
 * <p>
 * Note that this class should in general not be used directly. Instead, 
 * {@link org.apache.axiom.om.util.ElementHelper#getTextAsStream(org.apache.axiom.om.OMElement)}
 * should be called to get the most efficient stream implementation for a given an element.
 */
```

### JavadocReference
Cannot resolve symbol `getTextAsStream(org.apache.axiom.om.OMElement)`
in `axiom-api/src/main/java/org/apache/axiom/util/stax/TextFromElementReader.java`
#### Snippet
```java
 * <p>
 * Note that this class should in general not be used directly. Instead, 
 * {@link org.apache.axiom.om.util.ElementHelper#getTextAsStream(org.apache.axiom.om.OMElement)}
 * should be called to get the most efficient stream implementation for a given an element.
 */
```

### JavadocReference
Cannot resolve symbol `DTDInfo`
in `axiom-api/src/main/java/org/apache/axiom/util/stax/dialect/StAX2StreamReaderWrapper.java`
#### Snippet
```java
/**
 * {@link XMLStreamReaderWrapper} implementation that exposes the {@link DTDReader} extension
 * based on the {@link DTDInfo} defined by "StAX2".
 */
class StAX2StreamReaderWrapper extends XMLStreamReaderWrapper implements DTDReader {
```

### JavadocReference
Cannot resolve symbol `createAttribute(CoreElement, String, String, String, String)`
in `mixins/core-mixins/src/main/java/org/apache/axiom/core/CoreElement.java`
#### Snippet
```java
     * @param namespaceURI the <code>namespaceURI</code> parameter to pass to {@link
     *     AttributeMatcher#matches(CoreAttribute, String, String)} and {@link
     *     AttributeMatcher#createAttribute(CoreElement, String, String, String, String)}
     * @param name the <code>name</code> parameter to pass to {@link
     *     AttributeMatcher#matches(CoreAttribute, String, String)} and {@link
```

### JavadocReference
Cannot resolve symbol `ExceptionTranslator`
in `mixins/core-mixins/src/main/java/org/apache/axiom/core/NodeIterator.java`
#### Snippet
```java
 *   <li>If a {@link CoreModelException} occurs inside {@link Iterator#hasNext()}, {@link
 *       Iterator#next()} or {@link Iterator#remove()}, then the implementation MUST use the
 *       supplied {@link ExceptionTranslator} to translate that checked exception into an unchecked
 *       exception.
 * </ol>
```

### JavadocReference
Cannot resolve symbol `createAttribute(CoreElement, String, String, String, String)`
in `mixins/core-mixins/src/main/java/org/apache/axiom/core/CoreElement.java`
#### Snippet
```java
     * @param name the <code>name</code> parameter to pass to {@link
     *     AttributeMatcher#matches(CoreAttribute, String, String)} and {@link
     *     AttributeMatcher#createAttribute(CoreElement, String, String, String, String)}
     * @param prefix the <code>prefix</code> parameter to pass to {@link
     *     AttributeMatcher#createAttribute(CoreElement, String, String, String, String)} and {@link
```

### JavadocReference
Cannot resolve symbol `createAttribute(CoreElement, String, String, String, String)`
in `mixins/core-mixins/src/main/java/org/apache/axiom/core/CoreElement.java`
#### Snippet
```java
     *     AttributeMatcher#createAttribute(CoreElement, String, String, String, String)}
     * @param prefix the <code>prefix</code> parameter to pass to {@link
     *     AttributeMatcher#createAttribute(CoreElement, String, String, String, String)} and {@link
     *     AttributeMatcher#update(CoreAttribute, String, String)}
     * @param value the <code>value</code> parameter to pass to {@link
```

### JavadocReference
Cannot resolve symbol `createAttribute(CoreElement, String, String, String, String)`
in `mixins/core-mixins/src/main/java/org/apache/axiom/core/CoreElement.java`
#### Snippet
```java
     *     AttributeMatcher#update(CoreAttribute, String, String)}
     * @param value the <code>value</code> parameter to pass to {@link
     *     AttributeMatcher#createAttribute(CoreElement, String, String, String, String)} and {@link
     *     AttributeMatcher#update(CoreAttribute, String, String)}
     */
```

### JavadocReference
Cannot resolve symbol `handler`
in `mixins/core-mixins/src/main/java/org/apache/axiom/core/InputContext.java`
#### Snippet
```java
     * handler instead of building nodes for them.
     *
     * @param handler the handler to send events to
     * @throws IllegalStateException if a pass-through handler has already been set for this context
     */
```

### JavadocReference
Cannot resolve symbol `XmlInput`
in `mixins/core-mixins/src/main/java/org/apache/axiom/core/CoreModelStreamException.java`
#### Snippet
```java

/**
 * {@link StreamException} wrapper for {@link CoreModelException} used by the {@link XmlInput}
 * returned by {@link CoreParentNode#coreGetInput(boolean)}.
 */
```

### JavadocReference
Cannot resolve symbol `coreGetInput(boolean)`
in `mixins/core-mixins/src/main/java/org/apache/axiom/core/CoreModelStreamException.java`
#### Snippet
```java
/**
 * {@link StreamException} wrapper for {@link CoreModelException} used by the {@link XmlInput}
 * returned by {@link CoreParentNode#coreGetInput(boolean)}.
 */
public class CoreModelStreamException extends StreamException {
```

### JavadocReference
Cannot resolve symbol `OMNode`
in `mixins/core-mixins/src/main/java/org/apache/axiom/core/CoreParentNode.java`
#### Snippet
```java
    /**
     * Get the first child if it is available. The child is available if it is complete or if the
     * builder has started building the node. In the latter case, {@link OMNode#isComplete()} may
     * return <code>false</code> when called on the child. In contrast to {@link
     * OMContainer#getFirstOMChild()}, this method will never modify the state of the underlying
```

### JavadocReference
Cannot resolve symbol `node`
in `mixins/core-mixins/src/main/java/org/apache/axiom/core/Semantics.java`
#### Snippet
```java
     * selected by {@link Axis#DESCENDANTS} and {@link Axis#DESCENDANTS_OR_SELF}.
     *
     * @param node the node type to check
     * @return {@code true} if the node type is a parent node; {@link false} if the node type is
     *     considered a leaf node type
```

### JavadocReference
Cannot resolve symbol `isComplete()`
in `mixins/core-mixins/src/main/java/org/apache/axiom/core/CoreParentNode.java`
#### Snippet
```java
    /**
     * Get the first child if it is available. The child is available if it is complete or if the
     * builder has started building the node. In the latter case, {@link OMNode#isComplete()} may
     * return <code>false</code> when called on the child. In contrast to {@link
     * OMContainer#getFirstOMChild()}, this method will never modify the state of the underlying
```

### JavadocReference
Cannot resolve symbol `OMContainer`
in `mixins/core-mixins/src/main/java/org/apache/axiom/core/CoreParentNode.java`
#### Snippet
```java
     * builder has started building the node. In the latter case, {@link OMNode#isComplete()} may
     * return <code>false</code> when called on the child. In contrast to {@link
     * OMContainer#getFirstOMChild()}, this method will never modify the state of the underlying
     * parser.
     *
```

### JavadocReference
Cannot resolve symbol `getFirstOMChild()`
in `mixins/core-mixins/src/main/java/org/apache/axiom/core/CoreParentNode.java`
#### Snippet
```java
     * builder has started building the node. In the latter case, {@link OMNode#isComplete()} may
     * return <code>false</code> when called on the child. In contrast to {@link
     * OMContainer#getFirstOMChild()}, this method will never modify the state of the underlying
     * parser.
     *
```

### JavadocReference
Cannot resolve symbol `coreGetElements(Axis, Class,
* ElementMatcher, String, String, ExceptionTranslator, Semantics)`
in `mixins/core-mixins/src/main/java/org/apache/axiom/core/ElementMatcher.java`
#### Snippet
```java
    /**
     * Check if the given element matches. The values of the <code>namespaceURI</code> and <code>
     * name</code> parameters are those passed to {@link CoreParentNode#coreGetElements(Axis, Class,
     * ElementMatcher, String, String, ExceptionTranslator, Semantics)}.
     *
```

### JavadocReference
Cannot resolve symbol `ExceptionTranslator`
in `mixins/core-mixins/src/main/java/org/apache/axiom/core/ElementMatcher.java`
#### Snippet
```java
     * Check if the given element matches. The values of the <code>namespaceURI</code> and <code>
     * name</code> parameters are those passed to {@link CoreParentNode#coreGetElements(Axis, Class,
     * ElementMatcher, String, String, ExceptionTranslator, Semantics)}.
     *
     * @param element the element to check
```

### JavadocReference
Cannot resolve symbol `elementName`
in `mixins/core-mixins/src/main/java/org/apache/axiom/core/impl/builder/Model.java`
#### Snippet
```java
     *
     * @param parent the parent for the element
     * @param elementName the local name for the element
     * @return the element node, or {@code null} if a plain {@link CoreNSAwareElement} should be
     *     used
```

### JavadocReference
Cannot resolve symbol `target`
in `mixins/core-mixins/src/main/java/org/apache/axiom/core/impl/builder/BuilderHandler.java`
#### Snippet
```java

    /**
     * Tracks the depth of the node identified by {@link #target}. By definition, the document has
     * depth 0. Note that if caching is disabled, then this depth may be different from the actual
     * depth reached by the underlying parser.
```

### JavadocReference
Symbol `SizeAwareDataSource` is inaccessible from here
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/Attachments.java`
#### Snippet
```java
     * instance MAY implement {@link DataHandlerExt} in which case the caller can use that API to
     * stream the content of the part. In addition, the {@link DataSource} linked to the returned
     * {@link DataHandler} MAY be of type {@link SizeAwareDataSource} in which case the caller can
     * use that interface to determine the size of the MIME part.
     * 
```

### JavadocReference
Cannot resolve symbol `getAttachmentCacheMonitor`
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/AttachmentCacheMonitor.java`
#### Snippet
```java
     * Constructor
     * Intentionally private.  Callers should use getAttachmentCacheMonitor
     * @see getAttachmentCacheMonitor
     */
    private AttachmentCacheMonitor() {
```

### JavadocReference
Cannot resolve symbol `org.apache.axiom.blob.BlobDataSource`
in `axiom-compat/src/main/java/org/apache/axiom/util/blob/BlobDataSource.java`
#### Snippet
```java
 * Data source backed by a {@link Blob}.
 * 
 * @deprecated Use {@link org.apache.axiom.blob.BlobDataSource} instead.
 */
public class BlobDataSource implements SizeAwareDataSource {
```

### JavadocReference
Cannot resolve symbol `XOPDecodingStreamReader`
in `axiom-compat/src/main/java/org/apache/axiom/util/stax/xop/MimePartProvider.java`
#### Snippet
```java

/**
 * Interface used by {@link XOPDecodingStreamReader} to load MIME parts referenced by
 * {@code xop:Include} elements.
 */
```

### JavadocReference
Cannot resolve symbol `XOPUtils`
in `axiom-compat/src/main/java/org/apache/axiom/util/stax/xop/XOPEncodedStream.java`
#### Snippet
```java
 * representing the main part and a {@link MimePartProvider} giving access to
 * the attachments. Instances of this class can be obtained from
 * {@link XOPUtils#getXOPEncodedStream(XMLStreamReader)}.
 */
public class XOPEncodedStream {
```

### JavadocReference
Cannot resolve symbol `getXOPEncodedStream(XMLStreamReader)`
in `axiom-compat/src/main/java/org/apache/axiom/util/stax/xop/XOPEncodedStream.java`
#### Snippet
```java
 * representing the main part and a {@link MimePartProvider} giving access to
 * the attachments. Instances of this class can be obtained from
 * {@link XOPUtils#getXOPEncodedStream(XMLStreamReader)}.
 */
public class XOPEncodedStream {
```

### JavadocReference
Cannot resolve symbol `XOPEncodingStreamReader`
in `axiom-compat/src/main/java/org/apache/axiom/util/stax/xop/ContentIDGenerator.java`
#### Snippet
```java
/**
 * Content ID generator interface. Implementations of this interface are used by
 * {@link XOPEncodingStreamReader} to generate content IDs for use in {@code xop:Include}
 * elements.
 */
```

### JavadocReference
Cannot resolve symbol `org.apache.axiom.ext.stax.datahandler.DataHandlerReader`
in `axiom-compat/src/main/java/org/apache/axiom/util/stax/xop/ContentIDGenerator.java`
#### Snippet
```java
     *            An existing content ID for the {@link javax.activation.DataHandler} being
     *            processed, as returned by
     *            {@link org.apache.axiom.ext.stax.datahandler.DataHandlerReader#getContentID()},
     *            or <code>null</code> if no existing content ID is known. The implementation is
     *            free to use this information or not.
```

### JavadocReference
Cannot resolve symbol `getContentID()`
in `axiom-compat/src/main/java/org/apache/axiom/util/stax/xop/ContentIDGenerator.java`
#### Snippet
```java
     *            An existing content ID for the {@link javax.activation.DataHandler} being
     *            processed, as returned by
     *            {@link org.apache.axiom.ext.stax.datahandler.DataHandlerReader#getContentID()},
     *            or <code>null</code> if no existing content ID is known. The implementation is
     *            free to use this information or not.
```

### JavadocReference
Cannot resolve symbol `XOPDecodingStreamReader`
in `axiom-compat/src/main/java/org/apache/axiom/util/stax/xop/XOPConstants.java`
#### Snippet
```java

/**
 * Interface defining constants used by {@link XOPDecodingStreamReader} and
 * {@link XOPEncodingStreamReader}.
 * <p>
```

### JavadocReference
Cannot resolve symbol `XOPEncodingStreamReader`
in `axiom-compat/src/main/java/org/apache/axiom/util/stax/xop/XOPConstants.java`
#### Snippet
```java
/**
 * Interface defining constants used by {@link XOPDecodingStreamReader} and
 * {@link XOPEncodingStreamReader}.
 * <p>
 * For internal use only.
```

### JavadocReference
Symbol `org.apache.axiom.ext.activation.SizeAwareDataSource` is inaccessible from here
in `axiom-compat/src/main/java/org/apache/axiom/attachments/SizeAwareDataSource.java`
#### Snippet
```java

/**
 * @deprecated Please implement {@link org.apache.axiom.ext.activation.SizeAwareDataSource}
 */
public interface SizeAwareDataSource extends org.apache.axiom.ext.activation.SizeAwareDataSource {
```

### JavadocReference
Cannot resolve symbol `createStAXOMBuilder(XMLStreamReader)`
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/builder/TestCreateStAXOMBuilderNamespaceRepairing2.java`
#### Snippet
```java

/**
 * Tests that {@link OMMetaFactory#createStAXOMBuilder(XMLStreamReader)} correctly performs
 * namespace repairing in cases where a namespace declaration of the form {@code xmlns=""} is
 * required. This is a regression test for <a
```

### JavadocReference
Cannot resolve symbol `createStAXOMBuilder(XMLStreamReader)`
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/builder/TestCreateStAXOMBuilderNamespaceRepairing.java`
#### Snippet
```java

/**
 * Tests that {@link OMMetaFactory#createStAXOMBuilder(XMLStreamReader)} performs namespace
 * repairing.
 */
```

### JavadocReference
Cannot resolve symbol `registerCustomBuilderForPayload(CustomBuilder)`
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/builder/TestRegisterCustomBuilderForPayloadAfterSOAPFaultCheck.java`
#### Snippet
```java
/**
 * Tests that a custom builder registered with {@link
 * CustomBuilderSupport#registerCustomBuilderForPayload(CustomBuilder)} is still taken into account
 * after using {@link SOAPBody#hasFault()}. This assumes that the Axiom implementation supports the
 * optimization described by <a
```

### JavadocReference
Cannot resolve symbol `factory`
in `mixins/om-mixins/src/main/java/org/apache/axiom/om/impl/mixin/AxiomSourcedElementMixin.java`
#### Snippet
```java
     * @param localName
     * @param ns
     * @param factory
     * @param source
     */
```

### JavadocReference
Cannot resolve symbol `factory`
in `mixins/om-mixins/src/main/java/org/apache/axiom/om/impl/mixin/AxiomSourcedElementMixin.java`
#### Snippet
```java
     *
     * @param qName
     * @param factory
     * @param source
     */
```

### JavadocReference
Cannot resolve symbol `OMElementImpl`
in `mixins/om-mixins/src/main/java/org/apache/axiom/om/impl/mixin/AxiomSourcedElementMixin.java`
#### Snippet
```java
 * creating a parser from the data source.
 *
 * <p>Whenever methods are added to the base {@link OMElementImpl} class the corresponding methods
 * must be added to this class (there's a unit test to verify that this has been done, just to make
 * sure nothing gets accidentally broken). If the method only requires the element name and/or
```

### JavadocReference
Symbol `javax.activation.DataHandler` is inaccessible from here
in `mixins/om-mixins/src/main/java/org/apache/axiom/om/impl/stream/xop/ContentIDGenerator.java`
#### Snippet
```java
     * Generate a content ID.
     *
     * @param existingContentID An existing content ID for the {@link javax.activation.DataHandler}
     *     being processed, as returned by {@link
     *     org.apache.axiom.ext.stax.BlobReader#getContentID()}, or <code>null
```

### JavadocReference
Cannot resolve symbol `DataSource`
in `axiom-jakarta-activation/src/main/java/org/apache/axiom/mime/activation/PartDataSource.java`
#### Snippet
```java
import org.apache.axiom.mime.Part;

/** Default {@link DataSource} implementation for MIME parts. */
final class PartDataSource implements SizeAwareDataSource {
    private final Part part;
```

### JavadocReference
Cannot resolve symbol `Part`
in `axiom-jakarta-activation/src/main/java/org/apache/axiom/mime/activation/Util.java`
#### Snippet
```java
     * given part.
     *
     * @param Part the part
     * @return the content type
     */
```

### JavadocReference
Cannot resolve symbol `MatrixTestCase`
in `testing/jaxp-testsuite/src/main/java/org/apache/axiom/ts/jaxp/xslt/XSLTImplementation.java`
#### Snippet
```java
import org.xml.sax.ext.LexicalHandler;

/** Specifies an XSLT implementation for use in a {@link MatrixTestCase}. */
public abstract class XSLTImplementation extends Multiton {
    public static final XSLTImplementation JRE =
```

### JavadocReference
Symbol `org.apache.axiom.core.CoreCharacterDataNode` is inaccessible from here
in `components/core-streams/src/main/java/org/apache/axiom/core/stream/CharacterData.java`
#### Snippet
```java
// TODO: clean up this Javadoc
/**
 * Represents character data stored by a {@link org.apache.axiom.core.CoreCharacterDataNode}
 * instance. The content of a {@link CoreCharacterDataNode} is either a {@link String} object or an
 * instance of this interface.
```

### JavadocReference
Cannot resolve symbol `CoreCharacterDataNode`
in `components/core-streams/src/main/java/org/apache/axiom/core/stream/CharacterData.java`
#### Snippet
```java
/**
 * Represents character data stored by a {@link org.apache.axiom.core.CoreCharacterDataNode}
 * instance. The content of a {@link CoreCharacterDataNode} is either a {@link String} object or an
 * instance of this interface.
 */
```

### JavadocReference
Cannot resolve symbol `processAttribute(String, String, String, String, String)`
in `components/core-streams/src/main/java/org/apache/axiom/core/stream/XmlHandler.java`
#### Snippet
```java
     * Prepare to write an element start tag. A call to this method will be followed by zero or more
     * calls to {@link #processNamespaceDeclaration(String, String)} and {@link
     * #processAttribute(String, String, String, String, String)} and a single call to {@link
     * #attributesCompleted()}.
     *
```

### JavadocReference
Cannot resolve symbol `XOPDecodingFilterHandler`
in `components/core-streams/src/main/java/org/apache/axiom/core/stream/xop/XOPConstants.java`
#### Snippet
```java

/**
 * Interface defining constants used by {@link XOPDecodingFilterHandler} and {@link
 * XOPEncodingFilterHandler}.
 */
```

### JavadocReference
Cannot resolve symbol `XOPEncodingFilterHandler`
in `components/core-streams/src/main/java/org/apache/axiom/core/stream/xop/XOPConstants.java`
#### Snippet
```java
/**
 * Interface defining constants used by {@link XOPDecodingFilterHandler} and {@link
 * XOPEncodingFilterHandler}.
 */
interface XOPConstants {
```

### JavadocReference
Cannot resolve symbol `ToXMLStream`
in `components/core-streams/src/main/java/org/apache/axiom/core/stream/serializer/SerializerWriter.java`
#### Snippet
```java

/**
 * {@link Writer} that writes character data as {@link ToXMLStream#characters(char[], int, int)} to
 * a {@link ToXMLStream}.
 */
```

### JavadocReference
Cannot resolve symbol `characters(char[], int, int)`
in `components/core-streams/src/main/java/org/apache/axiom/core/stream/serializer/SerializerWriter.java`
#### Snippet
```java

/**
 * {@link Writer} that writes character data as {@link ToXMLStream#characters(char[], int, int)} to
 * a {@link ToXMLStream}.
 */
```

### JavadocReference
Cannot resolve symbol `ToXMLStream`
in `components/core-streams/src/main/java/org/apache/axiom/core/stream/serializer/SerializerWriter.java`
#### Snippet
```java
/**
 * {@link Writer} that writes character data as {@link ToXMLStream#characters(char[], int, int)} to
 * a {@link ToXMLStream}.
 */
final class SerializerWriter extends Writer {
```

### JavadocReference
Cannot resolve symbol `ignorableWhitespace`
in `components/core-streams/src/main/java/org/apache/axiom/core/stream/serializer/Serializer.java`
#### Snippet
```java
     * @param length The number of characters to read from the array.
     * @throws StreamException Any SAX exception, possibly wrapping another exception.
     * @see #ignorableWhitespace
     * @see org.xml.sax.Locator
     * @throws StreamException
```

## RuleId[id=ManualArrayToCollectionCopy]
### ManualArrayToCollectionCopy
Manual array to collection copy
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/header/MyRolePlayer.java`
#### Snippet
```java
        this.ultimateReceiver = ultimateReceiver;
        this.roles = new ArrayList<>();
        for (int i = 0; i < roles.length; i++) {
            this.roles.add(roles[i]);
        }
```

## RuleId[id=UnnecessarySemicolon]
### UnnecessarySemicolon
Unnecessary semicolon `;`
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/SOAPTestCase.java`
#### Snippet
```java
    protected SOAPHeaderBlock createSOAPHeaderBlock() {
        OMNamespace namespace = soapFactory.createOMNamespace("http://www.example.org", "test");
        ;
        SOAPEnvelope soapEnvelope = soapFactory.createSOAPEnvelope();
        SOAPHeader soapHeader = soapFactory.createSOAPHeader(soapEnvelope);
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/builder/TestDTD.java`
#### Snippet
```java
        String message = "<!DOCTYPE test []>" + soapFactory.getDefaultEnvelope();
        XMLStreamReader parser = StAXUtils.createXMLStreamReader(new StringReader(message));
        ;
        try {
            SOAPModelBuilder builder =
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `components/core-streams/src/main/java/org/apache/axiom/core/stream/sax/output/ContentHandlerXmlHandler.java`
#### Snippet
```java
        SKIP,
        ACCUMULATE
    };

    private final ContentHandler contentHandler;
```

## RuleId[id=DataFlowIssue]
### DataFlowIssue
Method invocation `toString` may produce `NullPointerException`
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/documenttype/TestWithParser1.java`
#### Snippet
```java
        Document document =
                dbf.newDocumentBuilder()
                        .parse(TestWithParser1.class.getResource("test1.xml").toString());
        DocumentType doctype = document.getDoctype();
        assertEquals("root", doctype.getName());
```

### DataFlowIssue
Method invocation `toString` may produce `NullPointerException`
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/documenttype/TestWithParser2.java`
#### Snippet
```java
        Document document =
                dbf.newDocumentBuilder()
                        .parse(TestWithParser2.class.getResource("test2.xml").toString());
        DocumentType doctype = document.getDoctype();
        assertEquals("root", doctype.getName());
```

### DataFlowIssue
Result of 'max' is the same as the first argument making the call meaningless
in `components/blob/src/main/java/org/apache/axiom/blob/Blobs.java`
#### Snippet
```java
    public static OverflowableBlob createOverflowableBlob(
            int threshold, WritableBlobFactory<?> overflowBlobFactory) {
        int numberOfChunks = Math.max(16, Math.min(1, threshold / 4096));
        int chunkSize = threshold / numberOfChunks;
        return new OverflowableBlobImpl(numberOfChunks, chunkSize, overflowBlobFactory);
```

### DataFlowIssue
Argument `classLoader` might be null
in `axiom-api/src/main/java/org/apache/axiom/om/util/StAXUtils.java`
#### Snippet
```java
                    // XMLInputFactory.properties so that this setting can be overridden.
                    factory.setProperty(XMLInputFactory.IS_COALESCING, Boolean.TRUE);
                    Map<String,Object> props = loadFactoryProperties(classLoader, "XMLInputFactory.properties");
                    if (props != null) {
                        for (Map.Entry<String,Object> entry : props.entrySet()) {
```

### DataFlowIssue
Argument `classLoader` might be null
in `axiom-api/src/main/java/org/apache/axiom/om/util/StAXUtils.java`
#### Snippet
```java
                    factory.setProperty(XMLOutputFactory.IS_REPAIRING_NAMESPACES, 
                                        Boolean.FALSE);
                    Map<String,Object> props = loadFactoryProperties(classLoader, "XMLOutputFactory.properties");
                    if (props != null) {
                        for (Map.Entry<String,Object> entry : props.entrySet()) {
```

### DataFlowIssue
Method invocation `equals` may produce `NullPointerException`
in `axiom-api/src/main/java/org/apache/axiom/util/stax/WrappedTextNodeStreamReader.java`
#### Snippet
```java
        if (type != eventType
             || (namespaceURI != null && !namespaceURI.equals(getNamespaceURI()))
             || (localName != null && !namespaceURI.equals(getLocalName()))) {
            throw new XMLStreamException("Unexpected event type");
        }
```

### DataFlowIssue
Method invocation `load` may produce `NullPointerException`
in `axiom-api/src/main/java/org/apache/axiom/locator/ImplementationFactory.java`
#### Snippet
```java
            return null;
        }
        OMMetaFactory metaFactory = ((OMMetaFactoryLoader)load(loader, loaderClassName)).load(null);
        if (metaFactory == null) {
            return null;
```

### DataFlowIssue
Argument `cl.getResourceAsStream( pkg.replace('.', '/') + "/no...` might be null
in `mixins/core-mixins/src/main/java/org/apache/axiom/core/NodeFactoryImpl.java`
#### Snippet
```java
                        new BufferedReader(
                                new InputStreamReader(
                                        cl.getResourceAsStream(
                                                pkg.replace('.', '/') + "/nodetypes.index"),
                                        StandardCharsets.UTF_8));
                try {
```

### DataFlowIssue
Casting `node` to `CoreChildNode` may produce `ClassCastException`
in `mixins/core-mixins/src/main/java/org/apache/axiom/core/impl/AbstractNodeIterator.java`
#### Snippet
```java
                            node = startNode.coreGetFirstChild();
                        } else {
                            node = ((CoreChildNode) node).coreGetNextSibling();
                        }
                        break;
```

### DataFlowIssue
Casting `node` to `CoreChildNode` may produce `ClassCastException`
in `mixins/core-mixins/src/main/java/org/apache/axiom/core/impl/AbstractNodeIterator.java`
#### Snippet
```java
                                }
                                CoreChildNode nextSibling =
                                        ((CoreChildNode) node).coreGetNextSibling();
                                if (nextSibling != null) {
                                    node = nextSibling;
```

### DataFlowIssue
Method invocation `internalGetContent` may produce `NullPointerException`
in `mixins/core-mixins/src/main/java/org/apache/axiom/core/impl/mixin/CoreChildNodeMixin.java`
#### Snippet
```java
        fragmentContent.lastChild.internalSetNextSibling(this);
        if (previousSibling == null) {
            parent.internalGetContent(true).firstChild = fragmentContent.firstChild;
        } else {
            previousSibling.internalSetNextSibling(fragmentContent.firstChild);
```

### DataFlowIssue
Method invocation `isDirectory` may produce `NullPointerException`
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/lifecycle/impl/LifecycleManagerImpl.java`
#### Snippet
```java
            }
        }
        if (!dir.isDirectory()) {
            throw new IllegalArgumentException("Given Attachment File Cache Location "
                + dir + " should be a directory.");
```

### DataFlowIssue
Method invocation `toString` may produce `NullPointerException`
in `testing/soap-testsuite/src/main/java/org/apache/axiom/ts/soap/SOAPSpec.java`
#### Snippet
```java
                sources[i] =
                        new StreamSource(
                                SOAPSpec.class.getResource("xsd/" + schemaResources[i]).toString());
            }
            try {
```

### DataFlowIssue
Method invocation `getTextContent` may produce `NullPointerException`
in `testing/soap-testsuite/src/main/java/org/apache/axiom/ts/soap/ConvertedSOAPSampleContent.java`
#### Snippet
```java
            element.setTextContent(
                    transform(
                            value.getTextContent(),
                            new TextTransformer() {
                                @Override
```

### DataFlowIssue
Method invocation `getTextContent` may produce `NullPointerException`
in `testing/soap-testsuite/src/main/java/org/apache/axiom/ts/soap/ConvertedSOAPSampleContent.java`
#### Snippet
```java
        } else if (type == SOAPFaultChild.REASON) {
            Element text = getChild(element, SOAPFaultChild.TEXT);
            element.setTextContent(text.getTextContent());
        } else {
            SOAPElementType[] childTypes = type.getChildTypes();
```

### DataFlowIssue
Method invocation `close` may produce `NullPointerException`
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/document/TestDigest.java`
#### Snippet
```java
            return document;
        } finally {
            in.close();
        }
    }
```

### DataFlowIssue
Argument `value` might be null
in `mixins/om-mixins/src/main/java/org/apache/axiom/om/impl/intf/TextContent.java`
#### Snippet
```java
        } else {
            // TODO: there must be a better way to just write a String
            sink.getWriter().write(value);
        }
    }
```

### DataFlowIssue
Method invocation `toCharArray` may produce `NullPointerException`
in `mixins/om-mixins/src/main/java/org/apache/axiom/om/impl/intf/TextContent.java`
#### Snippet
```java
            }
        } else {
            return value.toCharArray();
        }
    }
```

### DataFlowIssue
Argument `value` might be null
in `mixins/om-mixins/src/main/java/org/apache/axiom/om/impl/intf/TextContent.java`
#### Snippet
```java
            return blobObject;
        }
        return Blobs.createBlob(Base64Utils.decode(value));
    }

```

### DataFlowIssue
Method invocation `setContentID` may produce `NullPointerException`
in `mixins/om-mixins/src/main/java/org/apache/axiom/om/impl/mixin/AxiomTextMixin.java`
#### Snippet
```java
    @Override
    public final void setContentID(String cid) {
        getTextContent(true).setContentID(cid);
    }
}
```

### DataFlowIssue
Method invocation `getContentID` may produce `NullPointerException`
in `mixins/om-mixins/src/main/java/org/apache/axiom/om/impl/mixin/AxiomTextMixin.java`
#### Snippet
```java
    @Override
    public final String getContentID() {
        return getTextContent(true).getContentID();
    }

```

### DataFlowIssue
Method invocation `writePart` may produce `NullPointerException`
in `mixins/om-mixins/src/main/java/org/apache/axiom/om/impl/mixin/AxiomContainerMixin.java`
#### Snippet
```java
                Blob blob = encoder.getBlob(contentID);
                if (cache || !(blob instanceof PartBlob)) {
                    multipartWriter.writePart(blob, contentID);
                } else {
                    multipartWriter.writePart((PartBlob) blob, contentID, false);
```

### DataFlowIssue
Method invocation `writePart` may produce `NullPointerException`
in `mixins/om-mixins/src/main/java/org/apache/axiom/om/impl/mixin/AxiomContainerMixin.java`
#### Snippet
```java
                    multipartWriter.writePart(blob, contentID);
                } else {
                    multipartWriter.writePart((PartBlob) blob, contentID, false);
                }
            }
```

### DataFlowIssue
Method invocation `complete` may produce `NullPointerException`
in `mixins/om-mixins/src/main/java/org/apache/axiom/om/impl/mixin/AxiomContainerMixin.java`
#### Snippet
```java
                }
            }
            multipartWriter.complete();
        }
    }
```

### DataFlowIssue
Argument `StAXImplementation.class.getResourceAsStream("filelist")` might be null
in `testing/jaxp-testsuite/src/main/java/org/apache/axiom/ts/jaxp/stax/StAXImplementation.java`
#### Snippet
```java
                new BufferedReader(
                        new InputStreamReader(
                                StAXImplementation.class.getResourceAsStream("filelist")))) {
            String jarName;
            while ((jarName = in.readLine()) != null) {
```

### DataFlowIssue
Argument `in` might be null
in `testing/jaxp-testsuite/src/main/java/org/apache/axiom/ts/jaxp/stax/StAXImplementation.java`
#### Snippet
```java
        try (InputStream in = StAXImplementation.class.getResourceAsStream(jarName);
                FileOutputStream out = new FileOutputStream(jar)) {
            IOUtils.copy(in, out);
        }
        return jar;
```

### DataFlowIssue
Argument `XMLSample.class.getResourceAsStream("bulk/filelist")` might be null
in `testing/xml-testsuite/src/main/java/org/apache/axiom/ts/xml/XMLSample.java`
#### Snippet
```java
                new BufferedReader(
                        new InputStreamReader(
                                XMLSample.class.getResourceAsStream("bulk/filelist")));
        List<XMLSample> result = new ArrayList<>(10);
        String name;
```

### DataFlowIssue
Dereference of `parentToKeep` may produce `NullPointerException`
in `axiom-weaver/src/main/java/org/apache/axiom/weaver/ImplementationNode.java`
#### Snippet
```java
            mixins.addAll(parent.transitiveMixins.asSet());
        }
        mixins.removeAll(parentToKeep.transitiveMixins.asSet());
    }

```

### DataFlowIssue
Method invocation `close` may produce `NullPointerException`
in `systests/psychopath-tests/src/test/java/PsychoPathTest.java`
#### Snippet
```java
            return expr.evaluate(new DynamicContextBuilder(scb), new Object[] { doc });
        } finally {
            is.close();
        }
    }
```

## RuleId[id=MissingSerialAnnotation]
### MissingSerialAnnotation
`serialVersionUID` can be annotated with '@Serial' annotation
in `testing/spring-ws-testsuite/src/main/java/org/apache/axiom/ts/springws/scenario/broker/UnknownCustomerException.java`
#### Snippet
```java

public class UnknownCustomerException extends Exception {
    private static final long serialVersionUID = 1L;

    public UnknownCustomerException(String message) {
```

## RuleId[id=StringOperationCanBeSimplified]
### StringOperationCanBeSimplified
Unnecessary string length argument
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/text/TestSplitText.java`
#### Snippet
```java
        assertEquals(
                "Incorrect split point",
                textValue.substring(3, textValue.length()),
                txt.getNextSibling().getNodeValue());
    }
```

### StringOperationCanBeSimplified
Call to `substring()` is redundant
in `axiom-api/src/main/java/org/apache/axiom/util/stax/dialect/BEAStreamReaderWrapper.java`
#### Snippet
```java
                    if (buffer == null) {
                        buffer = new StringBuffer(len-1);
                        buffer.append(text.substring(0, i));
                    }
                } else {
```

### StringOperationCanBeSimplified
Call to `substring()` is redundant
in `axiom-compat/src/main/java/org/apache/axiom/om/util/CommonUtils.java`
#### Snippet
```java

            while (i >= 0) {
                sb.append(name.substring(start, i));
                sb.append(newT);
                start = i + len;
```

### StringOperationCanBeSimplified
Call to `toString()` is redundant
in `axiom-compat/src/main/java/org/apache/axiom/om/util/UUIDGenerator.java`
#### Snippet
```java
        if (begin < 0) begin = begin * -1;
        begin = begin % 8;
        return sb2.toString().substring(begin, begin + 18).toUpperCase();
    }

```

### StringOperationCanBeSimplified
`new String()` is redundant
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestGetXMLStreamReaderWithNamespaceURIInterning.java`
#### Snippet
```java
        OMFactory factory = metaFactory.getOMFactory();
        // Use "new String" to create String objects that are not interned
        OMNamespace ns1 = factory.createOMNamespace(new String("urn:ns1"), "p");
        OMNamespace ns2 = factory.createOMNamespace(new String("urn:ns2"), "q");
        OMElement root = factory.createOMElement("root", ns1);
```

### StringOperationCanBeSimplified
`new String()` is redundant
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestGetXMLStreamReaderWithNamespaceURIInterning.java`
#### Snippet
```java
        // Use "new String" to create String objects that are not interned
        OMNamespace ns1 = factory.createOMNamespace(new String("urn:ns1"), "p");
        OMNamespace ns2 = factory.createOMNamespace(new String("urn:ns2"), "q");
        OMElement root = factory.createOMElement("root", ns1);
        root.addAttribute("attr", "value", ns2);
```

### StringOperationCanBeSimplified
Inefficient conversion from ByteArrayOutputStream
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/document/TestSerializeWithIgnoreXMLDeclaration.java`
#### Snippet
```java
        document.serializeAndConsume(baos, format);

        String xmlDocument = new String(baos.toByteArray(), StandardCharsets.UTF_8);

        assertThat(xmlDocument).doesNotContain("<?xml");
```

### StringOperationCanBeSimplified
Inefficient conversion from ByteArrayOutputStream
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/document/TestSerializeXML11.java`
#### Snippet
```java
        document.serializeAndConsume(baos);

        String xmlDocument = new String(baos.toByteArray(), StandardCharsets.UTF_8);
        assertThat(xmlDocument).startsWith("<?xml version=\"1.1\"");
    }
```

### StringOperationCanBeSimplified
Inefficient conversion from ByteArrayOutputStream
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/document/TestSerializeXMLDeclaration.java`
#### Snippet
```java
        document.serializeAndConsume(baos);

        String xmlDocument = new String(baos.toByteArray(), StandardCharsets.UTF_8);

        assertThat(xmlDocument).startsWith("<?xml version=\"1.0\" encoding=\"utf-8\"?>");
```

### StringOperationCanBeSimplified
Inefficient conversion from ByteArrayOutputStream
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/document/TestSerializeUTF16.java`
#### Snippet
```java
        document.serializeAndConsume(baos, format);

        String xmlDocument = new String(baos.toByteArray(), StandardCharsets.UTF_16);
        assertThat(xmlDocument).startsWith("<?xml version=\"1.0\" encoding=\"UTF-16\"?>");
    }
```

### StringOperationCanBeSimplified
`new String()` is redundant
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/namespace/TestObjectEquals.java`
#### Snippet
```java
        // The implementation must not assume that namespace URI or prefixes are interned.
        // Therefore we use new String(String).
        OMNamespace ns1 = factory.createOMNamespace(new String("urn:ns"), new String("ns"));
        OMNamespace ns2 = factory.createOMNamespace(new String("urn:ns"), new String("ns"));
        assertTrue(ns1.equals(ns2));
```

### StringOperationCanBeSimplified
`new String()` is redundant
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/namespace/TestObjectEquals.java`
#### Snippet
```java
        // The implementation must not assume that namespace URI or prefixes are interned.
        // Therefore we use new String(String).
        OMNamespace ns1 = factory.createOMNamespace(new String("urn:ns"), new String("ns"));
        OMNamespace ns2 = factory.createOMNamespace(new String("urn:ns"), new String("ns"));
        assertTrue(ns1.equals(ns2));
```

### StringOperationCanBeSimplified
`new String()` is redundant
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/namespace/TestObjectEquals.java`
#### Snippet
```java
        // Therefore we use new String(String).
        OMNamespace ns1 = factory.createOMNamespace(new String("urn:ns"), new String("ns"));
        OMNamespace ns2 = factory.createOMNamespace(new String("urn:ns"), new String("ns"));
        assertTrue(ns1.equals(ns2));
    }
```

### StringOperationCanBeSimplified
`new String()` is redundant
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/namespace/TestObjectEquals.java`
#### Snippet
```java
        // Therefore we use new String(String).
        OMNamespace ns1 = factory.createOMNamespace(new String("urn:ns"), new String("ns"));
        OMNamespace ns2 = factory.createOMNamespace(new String("urn:ns"), new String("ns"));
        assertTrue(ns1.equals(ns2));
    }
```

### StringOperationCanBeSimplified
`new String()` is redundant
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/namespace/TestEquals.java`
#### Snippet
```java
        // The implementation must not assume that namespace URI or prefixes are interned
        assertTrue(
                ns.equals(new String("http://www.w3.org/XML/1998/namespace"), new String("xml")));
        assertFalse(ns.equals("http://www.w3.org/XML/1998/namespace", "xml2"));
        assertFalse(ns.equals("http://www.w3.org/XML/1998/namespace", null));
```

### StringOperationCanBeSimplified
`new String()` is redundant
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/namespace/TestEquals.java`
#### Snippet
```java
        // The implementation must not assume that namespace URI or prefixes are interned
        assertTrue(
                ns.equals(new String("http://www.w3.org/XML/1998/namespace"), new String("xml")));
        assertFalse(ns.equals("http://www.w3.org/XML/1998/namespace", "xml2"));
        assertFalse(ns.equals("http://www.w3.org/XML/1998/namespace", null));
```

### StringOperationCanBeSimplified
Inefficient conversion from ByteArrayOutputStream
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestSerializeModifiedOMSEWithNonDestructiveDataSource.java`
#### Snippet
```java
        ByteArrayOutputStream baos = new ByteArrayOutputStream();
        element.serialize(baos);
        assertTrue(new String(baos.toByteArray(), StandardCharsets.UTF_8).indexOf("TEST") != -1);

        assertTrue(element.toString().indexOf("TEST") != -1);
```

## RuleId[id=DeprecatedIsStillUsed]
### DeprecatedIsStillUsed
Deprecated member 'ByteStreamComparator' is still used
in `testing/testutils/src/main/java/org/apache/axiom/testutils/io/ByteStreamComparator.java`
#### Snippet
```java

    @Deprecated
    public ByteStreamComparator(InputStream in) {
        this(in, "s1", "s2");
    }
```

### DeprecatedIsStillUsed
Deprecated member 'CharacterStreamComparator' is still used
in `testing/testutils/src/main/java/org/apache/axiom/testutils/io/CharacterStreamComparator.java`
#### Snippet
```java

    @Deprecated
    public CharacterStreamComparator(Reader in) {
        this(in, "s1", "s2");
    }
```

### DeprecatedIsStillUsed
Deprecated member 'setDataSource' is still used
in `axiom-api/src/main/java/org/apache/axiom/om/OMSourcedElement.java`
#### Snippet
```java
     *             result of an invocation of this method.
     */
    OMDataSource setDataSource(OMDataSource dataSource);
    
    /**
```

### DeprecatedIsStillUsed
Deprecated member 'StAXWriterConfiguration' is still used
in `axiom-api/src/main/java/org/apache/axiom/om/util/StAXWriterConfiguration.java`
#### Snippet
```java
 * @deprecated
 */
public interface StAXWriterConfiguration {
    /**
     * The default configuration.
```

### DeprecatedIsStillUsed
Deprecated member 'getXMLOutputFactory' is still used
in `axiom-api/src/main/java/org/apache/axiom/om/util/StAXUtils.java`
#### Snippet
```java
     * @deprecated
     */
    public static XMLOutputFactory getXMLOutputFactory() {
        return getXMLOutputFactory(null);
    }
```

### DeprecatedIsStillUsed
Deprecated member 'getHeaderBlocksWithNSURI' is still used
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAPHeader.java`
#### Snippet
```java
     * @deprecated Use {@link #getHeaderBlocksWithNamespaceURI(String)} instead.
     */
    ArrayList<SOAPHeaderBlock> getHeaderBlocksWithNSURI(String nsURI);

    /**
```

### DeprecatedIsStillUsed
Deprecated member 'USE_CTE_BASE64_FOR_NON_TEXTUAL_ATTACHMENTS' is still used
in `axiom-api/src/main/java/org/apache/axiom/om/OMOutputFormat.java`
#### Snippet
```java
     * @deprecated Use {@link ContentTransferEncodingPolicy#USE_BASE64_FOR_NON_TEXTUAL_PARTS} instead.
     */
    public static final String USE_CTE_BASE64_FOR_NON_TEXTUAL_ATTACHMENTS = 
        "org.apache.axiom.om.OMFormat.use.cteBase64.forNonTextualAttachments";
    
```

### DeprecatedIsStillUsed
Deprecated member 'AXIOMUtil' is still used
in `axiom-compat/src/main/java/org/apache/axiom/om/impl/llom/util/AXIOMUtil.java`
#### Snippet
```java
 * @deprecated Use {@link org.apache.axiom.om.util.AXIOMUtil} instead.
 */
public class AXIOMUtil extends org.apache.axiom.om.util.AXIOMUtil {
}

```

### DeprecatedIsStillUsed
Deprecated member 'OMResult' is still used
in `axiom-compat/src/main/java/org/apache/axiom/om/impl/jaxp/OMResult.java`
#### Snippet
```java
 *             {@link OMDocument} instance and call {@link OMContainer#getSAXResult()} on it.
 */
public class OMResult extends SAXResult {
    private final OMDocument document;
    
```

### DeprecatedIsStillUsed
Deprecated member 'OMChildrenWithSpecificAttributeIterator' is still used
in `axiom-compat/src/main/java/org/apache/axiom/om/impl/traverse/OMChildrenWithSpecificAttributeIterator.java`
#### Snippet
```java
 * @deprecated
 */
public class OMChildrenWithSpecificAttributeIterator extends OMFilterIterator {
    /** Field attributeName */
    private QName attributeName;
```

### DeprecatedIsStillUsed
Deprecated member 'importOMAttribute' is still used
in `axiom-compat/src/main/java/org/apache/axiom/om/util/AttributeHelper.java`
#### Snippet
```java
     * @deprecated Use {@link OMFactory#importInformationItem(OMInformationItem)} instead.
     */
    public static void importOMAttribute(OMAttribute omAttribute, OMElement omElement) {
        // first check whether the given OMAttribute has the same OMFactory
        if (omAttribute.getOMFactory().getMetaFactory() == omElement.getOMFactory().getMetaFactory()) {
```

### DeprecatedIsStillUsed
Deprecated member 'ContentTypeBuilder' is still used
in `axiom-compat/src/main/java/org/apache/axiom/mime/ContentTypeBuilder.java`
#### Snippet
```java
 * @deprecated Use {@link ContentType.Builder} instead.
 */
public final class ContentTypeBuilder {
    private MediaType mediaType;
    private final LinkedHashMap<String,String> parameters = new LinkedHashMap<String,String>();
```

### DeprecatedIsStillUsed
Deprecated member 'CommonUtils' is still used
in `axiom-compat/src/main/java/org/apache/axiom/om/util/CommonUtils.java`
#### Snippet
```java
 * @deprecated
 */
public class CommonUtils {

    /**
```

### DeprecatedIsStillUsed
Deprecated member 'isTextualPart' is still used
in `axiom-compat/src/main/java/org/apache/axiom/om/util/CommonUtils.java`
#### Snippet
```java
     * @deprecated Use {@link ContentType#isTextual()} instead.
     */
    public static boolean isTextualPart(String contentType) {
        try {
            return new ContentType(contentType).isTextual();
```

### DeprecatedIsStillUsed
Deprecated member 'importOMElement' is still used
in `axiom-compat/src/main/java/org/apache/axiom/om/util/ElementHelper.java`
#### Snippet
```java
     * @deprecated Use {@link OMFactory#importInformationItem(OMInformationItem)} instead.
     */
    public static OMElement importOMElement(OMElement omElement, OMFactory omFactory) {
        // first check whether the given OMElement has the same omFactory
        if (omElement.getOMFactory().getMetaFactory() == omFactory.getMetaFactory()) {
```

### DeprecatedIsStillUsed
Deprecated member 'DataHandlerWrapper' is still used
in `axiom-compat/src/main/java/org/apache/axiom/util/activation/DataHandlerWrapper.java`
#### Snippet
```java
 * @deprecated
 */
public class DataHandlerWrapper extends DataHandler {
    private final DataHandler parent;

```

## RuleId[id=SpringXmlModelInspection]
### SpringXmlModelInspection
Cannot resolve class or package 'springframework'
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/common.xml`
#### Snippet
```java
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.0.xsd">
    <bean class="org.springframework.context.support.PropertySourcesPlaceholderConfigurer"/>
</beans>

```

### SpringXmlModelInspection
Cannot resolve class or package 'context'
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/common.xml`
#### Snippet
```java
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.0.xsd">
    <bean class="org.springframework.context.support.PropertySourcesPlaceholderConfigurer"/>
</beans>

```

### SpringXmlModelInspection
Cannot resolve class or package 'support'
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/common.xml`
#### Snippet
```java
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.0.xsd">
    <bean class="org.springframework.context.support.PropertySourcesPlaceholderConfigurer"/>
</beans>

```

### SpringXmlModelInspection
Cannot resolve class 'PropertySourcesPlaceholderConfigurer'
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/common.xml`
#### Snippet
```java
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.0.xsd">
    <bean class="org.springframework.context.support.PropertySourcesPlaceholderConfigurer"/>
</beans>

```

### SpringXmlModelInspection
Cannot resolve class or package 'springframework'
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/saaj-message-factory.xml`
#### Snippet
```java
         http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.0.xsd
         http://www.springframework.org/schema/util http://www.springframework.org/schema/util/spring-util-4.0.xsd">
    <bean name="messageFactory" class="org.springframework.ws.soap.saaj.SaajSoapMessageFactory">
        <property name="soapVersion">
            <util:constant static-field="org.springframework.ws.soap.SoapVersion.${soapVersion}"/>
```

### SpringXmlModelInspection
Cannot resolve class or package 'ws'
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/saaj-message-factory.xml`
#### Snippet
```java
         http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.0.xsd
         http://www.springframework.org/schema/util http://www.springframework.org/schema/util/spring-util-4.0.xsd">
    <bean name="messageFactory" class="org.springframework.ws.soap.saaj.SaajSoapMessageFactory">
        <property name="soapVersion">
            <util:constant static-field="org.springframework.ws.soap.SoapVersion.${soapVersion}"/>
```

### SpringXmlModelInspection
Cannot resolve class or package 'soap'
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/saaj-message-factory.xml`
#### Snippet
```java
         http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.0.xsd
         http://www.springframework.org/schema/util http://www.springframework.org/schema/util/spring-util-4.0.xsd">
    <bean name="messageFactory" class="org.springframework.ws.soap.saaj.SaajSoapMessageFactory">
        <property name="soapVersion">
            <util:constant static-field="org.springframework.ws.soap.SoapVersion.${soapVersion}"/>
```

### SpringXmlModelInspection
Cannot resolve class or package 'saaj'
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/saaj-message-factory.xml`
#### Snippet
```java
         http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.0.xsd
         http://www.springframework.org/schema/util http://www.springframework.org/schema/util/spring-util-4.0.xsd">
    <bean name="messageFactory" class="org.springframework.ws.soap.saaj.SaajSoapMessageFactory">
        <property name="soapVersion">
            <util:constant static-field="org.springframework.ws.soap.SoapVersion.${soapVersion}"/>
```

### SpringXmlModelInspection
Cannot resolve class 'SaajSoapMessageFactory'
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/saaj-message-factory.xml`
#### Snippet
```java
         http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.0.xsd
         http://www.springframework.org/schema/util http://www.springframework.org/schema/util/spring-util-4.0.xsd">
    <bean name="messageFactory" class="org.springframework.ws.soap.saaj.SaajSoapMessageFactory">
        <property name="soapVersion">
            <util:constant static-field="org.springframework.ws.soap.SoapVersion.${soapVersion}"/>
```

### SpringXmlModelInspection
Cannot resolve property 'soapVersion'
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/saaj-message-factory.xml`
#### Snippet
```java
         http://www.springframework.org/schema/util http://www.springframework.org/schema/util/spring-util-4.0.xsd">
    <bean name="messageFactory" class="org.springframework.ws.soap.saaj.SaajSoapMessageFactory">
        <property name="soapVersion">
            <util:constant static-field="org.springframework.ws.soap.SoapVersion.${soapVersion}"/>
        </property>
```

### SpringXmlModelInspection
Cannot resolve class or package 'springframework'
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/wsadom/client.xml`
#### Snippet
```java
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.0.xsd">
    <bean class="org.springframework.ws.client.core.WebServiceTemplate">
        <constructor-arg ref="messageFactory"/>
        <property name="defaultUri" value="http://localhost:${port}/"/>
```

### SpringXmlModelInspection
Cannot resolve class or package 'ws'
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/wsadom/client.xml`
#### Snippet
```java
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.0.xsd">
    <bean class="org.springframework.ws.client.core.WebServiceTemplate">
        <constructor-arg ref="messageFactory"/>
        <property name="defaultUri" value="http://localhost:${port}/"/>
```

### SpringXmlModelInspection
Cannot resolve class or package 'client'
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/wsadom/client.xml`
#### Snippet
```java
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.0.xsd">
    <bean class="org.springframework.ws.client.core.WebServiceTemplate">
        <constructor-arg ref="messageFactory"/>
        <property name="defaultUri" value="http://localhost:${port}/"/>
```

### SpringXmlModelInspection
Cannot resolve class or package 'core'
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/wsadom/client.xml`
#### Snippet
```java
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.0.xsd">
    <bean class="org.springframework.ws.client.core.WebServiceTemplate">
        <constructor-arg ref="messageFactory"/>
        <property name="defaultUri" value="http://localhost:${port}/"/>
```

### SpringXmlModelInspection
Cannot resolve class 'WebServiceTemplate'
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/wsadom/client.xml`
#### Snippet
```java
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.0.xsd">
    <bean class="org.springframework.ws.client.core.WebServiceTemplate">
        <constructor-arg ref="messageFactory"/>
        <property name="defaultUri" value="http://localhost:${port}/"/>
```

### SpringXmlModelInspection
Cannot resolve bean 'messageFactory'
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/wsadom/client.xml`
#### Snippet
```java
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.0.xsd">
    <bean class="org.springframework.ws.client.core.WebServiceTemplate">
        <constructor-arg ref="messageFactory"/>
        <property name="defaultUri" value="http://localhost:${port}/"/>
    </bean>
```

### SpringXmlModelInspection
Cannot resolve property 'defaultUri'
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/wsadom/client.xml`
#### Snippet
```java
    <bean class="org.springframework.ws.client.core.WebServiceTemplate">
        <constructor-arg ref="messageFactory"/>
        <property name="defaultUri" value="http://localhost:${port}/"/>
    </bean>
</beans>
```

### SpringXmlModelInspection
Cannot resolve class or package 'springframework'
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/jdom/client.xml`
#### Snippet
```java
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.0.xsd">
    <bean class="org.springframework.ws.client.core.WebServiceTemplate">
        <constructor-arg ref="messageFactory"/>
        <property name="defaultUri" value="http://localhost:${port}/"/>
```

### SpringXmlModelInspection
Cannot resolve class or package 'ws'
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/jdom/client.xml`
#### Snippet
```java
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.0.xsd">
    <bean class="org.springframework.ws.client.core.WebServiceTemplate">
        <constructor-arg ref="messageFactory"/>
        <property name="defaultUri" value="http://localhost:${port}/"/>
```

### SpringXmlModelInspection
Cannot resolve class or package 'client'
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/jdom/client.xml`
#### Snippet
```java
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.0.xsd">
    <bean class="org.springframework.ws.client.core.WebServiceTemplate">
        <constructor-arg ref="messageFactory"/>
        <property name="defaultUri" value="http://localhost:${port}/"/>
```

### SpringXmlModelInspection
Cannot resolve class or package 'core'
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/jdom/client.xml`
#### Snippet
```java
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.0.xsd">
    <bean class="org.springframework.ws.client.core.WebServiceTemplate">
        <constructor-arg ref="messageFactory"/>
        <property name="defaultUri" value="http://localhost:${port}/"/>
```

### SpringXmlModelInspection
Cannot resolve class 'WebServiceTemplate'
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/jdom/client.xml`
#### Snippet
```java
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.0.xsd">
    <bean class="org.springframework.ws.client.core.WebServiceTemplate">
        <constructor-arg ref="messageFactory"/>
        <property name="defaultUri" value="http://localhost:${port}/"/>
```

### SpringXmlModelInspection
Cannot resolve bean 'messageFactory'
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/jdom/client.xml`
#### Snippet
```java
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.0.xsd">
    <bean class="org.springframework.ws.client.core.WebServiceTemplate">
        <constructor-arg ref="messageFactory"/>
        <property name="defaultUri" value="http://localhost:${port}/"/>
    </bean>
```

### SpringXmlModelInspection
Cannot resolve property 'defaultUri'
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/jdom/client.xml`
#### Snippet
```java
    <bean class="org.springframework.ws.client.core.WebServiceTemplate">
        <constructor-arg ref="messageFactory"/>
        <property name="defaultUri" value="http://localhost:${port}/"/>
    </bean>
</beans>
```

### SpringXmlModelInspection
Cannot resolve class or package 'springframework'
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/jaxb2/client.xml`
#### Snippet
```java
        <oxm:class-to-be-bound name="org.apache.axiom.ts.springws.scenario.jaxb2.GetQuoteResponse"/>
    </oxm:jaxb2-marshaller>
    <bean class="org.springframework.ws.client.core.WebServiceTemplate">
        <constructor-arg ref="messageFactory"/>
        <property name="marshaller" ref="marshaller"/>
```

### SpringXmlModelInspection
Cannot resolve class or package 'springframework'
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/soapaction/client.xml`
#### Snippet
```java
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.0.xsd">
    <bean class="org.springframework.ws.client.core.WebServiceTemplate">
        <constructor-arg ref="messageFactory"/>
        <property name="defaultUri" value="http://localhost:${port}/"/>
```

### SpringXmlModelInspection
Cannot resolve class or package 'ws'
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/soapaction/client.xml`
#### Snippet
```java
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.0.xsd">
    <bean class="org.springframework.ws.client.core.WebServiceTemplate">
        <constructor-arg ref="messageFactory"/>
        <property name="defaultUri" value="http://localhost:${port}/"/>
```

### SpringXmlModelInspection
Cannot resolve class or package 'client'
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/soapaction/client.xml`
#### Snippet
```java
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.0.xsd">
    <bean class="org.springframework.ws.client.core.WebServiceTemplate">
        <constructor-arg ref="messageFactory"/>
        <property name="defaultUri" value="http://localhost:${port}/"/>
```

### SpringXmlModelInspection
Cannot resolve class or package 'core'
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/soapaction/client.xml`
#### Snippet
```java
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.0.xsd">
    <bean class="org.springframework.ws.client.core.WebServiceTemplate">
        <constructor-arg ref="messageFactory"/>
        <property name="defaultUri" value="http://localhost:${port}/"/>
```

### SpringXmlModelInspection
Cannot resolve class 'WebServiceTemplate'
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/soapaction/client.xml`
#### Snippet
```java
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.0.xsd">
    <bean class="org.springframework.ws.client.core.WebServiceTemplate">
        <constructor-arg ref="messageFactory"/>
        <property name="defaultUri" value="http://localhost:${port}/"/>
```

### SpringXmlModelInspection
Cannot resolve bean 'messageFactory'
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/soapaction/client.xml`
#### Snippet
```java
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.0.xsd">
    <bean class="org.springframework.ws.client.core.WebServiceTemplate">
        <constructor-arg ref="messageFactory"/>
        <property name="defaultUri" value="http://localhost:${port}/"/>
    </bean>
```

### SpringXmlModelInspection
Cannot resolve property 'defaultUri'
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/soapaction/client.xml`
#### Snippet
```java
    <bean class="org.springframework.ws.client.core.WebServiceTemplate">
        <constructor-arg ref="messageFactory"/>
        <property name="defaultUri" value="http://localhost:${port}/"/>
    </bean>
</beans>
```

### SpringXmlModelInspection
Cannot resolve class or package 'springframework'
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/secureecho/client.xml`
#### Snippet
```java
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.0.xsd">
    <bean class="org.springframework.ws.client.core.WebServiceTemplate">
        <constructor-arg ref="messageFactory"/>
        <property name="defaultUri" value="http://localhost:${port}/"/>
```

### SpringXmlModelInspection
Cannot resolve class or package 'ws'
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/secureecho/client.xml`
#### Snippet
```java
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.0.xsd">
    <bean class="org.springframework.ws.client.core.WebServiceTemplate">
        <constructor-arg ref="messageFactory"/>
        <property name="defaultUri" value="http://localhost:${port}/"/>
```

### SpringXmlModelInspection
Cannot resolve class or package 'client'
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/secureecho/client.xml`
#### Snippet
```java
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.0.xsd">
    <bean class="org.springframework.ws.client.core.WebServiceTemplate">
        <constructor-arg ref="messageFactory"/>
        <property name="defaultUri" value="http://localhost:${port}/"/>
```

### SpringXmlModelInspection
Cannot resolve class or package 'core'
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/secureecho/client.xml`
#### Snippet
```java
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.0.xsd">
    <bean class="org.springframework.ws.client.core.WebServiceTemplate">
        <constructor-arg ref="messageFactory"/>
        <property name="defaultUri" value="http://localhost:${port}/"/>
```

### SpringXmlModelInspection
Cannot resolve class 'WebServiceTemplate'
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/secureecho/client.xml`
#### Snippet
```java
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.0.xsd">
    <bean class="org.springframework.ws.client.core.WebServiceTemplate">
        <constructor-arg ref="messageFactory"/>
        <property name="defaultUri" value="http://localhost:${port}/"/>
```

### SpringXmlModelInspection
Cannot resolve bean 'messageFactory'
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/secureecho/client.xml`
#### Snippet
```java
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.0.xsd">
    <bean class="org.springframework.ws.client.core.WebServiceTemplate">
        <constructor-arg ref="messageFactory"/>
        <property name="defaultUri" value="http://localhost:${port}/"/>
        <property name="interceptors">
```

### SpringXmlModelInspection
Cannot resolve class or package 'ws'
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/jaxb2/client.xml`
#### Snippet
```java
        <oxm:class-to-be-bound name="org.apache.axiom.ts.springws.scenario.jaxb2.GetQuoteResponse"/>
    </oxm:jaxb2-marshaller>
    <bean class="org.springframework.ws.client.core.WebServiceTemplate">
        <constructor-arg ref="messageFactory"/>
        <property name="marshaller" ref="marshaller"/>
```

### SpringXmlModelInspection
Cannot resolve property 'defaultUri'
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/secureecho/client.xml`
#### Snippet
```java
    <bean class="org.springframework.ws.client.core.WebServiceTemplate">
        <constructor-arg ref="messageFactory"/>
        <property name="defaultUri" value="http://localhost:${port}/"/>
        <property name="interceptors">
            <bean class="org.springframework.ws.soap.security.wss4j2.Wss4jSecurityInterceptor">
```

### SpringXmlModelInspection
Cannot resolve class or package 'client'
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/jaxb2/client.xml`
#### Snippet
```java
        <oxm:class-to-be-bound name="org.apache.axiom.ts.springws.scenario.jaxb2.GetQuoteResponse"/>
    </oxm:jaxb2-marshaller>
    <bean class="org.springframework.ws.client.core.WebServiceTemplate">
        <constructor-arg ref="messageFactory"/>
        <property name="marshaller" ref="marshaller"/>
```

### SpringXmlModelInspection
Cannot resolve property 'interceptors'
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/secureecho/client.xml`
#### Snippet
```java
        <constructor-arg ref="messageFactory"/>
        <property name="defaultUri" value="http://localhost:${port}/"/>
        <property name="interceptors">
            <bean class="org.springframework.ws.soap.security.wss4j2.Wss4jSecurityInterceptor">
                <property name="securementActions" value="UsernameToken"/>
```

### SpringXmlModelInspection
Cannot resolve class or package 'core'
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/jaxb2/client.xml`
#### Snippet
```java
        <oxm:class-to-be-bound name="org.apache.axiom.ts.springws.scenario.jaxb2.GetQuoteResponse"/>
    </oxm:jaxb2-marshaller>
    <bean class="org.springframework.ws.client.core.WebServiceTemplate">
        <constructor-arg ref="messageFactory"/>
        <property name="marshaller" ref="marshaller"/>
```

### SpringXmlModelInspection
Cannot resolve class or package 'springframework'
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/secureecho/client.xml`
#### Snippet
```java
        <property name="defaultUri" value="http://localhost:${port}/"/>
        <property name="interceptors">
            <bean class="org.springframework.ws.soap.security.wss4j2.Wss4jSecurityInterceptor">
                <property name="securementActions" value="UsernameToken"/>
                <property name="securementUsername" value="user1"/>
```

### SpringXmlModelInspection
Cannot resolve class or package 'ws'
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/secureecho/client.xml`
#### Snippet
```java
        <property name="defaultUri" value="http://localhost:${port}/"/>
        <property name="interceptors">
            <bean class="org.springframework.ws.soap.security.wss4j2.Wss4jSecurityInterceptor">
                <property name="securementActions" value="UsernameToken"/>
                <property name="securementUsername" value="user1"/>
```

### SpringXmlModelInspection
Cannot resolve class 'WebServiceTemplate'
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/jaxb2/client.xml`
#### Snippet
```java
        <oxm:class-to-be-bound name="org.apache.axiom.ts.springws.scenario.jaxb2.GetQuoteResponse"/>
    </oxm:jaxb2-marshaller>
    <bean class="org.springframework.ws.client.core.WebServiceTemplate">
        <constructor-arg ref="messageFactory"/>
        <property name="marshaller" ref="marshaller"/>
```

### SpringXmlModelInspection
Cannot resolve bean 'messageFactory'
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/jaxb2/client.xml`
#### Snippet
```java
    </oxm:jaxb2-marshaller>
    <bean class="org.springframework.ws.client.core.WebServiceTemplate">
        <constructor-arg ref="messageFactory"/>
        <property name="marshaller" ref="marshaller"/>
        <property name="unmarshaller" ref="marshaller"/>
```

### SpringXmlModelInspection
Cannot resolve property 'marshaller'
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/jaxb2/client.xml`
#### Snippet
```java
    <bean class="org.springframework.ws.client.core.WebServiceTemplate">
        <constructor-arg ref="messageFactory"/>
        <property name="marshaller" ref="marshaller"/>
        <property name="unmarshaller" ref="marshaller"/>
        <property name="defaultUri" value="http://localhost:${port}/"/>
```

### SpringXmlModelInspection
Cannot resolve class or package 'soap'
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/secureecho/client.xml`
#### Snippet
```java
        <property name="defaultUri" value="http://localhost:${port}/"/>
        <property name="interceptors">
            <bean class="org.springframework.ws.soap.security.wss4j2.Wss4jSecurityInterceptor">
                <property name="securementActions" value="UsernameToken"/>
                <property name="securementUsername" value="user1"/>
```

### SpringXmlModelInspection
Cannot resolve class or package 'security'
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/secureecho/client.xml`
#### Snippet
```java
        <property name="defaultUri" value="http://localhost:${port}/"/>
        <property name="interceptors">
            <bean class="org.springframework.ws.soap.security.wss4j2.Wss4jSecurityInterceptor">
                <property name="securementActions" value="UsernameToken"/>
                <property name="securementUsername" value="user1"/>
```

### SpringXmlModelInspection
Cannot resolve class or package 'wss4j2'
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/secureecho/client.xml`
#### Snippet
```java
        <property name="defaultUri" value="http://localhost:${port}/"/>
        <property name="interceptors">
            <bean class="org.springframework.ws.soap.security.wss4j2.Wss4jSecurityInterceptor">
                <property name="securementActions" value="UsernameToken"/>
                <property name="securementUsername" value="user1"/>
```

### SpringXmlModelInspection
Cannot resolve class 'Wss4jSecurityInterceptor'
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/secureecho/client.xml`
#### Snippet
```java
        <property name="defaultUri" value="http://localhost:${port}/"/>
        <property name="interceptors">
            <bean class="org.springframework.ws.soap.security.wss4j2.Wss4jSecurityInterceptor">
                <property name="securementActions" value="UsernameToken"/>
                <property name="securementUsername" value="user1"/>
```

### SpringXmlModelInspection
Cannot resolve property 'securementActions'
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/secureecho/client.xml`
#### Snippet
```java
        <property name="interceptors">
            <bean class="org.springframework.ws.soap.security.wss4j2.Wss4jSecurityInterceptor">
                <property name="securementActions" value="UsernameToken"/>
                <property name="securementUsername" value="user1"/>
                <property name="securementPassword" value="123456"/>
```

### SpringXmlModelInspection
Cannot resolve property 'securementUsername'
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/secureecho/client.xml`
#### Snippet
```java
            <bean class="org.springframework.ws.soap.security.wss4j2.Wss4jSecurityInterceptor">
                <property name="securementActions" value="UsernameToken"/>
                <property name="securementUsername" value="user1"/>
                <property name="securementPassword" value="123456"/>
            </bean>
```

### SpringXmlModelInspection
Cannot resolve class or package 'springframework'
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/broker/server.xml`
#### Snippet
```java
    <context:component-scan base-package="org.apache.axiom.ts.springws.scenario.broker"/>
    <sws:annotation-driven/>
    <bean id="exceptionResolver" class="org.springframework.ws.soap.server.endpoint.SoapFaultMappingExceptionResolver">
        <property name="order" value="-1"/>
        <property name="defaultFault" value="SERVER"/>
```

### SpringXmlModelInspection
Cannot resolve property 'unmarshaller'
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/jaxb2/client.xml`
#### Snippet
```java
        <constructor-arg ref="messageFactory"/>
        <property name="marshaller" ref="marshaller"/>
        <property name="unmarshaller" ref="marshaller"/>
        <property name="defaultUri" value="http://localhost:${port}/"/>
    </bean>
```

### SpringXmlModelInspection
Cannot resolve class or package 'ws'
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/broker/server.xml`
#### Snippet
```java
    <context:component-scan base-package="org.apache.axiom.ts.springws.scenario.broker"/>
    <sws:annotation-driven/>
    <bean id="exceptionResolver" class="org.springframework.ws.soap.server.endpoint.SoapFaultMappingExceptionResolver">
        <property name="order" value="-1"/>
        <property name="defaultFault" value="SERVER"/>
```

### SpringXmlModelInspection
Cannot resolve property 'defaultUri'
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/jaxb2/client.xml`
#### Snippet
```java
        <property name="marshaller" ref="marshaller"/>
        <property name="unmarshaller" ref="marshaller"/>
        <property name="defaultUri" value="http://localhost:${port}/"/>
    </bean>
</beans>
```

### SpringXmlModelInspection
Cannot resolve class or package 'soap'
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/broker/server.xml`
#### Snippet
```java
    <context:component-scan base-package="org.apache.axiom.ts.springws.scenario.broker"/>
    <sws:annotation-driven/>
    <bean id="exceptionResolver" class="org.springframework.ws.soap.server.endpoint.SoapFaultMappingExceptionResolver">
        <property name="order" value="-1"/>
        <property name="defaultFault" value="SERVER"/>
```

### SpringXmlModelInspection
Cannot resolve class or package 'server'
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/broker/server.xml`
#### Snippet
```java
    <context:component-scan base-package="org.apache.axiom.ts.springws.scenario.broker"/>
    <sws:annotation-driven/>
    <bean id="exceptionResolver" class="org.springframework.ws.soap.server.endpoint.SoapFaultMappingExceptionResolver">
        <property name="order" value="-1"/>
        <property name="defaultFault" value="SERVER"/>
```

### SpringXmlModelInspection
Cannot resolve class or package 'endpoint'
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/broker/server.xml`
#### Snippet
```java
    <context:component-scan base-package="org.apache.axiom.ts.springws.scenario.broker"/>
    <sws:annotation-driven/>
    <bean id="exceptionResolver" class="org.springframework.ws.soap.server.endpoint.SoapFaultMappingExceptionResolver">
        <property name="order" value="-1"/>
        <property name="defaultFault" value="SERVER"/>
```

### SpringXmlModelInspection
Cannot resolve class 'SoapFaultMappingExceptionResolver'
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/broker/server.xml`
#### Snippet
```java
    <context:component-scan base-package="org.apache.axiom.ts.springws.scenario.broker"/>
    <sws:annotation-driven/>
    <bean id="exceptionResolver" class="org.springframework.ws.soap.server.endpoint.SoapFaultMappingExceptionResolver">
        <property name="order" value="-1"/>
        <property name="defaultFault" value="SERVER"/>
```

### SpringXmlModelInspection
Cannot resolve property 'order'
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/broker/server.xml`
#### Snippet
```java
    <sws:annotation-driven/>
    <bean id="exceptionResolver" class="org.springframework.ws.soap.server.endpoint.SoapFaultMappingExceptionResolver">
        <property name="order" value="-1"/>
        <property name="defaultFault" value="SERVER"/>
        <property name="exceptionMappings">
```

### SpringXmlModelInspection
Cannot resolve property 'defaultFault'
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/broker/server.xml`
#### Snippet
```java
    <bean id="exceptionResolver" class="org.springframework.ws.soap.server.endpoint.SoapFaultMappingExceptionResolver">
        <property name="order" value="-1"/>
        <property name="defaultFault" value="SERVER"/>
        <property name="exceptionMappings">
            <value>
```

### SpringXmlModelInspection
Cannot resolve property 'exceptionMappings'
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/broker/server.xml`
#### Snippet
```java
        <property name="order" value="-1"/>
        <property name="defaultFault" value="SERVER"/>
        <property name="exceptionMappings">
            <value>
                org.apache.axiom.ts.springws.scenario.broker.UnknownCustomerException=CLIENT
```

### SpringXmlModelInspection
Cannot resolve property 'securementPassword'
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/secureecho/client.xml`
#### Snippet
```java
                <property name="securementActions" value="UsernameToken"/>
                <property name="securementUsername" value="user1"/>
                <property name="securementPassword" value="123456"/>
            </bean>
        </property>
```

### SpringXmlModelInspection
Cannot resolve class or package 'springframework'
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/broker/client.xml`
#### Snippet
```java
    <bean class="org.apache.axiom.ts.springws.scenario.broker.BrokerClient">
        <constructor-arg>
            <bean class="org.springframework.ws.client.core.WebServiceTemplate">
                <constructor-arg ref="messageFactory"/>
                <property name="marshaller" ref="marshaller"/>
```

### SpringXmlModelInspection
Cannot resolve class or package 'ws'
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/broker/client.xml`
#### Snippet
```java
    <bean class="org.apache.axiom.ts.springws.scenario.broker.BrokerClient">
        <constructor-arg>
            <bean class="org.springframework.ws.client.core.WebServiceTemplate">
                <constructor-arg ref="messageFactory"/>
                <property name="marshaller" ref="marshaller"/>
```

### SpringXmlModelInspection
Cannot resolve class or package 'client'
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/broker/client.xml`
#### Snippet
```java
    <bean class="org.apache.axiom.ts.springws.scenario.broker.BrokerClient">
        <constructor-arg>
            <bean class="org.springframework.ws.client.core.WebServiceTemplate">
                <constructor-arg ref="messageFactory"/>
                <property name="marshaller" ref="marshaller"/>
```

### SpringXmlModelInspection
Cannot resolve class or package 'core'
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/broker/client.xml`
#### Snippet
```java
    <bean class="org.apache.axiom.ts.springws.scenario.broker.BrokerClient">
        <constructor-arg>
            <bean class="org.springframework.ws.client.core.WebServiceTemplate">
                <constructor-arg ref="messageFactory"/>
                <property name="marshaller" ref="marshaller"/>
```

### SpringXmlModelInspection
Cannot resolve class 'WebServiceTemplate'
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/broker/client.xml`
#### Snippet
```java
    <bean class="org.apache.axiom.ts.springws.scenario.broker.BrokerClient">
        <constructor-arg>
            <bean class="org.springframework.ws.client.core.WebServiceTemplate">
                <constructor-arg ref="messageFactory"/>
                <property name="marshaller" ref="marshaller"/>
```

### SpringXmlModelInspection
Cannot resolve bean 'messageFactory'
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/broker/client.xml`
#### Snippet
```java
        <constructor-arg>
            <bean class="org.springframework.ws.client.core.WebServiceTemplate">
                <constructor-arg ref="messageFactory"/>
                <property name="marshaller" ref="marshaller"/>
                <property name="unmarshaller" ref="marshaller"/>
```

### SpringXmlModelInspection
Cannot resolve property 'marshaller'
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/broker/client.xml`
#### Snippet
```java
            <bean class="org.springframework.ws.client.core.WebServiceTemplate">
                <constructor-arg ref="messageFactory"/>
                <property name="marshaller" ref="marshaller"/>
                <property name="unmarshaller" ref="marshaller"/>
                <property name="defaultUri" value="http://localhost:${port}/"/>
```

### SpringXmlModelInspection
Cannot resolve property 'unmarshaller'
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/broker/client.xml`
#### Snippet
```java
                <constructor-arg ref="messageFactory"/>
                <property name="marshaller" ref="marshaller"/>
                <property name="unmarshaller" ref="marshaller"/>
                <property name="defaultUri" value="http://localhost:${port}/"/>
            </bean>
```

### SpringXmlModelInspection
Cannot resolve property 'defaultUri'
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/broker/client.xml`
#### Snippet
```java
                <property name="marshaller" ref="marshaller"/>
                <property name="unmarshaller" ref="marshaller"/>
                <property name="defaultUri" value="http://localhost:${port}/"/>
            </bean>
        </constructor-arg>
```

### SpringXmlModelInspection
Cannot resolve class or package 'springframework'
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/validation/client.xml`
#### Snippet
```java
    <bean class="org.apache.axiom.ts.springws.scenario.validation.StockQuoteClient">
        <constructor-arg>
            <bean class="org.springframework.ws.client.core.WebServiceTemplate">
                <constructor-arg ref="messageFactory"/>
                <property name="marshaller" ref="marshaller"/>
```

### SpringXmlModelInspection
Cannot resolve class or package 'ws'
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/validation/client.xml`
#### Snippet
```java
    <bean class="org.apache.axiom.ts.springws.scenario.validation.StockQuoteClient">
        <constructor-arg>
            <bean class="org.springframework.ws.client.core.WebServiceTemplate">
                <constructor-arg ref="messageFactory"/>
                <property name="marshaller" ref="marshaller"/>
```

### SpringXmlModelInspection
Cannot resolve class or package 'client'
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/validation/client.xml`
#### Snippet
```java
    <bean class="org.apache.axiom.ts.springws.scenario.validation.StockQuoteClient">
        <constructor-arg>
            <bean class="org.springframework.ws.client.core.WebServiceTemplate">
                <constructor-arg ref="messageFactory"/>
                <property name="marshaller" ref="marshaller"/>
```

### SpringXmlModelInspection
Cannot resolve class or package 'core'
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/validation/client.xml`
#### Snippet
```java
    <bean class="org.apache.axiom.ts.springws.scenario.validation.StockQuoteClient">
        <constructor-arg>
            <bean class="org.springframework.ws.client.core.WebServiceTemplate">
                <constructor-arg ref="messageFactory"/>
                <property name="marshaller" ref="marshaller"/>
```

### SpringXmlModelInspection
Cannot resolve class 'WebServiceTemplate'
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/validation/client.xml`
#### Snippet
```java
    <bean class="org.apache.axiom.ts.springws.scenario.validation.StockQuoteClient">
        <constructor-arg>
            <bean class="org.springframework.ws.client.core.WebServiceTemplate">
                <constructor-arg ref="messageFactory"/>
                <property name="marshaller" ref="marshaller"/>
```

### SpringXmlModelInspection
Cannot resolve bean 'messageFactory'
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/validation/client.xml`
#### Snippet
```java
        <constructor-arg>
            <bean class="org.springframework.ws.client.core.WebServiceTemplate">
                <constructor-arg ref="messageFactory"/>
                <property name="marshaller" ref="marshaller"/>
                <property name="unmarshaller" ref="marshaller"/>
```

### SpringXmlModelInspection
Cannot resolve property 'marshaller'
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/validation/client.xml`
#### Snippet
```java
            <bean class="org.springframework.ws.client.core.WebServiceTemplate">
                <constructor-arg ref="messageFactory"/>
                <property name="marshaller" ref="marshaller"/>
                <property name="unmarshaller" ref="marshaller"/>
                <property name="defaultUri" value="http://localhost:${port}/"/>
```

### SpringXmlModelInspection
Cannot resolve property 'unmarshaller'
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/validation/client.xml`
#### Snippet
```java
                <constructor-arg ref="messageFactory"/>
                <property name="marshaller" ref="marshaller"/>
                <property name="unmarshaller" ref="marshaller"/>
                <property name="defaultUri" value="http://localhost:${port}/"/>
            </bean>
```

### SpringXmlModelInspection
Cannot resolve property 'defaultUri'
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/validation/client.xml`
#### Snippet
```java
                <property name="marshaller" ref="marshaller"/>
                <property name="unmarshaller" ref="marshaller"/>
                <property name="defaultUri" value="http://localhost:${port}/"/>
            </bean>
        </constructor-arg>
```

## RuleId[id=UnnecessaryCallToStringValueOf]
### UnnecessaryCallToStringValueOf
Unnecessary `Long.toString()` call
in `axiom-compat/src/main/java/org/apache/axiom/om/util/UUIDGenerator.java`
#### Snippet
```java
        sb.append(sid);
        sb.append(":");
        sb.append(Long.toString(rand));
        MessageDigest md5 = null;
        try {
```

## RuleId[id=WebProperties]
### WebProperties
Cannot resolve class 'SnoopServlet'
in `testing/axiom-testsuite/src/main/resources/web_w_dtd.xml`
#### Snippet
```java
  <servlet>
    <servlet-name>snoop</servlet-name>
    <servlet-class>SnoopServlet</servlet-class>
  </servlet>
  <servlet>
```

### WebProperties
Cannot resolve class 'ViewFile'
in `testing/axiom-testsuite/src/main/resources/web_w_dtd.xml`
#### Snippet
```java
  <servlet>
    <servlet-name>file</servlet-name>
    <servlet-class>ViewFile</servlet-class>
    <init-param>
      <param-name>initial</param-name>
```

### WebProperties
Cannot resolve Servlet 'mv'
in `testing/axiom-testsuite/src/main/resources/web_w_dtd.xml`
#### Snippet
```java
  <servlet-mapping>
    <servlet-name>
      mv
    </servlet-name>
    <url-pattern>
```

## RuleId[id=SimplifiableAssertion]
### SimplifiableAssertion
`assertFalse()` can be simplified to 'assertNotEquals()'
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/w3c/JUnitTestFramework.java`
#### Snippet
```java
    @Override
    public void assertNotEquals(DOMTestCase test, String assertID, int expected, int actual) {
        Assert.assertFalse(assertID, expected == actual);
    }

```

### SimplifiableAssertion
`assertFalse()` can be simplified to 'assertNotEquals()'
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/w3c/JUnitTestFramework.java`
#### Snippet
```java
    @Override
    public void assertNotEquals(DOMTestCase test, String assertID, String expected, String actual) {
        Assert.assertFalse(assertID, expected.equals(actual));
    }

```

### SimplifiableAssertion
`assertEquals()` can be simplified to 'assertNull()'
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/element/TestLookupNamespaceURIExplicit.java`
#### Snippet
```java

        assertEquals("urn:ns4", e1.lookupNamespaceURI("q"));
        assertEquals(null, e2.lookupNamespaceURI("q"));
        assertEquals(null, e3.lookupNamespaceURI("q"));
    }
```

### SimplifiableAssertion
`assertEquals()` can be simplified to 'assertNull()'
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/element/TestLookupNamespaceURIExplicit.java`
#### Snippet
```java
        assertEquals("urn:ns4", e1.lookupNamespaceURI("q"));
        assertEquals(null, e2.lookupNamespaceURI("q"));
        assertEquals(null, e3.lookupNamespaceURI("q"));
    }
}
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/misc/TestAxiom95.java`
#### Snippet
```java
        //        System.out.println("serializedXML = " + serializedXML);

        assertTrue(serializedXML.indexOf("xmlns=\"\"") == -1);
    }
}
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/xop/TestSerialize.java`
#### Snippet
```java
            assertTrue(out.indexOf("GBgcGBQgHBwcJCQgKDBQNDAsL") != -1);
        } else {
            assertTrue(out.indexOf("base64") == -1);
        }
    }
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/builder/TestStandaloneConfiguration.java`
#### Snippet
```java
                        metaFactory.getOMFactory(), StAXParserConfiguration.STANDALONE, is);
        OMElement root = builder.getDocumentElement();
        assertTrue(root.getLocalName().equals("web-app"));
        OMDocument document = builder.getDocument();
        Iterator<OMNode> i = document.getChildren();
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertNotNull()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/builder/TestStandaloneConfiguration.java`
#### Snippet
```java
            }
        }
        assertTrue(docType != null);
        root.close(false);
    }
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestSerializationWithTwoNonBuiltOMElements.java`
#### Snippet
```java
        rootElement.addChild(childTwo);

        assertTrue(expectedXML.equals(rootElement.toString()));

        childOne.close(false);
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertArrayEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestGetXMLStreamReaderCDATAEventFromElement.java`
#### Snippet
```java
        assertEquals(XMLStreamReader.CDATA, event);
        assertEquals("hello world", reader2.getText()); // AXIOM-146
        assertTrue(
                Arrays.equals(
                        "hello world".toCharArray(), reader2.getTextCharacters())); // AXIOM-144
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertArrayEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestGetXMLStreamReaderCDATAEventFromParser.java`
#### Snippet
```java
            assertEquals(XMLStreamReader.CDATA, event);
            assertEquals("hello world", reader2.getText()); // AXIOM-146
            assertTrue(
                    Arrays.equals(
                            "hello world".toCharArray(), reader2.getTextCharacters())); // AXIOM-144
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestGetChildElements.java`
#### Snippet
```java
            OMElement o = iter.next();
            assertNotNull("Must return not null objects!", o);
            assertTrue("All these should be elements!", o.getType() == OMNode.ELEMENT_NODE);
        }
        assertEquals("This element should contain only two elements ", 2, counter);
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestGetChildrenWithName.java`
#### Snippet
```java
            Object o = iter.next();
            assertNotNull("Must return not null objects!", o);
            assertTrue(
                    "All these should be elements!", ((OMNode) o).getType() == OMNode.ELEMENT_NODE);
        }
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/factory/TestCreateOMText.java`
#### Snippet
```java
        OMText omText = factory.createOMText(omElement, text);
        assertTrue("Programatically created OMText should have done = true ", omText.isComplete());
        assertTrue(
                "Programatically created OMText should have correct text value ",
                text.equals(omText.getText()));
```

### SimplifiableAssertion
`assertFalse()` can be simplified to 'assertNotEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/namespace/TestObjectEqualsWithDifferentPrefixes.java`
#### Snippet
```java
        OMNamespace ns1 = factory.createOMNamespace("urn:ns", "ns1");
        OMNamespace ns2 = factory.createOMNamespace("urn:ns", "ns2");
        assertFalse(ns1.equals(ns2));
    }
}
```

### SimplifiableAssertion
`assertFalse()` can be simplified to 'assertNotEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/namespace/TestObjectEqualsWithDifferentURIs.java`
#### Snippet
```java
        OMNamespace ns1 = factory.createOMNamespace("urn:ns1", "ns");
        OMNamespace ns2 = factory.createOMNamespace("urn:ns2", "ns");
        assertFalse(ns1.equals(ns2));
    }
}
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/namespace/TestObjectEquals.java`
#### Snippet
```java
        OMNamespace ns1 = factory.createOMNamespace(new String("urn:ns"), new String("ns"));
        OMNamespace ns2 = factory.createOMNamespace(new String("urn:ns"), new String("ns"));
        assertTrue(ns1.equals(ns2));
    }
}
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertFalse()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestBlobOMDataSource.java`
#### Snippet
```java
        assertTrue("Expected OMSourcedElement child", firstChild instanceof OMSourcedElement);
        OMSourcedElement child = (OMSourcedElement) firstChild;
        assertTrue("OMSourcedElement is expanded.  This is unexpected", !child.isExpanded());
        assertThat(child.getDataSource()).isSameInstanceAs(ds);

```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertFalse()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestBlobOMDataSource.java`
#### Snippet
```java
        XMLStreamReader reader = child.getXMLStreamReader();
        reader.next();
        assertTrue("OMSourcedElement is expanded.  This is unexpected", !child.isExpanded());

        // Likewise, a BlobOMDataSource does not consume the backing object when
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertFalse()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestBlobOMDataSource.java`
#### Snippet
```java
                "The payload was not present in the output",
                parent.toString().indexOf(payload) > 0);
        assertTrue("OMSourcedElement is expanded.  This is unexpected", !child.isExpanded());

        // If a consumer calls build or buildWithAttachments on the tree, the
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertFalse()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestBlobOMDataSource.java`
#### Snippet
```java
        // tree should not be expanded.
        parent.build();
        assertTrue(
                "OMSourcedElement is expanded after build().  This is unexpected",
                !child.isExpanded());
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertFalse()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestBlobOMDataSource.java`
#### Snippet
```java
                !child.isExpanded());
        parent.buildWithAttachments();
        assertTrue(
                "OMSourcedElement is expanded after buildWithAttachments().  This is unexpected",
                !child.isExpanded());
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName1DefaultPrefix.java`
#### Snippet
```java

        // Test getting the local name and namespace URI. This should used not result in expansion
        assertTrue(element.getLocalName().equals("library"));
        assertTrue(
                element.getNamespace()
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName1DefaultPrefix.java`
#### Snippet
```java
        // Test getting the local name and namespace URI. This should used not result in expansion
        assertTrue(element.getLocalName().equals("library"));
        assertTrue(
                element.getNamespace()
                        .getNamespaceURI()
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName1DefaultPrefix.java`
#### Snippet
```java
        String result = writer.toString();

        assertTrue(element.getLocalName().equals("library"));
        assertTrue(
                element.getNamespace()
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName1DefaultPrefix.java`
#### Snippet
```java

        assertTrue(element.getLocalName().equals("library"));
        assertTrue(
                element.getNamespace()
                        .getNamespaceURI()
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName1DefaultPrefix.java`
#### Snippet
```java
                        .getNamespaceURI()
                        .equals("http://www.sosnoski.com/uwjws/library"));
        assertTrue(element.getNamespace().getPrefix().equals(""));
        assertTrue(element.getDefaultNamespace() != null);
        assertTrue("Serialized text error" + result, result.indexOf("1930110111") > 0);
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertNotNull()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName1DefaultPrefix.java`
#### Snippet
```java
                        .equals("http://www.sosnoski.com/uwjws/library"));
        assertTrue(element.getNamespace().getPrefix().equals(""));
        assertTrue(element.getDefaultNamespace() != null);
        assertTrue("Serialized text error" + result, result.indexOf("1930110111") > 0);

```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName1DefaultPrefix.java`
#### Snippet
```java
        result = writer.toString();

        assertTrue(element.getLocalName().equals("library"));
        assertTrue(
                element.getNamespace()
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName1DefaultPrefix.java`
#### Snippet
```java

        assertTrue(element.getLocalName().equals("library"));
        assertTrue(
                element.getNamespace()
                        .getNamespaceURI()
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName1DefaultPrefix.java`
#### Snippet
```java
                        .getNamespaceURI()
                        .equals("http://www.sosnoski.com/uwjws/library"));
        assertTrue(element.getNamespace().getPrefix().equals(""));
        assertTrue(element.getDefaultNamespace() != null);
        assertTrue("Serialized text error" + result, result.indexOf("1930110111") > 0);
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertNotNull()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName1DefaultPrefix.java`
#### Snippet
```java
                        .equals("http://www.sosnoski.com/uwjws/library"));
        assertTrue(element.getNamespace().getPrefix().equals(""));
        assertTrue(element.getDefaultNamespace() != null);
        assertTrue("Serialized text error" + result, result.indexOf("1930110111") > 0);
    }
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName3QualifiedPrefix.java`
#### Snippet
```java
        // Test getting the namespace, localpart and prefix.  This should used not result in
        // expansion
        assertTrue(element.getLocalName().equals("DUMMYNAME"));
        assertTrue(element.getNamespace().getNamespaceURI().equals("http://DUMMYNS"));
        assertTrue(element.getNamespace().getPrefix().equals("DUMMYPREFIX"));
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName3QualifiedPrefix.java`
#### Snippet
```java
        // expansion
        assertTrue(element.getLocalName().equals("DUMMYNAME"));
        assertTrue(element.getNamespace().getNamespaceURI().equals("http://DUMMYNS"));
        assertTrue(element.getNamespace().getPrefix().equals("DUMMYPREFIX"));

```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName3QualifiedPrefix.java`
#### Snippet
```java
        assertTrue(element.getLocalName().equals("DUMMYNAME"));
        assertTrue(element.getNamespace().getNamespaceURI().equals("http://DUMMYNS"));
        assertTrue(element.getNamespace().getPrefix().equals("DUMMYPREFIX"));

        // Serialize and cache.  This should cause expansion and update the name to match the
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName3QualifiedPrefix.java`
#### Snippet
```java
        String result = writer.toString();

        assertTrue(element.getLocalName().equals("library"));
        assertTrue(
                element.getNamespace()
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName3QualifiedPrefix.java`
#### Snippet
```java

        assertTrue(element.getLocalName().equals("library"));
        assertTrue(
                element.getNamespace()
                        .getNamespaceURI()
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName3QualifiedPrefix.java`
#### Snippet
```java
                        .getNamespaceURI()
                        .equals("http://www.sosnoski.com/uwjws/library"));
        assertTrue(element.getNamespace().getPrefix().equals(""));
        // Make sure that the serialized string does not contain the DUMMY values
        assertTrue(result.indexOf("DUMMY") < 0);
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName3QualifiedPrefix.java`
#### Snippet
```java
        result = writer.toString();

        assertTrue(element.getLocalName().equals("library"));
        assertTrue(
                element.getNamespace()
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName3QualifiedPrefix.java`
#### Snippet
```java

        assertTrue(element.getLocalName().equals("library"));
        assertTrue(
                element.getNamespace()
                        .getNamespaceURI()
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName3QualifiedPrefix.java`
#### Snippet
```java
                        .getNamespaceURI()
                        .equals("http://www.sosnoski.com/uwjws/library"));
        assertTrue(element.getNamespace().getPrefix().equals(""));
        // Make sure that the serialized string does not contain the DUMMY values
        assertTrue(result.indexOf("DUMMY") < 0);
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertFalse()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestStringOMDataSource.java`
#### Snippet
```java
        assertTrue("Expected OMSourcedElement child", firstChild instanceof OMSourcedElement);
        OMSourcedElement child = (OMSourcedElement) firstChild;
        assertTrue("OMSourcedElement is expanded.  This is unexpected", !child.isExpanded());
        assertThat(child.getDataSource()).isInstanceOf(StringOMDataSource.class);

```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertFalse()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestStringOMDataSource.java`
#### Snippet
```java
        XMLStreamReader reader = child.getXMLStreamReader();
        reader.next();
        assertTrue("OMSourcedElement is expanded.  This is unexpected", !child.isExpanded());

        // Likewise, a StringOMDataSource does not consume the backing object when
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertFalse()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestStringOMDataSource.java`
#### Snippet
```java
        assertTrue(
                "The payload was not present in the output", out.toString().indexOf(payload1) > 0);
        assertTrue("OMSourcedElement is expanded.  This is unexpected", !child.isExpanded());

        // Test getting the raw content from the StringOMDataSource.
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertNull()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestStringOMDataSource.java`
#### Snippet
```java
        // Validate close
        ds.close();
        assertTrue("Close should free the resource", ds.getObject() == null);
    }
}
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName2QualifiedPrefix.java`
#### Snippet
```java
        // Test getting the namespace, localpart and prefix.  This should used not result in
        // expansion
        assertTrue(element.getLocalName().equals("library"));
        assertTrue(
                element.getNamespace()
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName2QualifiedPrefix.java`
#### Snippet
```java
        // expansion
        assertTrue(element.getLocalName().equals("library"));
        assertTrue(
                element.getNamespace()
                        .getNamespaceURI()
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName2QualifiedPrefix.java`
#### Snippet
```java
                        .getNamespaceURI()
                        .equals("http://www.sosnoski.com/uwjws/library"));
        assertTrue(element.getNamespace().getPrefix().equals(""));

        // Serialize and consume.  This should not cause expansion and currently won't update
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName2QualifiedPrefix.java`
#### Snippet
```java
        String result = writer.toString();

        assertTrue(element.getLocalName().equals("library"));
        assertTrue(
                element.getNamespace()
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName2QualifiedPrefix.java`
#### Snippet
```java

        assertTrue(element.getLocalName().equals("library"));
        assertTrue(
                element.getNamespace()
                        .getNamespaceURI()
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName2QualifiedPrefix.java`
#### Snippet
```java
                        .getNamespaceURI()
                        .equals("http://www.sosnoski.com/uwjws/library"));
        assertTrue(element.getNamespace().getPrefix().equals(""));
        // Make sure that the serialized string does not contain default prefix declaration
        assertTrue(result.indexOf("xmlns=") < 0);
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName2DefaultPrefix.java`
#### Snippet
```java
        // Test getting the namespace, localpart and prefix.  This should used not result in
        // expansion
        assertTrue(element.getLocalName().equals("library"));
        assertTrue(
                element.getNamespace()
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName2DefaultPrefix.java`
#### Snippet
```java
        // expansion
        assertTrue(element.getLocalName().equals("library"));
        assertTrue(
                element.getNamespace()
                        .getNamespaceURI()
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName2DefaultPrefix.java`
#### Snippet
```java
                        .getNamespaceURI()
                        .equals("http://www.sosnoski.com/uwjws/library"));
        assertTrue(element.getNamespace().getPrefix().equals("DUMMYPREFIX"));

        // Serialize and consume.  This should not cause expansion and currently won't update
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName2DefaultPrefix.java`
#### Snippet
```java
        String result = writer.toString();

        assertTrue(element.getLocalName().equals("library"));
        assertTrue(
                element.getNamespace()
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName2DefaultPrefix.java`
#### Snippet
```java

        assertTrue(element.getLocalName().equals("library"));
        assertTrue(
                element.getNamespace()
                        .getNamespaceURI()
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName2DefaultPrefix.java`
#### Snippet
```java
                        .getNamespaceURI()
                        .equals("http://www.sosnoski.com/uwjws/library"));
        assertTrue(element.getNamespace().getPrefix().equals("DUMMYPREFIX"));
        // Make sure that the serialized string does not contain DUMMYPREFIX
        assertTrue(result.indexOf("DUMMYPREFIX") < 0);
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName4QualifiedPrefix.java`
#### Snippet
```java
        // Test getting the namespace, localpart and prefix.  This should used not result in
        // expansion
        assertTrue(element.getLocalName().equals("DUMMYNAME"));
        assertTrue(element.getNamespace().getNamespaceURI().equals("http://DUMMYNS"));
        assertTrue(element.getNamespace().getPrefix().equals(""));
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName4QualifiedPrefix.java`
#### Snippet
```java
        // expansion
        assertTrue(element.getLocalName().equals("DUMMYNAME"));
        assertTrue(element.getNamespace().getNamespaceURI().equals("http://DUMMYNS"));
        assertTrue(element.getNamespace().getPrefix().equals(""));

```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName4QualifiedPrefix.java`
#### Snippet
```java
        assertTrue(element.getLocalName().equals("DUMMYNAME"));
        assertTrue(element.getNamespace().getNamespaceURI().equals("http://DUMMYNS"));
        assertTrue(element.getNamespace().getPrefix().equals(""));

        // Serialize and consume.  This should not cause expansion and currently won't update
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName4QualifiedPrefix.java`
#### Snippet
```java
        String result = writer.toString();

        assertTrue(element.getLocalName().equals("DUMMYNAME"));
        assertTrue(element.getNamespace().getNamespaceURI().equals("http://DUMMYNS"));
        assertTrue(element.getNamespace().getPrefix().equals(""));
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName4QualifiedPrefix.java`
#### Snippet
```java

        assertTrue(element.getLocalName().equals("DUMMYNAME"));
        assertTrue(element.getNamespace().getNamespaceURI().equals("http://DUMMYNS"));
        assertTrue(element.getNamespace().getPrefix().equals(""));
        // Make sure that the serialized string does not contain the DUMMY values
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName4QualifiedPrefix.java`
#### Snippet
```java
        assertTrue(element.getLocalName().equals("DUMMYNAME"));
        assertTrue(element.getNamespace().getNamespaceURI().equals("http://DUMMYNS"));
        assertTrue(element.getNamespace().getPrefix().equals(""));
        // Make sure that the serialized string does not contain the DUMMY values
        assertTrue(result.indexOf("DUMMY") < 0);
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertFalse()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestSetDataSource.java`
#### Snippet
```java
        assertTrue("Expected OMSourcedElement child", firstChild instanceof OMSourcedElement);
        OMSourcedElement child = (OMSourcedElement) firstChild;
        assertTrue("OMSourcedElement is expanded.  This is unexpected", !child.isExpanded());
        assertThat(child.getDataSource()).isSameInstanceAs(nonDestructiveOMDataSource1);

```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertFalse()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestSetDataSource.java`
#### Snippet
```java
        //        System.out.println(output);
        assertTrue("The payload was not present in the output", output.indexOf(payload1) > 0);
        assertTrue("OMSourcedElement is expanded.  This is unexpected", !child.isExpanded());

        // Replace with payload2.
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertFalse()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestSetDataSource.java`
#### Snippet
```java
        //        System.out.println(output);
        assertTrue("The payload was not present in the output", output.indexOf(payload2) > 0);
        assertTrue("OMSourcedElement is expanded.  This is unexpected", !child.isExpanded());

        // Now Replace with payload1 from an destructiveOMDataSource1
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName1Unqualified.java`
#### Snippet
```java
        // Test getting the namespace, localpart and prefix.  This should used not result in
        // expansion
        assertTrue(element.getLocalName().equals("library"));
        assertNull(element.getNamespace());

```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName1Unqualified.java`
#### Snippet
```java
        String result = writer.toString();

        assertTrue(element.getLocalName().equals("library"));
        assertNull(element.getNamespace());
        assertNull(element.getDefaultNamespace());
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName1Unqualified.java`
#### Snippet
```java
        result = writer.toString();

        assertTrue(element.getLocalName().equals("library"));
        assertNull(element.getNamespace());
        // Make sure that the serialized string does not contain default prefix declaration
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName4DefaultPrefix.java`
#### Snippet
```java
        // Test getting the namespace, localpart and prefix.  This should used not result in
        // expansion
        assertTrue(element.getLocalName().equals("DUMMYNAME"));
        assertTrue(element.getNamespace().getNamespaceURI().equals("http://DUMMYNS"));
        assertTrue(element.getNamespace().getPrefix().equals("DUMMYPREFIX"));
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName4DefaultPrefix.java`
#### Snippet
```java
        // expansion
        assertTrue(element.getLocalName().equals("DUMMYNAME"));
        assertTrue(element.getNamespace().getNamespaceURI().equals("http://DUMMYNS"));
        assertTrue(element.getNamespace().getPrefix().equals("DUMMYPREFIX"));

```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName4DefaultPrefix.java`
#### Snippet
```java
        assertTrue(element.getLocalName().equals("DUMMYNAME"));
        assertTrue(element.getNamespace().getNamespaceURI().equals("http://DUMMYNS"));
        assertTrue(element.getNamespace().getPrefix().equals("DUMMYPREFIX"));

        // Serialize and consume.  This should not cause expansion and currently won't update
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName4DefaultPrefix.java`
#### Snippet
```java
        String result = writer.toString();

        assertTrue(element.getLocalName().equals("DUMMYNAME"));
        assertTrue(element.getNamespace().getNamespaceURI().equals("http://DUMMYNS"));
        assertTrue(element.getNamespace().getPrefix().equals("DUMMYPREFIX"));
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName4DefaultPrefix.java`
#### Snippet
```java

        assertTrue(element.getLocalName().equals("DUMMYNAME"));
        assertTrue(element.getNamespace().getNamespaceURI().equals("http://DUMMYNS"));
        assertTrue(element.getNamespace().getPrefix().equals("DUMMYPREFIX"));
        // Make sure that the serialized string does not contain the DUMMY values
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName4DefaultPrefix.java`
#### Snippet
```java
        assertTrue(element.getLocalName().equals("DUMMYNAME"));
        assertTrue(element.getNamespace().getNamespaceURI().equals("http://DUMMYNS"));
        assertTrue(element.getNamespace().getPrefix().equals("DUMMYPREFIX"));
        // Make sure that the serialized string does not contain the DUMMY values
        assertTrue(result.indexOf("DUMMY") < 0);
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName4Unqualified.java`
#### Snippet
```java
        // Test getting the namespace, localpart and prefix.  This should used not result in
        // expansion
        assertTrue(element.getLocalName().equals("DUMMYNAME"));
        assertTrue(element.getNamespace().getNamespaceURI().equals("http://DUMMYNS"));
        assertTrue(element.getNamespace().getPrefix().equals(""));
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName4Unqualified.java`
#### Snippet
```java
        // expansion
        assertTrue(element.getLocalName().equals("DUMMYNAME"));
        assertTrue(element.getNamespace().getNamespaceURI().equals("http://DUMMYNS"));
        assertTrue(element.getNamespace().getPrefix().equals(""));

```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName4Unqualified.java`
#### Snippet
```java
        assertTrue(element.getLocalName().equals("DUMMYNAME"));
        assertTrue(element.getNamespace().getNamespaceURI().equals("http://DUMMYNS"));
        assertTrue(element.getNamespace().getPrefix().equals(""));

        // Serialize and consume.  This should not cause expansion and currently won't update
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName4Unqualified.java`
#### Snippet
```java
        String result = writer.toString();

        assertTrue(element.getLocalName().equals("DUMMYNAME"));
        assertTrue(element.getNamespace().getNamespaceURI().equals("http://DUMMYNS"));
        assertTrue(element.getNamespace().getPrefix().equals(""));
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName4Unqualified.java`
#### Snippet
```java

        assertTrue(element.getLocalName().equals("DUMMYNAME"));
        assertTrue(element.getNamespace().getNamespaceURI().equals("http://DUMMYNS"));
        assertTrue(element.getNamespace().getPrefix().equals(""));
        // Make sure that the serialized string does not contain the DUMMY values
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName4Unqualified.java`
#### Snippet
```java
        assertTrue(element.getLocalName().equals("DUMMYNAME"));
        assertTrue(element.getNamespace().getNamespaceURI().equals("http://DUMMYNS"));
        assertTrue(element.getNamespace().getPrefix().equals(""));
        // Make sure that the serialized string does not contain the DUMMY values
        assertTrue(result.indexOf("DUMMY") < 0);
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName3Unqualified.java`
#### Snippet
```java
        // Test getting the namespace, localpart and prefix.  This should used not result in
        // expansion
        assertTrue(element.getLocalName().equals("DUMMYNAME"));
        assertTrue(element.getNamespace().getNamespaceURI().equals("http://DUMMYNS"));
        assertTrue(element.getNamespace().getPrefix().equals("DUMMYPREFIX"));
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName3Unqualified.java`
#### Snippet
```java
        // expansion
        assertTrue(element.getLocalName().equals("DUMMYNAME"));
        assertTrue(element.getNamespace().getNamespaceURI().equals("http://DUMMYNS"));
        assertTrue(element.getNamespace().getPrefix().equals("DUMMYPREFIX"));

```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName3Unqualified.java`
#### Snippet
```java
        assertTrue(element.getLocalName().equals("DUMMYNAME"));
        assertTrue(element.getNamespace().getNamespaceURI().equals("http://DUMMYNS"));
        assertTrue(element.getNamespace().getPrefix().equals("DUMMYPREFIX"));

        // Serialize and cache.  This should cause expansion and update the name to match the
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName3Unqualified.java`
#### Snippet
```java
        String result = writer.toString();

        assertTrue(element.getLocalName().equals("library"));
        assertTrue(element.getNamespace().getNamespaceURI().equals(""));
        assertTrue(element.getNamespace().getPrefix().equals(""));
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName3Unqualified.java`
#### Snippet
```java

        assertTrue(element.getLocalName().equals("library"));
        assertTrue(element.getNamespace().getNamespaceURI().equals(""));
        assertTrue(element.getNamespace().getPrefix().equals(""));
        // Make sure that the serialized string does not contain the DUMMY values
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName3Unqualified.java`
#### Snippet
```java
        assertTrue(element.getLocalName().equals("library"));
        assertTrue(element.getNamespace().getNamespaceURI().equals(""));
        assertTrue(element.getNamespace().getPrefix().equals(""));
        // Make sure that the serialized string does not contain the DUMMY values
        assertTrue(result.indexOf("DUMMY") < 0);
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName3Unqualified.java`
#### Snippet
```java
        result = writer.toString();

        assertTrue(element.getLocalName().equals("library"));
        assertTrue(
                element.getNamespace()
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName3Unqualified.java`
#### Snippet
```java

        assertTrue(element.getLocalName().equals("library"));
        assertTrue(
                element.getNamespace()
                        .getNamespaceURI()
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName3Unqualified.java`
#### Snippet
```java
                        .getNamespaceURI()
                        .equals("http://www.sosnoski.com/uwjws/library"));
        assertTrue(element.getNamespace().getPrefix().equals(""));
        // Make sure that the serialized string does not contain the DUMMY values
        assertTrue(result.indexOf("DUMMY") < 0);
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName2Unqualified.java`
#### Snippet
```java
        // Test getting the namespace, localpart and prefix.  This should used not result in
        // expansion
        assertTrue(element.getLocalName().equals("library"));
        assertNull(element.getNamespace());

```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName2Unqualified.java`
#### Snippet
```java
        String result = writer.toString();

        assertTrue(element.getLocalName().equals("library"));
        assertNull(element.getNamespace());
        // Make sure that the serialized string does not contain default prefix declaration
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertFalse()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestComplete.java`
#### Snippet
```java
        // But OMSE's are self-contained...therefore the root
        // should still be complete
        assertTrue(!child.isComplete());
        assertTrue(root.isComplete());

```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertFalse()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestComplete.java`
#### Snippet
```java
        child.getFirstOMChild(); // causes partial parsing...i.e. incomplete child

        assertTrue(!child.isComplete());
        assertTrue(root.isComplete());
    }
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName1QualifiedPrefix.java`
#### Snippet
```java

        // Test getting the local name and namespace URI. This should used not result in expansion
        assertTrue(element.getLocalName().equals("library"));
        assertTrue(
                element.getNamespace()
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName1QualifiedPrefix.java`
#### Snippet
```java
        // Test getting the local name and namespace URI. This should used not result in expansion
        assertTrue(element.getLocalName().equals("library"));
        assertTrue(
                element.getNamespace()
                        .getNamespaceURI()
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName1QualifiedPrefix.java`
#### Snippet
```java
        String result = writer.toString();

        assertTrue(element.getLocalName().equals("library"));
        assertTrue(
                element.getNamespace()
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName1QualifiedPrefix.java`
#### Snippet
```java

        assertTrue(element.getLocalName().equals("library"));
        assertTrue(
                element.getNamespace()
                        .getNamespaceURI()
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName1QualifiedPrefix.java`
#### Snippet
```java
                        .getNamespaceURI()
                        .equals("http://www.sosnoski.com/uwjws/library"));
        assertTrue(element.getNamespace().getPrefix().equals("pre"));
        assertTrue(element.getDefaultNamespace() == null);
        // Make sure that the serialized string does not contain default prefix declaration
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertNull()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName1QualifiedPrefix.java`
#### Snippet
```java
                        .equals("http://www.sosnoski.com/uwjws/library"));
        assertTrue(element.getNamespace().getPrefix().equals("pre"));
        assertTrue(element.getDefaultNamespace() == null);
        // Make sure that the serialized string does not contain default prefix declaration
        assertTrue(result.indexOf("xmlns=") < 0);
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName1QualifiedPrefix.java`
#### Snippet
```java
        result = writer.toString();

        assertTrue(element.getLocalName().equals("library"));
        assertTrue(
                element.getNamespace()
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName1QualifiedPrefix.java`
#### Snippet
```java

        assertTrue(element.getLocalName().equals("library"));
        assertTrue(
                element.getNamespace()
                        .getNamespaceURI()
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName1QualifiedPrefix.java`
#### Snippet
```java
                        .getNamespaceURI()
                        .equals("http://www.sosnoski.com/uwjws/library"));
        assertTrue(element.getNamespace().getPrefix().equals("pre"));
        // Make sure that the serialized string does not contain default prefix declaration
        assertTrue(result.indexOf("xmlns=") < 0);
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertNull()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName1QualifiedPrefix.java`
#### Snippet
```java
        // Make sure that the serialized string does not contain default prefix declaration
        assertTrue(result.indexOf("xmlns=") < 0);
        assertTrue(element.getDefaultNamespace() == null);
        assertTrue("Serialized text error" + result, result.indexOf("1930110111") > 0);
    }
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName3DefaultPrefix.java`
#### Snippet
```java
        // Test getting the namespace, localpart and prefix.  This should used not result in
        // expansion
        assertTrue(element.getLocalName().equals("DUMMYNAME"));
        assertTrue(element.getNamespace().getNamespaceURI().equals("http://DUMMYNS"));
        assertTrue(element.getNamespace().getPrefix().equals("DUMMYPREFIX"));
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName3DefaultPrefix.java`
#### Snippet
```java
        // expansion
        assertTrue(element.getLocalName().equals("DUMMYNAME"));
        assertTrue(element.getNamespace().getNamespaceURI().equals("http://DUMMYNS"));
        assertTrue(element.getNamespace().getPrefix().equals("DUMMYPREFIX"));

```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName3DefaultPrefix.java`
#### Snippet
```java
        assertTrue(element.getLocalName().equals("DUMMYNAME"));
        assertTrue(element.getNamespace().getNamespaceURI().equals("http://DUMMYNS"));
        assertTrue(element.getNamespace().getPrefix().equals("DUMMYPREFIX"));

        // Serialize and cache.  This should cause expansion and update the name to match the
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName3DefaultPrefix.java`
#### Snippet
```java
        String result = writer.toString();

        assertTrue(element.getLocalName().equals("library"));
        assertTrue(
                element.getNamespace()
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName3DefaultPrefix.java`
#### Snippet
```java

        assertTrue(element.getLocalName().equals("library"));
        assertTrue(
                element.getNamespace()
                        .getNamespaceURI()
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName3DefaultPrefix.java`
#### Snippet
```java
                        .getNamespaceURI()
                        .equals("http://www.sosnoski.com/uwjws/library"));
        assertTrue(element.getNamespace().getPrefix().equals(""));
        // Make sure that the serialized string does not contain the DUMMY values
        assertTrue(result.indexOf("DUMMY") < 0);
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName3DefaultPrefix.java`
#### Snippet
```java
        result = writer.toString();

        assertTrue(element.getLocalName().equals("library"));
        assertTrue(
                element.getNamespace()
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName3DefaultPrefix.java`
#### Snippet
```java

        assertTrue(element.getLocalName().equals("library"));
        assertTrue(
                element.getNamespace()
                        .getNamespaceURI()
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName3DefaultPrefix.java`
#### Snippet
```java
                        .getNamespaceURI()
                        .equals("http://www.sosnoski.com/uwjws/library"));
        assertTrue(element.getNamespace().getPrefix().equals(""));
        // Make sure that the serialized string does not contain the DUMMY values
        assertTrue(result.indexOf("DUMMY") < 0);
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/faultdetail/TestWSCommons202.java`
#### Snippet
```java
        soapFaultDetail.setText("a");

        assertTrue(soapFaultDetail.getText().trim().equals("a"));
        assertTrue(
                "Text serialization has problems. It had serialized same text twice",
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/faultdetail/TestWSCommons202.java`
#### Snippet
```java

        assertTrue(soapFaultDetail.getText().trim().equals("a"));
        assertTrue(
                "Text serialization has problems. It had serialized same text twice",
                soapFaultDetail.toString().indexOf("aa") == -1);
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/headerblock/TestGetRoleWithParser.java`
#### Snippet
```java
        Iterator<SOAPHeaderBlock> iterator = envelope.getHeader().examineAllHeaderBlocks();
        iterator.next();
        assertTrue(
                "SOAP HeaderBlock Test With Parser : - getRole method returns incorrect role value",
                iterator.next().getRole().equals(spec.getNextRoleURI()));
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertFalse()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/headerblock/TestBlobOMDataSource.java`
#### Snippet
```java
        assertTrue("Expected OMSourcedElement child", firstChild instanceof SOAPHeaderBlock);
        SOAPHeaderBlock child = (SOAPHeaderBlock) firstChild;
        assertTrue("OMSourcedElement is expanded.  This is unexpected", !child.isExpanded());
        assertThat(child.getDataSource()).isSameInstanceAs(ds);

```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertFalse()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/headerblock/TestBlobOMDataSource.java`
#### Snippet
```java

        // Make sure that getting the MustUnderstand property does not cause expansion.
        assertTrue(!child.getMustUnderstand());
        assertTrue("OMSourcedElement is expanded.  This is unexpected", !child.isExpanded());
        assertThat(child.getDataSource()).isSameInstanceAs(ds);
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertFalse()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/headerblock/TestBlobOMDataSource.java`
#### Snippet
```java
        // Make sure that getting the MustUnderstand property does not cause expansion.
        assertTrue(!child.getMustUnderstand());
        assertTrue("OMSourcedElement is expanded.  This is unexpected", !child.isExpanded());
        assertThat(child.getDataSource()).isSameInstanceAs(ds);

```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertFalse()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/headerblock/TestBlobOMDataSource.java`
#### Snippet
```java
        XMLStreamReader reader = child.getXMLStreamReader();
        reader.next();
        assertTrue("OMSourcedElement is expanded.  This is unexpected", !child.isExpanded());

        // Likewise, a BlobOMDataSource does not consume the backing object when
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertFalse()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/headerblock/TestBlobOMDataSource.java`
#### Snippet
```java
                "The payload was not present in the output",
                soapHeader.toString().indexOf(payload) > 0);
        assertTrue("OMSourcedElement is expanded.  This is unexpected", !child.isExpanded());

        assertThat(child.getDataSource()).isSameInstanceAs(ds);
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap11/faultreason/TestGetTextWithCDATA.java`
#### Snippet
```java
        SOAPFault fault = se.getBody().getFault();
        SOAPFaultReason reason = fault.getReason();
        assertTrue(
                reason.getText()
                        .equals(
```

### SimplifiableAssertion
`assertFalse()` can be simplified to 'assertNotNull()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/header/TestExamineMustUnderstandHeaderBlocks.java`
#### Snippet
```java
        assertTrue(iterator.hasNext());
        SOAPHeaderBlock headerBlock = (SOAPHeaderBlock) iterator.next();
        assertFalse(
                "SOAP Header Test : - examineMustUnderstandHeaderBlocks method returns empty iterator",
                headerBlock == null);
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/header/TestExamineMustUnderstandHeaderBlocks.java`
#### Snippet
```java
                "SOAP Header Test : - examineMustUnderstandHeaderBlocks method returns empty iterator",
                headerBlock == null);
        assertTrue(
                "SOAP Header Test : - HeaderBlock local name mismatch",
                headerBlock.getLocalName().equals("echoOk2"));
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/header/TestExamineMustUnderstandHeaderBlocks.java`
#### Snippet
```java
                "SOAP Header Test : - HeaderBlock local name mismatch",
                headerBlock.getLocalName().equals("echoOk2"));
        assertTrue(
                "SOAP Header Test : - HeaderBlock role value mismatch",
                headerBlock
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertSame()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/fault/TestMoreChildrenAddition.java`
#### Snippet
```java
        envelope.getBody().getFault().setCode(code);

        assertTrue(
                "Parent Value of Code has not been set to new fault",
                code.getParent() == envelope.getBody().getFault());
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertNotSame()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/fault/TestMoreChildrenAddition.java`
#### Snippet
```java
                "Parent Value of Code has not been set to new fault",
                code.getParent() == envelope.getBody().getFault());
        assertTrue(
                "Parent Value of Code is still pointing to old fault",
                code.getParent() != soapEnvelope.getBody().getFault());
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertNotNull()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/mtom/TestGetXMLStreamReaderMTOMEncoded.java`
#### Snippet
```java
            }
        }
        assertTrue(blob != null);

        // Make sure next event is an an XOP_Include END element
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/mtom/TestGetXMLStreamReaderMTOMEncoded.java`
#### Snippet
```java
        xmlStreamReader.next();
        assertTrue(xmlStreamReader.isEndElement());
        assertTrue(XOP_INCLUDE.equals(xmlStreamReader.getName()));

        // Make sure the next event is the end tag of name
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/mtom/TestGetXMLStreamReaderMTOMEncoded.java`
#### Snippet
```java
        xmlStreamReader.next();
        assertTrue(xmlStreamReader.isEndElement());
        assertTrue("name".equals(xmlStreamReader.getLocalName()));
    }
}
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap11/builder/TestBuilder.java`
#### Snippet
```java
        //          writer.flush();

        assertTrue(
                "SOAP 1.1 :- envelope local name mismatch",
                soap11Envelope.getLocalName().equals(SOAPConstants.SOAPENVELOPE_LOCAL_NAME));
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap11/builder/TestBuilder.java`
#### Snippet
```java
                "SOAP 1.1 :- envelope local name mismatch",
                soap11Envelope.getLocalName().equals(SOAPConstants.SOAPENVELOPE_LOCAL_NAME));
        assertTrue(
                "SOAP 1.1 :- envelope namespace uri mismatch",
                soap11Envelope
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap11/builder/TestBuilder.java`
#### Snippet
```java

        SOAPHeader header = soap11Envelope.getHeader();
        assertTrue(
                "SOAP 1.1 :- Header local name mismatch",
                header.getLocalName().equals(SOAPConstants.HEADER_LOCAL_NAME));
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap11/builder/TestBuilder.java`
#### Snippet
```java
                "SOAP 1.1 :- Header local name mismatch",
                header.getLocalName().equals(SOAPConstants.HEADER_LOCAL_NAME));
        assertTrue(
                "SOAP 1.1 :- Header namespace uri mismatch",
                header.getNamespace()
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap11/builder/TestBuilder.java`
#### Snippet
```java

        SOAPHeaderBlock headerBlock = (SOAPHeaderBlock) header.getFirstElement();
        assertTrue(
                "SOAP 1.1 :- Header block name mismatch",
                headerBlock.getLocalName().equals("echoOk"));
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap11/builder/TestBuilder.java`
#### Snippet
```java
                "SOAP 1.1 :- Header block name mismatch",
                headerBlock.getLocalName().equals("echoOk"));
        assertTrue(
                "SOAP 1.1 :- Header block name space uri mismatch",
                headerBlock.getNamespace().getNamespaceURI().equals("http://example.org/ts-tests"));
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap11/builder/TestBuilder.java`
#### Snippet
```java
                "SOAP 1.1 :- Header block name space uri mismatch",
                headerBlock.getNamespace().getNamespaceURI().equals("http://example.org/ts-tests"));
        assertTrue(
                "SOAP 1.1 :- Headaer block text mismatch",
                headerBlock.getText().trim().equals("foo"));
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertNotNull()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap11/builder/TestBuilder.java`
#### Snippet
```java
        OMAttribute mustUnderstandAttribute = headerBlock.getAttribute(mustUnderstandQName);

        assertTrue(
                "SOAP 1.1 :- Mustunderstand attribute not found", mustUnderstandAttribute != null);
        assertTrue(
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap11/builder/TestBuilder.java`
#### Snippet
```java
        assertTrue(
                "SOAP 1.1 :- Mustunderstand attribute not found", mustUnderstandAttribute != null);
        assertTrue(
                "SOAP 1.1 :- Mustunderstand value mismatch",
                mustUnderstandAttribute
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap11/builder/TestBuilder.java`
#### Snippet
```java
                        .getAttributeValue()
                        .equals(SOAPConstants.ATTR_MUSTUNDERSTAND_1));
        assertTrue(
                "SOAP 1.1 :- Mustunderstand attribute namespace uri mismatch",
                mustUnderstandAttribute
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertNotNull()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap11/builder/TestBuilder.java`
#### Snippet
```java
                        .equals(SOAP11Constants.SOAP_ENVELOPE_NAMESPACE_URI));

        assertTrue("SOAP 1.1 :- Actor attribute name not found", actorAttribute != null);
        assertTrue(
                "SOAP 1.1 :- Actor value mismatch",
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap11/builder/TestBuilder.java`
#### Snippet
```java

        assertTrue("SOAP 1.1 :- Actor attribute name not found", actorAttribute != null);
        assertTrue(
                "SOAP 1.1 :- Actor value mismatch",
                actorAttribute
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap11/builder/TestBuilder.java`
#### Snippet
```java
                                        + "/"
                                        + "next"));
        assertTrue(
                "SOAP 1.1 :- Actor attribute namespace uri mismatch",
                actorAttribute
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap11/builder/TestBuilder.java`
#### Snippet
```java

        SOAPBody body = soap11Envelope.getBody();
        assertTrue(
                "SOAP 1.1 :- Body local name mismatch",
                body.getLocalName().equals(SOAPConstants.BODY_LOCAL_NAME));
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap11/builder/TestBuilder.java`
#### Snippet
```java
                "SOAP 1.1 :- Body local name mismatch",
                body.getLocalName().equals(SOAPConstants.BODY_LOCAL_NAME));
        assertTrue(
                "SOAP 1.1 :- Body namespace uri mismatch",
                body.getNamespace()
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap11/builder/TestBuilder.java`
#### Snippet
```java

        SOAPFault fault = body.getFault();
        assertTrue(
                "SOAP 1.1 :- Fault namespace uri mismatch",
                fault.getNamespace()
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap11/builder/TestBuilder.java`
#### Snippet
```java
        iteratorInFault.next();
        SOAPFaultReason reason = (SOAPFaultReason) iteratorInFault.next();
        assertTrue(
                "SOAP 1.1 :- Fault string local name mismatch",
                reason.getLocalName().equals(SOAP11Constants.SOAP_FAULT_STRING_LOCAL_NAME));
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap11/builder/TestBuilder.java`
#### Snippet
```java
                "SOAP 1.1 :- Fault string local name mismatch",
                reason.getLocalName().equals(SOAP11Constants.SOAP_FAULT_STRING_LOCAL_NAME));
        assertTrue(
                "SOAP 1.1 :- Fault string value mismatch",
                reason.getText().trim().equals("Sender Timeout"));
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap11/builder/TestBuilder.java`
#### Snippet
```java
        iteratorInFault.next();
        SOAPFaultRole role = (SOAPFaultRole) iteratorInFault.next();
        assertTrue(
                "SOAP 1.1 :- Fault actor local name mismatch",
                role.getLocalName().equals(SOAP11Constants.SOAP_FAULT_ACTOR_LOCAL_NAME));
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap11/builder/TestBuilder.java`
#### Snippet
```java
                "SOAP 1.1 :- Fault actor local name mismatch",
                role.getLocalName().equals(SOAP11Constants.SOAP_FAULT_ACTOR_LOCAL_NAME));
        assertTrue(
                "SOAP 1.1 :- Actor value mismatch",
                role.getText()
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap11/builder/TestBuilder.java`
#### Snippet
```java
        iteratorInFault.next();
        SOAPFaultDetail detail = (SOAPFaultDetail) iteratorInFault.next();
        assertTrue(
                "SOAP 1.1 :- Fault detail local name mismatch",
                detail.getLocalName().equals(SOAP11Constants.SOAP_FAULT_DETAIL_LOCAL_NAME));
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap11/builder/TestBuilder.java`
#### Snippet
```java
                "SOAP 1.1 :- Fault detail local name mismatch",
                detail.getLocalName().equals(SOAP11Constants.SOAP_FAULT_DETAIL_LOCAL_NAME));
        assertTrue(
                "SOAP 1.2 :- Text in detail mismatch",
                detail.getText().trim().equals("Details of error"));
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap11/builder/TestBuilder.java`
#### Snippet
```java
        iteratorInDetail.next();
        OMElement element1 = (OMElement) iteratorInDetail.next();
        assertTrue(
                "SOAP 1.1 :- MaxTime element mismatch", element1.getLocalName().equals("MaxTime"));
        assertTrue(
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap11/builder/TestBuilder.java`
#### Snippet
```java
        assertTrue(
                "SOAP 1.1 :- MaxTime element mismatch", element1.getLocalName().equals("MaxTime"));
        assertTrue(
                "SOAP 1.1 :- MaxTime element namespace mismatch",
                element1.getNamespace().getNamespaceURI().equals("http:www.sample.org"));
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap11/builder/TestBuilder.java`
#### Snippet
```java
                "SOAP 1.1 :- MaxTime element namespace mismatch",
                element1.getNamespace().getNamespaceURI().equals("http:www.sample.org"));
        assertTrue(
                "SOAP 1.1 :- Text value in MaxTime element mismatch",
                element1.getText().trim().equals("P5M"));
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap11/builder/TestBuilder.java`
#### Snippet
```java
        Iterator<OMAttribute> attributeIterator = element1.getAllAttributes();
        OMAttribute attributeInMaxTime = attributeIterator.next();
        assertTrue(
                "SOAP 1.1 :- Attribute local name mismatch",
                attributeInMaxTime.getLocalName().equals("detail"));
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap11/builder/TestBuilder.java`
#### Snippet
```java
                "SOAP 1.1 :- Attribute local name mismatch",
                attributeInMaxTime.getLocalName().equals("detail"));
        assertTrue(
                "SOAP 1.1 :- Attribute namespace mismatch",
                attributeInMaxTime.getNamespace().getNamespaceURI().equals("http:www.sample.org"));
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap11/builder/TestBuilder.java`
#### Snippet
```java
                "SOAP 1.1 :- Attribute namespace mismatch",
                attributeInMaxTime.getNamespace().getNamespaceURI().equals("http:www.sample.org"));
        assertTrue(
                "SOAP 1.1 :- Attribute value mismatch",
                attributeInMaxTime.getAttributeValue().equals("This is only a test"));
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap11/builder/TestBuilder.java`
#### Snippet
```java
        iteratorInDetail.next();
        OMElement element2 = (OMElement) iteratorInDetail.next();
        assertTrue(
                "SOAP 1.1 :- AveTime element mismatch", element2.getLocalName().equals("AveTime"));
        assertTrue(
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap11/builder/TestBuilder.java`
#### Snippet
```java
        assertTrue(
                "SOAP 1.1 :- AveTime element mismatch", element2.getLocalName().equals("AveTime"));
        assertTrue(
                "SOAP 1.1 :- AveTime element namespace mismatch",
                element2.getNamespace().getNamespaceURI().equals("http:www.sample.org"));
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap11/builder/TestBuilder.java`
#### Snippet
```java
        iteratorInAveTimeElement.next();
        OMElement element21 = (OMElement) iteratorInAveTimeElement.next();
        assertTrue("SOAP 1.1 :- Time element mismatch", element21.getLocalName().equals("Time"));
        assertTrue(
                "SOAP 1.1 :- Time element namespace mismatch",
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap11/builder/TestBuilder.java`
#### Snippet
```java
        OMElement element21 = (OMElement) iteratorInAveTimeElement.next();
        assertTrue("SOAP 1.1 :- Time element mismatch", element21.getLocalName().equals("Time"));
        assertTrue(
                "SOAP 1.1 :- Time element namespace mismatch",
                element21.getNamespace().getNamespaceURI().equals("http:www.sample.org"));
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap11/builder/TestBuilder.java`
#### Snippet
```java
                "SOAP 1.1 :- Time element namespace mismatch",
                element21.getNamespace().getNamespaceURI().equals("http:www.sample.org"));
        assertTrue(
                "SOAP 1.1 :- Text value in Time element mismatch",
                element21.getText().trim().equals("P3M"));
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap11/builder/TestBuilder.java`
#### Snippet
```java
        iteratorInFault.next();
        OMElement testElement = (OMElement) iteratorInFault.next();
        assertTrue("SOAP 1.1 :- Test element mismatch", testElement.getLocalName().equals("Test"));
        assertTrue(
                "SOAP 1.1 :- Test element namespace mismatch",
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap11/builder/TestBuilder.java`
#### Snippet
```java
        OMElement testElement = (OMElement) iteratorInFault.next();
        assertTrue("SOAP 1.1 :- Test element mismatch", testElement.getLocalName().equals("Test"));
        assertTrue(
                "SOAP 1.1 :- Test element namespace mismatch",
                testElement.getNamespace().getNamespaceURI().equals("http:www.Test.org"));
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap11/builder/TestBuilder.java`
#### Snippet
```java

        OMElement childOfTestElement = testElement.getFirstElement();
        assertTrue(
                "SOAP 1.1 :- Test element child local name mismatch",
                childOfTestElement.getLocalName().equals("TestElement"));
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap11/builder/TestBuilder.java`
#### Snippet
```java
                "SOAP 1.1 :- Test element child local name mismatch",
                childOfTestElement.getLocalName().equals("TestElement"));
        assertTrue(
                "SOAP 1.1 :- Test element child namespace mismatch",
                childOfTestElement.getNamespace().getNamespaceURI().equals("http:www.Test.org"));
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap11/builder/TestBuilder.java`
#### Snippet
```java
                "SOAP 1.1 :- Test element child namespace mismatch",
                childOfTestElement.getNamespace().getNamespaceURI().equals("http:www.Test.org"));
        assertTrue(
                "SOAP 1.1 :- Test element child value mismatch",
                childOfTestElement.getText().trim().equals("This is only a test"));
```

### SimplifiableAssertion
`assertFalse()` can be simplified to 'assertNotNull()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/faultreason/TestAddSOAPText.java`
#### Snippet
```java
        SOAPFaultReason faultReason = soapFactory.createSOAPFaultReason(fault);
        faultReason.addSOAPText(soapFactory.createSOAPFaultText(faultReason));
        assertFalse(
                "SOAP 1.2 FaultReason Test : - After calling addSOAPText, getFirstSOAPText returns null",
                faultReason.getFirstSOAPText() == null);
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertNull()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/faultreason/TestGetFirstSOAPText.java`
#### Snippet
```java
        SOAPFault fault = soapFactory.createSOAPFault();
        SOAPFaultReason faultReason = soapFactory.createSOAPFaultReason(fault);
        assertTrue(
                "SOAP 1.2 FaultReason Test : - After creating SOAP12FaultReason, it has a SOAPFaultText",
                faultReason.getFirstSOAPText() == null);
```

### SimplifiableAssertion
`assertFalse()` can be simplified to 'assertNotNull()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/faultreason/TestGetFirstSOAPText.java`
#### Snippet
```java
                faultReason.getFirstSOAPText() == null);
        faultReason.addSOAPText(soapFactory.createSOAPFaultText(faultReason));
        assertFalse(
                "SOAP 1.2 FaultReason Test : - After calling addSOAPText, getFirstSOAPText returns null",
                faultReason.getFirstSOAPText() == null);
```

### SimplifiableAssertion
`assertFalse()` can be simplified to 'assertNotNull()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/faultreason/TestGetFirstSOAPTextWithParser.java`
#### Snippet
```java
    protected void runTest(SOAPEnvelope envelope) throws Throwable {
        SOAPFaultReason faultReason = envelope.getBody().getFault().getReason();
        assertFalse(
                "SOAP 1.2 FaultReason Test With Parser : - getFirstSOAPText method returns null",
                faultReason.getFirstSOAPText() == null);
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/faulttext/TestGetLangWithParser.java`
#### Snippet
```java
    protected void runTest(SOAPEnvelope envelope) throws Throwable {
        SOAPFaultText faultText = envelope.getBody().getFault().getReason().getFirstSOAPText();
        assertTrue(
                "SOAP 1.2 Fault Text Test With Parser : - getLang method returns incorrect string",
                faultText.getLang().equals("en"));
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/faulttext/TestGetLangWithParser.java`
#### Snippet
```java
                faultText.getLang().equals("en"));
        OMAttribute langAttribute = faultText.getAllAttributes().next();
        assertTrue(
                "SOAP 1.2 Fault Text Test With Parser : - Lang attribute local name mismaatch",
                langAttribute
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/faulttext/TestGetLangWithParser.java`
#### Snippet
```java
                        .getLocalName()
                        .equals(SOAP12Constants.SOAP_FAULT_TEXT_LANG_ATTR_LOCAL_NAME));
        assertTrue(
                "SOAP 1.2 Fault Text Test With Parser : - Lang attribute namespace prefix mismatch",
                langAttribute
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/faulttext/TestGetLangWithParser.java`
#### Snippet
```java
                        .getPrefix()
                        .equals(SOAP12Constants.SOAP_FAULT_TEXT_LANG_ATTR_NS_PREFIX));
        assertTrue(
                "SOAP 1.2 Fault Text Test With Parser : - Lang attribute namespace uri mismatch",
                langAttribute
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/builder/TestBuilder.java`
#### Snippet
```java
        SOAPEnvelope soap12Envelope = (SOAPEnvelope) soap12Builder.getDocumentElement();

        assertTrue(
                "SOAP 1.2 :- envelope local name mismatch",
                soap12Envelope.getLocalName().equals(SOAPConstants.SOAPENVELOPE_LOCAL_NAME));
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/builder/TestBuilder.java`
#### Snippet
```java
                "SOAP 1.2 :- envelope local name mismatch",
                soap12Envelope.getLocalName().equals(SOAPConstants.SOAPENVELOPE_LOCAL_NAME));
        assertTrue(
                "SOAP 1.2 :- envelope namespace uri mismatch",
                soap12Envelope
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/builder/TestBuilder.java`
#### Snippet
```java

        SOAPHeader header = soap12Envelope.getHeader();
        assertTrue(
                "SOAP 1.2 :- Header local name mismatch",
                header.getLocalName().equals(SOAPConstants.HEADER_LOCAL_NAME));
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/builder/TestBuilder.java`
#### Snippet
```java
                "SOAP 1.2 :- Header local name mismatch",
                header.getLocalName().equals(SOAPConstants.HEADER_LOCAL_NAME));
        assertTrue(
                "SOAP 1.2 :- Header namespace uri mismatch",
                header.getNamespace()
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/builder/TestBuilder.java`
#### Snippet
```java

        SOAPHeaderBlock headerBlock = (SOAPHeaderBlock) header.getFirstElement();
        assertTrue(
                "SOAP 1.2 :- Header block name mismatch",
                headerBlock.getLocalName().equals("echoOk"));
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/builder/TestBuilder.java`
#### Snippet
```java
                "SOAP 1.2 :- Header block name mismatch",
                headerBlock.getLocalName().equals("echoOk"));
        assertTrue(
                "SOAP 1.2 :- Header block name space uri mismatch",
                headerBlock.getNamespace().getNamespaceURI().equals("http://example.org/ts-tests"));
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertNotNull()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/builder/TestBuilder.java`
#### Snippet
```java
        OMAttribute mustUnderstandAttribute = headerBlock.getAttribute(mustUnderstandQName);

        assertTrue("SOAP 1.2 :- Role attribute name not found", roleAttribute != null);

        assertTrue(
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/builder/TestBuilder.java`
#### Snippet
```java
        assertTrue("SOAP 1.2 :- Role attribute name not found", roleAttribute != null);

        assertTrue(
                "SOAP 1.2 :- Role value mismatch",
                roleAttribute
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertNotNull()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/builder/TestBuilder.java`
#### Snippet
```java
                                        + "ultimateReceiver"));

        assertTrue(
                "SOAP 1.2 :- Mustunderstand attribute not found", mustUnderstandAttribute != null);

```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/builder/TestBuilder.java`
#### Snippet
```java
                "SOAP 1.2 :- Mustunderstand attribute not found", mustUnderstandAttribute != null);

        assertTrue(
                "SOAP 1.2 :- Mustunderstand value mismatch",
                mustUnderstandAttribute
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/builder/TestBuilder.java`
#### Snippet
```java

        SOAPBody body = soap12Envelope.getBody();
        assertTrue(
                "SOAP 1.2 :- Body local name mismatch",
                body.getLocalName().equals(SOAPConstants.BODY_LOCAL_NAME));
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/builder/TestBuilder.java`
#### Snippet
```java
                "SOAP 1.2 :- Body local name mismatch",
                body.getLocalName().equals(SOAPConstants.BODY_LOCAL_NAME));
        assertTrue(
                "SOAP 1.2 :- Body namespace uri mismatch",
                body.getNamespace()
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/builder/TestBuilder.java`
#### Snippet
```java

        SOAPFault fault = body.getFault();
        assertTrue(
                "SOAP 1.2 :- Fault local name mismatch",
                fault.getLocalName().equals(SOAPConstants.SOAPFAULT_LOCAL_NAME));
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/builder/TestBuilder.java`
#### Snippet
```java
                "SOAP 1.2 :- Fault local name mismatch",
                fault.getLocalName().equals(SOAPConstants.SOAPFAULT_LOCAL_NAME));
        assertTrue(
                "SOAP 1.2 :- Fault namespace uri mismatch",
                fault.getNamespace()
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/builder/TestBuilder.java`
#### Snippet
```java
        iteratorInFault.next();
        SOAPFaultCode code = (SOAPFaultCode) iteratorInFault.next();
        assertTrue(
                "SOAP 1.2 :- Fault code local name mismatch",
                code.getLocalName().equals(SOAP12Constants.SOAP_FAULT_CODE_LOCAL_NAME));
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/builder/TestBuilder.java`
#### Snippet
```java
                "SOAP 1.2 :- Fault code local name mismatch",
                code.getLocalName().equals(SOAP12Constants.SOAP_FAULT_CODE_LOCAL_NAME));
        assertTrue(
                "SOAP 1.2 :- Fault code namespace uri mismatch",
                code.getNamespace()
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/builder/TestBuilder.java`
#### Snippet
```java
        iteratorInCode.next();
        SOAPFaultValue value1 = (SOAPFaultValue) iteratorInCode.next();
        assertTrue(
                "SOAP 1.2 :- Fault code value local name mismatch",
                value1.getLocalName().equals(SOAP12Constants.SOAP_FAULT_VALUE_LOCAL_NAME));
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/builder/TestBuilder.java`
#### Snippet
```java
                "SOAP 1.2 :- Fault code value local name mismatch",
                value1.getLocalName().equals(SOAP12Constants.SOAP_FAULT_VALUE_LOCAL_NAME));
        assertTrue(
                "SOAP 1.2 :- Fault code namespace uri mismatch",
                value1.getNamespace()
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/builder/TestBuilder.java`
#### Snippet
```java
                        .getNamespaceURI()
                        .equals(SOAP12Constants.SOAP_ENVELOPE_NAMESPACE_URI));
        assertTrue("SOAP 1.2 :- Value1 text mismatch", value1.getText().equals("env:Sender"));

        QName valueQName = value1.getTextAsQName();
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/builder/TestBuilder.java`
#### Snippet
```java

        QName valueQName = value1.getTextAsQName();
        assertTrue(
                "SOAP 1.2 :- Fault code value's qname local name mismatch",
                valueQName.getLocalPart().equals("Sender"));
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/builder/TestBuilder.java`
#### Snippet
```java
                valueQName.getLocalPart().equals("Sender"));

        assertTrue(
                "SOAP 1.2 :- Fault code value's qname namespace uri mismatch",
                valueQName.getNamespaceURI().equals(SOAP12Constants.SOAP_ENVELOPE_NAMESPACE_URI));
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/builder/TestBuilder.java`
#### Snippet
```java
        iteratorInCode.next();
        SOAPFaultSubCode subCode1 = (SOAPFaultSubCode) iteratorInCode.next();
        assertTrue(
                "SOAP 1.2 :- Fault sub code local name mismatch",
                subCode1.getLocalName().equals(SOAP12Constants.SOAP_FAULT_SUB_CODE_LOCAL_NAME));
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/builder/TestBuilder.java`
#### Snippet
```java
                "SOAP 1.2 :- Fault sub code local name mismatch",
                subCode1.getLocalName().equals(SOAP12Constants.SOAP_FAULT_SUB_CODE_LOCAL_NAME));
        assertTrue(
                "SOAP 1.2 :- Fault subcode namespace uri mismatch",
                subCode1.getNamespace()
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/builder/TestBuilder.java`
#### Snippet
```java
        iteratorInSubCode1.next();
        SOAPFaultValue value2 = (SOAPFaultValue) iteratorInSubCode1.next();
        assertTrue(
                "SOAP 1.2 :- Fault code value local name mismatch",
                value2.getLocalName().equals(SOAP12Constants.SOAP_FAULT_VALUE_LOCAL_NAME));
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/builder/TestBuilder.java`
#### Snippet
```java
                "SOAP 1.2 :- Fault code value local name mismatch",
                value2.getLocalName().equals(SOAP12Constants.SOAP_FAULT_VALUE_LOCAL_NAME));
        assertTrue(
                "SOAP 1.2 :- Fault code namespace uri mismatch",
                value2.getNamespace()
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/builder/TestBuilder.java`
#### Snippet
```java
                        .getNamespaceURI()
                        .equals(SOAP12Constants.SOAP_ENVELOPE_NAMESPACE_URI));
        assertTrue("SOAP 1.2 :- Value2 text mismatch", value2.getText().equals("m:MessageTimeout"));

        iteratorInSubCode1.next();
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/builder/TestBuilder.java`
#### Snippet
```java
        iteratorInSubCode1.next();
        SOAPFaultSubCode subCode2 = (SOAPFaultSubCode) iteratorInSubCode1.next();
        assertTrue(
                "SOAP 1.2 :- Fault sub code local name mismatch",
                subCode2.getLocalName().equals(SOAP12Constants.SOAP_FAULT_SUB_CODE_LOCAL_NAME));
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/builder/TestBuilder.java`
#### Snippet
```java
                "SOAP 1.2 :- Fault sub code local name mismatch",
                subCode2.getLocalName().equals(SOAP12Constants.SOAP_FAULT_SUB_CODE_LOCAL_NAME));
        assertTrue(
                "SOAP 1.2 :- Fault subcode namespace uri mismatch",
                subCode2.getNamespace()
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/builder/TestBuilder.java`
#### Snippet
```java
        iteratorInSubCode2.next();
        SOAPFaultValue value3 = (SOAPFaultValue) iteratorInSubCode2.next();
        assertTrue(
                "SOAP 1.2 :- Fault code value local name mismatch",
                value3.getLocalName().equals(SOAP12Constants.SOAP_FAULT_VALUE_LOCAL_NAME));
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/builder/TestBuilder.java`
#### Snippet
```java
                "SOAP 1.2 :- Fault code value local name mismatch",
                value3.getLocalName().equals(SOAP12Constants.SOAP_FAULT_VALUE_LOCAL_NAME));
        assertTrue(
                "SOAP 1.2 :- Fault code namespace uri mismatch",
                value3.getNamespace()
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/builder/TestBuilder.java`
#### Snippet
```java
                        .getNamespaceURI()
                        .equals(SOAP12Constants.SOAP_ENVELOPE_NAMESPACE_URI));
        assertTrue("SOAP 1.2 :- Value2 text mismatch", value3.getText().equals("m:MessageTimeout"));

        iteratorInFault.next();
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/builder/TestBuilder.java`
#### Snippet
```java
        iteratorInFault.next();
        SOAPFaultReason reason = (SOAPFaultReason) iteratorInFault.next();
        assertTrue(
                "SOAP 1.2 :- Fault reason local name mismatch",
                reason.getLocalName().equals(SOAP12Constants.SOAP_FAULT_REASON_LOCAL_NAME));
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/builder/TestBuilder.java`
#### Snippet
```java
                "SOAP 1.2 :- Fault reason local name mismatch",
                reason.getLocalName().equals(SOAP12Constants.SOAP_FAULT_REASON_LOCAL_NAME));
        assertTrue(
                "SOAP 1.2 :- Fault reason namespace uri mismatch",
                reason.getNamespace()
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/builder/TestBuilder.java`
#### Snippet
```java
        iteratorInReason.next();
        SOAPFaultText text = (SOAPFaultText) iteratorInReason.next();
        assertTrue(
                "SOAP 1.2 :- Fault text local name mismatch",
                text.getLocalName().equals(SOAP12Constants.SOAP_FAULT_TEXT_LOCAL_NAME));
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/builder/TestBuilder.java`
#### Snippet
```java
                "SOAP 1.2 :- Fault text local name mismatch",
                text.getLocalName().equals(SOAP12Constants.SOAP_FAULT_TEXT_LOCAL_NAME));
        assertTrue(
                "SOAP 1.2 :- Text namespace uri mismatch",
                text.getNamespace()
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/builder/TestBuilder.java`
#### Snippet
```java
                        .getNamespaceURI()
                        .equals(SOAP12Constants.SOAP_ENVELOPE_NAMESPACE_URI));
        assertTrue("SOAP 1.2 :- Text value mismatch", text.getText().equals("Sender Timeout"));

        iteratorInFault.next();
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/builder/TestBuilder.java`
#### Snippet
```java
        iteratorInFault.next();
        SOAPFaultNode node = (SOAPFaultNode) iteratorInFault.next();
        assertTrue(
                "SOAP 1.2 :- Fault node local name mismatch",
                node.getLocalName().equals(SOAP12Constants.SOAP_FAULT_NODE_LOCAL_NAME));
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/builder/TestBuilder.java`
#### Snippet
```java
                "SOAP 1.2 :- Fault node local name mismatch",
                node.getLocalName().equals(SOAP12Constants.SOAP_FAULT_NODE_LOCAL_NAME));
        assertTrue(
                "SOAP 1.2 :- Fault node namespace uri mismatch",
                node.getNamespace()
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/builder/TestBuilder.java`
#### Snippet
```java
                        .getNamespaceURI()
                        .equals(SOAP12Constants.SOAP_ENVELOPE_NAMESPACE_URI));
        assertTrue(
                "SOAP 1.2 :- Node value mismatch",
                node.getText()
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/builder/TestBuilder.java`
#### Snippet
```java
        iteratorInFault.next();
        SOAPFaultRole role = (SOAPFaultRole) iteratorInFault.next();
        assertTrue(
                "SOAP 1.2 :- Fault role local name mismatch",
                role.getLocalName().equals(SOAP12Constants.SOAP_FAULT_ROLE_LOCAL_NAME));
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/builder/TestBuilder.java`
#### Snippet
```java
                "SOAP 1.2 :- Fault role local name mismatch",
                role.getLocalName().equals(SOAP12Constants.SOAP_FAULT_ROLE_LOCAL_NAME));
        assertTrue(
                "SOAP 1.2 :- Fault role namespace uri mismatch",
                role.getNamespace()
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/builder/TestBuilder.java`
#### Snippet
```java
                        .getNamespaceURI()
                        .equals(SOAP12Constants.SOAP_ENVELOPE_NAMESPACE_URI));
        assertTrue(
                "SOAP 1.2 :- Role value mismatch",
                role.getText().trim().equals("ultimateReceiver"));
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/builder/TestBuilder.java`
#### Snippet
```java
        iteratorInFault.next();
        SOAPFaultDetail detail = (SOAPFaultDetail) iteratorInFault.next();
        assertTrue(
                "SOAP 1.2 :- Fault detail local name mismatch",
                detail.getLocalName().equals(SOAP12Constants.SOAP_FAULT_DETAIL_LOCAL_NAME));
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/builder/TestBuilder.java`
#### Snippet
```java
                "SOAP 1.2 :- Fault detail local name mismatch",
                detail.getLocalName().equals(SOAP12Constants.SOAP_FAULT_DETAIL_LOCAL_NAME));
        assertTrue(
                "SOAP 1.2 :- Fault detail namespace uri mismatch",
                detail.getNamespace()
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/builder/TestBuilder.java`
#### Snippet
```java
                        .equals(SOAP12Constants.SOAP_ENVELOPE_NAMESPACE_URI));

        assertTrue(
                "SOAP 1.2 :- Text in detail mismatch",
                detail.getText().trim().equals("Details of error"));
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/builder/TestBuilder.java`
#### Snippet
```java
        iteratorInDetail.next();
        OMElement element1 = (OMElement) iteratorInDetail.next();
        assertTrue(
                "SOAP 1.2 :- MaxTime element mismatch", element1.getLocalName().equals("MaxTime"));
        assertTrue(
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/builder/TestBuilder.java`
#### Snippet
```java
        assertTrue(
                "SOAP 1.2 :- MaxTime element mismatch", element1.getLocalName().equals("MaxTime"));
        assertTrue(
                "SOAP 1.2 :- MaxTime element namespace mismatch",
                element1.getNamespace().getNamespaceURI().equals("http:www.sample.org"));
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/builder/TestBuilder.java`
#### Snippet
```java
                "SOAP 1.2 :- MaxTime element namespace mismatch",
                element1.getNamespace().getNamespaceURI().equals("http:www.sample.org"));
        assertTrue(
                "SOAP 1.2 :- Text value in MaxTime element mismatch",
                element1.getText().trim().equals("P5M"));
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/builder/TestBuilder.java`
#### Snippet
```java
        Iterator<OMAttribute> attributeIterator = element1.getAllAttributes();
        OMAttribute attributeInMaxTime = attributeIterator.next();
        assertTrue(
                "SOAP 1.2 :- Attribute local name mismatch",
                attributeInMaxTime.getLocalName().equals("detail"));
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/builder/TestBuilder.java`
#### Snippet
```java
                "SOAP 1.2 :- Attribute local name mismatch",
                attributeInMaxTime.getLocalName().equals("detail"));
        assertTrue(
                "SOAP 1.2 :- Attribute namespace mismatch",
                attributeInMaxTime.getNamespace().getNamespaceURI().equals("http:www.sample.org"));
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/builder/TestBuilder.java`
#### Snippet
```java
                "SOAP 1.2 :- Attribute namespace mismatch",
                attributeInMaxTime.getNamespace().getNamespaceURI().equals("http:www.sample.org"));
        assertTrue(
                "SOAP 1.2 :- Attribute value mismatch",
                attributeInMaxTime.getAttributeValue().trim().equals("This is only a test"));
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/builder/TestBuilder.java`
#### Snippet
```java
        iteratorInDetail.next();
        OMElement element2 = (OMElement) iteratorInDetail.next();
        assertTrue(
                "SOAP 1.2 :- AveTime element mismatch", element2.getLocalName().equals("AveTime"));
        assertTrue(
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/builder/TestBuilder.java`
#### Snippet
```java
        assertTrue(
                "SOAP 1.2 :- AveTime element mismatch", element2.getLocalName().equals("AveTime"));
        assertTrue(
                "SOAP 1.2 :- AveTime element namespace mismatch",
                element2.getNamespace().getNamespaceURI().equals("http:www.sample.org"));
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/builder/TestBuilder.java`
#### Snippet
```java
        iteratorInAveTimeElement.next();
        OMElement element21 = (OMElement) iteratorInAveTimeElement.next();
        assertTrue("SOAP 1.2 :- Time element mismatch", element21.getLocalName().equals("Time"));
        assertTrue(
                "SOAP 1.2 :- Time element namespace mismatch",
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/builder/TestBuilder.java`
#### Snippet
```java
        OMElement element21 = (OMElement) iteratorInAveTimeElement.next();
        assertTrue("SOAP 1.2 :- Time element mismatch", element21.getLocalName().equals("Time"));
        assertTrue(
                "SOAP 1.2 :- Time element namespace mismatch",
                element21.getNamespace().getNamespaceURI().equals("http:www.sample.org"));
```

### SimplifiableAssertion
`assertTrue()` can be simplified to 'assertEquals()'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/builder/TestBuilder.java`
#### Snippet
```java
                "SOAP 1.2 :- Time element namespace mismatch",
                element21.getNamespace().getNamespaceURI().equals("http:www.sample.org"));
        assertTrue(
                "SOAP 1.2 :- Text value in Time element mismatch",
                element21.getText().trim().equals("P3M"));
```

## RuleId[id=RedundantCollectionOperation]
### RedundantCollectionOperation
`iterator` can be replaced with 'Collections.emptyIterator()'
in `testing/spring-ws-testsuite/src/main/java/org/apache/axiom/ts/springws/soap/messagefactory/TransportInputStreamImpl.java`
#### Snippet
```java
            return Collections.singleton(sample.getContentType()).iterator();
        } else {
            return Collections.<String>emptySet().iterator();
        }
    }
```

### RedundantCollectionOperation
`iterator` can be replaced with 'Collections.emptyIterator()'
in `axiom-api/src/main/java/org/apache/axiom/util/namespace/MapBasedNamespaceContext.java`
#### Snippet
```java
            return Collections.singleton("").iterator();
        } else {
            return Collections.<String>emptyList().iterator();
        }
    }
```

### RedundantCollectionOperation
`iterator` can be replaced with 'Collections.emptyIterator()'
in `mixins/core-mixins/src/main/java/org/apache/axiom/core/impl/AttributeIterator.java`
#### Snippet
```java
        }
        if (attribute == null) {
            return Collections.<S>emptyList().iterator();
        } else {
            return new AttributeIterator<T, S>(attribute, type, mapper, semantics);
```

## RuleId[id=DuplicatedCode]
### DuplicatedCode
Duplicated code
in `axiom-api/src/main/java/org/apache/axiom/om/OMOutputFormat.java`
#### Snippet
```java
        StringBuffer sb = new StringBuffer();
        sb.append("multipart/related");
        sb.append("; ");
        sb.append("boundary=");
        // The value of the boundary parameter must be enclosed in double quotation  
        // marks, according to the Basic Profile 2.0 Specification, Rule R1109:
        // "Parameters on the Content-Type MIME header field-value in a request 
        // MESSAGE MUST be a quoted string."
        sb.append("\"");
        sb.append(getMimeBoundary());
        sb.append("\"");
        sb.append("; ");
```

### DuplicatedCode
Duplicated code
in `axiom-api/src/main/java/org/apache/axiom/om/ds/AbstractOMDataSource.java`
#### Snippet
```java
        ByteArrayOutputStream baos = new ByteArrayOutputStream();
        OMOutputFormat format = new OMOutputFormat();
        format.setCharSetEncoding(encoding);
        try {
            serialize(baos, format);
        } catch (XMLStreamException ex) {
            throw new OMException(ex);
        }
        return baos.toByteArray();
```

### DuplicatedCode
Duplicated code
in `axiom-api/src/main/java/org/apache/axiom/om/util/StAXUtils.java`
#### Snippet
```java
                    if (props != null) {
                        for (Map.Entry<String,Object> entry : props.entrySet()) {
                            factory.setProperty(entry.getKey(), entry.getValue());
                        }
                    }
                    StAXDialect dialect = StAXDialectDetector.getDialect(factory);
                    if (configuration != null) {
                        factory = configuration.configure(factory, dialect);
                    }
```

### DuplicatedCode
Duplicated code
in `axiom-api/src/main/java/org/apache/axiom/util/base64/Base64Utils.java`
#### Snippet
```java
            if (ch == Base64Constants.S_BASE64PAD || ch < Base64Constants.S_DECODETABLE.length
                    && Base64Constants.S_DECODETABLE[ch] >= 0) {
                ibuf[ibufcount++] = ch;
                if (ibufcount == ibuf.length) {
                    ibufcount = 0;
                    int obufcount = decode0(ibuf, obuf, 0);
                    ostream.write(obuf, 0, obufcount);
                }
            }
```

### DuplicatedCode
Duplicated code
in `axiom-api/src/main/java/org/apache/axiom/util/base64/Base64Utils.java`
#### Snippet
```java
            int i = ((data[rindex] & 0xff) << 16)
                    + ((data[rindex + 1] & 0xff) << 8)
                    + (data[rindex + 2] & 0xff);
            out[0] = (char)Base64Constants.S_BASE64CHAR[i >> 18];
            out[1] = (char)Base64Constants.S_BASE64CHAR[(i >> 12) & 0x3f];
            out[2] = (char)Base64Constants.S_BASE64CHAR[(i >> 6) & 0x3f];
            out[3] = (char)Base64Constants.S_BASE64CHAR[i & 0x3f];
```

### DuplicatedCode
Duplicated code
in `axiom-api/src/main/java/org/apache/axiom/util/base64/Base64Utils.java`
#### Snippet
```java
            int i = ((data[rindex] & 0xff) << 8) + (data[rindex + 1] & 0xff);
            out[0] = (char)Base64Constants.S_BASE64CHAR[i >> 10];
            out[1] = (char)Base64Constants.S_BASE64CHAR[(i >> 4) & 0x3f];
            out[2] = (char)Base64Constants.S_BASE64CHAR[(i << 2) & 0x3f];
            out[3] = (char)Base64Constants.S_BASE64PAD;
```

### DuplicatedCode
Duplicated code
in `axiom-api/src/main/java/org/apache/axiom/util/base64/Base64Utils.java`
#### Snippet
```java
            int i = ((data[rindex] & 0xff) << 16)
                    + ((data[rindex + 1] & 0xff) << 8)
                    + (data[rindex + 2] & 0xff);
            out[0] = Base64Constants.S_BASE64CHAR[i >> 18];
            out[1] = Base64Constants.S_BASE64CHAR[(i >> 12) & 0x3f];
            out[2] = Base64Constants.S_BASE64CHAR[(i >> 6) & 0x3f];
            out[3] = Base64Constants.S_BASE64CHAR[i & 0x3f];
```

### DuplicatedCode
Duplicated code
in `axiom-api/src/main/java/org/apache/axiom/util/base64/Base64Utils.java`
#### Snippet
```java
            int i = data[rindex] & 0xff;
            out[0] = Base64Constants.S_BASE64CHAR[i >> 2];
            out[1] = Base64Constants.S_BASE64CHAR[(i << 4) & 0x3f];
            out[2] = Base64Constants.S_BASE64PAD;
            out[3] = Base64Constants.S_BASE64PAD;
```

### DuplicatedCode
Duplicated code
in `axiom-api/src/main/java/org/apache/axiom/util/base64/Base64Utils.java`
#### Snippet
```java
            int i = ((data[rindex] & 0xff) << 8) + (data[rindex + 1] & 0xff);
            out[0] = Base64Constants.S_BASE64CHAR[i >> 10];
            out[1] = Base64Constants.S_BASE64CHAR[(i >> 4) & 0x3f];
            out[2] = Base64Constants.S_BASE64CHAR[(i << 2) & 0x3f];
            out[3] = Base64Constants.S_BASE64PAD;
```

### DuplicatedCode
Duplicated code
in `axiom-api/src/main/java/org/apache/axiom/util/namespace/ScopedNamespaceContext.java`
#### Snippet
```java
        if (bindings == prefixArray.length) {
            int len = prefixArray.length;
            int newLen = len*2;
            String[] newPrefixArray = new String[newLen];
            System.arraycopy(prefixArray, 0, newPrefixArray, 0, len);
            String[] newUriArray = new String[newLen];
            System.arraycopy(uriArray, 0, newUriArray, 0, len);
            prefixArray = newPrefixArray;
            uriArray = newUriArray;
        }
        prefixArray[bindings] = prefix;
        uriArray[bindings] = namespaceURI;
        bindings++;
```

### DuplicatedCode
Duplicated code
in `axiom-api/src/main/java/org/apache/axiom/util/namespace/ScopedNamespaceContext.java`
#### Snippet
```java
        outer: for (int i=bindings-1; i>=0; i--) {
            if (namespaceURI.equals(uriArray[i])) {
                String prefix = prefixArray[i];
                // Now check that the prefix is not masked
                for (int j=i+1; j<bindings; j++) {
                    if (prefix.equals(prefixArray[j])) {
                        continue outer;
                    }
                }
                return prefix;
            }
        }
        return null;
```

### DuplicatedCode
Duplicated code
in `axiom-api/src/main/java/org/apache/axiom/util/namespace/ScopedNamespaceContext.java`
#### Snippet
```java
        return new Iterator<String>() {
            private int binding = bindings;
            private String next;

            @Override
            public boolean hasNext() {
                if (next == null) {
                    outer: while (--binding >= 0) {
                        if (namespaceURI.equals(uriArray[binding])) {
                            String prefix = prefixArray[binding];
                            // Now check that the prefix is not masked
                            for (int j=binding+1; j<bindings; j++) {
                                if (prefix.equals(prefixArray[j])) {
                                    continue outer;
                                }
                            }
                            next = prefix;
                            break;
                        }
                    }
                }
                return next != null;
            }

            @Override
            public String next() {
                if (hasNext()) {
                    String result = next;
                    next = null;
                    return result;
                } else {
                    throw new NoSuchElementException();
                }
            }

            @Override
            public void remove() {
                throw new UnsupportedOperationException();
            }
        };
```

### DuplicatedCode
Duplicated code
in `axiom-compat/src/main/java/org/apache/axiom/attachments/utils/ByteSearch.java`
#### Snippet
```java
                if (bytes[i] == search[0]) {
                    // Potential match..check remaining bytes
                    boolean found = true;  // assume found
                    for (int i2=1; found && i2<search.length; i2++) {
                        if (i+i2 >= end) {
                            found = false;
                        } else {
                            found = (bytes[i+i2] == search[i2]); 
                        }
                    }
                    // If found match, set return idx
                    if (found) {
                        idx = i;
                    }
                }
```

### DuplicatedCode
Duplicated code
in `axiom-compat/src/main/java/org/apache/axiom/om/impl/traverse/OMChildrenLegacyQNameIterator.java`
#### Snippet
```java
        String localPart = searchQName.getLocalPart();
        boolean localNameMatch =(localPart == null) || (localPart.equals("")) ||
            ((currentQName != null) && currentQName.getLocalPart().equals(localPart));
        
        String namespaceURI = searchQName.getNamespaceURI();
        boolean namespaceURIMatch = (namespaceURI.equals(""))||
            ((currentQName != null) && currentQName.getNamespaceURI().equals(namespaceURI));
        return localNameMatch && namespaceURIMatch;
```

### DuplicatedCode
Duplicated code
in `axiom-compat/src/main/java/org/apache/axiom/util/blob/OverflowBlob.java`
#### Snippet
```java
                while (len > 0) {

                    byte[] chunk = getCurrentChunk();

                    // Determine number of bytes that can be copied to the current chunk.
                    int c = Math.min(len, chunkSize-chunkOffset);
                    // Copy data to the chunk.
                    System.arraycopy(b, off, chunk, chunkOffset, c);

                    // Update variables.
                    len -= c;
                    off += c;
                    chunkOffset += c;
                    if (chunkOffset == chunkSize) {
                        chunkIndex++;
                        chunkOffset = 0;
                    }
                }
```

### DuplicatedCode
Duplicated code
in `axiom-compat/src/main/java/org/apache/axiom/util/blob/OverflowBlob.java`
#### Snippet
```java
            if (len == 0) {
                return 0;
            }

            int read = 0;
            while (len > 0 && !(currentChunkIndex == chunkIndex
                    && currentChunkOffset == chunkOffset)) {

                int c;
                if (currentChunkIndex == chunkIndex) {
                    // The current chunk is the last one => take into account the offset
                    c = Math.min(len, chunkOffset-currentChunkOffset);
                } else {
                    c = Math.min(len, chunkSize-currentChunkOffset);
                }

                // Copy the data.
                System.arraycopy(chunks[currentChunkIndex], currentChunkOffset, b, off, c);

                // Update variables
                len -= c;
                off += c;
                currentChunkOffset += c;
                read += c;
                if (currentChunkOffset == chunkSize) {
                    currentChunkIndex++;
                    currentChunkOffset = 0;
                }
            }

            if (read == 0) {
                // We didn't read anything (and the len argument was not 0) => we reached the end of the buffer.
                return -1;
            } else {
                return read;
            }
```

### DuplicatedCode
Duplicated code
in `axiom-compat/src/main/java/org/apache/axiom/util/blob/OverflowBlob.java`
#### Snippet
```java
            int available = available();
            int c = n < available ? (int)n : available;
            int newOffset = currentChunkOffset + c;
            int chunkDelta = newOffset/chunkSize;
            currentChunkIndex += chunkDelta;
            currentChunkOffset = newOffset - (chunkDelta*chunkSize);
            return c;
```

### DuplicatedCode
Duplicated code
in `axiom-compat/src/main/java/org/apache/axiom/util/blob/OverflowBlob.java`
#### Snippet
```java
            int c;
            try {
                int len = chunkSize-chunkOffset;
                if (len > toRead) {
                    len = (int)toRead;
                }
                c = in.read(getCurrentChunk(), chunkOffset, len);
            } catch (IOException ex) {
                throw new StreamCopyException(StreamCopyException.READ, ex);
            }
            if (c == -1) {
                break;
            }
            read += c;
            toRead -= c;
            chunkOffset += c;
```

### DuplicatedCode
Duplicated code
in `axiom-compat/src/main/java/org/apache/axiom/util/blob/OverflowBlob.java`
#### Snippet
```java
                        if (c2 == -1) {
                            break;
                        }
                        try {
                            fileOutputStream.write(buf, 0, c2);
                        } catch (IOException ex) {
                            throw new StreamCopyException(StreamCopyException.WRITE, ex);
                        }
                        read += c2;
                        toRead -= c2;
```

### DuplicatedCode
Duplicated code
in `axiom-compat/src/main/java/org/apache/axiom/util/blob/OverflowBlob.java`
#### Snippet
```java
            try {
                for (int i=0; i<chunkIndex; i++) {
                    out.write(chunks[i]);
                }
                if (chunkOffset > 0) {
                    out.write(chunks[chunkIndex], 0, chunkOffset);
                }
            } catch (IOException ex) {
                throw new StreamCopyException(StreamCopyException.WRITE, ex);
            }
```

### DuplicatedCode
Duplicated code
in `axiom-compat/src/main/java/org/apache/axiom/util/stax/AbstractXMLStreamReader.java`
#### Snippet
```java
                for (int i=0; i<text.length(); i++) {
                    char c = text.charAt(i);
                    if (c != ' ' && c != '\t' && c != '\r' && c != '\n') {
                        return false;
                    }
                }
```

### DuplicatedCode
Duplicated code
in `axiom-compat/src/main/java/org/apache/axiom/util/stax/XMLEventUtils.java`
#### Snippet
```java
        String state = null;
        switch(event) {
        case XMLStreamConstants.START_ELEMENT:
            state = "START_ELEMENT";
            break;
        case XMLStreamConstants.START_DOCUMENT:
            state = "START_DOCUMENT";
            break;
        case XMLStreamConstants.CHARACTERS:
            state = "CHARACTERS";
            break;
        case XMLStreamConstants.CDATA:
            state = "CDATA";
            break;
        case XMLStreamConstants.END_ELEMENT:
            state = "END_ELEMENT";
            break;
        case XMLStreamConstants.END_DOCUMENT:
            state = "END_DOCUMENT";
            break;
        case XMLStreamConstants.SPACE:
            state = "SPACE";
            break;
        case XMLStreamConstants.COMMENT:
            state = "COMMENT";
            break;
        case XMLStreamConstants.DTD:
            state = "DTD";
            break;
        case XMLStreamConstants.PROCESSING_INSTRUCTION:
            state = "PROCESSING_INSTRUCTION";
            break;
        case XMLStreamConstants.ENTITY_REFERENCE:
            state = "ENTITY_REFERENCE";
            break;
        default :
            state = "UNKNOWN_STATE: " + event;
        }
        return state;
```

### DuplicatedCode
Duplicated code
in `components/core-streams/src/main/java/org/apache/axiom/core/stream/NamespaceRepairingFilterHandler.java`
#### Snippet
```java
        super.startElement(namespaceURI, localName, prefix);
        if (scopes == scopeStack.length) {
            int[] newScopeStack = new int[scopeStack.length * 2];
            System.arraycopy(scopeStack, 0, newScopeStack, 0, scopeStack.length);
            scopeStack = newScopeStack;
        }
        scopeStack[scopes++] = bindings;
```

### DuplicatedCode
Duplicated code
in `components/core-streams/src/main/java/org/apache/axiom/core/stream/serializer/Serializer.java`
#### Snippet
```java
            DTDprolog();
            writer.write("<!ENTITY ");
            writer.write(name);
            if (publicId != null) {
                writer.write(" PUBLIC \"");
                writer.write(publicId);
            } else {
                writer.write(" SYSTEM \"");
                writer.write(systemId);
            }
```

### DuplicatedCode
Duplicated code
in `components/core-streams/src/main/java/org/apache/axiom/core/stream/serializer/writer/ASCIICompatibleXmlWriter.java`
#### Snippet
```java
            if (c < 128 && highSurrogate == 0) {
                if (bufferPosition == bufferLength) {
                    out.write(buffer, 0, bufferLength);
                    bufferPosition = 0;
                }
                buffer[bufferPosition++] = (byte) c;
            } else {
                this.bufferPosition = bufferPosition;
                internalWrite(c);
                bufferPosition = this.bufferPosition;
                highSurrogate = this.highSurrogate;
            }
```

### DuplicatedCode
Duplicated code
in `components/core-streams/src/main/java/org/apache/axiom/core/stream/serializer/writer/OutputStreamXmlWriter.java`
#### Snippet
```java
        while (length > 0) {
            if (!encoderIn.hasRemaining()) {
                flush(encoderIn);
            }
            int c = Math.min(length, encoderIn.remaining());
            encoderIn.put(src, offset, length);
            offset += c;
            length -= c;
        }
```

### DuplicatedCode
Duplicated code
in `components/core-streams/src/main/java/org/apache/axiom/core/stream/stax/pull/output/StAXPivot.java`
#### Snippet
```java
        switch (state) {
            case STATE_COLLECT_TEXT:
                text = stopCollectingText();
                break;
            case STATE_SKIP_CONTENT:
                state = previousState;
                break;
            default:
                throw new IllegalStateException();
        }
```

### DuplicatedCode
Duplicated code
in `components/core-streams/src/main/java/org/apache/axiom/core/stream/stax/pull/output/StAXPivot.java`
#### Snippet
```java
        if (extensions != null) {
            Object extension = extensions.get(name);
            if (extension != null) {
                return extension;
            }
        }
        if (extensionFactory != null) {
            Object extension = extensionFactory.createExtension(name, this);
            if (extension != null) {
                if (extensions == null) {
                    extensions = new HashMap<String, Object>();
                }
                extensions.put(name, extension);
                return extension;
            }
        }
```

### DuplicatedCode
Duplicated code
in `mixins/core-mixins/src/main/java/org/apache/axiom/core/impl/mixin/CoreChildNodeMixin.java`
#### Snippet
```java
        CoreParentNode parent = coreGetParent();
        if (parent == null) {
            throw new NoParentException("Parent can not be null");
        } else if (this == sibling) {
            throw new SelfRelationshipException("Inserting self as the sibling is not allowed");
        }
        parent.internalCheckNewChild(sibling, null);
        sibling.internalDetach(null, parent);
```

### DuplicatedCode
Duplicated code
in `mixins/dom-mixins/src/main/java/org/apache/axiom/dom/impl/mixin/DOMDocumentMixin.java`
#### Snippet
```java
        int i = NSUtil.validateQualifiedName(qualifiedName);
        String prefix;
        String localName;
        if (i == -1) {
            prefix = "";
            localName = qualifiedName;
        } else {
            prefix = qualifiedName.substring(0, i);
            localName = qualifiedName.substring(i + 1);
        }
```

### DuplicatedCode
Duplicated code
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestDeclareNamespaceWithGeneratedPrefix1.java`
#### Snippet
```java
        assertEquals("urn:ns", ns.getNamespaceURI());
        assertNotNull(ns.getPrefix());
        assertTrue(ns.getPrefix().length() > 0);
        Iterator<OMNamespace> it = element.getAllDeclaredNamespaces();
        assertTrue(it.hasNext());
        OMNamespace ns2 = it.next();
        assertEquals(ns, ns2);
        assertFalse(it.hasNext());
```

### DuplicatedCode
Duplicated code
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestGetChildrenWithName2.java`
#### Snippet
```java
        OMFactory factory = metaFactory.getOMFactory();
        OMNamespace testNamespace = factory.createOMNamespace("http://test.ws.org", "test");
        OMElement documentElement = factory.createOMElement("Employees", testNamespace);
        documentElement.declareNamespace(testNamespace);
        factory.createOMText(documentElement, " ");
        OMElement e = factory.createOMElement("Employee", testNamespace, documentElement);
        e.setText("Apache Developer");

        Iterator<OMElement> childrenIter =
                documentElement.getChildrenWithName(
                        new QName("http://test.ws.org", "Employee", "test"));
```

### DuplicatedCode
Duplicated code
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestGetChildrenWithName4.java`
#### Snippet
```java
        assertThat(getChildrenCount(documentElement.getChildrenWithName(qName))).isEqualTo(1);
        qName = new QName(NS_B, "sample");
        assertThat(getChildrenCount(documentElement.getChildrenWithName(qName))).isEqualTo(1);
        qName = new QName(NS_C, "sample");
        assertThat(getChildrenCount(documentElement.getChildrenWithName(qName))).isEqualTo(0);

        // Test for QName with no namespace.
        // Axiom 1.2.x implemented a legacy behavior where an empty namespace URI was interpreted
        // as a wildcard (see AXIOM-11). In Axiom 1.3.x, the matching is always strict.
        qName = new QName("", "sample");
```

### DuplicatedCode
Duplicated code
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/node/TestInsertSiblingAfterOnChild.java`
#### Snippet
```java
        OMFactory factory = metaFactory.getOMFactory();
        OMElement element = factory.createOMElement("test", null);
        OMText text = factory.createOMText("test");
        element.addChild(text);
        try {
            text.insertSiblingAfter(element);
            fail("Expected OMException");
        } catch (OMException ex) {
            // Expected
        }
```

### DuplicatedCode
Duplicated code
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/node/TestInsertSiblingAfterSameParent.java`
#### Snippet
```java
        OMFactory fac = metaFactory.getOMFactory();
        OMElement parent = fac.createOMElement("test", null);
        OMText text1 = fac.createOMText("text1");
        OMText text2 = fac.createOMText("text2");
        OMText text3 = fac.createOMText("text3");
        parent.addChild(text1);
        parent.addChild(text2);
        parent.addChild(text3);
```

### DuplicatedCode
Duplicated code
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/node/TestInsertSiblingBeforeOnChild.java`
#### Snippet
```java
        OMFactory factory = metaFactory.getOMFactory();
        OMElement element = factory.createOMElement("test", null);
        OMText text = factory.createOMText("test");
        element.addChild(text);
        try {
            text.insertSiblingBefore(element);
            fail("Expected OMException");
        } catch (OMException ex) {
            // Expected
        }
```

### DuplicatedCode
Duplicated code
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestBlobOMDataSource.java`
#### Snippet
```java
        OMElement parent = factory.createOMElement("root", null);
        OMSourcedElement omse = factory.createOMElement(ds, localName, ns);
        parent.addChild(omse);
        OMNode firstChild = parent.getFirstOMChild();
        assertTrue("Expected OMSourcedElement child", firstChild instanceof OMSourcedElement);
        OMSourcedElement child = (OMSourcedElement) firstChild;
        assertTrue("OMSourcedElement is expanded.  This is unexpected", !child.isExpanded());
```

### DuplicatedCode
Duplicated code
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/headerblock/TestBlobOMDataSource.java`
#### Snippet
```java
        assertTrue("OMSourcedElement is expanded.  This is unexpected", !child.isExpanded());
        assertThat(child.getDataSource()).isSameInstanceAs(ds);

        // A BlobOMDataSource does not consume the backing object when read.
        // Thus getting the XMLStreamReader of the BlobOMDataSource should not
        // cause expansion of the OMSourcedElement.
        XMLStreamReader reader = child.getXMLStreamReader();
        reader.next();
        assertTrue("OMSourcedElement is expanded.  This is unexpected", !child.isExpanded());

        // Likewise, a BlobOMDataSource does not consume the backing object when
        // written.  Thus serializing the OMSourcedElement should not cause the expansion
        // of the OMSourcedElement.
        assertTrue(
                "The payload was not present in the output",
                soapHeader.toString().indexOf(payload) > 0);
        assertTrue("OMSourcedElement is expanded.  This is unexpected", !child.isExpanded());
```

### DuplicatedCode
Duplicated code
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestBlobOMDataSource.java`
#### Snippet
```java
        assertThat(child.getDataSource()).isSameInstanceAs(ds);

        // A BlobOMDataSource does not consume the backing object when read.
        // Thus getting the XMLStreamReader of the BlobOMDataSource should not
        // cause expansion of the OMSourcedElement.
        XMLStreamReader reader = child.getXMLStreamReader();
        reader.next();
        assertTrue("OMSourcedElement is expanded.  This is unexpected", !child.isExpanded());

        // Likewise, a BlobOMDataSource does not consume the backing object when
        // written.  Thus serializing the OMSourcedElement should not cause the expansion
        // of the OMSourcedElement.
        assertTrue(
                "The payload was not present in the output",
                parent.toString().indexOf(payload) > 0);
        assertTrue("OMSourcedElement is expanded.  This is unexpected", !child.isExpanded());
```

### DuplicatedCode
Duplicated code
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName1DefaultPrefix.java`
#### Snippet
```java
        OMElement element =
                f.createOMElement(
                        new PullOMDataSource(TestDocument.DOCUMENT1.getContent()), "library", ns);
        OMElement root = f.createOMElement("root", rootNS);
        root.addChild(element);

        // Test getting the local name and namespace URI. This should used not result in expansion
        assertTrue(element.getLocalName().equals("library"));
        assertTrue(
                element.getNamespace()
                        .getNamespaceURI()
                        .equals("http://www.sosnoski.com/uwjws/library"));
```

### DuplicatedCode
Duplicated code
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName1QualifiedPrefix.java`
#### Snippet
```java
        OMElement element =
                f.createOMElement(
                        new PullOMDataSource(TestDocument.DOCUMENT2.getContent()), "library", ns);
        OMElement root = f.createOMElement("root", rootNS);
        root.addChild(element);

        // Test getting the local name and namespace URI. This should used not result in expansion
        assertTrue(element.getLocalName().equals("library"));
        assertTrue(
                element.getNamespace()
                        .getNamespaceURI()
                        .equals("http://www.sosnoski.com/uwjws/library"));
```

### DuplicatedCode
Duplicated code
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName1QualifiedPrefix.java`
#### Snippet
```java
        assertTrue(result.indexOf("xmlns=") < 0);
        assertTrue("Serialized text error" + result, result.indexOf("1930110111") > 0);

        // Serialize again
        writer = new StringWriter();
        root.serialize(writer);
        result = writer.toString();

        assertTrue(element.getLocalName().equals("library"));
```

### DuplicatedCode
Duplicated code
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName1Unqualified.java`
#### Snippet
```java
        OMFactory f = metaFactory.getOMFactory();

        // Create OMSE with a DUMMYPREFIX prefix even though the underlying element uses the default
        // prefix
        OMNamespace rootNS = f.createOMNamespace("http://sampleroot", "rootPrefix");
        OMNamespace ns = f.createOMNamespace("", "");
        OMElement element =
                f.createOMElement(
                        new PullOMDataSource(TestDocument.DOCUMENT3.getContent()), "library", ns);
        OMElement root = f.createOMElement("root", rootNS);
        root.addChild(element);

        // Test getting the namespace, localpart and prefix.  This should used not result in
        // expansion
        assertTrue(element.getLocalName().equals("library"));
        assertNull(element.getNamespace());

        // Serialize and cache.  This should cause expansion and update the name to match the
        // testDocument string
        StringWriter writer = new StringWriter();
```

### DuplicatedCode
Duplicated code
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName3DefaultPrefix.java`
#### Snippet
```java
        OMFactory f = metaFactory.getOMFactory();

        // Create OMSE with a DUMMYPREFIX prefix even though the underlying element uses the default
        // prefix
        OMNamespace rootNS = f.createOMNamespace("http://sampleroot", "rootPrefix");
        OMNamespace ns = f.createOMNamespace("http://DUMMYNS", "DUMMYPREFIX");
        OMElement element =
                f.createOMElement(
                        new PullOMDataSource(TestDocument.DOCUMENT1.getContent()), "DUMMYNAME", ns);
        OMElement root = f.createOMElement("root", rootNS);
        root.addChild(element);

        // Test getting the namespace, localpart and prefix.  This should used not result in
        // expansion
        assertTrue(element.getLocalName().equals("DUMMYNAME"));
        assertTrue(element.getNamespace().getNamespaceURI().equals("http://DUMMYNS"));
        assertTrue(element.getNamespace().getPrefix().equals("DUMMYPREFIX"));

        // Serialize and cache.  This should cause expansion and update the name to match the
        // testDocument string
        StringWriter writer = new StringWriter();
```

### DuplicatedCode
Duplicated code
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName3QualifiedPrefix.java`
#### Snippet
```java
        assertTrue(element.getNamespace().getPrefix().equals("DUMMYPREFIX"));

        // Serialize and cache.  This should cause expansion and update the name to match the
        // testDocument string
        StringWriter writer = new StringWriter();

        try {
            root.serialize(writer);
        } catch (Exception e) {
            // Current Behavior
            // The current OMSourceElementImpl ensures that the namespace and localName
            // are consistent with the original setting.
            return;
        }

        String result = writer.toString();

        assertTrue(element.getLocalName().equals("library"));
```

### DuplicatedCode
Duplicated code
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName3DefaultPrefix.java`
#### Snippet
```java
        try {
            root.serialize(writer);
        } catch (Exception e) {
            // Current Behavior
            // The current OMSourceElementImpl ensures that the namespace and localName
            // are consistent with the original setting.
            return;
        }

        String result = writer.toString();

        assertTrue(element.getLocalName().equals("library"));
        assertTrue(
                element.getNamespace()
                        .getNamespaceURI()
                        .equals("http://www.sosnoski.com/uwjws/library"));
        assertTrue(element.getNamespace().getPrefix().equals(""));
        // Make sure that the serialized string does not contain the DUMMY values
        assertTrue(result.indexOf("DUMMY") < 0);

        assertTrue("Serialized text error" + result, result.indexOf("1930110111") > 0);

        // Serialize again
        writer = new StringWriter();
        root.serialize(writer);
        result = writer.toString();

        assertTrue(element.getLocalName().equals("library"));
        assertTrue(
                element.getNamespace()
                        .getNamespaceURI()
                        .equals("http://www.sosnoski.com/uwjws/library"));
        assertTrue(element.getNamespace().getPrefix().equals(""));
        // Make sure that the serialized string does not contain the DUMMY values
        assertTrue(result.indexOf("DUMMY") < 0);

        assertTrue("Serialized text error" + result, result.indexOf("1930110111") > 0);
```

### DuplicatedCode
Duplicated code
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName3Unqualified.java`
#### Snippet
```java
        assertTrue(element.getNamespace().getPrefix().equals(""));
        // Make sure that the serialized string does not contain the DUMMY values
        assertTrue(result.indexOf("DUMMY") < 0);

        assertTrue("Serialized text error" + result, result.indexOf("1930110111") > 0);

        // Serialize again
        writer = new StringWriter();
        root.serialize(writer);
        result = writer.toString();

        assertTrue(element.getLocalName().equals("library"));
        assertTrue(
                element.getNamespace()
                        .getNamespaceURI()
                        .equals("http://www.sosnoski.com/uwjws/library"));
        assertTrue(element.getNamespace().getPrefix().equals(""));
        // Make sure that the serialized string does not contain the DUMMY values
        assertTrue(result.indexOf("DUMMY") < 0);

        assertTrue("Serialized text error" + result, result.indexOf("1930110111") > 0);
```

### DuplicatedCode
Duplicated code
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName3QualifiedPrefix.java`
#### Snippet
```java
        OMElement element =
                f.createOMElement(
                        new PullOMDataSource(TestDocument.DOCUMENT2.getContent()), "DUMMYNAME", ns);
        OMElement root = f.createOMElement("root", rootNS);
        root.addChild(element);

        // Test getting the namespace, localpart and prefix.  This should used not result in
        // expansion
        assertTrue(element.getLocalName().equals("DUMMYNAME"));
        assertTrue(element.getNamespace().getNamespaceURI().equals("http://DUMMYNS"));
```

### DuplicatedCode
Duplicated code
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName3Unqualified.java`
#### Snippet
```java
        OMElement element =
                f.createOMElement(
                        new PullOMDataSource(TestDocument.DOCUMENT3.getContent()), "DUMMYNAME", ns);
        OMElement root = f.createOMElement("root", rootNS);
        root.addChild(element);

        // Test getting the namespace, localpart and prefix.  This should used not result in
        // expansion
        assertTrue(element.getLocalName().equals("DUMMYNAME"));
        assertTrue(element.getNamespace().getNamespaceURI().equals("http://DUMMYNS"));
```

### DuplicatedCode
Duplicated code
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestName4QualifiedPrefix.java`
#### Snippet
```java
        assertTrue(element.getNamespace().getPrefix().equals(""));

        // Serialize and consume.  This should not cause expansion and currently won't update
        // the name of the element.
        StringWriter writer = new StringWriter();
        root.serializeAndConsume(writer);
        String result = writer.toString();

        assertTrue(element.getLocalName().equals("DUMMYNAME"));
        assertTrue(element.getNamespace().getNamespaceURI().equals("http://DUMMYNS"));
        assertTrue(element.getNamespace().getPrefix().equals(""));
        // Make sure that the serialized string does not contain the DUMMY values
        assertTrue(result.indexOf("DUMMY") < 0);
        // Make sure that the serialized string does not contain the default prefix declaration
        assertTrue(result.indexOf("xmlns=") < 0);

        assertTrue("Serialized text error" + result, result.indexOf("1930110111") > 0);
```

### DuplicatedCode
Duplicated code
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/push/WriteBlobProviderScenario.java`
#### Snippet
```java
        OMText child = (OMText) element.getFirstOMChild();
        if (blobsPreserved) {
            Assert.assertTrue(child.isBinary());
            Assert.assertSame(blob, child.getBlob());
        } else {
            child.setBinary(true);
            IOTestUtils.compareStreams(blob.getInputStream(), child.getBlob().getInputStream());
        }
```

### DuplicatedCode
Duplicated code
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/omdom/element/TestReplaceChildFirstIncomplete.java`
#### Snippet
```java
        assertSame(a2, b.getPreviousSibling());
        assertSame(b, c.getPreviousSibling());
        assertNull(c.getNextSibling());
        // Check parent-child relations
        assertSame(a2, element.getFirstChild());
        assertSame(c, element.getLastChild());
        assertSame(element, a2.getParentNode());
        assertSame(element, b.getParentNode());
        assertSame(element, c.getParentNode());
        NodeList children = element.getChildNodes();
        assertEquals(3, children.getLength());
        assertSame(a2, children.item(0));
        assertSame(b, children.item(1));
        assertSame(c, children.item(2));
        // Check that a has been detached properly
        assertNull(a.getPreviousSibling());
        assertNull(a.getNextSibling());
        assertNull(a.getParentNode());
```

### DuplicatedCode
Duplicated code
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/omdom/element/TestReplaceChildMiddleIncomplete.java`
#### Snippet
```java
        assertSame(b2, c.getPreviousSibling());
        assertNull(c.getNextSibling());
        // Check parent-child relations
        assertSame(a, element.getFirstChild());
        assertSame(c, element.getLastChild());
        assertSame(element, a.getParentNode());
        assertSame(element, b2.getParentNode());
        assertSame(element, c.getParentNode());
        NodeList children = element.getChildNodes();
        assertEquals(3, children.getLength());
        assertSame(a, children.item(0));
        assertSame(b2, children.item(1));
        assertSame(c, children.item(2));
        // Check that b has been detached properly
        assertNull(b.getPreviousSibling());
        assertNull(b.getNextSibling());
        assertNull(b.getParentNode());
```

### DuplicatedCode
Duplicated code
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/omdom/node/TestInsertSiblingAfterFromForeignDocument.java`
#### Snippet
```java
        DocumentBuilder db =
                ((DOMMetaFactory) metaFactory).newDocumentBuilderFactory().newDocumentBuilder();
        Document document1 = db.newDocument();
        Element element1 = document1.createElementNS(null, "element1");
        Text text = document1.createTextNode("test");
        element1.appendChild(text);
        Document document2 = db.newDocument();
        Element element2 = document2.createElementNS(null, "element2");
```

### DuplicatedCode
Duplicated code
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/envelope/CloneTestCase.java`
#### Snippet
```java
                    Iterator<OMNode> i = ((OMElement) source).getChildren();
                    Iterator<OMNode> j = ((OMElement) target).getChildren();
                    while (i.hasNext() && j.hasNext()) {
                        OMNode sourceChild = i.next();
                        OMNode targetChild = j.next();
                        identityCheck(sourceChild, targetChild, depth + "  ");
                    }
                    assertEquals(
                            "Source and Target have different number of children",
                            i.hasNext(),
                            j.hasNext());
```

### DuplicatedCode
Duplicated code
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/envelope/TestCloneWithSourcedElement1.java`
#### Snippet
```java
        OMDataSource bads =
                new StringOMDataSource(
                        "<tns:payload xmlns:tns=\"urn://test\">Hello World</tns:payload>");
        OMNamespace ns = body.getOMFactory().createOMNamespace("urn://test", "tns");
        OMSourcedElement omse = body.getOMFactory().createOMElement(bads, "payload", ns);
        body.addChild(omse);
        copyAndCheck(sourceEnv);
        assertFalse(omse.isExpanded());
```

### DuplicatedCode
Duplicated code
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/envelope/TestGetSOAPBodyFirstElementLocalNameAndNS.java`
#### Snippet
```java
        assertEquals(qname.getLocalPart(), envelope.getSOAPBodyFirstElementLocalName());
        OMNamespace ns = envelope.getSOAPBodyFirstElementNS();
        if (qname.getNamespaceURI().length() == 0) {
            assertNull(ns);
        } else {
            assertEquals(qname.getNamespaceURI(), ns.getNamespaceURI());
            assertEquals(qname.getPrefix(), ns.getPrefix());
        }
```

### DuplicatedCode
Duplicated code
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/faultdetail/TestAddDetailEntry.java`
#### Snippet
```java
        SOAPEnvelope envelope = soapFactory.createSOAPEnvelope();
        SOAPBody body = soapFactory.createSOAPBody(envelope);
        SOAPFault fault = soapFactory.createSOAPFault(body);
        SOAPFaultDetail soapFaultDetail = soapFactory.createSOAPFaultDetail(fault);
        OMNamespace omNamespace = soapFactory.createOMNamespace("http://www.test.org", "test");
        soapFaultDetail.addDetailEntry(soapFactory.createOMElement("DetailEntry1", omNamespace));
        soapFaultDetail.addDetailEntry(soapFactory.createOMElement("DetailEntry2", omNamespace));
```

### DuplicatedCode
Duplicated code
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap11/faultcode/TestSetValueFromQName.java`
#### Snippet
```java
        code.setValue(new QName("urn:test", "MyFaultCode", "p"));
        assertEquals("p:MyFaultCode", code.getText());
        Iterator<OMNamespace> it = code.getAllDeclaredNamespaces();
        assertTrue(it.hasNext());
        OMNamespace ns = it.next();
        assertEquals("p", ns.getPrefix());
        assertEquals("urn:test", ns.getNamespaceURI());
```

### DuplicatedCode
Duplicated code
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap12/builder/TestBuilder.java`
#### Snippet
```java
        iteratorInCode.next();
        SOAPFaultSubCode subCode1 = (SOAPFaultSubCode) iteratorInCode.next();
        assertTrue(
                "SOAP 1.2 :- Fault sub code local name mismatch",
                subCode1.getLocalName().equals(SOAP12Constants.SOAP_FAULT_SUB_CODE_LOCAL_NAME));
        assertTrue(
                "SOAP 1.2 :- Fault subcode namespace uri mismatch",
                subCode1.getNamespace()
                        .getNamespaceURI()
                        .equals(SOAP12Constants.SOAP_ENVELOPE_NAMESPACE_URI));

        Iterator<OMNode> iteratorInSubCode1 = subCode1.getChildren();

        iteratorInSubCode1.next();
        SOAPFaultValue value2 = (SOAPFaultValue) iteratorInSubCode1.next();
        assertTrue(
                "SOAP 1.2 :- Fault code value local name mismatch",
                value2.getLocalName().equals(SOAP12Constants.SOAP_FAULT_VALUE_LOCAL_NAME));
        assertTrue(
                "SOAP 1.2 :- Fault code namespace uri mismatch",
                value2.getNamespace()
                        .getNamespaceURI()
                        .equals(SOAP12Constants.SOAP_ENVELOPE_NAMESPACE_URI));
        assertTrue("SOAP 1.2 :- Value2 text mismatch", value2.getText().equals("m:MessageTimeout"));
```

### DuplicatedCode
Duplicated code
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/document/TestAdoptNode.java`
#### Snippet
```java
        DocumentBuilder builder = dbf.newDocumentBuilder();
        Document document1 = builder.newDocument();
        Document document2 = builder.newDocument();
        Element element = document1.createElementNS(null, "test");
        Text child = document1.createTextNode("test");
        element.appendChild(child);
        assertSame(element, document2.adoptNode(element));
        assertSame(document2, element.getOwnerDocument());
```

### DuplicatedCode
Duplicated code
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/document/TestAdoptNodeToSameDocument.java`
#### Snippet
```java
        Document document = builder.newDocument();
        Element parent = document.createElementNS(null, "test");
        Text node = document.createTextNode("test");
        parent.appendChild(node);
        assertSame(node, document.adoptNode(node));
        assertSame(document, node.getOwnerDocument());
        assertNull(node.getParentNode());
        assertNull(parent.getFirstChild());
```

### DuplicatedCode
Duplicated code
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/element/TestInsertBeforeWithDocumentFragment.java`
#### Snippet
```java
        Document document = dbf.newDocumentBuilder().newDocument();

        DocumentFragment fragment = document.createDocumentFragment();
        Element x = document.createElementNS(null, "x");
        Element y = document.createElementNS(null, "y");
        fragment.appendChild(x);
        fragment.appendChild(y);
```

### DuplicatedCode
Duplicated code
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/element/TestReplaceChildMiddleWithDocumentFragment.java`
#### Snippet
```java
        NodeList children = element.getChildNodes();
        assertEquals(4, children.getLength());
        assertSame(a, children.item(0));
        assertSame(x, children.item(1));
        assertSame(y, children.item(2));
        assertSame(c, children.item(3));

        assertSame(element, x.getParentNode());
        assertSame(element, y.getParentNode());

        assertNull(fragment.getFirstChild());
        assertNull(fragment.getLastChild());
        assertEquals(0, fragment.getChildNodes().getLength());
```

### DuplicatedCode
Duplicated code
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/element/TestInsertBeforeWithDocumentFragment.java`
#### Snippet
```java
        assertEquals(4, children.getLength());
        assertSame(a, children.item(0));
        assertSame(x, children.item(1));
        assertSame(y, children.item(2));
        assertSame(b, children.item(3));

        assertSame(element, x.getParentNode());
        assertSame(element, y.getParentNode());

        assertNull(fragment.getFirstChild());
        assertNull(fragment.getLastChild());
        assertEquals(0, fragment.getChildNodes().getLength());
```

### DuplicatedCode
Duplicated code
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/element/TestReplaceChildFirst.java`
#### Snippet
```java
        Element parent = doc.createElementNS(null, "parent");
        Element child1 = doc.createElementNS(null, "child1");
        Element child2 = doc.createElementNS(null, "child2");
        parent.appendChild(child1);
        parent.appendChild(child2);
        parent.replaceChild(newChild, child1);
        assertSame(newChild, parent.getFirstChild());
        assertSame(child2, parent.getLastChild());
```

### DuplicatedCode
Duplicated code
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/element/TestReplaceChildMiddle.java`
#### Snippet
```java
        parent.replaceChild(newChild, child2);
        NodeList children = parent.getChildNodes();
        assertEquals(3, children.getLength());
        assertSame(child1, children.item(0));
        assertSame(newChild, children.item(1));
        assertSame(child3, children.item(2));
```

### DuplicatedCode
Duplicated code
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/element/TestReplaceChildMiddle.java`
#### Snippet
```java
        Element parent = doc.createElementNS(null, "parent");
        Element child1 = doc.createElementNS(null, "child1");
        Element child2 = doc.createElementNS(null, "child2");
        Element child3 = doc.createElementNS(null, "child3");
        parent.appendChild(child1);
        parent.appendChild(child2);
        parent.appendChild(child3);
```

### DuplicatedCode
Duplicated code
in `testing/jaxen-testsuite/src/main/java/org/jaxen/test/XPathTestBase.java`
#### Snippet
```java
        log("Document [" + url + "]");
        Object document = nav.getDocument(url);
        XPath contextpath = new BaseXPath("/", nav);
        log("Initial Context :: " + contextpath);
        List list = contextpath.selectNodes(document);
        Iterator iter = list.iterator();
```

### DuplicatedCode
Duplicated code
in `testing/jaxen-testsuite/src/main/java/org/jaxen/test/XPathTestBase.java`
#### Snippet
```java
        Navigator nav = getNavigator();
        String url = "xml/simple.xml";
        log("Document [" + url + "]");
        Object document = nav.getDocument(url);
        XPath contextpath = new BaseXPath("/", nav);
        log("Initial Context :: " + contextpath);
        List list = contextpath.selectNodes(document);
        Iterator iter = list.iterator();
```

### DuplicatedCode
Duplicated code
in `testing/jaxen-testsuite/src/main/java/org/jaxen/test/XPathTestBase.java`
#### Snippet
```java
        Navigator nav = getNavigator();
        String url = "xml/simple.xml";
        log("Document [" + url + "]");
        Object document = nav.getDocument(url);
        XPath contextpath = new BaseXPath("/root/a", nav);
        log("Initial Context :: " + contextpath);
        List list = contextpath.selectNodes(document);
        Iterator iter = list.iterator();
```

### DuplicatedCode
Duplicated code
in `testing/jaxen-testsuite/src/main/java/org/jaxen/test/XPathTestBase.java`
#### Snippet
```java
        Navigator nav = getNavigator();
        String url = "xml/numbers.xml";
        log("Document [" + url + "]");
        Object document = nav.getDocument(url);
        XPath contextpath = new BaseXPath("/", nav);
        log("Initial Context :: " + contextpath);
        List list = contextpath.selectNodes(document);
        Iterator iter = list.iterator();
```

### DuplicatedCode
Duplicated code
in `testing/jaxen-testsuite/src/main/java/org/jaxen/test/XPathTestBase.java`
#### Snippet
```java
        Navigator nav = getNavigator();
        String url = "xml/web.xml";
        log("Document [" + url + "]");
        Object document = nav.getDocument(url);
        XPath contextpath = new BaseXPath("/", nav);
        log("Initial Context :: " + contextpath);
        List list = contextpath.selectNodes(document);
        Iterator iter = list.iterator();
```

### DuplicatedCode
Duplicated code
in `testing/jaxen-testsuite/src/main/java/org/jaxen/test/XPathTestBase.java`
#### Snippet
```java
        Navigator nav = getNavigator();
        String url = "xml/id.xml";
        log("Document [" + url + "]");
        Object document = nav.getDocument(url);
        XPath contextpath = new BaseXPath("/", nav);
        log("Initial Context :: " + contextpath);
        List list = contextpath.selectNodes(document);
```

### DuplicatedCode
Duplicated code
in `testing/jaxen-testsuite/src/main/java/org/jaxen/test/XPathTestBase.java`
#### Snippet
```java
        Navigator nav = getNavigator();
        String url = "xml/axis.xml";
        log("Document [" + url + "]");
        Object document = nav.getDocument(url);
        XPath contextpath = new BaseXPath("/root/a/a.3", nav);
        log("Initial Context :: " + contextpath);
        List list = contextpath.selectNodes(document);
        Iterator iter = list.iterator();
```

### DuplicatedCode
Duplicated code
in `testing/jaxen-testsuite/src/main/java/org/jaxen/test/XPathTestBase.java`
#### Snippet
```java
        Navigator nav = getNavigator();
        String url = "xml/axis.xml";
        log("Document [" + url + "]");
        Object document = nav.getDocument(url);
        XPath contextpath = new BaseXPath("/", nav);
        log("Initial Context :: " + contextpath);
        List list = contextpath.selectNodes(document);
        Iterator iter = list.iterator();
```

### DuplicatedCode
Duplicated code
in `testing/jaxen-testsuite/src/main/java/org/jaxen/test/XPathTestBase.java`
#### Snippet
```java
        Navigator nav = getNavigator();
        String url = "xml/web.xml";
        log("Document [" + url + "]");
        Object document = nav.getDocument(url);
        XPath contextpath = new BaseXPath("/web-app/servlet[1]", nav);
        log("Initial Context :: " + contextpath);
        List list = contextpath.selectNodes(document);
        Iterator iter = list.iterator();
```

### DuplicatedCode
Duplicated code
in `testing/jaxen-testsuite/src/main/java/org/jaxen/test/XPathTestBase.java`
#### Snippet
```java
        Navigator nav = getNavigator();
        String url = "xml/web.xml";
        log("Document [" + url + "]");
        Object document = nav.getDocument(url);
        XPath contextpath = new BaseXPath("/web-app/servlet[2]/servlet-name", nav);
        log("Initial Context :: " + contextpath);
        List list = contextpath.selectNodes(document);
        Iterator iter = list.iterator();
```

### DuplicatedCode
Duplicated code
in `testing/jaxen-testsuite/src/main/java/org/jaxen/test/XPathTestBase.java`
#### Snippet
```java
        Navigator nav = getNavigator();
        String url = "xml/much_ado.xml";
        log("Document [" + url + "]");
        Object document = nav.getDocument(url);
        XPath contextpath = new BaseXPath("/", nav);
        log("Initial Context :: " + contextpath);
        List list = contextpath.selectNodes(document);
        Iterator iter = list.iterator();
```

### DuplicatedCode
Duplicated code
in `testing/jaxen-testsuite/src/main/java/org/jaxen/test/XPathTestBase.java`
#### Snippet
```java
        Navigator nav = getNavigator();
        String url = "xml/text.xml";
        log("Document [" + url + "]");
        Object document = nav.getDocument(url);
        XPath contextpath = new BaseXPath("/", nav);
        log("Initial Context :: " + contextpath);
        List list = contextpath.selectNodes(document);
        Iterator iter = list.iterator();
```

### DuplicatedCode
Duplicated code
in `testing/jaxen-testsuite/src/main/java/org/jaxen/test/XPathTestBase.java`
#### Snippet
```java
        Navigator nav = getNavigator();
        String url = "xml/namespaces.xml";
        log("Document [" + url + "]");
        Object document = nav.getDocument(url);
        XPath contextpath = new BaseXPath("/", nav);
        log("Initial Context :: " + contextpath);
        List list = contextpath.selectNodes(document);
        SimpleNamespaceContext nsContext = new SimpleNamespaceContext();
```

### DuplicatedCode
Duplicated code
in `testing/jaxen-testsuite/src/main/java/org/jaxen/test/XPathTestBase.java`
#### Snippet
```java
        Navigator nav = getNavigator();
        String url = "xml/defaultNamespace.xml";
        log("Document [" + url + "]");
        Object document = nav.getDocument(url);
        XPath contextpath = new BaseXPath("/", nav);
        log("Initial Context :: " + contextpath);
        List list = contextpath.selectNodes(document);
```

### DuplicatedCode
Duplicated code
in `testing/jaxen-testsuite/src/main/java/org/jaxen/test/XPathTestBase.java`
#### Snippet
```java
        Navigator nav = getNavigator();
        String url = "xml/testNamespaces.xml";
        log("Document [" + url + "]");
        Object document = nav.getDocument(url);
        XPath contextpath = new BaseXPath("/", nav);
        log("Initial Context :: " + contextpath);
        List list = contextpath.selectNodes(document);
        Iterator iter = list.iterator();
```

### DuplicatedCode
Duplicated code
in `testing/spring-ws-testsuite/src/main/java/org/apache/axiom/ts/springws/scenario/secureecho/SecureEchoTest.java`
#### Snippet
```java
        DocumentBuilder documentBuilder = DocumentBuilderFactory.newInstance().newDocumentBuilder();
        Document requestDocument = documentBuilder.newDocument();
        Element request = requestDocument.createElementNS("urn:test", "p:Echo");
        request.setTextContent("Hello");
        Document responseDocument = documentBuilder.newDocument();
        context.getBean(WebServiceTemplate.class)
                .sendSourceAndReceiveToResult(
                        new DOMSource(request),
                        new SoapActionCallback("http://www.example.com/echo"),
                        new DOMResult(responseDocument));
        Element response = responseDocument.getDocumentElement();
        assertEquals("urn:test", response.getNamespaceURI());
        assertEquals("Echo", response.getLocalName());
        assertEquals("Hello", response.getTextContent());
```

### DuplicatedCode
Duplicated code
in `testing/testutils/src/main/java/org/apache/axiom/testutils/io/ByteStreamComparator.java`
#### Snippet
```java
        while (len > 0) {
            int c = in.read(compareBuffer, 0, Math.min(compareBuffer.length, len));
            if (c == -1) {
                fail(
                        "The two streams have different lengths: len("
                                + name1
                                + ") = "
                                + position
                                + " < len("
                                + name2
                                + ")");
            }
            for (int i = 0; i < c; i++) {
                if (buffer[off] != compareBuffer[i]) {
                    fail(
                            "Byte mismatch: "
                                    + name1
                                    + "["
                                    + position
                                    + "] = "
                                    + compareBuffer[i]
                                    + " != "
                                    + name2
                                    + "["
                                    + position
                                    + "] = "
                                    + buffer[off]);
                }
                off++;
                len--;
                position++;
            }
        }
```

## RuleId[id=CollectionContainsUrl]
### CollectionContainsUrl
Map `dialectByUrl` may contain URL objects
in `axiom-api/src/main/java/org/apache/axiom/util/stax/dialect/StAXDialectDetector.java`
#### Snippet
```java
     * URL that points to the root folder of the archive.
     */
    private static final Map<URL,StAXDialect> dialectByUrl =
            Collections.synchronizedMap(new HashMap<URL,StAXDialect>());

```

## RuleId[id=ProtectedMemberInFinalClass]
### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `testing/spring-ws-testsuite/src/main/java/org/apache/axiom/ts/springws/soap/messagefactory/TransportInputStreamImpl.java`
#### Snippet
```java

    @Override
    protected InputStream createInputStream() throws IOException {
        return sample.getInputStream();
    }
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/LegacyPartDataHandler.java`
#### Snippet
```java

    @Override
    protected DataSource createDataSource(Blob content, String contentType) {
        if (content instanceof LegacyTempFileBlob) {
            return ((LegacyTempFileBlob)content).getDataSource(contentType);
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/MultipartBodyAdapter.java`
#### Snippet
```java
                .setPartBlobFactory(new PartDataHandlerBlobFactory() {
                        @Override
                        protected PartDataHandler createDataHandler(Part part) {
                            return new LegacyPartDataHandler(part);
                        }
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `components/core-streams/src/main/java/org/apache/axiom/core/stream/serializer/Serializer.java`
#### Snippet
```java

    /** Tells if we're in an internal document type subset. */
    protected boolean inDoctype = false;

    private int context = MIXED_CONTENT;
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `components/core-streams/src/main/java/org/apache/axiom/core/stream/serializer/Serializer.java`
#### Snippet
```java

    /** Add space before '/>' for XHTML. */
    protected boolean spaceBeforeClose = false;

    /** Tells if we're in an internal document type subset. */
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `systests/jaxws-tests/src/test/java/MTOMTest.java`
#### Snippet
```java
        HttpServlet servlet = new HttpServlet() {
            @Override
            protected void doPost(HttpServletRequest request, HttpServletResponse response)
                    throws ServletException, IOException {
                MultipartBody mp = MultipartBody.builder()
```

## RuleId[id=CollectionAddAllCanBeReplacedWithConstructor]
### CollectionAddAllCanBeReplacedWithConstructor
'addAll()' call can be replaced with parametrized constructor call
in `testing/soap-testsuite/src/main/java/org/apache/axiom/ts/soap/SOAPSpec.java`
#### Snippet
```java
    public final String[] getInvalidBooleanLiterals() {
        Set<String> result = new LinkedHashSet<>();
        result.addAll(getReps(getAltSpec().booleanLiterals));
        List<String> valid = getReps(booleanLiterals);
        result.removeAll(valid);
```

## RuleId[id=UnnecessaryToStringCall]
### UnnecessaryToStringCall
Unnecessary `toString()` call
in `axiom-api/src/main/java/org/apache/axiom/om/OMOutputFormat.java`
#### Snippet
```java
        String ct = null;
        if (log.isDebugEnabled()) {
            log.debug("Start getContentType: " + toString());
        }
        if (contentType == null) {
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `axiom-api/src/main/java/org/apache/axiom/om/OMOutputFormat.java`
#### Snippet
```java
        }
        if (log.isDebugEnabled()) {
            log.debug("getContentType= {" + ct + "}   " + toString());
        }
        return ct;
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `mixins/core-mixins/src/main/java/org/apache/axiom/core/impl/mixin/CoreParentNodeMixin.java`
#### Snippet
```java
                                    buffer = new StringBuilder(textContent.toString());
                                }
                                buffer.append(textValue.toString());
                            }
                        }
```

## RuleId[id=InnerClassMayBeStatic]
### InnerClassMayBeStatic
Inner class `OMNamespaceEx` may be 'static'
in `axiom-api/src/main/java/org/apache/axiom/om/xpath/DocumentNavigator.java`
#### Snippet
```java
    }

    class OMNamespaceEx implements OMNamespace {
        final OMNamespace originalNsp;
        final OMContainer parent;
```

### InnerClassMayBeStatic
Inner class `FileDeletor` may be 'static'
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/lifecycle/impl/LifecycleManagerImpl.java`
#### Snippet
```java
    }

    public class FileDeletor implements Runnable{
        int interval;
        File _file;
```

### InnerClassMayBeStatic
Inner class `ByteArray` may be 'static'
in `axiom-compat/src/main/java/org/apache/axiom/om/ds/ByteArrayDataSource.java`
#### Snippet
```java
     * Object containing the byte[]/encoding pair
     */
    public class ByteArray {
        public byte[] bytes;
        public String encoding;
```

### InnerClassMayBeStatic
Inner class `Data` may be 'static'
in `axiom-compat/src/main/java/org/apache/axiom/om/ds/InputStreamDataSource.java`
#### Snippet
```java
     * Object containing the InputStream/encoding pair
     */
    public class Data {
        public String encoding;
        public InputStream is;
```

## RuleId[id=DanglingJavadoc]
### DanglingJavadoc
Dangling Javadoc comment
in `axiom-api/src/main/java/org/apache/axiom/om/OMDataSource.java`
#### Snippet
```java
 */

/**
 * 
 */
```

### DanglingJavadoc
Dangling Javadoc comment
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestDeclareDefaultNamespace2.java`
#### Snippet
```java
    @Override
    protected void runTest() throws Throwable {
        /**
         * <RootElement xmlns:ns1="http://one.org" xmlns:ns2="http://one.org"> <ns2:ChildElementOne
         * xmlns="http://one.org"> <ns2:ChildElementTwo /> </ns2:ChildElementOne> </RootElement>
```

### DanglingJavadoc
Dangling Javadoc comment
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestDeclareDefaultNamespace1.java`
#### Snippet
```java
    @Override
    protected void runTest() throws Throwable {
        /**
         * <RootElement xmlns="http://one.org"> <ns2:ChildElementOne
         * xmlns:ns2="http://ws.apache.org/axis2" xmlns="http://two.org"> <ChildElementTwo
```

## RuleId[id=UnnecessaryInitCause]
### UnnecessaryInitCause
Unnecessary `Throwable.initCause()` call
in `axiom-compat/src/main/java/org/apache/axiom/om/impl/serialize/ContentHandlerWriter.java`
#### Snippet
```java
        } catch (SAXException ex) {
            IOException ioException = new IOException();
            ioException.initCause(ex);
            throw ioException;
        }
```

### UnnecessaryInitCause
Unnecessary `Throwable.initCause()` call
in `buildutils/shade-axiom-xml/src/main/java/org/apache/axiom/buildutils/shade/axiomxml/DOMUtils.java`
#### Snippet
```java
    private static IOException toIOException(Exception ex) {
        IOException ioException = new IOException();
        ioException.initCause(ex);
        return ioException;
    }
```

## RuleId[id=TrivialIf]
### TrivialIf
`if` statement can be simplified
in `components/core-streams/src/main/java/org/apache/axiom/core/stream/stax/push/input/XMLStreamWriterNamespaceContextProvider.java`
#### Snippet
```java
                if (nsContext != null) {
                    String writerNS = nsContext.getNamespaceURI("");
                    if (writerNS != null && writerNS.length() > 0) {
                        return false;
                    }
```

## RuleId[id=NonStrictComparisonCanBeEquality]
### NonStrictComparisonCanBeEquality
Can be replaced with equality
in `axiom-compat/src/main/java/org/apache/axiom/om/util/UUIDGenerator.java`
#### Snippet
```java
            baseUUID = "urn:uuid:" + baseUUID;
        }
        if (++incrementingValue >= Long.MAX_VALUE) {
            incrementingValue = 0;
        }
```

## RuleId[id=EqualsHashCodeCalledOnUrl]
### EqualsHashCodeCalledOnUrl
Call to `equals()` on URL object
in `axiom-api/src/main/java/org/apache/axiom/util/stax/dialect/StAXDialectDetector.java`
#### Snippet
```java
            Class<?> cls = classLoader.loadClass(name);
            // Cross check if the class was loaded from the same location (JAR)
            return rootUrl.equals(getRootUrlForClass(cls)) ? cls : null;
        } catch (ClassNotFoundException ex) {
            return null;
```

## RuleId[id=CharsetObjectCanBeUsed]
### CharsetObjectCanBeUsed
StandardCharsets.US_ASCII can be used instead
in `axiom-jakarta-jaxb/src/main/java/org/apache/axiom/om/util/jaxb/AttachmentUnmarshallerImpl.java`
#### Snippet
```java
                // other hand, since non ASCII characters are not allowed in content IDs,
                // we can simply decode using ASCII (which is a subset of UTF-8)
                return URLDecoder.decode(url.substring(4), "ascii");
            } catch (UnsupportedEncodingException ex) {
                // We should never get here
```

### CharsetObjectCanBeUsed
StandardCharsets.US_ASCII can be used instead
in `axiom-compat/src/main/java/org/apache/axiom/om/util/ElementHelper.java`
#### Snippet
```java
                // other hand, since non ASCII characters are not allowed in content IDs,
                // we can simply decode using ASCII (which is a subset of UTF-8)
                return URLDecoder.decode(href.substring(4), "ascii");
            } catch (UnsupportedEncodingException ex) {
                // We should never get here
```

### CharsetObjectCanBeUsed
StandardCharsets.US_ASCII can be used instead
in `components/core-streams/src/main/java/org/apache/axiom/core/stream/xop/AbstractXOPDecodingFilterHandler.java`
#### Snippet
```java
                    // characters are not allowed in content IDs, we can simply decode using ASCII
                    // (which is a subset of UTF-8)
                    contentID = URLDecoder.decode(value.substring(4), "ascii");
                } catch (UnsupportedEncodingException ex) {
                    // We should never get here
```

## RuleId[id=AutoCloseableResource]
### AutoCloseableResource
'FileChannel' used without 'try'-with-resources statement
in `components/blob/src/main/java/org/apache/axiom/blob/TempFileInputStream.java`
#### Snippet
```java
    @Override
    public synchronized void reset() throws IOException {
        getChannel().position(markPosition);
    }
}
```

### AutoCloseableResource
'ParentLastURLClassLoader' used without 'try'-with-resources statement
in `testing/jaxp-testsuite/src/main/java/org/apache/axiom/ts/jaxp/stax/StAXImplementation.java`
#### Snippet
```java
                final File jar = extractJar(jarName);
                final ParentLastURLClassLoader classLoader =
                        new ParentLastURLClassLoader(
                                new URL[] {jar.toURI().toURL()},
                                StAXImplementation.class.getClassLoader());
```

## RuleId[id=ConditionCoveredByFurtherCondition]
### ConditionCoveredByFurtherCondition
Condition 'contextNode != null' covered by subsequent condition 'contextNode instanceof OMNode'
in `axiom-api/src/main/java/org/apache/axiom/om/xpath/DocumentNavigator.java`
#### Snippet
```java
            throws UnsupportedAxisException {
        List<OMNode> list = new ArrayList<OMNode>();
        if (contextNode != null && contextNode instanceof OMNode) {
            OMNode node = (OMNode)contextNode;
            while (true) {
```

### ConditionCoveredByFurtherCondition
Condition 'contextNode instanceof OMContainer' covered by subsequent condition 'contextNode instanceof OMElement'
in `axiom-api/src/main/java/org/apache/axiom/om/xpath/DocumentNavigator.java`
#### Snippet
```java
    @Override
    public Iterator<?> getNamespaceAxisIterator(Object contextNode) throws UnsupportedAxisException {
        if (!(contextNode instanceof OMContainer &&
                contextNode instanceof OMElement)) {
            return JaxenConstants.EMPTY_ITERATOR;
```

### ConditionCoveredByFurtherCondition
Condition 'contextNode != null' covered by subsequent condition 'contextNode instanceof OMNode'
in `axiom-api/src/main/java/org/apache/axiom/om/xpath/DocumentNavigator.java`
#### Snippet
```java
            throws UnsupportedAxisException {
        List<OMNode> list = new ArrayList<OMNode>();
        if (contextNode != null && contextNode instanceof OMNode) {
            OMNode node = (OMNode)contextNode;
            while (true) {
```

### ConditionCoveredByFurtherCondition
Condition 'c \<= 32' covered by subsequent condition 'c == ' ' \|\| c == '\\n' \|\| c == '\\r' \|\| c == '\\t''
in `components/base64-utils/src/main/java/org/apache/axiom/util/base64/AbstractBase64DecodingWriter.java`
#### Snippet
```java

    private static boolean isWhitespace(int c) {
        return c <= 32 && (c == ' ' || c == '\n' || c == '\r' || c == '\t'); 
    }
    
```

## RuleId[id=RegExpSingleCharAlternation]
### RegExpSingleCharAlternation
Single character alternation in RegExp
in `testing/soap-testsuite/src/main/resources/org/apache/axiom/ts/soap/xsd/soap-1.1.xsd`
#### Snippet
```java
     <xs:simpleType>
     <xs:restriction base="xs:boolean">
	   <xs:pattern value="0|1"/>
	 </xs:restriction>
   </xs:simpleType>
```

## RuleId[id=RedundantCast]
### RedundantCast
Casting `child` to `Element` is redundant
in `axiom-api/src/main/java/org/apache/axiom/locator/ImplementationFactory.java`
#### Snippet
```java
                QName childQName = getQName(child);
                if (childQName.equals(QNAME_EXTENSION_INTERFACE)) {
                    String className = ((Element)child).getTextContent();
                    try {
                        extensionInterfaces.add(loader.load(className));
```

### RedundantCast
Casting `nextNode` to `CoreLeafNode` is redundant
in `mixins/core-mixins/src/main/java/org/apache/axiom/core/impl/TreeWalkerImpl.java`
#### Snippet
```java
                    break;
                case STATE_LEAF:
                    ((CoreLeafNode) nextNode).internalSerialize(handler, preserve);
                    break;
                case STATE_ATTRIBUTE:
```

### RedundantCast
Casting `nextNode` to `CoreAttribute` is redundant
in `mixins/core-mixins/src/main/java/org/apache/axiom/core/impl/TreeWalkerImpl.java`
#### Snippet
```java
                    break;
                case STATE_ATTRIBUTE:
                    ((CoreAttribute) nextNode).internalSerialize(handler, preserve);
                    break;
                case STATE_NOT_VISITED:
```

### RedundantCast
Casting `paramMap.get(...)` to `Integer` is redundant
in `mixins/dom-mixins/src/main/java/org/apache/axiom/dom/DOMConfigurationImpl.java`
#### Snippet
```java
    @Override
    public Object getParameter(String name) throws DOMException {
        Integer mask = (Integer) paramMap.get(name);
        if (mask != null) {
            return Boolean.valueOf((params & mask.intValue()) != 0);
```

### RedundantCast
Casting `paramMap.get(...)` to `Integer` is redundant
in `mixins/dom-mixins/src/main/java/org/apache/axiom/dom/DOMConfigurationImpl.java`
#### Snippet
```java
    @Override
    public void setParameter(String name, Object value) throws DOMException {
        Integer mask = (Integer) paramMap.get(name);
        if (mask != null) {
            if (value instanceof Boolean) {
```

### RedundantCast
Casting `null` to `String` is redundant
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/factory/TestCreateOMElementWithNullURIAndPrefix.java`
#### Snippet
```java
    protected void runTest() {
        try {
            metaFactory.getOMFactory().createOMElement("test", (String) null, (String) null);
            fail("Expected IllegalArgumentException");
        } catch (IllegalArgumentException ex) {
```

### RedundantCast
Casting `parent` to `AxiomContainer` is redundant
in `mixins/om-mixins/src/main/java/org/apache/axiom/om/impl/common/factory/OMFactoryImpl.java`
#### Snippet
```java
        T element = type.create(nodeFactory);
        if (parent != null) {
            ((AxiomContainer) parent).addChild(element);
        }
        element.initName(localName, ns, true);
```

### RedundantCast
Casting `parent` to `AxiomContainer` is redundant
in `mixins/om-mixins/src/main/java/org/apache/axiom/om/impl/common/factory/OMFactoryImpl.java`
#### Snippet
```java
        }
        if (parent != null) {
            ((AxiomContainer) parent).addChild(node);
        }
        return node;
```

### RedundantCast
Casting `parent` to `AxiomContainer` is redundant
in `mixins/om-mixins/src/main/java/org/apache/axiom/om/impl/common/factory/OMFactoryImpl.java`
#### Snippet
```java
        node.coreSetInternalSubset(internalSubset);
        if (parent != null) {
            ((AxiomContainer) parent).addChild(node);
        }
        return node;
```

### RedundantCast
Casting `parent` to `AxiomContainer` is redundant
in `mixins/om-mixins/src/main/java/org/apache/axiom/om/impl/common/factory/OMFactoryImpl.java`
#### Snippet
```java
        node.coreSetName(name);
        if (parent != null) {
            ((AxiomContainer) parent).addChild(node);
        }
        return node;
```

### RedundantCast
Casting `parent` to `AxiomContainer` is redundant
in `mixins/om-mixins/src/main/java/org/apache/axiom/om/impl/common/factory/OMFactoryImpl.java`
#### Snippet
```java
        }
        if (parent != null) {
            ((AxiomContainer) parent).addChild(node);
        }
        try {
```

### RedundantCast
Casting `parent` to `AxiomContainer` is redundant
in `mixins/om-mixins/src/main/java/org/apache/axiom/om/impl/common/factory/OMFactoryImpl.java`
#### Snippet
```java
        }
        if (parent != null) {
            ((AxiomContainer) parent).addChild(node);
        }
        return node;
```

### RedundantCast
Casting `parent` to `AxiomElement` is redundant
in `mixins/om-mixins/src/main/java/org/apache/axiom/soap/impl/factory/SOAPFactoryImpl.java`
#### Snippet
```java
                ((AxiomElement) parent).insertChild(sequence, pos, element, false);
            } else {
                ((AxiomElement) parent).addChild(element);
            }
        }
```

### RedundantCast
Casting `root` to `OMDocument` is redundant
in `testing/axiom-truth/src/main/java/org/apache/axiom/truth/AxiomTraverser.java`
#### Snippet
```java
        if (node == null) {
            if (root instanceof OMDocument) {
                node = ((OMDocument) root).getFirstOMChild();
            } else {
                node = (OMElement) root;
```

## RuleId[id=JavadocDeclaration]
### JavadocDeclaration
`@param context` tag description is missing
in `testing/spring-ws-testsuite/src/main/java/org/apache/axiom/ts/springws/MessageFactoryConfigurator.java`
#### Snippet
```java
     * </ul>
     *
     * @param context
     */
    public abstract void configure(GenericApplicationContext context);
```

### JavadocDeclaration
`@throws` tag description is missing
in `components/blob/src/main/java/org/apache/axiom/blob/WritableBlob.java`
#### Snippet
```java
     * @param in An input stream to read data from. This method will not close the stream.
     * @return the number of bytes transferred
     * @throws StreamCopyException
     * @throws IllegalStateException if the blob is not in state NEW
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `components/blob/src/main/java/org/apache/axiom/blob/Blob.java`
#### Snippet
```java
     *
     * @return the input stream to read the data from
     * @throws IOException
     */
    InputStream getInputStream() throws IOException;
```

### JavadocDeclaration
Tag `return` is not allowed here
in `components/blob/src/main/java/org/apache/axiom/blob/OverflowableBlobImpl.java`
#### Snippet
```java
     * Create a temporary file and write the existing in memory data to it.
     *
     * @return an open FileOutputStream to the temporary file
     * @throws IOException
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `components/blob/src/main/java/org/apache/axiom/blob/OverflowableBlobImpl.java`
#### Snippet
```java
     *
     * @return an open FileOutputStream to the temporary file
     * @throws IOException
     */
    void switchToOverflowBlob() throws IOException {
```

### JavadocDeclaration
`@param version` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/om/OMDocument.java`
#### Snippet
```java
     * Sets the XML version.
     *
     * @param version
     * @see #XML_10
     * @see #XML_11
```

### JavadocDeclaration
`@param charsetEncoding` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/om/OMDocument.java`
#### Snippet
```java
     * Sets the character set encoding scheme to be used.
     *
     * @param charsetEncoding
     */
    void setCharsetEncoding(String charsetEncoding);
```

### JavadocDeclaration
`@param text` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/om/OMComment.java`
#### Snippet
```java
     * Sets the content of this comment to the specified string.
     *
     * @param text
     */
    void setValue(String text);
```

### JavadocDeclaration
Javadoc pointing to itself
in `axiom-api/src/main/java/org/apache/axiom/om/OMXMLParserWrapper.java`
#### Snippet
```java
     * <p>
     * <table border="1">
     * <caption>Actions performed by the {@link #detach()} method</caption>
     * <tr>
     * <th>Source object type</th>
```

### JavadocDeclaration
`@param xmlWriter` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/om/OMSerializable.java`
#### Snippet
```java
     * <code>true</code>.
     *
     * @param xmlWriter
     * @throws XMLStreamException
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/om/OMSerializable.java`
#### Snippet
```java
     *
     * @param xmlWriter
     * @throws XMLStreamException
     */
    void serialize(XMLStreamWriter xmlWriter) throws XMLStreamException;
```

### JavadocDeclaration
`@param writer` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/om/OMSerializable.java`
#### Snippet
```java
     * </ul>
     * 
     * @param writer
     * @param cache
     *            indicates if caching should be enabled
```

### JavadocDeclaration
`@param xmlWriter` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/om/OMSerializable.java`
#### Snippet
```java
     * <code>false</code>.
     *
     * @param xmlWriter
     * @throws XMLStreamException
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/om/OMSerializable.java`
#### Snippet
```java
     *
     * @param xmlWriter
     * @throws XMLStreamException
     */
    void serializeAndConsume(XMLStreamWriter xmlWriter) throws XMLStreamException;
```

### JavadocDeclaration
`@param value` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/om/OMAttribute.java`
#### Snippet
```java
    String getAttributeType();

    /** @param value  */
    void setAttributeType(String value);

```

### JavadocDeclaration
`@param value` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/om/OMAttribute.java`
#### Snippet
```java
    String getAttributeValue();

    /** @param value  */
    void setAttributeValue(String value);

```

### JavadocDeclaration
`@param cid` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/om/OMText.java`
#### Snippet
```java
    /**
     * Set a specific content id
     * @param cid
     */
    void setContentID(String cid);
```

### JavadocDeclaration
`@param uri` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/om/OMNamespace.java`
#### Snippet
```java
     * Method equals.
     *
     * @param uri
     * @param prefix
     * @return Returns boolean.
```

### JavadocDeclaration
`@param prefix` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/om/OMNamespace.java`
#### Snippet
```java
     *
     * @param uri
     * @param prefix
     * @return Returns boolean.
     */
```

### JavadocDeclaration
`@param localName` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/om/OMFactory.java`
#### Snippet
```java
     * {@link OMNamespace} was given.
     * 
     * @param localName
     * @param ns
     * @param value
```

### JavadocDeclaration
`@param ns` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/om/OMFactory.java`
#### Snippet
```java
     * 
     * @param localName
     * @param ns
     * @param value
     * @return the newly created attribute
```

### JavadocDeclaration
`@param value` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/om/OMFactory.java`
#### Snippet
```java
     * @param localName
     * @param ns
     * @param value
     * @return the newly created attribute
     * @throws IllegalArgumentException
```

### JavadocDeclaration
`@param s` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/om/OMFactory.java`
#### Snippet
```java

    /**
     * @param s
     * @return Returns OMText.
     */
```

### JavadocDeclaration
`@param parent` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/om/OMFactory.java`
#### Snippet
```java
    /**
     * Create OMText node that is a copy of the source text node
     * @param parent
     * @param source
     * @return TODO
```

### JavadocDeclaration
`@param source` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/om/OMFactory.java`
#### Snippet
```java
     * Create OMText node that is a copy of the source text node
     * @param parent
     * @param source
     * @return TODO
     */
```

### JavadocDeclaration
`@param parent` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/om/OMFactory.java`
#### Snippet
```java

    /**
     * @param parent
     * @param text
     * @param type
```

### JavadocDeclaration
`@param text` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/om/OMFactory.java`
#### Snippet
```java
    /**
     * @param parent
     * @param text
     * @param type
     *            the node type: {@link OMNode#TEXT_NODE}, {@link OMNode#CDATA_SECTION_NODE} or
```

### JavadocDeclaration
`@param text` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/om/OMFactory.java`
#### Snippet
```java
     *            <code>null</code>, in which case the behavior of the method is the same as
     *            {@link #createOMText(String)}
     * @param text
     * @return Returns OMText.
     */
```

### JavadocDeclaration
`@param parent` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/om/OMFactory.java`
#### Snippet
```java
     * Creates a comment.
     *
     * @param parent
     * @param content
     * @return Returns OMComment.
```

### JavadocDeclaration
`@param content` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/om/OMFactory.java`
#### Snippet
```java
     *
     * @param parent
     * @param content
     * @return Returns OMComment.
     */
```

### JavadocDeclaration
`@param s` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/om/OMFactory.java`
#### Snippet
```java

    /**
     * @param s
     * @param type
     *            the node type: {@link OMNode#TEXT_NODE}, {@link OMNode#CDATA_SECTION_NODE} or
```

### JavadocDeclaration
`@param parent` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/om/OMFactory.java`
#### Snippet
```java
     * Creates a PI.
     *
     * @param parent
     * @param piTarget
     * @param piData
```

### JavadocDeclaration
`@param piTarget` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/om/OMFactory.java`
#### Snippet
```java
     *
     * @param parent
     * @param piTarget
     * @param piData
     * @return Returns OMProcessingInstruction.
```

### JavadocDeclaration
`@param piData` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/om/OMFactory.java`
#### Snippet
```java
     * @param parent
     * @param piTarget
     * @param piData
     * @return Returns OMProcessingInstruction.
     */
```

### JavadocDeclaration
`@param localName` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/om/OMFactory.java`
#### Snippet
```java
     * to be added to the newly created element to override the default namespace.
     * 
     * @param localName
     * @param ns
     * @param parent
```

### JavadocDeclaration
`@param ns` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/om/OMFactory.java`
#### Snippet
```java
     * 
     * @param localName
     * @param ns
     * @param parent
     *            the parent to which the newly created element will be added; this may be
```

### JavadocDeclaration
`@throws` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/om/OMFactory.java`
#### Snippet
```java
     *            {@link #createOMElement(String, OMNamespace)}
     * @return the newly created element
     * @throws OMException
     * @throws IllegalArgumentException
     *             if an attempt is made to create a prefixed element with an empty namespace name
```

### JavadocDeclaration
`@param value` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/om/OMProcessingInstruction.java`
#### Snippet
```java
     * Sets the value of this Processing Instruction.
     *
     * @param value
     */
    void setValue(String value);
```

### JavadocDeclaration
`@param target` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/om/OMProcessingInstruction.java`
#### Snippet
```java
     * Sets the target of this Processing Instruction.
     *
     * @param target
     */
    void setTarget(String target);
```

### JavadocDeclaration
`@param cause` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/om/OMException.java`
#### Snippet
```java
     * Constructor OMException
     *
     * @param cause
     */
    public OMException(Throwable cause) {
```

### JavadocDeclaration
`@param message` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/om/OMException.java`
#### Snippet
```java
     * Constructor OMException
     *
     * @param message
     * @param cause
     */
```

### JavadocDeclaration
`@param cause` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/om/OMException.java`
#### Snippet
```java
     *
     * @param message
     * @param cause
     */
    public OMException(String message, Throwable cause) {
```

### JavadocDeclaration
`@param message` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/om/OMException.java`
#### Snippet
```java
     * Constructor OMException
     *
     * @param message
     */
    public OMException(String message) {
```

### JavadocDeclaration
`@param prefix` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/om/OMElement.java`
#### Snippet
```java
     * relevant namespace object, if available. If not available, returns null.
     *
     * @param prefix
     */
    // TODO: specify if null is a valid argument
```

### JavadocDeclaration
Javadoc pointing to itself
in `axiom-api/src/main/java/org/apache/axiom/om/OMElement.java`
#### Snippet
```java
     * </ul>
     * <p>
     * It is expected that applications only rarely use {@link #getAllDeclaredNamespaces()}
     * directly.
     * <p>
```

### JavadocDeclaration
`@throws` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/om/OMNode.java`
#### Snippet
```java
     * completely skipped at the parser level. </p>
     *
     * @throws OMException
     */
    void discard() throws OMException;
```

### JavadocDeclaration
`@throws` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/om/OMDataSource.java`
#### Snippet
```java
     *            the correct character set encoding when writing to the output stream. This
     *            parameter must not be null.
     * @throws XMLStreamException
     * 
     * @deprecated
```

### JavadocDeclaration
`@throws` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/om/OMDataSource.java`
#### Snippet
```java
     *            output format information (<code>null</code> if none; may be ignored if not
     *            supported by data binding even if supplied)
     * @throws XMLStreamException
     * 
     * @deprecated
```

### JavadocDeclaration
`@throws` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/om/OMDataSource.java`
#### Snippet
```java
     * 
     * @return element parser
     * @throws XMLStreamException
     */
    XMLStreamReader getReader() throws XMLStreamException;
```

### JavadocDeclaration
`@param uri` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/om/OMContainer.java`
#### Snippet
```java
     * Returns an iterator for child nodes matching the namespace uri.
     *
     * @param uri 
     * @return Returns an iterator of {@link OMElement} items that match the given uri
     */
```

### JavadocDeclaration
Javadoc pointing to itself
in `axiom-api/src/main/java/org/apache/axiom/om/OMContainer.java`
#### Snippet
```java
     * <table border="1">
     * <caption>Side effects of consuming events from the reader returned by
     * {@link #getXMLStreamReader(boolean)}</caption>
     * <tr>
     * <th>State</th>
```

### JavadocDeclaration
`@param localName` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/om/OMContainer.java`
#### Snippet
```java
     * Returns an iterator for child nodes matching the local name.
     *
     * @param localName 
     * @return Returns an iterator of {@link OMElement} items that match the given localName
     */
```

### JavadocDeclaration
`@param customBuilder` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/om/ds/custombuilder/CustomBuilderSupport.java`
#### Snippet
```java
     * 
     * @param selector specifies the elements to which the custom builder will be applied
     * @param customBuilder
     */
    void registerCustomBuilder(CustomBuilder.Selector selector, CustomBuilder customBuilder);
```

### JavadocDeclaration
Javadoc pointing to itself
in `axiom-api/src/main/java/org/apache/axiom/om/impl/MTOMXMLStreamWriter.java`
#### Snippet
```java
     * 3rd party libraries. The serialization code should then use
     * {@link #prepareBlob(Blob)} so that it can write {@code xop:Include} elements
     * directly to the stream. In that case, the code may use the {@link #isOptimized()} method
     * check if MTOM is enabled at all.
     * 
```

### JavadocDeclaration
`@throws` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/om/util/AXIOMUtil.java`
#### Snippet
```java
     * @param xmlFragment the well-formed XML fragment
     * @return The OMElement created out of the string XML fragment.
     * @throws XMLStreamException
     */
    public static OMElement stringToOM(String xmlFragment) throws XMLStreamException {
```

### JavadocDeclaration
`@throws` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/om/util/AXIOMUtil.java`
#### Snippet
```java
     * @param xmlFragment the well-formed XML fragment
     * @return The OMElement created out of the string XML fragment.
     * @throws XMLStreamException
     */
    public static OMElement stringToOM(OMFactory omFactory, String xmlFragment)
```

### JavadocDeclaration
`@param cl` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/om/util/StAXUtils.java`
#### Snippet
```java
     * All other values are returned as strings.
     * 
     * @param cl
     * @param name
     * @return the factory properties
```

### JavadocDeclaration
`@param name` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/om/util/StAXUtils.java`
#### Snippet
```java
     * 
     * @param cl
     * @param name
     * @return the factory properties
     */
```

### JavadocDeclaration
`@param uri` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAPFaultRole.java`
#### Snippet
```java
     * during processing of the message
     *
     * @param uri
     */
    void setRoleValue(String uri);
```

### JavadocDeclaration
`@param lang` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAPFaultText.java`
#### Snippet
```java
     * prefix
     *
     * @param lang
     */
    void setLang(String lang);
```

### JavadocDeclaration
`@param document` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/om/util/DigestGenerator.java`
#### Snippet
```java
     * only
     *
     * @param document
     * @return Returns a collection of OMProcessingInstructions and OMElements
     */
```

### JavadocDeclaration
`@param element` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/om/util/DigestGenerator.java`
#### Snippet
```java
     * Gets the collection of attributes which are none namespace declarations for an OMElement
     *
     * @param element
     * @return Returns the collection of attributes which are none namespace declarations
     */
```

### JavadocDeclaration
`@param attribute` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/om/util/DigestGenerator.java`
#### Snippet
```java
     * Compares two OMAttributes for the XML equality
     *
     * @param attribute
     * @param comparingAttribute
     * @param digestAlgorithm
```

### JavadocDeclaration
`@param comparingAttribute` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/om/util/DigestGenerator.java`
#### Snippet
```java
     *
     * @param attribute
     * @param comparingAttribute
     * @param digestAlgorithm
     * @return Returns true if the OMDocument XML content are equal
```

### JavadocDeclaration
`@param digestAlgorithm` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/om/util/DigestGenerator.java`
#### Snippet
```java
     * @param attribute
     * @param comparingAttribute
     * @param digestAlgorithm
     * @return Returns true if the OMDocument XML content are equal
     */
```

### JavadocDeclaration
`@param pi` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/om/util/DigestGenerator.java`
#### Snippet
```java
     * This method is an overloaded method for the digest generation for OMProcessingInstruction
     *
     * @param pi
     * @param digestAlgorithm
     * @return Returns a byte array representing the calculated digest value
```

### JavadocDeclaration
`@param digestAlgorithm` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/om/util/DigestGenerator.java`
#### Snippet
```java
     *
     * @param pi
     * @param digestAlgorithm
     * @return Returns a byte array representing the calculated digest value
     */
```

### JavadocDeclaration
`@param document` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/om/util/DigestGenerator.java`
#### Snippet
```java
     * This method is an overloaded method for the digest generation for OMDocument
     *
     * @param document
     * @param digestAlgorithm
     * @return Returns a byte array representing the calculated digest
```

### JavadocDeclaration
`@param digestAlgorithm` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/om/util/DigestGenerator.java`
#### Snippet
```java
     *
     * @param document
     * @param digestAlgorithm
     * @return Returns a byte array representing the calculated digest
     */
```

### JavadocDeclaration
`@param attribute` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/om/util/DigestGenerator.java`
#### Snippet
```java
     * This method is an overloaded method for the digest generation for OMAttribute
     *
     * @param attribute
     * @param digestAlgorithm
     * @return Returns a byte array representing the calculated digest value
```

### JavadocDeclaration
`@param digestAlgorithm` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/om/util/DigestGenerator.java`
#### Snippet
```java
     *
     * @param attribute
     * @param digestAlgorithm
     * @return Returns a byte array representing the calculated digest value
     */
```

### JavadocDeclaration
`@param soapFault` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAPBody.java`
#### Snippet
```java

    /**
     * @param soapFault
     * @throws OMException
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAPBody.java`
#### Snippet
```java
    /**
     * @param soapFault
     * @throws OMException
     */
    void addFault(SOAPFault soapFault) throws OMException;
```

### JavadocDeclaration
`@param e` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAPBody.java`
#### Snippet
```java
     * object.
     *
     * @param e
     * @return the new <code>SOAPFault</code> object
     * @throws org.apache.axiom.om.OMException
```

### JavadocDeclaration
`@throws` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAPBody.java`
#### Snippet
```java
     * @throws org.apache.axiom.om.OMException
     *          if there is a SOAP error
     * @throws org.apache.axiom.om.OMException
     *
     */
```

### JavadocDeclaration
`@param element` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/om/util/DigestGenerator.java`
#### Snippet
```java
     * This method is an overloaded method for the digest generation for OMElement
     *
     * @param element
     * @param digestAlgorithm
     * @return Returns a byte array representing the calculated digest value
```

### JavadocDeclaration
`@param digestAlgorithm` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/om/util/DigestGenerator.java`
#### Snippet
```java
     *
     * @param element
     * @param digestAlgorithm
     * @return Returns a byte array representing the calculated digest value
     */
```

### JavadocDeclaration
`@param text` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/om/util/DigestGenerator.java`
#### Snippet
```java
     * This method is an overloaded method for the digest generation for OMText
     *
     * @param text
     * @param digestAlgorithm
     * @return Returns a byte array representing the calculated digest value
```

### JavadocDeclaration
`@param digestAlgorithm` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/om/util/DigestGenerator.java`
#### Snippet
```java
     *
     * @param text
     * @param digestAlgorithm
     * @return Returns a byte array representing the calculated digest value
     */
```

### JavadocDeclaration
`@param attribute` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/om/util/DigestGenerator.java`
#### Snippet
```java
     * the local name for OMAttribute
     *
     * @param attribute
     * @return Returns the expanded name of the OMAttribute
     */
```

### JavadocDeclaration
`@param node` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/om/util/DigestGenerator.java`
#### Snippet
```java
     * This method is an overloaded method for the digest generation for OMNode
     *
     * @param node
     * @param digestAlgorithm
     * @return Returns a byte array representing the calculated digest value
```

### JavadocDeclaration
`@param digestAlgorithm` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/om/util/DigestGenerator.java`
#### Snippet
```java
     *
     * @param node
     * @param digestAlgorithm
     * @return Returns a byte array representing the calculated digest value
     */
```

### JavadocDeclaration
`@param document` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/om/util/DigestGenerator.java`
#### Snippet
```java
     * Compares two OMDocuments for the XML equality
     *
     * @param document
     * @param comparingDocument
     * @param digestAlgorithm
```

### JavadocDeclaration
`@param comparingDocument` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/om/util/DigestGenerator.java`
#### Snippet
```java
     *
     * @param document
     * @param comparingDocument
     * @param digestAlgorithm
     * @return Returns true if the OMDocument XML content are equal
```

### JavadocDeclaration
`@param digestAlgorithm` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/om/util/DigestGenerator.java`
#### Snippet
```java
     * @param document
     * @param comparingDocument
     * @param digestAlgorithm
     * @return Returns true if the OMDocument XML content are equal
     */
```

### JavadocDeclaration
`@param node` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/om/util/DigestGenerator.java`
#### Snippet
```java
     * Compares two OMNodes for the XML equality
     *
     * @param node
     * @param comparingNode
     * @param digestAlgorithm
```

### JavadocDeclaration
`@param comparingNode` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/om/util/DigestGenerator.java`
#### Snippet
```java
     *
     * @param node
     * @param comparingNode
     * @param digestAlgorithm
     * @return Returns true if the OMNode XML contents are equal
```

### JavadocDeclaration
`@param digestAlgorithm` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/om/util/DigestGenerator.java`
#### Snippet
```java
     * @param node
     * @param comparingNode
     * @param digestAlgorithm
     * @return Returns true if the OMNode XML contents are equal
     */
```

### JavadocDeclaration
`@param array` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/om/util/DigestGenerator.java`
#### Snippet
```java
     * Gets the String representation of the byte array
     *
     * @param array
     * @return Returns the String of the byte
     */
```

### JavadocDeclaration
`@param element` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/om/util/DigestGenerator.java`
#### Snippet
```java
     * the local name for OMElement
     *
     * @param element
     * @return Returns the expanded name of OMElement
     */
```

### JavadocDeclaration
`@param value` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAPFaultClassifier.java`
#### Snippet
```java
     * Fault SubCode contain only one mandatory Value child. This value child contains a QName
     *
     * @param value
     */
    void setValue(SOAPFaultValue value) throws SOAPProcessingException;
```

### JavadocDeclaration
`@param subCode` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAPFaultClassifier.java`
#### Snippet
```java
     * Fault SubCode can contain an optional SubCode
     *
     * @param subCode
     */
    void setSubCode(SOAPFaultSubCode subCode) throws SOAPProcessingException;
```

### JavadocDeclaration
`@param uri` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAPFaultNode.java`
#### Snippet
```java
     * fault.
     *
     * @param uri
     */
    void setFaultNodeValue(String uri);
```

### JavadocDeclaration
`@throws` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAPFault.java`
#### Snippet
```java
     *
     * @return Returns Exception.
     * @throws org.apache.axiom.om.OMException
     *
     */
```

### JavadocDeclaration
`@param node` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAPFault.java`
#### Snippet
```java
     * SOAPFaultNode is an optional item in a Fault, in SOAP 1.2 specification
     *
     * @param node
     */
    void setNode(SOAPFaultNode node) throws SOAPProcessingException;
```

### JavadocDeclaration
`@param role` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAPFault.java`
#### Snippet
```java
     * SOAPFaultRoleImpl is an optional item in a Fault, in SOAP 1.2 specification
     *
     * @param role
     */
    void setRole(SOAPFaultRole role) throws SOAPProcessingException;
```

### JavadocDeclaration
`@param detail` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAPFault.java`
#### Snippet
```java
     * SOAPFaultRoleImpl is an optional item in a Fault, in SOAP 1.2 specification
     *
     * @param detail
     */
    void setDetail(SOAPFaultDetail detail) throws SOAPProcessingException;
```

### JavadocDeclaration
`@param soapFaultCode` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAPFault.java`
#### Snippet
```java
     * SOAPFaultCode is a mandatory item in a Fault, in SOAP 1.2 specification
     *
     * @param soapFaultCode
     */
    void setCode(SOAPFaultCode soapFaultCode) throws SOAPProcessingException;
```

### JavadocDeclaration
`@param reason` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAPFault.java`
#### Snippet
```java
     * SOAPFaultReason is a mandatory item in a Fault, in SOAP 1.2 specification
     *
     * @param reason
     */
    void setReason(SOAPFaultReason reason) throws SOAPProcessingException;
```

### JavadocDeclaration
`@param nsURI` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAPHeader.java`
#### Snippet
```java
     * Return all the Headers that has the Namespace URI to given NS URI.
     *
     * @param nsURI
     * @return Returns ArrayList.
     * @deprecated Use {@link #getHeaderBlocksWithNamespaceURI(String)} instead.
```

### JavadocDeclaration
`@param parent` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAPFactory.java`
#### Snippet
```java
     * Role eii under SOAPFault (parent)
     *
     * @param parent
     * @return Returns SOAPFaultRole.
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAPFactory.java`
#### Snippet
```java
     *            the data source; must not be <code>null</code>
     * @return the newly created header block
     * @throws SOAPProcessingException
     */
    SOAPHeaderBlock createSOAPHeaderBlock(String localName,
```

### JavadocDeclaration
`@param localName` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAPFactory.java`
#### Snippet
```java

    /**
     * @param localName
     * @param ns
     * @return Returns SOAPHeaderBlock.
```

### JavadocDeclaration
`@param ns` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAPFactory.java`
#### Snippet
```java
    /**
     * @param localName
     * @param ns
     * @return Returns SOAPHeaderBlock.
     */
```

### JavadocDeclaration
`@param parent` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAPFactory.java`
#### Snippet
```java
     * SubCode eii under SubCode (parent)
     *
     * @param parent
     * @return Returns SOAPFaultText.
     */
```

### JavadocDeclaration
`@param parent` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAPFactory.java`
#### Snippet
```java
     * Detail eii under SOAPFault (parent)
     *
     * @param parent
     * @return Returns SOAPFaultDetail.
     */
```

### JavadocDeclaration
`@param parent` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAPFactory.java`
#### Snippet
```java
     * Node eii under SOAPFault (parent)
     *
     * @param parent
     * @return Returns SOAPFaultNode.
     */
```

### JavadocDeclaration
`@param parent` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAPFactory.java`
#### Snippet
```java
     * SubCode eii under SubCode (parent)
     *
     * @param parent
     * @return Returns SOAPFaultSubCode.
     */
```

### JavadocDeclaration
`@param envelope` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAPFactory.java`
#### Snippet
```java

    /**
     * @param envelope
     * @return Returns SOAPBody.
     */
```

### JavadocDeclaration
`@param parent` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAPFactory.java`
#### Snippet
```java

    /**
     * @param parent
     * @param e
     * @return Returns SOAPFault.
```

### JavadocDeclaration
`@param e` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAPFactory.java`
#### Snippet
```java
    /**
     * @param parent
     * @param e
     * @return Returns SOAPFault.
     */
```

### JavadocDeclaration
`@param parent` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAPFactory.java`
#### Snippet
```java
     * Reason eii under SOAPFault (parent)
     *
     * @param parent
     * @return Returns SOAPFaultReason.
     */
```

### JavadocDeclaration
`@param parent` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAPFactory.java`
#### Snippet
```java
     * SubCode eii under Value (parent)
     *
     * @param parent
     * @return Returns SOAPFaultValue.
     */
```

### JavadocDeclaration
`@param parent` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAPFactory.java`
#### Snippet
```java
     * Value eii under Code (parent)
     *
     * @param parent
     * @return Returns SOAPFaultValue.
     */
```

### JavadocDeclaration
`@param parent` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/soap/SOAPFactory.java`
#### Snippet
```java
     * Code eii under SOAPFault (parent)
     *
     * @param parent
     * @return Returns SOAPFaultCode.
     */
```

### JavadocDeclaration
`@param buffer` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/util/UIDGenerator.java`
#### Snippet
```java
     * comparing two IDs for equality more efficient.
     * 
     * @param buffer
     */
    private static void generateHex(StringBuilder buffer) {
```

### JavadocDeclaration
`@param key` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/om/OMOutputFormat.java`
#### Snippet
```java
    
    /**
     * @param key
     * @return true if known key
     */
```

### JavadocDeclaration
`@param c` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/om/OMOutputFormat.java`
#### Snippet
```java
     * If this method is not invoked, OMOutputFormat will choose
     * a content-type value consistent with the soap version.
     * @param c
     */
    public void setContentType(String c) {
```

### JavadocDeclaration
`@param SOAPContentType` tag description is missing
in `axiom-api/src/main/java/org/apache/axiom/om/OMOutputFormat.java`
#### Snippet
```java
     *      start-info="[MESSAGE CONTENT TYPE]";
     * 
     * @param SOAPContentType
     * @return TODO
     */
```

### JavadocDeclaration
`@param newNode` tag description is missing
in `mixins/core-mixins/src/main/java/org/apache/axiom/core/NodeIterator.java`
#### Snippet
```java
     * <p>This method only has an effect if the current node is a {@link CoreChildNode}.
     *
     * @param newNode
     * @throws CoreModelException
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `mixins/core-mixins/src/main/java/org/apache/axiom/core/NodeIterator.java`
#### Snippet
```java
     *
     * @param newNode
     * @throws CoreModelException
     */
    // TODO: the meaning of this method is not clear for Axis.DESCENDANTS
```

### JavadocDeclaration
`@return` tag description is missing
in `mixins/core-mixins/src/main/java/org/apache/axiom/core/AttributeMatcher.java`
#### Snippet
```java
     * @param prefix see above
     * @param value see above
     * @return
     * @throws CoreModelException
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `mixins/core-mixins/src/main/java/org/apache/axiom/core/AttributeMatcher.java`
#### Snippet
```java
     * @param value see above
     * @return
     * @throws CoreModelException
     */
    CoreAttribute createAttribute(
```

### JavadocDeclaration
`@throws` tag description is missing
in `mixins/core-mixins/src/main/java/org/apache/axiom/core/AttributeMatcher.java`
#### Snippet
```java
     * @param prefix see above
     * @param value see above
     * @throws CoreModelException
     */
    void update(CoreAttribute attr, String prefix, String value) throws CoreModelException;
```

### JavadocDeclaration
`@param ready` tag description is missing
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/IncomingAttachmentStreams.java`
#### Snippet
```java
     * Set the ready flag. Intended for the inner class to use.
     *
     * @param ready
     */
    void setReadyToGetNextStream(boolean ready) {
```

### JavadocDeclaration
`@param interval` tag description is missing
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/lifecycle/LifecycleManager.java`
#### Snippet
```java
     * Mark attachment file for deletion when designated time interval in seconds 
     * has elapsed.
     * @param interval
     * @param file
     * @throws IOException
```

### JavadocDeclaration
`@param file` tag description is missing
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/lifecycle/LifecycleManager.java`
#### Snippet
```java
     * has elapsed.
     * @param interval
     * @param file
     * @throws IOException
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/lifecycle/LifecycleManager.java`
#### Snippet
```java
     * @param interval
     * @param file
     * @throws IOException
     */
    public void deleteOnTimeInterval(int interval, File file) throws IOException;
```

### JavadocDeclaration
`@param name` tag description is missing
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/IncomingAttachmentInputStream.java`
#### Snippet
```java
     * Get a header value.
     *
     * @param name
     * @return The header found or null if not found.
     */
```

### JavadocDeclaration
`@param file` tag description is missing
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/lifecycle/LifecycleManager.java`
#### Snippet
```java
    /**
     * This method will return the file accessor associated with this file.
     * @param file
     * @return the {@link FileAccessor} object for the given file
     * @throws IOException
```

### JavadocDeclaration
`@param name` tag description is missing
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/IncomingAttachmentInputStream.java`
#### Snippet
```java
     * Add a header.
     *
     * @param name
     * @param value
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/lifecycle/LifecycleManager.java`
#### Snippet
```java
     * @param file
     * @return the {@link FileAccessor} object for the given file
     * @throws IOException
     */
    public FileAccessor getFileAccessor(String file) throws IOException;
```

### JavadocDeclaration
`@param value` tag description is missing
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/IncomingAttachmentInputStream.java`
#### Snippet
```java
     *
     * @param name
     * @param value
     */
    public void addHeader(String name, String value) {
```

### JavadocDeclaration
`@param file` tag description is missing
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/lifecycle/LifecycleManager.java`
#### Snippet
```java
    /** 
     * Deletes attachment file
     * @param file
     * @throws IOException
     */
```

### JavadocDeclaration
`@param in` tag description is missing
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/IncomingAttachmentInputStream.java`
#### Snippet
```java
    public static final String HEADER_CONTENT_ID = "content-id";

    /** @param in  */
    public IncomingAttachmentInputStream(InputStream in,
                                         IncomingAttachmentStreams parentContainer) {
```

### JavadocDeclaration
`@throws` tag description is missing
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/lifecycle/LifecycleManager.java`
#### Snippet
```java
     * Deletes attachment file
     * @param file
     * @throws IOException
     */
    public void delete(File file) throws IOException;
```

### JavadocDeclaration
`@param file` tag description is missing
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/lifecycle/LifecycleManager.java`
#### Snippet
```java
    /**
     * Mark the file for deletion on application/VM exit 
     * @param file
     * @throws IOException
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/lifecycle/LifecycleManager.java`
#### Snippet
```java
     * Mark the file for deletion on application/VM exit 
     * @param file
     * @throws IOException
     */
    public void deleteOnExit(File file) throws IOException;
```

### JavadocDeclaration
`@param attachmentDir` tag description is missing
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/lifecycle/LifecycleManager.java`
#### Snippet
```java
    /**
     * Create a unique file in the designated directory
     * @param attachmentDir
     * @return the {@link FileAccessor} for the file created by this method
     * @throws IOException
```

### JavadocDeclaration
`@throws` tag description is missing
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/lifecycle/LifecycleManager.java`
#### Snippet
```java
     * @param attachmentDir
     * @return the {@link FileAccessor} for the file created by this method
     * @throws IOException
     */    
    public FileAccessor create(String attachmentDir) throws IOException;
```

### JavadocDeclaration
`@param eventId` tag description is missing
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/lifecycle/impl/LifecycleEventHandler.java`
#### Snippet
```java
     * For example, if the delete behaviour is readOnce and if the inputstream on attachment is read 
     * and closed the first time, the delete operation in LifecycleManager should be executed. 
     * @param eventId
     */
    public void handleEvent(int eventId) throws IOException;
```

### JavadocDeclaration
`@param inStream` tag description is missing
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/Attachments.java`
#### Snippet
```java
     * found or end of stream is reached.
     *
     * @param inStream
     * @param contentTypeString
     * @param fileCacheEnable
```

### JavadocDeclaration
`@param contentTypeString` tag description is missing
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/Attachments.java`
#### Snippet
```java
     *
     * @param inStream
     * @param contentTypeString
     * @param fileCacheEnable
     * @param attachmentRepoDir
```

### JavadocDeclaration
`@param fileCacheEnable` tag description is missing
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/Attachments.java`
#### Snippet
```java
     * @param inStream
     * @param contentTypeString
     * @param fileCacheEnable
     * @param attachmentRepoDir
     * @throws OMException
```

### JavadocDeclaration
`@param attachmentRepoDir` tag description is missing
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/Attachments.java`
#### Snippet
```java
     * @param contentTypeString
     * @param fileCacheEnable
     * @param attachmentRepoDir
     * @throws OMException
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/Attachments.java`
#### Snippet
```java
     * @param fileCacheEnable
     * @param attachmentRepoDir
     * @throws OMException
     */
    public Attachments(LifecycleManager manager, InputStream inStream, String contentTypeString,
```

### JavadocDeclaration
`@param inStream` tag description is missing
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/Attachments.java`
#### Snippet
```java
     * Sets file cache to false.
     *
     * @param inStream
     * @param contentTypeString
     * @throws OMException
```

### JavadocDeclaration
`@param contentTypeString` tag description is missing
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/Attachments.java`
#### Snippet
```java
     *
     * @param inStream
     * @param contentTypeString
     * @throws OMException
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/Attachments.java`
#### Snippet
```java
     * @param inStream
     * @param contentTypeString
     * @throws OMException
     */
    public Attachments(InputStream inStream, String contentTypeString)
```

### JavadocDeclaration
`@param inStream` tag description is missing
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/Attachments.java`
#### Snippet
```java
     * found or end of stream is reached.
     *
     * @param inStream
     * @param contentTypeString
     * @param fileCacheEnable
```

### JavadocDeclaration
`@param contentTypeString` tag description is missing
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/Attachments.java`
#### Snippet
```java
     *
     * @param inStream
     * @param contentTypeString
     * @param fileCacheEnable
     * @param attachmentRepoDir
```

### JavadocDeclaration
`@param fileCacheEnable` tag description is missing
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/Attachments.java`
#### Snippet
```java
     * @param inStream
     * @param contentTypeString
     * @param fileCacheEnable
     * @param attachmentRepoDir
     * @throws OMException
```

### JavadocDeclaration
`@param attachmentRepoDir` tag description is missing
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/Attachments.java`
#### Snippet
```java
     * @param contentTypeString
     * @param fileCacheEnable
     * @param attachmentRepoDir
     * @throws OMException
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/Attachments.java`
#### Snippet
```java
     * @param fileCacheEnable
     * @param attachmentRepoDir
     * @throws OMException
     */
    public Attachments(InputStream inStream, String contentTypeString,
```

### JavadocDeclaration
`@param contentID` tag description is missing
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/Attachments.java`
#### Snippet
```java
     * serialized only if SOAP with Attachments is enabled.
     *
     * @param contentID
     * @param dataHandler
     */
```

### JavadocDeclaration
`@param dataHandler` tag description is missing
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/Attachments.java`
#### Snippet
```java
     *
     * @param contentID
     * @param dataHandler
     */
    public void addDataHandler(String contentID, DataHandler dataHandler) {
```

### JavadocDeclaration
`@param inStream` tag description is missing
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/Attachments.java`
#### Snippet
```java
     * till first MIME boundary is found or end of stream is reached.
     *
     * @param inStream
     * @param contentTypeString
     * @param fileCacheEnable
```

### JavadocDeclaration
`@param contentTypeString` tag description is missing
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/Attachments.java`
#### Snippet
```java
     *
     * @param inStream
     * @param contentTypeString
     * @param fileCacheEnable
     * @param attachmentRepoDir
```

### JavadocDeclaration
`@param fileCacheEnable` tag description is missing
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/Attachments.java`
#### Snippet
```java
     * @param inStream
     * @param contentTypeString
     * @param fileCacheEnable
     * @param attachmentRepoDir
     * @param fileThreshold
```

### JavadocDeclaration
`@param attachmentRepoDir` tag description is missing
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/Attachments.java`
#### Snippet
```java
     * @param contentTypeString
     * @param fileCacheEnable
     * @param attachmentRepoDir
     * @param fileThreshold
     * @param contentLength
```

### JavadocDeclaration
`@param fileThreshold` tag description is missing
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/Attachments.java`
#### Snippet
```java
     * @param fileCacheEnable
     * @param attachmentRepoDir
     * @param fileThreshold
     * @param contentLength
     * @throws OMException
```

### JavadocDeclaration
`@param contentLength` tag description is missing
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/Attachments.java`
#### Snippet
```java
     * @param attachmentRepoDir
     * @param fileThreshold
     * @param contentLength
     * @throws OMException
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/Attachments.java`
#### Snippet
```java
     * @param fileThreshold
     * @param contentLength
     * @throws OMException
     */
    public Attachments(InputStream inStream, String contentTypeString, boolean fileCacheEnable,
```

### JavadocDeclaration
`@param inStream` tag description is missing
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/Attachments.java`
#### Snippet
```java
     * till first MIME boundary is found or end of stream is reached.
     *
     * @param inStream
     * @param contentTypeString
     * @param fileCacheEnable
```

### JavadocDeclaration
`@param contentTypeString` tag description is missing
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/Attachments.java`
#### Snippet
```java
     *
     * @param inStream
     * @param contentTypeString
     * @param fileCacheEnable
     * @param attachmentRepoDir
```

### JavadocDeclaration
`@param fileCacheEnable` tag description is missing
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/Attachments.java`
#### Snippet
```java
     * @param inStream
     * @param contentTypeString
     * @param fileCacheEnable
     * @param attachmentRepoDir
     * @param fileThreshold
```

### JavadocDeclaration
`@param attachmentRepoDir` tag description is missing
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/Attachments.java`
#### Snippet
```java
     * @param contentTypeString
     * @param fileCacheEnable
     * @param attachmentRepoDir
     * @param fileThreshold
     * @param contentLength
```

### JavadocDeclaration
`@param fileThreshold` tag description is missing
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/Attachments.java`
#### Snippet
```java
     * @param fileCacheEnable
     * @param attachmentRepoDir
     * @param fileThreshold
     * @param contentLength
     * @throws OMException
```

### JavadocDeclaration
`@param contentLength` tag description is missing
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/Attachments.java`
#### Snippet
```java
     * @param attachmentRepoDir
     * @param fileThreshold
     * @param contentLength
     * @throws OMException
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/Attachments.java`
#### Snippet
```java
     * @param fileThreshold
     * @param contentLength
     * @throws OMException
     */
    public Attachments(LifecycleManager manager, InputStream inStream, String contentTypeString, boolean fileCacheEnable,
```

### JavadocDeclaration
`@param blobContentID` tag description is missing
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/Attachments.java`
#### Snippet
```java
     * trying to find it calling the getNextPart() till the required part is found.
     *
     * @param blobContentID
     */
    public void removeDataHandler(String blobContentID) {
```

### JavadocDeclaration
`@param fileName` tag description is missing
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/AttachmentCacheMonitor.java`
#### Snippet
```java
     * This will allow the Monitor to remove the file after
     * the timeout period.
     * @param fileName
     */
    public void  register(String fileName) {
```

### JavadocDeclaration
`@param fileName` tag description is missing
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/AttachmentCacheMonitor.java`
#### Snippet
```java
    /**
     * Indicates that the file was accessed.
     * @param fileName
     */
    public void access(String fileName) {
```

### JavadocDeclaration
`@param reader` tag description is missing
in `axiom-compat/src/main/java/org/apache/axiom/om/OMSerializer.java`
#### Snippet
```java
     * Method serialize
     *
     * @param reader
     * @param writer
     * @throws XMLStreamException
```

### JavadocDeclaration
`@param writer` tag description is missing
in `axiom-compat/src/main/java/org/apache/axiom/om/OMSerializer.java`
#### Snippet
```java
     *
     * @param reader
     * @param writer
     * @throws XMLStreamException
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `axiom-compat/src/main/java/org/apache/axiom/om/OMSerializer.java`
#### Snippet
```java
     * @param reader
     * @param writer
     * @throws XMLStreamException
     */
    void serialize(XMLStreamReader reader, XMLStreamWriter writer)
```

### JavadocDeclaration
`@param chars` tag description is missing
in `axiom-compat/src/main/java/org/apache/axiom/om/ds/CharArrayDataSource.java`
#### Snippet
```java
    /**
     * Constructor
     * @param chars
     */
    public CharArrayDataSource(char[] chars) {
```

### JavadocDeclaration
`@param bytes` tag description is missing
in `axiom-compat/src/main/java/org/apache/axiom/om/ds/ByteArrayDataSource.java`
#### Snippet
```java
    /**
     * Constructor
     * @param bytes 
     * @param encoding
     */
```

### JavadocDeclaration
`@param encoding` tag description is missing
in `axiom-compat/src/main/java/org/apache/axiom/om/ds/ByteArrayDataSource.java`
#### Snippet
```java
     * Constructor
     * @param bytes 
     * @param encoding
     */
    public ByteArrayDataSource(byte[] bytes, String encoding) {
```

### JavadocDeclaration
`@param is` tag description is missing
in `axiom-compat/src/main/java/org/apache/axiom/om/ds/InputStreamDataSource.java`
#### Snippet
```java
    /**
     * Constructor
     * @param is 
     * @param encoding
     */
```

### JavadocDeclaration
`@param encoding` tag description is missing
in `axiom-compat/src/main/java/org/apache/axiom/om/ds/InputStreamDataSource.java`
#### Snippet
```java
     * Constructor
     * @param is 
     * @param encoding
     */
    public InputStreamDataSource(InputStream is, String encoding) {
```

### JavadocDeclaration
`@param is` tag description is missing
in `axiom-compat/src/main/java/org/apache/axiom/om/ds/InputStreamDataSource.java`
#### Snippet
```java
    /**
     * Private utility to write the InputStream contents to the OutputStream.
     * @param is
     * @param os
     * @throws IOException
```

### JavadocDeclaration
`@param os` tag description is missing
in `axiom-compat/src/main/java/org/apache/axiom/om/ds/InputStreamDataSource.java`
#### Snippet
```java
     * Private utility to write the InputStream contents to the OutputStream.
     * @param is
     * @param os
     * @throws IOException
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `axiom-compat/src/main/java/org/apache/axiom/om/ds/InputStreamDataSource.java`
#### Snippet
```java
     * @param is
     * @param os
     * @throws IOException
     */
    private static void inputStream2OutputStream(InputStream is, 
```

### JavadocDeclaration
`@param encoding` tag description is missing
in `axiom-compat/src/main/java/org/apache/axiom/om/ds/custombuilder/ByteArrayCustomBuilder.java`
#### Snippet
```java
    /**
     * Constructor
     * @param encoding 
     */
    public ByteArrayCustomBuilder(String encoding) {
```

### JavadocDeclaration
`@param currentChild` tag description is missing
in `axiom-compat/src/main/java/org/apache/axiom/om/impl/traverse/OMChildrenIterator.java`
#### Snippet
```java
     * Constructor OMChildrenIterator.
     *
     * @param currentChild
     */
    public OMChildrenIterator(OMNode currentChild) {
```

### JavadocDeclaration
`@param reader` tag description is missing
in `axiom-compat/src/main/java/org/apache/axiom/om/ds/OMDataSourceExtBase.java`
#### Snippet
```java
     * Simple utility that takes an XMLStreamReader and writes it
     * to an XMLStreamWriter
     * @param reader
     * @param writer
     * @throws XMLStreamException
```

### JavadocDeclaration
`@param writer` tag description is missing
in `axiom-compat/src/main/java/org/apache/axiom/om/ds/OMDataSourceExtBase.java`
#### Snippet
```java
     * to an XMLStreamWriter
     * @param reader
     * @param writer
     * @throws XMLStreamException
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `axiom-compat/src/main/java/org/apache/axiom/om/ds/OMDataSourceExtBase.java`
#### Snippet
```java
     * @param reader
     * @param writer
     * @throws XMLStreamException
     */
    private static void reader2writer(XMLStreamReader reader, 
```

### JavadocDeclaration
`@param searchQName` tag description is missing
in `axiom-compat/src/main/java/org/apache/axiom/om/impl/traverse/OMChildrenLegacyQNameIterator.java`
#### Snippet
```java
     * This version of equals returns true if the local parts match.
     * 
     * @param searchQName
     * @param currentQName
     * @return true if equals
```

### JavadocDeclaration
`@param currentQName` tag description is missing
in `axiom-compat/src/main/java/org/apache/axiom/om/impl/traverse/OMChildrenLegacyQNameIterator.java`
#### Snippet
```java
     * 
     * @param searchQName
     * @param currentQName
     * @return true if equals
     */
```

### JavadocDeclaration
`@param searchQName` tag description is missing
in `axiom-compat/src/main/java/org/apache/axiom/om/impl/traverse/OMChildrenNamespaceIterator.java`
#### Snippet
```java
     * This version of equals returns true if the local parts match.
     * 
     * @param searchQName
     * @param currentQName
     * @return true if equals
```

### JavadocDeclaration
`@param currentQName` tag description is missing
in `axiom-compat/src/main/java/org/apache/axiom/om/impl/traverse/OMChildrenNamespaceIterator.java`
#### Snippet
```java
     * 
     * @param searchQName
     * @param currentQName
     * @return true if equals
     */
```

### JavadocDeclaration
`@param searchQName` tag description is missing
in `axiom-compat/src/main/java/org/apache/axiom/om/impl/traverse/OMChildrenLocalNameIterator.java`
#### Snippet
```java
     * This version of equals returns true if the local parts match.
     * 
     * @param searchQName
     * @param currentQName
     * @return true if equals
```

### JavadocDeclaration
`@param currentQName` tag description is missing
in `axiom-compat/src/main/java/org/apache/axiom/om/impl/traverse/OMChildrenLocalNameIterator.java`
#### Snippet
```java
     * 
     * @param searchQName
     * @param currentQName
     * @return true if equals
     */
```

### JavadocDeclaration
`@param uri` tag description is missing
in `axiom-compat/src/main/java/org/apache/axiom/om/impl/OMNamespaceImpl.java`
#### Snippet
```java

    /**
     * @param uri
     * @param prefix
     */
```

### JavadocDeclaration
`@param prefix` tag description is missing
in `axiom-compat/src/main/java/org/apache/axiom/om/impl/OMNamespaceImpl.java`
#### Snippet
```java
    /**
     * @param uri
     * @param prefix
     */
    public OMNamespaceImpl(String uri, String prefix) {
```

### JavadocDeclaration
`@param uri` tag description is missing
in `axiom-compat/src/main/java/org/apache/axiom/om/impl/OMNamespaceImpl.java`
#### Snippet
```java
     * Method equals.
     *
     * @param uri
     * @param prefix
     * @return Returns boolean.
```

### JavadocDeclaration
`@param prefix` tag description is missing
in `axiom-compat/src/main/java/org/apache/axiom/om/impl/OMNamespaceImpl.java`
#### Snippet
```java
     *
     * @param uri
     * @param prefix
     * @return Returns boolean.
     */
```

### JavadocDeclaration
`@param currentChild` tag description is missing
in `axiom-compat/src/main/java/org/apache/axiom/om/impl/traverse/OMChildElementIterator.java`
#### Snippet
```java
     * Constructor OMChildrenIterator.
     *
     * @param currentChild
     */
    public OMChildElementIterator(OMElement currentChild) {
```

### JavadocDeclaration
`@param searchQName` tag description is missing
in `axiom-compat/src/main/java/org/apache/axiom/om/impl/traverse/OMChildrenQNameIterator.java`
#### Snippet
```java
     * The default algorithm is to use the QName equality (which examines the namespace and localPart).
     * You can extend this class to provide your own equality algorithm.
     * @param searchQName
     * @param currentQName
     * @return true if qnames are equal.
```

### JavadocDeclaration
`@param currentQName` tag description is missing
in `axiom-compat/src/main/java/org/apache/axiom/om/impl/traverse/OMChildrenQNameIterator.java`
#### Snippet
```java
     * You can extend this class to provide your own equality algorithm.
     * @param searchQName
     * @param currentQName
     * @return true if qnames are equal.
     */
```

### JavadocDeclaration
`@param searchQName` tag description is missing
in `axiom-compat/src/main/java/org/apache/axiom/om/impl/traverse/OMChildrenQNameIterator.java`
#### Snippet
```java
     * them an opportunity to use the old algorithm.
     * 
     * @param searchQName
     * @param currentQName
     * @return true using legacy equality match
```

### JavadocDeclaration
`@param currentQName` tag description is missing
in `axiom-compat/src/main/java/org/apache/axiom/om/impl/traverse/OMChildrenQNameIterator.java`
#### Snippet
```java
     * 
     * @param searchQName
     * @param currentQName
     * @return true using legacy equality match
     * @deprecated
```

### JavadocDeclaration
`@param currentChild` tag description is missing
in `axiom-compat/src/main/java/org/apache/axiom/om/impl/traverse/OMChildrenQNameIterator.java`
#### Snippet
```java
     * Constructor OMChildrenQNameIterator.
     *
     * @param currentChild
     * @param givenQName
     */
```

### JavadocDeclaration
`@param givenQName` tag description is missing
in `axiom-compat/src/main/java/org/apache/axiom/om/impl/traverse/OMChildrenQNameIterator.java`
#### Snippet
```java
     *
     * @param currentChild
     * @param givenQName
     */
    public OMChildrenQNameIterator(OMNode currentChild, QName givenQName) {
```

### JavadocDeclaration
`@param message` tag description is missing
in `axiom-compat/src/main/java/org/apache/axiom/om/impl/exception/OMStreamingException.java`
#### Snippet
```java
     * Constructor OMStreamingException
     *
     * @param message
     * @param cause
     */
```

### JavadocDeclaration
`@param cause` tag description is missing
in `axiom-compat/src/main/java/org/apache/axiom/om/impl/exception/OMStreamingException.java`
#### Snippet
```java
     *
     * @param message
     * @param cause
     */
    public OMStreamingException(String message, Throwable cause) {
```

### JavadocDeclaration
`@param cause` tag description is missing
in `axiom-compat/src/main/java/org/apache/axiom/om/impl/exception/OMStreamingException.java`
#### Snippet
```java
     * Constructor OMStreamingException
     *
     * @param cause
     */
    public OMStreamingException(Throwable cause) {
```

### JavadocDeclaration
`@param message` tag description is missing
in `axiom-compat/src/main/java/org/apache/axiom/om/impl/exception/OMStreamingException.java`
#### Snippet
```java
     * Constructor OMStreamingException
     *
     * @param message
     */
    public OMStreamingException(String message) {
```

### JavadocDeclaration
`@param s` tag description is missing
in `axiom-compat/src/main/java/org/apache/axiom/om/impl/exception/OMBuilderException.java`
#### Snippet
```java
     * Constructor OMBuilderException
     *
     * @param s
     */
    public OMBuilderException(String s) {
```

### JavadocDeclaration
`@param currentChild` tag description is missing
in `axiom-compat/src/main/java/org/apache/axiom/om/impl/traverse/OMChildrenWithSpecificAttributeIterator.java`
#### Snippet
```java
     * Constructor OMChildrenWithSpecificAttributeIterator.
     *
     * @param currentChild
     * @param attributeName
     * @param attributeValue
```

### JavadocDeclaration
`@param attributeName` tag description is missing
in `axiom-compat/src/main/java/org/apache/axiom/om/impl/traverse/OMChildrenWithSpecificAttributeIterator.java`
#### Snippet
```java
     *
     * @param currentChild
     * @param attributeName
     * @param attributeValue
     * @param detach
```

### JavadocDeclaration
`@param attributeValue` tag description is missing
in `axiom-compat/src/main/java/org/apache/axiom/om/impl/traverse/OMChildrenWithSpecificAttributeIterator.java`
#### Snippet
```java
     * @param currentChild
     * @param attributeName
     * @param attributeValue
     * @param detach
     */
```

### JavadocDeclaration
`@param detach` tag description is missing
in `axiom-compat/src/main/java/org/apache/axiom/om/impl/traverse/OMChildrenWithSpecificAttributeIterator.java`
#### Snippet
```java
     * @param attributeName
     * @param attributeValue
     * @param detach
     */
    public OMChildrenWithSpecificAttributeIterator(OMNode currentChild,
```

### JavadocDeclaration
`@param reader` tag description is missing
in `axiom-compat/src/main/java/org/apache/axiom/om/util/CopyUtils.java`
#### Snippet
```java
     * Simple utility that takes an XMLStreamReader and writes it to an XMLStreamWriter
     * 
     * @param reader
     * @param writer
     * @throws XMLStreamException
```

### JavadocDeclaration
`@param writer` tag description is missing
in `axiom-compat/src/main/java/org/apache/axiom/om/util/CopyUtils.java`
#### Snippet
```java
     * 
     * @param reader
     * @param writer
     * @throws XMLStreamException
     * 
```

### JavadocDeclaration
`@throws` tag description is missing
in `axiom-compat/src/main/java/org/apache/axiom/om/util/CopyUtils.java`
#### Snippet
```java
     * @param reader
     * @param writer
     * @throws XMLStreamException
     * 
     * @deprecated Instead use {@link OMXMLBuilderFactory#createStAXOMBuilder(XMLStreamReader)} to
```

### JavadocDeclaration
`@param ch` tag description is missing
in `axiom-compat/src/main/java/org/apache/axiom/om/impl/serialize/StreamWriterToContentHandlerConverter.java`
#### Snippet
```java
     * Method characters.
     *
     * @param ch
     * @param start
     * @param length
```

### JavadocDeclaration
`@param start` tag description is missing
in `axiom-compat/src/main/java/org/apache/axiom/om/impl/serialize/StreamWriterToContentHandlerConverter.java`
#### Snippet
```java
     *
     * @param ch
     * @param start
     * @param length
     * @throws SAXException
```

### JavadocDeclaration
`@param length` tag description is missing
in `axiom-compat/src/main/java/org/apache/axiom/om/impl/serialize/StreamWriterToContentHandlerConverter.java`
#### Snippet
```java
     * @param ch
     * @param start
     * @param length
     * @throws SAXException
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `axiom-compat/src/main/java/org/apache/axiom/om/impl/serialize/StreamWriterToContentHandlerConverter.java`
#### Snippet
```java
     * @param start
     * @param length
     * @throws SAXException
     */
    @Override
```

### JavadocDeclaration
`@param prefix` tag description is missing
in `axiom-compat/src/main/java/org/apache/axiom/om/impl/serialize/StreamWriterToContentHandlerConverter.java`
#### Snippet
```java
     * Method endPrefixMapping.
     *
     * @param prefix
     * @throws SAXException
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `axiom-compat/src/main/java/org/apache/axiom/om/impl/serialize/StreamWriterToContentHandlerConverter.java`
#### Snippet
```java
     *
     * @param prefix
     * @throws SAXException
     */
    @Override
```

### JavadocDeclaration
`@throws` tag description is missing
in `axiom-compat/src/main/java/org/apache/axiom/om/impl/serialize/StreamWriterToContentHandlerConverter.java`
#### Snippet
```java
     * Method startDocument.
     *
     * @throws SAXException
     */
    @Override
```

### JavadocDeclaration
`@param qName` tag description is missing
in `axiom-compat/src/main/java/org/apache/axiom/om/impl/serialize/StreamWriterToContentHandlerConverter.java`
#### Snippet
```java
     * Method getPrefix.
     *
     * @param qName
     * @return Returns String.
     */
```

### JavadocDeclaration
`@param target` tag description is missing
in `axiom-compat/src/main/java/org/apache/axiom/om/impl/serialize/StreamWriterToContentHandlerConverter.java`
#### Snippet
```java
     * Method processingInstruction.
     *
     * @param target
     * @param data
     * @throws SAXException
```

### JavadocDeclaration
`@param data` tag description is missing
in `axiom-compat/src/main/java/org/apache/axiom/om/impl/serialize/StreamWriterToContentHandlerConverter.java`
#### Snippet
```java
     *
     * @param target
     * @param data
     * @throws SAXException
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `axiom-compat/src/main/java/org/apache/axiom/om/impl/serialize/StreamWriterToContentHandlerConverter.java`
#### Snippet
```java
     * @param target
     * @param data
     * @throws SAXException
     */
    @Override
```

### JavadocDeclaration
`@param namespaceURI` tag description is missing
in `axiom-compat/src/main/java/org/apache/axiom/om/impl/serialize/StreamWriterToContentHandlerConverter.java`
#### Snippet
```java
     * Method startElement.
     *
     * @param namespaceURI
     * @param localName
     * @param qName
```

### JavadocDeclaration
`@param localName` tag description is missing
in `axiom-compat/src/main/java/org/apache/axiom/om/impl/serialize/StreamWriterToContentHandlerConverter.java`
#### Snippet
```java
     *
     * @param namespaceURI
     * @param localName
     * @param qName
     * @param atts
```

### JavadocDeclaration
`@param qName` tag description is missing
in `axiom-compat/src/main/java/org/apache/axiom/om/impl/serialize/StreamWriterToContentHandlerConverter.java`
#### Snippet
```java
     * @param namespaceURI
     * @param localName
     * @param qName
     * @param atts
     * @throws SAXException
```

### JavadocDeclaration
`@param atts` tag description is missing
in `axiom-compat/src/main/java/org/apache/axiom/om/impl/serialize/StreamWriterToContentHandlerConverter.java`
#### Snippet
```java
     * @param localName
     * @param qName
     * @param atts
     * @throws SAXException
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `axiom-compat/src/main/java/org/apache/axiom/om/impl/serialize/StreamWriterToContentHandlerConverter.java`
#### Snippet
```java
     * @param qName
     * @param atts
     * @throws SAXException
     */
    @Override
```

### JavadocDeclaration
`@param name` tag description is missing
in `axiom-compat/src/main/java/org/apache/axiom/om/impl/serialize/StreamWriterToContentHandlerConverter.java`
#### Snippet
```java
     * Method skippedEntity.
     *
     * @param name
     * @throws SAXException
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `axiom-compat/src/main/java/org/apache/axiom/om/impl/serialize/StreamWriterToContentHandlerConverter.java`
#### Snippet
```java
     *
     * @param name
     * @throws SAXException
     */
    @Override
```

### JavadocDeclaration
`@param writer` tag description is missing
in `axiom-compat/src/main/java/org/apache/axiom/om/impl/serialize/StreamWriterToContentHandlerConverter.java`
#### Snippet
```java
     * Constructor StreamWriterToContentHandlerConverter.
     *
     * @param writer
     */
    public StreamWriterToContentHandlerConverter(XMLStreamWriter writer) {
```

### JavadocDeclaration
`@param prefix` tag description is missing
in `axiom-compat/src/main/java/org/apache/axiom/om/impl/serialize/StreamWriterToContentHandlerConverter.java`
#### Snippet
```java
     * Method startPrefixMapping.
     *
     * @param prefix
     * @param uri
     * @throws SAXException
```

### JavadocDeclaration
`@param uri` tag description is missing
in `axiom-compat/src/main/java/org/apache/axiom/om/impl/serialize/StreamWriterToContentHandlerConverter.java`
#### Snippet
```java
     *
     * @param prefix
     * @param uri
     * @throws SAXException
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `axiom-compat/src/main/java/org/apache/axiom/om/impl/serialize/StreamWriterToContentHandlerConverter.java`
#### Snippet
```java
     * @param prefix
     * @param uri
     * @throws SAXException
     */
    @Override
```

### JavadocDeclaration
`@throws` tag description is missing
in `axiom-compat/src/main/java/org/apache/axiom/om/impl/serialize/StreamWriterToContentHandlerConverter.java`
#### Snippet
```java
     * Method endDocument.
     *
     * @throws SAXException
     */
    @Override
```

### JavadocDeclaration
`@param ch` tag description is missing
in `axiom-compat/src/main/java/org/apache/axiom/om/impl/serialize/StreamWriterToContentHandlerConverter.java`
#### Snippet
```java
     * Method ignorableWhitespace.
     *
     * @param ch
     * @param start
     * @param length
```

### JavadocDeclaration
`@param start` tag description is missing
in `axiom-compat/src/main/java/org/apache/axiom/om/impl/serialize/StreamWriterToContentHandlerConverter.java`
#### Snippet
```java
     *
     * @param ch
     * @param start
     * @param length
     * @throws SAXException
```

### JavadocDeclaration
`@param length` tag description is missing
in `axiom-compat/src/main/java/org/apache/axiom/om/impl/serialize/StreamWriterToContentHandlerConverter.java`
#### Snippet
```java
     * @param ch
     * @param start
     * @param length
     * @throws SAXException
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `axiom-compat/src/main/java/org/apache/axiom/om/impl/serialize/StreamWriterToContentHandlerConverter.java`
#### Snippet
```java
     * @param start
     * @param length
     * @throws SAXException
     */
    @Override
```

### JavadocDeclaration
`@param namespaceURI` tag description is missing
in `axiom-compat/src/main/java/org/apache/axiom/om/impl/serialize/StreamWriterToContentHandlerConverter.java`
#### Snippet
```java
     * Method endElement.
     *
     * @param namespaceURI
     * @param localName
     * @param qName
```

### JavadocDeclaration
`@param localName` tag description is missing
in `axiom-compat/src/main/java/org/apache/axiom/om/impl/serialize/StreamWriterToContentHandlerConverter.java`
#### Snippet
```java
     *
     * @param namespaceURI
     * @param localName
     * @param qName
     * @throws SAXException
```

### JavadocDeclaration
`@param qName` tag description is missing
in `axiom-compat/src/main/java/org/apache/axiom/om/impl/serialize/StreamWriterToContentHandlerConverter.java`
#### Snippet
```java
     * @param namespaceURI
     * @param localName
     * @param qName
     * @throws SAXException
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `axiom-compat/src/main/java/org/apache/axiom/om/impl/serialize/StreamWriterToContentHandlerConverter.java`
#### Snippet
```java
     * @param localName
     * @param qName
     * @throws SAXException
     */
    @Override
```

### JavadocDeclaration
`@param locator` tag description is missing
in `axiom-compat/src/main/java/org/apache/axiom/om/impl/serialize/StreamWriterToContentHandlerConverter.java`
#### Snippet
```java
     * Method setDocumentLocator.
     *
     * @param locator
     */
    @Override
```

### JavadocDeclaration
`@throws` tag description is missing
in `axiom-compat/src/main/java/org/apache/axiom/util/blob/WritableBlob.java`
#### Snippet
```java
     * @param commit indicates whether the blob should be in state COMMITTED after the operation
     * @return the number of bytes transferred
     * @throws StreamCopyException
     * @throws IllegalStateException if the blob is in state COMMITTED or if
     *         {@link #getOutputStream()} has been called before and <code>commit</code> is
```

### JavadocDeclaration
`@throws` tag description is missing
in `axiom-compat/src/main/java/org/apache/axiom/util/blob/WritableBlob.java`
#### Snippet
```java
     *               transfer data until the end of the input stream is reached
     * @return the number of bytes transferred
     * @throws StreamCopyException
     * @throws IllegalStateException if the blob is in state COMMITTED
     */
```

### JavadocDeclaration
`@param contentType` tag description is missing
in `axiom-compat/src/main/java/org/apache/axiom/om/util/CommonUtils.java`
#### Snippet
```java
     *   An part with a content-type of "application/soap+xml" is an textual attachment
     *
     * @param contentType
     * @return true if text, false otherwise
     * 
```

### JavadocDeclaration
`@param e` tag description is missing
in `axiom-compat/src/main/java/org/apache/axiom/om/util/CommonUtils.java`
#### Snippet
```java
     * Get a string containing the stack of the specified exception
     *
     * @param e
     * @return TODO
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `axiom-compat/src/main/java/org/apache/axiom/util/blob/Blob.java`
#### Snippet
```java
     * 
     * @return the input stream to read the data from
     * @throws IOException
     */
    InputStream getInputStream() throws IOException;
```

### JavadocDeclaration
`@param omElement` tag description is missing
in `axiom-compat/src/main/java/org/apache/axiom/om/util/ElementHelper.java`
#### Snippet
```java
     * This is a method to convert regular OMElements to SOAPHeaderBlocks.
     * 
     * @param omElement
     * @param factory
     * @return TODO
```

### JavadocDeclaration
`@param factory` tag description is missing
in `axiom-compat/src/main/java/org/apache/axiom/om/util/ElementHelper.java`
#### Snippet
```java
     * 
     * @param omElement
     * @param factory
     * @return TODO
     * @throws Exception
```

### JavadocDeclaration
`@throws` tag description is missing
in `axiom-compat/src/main/java/org/apache/axiom/om/util/ElementHelper.java`
#### Snippet
```java
     * @param factory
     * @return TODO
     * @throws Exception
     * 
     * @deprecated Use {@link SOAPFactory#createSOAPHeaderBlock(OMElement)} instead.
```

### JavadocDeclaration
`@throws` tag description is missing
in `axiom-compat/src/main/java/org/apache/axiom/util/stax/AbstractXMLStreamReader.java`
#### Snippet
```java
     * @return Returns int.
     *
     * @throws XMLStreamException
     */
    @Override
```

### JavadocDeclaration
`@param pattern` tag description is missing
in `axiom-compat/src/main/java/org/apache/axiom/attachments/utils/ByteSearch.java`
#### Snippet
```java
     * 
     * isEqual
     * @param pattern
     * @param buffer
     * @param start index
```

### JavadocDeclaration
`@param buffer` tag description is missing
in `axiom-compat/src/main/java/org/apache/axiom/attachments/utils/ByteSearch.java`
#### Snippet
```java
     * isEqual
     * @param pattern
     * @param buffer
     * @param start index
     * @param end index
```

### JavadocDeclaration
`@param pattern` tag description is missing
in `axiom-compat/src/main/java/org/apache/axiom/attachments/utils/ByteSearch.java`
#### Snippet
```java
     * Builds a skip array for this pattern and direction.
     * The skipArray is used in the optimized skipSearch
     * @param pattern
     * @param direction
     * @return short[256]
```

### JavadocDeclaration
`@param direction` tag description is missing
in `axiom-compat/src/main/java/org/apache/axiom/attachments/utils/ByteSearch.java`
#### Snippet
```java
     * The skipArray is used in the optimized skipSearch
     * @param pattern
     * @param direction
     * @return short[256]
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `axiom-compat/src/main/java/org/apache/axiom/util/blob/OverflowBlob.java`
#### Snippet
```java
     * 
     * @return an open FileOutputStream to the temporary file
     * @throws IOException
     */
    FileOutputStream switchToTempFile() throws IOException {
```

### JavadocDeclaration
`@param metaFactory` tag description is missing
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/sr/TestGetElementTextFromParser.java`
#### Snippet
```java
     * Constructor.
     *
     * @param metaFactory
     * @param builderFactory
     * @param cache
```

### JavadocDeclaration
`@param builderFactory` tag description is missing
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/sr/TestGetElementTextFromParser.java`
#### Snippet
```java
     *
     * @param metaFactory
     * @param builderFactory
     * @param cache
     * @param build the number of descendants that should be built before calling {@link
```

### JavadocDeclaration
`@param cache` tag description is missing
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/sr/TestGetElementTextFromParser.java`
#### Snippet
```java
     * @param metaFactory
     * @param builderFactory
     * @param cache
     * @param build the number of descendants that should be built before calling {@link
     *     OMContainer#getXMLStreamReader(boolean)}
```

### JavadocDeclaration
`@throws` tag description is missing
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/container/OMContainerExtractor.java`
#### Snippet
```java
     * @param testFileContent the content of the test file
     * @return the {@link InputSource} for the control document
     * @throws Exception
     */
    public abstract InputSource getControl(InputStream testFileContent) throws Exception;
```

### JavadocDeclaration
`@throws` tag description is missing
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/push/PushOMDataSourceScenario.java`
#### Snippet
```java
     *     XMLStreamWriter} are expected to have been preserved as is; {@code false} if they are
     *     expected to have been transformed to base64 encoded character data
     * @throws Throwable
     */
    void validate(OMElement element, boolean blobsPreserved) throws Throwable;
```

### JavadocDeclaration
`@throws` tag description is missing
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/push/PushOMDataSourceScenario.java`
#### Snippet
```java
     *
     * @param writer the {@link XMLStreamWriter}
     * @throws XMLStreamException
     */
    void serialize(XMLStreamWriter writer) throws XMLStreamException;
```

### JavadocDeclaration
`@param sourceEnv` tag description is missing
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/envelope/CloneTestCase.java`
#### Snippet
```java
     * Make a copy of the source envelope and validate the target tree
     *
     * @param sourceEnv
     * @throws Exception
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/envelope/CloneTestCase.java`
#### Snippet
```java
     *
     * @param sourceEnv
     * @throws Exception
     */
    protected void copyAndCheck(SOAPEnvelope sourceEnv) throws Exception {
```

### JavadocDeclaration
`@param source` tag description is missing
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/envelope/CloneTestCase.java`
#### Snippet
```java
     * Check the identity of each object in the tree
     *
     * @param source
     * @param target
     * @param depth
```

### JavadocDeclaration
`@param target` tag description is missing
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/envelope/CloneTestCase.java`
#### Snippet
```java
     *
     * @param source
     * @param target
     * @param depth
     */
```

### JavadocDeclaration
`@param depth` tag description is missing
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/envelope/CloneTestCase.java`
#### Snippet
```java
     * @param source
     * @param target
     * @param depth
     */
    protected void identityCheck(OMNode source, OMNode target, String depth) {
```

### JavadocDeclaration
`@param metaFactory` tag description is missing
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/SOAPTestSuiteBuilder.java`
#### Snippet
```java
     * Constructor.
     *
     * @param metaFactory
     */
    public SOAPTestSuiteBuilder(OMMetaFactory metaFactory) {
```

### JavadocDeclaration
`@throws` tag description is missing
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/dimension/serialization/SerializationStrategy.java`
#### Snippet
```java
     * @param container the container to serialize to XML
     * @return the serialized XML
     * @throws Exception
     */
    public abstract XML serialize(OMContainer container) throws Exception;
```

### JavadocDeclaration
`@throws` tag description is missing
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/dimension/serialization/XML.java`
#### Snippet
```java
     *
     * @param out the stream to write the XML data to
     * @throws IOException
     */
    void dump(PrintStream out) throws IOException;
```

### JavadocDeclaration
`@param container` tag description is missing
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/dimension/ExpansionStrategy.java`
#### Snippet
```java
     * Apply the expansion strategy to the given {@link OMContainer}.
     *
     * @param container
     */
    public abstract void apply(OMContainer container);
```

### JavadocDeclaration
`@param namespace` tag description is missing
in `mixins/om-mixins/src/main/java/org/apache/axiom/om/impl/mixin/AxiomNamedInformationItemMixin.java`
#### Snippet
```java
     * Set the namespace of the node without adding a corresponding namespace declaration.
     *
     * @param namespace
     */
    @Override
```

### JavadocDeclaration
`@param uri` tag description is missing
in `mixins/om-mixins/src/main/java/org/apache/axiom/om/impl/common/OMNamespaceImpl.java`
#### Snippet
```java
     * Method equals.
     *
     * @param uri
     * @param prefix
     * @return Returns boolean.
```

### JavadocDeclaration
`@param prefix` tag description is missing
in `mixins/om-mixins/src/main/java/org/apache/axiom/om/impl/common/OMNamespaceImpl.java`
#### Snippet
```java
     *
     * @param uri
     * @param prefix
     * @return Returns boolean.
     */
```

### JavadocDeclaration
`@param uri` tag description is missing
in `mixins/om-mixins/src/main/java/org/apache/axiom/om/impl/common/OMNamespaceImpl.java`
#### Snippet
```java

    /**
     * @param uri
     * @param prefix
     */
```

### JavadocDeclaration
`@param prefix` tag description is missing
in `mixins/om-mixins/src/main/java/org/apache/axiom/om/impl/common/OMNamespaceImpl.java`
#### Snippet
```java
    /**
     * @param uri
     * @param prefix
     */
    public OMNamespaceImpl(String uri, String prefix) {
```

### JavadocDeclaration
`@param localName` tag description is missing
in `mixins/om-mixins/src/main/java/org/apache/axiom/om/impl/mixin/AxiomSourcedElementMixin.java`
#### Snippet
```java
     * Constructor.
     *
     * @param localName
     * @param ns
     * @param factory
```

### JavadocDeclaration
`@param ns` tag description is missing
in `mixins/om-mixins/src/main/java/org/apache/axiom/om/impl/mixin/AxiomSourcedElementMixin.java`
#### Snippet
```java
     *
     * @param localName
     * @param ns
     * @param factory
     * @param source
```

### JavadocDeclaration
`@param factory` tag description is missing
in `mixins/om-mixins/src/main/java/org/apache/axiom/om/impl/mixin/AxiomSourcedElementMixin.java`
#### Snippet
```java
     * @param localName
     * @param ns
     * @param factory
     * @param source
     */
```

### JavadocDeclaration
`@param source` tag description is missing
in `mixins/om-mixins/src/main/java/org/apache/axiom/om/impl/mixin/AxiomSourcedElementMixin.java`
#### Snippet
```java
     * @param ns
     * @param factory
     * @param source
     */
    @Override
```

### JavadocDeclaration
`@param qName` tag description is missing
in `mixins/om-mixins/src/main/java/org/apache/axiom/om/impl/mixin/AxiomSourcedElementMixin.java`
#### Snippet
```java
     * Constructor that takes a QName instead of the local name and the namespace seperately
     *
     * @param qName
     * @param factory
     * @param source
```

### JavadocDeclaration
`@param factory` tag description is missing
in `mixins/om-mixins/src/main/java/org/apache/axiom/om/impl/mixin/AxiomSourcedElementMixin.java`
#### Snippet
```java
     *
     * @param qName
     * @param factory
     * @param source
     */
```

### JavadocDeclaration
`@param source` tag description is missing
in `mixins/om-mixins/src/main/java/org/apache/axiom/om/impl/mixin/AxiomSourcedElementMixin.java`
#### Snippet
```java
     * @param qName
     * @param factory
     * @param source
     */
    @Override
```

### JavadocDeclaration
`@param staxPrefix` tag description is missing
in `mixins/om-mixins/src/main/java/org/apache/axiom/om/impl/mixin/AxiomSourcedElementMixin.java`
#### Snippet
```java
     * at the same time as namespace repairing).
     *
     * @param staxPrefix
     * @param staxLocalName
     * @param staxNamespaceURI
```

### JavadocDeclaration
`@param staxLocalName` tag description is missing
in `mixins/om-mixins/src/main/java/org/apache/axiom/om/impl/mixin/AxiomSourcedElementMixin.java`
#### Snippet
```java
     *
     * @param staxPrefix
     * @param staxLocalName
     * @param staxNamespaceURI
     */
```

### JavadocDeclaration
`@param staxNamespaceURI` tag description is missing
in `mixins/om-mixins/src/main/java/org/apache/axiom/om/impl/mixin/AxiomSourcedElementMixin.java`
#### Snippet
```java
     * @param staxPrefix
     * @param staxLocalName
     * @param staxNamespaceURI
     */
    @Override
```

### JavadocDeclaration
`@throws` tag description is missing
in `components/io/src/main/java/org/apache/axiom/ext/io/ReadFromSupport.java`
#### Snippet
```java
     *            the number of bytes to transfer, or <code>-1</code> if the method should
     *            transfer data until the end of the input stream is reached
     * @throws StreamCopyException
     * @return the number of bytes transferred
     */
```

### JavadocDeclaration
Javadoc pointing to itself
in `components/core-streams/src/main/java/org/apache/axiom/core/stream/XmlReader.java`
#### Snippet
```java
     *
     * @return {@code true} if the last event sent to the {@link XmlHandler} was {@link
     *     XmlHandler#completed()} and there are no more events to produce; {@code false} if {@link
     *     #proceed()} should be called again to produce more events
     * @throws StreamException
```

### JavadocDeclaration
`@throws` tag description is missing
in `components/core-streams/src/main/java/org/apache/axiom/core/stream/XmlReader.java`
#### Snippet
```java
     *     XmlHandler#completed()} and there are no more events to produce; {@code false} if {@link
     *     #proceed()} should be called again to produce more events
     * @throws StreamException
     */
    boolean proceed() throws StreamException;
```

### JavadocDeclaration
`@throws` tag description is missing
in `components/core-streams/src/main/java/org/apache/axiom/core/stream/XmlHandler.java`
#### Snippet
```java
     * @param prefix the namespace prefix; never <code>null</code>
     * @param namespaceURI the namespace URI; never <code>null</code>
     * @throws StreamException
     */
    void processNamespaceDeclaration(String prefix, String namespaceURI) throws StreamException;
```

### JavadocDeclaration
`@throws` tag description is missing
in `components/core-streams/src/main/java/org/apache/axiom/core/stream/XmlHandler.java`
#### Snippet
```java
     *     {@code false} if at least one event was pushed and this method should be called again to
     *     continue draining the pipeline
     * @throws StreamException
     */
    boolean drain() throws StreamException;
```

### JavadocDeclaration
`@throws` tag description is missing
in `components/core-streams/src/main/java/org/apache/axiom/core/stream/XmlHandler.java`
#### Snippet
```java
     * @param value the value of the attribute; never <code>null</code>
     * @param type the attribute type (e.g. {@code CDATA}); never <code>null</code>
     * @throws StreamException
     */
    void processAttribute(String name, String value, String type, boolean specified)
```

### JavadocDeclaration
`@throws` tag description is missing
in `components/core-streams/src/main/java/org/apache/axiom/core/stream/XmlHandler.java`
#### Snippet
```java
     * @param localName the local name of the element; never <code>null</code>
     * @param prefix the prefix of the element; never <code>null</code>
     * @throws StreamException
     */
    void startElement(String namespaceURI, String localName, String prefix) throws StreamException;
```

### JavadocDeclaration
`@throws` tag description is missing
in `components/core-streams/src/main/java/org/apache/axiom/core/stream/XmlHandler.java`
#### Snippet
```java
     * @param value the value of the attribute; never <code>null</code>
     * @param type the attribute type (e.g. {@code CDATA}); never <code>null</code>
     * @throws StreamException
     */
    void processAttribute(
```

### JavadocDeclaration
`@param prefix` tag description is missing
in `components/core-streams/src/main/java/org/apache/axiom/core/stream/stax/push/input/XMLStreamWriterNamespaceContextProvider.java`
#### Snippet
```java

    /**
     * @param prefix
     * @param namespace
     * @return true if the prefix is associated with the namespace in the current context
```

### JavadocDeclaration
`@param namespace` tag description is missing
in `components/core-streams/src/main/java/org/apache/axiom/core/stream/stax/push/input/XMLStreamWriterNamespaceContextProvider.java`
#### Snippet
```java
    /**
     * @param prefix
     * @param namespace
     * @return true if the prefix is associated with the namespace in the current context
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `components/core-streams/src/main/java/org/apache/axiom/core/stream/serializer/writer/XmlWriter.java`
#### Snippet
```java
     * Write any pending data to the underlying stream, without flushing the stream itself.
     *
     * @throws IOException
     */
    public abstract void flushBuffer() throws IOException;
```

### JavadocDeclaration
`@throws` tag description is missing
in `components/core-streams/src/main/java/org/apache/axiom/core/stream/stax/pull/input/StAXPullReader.java`
#### Snippet
```java
     *
     * @return next token
     * @throws StreamException
     */
    private int parserNext() throws StreamException {
```

### JavadocDeclaration
`@throws` tag description is missing
in `components/core-streams/src/main/java/org/apache/axiom/core/stream/serializer/Serializer.java`
#### Snippet
```java
     * A private helper method to output the
     *
     * @throws StreamException
     * @throws IOException
     */
```

### JavadocDeclaration
StreamException is not declared to be thrown by method DTDprolog
in `components/core-streams/src/main/java/org/apache/axiom/core/stream/serializer/Serializer.java`
#### Snippet
```java
     * A private helper method to output the
     *
     * @throws StreamException
     * @throws IOException
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `components/core-streams/src/main/java/org/apache/axiom/core/stream/serializer/Serializer.java`
#### Snippet
```java
     *
     * @throws StreamException
     * @throws IOException
     */
    private void DTDprolog() throws IOException {
```

### JavadocDeclaration
Wrong tag `xsl.usage`
in `components/core-streams/src/main/java/org/apache/axiom/core/stream/serializer/Serializer.java`
#### Snippet
```java
 * output to a stream.
 *
 * @xsl.usage internal
 */
public final class Serializer implements XmlHandler, CharacterDataSink {
```

### JavadocDeclaration
`@throws` tag description is missing
in `components/core-streams/src/main/java/org/apache/axiom/core/stream/serializer/Serializer.java`
#### Snippet
```java
     * For the enclosing elements starting tag write out out any attributes followed by ">"
     *
     * @throws StreamException
     */
    private void closeStartTag() throws StreamException {
```

### JavadocDeclaration
`@throws` tag description is missing
in `components/core-streams/src/main/java/org/apache/axiom/core/stream/serializer/Serializer.java`
#### Snippet
```java
     * @return the output stream, or {@code null} if this serializer is not writing to an output
     *     stream (but to a {@link Writer} e.g.)
     * @throws StreamException
     */
    public OutputStream getOutputStream() throws StreamException {
```

### JavadocDeclaration
`@throws` tag description is missing
in `components/core-streams/src/main/java/org/apache/axiom/core/stream/serializer/Serializer.java`
#### Snippet
```java
     * @see #ignorableWhitespace
     * @see org.xml.sax.Locator
     * @throws StreamException
     */
    public void characters(char chars[], int start, int length) throws StreamException {
```

### JavadocDeclaration
`@throws` tag description is missing
in `components/core-streams/src/main/java/org/apache/axiom/core/stream/serializer/Serializer.java`
#### Snippet
```java
     *
     * @param name non-null reference to document type name. NEEDSDOC @param value
     * @throws StreamException
     */
    private void outputEntityDecl(String name, String value) throws IOException {
```

### JavadocDeclaration
StreamException is not declared to be thrown by method outputEntityDecl
in `components/core-streams/src/main/java/org/apache/axiom/core/stream/serializer/Serializer.java`
#### Snippet
```java
     *
     * @param name non-null reference to document type name. NEEDSDOC @param value
     * @throws StreamException
     */
    private void outputEntityDecl(String name, String value) throws IOException {
```

### JavadocDeclaration
`@throws` tag description is missing
in `systests/jaxws-tests/src/test/java/MTOMTest.java`
#### Snippet
```java
     * Regression test for <a href="https://issues.apache.org/jira/browse/AXIOM-492">AXIOM-492</a>.
     * 
     * @throws Exception
     */
    @Test
```

## RuleId[id=FieldMayBeFinal]
### FieldMayBeFinal
Field `xmlStreamReaderProvider` may be 'final'
in `testing/xml-truth/src/main/java/org/apache/axiom/truth/xml/StAXXML.java`
#### Snippet
```java

final class StAXXML implements XML {
    private XMLStreamReaderProvider xmlStreamReaderProvider;

    StAXXML(XMLStreamReaderProvider xmlStreamReaderProvider) {
```

### FieldMayBeFinal
Field `operandExpression` may be 'final'
in `testing/spring-ws-testsuite/src/main/java/org/apache/axiom/ts/springws/scenario/jdom/CalculatorEndpoint.java`
#### Snippet
```java
    private static final Namespace NAMESPACE = Namespace.getNamespace("c", NAMESPACE_URI);

    private XPathExpression<Element> operandExpression =
            XPathFactory.instance().compile("c:Operand", Filters.element(), null, NAMESPACE);

```

### FieldMayBeFinal
Field `test` may be 'final'
in `testing/dom-testsuite/src/main/java/org/apache/axiom/ts/dom/w3c/W3CTestCase.java`
#### Snippet
```java

public final class W3CTestCase extends MatrixTestCase {
    private DOMTestCase test;

    public W3CTestCase(DOMTestCase test) {
```

### FieldMayBeFinal
Field `namespaces` may be 'final'
in `axiom-api/src/main/java/org/apache/axiom/om/xpath/AXIOMXPath.java`
#### Snippet
```java
    private static final long serialVersionUID = -5839161412925154639L;

    private Map<String,String> namespaces = new HashMap<String,String>();

    /**
```

### FieldMayBeFinal
Field `IS_ADV_DEBUG_ENABLED` may be 'final'
in `axiom-api/src/main/java/org/apache/axiom/util/stax/debug/XMLStreamReaderValidator.java`
#### Snippet
```java

    private static final Log log = LogFactory.getLog(XMLStreamReaderValidator.class);
    private static boolean IS_ADV_DEBUG_ENABLED = false;  // Turn this on to trace every event
    
    private boolean throwExceptions = false;   // Indicates whether OMException should be thrown if errors are disovered
```

### FieldMayBeFinal
Field `stack` may be 'final'
in `axiom-api/src/main/java/org/apache/axiom/util/stax/debug/XMLStreamReaderValidator.java`
#### Snippet
```java
    
    private boolean throwExceptions = false;   // Indicates whether OMException should be thrown if errors are disovered
    private Stack<QName> stack = new Stack<QName>(); // Stack keeps track of the nested element QName
    

```

### FieldMayBeFinal
Field `data` may be 'final'
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/ByteArrayDataSource.java`
#### Snippet
```java
public class ByteArrayDataSource implements org.apache.axiom.ext.activation.SizeAwareDataSource {

    private byte[] data;

    private String type;
```

### FieldMayBeFinal
Field `acm` may be 'final'
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/CachedFileDataSource.java`
#### Snippet
```java
    
    // The AttachmentCacheMonitor is used to delete expired copies of attachment files.
    private static AttachmentCacheMonitor acm = 
        AttachmentCacheMonitor.getAttachmentCacheMonitor();
    
```

### FieldMayBeFinal
Field `parentContainer` may be 'final'
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/IncomingAttachmentInputStream.java`
#### Snippet
```java
    
    private InputStream _stream = null;
    private IncomingAttachmentStreams parentContainer;

    public static final String HEADER_CONTENT_DESCRIPTION = "content-description";
```

### FieldMayBeFinal
Field `files` may be 'final'
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/lifecycle/impl/VMShutdownHook.java`
#### Snippet
```java
    private static final Log log = LogFactory.getLog(VMShutdownHook.class);
    private static VMShutdownHook instance = null;
    private static Set<File> files = Collections.synchronizedSet(new HashSet<File>());
    private boolean isRegistered = false;

```

### FieldMayBeFinal
Field `accessCount` may be 'final'
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/lifecycle/impl/FileAccessor.java`
#### Snippet
```java
    File file = null;
    LifecycleManager manager;
    private int accessCount = 0;
    public FileAccessor(LifecycleManager manager, File file) {
        super();
```

### FieldMayBeFinal
Field `files` may be 'final'
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/AttachmentCacheMonitor.java`
#### Snippet
```java
    // Key String = Absolute file name
    // Value Long = Last Access Time
    private Map<String,Long> files = new HashMap<String,Long>();

    // Delete detection is batched
```

### FieldMayBeFinal
Field `table` may be 'final'
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/lifecycle/impl/LifecycleManagerImpl.java`
#### Snippet
```java

    //Hashtable to store file accessors.
    private static Hashtable<String,FileAccessor> table = new Hashtable<String,FileAccessor>();
    private VMShutdownHook hook = null;
    public LifecycleManagerImpl() {
```

### FieldMayBeFinal
Field `attributeName` may be 'final'
in `axiom-compat/src/main/java/org/apache/axiom/om/impl/traverse/OMChildrenWithSpecificAttributeIterator.java`
#### Snippet
```java
public class OMChildrenWithSpecificAttributeIterator extends OMFilterIterator {
    /** Field attributeName */
    private QName attributeName;

    /** Field attributeValue */
```

### FieldMayBeFinal
Field `attributeValue` may be 'final'
in `axiom-compat/src/main/java/org/apache/axiom/om/impl/traverse/OMChildrenWithSpecificAttributeIterator.java`
#### Snippet
```java

    /** Field attributeValue */
    private String attributeValue;

    /** Field detach */
```

### FieldMayBeFinal
Field `detach` may be 'final'
in `axiom-compat/src/main/java/org/apache/axiom/om/impl/traverse/OMChildrenWithSpecificAttributeIterator.java`
#### Snippet
```java

    /** Field detach */
    private boolean detach;

    private boolean doCaseSensitiveValueChecks = true;
```

### FieldMayBeFinal
Field `ignorableNamespaceList` may be 'final'
in `axiom-compat/src/main/java/org/apache/axiom/om/impl/llom/util/XMLComparator.java`
#### Snippet
```java
    private static final Log log = LogFactory.getLog(XMLComparator.class);

    private Vector ignorableNamespaceList = new Vector();

    public void addIgnorableNamespace(String nsURI) {
```

### FieldMayBeFinal
Field `writer` may be 'final'
in `axiom-compat/src/main/java/org/apache/axiom/om/impl/serialize/StreamWriterToContentHandlerConverter.java`
#### Snippet
```java

    /** Field writer */
    private XMLStreamWriter writer;

    /**
```

### FieldMayBeFinal
Field `limit` may be 'final'
in `axiom-compat/src/main/java/org/apache/axiom/om/util/LogOutputStream.java`
#### Snippet
```java
    private byte[] buffer = new byte[BUFFER_LEN];
    private int bufferIndex = 0;
    private int limit;
    
    public LogOutputStream(Log log, int limit) {
```

### FieldMayBeFinal
Field `BUFFER_LEN` may be 'final'
in `axiom-compat/src/main/java/org/apache/axiom/om/util/LogOutputStream.java`
#### Snippet
```java
    private long count = 0;
    private Log log;
    private int BUFFER_LEN = 4 * 1024;
    private byte[] buffer = new byte[BUFFER_LEN];
    private int bufferIndex = 0;
```

### FieldMayBeFinal
Field `element` may be 'final'
in `axiom-compat/src/main/java/org/apache/axiom/om/util/ElementHelper.java`
#### Snippet
```java
public class ElementHelper {

    private OMElement element;

    /**
```

### FieldMayBeFinal
Field `read_byte` may be 'final'
in `axiom-compat/src/main/java/org/apache/axiom/util/blob/MemoryBlob.java`
#### Snippet
```java
        private int mark;
        private byte[] currBuffer;
        private byte[] read_byte = new byte[1];
        
        public InputStreamImpl() {
```

### FieldMayBeFinal
Field `faultCodeMap` may be 'final'
in `testing/soap-testsuite/src/main/java/org/apache/axiom/ts/soap/ConvertedSOAPSampleContent.java`
#### Snippet
```java

final class ConvertedSOAPSampleContent extends ComputedMessageContent {
    private static Map<String, String> faultCodeMap = new HashMap<>();

    static {
```

### FieldMayBeFinal
Field `fetch` may be 'final'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/text/TestCloneBinary.java`
#### Snippet
```java

public class TestCloneBinary extends AxiomTestCase {
    private boolean fetch;

    public TestCloneBinary(OMMetaFactory metaFactory, boolean fetch) {
```

### FieldMayBeFinal
Field `event` may be 'final'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/container/DocumentElementExtractor.java`
#### Snippet
```java
 */
public class DocumentElementExtractor extends XMLStreamReaderWrapper {
    private int event = START_DOCUMENT;
    private int depth;

```

### FieldMayBeFinal
Field `expand` may be 'final'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestSetLocalName.java`
#### Snippet
```java
 */
public class TestSetLocalName extends AxiomTestCase {
    private boolean expand;

    public TestSetLocalName(OMMetaFactory metaFactory, boolean expand) {
```

### FieldMayBeFinal
Field `serializeParent` may be 'final'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/sourcedelement/TestGetSAXSourceWithPushOMDataSource.java`
#### Snippet
```java
public class TestGetSAXSourceWithPushOMDataSource extends AxiomTestCase {
    private final PushOMDataSourceScenario scenario;
    private boolean serializeParent;

    public TestGetSAXSourceWithPushOMDataSource(
```

### FieldMayBeFinal
Field `createDocument` may be 'final'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/xpath/TestXPathAppliedToSOAPEnvelope.java`
#### Snippet
```java
/** Regression test for <a href="https://issues.apache.org/jira/browse/AXIOM-141">AXIOM-141</a>. */
public class TestXPathAppliedToSOAPEnvelope extends SOAPTestCase {
    private boolean createDocument;

    public TestXPathAppliedToSOAPEnvelope(
```

### FieldMayBeFinal
Field `xml` may be 'final'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/envelope/TestSerializeAndConsumeWithOMSEInBody.java`
#### Snippet
```java
        private XMLStreamReader reader;
        // private String xml = "<?xml version='1.0'?><d:dummy name='1' xmlns:d='http://ns1'/>";
        private String xml =
                "<?xml version='1.0'?><d:dummy name='1' xmlns:d='http://ns1'>hello<mixed/>world</d:dummy>";

```

### FieldMayBeFinal
Field `types` may be 'final'
in `mixins/om-mixins/src/main/java/org/apache/axiom/om/impl/intf/Sequence.java`
#### Snippet
```java

public final class Sequence {
    private List<Class<? extends OMElement>> types;

    public Sequence(Class<?>... types) {
```

### FieldMayBeFinal
Field `elementNameStack` may be 'final'
in `components/core-streams/src/main/java/org/apache/axiom/core/stream/sax/output/ContentHandlerXmlHandler.java`
#### Snippet
```java
    private int[] scopeStack = new int[8];
    private int depth;
    private Stack<String> elementNameStack = new Stack<String>();
    private String elementURI;
    private String elementLocalName;
```

### FieldMayBeFinal
Field `locals` may be 'final'
in `axiom-weaver/src/main/java/org/apache/axiom/weaver/MethodInliner.java`
#### Snippet
```java
    private int maxStack;
    private int maxLocals;
    private Object[] locals;

    MethodInliner(MethodVisitor methodVisitor, int maxStack, int maxLocals, Object[] locals) {
```

## RuleId[id=ServletWithoutMappingInspection]
### ServletWithoutMappingInspection
Servlet should have a mapping
in `testing/axiom-testsuite/src/main/resources/web_w_dtd.xml`
#### Snippet
```java
<web-app>
  <servlet>
    <servlet-name>snoop</servlet-name>
    <servlet-class>SnoopServlet</servlet-class>
  </servlet>
```

### ServletWithoutMappingInspection
Servlet should have a mapping
in `testing/axiom-testsuite/src/main/resources/web_w_dtd.xml`
#### Snippet
```java
  </servlet>
  <servlet>
    <servlet-name>file</servlet-name>
    <servlet-class>ViewFile</servlet-class>
    <init-param>
```

## RuleId[id=CachedNumberConstructorCall]
### CachedNumberConstructorCall
Number constructor call with primitive argument
in `testing/testutils/src/main/java/org/apache/axiom/testutils/stax/XMLStreamReaderComparator.java`
#### Snippet
```java
            for (int i = 0; i < (attributeCount == null ? 1 : attributeCount.intValue()); i++) {
                Class<?>[] paramTypes = {Integer.TYPE};
                Object[] args = {new Integer(i)};
                assertSameResult(String.class, "getAttributeLocalName", paramTypes, args);
                assertSameResult(QName.class, "getAttributeName", paramTypes, args);
```

### CachedNumberConstructorCall
Number constructor call with primitive argument
in `axiom-legacy-attachments/src/main/java/org/apache/axiom/attachments/AttachmentCacheMonitor.java`
#### Snippet
```java

    private Long getTime() {
        return new Long(System.currentTimeMillis());
    }

```

## RuleId[id=CaughtExceptionImmediatelyRethrown]
### CaughtExceptionImmediatelyRethrown
Caught exception `e` is immediately rethrown
in `axiom-api/src/main/java/org/apache/axiom/om/xpath/AXIOMXPath.java`
#### Snippet
```java
        try {
            super.addNamespace(prefix, uri);
        } catch (JaxenException e) {
            // the intention here is to prevent us caching a namespace, if the
            // underlying implementation does not accept it
```

### CaughtExceptionImmediatelyRethrown
Caught exception `e` is immediately rethrown
in `axiom-compat/src/main/java/org/apache/axiom/attachments/utils/ByteSearch.java`
#### Snippet
```java
                    // pattern.  Otherwise we skip ahead a distance less
                    // than the length.
                } catch (ArrayIndexOutOfBoundsException e) {
                    throw e;
                }
```

### CaughtExceptionImmediatelyRethrown
Caught exception `e` is immediately rethrown
in `axiom-compat/src/main/java/org/apache/axiom/attachments/utils/ByteSearch.java`
#### Snippet
```java
                        return k;
                    }
                } catch (ArrayIndexOutOfBoundsException e) {
                    throw e;
                }
```

## RuleId[id=UnnecessaryContinue]
### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/container/DocumentElementExtractor.java`
#### Snippet
```java
                        break loop;
                    } else {
                        continue loop;
                    }
            }
```

### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `axiom-weaver/src/main/java/org/apache/axiom/weaver/ImplementationNode.java`
#### Snippet
```java
            for (Class<?> i : interfaces) {
                if (iface.isAssignableFrom(i)) {
                    continue;
                }
            }
```

## RuleId[id=SpringXmlAutowireExplicitlyInspection]
### SpringXmlAutowireExplicitlyInspection
Make autowired dependency explicit
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/broker/server.xml`
#### Snippet
```java
         http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.0.xsd
         http://www.springframework.org/schema/web-services http://www.springframework.org/schema/web-services/web-services-2.0.xsd"
       default-autowire="constructor">
    <bean id="customerService" class="org.apache.axiom.ts.springws.scenario.broker.MockCustomerService"/>
    <context:component-scan base-package="org.apache.axiom.ts.springws.scenario.broker"/>
```

## RuleId[id=SynchronizationOnLocalVariableOrMethodParameter]
### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `cache`
in `axiom-api/src/main/java/org/apache/axiom/util/UIDGenerator.java`
#### Snippet
```java
        urn.append("urn:uuid:");
        UUIDCache cache = uuidCaches[(int)Thread.currentThread().getId() & 0xF];
        synchronized (cache) {
            boolean fill;
            int position = cache.position;
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `systemProps`
in `testing/url-registry/src/main/java/org/apache/axiom/net/protocol/registry/URLRegistry.java`
#### Snippet
```java
        if (!handlerRegistered) {
            Properties systemProps = System.getProperties();
            synchronized (systemProps) {
                StringBuilder pkgs =
                        new StringBuilder(
```

## RuleId[id=UnnecessaryLocalVariable]
### UnnecessaryLocalVariable
Local variable `block` is redundant
in `axiom-compat/src/main/java/org/apache/axiom/om/util/ElementHelper.java`
#### Snippet
```java
        out.close();
        BlobOMDataSource ds = new BlobOMDataSource(blob, "utf-8");
        SOAPHeaderBlock block = factory.createSOAPHeaderBlock(localName, namespace, ds);
        
        return block;
```

## RuleId[id=CopyConstructorMissesField]
### CopyConstructorMissesField
Copy constructor does not copy fields 'mimeBoundary', 'rootContentId' and 'nextid'
in `axiom-api/src/main/java/org/apache/axiom/om/OMOutputFormat.java`
#### Snippet
```java
     *            the existing instance
     */
    public OMOutputFormat(OMOutputFormat format) {
        doOptimize = format.doOptimize;
        doingSWA = format.doingSWA;
```

## RuleId[id=DuplicatedBeanNamesInspection]
### DuplicatedBeanNamesInspection
Bean name duplicated
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/jaxb2/client.xml`
#### Snippet
```java
         http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.0.xsd
         http://www.springframework.org/schema/oxm http://www.springframework.org/schema/oxm/spring-oxm-4.0.xsd">
    <oxm:jaxb2-marshaller id="marshaller">
        <oxm:class-to-be-bound name="org.apache.axiom.ts.springws.scenario.jaxb2.GetQuoteRequest"/>
        <oxm:class-to-be-bound name="org.apache.axiom.ts.springws.scenario.jaxb2.GetQuoteResponse"/>
```

### DuplicatedBeanNamesInspection
Bean name duplicated
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/broker/client.xml`
#### Snippet
```java
        http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.0.xsd
        http://www.springframework.org/schema/oxm http://www.springframework.org/schema/oxm/spring-oxm-4.0.xsd">
    <oxm:jaxb2-marshaller id="marshaller">
        <oxm:class-to-be-bound name="org.apache.axiom.ts.springws.scenario.broker.Order"/>
        <oxm:class-to-be-bound name="org.apache.axiom.ts.springws.scenario.broker.OrderStatus"/>
```

### DuplicatedBeanNamesInspection
Bean name duplicated
in `testing/spring-ws-testsuite/src/main/resources/org/apache/axiom/ts/springws/scenario/validation/client.xml`
#### Snippet
```java
         http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.0.xsd
         http://www.springframework.org/schema/oxm http://www.springframework.org/schema/oxm/spring-oxm-4.0.xsd">
    <oxm:jaxb2-marshaller id="marshaller" context-path="org.apache.axiom.ts.springws.scenario.validation"/>
    <bean class="org.apache.axiom.ts.springws.scenario.validation.StockQuoteClient">
        <constructor-arg>
```

## RuleId[id=UseBulkOperation]
### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/soap/header/MyRolePlayer.java`
#### Snippet
```java
        this.roles = new ArrayList<>();
        for (int i = 0; i < roles.length; i++) {
            this.roles.add(roles[i]);
        }
    }
```

## RuleId[id=CastCanBeRemovedNarrowingVariableType]
### CastCanBeRemovedNarrowingVariableType
Cast may be removed by changing the type of 'o' to 'OMNode'
in `testing/axiom-testsuite/src/main/java/org/apache/axiom/ts/om/element/TestGetChildrenWithName.java`
#### Snippet
```java
            assertNotNull("Must return not null objects!", o);
            assertTrue(
                    "All these should be elements!", ((OMNode) o).getType() == OMNode.ELEMENT_NODE);
        }
        assertEquals(
```

