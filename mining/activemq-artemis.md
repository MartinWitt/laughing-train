# activemq-artemis 
 
# Bad smells
I found 6741 bad smells with 642 repairable:
| ruleID | number | fixable |
| --- | --- | --- |
| ReturnNull | 880 | false |
| RedundantFieldInitialization | 634 | false |
| PublicFieldAccessedInSynchronizedContext | 486 | false |
| FieldAccessedSynchronizedAndUnsynchronized | 416 | false |
| StringConcatenationInsideStringBufferAppend | 322 | false |
| UnnecessaryFullyQualifiedName | 318 | false |
| BoundedWildcard | 232 | false |
| AssignmentToMethodParameter | 187 | false |
| DataFlowIssue | 174 | false |
| SynchronizeOnThis | 156 | false |
| SystemOutErr | 152 | false |
| SizeReplaceableByIsEmpty | 151 | true |
| ConstantValue | 150 | false |
| UnusedAssignment | 146 | false |
| UnnecessaryBoxing | 114 | false |
| UtilityClassWithoutPrivateConstructor | 108 | true |
| StringBufferReplaceableByStringBuilder | 103 | false |
| Convert2Lambda | 97 | false |
| UnnecessaryLocalVariable | 89 | true |
| CatchMayIgnoreException | 88 | false |
| StringBufferReplaceableByString | 82 | false |
| NonProtectedConstructorInAbstractClass | 61 | true |
| Convert2MethodRef | 61 | false |
| DeprecatedIsStillUsed | 59 | false |
| ThrowablePrintStackTrace | 53 | false |
| ProtectedMemberInFinalClass | 51 | true |
| EmptyMethod | 50 | false |
| ZeroLengthArrayInitialization | 49 | false |
| DefaultAnnotationParam | 48 | false |
| RedundantMethodOverride | 47 | false |
| UnnecessaryToStringCall | 47 | true |
| NestedAssignment | 45 | false |
| UnnecessaryUnboxing | 44 | false |
| ObsoleteCollection | 43 | false |
| IgnoreResultOfCall | 41 | false |
| ToArrayCallWithZeroLengthArrayArgument | 40 | true |
| StringEqualsEmptyString | 36 | false |
| StringOperationCanBeSimplified | 32 | false |
| TrivialStringConcatenation | 30 | false |
| CommentedOutCode | 29 | false |
| FinalStaticMethod | 29 | false |
| BusyWait | 29 | false |
| UnnecessaryQualifierForThis | 28 | false |
| UnnecessarySuperQualifier | 28 | false |
| InstanceofCatchParameter | 27 | false |
| UnnecessaryReturn | 23 | true |
| PointlessBooleanExpression | 22 | true |
| DuplicateBranchesInSwitch | 22 | false |
| IOResource | 22 | false |
| ConditionCoveredByFurtherCondition | 22 | false |
| CodeBlock2Expr | 21 | true |
| UseBulkOperation | 20 | false |
| Java8MapApi | 20 | false |
| PointlessBitwiseExpression | 16 | false |
| SynchronizationOnLocalVariableOrMethodParameter | 16 | false |
| InnerClassMayBeStatic | 15 | true |
| Anonymous2MethodRef | 15 | false |
| UnnecessarySemicolon | 14 | false |
| RedundantSuppression | 13 | false |
| ExceptionNameDoesntEndWithException | 13 | false |
| ManualArrayToCollectionCopy | 12 | false |
| DynamicRegexReplaceableByCompiledPattern | 12 | false |
| StringEquality | 12 | false |
| ClassNameSameAsAncestorName | 10 | false |
| CharsetObjectCanBeUsed | 10 | false |
| ArrayEquality | 10 | false |
| RedundantStringFormatCall | 10 | false |
| ReplaceNullCheck | 10 | false |
| AssignmentToStaticFieldFromInstanceMethod | 9 | false |
| KeySetIterationMayUseEntrySet | 9 | false |
| RedundantArrayCreation | 9 | true |
| AnonymousHasLambdaAlternative | 9 | false |
| NonSerializableFieldInSerializableClass | 9 | false |
| DuplicateCondition | 8 | false |
| CallToStringConcatCanBeReplacedByOperator | 8 | false |
| NestedSynchronizedStatement | 8 | false |
| ReplaceAssignmentWithOperatorAssignment | 7 | false |
| RedundantClassCall | 7 | false |
| OctalLiteral | 7 | false |
| RedundantImplements | 7 | false |
| WrapperTypeMayBePrimitive | 6 | false |
| EmptyStatementBody | 6 | false |
| CaughtExceptionImmediatelyRethrown | 6 | false |
| EnumSwitchStatementWhichMissesCases | 6 | false |
| FieldMayBeStatic | 6 | false |
| NonSynchronizedMethodOverridesSynchronizedMethod | 5 | false |
| ManualMinMaxCalculation | 5 | false |
| MethodOverridesStaticMethod | 5 | false |
| RedundantLengthCheck | 5 | false |
| CopyConstructorMissesField | 5 | false |
| SuspiciousInvocationHandlerImplementation | 4 | false |
| CastConflictsWithInstanceof | 4 | false |
| SimplifyStreamApiCallChains | 4 | false |
| UnnecessaryInitCause | 4 | false |
| UnnecessaryStringEscape | 4 | true |
| NonShortCircuitBoolean | 4 | false |
| NonFinalFieldOfException | 4 | false |
| TypeParameterExtendsObject | 4 | false |
| Java9CollectionFactory | 4 | false |
| Finalize | 3 | false |
| StaticInitializerReferencesSubClass | 3 | false |
| ObjectNotify | 3 | false |
| NumberEquality | 3 | false |
| IntegerMultiplicationImplicitCastToLong | 3 | false |
| MismatchedCollectionQueryUpdate | 3 | false |
| StringRepeatCanBeUsed | 3 | false |
| OptionalUsedAsFieldOrParameterType | 3 | false |
| AssignmentToLambdaParameter | 3 | false |
| FunctionalExpressionCanBeFolded | 3 | false |
| WhileLoopSpinsOnField | 3 | false |
| IfStatementWithIdenticalBranches | 3 | false |
| RedundantCollectionOperation | 3 | false |
| AbstractMethodCallInConstructor | 3 | false |
| MismatchedArrayReadWrite | 2 | false |
| EmptyFinallyBlock | 2 | false |
| RegExpRedundantEscape | 2 | false |
| ExplicitArrayFilling | 2 | false |
| ReturnFromFinallyBlock | 2 | false |
| CollectionContainsUrl | 2 | false |
| ConditionalBreakInInfiniteLoop | 2 | false |
| AssertWithSideEffects | 2 | false |
| DuplicateExpressions | 2 | false |
| InfiniteLoopStatement | 2 | false |
| UseOfPropertiesAsHashtable | 2 | false |
| RedundantOperationOnEmptyContainer | 2 | false |
| Java8MapForEach | 2 | false |
| MismatchedJavadocCode | 2 | false |
| UnnecessaryModifier | 1 | true |
| MagicConstant | 1 | false |
| PointlessArithmeticExpression | 1 | false |
| WhileCanBeForeach | 1 | false |
| ForLoopReplaceableByWhile | 1 | false |
| ListRemoveInLoop | 1 | false |
| SimplifiableBooleanExpression | 1 | false |
| HtmlWrongAttributeValue | 1 | false |
| MethodOverloadsParentMethod | 1 | false |
| UnnecessaryCallToStringValueOf | 1 | false |
| UtilityClassWithPublicConstructor | 1 | false |
| ImplicitArrayToString | 1 | false |
| PrimitiveArrayArgumentToVariableArgMethod | 1 | false |
| RedundantFileCreation | 1 | false |
| LoopStatementsThatDontLoop | 1 | false |
| StaticCallOnSubclass | 1 | false |
| SuspiciousSystemArraycopy | 1 | false |
| ReadWriteStringCanBeUsed | 1 | false |
| AbstractClassNeverImplemented | 1 | false |
| MismatchedStringBuilderQueryUpdate | 1 | false |
| IfStatementMissingBreakInLoop | 1 | false |
| FinalizeNotProtected | 1 | false |
| ThrowFromFinallyBlock | 1 | false |
| NullArgumentToVariableArgMethod | 1 | false |
| OptionalGetWithoutIsPresent | 1 | false |
| ComparatorCombinators | 1 | false |
| AssignmentToForLoopParameter | 1 | false |
| SetReplaceableByEnumSet | 1 | false |
| UnnecessaryContinue | 1 | false |
| EqualsWhichDoesntCheckParameterClass | 1 | false |
| CastCanBeRemovedNarrowingVariableType | 1 | false |
## RuleId[id=ToArrayCallWithZeroLengthArrayArgument]
### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new URL\[urls.size()\]'
in `artemis-boot/src/main/java/org/apache/activemq/artemis/boot/Artemis.java`
#### Snippet
```java

      // Now setup our classloader..
      URLClassLoader loader = new URLClassLoader(urls.toArray(new URL[urls.size()]));
      Thread.currentThread().setContextClassLoader(loader);
      Class<?> clazz = loader.loadClass("org.apache.activemq.artemis.cli.Artemis");
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new String\[args.size()\]'
in `artemis-boot/src/main/java/org/apache/activemq/artemis/boot/Artemis.java`
#### Snippet
```java
    */
   public static Object execute(File artemisHome, File artemisInstance, File fileBrokerETC, boolean useSystemOut, List<String> args) throws Throwable {
      return execute(artemisHome, artemisInstance, fileBrokerETC, useSystemOut, args.toArray(new String[args.size()]));
   }

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new JournalFile\[dataFiles.size()\]'
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalFilesRepository.java`
#### Snippet
```java

   public JournalFile[] getDataFilesArray() {
      return dataFiles.toArray(new JournalFile[dataFiles.size()]);
   }

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new ConnectionEventListener\[eventListeners.size()\]'
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/ActiveMQRAManagedConnection.java`
#### Snippet
```java

      // convert to an array to avoid concurrent modification exceptions
      ConnectionEventListener[] list = eventListeners.toArray(new ConnectionEventListener[eventListeners.size()]);

      for (ConnectionEventListener l : list) {
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new PropertyDescriptor\[propsNotSet.size()\]'
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/inflow/ActiveMQActivationValidationUtils.java`
#### Snippet
```java
         }
         InvalidPropertyException e = new InvalidPropertyException(b.toString());
         final PropertyDescriptor[] descriptors = propsNotSet.toArray(new PropertyDescriptor[propsNotSet.size()]);
         e.setInvalidPropertyDescriptors(descriptors);
         throw e;
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new XAResource\[xaresources.size()\]'
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/ActiveMQResourceAdapter.java`
#### Snippet
```java
            }
         }
         return xaresources.toArray(new XAResource[xaresources.size()]);
      }
   }
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new String\[args.size()\]'
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/Artemis.java`
#### Snippet
```java

   public static Object execute(File artemisHome, File artemisInstance, File etcFolder, List<String> args) throws Exception {
      return execute(false, false, artemisHome, artemisInstance, etcFolder, args.toArray(new String[args.size()]));
   }

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new CheckTask\[checkTasks.size()\]'
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/check/NodeCheck.java`
#### Snippet
```java
      }

      return checkTasks.toArray(new CheckTask[checkTasks.size()]);
   }

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new CheckTask\[checkTasks.size()\]'
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/check/QueueCheck.java`
#### Snippet
```java
      }

      return checkTasks.toArray(new CheckTask[checkTasks.size()]);
   }

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Connection\[connections.size()\]'
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/perf/ProducerBenchmark.java`
#### Snippet
```java
      }
      CompletableFuture<?> allGeneratorClosed = CompletableFuture.allOf(closedGenerators);
      final Connection[] openedConnections = connections.toArray(new Connection[connections.size()]);
      this.allGeneratorClosed = allGeneratorClosed.whenCompleteAsync((res, error) -> {
         synchronized (this) {
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new RecordingMessageListener\[listeners.size()\]'
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/perf/MessageListenerBenchmark.java`
#### Snippet
```java

   public synchronized RecordingMessageListener[] getListeners() {
      return listeners == null ? null : listeners.toArray(new RecordingMessageListener[listeners.size()]);
   }

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new TransactionId\[recovered.size()\]'
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenWireConnection.java`
#### Snippet
```java
            recovered.add(amqXid);
         }
         return new DataArrayResponse(recovered.toArray(new TransactionId[recovered.size()]));
      }

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new String\[bindingsToAdd.size()\]'
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/server/impl/JMSServerManagerImpl.java`
#### Snippet
```java
                  }

                  usedBindings = bindingsToAdd.toArray(new String[bindingsToAdd.size()]);
                  addToBindings(queueBindings, queueName, usedBindings);
               }
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new String\[bindingsToAdd.size()\]'
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/server/impl/JMSServerManagerImpl.java`
#### Snippet
```java
               }

               String[] usedBindings = bindingsToAdd.toArray(new String[bindingsToAdd.size()]);
               addToBindings(topicBindings, address, usedBindings);

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new String\[bindings.size()\]'
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/server/impl/JMSServerManagerImpl.java`
#### Snippet
```java
      }

      String[] usedBindings = bindings.toArray(new String[bindings.size()]);

      ActiveMQConnectionFactory realCF = internalCreateCFPOJO(cf);
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new String\[bindingsToAdd.size()\]'
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/server/impl/JMSServerManagerImpl.java`
#### Snippet
```java
            }

            String[] usedBindings = bindingsToAdd.toArray(new String[bindingsToAdd.size()]);
            addToBindings(connectionFactoryBindings, cfConfig.getName(), usedBindings);

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new String\[files.size()\]'
in `artemis-web/src/main/java/org/apache/activemq/artemis/component/WebTmpCleaner.java`
#### Snippet
```java
      if (!invm) {
         String classPath = SpawnedVMSupport.getClassPath(libFolder);
         return SpawnedVMSupport.spawnVM(classPath, WebTmpCleaner.class.getName(), false, (String[]) files.toArray(new String[files.size()]));
      }
      cleanupFilesWithRetry(files.toArray(new String[files.size()]), 2);
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new String\[files.size()\]'
in `artemis-web/src/main/java/org/apache/activemq/artemis/component/WebTmpCleaner.java`
#### Snippet
```java
         return SpawnedVMSupport.spawnVM(classPath, WebTmpCleaner.class.getName(), false, (String[]) files.toArray(new String[files.size()]));
      }
      cleanupFilesWithRetry(files.toArray(new String[files.size()]), 2);
      return null;
   }
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new String\[protocols.size()\]'
in `artemis-server-osgi/src/main/java/org/apache/activemq/artemis/osgi/OsgiBroker.java`
#### Snippet
```java
         }
      }
      return protocols.toArray(new String[protocols.size()]);
   }

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new String\[interfaces.size()\]'
in `artemis-server-osgi/src/main/java/org/apache/activemq/artemis/osgi/OsgiBroker.java`
#### Snippet
```java
      Set<String> interfaces = new HashSet<>();
      getInterfaces(value.getClass(), interfaces);
      return interfaces.toArray(new String[interfaces.size()]);
   }

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new TransportConfiguration\[newInitialconnectors.size()\]'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ServerLocatorImpl.java`
#### Snippet
```java
      }

      this.initialConnectors = newInitialconnectors.toArray(new TransportConfiguration[newInitialconnectors.size()]);

      if (clusterConnection && !receivedTopology && this.getNumInitialConnectors() > 0) {
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new String\[itemNamesList.size()\]'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/message/openmbean/MessageOpenTypeFactory.java`
#### Snippet
```java

   protected CompositeType createCompositeType() throws OpenDataException {
      String[] itemNames = itemNamesList.toArray(new String[itemNamesList.size()]);
      String[] itemDescriptions = itemDescriptionsList.toArray(new String[itemDescriptionsList.size()]);
      OpenType[] itemTypes = itemTypesList.toArray(new OpenType[itemTypesList.size()]);
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new String\[itemDescriptionsList.size()\]'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/message/openmbean/MessageOpenTypeFactory.java`
#### Snippet
```java
   protected CompositeType createCompositeType() throws OpenDataException {
      String[] itemNames = itemNamesList.toArray(new String[itemNamesList.size()]);
      String[] itemDescriptions = itemDescriptionsList.toArray(new String[itemDescriptionsList.size()]);
      OpenType[] itemTypes = itemTypesList.toArray(new OpenType[itemTypesList.size()]);
      return new CompositeType(getTypeName(), getDescription(), itemNames, itemDescriptions, itemTypes);
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new OpenType\[itemTypesList.size()\]'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/message/openmbean/MessageOpenTypeFactory.java`
#### Snippet
```java
      String[] itemNames = itemNamesList.toArray(new String[itemNamesList.size()]);
      String[] itemDescriptions = itemDescriptionsList.toArray(new String[itemDescriptionsList.size()]);
      OpenType[] itemTypes = itemTypesList.toArray(new OpenType[itemTypesList.size()]);
      return new CompositeType(getTypeName(), getDescription(), itemNames, itemDescriptions, itemTypes);
   }
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Xid\[xids.size()\]'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/ActiveMQSessionContext.java`
#### Snippet
```java
      List<Xid> xids = response.getXids();

      Xid[] xidArray = xids.toArray(new Xid[xids.size()]);

      if (logger.isTraceEnabled()) {
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new TransportConfiguration\[list.size()\]'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/ClusterConnectionConfiguration.java`
#### Snippet
```java
         }

         return list.toArray(new TransportConfiguration[list.size()]);
      } else {
         return staticConnectors != null ? configuration.getTransportConfigurations(staticConnectors) : null;
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new SimpleString\[names.size()\]'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/impl/PagingManagerImpl.java`
#### Snippet
```java
   public SimpleString[] getStoreNames() {
      Set<SimpleString> names = stores.keySet();
      return names.toArray(new SimpleString[names.size()]);
   }

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Object\[resources.size()\]'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/impl/ManagementServiceImpl.java`
#### Snippet
```java
         }
      }
      return resources.toArray(new Object[resources.size()]);
   }

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new String\[staticConnectors.size()\]'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/BridgeControlImpl.java`
#### Snippet
```java
      try {
         List<String> staticConnectors = bridge.getConfiguration().getStaticConnectors();
         return staticConnectors.toArray(new String[staticConnectors.size()]);
      } finally {
         blockOnIO();
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new MBeanAttributeInfo\[finalAttributes.size()\]'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/MBeanInfoHelper.java`
#### Snippet
```java
            }
         }
         MBeanAttributeInfo[] result = finalAttributes.toArray(new MBeanAttributeInfo[finalAttributes.size()]);
         attributesInfoCache.put(mbeanInterface, result);
         return result;
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new MBeanOperationInfo\[operations.size()\]'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/MBeanInfoHelper.java`
#### Snippet
```java
            }
         }
         MBeanOperationInfo[] result = operations.toArray(new MBeanOperationInfo[operations.size()]);
         operationsInfoCache.put(mbeanInterface, result);
         return result;
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new String\[set.size()\]'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/NettyAcceptor.java`
#### Snippet
```java
      warningPrinted.set(true);

      engine.setEnabledProtocols(set.toArray(new String[set.size()]));

      if (verifyHost) {
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new String\[staticConnectors.size()\]'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/ClusterConnectionControlImpl.java`
#### Snippet
```java
            return null;
         } else {
            return staticConnectors.toArray(new String[staticConnectors.size()]);
         }
      } finally {
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new String\[queueNames.size()\]'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/AddressControlImpl.java`
#### Snippet
```java
               }
            }
            return queueNames.toArray(new String[queueNames.size()]);
         } else {
            return new String[0];
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Map\[messages.size()\]'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/QueueControlImpl.java`
#### Snippet
```java
               // this could happen through paging browsing
            }
            return messages.toArray(new Map[messages.size()]);
         }
      } catch (ActiveMQException e) {
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new String\[mappedRoles.size()\]'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/deployers/impl/FileConfigurationParser.java`
#### Snippet
```java
         mappedRoles.add(role);
      }
      return mappedRoles.toArray(new String[mappedRoles.size()]);
   }

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new String\[configuration.getOutgoingInterceptorClassNames().size()\]'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/ActiveMQServerControlImpl.java`
#### Snippet
```java
      clearIO();
      try {
         return configuration.getOutgoingInterceptorClassNames().toArray(new String[configuration.getOutgoingInterceptorClassNames().size()]);
      } finally {
         blockOnIO();
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new String\[configuration.getIncomingInterceptorClassNames().size()\]'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/ActiveMQServerControlImpl.java`
#### Snippet
```java
      clearIO();
      try {
         return configuration.getIncomingInterceptorClassNames().toArray(new String[configuration.getIncomingInterceptorClassNames().size()]);
      } finally {
         blockOnIO();
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new String\[remoteConnections.size()\]'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/ActiveMQServerControlImpl.java`
#### Snippet
```java
            }
         }
         return remoteConnections.toArray(new String[remoteConnections.size()]);
      } finally {
         blockOnIO();
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new String\[configuration.getIncomingInterceptorClassNames().size()\]'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/ActiveMQServerControlImpl.java`
#### Snippet
```java
      clearIO();
      try {
         return configuration.getIncomingInterceptorClassNames().toArray(new String[configuration.getIncomingInterceptorClassNames().size()]);
      } finally {
         blockOnIO();
```

## RuleId[id=Finalize]
### Finalize
'finalize()' should not be overridden
in `artemis-unit-test-support/src/main/java/org/apache/activemq/artemis/utils/ThreadLeakCheckRule.java`
#### Snippet
```java

      @Override
      public void finalize() throws Throwable {
         finalized.countDown();
         super.finalize();
```

### Finalize
'finalize()' should not be overridden
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/jdbc/JdbcLeaseLock.java`
#### Snippet
```java

   @Override
   protected void finalize() throws Throwable {
      close();
   }
```

### Finalize
'finalize()' should not be overridden
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/jdbc/JdbcNodeManager.java`
#### Snippet
```java

   @Override
   protected void finalize() throws Throwable {
      stop();
   }
```

## RuleId[id=WrapperTypeMayBePrimitive]
### WrapperTypeMayBePrimitive
Type may be primitive
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/xml/XMLMessageImporter.java`
#### Snippet
```java

   private Byte getMessageType(String value) {
      Byte type = Message.DEFAULT_TYPE;
      switch (value) {
         case XmlDataConstants.DEFAULT_TYPE_PRETTY:
```

### WrapperTypeMayBePrimitive
Type may be primitive
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/xml/XMLMessageImporter.java`
#### Snippet
```java

      Byte type = 0;
      Byte priority = 0;
      long expiration = 0L;
      long timestamp = 0L;
```

### WrapperTypeMayBePrimitive
Type may be primitive
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ScaleDownHandler.java`
#### Snippet
```java

   private Long getQueueID(ClientSession session, SimpleString queueName) throws Exception {
      Long queueID = -1L;
      Object result;
      try (ClientRequestor requestor = new ClientRequestor(session, "activemq.management")) {
```

### WrapperTypeMayBePrimitive
Type may be primitive
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/ha/ColocatedHAManager.java`
#### Snippet
```java
            boolean httpUpgradeEnabled = ConfigurationHelper.getBooleanProperty(TransportConstants.HTTP_UPGRADE_ENABLED_PROP_NAME, TransportConstants.DEFAULT_HTTP_UPGRADE_ENABLED, params);
            if (!httpUpgradeEnabled) {
               Integer integer = Integer.valueOf(port.toString());
               integer += portOffset;
               params.put(TransportConstants.PORT_PROP_NAME, integer.toString());
```

### WrapperTypeMayBePrimitive
Type may be primitive
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/security/impl/SecurityStoreImpl.java`
#### Snippet
```java
         }

         final Boolean validated;
         if (securityManager instanceof ActiveMQSecurityManager5) {
            Subject subject = getSubjectForAuthorization(session, ((ActiveMQSecurityManager5) securityManager));
```

### WrapperTypeMayBePrimitive
Type may be primitive
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/deployers/impl/FileConfigurationParser.java`
#### Snippet
```java
      Long delayBeforeDispatch = null;
      Boolean enabled = null;
      Long ringSize = ActiveMQDefaultConfiguration.getDefaultRingSize();

      NamedNodeMap attributes = node.getAttributes();
```

## RuleId[id=MismatchedArrayReadWrite]
### MismatchedArrayReadWrite
Contents of array `obj` are written to, but never read
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/utils/MemorySize.java`
#### Snippet
```java
      // First we do a dry run with twice as many then throw away the results

      Object[] obj = new Object[MemorySize.numberOfObjects * 2];

      for (i = 0; i < MemorySize.numberOfObjects * 2; i++) {
```

### MismatchedArrayReadWrite
Contents of array `journalLoadInformation` are written to, but never read
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/replication/ReplicationEndpoint.java`
#### Snippet
```java

   private Journal[] journals;
   private final JournalLoadInformation[] journalLoadInformation = new JournalLoadInformation[2];

   /**
```

## RuleId[id=UnnecessaryModifier]
### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `artemis-protocols/artemis-stomp-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/stomp/Stomp.java`
#### Snippet
```java
         String ACTIVEMQ_NO_LOCAL = "activemq.noLocal";

         public interface AckModeValues {

            String AUTO = "auto";
```

## RuleId[id=MagicConstant]
### MagicConstant
Should be one of: Calendar.JANUARY, Calendar.FEBRUARY, Calendar.MARCH, Calendar.APRIL, Calendar.MAY, ...
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalImpl.java`
#### Snippet
```java

      calendar.clear();
      calendar.set(year, month - 1, day, hour, minutes, seconds);
      return calendar.getTimeInMillis();
   }
```

## RuleId[id=PointlessArithmeticExpression]
### PointlessArithmeticExpression
`1 * 60 * 1000` can be replaced with '60 \* 1000'
in `artemis-protocols/artemis-stomp-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/stomp/StompProtocolManager.java`
#### Snippet
```java
      } else {
         // Default to 1 minute - which is same as core protocol
         return new ConnectionEntry(conn, null, System.currentTimeMillis(), 1 * 60 * 1000);
      }
   }
```

## RuleId[id=EmptyStatementBody]
### EmptyStatementBody
`while` statement has empty body
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/ConcurrentAppendOnlyChunkedList.java`
#### Snippet
```java
   private static <E> E pollElement(AtomicChunk<E> buffer, int i) {
      E e;
      while ((e = buffer.get(i)) == null) {
      }
      return e;
```

### EmptyStatementBody
`while` statement has empty body
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/LinkedListImpl.java`
#### Snippet
```java
      //   more than one generation
      // - is sure to free memory even if there is a reachable Iterator
      while (poll() != null) {

      }
```

### EmptyStatementBody
`if` statement has empty body
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTProtocolHandler.java`
#### Snippet
```java

      MqttReasonCodeAndPropertiesVariableHeader header = (MqttReasonCodeAndPropertiesVariableHeader) auth.variableHeader();
      if (header.reasonCode() == MQTTReasonCodes.RE_AUTHENTICATE) {

      } else if (header.reasonCode() == MQTTReasonCodes.CONTINUE_AUTHENTICATION) {
```

### EmptyStatementBody
`if` statement has empty body
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTProtocolHandler.java`
#### Snippet
```java
      if (header.reasonCode() == MQTTReasonCodes.RE_AUTHENTICATE) {

      } else if (header.reasonCode() == MQTTReasonCodes.CONTINUE_AUTHENTICATION) {

      } else if (header.reasonCode() == MQTTReasonCodes.SUCCESS) {
```

### EmptyStatementBody
`if` statement has empty body
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTProtocolHandler.java`
#### Snippet
```java
      } else if (header.reasonCode() == MQTTReasonCodes.CONTINUE_AUTHENTICATION) {

      } else if (header.reasonCode() == MQTTReasonCodes.SUCCESS) {

      }
```

### EmptyStatementBody
`while` statement has empty body
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/cursor/impl/PageSubscriptionImpl.java`
#### Snippet
```java
      public synchronized boolean hasNext() {
         NextResult status;
         while ((status = tryNext()) == NextResult.retry) {
         }
         return status == NextResult.hasElements ? true : false;
```

## RuleId[id=WhileCanBeForeach]
### WhileCanBeForeach
`while` loop can be replaced with enhanced 'for'
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/security/jaas/PropertiesLoginModuleConfigurator.java`
#### Snippet
```java
      //do update
      Iterator<Pair<String, List<String>>> iterUpdate = updateMap.iterator();
      while (iterUpdate.hasNext()) {
         Pair<String, List<String>> entry = iterUpdate.next();
         roleConfig.clearProperty(entry.getA());
```

## RuleId[id=StaticInitializerReferencesSubClass]
### StaticInitializerReferencesSubClass
Referencing subclass BooleanConstantExpression from superclass ConstantExpression initializer might lead to class loading deadlock
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/ConstantExpression.java`
#### Snippet
```java
   public static final BooleanConstantExpression NULL = new BooleanConstantExpression(null);
   public static final BooleanConstantExpression TRUE = new BooleanConstantExpression(Boolean.TRUE);
   public static final BooleanConstantExpression FALSE = new BooleanConstantExpression(Boolean.FALSE);

   private Object value;
```

### StaticInitializerReferencesSubClass
Referencing subclass BooleanConstantExpression from superclass ConstantExpression initializer might lead to class loading deadlock
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/ConstantExpression.java`
#### Snippet
```java

   public static final BooleanConstantExpression NULL = new BooleanConstantExpression(null);
   public static final BooleanConstantExpression TRUE = new BooleanConstantExpression(Boolean.TRUE);
   public static final BooleanConstantExpression FALSE = new BooleanConstantExpression(Boolean.FALSE);

```

### StaticInitializerReferencesSubClass
Referencing subclass BooleanConstantExpression from superclass ConstantExpression initializer might lead to class loading deadlock
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/ConstantExpression.java`
#### Snippet
```java
   }

   public static final BooleanConstantExpression NULL = new BooleanConstantExpression(null);
   public static final BooleanConstantExpression TRUE = new BooleanConstantExpression(Boolean.TRUE);
   public static final BooleanConstantExpression FALSE = new BooleanConstantExpression(Boolean.FALSE);
```

## RuleId[id=EmptyFinallyBlock]
### EmptyFinallyBlock
Empty `finally` block
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTUtil.java`
#### Snippet
```java

            return size;
         } finally {
         }
      } catch (RuntimeException e) {
```

### EmptyFinallyBlock
Empty `finally` block
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/ProtonServerSenderContext.java`
#### Snippet
```java
         // This is because on AMQP we only send messages based in credits, not bytes
         return 1;
      } finally {

      }
```

## RuleId[id=CommentedOutCode]
### CommentedOutCode
Commented out code (2 lines)
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
      if (source[srcOffset + 2] == Base64.EQUALS_SIGN) {
         // Two ways to do the same thing. Don't know which way I like best.
         // int outBuff = ( ( DECODABET[ source[ srcOffset ] ] << 24 ) >>> 6 )
         // | ( ( DECODABET[ source[ srcOffset + 1] ] << 24 ) >>> 12 );
         int outBuff = (DECODABET[source[srcOffset]] & 0xFF) << 18 | (DECODABET[source[srcOffset + 1]] & 0xFF) << 12;
```

### CommentedOutCode
Commented out code (3 lines)
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java

         // Two ways to do the same thing. Don't know which way I like best.
         // int outBuff = ( ( DECODABET[ source[ srcOffset ] ] << 24 ) >>> 6 )
         // | ( ( DECODABET[ source[ srcOffset + 1 ] ] << 24 ) >>> 12 )
         // | ( ( DECODABET[ source[ srcOffset + 2 ] ] << 24 ) >>> 18 );
```

### CommentedOutCode
Commented out code (4 lines)
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
         try {
            // Two ways to do the same thing. Don't know which way I like best.
            // int outBuff = ( ( DECODABET[ source[ srcOffset ] ] << 24 ) >>> 6 )
            // | ( ( DECODABET[ source[ srcOffset + 1 ] ] << 24 ) >>> 12 )
            // | ( ( DECODABET[ source[ srcOffset + 2 ] ] << 24 ) >>> 18 )
```

### CommentedOutCode
Commented out code (2 lines)
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
            b = read();

            // if( b < 0 && i == 0 )
            // return -1;

```

### CommentedOutCode
Commented out code (24 lines)
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/server/impl/JMSServerManagerImpl.java`
#### Snippet
```java
    * for that topic.
    */
//   class JMSPostQueueDeletionCallback implements PostQueueDeletionCallback {
//
//      @Override
```

### CommentedOutCode
Commented out code (3 lines)
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/server/impl/JMSServerManagerImpl.java`
#### Snippet
```java
      }

//      server.setJMSQueueCreator(new JMSDestinationCreator());
//
//      server.setJMSQueueDeleter(new JMSQueueDeleter());
```

### CommentedOutCode
Commented out code (3 lines)
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/server/impl/JMSServerManagerImpl.java`
#### Snippet
```java
      server.registerActivateCallback(this);

//      server.registerPostQueueCreationCallback(new JMSPostQueueCreationCallback());
//
//      server.registerPostQueueDeletionCallback(new JMSPostQueueDeletionCallback());
```

### CommentedOutCode
Commented out code (35 lines)
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/server/impl/JMSServerManagerImpl.java`
#### Snippet
```java
    * to a non-existent JMS queue or topic
    */
//   class JMSDestinationCreator implements QueueCreator {
//
//      @Override
```

### CommentedOutCode
Commented out code (17 lines)
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/server/impl/JMSServerManagerImpl.java`
#### Snippet
```java
    * autoCreateJmsTopics = true.
    */
//   class JMSPostQueueCreationCallback implements PostQueueCreationCallback {
//
//      @Override
```

### CommentedOutCode
Commented out code (5 lines)
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/handler/ProtonHandler.java`
#### Snippet
```java
      });

      /*try {
         Thread.sleep(1000);
      } catch (Exception e) {
```

### CommentedOutCode
Commented out code (4 lines)
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionRequestProducerCreditsMessage.java`
#### Snippet
```java
   }

   // public boolean isRequiresConfirmations()
   // {
   // return false;
```

### CommentedOutCode
Commented out code (2 lines)
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/utils/MemorySize.java`
#### Snippet
```java

      if (totalMemory1 != totalMemory2) {
         // throw new IllegalStateException("Warning: JVM allocated more data what would make results invalid " +
         // totalMemory1 + ":" + totalMemory2);

```

### CommentedOutCode
Commented out code (3 lines)
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/reader/MessageUtil.java`
#### Snippet
```java
   public static final SimpleString CONNECTION_ID_PROPERTY_NAME = new SimpleString(CONNECTION_ID_PROPERTY_NAME_STRING);

   //   public static ActiveMQBuffer getBodyBuffer(Message message) {
   //      return message.getBodyBuffer();
   //   }
```

### CommentedOutCode
Commented out code (9 lines)
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/security/jaas/ReloadableProperties.java`
#### Snippet
```java
      try (FileInputStream in = new FileInputStream(source)) {
         props.load(in);
         //            if (key.isDecrypt()) {
         //                try {
         //                    EncryptionSupport.decrypt(this.props);
```

### CommentedOutCode
Commented out code (2 lines)
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/security/scram/StringPrep.java`
#### Snippet
```java
            // so it's included in the class.
            return true;
            // while(pos > 0 && tupleStart.get(pos-1) == c)
            // --pos;
         }
```

### CommentedOutCode
Commented out code (2 lines)
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/impl/BridgeImpl.java`
#### Snippet
```java
   @Override
   public void beforeReconnect(final ActiveMQException exception) {
      // log.warn("{}::Connection failed before reconnect ", name, exception);
      // fail(false);
   }
```

### CommentedOutCode
Commented out code (5 lines)
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/impl/ClusterConnectionImpl.java`
#### Snippet
```java

      // TODO: shouldn't we send the current time here? and change the current topology?
      // sf.sendNodeAnnounce(System.currentTimeMillis(),
      // manager.getNodeId(),
      // false,
```

### CommentedOutCode
Commented out code (5 lines)
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/impl/ConfigurationImpl.java`
#### Snippet
```java
      if (persistenceEnabled != other.persistenceEnabled)
         return false;
//      if (queueConfigurations == null) {
//         if (other.queueConfigurations != null)
//            return false;
```

### CommentedOutCode
Commented out code (2 lines)
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/ManagementService.java`
#### Snippet
```java
   void unregisterBroadcastGroup(String name) throws Exception;

   //  void registerDiscoveryGroup(DiscoveryGroup discoveryGroup, DiscoveryGroupConfiguration configuration) throws Exception;

   //void unregisterDiscoveryGroup(String name) throws Exception;
```

### CommentedOutCode
Commented out code (5 lines)
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/ActiveMQPacketHandler.java`
#### Snippet
```java

         // XXX HORNETQ-720 Taylor commented out this test. Should be verified.
         /*if (!server.checkActivate())
         {
            throw new ActiveMQException(ActiveMQException.SESSION_CREATION_REJECTED,
```

### CommentedOutCode
Commented out code (2 lines)
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/ResourceLimitSettings.java`
#### Snippet
```java
      result = prime * result + ((maxConnections == null) ? 0 : maxConnections.hashCode());
      result = prime * result + ((maxQueues == null) ? 0 : maxQueues.hashCode());
      //      result = prime * result + ((maxQueueSizeBytes == null) ? 0 : maxQueueSizeBytes.hashCode());
      //      result = prime * result + ((queueNameRegex == null) ? 0 : queueNameRegex.hashCode());
      return result;
```

### CommentedOutCode
Commented out code (2 lines)
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/ResourceLimitSettings.java`
#### Snippet
```java
         BufferHelper.sizeOfNullableInteger(maxConnections) +
         BufferHelper.sizeOfNullableInteger(maxQueues);
      //              BufferHelper.sizeOfNullableLong(maxQueueSizeBytes) +
      //              SimpleString.sizeofNullableString(queueNameRegex);
   }
```

### CommentedOutCode
Commented out code (2 lines)
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/ResourceLimitSettings.java`
#### Snippet
```java
      BufferHelper.writeNullableInteger(buffer, maxQueues);

      //      BufferHelper.writeNullableLong(buffer, maxQueueSizeBytes);

      //      buffer.writeNullableSimpleString(queueNameRegex);
```

### CommentedOutCode
Commented out code (2 lines)
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/ResourceLimitSettings.java`
#### Snippet
```java
      maxQueues = BufferHelper.readNullableInteger(buffer);

      //      maxQueueSizeBytes = BufferHelper.readNullableLong(buffer);

      //      queueNameRegex = buffer.readNullableSimpleString();
```

### CommentedOutCode
Commented out code (2 lines)
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/ResourceLimitSettings.java`
#### Snippet
```java
   public static final Integer DEFAULT_MAX_QUEUES = -1;

   //   public static final Long DEFAULT_MAX_QUEUE_SIZE_BYTES = -1L;

   //   public static final SimpleString DEFAULT_QUEUE_NAME_REGEX = new SimpleString(".+");
```

### CommentedOutCode
Commented out code (2 lines)
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/ResourceLimitSettings.java`
#### Snippet
```java
   Integer maxQueues = null;

   //   Long maxQueueSizeBytes = null;

   //   SimpleString queueNameRegex = null;
```

### CommentedOutCode
Commented out code (9 lines)
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/ResourceLimitSettings.java`
#### Snippet
```java
   }

   //   public long getMaxQueueSizeBytes()
   //   {
   //      return maxQueueSizeBytes != null ? maxQueueSizeBytes : DEFAULT_MAX_QUEUE_SIZE_BYTES;
```

### CommentedOutCode
Commented out code (9 lines)
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/ResourceLimitSettings.java`
#### Snippet
```java
   }

   //   public void setMaxQueueSizeBytes(long maxQueueSizeBytes)
   //   {
   //      this.maxQueueSizeBytes = maxQueueSizeBytes;
```

### CommentedOutCode
Commented out code (2 lines)
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/codec/DuplicateIDEncoding.java`
#### Snippet
```java
      // You may uncomment these two lines on that case and replcate the toString for the PrintData

      // SimpleString simpleStr = new SimpleString(duplID);
      // return "DuplicateIDEncoding [address=" + address + ", duplID=" + simpleStr + "]";

```

## RuleId[id=AssignmentToStaticFieldFromInstanceMethod]
### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `transactionManager` from instance context
in `artemis-service-extensions/src/main/java/org/apache/activemq/artemis/service/extensions/ServiceUtils.java`
#### Snippet
```java
            Iterator<TransactionManagerLocator> it = ServiceLoader.load(TransactionManagerLocator.class, ServiceUtils.class.getClassLoader()).iterator();
            while (it.hasNext() && transactionManager == null) {
               transactionManager = it.next().getTransactionManager();
            }
            return transactionManager;
```

### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `sr` from instance context
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/DefaultSensitiveStringCodec.java`
#### Snippet
```java
         skf = SecretKeyFactory.getInstance(sceretKeyAlgorithm);
         if (sr == null) {
            sr = SecureRandom.getInstance(randomScheme);
         }
      }
```

### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `serverLockFile` from instance context
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/LockAbstract.java`
#### Snippet
```java
         if (serverLockFile == null) {
            File fileLock = new File(lockPlace, "cli.lock");
            serverLockFile = new RandomAccessFile(fileLock, "rw");
         }
         try {
```

### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `serverLockLock` from instance context
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/LockAbstract.java`
#### Snippet
```java
               throw new CLIException("Error: There is another process using the server at " + lockPlace + ". Cannot start the process!");
            }
            serverLockLock = lock;
         } catch (OverlappingFileLockException e) {
            throw new CLIException("Error: There is another process using the server at " + lockPlace + ". Cannot start the process!");
```

### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `instance` from instance context
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/SharedEventLoopGroup.java`
#### Snippet
```java
                           }
                        });
                        instance = null;
                     }
                  }
```

### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `transportConfigurationType` from instance context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/ConnectionRouterControlImpl.java`
#### Snippet
```java
   private CompositeType getTransportConfigurationType() throws OpenDataException {
      if (transportConfigurationType == null) {
         transportConfigurationType = new CompositeType(TransportConfiguration.class.getName(),
            "TransportConfiguration", new String[]{"name", "factoryClassName", "params"},
            new String[]{"TransportConfiguration name", "TransportConfiguration factoryClassName", "TransportConfiguration params"},
```

### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `parametersType` from instance context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/ConnectionRouterControlImpl.java`
#### Snippet
```java
   private TabularType getParametersType() throws OpenDataException {
      if (parametersType == null) {
         parametersType = new TabularType("java.util.Map<java.lang.String, java.lang.String>",
            "Parameters", getParameterType(), new String[]{"key"});
      }
```

### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `targetType` from instance context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/ConnectionRouterControlImpl.java`
#### Snippet
```java
   private CompositeType getTargetCompositeType() throws OpenDataException {
      if (targetType == null) {
         targetType = new CompositeType(Target.class.getName(),
            "Target", new String[]{"nodeID", "local", "connector"},
            new String[]{"Target nodeID", "Target local", "Target connector"},
```

### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `parameterType` from instance context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/ConnectionRouterControlImpl.java`
#### Snippet
```java
   private CompositeType getParameterType() throws OpenDataException {
      if (parameterType == null) {
         parameterType = new CompositeType("java.util.Map.Entry<java.lang.String, java.lang.String>",
            "Parameter", new String[]{"key", "value"}, new String[]{"Parameter key", "Parameter value"},
            new OpenType[]{SimpleType.STRING, SimpleType.STRING});
```

## RuleId[id=RegExpRedundantEscape]
### RegExpRedundantEscape
Redundant character escape `\\}` in RegExp
in `artemis-dto/src/main/java/org/apache/activemq/artemis/dto/XmlUtil.java`
#### Snippet
```java
   static class PropertiesFilter extends StreamReaderDelegate {

      static final Pattern pattern = Pattern.compile("\\$\\{([^\\}]+)\\}");
      private final Properties props;

```

### RegExpRedundantEscape
Redundant character escape `\\}` in RegExp
in `artemis-dto/src/main/java/org/apache/activemq/artemis/dto/XmlUtil.java`
#### Snippet
```java
   static class PropertiesFilter extends StreamReaderDelegate {

      static final Pattern pattern = Pattern.compile("\\$\\{([^\\}]+)\\}");
      private final Properties props;

```

## RuleId[id=KeySetIterationMayUseEntrySet]
### KeySetIterationMayUseEntrySet
Iteration over `postThreads.keySet()` may be replaced with 'entrySet()' iteration
in `artemis-unit-test-support/src/main/java/org/apache/activemq/artemis/utils/ThreadLeakCheckRule.java`
#### Snippet
```java
      if (postThreads != null && previousThreads != null && postThreads.size() > previousThreads.size()) {

         for (Thread aliveThread : postThreads.keySet()) {
            if (aliveThread.isAlive() && !isExpectedThread(aliveThread) && !previousThreads.containsKey(aliveThread)) {
               if (!failedThread) {
```

### KeySetIterationMayUseEntrySet
Iteration over `protocols.keySet()` may be replaced with 'entrySet()' iteration
in `artemis-server-osgi/src/main/java/org/apache/activemq/artemis/osgi/ProtocolTracker.java`
#### Snippet
```java
   private List<String> getMissing() {
      List<String> missing = new ArrayList<>();
      for (String protocol : protocols.keySet()) {
         Boolean present = protocols.get(protocol);
         if (!present) {
```

### KeySetIterationMayUseEntrySet
Iteration over `update.keySet()` may be replaced with 'entrySet()' iteration
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/JsonUtil.java`
#### Snippet
```java
      }
      // apply any updates
      for (String updateKey : update.keySet()) {
         JsonValue updatedValue = update.get(updateKey);
         if (updatedValue != null) {
```

### KeySetIterationMayUseEntrySet
Iteration over `keys` may be replaced with 'entrySet()' iteration
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/JsonUtil.java`
#### Snippet
```java
            Set<String> keys = jsonObject.keySet();

            for (String key : keys) {
               Object innerVal = jsonObject.get(key);

```

### KeySetIterationMayUseEntrySet
Iteration over `innerKeys` may be replaced with 'entrySet()' iteration
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/JsonUtil.java`
#### Snippet
```java
                  JsonObject o = (JsonObject) innerVal;
                  Set<String> innerKeys = o.keySet();
                  for (String k : innerKeys) {
                     innerMap.put(k, o.get(k));
                  }
```

### KeySetIterationMayUseEntrySet
Iteration over `entries.keySet()` may be replaced with 'entrySet()' iteration
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/message/openmbean/MessageOpenTypeFactory.java`
#### Snippet
```java
      TabularDataSupport answer = new TabularDataSupport(type);

      for (String key : entries.keySet()) {
         Object value = entries.get(key);
         if (valueType.isInstance(value)) {
```

### KeySetIterationMayUseEntrySet
Iteration over `perPageMap.keySet()` may be replaced with 'entrySet()' iteration
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/PostOfficeJournalLoader.java`
#### Snippet
```java
         assert (perPageMap != null);

         for (Long pageId : perPageMap.keySet()) {
            Map<Long, List<PageCountPending>> perQueue = perPageMap.get(pageId);

```

### KeySetIterationMayUseEntrySet
Iteration over `protocolSet` may be replaced with 'entrySet()' iteration
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/protocol/ProtocolHandler.java`
#### Snippet
```java
            in.getBytes(0, bytes);

            for (String protocol : protocolSet) {
               ProtocolManager protocolManager = protocolMap.get(protocol);
               if (protocolManager.isProtocol(bytes)) {
```

### KeySetIterationMayUseEntrySet
Iteration over `keys` may be replaced with 'entrySet()' iteration
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/ActiveMQServerControlImpl.java`
#### Snippet
```java
               StringBuilder propstr = new StringBuilder();
               Set<String> keys = props.keySet();
               for (String key : keys) {
                  propstr.append(key);
                  propstr.append("=");
```

## RuleId[id=ForLoopReplaceableByWhile]
### ForLoopReplaceableByWhile
`for` loop statement may be replace by 'while' loop
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/ProtonServerSenderContext.java`
#### Snippet
```java
               }

               for (; sender.getLocalState() != EndpointState.CLOSED && position < bodySize; ) {
                  if (!connection.flowControl(this::resume)) {
                     return;
```

## RuleId[id=ObjectNotify]
### ObjectNotify
`notify` should probably be replaced with 'notifyAll()'
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/bridge/impl/JMSBridgeImpl.java`
#### Snippet
```java
               }
            }
            this.notify();
         }
      }
```

### ObjectNotify
`notify` should probably be replaced with 'notifyAll()'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ClientConsumerImpl.java`
#### Snippet
```java
            if (receiverThread != null) {
               // Wake up any receive() thread that might be waiting
               notify();
            }

```

### ObjectNotify
`notify` should probably be replaced with 'notifyAll()'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ClientConsumerImpl.java`
#### Snippet
```java
         }
      } else {
         notify();
      }
   }
```

## RuleId[id=UnnecessaryQualifierForThis]
### UnnecessaryQualifierForThis
Qualifier `ActiveMQScheduledComponent` on 'this' is unnecessary in this context
in `artemis-commons/src/main/java/org/apache/activemq/artemis/core/server/ActiveMQScheduledComponent.java`
#### Snippet
```java
      boolean alwaysTrue = booked.compareAndSet(true, false);
      assert alwaysTrue;
      ActiveMQScheduledComponent.this.run();
   }

```

### UnnecessaryQualifierForThis
Qualifier `AIOSequentialCallback` on 'this' is unnecessary in this context
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/io/aio/AIOSequentialFileFactory.java`
#### Snippet
```java
            }

            callbackPool.offer(AIOSequentialCallback.this);
         }
      }
```

### UnnecessaryQualifierForThis
Qualifier `LinkedListImpl` on 'this' is unnecessary in this context
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/LinkedListImpl.java`
#### Snippet
```java

      if (toRemove.iterCount != 0) {
         LinkedListImpl.this.nudgeIterators(toRemove);
      }

```

### UnnecessaryQualifierForThis
Qualifier `JournalImpl` on 'this' is unnecessary in this context
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalImpl.java`
#### Snippet
```java
      if (logger.isDebugEnabled()) {
         ++compactCount;  // Member used only for logging state when debug enabled
         logger.debug("JournalImpl::compact {} for its {} time", JournalImpl.this, compactCount);
      }

```

### UnnecessaryQualifierForThis
Qualifier `ActiveMQJMSConsumer` on 'this' is unnecessary in this context
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQJMSConsumer.java`
#### Snippet
```java
      try {
         Message message = consumer.receive();
         context.setLastMessage(ActiveMQJMSConsumer.this, message);
         return message == null ? null : message.getBody(c);
      } catch (JMSException e) {
```

### UnnecessaryQualifierForThis
Qualifier `ActiveMQJMSConsumer` on 'this' is unnecessary in this context
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQJMSConsumer.java`
#### Snippet
```java
      try {
         Message message = consumer.receive(timeout);
         context.setLastMessage(ActiveMQJMSConsumer.this, message);
         return message == null ? null : message.getBody(c);
      } catch (JMSException e) {
```

### UnnecessaryQualifierForThis
Qualifier `ActiveMQJMSConsumer` on 'this' is unnecessary in this context
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQJMSConsumer.java`
#### Snippet
```java
      try {
         Message message = consumer.receiveNoWait();
         context.setLastMessage(ActiveMQJMSConsumer.this, message);
         return message == null ? null : message.getBody(c);
      } catch (JMSException e) {
```

### UnnecessaryQualifierForThis
Qualifier `AMQPConnectionContext` on 'this' is unnecessary in this context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/AMQPConnectionContext.java`
#### Snippet
```java
         connection.close();
      } else {
         connection.setContext(AMQPConnectionContext.this);
         connection.setContainer(containerId);
         connection.setProperties(connectionProperties);
```

### UnnecessaryQualifierForThis
Qualifier `ACKMessageOperation` on 'this' is unnecessary in this context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/connect/mirror/AMQPMirrorControllerTarget.java`
#### Snippet
```java

      public void connectionRun() {
         connection.runNow(ACKMessageOperation.this);
      }

```

### UnnecessaryQualifierForThis
Qualifier `ACKMessageOperation` on 'this' is unnecessary in this context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/connect/mirror/AMQPMirrorControllerTarget.java`
#### Snippet
```java
         settle(delivery);
         connection.flush();
         AMQPMirrorControllerTarget.this.ackMessageMpscPool.release(ACKMessageOperation.this);
      }

```

### UnnecessaryQualifierForThis
Qualifier `CachingSSLContextFactory` on 'super' is unnecessary in this context
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/impl/ssl/CachingSSLContextFactory.java`
#### Snippet
```java
      return sslContextCache.computeIfAbsent(cacheKey, key -> {
         try {
            return CachingSSLContextFactory.super.getSSLContext(config, additionalOpts);
         } catch (final Exception ex) {
            throw new RuntimeException("An unexpected exception occured while creating JDK SSLContext with " + config, ex);
```

### UnnecessaryQualifierForThis
Qualifier `CursorIterator` on 'this' is unnecessary in this context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/cursor/impl/PageSubscriptionImpl.java`
#### Snippet
```java
                     ignored = true;
                  } else {
                     if (tx.deliverAfterCommit(CursorIterator.this, PageSubscriptionImpl.this, message)) {
                        valid = false;
                        ignored = false;
```

### UnnecessaryQualifierForThis
Qualifier `PageSubscriptionImpl` on 'this' is unnecessary in this context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/cursor/impl/PageSubscriptionImpl.java`
#### Snippet
```java
                     if (result == 0) {
                        try {
                           PageSubscriptionImpl.this.ackTx(tx, reference);
                           if (scanElemen.found != null) {
                              afterCommitList.add(scanElemen.found);
```

### UnnecessaryQualifierForThis
Qualifier `ServerConsumerImpl` on 'this' is unnecessary in this context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerConsumerImpl.java`
#### Snippet
```java

         applyPrefixForLegacyConsumer(forcedDeliveryMessage);
         callback.sendMessage(reference, forcedDeliveryMessage, ServerConsumerImpl.this, 0);
      });
   }
```

### UnnecessaryQualifierForThis
Qualifier `ServerConsumerImpl` on 'this' is unnecessary in this context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerConsumerImpl.java`
#### Snippet
```java
   private void deliverStandardMessage(final MessageReference ref, Message message) throws ActiveMQException {
      applyPrefixForLegacyConsumer(message);
      int packetSize = callback.sendMessage(ref, message, ServerConsumerImpl.this, ref.getDeliveryCount());

      if (availableCredits != null) {
```

### UnnecessaryQualifierForThis
Qualifier `BridgeImpl` on 'this' is unnecessary in this context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/impl/BridgeImpl.java`
#### Snippet
```java
            blockedOnFlowControl = false;
            producer = session.createProducer();
            session.addFailureListener(BridgeImpl.this);

            session.setSendAcknowledgementHandler(BridgeImpl.this);
```

### UnnecessaryQualifierForThis
Qualifier `BridgeImpl` on 'this' is unnecessary in this context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/impl/BridgeImpl.java`
#### Snippet
```java
            session.addFailureListener(BridgeImpl.this);

            session.setSendAcknowledgementHandler(BridgeImpl.this);

            afterConnect();
```

### UnnecessaryQualifierForThis
Qualifier `BridgeImpl` on 'this' is unnecessary in this context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/impl/BridgeImpl.java`
#### Snippet
```java
            active = true;

            queue.addConsumer(BridgeImpl.this);
            queue.deliverAsync();

```

### UnnecessaryQualifierForThis
Qualifier `BridgeImpl` on 'this' is unnecessary in this context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/impl/BridgeImpl.java`
#### Snippet
```java
         if (this.targetNodeID != null && this.targetNodeID.equals(member.getNodeId())) {
            // this could be an update of the topology say after a backup started
            BridgeImpl.this.targetNode = member;
         } else {
            // we don't need synchronization here, but we need to make sure we won't get a NPE on races
```

### UnnecessaryQualifierForThis
Qualifier `BridgeImpl` on 'this' is unnecessary in this context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/impl/BridgeImpl.java`
#### Snippet
```java
   @Override
   public void resume() throws Exception {
      queue.addConsumer(BridgeImpl.this);
      queue.deliverAsync();
   }
```

### UnnecessaryQualifierForThis
Qualifier `ActiveMQServerImpl` on 'this' is unnecessary in this context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ActiveMQServerImpl.java`
#### Snippet
```java
            criticalAction = criticalComponent -> {

               if (ActiveMQServerImpl.this.state == SERVER_STATE.STARTING) {
                  takingLongToStart(criticalComponent);
               } else {
```

### UnnecessaryQualifierForThis
Qualifier `ActiveMQServerImpl` on 'this' is unnecessary in this context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ActiveMQServerImpl.java`
#### Snippet
```java
            criticalAction = criticalComponent -> {

               if (ActiveMQServerImpl.this.state == SERVER_STATE.STARTING) {
                  takingLongToStart(criticalComponent);
               } else {
```

### UnnecessaryQualifierForThis
Qualifier `ActiveMQServerImpl` on 'this' is unnecessary in this context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ActiveMQServerImpl.java`
#### Snippet
```java
         case LOG:
            criticalAction = criticalComponent -> {
               if (ActiveMQServerImpl.this.state == SERVER_STATE.STARTING) {
                  takingLongToStart(criticalComponent);
               } else {
```

### UnnecessaryQualifierForThis
Qualifier `JournalStorageManager` on 'this' is unnecessary in this context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/JournalStorageManager.java`
#### Snippet
```java
      try (ArtemisCloseable lock = closeableReadLock()) {
         if (isReplicated()) {
            replicator.largeMessageClosed(largeServerMessage.toMessage().getMessageID(), JournalStorageManager.this);
         }
      }
```

### UnnecessaryQualifierForThis
Qualifier `JournalStorageManager` on 'this' is unnecessary in this context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/JournalStorageManager.java`
#### Snippet
```java
            }
            if (replicator != null) {
               replicator.largeMessageDelete(messageId, JournalStorageManager.this);
            }
         });
```

### UnnecessaryQualifierForThis
Qualifier `QueueImpl` on 'this' is unnecessary in this context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java

      try (ArtemisCloseable metric = measureCritical(CRITICAL_PATH_ADD_HEAD)) {
         synchronized (QueueImpl.this) {
            if (ringSize != -1) {
               enforceRing(ref, false, true);
```

### UnnecessaryQualifierForThis
Qualifier `QueueImpl` on 'this' is unnecessary in this context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java
         Consumer handledconsumer = null;

         synchronized (QueueImpl.this) {

            if (queueDestroyed) {
```

### UnnecessaryQualifierForThis
Qualifier `QueueImpl` on 'this' is unnecessary in this context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java

      try (ArtemisCloseable metric = measureCritical(CRITICAL_CONSUMER)) {
         synchronized (QueueImpl.this) {

            boolean consumerRemoved = false;
```

## RuleId[id=SizeReplaceableByIsEmpty]
### SizeReplaceableByIsEmpty
`getUsername().length() > 0` can be replaced with '!getUsername().isEmpty()'
in `artemis-cdi-client/src/main/java/org/apache/artemis/client/cdi/configuration/DefaultArtemisClientConfigurationImpl.java`
#### Snippet
```java
   @Override
   public boolean hasAuthentication() {
      return getUsername() != null && getUsername().length() > 0;
   }
}
```

### SizeReplaceableByIsEmpty
`config.length() == 0` can be replaced with 'config.isEmpty()'
in `artemis-service-extensions/src/main/java/org/apache/activemq/artemis/service/extensions/xa/recovery/ActiveMQXAResourceRecovery.java`
#### Snippet
```java

      public ConfigParser(final String config) {
         if (config == null || config.length() == 0) {
            throw new IllegalArgumentException("Must specify provider connector factory class name in config");
         }
```

### SizeReplaceableByIsEmpty
`username.length() == 0` can be replaced with 'username.isEmpty()'
in `artemis-service-extensions/src/main/java/org/apache/activemq/artemis/service/extensions/xa/recovery/ActiveMQXAResourceRecovery.java`
#### Snippet
```java

            username = strings[1].trim();
            if (username.length() == 0) {
               username = null;
            }
```

### SizeReplaceableByIsEmpty
`password.length() == 0` can be replaced with 'password.isEmpty()'
in `artemis-service-extensions/src/main/java/org/apache/activemq/artemis/service/extensions/xa/recovery/ActiveMQXAResourceRecovery.java`
#### Snippet
```java

            password = strings[2].trim();
            if (password.length() == 0) {
               password = null;
            }
```

### SizeReplaceableByIsEmpty
`commaSeparatedString.trim().length() == 0` can be replaced with 'commaSeparatedString.trim().isEmpty()'
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/ListUtil.java`
#### Snippet
```java
   public static List<String> toList(final String commaSeparatedString) {
      List<String> list = new ArrayList<>();
      if (commaSeparatedString == null || commaSeparatedString.trim().length() == 0) {
         return list;
      }
```

### SizeReplaceableByIsEmpty
`parts[i].length() == 0` can be replaced with 'parts\[i\].isEmpty()'
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/InetAddresses.java`
#### Snippet
```java
      int skipIndex = -1;
      for (int i = 1; i < parts.length - 1; i++) {
         if (parts[i].length() == 0) {
            if (skipIndex >= 0) {
               return null;  // Can't have more than one ::
```

### SizeReplaceableByIsEmpty
`parts[0].length() == 0` can be replaced with 'parts\[0\].isEmpty()'
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/InetAddresses.java`
#### Snippet
```java
         partsHi = skipIndex;
         partsLo = parts.length - skipIndex - 1;
         if (parts[0].length() == 0 && --partsHi != 0) {
            return null;  // ^: requires ^::
         }
```

### SizeReplaceableByIsEmpty
`parts[parts.length - 1].length() == 0` can be replaced with 'parts\[parts.length - 1\].isEmpty()'
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/InetAddresses.java`
#### Snippet
```java
            return null;  // ^: requires ^::
         }
         if (parts[parts.length - 1].length() == 0 && --partsLo != 0) {
            return null;  // :$ requires ::$
         }
```

### SizeReplaceableByIsEmpty
`ifaces.size() == 0` can be replaced with 'ifaces.isEmpty()'
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/UUIDGenerator.java`
#### Snippet
```java
         List<NetworkInterface> ifaces = getAllNetworkInterfaces();

         if (ifaces.size() == 0) {
            return null;
         }
```

### SizeReplaceableByIsEmpty
`key.trim().length() > 0` can be replaced with '!key.trim().isEmpty()'
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/DefaultSensitiveStringCodec.java`
#### Snippet
```java
         } else {
            key = System.getProperty(KEY_SYSTEM_PROPERTY);
            if (key != null && key.trim().length() > 0) {
               logger.trace("Set key from system property {}", KEY_SYSTEM_PROPERTY);
               updateKey(key);
```

### SizeReplaceableByIsEmpty
`factoryQuery.length() > 0` can be replaced with '!factoryQuery.isEmpty()'
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/uri/URIFactory.java`
#### Snippet
```java
         String factoryQuery = split.length > 1 ? split[1] : "";
         StringBuilder builder = new StringBuilder(connectorURIS[0]);
         if (factoryQuery != null && factoryQuery.length() > 0) {
            if (connectorURIS[0].contains("?")) {
               builder.append("&").append(factoryQuery.substring(1));
```

### SizeReplaceableByIsEmpty
`((Map)o).size() == 0` can be replaced with '((Map)o).isEmpty()'
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/NoOpMap.java`
#### Snippet
```java
   @Override
   public boolean equals(Object o) {
      return (o instanceof Map) && ((Map)o).size() == 0;
   }

```

### SizeReplaceableByIsEmpty
`query.length() > 0` can be replaced with '!query.isEmpty()'
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/uri/URISupport.java`
#### Snippet
```java
         schemeSpecificPart = schemeSpecificPart.substring(0, questionMark);
      }
      if (query != null && query.length() > 0) {
         schemeSpecificPart += "?" + query;
      }
```

### SizeReplaceableByIsEmpty
`s.length() == 0` can be replaced with 's.isEmpty()'
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/uri/URISupport.java`
#### Snippet
```java
   public static URI createRemainingURI(URI originalURI, Map<String, String> params) throws URISyntaxException {
      String s = createQueryString(params);
      if (s.length() == 0) {
         s = null;
      }
```

### SizeReplaceableByIsEmpty
`s.length() != 0` can be replaced with '!s.isEmpty()'
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/uri/URISupport.java`
#### Snippet
```java

      String s = str.substring(last);
      if (s.length() != 0) {
         l.add(s);
      }
```

### SizeReplaceableByIsEmpty
`host.length() != 0` can be replaced with '!host.isEmpty()'
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/uri/URISupport.java`
#### Snippet
```java
         }

         if (host != null && host.length() != 0) {
            sb.append(host);
         } else {
```

### SizeReplaceableByIsEmpty
`options.size() > 0` can be replaced with '!options.isEmpty()'
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/uri/URISupport.java`
#### Snippet
```java
   public static String createQueryString(Map<String, ? extends Object> options) throws URISyntaxException {
      try {
         if (options.size() > 0) {
            StringBuilder rc = new StringBuilder();
            boolean first = true;
```

### SizeReplaceableByIsEmpty
`params.length() > 0` can be replaced with '!params.isEmpty()'
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/uri/URISupport.java`
#### Snippet
```java
         rc.parameters = parseQuery(params.substring(p + 1));
      } else {
         if (params.length() > 0) {
            rc.path = stripPrefix(params, "/");
         }
```

### SizeReplaceableByIsEmpty
`destinationType.length() == 0` can be replaced with 'destinationType.isEmpty()'
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/inflow/ActiveMQActivationValidationUtils.java`
#### Snippet
```java
         }

         if ((destinationType == null || destinationType.length() == 0 || Topic.class.getName().equals(destinationType))
               && subscriptionDurability && (subscriptionName == null || subscriptionName.length() == 0)) {
            propsNotSet.add(new PropertyDescriptor("subscriptionName", ActiveMQActivationSpec.class));
```

### SizeReplaceableByIsEmpty
`subscriptionName.length() == 0` can be replaced with 'subscriptionName.isEmpty()'
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/inflow/ActiveMQActivationValidationUtils.java`
#### Snippet
```java

         if ((destinationType == null || destinationType.length() == 0 || Topic.class.getName().equals(destinationType))
               && subscriptionDurability && (subscriptionName == null || subscriptionName.length() == 0)) {
            propsNotSet.add(new PropertyDescriptor("subscriptionName", ActiveMQActivationSpec.class));
            errorMessages.add("If subscription is durable then subscription name must be specified.");
```

### SizeReplaceableByIsEmpty
`propsNotSet.size() > 0` can be replaced with '!propsNotSet.isEmpty()'
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/inflow/ActiveMQActivationValidationUtils.java`
#### Snippet
```java
   private static void buildAndThrowExceptionIfNeeded(List<PropertyDescriptor> propsNotSet, List<String> errorMessages)
         throws InvalidPropertyException {
      if (propsNotSet.size() > 0) {
         StringBuffer b = new StringBuffer();
         b.append("Invalid settings:");
```

### SizeReplaceableByIsEmpty
`records.size() == 0` can be replaced with 'records.isEmpty()'
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/AbstractJournalUpdateTask.java`
#### Snippet
```java
         }, wholeFileBufferRef, false, null);

         if (records.size() == 0) {
            // the record is damaged
            controlFile.delete();
```

### SizeReplaceableByIsEmpty
`checkTasks.size() == 0` can be replaced with 'checkTasks.isEmpty()'
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/check/NodeCheck.java`
#### Snippet
```java
      }

      if (up || checkTasks.size() == 0) {
         checkTasks.add(0, new CheckTask("the node is started", this::checkNodeUp));
      }
```

### SizeReplaceableByIsEmpty
`checkTasks.size() == 0` can be replaced with 'checkTasks.isEmpty()'
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/check/QueueCheck.java`
#### Snippet
```java
      }

      if (up || checkTasks.size() == 0) {
         checkTasks.add(0, new CheckTask(String.format("the queue %s exists and is not paused",
                                                       getName()), this::checkQueueUp));
```

### SizeReplaceableByIsEmpty
`fieldName.trim().length() > 0` can be replaced with '!fieldName.trim().isEmpty()'
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/queue/StatQueue.java`
#### Snippet
```java
      HashMap<String, Object> filterMap = new HashMap<>();

      if (((fieldName != null) && (fieldName.trim().length() > 0)) && ((queueName != null && queueName.trim().length() > 0))) {
         getActionContext().err.println("'--field' and '--queueName' cannot be specified together.");
         return null;
```

### SizeReplaceableByIsEmpty
`queueName.trim().length() > 0` can be replaced with '!queueName.trim().isEmpty()'
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/queue/StatQueue.java`
#### Snippet
```java
      HashMap<String, Object> filterMap = new HashMap<>();

      if (((fieldName != null) && (fieldName.trim().length() > 0)) && ((queueName != null && queueName.trim().length() > 0))) {
         getActionContext().err.println("'--field' and '--queueName' cannot be specified together.");
         return null;
```

### SizeReplaceableByIsEmpty
`fieldName.trim().length() > 0` can be replaced with '!fieldName.trim().isEmpty()'
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/queue/StatQueue.java`
#### Snippet
```java
      }

      if ((fieldName != null) && (fieldName.trim().length() > 0)) {
         try {
            FIELD field = FIELD.valueOfJsonId(fieldName);
```

### SizeReplaceableByIsEmpty
`value.trim().length() == 0` can be replaced with 'value.trim().isEmpty()'
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/queue/StatQueue.java`
#### Snippet
```java

         //full filter being set ensure value is set
         if (value == null || value.trim().length() == 0) {
            getActionContext().err.println("'--value' needs to be set when '--field' is specified");
            return null;
```

### SizeReplaceableByIsEmpty
`queueName.trim().length() > 0` can be replaced with '!queueName.trim().isEmpty()'
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/queue/StatQueue.java`
#### Snippet
```java
         }

      } else if (queueName != null && queueName.trim().length() > 0) {
         filterMap.put("field", FIELD.NAME.toString());
         filterMap.put("value", queueName);
```

### SizeReplaceableByIsEmpty
`journal.orderFiles().size() != 0` can be replaced with '!journal.orderFiles().isEmpty()'
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/journal/DecodeJournal.java`
#### Snippet
```java
      JournalImpl journal = new JournalImpl(fileSize, minFiles, minFiles, 0, 0, nio, journalPrefix, journalSuffix, 1);

      if (journal.orderFiles().size() != 0) {
         throw new IllegalStateException("Import needs to create a brand new journal");
      }
```

### SizeReplaceableByIsEmpty
`queueNames.size() > 0` can be replaced with '!queueNames.isEmpty()'
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/xml/XmlDataExporter.java`
#### Snippet
```java
                        }

                        if (queueNames.size() > 0 && (message.getTransactionID() == -1 || pgTXs.contains(message.getTransactionID()))) {
                           printSingleMessageAsXML(message.getMessage().toCore(), queueNames);
                        }
```

### SizeReplaceableByIsEmpty
`referenceDescribeHashMap.size() == 0` can be replaced with 'referenceDescribeHashMap.isEmpty()'
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/xml/XmlDataExporter.java`
#### Snippet
```java
         if (referenceDescribeHashMap != null) {
            referenceDescribeHashMap.remove(ack.refEncoding.queueID);
            if (referenceDescribeHashMap.size() == 0) {
               messages.remove(info.id);
               messageRefs.remove(info.id);
```

### SizeReplaceableByIsEmpty
`javaOptions.length() == 0` can be replaced with 'javaOptions.isEmpty()'
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Create.java`
#### Snippet
```java
      File oomeDumpFile = new File(logFolder, "oom_dump.hprof");

      if (javaOptions == null || javaOptions.length() == 0) {
         javaOptions = "";
      }
```

### SizeReplaceableByIsEmpty
`currentBindings.getBindings().size() == 0` can be replaced with 'currentBindings.getBindings().isEmpty()'
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/persistence/impl/journal/JMSJournalStorageManagerImpl.java`
#### Snippet
```java
      currentBindings.deleteBinding(address);

      if (currentBindings.getBindings().size() == 0) {
         mapBindings.remove(key);
      } else {
```

### SizeReplaceableByIsEmpty
`messages.size() > 0` can be replaced with '!messages.isEmpty()'
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/bridge/impl/JMSBridgeImpl.java`
#### Snippet
```java
            }

            if (messages.size() > 0) {
               // Clear outstanding messages so they don't get retransmitted and duplicated on the other side of the bridge
               logger.trace("Clearing up messages before stopping...");
```

### SizeReplaceableByIsEmpty
`members.size() > 0` can be replaced with '!members.isEmpty()'
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenWireProtocolManager.java`
#### Snippet
```java

      synchronized (members) {
         if (members.size() > 0) {
            for (TopologyMember member : members) {
               connectedBrokers.append(separator).append(member.toURI());
```

### SizeReplaceableByIsEmpty
`brokerPath.length() > 0` can be replaced with '!brokerPath.isEmpty()'
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenWireMessageConverter.java`
#### Snippet
```java

      final SimpleString brokerPath = getObjectProperty(coreMessage, SimpleString.class, OpenWireConstants.AMQ_MSG_BROKER_PATH);
      if (brokerPath != null && brokerPath.length() > 0) {
         setAMQMsgBrokerPath(amqMsg, brokerPath.toString());
      }
```

### SizeReplaceableByIsEmpty
`clusterPath.length() > 0` can be replaced with '!clusterPath.isEmpty()'
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenWireMessageConverter.java`
#### Snippet
```java

      final SimpleString clusterPath = getObjectProperty(coreMessage, SimpleString.class, OpenWireConstants.AMQ_MSG_CLUSTER);
      if (clusterPath != null && clusterPath.length() > 0) {
         setAMQMsgClusterPath(amqMsg, clusterPath.toString());
      }
```

### SizeReplaceableByIsEmpty
`((SimpleString) producerIdValue).length() > 0` can be replaced with '!((SimpleString) producerIdValue).isEmpty()'
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenWireMessageConverter.java`
#### Snippet
```java

      final Object producerIdValue = getObjectProperty(coreMessage, Object.class, OpenWireConstants.AMQ_MSG_PRODUCER_ID);
      if (producerIdValue instanceof SimpleString && ((SimpleString) producerIdValue).length() > 0) {
         amqMsg.setProducerId(new ProducerId(producerIdValue.toString()));
      } else if (producerIdValue instanceof byte[]) {
```

### SizeReplaceableByIsEmpty
`userId.length() > 0` can be replaced with '!userId.isEmpty()'
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenWireMessageConverter.java`
#### Snippet
```java

      final SimpleString userId = getObjectProperty(coreMessage, SimpleString.class, OpenWireConstants.AMQ_MSG_USER_ID);
      if (userId != null && userId.length() > 0) {
         amqMsg.setUserID(userId.toString());
      }
```

### SizeReplaceableByIsEmpty
`keyStr.length() == 0` can be replaced with 'keyStr.isEmpty()'
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenWireMessageConverter.java`
#### Snippet
```java
      for (SimpleString s : props) {
         final String keyStr = s.toString();
         if (keyStr.length() == 0) {
            logger.debug("ignoring property with empty key name");
            continue;
```

### SizeReplaceableByIsEmpty
`topic.length() == 0` can be replaced with 'topic.isEmpty()'
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTPublishManager.java`
#### Snippet
```java
                  if (topic == null) {
                     topic = message.variableHeader().topicName();
                     if (topic == null || topic.length() == 0) {
                        // using a topic alias with no matching topic in the state; potentially [MQTT-3.3.2-7]
                        throw new DisconnectException(MQTTReasonCodes.TOPIC_ALIAS_INVALID);
```

### SizeReplaceableByIsEmpty
`properties.size() > 0` can be replaced with '!properties.isEmpty()'
in `artemis-junit/artemis-junit-commons/src/main/java/org/apache/activemq/artemis/junit/AbstractActiveMQClientDelegate.java`
#### Snippet
```java
    */
   public static void addMessageProperties(ClientMessage message, Map<String, Object> properties) {
      if (properties != null && properties.size() > 0) {
         for (Map.Entry<String, Object> property : properties.entrySet()) {
            message.putObjectProperty(property.getKey(), property.getValue());
```

### SizeReplaceableByIsEmpty
`publishPayload.length() == 0` can be replaced with 'publishPayload.isEmpty()'
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTUtil.java`
#### Snippet
```java
      }
      String publishPayload = message.payload().toString(StandardCharsets.UTF_8);
      if (publishPayload.length() == 0) {
         return "<empty>";
      }
```

### SizeReplaceableByIsEmpty
`userProperties.size() != 0` can be replaced with '!userProperties.isEmpty()'
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTUtil.java`
#### Snippet
```java
       */
      List<MqttProperties.StringPair> userProperties = getProperty(List.class, properties, USER_PROPERTY);
      if (userProperties != null && userProperties.size() != 0) {
         message.putIntProperty(MQTT_USER_PROPERTY_EXISTS_KEY, userProperties.size());
         for (int i = 0; i < userProperties.size(); i++) {
```

### SizeReplaceableByIsEmpty
`topicName.length() == 0` can be replaced with 'topicName.isEmpty()'
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTUtil.java`
#### Snippet
```java
                  MqttPublishVariableHeader publishHeader = (MqttPublishVariableHeader) message.variableHeader();
                  String topicName = publishHeader.topicName();
                  if (topicName == null || topicName.length() == 0) {
                     topicName = "<empty>";
                  }
```

### SizeReplaceableByIsEmpty
`((ArrayList)value).size() > 0` can be replaced with '!((ArrayList)value).isEmpty()'
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTUtil.java`
#### Snippet
```java
                        if (value instanceof byte[]) {
                           value = new String((byte[]) value, StandardCharsets.UTF_8);
                        } else if (value instanceof ArrayList && ((ArrayList)value).size() > 0 && ((ArrayList)value).get(0) instanceof MqttProperties.StringPair) {
                           StringBuilder userProperties = new StringBuilder();
                           userProperties.append("[");
```

### SizeReplaceableByIsEmpty
`consumersList.size() == 0` can be replaced with 'consumersList.isEmpty()'
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenWireConnection.java`
#### Snippet
```java
         info.setLastDeliveredSequenceId(RemoveInfo.LAST_DELIVERED_UNKNOWN);

         if (consumersList.size() == 0) {
            return;
         }
```

### SizeReplaceableByIsEmpty
`registryBindings.size() == 0` can be replaced with 'registryBindings.isEmpty()'
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/server/impl/JMSServerManagerImpl.java`
#### Snippet
```java
      checkInitialised();
      List<String> registryBindings = bindingsMap.remove(name);
      if (registryBindings == null || registryBindings.size() == 0) {
         return false;
      } else {
```

### SizeReplaceableByIsEmpty
`bindings.size() > 0` can be replaced with '!bindings.isEmpty()'
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/server/impl/JMSServerManagerImpl.java`
#### Snippet
```java
   public void recoverregistryBindings(String name, PersistedType type) throws NamingException {
      List<String> bindings = unRecoveredBindings.get(name);
      if ((bindings != null) && (bindings.size() > 0)) {
         Map<String, List<String>> mapBindings;
         Map<String, ?> objects;
```

### SizeReplaceableByIsEmpty
`cfConfig.getConnectorNames().size() == 0` can be replaced with 'cfConfig.getConnectorNames().isEmpty()'
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/server/impl/JMSServerManagerImpl.java`
#### Snippet
```java
         }
      } else {
         if (cfConfig.getConnectorNames() == null || cfConfig.getConnectorNames().size() == 0) {
            throw ActiveMQJMSServerBundle.BUNDLE.noConnectorNameOnCF();
         }
```

### SizeReplaceableByIsEmpty
`registryBindings.size() == 0` can be replaced with 'registryBindings.isEmpty()'
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/server/impl/JMSServerManagerImpl.java`
#### Snippet
```java
      checkInitialised();
      List<String> registryBindings = bindingsMap.get(name);
      if (registryBindings == null || registryBindings.size() == 0) {
         return false;
      }
```

### SizeReplaceableByIsEmpty
`leftFiles.size() > 0` can be replaced with '!leftFiles.isEmpty()'
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalImpl.java`
#### Snippet
```java
      List<String> leftFiles = fileFactory.listFiles(getFileExtension() + extension);

      if (leftFiles.size() > 0) {
         ActiveMQJournalLogger.LOGGER.tempFilesLeftOpen();

```

### SizeReplaceableByIsEmpty
`dataFilesToProcess.size() == 0` can be replaced with 'dataFilesToProcess.isEmpty()'
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalImpl.java`
#### Snippet
```java
         filesRepository.clearDataFiles();

         if (dataFilesToProcess.size() == 0) {
            logger.trace("Finishing compacting, nothing to process");
            return null;
```

### SizeReplaceableByIsEmpty
`url.length() > 0` can be replaced with '!url.isEmpty()'
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQConnectionFactory.java`
#### Snippet
```java
      }
      try {
         if (url != null && url.length() > 0) {
            url = updateBrokerURL(url, props);
            setBrokerURL(url);
```

### SizeReplaceableByIsEmpty
`s.length() == 0` can be replaced with 's.isEmpty()'
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQMessage.java`
#### Snippet
```java

   private boolean isValidJavaIdentifier(final String s) {
      if (s == null || s.length() == 0) {
         return false;
      }
```

### SizeReplaceableByIsEmpty
`username.length() > 0` can be replaced with '!username.isEmpty()'
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/sasl/scram/SCRAMClientSASL.java`
#### Snippet
```java

   public static boolean isApplicable(String username, String password) {
      return username != null && username.length() > 0 && password != null && password.length() > 0;
   }

```

### SizeReplaceableByIsEmpty
`password.length() > 0` can be replaced with '!password.isEmpty()'
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/sasl/scram/SCRAMClientSASL.java`
#### Snippet
```java

   public static boolean isApplicable(String username, String password) {
      return username != null && username.length() > 0 && password != null && password.length() > 0;
   }

```

### SizeReplaceableByIsEmpty
`queueNames.size() > 0` can be replaced with '!queueNames.isEmpty()'
in `artemis-junit/artemis-junit-commons/src/main/java/org/apache/activemq/artemis/junit/EmbeddedJMSDelegate.java`
#### Snippet
```java
         if (bindingQueryResult.isExists()) {
            List<SimpleString> queueNames = bindingQueryResult.getQueueNames();
            if (queueNames.size() > 0) {
               queue = jmsServer.getActiveMQServer().locateQueue(queueNames.get(0));
            }
```

### SizeReplaceableByIsEmpty
`properties.size() > 0` can be replaced with '!properties.isEmpty()'
in `artemis-junit/artemis-junit-commons/src/main/java/org/apache/activemq/artemis/junit/EmbeddedJMSDelegate.java`
#### Snippet
```java

   public static void setMessageProperties(Message message, Map<String, Object> properties) {
      if (properties != null && properties.size() > 0) {
         for (Map.Entry<String, Object> property : properties.entrySet()) {
            try {
```

### SizeReplaceableByIsEmpty
`name.length() == 0` can be replaced with 'name.isEmpty()'
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jndi/ReadOnlyContext.java`
#### Snippet
```java
   @Override
   public Object lookup(String name) throws NamingException {
      if (name.length() == 0) {
         return this;
      }
```

### SizeReplaceableByIsEmpty
`path.size() == 0` can be replaced with 'path.isEmpty()'
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jndi/ReadOnlyContext.java`
#### Snippet
```java
            CompositeName path = new CompositeName(name);

            if (path.size() == 0) {
               return this;
            } else {
```

### SizeReplaceableByIsEmpty
`prefix.length() > 0` can be replaced with '!prefix.isEmpty()'
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jndi/ReadOnlyContext.java`
#### Snippet
```java
      if (result instanceof ReadOnlyContext) {
         String prefix = getNameInNamespace();
         if (prefix.length() > 0) {
            prefix = prefix + SEPARATOR;
         }
```

### SizeReplaceableByIsEmpty
`name.length() > 0` can be replaced with '!name.isEmpty()'
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jndi/ReadOnlyContext.java`
#### Snippet
```java
    */
   protected Map<String, Object> internalBind(String name, Object value) throws NamingException {
      assert name != null && name.length() > 0;
      assert !frozen;

```

### SizeReplaceableByIsEmpty
`amqpServicesConfiguration.size() > 0` can be replaced with '!amqpServicesConfiguration.isEmpty()'
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/ProtonProtocolManagerFactory.java`
#### Snippet
```java
   public void loadProtocolServices(ActiveMQServer server, List<ActiveMQComponent> services) {
      List<AMQPBrokerConnectConfiguration> amqpServicesConfiguration = server.getConfiguration().getAMQPConnection();
      if (amqpServicesConfiguration != null && amqpServicesConfiguration.size() > 0) {
         AMQPBrokerConnectionManager bridgeService = new AMQPBrokerConnectionManager(this, amqpServicesConfiguration, server);
         services.add(bridgeService);
```

### SizeReplaceableByIsEmpty
`partitions.size() > 0` can be replaced with '!partitions.isEmpty()'
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/connect/mirror/AMQPMirrorControllerAggregation.java`
#### Snippet
```java
   @Override
   public String getRemoteMirrorId() {
      if (partitions != null && partitions.size() > 0) {
         return partitions.get(0).getRemoteMirrorId();
      } else {
```

### SizeReplaceableByIsEmpty
`username.length() > 0` can be replaced with '!username.isEmpty()'
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/connect/AMQPBrokerConnection.java`
#### Snippet
```java

      public static boolean isApplicable(final String username, final String password) {
         return username != null && username.length() > 0 && password != null && password.length() > 0;
      }
   }
```

### SizeReplaceableByIsEmpty
`password.length() > 0` can be replaced with '!password.isEmpty()'
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/connect/AMQPBrokerConnection.java`
#### Snippet
```java

      public static boolean isApplicable(final String username, final String password) {
         return username != null && username.length() > 0 && password != null && password.length() > 0;
      }
   }
```

### SizeReplaceableByIsEmpty
`binding.apps.size() > 0` can be replaced with '!binding.apps.isEmpty()'
in `artemis-web/src/main/java/org/apache/activemq/artemis/component/WebServerComponent.java`
#### Snippet
```java
         virtualHosts[i] = "@Connector-" + i;

         if (binding.apps != null && binding.apps.size() > 0) {
            for (AppDTO app : binding.apps) {
               Path dirToUse = homeWarDir;
```

### SizeReplaceableByIsEmpty
`webServerConfig.requestLog.ignorePaths.length() > 0` can be replaced with '!webServerConfig.requestLog.ignorePaths.isEmpty()'
in `artemis-web/src/main/java/org/apache/activemq/artemis/component/WebServerComponent.java`
#### Snippet
```java
      }

      if (webServerConfig.requestLog.ignorePaths != null && webServerConfig.requestLog.ignorePaths.length() > 0) {
         String[] split = webServerConfig.requestLog.ignorePaths.split(",");
         String[] ignorePaths = new String[split.length];
```

### SizeReplaceableByIsEmpty
`webContextData.size() == 0` can be replaced with 'webContextData.isEmpty()'
in `artemis-web/src/main/java/org/apache/activemq/artemis/component/WebServerComponent.java`
#### Snippet
```java

   private void cleanupTmp() {
      if (webContextData.size() == 0) {
         //there is no webapp to be deployed (as in some tests)
         return;
```

### SizeReplaceableByIsEmpty
`temporaryFiles.size() > 0` can be replaced with '!temporaryFiles.isEmpty()'
in `artemis-web/src/main/java/org/apache/activemq/artemis/component/WebServerComponent.java`
#### Snippet
```java
         Files.newDirectoryStream(temporaryWarDir).forEach(path -> temporaryFiles.add(path.toFile()));

         if (temporaryFiles.size() > 0) {
            WebTmpCleaner.cleanupTmpFiles(getLibFolder(), temporaryFiles, true);
         }
```

### SizeReplaceableByIsEmpty
`value.length() == 0` can be replaced with 'value.isEmpty()'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/JsonUtil.java`
#### Snippet
```java

      NullableJsonString(String value) {
         if (value == null || value.length() == 0) {
            this.value = null;
         } else {
```

### SizeReplaceableByIsEmpty
`properties.size() > 0` can be replaced with '!properties.isEmpty()'
in `artemis-junit/artemis-junit-commons/src/main/java/org/apache/activemq/artemis/junit/EmbeddedActiveMQDelegate.java`
#### Snippet
```java
    */
   public static void addMessageProperties(ClientMessage message, Map<String, Object> properties) {
      if (properties != null && properties.size() > 0) {
         for (Map.Entry<String, Object> property : properties.entrySet()) {
            message.putObjectProperty(property.getKey(), property.getValue());
```

### SizeReplaceableByIsEmpty
`copy.size() > 0` can be replaced with '!copy.isEmpty()'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/Topology.java`
#### Snippet
```java
      }

      if (copy.size() > 0) {
         executor.execute(new Runnable() {
            @Override
```

### SizeReplaceableByIsEmpty
`membersCopy.size() == 0` can be replaced with 'membersCopy.isEmpty()'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ServerLocatorImpl.java`
#### Snippet
```java
         Collection<TopologyMemberImpl> membersCopy = topology.getMembers();

         if (membersCopy.size() == 0) {
            //it could happen when live is down, in that case we keeps the old copy
            //and don't update
```

### SizeReplaceableByIsEmpty
`sessions.size() == 0` can be replaced with 'sessions.isEmpty()'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ClientSessionFactoryImpl.java`
#### Snippet
```java
      Connector connectorInUse = connector;

      if (connectionInUse != null && sessions.size() == 0) {
         cancelScheduledTasks();

```

### SizeReplaceableByIsEmpty
`s.length() != 0` can be replaced with '!s.isEmpty()'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/utils/XMLUtil.java`
#### Snippet
```java

         String s = sb.toString();
         if (s.length() != 0) {
            return s;
         }
```

### SizeReplaceableByIsEmpty
`commaSeparatedString.trim().length() == 0` can be replaced with 'commaSeparatedString.trim().isEmpty()'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/utils/SecurityFormatter.java`
#### Snippet
```java
   private static List<String> toList(final String commaSeparatedString) {
      List<String> list = new ArrayList<>();
      if (commaSeparatedString == null || commaSeparatedString.trim().length() == 0) {
         return list;
      }
```

### SizeReplaceableByIsEmpty
`whiteList.size() == 0` can be replaced with 'whiteList.isEmpty()'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/utils/ObjectInputStreamWithClassLoader.java`
#### Snippet
```java
      // Failing outright rejection or allow from above
      // reject only if the whiteList is not empty.
      return whiteList.size() == 0;
   }

```

### SizeReplaceableByIsEmpty
`user.length() != 0` can be replaced with '!user.isEmpty()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/security/ActiveMQBasicSecurityManager.java`
#### Snippet
```java
   public synchronized Map<String, Set<String>> listUser(String user) {
      // a null or empty user is actually valid here
      if (user != null && user.length() != 0 && !userExists(user)) {
         throw ActiveMQMessageBundle.BUNDLE.userDoesNotExist(user);
      }
```

### SizeReplaceableByIsEmpty
`user.length() > 0` can be replaced with '!user.isEmpty()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/security/ActiveMQBasicSecurityManager.java`
#### Snippet
```java
      Map<String, Set<String>> result = new HashMap<>();

      if (user != null && user.length() > 0) {
         result.put(user, new HashSet<>(getRole(user).getRoles()));
      } else {
```

### SizeReplaceableByIsEmpty
`certificateConfigurationName.length() > 0` can be replaced with '!certificateConfigurationName.isEmpty()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/security/ActiveMQJAASSecurityManager.java`
#### Snippet
```java
         if (securityDomain != null) {
            lc = new LoginContext(securityDomain, null, new JaasCallbackHandler(user, password, remotingConnection), null);
         } else if (certificateConfigurationName != null && certificateConfigurationName.length() > 0 && getCertsFromConnection(remotingConnection) != null) {
            lc = new LoginContext(certificateConfigurationName, null, new JaasCallbackHandler(user, password, remotingConnection), certificateConfiguration);
         } else {
```

### SizeReplaceableByIsEmpty
`username.length() > 0` can be replaced with '!username.isEmpty()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/security/jaas/PropertiesLoginModuleConfigurator.java`
#### Snippet
```java
      Map<String, Set<String>> result = new HashMap<>();

      if (username != null && username.length() > 0) {
         result.put(username, findRoles(username));
      } else {
```

### SizeReplaceableByIsEmpty
`update.size() > 0` can be replaced with '!update.isEmpty()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/security/jaas/PropertiesLoginModuleConfigurator.java`
#### Snippet
```java
            }
            if (found) {
               if (update.size() > 0) {
                  newList.add(StringUtil.joinStringList(update, ","));
               }
```

### SizeReplaceableByIsEmpty
`entry.getB().size() > 0` can be replaced with '!entry.getB().isEmpty()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/security/jaas/PropertiesLoginModuleConfigurator.java`
#### Snippet
```java
         Pair<String, List<String>> entry = iterUpdate.next();
         roleConfig.clearProperty(entry.getA());
         if (entry.getB().size() > 0) {
            roleConfig.addProperty(entry.getA(), entry.getB());
         }
```

### SizeReplaceableByIsEmpty
`entryName.length() == 0` can be replaced with 'entryName.isEmpty()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/security/jaas/PropertiesLoginModuleConfigurator.java`
#### Snippet
```java

   public PropertiesLoginModuleConfigurator(String entryName, String brokerEtc) throws Exception {
      if (entryName == null || entryName.length() == 0) {
         entryName = "activemq";
      }
```

### SizeReplaceableByIsEmpty
`string.length() == 0` can be replaced with 'string.isEmpty()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/security/scram/ScramUtils.java`
#### Snippet
```java
    */
   public static boolean isNullOrEmpty(String string) {
      return string == null || string.length() == 0; // string.isEmpty() in Java 6
   }

```

### SizeReplaceableByIsEmpty
`tupleStart.size() > 0` can be replaced with '!tupleStart.isEmpty()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/security/scram/StringPrep.java`
#### Snippet
```java

            // Coalesce overlapping ranges.
            if (tupleStart.size() > 0) {
               int prevIndex = tupleStart.size() - 1;
               int prevStart = tupleStart.get(prevIndex);
```

### SizeReplaceableByIsEmpty
`password.length() == 0` can be replaced with 'password.isEmpty()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/security/jaas/LDAPLoginModule.java`
#### Snippet
```java
          * react appropriately.
          */
         if (password == null || password.length() == 0) {
            throw new FailedLoginException("Password cannot be null or empty");
         }
```

### SizeReplaceableByIsEmpty
`tc.getProperties().size() > 0` can be replaced with '!tc.getProperties().isEmpty()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/BridgeConfiguration.java`
#### Snippet
```java
      if (tc != null) {
         JsonObjectBuilder tcBuilder = JsonLoader.createObjectBuilder().add(TransformerConfiguration.CLASS_NAME, tc.getClassName());
         if (tc.getProperties() != null && tc.getProperties().size() > 0) {
            JsonObjectBuilder propBuilder = JsonLoader.createObjectBuilder();
            tc.getProperties().forEach(propBuilder::add);
```

### SizeReplaceableByIsEmpty
`str.length() == 0` can be replaced with 'str.isEmpty()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/impl/Validators.java`
#### Snippet
```java
      public void validate(final String name, final Object value) {
         String str = (String) value;
         if (str == null || str.length() == 0) {
            throw ActiveMQMessageBundle.BUNDLE.emptyOrNull(name);
         }
```

### SizeReplaceableByIsEmpty
`filterStr.length() == 0` can be replaced with 'filterStr.isEmpty()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/filter/impl/FilterImpl.java`
#### Snippet
```java
    */
   public static Filter createFilter(final SimpleString filterStr) throws ActiveMQException {
      if (filterStr == null || filterStr.length() == 0) {
         return null;
      }
```

### SizeReplaceableByIsEmpty
`scanList.size() == 0` can be replaced with 'scanList.isEmpty()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/cursor/impl/PageSubscriptionImpl.java`
#### Snippet
```java
         synchronized (scanList) {
            this.pageScanNeeded = true;
            if (scanList.size() == 0) {
               return;
            }
```

### SizeReplaceableByIsEmpty
`afterCommitList.size() > 0` can be replaced with '!afterCommitList.isEmpty()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/cursor/impl/PageSubscriptionImpl.java`
#### Snippet
```java
         }

         if (afterCommitList.size() > 0) {
            try {
               tx.commit();
```

### SizeReplaceableByIsEmpty
`value.length() == 0` can be replaced with 'value.isEmpty()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/QueueConfig.java`
#### Snippet
```java

      private static boolean isEmptyOrNull(SimpleString value) {
         return (value == null || value.length() == 0);
      }

```

### SizeReplaceableByIsEmpty
`liveConfigurations.size() == 0` can be replaced with 'liveConfigurations.isEmpty()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/NamedLiveNodeLocatorForReplication.java`
#### Snippet
```java
      try {
         lock.lock();
         if (liveConfigurations.size() == 0) {
            try {
               if (timeout != -1L) {
```

### SizeReplaceableByIsEmpty
`liveConfigurations.size() == 0` can be replaced with 'liveConfigurations.isEmpty()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/NamedLiveNodeLocatorForReplication.java`
#### Snippet
```java
                  ConcurrentUtil.await(condition, timeout);
               } else {
                  while (liveConfigurations.size() == 0) {
                     condition.await(retryReplicationWait, TimeUnit.MILLISECONDS);
                     liveConfigurations.addAll(triedConfigurations);
```

### SizeReplaceableByIsEmpty
`groupingInfos.size() > 0` can be replaced with '!groupingInfos.isEmpty()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/BackupRecoveryJournalLoader.java`
#### Snippet
```java
      //remote handlers, this means that you can only use FULL backup server when using group handlers.
      //todo maybe in the future we can restart the handler on the live server as a local handler and redistribute the state
      if (groupingInfos != null && groupingInfos.size() > 0) {
         ActiveMQServerLogger.LOGGER.groupBindingsOnRecovery();
      }
```

### SizeReplaceableByIsEmpty
`nodes.size() > 0` can be replaced with '!nodes.isEmpty()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ColocatedActivation.java`
#### Snippet
```java
      public void allVotesCast(Topology voteTopology) {
         //if we have any nodes that we can request then send a request
         if (nodes.size() > 0) {
            Pair<String, Integer> decision = getDecision();
            TopologyMemberImpl member = voteTopology.getMember(decision.getA());
```

### SizeReplaceableByIsEmpty
`connectors.size() > 0` can be replaced with '!connectors.isEmpty()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/AnyLiveNodeLocatorForScaleDown.java`
#### Snippet
```java
         lock.lock();
         connectors.remove(nodeID);
         if (connectors.size() > 0) {
            condition.signal();
         }
```

### SizeReplaceableByIsEmpty
`ids.size() > 0` can be replaced with '!ids.isEmpty()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ScaleDownHandler.java`
#### Snippet
```java
            message.putBytesProperty(Message.HDR_ROUTE_TO_IDS.toString(), buffer.array());
            ids = entry.getValue().getB();
            if (ids.size() > 0) {
               buffer = ByteBuffer.allocate(ids.size() * 8);
               for (Long id : ids) {
```

### SizeReplaceableByIsEmpty
`untriedConnectors.size() > 0` can be replaced with '!untriedConnectors.isEmpty()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/AnyLiveNodeLocatorForReplication.java`
#### Snippet
```java
         untriedConnectors.putAll(triedConnectors);
         triedConnectors.clear();
         if (untriedConnectors.size() > 0) {
            condition.signal();
         }
```

### SizeReplaceableByIsEmpty
`liveConfigurations.size() == 0` can be replaced with 'liveConfigurations.isEmpty()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/NamedLiveNodeIdLocatorForReplication.java`
#### Snippet
```java
      try {
         lock.lock();
         if (liveConfigurations.size() == 0) {
            try {
               if (timeout != -1L) {
```

### SizeReplaceableByIsEmpty
`liveConfigurations.size() == 0` can be replaced with 'liveConfigurations.isEmpty()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/NamedLiveNodeIdLocatorForReplication.java`
#### Snippet
```java
                  ConcurrentUtil.await(condition, timeout);
               } else {
                  while (liveConfigurations.size() == 0) {
                     condition.await(retryReplicationWait, TimeUnit.MILLISECONDS);
                     liveConfigurations.addAll(triedConfigurations);
```

### SizeReplaceableByIsEmpty
`connectors.size() > 0` can be replaced with '!connectors.isEmpty()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/NamedLiveNodeLocatorForScaleDown.java`
#### Snippet
```java
         lock.lock();
         connectors.remove(nodeID);
         if (connectors.size() > 0) {
            condition.signal();
         }
```

### SizeReplaceableByIsEmpty
`value.length() == 0` can be replaced with 'value.isEmpty()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueFactoryImpl.java`
#### Snippet
```java

   private static boolean isEmptyOrNull(SimpleString value) {
      return (value == null || value.length() == 0);
   }
}
```

### SizeReplaceableByIsEmpty
`expectedBindings.size() == 0` can be replaced with 'expectedBindings.isEmpty()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/group/impl/LocalGroupingHandler.java`
#### Snippet
```java
               }

               if (expectedBindings.size() == 0) {
                  awaitCondition.signal();
               }
```

### SizeReplaceableByIsEmpty
`groupMap.size() > 0` can be replaced with '!groupMap.isEmpty()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/group/impl/LocalGroupingHandler.java`
#### Snippet
```java
      lock.lock();
      try {
         if (groupMap.size() > 0) {
            waitingForBindings = true;

```

### SizeReplaceableByIsEmpty
`expectedBindings.size() > 0` can be replaced with '!expectedBindings.isEmpty()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/group/impl/LocalGroupingHandler.java`
#### Snippet
```java
            expectedBindings.removeAll(bindingsAlreadyAdded);

            if (expectedBindings.size() > 0) {
               logger.debug("Waiting remote group bindings to arrive before starting the server. timeout={} milliseconds", timeout);
               //now we wait here for the rest to be received in onNotification, it will signal once all have been received.
```

### SizeReplaceableByIsEmpty
`queueRecords.values().size() != 0` can be replaced with '!queueRecords.values().isEmpty()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/PostOfficeJournalLoader.java`
#### Snippet
```java

         if (queue == null) {
            if (queueRecords.values().size() != 0) {
               ActiveMQServerLogger.LOGGER.journalCannotFindQueueForMessage(queueID);
            }
```

### SizeReplaceableByIsEmpty
`clusterName.length() > 0` can be replaced with '!clusterName.isEmpty()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/ha/ReplicatedPolicy.java`
#### Snippet
```java
         replicaPolicy.setretryReplicationWait(retryReplicationWait);
         replicaPolicy.setMaxSavedReplicatedJournalsSize(maxSavedReplicatedJournalsSize);
         if (clusterName != null && clusterName.length() > 0) {
            replicaPolicy.setClusterName(clusterName);
         }
```

### SizeReplaceableByIsEmpty
`groupName.length() > 0` can be replaced with '!groupName.isEmpty()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/ha/ReplicatedPolicy.java`
#### Snippet
```java
            replicaPolicy.setClusterName(clusterName);
         }
         if (groupName != null && groupName.length() > 0) {
            replicaPolicy.setGroupName(groupName);
         }
```

### SizeReplaceableByIsEmpty
`includes.size() > 0` can be replaced with '!includes.isEmpty()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/impl/ClusterConnectionBridge.java`
#### Snippet
```java
      // Build the address matching part of the selector
      StringBuilder builder = new StringBuilder("(");
      if (includes.size() > 0) {
         if (excludes.size() > 0)
            builder.append("(");
```

### SizeReplaceableByIsEmpty
`excludes.size() > 0` can be replaced with '!excludes.isEmpty()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/impl/ClusterConnectionBridge.java`
#### Snippet
```java
      StringBuilder builder = new StringBuilder("(");
      if (includes.size() > 0) {
         if (excludes.size() > 0)
            builder.append("(");
         for (int i = 0; i < includes.size(); i++) {
```

### SizeReplaceableByIsEmpty
`excludes.size() > 0` can be replaced with '!excludes.isEmpty()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/impl/ClusterConnectionBridge.java`
#### Snippet
```java
               builder.append(" OR ");
         }
         if (excludes.size() > 0)
            builder.append(")");
      }
```

### SizeReplaceableByIsEmpty
`excludes.size() > 0` can be replaced with '!excludes.isEmpty()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/impl/ClusterConnectionBridge.java`
#### Snippet
```java

      // Build the address exclusion part of the selector
      if (excludes.size() > 0) {
         if (includes.size() > 0)
            builder.append(" AND (");
```

### SizeReplaceableByIsEmpty
`includes.size() > 0` can be replaced with '!includes.isEmpty()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/impl/ClusterConnectionBridge.java`
#### Snippet
```java
      // Build the address exclusion part of the selector
      if (excludes.size() > 0) {
         if (includes.size() > 0)
            builder.append(" AND (");
         for (int i = 0; i < excludes.size(); i++) {
```

### SizeReplaceableByIsEmpty
`includes.size() > 0` can be replaced with '!includes.isEmpty()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/impl/ClusterConnectionBridge.java`
#### Snippet
```java
               builder.append(" AND ");
         }
         if (includes.size() > 0)
            builder.append(")");
      }
```

### SizeReplaceableByIsEmpty
`targets.size() > 0` can be replaced with '!targets.isEmpty()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/routing/policies/RoundRobinPolicy.java`
#### Snippet
```java
   @Override
   public Target selectTarget(List<Target> targets, String key) {
      if (targets.size() > 0) {
         pos = pos % targets.size();
         return targets.get(pos++);
```

### SizeReplaceableByIsEmpty
`targets.size() > 0` can be replaced with '!targets.isEmpty()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/routing/policies/FirstElementPolicy.java`
#### Snippet
```java
   @Override
   public Target selectTarget(List<Target> targets, String key) {
      if (targets.size() > 0) {
         return targets.get(0);
      }
```

### SizeReplaceableByIsEmpty
`consistentTargets.size() > 0` can be replaced with '!consistentTargets.isEmpty()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/routing/policies/ConsistentHashPolicy.java`
#### Snippet
```java
         }

         if (consistentTargets.size() > 0) {
            Map.Entry<Integer, Target> consistentEntry = consistentTargets.floorEntry(getHash(key));

```

### SizeReplaceableByIsEmpty
`targets.size() > 0` can be replaced with '!targets.isEmpty()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/routing/policies/ConsistentHashPolicy.java`
#### Snippet
```java
            return consistentEntry.getValue();
         }
      } else if (targets.size() > 0) {
         return targets.get(0);
      }
```

### SizeReplaceableByIsEmpty
`activeMQServer.getClusterManager().getClusterConnections().size() == 0` can be replaced with 'activeMQServer.getClusterManager().getClusterConnections().isEmpty()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/embedded/EmbeddedActiveMQ.java`
#### Snippet
```java
    */
   public boolean waitClusterForming(long timeWait, TimeUnit unit, int iterations, int servers) throws Exception {
      if (activeMQServer.getClusterManager().getClusterConnections() == null || activeMQServer.getClusterManager().getClusterConnections().size() == 0) {
         return servers == 0;
      }
```

### SizeReplaceableByIsEmpty
`targets.size() > 0` can be replaced with '!targets.isEmpty()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/routing/policies/LeastConnectionsPolicy.java`
#### Snippet
```java
            return selectedTargets.get(0);
         }
      } else if (targets.size() > 0) {
         return targets.get(0);
      }
```

### SizeReplaceableByIsEmpty
`configuration.getTemporaryQueueNamespace().length() > 0` can be replaced with '!configuration.getTemporaryQueueNamespace().isEmpty()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ActiveMQServerImpl.java`
#### Snippet
```java
   public String getRuntimeTempQueueNamespace(boolean temporary) {
      StringBuilder runtimeTempQueueNamespace = new StringBuilder();
      if (temporary && configuration.getTemporaryQueueNamespace() != null && configuration.getTemporaryQueueNamespace().length() > 0) {
         runtimeTempQueueNamespace.append(configuration.getTemporaryQueueNamespace()).append(configuration.getWildcardConfiguration().getDelimiterString());
      }
```

### SizeReplaceableByIsEmpty
`queueConfiguration.getName().length() == 0` can be replaced with 'queueConfiguration.getName().isEmpty()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ActiveMQServerImpl.java`
#### Snippet
```java
      }
      synchronized (postOfficeInUse) {
         if (queueConfiguration.getName() == null || queueConfiguration.getName().length() == 0) {
            throw ActiveMQMessageBundle.BUNDLE.invalidQueueName(queueConfiguration.getName());
         }
```

### SizeReplaceableByIsEmpty
`configuration.getClusterConfigurations().size() > 0` can be replaced with '!configuration.getClusterConfigurations().isEmpty()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ActiveMQServerImpl.java`
#### Snippet
```java
      storageManager = createStorageManager();

      if (configuration.getClusterConfigurations().size() > 0 && ActiveMQDefaultConfiguration.getDefaultClusterUser().equals(configuration.getClusterUser()) && ActiveMQDefaultConfiguration.getDefaultClusterPassword().equals(configuration.getClusterPassword())) {
         ActiveMQServerLogger.LOGGER.clusterSecurityRisk();
      }
```

### SizeReplaceableByIsEmpty
`pendingNonTXPageCounter.size() != 0` can be replaced with '!pendingNonTXPageCounter.isEmpty()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ActiveMQServerImpl.java`
#### Snippet
```java
      }

      if (pendingNonTXPageCounter.size() != 0) {
         try {
            journalLoader.recoverPendingPageCounters(pendingNonTXPageCounter);
```

### SizeReplaceableByIsEmpty
`collectionPropertyName.length() > 0` can be replaced with '!collectionPropertyName.isEmpty()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/impl/ConfigurationImpl.java`
#### Snippet
```java
         StringBuilder addPropertyNameBuilder = new StringBuilder("add");
         // expect an add... without the plural for named accessors
         if (collectionPropertyName != null && collectionPropertyName.length() > 0) {
            addPropertyNameBuilder.append(Character.toUpperCase(collectionPropertyName.charAt(0)));
            addPropertyNameBuilder.append(collectionPropertyName, 1, collectionPropertyName.length() - 1);
```

### SizeReplaceableByIsEmpty
`servers.size() > 0` can be replaced with '!servers.isEmpty()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/MBeanServerFactory.java`
#### Snippet
```java
      if (this.locateExistingServerIfPossible) {
         List servers = javax.management.MBeanServerFactory.findMBeanServer(null);
         if (servers != null && servers.size() > 0) {
            this.server = (MBeanServer) servers.get(0);
         }
```

### SizeReplaceableByIsEmpty
`methods.size() == 0` can be replaced with 'methods.isEmpty()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/impl/HawtioSecurityControlImpl.java`
#### Snippet
```java
         String objectName = entry.getKey();
         List<String> methods = entry.getValue();
         if (methods.size() == 0) {
            boolean res = canInvoke(objectName);
            CompositeData data = new CompositeDataSupport(CAN_INVOKE_RESULT_ROW_TYPE,
```

### SizeReplaceableByIsEmpty
`eset1.size() == 0` can be replaced with 'eset1.isEmpty()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/postoffice/impl/SimpleAddressManager.java`
#### Snippet
```java
      Set<RoutingType> eset2 = set2 == null || set2.isEmpty() ? Collections.emptySet() : EnumSet.copyOf(set2);

      if (eset1.size() == 0 && eset2.size() == 0) {
         return true;
      }
```

### SizeReplaceableByIsEmpty
`eset2.size() == 0` can be replaced with 'eset2.isEmpty()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/postoffice/impl/SimpleAddressManager.java`
#### Snippet
```java
      Set<RoutingType> eset2 = set2 == null || set2.isEmpty() ? Collections.emptySet() : EnumSet.copyOf(set2);

      if (eset1.size() == 0 && eset2.size() == 0) {
         return true;
      }
```

### SizeReplaceableByIsEmpty
`otherAddrLastPart.length() > 0` can be replaced with '!otherAddrLastPart.isEmpty()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/postoffice/impl/AddressImpl.java`
#### Snippet
```java
            if (otherIdx == otherAddrPartsLastIdx) {
               final SimpleString otherAddrLastPart = otherAddrParts[otherAddrPartsLastIdx];
               return otherAddrLastPart.length() > 0 && otherAddrLastPart.charAt(0) == sepAnyWords;
            }
            return false;
```

### SizeReplaceableByIsEmpty
`internalRoleName.length() > 0` can be replaced with '!internalRoleName.isEmpty()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/deployers/impl/FileConfigurationParser.java`
#### Snippet
```java
            Set<String> externalRoleNames = new HashSet<>();
            externalRoleNames.add(externalRoleName);
            if ((internalRoleName.length() > 0) && (externalRoleName.length() > 0)) {
               mappedRoleNames.put(internalRoleName, externalRoleNames);
            }
```

### SizeReplaceableByIsEmpty
`externalRoleName.length() > 0` can be replaced with '!externalRoleName.isEmpty()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/deployers/impl/FileConfigurationParser.java`
#### Snippet
```java
            Set<String> externalRoleNames = new HashSet<>();
            externalRoleNames.add(externalRoleName);
            if ((internalRoleName.length() > 0) && (externalRoleName.length() > 0)) {
               mappedRoleNames.put(internalRoleName, externalRoleNames);
            }
```

### SizeReplaceableByIsEmpty
`counters.size() > 0` can be replaced with '!counters.isEmpty()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/DescribeJournal.java`
#### Snippet
```java
      }

      if (counters.size() > 0) {
         out.println("### Page Counters");
         printCounters(out, counters);
```

### SizeReplaceableByIsEmpty
`missingTX.length() > 0` can be replaced with '!missingTX.isEmpty()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/DescribeJournal.java`
#### Snippet
```java
      String missingTX = bufferFailingTransactions.toString();

      if (missingTX.length() > 0) {
         out.println();
         out.println("### Failed Transactions (Missing commit/prepare/rollback record) ###");
```

### SizeReplaceableByIsEmpty
`counters.size() != 0` can be replaced with '!counters.isEmpty()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/DescribeJournal.java`
#### Snippet
```java
      recordsPrintStream.println();

      if (counters.size() != 0) {
         recordsPrintStream.println("#Counters during initial load:");
         printCounters(recordsPrintStream, counters);
```

### SizeReplaceableByIsEmpty
`rolesForSubject.size() > 0` can be replaced with '!rolesForSubject.isEmpty()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/utils/SecurityManagerUtil.java`
#### Snippet
```java
            ActiveMQServerLogger.LOGGER.failedToFindRolesForTheSubject(e);
         }
         if (rolesForSubject.size() > 0 && rolesWithPermission.size() > 0) {
            Iterator<Principal> rolesForSubjectIter = rolesForSubject.iterator();
            while (!authorized && rolesForSubjectIter.hasNext()) {
```

### SizeReplaceableByIsEmpty
`rolesWithPermission.size() > 0` can be replaced with '!rolesWithPermission.isEmpty()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/utils/SecurityManagerUtil.java`
#### Snippet
```java
            ActiveMQServerLogger.LOGGER.failedToFindRolesForTheSubject(e);
         }
         if (rolesForSubject.size() > 0 && rolesWithPermission.size() > 0) {
            Iterator<Principal> rolesForSubjectIter = rolesForSubject.iterator();
            while (!authorized && rolesForSubjectIter.hasNext()) {
```

### SizeReplaceableByIsEmpty
`msgs.size() > 0` can be replaced with '!msgs.isEmpty()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java
         ConsumerHolder holder = consumerHolderIterator.next();
         List<MessageReference> msgs = holder.consumer.getDeliveringMessages();
         if (msgs != null && msgs.size() > 0) {
            mapReturn.put(holder.consumer.toManagementString(), msgs);
         }
```

### SizeReplaceableByIsEmpty
`expiryAddress.length() != 0` can be replaced with '!expiryAddress.isEmpty()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java
      SimpleString expiryAddress = addressSettings.getExpiryAddress();

      if (expiryAddress != null && expiryAddress.length() != 0) {

         createExpiryResources();
```

### SizeReplaceableByIsEmpty
`filterString.length() == 0` can be replaced with 'filterString.isEmpty()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java
   @Override
   public void deliverScheduledMessages(String filterString) throws ActiveMQException {
      final Filter filter = filterString == null || filterString.length() == 0 ? null : FilterImpl.createFilter(filterString);
      internalDeliverScheduleMessages(scheduledDeliveryHandler.cancel(ref -> filter == null ? true : filter.match(ref.getMessage())));
   }
```

### SizeReplaceableByIsEmpty
`messageReferences.size() == 0` can be replaced with 'messageReferences.isEmpty()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java

            if (queueDestroyed) {
               if (messageReferences.size() == 0) {
                  return false;
               }
```

### SizeReplaceableByIsEmpty
`messageReferences.size() == 0` can be replaced with 'messageReferences.isEmpty()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java
            }

            if (messageReferences.size() == 0) {
               break;
            }
```

### SizeReplaceableByIsEmpty
`scheduledMessages.size() > 0` can be replaced with '!scheduledMessages.isEmpty()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java

   private void internalDeliverScheduleMessages(List<MessageReference> scheduledMessages) {
      if (scheduledMessages != null && scheduledMessages.size() > 0) {
         for (MessageReference ref : scheduledMessages) {
            ref.getMessage().setScheduledDeliveryTime(ref.getScheduledDeliveryTime());
```

### SizeReplaceableByIsEmpty
`destinationAddress.length() != 0` can be replaced with '!destinationAddress.isEmpty()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java
   private void createResources(boolean isAutoCreate, SimpleString destinationAddress, SimpleString prefix, SimpleString suffix) throws Exception {
      if (isAutoCreate && !getAddress().equals(destinationAddress)) {
         if (destinationAddress != null && destinationAddress.length() != 0) {
            SimpleString destinationQueueName = prefix.concat(getAddress()).concat(suffix);
            SimpleString filter = new SimpleString(String.format("%s = '%s'", Message.HDR_ORIGINAL_ADDRESS, getAddress()));
```

### SizeReplaceableByIsEmpty
`xids.size() == 0` can be replaced with 'xids.isEmpty()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/ActiveMQServerControlImpl.java`
#### Snippet
```java
      try {
         Map<Xid, Long> xids = resourceManager.getPreparedTransactionsWithCreationTime();
         if (xids == null || xids.size() == 0) {
            return "";
         }
```

### SizeReplaceableByIsEmpty
`xids.size() == 0` can be replaced with 'xids.isEmpty()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/ActiveMQServerControlImpl.java`
#### Snippet
```java
      try {
         Map<Xid, Long> xids = resourceManager.getPreparedTransactionsWithCreationTime();
         if (xids == null || xids.size() == 0) {
            return "<h3>*** Prepared Transaction Details ***</h3><p>No entry.</p>";
         }
```

## RuleId[id=FinalStaticMethod]
### FinalStaticMethod
'static' method declared `final`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/FileUtil.java`
#### Snippet
```java
   }

   public static final boolean deleteDirectory(final File directory) {
      if (directory.isDirectory()) {
         String[] files = directory.list();
```

### FinalStaticMethod
'static' method declared `final`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/ByteUtil.java`
#### Snippet
```java
   }

   public static final byte[] intToBytes(int value) {
      return new byte[] {
         (byte)(value >>> 24),
```

### FinalStaticMethod
'static' method declared `final`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
    * @param args Input args
    */
   public static final void main(final String[] args) {
      if (args.length < 3) {
         Base64.usage("Not enough arguments.");
```

### FinalStaticMethod
'static' method declared `final`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/EmptyList.java`
#### Snippet
```java
   private static final LinkedList EMPTY_LIST = new EmptyList();

   public static final <T> LinkedList<T> getEmptyList() {
      return (LinkedList<T>) EMPTY_LIST;
   }
```

### FinalStaticMethod
'static' method declared `final`
in `artemis-unit-test-support/src/main/java/org/apache/activemq/artemis/logs/AssertionLoggerHandler.java`
#### Snippet
```java
    * @param individualMessages enables counting individual messages.
    */
   public static final void startCapture(boolean individualMessages, boolean captureStackTrace) {
      clear();
      if (individualMessages) {
```

### FinalStaticMethod
'static' method declared `final`
in `artemis-unit-test-support/src/main/java/org/apache/activemq/artemis/logs/AssertionLoggerHandler.java`
#### Snippet
```java
   }

   public static final void stopCapture() {
      capture = false;
      captureStackTrace = false;
```

### FinalStaticMethod
'static' method declared `final`
in `artemis-unit-test-support/src/main/java/org/apache/activemq/artemis/logs/AssertionLoggerHandler.java`
#### Snippet
```java
    * @param individualMessages enables counting individual messages.
    */
   public static final void startCapture(boolean individualMessages) {
      startCapture(individualMessages, captureStackTrace);
   }
```

### FinalStaticMethod
'static' method declared `final`
in `artemis-unit-test-support/src/main/java/org/apache/activemq/artemis/logs/AssertionLoggerHandler.java`
#### Snippet
```java
   }

   public static final void clear() {
      messages.clear();
      if (traceMessages != null) {
```

### FinalStaticMethod
'static' method declared `final`
in `artemis-unit-test-support/src/main/java/org/apache/activemq/artemis/logs/AssertionLoggerHandler.java`
#### Snippet
```java
   }

   public static final void startCapture() {
      startCapture(false);
   }
```

### FinalStaticMethod
'static' method declared `final`
in `artemis-web/src/main/java/org/apache/activemq/artemis/component/WebTmpCleaner.java`
#### Snippet
```java
   }

   public static final void deleteFolder(final File file) {
      if (file.isDirectory()) {
         String[] files = file.list();
```

### FinalStaticMethod
'static' method declared `final`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/ActiveMQClientProtocolManagerFactory.java`
#### Snippet
```java
   }

   public static final ActiveMQClientProtocolManagerFactory getInstance(ServerLocator locator) {
      ActiveMQClientProtocolManagerFactory factory = new ActiveMQClientProtocolManagerFactory();
      factory.setLocator(locator);
```

### FinalStaticMethod
'static' method declared `final`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/CheckDependencies.java`
#### Snippet
```java
   }

   public static final boolean isKQueueAvailable() {
      try {
         return Env.isMacOs() && KQueue.isAvailable();
```

### FinalStaticMethod
'static' method declared `final`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/CheckDependencies.java`
#### Snippet
```java
   private static final Logger logger = LoggerFactory.getLogger(MethodHandles.lookup().lookupClass());

   public static final boolean isEpollAvailable() {
      try {
         return Env.isLinuxOs() && Epoll.isAvailable();
```

### FinalStaticMethod
'static' method declared `final`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/reader/MessageUtil.java`
#### Snippet
```java
   }

   public static final void setJMSCorrelationIDAsBytes(Message message,
                                                       final byte[] correlationID) throws ActiveMQException {
      if (correlationID == null || correlationID.length == 0) {
```

### FinalStaticMethod
'static' method declared `final`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/ha/ReplicationBackupPolicyConfiguration.java`
#### Snippet
```java
   private DistributedPrimitiveManagerConfiguration distributedManagerConfiguration = null;

   public static final ReplicationBackupPolicyConfiguration withDefault() {
      return new ReplicationBackupPolicyConfiguration();
   }
```

### FinalStaticMethod
'static' method declared `final`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/impl/BridgeImpl.java`
#### Snippet
```java
   }

   public static final byte[] getDuplicateBytes(final UUID nodeUUID, final long messageID) {
      byte[] bytes = new byte[24];

```

### FinalStaticMethod
'static' method declared `final`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/OperationContextImpl.java`
#### Snippet
```java
   }

   public static final OperationContext getContext() {
      return getContext(null);
   }
```

### FinalStaticMethod
'static' method declared `final`
in `artemis-server/src/main/java/org/apache/activemq/artemis/utils/XMLConfigurationUtil.java`
#### Snippet
```java
public class XMLConfigurationUtil {

   public static final String getAttributeValue(Node element, String attribute) {
      Node node = element.getAttributes().getNamedItem(attribute);
      if (node == null) {
```

### FinalStaticMethod
'static' method declared `final`
in `artemis-server/src/main/java/org/apache/activemq/artemis/utils/XMLConfigurationUtil.java`
#### Snippet
```java
   }

   public static final Double getDouble(final Element e,
                                        final String name,
                                        final double def,
```

### FinalStaticMethod
'static' method declared `final`
in `artemis-server/src/main/java/org/apache/activemq/artemis/utils/XMLConfigurationUtil.java`
#### Snippet
```java
   }

   public static final Boolean getBoolean(final Element e, final String name, final Boolean def) {
      NodeList nl = e.getElementsByTagName(name);
      if (nl.getLength() > 0) {
```

### FinalStaticMethod
'static' method declared `final`
in `artemis-server/src/main/java/org/apache/activemq/artemis/utils/XMLConfigurationUtil.java`
#### Snippet
```java
   }

   public static final Integer getTextBytesAsIntBytes(final Element e,
                                          final String name,
                                          final int def,
```

### FinalStaticMethod
'static' method declared `final`
in `artemis-server/src/main/java/org/apache/activemq/artemis/utils/XMLConfigurationUtil.java`
#### Snippet
```java
   }

   public static final Integer getAttributeInteger(final Element e,
                                          final String name,
                                          final Integer def,
```

### FinalStaticMethod
'static' method declared `final`
in `artemis-server/src/main/java/org/apache/activemq/artemis/utils/XMLConfigurationUtil.java`
#### Snippet
```java
   }

   public static final Boolean getBooleanAttribute(final Element e, final String name, final Boolean def) {
      String attributeValue = e.getAttribute(name);
      if (attributeValue == null || attributeValue.isEmpty()) {
```

### FinalStaticMethod
'static' method declared `final`
in `artemis-server/src/main/java/org/apache/activemq/artemis/utils/XMLConfigurationUtil.java`
#### Snippet
```java
   }

   public static final Long getTextBytesAsLongBytes(final Element e,
                                    final String name,
                                    final long def,
```

### FinalStaticMethod
'static' method declared `final`
in `artemis-server/src/main/java/org/apache/activemq/artemis/utils/XMLConfigurationUtil.java`
#### Snippet
```java
   }

   public static final String getTrimmedTextContent(Node element) {
      String content = element.getTextContent();
      if (content == null)
```

### FinalStaticMethod
'static' method declared `final`
in `artemis-server/src/main/java/org/apache/activemq/artemis/utils/XMLConfigurationUtil.java`
#### Snippet
```java
   }

   public static final Integer getInteger(final Element e,
                                          final String name,
                                          final Integer def,
```

### FinalStaticMethod
'static' method declared `final`
in `artemis-server/src/main/java/org/apache/activemq/artemis/utils/XMLConfigurationUtil.java`
#### Snippet
```java
   }

   public static final Long getLong(final Element e,
                                    final String name,
                                    final long def,
```

### FinalStaticMethod
'static' method declared `final`
in `artemis-server/src/main/java/org/apache/activemq/artemis/utils/XMLConfigurationUtil.java`
#### Snippet
```java


   public static final Boolean parameterExists(final Element e, final String name) {
      NodeList nl = e.getElementsByTagName(name);
      if (nl.getLength() > 0) {
```

### FinalStaticMethod
'static' method declared `final`
in `artemis-server/src/main/java/org/apache/activemq/artemis/utils/XMLConfigurationUtil.java`
#### Snippet
```java
   }

   public static final String getString(final Element e,
                                        final String name,
                                        final String def,
```

## RuleId[id=TrivialStringConcatenation]
### TrivialStringConcatenation
Empty string used in concatenation
in `artemis-maven-plugin/src/main/java/org/apache/activemq/artemis/maven/ArtemisCreatePlugin.java`
#### Snippet
```java
      ArrayList<String> listCommands = new ArrayList<>();

      add(listCommands, "create", "--silent", "--force", "--user", user, "--password", password, "--role", role, "--port-offset", "" + portOffset, "--data", dataFolder);

      if (allowAnonymous) {
```

### TrivialStringConcatenation
Empty string used in concatenation
in `artemis-protocols/artemis-stomp-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/stomp/StompUtils.java`
#### Snippet
```java
         command.addHeader(Stomp.Headers.Message.CORRELATION_ID, message.getObjectProperty(MessageUtil.CORRELATIONID_HEADER_NAME).toString());
      }
      command.addHeader(Stomp.Headers.Message.EXPIRATION_TIME, "" + message.getExpiration());
      command.addHeader(Stomp.Headers.Message.REDELIVERED, String.valueOf(deliveryCount > 1));
      command.addHeader(Stomp.Headers.Message.PRIORITY, "" + message.getPriority());
```

### TrivialStringConcatenation
Empty string used in concatenation
in `artemis-protocols/artemis-stomp-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/stomp/StompUtils.java`
#### Snippet
```java
      command.addHeader(Stomp.Headers.Message.EXPIRATION_TIME, "" + message.getExpiration());
      command.addHeader(Stomp.Headers.Message.REDELIVERED, String.valueOf(deliveryCount > 1));
      command.addHeader(Stomp.Headers.Message.PRIORITY, "" + message.getPriority());
      command.addHeader(Stomp.Headers.Message.PERSISTENT, "" + message.isDurable());
      if (message.getStringProperty(ClientMessageImpl.REPLYTO_HEADER_NAME) != null) {
```

### TrivialStringConcatenation
Empty string used in concatenation
in `artemis-protocols/artemis-stomp-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/stomp/StompUtils.java`
#### Snippet
```java
      command.addHeader(Stomp.Headers.Message.REDELIVERED, String.valueOf(deliveryCount > 1));
      command.addHeader(Stomp.Headers.Message.PRIORITY, "" + message.getPriority());
      command.addHeader(Stomp.Headers.Message.PERSISTENT, "" + message.isDurable());
      if (message.getStringProperty(ClientMessageImpl.REPLYTO_HEADER_NAME) != null) {
         command.addHeader(Stomp.Headers.Message.REPLY_TO, message.getStringProperty(ClientMessageImpl.REPLYTO_HEADER_NAME));
```

### TrivialStringConcatenation
Empty string used in concatenation
in `artemis-protocols/artemis-stomp-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/stomp/StompUtils.java`
#### Snippet
```java
         command.addHeader(Stomp.Headers.Message.REPLY_TO, message.getStringProperty(ClientMessageImpl.REPLYTO_HEADER_NAME));
      }
      command.addHeader(Stomp.Headers.Message.TIMESTAMP, "" + message.getTimestamp());

      if (message.getObjectProperty(MessageUtil.TYPE_HEADER_NAME) != null) {
```

### TrivialStringConcatenation
Empty string used in concatenation
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
            return 3;
         } catch (Exception e) {
            System.out.println("" + source[srcOffset] + ": " + DECODABET[source[srcOffset]]);
            System.out.println("" + source[srcOffset + 1] + ": " + DECODABET[source[srcOffset + 1]]);
            System.out.println("" + source[srcOffset + 2] + ": " + DECODABET[source[srcOffset + 2]]);
```

### TrivialStringConcatenation
Empty string used in concatenation
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
         } catch (Exception e) {
            System.out.println("" + source[srcOffset] + ": " + DECODABET[source[srcOffset]]);
            System.out.println("" + source[srcOffset + 1] + ": " + DECODABET[source[srcOffset + 1]]);
            System.out.println("" + source[srcOffset + 2] + ": " + DECODABET[source[srcOffset + 2]]);
            System.out.println("" + source[srcOffset + 3] + ": " + DECODABET[source[srcOffset + 3]]);
```

### TrivialStringConcatenation
Empty string used in concatenation
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
            System.out.println("" + source[srcOffset] + ": " + DECODABET[source[srcOffset]]);
            System.out.println("" + source[srcOffset + 1] + ": " + DECODABET[source[srcOffset + 1]]);
            System.out.println("" + source[srcOffset + 2] + ": " + DECODABET[source[srcOffset + 2]]);
            System.out.println("" + source[srcOffset + 3] + ": " + DECODABET[source[srcOffset + 3]]);
            return -1;
```

### TrivialStringConcatenation
Empty string used in concatenation
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
            System.out.println("" + source[srcOffset + 1] + ": " + DECODABET[source[srcOffset + 1]]);
            System.out.println("" + source[srcOffset + 2] + ": " + DECODABET[source[srcOffset + 2]]);
            System.out.println("" + source[srcOffset + 3] + ": " + DECODABET[source[srcOffset + 3]]);
            return -1;
         } // end catch
```

### TrivialStringConcatenation
Empty string used in concatenation
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/uri/BeanSupport.java`
#### Snippet
```java
                              Map<String, Object> extraProps) {
      if (allowableProperties.contains("scheme")) {
         properties.put("scheme", "" + uri.getScheme());
      }
      if (allowableProperties.contains("host")) {
```

### TrivialStringConcatenation
Empty string used in concatenation
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/uri/BeanSupport.java`
#### Snippet
```java
      }
      if (allowableProperties.contains("host")) {
         properties.put("host", "" + uri.getHost());
      }
      if (allowableProperties.contains("port")) {
```

### TrivialStringConcatenation
Empty string used in concatenation
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/uri/BeanSupport.java`
#### Snippet
```java
      }
      if (allowableProperties.contains("port")) {
         properties.put("port", "" + uri.getPort());
      }
      if (allowableProperties.contains("userInfo")) {
```

### TrivialStringConcatenation
Empty string used in concatenation
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/uri/BeanSupport.java`
#### Snippet
```java
      }
      if (allowableProperties.contains("userInfo")) {
         properties.put("userInfo", "" + uri.getUserInfo());
      }
      for (Map.Entry<String, String> entry : query.entrySet()) {
```

### TrivialStringConcatenation
Empty string used in concatenation
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/TypedProperties.java`
#### Snippet
```java
      @Override
      public String toString() {
         return "" + getValue();
      }
   }
```

### TrivialStringConcatenation
Empty string used in concatenation
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Create.java`
#### Snippet
```java
         filters.put("${jdbcURL}", jdbcURL);
         filters.put("${jdbcClassName}", jdbcClassName);
         filters.put("${jdbcNetworkTimeout}", "" + jdbcNetworkTimeout);
         filters.put("${jdbcLockRenewPeriod}", "" + jdbcLockRenewPeriod);
         filters.put("${jdbcLockExpiration}", "" + jdbcLockExpiration);
```

### TrivialStringConcatenation
Empty string used in concatenation
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Create.java`
#### Snippet
```java
         filters.put("${jdbcClassName}", jdbcClassName);
         filters.put("${jdbcNetworkTimeout}", "" + jdbcNetworkTimeout);
         filters.put("${jdbcLockRenewPeriod}", "" + jdbcLockRenewPeriod);
         filters.put("${jdbcLockExpiration}", "" + jdbcLockExpiration);
         filters.put("${jdbc}", readTextFile(ETC_DATABASE_STORE_TXT, filters));
```

### TrivialStringConcatenation
Empty string used in concatenation
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Create.java`
#### Snippet
```java
         filters.put("${jdbcNetworkTimeout}", "" + jdbcNetworkTimeout);
         filters.put("${jdbcLockRenewPeriod}", "" + jdbcLockRenewPeriod);
         filters.put("${jdbcLockExpiration}", "" + jdbcLockExpiration);
         filters.put("${jdbc}", readTextFile(ETC_DATABASE_STORE_TXT, filters));
      } else {
```

### TrivialStringConcatenation
Empty string used in concatenation
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Create.java`
#### Snippet
```java
               syncFilter.put("${nanoseconds}", "0");
               syncFilter.put("${writesPerMillisecond}", "0");
               syncFilter.put("${maxaio}", journalType == JournalType.ASYNCIO ? "" + ActiveMQDefaultConfiguration.getDefaultJournalMaxIoAio() : "1");

               getActionContext().out.println("...Since you disabled sync and are using MAPPED journal, we are diabling buffer times");
```

### TrivialStringConcatenation
Empty string used in concatenation
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Create.java`
#### Snippet
```java
               syncFilter.put("${nanoseconds}", Long.toString(nanoseconds));
               syncFilter.put("${writesPerMillisecond}", writesPerMillisecondStr);
               syncFilter.put("${maxaio}", journalType == JournalType.ASYNCIO ? "" + ActiveMQDefaultConfiguration.getDefaultJournalMaxIoAio() : "1");

               getActionContext().out.println("done! Your system can make " + writesPerMillisecondStr +
```

### TrivialStringConcatenation
Empty string used in concatenation
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenWireProtocolManager.java`
#### Snippet
```java
      BrokerInfo brokerInfo = new BrokerInfo();
      brokerInfo.setBrokerName(getBrokerName());
      brokerInfo.setBrokerId(new BrokerId("" + server.getNodeID()));
      brokerInfo.setPeerBrokerInfos(null);
      brokerInfo.setFaultTolerantConfiguration(false);
```

### TrivialStringConcatenation
Empty string used in concatenation
in `artemis-web/src/main/java/org/apache/activemq/artemis/component/AuthenticationFilter.java`
#### Snippet
```java
            //Server error responses (500 – 599)
            //the user authentication has failed
            AuditLogger.userFailedAuthenticationInAudit("" + status);
         }
      }
```

### TrivialStringConcatenation
Empty string used in concatenation
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ClientProducerCreditsImpl.java`
#### Snippet
```java
               // I'm using string concatenation here in case address is null
               // better getting a "null" string than a NPE
               ActiveMQClientLogger.LOGGER.outOfCreditOnFlowControl("" + address);
            }
         } catch (InterruptedException interrupted) {
```

### TrivialStringConcatenation
Empty string used in concatenation
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/message/openmbean/MessageOpenTypeFactory.java`
#### Snippet
```java
   public Map<String, Object> getFields(M m, int valueSizeLimit, int deliveryCount) throws OpenDataException {
      Map<String, Object> rc = new HashMap<>();
      rc.put(CompositeDataConstants.MESSAGE_ID, "" + m.getMessageID());
      rc.put(CompositeDataConstants.PROTOCOL, m.getProtocolName());
      if (m.getUserID() != null) {
```

### TrivialStringConcatenation
Empty string used in concatenation
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/message/openmbean/MessageOpenTypeFactory.java`
#### Snippet
```java
      Map<String, Object> propertyMap = expandProperties(m, valueSizeLimit);

      rc.put(CompositeDataConstants.PROPERTIES, JsonUtil.truncate("" + propertyMap, valueSizeLimit));

      // only populate if there are some values
```

### TrivialStringConcatenation
Empty string used in concatenation
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/JMXConnectorConfiguration.java`
#### Snippet
```java
      if (rmiRegistryPort != 0) {
         // This is handy to use if you have a firewall and need to force JMX to use fixed ports.
         rmiServer = "" + getConnectorHost() + ":" + rmiRegistryPort;
      }
      return "service:jmx:rmi://" + rmiServer + "/jndi/rmi://" + getConnectorHost() + ":" + connectorPort + connectorPath;
```

### TrivialStringConcatenation
Empty string used in concatenation
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/impl/PagingStoreFactoryDatabase.java`
#### Snippet
```java
   @Override
   public synchronized SequentialFileFactory newFileFactory(final SimpleString address) throws Exception {
      String tableName = "" + storageManager.generateID();
      SequentialFileFactory factory = newFileFactory(tableName, true);
      factory.start();
```

### TrivialStringConcatenation
Empty string used in concatenation
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/impl/PagingStoreImpl.java`
#### Snippet
```java
            // this could be eventually negative for a short period of time
            // but after compensating the non existent file the assertion should still hold true
            assert numberOfPages >= 0 : "numberOfPages should never be negative. on depage(). currentPageId=" + currentPageId + ", firstPageId=" + firstPageId + "";

            return returnPage;
```

### TrivialStringConcatenation
Empty string used in concatenation
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/AddressInfo.java`
#### Snippet
```java

      builder.add("id", id);
      builder.add("name", "" + name);
      builder.add("auto-created", autoCreated);
      builder.add("temporary", temporary);
```

### TrivialStringConcatenation
Empty string used in concatenation
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/client/ActiveMQClient.java`
#### Snippet
```java
   public static void initializeGlobalThreadPoolProperties() {

      setGlobalThreadPoolProperties(Integer.valueOf(System.getProperty(ActiveMQClient.THREAD_POOL_MAX_SIZE_PROPERTY_KEY, "" + ActiveMQClient.DEFAULT_GLOBAL_THREAD_POOL_MAX_SIZE)), Integer.valueOf(System.getProperty(ActiveMQClient.SCHEDULED_THREAD_POOL_SIZE_PROPERTY_KEY, "" + ActiveMQClient.DEFAULT_SCHEDULED_THREAD_POOL_MAX_SIZE)));
   }

```

### TrivialStringConcatenation
Empty string used in concatenation
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/AbstractControl.java`
#### Snippet
```java
         // There's no point on direct delivery using the management thread, use false here
         serverSession.send(message, false, null);
         return "" + message.getMessageID();
      } finally {
         try {
```

## RuleId[id=NumberEquality]
### NumberEquality
Number objects are compared using `==`, not 'equals()'
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/ActiveMQRaUtils.java`
#### Snippet
```java
   public static boolean compare(final Long me, final Long you) {
      // If both null or intern equals
      if (me == you) {
         return true;
      }
```

### NumberEquality
Number objects are compared using `==`, not 'equals()'
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/ActiveMQRaUtils.java`
#### Snippet
```java
   public static boolean compare(final Integer me, final Integer you) {
      // If both null or intern equals
      if (me == you) {
         return true;
      }
```

### NumberEquality
Number objects are compared using `==`, not 'equals()'
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/ActiveMQRaUtils.java`
#### Snippet
```java
   public static boolean compare(final Double me, final Double you) {
      // If both null or intern equals
      if (me == you) {
         return true;
      }
```

## RuleId[id=SuspiciousInvocationHandlerImplementation]
### SuspiciousInvocationHandlerImplementation
Null might be returned when proxying method 'equals()': this may cause NullPointerException
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/xml/XmlDataExporter.java`
#### Snippet
```java
         method.invoke(target, args);

         return null;
      }

```

### SuspiciousInvocationHandlerImplementation
Null might be returned when proxying method 'equals()': this may cause NullPointerException
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/ArtemisMBeanServerGuard.java`
#### Snippet
```java
   public Object invoke(Object proxy, Method method, Object[] args) throws Throwable {
      if (method.getParameterTypes().length == 0)
         return null;

      if (!ObjectName.class.isAssignableFrom(method.getParameterTypes()[0]))
```

### SuspiciousInvocationHandlerImplementation
Null might be returned when proxying method 'equals()': this may cause NullPointerException
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/ArtemisMBeanServerGuard.java`
#### Snippet
```java

      if (!ObjectName.class.isAssignableFrom(method.getParameterTypes()[0]))
         return null;

      ObjectName objectName = (ObjectName) args[0];
```

### SuspiciousInvocationHandlerImplementation
Null might be returned when proxying method 'equals()': this may cause NullPointerException
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/ArtemisMBeanServerGuard.java`
#### Snippet
```java
      }

      return null;
   }

```

## RuleId[id=ExplicitArrayFilling]
### ExplicitArrayFilling
Can be replaced with single 'Arrays.fill()' method call
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/ProducerThread.java`
#### Snippet
```java
      if (messageSize > 0) {
         payload = new byte[messageSize];
         for (int i = 0; i < payload.length; i++) {
            payload[i] = '.';
         }
```

### ExplicitArrayFilling
Can be replaced with single 'Arrays.fill()' method call
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/protocol/MessagePersister.java`
#### Snippet
```java

   public static void clearPersisters() {
      for (int i = 0; i < persisters.length; i++) {
         persisters[i] = null;
      }
```

## RuleId[id=DuplicateCondition]
### DuplicateCondition
Duplicate condition `ok == null`
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/io/RunnableCallback.java`
#### Snippet
```java

   public RunnableCallback(Runnable ok, Runnable error) {
      if (ok == null) {
         throw new NullPointerException("ok = null");
      }
```

### DuplicateCondition
Duplicate condition `ok == null`
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/io/RunnableCallback.java`
#### Snippet
```java
         throw new NullPointerException("ok = null");
      }
      if (ok == null) {
         throw new NullPointerException("error = null");
      }
```

### DuplicateCondition
Duplicate condition `isTopic`
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/inflow/ActiveMQActivation.java`
#### Snippet
```java

               String calculatedDestinationName = destinationName.substring(destinationName.lastIndexOf('/') + 1);
               if (isTopic) {
                  calculatedDestinationName = getTopicWithPrefix(calculatedDestinationName);
               } else if (!isTopic) {
```

### DuplicateCondition
Duplicate condition `isTopic`
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/inflow/ActiveMQActivation.java`
#### Snippet
```java
               if (isTopic) {
                  calculatedDestinationName = getTopicWithPrefix(calculatedDestinationName);
               } else if (!isTopic) {
                  calculatedDestinationName = getQueueWithPrefix(calculatedDestinationName);
               }
```

### DuplicateCondition
Duplicate condition `threadName.contains("threadDeathWatcher")`
in `artemis-unit-test-support/src/main/java/org/apache/activemq/artemis/utils/ThreadLeakCheckRule.java`
#### Snippet
```java
      } else if (threadName.contains("globalEventExecutor")) {
         return true;
      } else if (threadName.contains("threadDeathWatcher")) {
         return true;
      } else if (threadName.contains("netty-threads")) {
```

### DuplicateCondition
Duplicate condition `threadName.contains("threadDeathWatcher")`
in `artemis-unit-test-support/src/main/java/org/apache/activemq/artemis/utils/ThreadLeakCheckRule.java`
#### Snippet
```java
         // if the EventLoop's are still busy.
         return true;
      } else if (threadName.contains("threadDeathWatcher")) {
         //another netty thread
         return true;
```

### DuplicateCondition
Duplicate condition `purgeOnNoConsumers != that.purgeOnNoConsumers`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/QueueConfig.java`
#### Snippet
```java
      if (nonDestructive != that.nonDestructive)
         return false;
      if (purgeOnNoConsumers != that.purgeOnNoConsumers)
         return false;
      if (consumersBeforeDispatch != that.consumersBeforeDispatch)
```

### DuplicateCondition
Duplicate condition `purgeOnNoConsumers != that.purgeOnNoConsumers`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/QueueConfig.java`
#### Snippet
```java
      if (delayBeforeDispatch != that.delayBeforeDispatch)
         return false;
      if (purgeOnNoConsumers != that.purgeOnNoConsumers)
         return false;
      if (groupRebalance != that.groupRebalance)
```

## RuleId[id=IgnoreResultOfCall]
### IgnoreResultOfCall
Result of `File.mkdirs()` is ignored
in `artemis-maven-plugin/src/main/java/org/apache/activemq/artemis/maven/ArtemisDependencyScanPlugin.java`
#### Snippet
```java

         if (targetFolder != null) {
            targetFolder.mkdirs();
            for (File file : filesSet) {
               Files.copy(file.toPath(), targetFolder.toPath(), StandardCopyOption.REPLACE_EXISTING);
```

### IgnoreResultOfCall
Result of `File.mkdirs()` is ignored
in `artemis-maven-plugin/src/main/java/org/apache/activemq/artemis/maven/ArtemisCreatePlugin.java`
#### Snippet
```java
      getLog().debug("***** Server created at " + instance + " with home=" + home + " *****");

      instance.mkdirs();
      File commandLine = new File(instance.getParentFile(), "create-" + instance.getName() + ".sh");
      FileOutputStream outputStream;
```

### IgnoreResultOfCall
Result of `File.mkdirs()` is ignored
in `artemis-maven-plugin/src/main/java/org/apache/activemq/artemis/maven/ArtemisCreatePlugin.java`
#### Snippet
```java
      File parent = file.getParentFile();
      if (!parent.exists()) {
         parent.mkdirs();
         commandLineStream.println("mkdir " + file.getParent());
      }
```

### IgnoreResultOfCall
Result of `File.mkdirs()` is ignored
in `artemis-maven-plugin/src/main/java/org/apache/activemq/artemis/maven/ArtemisDependencyDocPlugin.java`
#### Snippet
```java
      try {
         File javaFile = new File(file);
         javaFile.getParentFile().mkdirs();

         PrintStream stream = new PrintStream(new BufferedOutputStream(new FileOutputStream(file)));
```

### IgnoreResultOfCall
Result of `File.mkdirs()` is ignored
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/io/aio/AIOSequentialFileFactory.java`
#### Snippet
```java
      int alignment;
      try {
         journalDir.mkdirs();
         checkFile = File.createTempFile("journalCheck", ".tmp", journalDir);
         checkFile.mkdirs();
```

### IgnoreResultOfCall
Result of `File.mkdirs()` is ignored
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/io/aio/AIOSequentialFileFactory.java`
#### Snippet
```java
         journalDir.mkdirs();
         checkFile = File.createTempFile("journalCheck", ".tmp", journalDir);
         checkFile.mkdirs();
         checkFile.createNewFile();
         alignment = LibaioContext.getBlockSize(checkFile);
```

### IgnoreResultOfCall
Result of `File.createNewFile()` is ignored
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/io/aio/AIOSequentialFileFactory.java`
#### Snippet
```java
         checkFile = File.createTempFile("journalCheck", ".tmp", journalDir);
         checkFile.mkdirs();
         checkFile.createNewFile();
         alignment = LibaioContext.getBlockSize(checkFile);
      } catch (Throwable e) {
```

### IgnoreResultOfCall
Result of `File.delete()` is ignored
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/io/aio/AIOSequentialFileFactory.java`
#### Snippet
```java
      } finally {
         if (checkFile != null) {
            checkFile.delete();
         }
      }
```

### IgnoreResultOfCall
Result of `File.delete()` is ignored
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/io/aio/AIOSequentialFileFactory.java`
#### Snippet
```java
         int fd = LibaioContext.open(aioTestFile.getAbsolutePath(), true);
         LibaioContext.close(fd);
         aioTestFile.delete();
      } catch (Exception e) {
         // try to handle the file using plain Java
```

### IgnoreResultOfCall
Result of `File.createNewFile()` is ignored
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Stop.java`
#### Snippet
```java
      File stopFile = new File(file, STOP_FILE_NAME);

      stopFile.createNewFile();

      return null;
```

### IgnoreResultOfCall
Result of `File.createNewFile()` is ignored
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Kill.java`
#### Snippet
```java
      File killFile = new File(file, "KILL_ME");

      killFile.createNewFile();

      return null;
```

### IgnoreResultOfCall
Result of `File.delete()` is ignored
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Run.java`
#### Snippet
```java
               try {
                  System.err.println("Halting by user request");
                  fileKill.delete();
               } catch (Throwable ignored) {
               }
```

### IgnoreResultOfCall
Result of `Enumeration.hasMoreElements()` is ignored
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/check/QueueCheck.java`
#### Snippet
```java

         if (browse == -1) {
            queueBrowserEnum.hasMoreElements();
         } else {
            int count = 0;
```

### IgnoreResultOfCall
Result of `File.mkdirs()` is ignored
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/LockAbstract.java`
#### Snippet
```java
   void lockCLI(File lockPlace) throws Exception {
      if (lockPlace != null) {
         lockPlace.mkdirs();
         if (serverLockFile == null) {
            File fileLock = new File(lockPlace, "cli.lock");
```

### IgnoreResultOfCall
Result of `File.mkdirs()` is ignored
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/journal/PerfJournal.java`
#### Snippet
```java
      System.out.println("Performing " + tries + " tests writing " + writes + " blocks of " + size + " on each test, sync=" + fileConfiguration.isJournalDatasync() + " with journalType = " + fileConfiguration.getJournalType());

      fileConfiguration.getJournalLocation().mkdirs();

      if (maxAIO <= 0) {
```

### IgnoreResultOfCall
Result of `File.delete()` is ignored
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Create.java`
#### Snippet
```java
               supportsLibaio = false;
            }
            tmpFile.delete();
            if (!supportsLibaio) {
               System.err.println("The filesystem used on " + directory + " doesn't support libAIO and O_DIRECT files, switching journal-type to NIO");
```

### IgnoreResultOfCall
Result of `File.mkdirs()` is ignored
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Create.java`
#### Snippet
```java
      }

      new File(directory, "bin").mkdirs();
      File etcFolder = createDirectory(etc, directory);
      new File(directory, "tmp").mkdirs();
```

### IgnoreResultOfCall
Result of `File.mkdirs()` is ignored
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Create.java`
#### Snippet
```java
      new File(directory, "bin").mkdirs();
      File etcFolder = createDirectory(etc, directory);
      new File(directory, "tmp").mkdirs();
      new File(directory, "lib").mkdirs();
      File dataFolder = createDirectory(data, directory);
```

### IgnoreResultOfCall
Result of `File.mkdirs()` is ignored
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Create.java`
#### Snippet
```java
      File etcFolder = createDirectory(etc, directory);
      new File(directory, "tmp").mkdirs();
      new File(directory, "lib").mkdirs();
      File dataFolder = createDirectory(data, directory);
      File logFolder = createDirectory("log", directory);
```

### IgnoreResultOfCall
Result of `File.mkdirs()` is ignored
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Create.java`
#### Snippet
```java
         directory = new File(root, name);
      }
      directory.mkdirs();
      return directory;
   }
```

### IgnoreResultOfCall
Result of `File.mkdirs()` is ignored
in `artemis-cli/src/main/java/org/apache/activemq/artemis/integration/FileBroker.java`
#### Snippet
```java

   private void createDirectories(FileConfiguration fileConfiguration) {
      fileConfiguration.getPagingLocation().mkdirs();
      fileConfiguration.getJournalLocation().mkdirs();
      fileConfiguration.getBindingsLocation().mkdirs();
```

### IgnoreResultOfCall
Result of `File.mkdirs()` is ignored
in `artemis-cli/src/main/java/org/apache/activemq/artemis/integration/FileBroker.java`
#### Snippet
```java
   private void createDirectories(FileConfiguration fileConfiguration) {
      fileConfiguration.getPagingLocation().mkdirs();
      fileConfiguration.getJournalLocation().mkdirs();
      fileConfiguration.getBindingsLocation().mkdirs();
      fileConfiguration.getLargeMessagesLocation().mkdirs();
```

### IgnoreResultOfCall
Result of `File.mkdirs()` is ignored
in `artemis-cli/src/main/java/org/apache/activemq/artemis/integration/FileBroker.java`
#### Snippet
```java
      fileConfiguration.getPagingLocation().mkdirs();
      fileConfiguration.getJournalLocation().mkdirs();
      fileConfiguration.getBindingsLocation().mkdirs();
      fileConfiguration.getLargeMessagesLocation().mkdirs();
   }
```

### IgnoreResultOfCall
Result of `File.mkdirs()` is ignored
in `artemis-cli/src/main/java/org/apache/activemq/artemis/integration/FileBroker.java`
#### Snippet
```java
      fileConfiguration.getJournalLocation().mkdirs();
      fileConfiguration.getBindingsLocation().mkdirs();
      fileConfiguration.getLargeMessagesLocation().mkdirs();
   }

```

### IgnoreResultOfCall
Result of `DataInputStream.read()` is ignored
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenWireMessageConverter.java`
#### Snippet
```java
               int slen = sdis.readInt();
               byte[] sbytes = new byte[slen];
               sdis.read(sbytes);
               body.writeInt(slen);
               body.writeBytes(sbytes);
```

### IgnoreResultOfCall
Result of `File.mkdirs()` is ignored
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalImpl.java`
#### Snippet
```java
      }
      this.journalRetentionFolder = historyFolder;
      this.journalRetentionFolder.mkdirs();

      this.journalRetentionMaxFiles = (int) (maxBytes / this.fileSize);
```

### IgnoreResultOfCall
Result of `File.delete()` is ignored
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalImpl.java`
#### Snippet
```java
            for (File f : repeatFiles) {
               logger.warn("File {} was partially copied before, removing the file", f);
               f.delete();
            }
         }
```

### IgnoreResultOfCall
Result of `File.delete()` is ignored
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalImpl.java`
#### Snippet
```java
                  logger.debug("Removing {}", file);
                  File fileToRemove = new File(journalRetentionFolder, file);
                  fileToRemove.delete();
                  toRemove--;
                  if (toRemove <= 0) {
```

### IgnoreResultOfCall
Result of `InputStream.read()` is ignored
in `artemis-jdbc-store/src/main/java/org/apache/activemq/artemis/jdbc/store/journal/JDBCJournalRecord.java`
#### Snippet
```java
   private byte[] getRecordData() throws IOException {
      byte[] data = new byte[variableSize];
      record.read(data);
      return data;
   }
```

### IgnoreResultOfCall
Result of `InputStream.read()` is ignored
in `artemis-jdbc-store/src/main/java/org/apache/activemq/artemis/jdbc/store/journal/JDBCJournalRecord.java`
#### Snippet
```java

      try {
         record.read(recordBytes);
         txData.read(txDataBytes);
      } catch (IOException e) {
```

### IgnoreResultOfCall
Result of `InputStream.read()` is ignored
in `artemis-jdbc-store/src/main/java/org/apache/activemq/artemis/jdbc/store/journal/JDBCJournalRecord.java`
#### Snippet
```java
      try {
         record.read(recordBytes);
         txData.read(txDataBytes);
      } catch (IOException e) {
         logger.error("Error occurred whilst reading Journal Record", e);
```

### IgnoreResultOfCall
Result of `InputStream.read()` is ignored
in `artemis-jdbc-store/src/main/java/org/apache/activemq/artemis/jdbc/store/journal/JDBCJournalRecord.java`
#### Snippet
```java
   byte[] getTxDataAsByteArray() throws IOException {
      byte[] data = new byte[txDataSize];
      txData.read(data);
      return data;
   }
```

### IgnoreResultOfCall
Result of `File.delete()` is ignored
in `artemis-web/src/main/java/org/apache/activemq/artemis/component/WebTmpCleaner.java`
#### Snippet
```java
         }
      }
      file.delete();
   }

```

### IgnoreResultOfCall
Result of `FilterInputStream.read()` is ignored
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/CompressedLargeMessageControllerImpl.java`
#### Snippet
```java
      try {
         for (int i = 0; i < length; i++) {
            getStream().read();
         }
         return length;
```

### IgnoreResultOfCall
Result of `File.mkdirs()` is ignored
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/FileBasedNodeManager.java`
#### Snippet
```java
      this.directory = directory;
      if (directory != null) {
         directory.mkdirs();
      }
      activationSequencePath = new File(directory, SERVER_ACTIVATION_SEQUENCE_NAME).toPath();
```

### IgnoreResultOfCall
Result of `File.setLastModified()` is ignored
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/FileLockNodeManager.java`
#### Snippet
```java
               // if the backup acquires the file lock and the state is 'L' that means the primary died
               logger.debug("acquired live node lock state = {}", (char) state);
               serverLockFile.setLastModified(System.currentTimeMillis());
               logger.debug("touched {}; new time: {}", serverLockFile.getAbsoluteFile(), serverLockFile.lastModified());
               break;
```

### IgnoreResultOfCall
Result of `File.createNewFile()` is ignored
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/FileLockNodeManager.java`
#### Snippet
```java
         File fileLock = newFile("serverlock." + i);
         if (!fileLock.exists()) {
            fileLock.createNewFile();
         }
         RandomAccessFile randomFileLock = new RandomAccessFile(fileLock, "rw");
```

### IgnoreResultOfCall
Result of `File.mkdirs()` is ignored
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/files/FileMoveManager.java`
#### Snippet
```java
         internalCheckOldFolders(1);
         File folderTo = getFolder(whereToMove);
         folderTo.mkdirs();

         ActiveMQServerLogger.LOGGER.backupMovingDataAway(folder.getPath(), folderTo.getPath());
```

### IgnoreResultOfCall
Result of `File.delete()` is ignored
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/files/FileMoveManager.java`
#### Snippet
```java
      }

      file.delete();
   }

```

### IgnoreResultOfCall
Result of `File.mkdirs()` is ignored
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ActiveMQServerImpl.java`
#### Snippet
```java
      if (!journalDir.exists() && configuration.isPersistenceEnabled()) {
         if (configuration.isCreateJournalDir()) {
            journalDir.mkdirs();
         } else {
            throw ActiveMQMessageBundle.BUNDLE.cannotCreateDir(journalDir.getAbsolutePath());
```

### IgnoreResultOfCall
Result of `File.mkdirs()` is ignored
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ActiveMQServerImpl.java`
#### Snippet
```java
      if (!journalDir.equals(nodeManagerLockDir)) {
         if (configuration.isPersistenceEnabled() && !nodeManagerLockDir.exists()) {
            nodeManagerLockDir.mkdirs();
         }
      }
```

## RuleId[id=UnnecessaryUnboxing]
### UnnecessaryUnboxing
Unnecessary unboxing
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/JAXPXPathEvaluator.java`
#### Snippet
```java
   protected boolean evaluate(InputSource inputSource) {
      try {
         return ((Boolean)xpath.evaluate(xpathExpression, builder.parse(inputSource), XPathConstants.BOOLEAN)).booleanValue();
      } catch (Exception e) {
         return false;
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/ConstantExpression.java`
#### Snippet
```java
      }
      if (value instanceof Boolean) {
         return ((Boolean) value).booleanValue() ? "TRUE" : "FALSE";
      }
      if (value instanceof String) {
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/LogicExpression.java`
#### Snippet
```java
         for (BooleanExpression expression : expressions) {
            Boolean lv = (Boolean)expression.evaluate(message);
            if (lv != null && lv.booleanValue()) {
               return Boolean.TRUE;
            }
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/LogicExpression.java`
#### Snippet
```java
         for (BooleanExpression expression : expressions) {
            Boolean lv = (Boolean)expression.evaluate(message);
            if (lv != null && !lv.booleanValue()) {
               return Boolean.FALSE;
            }
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/UnaryExpression.java`
#### Snippet
```java
               return Boolean.FALSE;
            }
            return ((Boolean) rvalue).booleanValue();
         }

```

### UnnecessaryUnboxing
Unnecessary unboxing
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/UnaryExpression.java`
#### Snippet
```java
               return null;
            }
            return !lvalue.booleanValue();
         }

```

### UnnecessaryUnboxing
Unnecessary unboxing
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/ActiveMQRAManagedConnection.java`
#### Snippet
```java

      Integer tryLock = mcf.getUseTryLock();
      if (tryLock == null || tryLock.intValue() <= 0) {
         lock();
         return;
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/ActiveMQRAManagedConnection.java`
#### Snippet
```java
      }
      try {
         if (lock.tryLock(tryLock.intValue(), TimeUnit.SECONDS) == false) {
            throw new ResourceAllocationException("Unable to obtain lock in " + tryLock + " seconds: " + this);
         }
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/InputAbstract.java`
#### Snippet
```java
      while (booleanValue == null);

      return booleanValue.booleanValue();
   }

```

### UnnecessaryUnboxing
Unnecessary unboxing
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/InputAbstract.java`
#### Snippet
```java
      while(value == null);

      return value.intValue();
   }

```

### UnnecessaryUnboxing
Unnecessary unboxing
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/queue/QueueAbstract.java`
#### Snippet
```java

      if (purgeOnNoConsumers != null) {
         value = purgeOnNoConsumers.booleanValue();
      } else if (preserveOnNoConsumers != null) {
         value = !preserveOnNoConsumers.booleanValue();
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/queue/QueueAbstract.java`
#### Snippet
```java
         value = purgeOnNoConsumers.booleanValue();
      } else if (preserveOnNoConsumers != null) {
         value = !preserveOnNoConsumers.booleanValue();
      }

```

### UnnecessaryUnboxing
Unnecessary unboxing
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/queue/QueueAbstract.java`
#### Snippet
```java
      }

      purgeOnNoConsumers = value.booleanValue();
      preserveOnNoConsumers = !value.booleanValue();

```

### UnnecessaryUnboxing
Unnecessary unboxing
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/queue/QueueAbstract.java`
#### Snippet
```java

      purgeOnNoConsumers = value.booleanValue();
      preserveOnNoConsumers = !value.booleanValue();

      return value;
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/queue/QueueAbstract.java`
#### Snippet
```java
         if (anycast != null) {
            // if anycast is not null, it should be the opposite
            multicast = !anycast.booleanValue();
         }

```

### UnnecessaryUnboxing
Unnecessary unboxing
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/queue/QueueAbstract.java`
#### Snippet
```java
         if (multicast != null) {
            // if multicast is not null, it should be the opposite
            anycast = !multicast.booleanValue();
         }

```

### UnnecessaryUnboxing
Unnecessary unboxing
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/queue/QueueAbstract.java`
#### Snippet
```java
      if (durable == null) {
         if (noDurable != null) {
            durable = !noDurable.booleanValue();
         }
      }
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/address/AddressAbstract.java`
#### Snippet
```java
   public boolean isAnycast() {
      if (noAnycast != null) {
         anycast = !noAnycast.booleanValue();
      }
      if (anycast == null) {
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/address/AddressAbstract.java`
#### Snippet
```java
   public boolean isMulticast() {
      if (noMulticast != null) {
         multicast = !noMulticast.booleanValue();
      }
      if (multicast == null) {
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Create.java`
#### Snippet
```java
      if (autoCreate == null) {
         if (noAutoCreate != null) {
            autoCreate = !noAutoCreate.booleanValue();
         }
      }
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Create.java`
#### Snippet
```java

      // requireLogin should set allowAnonymous=false, to avoid user's questions
      if (requireLogin != null && requireLogin.booleanValue()) {
         allowAnonymous = Boolean.FALSE;
      }
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/ProtonProtocolManager.java`
#### Snippet
```java

      if (getAmqpIdleTimeout() != null) {
         ttl = getAmqpIdleTimeout().longValue();
      }

```

### UnnecessaryUnboxing
Unnecessary unboxing
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPLargeMessage.java`
#### Snippet
```java
   public final boolean isDurable() {
      if (fileDurable != null) {
         return fileDurable.booleanValue();
      } else {
         return super.isDurable();
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
   @Override
   public final AMQPMessage setScheduledDeliveryTime(Long time) {
      if (time != null && time.longValue() > 0) {
         setMessageAnnotation(AMQPMessageSupport.SCHEDULED_DELIVERY_TIME, time);
         removeMessageAnnotation(AMQPMessageSupport.SCHEDULED_DELIVERY_DELAY);
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/converter/AmqpCoreConverter.java`
#### Snippet
```java
         if (header.getDurable() != null) {
            jms.setBooleanProperty(JMS_AMQP_HEADER_DURABLE, true);
            jms.setDeliveryMode(header.getDurable().booleanValue() ? PERSISTENT : NON_PERSISTENT);
         } else {
            jms.setDeliveryMode(NON_PERSISTENT);
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/utils/BufferHelper.java`
#### Snippet
```java

      if (value != null) {
         buffer.writeDouble(value.doubleValue());
      }
   }
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/utils/BufferHelper.java`
#### Snippet
```java

      if (value != null) {
         buffer.writeLong(value.longValue());
      }
   }
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/utils/BufferHelper.java`
#### Snippet
```java

      if (value != null) {
         buffer.writeBoolean(value.booleanValue());
      }
   }
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/utils/BufferHelper.java`
#### Snippet
```java

      if (value != null) {
         buffer.writeInt(value.intValue());
      }
   }
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/impl/PagingStoreImpl.java`
#### Snippet
```java
         }

         pageMessageMessagesClear = (numberOfMessages < pageLimitMessages.longValue());
      }

```

### UnnecessaryUnboxing
Unnecessary unboxing
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/impl/PagingStoreImpl.java`
#### Snippet
```java
      pageLimitBytes = addressSettings.getPageLimitBytes();

      if (pageLimitBytes != null && pageLimitBytes.longValue() < 0) {
         logger.debug("address {} had pageLimitBytes<0, setting it as null", address);
         pageLimitBytes = null;
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/impl/PagingStoreImpl.java`
#### Snippet
```java
      pageLimitMessages = addressSettings.getPageLimitMessages();

      if (pageLimitMessages != null && pageLimitMessages.longValue() < 0) {
         logger.debug("address {} had pageLimitMessages<0, setting it as null", address);
         pageLimitMessages = null;
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/impl/PagingStoreImpl.java`
#### Snippet
```java
   private boolean isBelowPageLimitBytes() {
      if (estimatedMaxPages != null) {
         return (numberOfPages <= estimatedMaxPages.longValue());
      }  else {
         return true;
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/cursor/impl/PageSubscriptionCounterImpl.java`
#### Snippet
```java
      Long pageLimitMessages = pagingStore.getPageLimitMessages();
      if (pageLimitMessages != null) {
         if (numberOfMessages >= pageLimitMessages.longValue()) {
            pagingStore.pageFull(this.subscription);
         }
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ColocatedActivation.java`
#### Snippet
```java
            TopologyMemberImpl member = voteTopology.getMember(decision.getA());
            try {
               boolean backupStarted = colocatedHAManager.requestBackup(member.getConnector(), decision.getB().intValue(), !colocatedPolicy.isSharedStore());
               if (!backupStarted) {
                  nodes.clear();
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/RoutingContextImpl.java`
#### Snippet
```java
   @Override
   public RoutingContextImpl setReusable(boolean reusable) {
      if (this.reusable != null && !this.reusable.booleanValue()) {
         // cannot set to Reusable once it was set to false
         return this;
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/RoutingContextImpl.java`
#### Snippet
```java
      this.previousAddress = address;
      this.previousRoutingType = routingType;
      if (this.reusable != null && !this.reusable.booleanValue()) {
         // cannot set to Reusable once it was set to false
         return this;
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/protocol/core/ServerSessionPacketHandler.java`
#### Snippet
```java

      try {
         return future.get().intValue();
      } catch (Exception e) {
         throw new IllegalStateException(e);
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/postoffice/impl/PersistentDuplicateIDCache.java`
#### Snippet
```java
      if (posUsed != null) {
         synchronized (this) {
            final ObjLongPair<ByteArray> id = ids.get(posUsed.intValue());

            if (id.getA().equals(duplicateID)) {
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/postoffice/impl/PersistentDuplicateIDCache.java`
#### Snippet
```java
            continue;
         }
         assert id.getB() != null && id.getB().longValue() != NIL;
         if (toNotBeAdded > 0) {
            if (txID == -1) {
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/QueueControlImpl.java`
#### Snippet
```java
      }
      long now = new Date().getTime();
      return now - firstMessageTimestamp.longValue();
   }

```

### UnnecessaryUnboxing
Unnecessary unboxing
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/postoffice/impl/InMemoryDuplicateIDCache.java`
#### Snippet
```java

         synchronized (this) {
            final int index = posUsed.intValue();
            id = ids.get(index);

```

### UnnecessaryUnboxing
Unnecessary unboxing
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/BatchingIDGenerator.java`
#### Snippet
```java
            if (iterRecord.hasNext()) {
               // we don't want to remove the last record
               deleteID(record.longValue());
            }
         }
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java
                     txCount++;
                     count++;
                     if (expectedHits != null && count >= expectedHits.intValue()) {
                        break;
                     }
```

## RuleId[id=ClassNameSameAsAncestorName]
### ClassNameSameAsAncestorName
Class name `TypedProperties` is the same as one of its superclass' names
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/TypedProperties.java`
#### Snippet
```java
 */
@Deprecated
public class TypedProperties extends org.apache.activemq.artemis.utils.collections.TypedProperties {

   public TypedProperties() {
```

### ClassNameSameAsAncestorName
Class name `OutputStream` is the same as one of its superclass' names
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
    * @since 1.3
    */
   public static class OutputStream extends java.io.FilterOutputStream {

      private final boolean encode;
```

### ClassNameSameAsAncestorName
Class name `InputStream` is the same as one of its superclass' names
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
    * @since 1.3
    */
   public static class InputStream extends java.io.FilterInputStream {

      private final boolean encode; // Encoding or decoding
```

### ClassNameSameAsAncestorName
Class name `Iterator` is the same as one of its superclass' names
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/LinkedListImpl.java`
#### Snippet
```java
   }

   public class Iterator implements LinkedListIterator<E> {
      Node<E> last;

```

### ClassNameSameAsAncestorName
Class name `SerializableObjectRefAddr` is the same as one of its superclass' names
in `artemis-ra/src/main/java/org/apache/activemq/artemis/jms/referenceable/SerializableObjectRefAddr.java`
#### Snippet
```java
 * Should be removed on next major version change.
 */
public class SerializableObjectRefAddr extends org.apache.activemq.artemis.ra.referenceable.SerializableObjectRefAddr {

   public SerializableObjectRefAddr(final String type, final Object content) throws NamingException {
```

### ClassNameSameAsAncestorName
Class name `Builder` is the same as one of its superclass' names
in `artemis-unit-test-support/src/main/java/org/apache/activemq/artemis/logs/AssertionLoggerHandler.java`
#### Snippet
```java


   public static class Builder<B extends Builder<B>> extends AbstractAppender.Builder<B> implements org.apache.logging.log4j.core.util.Builder<AssertionLoggerHandler> {
      @Override
      public AssertionLoggerHandler build() {
```

### ClassNameSameAsAncestorName
Class name `Factory` is the same as one of its superclass' names
in `artemis-jdbc-store/src/main/java/org/apache/activemq/artemis/jdbc/store/sql/PropertySQLProvider.java`
#### Snippet
```java
   }

   public static final class Factory implements SQLProvider.Factory {

      private static final String SQL_PROPERTIES_FILE = "journal-sql.properties";
```

### ClassNameSameAsAncestorName
Class name `DefaultHandler` is the same as one of its superclass' names
in `artemis-web/src/main/java/org/apache/activemq/artemis/component/DefaultHandler.java`
#### Snippet
```java
import org.eclipse.jetty.server.Request;

public class DefaultHandler extends org.eclipse.jetty.server.handler.DefaultHandler {
   private String rootRedirectLocation;

```

### ClassNameSameAsAncestorName
Class name `Transformer` is the same as one of its superclass' names
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/Transformer.java`
#### Snippet
```java
 */
@Deprecated
public interface Transformer extends org.apache.activemq.artemis.core.server.transformer.Transformer {

   @Override
```

### ClassNameSameAsAncestorName
Class name `CompositeDataConstants` is the same as one of its superclass' names
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/openmbean/CompositeDataConstants.java`
#### Snippet
```java
 */
@Deprecated
public interface CompositeDataConstants extends org.apache.activemq.artemis.core.message.openmbean.CompositeDataConstants {

}
```

## RuleId[id=RedundantMethodOverride]
### RedundantMethodOverride
Method `parseHeaders()` is identical to its super method
in `artemis-protocols/artemis-stomp-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/stomp/v12/StompFrameHandlerV12.java`
#### Snippet
```java

      @Override
      protected boolean parseHeaders() throws ActiveMQStompException {
      outer:
         while (true) {
```

### RedundantMethodOverride
Method `init()` is identical to its super method
in `artemis-protocols/artemis-stomp-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/stomp/v12/StompFrameHandlerV12.java`
#### Snippet
```java

      @Override
      public void init(StompDecoder decoder) {
         this.data = decoder.data;
         this.workingBuffer = decoder.workingBuffer;
```

### RedundantMethodOverride
Method `asJsonArray()` only delegates to its super method
in `artemis-commons/src/main/java/org/apache/activemq/artemis/json/impl/JsonValueImpl.java`
#### Snippet
```java

   @Override
   public JsonArray asJsonArray() {
      return JsonValue.super.asJsonArray();
   }
```

### RedundantMethodOverride
Method `asJsonObject()` only delegates to its super method
in `artemis-commons/src/main/java/org/apache/activemq/artemis/json/impl/JsonValueImpl.java`
#### Snippet
```java

   @Override
   public JsonObject asJsonObject() {
      return JsonValue.super.asJsonObject();
   }
```

### RedundantMethodOverride
Method `onReadAddRecordTX()` is identical to its super method
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalReaderCallbackAbstract.java`
#### Snippet
```java

   @Override
   public void onReadAddRecordTX(final long transactionID, final RecordInfo recordInfo) throws Exception {
   }

```

### RedundantMethodOverride
Method `markAsDataFile()` is identical to its super method
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalReaderCallbackAbstract.java`
#### Snippet
```java

   @Override
   public void markAsDataFile(final JournalFile file) {
   }

```

### RedundantMethodOverride
Method `onReadRollbackRecord()` is identical to its super method
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalReaderCallbackAbstract.java`
#### Snippet
```java

   @Override
   public void onReadRollbackRecord(final long transactionID) throws Exception {
   }

```

### RedundantMethodOverride
Method `onReadAddRecord()` is identical to its super method
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalReaderCallbackAbstract.java`
#### Snippet
```java

   @Override
   public void onReadAddRecord(final RecordInfo info) throws Exception {
   }

```

### RedundantMethodOverride
Method `onReadUpdateRecordTX()` is identical to its super method
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalReaderCallbackAbstract.java`
#### Snippet
```java

   @Override
   public void onReadUpdateRecordTX(final long transactionID, final RecordInfo recordInfo) throws Exception {
   }

```

### RedundantMethodOverride
Method `onReadUpdateRecord()` is identical to its super method
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalReaderCallbackAbstract.java`
#### Snippet
```java

   @Override
   public void onReadUpdateRecord(final RecordInfo recordInfo) throws Exception {
   }

```

### RedundantMethodOverride
Method `onReadDeleteRecord()` is identical to its super method
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalReaderCallbackAbstract.java`
#### Snippet
```java

   @Override
   public void onReadDeleteRecord(final long recordID) throws Exception {
   }

```

### RedundantMethodOverride
Method `onReadPrepareRecord()` is identical to its super method
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalReaderCallbackAbstract.java`
#### Snippet
```java

   @Override
   public void onReadPrepareRecord(final long transactionID,
                                   final byte[] extraData,
                                   final int numberOfRecords) throws Exception {
```

### RedundantMethodOverride
Method `onReadDeleteRecordTX()` is identical to its super method
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalReaderCallbackAbstract.java`
#### Snippet
```java

   @Override
   public void onReadDeleteRecordTX(final long transactionID, final RecordInfo recordInfo) throws Exception {
   }

```

### RedundantMethodOverride
Method `onReadCommitRecord()` is identical to its super method
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalReaderCallbackAbstract.java`
#### Snippet
```java

   @Override
   public void onReadCommitRecord(final long transactionID, final int numberOfRecords) throws Exception {
   }

```

### RedundantMethodOverride
Method `markAsDataFile()` is identical to its super method
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalCompactor.java`
#### Snippet
```java

   @Override
   public void markAsDataFile(final JournalFile file) {
      // nothing to be done here
   }
```

### RedundantMethodOverride
Method `checkOptions()` is identical to its super method
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Mask.java`
#### Snippet
```java

   @Override
   public void checkOptions(String[] options) throws InvalidOptionsError {
      OptionsUtil.checkCommandOptions(this.getClass(), options);
   }
```

### RedundantMethodOverride
Method `onReadPrepareRecord()` is identical to its super method
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/RecoverMessages.java`
#### Snippet
```java

            @Override
            public void onReadPrepareRecord(long transactionID,
                                            byte[] extraData,
                                            int numberOfRecords) throws Exception {
```

### RedundantMethodOverride
Method `onReadDeleteRecordTX()` is identical to its super method
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/RecoverMessages.java`
#### Snippet
```java

            @Override
            public void onReadDeleteRecordTX(long transactionID, RecordInfo recordInfo) throws Exception {

            }
```

### RedundantMethodOverride
Method `markAsDataFile()` is identical to its super method
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/RecoverMessages.java`
#### Snippet
```java

            @Override
            public void markAsDataFile(JournalFile file) {

            }
```

### RedundantMethodOverride
Method `onReadRollbackRecord()` is identical to its super method
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/RecoverMessages.java`
#### Snippet
```java

            @Override
            public void onReadRollbackRecord(long transactionID) throws Exception {

            }
```

### RedundantMethodOverride
Method `onReadDeleteRecord()` is identical to its super method
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/RecoverMessages.java`
#### Snippet
```java

            @Override
            public void onReadDeleteRecord(long recordID) throws Exception {

            }
```

### RedundantMethodOverride
Method `onReadCommitRecord()` is identical to its super method
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/RecoverMessages.java`
#### Snippet
```java

            @Override
            public void onReadCommitRecord(long transactionID, int numberOfRecords) throws Exception {

            }
```

### RedundantMethodOverride
Method `markAsDataFile()` is identical to its super method
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/journal/EncodeJournal.java`
#### Snippet
```java

         @Override
         public void markAsDataFile(final JournalFile file) {
         }
      });
```

### RedundantMethodOverride
Method `isAutoCreateQueue()` is identical to its super method
in `artemis-junit/artemis-junit-commons/src/main/java/org/apache/activemq/artemis/junit/ActiveMQConsumerDelegate.java`
#### Snippet
```java

   @Override
   public boolean isAutoCreateQueue() {
      return autoCreateQueue;
   }
```

### RedundantMethodOverride
Method `setAutoCreateQueue()` is identical to its super method
in `artemis-junit/artemis-junit-commons/src/main/java/org/apache/activemq/artemis/junit/ActiveMQConsumerDelegate.java`
#### Snippet
```java

   @Override
   public void setAutoCreateQueue(boolean autoCreateQueue) {
      this.autoCreateQueue = autoCreateQueue;
   }
```

### RedundantMethodOverride
Method `getFileSize()` is identical to its super method
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalImpl.java`
#### Snippet
```java

   @Override
   public int getFileSize() {
      return fileSize;
   }
```

### RedundantMethodOverride
Method `hashCode()` is identical to its super method
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQTemporaryQueue.java`
#### Snippet
```java

   @Override
   public int hashCode() {
      return super.getAddress().hashCode();
   }
```

### RedundantMethodOverride
Method `hashCode()` is identical to its super method
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQTemporaryTopic.java`
#### Snippet
```java

   @Override
   public int hashCode() {
      return super.getAddress().hashCode();
   }
```

### RedundantMethodOverride
Method `addChannelHandlers()` is identical to its super method
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/client/ProtonClientProtocolManager.java`
#### Snippet
```java

   @Override
   public void addChannelHandlers(ChannelPipeline pipeline) {
   }

```

### RedundantMethodOverride
Method `commit()` only delegates to its super method
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/transaction/ProtonTransactionImpl.java`
#### Snippet
```java

   @Override
   public void commit() throws Exception {
      super.commit();
   }
```

### RedundantMethodOverride
Method `rollback()` only delegates to its super method
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/transaction/ProtonTransactionImpl.java`
#### Snippet
```java

   @Override
   public void rollback() throws Exception {
      super.rollback();
   }
```

### RedundantMethodOverride
Method `decode()` only delegates to its super method
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/converter/coreWrapper/CoreBytesMessageWrapper.java`
#### Snippet
```java

   @Override
   public void decode()  {
      super.decode();

```

### RedundantMethodOverride
Method `decode()` only delegates to its super method
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/converter/coreWrapper/CoreStreamMessageWrapper.java`
#### Snippet
```java

   @Override
   public void decode() {
      super.decode();
   }
```

### RedundantMethodOverride
Method `markAsDataFile()` is identical to its super method
in `artemis-jdbc-store/src/main/java/org/apache/activemq/artemis/jdbc/store/journal/JDBCJournalReaderCallback.java`
#### Snippet
```java

   @Override
   public void markAsDataFile(JournalFile file) {
      // Not needed for JDBC journal impl
   }
```

### RedundantMethodOverride
Method `isLargeMessage()` is identical to its super method
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ClientMessageImpl.java`
#### Snippet
```java
    */
   @Override
   public boolean isLargeMessage() {
      return false;
   }
```

### RedundantMethodOverride
Method `isLargeMessage()` is identical to its super method
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/message/impl/CoreMessage.java`
#### Snippet
```java

   @Override
   public boolean isLargeMessage() {
      return false;
   }
```

### RedundantMethodOverride
Method `isResponse()` is identical to its super method
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/ActiveMQExceptionMessage_V2.java`
#### Snippet
```java

   @Override
   public boolean isResponse() {
      return true;
   }
```

### RedundantMethodOverride
Method `extractFrame()` only delegates to its super method
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/ActiveMQAMQPFrameDecoder.java`
#### Snippet
```java

   @Override
   protected ByteBuf extractFrame(ChannelHandlerContext ctx, ByteBuf buffer, int index, int length) {
      return super.extractFrame(ctx, buffer, index, length);
   }
```

### RedundantMethodOverride
Method `getRelatedMessageReferences()` is identical to its super method
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/cursor/impl/PageSubscriptionImpl.java`
#### Snippet
```java

      @Override
      public List<MessageReference> getRelatedMessageReferences() {
         return Collections.emptyList();
      }
```

### RedundantMethodOverride
Method `preStorageClose()` is identical to its super method
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ReplicationBackupActivation.java`
#### Snippet
```java

   @Override
   public void preStorageClose() throws Exception {
      // TODO replication endpoint close?
   }
```

### RedundantMethodOverride
Method `getVoteMap()` is identical to its super method
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/qourum/ServerConnectVote.java`
#### Snippet
```java

   @Override
   public Map<String, Object> getVoteMap() {
      return null;
   }
```

### RedundantMethodOverride
Method `refDown()` is identical to its super method
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/transformer/ServerMessageImpl.java`
#### Snippet
```java

   @Override
   public int refDown() {
      throw new UnsupportedOperationException();
   }
```

### RedundantMethodOverride
Method `durableUp()` is identical to its super method
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/transformer/ServerMessageImpl.java`
#### Snippet
```java

   @Override
   public int durableUp() {
      throw new UnsupportedOperationException();
   }
```

### RedundantMethodOverride
Method `refUp()` is identical to its super method
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/transformer/ServerMessageImpl.java`
#### Snippet
```java

   @Override
   public int refUp() {
      throw new UnsupportedOperationException();
   }
```

### RedundantMethodOverride
Method `durableDown()` is identical to its super method
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/transformer/ServerMessageImpl.java`
#### Snippet
```java

   @Override
   public int durableDown() {
      throw new UnsupportedOperationException();
   }
```

### RedundantMethodOverride
Method `markAsDataFile()` is identical to its super method
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/DescribeJournal.java`
#### Snippet
```java

            @Override
            public void markAsDataFile(final JournalFile file1) {
            }

```

### RedundantMethodOverride
Method `getRelatedMessageReferences()` is identical to its super method
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/postoffice/impl/PostOfficeImpl.java`
#### Snippet
```java

      @Override
      public List<MessageReference> getRelatedMessageReferences() {
         return Collections.emptyList();
      }
```

## RuleId[id=IntegerMultiplicationImplicitCastToLong]
### IntegerMultiplicationImplicitCastToLong
sessionExpiryInterval \* 1000: integer multiplication implicitly cast to long
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTProtocolManager.java`
#### Snippet
```java
         logger.debug("Inspecting session: {}", state);
         int sessionExpiryInterval = getSessionExpiryInterval(state);
         if (!state.isAttached() && sessionExpiryInterval > 0 && state.getDisconnectedTime() + (sessionExpiryInterval * 1000) < System.currentTimeMillis()) {
            toRemove.add(entry.getKey());
         }
```

### IntegerMultiplicationImplicitCastToLong
pageSize \* 2: integer multiplication implicitly cast to long
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/impl/PagingStoreImpl.java`
#### Snippet
```java
      if (maxSize <= 0) {
         // if maxSize <= 0, we will return 2 pages for de-page purposes
         return pageSize * 2;
      } else {
         return maxSize;
```

### IntegerMultiplicationImplicitCastToLong
page \* pageSize: integer multiplication implicitly cast to long
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/QueueControlImpl.java`
#### Snippet
```java
            long index = 0;
            long start = (long) (page - 1) * pageSize;
            long end = Math.min(page * pageSize, queue.getMessageCount());

            ArrayList<CompositeData> c = new ArrayList<>();
```

## RuleId[id=ListRemoveInLoop]
### ListRemoveInLoop
Can be replaced with 'List.subList().clear()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/messagecounter/MessageCounter.java`
#### Snippet
```java
            int delta = dayCounters.size() - dayCounterMax;

            for (int i = 0; i < delta; i++) {
               // reduce array size to requested size by dropping
               // oldest day counters
```

## RuleId[id=NestedAssignment]
### NestedAssignment
Result of assignment expression used
in `artemis-protocols/artemis-stomp-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/stomp/StompSession.java`
#### Snippet
```java
   public void afterDelivery() throws Exception {
      PendingTask task;
      while ((task = afterDeliveryTasks.poll()) != null) {
         task.run();
      }
```

### NestedAssignment
Result of assignment expression used
in `artemis-commons/src/main/java/org/apache/activemq/artemis/core/buffers/impl/ChannelBufferWrapper.java`
#### Snippet
```java
   public static ByteBuf unwrap(ByteBuf buffer) {
      ByteBuf parent;
      while ((parent = buffer.unwrap()) != null && parent != buffer) { // this last part is just in case the semantic
         // ever changes where unwrap is returning itself
         buffer = parent;
```

### NestedAssignment
Result of assignment expression used
in `artemis-commons/src/main/java/org/apache/activemq/artemis/core/server/NetworkHealthCheck.java`
#### Snippet
```java

      String inputLine;
      while ((inputLine = reader.readLine()) != null) {
         if (error) {
            ActiveMQUtilLogger.LOGGER.failedToReadFromStream(inputLine);
```

### NestedAssignment
Result of assignment expression used
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/SpawnedVMSupport.java`
#### Snippet
```java
            BufferedReader br = new BufferedReader(isr);
            String line = null;
            while ((line = br.readLine()) != null) {
               if (wordMatch != null && wordRunner != null) {
                  if (line.contains(wordMatch)) {
```

### NestedAssignment
Result of assignment expression used
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/actors/ProcessorBase.java`
#### Snippet
```java
               //while the queue is not empty we process in order:
               //if requestedForcedShutdown==true than no new tasks will be drained from the tasks q.
               while (!yielded && !requestedForcedShutdown && (task = tasks.poll()) != null) {
                  doTask(task);
               }
```

### NestedAssignment
Result of assignment expression used
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/actors/ProcessorBase.java`
#### Snippet
```java

      T item;
      while ((item = tasks.poll()) != null) {
         onPendingItem.accept(item);
         pendingItems++;
```

### NestedAssignment
Result of assignment expression used
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
         byte[] buffer = new byte[65536]; // 64K
         int read = -1;
         while ((read = in.read(buffer)) >= 0) {
            out.write(buffer, 0, read);
         } // end while: through file
```

### NestedAssignment
Result of assignment expression used
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java

         // Read until done
         while ((numBytes = bis.read(buffer, length, 4096)) >= 0) {
            length += numBytes;
         }
```

### NestedAssignment
Result of assignment expression used
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
               gzis = new java.util.zip.GZIPInputStream(bais);

               while ((length = gzis.read(buffer)) >= 0) {
                  baos.write(buffer, 0, length);
               } // end while: reading input
```

### NestedAssignment
Result of assignment expression used
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java

         // Read until done
         while ((numBytes = bis.read(buffer, length, 4096)) >= 0) {
            length += numBytes;
         }
```

### NestedAssignment
Result of assignment expression used
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
         byte[] buffer = new byte[65536]; // 64K
         int read = -1;
         while ((read = in.read(buffer)) >= 0) {
            out.write(buffer, 0, read);
         } // end while: through file
```

### NestedAssignment
Result of assignment expression used
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/RepeatableIteratorWrapper.java`
#### Snippet
```java
         return last;
      }
      return last = iterator.next();
   }

```

### NestedAssignment
Result of assignment expression used
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/PriorityLinkedListImpl.java`
#### Snippet
```java

            if (lastIter == null) {
               lastIter = cachedIters[index] = levels[index].iterator();
            }

```

### NestedAssignment
Result of assignment expression used
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/ConcurrentAppendOnlyChunkedList.java`
#### Snippet
```java
   private static <E> E pollElement(AtomicChunk<E> buffer, int i) {
      E e;
      while ((e = buffer.get(i)) == null) {
      }
      return e;
```

### NestedAssignment
Result of assignment expression used
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/uri/URISupport.java`
#### Snippet
```java

         int i = 0;
         while ((i = str.indexOf('(', i)) >= 0) {
            i++;
            open++;
```

### NestedAssignment
Result of assignment expression used
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/uri/URISupport.java`
#### Snippet
```java
         }
         i = 0;
         while ((i = str.indexOf(')', i)) >= 0) {
            i++;
            closed++;
```

### NestedAssignment
Result of assignment expression used
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/io/aio/AIOSequentialFileFactory.java`
#### Snippet
```java
         ByteBuffer reusedBuffer;

         while ((reusedBuffer = reuseBuffersQueue.poll()) != null) {
            releaseBuffer(reusedBuffer);
         }
```

### NestedAssignment
Result of assignment expression used
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/TypedProperties.java`
#### Snippet
```java
      // it's a trick to not pay the cost of buffer.writeIndex without assertions enabled
      int writerIndex = 0;
      assert (writerIndex = buffer.writerIndex()) >= 0 : "Always true";
      if (properties == null || size == 0) {
         encodedSize = DataConstants.SIZE_BYTE;
```

### NestedAssignment
Result of assignment expression used
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/TypedProperties.java`
#### Snippet
```java
         ByteValue value = VALUES[index];
         if (value == null) {
            return VALUES[index] = new ByteValue(b);
         } else {
            return value;
```

### NestedAssignment
Result of assignment expression used
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/LinkedListImpl.java`
#### Snippet
```java
      //Help GC - otherwise GC potentially has to traverse a very long list to see if elements are reachable, this can result in OOM
      //https://jira.jboss.org/browse/HORNETQ-469
      toRemove.next = toRemove.prev = null;
   }

```

### NestedAssignment
Result of assignment expression used
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/process/ProcessBuilder.java`
#### Snippet
```java
            BufferedReader br = new BufferedReader(isr);
            String line;
            while ((line = br.readLine()) != null) {
               if (print) {
                  if (sendToErr) {
```

### NestedAssignment
Result of assignment expression used
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/journal/DecodeJournal.java`
#### Snippet
```java
      long lineNumber = 0;

      while ((line = buffReader.readLine()) != null) {
         lineNumber++;
         String[] splitLine = line.split(",");
```

### NestedAssignment
Result of assignment expression used
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/ProducerThread.java`
#### Snippet
```java
         int count;
         StringBuilder builder = new StringBuilder();
         while ((count = reader.read(buffer)) != -1) {
            builder.append(buffer, 0, count);
            if (size > 0)
```

### NestedAssignment
Result of assignment expression used
in `artemis-cli/src/main/java/org/apache/activemq/artemis/util/ServerUtil.java`
#### Snippet
```java
            BufferedReader br = new BufferedReader(isr);
            String line;
            while ((line = br.readLine()) != null) {
               if (print) {
                  if (sendToErr) {
```

### NestedAssignment
Result of assignment expression used
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalImpl.java`
#### Snippet
```java
            } finally {
               ByteBuffer wholeFileBuffer;
               if (wholeFileBufferRef != null && (wholeFileBuffer = wholeFileBufferRef.get()) != null) {
                  fileFactory.releaseDirectBuffer(wholeFileBuffer);
               }
```

### NestedAssignment
Result of assignment expression used
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalImpl.java`
#### Snippet
```java
                                                    final AtomicReference<ByteBuffer> wholeFileBufferRef) throws Exception {
      JournalState state;
      assert (state = this.state) != JournalState.STOPPED &&
         state != JournalState.LOADED &&
         state != replicationSync;
```

### NestedAssignment
Result of assignment expression used
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/handler/ProtonHandler.java`
#### Snippet
```java
            }
         }
         while ((ev = collector.peek()) != null) {
            for (EventHandler h : handlers) {
               logger.trace("Handling {} towards {}", ev, h);
```

### NestedAssignment
Result of assignment expression used
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
         } else {
            Object amqpValue;
            if (m.getBody() instanceof AmqpValue && (amqpValue = ((AmqpValue) m.getBody()).getValue()) != null) {
               rc.put(CompositeDataConstants.TEXT_BODY, JsonUtil.truncateString(String.valueOf(amqpValue), valueSizeLimit));
            } else {
```

### NestedAssignment
Result of assignment expression used
in `artemis-jdbc-store/src/main/java/org/apache/activemq/artemis/jdbc/store/drivers/AbstractJDBCDriver.java`
#### Snippet
```java
                  logger.trace("Validation of the existing table {} initialization is started", tableName);
                  int rows;
                  if (cntRs.next() && (rows = cntRs.getInt(1)) > 0) {
                     logger.trace("Table {} did exist but is not empty. Skipping initialization. Found {} rows.", tableName, rows);
                     if (logger.isDebugEnabled()) {
```

### NestedAssignment
Result of assignment expression used
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ClientProducerImpl.java`
#### Snippet
```java
      if (msgI.isServerMessage()) {
         largeMessageSendServer(sendBlocking, msgI, credits, handler);
      } else if ((input = getBodyInputStream(msgI)) != null) {
         largeMessageSendStreamed(sendBlocking, msgI, input, credits, handler);
      } else {
```

### NestedAssignment
Result of assignment expression used
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/LargeMessageControllerImpl.java`
#### Snippet
```java
         int totalSize = 0;
         LargeData polledPacket = null;
         while ((polledPacket = largeMessageData.poll()) != null) {
            totalSize += polledPacket.getFlowControlSize();
         }
```

### NestedAssignment
Result of assignment expression used
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ClientConsumerImpl.java`
#### Snippet
```java

            synchronized (this) {
               while ((stopped || (m = buffer.poll()) == null) && !closed && toWait > 0) {
                  if (start == -1) {
                     start = System.currentTimeMillis();
```

### NestedAssignment
Result of assignment expression used
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/NettyConnection.java`
#### Snippet
```java
         }
         boolean canWrite;
         while (!(canWrite = channel.isWritable()) && (System.nanoTime() - deadline) < 0) {
            //periodically check the connection state
            checkConnectionState();
```

### NestedAssignment
Result of assignment expression used
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/utils/XMLUtil.java`
#### Snippet
```java

      if (children.getLength() == 0) {
         if ((textContent = XMLUtil.getTextContent(n)) != null && !"".equals(textContent)) {
            sb.append(textContent).append("</").append(name).append('>');
         } else {
```

### NestedAssignment
Result of assignment expression used
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/utils/XMLUtil.java`
#### Snippet
```java
         }

         if (!hasValidChildren && (textContent = XMLUtil.getTextContent(n)) != null) {
            sb.append(textContent);
         }
```

### NestedAssignment
Result of assignment expression used
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/utils/XMLUtil.java`
#### Snippet
```java
      StringBuilder buff = new StringBuilder();
      int c;
      while ((c = r.read()) != -1) {
         buff.append((char) c);
      }
```

### NestedAssignment
Result of assignment expression used
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/impl/PagingManagerImpl.java`
#### Snippet
```java
      Runnable runnable;

      while ((runnable = memoryCallback.poll()) != null) {
         runnable.run();
      }
```

### NestedAssignment
Result of assignment expression used
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/impl/PagingStoreImpl.java`
#### Snippet
```java
      Runnable runnable;

      while ((runnable = onMemoryFreedRunnables.poll()) != null) {
         runnable.run();
      }
```

### NestedAssignment
Result of assignment expression used
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/cursor/impl/PageSubscriptionImpl.java`
#### Snippet
```java
      public synchronized boolean hasNext() {
         NextResult status;
         while ((status = tryNext()) == NextResult.retry) {
         }
         return status == NextResult.hasElements ? true : false;
```

### NestedAssignment
Result of assignment expression used
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerConsumerImpl.java`
#### Snippet
```java
         final List<MessageReference> refs = new ArrayList<>(deliveringRefs.size());
         MessageReference ref;
         while ((ref = deliveringRefs.poll()) != null) {
            metrics.addAcknowledge(ref.getMessage().getEncodeSize(), tx);
            if (performACK) {
```

### NestedAssignment
Result of assignment expression used
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/protocol/websocket/WebSocketFrameEncoder.java`
#### Snippet
```java
      ctx.writeAndFlush(new BinaryWebSocketFrame(finalFragment, 0, fragment), promise);

      while ((count = byteBuf.readableBytes()) > 0) {
         length = Math.min(count, maxFramePayloadLength);
         finalFragment = length == count;
```

### NestedAssignment
Result of assignment expression used
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/federation/address/FederatedAddress.java`
#### Snippet
```java
         synchronized (this) {
            final Set<SimpleString> matchingQueues;
            if ((matchingQueues = matchingDiverts.remove(binding)) != null) {
               try {
                  final AddressInfo addressInfo = server.getPostOffice().getAddressInfo(binding.getAddress());
```

### NestedAssignment
Result of assignment expression used
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/postoffice/impl/BindingsImpl.java`
#### Snippet
```java
            context.clear().setReusable(false);
            routeFromCluster(message, context, routeToIds);
         } else if (groupRouting && groupingHandler != null && (groupId = message.getGroupID()) != null) {
            context.clear().setReusable(false);
            routeUsingStrictOrdering(message, context, groupingHandler, groupId, 0);
```

### NestedAssignment
Result of assignment expression used
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/replication/ReplicationManager.java`
#### Snippet
```java
      assert checkEventLoop();
      ReplicatePacketRequest req;
      while ((req = requests.poll()) != null) {
         req.packet.release();
         req.context.replicationDone();
```

### NestedAssignment
Result of assignment expression used
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java
      MessageReference ref;

      while ((ref = intermediateMessageReferences.poll()) != null) {
         if (ref.skipDelivery()) {
            continue;
```

## RuleId[id=ReplaceAssignmentWithOperatorAssignment]
### ReplaceAssignmentWithOperatorAssignment
`data = data - pos` could be simplified to 'data -= pos'
in `artemis-protocols/artemis-stomp-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/stomp/StompDecoder.java`
#### Snippet
```java
         }

         data = data - pos;

         // reset
```

### ReplaceAssignmentWithOperatorAssignment
`data = data - pos` could be simplified to 'data -= pos'
in `artemis-protocols/artemis-stomp-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/stomp/v12/StompFrameHandlerV12.java`
#### Snippet
```java
            }

            data = data - pos;

            // reset
```

### ReplaceAssignmentWithOperatorAssignment
`data = data - pos` could be simplified to 'data -= pos'
in `artemis-protocols/artemis-stomp-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/stomp/v11/StompFrameHandlerV11.java`
#### Snippet
```java
            }

            data = data - pos;

            // reset
```

### ReplaceAssignmentWithOperatorAssignment
`data = data - offset` could be simplified to 'data -= offset'
in `artemis-protocols/artemis-stomp-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/stomp/v11/StompFrameHandlerV11.java`
#### Snippet
```java
         if (offset > 0) {
            System.arraycopy(workingBuffer, offset, workingBuffer, 0, data - offset);
            data = data - offset;
            offset = 0;
         }
```

### ReplaceAssignmentWithOperatorAssignment
`prefix = prefix + SEPARATOR` could be simplified to 'prefix += SEPARATOR'
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jndi/ReadOnlyContext.java`
#### Snippet
```java
         String prefix = getNameInNamespace();
         if (prefix.length() > 0) {
            prefix = prefix + SEPARATOR;
         }
         result = new ReadOnlyContext((ReadOnlyContext) result, environment, prefix + name);
```

### ReplaceAssignmentWithOperatorAssignment
`endOfBodyPosition = endOfBodyPosition - DataConstants.SIZE_INT` could be simplified to 'endOfBodyPosition -= DataConstants.SIZE_INT'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/message/impl/CoreMessage.java`
#### Snippet
```java

      if (beforeAddress) {
         endOfBodyPosition = endOfBodyPosition - DataConstants.SIZE_INT;
      }

```

### ReplaceAssignmentWithOperatorAssignment
`maxBytesToSend = maxBytesToSend - bytesRead` could be simplified to 'maxBytesToSend -= bytesRead'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/replication/ReplicationManager.java`
#### Snippet
```java
                     maxBytesToSend = 0;
                  } else {
                     maxBytesToSend = maxBytesToSend - bytesRead;
                  }
               }
```

## RuleId[id=SimplifiableBooleanExpression]
### SimplifiableBooleanExpression
`(lock != null && !lock.isValid()) || lock == null` can be simplified to 'lock == null\|\|!lock.isValid()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/FileLockNodeManager.java`
#### Snippet
```java
   private boolean isLiveLockLost() {
      final FileLock lock = this.liveLock;
      return (lock != null && !lock.isValid()) || lock == null;
   }

```

## RuleId[id=ReturnFromFinallyBlock]
### ReturnFromFinallyBlock
'return' inside 'finally' block
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/actors/ProcessorBase.java`
#### Snippet
```java
               //If a shutdown has happened there is no need to continue polling tasks
               if (!stateUpdater.compareAndSet(this, STATE_RUNNING, STATE_NOT_RUNNING)) {
                  return;
               }
            }
```

### ReturnFromFinallyBlock
'return' inside 'finally' block
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/jdbc/JdbcNodeManager.java`
#### Snippet
```java
         // the node manager was already stopped
         if (!isStarted()) {
            return;
         }
         try {
```

## RuleId[id=MismatchedCollectionQueryUpdate]
### MismatchedCollectionQueryUpdate
Contents of collection `systemProperties` are queried, but never updated
in `artemis-maven-plugin/src/main/java/org/apache/activemq/artemis/maven/ArtemisClientPlugin.java`
#### Snippet
```java
    * @parameter
    */
   private Properties systemProperties;

   @Override
```

### MismatchedCollectionQueryUpdate
Contents of collection `allProducers` are updated, but never queried
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/perf/ProducerBenchmark.java`
#### Snippet
```java
      // create producers/sessions/senders/load generators
      this.generators = new ProducerLoadGenerator[totalProducers];
      final ArrayList<AsyncJms2ProducerFacade> allProducers = new ArrayList<>(totalProducers);
      int producerSequence = 0;
      final int messageCountPerProducer = (int) (messageCount / totalProducers);
```

### MismatchedCollectionQueryUpdate
Contents of collection `serversSslContextCache` are updated, but never queried
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/impl/ssl/CachingOpenSSLContextFactory.java`
#### Snippet
```java

   private final ConcurrentMap<SSLContextConfig, SslContext> clientSslContextCache = new ConcurrentHashMap<>(2);
   private final ConcurrentMap<SSLContextConfig, SslContext> serversSslContextCache = new ConcurrentHashMap<>(2);

   @Override
```

## RuleId[id=FieldAccessedSynchronizedAndUnsynchronized]
### FieldAccessedSynchronizedAndUnsynchronized
Field `transactionManager` is accessed in both synchronized and unsynchronized contexts
in `artemis-service-extensions/src/main/java/org/apache/activemq/artemis/service/extensions/ServiceUtils.java`
#### Snippet
```java
   private static ActiveMQXAResourceWrapperFactory activeMQXAResourceWrapperFactory;

   private static TransactionManager transactionManager;

   private static boolean transactionManagerLoaded = false;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `transactionManagerLoaded` is accessed in both synchronized and unsynchronized contexts
in `artemis-service-extensions/src/main/java/org/apache/activemq/artemis/service/extensions/ServiceUtils.java`
#### Snippet
```java
   private static TransactionManager transactionManager;

   private static boolean transactionManagerLoaded = false;

   private static ActiveMQXAResourceWrapperFactory getActiveMQXAResourceWrapperFactory() {
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `serverLocator` is accessed in both synchronized and unsynchronized contexts
in `artemis-service-extensions/src/main/java/org/apache/activemq/artemis/service/extensions/xa/recovery/ActiveMQXAResourceWrapper.java`
#### Snippet
```java
   private static final Object lock = new Object();

   private ServerLocator serverLocator;

   private ClientSessionFactory csf;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `csf` is accessed in both synchronized and unsynchronized contexts
in `artemis-service-extensions/src/main/java/org/apache/activemq/artemis/service/extensions/xa/recovery/ActiveMQXAResourceWrapper.java`
#### Snippet
```java
   private ServerLocator serverLocator;

   private ClientSessionFactory csf;

   private ClientSession delegate;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `delegate` is accessed in both synchronized and unsynchronized contexts
in `artemis-service-extensions/src/main/java/org/apache/activemq/artemis/service/extensions/xa/recovery/ActiveMQXAResourceWrapper.java`
#### Snippet
```java
   private ClientSessionFactory csf;

   private ClientSession delegate;

   private XARecoveryConfig[] xaRecoveryConfigs;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `workingBuffer` is accessed in both synchronized and unsynchronized contexts
in `artemis-protocols/artemis-stomp-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/stomp/StompDecoder.java`
#### Snippet
```java
   public static final byte TAB = (byte) '\t';

   public byte[] workingBuffer = new byte[1024];

   public int pos;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `data` is accessed in both synchronized and unsynchronized contexts
in `artemis-protocols/artemis-stomp-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/stomp/StompDecoder.java`
#### Snippet
```java
   public int pos;

   public int data;

   public String command;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `command` is accessed in both synchronized and unsynchronized contexts
in `artemis-protocols/artemis-stomp-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/stomp/StompDecoder.java`
#### Snippet
```java
   public int data;

   public String command;

   public Map<String, String> headers;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `readingHeaders` is accessed in both synchronized and unsynchronized contexts
in `artemis-protocols/artemis-stomp-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/stomp/StompDecoder.java`
#### Snippet
```java
   private int headerBytesCopyStart;

   public boolean readingHeaders;

   public boolean headerValueWhitespace;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `frameHandler` is accessed in both synchronized and unsynchronized contexts
in `artemis-protocols/artemis-stomp-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/stomp/StompConnection.java`
#### Snippet
```java
   private StompVersions version;

   private VersionedStompFrameHandler frameHandler;

   //this means the version negotiation done.
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `period` is accessed in both synchronized and unsynchronized contexts
in `artemis-commons/src/main/java/org/apache/activemq/artemis/core/server/ActiveMQScheduledComponent.java`
#### Snippet
```java
   /** initialDelay < 0 would mean no initial delay, use the period instead */
   private long initialDelay;
   private long period;
   private TimeUnit timeUnit;
   protected final Executor executor;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `initialDelay` is accessed in both synchronized and unsynchronized contexts
in `artemis-commons/src/main/java/org/apache/activemq/artemis/core/server/ActiveMQScheduledComponent.java`
#### Snippet
```java

   /** initialDelay < 0 would mean no initial delay, use the period instead */
   private long initialDelay;
   private long period;
   private TimeUnit timeUnit;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `bookedForRunning` is accessed in both synchronized and unsynchronized contexts
in `artemis-commons/src/main/java/org/apache/activemq/artemis/core/server/ActiveMQScheduledComponent.java`
#### Snippet
```java
   private final boolean onDemand;
   // The start/stop actions shouldn't interact concurrently with delay so it doesn't need to be volatile
   private AtomicBoolean bookedForRunning;

   /**
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `timeUnit` is accessed in both synchronized and unsynchronized contexts
in `artemis-commons/src/main/java/org/apache/activemq/artemis/core/server/ActiveMQScheduledComponent.java`
#### Snippet
```java
   private long initialDelay;
   private long period;
   private TimeUnit timeUnit;
   protected final Executor executor;
   private volatile boolean isStarted;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `scheduledExecutorService` is accessed in both synchronized and unsynchronized contexts
in `artemis-commons/src/main/java/org/apache/activemq/artemis/core/server/ActiveMQScheduledComponent.java`
#### Snippet
```java

   private static final Logger logger = LoggerFactory.getLogger(MethodHandles.lookup().lookupClass());
   protected ScheduledExecutorService scheduledExecutorService;
   private boolean startedOwnScheduler;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `value` is accessed in both synchronized and unsynchronized contexts
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Suppliers.java`
#### Snippet
```java
      // "value" does not need to be volatile; visibility piggy-backs
      // on volatile read of "initialized".
      transient T value;

      MemoizingSupplier(Supplier<T> delegate) {
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `aioFile` is accessed in both synchronized and unsynchronized contexts
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/io/aio/AIOSequentialFile.java`
#### Snippet
```java
   private boolean opened = false;

   private LibaioFile aioFile;

   private final AIOSequentialFileFactory aioFactory;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `opened` is accessed in both synchronized and unsynchronized contexts
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/io/aio/AIOSequentialFile.java`
#### Snippet
```java
   private static final Logger logger = LoggerFactory.getLogger(MethodHandles.lookup().lookupClass());

   private boolean opened = false;

   private LibaioFile aioFile;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `rfile` is accessed in both synchronized and unsynchronized contexts
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/io/nio/NIOSequentialFile.java`
#### Snippet
```java
   private FileChannel channel;

   private RandomAccessFile rfile;

   private final int maxIO;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `channel` is accessed in both synchronized and unsynchronized contexts
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/io/nio/NIOSequentialFile.java`
#### Snippet
```java
   private static final int CHUNK_SIZE = 2 * 1024 * 1024;

   private FileChannel channel;

   private RandomAccessFile rfile;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `bufferObserver` is accessed in both synchronized and unsynchronized contexts
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/io/buffer/TimedBuffer.java`
#### Snippet
```java
   private final AtomicLong bytesFlushed = new AtomicLong(0);
   private final AtomicLong flushesDone = new AtomicLong(0);
   private TimedBufferObserver bufferObserver;
   private CheckTimer timerRunnable;
   private int bufferLimit = 0;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `repeat` is accessed in both synchronized and unsynchronized contexts
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/LinkedListImpl.java`
#### Snippet
```java
      Node<E> current = head.next;

      boolean repeat;

      Iterator() {
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `last` is accessed in both synchronized and unsynchronized contexts
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/LinkedListImpl.java`
#### Snippet
```java

   public class Iterator implements LinkedListIterator<E> {
      Node<E> last;

      Node<E> current = head.next;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `current` is accessed in both synchronized and unsynchronized contexts
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/LinkedListImpl.java`
#### Snippet
```java
      Node<E> last;

      Node<E> current = head.next;

      boolean repeat;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `numIters` is accessed in both synchronized and unsynchronized contexts
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/LinkedListImpl.java`
#### Snippet
```java
   // We store in an array rather than a Map for the best performance
   private volatile Iterator[] iters;
   private int numIters;
   private int nextIndex;
   private NodeStore<E> nodeStore;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `nodeStore` is accessed in both synchronized and unsynchronized contexts
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/LinkedListImpl.java`
#### Snippet
```java
   private int numIters;
   private int nextIndex;
   private NodeStore<E> nodeStore;

   private volatile Node<E> lastAdd;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `passwordCodec` is accessed in both synchronized and unsynchronized contexts
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/ActiveMQRAProperties.java`
#### Snippet
```java
   private Boolean useMaskedPassword = null;

   private String passwordCodec;

   private boolean initialized = false;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `useMaskedPassword` is accessed in both synchronized and unsynchronized contexts
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/ActiveMQRAProperties.java`
#### Snippet
```java
   private boolean useJNDI;

   private Boolean useMaskedPassword = null;

   private String passwordCodec;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `password` is accessed in both synchronized and unsynchronized contexts
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/ActiveMQRAProperties.java`
#### Snippet
```java
    * The password
    */
   private String password = null;

   /**
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `userName` is accessed in both synchronized and unsynchronized contexts
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/ActiveMQRASessionFactoryImpl.java`
#### Snippet
```java
    * The user name
    */
   private String userName;

   /**
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `clientID` is accessed in both synchronized and unsynchronized contexts
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/ActiveMQRASessionFactoryImpl.java`
#### Snippet
```java
    * The client ID
    */
   private String clientID;

   /**
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `password` is accessed in both synchronized and unsynchronized contexts
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/ActiveMQRASessionFactoryImpl.java`
#### Snippet
```java
    * The password
    */
   private String password;

   /**
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `ra` is accessed in both synchronized and unsynchronized contexts
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/ActiveMQRAManagedConnectionFactory.java`
#### Snippet
```java
    * The resource adapter
    */
   private ActiveMQResourceAdapter ra;

   /**
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `recoveryConnectionFactory` is accessed in both synchronized and unsynchronized contexts
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/ActiveMQRAManagedConnectionFactory.java`
#### Snippet
```java
    * Connection Factory used if properties are set
    */
   private ActiveMQConnectionFactory recoveryConnectionFactory;

   /**
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `resourceRecovery` is accessed in both synchronized and unsynchronized contexts
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/ActiveMQRAManagedConnectionFactory.java`
#### Snippet
```java
    * The resource recovery if there is one
    */
   private XARecoveryConfig resourceRecovery;

   /**
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `defaultActiveMQConnectionFactory` is accessed in both synchronized and unsynchronized contexts
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/ActiveMQResourceAdapter.java`
#### Snippet
```java
   private final Map<ActivationSpec, ActiveMQActivation> activations;

   private ActiveMQConnectionFactory defaultActiveMQConnectionFactory;

   private ActiveMQConnectionFactory recoveryActiveMQConnectionFactory;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `factory` is accessed in both synchronized and unsynchronized contexts
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/inflow/ActiveMQActivation.java`
#### Snippet
```java
   private final List<ActiveMQMessageHandler> handlers = new ArrayList<>();

   private ActiveMQConnectionFactory factory;

   private final List<String> nodes = Collections.synchronizedList(new ArrayList<String>());
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `lastReceived` is accessed in both synchronized and unsynchronized contexts
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/inflow/ActiveMQActivation.java`
#### Snippet
```java
   private final Map<String, Long> removedNodes = new ConcurrentHashMap<>();

   private boolean lastReceived = false;

   private final Object teardownLock = new Object();
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `writer` is accessed in both synchronized and unsynchronized contexts
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/factory/serialize/XMLMessageSerializer.java`
#### Snippet
```java
public class XMLMessageSerializer implements MessageSerializer {

   private XMLMessageExporter writer;

   private XMLMessageImporter reader;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `reader` is accessed in both synchronized and unsynchronized contexts
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/factory/serialize/XMLMessageSerializer.java`
#### Snippet
```java
   private XMLMessageExporter writer;

   private XMLMessageImporter reader;

   private ClientSession clientSession;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `clientSession` is accessed in both synchronized and unsynchronized contexts
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/factory/serialize/XMLMessageSerializer.java`
#### Snippet
```java
   private XMLMessageImporter reader;

   private ClientSession clientSession;

   private OutputStream out;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `out` is accessed in both synchronized and unsynchronized contexts
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/factory/serialize/XMLMessageSerializer.java`
#### Snippet
```java
   private ClientSession clientSession;

   private OutputStream out;

   @Override
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `connections` is accessed in both synchronized and unsynchronized contexts
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/perf/ProducerBenchmark.java`
#### Snippet
```java
   private final boolean enableTimestamp;
   private final boolean enableMessageID;
   private Set<Connection> connections;
   private ProducerLoadGenerator[] generators;
   private boolean started;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `started` is accessed in both synchronized and unsynchronized contexts
in `artemis-cli/src/main/java/org/apache/activemq/artemis/integration/FileBroker.java`
#### Snippet
```java
   private final ActivateCallback activateCallback;

   private boolean started;

   private final ActiveMQSecurityManager securityManager;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `components` is accessed in both synchronized and unsynchronized contexts
in `artemis-cli/src/main/java/org/apache/activemq/artemis/integration/FileBroker.java`
#### Snippet
```java
   private final ActiveMQSecurityManager securityManager;

   private Map<String, ActiveMQComponent> components;

   public FileBroker(ServerDTO broker, ActiveMQSecurityManager security, ActivateCallback activateCallback) {
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `networkConnection` is accessed in both synchronized and unsynchronized contexts
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/amq/AMQConnectionContext.java`
#### Snippet
```java
   private Object longTermStoreContext;
   private boolean producerFlowControl = true;
   private boolean networkConnection;
   private boolean faultTolerant;
   private final AtomicBoolean stopping = new AtomicBoolean();
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `tm` is accessed in both synchronized and unsynchronized contexts
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/bridge/impl/JMSBridgeImpl.java`
#### Snippet
```java
   private String targetPassword;

   private TransactionManager tm;

   private String selector;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `maxRetries` is accessed in both synchronized and unsynchronized contexts
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/bridge/impl/JMSBridgeImpl.java`
#### Snippet
```java
   private long failureRetryInterval;

   private int maxRetries;

   private QualityOfServiceMode qualityOfServiceMode;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `targetSession` is accessed in both synchronized and unsynchronized contexts
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/bridge/impl/JMSBridgeImpl.java`
#### Snippet
```java
   private Session sourceSession;

   private Session targetSession;

   private MessageConsumer sourceConsumer;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `tx` is accessed in both synchronized and unsynchronized contexts
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/bridge/impl/JMSBridgeImpl.java`
#### Snippet
```java
   private boolean paused;

   private Transaction tx;

   private boolean failed;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `passwordCodec` is accessed in both synchronized and unsynchronized contexts
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/bridge/impl/JMSBridgeImpl.java`
#### Snippet
```java
   private Boolean useMaskedPassword;

   private String passwordCodec;

   private long failoverTimeout;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `sourceUsername` is accessed in both synchronized and unsynchronized contexts
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/bridge/impl/JMSBridgeImpl.java`
#### Snippet
```java
   private String bridgeName = "N/A";

   private String sourceUsername;

   private String sourcePassword;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `moduleTccl` is accessed in both synchronized and unsynchronized contexts
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/bridge/impl/JMSBridgeImpl.java`
#### Snippet
```java
   private ActiveMQRegistry registry;

   private ClassLoader moduleTccl;

   private long messageCount = 0;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `started` is accessed in both synchronized and unsynchronized contexts
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/bridge/impl/JMSBridgeImpl.java`
#### Snippet
```java
   private volatile boolean addMessageIDInHeader;

   private boolean started;

   private static final Object stoppingGuard = new Object();
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `sourceDestinationFactory` is accessed in both synchronized and unsynchronized contexts
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/bridge/impl/JMSBridgeImpl.java`
#### Snippet
```java
   private ConnectionFactoryFactory targetCff;

   private DestinationFactory sourceDestinationFactory;

   private DestinationFactory targetDestinationFactory;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `clientID` is accessed in both synchronized and unsynchronized contexts
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/bridge/impl/JMSBridgeImpl.java`
#### Snippet
```java
   private String subName;

   private String clientID;

   private volatile boolean addMessageIDInHeader;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `failoverTimeout` is accessed in both synchronized and unsynchronized contexts
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/bridge/impl/JMSBridgeImpl.java`
#### Snippet
```java
   private String passwordCodec;

   private long failoverTimeout;

   private static final int FORWARD_MODE_XA = 0;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `targetProducer` is accessed in both synchronized and unsynchronized contexts
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/bridge/impl/JMSBridgeImpl.java`
#### Snippet
```java
   private MessageConsumer sourceConsumer;

   private MessageProducer targetProducer;

   private CountDownLatch batchTimeCheckerFinished;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `sourcePassword` is accessed in both synchronized and unsynchronized contexts
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/bridge/impl/JMSBridgeImpl.java`
#### Snippet
```java
   private String sourceUsername;

   private String sourcePassword;

   private String targetUsername;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `abortedMessageCount` is accessed in both synchronized and unsynchronized contexts
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/bridge/impl/JMSBridgeImpl.java`
#### Snippet
```java
   private long messageCount = 0;

   private long abortedMessageCount = 0;

   /*
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `targetConn` is accessed in both synchronized and unsynchronized contexts
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/bridge/impl/JMSBridgeImpl.java`
#### Snippet
```java
   private Connection sourceConn;

   private Connection targetConn;

   private Destination sourceDestination;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `sourceReceiverFinished` is accessed in both synchronized and unsynchronized contexts
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/bridge/impl/JMSBridgeImpl.java`
#### Snippet
```java
   private Future<?> batchTimeCheckerTask;

   private CountDownLatch sourceReceiverFinished;

   private Future<?> sourceReceiverTask;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `connectedTarget` is accessed in both synchronized and unsynchronized contexts
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/bridge/impl/JMSBridgeImpl.java`
#### Snippet
```java
   private boolean connectedSource = false;

   private boolean connectedTarget = false;

   private int forwardMode;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `targetDestination` is accessed in both synchronized and unsynchronized contexts
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/bridge/impl/JMSBridgeImpl.java`
#### Snippet
```java
   private Destination sourceDestination;

   private Destination targetDestination;

   private Session sourceSession;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `sourceSession` is accessed in both synchronized and unsynchronized contexts
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/bridge/impl/JMSBridgeImpl.java`
#### Snippet
```java
   private Destination targetDestination;

   private Session sourceSession;

   private Session targetSession;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `qualityOfServiceMode` is accessed in both synchronized and unsynchronized contexts
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/bridge/impl/JMSBridgeImpl.java`
#### Snippet
```java
   private int maxRetries;

   private QualityOfServiceMode qualityOfServiceMode;

   private int maxBatchSize;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `failureRetryInterval` is accessed in both synchronized and unsynchronized contexts
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/bridge/impl/JMSBridgeImpl.java`
#### Snippet
```java
   private String selector;

   private long failureRetryInterval;

   private int maxRetries;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `useMaskedPassword` is accessed in both synchronized and unsynchronized contexts
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/bridge/impl/JMSBridgeImpl.java`
#### Snippet
```java
   private ObjectName objectName;

   private Boolean useMaskedPassword;

   private String passwordCodec;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `targetUsername` is accessed in both synchronized and unsynchronized contexts
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/bridge/impl/JMSBridgeImpl.java`
#### Snippet
```java
   private String sourcePassword;

   private String targetUsername;

   private String targetPassword;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `bridgeName` is accessed in both synchronized and unsynchronized contexts
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/bridge/impl/JMSBridgeImpl.java`
#### Snippet
```java
   private final Object lock = new Object();

   private String bridgeName = "N/A";

   private String sourceUsername;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `selector` is accessed in both synchronized and unsynchronized contexts
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/bridge/impl/JMSBridgeImpl.java`
#### Snippet
```java
   private TransactionManager tm;

   private String selector;

   private long failureRetryInterval;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `registry` is accessed in both synchronized and unsynchronized contexts
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/bridge/impl/JMSBridgeImpl.java`
#### Snippet
```java
   private static final int FORWARD_MODE_NONTX = 2;

   private ActiveMQRegistry registry;

   private ClassLoader moduleTccl;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `targetCff` is accessed in both synchronized and unsynchronized contexts
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/bridge/impl/JMSBridgeImpl.java`
#### Snippet
```java
   private ConnectionFactoryFactory sourceCff;

   private ConnectionFactoryFactory targetCff;

   private DestinationFactory sourceDestinationFactory;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `batchTimeCheckerFinished` is accessed in both synchronized and unsynchronized contexts
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/bridge/impl/JMSBridgeImpl.java`
#### Snippet
```java
   private MessageProducer targetProducer;

   private CountDownLatch batchTimeCheckerFinished;

   private Future<?> batchTimeCheckerTask;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `targetDestinationFactory` is accessed in both synchronized and unsynchronized contexts
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/bridge/impl/JMSBridgeImpl.java`
#### Snippet
```java
   private DestinationFactory sourceDestinationFactory;

   private DestinationFactory targetDestinationFactory;

   private Connection sourceConn;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `sourceConn` is accessed in both synchronized and unsynchronized contexts
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/bridge/impl/JMSBridgeImpl.java`
#### Snippet
```java
   private DestinationFactory targetDestinationFactory;

   private Connection sourceConn;

   private Connection targetConn;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `connectedSource` is accessed in both synchronized and unsynchronized contexts
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/bridge/impl/JMSBridgeImpl.java`
#### Snippet
```java
   private boolean failed;

   private boolean connectedSource = false;

   private boolean connectedTarget = false;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `sourceCff` is accessed in both synchronized and unsynchronized contexts
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/bridge/impl/JMSBridgeImpl.java`
#### Snippet
```java
   private final LinkedList<Message> messages;

   private ConnectionFactoryFactory sourceCff;

   private ConnectionFactoryFactory targetCff;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `maxBatchSize` is accessed in both synchronized and unsynchronized contexts
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/bridge/impl/JMSBridgeImpl.java`
#### Snippet
```java
   private QualityOfServiceMode qualityOfServiceMode;

   private int maxBatchSize;

   private long maxBatchTime;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `targetPassword` is accessed in both synchronized and unsynchronized contexts
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/bridge/impl/JMSBridgeImpl.java`
#### Snippet
```java
   private String targetUsername;

   private String targetPassword;

   private TransactionManager tm;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `sourceConsumer` is accessed in both synchronized and unsynchronized contexts
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/bridge/impl/JMSBridgeImpl.java`
#### Snippet
```java
   private Session targetSession;

   private MessageConsumer sourceConsumer;

   private MessageProducer targetProducer;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `subName` is accessed in both synchronized and unsynchronized contexts
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/bridge/impl/JMSBridgeImpl.java`
#### Snippet
```java
   private long maxBatchTime;

   private String subName;

   private String clientID;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `forwardMode` is accessed in both synchronized and unsynchronized contexts
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/bridge/impl/JMSBridgeImpl.java`
#### Snippet
```java
   private boolean connectedTarget = false;

   private int forwardMode;

   private MBeanServer mbeanServer;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `protocolHandler` is accessed in both synchronized and unsynchronized contexts
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTSession.java`
#### Snippet
```java
   private final String id = UUID.randomUUID().toString();

   private MQTTProtocolHandler protocolHandler;

   private MQTTSubscriptionManager subscriptionManager;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `state` is accessed in both synchronized and unsynchronized contexts
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTSession.java`
#### Snippet
```java
   private MQTTConnection connection;

   protected MQTTSessionState state;

   private boolean stopped = false;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `serverSession` is accessed in both synchronized and unsynchronized contexts
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTSession.java`
#### Snippet
```java
   private MQTTSessionCallback sessionCallback;

   private ServerSessionImpl serverSession;

   private ServerSessionImpl internalServerSession;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `internalServerSession` is accessed in both synchronized and unsynchronized contexts
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTSession.java`
#### Snippet
```java
   private ServerSessionImpl serverSession;

   private ServerSessionImpl internalServerSession;

   private MQTTPublishManager mqttPublishManager;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `mqttPublishManager` is accessed in both synchronized and unsynchronized contexts
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTSession.java`
#### Snippet
```java
   private ServerSessionImpl internalServerSession;

   private MQTTPublishManager mqttPublishManager;

   private MQTTConnectionManager mqttConnectionManager;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `stopped` is accessed in both synchronized and unsynchronized contexts
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTSession.java`
#### Snippet
```java
   protected MQTTSessionState state;

   private boolean stopped = false;

   private MQTTProtocolManager protocolManager;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `connection` is accessed in both synchronized and unsynchronized contexts
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTSession.java`
#### Snippet
```java
   private MQTTRetainMessageManager retainMessageManager;

   private MQTTConnection connection;

   protected MQTTSessionState state;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `subscriptionManager` is accessed in both synchronized and unsynchronized contexts
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTSession.java`
#### Snippet
```java
   private MQTTProtocolHandler protocolHandler;

   private MQTTSubscriptionManager subscriptionManager;

   private MQTTSessionCallback sessionCallback;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `protocolManager` is accessed in both synchronized and unsynchronized contexts
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTSession.java`
#### Snippet
```java
   private boolean stopped = false;

   private MQTTProtocolManager protocolManager;

   private boolean clean;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `disconnectedTime` is accessed in both synchronized and unsynchronized contexts
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTSessionState.java`
#### Snippet
```java
   private boolean attached = false;

   private long disconnectedTime = 0;

   private final OutboundStore outboundStore = new OutboundStore();
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `willStatus` is accessed in both synchronized and unsynchronized contexts
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTSessionState.java`
#### Snippet
```java
   private List<? extends MqttProperties.MqttProperty> willUserProperties;

   private WillStatus willStatus = WillStatus.NOT_SENT;

   private boolean failed = false;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `clientMaxPacketSize` is accessed in both synchronized and unsynchronized contexts
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTSessionState.java`
#### Snippet
```java
   private boolean failed = false;

   private int clientMaxPacketSize = 0;

   private Map<Integer, String> clientTopicAliases;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `willTopic` is accessed in both synchronized and unsynchronized contexts
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTSessionState.java`
#### Snippet
```java
   private ByteBuf willMessage;

   private String willTopic;

   private int willQoSLevel;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `willMessage` is accessed in both synchronized and unsynchronized contexts
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTSessionState.java`
#### Snippet
```java
   private boolean isWill = false;

   private ByteBuf willMessage;

   private String willTopic;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `willDelayInterval` is accessed in both synchronized and unsynchronized contexts
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTSessionState.java`
#### Snippet
```java
   private boolean willRetain = false;

   private long willDelayInterval = 0;

   private List<? extends MqttProperties.MqttProperty> willUserProperties;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `failed` is accessed in both synchronized and unsynchronized contexts
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTSessionState.java`
#### Snippet
```java
   private WillStatus willStatus = WillStatus.NOT_SENT;

   private boolean failed = false;

   private int clientMaxPacketSize = 0;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `clientTopicAliasMaximum` is accessed in both synchronized and unsynchronized contexts
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTSessionState.java`
#### Snippet
```java
   private Map<Integer, String> clientTopicAliases;

   private Integer clientTopicAliasMaximum;

   private Map<String, Integer> serverTopicAliases;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `willRetain` is accessed in both synchronized and unsynchronized contexts
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTSessionState.java`
#### Snippet
```java
   private int willQoSLevel;

   private boolean willRetain = false;

   private long willDelayInterval = 0;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `session` is accessed in both synchronized and unsynchronized contexts
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTConnectionManager.java`
#### Snippet
```java
public class MQTTConnectionManager {

   private MQTTSession session;

   public MQTTConnectionManager(MQTTSession session) {
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `managementConsumer` is accessed in both synchronized and unsynchronized contexts
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTPublishManager.java`
#### Snippet
```java
   private boolean createProducer = true;

   private ServerConsumer managementConsumer;

   private MQTTSession session;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `outboundStore` is accessed in both synchronized and unsynchronized contexts
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTPublishManager.java`
#### Snippet
```java
   private MQTTSessionState state;

   private MQTTSessionState.OutboundStore outboundStore;

   private boolean closeMqttConnectionOnPublishAuthorizationFailure;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `state` is accessed in both synchronized and unsynchronized contexts
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTPublishManager.java`
#### Snippet
```java
   private final Object lock = new Object();

   private MQTTSessionState state;

   private MQTTSessionState.OutboundStore outboundStore;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `session` is accessed in both synchronized and unsynchronized contexts
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTPublishManager.java`
#### Snippet
```java
   private ServerConsumer managementConsumer;

   private MQTTSession session;

   private final Object lock = new Object();
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `context` is accessed in both synchronized and unsynchronized contexts
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenWireConnection.java`
#### Snippet
```java
   private final OpenWireFormat outWireFormat;

   private AMQConnectionContext context;

   private final int actorThresholdBytes;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `active` is accessed in both synchronized and unsynchronized contexts
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/server/impl/JMSServerManagerImpl.java`
#### Snippet
```java
   private boolean startCalled;

   private boolean active;

   private JMSConfiguration config;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `registry` is accessed in both synchronized and unsynchronized contexts
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/server/impl/JMSServerManagerImpl.java`
#### Snippet
```java
   private static final String REJECT_FILTER = Filter.GENERIC_IGNORED_FILTER;

   private BindingRegistry registry;

   // keys are the core addresses of the JMS queues
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `storage` is accessed in both synchronized and unsynchronized contexts
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/server/impl/JMSServerManagerImpl.java`
#### Snippet
```java
   private Configuration coreConfig;

   private JMSStorageManager storage;

   private final Map<String, List<String>> unRecoveredBindings = new HashMap<>();
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `unavailable` is accessed in both synchronized and unsynchronized contexts
in `artemis-quorum-ri/src/main/java/org/apache/activemq/artemis/quorum/zookeeper/CuratorDistributedPrimitive.java`
#### Snippet
```java
   private final PrimitiveId id;

   private boolean unavailable;
   private boolean closed;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `appendExecutor` is accessed in both synchronized and unsynchronized contexts
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalImpl.java`
#### Snippet
```java
   private Executor compactorExecutor = null;

   private Executor appendExecutor = null;

   private final ConcurrentHashSet<CountDownLatch> latches = new ConcurrentHashSet<>();
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `journalRetentionFolder` is accessed in both synchronized and unsynchronized contexts
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalImpl.java`
#### Snippet
```java
   private final JournalFilesRepository filesRepository;

   private File journalRetentionFolder;

   private long journalRetentionPeriod = -1;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `compactorExecutor` is accessed in both synchronized and unsynchronized contexts
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalImpl.java`
#### Snippet
```java
   private Executor filesExecutor = null;

   private Executor compactorExecutor = null;

   private Executor appendExecutor = null;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `filesExecutor` is accessed in both synchronized and unsynchronized contexts
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalImpl.java`
#### Snippet
```java
   private final AtomicBoolean compactorRunning = new AtomicBoolean();

   private Executor filesExecutor = null;

   private Executor compactorExecutor = null;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `replaceableRecords` is accessed in both synchronized and unsynchronized contexts
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalImpl.java`
#### Snippet
```java
   private final ReadWriteLock compactorLock = new ReentrantReadWriteLock();

   ByteObjectHashMap<Boolean> replaceableRecords;


```

### FieldAccessedSynchronizedAndUnsynchronized
Field `calendarThreadLocal` is accessed in both synchronized and unsynchronized contexts
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalImpl.java`
#### Snippet
```java
   private ThreadPoolExecutor threadPool;

   ThreadLocal<GregorianCalendar> calendarThreadLocal = ThreadLocal.withInitial(() -> new GregorianCalendar());

   /**
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `closed` is accessed in both synchronized and unsynchronized contexts
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQJMSContext.java`
#### Snippet
```java
   private MessageProducer innerProducer;
   private boolean xa;
   private boolean closed;

   ActiveMQJMSContext(final ActiveMQConnectionForContext connection,
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `autoStart` is accessed in both synchronized and unsynchronized contexts
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQJMSContext.java`
#### Snippet
```java
   private final ActiveMQConnectionForContext connection;
   private volatile Session session;
   private boolean autoStart = ActiveMQJMSContext.DEFAULT_AUTO_START;
   private MessageProducer innerProducer;
   private boolean xa;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `initialSession` is accessed in both synchronized and unsynchronized contexts
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQConnection.java`
#### Snippet
```java
   private final boolean enable1xPrefixes;

   private ClientSession initialSession;

   private final Exception creationStack;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `serverLocator` is accessed in both synchronized and unsynchronized contexts
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQConnectionFactory.java`
#### Snippet
```java
   private static final long serialVersionUID = 6730844785641767519L;

   private ServerLocator serverLocator;

   private String clientID;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `clientID` is accessed in both synchronized and unsynchronized contexts
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQConnectionFactory.java`
#### Snippet
```java
   private ServerLocator serverLocator;

   private String clientID;

   private boolean enableSharedClientID = ActiveMQClient.DEFAULT_ENABLED_SHARED_CLIENT_ID;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `passwordCodec` is accessed in both synchronized and unsynchronized contexts
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQConnectionFactory.java`
#### Snippet
```java
   private String password;

   private String passwordCodec;

   private String protocolManagerFactoryStr;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `dupsOKBatchSize` is accessed in both synchronized and unsynchronized contexts
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQConnectionFactory.java`
#### Snippet
```java
   private boolean enableSharedClientID = ActiveMQClient.DEFAULT_ENABLED_SHARED_CLIENT_ID;

   private int dupsOKBatchSize = ActiveMQClient.DEFAULT_ACK_BATCH_SIZE;

   private int transactionBatchSize = ActiveMQClient.DEFAULT_ACK_BATCH_SIZE;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `transactionBatchSize` is accessed in both synchronized and unsynchronized contexts
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQConnectionFactory.java`
#### Snippet
```java
   private int dupsOKBatchSize = ActiveMQClient.DEFAULT_ACK_BATCH_SIZE;

   private int transactionBatchSize = ActiveMQClient.DEFAULT_ACK_BATCH_SIZE;

   private boolean readOnly;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `client` is accessed in both synchronized and unsynchronized contexts
in `artemis-quorum-ri/src/main/java/org/apache/activemq/artemis/quorum/zookeeper/CuratorDistributedPrimitiveManager.java`
#### Snippet
```java
   }

   private CuratorFramework client;
   private final Map<PrimitiveId, CuratorDistributedPrimitive> primitives;
   private CopyOnWriteArrayList<UnavailableManagerListener> listeners;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `maxPoolSize` is accessed in both synchronized and unsynchronized contexts
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/AmqpTransferTagGenerator.java`
#### Snippet
```java

   private long nextTagId;
   private int maxPoolSize = DEFAULT_TAG_POOL_SIZE;

   public AmqpTransferTagGenerator() {
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `data` is accessed in both synchronized and unsynchronized contexts
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPStandardMessage.java`
#### Snippet
```java

   // Buffer and state for the data backing this message.
   protected ReadableBuffer data;

   /**
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `temporaryBuffer` is accessed in both synchronized and unsynchronized contexts
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPLargeMessage.java`
#### Snippet
```java
    * AMQPLargeMessagePersister will save the buffer here.
    * */
   private ByteBuf temporaryBuffer;

   private final LargeBody largeBody;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `scheduledTime` is accessed in both synchronized and unsynchronized contexts
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
   protected long expiration;
   protected boolean expirationReload = false;
   protected long scheduledTime = -1;

   // The Proton based AMQP message section that are retained in memory, these are the
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `properties` is accessed in both synchronized and unsynchronized contexts
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
   protected Header header;
   protected MessageAnnotations messageAnnotations;
   protected Properties properties;
   protected ApplicationProperties applicationProperties;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `encodedHeaderSize` is accessed in both synchronized and unsynchronized contexts
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
   // the delivery annotations later and perform efficient encodes or copies.
   protected int headerPosition = VALUE_NOT_PRESENT;
   protected int encodedHeaderSize;
   protected int deliveryAnnotationsPosition = VALUE_NOT_PRESENT;
   protected int encodedDeliveryAnnotationsSize;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `encodedDeliveryAnnotationsSize` is accessed in both synchronized and unsynchronized contexts
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
   protected int encodedHeaderSize;
   protected int deliveryAnnotationsPosition = VALUE_NOT_PRESENT;
   protected int encodedDeliveryAnnotationsSize;
   protected int messageAnnotationsPosition = VALUE_NOT_PRESENT;
   protected int propertiesPosition = VALUE_NOT_PRESENT;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `deliveryAnnotations` is accessed in both synchronized and unsynchronized contexts
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
   protected Set<Object> rejectedConsumers;
   protected DeliveryAnnotations deliveryAnnotationsForSendBuffer;
   protected DeliveryAnnotations deliveryAnnotations;

   // These are properties set at the broker level and carried only internally by broker storage.
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `messageAnnotationsPosition` is accessed in both synchronized and unsynchronized contexts
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
   protected int deliveryAnnotationsPosition = VALUE_NOT_PRESENT;
   protected int encodedDeliveryAnnotationsSize;
   protected int messageAnnotationsPosition = VALUE_NOT_PRESENT;
   protected int propertiesPosition = VALUE_NOT_PRESENT;
   protected int applicationPropertiesPosition = VALUE_NOT_PRESENT;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `propertiesPosition` is accessed in both synchronized and unsynchronized contexts
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
   protected int encodedDeliveryAnnotationsSize;
   protected int messageAnnotationsPosition = VALUE_NOT_PRESENT;
   protected int propertiesPosition = VALUE_NOT_PRESENT;
   protected int applicationPropertiesPosition = VALUE_NOT_PRESENT;
   protected int remainingBodyPosition = VALUE_NOT_PRESENT;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `applicationProperties` is accessed in both synchronized and unsynchronized contexts
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
   protected MessageAnnotations messageAnnotations;
   protected Properties properties;
   protected ApplicationProperties applicationProperties;

   protected String connectionID;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `remainingBodyPosition` is accessed in both synchronized and unsynchronized contexts
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
   protected int propertiesPosition = VALUE_NOT_PRESENT;
   protected int applicationPropertiesPosition = VALUE_NOT_PRESENT;
   protected int remainingBodyPosition = VALUE_NOT_PRESENT;

   // Message level meta data
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `messageDataScanned` is accessed in both synchronized and unsynchronized contexts
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
   }
   // Buffer and state for the data backing this message.
   protected byte messageDataScanned = MessageDataScanningStatus.NOT_SCANNED.code;

   // Marks the message as needed to be re-encoded to update the backing buffer
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `deliveryAnnotationsPosition` is accessed in both synchronized and unsynchronized contexts
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
   protected int headerPosition = VALUE_NOT_PRESENT;
   protected int encodedHeaderSize;
   protected int deliveryAnnotationsPosition = VALUE_NOT_PRESENT;
   protected int encodedDeliveryAnnotationsSize;
   protected int messageAnnotationsPosition = VALUE_NOT_PRESENT;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `messageAnnotations` is accessed in both synchronized and unsynchronized contexts
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
   // here when making changes to those Sections.
   protected Header header;
   protected MessageAnnotations messageAnnotations;
   protected Properties properties;
   protected ApplicationProperties applicationProperties;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `header` is accessed in both synchronized and unsynchronized contexts
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
   // the Properties and ApplicationProperties are immutable so care should be taken
   // here when making changes to those Sections.
   protected Header header;
   protected MessageAnnotations messageAnnotations;
   protected Properties properties;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `applicationPropertiesPosition` is accessed in both synchronized and unsynchronized contexts
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
   protected int messageAnnotationsPosition = VALUE_NOT_PRESENT;
   protected int propertiesPosition = VALUE_NOT_PRESENT;
   protected int applicationPropertiesPosition = VALUE_NOT_PRESENT;
   protected int remainingBodyPosition = VALUE_NOT_PRESENT;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `expirationReload` is accessed in both synchronized and unsynchronized contexts
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
   protected volatile int memoryEstimate = -1;
   protected long expiration;
   protected boolean expirationReload = false;
   protected long scheduledTime = -1;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `expiration` is accessed in both synchronized and unsynchronized contexts
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
   protected SimpleString address;
   protected volatile int memoryEstimate = -1;
   protected long expiration;
   protected boolean expirationReload = false;
   protected long scheduledTime = -1;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `headerPosition` is accessed in both synchronized and unsynchronized contexts
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
   // of the header and delivery annotations if present so we can easily exclude
   // the delivery annotations later and perform efficient encodes or copies.
   protected int headerPosition = VALUE_NOT_PRESENT;
   protected int encodedHeaderSize;
   protected int deliveryAnnotationsPosition = VALUE_NOT_PRESENT;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `modified` is accessed in both synchronized and unsynchronized contexts
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java

   // Marks the message as needed to be re-encoded to update the backing buffer
   protected boolean modified;

   // Track locations of the message sections for later use and track the size
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `brokerConsumer` is accessed in both synchronized and unsynchronized contexts
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/ProtonServerSenderContext.java`
#### Snippet
```java
   private final ConnectionFlushIOCallback connectionFlusher = new ConnectionFlushIOCallback();

   private Consumer brokerConsumer;
   private ReadyListener onflowControlReady;
   protected final AMQPSessionContext protonSession;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `lists` is accessed in both synchronized and unsynchronized contexts
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/connect/mirror/ReferenceNodeStore.java`
#### Snippet
```java

   // This is where the messages are stored by server id...
   HashMap<String, LongObjectHashMap<LinkedListImpl.Node<MessageReference>>> lists;

   String lruListID;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `readPosition` is accessed in both synchronized and unsynchronized contexts
in `artemis-jdbc-store/src/main/java/org/apache/activemq/artemis/jdbc/store/file/JDBCSequentialFile.java`
#### Snippet
```java
   private long id = -1;

   private long readPosition = 0;

   private long writePosition = 0;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `writePosition` is accessed in both synchronized and unsynchronized contexts
in `artemis-jdbc-store/src/main/java/org/apache/activemq/artemis/jdbc/store/file/JDBCSequentialFile.java`
#### Snippet
```java
   private long readPosition = 0;

   private long writePosition = 0;

   private final Executor executor;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `started` is accessed in both synchronized and unsynchronized contexts
in `artemis-jdbc-store/src/main/java/org/apache/activemq/artemis/jdbc/store/file/JDBCSequentialFileFactory.java`
#### Snippet
```java
   private static final Logger logger = LoggerFactory.getLogger(MethodHandles.lookup().lookupClass());

   private boolean started;

   private final Set<JDBCSequentialFile> files = new ConcurrentHashSet<>();
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `dbDriver` is accessed in both synchronized and unsynchronized contexts
in `artemis-jdbc-store/src/main/java/org/apache/activemq/artemis/jdbc/store/file/JDBCSequentialFileFactory.java`
#### Snippet
```java
   private final Map<String, Object> fileLocks = new ConcurrentHashMap<>();

   private JDBCSequentialFileFactoryDriver dbDriver;

   private final IOCriticalErrorListener criticalErrorListener;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `networkTimeoutExecutor` is accessed in both synchronized and unsynchronized contexts
in `artemis-jdbc-store/src/main/java/org/apache/activemq/artemis/jdbc/store/drivers/JDBCConnectionProvider.java`
#### Snippet
```java
   private static final Logger logger = LoggerFactory.getLogger(MethodHandles.lookup().lookupClass());
   private DataSource dataSource;
   private Executor networkTimeoutExecutor;
   private int networkTimeoutMillis;
   private boolean supportNetworkTimeout;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `networkTimeoutMillis` is accessed in both synchronized and unsynchronized contexts
in `artemis-jdbc-store/src/main/java/org/apache/activemq/artemis/jdbc/store/drivers/JDBCConnectionProvider.java`
#### Snippet
```java
   private DataSource dataSource;
   private Executor networkTimeoutExecutor;
   private int networkTimeoutMillis;
   private boolean supportNetworkTimeout;
   private final String user;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `maxId` is accessed in both synchronized and unsynchronized contexts
in `artemis-jdbc-store/src/main/java/org/apache/activemq/artemis/jdbc/store/journal/JDBCJournalLoaderCallback.java`
#### Snippet
```java
   private final List<RecordInfo> committedRecords;

   private long maxId = -1;

   JDBCJournalLoaderCallback(final List<RecordInfo> committedRecords,
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `temporaryWarDir` is accessed in both synchronized and unsynchronized contexts
in `artemis-web/src/main/java/org/apache/activemq/artemis/component/WebServerComponent.java`
#### Snippet
```java
   private ServerConnector[] connectors;
   private Path artemisHomePath;
   private Path temporaryWarDir;
   private String artemisInstance;
   private String artemisHome;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `connectors` is accessed in both synchronized and unsynchronized contexts
in `artemis-web/src/main/java/org/apache/activemq/artemis/component/WebServerComponent.java`
#### Snippet
```java
   private final List<String> jolokiaUrls = new ArrayList<>();
   private final List<Pair<WebAppContext, String>> webContextData = new ArrayList<>();
   private ServerConnector[] connectors;
   private Path artemisHomePath;
   private Path temporaryWarDir;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `artemisHome` is accessed in both synchronized and unsynchronized contexts
in `artemis-web/src/main/java/org/apache/activemq/artemis/component/WebServerComponent.java`
#### Snippet
```java
   private Path temporaryWarDir;
   private String artemisInstance;
   private String artemisHome;

   @Override
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `server` is accessed in both synchronized and unsynchronized contexts
in `artemis-web/src/main/java/org/apache/activemq/artemis/component/WebServerComponent.java`
#### Snippet
```java
   public static final String WEB_CONSOLE_DISPLAY_NAME = System.getProperty("org.apache.activemq.artemis.webConsoleDisplayName", "hawtio");

   private Server server;
   private HandlerList handlers;
   private WebServerDTO webServerConfig;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `artemisInstance` is accessed in both synchronized and unsynchronized contexts
in `artemis-web/src/main/java/org/apache/activemq/artemis/component/WebServerComponent.java`
#### Snippet
```java
   private Path artemisHomePath;
   private Path temporaryWarDir;
   private String artemisInstance;
   private String artemisHome;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `webServerConfig` is accessed in both synchronized and unsynchronized contexts
in `artemis-web/src/main/java/org/apache/activemq/artemis/component/WebServerComponent.java`
#### Snippet
```java
   private Server server;
   private HandlerList handlers;
   private WebServerDTO webServerConfig;
   private final List<String> consoleUrls = new ArrayList<>();
   private final List<String> jolokiaUrls = new ArrayList<>();
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `artemisHomePath` is accessed in both synchronized and unsynchronized contexts
in `artemis-web/src/main/java/org/apache/activemq/artemis/component/WebServerComponent.java`
#### Snippet
```java
   private final List<Pair<WebAppContext, String>> webContextData = new ArrayList<>();
   private ServerConnector[] connectors;
   private Path artemisHomePath;
   private Path temporaryWarDir;
   private String artemisInstance;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `broadcastOpened` is accessed in both synchronized and unsynchronized contexts
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/JGroupsBroadcastEndpoint.java`
#### Snippet
```java
   private boolean clientOpened;

   private boolean broadcastOpened;

   private JChannelWrapper channel;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `clientOpened` is accessed in both synchronized and unsynchronized contexts
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/JGroupsBroadcastEndpoint.java`
#### Snippet
```java
   private final String channelName;

   private boolean clientOpened;

   private boolean broadcastOpened;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `receiver` is accessed in both synchronized and unsynchronized contexts
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/JGroupsBroadcastEndpoint.java`
#### Snippet
```java
   private JChannelWrapper channel;

   private JGroupsReceiver receiver;

   private JChannelManager manager;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `channel` is accessed in both synchronized and unsynchronized contexts
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/JGroupsBroadcastEndpoint.java`
#### Snippet
```java
   private boolean broadcastOpened;

   private JChannelWrapper channel;

   private JGroupsReceiver receiver;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `refCount` is accessed in both synchronized and unsynchronized contexts
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/jgroups/JChannelWrapper.java`
#### Snippet
```java

   private boolean connected = false;
   int refCount = 1;
   final JChannel channel;
   final String channelName;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `connected` is accessed in both synchronized and unsynchronized contexts
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/jgroups/JChannelWrapper.java`
#### Snippet
```java
   private static final Logger logger = LoggerFactory.getLogger(MethodHandles.lookup().lookupClass());

   private boolean connected = false;
   int refCount = 1;
   final JChannel channel;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `failed` is accessed in both synchronized and unsynchronized contexts
in `artemis-jdbc-store/src/main/java/org/apache/activemq/artemis/jdbc/store/journal/JDBCJournalImpl.java`
#### Snippet
```java
   private boolean started;

   private AtomicBoolean failed = new AtomicBoolean(false);

   private JDBCJournalSync syncTimer;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `selectJournalRecords` is accessed in both synchronized and unsynchronized contexts
in `artemis-jdbc-store/src/main/java/org/apache/activemq/artemis/jdbc/store/journal/JDBCJournalImpl.java`
#### Snippet
```java
   private String insertJournalRecords;

   private String selectJournalRecords;

   private String countJournalRecords;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `insertJournalRecords` is accessed in both synchronized and unsynchronized contexts
in `artemis-jdbc-store/src/main/java/org/apache/activemq/artemis/jdbc/store/journal/JDBCJournalImpl.java`
#### Snippet
```java
   private final List<JDBCJournalRecord> records;

   private String insertJournalRecords;

   private String selectJournalRecords;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `started` is accessed in both synchronized and unsynchronized contexts
in `artemis-jdbc-store/src/main/java/org/apache/activemq/artemis/jdbc/store/journal/JDBCJournalImpl.java`
#### Snippet
```java
   private String deleteJournalTxRecords;

   private boolean started;

   private AtomicBoolean failed = new AtomicBoolean(false);
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `deleteJournalTxRecords` is accessed in both synchronized and unsynchronized contexts
in `artemis-jdbc-store/src/main/java/org/apache/activemq/artemis/jdbc/store/journal/JDBCJournalImpl.java`
#### Snippet
```java
   private String deleteJournalRecords;

   private String deleteJournalTxRecords;

   private boolean started;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `deleteJournalRecords` is accessed in both synchronized and unsynchronized contexts
in `artemis-jdbc-store/src/main/java/org/apache/activemq/artemis/jdbc/store/journal/JDBCJournalImpl.java`
#### Snippet
```java
   private String countJournalRecords;

   private String deleteJournalRecords;

   private String deleteJournalTxRecords;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `balance` is accessed in both synchronized and unsynchronized contexts
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/AsynchronousProducerCreditsImpl.java`
#### Snippet
```java
   private static final Logger logger = LoggerFactory.getLogger(MethodHandles.lookup().lookupClass());

   int balance;

   final ClientProducerFlowCallback callback;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `arriving` is accessed in both synchronized and unsynchronized contexts
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/AbstractProducerCreditsImpl.java`
#### Snippet
```java
   private final ClientSessionInternal session;

   protected int arriving;

   private int refCount;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `callback` is accessed in both synchronized and unsynchronized contexts
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ClientProducerCreditManagerImpl.java`
#### Snippet
```java
   private int windowSize;

   private ClientProducerFlowCallback callback;

   public ClientProducerCreditManagerImpl(final ClientSessionInternal session, final int windowSize) {
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `executor` is accessed in both synchronized and unsynchronized contexts
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/Topology.java`
#### Snippet
```java
   private final Set<ClusterTopologyListener> topologyListeners;

   private Executor executor;

   /**
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `streamClosed` is accessed in both synchronized and unsynchronized contexts
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/LargeMessageControllerImpl.java`
#### Snippet
```java
   private boolean streamEnded = false;

   private boolean streamClosed = false;

   private final long readTimeout;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `streamEnded` is accessed in both synchronized and unsynchronized contexts
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/LargeMessageControllerImpl.java`
#### Snippet
```java
   private final int bufferSize;

   private boolean streamEnded = false;

   private boolean streamClosed = false;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `outStream` is accessed in both synchronized and unsynchronized contexts
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/LargeMessageControllerImpl.java`
#### Snippet
```java
   private long packetLastPosition = -1;

   private OutputStream outStream;

   // There's no need to wait a synchronization
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `currentLargeMessageController` is accessed in both synchronized and unsynchronized contexts
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ClientConsumerImpl.java`
#### Snippet
```java
   private final Runner runner = new Runner();

   private LargeMessageControllerImpl currentLargeMessageController;

   // When receiving LargeMessages, the user may choose to not read the body, on this case we need to discard the body
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `stopped` is accessed in both synchronized and unsynchronized contexts
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ClientConsumerImpl.java`
#### Snippet
```java
   private volatile ClientMessageInternal lastAckedMessage;

   private boolean stopped = false;

   private AtomicLong forceDeliveryCount = new AtomicLong(0);
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `startExecutor` is accessed in both synchronized and unsynchronized contexts
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ServerLocatorImpl.java`
#### Snippet
```java
   private final List<Interceptor> outgoingInterceptors = new CopyOnWriteArrayList<>();

   private Executor startExecutor;

   private Actor<Long> updateArrayActor;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `config` is accessed in both synchronized and unsynchronized contexts
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ServerLocatorImpl.java`
#### Snippet
```java
   private final Exception traceException = new Exception();

   private ServerLocatorConfig config = new ServerLocatorConfig();

   public static synchronized void clearThreadPools() {
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `scheduledThreadPool` is accessed in both synchronized and unsynchronized contexts
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ServerLocatorImpl.java`
#### Snippet
```java
   private transient Executor threadPool;

   private transient ScheduledExecutorService scheduledThreadPool;

   private transient DiscoveryGroup discoveryGroup;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `topology` is accessed in both synchronized and unsynchronized contexts
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ServerLocatorImpl.java`
#### Snippet
```java
   private final StaticConnector staticConnector = new StaticConnector();

   private Topology topology;

   private final Object topologyArrayGuard = new Object();
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `clusterTransportConfiguration` is accessed in both synchronized and unsynchronized contexts
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ServerLocatorImpl.java`
#### Snippet
```java
   private String nodeID;

   private TransportConfiguration clusterTransportConfiguration;

   /** For tests only */
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `shutdownPool` is accessed in both synchronized and unsynchronized contexts
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ServerLocatorImpl.java`
#### Snippet
```java

   // if the system should shutdown the pool when shutting down
   private transient boolean shutdownPool;

   private transient Executor threadPool;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `latch` is accessed in both synchronized and unsynchronized contexts
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ServerLocatorImpl.java`
#### Snippet
```java
   private final Object stateGuard = new Object();
   private transient STATE state;
   private transient CountDownLatch latch;

   private final List<Interceptor> incomingInterceptors = new CopyOnWriteArrayList<>();
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `clusterConnection` is accessed in both synchronized and unsynchronized contexts
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ServerLocatorImpl.java`
#### Snippet
```java
   private boolean finalizeCheck;

   private boolean clusterConnection;

   private transient String identity;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `loadBalancingPolicy` is accessed in both synchronized and unsynchronized contexts
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ServerLocatorImpl.java`
#### Snippet
```java
   private transient DiscoveryGroup discoveryGroup;

   private transient ConnectionLoadBalancingPolicy loadBalancingPolicy;

   private final Object discoveryGroupGuardian = new Object();
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `discoveryGroup` is accessed in both synchronized and unsynchronized contexts
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ServerLocatorImpl.java`
#### Snippet
```java
   private transient ScheduledExecutorService scheduledThreadPool;

   private transient DiscoveryGroup discoveryGroup;

   private transient ConnectionLoadBalancingPolicy loadBalancingPolicy;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `nodeID` is accessed in both synchronized and unsynchronized contexts
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ServerLocatorImpl.java`
#### Snippet
```java
   private String groupID;

   private String nodeID;

   private TransportConfiguration clusterTransportConfiguration;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `updateArrayActor` is accessed in both synchronized and unsynchronized contexts
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ServerLocatorImpl.java`
#### Snippet
```java
   private Executor startExecutor;

   private Actor<Long> updateArrayActor;

   private AfterConnectInternalListener afterConnectListener;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `threadPool` is accessed in both synchronized and unsynchronized contexts
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ServerLocatorImpl.java`
#### Snippet
```java
   private transient boolean shutdownPool;

   private transient Executor threadPool;

   private transient ScheduledExecutorService scheduledThreadPool;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `connectors` is accessed in both synchronized and unsynchronized contexts
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ServerLocatorImpl.java`
#### Snippet
```java
      private static final long serialVersionUID = 6772279632415242634L;

      private List<Connector> connectors;

      public ClientSessionFactory connect(boolean skipWarnings) throws ActiveMQException {
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `protocolManagerFactory` is accessed in both synchronized and unsynchronized contexts
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ServerLocatorImpl.java`
#### Snippet
```java

   // This is the default value
   private ClientProtocolManagerFactory protocolManagerFactory = new ActiveMQClientProtocolManagerFactory().setLocator(this);

   private final boolean ha;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `name` is accessed in both synchronized and unsynchronized contexts
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ClientSessionImpl.java`
#### Snippet
```java
   private final ClientSessionFactoryInternal sessionFactory;

   private String name;

   private final String username;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `currentXID` is accessed in both synchronized and unsynchronized contexts
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ClientSessionImpl.java`
#### Snippet
```java
    * Current XID. this will be used in case of failover
    */
   private Xid currentXID;

   private final AtomicInteger concurrentCall = new AtomicInteger(0);
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `received` is accessed in both synchronized and unsynchronized contexts
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/cluster/DiscoveryGroup.java`
#### Snippet
```java
   private Thread thread;

   private boolean received;

   private final Object waitLock = new Object();
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `thread` is accessed in both synchronized and unsynchronized contexts
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/cluster/DiscoveryGroup.java`
#### Snippet
```java
   private final String name;

   private Thread thread;

   private boolean received;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `userID` is accessed in both synchronized and unsynchronized contexts
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/message/impl/CoreMessage.java`
#### Snippet
```java
   protected byte priority;

   private UUID userID;

   private int propertiesLocation = -1;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `buffer` is accessed in both synchronized and unsynchronized contexts
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/message/impl/CoreMessage.java`
#### Snippet
```java
   /** That is the readInto for the whole message, including properties..
       it does not include the buffer for the Packet send and receive header on core protocol */
   protected ByteBuf buffer;

   private volatile boolean validBuffer = false;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `expiration` is accessed in both synchronized and unsynchronized contexts
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/message/impl/CoreMessage.java`
#### Snippet
```java
    * GMT milliseconds at which this message expires. 0 means never expires *
    */
   private long expiration;

   protected long timestamp;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `propertiesLocation` is accessed in both synchronized and unsynchronized contexts
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/message/impl/CoreMessage.java`
#### Snippet
```java
   private UUID userID;

   private int propertiesLocation = -1;

   protected volatile TypedProperties properties;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `type` is accessed in both synchronized and unsynchronized contexts
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/message/impl/CoreMessage.java`
#### Snippet
```java
   protected SimpleString address;

   protected byte type;

   protected boolean durable;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `priority` is accessed in both synchronized and unsynchronized contexts
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/message/impl/CoreMessage.java`
#### Snippet
```java
   protected long timestamp;

   protected byte priority;

   private UUID userID;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `durable` is accessed in both synchronized and unsynchronized contexts
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/message/impl/CoreMessage.java`
#### Snippet
```java
   protected byte type;

   protected boolean durable;

   /**
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `messageID` is accessed in both synchronized and unsynchronized contexts
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/message/impl/CoreMessage.java`
#### Snippet
```java
   protected int messageIDPosition = -1;

   protected long messageID;

   protected SimpleString address;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `endOfBodyPosition` is accessed in both synchronized and unsynchronized contexts
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/message/impl/CoreMessage.java`
#### Snippet
```java
   protected volatile ResetLimitWrappedActiveMQBuffer writableBuffer;

   protected int endOfBodyPosition = -1;

   protected int messageIDPosition = -1;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `timestamp` is accessed in both synchronized and unsynchronized contexts
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/message/impl/CoreMessage.java`
#### Snippet
```java
   private long expiration;

   protected long timestamp;

   protected byte priority;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `address` is accessed in both synchronized and unsynchronized contexts
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/message/impl/CoreMessage.java`
#### Snippet
```java
   protected long messageID;

   protected SimpleString address;

   protected byte type;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `inCreateSessionLatch` is accessed in both synchronized and unsynchronized contexts
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/ActiveMQClientProtocolManager.java`
#### Snippet
```java
    * Used to wait for the creation of a session.
    */
   private CountDownLatch inCreateSessionLatch;

   protected volatile RemotingConnectionImpl connection;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `inCreateSession` is accessed in both synchronized and unsynchronized contexts
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/ActiveMQClientProtocolManager.java`
#### Snippet
```java
    * Flag that tells whether we are trying to create a session.
    */
   private boolean inCreateSession;

   /**
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `failingOver` is accessed in both synchronized and unsynchronized contexts
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/ChannelImpl.java`
#### Snippet
```java
   private final Object sendBlockingLock = new Object();

   private boolean failingOver;

   private final int confWindowSize;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `firstStoredCommandID` is accessed in both synchronized and unsynchronized contexts
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/ChannelImpl.java`
#### Snippet
```java
   private final ResponseCache responseAsyncCache;

   private int firstStoredCommandID;

   private final AtomicInteger lastConfirmedCommandID = new AtomicInteger(-1);
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `receivedBytes` is accessed in both synchronized and unsynchronized contexts
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/ChannelImpl.java`
#### Snippet
```java
   private final int confWindowSize;

   private int receivedBytes;

   private CommandConfirmationHandler commandConfirmationHandler;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `response` is accessed in both synchronized and unsynchronized contexts
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/ChannelImpl.java`
#### Snippet
```java
   private ChannelHandler handler;

   private Packet response;

   private final java.util.Queue<Packet> resendCache;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `liveNodeID` is accessed in both synchronized and unsynchronized contexts
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ClientSessionFactoryImpl.java`
#### Snippet
```java
   private final ConfirmationWindowWarning confirmationWindowWarning;

   private String liveNodeID;

   // We need to cache this value here since some listeners may be registered after connectionReadyForWrites was called.
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `connection` is accessed in both synchronized and unsynchronized contexts
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ClientSessionFactoryImpl.java`
#### Snippet
```java
   private final Executor closeExecutor;

   private RemotingConnection connection;

   private final long retryInterval;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `connector` is accessed in both synchronized and unsynchronized contexts
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ClientSessionFactoryImpl.java`
#### Snippet
```java
   private final Set<FailoverEventListener> failoverListeners = new ConcurrentHashSet<>();

   private Connector connector;

   private Future<?> pingerFuture;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `storageManager` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/security/ActiveMQBasicSecurityManager.java`
#### Snippet
```java
   private Map<String, String> properties;
   private String rolePrincipalClass = RolePrincipal.class.getName();
   private StorageManager storageManager;

   @Override
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `regexpByUser` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/security/jaas/TextFileCertificateLoginModule.java`
#### Snippet
```java

   private Map<String, Set<String>> rolesByUser;
   private Map<String, Pattern> regexpByUser;
   private Map<String, String> usersByDn;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `usersByDn` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/security/jaas/TextFileCertificateLoginModule.java`
#### Snippet
```java
   private Map<String, Set<String>> rolesByUser;
   private Map<String, Pattern> regexpByUser;
   private Map<String, String> usersByDn;

   /**
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `scheduledThreadPool` is accessed in both synchronized and unsynchronized contexts
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/NettyConnector.java`
#### Snippet
```java
   private boolean useGlobalWorkerPool;

   private ScheduledExecutorService scheduledThreadPool;

   private Executor closeExecutor;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `sslEnabled` is accessed in both synchronized and unsynchronized contexts
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/NettyConnector.java`
#### Snippet
```java
   private final BaseConnectionLifeCycleListener<?> listener;

   private boolean sslEnabled = TransportConstants.DEFAULT_SSL_ENABLED;

   private boolean httpEnabled;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `lastSendTime` is accessed in both synchronized and unsynchronized contexts
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/NettyConnector.java`
#### Snippet
```java
      private Channel channel;

      private long lastSendTime = 0;

      private boolean waitingGet = false;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `batchDelay` is accessed in both synchronized and unsynchronized contexts
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/NettyConnector.java`
#### Snippet
```java
   private final int writeBufferHighWaterMark;

   private long batchDelay;

   private ConcurrentMap<Object, Connection> connections = new ConcurrentHashMap<>();
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `trustStorePassword` is accessed in both synchronized and unsynchronized contexts
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/NettyConnector.java`
#### Snippet
```java
   private String trustStorePath;

   private String trustStorePassword;

   private String crlPath;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `keyStorePath` is accessed in both synchronized and unsynchronized contexts
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/NettyConnector.java`
#### Snippet
```java
   private String keyStoreType;

   private String keyStorePath;

   private String keyStorePassword;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `channelClazz` is accessed in both synchronized and unsynchronized contexts
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/NettyConnector.java`
#### Snippet
```java
   private final boolean serverConnection;

   private Class<? extends Channel> channelClazz;

   private Bootstrap bootstrap;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `waitingGet` is accessed in both synchronized and unsynchronized contexts
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/NettyConnector.java`
#### Snippet
```java
      private long lastSendTime = 0;

      private boolean waitingGet = false;

      private HttpIdleTimer task;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `keyStorePassword` is accessed in both synchronized and unsynchronized contexts
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/NettyConnector.java`
#### Snippet
```java
   private String keyStorePath;

   private String keyStorePassword;

   private String keyStoreAlias;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `trustStoreType` is accessed in both synchronized and unsynchronized contexts
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/NettyConnector.java`
#### Snippet
```java
   private String trustStoreProvider;

   private String trustStoreType;

   private String trustStorePath;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `bootstrap` is accessed in both synchronized and unsynchronized contexts
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/NettyConnector.java`
#### Snippet
```java
   private Class<? extends Channel> channelClazz;

   private Bootstrap bootstrap;

   private ChannelGroup channelGroup;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `keyStoreType` is accessed in both synchronized and unsynchronized contexts
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/NettyConnector.java`
#### Snippet
```java
   private String keyStoreProvider;

   private String keyStoreType;

   private String keyStorePath;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `connectTimeoutMillis` is accessed in both synchronized and unsynchronized contexts
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/NettyConnector.java`
#### Snippet
```java
   private EventLoopGroup group;

   private int connectTimeoutMillis;

   private final ClientProtocolManager protocolManager;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `future` is accessed in both synchronized and unsynchronized contexts
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/NettyConnector.java`
#### Snippet
```java
         private boolean closed = false;

         private java.util.concurrent.Future<?> future;

         @Override
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `trustStorePath` is accessed in both synchronized and unsynchronized contexts
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/NettyConnector.java`
#### Snippet
```java
   private String trustStoreType;

   private String trustStorePath;

   private String trustStorePassword;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `passwordCodecClass` is accessed in both synchronized and unsynchronized contexts
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/NettyConnector.java`
#### Snippet
```java
   private int localPort;

   private String passwordCodecClass;

   private String keyStoreProvider;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `keyStoreProvider` is accessed in both synchronized and unsynchronized contexts
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/NettyConnector.java`
#### Snippet
```java
   private String passwordCodecClass;

   private String keyStoreProvider;

   private String keyStoreType;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `trustStoreProvider` is accessed in both synchronized and unsynchronized contexts
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/NettyConnector.java`
#### Snippet
```java
   private String keyStoreAlias;

   private String trustStoreProvider;

   private String trustStoreType;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `port` is accessed in both synchronized and unsynchronized contexts
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/NettyConnector.java`
#### Snippet
```java
   private String host;

   private int port;

   private String localAddress;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `connections` is accessed in both synchronized and unsynchronized contexts
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/NettyConnector.java`
#### Snippet
```java
   private long batchDelay;

   private ConcurrentMap<Object, Connection> connections = new ConcurrentHashMap<>();

   private String servletPath;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `channel` is accessed in both synchronized and unsynchronized contexts
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/NettyConnector.java`
#### Snippet
```java
   class HttpHandler extends ChannelDuplexHandler {

      private Channel channel;

      private long lastSendTime = 0;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `closed` is accessed in both synchronized and unsynchronized contexts
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/NettyConnector.java`
#### Snippet
```java
      private class HttpIdleTimer implements Runnable {

         private boolean closed = false;

         private java.util.concurrent.Future<?> future;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `host` is accessed in both synchronized and unsynchronized contexts
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/NettyConnector.java`
#### Snippet
```java
   private boolean useServlet;

   private String host;

   private int port;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `channelGroup` is accessed in both synchronized and unsynchronized contexts
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/NettyConnector.java`
#### Snippet
```java
   private Bootstrap bootstrap;

   private ChannelGroup channelGroup;

   private final BufferHandler handler;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `remotingThreads` is accessed in both synchronized and unsynchronized contexts
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/NettyConnector.java`
#### Snippet
```java
   private boolean useKQueue;

   private int remotingThreads;

   private boolean useGlobalWorkerPool;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `pagingManager` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/impl/PagingStoreFactoryNIO.java`
#### Snippet
```java
   private final boolean syncNonTransactional;

   private PagingManager pagingManager;

   private final ScheduledExecutorService scheduledExecutor;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `suspiciousRecords` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/impl/Page.java`
#### Snippet
```java
   private final long pageId;

   private boolean suspiciousRecords = false;

   private volatile int numberOfMessages;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `pagingManager` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/impl/PagingStoreFactoryDatabase.java`
#### Snippet
```java
   protected final boolean syncNonTransactional;

   private PagingManager pagingManager;

   private final ScheduledExecutorService scheduledExecutor;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `executorFactory` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/impl/PagingStoreFactoryDatabase.java`
#### Snippet
```java
   private DatabaseStorageConfiguration dbConf;

   private ExecutorFactory executorFactory;

   private ExecutorFactory ioExecutorFactory;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `transactionID` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/impl/PageTransactionInfoImpl.java`
#### Snippet
```java
      AtomicIntegerFieldUpdater.newUpdater(PageTransactionInfoImpl.class, "numberOfPersistentMessages");

   private long transactionID;

   private volatile long recordID = -1;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `pagingStoreFactory` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/impl/PagingManagerImpl.java`
#### Snippet
```java
   private final HierarchicalRepository<AddressSettings> addressSettingsRepository;

   private PagingStoreFactory pagingStoreFactory;

   private volatile boolean globalFull;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `snapshotUpdater` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/impl/PagingManagerImpl.java`
#### Snippet
```java
   private final ConcurrentMap</*TransactionID*/Long, PageTransactionInfo> transactions = new ConcurrentHashMap<>();

   private ActiveMQScheduledComponent snapshotUpdater = null;

   private final SimpleString managementAddress;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `message` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/cursor/PagedReferenceImpl.java`
#### Snippet
```java
      .newUpdater(PagedReferenceImpl.class, "deliveryCount");

   protected PagedMessage message;

   private static final long UNDEFINED_DELIVERY_TIME = Long.MIN_VALUE;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `subscription` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/cursor/impl/PageSubscriptionCounterImpl.java`
#### Snippet
```java
   private static final AtomicLongFieldUpdater<PageSubscriptionCounterImpl> recordedSizeUpdater = AtomicLongFieldUpdater.newUpdater(PageSubscriptionCounterImpl.class, "recordedSize");

   private PageSubscription subscription;

   private PagingStore pagingStore;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `thread` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/MemoryManager.java`
#### Snippet
```java
   private volatile boolean started;

   private Thread thread;

   private volatile boolean low;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `completePage` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/cursor/impl/PageSubscriptionImpl.java`
#### Snippet
```java
       * This is to be set when all the messages are complete on a given page, and we cleanup the records that are marked on it
       */
      private PagePosition completePage;

      // We need a separate counter as the cursor 3124'gmay be ignoring certain values because of incomplete transactions or
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `acks` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/cursor/impl/PageSubscriptionImpl.java`
#### Snippet
```java
      private final long pageId;

      private IntObjectHashMap<PagePosition> acks = new IntObjectHashMap<>();

      // This will take DUMMY elements. This is used like a HashSet of Int
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `currentPage` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/cursor/impl/PageSubscriptionImpl.java`
#### Snippet
```java
   private class CursorIterator implements PageIterator {

      private Page currentPage;
      private LinkedListIterator<PagedMessage> currentPageIterator;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `currentDelivery` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/cursor/impl/PageSubscriptionImpl.java`
#### Snippet
```java
      }

      private PagedReference currentDelivery = null;

      private volatile PagedReference lastDelivery = null;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `empty` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/cursor/impl/PageSubscriptionImpl.java`
#### Snippet
```java
   private static final PagedReference RETRY_MARK = new PagedReferenceImpl(null, null);

   private boolean empty = true;

   /** for tests */
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `queue` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/cursor/impl/PageSubscriptionImpl.java`
#### Snippet
```java
   private final long cursorId;

   private Queue queue;

   private final boolean persistent;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `isStarted` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/NodeManager.java`
#### Snippet
```java
   private SimpleString nodeID;
   private UUID uuid;
   private boolean isStarted = false;
   private final Set<FileLockNodeManager.LockListener> lockListeners;
   public static final long NULL_NODE_ACTIVATION_SEQUENCE = -1;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `activeLockListener` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/SharedStoreBackupActivation.java`
#### Snippet
```java
   private boolean cancelFailBackChecker;

   private LockListener activeLockListener;

   private final IOCriticalErrorListener ioCriticalErrorListener;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `activationSequenceChannel` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/FileBasedNodeManager.java`
#### Snippet
```java
   private final Path activationSequencePath;
   protected FileChannel channel;
   protected FileChannel activationSequenceChannel;

   public FileBasedNodeManager(boolean replicatedBackup, File directory) {
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `activeMQServer` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/SharedNothingLiveActivation.java`
#### Snippet
```java
   private ReplicatedPolicy replicatedPolicy;

   private ActiveMQServerImpl activeMQServer;

   private ReplicationManager replicationManager;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `replicatedPolicy` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/SharedNothingLiveActivation.java`
#### Snippet
```java

   //this is how we act when we initially start as a live
   private ReplicatedPolicy replicatedPolicy;

   private ActiveMQServerImpl activeMQServer;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `replicationManager` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/SharedNothingLiveActivation.java`
#### Snippet
```java
   private ActiveMQServerImpl activeMQServer;

   private ReplicationManager replicationManager;

   private final Object replicationLock = new Object();
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `replicaPolicy` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/SharedNothingBackupActivation.java`
#### Snippet
```java

   //this is how we act when we start as a backup
   private ReplicaPolicy replicaPolicy;

   //this is the endpoint where we replicate to
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `closed` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/SharedNothingBackupActivation.java`
#### Snippet
```java
   private String nodeID;
   ClusterControl clusterControl;
   private boolean closed;
   private volatile boolean backupUpToDate = true;
   private final NetworkHealthCheck networkHealthCheck;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `backupQuorum` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/SharedNothingBackupActivation.java`
#### Snippet
```java

   private final ActiveMQServerImpl activeMQServer;
   private SharedNothingBackupQuorum backupQuorum;
   private final boolean attemptFailBack;
   private final Map<String, Object> activationParams;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `replicationEndpoint` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/SharedNothingBackupActivation.java`
#### Snippet
```java

   //this is the endpoint where we replicate to
   private ReplicationEndpoint replicationEndpoint;

   private final ActiveMQServerImpl activeMQServer;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `pauseStatusRecord` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/AddressInfo.java`
#### Snippet
```java

   private long id;
   private long pauseStatusRecord = -1;
   private boolean swept;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `storageManager` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/AddressInfo.java`
#### Snippet
```java

   private PostOffice postOffice;
   private StorageManager storageManager;
   private HierarchicalRepositoryChangeListener repositoryChangeListener;
   private long createdTimestamp = -1;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `postOffice` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/AddressInfo.java`
#### Snippet
```java
   private volatile boolean paused = false;

   private PostOffice postOffice;
   private StorageManager storageManager;
   private HierarchicalRepositoryChangeListener repositoryChangeListener;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `delivering` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/RefsOperation.java`
#### Snippet
```java
   protected boolean ignoreRedeliveryCheck = false;

   private boolean delivering = true;

   private String lingerSessionId = null;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `lingerSessionId` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/RefsOperation.java`
#### Snippet
```java
   private boolean delivering = true;

   private String lingerSessionId = null;

   public RefsOperation(Queue queue, AckReason reason, StorageManager storageManager) {
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `refsToAck` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/RefsOperation.java`
#### Snippet
```java
   private final StorageManager storageManager;
   private Queue queue;
   List<MessageReference> refsToAck = new ArrayList<>();

   List<MessageReference> pagedMessagesToPostACK = null;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `pagedMessagesToPostACK` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/RefsOperation.java`
#### Snippet
```java
   List<MessageReference> refsToAck = new ArrayList<>();

   List<MessageReference> pagedMessagesToPostACK = null;

   /**
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `metrics` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerConsumerImpl.java`
#### Snippet
```java
   }

   ServerConsumerMetrics metrics = new ServerConsumerMetrics();


```

### FieldAccessedSynchronizedAndUnsynchronized
Field `preAcknowledge` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerConsumerImpl.java`
#### Snippet
```java
   private SessionCallback callback;

   private boolean preAcknowledge;

   private final ManagementService managementService;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `largeMessage` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerConsumerImpl.java`
#### Snippet
```java
      private long sizePendingLargeMessage;

      private LargeServerMessage largeMessage;

      private final MessageReference ref;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `isClosed` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerConsumerImpl.java`
#### Snippet
```java
   private boolean anycast = false;

   private boolean isClosed = false;

   @Override
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `session` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerConsumerImpl.java`
#### Snippet
```java
   private final int minLargeMessageSize;

   private ServerSession session;

   protected final Object lock = new Object();
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `protocolContext` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerConsumerImpl.java`
#### Snippet
```java
   private Object protocolData;

   private Object protocolContext;

   private final ActiveMQServer server;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `callback` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerConsumerImpl.java`
#### Snippet
```java
   private final java.util.Deque<MessageReference> deliveringRefs = new ArrayDeque<>();

   private SessionCallback callback;

   private boolean preAcknowledge;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `started` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerConsumerImpl.java`
#### Snippet
```java
   private volatile AtomicInteger availableCredits = new AtomicInteger(0);

   private boolean started;

   private volatile CoreLargeMessageDeliverer largeMessageDeliverer = null;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `context` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerConsumerImpl.java`
#### Snippet
```java
      private long positionPendingLargeMessage;

      private LargeBodyReader context;

      private ByteBuffer chunkBytes;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `serverLockLastModified` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/FileLockNodeManager.java`
#### Snippet
```java
   private final FileChannel[] lockChannels = new FileChannel[3];

   private long serverLockLastModified;

   private final long lockAcquisitionTimeoutNanos;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `deliveryTime` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ScheduledDeliveryHandlerImpl.java`
#### Snippet
```java
   private class ScheduledDeliveryRunnable implements Runnable {

      long deliveryTime;

      private ScheduledDeliveryRunnable(final long deliveryTime) {
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `remotingConnection` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java
   protected final boolean strictUpdateDeliveryCount;

   protected RemotingConnection remotingConnection;

   protected final Map<Long, ServerConsumer> consumers = new ConcurrentHashMap<>();
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `tx` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java
   protected final ServerProducers serverProducers;

   protected Transaction tx;

   /** This will store the Transaction between xaEnd and xaPrepare or xaCommit.
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `autoCommitSends` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java
   private final int minLargeMessageSize;

   protected boolean autoCommitSends;

   protected boolean autoCommitAcks;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `globalThreadPoolSize` is accessed in both synchronized and unsynchronized contexts
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/client/ActiveMQClient.java`
#### Snippet
```java
public final class ActiveMQClient {

   private static int globalThreadPoolSize;

   private static int globalScheduledThreadPoolSize;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `initializeNodeId` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/jdbc/JdbcSharedStateManager.java`
#### Snippet
```java
   private String readNodeId;
   private String writeNodeId;
   private String initializeNodeId;
   private String readState;
   private String writeState;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `maxUsage` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/files/FileStoreMonitor.java`
#### Snippet
```java
   private final Set<Callback> callbackList = new HashSet<>();
   private final Set<FileStore> stores = new HashSet<>();
   private double maxUsage;
   private final Object monitorLock = new Object();
   private final IOCriticalErrorListener ioCriticalErrorListener;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `scheduledLiveLock` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/jdbc/JdbcNodeManager.java`
#### Snippet
```java
   private final Supplier<? extends ScheduledLeaseLock> scheduledBackupLockFactory;
   private SharedStateManager sharedStateManager;
   private ScheduledLeaseLock scheduledLiveLock;
   private ScheduledLeaseLock scheduledBackupLock;
   private final long lockAcquisitionTimeoutMillis;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `sharedStateManager` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/jdbc/JdbcNodeManager.java`
#### Snippet
```java
   private final Supplier<? extends ScheduledLeaseLock> scheduledLiveLockFactory;
   private final Supplier<? extends ScheduledLeaseLock> scheduledBackupLockFactory;
   private SharedStateManager sharedStateManager;
   private ScheduledLeaseLock scheduledLiveLock;
   private ScheduledLeaseLock scheduledBackupLock;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `scheduledBackupLock` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/jdbc/JdbcNodeManager.java`
#### Snippet
```java
   private SharedStateManager sharedStateManager;
   private ScheduledLeaseLock scheduledLiveLock;
   private ScheduledLeaseLock scheduledBackupLock;
   private final long lockAcquisitionTimeoutMillis;
   private volatile boolean interrupted = false;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `expectedBindings` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/group/impl/LocalGroupingHandler.java`
#### Snippet
```java
    * During a small window between the server is started and the wait wasn't called yet, this will contain bindings that were already added
    */
   private List<SimpleString> expectedBindings = new LinkedList<>();

   private final long groupTimeout;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `started` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/group/impl/LocalGroupingHandler.java`
#### Snippet
```java
   private final ScheduledExecutorService scheduledExecutor;

   private boolean started;

   private ScheduledFuture reaperFuture;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `backupConnectors` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/BackupManager.java`
#### Snippet
```java
   private ClusterManager clusterManager;

   List<BackupConnector> backupConnectors = new ArrayList<>();

   private boolean started;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `server` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/BackupManager.java`
#### Snippet
```java
   private static final Logger logger = LoggerFactory.getLogger(MethodHandles.lookup().lookupClass());

   private ActiveMQServer server;
   private Executor executor;
   private ScheduledExecutorService scheduledExecutor;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `started` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/BackupManager.java`
#### Snippet
```java
   List<BackupConnector> backupConnectors = new ArrayList<>();

   private boolean started;

   public BackupManager(ActiveMQServer server,
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `haManager` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/ClusterManager.java`
#### Snippet
```java
   private ClusterController clusterController;

   private HAManager haManager;

   private final Map<String, BroadcastGroup> broadcastGroups = new HashMap<>();
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `defaultClusterConnection` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/ClusterManager.java`
#### Snippet
```java
   private final ScheduledExecutorService scheduledExecutor;

   private ClusterConnection defaultClusterConnection;

   private final ManagementService managementService;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `clusterController` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/ClusterManager.java`
#### Snippet
```java
   private static final Logger logger = LoggerFactory.getLogger(MethodHandles.lookup().lookupClass());

   private ClusterController clusterController;

   private HAManager haManager;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `started` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/impl/BroadcastGroupImpl.java`
#### Snippet
```java
   private final List<TransportConfiguration> connectors = new ArrayList<>();

   private boolean started;

   private final long broadCastPeriod;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `notificationService` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/impl/BroadcastGroupImpl.java`
#### Snippet
```java
   private final String uniqueID;

   private NotificationService notificationService;

   private BroadcastEndpoint endpoint;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `consumerCount` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/impl/RemoteQueueBindingImpl.java`
#### Snippet
```java
   private final Map<SimpleString, Integer> filterCounts = new HashMap<>();

   private int consumerCount;

   private final SimpleString idsHeaderName;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `quorumManager` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/qourum/SharedNothingBackupQuorum.java`
#### Snippet
```java
   }

   private QuorumManager quorumManager;

   private String targetServerID = "";
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `latch` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/qourum/SharedNothingBackupQuorum.java`
#### Snippet
```java
   private ScheduledFuture<?> decisionGuard;

   private CountDownLatch latch;

   private final Object onConnectionFailureGuard = new Object();
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `targetServerID` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/qourum/SharedNothingBackupQuorum.java`
#### Snippet
```java
   private QuorumManager quorumManager;

   private String targetServerID = "";

   private final NodeManager nodeManager;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `connection` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/qourum/SharedNothingBackupQuorum.java`
#### Snippet
```java
   private ClientSessionFactoryInternal sessionFactory;

   private CoreRemotingConnection connection;

   private final NetworkHealthCheck networkHealthCheck;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `sessionFactory` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/qourum/SharedNothingBackupQuorum.java`
#### Snippet
```java
   private final Object voteGuard = new Object();

   private ClientSessionFactoryInternal sessionFactory;

   private CoreRemotingConnection connection;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `notificationService` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/impl/BridgeImpl.java`
#### Snippet
```java
   private int retryCount = 0;

   private NotificationService notificationService;

   private boolean keepConnecting = true;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `retryCount` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/impl/BridgeImpl.java`
#### Snippet
```java
   private int reconnectAttemptsInUse;

   private int retryCount = 0;

   private NotificationService notificationService;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `server` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/impl/BridgeImpl.java`
#### Snippet
```java
   private boolean keepConnecting = true;

   private ActiveMQServer server;

   private final BridgeMetrics metrics = new BridgeMetrics();
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `configuration` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/impl/BridgeImpl.java`
#### Snippet
```java
   private final BridgeMetrics metrics = new BridgeMetrics();

   private BridgeConfiguration configuration;

   public BridgeImpl(final ServerLocatorInternal serverLocator,
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `blockedOnFlowControl` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/impl/BridgeImpl.java`
#### Snippet
```java
   private final Object connectionGuard = new Object();

   private boolean blockedOnFlowControl;

   /**
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `started` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/qourum/QuorumManager.java`
#### Snippet
```java
   private final Map<SimpleString, QuorumVoteHandler> handlers = new HashMap<>();

   private boolean started = false;

   /**
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `bridge` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/impl/ClusterConnectionImpl.java`
#### Snippet
```java
   private class MessageFlowRecordImpl implements MessageFlowRecord {

      private BridgeImpl bridge;

      private final long eventUID;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `serverLocator` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/impl/ClusterConnectionImpl.java`
#### Snippet
```java
   private final ClusterConnector clusterConnector;

   private ServerLocatorInternal serverLocator;

   private final TransportConfiguration connector;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `state` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/federation/FederationManager.java`
#### Snippet
```java

   private Map<String, Federation> federations = new HashMap<>();
   private State state;

   enum State {
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `federations` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/federation/FederationManager.java`
#### Snippet
```java
   private final ActiveMQServer server;

   private Map<String, Federation> federations = new HashMap<>();
   private State state;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `started` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/federation/FederatedAbstract.java`
#### Snippet
```java
   protected WildcardConfiguration wildcardConfiguration;
   protected final Map<FederatedConsumerKey, FederatedQueueConsumer> remoteQueueConsumers = new HashMap<>();
   private boolean started;

   public FederatedAbstract(Federation federation, ActiveMQServer server, FederationUpstream upstream) {
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `server` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/federation/FederatedAbstract.java`
#### Snippet
```java
   private static final WildcardConfiguration DEFAULT_WILDCARD_CONFIGURATION = new WildcardConfiguration();
   protected final Federation federation;
   protected ActiveMQServer server;
   protected FederationUpstream upstream;
   protected WildcardConfiguration wildcardConfiguration;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `activation` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ActiveMQServerImpl.java`
#### Snippet
```java
   private Thread activationThread;

   private Activation activation;

   private final Map<String, Object> activationParams = new HashMap<>();
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `rebuildCounters` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ActiveMQServerImpl.java`
#### Snippet
```java

   // This will be useful on tests or embedded
   private boolean rebuildCounters = true;

   private volatile SERVER_STATE state = SERVER_STATE.STOPPED;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `mbeanServer` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ActiveMQServerImpl.java`
#### Snippet
```java
   private final AtomicBoolean configurationReloadDeployed;

   private MBeanServer mbeanServer;

   private volatile SecurityStore securityStore;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `fileStoreMonitor` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ActiveMQServerImpl.java`
#### Snippet
```java
   private ReloadManager reloadManager;

   private FileStoreMonitor fileStoreMonitor;

   private final ConcurrentMap<String, ServerSession> sessions = new ConcurrentHashMap<>();
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `securityManager` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ActiveMQServerImpl.java`
#### Snippet
```java
   private final Version version;

   private ActiveMQSecurityManager securityManager;

   private final Configuration configuration;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `memoryManager` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ActiveMQServerImpl.java`
#### Snippet
```java
   private volatile ConnectorsService connectorsService;

   private MemoryManager memoryManager;

   private ReloadManager reloadManager;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `nodeManager` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ActiveMQServerImpl.java`
#### Snippet
```java
   private volatile GroupingHandler groupingHandler;

   private NodeManager nodeManager;

   // Used to identify the server on tests... useful on debugging testcases
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `haPolicy` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ActiveMQServerImpl.java`
#### Snippet
```java
   public static final String GENERIC_IGNORED_FILTER = Filter.GENERIC_IGNORED_FILTER;

   private HAPolicy haPolicy;

   // This will be useful on tests or embedded
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `propertiesFileUrl` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ActiveMQServerImpl.java`
#### Snippet
```java
   private volatile FederationManager federationManager;

   private String propertiesFileUrl;

   private final ActiveMQComponent networkCheckMonitor = new ActiveMQComponent() {
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `reloadManager` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ActiveMQServerImpl.java`
#### Snippet
```java
   private MemoryManager memoryManager;

   private ReloadManager reloadManager;

   private FileStoreMonitor fileStoreMonitor;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `federationConfiguration` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/federation/FederationDownstream.java`
#### Snippet
```java
   public static final String FEDERATION_DOWNSTREAM_NAME = "federation-downstream-name";
   private AtomicBoolean started = new AtomicBoolean();
   private FederationConfiguration federationConfiguration;

   public FederationDownstream(ActiveMQServer server, Federation federation, String name, FederationDownstreamConfiguration config,
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `clientSession` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/federation/FederationDownstream.java`
#### Snippet
```java
   private FederationDownstreamConfiguration config;
   private ClientSessionFactoryInternal clientSessionFactory;
   private ClientSessionInternal clientSession;
   private Channel channel;
   private AtomicBoolean initialized = new AtomicBoolean();
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `channel` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/federation/FederationDownstream.java`
#### Snippet
```java
   private ClientSessionFactoryInternal clientSessionFactory;
   private ClientSessionInternal clientSession;
   private Channel channel;
   private AtomicBoolean initialized = new AtomicBoolean();
   private final ScheduledExecutorService scheduledExecutorService;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `config` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/federation/FederationDownstream.java`
#### Snippet
```java
public class FederationDownstream extends AbstractFederationStream implements SessionFailureListener {

   private FederationDownstreamConfiguration config;
   private ClientSessionFactoryInternal clientSessionFactory;
   private ClientSessionInternal clientSession;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `clientSessionFactory` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/federation/FederationDownstream.java`
#### Snippet
```java

   private FederationDownstreamConfiguration config;
   private ClientSessionFactoryInternal clientSessionFactory;
   private ClientSessionInternal clientSession;
   private Channel channel;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `clientConsumer` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/federation/FederatedQueueConsumerImpl.java`
#### Snippet
```java
   private ClientSessionFactoryInternal clientSessionFactory;
   private ClientSession clientSession;
   private ClientConsumer clientConsumer;

   public FederatedQueueConsumerImpl(Federation federation, ActiveMQServer server, Transformer transformer, FederatedConsumerKey key, FederationUpstream upstream, ClientSessionCallback clientSessionCallback) {
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `clientSession` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/federation/FederatedQueueConsumerImpl.java`
#### Snippet
```java

   private ClientSessionFactoryInternal clientSessionFactory;
   private ClientSession clientSession;
   private ClientConsumer clientConsumer;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `clientSessionFactory` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/federation/FederatedQueueConsumerImpl.java`
#### Snippet
```java
   private final ClientSessionCallback clientSessionCallback;

   private ClientSessionFactoryInternal clientSessionFactory;
   private ClientSession clientSession;
   private ClientConsumer clientConsumer;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `state` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/federation/Federation.java`
#### Snippet
```java
   private final Map<String, FederationDownstream> downstreams = new HashMap<>();
   private final FederationConfiguration config;
   private FederationManager.State state;

   private final Map<String, FederationConnection> connections = new HashMap<>();
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `mBeanServer` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/ManagementContext.java`
#### Snippet
```java
   private JMXAccessControlList accessControlList;
   private JMXConnectorConfiguration jmxConnectorConfiguration;
   private ManagementConnector mBeanServer;
   private ArtemisMBeanServerGuard guardHandler;
   private ActiveMQSecurityManager securityManager;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `jmxConnectorConfiguration` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/ManagementContext.java`
#### Snippet
```java
   private volatile boolean isStarted = false;
   private JMXAccessControlList accessControlList;
   private JMXConnectorConfiguration jmxConnectorConfiguration;
   private ManagementConnector mBeanServer;
   private ArtemisMBeanServerGuard guardHandler;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `securityManager` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/ManagementContext.java`
#### Snippet
```java
   private ManagementConnector mBeanServer;
   private ArtemisMBeanServerGuard guardHandler;
   private ActiveMQSecurityManager securityManager;

   public void init() {
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `methodRoles` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/JMXAccessControlList.java`
#### Snippet
```java
      private final Pattern keyPattern;
      List<String> catchAllRoles = new ArrayList<>();
      Map<String, List<String>> methodRoles = new HashMap<>();
      Map<String, List<String>> methodPrefixRoles = new LinkedHashMap<>();

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `methodPrefixRoles` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/JMXAccessControlList.java`
#### Snippet
```java
      List<String> catchAllRoles = new ArrayList<>();
      Map<String, List<String>> methodRoles = new HashMap<>();
      Map<String, List<String>> methodPrefixRoles = new LinkedHashMap<>();

      Access(String id) {
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `future` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/HttpKeepAliveRunnable.java`
#### Snippet
```java
   private boolean closed = false;

   private Future<?> future;

   @Override
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `closed` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/HttpKeepAliveRunnable.java`
#### Snippet
```java
   private final List<HttpAcceptorHandler> handlers = new ArrayList<>();

   private boolean closed = false;

   private Future<?> future;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `closed` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/remoting/impl/invm/InVMConnection.java`
#### Snippet
```java
   private final String id;

   private boolean closed;

   // Used on tests
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `securityStore` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/impl/ManagementServiceImpl.java`
#### Snippet
```java
   private PostOffice postOffice;

   private SecurityStore securityStore;

   private PagingManager pagingManager;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `postOffice` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/impl/ManagementServiceImpl.java`
#### Snippet
```java
   private final NotificationBroadcasterSupport broadcaster;

   private PostOffice postOffice;

   private SecurityStore securityStore;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `storageManager` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/impl/ManagementServiceImpl.java`
#### Snippet
```java
   private PagingManager pagingManager;

   private StorageManager storageManager;

   private ActiveMQServer messagingServer;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `notificationsEnabled` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/impl/ManagementServiceImpl.java`
#### Snippet
```java
   private final boolean messageCounterEnabled;

   private boolean notificationsEnabled;

   private final Set<NotificationListener> listeners = new ConcurrentHashSet<>();
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `messagingServerControl` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/impl/ManagementServiceImpl.java`
#### Snippet
```java
   private HierarchicalRepository<AddressSettings> addressSettingsRepository;

   private ActiveMQServerControlImpl messagingServerControl;

   private MessageCounterManager messageCounterManager;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `pagingManager` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/impl/ManagementServiceImpl.java`
#### Snippet
```java
   private SecurityStore securityStore;

   private PagingManager pagingManager;

   private StorageManager storageManager;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `securityRepository` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/impl/ManagementServiceImpl.java`
#### Snippet
```java
   private ActiveMQServer messagingServer;

   private HierarchicalRepository<Set<Role>> securityRepository;

   private HierarchicalRepository<AddressSettings> addressSettingsRepository;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `messagingServer` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/impl/ManagementServiceImpl.java`
#### Snippet
```java
   private StorageManager storageManager;

   private ActiveMQServer messagingServer;

   private HierarchicalRepository<Set<Role>> securityRepository;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `messageCounterManager` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/impl/ManagementServiceImpl.java`
#### Snippet
```java
   private ActiveMQServerControlImpl messagingServerControl;

   private MessageCounterManager messageCounterManager;

   private final SimpleString managementNotificationAddress;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `addressSettingsRepository` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/impl/ManagementServiceImpl.java`
#### Snippet
```java
   private HierarchicalRepository<Set<Role>> securityRepository;

   private HierarchicalRepository<AddressSettings> addressSettingsRepository;

   private ActiveMQServerControlImpl messagingServerControl;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `started` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/impl/ManagementServiceImpl.java`
#### Snippet
```java
   private final SimpleString managementAddress;

   private boolean started = false;

   private final boolean messageCounterEnabled;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `threadPool` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/remoting/server/impl/RemotingServiceImpl.java`
#### Snippet
```java
   private final ManagementService managementService;

   private ExecutorService threadPool;

   private final Executor flushExecutor;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `failureCheckAndFlushThread` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/remoting/server/impl/RemotingServiceImpl.java`
#### Snippet
```java
   private final ScheduledExecutorService scheduledThreadPool;

   private FailureCheckAndFlushThread failureCheckAndFlushThread;

   private final ClusterManager clusterManager;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `defaultInvmSecurityPrincipal` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/remoting/server/impl/RemotingServiceImpl.java`
#### Snippet
```java
   private final Map<String, ProtocolManagerFactory> protocolMap = new ConcurrentHashMap<>();

   private ActiveMQPrincipal defaultInvmSecurityPrincipal;

   private ServiceRegistry serviceRegistry;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `channelClazz` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/NettyAcceptor.java`
#### Snippet
```java
   private final ClusterConnection clusterConnection;

   private Class<? extends ServerChannel> channelClazz;
   private EventLoopGroup eventLoopGroup;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `quietPeriod` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/NettyAcceptor.java`
#### Snippet
```java

   /** The amount of time we wait before new tasks are added during a shutdown period. */
   private int quietPeriod;

   /** The total amount of time we wait before a hard shutdown. */
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `eventLoopGroup` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/NettyAcceptor.java`
#### Snippet
```java

   private Class<? extends ServerChannel> channelClazz;
   private EventLoopGroup eventLoopGroup;

   private volatile ChannelGroup serverChannelGroup;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `shutdownTimeout` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/NettyAcceptor.java`
#### Snippet
```java

   /** The total amount of time we wait before a hard shutdown. */
   private int shutdownTimeout;

   private boolean paused;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `bootstrap` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/NettyAcceptor.java`
#### Snippet
```java
   private volatile ChannelGroup channelGroup;

   private ServerBootstrap bootstrap;

   private final BufferHandler handler;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `notificationService` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/NettyAcceptor.java`
#### Snippet
```java
   private final ScheduledExecutorService scheduledThreadPool;

   private NotificationService notificationService;

   /** The amount of time we wait before new tasks are added during a shutdown period. */
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `paused` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/NettyAcceptor.java`
#### Snippet
```java
   private int shutdownTimeout;

   private boolean paused;

   private BatchFlusher flusher;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `paged` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/OperationContextImpl.java`
#### Snippet
```java
   long stored = 0;
   long replicated = 0;
   long paged = 0;

   private int errorCode = -1;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `minimalPage` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/OperationContextImpl.java`
#### Snippet
```java
   long minimalStore = Long.MAX_VALUE;
   long minimalReplicated = Long.MAX_VALUE;
   long minimalPage = Long.MAX_VALUE;


```

### FieldAccessedSynchronizedAndUnsynchronized
Field `errorCode` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/OperationContextImpl.java`
#### Snippet
```java
   long paged = 0;

   private int errorCode = -1;

   private String errorMessage = null;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `minimalStore` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/OperationContextImpl.java`
#### Snippet
```java
   LinkedList<StoreOnlyTaskHolder> storeOnlyTasks;

   long minimalStore = Long.MAX_VALUE;
   long minimalReplicated = Long.MAX_VALUE;
   long minimalPage = Long.MAX_VALUE;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `stored` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/OperationContextImpl.java`
#### Snippet
```java
   volatile long pageLineUpField = 0;

   long stored = 0;
   long replicated = 0;
   long paged = 0;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `errorMessage` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/OperationContextImpl.java`
#### Snippet
```java
   private int errorCode = -1;

   private String errorMessage = null;

   private final Executor executor;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `replicated` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/OperationContextImpl.java`
#### Snippet
```java

   long stored = 0;
   long replicated = 0;
   long paged = 0;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `minimalReplicated` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/OperationContextImpl.java`
#### Snippet
```java

   long minimalStore = Long.MAX_VALUE;
   long minimalReplicated = Long.MAX_VALUE;
   long minimalPage = Long.MAX_VALUE;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `deleted` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/LargeBody.java`
#### Snippet
```java
   private SequentialFile file;

   private boolean deleted;

   public LargeBody(LargeServerMessage message, StorageManager storageManager) {
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `message` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/LargeBody.java`
#### Snippet
```java
   private long messageID = -1;

   private LargeServerMessage message;

   private boolean paged;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `bodySize` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/LargeBody.java`
#### Snippet
```java
   private static final Logger logger = LoggerFactory.getLogger(MethodHandles.lookup().lookupClass());

   private long bodySize = -1;

   long NO_PENDING_ID = -1;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `file` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/LargeBody.java`
#### Snippet
```java

   // This is to be used only for appending
   private SequentialFile file;

   private boolean deleted;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `storageManager` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/LargeBody.java`
#### Snippet
```java
   private long pendingRecordID = NO_PENDING_ID;

   StorageManager storageManager;

   private long messageID = -1;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `replicator` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/JournalStorageManager.java`
#### Snippet
```java
   protected String largeMessagesDirectory;

   protected ReplicationManager replicator;

   public JournalStorageManager(final Configuration config,
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `mirrorControllerSource` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/postoffice/impl/PostOfficeImpl.java`
#### Snippet
```java
   private final ActiveMQServer server;

   private MirrorController mirrorControllerSource;

   public PostOfficeImpl(final ActiveMQServer server,
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `exception` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/transaction/impl/TransactionImpl.java`
#### Snippet
```java
   private volatile State state = State.ACTIVE;

   private ActiveMQException exception;

   private final Object timeoutLock = new Object();
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `operations` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/transaction/impl/TransactionImpl.java`
#### Snippet
```java
   private static final Logger logger = LoggerFactory.getLogger(MethodHandles.lookup().lookupClass());

   private List<TransactionOperation> operations;

   private List<TransactionOperation> storeOperations;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `timeoutSeconds` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/transaction/impl/TransactionImpl.java`
#### Snippet
```java
   private volatile boolean containsPersistent;

   private int timeoutSeconds = -1;

   private Object protocolData;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `future` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/transaction/impl/ResourceManagerImpl.java`
#### Snippet
```java
      private boolean closed = false;

      private Future<?> future;

      @Override
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `dayCounterMax` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/messagecounter/MessageCounter.java`
#### Snippet
```java

   // per hour day counter history
   private int dayCounterMax;

   private final List<DayCounter> dayCounters;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `checkSlowReplication` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/replication/ReplicationManager.java`
#### Snippet
```java
   private ScheduledFuture<?> slowReplicationChecker;
   private long notWritableFrom;
   private boolean checkSlowReplication;
   private final ReadyListener onResume;
   private boolean isFlushing;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `notWritableFrom` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/replication/ReplicationManager.java`
#### Snippet
```java
   private final ScheduledExecutorService scheduledExecutorService;
   private ScheduledFuture<?> slowReplicationChecker;
   private long notWritableFrom;
   private boolean checkSlowReplication;
   private final ReadyListener onResume;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `slowReplicationChecker` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/replication/ReplicationManager.java`
#### Snippet
```java
   private final Executor replicationStream;
   private final ScheduledExecutorService scheduledExecutorService;
   private ScheduledFuture<?> slowReplicationChecker;
   private long notWritableFrom;
   private boolean checkSlowReplication;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `failureListener` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/replication/ReplicationManager.java`
#### Snippet
```java
   private final ExecutorFactory ioExecutorFactory;

   private SessionFailureListener failureListener;

   private CoreRemotingConnection remotingConnection;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `remotingConnection` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/replication/ReplicationManager.java`
#### Snippet
```java
   private SessionFailureListener failureListener;

   private CoreRemotingConnection remotingConnection;

   private final long maxAllowedSlownessNanos;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `journals` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/replication/ReplicationEndpoint.java`
#### Snippet
```java
   private boolean supportResponseBatching;

   private Journal[] journals;
   private final JournalLoadInformation[] journalLoadInformation = new JournalLoadInformation[2];

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `channel` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/replication/ReplicationEndpoint.java`
#### Snippet
```java
   private final ReplicationEndpointEventListener eventListener;
   private final boolean noSync = false;
   private Channel channel;
   private boolean supportResponseBatching;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `storageManager` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/replication/ReplicationEndpoint.java`
#### Snippet
```java
   private Map<JournalContent, Journal> journalsHolder = new HashMap<>();

   private StorageManager storageManager;

   private PagingManager pageManager;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `journalsHolder` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/replication/ReplicationEndpoint.java`
#### Snippet
```java
    * {@code null} on an up-to-date server.
    */
   private Map<JournalContent, Journal> journalsHolder = new HashMap<>();

   private StorageManager storageManager;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `executor` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/replication/ReplicationEndpoint.java`
#### Snippet
```java
   private volatile boolean started;

   private Executor executor;

   private List<Interceptor> outgoingInterceptors = null;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `singleThreadExecutor` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/AbstractJournalStorageManager.java`
#### Snippet
```java
   final Executor executor;

   Executor singleThreadExecutor;

   private final boolean syncTransactional;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `messageJournal` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/AbstractJournalStorageManager.java`
#### Snippet
```java
   }

   protected Journal messageJournal;

   protected Journal bindingsJournal;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `bindingsJournal` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/AbstractJournalStorageManager.java`
#### Snippet
```java
   protected Journal messageJournal;

   protected Journal bindingsJournal;

   protected volatile boolean started;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `idGenerator` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/AbstractJournalStorageManager.java`
#### Snippet
```java
   private static final long CHECKPOINT_BATCH_SIZE = Integer.MAX_VALUE;

   protected BatchingIDGenerator idGenerator;

   protected final ExecutorFactory ioExecutorFactory;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `journalLoaded` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/AbstractJournalStorageManager.java`
#### Snippet
```java
   private final boolean syncNonTransactional;

   protected boolean journalLoaded = false;

   protected final IOCriticalErrorListener ioCriticalErrorListener;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `internalQueue` is accessed in both synchronized and unsynchronized contexts
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java
   private final ArtemisExecutor executor;

   private boolean internalQueue;

   private volatile long lastDirectDeliveryCheck = 0;
```

## RuleId[id=PointlessBitwiseExpression]
### PointlessBitwiseExpression
`~(pow2alignment - 1)` can be replaced with '-pow2alignment'
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/PowerOf2Util.java`
#### Snippet
```java
    */
   public static int align(final int value, final int pow2alignment) {
      return (value + (pow2alignment - 1)) & ~(pow2alignment - 1);
   }

```

### PointlessBitwiseExpression
`(byte3 & 0x3F) << 0` can be replaced with '(byte3 \& 0x3F)'
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/UTF8Util.java`
#### Snippet
```java
                  byte2 = PlatformDependent.getByte(bytes, count++);
                  byte3 = PlatformDependent.getByte(bytes, count++);
                  chars[charCount++] = (char) ((c & 0x0F) << 12 | (byte2 & 0x3F) << 6 | (byte3 & 0x3F) << 0);
                  break;
               default:
```

### PointlessBitwiseExpression
`charAtPos >> 0` can be replaced with 'charAtPos'
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/UTF8Util.java`
#### Snippet
```java
            bytes[charCount++] = (byte) (0xE0 | charAtPos >> 12 & 0x0F);
            bytes[charCount++] = (byte) (0x80 | charAtPos >> 6 & 0x3F);
            bytes[charCount++] = (byte) (0x80 | charAtPos >> 0 & 0x3F);
         } else {
            bytes[charCount++] = (byte) (0xC0 | charAtPos >> 6 & 0x1F);
```

### PointlessBitwiseExpression
`charAtPos >> 0` can be replaced with 'charAtPos'
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/UTF8Util.java`
#### Snippet
```java
         } else {
            bytes[charCount++] = (byte) (0xC0 | charAtPos >> 6 & 0x1F);
            bytes[charCount++] = (byte) (0x80 | charAtPos >> 0 & 0x3F);
         }
      }
```

### PointlessBitwiseExpression
`charAtPos >> 0` can be replaced with 'charAtPos'
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/UTF8Util.java`
#### Snippet
```java
            PlatformDependent.putByte(bytes, charCount++, (byte) (0xE0 | charAtPos >> 12 & 0x0F));
            PlatformDependent.putByte(bytes, charCount++, (byte) (0x80 | charAtPos >> 6 & 0x3F));
            PlatformDependent.putByte(bytes, charCount++, (byte) (0x80 | charAtPos >> 0 & 0x3F));
         } else {
            PlatformDependent.putByte(bytes, charCount++, (byte) (0xC0 | charAtPos >> 6 & 0x1F));
```

### PointlessBitwiseExpression
`charAtPos >> 0` can be replaced with 'charAtPos'
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/UTF8Util.java`
#### Snippet
```java
         } else {
            PlatformDependent.putByte(bytes, charCount++, (byte) (0xC0 | charAtPos >> 6 & 0x1F));
            PlatformDependent.putByte(bytes, charCount++, (byte) (0x80 | charAtPos >> 0 & 0x3F));
         }
      }
```

### PointlessBitwiseExpression
`(byte3 & 0x3F) << 0` can be replaced with '(byte3 \& 0x3F)'
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/UTF8Util.java`
#### Snippet
```java
                  byte2 = PlatformDependent.getByte(addressBytes + count++);
                  byte3 = PlatformDependent.getByte(addressBytes + count++);
                  chars[charCount++] = (char) ((c & 0x0F) << 12 | (byte2 & 0x3F) << 6 | (byte3 & 0x3F) << 0);
                  break;
               default:
```

### PointlessBitwiseExpression
`(byte3 & 0x3F) << 0` can be replaced with '(byte3 \& 0x3F)'
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/UTF8Util.java`
#### Snippet
```java
                  byte2 = bytes[count++];
                  byte3 = bytes[count++];
                  chars[charCount++] = (char) ((c & 0x0F) << 12 | (byte2 & 0x3F) << 6 | (byte3 & 0x3F) << 0);
                  break;
               default:
```

### PointlessBitwiseExpression
`charAtPos >> 0` can be replaced with 'charAtPos'
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/UTF8Util.java`
#### Snippet
```java
            PlatformDependent.putByte(addressBytes + charCount++, (byte) (0xE0 | charAtPos >> 12 & 0x0F));
            PlatformDependent.putByte(addressBytes + charCount++, (byte) (0x80 | charAtPos >> 6 & 0x3F));
            PlatformDependent.putByte(addressBytes + charCount++, (byte) (0x80 | charAtPos >> 0 & 0x3F));
         } else {
            PlatformDependent.putByte(addressBytes + charCount++, (byte) (0xC0 | charAtPos >> 6 & 0x1F));
```

### PointlessBitwiseExpression
`charAtPos >> 0` can be replaced with 'charAtPos'
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/UTF8Util.java`
#### Snippet
```java
         } else {
            PlatformDependent.putByte(addressBytes + charCount++, (byte) (0xC0 | charAtPos >> 6 & 0x1F));
            PlatformDependent.putByte(addressBytes + charCount++, (byte) (0x80 | charAtPos >> 0 & 0x3F));
         }
      }
```

### PointlessBitwiseExpression
`(getByte(index + 3) & 0xff) << 0` can be replaced with '(getByte(index + 3) \& 0xff)'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/LargeMessageControllerImpl.java`
#### Snippet
```java
      return (getByte(index) & 0xff) << 24 | (getByte(index + 1) & 0xff) << 16 |
         (getByte(index + 2) & 0xff) << 8 |
         (getByte(index + 3) & 0xff) << 0;
   }

```

### PointlessBitwiseExpression
`(getByte(index + 3) & 0xff) << 0` can be replaced with '(getByte(index + 3) \& 0xff)'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/LargeMessageControllerImpl.java`
#### Snippet
```java
      return (getByte(index) & 0xff) << 24 | (getByte(index + 1) & 0xff) << 16 |
         (getByte(index + 2) & 0xff) << 8 |
         (getByte(index + 3) & 0xff) << 0;
   }

```

### PointlessBitwiseExpression
`(getByte(index + 2) & 0xff) << 0` can be replaced with '(getByte(index + 2) \& 0xff)'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/LargeMessageControllerImpl.java`
#### Snippet
```java

   public int getUnsignedMedium(final long index) {
      return (getByte(index) & 0xff) << 16 | (getByte(index + 1) & 0xff) << 8 | (getByte(index + 2) & 0xff) << 0;
   }

```

### PointlessBitwiseExpression
`((long) getByte(index + 7) & 0xff) << 0` can be replaced with '((long) getByte(index + 7) \& 0xff)'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/LargeMessageControllerImpl.java`
#### Snippet
```java
         ((long) getByte(index + 5) & 0xff) << 16 |
         ((long) getByte(index + 6) & 0xff) << 8 |
         ((long) getByte(index + 7) & 0xff) << 0;
   }

```

### PointlessBitwiseExpression
`(getByte(index + 2) & 0xff) << 0` can be replaced with '(getByte(index + 2) \& 0xff)'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/LargeMessageControllerImpl.java`
#### Snippet
```java

   private int getUnsignedMedium(final int index) {
      return (getByte(index) & 0xff) << 16 | (getByte(index + 1) & 0xff) << 8 | (getByte(index + 2) & 0xff) << 0;
   }

```

### PointlessBitwiseExpression
`((long) getByte(index + 7) & 0xff) << 0` can be replaced with '((long) getByte(index + 7) \& 0xff)'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/LargeMessageControllerImpl.java`
#### Snippet
```java
         ((long) getByte(index + 5) & 0xff) << 16 |
         ((long) getByte(index + 6) & 0xff) << 8 |
         ((long) getByte(index + 7) & 0xff) << 0;
   }

```

## RuleId[id=HtmlWrongAttributeValue]
### HtmlWrongAttributeValue
Wrong attribute value
in `log/indexing-diagnostic/project.15375f63/diagnostic-2023-04-16-17-27-23.706.html`
#### Snippet
```java
              <td>0</td>
              <td>0</td>
              <td><textarea rows="10" cols="75" readonly="true" placeholder="empty" style="white-space: pre; border: none">Not collected for refresh</textarea></td>
            </tr>
          </tbody>
```

## RuleId[id=CallToStringConcatCanBeReplacedByOperator]
### CallToStringConcatCanBeReplacedByOperator
Call to `concat()` can be replaced with '+' expression
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/converter/AMQPMessageSupport.java`
#### Snippet
```java
      if (!key.startsWith(X_OPT_PREFIX.toString())) {
         if (key.startsWith(AMQ_PROPERTY_PREFIX)) {
            return X_OPT_PREFIX.concat(key.substring(AMQ_PROPERTY_PREFIX.length()).replace('_', '-'));
         }

```

### CallToStringConcatCanBeReplacedByOperator
Call to `concat()` can be replaced with '+' expression
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/management/ResourceNames.java`
#### Snippet
```java

   private static SimpleString getRetroactiveResourceName(String prefix, String delimiter, SimpleString address, String resourceType) {
      return SimpleString.toSimpleString(prefix.concat(address.toString()).concat(delimiter).concat(resourceType).concat(delimiter).concat(RETROACTIVE_SUFFIX));
   }

```

### CallToStringConcatCanBeReplacedByOperator
Call to `concat()` can be replaced with '+' expression
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/management/ResourceNames.java`
#### Snippet
```java

   private static SimpleString getRetroactiveResourceName(String prefix, String delimiter, SimpleString address, String resourceType) {
      return SimpleString.toSimpleString(prefix.concat(address.toString()).concat(delimiter).concat(resourceType).concat(delimiter).concat(RETROACTIVE_SUFFIX));
   }

```

### CallToStringConcatCanBeReplacedByOperator
Call to `concat()` can be replaced with '+' expression
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/management/ResourceNames.java`
#### Snippet
```java

   private static SimpleString getRetroactiveResourceName(String prefix, String delimiter, SimpleString address, String resourceType) {
      return SimpleString.toSimpleString(prefix.concat(address.toString()).concat(delimiter).concat(resourceType).concat(delimiter).concat(RETROACTIVE_SUFFIX));
   }

```

### CallToStringConcatCanBeReplacedByOperator
Call to `concat()` can be replaced with '+' expression
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/management/ResourceNames.java`
#### Snippet
```java

   private static SimpleString getRetroactiveResourceName(String prefix, String delimiter, SimpleString address, String resourceType) {
      return SimpleString.toSimpleString(prefix.concat(address.toString()).concat(delimiter).concat(resourceType).concat(delimiter).concat(RETROACTIVE_SUFFIX));
   }

```

### CallToStringConcatCanBeReplacedByOperator
Call to `concat()` can be replaced with '+' expression
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/management/ResourceNames.java`
#### Snippet
```java

   private static SimpleString getRetroactiveResourceName(String prefix, String delimiter, SimpleString address, String resourceType) {
      return SimpleString.toSimpleString(prefix.concat(address.toString()).concat(delimiter).concat(resourceType).concat(delimiter).concat(RETROACTIVE_SUFFIX));
   }

```

### CallToStringConcatCanBeReplacedByOperator
Call to `concat()` can be replaced with '+' expression
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/management/ResourceNames.java`
#### Snippet
```java

   public static SimpleString getRetroactiveResourceQueueName(String prefix, String delimiter, SimpleString address, RoutingType routingType) {
      return getRetroactiveResourceName(prefix, delimiter, address, trimLastCharacter(QUEUE).concat(delimiter).concat(routingType.toString().toLowerCase()));
   }

```

### CallToStringConcatCanBeReplacedByOperator
Call to `concat()` can be replaced with '+' expression
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/management/ResourceNames.java`
#### Snippet
```java

   public static SimpleString getRetroactiveResourceQueueName(String prefix, String delimiter, SimpleString address, RoutingType routingType) {
      return getRetroactiveResourceName(prefix, delimiter, address, trimLastCharacter(QUEUE).concat(delimiter).concat(routingType.toString().toLowerCase()));
   }

```

## RuleId[id=StringRepeatCanBeUsed]
### StringRepeatCanBeUsed
Can be replaced with 'String.repeat()'
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Create.java`
#### Snippet
```java
      int size = Math.min(message.length(), 80);
      StringBuffer buffer = new StringBuffer(size);
      for (int i = 0; i < size; i++) {
         buffer.append("*");
      }
```

### StringRepeatCanBeUsed
Can be replaced with 'String.repeat()'
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/perf/PaddingDecimalFormat.java`
#### Snippet
```java
      if (padLength > 0) {
         final int initialPadLength = pad.length();
         for (int i = initialPadLength; i < padLength; i++) {
            pad.append(' ');
         }
```

### StringRepeatCanBeUsed
Can be replaced with 'String.repeat()'
in `artemis-jdbc-store/src/main/java/org/apache/activemq/artemis/jdbc/store/drivers/JDBCUtils.java`
#### Snippet
```java
      do {
         errorMessage.append('\n');
         for (int i = 0; i < level; i++) {
            errorMessage.append(' ');
         }
```

## RuleId[id=SynchronizeOnThis]
### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Suppliers.java`
#### Snippet
```java
         // A 2-field variant of Double Checked Locking.
         if (!initialized) {
            synchronized (this) {
               if (!initialized) {
                  T t = delegate.get();
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/io/nio/NIOSequentialFile.java`
#### Snippet
```java


      notifyAll();
   }

```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/io/buffer/TimedBuffer.java`
#### Snippet
```java
   public boolean flushBatch() {
      try (ArtemisCloseable measure = measureCritical(CRITICAL_PATH_FLUSH)) {
         synchronized (this) {
            if (!started) {
               throw new IllegalStateException("TimedBuffer is not started");
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/io/buffer/TimedBuffer.java`
#### Snippet
```java
   public void start() {
      try (ArtemisCloseable critical = measureCritical(CRITICAL_PATH_START)) {
         synchronized (this) {
            if (started) {
               return;
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/io/buffer/TimedBuffer.java`
#### Snippet
```java
   public void addBytes(final ActiveMQBuffer bytes, final boolean sync, final IOCallback callback) {
      try (ArtemisCloseable measure = measureCritical(CRITICAL_PATH_ADD_BYTES)) {
         synchronized (this) {
            if (!started) {
               throw new IllegalStateException("TimedBuffer is not started");
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/io/buffer/TimedBuffer.java`
#### Snippet
```java
   public void setObserver(final TimedBufferObserver observer) {
      try (AutoCloseable measure = measureCritical(CRITICAL_PATH_SET_OBSERVER)) {
         synchronized (this) {
            if (bufferObserver != null) {
               flush();
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/io/buffer/TimedBuffer.java`
#### Snippet
```java
      try (ArtemisCloseable measure = measureCritical(CRITICAL_PATH_STOP)) {
         // add critical analyzer here.... <<<<
         synchronized (this) {
            try {
               if (!started) {
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/io/buffer/TimedBuffer.java`
#### Snippet
```java
   public boolean checkSize(final int sizeChecked) {
      try (ArtemisCloseable measure = measureCritical(CRITICAL_PATH_CHECK_SIZE)) {
         synchronized (this) {
            if (!started) {
               throw new IllegalStateException("TimedBuffer is not started");
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/io/buffer/TimedBuffer.java`
#### Snippet
```java
   public void addBytes(final EncodingSupport bytes, final boolean sync, final IOCallback callback) {
      try (ArtemisCloseable measure = measureCritical(CRITICAL_PATH_ADD_BYTES)) {
         synchronized (this) {
            if (!started) {
               throw new IllegalStateException("TimedBuffer is not started");
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/io/aio/AIOSequentialFileFactory.java`
#### Snippet
```java

      public void bufferDone(final ByteBuffer buffer) {
         synchronized (this) {

            if (stopped) {
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/LinkedListImpl.java`
#### Snippet
```java
      @Override
      public E next() {
         synchronized (LinkedListImpl.this) {
            Node<E> e = getNode();

```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/LinkedListImpl.java`
#### Snippet
```java
      @Override
      public void remove() {
         synchronized (LinkedListImpl.this) {
            if (last == null) {
               throw new NoSuchElementException();
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/LinkedListImpl.java`
#### Snippet
```java
      @Override
      public boolean hasNext() {
         synchronized (LinkedListImpl.this) {
            Node<E> e = getNode();

```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/perf/ProducerBenchmark.java`
#### Snippet
```java
      final Connection[] openedConnections = connections.toArray(new Connection[connections.size()]);
      this.allGeneratorClosed = allGeneratorClosed.whenCompleteAsync((res, error) -> {
         synchronized (this) {
            generators = null;
            started = false;
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/bridge/impl/JMSBridgeImpl.java`
#### Snippet
```java
      @Override
      public void failoverEvent(FailoverEventType eventType) {
         synchronized (this) {
            lastEvent = eventType;
            if (eventType == FailoverEventType.FAILURE_DETECTED) {
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/bridge/impl/JMSBridgeImpl.java`
#### Snippet
```java
               }
            }
            this.notify();
         }
      }
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/bridge/impl/JMSBridgeImpl.java`
#### Snippet
```java
      }

      synchronized (this) {
         logger.trace("Stopping {}", this);
         Connection sourceConn = this.sourceConn;
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/bridge/impl/JMSBridgeImpl.java`
#### Snippet
```java
         boolean timedOut = false;
         FailoverEventType result = null;
         synchronized (this) {
            while ((lastEvent == null || lastEvent == FailoverEventType.FAILURE_DETECTED)) {
               try {
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/bridge/impl/JMSBridgeImpl.java`
#### Snippet
```java
                  }
                  start = System.currentTimeMillis();
                  this.wait(toWait);
               } catch (InterruptedException e) {
               } finally {
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/amq/AMQConsumer.java`
#### Snippet
```java

   private Set<MessageReference> guardedInitializationOfRolledBackMessageRefs() {
      synchronized (this) {
         Set<MessageReference> rollbackedMessageRefs = this.rolledbackMessageRefs;
         if (rollbackedMessageRefs == null) {
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/server/impl/JMSServerManagerImpl.java`
#### Snippet
```java
   @Override
   public void stop() throws Exception {
      synchronized (this) {
         if (!startCalled) {
            return;
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/server/impl/JMSServerManagerImpl.java`
#### Snippet
```java
   public void deActivate() {
      try {
         synchronized (this) {
            if (!active) {
               return;
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQJMSContext.java`
#### Snippet
```java
   private void checkSession() {
      if (session == null) {
         synchronized (this) {
            if (closed)
               throw new IllegalStateRuntimeException("Context is closed");
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQJMSContext.java`
#### Snippet
```java
      threadAwareContext.assertNotMessageListenerThreadRuntime();
      try {
         synchronized (this) {
            if (session != null)
               session.close();
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jndi/LazyCreateContext.java`
#### Snippet
```java
       * threads initially then the internalBind fails for the second thread.
       */
      synchronized (this) {
         try {
            return super.lookup(name);
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/ActiveMQProtonRemotingConnection.java`
#### Snippet
```java
   @Override
   public void destroy() {
      synchronized (this) {
         if (destroyed) {
            return;
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/connect/AMQPBrokerConnectionChannelHandler.java`
#### Snippet
```java
   @Override
   public void channelInactive(ChannelHandlerContext ctx) throws Exception {
      synchronized (this) {
         if (active) {
            listenerExecutor.execute(() -> listener.connectionDestroyed(channelId(ctx.channel())));
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-jdbc-store/src/main/java/org/apache/activemq/artemis/jdbc/store/journal/JDBCJournalImpl.java`
#### Snippet
```java
      }

      synchronized (this) {
         if (record.isTransactional() || record.getRecordType() == JDBCJournalRecord.PREPARE_RECORD) {
            addTxRecord(record);
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ClientProducerCreditsImpl.java`
#### Snippet
```java
   @Override
   public void receiveCredits(final int credits) {
      synchronized (this) {
         super.receiveCredits(credits);
      }
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ClientProducerCreditsImpl.java`
#### Snippet
```java

      boolean tryAcquire;
      synchronized (this) {
         tryAcquire = semaphore.tryAcquire(credits);
      }
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ClientProducerCreditsImpl.java`
#### Snippet
```java
   protected void afterAcquired(int credits) throws ActiveMQAddressFullException {
      // check to see if the blocking mode is FAIL on the server
      synchronized (this) {
         super.afterAcquired(credits);

```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/AsynchronousProducerCreditsImpl.java`
#### Snippet
```java
   @Override
   public void receiveCredits(int credits) {
      synchronized (this) {
         super.receiveCredits(credits);
         balance += credits;
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/AsynchronousProducerCreditsImpl.java`
#### Snippet
```java
   @Override
   public void releaseOutstanding() {
      synchronized (this) {
         balance = 0;
         callback.onCreditsFlow(true, this);
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/AsynchronousProducerCreditsImpl.java`
#### Snippet
```java
   @Override
   protected synchronized void actualAcquire(int credits) {
      synchronized (this) {
         balance -= credits;
         if (logger.isDebugEnabled()) {
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/AbstractProducerCreditsImpl.java`
#### Snippet
```java
   @Override
   public void receiveCredits(final int credits) {
      synchronized (this) {
         arriving -= credits;
      }
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/AbstractProducerCreditsImpl.java`
#### Snippet
```java
      int toRequest = -1;

      synchronized (this) {
         if (getBalance() + arriving < needed) {
            toRequest = needed - arriving;
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/AbstractProducerCreditsImpl.java`
#### Snippet
```java
      }

      synchronized (this) {
         pendingCredits -= credits;
      }
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/AbstractProducerCreditsImpl.java`
#### Snippet
```java
   protected void requestCredits(final int credits) {
      logger.debug("Request {} credits on address {}", credits, address);
      synchronized (this) {
         pendingCredits += credits;
         arriving += credits;
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ClientProducerCreditManagerImpl.java`
#### Snippet
```java
         ClientProducerCredits credits;

         synchronized (this) {
            credits = producerCredits.get(address);

```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/Topology.java`
#### Snippet
```java
    */
   public void resendNode(final String nodeId) {
      synchronized (this) {
         TopologyMemberImpl memberInput = topology.get(nodeId);
         if (memberInput != null) {
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/Topology.java`
#### Snippet
```java
    */
   public void updateAsLive(final String nodeId, final TopologyMemberImpl memberInput) {
      synchronized (this) {
         if (logger.isDebugEnabled()) {
            logger.debug("{}::node {}={}", this, nodeId, memberInput);
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/Topology.java`
#### Snippet
```java
            final Map<String, TopologyMemberImpl> copy;

            synchronized (Topology.this) {
               copy = new HashMap<>(topology);
            }
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/Topology.java`
#### Snippet
```java
   public Collection<TopologyMemberImpl> getMembers() {
      ArrayList<TopologyMemberImpl> members;
      synchronized (this) {
         members = new ArrayList<>(topology.values());
      }
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/Topology.java`
#### Snippet
```java
      }

      synchronized (this) {
         member = topology.get(nodeId);
         if (member != null) {
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/Topology.java`
#### Snippet
```java
      }

      synchronized (this) {
         TopologyMemberImpl currentMember = topology.get(nodeId);

```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/Topology.java`
#### Snippet
```java
      }

      synchronized (this) {
         TopologyMemberImpl currentMember = getMember(nodeId);
         if (currentMember == null) {
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/LargeMessageControllerImpl.java`
#### Snippet
```java
      while (!streamEnded && handledException == null) {
         try {
            this.wait(timeWait == 0 ? readTimeout : timeWait);
         } catch (InterruptedException e) {
            throw new ActiveMQInterruptedException(e);
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/LargeMessageControllerImpl.java`
#### Snippet
```java
      boolean continues = false;

      synchronized (this) {
         if (currentPacket != null) {
            sendPacketToOutput(output, currentPacket);
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/LargeMessageControllerImpl.java`
#### Snippet
```java
      this.handledException = ActiveMQClientMessageBundle.BUNDLE.largeMessageInterrupted();

      synchronized (this) {
         int totalSize = 0;
         LargeData polledPacket = null;
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/LargeMessageControllerImpl.java`
#### Snippet
```java
         streamClosed = true;

         notifyAll();
      }
   }
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/LargeMessageControllerImpl.java`
#### Snippet
```java
      int flowControlCredit = 0;

      synchronized (this) {
         packetAdded = true;
         if (outStream != null) {
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/LargeMessageControllerImpl.java`
#### Snippet
```java
               handledException = e;
            } finally {
               notifyAll();
            }
         } else {
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ClientConsumerImpl.java`
#### Snippet
```java
         closed = true;

         synchronized (this) {
            if (receiverThread != null) {
               // Wake up any receive() thread that might be waiting
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ClientConsumerImpl.java`
#### Snippet
```java
            if (receiverThread != null) {
               // Wake up any receive() thread that might be waiting
               notify();
            }

```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ClientConsumerImpl.java`
#### Snippet
```java
   @Override
   public void clear(boolean waitForOnMessage) throws ActiveMQException {
      synchronized (this) {
         // Need to send credits for the messages in the buffer

```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ClientConsumerImpl.java`
#### Snippet
```java
      }

      synchronized (this) {
         if (stopped) {
            return;
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ClientConsumerImpl.java`
#### Snippet
```java
         failedOver = false;

         synchronized (this) {
            message = buffer.poll();
         }
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ClientConsumerImpl.java`
#### Snippet
```java
            ClientMessageInternal m = null;

            synchronized (this) {
               while ((stopped || (m = buffer.poll()) == null) && !closed && toWait > 0) {
                  if (start == -1) {
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ClientConsumerImpl.java`
#### Snippet
```java

                  try {
                     wait(toWait);
                  } catch (InterruptedException e) {
                     throw new ActiveMQInterruptedException(e);
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ClientConsumerImpl.java`
#### Snippet
```java
         }
      } else {
         notify();
      }
   }
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ServerLocatorImpl.java`
#### Snippet
```java
      ClientSessionFactoryInternal returnFactory = null;

      synchronized (this) {
         // static list of initial connectors
         if (getNumInitialConnectors() > 0 && discoveryGroup == null) {
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ServerLocatorImpl.java`
#### Snippet
```java

      if (discoveryGroup != null) {
         synchronized (this) {
            try {
               discoveryGroup.stop();
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ServerLocatorImpl.java`
#### Snippet
```java
      }

      synchronized (this) {
         if (usedTopology != null && config.useTopologyForLoadBalancing) {
            return usedTopology.length;
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ServerLocatorImpl.java`
#### Snippet
```java
      }

      synchronized (this) {
         if (usedTopology != null && config.useTopologyForLoadBalancing && !useInitConnector) {
            logger.trace("Selecting connector from topology.");
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ServerLocatorImpl.java`
#### Snippet
```java
      ClientSessionFactoryInternal factory = null;

      synchronized (this) {
         boolean retry = true;
         int attempts = 0;
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ClientSessionImpl.java`
#### Snippet
```java

      if (successful) {
         synchronized (this) {
            if (closed) {
               return;
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ClientSessionImpl.java`
#### Snippet
```java
      logger.debug("calling cleanup on {}", this);

      synchronized (this) {
         closed = true;

```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ClientSessionImpl.java`
#### Snippet
```java
      boolean suc = true;

      synchronized (this) {
         if (closed) {
            return true;
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/cluster/DiscoveryGroup.java`
#### Snippet
```java
         logger.debug("Stopping discovery. There's an exception just as a trace where it happened", new Exception("trace"));
      }
      synchronized (this) {
         if (!started) {
            return;
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/cluster/DiscoveryGroup.java`
#### Snippet
```java


               synchronized (DiscoveryGroup.this) {
                  for (DiscoveryEntry entry : entriesRead) {
                     if (connectors.put(originatingNodeID, entry) == null) {
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/message/impl/CoreMessage.java`
#### Snippet
```java
         return properties.containsProperty(key);
      }
      synchronized (this) {
         final ByteBuf buffer = this.buffer;
         // acquiring the lock here, although heavy-weight, is the safer way to do this,
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/message/impl/CoreMessage.java`
#### Snippet
```java
   private void internalWritableBuffer() {
      if (writableBuffer == null) {
         synchronized (this) {
            if (writableBuffer == null) {
               ResetLimitWrappedActiveMQBuffer writableBuffer = new ResetLimitWrappedActiveMQBuffer(BODY_OFFSET, buffer.duplicate(), this);
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/ActiveMQChannelHandler.java`
#### Snippet
```java
   @Override
   public void channelInactive(final ChannelHandlerContext ctx) throws Exception {
      synchronized (this) {
         if (active) {
            listenerExecutor.execute(() -> listener.connectionDestroyed(channelId(ctx.channel())));
```

### SynchronizeOnThis
Lock operations on a class may have unforeseen side-effects
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/SharedEventLoopGroup.java`
#### Snippet
```java
   @Override
   public Future<?> shutdownGracefully(final long l, final long l2, final TimeUnit timeUnit) {
      synchronized (SharedEventLoopGroup.class) {
         if (channelFactoryCount.decrementAndGet() == 0) {
            shutdown.compareAndSet(null, next().scheduleAtFixedRate(new Runnable() {
```

### SynchronizeOnThis
Lock operations on a class may have unforeseen side-effects
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/SharedEventLoopGroup.java`
#### Snippet
```java
               @Override
               public void run() {
                  synchronized (SharedEventLoopGroup.class) {
                     if (shutdown.get() != null) {
                        Future<?> future = SharedEventLoopGroup.super.shutdownGracefully(l, l2, timeUnit);
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/impl/PageSyncTimer.java`
#### Snippet
```java
   private void tick() {
      OperationContext[] pendingSyncsArray;
      synchronized (this) {

         pendingSync = false;
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/impl/PagingStoreImpl.java`
#### Snippet
```java
   @Override
   public void stop() throws Exception {
      synchronized (this) {
         if (running) {
            cursorProvider.stop();
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/cursor/impl/PageSubscriptionCounterImpl.java`
#### Snippet
```java
      // always lock the StorageManager first.
      try (ArtemisCloseable lock = storage.closeableReadLock()) {
         synchronized (this) {
            if (recordID >= 0) {
               storage.deletePageCounter(tx.getID(), this.recordID);
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/cursor/impl/PageCursorProviderImpl.java`
#### Snippet
```java
         logger.trace(">>>> Cleanup {}", this.pagingStore.getAddress());

         synchronized (this) {
            try {
               if (!pagingStore.isStarted()) {
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/cursor/impl/PageSubscriptionImpl.java`
#### Snippet
```java

      private PagedReference moveNext() {
         synchronized (PageSubscriptionImpl.this) {
            boolean match = false;

```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/Queue.java`
#### Snippet
```java

   default void forEach(java.util.function.Consumer<MessageReference> consumer) {
      synchronized (this) {
         try (LinkedListIterator<MessageReference> iterator = iterator()) {
            while (iterator.hasNext()) {
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ReplicationObserver.java`
#### Snippet
```java
         return;
      }
      synchronized (this) {
         if (closed) {
            return;
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ReplicationObserver.java`
#### Snippet
```java
         return;
      }
      synchronized (this) {
         if (closed || replicationFailure.isDone()) {
            return;
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ReplicationObserver.java`
#### Snippet
```java
         return;
      }
      synchronized (this) {
         if (backupUpToDate || closed || replicationFailure.isDone()) {
            return;
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ReplicationObserver.java`
#### Snippet
```java
         return;
      }
      synchronized (this) {
         if (closed || replicationFailure.isDone()) {
            return;
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ReplicationObserver.java`
#### Snippet
```java
         return;
      }
      synchronized (this) {
         if (closed || replicationFailure.isDone()) {
            return;
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ReplicationObserver.java`
#### Snippet
```java
         return;
      }
      synchronized (this) {
         if (closed || replicationFailure.isDone()) {
            return;
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ReplicationBackupActivation.java`
#### Snippet
```java
   @Override
   public void close(final boolean permanently, final boolean restarting) throws Exception {
      synchronized (this) {
         closed = true;
         final ReplicationObserver replicationObserver = this.replicationObserver;
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ReplicationBackupActivation.java`
#### Snippet
```java
   @Override
   public void onUnavailableManagerEvent() {
      synchronized (this) {
         if (closed) {
            return;
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ReplicationBackupActivation.java`
#### Snippet
```java
      try {
         while (true) {
            synchronized (this) {
               if (closed) {
                  return null;
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ReplicationBackupActivation.java`
#### Snippet
```java
   @Override
   public void run() {
      synchronized (this) {
         if (closed) {
            return;
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ReplicationBackupActivation.java`
#### Snippet
```java
            return;
         }
         synchronized (this) {
            if (closed)
               return;
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/SharedNothingBackupActivation.java`
#### Snippet
```java
         activeMQServer.moveServerData(replicaPolicy.getMaxSavedReplicatedJournalsSize());
         activeMQServer.getNodeManager().start();
         synchronized (this) {
            if (closed) {
               logger.trace("SharedNothingBackupActivation is closed, ignoring activation!");
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/SharedNothingBackupActivation.java`
#### Snippet
```java
         }

         synchronized (this) {
            if (closed) {
               return;
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/SharedNothingBackupActivation.java`
#### Snippet
```java
   @Override
   public void close(final boolean permanently, boolean restarting) throws Exception {
      synchronized (this) {
         if (backupQuorum != null)
            backupQuorum.causeExit(STOP);
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java
   @Override
   public void transferConnection(RemotingConnection newConnection) {
      synchronized (this) {
         // Remove failure listeners from old connection
         remotingConnection.removeFailureListener(this);
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java
         callback.close(failed);
      }
      synchronized (this) {
         if (!closed) {
            if (server.hasBrokerSessionPlugins()) {
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java
      }

      synchronized (this) {
         server.removeSession(name);

```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java

      ServerConsumer consumer;
      synchronized (this) {
         if (closed) {
            throw ActiveMQMessageBundle.BUNDLE.cannotCreateConsumerOnClosedSession(queueName);
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/GroupFirstMessageReference.java`
#### Snippet
```java
   public Message getMessage() {
      if (message == null) {
         synchronized (this) {
            if (message == null) {
               message = messageReference.getMessage().copy();
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/BackupManager.java`
#### Snippet
```java
            @Override
            public void run() {
               synchronized (BackupConnector.this) {
                  closeLocator(backupServerLocator);
                  backupServerLocator = null;
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/ClusterManager.java`
#### Snippet
```java
      List<Bridge> bridgesToRemove = new ArrayList<>();

      synchronized (this) {
         for (Bridge bridge : bridges.values()) {
            if (bridge.getName().toString().matches(name + "|" + name + "-\\d+")) {
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/ClusterManager.java`
#### Snippet
```java
   public void stop() throws Exception {
      haManager.stop();
      synchronized (this) {
         if (state == State.STOPPED || state == State.STOPPING) {
            return;
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/impl/BridgeImpl.java`
#### Snippet
```java
      }

      synchronized (this) {
         if (!active || !session.isWritable(this)) {
            if (logger.isDebugEnabled()) {
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/impl/BridgeImpl.java`
#### Snippet
```java
         queue.removeConsumer(BridgeImpl.this);

         synchronized (BridgeImpl.this) {
            logger.debug("Closing Session for bridge {}", configuration.getName());
            started = false;
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/impl/BridgeImpl.java`
#### Snippet
```java

   protected void scaleDown(String scaleDownTargetNodeID) {
      synchronized (this) {
         try {
            if (logger.isDebugEnabled()) {
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/impl/BridgeImpl.java`
#### Snippet
```java
            }

            synchronized (BridgeImpl.this) {
               started = false;
               active = false;
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/impl/ClusterConnectionImpl.java`
#### Snippet
```java
      }

      synchronized (this) {
         for (MessageFlowRecord record : records.values()) {
            try {
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/impl/ClusterConnectionImpl.java`
#### Snippet
```java
      String nodeId;

      synchronized (this) {
         if (!started) {
            return;
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/impl/ClusterConnectionImpl.java`
#### Snippet
```java
         @Override
         public void run() {
            synchronized (ClusterConnectionImpl.this) {
               closeLocator(serverLocator);
               serverLocator = null;
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/impl/ClusterConnectionImpl.java`
#### Snippet
```java
   @Override
   public void start() throws Exception {
      synchronized (this) {
         if (started) {
            return;
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ActiveMQServerImpl.java`
#### Snippet
```java
      logger.debug("Stopping server {}", this);

      synchronized (this) {
         if (state == SERVER_STATE.STOPPED || state == SERVER_STATE.STOPPING) {
            return;
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/federation/FederationDownstream.java`
#### Snippet
```java
      try {
         if (clientSession == null) {
            synchronized (this) {
               this.clientSessionFactory = (ClientSessionFactoryInternal) getConnection().clientSessionFactory();
               this.clientSession = (ClientSessionInternal) clientSessionFactory.createSession(getUser(), getPassword(), false, true, true, clientSessionFactory.getServerLocator().isPreAcknowledge(), clientSessionFactory.getServerLocator().getAckBatchSize());
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/federation/FederatedQueueConsumerImpl.java`
#### Snippet
```java
      try {
         if (clientConsumer == null) {
            synchronized (this) {
               this.clientSessionFactory = (ClientSessionFactoryInternal) upstream.getConnection().clientSessionFactory();
               this.clientSession = clientSessionFactory.createSession(upstream.getUser(), upstream.getPassword(), false, true, true, clientSessionFactory.getServerLocator().isPreAcknowledge(), clientSessionFactory.getServerLocator().getAckBatchSize());
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/ManagementContext.java`
#### Snippet
```java
         return;
      }
      synchronized (this) {
         if (isStarted) {
            return;
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/ManagementContext.java`
#### Snippet
```java
         return;
      }
      synchronized (this) {
         if (!isStarted) {
            return;
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/federation/address/FederatedAddress.java`
#### Snippet
```java
            //See if there is any matching diverts that match this queue binding and remove demand now that
            //the queue is going away
            synchronized (this) {
               matchingDiverts.entrySet().forEach(entry -> {
                  if (entry.getKey().getDivert().getForwardAddress().equals(queue.getAddress())) {
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/federation/address/FederatedAddress.java`
#### Snippet
```java
         //If we have then we need to look for any still existing queue bindings that map to this divert
         //and remove consumers if they haven't already been removed
         synchronized (this) {
            final Set<SimpleString> matchingQueues;
            if ((matchingQueues = matchingDiverts.remove(binding)) != null) {
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/federation/address/FederatedAddress.java`
#### Snippet
```java

         if (config.isEnableDivertBindings()) {
            synchronized (this) {
               for (Map.Entry<DivertBinding, Set<SimpleString>> entry : matchingDiverts.entrySet()) {
                  //for each divert check the new QueueBinding to see if the divert matches and is not already tracking
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/federation/address/FederatedAddress.java`
#### Snippet
```java
         final AddressInfo addressInfo = server.getPostOffice().getAddressInfo(binding.getAddress());

         synchronized (this) {
            if (match(addressInfo) && matchingDiverts.get(divertBinding) == null) {
               final Set<SimpleString> matchingQueues = new HashSet<>();
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/remoting/impl/invm/InVMConnection.java`
#### Snippet
```java
      closing = true;

      synchronized (this) {
         if (!closed) {
            listener.connectionDestroyed(id);
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/impl/ManagementServiceImpl.java`
#### Snippet
```java
   public void unregisterAcceptors() {
      List<String> acceptors = new ArrayList<>();
      synchronized (this) {
         for (String resourceName : registry.keySet()) {
            if (resourceName.startsWith(ResourceNames.ACCEPTOR)) {
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/impl/ManagementServiceImpl.java`
#### Snippet
```java
      }
      // This needs to be synchronized since we need to ensure notifications are processed in strict sequence
      synchronized (this) {
         if (messagingServerControl != null && notificationsEnabled) {
            // We also need to synchronize on the post office notification lock
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/postoffice/impl/PersistentDuplicateIDCache.java`
#### Snippet
```java

      if (posUsed != null) {
         synchronized (this) {
            final ObjLongPair<ByteArray> id = ids.get(posUsed.intValue());

```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/postoffice/impl/InMemoryDuplicateIDCache.java`
#### Snippet
```java
         ByteArray id;

         synchronized (this) {
            final int index = posUsed.intValue();
            id = ids.get(index);
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/OperationContextImpl.java`
#### Snippet
```java
      boolean executeNow = false;

      synchronized (this) {
         if (errorCode == -1) {
            final long storeLined = STORE_LINEUP_UPDATER.get(this);
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/JournalStorageManager.java`
#### Snippet
```java
   public void stop(boolean ioCriticalError, boolean sendFailover) throws Exception {
      try (ArtemisCloseable critical = measureCritical(CRITICAL_STOP)) {
         synchronized (this) {
            if (internalStop(ioCriticalError, sendFailover))
               return;
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/postoffice/impl/PostOfficeImpl.java`
#### Snippet
```java
   @Override
   public QueueBinding updateQueue(QueueConfiguration queueConfiguration, boolean forceUpdate) throws Exception {
      synchronized (this) {
         final QueueBinding queueBinding = (QueueBinding) addressManager.getBinding(queueConfiguration.getName());
         if (queueBinding == null) {
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/postoffice/impl/PostOfficeImpl.java`
#### Snippet
```java
   @Override
   public AddressInfo removeAddressInfo(SimpleString address, boolean force) throws Exception {
      synchronized (this) {
         if (server.hasBrokerAddressPlugins()) {
            server.callBrokerAddressPlugins(plugin -> plugin.beforeRemoveAddress(address));
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/postoffice/impl/PostOfficeImpl.java`
#### Snippet
```java

   private boolean internalAddressInfo(AddressInfo addressInfo, boolean reload) throws Exception {
      synchronized (this) {
         if (server.hasBrokerAddressPlugins()) {
            server.callBrokerAddressPlugins(plugin -> plugin.beforeAddAddress(addressInfo, reload));
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/postoffice/impl/PostOfficeImpl.java`
#### Snippet
```java
   public AddressInfo updateAddressInfo(SimpleString addressName,
                                        EnumSet<RoutingType> routingTypes) throws Exception {
      synchronized (this) {
         if (server.hasBrokerAddressPlugins()) {
            server.callBrokerAddressPlugins(plugin -> plugin.beforeUpdateAddress(addressName, routingTypes));
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/replication/ReplicationManager.java`
#### Snippet
```java

   public void stop(boolean clearTokens) throws Exception {
      synchronized (this) {
         if (!started) {
            logger.trace("Stopping being ignored as it hasn't been started");
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java
      @Override
      public void run() {
         synchronized (QueueImpl.this) {
            internalAddRedistributor();

```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java
   public void addHead(final List<MessageReference> refs, boolean scheduling) {
      try (ArtemisCloseable metric = measureCritical(CRITICAL_PATH_ADD_HEAD)) {
         synchronized (this) {
            for (MessageReference ref : refs) {
               addHead(ref, scheduling);
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java

      try (ArtemisCloseable metric = measureCritical(CRITICAL_PATH_ADD_HEAD)) {
         synchronized (QueueImpl.this) {
            if (ringSize != -1) {
               enforceRing(ref, false, true);
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java
         Transaction tx = new TransactionImpl(storageManager);

         synchronized (this) {

            try (LinkedListIterator<MessageReference> iter = iterator()) {
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java

      try (ArtemisCloseable metric = measureCritical(CRITICAL_PATH_ADD_HEAD)) {
         synchronized (this) {
            if (ringSize != -1) {
               enforceRing(ref, scheduling, true);
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java
   protected boolean scheduleIfPossible(MessageReference ref) {
      if (scheduledDeliveryHandler.checkAndSchedule(ref, true)) {
         synchronized (this) {
            if (!ref.isPaged()) {
               incrementMesssagesAdded();
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java
      }
      try (ArtemisCloseable metric = measureCritical(CRITICAL_PATH_ADD_HEAD)) {
         synchronized (this) {
            for (MessageReference ref : refs) {
               addSorted(ref, scheduling);
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java
   public Map<String, List<MessageReference>> getDeliveringMessages() {
      final Iterator<ConsumerHolder<? extends Consumer>> consumerHolderIterator;
      synchronized (this) {
         consumerHolderIterator = consumers.iterator();
      }
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java
      boolean foundRef = false;

      synchronized (this) {
         try (LinkedListIterator<MessageReference> iter = messageReferences.iterator()) {
            while (iter.hasNext()) {
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java

         LinkedList<MessageReference> expiredMessages = new LinkedList<>();
         synchronized (QueueImpl.this) {
            logger.debug("Scanning for expires on {}", name);

```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java
         Consumer handledconsumer = null;

         synchronized (QueueImpl.this) {

            if (queueDestroyed) {
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java

      try (ArtemisCloseable metric = measureCritical(CRITICAL_CONSUMER)) {
         synchronized (QueueImpl.this) {

            boolean consumerRemoved = false;
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java

   private boolean deliver(final MessageReference ref) {
      synchronized (this) {
         if (!supportsDirectDeliver) {
            return false;
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java
   @Override
   public MessageReference peekFirstMessage() {
      synchronized (this) {
         if (messageReferences != null) {
            return messageReferences.peek();
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java
      @Override
      public void repeat() {
         synchronized (QueueImpl.this) {
            iter.repeat();
         }
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java
            @Override
            public void run() {
               synchronized (QueueImpl.this) {
                  if (groups.remove(groupIDToRemove) != null) {
                     logger.debug("Removing group after unproposal {} from queue {}", groupID, QueueImpl.this);
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java
      @Override
      public void close() {
         synchronized (QueueImpl.this) {
            iter.close();
         }
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java

      try (ArtemisCloseable metric = measureCritical(CRITICAL_CONSUMER)) {
         synchronized (this) {
            if (maxConsumers != MAX_CONSUMERS_UNLIMITED && getConsumerCount() >= maxConsumers) {
               throw ActiveMQMessageBundle.BUNDLE.maxConsumerLimitReachedForQueue(address, name);
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java

      try {
         synchronized (this) {
            if (isPaused() || pageIterator == null) {
               return;
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java
   @Override
   public void deleteQueue(boolean removeConsumers) throws Exception {
      synchronized (this) {
         if (this.queueDestroyed)
            return;
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java
      @Override
      public boolean hasNext() {
         synchronized (QueueImpl.this) {
            return iter.hasNext();
         }
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java
      @Override
      public void remove() {
         synchronized (QueueImpl.this) {
            iter.remove();
         }
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java
      @Override
      public MessageReference next() {
         synchronized (QueueImpl.this) {
            return iter.next();
         }
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java
      }

      synchronized (this) {

         resetAllIterators();
```

## RuleId[id=CastConflictsWithInstanceof]
### CastConflictsWithInstanceof
Cast to 'AMQPLargeMessage' type conflicts with preceding 'instanceof LargeServerMessageImpl' check
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPLargeMessage.java`
#### Snippet
```java

      if (original instanceof LargeServerMessageImpl) {
         this.largeBody.referenceOriginalMessage(((AMQPLargeMessage) original).largeBody);
      }
   }
```

### CastConflictsWithInstanceof
Cast to 'ReplicatedPolicyConfiguration' type conflicts with preceding 'instanceof ReplicaPolicyConfiguration' check
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/impl/ConfigurationImpl.java`
#### Snippet
```java
   public boolean isCheckForLiveServer() {
      if (haPolicyConfiguration instanceof ReplicaPolicyConfiguration) {
         return ((ReplicatedPolicyConfiguration) haPolicyConfiguration).isCheckForLiveServer();
      } else {
         return false;
```

### CastConflictsWithInstanceof
Cast to 'ReplicatedPolicyConfiguration' type conflicts with preceding 'instanceof ReplicaPolicyConfiguration' check
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/impl/ConfigurationImpl.java`
#### Snippet
```java
   public ConfigurationImpl setCheckForLiveServer(boolean checkForLiveServer) {
      if (haPolicyConfiguration instanceof ReplicaPolicyConfiguration) {
         ((ReplicatedPolicyConfiguration) haPolicyConfiguration).setCheckForLiveServer(checkForLiveServer);
      }

```

### CastConflictsWithInstanceof
Cast to 'ServiceComponent' type conflicts with preceding 'instanceof WebServerComponentMarker' check
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/ActiveMQServerControlImpl.java`
#### Snippet
```java
      for (ActiveMQComponent component : server.getExternalComponents()) {
         if (component instanceof WebServerComponentMarker) {
            return (ServiceComponent) component;
         }
      }
```

## RuleId[id=UnusedAssignment]
### UnusedAssignment
Variable `oldServerLocator` initializer `null` is redundant
in `artemis-service-extensions/src/main/java/org/apache/activemq/artemis/service/extensions/xa/recovery/ActiveMQXAResourceWrapper.java`
#### Snippet
```java
    */
   public void close() {
      ServerLocator oldServerLocator = null;
      ClientSessionFactory oldCSF = null;
      ClientSession oldDelegate = null;
```

### UnusedAssignment
Variable `oldCSF` initializer `null` is redundant
in `artemis-service-extensions/src/main/java/org/apache/activemq/artemis/service/extensions/xa/recovery/ActiveMQXAResourceWrapper.java`
#### Snippet
```java
   public void close() {
      ServerLocator oldServerLocator = null;
      ClientSessionFactory oldCSF = null;
      ClientSession oldDelegate = null;
      synchronized (ActiveMQXAResourceWrapper.lock) {
```

### UnusedAssignment
Variable `oldDelegate` initializer `null` is redundant
in `artemis-service-extensions/src/main/java/org/apache/activemq/artemis/service/extensions/xa/recovery/ActiveMQXAResourceWrapper.java`
#### Snippet
```java
      ServerLocator oldServerLocator = null;
      ClientSessionFactory oldCSF = null;
      ClientSession oldDelegate = null;
      synchronized (ActiveMQXAResourceWrapper.lock) {
         oldCSF = csf;
```

### UnusedAssignment
Variable `uri` initializer `""` is redundant
in `artemis-maven-plugin/src/main/java/org/apache/activemq/artemis/maven/ArtemisDependencyDocPlugin.java`
#### Snippet
```java
   private String getURI(ArtifactResult result) {
      Artifact art = result.getArtifact();
      String uri = "";
      if (result.getRepository() instanceof RemoteRepository) {
         RemoteRepository remoteRepository = (RemoteRepository) result.getRepository();
```

### UnusedAssignment
Variable `maxCacheSize` initializer `10000` is redundant
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/impl/LRUCache.java`
#### Snippet
```java

   private static final long serialVersionUID = -342098639681884413L;
   protected int maxCacheSize = 10000;

   /**
```

### UnusedAssignment
Variable `e` initializer `null` is redundant
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/impl/SelectorParser.java`
#### Snippet
```java
      }
      try {
         BooleanExpression e = null;
         if (hyphenatedProps) {
            HyphenatedParser parser = new HyphenatedParser(new StringReader(actual));
```

### UnusedAssignment
Variable `convertStringExpressions` initializer `false` is redundant
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/ArithmeticExpression.java`
#### Snippet
```java
   protected static final int LONG = 2;
   protected static final int DOUBLE = 3;
   boolean convertStringExpressions = false;

   /**
```

### UnusedAssignment
Variable `convertStringExpressions` initializer `false` is redundant
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/ComparisonExpression.java`
#### Snippet
```java
   public static final ThreadLocal<Boolean> CONVERT_STRING_EXPRESSIONS = new ThreadLocal<>();

   boolean convertStringExpressions = false;
   private static final Set<Character> REGEXP_CONTROL_CHARS = new HashSet<>();

```

### UnusedAssignment
Variable `frame` initializer `null` is redundant
in `artemis-protocols/artemis-stomp-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/stomp/ActiveMQStompException.java`
#### Snippet
```java

   public StompFrame getFrame() {
      StompFrame frame = null;
      if (handler == null) {
         frame = new StompFrame(Stomp.Responses.ERROR);
```

### UnusedAssignment
Variable `subscriptionID` initializer `null` is redundant
in `artemis-protocols/artemis-stomp-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/stomp/v10/StompFrameHandlerV10.java`
#### Snippet
```java
         }

         String subscriptionID = null;
         if (id != null) {
            subscriptionID = id;
```

### UnusedAssignment
Variable `response` initializer `null` is redundant
in `artemis-protocols/artemis-stomp-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/stomp/v10/StompFrameHandlerV10.java`
#### Snippet
```java
   @Override
   public StompFrame onConnect(StompFrame frame) {
      StompFrame response = null;
      Map<String, String> headers = frame.getHeadersMap();
      String login = headers.get(Stomp.Headers.Connect.LOGIN);
```

### UnusedAssignment
Variable `response` initializer `null` is redundant
in `artemis-protocols/artemis-stomp-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/stomp/VersionedStompFrameHandler.java`
#### Snippet
```java

   public StompFrame handleFrame(StompFrame request) {
      StompFrame response = null;

      if (Stomp.Commands.SEND.equals(request.getCommand())) {
```

### UnusedAssignment
Variable `response` initializer `null` is redundant
in `artemis-protocols/artemis-stomp-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/stomp/VersionedStompFrameHandler.java`
#### Snippet
```java

   private StompFrame handleSubscribe(StompFrame request) {
      StompFrame response = null;
      try {
         StompPostReceiptFunction postProcessFunction = onSubscribe(request);
```

### UnusedAssignment
Variable `session` initializer `null` is redundant
in `artemis-protocols/artemis-stomp-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/stomp/StompConnection.java`
#### Snippet
```java

   public StompSession getSession(String txID) throws ActiveMQStompException, ActiveMQSecurityException {
      StompSession session = null;
      try {
         if (txID == null) {
```

### UnusedAssignment
Variable `subscriptionID` initializer `null` is redundant
in `artemis-protocols/artemis-stomp-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/stomp/StompConnection.java`
#### Snippet
```java
      }

      String subscriptionID = null;
      if (id != null) {
         subscriptionID = id;
```

### UnusedAssignment
Variable `reply` initializer `null` is redundant
in `artemis-protocols/artemis-stomp-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/stomp/StompConnection.java`
#### Snippet
```java

   public void handleFrame(StompFrame request) {
      StompFrame reply = null;

      if (stompListener != null) {
```

### UnusedAssignment
Variable `response` initializer `null` is redundant
in `artemis-protocols/artemis-stomp-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/stomp/v11/StompFrameHandlerV11.java`
#### Snippet
```java
   @Override
   public StompFrame onConnect(StompFrame frame) {
      StompFrame response = null;
      Map<String, String> headers = frame.getHeadersMap();
      String login = headers.get(Stomp.Headers.Connect.LOGIN);
```

### UnusedAssignment
Variable `line` initializer `null` is redundant
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/SpawnedVMSupport.java`
#### Snippet
```java
            InputStreamReader isr = new InputStreamReader(is);
            BufferedReader br = new BufferedReader(isr);
            String line = null;
            while ((line = br.readLine()) != null) {
               if (wordMatch != null && wordRunner != null) {
```

### UnusedAssignment
Variable `elementsEnabled` initializer `false` is redundant
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/SizeAwareMetric.java`
#### Snippet
```java
   private boolean sizeEnabled = false;

   private boolean elementsEnabled = false;

   private AddCallback onSizeCallback;
```

### UnusedAssignment
Variable `sizeEnabled` initializer `false` is redundant
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/SizeAwareMetric.java`
#### Snippet
```java
   private long lowerMarkSize;

   private boolean sizeEnabled = false;

   private boolean elementsEnabled = false;
```

### UnusedAssignment
Variable `mLastSystemTimestamp` initializer `0L` is redundant
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/UUIDTimer.java`
#### Snippet
```java
    * to be monotonically increasing.
    */
   private long mLastSystemTimestamp = 0L;

   /**
```

### UnusedAssignment
Variable `mLastUsedTimestamp` initializer `0L` is redundant
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/UUIDTimer.java`
#### Snippet
```java
    * points t1 and t2 (where t2 is after t1), t1 <= t2 will always hold true.
    */
   private long mLastUsedTimestamp = 0L;

   /**
```

### UnusedAssignment
Variable `read` initializer `-1` is redundant
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
         out = new java.io.BufferedOutputStream(new java.io.FileOutputStream(outfile));
         byte[] buffer = new byte[65536]; // 64K
         int read = -1;
         while ((read = in.read(buffer)) >= 0) {
            out.write(buffer, 0, read);
```

### UnusedAssignment
Variable `buffer` initializer `null` is redundant
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
         // Set up some useful variables
         java.io.File file = new java.io.File(filename);
         byte[] buffer = null;
         int length = 0;
         int numBytes = 0;
```

### UnusedAssignment
Variable `numBytes` initializer `0` is redundant
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
         byte[] buffer = null;
         int length = 0;
         int numBytes = 0;

         // Check for size of file
```

### UnusedAssignment
Variable `length` initializer `0` is redundant
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
            java.io.ByteArrayOutputStream baos = null;
            byte[] buffer = new byte[2048];
            int length = 0;

            try {
```

### UnusedAssignment
Variable `i` initializer `0` is redundant
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
            } else { // Else decoding
               byte[] b4 = new byte[4];
               int i = 0;
               for (i = 0; i < 4; i++) {
                  // Read four "meaningful" bytes:
```

### UnusedAssignment
Variable `b` initializer `0` is redundant
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
               for (i = 0; i < 4; i++) {
                  // Read four "meaningful" bytes:
                  int b = 0;
                  do {
                     b = in.read();
```

### UnusedAssignment
Variable `numBytes` initializer `0` is redundant
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
         // (v2.2.1)
         int length = 0;
         int numBytes = 0;

         // Open a stream
```

### UnusedAssignment
Variable `success` initializer `false` is redundant
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
    */
   public static boolean decodeToFile(final String dataToDecode, final String filename) {
      boolean success = false;
      Base64.OutputStream bos = null;
      try {
```

### UnusedAssignment
Variable `read` initializer `-1` is redundant
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
         out = new java.io.BufferedOutputStream(new java.io.FileOutputStream(outfile));
         byte[] buffer = new byte[65536]; // 64K
         int read = -1;
         while ((read = in.read(buffer)) >= 0) {
            out.write(buffer, 0, read);
```

### UnusedAssignment
Variable `success` initializer `false` is redundant
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
    */
   public static boolean encodeToFile(final byte[] dataToEncode, final String filename) {
      boolean success = false;
      Base64.OutputStream bos = null;
      try {
```

### UnusedAssignment
Variable `i` initializer `0` is redundant
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
      byte[] b4 = new byte[4];
      int b4Posn = 0;
      int i = 0;
      byte sbiCrop = 0;
      byte sbiDecode = 0;
```

### UnusedAssignment
Variable `sbiCrop` initializer `0` is redundant
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
      int b4Posn = 0;
      int i = 0;
      byte sbiCrop = 0;
      byte sbiDecode = 0;
      for (i = off; i < off + len; i++) {
```

### UnusedAssignment
Variable `sbiDecode` initializer `0` is redundant
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
      int i = 0;
      byte sbiCrop = 0;
      byte sbiDecode = 0;
      for (i = off; i < off + len; i++) {
         sbiCrop = (byte) (source[i] & 0x7f); // Only the low seven bits
```

### UnusedAssignment
Variable `obj` initializer `null` is redundant
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
      java.io.ByteArrayInputStream bais = null;
      java.io.ObjectInputStream ois = null;
      Object obj = null;

      try {
```

### UnusedAssignment
Variable `buffer2` initializer `null` is redundant
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/io/nio/NIOSequentialFileFactory.java`
#### Snippet
```java
   public static ByteBuffer allocateDirectByteBuffer(final int size) {
      // Using direct buffer, as described on https://jira.jboss.org/browse/HORNETQ-467
      ByteBuffer buffer2 = null;
      try {
         buffer2 = ByteBuffer.allocateDirect(size);
```

### UnusedAssignment
Variable `index` initializer `-1` is redundant
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/uri/URISupport.java`
#### Snippet
```java
    */
   public static int indexOfParenthesisMatch(String str, int first) throws URISyntaxException {
      int index = -1;

      if (first < 0 || first > str.length()) {
```

### UnusedAssignment
Variable `localTimer` initializer `null` is redundant
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/io/buffer/TimedBuffer.java`
#### Snippet
```java

   public void stop() {
      Thread localTimer = null;
      try (ArtemisCloseable measure = measureCritical(CRITICAL_PATH_STOP)) {
         // add critical analyzer here.... <<<<
```

### UnusedAssignment
Variable `bufferLimit` initializer `0` is redundant
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/io/buffer/TimedBuffer.java`
#### Snippet
```java
   private TimedBufferObserver bufferObserver;
   private CheckTimer timerRunnable;
   private int bufferLimit = 0;
   private List<IOCallback> callbacks;
   // used to measure sync requests. When a sync is requested, it shouldn't take more than timeout to happen
```

### UnusedAssignment
Variable `calculatedSize` initializer `0` is redundant
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalFilesRepository.java`
#### Snippet
```java
         logger.debug("Adding free file {}, renameTMP={}, checkDelete={}", file, renameTmp, checkDelete);
      }
      long calculatedSize = 0;
      try {
         calculatedSize = file.getFile().size();
```

### UnusedAssignment
Variable `nextFile` initializer `null` is redundant
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalFilesRepository.java`
#### Snippet
```java
                               final boolean initFile,
                               final boolean tmpCompactExtension) throws Exception {
      JournalFile nextFile = null;

      nextFile = freeFiles.poll();
```

### UnusedAssignment
The value `false` assigned to `inManagedTx` is never used
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/ActiveMQRAManagedConnection.java`
#### Snippet
```java
      destroyHandles();

      inManagedTx = false;

      inManagedTx = false;
```

### UnusedAssignment
Variable `factory` initializer `null` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/factory/BrokerFactory.java`
#### Snippet
```java
      }

      BrokerFactoryHandler factory = null;
      try {
         FactoryFinder finder = new FactoryFinder("META-INF/services/org/apache/activemq/artemis/broker/");
```

### UnusedAssignment
Variable `value` initializer `null` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/InputAbstract.java`
#### Snippet
```java
   public int inputInteger(String propertyName, String prompt, String silentDefault) {

      Integer value = null;
      do {
         String input = input(propertyName, prompt, silentDefault);
```

### UnusedAssignment
Variable `factory` initializer `null` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/factory/jmx/ManagementFactory.java`
#### Snippet
```java
      }

      JmxAclHandler factory = null;
      try {
         FactoryFinder finder = new FactoryFinder("META-INF/services/org/apache/activemq/artemis/broker/jmx/");
```

### UnusedAssignment
The value `null` assigned to `preparedTransactions` is never used
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/xml/XmlDataExporter.java`
#### Snippet
```java

      // Since we don't use these nullify the reference so that the garbage collector can clean them up
      preparedTransactions = null;

      for (RecordInfo info : records) {
```

### UnusedAssignment
Variable `describeJournal` initializer `null` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/PrintData.java`
#### Snippet
```java
         out.println();
      }
      DescribeJournal describeJournal = null;
      describeJournal = printMessages(messagesDirectory, out, safe, !skipJournal, !skipJournal, reclaimed);

```

### UnusedAssignment
Variable `folder` initializer `null` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/PrintData.java`
#### Snippet
```java
      for (SimpleString store : stores) {
         PagingStore pgStore = manager.getPageStore(store);
         File folder = null;

         if (pgStore != null) {
```

### UnusedAssignment
Variable `builder` initializer `null` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/util/ServerUtil.java`
#### Snippet
```java
         ArrayList<String> command = new ArrayList<>();

         ProcessBuilder builder = null;
         if (IS_WINDOWS) {
            command.add("cmd");
```

### UnusedAssignment
Variable `obj` initializer `null` is redundant
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/bridge/impl/JNDIFactorySupport.java`
#### Snippet
```java
      InitialContext ic = null;

      Object obj = null;

      try {
```

### UnusedAssignment
Variable `val` initializer `null` is redundant
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/bridge/impl/JMSBridgeImpl.java`
#### Snippet
```java
      JMSBridgeImpl.copyProperties(msg);

      String val = null;

      val = msg.getStringProperty(ActiveMQJMSConstants.AMQ_MESSAGING_BRIDGE_MESSAGE_ID_LIST);
```

### UnusedAssignment
Variable `msg` initializer `null` is redundant
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/bridge/impl/JMSBridgeImpl.java`
#### Snippet
```java
      Iterator<Message> iter = messages.iterator();

      Message msg = null;

      while (iter.hasNext()) {
```

### UnusedAssignment
Variable `waited` initializer `0` is redundant
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/bridge/impl/JMSBridgeImpl.java`
#### Snippet
```java
         long toWait = failoverTimeout;
         long start = 0;
         long waited = 0;
         boolean timedOut = false;
         FailoverEventType result = null;
```

### UnusedAssignment
Variable `result` initializer `null` is redundant
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/bridge/impl/JMSBridgeImpl.java`
#### Snippet
```java
         long waited = 0;
         boolean timedOut = false;
         FailoverEventType result = null;
         synchronized (this) {
            while ((lastEvent == null || lastEvent == FailoverEventType.FAILURE_DETECTED)) {
```

### UnusedAssignment
Variable `mappedDestination` initializer `null` is redundant
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenWireProtocolManager.java`
#### Snippet
```java
      }

      ActiveMQDestination mappedDestination = null;
      synchronized (vtDestMapCache) {
         mappedDestination = vtDestMapCache.get(destination);
```

### UnusedAssignment
Variable `internalAddress` initializer `false` is redundant
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/amq/AMQConsumer.java`
#### Snippet
```java
   //internal means we don't expose
   //it's address/queue to management service
   private boolean internalAddress = false;
   private volatile Set<MessageReference> rolledbackMessageRefs;

```

### UnusedAssignment
Variable `dests` initializer `null` is redundant
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/amq/AMQSession.java`
#### Snippet
```java
      //check destination
      ActiveMQDestination dest = info.getDestination();
      ActiveMQDestination[] dests = null;
      if (dest.isComposite()) {
         dests = dest.getCompositeDestinations();
```

### UnusedAssignment
Variable `validationData` initializer `null` is redundant
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTProtocolHandler.java`
#### Snippet
```java
      String password = connect.payload().passwordInBytes() == null ? null : new String(connect.payload().passwordInBytes(), CharsetUtil.UTF_8);
      String username = connect.payload().userName();
      Pair<Boolean, String> validationData = null;
      try {
         validationData = validateUser(username, password);
```

### UnusedAssignment
Variable `message` initializer `null` is redundant
in `artemis-junit/artemis-junit-commons/src/main/java/org/apache/activemq/artemis/junit/ActiveMQConsumerDelegate.java`
#### Snippet
```java
   @Override
   public ClientMessage receiveMessage(long timeout) {
      ClientMessage message = null;
      if (timeout > 0) {
         try {
```

### UnusedAssignment
Variable `xid` initializer `null` is redundant
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenWireConnection.java`
#### Snippet
```java
      }

      Xid xid = null;
      Transaction transaction;
      if (txID.isXATransaction()) {
```

### UnusedAssignment
Variable `currentException` initializer `null` is redundant
in `artemis-unit-test-support/src/main/java/org/apache/activemq/artemis/utils/RetryRule.java`
#### Snippet
```java
         @Override
         public void evaluate() throws Throwable {
            Throwable currentException = null;
            try {
               base.evaluate();
```

### UnusedAssignment
The value `null` assigned to `list` is never used
in `artemis-unit-test-support/src/main/java/org/apache/activemq/artemis/utils/ThreadLeakCheckRule.java`
#### Snippet
```java
         }
         list.clear();
         list = null;
         System.gc();
         try {
```

### UnusedAssignment
The value `null` assigned to `bb` is never used
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalImpl.java`
#### Snippet
```java
      fileFactory.releaseBuffer(bb);

      bb = null;

      return new JournalFileImpl(file, fileID, journalVersion);
```

### UnusedAssignment
Variable `newDatafiles` initializer `null` is redundant
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalImpl.java`
#### Snippet
```java
            onCompactDone();

            List<JournalFile> newDatafiles = null;

            JournalCompactor localCompactor = compactor;
```

### UnusedAssignment
Variable `recordSize` initializer `0` is redundant
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalImpl.java`
#### Snippet
```java
   private static int getRecordSize(final byte recordType, final int journalVersion) {
      // The record size (without the variable portion)
      int recordSize = 0;
      switch (recordType) {
         case ADD_RECORD:
```

### UnusedAssignment
Variable `timeToLive` initializer `0` is redundant
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQJMSProducer.java`
#### Snippet
```java
   @Override
   public long getTimeToLive() {
      long timeToLive = 0;
      try {
         timeToLive = producer.getTimeToLive();
```

### UnusedAssignment
Variable `bindingQueryResult` initializer `null` is redundant
in `artemis-junit/artemis-junit-commons/src/main/java/org/apache/activemq/artemis/junit/EmbeddedJMSDelegate.java`
#### Snippet
```java
               ActiveMQDestination.createDestination(topicName, ActiveMQDestination.TYPE.TOPIC);
      if (!destination.isQueue()) {
         BindingQueryResult bindingQueryResult = null;
         try {
            bindingQueryResult = jmsServer.getActiveMQServer().bindingQuery(destination.getSimpleAddress());
```

### UnusedAssignment
Variable `bindingQueryResult` initializer `null` is redundant
in `artemis-junit/artemis-junit-commons/src/main/java/org/apache/activemq/artemis/junit/EmbeddedJMSDelegate.java`
#### Snippet
```java
         queue = jmsServer.getActiveMQServer().locateQueue(destination.getSimpleAddress());
      } else {
         BindingQueryResult bindingQueryResult = null;
         try {
            bindingQueryResult = jmsServer.getActiveMQServer().bindingQuery(destination.getSimpleAddress());
```

### UnusedAssignment
Variable `routingType` initializer `null` is redundant
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPSessionCallback.java`
#### Snippet
```java
      context.incrementSettle();

      RoutingType routingType = null;
      if (address != null) {
         // set Fixed-address producer if the message.properties.to address differs from the producer
```

### UnusedAssignment
Variable `addressQueryResult` initializer `null` is redundant
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/ProtonServerSenderContext.java`
#### Snippet
```java
            if (clientDefined) {
               multicast = hasCapabilities(TOPIC, source);
               AddressQueryResult addressQueryResult = null;
               try {
                  addressQueryResult = sessionSPI.addressQuery(addressToUse, multicast ? RoutingType.MULTICAST : RoutingType.ANYCAST, true);
```

### UnusedAssignment
Variable `addressQueryResult` initializer `null` is redundant
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/ProtonServerSenderContext.java`
#### Snippet
```java
            } else {
               // if not we look up the address
               AddressQueryResult addressQueryResult = null;

               // Set this to the broker configured default for the address prior to the lookup so that
```

### UnusedAssignment
Variable `decodedType` initializer `null` is redundant
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/converter/CoreAmqpConverter.java`
#### Snippet
```java

      final DecoderImpl decoder = TLSEncode.getDecoder();
      Object decodedType = null;

      decoder.setBuffer(ByteBufferReader.wrap(encodedType));
```

### UnusedAssignment
Variable `refCount` initializer `1` is redundant
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/jgroups/JChannelWrapper.java`
#### Snippet
```java

   private boolean connected = false;
   int refCount = 1;
   final JChannel channel;
   final String channelName;
```

### UnusedAssignment
Variable `queue` initializer `null` is redundant
in `artemis-junit/artemis-junit-commons/src/main/java/org/apache/activemq/artemis/junit/EmbeddedActiveMQDelegate.java`
#### Snippet
```java
   @Override
   public Queue createQueue(SimpleString address, SimpleString name) {
      Queue queue = null;
      try {
         queue = server.getActiveMQServer()
```

### UnusedAssignment
Variable `bindingQueryResult` initializer `null` is redundant
in `artemis-junit/artemis-junit-commons/src/main/java/org/apache/activemq/artemis/junit/EmbeddedActiveMQDelegate.java`
#### Snippet
```java
      List<Queue> boundQueues = new java.util.LinkedList<>();

      BindingQueryResult bindingQueryResult = null;
      try {
         bindingQueryResult = server.getActiveMQServer().bindingQuery(address);
```

### UnusedAssignment
Variable `message` initializer `null` is redundant
in `artemis-junit/artemis-junit-commons/src/main/java/org/apache/activemq/artemis/junit/EmbeddedActiveMQDelegate.java`
#### Snippet
```java

         try (ClientConsumer consumer = session.createConsumer(address, browseOnly)) {
            ClientMessage message = null;
            if (timeout > 0) {
               try {
```

### UnusedAssignment
Variable `count` initializer `0` is redundant
in `artemis-jdbc-store/src/main/java/org/apache/activemq/artemis/jdbc/store/journal/JDBCJournalImpl.java`
#### Snippet
```java
   @Override
   public int getNumberOfRecords() {
      int count = 0;
      try (Connection connection = connectionProvider.getConnection();
           PreparedStatement countJournalRecords = connection.prepareStatement(this.countJournalRecords)) {
```

### UnusedAssignment
Variable `uniqueEventID` initializer `System.currentTimeMillis()` is redundant
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/TopologyMemberImpl.java`
#### Snippet
```java
    * transient to avoid serialization changes
    */
   private transient long uniqueEventID = System.currentTimeMillis();

   private final String nodeId;
```

### UnusedAssignment
Variable `polledPacket` initializer `null` is redundant
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/LargeMessageControllerImpl.java`
#### Snippet
```java
      synchronized (this) {
         int totalSize = 0;
         LargeData polledPacket = null;
         while ((polledPacket = largeMessageData.poll()) != null) {
            totalSize += polledPacket.getFlowControlSize();
```

### UnusedAssignment
Variable `discoveryOK` initializer `false` is redundant
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ServerLocatorImpl.java`
#### Snippet
```java

   private void executeDiscovery() throws ActiveMQException {
      boolean discoveryOK = false;
      boolean retryDiscovery = false;
      int tryNumber = 0;
```

### UnusedAssignment
Variable `retryDiscovery` initializer `false` is redundant
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ServerLocatorImpl.java`
#### Snippet
```java
   private void executeDiscovery() throws ActiveMQException {
      boolean discoveryOK = false;
      boolean retryDiscovery = false;
      int tryNumber = 0;

```

### UnusedAssignment
Variable `allSessionReconnected` initializer `false` is redundant
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ClientSessionFactoryImpl.java`
#### Snippet
```java
               }

               boolean allSessionReconnected = false;
               int failedReconnectSessionsCounter = 0;
               do {
```

### UnusedAssignment
Variable `result` initializer `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/security/jaas/KubernetesLoginModule.java`
#### Snippet
```java
   @Override
   public boolean commit() throws LoginException {
      boolean result = false;
      result = tokenReview.isAuthenticated();

```

### UnusedAssignment
Variable `baseDir` initializer `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/security/jaas/PropertiesLoader.java`
#### Snippet
```java

      private File baseDir(Map options) {
         File baseDir = null;
         if (options.get("baseDir") != null) {
            baseDir = new File((String) options.get("baseDir"));
```

### UnusedAssignment
Variable `authToken` initializer `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/security/jaas/kubernetes/client/KubernetesClientImpl.java`
#### Snippet
```java
   public TokenReview getTokenReview(String token) {
      TokenReview tokenReview = new TokenReview();
      String authToken = null;
      try {
         logger.debug("Loading client authentication token from {}", tokenPath);
```

### UnusedAssignment
Variable `sslEnabled` initializer `TransportConstants.DEFAULT_SSL_ENABLED` is redundant
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/NettyConnector.java`
#### Snippet
```java
   private final BaseConnectionLifeCycleListener<?> listener;

   private boolean sslEnabled = TransportConstants.DEFAULT_SSL_ENABLED;

   private boolean httpEnabled;
```

### UnusedAssignment
Variable `dn` initializer `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/security/jaas/LDAPLoginModule.java`
#### Snippet
```java

   private String resolveDN(String username, List<String> roles) throws FailedLoginException {
      String dn = null;

      logger.debug("Create the LDAP initial context.");
```

### UnusedAssignment
Variable `results` initializer `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/security/jaas/LDAPLoginModule.java`
#### Snippet
```java
         }

         NamingEnumeration<SearchResult> results = null;
         try {
            results = Subject.doAs(brokerGssapiIdentity, (PrivilegedExceptionAction< NamingEnumeration<SearchResult>>) () -> context.search(getLDAPPropertyValue(ConfigKey.USER_BASE), filter, constraints));
```

### UnusedAssignment
Variable `results` initializer `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/security/jaas/LDAPLoginModule.java`
#### Snippet
```java
      HashSet<String> haveSeenNames = new HashSet<>();
      Queue<String> pendingNameExpansion = new LinkedList<>();
      NamingEnumeration<SearchResult> results = null;
      try {
         results = Subject.doAs(brokerGssapiIdentity, (PrivilegedExceptionAction< NamingEnumeration<SearchResult>>) () -> context.search(getLDAPPropertyValue(ConfigKey.ROLE_BASE), filter, constraints));
```

### UnusedAssignment
Variable `isValid` initializer `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/security/jaas/LDAPLoginModule.java`
#### Snippet
```java

   protected boolean bindUser(DirContext context, String dn, String password) throws NamingException {
      boolean isValid = false;

      logger.debug("Binding the user.");
```

### UnusedAssignment
Variable `durable` initializer `UNDEFINED_IS_DURABLE` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/cursor/PagedReferenceImpl.java`
#### Snippet
```java
   private static final byte IS_DURABLE = 1;
   private static final byte UNDEFINED_IS_DURABLE = -1;
   private byte durable = UNDEFINED_IS_DURABLE;

   @Override
```

### UnusedAssignment
Variable `deliveryTime` initializer `UNDEFINED_DELIVERY_TIME` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/cursor/PagedReferenceImpl.java`
#### Snippet
```java

   private static final long UNDEFINED_DELIVERY_TIME = Long.MIN_VALUE;
   private long deliveryTime = UNDEFINED_DELIVERY_TIME;

   private int persistedCount;
```

### UnusedAssignment
Variable `messageID` initializer `-1` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/cursor/PagedReferenceImpl.java`
#### Snippet
```java
   private long transactionID = -2;

   private long messageID = -1;

   private long messageSize = -1;
```

### UnusedAssignment
Variable `transactionID` initializer `-2` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/cursor/PagedReferenceImpl.java`
#### Snippet
```java
   private byte largeMessage;

   private long transactionID = -2;

   private long messageID = -1;
```

### UnusedAssignment
Variable `factory` initializer `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/impl/PagingStoreImpl.java`
#### Snippet
```java
      String fileName = createFileName(pageNumber);

      SequentialFileFactory factory = null;
      try {
         factory = checkFileFactory();
```

### UnusedAssignment
Variable `pgdMessagesList` initializer `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/cursor/impl/PageCursorProviderImpl.java`
#### Snippet
```java
      try {
         for (Page depagedPage : depagedPages) {
            LinkedList<PagedMessage> pgdMessagesList = null;
            try {
               depagedPage.open(false);
```

### UnusedAssignment
Variable `valid` initializer `true` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/cursor/impl/PageSubscriptionImpl.java`
#### Snippet
```java
               }

               boolean valid = true;
               boolean ignored = false;

```

### UnusedAssignment
Variable `copy` initializer `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/DivertImpl.java`
#### Snippet
```java
         context.setReusable(false);

         Message copy = null;

         // Shouldn't copy if it's not routed anywhere else
```

### UnusedAssignment
Variable `ackedRefs` initializer `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerConsumerImpl.java`
#### Snippet
```java
      }

      List<Long> ackedRefs = null;

      // Acknowledge acknowledges all refs delivered by the consumer up to and including the one explicitly
```

### UnusedAssignment
Variable `possibleLive` initializer `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/LiveOnlyActivation.java`
#### Snippet
```java

         while (clientSessionFactory == null) {
            Pair<TransportConfiguration, TransportConfiguration> possibleLive = null;
            possibleLive = nodeLocator.getLiveConfiguration();
            if (possibleLive == null)  // we've tried every connector
```

### UnusedAssignment
Variable `lostLock` initializer `true` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/FileLockNodeManager.java`
#### Snippet
```java
      public void run() {

         boolean lostLock = true;
         try {
            if (liveLock == null) {
```

### UnusedAssignment
Variable `result` initializer `RoutingStatus.OK` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java
                                            final RoutingContext routingContext) throws Exception {

      RoutingStatus result = RoutingStatus.OK;

      RoutingType routingType = msg.getRoutingType();
```

### UnusedAssignment
Variable `addRecord` initializer `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/group/impl/LocalGroupingHandler.java`
#### Snippet
```java
         }

         boolean addRecord = false;

         GroupBinding groupBinding = null;
```

### UnusedAssignment
Variable `groupBinding` initializer `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/group/impl/LocalGroupingHandler.java`
#### Snippet
```java
         boolean addRecord = false;

         GroupBinding groupBinding = null;
         lock.lock();
         try {
```

### UnusedAssignment
Variable `excludedConnectors` initializer `new ArrayList<>()` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/ha/ColocatedPolicy.java`
#### Snippet
```java

   /*backup stuff*/
   private List<String> excludedConnectors = new ArrayList<>();

   private BackupPolicy backupPolicy;
```

### UnusedAssignment
Variable `backupRequestRetries` initializer `ActiveMQDefaultConfiguration.getDefaultHapolicyBackupRequestRetries()` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/ha/ColocatedPolicy.java`
#### Snippet
```java
   private boolean requestBackup = ActiveMQDefaultConfiguration.isDefaultHapolicyRequestBackup();

   private int backupRequestRetries = ActiveMQDefaultConfiguration.getDefaultHapolicyBackupRequestRetries();

   private long backupRequestRetryInterval = ActiveMQDefaultConfiguration.getDefaultHapolicyBackupRequestRetryInterval();
```

### UnusedAssignment
Variable `maxBackups` initializer `ActiveMQDefaultConfiguration.getDefaultHapolicyMaxBackups()` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/ha/ColocatedPolicy.java`
#### Snippet
```java
   private long backupRequestRetryInterval = ActiveMQDefaultConfiguration.getDefaultHapolicyBackupRequestRetryInterval();

   private int maxBackups = ActiveMQDefaultConfiguration.getDefaultHapolicyMaxBackups();

   private int backupPortOffset = ActiveMQDefaultConfiguration.getDefaultHapolicyBackupPortOffset();
```

### UnusedAssignment
Variable `requestBackup` initializer `ActiveMQDefaultConfiguration.isDefaultHapolicyRequestBackup()` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/ha/ColocatedPolicy.java`
#### Snippet
```java

   /*live stuff*/
   private boolean requestBackup = ActiveMQDefaultConfiguration.isDefaultHapolicyRequestBackup();

   private int backupRequestRetries = ActiveMQDefaultConfiguration.getDefaultHapolicyBackupRequestRetries();
```

### UnusedAssignment
Variable `backupRequestRetryInterval` initializer `ActiveMQDefaultConfiguration.getDefaultHapolicyBackupRequestRetryInterval()` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/ha/ColocatedPolicy.java`
#### Snippet
```java
   private int backupRequestRetries = ActiveMQDefaultConfiguration.getDefaultHapolicyBackupRequestRetries();

   private long backupRequestRetryInterval = ActiveMQDefaultConfiguration.getDefaultHapolicyBackupRequestRetryInterval();

   private int maxBackups = ActiveMQDefaultConfiguration.getDefaultHapolicyMaxBackups();
```

### UnusedAssignment
Variable `backupPortOffset` initializer `ActiveMQDefaultConfiguration.getDefaultHapolicyBackupPortOffset()` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/ha/ColocatedPolicy.java`
#### Snippet
```java
   private int maxBackups = ActiveMQDefaultConfiguration.getDefaultHapolicyMaxBackups();

   private int backupPortOffset = ActiveMQDefaultConfiguration.getDefaultHapolicyBackupPortOffset();

   /*backup stuff*/
```

### UnusedAssignment
The value `retryReplicationWait` assigned to `this.retryReplicationWait` is never used
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/ha/ReplicaPolicy.java`
#### Snippet
```java
      this.voteRetries = voteRetries;
      this.voteRetryWait = voteRetryWait;
      this.retryReplicationWait = retryReplicationWait;
      this.scaleDownPolicy = scaleDownPolicy;
      this.networkHealthCheck = networkHealthCheck;
```

### UnusedAssignment
Variable `query` initializer `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/impl/BridgeImpl.java`
#### Snippet
```java

            if (configuration.getForwardingAddress() != null) {
               ClientSession.AddressQuery query = null;

               try {
```

### UnusedAssignment
Variable `largeData` initializer `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/federation/FederatedQueueConsumerImpl.java`
#### Snippet
```java
            LargeServerMessage lsm = storageManager.createLargeMessage(storageManager.generateID(), message);

            LargeData largeData = null;
            do {
               // block on reading all pending chunks, ok as we are called from an executor
```

### UnusedAssignment
Variable `type` initializer `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/impl/ConfigurationImpl.java`
#### Snippet
```java
               // Declare local variables we will require
               final String propName = resolver.getProperty(name); // Simple name of target property
               Class<?> type = null;                         // Java type of target property
               final int index = resolver.getIndex(name);         // Indexed subscript value (if any)
               final String key = resolver.getKey(name);           // Mapped key value (if any)
```

### UnusedAssignment
Variable `descriptor` initializer `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/impl/ConfigurationImpl.java`
#### Snippet
```java
                  type = Array.get(target, index).getClass();
               } else {
                  PropertyDescriptor descriptor = null;
                  try {
                     descriptor = getPropertyUtils().getPropertyDescriptor(target, name);
```

### UnusedAssignment
Variable `newValue` initializer `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/impl/ConfigurationImpl.java`
#### Snippet
```java

               // Convert the specified value to the required type
               Object newValue = null;
               if (type.isArray() && (index < 0)) { // Scalar value into array
                  if (value == null) {
```

### UnusedAssignment
Variable `resolved` initializer `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/impl/ConfigurationImpl.java`
#### Snippet
```java
         }

         Object resolved = null;

         try {
```

### UnusedAssignment
Variable `alder32Hash` initializer `0` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/impl/ConfigurationImpl.java`
#### Snippet
```java
   public void parsePrefixedProperties(String name, Properties properties, String prefix) throws Exception {
      Map<String, Object> beanProperties = new LinkedHashMap<>();
      long alder32Hash = 0;
      synchronized (properties) {
         Checksum checksum = getCheckSun();
```

### UnusedAssignment
Variable `key` initializer `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/impl/ConfigurationImpl.java`
#### Snippet
```java

         checksum.reset();
         String key = null;
         for (Map.Entry<Object, Object> entry : properties.entrySet()) {
            key = entry.getKey().toString();
```

### UnusedAssignment
Variable `objectName` initializer `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/ArtemisMBeanServerGuard.java`
#### Snippet
```java

   public boolean canInvoke(String object, String operationName) {
      ObjectName objectName = null;
      try {
         objectName = ObjectName.getInstance(object);
```

### UnusedAssignment
Variable `response` initializer `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/ActiveMQPacketHandler.java`
#### Snippet
```java

   private void handleReattachSession(final ReattachSessionMessage request) {
      Packet response = null;

      try {
```

### UnusedAssignment
The value `new ReattachSessionResponseMessage(-1, false)` assigned to `response` is never used
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/ActiveMQPacketHandler.java`
#### Snippet
```java

         if (!server.isStarted()) {
            response = new ReattachSessionResponseMessage(-1, false);
         }

```

### UnusedAssignment
Variable `method` initializer `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/impl/ManagementServiceImpl.java`
#### Snippet
```java
         }

         Method method = null;

         String upperCaseAttribute = attribute.substring(0, 1).toUpperCase() + attribute.substring(1);
```

### UnusedAssignment
Variable `control` initializer `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/impl/ManagementServiceImpl.java`
#### Snippet
```java
      ObjectName objectName = objectNameBuilder.getBroadcastGroupObjectName(configuration.getName());
      BroadcastEndpointFactory endpointFactory = configuration.getEndpointFactory();
      BaseBroadcastGroupControl control = null;
      if (endpointFactory instanceof UDPBroadcastEndpointFactory) {
         control = new BroadcastGroupControlImpl(broadcastGroup, storageManager, configuration, (UDPBroadcastEndpointFactory) endpointFactory);
```

### UnusedAssignment
The value `null` assigned to `messagingServer` is never used
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/impl/ManagementServiceImpl.java`
#### Snippet
```java
      registry.clear();

      messagingServer = null;

      securityRepository = null;
```

### UnusedAssignment
Variable `info` initializer `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/MBeanInfoHelper.java`
#### Snippet
```java

   private static MBeanOperationInfo getOperationInfo(final Method operation) {
      MBeanOperationInfo info = null;
      Class<?> returnType = operation.getReturnType();

```

### UnusedAssignment
Variable `serverChannel` initializer `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/NettyAcceptor.java`
#### Snippet
```java
            address = new InetSocketAddress(h, port);
         }
         Channel serverChannel = null;
         try {
            serverChannel = bootstrap.bind(address).syncUninterruptibly().channel();
```

### UnusedAssignment
Variable `anyDescendentNode` initializer `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/postoffice/impl/AddressPartNode.java`
#### Snippet
```java
      boolean canVisitAnyDescendent = true;
      AddressPartNode<T> node = this;
      AddressPartNode<T> anyDescendentNode = null;
      AddressPartNode<T> anyChildNode = null;
      final int size = paths.length;
```

### UnusedAssignment
Variable `anyChildNode` initializer `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/postoffice/impl/AddressPartNode.java`
#### Snippet
```java
      AddressPartNode<T> node = this;
      AddressPartNode<T> anyDescendentNode = null;
      AddressPartNode<T> anyChildNode = null;
      final int size = paths.length;
      for (int i = startIndex; i < size && node != null; i++) {
```

### UnusedAssignment
Variable `removed` initializer `0` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/QueueControlImpl.java`
#### Snippet
```java
            Filter filter = FilterImpl.createFilter(filterStr);

            int removed = 0;
            try {
               removed = queue.deleteMatchingReferences(flushLimit, filter);
```

### UnusedAssignment
Variable `messageFiles` initializer `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/JournalStorageManager.java`
#### Snippet
```java
      originalBindingsJournal.scheduleCompactAndBlock(-1);

      JournalFile[] messageFiles = null;
      JournalFile[] bindingsFiles = null;

```

### UnusedAssignment
Variable `bindingsFiles` initializer `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/JournalStorageManager.java`
#### Snippet
```java

      JournalFile[] messageFiles = null;
      JournalFile[] bindingsFiles = null;

      // We get a picture of the current sitaution on the large messages
```

### UnusedAssignment
Variable `pendingLargeMessages` initializer `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/JournalStorageManager.java`
#### Snippet
```java
      // We get a picture of the current sitaution on the large messages
      // and we send the current messages while more state is coming
      Map<Long, Pair<String, Long>> pendingLargeMessages = null;

      try {
```

### UnusedAssignment
Variable `policyConfiguration` initializer `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/deployers/impl/FileConfigurationParser.java`
#### Snippet
```java
      connectionRouterConfiguration.setLocalTargetFilter(getString(e, "local-target-filter", connectionRouterConfiguration.getLocalTargetFilter(), Validators.NO_CHECK));

      NamedPropertyConfiguration policyConfiguration = null;
      PoolConfiguration poolConfiguration = null;
      NodeList children = e.getChildNodes();
```

### UnusedAssignment
Variable `poolConfiguration` initializer `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/deployers/impl/FileConfigurationParser.java`
#### Snippet
```java

      NamedPropertyConfiguration policyConfiguration = null;
      PoolConfiguration poolConfiguration = null;
      NodeList children = e.getChildNodes();

```

### UnusedAssignment
Variable `timeoutSeconds` initializer `-1` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/transaction/impl/TransactionImpl.java`
#### Snippet
```java
   private volatile boolean containsPersistent;

   private int timeoutSeconds = -1;

   private Object protocolData;
```

### UnusedAssignment
Variable `date` initializer `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/messagecounter/MessageCounter.java`
#### Snippet
```java
      static final int HOURS = 24;

      GregorianCalendar date = null;

      long[] counters = new long[DayCounter.HOURS];
```

### UnusedAssignment
The value `0` assigned to `i` is never used
in `artemis-server/src/main/java/org/apache/activemq/artemis/utils/SecurityManagerUtil.java`
#### Snippet
```java
         instance = cls.newInstance();
         Method[] methods = cls.getMethods();
         i = 0;
         for (i = 0; i < methods.length; i++) {
            Class<?>[] paramTypes = methods[i].getParameterTypes();
```

### UnusedAssignment
The value `null` assigned to `records` is never used
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/AbstractJournalStorageManager.java`
#### Snippet
```java

         // Release the memory as soon as not needed any longer
         records = null;

         journalLoader.handleAddMessage(queueMap);
```

### UnusedAssignment
Variable `needCheckDepage` initializer `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java
            // We will be using the deliverRunner instance as the guard object to avoid multiple threads executing
            // an asynchronous delivery
            boolean needCheckDepage = false;
            try (ArtemisCloseable metric = measureCritical(CRITICAL_DELIVER)) {
               deliverLock.lock();
```

### UnusedAssignment
The value `null` assigned to `tx` is never used
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java
         if (txCount > 0) {
            tx.commit();
            tx = null;
         }

```

### UnusedAssignment
Variable `messagesIterator` initializer `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java

      LinkedListIterator<PagedReference> pagingIterator = null;
      LinkedListIterator<MessageReference> messagesIterator = null;

      private LinkedListIterator<PagedReference> getPagingIterator() {
```

### UnusedAssignment
Variable `delayBeforeDispatch` initializer `0` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java
   private volatile int consumersBeforeDispatch = 0;

   private volatile long delayBeforeDispatch = 0;

   private final boolean autoDelete;
```

### UnusedAssignment
Variable `createdTimestamp` initializer `-1` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java
   private volatile long ringSize;

   private volatile long createdTimestamp = -1;

   @Override
```

### UnusedAssignment
Variable `consumersBeforeDispatch` initializer `0` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java
   public volatile long dispatchStartTime = -1;

   private volatile int consumersBeforeDispatch = 0;

   private volatile long delayBeforeDispatch = 0;
```

## RuleId[id=ManualArrayToCollectionCopy]
### ManualArrayToCollectionCopy
Manual array to collection copy
in `artemis-maven-plugin/src/main/java/org/apache/activemq/artemis/maven/ArtemisUpgradePlugin.java`
#### Snippet
```java

   private void add(List<String> list, String... str) {
      for (String s : str) {
         list.add(s);
      }
```

### ManualArrayToCollectionCopy
Manual array to collection copy
in `artemis-maven-plugin/src/main/java/org/apache/activemq/artemis/maven/ArtemisCreatePlugin.java`
#### Snippet
```java

   private void add(List<String> list, String... str) {
      for (String s : str) {
         list.add(s);
      }
```

### ManualArrayToCollectionCopy
Manual array to collection copy
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/SpawnedVMSupport.java`
#### Snippet
```java

      if (vmargs != null) {
         for (String arg : vmargs) {
            commandList.add(arg);
         }
```

### ManualArrayToCollectionCopy
Manual array to collection copy
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/SpawnedVMSupport.java`
#### Snippet
```java

      commandList.add(className);
      for (String arg : args) {
         commandList.add(arg);
      }
```

### ManualArrayToCollectionCopy
Manual array to collection copy
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/OptionsUtil.java`
#### Snippet
```java
            Option annotation = field.getAnnotation(Option.class);
            String[] names = annotation.name();
            for (String n : names) {
               options.add(n);
            }
```

### ManualArrayToCollectionCopy
Manual array to collection copy
in `artemis-cli/src/main/java/org/apache/activemq/artemis/util/ServerUtil.java`
#### Snippet
```java
         }

         for (String arg: args) {
            command.add(arg);
         }
```

### ManualArrayToCollectionCopy
Manual array to collection copy
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/server/impl/JMSServerManagerImpl.java`
#### Snippet
```java
         map.put(name, list);
      }
      for (String bindingsItem : bindings) {
         list.add(bindingsItem);
      }
```

### ManualArrayToCollectionCopy
Manual array to collection copy
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/impl/ConfigurationImpl.java`
#### Snippet
```java
         public <T> T convert(Class<T> type, Object value) {
            Set convertedValue = new HashSet();
            for (String entry : value.toString().split(",")) {
               convertedValue.add(entry);
            }
```

### ManualArrayToCollectionCopy
Manual array to collection copy
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/impl/ConfigurationImpl.java`
#### Snippet
```java
         public <T> T convert(Class<T> type, Object value) {
            List convertedValue = new ArrayList<String>();
            for (String entry : value.toString().split(",")) {
               convertedValue.add(entry);
            }
```

### ManualArrayToCollectionCopy
Manual array to collection copy
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/JMXAccessControlList.java`
#### Snippet
```java
            methodRoles.put(prefix, rolesList);
         }
         for (String role : roles) {
            rolesList.add(role);
         }
```

### ManualArrayToCollectionCopy
Manual array to collection copy
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/JMXAccessControlList.java`
#### Snippet
```java

      public void addCatchAll(String... roles) {
         for (String role : roles) {
            catchAllRoles.add(role);
         }
```

### ManualArrayToCollectionCopy
Manual array to collection copy
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/JMXAccessControlList.java`
#### Snippet
```java
            methodPrefixRoles.put(prefix, rolesList);
         }
         for (String role : roles) {
            rolesList.add(role);
         }
```

## RuleId[id=UnnecessarySemicolon]
### UnnecessarySemicolon
Unnecessary semicolon `;`
in `artemis-log-annotation-processor/src/main/java/org/apache/activemq/artemis/logs/annotation/LogMessage.java`
#### Snippet
```java
       */
      @Deprecated
      TRACE;
   }

```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/critical/CriticalAnalyzerPolicy.java`
#### Snippet
```java

public enum CriticalAnalyzerPolicy {
   HALT, SHUTDOWN, LOG;
}

```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/dataformat/JournalCompleteRecordTX.java`
#### Snippet
```java

   public enum TX_RECORD_TYPE {
      COMMIT, PREPARE;
   }

```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/Journal.java`
#### Snippet
```java
       * is live.
       */
      LOADED;
   }

```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTProtocolHandler.java`
#### Snippet
```java

   private enum LinkStealingResult {
      EXISTING_LINK_STOLEN, NEW_LINK_DENIED, NO_ACTION;
   }
}
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQSession.java`
#### Snippet
```java

   enum ConsumerDurability {
      DURABLE, NON_DURABLE;
   }

```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/ServerProducer.java`
#### Snippet
```java

   String getName();
   String getAddress();;

   String getProtocol();
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/HandleStatus.java`
#### Snippet
```java
 */
public enum HandleStatus {
   HANDLED, NO_MATCH, BUSY;
}

```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/impl/MessageLoadBalancingType.java`
#### Snippet
```java

public enum MessageLoadBalancingType {
   OFF, STRICT, ON_DEMAND, OFF_WITH_REDISTRIBUTION;
}

```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/ha/ColocatedHAManager.java`
#### Snippet
```java
      try
         (
            ClusterControl clusterControl = clusterController.connectToNode(connectorPair.getA());
         ) {
         clusterControl.authorize();
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/qourum/SharedNothingBackupQuorum.java`
#### Snippet
```java

   public enum BACKUP_ACTIVATION {
      FAIL_OVER, FAILURE_RETRY, FAILURE_REPLICATING, ALREADY_REPLICATING, STOP;
   }

```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/ConnectorServerFactory.java`
#### Snippet
```java
   }

   private enum AuthenticatorType { NONE, PASSWORD, CERTIFICATE };

   private MBeanServer server;
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/view/predicate/ActiveMQFilterPredicate.java`
#### Snippet
```java

   enum Operation {
      CONTAINS, NOT_CONTAINS, EQUALS, GREATER_THAN, LESS_THAN;
   }

```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/postoffice/RoutingStatus.java`
#### Snippet
```java
   NO_BINDINGS,
   NO_BINDINGS_DLA,
   DUPLICATED_ID;
}

```

## RuleId[id=SimplifyStreamApiCallChains]
### SimplifyStreamApiCallChains
Can be replaced with 'String.join'
in `artemis-quorum-ri/src/main/java/org/apache/activemq/artemis/quorum/zookeeper/CuratorDistributedPrimitiveManager.java`
#### Snippet
```java
      RETRIES_PARAM,
      RETRIES_MS_PARAM).collect(Collectors.toSet());
   private static final String VALID_PARAMS_ON_ERROR = VALID_PARAMS.stream().collect(joining(","));
   // It's 9 times the default ZK tick time ie 2000 ms
   private static final String DEFAULT_SESSION_TIMEOUT_MS = Integer.toString(18_000);
```

### SimplifyStreamApiCallChains
Can be replaced with 'collection.toArray()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/cursor/impl/PageSubscriptionImpl.java`
#### Snippet
```java
               return;
            }
            localScanList = scanList.stream().toArray(i -> new PageScan[i]);
            scanList.clear();
         }
```

### SimplifyStreamApiCallChains
'!Stream.anyMatch(...)' can be replaced with 'noneMatch()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ActiveMQServerImpl.java`
#### Snippet
```java
            for (Queue queue : listConfiguredQueues(addressName)) {
               List<QueueConfiguration> queueConfigsInConfig = queuesInConfig.get(queue.getName());
               if (queueConfigsInConfig == null || !queueConfigsInConfig.stream().anyMatch(
                  queueConfiguration -> queueConfiguration.getAddress().equals(addressName))) {
                  ActiveMQServerLogger.LOGGER.undeployQueue(queue.getName());
```

### SimplifyStreamApiCallChains
Can be replaced with 'min()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/impl/ConfigurationImpl.java`
#### Snippet
```java
               // but not initialised from a String form (eg: uri)
               && !String.class.equals(method.getParameterTypes()[1]))))
            .sorted((method1, method2) -> method2.getParameterCount() - method1.getParameterCount()).findFirst().orElse(null);

         if (candidate == null) {
```

## RuleId[id=StringOperationCanBeSimplified]
### StringOperationCanBeSimplified
Unnecessary empty string argument
in `artemis-maven-plugin/src/main/java/org/apache/activemq/artemis/maven/ArtemisCreatePlugin.java`
#### Snippet
```java
         }

         commandLineStream.println("");

         if (originalFile.toFile().isDirectory()) {
```

### StringOperationCanBeSimplified
Unnecessary string length argument
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/SelectorTranslator.java`
#### Snippet
```java

         if (startPos < input.length()) {
            buff.append(input.substring(startPos, input.length()));
         }

```

### StringOperationCanBeSimplified
Unnecessary empty string argument
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/ByteUtil.java`
#### Snippet
```java

   public static String readLine(ActiveMQBuffer buffer) {
      StringBuilder sb = new StringBuilder("");
      char c = buffer.readChar();
      while (c != '\n') {
```

### StringOperationCanBeSimplified
Call to `substring()` is redundant
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/ByteUtil.java`
#### Snippet
```java
      buffer.append("/*  0 */ \"");
      for (int i = 0; i < str.length(); i += groupSize) {
         buffer.append(str.substring(i, i + Math.min(str.length() - i, groupSize)));

         if ((i + groupSize) % lineBreak == 0) {
```

### StringOperationCanBeSimplified
Inefficient conversion from ByteArrayOutputStream
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java

      // Return value according to relevant encoding.
      return new String(baos.toByteArray(), Base64.PREFERRED_ENCODING);

   } // end encode
```

### StringOperationCanBeSimplified
Inefficient conversion from ByteArrayOutputStream
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java

         // Return value according to relevant encoding.
         return new String(baos.toByteArray(), Base64.PREFERRED_ENCODING);
      } else { // Else, don't compress. Better not to use streams at all then.

```

### StringOperationCanBeSimplified
Inefficient conversion from ByteArrayOutputStream
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/InstallAbstract.java`
#### Snippet
```java
         copy(in, out);
      }
      return applyFilters(new String(out.toByteArray(), StandardCharsets.UTF_8), filters);
   }

```

### StringOperationCanBeSimplified
Unnecessary empty string argument
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/journal/PerfJournal.java`
#### Snippet
```java
      }

      System.out.println("");
      System.out.println("Auto tuning journal ...");

```

### StringOperationCanBeSimplified
Call to `toString()` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/xml/XmlDataExporter.java`
#### Snippet
```java
            routingTypes.append(routingType.toString()).append(", ");
         }
         xmlWriter.writeAttribute(XmlDataConstants.ADDRESS_BINDING_ROUTING_TYPE, routingTypes.toString().substring(0, routingTypes.length() - 2));
         xmlWriter.writeAttribute(XmlDataConstants.ADDRESS_BINDING_NAME, bindingEncoding.getName().toString());
         xmlWriter.writeAttribute(XmlDataConstants.ADDRESS_BINDING_ID, Long.toString(bindingEncoding.getId()));
```

### StringOperationCanBeSimplified
Unnecessary empty string argument
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Create.java`
#### Snippet
```java
      writeEtc(ETC_JOLOKIA_ACCESS_XML, etcFolder, filters, false);

      context.out.println("");
      context.out.println("You can now start the broker by executing:  ");
      context.out.println("");
```

### StringOperationCanBeSimplified
Unnecessary empty string argument
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Create.java`
#### Snippet
```java
      context.out.println("");
      context.out.println("You can now start the broker by executing:  ");
      context.out.println("");
      context.out.println(String.format("   \"%s\" run", path(new File(directory, "bin/artemis"))));

```

### StringOperationCanBeSimplified
Unnecessary empty string argument
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Create.java`
#### Snippet
```java

      File service = new File(directory, BIN_ARTEMIS_SERVICE);
      context.out.println("");

      if (!IS_WINDOWS || IS_CYGWIN) {
```

### StringOperationCanBeSimplified
Unnecessary empty string argument
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Create.java`
#### Snippet
```java
      if (!IS_WINDOWS || IS_CYGWIN) {
         context.out.println("Or you can run the broker in the background using:");
         context.out.println("");
         context.out.println(String.format("   \"%s\" start", path(service)));
         context.out.println("");
```

### StringOperationCanBeSimplified
Unnecessary empty string argument
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Create.java`
#### Snippet
```java
         context.out.println("");
         context.out.println(String.format("   \"%s\" start", path(service)));
         context.out.println("");
      }

```

### StringOperationCanBeSimplified
Unnecessary empty string argument
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Create.java`
#### Snippet
```java
         service = new File(directory, BIN_ARTEMIS_SERVICE_EXE);
         context.out.println("Or you can setup the broker as Windows service and run it in the background:");
         context.out.println("");
         context.out.println(String.format("   \"%s\" install", path(service)));
         context.out.println(String.format("   \"%s\" start", path(service)));
```

### StringOperationCanBeSimplified
Unnecessary empty string argument
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Create.java`
#### Snippet
```java
         context.out.println(String.format("   \"%s\" install", path(service)));
         context.out.println(String.format("   \"%s\" start", path(service)));
         context.out.println("");
         context.out.println("   To stop the windows service:");
         context.out.println(String.format("      \"%s\" stop", path(service)));
```

### StringOperationCanBeSimplified
Unnecessary empty string argument
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Create.java`
#### Snippet
```java
         context.out.println("   To stop the windows service:");
         context.out.println(String.format("      \"%s\" stop", path(service)));
         context.out.println("");
         context.out.println("   To uninstall the windows service");
         context.out.println(String.format("      \"%s\" uninstall", path(service)));
```

### StringOperationCanBeSimplified
Unnecessary empty string argument
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Create.java`
#### Snippet
```java
         try {
            int writes = 250;
            getActionContext().out.println("");
            getActionContext().out.println("Auto tuning journal ...");

```

### StringOperationCanBeSimplified
Call to `toString()` is redundant
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/converter/AMQPMessageSupport.java`
#### Snippet
```java

   public static String toAnnotationName(String key) {
      if (!key.startsWith(X_OPT_PREFIX.toString())) {
         if (key.startsWith(AMQ_PROPERTY_PREFIX)) {
            return X_OPT_PREFIX.concat(key.substring(AMQ_PROPERTY_PREFIX.length()).replace('_', '-'));
```

### StringOperationCanBeSimplified
Unnecessary string length argument
in `artemis-jdbc-store/src/main/java/org/apache/activemq/artemis/jdbc/store/file/JDBCSequentialFile.java`
#### Snippet
```java
      this.fileFactory = fileFactory;
      this.filename = filename;
      this.extension = filename.contains(".") ? filename.substring(filename.lastIndexOf(".") + 1, filename.length()) : "";
      this.executor = executor;
      this.writeLock = writeLock;
```

### StringOperationCanBeSimplified
Call to `substring()` is redundant
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/JsonUtil.java`
#### Snippet
```java
   public static String truncateString(final String str, final int valueSizeLimit) {
      if (valueSizeLimit >= 0 && str.length() > valueSizeLimit) {
         return new StringBuilder(valueSizeLimit + 32).append(str.substring(0, valueSizeLimit)).append(", + ").append(str.length() - valueSizeLimit).append(" more").toString();
      } else {
         return str;
```

### StringOperationCanBeSimplified
Call to `toString()` is redundant
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/management/ObjectNameBuilder.java`
#### Snippet
```java
    */
   public ObjectName getDivertObjectName(final String name, String address) throws Exception {
      return ObjectName.getInstance(String.format("%s,component=addresses,address=%s,subcomponent=diverts,divert=%s", getActiveMQServerName(), ObjectName.quote(address.toString()), ObjectName.quote(name.toString())));
   }

```

### StringOperationCanBeSimplified
Call to `toString()` is redundant
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/management/ObjectNameBuilder.java`
#### Snippet
```java
    */
   public ObjectName getDivertObjectName(final String name, String address) throws Exception {
      return ObjectName.getInstance(String.format("%s,component=addresses,address=%s,subcomponent=diverts,divert=%s", getActiveMQServerName(), ObjectName.quote(address.toString()), ObjectName.quote(name.toString())));
   }

```

### StringOperationCanBeSimplified
Call to `toString()` is redundant
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/message/openmbean/MessageOpenTypeFactory.java`
#### Snippet
```java
         rc.put(CompositeDataConstants.USER_ID, "");
      }
      rc.put(CompositeDataConstants.ADDRESS, m.getAddress() == null ? "" : m.getAddress().toString());
      rc.put(CompositeDataConstants.DURABLE, m.isDurable());
      rc.put(CompositeDataConstants.EXPIRATION, m.getExpiration());
```

### StringOperationCanBeSimplified
Call to `toString()` is redundant
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/utils/PrefixUtil.java`
#### Snippet
```java
    *  This is mostly used on conversions to treat JMSReplyTo or similar usages on core protocol */
   public static String getURIPrefix(String address) {
      int index = address.toString().indexOf("://");
      if (index > 0) {
         return address.substring(0, index + 3);
```

### StringOperationCanBeSimplified
`toLowerCase()` call can be replaced with 'equalsIgnoreCase()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/storage/DatabaseStorageConfiguration.java`
#### Snippet
```java

   public void addDataSourceProperty(String key, String value) {
      if (value.toLowerCase().equals("true") || value.toLowerCase().equals("false")) {
         dataSourceProperties.put(key, Boolean.parseBoolean(value.toLowerCase()));
      } else {
```

### StringOperationCanBeSimplified
`toLowerCase()` call can be replaced with 'equalsIgnoreCase()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/storage/DatabaseStorageConfiguration.java`
#### Snippet
```java

   public void addDataSourceProperty(String key, String value) {
      if (value.toLowerCase().equals("true") || value.toLowerCase().equals("false")) {
         dataSourceProperties.put(key, Boolean.parseBoolean(value.toLowerCase()));
      } else {
```

### StringOperationCanBeSimplified
Unnecessary string length argument
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/impl/ClusterConnectionBridge.java`
#### Snippet
```java
      for (String s : list) {
         if (s.startsWith("!")) {
            excludes.add(s.substring(1, s.length()));
         } else {
            includes.add(s);
```

### StringOperationCanBeSimplified
Unnecessary string length argument
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/impl/ClusterConnectionBridge.java`
#### Snippet
```java
      if (!address.contains(",")) {
         if (address.startsWith("!")) {
            stringBuilder.append(ManagementHelper.HDR_ADDRESS + " NOT LIKE '" + address.substring(1, address.length()) + "%'");
         } else {
            stringBuilder.append(ManagementHelper.HDR_ADDRESS + " LIKE '" + address + "%'");
```

### StringOperationCanBeSimplified
Call to `toString()` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/federation/FederationDownstream.java`
#### Snippet
```java
               this.clientSession.addFailureListener(this);
               this.clientSession.addMetaData(FederatedQueueConsumer.FEDERATION_NAME, federation.getName().toString());
               this.clientSession.addMetaData(FEDERATION_DOWNSTREAM_NAME, config.getName().toString());
               this.clientSession.start();

```

### StringOperationCanBeSimplified
Call to `toString()` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/ActiveMQServerControlImpl.java`
#### Snippet
```java
            Set<ServerSession> sessions = server.getSessions();
            for (ServerSession session : sessions) {
               if (session.getName().equals(sessionID.toString())) {
                  Set<ServerConsumer> serverConsumers = session.getServerConsumers();
                  for (ServerConsumer serverConsumer : serverConsumers) {
```

### StringOperationCanBeSimplified
Call to `toString()` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/ActiveMQServerControlImpl.java`
#### Snippet
```java
            List<ServerSession> sessions = server.getSessions(connectionID);
            for (ServerSession session : sessions) {
               if (session.getName().equals(ID.toString())) {
                  session.close(true);
                  return true;
```

## RuleId[id=MethodOverloadsParentMethod]
### MethodOverloadsParentMethod
Method `containsAll()` overloads a compatible method of a superclass, when overriding might have been intended
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/LongHashSet.java`
#### Snippet
```java
    * @return true if every element in other is in this.
    */
   public boolean containsAll(final LongHashSet other) {
      for (final long value : other.values) {
         if (value != MISSING_VALUE && !contains(value)) {
```

## RuleId[id=UnnecessaryCallToStringValueOf]
### UnnecessaryCallToStringValueOf
Unnecessary `Integer.toString()` call
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/ByteUtil.java`
#### Snippet
```java
               buffer.append(" ");
            }
            buffer.append(Integer.toString(i) + " */ \"");
         } else if ((i + groupSize) % groupSize == 0 && str.length() - i > groupSize) {
            buffer.append("\" + \"");
```

## RuleId[id=NonSynchronizedMethodOverridesSynchronizedMethod]
### NonSynchronizedMethodOverridesSynchronizedMethod
Unsynchronized method `close()` overrides synchronized method
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/io/aio/AIOSequentialFile.java`
#### Snippet
```java

   @Override
   public void close() throws IOException, InterruptedException, ActiveMQException {
      close(true, true);
   }
```

### NonSynchronizedMethodOverridesSynchronizedMethod
Unsynchronized method `put()` overrides synchronized method
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/impl/ConfigurationImpl.java`
#### Snippet
```java

      @Override
      public Object put(Object key, Object value) {
         return orderedMap.put(key.toString(), value.toString());
      }
```

### NonSynchronizedMethodOverridesSynchronizedMethod
Unsynchronized method `clear()` overrides synchronized method
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/impl/ConfigurationImpl.java`
#### Snippet
```java

      @Override
      public void clear() {
         orderedMap.clear();
      }
```

### NonSynchronizedMethodOverridesSynchronizedMethod
Unsynchronized method `start()` overrides synchronized method
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/federation/queue/FederatedQueue.java`
#### Snippet
```java

   @Override
   public void start() {
      super.start();
      server.getPostOffice()
```

### NonSynchronizedMethodOverridesSynchronizedMethod
Unsynchronized method `stop()` overrides synchronized method
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/JournalStorageManager.java`
#### Snippet
```java

   @Override
   public void stop(boolean ioCriticalError, boolean sendFailover) throws Exception {
      try (ArtemisCloseable critical = measureCritical(CRITICAL_STOP)) {
         synchronized (this) {
```

## RuleId[id=CollectionContainsUrl]
### CollectionContainsUrl
Set `urls` may contain URL objects
in `artemis-commons/src/main/java/org/apache/activemq/artemis/core/server/NetworkHealthCheck.java`
#### Snippet
```java
   private final Set<ActiveMQComponent> componentList = new ConcurrentHashSet<>();
   private final Set<String> addresses = new ConcurrentHashSet<>();
   private final Set<URL> urls = new ConcurrentHashSet<>();
   private NetworkInterface networkInterface;

```

### CollectionContainsUrl
Map `registry` may contain URL objects
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/reload/ReloadManagerImpl.java`
#### Snippet
```java
   private volatile Runnable tick;

   private final Map<URL, ReloadRegistry> registry = new HashMap<>();

   public ReloadManagerImpl(ScheduledExecutorService scheduledExecutorService, Executor executor, long checkPeriod) {
```

## RuleId[id=ProtectedMemberInFinalClass]
### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/XPathExpression.java`
#### Snippet
```java
   }

   protected static Map<String, Boolean> getFeatures() throws ParserConfigurationException {
      Map<String, Boolean> features = new HashMap<>();
      Properties properties = System.getProperties();
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `artemis-protocols/artemis-stomp-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/stomp/StompConnection.java`
#### Snippet
```java
   }

   protected void validate() throws ActiveMQStompException {
      if (!this.valid) {
         throw BUNDLE.invalidConnection().setHandler(frameHandler);
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `artemis-protocols/artemis-stomp-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/stomp/StompConnection.java`
#### Snippet
```java
   }

   protected void beginTransaction(String txID) throws ActiveMQStompException {
      try {
         manager.beginTransaction(this, txID);
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `artemis-protocols/artemis-stomp-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/stomp/StompConnection.java`
#### Snippet
```java
   }

   protected void sendServerMessage(ICoreMessage message, String txID) throws ActiveMQStompException {
      try {
         StompSession stompSession = getSession(txID);
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/io/buffer/TimedBuffer.java`
#### Snippet
```java
    * @param sleepNanos
    */
   protected void sleep(long sleepNanos) {
      LockSupport.parkNanos(sleepNanos);
   }
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/io/buffer/TimedBuffer.java`
#### Snippet
```java
   protected static final int CRITICAL_PATH_CHECK_SIZE = 3;
   protected static final int CRITICAL_PATH_ADD_BYTES = 4;
   protected static final int CRITICAL_PATH_SET_OBSERVER = 5;

   private static final Logger logger = LoggerFactory.getLogger(MethodHandles.lookup().lookupClass());
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/io/buffer/TimedBuffer.java`
#### Snippet
```java
    * Sub classes (tests basically) can use this to override disabling spinning
    */
   protected void stopSpin() {
      if (spinning) {
         try {
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/io/buffer/TimedBuffer.java`
#### Snippet
```java
public final class TimedBuffer extends CriticalComponentImpl {

   protected static final int CRITICAL_PATHS = 6;
   protected static final int CRITICAL_PATH_FLUSH = 0;
   protected static final int CRITICAL_PATH_STOP = 1;
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/io/buffer/TimedBuffer.java`
#### Snippet
```java
    * Sub classes (tests basically) can use this to override disabling spinning
    */
   protected void startSpin() {
      if (!spinning) {
         spinLimiter.release();
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/io/buffer/TimedBuffer.java`
#### Snippet
```java
   protected static final int CRITICAL_PATH_STOP = 1;
   protected static final int CRITICAL_PATH_START = 2;
   protected static final int CRITICAL_PATH_CHECK_SIZE = 3;
   protected static final int CRITICAL_PATH_ADD_BYTES = 4;
   protected static final int CRITICAL_PATH_SET_OBSERVER = 5;
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/io/buffer/TimedBuffer.java`
#### Snippet
```java
   protected static final int CRITICAL_PATHS = 6;
   protected static final int CRITICAL_PATH_FLUSH = 0;
   protected static final int CRITICAL_PATH_STOP = 1;
   protected static final int CRITICAL_PATH_START = 2;
   protected static final int CRITICAL_PATH_CHECK_SIZE = 3;
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/io/buffer/TimedBuffer.java`
#### Snippet
```java
   protected static final int CRITICAL_PATH_START = 2;
   protected static final int CRITICAL_PATH_CHECK_SIZE = 3;
   protected static final int CRITICAL_PATH_ADD_BYTES = 4;
   protected static final int CRITICAL_PATH_SET_OBSERVER = 5;

```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/io/buffer/TimedBuffer.java`
#### Snippet
```java
   protected static final int CRITICAL_PATH_FLUSH = 0;
   protected static final int CRITICAL_PATH_STOP = 1;
   protected static final int CRITICAL_PATH_START = 2;
   protected static final int CRITICAL_PATH_CHECK_SIZE = 3;
   protected static final int CRITICAL_PATH_ADD_BYTES = 4;
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/io/buffer/TimedBuffer.java`
#### Snippet
```java

   protected static final int CRITICAL_PATHS = 6;
   protected static final int CRITICAL_PATH_FLUSH = 0;
   protected static final int CRITICAL_PATH_STOP = 1;
   protected static final int CRITICAL_PATH_START = 2;
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/FileWrapperJournal.java`
#### Snippet
```java
   private final ConcurrentLongHashMap<AtomicInteger> transactions = new ConcurrentLongHashMap<>();
   private final JournalImpl journal;
   protected volatile JournalFile currentFile;

   @Override
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/ActiveMQRASessionFactoryImpl.java`
#### Snippet
```java
    * @throws JMSException Thrown if an error occurs
    */
   protected ActiveMQRASession allocateConnection(final int sessionType) throws JMSException {
      return allocateConnection(false, Session.AUTO_ACKNOWLEDGE, sessionType);
   }
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/ActiveMQRASessionFactoryImpl.java`
#### Snippet
```java
    * @throws JMSException Thrown if an error occurs
    */
   protected ActiveMQRASession allocateConnection(boolean transacted,
                                                  int acknowledgeMode,
                                                  final int sessionType) throws JMSException {
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/ActiveMQRASessionFactoryImpl.java`
#### Snippet
```java
    * @throws IllegalStateException Thrown if closed
    */
   protected void checkClosed() throws IllegalStateException {
      logger.trace("checkClosed() {}", this);

```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/ActiveMQRAManagedConnection.java`
#### Snippet
```java
    * @param handle The handle to remove.
    */
   protected void removeHandle(final ActiveMQRASession handle) {
      logger.trace("removeHandle({})", handle);

```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/ActiveMQRAManagedConnection.java`
#### Snippet
```java
    * @return The user name
    */
   protected String getUserName() {
      logger.trace("getUserName()");

```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/ActiveMQRAManagedConnection.java`
#### Snippet
```java
    * @param event The event to send.
    */
   protected void sendEvent(final ConnectionEvent event) {
      logger.trace("sendEvent({})", event);

```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/ActiveMQRAManagedConnection.java`
#### Snippet
```java
    * @return The connection factory for this connection.
    */
   protected ActiveMQRAManagedConnectionFactory getManagedConnectionFactory() {
      logger.trace("getManagedConnectionFactory()");

```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/ActiveMQRAManagedConnection.java`
#### Snippet
```java
    * @return The connection request info for this connection.
    */
   protected ActiveMQRAConnectionRequestInfo getCRI() {
      logger.trace("getCRI()");

```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/ActiveMQRAManagedConnection.java`
#### Snippet
```java
    * Unlock the managed connection
    */
   protected void unlock() {
      logger.trace("unlock()");

```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/ActiveMQRAManagedConnection.java`
#### Snippet
```java
    * Aqquire a lock on the managed connection
    */
   protected void lock() {
      logger.trace("lock()");

```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/ActiveMQRAManagedConnection.java`
#### Snippet
```java
    * @throws JMSException
    */
   protected Session getSession() throws JMSException {
      if (xaResource != null && inManagedTx) {
         logger.trace("getSession() -> XA session {}", xaSession.getSession());
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/ActiveMQRAManagedConnection.java`
#### Snippet
```java
    * @throws JMSException Thrown if an error occurs
    */
   protected void tryLock() throws JMSException {
      logger.trace("tryLock()");

```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/ActiveMQRAManagedConnection.java`
#### Snippet
```java
   }

   protected void setInManagedTx(boolean inManagedTx) {
      this.inManagedTx = inManagedTx;
   }
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/ActiveMQRAManagedConnectionFactory.java`
#### Snippet
```java
    * @return The properties
    */
   protected ActiveMQRAMCFProperties getProperties() {
      logger.trace("getProperties()");

```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/xml/XmlDataExporter.java`
#### Snippet
```java
   }

   protected void writeOutput(OutputStream out) throws Exception {
      XMLOutputFactory factory = XMLOutputFactory.newInstance();
      XMLStreamWriter rawXmlWriter = factory.createXMLStreamWriter(out, "UTF-8");
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/perf/AsyncJms2ProducerFacade.java`
#### Snippet
```java

   private final long id;
   protected final Session session;
   private final MessageProducer producer;

```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQMessageConsumer.java`
#### Snippet
```java


   protected ActiveMQMessageConsumer(final ConnectionFactoryOptions options,
                                     final ActiveMQConnection connection,
                                     final ActiveMQSession session,
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQQueueBrowser.java`
#### Snippet
```java


   protected ActiveMQQueueBrowser(final ConnectionFactoryOptions options,
                                  final ActiveMQQueue queue,
                                  final String messageSelector,
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/ChannelImpl.java`
#### Snippet
```java
   }

   protected ResponseCache getCache() {
      return responseAsyncCache;
   }
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/ChannelImpl.java`
#### Snippet
```java
      }

      protected static String idToString(long code) {
         for (CHANNEL_ID channel : EnumSet.allOf(CHANNEL_ID.class)) {
            if (channel.id == code)
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/impl/PageTransactionInfoImpl.java`
#### Snippet
```java
    * @param tx
    */
   protected UpdatePageTXOperation internalUpdatePageManager(final StorageManager storageManager,
                                                             final PagingManager pagingManager,
                                                             final Transaction tx,
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/impl/PagingManagerImpl.java`
#### Snippet
```java
   }

   protected void checkMemoryRelease() {
      if (!diskFull && (maxSize < 0 || !globalFull) && !blockedStored.isEmpty()) {
         if (!memoryCallback.isEmpty()) {
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/impl/PagingManagerImpl.java`
#### Snippet
```java
    * For tests only!
    */
   protected void setDiskFull(boolean diskFull) {
      this.diskFull = diskFull;
   }
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/cursor/impl/PageSubscriptionImpl.java`
#### Snippet
```java
       *
       */
      protected void checkDone() {
         if (isDone()) {
            onPageDone(this);
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/postoffice/impl/AddressPartNode.java`
#### Snippet
```java
   }

   protected void pruneIfEmpty() {
      if (parent != null && childNodes.isEmpty() && values.isEmpty()) {
         parent.removeChild(this);
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/postoffice/impl/AddressPartNode.java`
#### Snippet
```java
public final class AddressPartNode<T> {

   protected final String ANY_CHILD;
   protected final String ANY_DESCENDENT;

```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/postoffice/impl/AddressPartNode.java`
#### Snippet
```java

   protected final String ANY_CHILD;
   protected final String ANY_DESCENDENT;

   private final AddressPartNode<T> parent;
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/postoffice/impl/AddressPartNode.java`
#### Snippet
```java
   }

   protected void removeChild(final AddressPartNode<T> node) {
      childNodes.remove(node.getPath());
      pruneIfEmpty();
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/deployers/impl/FileConfigurationParser.java`
#### Snippet
```java
   }

   protected CoreAddressConfiguration parseAddressConfiguration(final Node node) {
      CoreAddressConfiguration addressConfiguration = new CoreAddressConfiguration();

```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/deployers/impl/FileConfigurationParser.java`
#### Snippet
```java
   }

   protected QueueConfiguration parseQueueConfiguration(final Node node) {
      String name = getAttributeValue(node, "name");
      String address = null;
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/deployers/impl/FileConfigurationParser.java`
#### Snippet
```java
    * @return
    */
   protected Pair<String, AddressSettings> parseAddressSettings(final Node node) {
      String match = getAttributeValue(node, "match");

```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/deployers/impl/FileConfigurationParser.java`
#### Snippet
```java
    * @return
    */
   protected ResourceLimitSettings parseResourceLimitSettings(final Node node) {
      ResourceLimitSettings resourceLimitSettings = new ResourceLimitSettings();

```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/deployers/impl/FileConfigurationParser.java`
#### Snippet
```java
    * @return
    */
   protected Pair<String, Set<Role>> parseSecurityRoles(final Node node, final Map<String, Set<String>> roleMappings) {
      final String match = node.getAttributes().getNamedItem("match").getNodeValue();

```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/deployers/impl/FileConfigurationParser.java`
#### Snippet
```java
    * @param e
    */
   protected void parseWildcardConfiguration(final Element e, final Configuration mainConfig) {
      WildcardConfiguration conf = mainConfig.getWildcardConfiguration();

```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/DescribeJournal.java`
#### Snippet
```java
   }

   protected static void printCounters(final PrintStream out, final Map<Long, PageSubscriptionCounterImpl> counters) {
      for (Map.Entry<Long, PageSubscriptionCounterImpl> entry : counters.entrySet()) {
         out.println("Queue " + entry.getKey() + " value=" + entry.getValue().getValue());
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/DescribeJournal.java`
#### Snippet
```java
   }

   protected static PageSubscriptionCounterImpl lookupCounter(Map<Long, PageSubscriptionCounterImpl> counters,
                                                              long queueIDForCounter) {
      PageSubscriptionCounterImpl subsCounter;
```

## RuleId[id=PublicFieldAccessedInSynchronizedContext]
### PublicFieldAccessedInSynchronizedContext
Non-private field `data` accessed in synchronized context
in `artemis-protocols/artemis-stomp-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/stomp/StompDecoder.java`
#### Snippet
```java
      int readable = buffer.readableBytes();

      if (data + readable >= workingBuffer.length) {
         resizeWorking(data + readable);
      }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `workingBuffer` accessed in synchronized context
in `artemis-protocols/artemis-stomp-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/stomp/StompDecoder.java`
#### Snippet
```java
      int readable = buffer.readableBytes();

      if (data + readable >= workingBuffer.length) {
         resizeWorking(data + readable);
      }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `data` accessed in synchronized context
in `artemis-protocols/artemis-stomp-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/stomp/StompDecoder.java`
#### Snippet
```java

      if (data + readable >= workingBuffer.length) {
         resizeWorking(data + readable);
      }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `workingBuffer` accessed in synchronized context
in `artemis-protocols/artemis-stomp-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/stomp/StompDecoder.java`
#### Snippet
```java
      }

      buffer.readBytes(workingBuffer, data, readable);

      data += readable;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `data` accessed in synchronized context
in `artemis-protocols/artemis-stomp-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/stomp/StompDecoder.java`
#### Snippet
```java
      }

      buffer.readBytes(workingBuffer, data, readable);

      data += readable;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `data` accessed in synchronized context
in `artemis-protocols/artemis-stomp-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/stomp/StompDecoder.java`
#### Snippet
```java
      buffer.readBytes(workingBuffer, data, readable);

      data += readable;

      if (command == null) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `command` accessed in synchronized context
in `artemis-protocols/artemis-stomp-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/stomp/StompDecoder.java`
#### Snippet
```java
      data += readable;

      if (command == null) {
         if (!parseCommand()) {
            return null;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `readingHeaders` accessed in synchronized context
in `artemis-protocols/artemis-stomp-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/stomp/StompDecoder.java`
#### Snippet
```java
      //if command is not null, should it automatically be in
      //reading headers mode? think of get rid of readingHeaders flag
      if (readingHeaders) {
         if (!parseHeaders()) {
            return null;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.scheduledExecutorService` accessed in synchronized context
in `artemis-commons/src/main/java/org/apache/activemq/artemis/core/server/ActiveMQScheduledComponent.java`
#### Snippet
```java
      }
      if (startedOwnScheduler) {
         this.scheduledExecutorService.shutdownNow();
         scheduledExecutorService = null;
         startedOwnScheduler = false;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `scheduledExecutorService` accessed in synchronized context
in `artemis-commons/src/main/java/org/apache/activemq/artemis/core/server/ActiveMQScheduledComponent.java`
#### Snippet
```java
      if (startedOwnScheduler) {
         this.scheduledExecutorService.shutdownNow();
         scheduledExecutorService = null;
         startedOwnScheduler = false;
      }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `scheduledExecutorService` accessed in synchronized context
in `artemis-commons/src/main/java/org/apache/activemq/artemis/core/server/ActiveMQScheduledComponent.java`
#### Snippet
```java
      }
      isStarted = true;
      if (scheduledExecutorService == null) {
         scheduledExecutorService = new ScheduledThreadPoolExecutor(1, getThreadFactory());
         startedOwnScheduler = true;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `scheduledExecutorService` accessed in synchronized context
in `artemis-commons/src/main/java/org/apache/activemq/artemis/core/server/ActiveMQScheduledComponent.java`
#### Snippet
```java
      isStarted = true;
      if (scheduledExecutorService == null) {
         scheduledExecutorService = new ScheduledThreadPoolExecutor(1, getThreadFactory());
         startedOwnScheduler = true;

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `scheduledExecutorService` accessed in synchronized context
in `artemis-commons/src/main/java/org/apache/activemq/artemis/core/server/ActiveMQScheduledComponent.java`
#### Snippet
```java
      if (period >= 0) {
         final AtomicBoolean booked = this.bookedForRunning;
         future = scheduledExecutorService.scheduleWithFixedDelay(() -> runForScheduler(booked), initialDelay >= 0 ? initialDelay : period, period, timeUnit);
      } else {
         logger.trace("did not start scheduled executor on {} because period was configured as {}", this, period);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.timedBuffer` accessed in synchronized context
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/io/aio/AIOSequentialFile.java`
#### Snippet
```java
      super.close();
      opened = false;
      this.timedBuffer = null;

      try {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `fileSize` accessed in synchronized context
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/io/aio/AIOSequentialFile.java`
#### Snippet
```java
      position.set(0);

      fileSize = aioFile.getSize();
   }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `fileSize` accessed in synchronized context
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/io/aio/AIOSequentialFile.java`
#### Snippet
```java
      aioFile.fill(aioFactory.getAlignment(), size);

      fileSize = aioFile.getSize();
   }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `repeat` accessed in synchronized context
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/LinkedListImpl.java`
#### Snippet
```java
            Node<E> e = getNode();

            if (repeat) {
               repeat = false;

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `repeat` accessed in synchronized context
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/LinkedListImpl.java`
#### Snippet
```java

            if (repeat) {
               repeat = false;

               if (e != null) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `last` accessed in synchronized context
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/LinkedListImpl.java`
#### Snippet
```java
            }

            if (e == null || e == last) {
               if (canAdvance()) {
                  advance();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `last` accessed in synchronized context
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/LinkedListImpl.java`
#### Snippet
```java
            }

            last = e;

            repeat = false;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `repeat` accessed in synchronized context
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/LinkedListImpl.java`
#### Snippet
```java
            last = e;

            repeat = false;

            return e.val();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `last` accessed in synchronized context
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/LinkedListImpl.java`
#### Snippet
```java
      public void remove() {
         synchronized (LinkedListImpl.this) {
            if (last == null) {
               throw new NoSuchElementException();
            }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `current` accessed in synchronized context
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/LinkedListImpl.java`
#### Snippet
```java
            }

            if (current == null) {
               return;
            }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `current` accessed in synchronized context
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/LinkedListImpl.java`
#### Snippet
```java
            }

            Node<E> prev = current.prev;

            if (prev != null) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `last` accessed in synchronized context
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/LinkedListImpl.java`
#### Snippet
```java
               LinkedListImpl.this.removeAfter(prev);

               last = null;
            }
         }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `last` accessed in synchronized context
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/LinkedListImpl.java`
#### Snippet
```java
            Node<E> e = getNode();

            if (e != null && (e != last || repeat)) {
               return true;
            }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `repeat` accessed in synchronized context
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/LinkedListImpl.java`
#### Snippet
```java
            Node<E> e = getNode();

            if (e != null && (e != last || repeat)) {
               return true;
            }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `state` accessed in synchronized context
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTSession.java`
#### Snippet
```java

   synchronized void stop(boolean failure) throws Exception {
      state.setFailed(failure);

      if (!stopped) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `state` accessed in synchronized context
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTSession.java`
#### Snippet
```java
         }

         if (state != null) {
            state.setAttached(false);
            state.setDisconnectedTime(System.currentTimeMillis());
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `state` accessed in synchronized context
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTSession.java`
#### Snippet
```java

         if (state != null) {
            state.setAttached(false);
            state.setDisconnectedTime(System.currentTimeMillis());
            state.clearTopicAliases();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `state` accessed in synchronized context
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTSession.java`
#### Snippet
```java
         if (state != null) {
            state.setAttached(false);
            state.setDisconnectedTime(System.currentTimeMillis());
            state.clearTopicAliases();
         }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `state` accessed in synchronized context
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTSession.java`
#### Snippet
```java
            state.setAttached(false);
            state.setDisconnectedTime(System.currentTimeMillis());
            state.clearTopicAliases();
         }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `state` accessed in synchronized context
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTSession.java`
#### Snippet
```java

         if (getVersion() == MQTTVersion.MQTT_5) {
            if (state.getClientSessionExpiryInterval() == 0) {
               if (state.isWill() && failure) {
                  // If the session expires the will message must be sent no matter the will delay
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `state` accessed in synchronized context
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTSession.java`
#### Snippet
```java
         if (getVersion() == MQTTVersion.MQTT_5) {
            if (state.getClientSessionExpiryInterval() == 0) {
               if (state.isWill() && failure) {
                  // If the session expires the will message must be sent no matter the will delay
                  sendWillMessage();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `state` accessed in synchronized context
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTSession.java`
#### Snippet
```java
               protocolManager.removeSessionState(connection.getClientID());
            } else {
               state.setDisconnectedTime(System.currentTimeMillis());
            }
         } else {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `state` accessed in synchronized context
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTSession.java`
#### Snippet
```java
            }
         } else {
            if (state.isWill() && failure) {
               sendWillMessage();
            }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.replaceableRecords` accessed in synchronized context
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalImpl.java`
#### Snippet
```java
               for (final JournalFile file : dataFilesToProcess) {
                  try {
                     JournalImpl.readJournalFile(fileFactory, file, compactor, wholeFileBufferRef, false, this.replaceableRecords);
                  } catch (Throwable e) {
                     ActiveMQJournalLogger.LOGGER.compactReadError(file);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `calendarThreadLocal` accessed in synchronized context
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalImpl.java`
#### Snippet
```java
         long fileId = fileToCopy.getFileID();

         GregorianCalendar calendar = calendarThreadLocal.get();

         calendar.setTimeInMillis(System.currentTimeMillis());
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `ioExecutorFactory` accessed in synchronized context
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalImpl.java`
#### Snippet
```java
         }
         threadPool = null;
         ioExecutorFactory = null;
      }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.replaceableRecords` accessed in synchronized context
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalImpl.java`
#### Snippet
```java
            }

         }, wholeFileBufferRef, false, this.replaceableRecords);

         if (hasData.get()) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `ioExecutorFactory` accessed in synchronized context
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalImpl.java`
#### Snippet
```java

         threadPool = new ThreadPoolExecutor(0, Integer.MAX_VALUE, 60L,TimeUnit.SECONDS, new SynchronousQueue(), factory);
         ioExecutorFactory = new OrderedExecutorFactory(threadPool);
      } else {
         ioExecutorFactory = providedIOThreadPool;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `ioExecutorFactory` accessed in synchronized context
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalImpl.java`
#### Snippet
```java
         ioExecutorFactory = new OrderedExecutorFactory(threadPool);
      } else {
         ioExecutorFactory = providedIOThreadPool;
      }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `ioExecutorFactory` accessed in synchronized context
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalImpl.java`
#### Snippet
```java
      }

      filesExecutor = ioExecutorFactory.getExecutor();

      compactorExecutor = ioExecutorFactory.getExecutor();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `ioExecutorFactory` accessed in synchronized context
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalImpl.java`
#### Snippet
```java
      filesExecutor = ioExecutorFactory.getExecutor();

      compactorExecutor = ioExecutorFactory.getExecutor();

      appendExecutor = ioExecutorFactory.getExecutor();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `ioExecutorFactory` accessed in synchronized context
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalImpl.java`
#### Snippet
```java
      compactorExecutor = ioExecutorFactory.getExecutor();

      appendExecutor = ioExecutorFactory.getExecutor();

      filesRepository.setExecutor(filesExecutor);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `destroyed` accessed in synchronized context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/ActiveMQProtonRemotingConnection.java`
#### Snippet
```java
   public void destroy() {
      synchronized (this) {
         if (destroyed) {
            return;
         }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `destroyed` accessed in synchronized context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/ActiveMQProtonRemotingConnection.java`
#### Snippet
```java
         }

         destroyed = true;
      }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.address` accessed in synchronized context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPSessionCallback.java`
#### Snippet
```java

      public synchronized void setResult(SimpleString parameterAddress, T result) {
         this.address = parameterAddress;
         this.result = result;
      }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.result` accessed in synchronized context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPSessionCallback.java`
#### Snippet
```java
      public synchronized void setResult(SimpleString parameterAddress, T result) {
         this.address = parameterAddress;
         this.result = result;
      }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `address` accessed in synchronized context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPSessionCallback.java`
#### Snippet
```java

      public synchronized T getResult(SimpleString parameterAddress) {
         if (address != null && address.equals(parameterAddress)) {
            return result;
         } else {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `address` accessed in synchronized context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPSessionCallback.java`
#### Snippet
```java

      public synchronized T getResult(SimpleString parameterAddress) {
         if (address != null && address.equals(parameterAddress)) {
            return result;
         } else {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `result` accessed in synchronized context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPSessionCallback.java`
#### Snippet
```java
      public synchronized T getResult(SimpleString parameterAddress) {
         if (address != null && address.equals(parameterAddress)) {
            return result;
         } else {
            result = null;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `result` accessed in synchronized context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPSessionCallback.java`
#### Snippet
```java
            return result;
         } else {
            result = null;
            address = null;
            return null;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `address` accessed in synchronized context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPSessionCallback.java`
#### Snippet
```java
         } else {
            result = null;
            address = null;
            return null;
         }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `modified` accessed in synchronized context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPStandardMessage.java`
#### Snippet
```java
   @Override
   protected synchronized void ensureDataIsValid() {
      if (modified) {
         encodeMessage();
      }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.modified` accessed in synchronized context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPStandardMessage.java`
#### Snippet
```java
   @Override
   protected synchronized void encodeMessage() {
      this.modified = false;
      this.messageDataScanned = MessageDataScanningStatus.NOT_SCANNED.code;
      int estimated = Math.max(1500, data != null ? data.capacity() + 1000 : 0);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.messageDataScanned` accessed in synchronized context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPStandardMessage.java`
#### Snippet
```java
   protected synchronized void encodeMessage() {
      this.modified = false;
      this.messageDataScanned = MessageDataScanningStatus.NOT_SCANNED.code;
      int estimated = Math.max(1500, data != null ? data.capacity() + 1000 : 0);
      ByteBuf buffer = PooledByteBufAllocator.DEFAULT.directBuffer(estimated);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `data` accessed in synchronized context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPStandardMessage.java`
#### Snippet
```java
      this.modified = false;
      this.messageDataScanned = MessageDataScanningStatus.NOT_SCANNED.code;
      int estimated = Math.max(1500, data != null ? data.capacity() + 1000 : 0);
      ByteBuf buffer = PooledByteBufAllocator.DEFAULT.directBuffer(estimated);
      EncoderImpl encoder = TLSEncode.getEncoder();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `data` accessed in synchronized context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPStandardMessage.java`
#### Snippet
```java
      this.modified = false;
      this.messageDataScanned = MessageDataScanningStatus.NOT_SCANNED.code;
      int estimated = Math.max(1500, data != null ? data.capacity() + 1000 : 0);
      ByteBuf buffer = PooledByteBufAllocator.DEFAULT.directBuffer(estimated);
      EncoderImpl encoder = TLSEncode.getEncoder();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `header` accessed in synchronized context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPStandardMessage.java`
#### Snippet
```java

         encoder.setByteBuffer(writable);
         if (header != null) {
            encoder.writeObject(header);
         }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `header` accessed in synchronized context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPStandardMessage.java`
#### Snippet
```java
         encoder.setByteBuffer(writable);
         if (header != null) {
            encoder.writeObject(header);
         }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `messageAnnotations` accessed in synchronized context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPStandardMessage.java`
#### Snippet
```java
         // to happen.

         if (messageAnnotations != null) {
            encoder.writeObject(messageAnnotations);
         }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `messageAnnotations` accessed in synchronized context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPStandardMessage.java`
#### Snippet
```java

         if (messageAnnotations != null) {
            encoder.writeObject(messageAnnotations);
         }
         if (properties != null) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `properties` accessed in synchronized context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPStandardMessage.java`
#### Snippet
```java
            encoder.writeObject(messageAnnotations);
         }
         if (properties != null) {
            encoder.writeObject(properties);
         }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `properties` accessed in synchronized context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPStandardMessage.java`
#### Snippet
```java
         }
         if (properties != null) {
            encoder.writeObject(properties);
         }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `applicationProperties` accessed in synchronized context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPStandardMessage.java`
#### Snippet
```java
         // checking if application properties where loaded or added and
         // encoding only in that case.
         if (applicationProperties != null) {
            encoder.writeObject(applicationProperties);

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `applicationProperties` accessed in synchronized context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPStandardMessage.java`
#### Snippet
```java
         // encoding only in that case.
         if (applicationProperties != null) {
            encoder.writeObject(applicationProperties);

            // Now raw write the remainder body and footer if present.
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `data` accessed in synchronized context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPStandardMessage.java`
#### Snippet
```java

            // Now raw write the remainder body and footer if present.
            if (data != null && remainingBodyPosition != VALUE_NOT_PRESENT) {
               writable.put(data.position(remainingBodyPosition));
            }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `remainingBodyPosition` accessed in synchronized context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPStandardMessage.java`
#### Snippet
```java

            // Now raw write the remainder body and footer if present.
            if (data != null && remainingBodyPosition != VALUE_NOT_PRESENT) {
               writable.put(data.position(remainingBodyPosition));
            }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `data` accessed in synchronized context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPStandardMessage.java`
#### Snippet
```java
            // Now raw write the remainder body and footer if present.
            if (data != null && remainingBodyPosition != VALUE_NOT_PRESENT) {
               writable.put(data.position(remainingBodyPosition));
            }
         } else if (data != null && applicationPropertiesPosition != VALUE_NOT_PRESENT) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `remainingBodyPosition` accessed in synchronized context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPStandardMessage.java`
#### Snippet
```java
            // Now raw write the remainder body and footer if present.
            if (data != null && remainingBodyPosition != VALUE_NOT_PRESENT) {
               writable.put(data.position(remainingBodyPosition));
            }
         } else if (data != null && applicationPropertiesPosition != VALUE_NOT_PRESENT) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `data` accessed in synchronized context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPStandardMessage.java`
#### Snippet
```java
               writable.put(data.position(remainingBodyPosition));
            }
         } else if (data != null && applicationPropertiesPosition != VALUE_NOT_PRESENT) {
            // Writes out ApplicationProperties, Body and Footer in one go if present.
            writable.put(data.position(applicationPropertiesPosition));
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `applicationPropertiesPosition` accessed in synchronized context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPStandardMessage.java`
#### Snippet
```java
               writable.put(data.position(remainingBodyPosition));
            }
         } else if (data != null && applicationPropertiesPosition != VALUE_NOT_PRESENT) {
            // Writes out ApplicationProperties, Body and Footer in one go if present.
            writable.put(data.position(applicationPropertiesPosition));
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `data` accessed in synchronized context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPStandardMessage.java`
#### Snippet
```java
         } else if (data != null && applicationPropertiesPosition != VALUE_NOT_PRESENT) {
            // Writes out ApplicationProperties, Body and Footer in one go if present.
            writable.put(data.position(applicationPropertiesPosition));
         } else if (data != null && remainingBodyPosition != VALUE_NOT_PRESENT) {
            // No Application properties at all so raw write Body and Footer sections
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `applicationPropertiesPosition` accessed in synchronized context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPStandardMessage.java`
#### Snippet
```java
         } else if (data != null && applicationPropertiesPosition != VALUE_NOT_PRESENT) {
            // Writes out ApplicationProperties, Body and Footer in one go if present.
            writable.put(data.position(applicationPropertiesPosition));
         } else if (data != null && remainingBodyPosition != VALUE_NOT_PRESENT) {
            // No Application properties at all so raw write Body and Footer sections
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `data` accessed in synchronized context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPStandardMessage.java`
#### Snippet
```java
            // Writes out ApplicationProperties, Body and Footer in one go if present.
            writable.put(data.position(applicationPropertiesPosition));
         } else if (data != null && remainingBodyPosition != VALUE_NOT_PRESENT) {
            // No Application properties at all so raw write Body and Footer sections
            writable.put(data.position(remainingBodyPosition));
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `remainingBodyPosition` accessed in synchronized context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPStandardMessage.java`
#### Snippet
```java
            // Writes out ApplicationProperties, Body and Footer in one go if present.
            writable.put(data.position(applicationPropertiesPosition));
         } else if (data != null && remainingBodyPosition != VALUE_NOT_PRESENT) {
            // No Application properties at all so raw write Body and Footer sections
            writable.put(data.position(remainingBodyPosition));
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `data` accessed in synchronized context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPStandardMessage.java`
#### Snippet
```java
         } else if (data != null && remainingBodyPosition != VALUE_NOT_PRESENT) {
            // No Application properties at all so raw write Body and Footer sections
            writable.put(data.position(remainingBodyPosition));
         }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `remainingBodyPosition` accessed in synchronized context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPStandardMessage.java`
#### Snippet
```java
         } else if (data != null && remainingBodyPosition != VALUE_NOT_PRESENT) {
            // No Application properties at all so raw write Body and Footer sections
            writable.put(data.position(remainingBodyPosition));
         }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `data` accessed in synchronized context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPStandardMessage.java`
#### Snippet
```java

         buffer.readBytes(bytes);
         data = ReadableBuffer.ByteBufferReader.wrap(ByteBuffer.wrap(bytes));
      } finally {
         encoder.setByteBuffer((WritableBuffer) null);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `controlInUse` accessed in synchronized context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/connect/mirror/MirrorTransaction.java`
#### Snippet
```java
   protected synchronized void afterCommit(List<TransactionOperation> operationsToComplete) {
      MirrorController beforeController = AMQPMirrorControllerTarget.getControllerInUse();
      AMQPMirrorControllerTarget.setControllerInUse(controlInUse);
      try {
         super.afterCommit(operationsToComplete);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `rejectedConsumers` accessed in synchronized context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
   @Override
   public final synchronized boolean acceptsConsumer(long consumer) {
      if (rejectedConsumers == null) {
         return true;
      } else {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `rejectedConsumers` accessed in synchronized context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
         return true;
      } else {
         return !rejectedConsumers.contains(consumer);
      }
   }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `header` accessed in synchronized context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java

   protected synchronized void resetMessageData() {
      header = null;
      messageAnnotations = null;
      properties = null;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `messageAnnotations` accessed in synchronized context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
   protected synchronized void resetMessageData() {
      header = null;
      messageAnnotations = null;
      properties = null;
      applicationProperties = null;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `properties` accessed in synchronized context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
      header = null;
      messageAnnotations = null;
      properties = null;
      applicationProperties = null;
      if (!expirationReload) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `applicationProperties` accessed in synchronized context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
      messageAnnotations = null;
      properties = null;
      applicationProperties = null;
      if (!expirationReload) {
         expiration = 0;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `expirationReload` accessed in synchronized context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
      properties = null;
      applicationProperties = null;
      if (!expirationReload) {
         expiration = 0;
      }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `expiration` accessed in synchronized context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
      applicationProperties = null;
      if (!expirationReload) {
         expiration = 0;
      }
      encodedHeaderSize = 0;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `encodedHeaderSize` accessed in synchronized context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
         expiration = 0;
      }
      encodedHeaderSize = 0;
      memoryEstimate = -1;
      scheduledTime = -1;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `memoryEstimate` accessed in synchronized context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
      }
      encodedHeaderSize = 0;
      memoryEstimate = -1;
      scheduledTime = -1;
      encodedDeliveryAnnotationsSize = 0;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `scheduledTime` accessed in synchronized context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
      encodedHeaderSize = 0;
      memoryEstimate = -1;
      scheduledTime = -1;
      encodedDeliveryAnnotationsSize = 0;
      headerPosition = VALUE_NOT_PRESENT;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `encodedDeliveryAnnotationsSize` accessed in synchronized context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
      memoryEstimate = -1;
      scheduledTime = -1;
      encodedDeliveryAnnotationsSize = 0;
      headerPosition = VALUE_NOT_PRESENT;
      deliveryAnnotationsPosition = VALUE_NOT_PRESENT;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `headerPosition` accessed in synchronized context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
      scheduledTime = -1;
      encodedDeliveryAnnotationsSize = 0;
      headerPosition = VALUE_NOT_PRESENT;
      deliveryAnnotationsPosition = VALUE_NOT_PRESENT;
      propertiesPosition = VALUE_NOT_PRESENT;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `deliveryAnnotationsPosition` accessed in synchronized context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
      encodedDeliveryAnnotationsSize = 0;
      headerPosition = VALUE_NOT_PRESENT;
      deliveryAnnotationsPosition = VALUE_NOT_PRESENT;
      propertiesPosition = VALUE_NOT_PRESENT;
      applicationPropertiesPosition = VALUE_NOT_PRESENT;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `propertiesPosition` accessed in synchronized context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
      headerPosition = VALUE_NOT_PRESENT;
      deliveryAnnotationsPosition = VALUE_NOT_PRESENT;
      propertiesPosition = VALUE_NOT_PRESENT;
      applicationPropertiesPosition = VALUE_NOT_PRESENT;
      remainingBodyPosition = VALUE_NOT_PRESENT;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `applicationPropertiesPosition` accessed in synchronized context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
      deliveryAnnotationsPosition = VALUE_NOT_PRESENT;
      propertiesPosition = VALUE_NOT_PRESENT;
      applicationPropertiesPosition = VALUE_NOT_PRESENT;
      remainingBodyPosition = VALUE_NOT_PRESENT;
   }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `remainingBodyPosition` accessed in synchronized context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
      propertiesPosition = VALUE_NOT_PRESENT;
      applicationPropertiesPosition = VALUE_NOT_PRESENT;
      remainingBodyPosition = VALUE_NOT_PRESENT;
   }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `rejectedConsumers` accessed in synchronized context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
   @Override
   public final synchronized void rejectConsumer(long consumer) {
      if (rejectedConsumers == null) {
         rejectedConsumers = new HashSet<>();
      }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `rejectedConsumers` accessed in synchronized context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
   public final synchronized void rejectConsumer(long consumer) {
      if (rejectedConsumers == null) {
         rejectedConsumers = new HashSet<>();
      }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `rejectedConsumers` accessed in synchronized context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
      }

      rejectedConsumers.add(consumer);
   }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `header` accessed in synchronized context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
            TypeConstructor<?> constructor = decoder.readConstructor();
            if (Header.class.equals(constructor.getTypeClass())) {
               header = (Header) constructor.readValue();
               headerPosition = constructorPos;
               encodedHeaderSize = data.position();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `headerPosition` accessed in synchronized context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
            if (Header.class.equals(constructor.getTypeClass())) {
               header = (Header) constructor.readValue();
               headerPosition = constructorPos;
               encodedHeaderSize = data.position();
               if (header.getTtl() != null) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `encodedHeaderSize` accessed in synchronized context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
               header = (Header) constructor.readValue();
               headerPosition = constructorPos;
               encodedHeaderSize = data.position();
               if (header.getTtl() != null) {
                  if (!expirationReload) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `header` accessed in synchronized context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
               headerPosition = constructorPos;
               encodedHeaderSize = data.position();
               if (header.getTtl() != null) {
                  if (!expirationReload) {
                     expiration = System.currentTimeMillis() + header.getTtl().intValue();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `expirationReload` accessed in synchronized context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
               encodedHeaderSize = data.position();
               if (header.getTtl() != null) {
                  if (!expirationReload) {
                     expiration = System.currentTimeMillis() + header.getTtl().intValue();
                  }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `expiration` accessed in synchronized context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
               if (header.getTtl() != null) {
                  if (!expirationReload) {
                     expiration = System.currentTimeMillis() + header.getTtl().intValue();
                  }
               }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `header` accessed in synchronized context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
               if (header.getTtl() != null) {
                  if (!expirationReload) {
                     expiration = System.currentTimeMillis() + header.getTtl().intValue();
                  }
               }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `deliveryAnnotationsPosition` accessed in synchronized context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
               }
            } else if (DeliveryAnnotations.class.equals(constructor.getTypeClass())) {
               deliveryAnnotationsPosition = constructorPos;
               this.deliveryAnnotations = (DeliveryAnnotations) constructor.readValue();
               encodedDeliveryAnnotationsSize = data.position() - constructorPos;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.deliveryAnnotations` accessed in synchronized context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
            } else if (DeliveryAnnotations.class.equals(constructor.getTypeClass())) {
               deliveryAnnotationsPosition = constructorPos;
               this.deliveryAnnotations = (DeliveryAnnotations) constructor.readValue();
               encodedDeliveryAnnotationsSize = data.position() - constructorPos;
            } else if (MessageAnnotations.class.equals(constructor.getTypeClass())) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `encodedDeliveryAnnotationsSize` accessed in synchronized context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
               deliveryAnnotationsPosition = constructorPos;
               this.deliveryAnnotations = (DeliveryAnnotations) constructor.readValue();
               encodedDeliveryAnnotationsSize = data.position() - constructorPos;
            } else if (MessageAnnotations.class.equals(constructor.getTypeClass())) {
               messageAnnotationsPosition = constructorPos;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `messageAnnotationsPosition` accessed in synchronized context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
               encodedDeliveryAnnotationsSize = data.position() - constructorPos;
            } else if (MessageAnnotations.class.equals(constructor.getTypeClass())) {
               messageAnnotationsPosition = constructorPos;
               messageAnnotations = (MessageAnnotations) constructor.readValue();
            } else if (Properties.class.equals(constructor.getTypeClass())) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `messageAnnotations` accessed in synchronized context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
            } else if (MessageAnnotations.class.equals(constructor.getTypeClass())) {
               messageAnnotationsPosition = constructorPos;
               messageAnnotations = (MessageAnnotations) constructor.readValue();
            } else if (Properties.class.equals(constructor.getTypeClass())) {
               propertiesPosition = constructorPos;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `propertiesPosition` accessed in synchronized context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
               messageAnnotations = (MessageAnnotations) constructor.readValue();
            } else if (Properties.class.equals(constructor.getTypeClass())) {
               propertiesPosition = constructorPos;
               properties = (Properties) constructor.readValue();

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `properties` accessed in synchronized context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
            } else if (Properties.class.equals(constructor.getTypeClass())) {
               propertiesPosition = constructorPos;
               properties = (Properties) constructor.readValue();

               if (properties.getAbsoluteExpiryTime() != null && properties.getAbsoluteExpiryTime().getTime() > 0) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `properties` accessed in synchronized context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
               properties = (Properties) constructor.readValue();

               if (properties.getAbsoluteExpiryTime() != null && properties.getAbsoluteExpiryTime().getTime() > 0) {
                  if (!expirationReload) {
                     expiration = properties.getAbsoluteExpiryTime().getTime();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `properties` accessed in synchronized context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
               properties = (Properties) constructor.readValue();

               if (properties.getAbsoluteExpiryTime() != null && properties.getAbsoluteExpiryTime().getTime() > 0) {
                  if (!expirationReload) {
                     expiration = properties.getAbsoluteExpiryTime().getTime();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `expirationReload` accessed in synchronized context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java

               if (properties.getAbsoluteExpiryTime() != null && properties.getAbsoluteExpiryTime().getTime() > 0) {
                  if (!expirationReload) {
                     expiration = properties.getAbsoluteExpiryTime().getTime();
                  }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `expiration` accessed in synchronized context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
               if (properties.getAbsoluteExpiryTime() != null && properties.getAbsoluteExpiryTime().getTime() > 0) {
                  if (!expirationReload) {
                     expiration = properties.getAbsoluteExpiryTime().getTime();
                  }
               }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `properties` accessed in synchronized context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
               if (properties.getAbsoluteExpiryTime() != null && properties.getAbsoluteExpiryTime().getTime() > 0) {
                  if (!expirationReload) {
                     expiration = properties.getAbsoluteExpiryTime().getTime();
                  }
               }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `applicationPropertiesPosition` accessed in synchronized context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
               // Lazy decoding will start at the TypeConstructor of these ApplicationProperties
               // but we scan past it to grab the location of the possible body and footer section.
               applicationPropertiesPosition = constructorPos;
               constructor.skipValue();
               remainingBodyPosition = data.hasRemaining() ? data.position() : VALUE_NOT_PRESENT;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `remainingBodyPosition` accessed in synchronized context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
               applicationPropertiesPosition = constructorPos;
               constructor.skipValue();
               remainingBodyPosition = data.hasRemaining() ? data.position() : VALUE_NOT_PRESENT;
               break;
            } else {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `remainingBodyPosition` accessed in synchronized context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
               // This will be either the body or a Footer section which will be treated as an immutable
               // and be copied as is when re-encoding the message.
               remainingBodyPosition = constructorPos;
               break;
            }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.messageDataScanned` accessed in synchronized context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
         data.rewind();
      }
      this.messageDataScanned = MessageDataScanningStatus.SCANNED.code;
   }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `messageDataScanned` accessed in synchronized context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java

   protected synchronized void lazyScanAfterReloadPersistence() {
      assert messageDataScanned == MessageDataScanningStatus.RELOAD_PERSISTENCE.code;
      scanMessageData();
      messageDataScanned = MessageDataScanningStatus.SCANNED.code;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `messageDataScanned` accessed in synchronized context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
      assert messageDataScanned == MessageDataScanningStatus.RELOAD_PERSISTENCE.code;
      scanMessageData();
      messageDataScanned = MessageDataScanningStatus.SCANNED.code;
      modified = false;
      // reinitialise memory estimate as message will already be on a queue
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `modified` accessed in synchronized context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
      scanMessageData();
      messageDataScanned = MessageDataScanningStatus.SCANNED.code;
      modified = false;
      // reinitialise memory estimate as message will already be on a queue
      // and lazy decode will want to update
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `lruListID` accessed in synchronized context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/connect/mirror/ReferenceNodeStore.java`
#### Snippet
```java
      }

      if (lruListID != null && lruListID.equals(serverID)) {
         return lruMap;
      }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `lruListID` accessed in synchronized context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/connect/mirror/ReferenceNodeStore.java`
#### Snippet
```java
      }

      if (lruListID != null && lruListID.equals(serverID)) {
         return lruMap;
      }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `lruMap` accessed in synchronized context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/connect/mirror/ReferenceNodeStore.java`
#### Snippet
```java

      if (lruListID != null && lruListID.equals(serverID)) {
         return lruMap;
      }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `lists` accessed in synchronized context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/connect/mirror/ReferenceNodeStore.java`
#### Snippet
```java
      }

      if (lists == null) {
         lists = new HashMap<>();
      }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `lists` accessed in synchronized context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/connect/mirror/ReferenceNodeStore.java`
#### Snippet
```java

      if (lists == null) {
         lists = new HashMap<>();
      }
      LongObjectHashMap<LinkedListImpl.Node<MessageReference>> theList = lists.get(serverID);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `lists` accessed in synchronized context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/connect/mirror/ReferenceNodeStore.java`
#### Snippet
```java
         lists = new HashMap<>();
      }
      LongObjectHashMap<LinkedListImpl.Node<MessageReference>> theList = lists.get(serverID);
      if (theList == null) {
         theList = new LongObjectHashMap<>();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `lists` accessed in synchronized context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/connect/mirror/ReferenceNodeStore.java`
#### Snippet
```java
      if (theList == null) {
         theList = new LongObjectHashMap<>();
         lists.put(serverID, theList);
      }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `lruMap` accessed in synchronized context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/connect/mirror/ReferenceNodeStore.java`
#### Snippet
```java
      }

      lruMap = theList; // cached result
      lruListID = serverID;

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `lruListID` accessed in synchronized context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/connect/mirror/ReferenceNodeStore.java`
#### Snippet
```java

      lruMap = theList; // cached result
      lruListID = serverID;

      return theList;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `lists` accessed in synchronized context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/connect/mirror/ReferenceNodeStore.java`
#### Snippet
```java
   @Override
   public synchronized void clear() {
      lists.forEach((k, v) -> v.clear());
      lists.clear();
      lruListID = null;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `lists` accessed in synchronized context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/connect/mirror/ReferenceNodeStore.java`
#### Snippet
```java
   public synchronized void clear() {
      lists.forEach((k, v) -> v.clear());
      lists.clear();
      lruListID = null;
      lruMap = null;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `lruListID` accessed in synchronized context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/connect/mirror/ReferenceNodeStore.java`
#### Snippet
```java
      lists.forEach((k, v) -> v.clear());
      lists.clear();
      lruListID = null;
      lruMap = null;
   }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `lruMap` accessed in synchronized context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/connect/mirror/ReferenceNodeStore.java`
#### Snippet
```java
      lists.clear();
      lruListID = null;
      lruMap = null;
   }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `refCount` accessed in synchronized context
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/jgroups/JChannelWrapper.java`
#### Snippet
```java

   public synchronized void close(boolean closeWrappedChannel) {
      refCount--;
      if (logger.isTraceEnabled())
         logger.trace("{}::RefCount-- {} on channel {}", this, refCount, channelName, new Exception("Trace"));
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `refCount` accessed in synchronized context
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/jgroups/JChannelWrapper.java`
#### Snippet
```java
      refCount--;
      if (logger.isTraceEnabled())
         logger.trace("{}::RefCount-- {} on channel {}", this, refCount, channelName, new Exception("Trace"));
      if (refCount == 0) {
         if (closeWrappedChannel) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `refCount` accessed in synchronized context
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/jgroups/JChannelWrapper.java`
#### Snippet
```java
      if (logger.isTraceEnabled())
         logger.trace("{}::RefCount-- {} on channel {}", this, refCount, channelName, new Exception("Trace"));
      if (refCount == 0) {
         if (closeWrappedChannel) {
            closeChannel();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `connectionProvider` accessed in synchronized context
in `artemis-jdbc-store/src/main/java/org/apache/activemq/artemis/jdbc/store/journal/JDBCJournalImpl.java`
#### Snippet
```java
      JDBCJournalRecord r;

      try (Connection connection = connectionProvider.getConnection();
           PreparedStatement selectJournalRecords = connection.prepareStatement(this.selectJournalRecords)) {
         try (ResultSet rs = selectJournalRecords.executeQuery()) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `connectionProvider` accessed in synchronized context
in `artemis-jdbc-store/src/main/java/org/apache/activemq/artemis/jdbc/store/journal/JDBCJournalImpl.java`
#### Snippet
```java
      TransactionHolder holder;

      try (Connection connection = connectionProvider.getConnection();
           PreparedStatement deleteJournalRecords = connection.prepareStatement(this.deleteJournalRecords);
           PreparedStatement deleteJournalTxRecords = connection.prepareStatement(this.deleteJournalTxRecords);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `serverRespondedWithFail` accessed in synchronized context
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ClientProducerCreditsImpl.java`
#### Snippet
```java
         super.afterAcquired(credits);

         if (serverRespondedWithFail) {
            serverRespondedWithFail = false;

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `serverRespondedWithFail` accessed in synchronized context
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ClientProducerCreditsImpl.java`
#### Snippet
```java

         if (serverRespondedWithFail) {
            serverRespondedWithFail = false;

            // remove existing credits to force the client to ask the server for more on the next send
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `pendingCredits` accessed in synchronized context
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ClientProducerCreditsImpl.java`
#### Snippet
```java
            // remove existing credits to force the client to ask the server for more on the next send
            semaphore.drainPermits();
            pendingCredits = 0;
            arriving = 0;

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `arriving` accessed in synchronized context
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ClientProducerCreditsImpl.java`
#### Snippet
```java
            semaphore.drainPermits();
            pendingCredits = 0;
            arriving = 0;

            throw ActiveMQClientMessageBundle.BUNDLE.addressIsFull(address.toString(), credits);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `balance` accessed in synchronized context
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/AsynchronousProducerCreditsImpl.java`
#### Snippet
```java
      synchronized (this) {
         super.receiveCredits(credits);
         balance += credits;
         if (logger.isDebugEnabled()) {
            logger.debug("receiveCredits with credits={}, balance={}, arriving={}, callbackType={}", credits, balance, arriving, callback.getClass());
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `balance` accessed in synchronized context
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/AsynchronousProducerCreditsImpl.java`
#### Snippet
```java
         balance += credits;
         if (logger.isDebugEnabled()) {
            logger.debug("receiveCredits with credits={}, balance={}, arriving={}, callbackType={}", credits, balance, arriving, callback.getClass());
         }
         callback.onCreditsFlow(balance <= 0, this);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `arriving` accessed in synchronized context
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/AsynchronousProducerCreditsImpl.java`
#### Snippet
```java
         balance += credits;
         if (logger.isDebugEnabled()) {
            logger.debug("receiveCredits with credits={}, balance={}, arriving={}, callbackType={}", credits, balance, arriving, callback.getClass());
         }
         callback.onCreditsFlow(balance <= 0, this);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `balance` accessed in synchronized context
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/AsynchronousProducerCreditsImpl.java`
#### Snippet
```java
            logger.debug("receiveCredits with credits={}, balance={}, arriving={}, callbackType={}", credits, balance, arriving, callback.getClass());
         }
         callback.onCreditsFlow(balance <= 0, this);

         if (balance < 0 && arriving == 0) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `balance` accessed in synchronized context
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/AsynchronousProducerCreditsImpl.java`
#### Snippet
```java
         callback.onCreditsFlow(balance <= 0, this);

         if (balance < 0 && arriving == 0) {
            // there are no more credits arriving and we are still negative, async large message send asked too much and we need to counter balance
            logger.debug("Starve credits counter balance");
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `arriving` accessed in synchronized context
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/AsynchronousProducerCreditsImpl.java`
#### Snippet
```java
         callback.onCreditsFlow(balance <= 0, this);

         if (balance < 0 && arriving == 0) {
            // there are no more credits arriving and we are still negative, async large message send asked too much and we need to counter balance
            logger.debug("Starve credits counter balance");
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `balance` accessed in synchronized context
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/AsynchronousProducerCreditsImpl.java`
#### Snippet
```java
            // there are no more credits arriving and we are still negative, async large message send asked too much and we need to counter balance
            logger.debug("Starve credits counter balance");
            int request = -balance + windowSize * 2;
            requestCredits(request);
         }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `balance` accessed in synchronized context
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/AsynchronousProducerCreditsImpl.java`
#### Snippet
```java
   public void releaseOutstanding() {
      synchronized (this) {
         balance = 0;
         callback.onCreditsFlow(true, this);
         if (logger.isDebugEnabled()) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `balance` accessed in synchronized context
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/AsynchronousProducerCreditsImpl.java`
#### Snippet
```java
         callback.onCreditsFlow(true, this);
         if (logger.isDebugEnabled()) {
            logger.debug("releaseOutstanding credits, balance={}, callback={}", balance, callback.getClass());
         }
      }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `balance` accessed in synchronized context
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/AsynchronousProducerCreditsImpl.java`
#### Snippet
```java
   protected synchronized void actualAcquire(int credits) {
      synchronized (this) {
         balance -= credits;
         if (logger.isDebugEnabled()) {
            logger.debug("actualAcquire on address {} with credits={}, balance={}, callbackType={}", address, credits, balance, callback.getClass());
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `balance` accessed in synchronized context
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/AsynchronousProducerCreditsImpl.java`
#### Snippet
```java
         balance -= credits;
         if (logger.isDebugEnabled()) {
            logger.debug("actualAcquire on address {} with credits={}, balance={}, callbackType={}", address, credits, balance, callback.getClass());
         }
         if (balance <= 0) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `balance` accessed in synchronized context
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/AsynchronousProducerCreditsImpl.java`
#### Snippet
```java
            logger.debug("actualAcquire on address {} with credits={}, balance={}, callbackType={}", address, credits, balance, callback.getClass());
         }
         if (balance <= 0) {
            callback.onCreditsFlow(true, this);
         }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `arriving` accessed in synchronized context
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/AbstractProducerCreditsImpl.java`
#### Snippet
```java
   public void receiveCredits(final int credits) {
      synchronized (this) {
         arriving -= credits;
      }
   }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `arriving` accessed in synchronized context
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/AbstractProducerCreditsImpl.java`
#### Snippet
```java

      synchronized (this) {
         if (getBalance() + arriving < needed) {
            toRequest = needed - arriving;

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `arriving` accessed in synchronized context
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/AbstractProducerCreditsImpl.java`
#### Snippet
```java
      synchronized (this) {
         if (getBalance() + arriving < needed) {
            toRequest = needed - arriving;

            if (logger.isTraceEnabled()) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `arriving` accessed in synchronized context
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/AbstractProducerCreditsImpl.java`
#### Snippet
```java

            if (logger.isTraceEnabled()) {
               logger.trace("CheckCredits on Address {}, requesting={}, arriving={}, balance={}", address, toRequest, arriving, getBalance());
            }
         } else {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `arriving` accessed in synchronized context
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/AbstractProducerCreditsImpl.java`
#### Snippet
```java
         } else {
            if (logger.isTraceEnabled()) {
               logger.trace("CheckCredits did not need it, balance={}, arriving={},  needed={}, getbalance + arriving < needed={}", getBalance(), arriving, needed, (boolean)(getBalance() + arriving < needed));
            }
         }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `arriving` accessed in synchronized context
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/AbstractProducerCreditsImpl.java`
#### Snippet
```java
         } else {
            if (logger.isTraceEnabled()) {
               logger.trace("CheckCredits did not need it, balance={}, arriving={},  needed={}, getbalance + arriving < needed={}", getBalance(), arriving, needed, (boolean)(getBalance() + arriving < needed));
            }
         }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `pendingCredits` accessed in synchronized context
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/AbstractProducerCreditsImpl.java`
#### Snippet
```java
      // Any pendingCredits credits from before failover won't arrive, so we re-initialise

      int beforeFailure = pendingCredits;

      pendingCredits = 0;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `pendingCredits` accessed in synchronized context
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/AbstractProducerCreditsImpl.java`
#### Snippet
```java
      int beforeFailure = pendingCredits;

      pendingCredits = 0;
      arriving = 0;

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `arriving` accessed in synchronized context
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/AbstractProducerCreditsImpl.java`
#### Snippet
```java

      pendingCredits = 0;
      arriving = 0;

      // If we are waiting for more credits than what's configured, then we need to use what we tried before
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `pendingCredits` accessed in synchronized context
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/AbstractProducerCreditsImpl.java`
#### Snippet
```java

      synchronized (this) {
         pendingCredits -= credits;
      }
   }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `pendingCredits` accessed in synchronized context
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/AbstractProducerCreditsImpl.java`
#### Snippet
```java
      logger.debug("Request {} credits on address {}", credits, address);
      synchronized (this) {
         pendingCredits += credits;
         arriving += credits;
      }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `arriving` accessed in synchronized context
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/AbstractProducerCreditsImpl.java`
#### Snippet
```java
      synchronized (this) {
         pendingCredits += credits;
         arriving += credits;
      }
      session.sendProducerCreditsMessage(credits, address);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.buffer` accessed in synchronized context
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/message/impl/CoreMessage.java`
#### Snippet
```java
      }
      synchronized (this) {
         final ByteBuf buffer = this.buffer;
         // acquiring the lock here, although heavy-weight, is the safer way to do this,
         // because we cannot trust that a racing thread won't modify buffer
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.endOfBodyPosition` accessed in synchronized context
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/message/impl/CoreMessage.java`
#### Snippet
```java
      // many subscriptions and bridging to other nodes in a cluster
      synchronized (other) {
         this.endOfBodyPosition = other.endOfBodyPosition;
         internalSetMessageID(other.messageID);
         this.address = other.address;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.address` accessed in synchronized context
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/message/impl/CoreMessage.java`
#### Snippet
```java
         this.endOfBodyPosition = other.endOfBodyPosition;
         internalSetMessageID(other.messageID);
         this.address = other.address;
         this.type = other.type;
         this.durable = other.durable;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.type` accessed in synchronized context
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/message/impl/CoreMessage.java`
#### Snippet
```java
         internalSetMessageID(other.messageID);
         this.address = other.address;
         this.type = other.type;
         this.durable = other.durable;
         this.expiration = other.expiration;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.durable` accessed in synchronized context
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/message/impl/CoreMessage.java`
#### Snippet
```java
         this.address = other.address;
         this.type = other.type;
         this.durable = other.durable;
         this.expiration = other.expiration;
         this.timestamp = other.timestamp;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.timestamp` accessed in synchronized context
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/message/impl/CoreMessage.java`
#### Snippet
```java
         this.durable = other.durable;
         this.expiration = other.expiration;
         this.timestamp = other.timestamp;
         this.priority = other.priority;
         this.userID = other.userID;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.priority` accessed in synchronized context
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/message/impl/CoreMessage.java`
#### Snippet
```java
         this.expiration = other.expiration;
         this.timestamp = other.timestamp;
         this.priority = other.priority;
         this.userID = other.userID;
         this.coreMessageObjectPools = other.coreMessageObjectPools;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.properties` accessed in synchronized context
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/message/impl/CoreMessage.java`
#### Snippet
```java
         this.coreMessageObjectPools = other.coreMessageObjectPools;
         if (copyProperties != null) {
            this.properties = new TypedProperties(copyProperties);
         }
         if (other.buffer != null) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.buffer` accessed in synchronized context
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/message/impl/CoreMessage.java`
#### Snippet
```java
         }
         if (other.buffer != null) {
            this.buffer = other.buffer.copy();
         }
      }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `buffer` accessed in synchronized context
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/message/impl/CoreMessage.java`
#### Snippet
```java
   public synchronized void sendBuffer(ByteBuf sendBuffer, int deliveryCount) {
      checkEncode();
      sendBuffer.writeBytes(buffer, 0, buffer.writerIndex());
   }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `buffer` accessed in synchronized context
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/message/impl/CoreMessage.java`
#### Snippet
```java
   public synchronized void sendBuffer(ByteBuf sendBuffer, int deliveryCount) {
      checkEncode();
      sendBuffer.writeBytes(buffer, 0, buffer.writerIndex());
   }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.properties` accessed in synchronized context
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/message/impl/CoreMessage.java`
#### Snippet
```java
   private synchronized TypedProperties getOrInitializeTypedProperties() {
      try {
         TypedProperties properties = this.properties;
         if (properties == null) {
            properties = new TypedProperties(INTERNAL_PROPERTY_NAMES_PREDICATE);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `buffer` accessed in synchronized context
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/message/impl/CoreMessage.java`
#### Snippet
```java
         if (properties == null) {
            properties = new TypedProperties(INTERNAL_PROPERTY_NAMES_PREDICATE);
            if (buffer != null && propertiesLocation >= 0) {
               final ByteBuf byteBuf = buffer.duplicate().readerIndex(propertiesLocation);
               properties.decode(byteBuf, coreMessageObjectPools == null ? null : coreMessageObjectPools.getPropertiesDecoderPools());
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `buffer` accessed in synchronized context
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/message/impl/CoreMessage.java`
#### Snippet
```java
            properties = new TypedProperties(INTERNAL_PROPERTY_NAMES_PREDICATE);
            if (buffer != null && propertiesLocation >= 0) {
               final ByteBuf byteBuf = buffer.duplicate().readerIndex(propertiesLocation);
               properties.decode(byteBuf, coreMessageObjectPools == null ? null : coreMessageObjectPools.getPropertiesDecoderPools());
            }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.properties` accessed in synchronized context
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/message/impl/CoreMessage.java`
#### Snippet
```java
               properties.decode(byteBuf, coreMessageObjectPools == null ? null : coreMessageObjectPools.getPropertiesDecoderPools());
            }
            this.properties = properties;
         }
         return properties;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `buffer` accessed in synchronized context
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/message/impl/CoreMessage.java`
#### Snippet
```java
   public synchronized void sendBuffer_1X(ByteBuf sendBuffer) {
      checkEncode();
      ByteBuf tmpBuffer = buffer.duplicate();
      sendBuffer.writeInt(endOfBodyPosition + DataConstants.SIZE_INT);
      tmpBuffer.readerIndex(DataConstants.SIZE_INT);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `endOfBodyPosition` accessed in synchronized context
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/message/impl/CoreMessage.java`
#### Snippet
```java
      checkEncode();
      ByteBuf tmpBuffer = buffer.duplicate();
      sendBuffer.writeInt(endOfBodyPosition + DataConstants.SIZE_INT);
      tmpBuffer.readerIndex(DataConstants.SIZE_INT);
      tmpBuffer.readBytes(sendBuffer, endOfBodyPosition - BUFFER_HEADER_SPACE);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `endOfBodyPosition` accessed in synchronized context
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/message/impl/CoreMessage.java`
#### Snippet
```java
      sendBuffer.writeInt(endOfBodyPosition + DataConstants.SIZE_INT);
      tmpBuffer.readerIndex(DataConstants.SIZE_INT);
      tmpBuffer.readBytes(sendBuffer, endOfBodyPosition - BUFFER_HEADER_SPACE);
      sendBuffer.writeInt(tmpBuffer.writerIndex() + DataConstants.SIZE_INT + BUFFER_HEADER_SPACE);
      tmpBuffer.readBytes(sendBuffer, tmpBuffer.readableBytes());
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `messageID` accessed in synchronized context
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/message/impl/CoreMessage.java`
#### Snippet
```java
         final int totalEncodedSize = headersSize + propertiesEncodeSize;
         ensureExactWritable(buffer, totalEncodedSize);
         buffer.writeLong(messageID);
         SimpleString.writeNullableSimpleString(buffer, address);
         if (userID == null) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `type` accessed in synchronized context
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/message/impl/CoreMessage.java`
#### Snippet
```java
            buffer.writeBytes(userID.asBytes());
         }
         buffer.writeByte(type);
         buffer.writeBoolean(durable);
         buffer.writeLong(expiration);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `durable` accessed in synchronized context
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/message/impl/CoreMessage.java`
#### Snippet
```java
         }
         buffer.writeByte(type);
         buffer.writeBoolean(durable);
         buffer.writeLong(expiration);
         buffer.writeLong(timestamp);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `timestamp` accessed in synchronized context
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/message/impl/CoreMessage.java`
#### Snippet
```java
         buffer.writeBoolean(durable);
         buffer.writeLong(expiration);
         buffer.writeLong(timestamp);
         buffer.writeByte(priority);
         assert buffer.writerIndex() == initialWriterIndex + headersSize : "Bad Headers encode size estimation";
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `priority` accessed in synchronized context
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/message/impl/CoreMessage.java`
#### Snippet
```java
         buffer.writeLong(expiration);
         buffer.writeLong(timestamp);
         buffer.writeByte(priority);
         assert buffer.writerIndex() == initialWriterIndex + headersSize : "Bad Headers encode size estimation";
         final int realPropertiesEncodeSize = properties.encode(buffer);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `writableBuffer` accessed in synchronized context
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/message/impl/CoreMessage.java`
#### Snippet
```java
      getProperties();

      if (writableBuffer != null) {
         // The message encode takes into consideration the PacketImpl which is not part of this encoding
         // so we always need to take the BUFFER_HEADER_SPACE from packet impl into consideration
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `endOfBodyPosition` accessed in synchronized context
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/message/impl/CoreMessage.java`
#### Snippet
```java
         // The message encode takes into consideration the PacketImpl which is not part of this encoding
         // so we always need to take the BUFFER_HEADER_SPACE from packet impl into consideration
         endOfBodyPosition = writableBuffer.writerIndex() + BUFFER_HEADER_SPACE - 4;
      } else if (endOfBodyPosition <= 0) {
         endOfBodyPosition = BUFFER_HEADER_SPACE + DataConstants.SIZE_INT;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `writableBuffer` accessed in synchronized context
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/message/impl/CoreMessage.java`
#### Snippet
```java
         // The message encode takes into consideration the PacketImpl which is not part of this encoding
         // so we always need to take the BUFFER_HEADER_SPACE from packet impl into consideration
         endOfBodyPosition = writableBuffer.writerIndex() + BUFFER_HEADER_SPACE - 4;
      } else if (endOfBodyPosition <= 0) {
         endOfBodyPosition = BUFFER_HEADER_SPACE + DataConstants.SIZE_INT;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `endOfBodyPosition` accessed in synchronized context
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/message/impl/CoreMessage.java`
#### Snippet
```java
         // so we always need to take the BUFFER_HEADER_SPACE from packet impl into consideration
         endOfBodyPosition = writableBuffer.writerIndex() + BUFFER_HEADER_SPACE - 4;
      } else if (endOfBodyPosition <= 0) {
         endOfBodyPosition = BUFFER_HEADER_SPACE + DataConstants.SIZE_INT;
      }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `endOfBodyPosition` accessed in synchronized context
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/message/impl/CoreMessage.java`
#### Snippet
```java
         endOfBodyPosition = writableBuffer.writerIndex() + BUFFER_HEADER_SPACE - 4;
      } else if (endOfBodyPosition <= 0) {
         endOfBodyPosition = BUFFER_HEADER_SPACE + DataConstants.SIZE_INT;
      }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `buffer` accessed in synchronized context
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/message/impl/CoreMessage.java`
#### Snippet
```java
      }

      buffer.setInt(0, endOfBodyPosition);
      // The end of body position
      buffer.setIndex(0, endOfBodyPosition - BUFFER_HEADER_SPACE + DataConstants.SIZE_INT);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `endOfBodyPosition` accessed in synchronized context
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/message/impl/CoreMessage.java`
#### Snippet
```java
      }

      buffer.setInt(0, endOfBodyPosition);
      // The end of body position
      buffer.setIndex(0, endOfBodyPosition - BUFFER_HEADER_SPACE + DataConstants.SIZE_INT);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `buffer` accessed in synchronized context
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/message/impl/CoreMessage.java`
#### Snippet
```java
      buffer.setInt(0, endOfBodyPosition);
      // The end of body position
      buffer.setIndex(0, endOfBodyPosition - BUFFER_HEADER_SPACE + DataConstants.SIZE_INT);

      encodeHeadersAndProperties(buffer);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `endOfBodyPosition` accessed in synchronized context
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/message/impl/CoreMessage.java`
#### Snippet
```java
      buffer.setInt(0, endOfBodyPosition);
      // The end of body position
      buffer.setIndex(0, endOfBodyPosition - BUFFER_HEADER_SPACE + DataConstants.SIZE_INT);

      encodeHeadersAndProperties(buffer);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `buffer` accessed in synchronized context
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/message/impl/CoreMessage.java`
#### Snippet
```java
      buffer.setIndex(0, endOfBodyPosition - BUFFER_HEADER_SPACE + DataConstants.SIZE_INT);

      encodeHeadersAndProperties(buffer);

      validBuffer = true;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `writableBuffer` accessed in synchronized context
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/message/impl/CoreMessage.java`
#### Snippet
```java
      if (writableBuffer == null) {
         synchronized (this) {
            if (writableBuffer == null) {
               ResetLimitWrappedActiveMQBuffer writableBuffer = new ResetLimitWrappedActiveMQBuffer(BODY_OFFSET, buffer.duplicate(), this);
               if (endOfBodyPosition > 0) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `buffer` accessed in synchronized context
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/message/impl/CoreMessage.java`
#### Snippet
```java
         synchronized (this) {
            if (writableBuffer == null) {
               ResetLimitWrappedActiveMQBuffer writableBuffer = new ResetLimitWrappedActiveMQBuffer(BODY_OFFSET, buffer.duplicate(), this);
               if (endOfBodyPosition > 0) {
                  writableBuffer.byteBuf().setIndex(BODY_OFFSET, endOfBodyPosition - BUFFER_HEADER_SPACE + BODY_OFFSET);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `endOfBodyPosition` accessed in synchronized context
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/message/impl/CoreMessage.java`
#### Snippet
```java
            if (writableBuffer == null) {
               ResetLimitWrappedActiveMQBuffer writableBuffer = new ResetLimitWrappedActiveMQBuffer(BODY_OFFSET, buffer.duplicate(), this);
               if (endOfBodyPosition > 0) {
                  writableBuffer.byteBuf().setIndex(BODY_OFFSET, endOfBodyPosition - BUFFER_HEADER_SPACE + BODY_OFFSET);
                  writableBuffer.resetReaderIndex();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `endOfBodyPosition` accessed in synchronized context
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/message/impl/CoreMessage.java`
#### Snippet
```java
               ResetLimitWrappedActiveMQBuffer writableBuffer = new ResetLimitWrappedActiveMQBuffer(BODY_OFFSET, buffer.duplicate(), this);
               if (endOfBodyPosition > 0) {
                  writableBuffer.byteBuf().setIndex(BODY_OFFSET, endOfBodyPosition - BUFFER_HEADER_SPACE + BODY_OFFSET);
                  writableBuffer.resetReaderIndex();
               }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.writableBuffer` accessed in synchronized context
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/message/impl/CoreMessage.java`
#### Snippet
```java
                  writableBuffer.resetReaderIndex();
               }
               this.writableBuffer = writableBuffer;
            }
         }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `destroyed` accessed in synchronized context
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/RemotingConnectionImpl.java`
#### Snippet
```java
   public void destroy() {
      synchronized (failLock) {
         if (destroyed) {
            return;
         }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `destroyed` accessed in synchronized context
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/RemotingConnectionImpl.java`
#### Snippet
```java
         }

         destroyed = true;
      }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `destroyed` accessed in synchronized context
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/RemotingConnectionImpl.java`
#### Snippet
```java
   public void fail(final ActiveMQException me, String scaleDownTargetNodeID) {
      synchronized (failLock) {
         if (destroyed) {
            return;
         }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `destroyed` accessed in synchronized context
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/RemotingConnectionImpl.java`
#### Snippet
```java
         }

         destroyed = true;
      }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `active` accessed in synchronized context
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/ActiveMQChannelHandler.java`
#### Snippet
```java
   public void channelInactive(final ChannelHandlerContext ctx) throws Exception {
      synchronized (this) {
         if (active) {
            listenerExecutor.execute(() -> listener.connectionDestroyed(channelId(ctx.channel())));

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `active` accessed in synchronized context
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/ActiveMQChannelHandler.java`
#### Snippet
```java
            listenerExecutor.execute(() -> listener.connectionDestroyed(channelId(ctx.channel())));

            active = false;
         }
      }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `active` accessed in synchronized context
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/ActiveMQChannelHandler.java`
#### Snippet
```java
         try {
            listenerExecutor.execute(() -> listener.connectionException(channelId(ctx.channel()), me));
            active = false;
         } catch (Exception ex) {
            ActiveMQClientLogger.LOGGER.errorCallingLifeCycleListener(ex);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `EPOLL_CONNECTOR_TYPE` accessed in synchronized context
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/NettyConnector.java`
#### Snippet
```java
            group = new EpollEventLoopGroup(remotingThreads);
         }
         connectorType = EPOLL_CONNECTOR_TYPE;
         channelClazz = EpollSocketChannel.class;
         logger.debug("Connector {} using native epoll", this);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `KQUEUE_CONNECTOR_TYPE` accessed in synchronized context
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/NettyConnector.java`
#### Snippet
```java
            group = new KQueueEventLoopGroup(remotingThreads);
         }
         connectorType = KQUEUE_CONNECTOR_TYPE;
         channelClazz = KQueueSocketChannel.class;
         logger.debug("Connector {} using native kqueue", this);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `NIO_CONNECTOR_TYPE` accessed in synchronized context
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/NettyConnector.java`
#### Snippet
```java
            group = new NioEventLoopGroup(remotingThreads);
         }
         connectorType = NIO_CONNECTOR_TYPE;
         channelClazz = NioSocketChannel.class;
         logger.debug("Connector {} using nio", this);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `message` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/cursor/QueryPagedReferenceImpl.java`
#### Snippet
```java
   @Override
   public synchronized PagedMessage getPagedMessage() {
      if (message == null) {
         message = subscription.queryMessage(position);
      }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `message` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/cursor/QueryPagedReferenceImpl.java`
#### Snippet
```java
   public synchronized PagedMessage getPagedMessage() {
      if (message == null) {
         message = subscription.queryMessage(position);
      }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `message` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/cursor/QueryPagedReferenceImpl.java`
#### Snippet
```java
      }

      return message;
   }
}
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `message` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/cursor/PagedReferenceImpl.java`
#### Snippet
```java
   @Override
   public synchronized PagedMessage getPagedMessage() {
      return message;
   }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `nodeActivationSequence` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/NodeManager.java`
#### Snippet
```java
   public long getNodeActivationSequence() {
      synchronized (nodeIDGuard) {
         return nodeActivationSequence;
      }
   }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `nodeActivationSequence` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/NodeManager.java`
#### Snippet
```java
      }
      synchronized (nodeIDGuard) {
         nodeActivationSequence = sequence;
      }
   }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `channel` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/FileBasedNodeManager.java`
#### Snippet
```java
      synchronized (nodeIDGuard) {
         ByteBuffer id = ByteBuffer.allocateDirect(16);
         int read = channel.read(id, 3);
         if (replicatedBackup) {
            id.position(0);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `channel` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/FileBasedNodeManager.java`
#### Snippet
```java
            id.put(getUUID().asBytes(), 0, 16);
            id.position(0);
            channel.write(id, 3);
            channel.force(true);
         } else if (read != 16) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `channel` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/FileBasedNodeManager.java`
#### Snippet
```java
            id.position(0);
            channel.write(id, 3);
            channel.force(true);
         } else if (read != 16) {
            setUUID(UUIDGenerator.getInstance().generateUUID());
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `channel` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/FileBasedNodeManager.java`
#### Snippet
```java
            id.put(getUUID().asBytes(), 0, 16);
            id.position(0);
            channel.write(id, 3);
            channel.force(true);
         } else {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `channel` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/FileBasedNodeManager.java`
#### Snippet
```java
            id.position(0);
            channel.write(id, 3);
            channel.force(true);
         } else {
            byte[] bytes = new byte[16];
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.nodeActivationSequence` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/FileBasedNodeManager.java`
#### Snippet
```java
            try {
               setUpServerLockFile();
               final long nodeActivationSequence = this.nodeActivationSequence;
               if (nodeActivationSequence != NULL_NODE_ACTIVATION_SEQUENCE) {
                  writeNodeActivationSequence(nodeActivationSequence);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `channel` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/FileBasedNodeManager.java`
#### Snippet
```java
   @Override
   public synchronized void stop() throws Exception {
      FileChannel channelCopy = channel;
      try {
         if (channelCopy != null)
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.activationSequenceChannel` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/FileBasedNodeManager.java`
#### Snippet
```java
         try {
            setNodeActivationSequence(NULL_NODE_ACTIVATION_SEQUENCE);
            FileChannel dataVersionChannel = this.activationSequenceChannel;
            this.activationSequenceChannel = null;
            if (dataVersionChannel != null) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.activationSequenceChannel` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/FileBasedNodeManager.java`
#### Snippet
```java
            setNodeActivationSequence(NULL_NODE_ACTIVATION_SEQUENCE);
            FileChannel dataVersionChannel = this.activationSequenceChannel;
            this.activationSequenceChannel = null;
            if (dataVersionChannel != null) {
               dataVersionChannel.close();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `serverLockFile` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/FileBasedNodeManager.java`
#### Snippet
```java
    */
   protected synchronized void setUpServerLockFile() throws IOException {
      serverLockFile = newFile(SERVER_LOCK_NAME);

      boolean fileCreated = false;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `serverLockFile` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/FileBasedNodeManager.java`
#### Snippet
```java

      int count = 0;
      while (!serverLockFile.exists()) {
         try {
            fileCreated = serverLockFile.createNewFile();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `serverLockFile` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/FileBasedNodeManager.java`
#### Snippet
```java
      while (!serverLockFile.exists()) {
         try {
            fileCreated = serverLockFile.createNewFile();
         } catch (RuntimeException e) {
            ActiveMQServerLogger.LOGGER.nodeManagerCantOpenFile(serverLockFile, e);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `serverLockFile` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/FileBasedNodeManager.java`
#### Snippet
```java
            fileCreated = serverLockFile.createNewFile();
         } catch (RuntimeException e) {
            ActiveMQServerLogger.LOGGER.nodeManagerCantOpenFile(serverLockFile, e);
            throw e;
         } catch (IOException e) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `serverLockFile` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/FileBasedNodeManager.java`
#### Snippet
```java
               continue;
            }
            ActiveMQServerLogger.LOGGER.nodeManagerCantOpenFile(serverLockFile, e);
            throw e;
         }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `channel` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/FileBasedNodeManager.java`
#### Snippet
```java
      }

      if (channel != null) {
         try {
            channel.close();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `channel` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/FileBasedNodeManager.java`
#### Snippet
```java
      if (channel != null) {
         try {
            channel.close();
         } catch (IOException ignored) {
            // can ignore it: going to open a new file and that's the I/O to care about
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `channel` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/FileBasedNodeManager.java`
#### Snippet
```java
            // can ignore it: going to open a new file and that's the I/O to care about
         } finally {
            channel = null;
         }
      }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `serverLockFile` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/FileBasedNodeManager.java`
#### Snippet
```java

      @SuppressWarnings("resource")
      RandomAccessFile raFile = new RandomAccessFile(serverLockFile, ACCESS_MODE);

      channel = raFile.getChannel();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `channel` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/FileBasedNodeManager.java`
#### Snippet
```java
      RandomAccessFile raFile = new RandomAccessFile(serverLockFile, ACCESS_MODE);

      channel = raFile.getChannel();

      if (fileCreated) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `channel` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/FileBasedNodeManager.java`
#### Snippet
```java
         id.put(bytes, 0, 3);
         id.position(0);
         channel.write(id, 0);
         channel.force(true);
      }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `channel` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/FileBasedNodeManager.java`
#### Snippet
```java
         id.position(0);
         channel.write(id, 0);
         channel.force(true);
      }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `refsToAck` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/RefsOperation.java`
#### Snippet
```java
   public synchronized List<MessageReference> getLingerMessages() {
      List<MessageReference> list = new LinkedList<>();
      for (MessageReference ref : refsToAck) {
         if (!ref.hasConsumerId() && lingerSessionId != null) {
            list.add(ref);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `refsToAck` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/RefsOperation.java`
#### Snippet
```java

   synchronized void addAck(final MessageReference ref) {
      refsToAck.add(ref);
      if (ref.isPaged()) {
         if (pagedMessagesToPostACK == null) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `pagedMessagesToPostACK` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/RefsOperation.java`
#### Snippet
```java
      refsToAck.add(ref);
      if (ref.isPaged()) {
         if (pagedMessagesToPostACK == null) {
            pagedMessagesToPostACK = new ArrayList<>();
         }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `pagedMessagesToPostACK` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/RefsOperation.java`
#### Snippet
```java
      if (ref.isPaged()) {
         if (pagedMessagesToPostACK == null) {
            pagedMessagesToPostACK = new ArrayList<>();
         }
         pagedMessagesToPostACK.add(ref);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `pagedMessagesToPostACK` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/RefsOperation.java`
#### Snippet
```java
            pagedMessagesToPostACK = new ArrayList<>();
         }
         pagedMessagesToPostACK.add(ref);
         //here we do something to prevent page file
         //from being deleted until the operation is done.
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `refsToAck` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/RefsOperation.java`
#### Snippet
```java
   public synchronized List<MessageReference> getListOnConsumer(long consumerID) {
      List<MessageReference> list = new LinkedList<>();
      for (MessageReference ref : refsToAck) {
         if (ref.hasConsumerId() && ref.getConsumerId() == consumerID) {
            list.add(ref);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `refsToAck` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/RefsOperation.java`
#### Snippet
```java
      List<MessageReference> listRet = new LinkedList<>();

      if (refsToAck != null && !refsToAck.isEmpty()) {
         listRet.addAll(refsToAck);
      }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `refsToAck` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/RefsOperation.java`
#### Snippet
```java
      List<MessageReference> listRet = new LinkedList<>();

      if (refsToAck != null && !refsToAck.isEmpty()) {
         listRet.addAll(refsToAck);
      }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `refsToAck` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/RefsOperation.java`
#### Snippet
```java

      if (refsToAck != null && !refsToAck.isEmpty()) {
         listRet.addAll(refsToAck);
      }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `refsToAck` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/RefsOperation.java`
#### Snippet
```java

   synchronized void addOnlyRefAck(final MessageReference ref) {
      refsToAck.add(ref);
   }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `current` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerConsumerImpl.java`
#### Snippet
```java
      public synchronized void run() {
         // if the reference was busy during the previous iteration, handle it now
         if (current != null) {
            try {
               HandleStatus status = handle(current);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `current` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerConsumerImpl.java`
#### Snippet
```java
         if (current != null) {
            try {
               HandleStatus status = handle(current);

               if (status == HandleStatus.BUSY) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `current` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerConsumerImpl.java`
#### Snippet
```java

               if (status == HandleStatus.HANDLED) {
                  proceedDeliver(current);
               }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `current` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerConsumerImpl.java`
#### Snippet
```java
               }

               current = null;
            } catch (Exception e) {
               ActiveMQServerLogger.LOGGER.errorBrowserHandlingMessage(current, e);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `current` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerConsumerImpl.java`
#### Snippet
```java
               current = null;
            } catch (Exception e) {
               ActiveMQServerLogger.LOGGER.errorBrowserHandlingMessage(current, e);
               return;
            }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `current` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerConsumerImpl.java`
#### Snippet
```java
                  // keep a reference on the current message reference
                  // to handle it next time the browser deliverer is executed
                  current = ref;
                  break;
               }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `metrics` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerConsumerImpl.java`
#### Snippet
```java
         }

         metrics.addAcknowledge(ref.getMessage().getEncodeSize(), tx);
         ref.acknowledge(tx, this);

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `metrics` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerConsumerImpl.java`
#### Snippet
```java
            }

            metrics.addMessage(ref.getMessage().getEncodeSize());

            ref.handled();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `metrics` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerConsumerImpl.java`
#### Snippet
```java
               // With pre-ack, we ack *before* sending to the client
               ref.getQueue().acknowledge(ref, this);
               metrics.addAcknowledge(ref.getMessage().getEncodeSize(), null);
            }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `metrics` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerConsumerImpl.java`
#### Snippet
```java
         ref.decrementDeliveryCount();
      }
      metrics.addAcknowledge(ref.getMessage().getEncodeSize(), null);
      ref.getQueue().cancel(ref, System.currentTimeMillis());
   }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `metrics` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerConsumerImpl.java`
#### Snippet
```java
            ref.acknowledge(tx, this);
            ackedRefs.add(ref.getMessageID());
            metrics.addAcknowledge(ref.getMessage().getEncodeSize(), tx);
         }
         while (ref.getMessageID() != messageID);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `metrics` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerConsumerImpl.java`
#### Snippet
```java
         logger.trace("Message {} back to delivering", reference);
         deliveringRefs.addFirst(reference);
         metrics.addMessage(reference.getMessage().getEncodeSize());
      }
   }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `metrics` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerConsumerImpl.java`
#### Snippet
```java
         MessageReference ref;
         while ((ref = deliveringRefs.poll()) != null) {
            metrics.addAcknowledge(ref.getMessage().getEncodeSize(), tx);
            if (performACK) {
               ref.acknowledge(tx, this);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `metrics` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerConsumerImpl.java`
#### Snippet
```java
         return; // nothing to be done
      }
      metrics.addAcknowledge(ref.getMessage().getEncodeSize(), null);
      ref.getQueue().sendToDeadLetterAddress(null, ref);
   }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `channel` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/FileLockNodeManager.java`
#### Snippet
```java
      super.setUpServerLockFile();

      lockChannels[0] = channel;
      serverLockLastModified = serverLockFile.lastModified();

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `serverLockFile` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/FileLockNodeManager.java`
#### Snippet
```java

      lockChannels[0] = channel;
      serverLockLastModified = serverLockFile.lastModified();

      for (int i = 1; i < 3; i++) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.pendingTX` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java
   @Override
   public synchronized void xaRollback(final Xid xid) throws Exception {
      this.pendingTX = null;

      if (tx != null && tx.getXid().equals(xid)) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `tx` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java
      this.pendingTX = null;

      if (tx != null && tx.getXid().equals(xid)) {
         final String msg = "Cannot roll back, session is currently doing work in a transaction " + tx.getXid();

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `tx` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java
      this.pendingTX = null;

      if (tx != null && tx.getXid().equals(xid)) {
         final String msg = "Cannot roll back, session is currently doing work in a transaction " + tx.getXid();

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `tx` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java

      if (tx != null && tx.getXid().equals(xid)) {
         final String msg = "Cannot roll back, session is currently doing work in a transaction " + tx.getXid();

         throw new ActiveMQXAException(XAException.XAER_PROTO, msg);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `remotingConnection` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java
         throw new ActiveMQXAException(XAException.XAER_PROTO, msg);
      } else {
         Transaction theTx = resourceManager.removeTransaction(xid, remotingConnection);
         logger.trace("xarollback into {}", theTx);

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `tx` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java

               // Put it back
               resourceManager.putTransaction(xid, tx, remotingConnection);

               throw new ActiveMQXAException(XAException.XAER_PROTO, "Cannot rollback transaction, it is suspended " + xid);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `remotingConnection` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java

               // Put it back
               resourceManager.putTransaction(xid, tx, remotingConnection);

               throw new ActiveMQXAException(XAException.XAER_PROTO, "Cannot rollback transaction, it is suspended " + xid);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.pendingTX` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java
   @Override
   public synchronized void xaCommit(final Xid xid, final boolean onePhase) throws Exception {
      this.pendingTX = null;

      if (tx != null && tx.getXid().equals(xid)) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `tx` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java
      this.pendingTX = null;

      if (tx != null && tx.getXid().equals(xid)) {
         final String msg = "Cannot commit, session is currently doing work in transaction " + tx.getXid();

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `tx` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java
      this.pendingTX = null;

      if (tx != null && tx.getXid().equals(xid)) {
         final String msg = "Cannot commit, session is currently doing work in transaction " + tx.getXid();

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `tx` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java

      if (tx != null && tx.getXid().equals(xid)) {
         final String msg = "Cannot commit, session is currently doing work in transaction " + tx.getXid();

         throw new ActiveMQXAException(XAException.XAER_PROTO, msg);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `remotingConnection` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java
         throw new ActiveMQXAException(XAException.XAER_PROTO, msg);
      } else {
         Transaction theTx = resourceManager.removeTransaction(xid, remotingConnection);

         logger.trace("XAcommit into {}, xid={}", theTx, xid);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `remotingConnection` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java
            if (theTx.getState() == Transaction.State.SUSPENDED) {
               // Put it back
               resourceManager.putTransaction(xid, theTx, remotingConnection);

               throw new ActiveMQXAException(XAException.XAER_PROTO, "Cannot commit transaction, it is suspended " + xid);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `tx` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java
            throw new ActiveMQXAException(XAException.XAER_PROTO, "Cannot join tx, it is suspended " + xid);
         } else {
            tx = theTx;
         }
      }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `tx` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java
   @Override
   public synchronized void xaResume(final Xid xid) throws Exception {
      if (tx != null) {
         final String msg = "Cannot resume, session is currently doing work in a transaction " + tx.getXid();

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `tx` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java
   public synchronized void xaResume(final Xid xid) throws Exception {
      if (tx != null) {
         final String msg = "Cannot resume, session is currently doing work in a transaction " + tx.getXid();

         throw new ActiveMQXAException(XAException.XAER_PROTO, msg);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `tx` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java
               throw new ActiveMQXAException(XAException.XAER_PROTO, "Cannot resume transaction, it is not suspended " + xid);
            } else {
               tx = theTx;

               tx.resume();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `tx` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java
               tx = theTx;

               tx.resume();
            }
         }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `started` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java
            throw ActiveMQMessageBundle.BUNDLE.cannotCreateConsumerOnClosedSession(queueName);
         }
         consumer = new ServerConsumerImpl(consumerID, this, (QueueBinding) binding, filter, priority, started, browseOnly, storageManager, callback, preAcknowledge, strictUpdateDeliveryCount, managementService, supportLargeMessage, credits, server);
         consumers.put(consumer.getID(), consumer);
      }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `autoCommitSends` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java

         if (AuditLogger.isMessageLoggingEnabled()) {
            if (tx != null && !autoCommitSends) {
               AuditLogger.addSendToTransaction(remotingConnection.getSubject(), remotingConnection.getRemoteAddress(), message.toString(), tx.toString());
               tx.addOperation(new TransactionOperationAbstract() {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `remotingConnection` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java
         if (AuditLogger.isMessageLoggingEnabled()) {
            if (tx != null && !autoCommitSends) {
               AuditLogger.addSendToTransaction(remotingConnection.getSubject(), remotingConnection.getRemoteAddress(), message.toString(), tx.toString());
               tx.addOperation(new TransactionOperationAbstract() {
                  @Override
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `remotingConnection` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java
         if (AuditLogger.isMessageLoggingEnabled()) {
            if (tx != null && !autoCommitSends) {
               AuditLogger.addSendToTransaction(remotingConnection.getSubject(), remotingConnection.getRemoteAddress(), message.toString(), tx.toString());
               tx.addOperation(new TransactionOperationAbstract() {
                  @Override
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `remotingConnection` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java
      if (theTX == null) {
         theTX = newTransaction(xid);
         resourceManager.putTransaction(xid, theTX, remotingConnection);
      }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `tx` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java
      if (theTX.isEffective()) {
         logger.debug("Client failed with Xid {} but the server already had it {}", xid, theTX.getState());
         tx = null;
      } else {
         theTX.markAsRollbackOnly(new ActiveMQException("Can't commit as a Failover happened during the operation"));
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `tx` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java
      } else {
         theTX.markAsRollbackOnly(new ActiveMQException("Can't commit as a Failover happened during the operation"));
         tx = theTX;
      }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `tx` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java
      }

      logger.trace("xastart into tx= {}", tx);
   }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `tx` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java
   private synchronized void rollback(final boolean clientFailed,
                                      final boolean considerLastMessageAsDelivered) throws Exception {
      if (tx == null) {
         // Might be null if XA

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `tx` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java
         // Might be null if XA

         tx = newTransaction();
      }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `tx` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java
      }

      doRollback(clientFailed, considerLastMessageAsDelivered, tx);

      if (xa) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `xa` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java
      doRollback(clientFailed, considerLastMessageAsDelivered, tx);

      if (xa) {
         tx = null;
      } else {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `tx` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java

      if (xa) {
         tx = null;
      } else {
         tx = newTransaction();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `tx` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java
         tx = null;
      } else {
         tx = newTransaction();
      }
   }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `remotingConnection` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java
      synchronized (this) {
         // Remove failure listeners from old connection
         remotingConnection.removeFailureListener(this);
         tempQueueCleannerUppers.values()
                 .forEach(cleanerUpper -> {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `remotingConnection` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java

         // Set the new connection
         remotingConnection = newConnection;

         // Add failure listeners to new connection
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `tx` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java
   @Override
   public synchronized void xaSuspend() throws Exception {
      logger.trace("xasuspend on {}", tx);

      if (tx == null) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `tx` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java
      logger.trace("xasuspend on {}", tx);

      if (tx == null) {
         final String msg = "Cannot suspend, session is not doing work in a transaction ";

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `tx` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java
         throw new ActiveMQXAException(XAException.XAER_PROTO, msg);
      } else {
         if (tx.getState() == Transaction.State.SUSPENDED) {
            final String msg = "Cannot suspend, transaction is already suspended " + tx.getXid();

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `tx` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java
      } else {
         if (tx.getState() == Transaction.State.SUSPENDED) {
            final String msg = "Cannot suspend, transaction is already suspended " + tx.getXid();

            throw new ActiveMQXAException(XAException.XAER_PROTO, msg);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `tx` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java
            throw new ActiveMQXAException(XAException.XAER_PROTO, msg);
         } else {
            tx.suspend();

            tx = null;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `tx` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java
            tx.suspend();

            tx = null;
         }
      }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `tx` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java
   @Override
   public synchronized void xaPrepare(final Xid xid) throws Exception {
      if (tx != null && tx.getXid().equals(xid)) {
         final String msg = "Cannot commit, session is currently doing work in a transaction " + tx.getXid();

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `tx` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java
   @Override
   public synchronized void xaPrepare(final Xid xid) throws Exception {
      if (tx != null && tx.getXid().equals(xid)) {
         final String msg = "Cannot commit, session is currently doing work in a transaction " + tx.getXid();

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `tx` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java
   public synchronized void xaPrepare(final Xid xid) throws Exception {
      if (tx != null && tx.getXid().equals(xid)) {
         final String msg = "Cannot commit, session is currently doing work in a transaction " + tx.getXid();

         throw new ActiveMQXAException(XAException.XAER_PROTO, msg);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `tx` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java
         Transaction theTx = resourceManager.getTransaction(xid);

         logger.trace("xaprepare into xid={}, tx={}", xid, tx);

         if (theTx == null) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `tx` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java
         if (failed) {

            Transaction txToRollback = tx;
            if (txToRollback != null) {
               if (txToRollback.tryRollback() && txToRollback.getXid() != null) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `remotingConnection` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java
            if (txToRollback != null) {
               if (txToRollback.tryRollback() && txToRollback.getXid() != null) {
                  resourceManager.removeTransaction(txToRollback.getXid(), remotingConnection);
               }
            }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `pendingTX` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java
            }

            txToRollback = pendingTX;

            if (txToRollback != null) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `remotingConnection` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java
            if (txToRollback != null) {
               if (txToRollback.tryRollback() && txToRollback.getXid() != null) {
                  resourceManager.removeTransaction(txToRollback.getXid(), remotingConnection);
               }
            }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `tx` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java

         } else {
            if (tx != null && tx.getXid() == null) {
               // We only rollback local txs on close, not XA tx branches

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `tx` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java

         } else {
            if (tx != null && tx.getXid() == null) {
               // We only rollback local txs on close, not XA tx branches

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `remotingConnection` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java
         server.removeSession(name);

         remotingConnection.removeFailureListener(this);

         if (callback != null) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `tx` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java

      try {
         if (tx != null) {
            tx.commit();
         }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `tx` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java
      try {
         if (tx != null) {
            tx.commit();
         }
      } finally {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `xa` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java
         }
      } finally {
         if (xa) {
            tx = null;
         } else {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `tx` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java
      } finally {
         if (xa) {
            tx = null;
         } else {
            tx = newTransaction();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `tx` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java
            tx = null;
         } else {
            tx = newTransaction();
         }
      }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `autoCommitSends` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java
         securityCheck(CompositeAddress.extractAddressName(art.getName()), CompositeAddress.isFullyQualified(art.getName()) ? CompositeAddress.extractQueueName(art.getName()) : null, CheckType.SEND, this);
      } catch (ActiveMQException e) {
         if (!autoCommitSends && tx != null) {
            tx.markAsRollbackOnly(e);
         }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `autoCommitSends` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java
      }

      if (tx == null || autoCommitSends) {
         routingContext.setTransaction(null);
      } else {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `tx` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java
   @Override
   public synchronized void xaEnd(final Xid xid) throws Exception {
      if (tx != null && tx.getXid().equals(xid)) {
         if (tx.getState() == Transaction.State.SUSPENDED) {
            final String msg = "Cannot end, transaction is suspended";
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `tx` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java
   @Override
   public synchronized void xaEnd(final Xid xid) throws Exception {
      if (tx != null && tx.getXid().equals(xid)) {
         if (tx.getState() == Transaction.State.SUSPENDED) {
            final String msg = "Cannot end, transaction is suspended";
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `tx` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java
   public synchronized void xaEnd(final Xid xid) throws Exception {
      if (tx != null && tx.getXid().equals(xid)) {
         if (tx.getState() == Transaction.State.SUSPENDED) {
            final String msg = "Cannot end, transaction is suspended";

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `tx` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java

            throw new ActiveMQXAException(XAException.XAER_PROTO, msg);
         } else if (tx.getState() == Transaction.State.ROLLEDBACK) {
            final String msg = "Cannot end, transaction is rolled back";

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `tx` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java
            final String msg = "Cannot end, transaction is rolled back";

            final boolean timeout = tx.hasTimedOut();
            tx = null;

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `tx` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java

            final boolean timeout = tx.hasTimedOut();
            tx = null;

            if (timeout) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.pendingTX` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java
            }
         } else {
            this.pendingTX = tx;
            tx = null;
         }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `tx` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java
            }
         } else {
            this.pendingTX = tx;
            tx = null;
         }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `tx` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java
         } else {
            this.pendingTX = tx;
            tx = null;
         }
      } else {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `tx` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java
   @Override
   public synchronized void xaStart(final Xid xid) throws Exception {
      if (tx != null) {
         ActiveMQServerLogger.LOGGER.xidReplacedOnXStart(tx.getXid().toString(), xid.toString());

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `tx` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java
   public synchronized void xaStart(final Xid xid) throws Exception {
      if (tx != null) {
         ActiveMQServerLogger.LOGGER.xidReplacedOnXStart(tx.getXid().toString(), xid.toString());

         try {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `tx` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java

         try {
            if (tx.getState() != Transaction.State.PREPARED) {
               // we don't want to rollback anything prepared here
               if (tx.getXid() != null) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `tx` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java
            if (tx.getState() != Transaction.State.PREPARED) {
               // we don't want to rollback anything prepared here
               if (tx.getXid() != null) {
                  resourceManager.removeTransaction(tx.getXid(), remotingConnection);
               }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `tx` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java
               // we don't want to rollback anything prepared here
               if (tx.getXid() != null) {
                  resourceManager.removeTransaction(tx.getXid(), remotingConnection);
               }
               tx.rollback();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `remotingConnection` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java
               // we don't want to rollback anything prepared here
               if (tx.getXid() != null) {
                  resourceManager.removeTransaction(tx.getXid(), remotingConnection);
               }
               tx.rollback();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `tx` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java
                  resourceManager.removeTransaction(tx.getXid(), remotingConnection);
               }
               tx.rollback();
            }
         } catch (Exception e) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `tx` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java
      }

      tx = newTransaction(xid);

      logger.trace("xastart into tx= {}", tx);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `tx` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java
      tx = newTransaction(xid);

      logger.trace("xastart into tx= {}", tx);

      boolean added = resourceManager.putTransaction(xid, tx, remotingConnection);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `tx` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java
      logger.trace("xastart into tx= {}", tx);

      boolean added = resourceManager.putTransaction(xid, tx, remotingConnection);

      if (!added) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `remotingConnection` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java
      logger.trace("xastart into tx= {}", tx);

      boolean added = resourceManager.putTransaction(xid, tx, remotingConnection);

      if (!added) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `tx` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java

      if (!added) {
         final String msg = "Cannot start, there is already a xid " + tx.getXid();

         throw new ActiveMQXAException(XAException.XAER_DUPID, msg);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `backupConnectors` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/BackupManager.java`
#### Snippet
```java
      //start each connector and if we are backup and shared store announce ourselves. NB with replication we don't do this
      //as we wait for replication to start and be notified by the replication manager.
      for (BackupConnector conn : backupConnectors) {
         logger.debug("****** BackupManager connecting to {}", conn);

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `backupConnectors` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/BackupManager.java`
#### Snippet
```java
      if (!started)
         return;
      for (BackupConnector backupConnector : backupConnectors) {
         backupConnector.close();
      }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `session` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/impl/BridgeImpl.java`
#### Snippet
```java
               }
               // Session is pre-acknowledge
               session = (ClientSessionInternal) csf.createSession(configuration.getUser(), configuration.getPassword(), false, true, true, true, 1);
               session.getProducerCreditManager().setCallback(this);
               sessionConsumer = (ClientSessionInternal) csf.createSession(configuration.getUser(), configuration.getPassword(), false, true, true, true, 1);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `session` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/impl/BridgeImpl.java`
#### Snippet
```java
               // Session is pre-acknowledge
               session = (ClientSessionInternal) csf.createSession(configuration.getUser(), configuration.getPassword(), false, true, true, true, 1);
               session.getProducerCreditManager().setCallback(this);
               sessionConsumer = (ClientSessionInternal) csf.createSession(configuration.getUser(), configuration.getPassword(), false, true, true, true, 1);
            }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `sessionConsumer` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/impl/BridgeImpl.java`
#### Snippet
```java
               session = (ClientSessionInternal) csf.createSession(configuration.getUser(), configuration.getPassword(), false, true, true, true, 1);
               session.getProducerCreditManager().setCallback(this);
               sessionConsumer = (ClientSessionInternal) csf.createSession(configuration.getUser(), configuration.getPassword(), false, true, true, true, 1);
            }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `session` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/impl/BridgeImpl.java`
#### Snippet
```java

               try {
                  query = session.addressQuery(SimpleString.toSimpleString(configuration.getForwardingAddress()));
               } catch (Throwable e) {
                  ActiveMQServerLogger.LOGGER.errorQueryingBridge(configuration.getName(), e);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `session` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/impl/BridgeImpl.java`
#### Snippet
```java
            // this would never resume
            blockedOnFlowControl = false;
            producer = session.createProducer();
            session.addFailureListener(BridgeImpl.this);

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `session` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/impl/BridgeImpl.java`
#### Snippet
```java
            blockedOnFlowControl = false;
            producer = session.createProducer();
            session.addFailureListener(BridgeImpl.this);

            session.setSendAcknowledgementHandler(BridgeImpl.this);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `session` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/impl/BridgeImpl.java`
#### Snippet
```java
            session.addFailureListener(BridgeImpl.this);

            session.setSendAcknowledgementHandler(BridgeImpl.this);

            afterConnect();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `session` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/impl/BridgeImpl.java`
#### Snippet
```java

      synchronized (this) {
         if (!active || !session.isWritable(this)) {
            if (logger.isDebugEnabled()) {
               logger.debug("{}::Ignoring reference on bridge as it is set to inactive ref {}, active = {}", this, ref, active);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `server` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/federation/FederatedAbstract.java`
#### Snippet
```java
         FederatedQueueConsumer remoteQueueConsumer = remoteQueueConsumers.get(key);
         if (remoteQueueConsumer == null) {
            if (server.hasBrokerFederationPlugins()) {
               try {
                  server.callBrokerFederationPlugins(plugin -> plugin.beforeCreateFederatedQueueConsumer(key));
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `server` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/federation/FederatedAbstract.java`
#### Snippet
```java
            if (server.hasBrokerFederationPlugins()) {
               try {
                  server.callBrokerFederationPlugins(plugin -> plugin.beforeCreateFederatedQueueConsumer(key));
               } catch (ActiveMQException t) {
                  ActiveMQServerLogger.LOGGER.federationPluginExecutionError("beforeCreateFederatedQueueConsumer", t);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `server` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/federation/FederatedAbstract.java`
#### Snippet
```java
               }
            }
            remoteQueueConsumer = new FederatedQueueConsumerImpl(federation, server, transformer, key, upstream, callback);
            remoteQueueConsumer.start();
            remoteQueueConsumers.put(key, remoteQueueConsumer);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `upstream` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/federation/FederatedAbstract.java`
#### Snippet
```java
               }
            }
            remoteQueueConsumer = new FederatedQueueConsumerImpl(federation, server, transformer, key, upstream, callback);
            remoteQueueConsumer.start();
            remoteQueueConsumers.put(key, remoteQueueConsumer);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `server` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/federation/FederatedAbstract.java`
#### Snippet
```java
            remoteQueueConsumers.put(key, remoteQueueConsumer);

            if (server.hasBrokerFederationPlugins()) {
               try {
                  final FederatedQueueConsumer finalConsumer = remoteQueueConsumer;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `server` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/federation/FederatedAbstract.java`
#### Snippet
```java
               try {
                  final FederatedQueueConsumer finalConsumer = remoteQueueConsumer;
                  server.callBrokerFederationPlugins(plugin -> plugin.afterCreateFederatedQueueConsumer(finalConsumer));
               } catch (ActiveMQException t) {
                  ActiveMQServerLogger.LOGGER.federationPluginExecutionError("afterCreateFederatedQueueConsumer", t);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `server` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/federation/FederatedAbstract.java`
#### Snippet
```java
      FederatedQueueConsumer remoteQueueConsumer = remoteQueueConsumers.get(key);
      if (remoteQueueConsumer != null) {
         if (server.hasBrokerFederationPlugins()) {
            try {
               server.callBrokerFederationPlugins(plugin -> plugin.beforeCloseFederatedQueueConsumer(remoteQueueConsumer));
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `server` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/federation/FederatedAbstract.java`
#### Snippet
```java
         if (server.hasBrokerFederationPlugins()) {
            try {
               server.callBrokerFederationPlugins(plugin -> plugin.beforeCloseFederatedQueueConsumer(remoteQueueConsumer));
            } catch (ActiveMQException t) {
               ActiveMQServerLogger.LOGGER.federationPluginExecutionError("beforeCloseFederatedQueueConsumer", t);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `server` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/federation/FederatedAbstract.java`
#### Snippet
```java
            remoteQueueConsumers.remove(key);
         }
         if (server.hasBrokerFederationPlugins()) {
            try {
               server.callBrokerFederationPlugins(plugin -> plugin.afterCloseFederatedQueueConsumer(remoteQueueConsumer));
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `server` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/federation/FederatedAbstract.java`
#### Snippet
```java
         if (server.hasBrokerFederationPlugins()) {
            try {
               server.callBrokerFederationPlugins(plugin -> plugin.afterCloseFederatedQueueConsumer(remoteQueueConsumer));
            } catch (ActiveMQException t) {
               ActiveMQServerLogger.LOGGER.federationPluginExecutionError("afterCloseFederatedQueueConsumer", t);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `federatedAddressMap` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/federation/FederationUpstream.java`
#### Snippet
```java
   @Override
   public synchronized void stop() {
      for (FederatedAddress federatedAddress : federatedAddressMap.values()) {
         federatedAddress.stop();
      }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `federatedAddressMap` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/federation/FederationUpstream.java`
#### Snippet
```java
         federatedAddress.stop();
      }
      federatedAddressMap.clear();

      for (FederatedQueue federatedQueue : federatedQueueMap.values()) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `federatedQueueMap` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/federation/FederationUpstream.java`
#### Snippet
```java
      federatedAddressMap.clear();

      for (FederatedQueue federatedQueue : federatedQueueMap.values()) {
         federatedQueue.stop();
      }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `federatedQueueMap` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/federation/FederationUpstream.java`
#### Snippet
```java
         federatedQueue.stop();
      }
      federatedQueueMap.clear();

      super.stop();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `federatedAddressMap` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/federation/FederationUpstream.java`
#### Snippet
```java
   public synchronized boolean deploy(FederationAddressPolicyConfiguration federatedAddressConfig) throws ActiveMQException {
      String name = federatedAddressConfig.getName();
      FederatedAddress existing = federatedAddressMap.get(name);
      if (existing == null || !existing.getConfig().equals(federatedAddressConfig)) {
         undeployAddress(name);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `federatedAddressMap` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/federation/FederationUpstream.java`
#### Snippet
```java

         FederatedAddress federatedAddress = new FederatedAddress(federation, federatedAddressConfig, server, this);
         federatedAddressMap.put(name, federatedAddress);
         federation.register(federatedAddress);
         if (connection.isStarted()) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `federatedQueueMap` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/federation/FederationUpstream.java`
#### Snippet
```java
      super.start();

      for (FederatedQueue federatedQueue : federatedQueueMap.values()) {
         federatedQueue.start();
      }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `federatedAddressMap` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/federation/FederationUpstream.java`
#### Snippet
```java
         federatedQueue.start();
      }
      for (FederatedAddress federatedAddress : federatedAddressMap.values()) {
         federatedAddress.start();
      }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `federatedQueueMap` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/federation/FederationUpstream.java`
#### Snippet
```java
   public synchronized boolean deploy(FederationQueuePolicyConfiguration federatedQueueConfig) throws ActiveMQException {
      String name = federatedQueueConfig.getName();
      FederatedQueue existing = federatedQueueMap.get(name);
      if (existing == null || !existing.getConfig().equals(federatedQueueConfig)) {
         undeployQueue(name);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `federatedQueueMap` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/federation/FederationUpstream.java`
#### Snippet
```java

         FederatedQueue federatedQueue = new FederatedQueue(federation, federatedQueueConfig, server, this);
         federatedQueueMap.put(name, federatedQueue);
         federation.register(federatedQueue);
         if (connection.isStarted()) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `scheduledPool` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ActiveMQServerImpl.java`
#### Snippet
```java

      if (dumpInfoInterval > 0) {
         scheduledPool.scheduleWithFixedDelay(new Runnable() {
            @Override
            public void run() {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `executorFactory` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ActiveMQServerImpl.java`
#### Snippet
```java
      if (configuration.getMaxDiskUsage() != -1) {
         try {
            injectMonitor(new FileStoreMonitor(getScheduledPool(), executorFactory.getExecutor(), configuration.getDiskScanPeriod(), TimeUnit.MILLISECONDS, configuration.getMaxDiskUsage() / 100f, ioCriticalErrorListener));
         } catch (Exception e) {
            ActiveMQServerLogger.LOGGER.unableToInjectMonitor(e);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `executorFactory` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ActiveMQServerImpl.java`
#### Snippet
```java
      securityStore = new SecurityStoreImpl(securityRepository, securityManager, configuration.getSecurityInvalidationInterval(), configuration.isSecurityEnabled(), configuration.getClusterUser(), configuration.getClusterPassword(), managementService, configuration.getAuthenticationCacheSize(), configuration.getAuthorizationCacheSize());

      queueFactory = new QueueFactoryImpl(executorFactory, scheduledPool, addressSettingsRepository, storageManager, this);

      pagingManager = createPagingManager();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `scheduledPool` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ActiveMQServerImpl.java`
#### Snippet
```java
      securityStore = new SecurityStoreImpl(securityRepository, securityManager, configuration.getSecurityInvalidationInterval(), configuration.isSecurityEnabled(), configuration.getClusterUser(), configuration.getClusterPassword(), managementService, configuration.getAuthenticationCacheSize(), configuration.getAuthorizationCacheSize());

      queueFactory = new QueueFactoryImpl(executorFactory, scheduledPool, addressSettingsRepository, storageManager, this);

      pagingManager = createPagingManager();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `scheduledPool` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ActiveMQServerImpl.java`
#### Snippet
```java
      pagingManager = createPagingManager();

      resourceManager = new ResourceManagerImpl(this, (int) (configuration.getTransactionTimeout() / 1000), configuration.getTransactionTimeoutScanPeriod(), scheduledPool);

      /**
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `executorFactory` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ActiveMQServerImpl.java`
#### Snippet
```java

      // This can't be created until node id is set
      clusterManager = new ClusterManager(executorFactory, this, postOffice, scheduledPool, managementService, configuration, nodeManager, haPolicy.useQuorumManager());

      federationManager = new FederationManager(this);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `scheduledPool` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ActiveMQServerImpl.java`
#### Snippet
```java

      // This can't be created until node id is set
      clusterManager = new ClusterManager(executorFactory, this, postOffice, scheduledPool, managementService, configuration, nodeManager, haPolicy.useQuorumManager());

      federationManager = new FederationManager(this);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `executorFactory` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ActiveMQServerImpl.java`
#### Snippet
```java
      federationManager = new FederationManager(this);

      backupManager = new BackupManager(this, executorFactory, scheduledPool, nodeManager, configuration, clusterManager);

      clusterManager.deploy();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `scheduledPool` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ActiveMQServerImpl.java`
#### Snippet
```java
      federationManager = new FederationManager(this);

      backupManager = new BackupManager(this, executorFactory, scheduledPool, nodeManager, configuration, clusterManager);

      clusterManager.deploy();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `scheduledPool` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ActiveMQServerImpl.java`
#### Snippet
```java
      federationManager.deploy();

      connectionRouterManager = new ConnectionRouterManager(configuration, this, scheduledPool);

      connectionRouterManager.deploy();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `scheduledPool` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ActiveMQServerImpl.java`
#### Snippet
```java
      connectionRouterManager.deploy();

      remotingService = new RemotingServiceImpl(clusterManager, configuration, this, managementService, scheduledPool, protocolManagerFactories, executorFactory.getExecutor(), serviceRegistry);

      messagingServerControl = managementService.registerServer(postOffice, securityStore, storageManager, configuration, addressSettingsRepository, securityRepository, resourceManager, remotingService, this, queueFactory, scheduledPool, pagingManager, haPolicy.isBackup());
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `executorFactory` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ActiveMQServerImpl.java`
#### Snippet
```java
      connectionRouterManager.deploy();

      remotingService = new RemotingServiceImpl(clusterManager, configuration, this, managementService, scheduledPool, protocolManagerFactories, executorFactory.getExecutor(), serviceRegistry);

      messagingServerControl = managementService.registerServer(postOffice, securityStore, storageManager, configuration, addressSettingsRepository, securityRepository, resourceManager, remotingService, this, queueFactory, scheduledPool, pagingManager, haPolicy.isBackup());
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `scheduledPool` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ActiveMQServerImpl.java`
#### Snippet
```java
      remotingService = new RemotingServiceImpl(clusterManager, configuration, this, managementService, scheduledPool, protocolManagerFactories, executorFactory.getExecutor(), serviceRegistry);

      messagingServerControl = managementService.registerServer(postOffice, securityStore, storageManager, configuration, addressSettingsRepository, securityRepository, resourceManager, remotingService, this, queueFactory, scheduledPool, pagingManager, haPolicy.isBackup());

      // Address settings need to deployed initially, since they're require on paging manager.start()
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `executorFactory` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ActiveMQServerImpl.java`
#### Snippet
```java
      long configurationFileRefreshPeriod = configuration.getConfigurationFileRefreshPeriod();
      if (configurationFileRefreshPeriod > 0) {
         this.reloadManager = new ReloadManagerImpl(getScheduledPool(), executorFactory.getExecutor(), configurationFileRefreshPeriod);

         if (configuration.getConfigurationUrl() != null && getScheduledPool() != null) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `methodRoles` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/JMXAccessControlList.java`
#### Snippet
```java

      public synchronized void addMethods(String prefix, String... roles) {
         List<String> rolesList = methodRoles.get(prefix);
         if (rolesList == null) {
            rolesList = new ArrayList<>();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `methodRoles` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/JMXAccessControlList.java`
#### Snippet
```java
         if (rolesList == null) {
            rolesList = new ArrayList<>();
            methodRoles.put(prefix, rolesList);
         }
         for (String role : roles) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `methodPrefixRoles` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/JMXAccessControlList.java`
#### Snippet
```java

      public synchronized void addMethodsPrefixes(String prefix, String... roles) {
         List<String> rolesList = methodPrefixRoles.get(prefix);
         if (rolesList == null) {
            rolesList = new ArrayList<>();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `methodPrefixRoles` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/JMXAccessControlList.java`
#### Snippet
```java
         if (rolesList == null) {
            rolesList = new ArrayList<>();
            methodPrefixRoles.put(prefix, rolesList);
         }
         for (String role : roles) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `server` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/federation/address/FederatedAddress.java`
#### Snippet
```java
            if ((matchingQueues = matchingDiverts.remove(binding)) != null) {
               try {
                  final AddressInfo addressInfo = server.getPostOffice().getAddressInfo(binding.getAddress());
                  if (addressInfo != null) {
                     //remove queue binding demand if tracked by the divert
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `server` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/federation/address/FederatedAddress.java`
#### Snippet
```java
                  if (addressInfo != null) {
                     //remove queue binding demand if tracked by the divert
                     server.getPostOffice().getBindingsForAddress(forwardAddress)
                        .getBindings().stream().filter(b -> b instanceof QueueBinding && matchingQueues.remove(((QueueBinding) b).getQueue().getName()))
                        .forEach(queueBinding -> removeRemoteConsumer(getKey(addressInfo)));
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `server` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/federation/address/FederatedAddress.java`
#### Snippet
```java
      if (!isStarted()) {
         super.start();
         server.getPostOffice()
             .getAllBindings()
             .filter(b -> b instanceof QueueBinding || b instanceof DivertBinding)
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `server` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/federation/address/FederatedAddress.java`
#### Snippet
```java
                  //create demand for each matching queue binding that isn't already tracked by the divert
                  //conditionalCreateRemoteConsumer will check if the queue is a target of the divert before adding
                  server.getPostOffice().getBindingsForAddress(forwardAddress).getBindings()
                     .stream().filter(b -> b instanceof QueueBinding).map(b -> (QueueBinding) b)
                     .forEach(queueBinding -> conditionalCreateRemoteConsumer(divertBinding, matchingQueues, queueBinding));
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `buffer` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/nullpm/NullStorageLargeServerMessage.java`
#### Snippet
```java
   @Override
   public synchronized int getBodyBufferSize() {
      return buffer.writerIndex();
   }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `buffer` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/nullpm/NullStorageLargeServerMessage.java`
#### Snippet
```java
   @Override
   public synchronized void addBytes(final byte[] bytes) {
      if (buffer == null) {
         buffer = Unpooled.buffer(bytes.length);
      }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `buffer` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/nullpm/NullStorageLargeServerMessage.java`
#### Snippet
```java
   public synchronized void addBytes(final byte[] bytes) {
      if (buffer == null) {
         buffer = Unpooled.buffer(bytes.length);
      }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `buffer` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/nullpm/NullStorageLargeServerMessage.java`
#### Snippet
```java

      // expand the buffer
      buffer.writeBytes(bytes);
   }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `buffer` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/nullpm/NullStorageLargeServerMessage.java`
#### Snippet
```java
   public synchronized void addBytes(ActiveMQBuffer bytes, boolean initialHeader) {
      final int readableBytes = bytes.readableBytes();
      if (buffer == null) {
         buffer = Unpooled.buffer(readableBytes);
      }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `buffer` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/nullpm/NullStorageLargeServerMessage.java`
#### Snippet
```java
      final int readableBytes = bytes.readableBytes();
      if (buffer == null) {
         buffer = Unpooled.buffer(readableBytes);
      }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `buffer` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/nullpm/NullStorageLargeServerMessage.java`
#### Snippet
```java

      // expand the buffer
      buffer.ensureWritable(readableBytes);
      assert buffer.hasArray();
      final int writerIndex = buffer.writerIndex();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `buffer` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/nullpm/NullStorageLargeServerMessage.java`
#### Snippet
```java
      // expand the buffer
      buffer.ensureWritable(readableBytes);
      assert buffer.hasArray();
      final int writerIndex = buffer.writerIndex();
      bytes.readBytes(buffer.array(), buffer.arrayOffset() + writerIndex, readableBytes);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `buffer` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/nullpm/NullStorageLargeServerMessage.java`
#### Snippet
```java
      buffer.ensureWritable(readableBytes);
      assert buffer.hasArray();
      final int writerIndex = buffer.writerIndex();
      bytes.readBytes(buffer.array(), buffer.arrayOffset() + writerIndex, readableBytes);
      buffer.writerIndex(writerIndex + readableBytes);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `buffer` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/nullpm/NullStorageLargeServerMessage.java`
#### Snippet
```java
      assert buffer.hasArray();
      final int writerIndex = buffer.writerIndex();
      bytes.readBytes(buffer.array(), buffer.arrayOffset() + writerIndex, readableBytes);
      buffer.writerIndex(writerIndex + readableBytes);
   }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `buffer` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/nullpm/NullStorageLargeServerMessage.java`
#### Snippet
```java
      assert buffer.hasArray();
      final int writerIndex = buffer.writerIndex();
      bytes.readBytes(buffer.array(), buffer.arrayOffset() + writerIndex, readableBytes);
      buffer.writerIndex(writerIndex + readableBytes);
   }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `buffer` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/nullpm/NullStorageLargeServerMessage.java`
#### Snippet
```java
      final int writerIndex = buffer.writerIndex();
      bytes.readBytes(buffer.array(), buffer.arrayOffset() + writerIndex, readableBytes);
      buffer.writerIndex(writerIndex + readableBytes);
   }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `buffer` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/nullpm/NullStorageLargeServerMessage.java`
#### Snippet
```java
   @Override
   public synchronized ActiveMQBuffer getReadOnlyBodyBuffer() {
      return new ChannelBufferWrapper(buffer.slice(0, buffer.writerIndex()).asReadOnly());
   }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `buffer` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/nullpm/NullStorageLargeServerMessage.java`
#### Snippet
```java
   @Override
   public synchronized ActiveMQBuffer getReadOnlyBodyBuffer() {
      return new ChannelBufferWrapper(buffer.slice(0, buffer.writerIndex()).asReadOnly());
   }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `tasks` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/OperationContextImpl.java`
#### Snippet
```java
      this.errorMessage = errorMessage;

      if (tasks != null) {
         // it's saving the Iterator allocation cost
         final int size = tasks.size();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `tasks` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/OperationContextImpl.java`
#### Snippet
```java
      if (tasks != null) {
         // it's saving the Iterator allocation cost
         final int size = tasks.size();
         for (int i = 0; i < size; i++) {
            final TaskHolder holder = tasks.poll();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `tasks` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/OperationContextImpl.java`
#### Snippet
```java
         final int size = tasks.size();
         for (int i = 0; i < size; i++) {
            final TaskHolder holder = tasks.poll();
            holder.task.onError(errorCode, errorMessage);
         }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `storeOnlyTasks` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/OperationContextImpl.java`
#### Snippet
```java
            final long replicationLined = REPLICATION_LINEUP_UPDATER.get(this);
            if (storeOnly) {
               if (storeOnlyTasks == null) {
                  storeOnlyTasks = new LinkedList<>();
               }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `storeOnlyTasks` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/OperationContextImpl.java`
#### Snippet
```java
            if (storeOnly) {
               if (storeOnlyTasks == null) {
                  storeOnlyTasks = new LinkedList<>();
               }
            } else {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `tasks` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/OperationContextImpl.java`
#### Snippet
```java
               }
            } else {
               if (tasks == null) {
                  tasks = new LinkedList<>();
                  minimalReplicated = replicationLined;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `tasks` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/OperationContextImpl.java`
#### Snippet
```java
            } else {
               if (tasks == null) {
                  tasks = new LinkedList<>();
                  minimalReplicated = replicationLined;
                  minimalStore = storeLined;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `minimalReplicated` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/OperationContextImpl.java`
#### Snippet
```java
               if (tasks == null) {
                  tasks = new LinkedList<>();
                  minimalReplicated = replicationLined;
                  minimalStore = storeLined;
                  minimalPage = pageLined;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `minimalStore` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/OperationContextImpl.java`
#### Snippet
```java
                  tasks = new LinkedList<>();
                  minimalReplicated = replicationLined;
                  minimalStore = storeLined;
                  minimalPage = pageLined;
               }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `minimalPage` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/OperationContextImpl.java`
#### Snippet
```java
                  minimalReplicated = replicationLined;
                  minimalStore = storeLined;
                  minimalPage = pageLined;
               }
            }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `replicated` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/OperationContextImpl.java`
#### Snippet
```java
            // On this case, we can just execute the context directly

            if (replicationLined == replicated && storeLined == stored && pageLined == paged) {
               // We want to avoid the executor if everything is complete...
               // However, we can't execute the context if there are executions pending
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `stored` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/OperationContextImpl.java`
#### Snippet
```java
            // On this case, we can just execute the context directly

            if (replicationLined == replicated && storeLined == stored && pageLined == paged) {
               // We want to avoid the executor if everything is complete...
               // However, we can't execute the context if there are executions pending
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `paged` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/OperationContextImpl.java`
#### Snippet
```java
            // On this case, we can just execute the context directly

            if (replicationLined == replicated && storeLined == stored && pageLined == paged) {
               // We want to avoid the executor if everything is complete...
               // However, we can't execute the context if there are executions pending
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `stored` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/OperationContextImpl.java`
#### Snippet
```java
            } else {
               if (storeOnly) {
                  if (storeLined == stored && EXECUTORS_PENDING_UPDATER.get(this) == 0) {
                     executeNow = true;
                  } else {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `storeOnlyTasks` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/OperationContextImpl.java`
#### Snippet
```java
                     executeNow = true;
                  } else {
                     assert !storeOnlyTasks.isEmpty() ? storeOnlyTasks.peekLast().storeLined <= storeLined : true;
                     storeOnlyTasks.add(new StoreOnlyTaskHolder(completion, storeLined));
                  }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `storeOnlyTasks` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/OperationContextImpl.java`
#### Snippet
```java
                     executeNow = true;
                  } else {
                     assert !storeOnlyTasks.isEmpty() ? storeOnlyTasks.peekLast().storeLined <= storeLined : true;
                     storeOnlyTasks.add(new StoreOnlyTaskHolder(completion, storeLined));
                  }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `storeOnlyTasks` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/OperationContextImpl.java`
#### Snippet
```java
                  } else {
                     assert !storeOnlyTasks.isEmpty() ? storeOnlyTasks.peekLast().storeLined <= storeLined : true;
                     storeOnlyTasks.add(new StoreOnlyTaskHolder(completion, storeLined));
                  }
               } else {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `tasks` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/OperationContextImpl.java`
#### Snippet
```java
                  // ensure total ordering
                  assert validateTasksAdd(storeLined, replicationLined, pageLined);
                  tasks.add(new TaskHolder(completion, storeLined, replicationLined, pageLined));
               }
            }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `paged` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/OperationContextImpl.java`
#### Snippet
```java
   @Override
   public synchronized void pageSyncDone() {
      paged++;
      checkTasks();
   }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `stored` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/OperationContextImpl.java`
#### Snippet
```java
   @Override
   public synchronized void done() {
      stored++;
      checkTasks();
   }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `replicated` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/OperationContextImpl.java`
#### Snippet
```java
   @Override
   public synchronized void replicationDone() {
      replicated++;
      checkTasks();
   }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.bindingsJournal` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/JDBCJournalStorageManager.java`
#### Snippet
```java
                 executorFactory.getExecutor(),
                 criticalErrorListener);
         this.bindingsJournal = bindingsJournal;
         this.messageJournal = messageJournal;
         this.largeMessagesFactory = largeMessagesFactory;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.messageJournal` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/JDBCJournalStorageManager.java`
#### Snippet
```java
                 criticalErrorListener);
         this.bindingsJournal = bindingsJournal;
         this.messageJournal = messageJournal;
         this.largeMessagesFactory = largeMessagesFactory;
         largeMessagesFactory.start();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.largeMessagesFactory` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/JDBCJournalStorageManager.java`
#### Snippet
```java
         this.bindingsJournal = bindingsJournal;
         this.messageJournal = messageJournal;
         this.largeMessagesFactory = largeMessagesFactory;
         largeMessagesFactory.start();
      } catch (Exception e) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `started` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/JDBCJournalStorageManager.java`
#### Snippet
```java
   @Override
   public synchronized void stop(boolean ioCriticalError, boolean sendFailover) throws Exception {
      if (!started) {
         return;
      }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `journalLoaded` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/JDBCJournalStorageManager.java`
#### Snippet
```java
         performCachedLargeMessageDeletes();
         // Must call close to make sure last id is persisted
         if (journalLoaded && idGenerator != null)
            idGenerator.persistCurrentID();
      }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `idGenerator` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/JDBCJournalStorageManager.java`
#### Snippet
```java
         performCachedLargeMessageDeletes();
         // Must call close to make sure last id is persisted
         if (journalLoaded && idGenerator != null)
            idGenerator.persistCurrentID();
      }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `idGenerator` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/JDBCJournalStorageManager.java`
#### Snippet
```java
         // Must call close to make sure last id is persisted
         if (journalLoaded && idGenerator != null)
            idGenerator.persistCurrentID();
      }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `bindingsJournal` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/JDBCJournalStorageManager.java`
#### Snippet
```java
      beforeStop();

      bindingsJournal.stop();
      messageJournal.stop();
      largeMessagesFactory.stop();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `messageJournal` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/JDBCJournalStorageManager.java`
#### Snippet
```java

      bindingsJournal.stop();
      messageJournal.stop();
      largeMessagesFactory.stop();

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `largeMessagesFactory` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/JDBCJournalStorageManager.java`
#### Snippet
```java
      bindingsJournal.stop();
      messageJournal.stop();
      largeMessagesFactory.stop();

      journalLoaded = false;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `journalLoaded` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/JDBCJournalStorageManager.java`
#### Snippet
```java
      largeMessagesFactory.stop();

      journalLoaded = false;

      started = false;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `started` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/JDBCJournalStorageManager.java`
#### Snippet
```java
      journalLoaded = false;

      started = false;
   }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `storageManager` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/LargeBody.java`
#### Snippet
```java
            file.close(false, false);
            if (sendEvent) {
               storageManager.largeMessageClosed(message);
            }
         } catch (Exception e) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `storageManager` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/LargeBody.java`
#### Snippet
```java
      }

      storageManager.addBytesToLargeMessage(file, getMessageID(), bytes);

      bodySize += bytes.length;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `storageManager` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/LargeBody.java`
#### Snippet
```java
         validateFile();
         releaseResources(false, false);
         storageManager.deleteLargeMessageBody(message);
      } catch (Exception e) {
         storageManager.criticalError(e);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `storageManager` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/LargeBody.java`
#### Snippet
```java
         storageManager.deleteLargeMessageBody(message);
      } catch (Exception e) {
         storageManager.criticalError(e);
      }
   }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `storageManager` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/LargeBody.java`
#### Snippet
```java
      final int readableBytes = bytes.readableBytes();

      storageManager.addBytesToLargeMessage(file, getMessageID(), bytes);

      bodySize += readableBytes;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `memoryEstimate` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/LargeServerMessageImpl.java`
#### Snippet
```java
   public int getMemoryEstimate() {
      synchronized (largeBody) {
         if (memoryEstimate == -1) {
            // The body won't be on memory (always on-file), so we don't consider this for paging
            memoryEstimate = MEMORY_OFFSET +
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `memoryEstimate` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/LargeServerMessageImpl.java`
#### Snippet
```java
         if (memoryEstimate == -1) {
            // The body won't be on memory (always on-file), so we don't consider this for paging
            memoryEstimate = MEMORY_OFFSET +
               getHeadersAndPropertiesEncodeSize() +
               DataConstants.SIZE_INT +
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `memoryEstimate` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/LargeServerMessageImpl.java`
#### Snippet
```java
         }

         return memoryEstimate;
      }
   }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `started` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/AbstractJournalStorageManager.java`
#### Snippet
```java
   public synchronized boolean isStarted() {
      if (ioCriticalErrorListener != null) {
         return started && !ioCriticalErrorListener.isPreviouslyFailed();
      } else {
         return started;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `started` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/AbstractJournalStorageManager.java`
#### Snippet
```java
         return started && !ioCriticalErrorListener.isPreviouslyFailed();
      } else {
         return started;
      }
   }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `journalLoaded` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/AbstractJournalStorageManager.java`
#### Snippet
```java
   @Override
   public synchronized void persistIdGenerator() {
      if (journalLoaded && idGenerator != null) {
         // Must call close to make sure last id is persisted
         idGenerator.persistCurrentID();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `idGenerator` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/AbstractJournalStorageManager.java`
#### Snippet
```java
   @Override
   public synchronized void persistIdGenerator() {
      if (journalLoaded && idGenerator != null) {
         // Must call close to make sure last id is persisted
         idGenerator.persistCurrentID();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `idGenerator` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/AbstractJournalStorageManager.java`
#### Snippet
```java
      if (journalLoaded && idGenerator != null) {
         // Must call close to make sure last id is persisted
         idGenerator.persistCurrentID();
      }
   }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `started` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/AbstractJournalStorageManager.java`
#### Snippet
```java
   @Override
   public synchronized void start() throws Exception {
      if (started) {
         return;
      }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `singleThreadExecutor` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/AbstractJournalStorageManager.java`
#### Snippet
```java
      beforeStart();

      singleThreadExecutor = ioExecutorFactory.getExecutor();

      bindingsJournal.start();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `bindingsJournal` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/AbstractJournalStorageManager.java`
#### Snippet
```java
      singleThreadExecutor = ioExecutorFactory.getExecutor();

      bindingsJournal.start();


```

### PublicFieldAccessedInSynchronizedContext
Non-private field `messageJournal` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/AbstractJournalStorageManager.java`
#### Snippet
```java

      if (config.getJournalRetentionLocation() != null) {
         messageJournal.setHistoryFolder(config.getJournalRetentionLocation(), config.getJournalRetentionMaxBytes(), config.getJournalRetentionPeriod());
      }
      messageJournal.start();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `messageJournal` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/AbstractJournalStorageManager.java`
#### Snippet
```java
         messageJournal.setHistoryFolder(config.getJournalRetentionLocation(), config.getJournalRetentionMaxBytes(), config.getJournalRetentionPeriod());
      }
      messageJournal.start();

      started = true;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `started` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/AbstractJournalStorageManager.java`
#### Snippet
```java
      messageJournal.start();

      started = true;
   }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `started` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/AbstractJournalStorageManager.java`
#### Snippet
```java
   @Override
   public synchronized void stop(boolean ioCriticalError, boolean sendFailover) throws Exception {
      if (!started) {
         return;
      }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `journalLoaded` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/AbstractJournalStorageManager.java`
#### Snippet
```java
         performCachedLargeMessageDeletes();
         // Must call close to make sure last id is persisted
         if (journalLoaded && idGenerator != null)
            idGenerator.persistCurrentID();
      }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `idGenerator` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/AbstractJournalStorageManager.java`
#### Snippet
```java
         performCachedLargeMessageDeletes();
         // Must call close to make sure last id is persisted
         if (journalLoaded && idGenerator != null)
            idGenerator.persistCurrentID();
      }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `idGenerator` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/AbstractJournalStorageManager.java`
#### Snippet
```java
         // Must call close to make sure last id is persisted
         if (journalLoaded && idGenerator != null)
            idGenerator.persistCurrentID();
      }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `bindingsJournal` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/AbstractJournalStorageManager.java`
#### Snippet
```java
      beforeStop();

      bindingsJournal.stop();

      messageJournal.stop();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `messageJournal` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/AbstractJournalStorageManager.java`
#### Snippet
```java
      bindingsJournal.stop();

      messageJournal.stop();

      journalLoaded = false;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `journalLoaded` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/AbstractJournalStorageManager.java`
#### Snippet
```java
      messageJournal.stop();

      journalLoaded = false;

      started = false;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `started` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/AbstractJournalStorageManager.java`
#### Snippet
```java
      journalLoaded = false;

      started = false;
   }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `iter` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java
            logger.debug("Scanning for expires on {}", name);

            if (iter == null) {
               if (server.hasBrokerQueuePlugins()) {
                  try {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `iter` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java
                  }
               }
               iter = iterator();
            }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `iter` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java

            try {
               while (!queueDestroyed && postOffice.isStarted() && iter.hasNext()) {
                  hasElements = true;
                  MessageReference ref = iter.next();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `iter` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java
               while (!queueDestroyed && postOffice.isStarted() && iter.hasNext()) {
                  hasElements = true;
                  MessageReference ref = iter.next();
                  if (ref.getMessage().isExpired()) {
                     elementsExpired++;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `iter` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java
                     expired = true;
                     expiredMessages.add(ref);
                     iter.remove();
                  }
                  if (++elementsIterated >= MAX_DELIVERIES_IN_LOOP) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `iter` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java
                  }

                  iter.close();
                  iter = null;

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `iter` accessed in synchronized context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java

                  iter.close();
                  iter = null;

                  if (doneCallback != null) {
```

## RuleId[id=RedundantSuppression]
### RedundantSuppression
Redundant suppression
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ServerLocatorImpl.java`
#### Snippet
```java

   @SuppressWarnings("unchecked")
   private void updateArraysAndPairs(long time) {
      if (updateArrayActor == null) {
         // if for some reason we don't have an actor, just go straight
```

### RedundantSuppression
Redundant suppression
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/TransportConstants.java`
#### Snippet
```java
      allowableAcceptorKeys.add(TransportConstants.USE_KQUEUE_PROP_NAME);
      allowableAcceptorKeys.add(TransportConstants.USE_INVM_PROP_NAME);
      //noinspection deprecation
      allowableAcceptorKeys.add(TransportConstants.PROTOCOL_PROP_NAME);
      allowableAcceptorKeys.add(TransportConstants.PROTOCOLS_PROP_NAME);
```

### RedundantSuppression
Redundant suppression
in `artemis-jdbc-store/src/main/java/org/apache/activemq/artemis/jdbc/store/drivers/AbstractJDBCDriver.java`
#### Snippet
```java
 */
@SuppressWarnings("SynchronizeOnNonFinalField")
public abstract class AbstractJDBCDriver {

   private static final Logger logger = LoggerFactory.getLogger(MethodHandles.lookup().lookupClass());
```

### RedundantSuppression
Redundant suppression
in `artemis-jdbc-store/src/main/java/org/apache/activemq/artemis/jdbc/store/file/Db2SequentialFileDriver.java`
#### Snippet
```java

@SuppressWarnings("SynchronizeOnNonFinalField")
public final class Db2SequentialFileDriver extends JDBCSequentialFileFactoryDriver {

   private String replaceLargeObject;
```

### RedundantSuppression
Redundant suppression
in `artemis-jdbc-store/src/main/java/org/apache/activemq/artemis/jdbc/store/file/JDBCSequentialFileFactoryDriver.java`
#### Snippet
```java

@SuppressWarnings("SynchronizeOnNonFinalField")
public class JDBCSequentialFileFactoryDriver extends AbstractJDBCDriver {

   private static final Logger logger = LoggerFactory.getLogger(MethodHandles.lookup().lookupClass());
```

### RedundantSuppression
Redundant suppression
in `artemis-jdbc-store/src/main/java/org/apache/activemq/artemis/jdbc/store/file/PostgresSequentialSequentialFileDriver.java`
#### Snippet
```java

@SuppressWarnings("SynchronizeOnNonFinalField")
public final class PostgresSequentialSequentialFileDriver extends JDBCSequentialFileFactoryDriver {

   private static final String POSTGRES_OID_KEY = "POSTGRES_OID_KEY";
```

### RedundantSuppression
Redundant suppression
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/bridge/impl/JMSBridgeImpl.java`
#### Snippet
```java
      @SuppressWarnings("WaitNotInLoop")
      // both lock.wait(..) either returns, throws or continue, thus avoiding spurious wakes
      public void run() {
         final CountDownLatch finished = sourceReceiverFinished;
         try {
```

### RedundantSuppression
Redundant suppression
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/ActiveMQRAConnectionFactoryImpl.java`
#### Snippet
```java
   @Override
   public JMSContext createContext(String userName, String password, int sessionMode) {
      @SuppressWarnings("resource")
      ActiveMQRASessionFactoryImpl conn = new ActiveMQRASessionFactoryImpl(mcf, cm, getResourceAdapter().getTSR(), ActiveMQRAConnectionFactory.CONNECTION);
      conn.setUserName(userName);
      conn.setPassword(password);
```

### RedundantSuppression
Redundant suppression
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/FileBasedNodeManager.java`
#### Snippet
```java
      }

      @SuppressWarnings("resource")
      RandomAccessFile raFile = new RandomAccessFile(serverLockFile, ACCESS_MODE);

      channel = raFile.getChannel();
```

### RedundantSuppression
Redundant suppression
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/jdbc/JdbcSharedStateManager.java`
#### Snippet
```java
 */
@SuppressWarnings("SynchronizeOnNonFinalField")
final class JdbcSharedStateManager extends AbstractJDBCDriver implements SharedStateManager {

   private static final Logger logger = LoggerFactory.getLogger(MethodHandles.lookup().lookupClass());
```

### RedundantSuppression
Redundant suppression
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/security/scram/StringPrep.java`
#### Snippet
```java
   /** A prohibited character was detected. */
   @SuppressWarnings({"WeakerAccess", "JavaDoc"})
   public static class StringPrepProhibitedCharacter extends StringPrepError {
      /**
       *
```

### RedundantSuppression
Redundant suppression
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/security/scram/StringPrep.java`
#### Snippet
```java
   /** A prohibited unassigned codepoint was detected. */
   @SuppressWarnings("JavaDoc")
   public static class StringPrepUnassignedCodepoint extends StringPrepProhibitedCharacter {
      /**
       *
```

### RedundantSuppression
Redundant suppression
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/security/scram/StringPrep.java`
#### Snippet
```java
   /** RTL verification has failed, according to rfc3454 section 6. */
   @SuppressWarnings({"unused", "JavaDoc"})
   public static class StringPrepRTLError extends StringPrepError {
      /**
       *
```

## RuleId[id=UtilityClassWithPublicConstructor]
### UtilityClassWithPublicConstructor
Class `OpenWireMessageConverter` has only 'static' members, and a 'public' constructor
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenWireMessageConverter.java`
#### Snippet
```java
import static org.apache.activemq.command.ActiveMQDestination.QUEUE_TYPE;

public final class OpenWireMessageConverter {

   private static final Logger logger = LoggerFactory.getLogger(MethodHandles.lookup().lookupClass());
```

## RuleId[id=UnnecessaryInitCause]
### UnnecessaryInitCause
Unnecessary `Throwable.initCause()` call
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenWireProtocolManager.java`
#### Snippet
```java
         // We need to send an exception used by the openwire
         SecurityException ex = new SecurityException("User name [" + username + "] or password is invalid.");
         ex.initCause(e);
         throw ex;
      }
```

### UnnecessaryInitCause
Unnecessary `Throwable.initCause()` call
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalImpl.java`
#### Snippet
```java
      } else {
         IOException ioex = new IOException();
         ioex.initCause(e);
         throw ioex;
      }
```

### UnnecessaryInitCause
Unnecessary `Throwable.initCause()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/utils/InflaterWriter.java`
#### Snippet
```java
         } catch (DataFormatException e) {
            IOException io = new IOException(e.getMessage());
            io.initCause(e);
            throw io;
         }
```

### UnnecessaryInitCause
Unnecessary `Throwable.initCause()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/utils/InflaterWriter.java`
#### Snippet
```java
         } catch (DataFormatException e) {
            IOException ie = new IOException("Error decompressing data");
            ie.initCause(e);
            throw ie;
         }
```

## RuleId[id=OptionalUsedAsFieldOrParameterType]
### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'eventHandler'
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/client/ProtonClientConnectionManager.java`
#### Snippet
```java
   private static final Logger logger = LoggerFactory.getLogger(MethodHandles.lookup().lookupClass());
   private final AMQPClientConnectionFactory connectionFactory;
   private final Optional<EventHandler> eventHandler;
   private final ClientSASLFactory clientSASLFactory;

```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'eventHandler'
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/client/ProtonClientConnectionManager.java`
#### Snippet
```java
   private final ClientSASLFactory clientSASLFactory;

   public ProtonClientConnectionManager(AMQPClientConnectionFactory connectionFactory, Optional<EventHandler> eventHandler, ClientSASLFactory clientSASLFactory) {
      this.connectionFactory = connectionFactory;
      this.eventHandler = eventHandler;
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'eventHandler'
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/client/AMQPClientConnectionFactory.java`
#### Snippet
```java
   }

   public ActiveMQProtonRemotingConnection createConnection(ProtonProtocolManager protocolManager, Connection connection, Optional<EventHandler> eventHandler, ClientSASLFactory clientSASLFactory) {
      AMQPConnectionCallback connectionCallback = new AMQPConnectionCallback(protocolManager, connection, server.getExecutorFactory().getExecutor(), server);

```

## RuleId[id=CharsetObjectCanBeUsed]
### CharsetObjectCanBeUsed
StandardCharsets.UTF_8 can be used instead
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/StringPrintStream.java`
#### Snippet
```java
   public String toString() {
      try {
         return byteOuptut.toString(StandardCharsets.UTF_8.name());
      } catch (Throwable e) {
         throw new RuntimeException(e);
```

### CharsetObjectCanBeUsed
StandardCharsets.UTF_8 can be used instead
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/StringPrintStream.java`
#### Snippet
```java

   public PrintStream newStream() throws IOException {
      return new PrintStream(byteOuptut, true, StandardCharsets.UTF_8.name());
   }

```

### CharsetObjectCanBeUsed
StandardCharsets.UTF_8 can be used instead
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/uri/BeanSupport.java`
#### Snippet
```java

   public static String encodeURI(String value) throws UnsupportedEncodingException {
      return URLEncoder.encode(value, "UTF-8");
   }

```

### CharsetObjectCanBeUsed
StandardCharsets.UTF_8 can be used instead
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/uri/BeanSupport.java`
#### Snippet
```java

   public static String decodeURI(String value) throws UnsupportedEncodingException {
      return URLDecoder.decode(value, "UTF-8");
   }

```

### CharsetObjectCanBeUsed
StandardCharsets.UTF_8 can be used instead
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/uri/URISupport.java`
#### Snippet
```java
         int p = parameter.indexOf("=");
         if (p >= 0) {
            String name = URLDecoder.decode(parameter.substring(0, p), "UTF-8");
            String value = URLDecoder.decode(parameter.substring(p + 1), "UTF-8");
            rc.put(name, value);
```

### CharsetObjectCanBeUsed
StandardCharsets.UTF_8 can be used instead
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/uri/URISupport.java`
#### Snippet
```java
         if (p >= 0) {
            String name = URLDecoder.decode(parameter.substring(0, p), "UTF-8");
            String value = URLDecoder.decode(parameter.substring(p + 1), "UTF-8");
            rc.put(name, value);
         } else {
```

### CharsetObjectCanBeUsed
StandardCharsets.UTF_8 can be used instead
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/uri/URISupport.java`
#### Snippet
```java
               }
               String value = (String) options.get(key);
               rc.append(URLEncoder.encode(key, "UTF-8"));
               rc.append("=");
               rc.append(URLEncoder.encode(value, "UTF-8"));
```

### CharsetObjectCanBeUsed
StandardCharsets.UTF_8 can be used instead
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/uri/URISupport.java`
#### Snippet
```java
               rc.append(URLEncoder.encode(key, "UTF-8"));
               rc.append("=");
               rc.append(URLEncoder.encode(value, "UTF-8"));
            }
            return rc.toString();
```

### CharsetObjectCanBeUsed
StandardCharsets.US_ASCII can be used instead
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/sasl/scram/ScramClientFunctionalityImpl.java`
#### Snippet
```java

   private static final String GS2_HEADER = "n,,";
   private static final Charset ASCII = Charset.forName("ASCII");

   private final String mDigestName;
```

### CharsetObjectCanBeUsed
StandardCharsets.UTF_8 can be used instead
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/util/NettyReadable.java`
#### Snippet
```java
public class NettyReadable implements ReadableBuffer {

   private static final Charset Charset_UTF8 = Charset.forName("UTF-8");

   private final ByteBuf buffer;
```

## RuleId[id=SystemOutErr]
### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-maven-plugin/src/main/java/org/apache/activemq/artemis/maven/ArtemisDependencyDocPlugin.java`
#### Snippet
```java
               content = replace(content, entry.getKey(), entry.getValue());
            } catch (Throwable e) {
               System.out.println("Error on " + entry.getKey());
               e.printStackTrace();
            }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-log-annotation-processor/src/main/java/org/apache/activemq/artemis/logs/annotation/processor/LogAnnotationProcessor.java`
#### Snippet
```java
   protected static void debug(String debugMessage) {
      if (DEBUG) {
         System.out.println(debugMessage);
      }
   }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/SpawnedVMSupport.java`
#### Snippet
```java
               }
               if (print) {
                  System.out.println(className + ":" + line);
               }
            }
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/DefaultSensitiveStringCodec.java`
#### Snippet
```java
   public static void main(String[] args) throws Exception {
      if (args.length != 1) {
         System.err.println("Use: java -cp <classPath> org.apache.activemq.artemis.utils.DefaultSensitiveStringCodec password-to-encode");
         System.err.println("Error: no password on the args");
         System.exit(-1);
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/DefaultSensitiveStringCodec.java`
#### Snippet
```java
      if (args.length != 1) {
         System.err.println("Use: java -cp <classPath> org.apache.activemq.artemis.utils.DefaultSensitiveStringCodec password-to-encode");
         System.err.println("Error: no password on the args");
         System.exit(-1);
      }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/DefaultSensitiveStringCodec.java`
#### Snippet
```java
      Object encode = codec.encode(args[0]);

      System.out.println("Encoded password (without quotes): \"" + encode + "\"");
   }

```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/ClassloadingUtil.java`
#### Snippet
```java
      } catch (Throwable t) {
         if (t instanceof InstantiationException) {
            System.out.println(INSTANTIATION_EXCEPTION_MESSAGE);
         }
         loader = Thread.currentThread().getContextClassLoader();
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/ClassloadingUtil.java`
#### Snippet
```java
      } catch (Throwable t) {
         if (t instanceof InstantiationException) {
            System.out.println(INSTANTIATION_EXCEPTION_MESSAGE);
         }
         loader = Thread.currentThread().getContextClassLoader();
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
    */
   private static void usage(final String msg) {
      System.err.println(msg);
      System.err.println("Usage: java Base64 -e|-d inputfile outputfile");
   } // end usage
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
   private static void usage(final String msg) {
      System.err.println(msg);
      System.err.println("Usage: java Base64 -e|-d inputfile outputfile");
   } // end usage

```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
         // Check for size of file
         if (file.length() > Integer.MAX_VALUE) {
            System.err.println("File is too big for this convenience method (" + file.length() + " bytes).");
            return null;
         } // end if: file too big for int index
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java

      } catch (java.io.IOException e) {
         System.err.println("Error decoding from file " + filename);
      } finally {
         try {
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java

      } catch (java.io.IOException e) {
         System.err.println("Error encoding from file " + filename);
      } finally {
         try {
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
            return 3;
         } catch (Exception e) {
            System.out.println("" + source[srcOffset] + ": " + DECODABET[source[srcOffset]]);
            System.out.println("" + source[srcOffset + 1] + ": " + DECODABET[source[srcOffset + 1]]);
            System.out.println("" + source[srcOffset + 2] + ": " + DECODABET[source[srcOffset + 2]]);
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
         } catch (Exception e) {
            System.out.println("" + source[srcOffset] + ": " + DECODABET[source[srcOffset]]);
            System.out.println("" + source[srcOffset + 1] + ": " + DECODABET[source[srcOffset + 1]]);
            System.out.println("" + source[srcOffset + 2] + ": " + DECODABET[source[srcOffset + 2]]);
            System.out.println("" + source[srcOffset + 3] + ": " + DECODABET[source[srcOffset + 3]]);
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
            System.out.println("" + source[srcOffset] + ": " + DECODABET[source[srcOffset]]);
            System.out.println("" + source[srcOffset + 1] + ": " + DECODABET[source[srcOffset + 1]]);
            System.out.println("" + source[srcOffset + 2] + ": " + DECODABET[source[srcOffset + 2]]);
            System.out.println("" + source[srcOffset + 3] + ": " + DECODABET[source[srcOffset + 3]]);
            return -1;
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
            System.out.println("" + source[srcOffset + 1] + ": " + DECODABET[source[srcOffset + 1]]);
            System.out.println("" + source[srcOffset + 2] + ": " + DECODABET[source[srcOffset + 2]]);
            System.out.println("" + source[srcOffset + 3] + ": " + DECODABET[source[srcOffset + 3]]);
            return -1;
         } // end catch
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java

         } else {
            System.err.println("Bad Base64 input character at " + i + ": " + source[i] + "(decimal)");
            return null;
         } // end else:
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/io/nio/NIOSequentialFile.java`
#### Snippet
```java
   public static void printDebug() {
      for (Map.Entry<String, AtomicInteger> entry : counters.entrySet()) {
         System.out.println(entry.getValue() + " " + entry.getKey());
      }
   }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/ActionContext.java`
#### Snippet
```java

   public ActionContext() {
      this(System.in, System.out, System.err);
   }

```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/ActionContext.java`
#### Snippet
```java

   public ActionContext() {
      this(System.in, System.out, System.err);
   }

```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/process/ProcessBuilder.java`
#### Snippet
```java
               if (print) {
                  if (sendToErr) {
                     System.err.println(logName + "-err:" + line);
                  } else {
                     System.out.println(logName + "-out:" + line);
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/process/ProcessBuilder.java`
#### Snippet
```java
                     System.err.println(logName + "-err:" + line);
                  } else {
                     System.out.println(logName + "-out:" + line);
                  }
               }
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Run.java`
#### Snippet
```java
            if (allowKill && fileKill.exists()) {
               try {
                  System.err.println("Halting by user request");
                  fileKill.delete();
               } catch (Throwable ignored) {
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Run.java`
#### Snippet
```java

      if (oldLogging.exists() && !newLogging.exists()) {
         System.out.println("******************************************************************************************************************************************************************************");
         System.out.println("Your system has the older logging file " + Upgrade.OLD_LOGGING_PROPERTIES + ", but not the new " + Create.ETC_LOG4J2_PROPERTIES);
         System.out.println("It appears you did not complete the migration on this artemis instance properly. Please check all the settings or run the './artemis upgrade' command from the new artemis home");
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Run.java`
#### Snippet
```java
      if (oldLogging.exists() && !newLogging.exists()) {
         System.out.println("******************************************************************************************************************************************************************************");
         System.out.println("Your system has the older logging file " + Upgrade.OLD_LOGGING_PROPERTIES + ", but not the new " + Create.ETC_LOG4J2_PROPERTIES);
         System.out.println("It appears you did not complete the migration on this artemis instance properly. Please check all the settings or run the './artemis upgrade' command from the new artemis home");
         System.out.println("******************************************************************************************************************************************************************************");
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Run.java`
#### Snippet
```java
         System.out.println("******************************************************************************************************************************************************************************");
         System.out.println("Your system has the older logging file " + Upgrade.OLD_LOGGING_PROPERTIES + ", but not the new " + Create.ETC_LOG4J2_PROPERTIES);
         System.out.println("It appears you did not complete the migration on this artemis instance properly. Please check all the settings or run the './artemis upgrade' command from the new artemis home");
         System.out.println("******************************************************************************************************************************************************************************");
      }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Run.java`
#### Snippet
```java
         System.out.println("Your system has the older logging file " + Upgrade.OLD_LOGGING_PROPERTIES + ", but not the new " + Create.ETC_LOG4J2_PROPERTIES);
         System.out.println("It appears you did not complete the migration on this artemis instance properly. Please check all the settings or run the './artemis upgrade' command from the new artemis home");
         System.out.println("******************************************************************************************************************************************************************************");
      }
   }
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Configurable.java`
#### Snippet
```java
   protected void treatError(Exception e, String group, String command) {
      logger.debug(e.getMessage(), e);
      System.err.println();
      System.err.println("Error:" + e.getMessage());
      System.err.println();
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Configurable.java`
#### Snippet
```java
      logger.debug(e.getMessage(), e);
      System.err.println();
      System.err.println("Error:" + e.getMessage());
      System.err.println();

```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Configurable.java`
#### Snippet
```java
      System.err.println();
      System.err.println("Error:" + e.getMessage());
      System.err.println();

      if (!(e instanceof ActiveMQException)) {
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/Artemis.java`
#### Snippet
```java
         context = new ActionContext();
      } else {
         context = new ActionContext(InputStream.nullInputStream(), new PrintStream(OutputStream.nullOutputStream()), System.err);
      }

```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/util/SyncCalculation.java`
#### Snippet
```java

      if (journalType == JournalType.ASYNCIO && syncWrites) {
         System.out.println();
         System.out.println("*******************************************************************************************");
         System.out.println("*** Notice: The recommendation for AsyncIO journal is to not use --sync-writes          ***");
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/util/SyncCalculation.java`
#### Snippet
```java
      if (journalType == JournalType.ASYNCIO && syncWrites) {
         System.out.println();
         System.out.println("*******************************************************************************************");
         System.out.println("*** Notice: The recommendation for AsyncIO journal is to not use --sync-writes          ***");
         System.out.println("***         The measures here will be useful to understand your device                  ***");
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/util/SyncCalculation.java`
#### Snippet
```java
         System.out.println();
         System.out.println("*******************************************************************************************");
         System.out.println("*** Notice: The recommendation for AsyncIO journal is to not use --sync-writes          ***");
         System.out.println("***         The measures here will be useful to understand your device                  ***");
         System.out.println("***         however the result here won't represent the best configuration option       ***");
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/util/SyncCalculation.java`
#### Snippet
```java
         System.out.println("*******************************************************************************************");
         System.out.println("*** Notice: The recommendation for AsyncIO journal is to not use --sync-writes          ***");
         System.out.println("***         The measures here will be useful to understand your device                  ***");
         System.out.println("***         however the result here won't represent the best configuration option       ***");
         System.out.println("*******************************************************************************************");
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/util/SyncCalculation.java`
#### Snippet
```java
         System.out.println("*** Notice: The recommendation for AsyncIO journal is to not use --sync-writes          ***");
         System.out.println("***         The measures here will be useful to understand your device                  ***");
         System.out.println("***         however the result here won't represent the best configuration option       ***");
         System.out.println("*******************************************************************************************");
         System.out.println();
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/util/SyncCalculation.java`
#### Snippet
```java
         System.out.println("***         The measures here will be useful to understand your device                  ***");
         System.out.println("***         however the result here won't represent the best configuration option       ***");
         System.out.println("*******************************************************************************************");
         System.out.println();
      }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/util/SyncCalculation.java`
#### Snippet
```java
         System.out.println("***         however the result here won't represent the best configuration option       ***");
         System.out.println("*******************************************************************************************");
         System.out.println();
      }

```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/util/SyncCalculation.java`
#### Snippet
```java

      if (verbose) {
         System.out.println("Using " + factory.getClass().getName() + " to calculate sync times, alignment=" + factory.getAlignment());
      }
      SequentialFile file = factory.createSequentialFile(fileName);
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/util/SyncCalculation.java`
#### Snippet
```java

            if (verbose) {
               System.out.println("**************************************************");
               System.out.println(ntry + " of " + tries + " calculation");
            }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/util/SyncCalculation.java`
#### Snippet
```java
            if (verbose) {
               System.out.println("**************************************************");
               System.out.println(ntry + " of " + tries + " calculation");
            }
            file.open();
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/util/SyncCalculation.java`
#### Snippet
```java
            if (verbose) {
               double writesPerMillisecond = (double) blocks / (double) result[ntry];
               System.out.println("Time = " + result[ntry] + " milliseconds");
               System.out.println("Writes / millisecond = " + dcformat.format(writesPerMillisecond));
               System.out.println("bufferTimeout = " + toNanos(result[ntry], blocks, verbose));
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/util/SyncCalculation.java`
#### Snippet
```java
               double writesPerMillisecond = (double) blocks / (double) result[ntry];
               System.out.println("Time = " + result[ntry] + " milliseconds");
               System.out.println("Writes / millisecond = " + dcformat.format(writesPerMillisecond));
               System.out.println("bufferTimeout = " + toNanos(result[ntry], blocks, verbose));
               System.out.println("**************************************************");
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/util/SyncCalculation.java`
#### Snippet
```java
               System.out.println("Time = " + result[ntry] + " milliseconds");
               System.out.println("Writes / millisecond = " + dcformat.format(writesPerMillisecond));
               System.out.println("bufferTimeout = " + toNanos(result[ntry], blocks, verbose));
               System.out.println("**************************************************");
            }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/util/SyncCalculation.java`
#### Snippet
```java
               System.out.println("Writes / millisecond = " + dcformat.format(writesPerMillisecond));
               System.out.println("bufferTimeout = " + toNanos(result[ntry], blocks, verbose));
               System.out.println("**************************************************");
            }
            file.close();
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/util/SyncCalculation.java`
#### Snippet
```java

      if (verbose) {
         System.out.println("Blocks per millisecond::" + blocksPerMillisecond);
      }

```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/util/SyncCalculation.java`
#### Snippet
```java

      if (verbose) {
         System.out.println("your system could make a sync every " + timeWait + " nanoseconds, and this will be your timeout");
      }

```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/LockAbstract.java`
#### Snippet
```java

      if (getBrokerInstance() == null) {
         System.err.println("Warning: You are running a data tool outside of any broker instance. Modifying data on a running server might break the server's data");
         System.err.println();
      } else {
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/LockAbstract.java`
#### Snippet
```java
      if (getBrokerInstance() == null) {
         System.err.println("Warning: You are running a data tool outside of any broker instance. Modifying data on a running server might break the server's data");
         System.err.println();
      } else {
         lockCLI(getLockPlace());
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/journal/EncodeJournal.java`
#### Snippet
```java
                                     final int fileSize) throws Exception {

      exportJournal(directory, journalPrefix, journalSuffix, minFiles, fileSize, System.out);
   }

```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/journal/CompactJournal.java`
#### Snippet
```java
                     configuration.getJournalPoolFiles(), configuration.getJournalFileSize(), null, JournalRecordIds.UPDATE_DELIVERY_COUNT,
                     JournalRecordIds.SET_SCHEDULED_DELIVERY_TIME);
      System.out.println("Compactation succeeded for " + configuration.getJournalLocation().getAbsolutePath());
      compactJournal(configuration.getBindingsLocation(), null, "activemq-bindings", "bindings", 2, 2, 1048576, null);
      System.out.println("Compactation succeeded for " + configuration.getBindingsLocation());
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/journal/CompactJournal.java`
#### Snippet
```java
      System.out.println("Compactation succeeded for " + configuration.getJournalLocation().getAbsolutePath());
      compactJournal(configuration.getBindingsLocation(), null, "activemq-bindings", "bindings", 2, 2, 1048576, null);
      System.out.println("Compactation succeeded for " + configuration.getBindingsLocation());
   }

```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/journal/PerfJournal.java`
#### Snippet
```java
      }

      System.out.println("");
      System.out.println("Auto tuning journal ...");

```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/journal/PerfJournal.java`
#### Snippet
```java

      System.out.println("");
      System.out.println("Auto tuning journal ...");

      System.out.println("Performing " + tries + " tests writing " + writes + " blocks of " + size + " on each test, sync=" + fileConfiguration.isJournalDatasync() + " with journalType = " + fileConfiguration.getJournalType());
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/journal/PerfJournal.java`
#### Snippet
```java
      System.out.println("Auto tuning journal ...");

      System.out.println("Performing " + tries + " tests writing " + writes + " blocks of " + size + " on each test, sync=" + fileConfiguration.isJournalDatasync() + " with journalType = " + fileConfiguration.getJournalType());

      fileConfiguration.getJournalLocation().mkdirs();
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/journal/DecodeJournal.java`
#### Snippet
```java
      if (!journalDir.exists()) {
         if (!journalDir.mkdirs())
            System.err.println("Could not create directory " + directory);
      }

```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/journal/DecodeJournal.java`
#### Snippet
```java
                  journal.appendPrepareRecord(txID, data, false);
               } else {
                  System.err.println("Transaction " + txID +
                                        " at line " +
                                        lineNumber +
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/journal/DecodeJournal.java`
#### Snippet
```java
                  journal.appendCommitRecord(txID, false);
               } else {
                  System.err.println("Transaction " + txID +
                                        " at line " +
                                        lineNumber +
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/journal/DecodeJournal.java`
#### Snippet
```java
               journal.appendRollbackRecord(txID, false);
            } else {
               System.err.println("Invalid operation " + operation + " at line " + lineNumber);
            }
         } catch (Exception ex) {
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/journal/DecodeJournal.java`
#### Snippet
```java
            }
         } catch (Exception ex) {
            System.err.println("Error at line " + lineNumber + ", operation=" + operation + " msg = " + ex.getMessage());
         }
      }
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Create.java`
#### Snippet
```java
            tmpFile.delete();
            if (!supportsLibaio) {
               System.err.println("The filesystem used on " + directory + " doesn't support libAIO and O_DIRECT files, switching journal-type to NIO");
            }
            return supportsLibaio;
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Create.java`
#### Snippet
```java
         } catch (Exception e) {
            e.printStackTrace();
            System.err.println("Invalid routing type: " + routingType);
         }
         printWriter.println("         <address name=\"" + name + "\">");
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Create.java`
#### Snippet
```java
            filters.put("${page-sync.settings}", "");
            e.printStackTrace();
            System.err.println("Couldn't perform sync calculation, using default values");
         }
      }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/PrintData.java`
#### Snippet
```java

   public static void printData(File bindingsDirectory, File messagesDirectory, File pagingDirectory, boolean secret) throws Exception {
      printData(bindingsDirectory, messagesDirectory, pagingDirectory, System.out, secret, false, false, false, -1);
   }

```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/PrintData.java`
#### Snippet
```java

            if (!cursorInfo.getCompletePages(queueID).add(pageNR)) {
               System.err.println("Page " + pageNR + " has been already set as complete on queue " + queueID);
            }
         } else if (record.userRecordType == JournalRecordIds.PAGE_TRANSACTION) {
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/DestAbstract.java`
#### Snippet
```java
            return (MessageSerializer) Class.forName(serializer).getConstructor().newInstance();
         } catch (Exception e) {
            System.err.println("Error: unable to instantiate serializer class: " + serializer);
            System.err.println("Defaulting to: " + XMLMessageSerializer.class.getName());
         }
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/DestAbstract.java`
#### Snippet
```java
         } catch (Exception e) {
            System.err.println("Error: unable to instantiate serializer class: " + serializer);
            System.err.println("Defaulting to: " + XMLMessageSerializer.class.getName());
         }
      }
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/DestAbstract.java`
#### Snippet
```java

      if (!protocol.equalsIgnoreCase("CORE")) {
         System.err.println("Default Serializer does not support: " + protocol + " protocol");
         return null;
      }
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/Consumer.java`
#### Snippet
```java
         serializer = getMessageSerializer();
         if (serializer == null) {
            System.err.println("Error. Unable to instantiate serializer class: " + this.serializer);
            return null;
         }
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/Consumer.java`
#### Snippet
```java
            out = new FileOutputStream(file);
         } catch (Exception e) {
            System.err.println("Error: Unable to open file for writing\n" + e.getMessage());
            return null;
         }
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/Producer.java`
#### Snippet
```java
               MessageSerializer serializer = getMessageSerializer();
               if (serializer == null) {
                  System.err.println("Error. Unable to instantiate serializer class: " + serializer);
                  return null;
               }
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/Producer.java`
#### Snippet
```java
                  in = new FileInputStream(file);
               } catch (Exception e) {
                  System.err.println("Error: Unable to open file for reading\n" + e.getMessage());
                  return null;
               }
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/Producer.java`
#### Snippet
```java
               serializer.stop();
            } catch (Exception e) {
               System.err.println("Error occurred during import.  Rolling back.");
               session.rollback();
               e.printStackTrace();
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/Producer.java`
#### Snippet
```java
               return 0;
            }
            System.out.println("Sent " + messageCount + " Messages.");
            return messageCount;
         } else {
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/ProducerThread.java`
#### Snippet
```java
         running = true;

         System.out.println(threadName + " Started to calculate elapsed time ...\n");
         long tStart = System.currentTimeMillis();

```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/ProducerThread.java`
#### Snippet
```java
         }

         System.out.println(threadName + " Produced: " + this.getSentCount() + " messages");
         long tEnd = System.currentTimeMillis();
         long elapsed = (tEnd - tStart) / 1000;
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/ProducerThread.java`
#### Snippet
```java
         long tEnd = System.currentTimeMillis();
         long elapsed = (tEnd - tStart) / 1000;
         System.out.println(threadName + " Elapsed time in second : " + elapsed + " s");
         System.out.println(threadName + " Elapsed time in milli second : " + (tEnd - tStart) + " milli seconds");

```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/ProducerThread.java`
#### Snippet
```java
         long elapsed = (tEnd - tStart) / 1000;
         System.out.println(threadName + " Elapsed time in second : " + elapsed + " s");
         System.out.println(threadName + " Elapsed time in milli second : " + (tEnd - tStart) + " milli seconds");

      } catch (Exception e) {
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/ProducerThread.java`
#### Snippet
```java
      producer.send(message);
      if (verbose) {
         System.out.println(threadName + " Sent: " + (message instanceof TextMessage ? ((TextMessage) message).getText() : message.getJMSMessageID()));
      }

```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/ProducerThread.java`
#### Snippet
```java

      if (transactionBatchSize > 0 && sentCount.get() > 0 && sentCount.get() % transactionBatchSize == 0) {
         System.out.println(threadName + " Committing transaction: " + transactions++);
         session.commit();
      }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/Transfer.java`
#### Snippet
```java
      if (protocol.equals("core")) {
         if (isVerbose()) {
            System.out.println("Creating " + role + " CORE Connection towards " + brokerURL);
         }
         return createCoreConnectionFactory(brokerURL, user, password, clientID);
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/Transfer.java`
#### Snippet
```java
      } else if (protocol.equals("amqp")) {
         if (isVerbose()) {
            System.out.println("Creating " + role + " AMQP Connection towards " + brokerURL);
         }
         return createAMQPConnectionFactory(brokerURL, user, password, clientID);
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/Transfer.java`
#### Snippet
```java
      }

      System.out.println("Connection brokerURL = " + sourceURL);

      ConnectionFactory sourceConnectionFactory = createConnectionFactory("source", sourceProtocol, sourceURL, sourceUser, sourcePassword, sourceClientID);
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/Transfer.java`
#### Snippet
```java

      if (sourceURL.equals(targetURL) && sourceDestination.equals(targetDestination)) {
         System.out.println("You cannot transfer between " + sourceURL + "/" + sourceDestination + " and " + targetURL + "/" + targetDestination + ".\n" + "That would create an infinite recursion.");
         throw new IllegalArgumentException("cannot use " + sourceDestination + " == " + targetDestination);
      }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/Transfer.java`
#### Snippet
```java
         if (receivedMessage == null) {
            if (isVerbose()) {
               System.out.println("could not receive any more messages");
            }
            break;
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/Transfer.java`
#### Snippet
```java

         if (isVerbose()) {
            System.out.println("Received message " + total + " with " + pending + " messages pending to be commited");
         }
         if (pending > commitInterval) {
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/Transfer.java`
#### Snippet
```java
         }
         if (pending > commitInterval) {
            System.out.println("Transferred " + pending + " messages of " + total);
            pending = 0;
            targetSession.commit();
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/Transfer.java`
#### Snippet
```java
      }

      System.out.println("Transferred a total of " + total + " messages");

      if (pending != 0) {
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/Transfer.java`
#### Snippet
```java

   Pair<String, String> userPassword(String uri) {
      System.out.println("Type in user/password towards " + uri);
      String user, password;
      user = input("--user", "Type the username for a retry", null);
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/Transfer.java`
#### Snippet
```java

      if (clientID != null) {
         System.out.println("Consumer:: clientID = " + clientID);
         cf.setClientID(clientID);
      }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/ConsumerThread.java`
#### Snippet
```java
      QueueBrowser consumer = null;
      String threadName = Thread.currentThread().getName();
      System.out.println(threadName + " trying to browse " + messageCount + " messages");
      try {
         if (filter != null) {
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/ConsumerThread.java`
#### Snippet
```java
            Message msg = enumBrowse.nextElement();
            if (msg != null) {
               System.out.println(threadName + " browsing " + (msg instanceof TextMessage ? ((TextMessage) msg).getText() : msg.getJMSMessageID()));
               handle(msg, true);
               received++;
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/ConsumerThread.java`
#### Snippet
```java
         }
         if (consumer != null) {
            System.out.println(threadName + " browsed: " + this.getReceived() + " messages");
            try {
               consumer.close();
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/ConsumerThread.java`
#### Snippet
```java
      }

      System.out.println(threadName + " Browser thread finished");
   }

```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/ConsumerThread.java`
#### Snippet
```java
      MessageConsumer consumer = null;
      String threadName = Thread.currentThread().getName();
      System.out.println(threadName + " wait until " + messageCount + " messages are consumed");
      try {
         if (durable && destination instanceof Topic) {
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/ConsumerThread.java`
#### Snippet
```java
            if (msg != null) {
               if (verbose) {
                  System.out.println(threadName + " Received " + (msg instanceof TextMessage ? ((TextMessage) msg).getText() : msg.getJMSMessageID()));
               } else {
                  if (++count % 1000 == 0) {
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/ConsumerThread.java`
#### Snippet
```java
               } else {
                  if (++count % 1000 == 0) {
                     System.out.println("Received " + count);
                  }
               }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/ConsumerThread.java`
#### Snippet
```java
            if (session.getTransacted()) {
               if (batchSize > 0 && received > 0 && received % batchSize == 0) {
                  System.out.println(threadName + " Committing transaction: " + transactions++);
                  session.commit();
               }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/ConsumerThread.java`
#### Snippet
```java
            } else if (session.getAcknowledgeMode() == Session.CLIENT_ACKNOWLEDGE && msg != null) {
               if (batchSize > 0 && received > 0 && received % batchSize == 0) {
                  System.out.println("Acknowledging last " + batchSize + " messages; messages so far = " + received);
                  msg.acknowledge();
               }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/ConsumerThread.java`
#### Snippet
```java


         System.out.println(threadName + " Consumed: " + this.getMessageCount() + " messages");
         long tEnd = System.currentTimeMillis();
         long elapsed = (tEnd - tStart) / 1000;
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/ConsumerThread.java`
#### Snippet
```java
         long tEnd = System.currentTimeMillis();
         long elapsed = (tEnd - tStart) / 1000;
         System.out.println(threadName + " Elapsed time in second : " + elapsed + " s");
         System.out.println(threadName + " Elapsed time in milli second : " + (tEnd - tStart) + " milli seconds");

```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/ConsumerThread.java`
#### Snippet
```java
         long elapsed = (tEnd - tStart) / 1000;
         System.out.println(threadName + " Elapsed time in second : " + elapsed + " s");
         System.out.println(threadName + " Elapsed time in milli second : " + (tEnd - tStart) + " milli seconds");

      } catch (Exception e) {
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/ConsumerThread.java`
#### Snippet
```java
         }
         if (consumer != null) {
            System.out.println(threadName + " Consumed: " + this.getReceived() + " messages");
            try {
               consumer.close();
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/ConsumerThread.java`
#### Snippet
```java
      }

      System.out.println(threadName + " Consumer thread finished");
   }

```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/ConsumerThread.java`
#### Snippet
```java
         if (browse) {
            if (verbose) {
               System.out.println("..." + msg);
            }
            if (bytesAsText && (msg instanceof BytesMessage)) {
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/ConsumerThread.java`
#### Snippet
```java
               byte[] bytes = new byte[(int) length];
               ((BytesMessage) msg).readBytes(bytes);
               System.out.println("Message:" + msg);
            }
         } else {
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/ConsumerThread.java`
#### Snippet
```java
         } else {
            if (verbose) {
               System.out.println("JMS Message ID:" + msg.getJMSMessageID());
               if (bytesAsText && (msg instanceof BytesMessage)) {
                  long length = ((BytesMessage) msg).getBodyLength();
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/ConsumerThread.java`
#### Snippet
```java
                  byte[] bytes = new byte[(int) length];
                  ((BytesMessage) msg).readBytes(bytes);
                  System.out.println("Received a message with " + bytes.length);
               }

```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/ConsumerThread.java`
#### Snippet
```java
               if (msg instanceof TextMessage) {
                  String text = ((TextMessage) msg).getText();
                  System.out.println("Received text sized at " + text.length());
               }

```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/ConsumerThread.java`
#### Snippet
```java
               if (msg instanceof ObjectMessage) {
                  Object obj = ((ObjectMessage) msg).getObject();
                  System.out.println("Received object " + obj.toString().length());
               }
            }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/util/ServerUtil.java`
#### Snippet
```java
         try (ActiveMQConnectionFactory cf = ActiveMQJMSClient.createConnectionFactory(uri, null);
              Connection c = cf.createConnection(username, password)) {
            System.out.println("server " + uri + " started");
         } catch (Exception e) {
            System.out.println("awaiting server " + uri + " start at ");
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/util/ServerUtil.java`
#### Snippet
```java
            System.out.println("server " + uri + " started");
         } catch (Exception e) {
            System.out.println("awaiting server " + uri + " start at ");
            Thread.sleep(500);
            continue;
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/util/ServerUtil.java`
#### Snippet
```java
               if (print) {
                  if (sendToErr) {
                     System.err.println(logName + "-err:" + line);
                  } else {
                     System.out.println(logName + "-out:" + line);
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/util/ServerUtil.java`
#### Snippet
```java
                     System.err.println(logName + "-err:" + line);
                  } else {
                     System.out.println(logName + "-out:" + line);
                  }
               }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/util/ServerUtil.java`
#### Snippet
```java
   public static void killServer(final Process server, boolean forcibly) throws Exception {
      if (server != null) {
         System.out.println("**********************************");
         System.out.println("Killing server " + server);
         System.out.println("**********************************");
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/util/ServerUtil.java`
#### Snippet
```java
      if (server != null) {
         System.out.println("**********************************");
         System.out.println("Killing server " + server);
         System.out.println("**********************************");
         if (forcibly) {
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/util/ServerUtil.java`
#### Snippet
```java
         System.out.println("**********************************");
         System.out.println("Killing server " + server);
         System.out.println("**********************************");
         if (forcibly) {
            server.destroyForcibly();
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-unit-test-support/src/main/java/org/apache/activemq/artemis/utils/Wait.java`
#### Snippet
```java
      try {
         Object threadDump = tdUtilMethod.invoke(null, msg);
         System.out.println(threadDump);
      } catch (Exception e) {
         throw new RuntimeException("Failure running ThreadDumpUtil", e);
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-unit-test-support/src/main/java/org/apache/activemq/artemis/utils/SilentTestCase.java`
#### Snippet
```java
   @Before
   public void setUp() throws Exception {
      origSysOut = System.out;
      origSysErr = System.err;
      sysOut = new PrintStream(new ByteArrayOutputStream());
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `artemis-unit-test-support/src/main/java/org/apache/activemq/artemis/utils/SilentTestCase.java`
#### Snippet
```java
   public void setUp() throws Exception {
      origSysOut = System.out;
      origSysErr = System.err;
      sysOut = new PrintStream(new ByteArrayOutputStream());
      System.setOut(sysOut);
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-unit-test-support/src/main/java/org/apache/activemq/artemis/utils/CleanupSystemPropertiesRule.java`
#### Snippet
```java
      for (Map.Entry<Object, Object> entry : originalProperties.entrySet()) {
         if (System.getProperty((String) entry.getKey()) == null) {
            System.out.println("======================================================================================================");
            System.out.println("Reinstating property " + entry.getKey() + "=" + entry.getValue());
            System.setProperty((String) entry.getKey(), (String) entry.getValue());
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-unit-test-support/src/main/java/org/apache/activemq/artemis/utils/CleanupSystemPropertiesRule.java`
#### Snippet
```java
         if (System.getProperty((String) entry.getKey()) == null) {
            System.out.println("======================================================================================================");
            System.out.println("Reinstating property " + entry.getKey() + "=" + entry.getValue());
            System.setProperty((String) entry.getKey(), (String) entry.getValue());
            System.out.println("======================================================================================================");
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-unit-test-support/src/main/java/org/apache/activemq/artemis/utils/CleanupSystemPropertiesRule.java`
#### Snippet
```java
            System.out.println("Reinstating property " + entry.getKey() + "=" + entry.getValue());
            System.setProperty((String) entry.getKey(), (String) entry.getValue());
            System.out.println("======================================================================================================");
         }

```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-unit-test-support/src/main/java/org/apache/activemq/artemis/utils/CleanupSystemPropertiesRule.java`
#### Snippet
```java
      if (!newProperties.isEmpty() || !changed.isEmpty()) {

         System.out.println("======================================================================================================");

         if (!newProperties.isEmpty()) {
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-unit-test-support/src/main/java/org/apache/activemq/artemis/utils/CleanupSystemPropertiesRule.java`
#### Snippet
```java

         if (!newProperties.isEmpty()) {
            System.out.println("Clearing system property...");

            int i = 1;
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-unit-test-support/src/main/java/org/apache/activemq/artemis/utils/CleanupSystemPropertiesRule.java`
#### Snippet
```java
            int i = 1;
            for (Object key : newProperties) {
               System.out.printf("\t%3d. %s = %s%n", i++, key, System.getProperty(key.toString()));
               System.clearProperty(key.toString());
            }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-unit-test-support/src/main/java/org/apache/activemq/artemis/utils/CleanupSystemPropertiesRule.java`
#### Snippet
```java

         if (!changed.isEmpty()) {
            System.out.println("Resetting system property...");

            int i = 1;
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-unit-test-support/src/main/java/org/apache/activemq/artemis/utils/CleanupSystemPropertiesRule.java`
#### Snippet
```java
            int i = 1;
            for (Map.Entry<Object, Object> entry : changed.entrySet()) {
               System.out.printf("\t%3d. %s = %s (was %s)%n", i++, entry.getKey(), entry.getValue(), System.getProperty(entry.getKey().toString()));
               System.setProperty(entry.getKey().toString(), entry.getValue().toString());
            }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-unit-test-support/src/main/java/org/apache/activemq/artemis/utils/CleanupSystemPropertiesRule.java`
#### Snippet
```java
         }

         System.out.println("======================================================================================================");
      }

```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-unit-test-support/src/main/java/org/apache/activemq/artemis/utils/ThreadLeakCheckRule.java`
#### Snippet
```java
            if (aliveThread.isAlive() && !isExpectedThread(aliveThread) && !previousThreads.containsKey(aliveThread)) {
               if (!failedThread) {
                  System.out.println("*********************************************************************************");
                  System.out.println("LEAKING THREADS");
               }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-unit-test-support/src/main/java/org/apache/activemq/artemis/utils/ThreadLeakCheckRule.java`
#### Snippet
```java
               if (!failedThread) {
                  System.out.println("*********************************************************************************");
                  System.out.println("LEAKING THREADS");
               }
               failedThread = true;
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-unit-test-support/src/main/java/org/apache/activemq/artemis/utils/ThreadLeakCheckRule.java`
#### Snippet
```java
               }
               failedThread = true;
               System.out.println("=============================================================================");
               System.out.println("Thread " + aliveThread + " is still alive with the following stackTrace:");
               StackTraceElement[] elements = postThreads.get(aliveThread);
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-unit-test-support/src/main/java/org/apache/activemq/artemis/utils/ThreadLeakCheckRule.java`
#### Snippet
```java
               failedThread = true;
               System.out.println("=============================================================================");
               System.out.println("Thread " + aliveThread + " is still alive with the following stackTrace:");
               StackTraceElement[] elements = postThreads.get(aliveThread);
               for (StackTraceElement el : elements) {
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-unit-test-support/src/main/java/org/apache/activemq/artemis/utils/ThreadLeakCheckRule.java`
#### Snippet
```java
               StackTraceElement[] elements = postThreads.get(aliveThread);
               for (StackTraceElement el : elements) {
                  System.out.println(el);
               }
               aliveThread.interrupt();
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-unit-test-support/src/main/java/org/apache/activemq/artemis/utils/ThreadLeakCheckRule.java`
#### Snippet
```java
         }
         if (failedThread) {
            System.out.println("*********************************************************************************");
         }
      }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-unit-test-support/src/main/java/org/apache/activemq/artemis/utils/ThreadLeakCheckRule.java`
#### Snippet
```java
                  Assert.fail("Thread leaked");
               } else {
                  System.out.println("***********************************************************************");
                  System.out.println("             The test failed and there is a leak");
                  System.out.println("***********************************************************************");
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-unit-test-support/src/main/java/org/apache/activemq/artemis/utils/ThreadLeakCheckRule.java`
#### Snippet
```java
               } else {
                  System.out.println("***********************************************************************");
                  System.out.println("             The test failed and there is a leak");
                  System.out.println("***********************************************************************");
                  failure.printStackTrace();
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-unit-test-support/src/main/java/org/apache/activemq/artemis/utils/ThreadLeakCheckRule.java`
#### Snippet
```java
                  System.out.println("***********************************************************************");
                  System.out.println("             The test failed and there is a leak");
                  System.out.println("***********************************************************************");
                  failure.printStackTrace();
                  Assert.fail("Test " + testDescription + " Failed with a leak - " + failure.getMessage());
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-unit-test-support/src/main/java/org/apache/activemq/artemis/utils/ThreadLeakCheckRule.java`
#### Snippet
```java
               }
            } else if (failedOnce) {
               System.out.println("******************** Threads cleared after retries ********************");
               System.out.println();
            }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-unit-test-support/src/main/java/org/apache/activemq/artemis/utils/ThreadLeakCheckRule.java`
#### Snippet
```java
            } else if (failedOnce) {
               System.out.println("******************** Threads cleared after retries ********************");
               System.out.println();
            }

```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-jdbc-store/src/main/java/org/apache/activemq/artemis/jdbc/store/file/PostgresSequentialSequentialFileDriver.java`
#### Snippet
```java
      }
      if ((Long) file.getMetaData(POSTGRES_OID_KEY) == 0) {
         System.out.println("FD");
      }
      return (Long) file.getMetaData(POSTGRES_OID_KEY);
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/ActiveMQClientProtocolManager.java`
#### Snippet
```java
            connection.setChannelVersion(topMessage.getServerVersion());
         } else if (type == PacketImpl.CHECK_FOR_FAILOVER_REPLY) {
            System.out.println("Channel0Handler.handlePacket");
         }
      }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/security/jaas/SCRAMPropertiesLoginModule.java`
#### Snippet
```java
                                          IOException {
      if (args.length < 2) {
         System.out.println("Usage: " + SCRAMPropertiesLoginModule.class.getSimpleName() +
                  " <username> <password> [<iterations>]");
         System.out.println("\ttype: " + getSupportedTypes());
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/security/jaas/SCRAMPropertiesLoginModule.java`
#### Snippet
```java
         System.out.println("Usage: " + SCRAMPropertiesLoginModule.class.getSimpleName() +
                  " <username> <password> [<iterations>]");
         System.out.println("\ttype: " + getSupportedTypes());
         System.out.println("\titerations desired number of iteration (min value: " + MIN_ITERATIONS + ")");
         return;
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/security/jaas/SCRAMPropertiesLoginModule.java`
#### Snippet
```java
                  " <username> <password> [<iterations>]");
         System.out.println("\ttype: " + getSupportedTypes());
         System.out.println("\titerations desired number of iteration (min value: " + MIN_ITERATIONS + ")");
         return;
      }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/security/jaas/SCRAMPropertiesLoginModule.java`
#### Snippet
```java
         properties.setProperty(encodedUser + SEPARATOR_MECHANISM + scram.name(), encodedPassword);
      }
      properties.store(System.out,
                       "Insert the lines stating with '" + encodedUser + "' into the desired user properties file");
   }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/cursor/impl/PageSubscriptionImpl.java`
#### Snippet
```java

   public void printDebug(final String msg) {
      System.out.println("Debug information on PageCurorImpl- " + msg);
      for (PageCursorInfo info : consumedPages.values()) {
         System.out.println(info);
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/cursor/impl/PageSubscriptionImpl.java`
#### Snippet
```java
      System.out.println("Debug information on PageCurorImpl- " + msg);
      for (PageCursorInfo info : consumedPages.values()) {
         System.out.println(info);
      }
   }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/DescribeJournal.java`
#### Snippet
```java

   public static DescribeJournal describeMessagesJournal(final File messagesDir) throws Exception {
      return describeMessagesJournal(messagesDir, System.out, false, true, true, false);
   }

```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/DescribeJournal.java`
#### Snippet
```java

   public static void describeBindingsJournal(final File bindingsDir) throws Exception {
      describeBindingsJournal(bindingsDir, System.out, false, true, true);
   }

```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java
      }

      System.out.println(str.toString());

      return str.toString();
```

## RuleId[id=ImplicitArrayToString]
### ImplicitArrayToString
Call to 'toString()' on array
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java

            if (logger.isDebugEnabled()) {
               final String hashcodeToString = oldRouteToIDs.toString(); // don't use Arrays.toString(..) here
               logger.debug("Removed property from message: {} = {} ({})", propName, hashcodeToString, ByteBuffer.wrap(oldRouteToIDs).getLong());
            }
```

## RuleId[id=DynamicRegexReplaceableByCompiledPattern]
### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `artemis-log-annotation-processor/src/main/java/org/apache/activemq/artemis/logs/annotation/processor/LogAnnotationProcessor.java`
#### Snippet
```java

   private static String encodeSpecialChars(String input) {
      return input.replaceAll("\n", "\\\\n").replaceAll("\"", "\\\\\"");
   }

```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `artemis-log-annotation-processor/src/main/java/org/apache/activemq/artemis/logs/annotation/processor/LogAnnotationProcessor.java`
#### Snippet
```java

   private static String encodeSpecialChars(String input) {
      return input.replaceAll("\n", "\\\\n").replaceAll("\"", "\\\\\"");
   }

```

### DynamicRegexReplaceableByCompiledPattern
`split()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/uri/URISupport.java`
#### Snippet
```java
         Map<String, String> rc = new HashMap<>();
         if (uri != null && !uri.isEmpty()) {
            parseParameters(rc, uri.split("[&;]"));
         }
         return rc;
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/InstallAbstract.java`
#### Snippet
```java
      // that is correct for the current platform.
      String separator = unixTarget && IS_CYGWIN ? "\n" : System.getProperty("line.separator");
      content = content.replaceAll("\\r?\\n", Matcher.quoteReplacement(separator));
      ByteArrayInputStream in = new ByteArrayInputStream(content.getBytes(encoding));
      try (FileOutputStream fout = new FileOutputStream(target)) {
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Create.java`
#### Snippet
```java
      filters.put("@artemis.instance@", path(directory));
      filters.put("${artemis.instance.uri}", directory.toURI().toString());
      filters.put("${artemis.instance.uri.windows}", directory.toURI().toString().replaceAll("%", "%%"));
      filters.put("${artemis.instance.name}", directory.getName());
      filters.put("${java.home}", path(System.getProperty("java.home")));
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Create.java`
#### Snippet
```java

      filters.put("${artemis.instance.etc.uri}", etcFolder.toURI().toString());
      filters.put("${artemis.instance.etc.uri.windows}", etcFolder.toURI().toString().replaceAll("%", "%%"));
      filters.put("${artemis.instance.etc}", path(etcFolder));
      filters.put("${artemis.instance.oome.dump}", path(oomeDumpFile));
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Create.java`
#### Snippet
```java
      filters.put("${user}", getUser());
      filters.put("${password}", getPassword());
      filters.put("${encoded.role}", role.replaceAll(" ", "\\\\ "));


```

### DynamicRegexReplaceableByCompiledPattern
`matches()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `artemis-web/src/main/java/org/apache/activemq/artemis/component/DefaultHandler.java`
#### Snippet
```java
                      HttpServletRequest request,
                      HttpServletResponse response) throws IOException, ServletException {
      if (rootRedirectLocation != null && target.matches("^$|/")) {
         response.sendRedirect(rootRedirectLocation);
      } else {
```

### DynamicRegexReplaceableByCompiledPattern
`replaceFirst()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/security/jaas/LDAPLoginModule.java`
#### Snippet
```java
      context.addToEnvironment(Context.SECURITY_CREDENTIALS, password);
      try {
         String baseDn = getLDAPPropertyValue(ConfigKey.CONNECTION_URL).replaceFirst(".*/", ",");
         String userDn = dn.replace(baseDn, "");
         logger.debug("Get user Attributes with dn {}", userDn);
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/ConfigurationUtils.java`
#### Snippet
```java
      try {
         // remove all whitespace
         uri = uri.replaceAll("\\s", "");

         AcceptorTransportConfigurationParser parser = new AcceptorTransportConfigurationParser();
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/ConfigurationUtils.java`
#### Snippet
```java
      try {
         // remove all whitespace
         uri = uri.replaceAll("\\s", "");

         ConnectorTransportConfigurationParser parser = new ConnectorTransportConfigurationParser();
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/impl/ClusterConnectionBridge.java`
#### Snippet
```java
         String qName = "notif." + UUIDGenerator.getInstance().generateStringUUID() +
            "." +
            clusterConnection.getServer().toString().replaceAll(CompositeAddress.SEPARATOR, "_");

         SimpleString notifQueueName = new SimpleString(qName);
```

## RuleId[id=NonProtectedConstructorInAbstractClass]
### NonProtectedConstructorInAbstractClass
Constructor `BinaryExpression()` of an abstract class should not be declared 'public'
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/BinaryExpression.java`
#### Snippet
```java
   protected Expression right;

   public BinaryExpression(Expression left, Expression right) {
      this.left = left;
      this.right = right;
```

### NonProtectedConstructorInAbstractClass
Constructor `UnaryExpression()` of an abstract class should not be declared 'public'
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/UnaryExpression.java`
#### Snippet
```java
   protected Expression right;

   public UnaryExpression(Expression left) {
      this.right = left;
   }
```

### NonProtectedConstructorInAbstractClass
Constructor `ArithmeticExpression()` of an abstract class should not be declared 'public'
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/ArithmeticExpression.java`
#### Snippet
```java
    * @param right
    */
   public ArithmeticExpression(Expression left, Expression right) {
      super(left, right);
      convertStringExpressions = ComparisonExpression.CONVERT_STRING_EXPRESSIONS.get() != null;
```

### NonProtectedConstructorInAbstractClass
Constructor `ComparisonExpression()` of an abstract class should not be declared 'public'
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/ComparisonExpression.java`
#### Snippet
```java
    * @param right
    */
   public ComparisonExpression(Expression left, Expression right) {
      super(left, right);
      convertStringExpressions = CONVERT_STRING_EXPRESSIONS.get() != null;
```

### NonProtectedConstructorInAbstractClass
Constructor `ActiveMQScheduledComponent()` of an abstract class should not be declared 'public'
in `artemis-commons/src/main/java/org/apache/activemq/artemis/core/server/ActiveMQScheduledComponent.java`
#### Snippet
```java
    * @param onDemand                 if {@code true} the task won't be scheduled on {@link #start()}, {@code false} otherwise
    */
   public ActiveMQScheduledComponent(ScheduledExecutorService scheduledExecutorService,
                                     Executor executor,
                                     long checkPeriod,
```

### NonProtectedConstructorInAbstractClass
Constructor `ActiveMQScheduledComponent()` of an abstract class should not be declared 'public'
in `artemis-commons/src/main/java/org/apache/activemq/artemis/core/server/ActiveMQScheduledComponent.java`
#### Snippet
```java
    * @param onDemand     if {@code true} the task won't be scheduled on {@link #start()}, {@code false} otherwise
    */
   public ActiveMQScheduledComponent(long initialDelay, long checkPeriod, TimeUnit timeUnit, boolean onDemand) {
      this(null, null, initialDelay, checkPeriod, timeUnit, onDemand);
   }
```

### NonProtectedConstructorInAbstractClass
Constructor `ActiveMQScheduledComponent()` of an abstract class should not be declared 'public'
in `artemis-commons/src/main/java/org/apache/activemq/artemis/core/server/ActiveMQScheduledComponent.java`
#### Snippet
```java
    * @param onDemand                 if {@code true} the task won't be scheduled on {@link #start()}, {@code false} otherwise
    */
   public ActiveMQScheduledComponent(ScheduledExecutorService scheduledExecutorService,
                                     Executor executor,
                                     long initialDelay,
```

### NonProtectedConstructorInAbstractClass
Constructor `ActiveMQScheduledComponent()` of an abstract class should not be declared 'public'
in `artemis-commons/src/main/java/org/apache/activemq/artemis/core/server/ActiveMQScheduledComponent.java`
#### Snippet
```java
    * @param onDemand                 if {@code true} the task won't be scheduled on {@link #start()}, {@code false} otherwise
    */
   public ActiveMQScheduledComponent(ScheduledExecutorService scheduledExecutorService,
                                     long initialDelay,
                                     long checkPeriod,
```

### NonProtectedConstructorInAbstractClass
Constructor `ActiveMQScheduledComponent()` of an abstract class should not be declared 'public'
in `artemis-commons/src/main/java/org/apache/activemq/artemis/core/server/ActiveMQScheduledComponent.java`
#### Snippet
```java
    * @param onDemand    if {@code true} the task won't be scheduled on {@link #start()}, {@code false} otherwise
    */
   public ActiveMQScheduledComponent(long checkPeriod, TimeUnit timeUnit, boolean onDemand) {
      this(null, null, checkPeriod, checkPeriod, timeUnit, onDemand);
   }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractLatch()` of an abstract class should not be declared 'public'
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/AbstractLatch.java`
#### Snippet
```java
   protected final CountSync control;

   public AbstractLatch() {
      this(0);
   }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractLatch()` of an abstract class should not be declared 'public'
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/AbstractLatch.java`
#### Snippet
```java
   }

   public AbstractLatch(final int count) {
      control = new CountSync(count);
   }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractByteBufPool()` of an abstract class should not be declared 'public'
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/AbstractByteBufPool.java`
#### Snippet
```java
   }

   public AbstractByteBufPool(final int capacity) {
      entries = (T[]) new Object[MathUtil.findNextPositivePowerOfTwo(capacity)];
      mask = entries.length - 1;
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractByteBufPool()` of an abstract class should not be declared 'public'
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/AbstractByteBufPool.java`
#### Snippet
```java
   private final int shift;

   public AbstractByteBufPool() {
      this(DEFAULT_POOL_CAPACITY);
   }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractPool()` of an abstract class should not be declared 'public'
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/AbstractPool.java`
#### Snippet
```java
   private final int shift;

   public AbstractPool() {
      this(DEFAULT_POOL_CAPACITY);
   }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractPool()` of an abstract class should not be declared 'public'
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/AbstractPool.java`
#### Snippet
```java
   }

   public AbstractPool(final int capacity) {
      entries = (O[]) new Object[MathUtil.findNextPositivePowerOfTwo(capacity)];
      mask = entries.length - 1;
```

### NonProtectedConstructorInAbstractClass
Constructor `Pool()` of an abstract class should not be declared 'public'
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/pools/Pool.java`
#### Snippet
```java
   private final Supplier<T> supplier;

   public Pool(int maxSize, Consumer<T> cleaner, Supplier<T> supplier) {
      internalPool = createQueue(maxSize);
      this.cleaner = cleaner;
```

### NonProtectedConstructorInAbstractClass
Constructor `ProcessorBase()` of an abstract class should not be declared 'public'
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/actors/ProcessorBase.java`
#### Snippet
```java
   protected abstract void doTask(T task);

   public ProcessorBase(Executor parent) {
      this.delegate = parent;
   }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractSequentialFile()` of an abstract class should not be declared 'public'
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/io/AbstractSequentialFile.java`
#### Snippet
```java
    * @param directory
    */
   public AbstractSequentialFile(final File directory,
                                 final String file,
                                 final SequentialFileFactory factory,
```

### NonProtectedConstructorInAbstractClass
Constructor `SkeletalProducerLoadGenerator()` of an abstract class should not be declared 'public'
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/perf/SkeletalProducerLoadGenerator.java`
#### Snippet
```java
   private boolean stopHandlingCompletions;

   public SkeletalProducerLoadGenerator(final AsyncJms2ProducerFacade producer,
                                        final OrderedEventExecutor executor,
                                        final MicrosTimeProvider timeProvider,
```

### NonProtectedConstructorInAbstractClass
Constructor `AMQConsumerBrokerExchange()` of an abstract class should not be declared 'public'
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/amq/AMQConsumerBrokerExchange.java`
#### Snippet
```java
   protected final AMQSession amqSession;

   public AMQConsumerBrokerExchange(AMQSession amqSession) {
      this.amqSession = amqSession;
   }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractActiveMQClientDelegate()` of an abstract class should not be declared 'public'
in `artemis-junit/artemis-junit-commons/src/main/java/org/apache/activemq/artemis/junit/AbstractActiveMQClientDelegate.java`
#### Snippet
```java
   }

   public AbstractActiveMQClientDelegate(ServerLocator serverLocator) {
      if (serverLocator == null) {
         throw new IllegalArgumentException(String.format("Error creating %s - ServerLocator cannot be null",
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractActiveMQClientDelegate()` of an abstract class should not be declared 'public'
in `artemis-junit/artemis-junit-commons/src/main/java/org/apache/activemq/artemis/junit/AbstractActiveMQClientDelegate.java`
#### Snippet
```java
   String password;

   public AbstractActiveMQClientDelegate(String url, String username, String password) {
      this(url);
      this.username = username;
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractActiveMQClientDelegate()` of an abstract class should not be declared 'public'
in `artemis-junit/artemis-junit-commons/src/main/java/org/apache/activemq/artemis/junit/AbstractActiveMQClientDelegate.java`
#### Snippet
```java
   }

   public AbstractActiveMQClientDelegate(String url) {
      if (url == null) {
         throw new IllegalArgumentException(String.format("Error creating %s - url cannot be null",
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractActiveMQClientDelegate()` of an abstract class should not be declared 'public'
in `artemis-junit/artemis-junit-commons/src/main/java/org/apache/activemq/artemis/junit/AbstractActiveMQClientDelegate.java`
#### Snippet
```java
   }

   public AbstractActiveMQClientDelegate(ServerLocator serverLocator, String username, String password) {
      this(serverLocator);
      this.username = username;
```

### NonProtectedConstructorInAbstractClass
Constructor `SCRAMServerSASL()` of an abstract class should not be declared 'public'
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/sasl/scram/SCRAMServerSASL.java`
#### Snippet
```java
   private SASLResult result;

   public SCRAMServerSASL(SCRAM mechanism) throws NoSuchAlgorithmException {
      this(mechanism, UUID.randomUUID().toString());
   }
```

### NonProtectedConstructorInAbstractClass
Constructor `SCRAMServerSASLFactory()` of an abstract class should not be declared 'public'
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/sasl/scram/SCRAMServerSASLFactory.java`
#### Snippet
```java
   private final SCRAM scramType;

   public SCRAMServerSASLFactory(SCRAM scram, Logger logger) {
      this.scramType = scram;
      this.logger = logger;
```

### NonProtectedConstructorInAbstractClass
Constructor `ProtonAbstractReceiver()` of an abstract class should not be declared 'public'
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/ProtonAbstractReceiver.java`
#### Snippet
```java
   }

   public ProtonAbstractReceiver(AMQPSessionCallback sessionSPI,
                                 AMQPConnectionContext connection,
                                 AMQPSessionContext protonSession,
```

### NonProtectedConstructorInAbstractClass
Constructor `AMQPMessage()` of an abstract class should not be declared 'public'
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
    *       Broker specific extra properties that should be carried with this message
    */
   public AMQPMessage(long messageFormat, TypedProperties extraProperties, CoreMessageObjectPools coreMessageObjectPools) {
      this.messageFormat = messageFormat;
      this.coreMessageObjectPools = coreMessageObjectPools;
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractJDBCDriver()` of an abstract class should not be declared 'public'
in `artemis-jdbc-store/src/main/java/org/apache/activemq/artemis/jdbc/store/drivers/AbstractJDBCDriver.java`
#### Snippet
```java
   protected JDBCConnectionProvider connectionProvider;

   public AbstractJDBCDriver() {
   }

```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractJDBCDriver()` of an abstract class should not be declared 'public'
in `artemis-jdbc-store/src/main/java/org/apache/activemq/artemis/jdbc/store/drivers/AbstractJDBCDriver.java`
#### Snippet
```java
   }

   public AbstractJDBCDriver(JDBCConnectionProvider connectionProvider, SQLProvider provider) {
      this.connectionProvider = connectionProvider;
      this.sqlProvider = provider;
```

### NonProtectedConstructorInAbstractClass
Constructor `JGroupsBroadcastEndpoint()` of an abstract class should not be declared 'public'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/JGroupsBroadcastEndpoint.java`
#### Snippet
```java
   private JChannelManager manager;

   public JGroupsBroadcastEndpoint(JChannelManager manager, String channelName) {
      this.manager = manager;
      this.channelName = channelName;
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractRemotingConnection()` of an abstract class should not be declared 'public'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/spi/core/protocol/AbstractRemotingConnection.java`
#### Snippet
```java
   private Subject subject;

   public AbstractRemotingConnection(final Connection transportConnection, final Executor executor) {
      this.transportConnection = transportConnection;
      this.executor = executor;
```

### NonProtectedConstructorInAbstractClass
Constructor `SessionContext()` of an abstract class should not be declared 'public'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/spi/core/remoting/SessionContext.java`
#### Snippet
```java
   protected final IDGenerator idGenerator = new SimpleIDGenerator(0);

   public SessionContext(RemotingConnection remotingConnection) {
      this.remotingConnection = remotingConnection;
   }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractProducerCreditsImpl()` of an abstract class should not be declared 'public'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/AbstractProducerCreditsImpl.java`
#### Snippet
```java
   protected SessionContext sessionContext;

   public AbstractProducerCreditsImpl(final ClientSessionInternal session,
                                      final SimpleString address,
                                      final int windowSize) {
```

### NonProtectedConstructorInAbstractClass
Constructor `FederationStreamConnectMessage()` of an abstract class should not be declared 'public'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/FederationStreamConnectMessage.java`
#### Snippet
```java
   private T streamConfiguration;

   public FederationStreamConnectMessage(final byte type) {
      super(type);
   }
```

### NonProtectedConstructorInAbstractClass
Constructor `MessagePacket()` of an abstract class should not be declared 'public'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/MessagePacket.java`
#### Snippet
```java
   protected ICoreMessage message;

   public MessagePacket(final byte type, final ICoreMessage message) {
      super(type);

```

### NonProtectedConstructorInAbstractClass
Constructor `QueueAbstractPacket()` of an abstract class should not be declared 'public'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/QueueAbstractPacket.java`
#### Snippet
```java
   }

   public QueueAbstractPacket(byte type) {
      super(type);
   }
```

### NonProtectedConstructorInAbstractClass
Constructor `SessionContinuationMessage()` of an abstract class should not be declared 'public'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionContinuationMessage.java`
#### Snippet
```java
   protected boolean continues;

   public SessionContinuationMessage(final byte type, final byte[] body, final boolean continues) {
      super(type);
      this.body = body;
```

### NonProtectedConstructorInAbstractClass
Constructor `SessionContinuationMessage()` of an abstract class should not be declared 'public'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionContinuationMessage.java`
#### Snippet
```java
   }

   public SessionContinuationMessage(final byte type) {
      super(type);
   }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractPrincipalLoginModule()` of an abstract class should not be declared 'public'
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/security/jaas/AbstractPrincipalLoginModule.java`
#### Snippet
```java
   private Principal[] principals;

   public AbstractPrincipalLoginModule(Logger logger) {
      this.logger = logger;
   }
```

### NonProtectedConstructorInAbstractClass
Constructor `BrokerConnectConfiguration()` of an abstract class should not be declared 'public'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/brokerConnectivity/BrokerConnectConfiguration.java`
#### Snippet
```java
   private boolean autostart = true;

   public BrokerConnectConfiguration(String name, String uri) {
      this.name = name;
      this.uri = uri;
```

### NonProtectedConstructorInAbstractClass
Constructor `NodeManager()` of an abstract class should not be declared 'public'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/NodeManager.java`
#### Snippet
```java
   protected long nodeActivationSequence = NULL_NODE_ACTIVATION_SEQUENCE;  // local version of a coordinated sequence, tracking state transitions of ownership

   public NodeManager(final boolean replicatedBackup) {
      this.replicatedBackup = replicatedBackup;
      this.lockListeners = new HashSet<>();
```

### NonProtectedConstructorInAbstractClass
Constructor `LiveNodeLocator()` of an abstract class should not be declared 'public'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/LiveNodeLocator.java`
#### Snippet
```java
   private final BackupRegistrationListener backupRegistrationListener;

   public LiveNodeLocator(BackupRegistrationListener backupRegistrationListener) {
      this.backupRegistrationListener = backupRegistrationListener;
   }
```

### NonProtectedConstructorInAbstractClass
Constructor `LiveNodeLocator()` of an abstract class should not be declared 'public'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/LiveNodeLocator.java`
#### Snippet
```java
    * Use this constructor when the LiveNodeLocator is used for scaling down rather than replicating
    */
   public LiveNodeLocator() {
      this(null);
   }
```

### NonProtectedConstructorInAbstractClass
Constructor `CleaningActivateCallback()` of an abstract class should not be declared 'public'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/CleaningActivateCallback.java`
#### Snippet
```java
public abstract class CleaningActivateCallback implements ActivateCallback {

   public CleaningActivateCallback() {
   }

```

### NonProtectedConstructorInAbstractClass
Constructor `FileBasedNodeManager()` of an abstract class should not be declared 'public'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/FileBasedNodeManager.java`
#### Snippet
```java
   protected FileChannel activationSequenceChannel;

   public FileBasedNodeManager(boolean replicatedBackup, File directory) {
      super(replicatedBackup);
      this.directory = directory;
```

### NonProtectedConstructorInAbstractClass
Constructor `GroupHandlingAbstract()` of an abstract class should not be declared 'public'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/group/impl/GroupHandlingAbstract.java`
#### Snippet
```java
   final Set<UnproposalListener> listeners = Collections.newSetFromMap(new WeakHashMap<UnproposalListener, Boolean>());

   public GroupHandlingAbstract(final Executor executor,
                                final ManagementService managementService,
                                final SimpleString address) {
```

### NonProtectedConstructorInAbstractClass
Constructor `QuorumVote()` of an abstract class should not be declared 'public'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/qourum/QuorumVote.java`
#### Snippet
```java
   private SimpleString name;

   public QuorumVote(SimpleString name) {
      this.name = name;
   }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractPool()` of an abstract class should not be declared 'public'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/routing/pools/AbstractPool.java`
#### Snippet
```java


   public AbstractPool(TargetFactory targetFactory, ScheduledExecutorService scheduledExecutor, int checkPeriod) {
      this.targetFactory = targetFactory;

```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractTarget()` of an abstract class should not be declared 'public'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/routing/targets/AbstractTarget.java`
#### Snippet
```java


   public AbstractTarget(TransportConfiguration connector, String nodeID) {
      this.connector = connector;
      this.nodeID = nodeID;
```

### NonProtectedConstructorInAbstractClass
Constructor `TargetProbe()` of an abstract class should not be declared 'public'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/routing/targets/TargetProbe.java`
#### Snippet
```java
   }

   public TargetProbe(String name) {
      this.name = name;
   }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractPolicy()` of an abstract class should not be declared 'public'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/routing/policies/AbstractPolicy.java`
#### Snippet
```java
   }

   public AbstractPolicy(final String name) {
      this.name = name;
   }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractFederationStream()` of an abstract class should not be declared 'public'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/federation/AbstractFederationStream.java`
#### Snippet
```java
   }

   public AbstractFederationStream(final ActiveMQServer server, final Federation federation, final String name, final FederationStreamConfiguration config,
                                   final FederationConnection connection) {
      this.server = server;
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractFederationStream()` of an abstract class should not be declared 'public'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/federation/AbstractFederationStream.java`
#### Snippet
```java


   public AbstractFederationStream(ActiveMQServer server, Federation federation, String name, FederationStreamConfiguration config) {
      this(server, federation, name, config, null);
   }
```

### NonProtectedConstructorInAbstractClass
Constructor `FederatedAbstract()` of an abstract class should not be declared 'public'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/federation/FederatedAbstract.java`
#### Snippet
```java
   private boolean started;

   public FederatedAbstract(Federation federation, ActiveMQServer server, FederationUpstream upstream) {
      this.federation = federation;
      this.server = server;
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractAcceptor()` of an abstract class should not be declared 'public'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/remoting/impl/AbstractAcceptor.java`
#### Snippet
```java
   protected final Map<String, ProtocolManager> protocolMap;

   public AbstractAcceptor(Map<String, ProtocolManager> protocolMap) {
      this.protocolMap = protocolMap;
   }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractControl()` of an abstract class should not be declared 'public'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/AbstractControl.java`
#### Snippet
```java


   public AbstractControl(final Class<?> clazz, final StorageManager storageManager) throws NotCompliantMBeanException {
      super(clazz);
      this.storageManager = storageManager;
```

### NonProtectedConstructorInAbstractClass
Constructor `ActiveMQAbstractView()` of an abstract class should not be declared 'public'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/view/ActiveMQAbstractView.java`
#### Snippet
```java
   protected String options;

   public ActiveMQAbstractView() {
      this.sortColumn = getDefaultOrderColumn();
      this.sortOrder = "asc";
```

### NonProtectedConstructorInAbstractClass
Constructor `TransactionDetail()` of an abstract class should not be declared 'public'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/transaction/TransactionDetail.java`
#### Snippet
```java
   private final Long creationTime;

   public TransactionDetail(Xid xid, Transaction tx, Long creation) {
      this.xid = xid;
      this.transaction = tx;
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractJournalStorageManager()` of an abstract class should not be declared 'public'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/AbstractJournalStorageManager.java`
#### Snippet
```java
   }

   public AbstractJournalStorageManager(Configuration config,
                                        CriticalAnalyzer analyzer,
                                        ExecutorFactory executorFactory,
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractJournalStorageManager()` of an abstract class should not be declared 'public'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/AbstractJournalStorageManager.java`
#### Snippet
```java
   protected final ConcurrentLongHashMap<LargeServerMessage> largeMessagesToDelete = new ConcurrentLongHashMap<>();

   public AbstractJournalStorageManager(final Configuration config,
                                        final CriticalAnalyzer analyzer,
                                        final ExecutorFactory executorFactory,
```

## RuleId[id=CaughtExceptionImmediatelyRethrown]
### CaughtExceptionImmediatelyRethrown
Caught exception `e` is immediately rethrown
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTUtil.java`
#### Snippet
```java
         } finally {
         }
      } catch (RuntimeException e) {
         throw e;
      }
```

### CaughtExceptionImmediatelyRethrown
Caught exception `e` is immediately rethrown
in `artemis-jdbc-store/src/main/java/org/apache/activemq/artemis/jdbc/store/file/JDBCSequentialFileFactory.java`
#### Snippet
```java
      try {
         return dbDriver.listFiles(extension);
      } catch (SQLException e) {
         // We can't throw critical error here
         // exists will call listfiles, and if the store does not exists
```

### CaughtExceptionImmediatelyRethrown
Caught exception `ie` is immediately rethrown
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/jdbc/JdbcNodeManager.java`
#### Snippet
```java
         if (getUUID() == null)
            readNodeId();
      } catch (InterruptedException ie) {
         throw ie;
      } catch (ActiveMQLockAcquisitionTimeoutException | IllegalStateException e) {
```

### CaughtExceptionImmediatelyRethrown
Caught exception `ie` is immediately rethrown
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/jdbc/JdbcNodeManager.java`
#### Snippet
```java
            }
         };
      } catch (InterruptedException ie) {
         throw ie;
      } catch (ActiveMQLockAcquisitionTimeoutException | IllegalStateException e) {
```

### CaughtExceptionImmediatelyRethrown
Caught exception `e` is immediately rethrown
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/jdbc/JdbcNodeManager.java`
#### Snippet
```java
            }
         }
      } catch (InterruptedException e) {
         throw e;
      }
```

### CaughtExceptionImmediatelyRethrown
Caught exception `e` is immediately rethrown
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/jdbc/JdbcNodeManager.java`
#### Snippet
```java
         logger.debug("acquired live node lock while state is {}: starting scheduledLiveLock", SharedStateManager.State.LIVE);
         this.scheduledLiveLock.start();
      } catch (InterruptedException e) {
         throw e;
      } catch (ActiveMQLockAcquisitionTimeoutException | IllegalStateException e) {
```

## RuleId[id=AssignmentToMethodParameter]
### AssignmentToMethodParameter
Assignment to method parameter `xaRes`
in `artemis-service-extensions/src/main/java/org/apache/activemq/artemis/service/extensions/xa/recovery/ActiveMQXAResourceWrapper.java`
#### Snippet
```java
   public boolean isSameRM(XAResource xaRes) throws XAException {
      if (xaRes instanceof ActiveMQXAResourceWrapper) {
         xaRes = ((ActiveMQXAResourceWrapper) xaRes).getDelegate(false);
      }

```

### AssignmentToMethodParameter
Assignment to method parameter `content`
in `artemis-maven-plugin/src/main/java/org/apache/activemq/artemis/maven/ArtemisDependencyDocPlugin.java`
#### Snippet
```java
         for (Map.Entry<String, String> entry : filters.entrySet()) {
            try {
               content = replace(content, entry.getKey(), entry.getValue());
            } catch (Throwable e) {
               System.out.println("Error on " + entry.getKey());
```

### AssignmentToMethodParameter
Assignment to method parameter `text`
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/ConstantExpression.java`
#### Snippet
```java
      // Strip off the 'l' or 'L' if needed.
      if (text.endsWith("l") || text.endsWith("L")) {
         text = text.substring(0, text.length() - 1);
      }

```

### AssignmentToMethodParameter
Assignment to method parameter `fileBrokerETC`
in `artemis-boot/src/main/java/org/apache/activemq/artemis/boot/Artemis.java`
#### Snippet
```java
      if (fileBrokerETC == null && fileInstance != null) {
         // the fileBrokerETC could be null if execute is called directly from an external module
         fileBrokerETC = new File(fileInstance, "etc");
      }

```

### AssignmentToMethodParameter
Assignment to method parameter `rv`
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/ComparisonExpression.java`
#### Snippet
```java
            if (lc == Boolean.class) {
               if (convertStringExpressions && rc == String.class) {
                  rv = Boolean.valueOf((String) rv);
               } else {
                  return Boolean.FALSE;
```

### AssignmentToMethodParameter
Assignment to method parameter `lv`
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/ComparisonExpression.java`
#### Snippet
```java
            } else if (lc == Byte.class) {
               if (rc == Short.class) {
                  lv = ((Number) lv).shortValue();
               } else if (rc == Integer.class) {
                  lv = ((Number) lv).intValue();
```

### AssignmentToMethodParameter
Assignment to method parameter `lv`
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/ComparisonExpression.java`
#### Snippet
```java
                  lv = ((Number) lv).shortValue();
               } else if (rc == Integer.class) {
                  lv = ((Number) lv).intValue();
               } else if (rc == Long.class) {
                  lv = ((Number) lv).longValue();
```

### AssignmentToMethodParameter
Assignment to method parameter `lv`
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/ComparisonExpression.java`
#### Snippet
```java
                  lv = ((Number) lv).intValue();
               } else if (rc == Long.class) {
                  lv = ((Number) lv).longValue();
               } else if (rc == Float.class) {
                  lv = ((Number) lv).floatValue();
```

### AssignmentToMethodParameter
Assignment to method parameter `lv`
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/ComparisonExpression.java`
#### Snippet
```java
                  lv = ((Number) lv).longValue();
               } else if (rc == Float.class) {
                  lv = ((Number) lv).floatValue();
               } else if (rc == Double.class) {
                  lv = ((Number) lv).doubleValue();
```

### AssignmentToMethodParameter
Assignment to method parameter `lv`
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/ComparisonExpression.java`
#### Snippet
```java
                  lv = ((Number) lv).floatValue();
               } else if (rc == Double.class) {
                  lv = ((Number) lv).doubleValue();
               } else if (convertStringExpressions && rc == String.class) {
                  rv = Byte.valueOf((String) rv);
```

### AssignmentToMethodParameter
Assignment to method parameter `rv`
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/ComparisonExpression.java`
#### Snippet
```java
                  lv = ((Number) lv).doubleValue();
               } else if (convertStringExpressions && rc == String.class) {
                  rv = Byte.valueOf((String) rv);
               } else {
                  return Boolean.FALSE;
```

### AssignmentToMethodParameter
Assignment to method parameter `lv`
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/ComparisonExpression.java`
#### Snippet
```java
            } else if (lc == Short.class) {
               if (rc == Integer.class) {
                  lv = ((Number) lv).intValue();
               } else if (rc == Long.class) {
                  lv = ((Number) lv).longValue();
```

### AssignmentToMethodParameter
Assignment to method parameter `lv`
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/ComparisonExpression.java`
#### Snippet
```java
                  lv = ((Number) lv).intValue();
               } else if (rc == Long.class) {
                  lv = ((Number) lv).longValue();
               } else if (rc == Float.class) {
                  lv = ((Number) lv).floatValue();
```

### AssignmentToMethodParameter
Assignment to method parameter `lv`
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/ComparisonExpression.java`
#### Snippet
```java
                  lv = ((Number) lv).longValue();
               } else if (rc == Float.class) {
                  lv = ((Number) lv).floatValue();
               } else if (rc == Double.class) {
                  lv = ((Number) lv).doubleValue();
```

### AssignmentToMethodParameter
Assignment to method parameter `lv`
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/ComparisonExpression.java`
#### Snippet
```java
                  lv = ((Number) lv).floatValue();
               } else if (rc == Double.class) {
                  lv = ((Number) lv).doubleValue();
               } else if (convertStringExpressions && rc == String.class) {
                  rv = Short.valueOf((String) rv);
```

### AssignmentToMethodParameter
Assignment to method parameter `rv`
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/ComparisonExpression.java`
#### Snippet
```java
                  lv = ((Number) lv).doubleValue();
               } else if (convertStringExpressions && rc == String.class) {
                  rv = Short.valueOf((String) rv);
               } else {
                  return Boolean.FALSE;
```

### AssignmentToMethodParameter
Assignment to method parameter `lv`
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/ComparisonExpression.java`
#### Snippet
```java
            } else if (lc == Integer.class) {
               if (rc == Long.class) {
                  lv = ((Number) lv).longValue();
               } else if (rc == Float.class) {
                  lv = ((Number) lv).floatValue();
```

### AssignmentToMethodParameter
Assignment to method parameter `lv`
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/ComparisonExpression.java`
#### Snippet
```java
                  lv = ((Number) lv).longValue();
               } else if (rc == Float.class) {
                  lv = ((Number) lv).floatValue();
               } else if (rc == Double.class) {
                  lv = ((Number) lv).doubleValue();
```

### AssignmentToMethodParameter
Assignment to method parameter `lv`
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/ComparisonExpression.java`
#### Snippet
```java
                  lv = ((Number) lv).floatValue();
               } else if (rc == Double.class) {
                  lv = ((Number) lv).doubleValue();
               } else if (convertStringExpressions && rc == String.class) {
                  rv = Integer.valueOf((String) rv);
```

### AssignmentToMethodParameter
Assignment to method parameter `rv`
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/ComparisonExpression.java`
#### Snippet
```java
                  lv = ((Number) lv).doubleValue();
               } else if (convertStringExpressions && rc == String.class) {
                  rv = Integer.valueOf((String) rv);
               } else {
                  return Boolean.FALSE;
```

### AssignmentToMethodParameter
Assignment to method parameter `rv`
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/ComparisonExpression.java`
#### Snippet
```java
            } else if (lc == Long.class) {
               if (rc == Integer.class) {
                  rv = ((Number) rv).longValue();
               } else if (rc == Float.class) {
                  lv = ((Number) lv).floatValue();
```

### AssignmentToMethodParameter
Assignment to method parameter `lv`
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/ComparisonExpression.java`
#### Snippet
```java
                  rv = ((Number) rv).longValue();
               } else if (rc == Float.class) {
                  lv = ((Number) lv).floatValue();
               } else if (rc == Double.class) {
                  lv = ((Number) lv).doubleValue();
```

### AssignmentToMethodParameter
Assignment to method parameter `lv`
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/ComparisonExpression.java`
#### Snippet
```java
                  lv = ((Number) lv).floatValue();
               } else if (rc == Double.class) {
                  lv = ((Number) lv).doubleValue();
               } else if (convertStringExpressions && rc == String.class) {
                  rv = Long.valueOf((String) rv);
```

### AssignmentToMethodParameter
Assignment to method parameter `rv`
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/ComparisonExpression.java`
#### Snippet
```java
                  lv = ((Number) lv).doubleValue();
               } else if (convertStringExpressions && rc == String.class) {
                  rv = Long.valueOf((String) rv);
               } else {
                  return Boolean.FALSE;
```

### AssignmentToMethodParameter
Assignment to method parameter `rv`
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/ComparisonExpression.java`
#### Snippet
```java
            } else if (lc == Float.class) {
               if (rc == Integer.class || rc == Long.class) {
                  rv = ((Number) rv).floatValue();
               } else if (rc == Double.class) {
                  lv = ((Number) lv).doubleValue();
```

### AssignmentToMethodParameter
Assignment to method parameter `lv`
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/ComparisonExpression.java`
#### Snippet
```java
                  rv = ((Number) rv).floatValue();
               } else if (rc == Double.class) {
                  lv = ((Number) lv).doubleValue();
               } else if (convertStringExpressions && rc == String.class) {
                  rv = Float.valueOf((String) rv);
```

### AssignmentToMethodParameter
Assignment to method parameter `rv`
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/ComparisonExpression.java`
#### Snippet
```java
                  lv = ((Number) lv).doubleValue();
               } else if (convertStringExpressions && rc == String.class) {
                  rv = Float.valueOf((String) rv);
               } else {
                  return Boolean.FALSE;
```

### AssignmentToMethodParameter
Assignment to method parameter `rv`
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/ComparisonExpression.java`
#### Snippet
```java
            } else if (lc == Double.class) {
               if (rc == Integer.class || rc == Long.class || rc == Float.class) {
                  rv = ((Number) rv).doubleValue();
               } else if (convertStringExpressions && rc == String.class) {
                  rv = Double.valueOf((String) rv);
```

### AssignmentToMethodParameter
Assignment to method parameter `rv`
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/ComparisonExpression.java`
#### Snippet
```java
                  rv = ((Number) rv).doubleValue();
               } else if (convertStringExpressions && rc == String.class) {
                  rv = Double.valueOf((String) rv);
               } else {
                  return Boolean.FALSE;
```

### AssignmentToMethodParameter
Assignment to method parameter `lv`
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/ComparisonExpression.java`
#### Snippet
```java

               if (rc == Boolean.class) {
                  lv = Boolean.valueOf((String) lv);
               } else if (rc == Byte.class) {
                  lv = Byte.valueOf((String) lv);
```

### AssignmentToMethodParameter
Assignment to method parameter `lv`
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/ComparisonExpression.java`
#### Snippet
```java
                  lv = Boolean.valueOf((String) lv);
               } else if (rc == Byte.class) {
                  lv = Byte.valueOf((String) lv);
               } else if (rc == Short.class) {
                  lv = Short.valueOf((String) lv);
```

### AssignmentToMethodParameter
Assignment to method parameter `lv`
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/ComparisonExpression.java`
#### Snippet
```java
                  lv = Byte.valueOf((String) lv);
               } else if (rc == Short.class) {
                  lv = Short.valueOf((String) lv);
               } else if (rc == Integer.class) {
                  lv = Integer.valueOf((String) lv);
```

### AssignmentToMethodParameter
Assignment to method parameter `lv`
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/ComparisonExpression.java`
#### Snippet
```java
                  lv = Short.valueOf((String) lv);
               } else if (rc == Integer.class) {
                  lv = Integer.valueOf((String) lv);
               } else if (rc == Long.class) {
                  lv = Long.valueOf((String) lv);
```

### AssignmentToMethodParameter
Assignment to method parameter `lv`
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/ComparisonExpression.java`
#### Snippet
```java
                  lv = Integer.valueOf((String) lv);
               } else if (rc == Long.class) {
                  lv = Long.valueOf((String) lv);
               } else if (rc == Float.class) {
                  lv = Float.valueOf((String) lv);
```

### AssignmentToMethodParameter
Assignment to method parameter `lv`
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/ComparisonExpression.java`
#### Snippet
```java
                  lv = Long.valueOf((String) lv);
               } else if (rc == Float.class) {
                  lv = Float.valueOf((String) lv);
               } else if (rc == Double.class) {
                  lv = Double.valueOf((String) lv);
```

### AssignmentToMethodParameter
Assignment to method parameter `lv`
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/ComparisonExpression.java`
#### Snippet
```java
                  lv = Float.valueOf((String) lv);
               } else if (rc == Double.class) {
                  lv = Double.valueOf((String) lv);
               } else {
                  return Boolean.FALSE;
```

### AssignmentToMethodParameter
Assignment to method parameter `selector`
in `artemis-protocols/artemis-stomp-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/stomp/StompConnection.java`
#### Snippet
```java
         String noLocalFilter = CONNECTION_ID_PROPERTY_NAME_STRING + " <> '" + getID().toString() + "'";
         if (selector == null) {
            selector = noLocalFilter;
         } else {
            selector += " AND " + noLocalFilter;
```

### AssignmentToMethodParameter
Assignment to method parameter `selector`
in `artemis-protocols/artemis-stomp-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/stomp/StompConnection.java`
#### Snippet
```java
            selector = noLocalFilter;
         } else {
            selector += " AND " + noLocalFilter;
         }
      }
```

### AssignmentToMethodParameter
Assignment to method parameter `ack`
in `artemis-protocols/artemis-stomp-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/stomp/StompConnection.java`
#### Snippet
```java

      if (ack == null) {
         ack = Stomp.Headers.Subscribe.AckModeValues.AUTO;
      }

```

### AssignmentToMethodParameter
Assignment to method parameter `all`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/api/core/SimpleString.java`
#### Snippet
```java
         // There will be at least 3 strings on this case (which is the actual common usecase)
         // For that reason I'm allocating the ArrayList with 3 already
         all = new ArrayList<>(3);
      }
      all.add(ss);
```

### AssignmentToMethodParameter
Assignment to method parameter `pos`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/api/core/SimpleString.java`
#### Snippet
```java
         throw new IndexOutOfBoundsException();
      }
      pos <<= 1;

      return (char) ((data[pos] & 0xFF) | (data[pos + 1] << 8) & 0xFF00);
```

### AssignmentToMethodParameter
Assignment to method parameter `buffer`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/core/buffers/impl/ChannelBufferWrapper.java`
#### Snippet
```java
      while ((parent = buffer.unwrap()) != null && parent != buffer) { // this last part is just in case the semantic
         // ever changes where unwrap is returning itself
         buffer = parent;
      }

```

### AssignmentToMethodParameter
Assignment to method parameter `host`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/IPV6Util.java`
#### Snippet
```java
         // Strip opening/closing brackets
         if (host.startsWith("[") && host.endsWith("]")) {
            host = host.substring(1, host.length() - 1);
         }

```

### AssignmentToMethodParameter
Assignment to method parameter `memoryArg1`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/SpawnedVMSupport.java`
#### Snippet
```java
      ProcessBuilder builder = new ProcessBuilder();
      if (memoryArg1 == null) {
         memoryArg1 = "-Xms128m";
      }
      if (memoryArg2 == null) {
```

### AssignmentToMethodParameter
Assignment to method parameter `memoryArg2`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/SpawnedVMSupport.java`
#### Snippet
```java
      }
      if (memoryArg2 == null) {
         memoryArg2 = "-Xmx128m";
      }
      builder.command(javaPath, memoryArg1, memoryArg2);
```

### AssignmentToMethodParameter
Assignment to method parameter `uptime`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/TimeUtils.java`
#### Snippet
```java
      NumberFormat fmtD = new DecimalFormat("###,##0.000", new DecimalFormatSymbols(Locale.ENGLISH));

      uptime /= 1000;
      if (uptime < 60) {
         return fmtD.format(uptime) + " seconds";
```

### AssignmentToMethodParameter
Assignment to method parameter `uptime`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/TimeUtils.java`
#### Snippet
```java
         return fmtD.format(uptime) + " seconds";
      }
      uptime /= 60;
      if (uptime < 60) {
         long minutes = (long) uptime;
```

### AssignmentToMethodParameter
Assignment to method parameter `uptime`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/TimeUtils.java`
#### Snippet
```java
         return s;
      }
      uptime /= 60;
      if (uptime < 24) {
         long hours = (long) uptime;
```

### AssignmentToMethodParameter
Assignment to method parameter `uptime`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/TimeUtils.java`
#### Snippet
```java
         return s;
      }
      uptime /= 24;
      long days = (long) uptime;
      long hours = (long) ((uptime - days) * 24);
```

### AssignmentToMethodParameter
Assignment to method parameter `ipString`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/InetAddresses.java`
#### Snippet
```java
      if (hasColon) {
         if (hasDot) {
            ipString = convertDottedQuadToHex(ipString);
            if (ipString == null) {
               return null;
```

### AssignmentToMethodParameter
Assignment to method parameter `h`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/ByteUtil.java`
#### Snippet
```java
      // then a plain loop, given that we know no loop is needed here
      assert bytesCount > 0 && bytesCount < 4;
      h = 31 * h + PlatformDependent.getByte(bytes, index);
      if (bytesCount == 1) {
         return h;
```

### AssignmentToMethodParameter
Assignment to method parameter `h`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/ByteUtil.java`
#### Snippet
```java
         return h;
      }
      h = 31 * h + PlatformDependent.getByte(bytes, index + 1);
      if (bytesCount == 2) {
         return h;
```

### AssignmentToMethodParameter
Assignment to method parameter `h`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/ByteUtil.java`
#### Snippet
```java
         return h;
      }
      h = 31 * h + PlatformDependent.getByte(bytes, index + 2);
      return h;
   }
```

### AssignmentToMethodParameter
Assignment to method parameter `loader`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/ClassloadingUtil.java`
#### Snippet
```java
      }

      loader = Thread.currentThread().getContextClassLoader();
      if (loader == null)
         return null;
```

### AssignmentToMethodParameter
Assignment to method parameter `analyzer`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/critical/CriticalComponentImpl.java`
#### Snippet
```java
   public CriticalComponentImpl(CriticalAnalyzer analyzer, int numberOfPaths) {
      if (analyzer == null) {
         analyzer = EmptyCriticalAnalyzer.getInstance();
      }
      this.analyzer = analyzer;
```

### AssignmentToMethodParameter
Assignment to method parameter `uri`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/uri/URISchema.java`
#### Snippet
```java
      URI defaultFactory = getDefaultURI();
      if (defaultFactory != null && uri.getPort() < 0 && defaultFactory.getScheme().equals(uri.getScheme())) {
         uri = defaultFactory;
      }
      return uri.getPort();
```

### AssignmentToMethodParameter
Assignment to method parameter `uri`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/uri/URISchema.java`
#### Snippet
```java
      URI defaultFactory = getDefaultURI();
      if (defaultFactory != null && uri.getHost() == null && defaultFactory.getScheme().equals(uri.getScheme())) {
         uri = defaultFactory;
      }
      return uri.getHost();
```

### AssignmentToMethodParameter
Assignment to method parameter `expectedItems`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/ConcurrentLongHashSet.java`
#### Snippet
```java
      checkArgument(numSections > 0);
      if (expectedItems < numSections) {
         expectedItems = numSections;
      }

```

### AssignmentToMethodParameter
Assignment to method parameter `elementNumber`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/ConcurrentAppendOnlyChunkedList.java`
#### Snippet
```java
         assert e != null;
         elements[elementNumber] = e;
         elementNumber++;
      }
   }
```

### AssignmentToMethodParameter
Assignment to method parameter `expectedItems`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/ConcurrentLongHashMap.java`
#### Snippet
```java
      checkArgument(numSections > 0);
      if (expectedItems < numSections) {
         expectedItems = numSections;
      }

```

### AssignmentToMethodParameter
Assignment to method parameter `deleteIndex`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/LongHashSet.java`
#### Snippet
```java

            values[index] = MISSING_VALUE;
            deleteIndex = index;
         }
      }
```

### AssignmentToMethodParameter
Assignment to method parameter `criticalAnalyzer`
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/io/AbstractSequentialFileFactory.java`
#### Snippet
```java

      if (criticalAnalyzer == null) {
         criticalAnalyzer = EmptyCriticalAnalyzer.getInstance();
      }

```

### AssignmentToMethodParameter
Assignment to method parameter `uri`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/uri/URISupport.java`
#### Snippet
```java
   public static Map<String, String> parseQuery(String uri) throws URISyntaxException {
      try {
         uri = uri.substring(uri.lastIndexOf("?") + 1); // get only the relevant part of the query
         Map<String, String> rc = new HashMap<>();
         if (uri != null && !uri.isEmpty()) {
```

### AssignmentToMethodParameter
Assignment to method parameter `uri`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/uri/URISupport.java`
#### Snippet
```java
            }
         }
         uri = createURIWithQuery(uri, newQuery.toString());
      }
      return uri;
```

### AssignmentToMethodParameter
Assignment to method parameter `capacity`
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/io/mapped/MappedFile.java`
#### Snippet
```java
            logger.debug("Adjusting capacity to {} while it was {} on file {}", length, capacity, file);
         }
         capacity = length;
      }
      buffer = channel.map(FileChannel.MapMode.READ_WRITE, position, capacity);
```

### AssignmentToMethodParameter
Assignment to method parameter `position`
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/io/mapped/MappedFile.java`
#### Snippet
```java
      }
      //do not move this.position: only this.length can be changed
      position += count;
      if (position > this.length) {
         this.length = position;
```

### AssignmentToMethodParameter
Assignment to method parameter `message`
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/ActiveMQRAMessageListener.java`
#### Snippet
```java
      logger.trace("onMessage({})", message);

      message = consumer.wrapMessage(message);
      listener.onMessage(message);
   }
```

### AssignmentToMethodParameter
Assignment to method parameter `position`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/LinkedListImpl.java`
#### Snippet
```java
            return true;
         }
         position = fetching;
         fetching = fetching.next;
      }
```

### AssignmentToMethodParameter
Assignment to method parameter `transacted`
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/ActiveMQRASessionFactoryImpl.java`
#### Snippet
```java
            // then it should not be included in any JTA transaction and behave like that there is no JTA transaction.
            if (!mcf.isIgnoreJTA() && (inJtaTransaction() || mcf.isInJtaTransaction())) {
               transacted = true;
               //from getAcknowledgeMode
               // If the session is transacted, returns SESSION_TRANSACTED.
```

### AssignmentToMethodParameter
Assignment to method parameter `acknowledgeMode`
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/ActiveMQRASessionFactoryImpl.java`
#### Snippet
```java
               //from getAcknowledgeMode
               // If the session is transacted, returns SESSION_TRANSACTED.
               acknowledgeMode = Session.SESSION_TRANSACTED;
            } else {
               //In the Java EE web or EJB container, when there is no active JTA transaction in progress
```

### AssignmentToMethodParameter
Assignment to method parameter `acknowledgeMode`
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/ActiveMQRASessionFactoryImpl.java`
#### Snippet
```java
               //The session will always be non-transacted, unless allow-local-transactions is true
               if (transacted && mcf.isAllowLocalTransactions()) {
                  acknowledgeMode = Session.SESSION_TRANSACTED;
               } else {
                  transacted = false;
```

### AssignmentToMethodParameter
Assignment to method parameter `transacted`
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/ActiveMQRASessionFactoryImpl.java`
#### Snippet
```java
                  acknowledgeMode = Session.SESSION_TRANSACTED;
               } else {
                  transacted = false;
                  switch (acknowledgeMode) {
                     //using one of the two acknowledgement modes AUTO_ACKNOWLEDGE and DUPS_OK_ACKNOWLEDGE.
```

### AssignmentToMethodParameter
Assignment to method parameter `transacted`
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/ActiveMQRASessionFactoryImpl.java`
#### Snippet
```java
                           throw ActiveMQRABundle.BUNDLE.invalidSessionTransactedModeRuntimeAllowLocal();
                        }
                        transacted = true;
                        break;
                     default:
```

### AssignmentToMethodParameter
Assignment to method parameter `acceptor`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/ActionAbstract.java`
#### Snippet
```java

            if (acceptor == null) {
               acceptor = DEFAULT_BROKER_ACCEPTOR;
            }

```

### AssignmentToMethodParameter
Assignment to method parameter `etcFolder`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/Artemis.java`
#### Snippet
```java
      // using a default etc in case that is not set in the variables
      if (etcFolder == null && artemisInstance != null) {
         etcFolder = new File(artemisInstance, "etc");
      }

```

### AssignmentToMethodParameter
Assignment to method parameter `etcFolder`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Upgrade.java`
#### Snippet
```java
         etcLine = etcLine.trim();
         etcLine = etcLine.substring(pattern.length() + 1, etcLine.length() - 1);
         etcFolder = new File(etcLine);
         context.out.println("ETC found at " + etcFolder);
      }
```

### AssignmentToMethodParameter
Assignment to method parameter `content`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/InstallAbstract.java`
#### Snippet
```java
      if (filters != null) {
         for (Map.Entry<String, String> entry : filters.entrySet()) {
            content = replace(content, entry.getKey(), entry.getValue());
         }
      }
```

### AssignmentToMethodParameter
Assignment to method parameter `blockSize`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/util/SyncCalculation.java`
#### Snippet
```java

         // making sure the blockSize matches the device
         blockSize = bufferBlock.remaining();

         file.delete();
```

### AssignmentToMethodParameter
Assignment to method parameter `journalType`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/util/SyncCalculation.java`
#### Snippet
```java

      if (journalType == JournalType.ASYNCIO && !LibaioContext.isLoaded()) {
         journalType = JournalType.NIO;
      }

```

### AssignmentToMethodParameter
Assignment to method parameter `chunkBytes`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/xml/XMLMessageExporter.java`
#### Snippet
```java
   private static ByteBuffer acquireHeapBodyBuffer(ByteBuffer chunkBytes, int requiredCapacity) {
      if (chunkBytes == null || chunkBytes.capacity() != requiredCapacity) {
         chunkBytes = ByteBuffer.allocate(requiredCapacity);
      } else {
         chunkBytes.clear();
```

### AssignmentToMethodParameter
Assignment to method parameter `depth`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/xml/XmlDataExporter.java`
#### Snippet
```java

      private String indent(int depth) {
         depth *= 3; // level of indentation
         char[] output = new char[depth];
         while (depth-- > 0) {
```

### AssignmentToMethodParameter
Assignment to method parameter `depth`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/xml/XmlDataExporter.java`
#### Snippet
```java
         depth *= 3; // level of indentation
         char[] output = new char[depth];
         while (depth-- > 0) {
            output[depth] = INDENT_CHAR;
         }
```

### AssignmentToMethodParameter
Assignment to method parameter `type`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Create.java`
#### Snippet
```java

      public static SecurityManagerType getType(String type) {
         type = type.toLowerCase();
         switch (type) {
            case "jaas":
```

### AssignmentToMethodParameter
Assignment to method parameter `brokerURL`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/ConnectionAbstract.java`
#### Snippet
```java
      if (brokerURL.startsWith("tcp://")) {
         // replacing tcp:// by amqp://
         brokerURL = "amqp" + brokerURL.substring(3);
      }
      JmsConnectionFactory cf = new JmsConnectionFactory(user, password, brokerURL);
```

### AssignmentToMethodParameter
Assignment to method parameter `warmingUp`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/perf/PerfCommand.java`
#### Snippet
```java
            final long tick = System.currentTimeMillis();
            if (tick - endWarmup >= 0) {
               warmingUp = false;
            }
         }
```

### AssignmentToMethodParameter
Assignment to method parameter `brokerURL`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/Transfer.java`
#### Snippet
```java
      if (brokerURL.startsWith("tcp://")) {
         // replacing tcp:// by amqp://
         brokerURL = "amqp" + brokerURL.substring(3);
      }
      JmsConnectionFactory cf = new JmsConnectionFactory(user, password, brokerURL);
```

### AssignmentToMethodParameter
Assignment to method parameter `brokerURL`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/Transfer.java`
#### Snippet
```java
         // if a connection exception will ask for the URL, user and password
         getActionContext().err.println("Connection failed::" + e.getMessage());
         brokerURL = input("--url", "Type in the broker URL for a retry (e.g. tcp://localhost:61616)", brokerURL);
         userPassword(brokerURL);
         cf = new JmsConnectionFactory(user, password, brokerURL);
```

### AssignmentToMethodParameter
Assignment to method parameter `brokerURL`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/Transfer.java`
#### Snippet
```java
         // if a connection exception will ask for the URL, user and password
         getActionContext().err.println("Connection failed::" + e.getMessage());
         brokerURL = input("--url", "Type in the broker URL for a retry (e.g. tcp://localhost:61616)", brokerURL);
         Pair<String, String> userPair = userPassword(brokerURL);
         cf = new ActiveMQConnectionFactory(brokerURL, userPair.getA(), userPair.getB());
```

### AssignmentToMethodParameter
Assignment to method parameter `completionError`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/perf/SkeletalProducerLoadGenerator.java`
#### Snippet
```java
            scheduleProcessingCompletions();
         } catch (final JMSException jmsException) {
            completionError = jmsException;
         }
      }
```

### AssignmentToMethodParameter
Assignment to method parameter `newLine`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/util/JVMArgumentParser.java`
#### Snippet
```java
   public static String parseNewLine(String prefix, String endOfLine, String newLine, String[] keepingPrefixes, Map<String, String> originalArgs) {
      String spacesBeginning = newLine.substring(0, newLine.indexOf(prefix));
      newLine = newLine.trim();
      StringBuffer output = new StringBuffer();
      String line = newLine.substring(prefix.length(), newLine.length() - endOfLine.length());
```

### AssignmentToMethodParameter
Assignment to method parameter `originalLine`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/util/JVMArgumentParser.java`
#### Snippet
```java

   public static void parseOriginalArgs(String prefix, String endOfLine, String originalLine, String[] keepingPrefixes, Map<String, String> originalArgs) {
      originalLine = originalLine.trim();
      String line = originalLine.substring(prefix.length(), originalLine.length() - endOfLine.length());
      String[] split = line.split(" ");
```

### AssignmentToMethodParameter
Assignment to method parameter `originalConnectionId`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenWireProtocolManager.java`
#### Snippet
```java

      if (originalConnectionId == null) {
         originalConnectionId = context.getConnectionId().getValue();
      }
      advisoryMessage.setStringProperty(MessageUtil.CONNECTION_ID_PROPERTY_NAME.toString(), originalConnectionId);
```

### AssignmentToMethodParameter
Assignment to method parameter `n`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/amq/AMQConsumer.java`
#### Snippet
```java
      } else if (deliveredAcksCreditExtension > 0) {
         if (deliveredAcksCreditExtension < n) {
            n -= deliveredAcksCreditExtension;
            deliveredAcksCreditExtension = 0;
         } else {
```

### AssignmentToMethodParameter
Assignment to method parameter `contents`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenWireMessageConverter.java`
#### Snippet
```java
                                        final ActiveMQBuffer body) throws IOException {
      if (messageCompressed) {
         contents = writeCompressedDefaultType(contents);
      }
      body.writeBytes(contents.data, contents.offset, contents.length);
```

### AssignmentToMethodParameter
Assignment to method parameter `contents`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenWireMessageConverter.java`
#### Snippet
```java
                                       final ActiveMQBuffer body) throws IOException {
      if (messageCompressed) {
         contents = writeCompressedObjectType(contents);
      }
      body.writeInt(contents.length);
```

### AssignmentToMethodParameter
Assignment to method parameter `contents`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenWireMessageConverter.java`
#### Snippet
```java
                                      final ActiveMQBuffer body) throws IOException {
      if (messageCompressed) {
         contents = writeCompressedBytesType(contents);
      }
      body.writeBytes(contents.data, contents.offset, contents.length);
```

### AssignmentToMethodParameter
Assignment to method parameter `address`
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTSessionState.java`
#### Snippet
```java

   public List<Integer> getMatchingSubscriptionIdentifiers(String address) {
      address = MQTTUtil.convertCoreAddressToMqttTopicFilter(address, session.getServer().getConfiguration().getWildcardConfiguration());
      List<Integer> result = null;
      for (Pair<MqttTopicSubscription, Integer> pair : subscriptions.values()) {
```

### AssignmentToMethodParameter
Assignment to method parameter `sessionPresent`
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTProtocolHandler.java`
#### Snippet
```java
      // 5     - [MQTT-3.2.2-6] If a Server sends a CONNACK packet containing a non-zero Reason Code it MUST set Session Present to 0.
      if (returnCode != MQTTReasonCodes.SUCCESS) {
         sessionPresent = false;
      }
      sendToClient(MqttMessageBuilders
```

### AssignmentToMethodParameter
Assignment to method parameter `vbi`
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTUtil.java`
#### Snippet
```java
      int count = 0;
      do {
         vbi /= 128;
         count++;
      }
```

### AssignmentToMethodParameter
Assignment to method parameter `address`
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTUtil.java`
#### Snippet
```java

      if (address.startsWith(MQTT_RETAIN_ADDRESS_PREFIX)) {
         address = address.substring(MQTT_RETAIN_ADDRESS_PREFIX.length());
      }

```

### AssignmentToMethodParameter
Assignment to method parameter `str`
in `artemis-dto/src/main/java/org/apache/activemq/artemis/dto/XmlUtil.java`
#### Snippet
```java
            String property = props.getProperty(group);
            if (property != null) {
               str = matcher.replaceFirst(Matcher.quoteReplacement(property));
            } else {
               start = matcher.end();
```

### AssignmentToMethodParameter
Assignment to method parameter `name`
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalImpl.java`
#### Snippet
```java
    */
   protected static String renameExtensionFile(String name, final String extension) {
      name = name.substring(0, name.lastIndexOf(extension));
      return name;
   }
```

### AssignmentToMethodParameter
Assignment to method parameter `acknowledgeMode`
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQConnection.java`
#### Snippet
```java
                                                         final int type) throws JMSException {
      if (transacted) {
         acknowledgeMode = Session.SESSION_TRANSACTED;
      }

```

### AssignmentToMethodParameter
Assignment to method parameter `filterString`
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQSession.java`
#### Snippet
```java
      }
      if ("".equals(filterString)) {
         filterString = null;
      }

```

### AssignmentToMethodParameter
Assignment to method parameter `selectorString`
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQSession.java`
#### Snippet
```java
         }

         selectorString = "".equals(selectorString) ? null : selectorString;

         SimpleString coreFilterString = null;
```

### AssignmentToMethodParameter
Assignment to method parameter `messageSelector`
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQSession.java`
#### Snippet
```java
      }
      if ("".equals(messageSelector)) {
         messageSelector = null;
      }

```

### AssignmentToMethodParameter
Assignment to method parameter `selectorString`
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQSession.java`
#### Snippet
```java
                                                  ConsumerDurability durability) throws JMSException {
      try {
         selectorString = "".equals(selectorString) ? null : selectorString;

         if (noLocal) {
```

### AssignmentToMethodParameter
Assignment to method parameter `selectorString`
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQSession.java`
#### Snippet
```java

            if (selectorString != null) {
               selectorString += " AND " + filter;
            } else {
               selectorString = filter;
```

### AssignmentToMethodParameter
Assignment to method parameter `selectorString`
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQSession.java`
#### Snippet
```java
               selectorString += " AND " + filter;
            } else {
               selectorString = filter;
            }
         }
```

### AssignmentToMethodParameter
Assignment to method parameter `destination`
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQMessageProducer.java`
#### Snippet
```java
         }

         destination = defaultDestination;
         // address is meant to be null on this case, as it will use the coreProducer's default address
      } else {
```

### AssignmentToMethodParameter
Assignment to method parameter `exception`
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQMessageProducer.java`
#### Snippet
```java
            producer.connection.getThreadAwareContext().setCurrentThread(true);
            if (exception instanceof ActiveMQException) {
               exception = JMSExceptionHelper.convertFromActiveMQException((ActiveMQException) exception);
            } else if (exception instanceof ActiveMQInterruptedException) {
               exception = JMSExceptionHelper.convertFromActiveMQException((ActiveMQInterruptedException) exception);
```

### AssignmentToMethodParameter
Assignment to method parameter `exception`
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQMessageProducer.java`
#### Snippet
```java
               exception = JMSExceptionHelper.convertFromActiveMQException((ActiveMQException) exception);
            } else if (exception instanceof ActiveMQInterruptedException) {
               exception = JMSExceptionHelper.convertFromActiveMQException((ActiveMQInterruptedException) exception);
            }
            completionListener.onException(jmsMessage, exception);
```

### AssignmentToMethodParameter
Assignment to method parameter `record`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessagePersister.java`
#### Snippet
```java
         address = SimpleString.readNullableSimpleString(buffer.byteBuf(), pool.getAddressDecoderPool());
      }
      record = new AMQPStandardMessage(format);
      record.reloadPersistence(buffer, pool);
      record.setMessageID(id);
```

### AssignmentToMethodParameter
Assignment to method parameter `filter`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPSessionCallback.java`
#### Snippet
```java
      long consumerID = consumerIDGenerator.generateID();

      filter = SelectorTranslator.convertToActiveMQFilterString(filter);

      int priority = getPriority(protonSender.getSender().getRemoteProperties());
```

### AssignmentToMethodParameter
Assignment to method parameter `address`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPSessionCallback.java`
#### Snippet
```java
      } else {
         // Anonymous-relay producer, message must carry a To value
         address = message.getAddressSimpleString();
         if (address == null) {
            // Errors are not currently handled as required by AMQP 1.0 anonterm-v1.0
```

### AssignmentToMethodParameter
Assignment to method parameter `transaction`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPSessionCallback.java`
#### Snippet
```java
   public void ack(Transaction transaction, Object brokerConsumer, Message message) throws Exception {
      if (transaction == null) {
         transaction = serverSession.getCurrentTransaction();
      }
      OperationContext oldContext = recoverContext();
```

### AssignmentToMethodParameter
Assignment to method parameter `value`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
   protected void setMessageAnnotation(Symbol annotation, Object value) {
      if (value instanceof SimpleString) {
         value = value.toString();
      }
      getMessageAnnotationsMap(true).put(annotation, value);
```

### AssignmentToMethodParameter
Assignment to method parameter `key`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
   public Object getObjectPropertyForFilter(SimpleString key) {
      if (key.startsWith(ANNOTATION_AREA_PREFIX)) {
         key = key.subSeq(ANNOTATION_AREA_PREFIX.length(), key.length());
         return getAnnotation(key);
      }
```

### AssignmentToMethodParameter
Assignment to method parameter `serverID`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/connect/mirror/ReferenceNodeStore.java`
#### Snippet
```java
   private synchronized LongObjectHashMap<LinkedListImpl.Node<MessageReference>> getMap(String serverID) {
      if (serverID == null) {
         serverID = this.serverID; // returning for the localList in case it's null
      }

```

### AssignmentToMethodParameter
Assignment to method parameter `destinationType`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/converter/AMQPMessageSupport.java`
#### Snippet
```java

      if (destinationType == null) {
         destinationType = RoutingType.ANYCAST;
      }

```

### AssignmentToMethodParameter
Assignment to method parameter `sqls`
in `artemis-jdbc-store/src/main/java/org/apache/activemq/artemis/jdbc/store/drivers/AbstractJDBCDriver.java`
#### Snippet
```java
                     return;
                  } else {
                     sqls = Stream.of(sqls).filter(sql -> {
                        final String upperCaseSql = sql.toUpperCase();
                        return !(upperCaseSql.contains("CREATE TABLE") || upperCaseSql.contains("CREATE INDEX"));
```

### AssignmentToMethodParameter
Assignment to method parameter `maxRetries`
in `artemis-web/src/main/java/org/apache/activemq/artemis/component/WebTmpCleaner.java`
#### Snippet
```java
   private static boolean cleanupFilesWithRetry(String[] filesToClean, int maxRetries) throws Exception {
      boolean allCleaned = false;
      while (!allCleaned && maxRetries-- > 0) {
         allCleaned = true;
         for (String f : filesToClean) {
```

### AssignmentToMethodParameter
Assignment to method parameter `componentStatus`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/JsonUtil.java`
#### Snippet
```java
         JsonObjectBuilder nestedBuilder = JsonLoader.createObjectBuilder();
         nestedBuilder.add(nestedComponents[i], componentStatus);
         componentStatus = nestedBuilder.build();
      }
      jsonObjectBuilder.add(nestedComponents[0], componentStatus);
```

### AssignmentToMethodParameter
Assignment to method parameter `sendingAddress`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ClientProducerImpl.java`
#### Snippet
```java
                       final SendAcknowledgementHandler handler) throws ActiveMQException {
      if (sendingAddress == null) {
         sendingAddress = this.address;
      }
      session.startCall();
```

### AssignmentToMethodParameter
Assignment to method parameter `handler`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ClientProducerImpl.java`
#### Snippet
```java

      if (handler != null) {
         handler =  session.wrap(handler);
      }

```

### AssignmentToMethodParameter
Assignment to method parameter `index`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/CompressedLargeMessageControllerImpl.java`
#### Snippet
```java
      // TODO: optimize this by using System.arraycopy
      for (int i = 0; i < dst.length; i++) {
         dst[i] = getByte(index++);
      }
   }
```

### AssignmentToMethodParameter
Assignment to method parameter `index`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/LargeMessageControllerImpl.java`
#### Snippet
```java
      // TODO: optimize this by using System.arraycopy
      for (int i = 0; i < dst.length; i++) {
         dst[i] = getByte(index++);
      }
   }
```

### AssignmentToMethodParameter
Assignment to method parameter `index`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/LargeMessageControllerImpl.java`
#### Snippet
```java
      // TODO: optimize this by using System.arraycopy
      for (int i = 0; i < dst.length; i++) {
         dst[i] = getByte(index++);
      }
   }
```

### AssignmentToMethodParameter
Assignment to method parameter `writerIndex`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/buffers/impl/ResetLimitWrappedActiveMQBuffer.java`
#### Snippet
```java

      if (writerIndex < limit) {
         writerIndex = limit;
      }

```

### AssignmentToMethodParameter
Assignment to method parameter `readerIndex`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/buffers/impl/ResetLimitWrappedActiveMQBuffer.java`
#### Snippet
```java

      if (readerIndex < limit) {
         readerIndex = limit;
      }

```

### AssignmentToMethodParameter
Assignment to method parameter `readerIndex`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/buffers/impl/ResetLimitWrappedActiveMQBuffer.java`
#### Snippet
```java

      if (readerIndex < limit) {
         readerIndex = limit;
      }
      if (writerIndex < limit) {
```

### AssignmentToMethodParameter
Assignment to method parameter `writerIndex`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/buffers/impl/ResetLimitWrappedActiveMQBuffer.java`
#### Snippet
```java
      }
      if (writerIndex < limit) {
         writerIndex = limit;
      }
      buffer.setIndex(readerIndex, writerIndex);
```

### AssignmentToMethodParameter
Assignment to method parameter `record`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/message/impl/CoreMessagePersister.java`
#### Snippet
```java
         address = SimpleString.readNullableSimpleString(buffer.byteBuf(), pool.getAddressDecoderPool());
      }
      record = new CoreMessage();
      record.reloadPersistence(buffer, pool);
      record.setMessageID(id);
```

### AssignmentToMethodParameter
Assignment to method parameter `handler`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ClientSessionImpl.java`
#### Snippet
```java
   public SendAcknowledgementHandler wrap(SendAcknowledgementHandler handler) {
      if (!(handler instanceof SendAcknowledgementHandlerWrapper)) {
         handler = new SendAcknowledgementHandlerWrapper(handler, confirmationExecutor);
      }
      return handler;
```

### AssignmentToMethodParameter
Assignment to method parameter `buffer`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/message/impl/CoreMessage.java`
#### Snippet
```java
      qbuff.resetWriterIndex();
      qbuff.writeBytes(data);
      buffer = qbuff;
      return buffer;
   }
```

### AssignmentToMethodParameter
Assignment to method parameter `offset`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/utils/DeflaterReader.java`
#### Snippet
```java
         } else {
            read += n;
            offset += n;
            len -= n;
         }
```

### AssignmentToMethodParameter
Assignment to method parameter `len`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/utils/DeflaterReader.java`
#### Snippet
```java
            read += n;
            offset += n;
            len -= n;
         }
      }
```

### AssignmentToMethodParameter
Assignment to method parameter `offset`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/utils/InflaterReader.java`
#### Snippet
```java
                  if (n > 0) {
                     read += n;
                     offset += n;
                     len -= n;
                  }
```

### AssignmentToMethodParameter
Assignment to method parameter `len`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/utils/InflaterReader.java`
#### Snippet
```java
                     read += n;
                     offset += n;
                     len -= n;
                  }
               }
```

### AssignmentToMethodParameter
Assignment to method parameter `offset`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/utils/InflaterReader.java`
#### Snippet
```java
         } else {
            read += n;
            offset += n;
            len -= n;
         }
```

### AssignmentToMethodParameter
Assignment to method parameter `len`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/utils/InflaterReader.java`
#### Snippet
```java
            read += n;
            offset += n;
            len -= n;
         }
      }
```

### AssignmentToMethodParameter
Assignment to method parameter `xml`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/utils/XMLUtil.java`
#### Snippet
```java
         // interesting choice to replace with val == "" with no match!
         logger.debug("replacing {} with {}", subString, sysProp);
         xml = xml.replace(subString, sysProp);
      }

```

### AssignmentToMethodParameter
Assignment to method parameter `s`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/utils/XMLUtil.java`
#### Snippet
```java

   public static String stripCDATA(String s) {
      s = s.trim();
      if (s.startsWith("<![CDATA[")) {
         s = s.substring(9);
```

### AssignmentToMethodParameter
Assignment to method parameter `s`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/utils/XMLUtil.java`
#### Snippet
```java
      s = s.trim();
      if (s.startsWith("<![CDATA[")) {
         s = s.substring(9);
         int i = s.indexOf("]]>");
         if (i == -1) {
```

### AssignmentToMethodParameter
Assignment to method parameter `s`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/utils/XMLUtil.java`
#### Snippet
```java
            throw new IllegalStateException("argument starts with <![CDATA[ but cannot find pairing ]]>");
         }
         s = s.substring(0, i);
      }
      return s;
```

### AssignmentToMethodParameter
Assignment to method parameter `remainingBytes`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/reader/StreamMessageUtil.java`
#### Snippet
```java
            throw new IllegalStateException("Invalid conversion");
         }
         remainingBytes = buff.readInt();
      }
      int read = Math.min(value.length, remainingBytes);
```

### AssignmentToMethodParameter
Assignment to method parameter `remainingBytes`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/reader/StreamMessageUtil.java`
#### Snippet
```java
      int read = Math.min(value.length, remainingBytes);
      buff.readBytes(value, 0, read);
      remainingBytes -= read;
      if (remainingBytes == 0) {
         remainingBytes = -1;
```

### AssignmentToMethodParameter
Assignment to method parameter `remainingBytes`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/reader/StreamMessageUtil.java`
#### Snippet
```java
      remainingBytes -= read;
      if (remainingBytes == 0) {
         remainingBytes = -1;
      }
      return new Pair<>(remainingBytes, read);
```

### AssignmentToMethodParameter
Assignment to method parameter `plainTextPassword`
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/security/jaas/SCRAMPropertiesLoginModule.java`
#### Snippet
```java
         byte[] randomPassword = new byte[256];
         RANDOM_GENERATOR.nextBytes(randomPassword);
         plainTextPassword = new String(randomPassword);
      }
      DigestCallback digestCallback = new DigestCallback();
```

### AssignmentToMethodParameter
Assignment to method parameter `entryName`
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/security/jaas/PropertiesLoginModuleConfigurator.java`
#### Snippet
```java
   public PropertiesLoginModuleConfigurator(String entryName, String brokerEtc) throws Exception {
      if (entryName == null || entryName.length() == 0) {
         entryName = "activemq";
      }

```

### AssignmentToMethodParameter
Assignment to method parameter `s`
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/security/scram/StringPrep.java`
#### Snippet
```java
      // another string (eg. case folding) can't be applied sequentially like
      // this.
      s = applyMapTo(s, B1, "");
      s = applyMapTo(s, C12, " ");

```

### AssignmentToMethodParameter
Assignment to method parameter `s`
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/security/scram/StringPrep.java`
#### Snippet
```java
      // this.
      s = applyMapTo(s, B1, "");
      s = applyMapTo(s, C12, " ");

      // 2) Normalize
```

### AssignmentToMethodParameter
Assignment to method parameter `s`
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/security/scram/StringPrep.java`
#### Snippet
```java
      // 2) Normalize
      // rfc4013: 2.2. Normalization
      s = Normalizer.normalize(s, Normalizer.Form.NFKC);

      // 3) Prohibit
```

### AssignmentToMethodParameter
Assignment to method parameter `s`
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/security/scram/StringPrep.java`
#### Snippet
```java
   /** Apply SASLPrep and return the result. {@code} is treated as a stored string. */
   public static String prepAsStoredString(String s) throws StringPrepError {
      s = prepAsQueryString(s);

      // rfc3454: 7. Unassigned Code Points in Stringprep Profiles
```

### AssignmentToMethodParameter
Assignment to method parameter `uri`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/ConfigurationUtils.java`
#### Snippet
```java
      try {
         // remove all whitespace
         uri = uri.replaceAll("\\s", "");

         AcceptorTransportConfigurationParser parser = new AcceptorTransportConfigurationParser();
```

### AssignmentToMethodParameter
Assignment to method parameter `uri`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/ConfigurationUtils.java`
#### Snippet
```java
      try {
         // remove all whitespace
         uri = uri.replaceAll("\\s", "");

         ConnectorTransportConfigurationParser parser = new ConnectorTransportConfigurationParser();
```

### AssignmentToMethodParameter
Assignment to method parameter `fileBuffer`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/impl/PageReadWriter.java`
#### Snippet
```java

         } else {
            fileBuffer = allocateAndReadIntoFileBuffer(fileFactory, file, fileBuffer, requiredBytes, direct);
         }
      }
```

### AssignmentToMethodParameter
Assignment to method parameter `fileBuffer`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/impl/PageReadWriter.java`
#### Snippet
```java
      }

      fileBuffer = newFileBuffer;

      //move the limit to allow reading as much as possible from the file
```

### AssignmentToMethodParameter
Assignment to method parameter `tx`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerConsumerImpl.java`
#### Snippet
```java

         startedTransaction = true;
         tx = new TransactionImpl(storageManager);
      }

```

### AssignmentToMethodParameter
Assignment to method parameter `tx`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerConsumerImpl.java`
#### Snippet
```java
      if (tx == null) {
         startedTransaction = true;
         tx = new TransactionImpl(storageManager);
      }

```

### AssignmentToMethodParameter
Assignment to method parameter `globalThreadMaxPoolSize`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/client/ActiveMQClient.java`
#### Snippet
```java

      if (globalThreadMaxPoolSize < 2 && globalThreadMaxPoolSize != -1)
         globalThreadMaxPoolSize = 2;

      ActiveMQClient.globalScheduledThreadPoolSize = globalScheduledThreadPoolSize;
```

### AssignmentToMethodParameter
Assignment to method parameter `targetAddressParameter`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/replay/ReplayManager.java`
#### Snippet
```java

      if (targetAddressParameter == null || targetAddressParameter.trim().isEmpty()) {
         targetAddressParameter = sourceAddress;
      }

```

### AssignmentToMethodParameter
Assignment to method parameter `filterString`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/impl/ClusterConnectionBridge.java`
#### Snippet
```java
   private String appendIgnoresToFilter(String filterString) {
      if (filterString != null && !filterString.isEmpty()) {
         filterString += ",";
      }
      filterString += "!" + storeAndForwardPrefix;
```

### AssignmentToMethodParameter
Assignment to method parameter `filterString`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/impl/ClusterConnectionBridge.java`
#### Snippet
```java
         filterString += ",";
      }
      filterString += "!" + storeAndForwardPrefix;
      filterString += ",!" + managementAddress;
      //add any protocol specific ignored addresses
```

### AssignmentToMethodParameter
Assignment to method parameter `filterString`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/impl/ClusterConnectionBridge.java`
#### Snippet
```java
      }
      filterString += "!" + storeAndForwardPrefix;
      filterString += ",!" + managementAddress;
      //add any protocol specific ignored addresses
      for (String ignoreAddress : clusterManager.getProtocolIgnoredAddresses()) {
```

### AssignmentToMethodParameter
Assignment to method parameter `filterString`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/impl/ClusterConnectionBridge.java`
#### Snippet
```java
      //add any protocol specific ignored addresses
      for (String ignoreAddress : clusterManager.getProtocolIgnoredAddresses()) {
         filterString += ",!" + ignoreAddress;
      }
      return filterString;
```

### AssignmentToMethodParameter
Assignment to method parameter `message`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/impl/BridgeImpl.java`
#### Snippet
```java
   /* Hook for processing message before forwarding */
   protected Message beforeForward(Message message, final SimpleString forwardingAddress) {
      message = message.copy();
      ((RefCountMessage)message).setParentRef((RefCountMessage)message);

```

### AssignmentToMethodParameter
Assignment to method parameter `key`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/routing/ConnectionRouter.java`
#### Snippet
```java
   public TargetResult getTarget(String key) {
      if (policy != null && !KeyResolver.NULL_KEY_VALUE.equals(key)) {
         key = policy.transformKey(key);
      }

```

### AssignmentToMethodParameter
Assignment to method parameter `recursionDepth`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/federation/FederationUpstream.java`
#### Snippet
```java
               FederationPolicySet federationPolicySet = (FederationPolicySet) policy;
               if (recursionDepth < 10) {
                  deployPolicyRefs(federationPolicySet.getPolicyRefs(), policyMap, ++recursionDepth);
               } else {
                  ActiveMQServerLogger.LOGGER.federationAvoidStackOverflowPolicyRef(name.toString(), policyRef);
```

### AssignmentToMethodParameter
Assignment to method parameter `validatedUser`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ActiveMQServerImpl.java`
#### Snippet
```java
                                      boolean isLegacyProducer) throws Exception {
      if (validatedUser == null) {
         validatedUser = validateUser(username, password, connection, securityDomain);
      }

```

### AssignmentToMethodParameter
Assignment to method parameter `configuration`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ActiveMQServerImpl.java`
#### Snippet
```java
                             final ServiceRegistry serviceRegistry) {
      if (configuration == null) {
         configuration = new ConfigurationImpl();
      } else {
         ConfigurationUtils.validateConfiguration(configuration);
```

### AssignmentToMethodParameter
Assignment to method parameter `mbeanServer`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ActiveMQServerImpl.java`
#### Snippet
```java
      if (mbeanServer == null) {
         // Just use JVM mbean server
         mbeanServer = ManagementFactory.getPlatformMBeanServer();
      }

```

### AssignmentToMethodParameter
Assignment to method parameter `name`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/impl/ConfigurationImpl.java`
#### Snippet
```java
                        throw new InvocationTargetException(null, "Resolved nested property for:" + name + ", on: " + bean + " was null");
                     }
                     name = resolver.remove(name);
                  } catch (final NoSuchMethodException e) {
                     throw new InvocationTargetException(e, "No getter for property:" + name + ", on: " + bean);
```

### AssignmentToMethodParameter
Assignment to method parameter `fileUrlToProperties`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/impl/ConfigurationImpl.java`
#### Snippet
```java
   public Configuration parseProperties(String fileUrlToProperties) throws Exception {
      // system property overrides location of file(s)
      fileUrlToProperties = resolvePropertiesSources(fileUrlToProperties);
      if (fileUrlToProperties != null) {
         for (String fileUrl : fileUrlToProperties.split(",")) {
```

### AssignmentToMethodParameter
Assignment to method parameter `method`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/impl/HawtioSecurityControlImpl.java`
#### Snippet
```java

   private String parseMethodName(String method, List<String> argTypes) {
      method = method.trim();
      int index = method.indexOf('(');
      if (index < 0) {
```

### AssignmentToMethodParameter
Assignment to method parameter `response`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/protocol/core/ServerSessionPacketHandler.java`
#### Snippet
```java
         logger.debug("Sending exception to client", e);
         if (packet.isResponseAsync()) {
            response = new SessionXAResponseMessage_V2(packet.getCorrelationID(), true, e.errorCode, e.getMessage());
         } else {
            response = new SessionXAResponseMessage(true, e.errorCode, e.getMessage());
```

### AssignmentToMethodParameter
Assignment to method parameter `response`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/protocol/core/ServerSessionPacketHandler.java`
#### Snippet
```java
            response = new SessionXAResponseMessage_V2(packet.getCorrelationID(), true, e.errorCode, e.getMessage());
         } else {
            response = new SessionXAResponseMessage(true, e.errorCode, e.getMessage());
         }
      } else {
```

### AssignmentToMethodParameter
Assignment to method parameter `response`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/protocol/core/ServerSessionPacketHandler.java`
#### Snippet
```java
      if (requiresResponse) {
         logger.debug("Sending exception to client", e);
         response = convertToExceptionPacket(packet, e);
      } else {
         ActiveMQServerLogger.LOGGER.caughtException(e);
```

### AssignmentToMethodParameter
Assignment to method parameter `response`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/protocol/core/ServerSessionPacketHandler.java`
#### Snippet
```java
      if (requiresResponse) {
         logger.debug("Sending exception to client", e);
         response = convertToExceptionPacket(packet, e);
      } else {
         ActiveMQServerLogger.LOGGER.caughtException(e);
```

### AssignmentToMethodParameter
Assignment to method parameter `response`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/protocol/core/ServerSessionPacketHandler.java`
#### Snippet
```java
      if (requiresResponse) {
         logger.debug("Sending exception to client", e);
         response = convertToExceptionPacket(packet, e);
      } else {
         if (e.getType() == ActiveMQExceptionType.QUEUE_EXISTS) {
```

### AssignmentToMethodParameter
Assignment to method parameter `response`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/protocol/core/ServerSessionPacketHandler.java`
#### Snippet
```java
         ActiveMQException activeMQInternalErrorException = new ActiveMQInternalErrorException();
         activeMQInternalErrorException.initCause(t);
         response = convertToExceptionPacket(packet, activeMQInternalErrorException);
      } else {
         ActiveMQServerLogger.LOGGER.caughtException(t);
```

### AssignmentToMethodParameter
Assignment to method parameter `message`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/impl/ManagementServiceImpl.java`
#### Snippet
```java
   @Override
   public ICoreMessage handleMessage(Message message) throws Exception {
      message = message.toCore();
      // a reply message is sent with the result stored in the message body.
      CoreMessage reply = new CoreMessage(storageManager.generateID(), 512);
```

### AssignmentToMethodParameter
Assignment to method parameter `position`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/postoffice/impl/BindingsImpl.java`
#### Snippet
```java

   private static int moveNextPosition(int position, final int length) {
      position++;

      if (position == length) {
```

### AssignmentToMethodParameter
Assignment to method parameter `position`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/postoffice/impl/BindingsImpl.java`
#### Snippet
```java

      if (position == length) {
         position = 0;
      }

```

### AssignmentToMethodParameter
Assignment to method parameter `maxBytesToSend`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/replication/ReplicationManager.java`
#### Snippet
```java
                  if (bytesRead >= maxBytesToSend) {
                     toSend = (int) maxBytesToSend;
                     maxBytesToSend = 0;
                  } else {
                     maxBytesToSend = maxBytesToSend - bytesRead;
```

### AssignmentToMethodParameter
Assignment to method parameter `maxBytesToSend`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/replication/ReplicationManager.java`
#### Snippet
```java
                     maxBytesToSend = 0;
                  } else {
                     maxBytesToSend = maxBytesToSend - bytesRead;
                  }
               }
```

### AssignmentToMethodParameter
Assignment to method parameter `refToAck`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java

      if (getMessageCountForRing() + adjustment > ringSize) {
         refToAck = refToAck == null ? messageReferences.poll() : refToAck;

         if (refToAck != null) {
```

## RuleId[id=PrimitiveArrayArgumentToVariableArgMethod]
### PrimitiveArrayArgumentToVariableArgMethod
Confusing primitive array argument to varargs method
in `artemis-jdbc-store/src/main/java/org/apache/activemq/artemis/jdbc/store/file/PostgresLargeObjectManager.java`
#### Snippet
```java
         try {
            Method method = largeObject.getClass().getMethod("write", byte[].class);
            method.invoke(largeObject, data);
         } catch (NoSuchMethodException | SecurityException | IllegalAccessException | IllegalArgumentException | InvocationTargetException ex) {
            throw new SQLException("Couldn't access org.postgresql.largeobject.LargeObject", ex);
```

## RuleId[id=SynchronizationOnLocalVariableOrMethodParameter]
### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on method parameter `connection`
in `artemis-protocols/artemis-stomp-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/stomp/StompProtocolManager.java`
#### Snippet
```java
      connection.logFrame(frame, false);

      synchronized (connection) {
         if (connection.isDestroyed()) {
            ActiveMQStompProtocolLogger.LOGGER.connectionClosed(connection);
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `properties`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/DefaultSensitiveStringCodec.java`
#### Snippet
```java
      Map<String, String> params = new HashMap<>();
      Properties properties = System.getProperties();
      synchronized (properties) {
         for (final String name : properties.stringPropertyNames()) {
            params.put(name, properties.getProperty(name));
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on method parameter `other`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/TypedProperties.java`
#### Snippet
```java

   public TypedProperties(final TypedProperties other) {
      synchronized (other) {
         properties = other.properties == null ? null : new HashMap<>(other.properties);
         size = other.size;
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `sessionState`
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTConnectionManager.java`
#### Snippet
```java
      boolean sessionPresent = session.getProtocolManager().getSessionStates().containsKey(clientId);
      MQTTSessionState sessionState = getSessionState(clientId);
      synchronized (sessionState) {
         session.setSessionState(sessionState);
         sessionState.setFailed(false);
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `nodesMap`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/connect/mirror/ReferenceNodeStore.java`
#### Snippet
```java
      LongObjectHashMap<LinkedListImpl.Node<MessageReference>> nodesMap = getMap(serverID);
      if (nodesMap != null) {
         synchronized (nodesMap) {
            nodesMap.put(id, node);
         }
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `nodeMap`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/connect/mirror/ReferenceNodeStore.java`
#### Snippet
```java
      assert nodeMap != null;

      synchronized (nodeMap) {
         return nodeMap.get(id);
      }
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `nodeMap`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/connect/mirror/ReferenceNodeStore.java`
#### Snippet
```java
      LongObjectHashMap<LinkedListImpl.Node<MessageReference>> nodeMap = getMap(serverID);
      if (nodeMap != null) {
         synchronized (nodeMap) {
            nodeMap.remove(id);
         }
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `consumerInternal`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ClientSessionImpl.java`
#### Snippet
```java

                     ClientConsumerInternal consumerInternal = entryx.getValue();
                     synchronized (consumerInternal) {
                        if (!consumerInternal.isClosed()) {
                           sessionContext.recreateConsumerOnServer(consumerInternal, entryx.getKey().getId(), started);
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on method parameter `other`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/message/impl/CoreMessage.java`
#### Snippet
```java
      // with getEncodedBuffer(), otherwise can introduce race condition when delivering concurrently to
      // many subscriptions and bridging to other nodes in a cluster
      synchronized (other) {
         this.endOfBodyPosition = other.endOfBodyPosition;
         internalSetMessageID(other.messageID);
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `properties`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/message/impl/CoreMessage.java`
#### Snippet
```java
         Long.BYTES +                                    // timestamp
         Byte.BYTES;                                     // priority
      synchronized (properties) {
         final int propertiesEncodeSize = properties.getEncodeSize();
         final int totalEncodedSize = headersSize + propertiesEncodeSize;
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `messageSynchronize`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionReceiveMessage_1X.java`
#### Snippet
```java
   public ActiveMQBuffer encode(final CoreRemotingConnection connection) {
      ICoreMessage messageSynchronize = message;
      synchronized (messageSynchronize) {
         // there's a possible race, in case non standard components touch the message body
         // between allocating and actual encoding there could be a difference in size
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `transaction`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java
      }
      if (transaction != null) {
         synchronized (transaction) {
            // Transaction might be committed/rolledback, we need to synchronize and judge state
            if (transaction.getState() != State.COMMITTED && transaction.getState() != State.ROLLEDBACK) {
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on method parameter `connection`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/jdbc/JdbcSharedStateManager.java`
#### Snippet
```java

   private UUID initializeOrReadNodeId(Connection connection, final UUID newNodeId) throws SQLException {
      synchronized (connection) {
         connection.setTransactionIsolation(Connection.TRANSACTION_SERIALIZABLE);
         final boolean autoCommit = connection.getAutoCommit();
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `postOfficeInUse`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ActiveMQServerImpl.java`
#### Snippet
```java
         return null;
      }
      synchronized (postOfficeInUse) {
         if (queueConfiguration.getName() == null || queueConfiguration.getName().length() == 0) {
            throw ActiveMQMessageBundle.BUNDLE.invalidQueueName(queueConfiguration.getName());
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on method parameter `properties`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/impl/ConfigurationImpl.java`
#### Snippet
```java
      Map<String, Object> beanProperties = new LinkedHashMap<>();
      long alder32Hash = 0;
      synchronized (properties) {
         Checksum checksum = getCheckSun();

```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on method parameter `tx`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java

   private RefsOperation getRefsOperation(final Transaction tx, AckReason ackReason, boolean ignoreRedlieveryCheck, boolean delivering) {
      synchronized (tx) {
         RefsOperation oper = (RefsOperation) tx.getProperty(TransactionPropertyIndexes.REFS_OPERATION);

```

## RuleId[id=ReturnNull]
### ReturnNull
Return of `null`
in `artemis-cdi-client/src/main/java/org/apache/artemis/client/cdi/extension/ArtemisClientConfigBean.java`
#### Snippet
```java
   @Override
   public String getName() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-cdi-client/src/main/java/org/apache/artemis/client/cdi/extension/ArtemisEmbeddedServerConfigBean.java`
#### Snippet
```java
   @Override
   public String getName() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/LogicExpression.java`
#### Snippet
```java
         }
         if (someNulls) {
            return null;
         }
         return Boolean.FALSE;
```

### ReturnNull
Return of `null`
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/LogicExpression.java`
#### Snippet
```java
         }
         if (someNulls) {
            return null;
         }
         return Boolean.TRUE;
```

### ReturnNull
Return of `null`
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/UnaryExpression.java`
#### Snippet
```java
            Object rvalue = right.evaluate(message);
            if (rvalue == null) {
               return null;
            }
            if (rvalue instanceof Number) {
```

### ReturnNull
Return of `null`
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/UnaryExpression.java`
#### Snippet
```java
               return negate((Number) rvalue);
            }
            return null;
         }

```

### ReturnNull
Return of `null`
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/UnaryExpression.java`
#### Snippet
```java
            Object rvalue = right.evaluate(message);
            if (rvalue == null) {
               return null;
            }
            if (rvalue.getClass() != String.class) {
```

### ReturnNull
Return of `null`
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/UnaryExpression.java`
#### Snippet
```java
            }
            if (rvalue.getClass() != String.class) {
               return null;
            }

```

### ReturnNull
Return of `null`
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/UnaryExpression.java`
#### Snippet
```java
            Object rvalue = right.evaluate(message);
            if (rvalue == null) {
               return null;
            }
            if (!rvalue.getClass().equals(Boolean.class)) {
```

### ReturnNull
Return of `null`
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/UnaryExpression.java`
#### Snippet
```java
            Boolean lvalue = (Boolean) right.evaluate(message);
            if (lvalue == null) {
               return null;
            }
            return !lvalue.booleanValue();
```

### ReturnNull
Return of `null`
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/ArithmeticExpression.java`
#### Snippet
```java
      Object lvalue = left.evaluate(message);
      if (lvalue == null) {
         return null;
      }
      Object rvalue = right.evaluate(message);
```

### ReturnNull
Return of `null`
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/ArithmeticExpression.java`
#### Snippet
```java
      Object rvalue = right.evaluate(message);
      if (rvalue == null) {
         return null;
      }
      return evaluate(lvalue, rvalue);
```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-hqclient-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/hornetq/HQFilterConversionInterceptor.java`
#### Snippet
```java
   public static String convertHQToActiveMQFilterString(final String hqFilterString) {
      if (hqFilterString == null) {
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-hqclient-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/hornetq/HQFilterConversionInterceptor.java`
#### Snippet
```java
   private SimpleString replaceFilterString(SimpleString filterString) {
      if (filterString == null) {
         return null;
      }
      return SimpleString.toSimpleString(convertHQToActiveMQFilterString(filterString.toString()));
```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-hqclient-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/hornetq/client/HornetQClientProtocolManagerFactory.java`
#### Snippet
```java
   public TransportConfiguration adaptTransportConfiguration(TransportConfiguration tc) {
      if (tc == null) {
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/ComparisonExpression.java`
#### Snippet
```java
      Comparable<Comparable> lv = (Comparable) left.evaluate(message);
      if (lv == null) {
         return null;
      }
      Comparable rv = (Comparable) right.evaluate(message);
```

### ReturnNull
Return of `null`
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/ComparisonExpression.java`
#### Snippet
```java
      Comparable rv = (Comparable) right.evaluate(message);
      if (rv == null) {
         return null;
      }
      return compare(lv, rv);
```

### ReturnNull
Return of `null`
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/ComparisonExpression.java`
#### Snippet
```java
            if (lv == null ^ rv == null) {
               if (lv == null) {
                  return null;
               }
               return Boolean.FALSE;
```

### ReturnNull
Return of `null`
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/ComparisonExpression.java`
#### Snippet
```java

         if (rv == null) {
            return null;
         }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-stomp-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/stomp/StompDecoder.java`
#### Snippet
```java
         return ret;
      } else {
         return null;
      }
   }
```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-stomp-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/stomp/StompDecoder.java`
#### Snippet
```java
      if (command == null) {
         if (!parseCommand()) {
            return null;
         }
      }
```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-stomp-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/stomp/StompDecoder.java`
#### Snippet
```java
      if (readingHeaders) {
         if (!parseHeaders()) {
            return null;
         }
      }
```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-stomp-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/stomp/v10/StompFrameHandlerV10.java`
#### Snippet
```java
   @Override
   public StompFrame onDisconnect(StompFrame frame) {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-stomp-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/stomp/VersionedStompFrameHandler.java`
#### Snippet
```java
      String destination = request.getHeader(header);
      if (destination == null) {
         return null;
      }
      return connection.getSession().getCoreSession().removePrefix(SimpleString.toSimpleString(destination)).toString();
```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-stomp-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/stomp/VersionedStompFrameHandler.java`
#### Snippet
```java
         response = postprocess(request);
         connection.sendFrame(response, postProcessFunction);
         return null;
      } catch (ActiveMQStompException e) {
         return e.getFrame();
```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-stomp-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/stomp/VersionedStompFrameHandler.java`
#### Snippet
```java
         return new StompFrameHandlerV12(connection, scheduledExecutorService, executorFactory);
      }
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-stomp-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/stomp/VersionedStompFrameHandler.java`
#### Snippet
```java
      String destination = request.getHeader(Headers.Send.DESTINATION);
      if (destination == null) {
         return null;
      }
      SimpleString prefix = connection.getSession().getCoreSession().getPrefix(SimpleString.toSimpleString(destination));
```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-stomp-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/stomp/VersionedStompFrameHandler.java`
#### Snippet
```java
      }
      SimpleString prefix = connection.getSession().getCoreSession().getPrefix(SimpleString.toSimpleString(destination));
      return prefix == null ? null : prefix.toString();
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-stomp-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/stomp/StompConnection.java`
#### Snippet
```java
      FutureTask<Void> task = new FutureTask(() -> {
         fail(me);
         return null;
      });

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-stomp-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/stomp/v12/StompFrameHandlerV12.java`
#### Snippet
```java
            return ret;
         } else {
            return null;
         }
      }
```

### ReturnNull
Return of `null`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/api/core/RoutingType.java`
#### Snippet
```java
            return ANYCAST;
         default:
            return null;
      }
   }
```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-stomp-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/stomp/v11/StompFrameHandlerV11.java`
#### Snippet
```java
            return ret;
         } else {
            return null;
         }
      }
```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-stomp-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/stomp/v11/StompFrameHandlerV11.java`
#### Snippet
```java
   public StompFrame onDisconnect(StompFrame frame) {
      disconnect();
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-stomp-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/stomp/v11/StompFrameHandlerV11.java`
#### Snippet
```java
         return onConnect(request);
      }
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/api/core/QueueAttributes.java`
#### Snippet
```java
   public static QueueAttributes fromQueueConfiguration(QueueConfiguration queueConfiguration) {
      if (queueConfiguration == null) {
         return null;
      } else {
         return new QueueAttributes()
```

### ReturnNull
Return of `null`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/api/core/DisconnectReason.java`
#### Snippet
```java
            return SHUT_DOWN_ON_CRITICAL_ERROR;
         default:
            return null;
      }
   }
```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-stomp-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/stomp/StompProtocolManager.java`
#### Snippet
```java
   @Override
   public RoutingHandler getRoutingHandler() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/api/core/QueueConfiguration.java`
#### Snippet
```java
      // name is the only required value
      if (!json.keySet().contains(NAME)) {
         return null;
      }
      QueueConfiguration result = new QueueConfiguration(json.getString(NAME));
```

### ReturnNull
Return of `null`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/api/core/SimpleString.java`
#### Snippet
```java
      int b = buffer.readByte();
      if (b == DataConstants.NULL) {
         return null;
      }
      return readSimpleString(buffer);
```

### ReturnNull
Return of `null`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/api/core/SimpleString.java`
#### Snippet
```java
      int b = buffer.readByte();
      if (b == DataConstants.NULL) {
         return null;
      }
      return readSimpleString(buffer, pool);
```

### ReturnNull
Return of `null`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/api/core/SimpleString.java`
#### Snippet
```java
   public static SimpleString toSimpleString(final String string) {
      if (string == null) {
         return null;
      }
      return new SimpleString(string);
```

### ReturnNull
Return of `null`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/core/buffers/impl/ChannelBufferWrapper.java`
#### Snippet
```java
      int b = readByte();
      if (b == DataConstants.NULL) {
         return null;
      } else {
         return readBoolean();
```

### ReturnNull
Return of `null`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/core/buffers/impl/ChannelBufferWrapper.java`
#### Snippet
```java
      int b = readByte();
      if (b == DataConstants.NULL) {
         return null;
      } else {
         return readLong();
```

### ReturnNull
Return of `null`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/core/buffers/impl/ChannelBufferWrapper.java`
#### Snippet
```java
      int b = readByte();
      if (b == DataConstants.NULL) {
         return null;
      } else {
         return readInt();
```

### ReturnNull
Return of `null`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/core/buffers/impl/ChannelBufferWrapper.java`
#### Snippet
```java
      int b = buffer.readByte();
      if (b == DataConstants.NULL) {
         return null;
      }
      return readStringInternal();
```

### ReturnNull
Return of `null`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/json/impl/JsonValueImpl.java`
#### Snippet
```java
   public JsonValue wrap(javax.json.JsonValue rawValue) {
      if (rawValue == null) {
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/core/server/NetworkHealthCheck.java`
#### Snippet
```java
            return address;
         } else {
            return null;
         }
      } catch (Exception e) {
```

### ReturnNull
Return of `null`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/core/server/NetworkHealthCheck.java`
#### Snippet
```java
      } catch (Exception e) {
         ActiveMQUtilLogger.LOGGER.failedToCheckAddress(straddress, e);
         return null;
      }
   }
```

### ReturnNull
Return of `null`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/core/server/NetworkHealthCheck.java`
#### Snippet
```java
         return networkInterface.getName();
      } else {
         return null;
      }
   }
```

### ReturnNull
Return of `null`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/CompositeAddress.java`
#### Snippet
```java
   public static SimpleString extractQueueName(SimpleString name) {
      if (name == null) {
         return null;
      }
      final String nameString = name.toString();
```

### ReturnNull
Return of `null`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/CompositeAddress.java`
#### Snippet
```java
   public static String extractAddressName(String address) {
      if (address == null) {
         return null;
      }
      int index = address.indexOf(SEPARATOR);
```

### ReturnNull
Return of `null`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/CompositeAddress.java`
#### Snippet
```java
   public static String extractQueueName(String queue) {
      if (queue == null) {
         return null;
      }
      int index = queue.indexOf(SEPARATOR);
```

### ReturnNull
Return of `null`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/CompositeAddress.java`
#### Snippet
```java
   public static SimpleString extractAddressName(SimpleString address) {
      if (address == null) {
         return null;
      }
      final String addrString = address.toString();
```

### ReturnNull
Return of `null`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/SelectorTranslator.java`
#### Snippet
```java
   public static String convertToActiveMQFilterString(final String selectorString) {
      if (selectorString == null) {
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/json/impl/JsonObjectImpl.java`
#### Snippet
```java
      javax.json.JsonValue rawValue = rawObject.remove(key);

      return rawValue != null ? wrap(rawValue) : null;
   }

```

### ReturnNull
Return of `null`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/json/impl/JsonObjectImpl.java`
#### Snippet
```java
                  Map.Entry<String, javax.json.JsonValue> rawEntry = rawIterator.next();

                  return rawEntry != null ? new AbstractMap.SimpleEntry<>(rawEntry.getKey(), wrap(rawEntry.getValue())) : null;
               }
            };
```

### ReturnNull
Return of `null`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/XmlProvider.java`
#### Snippet
```java
         @Override
         public String getEncoding() {
            return null;
         }

```

### ReturnNull
Return of `null`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/XmlProvider.java`
#### Snippet
```java
            }

            return null;
         });
      }
```

### ReturnNull
Return of `null`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/XmlProvider.java`
#### Snippet
```java
         @Override
         public Reader getCharacterStream() {
            return null;
         }

```

### ReturnNull
Return of `null`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/XmlProvider.java`
#### Snippet
```java
         @Override
         public String getStringData() {
            return null;
         }

```

### ReturnNull
Return of `null`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/InetAddresses.java`
#### Snippet
```java
         } else if (c == ':') {
            if (hasDot) {
               return null;  // Colons must not appear after dots.
            }
            hasColon = true;
```

### ReturnNull
Return of `null`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/InetAddresses.java`
#### Snippet
```java
            hasColon = true;
         } else if (Character.digit(c, 16) == -1) {
            return null;  // Everything else must be a decimal or hex digit.
         }
      }
```

### ReturnNull
Return of `null`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/InetAddresses.java`
#### Snippet
```java
            ipString = convertDottedQuadToHex(ipString);
            if (ipString == null) {
               return null;
            }
         }
```

### ReturnNull
Return of `null`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/InetAddresses.java`
#### Snippet
```java
         return textToNumericFormatV4(ipString);
      }
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/InetAddresses.java`
#### Snippet
```java
      byte[] quad = textToNumericFormatV4(dottedQuad);
      if (quad == null) {
         return null;
      }
      String penultimate = Integer.toHexString(((quad[0] & 0xff) << 8) | (quad[1] & 0xff));
```

### ReturnNull
Return of `null`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/InetAddresses.java`
#### Snippet
```java
      String[] address = ipString.split("\\.", IPV4_PART_COUNT + 1);
      if (address.length != IPV4_PART_COUNT) {
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/InetAddresses.java`
#### Snippet
```java
         }
      } catch (NumberFormatException ex) {
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/InetAddresses.java`
#### Snippet
```java
      String[] parts = ipString.split(":", IPV6_PART_COUNT + 2);
      if (parts.length < 3 || parts.length > IPV6_PART_COUNT + 1) {
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/InetAddresses.java`
#### Snippet
```java
         if (parts[i].length() == 0) {
            if (skipIndex >= 0) {
               return null;  // Can't have more than one ::
            }
            skipIndex = i;
```

### ReturnNull
Return of `null`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/InetAddresses.java`
#### Snippet
```java
         partsLo = parts.length - skipIndex - 1;
         if (parts[0].length() == 0 && --partsHi != 0) {
            return null;  // ^: requires ^::
         }
         if (parts[parts.length - 1].length() == 0 && --partsLo != 0) {
```

### ReturnNull
Return of `null`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/InetAddresses.java`
#### Snippet
```java
         }
         if (parts[parts.length - 1].length() == 0 && --partsLo != 0) {
            return null;  // :$ requires ::$
         }
      } else {
```

### ReturnNull
Return of `null`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/InetAddresses.java`
#### Snippet
```java
      int partsSkipped = IPV6_PART_COUNT - (partsHi + partsLo);
      if (!(skipIndex >= 0 ? partsSkipped >= 1 : partsSkipped == 0)) {
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/InetAddresses.java`
#### Snippet
```java
         }
      } catch (NumberFormatException ex) {
         return null;
      }
      return rawBytes.array();
```

### ReturnNull
Return of `null`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/AbstractPool.java`
#### Snippet
```java
   public final O getOrCreate(final I value) {
      if (value == null) {
         return null;
      }
      final int hashCode = hashCode(value);
```

### ReturnNull
Return of `null`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/UUIDGenerator.java`
#### Snippet
```java
   private static String asString(final byte[] bytes) {
      if (bytes == null) {
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/UUIDGenerator.java`
#### Snippet
```java
   public static byte[] getZeroPaddedSixBytes(final byte[] bytes) {
      if (bytes == null) {
         return null;
      }
      if (bytes.length > 0 && bytes.length <= 6) {
```

### ReturnNull
Return of `null`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/UUIDGenerator.java`
#### Snippet
```java
         }
      }
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/UUIDGenerator.java`
#### Snippet
```java
         return address;
      } catch (Exception e) {
         return null;
      } finally {
         executor.shutdownNow();
```

### ReturnNull
Return of `null`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/UUIDGenerator.java`
#### Snippet
```java
      } catch (Throwable t) {
         // not enough security permission
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/UUIDGenerator.java`
#### Snippet
```java

         if (ifaces.size() == 0) {
            return null;
         }

```

### ReturnNull
Return of `null`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/UUIDGenerator.java`
#### Snippet
```java
            return address;
         }
         return null;
      } catch (Exception e) {
         return null;
```

### ReturnNull
Return of `null`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/UUIDGenerator.java`
#### Snippet
```java
         return null;
      } catch (Exception e) {
         return null;
      }
   }
```

### ReturnNull
Return of `null`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/SimpleFuture.java`
#### Snippet
```java
      @Override
      public Object get() throws InterruptedException, ExecutionException {
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/SimpleFuture.java`
#### Snippet
```java
      @Override
      public Object get(long timeout, TimeUnit unit) throws InterruptedException, ExecutionException, TimeoutException {
         return null;
      }
   };
```

### ReturnNull
Return of `null`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/ClassloadingUtil.java`
#### Snippet
```java
      loader = Thread.currentThread().getContextClassLoader();
      if (loader == null)
         return null;

      return loader.getResource(resourceName);
```

### ReturnNull
Return of `null`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
      } catch (java.io.IOException e) {
         ActiveMQUtilLogger.LOGGER.failedToSerializeObject(e);
         return null;
      } finally {
         try {
```

### ReturnNull
Return of `null`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
         } catch (java.io.IOException e) {
            ActiveMQUtilLogger.LOGGER.failedToEncodeByteArrayToBase64Notation(e);
            return null;
         } finally {
            try {
```

### ReturnNull
Return of `null`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
         if (file.length() > Integer.MAX_VALUE) {
            System.err.println("File is too big for this convenience method (" + file.length() + " bytes).");
            return null;
         } // end if: file too big for int index
         buffer = new byte[(int) file.length()];
```

### ReturnNull
Return of `null`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
         } else {
            System.err.println("Bad Base64 input character at " + i + ": " + source[i] + "(decimal)");
            return null;
         } // end else:
      } // each input character
```

### ReturnNull
Return of `null`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/uri/URISchema.java`
#### Snippet
```java
      URIFactory<T, P> factory = getFactory();
      if (factory == null) {
         return null;
      } else {
         return factory.getDefaultURI();
```

### ReturnNull
Return of `null`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/NoOpMap.java`
#### Snippet
```java
   @Override
   public V get(Object key) {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/EmptyList.java`
#### Snippet
```java
   @Override
   public E peek() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/EmptyList.java`
#### Snippet
```java
   @Override
   public E poll() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/MultiIteratorBase.java`
#### Snippet
```java
            moveTo(next);
         } else {
            return null;
         }
      }
```

### ReturnNull
Return of `null`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/PriorityLinkedListImpl.java`
#### Snippet
```java
      }

      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/PriorityLinkedListImpl.java`
#### Snippet
```java
      }

      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/uri/BeanSupport.java`
#### Snippet
```java
         public <T> T convert(Class<T> type, Object value) {
            if (value == null) {
               return null;
            }

```

### ReturnNull
Return of `null`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/ConcurrentAppendOnlyChunkedList.java`
#### Snippet
```java
   public E get(int index) {
      if (index < 0) {
         return null;
      }
      final long lastIndex = getValidLastIndex();
```

### ReturnNull
Return of `null`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/ConcurrentAppendOnlyChunkedList.java`
#### Snippet
```java
      //it is a element over the current size?
      if (index >= lastIndex) {
         return null;
      }
      final AtomicChunk<E> buffer;
```

### ReturnNull
Return of `null`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/ConcurrentLongHashMap.java`
#### Snippet
```java
                  if (value == null || value.equals(storedValue)) {
                     if (storedValue == EmptyValue || storedValue == DeletedValue) {
                        return null;
                     }

```

### ReturnNull
Return of `null`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/ConcurrentLongHashMap.java`
#### Snippet
```java
                     return storedValue;
                  } else {
                     return null;
                  }
               } else if (storedValue == EmptyValue) {
```

### ReturnNull
Return of `null`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/ConcurrentLongHashMap.java`
#### Snippet
```java
               } else if (storedValue == EmptyValue) {
                  // Key wasn't found
                  return null;
               }

```

### ReturnNull
Return of `null`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/ConcurrentLongHashMap.java`
#### Snippet
```java
                  // The values we have read are consistent
                  if (storedKey == key) {
                     return storedValue != DeletedValue ? storedValue : null;
                  } else if (storedValue == EmptyValue) {
                     // Not found
```

### ReturnNull
Return of `null`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/ConcurrentLongHashMap.java`
#### Snippet
```java
                  } else if (storedValue == EmptyValue) {
                     // Not found
                     return null;
                  }
               } else {
```

### ReturnNull
Return of `null`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/ConcurrentLongHashMap.java`
#### Snippet
```java

                  if (storedKey == key) {
                     return storedValue != DeletedValue ? storedValue : null;
                  } else if (storedValue == EmptyValue) {
                     // Not found
```

### ReturnNull
Return of `null`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/ConcurrentLongHashMap.java`
#### Snippet
```java
                  } else if (storedValue == EmptyValue) {
                     // Not found
                     return null;
                  }
               }
```

### ReturnNull
Return of `null`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/ConcurrentLongHashMap.java`
#### Snippet
```java
                     ++size;
                     ++usedBuckets;
                     return valueProvider != null ? values[bucket] : null;
                  } else if (storedValue == DeletedValue) {
                     values[bucket] = value != null ? value : (valueProvider != null ? valueProvider.apply(key) : null);
```

### ReturnNull
Return of `null`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/ConcurrentLongHashMap.java`
#### Snippet
```java
                     values[bucket] = value != null ? value : (valueProvider != null ? valueProvider.apply(key) : null);
                     ++size;
                     return valueProvider != null ? values[bucket] : null;
                  } else if (!onlyIfAbsent) {
                     // Over written an old value for same key
```

### ReturnNull
Return of `null`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/ConcurrentLongHashMap.java`
#### Snippet
```java
                  values[bucket] = value != null ? value : (valueProvider != null ? valueProvider.apply(key) : null);
                  ++size;
                  return valueProvider != null ? values[bucket] : null;
               } else if (storedValue == DeletedValue) {
                  // The bucket contained a different deleted key
```

### ReturnNull
Return of `null`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/PriorityCollection.java`
#### Snippet
```java
         return newArray[low].getValues();
      }
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/PriorityCollection.java`
#### Snippet
```java
         }
      }
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/io/SequentialFileFactory.java`
#### Snippet
```java

   default CriticalAnalyzer getCriticalAnalyzer() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/io/SequentialFileFactory.java`
#### Snippet
```java

   default IOCriticalErrorListener getCriticalErrorListener() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/io/aio/AIOSequentialFile.java`
#### Snippet
```java
   @Override
   public ByteBuffer map(int position, long size) throws IOException {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/io/mapped/TimedSequentialFile.java`
#### Snippet
```java
   @Override
   public ByteBuffer map(int position, long size) throws IOException {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/io/mapped/MappedSequentialFile.java`
#### Snippet
```java
   @Override
   public ByteBuffer map(int position, long size) throws IOException {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalCompactor.java`
#### Snippet
```java
   @Override
   public JournalCompactor getCompactor() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/TypedProperties.java`
#### Snippet
```java
      Object value = doGetProperty(key);
      if (value == null) {
         return null;
      } else if (value instanceof byte[]) {
         return (byte[]) value;
```

### ReturnNull
Return of `null`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/TypedProperties.java`
#### Snippet
```java
   private synchronized Object doGetProperty(final SimpleString key) {
      if (properties == null) {
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/TypedProperties.java`
#### Snippet
```java
      PropertyValue val = properties.get(key);
      if (val == null) {
         return null;
      } else {
         return val.getValue();
```

### ReturnNull
Return of `null`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/TypedProperties.java`
#### Snippet
```java
   private synchronized Object doRemoveProperty(final SimpleString key) {
      if (properties == null) {
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/TypedProperties.java`
#### Snippet
```java
      PropertyValue val = properties.remove(key);
      if (val == null) {
         return null;
      } else {
         size -= SimpleString.sizeofString(key) + val.encodeSize();
```

### ReturnNull
Return of `null`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/TypedProperties.java`
#### Snippet
```java

      if (value == null) {
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/TypedProperties.java`
#### Snippet
```java
      @Override
      public Object getValue() {
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/LinkedListImpl.java`
#### Snippet
```java
            return current;
         } else {
            return null;
         }
      }
```

### ReturnNull
Return of `null`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/LinkedListImpl.java`
#### Snippet
```java
      Node<E> current = head.next;
      if (current == null) {
         return null;
      } else {
         return current.val();
```

### ReturnNull
Return of `null`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/LinkedListImpl.java`
#### Snippet
```java
         return ret.val();
      } else {
         return null;
      }
   }
```

### ReturnNull
Return of `null`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/LinkedListImpl.java`
#### Snippet
```java

      if (node == null) {
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/ActiveMQRAMessageConsumer.java`
#### Snippet
```java

         if (message == null) {
            return null;
         } else {
            return wrapMessage(message);
```

### ReturnNull
Return of `null`
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/ActiveMQRAMessageConsumer.java`
#### Snippet
```java

         if (message == null) {
            return null;
         } else {
            return wrapMessage(message);
```

### ReturnNull
Return of `null`
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/ActiveMQRAMessageConsumer.java`
#### Snippet
```java

         if (message == null) {
            return null;
         } else {
            return wrapMessage(message);
```

### ReturnNull
Return of `null`
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/ActiveMQRaUtils.java`
#### Snippet
```java
            } catch (Throwable e) {
               logger.debug(e.getMessage(), e);
               return null;
            }
         }
```

### ReturnNull
Return of `null`
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/ActiveMQRAManagedConnection.java`
#### Snippet
```java
      logger.trace("getLogWriter()");

      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/ActiveMQRAManagedConnectionFactory.java`
#### Snippet
```java
      logger.trace("getLogWriter()");

      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/ActiveMQRAManagedConnectionFactory.java`
#### Snippet
```java
      logger.trace("No matching connection was found");

      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/ActiveMQRASession.java`
#### Snippet
```java
      if (cri.getType() == ActiveMQRAConnectionFactory.CONNECTION || cri.getType() == ActiveMQRAConnectionFactory.QUEUE_CONNECTION ||
         cri.getType() == ActiveMQRAConnectionFactory.TOPIC_CONNECTION) {
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/ActiveMQRASession.java`
#### Snippet
```java
         return getXAResourceInternal();
      } catch (Throwable t) {
         return null;
      } finally {
         unlock();
```

### ReturnNull
Return of `null`
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/Journal.java`
#### Snippet
```java

   default IOCriticalErrorListener getCriticalErrorListener() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/AbstractJournalUpdateTask.java`
#### Snippet
```java
            // the record is damaged
            controlFile.delete();
            return null;
         } else {
            ActiveMQBuffer input = ActiveMQBuffers.wrappedBuffer(records.get(0).data);
```

### ReturnNull
Return of `null`
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/AbstractJournalUpdateTask.java`
#### Snippet
```java
         return controlFile;
      } else {
         return null;
      }
   }
```

### ReturnNull
Return of `null`
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/ActiveMQResourceAdapter.java`
#### Snippet
```java

      if (ctx == null) {
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/ActiveMQResourceAdapter.java`
#### Snippet
```java
         }
      }
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/ActiveMQResourceAdapter.java`
#### Snippet
```java
      if (useAutoRecovery) {
         // let the TM handle the recovery
         return null;
      } else {
         List<XAResource> xaresources = new ArrayList<>();
```

### ReturnNull
Return of `null`
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/ActiveMQResourceAdapter.java`
#### Snippet
```java
      }

      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/inflow/ActiveMQMessageHandler.java`
#### Snippet
```java
         ActiveMQRALogger.LOGGER.errorInterruptingHandler(endpoint.toString(), consumer.toString(), e);
      }
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/inflow/ActiveMQMessageHandler.java`
#### Snippet
```java
   public Thread getCurrentThread() {
      if (consumer == null) {
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/inflow/ActiveMQMessageHandler.java`
#### Snippet
```java

   XAResource getXAResource() {
      return useXA ? session : null;
   }

```

### ReturnNull
Return of `null`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/factory/BrokerFactory.java`
#### Snippet
```java
         return handler.createServer(brokerDTO, security, activateCallback);
      }
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/factory/serialize/XMLMessageSerializer.java`
#### Snippet
```java

      // End of document.
      if (reader.getRawXMLReader().getLocalName().equals("messages")) return null;

      XMLMessageImporter.MessageInfo messageInfo = reader.readMessage(true);
```

### ReturnNull
Return of `null`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/factory/serialize/XMLMessageSerializer.java`
#### Snippet
```java

      XMLMessageImporter.MessageInfo messageInfo = reader.readMessage(true);
      if (messageInfo == null) return null;

      // This is a large message
```

### ReturnNull
Return of `null`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Stop.java`
#### Snippet
```java
      stopFile.createNewFile();

      return null;
   }
}
```

### ReturnNull
Return of `null`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Kill.java`
#### Snippet
```java
      killFile.createNewFile();

      return null;
   }
}
```

### ReturnNull
Return of `null`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/ActionAbstract.java`
#### Snippet
```java
      }

      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/ActionAbstract.java`
#### Snippet
```java
      ActionContext.setSystem(context);

      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/InputAbstract.java`
#### Snippet
```java
      this.scanner = new Scanner(context.in);

      return null;
   }
}
```

### ReturnNull
Return of `null`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/HelpAction.java`
#### Snippet
```java
   @Override
   public String getBrokerInstance() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/HelpAction.java`
#### Snippet
```java
   @Override
   public String getBrokerHome() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/HelpAction.java`
#### Snippet
```java
   public Object execute(ActionContext context) throws Exception {
      super.run();
      return null;
   }
}
```

### ReturnNull
Return of `null`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/user/HelpUser.java`
#### Snippet
```java
   @Override
   public String getBrokerInstance() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/user/HelpUser.java`
#### Snippet
```java
      commands.add("user");
      help(global, commands);
      return null;
   }
}
```

### ReturnNull
Return of `null`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/user/HelpUser.java`
#### Snippet
```java
   @Override
   public String getBrokerHome() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/user/RemoveUser.java`
#### Snippet
```java
      checkInputUser();
      remove();
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/user/AddUser.java`
#### Snippet
```java
      checkInputRole();
      add();
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/user/ListUser.java`
#### Snippet
```java
      list();

      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/user/ResetUser.java`
#### Snippet
```java
      checkInputPassword();
      reset();
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Upgrade.java`
#### Snippet
```java
      }

      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Upgrade.java`
#### Snippet
```java
      context.out.println("*******************************************************************************************************************************");

      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/check/HelpCheck.java`
#### Snippet
```java
   @Override
   public String getBrokerHome() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/check/HelpCheck.java`
#### Snippet
```java
   @Override
   public String getBrokerInstance() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/check/HelpCheck.java`
#### Snippet
```java
      commands.add("check");
      help(global, commands);
      return null;
   }
}
```

### ReturnNull
Return of `null`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/queue/CreateQueue.java`
#### Snippet
```java
      super.execute(context);
      createQueue(context);
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/queue/HelpQueue.java`
#### Snippet
```java
      commands.add("queue");
      help(global, commands);
      return null;
   }
}
```

### ReturnNull
Return of `null`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/queue/HelpQueue.java`
#### Snippet
```java
   @Override
   public String getBrokerInstance() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/queue/HelpQueue.java`
#### Snippet
```java
   @Override
   public String getBrokerHome() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/InstallAbstract.java`
#### Snippet
```java
      IS_CYGWIN = cygwin | IS_WINDOWS && "cygwin".equals(System.getenv("OSTYPE"));

      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/queue/PurgeQueue.java`
#### Snippet
```java
      super.execute(context);
      purgeQueue(context);
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/queue/UpdateQueue.java`
#### Snippet
```java
      super.execute(context);
      updateQueue(context);
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/LockAbstract.java`
#### Snippet
```java
         return new File(new File(brokerInstance), "lock");
      } else {
         return null;
      }
   }
```

### ReturnNull
Return of `null`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/LockAbstract.java`
#### Snippet
```java
      }

      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/queue/DeleteQueue.java`
#### Snippet
```java
      super.execute(context);
      deleteQueue(context);
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/queue/QueueAbstract.java`
#### Snippet
```java
      if (value == null) {
         // return null if still null
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/queue/QueueAbstract.java`
#### Snippet
```java
         return "ANYCAST";
      } else {
         return null;
      }
   }
```

### ReturnNull
Return of `null`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/HelpData.java`
#### Snippet
```java
   @Override
   public String getBrokerHome() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/HelpData.java`
#### Snippet
```java
   @Override
   public String getBrokerInstance() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/HelpData.java`
#### Snippet
```java
      commands.add("data");
      help(global, commands);
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/RecoverMessages.java`
#### Snippet
```java
         treatError(e, "data", "recover");
      }
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/queue/StatQueue.java`
#### Snippet
```java
      if (((fieldName != null) && (fieldName.trim().length() > 0)) && ((queueName != null && queueName.trim().length() > 0))) {
         getActionContext().err.println("'--field' and '--queueName' cannot be specified together.");
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/queue/StatQueue.java`
#### Snippet
```java
         } catch (IllegalArgumentException ex) {
            getActionContext().err.println("'--field' must be set to one of the following " + Arrays.toString(FIELD.values()));
            return null;
         }

```

### ReturnNull
Return of `null`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/queue/StatQueue.java`
#### Snippet
```java
         if (value == null || value.trim().length() == 0) {
            getActionContext().err.println("'--value' needs to be set when '--field' is specified");
            return null;
         }
         filterMap.put("value", value);
```

### ReturnNull
Return of `null`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/queue/StatQueue.java`
#### Snippet
```java
         if (operationName == null) {
            getActionContext().err.println("'--operation' must be set when '--field' is specified " + Arrays.toString(OPERATION.values()));
            return null;
         }

```

### ReturnNull
Return of `null`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/queue/StatQueue.java`
#### Snippet
```java
         } catch (IllegalArgumentException ex) {
            getActionContext().err.println("'--operation' must be set to one of the following " + Arrays.toString(OPERATION.values()));
            return null;
         }

```

### ReturnNull
Return of `null`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/queue/StatQueue.java`
#### Snippet
```java
      //should always get an initialised filter back when values correct
      if (filter == null) {
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/DBOption.java`
#### Snippet
```java
         Runtime.getRuntime().addShutdownHook(new Thread(printStream::close));
      }
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/journal/EncodeJournal.java`
#### Snippet
```java
      }

      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/journal/CompactJournal.java`
#### Snippet
```java
         treatError(e, "data", "compact");
      }
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/journal/PerfJournal.java`
#### Snippet
```java
      context.out.println("<journal-buffer-timeout>" + nanosecondsWait + "</journal-buffer-timeout>");

      return null;
   }
}
```

### ReturnNull
Return of `null`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/journal/DecodeJournal.java`
#### Snippet
```java
      }

      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/address/UpdateAddress.java`
#### Snippet
```java
      super.execute(context);
      updateAddress(context);
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/address/DeleteAddress.java`
#### Snippet
```java
      super.execute(context);
      deleteAddress(context);
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/address/CreateAddress.java`
#### Snippet
```java
      super.execute(context);
      createAddress(context);
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/xml/XMLMessageImporter.java`
#### Snippet
```java

   public MessageInfo readMessage(boolean decodeUTF8) throws Exception {
      if (!reader.hasNext()) return null;

      Byte type = 0;
```

### ReturnNull
Return of `null`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/xml/XmlDataImporter.java`
#### Snippet
```java
   public Object execute(ActionContext context) throws Exception {
      process(input, host, port, transactional);
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/address/AddressAbstract.java`
#### Snippet
```java
            return "MULTICAST"; // the default;
         } else {
            return null;
         }

```

### ReturnNull
Return of `null`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/address/HelpAddress.java`
#### Snippet
```java
   @Override
   public String getBrokerHome() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/address/HelpAddress.java`
#### Snippet
```java
   @Override
   public String getBrokerInstance() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/address/HelpAddress.java`
#### Snippet
```java
      commands.add("address");
      help(global, commands);
      return null;
   }
}
```

### ReturnNull
Return of `null`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/address/ShowAddress.java`
#### Snippet
```java
      super.execute(context);
      showAddress(context);
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/xml/XmlDataExporter.java`
#### Snippet
```java
         method.invoke(target, args);

         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/xml/XmlDataExporter.java`
#### Snippet
```java
         treatError(e, "data", "exp");
      }
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Create.java`
#### Snippet
```java
      }

      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Create.java`
#### Snippet
```java
               return BASIC;
            default:
               return null;
         }
      }
```

### ReturnNull
Return of `null`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/PrintData.java`
#### Snippet
```java
      } catch (Exception e) {
         e.printStackTrace();
         return null;
      }
   }
```

### ReturnNull
Return of `null`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/PrintData.java`
#### Snippet
```java
      } catch (Exception e) {
         e.printStackTrace();
         return null;
      }
      return describeJournal;
```

### ReturnNull
Return of `null`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/PrintData.java`
#### Snippet
```java
         treatError(e, "data", "print");
      }
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/ConnectionAbstract.java`
#### Snippet
```java
      context.out.println("Connection brokerURL = " + brokerURL);

      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/DestAbstract.java`
#### Snippet
```java
      if (!protocol.equalsIgnoreCase("CORE")) {
         System.err.println("Default Serializer does not support: " + protocol + " protocol");
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/Consumer.java`
#### Snippet
```java
         if (serializer == null) {
            System.err.println("Error. Unable to instantiate serializer class: " + this.serializer);
            return null;
         }

```

### ReturnNull
Return of `null`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/Consumer.java`
#### Snippet
```java
         } catch (Exception e) {
            System.err.println("Error: Unable to open file for writing\n" + e.getMessage());
            return null;
         }

```

### ReturnNull
Return of `null`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/Producer.java`
#### Snippet
```java
               if (serializer == null) {
                  System.err.println("Error. Unable to instantiate serializer class: " + serializer);
                  return null;
               }

```

### ReturnNull
Return of `null`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/Producer.java`
#### Snippet
```java
               } catch (Exception e) {
                  System.err.println("Error: Unable to open file for reading\n" + e.getMessage());
                  return null;
               }

```

### ReturnNull
Return of `null`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/perf/AsyncJms2ProducerFacade.java`
#### Snippet
```java
   public JMSException onSendCompleted() {
      if (closed) {
         return null;
      }
      JMSException completionError = null;
```

### ReturnNull
Return of `null`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/perf/PerfCommand.java`
#### Snippet
```java
         completed.countDown();
      }
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/util/ServerUtil.java`
#### Snippet
```java
         }
      }
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/bridge/impl/JMSBridgeControlImpl.java`
#### Snippet
```java
         return mode.name();
      } else {
         return null;
      }
   }
```

### ReturnNull
Return of `null`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/perf/MessageListenerBenchmark.java`
#### Snippet
```java

   public synchronized RecordingMessageListener[] getListeners() {
      return listeners == null ? null : listeners.toArray(new RecordingMessageListener[listeners.size()]);
   }

```

### ReturnNull
Return of `null`
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/persistence/config/PersistedType.java`
#### Snippet
```java
            return Queue;
         default:
            return null;
      }
   }
```

### ReturnNull
Return of `null`
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/transaction/JMSTransactionDetail.java`
#### Snippet
```java
         return ActiveMQMessage.coreMaptoJMSMap(msg.toMap());
      } catch (Throwable t) {
         return null;
      }
   }
```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenwireMessage.java`
#### Snippet
```java
   @Override
   public Message setDurable(boolean durable) {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenwireMessage.java`
#### Snippet
```java
   @Override
   public Float getFloatProperty(SimpleString key) throws ActiveMQPropertyConversionException {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenwireMessage.java`
#### Snippet
```java
   @Override
   public Message putObjectProperty(String key, Object value) throws ActiveMQPropertyConversionException {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenwireMessage.java`
#### Snippet
```java
   @Override
   public String getAddress() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenwireMessage.java`
#### Snippet
```java
   @Override
   public Short getShortProperty(String key) throws ActiveMQPropertyConversionException {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenwireMessage.java`
#### Snippet
```java
   @Override
   public Message setAddress(String address) {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenwireMessage.java`
#### Snippet
```java
   @Override
   public Message putDoubleProperty(SimpleString key, double value) {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenwireMessage.java`
#### Snippet
```java
   @Override
   public Message putBooleanProperty(String key, boolean value) {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenwireMessage.java`
#### Snippet
```java
   @Override
   public Persister<Message> getPersister() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenwireMessage.java`
#### Snippet
```java
   @Override
   public ICoreMessage toCore(CoreMessageObjectPools coreMessageObjectPools) {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenwireMessage.java`
#### Snippet
```java
   @Override
   public Short getShortProperty(SimpleString key) throws ActiveMQPropertyConversionException {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenwireMessage.java`
#### Snippet
```java
   @Override
   public Message setAddress(SimpleString address) {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenwireMessage.java`
#### Snippet
```java
   @Override
   public SimpleString getAddressSimpleString() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenwireMessage.java`
#### Snippet
```java
   @Override
   public Message putIntProperty(String key, int value) {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenwireMessage.java`
#### Snippet
```java
   @Override
   public Message putStringProperty(SimpleString key, String value) {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenwireMessage.java`
#### Snippet
```java
   @Override
   public Long getLongProperty(String key) throws ActiveMQPropertyConversionException {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenwireMessage.java`
#### Snippet
```java
   @Override
   public Message putStringProperty(String key, String value) {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenwireMessage.java`
#### Snippet
```java
   @Override
   public Message putFloatProperty(String key, float value) {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenwireMessage.java`
#### Snippet
```java
   @Override
   public Message setUserID(Object userID) {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenwireMessage.java`
#### Snippet
```java
   @Override
   public Message putFloatProperty(SimpleString key, float value) {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenwireMessage.java`
#### Snippet
```java
   @Override
   public Float getFloatProperty(String key) throws ActiveMQPropertyConversionException {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenwireMessage.java`
#### Snippet
```java
   @Override
   public Message setReplyTo(SimpleString address) {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenwireMessage.java`
#### Snippet
```java
   @Override
   public Message putStringProperty(SimpleString key, SimpleString value) {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenwireMessage.java`
#### Snippet
```java
   @Override
   public String getStringProperty(SimpleString key) throws ActiveMQPropertyConversionException {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenwireMessage.java`
#### Snippet
```java
   @Override
   public Object getObjectProperty(SimpleString key) {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenwireMessage.java`
#### Snippet
```java
   @Override
   public Message putShortProperty(SimpleString key, short value) {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenwireMessage.java`
#### Snippet
```java
   @Override
   public Message putLongProperty(String key, long value) {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenwireMessage.java`
#### Snippet
```java
   @Override
   public Message setPriority(byte priority) {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenwireMessage.java`
#### Snippet
```java
   @Override
   public Long getLongProperty(SimpleString key) throws ActiveMQPropertyConversionException {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenwireMessage.java`
#### Snippet
```java
   @Override
   public Boolean getBooleanProperty(String key) throws ActiveMQPropertyConversionException {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenwireMessage.java`
#### Snippet
```java
   @Override
   public Object getOwner() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenwireMessage.java`
#### Snippet
```java
   @Override
   public Set<SimpleString> getPropertyNames() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenwireMessage.java`
#### Snippet
```java
   @Override
   public Message putBooleanProperty(SimpleString key, boolean value) {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenwireMessage.java`
#### Snippet
```java
   @Override
   public Double getDoubleProperty(String key) throws ActiveMQPropertyConversionException {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenwireMessage.java`
#### Snippet
```java
   @Override
   public Message putBytesProperty(String key, byte[] value) {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenwireMessage.java`
#### Snippet
```java
   @Override
   public Boolean getBooleanProperty(SimpleString key) throws ActiveMQPropertyConversionException {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenwireMessage.java`
#### Snippet
```java
   @Override
   public Message putCharProperty(SimpleString key, char value) {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenwireMessage.java`
#### Snippet
```java
   @Override
   public Message putBytesProperty(SimpleString key, byte[] value) {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenwireMessage.java`
#### Snippet
```java
   @Override
   public Double getDoubleProperty(SimpleString key) throws ActiveMQPropertyConversionException {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenwireMessage.java`
#### Snippet
```java
   @Override
   public Message setExpiration(long expiration) {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenwireMessage.java`
#### Snippet
```java
   @Override
   public Object removeProperty(SimpleString key) {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenwireMessage.java`
#### Snippet
```java
   @Override
   public Long getScheduledDeliveryTime() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenwireMessage.java`
#### Snippet
```java
   @Override
   public Message putDoubleProperty(String key, double value) {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenwireMessage.java`
#### Snippet
```java
   @Override
   public Message setTimestamp(long timestamp) {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenwireMessage.java`
#### Snippet
```java
   @Override
   public Object getUserID() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenwireMessage.java`
#### Snippet
```java
   @Override
   public String getStringProperty(String key) throws ActiveMQPropertyConversionException {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenwireMessage.java`
#### Snippet
```java
   @Override
   public Object getObjectProperty(String key) {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenwireMessage.java`
#### Snippet
```java
   @Override
   public Byte getByteProperty(SimpleString key) throws ActiveMQPropertyConversionException {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenwireMessage.java`
#### Snippet
```java
   @Override
   public Message setMessageID(long id) {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenwireMessage.java`
#### Snippet
```java
   @Override
   public Message putByteProperty(SimpleString key, byte value) {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenwireMessage.java`
#### Snippet
```java
   @Override
   public Message putIntProperty(SimpleString key, int value) {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenwireMessage.java`
#### Snippet
```java
   @Override
   public Object removeProperty(String key) {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenwireMessage.java`
#### Snippet
```java
   @Override
   public Message putShortProperty(String key, short value) {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenwireMessage.java`
#### Snippet
```java
   @Override
   public Message putObjectProperty(SimpleString key, Object value) throws ActiveMQPropertyConversionException {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenwireMessage.java`
#### Snippet
```java
   @Override
   public Message putByteProperty(String key, byte value) {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenwireMessage.java`
#### Snippet
```java
   @Override
   public Message copy(long newID) {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenwireMessage.java`
#### Snippet
```java
   @Override
   public Object getUserContext(Object key) {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenwireMessage.java`
#### Snippet
```java
   @Override
   public Message copy() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenwireMessage.java`
#### Snippet
```java
   @Override
   public SimpleString getSimpleStringProperty(String key) throws ActiveMQPropertyConversionException {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenwireMessage.java`
#### Snippet
```java
   @Override
   public Object getAnnotation(SimpleString key) {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenwireMessage.java`
#### Snippet
```java
   @Override
   public SimpleString getSimpleStringProperty(SimpleString key) throws ActiveMQPropertyConversionException {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenwireMessage.java`
#### Snippet
```java
   @Override
   public Object removeAnnotation(SimpleString key) {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenwireMessage.java`
#### Snippet
```java
   @Override
   public Integer getIntProperty(String key) throws ActiveMQPropertyConversionException {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenwireMessage.java`
#### Snippet
```java
   @Override
   public Message putCharProperty(String key, char value) {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenwireMessage.java`
#### Snippet
```java
   @Override
   public SimpleString getReplyTo() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenwireMessage.java`
#### Snippet
```java
   @Override
   public Integer getIntProperty(SimpleString key) throws ActiveMQPropertyConversionException {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenwireMessage.java`
#### Snippet
```java
   @Override
   public Byte getByteProperty(String key) throws ActiveMQPropertyConversionException {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenwireMessage.java`
#### Snippet
```java
   @Override
   public Message putLongProperty(SimpleString key, long value) {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/util/OpenWireUtil.java`
#### Snippet
```java
         }
      }
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/bridge/impl/JMSBridgeImpl.java`
#### Snippet
```java
                  public Object run() {
                     thr.setContextClassLoader(moduleTccl);
                     return null;
                  }
               });
```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/amq/AMQConsumer.java`
#### Snippet
```java
   private static String convertOpenWireToActiveMQFilterString(final String selectorString) {
      if (selectorString == null) {
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenWireMessageConverter.java`
#### Snippet
```java
         }
      }
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTConnection.java`
#### Snippet
```java
      FutureTask<Void> task = new FutureTask(() -> {
         fail(me);
         return null;
      });

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTVersion.java`
#### Snippet
```java
            return MQTT_5;
         default:
            return null;
      }
   }
```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTProtocolManager.java`
#### Snippet
```java
   public MQTTSessionState removeSessionState(String clientId) {
      if (clientId == null) {
         return null;
      }
      return sessionStates.remove(clientId);
```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTProtocolManager.java`
#### Snippet
```java
      } catch (Exception e) {
         logger.error("Error creating connection entry", e);
         return null;
      }
   }
```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTSessionState.java`
#### Snippet
```java

   public Integer getServerTopicAlias(String topicName) {
      return serverTopicAliases == null ? null : serverTopicAliases.get(topicName);
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTSessionState.java`
#### Snippet
```java
               return SENDING;
            default:
               return null;
         }
      }
```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTSessionState.java`
#### Snippet
```java
         return alias;
      } else {
         return null;
      }
   }
```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTSessionState.java`
#### Snippet
```java

   public MqttTopicSubscription getSubscription(String address) {
      return subscriptions.get(address) != null ? subscriptions.get(address).getA() : null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTUtil.java`
#### Snippet
```java
      }

      return defaultReturnValue == null ? null : defaultReturnValue;
   }
}
```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenWireConnection.java`
#### Snippet
```java
         }
         amqConsumerBrokerExchange.processMessagePull(arg0);
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenWireConnection.java`
#### Snippet
```java
               if (protocolManager.getRoutingHandler().route(OpenWireConnection.this, info)) {
                  shutdown(true);
                  return null;
               }
            }
```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenWireConnection.java`
#### Snippet
```java
            dispatchAsync(command);
         }
         return null;

      }
```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenWireConnection.java`
#### Snippet
```java
      public Response processBrokerSubscriptionInfo(BrokerSubscriptionInfo brokerSubscriptionInfo) throws Exception {
         // TBD
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenWireConnection.java`
#### Snippet
```java
   private Transaction lookupTX(TransactionId txID, AMQSession session, boolean remove) throws Exception {
      if (txID == null) {
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenWireConnection.java`
#### Snippet
```java

      if (transaction == null) {
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenWireConnection.java`
#### Snippet
```java
         //this can be sent over a reconnected transport as the first command
         //before restoring the connection.
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenWireConnection.java`
#### Snippet
```java
         }

         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenWireConnection.java`
#### Snippet
```java
            // log
         }
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenWireConnection.java`
#### Snippet
```java
         }

         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenWireConnection.java`
#### Snippet
```java
      @Override
      public Response processMessageDispatchNotification(MessageDispatchNotification arg0) throws Exception {
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenWireConnection.java`
#### Snippet
```java
         server.destroyQueue(subQueueName);

         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenWireConnection.java`
#### Snippet
```java
   @Override
   public String getClientID() {
      return context != null ? context.getClientId() : null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenWireConnection.java`
#### Snippet
```java
         }

         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenWireConnection.java`
#### Snippet
```java
   private ConnectionInfo getConnectionInfo() {
      if (state == null) {
         return null;
      }
      return state.getInfo();
```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenWireConnection.java`
#### Snippet
```java
      ConnectionInfo info = getConnectionInfo();
      if (info == null) {
         return null;
      }
      return info.getPassword();
```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenWireConnection.java`
#### Snippet
```java
         }

         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenWireConnection.java`
#### Snippet
```java
            internalSession.resetTX(null);
         }
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenWireConnection.java`
#### Snippet
```java
         ActiveMQDestination dest = info.getDestination();
         removeDestination(dest);
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenWireConnection.java`
#### Snippet
```java

         }
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenWireConnection.java`
#### Snippet
```java
      @Override
      public Response processMessageDispatch(MessageDispatch arg0) throws Exception {
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenWireConnection.java`
#### Snippet
```java
            getSession(id.getParentId()).getCoreSession().removeProducer(id.toString());
         }
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenWireConnection.java`
#### Snippet
```java
      public Response processAddConsumer(ConsumerInfo info) throws Exception {
         addConsumer(info);
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenWireConnection.java`
#### Snippet
```java
            //log error
         }
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenWireConnection.java`
#### Snippet
```java
         }

         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenWireConnection.java`
#### Snippet
```java
         protocolManager.sendBrokerInfo(OpenWireConnection.this);
         protocolManager.configureInactivityParams(OpenWireConnection.this, command);
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenWireConnection.java`
#### Snippet
```java
            state.addSession(info);
         }
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenWireConnection.java`
#### Snippet
```java
      public Response processShutdown(ShutdownInfo info) throws Exception {
         OpenWireConnection.this.shutdown(false);
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenWireConnection.java`
#### Snippet
```java
      public Response processProducerAck(ProducerAck arg0) throws Exception {
         // a broker doesn't do producers.. this shouldn't happen
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenWireConnection.java`
#### Snippet
```java
      public Response processRemoveConsumer(ConsumerId id, long lastDeliveredSequenceId) throws Exception {
         if (destroyed) {
            return null;
         }
         SessionId sessionId = id.getParentId();
```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenWireConnection.java`
#### Snippet
```java
         consumerBrokerExchange.removeConsumer();

         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenWireConnection.java`
#### Snippet
```java
            session.getCoreSession().resetTX(null);
         }
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenWireConnection.java`
#### Snippet
```java
         propagateLastSequenceId(session, lastDeliveredSequenceId);
         removeSession(context, session.getInfo());
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenWireConnection.java`
#### Snippet
```java
      ConnectionInfo info = getConnectionInfo();
      if (info == null) {
         return null;
      }
      return info.getUserName();
```

### ReturnNull
Return of `null`
in `artemis-dto/src/main/java/org/apache/activemq/artemis/dto/BindingDTO.java`
#### Snippet
```java
   private String marshalArray(String[] array) {
      if (array == null || (array.length == 1 && array[0] == null)) {
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-dto/src/main/java/org/apache/activemq/artemis/dto/BindingDTO.java`
#### Snippet
```java
   private String[] unmarshalArray(String text) {
      if (text == null) {
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-quorum-ri/src/main/java/org/apache/activemq/artemis/quorum/zookeeper/CuratorDistributedLock.java`
#### Snippet
```java
      run(() -> {
         listeners.remove(listener);
         return null;
      });
   }
```

### ReturnNull
Return of `null`
in `artemis-quorum-ri/src/main/java/org/apache/activemq/artemis/quorum/zookeeper/CuratorDistributedLock.java`
#### Snippet
```java
         listeners.add(listener);
         fireUnavailableListener(listener::onUnavailableLockEvent);
         return null;
      });
   }
```

### ReturnNull
Return of `null`
in `artemis-quorum-ri/src/main/java/org/apache/activemq/artemis/quorum/zookeeper/CuratorDistributedLock.java`
#### Snippet
```java
            }
         }
         return null;
      });
   }
```

### ReturnNull
Return of `null`
in `artemis-quorum-ri/src/main/java/org/apache/activemq/artemis/quorum/zookeeper/CuratorMutableLong.java`
#### Snippet
```java
         try {
            atomicLong.forceSet(value);
            return null;
         } catch (Throwable e) {
            throw new UnavailableStateException(e);
```

### ReturnNull
Return of `null`
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalImpl.java`
#### Snippet
```java
               Exception reportingException = ActiveMQJournalBundle.BUNDLE.unexpectedFileSize(currentFile.getFile().getFileName(), size, currentFile.getFile().size());
               fileFactory.onIOError(reportingException, reportingException.getMessage());
               return null;
            }
         }
```

### ReturnNull
Return of `null`
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalImpl.java`
#### Snippet
```java
      } catch (Throwable e) {
         criticalIO(e, null);
         return null; // this will never happen, the method will call throw
      }
   }
```

### ReturnNull
Return of `null`
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalImpl.java`
#### Snippet
```java
      try {
         if (state != JournalState.LOADED) {
            return null;
         }

```

### ReturnNull
Return of `null`
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalImpl.java`
#### Snippet
```java
         if (dataFilesToProcess.size() == 0) {
            logger.trace("Finishing compacting, nothing to process");
            return null;
         }

```

### ReturnNull
Return of `null`
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQConnection.java`
#### Snippet
```java

      // We offer a RA, so no need to implement this for MDBs
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQConnection.java`
#### Snippet
```java
            public Object run() {
               failoverListenerExecutor.shutdown();
               return null;
            }
         });
```

### ReturnNull
Return of `null`
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQConnection.java`
#### Snippet
```java
                                                            ServerSessionPool sessionPool,
                                                            int maxMessages) throws JMSException {
      return null; // we offer RA
   }

```

### ReturnNull
Return of `null`
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQConnection.java`
#### Snippet
```java
      checkClosed();
      checkTempQueues(queue);
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQConnection.java`
#### Snippet
```java
                                                                   ServerSessionPool sessionPool,
                                                                   int maxMessages) throws JMSException {
      return null; // we offer RA
   }

```

### ReturnNull
Return of `null`
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQConnection.java`
#### Snippet
```java
      checkTempQueues(topic);
      // We offer RA, so no need for this
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQConnection.java`
#### Snippet
```java
      checkClosed();
      checkTempQueues(topic);
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQSession.java`
#### Snippet
```java

      if (!query.isExists() && !query.isAutoCreateAddresses()) {
         return null;
      } else {
         return topic;
```

### ReturnNull
Return of `null`
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQSession.java`
#### Snippet
```java

      if (!response.isExists() && !response.isAutoCreateQueues()) {
         return null;
      } else {
         return queue;
```

### ReturnNull
Return of `null`
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQSession.java`
#### Snippet
```java
      checkClosed();

      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQQueueBrowser.java`
#### Snippet
```java
   @Override
   public String getMessageSelector() throws JMSException {
      return filterString == null ? null : filterString.toString();
   }

```

### ReturnNull
Return of `null`
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQQueueBrowser.java`
#### Snippet
```java
               ActiveMQJMSClientLogger.LOGGER.errorCreatingMessage(msg.getCoreMessage().toString(), e);

               return null;
            }
            return msg;
```

### ReturnNull
Return of `null`
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQConnectionFactory.java`
#### Snippet
```java
               ClientProtocolManagerFactory protocolManagerFactory = (ClientProtocolManagerFactory) ClassloadingUtil.newInstanceFromClassLoader(ActiveMQConnectionFactory.class, protocolManagerFactoryStr);
               serverLocator.setProtocolManagerFactory(protocolManagerFactory);
               return null;
            }
         });
```

### ReturnNull
Return of `null`
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQMapMessage.java`
#### Snippet
```java
         SimpleString str = map.getSimpleStringProperty(new SimpleString(name));
         if (str == null) {
            return null;
         } else {
            return str.toString();
```

### ReturnNull
Return of `null`
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQTextMessage.java`
#### Snippet
```java
         return text.toString();
      } else {
         return null;
      }
   }
```

### ReturnNull
Return of `null`
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQJMSConsumer.java`
#### Snippet
```java
         Message message = consumer.receive();
         context.setLastMessage(ActiveMQJMSConsumer.this, message);
         return message == null ? null : message.getBody(c);
      } catch (JMSException e) {
         throw JmsExceptionUtils.convertToRuntimeException(e);
```

### ReturnNull
Return of `null`
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQJMSConsumer.java`
#### Snippet
```java
         Message message = consumer.receive(timeout);
         context.setLastMessage(ActiveMQJMSConsumer.this, message);
         return message == null ? null : message.getBody(c);
      } catch (JMSException e) {
         throw JmsExceptionUtils.convertToRuntimeException(e);
```

### ReturnNull
Return of `null`
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQJMSConsumer.java`
#### Snippet
```java
         Message message = consumer.receiveNoWait();
         context.setLastMessage(ActiveMQJMSConsumer.this, message);
         return message == null ? null : message.getBody(c);
      } catch (JMSException e) {
         throw JmsExceptionUtils.convertToRuntimeException(e);
```

### ReturnNull
Return of `null`
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQBytesMessage.java`
#### Snippet
```java
   protected <T> T getBodyInternal(Class<T> c) {
      if (bodyLength == 0)
         return null;
      byte[] dst = new byte[bodyLength];
      message.getBodyBuffer().getBytes(CoreMessage.BODY_OFFSET, dst);
```

### ReturnNull
Return of `null`
in `artemis-quorum-ri/src/main/java/org/apache/activemq/artemis/quorum/zookeeper/CuratorDistributedPrimitiveManager.java`
#### Snippet
```java
      static <T extends CuratorDistributedPrimitive> T validatePrimitiveInstance(T primitive) {
         if (primitive == null) {
            return null;
         }
         boolean valid = false;
```

### ReturnNull
Return of `null`
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQDestination.java`
#### Snippet
```java
   public static ActiveMQDestination createDestination(RoutingType routingType, SimpleString address) {
      if (address == null) {
         return null;
      } else if (RoutingType.ANYCAST.equals(routingType)) {
         return ActiveMQDestination.createQueue(address);
```

### ReturnNull
Return of `null`
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQDestination.java`
#### Snippet
```java

   public String getAddress() {
      return simpleAddress != null ? simpleAddress.toString() : null;
   }

```

### ReturnNull
Return of `null`
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQDestination.java`
#### Snippet
```java
               return DESTINATION;
            default:
               return null;
         }
      }
```

### ReturnNull
Return of `null`
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/compatible1X/ActiveMQCompatibleMessage.java`
#### Snippet
```java
      }

      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/compatible1X/ActiveMQCompatibleMessage.java`
#### Snippet
```java
      if (dest == null) {
         MessageUtil.setJMSReplyTo(message, (String) null);
         return null;
      } else {
         if (dest instanceof ActiveMQDestination == false) {
```

### ReturnNull
Return of `null`
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/compatible1X/ActiveMQCompatibleMessage.java`
#### Snippet
```java
      }

      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQObjectMessage.java`
#### Snippet
```java
   private String getDeserializationBlackList() {
      if (options == null) {
         return null;
      } else {
         return options.getDeserializationBlackList();
```

### ReturnNull
Return of `null`
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQObjectMessage.java`
#### Snippet
```java
   public Serializable getObject() throws JMSException {
      if (data == null || data.length == 0) {
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQObjectMessage.java`
#### Snippet
```java
   private String getDeserializationWhiteList() {
      if (options == null) {
         return null;
      } else {
         return options.getDeserializationWhiteList();
```

### ReturnNull
Return of `null`
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/uri/JMSConnectionOptions.java`
#### Snippet
```java
         }
      } catch (Exception e) {
         return null;
      }
   }
```

### ReturnNull
Return of `null`
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQJMSProducer.java`
#### Snippet
```java
         SimpleString prop = properties.getSimpleStringProperty(new SimpleString(name));
         if (prop == null)
            return null;
         return prop.toString();
      } catch (ActiveMQPropertyConversionException ce) {
```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/sasl/AnonymousServerSASL.java`
#### Snippet
```java
   @Override
   public byte[] processSASL(byte[] bytes) {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQMessage.java`
#### Snippet
```java
         return getBodyInternal(c);
      } else if (hasNoBody()) {
         return null;
      }
      // XXX HORNETQ-1209 Do we need translations here?
```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/sasl/PlainSASLResult.java`
#### Snippet
```java
   @Override
   public Subject getSubject() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/sasl/ServerSASLPlain.java`
#### Snippet
```java
      result = new PlainSASLResult(success, username, password);

      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/sasl/GSSAPIServerSASLFactory.java`
#### Snippet
```java
      }
      logger.debug("SASL GSSAPI requires ProtonProtocolManager");
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/sasl/ExternalServerSASLFactory.java`
#### Snippet
```java
      }
      logger.debug("SASL EXTERNAL mechanism requires a TLS peer principal");
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/sasl/GSSAPIServerSASL.java`
#### Snippet
```java
         result = new PrincipalSASLResult(false, null);
      }
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/sasl/scram/SCRAMServerSASL.java`
#### Snippet
```java
   public SASLResult result() {
      if (result instanceof SCRAMSASLResult) {
         return scram.isEnded() ? result : null;
      }
      return result;
```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/sasl/scram/SCRAMServerSASL.java`
#### Snippet
```java
      @Override
      public String getUser() {
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/sasl/scram/SCRAMServerSASL.java`
#### Snippet
```java
         failed(e);
      }
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/sasl/scram/SCRAMServerSASL.java`
#### Snippet
```java
      @Override
      public Subject getSubject() {
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/sasl/scram/SCRAMServerSASLFactory.java`
#### Snippet
```java
         // can't be used then...
      }
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/sasl/scram/ScramClientFunctionalityImpl.java`
#### Snippet
```java
      if (!m.matches()) {
         mState = State.ENDED;
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/sasl/scram/ScramClientFunctionalityImpl.java`
#### Snippet
```java
      if (!nonce.startsWith(mClientNonce)) {
         mState = State.ENDED;
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/sasl/scram/ScramClientFunctionalityImpl.java`
#### Snippet
```java
      if (iterations <= 0) {
         mState = State.ENDED;
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-junit/artemis-junit-commons/src/main/java/org/apache/activemq/artemis/junit/EmbeddedJMSDelegate.java`
#### Snippet
```java
                                       destination.getAddress()),
                         ex);
            return null;
         }
         if (bindingQueryResult.isExists()) {
```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/KMPNeedle.java`
#### Snippet
```java
      }
      // optimization over the original algorithm: it would save from accessing any jump table
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jndi/ActiveMQInitialContextFactory.java`
#### Snippet
```java
      Map<String, String> queryParams = URISchema.parseQuery(parser.expandURI(uri).getQuery(), null);
      String type = queryParams.get("type");
      return type == null ? null : JMSConnectionOptions.convertCFType(type);
   }
}
```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPConnectionCallback.java`
#### Snippet
```java
         }
      }
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/client/ProtonClientProtocolManager.java`
#### Snippet
```java
   @Override
   public ClientProtocolManager setExecutor(Executor executor) {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPSessionCallback.java`
#### Snippet
```java
            result = null;
            address = null;
            return null;
         }
      }
```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPSessionCallback.java`
#### Snippet
```java
         return this.transactionHandler.getCurrentTransaction();
      }
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPStandardMessage.java`
#### Snippet
```java
         return (String) ((AmqpValue) body).getValue();
      } else {
         return null;
      }
   }
```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPLargeMessage.java`
#### Snippet
```java
      } catch (Exception e) {
         ActiveMQServerLogger.LOGGER.lareMessageErrorCopying(this, e);
         return null;
      }
   }
```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/AmqpSupport.java`
#### Snippet
```java

      if (filters == null || filters.isEmpty()) {
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/AmqpSupport.java`
#### Snippet
```java
      }

      return null;
   }
}
```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/ProtonServerReceiverContext.java`
#### Snippet
```java

   private Outcome getEffectiveDefaultOutcome(final Source source) {
      return (source.getOutcomes() == null || source.getOutcomes().length == 0) ? source.getDefaultOutcome() : null;
   }
}
```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/handler/ExecutorNettyAdapter.java`
#### Snippet
```java
   @Override
   public EventLoop next() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/handler/ExecutorNettyAdapter.java`
#### Snippet
```java
                                                             long timeout,
                                                             TimeUnit unit) throws InterruptedException {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/handler/ExecutorNettyAdapter.java`
#### Snippet
```java
   @Override
   public Iterator<EventExecutor> iterator() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/handler/ExecutorNettyAdapter.java`
#### Snippet
```java
   @Override
   public Future<?> shutdownGracefully() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/handler/ExecutorNettyAdapter.java`
#### Snippet
```java
   @Override
   public <V> ScheduledFuture<V> schedule(Callable<V> callable, long delay, TimeUnit unit) {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/handler/ExecutorNettyAdapter.java`
#### Snippet
```java
   @Override
   public ScheduledFuture<?> schedule(Runnable command, long delay, TimeUnit unit) {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/handler/ExecutorNettyAdapter.java`
#### Snippet
```java
   @Override
   public List<Runnable> shutdownNow() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/handler/ExecutorNettyAdapter.java`
#### Snippet
```java
   @Override
   public <V> Promise<V> newPromise() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/handler/ExecutorNettyAdapter.java`
#### Snippet
```java
   public Future<?> submit(Runnable task) {
      execute(task);
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/handler/ExecutorNettyAdapter.java`
#### Snippet
```java
   @Override
   public ChannelFuture register(Channel channel) {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/handler/ExecutorNettyAdapter.java`
#### Snippet
```java
   @Override
   public <V> Future<V> newSucceededFuture(V result) {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/handler/ExecutorNettyAdapter.java`
#### Snippet
```java
   @Override
   public ScheduledFuture<?> scheduleAtFixedRate(Runnable command, long initialDelay, long period, TimeUnit unit) {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/handler/ExecutorNettyAdapter.java`
#### Snippet
```java
   public <T> Future<T> submit(Runnable task, T result) {
      execute(task);
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/handler/ExecutorNettyAdapter.java`
#### Snippet
```java
   @Override
   public <V> ProgressivePromise<V> newProgressivePromise() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/handler/ExecutorNettyAdapter.java`
#### Snippet
```java
   @Override
   public ChannelFuture register(ChannelPromise promise) {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/handler/ExecutorNettyAdapter.java`
#### Snippet
```java
   @Override
   public ScheduledFuture<?> scheduleWithFixedDelay(Runnable command, long initialDelay, long delay, TimeUnit unit) {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/handler/ExecutorNettyAdapter.java`
#### Snippet
```java
   @Override
   public Future<?> terminationFuture() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/handler/ExecutorNettyAdapter.java`
#### Snippet
```java
   @Override
   public <T> T invokeAny(Collection<? extends Callable<T>> tasks) throws InterruptedException, ExecutionException {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/handler/ExecutorNettyAdapter.java`
#### Snippet
```java
   @Override
   public Future<?> shutdownGracefully(long quietPeriod, long timeout, TimeUnit unit) {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/handler/ExecutorNettyAdapter.java`
#### Snippet
```java
   @Override
   public EventLoopGroup parent() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/handler/ExecutorNettyAdapter.java`
#### Snippet
```java
                          long timeout,
                          TimeUnit unit) throws InterruptedException, ExecutionException, TimeoutException {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/handler/ExecutorNettyAdapter.java`
#### Snippet
```java
   @Override
   public <V> Future<V> newFailedFuture(Throwable cause) {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/handler/ExecutorNettyAdapter.java`
#### Snippet
```java
   @Override
   public ChannelFuture register(Channel channel, ChannelPromise promise) {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/handler/ExecutorNettyAdapter.java`
#### Snippet
```java
   @Override
   public <T> List<java.util.concurrent.Future<T>> invokeAll(Collection<? extends Callable<T>> tasks) throws InterruptedException {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/handler/ExecutorNettyAdapter.java`
#### Snippet
```java
   @Override
   public <T> Future<T> submit(Callable<T> task) {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/handler/ProtonHandler.java`
#### Snippet
```java
               @Override
               public Subject getSubject() {
                  return null;
               }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/handler/ProtonHandler.java`
#### Snippet
```java
               @Override
               public String getUser() {
                  return null;
               }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
   @Override
   public final Object getCorrelationID() {
      return properties != null ? properties.getCorrelationId() : null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
      TypedProperties extra = getExtraProperties();
      if (extra == null) {
         return null;
      }
      return extra.getProperty(key);
```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
            }
         } else {
            return null;
         }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
         }

         return null;
      }
   }
```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
               return properties.getSubject();
            }
            return null;
         case MessageUtil.CONNECTION_ID_PROPERTY_NAME_STRING:
            return getConnectionID();
```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
               return AMQPMessageIdHelper.INSTANCE.toCorrelationIdString(properties.getCorrelationId());
            }
            return null;
         default:
            return getApplicationObjectProperty(key);
```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
   public final byte[] getExtraBytesProperty(SimpleString key) throws ActiveMQPropertyConversionException {
      if (extraProperties == null) {
         return null;
      } else {
         return extraProperties.getBytesProperty(key);
```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java

   protected SimpleString.StringSimpleStringPool getPropertyKeysPool() {
      return coreMessageObjectPools == null ? null : coreMessageObjectPools.getPropertiesStringSimpleStringPools().getPropertyKeysPool();
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
   public final byte[] removeExtraBytesProperty(SimpleString key) throws ActiveMQPropertyConversionException {
      if (extraProperties == null) {
         return null;
      } else {
         return (byte[])extraProperties.removeProperty(key);
```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
         if (!AMQPMessageSymbolSearch.anyApplicationProperties(getData(), DUPLICATE_ID_NEEDLES, applicationPropertiesPosition)) {
            // no need for duplicate-property
            return null;
         }
      }
```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
            coreMessageObjectPools == null ? null : coreMessageObjectPools.getGroupIdStringSimpleStringPool());
      } else {
         return null;
      }
   }
```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
         return properties.getMessageId();
      } else {
         return null;
      }
   }
```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
      // we can exit without doing any reads or buffer hopping.
      if (scanStartPosition == VALUE_NOT_PRESENT) {
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
         return new String(binary.getArray(), binary.getArrayOffset(), binary.getLength(), StandardCharsets.UTF_8);
      } else {
         return null;
      }
   }
```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
      }
      if (daToUse == null) {
         return null;
      } else {
         return daToUse.getValue().get(symbol);
```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
         return SimpleString.toSimpleString(properties.getReplyTo());
      } else {
         return null;
      }
   }
```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
   public final String getAddress() {
      SimpleString addressSimpleString = getAddressSimpleString();
      return addressSimpleString == null ? null : addressSimpleString.toString();
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java

   protected SimpleString.StringSimpleStringPool getPropertyValuesPool() {
      return coreMessageObjectPools == null ? null : coreMessageObjectPools.getPropertiesStringSimpleStringPools().getPropertyValuesPool();
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/connect/AMQPBrokerConnectionManager.java`
#### Snippet
```java
                                        List<Interceptor> outgoingInterceptors,
                                        TopologyResponseHandler topologyResponseHandler) {
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/connect/AMQPBrokerConnectionManager.java`
#### Snippet
```java
      @Override
      public RemotingConnection getCurrentConnection() {
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/connect/AMQPBrokerConnectionManager.java`
#### Snippet
```java
      @Override
      public Lock lockSessionCreation() {
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/connect/AMQPBrokerConnectionManager.java`
#### Snippet
```java
                                                 int confirmationWindowSize,
                                                 String clientID) throws ActiveMQException {
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/connect/AMQPBrokerConnectionManager.java`
#### Snippet
```java
      @Override
      public ClientProtocolManager setExecutor(Executor executor) {
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/connect/AMQPBrokerConnectionManager.java`
#### Snippet
```java
      @Override
      public ClientSessionFactory getSessionFactory() {
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/connect/AMQPBrokerConnectionManager.java`
#### Snippet
```java
      @Override
      public String getName() {
         return null;
      }
   }
```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/connect/mirror/BasicMirrorController.java`
#### Snippet
```java
         return (String)link.getRemoteProperties().get(AMQPMirrorControllerSource.BROKER_ID);
      } else {
         return null;
      }
   }
```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/AMQPConnectionContext.java`
#### Snippet
```java

   public String getPubSubPrefix() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/AMQPConnectionContext.java`
#### Snippet
```java
      @Override
      public Future<?> apply(Future<?> future) {
         return (future != null) ? scheduledPool.schedule(scheduleRunnable, delay, TimeUnit.MILLISECONDS) : null;
      }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/ProtonServerSenderContext.java`
#### Snippet
```java
            }
         }
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/connect/mirror/ReferenceNodeStore.java`
#### Snippet
```java
         // in the case the nodeID being from the originating server.
         // don't be tempted to return this.serverID here.
         return null;
      }
   }
```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/converter/AMQPContentTypeSupport.java`
#### Snippet
```java
      }

      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/connect/mirror/AMQPMirrorControllerSource.java`
#### Snippet
```java
         return AMQPMessageBrokerAccessor.getCurrentProperties((AMQPMessage)message);
      } else {
         return null;
      }
   }
```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/connect/mirror/AMQPMirrorControllerSource.java`
#### Snippet
```java
   private MirrorSendOperation getSendOperation(Transaction tx) {
      if (tx == null) {
         return null;
      }
      MirrorSendOperation sendOperation = (MirrorSendOperation) tx.getProperty(TransactionPropertyIndexes.MIRROR_SEND_OPERATION);
```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/converter/AMQPMessageIdHelper.java`
#### Snippet
```java
   private String convertToIdString(Object idObject) {
      if (idObject == null) {
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/converter/AMQPMessageIdHelper.java`
#### Snippet
```java
   public Object toIdObject(final String origId) throws ActiveMQAMQPIllegalStateException {
      if (origId == null) {
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/converter/coreWrapper/CoreBytesMessageWrapper.java`
#### Snippet
```java
      switch (orignalEncoding) {
         case AMQP_NULL:
            return null;
         case AMQP_VALUE_BINARY:
            return new AmqpValue(payload);
```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/converter/coreWrapper/CoreTextMessageWrapper.java`
#### Snippet
```java
         return text.toString();
      } else {
         return null;
      }
   }
```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/converter/AMQPMessageSupport.java`
#### Snippet
```java
      }

      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/converter/AMQPMessageSupport.java`
#### Snippet
```java
         return AMQPContentTypeSupport.parseContentTypeForTextualCharset(contentType);
      } catch (ActiveMQAMQPInvalidContentTypeException e) {
         return null;
      }
   }
```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/converter/coreWrapper/CoreMessageWrapper.java`
#### Snippet
```java
   public SimpleString getDestination()  {
      if (message.getAddress() == null || message.getAddress().isEmpty()) {
         return null;
      }
      return SimpleString.toSimpleString(AMQPMessageSupport.destination(message.getRoutingType(), message.getAddress()));
```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/converter/coreWrapper/CoreMessageWrapper.java`
#### Snippet
```java
         }

         return null;
      } catch (Throwable e) {
         logger.debug("Exception ignored during conversion {}", e.getMessage(), e);
```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/converter/coreWrapper/CoreMessageWrapper.java`
#### Snippet
```java
         return String.valueOf(correlationID);
      } else {
         return null;
      }
   }
```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/converter/coreWrapper/CoreMessageWrapper.java`
#### Snippet
```java
         return getStringProperty(NATIVE_MESSAGE_ID);
      }
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/converter/coreWrapper/CoreMapMessageWrapper.java`
#### Snippet
```java
      SimpleString str = map.getSimpleStringProperty(new SimpleString(name));
      if (str == null) {
         return null;
      } else {
         return str.toString();
```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/connect/AMQPBrokerConnection.java`
#### Snippet
```java
      }

      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/connect/AMQPBrokerConnection.java`
#### Snippet
```java
         }

         return null;
      }
   }
```

### ReturnNull
Return of `null`
in `artemis-jdbc-store/src/main/java/org/apache/activemq/artemis/jdbc/store/file/JDBCSequentialFile.java`
#### Snippet
```java
   @Override
   public File getJavaFile() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-jdbc-store/src/main/java/org/apache/activemq/artemis/jdbc/store/file/JDBCSequentialFile.java`
#### Snippet
```java
         fileFactory.onIOError(e, "Error cloning JDBC file.", this);
      }
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-jdbc-store/src/main/java/org/apache/activemq/artemis/jdbc/store/file/JDBCSequentialFile.java`
#### Snippet
```java
   @Override
   public ByteBuffer map(int position, long size) throws IOException {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-jdbc-store/src/main/java/org/apache/activemq/artemis/jdbc/store/file/JDBCSequentialFileFactory.java`
#### Snippet
```java
   @Override
   public File getDirectory() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-jdbc-store/src/main/java/org/apache/activemq/artemis/jdbc/store/file/JDBCSequentialFileFactory.java`
#### Snippet
```java
         criticalErrorListener.onIOException(e, "Error whilst creating JDBC file", null);
      }
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-jdbc-store/src/main/java/org/apache/activemq/artemis/jdbc/store/sql/PropertySQLProvider.java`
#### Snippet
```java
         } catch (Exception e) {
            logger.debug("Unable to read JDBC metadata.", e);
            return null;
         }
      }
```

### ReturnNull
Return of `null`
in `artemis-jdbc-store/src/main/java/org/apache/activemq/artemis/jdbc/store/sql/PropertySQLProvider.java`
#### Snippet
```java
         public static SQLDialect identifyDialect(String name) {
            if (name == null) {
               return null;
            }
            //use a lower case name to make it more resilient
```

### ReturnNull
Return of `null`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/converter/CoreAmqpConverter.java`
#### Snippet
```java
   public static AMQPMessage fromCore(ICoreMessage coreMessage, StorageManager storageManager) throws ConversionException {
      if (coreMessage == null) {
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-web/src/main/java/org/apache/activemq/artemis/component/WebTmpCleaner.java`
#### Snippet
```java
      }
      cleanupFilesWithRetry(files.toArray(new String[files.size()]), 2);
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/Message.java`
#### Snippet
```java
         return value.toString();
      } else {
         return null;
      }
   }
```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/Message.java`
#### Snippet
```java

   default Object getDuplicateProperty() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/Message.java`
#### Snippet
```java

   default Object getCorrelationID() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/Message.java`
#### Snippet
```java

   default  String getValidatedUserID() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/Message.java`
#### Snippet
```java
   @Deprecated
   default ActiveMQBuffer getBodyBuffer() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/Message.java`
#### Snippet
```java

   default RoutingType getRoutingType() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/Message.java`
#### Snippet
```java

   default SimpleString getGroupID() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/Message.java`
#### Snippet
```java

   default CompositeData toCompositeData(int fieldsLimit, int deliveryCount) throws OpenDataException {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/Message.java`
#### Snippet
```java

   default String getStringBody() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/Message.java`
#### Snippet
```java

   default String getConnectionID() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/Message.java`
#### Snippet
```java

   default SimpleString getLastValueProperty() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/Message.java`
#### Snippet
```java

      if (duplicateID == null) {
         return null;
      } else {
         if (duplicateID instanceof SimpleString) {
```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/Message.java`
#### Snippet
```java
   @Deprecated
   default InputStream getBodyInputStream() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/JGroupsBroadcastEndpoint.java`
#### Snippet
```java
         return receiver.receiveBroadcast();
      } else {
         return null;
      }
   }
```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/JGroupsBroadcastEndpoint.java`
#### Snippet
```java
         return receiver.receiveBroadcast(time, unit);
      } else {
         return null;
      }
   }
```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/RefCountMessage.java`
#### Snippet
```java
   public Object getUserContext(Object key) {
      if (userContext == null) {
         return null;
      } else {
         return userContext.get(key);
```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/JsonUtil.java`
#### Snippet
```java
      public String toString() {
         if (this.value == null) {
            return null;
         }
         String s = this.escape;
```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/management/ManagementHelper.java`
#### Snippet
```java
         return JsonUtil.fromJsonArray(jsonArray);
      } else {
         return null;
      }
   }
```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/management/ManagementHelper.java`
#### Snippet
```java
         return JsonUtil.fromJsonArray(jsonArray);
      } else {
         return null;
      }
   }
```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/management/ManagementHelper.java`
#### Snippet
```java
         return JsonUtil.convertJsonValue(res[0], desiredType);
      } else {
         return null;
      }
   }
```

### ReturnNull
Return of `null`
in `artemis-jdbc-store/src/main/java/org/apache/activemq/artemis/jdbc/store/journal/JDBCJournalImpl.java`
#### Snippet
```java
   @Override
   public SequentialFileFactory getFileFactory() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-jdbc-store/src/main/java/org/apache/activemq/artemis/jdbc/store/journal/JDBCJournalImpl.java`
#### Snippet
```java
   @Override
   public JournalLoadInformation loadInternalOnly() throws Exception {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-jdbc-store/src/main/java/org/apache/activemq/artemis/jdbc/store/journal/JDBCJournalImpl.java`
#### Snippet
```java
   @Override
   public Map<Long, JournalFile> createFilesForBackupSync(long[] fileIds) throws Exception {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-jdbc-store/src/main/java/org/apache/activemq/artemis/jdbc/store/journal/JDBCJournalImpl.java`
#### Snippet
```java
   @Override
   public JournalLoadInformation loadSyncOnly(JournalState state) throws Exception {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/spi/core/remoting/Connection.java`
#### Snippet
```java

   default String getRouter() {
      return null;
   }
}
```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/spi/core/remoting/Connection.java`
#### Snippet
```java

   default String getSNIHostName() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/spi/core/protocol/AbstractRemotingConnection.java`
#### Snippet
```java
      FutureTask<Void> task = new FutureTask(() -> {
         fail(me);
         return null;
      });

```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/uri/schema/connector/InVMTransportConfigurationSchema.java`
#### Snippet
```java
   @Override
   protected URI internalNewURI(List<TransportConfiguration> bean) throws Exception {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/TopologyMemberImpl.java`
#### Snippet
```java
      TransportConfiguration backupConnector = getBackup();
      if (backupConnector == null) {
         return null;
      }
      Map<String, Object> props = backupConnector.getParams();
```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/TopologyMemberImpl.java`
#### Snippet
```java
         return new URI("tcp://" + host + ":" + port + "?" + TransportConstants.SSL_ENABLED_PROP_NAME + "=" + sslEnabled);
      } catch (URISyntaxException e) {
         return null;
      }
   }
```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/uri/schema/connector/TCPTransportConfigurationSchema.java`
#### Snippet
```java
   @Override
   protected URI internalNewURI(List<TransportConfiguration> bean) throws Exception {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/CompressedLargeMessageControllerImpl.java`
#### Snippet
```java
      int b = readByte();
      if (b == DataConstants.NULL) {
         return null;
      } else {
         return readSimpleString();
```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/CompressedLargeMessageControllerImpl.java`
#### Snippet
```java
      int b = readByte();
      if (b == DataConstants.NULL) {
         return null;
      } else {
         return readLong();
```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/CompressedLargeMessageControllerImpl.java`
#### Snippet
```java
      int b = readByte();
      if (b == DataConstants.NULL) {
         return null;
      } else {
         return readBoolean();
```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/CompressedLargeMessageControllerImpl.java`
#### Snippet
```java
      int b = readByte();
      if (b == DataConstants.NULL) {
         return null;
      } else {
         return readInt();
```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/CompressedLargeMessageControllerImpl.java`
#### Snippet
```java
      int b = readByte();
      if (b == DataConstants.NULL) {
         return null;
      } else {
         return readString();
```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/CompressedLargeMessageControllerImpl.java`
#### Snippet
```java
   @Override
   public ByteBuf byteBuf() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/Topology.java`
#### Snippet
```java
         }
      }
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/Topology.java`
#### Snippet
```java
      }

      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/config/TransformerConfiguration.java`
#### Snippet
```java
      // name is the only required value
      if (!json.containsKey(CLASS_NAME)) {
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/LargeMessageControllerImpl.java`
#### Snippet
```java
      int b = readByte();
      if (b == DataConstants.NULL) {
         return null;
      } else {
         return readLong();
```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/LargeMessageControllerImpl.java`
#### Snippet
```java
      int b = readByte();
      if (b == DataConstants.NULL) {
         return null;
      } else {
         return readInt();
```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/LargeMessageControllerImpl.java`
#### Snippet
```java
      int b = readByte();
      if (b == DataConstants.NULL) {
         return null;
      } else {
         return readBoolean();
```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/LargeMessageControllerImpl.java`
#### Snippet
```java
      int b = readByte();
      if (b == DataConstants.NULL) {
         return null;
      } else {
         return readSimpleString();
```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/LargeMessageControllerImpl.java`
#### Snippet
```java
   @Override
   public ByteBuf byteBuf() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/LargeMessageControllerImpl.java`
#### Snippet
```java
      int b = readByte();
      if (b == DataConstants.NULL) {
         return null;
      } else {
         return readString();
```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ClientConsumerImpl.java`
#### Snippet
```java
                     logger.trace("{}::There was nothing on the queue, leaving it now:: returning null", this);

                     return null;
                  } else {
                     logger.trace("{}::Ignored force delivery answer as it belonged to another call", this);
```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ClientConsumerImpl.java`
#### Snippet
```java
                     continue;
                  } else {
                     return null;
                  }
               }
```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ClientConsumerImpl.java`
#### Snippet
```java
               logger.trace("{}::Returning null", this);
               resetIfSlowConsumer();
               return null;
            }
         }
```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/server/ComponentConfigurationRoutingType.java`
#### Snippet
```java
            return PASS;
         default:
            return null;
      }
   }
```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ServerLocatorImpl.java`
#### Snippet
```java
            } catch (ActiveMQException e) {
               logger.trace("{}::Exception on establish connector initial connection", this, e);
               return null;
            }
         }
```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ServerLocatorImpl.java`
#### Snippet
```java

            if (initialConnectors.length == 0) {
               return null;
            }

```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ServerLocatorImpl.java`
#### Snippet
```java
               interceptors.add(interceptor);
            }
            return null;
         }
      });
```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ServerLocatorImpl.java`
#### Snippet
```java

               if (latch.await(config.retryInterval, TimeUnit.MILLISECONDS))
                  return null;
            }

```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ServerLocatorImpl.java`
#### Snippet
```java
         } catch (RejectedExecutionException e) {
            if (isClosed() || skipWarnings)
               return null;
            logger.trace("Rejected execution", e);
            throw e;
```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ServerLocatorImpl.java`
#### Snippet
```java
         } catch (Exception e) {
            if (isClosed() || skipWarnings)
               return null;
            ActiveMQClientLogger.LOGGER.errorConnectingToNodes(e);
            throw ActiveMQClientMessageBundle.BUNDLE.cannotConnectToStaticConnectors(e);
```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ServerLocatorImpl.java`
#### Snippet
```java

         if (isClosed() || skipWarnings) {
            return null;
         }

```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ServerLocatorImpl.java`
#### Snippet
```java

      if (topologyMember == null) {
         return null;
      }
      if (topologyMember.getLive() != null) {
```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ServerLocatorImpl.java`
#### Snippet
```java
      }
      // it shouldn't happen
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ServerLocatorImpl.java`
#### Snippet
```java
         public Object run() {
               loadBalancingPolicy = (ConnectionLoadBalancingPolicy) ClassloadingUtil.newInstanceFromClassLoader(ServerLocatorImpl.class, config.connectionLoadBalancingPolicyClassName);
               return null;
         }
      });
```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ClientSessionImpl.java`
#### Snippet
```java
   private ClientSessionInternal getSessionInternalFromXAResource(final XAResource xares) {
      if (xares == null) {
         return null;
      }
      if (xares instanceof ClientSessionInternal) {
```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ClientSessionImpl.java`
#### Snippet
```java
         return getSessionInternalFromXAResource(((ActiveMQXAResource) xares).getResource());
      }
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/message/openmbean/MessageOpenTypeFactory.java`
#### Snippet
```java
   protected String toString(Object value) {
      if (value == null) {
         return null;
      }
      return value.toString();
```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/PacketImpl.java`
#### Snippet
```java
   public SimpleString convertName(SimpleString name) {
      if (name == null) {
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/BackwardsCompatibilityUtils.java`
#### Snippet
```java
   private static Map<String, Object> convertParameters(Map<String, Object> params) {
      if (params == null) {
         return null;
      }
      Map<String, Object> convertedParams = new HashMap<>(params.size());
```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/message/impl/CoreMessage.java`
#### Snippet
```java

   private SimpleString.StringSimpleStringPool getPropertyValuesPool() {
      return coreMessageObjectPools == null ? null : coreMessageObjectPools.getPropertiesStringSimpleStringPools().getPropertyValuesPool();
   }

```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/message/impl/CoreMessage.java`
#### Snippet
```java

   private SimpleString.StringSimpleStringPool getPropertyKeysPool() {
      return coreMessageObjectPools == null ? null : coreMessageObjectPools.getPropertiesStringSimpleStringPools().getPropertyKeysPool();
   }

```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/message/impl/CoreMessage.java`
#### Snippet
```java

      if (str == null) {
         return null;
      } else {
         return str.toString();
```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/message/impl/CoreMessage.java`
#### Snippet
```java
   @Override
   public RoutingType getRoutingType() {
      final Byte maybeByte = getProperties().getByteProperty(Message.HDR_ROUTING_TYPE, () -> null);
      if (maybeByte == null) {
         return null;
```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/message/impl/CoreMessage.java`
#### Snippet
```java
      final Byte maybeByte = getProperties().getByteProperty(Message.HDR_ROUTING_TYPE, () -> null);
      if (maybeByte == null) {
         return null;
      }
      return RoutingType.getType(maybeByte);
```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/message/impl/CoreMessage.java`
#### Snippet
```java
   public String getAddress() {
      if (address == null) {
         return null;
      } else {
         return address.toString();
```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/message/impl/CoreMessage.java`
#### Snippet
```java
   @Override
   public InputStream getBodyInputStream() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/ActiveMQClientProtocolManager.java`
#### Snippet
```java
   public Channel getChannel1() {
      if (connection == null) {
         return null;
      } else {
         return connection.getChannel(1, -1);
```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/ActiveMQClientProtocolManager.java`
#### Snippet
```java
         try {
            if (connection == null) {
               return null;
            }

```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/ActiveMQClientProtocolManager.java`
#### Snippet
```java
            }

            return null;
         } finally {
            localFailoverLock.unlock();
```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/ActiveMQClientProtocolManager.java`
#### Snippet
```java
      } catch (InterruptedException e) {
         Thread.currentThread().interrupt();
         return null;
      }
   }
```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/ActiveMQClientProtocolManager.java`
#### Snippet
```java
   public Channel getChannel0() {
      if (connection == null) {
         return null;
      } else {
         return connection.getChannel(ChannelImpl.CHANNEL_ID.PING.id, -1);
```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/ChannelImpl.java`
#### Snippet
```java
      }

      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/QueueAbstractPacket.java`
#### Snippet
```java
         return OLD_TOPIC_PREFIX;
      } else {
         return null;
      }
   }
```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/CertificateUtil.java`
#### Snippet
```java
               } catch (Exception ex) {
                  logger.trace("Failed to convert SSL cert", ex);
                  return null;
               }
            }
```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/impl/TransportConfigurationUtil.java`
#### Snippet
```java
               return ClassloadingUtil.newInstanceFromClassLoader(TransportConfigurationUtil.class, className);
            } catch (IllegalStateException e) {
               return null;
            }
         }
```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/impl/ssl/SSLSupport.java`
#### Snippet
```java
         return InsecureTrustManagerFactory.INSTANCE;
      } else if ((truststorePath == null || truststorePath.isEmpty() || truststorePath.equalsIgnoreCase(NONE)) && (truststoreProvider == null || !truststoreProvider.toUpperCase().contains("PKCS11"))) {
         return null;
      } else {
         TrustManagerFactory trustMgrFactory = TrustManagerFactory.getInstance(TrustManagerFactory.getDefaultAlgorithm());
```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/impl/ssl/SSLSupport.java`
#### Snippet
```java
      KeyManagerFactory factory = loadKeyManagerFactory();
      if (factory == null) {
         return null;
      }
      KeyManager[] keyManagers = factory.getKeyManagers();
```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/impl/ssl/SSLSupport.java`
#### Snippet
```java
      TrustManagerFactory trustManagerFactory = loadTrustManagerFactory();
      if (trustManagerFactory == null) {
         return null;
      }
      return trustManagerFactory.getTrustManagers();
```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/impl/ssl/SSLSupport.java`
#### Snippet
```java
   private KeyManagerFactory loadKeyManagerFactory() throws Exception {
      if ((keystorePath == null || keystorePath.isEmpty() || keystorePath.equalsIgnoreCase(NONE)) && (keystoreProvider == null || !keystoreProvider.toUpperCase().contains("PKCS11"))) {
         return null;
      } else {
         KeyManagerFactory kmf = KeyManagerFactory.getInstance(KeyManagerFactory.getDefaultAlgorithm());
```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/impl/ssl/SSLSupport.java`
#### Snippet
```java
   private Collection<? extends CRL> loadCRL() throws Exception {
      if (crlPath == null) {
         return null;
      }
      URL resource = validateStoreURL(crlPath);
```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/NettyConnection.java`
#### Snippet
```java
   @Override
   public final ActiveMQPrincipal getDefaultActiveMQPrincipal() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/NettyConnection.java`
#### Snippet
```java
      SocketAddress address = channel.localAddress();
      if (address == null) {
         return null;
      }
      return "tcp://" + IPV6Util.encloseHost(address.toString());
```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/NettyConnection.java`
#### Snippet
```java
      SocketAddress address = channel.remoteAddress();
      if (address == null) {
         return null;
      }
      return address.toString();
```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/NettyConnection.java`
#### Snippet
```java
         return new TransportConfiguration(NettyConnectorFactory.class.getName(), this.configuration);
      } else {
         return null;
      }
   }
```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/utils/PrefixUtil.java`
#### Snippet
```java
         }
      }
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ClientSessionFactoryImpl.java`
#### Snippet
```java

         logger.debug("no connection been made, returning null");
         return null;
      } catch (Exception cause) {
         // Sanity catch for badly behaved remoting plugins
```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ClientSessionFactoryImpl.java`
#### Snippet
```java
         }
         connector = null;
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ClientSessionFactoryImpl.java`
#### Snippet
```java
      if (transportConnection == null) {
         logger.trace("Neither backup or live were active, will just give up now");
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ClientSessionFactoryImpl.java`
#### Snippet
```java
         if (closed || !clientProtocolManager.isAlive()) {
            session.close();
            return null;
         }
         sessions.add(session);
```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ClientSessionFactoryImpl.java`
#### Snippet
```java
         throw new IllegalStateException("ClientSessionFactory is closed!");
      if (!clientProtocolManager.isAlive())
         return null;
      synchronized (connectionLock) {
         if (connection != null) {
```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ClientSessionFactoryImpl.java`
#### Snippet
```java
                     connection.destroy();
                     this.connection = null;
                     return null;
                  }
               } catch (ActiveMQException e) {
```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ClientSessionFactoryImpl.java`
#### Snippet
```java
                  connection.destroy();
                  this.connection = null;
                  return null;
               }
            }
```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/utils/BufferHelper.java`
#### Snippet
```java
         return buffer.readInt();
      } else {
         return null;
      }
   }
```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/utils/BufferHelper.java`
#### Snippet
```java
         return buffer.readBoolean();
      } else {
         return null;
      }
   }
```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/utils/BufferHelper.java`
#### Snippet
```java
         return buffer.readLong();
      } else {
         return null;
      }
   }
```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/utils/BufferHelper.java`
#### Snippet
```java
   public static String readNullableSimpleStringAsString(ActiveMQBuffer buffer) {
      SimpleString str = buffer.readNullableSimpleString();
      return str != null ? str.toString() : null;
   }

```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/utils/BufferHelper.java`
#### Snippet
```java
         return buffer.readDouble();
      } else {
         return null;
      }
   }
```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/reader/MessageUtil.java`
#### Snippet
```java
         return message.getStringProperty(CORRELATIONID_HEADER_NAME);
      } catch (ActiveMQPropertyConversionException e) {
         return null;
      }
   }
```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/reader/MessageUtil.java`
#### Snippet
```java
         return ss.toString();
      } else {
         return null;
      }
   }
```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/reader/MessageUtil.java`
#### Snippet
```java
         return (byte[]) obj;
      } else {
         return null;
      }
   }
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/protocol/AbstractProtocolManager.java`
#### Snippet
```java
      }

      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/protocol/MessagePersister.java`
#### Snippet
```java
   public static Persister getPersister(byte id) {
      if (id == 0 || id > MAX_PERSISTERS) {
         return null;
      }
      return persisters[id - 1];
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/protocol/SessionCallback.java`
#### Snippet
```java

   default Transaction getCurrentTransaction() {
      return null;
   }
}
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/remoting/Acceptor.java`
#### Snippet
```java

   default ProtocolHandler getProtocolHandler() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/security/ActiveMQBasicSecurityManager.java`
#### Snippet
```java
      }

      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/security/ActiveMQJAASSecurityManager.java`
#### Snippet
```java
      } catch (LoginException e) {
         logger.debug("Couldn't validate user", e);
         return null;
      }
   }
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/security/jaas/CertificateLoginModule.java`
#### Snippet
```java
         return certs[0].getSubjectDN().getName();
      } else {
         return null;
      }
   }
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/security/jaas/PropertiesLoader.java`
#### Snippet
```java
            return new File(path).getParentFile();
         }
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/security/jaas/kubernetes/model/TokenReview.java`
#### Snippet
```java
      public static Extra fromJson(JsonObject obj) {
         if (obj == null) {
            return null;
         }
         Extra e = new Extra();
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/security/jaas/kubernetes/model/TokenReview.java`
#### Snippet
```java
      public static User fromJson(JsonObject obj) {
         if (obj == null) {
            return null;
         }
         User u = new User();
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/security/jaas/kubernetes/model/TokenReview.java`
#### Snippet
```java
   public String getUsername() {
      if (user == null) {
         return null;
      }
      return user.getUsername();
```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/NettyConnector.java`
#### Snippet
```java
   public final Connection createConnection(Consumer<ChannelFuture> onConnect) {
      if (channelClazz == null) {
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/NettyConnector.java`
#### Snippet
```java
                        ch.close().awaitUninterruptibly();
                        ActiveMQClientLogger.LOGGER.errorCreatingNettyConnection(new IllegalStateException("No ActiveMQChannelHandler has been found while connecting to " + remoteDestination + " from Channel with id = " + ch.id()));
                        return null;
                     }
                  }
```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/NettyConnector.java`
#### Snippet
```java
                  ch.close().awaitUninterruptibly();
                  ActiveMQClientLogger.LOGGER.errorCreatingNettyConnection(handshakeFuture.cause());
                  return null;
               }
            } else {
```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/NettyConnector.java`
#### Snippet
```java
               //handshakeFuture.setFailure(new SSLException("Handshake was not completed in 30 seconds"));
               ch.close().awaitUninterruptibly();
               return null;
            }

```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/NettyConnector.java`
#### Snippet
```java
               if (!httpUpgradeHandler.awaitHandshake()) {
                  ch.close().awaitUninterruptibly();
                  return null;
               }
            } catch (URISyntaxException e) {
```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/NettyConnector.java`
#### Snippet
```java
            } catch (URISyntaxException e) {
               ActiveMQClientLogger.LOGGER.errorCreatingNettyConnection(e);
               return null;
            }
         } else {
```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/NettyConnector.java`
#### Snippet
```java
                  new IllegalStateException("No ActiveMQChannelHandler has been found while connecting to " +
                                               remoteDestination + " from Channel with id = " + ch.id()));
               return null;
            }
         }
```

### ReturnNull
Return of `null`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/NettyConnector.java`
#### Snippet
```java
         }

         return null;
      }
   }
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/uri/schemas/clusterConnection/ClusterConnectionStaticSchema.java`
#### Snippet
```java
   @Override
   protected URI internalNewURI(ClusterConnectionConfiguration bean) throws Exception {
      return null;
   }
}
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/security/jaas/LDAPLoginModule.java`
#### Snippet
```java
            return conf.getPropertyValue();
         }
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/ClusterConnectionConfiguration.java`
#### Snippet
```java
         if (dg == null) {
            ActiveMQServerLogger.LOGGER.clusterConnectionNoDiscoveryGroup(discoveryGroupName);
            return null;
         }
         return dg;
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/ClusterConnectionConfiguration.java`
#### Snippet
```java
         return dg;
      } else {
         return null;
      }
   }
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/ClusterConnectionConfiguration.java`
#### Snippet
```java
         return list.toArray(new TransportConfiguration[list.size()]);
      } else {
         return staticConnectors != null ? configuration.getTransportConfigurations(staticConnectors) : null;
      }
   }
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/ClusterConnectionConfiguration.java`
#### Snippet
```java
      if (connector == null) {
         ActiveMQServerLogger.LOGGER.clusterConnectionNoConnector(connectorName);
         return null;
      }
      return connector;
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/ConfigurationUtils.java`
#### Snippet
```java
         }
      }
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/BridgeConfiguration.java`
#### Snippet
```java
      // name is the only required value
      if (!json.containsKey(NAME)) {
         return null;
      }
      BridgeConfiguration result = new BridgeConfiguration(json.getString(NAME));
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/filter/Filter.java`
#### Snippet
```java

   static SimpleString toFilterString(Filter filter) {
      return filter == null ? null : filter.getFilterString();
   }
}
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/PagingStore.java`
#### Snippet
```java

   default StorageManager getStorageManager() {
      return null;
   }
}
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/PagingManager.java`
#### Snippet
```java
    */
   default Future<Object> rebuildCounters(Set<Long> storedLargeMessages) {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/PagingStoreFactory.java`
#### Snippet
```java

   default ScheduledExecutorService getScheduledExecutor() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/PagingStoreFactory.java`
#### Snippet
```java

   default Executor newExecutor() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/filter/impl/FilterImpl.java`
#### Snippet
```java
   public static Filter createFilter(final SimpleString filterStr) throws ActiveMQException {
      if (filterStr == null || filterStr.length() == 0) {
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/filter/impl/FilterImpl.java`
#### Snippet
```java
      public Object getLocalConnectionId() {
         // Only needed if the NoLocal
         return null;
      }
   }
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/filter/impl/FilterImpl.java`
#### Snippet
```java
      @Override
      public <T> T getBodyAs(Class<T> type) throws FilterException {
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/filter/impl/FilterImpl.java`
#### Snippet
```java
      @Override
      public Object getLocalConnectionId() {
         return null;
      }
   }
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/filter/impl/FilterImpl.java`
#### Snippet
```java
         return msg.getGroupID();
      } else {
         return null;
      }
   }
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/impl/PagingManagerImpl.java`
#### Snippet
```java
      });

      FutureTask<Object> task = new FutureTask<>(() -> null);
      managerExecutor.execute(task);

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/impl/PagingManagerImpl.java`
#### Snippet
```java
   public PagingStore getPageStore(final SimpleString storeName) throws Exception {
      if (managementAddress != null && storeName.startsWith(managementAddress)) {
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/impl/PagingStoreImpl.java`
#### Snippet
```java
         try {
            if (!running) {
               return null;
            }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/impl/PagingStoreImpl.java`
#### Snippet
```java
               }
               // we don't deal with the current page, we let that one to be cleared from the regular depage
               return null;
            }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/impl/PagingStoreImpl.java`
#### Snippet
```java
         }
         storageManager.criticalError(e);
         return null;
      }
   }
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/impl/PagingStoreImpl.java`
#### Snippet
```java
      try {
         if (!running) {
            return null;
         }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/impl/PagingStoreImpl.java`
#### Snippet
```java

         if (numberOfPages == 0) {
            return null;
         } else {
            final Page returnPage;
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/impl/PagingStoreImpl.java`
#### Snippet
```java
                  // This will trigger this address to exit the page mode,
                  // and this will make ActiveMQ Artemis start using the journal again
                  return null;
               } else {
                  // We need to create a new page, as we can't lock the address until we finish depaging.
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/impl/PagingStoreImpl.java`
#### Snippet
```java
         return factoryUsed.getDirectory();
      } else {
         return null;
      }
   }
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/cursor/impl/PageCounterRebuildManager.java`
#### Snippet
```java
         return copiedSubscription.subscriptionCounter;
      } else {
         return null;
      }
   }
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/Consumer.java`
#### Snippet
```java

   default Binding getBinding() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/cursor/impl/PageSubscriptionImpl.java`
#### Snippet
```java

         if (page == null) {
            return null;
         }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/cursor/impl/PageSubscriptionImpl.java`
#### Snippet
```java
               initPage(nextPage);
            } else {
               return null;
            }
         }
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/cursor/impl/PageSubscriptionImpl.java`
#### Snippet
```java
         return pageStore.getPagingManager().getTransaction(reference.getTransactionID());
      } else {
         return null;
      }
   }
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/Queue.java`
#### Snippet
```java

   default MessageReference peekFirstMessage() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/Queue.java`
#### Snippet
```java

   default QueueConfiguration getQueueConfiguration() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/BucketMessageGroups.java`
#### Snippet
```java
   public C remove(SimpleString key) {
      if (buckets == null) {
         return null;
      }
      return remove(getBucket(key));
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/BucketMessageGroups.java`
#### Snippet
```java
   public C get(SimpleString key) {
      if (buckets == null) {
         return null;
      }
      return buckets[getBucket(key)];
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/FileBasedNodeManager.java`
#### Snippet
```java
      if (!createIfNotExists) {
         if (!Files.exists(activationSequencePath)) {
            return null;
         }
         openOptions = new OpenOption[]{READ, WRITE};
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ReplicationBackupActivation.java`
#### Snippet
```java
      }
      if (possibleLive == null) {
         return null;
      }
      final ClusterControl liveControl = tryConnectToNodeInReplicatedCluster(clusterController, possibleLive.getA());
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ReplicationBackupActivation.java`
#### Snippet
```java
         logger.debug(e.getMessage(), e);
      }
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ReplicationBackupActivation.java`
#### Snippet
```java
            synchronized (this) {
               if (closed) {
                  return null;
               }
            }
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ReplicationBackupActivation.java`
#### Snippet
```java
            }
            if (!activeMQServer.isStarted()) {
               return null;
            }
            logger.debug("ReplicationFailure = {}", failure);
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ReplicationBackupActivation.java`
#### Snippet
```java
                  // - due to restart/stop
                  if (!stopping.compareAndSet(false, true)) {
                     return null;
                  }
                  // no more interested into these events: handling it manually from here
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ReplicationBackupActivation.java`
#### Snippet
```java
                  // let's ignore the stopping flag here, we're already in control of it
                  asyncRestartServer(activeMQServer, true, false);
                  return null;
               case RegistrationError:
                  logger.error("Stopping broker because of critical registration error");
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ReplicationBackupActivation.java`
#### Snippet
```java
                  logger.error("Stopping broker because of critical registration error");
                  asyncRestartServer(activeMQServer, false);
                  return null;
               case AlreadyReplicating:
                  // can just retry here, data should be clean and nodeLocator
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ReplicationBackupActivation.java`
#### Snippet
```java
                  continue;
               case ClosedObserver:
                  return null;
               case BackupNotInSync:
                  // cleanup any dirty activation sequence to save a leaked activation sequence/nodeID to cause activation
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ReplicationBackupActivation.java`
#### Snippet
```java
                  }
                  asyncRestartServer(activeMQServer, restart);
                  return null;
               case WrongNodeId:
                  logger.error("Stopping broker because of wrong node ID communication from live: maybe a misbehaving live?");
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ReplicationBackupActivation.java`
#### Snippet
```java
                  logger.error("Stopping broker because of wrong node ID communication from live: maybe a misbehaving live?");
                  asyncRestartServer(activeMQServer, false);
                  return null;
               case WrongActivationSequence:
                  logger.error("Stopping broker because of wrong activation sequence communication from live: maybe a misbehaving live?");
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ReplicationBackupActivation.java`
#### Snippet
```java
                  logger.error("Stopping broker because of wrong activation sequence communication from live: maybe a misbehaving live?");
                  asyncRestartServer(activeMQServer, false);
                  return null;
               default:
                  throw new AssertionError("Unsupported failure " + failure);
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ReplicationBackupActivation.java`
#### Snippet
```java
         ActiveMQServerImpl.stopComponent(replicationEndpoint);
         closeChannelOf(replicationEndpoint);
         return null;
      }
   }
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ReplicationBackupActivation.java`
#### Snippet
```java
            final ClusterControl liveControl = tryLocateAndConnectToLive(liveLocator, clusterController);
            if (liveControl == null) {
               return null;
            }
            try {
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/SharedNothingBackupActivation.java`
#### Snippet
```java
      private synchronized ReplicationEndpoint connectToReplicationEndpoint(final ClusterControl control) throws Exception {
         if (!activeMQServer.isStarted())
            return null;
         if (!activeMQServer.getHAPolicy().isBackup()) {
            throw ActiveMQMessageBundle.BUNDLE.serverNotBackupServer();
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/SharedNothingBackupActivation.java`
#### Snippet
```java
         logger.debug(e.getMessage(), e);
      }
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/Activation.java`
#### Snippet
```java
   * */
   public ChannelHandler getActivationChannelHandler(Channel channel, Acceptor acceptorUsed) {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/Activation.java`
#### Snippet
```java
   * */
   public ReplicationManager getReplicationManager() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ReplicationPrimaryActivation.java`
#### Snippet
```java
   public ChannelHandler getActivationChannelHandler(final Channel channel, final Acceptor acceptorUsed) {
      if (stoppingServer.get()) {
         return null;
      }
      return packet -> {
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerConsumerImpl.java`
#### Snippet
```java
   public synchronized MessageReference removeReferenceByID(final long messageID) throws Exception {
      if (browseOnly) {
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerConsumerImpl.java`
#### Snippet
```java
         if (deliveringRefs.isEmpty()) {
            logger.trace("removeReferenceByID {} return null", messageID);
            return null;
         }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerConsumerImpl.java`
#### Snippet
```java
   public synchronized List<Long> acknowledge(Transaction tx, final long messageID) throws Exception {
      if (browseOnly) {
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerConsumerImpl.java`
#### Snippet
```java
   @Override
   public SimpleString getFilterString() {
      return filter == null ? null : filter.getFilterString();
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/NamedLiveNodeIdLocatorForReplication.java`
#### Snippet
```java
   @Override
   public String getNodeID() {
      return found ? nodeID : null;
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/AbstractProtocolReference.java`
#### Snippet
```java
   public <T> T getProtocolData(Class<T> classType) {
      if (protocolDataMap == null) {
         return null;
      } else {
         return (T)protocolDataMap.get(classType);
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/FileLockNodeManager.java`
#### Snippet
```java
                  Thread.sleep(500);
               } catch (InterruptedException e) {
                  return null;
               }
            } else {
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/FileLockNodeManager.java`
#### Snippet
```java
               TimeUnit.NANOSECONDS.sleep(waitTime);
            } catch (InterruptedException interrupt) {
               return null;
            }
         }
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/FileLockNodeManager.java`
#### Snippet
```java

      // presumed interrupted
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/FileLockNodeManager.java`
#### Snippet
```java
      } catch (java.nio.channels.OverlappingFileLockException ex) {
         // This just means that another object on the same JVM is holding the lock
         return null;
      }
   }
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerProducersImpl.java`
#### Snippet
```java
         return producers.get(senderName);
      }
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueFactoryImpl.java`
#### Snippet
```java
         return Message.HDR_LAST_VALUE_NAME;
      } else  {
         return null;
      }
   }
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueFactoryImpl.java`
#### Snippet
```java
         return Message.HDR_LAST_VALUE_NAME;
      } else {
         return null;
      }
   }
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueFactoryImpl.java`
#### Snippet
```java
         return Message.HDR_LAST_VALUE_NAME;
      } else {
         return null;
      }
   }
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ScheduledDeliveryHandlerImpl.java`
#### Snippet
```java
      }

      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java
      RefsOperation operation = transaction == null ? null : (RefsOperation) transaction.getProperty(TransactionPropertyIndexes.REFS_OPERATION);

      return operation == null ? null : operation.getLingerMessages();
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java
   @Override
   public String getDefaultAddress() {
      return defaultAddress != null ? defaultAddress.toString() : null;
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java
         return PrefixUtil.getPrefix(address, prefixes);
      }
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/jdbc/JdbcSharedStateManager.java`
#### Snippet
```java
         try (ResultSet resultSet = preparedStatement.executeQuery()) {
            if (!resultSet.next()) {
               return null;
            } else {
               final String nodeId = resultSet.getString(1);
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/jdbc/JdbcSharedStateManager.java`
#### Snippet
```java
                  return new UUID(UUID.TYPE_TIME_BASED, UUID.stringToBytes(nodeId));
               } else {
                  return null;
               }
            }
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/jdbc/JdbcSharedStateManager.java`
#### Snippet
```java
               connection.rollback();
               logger.debug("Rollback after failed to update NodeId to {} and haven't found any NodeId", newNodeId);
               return null;
            }
         } catch (SQLException e) {
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/jdbc/JdbcSharedStateManager.java`
#### Snippet
```java
            connection.rollback();
            logger.debug("Rollback while trying to update NodeId to {}", newNodeId, e);
            return null;
         } finally {
            connection.setAutoCommit(autoCommit);
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/quorum/ActivationSequenceStateMachine.java`
#### Snippet
```java
               case Stale:
                  activationLock.close();
                  return null;
               case InSync:
                  timeout = LIVE_LOCK_ACQUIRE_TIMEOUT_MILLIS;
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/quorum/ActivationSequenceStateMachine.java`
#### Snippet
```java
               case Stale:
                  activationLock.close();
                  return null;
               case InSync:
                  // we are an in_sync_replica, good to go live as UNREPLICATED
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/group/impl/RemoteGroupingHandler.java`
#### Snippet
```java
      Notification notification = new Notification(null, CoreNotificationType.PROPOSAL, props);
      managementService.sendNotification(notification);
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/group/impl/LocalGroupingHandler.java`
#### Snippet
```java
               return new Response(proposal.getGroupId(), original.getClusterName());
            } else {
               return null;
            }
         }
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/group/impl/LocalGroupingHandler.java`
#### Snippet
```java
         return new Response(fullID, original.getClusterName());
      } else {
         return null;
      }
   }
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/ha/ReplicatedPolicy.java`
#### Snippet
```java
   @Override
   public String getScaleDownGroupName() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/ha/ReplicatedPolicy.java`
#### Snippet
```java
   @Override
   public String getScaleDownClustername() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/BackupManager.java`
#### Snippet
```java
            return locator;
         }
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/ha/BackupPolicy.java`
#### Snippet
```java
   @Override
   public String getScaleDownClustername() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/ha/BackupPolicy.java`
#### Snippet
```java
   @Override
   public String getScaleDownGroupName() {
      return getScaleDownPolicy() != null ? getScaleDownPolicy().getGroupName() : null;
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/ClusterManager.java`
#### Snippet
```java
               logWarnNoConnector(connectorInfo, config.getName());

               return null;
            }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/ClusterManager.java`
#### Snippet
```java
      if (group.size() == 0) {
         logWarnNoConnector(config.getConnectorInfos().toString(), group.getName());
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/ClusterManager.java`
#### Snippet
```java
            }
         }
         return null;
      }
   }
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/ha/SharedStoreSlavePolicy.java`
#### Snippet
```java
   @Override
   public String getBackupGroupName() {
      return null;
   }
}
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/ha/LiveOnlyPolicy.java`
#### Snippet
```java
   @Override
   public String getScaleDownGroupName() {
      return scaleDownPolicy == null ? null : scaleDownPolicy.getGroupName();
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/ha/LiveOnlyPolicy.java`
#### Snippet
```java
   @Override
   public String getBackupGroupName() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/ha/LiveOnlyPolicy.java`
#### Snippet
```java
   @Override
   public String getScaleDownClustername() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/ha/SharedStoreMasterPolicy.java`
#### Snippet
```java
   @Override
   public String getScaleDownClustername() {
      return null;
   }
}
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/ha/SharedStoreMasterPolicy.java`
#### Snippet
```java
   @Override
   public String getScaleDownGroupName() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/ha/SharedStoreMasterPolicy.java`
#### Snippet
```java
   @Override
   public String getBackupGroupName() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/ha/ReplicationBackupPolicy.java`
#### Snippet
```java
   @Override
   public String getScaleDownGroupName() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/ha/ReplicationBackupPolicy.java`
#### Snippet
```java
   @Override
   public String getScaleDownClustername() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/ha/ColocatedPolicy.java`
#### Snippet
```java
   @Override
   public String getScaleDownClustername() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/ha/ColocatedPolicy.java`
#### Snippet
```java
   @Override
   public String getScaleDownGroupName() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/ha/ColocatedPolicy.java`
#### Snippet
```java
      final HAPolicy<LiveActivation> livePolicy = this.livePolicy;
      if (livePolicy == null) {
         return null;
      }
      return livePolicy.getBackupGroupName();
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/ha/ReplicationPrimaryPolicy.java`
#### Snippet
```java
   @Override
   public String getScaleDownGroupName() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/ha/ReplicationPrimaryPolicy.java`
#### Snippet
```java
   @Override
   public String getScaleDownClustername() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/impl/Redistributor.java`
#### Snippet
```java
   @Override
   public Filter getFilter() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/impl/ClusterConnectionBridge.java`
#### Snippet
```java
         factory = reconnectOnOriginalNode();
         if (factory == null) {
            return null;
         }
      }
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/impl/ClusterConnectionBridge.java`
#### Snippet
```java

      if (factory == null) {
         return null;
      }
      factory.setReconnectAttempts(0);
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/impl/BridgeImpl.java`
#### Snippet
```java
      }

      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/impl/BridgeImpl.java`
#### Snippet
```java
   public RemotingConnection getForwardingConnection() {
      if (session == null) {
         return null;
      } else {
         return session.getConnection();
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/qourum/ServerConnectVote.java`
#### Snippet
```java
   @Override
   public Map<String, Object> getVoteMap() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/qourum/BooleanVote.java`
#### Snippet
```java
   @Override
   public Map<String, Object> getVoteMap() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/qourum/QuorumManager.java`
#### Snippet
```java
         return voteResponse;
      } catch (ActiveMQException e) {
         return null;
      }
   }
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/impl/ClusterConnectionImpl.java`
#### Snippet
```java
            return locator;
         }
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/impl/ClusterConnectionImpl.java`
#### Snippet
```java
   public BridgeMetrics getBridgeMetrics(String nodeId) {
      final MessageFlowRecord record = records.get(nodeId);
      return record != null && record.getBridge() != null ? record.getBridge().getMetrics() : null;
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/impl/ClusterConnectionImpl.java`
#### Snippet
```java
   @Override
   public String getNodeID() {
      return nodeManager == null ? null : (nodeManager.getNodeId() == null ? null : nodeManager.getNodeId().toString());
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/impl/ClusterConnectionImpl.java`
#### Snippet
```java
   @Override
   public String getNodeID() {
      return nodeManager == null ? null : (nodeManager.getNodeId() == null ? null : nodeManager.getNodeId().toString());
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/routing/ConnectionRouter.java`
#### Snippet
```java

   public String getLocalTargetFilter() {
      return localTargetFilter != null ? localTargetFilter.pattern() : null;
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/routing/KeyResolver.java`
#### Snippet
```java

   public String getKeyFilter() {
      return keyFilter != null ? keyFilter.pattern() : null;
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/routing/pools/AbstractPool.java`
#### Snippet
```java
      }

      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/routing/pools/AbstractPool.java`
#### Snippet
```java
      while(readyTargets < quorumSize && deadline > 0 && (System.nanoTime() - deadline) < 0);

      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/routing/policies/RoundRobinPolicy.java`
#### Snippet
```java
      }

      return null;
   }
}
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/routing/policies/FirstElementPolicy.java`
#### Snippet
```java
      }

      return null;
   }
}
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/routing/policies/Policy.java`
#### Snippet
```java

   default Target selectTarget(List<Target> targets, String key) {
      return null;
   }
}
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/routing/policies/ConsistentHashPolicy.java`
#### Snippet
```java
      }

      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/routing/policies/ConsistentHashModuloPolicy.java`
#### Snippet
```java
   @Override
   public Target selectTarget(List<Target> targets, String key) {
      return null;
   }
}
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/routing/policies/AbstractPolicy.java`
#### Snippet
```java
   @Override
   public TargetProbe getTargetProbe() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/routing/policies/LeastConnectionsPolicy.java`
#### Snippet
```java
      }

      return null;
   }
}
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ActiveMQServerImpl.java`
#### Snippet
```java
         ActiveMQServerLogger.LOGGER.divertWithNoAddress();

         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ActiveMQServerImpl.java`
#### Snippet
```java
         ActiveMQServerLogger.LOGGER.divertWithNoForwardingAddress();

         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ActiveMQServerImpl.java`
#### Snippet
```java
         ActiveMQServerLogger.LOGGER.divertBindingAlreadyExists(sName);

         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ActiveMQServerImpl.java`
#### Snippet
```java

      if (binding == null) {
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ActiveMQServerImpl.java`
#### Snippet
```java
         return queueBinding.getQueue();
      } else {
         return null;
      }
   }
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ActiveMQServerImpl.java`
#### Snippet
```java
      final PostOffice postOfficeInUse = postOffice;
      if (postOfficeInUse == null) {
         return null;
      }
      synchronized (postOfficeInUse) {
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ActiveMQServerImpl.java`
#### Snippet
```java
      }

      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ActiveMQServerImpl.java`
#### Snippet
```java
      final DivertBinding divertBinding = (DivertBinding) postOffice.getBinding(SimpleString.toSimpleString(config.getName()));
      if (divertBinding == null) {
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ActiveMQServerImpl.java`
#### Snippet
```java
   @Override
   public SimpleString getNodeID() {
      return nodeManager == null ? null : nodeManager.getNodeId();
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/federation/address/FederatedAddressConsumerKey.java`
#### Snippet
```java
   @Override
   public SimpleString getFilterString() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/impl/ConfigurationImpl.java`
#### Snippet
```java
         return metricsConfiguration.getPlugin();
      }
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/impl/ConfigurationImpl.java`
#### Snippet
```java
            }
         }
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/impl/ConfigurationImpl.java`
#### Snippet
```java
   public File getJournalRetentionLocation() {
      if (journalRetentionDirectory == null) {
         return null;
      } else {
         return subFolder(getJournalRetentionDirectory());
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/impl/ConfigurationImpl.java`
#### Snippet
```java
         if (connector == null) {
            ActiveMQServerLogger.LOGGER.connectionConfigurationIsNull(connectorName == null ? "null" : connectorName);
            return null;
         }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/federation/queue/FederatedQueue.java`
#### Snippet
```java
         FederatedConsumerKey key = getKey(consumer);
         Transformer transformer = getTransformer(config.getTransformerRef());
         Transformer fqqnTransformer = message -> message == null ? null : message.setAddress(key.getFqqn());
         createRemoteConsumer(key, mergeTransformers(fqqnTransformer, transformer), null);
      }
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/federation/Federation.java`
#### Snippet
```java

   String getFederationPassword() {
      return config.getCredentials() == null ? null : config.getCredentials().getPassword();
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/federation/Federation.java`
#### Snippet
```java

   String getFederationUser() {
      return config.getCredentials() == null ? null : config.getCredentials().getUser();
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/ManagementContext.java`
#### Snippet
```java
      } catch (NotCompliantMBeanException e) {
         e.printStackTrace();
         return null;
      }
   }
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/ArtemisMBeanServerBuilder.java`
#### Snippet
```java
         } else if (method.getName().equals("finalize") && method.getParameterTypes().length == 0) {
            // special case finalize, don't route through to delegate because that will get its own call
            return null;
         }
         try {
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/ArtemisMBeanServerGuard.java`
#### Snippet
```java
   public Object invoke(Object proxy, Method method, Object[] args) throws Throwable {
      if (method.getParameterTypes().length == 0)
         return null;

      if (!ObjectName.class.isAssignableFrom(method.getParameterTypes()[0]))
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/ArtemisMBeanServerGuard.java`
#### Snippet
```java

      if (!ObjectName.class.isAssignableFrom(method.getParameterTypes()[0]))
         return null;

      ObjectName objectName = (ObjectName) args[0];
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/ArtemisMBeanServerGuard.java`
#### Snippet
```java
      }

      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/registry/JndiBindingRegistry.java`
#### Snippet
```java
      try {
         if (context == null) {
            return null;
         } else {
            return context.lookup(name);
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/registry/JndiBindingRegistry.java`
#### Snippet
```java
         }
      } catch (NamingException e) {
         return null;
      }
   }
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/remoting/impl/invm/InVMConnector.java`
#### Snippet
```java
         logger.debug("Returning null on InVMConnector for tests");
         // For testing only
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/remoting/impl/invm/InVMConnector.java`
#### Snippet
```java

      if (acceptor == null) {
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/remoting/impl/invm/InVMConnector.java`
#### Snippet
```java
            logger.debug("Connection limit of {} reached. Refusing connection.", acceptor.getConnectionsAllowed());
         }
         return null;
      }
   }
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/SlowConsumerThresholdMeasurementUnit.java`
#### Snippet
```java
            return MESSAGES_PER_DAY;
         default:
            return null;
      }
   }
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/SlowConsumerThresholdMeasurementUnit.java`
#### Snippet
```java
            return TimeUnit.DAYS;
         default:
            return null;
      }
   }
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/SlowConsumerPolicy.java`
#### Snippet
```java
            return NOTIFY;
         default:
            return null;
      }
   }
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/DeletionPolicy.java`
#### Snippet
```java
            return FORCE;
         default:
            return null;
      }
   }
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/security/impl/SecurityStoreImpl.java`
#### Snippet
```java
            return subject;
         } catch (NoCacheLoginException e) {
            return null;
         }
      }
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/security/impl/SecurityStoreImpl.java`
#### Snippet
```java
   public static String getUserFromSubject(Subject subject) {
      if (subject == null) {
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/security/impl/SecurityStoreImpl.java`
#### Snippet
```java
      }

      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/security/impl/SecurityStoreImpl.java`
#### Snippet
```java
         return getSubjectForAuthorization(session, (ActiveMQSecurityManager5) securityManager);
      }
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/BridgeControlImpl.java`
#### Snippet
```java
      clearIO();
      try {
         return bridge.getConfiguration().getTransformerConfiguration() == null ? null : bridge.getConfiguration().getTransformerConfiguration().getProperties();
      } finally {
         blockOnIO();
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/BridgeControlImpl.java`
#### Snippet
```java
      clearIO();
      try {
         return bridge.getConfiguration().getTransformerConfiguration() == null ? null : bridge.getConfiguration().getTransformerConfiguration().getClassName();
      } finally {
         blockOnIO();
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/ConnectionRouterControlImpl.java`
#### Snippet
```java
      }

      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/ConnectionRouterControlImpl.java`
#### Snippet
```java
      }

      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/remoting/server/impl/RemotingServiceImpl.java`
#### Snippet
```java
         ActiveMQServerLogger.LOGGER.errorRemovingConnection();

         return null;
      }
   }
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/remoting/server/impl/RemotingServiceImpl.java`
#### Snippet
```java
         return entry;
      } else {
         return null;
      }
   }
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/remoting/server/impl/RemotingServiceImpl.java`
#### Snippet
```java
         logger.debug("The connectionID::{} was already removed by some other module", remotingConnectionID);

         return null;
      }
   }
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/DivertControlImpl.java`
#### Snippet
```java
         Transformer transformer = divert.getTransformer();
         return transformer != null ? (transformer instanceof RegisteredTransformer ?
            ((RegisteredTransformer)transformer).getTransformer() : transformer).getClass().getName() : null;
      } finally {
         blockOnIO();
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/DivertControlImpl.java`
#### Snippet
```java
      try {
         Filter filter = divert.getFilter();
         return filter != null ? filter.getFilterString().toString() : null;
      } finally {
         blockOnIO();
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/ManagementRemotingConnection.java`
#### Snippet
```java
   @Override
   public List<FailureListener> removeFailureListeners() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/ManagementRemotingConnection.java`
#### Snippet
```java
   @Override
   public ActiveMQBuffer createTransportBuffer(int size) {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/ManagementRemotingConnection.java`
#### Snippet
```java
   @Override
   public Connection getTransportConnection() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/ManagementRemotingConnection.java`
#### Snippet
```java
   @Override
   public List<CloseListener> removeCloseListeners() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/ManagementRemotingConnection.java`
#### Snippet
```java
   @Override
   public Subject getSubject() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/ManagementRemotingConnection.java`
#### Snippet
```java
   @Override
   public String getClientID() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/ManagementRemotingConnection.java`
#### Snippet
```java
   @Override
   public Future asyncFail(ActiveMQException me) {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/ManagementRemotingConnection.java`
#### Snippet
```java
   @Override
   public List<FailureListener> getFailureListeners() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/ManagementRemotingConnection.java`
#### Snippet
```java
   @Override
   public String getProtocolName() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/NettyAcceptor.java`
#### Snippet
```java
            ActiveMQServerLogger.LOGGER.connectionLimitReached(connectionsAllowed, ctx.channel().remoteAddress().toString());
            ctx.channel().close();
            return null;
         }
      }
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/ClusterConnectionControlImpl.java`
#### Snippet
```java
      try {
         final BridgeMetrics bridgeMetrics = clusterConnection.getBridgeMetrics(nodeId);
         return bridgeMetrics != null ? bridgeMetrics.convertToMap() : null;
      } finally {
         blockOnIO();
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/ClusterConnectionControlImpl.java`
#### Snippet
```java
         List<String> staticConnectors = configuration.getStaticConnectors();
         if (staticConnectors == null) {
            return null;
         } else {
            return staticConnectors.toArray(new String[staticConnectors.size()]);
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/view/AddressView.java`
#### Snippet
```java
   public Object getField(AddressInfo address, String fieldName) {
      if (address == null) {
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/view/AddressView.java`
#### Snippet
```java
   public JsonObjectBuilder toJson(AddressInfo address) {
      if (address == null) {
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/view/ProducerView.java`
#### Snippet
```java
      //if session is not available then consumer is not in valid state - ignore
      if (session == null) {
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/view/ProducerView.java`
#### Snippet
```java
      //if session is not available then consumer is not in valid state - ignore
      if (session == null) {
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/view/ConsumerView.java`
#### Snippet
```java
      //if session is not available then consumer is not in valid state - ignore
      if (session == null) {
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/view/ConsumerView.java`
#### Snippet
```java
      //if session is not available then consumer is not in valid state - ignore
      if (session == null) {
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/postoffice/PostOffice.java`
#### Snippet
```java

   default AddressManager getAddressManager() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/postoffice/impl/BindingsImpl.java`
#### Snippet
```java
      final Binding binding = bindingsNameMap.remove(bindingUniqueName);
      if (binding == null) {
         return null;
      }
      try {
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/postoffice/impl/BindingsImpl.java`
#### Snippet
```java
      }

      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/postoffice/impl/BindingsImpl.java`
#### Snippet
```java
      final MessageLoadBalancingType loadBalancingType = this.messageLoadBalancingType;
      if (loadBalancingType.equals(MessageLoadBalancingType.STRICT) || loadBalancingType.equals(MessageLoadBalancingType.OFF)) {
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/postoffice/impl/BindingsImpl.java`
#### Snippet
```java
         // The value can become null if it's concurrently removed while we're iterating - this is expected
         // ConcurrentHashMap behaviour!
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/postoffice/impl/BindingsImpl.java`
#### Snippet
```java
      }
      if (nextBinding == null) {
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/postoffice/impl/CopyOnWriteBindings.java`
#### Snippet
```java
         final int found = indexOfBinding(oldBindings, bindingToRemove);
         if (found == -1) {
            return null;
         }
         final int oldBindingsCount = oldBindings.length;
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/postoffice/impl/CopyOnWriteBindings.java`
#### Snippet
```java
      BindingsAndPosition bindings = map.get(routingName);
      if (bindings == null) {
         return null;
      }
      final Binding[] bindingsSnapshot = bindings.get();
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/postoffice/impl/CopyOnWriteBindings.java`
#### Snippet
```java
      final Binding[] bindingsSnapshot = bindings.get();
      if (bindingsSnapshot == TOMBSTONE_BINDINGS) {
         return null;
      }
      assert bindingsSnapshot != null && bindingsSnapshot.length > 0;
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/postoffice/impl/PersistentDuplicateIDCache.java`
#### Snippet
```java
      @Override
      public List<MessageReference> getRelatedMessageReferences() {
         return null;
      }
   }
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/postoffice/impl/SimpleAddressManager.java`
#### Snippet
```java

      if (binding == null) {
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/postoffice/impl/SimpleAddressManager.java`
#### Snippet
```java
                                      final Collection<Binding> newBindings) throws Exception {
      if (newBindings.isEmpty()) {
         return null;
      }
      SimpleString realAddress = CompositeAddress.extractAddressName(address);
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/postoffice/impl/SimpleAddressManager.java`
#### Snippet
```java
         throw new IllegalStateException("queue belongs to address" + binding.getAddress());
      }
      return binding != null ? binding.getUniqueName() : null;
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/postoffice/impl/SimpleAddressManager.java`
#### Snippet
```java
      }

      return binding != null ? binding.getUniqueName() : null;
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/postoffice/impl/SimpleAddressManager.java`
#### Snippet
```java
   public Binding getBinding(final SimpleString bindableName) {
      final Pair<Binding, Address> bindingAddressPair = nameMap.get(CompositeAddress.extractQueueName(bindableName));
      return bindingAddressPair == null ? null : bindingAddressPair.getA();
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/AddressQueueStatus.java`
#### Snippet
```java
   public static AddressQueueStatus fromID(short id) {
      if (id < 0 || id >= values.length) {
         return null;
      }
      return values[id];
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/QueueControlImpl.java`
#### Snippet
```java
      try {
         SimpleString user = queue.getUser();
         return user == null ? null : user.toString();
      } finally {
         blockOnIO();
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/QueueControlImpl.java`
#### Snippet
```java
         Filter filter = queue.getFilter();

         return filter != null ? filter.getFilterString().toString() : null;
      } finally {
         blockOnIO();
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/QueueControlImpl.java`
#### Snippet
```java
      Map<String, Object> message = getFirstMessage();
      if (message == null) {
         return null;
      } else {
         if (!message.containsKey("timestamp")) {
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/QueueControlImpl.java`
#### Snippet
```java
      } else {
         if (!message.containsKey("timestamp")) {
            return null;
         } else {
            return (Long) message.get("timestamp");
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/QueueControlImpl.java`
#### Snippet
```java
            return firstMessage.getMessage().toMap(attributeSizeLimit);
         } else {
            return null;
         }
      } finally {
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/QueueControlImpl.java`
#### Snippet
```java
      clearIO();
      try {
         return queue.getExpiryAddress() == null ? null : queue.getExpiryAddress().toString();
      } finally {
         blockOnIO();
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/QueueControlImpl.java`
#### Snippet
```java
      clearIO();
      try {
         return queue.getDeadLetterAddress() == null ? null : queue.getDeadLetterAddress().toString();
      } finally {
         blockOnIO();
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/QueueControlImpl.java`
#### Snippet
```java
      Long firstMessageTimestamp = getFirstMessageTimestamp();
      if (firstMessageTimestamp == null) {
         return null;
      }
      long now = new Date().getTime();
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/QueueControlImpl.java`
#### Snippet
```java
            return queue.getLastValueKey().toString();
         } else {
            return null;
         }
      } finally {
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/QueueControlImpl.java`
#### Snippet
```java
         SimpleString groupFirstKey = queue.getGroupFirstKey();

         return groupFirstKey != null ? groupFirstKey.toString() : null;
      } finally {
         blockOnIO();
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/postoffice/impl/InMemoryDuplicateIDCache.java`
#### Snippet
```java
      @Override
      public List<MessageReference> getRelatedMessageReferences() {
         return null;
      }
   }
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/nullpm/NullStorageLargeServerMessage.java`
#### Snippet
```java
   @Override
   public LargeBody getLargeBody() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/nullpm/NullStorageLargeServerMessage.java`
#### Snippet
```java
   @Override
   public SequentialFile getAppendFile() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/OperationContextImpl.java`
#### Snippet
```java
      if (token == null) {
         if (executorFactory == null) {
            return null;
         } else {
            token = new OperationContextImpl(executorFactory.getExecutor());
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/nullpm/NullStorageManager.java`
#### Snippet
```java
   @Override
   public Journal getMessageJournal() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/nullpm/NullStorageManager.java`
#### Snippet
```java
   @Override
   public List<PersistedBridgeConfiguration> recoverBridgeConfigurations() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/nullpm/NullStorageManager.java`
#### Snippet
```java
   @Override
   public Journal getBindingsJournal() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/nullpm/NullStorageManager.java`
#### Snippet
```java
   @Override
   public Map<String, PersistedRole> getPersistedRoles() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/nullpm/NullStorageManager.java`
#### Snippet
```java
   @Override
   public Map<String, PersistedKeyValuePair> getPersistedKeyValuePairs(String mapId) {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/nullpm/NullStorageManager.java`
#### Snippet
```java
   @Override
   public Map<String, PersistedUser> getPersistedUsers() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/nullpm/NullStorageManager.java`
#### Snippet
```java
   @Override
   public LargeServerMessage largeMessageCreated(long id, LargeServerMessage largeMessage) throws Exception {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/nullpm/NullStorageManager.java`
#### Snippet
```java
   @Override
   public List<PersistedDivertConfiguration> recoverDivertConfigurations() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/nullpm/NullStorageManager.java`
#### Snippet
```java
   @Override
   public List<PersistedConnector> recoverConnectors() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/LargeServerMessageImpl.java`
#### Snippet
```java
      } catch (Exception e) {
         ActiveMQServerLogger.LOGGER.lareMessageErrorCopying(this, e);
         return null;
      }
   }
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/codec/PageUpdateTXEncoding.java`
#### Snippet
```java

   public List<MessageReference> getRelatedMessageReferences() {
      return null;
   }
}
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/deployers/impl/FileConfigurationParser.java`
#### Snippet
```java
         return scaleDownConfiguration;
      }
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/DescribeJournal.java`
#### Snippet
```java

         default:
            return null;
      }
   }
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/DescribeJournal.java`
#### Snippet
```java
         return XidCodecSupport.decodeXid(ActiveMQBuffers.wrappedBuffer(data));
      } catch (Exception e) {
         return null;
      }
   }
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/config/PersistedSecuritySetting.java`
#### Snippet
```java
    */
   public String getDeleteAddressRoles() {
      return deleteAddressRoles != null ? deleteAddressRoles.toString() : null;
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/config/PersistedSecuritySetting.java`
#### Snippet
```java
    */
   public String getManageRoles() {
      return manageRoles != null ? manageRoles.toString() : null;
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/config/PersistedSecuritySetting.java`
#### Snippet
```java
    */
   public String getDeleteDurableQueueRoles() {
      return deleteDurableQueueRoles != null ? deleteDurableQueueRoles.toString() : null;
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/config/PersistedSecuritySetting.java`
#### Snippet
```java
    */
   public String getCreateAddressRoles() {
      return createAddressRoles != null ? createAddressRoles.toString() : null;
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/config/PersistedSecuritySetting.java`
#### Snippet
```java
    */
   public String getDeleteNonDurableQueueRoles() {
      return deleteNonDurableQueueRoles != null ? deleteNonDurableQueueRoles.toString() : null;
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/config/PersistedSecuritySetting.java`
#### Snippet
```java
    */
   public String getBrowseRoles() {
      return browseRoles != null ? browseRoles.toString() : null;
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/config/PersistedSecuritySetting.java`
#### Snippet
```java
    */
   public String getCreateDurableQueueRoles() {
      return createDurableQueueRoles != null ? createDurableQueueRoles.toString() : null;
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/config/PersistedSecuritySetting.java`
#### Snippet
```java
    */
   public String getCreateNonDurableQueueRoles() {
      return createNonDurableQueueRoles != null ? createNonDurableQueueRoles.toString() : null;
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/config/PersistedSecuritySetting.java`
#### Snippet
```java
    */
   public String getSendRoles() {
      return sendRoles != null ? sendRoles.toString() : null;
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/config/PersistedSecuritySetting.java`
#### Snippet
```java
    */
   public String getConsumeRoles() {
      return consumeRoles != null ? consumeRoles.toString() : null;
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/postoffice/impl/PostOfficeImpl.java`
#### Snippet
```java
         final QueueBinding queueBinding = (QueueBinding) addressManager.getBinding(queueConfiguration.getName());
         if (queueBinding == null) {
            return null;
         }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/postoffice/impl/PostOfficeImpl.java`
#### Snippet
```java
      }

      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/transaction/impl/TransactionImpl.java`
#### Snippet
```java
   @Override
   public Object getProperty(final int index) {
      return properties == null ? null : properties.get(index);
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/replication/ReplicationManager.java`
#### Snippet
```java
         return sendReplicatePacket(new ReplicationLiveIsStoppingMessage(finalMessage));
      }
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/replication/ReplicationManager.java`
#### Snippet
```java
      if (!enabled) {
         packet.release();
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/utils/XMLConfigurationUtil.java`
#### Snippet
```java
      Node node = element.getAttributes().getNamedItem(attribute);
      if (node == null) {
         return null;
      } else {
         return node.getNodeValue();
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/utils/XMLConfigurationUtil.java`
#### Snippet
```java
      String content = element.getTextContent();
      if (content == null)
         return null;
      return content.trim();
   }
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/messagecounter/impl/MessageCounterHelper.java`
#### Snippet
```java
   public static String listMessageCounterAsHTML(final MessageCounter[] counters) {
      if (counters == null) {
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/messagecounter/impl/MessageCounterHelper.java`
#### Snippet
```java
   public static String listMessageCounterHistoryAsHTML(final MessageCounter[] counters) {
      if (counters == null) {
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/StorageManager.java`
#### Snippet
```java

   default SequentialFileFactory getJournalSequentialFileFactory() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/AbstractJournalStorageManager.java`
#### Snippet
```java
            PagingStore store = pagingManager.getPageStore(address);
            if (store == null) {
               return null;
            }
            subs = store.getCursorProvider().getSubscription(queueID);
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java
            }
         }
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java
      }

      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java
   @Override
   public SimpleString getLastValueKey() {
      return null;
   }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java
   private SimpleString extractGroupID(MessageReference ref) {
      if (internalQueue || exclusive || groupBuckets == 0) {
         return null;
      } else {
         try {
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java
         } catch (Throwable e) {
            ActiveMQServerLogger.LOGGER.unableToExtractGroupID(e);
            return null;
         }
      }
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java
            return ref;
         }
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java

      public Integer expectedHits() {
         return null;
      }

```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java
         }

         return null;
      }
   }
```

### ReturnNull
Return of `null`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/ActiveMQServerControlImpl.java`
#### Snippet
```java
         AuditLogger.getNodeID(this.server);
      }
      return server.getNodeID() == null ? null : server.getNodeID().toString();
   }

```

## RuleId[id=AssignmentToLambdaParameter]
### AssignmentToLambdaParameter
Assignment to lambda parameter `value`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/postoffice/QueueInfo.java`
#### Snippet
```java
      consumerUpdater.getAndUpdate(this, value -> {
         if (value > 0) {
            return --value;
         } else {
            ActiveMQServerLogger.LOGGER.consumerCountError("Tried to decrement consumer count below 0: " + this);
```

### AssignmentToLambdaParameter
Assignment to lambda parameter `v`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/QueueControlImpl.java`
#### Snippet
```java
      if (filter == null || filter.match(message)) {
         if (groupByProperty == null) {
            result.compute(null, (k, v) -> v == null ? 1 : ++v);
         } else {
            Object value = message.getObjectProperty(groupByProperty);
```

### AssignmentToLambdaParameter
Assignment to lambda parameter `v`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/QueueControlImpl.java`
#### Snippet
```java
            Object value = message.getObjectProperty(groupByProperty);
            String valueStr = value == null ? null : value.toString();
            result.compute(valueStr, (k, v) -> v == null ? 1 : ++v);
         }
      }
```

## RuleId[id=UnnecessaryLocalVariable]
### UnnecessaryLocalVariable
Local variable `fe` is redundant
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/impl/SelectorParser.java`
#### Snippet
```java
         return e;
      } catch (Throwable e) {
         FilterException fe = new FilterException(actual, e);
         throw fe;
      } finally {
```

### UnnecessaryLocalVariable
Local variable `answer` is redundant
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/ArithmeticExpression.java`
#### Snippet
```java
            if (lvalue instanceof String) {
               String text = (String) lvalue;
               String answer = text + rvalue;
               return answer;
            } else {
```

### UnnecessaryLocalVariable
Local variable `newConfig` is redundant
in `artemis-protocols/artemis-hqclient-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/hornetq/client/HornetQClientProtocolManagerFactory.java`
#### Snippet
```java
         factoryClassName = NettyConnectorFactory.class.getName();
      }
      TransportConfiguration newConfig = new TransportConfiguration(factoryClassName,
              tc.getParams(),
              tc.getName(),
```

### UnnecessaryLocalVariable
Local variable `ret` is redundant
in `artemis-protocols/artemis-stomp-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/stomp/StompDecoder.java`
#### Snippet
```java

      // Now the body
      StompFrame ret = parseBody();

      return ret;
```

### UnnecessaryLocalVariable
Local variable `response` is redundant
in `artemis-protocols/artemis-stomp-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/stomp/VersionedStompFrameHandler.java`
#### Snippet
```java
   public StompFrame onUnknown(String command) {
      ActiveMQStompException error = BUNDLE.unknownCommand(command).setHandler(this);
      StompFrame response = error.getFrame();
      return response;
   }
```

### UnnecessaryLocalVariable
Local variable `buff` is redundant
in `artemis-commons/src/main/java/org/apache/activemq/artemis/api/core/ActiveMQBuffers.java`
#### Snippet
```java
    */
   public static ActiveMQBuffer wrappedBuffer(final ByteBuf underlying) {
      ActiveMQBuffer buff = new ChannelBufferWrapper(underlying.duplicate());

      return buff;
```

### UnnecessaryLocalVariable
Local variable `builder` is redundant
in `artemis-commons/src/main/java/org/apache/activemq/artemis/core/server/NetworkHealthCheck.java`
#### Snippet
```java

      logger.debug("executing ping:: {}", command);
      ProcessBuilder builder = new ProcessBuilder(command.split(" "));

      return builder;
```

### UnnecessaryLocalVariable
Local variable `thisId` is redundant
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/UUID.java`
#### Snippet
```java
      }
      byte[] otherId = ((UUID) o).mId;
      byte[] thisId = mId;
      for (int i = 0; i < 16; ++i) {
         if (otherId[i] != thisId[i]) {
```

### UnnecessaryLocalVariable
Local variable `s` is redundant
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/TimeUtils.java`
#### Snippet
```java
      if (uptime < 60) {
         long minutes = (long) uptime;
         String s = fmtI.format(minutes) + (minutes > 1 ? " minutes" : " minute");
         return s;
      }
```

### UnnecessaryLocalVariable
Local variable `address` is redundant
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/UUIDGenerator.java`
#### Snippet
```java
      try {
         // we wait 5 seconds to get the first matching hardware address. After that, we give up and return null
         byte[] address = executor.invokeAny(tasks, 5, TimeUnit.SECONDS);
         return address;
      } catch (Exception e) {
```

### UnnecessaryLocalVariable
Local variable `uid` is redundant
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/UUIDGenerator.java`
#### Snippet
```java
      byte[] address = getAddressBytes();

      UUID uid = generateTimeBasedUUID(address);

      return uid;
```

### UnnecessaryLocalVariable
Local variable `writtenBytes` is redundant
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/UTF8Util.java`
#### Snippet
```java
      }

      final int writtenBytes = (charCount - index);
      return writtenBytes;
   }
```

### UnnecessaryLocalVariable
Local variable `writtenBytes` is redundant
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/UTF8Util.java`
#### Snippet
```java
      }

      final int writtenBytes = (charCount - index);
      return writtenBytes;
   }
```

### UnnecessaryLocalVariable
Local variable `writtenBytes` is redundant
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/UTF8Util.java`
#### Snippet
```java
      }

      final int writtenBytes = (charCount - index);
      return writtenBytes;
   }
```

### UnnecessaryLocalVariable
Local variable `capacity` is redundant
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/LongHashSet.java`
#### Snippet
```java

   private void rehash(final int newCapacity) {
      final int capacity = newCapacity;
      final int mask = newCapacity - 1;
      resizeThreshold = (int) (newCapacity * loadFactor);
```

### UnnecessaryLocalVariable
Local variable `hasRemaining` is redundant
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/io/mapped/MappedSequentialFile.java`
#### Snippet
```java
      checkIsOpen();
      final long newPosition = (long) this.mappedFile.position() + size;
      final boolean hasRemaining = newPosition <= this.mappedFile.length();
      return hasRemaining;
   }
```

### UnnecessaryLocalVariable
Local variable `hash` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/util/HashUtil.java`
#### Snippet
```java

      try {
         String hash = HASH_PROCESSOR.hash(plaintext);
         return hash;
      } catch (Exception e) {
```

### UnnecessaryLocalVariable
Local variable `result` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Upgrade.java`
#### Snippet
```java
                newLine -> {
                   if (newLine.trim().startsWith(jdkPrefix)) {
                      String result =  JVMArgumentParser.parseNewLine(jdkPrefix, endOfLine, newLine, keepArguments, currentArguments);
                      return result;
                   } else {
```

### UnnecessaryLocalVariable
Local variable `originalLine` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Upgrade.java`
#### Snippet
```java
                      for (String prefix : keepingPrefixes) {
                         if (newLine.trim().startsWith(prefix)) {
                            String originalLine = replaceMatrix.get(prefix);
                            return originalLine;
                         }
```

### UnnecessaryLocalVariable
Local variable `originalLine` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Upgrade.java`
#### Snippet
```java
                  for (String prefix : keepingPrefixes) {
                     if (newLine.trim().startsWith(prefix)) {
                        String originalLine = replaceMatrix.get(prefix);
                        return originalLine;
                     }
```

### UnnecessaryLocalVariable
Local variable `newaddress` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/xml/XmlDataImporter.java`
#### Snippet
```java
         routingType = RoutingType.ANYCAST;
         if (!legacyPrefixes) {
            String newaddress = address.substring(PacketImpl.OLD_QUEUE_PREFIX.length());
            address = newaddress;
         }
```

### UnnecessaryLocalVariable
Local variable `newaddress` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/xml/XmlDataImporter.java`
#### Snippet
```java
         routingType = RoutingType.MULTICAST;
         if (!legacyPrefixes) {
            String newaddress = address.substring(PacketImpl.OLD_TOPIC_PREFIX.length());
            address = newaddress;
         }
```

### UnnecessaryLocalVariable
Local variable `config` is redundant
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/server/config/impl/TransportConfigurationEncodingSupport.java`
#### Snippet
```java
         params.put(key, value);
      }
      TransportConfiguration config = new TransportConfiguration(factoryClassName, params, name);
      return config;
   }
```

### UnnecessaryLocalVariable
Local variable `cfs` is redundant
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/persistence/impl/journal/JMSJournalStorageManagerImpl.java`
#### Snippet
```java
   @Override
   public List<PersistedConnectionFactory> recoverConnectionFactories() {
      List<PersistedConnectionFactory> cfs = new ArrayList<>(mapFactories.values());
      return cfs;
   }
```

### UnnecessaryLocalVariable
Local variable `list` is redundant
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/persistence/impl/journal/JMSJournalStorageManagerImpl.java`
#### Snippet
```java
   @Override
   public List<PersistedBindings> recoverPersistedBindings() throws Exception {
      ArrayList<PersistedBindings> list = new ArrayList<>(mapBindings.values());
      return list;
   }
```

### UnnecessaryLocalVariable
Local variable `destinations` is redundant
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/persistence/impl/journal/JMSJournalStorageManagerImpl.java`
#### Snippet
```java
   @Override
   public List<PersistedDestination> recoverDestinations() {
      List<PersistedDestination> destinations = new ArrayList<>(this.destinations.values());
      return destinations;
   }
```

### UnnecessaryLocalVariable
Local variable `service` is redundant
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/bridge/impl/JMSBridgeImpl.java`
#### Snippet
```java
    */
   private ExecutorService createExecutor() {
      ExecutorService service = Executors.newFixedThreadPool(3, new ThreadFactory() {

         ThreadGroup group = new ThreadGroup("JMSBridgeImpl");
```

### UnnecessaryLocalVariable
Local variable `message` is redundant
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTUtil.java`
#### Snippet
```java
      MqttFixedHeader fixedHeader = new MqttFixedHeader(MqttMessageType.PUBREL, false, MqttQoS.AT_LEAST_ONCE, false, 0);
      MqttPublishMessage publishMessage = new MqttPublishMessage(fixedHeader, null, null);
      Message message = createServerMessage(session, address, publishMessage)
         .putIntProperty(MQTTUtil.MQTT_MESSAGE_ID_KEY, messageId)
         .putIntProperty(MQTTUtil.MQTT_MESSAGE_TYPE_KEY, MqttMessageType.PUBREL.value());
```

### UnnecessaryLocalVariable
Local variable `resp` is redundant
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenWireConnection.java`
#### Snippet
```java
            protocolManager.addConnection(OpenWireConnection.this, info);
         } catch (Exception e) {
            Response resp = new ExceptionResponse(e);
            return resp;
         }
```

### UnnecessaryLocalVariable
Local variable `fileName` is redundant
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalImpl.java`
#### Snippet
```java
   public String getHistoryFileName(long sequence, Calendar calendar) {

      String fileName = String.format("%s-%04d%02d%02d%02d%02d%02d-%d.%s", filesRepository.getFilePrefix(), calendar.get(Calendar.YEAR),
                                      (calendar.get(Calendar.MONTH) + 1), calendar.get(Calendar.DAY_OF_MONTH), calendar.get(Calendar.HOUR_OF_DAY),
                                      calendar.get(Calendar.MINUTE), calendar.get(Calendar.SECOND), sequence, filesRepository.getFileExtension());
```

### UnnecessaryLocalVariable
Local variable `newUrl` is redundant
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQConnectionFactory.java`
#### Snippet
```java
         }

         final String newUrl = URISupport.applyParameters(uri, params).toString();
         return newUrl;
      } catch (Exception e) {
```

### UnnecessaryLocalVariable
Local variable `pair` is redundant
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQDestination.java`
#### Snippet
```java
      }

      Pair<String, String> pair = new Pair<>(parts[0].toString(), parts[1].toString());

      return pair;
```

### UnnecessaryLocalVariable
Local variable `object` is redundant
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQObjectMessage.java`
#### Snippet
```java
            ois.setWhiteList(whiteList);
         }
         Serializable object = (Serializable) ois.readObject();
         return object;
      } catch (Exception e) {
```

### UnnecessaryLocalVariable
Local variable `jmsdata` is redundant
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQMessage.java`
#### Snippet
```java

   public static CompositeData coreCompositeTypeToJMSCompositeType(CompositeDataSupport data) throws Exception {
      CompositeData jmsdata = new CompositeDataSupport(data.getCompositeType(), new HashMap<String, Object>());
      return jmsdata;
   }
```

### UnnecessaryLocalVariable
Local variable `saslSubject` is redundant
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/sasl/scram/SCRAMServerSASL.java`
#### Snippet
```java
   protected Subject createSaslSubject(String userName, UserData userData) {
      UserPrincipal userPrincipal = new UserPrincipal(userName);
      Subject saslSubject = new Subject(true, Collections.singleton(userPrincipal), Collections.singleton(userData),
                                        Collections.emptySet());
      return saslSubject;
```

### UnnecessaryLocalVariable
Local variable `startMatch` is redundant
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/KMPNeedle.java`
#### Snippet
```java
         }
         if (j == needleLength) {
            final int startMatch = index - needleLength + 1;
            return startMatch;
         }
```

### UnnecessaryLocalVariable
Local variable `clientOutgoing` is redundant
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/ProtonProtocolManager.java`
#### Snippet
```java
   /** for outgoing */
   public ProtonClientProtocolManager createClientManager() {
      ProtonClientProtocolManager clientOutgoing = new ProtonClientProtocolManager(factory, server);
      return clientOutgoing;
   }
```

### UnnecessaryLocalVariable
Local variable `entry` is redundant
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/ProtonProtocolManager.java`
#### Snippet
```java

      // connection entry only understands -1 otherwise we would see disconnects for no reason
      ConnectionEntry entry = new ConnectionEntry(protonRemotingConnection, executor, System.currentTimeMillis(), ttl <= 0 ? -1 : ttl);

      return entry;
```

### UnnecessaryLocalVariable
Local variable `encodeSize` is redundant
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessagePersisterV3.java`
#### Snippet
```java
   @Override
   public int getEncodeSize(Message record) {
      int encodeSize = super.getEncodeSize(record) +
         DataConstants.SIZE_LONG; // expiration
      return encodeSize;
```

### UnnecessaryLocalVariable
Local variable `persisters` is redundant
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/ProtonProtocolManagerFactory.java`
#### Snippet
```java
   public Persister<Message>[] getPersister() {

      Persister[] persisters = new Persister[]{AMQPMessagePersister.getInstance(), AMQPMessagePersisterV2.getInstance(), AMQPLargeMessagePersister.getInstance(), AMQPMessagePersisterV3.getInstance()};
      return persisters;
   }
```

### UnnecessaryLocalVariable
Local variable `updatedEstimate` is redundant
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
            int addition = unmarshalledApplicationPropertiesMemoryEstimateFromData(data);
            ((PagingStore)owner).addSize(addition, false);
            final int updatedEstimate = memoryEstimate + addition;
            memoryEstimate = updatedEstimate;
         }
```

### UnnecessaryLocalVariable
Local variable `protonSession` is redundant
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/AMQPConnectionContext.java`
#### Snippet
```java
   protected AMQPSessionContext newSessionExtension(Session realSession) throws ActiveMQAMQPException {
      AMQPSessionCallback sessionSPI = connectionCallback.createSessionCallback(this);
      AMQPSessionContext protonSession = new AMQPSessionContext(sessionSPI, this, realSession, protocolManager.getServer());

      return protonSession;
```

### UnnecessaryLocalVariable
Local variable `source` is redundant
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/connect/AMQPBrokerConnection.java`
#### Snippet
```java
   private static Queue checkCurrentMirror(AMQPBrokerConnection brokerConnection,
                                             AMQPMirrorControllerSource currentMirrorController) {
      AMQPMirrorControllerSource source = currentMirrorController;
      if (source.getBrokerConnection() == brokerConnection) {
         return source.getSnfQueue();
```

### UnnecessaryLocalVariable
Local variable `result` is redundant
in `artemis-jdbc-store/src/main/java/org/apache/activemq/artemis/jdbc/store/sql/PropertySQLProvider.java`
#### Snippet
```java
         }
      }
      String result = sql.getProperty(key);
      return result;
   }
```

### UnnecessaryLocalVariable
Local variable `libFolder` is redundant
in `artemis-web/src/main/java/org/apache/activemq/artemis/component/WebServerComponent.java`
#### Snippet
```java
   private File getLibFolder() {
      Path lib = artemisHomePath.resolve("lib");
      File libFolder = new File(lib.toUri());
      return libFolder;
   }
```

### UnnecessaryLocalVariable
Local variable `pair` is redundant
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ServerLocatorImpl.java`
#### Snippet
```java
            logger.trace("Selecting connector from topology.");
            int pos = loadBalancingPolicy.select(usedTopology.length);
            Pair<TransportConfiguration, TransportConfiguration> pair = usedTopology[pos];

            return pair;
```

### UnnecessaryLocalVariable
Local variable `message` is redundant
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/ActiveMQSessionContext.java`
#### Snippet
```java
    */
   private void handleConsumerDisconnected(DisconnectConsumerMessage packet) throws ActiveMQException {
      DisconnectConsumerMessage message = packet;

      session.handleConsumerDisconnect(new ActiveMQConsumerContext(message.getConsumerId()));
```

### UnnecessaryLocalVariable
Local variable `largeMessageReturn` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/protocol/EmbedMessageUtil.java`
#### Snippet
```java
      ActiveMQBuffer buffer = ActiveMQBuffers.wrappedBuffer(message.getBytesProperty(AMQP_ENCODE_PROPERTY));

      Message largeMessageReturn = readEncoded(message, storageManager, buffer);

      return largeMessageReturn;
```

### UnnecessaryLocalVariable
Local variable `scram` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/security/jaas/SCRAMPropertiesLoginModule.java`
#### Snippet
```java

   private static SCRAM getTypeByString(String type) {
      SCRAM scram = Arrays.stream(SCRAM.values())
                          .filter(v -> v.getName().equals(type))
                          .findFirst()
```

### UnnecessaryLocalVariable
Local variable `equivalent` is redundant
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/NettyConnector.java`
#### Snippet
```java
         String otherActiveMQServerName = ConfigurationHelper.getStringProperty(TransportConstants.ACTIVEMQ_SERVER_NAME, null, configuration);
         String activeMQServerName = ConfigurationHelper.getStringProperty(TransportConstants.ACTIVEMQ_SERVER_NAME, null, this.configuration);
         boolean equivalent = isSameHostAndPort(configuration) && otherActiveMQServerName != null && otherActiveMQServerName.equals(activeMQServerName);
         return equivalent;
      } else {
```

### UnnecessaryLocalVariable
Local variable `size` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/DivertConfiguration.java`
#### Snippet
```java
         }
      }
      int size =  BufferHelper.sizeOfNullableString(name) +
            BufferHelper.sizeOfNullableString(address) +
            BufferHelper.sizeOfNullableString(forwardingAddress) +
```

### UnnecessaryLocalVariable
Local variable `configurations` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/ConfigurationUtils.java`
#### Snippet
```java
         AcceptorTransportConfigurationParser parser = new AcceptorTransportConfigurationParser();

         List<TransportConfiguration> configurations = parser.newObject(uri, name);

         return configurations;
```

### UnnecessaryLocalVariable
Local variable `configurations` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/ConfigurationUtils.java`
#### Snippet
```java
         AcceptorTransportConfigurationParser parser = new AcceptorTransportConfigurationParser();

         List<TransportConfiguration> configurations = parser.newObject(parser.expandURI(uri), name);

         return configurations;
```

### UnnecessaryLocalVariable
Local variable `configurations` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/ConfigurationUtils.java`
#### Snippet
```java
         ConnectorTransportConfigurationParser parser = new ConnectorTransportConfigurationParser();

         List<TransportConfiguration> configurations = parser.newObject(uri, name);

         return configurations;
```

### UnnecessaryLocalVariable
Local variable `configurations` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/ConfigurationUtils.java`
#### Snippet
```java
         ConnectorTransportConfigurationParser parser = new ConnectorTransportConfigurationParser();

         List<TransportConfiguration> configurations = parser.newObject(parser.expandURI(uri), name);

         return configurations;
```

### UnnecessaryLocalVariable
Local variable `size` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/BridgeConfiguration.java`
#### Snippet
```java
         }
      }
      int size =  BufferHelper.sizeOfNullableString(name) +
         BufferHelper.sizeOfNullableString(parentName) +
         BufferHelper.sizeOfNullableString(queueName) +
```

### UnnecessaryLocalVariable
Local variable `selectorNode` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/impl/LegacyJMSConfiguration.java`
#### Snippet
```java

         if (QUEUE_SELECTOR_NODE_NAME.equals(child.getNodeName())) {
            Node selectorNode = child;
            Node attNode = selectorNode.getAttributes().getNamedItem("string");
            selectorString = attNode.getNodeValue();
```

### UnnecessaryLocalVariable
Local variable `fileBufferWrapper` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/impl/PageReadWriter.java`
#### Snippet
```java
         //to fail due to ByteBuf::capacity == ByteBuffer::remaining bytes
         assert wrappedBuffer.readableBytes() == capacity;
         final ChannelBufferWrapper fileBufferWrapper = new ChannelBufferWrapper(wrappedBuffer);
         return fileBufferWrapper;
      } finally {
```

### UnnecessaryLocalVariable
Local variable `page` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/impl/PagingStoreImpl.java`
#### Snippet
```java
      SequentialFile file = factory.createSequentialFile(fileName);

      Page page = new Page(storeName, storageManager, factory, file, pageNumber);

      return page;
```

### UnnecessaryLocalVariable
Local variable `server` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/ActiveMQServers.java`
#### Snippet
```java
                                                  final MBeanServer mbeanServer,
                                                  final ActiveMQSecurityManager securityManager) {
      ActiveMQServer server = ActiveMQServers.newActiveMQServer(config, mbeanServer, securityManager, true);

      return server;
```

### UnnecessaryLocalVariable
Local variable `server` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/ActiveMQServers.java`
#### Snippet
```java
      new FileDeploymentManager(configURL).addDeployable(config).addDeployable(legacyJMSConfiguration).readConfiguration();

      ActiveMQServer server = ActiveMQServers.newActiveMQServer(config, mbeanServer, securityManager);

      return server;
```

### UnnecessaryLocalVariable
Local variable `server` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/ActiveMQServers.java`
#### Snippet
```java
      securityManager.getConfiguration().addUser(defUser, defPass);

      ActiveMQServer server = ActiveMQServers.newActiveMQServer(config, ManagementFactory.getPlatformMBeanServer(), securityManager, config.isPersistenceEnabled());

      return server;
```

### UnnecessaryLocalVariable
Local variable `server` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/ActiveMQServers.java`
#### Snippet
```java
      ActiveMQJAASSecurityManager securityManager = new ActiveMQJAASSecurityManager(InVMLoginModule.class.getName(), securityConfiguration);

      ActiveMQServer server = ActiveMQServers.newActiveMQServer(config, mbeanServer, securityManager, enablePersistence);

      return server;
```

### UnnecessaryLocalVariable
Local variable `server` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/ActiveMQServers.java`
#### Snippet
```java
      ActiveMQSecurityManager securityManager = new ActiveMQJAASSecurityManager(InVMLoginModule.class.getName(), new SecurityConfiguration());

      ActiveMQServer server = ActiveMQServers.newActiveMQServer(config, mbeanServer, securityManager, enablePersistence);

      return server;
```

### UnnecessaryLocalVariable
Local variable `server` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/ActiveMQServers.java`
#### Snippet
```java
      ActiveMQSecurityManager securityManager = new ActiveMQJAASSecurityManager(InVMLoginModule.class.getName(), new SecurityConfiguration());

      ActiveMQServer server = ActiveMQServers.newActiveMQServer(config, ManagementFactory.getPlatformMBeanServer(), securityManager, enablePersistence);

      return server;
```

### UnnecessaryLocalVariable
Local variable `server` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/ActiveMQServers.java`
#### Snippet
```java
      config.setPersistenceEnabled(enablePersistence);

      ActiveMQServer server = new ActiveMQServerImpl(config, mbeanServer, securityManager);

      return server;
```

### UnnecessaryLocalVariable
Local variable `cursorList` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/cursor/impl/PageCursorProviderImpl.java`
#### Snippet
```java
    */
   private synchronized ArrayList<PageSubscription> cloneSubscriptions() {
      ArrayList<PageSubscription> cursorList = new ArrayList<>(activeCursors.values());
      return cursorList;
   }
```

### UnnecessaryLocalVariable
Local variable `newAddressInfo` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/AddressInfo.java`
#### Snippet
```java
      for (Map.Entry<SimpleString, RoutingType> entry : prefixes.entrySet()) {
         if (this.getName().startsWith(entry.getKey())) {
            AddressInfo newAddressInfo = this.create(PrefixUtil.removePrefix(this.getName(), entry.getKey()), entry.getValue());
            return newAddressInfo;
         }
```

### UnnecessaryLocalVariable
Local variable `b` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerConsumerImpl.java`
#### Snippet
```java
      public boolean isBrowsed() {
         messageQueue.deliverAsync();
         boolean b = !iterator.hasNext();
         return b;
      }
```

### UnnecessaryLocalVariable
Local variable `dbTime` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/jdbc/JdbcLeaseLock.java`
#### Snippet
```java
            resultSet.next();

            final long dbTime = (currentDateTimeTimeZone == null ?
               resultSet.getTimestamp(1) :
               resultSet.getTimestamp(1, Calendar.getInstance(currentDateTimeTimeZone))).getTime();
```

### UnnecessaryLocalVariable
Local variable `list` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/ClusterManager.java`
#### Snippet
```java

   private synchronized Collection<ClusterConnection> cloneClusterConnections() {
      ArrayList<ClusterConnection> list = new ArrayList<>(clusterConnections.values());
      return list;
   }
```

### UnnecessaryLocalVariable
Local variable `map` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/qourum/Vote.java`
#### Snippet
```java

   public Map<String, Object> getVoteMap() {
      HashMap<String, Object> map = new HashMap<>();
      return map;
   }
```

### UnnecessaryLocalVariable
Local variable `cache` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/routing/ConnectionRouterManager.java`
#### Snippet
```java

   private Cache deployCache(CacheConfiguration configuration, String name) throws ClassNotFoundException {
      Cache cache = new LocalCache(CACHE_ID_PREFIX + name, configuration.isPersisted(),
         configuration.getTimeout(), server.getStorageManager());

```

### UnnecessaryLocalVariable
Local variable `session` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ActiveMQServerImpl.java`
#### Snippet
```java
      }

      final ServerSessionImpl session = internalCreateSession(name, username, password, validatedUser, minLargeMessageSize, connection, autoCommitSends, autoCommitAcks, preAcknowledge, xa, defaultAddress, callback, context, autoCreateQueues, prefixes, securityDomain, isLegacyProducer);

      return session;
```

### UnnecessaryLocalVariable
Local variable `result` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ActiveMQServerImpl.java`
#### Snippet
```java
   @Override
   public boolean addAddressInfo(AddressInfo addressInfo) throws Exception {
      boolean result = postOffice.addAddressInfo(addressInfo);


```

### UnnecessaryLocalVariable
Local variable `serverSocket` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/ConnectorServerFactory.java`
#### Snippet
```java
      @Override
      public ServerSocket createServerSocket(int port) throws IOException {
         ServerSocket serverSocket = (ServerSocket) ServerSocketFactory.getDefault().createServerSocket(port, 50, InetAddress.getByName(rmiServerHost));
         return serverSocket;
      }
```

### UnnecessaryLocalVariable
Local variable `size` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/CoreSessionCallback.java`
#### Snippet
```java
      channel.send(packet);

      int size = packet.getPacketSize();

      return size;
```

### UnnecessaryLocalVariable
Local variable `connector` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/remoting/impl/invm/InVMConnectorFactory.java`
#### Snippet
```java
                                    final ScheduledExecutorService scheduledThreadPool,
                                    final ClientProtocolManager protocolManager) {
      InVMConnector connector = new InVMConnector(configuration, handler, listener, closeExecutor, threadPool, protocolManager);

      return connector;
```

### UnnecessaryLocalVariable
Local variable `result` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/impl/ManagementServiceImpl.java`
#### Snippet
```java
      }

      Object result = method.invoke(resource, params);
      return result;
   }
```

### UnnecessaryLocalVariable
Local variable `info` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/MBeanInfoHelper.java`
#### Snippet
```java
      }

      MBeanAttributeInfo info = new MBeanAttributeInfo(getAttributeName(operation), operation.getReturnType().getName(), description, (isGetterMethod(operation) || isIsBooleanMethod(operation)), isSetterMethod(operation), isIsBooleanMethod(operation));

      return info;
```

### UnnecessaryLocalVariable
Local variable `ise` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/NettyAcceptor.java`
#### Snippet
```java
         }
      } catch (Exception e) {
         IllegalStateException ise = new IllegalStateException("Unable to create NettyAcceptor for " + host + ":" + port, e);
         throw ise;
      }
```

### UnnecessaryLocalVariable
Local variable `obj` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/view/QueueView.java`
#### Snippet
```java
   public JsonObjectBuilder toJson(QueueControl queue) {
      Queue q = server.locateQueue(new SimpleString(queue.getName()));
      JsonObjectBuilder obj = JsonLoader.createObjectBuilder()
         .add(QueueField.ID.getName(), toString(queue.getID()))
         .add(QueueField.NAME.getName(), toString(queue.getName()))
```

### UnnecessaryLocalVariable
Local variable `obj` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/view/SessionView.java`
#### Snippet
```java
   @Override
   public JsonObjectBuilder toJson(ServerSession session) {
      JsonObjectBuilder obj = JsonLoader.createObjectBuilder()
         .add(SessionField.ID.getName(), toString(session.getName()))
         .add(SessionField.USER.getName(), toString(session.getUsername()))
```

### UnnecessaryLocalVariable
Local variable `obj` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/view/ProducerView.java`
#### Snippet
```java
      }

      JsonObjectBuilder obj = JsonLoader.createObjectBuilder()
         .add(ProducerField.ID.getName(), toString(producer.getID()))
         .add(ProducerField.NAME.getName(), toString(producer.getName()))
```

### UnnecessaryLocalVariable
Local variable `obj` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/view/ConsumerView.java`
#### Snippet
```java
      }

      JsonObjectBuilder obj = JsonLoader.createObjectBuilder()
         .add(ConsumerField.ID.getName(), toString(consumer.getSequentialID()))
         .add(ConsumerField.SESSION.getName(), toString(consumer.getSessionName()))
```

### UnnecessaryLocalVariable
Local variable `retValue` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/QueueControlImpl.java`
#### Snippet
```java
            }

            int retValue = queue.moveReferences(flushLimit, filter, binding.getAddress(), rejectDuplicates, messageCount, binding);
            return retValue;
         } finally {
```

### UnnecessaryLocalVariable
Local variable `id` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/nullpm/NullStorageManager.java`
#### Snippet
```java
   @Override
   public long generateID() {
      long id = idSequence.getAndIncrement();

      return id;
```

### UnnecessaryLocalVariable
Local variable `difference` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/JournalStorageManager.java`
#### Snippet
```java
         int modulus = fileSize % alignment;
         if (modulus != 0) {
            int difference = modulus;
            int low = fileSize - difference;
            int high = low + alignment;
```

### UnnecessaryLocalVariable
Local variable `message` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/AbstractJournalStorageManager.java`
#### Snippet
```java

   private Message decodeMessage(CoreMessageObjectPools pools, ActiveMQBuffer buff) {
      Message message = MessagePersister.getInstance().decode(buff, null, pools, this);
      return message;
   }
```

## RuleId[id=BusyWait]
### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `artemis-maven-plugin/src/main/java/org/apache/activemq/artemis/maven/ArtemisCLIPlugin.java`
#### Snippet
```java
                  } catch (Exception e) {
                     getLog().info("awaiting server to start");
                     Thread.sleep(500);
                     continue;
                  }
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/TimeUtils.java`
#### Snippet
```java
      while (check.check() != expected && timeLeft > 0) {
         try {
            Thread.sleep(interval);
         } catch (InterruptedException e) {
         }
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/UUIDTimer.java`
#### Snippet
```java
      do {
         try {
            Thread.sleep(delay);
         } catch (InterruptedException ie) {
         }
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/actors/ProcessorBase.java`
#### Snippet
```java
            }

            Thread.sleep(10);
         }
      } catch (InterruptedException e) {
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/io/nio/NIOSequentialFileFactory.java`
#### Snippet
```java
            while (System.currentTimeMillis() > timeout && obj.get() != null) {
               System.gc();
               Thread.sleep(100);
            }
         } catch (InterruptedException e) {
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/inflow/ActiveMQActivation.java`
#### Snippet
```java

            try {
               Thread.sleep(setupInterval);
            } catch (InterruptedException e) {
               logger.debug("Interrupted trying to reconnect {}", spec, e);
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/ConsumerThread.java`
#### Snippet
```java

            if (sleep > 0) {
               Thread.sleep(sleep);
            }

```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/ConsumerThread.java`
#### Snippet
```java
            }
            if (sleep > 0) {
               Thread.sleep(sleep);
            }

```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `artemis-cli/src/main/java/org/apache/activemq/artemis/util/ServerUtil.java`
#### Snippet
```java
         } catch (Exception e) {
            System.out.println("awaiting server " + uri + " start at ");
            Thread.sleep(500);
            continue;
         }
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `artemis-unit-test-support/src/main/java/org/apache/activemq/artemis/utils/ThreadLeakCheckRule.java`
#### Snippet
```java
         System.gc();
         try {
            Thread.sleep(500);
         } catch (InterruptedException e) {
         }
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `artemis-unit-test-support/src/main/java/org/apache/activemq/artemis/utils/ThreadLeakCheckRule.java`
#### Snippet
```java
                  forceGC();
                  try {
                     Thread.sleep(500);
                  } catch (Throwable e) {
                  }
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/ChannelImpl.java`
#### Snippet
```java
            while (!responseAsyncCache.add(packet)) {
               try {
                  Thread.sleep(1);
               } catch (Exception e) {
                  // Ignore
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/ChannelImpl.java`
#### Snippet
```java
            while (!responseAsyncCache.add(packet)) {
               try {
                  Thread.sleep(1);
               } catch (Exception e) {
                  // Ignore
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/ActiveMQSessionContext.java`
#### Snippet
```java
               // sleep a little bit to avoid spinning too much
               try {
                  Thread.sleep(10);
               } catch (InterruptedException ie) {
                  Thread.currentThread().interrupt();
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/utils/TokenBucketLimiterImpl.java`
#### Snippet
```java
         } else {
            try {
               Thread.sleep(1);
            } catch (Exception e) {
               // Ignore
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/utils/MemorySize.java`
#### Snippet
```java
         System.gc();
         try {
            Thread.sleep(500);
         } catch (InterruptedException e) {
         }
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/MemoryManager.java`
#### Snippet
```java
               }

               Thread.sleep(measureInterval);
            } catch (InterruptedException ignore) {
               if (!started) {
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/FileBasedNodeManager.java`
#### Snippet
```java
            if (count < 5) {
               try {
                  Thread.sleep(100);
               } catch (InterruptedException e1) {
               }
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ReplicationBackupActivation.java`
#### Snippet
```java
            final ReplicationFailure failure = replicateLive(clusterController, nodeLocator, registrationFailureForwarder);
            if (failure == null) {
               Thread.sleep(clusterController.getRetryIntervalForReplicatedCluster());
               continue;
            }
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/InVMNodeManager.java`
#### Snippet
```java
      do {
         while (state == NOT_STARTED) {
            Thread.sleep(10);
         }

```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/InVMNodeManager.java`
#### Snippet
```java
         if (state == PAUSED) {
            liveLock.release();
            Thread.sleep(10);
         } else if (state == FAILING_BACK) {
            liveLock.release();
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/InVMNodeManager.java`
#### Snippet
```java
         } else if (state == FAILING_BACK) {
            liveLock.release();
            Thread.sleep(10);
         } else if (state == LIVE) {
            break;
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/InVMNodeManager.java`
#### Snippet
```java
   public void awaitLiveStatus() throws InterruptedException {
      while (state != LIVE) {
         Thread.sleep(10);
      }
   }
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/FileLockNodeManager.java`
#### Snippet
```java
               logger.debug("awaiting live node startup state = '{}'", (char) state);

               Thread.sleep(2000);
               state = getState();
            }
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/FileLockNodeManager.java`
#### Snippet
```java
               liveLock.release();
               logger.debug("awaiting live node restarting");
               Thread.sleep(2000);
            } else if (state == FAILINGBACK) {
               liveLock.release();
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/FileLockNodeManager.java`
#### Snippet
```java
               liveLock.release();
               logger.debug("awaiting live node failing back");
               Thread.sleep(2000);
            } else if (state == LIVE) {
               // if the backup acquires the file lock and the state is 'L' that means the primary died
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/FileLockNodeManager.java`
#### Snippet
```java
            if (lock == null) {
               try {
                  Thread.sleep(500);
               } catch (InterruptedException e) {
                  return null;
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/FileLockNodeManager.java`
#### Snippet
```java
   public void awaitLiveStatus() throws NodeManagerException, InterruptedException {
      while (getState() != LIVE) {
         Thread.sleep(2000);
      }
   }
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/quorum/ActivationSequenceStateMachine.java`
#### Snippet
```java
                             nodeId, nodeActivationSequence, timeout);
               if (timeout == 0) {
                  Thread.sleep(CHECK_ACTIVATION_SEQUENCE_WAIT_MILLIS);
               }
               continue;
```

## RuleId[id=RedundantFileCreation]
### RedundantFileCreation
`new File` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/journal/DecodeJournal.java`
#### Snippet
```java
                                    final int fileSize,
                                    final String fileInput) throws Exception {
      try (FileInputStream fileInputStream = new FileInputStream(new File(fileInput))) {
         importJournal(directory, journalPrefix, journalSuffix, minFiles, fileSize, fileInputStream);
      }
```

## RuleId[id=PointlessBooleanExpression]
### PointlessBooleanExpression
`other instanceof Pair == false` can be simplified to '!(other instanceof Pair)'
in `artemis-commons/src/main/java/org/apache/activemq/artemis/api/core/Pair.java`
#### Snippet
```java
      }

      if (other instanceof Pair == false) {
         return false;
      }
```

### PointlessBooleanExpression
`other instanceof ObjLongPair == false` can be simplified to '!(other instanceof ObjLongPair)'
in `artemis-commons/src/main/java/org/apache/activemq/artemis/api/core/ObjLongPair.java`
#### Snippet
```java
      }

      if (other instanceof ObjLongPair == false) {
         return false;
      }
```

### PointlessBooleanExpression
`list.contains(current) == false` can be simplified to '!list.contains(current)'
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/ExceptionUtil.java`
#### Snippet
```java
      List<Throwable> list = new ArrayList<>();
      Throwable current = throwable;
      while (current != null && list.contains(current) == false) {
         list.add(current);
         current = current.getCause();
```

### PointlessBooleanExpression
`IS_WINDOWS == true` can be simplified to 'IS_WINDOWS'
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Env.java`
#### Snippet
```java

   public static boolean isWindowsOs() {
      return IS_WINDOWS == true;
   }
}
```

### PointlessBooleanExpression
`IS_LINUX == true` can be simplified to 'IS_LINUX'
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Env.java`
#### Snippet
```java

   public static boolean isLinuxOs() {
      return IS_LINUX == true;
   }

```

### PointlessBooleanExpression
`IS_MAC == true` can be simplified to 'IS_MAC'
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Env.java`
#### Snippet
```java

   public static boolean isMacOs() {
      return IS_MAC == true;
   }

```

### PointlessBooleanExpression
`sessions.isEmpty() == false` can be simplified to '!sessions.isEmpty()'
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/ActiveMQRASessionFactoryImpl.java`
#### Snippet
```java
      try {
         synchronized (sessions) {
            if (sessions.isEmpty() == false) {
               throw new IllegalStateException("Only allowed one session per connection. See the J2EE spec, e.g. J2EE1.4 Section 6.6");
            }
```

### PointlessBooleanExpression
`lock.tryLock(tryLock.intValue(), TimeUnit.SECONDS) == false` can be simplified to '!lock.tryLock(tryLock.intValue(), TimeUnit.SECONDS)'
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/ActiveMQRAManagedConnection.java`
#### Snippet
```java
      }
      try {
         if (lock.tryLock(tryLock.intValue(), TimeUnit.SECONDS) == false) {
            throw new ResourceAllocationException("Unable to obtain lock in " + tryLock + " seconds: " + this);
         }
```

### PointlessBooleanExpression
`cri.isTransacted() == false` can be simplified to '!cri.isTransacted()'
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/ActiveMQRASession.java`
#### Snippet
```java
         Session session = getSessionInternal();

         if (cri.isTransacted() == false) {
            throw new IllegalStateException("Session is not transacted");
         }
```

### PointlessBooleanExpression
`cri.isTransacted() == false` can be simplified to '!cri.isTransacted()'
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/ActiveMQRASession.java`
#### Snippet
```java
         Session session = getSessionInternal();

         if (cri.isTransacted() == false) {
            throw new IllegalStateException("Session is not transacted");
         }
```

### PointlessBooleanExpression
`val instanceof byte[] == false` can be simplified to '!(val instanceof byte\[\])'
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/bridge/impl/JMSBridgeImpl.java`
#### Snippet
```java
            Object val = entry.getValue();

            if (val instanceof byte[] == false) {
               //Can't set byte[] array props through the JMS API - if we're bridging an ActiveMQ Artemis message it might have such props
               msg.setObjectProperty(propName, entry.getValue());
```

### PointlessBooleanExpression
`ref.isDurable() == true` can be simplified to 'ref.isDurable()'
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTSessionCallback.java`
#### Snippet
```java
       * Therefore, enforce flow-control based on the number of pending QoS 1 & 2 messages
       */
      if (ref != null && ref.isDurable() == true && connection.getReceiveMaximum() != -1 && session.getState().getOutboundStore().getPendingMessages() >= connection.getReceiveMaximum()) {
         return false;
      } else {
```

### PointlessBooleanExpression
`jmsMessage instanceof ActiveMQMessage == false` can be simplified to '!(jmsMessage instanceof ActiveMQMessage)'
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/api/jms/management/JMSManagementHelper.java`
#### Snippet
```java

   private static ClientMessage getCoreMessage(final Message jmsMessage) {
      if (jmsMessage instanceof ActiveMQMessage == false) {
         throw new IllegalArgumentException("Cannot send a foreign message as a management message " + jmsMessage.getClass().getName());
      }
```

### PointlessBooleanExpression
`dest instanceof ActiveMQDestination == false` can be simplified to '!(dest instanceof ActiveMQDestination)'
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/compatible1X/ActiveMQCompatibleMessage.java`
#### Snippet
```java
         return null;
      } else {
         if (dest instanceof ActiveMQDestination == false) {
            throw new InvalidDestinationException("Foreign destination " + dest);
         }
```

### PointlessBooleanExpression
`dest instanceof ActiveMQDestination == false` can be simplified to '!(dest instanceof ActiveMQDestination)'
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQMessage.java`
#### Snippet
```java
         replyTo = null;
      } else {
         if (dest instanceof ActiveMQDestination == false) {
            throw new InvalidDestinationException("Foreign destination " + dest);
         }
```

### PointlessBooleanExpression
`other instanceof SessionCloseMessage == false` can be simplified to '!(other instanceof SessionCloseMessage)'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionCloseMessage.java`
#### Snippet
```java
   @Override
   public boolean equals(final Object other) {
      if (other instanceof SessionCloseMessage == false) {
         return false;
      }
```

### PointlessBooleanExpression
`prop instanceof Number == false` can be simplified to '!(prop instanceof Number)'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/utils/ConfigurationHelper.java`
#### Snippet
```java
               return def;
            }
         } else if (prop instanceof Number == false) {
            ActiveMQClientLogger.LOGGER.propertyNotLong(propName, prop.getClass().getName());

```

### PointlessBooleanExpression
`prop instanceof Number == false` can be simplified to '!(prop instanceof Number)'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/utils/ConfigurationHelper.java`
#### Snippet
```java
               return def;
            }
         } else if (prop instanceof Number == false) {
            ActiveMQClientLogger.LOGGER.propertyNotInteger(propName, prop.getClass().getName());

```

### PointlessBooleanExpression
`prop instanceof String == false` can be simplified to '!(prop instanceof String)'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/utils/ConfigurationHelper.java`
#### Snippet
```java
         return def;
      } else {
         if (prop instanceof String == false) {
            return prop.toString();
         } else {
```

### PointlessBooleanExpression
`prop instanceof Boolean == false` can be simplified to '!(prop instanceof Boolean)'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/utils/ConfigurationHelper.java`
#### Snippet
```java
         if (prop instanceof String) {
            return Boolean.valueOf((String) prop);
         } else if (prop instanceof Boolean == false) {
            ActiveMQClientLogger.LOGGER.propertyNotBoolean(propName, prop.getClass().getName());

```

### PointlessBooleanExpression
`listener instanceof DelegatingFailureListener == false` can be simplified to '!(listener instanceof DelegatingFailureListener)'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ClientSessionFactoryImpl.java`
#### Snippet
```java
      for (FailureListener listener : oldListeners) {
         // Add all apart from the old DelegatingFailureListener
         if (listener instanceof DelegatingFailureListener == false) {
            newListeners.add(listener);
         }
```

### PointlessBooleanExpression
`bUpdate == true` can be simplified to 'bUpdate'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/messagecounter/MessageCounter.java`
#### Snippet
```java
            }

            if (bUpdate == true) {
               if (counters[i] == -1) {
                  counters[i] = 0;
```

## RuleId[id=UseBulkOperation]
### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `artemis-maven-plugin/src/main/java/org/apache/activemq/artemis/maven/ArtemisUpgradePlugin.java`
#### Snippet
```java
   private void add(List<String> list, String... str) {
      for (String s : str) {
         list.add(s);
      }
   }
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `artemis-maven-plugin/src/main/java/org/apache/activemq/artemis/maven/ArtemisCreatePlugin.java`
#### Snippet
```java
   private void add(List<String> list, String... str) {
      for (String s : str) {
         list.add(s);
      }
   }
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `artemis-protocols/artemis-stomp-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/stomp/StompConnection.java`
#### Snippet
```java
               EnumSet<RoutingType> routingTypes = EnumSet.noneOf(RoutingType.class);
               for (RoutingType existingRoutingType : addressInfo.getRoutingTypes()) {
                  routingTypes.add(existingRoutingType);
               }
               routingTypes.add(effectiveAddressRoutingType);
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/SpawnedVMSupport.java`
#### Snippet
```java
      if (vmargs != null) {
         for (String arg : vmargs) {
            commandList.add(arg);
         }
      }
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/SpawnedVMSupport.java`
#### Snippet
```java
      commandList.add(className);
      for (String arg : args) {
         commandList.add(arg);
      }

```

### UseBulkOperation
Iteration can be replaced with bulk 'Map.putAll()' call
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/uri/URISchema.java`
#### Snippet
```java
         if (propertyOverrides != null) {
            for (Map.Entry<String, String> entry : propertyOverrides.entrySet()) {
               rc.put(entry.getKey(), entry.getValue());
            }
         }
```

### UseBulkOperation
Iteration can be replaced with bulk 'Map.putAll()' call
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/ActiveMQResourceAdapter.java`
#### Snippet
```java
      if (overrideConnectionParams != null) {
         for (Map.Entry<String, Object> stringObjectEntry : overrideConnectionParams.entrySet()) {
            map.put(stringObjectEntry.getKey(), stringObjectEntry.getValue());
         }
      }
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/OptionsUtil.java`
#### Snippet
```java
            String[] names = annotation.name();
            for (String n : names) {
               options.add(n);
            }
         }
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `artemis-cli/src/main/java/org/apache/activemq/artemis/util/ServerUtil.java`
#### Snippet
```java

         for (String arg: args) {
            command.add(arg);
         }

```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/server/impl/JMSServerManagerImpl.java`
#### Snippet
```java
      }
      for (String bindingsItem : bindings) {
         list.add(bindingsItem);
      }
   }
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/config/federation/FederationPolicySet.java`
#### Snippet
```java
   public FederationPolicySet setPolicyRefs(Set<String> refs) {
      for (String ref : refs) {
         policyRefs.add(ref);
      }
      return this;
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/config/federation/FederationStreamConfiguration.java`
#### Snippet
```java
   public T setPolicyRefs(Set<String> refs) {
      for (String ref : refs) {
         policyRefs.add(ref);
      }
      return (T) this;
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/impl/ConfigurationImpl.java`
#### Snippet
```java
            Set convertedValue = new HashSet();
            for (String entry : value.toString().split(",")) {
               convertedValue.add(entry);
            }
            return (T) convertedValue;
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/impl/ConfigurationImpl.java`
#### Snippet
```java
            List convertedValue = new ArrayList<String>();
            for (String entry : value.toString().split(",")) {
               convertedValue.add(entry);
            }
            return (T) convertedValue;
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/JMXAccessControlList.java`
#### Snippet
```java
         }
         for (String role : roles) {
            rolesList.add(role);
         }
      }
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/JMXAccessControlList.java`
#### Snippet
```java
      public void addCatchAll(String... roles) {
         for (String role : roles) {
            catchAllRoles.add(role);
         }
      }
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/JMXAccessControlList.java`
#### Snippet
```java
         }
         for (String role : roles) {
            rolesList.add(role);
         }
      }
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/remoting/server/impl/RemotingServiceImpl.java`
#### Snippet
```java

      for (ConnectionEntry entry : connections.values()) {
         conns.add(entry);
      }

```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/replication/ReplicationManager.java`
#### Snippet
```java

      for (OperationContext ctx : pendingTokens) {
         activeContexts.add(ctx);
      }

```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/AbstractJournalStorageManager.java`
#### Snippet
```java

      for (PersistentQueueBindingEncoding queue : mapBindings.values()) {
         queueBindingInfos.add(queue);
      }

```

## RuleId[id=EnumSwitchStatementWhichMissesCases]
### EnumSwitchStatementWhichMissesCases
`switch (context.getResult().getStatus()) { case REFUSED_USE_ANOTHER: context.ge...` statement on enum type 'org.apache.activemq.artemis.core.server.routing.targets.TargetResult.Status' misses case 'OK'
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTRoutingHandler.java`
#### Snippet
```java
   @Override
   protected void refuse(MQTTRoutingContext context) {
      switch (context.getResult().getStatus()) {
         case REFUSED_USE_ANOTHER:
            context.getMQTTSession().getProtocolHandler().sendConnack(MQTTReasonCodes.USE_ANOTHER_SERVER);
            break;
         case REFUSED_UNAVAILABLE:
            context.getMQTTSession().getProtocolHandler().sendConnack(MQTTReasonCodes.SERVER_UNAVAILABLE);
            break;
      }
      context.getMQTTSession().getProtocolHandler().disconnect(true);
   }
```

### EnumSwitchStatementWhichMissesCases
`switch (message.fixedHeader().messageType()) { case PUBLISH: MqttPu...` statement on enum type 'io.netty.handler.codec.mqtt.MqttMessageType' misses cases: 'UNSUBACK', 'PINGREQ', 'PINGRESP', and 'AUTH'
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTUtil.java`
#### Snippet
```java
switch (message.fixedHeader().messageType()) {
               case PUBLISH:
                  MqttPublishVariableHeader publishHeader = (MqttPublishVariableHeader) message.variableHeader();
                  String topicName = publishHeader.topicName();
                  if (topicName == null || topicName.length() == 0) {
                     topicName = "<empty>";
                  }
                  log.append("(" + publishHeader.packetId() + ")")
                     .append(" topic=" + topicName)
                     .append(", qos=" + message.fixedHeader().qosLevel().value())
                     .append(", retain=" + message.fixedHeader().isRetain())
                     .append(", dup=" + message.fixedHeader().isDup())
                     .append(", remainingLength=" + message.fixedHeader().remainingLength());
                  for (MqttProperties.MqttProperty property : ((MqttPublishMessage)message).variableHeader().properties().listAll()) {
                     Object value = property.value();
                     if (value != null) {
                        if (value instanceof byte[]) {
                           value = new String((byte[]) value, StandardCharsets.UTF_8);
                        } else if (value instanceof ArrayList && ((ArrayList)value).size() > 0 && ((ArrayList)value).get(0) instanceof MqttProperties.StringPair) {
                           StringBuilder userProperties = new StringBuilder();
                           userProperties.append("[");
                           for (MqttProperties.StringPair pair : (ArrayList<MqttProperties.StringPair>) value) {
                              userProperties.append(pair.key).append(": ").append(pair.value).append(", ");
                           }
                           userProperties.delete(userProperties.length() - 2, userProperties.length());
                           userProperties.append("]");
                           value = userProperties.toString();
                        }
                     }
                     log.append(", " + formatCase(MqttPropertyType.valueOf(property.propertyId()).name()) + "=" + value);
                  }
                  log.append(", payload=" + getPayloadForLogging((MqttPublishMessage) message, 256));
                  break;
               case CONNECT:
                  // intentionally omit the username & password from the log
                  MqttConnectVariableHeader connectHeader = (MqttConnectVariableHeader) message.variableHeader();
                  MqttConnectPayload payload = ((MqttConnectMessage)message).payload();
                  log.append(" protocol=(").append(connectHeader.name()).append(", ").append(connectHeader.version()).append(")")
                     .append(", hasPassword=").append(connectHeader.hasPassword())
                     .append(", isCleanStart=").append(connectHeader.isCleanSession())
                     .append(", keepAliveTimeSeconds=").append(connectHeader.keepAliveTimeSeconds())
                     .append(", clientIdentifier=").append(payload.clientIdentifier())
                     .append(", hasUserName=").append(connectHeader.hasUserName())
                     .append(", isWillFlag=").append(connectHeader.isWillFlag());
                  if (connectHeader.isWillFlag()) {
                     log.append(", willQos=").append(connectHeader.willQos())
                        .append(", isWillRetain=").append(connectHeader.isWillRetain())
                        .append(", willTopic=").append(payload.willTopic());
                  }
                  for (MqttProperties.MqttProperty property : connectHeader.properties().listAll()) {
                     log.append(", " + formatCase(MqttPropertyType.valueOf(property.propertyId()).name()) + "=" + property.value());
                  }
                  break;
               case CONNACK:
                  MqttConnAckVariableHeader connackHeader = (MqttConnAckVariableHeader) message.variableHeader();
                  log.append(" connectReasonCode=").append(formatByte(connackHeader.connectReturnCode().byteValue()))
                     .append(", sessionPresent=").append(connackHeader.isSessionPresent());
                  for (MqttProperties.MqttProperty property : connackHeader.properties().listAll()) {
                     log.append(", " + formatCase(MqttPropertyType.valueOf(property.propertyId()).name()) + "=" + property.value());
                  }
                  break;
               case SUBSCRIBE:
                  for (MqttTopicSubscription sub : ((MqttSubscribeMessage) message).payload().topicSubscriptions()) {
                     log.append("\n\ttopic: ").append(sub.topicName())
                        .append(", qos: ").append(sub.qualityOfService())
                        .append(", nolocal: ").append(sub.option().isNoLocal())
                        .append(", retainHandling: ").append(sub.option().retainHandling())
                        .append(", isRetainAsPublished: ").append(sub.option().isRetainAsPublished());
                  }
                  break;
               case SUBACK:
                  for (Integer qos : ((MqttSubAckMessage) message).payload().grantedQoSLevels()) {
                     log.append("\n\t" + qos);
                  }
                  break;
               case UNSUBSCRIBE:
                  for (String topic : ((MqttUnsubscribeMessage) message).payload().topics()) {
                     log.append("\n\t" + topic);
                  }
                  break;
               case PUBACK:
                  break;
               case PUBREC:
               case PUBREL:
               case PUBCOMP:
                  if (version == MQTTVersion.MQTT_5) {
                     MqttPubReplyMessageVariableHeader pubReplyVariableHeader = (MqttPubReplyMessageVariableHeader) message.variableHeader();
                     log.append(" reasonCode=").append(formatByte(pubReplyVariableHeader.reasonCode()));
                  }
                  break;
               case DISCONNECT:
                  if (version == MQTTVersion.MQTT_5) {
                     MqttReasonCodeAndPropertiesVariableHeader disconnectVariableHeader = (MqttReasonCodeAndPropertiesVariableHeader) message.variableHeader();
                     log.append(" reasonCode=").append(formatByte(disconnectVariableHeader.reasonCode()));
                  }
                  break;
            }
```

### EnumSwitchStatementWhichMissesCases
`switch (newState) { case LOST: unavailable = true; listene...` statement on enum type 'org.apache.curator.framework.state.ConnectionState' misses cases: 'CONNECTED', and 'READ_ONLY'
in `artemis-quorum-ri/src/main/java/org/apache/activemq/artemis/quorum/zookeeper/CuratorDistributedPrimitiveManager.java`
#### Snippet
```java
      startHandlingEvents();
      try {
         switch (newState) {
            case LOST:
               unavailable = true;
               listeners.forEach(listener -> listener.onUnavailableManagerEvent());
               primitives.forEach((id, primitive) -> primitive.onLost());
               break;
            case RECONNECTED:
               primitives.forEach((id, primitive) -> primitive.onReconnected());
               break;
            case SUSPENDED:
               primitives.forEach((id, primitive) -> primitive.onSuspended());
               break;
         }
      } finally {
         completeHandlingEvents();
```

### EnumSwitchStatementWhichMissesCases
`switch (context.getResult().getStatus()) { case REFUSED_USE_ANOTHER: error.setD...` statement on enum type 'org.apache.activemq.artemis.core.server.routing.targets.TargetResult.Status' misses case 'OK'
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/AMQPRoutingHandler.java`
#### Snippet
```java
      ErrorCondition error = new ErrorCondition();
      error.setCondition(ConnectionError.CONNECTION_FORCED);
      switch (context.getResult().getStatus()) {
         case REFUSED_USE_ANOTHER:
            error.setDescription(String.format("Connection router %s rejected this connection", context.getRouter()));
            break;
         case REFUSED_UNAVAILABLE:
            error.setDescription(String.format("Connection router %s is not ready", context.getRouter()));
            break;
      }

      Connection protonConnection = context.getProtonConnection();
```

### EnumSwitchStatementWhichMissesCases
`switch (context.getResult().getStatus()) { case REFUSED_UNAVAILABLE: throw Acti...` statement on enum type 'org.apache.activemq.artemis.core.server.routing.targets.TargetResult.Status' misses case 'OK'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/ActiveMQRoutingHandler.java`
#### Snippet
```java
   @Override
   public void refuse(ActiveMQRoutingContext context) throws Exception {
      switch (context.getResult().getStatus()) {
         case REFUSED_UNAVAILABLE:
            throw ActiveMQMessageBundle.BUNDLE.connectionRouterNotReady(context.getRouter());
         case REFUSED_USE_ANOTHER:
            throw ActiveMQMessageBundle.BUNDLE.connectionRejected(context.getRouter());
      }
   }

```

### EnumSwitchStatementWhichMissesCases
`switch (f) { case ID: return matches(consumer.getSequentialID()); case...` statement on enum type 'org.apache.activemq.artemis.core.management.impl.view.ConsumerField' misses cases: 'CONNECTION', 'QUEUE_TYPE', 'BROWSE_ONLY', ...
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/view/predicate/ConsumerFilterPredicate.java`
#### Snippet
```java
switch (f) {
         case ID:
            return matches(consumer.getSequentialID());
         case SESSION:
            return matches(consumer.getSessionID());
         case USER:
            return matches(server.getSessionByID(consumer.getSessionID()).getUsername());
         case VALIDATED_USER:
            return matches(server.getSessionByID(consumer.getSessionID()).getValidatedUser());
         case ADDRESS:
            return matches(consumer.getQueue().getAddress());
         case QUEUE:
            return matches(consumer.getQueue().getName());
         case FILTER:
            return matches(consumer.getFilterString());
         case PROTOCOL:
            return matches(server.getSessionByID(consumer.getSessionID()).getRemotingConnection().getProtocolName());
         case CLIENT_ID:
            return matches(server.getSessionByID(consumer.getSessionID()).getRemotingConnection().getClientID());
         case LOCAL_ADDRESS:
            return matches(server.getSessionByID(consumer.getSessionID()).getRemotingConnection().getTransportConnection().getLocalAddress());
         case REMOTE_ADDRESS:
            return matches(server.getSessionByID(consumer.getSessionID()).getRemotingConnection().getTransportConnection().getRemoteAddress());
         case MESSAGES_IN_TRANSIT:
            return matches(consumer.getMessagesInTransit());
         case MESSAGES_IN_TRANSIT_SIZE:
            return matches(consumer.getMessagesInTransitSize());
         case MESSAGES_DELIVERED:
            return matches(consumer.getDeliveringMessages());
         case MESSAGES_DELIVERED_SIZE:
            return matches(consumer.getMessagesDeliveredSize());
         case MESSAGES_ACKNOWLEDGED:
            return matches(consumer.getMessagesAcknowledged());
         case MESSAGES_ACKNOWLEDGED_AWAITING_COMMIT:
            return matches(consumer.getMessagesAcknowledgedAwaitingCommit());
      }
```

## RuleId[id=StringEquality]
### StringEquality
String values are compared using `!=`, not 'equals()'
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQSession.java`
#### Snippet
```java
      }

      if (topicNameToUse != topicName) {
         topic.setName(topicName);
      }
```

### StringEquality
String values are compared using `!=`, not 'equals()'
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQSession.java`
#### Snippet
```java
      }

      if (queueName != queueNameToUse) {
         queue.setName(queueName);
      }
```

### StringEquality
String values are compared using `==`, not 'equals()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/storage/DatabaseStorageConfiguration.java`
#### Snippet
```java
      if (connectionProvider == null) {
         // commons-dbcp2 doesn't support DataSource::getConnection(user, password)
         if (dataSourceClassName == ActiveMQDefaultConfiguration.getDefaultDataSourceClassName()) {
            connectionProvider = new JDBCConnectionProvider(getDataSource());
         } else {
```

### StringEquality
String values are compared using `==`, not 'equals()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/embedded/EmbeddedActiveMQ.java`
#### Snippet
```java

      if (propertiesResourcePath != null) {
         if (propertiesResourcePath == ActiveMQDefaultConfiguration.BROKER_PROPERTIES_SYSTEM_PROPERTY_NAME) {
            // try and locate on the classpath, broker.properties
            URL brokerPropertiesFromClasspath = this.getClass().getClassLoader().getResource(propertiesResourcePath);
```

### StringEquality
String values are compared using `==`, not 'equals()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/postoffice/impl/AddressPartNode.java`
#### Snippet
```java
            for (AddressPartNode<T> anyChild : node.getChildren()) {

               if (ANY_CHILD == anyChild.getPath() || ANY_DESCENDENT == anyChild.getPath()) {
                  continue;
               }
```

### StringEquality
String values are compared using `==`, not 'equals()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/postoffice/impl/AddressPartNode.java`
#### Snippet
```java
            for (AddressPartNode<T> anyChild : node.getChildren()) {

               if (ANY_CHILD == anyChild.getPath() || ANY_DESCENDENT == anyChild.getPath()) {
                  continue;
               }
```

### StringEquality
String values are compared using `==`, not 'equals()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/postoffice/impl/AddressPartNode.java`
#### Snippet
```java
      visitValues(collector);
      for (AddressPartNode<T> child : childNodes.values()) {
         if (ANY_CHILD == child.getPath() || ANY_DESCENDENT == child.getPath()) {
            continue;
         }
```

### StringEquality
String values are compared using `==`, not 'equals()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/postoffice/impl/AddressPartNode.java`
#### Snippet
```java
      visitValues(collector);
      for (AddressPartNode<T> child : childNodes.values()) {
         if (ANY_CHILD == child.getPath() || ANY_DESCENDENT == child.getPath()) {
            continue;
         }
```

### StringEquality
String values are compared using `==`, not 'equals()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/postoffice/impl/AddressPartNode.java`
#### Snippet
```java
         match = getChild(paths[i]);
         if (match != null) {
            if (ANY_CHILD == match.getPath() || ANY_DESCENDENT == match.getPath()) {
               continue;
            }
```

### StringEquality
String values are compared using `==`, not 'equals()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/postoffice/impl/AddressPartNode.java`
#### Snippet
```java
         match = getChild(paths[i]);
         if (match != null) {
            if (ANY_CHILD == match.getPath() || ANY_DESCENDENT == match.getPath()) {
               continue;
            }
```

### StringEquality
String values are compared using `!=`, not 'equals()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/postoffice/impl/AddressPartNode.java`
#### Snippet
```java
      for (AddressPartNode<T> child : childNodes.values()) {
         // instance equality arranged in node creation getChildOrCreate
         if (child != match && ANY_DESCENDENT != child.getPath() && ANY_CHILD != child.getPath()) {

            child.visitPathTailNonWildcard(paths, startIndex, collector);
```

### StringEquality
String values are compared using `!=`, not 'equals()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/postoffice/impl/AddressPartNode.java`
#### Snippet
```java
      for (AddressPartNode<T> child : childNodes.values()) {
         // instance equality arranged in node creation getChildOrCreate
         if (child != match && ANY_DESCENDENT != child.getPath() && ANY_CHILD != child.getPath()) {

            child.visitPathTailNonWildcard(paths, startIndex, collector);
```

## RuleId[id=ConditionalBreakInInfiniteLoop]
### ConditionalBreakInInfiniteLoop
Conditional break inside loop
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalImpl.java`
#### Snippet
```java

      // We can't start reclaim while compacting is working
      while (true) {
         if (state != JournalImpl.JournalState.LOADED)
            return false;
```

### ConditionalBreakInInfiniteLoop
Conditional break inside loop
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/cursor/impl/PageCursorProviderImpl.java`
#### Snippet
```java
      try (ArtemisCloseable readLock = storageManager.closeableReadLock()) {

         while (true) {
            if (pagingStore.lock(100)) {
               break;
```

## RuleId[id=LoopStatementsThatDontLoop]
### LoopStatementsThatDontLoop
`while` statement does not loop
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java

      private MessageReference iterate(LinkedListIterator<MessageReference> iterator) {
         while (iterator.hasNext()) {
            MessageReference ref = iterator.next();
            if (ref.isPaged()) {
```

## RuleId[id=StaticCallOnSubclass]
### StaticCallOnSubclass
Static method `getOldPrefixedAddress()` declared in class 'org.apache.activemq.artemis.core.protocol.core.impl.wireformat.QueueAbstractPacket' but referenced via subclass 'org.apache.activemq.artemis.core.protocol.core.impl.wireformat.SessionQueueQueryMessage'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/protocol/core/ServerSessionPacketHandler.java`
#### Snippet
```java

                  if (result.isExists() && remotingConnection.getChannelVersion() < PacketImpl.ADDRESSING_CHANGE_VERSION) {
                     result.setAddress(SessionQueueQueryMessage.getOldPrefixedAddress(result.getAddress(), result.getRoutingType()));
                  }

```

## RuleId[id=SuspiciousSystemArraycopy]
### SuspiciousSystemArraycopy
Source parameter type 'java.security.cert.Certificate\[\]' is not assignable to destination parameter `certChain` of type 'java.security.cert.X509Certificate\[\]'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/impl/ssl/SSLSupport.java`
#### Snippet
```java
      Certificate[] chain = keyStore.getCertificateChain(keystoreAlias);
      X509Certificate[] certChain = new X509Certificate[chain.length];
      System.arraycopy(chain, 0, certChain, 0, chain.length);
      return new Pair(key, certChain);
   }
```

## RuleId[id=RedundantClassCall]
### RedundantClassCall
Redundant call to `cast()`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/json/JsonValue.java`
#### Snippet
```java

   default JsonArray asJsonArray() {
      return JsonArray.class.cast(this);
   }

```

### RedundantClassCall
Redundant call to `cast()`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/json/JsonValue.java`
#### Snippet
```java

   default JsonObject asJsonObject() {
      return JsonObject.class.cast(this);
   }

```

### RedundantClassCall
Redundant call to `cast()`
in `artemis-server-osgi/src/main/java/org/apache/activemq/artemis/osgi/OsgiBroker.java`
#### Snippet
```java

      StoreConfiguration storeConfiguration = server.getConfiguration().getStoreConfiguration();
      String dataSourceName = String.class.cast(properties.get("dataSourceName"));

      if (storeConfiguration != null &&
```

### RedundantClassCall
Redundant call to `cast()`
in `artemis-server-osgi/src/main/java/org/apache/activemq/artemis/osgi/OsgiBroker.java`
#### Snippet
```java
         String filter = "(&(objectClass=javax.sql.DataSource)(osgi.jndi.service.name=" + dataSourceName + "))";
         DataSourceTracker trackerCust =
                  new DataSourceTracker(name, context, DatabaseStorageConfiguration.class.cast(storeConfiguration),
                                        (ServerTrackerCallBack) callback);
         dataSourceTracker = new ServiceTracker(context, context.createFilter(filter), trackerCust);
```

### RedundantClassCall
Redundant call to `isInstance()`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/MBeanInfoHelper.java`
#### Snippet
```java
         String type = paramTypes[i].getName();
         for (Annotation anno : params[i]) {
            if (Parameter.class.isInstance(anno)) {
               String name = Parameter.class.cast(anno).name();
               String description = Parameter.class.cast(anno).desc();
```

### RedundantClassCall
Redundant call to `cast()`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/MBeanInfoHelper.java`
#### Snippet
```java
         for (Annotation anno : params[i]) {
            if (Parameter.class.isInstance(anno)) {
               String name = Parameter.class.cast(anno).name();
               String description = Parameter.class.cast(anno).desc();
               paramInfo = new MBeanParameterInfo(name, type, description);
```

### RedundantClassCall
Redundant call to `cast()`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/MBeanInfoHelper.java`
#### Snippet
```java
            if (Parameter.class.isInstance(anno)) {
               String name = Parameter.class.cast(anno).name();
               String description = Parameter.class.cast(anno).desc();
               paramInfo = new MBeanParameterInfo(name, type, description);
            }
```

## RuleId[id=UnnecessaryStringEscape]
### UnnecessaryStringEscape
`\'` is unnecessarily escaped
in `artemis-log-annotation-processor/src/main/java/org/apache/activemq/artemis/logs/annotation/processor/LogAnnotationProcessor.java`
#### Snippet
```java
      tupples(message, '{', '}', (tupple) -> {
         if (!tupple.equals("")) {
            throw new IllegalArgumentException("Invalid placeholder argument {" + tupple + "} on message \'" + message + "\' as part of " + holder + "\nreplace it by {}");
         }
      });
```

### UnnecessaryStringEscape
`\'` is unnecessarily escaped
in `artemis-log-annotation-processor/src/main/java/org/apache/activemq/artemis/logs/annotation/processor/LogAnnotationProcessor.java`
#### Snippet
```java
      tupples(message, '{', '}', (tupple) -> {
         if (!tupple.equals("")) {
            throw new IllegalArgumentException("Invalid placeholder argument {" + tupple + "} on message \'" + message + "\' as part of " + holder + "\nreplace it by {}");
         }
      });
```

### UnnecessaryStringEscape
`\'` is unnecessarily escaped
in `artemis-log-annotation-processor/src/main/java/org/apache/activemq/artemis/logs/annotation/processor/LogAnnotationProcessor.java`
#### Snippet
```java

      if (message.contains("%s") || message.contains("%d")) {
         throw new IllegalArgumentException("Cannot use %s or %d in loggers. Please use {} on message \'" + message + "\'");
      }
   }
```

### UnnecessaryStringEscape
`\'` is unnecessarily escaped
in `artemis-log-annotation-processor/src/main/java/org/apache/activemq/artemis/logs/annotation/processor/LogAnnotationProcessor.java`
#### Snippet
```java

      if (message.contains("%s") || message.contains("%d")) {
         throw new IllegalArgumentException("Cannot use %s or %d in loggers. Please use {} on message \'" + message + "\'");
      }
   }
```

## RuleId[id=AssertWithSideEffects]
### AssertWithSideEffects
`assert` has side effects: writerIndex = ...
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/TypedProperties.java`
#### Snippet
```java
      // it's a trick to not pay the cost of buffer.writeIndex without assertions enabled
      int writerIndex = 0;
      assert (writerIndex = buffer.writerIndex()) >= 0 : "Always true";
      if (properties == null || size == 0) {
         encodedSize = DataConstants.SIZE_BYTE;
```

### AssertWithSideEffects
`assert` has side effects: state = ...
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalImpl.java`
#### Snippet
```java
                                                    final AtomicReference<ByteBuffer> wholeFileBufferRef) throws Exception {
      JournalState state;
      assert (state = this.state) != JournalState.STOPPED &&
         state != JournalState.LOADED &&
         state != replicationSync;
```

## RuleId[id=ObsoleteCollection]
### ObsoleteCollection
Obsolete collection type `Vector` used
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/ActiveMQRAConnectionMetaData.java`
#### Snippet
```java
   @Override
   public Enumeration<Object> getJMSXPropertyNames() {
      Vector<Object> v = new Vector<>();
      v.add("JMSXGroupID");
      v.add("JMSXGroupSeq");
```

### ObsoleteCollection
Obsolete collection type `Vector<>` used
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/ActiveMQRAConnectionMetaData.java`
#### Snippet
```java
   @Override
   public Enumeration<Object> getJMSXPropertyNames() {
      Vector<Object> v = new Vector<>();
      v.add("JMSXGroupID");
      v.add("JMSXGroupSeq");
```

### ObsoleteCollection
Obsolete collection type `Hashtable` used
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/ActiveMQRAProperties.java`
#### Snippet
```java
    * @return return the jndi params to use
    */
   public Hashtable<?, ?> getParsedJndiParams() {
      return jndiParams;
   }
```

### ObsoleteCollection
Obsolete collection type `Hashtable` used
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/ActiveMQRAProperties.java`
#### Snippet
```java
   }

   public void setParsedJndiParams(Hashtable<?, ?> params) {
      jndiParams = params;
   }
```

### ObsoleteCollection
Obsolete collection type `Hashtable` used
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/ActiveMQRAProperties.java`
#### Snippet
```java
   private long setupInterval = DEFAULT_SETUP_INTERVAL;

   private Hashtable<?, ?> jndiParams;

   private boolean useJNDI;
```

### ObsoleteCollection
Obsolete collection type `Hashtable` used
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/ActiveMQRaUtils.java`
#### Snippet
```java
    * @return hash-table with configuration option pairs
    */
   public static Hashtable<String, String> parseHashtableConfig(final String config) {
      Hashtable<String, String> hashtable = new Hashtable<>();

```

### ObsoleteCollection
Obsolete collection type `Hashtable` used
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/ActiveMQRaUtils.java`
#### Snippet
```java
    */
   public static Hashtable<String, String> parseHashtableConfig(final String config) {
      Hashtable<String, String> hashtable = new Hashtable<>();

      String[] topElements = config.split(";");
```

### ObsoleteCollection
Obsolete collection type `Hashtable<>` used
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/ActiveMQRaUtils.java`
#### Snippet
```java
    */
   public static Hashtable<String, String> parseHashtableConfig(final String config) {
      Hashtable<String, String> hashtable = new Hashtable<>();

      String[] topElements = config.split(";");
```

### ObsoleteCollection
Obsolete collection type `Hashtable` used
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/ActiveMQResourceAdapter.java`
#### Snippet
```java
   }

   public Hashtable<?, ?> getParsedJndiParams() {
      return raProperties.getParsedJndiParams();
   }
```

### ObsoleteCollection
Obsolete collection type `Hashtable` used
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/inflow/ActiveMQActivationSpec.java`
#### Snippet
```java
   }

   public Hashtable<?, ?> getParsedJndiParams() {
      if (parsedJndiParams == null) {
         return ra.getParsedJndiParams();
```

### ObsoleteCollection
Obsolete collection type `Hashtable` used
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/inflow/ActiveMQActivationSpec.java`
#### Snippet
```java
   private String jndiParams = null;

   private Hashtable<String, String> parsedJndiParams;

   /* use local tx instead of XA*/
```

### ObsoleteCollection
Obsolete collection type `Hashtable` used
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/bridge/impl/JNDIConnectionFactoryFactory.java`
#### Snippet
```java
public class JNDIConnectionFactoryFactory extends JNDIFactorySupport implements ConnectionFactoryFactory {

   public JNDIConnectionFactoryFactory(final Hashtable<?, ?> jndiProperties, final String lookup) {
      super(jndiProperties, lookup);
   }
```

### ObsoleteCollection
Obsolete collection type `Hashtable` used
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/bridge/impl/JNDIDestinationFactory.java`
#### Snippet
```java
public class JNDIDestinationFactory extends JNDIFactorySupport implements DestinationFactory {

   public JNDIDestinationFactory(final Hashtable<?, ?> jndiProperties, final String lookup) {
      super(jndiProperties, lookup);
   }
```

### ObsoleteCollection
Obsolete collection type `Hashtable` used
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/bridge/impl/JNDIFactorySupport.java`
#### Snippet
```java
public abstract class JNDIFactorySupport {

   protected Hashtable<?, ?> jndiProperties;

   protected String lookup;
```

### ObsoleteCollection
Obsolete collection type `Hashtable` used
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/bridge/impl/JNDIFactorySupport.java`
#### Snippet
```java
   protected String lookup;

   protected JNDIFactorySupport(final Hashtable<?, ?> jndiProperties, final String lookup) {
      this.jndiProperties = jndiProperties;

```

### ObsoleteCollection
Obsolete collection type `Vector` used
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQConnectionMetaData.java`
#### Snippet
```java
   @Override
   public Enumeration getJMSXPropertyNames() throws JMSException {
      Vector<Object> v = new Vector<>();
      v.add("JMSXGroupID");
      v.add("JMSXGroupSeq");
```

### ObsoleteCollection
Obsolete collection type `Vector<>` used
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQConnectionMetaData.java`
#### Snippet
```java
   @Override
   public Enumeration getJMSXPropertyNames() throws JMSException {
      Vector<Object> v = new Vector<>();
      v.add("JMSXGroupID");
      v.add("JMSXGroupSeq");
```

### ObsoleteCollection
Obsolete collection type `Hashtable` used
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jndi/ActiveMQInitialContextFactory.java`
#### Snippet
```java
   // -------------------------------------------------------------------------

   protected ReadOnlyContext createContext(Hashtable<?, ?> environment, Map<String, Object> data) {
      return new ReadOnlyContext(environment, data);
   }
```

### ObsoleteCollection
Obsolete collection type `Hashtable` used
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jndi/ActiveMQInitialContextFactory.java`
#### Snippet
```java
   }

   protected void createTopics(Map<String, Object> data, Hashtable<?, ?> environment) {
      for (Map.Entry<?, ?> entry : environment.entrySet()) {
         String key = entry.getKey().toString();
```

### ObsoleteCollection
Obsolete collection type `Hashtable` used
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jndi/ActiveMQInitialContextFactory.java`
#### Snippet
```java
   }

   protected void createQueues(Map<String, Object> data, Hashtable<?, ?> environment) {
      for (Map.Entry<?, ?> entry : environment.entrySet()) {
         String key = entry.getKey().toString();
```

### ObsoleteCollection
Obsolete collection type `Hashtable` used
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jndi/ReadOnlyContext.java`
#### Snippet
```java
   }

   protected ReadOnlyContext(ReadOnlyContext clone, Hashtable<String, Object> env, String nameInNamespace) {
      this(clone, env);
      this.nameInNamespace = nameInNamespace;
```

### ObsoleteCollection
Obsolete collection type `Hashtable` used
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jndi/ReadOnlyContext.java`
#### Snippet
```java
   }

   public ReadOnlyContext(Hashtable environment, Map<String, Object> bindings, String nameInNamespace) {
      this(environment, bindings);
      this.nameInNamespace = nameInNamespace;
```

### ObsoleteCollection
Obsolete collection type `Hashtable` used
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jndi/ReadOnlyContext.java`
#### Snippet
```java
   }

   public ReadOnlyContext(Hashtable env) {
      if (env == null) {
         this.environment = new Hashtable<>();
```

### ObsoleteCollection
Obsolete collection type `Hashtable<>` used
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jndi/ReadOnlyContext.java`
#### Snippet
```java
   public ReadOnlyContext(Hashtable env) {
      if (env == null) {
         this.environment = new Hashtable<>();
      } else {
         this.environment = new Hashtable<>(env);
```

### ObsoleteCollection
Obsolete collection type `Hashtable<>` used
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jndi/ReadOnlyContext.java`
#### Snippet
```java
         this.environment = new Hashtable<>();
      } else {
         this.environment = new Hashtable<>(env);
      }
      this.bindings = Collections.EMPTY_MAP;
```

### ObsoleteCollection
Obsolete collection type `Hashtable` used
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jndi/ReadOnlyContext.java`
#### Snippet
```java
   }

   protected ReadOnlyContext(ReadOnlyContext clone, Hashtable env) {
      this.bindings = clone.bindings;
      this.treeBindings = clone.treeBindings;
```

### ObsoleteCollection
Obsolete collection type `Hashtable<>` used
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jndi/ReadOnlyContext.java`
#### Snippet
```java
      this.bindings = clone.bindings;
      this.treeBindings = clone.treeBindings;
      this.environment = new Hashtable<>(env);
   }

```

### ObsoleteCollection
Obsolete collection type `Hashtable` used
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jndi/ReadOnlyContext.java`
#### Snippet
```java
   }

   public ReadOnlyContext(Hashtable environment, Map<String, Object> bindings) {
      if (environment == null) {
         this.environment = new Hashtable<>();
```

### ObsoleteCollection
Obsolete collection type `Hashtable<>` used
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jndi/ReadOnlyContext.java`
#### Snippet
```java
   public ReadOnlyContext(Hashtable environment, Map<String, Object> bindings) {
      if (environment == null) {
         this.environment = new Hashtable<>();
      } else {
         this.environment = new Hashtable<>(environment);
```

### ObsoleteCollection
Obsolete collection type `Hashtable<>` used
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jndi/ReadOnlyContext.java`
#### Snippet
```java
         this.environment = new Hashtable<>();
      } else {
         this.environment = new Hashtable<>(environment);
      }
      this.bindings = new HashMap<>();
```

### ObsoleteCollection
Obsolete collection type `Hashtable` used
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jndi/ReadOnlyContext.java`
#### Snippet
```java
   private static final long serialVersionUID = -5754338187296859149L;

   protected final Hashtable<String, Object> environment; // environment for this context
   protected final Map<String, Object> bindings; // bindings at my level
   protected final Map<String, Object> treeBindings; // all bindings under me
```

### ObsoleteCollection
Obsolete collection type `Hashtable<>` used
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jndi/ReadOnlyContext.java`
#### Snippet
```java

   public ReadOnlyContext() {
      environment = new Hashtable<>();
      bindings = new HashMap<>();
      treeBindings = new HashMap<>();
```

### ObsoleteCollection
Obsolete collection type `Hashtable` used
in `artemis-server-osgi/src/main/java/org/apache/activemq/artemis/osgi/OsgiBroker.java`
#### Snippet
```java
      for (Map.Entry<String, ActiveMQComponent> component : getComponents().entrySet()) {
         String[] classes = getInterfaces(component.getValue());
         Hashtable<String, Object> props = new Hashtable<>();
         for (Enumeration<String> keyEnum = properties.keys(); keyEnum.hasMoreElements(); ) {
            String key = keyEnum.nextElement();
```

### ObsoleteCollection
Obsolete collection type `Hashtable<>` used
in `artemis-server-osgi/src/main/java/org/apache/activemq/artemis/osgi/OsgiBroker.java`
#### Snippet
```java
      for (Map.Entry<String, ActiveMQComponent> component : getComponents().entrySet()) {
         String[] classes = getInterfaces(component.getValue());
         Hashtable<String, Object> props = new Hashtable<>();
         for (Enumeration<String> keyEnum = properties.keys(); keyEnum.hasMoreElements(); ) {
            String key = keyEnum.nextElement();
```

### ObsoleteCollection
Obsolete collection type `Hashtable` used
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/security/jaas/LDAPLoginModule.java`
#### Snippet
```java
   }

   protected void extendInitialEnvironment(Set<LDAPLoginProperty> moduleConfig, Hashtable<String, String> initialContextEnv) {
      // sub-classes may override the method if the default implementation is not sufficient:
      // add all non-module configs to initial DirContext environment to support passing
```

### ObsoleteCollection
Obsolete collection type `Hashtable` used
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/security/jaas/LDAPLoginModule.java`
#### Snippet
```java
      if (context == null) {
         try {
            Hashtable<String, String> env = new Hashtable<>();
            env.put(Context.INITIAL_CONTEXT_FACTORY, getLDAPPropertyValue(ConfigKey.INITIAL_CONTEXT_FACTORY));
            env.put(Context.SECURITY_PROTOCOL, getLDAPPropertyValue(ConfigKey.CONNECTION_PROTOCOL));
```

### ObsoleteCollection
Obsolete collection type `Hashtable<>` used
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/security/jaas/LDAPLoginModule.java`
#### Snippet
```java
      if (context == null) {
         try {
            Hashtable<String, String> env = new Hashtable<>();
            env.put(Context.INITIAL_CONTEXT_FACTORY, getLDAPPropertyValue(ConfigKey.INITIAL_CONTEXT_FACTORY));
            env.put(Context.SECURITY_PROTOCOL, getLDAPPropertyValue(ConfigKey.CONNECTION_PROTOCOL));
```

### ObsoleteCollection
Obsolete collection type `Hashtable` used
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/LegacyLDAPSecuritySettingPlugin.java`
#### Snippet
```java

   private DirContext createContext() throws NamingException {
      Hashtable<String, String> env = new Hashtable<>();
      env.put(Context.INITIAL_CONTEXT_FACTORY, initialContextFactory);
      if (connectionUsername != null && !"".equals(connectionUsername)) {
```

### ObsoleteCollection
Obsolete collection type `Hashtable<>` used
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/LegacyLDAPSecuritySettingPlugin.java`
#### Snippet
```java

   private DirContext createContext() throws NamingException {
      Hashtable<String, String> env = new Hashtable<>();
      env.put(Context.INITIAL_CONTEXT_FACTORY, initialContextFactory);
      if (connectionUsername != null && !"".equals(connectionUsername)) {
```

### ObsoleteCollection
Obsolete collection type `Stack`> used
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/impl/ConfigurationImpl.java`
#### Snippet
```java

      private static final Object[] EMPTY_OBJECT_ARRAY = new Object[]{};
      final Stack<Pair<String, Object>> collections = new Stack<>();
      private BeanUtilsBean beanUtilsBean;

```

### ObsoleteCollection
Obsolete collection type `Stack<>` used
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/impl/ConfigurationImpl.java`
#### Snippet
```java

      private static final Object[] EMPTY_OBJECT_ARRAY = new Object[]{};
      final Stack<Pair<String, Object>> collections = new Stack<>();
      private BeanUtilsBean beanUtilsBean;

```

### ObsoleteCollection
Obsolete collection type `Hashtable` used
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/JMXAccessControlList.java`
#### Snippet
```java
         }

         Hashtable<String, String> keyPropertyList = objectName.getKeyPropertyList();
         for (Map.Entry<String, String> keyEntry : keyPropertyList.entrySet()) {
            String key = normalizeKey(keyEntry.getKey() + "=" + keyEntry.getValue());
```

### ObsoleteCollection
Obsolete collection type `Hashtable` used
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/JMXAccessControlList.java`
#### Snippet
```java
      TreeMap<String, Access> domainMap = domainAccess.get(objectName.getDomain());
      if (domainMap != null) {
         Hashtable<String, String> keyPropertyList = objectName.getKeyPropertyList();
         for (Map.Entry<String, String> keyEntry : keyPropertyList.entrySet()) {
            String key = normalizeKey(keyEntry.getKey() + "=" + keyEntry.getValue());
```

## RuleId[id=RedundantArrayCreation]
### RedundantArrayCreation
Redundant array creation for calling varargs method
in `artemis-maven-plugin/src/main/java/org/apache/activemq/artemis/maven/ArtemisClientPlugin.java`
#### Snippet
```java
            aClass = Class.forName(clientClass);
         }
         Method method = aClass.getDeclaredMethod("main", new Class[]{String[].class});
         method.invoke(null, new Object[]{args});
      } catch (Exception e) {
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Env.java`
#### Snippet
```java
      } catch (Throwable t) {
         try {
            Constructor<sun.misc.Unsafe> c = sun.misc.Unsafe.class.getDeclaredConstructor(new Class[0]);
            c.setAccessible(true);
            instance = c.newInstance(new Object[0]);
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Env.java`
#### Snippet
```java
            Constructor<sun.misc.Unsafe> c = sun.misc.Unsafe.class.getDeclaredConstructor(new Class[0]);
            c.setAccessible(true);
            instance = c.newInstance(new Object[0]);
         } catch (Throwable t1) {
            instance = null;
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/ActiveMQRaUtils.java`
#### Snippet
```java
               Class<?> aClass = loader.loadClass(locatorClass);
               Object o = aClass.newInstance();
               Method m = aClass.getMethod("locateChannel", new Class[]{String.class});
               return (JChannel) m.invoke(o, name);
            } catch (Throwable e) {
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/inflow/ActiveMQActivation.java`
#### Snippet
```java
   static {
      try {
         ONMESSAGE = MessageListener.class.getMethod("onMessage", new Class[]{Message.class});
      } catch (Exception e) {
         throw new RuntimeException(e);
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/security/ActiveMQBasicSecurityManager.java`
#### Snippet
```java
         }
      } else if (properties != null && properties.containsKey(BOOTSTRAP_USER) && properties.containsKey(BOOTSTRAP_PASSWORD) && properties.containsKey(BOOTSTRAP_ROLE)) {
         addOrUpdateUser(properties.get(BOOTSTRAP_USER), properties.get(BOOTSTRAP_PASSWORD), new String[]{properties.get(BOOTSTRAP_ROLE)});
      }
   }
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/impl/ManagementServiceImpl.java`
#### Snippet
```java
         String upperCaseAttribute = attribute.substring(0, 1).toUpperCase() + attribute.substring(1);
         try {
            method = resource.getClass().getMethod("get" + upperCaseAttribute, new Class[0]);
         } catch (NoSuchMethodException nsme) {
            try {
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/impl/ManagementServiceImpl.java`
#### Snippet
```java
         } catch (NoSuchMethodException nsme) {
            try {
               method = resource.getClass().getMethod("is" + upperCaseAttribute, new Class[0]);
            } catch (NoSuchMethodException nsme2) {
               throw ActiveMQMessageBundle.BUNDLE.noGetterMethod(attribute);
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/impl/ManagementServiceImpl.java`
#### Snippet
```java
            }
         }
         return method.invoke(resource, new Object[0]);
      } catch (Throwable t) {
         throw new IllegalStateException("Problem while retrieving attribute " + attribute, t);
```

## RuleId[id=DuplicateBranchesInSwitch]
### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/ThreadDumpUtil.java`
#### Snippet
```java
                  break;
               case TIMED_WAITING:
                  sb.append("\t-  waiting on " + threadInfo.getLockInfo());
                  sb.append('\n');
                  break;
               default:
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/api/jms/JMSFactoryType.java`
#### Snippet
```java
            break;
         case 3:
            type = XA_CF;
            break;
         case 4:
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/JMSExceptionHelper.java`
#### Snippet
```java
      switch (me.getType()) {
         case CONNECTION_TIMEDOUT:
            je = new JMSException(me.getMessage());
            break;

```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/JMSExceptionHelper.java`
#### Snippet
```java

         case INTERNAL_ERROR:
            je = new JMSException(me.getMessage());
            break;

```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/JMSExceptionHelper.java`
#### Snippet
```java

         case NOT_CONNECTED:
            je = new JMSException(me.getMessage());
            break;

```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/JMSExceptionHelper.java`
#### Snippet
```java

         case OBJECT_CLOSED:
            je = new javax.jms.IllegalStateException(me.getMessage());
            break;

```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/JMSExceptionHelper.java`
#### Snippet
```java

         case QUEUE_EXISTS:
            je = new InvalidDestinationException(me.getMessage());
            break;

```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/JMSExceptionHelper.java`
#### Snippet
```java

         case UNSUPPORTED_PACKET:
            je = new javax.jms.IllegalStateException(me.getMessage());
            break;

```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalImpl.java`
#### Snippet
```java
            break;
         case UPDATE_RECORD:
            recordSize = JournalImpl.SIZE_ADD_RECORD;
            break;
         case ADD_RECORD_TX:
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalImpl.java`
#### Snippet
```java
            break;
         case UPDATE_RECORD_TX:
            recordSize = JournalImpl.SIZE_ADD_RECORD_TX;
            break;
         case DELETE_RECORD:
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/converter/AmqpCoreConverter.java`
#### Snippet
```java
            switch (value) {
               case AMQPMessageSupport.QUEUE_TYPE:
                  org.apache.activemq.artemis.reader.MessageUtil.setJMSReplyTo(jms.getInnerMessage(), QUEUE_QUALIFIED_PREFIX + properties.getReplyTo());
                  break;
               case AMQPMessageSupport.TEMP_QUEUE_TYPE:
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/converter/AMQPMessageSupport.java`
#### Snippet
```java

      switch (destinationType) {
         case ANYCAST: prefix = QUEUE_QUALIFIED_PREFIX; break;
         case MULTICAST: prefix = TOPIC_QUALIFIED_PREFIX; break;
         default: prefix = QUEUE_QUALIFIED_PREFIX; break;
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/ChannelImpl.java`
#### Snippet
```java
            return version >= 125;
         case PacketImpl.DISCONNECT_V2:
            return version >= 125;
         case PacketImpl.SESS_QUEUEQUERY_RESP_V2:
            return version >= 126;
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/ChannelImpl.java`
#### Snippet
```java
            return version >= 126;
         case PacketImpl.SESS_BINDINGQUERY_RESP_V2:
            return version >= 126;
         case PacketImpl.SESS_BINDINGQUERY_RESP_V3:
            return version >= 127;
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/ChannelImpl.java`
#### Snippet
```java
            return version >= 129;
         case PacketImpl.SESS_BINDINGQUERY_RESP_V4:
            return version >= 129;
         case PacketImpl.CLUSTER_TOPOLOGY_V4:
         case PacketImpl.CREATESESSION_V2:
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ReplicationError.java`
#### Snippet
```java
            break;
         case AUTHENTICATION:
            failed();
            break;
         case EXCEPTION:
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ReplicationError.java`
#### Snippet
```java
            break;
         case EXCEPTION:
            failed();
            break;
         default:
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/view/predicate/ActiveMQFilterPredicate.java`
#### Snippet
```java
               return false;
            case NOT_CONTAINS:
               return false;
            case LESS_THAN:
               return field < intValue;
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/view/predicate/ActiveMQFilterPredicate.java`
#### Snippet
```java
               return false;
            case NOT_CONTAINS:
               return false;
            case LESS_THAN:
               return field < floatValue;
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/view/predicate/ActiveMQFilterPredicate.java`
#### Snippet
```java
               return false;
            case NOT_CONTAINS:
               return false;
            case LESS_THAN:
               return field < longValue;
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/view/predicate/ActiveMQFilterPredicate.java`
#### Snippet
```java
               return false;
            case LESS_THAN:
               return false;
         }
      }
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/AbstractJournalStorageManager.java`
#### Snippet
```java
               break;
            }
            case SET_SCHEDULED_DELIVERY_TIME: {
               // Do nothing - for prepared txs, the set scheduled delivery time will only occur in a send in which
               // case the message will already have the header for the scheduled delivery time, so no need to do
               // anything.

               break;
            }
            case DUPLICATE_ID: {
               // We need load the duplicate ids at prepare time too
```

## RuleId[id=OctalLiteral]
### OctalLiteral
Octal integer `005`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/api/core/ActiveMQExceptionType.java`
#### Snippet
```java
      }
   },
   UNBLOCKED(005) {
      @Override
      public ActiveMQException createException(String msg) {
```

### OctalLiteral
Octal integer `002`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/api/core/ActiveMQExceptionType.java`
#### Snippet
```java
      }
   },
   NOT_CONNECTED(002) {
      @Override
      public ActiveMQException createException(String msg) {
```

### OctalLiteral
Octal integer `006`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/api/core/ActiveMQExceptionType.java`
#### Snippet
```java
      }
   },
   IO_ERROR(006) {
      @Override
      public ActiveMQException createException(String msg) {
```

### OctalLiteral
Octal integer `004`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/api/core/ActiveMQExceptionType.java`
#### Snippet
```java
      }
   },
   DISCONNECTED(004) {
      @Override
      public ActiveMQException createException(String msg) {
```

### OctalLiteral
Octal integer `003`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/api/core/ActiveMQExceptionType.java`
#### Snippet
```java
      }
   },
   CONNECTION_TIMEDOUT(003) {
      @Override
      public ActiveMQException createException(String msg) {
```

### OctalLiteral
Octal integer `000`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/api/core/ActiveMQExceptionType.java`
#### Snippet
```java
   // Error codes -------------------------------------------------

   INTERNAL_ERROR(000) {
      @Override
      public ActiveMQException createException(String msg) {
```

### OctalLiteral
Octal integer `001`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/api/core/ActiveMQExceptionType.java`
#### Snippet
```java
      }
   },
   UNSUPPORTED_PACKET(001) {
      @Override
      public ActiveMQException createException(String msg) {
```

## RuleId[id=DuplicateExpressions]
### DuplicateExpressions
Multiple occurrences of `contentType.substring(subTypeSeparator + 1).toLowerCase().trim()`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/converter/AMQPContentTypeSupport.java`
#### Snippet
```java
      final String type = contentType.substring(0, subTypeSeparator).toLowerCase().trim();

      String subTypePart = contentType.substring(subTypeSeparator + 1).toLowerCase().trim();

      String parameterPart = null;
```

### DuplicateExpressions
Multiple occurrences of `contentType.substring(subTypeSeparator + 1).toLowerCase().trim()`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/converter/AMQPContentTypeSupport.java`
#### Snippet
```java
      if (parameterSeparator != -1) {
         if (parameterSeparator < subTypePart.length() - 1) {
            parameterPart = contentType.substring(subTypeSeparator + 1).toLowerCase().trim();
         }
         subTypePart = subTypePart.substring(0, parameterSeparator).trim();
```

## RuleId[id=StringBufferReplaceableByString]
### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/PasswordMaskingUtil.java`
#### Snippet
```java
         HashProcessor processor = null;
         Exception exception = null;
         final String codecDesc = new StringBuilder().append(DefaultSensitiveStringCodec.class.getName()).append(";").append(DefaultSensitiveStringCodec.ALGORITHM).append("=").append(DefaultSensitiveStringCodec.ONE_WAY).toString();
         try {
            final DefaultSensitiveStringCodec codec = (DefaultSensitiveStringCodec) PasswordMaskingUtil.getCodec(codecDesc);
```

### StringBufferReplaceableByString
`StringBuffer sb` can be replaced with 'String'
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/bridge/impl/JMSBridgeImpl.java`
#### Snippet
```java
         val = msg.getJMSMessageID();
      } else {
         StringBuffer sb = new StringBuffer(val);

         sb.append(",").append(msg.getJMSMessageID());
```

### StringBufferReplaceableByString
`StringBuilder messageFilter` can be replaced with 'String'
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTSubscriptionManager.java`
#### Snippet
```java
      baseFilter.append("(").append(FilterConstants.ACTIVEMQ_ADDRESS).append(" = '").append(session.getServer().getConfiguration().getManagementNotificationAddress()).append("')");

      StringBuilder messageFilter = new StringBuilder(baseFilter);
      messageFilter.append(")");
      this.messageFilter = new SimpleString(messageFilter.toString());
```

### StringBufferReplaceableByString
`StringBuilder messageFilterNoDollar` can be replaced with 'String'
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTSubscriptionManager.java`
#### Snippet
```java

      // [MQTT-4.7.2-1]
      StringBuilder messageFilterNoDollar = new StringBuilder(baseFilter);
      messageFilterNoDollar.append(" OR ");
      messageFilterNoDollar.append("(").append(FilterConstants.ACTIVEMQ_ADDRESS).append(" LIKE '").append(DOLLAR).append("%')");
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTUtil.java`
#### Snippet
```java
         message.putIntProperty(MQTT_USER_PROPERTY_EXISTS_KEY, userProperties.size());
         for (int i = 0; i < userProperties.size(); i++) {
            String key = new StringBuilder()
               .append(MQTT_USER_PROPERTY_KEY_PREFIX)
               .append(i)
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `artemis-jdbc-store/src/main/java/org/apache/activemq/artemis/jdbc/store/logging/LoggingUtil.java`
#### Snippet
```java
public class LoggingUtil {
   public static String getID(Object object) {
      return new StringBuilder().append(object.getClass().getName()).append("@").append(Integer.toHexString(System.identityHashCode(object))).toString();
   }
}
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/JsonUtil.java`
#### Snippet
```java
   public static String truncateString(final String str, final int valueSizeLimit) {
      if (valueSizeLimit >= 0 && str.length() > valueSizeLimit) {
         return new StringBuilder(valueSizeLimit + 32).append(str.substring(0, valueSizeLimit)).append(", + ").append(str.length() - valueSizeLimit).append(" more").toString();
      } else {
         return str;
```

### StringBufferReplaceableByString
`StringBuilder str` can be replaced with 'String'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/TransportConfiguration.java`
#### Snippet
```java
   @Override
   public String toString() {
      StringBuilder str = new StringBuilder(TransportConfiguration.class.getSimpleName());
      str.append("(name=" + name + ", ");
      str.append("factory=" + (factoryClassName == null ? "null" : replaceWildcardChars(factoryClassName)));
```

### StringBufferReplaceableByString
`StringBuffer buff` can be replaced with 'String'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionDeleteQueueMessage.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", queueName=" + queueName);
      return buff.toString();
```

### StringBufferReplaceableByString
`StringBuffer buff` can be replaced with 'String'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionSendContinuationMessage_V2.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", continues=" + continues);
      buff.append(", message=" + message);
```

### StringBufferReplaceableByString
`StringBuffer buff` can be replaced with 'String'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionXAForgetMessage.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append("]");
      return buff.toString();
```

### StringBufferReplaceableByString
`StringBuffer buff` can be replaced with 'String'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionXAResponseMessage_V2.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", error=" + error);
      buff.append(", message=" + message);
```

### StringBufferReplaceableByString
`StringBuffer buff` can be replaced with 'String'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionXASetTimeoutResponseMessage.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", ok=" + ok);
      return buff.toString();
```

### StringBufferReplaceableByString
`StringBuffer buf` can be replaced with 'String'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionForceConsumerDelivery.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buf = new StringBuffer(super.getPacketString());
      buf.append(", consumerID=" + consumerID);
      buf.append(", sequence=" + sequence);
```

### StringBufferReplaceableByString
`StringBuffer buff` can be replaced with 'String'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/CheckFailoverReplyMessage.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", okToFailover=" + okToFailover);
      return buff.toString();
```

### StringBufferReplaceableByString
`StringBuffer buff` can be replaced with 'String'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionReceiveLargeMessage.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", consumerID=" + consumerID);
      buff.append(", deliveryCount=" + deliveryCount);
```

### StringBufferReplaceableByString
`StringBuffer buff` can be replaced with 'String'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/CreateSessionResponseMessage.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", serverVersion=" + serverVersion);
      return buff.toString();
```

### StringBufferReplaceableByString
`StringBuffer buff` can be replaced with 'String'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/DisconnectMessage_V2.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", nodeID=" + nodeID);
      buff.append(", scaleDownNodeID=" + scaleDownNodeID);
```

### StringBufferReplaceableByString
`StringBuffer buff` can be replaced with 'String'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionSendLargeMessage.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", largeMessage=" + largeMessage);
      return buff.toString();
```

### StringBufferReplaceableByString
`StringBuilder msg` can be replaced with 'String'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/ChannelImpl.java`
#### Snippet
```java
               if (logger.isDebugEnabled()) {
                  // use a StringBuilder for speed since this may be executed a lot
                  StringBuilder msg = new StringBuilder();
                  msg.append("Invocation of interceptor ").append(interceptor.getClass().getName()).append(" on ").
                     append(packet).append(" returned ").append(callNext);
```

### StringBufferReplaceableByString
`StringBuffer buff` can be replaced with 'String'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionQueueQueryResponseMessage_V3.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", autoCreated=" + autoCreated);
      buff.append(", purgeOnNoConsumers=" + purgeOnNoConsumers);
```

### StringBufferReplaceableByString
`StringBuffer buff` can be replaced with 'String'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/RollbackMessage.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", considerLastMessageAsDelivered=" + considerLastMessageAsDelivered);
      return buff.toString();
```

### StringBufferReplaceableByString
`StringBuffer buff` can be replaced with 'String'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionXASetTimeoutMessage.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", timeoutSeconds=" + timeoutSeconds);
      return buff.toString();
```

### StringBufferReplaceableByString
`StringBuffer buff` can be replaced with 'String'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionXAPrepareMessage.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", xid=" + xid);
      return buff.toString();
```

### StringBufferReplaceableByString
`StringBuffer buff` can be replaced with 'String'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionExpireMessage.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", consumerID=" + consumerID);
      buff.append(", messageID=" + messageID);
```

### StringBufferReplaceableByString
`StringBuffer buff` can be replaced with 'String'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SubscribeClusterTopologyUpdatesMessage.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", clusterConnection=" + clusterConnection);
      return buff.toString();
```

### StringBufferReplaceableByString
`StringBuffer buff` can be replaced with 'String'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionQueueQueryResponseMessage.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", address=" + address);
      buff.append(", name=" + name);
```

### StringBufferReplaceableByString
`StringBuffer buff` can be replaced with 'String'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/CreateSessionMessage.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", autoCommitAcks=" + autoCommitAcks);
      buff.append(", autoCommitSends=" + autoCommitSends);
```

### StringBufferReplaceableByString
`StringBuffer buff` can be replaced with 'String'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionProducerCreditsMessage.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", address=" + address);
      buff.append(", credits=" + credits);
```

### StringBufferReplaceableByString
`StringBuffer buff` can be replaced with 'String'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/ReattachSessionMessage.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", lastConfirmedCommandID=" + lastConfirmedCommandID);
      buff.append(", name=" + name);
```

### StringBufferReplaceableByString
`StringBuffer buff` can be replaced with 'String'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionQueueQueryResponseMessage_V2.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", autoCreationEnabled=" + autoCreateQueues);
      return buff.toString();
```

### StringBufferReplaceableByString
`StringBuffer buff` can be replaced with 'String'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionXAStartMessage.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", xid=" + xid);
      return buff.toString();
```

### StringBufferReplaceableByString
`StringBuffer buff` can be replaced with 'String'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/DisconnectConsumerMessage.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", consumerId=" + consumerId);
      return buff.toString();
```

### StringBufferReplaceableByString
`StringBuffer buff` can be replaced with 'String'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionProducerCreditsFailMessage.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", address=" + address);
      buff.append(", credits=" + credits);
```

### StringBufferReplaceableByString
`StringBuffer buff` can be replaced with 'String'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/CheckFailoverMessage.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", nodeID=" + nodeID);
      return buff.toString();
```

### StringBufferReplaceableByString
`StringBuffer buff` can be replaced with 'String'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionSendMessage_V2.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", correlationID=" + correlationID);
      buff.append(", requiresResponse=" + super.isRequiresResponse());
```

### StringBufferReplaceableByString
`StringBuffer buff` can be replaced with 'String'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/ReattachSessionResponseMessage.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", lastConfirmedCommandID=" + lastConfirmedCommandID);
      buff.append(", reattached=" + reattached);
```

### StringBufferReplaceableByString
`StringBuffer buff` can be replaced with 'String'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionXAResponseMessage.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", error=" + error);
      buff.append(", message=" + message);
```

### StringBufferReplaceableByString
`StringBuffer buff` can be replaced with 'String'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionReceiveMessage.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", consumerID=" + consumerID);
      buff.append(", deliveryCount=" + deliveryCount);
```

### StringBufferReplaceableByString
`StringBuffer buff` can be replaced with 'String'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionAcknowledgeMessage.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", consumerID=" + consumerID);
      buff.append(", messageID=" + messageID);
```

### StringBufferReplaceableByString
`StringBuffer buff` can be replaced with 'String'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/ClusterTopologyChangeMessage.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", exit=" + exit);
      buff.append(", last=" + last);
```

### StringBufferReplaceableByString
`StringBuffer buff` can be replaced with 'String'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionBindingQueryResponseMessage_V3.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", autoCreateAddresses=" + autoCreateAddresses);
      return buff.toString();
```

### StringBufferReplaceableByString
`StringBuffer buff` can be replaced with 'String'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionXAResumeMessage.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", xid=" + xid);
      return buff.toString();
```

### StringBufferReplaceableByString
`StringBuffer buff` can be replaced with 'String'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionSendMessage.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", requiresResponse=" + requiresResponse);
      return buff.toString();
```

### StringBufferReplaceableByString
`StringBuffer buff` can be replaced with 'String'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/ClusterTopologyChangeMessage_V2.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", exit=" + exit);
      buff.append(", last=" + last);
```

### StringBufferReplaceableByString
`StringBuffer buff` can be replaced with 'String'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/CreateSessionMessage_V2.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", metadata=" + clientID);
      return buff.toString();
```

### StringBufferReplaceableByString
`StringBuffer buff` can be replaced with 'String'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionIndividualAcknowledgeMessage.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", consumerID=" + consumerID);
      buff.append(", messageID=" + messageID);
```

### StringBufferReplaceableByString
`StringBuffer buff` can be replaced with 'String'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionRequestProducerCreditsMessage.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", credits=" + credits);
      buff.append(", address=" + address);
```

### StringBufferReplaceableByString
`StringBuffer buff` can be replaced with 'String'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/CreateAddressMessage.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", address=" + address);
      buff.append(", routingTypes=" + routingTypes);
```

### StringBufferReplaceableByString
`StringBuffer buff` can be replaced with 'String'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionReceiveContinuationMessage.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", consumerID=" + consumerID);
      return buff.toString();
```

### StringBufferReplaceableByString
`StringBuffer buff` can be replaced with 'String'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/CreateSharedQueueMessage_V2.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", address=" + address);
      buff.append(", queueName=" + queueName);
```

### StringBufferReplaceableByString
`StringBuffer buf` can be replaced with 'String'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/Ping.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buf = new StringBuffer(super.getPacketString());
      buf.append(", connectionTTL=" + connectionTTL);
      return buf.toString();
```

### StringBufferReplaceableByString
`StringBuffer buff` can be replaced with 'String'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionBindingQueryMessage.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", address=" + address);
      buff.append("]");
```

### StringBufferReplaceableByString
`StringBuffer buff` can be replaced with 'String'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/CreateSharedQueueMessage.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", address=" + address);
      buff.append(", queueName=" + queueName);
```

### StringBufferReplaceableByString
`StringBuffer buff` can be replaced with 'String'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/DisconnectConsumerWithKillMessage.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", nodeID=" + nodeID);
      return buff.toString();
```

### StringBufferReplaceableByString
`StringBuffer buff` can be replaced with 'String'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionQueueQueryMessage.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", queueName=" + queueName);
      return buff.toString();
```

### StringBufferReplaceableByString
`StringBuffer buff` can be replaced with 'String'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/CreateQueueMessage_V2.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", autoCreated=" + autoCreated);
      buff.append(", routingType=" + routingType);
```

### StringBufferReplaceableByString
`StringBuffer buff` can be replaced with 'String'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionBindingQueryResponseMessage_V4.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", defaultPurgeOnNoConsumers=" + defaultPurgeOnNoConsumers);
      buff.append(", defaultMaxConsumers=" + defaultMaxConsumers);
```

### StringBufferReplaceableByString
`StringBuffer buf` can be replaced with 'String'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/DisconnectMessage_V3.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buf = new StringBuffer(super.getPacketString());
      buf.append(", nodeID=" + nodeID);
      buf.append(", reason=" + reason);
```

### StringBufferReplaceableByString
`StringBuffer buff` can be replaced with 'String'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/DisconnectMessage.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", nodeID=" + nodeID);
      return buff.toString();
```

### StringBufferReplaceableByString
`StringBuffer buff` can be replaced with 'String'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionCreateConsumerMessage.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", queueName=" + queueName);
      buff.append(", filterString=" + filterString);
```

### StringBufferReplaceableByString
`StringBuffer buff` can be replaced with 'String'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionSendContinuationMessage.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", continues=" + continues);
      buff.append(", message=" + message);
```

### StringBufferReplaceableByString
`StringBuffer buff` can be replaced with 'String'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/ClusterTopologyChangeMessage_V3.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", exit=" + exit);
      buff.append(", last=" + last);
```

### StringBufferReplaceableByString
`StringBuffer buff` can be replaced with 'String'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionAddMetaDataMessageV2.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", key=" + key);
      buff.append(", data=" + data);
```

### StringBufferReplaceableByString
`StringBuffer buff` can be replaced with 'String'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionBindingQueryResponseMessage_V2.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", autoCreateQueues=" + autoCreateQueues);
      return buff.toString();
```

### StringBufferReplaceableByString
`StringBuffer buff` can be replaced with 'String'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionXARollbackMessage.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", xid=" + xid);
      return buff.toString();
```

### StringBufferReplaceableByString
`StringBuffer buff` can be replaced with 'String'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionAddMetaDataMessage.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", key=" + key);
      buff.append(", data=" + data);
```

### StringBufferReplaceableByString
`StringBuffer buff` can be replaced with 'String'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionXAJoinMessage.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", xid=" + xid);
      return buff.toString();
```

### StringBufferReplaceableByString
`StringBuffer buff` can be replaced with 'String'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionXAGetInDoubtXidsResponseMessage.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", xids=" + xids);
      buff.append("]");
```

### StringBufferReplaceableByString
`StringBuffer buff` can be replaced with 'String'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/ClusterTopologyChangeMessage_V4.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", clientVersion=" + serverVersion);
      return buff.toString();
```

### StringBufferReplaceableByString
`StringBuffer buff` can be replaced with 'String'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionXAAfterFailedMessage.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", xid=" + xid);
      return buff.toString();
```

### StringBufferReplaceableByString
`StringBuffer buff` can be replaced with 'String'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SubscribeClusterTopologyUpdatesMessageV2.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", clientVersion=" + clientVersion);
      return buff.toString();
```

### StringBufferReplaceableByString
`StringBuffer buff` can be replaced with 'String'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/CreateQueueMessage.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", address=" + address);
      buff.append(", queueName=" + queueName);
```

### StringBufferReplaceableByString
`StringBuffer buff` can be replaced with 'String'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionXAGetTimeoutResponseMessage.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", timeoutSeconds=" + timeoutSeconds);
      return buff.toString();
```

### StringBufferReplaceableByString
`StringBuffer buff` can be replaced with 'String'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionContinuationMessage.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", body=" + Arrays.toString(body));
      buff.append(", continues=" + continues);
```

### StringBufferReplaceableByString
`StringBuffer buff` can be replaced with 'String'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionBindingQueryResponseMessage.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", exists=" + exists);
      buff.append(", queueNames=" + queueNames);
```

### StringBufferReplaceableByString
`StringBuilder info` can be replaced with 'String'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerInfo.java`
#### Snippet
```java
      ThreadMXBean threadMXBean = ManagementFactory.getThreadMXBean();

      StringBuilder info = new StringBuilder("\n**** Server Dump ****\n");
      info.append(String.format("date:            %s%n", new Date()));
      info.append(String.format("free memory:      %s%n", SizeFormatterUtil.sizeof(freeMemory)));
```

### StringBufferReplaceableByString
`StringBuilder filterBuilder` can be replaced with 'String'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/impl/ClusterConnectionBridge.java`
#### Snippet
```java
    */
   private String createPermissiveManagementNotificationToFilter() {
      StringBuilder filterBuilder = new StringBuilder(ManagementHelper.HDR_NOTIFICATION_TYPE).append(" = '")
              .append(CoreNotificationType.SESSION_CREATED).append("' OR (").append(ManagementHelper.HDR_ADDRESS)
              .append(" NOT LIKE '").append(managementNotificationAddress).append("%')");
```

### StringBufferReplaceableByString
`StringBuilder stringBuilder` can be replaced with 'String'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/routing/pools/DiscoveryService.java`
#### Snippet
```java
      @Override
      public String toString() {
         StringBuilder stringBuilder = new StringBuilder(Entry.class.getSimpleName());
         stringBuilder.append("(nodeID=" + nodeID);
         stringBuilder.append(", connector=" + connector);
```

### StringBufferReplaceableByString
`StringBuffer buf` can be replaced with 'String'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/ReplicationResponseMessageV2.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buf = new StringBuffer(super.getPacketString());
      buf.append(", synchronizationIsFinishedAcknowledgement=" + synchronizationIsFinishedAcknowledgement);
      return buf.toString();
```

### StringBufferReplaceableByString
`StringBuffer buff` can be replaced with 'String'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/QuorumVoteMessage.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", vote=" + vote);
      buff.append(", handler=" + handler);
```

### StringBufferReplaceableByString
`StringBuffer buf` can be replaced with 'String'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/ReplicationStartSyncMessage.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buf = new StringBuffer(super.getPacketString());
      buf.append(", synchronizationIsFinished=" + synchronizationIsFinished);
      buf.append(", dataType=" + dataType);
```

## RuleId[id=ReadWriteStringCanBeUsed]
### ReadWriteStringCanBeUsed
Can be replaced with 'Files.readString()'
in `artemis-maven-plugin/src/main/java/org/apache/activemq/artemis/maven/ArtemisCreatePlugin.java`
#### Snippet
```java
      Charset charset = StandardCharsets.UTF_8;

      String content = new String(Files.readAllBytes(original), charset);
      for (int i = 0; i + 1 < replacePairs.length; i += 2) {
         content = content.replaceAll(replacePairs[i], replacePairs[i + 1]);
```

## RuleId[id=NonShortCircuitBoolean]
### NonShortCircuitBoolean
Non-short-circuit boolean expression `added |= add(value)`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/LongHashSet.java`
#### Snippet
```java

      for (final Long value : coll) {
         added |= add(value);
      }

```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `removed |= remove(value)`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/LongHashSet.java`
#### Snippet
```java

      for (final Object value : coll) {
         removed |= remove(value);
      }

```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `windows | System.getProperty("os.name").toLowerCase().trim().startsWith("win")`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/InstallAbstract.java`
#### Snippet
```java

   public Object run(ActionContext context) throws Exception {
      IS_WINDOWS = windows | System.getProperty("os.name").toLowerCase().trim().startsWith("win");
      IS_CYGWIN = cygwin | IS_WINDOWS && "cygwin".equals(System.getenv("OSTYPE"));

```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `cygwin | IS_WINDOWS`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/InstallAbstract.java`
#### Snippet
```java
   public Object run(ActionContext context) throws Exception {
      IS_WINDOWS = windows | System.getProperty("os.name").toLowerCase().trim().startsWith("win");
      IS_CYGWIN = cygwin | IS_WINDOWS && "cygwin".equals(System.getenv("OSTYPE"));

      return null;
```

## RuleId[id=UnnecessaryReturn]
### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/SizeAwareMetric.java`
#### Snippet
```java
               }
            }
            return; // this return here is moot I know. I am keeping it here for consistence with the size portion
                    //  and in case eventually further checks are added on this method, this needs to be reviewed.
         }
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/SimpleWaitIOCallback.java`
#### Snippet
```java
      }

      return;
   }

```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Upgrade.java`
#### Snippet
```java
                   if (oldLine.trim().startsWith(jdkPrefix)) {
                      JVMArgumentParser.parseOriginalArgs(jdkPrefix, endOfLine, oldLine, keepArguments, currentArguments);
                      return;
                   } else {
                      for (String prefix : keepingPrefixes) {
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/PrintData.java`
#### Snippet
```java
      } catch (Exception e) {
         e.printStackTrace();
         return;
      }

```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalImpl.java`
#### Snippet
```java
      cleanupTmpFiles(".tmp");

      return;
   }

```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/handler/ProtonHandler.java`
#### Snippet
```java
      if (afterFlush == null) {
         afterFlush = runnable;
         return;
      } else {
         if (afterFlush != runnable) {
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/connect/mirror/AMQPMirrorControllerSource.java`
#### Snippet
```java
      if (!acks || ref.getQueue().isMirrorController()) {
         postACKInternalMessage(ref);
         return;
      }
   }
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `artemis-jdbc-store/src/main/java/org/apache/activemq/artemis/jdbc/store/file/JDBCSequentialFile.java`
#### Snippet
```java
         // file is already gone from a drop somewhere
         logger.debug("Expected error deleting Sequential File", e);
         return;
      }
   }
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/ChannelImpl.java`
#### Snippet
```java
         }

         return;
      } else {
         if (packet.isResponse()) {
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/impl/Validators.java`
#### Snippet
```java
      @Override
      public void validate(final String name, final Object value) {
         return;
      }
   };
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/impl/PagingStoreImpl.java`
#### Snippet
```java
      pgOper.pageTransaction.increment(listCtx.getNumberOfDurableQueues(), listCtx.getNumberOfNonDurableQueues());

      return;
   }

```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/impl/PagingStoreImpl.java`
#### Snippet
```java
            // and having both threads calling init. One of the calls should just
            // need to be ignored
            return;
         } else {
            running = true;
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/impl/PagingStoreImpl.java`
#### Snippet
```java
         }

         return;
      } else if (addressFullMessagePolicy == AddressFullMessagePolicy.PAGE) {
         if (size > 0) {
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/impl/PagingStoreImpl.java`
#### Snippet
```java
         }

         return;
      }
   }
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/cursor/impl/PageCursorProviderImpl.java`
#### Snippet
```java
      } catch (Exception ex) {
         ActiveMQServerLogger.LOGGER.problemCleaningPageAddress(pagingStore.getAddress(), ex);
         return;
      }

```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java
               // nothing to be done on this case, it's already active, we just ignore it and keep live as usual
               // TM 1.2 specs expects this as a regular scenario and it should just be ignored by TM Spec
               return;
            } else if (theTx.getState() != Transaction.State.SUSPENDED) {

```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/impl/BridgeImpl.java`
#### Snippet
```java

            keepConnecting = false;
            return;
         } catch (ActiveMQException e) {
            // the session was created while its server was starting, retry it:
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/impl/BridgeImpl.java`
#### Snippet
```java

               scheduleRetryConnectFixedTimeout(this.configuration.getRetryInterval());
               return;
            } else {
               ActiveMQServerLogger.LOGGER.errorConnectingBridgeRetry(this);
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ActiveMQServerImpl.java`
#### Snippet
```java

         if (limits.getMaxQueues() == -1) {
            return;
         } else if (limits.getMaxQueues() == 0 || getQueueCountForUser(username) >= limits.getMaxQueues()) {
            throw ActiveMQMessageBundle.BUNDLE.queueLimitReached(username, limits.getMaxQueues());
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ActiveMQServerImpl.java`
#### Snippet
```java

         if (limits.getMaxConnections() == -1) {
            return;
         } else if (limits.getMaxConnections() == 0 || getSessionCountForUser(username) >= limits.getMaxConnections()) {
            throw ActiveMQMessageBundle.BUNDLE.sessionLimitReached(username, limits.getMaxConnections());
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/JournalStorageManager.java`
#### Snippet
```java
         synchronized (this) {
            if (internalStop(ioCriticalError, sendFailover))
               return;
         }
      }
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/deployers/impl/FileConfigurationParser.java`
#### Snippet
```java
         ActiveMQServerLogger.LOGGER.discoveryGroupAlreadyDeployed(name);

         return;
      } else {
         mainConfig.getDiscoveryGroupConfigurations().put(name, config);
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/replication/ReplicationEndpoint.java`
#### Snippet
```java

      ActiveMQServerLogger.LOGGER.backupServerSynchronized(server, liveID);
      return;
   }

```

## RuleId[id=ManualMinMaxCalculation]
### ManualMinMaxCalculation
Can be replaced with 'Math.min()' call
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenWireProtocolManager.java`
#### Snippet
```java

   public void configureInactivityParams(OpenWireConnection connection, WireFormatInfo command) throws IOException {
      long inactivityDurationToUse = command.getMaxInactivityDuration() > this.maxInactivityDuration ? this.maxInactivityDuration : command.getMaxInactivityDuration();
      long inactivityDurationInitialDelayToUse = command.getMaxInactivityDurationInitalDelay() > this.maxInactivityDurationInitalDelay ? this.maxInactivityDurationInitalDelay : command.getMaxInactivityDurationInitalDelay();
      boolean useKeepAliveToUse = inactivityDurationToUse == 0L ? false : this.useKeepAlive;
```

### ManualMinMaxCalculation
Can be replaced with 'Math.min()' call
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenWireProtocolManager.java`
#### Snippet
```java
   public void configureInactivityParams(OpenWireConnection connection, WireFormatInfo command) throws IOException {
      long inactivityDurationToUse = command.getMaxInactivityDuration() > this.maxInactivityDuration ? this.maxInactivityDuration : command.getMaxInactivityDuration();
      long inactivityDurationInitialDelayToUse = command.getMaxInactivityDurationInitalDelay() > this.maxInactivityDurationInitalDelay ? this.maxInactivityDurationInitalDelay : command.getMaxInactivityDurationInitalDelay();
      boolean useKeepAliveToUse = inactivityDurationToUse == 0L ? false : this.useKeepAlive;
      connection.setUpTtl(inactivityDurationToUse, inactivityDurationInitialDelayToUse, useKeepAliveToUse);
```

### ManualMinMaxCalculation
Can be replaced with 'Math.min()' call
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenWireProtocolManager.java`
#### Snippet
```java
      final byte[] magic = info.getMagic();
      int remainingLen = array.length - start;
      int useLen = remainingLen > magic.length ? magic.length : remainingLen;
      useLen += start;
      // magic
```

### ManualMinMaxCalculation
Can be replaced with 'Math.min()' call
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTPublishManager.java`
#### Snippet
```java
       * is less than the subscription QoS then use it, otherwise use the subscription qos).
       */
      return subscriptionQoS < qos ? subscriptionQoS : qos;
   }
}
```

### ManualMinMaxCalculation
Can be replaced with 'Math.max()' call
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/qourum/QuorumManager.java`
#### Snippet
```java
   public void nodeUP(TopologyMember topologyMember, boolean last) {
      final int newClusterSize = clusterController.getDefaultClusterSize();
      maxClusterSize = newClusterSize > maxClusterSize ? newClusterSize : maxClusterSize;
      for (Quorum quorum : quorums.values()) {
         quorum.nodeUp(clusterController.getDefaultClusterTopology());
```

## RuleId[id=AbstractClassNeverImplemented]
### AbstractClassNeverImplemented
Abstract class `StringEscapeUtils` has no concrete subclass
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/StringEscapeUtils.java`
#### Snippet
```java
package org.apache.activemq.artemis.utils;

public abstract class StringEscapeUtils {

   /**
```

## RuleId[id=MismatchedStringBuilderQueryUpdate]
### MismatchedStringBuilderQueryUpdate
Contents of `StringBuffer buffer` are updated, but never queried
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/SpawnedVMSupport.java`
#### Snippet
```java
      try {
         if (!aliveProcess.isEmpty()) {
            StringBuffer buffer = new StringBuffer();
            for (Process alive : aliveProcess) {
               alive.destroyForcibly();
```

## RuleId[id=BoundedWildcard]
### BoundedWildcard
Can generalize to `? super String`
in `artemis-maven-plugin/src/main/java/org/apache/activemq/artemis/maven/ArtemisUpgradePlugin.java`
#### Snippet
```java
   @Parameter boolean useSystemOutput = getLog().isDebugEnabled();

   private void add(List<String> list, String... str) {
      for (String s : str) {
         list.add(s);
```

### BoundedWildcard
Can generalize to `? super String`
in `artemis-maven-plugin/src/main/java/org/apache/activemq/artemis/maven/ArtemisCreatePlugin.java`
#### Snippet
```java
   @Parameter boolean useSystemOutput = getLog().isDebugEnabled();

   private void add(List<String> list, String... str) {
      for (String s : str) {
         list.add(s);
```

### BoundedWildcard
Can generalize to `? super String`
in `artemis-log-annotation-processor/src/main/java/org/apache/activemq/artemis/logs/annotation/processor/LogAnnotationProcessor.java`
#### Snippet
```java
   }

   private static void tupples(String arg, char open, char close, Consumer<String> stringConsumer) {
      int openAt = -1;
      for (int i = 0; i < arg.length(); i++) {
```

### BoundedWildcard
Can generalize to `? super URL`
in `artemis-boot/src/main/java/org/apache/activemq/artemis/boot/Artemis.java`
#### Snippet
```java
   }

   private static void add(ArrayList<URL> urls, File file) {
      try {
         urls.add(file.toURI().toURL());
```

### BoundedWildcard
Can generalize to `? extends BaseInterceptor`
in `artemis-protocols/artemis-stomp-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/stomp/StompProtocolManagerFactory.java`
#### Snippet
```java

   @Override
   public List<StompFrameInterceptor> filterInterceptors(List<BaseInterceptor> interceptors) {
      return internalFilterInterceptors(StompFrameInterceptor.class, interceptors);
   }
```

### BoundedWildcard
Can generalize to `? extends T`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/json/JsonArray.java`
#### Snippet
```java
    * @throws ClassCastException if the {@code JsonArray} contains a value of wrong type
    */
   default <T, K extends JsonValue> List<T> getValuesAs(Function<K, T> func) {
      Stream<K> stream = (Stream<K>) stream();
      return stream.map(func).collect(Collectors.toList());
```

### BoundedWildcard
Can generalize to `? extends T`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Suppliers.java`
#### Snippet
```java
      transient T value;

      MemoizingSupplier(Supplier<T> delegate) {
         this.delegate = delegate;
      }
```

### BoundedWildcard
Can generalize to `? super T`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/pools/Pool.java`
#### Snippet
```java
   private final Supplier<T> supplier;

   public Pool(int maxSize, Consumer<T> cleaner, Supplier<T> supplier) {
      internalPool = createQueue(maxSize);
      this.cleaner = cleaner;
```

### BoundedWildcard
Can generalize to `? extends T`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/pools/Pool.java`
#### Snippet
```java
   private final Supplier<T> supplier;

   public Pool(int maxSize, Consumer<T> cleaner, Supplier<T> supplier) {
      internalPool = createQueue(maxSize);
      this.cleaner = cleaner;
```

### BoundedWildcard
Can generalize to `? super T`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/actors/Actor.java`
#### Snippet
```java
   private final ActorListener<T> listener;

   public Actor(Executor parent, ActorListener<T> listener) {
      super(parent);
      this.listener = listener;
```

### BoundedWildcard
Can generalize to `? extends E`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/RepeatableIteratorWrapper.java`
#### Snippet
```java
   private boolean repeat;

   public RepeatableIteratorWrapper(ResettableIterator<E> iterator) {
      this.iterator = iterator;
   }
```

### BoundedWildcard
Can generalize to `? super String`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/uri/BeanSupport.java`
#### Snippet
```java

   public static void setData(URI uri,
                              HashMap<String, Object> properties,
                              Set<String> allowableProperties,
                              Map<String, String> query,
```

### BoundedWildcard
Can generalize to `? super String`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/uri/BeanSupport.java`
#### Snippet
```java
                              Set<String> allowableProperties,
                              Map<String, String> query,
                              Map<String, Object> extraProps) {
      if (allowableProperties.contains("scheme")) {
         properties.put("scheme", "" + uri.getScheme());
```

### BoundedWildcard
Can generalize to `? extends E`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/ConcurrentAppendOnlyChunkedList.java`
#### Snippet
```java
   }

   private static <E> void drain(AtomicChunk<E> buffer, E[] elements, int elementNumber, int length) {
      for (int j = 0; j < length; j++) {
         final E e = pollElement(buffer, j);
```

### BoundedWildcard
Can generalize to `? extends V`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/ConcurrentLongHashMap.java`
#### Snippet
```java

      @SuppressWarnings("NonAtomicVolatileUpdate")
      V put(long key, V value, int keyHash, boolean onlyIfAbsent, LongFunction<V> valueProvider) {
         int bucket = keyHash;

```

### BoundedWildcard
Can generalize to `? super V`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/ConcurrentLongHashMap.java`
#### Snippet
```java
      }

      public void forEach(EntryProcessor<V> processor) {
         long stamp = tryOptimisticRead();

```

### BoundedWildcard
Can generalize to `? extends IOCallback`
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/io/AbstractSequentialFile.java`
#### Snippet
```java

      @Override
      public void flushBuffer(final ByteBuf byteBuf, final boolean requestedSync, final List<IOCallback> callbacks) {
         final int bytes = byteBuf.readableBytes();
         if (bytes > 0) {
```

### BoundedWildcard
Can generalize to `? super String`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/uri/URISupport.java`
#### Snippet
```java
   }

   private static void parseParameters(Map<String, String> rc,
                                       String[] parameters) throws UnsupportedEncodingException {
      for (String parameter : parameters) {
```

### BoundedWildcard
Can generalize to `? super String`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/uri/URISupport.java`
#### Snippet
```java
   }

   private static void parseParameters(Map<String, String> rc,
                                       String[] parameters) throws UnsupportedEncodingException {
      for (String parameter : parameters) {
```

### BoundedWildcard
Can generalize to `? extends IOCallback`
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/io/mapped/TimedSequentialFile.java`
#### Snippet
```java

      @Override
      public void flushBuffer(final ByteBuf byteBuf, final boolean requestedSync, final List<IOCallback> callbacks) {
         final int bytes = byteBuf.readableBytes();
         if (bytes > 0) {
```

### BoundedWildcard
Can generalize to `? extends IOCallback`
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/io/nio/NIOSequentialFile.java`
#### Snippet
```java

      @Override
      public void flushBuffer(ByteBuf byteBuf, boolean requestedSync, List<IOCallback> callbacks) {
         //maybe no need to perform any copy
         final int bytes = byteBuf.readableBytes();
```

### BoundedWildcard
Can generalize to `? super SimpleString`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/TypedProperties.java`
#### Snippet
```java
   }

   public synchronized void forEachKey(Consumer<SimpleString> action) {
      if (properties != null) {
         properties.keySet().forEach(action::accept);
```

### BoundedWildcard
Can generalize to `? super SimpleString`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/TypedProperties.java`
#### Snippet
```java
   }

   public synchronized void forEach(BiConsumer<SimpleString, Object> action) {
      if (properties != null) {
         properties.forEach((k, v) -> action.accept(k, v.getValue()));
```

### BoundedWildcard
Can generalize to `? super SimpleString`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/TypedProperties.java`
#### Snippet
```java
   }

   private synchronized void forEachInternal(BiConsumer<SimpleString, PropertyValue> action) {
      if (properties != null) {
         properties.forEach(action::accept);
```

### BoundedWildcard
Can generalize to `? super PropertyValue`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/TypedProperties.java`
#### Snippet
```java
   }

   private synchronized void forEachInternal(BiConsumer<SimpleString, PropertyValue> action) {
      if (properties != null) {
         properties.forEach(action::accept);
```

### BoundedWildcard
Can generalize to `? super SimpleString`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/TypedProperties.java`
#### Snippet
```java
   }

   public TypedProperties(Predicate<SimpleString> internalPropertyPredicate) {
      this.internalPropertyPredicate = internalPropertyPredicate;
   }
```

### BoundedWildcard
Can generalize to `? extends JournalFile`
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalFilesRepository.java`
#### Snippet
```java
   }

   public void calculateNextfileID(final List<JournalFile> files) {

      for (JournalFile file : files) {
```

### BoundedWildcard
Can generalize to `? super E`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/LinkedListImpl.java`
#### Snippet
```java

   @Override
   public void forEach(Consumer<E> consumer) {
      try (LinkedListIterator<E> iter = iterator()) {
         while (iter.hasNext()) {
```

### BoundedWildcard
Can generalize to `? super E`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/LinkedListImpl.java`
#### Snippet
```java
   }

   public LinkedListImpl(Comparator<E> comparator, NodeStore<E> supplier) {
      iters = createIteratorArray(INITIAL_ITERATOR_ARRAY_SIZE);
      this.comparator = comparator;
```

### BoundedWildcard
Can generalize to `? extends JournalFile`
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/AbstractJournalUpdateTask.java`
#### Snippet
```java

   public static SequentialFile writeControlFile(final SequentialFileFactory fileFactory,
                                                 final List<JournalFile> files,
                                                 final List<JournalFile> newFiles,
                                                 final List<Pair<String, String>> renames) throws Exception {
```

### BoundedWildcard
Can generalize to `? extends JournalFile`
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/AbstractJournalUpdateTask.java`
#### Snippet
```java
   public static SequentialFile writeControlFile(final SequentialFileFactory fileFactory,
                                                 final List<JournalFile> files,
                                                 final List<JournalFile> newFiles,
                                                 final List<Pair<String, String>> renames) throws Exception {

```

### BoundedWildcard
Can generalize to `? super String`
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/AbstractJournalUpdateTask.java`
#### Snippet
```java

   static SequentialFile readControlFile(final SequentialFileFactory fileFactory,
                                                final List<String> dataFiles,
                                                final List<String> newFiles,
                                                final List<Pair<String, String>> renameFile,
```

### BoundedWildcard
Can generalize to `? super String`
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/AbstractJournalUpdateTask.java`
#### Snippet
```java
   static SequentialFile readControlFile(final SequentialFileFactory fileFactory,
                                                final List<String> dataFiles,
                                                final List<String> newFiles,
                                                final List<Pair<String, String>> renameFile,
                                                final AtomicReference<ByteBuffer> wholeFileBufferRef) throws Exception {
```

### BoundedWildcard
Can generalize to `? super Pair`
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/AbstractJournalUpdateTask.java`
#### Snippet
```java
                                                final List<String> dataFiles,
                                                final List<String> newFiles,
                                                final List<Pair<String, String>> renameFile,
                                                final AtomicReference<ByteBuffer> wholeFileBufferRef) throws Exception {
      SequentialFile controlFile = fileFactory.createSequentialFile(AbstractJournalUpdateTask.FILE_COMPACT_CONTROL);
```

### BoundedWildcard
Can generalize to `? super String`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/OptionsUtil.java`
#### Snippet
```java
public class OptionsUtil {

   private static void findAllOptions(Set<String> options, Class<? extends Action> command) {
      for (Field field  : command.getDeclaredFields()) {
         if (field.isAnnotationPresent(Option.class)) {
```

### BoundedWildcard
Can generalize to `? super ClientMessage`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/AbstractAction.java`
#### Snippet
```java
public abstract class AbstractAction extends ConnectionAbstract {

   public void performCoreManagement(ManagementCallback<ClientMessage> cb) throws Exception {

      try (ActiveMQConnectionFactory factory = createCoreConnectionFactory();
```

### BoundedWildcard
Can generalize to `? super String`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Upgrade.java`
#### Snippet
```java
   }

   private void doUpgrade(ActionContext context, File tmpFile, File targetFile, File bkpFile, Consumer<String> originalConsumer, Function<String, String> targetFunction) throws Exception {
      context.out.println("Copying " + targetFile + " to " + bkpFile);
      Files.copy(targetFile.toPath(), bkpFile.toPath(), StandardCopyOption.REPLACE_EXISTING);
```

### BoundedWildcard
Can generalize to `? super String`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Upgrade.java`
#### Snippet
```java
   }

   private void doUpgrade(ActionContext context, File tmpFile, File targetFile, File bkpFile, Consumer<String> originalConsumer, Function<String, String> targetFunction) throws Exception {
      context.out.println("Copying " + targetFile + " to " + bkpFile);
      Files.copy(targetFile.toPath(), bkpFile.toPath(), StandardCopyOption.REPLACE_EXISTING);
```

### BoundedWildcard
Can generalize to `? super Long`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/journal/DecodeJournal.java`
#### Snippet
```java
   }

   private static AtomicInteger getCounter(final Long txID, final Map<Long, AtomicInteger> txCounters) {
      AtomicInteger counter = txCounters.get(txID);
      if (counter == null) {
```

### BoundedWildcard
Can generalize to `? super String`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/xml/XMLMessageImporter.java`
#### Snippet
```java
   }

   private void processMessageQueues(ArrayList<String> queues) {
      for (int i = 0; i < reader.getAttributeCount(); i++) {
         if (XmlDataConstants.QUEUE_NAME.equals(reader.getAttributeLocalName(i))) {
```

### BoundedWildcard
Can generalize to `? super String`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Create.java`
#### Snippet
```java
    * It will create the address and queue configurations
    */
   private void applyAddressesAndQueues(HashMap<String, String> filters) {
      StringWriter writer = new StringWriter();
      PrintWriter printWriter = new PrintWriter(writer);
```

### BoundedWildcard
Can generalize to `? extends RecordInfo`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/xml/XmlDataExporter.java`
#### Snippet
```java
    * @param acks the list of ack records we got from the journal
    */
   private void removeAcked(ArrayList<RecordInfo> acks) {
      for (RecordInfo info : acks) {
         AckDescribe ack = (AckDescribe) DescribeJournal.newObjectEncoding(info, null);
```

### BoundedWildcard
Can generalize to `? super String`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Create.java`
#### Snippet
```java
    * It will create the address and queue configurations
    */
   private void applyAddressesAndQueues(HashMap<String, String> filters) {
      StringWriter writer = new StringWriter();
      PrintWriter printWriter = new PrintWriter(writer);
```

### BoundedWildcard
Can generalize to `? super String`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Create.java`
#### Snippet
```java
   }

   protected static void addScriptFilters(HashMap<String, String> filters,
                          File home,
                          File directory,
```

### BoundedWildcard
Can generalize to `? super String`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Create.java`
#### Snippet
```java
   }

   protected static void addScriptFilters(HashMap<String, String> filters,
                          File home,
                          File directory,
```

### BoundedWildcard
Can generalize to `? super String`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Create.java`
#### Snippet
```java
   }

   private void performAutoTune(HashMap<String, String> filters, JournalType journalType, File dataFolder) {
      if (noAutoTune) {
         filters.put("${journal-buffer.settings}", "");
```

### BoundedWildcard
Can generalize to `? super String`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Create.java`
#### Snippet
```java
   }

   private void performAutoTune(HashMap<String, String> filters, JournalType journalType, File dataFolder) {
      if (noAutoTune) {
         filters.put("${journal-buffer.settings}", "");
```

### BoundedWildcard
Can generalize to `? extends RecordInfo`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/PrintData.java`
#### Snippet
```java
    * Calculate the acks on the page system
    */
   private static PageCursorsInfo calculateCursorsInfo(List<RecordInfo> records) throws Exception {

      PageCursorsInfo cursorInfo = new PageCursorsInfo();
```

### BoundedWildcard
Can generalize to `? super String`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/util/JVMArgumentParser.java`
#### Snippet
```java
public class JVMArgumentParser {

   public static void parseOriginalArgs(String prefix, String endOfLine, String originalLine, String[] keepingPrefixes, Map<String, String> originalArgs) {
      originalLine = originalLine.trim();
      String line = originalLine.substring(prefix.length(), originalLine.length() - endOfLine.length());
```

### BoundedWildcard
Can generalize to `? super String`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/util/JVMArgumentParser.java`
#### Snippet
```java
public class JVMArgumentParser {

   public static void parseOriginalArgs(String prefix, String endOfLine, String originalLine, String[] keepingPrefixes, Map<String, String> originalArgs) {
      originalLine = originalLine.trim();
      String line = originalLine.substring(prefix.length(), originalLine.length() - endOfLine.length());
```

### BoundedWildcard
Can generalize to `? extends ObjLongPair`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/perf/ProducerBenchmark.java`
#### Snippet
```java
   }

   public static long expectedTotalMessageCountToReceive(final Stream<ObjLongPair<Destination>> messageSentPerDestination,
                                                         final int sharedSubscriptions,
                                                         final int consumersPerDestination) {
```

### BoundedWildcard
Can generalize to `? extends ActiveMQComponent`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/integration/FileBroker.java`
#### Snippet
```java
   * will need impproving if we get more.
   * */
   private ArrayList<ActiveMQComponent> getComponentsByStartOrder(Map<String, ActiveMQComponent> components) {
      ArrayList<ActiveMQComponent> activeMQComponents = new ArrayList<>();
      ActiveMQComponent jmsComponent = components.get("jms");
```

### BoundedWildcard
Can generalize to `? super String`
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/server/config/impl/FileJMSConfiguration.java`
#### Snippet
```java
                            MBeanServer mBeanServer,
                            Map<String, Deployable> deployables,
                            Map<String, ActiveMQComponent> components, ActivateCallback activateCallback) throws Exception {
      ActiveMQServerImpl server = (ActiveMQServerImpl) components.get("core");
      components.put(CONFIGURATION_SCHEMA_ROOT_ELEMENT, new JMSServerManagerImpl(server, this));
```

### BoundedWildcard
Can generalize to `? super ActiveMQComponent`
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/server/config/impl/FileJMSConfiguration.java`
#### Snippet
```java
                            MBeanServer mBeanServer,
                            Map<String, Deployable> deployables,
                            Map<String, ActiveMQComponent> components, ActivateCallback activateCallback) throws Exception {
      ActiveMQServerImpl server = (ActiveMQServerImpl) components.get("core");
      components.put(CONFIGURATION_SCHEMA_ROOT_ELEMENT, new JMSServerManagerImpl(server, this));
```

### BoundedWildcard
Can generalize to `? extends BaseInterceptor`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenWireProtocolManagerFactory.java`
#### Snippet
```java

   @Override
   public List<OpenWireInterceptor> filterInterceptors(List<BaseInterceptor> interceptors) {
      return internalFilterInterceptors(OpenWireInterceptor.class, interceptors);
   }
```

### BoundedWildcard
Can generalize to `? extends BaseInterceptor`
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTProtocolManagerFactory.java`
#### Snippet
```java

   @Override
   public List<MQTTInterceptor> filterInterceptors(List<BaseInterceptor> interceptors) {
      return internalFilterInterceptors(MQTTInterceptor.class, interceptors);
   }
```

### BoundedWildcard
Can generalize to `? super ActiveMQComponent`
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTProtocolManagerFactory.java`
#### Snippet
```java

   @Override
   public void loadProtocolServices(ActiveMQServer server, List<ActiveMQComponent> services) {
      services.add(new MQTTPeriodicTasks(server, server.getScheduledPool()));
   }
```

### BoundedWildcard
Can generalize to `? extends T`
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTUtil.java`
#### Snippet
```java
   }

   public static <T> T getProperty(Class<T> type, MqttProperties properties, MqttPropertyType propertyName, T defaultReturnValue) {
      if (properties != null) {
         MqttProperties.MqttProperty o = properties.getProperty(propertyName.value());
```

### BoundedWildcard
Can generalize to `? extends SessionId`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenWireConnection.java`
#### Snippet
```java
   }

   public void addSessions(Set<SessionId> sessionSet) {
      for (SessionId sid : sessionSet) {
         addSession(getState().getSessionState(sid).getInfo());
```

### BoundedWildcard
Can generalize to `? extends List`
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/server/impl/JMSServerManagerImpl.java`
#### Snippet
```java

   private synchronized boolean removeFromBindings(final Map<String, ?> keys,
                                                   final Map<String, List<String>> bindingsMap,
                                                   final String name) throws Exception {
      checkInitialised();
```

### BoundedWildcard
Can generalize to `? extends List`
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/server/impl/JMSServerManagerImpl.java`
#### Snippet
```java
    * @param param
    */
   private void unbindBindings(Map<String, List<String>> param) {
      if (registry != null) {
         for (List<String> elementList : param.values()) {
```

### BoundedWildcard
Can generalize to `? extends List`
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/server/impl/JMSServerManagerImpl.java`
#### Snippet
```java
   }

   private String[] getBindingsList(final Map<String, List<String>> map, final String name) {
      List<String> result = map.get(name);
      if (result == null) {
```

### BoundedWildcard
Can generalize to `? extends List`
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/server/impl/JMSServerManagerImpl.java`
#### Snippet
```java
   }

   private synchronized boolean removeFromBindings(final Map<String, List<String>> bindingsMap,
                                                   final String name,
                                                   final String bindings) throws Exception {
```

### BoundedWildcard
Can generalize to `? super String`
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/server/impl/JMSServerManagerImpl.java`
#### Snippet
```java
   }

   private void addToBindings(Map<String, List<String>> map, String name, String... bindings) {
      List<String> list = map.get(name);
      if (list == null) {
```

### BoundedWildcard
Can generalize to `? super String`
in `artemis-quorum-ri/src/main/java/org/apache/activemq/artemis/quorum/file/FileDistributedLock.java`
#### Snippet
```java
   private final FileChannel channel;

   FileDistributedLock(Consumer<String> onClosedLock, File locksFolder, String lockId) throws IOException {
      this.onClosedLock = onClosedLock;
      this.lockId = lockId;
```

### BoundedWildcard
Can generalize to `? extends JournalFile`
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalImpl.java`
#### Snippet
```java
    * being protected as testcases can override this method
    */
   protected void renameFiles(final List<JournalFile> oldFiles, final List<JournalFile> newFiles) throws Exception {

      // addFreeFiles has to be called through filesExecutor, or the fileID on the orderedFiles may end up in a wrong
```

### BoundedWildcard
Can generalize to `? extends JournalFile`
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalImpl.java`
#### Snippet
```java
    * being protected as testcases can override this method
    */
   protected void renameFiles(final List<JournalFile> oldFiles, final List<JournalFile> newFiles) throws Exception {

      // addFreeFiles has to be called through filesExecutor, or the fileID on the orderedFiles may end up in a wrong
```

### BoundedWildcard
Can generalize to `? super RecordInfo`
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalImpl.java`
#### Snippet
```java

   @Override
   public JournalLoadInformation load(List<RecordInfo> committedRecords,
                                      List<PreparedTransactionInfo> preparedTransactions,
                                      TransactionFailureCallback transactionFailure,
```

### BoundedWildcard
Can generalize to `? super PreparedTransactionInfo`
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalImpl.java`
#### Snippet
```java
   @Override
   public synchronized JournalLoadInformation load(final SparseArrayLinkedList<RecordInfo> committedRecords,
                                                   final List<PreparedTransactionInfo> preparedTransactions,
                                                   final TransactionFailureCallback failureCallback,
                                                   final boolean fixBadTX) throws Exception {
```

### BoundedWildcard
Can generalize to `? super PrimitiveId`
in `artemis-quorum-ri/src/main/java/org/apache/activemq/artemis/quorum/zookeeper/CuratorDistributedPrimitiveManager.java`
#### Snippet
```java

   private synchronized <T extends CuratorDistributedPrimitive> T getPrimitive(PrimitiveId id,
                                                                               Function<PrimitiveId, ? extends T> primitiveFactory) {
      checkHandlingEvents();
      requireNonNull(id);
```

### BoundedWildcard
Can generalize to `? super String`
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jndi/ActiveMQInitialContextFactory.java`
#### Snippet
```java
   }

   protected void createTopics(Map<String, Object> data, Hashtable<?, ?> environment) {
      for (Map.Entry<?, ?> entry : environment.entrySet()) {
         String key = entry.getKey().toString();
```

### BoundedWildcard
Can generalize to `? super String`
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jndi/ActiveMQInitialContextFactory.java`
#### Snippet
```java
   }

   protected void createQueues(Map<String, Object> data, Hashtable<?, ?> environment) {
      for (Map.Entry<?, ?> entry : environment.entrySet()) {
         String key = entry.getKey().toString();
```

### BoundedWildcard
Can generalize to `? extends BaseInterceptor`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/ProtonProtocolManagerFactory.java`
#### Snippet
```java

   @Override
   public List<AmqpInterceptor> filterInterceptors(List<BaseInterceptor> interceptors) {
      return internalFilterInterceptors(AmqpInterceptor.class, interceptors);
   }
```

### BoundedWildcard
Can generalize to `? super ActiveMQComponent`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/ProtonProtocolManagerFactory.java`
#### Snippet
```java
    *  As the broker may choose to not load the AMQP Protocol */
   @Override
   public void loadProtocolServices(ActiveMQServer server, List<ActiveMQComponent> services) {
      List<AMQPBrokerConnectConfiguration> amqpServicesConfiguration = server.getConfiguration().getAMQPConnection();
      if (amqpServicesConfiguration != null && amqpServicesConfiguration.size() > 0) {
```

### BoundedWildcard
Can generalize to `? extends EventHandler`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/client/AMQPClientConnectionFactory.java`
#### Snippet
```java
   }

   public ActiveMQProtonRemotingConnection createConnection(ProtonProtocolManager protocolManager, Connection connection, Optional<EventHandler> eventHandler, ClientSASLFactory clientSASLFactory) {
      AMQPConnectionCallback connectionCallback = new AMQPConnectionCallback(protocolManager, connection, server.getExecutorFactory().getExecutor(), server);

```

### BoundedWildcard
Can generalize to `? super Symbol`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/converter/coreWrapper/CoreObjectMessageWrapper.java`
#### Snippet
```java

   @Override
   public Section createAMQPSection(Map<Symbol, Object> maMap, Properties properties) throws ConversionException {
      properties.setContentType(AMQPMessageSupport.SERIALIZED_JAVA_OBJECT_CONTENT_TYPE);
      maMap.put(AMQPMessageSupport.JMS_MSG_TYPE, AMQPMessageSupport.JMS_OBJECT_MESSAGE);
```

### BoundedWildcard
Can generalize to `? super Symbol`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/converter/coreWrapper/CoreBytesMessageWrapper.java`
#### Snippet
```java

   @Override
   public Section createAMQPSection(Map<Symbol, Object> maMap, Properties properties) {
      short orignalEncoding = getOrignalEncoding();
      Binary payload = getBinaryFromMessageBody(this);
```

### BoundedWildcard
Can generalize to `? super Symbol`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/converter/coreWrapper/CoreTextMessageWrapper.java`
#### Snippet
```java

   @Override
   public Section createAMQPSection(Map<Symbol, Object> maMap, Properties properties) {
      Section body = null;

```

### BoundedWildcard
Can generalize to `? super Symbol`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/converter/coreWrapper/CoreStreamMessageWrapper.java`
#### Snippet
```java

   @Override
   public Section createAMQPSection(Map<Symbol, Object> maMap, Properties properties) throws ConversionException {
      maMap.put(AMQPMessageSupport.JMS_MSG_TYPE, AMQPMessageSupport.JMS_STREAM_MESSAGE);
      ArrayList<Object> list = new ArrayList<>();
```

### BoundedWildcard
Can generalize to `? super Symbol`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/converter/coreWrapper/CoreMessageWrapper.java`
#### Snippet
```java

   // returns the AMQP Section for the message type
   public Section createAMQPSection(Map<Symbol, Object> maMap, Properties properties) throws ConversionException {
      maMap.put(AMQPMessageSupport.JMS_MSG_TYPE, AMQPMessageSupport.JMS_MESSAGE);
      // If this is not an AMQP message that was converted then the original encoding
```

### BoundedWildcard
Can generalize to `? super Symbol`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/converter/coreWrapper/CoreMapMessageWrapper.java`
#### Snippet
```java

   @Override
   public Section createAMQPSection(Map<Symbol, Object> maMap, Properties properties) {
      maMap.put(AMQPMessageSupport.JMS_MSG_TYPE, AMQPMessageSupport.JMS_MAP_MESSAGE);
      return new AmqpValue(getMapFromMessageBody(this));
```

### BoundedWildcard
Can generalize to `? super Sender`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/connect/AMQPBrokerConnection.java`
#### Snippet
```java
   private void connectSender(Queue queue,
                              String targetName,
                              java.util.function.Consumer<Sender> senderConsumer,
                              java.util.function.Consumer<? super MessageReference> beforeDeliver,
                              String brokerID,
```

### BoundedWildcard
Can generalize to `? super RecordInfo`
in `artemis-jdbc-store/src/main/java/org/apache/activemq/artemis/jdbc/store/journal/JDBCJournalLoaderCallback.java`
#### Snippet
```java
   private long maxId = -1;

   JDBCJournalLoaderCallback(final List<RecordInfo> committedRecords,
                             final List<PreparedTransactionInfo> preparedTransactions,
                             final TransactionFailureCallback failureCallback,
```

### BoundedWildcard
Can generalize to `? super PreparedTransactionInfo`
in `artemis-jdbc-store/src/main/java/org/apache/activemq/artemis/jdbc/store/journal/JDBCJournalLoaderCallback.java`
#### Snippet
```java

   JDBCJournalLoaderCallback(final List<RecordInfo> committedRecords,
                             final List<PreparedTransactionInfo> preparedTransactions,
                             final TransactionFailureCallback failureCallback,
                             final boolean fixBadTX) {
```

### BoundedWildcard
Can generalize to `? super String`
in `artemis-jdbc-store/src/main/java/org/apache/activemq/artemis/jdbc/store/sql/PropertySQLProvider.java`
#### Snippet
```java
      private final Function<String, String> transform;

      LetterCase(Function<String, String> transform) {
         this.transform = transform;
      }
```

### BoundedWildcard
Can generalize to `? extends File`
in `artemis-web/src/main/java/org/apache/activemq/artemis/component/WebTmpCleaner.java`
#### Snippet
```java
   }

   public static Process cleanupTmpFiles(File libFolder, List<File> temporaryFiles, boolean invm) throws Exception {
      ArrayList<String> files = new ArrayList<>(temporaryFiles.size());
      for (File f : temporaryFiles) {
```

### BoundedWildcard
Can generalize to `? extends ActiveMQComponent`
in `artemis-server-osgi/src/main/java/org/apache/activemq/artemis/osgi/OsgiBroker.java`
#### Snippet
```java
    * we get more.
    */
   public ArrayList<ActiveMQComponent> getComponentsByStartOrder(Map<String, ActiveMQComponent> components) {
      ArrayList<ActiveMQComponent> activeMQComponents = new ArrayList<>();
      ActiveMQComponent jmsComponent = components.get("jms");
```

### BoundedWildcard
Can generalize to `? super String`
in `artemis-server-osgi/src/main/java/org/apache/activemq/artemis/osgi/OsgiBroker.java`
#### Snippet
```java
   }

   private void getInterfaces(Class<?> clazz, Set<String> interfaces) {
      for (Class<?> itf : clazz.getInterfaces()) {
         if (interfaces.add(itf.getName())) {
```

### BoundedWildcard
Can generalize to `? extends TransportConfiguration`
in `artemis-server-osgi/src/main/java/org/apache/activemq/artemis/osgi/OsgiBroker.java`
#### Snippet
```java
   }

   private String[] getRequiredProtocols(Set<TransportConfiguration> acceptors) {
      ArrayList<String> protocols = new ArrayList<>();
      for (TransportConfiguration acceptor : acceptors) {
```

### BoundedWildcard
Can generalize to `? extends BindingDTO`
in `artemis-web/src/main/java/org/apache/activemq/artemis/component/WebServerComponent.java`
#### Snippet
```java
   }

   private void printStatus(List<BindingDTO> bindings) {
      ActiveMQWebLogger.LOGGER.webserverStarted(bindings
                                                   .stream()
```

### BoundedWildcard
Can generalize to `? super RecordInfo`
in `artemis-jdbc-store/src/main/java/org/apache/activemq/artemis/jdbc/store/journal/JDBCJournalImpl.java`
#### Snippet
```java

   @Override
   public synchronized JournalLoadInformation load(final SparseArrayLinkedList<RecordInfo> committedRecords,
                                                   final List<PreparedTransactionInfo> preparedTransactions,
                                                   final TransactionFailureCallback failureCallback,
```

### BoundedWildcard
Can generalize to `? extends JDBCJournalRecord`
in `artemis-jdbc-store/src/main/java/org/apache/activemq/artemis/jdbc/store/journal/JDBCJournalImpl.java`
#### Snippet
```java
   }

   private void executeCallbacks(final List<JDBCJournalRecord> records, final boolean success) {
      Runnable r = new Runnable() {
         @Override
```

### BoundedWildcard
Can generalize to `? extends CloseListener`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/spi/core/protocol/AbstractRemotingConnection.java`
#### Snippet
```java

   @Override
   public void setCloseListeners(List<CloseListener> listeners) {
      closeListeners.clear();

```

### BoundedWildcard
Can generalize to `? extends FailureListener`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/spi/core/protocol/AbstractRemotingConnection.java`
#### Snippet
```java

   @Override
   public void setFailureListeners(final List<FailureListener> listeners) {
      failureListeners.clear();

```

### BoundedWildcard
Can generalize to `? extends Matcher`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/config/federation/FederationAddressPolicyConfiguration.java`
#### Snippet
```java
   }

   private void encodeMatchers(final ActiveMQBuffer buffer, final Set<Matcher> matchers) {
      buffer.writeInt(matchers == null ? 0 : matchers.size());
      if (matchers != null) {
```

### BoundedWildcard
Can generalize to `? super Matcher`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/config/federation/FederationAddressPolicyConfiguration.java`
#### Snippet
```java
   }

   private void decodeMatchers(final ActiveMQBuffer buffer, final Set<Matcher> matchers) {
      final int size = buffer.readInt();

```

### BoundedWildcard
Can generalize to `? super Matcher`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/config/federation/FederationQueuePolicyConfiguration.java`
#### Snippet
```java
   }

   private void decodeMatchers(final ActiveMQBuffer buffer, final Set<Matcher> matchers) {
      final int size = buffer.readInt();

```

### BoundedWildcard
Can generalize to `? extends Matcher`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/config/federation/FederationQueuePolicyConfiguration.java`
#### Snippet
```java
   }

   private void encodeMatchers(final ActiveMQBuffer buffer, final Set<Matcher> matchers) {
      buffer.writeInt(matchers == null ? 0 : matchers.size());
      if (matchers != null) {
```

### BoundedWildcard
Can generalize to `? extends DiscoveryEntry`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ServerLocatorImpl.java`
#### Snippet
```java

   @Override
   public synchronized void connectorsChanged(List<DiscoveryEntry> newConnectors) {
      if (receivedTopology) {
         return;
```

### BoundedWildcard
Can generalize to `? super Interceptor`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ServerLocatorImpl.java`
#### Snippet
```java
   }

   private void feedInterceptors(final List<Interceptor> interceptors, final String interceptorList) {
      interceptors.clear();

```

### BoundedWildcard
Can generalize to `? extends TransportConfiguration`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ServerLocatorImpl.java`
#### Snippet
```java
                            final String backupGroupName,
                            final String scaleDownGroupName,
                            final Pair<TransportConfiguration, TransportConfiguration> connectorPair,
                            final boolean last) {
      if (logger.isTraceEnabled()) {
```

### BoundedWildcard
Can generalize to `? extends TransportConfiguration`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ServerLocatorImpl.java`
#### Snippet
```java
                            final String backupGroupName,
                            final String scaleDownGroupName,
                            final Pair<TransportConfiguration, TransportConfiguration> connectorPair,
                            final boolean last) {
      if (logger.isTraceEnabled()) {
```

### BoundedWildcard
Can generalize to `? extends Interceptor`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ServerLocatorImpl.java`
#### Snippet
```java
   }

   private String fromInterceptors(final List<Interceptor> interceptors) {
      StringBuffer buffer = new StringBuffer();
      boolean first = true;
```

### BoundedWildcard
Can generalize to `? extends Interceptor`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/ChannelImpl.java`
#### Snippet
```java
    */
   public static String invokeInterceptors(final Packet packet,
                                           final List<Interceptor> interceptors,
                                           final RemotingConnection connection) {
      if (interceptors != null && !interceptors.isEmpty()) {
```

### BoundedWildcard
Can generalize to `? super ThreadFactory`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/SharedEventLoopGroup.java`
#### Snippet
```java
   }

   public static synchronized SharedEventLoopGroup getInstance(Function<ThreadFactory, EventLoopGroup> eventLoopGroupSupplier) {
      if (instance != null) {
         ScheduledFuture<?> f = instance.shutdown.getAndSet(null);
```

### BoundedWildcard
Can generalize to `? extends EventLoopGroup`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/SharedEventLoopGroup.java`
#### Snippet
```java
   }

   public static synchronized SharedEventLoopGroup getInstance(Function<ThreadFactory, EventLoopGroup> eventLoopGroupSupplier) {
      if (instance != null) {
         ScheduledFuture<?> f = instance.shutdown.getAndSet(null);
```

### BoundedWildcard
Can generalize to `? extends ClientSessionInternal`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ClientSessionFactoryImpl.java`
#### Snippet
```java
    * Re-attach sessions all pre-existing sessions to the new remoting connection
    */
   private boolean reconnectSessions(final Set<ClientSessionInternal> sessionsToFailover,
                                     final RemotingConnection oldConnection,
                                     final int reconnectAttempts,
```

### BoundedWildcard
Can generalize to `? extends I`
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/protocol/AbstractProtocolManager.java`
#### Snippet
```java
   private String securityDomain;

   protected String invokeInterceptors(final List<I> interceptors, final P message, final C connection) {
      if (interceptors != null && !interceptors.isEmpty()) {
         for (I interceptor : interceptors) {
```

### BoundedWildcard
Can generalize to `? super ChannelFuture`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/NettyConnector.java`
#### Snippet
```java
   }

   public NettyConnection createConnection(Consumer<ChannelFuture> onConnect, String host, int port) {
      InetSocketAddress remoteDestination;
      if (proxyEnabled && proxyRemoteDNS) {
```

### BoundedWildcard
Can generalize to `? super String`
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/security/jaas/LDAPLoginModule.java`
#### Snippet
```java
   }

   private String resolveDN(String username, List<String> roles) throws FailedLoginException {
      String dn = null;

```

### BoundedWildcard
Can generalize to `? super String`
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/security/jaas/LDAPLoginModule.java`
#### Snippet
```java
   }

   private void addRoleAttribute(SearchResult searchResult, List<String> roles) throws NamingException {
      if (isRoleAttributeSet) {
         Attribute roleAttribute = searchResult.getAttributes().get(roleAttributeName);
```

### BoundedWildcard
Can generalize to `? extends LDAPLoginProperty`
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/security/jaas/LDAPLoginModule.java`
#### Snippet
```java
   }

   protected void extendInitialEnvironment(Set<LDAPLoginProperty> moduleConfig, Hashtable<String, String> initialContextEnv) {
      // sub-classes may override the method if the default implementation is not sufficient:
      // add all non-module configs to initial DirContext environment to support passing
```

### BoundedWildcard
Can generalize to `? super String`
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/security/jaas/LDAPLoginModule.java`
#### Snippet
```java
   }

   protected void extendInitialEnvironment(Set<LDAPLoginProperty> moduleConfig, Hashtable<String, String> initialContextEnv) {
      // sub-classes may override the method if the default implementation is not sufficient:
      // add all non-module configs to initial DirContext environment to support passing
```

### BoundedWildcard
Can generalize to `? super String`
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/security/jaas/LDAPLoginModule.java`
#### Snippet
```java
   }

   protected void extendInitialEnvironment(Set<LDAPLoginProperty> moduleConfig, Hashtable<String, String> initialContextEnv) {
      // sub-classes may override the method if the default implementation is not sufficient:
      // add all non-module configs to initial DirContext environment to support passing
```

### BoundedWildcard
Can generalize to `? extends CoreQueueConfiguration`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/CoreAddressConfiguration.java`
#### Snippet
```java

   @Deprecated
   public CoreAddressConfiguration setQueueConfigurations(List<CoreQueueConfiguration> coreQueueConfigurations) {
      for (CoreQueueConfiguration coreQueueConfiguration : coreQueueConfigurations) {
         queueConfigurations.add(coreQueueConfiguration.toQueueConfiguration());
```

### BoundedWildcard
Can generalize to `? super String`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/impl/FileConfiguration.java`
#### Snippet
```java
                            MBeanServer mBeanServer,
                            Map<String, Deployable> deployables,
                            Map<String, ActiveMQComponent> components, ActivateCallback activateCallback) {
      ActiveMQServerImpl activeMQServer = new ActiveMQServerImpl(this, mBeanServer, securityManager);
      if (activateCallback != null) {
```

### BoundedWildcard
Can generalize to `? super ActiveMQComponent`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/impl/FileConfiguration.java`
#### Snippet
```java
                            MBeanServer mBeanServer,
                            Map<String, Deployable> deployables,
                            Map<String, ActiveMQComponent> components, ActivateCallback activateCallback) {
      ActiveMQServerImpl activeMQServer = new ActiveMQServerImpl(this, mBeanServer, securityManager);
      if (activateCallback != null) {
```

### BoundedWildcard
Can generalize to `? extends Role`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/impl/RoleSet.java`
#### Snippet
```java
   }

   public RoleSet(String key, Set<Role> value) {
      setName(key);
      if (value != null) {
```

### BoundedWildcard
Can generalize to `? super Page`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/impl/PageCache.java`
#### Snippet
```java
   }

   public synchronized void forEachUsedPage(Consumer<Page> consumerPage) {
      usedPages.values().forEach(consumerPage);
   }
```

### BoundedWildcard
Can generalize to `? extends AddressSettings`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/impl/PagingStoreFactoryNIO.java`
#### Snippet
```java

   @Override
   public List<PagingStore> reloadStores(final HierarchicalRepository<AddressSettings> addressSettingsRepository) throws Exception {
      File[] files = directory.listFiles();

```

### BoundedWildcard
Can generalize to `? super PagedMessage`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/impl/PageReadWriter.java`
#### Snippet
```java
                                             SequentialFile file,
                                             long pageId,
                                             Consumer<PagedMessage> messages,
                                             PageRecordFilter skipRecord,
                                             SuspectFileCallback suspectFileCallback,
```

### BoundedWildcard
Can generalize to `? super Page`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/impl/Page.java`
#### Snippet
```java

   /** to be called when the page is supposed to be released */
   public void releaseTask(Consumer<Page> releaseTask) {
      referenceCounter.setTask(() -> releaseTask.accept(this));
   }
```

### BoundedWildcard
Can generalize to `? extends AddressSettings`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/impl/PagingStoreFactoryDatabase.java`
#### Snippet
```java

   @Override
   public synchronized List<PagingStore> reloadStores(final HierarchicalRepository<AddressSettings> addressSettingsRepository) throws Exception {
      // We assume the directory list < Integer.MAX_VALUE (this is only a list of addresses).
      JDBCSequentialFile directoryList = (JDBCSequentialFile) pagingFactoryFileFactory.createSequentialFile(DIRECTORY_NAME);
```

### BoundedWildcard
Can generalize to `? super Long`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/impl/PagingManagerImpl.java`
#### Snippet
```java

   @Override
   public void forEachTransaction(BiConsumer<Long, PageTransactionInfo> transactionConsumer) {
      transactions.forEach(transactionConsumer);
   }
```

### BoundedWildcard
Can generalize to `? super PageTransactionInfo`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/impl/PagingManagerImpl.java`
#### Snippet
```java

   @Override
   public void forEachTransaction(BiConsumer<Long, PageTransactionInfo> transactionConsumer) {
      transactions.forEach(transactionConsumer);
   }
```

### BoundedWildcard
Can generalize to `? extends PageSubscription`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/cursor/impl/PageCursorProviderImpl.java`
#### Snippet
```java
    * This method is synchronized because we want it to be atomic with the cursors being used
    */
   private long checkMinPage(Collection<PageSubscription> cursorList) {
      long minPage = Long.MAX_VALUE;

```

### BoundedWildcard
Can generalize to `? extends PageSubscription`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/cursor/impl/PageCursorProviderImpl.java`
#### Snippet
```java
   }

   private void deliverIfNecessary(Collection<PageSubscription> cursorList, long minPage) {
      boolean currentWriting = minPage == pagingStore.getCurrentWritingPage() ? true : false;
      for (PageSubscription cursor : cursorList) {
```

### BoundedWildcard
Can generalize to `? extends PageSubscription`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/cursor/impl/PageCursorProviderImpl.java`
#### Snippet
```java
    * @throws Exception
    */
   protected void storeBookmark(ArrayList<PageSubscription> cursorList, Page currentPage) throws Exception {
      try {
         // First step: Move every cursor to the next bookmarked page (that was just created)
```

### BoundedWildcard
Can generalize to `? super PageSubscription`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/cursor/impl/PageCursorProviderImpl.java`
#### Snippet
```java

   @Override
   public void forEachSubscription(Consumer<PageSubscription> consumer) {
      activeCursors.forEach((k, v) -> consumer.accept(v));
   }
```

### BoundedWildcard
Can generalize to `? extends PageSubscription`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/cursor/impl/PageCursorProviderImpl.java`
#### Snippet
```java
   }

   private boolean checkPageCompletion(ArrayList<PageSubscription> cursorList, long minPage) throws Exception {

      logger.trace("checkPageCompletion({})", minPage);
```

### BoundedWildcard
Can generalize to `? super Page`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/cursor/impl/PageCursorProviderImpl.java`
#### Snippet
```java
    * @throws Exception
    */
   private void cleanupRegularStream(ArrayList<Page> depagedPages,
                          LongHashSet depagedPagesSet,
                          ArrayList<PageSubscription> cursorList,
```

### BoundedWildcard
Can generalize to `? super Page`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/cursor/impl/PageCursorProviderImpl.java`
#### Snippet
```java
    * We still need regular depaging on a streamed manner as it will check the min page for all the existent cursors.
    * */
   private void cleanupMiddleStream(ArrayList<Page> depagedPages,
                          LongHashSet depagedPagesSet,
                          ArrayList<PageSubscription> cursorList,
```

### BoundedWildcard
Can generalize to `? extends PageSubscription`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/cursor/impl/PageCursorProviderImpl.java`
#### Snippet
```java
   private void cleanupMiddleStream(ArrayList<Page> depagedPages,
                          LongHashSet depagedPagesSet,
                          ArrayList<PageSubscription> cursorList,
                          long minPage,
                          long firstPage) {
```

### BoundedWildcard
Can generalize to `? super ConsumedPage`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/cursor/impl/PageSubscriptionImpl.java`
#### Snippet
```java

   @Override
   public void forEachConsumedPage(Consumer<ConsumedPage> pageConsumer) {
      synchronized (consumedPages) {
         consumedPages.values().forEach(pageConsumer);
```

### BoundedWildcard
Can generalize to `? super Integer`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/cursor/impl/PageSubscriptionImpl.java`
#### Snippet
```java

      @Override
      public void forEachAck(BiConsumer<Integer, PagePosition> ackConsumer) {
         acks.forEach(ackConsumer);
      }
```

### BoundedWildcard
Can generalize to `? super PagePosition`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/cursor/impl/PageSubscriptionImpl.java`
#### Snippet
```java

      @Override
      public void forEachAck(BiConsumer<Integer, PagePosition> ackConsumer) {
         acks.forEach(ackConsumer);
      }
```

### BoundedWildcard
Can generalize to `? super MessageReference`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/Queue.java`
#### Snippet
```java
   LinkedListIterator<MessageReference> iterator();

   default void forEach(java.util.function.Consumer<MessageReference> consumer) {
      synchronized (this) {
         try (LinkedListIterator<MessageReference> iterator = iterator()) {
```

### BoundedWildcard
Can generalize to `? super String`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/LegacyLDAPSecuritySettingPlugin.java`
#### Snippet
```java
   }

   private void processSearchResult(Map<String, Set<Role>> securityRoles,
                                    SearchResult searchResult) throws NamingException {
      LdapName searchResultLdapName = new LdapName(searchResult.getName());
```

### BoundedWildcard
Can generalize to `? extends Queue`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ScaleDownHandler.java`
#### Snippet
```java

   private long scaleDownSNF(final SimpleString address,
                             final Set<Queue> queues,
                             final ClientProducer producer) throws Exception {
      long messageCount = 0;
```

### BoundedWildcard
Can generalize to `? extends Queue`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ScaleDownHandler.java`
#### Snippet
```java

   public long scaleDownRegularMessages(final SimpleString address,
                                        final Set<Queue> queues,
                                        final ClientSession clientSession,
                                        final ClientProducer producer) throws Exception {
```

### BoundedWildcard
Can generalize to `? super QueueImpl`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/RefsOperation.java`
#### Snippet
```java
   }

   protected void rollbackRedelivery(Transaction tx, MessageReference ref, long timeBase, Map<QueueImpl, LinkedList<MessageReference>> queueMap) throws Exception {
      if (RefCountMessage.isRefTraceEnabled()) {
         RefCountMessage.deferredDebug(ref.getMessage(), "RollbackDelivery");
```

### BoundedWildcard
Can generalize to `? super MessageReference`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerConsumerImpl.java`
#### Snippet
```java
   @Override
   public synchronized List<MessageReference> scanDeliveringReferences(boolean remove,
                                                                        Function<MessageReference, Boolean> startFunction,
                                                                        Function<MessageReference, Boolean> endFunction) {
      LinkedList<MessageReference> retReferences = new LinkedList<>();
```

### BoundedWildcard
Can generalize to `? super MessageReference`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerConsumerImpl.java`
#### Snippet
```java
   public synchronized List<MessageReference> scanDeliveringReferences(boolean remove,
                                                                        Function<MessageReference, Boolean> startFunction,
                                                                        Function<MessageReference, Boolean> endFunction) {
      LinkedList<MessageReference> retReferences = new LinkedList<>();
      boolean hit = false;
```

### BoundedWildcard
Can generalize to `? extends ConnectorServiceConfiguration`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServiceRegistryImpl.java`
#### Snippet
```java

   @Override
   public Collection<Pair<ConnectorServiceFactory, ConnectorServiceConfiguration>> getConnectorServices(List<ConnectorServiceConfiguration> configs) {
      if (configs != null) {
         for (final ConnectorServiceConfiguration config : configs) {
```

### BoundedWildcard
Can generalize to `? super BaseInterceptor`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServiceRegistryImpl.java`
#### Snippet
```java
   }

   private void instantiateInterceptors(List<String> classNames, List<BaseInterceptor> interceptors) {
      if (classNames != null) {
         for (final String className : classNames) {
```

### BoundedWildcard
Can generalize to `? super MessageReference`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ScheduledDeliveryHandlerImpl.java`
#### Snippet
```java

   @Override
   public List<MessageReference> cancel(Predicate<MessageReference> predicate) throws ActiveMQException {
      List<MessageReference> refs = new ArrayList<>();

```

### BoundedWildcard
Can generalize to `? extends MessageReference`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java
                                  Transaction theTx,
                                  boolean wasStarted,
                                  List<MessageReference> toCancel) throws Exception {
      for (MessageReference ref : toCancel) {
         ref.getQueue().cancel(theTx, ref);
```

### BoundedWildcard
Can generalize to `? extends AddressBindingInfo`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/PostOfficeJournalLoader.java`
#### Snippet
```java

   @Override
   public void initAddresses(List<AddressBindingInfo> addressBindingInfos) throws Exception {

      for (AddressBindingInfo addressBindingInfo : addressBindingInfos) {
```

### BoundedWildcard
Can generalize to `? super Long`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/PostOfficeJournalLoader.java`
#### Snippet
```java

   @Override
   public void initQueues(Map<Long, QueueBindingInfo> queueBindingInfosMap,
                          List<QueueBindingInfo> queueBindingInfos) throws Exception {
      int duplicateID = 0;
```

### BoundedWildcard
Can generalize to `? super QueueBindingInfo`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/PostOfficeJournalLoader.java`
#### Snippet
```java

   @Override
   public void initQueues(Map<Long, QueueBindingInfo> queueBindingInfosMap,
                          List<QueueBindingInfo> queueBindingInfos) throws Exception {
      int duplicateID = 0;
```

### BoundedWildcard
Can generalize to `? extends QueueBindingInfo`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/PostOfficeJournalLoader.java`
#### Snippet
```java
   @Override
   public void initQueues(Map<Long, QueueBindingInfo> queueBindingInfosMap,
                          List<QueueBindingInfo> queueBindingInfos) throws Exception {
      int duplicateID = 0;
      for (final QueueBindingInfo queueBindingInfo : queueBindingInfos) {
```

### BoundedWildcard
Can generalize to `? extends Message`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/PostOfficeJournalLoader.java`
#### Snippet
```java

   @Override
   public void handleNoMessageReferences(Map<Long, Message> messages) {
      for (Message msg : messages.values()) {
         if (msg.getRefCount() == 0 && msg.getDurableCount() == 0) {
```

### BoundedWildcard
Can generalize to `? extends GroupingInfo`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/PostOfficeJournalLoader.java`
#### Snippet
```java

   @Override
   public void handleGroupingBindings(List<GroupingInfo> groupingInfos) {
      for (GroupingInfo groupingInfo : groupingInfos) {
         if (groupingHandler != null) {
```

### BoundedWildcard
Can generalize to `? extends Queue`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/PostOfficeJournalLoader.java`
#### Snippet
```java
    * @throws Exception
    */
   private Map<SimpleString, Map<Long, Map<Long, List<PageCountPending>>>> generateMapsOnPendingCount(Map<Long, Queue> queues,
                                                                                                      List<PageCountPending> pendingNonTXPageCounter,
                                                                                                      Transaction txRecoverCounter) throws Exception {
```

### BoundedWildcard
Can generalize to `? extends PageCountPending`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/PostOfficeJournalLoader.java`
#### Snippet
```java
    */
   private Map<SimpleString, Map<Long, Map<Long, List<PageCountPending>>>> generateMapsOnPendingCount(Map<Long, Queue> queues,
                                                                                                      List<PageCountPending> pendingNonTXPageCounter,
                                                                                                      Transaction txRecoverCounter) throws Exception {
      Map<SimpleString, Map<Long, Map<Long, List<PageCountPending>>>> perAddressMap = new HashMap<>();
```

### BoundedWildcard
Can generalize to `? extends MessageReference`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/PostOfficeJournalLoader.java`
#### Snippet
```java
   @Override
   public void handlePreparedTransaction(Transaction tx,
                                         List<MessageReference> referencesToAck,
                                         Xid xid,
                                         ResourceManager resourceManager) throws Exception {
```

### BoundedWildcard
Can generalize to `? super MessageReference`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/PostOfficeJournalLoader.java`
#### Snippet
```java
   @Override
   public void handlePreparedAcknowledge(long messageID,
                                         List<MessageReference> referencesToAck,
                                         long queueID) throws Exception {
      Queue queue = queues.get(queueID);
```

### BoundedWildcard
Can generalize to `? extends LinkedHashSet`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/replay/ReplayManager.java`
#### Snippet
```java
   private void readLargeMessageBody(SequentialFileFactory messagesFF,
                          Message message,
                          HashMap<Long, LinkedHashSet<JournalFile>> filesMap,
                          long originalMessageID) throws Exception {
      long newMessageID = message.getMessageID();
```

### BoundedWildcard
Can generalize to `? extends TransportConfiguration`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/ha/ColocatedHAManager.java`
#### Snippet
```java
    * @throws Exception
    */
   public boolean requestBackup(Pair<TransportConfiguration, TransportConfiguration> connectorPair,
                                int backupSize,
                                boolean replicated) throws Exception {
```

### BoundedWildcard
Can generalize to `? extends Gauge.Builder`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/metrics/MetricsManager.java`
#### Snippet
```java
   }

   private void registerMeters(List<Gauge.Builder> gaugeBuilders, String resource) {
      if (meters.get(resource) != null) {
         throw ActiveMQMessageBundle.BUNDLE.metersAlreadyRegistered(resource);
```

### BoundedWildcard
Can generalize to `? extends AddressSettings`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/metrics/MetricsManager.java`
#### Snippet
```java
   public MetricsManager(String brokerName,
                         MetricsConfiguration metricsConfiguration,
                         HierarchicalRepository<AddressSettings> addressSettingsRepository) {
      this.brokerName = brokerName;
      meterRegistry = metricsConfiguration.getPlugin().getRegistry();
```

### BoundedWildcard
Can generalize to `? extends TransportConfiguration`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/impl/ClusterConnectionImpl.java`
#### Snippet
```java
                             final String backupGroupName,
                             final String scaleDownGroupName,
                             final Pair<TransportConfiguration, TransportConfiguration> connectorPair,
                             final boolean backup) {
      if (logger.isDebugEnabled()) {
```

### BoundedWildcard
Can generalize to `? extends TransportConfiguration`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/impl/ClusterConnectionImpl.java`
#### Snippet
```java
                             final String backupGroupName,
                             final String scaleDownGroupName,
                             final Pair<TransportConfiguration, TransportConfiguration> connectorPair,
                             final boolean backup) {
      if (logger.isDebugEnabled()) {
```

### BoundedWildcard
Can generalize to `? extends List`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/metrics/NettyPooledAllocatorMetrics.java`
#### Snippet
```java
   private void metricsOfPoolSubpageMetric(final MeterRegistry registry,
                                           final PoolArenaMetric poolArenaMetric,
                                           final Function<? super PoolArenaMetric, List<PoolSubpageMetric>> poolSubpageMetricProvider,
                                           final String poolArenaIndex,
                                           final String poolArenaType,
```

### BoundedWildcard
Can generalize to `? extends TransportConfiguration`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/routing/pools/StaticPool.java`
#### Snippet
```java

   public StaticPool(TargetFactory targetFactory, ScheduledExecutorService scheduledExecutor,
                     int checkPeriod, List<TransportConfiguration> staticConnectors) {
      super(targetFactory, scheduledExecutor, checkPeriod);

```

### BoundedWildcard
Can generalize to `? extends TargetProbe`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/routing/targets/TargetMonitor.java`
#### Snippet
```java


   public TargetMonitor(ScheduledExecutorService scheduledExecutorService, int checkPeriod, Target target, List<TargetProbe> targetProbes) {
      super(scheduledExecutorService, 0, checkPeriod, TimeUnit.MILLISECONDS, false);

```

### BoundedWildcard
Can generalize to `? extends Target`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/routing/policies/RoundRobinPolicy.java`
#### Snippet
```java

   @Override
   public Target selectTarget(List<Target> targets, String key) {
      if (targets.size() > 0) {
         pos = pos % targets.size();
```

### BoundedWildcard
Can generalize to `? extends Target`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/routing/policies/FirstElementPolicy.java`
#### Snippet
```java

   @Override
   public Target selectTarget(List<Target> targets, String key) {
      if (targets.size() > 0) {
         return targets.get(0);
```

### BoundedWildcard
Can generalize to `? extends Target`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/routing/policies/ConsistentHashPolicy.java`
#### Snippet
```java

   @Override
   public Target selectTarget(List<Target> targets, String key) {
      if (targets.size() > 1) {
         NavigableMap<Integer, Target> consistentTargets = new TreeMap<>();
```

### BoundedWildcard
Can generalize to `? extends Target`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/routing/policies/LeastConnectionsPolicy.java`
#### Snippet
```java

   @Override
   public Target selectTarget(List<Target> targets, String key) {
      if (targets.size() > 1) {
         NavigableMap<Integer, List<Target>> sortedTargets = new TreeMap<>();
```

### BoundedWildcard
Can generalize to `? extends FederationPolicy`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/federation/FederationUpstream.java`
#### Snippet
```java
   }

   private void deployPolicyRefs(Set<String> policyRefsToDeploy, Map<String, FederationPolicy> policyMap, int recursionDepth) throws ActiveMQException {
      for (String policyRef : policyRefsToDeploy) {
         FederationPolicy policy = policyMap.get(policyRef);
```

### BoundedWildcard
Can generalize to `? extends P`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ActiveMQServerImpl.java`
#### Snippet
```java
   }

   private <P extends ActiveMQServerBasePlugin> void callBrokerPlugins(final List<P> plugins, final ActiveMQPluginRunnable<P> pluginRun) throws ActiveMQException {
      if (pluginRun != null) {
         for (P plugin : plugins) {
```

### BoundedWildcard
Can generalize to `? super P`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ActiveMQServerImpl.java`
#### Snippet
```java
   }

   private <P extends ActiveMQServerBasePlugin> void callBrokerPlugins(final List<P> plugins, final ActiveMQPluginRunnable<P> pluginRun) throws ActiveMQException {
      if (pluginRun != null) {
         for (P plugin : plugins) {
```

### BoundedWildcard
Can generalize to `? super T`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ActiveMQServerImpl.java`
#### Snippet
```java
   }

   private static <T> void setDefaultIfUnset(Supplier<T> getter, Consumer<T> setter, T defaultValue) {
      if (getter.get() == null) {
         setter.accept(defaultValue);
```

### BoundedWildcard
Can generalize to `? extends QueueConfiguration`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ActiveMQServerImpl.java`
#### Snippet
```java
   }

   private void deployQueuesFromListQueueConfiguration(List<QueueConfiguration> queues) throws Exception {
      for (QueueConfiguration config : queues) {
         try {
```

### BoundedWildcard
Can generalize to `? extends Role`
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/security/ActiveMQSecurityManagerImpl.java`
#### Snippet
```java
   public boolean validateUserAndRole(final String user,
                                      final String password,
                                      final Set<Role> roles,
                                      final CheckType checkType) {
      if (validateUser(user, password)) {
```

### BoundedWildcard
Can generalize to `? extends QueueConfiguration`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/impl/ConfigurationImpl.java`
#### Snippet
```java

   @Override
   public ConfigurationImpl setQueueConfigs(final List<QueueConfiguration> configs) {
      for (QueueConfiguration queueConfiguration : configs) {
         coreQueueConfigurations.add(CoreQueueConfiguration.fromQueueConfiguration(queueConfiguration));
```

### BoundedWildcard
Can generalize to `? extends ActiveMQServerBasePlugin`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/impl/ConfigurationImpl.java`
#### Snippet
```java

   @Override
   public void registerBrokerPlugins(final List<ActiveMQServerBasePlugin> plugins) {
      plugins.forEach(plugin -> registerBrokerPlugin(plugin));
   }
```

### BoundedWildcard
Can generalize to `? super String`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/impl/ConfigurationImpl.java`
#### Snippet
```java
   }

   private void trackError(HashMap<String, String> errors, Map.Entry<String,?> entry, Throwable oops) {
      logger.debug("failed to populate property entry({}), reason: {}", entry, oops);
      errors.put(entry.toString(), oops.getLocalizedMessage());
```

### BoundedWildcard
Can generalize to `? super String`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/impl/ConfigurationImpl.java`
#### Snippet
```java
   }

   private void trackError(HashMap<String, String> errors, Map.Entry<String,?> entry, Throwable oops) {
      logger.debug("failed to populate property entry({}), reason: {}", entry, oops);
      errors.put(entry.toString(), oops.getLocalizedMessage());
```

### BoundedWildcard
Can generalize to `? super String`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/impl/HawtioSecurityControlImpl.java`
#### Snippet
```java
   }

   private String parseMethodName(String method, List<String> argTypes) {
      method = method.trim();
      int index = method.indexOf('(');
```

### BoundedWildcard
Can generalize to `? extends BaseInterceptor`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/CoreProtocolManagerFactory.java`
#### Snippet
```java

   @Override
   public List<Interceptor> filterInterceptors(List<BaseInterceptor> interceptors) {
      // This is using this tool method
      // it wouldn't be possible to write a generic method without this class parameter
```

### BoundedWildcard
Can generalize to `? super SimpleString`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/federation/address/FederatedAddress.java`
#### Snippet
```java
   }

   private void createRemoteConsumer(DivertBinding divertBinding, final Set<SimpleString> matchingQueues, QueueBinding queueBinding)  {
      final AddressInfo addressInfo = server.getPostOffice().getAddressInfo(divertBinding.getAddress());

```

### BoundedWildcard
Can generalize to `? extends Set`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/security/impl/SecurityStoreImpl.java`
#### Snippet
```java
    * @param notificationService can be <code>null</code>
    */
   public SecurityStoreImpl(final HierarchicalRepository<Set<Role>> securityRepository,
                            final ActiveMQSecurityManager securityManager,
                            final long invalidationInterval,
```

### BoundedWildcard
Can generalize to `? extends ProtocolManagerFactory`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/remoting/server/impl/RemotingServiceImpl.java`
#### Snippet
```java
    * @param protocolManagerFactoryCollection
    */
   private void loadProtocolManagerFactories(Iterable<ProtocolManagerFactory> protocolManagerFactoryCollection) {
      for (ProtocolManagerFactory next : protocolManagerFactoryCollection) {
         MessagePersister.registerProtocol(next);
```

### BoundedWildcard
Can generalize to `? super String`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/remoting/server/impl/RemotingServiceImpl.java`
#### Snippet
```java
   private void locateProtocols(String protocolList,
                                Object transportConfig,
                                Map<String, ProtocolManagerFactory> protocolMap) {
      String[] protocolsSplit = protocolList.split(",");

```

### BoundedWildcard
Can generalize to `? super ProtocolManagerFactory`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/remoting/server/impl/RemotingServiceImpl.java`
#### Snippet
```java
   private void locateProtocols(String protocolList,
                                Object transportConfig,
                                Map<String, ProtocolManagerFactory> protocolMap) {
      String[] protocolsSplit = protocolList.split(",");

```

### BoundedWildcard
Can generalize to `? extends Map.Entry`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/HierarchicalObjectRepository.java`
#### Snippet
```java

   @Override
   public void swap(Set<Map.Entry<String, T>> entries) {
      lock.writeLock().lock();
      try {
```

### BoundedWildcard
Can generalize to `? extends Match`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/HierarchicalObjectRepository.java`
#### Snippet
```java
    * @return
    */
   private List<Match<T>> sort(final Map<String, Match<T>> possibleMatches) {
      List<String> keys = new ArrayList<>(possibleMatches.keySet());
      Collections.sort(keys, matchComparator);
```

### BoundedWildcard
Can generalize to `? extends Match`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/HierarchicalObjectRepository.java`
#### Snippet
```java
    * @return
    */
   private T merge(final Collection<Match<T>> orderedMatches) {
      T actualMatch = null;
      for (Match<T> match : orderedMatches) {
```

### BoundedWildcard
Can generalize to `? super ServerSession`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/view/predicate/ConnectionFilterPredicate.java`
#### Snippet
```java
   }

   Set<String> collectFromSessions(String connectionId, Function<ServerSession, String> getter) {
      List<ServerSession> sessions = server.getSessions(connectionId);
      Set<String> sessionAttributes = new HashSet<>();
```

### BoundedWildcard
Can generalize to `? super T`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/postoffice/impl/AddressPartNode.java`
#### Snippet
```java
   }

   public void visitValues(final AddressMapVisitor<T> collector) throws Exception {
      for (T o : values) {
         collector.visit(o);
```

### BoundedWildcard
Can generalize to `? extends Set`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/AddressControlImpl.java`
#### Snippet
```java
                             final PagingManager pagingManager,
                             final StorageManager storageManager,
                             final HierarchicalRepository<Set<Role>> securityRepository,
                             final SecurityStore securityStore,
                             final ManagementService managementService)throws Exception {
```

### BoundedWildcard
Can generalize to `? extends Binding`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/postoffice/impl/SimpleAddressManager.java`
#### Snippet
```java

   protected Bindings addMappingsInternal(final SimpleString address,
                                      final Collection<Binding> newBindings) throws Exception {
      if (newBindings.isEmpty()) {
         return null;
```

### BoundedWildcard
Can generalize to `? super String`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/QueueControlImpl.java`
#### Snippet
```java
   }

   private void internalComputeMessage(Map<String, Long> result, Filter filter, SimpleString groupByProperty, Message message) {
      if (filter == null || filter.match(message)) {
         if (groupByProperty == null) {
```

### BoundedWildcard
Can generalize to `? extends MessageReference`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/QueueControlImpl.java`
#### Snippet
```java
    * @return
    */
   private Map<String, Object>[] convertMessagesToMaps(List<MessageReference> refs) throws ActiveMQException {
      final int attributeSizeLimit = addressSettingsRepository.getMatch(address).getManagementMessageAttributeSizeLimit();
      Map<String, Object>[] messages = new Map[refs.size()];
```

### BoundedWildcard
Can generalize to `? extends AddressSettings`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/QueueControlImpl.java`
#### Snippet
```java
                           final StorageManager storageManager,
                           final SecurityStore securityStore,
                           final HierarchicalRepository<AddressSettings> addressSettingsRepository) throws Exception {
      super(QueueControl.class, storageManager);
      this.queue = queue;
```

### BoundedWildcard
Can generalize to `? super EncodingSupport`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/BufferSplitter.java`
#### Snippet
```java
   }

   public static void split(byte[] buffer, int splitSize, Consumer<EncodingSupport> target) {

      int location = 0;
```

### BoundedWildcard
Can generalize to `? extends RecordInfo`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/LargeMessageTXFailureCallback.java`
#### Snippet
```java
   @Override
   public void failedTransaction(final long transactionID,
                                 final List<RecordInfo> records,
                                 final List<RecordInfo> recordsToDelete) {
      for (RecordInfo record : records) {
```

### BoundedWildcard
Can generalize to `? super Long`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/JournalStorageManager.java`
#### Snippet
```java

   @Override
   public void recoverLargeMessagesOnFolder(Set<Long> storedLargeMessages) throws Exception {
      List<String> filenames = largeMessagesFactory.listFiles("msg");
      filenames.forEach(f -> {
```

### BoundedWildcard
Can generalize to `? super String`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/deployers/impl/FileConfigurationParser.java`
#### Snippet
```java
   }

   private void getStaticConnectors(List<String> staticConnectorNames, Node child) {
      NodeList children2 = ((Element) child).getElementsByTagName("connector-ref");

```

### BoundedWildcard
Can generalize to `? extends Set`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/deployers/impl/FileConfigurationParser.java`
#### Snippet
```java
    * @return the final set of mapped role names
    */
   private String[] getMappedRoleNames(String[] roles, Map<String, Set<String>> roleMappings) {
      Set<String> mappedRoles = new HashSet<>();
      for (String role : roles) {
```

### BoundedWildcard
Can generalize to `? super Long`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/DescribeJournal.java`
#### Snippet
```java
   }

   protected static PageSubscriptionCounterImpl lookupCounter(Map<Long, PageSubscriptionCounterImpl> counters,
                                                              long queueIDForCounter) {
      PageSubscriptionCounterImpl subsCounter;
```

### BoundedWildcard
Can generalize to `? extends RecordInfo`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/DescribeJournal.java`
#### Snippet
```java
         @Override
         public void failedTransaction(long transactionID,
                                       List<RecordInfo> records1,
                                       List<RecordInfo> recordsToDelete) {
            bufferFailingTransactions.append("Transaction " + transactionID + " failed with these records:\n");
```

### BoundedWildcard
Can generalize to `? extends RecordInfo`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/DescribeJournal.java`
#### Snippet
```java
         public void failedTransaction(long transactionID,
                                       List<RecordInfo> records1,
                                       List<RecordInfo> recordsToDelete) {
            bufferFailingTransactions.append("Transaction " + transactionID + " failed with these records:\n");
            for (RecordInfo info : records1) {
```

### BoundedWildcard
Can generalize to `? extends RouteContextList`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/postoffice/impl/PostOfficeImpl.java`
#### Snippet
```java
    * @param entry
    */
   private void schedulePageDelivery(Transaction tx, Map.Entry<SimpleString, RouteContextList> entry) {
      if (tx != null) {
         PageDelivery delivery = (PageDelivery) tx.getProperty(TransactionPropertyIndexes.PAGE_DELIVERY);
```

### BoundedWildcard
Can generalize to `? super MessageReference`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/postoffice/impl/PostOfficeImpl.java`
#### Snippet
```java
                                            final Transaction tx,
                                            final List<Queue> durableQueues,
                                            final ArrayList<MessageReference> refs) throws Exception {
      final int durableQueuesCount = durableQueues.size();
      refs.ensureCapacity(durableQueuesCount);
```

### BoundedWildcard
Can generalize to `? extends MessageReference`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/postoffice/impl/PostOfficeImpl.java`
#### Snippet
```java
   }

   public static void processReferences(List<MessageReference> refs, boolean direct) {
      refs.forEach((ref) -> processReference(ref, direct));
   }
```

### BoundedWildcard
Can generalize to `? extends Queue`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/postoffice/impl/PostOfficeImpl.java`
#### Snippet
```java
      private final Set<Queue> queues = new HashSet<>();

      public void addQueues(List<Queue> queueList) {
         queues.addAll(queueList);
      }
```

### BoundedWildcard
Can generalize to `? extends AddressSettings`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/postoffice/impl/PostOfficeImpl.java`
#### Snippet
```java
                         final int idCacheSize,
                         final boolean persistIDCache,
                         final HierarchicalRepository<AddressSettings> addressSettingsRepository) {
      this.storageManager = storageManager;

```

### BoundedWildcard
Can generalize to `? extends TransactionOperation`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/transaction/impl/TransactionImpl.java`
#### Snippet
```java
   }

   protected synchronized void afterCommit(List<TransactionOperation> operationsToComplete) {
      if (operationsToComplete != null) {
         for (TransactionOperation operation : operationsToComplete) {
```

### BoundedWildcard
Can generalize to `? extends TransactionOperation`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/transaction/impl/TransactionImpl.java`
#### Snippet
```java
   }

   private synchronized void afterRollback(List<TransactionOperation> operationsToComplete) {
      if (operationsToComplete != null) {
         for (TransactionOperation operation : operationsToComplete) {
```

### BoundedWildcard
Can generalize to `? super Long`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/replication/ReplicationEndpoint.java`
#### Snippet
```java
   private synchronized Page newPage(final long pageId,
                                     final SimpleString storeName,
                                     final ConcurrentMap<Long, Page> map) throws Exception {
      Page page = map.get(pageId);

```

### BoundedWildcard
Can generalize to `? extends Role`
in `artemis-server/src/main/java/org/apache/activemq/artemis/utils/SecurityManagerUtil.java`
#### Snippet
```java
   private static final String WILDCARD = "*";

   public static Set<RolePrincipal> getPrincipalsInRole(final CheckType checkType, final Set<Role> roles, final String rolePrincipalClass) {
      Set principals = new HashSet<>();
      for (Role role : roles) {
```

### BoundedWildcard
Can generalize to `? extends PageTransactionInfo`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/AbstractJournalStorageManager.java`
#### Snippet
```java

   public void checkInvalidPageTransactions(PagingManager pagingManager,
                                            Set<PageTransactionInfo> invalidPageTransactions) {
      if (invalidPageTransactions != null && !invalidPageTransactions.isEmpty()) {
         for (PageTransactionInfo pginfo : invalidPageTransactions) {
```

### BoundedWildcard
Can generalize to `? super Long`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/AbstractJournalStorageManager.java`
#### Snippet
```java
    */
   private static PageSubscription locateSubscription(final long queueID,
                                                      final Map<Long, PageSubscription> pageSubscriptions,
                                                      final Map<Long, QueueBindingInfo> queueInfos,
                                                      final PagingManager pagingManager) throws Exception {
```

### BoundedWildcard
Can generalize to `? extends QueueBindingInfo`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/AbstractJournalStorageManager.java`
#### Snippet
```java
   private static PageSubscription locateSubscription(final long queueID,
                                                      final Map<Long, PageSubscription> pageSubscriptions,
                                                      final Map<Long, QueueBindingInfo> queueInfos,
                                                      final PagingManager pagingManager) throws Exception {

```

### BoundedWildcard
Can generalize to `? extends PreparedTransactionInfo`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/AbstractJournalStorageManager.java`
#### Snippet
```java
                                         final ResourceManager resourceManager,
                                         final Map<Long, QueueBindingInfo> queueInfos,
                                         final List<PreparedTransactionInfo> preparedTransactions,
                                         final BiConsumer<PreparedTransactionInfo, Throwable> failedTransactionCallback,
                                         final Map<Long, PageSubscription> pageSubscriptions,
```

### BoundedWildcard
Can generalize to `? super PreparedTransactionInfo`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/AbstractJournalStorageManager.java`
#### Snippet
```java
                                         final Map<Long, QueueBindingInfo> queueInfos,
                                         final List<PreparedTransactionInfo> preparedTransactions,
                                         final BiConsumer<PreparedTransactionInfo, Throwable> failedTransactionCallback,
                                         final Map<Long, PageSubscription> pageSubscriptions,
                                         final Set<Pair<Long, Long>> pendingLargeMessages,
```

### BoundedWildcard
Can generalize to `? super Throwable`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/AbstractJournalStorageManager.java`
#### Snippet
```java
                                         final Map<Long, QueueBindingInfo> queueInfos,
                                         final List<PreparedTransactionInfo> preparedTransactions,
                                         final BiConsumer<PreparedTransactionInfo, Throwable> failedTransactionCallback,
                                         final Map<Long, PageSubscription> pageSubscriptions,
                                         final Set<Pair<Long, Long>> pendingLargeMessages,
```

### BoundedWildcard
Can generalize to `? super QueueBindingInfo`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/AbstractJournalStorageManager.java`
#### Snippet
```java

   @Override
   public JournalLoadInformation loadBindingJournal(final List<QueueBindingInfo> queueBindingInfos,
                                                    final List<GroupingInfo> groupingInfos,
                                                    final List<AddressBindingInfo> addressBindingInfos) throws Exception {
```

### BoundedWildcard
Can generalize to `? super GroupingInfo`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/AbstractJournalStorageManager.java`
#### Snippet
```java
   @Override
   public JournalLoadInformation loadBindingJournal(final List<QueueBindingInfo> queueBindingInfos,
                                                    final List<GroupingInfo> groupingInfos,
                                                    final List<AddressBindingInfo> addressBindingInfos) throws Exception {
      SparseArrayLinkedList<RecordInfo> records = new SparseArrayLinkedList<>();
```

### BoundedWildcard
Can generalize to `? super AddressBindingInfo`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/AbstractJournalStorageManager.java`
#### Snippet
```java
   public JournalLoadInformation loadBindingJournal(final List<QueueBindingInfo> queueBindingInfos,
                                                    final List<GroupingInfo> groupingInfos,
                                                    final List<AddressBindingInfo> addressBindingInfos) throws Exception {
      SparseArrayLinkedList<RecordInfo> records = new SparseArrayLinkedList<>();

```

### BoundedWildcard
Can generalize to `? super PageCountPending`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/AbstractJournalStorageManager.java`
#### Snippet
```java
                                                    final Set<Pair<Long, Long>> pendingLargeMessages,
                                                    final Set<Long> storedLargeMessages,
                                                    List<PageCountPending> pendingNonTXPageCounter,
                                                    final JournalLoader journalLoader) throws Exception {
      SparseArrayLinkedList<RecordInfo> records = new SparseArrayLinkedList<>();
```

### BoundedWildcard
Can generalize to `? extends MessageReference`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java
   }

   private void purgeAfterRollback(LinkedList<MessageReference> refs) {
      try {
         Transaction transaction = new TransactionImpl(storageManager);
```

### BoundedWildcard
Can generalize to `? extends PagedReference`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java
      }

      private PagedReference iteratePaging(LinkedListIterator<PagedReference> iterator) {
         while (iterator.hasNext()) {
            PagedReference ref = iterator.next();
```

### BoundedWildcard
Can generalize to `? extends MessageReference`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java
   /* Called when a message is cancelled back into the queue */
   @Override
   public void addHead(final List<MessageReference> refs, boolean scheduling) {
      try (ArtemisCloseable metric = measureCritical(CRITICAL_PATH_ADD_HEAD)) {
         synchronized (this) {
```

### BoundedWildcard
Can generalize to `? extends MessageReference`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java
      private final LinkedListIterator<MessageReference> iter;

      SynchronizedIterator(LinkedListIterator<MessageReference> iter) {
         this.iter = iter;
      }
```

### BoundedWildcard
Can generalize to `? extends MessageReference`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java


      private MessageReference iterate(LinkedListIterator<MessageReference> iterator) {
         while (iterator.hasNext()) {
            MessageReference ref = iterator.next();
```

### BoundedWildcard
Can generalize to `? extends MessageReference`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java
   // we certainly can in the case of a rollback in a huge TX.
   // so I am just playing safe and keeping the original semantic for small transactions.
   private void addSortedLargeTX(final List<MessageReference> refs, boolean scheduling) {
      for (MessageReference ref : refs) {
         // When dealing with large transactions, we are not holding a synchronization lock here.
```

### BoundedWildcard
Can generalize to `? extends TransportConfiguration`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/ActiveMQServerControlImpl.java`
#### Snippet
```java
   }

   private String getNetworkConfigsAsJSON(Collection<TransportConfiguration> configs) throws Exception {
      checkStarted();

```

### BoundedWildcard
Can generalize to `? extends TransportConfiguration`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/ActiveMQServerControlImpl.java`
#### Snippet
```java
   }

   private Object[] getNetworkConfigs(Collection<TransportConfiguration> configs) throws Exception {
      checkStarted();

```

## RuleId[id=AnonymousHasLambdaAlternative]
### AnonymousHasLambdaAlternative
Anonymous new Thread() can be replaced with new Thread(() -\> {...})
in `artemis-maven-plugin/src/main/java/org/apache/activemq/artemis/maven/ArtemisCLIPlugin.java`
#### Snippet
```java
         if (spawn) {
            final Process process = org.apache.activemq.artemis.cli.process.ProcessBuilder.build(name, location, true, args);
            Runtime.getRuntime().addShutdownHook(new Thread() {
               @Override
               public void run() {
```

### AnonymousHasLambdaAlternative
Anonymous new Thread() can be replaced with new Thread(() -\> {...})
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/process/ProcessBuilder.java`
#### Snippet
```java

   static {
      Runtime.getRuntime().addShutdownHook(new Thread() {
         @Override
         public void run() {
```

### AnonymousHasLambdaAlternative
Anonymous new Thread() can be replaced with new Thread(() -\> {...})
in `artemis-cli/src/main/java/org/apache/activemq/artemis/util/ServerUtil.java`
#### Snippet
```java

         final Process process = builder.start();
         Runtime.getRuntime().addShutdownHook(new Thread() {
            @Override
            public void run() {
```

### AnonymousHasLambdaAlternative
Anonymous new ThreadLocal() can be replaced with ThreadLocal.withInitial()
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/util/TLSEncode.java`
#### Snippet
```java
   }

   private static final ThreadLocal<EncoderDecoderPair> tlsCodec = new ThreadLocal<EncoderDecoderPair>() {
      @Override
      protected EncoderDecoderPair initialValue() {
```

### AnonymousHasLambdaAlternative
Anonymous new Thread() can be replaced with new Thread(() -\> {...})
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ActiveMQServerImpl.java`
#### Snippet
```java
    */
   public final void stopTheServer(final boolean criticalIOError) {
      Thread thread = new Thread() {
         @Override
         public void run() {
```

### AnonymousHasLambdaAlternative
Anonymous new Thread() can be replaced with new Thread(() -\> {...})
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ActiveMQServerImpl.java`
#### Snippet
```java
      long timeout = configuration.getGracefulShutdownTimeout() < 0 ? 30000 : configuration.getGracefulShutdownTimeout();

      Thread notificationSender = new Thread() {
         @Override
         public void run() {
```

### AnonymousHasLambdaAlternative
Anonymous new Thread() can be replaced with new Thread(() -\> {...})
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ActiveMQServerImpl.java`
#### Snippet
```java
                  // you can't stop from the check thread,
                  // nor can use an executor
                  Thread stopThread = new Thread() {
                     @Override
                     public void run() {
```

### AnonymousHasLambdaAlternative
Anonymous new Thread() can be replaced with new Thread(() -\> {...})
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/NettyAcceptor.java`
#### Snippet
```java
      public void connectionException(final Object connectionID, final ActiveMQException me) {
         // Execute on different thread to avoid deadlocks
         new Thread() {
            @Override
            public void run() {
```

### AnonymousHasLambdaAlternative
Anonymous new Thread() can be replaced with new Thread(() -\> {...})
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/ActiveMQServerControlImpl.java`
#### Snippet
```java
      clearIO();

      Thread t = new Thread() {
         @Override
         public void run() {
```

## RuleId[id=IfStatementMissingBreakInLoop]
### IfStatementMissingBreakInLoop
Loop can be terminated after condition is met
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalImpl.java`
#### Snippet
```java

         for (int v : JournalImpl.COMPATIBLE_VERSIONS) {
            if (v == journalVersion) {
               isCompatible = true;
            }
```

## RuleId[id=FunctionalExpressionCanBeFolded]
### FunctionalExpressionCanBeFolded
Method reference can be replaced with qualifier
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/TypedProperties.java`
#### Snippet
```java
   public synchronized void forEachKey(Consumer<SimpleString> action) {
      if (properties != null) {
         properties.keySet().forEach(action::accept);
      }
   }
```

### FunctionalExpressionCanBeFolded
Method reference can be replaced with qualifier
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/TypedProperties.java`
#### Snippet
```java
   private synchronized void forEachInternal(BiConsumer<SimpleString, PropertyValue> action) {
      if (properties != null) {
         properties.forEach(action::accept);
      }
   }
```

### FunctionalExpressionCanBeFolded
Lambda can be replaced with call qualifier
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Upgrade.java`
#### Snippet
```java
         try (Stream<String> originalLines = Files.lines(targetFile.toPath())) {
            originalLines.forEach(line -> {
               originalConsumer.accept(line);
            });
         }
```

## RuleId[id=FinalizeNotProtected]
### FinalizeNotProtected
'finalize()' should have protected access, not public
in `artemis-unit-test-support/src/main/java/org/apache/activemq/artemis/utils/ThreadLeakCheckRule.java`
#### Snippet
```java

      @Override
      public void finalize() throws Throwable {
         finalized.countDown();
         super.finalize();
```

## RuleId[id=WhileLoopSpinsOnField]
### WhileLoopSpinsOnField
`while` loop spins on field
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/ProducerThread.java`
#### Snippet
```java

         if (runIndefinitely) {
            while (running) {
               paused.await();
               sendMessage(producer, threadName);
```

### WhileLoopSpinsOnField
`while` loop spins on field
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/ChannelImpl.java`
#### Snippet
```java
      try {
         if (connection.getBlockingCallFailoverTimeout() < 0) {
            while (failingOver) {
               failoverCondition.await();
            }
```

### WhileLoopSpinsOnField
`while` loop spins on field
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/FileLockNodeManager.java`
#### Snippet
```java
      boolean isRecurringFailure = false;

      while (!interrupted) {
         try {
            FileLock lock = tryLock(lockPosition);
```

## RuleId[id=DefaultAnnotationParam]
### DefaultAnnotationParam
Redundant default parameter value assignment
in `artemis-maven-plugin/src/main/java/org/apache/activemq/artemis/maven/ArtemisCreatePlugin.java`
#### Snippet
```java
   private String role;

   @Parameter(defaultValue = "")
   private String javaOptions = "";

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `artemis-dto/src/main/java/org/apache/activemq/artemis/dto/JaasSecurityDTO.java`
#### Snippet
```java
   public String domain;

   @XmlAttribute(name = "certificate-domain", required = false)
   public String certificateDomain;
}
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/management/ConnectionRouterControl.java`
#### Snippet
```java
   void setTargetKeyFilter(@Parameter(desc = "the regular expression", name = "regExp") String regExp);

   @Operation(desc = "Get the target key filter regular expression", impact = MBeanOperationInfo.INFO)
   String getTargetKeyFilter();
}
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/management/ConnectionRouterControl.java`
#### Snippet
```java
 */
public interface ConnectionRouterControl {
   @Operation(desc = "Get the target associated with key", impact = MBeanOperationInfo.INFO)
   CompositeData getTarget(@Parameter(desc = "a key", name = "key") String key) throws Exception;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/management/ConnectionRouterControl.java`
#### Snippet
```java
   CompositeData getTarget(@Parameter(desc = "a key", name = "key") String key) throws Exception;

   @Operation(desc = "Get the target associated with key as JSON", impact = MBeanOperationInfo.INFO)
   String getTargetAsJSON(@Parameter(desc = "a key", name = "key") String key);

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/management/ConnectionRouterControl.java`
#### Snippet
```java
   void setLocalTargetFilter(@Parameter(desc = "the regular expression", name = "regExp") String regExp);

   @Operation(desc = "Get the local target filter regular expression", impact = MBeanOperationInfo.INFO)
   String getLocalTargetFilter();

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/management/ActiveMQServerControl.java`
#### Snippet
```java
    * Closes all the connections of clients connected to this server which matches the specified IP address.
    */
   @Operation(desc = "Closes all the consumer connections for the given messaging address", impact = MBeanOperationInfo.INFO)
   boolean closeConsumerConnectionsForAddress(@Parameter(desc = "a messaging address", name = "address") String address) throws Exception;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/management/ActiveMQServerControl.java`
#### Snippet
```java
   String listAddresses(@Parameter(name = "separator", desc = "Separator used on the string listing") String separator) throws Exception;

   @Operation(desc = "Search for Connections", impact = MBeanOperationInfo.INFO)
   String listConnections(@Parameter(name = "options", desc = "Options") String options,
                          @Parameter(name = "pageNumber", desc = "Page Number") int page,
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/management/ActiveMQServerControl.java`
#### Snippet
```java
    * Closes the session with the given id.
    */
   @Operation(desc = "Closes the session with the id", impact = MBeanOperationInfo.INFO)
   boolean closeSessionWithID(@Parameter(desc = "The connection ID", name = "connectionID") String connectionID,
                              @Parameter(desc = "The session ID", name = "ID") String ID) throws Exception;
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/management/ActiveMQServerControl.java`
#### Snippet
```java
   String listNetworkTopology() throws Exception;

   @Operation(desc = "Get the selected address", impact = MBeanOperationInfo.INFO)
   String getAddressInfo(@Parameter(name = "address", desc = "The address") String address) throws ActiveMQAddressDoesNotExistException;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/management/ActiveMQServerControl.java`
#### Snippet
```java
    * </pre>
    */
   @Operation(desc = "List all sessions as a JSON string", impact = MBeanOperationInfo.INFO)
   String listAllSessionsAsJSON() throws Exception;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/management/ActiveMQServerControl.java`
#### Snippet
```java
                        @Parameter(name = "pageSize", desc = "Page Size") int pageSize) throws Exception;

   @Operation(desc = "Search for Consumers", impact = MBeanOperationInfo.INFO)
   String listProducers(@Parameter(name = "options", desc = "Options") String options,
                        @Parameter(name = "pageNumber", desc = "Page Number") int page,
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/management/ActiveMQServerControl.java`
#### Snippet
```java
                       @Parameter(name = "pageSize", desc = "Page Size") int pageSize) throws Exception;

   @Operation(desc = "Search for Consumers", impact = MBeanOperationInfo.INFO)
   String listConsumers(@Parameter(name = "options", desc = "Options") String options,
                        @Parameter(name = "pageNumber", desc = "Page Number") int page,
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/management/ActiveMQServerControl.java`
#### Snippet
```java
    * Returns the names of the queues created on this server with the given routing-type.
    */
   @Operation(desc = "Names of the queues created on this server with the given routing-type (i.e. ANYCAST or MULTICAST)", impact = MBeanOperationInfo.INFO)
   String[] getQueueNames(@Parameter(name = "routingType", desc = "The routing type, MULTICAST or ANYCAST") String routingType) throws Exception;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/management/ActiveMQServerControl.java`
#### Snippet
```java
                        @Parameter(name = "pageSize", desc = "Page Size") int pageSize) throws Exception;

   @Operation(desc = "Search for Addresses", impact = MBeanOperationInfo.INFO)
   String listAddresses(@Parameter(name = "options", desc = "Options") String options,
                        @Parameter(name = "pageNumber", desc = "Page Number") int page,
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/management/ActiveMQServerControl.java`
#### Snippet
```java
   void removeSecuritySettings(@Parameter(desc = "an address match", name = "addressMatch") String addressMatch) throws Exception;

   @Operation(desc = "Get roles for a specific address match", impact = MBeanOperationInfo.INFO)
   Object[] getRoles(@Parameter(desc = "an address match", name = "addressMatch") String addressMatch) throws Exception;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/management/ActiveMQServerControl.java`
#### Snippet
```java
   String[] listConnectionIDs() throws Exception;

   @Operation(desc = "List all producers", impact = MBeanOperationInfo.INFO)
   String listProducersInfoAsJSON() throws Exception;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/management/ActiveMQServerControl.java`
#### Snippet
```java
    * Lists the addresses of the clients connected to this address which matches the specified IP address.
    */
   @Operation(desc = "List the client addresses which match the given IP Address", impact = MBeanOperationInfo.INFO)
   String[] listRemoteAddresses(@Parameter(desc = "an IP address", name = "ipAddress") String ipAddress) throws Exception;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/management/ActiveMQServerControl.java`
#### Snippet
```java
    * Closes the connection with the given id.
    */
   @Operation(desc = "Closes all the connection with the id", impact = MBeanOperationInfo.INFO)
   boolean closeConnectionWithID(@Parameter(desc = "The connection ID", name = "ID") String ID) throws Exception;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/management/ActiveMQServerControl.java`
#### Snippet
```java
    * Closes all the connections of sessions with a matching user name.
    */
   @Operation(desc = "Closes all the connections for sessions with the given user name", impact = MBeanOperationInfo.INFO)
   boolean closeConnectionsForUser(@Parameter(desc = "a user name", name = "userName") String address) throws Exception;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/management/ActiveMQServerControl.java`
#### Snippet
```java
    * Returns the names of the cluster-connections deployed on this server.
    */
   @Operation(desc = "Names of the cluster-connections deployed on this server", impact = MBeanOperationInfo.INFO)
   String[] getClusterConnectionNames();

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/management/ActiveMQServerControl.java`
#### Snippet
```java
    * Lists all the IDs of the connections connected to this server.
    */
   @Operation(desc = "List all the connection IDs", impact = MBeanOperationInfo.INFO)
   String[] listConnectionIDs() throws Exception;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/management/ActiveMQServerControl.java`
#### Snippet
```java
   void removeConnector(@Parameter(name = "name", desc = "the name of the connector to remove") String name) throws Exception;

   @Operation(desc = "List the existing broker connections", impact = MBeanOperationInfo.INFO)
   String listBrokerConnections();

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/management/ActiveMQServerControl.java`
#### Snippet
```java
                          @Parameter(name = "pageSize", desc = "Page Size") int pageSize) throws Exception;

   @Operation(desc = "Search for Sessions", impact = MBeanOperationInfo.INFO)
   String listSessions(@Parameter(name = "options", desc = "Options") String options,
                       @Parameter(name = "pageNumber", desc = "Page Number") int page,
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/management/ActiveMQServerControl.java`
#### Snippet
```java
    * </pre>
    */
   @Operation(desc = "List the sessions for the given connectionID as a JSON string", impact = MBeanOperationInfo.INFO)
   String listSessionsAsJSON(@Parameter(desc = "a connection ID", name = "connectionID") String connectionID) throws Exception;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/management/ActiveMQServerControl.java`
#### Snippet
```java
   void scaleDown(@Parameter(name = "name", desc = "The connector to use to scale down, if not provided the first appropriate connector will be used") String connector) throws Exception;

   @Operation(desc = "List the Network Topology", impact = MBeanOperationInfo.INFO)
   String listNetworkTopology() throws Exception;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/management/ActiveMQServerControl.java`
#### Snippet
```java
    * Closes the consumer with the given id.
    */
   @Operation(desc = "Closes the consumer with the id", impact = MBeanOperationInfo.INFO)
   boolean closeConsumerWithID(@Parameter(desc = "The session ID", name = "sessionID") String sessionID,
                               @Parameter(desc = "The consumer ID", name = "ID") String ID) throws Exception;
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/management/ActiveMQServerControl.java`
#### Snippet
```java
    * Closes all the connections of clients connected to this server which matches the specified IP address.
    */
   @Operation(desc = "Closes all the connections for the given IP Address", impact = MBeanOperationInfo.INFO)
   boolean closeConnectionsForAddress(@Parameter(desc = "an IP address", name = "ipAddress") String ipAddress) throws Exception;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/management/ActiveMQServerControl.java`
#### Snippet
```java
    * Lists the addresses of all the clients connected to this address.
    */
   @Operation(desc = "List the client addresses", impact = MBeanOperationInfo.INFO)
   String[] listRemoteAddresses() throws Exception;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/management/ActiveMQServerControl.java`
#### Snippet
```java
   String listBindingsForAddress(@Parameter(name = "address", desc = "The address") String address) throws Exception;

   @Operation(desc = "List Addresses on the broker", impact = MBeanOperationInfo.INFO)
   String listAddresses(@Parameter(name = "separator", desc = "Separator used on the string listing") String separator) throws Exception;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/management/ActiveMQServerControl.java`
#### Snippet
```java
   String getAddressInfo(@Parameter(name = "address", desc = "The address") String address) throws ActiveMQAddressDoesNotExistException;

   @Operation(desc = "Get a list of bindings associated with an address", impact = MBeanOperationInfo.INFO)
   String listBindingsForAddress(@Parameter(name = "address", desc = "The address") String address) throws Exception;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/management/ActiveMQServerControl.java`
#### Snippet
```java
    * returns the address settings as a JSON string
    */
   @Operation(desc = "Returns the address settings as a JSON string for an address match", impact = MBeanOperationInfo.INFO)
   String getAddressSettingsAsJSON(@Parameter(desc = "an address match", name = "addressMatch") String addressMatch) throws Exception;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/management/ActiveMQServerControl.java`
#### Snippet
```java
                        @Parameter(name = "pageSize", desc = "Page Size") int pageSize) throws Exception;

   @Operation(desc = "Search for Queues", impact = MBeanOperationInfo.INFO)
   String listQueues(@Parameter(name = "options", desc = "Options") String options,
                     @Parameter(name = "pageNumber", desc = "Page Number") int page,
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/management/ActiveMQServerControl.java`
#### Snippet
```java
   Object[] getRoles(@Parameter(desc = "an address match", name = "addressMatch") String addressMatch) throws Exception;

   @Operation(desc = "Get roles (as a JSON string) for a specific address match", impact = MBeanOperationInfo.INFO)
   String getRolesAsJSON(@Parameter(desc = "an address match", name = "addressMatch") String addressMatch) throws Exception;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/management/ActiveMQServerControl.java`
#### Snippet
```java
    * Lists all the sessions IDs for the specified connection ID.
    */
   @Operation(desc = "List the sessions for the given connectionID", impact = MBeanOperationInfo.INFO)
   String[] listSessions(@Parameter(desc = "a connection ID", name = "connectionID") String connectionID) throws Exception;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/management/ActiveMQServerControl.java`
#### Snippet
```java
    * form getXXXX(), so add this one.
    */
   @Operation(desc = "names of the diverts deployed on this server", impact = MBeanOperationInfo.INFO)
   default String[] listDivertNames() {
      return getDivertNames();
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/management/QueueControl.java`
#### Snippet
```java
    * Lists the message counter for this queue as a HTML table.
    */
   @Operation(desc = "List the message counters as HTML", impact = MBeanOperationInfo.INFO)
   String listMessageCounterAsHTML() throws Exception;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/management/QueueControl.java`
#### Snippet
```java
    * Using {@code null} or an empty filter will list <em>all</em> messages from this queue.
    */
   @Operation(desc = "List all the messages in the queue matching the given filter", impact = MBeanOperationInfo.INFO)
   Map<String, Object>[] listMessages(@Parameter(name = "filter", desc = "A message filter (can be empty)") String filter) throws Exception;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/management/QueueControl.java`
#### Snippet
```java
    * Lists all the messages scheduled for delivery for this queue using JSON serialization.
    */
   @Operation(desc = "List the messages scheduled for delivery and returns them using JSON", impact = MBeanOperationInfo.INFO)
   String listScheduledMessagesAsJSON() throws Exception;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/management/QueueControl.java`
#### Snippet
```java
    * Using {@code null} or an empty filter will list <em>all</em> messages from this queue.
    */
   @Operation(desc = "List all the messages in the queue matching the given filter and returns them using JSON", impact = MBeanOperationInfo.INFO)
   String listMessagesAsJSON(@Parameter(name = "filter", desc = "A message filter (can be empty)") String filter) throws Exception;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/management/QueueControl.java`
#### Snippet
```java
    * 1 Map represents 1 message, keys are the message's properties and headers, values are the corresponding values.
    */
   @Operation(desc = "List the messages scheduled for delivery", impact = MBeanOperationInfo.INFO)
   Map<String, Object>[] listScheduledMessages() throws Exception;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/management/QueueControl.java`
#### Snippet
```java
    * Resets the message counter for this queue.
    */
   @Operation(desc = "Reset the message counters", impact = MBeanOperationInfo.INFO)
   void resetMessageCounter() throws Exception;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/management/QueueControl.java`
#### Snippet
```java
    * Lists the message counter for this queue.
    */
   @Operation(desc = "List the message counters", impact = MBeanOperationInfo.INFO)
   String listMessageCounter() throws Exception;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/management/QueueControl.java`
#### Snippet
```java
    * Using {@code null} or an empty filter will count <em>all</em> messages from this queue.
    */
   @Operation(desc = "Returns the number of the messages in the queue matching the given filter, grouped by the given property field", impact = MBeanOperationInfo.INFO)
   String countMessages(@Parameter(name = "filter", desc = "This filter separate account messages") String filter, @Parameter(name = "groupByProperty", desc = "This property to group by") String groupByProperty) throws Exception;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/management/QueueControl.java`
#### Snippet
```java
    */
   @Deprecated
   @Operation(desc = "List the message counters history HTML", impact = MBeanOperationInfo.INFO)
   String listMessageCounterHistoryAsHTML() throws Exception;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/management/QueueControl.java`
#### Snippet
```java
    * Lists the message counter history for this queue.
    */
   @Operation(desc = "List the message counters history", impact = MBeanOperationInfo.INFO)
   String listMessageCounterHistory() throws Exception;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/management/QueueControl.java`
#### Snippet
```java
    * Using {@code null} or an empty filter will count <em>all</em> messages from this queue.
    */
   @Operation(desc = "Returns the number of the messages in the queue matching the given filter", impact = MBeanOperationInfo.INFO)
   long countMessages(@Parameter(name = "filter", desc = "A message filter (can be empty)") String filter) throws Exception;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/management/QueueControl.java`
#### Snippet
```java
   long countMessages(@Parameter(name = "filter", desc = "A message filter (can be empty)") String filter) throws Exception;

   @Operation(desc = "Returns the number of the messages in the queue", impact = MBeanOperationInfo.INFO)
   long countMessages() throws Exception;

```

## RuleId[id=ThrowFromFinallyBlock]
### ThrowFromFinallyBlock
`throw` inside 'finally' block
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ClientProducerImpl.java`
#### Snippet
```java
            input.close();
         } catch (IOException e) {
            throw ActiveMQClientMessageBundle.BUNDLE.errorClosingLargeMessage(e);
         }
      }
```

## RuleId[id=IfStatementWithIdenticalBranches]
### IfStatementWithIdenticalBranches
Common part can be extracted removing branch
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/connect/mirror/AMQPMirrorControllerTarget.java`
#### Snippet
```java

      DuplicateIDCache duplicateIDCache;
      if (lruDuplicateIDKey != null && lruDuplicateIDKey.equals(internalMirrorID)) {
         duplicateIDCache = lruduplicateIDCache;
      } else {
```

### IfStatementWithIdenticalBranches
Common part can be extracted from 'if'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/NettyConnector.java`
#### Snippet
```java
      @Override
      public void write(final ChannelHandlerContext ctx, final Object msg, ChannelPromise promise) throws Exception {
         if (msg instanceof ByteBuf) {
            if (httpRequiresSessionId && !active) {
               if (handshaking) {
```

### IfStatementWithIdenticalBranches
Common part can be extracted from 'if'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/HttpAcceptorHandler.java`
#### Snippet
```java
         }
         while (responseHolder == null);
         if (!bogusResponse) {
            piggyBackResponses(responseHolder.response.content());
            responseHolder.response.headers().set(HttpHeaderNames.CONTENT_LENGTH, String.valueOf(responseHolder.response.content().readableBytes()));
```

## RuleId[id=UnnecessarySuperQualifier]
### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `artemis-protocols/artemis-stomp-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/stomp/v11/StompFrameHandlerV11.java`
#### Snippet
```java

         if (clientAcceptPing != 0) {
            serverPingPeriod = super.getPeriod();
         }

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
         // Encoding suspended?
         if (suspendEncoding) {
            super.out.write(theByte);
            return;
         } // end if: supsended
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
         // Encoding suspended?
         if (suspendEncoding) {
            super.out.write(theBytes, off, len);
            return;
         } // end if: supsended
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/HelpAction.java`
#### Snippet
```java
   @Override
   public Object execute(ActionContext context) throws Exception {
      super.run();
      return null;
   }
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/server/embedded/EmbeddedJMS.java`
#### Snippet
```java
   @Override
   public EmbeddedJMS start() throws Exception {
      super.initStart();
      if (jmsConfiguration != null) {
         serverManager = new JMSServerManagerImpl(activeMQServer, jmsConfiguration);
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenWireProtocolManager.java`
#### Snippet
```java

   public String invokeIncoming(Command command, OpenWireConnection connection) {
      return super.invokeInterceptors(this.incomingInterceptors, command, connection);
   }

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenWireProtocolManager.java`
#### Snippet
```java

   public String invokeOutgoing(Command command, OpenWireConnection connection) {
      return super.invokeInterceptors(this.outgoingInterceptors, command, connection);
   }

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTProtocolManager.java`
#### Snippet
```java

   public String invokeIncoming(MqttMessage mqttMessage, MQTTConnection connection) {
      return super.invokeInterceptors(this.incomingInterceptors, mqttMessage, connection);
   }

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTProtocolManager.java`
#### Snippet
```java

   public String invokeOutgoing(MqttMessage mqttMessage, MQTTConnection connection) {
      return super.invokeInterceptors(this.outgoingInterceptors, mqttMessage, connection);
   }

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQTopic.java`
#### Snippet
```java
      ActiveMQTopic that = (ActiveMQTopic) o;

      return super.getAddress().equals(that.getAddress());
   }

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQTopic.java`
#### Snippet
```java
   @Override
   public int hashCode() {
      return super.getAddress().hashCode();
   }

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQTemporaryQueue.java`
#### Snippet
```java
   @Override
   public int hashCode() {
      return super.getAddress().hashCode();
   }

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQTemporaryQueue.java`
#### Snippet
```java
      ActiveMQTemporaryQueue that = (ActiveMQTemporaryQueue) o;

      return super.getAddress().equals(that.getAddress());
   }

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQTemporaryTopic.java`
#### Snippet
```java
   @Override
   public int hashCode() {
      return super.getAddress().hashCode();
   }

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQTemporaryTopic.java`
#### Snippet
```java
      ActiveMQTemporaryTopic that = (ActiveMQTemporaryTopic) o;

      return super.getAddress().equals(that.getAddress());
   }

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQQueue.java`
#### Snippet
```java
   @Override
   public int hashCode() {
      return super.getAddress().hashCode();
   }

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQQueue.java`
#### Snippet
```java
      ActiveMQQueue that = (ActiveMQQueue) o;

      return super.getAddress().equals(that.getAddress());
   }

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/ProtonProtocolManager.java`
#### Snippet
```java

   public String invokeIncoming(AMQPMessage message, ActiveMQProtonRemotingConnection connection) {
      return super.invokeInterceptors(this.incomingInterceptors, message, connection);
   }

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/ProtonProtocolManager.java`
#### Snippet
```java

   public String invokeOutgoing(AMQPMessage message, ActiveMQProtonRemotingConnection connection) {
      return super.invokeInterceptors(this.outgoingInterceptors, message, connection);
   }

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `artemis-protocols/artemis-hornetq-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/hornetq/HornetQProtocolManager.java`
#### Snippet
```java
   @Override
   public String toString() {
      return "HornetQProtocolManager(server=" + super.server + ")";
   }
}
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionSendMessage_V2.java`
#### Snippet
```java
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", correlationID=" + correlationID);
      buff.append(", requiresResponse=" + super.isRequiresResponse());
      return buff.toString();
   }
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ClientSessionFactoryImpl.java`
#### Snippet
```java
      public String toString() {
         return DelegatingFailureListener.class.getSimpleName() + "('reconnectsOrFailover', hash=" +
            super.hashCode() + ")";
      }
   }
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/impl/RoleSet.java`
#### Snippet
```java
   // provide a helper add method with the type
   public void add(String name, Role value) {
      super.add(value);
   }

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/impl/ClusterConnectionBridge.java`
#### Snippet
```java
      messageCopy.putExtraBytesProperty(Message.HDR_ROUTE_TO_IDS, queueIds);

      messageCopy = super.beforeForwardingNoCopy(messageCopy, forwardingAddress);

      return messageCopy;
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/protocol/ServerPacketDecoder.java`
#### Snippet
```java
         }
         default: {
            packet = super.decode(packetType, connection);
         }
      }
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/NettyAcceptor.java`
#### Snippet
```java
                                                    boolean httpEnabled) throws Exception {
         if (connectionsAllowed == -1 || connections.size() < connectionsAllowed) {
            super.channelActive(ctx);
            Listener connectionListener = new Listener();

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/LargeServerMessageImpl.java`
#### Snippet
```java

   public void encode(final ActiveMQBuffer buffer1) {
      super.encodeHeadersAndProperties(buffer1.byteBuf());
   }

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/LargeServerMessageImpl.java`
#### Snippet
```java
   public void decode(final ActiveMQBuffer buffer1) {
      largeBody.clearFile();
      super.decodeHeadersAndProperties(buffer1.byteBuf());
   }

```

## RuleId[id=InfiniteLoopStatement]
### InfiniteLoopStatement
`while` statement cannot complete without throwing an exception
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQStreamMessage.java`
#### Snippet
```java

      try {
         while (true) {
            Object obj = foreign.readObject();
            writeObject(obj);
```

### InfiniteLoopStatement
`while` statement cannot complete without throwing an exception
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/converter/coreWrapper/CoreStreamMessageWrapper.java`
#### Snippet
```java
      ArrayList<Object> list = new ArrayList<>();
      try {
         while (true) {
            list.add(readObject());
         }
```

## RuleId[id=CodeBlock2Expr]
### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `artemis-maven-plugin/src/main/java/org/apache/activemq/artemis/maven/ArtemisDependencyDocPlugin.java`
#### Snippet
```java

               if (getLog().isDebugEnabled()) {
                  filter.forEach((a, b) -> {
                     getLog().debug("filter.put(" + a + ", " + b + ")");
                  });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Upgrade.java`
#### Snippet
```java
      if (originalConsumer != null) {
         try (Stream<String> originalLines = Files.lines(targetFile.toPath())) {
            originalLines.forEach(line -> {
               originalConsumer.accept(line);
            });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/perf/MessageListenerBenchmark.java`
#### Snippet
```java
               }
            }
            connection.setExceptionListener(ignore -> {
               signalBrokenConnection.set(true);
            });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalImpl.java`
#### Snippet
```java

               // Restore newRecords created during compacting
               localCompactor.getNewRecords().forEach((id, newRecord) -> {
                  records.put(id, newRecord);
               });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/handler/ProtonHandler.java`
#### Snippet
```java
      // after flushed, we close the local connection
      // otherwise this could close the netty connection before the Writable is complete
      runLater(() -> {
         connectionContext.getConnectionCallback().getTransportConnection().close();
      });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `artemis-jdbc-store/src/main/java/org/apache/activemq/artemis/jdbc/store/file/JDBCSequentialFile.java`
#### Snippet
```java

   private void scheduleWrite(final ActiveMQBuffer bytes, final IOCallback callback, boolean append) {
      executor.execute(() -> {
         internalWrite(bytes, callback, append);
      });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `artemis-jdbc-store/src/main/java/org/apache/activemq/artemis/jdbc/store/file/JDBCSequentialFile.java`
#### Snippet
```java

   private void scheduleWrite(final ByteBuffer bytes, final IOCallback callback) {
      executor.execute(() -> {
         internalWrite(bytes, callback);
      });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/ResponseCache.java`
#### Snippet
```java
   public void errorAll(ActiveMQException exception) {
      ConcurrentLongHashSet keys = store.keysLongHashSet();
      keys.forEach(correlationID -> {
         handleResponse(new ActiveMQExceptionMessage_V2(correlationID, exception));
      });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/NettyConnection.java`
#### Snippet
```java
         closeChannel(channel, false);
      } else {
         eventLoop.execute(() -> {
            closeChannel(channel, true);
         });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/impl/PagingManagerImpl.java`
#### Snippet
```java
   public Future<Object> rebuildCounters(Set<Long> storedLargeMessages) {
      LongHashSet transactionsSet = new LongHashSet();
      transactions.forEach((txId, tx) -> {
         transactionsSet.add(txId);
      });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/cursor/impl/PageCursorProviderImpl.java`
#### Snippet
```java
      int subscriptions = cursorList.size();

      cursorList.forEach(sub -> {
         sub.forEachConsumedPage(consumedPage -> {
            if (consumedPage.isDone()) {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ActiveMQServerImpl.java`
#### Snippet
```java
         pagingManager.rebuildCounters(storedLargeMessages);

         pagingManager.execute(() -> {
            storedLargeMessages.forEach(id -> {
               try {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ActiveMQServerImpl.java`
#### Snippet
```java
         File optionalJaasConfigBaseDir = PropertiesLoader.FileNameKey.parentDirOfLoginConfigSystemProperty();
         if (optionalJaasConfigBaseDir != null) {
            reloadManager.addCallback(optionalJaasConfigBaseDir.toURI().toURL(), (uri) -> {
               PropertiesLoader.reload();
            });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/federation/queue/FederatedQueue.java`
#### Snippet
```java
         final AtomicBoolean conditionalCreate = new AtomicBoolean(true);
         try {
            server.callBrokerFederationPlugins(plugin -> {
               conditionalCreate.set(conditionalCreate.get() && plugin.federatedQueueConditionalCreateConsumer(consumer));
            });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/federation/address/FederatedAddress.java`
#### Snippet
```java
         final AtomicBoolean conditionalCreate = new AtomicBoolean(true);
         try {
            server.callBrokerFederationPlugins(plugin -> {
               conditionalCreate.set(conditionalCreate.get() && plugin.federatedAddressConditionalCreateDivertConsumer(divertBinding, queueBinding));
            });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/federation/address/FederatedAddress.java`
#### Snippet
```java
         final AtomicBoolean conditionalCreate = new AtomicBoolean(true);
         try {
            server.callBrokerFederationPlugins(plugin -> {
               conditionalCreate.set(conditionalCreate.get() && plugin.federatedAddressConditionalCreateConsumer(queue));
            });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/CoreProtocolManager.java`
#### Snippet
```java
            //Register close and failure listeners, if the initial downstream connection goes down then we
            //want to terminate the upstream connection
            rc.addCloseListener(() -> {
               server.getFederationManager().undeploy(config.getName());
            });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/JournalStorageManager.java`
#### Snippet
```java
   public void recoverLargeMessagesOnFolder(Set<Long> storedLargeMessages) throws Exception {
      List<String> filenames = largeMessagesFactory.listFiles("msg");
      filenames.forEach(f -> {
         storedLargeMessages.add(getLargeMessageIdFromFilename(f));
      });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/DescribeJournal.java`
#### Snippet
```java
      out.println("large message count=" + largeMessageCount);
      out.println("message reference count");
      messageRefCounts.forEach((queueId, count) -> {
         out.println("queue id " + queueId + ",count=" + count);
      });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/DescribeJournal.java`
#### Snippet
```java
      out.println("prepared large message count=" + preparedLargeMessageCount);
      out.println("prepared message reference count");
      preparedMessageRefCount.forEach((queueId, count) -> {
         out.println("queue id " + queueId + ",count=" + count);
      });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/ActiveMQServerControlImpl.java`
#### Snippet
```java
         synchronized (userLock) {
            return (String) tcclCall(ActiveMQServerControlImpl.class.getClassLoader(), () -> {
               return buildJsonUserList(getPropertiesLoginModuleConfigurator().listUser(username));
            });
         }
```

## RuleId[id=UseOfPropertiesAsHashtable]
### UseOfPropertiesAsHashtable
Call to `Hashtable.get()` on properties object
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/ClassloadingUtil.java`
#### Snippet
```java
      Properties properties = loadProperties(loader, propertiesFile);

      return (String)properties.get(name);
   }

```

### UseOfPropertiesAsHashtable
Call to `Hashtable.get()` on properties object
in `artemis-unit-test-support/src/main/java/org/apache/activemq/artemis/utils/CleanupSystemPropertiesRule.java`
#### Snippet
```java
      HashSet newProperties = new HashSet();
      for (Map.Entry<Object, Object> entry : System.getProperties().entrySet()) {
         Object originalValue = originalProperties.get(entry.getKey());
         if (originalValue == null) {
            newProperties.add(entry.getKey());
```

## RuleId[id=EmptyMethod]
### EmptyMethod
Method only calls its super
in `artemis-commons/src/main/java/org/apache/activemq/artemis/json/impl/JsonValueImpl.java`
#### Snippet
```java

   @Override
   public JsonObject asJsonObject() {
      return JsonValue.super.asJsonObject();
   }
```

### EmptyMethod
Method only calls its super
in `artemis-commons/src/main/java/org/apache/activemq/artemis/json/impl/JsonValueImpl.java`
#### Snippet
```java

   @Override
   public JsonArray asJsonArray() {
      return JsonValue.super.asJsonArray();
   }
```

### EmptyMethod
The method is empty
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ClientSessionFactoryImpl.java`
#### Snippet
```java
   }

   private void checkTransportKeys(final ConnectorFactory factory, final TransportConfiguration tc) {
   }

```

### EmptyMethod
All implementations of this method are empty
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/spi/core/remoting/SessionContext.java`
#### Snippet
```java
   public abstract void cleanup();

   public abstract void linkFlowControl(SimpleString address, ClientProducerCredits clientProducerCredits);

   public abstract boolean isWritable(ReadyListener callback);
```

### EmptyMethod
The method is empty
in `artemis-jdbc-store/src/main/java/org/apache/activemq/artemis/jdbc/store/file/JDBCSequentialFileFactoryDriver.java`
#### Snippet
```java
   }

   void removeFile(JDBCSequentialFile file) {

   }
```

### EmptyMethod
The method is empty
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQConnectionFactory.java`
#### Snippet
```java

   @Deprecated
   public synchronized void setFailoverOnInitialConnection(final boolean failover) {
   }

```

### EmptyMethod
The method is empty
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/io/aio/AIOSequentialFileFactory.java`
#### Snippet
```java
   private static final String AIO_TEST_FILE = ".aio-test";

   public void beforeClose() {
   }

```

### EmptyMethod
The method is empty
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/io/aio/AIOSequentialFileFactory.java`
#### Snippet
```java
   }

   public void afterClose() {
   }

```

### EmptyMethod
The method and all its derivables are empty
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalImpl.java`
#### Snippet
```java
    * as soon as the compactor gets a writeLock
    */
   protected void onCompactLockingTheJournal() throws Exception {
   }

```

### EmptyMethod
The method is empty
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPSessionCallback.java`
#### Snippet
```java
   }

   public void start() {

   }
```

### EmptyMethod
The method and all its derivables are empty
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/ProtonProtocolManager.java`
#### Snippet
```java

   @Override
   public void addChannelHandlers(ChannelPipeline pipeline) {

   }
```

### EmptyMethod
The method is empty
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/connect/AMQPBrokerConnection.java`
#### Snippet
```java
   }

   private static void uninstallMirrorController(AMQPMirrorBrokerConnectionElement replicaConfig, ActiveMQServer server) {
      // TODO implement this as part of https://issues.apache.org/jira/browse/ARTEMIS-2965
   }
```

### EmptyMethod
The method is empty
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/connect/AMQPBrokerConnectionManager.java`
#### Snippet
```java
   }

   public void connected(NettyConnection nettyConnection, AMQPBrokerConnection bridgeConnection) {
   }

```

### EmptyMethod
Method only calls its super
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/converter/coreWrapper/CoreBytesMessageWrapper.java`
#### Snippet
```java

   @Override
   public void decode()  {
      super.decode();

```

### EmptyMethod
Method only calls its super
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/converter/coreWrapper/CoreStreamMessageWrapper.java`
#### Snippet
```java

   @Override
   public void decode() {
      super.decode();
   }
```

### EmptyMethod
The method is empty
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/AMQPConnectionContext.java`
#### Snippet
```java
   }

   protected void initInternal() throws Exception {
   }

```

### EmptyMethod
Method only calls its super
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/transaction/ProtonTransactionImpl.java`
#### Snippet
```java

   @Override
   public void rollback() throws Exception {
      super.rollback();
   }
```

### EmptyMethod
Method only calls its super
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/transaction/ProtonTransactionImpl.java`
#### Snippet
```java

   @Override
   public void commit() throws Exception {
      super.commit();
   }
```

### EmptyMethod
The method is empty
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenWireProtocolManager.java`
#### Snippet
```java
   }

   public void postProcessDispatch(MessageDispatch md) {
      // TODO Auto-generated method stub

```

### EmptyMethod
The method is empty
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenWireProtocolManager.java`
#### Snippet
```java
   }

   public void preProcessDispatch(MessageDispatch messageDispatch) {
      // TODO Auto-generated method stub

```

### EmptyMethod
The method is empty
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/amq/AMQProducerBrokerExchange.java`
#### Snippet
```java
   }

   public void setLastStoredSequenceId(long l) {
   }
}
```

### EmptyMethod
All implementations of this method are empty
in `artemis-protocols/artemis-stomp-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/stomp/FrameEventListener.java`
#### Snippet
```java
   void replySent(StompFrame reply);

   void requestAccepted(StompFrame request);

}
```

### EmptyMethod
The method is empty
in `artemis-quorum-ri/src/main/java/org/apache/activemq/artemis/quorum/zookeeper/CuratorDistributedPrimitive.java`
#### Snippet
```java
   }

   protected void handleSuspended() {

   }
```

### EmptyMethod
The method is empty
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/ActiveMQResourceAdapter.java`
#### Snippet
```java
    */
   @Deprecated
   public void setFailoverOnInitialConnection(final Boolean failoverOnInitialConnection) {
   }

```

### EmptyMethod
The method is empty
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/ConnectionFactoryProperties.java`
#### Snippet
```java

   @Deprecated
   public void setFailoverOnInitialConnection(Boolean failoverOnInitialConnection) {
   }

```

### EmptyMethod
The method is empty
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/inflow/ActiveMQActivationSpec.java`
#### Snippet
```java

   // here for backwards compatibility
   public void setUseDLQ(final Boolean b) {
   }

```

### EmptyMethod
The method is empty
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/inflow/ActiveMQActivationSpec.java`
#### Snippet
```java
   }

   public void setDLQJNDIName(final String name) {
   }

```

### EmptyMethod
The method is empty
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/inflow/ActiveMQActivationSpec.java`
#### Snippet
```java
   }

   public void setDLQHandler(final String handler) {
   }

```

### EmptyMethod
The method is empty
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/inflow/ActiveMQActivationSpec.java`
#### Snippet
```java
   }

   public void setDLQMaxResent(final Integer maxResent) {
   }

```

### EmptyMethod
The method is empty
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/inflow/ActiveMQActivationSpec.java`
#### Snippet
```java
   }

   public void setProviderAdapterJNDI(final String jndi) {
   }

```

### EmptyMethod
The method is empty
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/inflow/ActiveMQActivationSpec.java`
#### Snippet
```java
    * @param keepAlive the keepAlive to set
    */
   public void setKeepAlive(Boolean keepAlive) {
   }

```

### EmptyMethod
The method is empty
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/inflow/ActiveMQActivationSpec.java`
#### Snippet
```java
    * @param keepAliveMillis the keepAliveMillis to set
    */
   public void setKeepAliveMillis(Long keepAliveMillis) {
   }

```

### EmptyMethod
The method is empty
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/inflow/ActiveMQActivationSpec.java`
#### Snippet
```java
   }

   public void setReconnectInterval(Long interval) {
   }

```

### EmptyMethod
The method is empty
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/inflow/ActiveMQActivationSpec.java`
#### Snippet
```java
   }

   public void setMinSession(final Integer value) {
   }

```

### EmptyMethod
The method is empty
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/inflow/ActiveMQActivationSpec.java`
#### Snippet
```java
   }

   public void setMaxMessages(final Integer value) {
   }

```

### EmptyMethod
The method is empty
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/impl/LRUCache.java`
#### Snippet
```java
   }

   protected void onCacheEviction(Map.Entry<K, V> eldest) {
   }
}
```

### EmptyMethod
Method only calls its super
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/impl/Validators.java`
#### Snippet
```java
   public static final Validator NO_CHECK = new Validator() {
      @Override
      public void validate(final String name, final Object value) {
         return;
      }
```

### EmptyMethod
All implementations of this method are empty
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/cursor/PageSubscription.java`
#### Snippet
```java

   // To be called before the server is down
   void stop();

   /** Save a snapshot of the current counter value in the journal */
```

### EmptyMethod
The method is empty
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/ha/ReplicaPolicy.java`
#### Snippet
```java

   @Deprecated
   public void setFailbackDelay(long failbackDelay) {
   }

```

### EmptyMethod
The method is empty
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/ha/ReplicatedPolicy.java`
#### Snippet
```java

   @Deprecated
   public void setFailbackDelay(long failbackDelay) {
   }

```

### EmptyMethod
The method is empty
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/ha/SharedStoreMasterPolicy.java`
#### Snippet
```java

   @Deprecated
   public void setFailbackDelay(long failbackDelay) {
   }

```

### EmptyMethod
The method is empty
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/ha/SharedStoreSlavePolicy.java`
#### Snippet
```java

   @Deprecated
   public void setFailbackDelay(long failbackDelay) {
   }

```

### EmptyMethod
All implementations of this method are empty
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/qourum/Quorum.java`
#### Snippet
```java
    * called by the quorum when a node in the quorum goes up
    */
   void nodeUp(Topology topology);

   /**
```

### EmptyMethod
The method is empty
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/LegacyLDAPSecuritySettingPlugin.java`
#### Snippet
```java
    * @param namingEvent the renaming entry event that occurred
    */
   public void objectRenamed(NamingEvent namingEvent) {

   }
```

### EmptyMethod
Empty method overrides empty method
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ReplicationBackupActivation.java`
#### Snippet
```java

   @Override
   public void preStorageClose() throws Exception {
      // TODO replication endpoint close?
   }
```

### EmptyMethod
All implementations of this method are empty
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/routing/targets/TargetListener.java`
#### Snippet
```java

public interface TargetListener {
   void targetConnected();

   void targetDisconnected();
```

### EmptyMethod
All implementations of this method are empty
in `artemis-service-extensions/src/main/java/org/apache/activemq/artemis/service/extensions/xa/recovery/ActiveMQRegistry.java`
#### Snippet
```java
public interface ActiveMQRegistry {

   void register(XARecoveryConfig resourceConfig);

   void unRegister(XARecoveryConfig resourceConfig);
```

### EmptyMethod
All implementations of this method are empty
in `artemis-service-extensions/src/main/java/org/apache/activemq/artemis/service/extensions/xa/recovery/ActiveMQRegistry.java`
#### Snippet
```java
   void register(XARecoveryConfig resourceConfig);

   void unRegister(XARecoveryConfig resourceConfig);

   void stop();
```

### EmptyMethod
All implementations of this method are empty
in `artemis-service-extensions/src/main/java/org/apache/activemq/artemis/service/extensions/xa/recovery/ActiveMQRegistry.java`
#### Snippet
```java
   void unRegister(XARecoveryConfig resourceConfig);

   void stop();

   void init();
```

### EmptyMethod
All implementations of this method are empty
in `artemis-service-extensions/src/main/java/org/apache/activemq/artemis/service/extensions/xa/recovery/ActiveMQRegistry.java`
#### Snippet
```java
   void stop();

   void init();
}

```

## RuleId[id=RedundantFieldInitialization]
### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-cdi-client/src/main/java/org/apache/artemis/client/cdi/extension/ArtemisExtension.java`
#### Snippet
```java
public class ArtemisExtension implements Extension {

   private boolean foundEmbeddedConfig = false;
   private boolean foundConfiguration = false;

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-cdi-client/src/main/java/org/apache/artemis/client/cdi/extension/ArtemisExtension.java`
#### Snippet
```java

   private boolean foundEmbeddedConfig = false;
   private boolean foundConfiguration = false;

   void foundClientConfig(@Observes ProcessBean<?> processBean) {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-service-extensions/src/main/java/org/apache/activemq/artemis/service/extensions/ServiceUtils.java`
#### Snippet
```java
   private static TransactionManager transactionManager;

   private static boolean transactionManagerLoaded = false;

   private static ActiveMQXAResourceWrapperFactory getActiveMQXAResourceWrapperFactory() {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-maven-plugin/src/main/java/org/apache/activemq/artemis/maven/ArtemisDependencyScanPlugin.java`
#### Snippet
```java

   @Parameter
   private boolean optional = false;

   @Override
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-maven-plugin/src/main/java/org/apache/activemq/artemis/maven/ArtemisCLIPlugin.java`
#### Snippet
```java

   @Parameter
   private String testURI = null;

   @Parameter
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-maven-plugin/src/main/java/org/apache/activemq/artemis/maven/ArtemisCLIPlugin.java`
#### Snippet
```java

   @Parameter
   private boolean spawn = false;

   @Parameter(defaultValue = "300000")
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-maven-plugin/src/main/java/org/apache/activemq/artemis/maven/ArtemisCLIPlugin.java`
#### Snippet
```java

   @Parameter
   private String testPassword = null;

   @Parameter boolean useSystemOutput = getLog().isDebugEnabled();
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-maven-plugin/src/main/java/org/apache/activemq/artemis/maven/ArtemisCLIPlugin.java`
#### Snippet
```java

   @Parameter
   private String testUser = null;

   @Parameter
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-maven-plugin/src/main/java/org/apache/activemq/artemis/maven/ArtemisCLIPlugin.java`
#### Snippet
```java

   @Parameter
   private String testClientID = null;

   @Parameter
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-maven-plugin/src/main/java/org/apache/activemq/artemis/maven/ArtemisCreatePlugin.java`
#### Snippet
```java

   @Parameter(defaultValue = "0")
   private int portOffset = 0;

   @Parameter(defaultValue = "true")
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-hawtio/activemq-branding/src/main/java/org/apache/activemq/hawtio/branding/PluginContextListener.java`
#### Snippet
```java
   private static final Logger logger = LoggerFactory.getLogger(MethodHandles.lookup().lookupClass());

   HawtioPlugin plugin = null;

   @Override
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/XPathExpression.java`
#### Snippet
```java
   private static DocumentBuilder builder;

   public static XPathEvaluatorFactory XPATH_EVALUATOR_FACTORY = null;
   public static final String DOCUMENT_BUILDER_FACTORY_FEATURE_PREFIX = "org.apache.activemq.documentBuilderFactory.feature:";

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/ArithmeticExpression.java`
#### Snippet
```java
   protected static final int LONG = 2;
   protected static final int DOUBLE = 3;
   boolean convertStringExpressions = false;

   /**
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/ComparisonExpression.java`
#### Snippet
```java
   public static final ThreadLocal<Boolean> CONVERT_STRING_EXPRESSIONS = new ThreadLocal<>();

   boolean convertStringExpressions = false;
   private static final Set<Character> REGEXP_CONTROL_CHARS = new HashSet<>();

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-protocols/artemis-stomp-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/stomp/StompFrame.java`
#### Snippet
```java
   protected byte[] bytesBody;

   protected ActiveMQBuffer buffer = null;

   protected int size;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-protocols/artemis-stomp-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/stomp/StompSession.java`
#### Snippet
```java
   private volatile boolean noLocal = false;

   private boolean txPending = false;

   public synchronized void begin() {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-protocols/artemis-stomp-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/stomp/StompSession.java`
#### Snippet
```java
   private final Map<Long, Pair<Long, Integer>> messagesToAck = new ConcurrentHashMap<>();

   private volatile boolean noLocal = false;

   private boolean txPending = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-protocols/artemis-stomp-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/stomp/StompConnection.java`
#### Snippet
```java
   private boolean valid;

   private boolean destroyed = false;

   private final Acceptor acceptorUsed;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-protocols/artemis-stomp-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/stomp/v12/StompFrameHandlerV12.java`
#### Snippet
```java
   protected class StompDecoderV12 extends StompDecoderV11 {

      protected boolean nextEOLChar = false;

      public StompDecoderV12(StompFrameHandlerV12 handler) {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-protocols/artemis-stomp-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/stomp/v11/StompFrameHandlerV11.java`
#### Snippet
```java
      long serverPingPeriod = 0;
      long clientPingResponse;
      volatile boolean shutdown = false;
      AtomicLong lastPingTimestamp = new AtomicLong(0);
      ConnectionEntry connectionEntry;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-protocols/artemis-stomp-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/stomp/v11/StompFrameHandlerV11.java`
#### Snippet
```java
      private static final int MIN_SERVER_PING = 500;

      long serverPingPeriod = 0;
      long clientPingResponse;
      volatile boolean shutdown = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-protocols/artemis-stomp-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/stomp/v11/StompFrameHandlerV11.java`
#### Snippet
```java
   protected class StompDecoderV11 extends StompDecoder {

      protected boolean isEscaping = false;
      protected SimpleBytes holder = new SimpleBytes(1024);

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/SimpleFutureImpl.java`
#### Snippet
```java
   private final CountDownLatch latch = new CountDownLatch(1);

   boolean canceled = false;

   @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-commons/src/main/java/org/apache/activemq/artemis/core/server/NetworkHealthCheck.java`
#### Snippet
```java

   // To be used on tests. As we use the loopback as a valid address on tests.
   private boolean ignoreLoopback = false;

   private boolean ownShutdown = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-commons/src/main/java/org/apache/activemq/artemis/core/server/NetworkHealthCheck.java`
#### Snippet
```java
   private boolean ignoreLoopback = false;

   private boolean ownShutdown = false;

   /**
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/UUID.java`
#### Snippet
```java
   private transient String mDesc = null;

   private transient int mHashCode = 0;

   /**
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/UUID.java`
#### Snippet
```java

   // Both string presentation and hash value may be cached...
   private transient String mDesc = null;

   private transient int mHashCode = 0;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Env.java`
#### Snippet
```java
    * or a few cases we need to know as warn on the testsuite and as log in production.
    */
   private static boolean testEnv = false;

   private static final String OS = System.getProperty("os.name").toLowerCase();
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/SpawnedVMSupport.java`
#### Snippet
```java
public class SpawnedVMSupport {

   static ConcurrentHashMap<Process, String> startedProcesses = null;

   public static Process spawnVM(final String className, final String... args) throws Exception {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/SizeAwareMetric.java`
#### Snippet
```java
   private boolean sizeEnabled = false;

   private boolean elementsEnabled = false;

   private AddCallback onSizeCallback;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/SizeAwareMetric.java`
#### Snippet
```java
   private long lowerMarkSize;

   private boolean sizeEnabled = false;

   private boolean elementsEnabled = false;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/ActiveMQThreadPoolExecutor.java`
#### Snippet
```java
      // The delta is decremented if a thread leaves idle state or if a task is added to the queue.
      private static final AtomicIntegerFieldUpdater<ThreadPoolQueue> DELTA_UPDATER = AtomicIntegerFieldUpdater.newUpdater(ThreadPoolQueue.class, "threadTaskDelta");
      private volatile int threadTaskDelta = 0;

      public void setExecutor(ActiveMQThreadPoolExecutor executor) {
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/ActiveMQThreadPoolExecutor.java`
#### Snippet
```java
   private static class ThreadPoolQueue extends LinkedBlockingQueue<Runnable> {

      private ActiveMQThreadPoolExecutor executor = null;

      // keep track of the difference between the number of idle threads and
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/UUIDGenerator.java`
#### Snippet
```java
    * Random-generator, used by various UUID-generation methods:
    */
   private Random mRnd = null;

   private final Object mTimerLock = new Object();
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/UUIDGenerator.java`
#### Snippet
```java
   private final Object mTimerLock = new Object();

   private UUIDTimer mTimer = null;

   private byte[] address;
```

### RedundantFieldInitialization
Field initialization to `0L` is redundant
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/UUIDTimer.java`
#### Snippet
```java
    * to be monotonically increasing.
    */
   private long mLastSystemTimestamp = 0L;

   /**
```

### RedundantFieldInitialization
Field initialization to `0L` is redundant
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/UUIDTimer.java`
#### Snippet
```java
    * points t1 and t2 (where t2 is after t1), t1 <= t2 will always hold true.
    */
   private long mLastUsedTimestamp = 0L;

   /**
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/UUIDTimer.java`
#### Snippet
```java
    * Counter used to compensate inadequate resolution of JDK system timer.
    */
   private int mClockCounter = 0;

   UUIDTimer(final Random rnd) {
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/ReferenceCounterUtil.java`
#### Snippet
```java
   private final Executor executor;

   private volatile int use = 0;

   private static final AtomicIntegerFieldUpdater<ReferenceCounterUtil> useUpdater = AtomicIntegerFieldUpdater.newUpdater(ReferenceCounterUtil.class, "use");
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/UTF8Util.java`
#### Snippet
```java
      private char[] charBuffer = null;

      private byte[] byteBuffer = null;

      public char[] borrowCharBuffer(final int newSize) {
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/UTF8Util.java`
#### Snippet
```java
   private static final class StringUtilBuffer {

      private char[] charBuffer = null;

      private byte[] byteBuffer = null;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/actors/HandlerBase.java`
#### Snippet
```java

   private static class Counter {
      int count = 0;
   }

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/actors/ProcessorBase.java`
#### Snippet
```java
   private volatile int state = STATE_NOT_RUNNING;
   // Request of forced shutdown
   private volatile boolean requestedForcedShutdown = false;
   // Request of educated shutdown:
   private volatile boolean requestedShutdown = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/actors/ProcessorBase.java`
#### Snippet
```java
   private volatile boolean requestedShutdown = false;
   // Request to yield to another thread
   private volatile boolean yielded = false;

   private static final AtomicIntegerFieldUpdater<ProcessorBase> stateUpdater = AtomicIntegerFieldUpdater.newUpdater(ProcessorBase.class, "state");
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/actors/ProcessorBase.java`
#### Snippet
```java
   private volatile boolean requestedForcedShutdown = false;
   // Request of educated shutdown:
   private volatile boolean requestedShutdown = false;
   // Request to yield to another thread
   private volatile boolean yielded = false;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/actors/ThresholdActor.java`
#### Snippet
```java

   private static final AtomicIntegerFieldUpdater<ThresholdActor> SIZE_UPDATER = AtomicIntegerFieldUpdater.newUpdater(ThresholdActor.class, "size");
   private volatile int size = 0;

   private static final AtomicIntegerFieldUpdater<ThresholdActor> SCHEDULED_FLUSH_UPDATER = AtomicIntegerFieldUpdater.newUpdater(ThresholdActor.class, "scheduledFlush");
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/actors/ThresholdActor.java`
#### Snippet
```java

   private static final AtomicIntegerFieldUpdater<ThresholdActor> SCHEDULED_FLUSH_UPDATER = AtomicIntegerFieldUpdater.newUpdater(ThresholdActor.class, "scheduledFlush");
   private volatile int scheduledFlush = 0;

   private static final Object FLUSH = new Object();
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/ArrayResettableIterator.java`
#### Snippet
```java

   private final Object[] array;
   private int cursor = 0;
   private int endPos = -1;
   private boolean hasNext;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/PriorityLinkedListImpl.java`
#### Snippet
```java
      private int resetCount = lastReset;

      volatile boolean closed = false;

      PriorityLinkedListIterator() {
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/ConcurrentAppendOnlyChunkedList.java`
#### Snippet
```java
   private final int chunkSizeLog2;

   private AtomicChunk<E> firstBuffer = null;

   private AtomicChunk<E> lastBuffer = null;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/ConcurrentAppendOnlyChunkedList.java`
#### Snippet
```java
   //it is both the current index of the next element to be claimed and the current size of the collection
   //it's using a parity bit to mark the rotation state ie size === lastIndex >> 1
   private volatile long lastIndex = 0;

   /**
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/ConcurrentAppendOnlyChunkedList.java`
#### Snippet
```java
   private static final class AtomicChunk<E> extends AtomicReferenceArray<E> {

      AtomicChunk<E> next = null;
      final AtomicChunk<E> prev;
      final int index;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/ConcurrentAppendOnlyChunkedList.java`
#### Snippet
```java
   private AtomicChunk<E> firstBuffer = null;

   private AtomicChunk<E> lastBuffer = null;

   //it is both the current index of the next element to be claimed and the current size of the collection
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/LongHashSet.java`
#### Snippet
```java
      private int positionCounter;
      private int stopCounter;
      private boolean isPositionValid = false;

      LongIterator reset() {
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/io/AbstractSequentialFile.java`
#### Snippet
```java
   protected final SequentialFileFactory factory;

   protected long fileSize = 0;

   protected final AtomicLong position = new AtomicLong(0);
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/io/aio/AIOSequentialFile.java`
#### Snippet
```java
    * This is accessed from a single thread (the Poller Thread)
    */
   private long nextReadSequence = 0;

   public AIOSequentialFile(final AIOSequentialFileFactory factory,
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/io/aio/AIOSequentialFile.java`
#### Snippet
```java
   private static final Logger logger = LoggerFactory.getLogger(MethodHandles.lookup().lookupClass());

   private boolean opened = false;

   private LibaioFile aioFile;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/io/buffer/TimedBuffer.java`
#### Snippet
```java
   private class CheckTimer implements Runnable {

      int checks = 0;
      int failedChecks = 0;
      private volatile boolean closed = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/io/buffer/TimedBuffer.java`
#### Snippet
```java

   // no need to be volatile as every access is synchronized
   private boolean spinning = false;


```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/io/buffer/TimedBuffer.java`
#### Snippet
```java
   private List<IOCallback> callbacks;
   // used to measure sync requests. When a sync is requested, it shouldn't take more than timeout to happen
   private volatile boolean pendingSync = false;

   // for logging write rates
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/io/buffer/TimedBuffer.java`
#### Snippet
```java

      int checks = 0;
      int failedChecks = 0;
      private volatile boolean closed = false;

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/io/buffer/TimedBuffer.java`
#### Snippet
```java
      int checks = 0;
      int failedChecks = 0;
      private volatile boolean closed = false;

      @Override
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/io/buffer/TimedBuffer.java`
#### Snippet
```java
   private TimedBufferObserver bufferObserver;
   private CheckTimer timerRunnable;
   private int bufferLimit = 0;
   private List<IOCallback> callbacks;
   // used to measure sync requests. When a sync is requested, it shouldn't take more than timeout to happen
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/io/aio/AIOSequentialFileFactory.java`
#### Snippet
```java
      private boolean stopped = false;

      private int alignedBufferSize = 0;

      private int getAlignedBufferSize() {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/io/aio/AIOSequentialFileFactory.java`
#### Snippet
```java
      private final ConcurrentLinkedQueue<ByteBuffer> reuseBuffersQueue = new ConcurrentLinkedQueue<>();

      private boolean stopped = false;

      private int alignedBufferSize = 0;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/io/aio/AIOSequentialFileFactory.java`
#### Snippet
```java

      IOCallback callback;
      boolean error = false;
      AIOSequentialFile sequentialFile;
      ByteBuffer buffer;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/JournalLoadInformation.java`
#### Snippet
```java
public class JournalLoadInformation {

   private int numberOfRecords = 0;

   private long maxID = -1;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/SimpleWaitIOCallback.java`
#### Snippet
```java
   private volatile String errorMessage;

   private volatile int errorCode = 0;

   public SimpleWaitIOCallback() {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalCompactor.java`
#### Snippet
```java

   // Compacting should split the compacting counts only once
   boolean split = false;

   // Snapshot of transactions that were pending when the compactor started
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalBase.java`
#### Snippet
```java
   protected final int fileSize;
   private final boolean supportsCallback;
   protected boolean removeExtraFilesOnLoad = false;

   @Override
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/TransactionCallback.java`
#### Snippet
```java
   private final ReusableLatch countLatch = new ReusableLatch();

   private volatile String errorMessage = null;

   private volatile int errorCode = 0;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/TransactionCallback.java`
#### Snippet
```java
   private final AtomicInteger up = new AtomicInteger();

   private int done = 0;

   private volatile IOCallback delegateCompletion;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/TransactionCallback.java`
#### Snippet
```java
   private volatile String errorMessage = null;

   private volatile int errorCode = 0;

   private final AtomicInteger up = new AtomicInteger();
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalFileImpl.java`
#### Snippet
```java
   private volatile int posCountField = 0;
   private volatile int addRecordField = 0;
   private volatile int liveBytesField = 0;

   // Flags to be used by determine if the journal file can be reclaimed
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalFileImpl.java`
#### Snippet
```java
   private static final AtomicIntegerFieldUpdater<JournalFileImpl> liveBytesUpdater = AtomicIntegerFieldUpdater.newUpdater(JournalFileImpl.class, "liveBytesField");

   private volatile int replaceableCountField = 0;
   private volatile int posCountField = 0;
   private volatile int addRecordField = 0;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalFileImpl.java`
#### Snippet
```java

   // Flags to be used by determine if the journal file can be reclaimed
   private boolean posReclaimCriteria = false;
   private boolean negReclaimCriteria = false;

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalFileImpl.java`
#### Snippet
```java
   // Flags to be used by determine if the journal file can be reclaimed
   private boolean posReclaimCriteria = false;
   private boolean negReclaimCriteria = false;

   private final AtomicInteger totalNegativeToOthers = new AtomicInteger(0);
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalFileImpl.java`
#### Snippet
```java
   private volatile int replaceableCountField = 0;
   private volatile int posCountField = 0;
   private volatile int addRecordField = 0;
   private volatile int liveBytesField = 0;

```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalFileImpl.java`
#### Snippet
```java

   private volatile int replaceableCountField = 0;
   private volatile int posCountField = 0;
   private volatile int addRecordField = 0;
   private volatile int liveBytesField = 0;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalTransaction.java`
#### Snippet
```java
   private TransactionCallback currentCallback;

   private boolean compacting = false;

   private final Map<JournalFile, TransactionCallback> callbackList = Collections.synchronizedMap(new HashMap<JournalFile, TransactionCallback>());
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalTransaction.java`
#### Snippet
```java
   private final Map<JournalFile, TransactionCallback> callbackList = Collections.synchronizedMap(new HashMap<JournalFile, TransactionCallback>());

   private JournalFile lastFile = null;

   private final AtomicInteger counter = new AtomicInteger();
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/LinkedListImpl.java`
#### Snippet
```java
   private final Node<E> head = new NodeHolder<>(null);
   private final Comparator<E> comparator;
   private Node<E> tail = null;
   private int size;
   // We store in an array rather than a Map for the best performance
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/ActiveMQRAProperties.java`
#### Snippet
```java
   private boolean useJNDI;

   private Boolean useMaskedPassword = null;

   private String passwordCodec;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/ActiveMQRAProperties.java`
#### Snippet
```java
   private String passwordCodec;

   private boolean initialized = false;

   /**
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/ActiveMQRAProperties.java`
#### Snippet
```java
    * The password
    */
   private String password = null;

   /**
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/ActiveMQRASessionFactoryImpl.java`
#### Snippet
```java
    * Are we closed?
    */
   private boolean closed = false;

   /**
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/ActiveMQRASessionFactoryImpl.java`
#### Snippet
```java
    * Whether we are started
    */
   private boolean started = false;

   /**
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/inflow/ActiveMQActivationSpec.java`
#### Snippet
```java
   private Boolean useJNDI = true;

   private String jndiParams = null;

   private Hashtable<String, String> parsedJndiParams;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/inflow/ActiveMQMessageHandler.java`
#### Snippet
```java
   private boolean transacted;

   private boolean useXA = false;

   private final int sessionNr;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/inflow/ActiveMQActivation.java`
#### Snippet
```java
   private final Map<String, Long> removedNodes = new ConcurrentHashMap<>();

   private boolean lastReceived = false;

   private final Object teardownLock = new Object();
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/inflow/ActiveMQActivation.java`
#### Snippet
```java
    * The destination type
    */
   private boolean isTopic = false;

   /**
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/process/ProcessBuilder.java`
#### Snippet
```java
      private final boolean sendToErr;

      boolean failed = false;

      ProcessLogger(final boolean print,
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Mask.java`
#### Snippet
```java

   @Option(name = "--hash", description = "Whether to use a hash (one-way). Default: false.")
   boolean hash = false;

   @Option(name = "--key", description = "The key (Blowfish) to mask a password.")
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Mask.java`
#### Snippet
```java

   @Option(name = "--password-codec", description = "Whether to use the password codec defined in the configuration. Default: false")
   boolean passwordCodec = false;

   private SensitiveDataCodec<String> codec;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/InputAbstract.java`
#### Snippet
```java

   @Option(name = "--silent", description = "Disable all the inputs, and make a best guess for any required input.")
   private boolean silentInput = false;

   public boolean isSilentInput() {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/InputAbstract.java`
#### Snippet
```java
   private Scanner scanner;

   private static boolean inputEnabled = false;

   /**
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Run.java`
#### Snippet
```java
   String properties;

   private static boolean embedded = false;

   public static final ReusableLatch latchRunning = new ReusableLatch(0);
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Configurable.java`
#### Snippet
```java
   public GlobalMetadata global;

   private BrokerDTO brokerDTO = null;

   private FileConfiguration fileConfiguration;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/user/AddUser.java`
#### Snippet
```java

   @Option(name = "--plaintext", description = "Store the password in plaintext. Default: false.")
   boolean plaintext = false;

   @Override
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/user/UserAction.java`
#### Snippet
```java

   @Option(name = "--user-command-user", description = "The username to use for the chosen user command. Default: input.")
   String userCommandUser = null;

   void checkInputUser() {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/user/ResetUser.java`
#### Snippet
```java

   @Option(name = "--plaintext", description = "Store the password in plaintext. Default: false.")
   boolean plaintext = false;

   @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/check/CheckAbstract.java`
#### Snippet
```java

   @Option(name = "--fail-at-end", description = "Continue with the rest of the checks even if a particular module check fails.")
   private boolean failAtEnd = false;

   public String getName() {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/InstallAbstract.java`
#### Snippet
```java

   @Option(name = "--cygwin", description = "Force Cygwin script creation. Default: based on your actual system.")
   protected boolean cygwin = false;

   @Option(name = "--java-options", description = "Extra Java options to be passed to the profile.")
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/InstallAbstract.java`
#### Snippet
```java

   @Option(name = "--windows", description = "Force Windows script creation. Default: based on your actual system.")
   protected boolean windows = false;

   @Option(name = "--cygwin", description = "Force Cygwin script creation. Default: based on your actual system.")
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/LockAbstract.java`
#### Snippet
```java
   // These will be locked as long as the VM is running
   private static RandomAccessFile serverLockFile = null;
   private static FileLock serverLockLock = null;

   public static void unlock() {
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/LockAbstract.java`
#### Snippet
```java
   // There should be one lock per VM
   // These will be locked as long as the VM is running
   private static RandomAccessFile serverLockFile = null;
   private static FileLock serverLockLock = null;

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/queue/DeleteQueue.java`
#### Snippet
```java

   @Option(name = "--autoDeleteAddress", description = "Whether to delete the address if this is its only queue.")
   boolean autoDeleteAddress = false;

   @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/queue/DeleteQueue.java`
#### Snippet
```java

   @Option(name = "--removeConsumers", description = "Whether to delete the queue even if it has active consumers. Default: false.")
   boolean removeConsumers = false;

   @Option(name = "--autoDeleteAddress", description = "Whether to delete the address if this is its only queue.")
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/queue/QueueAbstract.java`
#### Snippet
```java

   @Option(name = "--filter", description = "The queue's filter string. Default: null.")
   private String filter = null;

   @Option(name = "--address", description = "The queue's address. Default: queue's name.")
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/RecoverMessages.java`
#### Snippet
```java

   @Option(name = "--reclaimed", description = "Try to recover as many records as possible from reclaimed files.")
   private boolean reclaimed = false;

   @Option(name = "--target", description = "Output folder container the new journal with all the generated messages.", required = true)
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/queue/StatQueue.java`
#### Snippet
```java
   private int maxColumnSize = DEFAULT_MAX_COLUMN_SIZE;

   private int statCount = 0;

   //easier for testing
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/DBOption.java`
#### Snippet
```java

   @Option(name = "--jdbc-connection-url", description = "The URL used for the database connection.")
   private String jdbcURL = null;

   @Option(name = "--jdbc-driver-class-name", description = "JDBC driver classname.")
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/journal/PerfJournal.java`
#### Snippet
```java

   @Option(name = "--sync", description = "Enable syncs.")
   public boolean syncs = false;

   @Option(name = "--journal-type", description = "Journal type to be used: Default: read from broker.xml.")
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/journal/PerfJournal.java`
#### Snippet
```java

   @Option(name = "--journal-type", description = "Journal type to be used: Default: read from broker.xml.")
   public String journalType = null;

   @Option(name = "--sync-writes", description = "Perform each write synchronously, e.g. if there was a single producer.")
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/journal/PerfJournal.java`
#### Snippet
```java

   @Option(name = "--sync-writes", description = "Perform each write synchronously, e.g. if there was a single producer.")
   public boolean syncWrites = false;

   @Option(name = "--file", description = "The file name to be used. Default: test.tmp.")
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/journal/PerfJournal.java`
#### Snippet
```java

   @Option(name = "--max-aio", description = "libaio.maxAIO to be used. Default: read from broker.xml.")
   public int maxAIO = 0;


```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/journal/PerfJournal.java`
#### Snippet
```java

   @Option(name = "--no-sync", description = "Disable syncs.")
   public boolean nosyncs = false;

   @Option(name = "--sync", description = "Enable syncs.")
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/xml/XmlDataImporter.java`
#### Snippet
```java

   @Option(name = "--password", description = "User name used to import the data. Default: null.")
   public String password = null;

   @Option(name = "--input", description = "The input file name. Default: exp.dmp.", required = true)
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/xml/XmlDataImporter.java`
#### Snippet
```java
   ClientSession managementSession;

   boolean localSession = false;

   final Map<String, String> addressMap = new HashMap<>();
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/xml/XmlDataImporter.java`
#### Snippet
```java

   @Option(name = "--legacy-prefixes", description = "Do not remove prefixes from legacy imports.")
   public boolean legacyPrefixes = false;

   TreeSet<XMLMessageImporter.MessageInfo> messages;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/xml/XmlDataImporter.java`
#### Snippet
```java

   @Option(name = "--sort", description = "Sort the messages from the input (used for older versions that won't sort messages).")
   public boolean sort = false;

   @Option(name = "--legacy-prefixes", description = "Do not remove prefixes from legacy imports.")
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/xml/XmlDataImporter.java`
#### Snippet
```java

   @Option(name = "--user", description = "User name used to import the data. Default: null.")
   public String user = null;

   @Option(name = "--password", description = "User name used to import the data. Default: null.")
```

### RedundantFieldInitialization
Field initialization to `0L` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/xml/XmlDataExporter.java`
#### Snippet
```java
   private final HashMap<Long, PersistentAddressBindingEncoding> addressBindings = new HashMap<>();

   long messagesPrinted = 0L;

   long bindingsPrinted = 0L;
```

### RedundantFieldInitialization
Field initialization to `0L` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/xml/XmlDataExporter.java`
#### Snippet
```java
   long messagesPrinted = 0L;

   long bindingsPrinted = 0L;

   XMLMessageExporter exporter;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/xml/XmlDataExporter.java`
#### Snippet
```java
      private final XMLStreamWriter target;

      private int depth = 0;

      private static final char INDENT_CHAR = ' ';
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Create.java`
#### Snippet
```java

   @Option(name = "--suppress-internal-management-objects", description = "Do not register any advisory addresses/queues for the OpenWire protocol with the broker's management service.")
   boolean suppressInternalManagementObjects = false;

   public String[] getStaticNodes() {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Create.java`
#### Snippet
```java

   @Option(name = "--support-advisory", description = "Support advisory messages for the OpenWire protocol.")
   boolean supportAdvisory = false;

   @Option(name = "--suppress-internal-management-objects", description = "Do not register any advisory addresses/queues for the OpenWire protocol with the broker's management service.")
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Create.java`
#### Snippet
```java

   @Option(name = "--allow-anonymous", description = "Allow connections from users with no security credentials. Opposite of --require-login. Default: input.")
   private Boolean allowAnonymous = null;

   @Option(name = "--require-login", description = "Require security credentials from users for connection. Opposite of --allow-anonymous.")
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Create.java`
#### Snippet
```java

   @Option(name = "--shared-store", description = "Enable broker shared store.")
   private boolean sharedStore = false;

   @Option(name = "--slave", description = "Be a slave broker. Valid for shared store or replication.")
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Create.java`
#### Snippet
```java

   @Option(name = "--clustered", description = "Enable clustering.")
   private boolean clustered = false;

   @Option(name = "--max-hops", description = "Number of hops on the cluster configuration.")
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Create.java`
#### Snippet
```java

   @Option(name = "--cluster-user", description = "The user to use for clustering. Default: input.")
   private String clusterUser = null;

   @Option(name = "--cluster-password", description = "The password to use for clustering. Default: input.")
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Create.java`
#### Snippet
```java

   @Option(name = "--replicated", description = "Enable broker replication.")
   private boolean replicated = false;

   @Option(name = "--shared-store", description = "Enable broker shared store.")
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Create.java`
#### Snippet
```java

   @Option(name = "--jdbc-connection-url", description = "The URL used for the database connection.")
   private String jdbcURL = null;

   @Option(name = "--jdbc-driver-class-name", description = "JDBC driver classname.")
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Create.java`
#### Snippet
```java

   @Option(name = "--max-hops", description = "Number of hops on the cluster configuration.")
   private int maxHops = 0;

   @Option(name = "--message-load-balancing", description = "Message load balancing policy for cluster. Default: ON_DEMAND. Valid values: ON_DEMAND, STRICT, OFF, OFF_WITH_REDISTRIBUTION.")
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Create.java`
#### Snippet
```java

   @Option(name = "--require-login", description = "Require security credentials from users for connection. Opposite of --allow-anonymous.")
   private Boolean requireLogin = null;

   @Option(name = "--paging", description = "Page messages to disk when address becomes full. Opposite of --blocking. Default: true.")
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Create.java`
#### Snippet
```java

   @Option(name = "--cluster-password", description = "The password to use for clustering. Default: input.")
   private String clusterPassword = null;

   @Option(name = "--allow-anonymous", description = "Allow connections from users with no security credentials. Opposite of --require-login. Default: input.")
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/PrintData.java`
#### Snippet
```java

   @Option(name = "--safe", description = "Print your data structure without showing your data.")
   private boolean safe = false;


```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/PrintData.java`
#### Snippet
```java

   @Option(name = "--reclaimed", description = "Try to print as many records as possible from reclaimed files.")
   private boolean reclaimed = false;

   @Option(name = "--max-pages", description = "Maximum number of pages to read. Default: unlimited (-1).")
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/PrintData.java`
#### Snippet
```java

   @Option(name = "--skip-bindings", description = "Do not print data from the bindings journal.")
   private boolean skipBindings = false;

   @Option(name = "--skip-journal", description = "Do not print data from the messages journal.")
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/PrintData.java`
#### Snippet
```java

   @Option(name = "--skip-journal", description = "Do not print data from the messages journal.")
   private boolean skipJournal = false;

   private static final String BINDINGS_BANNER = "B I N D I N G S  J O U R N A L";
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/DestAbstract.java`
#### Snippet
```java

   @Option(name = "--sleep", description = "Time wait between each message.")
   int sleep = 0;

   @Option(name = "--txt-size", description = "Transaction batch size.")
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/Consumer.java`
#### Snippet
```java

   @Option(name = "--durable", description = "Whether the consumer's subscription will be durable.")
   boolean durable = false;

   @Option(name = "--break-on-null", description = "Stop consuming when a null message is received.")
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/Consumer.java`
#### Snippet
```java

   @Option(name = "--break-on-null", description = "Stop consuming when a null message is received.")
   boolean breakOnNull = false;

   @Option(name = "--receive-timeout", description = "Timeout for receiving messages (in milliseconds).")
```

### RedundantFieldInitialization
Field initialization to `0L` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/Producer.java`
#### Snippet
```java

   @Option(name = "--msgttl", description = "TTL for each message.")
   long msgTTL = 0L;

   @Option(name = "--group", description = "Message Group to be used.")
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/Producer.java`
#### Snippet
```java

   @Option(name = "--non-persistent", description = "Send messages non persistently.")
   boolean nonpersistent = false;

   @Option(name = "--message-size", description = "Size of each bytesMessage. The producer will use JMS BytesMessage.")
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/Producer.java`
#### Snippet
```java

   @Option(name = "--message", description = "Content of each textMessage. The producer will use JMS TextMessage.")
   String message = null;

   @Option(name = "--text-size", description = "Size of each textMessage. The producer will use JMS TextMessage.")
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/Producer.java`
#### Snippet
```java

   @Option(name = "--data", description = "Messages will be read from the specified file. Other message options will be ignored.")
   String file = null;

   public boolean isNonpersistent() {
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/Producer.java`
#### Snippet
```java

   @Option(name = "--group", description = "Message Group to be used.")
   String msgGroupID = null;

   @Option(name = "--data", description = "Messages will be read from the specified file. Other message options will be ignored.")
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/Producer.java`
#### Snippet
```java

   @Option(name = "--message-size", description = "Size of each bytesMessage. The producer will use JMS BytesMessage.")
   int messageSize = 0;

   @Option(name = "--message", description = "Content of each textMessage. The producer will use JMS TextMessage.")
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/perf/PerfConsumerCommand.java`
#### Snippet
```java

   @Option(name = "--shared", description = "Create shared subscription. Default: 0.")
   protected int sharedSubscription = 0;

   @Option(name = "--durable", description = "Enabled durable subscription. Default: disabled.")
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/perf/PerfConsumerCommand.java`
#### Snippet
```java

   @Option(name = "--durable", description = "Enabled durable subscription. Default: disabled.")
   protected boolean durableSubscription = false;

   @Option(name = "--num-connections", description = "Number of connections to be used. Default: same as the total number of consumers.")
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/perf/PerfConsumerCommand.java`
#### Snippet
```java

   @Option(name = "--num-connections", description = "Number of connections to be used. Default: same as the total number of consumers.")
   protected int connections = 0;

   @Option(name = "--consumers", description = "Number of consumer to use for each generated destination. Default: 1.")
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/ProducerThread.java`
#### Snippet
```java
   boolean verbose;
   int messageCount = 1000;
   boolean runIndefinitely = false;
   Destination destination;
   int sleep = 0;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/ProducerThread.java`
#### Snippet
```java
   String payloadUrl = null;
   byte[] payload = null;
   boolean running = false;
   final ReusableLatch finished = new ReusableLatch(1);
   final ReusableLatch paused = new ReusableLatch(0);
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/ProducerThread.java`
#### Snippet
```java
   int transactions = 0;
   final AtomicInteger sentCount = new AtomicInteger(0);
   String message = null;
   String messageText = null;
   String payloadUrl = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/ProducerThread.java`
#### Snippet
```java
   int objectSize;
   long msgTTL = 0L;
   String msgGroupID = null;
   int transactionBatchSize;

```

### RedundantFieldInitialization
Field initialization to `0L` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/ProducerThread.java`
#### Snippet
```java
   int textMessageSize;
   int objectSize;
   long msgTTL = 0L;
   String msgGroupID = null;
   int transactionBatchSize;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/ProducerThread.java`
#### Snippet
```java
   String messageText = null;
   String payloadUrl = null;
   byte[] payload = null;
   boolean running = false;
   final ReusableLatch finished = new ReusableLatch(1);
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/ProducerThread.java`
#### Snippet
```java
   final AtomicInteger sentCount = new AtomicInteger(0);
   String message = null;
   String messageText = null;
   String payloadUrl = null;
   byte[] payload = null;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/ProducerThread.java`
#### Snippet
```java
   int transactionBatchSize;

   int transactions = 0;
   final AtomicInteger sentCount = new AtomicInteger(0);
   String message = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/ProducerThread.java`
#### Snippet
```java
   String message = null;
   String messageText = null;
   String payloadUrl = null;
   byte[] payload = null;
   boolean running = false;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/ProducerThread.java`
#### Snippet
```java
   boolean runIndefinitely = false;
   Destination destination;
   int sleep = 0;
   boolean persistent = true;
   int messageSize = 0;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/ProducerThread.java`
#### Snippet
```java
   int sleep = 0;
   boolean persistent = true;
   int messageSize = 0;
   int textMessageSize;
   int objectSize;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/perf/PerfCommand.java`
#### Snippet
```java

   @Option(name = "--warmup", description = "Warmup time (in seconds). Default: 0.")
   protected int warmup = 0;

   @Option(name = "--message-count", description = "Total number of messages. Default: 0.")
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/perf/PerfCommand.java`
#### Snippet
```java

   @Option(name = "--duration", description = "Test duration (in seconds). Default: 0.")
   protected int duration = 0;

   @Option(name = "--warmup", description = "Warmup time (in seconds). Default: 0.")
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/perf/PerfCommand.java`
#### Snippet
```java

   @Option(name = "--show-latency", description = "Show latencies at interval on output. Default: disabled.")
   protected boolean showLatency = false;

   @Option(name = "--json", description = "Report file name. Default: none.")
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/perf/PerfCommand.java`
#### Snippet
```java

   @Option(name = "--message-count", description = "Total number of messages. Default: 0.")
   protected long messageCount = 0;

   @Option(name = "--num-destinations", description = "If present, generate --num-destinations for each destination name, using it as a prefix and adding a number [0,--num-destinations) as suffix. Default: none.")
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/perf/PerfCommand.java`
#### Snippet
```java

   @Option(name = "--json", description = "Report file name. Default: none.")
   protected String reportFileName = null;

   @Option(name = "--hdr", description = "HDR Histogram Report file name. Default: none.")
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/perf/PerfCommand.java`
#### Snippet
```java

   @Option(name = "--hdr", description = "HDR Histogram Report file name. Default: none.")
   protected String hdrFileName = null;

   @Option(name = "--duration", description = "Test duration (in seconds). Default: 0.")
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/perf/PerfProducerCommand.java`
#### Snippet
```java

   @Option(name = "--rate", description = "Expected total message rate. Default: unbounded.")
   protected Long rate = null;

   @Option(name = "--ttl", description = "TTL for each message.")
```

### RedundantFieldInitialization
Field initialization to `0L` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/perf/PerfProducerCommand.java`
#### Snippet
```java

   @Option(name = "--ttl", description = "TTL for each message.")
   protected long ttl = 0L;

   @Option(name = "--group", description = "Message Group to be used.")
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/perf/PerfProducerCommand.java`
#### Snippet
```java

   @Option(name = "--shared-connections", description = "Create --threads shared connections among producers. Default: not shared.")
   protected boolean sharedConnections = false;

   @Option(name = "--tx-size", description = "Transaction size.")
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/perf/PerfProducerCommand.java`
#### Snippet
```java

   @Option(name = "--group", description = "Message Group to be used.")
   protected String msgGroupID = null;

   @Option(name = "--shared-connections", description = "Create --threads shared connections among producers. Default: not shared.")
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/perf/PerfProducerCommand.java`
#### Snippet
```java
public class PerfProducerCommand extends PerfCommand {
   @Option(name = "--persistent", description = "Send messages persistently. Default: non persistent.")
   protected boolean persistent = false;

   @Option(name = "--message-size", description = "Size of each bytesMessage. Default: 1024.")
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/ConsumerThread.java`
#### Snippet
```java
   String filter;

   int received = 0;
   int transactions = 0;
   boolean running = false;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/ConsumerThread.java`
#### Snippet
```java

   int received = 0;
   int transactions = 0;
   boolean running = false;
   CountDownLatch finished;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/ConsumerThread.java`
#### Snippet
```java
   int received = 0;
   int transactions = 0;
   boolean running = false;
   CountDownLatch finished;
   boolean bytesAsText;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/perf/PerfClientCommand.java`
#### Snippet
```java

   @Option(name = "--persistent", description = "Send messages persistently. Default: non persistent")
   protected boolean persistent = false;

   @Option(name = "--message-size", description = "Size of each bytesMessage. Default: is 1024.")
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/perf/PerfClientCommand.java`
#### Snippet
```java

   @Option(name = "--consumer-url", description = "The url used for MessageListener(s) connections. Default: same as --url.")
   protected String consumerUrl = null;

   @Option(name = "--consumer-protocol", description = "The protocol used for MessageListener(s) connections. Default: same as --protocol.")
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/perf/PerfClientCommand.java`
#### Snippet
```java

   @Option(name = "--group", description = "Message Group to be used.")
   protected String msgGroupID = null;

   @Option(name = "--shared-connections", description = "Create --threads shared connections among producers. Default: not shared.")
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/perf/PerfClientCommand.java`
#### Snippet
```java

   @Option(name = "--consumer-connections", description = "Number of consumer connections to be used. Default: same as the total number of consumers")
   protected int consumerConnections = 0;

   @Option(name = "--consumers", description = "Number of consumer to use for each generated destination. Default: 1.")
```

### RedundantFieldInitialization
Field initialization to `0L` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/perf/PerfClientCommand.java`
#### Snippet
```java

   @Option(name = "--ttl", description = "TTL for each message.")
   protected long ttl = 0L;

   @Option(name = "--group", description = "Message Group to be used.")
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/perf/PerfClientCommand.java`
#### Snippet
```java

   @Option(name = "--shared-connections", description = "Create --threads shared connections among producers. Default: not shared.")
   protected boolean sharedConnections = false;

   @Option(name = "--tx-size", description = "Transaction size.")
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/perf/PerfClientCommand.java`
#### Snippet
```java

   @Option(name = "--shared", description = "Create a shared subscription. Default: 0.")
   protected int sharedSubscription = 0;

   @Option(name = "--durable", description = "Enabled durable subscription. Default: disabled.")
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/perf/PerfClientCommand.java`
#### Snippet
```java

   @Option(name = "--rate", description = "Expected total message rate. Default: unbounded.")
   protected Long rate = null;

   @Option(name = "--ttl", description = "TTL for each message.")
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/perf/PerfClientCommand.java`
#### Snippet
```java

   @Option(name = "--consumer-protocol", description = "The protocol used for MessageListener(s) connections. Default: same as --protocol.")
   protected String consumerProtocol = null;

   @Option(name = "--enable-msg-id", description = "Set JMS messageID per-message. Default: disabled.")
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/perf/PerfClientCommand.java`
#### Snippet
```java

   @Option(name = "--durable", description = "Enabled durable subscription. Default: disabled.")
   protected boolean durableSubscription = false;

   @Option(name = "--consumer-connections", description = "Number of consumer connections to be used. Default: same as the total number of consumers")
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/activation/ActivationSequenceList.java`
#### Snippet
```java
   private static final int MANAGER_START_TIMEOUT_SECONDS = 60;
   @Option(name = "--node-id", description = "This can be used just with --remote option. If not set, broker NodeID is used instead.")
   public String nodeId = null;
   @Option(name = "--remote", description = "List just remote (i.e. coordinated) activation sequence.")
   public boolean remote = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/activation/ActivationSequenceList.java`
#### Snippet
```java
   public String nodeId = null;
   @Option(name = "--remote", description = "List just remote (i.e. coordinated) activation sequence.")
   public boolean remote = false;
   @Option(name = "--local", description = "List just local activation sequence.")
   public boolean local = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/activation/ActivationSequenceList.java`
#### Snippet
```java
   public boolean remote = false;
   @Option(name = "--local", description = "List just local activation sequence.")
   public boolean local = false;

   @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/activation/ActivationSequenceSet.java`
#### Snippet
```java
   public String nodeId = null;
   @Option(name = "--remote", description = "Set just remote (i.e. coordinated) activation sequence.")
   public boolean remote = false;
   @Option(name = "--local", description = "Set just local activation sequence.")
   public boolean local = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/activation/ActivationSequenceSet.java`
#### Snippet
```java
   public boolean remote = false;
   @Option(name = "--local", description = "Set just local activation sequence.")
   public boolean local = false;
   @Option(name = "--to", description = "The new activation sequence.", required = true)
   public long value;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/activation/ActivationSequenceSet.java`
#### Snippet
```java
   private static final int MANAGER_START_TIMEOUT_SECONDS = 60;
   @Option(name = "--node-id", description = "Target sequence for this UUID overwriting the NodeID of this broker too. If not set, broker NodeID is used instead.")
   public String nodeId = null;
   @Option(name = "--remote", description = "Set just remote (i.e. coordinated) activation sequence.")
   public boolean remote = false;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/server/config/impl/JMSQueueConfigurationImpl.java`
#### Snippet
```java
   private boolean durable = true;

   private String[] bindings = null;


```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/server/config/impl/JMSQueueConfigurationImpl.java`
#### Snippet
```java
   private String name = null;

   private String selector = null;

   private boolean durable = true;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/server/config/impl/JMSQueueConfigurationImpl.java`
#### Snippet
```java


   private String name = null;

   private String selector = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/server/config/impl/ConnectionFactoryConfigurationImpl.java`
#### Snippet
```java
   private boolean persisted = false;

   private String[] bindings = null;

   private List<String> connectorNames = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/server/config/impl/ConnectionFactoryConfigurationImpl.java`
#### Snippet
```java
   private int reconnectAttempts = ActiveMQClient.DEFAULT_RECONNECT_ATTEMPTS;

   private String groupID = null;

   private String protocolManagerFactoryStr;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/server/config/impl/ConnectionFactoryConfigurationImpl.java`
#### Snippet
```java
   private String[] bindings = null;

   private List<String> connectorNames = null;

   private String discoveryGroupName = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/server/config/impl/ConnectionFactoryConfigurationImpl.java`
#### Snippet
```java
   private String discoveryGroupName = null;

   private String clientID = null;

   private boolean ha = ActiveMQClient.DEFAULT_HA;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/server/config/impl/ConnectionFactoryConfigurationImpl.java`
#### Snippet
```java
   private List<String> connectorNames = null;

   private String discoveryGroupName = null;

   private String clientID = null;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/server/config/impl/ConnectionFactoryConfigurationImpl.java`
#### Snippet
```java
   private String name = null;

   private boolean persisted = false;

   private String[] bindings = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/server/config/impl/ConnectionFactoryConfigurationImpl.java`
#### Snippet
```java


   private String name = null;

   private boolean persisted = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/ConnectionFactoryProperties.java`
#### Snippet
```java
   private static final Logger logger = LoggerFactory.getLogger(MethodHandles.lookup().lookupClass());

   private boolean hasBeenUpdated = false;

   /**
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/bridge/impl/JMSBridgeImpl.java`
#### Snippet
```java
   private boolean failed;

   private boolean connectedSource = false;

   private boolean connectedTarget = false;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/bridge/impl/JMSBridgeImpl.java`
#### Snippet
```java
   private long messageCount = 0;

   private long abortedMessageCount = 0;

   /*
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/bridge/impl/JMSBridgeImpl.java`
#### Snippet
```java
   private ClassLoader moduleTccl;

   private long messageCount = 0;

   private long abortedMessageCount = 0;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/bridge/impl/JMSBridgeImpl.java`
#### Snippet
```java
   private boolean connectedSource = false;

   private boolean connectedTarget = false;

   private int forwardMode;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/bridge/impl/JMSBridgeImpl.java`
#### Snippet
```java

   private static final Object stoppingGuard = new Object();
   private volatile boolean stopping = false;

   private final LinkedList<Message> messages;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenWireProtocolManager.java`
#### Snippet
```java
   private boolean rebalanceClusterClients = false;
   private boolean updateClusterClients = false;
   private boolean updateClusterClientsOnRemove = false;

   private boolean openwireUseDuplicateDetectionOnFailover = true;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenWireProtocolManager.java`
#### Snippet
```java
   //http://activemq.apache.org/failover-transport-reference.html
   private boolean rebalanceClusterClients = false;
   private boolean updateClusterClients = false;
   private boolean updateClusterClientsOnRemove = false;

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenWireProtocolManager.java`
#### Snippet
```java
   //bean properties
   //http://activemq.apache.org/failover-transport-reference.html
   private boolean rebalanceClusterClients = false;
   private boolean updateClusterClients = false;
   private boolean updateClusterClientsOnRemove = false;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/amq/AMQConsumer.java`
#### Snippet
```java
   private final AtomicInteger currentWindow;
   private int deliveredAcksCreditExtension = 0;
   private long messagePullSequence = 0;
   private final AtomicReference<MessagePullHandler> messagePullHandler = new AtomicReference<>(null);
   //internal means we don't expose
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/amq/AMQConsumer.java`
#### Snippet
```java
   private int prefetchSize;
   private final AtomicInteger currentWindow;
   private int deliveredAcksCreditExtension = 0;
   private long messagePullSequence = 0;
   private final AtomicReference<MessagePullHandler> messagePullHandler = new AtomicReference<>(null);
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/amq/AMQConsumer.java`
#### Snippet
```java
   //internal means we don't expose
   //it's address/queue to management service
   private boolean internalAddress = false;
   private volatile Set<MessageReference> rolledbackMessageRefs;

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTSession.java`
#### Snippet
```java
   private MQTTVersion version = null;

   private boolean usingServerKeepAlive = false;

   public MQTTSession(MQTTProtocolHandler protocolHandler,
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTSession.java`
#### Snippet
```java
   private CoreMessageObjectPools coreMessageObjectPools = new CoreMessageObjectPools();

   private MQTTVersion version = null;

   private boolean usingServerKeepAlive = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTSession.java`
#### Snippet
```java
   protected MQTTSessionState state;

   private boolean stopped = false;

   private MQTTProtocolManager protocolManager;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTConnection.java`
#### Snippet
```java
   private String protocolVersion;

   private boolean clientIdAssignedByBroker = false;

   public MQTTConnection(Connection transportConnection) throws Exception {
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTSessionState.java`
#### Snippet
```java
   private boolean attached = false;

   private long disconnectedTime = 0;

   private final OutboundStore outboundStore = new OutboundStore();
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTSessionState.java`
#### Snippet
```java
   private boolean failed = false;

   private int clientMaxPacketSize = 0;

   private Map<Integer, String> clientTopicAliases;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTSessionState.java`
#### Snippet
```java
   private boolean willRetain = false;

   private long willDelayInterval = 0;

   private List<? extends MqttProperties.MqttProperty> willUserProperties;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTSessionState.java`
#### Snippet
```java
   private WillStatus willStatus = WillStatus.NOT_SENT;

   private boolean failed = false;

   private int clientMaxPacketSize = 0;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTSessionState.java`
#### Snippet
```java
   private int clientSessionExpiryInterval;

   private boolean isWill = false;

   private ByteBuf willMessage;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTSessionState.java`
#### Snippet
```java
   private final Set<Integer> pubRec = new HashSet<>();

   private boolean attached = false;

   private long disconnectedTime = 0;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTSessionState.java`
#### Snippet
```java
   private int willQoSLevel;

   private boolean willRetain = false;

   private long willDelayInterval = 0;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTProtocolHandler.java`
#### Snippet
```java
   private ChannelHandlerContext ctx;

   private boolean stopped = false;

   private final Actor<MqttMessage> mqttMessageActor;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-unit-test-support/src/main/java/org/apache/activemq/artemis/logs/AssertionLoggerHandler.java`
#### Snippet
```java
   private static List<String> traceMessages;
   private static volatile boolean capture = false;
   private static volatile boolean captureStackTrace = false;


```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-unit-test-support/src/main/java/org/apache/activemq/artemis/logs/AssertionLoggerHandler.java`
#### Snippet
```java
   private static final Map<String, LogEvent> messages = new ConcurrentHashMap<>();
   private static List<String> traceMessages;
   private static volatile boolean capture = false;
   private static volatile boolean captureStackTrace = false;

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenWireConnection.java`
#### Snippet
```java
   private final OpenWireProtocolManager protocolManager;

   private boolean destroyed = false;

   private volatile ScheduledFuture ttlCheck;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenWireConnection.java`
#### Snippet
```java
   private final AtomicBoolean disableTtl = new AtomicBoolean(false);

   private String validatedUser = null;

   public OpenWireConnection(Connection connection,
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-unit-test-support/src/main/java/org/apache/activemq/artemis/utils/ThreadLeakCheckRule.java`
#### Snippet
```java
   protected boolean enabled = true;

   protected boolean testFailed = false;

   protected Description testDescription = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-unit-test-support/src/main/java/org/apache/activemq/artemis/utils/ThreadLeakCheckRule.java`
#### Snippet
```java
   protected Description testDescription = null;

   protected Throwable failure = null;

   protected Map<Thread, StackTraceElement[]> previousThreads;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-unit-test-support/src/main/java/org/apache/activemq/artemis/utils/ThreadLeakCheckRule.java`
#### Snippet
```java
   }

   private static int failedGCCalls = 0;

   public static void forceGC() {
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-unit-test-support/src/main/java/org/apache/activemq/artemis/utils/ThreadLeakCheckRule.java`
#### Snippet
```java
   protected boolean testFailed = false;

   protected Description testDescription = null;

   protected Throwable failure = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalImpl.java`
#### Snippet
```java
   private Executor compactorExecutor = null;

   private Executor appendExecutor = null;

   private final ConcurrentHashSet<CountDownLatch> latches = new ConcurrentHashSet<>();
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalImpl.java`
#### Snippet
```java
   private volatile JournalState state = JournalState.STOPPED;

   private volatile int compactCount = 0;

   public float getCompactPercentage() {
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalImpl.java`
#### Snippet
```java
   private Executor filesExecutor = null;

   private Executor compactorExecutor = null;

   private Executor appendExecutor = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalImpl.java`
#### Snippet
```java
   private final AtomicBoolean compactorRunning = new AtomicBoolean();

   private Executor filesExecutor = null;

   private Executor compactorExecutor = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQQueueBrowser.java`
#### Snippet
```java
   private final class BrowserEnumeration implements Enumeration<ActiveMQMessage> {

      ClientMessage current = null;

      @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQMessageProducer.java`
#### Snippet
```java
   private final ActiveMQSession session;

   private boolean disableMessageID = false;

   private boolean disableMessageTimestamp = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQMessageProducer.java`
#### Snippet
```java
   private boolean disableMessageID = false;

   private boolean disableMessageTimestamp = false;

   private int defaultPriority = Message.DEFAULT_PRIORITY;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQStreamMessage.java`
#### Snippet
```java
    * len here is used to control how many more bytes to read
    */
   private int len = 0;

   @Override
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-junit/artemis-junit-commons/src/main/java/org/apache/activemq/artemis/junit/ActiveMQProducerDelegate.java`
#### Snippet
```java

   boolean useDurableMessage = true;
   SimpleString address = null;
   ClientProducer producer;

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/sasl/ServerSASLPlain.java`
#### Snippet
```java

   public static final String NAME = "PLAIN";
   private SASLResult result = null;

   @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/sasl/scram/ScramServerFunctionalityImpl.java`
#### Snippet
```java
   private final String mServerPartNonce;

   private boolean mIsSuccessful = false;
   private State mState = State.INITIAL;
   private String mClientFirstMessageBare;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/sasl/scram/SCRAMServerSASLFactory.java`
#### Snippet
```java

      private final String loginConfigScope;
      private LoginContext loginContext = null;
      private Subject loginSubject;
      private final Logger logger;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-junit/artemis-junit-commons/src/main/java/org/apache/activemq/artemis/junit/EmbeddedJMSDelegate.java`
#### Snippet
```java

   private static final Logger logger = LoggerFactory.getLogger(MethodHandles.lookup().lookupClass());
   Integer serverId = null;

   Configuration configuration;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/ProtonInitializable.java`
#### Snippet
```java
public class ProtonInitializable {

   private boolean initialized = false;

   public boolean isInitialized() {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPLargeMessage.java`
#### Snippet
```java
   }

   private boolean reencoded = false;

   /**
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/ProtonAbstractReceiver.java`
#### Snippet
```java
   protected final boolean useModified;

   protected int pendingSettles = 0;
   public static boolean isBellowThreshold(int credit, int pending, int threshold) {
      return credit <= threshold - pending;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/AMQPSessionContext.java`
#### Snippet
```java
   protected Map<Sender, ProtonServerSenderContext> senders = new ConcurrentHashMap<>();

   protected boolean closed = false;

   protected final AmqpTransferTagGenerator tagCache = new AmqpTransferTagGenerator();
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/ProtonServerReceiverContext.java`
#### Snippet
```java
   protected SimpleString lastAddress;
   protected AddressFullMessagePolicy lastAddressPolicy;
   protected boolean addressAlreadyClashed = false;


```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/handler/ProtonHandler.java`
#### Snippet
```java
   boolean scheduledFlush = false;

   boolean flushInstantly = false;

   /** afterFlush and afterFlushSet properties
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/handler/ProtonHandler.java`
#### Snippet
```java
   boolean inDispatch = false;

   boolean scheduledFlush = false;

   boolean flushInstantly = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/handler/ProtonHandler.java`
#### Snippet
```java
   protected final ReadyListener readyListener;

   boolean inDispatch = false;

   boolean scheduledFlush = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/handler/ProtonHandler.java`
#### Snippet
```java
   protected volatile boolean dataReceived;

   protected boolean receivedFirstPacket = false;

   private final EventLoop workerExecutor;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
   protected volatile int memoryEstimate = -1;
   protected long expiration;
   protected boolean expirationReload = false;
   protected long scheduledTime = -1;

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/connect/AMQPBrokerConnectionManager.java`
#### Snippet
```java
   private final ProtonProtocolManagerFactory protonProtocolManagerFactory;
   private final ActiveMQServer server;
   private volatile boolean started = false;

   List<AMQPBrokerConnectConfiguration> amqpConnectionsConfig;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/ProtonServerSenderContext.java`
#### Snippet
```java


   private int credits = 0;

   private AtomicInteger pending = new AtomicInteger(0);
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/ProtonServerSenderContext.java`
#### Snippet
```java


      private boolean shared = false;
      boolean global = false;
      boolean multicast;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/ProtonServerSenderContext.java`
#### Snippet
```java
   protected final Sender sender;
   protected final AMQPConnectionContext connection;
   protected boolean closed = false;
   protected final AMQPSessionCallback sessionSPI;

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/ProtonServerSenderContext.java`
#### Snippet
```java
      boolean multicast;
      final AMQPSessionCallback sessionSPI;
      SimpleString queue = null;
      SimpleString tempQueueName;
      String selector;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/ProtonServerSenderContext.java`
#### Snippet
```java
   // as large message could be interrupted due to flow control and resumed at the same message
   volatile boolean hasLarge = false;
   volatile LargeMessageDeliveryContext pendingLargeMessage = null;
   volatile Runnable afterLargeMessage;

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/ProtonServerSenderContext.java`
#### Snippet
```java
   // once a large message is accepted, we shouldn't accept any further messages
   // as large message could be interrupted due to flow control and resumed at the same message
   volatile boolean hasLarge = false;
   volatile LargeMessageDeliveryContext pendingLargeMessage = null;
   volatile Runnable afterLargeMessage;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/ProtonServerSenderContext.java`
#### Snippet
```java

      private boolean shared = false;
      boolean global = false;
      boolean multicast;
      final AMQPSessionCallback sessionSPI;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/ProtonServerSenderContext.java`
#### Snippet
```java
      private RoutingType routingTypeToUse = RoutingType.ANYCAST;

      private boolean isVolatile = false;

      DefaultController(AMQPSessionCallback sessionSPI) {
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/converter/coreWrapper/CoreStreamMessageWrapper.java`
#### Snippet
```java
    * len here is used to control how many more bytes to read
    */
   private int len = 0;

   public CoreStreamMessageWrapper(ICoreMessage message) {
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/converter/coreWrapper/CoreStreamMessageWrapper.java`
#### Snippet
```java
   public static final byte TYPE = Message.STREAM_TYPE;

   private int bodyLength = 0;
   /**
    * len here is used to control how many more bytes to read
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/connect/AMQPBrokerConnection.java`
#### Snippet
```java
   private final AMQPBrokerConnectionManager bridgeManager;
   private AMQPMirrorControllerSource mirrorControllerSource;
   private int retryCounter = 0;
   private int lastRetryCounter;
   private boolean connecting = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/connect/AMQPBrokerConnection.java`
#### Snippet
```java
   private AMQPSessionContext sessionContext;
   private ActiveMQProtonRemotingConnection protonRemotingConnection;
   private volatile boolean started = false;
   private final AMQPBrokerConnectionManager bridgeManager;
   private AMQPMirrorControllerSource mirrorControllerSource;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/connect/AMQPBrokerConnection.java`
#### Snippet
```java
   private int retryCounter = 0;
   private int lastRetryCounter;
   private boolean connecting = false;
   private volatile ScheduledFuture reconnectFuture;
   private final Set<Queue> senders = new HashSet<>();
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-jdbc-store/src/main/java/org/apache/activemq/artemis/jdbc/store/file/JDBCSequentialFile.java`
#### Snippet
```java
   private long id = -1;

   private long readPosition = 0;

   private long writePosition = 0;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-jdbc-store/src/main/java/org/apache/activemq/artemis/jdbc/store/file/JDBCSequentialFile.java`
#### Snippet
```java
   private long readPosition = 0;

   private long writePosition = 0;

   private final Executor executor;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-jdbc-store/src/main/java/org/apache/activemq/artemis/jdbc/store/journal/JDBCJournalRecord.java`
#### Snippet
```java

   // Callback and sync operations
   private IOCompletion ioCompletion = null;
   private boolean storeLineUp = true;
   private boolean sync = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-jdbc-store/src/main/java/org/apache/activemq/artemis/jdbc/store/journal/JDBCJournalRecord.java`
#### Snippet
```java
   private IOCompletion ioCompletion = null;
   private boolean storeLineUp = true;
   private boolean sync = false;

   // DB Fields for all records
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-server-osgi/src/main/java/org/apache/activemq/artemis/osgi/OsgiBroker.java`
#### Snippet
```java
   private class ServerTrackerCallBackImpl implements ServerTrackerCallBack {

      private volatile boolean dataSourceDependency = false;

      private final ActiveMQServer server;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/UDPBroadcastEndpointFactory.java`
#### Snippet
```java
   private transient int localBindPort = -1;

   private String groupAddress = null;

   private int groupPort = -1;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/BroadcastGroupConfiguration.java`
#### Snippet
```java
   private long broadcastPeriod = ActiveMQDefaultConfiguration.getDefaultBroadcastPeriod();

   private BroadcastEndpointFactory endpointFactory = null;

   private List<String> connectorInfos = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/BroadcastGroupConfiguration.java`
#### Snippet
```java
   private static final long serialVersionUID = 2335634694112319124L;

   private String name = null;

   private long broadcastPeriod = ActiveMQDefaultConfiguration.getDefaultBroadcastPeriod();
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/BroadcastGroupConfiguration.java`
#### Snippet
```java
   private BroadcastEndpointFactory endpointFactory = null;

   private List<String> connectorInfos = null;

   public BroadcastGroupConfiguration() {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/RefCountMessage.java`
#### Snippet
```java
   private volatile int usageCount = 0;

   private volatile boolean released = false;

   private volatile boolean errorCheck = true;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/RefCountMessage.java`
#### Snippet
```java
   private volatile int refCount = 0;

   private volatile int usageCount = 0;

   private volatile boolean released = false;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/RefCountMessage.java`
#### Snippet
```java
   private volatile int durableRefCount = 0;

   private volatile int refCount = 0;

   private volatile int usageCount = 0;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/RefCountMessage.java`
#### Snippet
```java
   private volatile HashMap userContext;

   private volatile int durableRefCount = 0;

   private volatile int refCount = 0;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/jgroups/JChannelManager.java`
#### Snippet
```java
   // if true, messages will be loopbacked
   // this is useful for testcases using a single channel.
   private boolean loopbackMessages = false;

   private static final Map<String, JChannelWrapper> channels = new HashMap<>();
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/jgroups/JChannelWrapper.java`
#### Snippet
```java
   private static final Logger logger = LoggerFactory.getLogger(MethodHandles.lookup().lookupClass());

   private boolean connected = false;
   int refCount = 1;
   final JChannel channel;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/config/ActiveMQDefaultConfiguration.java`
#### Snippet
```java

   // If true the whole page would be read, otherwise just seek and read while getting message
   private static boolean DEFAULT_READ_WHOLE_PAGE = false;

   // the directory to store the journal files in
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/config/ActiveMQDefaultConfiguration.java`
#### Snippet
```java

   // true means that message counters are enabled
   private static boolean DEFAULT_MESSAGE_COUNTER_ENABLED = false;

   // the sample period (in ms) to use for message counters
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/config/ActiveMQDefaultConfiguration.java`
#### Snippet
```java

   // This option controls whether passwords in server configuration need be masked. If set to "true" the passwords are masked.
   private static Boolean DEFAULT_MASK_PASSWORD = null;

   // true means that the management API is available via JMX
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/config/ActiveMQDefaultConfiguration.java`
#### Snippet
```java

   // Will the broker populate the message with the name of the validated user
   private static boolean DEFAULT_POPULATE_VALIDATED_USER = false;

   // Will the broker allow messages with no validated user
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/config/ActiveMQDefaultConfiguration.java`
#### Snippet
```java

   // Whether to check the cluster for a (live) server using our own server ID when starting up. This option is only necessary for performing 'fail-back' on replicating servers. Strictly speaking this setting only applies to live servers and not to backups.
   private static boolean DEFAULT_CHECK_FOR_LIVE_SERVER = false;

   // This specifies how many times a replicated backup server can restart after moving its files on start. Once there are this number of backup journal files the server will stop permanently after if fails back.
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/config/ActiveMQDefaultConfiguration.java`
#### Snippet
```java

   // true means that graceful shutdown is enabled
   private static boolean DEFAULT_GRACEFUL_SHUTDOWN_ENABLED = false;

   // how long (in ms) to wait before forcing the server to stop even if clients are still connected (i.e circumventing graceful shutdown)
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/config/ActiveMQDefaultConfiguration.java`
#### Snippet
```java
   public static int DEFAULT_NETWORK_CHECK_TIMEOUT = 1000;

   public static String DEFAULT_NETWORK_CHECK_NIC = null;

   public static final String DEFAULT_INTERNAL_NAMING_PREFIX = "$.artemis.internal.";
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/config/ActiveMQDefaultConfiguration.java`
#### Snippet
```java

   // Will this backup server come live on a normal server shutdown
   private static boolean DEFAULT_FAILOVER_ON_SERVER_SHUTDOWN = false;

   // Will a shared-store master startup wait for activation
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/config/ActiveMQDefaultConfiguration.java`
#### Snippet
```java

   // Will the broker allow messages with no validated user
   private static boolean DEFAULT_REJECT_EMPTY_VALIDATED_USER = false;

   // its possible that you only want a server to partake in scale down as a receiver, via a group. In this case set scale-down to false
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/config/ActiveMQDefaultConfiguration.java`
#### Snippet
```java
   public static final String DEFAULT_INTERNAL_NAMING_PREFIX = "$.artemis.internal.";

   public static boolean DEFAULT_VOTE_ON_REPLICATION_FAILURE = false;

   //how many times we retry a vote before restarting as a backup
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/config/ActiveMQDefaultConfiguration.java`
#### Snippet
```java

   // If true then the server will request a backup on another node
   private static boolean DEFAULT_HAPOLICY_REQUEST_BACKUP = false;

   // How many times the live server will try to request a backup, -1 means for ever.
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/config/ActiveMQDefaultConfiguration.java`
#### Snippet
```java
   private static boolean DEFAULT_CLUSTER_DUPLICATE_DETECTION = true;

   private static boolean DEFAULT_CLUSTER_FORWARD_WHEN_NO_CONSUMERS = false;

   // how should messages be load balanced?
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/config/ActiveMQDefaultConfiguration.java`
#### Snippet
```java
   public static String DEFAULT_NETWORK_CHECK_LIST = null;

   public static String DEFAULT_NETWORK_CHECK_URL_LIST = null;

   public static long DEFAULT_NETWORK_CHECK_PERIOD = 5000;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/config/ActiveMQDefaultConfiguration.java`
#### Snippet
```java

   // true means that the server will load configuration from the configuration files
   private static boolean DEFAULT_FILE_DEPLOYMENT_ENABLED = false;

   // true means that the server will use the file based journal for persistence.
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/config/ActiveMQDefaultConfiguration.java`
#### Snippet
```java

   // Default database url.  Derby database is used by default.
   private static String DEFAULT_DATABASE_URL = null;

   // Default JDBC Driver class name, derby by default just for demo purposes
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/config/ActiveMQDefaultConfiguration.java`
#### Snippet
```java

   // whether this is an exclusive divert
   private static boolean DEFAULT_DIVERT_EXCLUSIVE = false;

   // how the divert should handle the message's routing type
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/config/ActiveMQDefaultConfiguration.java`
#### Snippet
```java

   // Whether to log messages about the journal write rate
   private static boolean DEFAULT_JOURNAL_LOG_WRITE_RATE = false;

   // the size (in bytes) of each journal file
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/config/ActiveMQDefaultConfiguration.java`
#### Snippet
```java

   // If SESSION-notifications should be suppressed or not
   public static boolean DEFAULT_SUPPRESS_SESSION_NOTIFICATIONS = false;

   public static final long DEFAULT_EMBEDDED_WEB_SERVER_RESTART_TIMEOUT = 5000;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/config/ActiveMQDefaultConfiguration.java`
#### Snippet
```java
   public static final String BROKER_PROPERTIES_KEY_SURROUND_PROPERTY = "key.surround";

   public static String DEFAULT_NETWORK_CHECK_LIST = null;

   public static String DEFAULT_NETWORK_CHECK_URL_LIST = null;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/config/ActiveMQDefaultConfiguration.java`
#### Snippet
```java

   // True means that the delivery count is persisted before delivery. False means that this only happens after a message has been cancelled.
   private static boolean DEFAULT_PERSIST_DELIVERY_COUNT_BEFORE_DELIVERY = false;

   // the directory to store paged messages in
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/CompressedLargeMessageControllerImpl.java`
#### Snippet
```java
   }

   DataInputStream dataInput = null;

   private DataInputStream getStream() {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/LargeMessageControllerImpl.java`
#### Snippet
```java
   private final FileCache fileCache;

   private boolean local = false;

   public LargeMessageControllerImpl(final ClientConsumerInternal consumerInternal,
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/LargeMessageControllerImpl.java`
#### Snippet
```java
   private boolean streamEnded = false;

   private boolean streamClosed = false;

   private final long readTimeout;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/LargeMessageControllerImpl.java`
#### Snippet
```java
   private final int bufferSize;

   private boolean streamEnded = false;

   private boolean streamClosed = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/LargeMessageControllerImpl.java`
#### Snippet
```java
    * This is to control if packets are arriving for a better timeout control
    */
   private boolean packetAdded = false;

   private long packetPosition = -1;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/LargeMessageControllerImpl.java`
#### Snippet
```java
   private long packetPosition = -1;

   private long lastIndex = 0;

   private long packetLastPosition = -1;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/LargeMessageControllerImpl.java`
#### Snippet
```java
   private final long readTimeout;

   private long readerIndex = 0;

   /**
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/LargeMessageControllerImpl.java`
#### Snippet
```java
   private final LinkedBlockingQueue<LargeData> largeMessageData = new LinkedBlockingQueue<>();

   private volatile LargeData currentPacket = null;

   private final long totalSize;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ClientConsumerImpl.java`
#### Snippet
```java
   private volatile ClientMessageInternal lastAckedMessage;

   private boolean stopped = false;

   private AtomicLong forceDeliveryCount = new AtomicLong(0);
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ServerLocatorImpl.java`
#### Snippet
```java
    *  which means it's a cluster connection.
    *  We should not use retries */
   private volatile boolean disableDiscoveryRetries = false;

   // if the system should shutdown the pool when shutting down
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ClientSessionImpl.java`
#### Snippet
```java
   private volatile boolean mayAttemptToFailover = true;

   private volatile boolean resetCreditManager = false;

   /**
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/message/impl/CoreMessage.java`
#### Snippet
```java
   protected ByteBuf buffer;

   private volatile boolean validBuffer = false;

   protected volatile ResetLimitWrappedActiveMQBuffer writableBuffer;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/message/impl/CoreMessage.java`
#### Snippet
```java
   private final class CoreLargeBodyReaderImpl implements LargeBodyReader {

      private int lastPos = 0;

      private CoreLargeBodyReaderImpl() {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/RemotingConnectionImpl.java`
#### Snippet
```java
   private volatile SimpleIDGenerator idGenerator = new SimpleIDGenerator(CHANNEL_ID.USER.id);

   private boolean idGeneratorSynced = false;

   private final Object transferLock = new Object();
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/CreateSessionMessage_V2.java`
#### Snippet
```java
public class CreateSessionMessage_V2 extends CreateSessionMessage {

   private String clientID = null;

   public CreateSessionMessage_V2(final String name,
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/utils/DeflaterReader.java`
#### Snippet
```java

   private final Deflater deflater = new Deflater();
   private boolean isFinished = false;
   private boolean compressDone = false;

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/utils/DeflaterReader.java`
#### Snippet
```java
   private final Deflater deflater = new Deflater();
   private boolean isFinished = false;
   private boolean compressDone = false;

   private InputStream input;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/utils/InflaterWriter.java`
#### Snippet
```java
   private final byte[] writeBuffer = new byte[1024];

   private int writePointer = 0;

   private final byte[] outputBuffer = new byte[writeBuffer.length * 2];
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/utils/TimeAndCounterIDGenerator.java`
#### Snippet
```java
   private final AtomicLong counter = new AtomicLong(0);

   private volatile boolean wrapped = false;

   private volatile long tmMark;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-hawtio/artemis-plugin/src/main/java/org/apache/activemq/hawtio/plugin/PluginContextListener.java`
#### Snippet
```java
   private static final Logger logger = LoggerFactory.getLogger(MethodHandles.lookup().lookupClass());

   HawtioPlugin plugin = null;

   @Override
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/security/jaas/ReloadableProperties.java`
#### Snippet
```java
   private final PropertiesLoader.FileNameKey key;
   private final Checksum adler32 = new Adler32();
   private long checksum = 0;

   public ReloadableProperties(PropertiesLoader.FileNameKey key) {
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/NettyConnector.java`
#### Snippet
```java
      private Channel channel;

      private long lastSendTime = 0;

      private boolean waitingGet = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/NettyConnector.java`
#### Snippet
```java
      private final HttpClientCodec httpClientCodec;
      private final CountDownLatch latch = new CountDownLatch(1);
      private boolean handshakeComplete = false;

      private HttpUpgradeHandler(ChannelPipeline pipeline, HttpClientCodec httpClientCodec) {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/NettyConnector.java`
#### Snippet
```java
      private final FutureLatch handShakeFuture = new FutureLatch();

      private boolean active = false;

      private boolean handshaking = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/NettyConnector.java`
#### Snippet
```java
      private boolean active = false;

      private boolean handshaking = false;

      private String cookie;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/NettyConnector.java`
#### Snippet
```java
      private long lastSendTime = 0;

      private boolean waitingGet = false;

      private HttpIdleTimer task;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/NettyConnector.java`
#### Snippet
```java
      private class HttpIdleTimer implements Runnable {

         private boolean closed = false;

         private java.util.concurrent.Future<?> future;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/security/jaas/LDAPLoginModule.java`
#### Snippet
```java
   private String username;
   private final Set<RolePrincipal> groups = new HashSet<>();
   private boolean userAuthenticated = false;
   private boolean authenticateUser = true;
   private Subject brokerGssapiIdentity = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/security/jaas/LDAPLoginModule.java`
#### Snippet
```java
   private boolean userAuthenticated = false;
   private boolean authenticateUser = true;
   private Subject brokerGssapiIdentity = null;
   private boolean isRoleAttributeSet = false;
   private String roleAttributeName = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/security/jaas/LDAPLoginModule.java`
#### Snippet
```java
   private Subject brokerGssapiIdentity = null;
   private boolean isRoleAttributeSet = false;
   private String roleAttributeName = null;
   private List<String> noCacheExceptions;

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/security/jaas/LDAPLoginModule.java`
#### Snippet
```java
   private boolean authenticateUser = true;
   private Subject brokerGssapiIdentity = null;
   private boolean isRoleAttributeSet = false;
   private String roleAttributeName = null;
   private List<String> noCacheExceptions;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/security/jaas/LDAPLoginModule.java`
#### Snippet
```java
   private List<String> noCacheExceptions;

   private String codecClass = null;

   @Override
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/CoreQueueConfiguration.java`
#### Snippet
```java
   private boolean durable = true;

   private String user = null;

   private Boolean exclusive;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/CoreQueueConfiguration.java`
#### Snippet
```java
   private static final long serialVersionUID = 650404974977490254L;

   private String address = null;

   private String name = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/CoreQueueConfiguration.java`
#### Snippet
```java
   private String name = null;

   private String filterString = null;

   private boolean durable = true;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/CoreQueueConfiguration.java`
#### Snippet
```java
   private String address = null;

   private String name = null;

   private String filterString = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/ClusterConnectionConfiguration.java`
#### Snippet
```java
   private List<String> staticConnectors = Collections.emptyList();

   private String discoveryGroupName = null;

   private int maxHops = ActiveMQDefaultConfiguration.getDefaultClusterMaxHops();
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/ClusterConnectionConfiguration.java`
#### Snippet
```java
   private int producerWindowSize = ActiveMQDefaultConfiguration.getDefaultBridgeProducerWindowSize();

   private boolean allowDirectConnectionsOnly = false;

   private int minLargeMessageSize = ActiveMQClient.DEFAULT_MIN_LARGE_MESSAGE_SIZE;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/DivertConfiguration.java`
#### Snippet
```java
   private static final long serialVersionUID = 6910543740464269629L;

   private String name = null;

   private String routingName = UUIDGenerator.getInstance().generateStringUUID();
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/DivertConfiguration.java`
#### Snippet
```java
   private String address = null;

   private String forwardingAddress = null;

   private boolean exclusive = ActiveMQDefaultConfiguration.isDefaultDivertExclusive();
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/DivertConfiguration.java`
#### Snippet
```java
   private String routingName = UUIDGenerator.getInstance().generateStringUUID();

   private String address = null;

   private String forwardingAddress = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/DivertConfiguration.java`
#### Snippet
```java
   private boolean exclusive = ActiveMQDefaultConfiguration.isDefaultDivertExclusive();

   private String filterString = null;

   private TransformerConfiguration transformerConfiguration = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/DivertConfiguration.java`
#### Snippet
```java
   private String filterString = null;

   private TransformerConfiguration transformerConfiguration = null;

   private ComponentConfigurationRoutingType routingType = ComponentConfigurationRoutingType.valueOf(ActiveMQDefaultConfiguration.getDefaultDivertRoutingType());
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/CoreAddressConfiguration.java`
#### Snippet
```java
public class CoreAddressConfiguration implements Serializable {

   private String name = null;

   private EnumSet<RoutingType> routingTypes = EnumSet.noneOf(RoutingType.class);
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/ScaleDownConfiguration.java`
#### Snippet
```java
   private List<String> connectors = new ArrayList<>();

   private String discoveryGroup = null;

   private String groupName = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/ScaleDownConfiguration.java`
#### Snippet
```java
   private String discoveryGroup = null;

   private String groupName = null;

   private String clusterName = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/ScaleDownConfiguration.java`
#### Snippet
```java
   private String groupName = null;

   private String clusterName = null;

   private boolean enabled = ActiveMQDefaultConfiguration.isDefaultScaleDownEnabled();
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/JMXConnectorConfiguration.java`
#### Snippet
```java
   private String objectName = "connector:name=rmi";
   private String authenticatorType = "password";
   private boolean secured =  false;
   private String keyStoreProvider = TransportConstants.DEFAULT_KEYSTORE_PROVIDER;
   private String keyStoreType = TransportConstants.DEFAULT_KEYSTORE_TYPE;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/ha/ReplicationPrimaryPolicyConfiguration.java`
#### Snippet
```java
   private Long retryReplicationWait = ActiveMQDefaultConfiguration.getDefaultRetryReplicationWait();

   private DistributedPrimitiveManagerConfiguration distributedManagerConfiguration = null;

   private String coordinationId = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/ha/ReplicationPrimaryPolicyConfiguration.java`
#### Snippet
```java
   private String groupName = null;

   private String clusterName = null;

   private long initialReplicationSyncTimeout = ActiveMQDefaultConfiguration.getDefaultInitialReplicationSyncTimeout();
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/ha/ReplicationPrimaryPolicyConfiguration.java`
#### Snippet
```java
public class ReplicationPrimaryPolicyConfiguration implements HAPolicyConfiguration {

   private String groupName = null;

   private String clusterName = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/ha/ReplicationPrimaryPolicyConfiguration.java`
#### Snippet
```java
   private DistributedPrimitiveManagerConfiguration distributedManagerConfiguration = null;

   private String coordinationId = null;

   private int maxSavedReplicatedJournalsSize = ActiveMQDefaultConfiguration.getDefaultMaxSavedReplicatedJournalsSize();
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/ha/ReplicatedPolicyConfiguration.java`
#### Snippet
```java
   private boolean checkForLiveServer = ActiveMQDefaultConfiguration.isDefaultCheckForLiveServer();

   private String groupName = null;

   private String clusterName = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/ha/ReplicatedPolicyConfiguration.java`
#### Snippet
```java
   private String groupName = null;

   private String clusterName = null;

   private int maxSavedReplicatedJournalsSize = ActiveMQDefaultConfiguration.getDefaultMaxSavedReplicatedJournalsSize();
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/ha/ReplicaPolicyConfiguration.java`
#### Snippet
```java
   * used in the replicated policy after failover
   * */
   private boolean allowFailBack = false;

   private long initialReplicationSyncTimeout = ActiveMQDefaultConfiguration.getDefaultInitialReplicationSyncTimeout();
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/ha/ReplicaPolicyConfiguration.java`
#### Snippet
```java
   private int maxSavedReplicatedJournalsSize = ActiveMQDefaultConfiguration.getDefaultMaxSavedReplicatedJournalsSize();

   private String groupName = null;

   private boolean restartBackup = ActiveMQDefaultConfiguration.isDefaultRestartBackup();
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/ha/ReplicaPolicyConfiguration.java`
#### Snippet
```java
public class ReplicaPolicyConfiguration implements HAPolicyConfiguration {

   private String clusterName = null;

   private int maxSavedReplicatedJournalsSize = ActiveMQDefaultConfiguration.getDefaultMaxSavedReplicatedJournalsSize();
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/ha/ReplicationBackupPolicyConfiguration.java`
#### Snippet
```java
   private long retryReplicationWait = ActiveMQDefaultConfiguration.getDefaultRetryReplicationWait();

   private DistributedPrimitiveManagerConfiguration distributedManagerConfiguration = null;

   public static final ReplicationBackupPolicyConfiguration withDefault() {
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/ha/ReplicationBackupPolicyConfiguration.java`
#### Snippet
```java
public class ReplicationBackupPolicyConfiguration implements HAPolicyConfiguration {

   private String clusterName = null;

   private int maxSavedReplicatedJournalsSize = ActiveMQDefaultConfiguration.getDefaultMaxSavedReplicatedJournalsSize();
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/ha/ReplicationBackupPolicyConfiguration.java`
#### Snippet
```java
    * used in the replicated policy after failover
    * */
   private boolean allowFailBack = false;

   private long initialReplicationSyncTimeout = ActiveMQDefaultConfiguration.getDefaultInitialReplicationSyncTimeout();
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/ha/ReplicationBackupPolicyConfiguration.java`
#### Snippet
```java
   private int maxSavedReplicatedJournalsSize = ActiveMQDefaultConfiguration.getDefaultMaxSavedReplicatedJournalsSize();

   private String groupName = null;

   /*
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/BridgeConfiguration.java`
#### Snippet
```java
   public static String CONFIGURATION_MANAGED = "configuration-managed";

   private String name = null;

   private String queueName = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/BridgeConfiguration.java`
#### Snippet
```java
   private String queueName = null;

   private String forwardingAddress = null;

   private String filterString = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/BridgeConfiguration.java`
#### Snippet
```java
   private int concurrency = ActiveMQDefaultConfiguration.getDefaultBridgeConcurrency();

   private String parentName = null;

   private boolean configurationManaged = true;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/BridgeConfiguration.java`
#### Snippet
```java
   private boolean ha = false;

   private TransformerConfiguration transformerConfiguration = null;

   private long retryInterval = ActiveMQClient.DEFAULT_RETRY_INTERVAL;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/BridgeConfiguration.java`
#### Snippet
```java
   private String discoveryGroupName = null;

   private boolean ha = false;

   private TransformerConfiguration transformerConfiguration = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/BridgeConfiguration.java`
#### Snippet
```java
   private String name = null;

   private String queueName = null;

   private String forwardingAddress = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/BridgeConfiguration.java`
#### Snippet
```java
   private List<String> staticConnectors = null;

   private String discoveryGroupName = null;

   private boolean ha = false;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/BridgeConfiguration.java`
#### Snippet
```java
   private String forwardingAddress = null;

   private String filterString = null;

   private List<String> staticConnectors = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/BridgeConfiguration.java`
#### Snippet
```java
   private String filterString = null;

   private List<String> staticConnectors = null;

   private String discoveryGroupName = null;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/impl/FileConfiguration.java`
#### Snippet
```java
   public static final int DEFAULT_CONFIRMATION_WINDOW_SIZE = 1024 * 1024;

   private boolean parsed = false;

   @Override
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/impl/SecurityConfiguration.java`
#### Snippet
```java
   protected final Map<String, User> users = new HashMap<>();

   protected String defaultUser = null;

   /**
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/routing/PoolConfiguration.java`
#### Snippet
```java
   private boolean localTargetEnabled = false;

   private String clusterConnection = null;

   private List<String> staticConnectors = null;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/routing/PoolConfiguration.java`
#### Snippet
```java
   private String password;

   private boolean localTargetEnabled = false;

   private String clusterConnection = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/routing/PoolConfiguration.java`
#### Snippet
```java
   private List<String> staticConnectors = null;

   private String discoveryGroupName = null;

   private int checkPeriod = 5000;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/routing/PoolConfiguration.java`
#### Snippet
```java
   private String clusterConnection = null;

   private List<String> staticConnectors = null;

   private String discoveryGroupName = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/routing/ConnectionRouterConfiguration.java`
#### Snippet
```java
   private String localTargetFilter = null;
   private CacheConfiguration cacheConfiguration = null;
   private PoolConfiguration poolConfiguration = null;
   private NamedPropertyConfiguration policyConfiguration = null;

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/routing/ConnectionRouterConfiguration.java`
#### Snippet
```java
   private KeyType keyType = KeyType.SOURCE_IP;
   private String keyFilter = null;
   private String localTargetFilter = null;
   private CacheConfiguration cacheConfiguration = null;
   private PoolConfiguration poolConfiguration = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/routing/ConnectionRouterConfiguration.java`
#### Snippet
```java
   private String name = null;
   private KeyType keyType = KeyType.SOURCE_IP;
   private String keyFilter = null;
   private String localTargetFilter = null;
   private CacheConfiguration cacheConfiguration = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/routing/ConnectionRouterConfiguration.java`
#### Snippet
```java
public class ConnectionRouterConfiguration implements Serializable {

   private String name = null;
   private KeyType keyType = KeyType.SOURCE_IP;
   private String keyFilter = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/routing/ConnectionRouterConfiguration.java`
#### Snippet
```java
   private CacheConfiguration cacheConfiguration = null;
   private PoolConfiguration poolConfiguration = null;
   private NamedPropertyConfiguration policyConfiguration = null;

   public String getName() {
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/routing/ConnectionRouterConfiguration.java`
#### Snippet
```java
   private String keyFilter = null;
   private String localTargetFilter = null;
   private CacheConfiguration cacheConfiguration = null;
   private PoolConfiguration poolConfiguration = null;
   private NamedPropertyConfiguration policyConfiguration = null;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/routing/CacheConfiguration.java`
#### Snippet
```java

public class CacheConfiguration implements Serializable {
   private boolean persisted = false;

   private int timeout = 0;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/routing/CacheConfiguration.java`
#### Snippet
```java
   private boolean persisted = false;

   private int timeout = 0;

   public CacheConfiguration() {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/amqpBrokerConnectivity/AMQPMirrorBrokerConnectionElement.java`
#### Snippet
```java
   boolean messageAcknowledgements = true;

   boolean sync = false;

   SimpleString mirrorSNF;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/impl/Page.java`
#### Snippet
```java
   private final long pageId;

   private boolean suspiciousRecords = false;

   private volatile int numberOfMessages;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/impl/PagingStoreFactoryDatabase.java`
#### Snippet
```java
   }

   private boolean started = false;

   private final IOCriticalErrorListener criticalErrorListener;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/impl/PagedMessageImpl.java`
#### Snippet
```java
   private long[] queueIDs;

   private long transactionID = 0;

   private final int storedSize;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/impl/PageTransactionInfoImpl.java`
#### Snippet
```java
   private volatile boolean committed = false;

   private volatile boolean useRedelivery = false;

   private volatile boolean rolledback = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/impl/PageTransactionInfoImpl.java`
#### Snippet
```java
   private volatile long recordID = -1;

   private volatile boolean committed = false;

   private volatile boolean useRedelivery = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/impl/PageTransactionInfoImpl.java`
#### Snippet
```java
   private volatile boolean useRedelivery = false;

   private volatile boolean rolledback = false;

   private volatile int numberOfMessages = 0;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/impl/PageTransactionInfoImpl.java`
#### Snippet
```java
   private volatile int numberOfMessages = 0;

   private volatile int numberOfPersistentMessages = 0;

   private List<LateDelivery> lateDeliveries;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/impl/PageTransactionInfoImpl.java`
#### Snippet
```java
   private volatile boolean rolledback = false;

   private volatile int numberOfMessages = 0;

   private volatile int numberOfPersistentMessages = 0;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/impl/PageTransactionInfoImpl.java`
#### Snippet
```java
      private final HashMap<PageTransactionInfo, AtomicInteger> countsToUpdate = new HashMap<>();

      private boolean stored = false;

      private final StorageManager storageManager;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/impl/PagingManagerImpl.java`
#### Snippet
```java
   private volatile boolean cleanupEnabled = true;

   private volatile boolean diskFull = false;

   private volatile long diskUsableSpace = 0;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/impl/PagingManagerImpl.java`
#### Snippet
```java
   private static final Logger logger = LoggerFactory.getLogger(MethodHandles.lookup().lookupClass());

   private volatile boolean started = false;

   /**
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/impl/PagingManagerImpl.java`
#### Snippet
```java
   private volatile long diskUsableSpace = 0;

   private volatile long diskTotalSpace = 0;

   private final Executor managerExecutor;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/impl/PagingManagerImpl.java`
#### Snippet
```java
   private volatile boolean diskFull = false;

   private volatile long diskUsableSpace = 0;

   private volatile long diskTotalSpace = 0;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/impl/PagingManagerImpl.java`
#### Snippet
```java
   private final ConcurrentMap</*TransactionID*/Long, PageTransactionInfo> transactions = new ConcurrentHashMap<>();

   private ActiveMQScheduledComponent snapshotUpdater = null;

   private final SimpleString managementAddress;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/cursor/PagedReferenceImpl.java`
#### Snippet
```java

   @SuppressWarnings("unused")
   private volatile int deliveryCount = 0;

   protected final PageSubscription subscription;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/cursor/PagedReferenceImpl.java`
#### Snippet
```java
   private long consumerID;

   private boolean hasConsumerID = false;

   @SuppressWarnings("unused")
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/impl/PagingStoreImpl.java`
#### Snippet
```java
      private final Set<PagingStore> usedStores = new HashSet<>();

      private boolean stored = false;

      public void addStore(PagingStore store) {
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/impl/PagingStoreImpl.java`
#### Snippet
```java

   //it's being guarded by lock.writeLock().lock() and never read concurrently
   private long currentPageSize = 0;

   private final SimpleString storeName;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/impl/PagingStoreImpl.java`
#### Snippet
```java
   private final ReadWriteLock lock = new ReentrantReadWriteLock();

   private volatile boolean running = false;

   private final boolean syncNonTransactional;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/impl/PagingStoreImpl.java`
#### Snippet
```java
   private final boolean syncNonTransactional;

   private volatile boolean blocking = false;

   private volatile boolean blockedViaAddressControl = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/impl/PagingStoreImpl.java`
#### Snippet
```java
   private volatile boolean blocking = false;

   private volatile boolean blockedViaAddressControl = false;

   private long rejectThreshold;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/impl/PagingStoreImpl.java`
#### Snippet
```java
   private volatile Page currentPage;

   private volatile boolean paging = false;

   private final PageCursorProvider cursorProvider;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/cursor/impl/BasePagingCounter.java`
#### Snippet
```java
public abstract class BasePagingCounter implements PageSubscriptionCounter {

   private volatile  boolean rebuilding = false;

   @Override
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/cursor/impl/PageSubscriptionImpl.java`
#### Snippet
```java
      private PagedReference currentDelivery = null;

      private volatile PagedReference lastDelivery = null;

      private final boolean browsing;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/cursor/impl/PageSubscriptionImpl.java`
#### Snippet
```java
      }

      private PagedReference currentDelivery = null;

      private volatile PagedReference lastDelivery = null;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/NodeManager.java`
#### Snippet
```java
   private SimpleString nodeID;
   private UUID uuid;
   private boolean isStarted = false;
   private final Set<FileLockNodeManager.LockListener> lockListeners;
   public static final long NULL_NODE_ACTIVATION_SEQUENCE = -1;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/BucketMessageGroups.java`
#### Snippet
```java
   private final int bucketCount;
   private C[] buckets;
   private int size = 0;

   public BucketMessageGroups(int bucketCount) {
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerProducerImpl.java`
#### Snippet
```java
   private final long creationTime;
   private volatile long messagesSent = 0;
   private volatile long messagesSentSize = 0;

   private static final AtomicLongFieldUpdater<ServerProducerImpl> messagesSentUpdater = AtomicLongFieldUpdater.newUpdater(ServerProducerImpl.class, "messagesSent");
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerProducerImpl.java`
#### Snippet
```java
   private final String protocol;
   private final long creationTime;
   private volatile long messagesSent = 0;
   private volatile long messagesSentSize = 0;

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/SharedNothingLiveActivation.java`
#### Snippet
```java
   static final class NodeIdListener implements ClusterTopologyListener {

      volatile boolean isNodePresent = false;

      private final SimpleString nodeId;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/LegacyLDAPSecuritySettingPlugin.java`
#### Snippet
```java
   private boolean enableListener = true;
   private int refreshInterval = 0;
   private boolean mapAdminToManage = false;
   private boolean allowQueueAdminOnRead = false;

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/LegacyLDAPSecuritySettingPlugin.java`
#### Snippet
```java
   private int refreshInterval = 0;
   private boolean mapAdminToManage = false;
   private boolean allowQueueAdminOnRead = false;

   private DirContext context;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/LegacyLDAPSecuritySettingPlugin.java`
#### Snippet
```java
   private String writePermissionValue = "write";
   private boolean enableListener = true;
   private int refreshInterval = 0;
   private boolean mapAdminToManage = false;
   private boolean allowQueueAdminOnRead = false;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/RoutingContextImpl.java`
#### Snippet
```java
   private RoutingType routingType;

   Boolean reusable = null;

   Boolean internalOnly = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/RoutingContextImpl.java`
#### Snippet
```java
   Boolean reusable = null;

   Boolean internalOnly = null;

   volatile int version;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/RoutingContextImpl.java`
#### Snippet
```java
   volatile int version;

   boolean mirrorDisabled = false;

   private boolean duplicateDetection = true;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/AddressInfo.java`
#### Snippet
```java
   private volatile long bindingRemovedTimestamp = -1;

   private volatile boolean paused = false;

   private PostOffice postOffice;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/AddressInfo.java`
#### Snippet
```java
   private static final AtomicLongFieldUpdater<AddressInfo> routedMessageCountUpdater = AtomicLongFieldUpdater.newUpdater(AddressInfo.class, "routedMessageCount");

   private volatile long unRoutedMessageCount = 0;

   private static final AtomicLongFieldUpdater<AddressInfo> unRoutedMessageCountUpdater = AtomicLongFieldUpdater.newUpdater(AddressInfo.class, "unRoutedMessageCount");
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/AddressInfo.java`
#### Snippet
```java
   private boolean internal = false;

   private volatile long routedMessageCount = 0;

   private static final AtomicLongFieldUpdater<AddressInfo> routedMessageCountUpdater = AtomicLongFieldUpdater.newUpdater(AddressInfo.class, "routedMessageCount");
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/AddressInfo.java`
#### Snippet
```java
   private RoutingType firstSeen;

   private boolean internal = false;

   private volatile long routedMessageCount = 0;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/AddressInfo.java`
#### Snippet
```java
   private boolean autoCreated = false;

   private volatile boolean temporary = false;

   // Do not be tempted to cache this on an static variable.
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/AddressInfo.java`
#### Snippet
```java
   private SimpleString name;

   private boolean autoCreated = false;

   private volatile boolean temporary = false;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ScaleDownHandler.java`
#### Snippet
```java
      private final Queue queue;
      private LinkedListIterator<MessageReference> memoryIterator;
      private MessageReference lastRef = null;
      private final PagingStore store;

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/RefsOperation.java`
#### Snippet
```java
   private boolean delivering = true;

   private String lingerSessionId = null;

   public RefsOperation(Queue queue, AckReason reason, StorageManager storageManager) {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/RefsOperation.java`
#### Snippet
```java
    * to not perform reschedule redelivery check
    */
   protected boolean ignoreRedeliveryCheck = false;

   private boolean delivering = true;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/RefsOperation.java`
#### Snippet
```java
   List<MessageReference> refsToAck = new ArrayList<>();

   List<MessageReference> pagedMessagesToPostACK = null;

   /**
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerConsumerImpl.java`
#### Snippet
```java
   private final Binding binding;

   private boolean transferring = false;

   private final long creationTime;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerConsumerImpl.java`
#### Snippet
```java
      private static final AtomicLongFieldUpdater<ServerConsumerMetrics> messagesAcknowledgedUpdater = AtomicLongFieldUpdater.newUpdater(ServerConsumerMetrics.class, "messagesAcknowledged");

      private volatile long messagesAcknowledged = 0;


```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerConsumerImpl.java`
#### Snippet
```java
   private boolean anycast = false;

   private boolean isClosed = false;

   @Override
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerConsumerImpl.java`
#### Snippet
```java
      private volatile long lastDeliveredTime = 0;

      private volatile long lastAcknowledgedTime = 0;

      private static final AtomicLongFieldUpdater<ServerConsumerMetrics>messagesDeliveredUpdater = AtomicLongFieldUpdater.newUpdater(ServerConsumerMetrics.class, "messagesDelivered");
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerConsumerImpl.java`
#### Snippet
```java
      private volatile long messagesDeliveredSize = 0;

      private volatile long lastDeliveredTime = 0;

      private volatile long lastAcknowledgedTime = 0;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerConsumerImpl.java`
#### Snippet
```java
   private AtomicLong messageConsumedSnapshot = new AtomicLong(0);

   private boolean requiresLegacyPrefix = false;

   private boolean anycast = false;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerConsumerImpl.java`
#### Snippet
```java
   protected class BrowserDeliverer implements Runnable {

      protected MessageReference current = null;

      public BrowserDeliverer(final LinkedListIterator<MessageReference> iterator) {
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerConsumerImpl.java`
#### Snippet
```java
      private static final AtomicLongFieldUpdater<ServerConsumerMetrics>messagesDeliveredUpdater = AtomicLongFieldUpdater.newUpdater(ServerConsumerMetrics.class, "messagesDelivered");

      private volatile long messagesDelivered = 0;

      private static final AtomicLongFieldUpdater<ServerConsumerMetrics> messagesAcknowledgedUpdater = AtomicLongFieldUpdater.newUpdater(ServerConsumerMetrics.class, "messagesAcknowledged");
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerConsumerImpl.java`
#### Snippet
```java
      private final MessageReference ref;

      private boolean sentInitialPacket = false;

      /**
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerConsumerImpl.java`
#### Snippet
```java
      private static final AtomicLongFieldUpdater<ServerConsumerMetrics> messagesInTransitSizeUpdater = AtomicLongFieldUpdater.newUpdater(ServerConsumerMetrics.class, "messagesInTransitSize");

      private volatile long messagesInTransitSize = 0;

      private static final AtomicIntegerFieldUpdater<ServerConsumerMetrics> messagesAcknowledgedAwaitingCommitUpdater = AtomicIntegerFieldUpdater.newUpdater(ServerConsumerMetrics.class, "messagesAcknowledgedAwaitingCommit");
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerConsumerImpl.java`
#### Snippet
```java
      private static final AtomicLongFieldUpdater<ServerConsumerMetrics>messagesDeliveredSizeUpdater = AtomicLongFieldUpdater.newUpdater(ServerConsumerMetrics.class, "messagesDeliveredSize");

      private volatile long messagesDeliveredSize = 0;

      private volatile long lastDeliveredTime = 0;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerConsumerImpl.java`
#### Snippet
```java
      private static final AtomicIntegerFieldUpdater<ServerConsumerMetrics> messagesAcknowledgedAwaitingCommitUpdater = AtomicIntegerFieldUpdater.newUpdater(ServerConsumerMetrics.class, "messagesAcknowledgedAwaitingCommit");

      private volatile int messagesAcknowledgedAwaitingCommit = 0;

      private static final AtomicLongFieldUpdater<ServerConsumerMetrics>messagesDeliveredSizeUpdater = AtomicLongFieldUpdater.newUpdater(ServerConsumerMetrics.class, "messagesDeliveredSize");
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerConsumerImpl.java`
#### Snippet
```java
   private boolean requiresLegacyPrefix = false;

   private boolean anycast = false;

   private boolean isClosed = false;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerConsumerImpl.java`
#### Snippet
```java
   private boolean started;

   private volatile CoreLargeMessageDeliverer largeMessageDeliverer = null;

   @Override
```

### RedundantFieldInitialization
Field initialization to `0L` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/InVMNodeManager.java`
#### Snippet
```java
   public volatile State state = NOT_STARTED;

   public long failoverPause = 0L;

   public InVMNodeManager(boolean replicatedBackup) {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/FileLockNodeManager.java`
#### Snippet
```java
   private final long lockAcquisitionTimeoutNanos;

   protected boolean interrupted = false;

   private final ScheduledExecutorService scheduledPool;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ConnectorsService.java`
#### Snippet
```java
   private final ScheduledExecutorService scheduledPool;

   private boolean isStarted = false;

   private final Configuration configuration;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java
   // try to close one session from different threads
   // concurrently.
   private volatile boolean closed = false;

   private boolean prefixEnabled = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java
   private volatile boolean closed = false;

   private boolean prefixEnabled = false;

   private Map<SimpleString, RoutingType> prefixes;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java
   protected final ManagementService managementService;

   protected volatile boolean started = false;

   protected final Map<SimpleString, TempQueueCleanerUpper> tempQueueCleannerUppers = new HashMap<>();
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/MessageReferenceImpl.java`
#### Snippet
```java

   @SuppressWarnings("unused")
   private volatile int deliveryCount = 0;

   private volatile int persistedCount;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/MessageReferenceImpl.java`
#### Snippet
```java
   private long consumerID;

   private boolean hasConsumerID = false;

   private boolean alreadyAcked;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/client/ActiveMQClient.java`
#### Snippet
```java
   private static ExecutorService globalThreadPool;

   private static boolean injectedPools = false;

   private static ScheduledExecutorService globalScheduledThreadPool;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/jdbc/JdbcNodeManager.java`
#### Snippet
```java
   private ScheduledLeaseLock scheduledBackupLock;
   private final long lockAcquisitionTimeoutMillis;
   private volatile boolean interrupted = false;
   private final LeaseLock.Pauser pauser;

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/group/impl/GroupingHandlerConfiguration.java`
#### Snippet
```java
   private TYPE type = null;

   private SimpleString address = null;

   private long timeout = ActiveMQDefaultConfiguration.getDefaultGroupingHandlerTimeout();
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/group/impl/GroupingHandlerConfiguration.java`
#### Snippet
```java
   private SimpleString name = null;

   private TYPE type = null;

   private SimpleString address = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/group/impl/GroupingHandlerConfiguration.java`
#### Snippet
```java
   public static final String REAPER_PERIOD_PROP_NAME = "org.apache.activemq.GroupingHandlerConfiguration.reaperPeriod";

   private SimpleString name = null;

   private TYPE type = null;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/group/impl/RemoteGroupingHandler.java`
#### Snippet
```java
   private final ConcurrentHashSet<Notification> pendingNotifications = new ConcurrentHashSet<>();

   private boolean started = false;

   public RemoteGroupingHandler(final ExecutorFactory executorFactory,
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/group/impl/LocalGroupingHandler.java`
#### Snippet
```java
   private final long groupTimeout;

   private boolean waitingForBindings = false;

   private final ScheduledExecutorService scheduledExecutor;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/plugin/impl/LoggingActiveMQServerPlugin.java`
#### Snippet
```java
   private boolean logConsumerEvents = false;
   private boolean logDeliveringEvents = false;
   private boolean logSendingEvents = false;
   private boolean logInternalEvents = false;

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/plugin/impl/LoggingActiveMQServerPlugin.java`
#### Snippet
```java
   private boolean logAll = false;
   private boolean logConnectionEvents = false;
   private boolean logSessionEvents = false;
   private boolean logConsumerEvents = false;
   private boolean logDeliveringEvents = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/plugin/impl/LoggingActiveMQServerPlugin.java`
#### Snippet
```java
   private boolean logDeliveringEvents = false;
   private boolean logSendingEvents = false;
   private boolean logInternalEvents = false;

   public boolean isLogAll() {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/plugin/impl/LoggingActiveMQServerPlugin.java`
#### Snippet
```java
   private static final Logger logger = LoggerFactory.getLogger(MethodHandles.lookup().lookupClass());
   private static final long serialVersionUID = 1L;
   private boolean logAll = false;
   private boolean logConnectionEvents = false;
   private boolean logSessionEvents = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/plugin/impl/LoggingActiveMQServerPlugin.java`
#### Snippet
```java
   private boolean logSessionEvents = false;
   private boolean logConsumerEvents = false;
   private boolean logDeliveringEvents = false;
   private boolean logSendingEvents = false;
   private boolean logInternalEvents = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/plugin/impl/LoggingActiveMQServerPlugin.java`
#### Snippet
```java
   private boolean logConnectionEvents = false;
   private boolean logSessionEvents = false;
   private boolean logConsumerEvents = false;
   private boolean logDeliveringEvents = false;
   private boolean logSendingEvents = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/plugin/impl/LoggingActiveMQServerPlugin.java`
#### Snippet
```java
   private static final long serialVersionUID = 1L;
   private boolean logAll = false;
   private boolean logConnectionEvents = false;
   private boolean logSessionEvents = false;
   private boolean logConsumerEvents = false;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/ha/ReplicatedPolicy.java`
#### Snippet
```java
   private boolean checkForLiveServer = ActiveMQDefaultConfiguration.isDefaultCheckForLiveServer();

   private String groupName = null;

   private String clusterName;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/BackupManager.java`
#### Snippet
```java
      private TransportConfiguration connector;
      private ClusterManager clusterManager;
      private volatile boolean stopping = false;
      private volatile boolean announcingBackup;
      private volatile boolean backupAnnounced = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/BackupManager.java`
#### Snippet
```java
      private volatile boolean stopping = false;
      private volatile boolean announcingBackup;
      private volatile boolean backupAnnounced = false;


```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/ha/StandaloneHAManager.java`
#### Snippet
```java
   Map<String, ActiveMQServer> servers = new HashMap<>();

   boolean isStarted = false;

   @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/ClusterController.java`
#### Snippet
```java
      private final CoreRemotingConnection remotingConnection;
      private final ChannelHandler channelHandler;
      boolean authorized = false;

      private ClusterControllerChannelHandler(Channel clusterChannel,
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/ha/ScaleDownPolicy.java`
#### Snippet
```java
   private String discoveryGroup = null;

   private String groupName = null;

   private String clusterName;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/ha/ScaleDownPolicy.java`
#### Snippet
```java
   private List<String> connectors = new ArrayList<>();

   private String discoveryGroup = null;

   private String groupName = null;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/impl/BroadcastGroupImpl.java`
#### Snippet
```java
   private ScheduledFuture<?> future;

   private boolean loggedBroadcastException = false;

   // Each broadcast group has a unique id - we use this to detect when more than one group broadcasts the same node id
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/ha/ReplicaPolicy.java`
#### Snippet
```java
   private int maxSavedReplicatedJournalsSize = ActiveMQDefaultConfiguration.getDefaultMaxSavedReplicatedJournalsSize();

   private String groupName = null;

   private boolean restartBackup = ActiveMQDefaultConfiguration.isDefaultRestartBackup();
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/qourum/SharedNothingBackupQuorum.java`
#### Snippet
```java
   private final NetworkHealthCheck networkHealthCheck;

   private volatile boolean stopped = false;

   private final int quorumVoteWait;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/impl/BridgeImpl.java`
#### Snippet
```java
   private int reconnectAttemptsInUse;

   private int retryCount = 0;

   private NotificationService notificationService;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/impl/BridgeImpl.java`
#### Snippet
```java
   private volatile boolean started;

   private volatile boolean stopping = false;

   private volatile boolean active;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/impl/BridgeImpl.java`
#### Snippet
```java
   // this will happen if a disconnect happened
   // upon reconnection we need to send the nodeUP back into the topology
   protected volatile boolean disconnectedAndDown = false;

   protected String targetNodeID;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/qourum/QuorumManager.java`
#### Snippet
```java
   private final Map<SimpleString, QuorumVoteHandler> handlers = new HashMap<>();

   private boolean started = false;

   /**
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/qourum/QuorumManager.java`
#### Snippet
```java
    * this is the max size that the cluster has been.
    */
   private int maxClusterSize = 0;

   public QuorumManager(ExecutorService threadPool, ClusterController clusterController) {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/impl/ClusterConnectionImpl.java`
#### Snippet
```java
      private final SimpleString queueName;

      private boolean disconnected = false;

      private final Queue queue;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/impl/ClusterConnectionImpl.java`
#### Snippet
```java
      private volatile boolean isClosed = false;

      private volatile boolean reset = false;

      private MessageFlowRecordImpl(final ServerLocatorInternal targetLocator,
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/impl/ClusterConnectionImpl.java`
#### Snippet
```java
   private volatile boolean stopping = false;

   private LiveNotifier liveNotifier = null;

   private final long clusterNotificationInterval;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/impl/ClusterConnectionImpl.java`
#### Snippet
```java
   private final Topology topology;

   private volatile boolean stopping = false;

   private LiveNotifier liveNotifier = null;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/impl/ClusterConnectionImpl.java`
#### Snippet
```java
   private final class LiveNotifier implements Runnable {

      int notificationsSent = 0;

      @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/impl/ClusterConnectionImpl.java`
#### Snippet
```java
      private final Map<SimpleString, RemoteQueueBinding> bindings = new HashMap<>();

      private volatile boolean isClosed = false;

      private volatile boolean reset = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/routing/ConnectionRouter.java`
#### Snippet
```java
   private final Cache cache;

   private volatile boolean started = false;

   public String getName() {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/routing/ConnectionRouterManager.java`
#### Snippet
```java
   private final ScheduledExecutorService scheduledExecutor;

   private volatile boolean started = false;

   private Map<String, ConnectionRouter> connectionRouters = new HashMap<>();
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/routing/pools/AbstractPool.java`
#### Snippet
```java
   private final long quorumParkNanos = TimeUnit.MILLISECONDS.toNanos(100);

   private volatile boolean started = false;


```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/routing/targets/TargetMonitor.java`
#### Snippet
```java
   private final List<TargetProbe> targetProbes;

   private volatile boolean targetReady = false;


```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/routing/targets/ActiveMQTarget.java`
#### Snippet
```java
   private static final Logger logger = LoggerFactory.getLogger(MethodHandles.lookup().lookupClass());

   private boolean connected = false;

   private final ServerLocator serverLocator;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/routing/policies/ConsistentHashModuloPolicy.java`
#### Snippet
```java
   public static final String MODULO = "MODULO";

   int modulo = 0;

   public ConsistentHashModuloPolicy() {
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/embedded/EmbeddedActiveMQ.java`
#### Snippet
```java

   protected ActiveMQSecurityManager securityManager;
   protected String configResourcePath = null;
   protected Configuration configuration;
   protected ActiveMQServer activeMQServer;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/routing/policies/LeastConnectionsPolicy.java`
#### Snippet
```java


   private int connectionCountThreshold = 0;


```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ActiveMQServerImpl.java`
#### Snippet
```java
   private boolean threadPoolSupplied = false;

   private boolean scheduledPoolSupplied = false;

   private final ServiceRegistry serviceRegistry;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ActiveMQServerImpl.java`
#### Snippet
```java
   private final List<SimpleString> scaledDownNodeIDs = new ArrayList<>();

   private boolean threadPoolSupplied = false;

   private boolean scheduledPoolSupplied = false;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/impl/ConfigurationImpl.java`
#### Snippet
```java
   protected String journalDirectory = ActiveMQDefaultConfiguration.getDefaultJournalDir();

   protected String journalRetentionDirectory = null;

   protected long journalRetentionMaxBytes = 0;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/impl/ConfigurationImpl.java`
#### Snippet
```java
   protected int journalBufferTimeout_AIO = ActiveMQDefaultConfiguration.getDefaultJournalBufferTimeoutAio();

   protected Integer deviceBlockSize = null;

   protected int journalBufferSize_AIO = ActiveMQDefaultConfiguration.getDefaultJournalBufferSizeAio();
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/impl/ConfigurationImpl.java`
#### Snippet
```java
   private List<SecuritySettingPlugin> securitySettingPlugins = new ArrayList<>();

   private MetricsConfiguration metricsConfiguration = null;

   private final List<ActiveMQServerBasePlugin> brokerPlugins = new CopyOnWriteArrayList<>();
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/impl/ConfigurationImpl.java`
#### Snippet
```java
   private File artemisInstance;
   private transient JsonObject jsonStatus = JsonLoader.createObjectBuilder().build();
   private transient Checksum transientChecksum = null;


```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/impl/ConfigurationImpl.java`
#### Snippet
```java
   private long criticalAnalyzerTimeout = ActiveMQDefaultConfiguration.getCriticalAnalyzerTimeout();

   private long criticalAnalyzerCheckPeriod = 0; // non set

   private int pageSyncTimeout = ActiveMQDefaultConfiguration.getDefaultJournalBufferTimeoutNio();
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/impl/ConfigurationImpl.java`
#### Snippet
```java
   protected long journalRetentionPeriod;

   protected String nodeManagerLockDirectory = null;

   protected boolean createJournalDir = ActiveMQDefaultConfiguration.isDefaultCreateJournalDir();
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/impl/ConfigurationImpl.java`
#### Snippet
```java
   protected String journalRetentionDirectory = null;

   protected long journalRetentionMaxBytes = 0;

   protected long journalRetentionPeriod;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/ManagementContext.java`
#### Snippet
```java
public class ManagementContext implements ServiceComponent {

   private volatile boolean isStarted = false;
   private JMXAccessControlList accessControlList;
   private JMXConnectorConfiguration jmxConnectorConfiguration;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/MBeanServerFactory.java`
#### Snippet
```java
   private boolean registerWithFactory = true;
   private MBeanServer server;
   private boolean newlyRegistered = false;

   public boolean isLocateExistingServerIfPossible() {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/MBeanServerFactory.java`
#### Snippet
```java
public class MBeanServerFactory {

   private boolean locateExistingServerIfPossible = false;
   private String defaultDomain;
   private boolean registerWithFactory = true;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/transformer/ServerMessageImpl.java`
#### Snippet
```java
   private CoreMessage message;

   private boolean valid = false;

   public boolean isValid() {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/ReplicationResponseMessageV2.java`
#### Snippet
```java
public final class ReplicationResponseMessageV2 extends ReplicationResponseMessage {

   boolean synchronizationIsFinishedAcknowledgement = false;

   public ReplicationResponseMessageV2(final boolean synchronizationIsFinishedAcknowledgement) {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/HttpKeepAliveRunnable.java`
#### Snippet
```java
   private final List<HttpAcceptorHandler> handlers = new ArrayList<>();

   private boolean closed = false;

   private Future<?> future;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/NettySNIHostnameHandler.java`
#### Snippet
```java
public class NettySNIHostnameHandler extends AbstractSniHandler {

   private String hostname = null;

   public String getHostname() {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/impl/ManagementServiceImpl.java`
#### Snippet
```java
   private final SimpleString managementAddress;

   private boolean started = false;

   private final boolean messageCounterEnabled;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/ResourceLimitSettings.java`
#### Snippet
```java
   SimpleString match = null;

   Integer maxConnections = null;

   Integer maxQueues = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/ResourceLimitSettings.java`
#### Snippet
```java
   //   public static final SimpleString DEFAULT_QUEUE_NAME_REGEX = new SimpleString(".+");

   SimpleString match = null;

   Integer maxConnections = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/ResourceLimitSettings.java`
#### Snippet
```java
   Integer maxConnections = null;

   Integer maxQueues = null;

   //   Long maxQueueSizeBytes = null;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/remoting/server/impl/RemotingServiceImpl.java`
#### Snippet
```java


   private volatile boolean started = false;

   private final Set<TransportConfiguration> acceptorsConfig;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/remoting/server/impl/RemotingServiceImpl.java`
#### Snippet
```java
   private ServiceRegistry serviceRegistry;

   private boolean paused = false;

   private AtomicLong totalConnectionCount = new AtomicLong(0);
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/NettyAcceptor.java`
#### Snippet
```java
   private volatile Object providerAgnosticSslContext;

   private volatile int actualPort = 0;

   public NettyAcceptor(final String name,
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/postoffice/QueueInfo.java`
#### Snippet
```java
   private List<SimpleString> filterStrings;

   private volatile int consumersCount = 0;

   private static final AtomicIntegerFieldUpdater<QueueInfo> consumerUpdater = AtomicIntegerFieldUpdater.newUpdater(QueueInfo.class, "consumersCount");
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/postoffice/impl/WildcardAddressManager.java`
#### Snippet
```java
         addressMap.visitMatchingWildcards(address, new AddressMapVisitor<Bindings>() {

            Bindings newBindings = null;
            @Override
            public void visit(Bindings matchingBindings) throws Exception {
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/postoffice/impl/IntegerCache.java`
#### Snippet
```java

   // we're not interested into safe publication here: we need to scale, be fast and save "some" GC to happen
   private static WeakReference<Integer[]> INDEXES = null;

   private static Integer[] ints(int size) {
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/AddressSettings.java`
#### Snippet
```java
   private Long autoDeleteQueuesDelay = null;

   private Boolean autoDeleteQueuesSkipUsageCheck = null;

   private Long autoDeleteQueuesMessageCount = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/AddressSettings.java`
#### Snippet
```java
   private Boolean enableMetrics = null;

   private Integer managementMessageAttributeSizeLimit = null;

   private Boolean enableIngressTimestamp = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/AddressSettings.java`
#### Snippet
```java
   private Integer messageCounterHistoryDayLimit = null;

   private Long redeliveryDelay = null;

   private Double redeliveryMultiplier = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/AddressSettings.java`
#### Snippet
```java
   private Integer defaultConsumersBeforeDispatch = null;

   private Long defaultDelayBeforeDispatch = null;

   private RoutingType defaultQueueRoutingType = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/AddressSettings.java`
#### Snippet
```java
   private Boolean autoDeleteQueues = null;

   private Boolean autoDeleteCreatedQueues = null;

   private Long autoDeleteQueuesDelay = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/AddressSettings.java`
#### Snippet
```java
   private Boolean sendToDLAOnNoRoute = null;

   private Long slowConsumerThreshold = null;

   private SlowConsumerThresholdMeasurementUnit slowConsumerThresholdMeasurementUnit = DEFAULT_SLOW_CONSUMER_THRESHOLD_MEASUREMENT_UNIT;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/AddressSettings.java`
#### Snippet
```java
   private SlowConsumerThresholdMeasurementUnit slowConsumerThresholdMeasurementUnit = DEFAULT_SLOW_CONSUMER_THRESHOLD_MEASUREMENT_UNIT;

   private Long slowConsumerCheckPeriod = null;

   private SlowConsumerPolicy slowConsumerPolicy = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/AddressSettings.java`
#### Snippet
```java
   private SimpleString deadLetterAddress = null;

   private SimpleString expiryAddress = null;

   private Long expiryDelay = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/AddressSettings.java`
#### Snippet
```java
   private RoutingType defaultAddressRoutingType = null;

   private Integer defaultConsumerWindowSize = null;

   private Boolean autoCreateDeadLetterResources = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/AddressSettings.java`
#### Snippet
```java
   private Integer defaultGroupBuckets = null;

   private SimpleString defaultGroupFirstKey = null;

   private Long redistributionDelay = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/AddressSettings.java`
#### Snippet
```java
   private Boolean defaultGroupRebalance = null;

   private Boolean defaultGroupRebalancePauseDispatch = null;

   private Integer defaultGroupBuckets = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/AddressSettings.java`
#### Snippet
```java
   private Integer pageMaxCache = null;

   private Boolean dropMessagesWhenFull = null;

   private Integer maxDeliveryAttempts = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/AddressSettings.java`
#### Snippet
```java
   private Long expiryDelay = null;

   private Long minExpiryDelay = null;

   private Long maxExpiryDelay = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/AddressSettings.java`
#### Snippet
```java

   @Deprecated
   private Boolean autoDeleteJmsQueues = null;

   @Deprecated
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/AddressSettings.java`
#### Snippet
```java
   private SimpleString expiryQueuePrefix = null;

   private SimpleString expiryQueueSuffix = null;

   private Boolean enableMetrics = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/AddressSettings.java`
#### Snippet
```java
   private Long maxRedeliveryDelay = null;

   private SimpleString deadLetterAddress = null;

   private SimpleString expiryAddress = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/AddressSettings.java`
#### Snippet
```java
   private Boolean autoCreateQueues = null;

   private Boolean autoDeleteQueues = null;

   private Boolean autoDeleteCreatedQueues = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/AddressSettings.java`
#### Snippet
```java
   private Boolean autoCreateDeadLetterResources = null;

   private SimpleString deadLetterQueuePrefix = null;

   private SimpleString deadLetterQueueSuffix = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/AddressSettings.java`
#### Snippet
```java

   @Deprecated
   private Boolean autoCreateJmsTopics = null;

   @Deprecated
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/AddressSettings.java`
#### Snippet
```java
   private Integer maxReadPageMessages = null;

   private Long pageLimitBytes = null;

   private Long pageLimitMessages = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/AddressSettings.java`
#### Snippet
```java
   //from amq5
   //make it transient
   private transient Integer queuePrefetch = null;

   public AddressSettings(AddressSettings other) {
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/AddressSettings.java`
#### Snippet
```java
   private Long maxSizeBytes = null;

   private Integer maxReadPageBytes = null;

   private Integer maxReadPageMessages = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/AddressSettings.java`
#### Snippet
```java
   private Boolean autoCreateExpiryResources = null;

   private SimpleString expiryQueuePrefix = null;

   private SimpleString expiryQueueSuffix = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/AddressSettings.java`
#### Snippet
```java
   private Long autoDeleteQueuesMessageCount = null;

   private Long defaultRingSize = null;

   private Long retroactiveMessageCount = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/AddressSettings.java`
#### Snippet
```java
   private SimpleString deadLetterQueueSuffix = null;

   private Boolean autoCreateExpiryResources = null;

   private SimpleString expiryQueuePrefix = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/AddressSettings.java`
#### Snippet
```java
   private Integer managementBrowsePageSize = AddressSettings.MANAGEMENT_BROWSE_PAGE_SIZE;

   private Long maxSizeBytesRejectThreshold = null;

   private Integer defaultMaxConsumers = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/AddressSettings.java`
#### Snippet
```java
   private SimpleString expiryAddress = null;

   private Long expiryDelay = null;

   private Long minExpiryDelay = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/AddressSettings.java`
#### Snippet
```java
   public static final boolean DEFAULT_ENABLE_INGRESS_TIMESTAMP = false;

   private AddressFullMessagePolicy addressFullMessagePolicy = null;

   private Long maxSizeBytes = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/AddressSettings.java`
#### Snippet
```java
   private Integer managementMessageAttributeSizeLimit = null;

   private Boolean enableIngressTimestamp = null;

   //from amq5
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/AddressSettings.java`
#### Snippet
```java
   private Boolean defaultExclusiveQueue = null;

   private Boolean defaultGroupRebalance = null;

   private Boolean defaultGroupRebalancePauseDispatch = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/AddressSettings.java`
#### Snippet
```java
   private Boolean autoDeleteCreatedQueues = null;

   private Long autoDeleteQueuesDelay = null;

   private Boolean autoDeleteQueuesSkipUsageCheck = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/AddressSettings.java`
#### Snippet
```java
   private Long defaultDelayBeforeDispatch = null;

   private RoutingType defaultQueueRoutingType = null;

   private RoutingType defaultAddressRoutingType = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/AddressSettings.java`
#### Snippet
```java
   private Long autoDeleteAddressesDelay = null;

   private Boolean autoDeleteAddressesSkipUsageCheck = null;

   private DeletionPolicy configDeleteAddresses = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/AddressSettings.java`
#### Snippet
```java
   private Boolean defaultPurgeOnNoConsumers = null;

   private Integer defaultConsumersBeforeDispatch = null;

   private Long defaultDelayBeforeDispatch = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/AddressSettings.java`
#### Snippet
```java
   private Integer pageSizeBytes = null;

   private Integer pageMaxCache = null;

   private Boolean dropMessagesWhenFull = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/AddressSettings.java`
#### Snippet
```java
   private Boolean defaultGroupRebalancePauseDispatch = null;

   private Integer defaultGroupBuckets = null;

   private SimpleString defaultGroupFirstKey = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/AddressSettings.java`
#### Snippet
```java
   private Long pageLimitMessages = null;

   private PageFullMessagePolicy pageFullMessagePolicy = null;

   private Long maxSizeMessages = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/AddressSettings.java`
#### Snippet
```java
   private SimpleString expiryQueueSuffix = null;

   private Boolean enableMetrics = null;

   private Integer managementMessageAttributeSizeLimit = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/AddressSettings.java`
#### Snippet
```java
   private SimpleString defaultGroupFirstKey = null;

   private Long redistributionDelay = null;

   private Boolean sendToDLAOnNoRoute = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/AddressSettings.java`
#### Snippet
```java
   private Long redistributionDelay = null;

   private Boolean sendToDLAOnNoRoute = null;

   private Long slowConsumerThreshold = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/AddressSettings.java`
#### Snippet
```java
   private Boolean defaultNonDestructive = null;

   private Boolean defaultExclusiveQueue = null;

   private Boolean defaultGroupRebalance = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/AddressSettings.java`
#### Snippet
```java
   private Boolean autoDeleteAddressesSkipUsageCheck = null;

   private DeletionPolicy configDeleteAddresses = null;

   private DeletionPolicy configDeleteDiverts = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/AddressSettings.java`
#### Snippet
```java
   private DeletionPolicy configDeleteQueues = null;

   private Boolean autoCreateAddresses = null;

   private Boolean autoDeleteAddresses = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/AddressSettings.java`
#### Snippet
```java
   private Long minExpiryDelay = null;

   private Long maxExpiryDelay = null;

   private Boolean defaultLastValueQueue = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/AddressSettings.java`
#### Snippet
```java
   private PageFullMessagePolicy pageFullMessagePolicy = null;

   private Long maxSizeMessages = null;

   private Integer pageSizeBytes = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/AddressSettings.java`
#### Snippet
```java

   @Deprecated
   private Boolean autoCreateJmsQueues = null;

   @Deprecated
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/AddressSettings.java`
#### Snippet
```java
   private RoutingType defaultQueueRoutingType = null;

   private RoutingType defaultAddressRoutingType = null;

   private Integer defaultConsumerWindowSize = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/AddressSettings.java`
#### Snippet
```java
   private DeletionPolicy configDeleteAddresses = null;

   private DeletionPolicy configDeleteDiverts = null;

   private Integer managementBrowsePageSize = AddressSettings.MANAGEMENT_BROWSE_PAGE_SIZE;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/AddressSettings.java`
#### Snippet
```java
   private Boolean autoDeleteAddresses = null;

   private Long autoDeleteAddressesDelay = null;

   private Boolean autoDeleteAddressesSkipUsageCheck = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/AddressSettings.java`
#### Snippet
```java
   private Long slowConsumerCheckPeriod = null;

   private SlowConsumerPolicy slowConsumerPolicy = null;

   @Deprecated
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/AddressSettings.java`
#### Snippet
```java
   private Integer maxReadPageBytes = null;

   private Integer maxReadPageMessages = null;

   private Long pageLimitBytes = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/AddressSettings.java`
#### Snippet
```java
   private Long retroactiveMessageCount = null;

   private DeletionPolicy configDeleteQueues = null;

   private Boolean autoCreateAddresses = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/AddressSettings.java`
#### Snippet
```java
   private Long defaultRingSize = null;

   private Long retroactiveMessageCount = null;

   private DeletionPolicy configDeleteQueues = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/AddressSettings.java`
#### Snippet
```java

   @Deprecated
   private Boolean autoDeleteJmsTopics = null;

   private Boolean autoCreateQueues = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/AddressSettings.java`
#### Snippet
```java
   private Long maxSizeMessages = null;

   private Integer pageSizeBytes = null;

   private Integer pageMaxCache = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/AddressSettings.java`
#### Snippet
```java
   private Boolean autoCreateAddresses = null;

   private Boolean autoDeleteAddresses = null;

   private Long autoDeleteAddressesDelay = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/AddressSettings.java`
#### Snippet
```java
   private Boolean dropMessagesWhenFull = null;

   private Integer maxDeliveryAttempts = null;

   private Integer messageCounterHistoryDayLimit = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/AddressSettings.java`
#### Snippet
```java
   private Double redeliveryCollisionAvoidanceFactor = null;

   private Long maxRedeliveryDelay = null;

   private SimpleString deadLetterAddress = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/AddressSettings.java`
#### Snippet
```java
   private Long pageLimitBytes = null;

   private Long pageLimitMessages = null;

   private PageFullMessagePolicy pageFullMessagePolicy = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/AddressSettings.java`
#### Snippet
```java
   private AddressFullMessagePolicy addressFullMessagePolicy = null;

   private Long maxSizeBytes = null;

   private Integer maxReadPageBytes = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/AddressSettings.java`
#### Snippet
```java
   private Boolean autoDeleteQueuesSkipUsageCheck = null;

   private Long autoDeleteQueuesMessageCount = null;

   private Long defaultRingSize = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/AddressSettings.java`
#### Snippet
```java
   private Integer defaultConsumerWindowSize = null;

   private Boolean autoCreateDeadLetterResources = null;

   private SimpleString deadLetterQueuePrefix = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/AddressSettings.java`
#### Snippet
```java
   private Double redeliveryMultiplier = null;

   private Double redeliveryCollisionAvoidanceFactor = null;

   private Long maxRedeliveryDelay = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/AddressSettings.java`
#### Snippet
```java
   private Long redeliveryDelay = null;

   private Double redeliveryMultiplier = null;

   private Double redeliveryCollisionAvoidanceFactor = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/AddressSettings.java`
#### Snippet
```java
   private Boolean defaultLastValueQueue = null;

   private SimpleString defaultLastValueKey = null;

   private Boolean defaultNonDestructive = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/AddressSettings.java`
#### Snippet
```java
   private Long maxExpiryDelay = null;

   private Boolean defaultLastValueQueue = null;

   private SimpleString defaultLastValueKey = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/AddressSettings.java`
#### Snippet
```java
   private Integer maxDeliveryAttempts = null;

   private Integer messageCounterHistoryDayLimit = null;

   private Long redeliveryDelay = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/AddressSettings.java`
#### Snippet
```java
   private Integer defaultMaxConsumers = null;

   private Boolean defaultPurgeOnNoConsumers = null;

   private Integer defaultConsumersBeforeDispatch = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/AddressSettings.java`
#### Snippet
```java
   private SimpleString defaultLastValueKey = null;

   private Boolean defaultNonDestructive = null;

   private Boolean defaultExclusiveQueue = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/AddressSettings.java`
#### Snippet
```java
   private Boolean autoDeleteJmsTopics = null;

   private Boolean autoCreateQueues = null;

   private Boolean autoDeleteQueues = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/AddressSettings.java`
#### Snippet
```java
   private SimpleString deadLetterQueuePrefix = null;

   private SimpleString deadLetterQueueSuffix = null;

   private Boolean autoCreateExpiryResources = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/AddressSettings.java`
#### Snippet
```java
   private Long maxSizeBytesRejectThreshold = null;

   private Integer defaultMaxConsumers = null;

   private Boolean defaultPurgeOnNoConsumers = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/postoffice/impl/AddressImpl.java`
#### Snippet
```java
   private final boolean containsWildCard;

   private Set<Address> linkedAddresses = null;

   private final WildcardConfiguration wildcardConfiguration;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/OperationContextImpl.java`
#### Snippet
```java
   long stored = 0;
   long replicated = 0;
   long paged = 0;

   private int errorCode = -1;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/OperationContextImpl.java`
#### Snippet
```java


   volatile int executorsPendingField = 0;
   volatile long storeLineUpField = 0;
   volatile long replicationLineUpField = 0;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/OperationContextImpl.java`
#### Snippet
```java
   volatile int executorsPendingField = 0;
   volatile long storeLineUpField = 0;
   volatile long replicationLineUpField = 0;
   volatile long pageLineUpField = 0;

```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/OperationContextImpl.java`
#### Snippet
```java
   volatile long storeLineUpField = 0;
   volatile long replicationLineUpField = 0;
   volatile long pageLineUpField = 0;

   long stored = 0;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/OperationContextImpl.java`
#### Snippet
```java
   volatile long pageLineUpField = 0;

   long stored = 0;
   long replicated = 0;
   long paged = 0;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/OperationContextImpl.java`
#### Snippet
```java
   private int errorCode = -1;

   private String errorMessage = null;

   private final Executor executor;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/OperationContextImpl.java`
#### Snippet
```java

   volatile int executorsPendingField = 0;
   volatile long storeLineUpField = 0;
   volatile long replicationLineUpField = 0;
   volatile long pageLineUpField = 0;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/OperationContextImpl.java`
#### Snippet
```java

   long stored = 0;
   long replicated = 0;
   long paged = 0;

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/BatchingIDGenerator.java`
#### Snippet
```java
   private final StorageManager storageManager;

   private List<Long> cleanupRecords = null;

   public BatchingIDGenerator(final long start, final long checkpointSize, final StorageManager storageManager) {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/deployers/impl/FileConfigurationParser.java`
#### Snippet
```java
   private static final String ENABLE_INGRESS_TIMESTAMP = "enable-ingress-timestamp";

   private boolean validateAIO = false;

   private boolean printPageMaxSizeUsed = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/deployers/impl/FileConfigurationParser.java`
#### Snippet
```java
   private boolean validateAIO = false;

   private boolean printPageMaxSizeUsed = false;

   /**
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/transaction/impl/TransactionImpl.java`
#### Snippet
```java
   private static final int INITIAL_NUM_PROPERTIES = 11;

   private IntObjectHashMap properties = null;

   protected final StorageManager storageManager;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/transaction/impl/ResourceManagerImpl.java`
#### Snippet
```java
   private final int defaultTimeoutSeconds;

   private boolean started = false;

   private TxTimeoutHandler task;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/transaction/impl/ResourceManagerImpl.java`
#### Snippet
```java
   private class TxTimeoutHandler implements Runnable {

      private boolean closed = false;

      private Future<?> future;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/messagecounter/MessageCounter.java`
#### Snippet
```java
      static final int HOURS = 24;

      GregorianCalendar date = null;

      long[] counters = new long[DayCounter.HOURS];
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/replication/ReplicationEndpoint.java`
#### Snippet
```java
   private Executor executor;

   private List<Interceptor> outgoingInterceptors = null;

   private final ArrayDeque<Packet> pendingPackets;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/AbstractJournalStorageManager.java`
#### Snippet
```java
   private final boolean syncNonTransactional;

   protected boolean journalLoaded = false;

   protected final IOCriticalErrorListener ioCriticalErrorListener;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java
   private final class QueueBrowserIterator implements LinkedListIterator<MessageReference> {

      LinkedListIterator<PagedReference> pagingIterator = null;
      LinkedListIterator<MessageReference> messagesIterator = null;

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java
   private final PostOffice postOffice;

   private volatile boolean queueDestroyed = false;

   // once we delivered messages from paging, we need to call asyncDelivery upon acks
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java
   private final ReentrantLock depageLock = new ReentrantLock();

   private volatile boolean depagePending = false;

   private final StorageManager storageManager;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java
      Iterator<? extends MessageReference> lastIterator = null;

      MessageReference cachedNext = null;
      HashSet<PagePosition> previouslyBrowsed = new HashSet<>();

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java

      LinkedListIterator<PagedReference> pagingIterator = null;
      LinkedListIterator<MessageReference> messagesIterator = null;

      private LinkedListIterator<PagedReference> getPagingIterator() {
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java
   private volatile int consumersBeforeDispatch = 0;

   private volatile long delayBeforeDispatch = 0;

   private final boolean autoDelete;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java
   // if we flow control paging, ack more messages will open the space to deliver more messages
   // hence we will need this flag to determine if it was paging before.
   private volatile boolean pageDelivered = false;

   private final PagingStore pagingStore;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java
      }

      Iterator<? extends MessageReference> lastIterator = null;

      MessageReference cachedNext = null;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java
   private final QueueFactory factory;

   public volatile int dispatching = 0;

   public volatile long dispatchStartTime = -1;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java
   private volatile boolean directDeliver = true;

   private volatile boolean supportsDirectDeliver = false;

   private AddressSettingsRepositoryListener addressSettingsRepositoryListener;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java
   private boolean mirrorController;

   private volatile boolean hasUnMatchedPending = false;

   // Messages will first enter intermediateMessageReferences
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java
      }

      LinkedListIterator<MessageReference> iter = null;

      @Override
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java
   private boolean internalQueue;

   private volatile long lastDirectDeliveryCheck = 0;

   private volatile boolean directDeliver = true;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java
   private final PageIterator pageIterator;

   private volatile boolean printErrorExpiring = false;

   private boolean mirrorController;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java
   public volatile long dispatchStartTime = -1;

   private volatile int consumersBeforeDispatch = 0;

   private volatile long delayBeforeDispatch = 0;
```

## RuleId[id=RedundantImplements]
### RedundantImplements
Redundant interface declaration `ActiveMQConnectionForContext`
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/ActiveMQRASessionFactoryImpl.java`
#### Snippet
```java
 * Implements the JMS Connection API and produces {@link ActiveMQRASession} objects.
 */
public final class ActiveMQRASessionFactoryImpl extends ActiveMQConnectionForContextImpl implements ActiveMQRASessionFactory, ActiveMQConnectionForContext, Referenceable {

   private static final Logger logger = LoggerFactory.getLogger(MethodHandles.lookup().lookupClass());
```

### RedundantImplements
Redundant interface declaration `MessageProducer`
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQMessageProducer.java`
#### Snippet
```java
 * ActiveMQ Artemis implementation of a JMS MessageProducer.
 */
public class ActiveMQMessageProducer implements MessageProducer, QueueSender, TopicPublisher {

   private static final Logger logger = LoggerFactory.getLogger(MethodHandles.lookup().lookupClass());
```

### RedundantImplements
Redundant interface declaration `JsonValue`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/JsonUtil.java`
#### Snippet
```java


   private static class NullableJsonString implements JsonValue, JsonString {

      private final String value;
```

### RedundantImplements
Redundant interface declaration `TransactionOperation`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/cursor/impl/PageSubscriptionCounterImpl.java`
#### Snippet
```java
   }

   private static class CounterOperations extends TransactionOperationAbstract implements TransactionOperation {

      LinkedList<ItemOper> operations = new LinkedList<>();
```

### RedundantImplements
Redundant interface declaration `MessageReference`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/MessageReferenceImpl.java`
#### Snippet
```java
 * Implementation of a MessageReference
 */
public class MessageReferenceImpl extends AbstractProtocolReference implements MessageReference, Runnable {

   private static final MessageReferenceComparatorByID idComparator = new MessageReferenceComparatorByID();
```

### RedundantImplements
Redundant interface declaration `ActiveMQServerBasePlugin`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/plugin/ActiveMQServerPlugin.java`
#### Snippet
```java
 */
public interface ActiveMQServerPlugin extends
        ActiveMQServerBasePlugin,
        ActiveMQServerConnectionPlugin,
        ActiveMQServerSessionPlugin,
```

### RedundantImplements
Redundant interface declaration `TransactionOperation`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/codec/FinishPageMessageOperation.java`
#### Snippet
```java
 */
// TODO: merge this class with the one on the PagingStoreImpl
public class FinishPageMessageOperation extends TransactionOperationAbstract implements TransactionOperation {

   @Override
```

## RuleId[id=InstanceofCatchParameter]
### InstanceofCatchParameter
'instanceof' on 'catch' parameter `t`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/ClassloadingUtil.java`
#### Snippet
```java
         return clazz.newInstance();
      } catch (Throwable t) {
         if (t instanceof InstantiationException) {
            System.out.println(INSTANTIATION_EXCEPTION_MESSAGE);
         }
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `t`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/ClassloadingUtil.java`
#### Snippet
```java
         return clazz.getConstructor(parametersType).newInstance(objs);
      } catch (Throwable t) {
         if (t instanceof InstantiationException) {
            System.out.println(INSTANTIATION_EXCEPTION_MESSAGE);
         }
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `t`
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/io/mapped/TimedSequentialFile.java`
#### Snippet
```java
            } catch (Throwable t) {
               final int code;
               if (t instanceof IOException) {
                  code = ActiveMQExceptionType.IO_ERROR.getCode();
                  factory.onIOError(new ActiveMQIOErrorException(t.getMessage(), t), t.getMessage(), TimedSequentialFile.this.sequentialFile);
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `t`
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/ActiveMQRASessionFactoryImpl.java`
#### Snippet
```java
               } catch (Throwable ignored) {
               }
               if (t instanceof Exception) {
                  throw (Exception) t;
               } else {
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `t`
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/inflow/ActiveMQActivation.java`
#### Snippet
```java
            logger.trace("Ignored error closing connection", e);
         }
         if (t instanceof Exception) {
            throw (Exception) t;
         }
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `t`
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/inflow/ActiveMQActivation.java`
#### Snippet
```java
            } catch (Throwable t) {
               if (failure instanceof ActiveMQException && ((ActiveMQException) failure).getType() == ActiveMQExceptionType.QUEUE_DOES_NOT_EXIST) {
                  if (lastException == null || !(t instanceof ActiveMQNonExistentQueueException)) {
                     lastException = t;
                     ActiveMQRALogger.LOGGER.awaitingTopicQueueCreation(getActivationSpec().getDestination());
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `t`
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/inflow/ActiveMQActivation.java`
#### Snippet
```java
                  }
               } else if (failure instanceof ActiveMQException && ((ActiveMQException) failure).getType() == ActiveMQExceptionType.NOT_CONNECTED) {
                  if (lastException == null || !(t instanceof ActiveMQNotConnectedException)) {
                     lastException = t;
                     ActiveMQRALogger.LOGGER.awaitingJMSServerCreation();
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/check/CheckAbstract.java`
#### Snippet
```java
                     String reason;

                     if (e instanceof CheckException) {
                        failedTasks++;
                        reason = "failure: " + e.getMessage();
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenWireConnection.java`
#### Snippet
```java
            addDestination(dest);
         } catch (Exception e) {
            if (e instanceof ActiveMQSecurityException) {
               resp = new ExceptionResponse(new JMSSecurityException(e.getMessage()));
            } else {
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenWireConnection.java`
#### Snippet
```java
            if (tx != null) {
               tx.markAsRollbackOnly(new ActiveMQException(e.getMessage()));
            } else if (e instanceof ActiveMQNonExistentQueueException && producerInfo.getDestination() == null) {
               //Send exception for non transacted anonymous producers using an incorrect destination
               sendException(e);
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `t`
in `artemis-unit-test-support/src/main/java/org/apache/activemq/artemis/utils/RetryRule.java`
#### Snippet
```java
            } catch (Throwable t) {

               if (t instanceof AssumptionViolatedException) {
                  throw t;
               }
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQConnectionFactory.java`
#### Snippet
```java
         uri = parser.createSchema(scheme, this);
      } catch (Exception e) {
         if (e instanceof IOException) {
            throw (IOException) e;
         }
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQConnectionFactory.java`
#### Snippet
```java
         out.writeUTF(uri.toASCIIString());
      } catch (Exception e) {
         if (e instanceof IOException) {
            throw (IOException) e;
         }
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/ProtonServerSenderContext.java`
#### Snippet
```java

      } catch (Exception e) {
         if (e instanceof ConversionException && brokerConsumer.getBinding() instanceof LocalQueueBinding) {
            ActiveMQAMQPProtocolLogger.LOGGER.messageConversionFailed(e);
            LocalQueueBinding queueBinding = (LocalQueueBinding) brokerConsumer.getBinding();
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `t`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/ActiveMQClientProtocolManager.java`
#### Snippet
```java
            }

            if (t instanceof ActiveMQException) {
               throw (ActiveMQException) t;
            } else {
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/impl/PagingStoreImpl.java`
#### Snippet
```java
      } catch (Throwable e) {
         logger.warn(e.getMessage(), e);
         if (e instanceof AssertionError) {
            // this will give a chance to callers log an AssertionError if assertion flag is enabled
            throw (AssertionError)e;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ReplicationBackupActivation.java`
#### Snippet
```java
         startAsLive(liveLock);
      } catch (Exception e) {
         if ((e instanceof InterruptedException || e instanceof IllegalStateException) && !activeMQServer.isStarted()) {
            // do not log these errors if the server is being stopped.
            return;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ReplicationBackupActivation.java`
#### Snippet
```java
         startAsLive(liveLock);
      } catch (Exception e) {
         if ((e instanceof InterruptedException || e instanceof IllegalStateException) && !activeMQServer.isStarted()) {
            // do not log these errors if the server is being stopped.
            return;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `notConnected`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/SharedNothingLiveActivation.java`
#### Snippet
```java
            listener.latch.await(5, TimeUnit.SECONDS);
         } catch (Exception notConnected) {
            if (!(notConnected instanceof ActiveMQException) || ActiveMQExceptionType.INTERNAL_ERROR.equals(((ActiveMQException) notConnected).getType())) {
               // report all exceptions that aren't ActiveMQException and all INTERNAL_ERRORs
               ActiveMQServerLogger.LOGGER.failedConnectingToCluster(notConnected);
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/SharedNothingBackupActivation.java`
#### Snippet
```java
            logger.trace("{}, serverStarted={}", e.getMessage(), activeMQServer.isStarted(), e);
         }
         if ((e instanceof InterruptedException || e instanceof IllegalStateException) && !activeMQServer.isStarted())
            // do not log these errors if the server is being stopped.
            return;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/SharedNothingBackupActivation.java`
#### Snippet
```java
            logger.trace("{}, serverStarted={}", e.getMessage(), activeMQServer.isStarted(), e);
         }
         if ((e instanceof InterruptedException || e instanceof IllegalStateException) && !activeMQServer.isStarted())
            // do not log these errors if the server is being stopped.
            return;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ActiveMQServerImpl.java`
#### Snippet
```java
               pluginRun.run(plugin);
            } catch (Throwable e) {
               if (e instanceof ActiveMQException) {
                  logger.debug("plugin {} is throwing ActiveMQException", plugin);
                  throw (ActiveMQException) e;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ActiveMQServerImpl.java`
#### Snippet
```java
            }
         } catch (Throwable e) {
            if (e instanceof ActiveMQException) {
               logger.debug("plugin {} is throwing ActiveMQException", plugin);
               throw (ActiveMQException) e;
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/impl/ManagementServiceImpl.java`
#### Snippet
```java
            reply.putBooleanProperty(ManagementHelper.HDR_OPERATION_SUCCEEDED, false);
            String exceptionMessage;
            if (e instanceof InvocationTargetException) {
               exceptionMessage = ((InvocationTargetException) e).getTargetException().getMessage();
            } else {
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/impl/ManagementServiceImpl.java`
#### Snippet
```java
               reply.putBooleanProperty(ManagementHelper.HDR_OPERATION_SUCCEEDED, false);
               String exceptionMessage;
               if (e instanceof InvocationTargetException) {
                  exceptionMessage = ((InvocationTargetException) e).getTargetException().getMessage();
               } else {
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/postoffice/impl/PostOfficeImpl.java`
#### Snippet
```java
            logger.debug(e.getMessage(), e);
         } catch (Exception e) {
            if (e instanceof ActiveMQAddressDoesNotExistException && getAddressInfo(address) == null) {
               // the address and queue reaper is asynchronous so it may happen
               // that the address is removed before the reaper removes it
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `t`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/replication/ReplicationManager.java`
#### Snippet
```java
         }
      } catch (Throwable t) {
         assert !(t instanceof AssertionError) : t.getMessage();
         if (!connection.getTransportConnection().isOpen()) {
            // that's an handled state: right after this cleanup is expected to be stopped/closed
```

## RuleId[id=ExceptionNameDoesntEndWithException]
### ExceptionNameDoesntEndWithException
Exception class name `ActiveMQNativeIOError` does not end with 'Exception'
in `artemis-commons/src/main/java/org/apache/activemq/artemis/api/core/ActiveMQNativeIOError.java`
#### Snippet
```java
 */
// XXX
public final class ActiveMQNativeIOError extends ActiveMQException {

   private static final long serialVersionUID = 2355120980683293085L;
```

### ExceptionNameDoesntEndWithException
Exception class name `ActiveMQQueueMaxConsumerLimitReached` does not end with 'Exception'
in `artemis-commons/src/main/java/org/apache/activemq/artemis/api/core/ActiveMQQueueMaxConsumerLimitReached.java`
#### Snippet
```java
 * An operation failed because a queue exists on the server.
 */
public final class ActiveMQQueueMaxConsumerLimitReached extends ActiveMQException {

   public ActiveMQQueueMaxConsumerLimitReached() {
```

### ExceptionNameDoesntEndWithException
Exception class name `ActiveMQInvalidQueueConfiguration` does not end with 'Exception'
in `artemis-commons/src/main/java/org/apache/activemq/artemis/api/core/ActiveMQInvalidQueueConfiguration.java`
#### Snippet
```java
 * An operation failed because a queue exists on the server.
 */
public final class ActiveMQInvalidQueueConfiguration extends ActiveMQException {

   public ActiveMQInvalidQueueConfiguration() {
```

### ExceptionNameDoesntEndWithException
Exception class name `ActiveMQUnexpectedRoutingTypeForAddress` does not end with 'Exception'
in `artemis-commons/src/main/java/org/apache/activemq/artemis/api/core/ActiveMQUnexpectedRoutingTypeForAddress.java`
#### Snippet
```java
 * An operation failed because a queue exists on the server.
 */
public final class ActiveMQUnexpectedRoutingTypeForAddress extends ActiveMQException {

   public ActiveMQUnexpectedRoutingTypeForAddress() {
```

### ExceptionNameDoesntEndWithException
Exception class name `InvalidOptionsError` does not end with 'Exception'
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/InvalidOptionsError.java`
#### Snippet
```java
package org.apache.activemq.artemis.cli.commands;

public class InvalidOptionsError extends Exception {

   public InvalidOptionsError(String msg) {
```

### ExceptionNameDoesntEndWithException
Exception class name `StringPrepRTLError` does not end with 'Exception'
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/security/scram/StringPrep.java`
#### Snippet
```java
   /** RTL verification has failed, according to rfc3454 section 6. */
   @SuppressWarnings({"unused", "JavaDoc"})
   public static class StringPrepRTLError extends StringPrepError {
      /**
       *
```

### ExceptionNameDoesntEndWithException
Exception class name `StringPrepRTLErrorBothRALandL` does not end with 'Exception'
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/security/scram/StringPrep.java`
#### Snippet
```java
   }

   public static class StringPrepRTLErrorBothRALandL extends StringPrepRTLError {

      /**
```

### ExceptionNameDoesntEndWithException
Exception class name `StringPrepRTLErrorRALWithoutPrefix` does not end with 'Exception'
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/security/scram/StringPrep.java`
#### Snippet
```java
   }

   public static class StringPrepRTLErrorRALWithoutPrefix extends StringPrepRTLError {

      /**
```

### ExceptionNameDoesntEndWithException
Exception class name `StringPrepUnassignedCodepoint` does not end with 'Exception'
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/security/scram/StringPrep.java`
#### Snippet
```java
   /** A prohibited unassigned codepoint was detected. */
   @SuppressWarnings("JavaDoc")
   public static class StringPrepUnassignedCodepoint extends StringPrepProhibitedCharacter {
      /**
       *
```

### ExceptionNameDoesntEndWithException
Exception class name `StringPrepError` does not end with 'Exception'
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/security/scram/StringPrep.java`
#### Snippet
```java

   /** A prohibited string has been passed to StringPrep. */
   public abstract static class StringPrepError extends Exception {
      /**
       *
```

### ExceptionNameDoesntEndWithException
Exception class name `StringPrepRTLErrorRALWithoutSuffix` does not end with 'Exception'
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/security/scram/StringPrep.java`
#### Snippet
```java
   }

   public static class StringPrepRTLErrorRALWithoutSuffix extends StringPrepRTLError {

      /**
```

### ExceptionNameDoesntEndWithException
Exception class name `StringPrepProhibitedCharacter` does not end with 'Exception'
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/security/scram/StringPrep.java`
#### Snippet
```java
   /** A prohibited character was detected. */
   @SuppressWarnings({"WeakerAccess", "JavaDoc"})
   public static class StringPrepProhibitedCharacter extends StringPrepError {
      /**
       *
```

### ExceptionNameDoesntEndWithException
Exception class name `NonExistentPage` does not end with 'Exception'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/cursor/NonExistentPage.java`
#### Snippet
```java
 * But if the whole page is cleaned an exception could happen, which is ok on that path, and we need to identify it.
 */
public class NonExistentPage extends RuntimeException {

   public NonExistentPage() {
```

## RuleId[id=ArrayEquality]
### ArrayEquality
Array objects are compared using `==`, not 'Arrays.equals()'
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/ByteUtil.java`
#### Snippet
```java
                                final int rightOffset,
                                final int rightLength) {
      if (left == right)
         return true;
      if (left == null || right == null)
```

### ArrayEquality
Array objects are compared using `==`, not 'Arrays.equals()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/postoffice/impl/CopyOnWriteBindings.java`
#### Snippet
```java
      do {
         oldBindings = bindings.get();
         if (oldBindings == TOMBSTONE_BINDINGS) {
            return false;
         }
```

### ArrayEquality
Array objects are compared using `==`, not 'Arrays.equals()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/postoffice/impl/CopyOnWriteBindings.java`
#### Snippet
```java
      for (BindingsAndPosition bindingsAndPosition : map.values()) {
         final Binding[] bindings = bindingsAndPosition.get();
         if (bindings == TOMBSTONE_BINDINGS) {
            continue;
         }
```

### ArrayEquality
Array objects are compared using `==`, not 'Arrays.equals()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/postoffice/impl/CopyOnWriteBindings.java`
#### Snippet
```java
      do {
         bindings = map.get(routingName);
         if (bindings == null || bindings.get() == TOMBSTONE_BINDINGS) {
            final BindingsAndPosition newBindings = new BindingsAndPosition(new Binding[]{binding});
            bindings = map.compute(routingName, (ignored, bindingsAndPosition) -> {
```

### ArrayEquality
Array objects are compared using `==`, not 'Arrays.equals()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/postoffice/impl/CopyOnWriteBindings.java`
#### Snippet
```java
            final BindingsAndPosition newBindings = new BindingsAndPosition(new Binding[]{binding});
            bindings = map.compute(routingName, (ignored, bindingsAndPosition) -> {
               if (bindingsAndPosition == null || bindingsAndPosition.get() == TOMBSTONE_BINDINGS) {
                  return newBindings;
               }
```

### ArrayEquality
Array objects are compared using `==`, not 'Arrays.equals()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/postoffice/impl/CopyOnWriteBindings.java`
#### Snippet
```java
      map.forEach((routingName, bindings) -> {
         final Binding[] bindingArray = bindings.get();
         if (bindingArray == TOMBSTONE_BINDINGS) {
            return;
         }
```

### ArrayEquality
Array objects are compared using `==`, not 'Arrays.equals()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/postoffice/impl/CopyOnWriteBindings.java`
#### Snippet
```java
         final BindingsAndPosition value = entry.getValue();
         final Binding[] bindings = value.get();
         if (bindings == TOMBSTONE_BINDINGS) {
            continue;
         }
```

### ArrayEquality
Array objects are compared using `==`, not 'Arrays.equals()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/postoffice/impl/CopyOnWriteBindings.java`
#### Snippet
```java
      }
      final Binding[] bindingsSnapshot = bindings.get();
      if (bindingsSnapshot == TOMBSTONE_BINDINGS) {
         return null;
      }
```

### ArrayEquality
Array objects are compared using `==`, not 'Arrays.equals()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/postoffice/impl/CopyOnWriteBindings.java`
#### Snippet
```java
      }
      final Binding[] newBindings = removeBindingIfPresent(bindings, binding);
      if (newBindings == TOMBSTONE_BINDINGS) {
         // GC attempt
         map.computeIfPresent(routingName, (bindingsRoutingName, existingBindings) -> {
```

### ArrayEquality
Array objects are compared using `==`, not 'Arrays.equals()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/postoffice/impl/CopyOnWriteBindings.java`
#### Snippet
```java
         // GC attempt
         map.computeIfPresent(routingName, (bindingsRoutingName, existingBindings) -> {
            if (existingBindings.get() == TOMBSTONE_BINDINGS) {
               return null;
            }
```

## RuleId[id=StringBufferReplaceableByStringBuilder]
### StringBufferReplaceableByStringBuilder
`StringBuffer buffer` may be declared as 'StringBuilder'
in `artemis-maven-plugin/src/main/java/org/apache/activemq/artemis/maven/ArtemisDependencyScanPlugin.java`
#### Snippet
```java

      try {
         StringBuffer buffer = new StringBuffer();
         Set<File> filesSet = resolveDependencies(libListWithDeps, libList);

```

### StringBufferReplaceableByStringBuilder
`StringBuffer buffer` may be declared as 'StringBuilder'
in `artemis-maven-plugin/src/main/java/org/apache/activemq/artemis/maven/ArtemisCreatePlugin.java`
#### Snippet
```java

   private String getCommandline(ArrayList<String> listCommands) {
      StringBuffer buffer = new StringBuffer();
      buffer.append(home.getAbsolutePath() + "/bin/artemis ");
      for (String string : listCommands) {
```

### StringBufferReplaceableByStringBuilder
`StringBuffer b` may be declared as 'StringBuilder'
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/ConstantExpression.java`
#### Snippet
```java
    */
   public static String encodeString(String s) {
      StringBuffer b = new StringBuffer();
      b.append('\'');
      for (int i = 0; i < s.length(); i++) {
```

### StringBufferReplaceableByStringBuilder
`StringBuffer answer` may be declared as 'StringBuilder'
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/UnaryExpression.java`
#### Snippet
```java
         @Override
         public String toString() {
            StringBuffer answer = new StringBuffer();
            answer.append(right);
            answer.append(" ");
```

### StringBufferReplaceableByStringBuilder
`StringBuffer callList` may be declared as 'StringBuilder'
in `artemis-log-annotation-processor/src/main/java/org/apache/activemq/artemis/logs/annotation/processor/LogAnnotationProcessor.java`
#### Snippet
```java

      // the one that will be used on the call
      StringBuffer callList = new StringBuffer();
      while (parameters.hasNext()) {
         hasParameters = true;
```

### StringBufferReplaceableByStringBuilder
`StringBuffer callList` may be declared as 'StringBuilder'
in `artemis-log-annotation-processor/src/main/java/org/apache/activemq/artemis/logs/annotation/processor/LogAnnotationProcessor.java`
#### Snippet
```java
      writerOutput.println(") {");

      StringBuffer callList = null;
      if (hasParameters) {
         // the one that will be used on the logger call
```

### StringBufferReplaceableByStringBuilder
`StringBuffer b` may be declared as 'StringBuilder'
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/UUID.java`
#### Snippet
```java

      if (mDesc == null) {
         StringBuffer b = new StringBuffer(36);

         for (int i = 0; i < 16; ++i) {
```

### StringBufferReplaceableByStringBuilder
`StringBuffer buffer` may be declared as 'StringBuilder'
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/SpawnedVMSupport.java`
#### Snippet
```java
      try {
         if (!aliveProcess.isEmpty()) {
            StringBuffer buffer = new StringBuffer();
            for (Process alive : aliveProcess) {
               alive.destroyForcibly();
```

### StringBufferReplaceableByStringBuilder
`StringBuffer buff` may be declared as 'StringBuilder'
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/SelectorTranslator.java`
#### Snippet
```java

      if (!positions.isEmpty()) {
         StringBuffer buff = new StringBuffer();

         int startPos = 0;
```

### StringBufferReplaceableByStringBuilder
`StringBuffer buffer` may be declared as 'StringBuilder'
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/ByteUtil.java`
#### Snippet
```java

   public static String formatGroup(String str, int groupSize, int lineBreak) {
      StringBuffer buffer = new StringBuffer();

      int line = 1;
```

### StringBufferReplaceableByStringBuilder
`StringBuffer buffer` may be declared as 'StringBuilder'
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/uri/URISchema.java`
#### Snippet
```java

   protected String printQuery(Map<String, String> query) {
      StringBuffer buffer = new StringBuffer();
      for (Map.Entry<String, String> entry : query.entrySet()) {
         buffer.append(entry.getKey() + "=" + entry.getValue());
```

### StringBufferReplaceableByStringBuilder
`StringBuffer b` may be declared as 'StringBuilder'
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/inflow/ActiveMQActivationValidationUtils.java`
#### Snippet
```java
         throws InvalidPropertyException {
      if (propsNotSet.size() > 0) {
         StringBuffer b = new StringBuffer();
         b.append("Invalid settings:");
         for (String errorMessage : errorMessages) {
```

### StringBufferReplaceableByStringBuilder
`StringBuffer buffer` may be declared as 'StringBuilder'
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/inflow/ActiveMQActivationSpec.java`
#### Snippet
```java
   @Override
   public String toString() {
      StringBuffer buffer = new StringBuffer();
      buffer.append(ActiveMQActivationSpec.class.getName()).append('(');
      buffer.append("ra=").append(ra);
```

### StringBufferReplaceableByStringBuilder
`StringBuffer buffer` may be declared as 'StringBuilder'
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/address/AddressAbstract.java`
#### Snippet
```java

   public String getRoutingTypes(boolean useDefault) {
      StringBuffer buffer = new StringBuffer();

      if (isAnycast()) {
```

### StringBufferReplaceableByStringBuilder
`StringBuffer buffer` may be declared as 'StringBuilder'
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Create.java`
#### Snippet
```java
   private void printStar(String message) {
      int size = Math.min(message.length(), 80);
      StringBuffer buffer = new StringBuffer(size);
      for (int i = 0; i < size; i++) {
         buffer.append("*");
```

### StringBufferReplaceableByStringBuilder
`StringBuffer buffer` may be declared as 'StringBuilder'
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/ProducerThread.java`
#### Snippet
```java
                  messageText = read;
               } else {
                  StringBuffer buffer = new StringBuffer(read);
                  while (buffer.length() < textMessageSize) {
                     buffer.append(read);
```

### StringBufferReplaceableByStringBuilder
`StringBuffer output` may be declared as 'StringBuilder'
in `artemis-cli/src/main/java/org/apache/activemq/artemis/util/JVMArgumentParser.java`
#### Snippet
```java
      String spacesBeginning = newLine.substring(0, newLine.indexOf(prefix));
      newLine = newLine.trim();
      StringBuffer output = new StringBuffer();
      String line = newLine.substring(prefix.length(), newLine.length() - endOfLine.length());
      String[] split = line.split(" ");
```

### StringBufferReplaceableByStringBuilder
`StringBuffer sb` may be declared as 'StringBuilder'
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/bridge/impl/JMSBridgeImpl.java`
#### Snippet
```java
         val = msg.getJMSMessageID();
      } else {
         StringBuffer sb = new StringBuffer(val);

         sb.append(",").append(msg.getJMSMessageID());
```

### StringBufferReplaceableByStringBuilder
`StringBuffer connectedBrokers` may be declared as 'StringBuilder'
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenWireProtocolManager.java`
#### Snippet
```java
   private String generateMembersURI(boolean flip) {
      String uri;
      StringBuffer connectedBrokers = new StringBuffer();
      String separator = "";

```

### StringBufferReplaceableByStringBuilder
`StringBuffer sb` may be declared as 'StringBuilder'
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQMessage.java`
#### Snippet
```java
   @Override
   public String toString() {
      StringBuffer sb = new StringBuffer("ActiveMQMessage[");
      if (message != null) {
         sb.append(getJMSMessageID());
```

### StringBufferReplaceableByStringBuilder
`StringBuffer buffer` may be declared as 'StringBuilder'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ServerLocatorImpl.java`
#### Snippet
```java

   private String fromInterceptors(final List<Interceptor> interceptors) {
      StringBuffer buffer = new StringBuffer();
      boolean first = true;
      for (Interceptor value : interceptors) {
```

### StringBufferReplaceableByStringBuilder
`StringBuffer buff` may be declared as 'StringBuilder'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionDeleteQueueMessage.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", queueName=" + queueName);
      return buff.toString();
```

### StringBufferReplaceableByStringBuilder
`StringBuffer buff` may be declared as 'StringBuilder'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionSendContinuationMessage_V2.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", continues=" + continues);
      buff.append(", message=" + message);
```

### StringBufferReplaceableByStringBuilder
`StringBuffer buff` may be declared as 'StringBuilder'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionXAForgetMessage.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append("]");
      return buff.toString();
```

### StringBufferReplaceableByStringBuilder
`StringBuffer buff` may be declared as 'StringBuilder'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionXAResponseMessage_V2.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", error=" + error);
      buff.append(", message=" + message);
```

### StringBufferReplaceableByStringBuilder
`StringBuffer buff` may be declared as 'StringBuilder'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionXASetTimeoutResponseMessage.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", ok=" + ok);
      return buff.toString();
```

### StringBufferReplaceableByStringBuilder
`StringBuffer buf` may be declared as 'StringBuilder'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionForceConsumerDelivery.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buf = new StringBuffer(super.getPacketString());
      buf.append(", consumerID=" + consumerID);
      buf.append(", sequence=" + sequence);
```

### StringBufferReplaceableByStringBuilder
`StringBuffer buff` may be declared as 'StringBuilder'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/CheckFailoverReplyMessage.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", okToFailover=" + okToFailover);
      return buff.toString();
```

### StringBufferReplaceableByStringBuilder
`StringBuffer buff` may be declared as 'StringBuilder'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionReceiveLargeMessage.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", consumerID=" + consumerID);
      buff.append(", deliveryCount=" + deliveryCount);
```

### StringBufferReplaceableByStringBuilder
`StringBuffer buff` may be declared as 'StringBuilder'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/CreateSessionResponseMessage.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", serverVersion=" + serverVersion);
      return buff.toString();
```

### StringBufferReplaceableByStringBuilder
`StringBuffer buff` may be declared as 'StringBuilder'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/DisconnectMessage_V2.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", nodeID=" + nodeID);
      buff.append(", scaleDownNodeID=" + scaleDownNodeID);
```

### StringBufferReplaceableByStringBuilder
`StringBuffer buff` may be declared as 'StringBuilder'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionSendLargeMessage.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", largeMessage=" + largeMessage);
      return buff.toString();
```

### StringBufferReplaceableByStringBuilder
`StringBuffer buff` may be declared as 'StringBuilder'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionQueueQueryResponseMessage_V3.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", autoCreated=" + autoCreated);
      buff.append(", purgeOnNoConsumers=" + purgeOnNoConsumers);
```

### StringBufferReplaceableByStringBuilder
`StringBuffer buff` may be declared as 'StringBuilder'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/RollbackMessage.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", considerLastMessageAsDelivered=" + considerLastMessageAsDelivered);
      return buff.toString();
```

### StringBufferReplaceableByStringBuilder
`StringBuffer buff` may be declared as 'StringBuilder'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionXASetTimeoutMessage.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", timeoutSeconds=" + timeoutSeconds);
      return buff.toString();
```

### StringBufferReplaceableByStringBuilder
`StringBuffer buff` may be declared as 'StringBuilder'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionXAPrepareMessage.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", xid=" + xid);
      return buff.toString();
```

### StringBufferReplaceableByStringBuilder
`StringBuffer buff` may be declared as 'StringBuilder'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionExpireMessage.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", consumerID=" + consumerID);
      buff.append(", messageID=" + messageID);
```

### StringBufferReplaceableByStringBuilder
`StringBuffer buff` may be declared as 'StringBuilder'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SubscribeClusterTopologyUpdatesMessage.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", clusterConnection=" + clusterConnection);
      return buff.toString();
```

### StringBufferReplaceableByStringBuilder
`StringBuffer buff` may be declared as 'StringBuilder'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionQueueQueryResponseMessage.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", address=" + address);
      buff.append(", name=" + name);
```

### StringBufferReplaceableByStringBuilder
`StringBuffer buffer` may be declared as 'StringBuilder'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/ActiveMQSessionContext.java`
#### Snippet
```java

      if (logger.isTraceEnabled()) {
         StringBuffer buffer = new StringBuffer();
         for (int i = 0; i < xidArray.length; i++) {
            buffer.append(xidArray[i].toString());
```

### StringBufferReplaceableByStringBuilder
`StringBuffer buff` may be declared as 'StringBuilder'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/CreateSessionMessage.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", autoCommitAcks=" + autoCommitAcks);
      buff.append(", autoCommitSends=" + autoCommitSends);
```

### StringBufferReplaceableByStringBuilder
`StringBuffer buff` may be declared as 'StringBuilder'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionProducerCreditsMessage.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", address=" + address);
      buff.append(", credits=" + credits);
```

### StringBufferReplaceableByStringBuilder
`StringBuffer buff` may be declared as 'StringBuilder'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/ReattachSessionMessage.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", lastConfirmedCommandID=" + lastConfirmedCommandID);
      buff.append(", name=" + name);
```

### StringBufferReplaceableByStringBuilder
`StringBuffer buff` may be declared as 'StringBuilder'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionQueueQueryResponseMessage_V2.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", autoCreationEnabled=" + autoCreateQueues);
      return buff.toString();
```

### StringBufferReplaceableByStringBuilder
`StringBuffer buff` may be declared as 'StringBuilder'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionXAStartMessage.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", xid=" + xid);
      return buff.toString();
```

### StringBufferReplaceableByStringBuilder
`StringBuffer buff` may be declared as 'StringBuilder'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/DisconnectConsumerMessage.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", consumerId=" + consumerId);
      return buff.toString();
```

### StringBufferReplaceableByStringBuilder
`StringBuffer buff` may be declared as 'StringBuilder'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionProducerCreditsFailMessage.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", address=" + address);
      buff.append(", credits=" + credits);
```

### StringBufferReplaceableByStringBuilder
`StringBuffer buff` may be declared as 'StringBuilder'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/CheckFailoverMessage.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", nodeID=" + nodeID);
      return buff.toString();
```

### StringBufferReplaceableByStringBuilder
`StringBuffer buff` may be declared as 'StringBuilder'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionSendMessage_V2.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", correlationID=" + correlationID);
      buff.append(", requiresResponse=" + super.isRequiresResponse());
```

### StringBufferReplaceableByStringBuilder
`StringBuffer buff` may be declared as 'StringBuilder'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/ReattachSessionResponseMessage.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", lastConfirmedCommandID=" + lastConfirmedCommandID);
      buff.append(", reattached=" + reattached);
```

### StringBufferReplaceableByStringBuilder
`StringBuffer buff` may be declared as 'StringBuilder'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionXAResponseMessage.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", error=" + error);
      buff.append(", message=" + message);
```

### StringBufferReplaceableByStringBuilder
`StringBuffer buff` may be declared as 'StringBuilder'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionReceiveMessage.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", consumerID=" + consumerID);
      buff.append(", deliveryCount=" + deliveryCount);
```

### StringBufferReplaceableByStringBuilder
`StringBuffer buff` may be declared as 'StringBuilder'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionAcknowledgeMessage.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", consumerID=" + consumerID);
      buff.append(", messageID=" + messageID);
```

### StringBufferReplaceableByStringBuilder
`StringBuffer buff` may be declared as 'StringBuilder'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/ClusterTopologyChangeMessage.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", exit=" + exit);
      buff.append(", last=" + last);
```

### StringBufferReplaceableByStringBuilder
`StringBuffer buff` may be declared as 'StringBuilder'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionBindingQueryResponseMessage_V3.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", autoCreateAddresses=" + autoCreateAddresses);
      return buff.toString();
```

### StringBufferReplaceableByStringBuilder
`StringBuffer buff` may be declared as 'StringBuilder'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionXAResumeMessage.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", xid=" + xid);
      return buff.toString();
```

### StringBufferReplaceableByStringBuilder
`StringBuffer buff` may be declared as 'StringBuilder'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionSendMessage.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", requiresResponse=" + requiresResponse);
      return buff.toString();
```

### StringBufferReplaceableByStringBuilder
`StringBuffer buff` may be declared as 'StringBuilder'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/ClusterTopologyChangeMessage_V2.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", exit=" + exit);
      buff.append(", last=" + last);
```

### StringBufferReplaceableByStringBuilder
`StringBuffer buff` may be declared as 'StringBuilder'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/CreateSessionMessage_V2.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", metadata=" + clientID);
      return buff.toString();
```

### StringBufferReplaceableByStringBuilder
`StringBuffer buff` may be declared as 'StringBuilder'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionIndividualAcknowledgeMessage.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", consumerID=" + consumerID);
      buff.append(", messageID=" + messageID);
```

### StringBufferReplaceableByStringBuilder
`StringBuffer buff` may be declared as 'StringBuilder'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionRequestProducerCreditsMessage.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", credits=" + credits);
      buff.append(", address=" + address);
```

### StringBufferReplaceableByStringBuilder
`StringBuffer buff` may be declared as 'StringBuilder'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/CreateAddressMessage.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", address=" + address);
      buff.append(", routingTypes=" + routingTypes);
```

### StringBufferReplaceableByStringBuilder
`StringBuffer buff` may be declared as 'StringBuilder'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionReceiveContinuationMessage.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", consumerID=" + consumerID);
      return buff.toString();
```

### StringBufferReplaceableByStringBuilder
`StringBuffer buff` may be declared as 'StringBuilder'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/CreateSharedQueueMessage_V2.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", address=" + address);
      buff.append(", queueName=" + queueName);
```

### StringBufferReplaceableByStringBuilder
`StringBuffer buf` may be declared as 'StringBuilder'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/Ping.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buf = new StringBuffer(super.getPacketString());
      buf.append(", connectionTTL=" + connectionTTL);
      return buf.toString();
```

### StringBufferReplaceableByStringBuilder
`StringBuffer buff` may be declared as 'StringBuilder'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionBindingQueryMessage.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", address=" + address);
      buff.append("]");
```

### StringBufferReplaceableByStringBuilder
`StringBuffer buff` may be declared as 'StringBuilder'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/CreateSharedQueueMessage.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", address=" + address);
      buff.append(", queueName=" + queueName);
```

### StringBufferReplaceableByStringBuilder
`StringBuffer buff` may be declared as 'StringBuilder'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/DisconnectConsumerWithKillMessage.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", nodeID=" + nodeID);
      return buff.toString();
```

### StringBufferReplaceableByStringBuilder
`StringBuffer buff` may be declared as 'StringBuilder'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionQueueQueryMessage.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", queueName=" + queueName);
      return buff.toString();
```

### StringBufferReplaceableByStringBuilder
`StringBuffer buff` may be declared as 'StringBuilder'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/CreateQueueMessage_V2.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", autoCreated=" + autoCreated);
      buff.append(", routingType=" + routingType);
```

### StringBufferReplaceableByStringBuilder
`StringBuffer buff` may be declared as 'StringBuilder'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionBindingQueryResponseMessage_V4.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", defaultPurgeOnNoConsumers=" + defaultPurgeOnNoConsumers);
      buff.append(", defaultMaxConsumers=" + defaultMaxConsumers);
```

### StringBufferReplaceableByStringBuilder
`StringBuffer buf` may be declared as 'StringBuilder'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/DisconnectMessage_V3.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buf = new StringBuffer(super.getPacketString());
      buf.append(", nodeID=" + nodeID);
      buf.append(", reason=" + reason);
```

### StringBufferReplaceableByStringBuilder
`StringBuffer buff` may be declared as 'StringBuilder'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/DisconnectMessage.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", nodeID=" + nodeID);
      return buff.toString();
```

### StringBufferReplaceableByStringBuilder
`StringBuffer buff` may be declared as 'StringBuilder'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionCreateConsumerMessage.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", queueName=" + queueName);
      buff.append(", filterString=" + filterString);
```

### StringBufferReplaceableByStringBuilder
`StringBuffer buff` may be declared as 'StringBuilder'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionSendContinuationMessage.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", continues=" + continues);
      buff.append(", message=" + message);
```

### StringBufferReplaceableByStringBuilder
`StringBuffer buff` may be declared as 'StringBuilder'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/ClusterTopologyChangeMessage_V3.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", exit=" + exit);
      buff.append(", last=" + last);
```

### StringBufferReplaceableByStringBuilder
`StringBuffer buff` may be declared as 'StringBuilder'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionAddMetaDataMessageV2.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", key=" + key);
      buff.append(", data=" + data);
```

### StringBufferReplaceableByStringBuilder
`StringBuffer buff` may be declared as 'StringBuilder'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionBindingQueryResponseMessage_V2.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", autoCreateQueues=" + autoCreateQueues);
      return buff.toString();
```

### StringBufferReplaceableByStringBuilder
`StringBuffer buff` may be declared as 'StringBuilder'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionXARollbackMessage.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", xid=" + xid);
      return buff.toString();
```

### StringBufferReplaceableByStringBuilder
`StringBuffer buff` may be declared as 'StringBuilder'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionAddMetaDataMessage.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", key=" + key);
      buff.append(", data=" + data);
```

### StringBufferReplaceableByStringBuilder
`StringBuffer buff` may be declared as 'StringBuilder'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionXAJoinMessage.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", xid=" + xid);
      return buff.toString();
```

### StringBufferReplaceableByStringBuilder
`StringBuffer buff` may be declared as 'StringBuilder'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionXAGetInDoubtXidsResponseMessage.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", xids=" + xids);
      buff.append("]");
```

### StringBufferReplaceableByStringBuilder
`StringBuffer buff` may be declared as 'StringBuilder'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/ClusterTopologyChangeMessage_V4.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", clientVersion=" + serverVersion);
      return buff.toString();
```

### StringBufferReplaceableByStringBuilder
`StringBuffer buff` may be declared as 'StringBuilder'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SubscribeClusterTopologyUpdatesMessageV2.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", clientVersion=" + clientVersion);
      return buff.toString();
```

### StringBufferReplaceableByStringBuilder
`StringBuffer buff` may be declared as 'StringBuilder'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionXAAfterFailedMessage.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", xid=" + xid);
      return buff.toString();
```

### StringBufferReplaceableByStringBuilder
`StringBuffer buff` may be declared as 'StringBuilder'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/CreateQueueMessage.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", address=" + address);
      buff.append(", queueName=" + queueName);
```

### StringBufferReplaceableByStringBuilder
`StringBuffer buff` may be declared as 'StringBuilder'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionXAGetTimeoutResponseMessage.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", timeoutSeconds=" + timeoutSeconds);
      return buff.toString();
```

### StringBufferReplaceableByStringBuilder
`StringBuffer buff` may be declared as 'StringBuilder'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionContinuationMessage.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", body=" + Arrays.toString(body));
      buff.append(", continues=" + continues);
```

### StringBufferReplaceableByStringBuilder
`StringBuffer buff` may be declared as 'StringBuilder'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionBindingQueryResponseMessage.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", exists=" + exists);
      buff.append(", queueNames=" + queueNames);
```

### StringBufferReplaceableByStringBuilder
`StringBuffer buff` may be declared as 'StringBuilder'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/transaction/impl/XidImpl.java`
#### Snippet
```java

   private String stringRep(final byte[] bytes) {
      StringBuffer buff = new StringBuffer();
      for (int i = 0; i < bytes.length; i++) {
         byte b = bytes[i];
```

### StringBufferReplaceableByStringBuilder
`StringBuffer stringReturn` may be declared as 'StringBuilder'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/security/Role.java`
#### Snippet
```java
   @Override
   public String toString() {
      StringBuffer stringReturn = new StringBuffer("Role {name=" + name + "; allows=[");

      if (send) {
```

### StringBufferReplaceableByStringBuilder
`StringBuffer sb` may be declared as 'StringBuilder'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/utils/XMLUtil.java`
#### Snippet
```java
      }

      StringBuffer sb = new StringBuffer();
      sb.append('<').append(name);

```

### StringBufferReplaceableByStringBuilder
`StringBuffer sb` may be declared as 'StringBuilder'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/utils/XMLUtil.java`
#### Snippet
```java
   public static String getTextContent(final Node n) {
      if (n.hasChildNodes()) {
         StringBuffer sb = new StringBuffer();
         NodeList nl = n.getChildNodes();
         for (int i = 0; i < nl.getLength(); i++) {
```

### StringBufferReplaceableByStringBuilder
`StringBuffer classpath` may be declared as 'StringBuilder'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/utils/VersionLoader.java`
#### Snippet
```java

   public static String getClasspathString() {
      StringBuffer classpath = new StringBuffer();
      ClassLoader applicationClassLoader = VersionImpl.class.getClassLoader();
      URL[] urls = ((URLClassLoader) applicationClassLoader).getURLs();
```

### StringBufferReplaceableByStringBuilder
`StringBuffer buffer` may be declared as 'StringBuilder'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/impl/Page.java`
#### Snippet
```java

   public String debugMessages() throws Exception {
      StringBuffer buffer = new StringBuffer();
      LinkedListIterator<PagedMessage> iter = getMessages().iterator();
      while (iter.hasNext()) {
```

### StringBufferReplaceableByStringBuilder
`StringBuffer stringBuffer` may be declared as 'StringBuilder'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/JournalType.java`
#### Snippet
```java

   static {
      StringBuffer stringBuffer = new StringBuffer();
      for (JournalType type : JournalType.values()) {

```

### StringBufferReplaceableByStringBuilder
`StringBuffer buffer` may be declared as 'StringBuilder'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java
   @Override
   public String toString() {
      StringBuffer buffer = new StringBuffer();
      if (this.metaData != null) {
         for (Map.Entry<String, String> value : metaData.entrySet()) {
```

### StringBufferReplaceableByStringBuilder
`StringBuffer stringBuffer` may be declared as 'StringBuilder'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/routing/KeyType.java`
#### Snippet
```java

   static {
      StringBuffer stringBuffer = new StringBuffer();
      for (KeyType type : KeyType.values()) {

```

### StringBufferReplaceableByStringBuilder
`StringBuffer operationsExecuted` may be declared as 'StringBuilder'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ActiveMQServerImpl.java`
#### Snippet
```java
   @Override
   public String destroyConnectionWithSessionMetadata(String metaKey, String parameterValue) throws Exception {
      StringBuffer operationsExecuted = new StringBuffer();

      try {
```

### StringBufferReplaceableByStringBuilder
`StringBuffer buf` may be declared as 'StringBuilder'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/ReplicationResponseMessageV2.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buf = new StringBuffer(super.getPacketString());
      buf.append(", synchronizationIsFinishedAcknowledgement=" + synchronizationIsFinishedAcknowledgement);
      return buf.toString();
```

### StringBufferReplaceableByStringBuilder
`StringBuffer buff` may be declared as 'StringBuilder'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/QuorumVoteMessage.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", vote=" + vote);
      buff.append(", handler=" + handler);
```

### StringBufferReplaceableByStringBuilder
`StringBuffer buf` may be declared as 'StringBuilder'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/ReplicationStartSyncMessage.java`
#### Snippet
```java
   @Override
   protected String getPacketString() {
      StringBuffer buf = new StringBuffer(super.getPacketString());
      buf.append(", synchronizationIsFinished=" + synchronizationIsFinished);
      buf.append(", dataType=" + dataType);
```

### StringBufferReplaceableByStringBuilder
`StringBuffer buffer` may be declared as 'StringBuilder'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/DescribeJournal.java`
#### Snippet
```java
      @Override
      public String toString() {
         StringBuffer buffer = new StringBuffer();
         buffer.append(msg.isLargeMessage() ? "LargeMessage(" : "Message(");
         buffer.append("messageID=" + msg.getMessageID());
```

## RuleId[id=RedundantStringFormatCall]
### RedundantStringFormatCall
Redundant call to `format()`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/check/CheckAbstract.java`
#### Snippet
```java
               int skippedTasks = checkTasks.length - failedTasks - errorTasks - successTasks;

               context.out.println(String.format("Checks run: %d, Failures: %d, Errors: %d, Skipped: %d, Time elapsed: %.03f sec - %s",
                                                 checkTasks.length, failedTasks, errorTasks, skippedTasks,
                                                 ((float)watch.getTime()) / 1000, this.getClass().getSimpleName()));
```

### RedundantStringFormatCall
Redundant call to `format()`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/queue/StatQueue.java`
#### Snippet
```java

      if (count > maxRows) {
         getActionContext().out.println(String.format("WARNING: the displayed queues are %d/%d, set maxRows to display more queues.", maxRows, count));
      }
   }
```

### RedundantStringFormatCall
Redundant call to `format()`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Create.java`
#### Snippet
```java
      }

      context.out.println(String.format("Creating ActiveMQ Artemis instance at: %s", directory.getCanonicalPath()));

      HashMap<String, String> filters = new LinkedHashMap<>();
```

### RedundantStringFormatCall
Redundant call to `format()`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Create.java`
#### Snippet
```java
      context.out.println("You can now start the broker by executing:  ");
      context.out.println("");
      context.out.println(String.format("   \"%s\" run", path(new File(directory, "bin/artemis"))));

      File service = new File(directory, BIN_ARTEMIS_SERVICE);
```

### RedundantStringFormatCall
Redundant call to `format()`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Create.java`
#### Snippet
```java
         context.out.println("Or you can run the broker in the background using:");
         context.out.println("");
         context.out.println(String.format("   \"%s\" start", path(service)));
         context.out.println("");
      }
```

### RedundantStringFormatCall
Redundant call to `format()`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Create.java`
#### Snippet
```java
         context.out.println("Or you can setup the broker as Windows service and run it in the background:");
         context.out.println("");
         context.out.println(String.format("   \"%s\" install", path(service)));
         context.out.println(String.format("   \"%s\" start", path(service)));
         context.out.println("");
```

### RedundantStringFormatCall
Redundant call to `format()`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Create.java`
#### Snippet
```java
         context.out.println("");
         context.out.println(String.format("   \"%s\" install", path(service)));
         context.out.println(String.format("   \"%s\" start", path(service)));
         context.out.println("");
         context.out.println("   To stop the windows service:");
```

### RedundantStringFormatCall
Redundant call to `format()`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Create.java`
#### Snippet
```java
         context.out.println("");
         context.out.println("   To stop the windows service:");
         context.out.println(String.format("      \"%s\" stop", path(service)));
         context.out.println("");
         context.out.println("   To uninstall the windows service");
```

### RedundantStringFormatCall
Redundant call to `format()`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Create.java`
#### Snippet
```java
         context.out.println("");
         context.out.println("   To uninstall the windows service");
         context.out.println(String.format("      \"%s\" uninstall", path(service)));
      }

```

### RedundantStringFormatCall
Redundant call to `format()`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/management/ObjectNameBuilder.java`
#### Snippet
```java

   public ObjectName getManagementContextObjectName() throws Exception {
      return ObjectName.getInstance(String.format("hawtio:type=security,area=jmx,name=ArtemisJMXSecurity"));
   }
}
```

## RuleId[id=ZeroLengthArrayInitialization]
### ZeroLengthArrayInitialization
Allocation of zero length array
in `artemis-protocols/artemis-stomp-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/stomp/ActiveMQStompException.java`
#### Snippet
```java
         frame.setByteBody(body.getBytes(StandardCharsets.UTF_8));
      } else {
         frame.setByteBody(new byte[0]);
      }
      if (disconnect != null) {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `artemis-protocols/artemis-stomp-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/stomp/VersionedStompFrameHandler.java`
#### Snippet
```java
            data = text.toString().getBytes(StandardCharsets.UTF_8);
         } else {
            data = new byte[0];
         }
      }
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `artemis-commons/src/main/java/org/apache/activemq/artemis/api/core/SimpleString.java`
#### Snippet
```java
      pathsList.add(pathAccumulator.toString());

      paths = pathsList.toArray(new String[0]);
      return paths;
   }
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Env.java`
#### Snippet
```java
      } catch (Throwable t) {
         try {
            Constructor<sun.misc.Unsafe> c = sun.misc.Unsafe.class.getDeclaredConstructor(new Class[0]);
            c.setAccessible(true);
            instance = c.newInstance(new Object[0]);
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Env.java`
#### Snippet
```java
            Constructor<sun.misc.Unsafe> c = sun.misc.Unsafe.class.getDeclaredConstructor(new Class[0]);
            c.setAccessible(true);
            instance = c.newInstance(new Object[0]);
         } catch (Throwable t1) {
            instance = null;
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/SpawnedVMSupport.java`
#### Snippet
```java
                                 final boolean logOutput,
                                 final String... args) throws Exception {
      return SpawnedVMSupport.spawnVM(className, new String[0], logOutput, args);
   }
   public static Process spawnVM(final String classPath,
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/SpawnedVMSupport.java`
#### Snippet
```java
                                 final boolean logOutput,
                                 final String... args) throws Exception {
      return SpawnedVMSupport.spawnVM(classPath, className, new String[0], logOutput, args);
   }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/SpawnedVMSupport.java`
#### Snippet
```java

   public static Process spawnVM(final String className, final String... args) throws Exception {
      return SpawnedVMSupport.spawnVM(className, new String[0], true, args);
   }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalTransaction.java`
#### Snippet
```java
   public long[] getPositiveArray() {
      if (pos == null) {
         return new long[0];
      } else {
         int i = 0;
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/ActiveMQRAService.java`
#### Snippet
```java

            if ("Started".equalsIgnoreCase(stateString) || "Starting".equalsIgnoreCase(stateString)) {
               mBeanServer.invoke(mbean.getObjectName(), "stop", new Object[0], new String[0]);
            }
         }
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/ActiveMQRAService.java`
#### Snippet
```java

            if ("Started".equalsIgnoreCase(stateString) || "Starting".equalsIgnoreCase(stateString)) {
               mBeanServer.invoke(mbean.getObjectName(), "stop", new Object[0], new String[0]);
            }
         }
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Create.java`
#### Snippet
```java
   private String[] getAddressList() {
      if (addresses == null) {
         return new String[0];
      } else {
         return addresses.split(",");
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Create.java`
#### Snippet
```java
   private String[] getQueueList() {
      if (queues == null) {
         return new String[0];
      } else {
         return queues.split(",");
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Create.java`
#### Snippet
```java
   public String[] getStaticNodes() {
      if (staticNode == null) {
         return new String[0];
      } else {
         return staticNode.split(",");
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenwireMessage.java`
#### Snippet
```java
   @Override
   public byte[] getBytesProperty(SimpleString key) throws ActiveMQPropertyConversionException {
      return new byte[0];
   }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenwireMessage.java`
#### Snippet
```java
   @Override
   public byte[] getBytesProperty(String key) throws ActiveMQPropertyConversionException {
      return new byte[0];
   }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/server/impl/JMSServerManagerImpl.java`
#### Snippet
```java
      List<String> result = map.get(name);
      if (result == null) {
         return new String[0];
      } else {
         String[] strings = new String[result.size()];
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/sasl/scram/SCRAMClientSASL.java`
#### Snippet
```java
         }
      }
      return new byte[0];
   }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/connect/mirror/MirrorAddressFilter.java`
#### Snippet
```java
      }

      this.allowList = allowList.toArray(new SimpleString[]{});
      this.denyList = denyList.toArray(new SimpleString[]{});
   }
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/connect/mirror/MirrorAddressFilter.java`
#### Snippet
```java

      this.allowList = allowList.toArray(new SimpleString[]{});
      this.denyList = denyList.toArray(new SimpleString[]{});
   }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/ProtonServerSenderContext.java`
#### Snippet
```java
      ReadableBuffer sendBuffer = message.getSendBuffer(messageReference.getDeliveryCount(), messageReference);
      // we only need a tag if we are going to settle later
      byte[] tag = preSettle ? new byte[0] : protonSession.getTag();

      boolean releaseRequired = sendBuffer instanceof NettyReadable;
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/ProtonServerSenderContext.java`
#### Snippet
```java

      // we only need a tag if we are going to settle later
      byte[] tag = preSettle ? new byte[0] : protonSession.getTag();

      final Delivery delivery;
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `artemis-jdbc-store/src/main/java/org/apache/activemq/artemis/jdbc/store/file/JDBCSequentialFileFactoryDriver.java`
#### Snippet
```java
               createFile.setString(1, file.getFileName());
               createFile.setString(2, file.getExtension());
               createFile.setBytes(3, new byte[0]);
               createFile.executeUpdate();
               try (ResultSet keys = createFile.getGeneratedKeys()) {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `artemis-jdbc-store/src/main/java/org/apache/activemq/artemis/jdbc/store/journal/JDBCJournalRecord.java`
#### Snippet
```java
      variableSize = 0;
      userRecordType = -1;
      record = new ByteArrayInputStream(new byte[0]);
      txDataSize = 0;
      txData = new ByteArrayInputStream(new byte[0]);
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `artemis-jdbc-store/src/main/java/org/apache/activemq/artemis/jdbc/store/journal/JDBCJournalRecord.java`
#### Snippet
```java
      record = new ByteArrayInputStream(new byte[0]);
      txDataSize = 0;
      txData = new ByteArrayInputStream(new byte[0]);
      txCheckNoRecords = 0;

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `artemis-jdbc-store/src/main/java/org/apache/activemq/artemis/jdbc/store/journal/JDBCJournalImpl.java`
#### Snippet
```java
   @Override
   public JournalFile[] getDataFiles() {
      return new JournalFile[0];
   }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `artemis-jdbc-store/src/main/java/org/apache/activemq/artemis/jdbc/store/journal/JDBCJournalImpl.java`
#### Snippet
```java
      // We actually only need the record ID in this instance.
      if (record.isTransactional()) {
         RecordInfo info = new RecordInfo(record.getId(), record.getRecordType(), new byte[0], record.isUpdate(), false, record.getCompactCount());
         if (record.getRecordType() == JDBCJournalRecord.DELETE_RECORD_TX) {
            txHolder.recordsToDelete.add(info);
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/TransportConfiguration.java`
#### Snippet
```java
   public static String[] splitHosts(final String commaSeparatedHosts) {
      if (commaSeparatedHosts == null) {
         return new String[0];
      }
      String[] hosts = commaSeparatedHosts.split(",");
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ServerLocatorImpl.java`
#### Snippet
```java
         return "ServerLocatorImpl (identity=" + identity +
            ") [initialConnectors=" +
            Arrays.toString(initialConnectors == null ? new TransportConfiguration[0] : initialConnectors) +
            ", discoveryGroupConfiguration=" +
            discoveryGroupConfiguration +
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ServerLocatorImpl.java`
#### Snippet
```java
            "]";
      }
      return "ServerLocatorImpl [initialConnectors=" + Arrays.toString(initialConnectors == null ? new TransportConfiguration[0] : initialConnectors) +
         ", discoveryGroupConfiguration=" +
         discoveryGroupConfiguration +
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ServerLocatorImpl.java`
#### Snippet
```java
   public TransportConfiguration[] getStaticTransportConfigurations() {
      if (initialConnectors == null)
         return new TransportConfiguration[]{};
      return Arrays.copyOf(initialConnectors, initialConnectors.length);
   }
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ClientSessionImpl.java`
#### Snippet
```java
      }

      return new Xid[0];
   }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/message/impl/CoreMessage.java`
#### Snippet
```java
            rc.put(CompositeDataConstants.BODY, JsonUtil.truncate(bytes, valueSizeLimit));
         } else {
            rc.put(CompositeDataConstants.BODY, new byte[0]);
         }
         return rc;
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionContinuationMessage.java`
#### Snippet
```java
   public byte[] getBody() {
      if (size <= 0) {
         return new byte[0];
      } else {
         return body;
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/utils/VersionLoader.java`
#### Snippet
```java

   private static int[] parseCompatibleVersionList(String property) throws IOException {
      int[] verArray = new int[0];
      StringTokenizer tokenizer = new StringTokenizer(property, ",");
      while (tokenizer.hasMoreTokens()) {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/protocol/ProtocolManagerFactory.java`
#### Snippet
```java

   default Persister<Message>[] getPersister() {
      return new Persister[]{};
   }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/security/ActiveMQBasicSecurityManager.java`
#### Snippet
```java
         Map<String, Set<String>> rolesByUser = invertProperties(ClassloadingUtil.loadProperties(properties.get(BOOTSTRAP_ROLE_FILE)));
         for (String user : users.stringPropertyNames()) {
            addOrUpdateUser(user, users.getProperty(user), rolesByUser.get(user).toArray(new String[0]));
         }
      } else if (properties != null && properties.containsKey(BOOTSTRAP_USER) && properties.containsKey(BOOTSTRAP_PASSWORD) && properties.containsKey(BOOTSTRAP_ROLE)) {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/security/jaas/PropertiesLoginModule.java`
#### Snippet
```java
      char[] tmpPassword = ((PasswordCallback) callbacks[1]).getPassword();
      if (tmpPassword == null) {
         tmpPassword = new char[0];
      }
      if (user == null) {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/security/jaas/JaasCallbackHandler.java`
#### Snippet
```java
               Set<Principal> principals = peerSubject.getPrincipals();
               if (!principals.isEmpty()) {
                  principalsCallback.setPeerPrincipals(principals.toArray(new Principal[0]));
                  return;
               }
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/security/jaas/InVMLoginModule.java`
#### Snippet
```java
      char[] tmpPassword = ((PasswordCallback) callbacks[1]).getPassword();
      if (tmpPassword == null) {
         tmpPassword = new char[0];
      }
      if (user == null) {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/files/FileMoveManager.java`
#### Snippet
```java

      if (list == null) {
         list = new String[0];
      }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/impl/HawtioSecurityControlImpl.java`
#### Snippet
```java
                  res = canInvoke(objectName, name);
               } else {
                  res = canInvoke(objectName, name, argTypes.toArray(new String[]{}));
               }
               CompositeData data = new CompositeDataSupport(CAN_INVOKE_RESULT_ROW_TYPE,
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/impl/ManagementServiceImpl.java`
#### Snippet
```java

         if (params == null) {
            params = new Object[0];
         }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/impl/ManagementServiceImpl.java`
#### Snippet
```java
         String upperCaseAttribute = attribute.substring(0, 1).toUpperCase() + attribute.substring(1);
         try {
            method = resource.getClass().getMethod("get" + upperCaseAttribute, new Class[0]);
         } catch (NoSuchMethodException nsme) {
            try {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/impl/ManagementServiceImpl.java`
#### Snippet
```java
         } catch (NoSuchMethodException nsme) {
            try {
               method = resource.getClass().getMethod("is" + upperCaseAttribute, new Class[0]);
            } catch (NoSuchMethodException nsme2) {
               throw ActiveMQMessageBundle.BUNDLE.noGetterMethod(attribute);
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/impl/ManagementServiceImpl.java`
#### Snippet
```java
            }
         }
         return method.invoke(resource, new Object[0]);
      } catch (Throwable t) {
         throw new IllegalStateException("Problem while retrieving attribute " + attribute, t);
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/AddressControlImpl.java`
#### Snippet
```java
            return bindingNames;
         } else {
            return new String[0];
         }
      } catch (Throwable t) {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/AddressControlImpl.java`
#### Snippet
```java
            return queueNames.toArray(new String[queueNames.size()]);
         } else {
            return new String[0];
         }
      } catch (Throwable t) {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/ActiveMQServerControlImpl.java`
#### Snippet
```java

      try {
         return server.getConnectorsService().getConnectors().keySet().toArray(new String[0]);
      } finally {
         blockOnIO();
```

## RuleId[id=NonFinalFieldOfException]
### NonFinalFieldOfException
Non-final field `body` of exception class
in `artemis-protocols/artemis-stomp-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/stomp/ActiveMQStompException.java`
#### Snippet
```java
   private int code = NONE;
   private final List<Header> headers = new ArrayList<>(10);
   private String body;
   private VersionedStompFrameHandler handler;
   private Boolean disconnect;
```

### NonFinalFieldOfException
Non-final field `disconnect` of exception class
in `artemis-protocols/artemis-stomp-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/stomp/ActiveMQStompException.java`
#### Snippet
```java
   private String body;
   private VersionedStompFrameHandler handler;
   private Boolean disconnect;

   public ActiveMQStompException(StompConnection connection, String msg) {
```

### NonFinalFieldOfException
Non-final field `handler` of exception class
in `artemis-protocols/artemis-stomp-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/stomp/ActiveMQStompException.java`
#### Snippet
```java
   private final List<Header> headers = new ArrayList<>(10);
   private String body;
   private VersionedStompFrameHandler handler;
   private Boolean disconnect;

```

### NonFinalFieldOfException
Non-final field `code` of exception class
in `artemis-protocols/artemis-stomp-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/stomp/ActiveMQStompException.java`
#### Snippet
```java
   private static final long serialVersionUID = -274452327574950068L;

   private int code = NONE;
   private final List<Header> headers = new ArrayList<>(10);
   private String body;
```

## RuleId[id=NullArgumentToVariableArgMethod]
### NullArgumentToVariableArgMethod
Confusing argument `null`, unclear if a varargs or non-varargs call is desired
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Run.java`
#### Snippet
```java
         for (ComponentDTO componentDTO : broker.components) {
            Class clazz = this.getClass().getClassLoader().loadClass(componentDTO.componentClassName);
            ExternalComponent component = (ExternalComponent) clazz.getDeclaredConstructor(null).newInstance();
            component.configure(componentDTO, getBrokerInstance(), getBrokerHome());
            server.getServer().addExternalComponent(component, true);
```

## RuleId[id=TypeParameterExtendsObject]
### TypeParameterExtendsObject
Type parameter `T` explicitly extends 'java.lang.Object'
in `artemis-commons/src/main/java/org/apache/activemq/artemis/core/persistence/Persister.java`
#### Snippet
```java
import org.apache.activemq.artemis.api.core.ActiveMQBuffer;

public interface Persister<T extends Object> {


```

### TypeParameterExtendsObject
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/uri/URISupport.java`
#### Snippet
```java
    * @throws java.net.URISyntaxException
    */
   public static String createQueryString(Map<String, ? extends Object> options) throws URISyntaxException {
      try {
         if (options.size() > 0) {
```

### TypeParameterExtendsObject
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/ActiveMQMessageBundle.java`
#### Snippet
```java

   @Message(id = 229075, value = "method autoEncode doesn't know how to convert {} yet")
   IllegalArgumentException autoConvertError(Class<? extends Object> aClass);

   /**
```

### TypeParameterExtendsObject
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/impl/ConfigurationImpl.java`
#### Snippet
```java
      HashMap<String, String> errors = new HashMap<>();
      // Loop through the property name/value pairs to be set
      for (final Map.Entry<String, ? extends Object> entry : beanProperties.entrySet()) {
         // Identify the property name and value(s) to be assigned
         final String name = entry.getKey();
```

## RuleId[id=OptionalGetWithoutIsPresent]
### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/qourum/QuorumManager.java`
#### Snippet
```java
         final String remoteAddress = sessionFactory.getConnection().getRemoteAddress();
         ActiveMQServerLogger.LOGGER.sendingQuorumVoteRequest(remoteAddress, vote.toString());
         QuorumVoteReplyMessage replyMessage = (QuorumVoteReplyMessage) clusterControl.getClusterChannel().get()
            .sendBlocking(new QuorumVoteMessage(handler, vote), PacketImpl.QUORUM_VOTE_REPLY);
         QuorumVoteHandler voteHandler = getVoteHandler(replyMessage.getHandler());
```

## RuleId[id=ConstantValue]
### ConstantValue
Condition `cs != null` is always `false`
in `artemis-service-extensions/src/main/java/org/apache/activemq/artemis/service/extensions/xa/recovery/ActiveMQXAResourceWrapper.java`
#### Snippet
```java

            try {
               if (cs != null)
                  cs.close();
               if (serverLocator != null)
```

### ConstantValue
Condition `booked != null` is always `true`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/core/server/ActiveMQScheduledComponent.java`
#### Snippet
```java
            executor.execute(() -> runForExecutor(booked));
         } catch (RejectedExecutionException e) {
            if (booked != null) {
               booked.set(false);
            }
```

### ConstantValue
Value `isPooled` is always 'false'
in `artemis-commons/src/main/java/org/apache/activemq/artemis/core/buffers/impl/ChannelBufferWrapper.java`
#### Snippet
```java
         return returnBuffer;
      }
      return new ChannelBufferWrapper(buffer.readSlice(length), releasable, isPooled);
   }

```

### ConstantValue
Condition `qName != null` is always `true` when reached
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/CompositeAddress.java`
#### Snippet
```java
      } else if (address != null && qName == null) {
         result = address;
      } else if (address == null && qName != null) {
         result = qName;
      } else {
```

### ConstantValue
Value `str` is always 'null'
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/StringEscapeUtils.java`
#### Snippet
```java
   public static String escapeString(String str) {
      if (str == null) {
         return str;
      }
      int sz = str.length();
```

### ConstantValue
Condition `networkInterfaces == null` is always `false`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/UUIDGenerator.java`
#### Snippet
```java
         networkInterfaces = NetworkInterface.getNetworkInterfaces();

         if (networkInterfaces == null) {
            return Collections.emptyList();
         }
```

### ConstantValue
Condition `byte1 <= 0x7F` is always `true` when reached
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/UTF8Util.java`
#### Snippet
```java
         byte1 = PlatformDependent.getByte(bytes, count++);

         if (byte1 >= 0 && byte1 <= 0x7F) {
            chars[charCount++] = (char) byte1;
         } else {
```

### ConstantValue
Condition `byte1 <= 0x7F` is always `true` when reached
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/UTF8Util.java`
#### Snippet
```java
         byte1 = PlatformDependent.getByte(addressBytes + count++);

         if (byte1 >= 0 && byte1 <= 0x7F) {
            chars[charCount++] = (char) byte1;
         } else {
```

### ConstantValue
Condition `byte1 <= 0x7F` is always `true` when reached
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/UTF8Util.java`
#### Snippet
```java
         byte1 = bytes[count++];

         if (byte1 >= 0 && byte1 <= 0x7F) {
            chars[charCount++] = (char) byte1;
         } else {
```

### ConstantValue
Condition `position >= 0` is always `true`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java

         // Got data?
         if (position >= 0) {
            // End of relevant data?
            if ( /*!encode &&*/position >= numSigBytes) {
```

### ConstantValue
Condition `ok == null` is always `false`
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/io/RunnableCallback.java`
#### Snippet
```java
         throw new NullPointerException("ok = null");
      }
      if (ok == null) {
         throw new NullPointerException("error = null");
      }
```

### ConstantValue
Condition `uri != null` is always `true`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/uri/URISupport.java`
#### Snippet
```java
         uri = uri.substring(uri.lastIndexOf("?") + 1); // get only the relevant part of the query
         Map<String, String> rc = new HashMap<>();
         if (uri != null && !uri.isEmpty()) {
            parseParameters(rc, uri.split("[&;]"));
         }
```

### ConstantValue
Condition `p >= 0` is always `false`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/uri/URISupport.java`
#### Snippet
```java
         p = rc.host.indexOf("/");

         if (p >= 0) {
            rc.path = rc.host.substring(p);
            rc.host = rc.host.substring(0, p);
```

### ConstantValue
Condition `current instanceof JMSException` is always `true` when reached
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/ActiveMQRASessionFactoryImpl.java`
#### Snippet
```java
         }

         if (current != null && current instanceof JMSException) {
            throw (JMSException) current;
         } else {
```

### ConstantValue
Condition `status == Status.STATUS_ROLLING_BACK` is always `true`
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/ActiveMQRAManagedConnection.java`
#### Snippet
```java
            } else if (status == Status.STATUS_ROLLEDBACK) {
               statusMessage = "rolled back";
            } else if (status == Status.STATUS_ROLLING_BACK) {
               statusMessage = "rolling back";
            }
```

### ConstantValue
Condition `selector != null` is always `true`
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/inflow/ActiveMQMessageHandler.java`
#### Snippet
```java
            SimpleString oldFilterString = subResponse.getFilterString();

            boolean selectorChanged = selector == null && oldFilterString != null || oldFilterString == null && selector != null || (oldFilterString != null && selector != null && !oldFilterString.toString().equals(selector));

            SimpleString oldTopicName = (enable1XPrefix && !subResponse.getAddress().startsWith(PacketImpl.OLD_TOPIC_PREFIX) ? PacketImpl.OLD_TOPIC_PREFIX : SimpleString.toSimpleString("")).concat(subResponse.getAddress());
```

### ConstantValue
Condition `!isTopic` is always `true`
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/inflow/ActiveMQActivation.java`
#### Snippet
```java
               if (isTopic) {
                  calculatedDestinationName = getTopicWithPrefix(calculatedDestinationName);
               } else if (!isTopic) {
                  calculatedDestinationName = getQueueWithPrefix(calculatedDestinationName);
               }
```

### ConstantValue
Value `isTopic` is always 'false'
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/inflow/ActiveMQActivation.java`
#### Snippet
```java
               if (isTopic) {
                  calculatedDestinationName = getTopicWithPrefix(calculatedDestinationName);
               } else if (!isTopic) {
                  calculatedDestinationName = getQueueWithPrefix(calculatedDestinationName);
               }
```

### ConstantValue
Condition `lastException == null` is always `false`
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/inflow/ActiveMQActivation.java`
#### Snippet
```java
            } catch (Throwable t) {
               if (failure instanceof ActiveMQException && ((ActiveMQException) failure).getType() == ActiveMQExceptionType.QUEUE_DOES_NOT_EXIST) {
                  if (lastException == null || !(t instanceof ActiveMQNonExistentQueueException)) {
                     lastException = t;
                     ActiveMQRALogger.LOGGER.awaitingTopicQueueCreation(getActivationSpec().getDestination());
```

### ConstantValue
Condition `lastException == null` is always `false`
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/inflow/ActiveMQActivation.java`
#### Snippet
```java
                  }
               } else if (failure instanceof ActiveMQException && ((ActiveMQException) failure).getType() == ActiveMQExceptionType.NOT_CONNECTED) {
                  if (lastException == null || !(t instanceof ActiveMQNotConnectedException)) {
                     lastException = t;
                     ActiveMQRALogger.LOGGER.awaitingJMSServerCreation();
```

### ConstantValue
Condition `artemisInstance == null` is always `true`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/ActionAbstract.java`
#### Snippet
```java

         File artemisInstance = null;
         if (artemisInstance == null && instanceProperty != null) {
            artemisInstance = new File(instanceProperty);
         }
```

### ConstantValue
Condition `bin == null` is always `false`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Upgrade.java`
#### Snippet
```java
      }

      if (bin == null || !bin.exists()) { // it can't be null, just being cautious
         throw new IOException(bin + " does not exist for binary");
      }
```

### ConstantValue
Value `verbose` is always 'true'
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/util/SyncCalculation.java`
#### Snippet
```java
               System.out.println("Time = " + result[ntry] + " milliseconds");
               System.out.println("Writes / millisecond = " + dcformat.format(writesPerMillisecond));
               System.out.println("bufferTimeout = " + toNanos(result[ntry], blocks, verbose));
               System.out.println("**************************************************");
            }
```

### ConstantValue
Condition `!noJournalSync` is always `true`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Create.java`
#### Snippet
```java
                  syncFilter.put("${nanoseconds}", "0");
               } else if (journalType != JournalType.NIO) {
                  long nioTime = SyncCalculation.syncTest(dataFolder, 4096, writes, 5, verbose, !noJournalSync, false, "journal-test.tmp", ActiveMQDefaultConfiguration.getDefaultJournalMaxIoAio(), JournalType.NIO);
                  long nioNanoseconds = SyncCalculation.toNanos(nioTime, writes, verbose);
                  syncFilter.put("${nanoseconds}", Long.toString(nioNanoseconds));
```

### ConstantValue
Value `noJournalSync` is always 'false'
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Create.java`
#### Snippet
```java
                  syncFilter.put("${nanoseconds}", "0");
               } else if (journalType != JournalType.NIO) {
                  long nioTime = SyncCalculation.syncTest(dataFolder, 4096, writes, 5, verbose, !noJournalSync, false, "journal-test.tmp", ActiveMQDefaultConfiguration.getDefaultJournalMaxIoAio(), JournalType.NIO);
                  long nioNanoseconds = SyncCalculation.toNanos(nioTime, writes, verbose);
                  syncFilter.put("${nanoseconds}", Long.toString(nioNanoseconds));
```

### ConstantValue
Value `serializer` is always 'null'
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/Producer.java`
#### Snippet
```java
               MessageSerializer serializer = getMessageSerializer();
               if (serializer == null) {
                  System.err.println("Error. Unable to instantiate serializer class: " + serializer);
                  return null;
               }
```

### ConstantValue
Value `filter` is always 'null'
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/Transfer.java`
#### Snippet
```java
               consumer = sourceSession.createDurableConsumer(topic, durableConsumer);
            } else {
               consumer = sourceSession.createDurableConsumer(topic, durableConsumer, filter, noLocal);
            }
         } else if (sharedDurableSubscription != null) {
```

### ConstantValue
Condition `nodeId == null` is always `true`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/activation/ActivationSequenceList.java`
#### Snippet
```java
         Long localSequence = null;
         if (nodeManager != null) {
            assert nodeId == null;
            nodeId = nodeManager.getNodeId().toString();
         } else {
```

### ConstantValue
Condition `nodeId != null` is always `true`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/activation/ActivationSequenceList.java`
#### Snippet
```java
            nodeId = nodeManager.getNodeId().toString();
         } else {
            assert nodeId != null;
         }
         if (!remote && nodeManager != null) {
```

### ConstantValue
Condition `nodeManager != null` is always `true` when reached
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/activation/ActivationSequenceList.java`
#### Snippet
```java
            assert nodeId != null;
         }
         if (!remote && nodeManager != null) {
            final long localActivationSequence = nodeManager.getNodeActivationSequence();
            if (localActivationSequence == NodeManager.NULL_NODE_ACTIVATION_SEQUENCE) {
```

### ConstantValue
Condition `selector != null` is always `true`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/amq/AMQConsumer.java`
#### Snippet
```java
            SimpleString oldFilterString = result.getFilterString();

            boolean selectorChanged = selector == null && oldFilterString != null || oldFilterString == null && selector != null || oldFilterString != null && selector != null && !oldFilterString.equals(selector);

            SimpleString oldTopicName = result.getAddress();
```

### ConstantValue
Condition `topicAliasMax != null` is always `true`
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTPublishManager.java`
#### Snippet
```java
                  // [MQTT-3.3.2-8]
                  throw new DisconnectException(MQTTReasonCodes.TOPIC_ALIAS_INVALID);
               } else if (topicAliasMax != null && alias > topicAliasMax) {
                  // [MQTT-3.3.2-9]
                  throw new DisconnectException(MQTTReasonCodes.TOPIC_ALIAS_INVALID);
```

### ConstantValue
Value `internal` is always 'false'
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTPublishManager.java`
#### Snippet
```java
               }
               if (session.getVersion() == MQTTVersion.MQTT_5) {
                  sendMessageAck(internal, qos, messageId, MQTTReasonCodes.NOT_AUTHORIZED);
                  return;
               } else if (session.getVersion() == MQTTVersion.MQTT_3_1_1) {
```

### ConstantValue
Value `tx` is always 'null'
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenWireConnection.java`
#### Snippet
```java
                  throw ex;
               } else {
                  logger.trace("XAcommit into {}, xid={} cannot find it", tx, xid);
                  XAException ex = new XAException("Cannot find xid in resource manager: " + xid);
                  ex.errorCode = XAException.XAER_NOTA;
```

### ConstantValue
Value `tx` is always 'null'
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenWireConnection.java`
#### Snippet
```java
                  throw ex;
               } else {
                  logger.trace("xarollback into {}, xid={} forcing a rollback regular", tx, xid);

                  try {
```

### ConstantValue
Condition `amqSession != null` is always `false`
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenWireConnection.java`
#### Snippet
```java

                  try {
                     if (amqSession != null) {
                        amqSession.getCoreSession().rollback(false);
                     }
```

### ConstantValue
Value `fail` is always 'true'
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenWireConnection.java`
#### Snippet
```java
      }
      if (fail) {
         shutdown(fail);
      }
   }
```

### ConstantValue
Value `durable` is always 'true'
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/server/impl/JMSServerManagerImpl.java`
#### Snippet
```java

               if (storeConfig && durable) {
                  storage.storeDestination(new PersistedDestination(PersistedType.Queue, queueName, selectorString, durable));
                  if (usedBindings != null) {
                     storage.addBindings(PersistedType.Queue, queueName, usedBindings);
```

### ConstantValue
Condition `postThreads != null` is always `true`
in `artemis-unit-test-support/src/main/java/org/apache/activemq/artemis/utils/ThreadLeakCheckRule.java`
#### Snippet
```java
      Map<Thread, StackTraceElement[]> postThreads = Thread.getAllStackTraces();

      if (postThreads != null && previousThreads != null && postThreads.size() > previousThreads.size()) {

         for (Thread aliveThread : postThreads.keySet()) {
```

### ConstantValue
Condition `compactor != null` is always `true`
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalImpl.java`
#### Snippet
```java
               // computing the delete should be done after compacting is done
               if (jrnRecord == null) {
                  if (compactor != null) {
                     compactor.addCommandUpdate(id, usedFile, updateRecord.getEncodeSize(), replaceableUpdate);
                  }
```

### ConstantValue
Condition `e instanceof IllegalStateException` is always `false`
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalImpl.java`
#### Snippet
```java
      if (e instanceof Exception) {
         throw (Exception) e;
      } else if (e instanceof IllegalStateException) {
         throw (IllegalStateException) e;
      } else {
```

### ConstantValue
Value `autoDeleteQueueName` is always 'null'
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQSession.java`
#### Snippet
```java
         consumer = createClientConsumer(dest, queueName, null);

         ActiveMQMessageConsumer jbc = new ActiveMQMessageConsumer(options, connection, this, consumer, false, dest, selectorString, autoDeleteQueueName);

         consumers.add(jbc);
```

### ConstantValue
Condition `coreFilterString != null` is always `true`
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQSession.java`
#### Snippet
```java
                     oldFilterString == null && coreFilterString != null ||
                     oldFilterString != null &&
                        coreFilterString != null &&
                        !oldFilterString.equals(coreFilterString);

```

### ConstantValue
Condition `url != null` is always `true`
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQConnectionFactory.java`
#### Snippet
```java
      }
      try {
         if (url != null && url.length() > 0) {
            url = updateBrokerURL(url, props);
            setBrokerURL(url);
```

### ConstantValue
Condition `url != null && url.length() > 0` is always `true`
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQConnectionFactory.java`
#### Snippet
```java
      }
      try {
         if (url != null && url.length() > 0) {
            url = updateBrokerURL(url, props);
            setBrokerURL(url);
```

### ConstantValue
Condition `url.length() > 0` is always `true` when reached
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQConnectionFactory.java`
#### Snippet
```java
      }
      try {
         if (url != null && url.length() > 0) {
            url = updateBrokerURL(url, props);
            setBrokerURL(url);
```

### ConstantValue
Condition `segment != null` is always `true`
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jndi/ReadOnlyContext.java`
#### Snippet
```java
      } else {
         String segment = name.substring(0, pos);
         assert segment != null;
         assert !segment.equals("");
         Object o = treeBindings.get(segment);
```

### ConstantValue
Condition `manager != null` is always `true`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPSessionCallback.java`
#### Snippet
```java
         if (address == null) {
            PagingManager pagingManager = manager.getServer().getPagingManager();
            if (manager != null && manager.getServer() != null &&
                manager.getServer().getAddressSettingsRepository() != null &&
                manager.getServer().getAddressSettingsRepository().getMatch("#").getAddressFullMessagePolicy().equals(AddressFullMessagePolicy.PAGE)) {
```

### ConstantValue
Value `section` is always 'null'
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
      }

      return section;
   }

```

### ConstantValue
Value `o` is always 'null'
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
               map.put(extraPropertiesPrefix + s.toString(), o);
            } else {
               map.put(extraPropertiesPrefix + s.toString(), JsonUtil.truncate(o != null ? o.toString() : o, valueSizeLimit));
            }
         });
```

### ConstantValue
Value `remoteState` is always 'null'
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/ProtonServerSenderContext.java`
#### Snippet
```java

      if (remoteState == null) {
         logger.debug("Received null disposition for delivery update: {}", remoteState);
         return true;
      }
```

### ConstantValue
Condition `payload == null` is always `false`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/converter/coreWrapper/CoreBytesMessageWrapper.java`
#### Snippet
```java

      maMap.put(AMQPMessageSupport.JMS_MSG_TYPE, AMQPMessageSupport.JMS_BYTES_MESSAGE);
      if (payload == null) {
         payload = EMPTY_BINARY;
      }
```

### ConstantValue
Value `actual` is always 'null'
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/converter/AMQPMessageSupport.java`
#### Snippet
```java
         return actual == null;
      } else {
         return expected.equals(actual != null ? actual.toString() : actual);
      }
   }
```

### ConstantValue
Value `dialect` is always 'null'
in `artemis-jdbc-store/src/main/java/org/apache/activemq/artemis/jdbc/store/sql/PropertySQLProvider.java`
#### Snippet
```java
            }
         }
         return dialect;
      }

```

### ConstantValue
Condition `session != null` is always `true`
in `artemis-web/src/main/java/org/apache/activemq/artemis/component/AuthenticationFilter.java`
#### Snippet
```java
            HttpSession session = ((Request) servletRequest).getSession(false);
            if (session != null) {
               AuditLogger.userSuccesfullyAuthenticatedInAudit(session != null ? (Subject) session.getAttribute("subject") : null);
            }
         } else if (status >= 400 && status < 599) {
```

### ConstantValue
Condition `header == null` is always `true`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/converter/CoreAmqpConverter.java`
#### Snippet
```java

         if (message.getInnerMessage().isDurable()) {
            if (header == null) {
               header = new Header();
            }
```

### ConstantValue
Condition `failureCause == null` is always `true`
in `artemis-junit/artemis-junit-commons/src/main/java/org/apache/activemq/artemis/junit/EmbeddedActiveMQDelegate.java`
#### Snippet
```java
            return message;
         } catch (ActiveMQException amqEx) {
            if (failureCause == null) {
               failureCause = new EmbeddedActiveMQResourceException(String.format("Failed to create consumer for %s",
                                                                    address.toString()), amqEx);
```

### ConstantValue
Condition `m == null` is always `true`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ClientConsumerImpl.java`
#### Snippet
```java
                  }

                  if (m == null && forcingDelivery) {
                     if (stopped) {
                        break;
```

### ConstantValue
Condition `m != null` is always `false`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ClientConsumerImpl.java`
#### Snippet
```java
                  }

                  if (m != null || closed) {
                     break;
                  }
```

### ConstantValue
Condition `retryDiscovery` is always `true` when reached
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ServerLocatorImpl.java`
#### Snippet
```java
         }
      }
      while (!discoveryOK && retryDiscovery);

      if (!discoveryOK) {
```

### ConstantValue
Condition `!discoveryOK` is always `false`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ServerLocatorImpl.java`
#### Snippet
```java
      while (!discoveryOK && retryDiscovery);

      if (!discoveryOK) {
         // I don't think the code would ever get to this situation, since there's an exception thrown on the previous loop
         // however I will keep this just in case
```

### ConstantValue
Value `discoveryOK` is always 'true'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ServerLocatorImpl.java`
#### Snippet
```java
      while (!discoveryOK && retryDiscovery);

      if (!discoveryOK) {
         // I don't think the code would ever get to this situation, since there's an exception thrown on the previous loop
         // however I will keep this just in case
```

### ConstantValue
Value `tc` is always 'null'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/BackwardsCompatibilityUtils.java`
#### Snippet
```java
         return new TransportConfiguration(className, convertParameters(tc.getParams()), tc.getName(), convertParameters(tc.getExtraParams()));
      }
      return tc;
   }

```

### ConstantValue
Value `text` is always 'null'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/message/impl/CoreMessage.java`
#### Snippet
```java
            } else {
               SimpleString text = m.getReadOnlyBodyBuffer().readNullableSimpleString();
               rc.put(CompositeDataConstants.TEXT_BODY, JsonUtil.truncate(text != null ? text.toString() : text, valueSizeLimit));
            }
         } else {
```

### ConstantValue
Condition `transportConnection != null` is always `false`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ClientSessionFactoryImpl.java`
#### Snippet
```java
         ActiveMQClientLogger.LOGGER.createConnectorException(cause);

         if (transportConnection != null) {
            try {
               transportConnection.close();
```

### ConstantValue
Condition `clazz == null` is always `false`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/utils/ObjectInputStreamWithClassLoader.java`
#### Snippet
```java
         Class clazz = Class.forName(name, false, loader);
         // sanity check only.. if a classLoader can't find a clazz, it will throw an exception
         if (clazz == null) {
            clazz = super.resolveClass(desc);
         }
```

### ConstantValue
Value `debug` is always 'true'
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/security/jaas/GuestLoginModule.java`
#### Snippet
```java

      if (debug) {
         logger.debug("Initialized debug={} guestUser={} guestGroup={}", debug, userName, roleName);
      }

```

### ConstantValue
Condition `managementAddress != null` is always `true` when reached
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/impl/PagingManagerImpl.java`
#### Snippet
```java
   //any caller that calls this method must guarantee the store doesn't exist.
   private PagingStore newStore(final SimpleString address) throws Exception {
      assert managementAddress == null || (managementAddress != null && !address.startsWith(managementAddress));
      syncLock.readLock().lock();
      try {
```

### ConstantValue
Condition `txCleanup < 0` is always `true`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/cursor/impl/PageSubscriptionCounterImpl.java`
#### Snippet
```java
      try {
         if (recordID >= 0) {
            if (txCleanup < 0) {
               txCleanup = storage.generateID();
            }
```

### ConstantValue
Condition `info != null` is always `true`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/cursor/impl/PageSubscriptionImpl.java`
#### Snippet
```java
      PageCursorInfo info = getPageInfo(pagedMessage.getPageNumber());

      if (info != null) {
         info.decrementPendingTX();
      }
```

### ConstantValue
Condition `info != null` is always `true`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/cursor/impl/PageSubscriptionImpl.java`
#### Snippet
```java
               PageCursorInfo info = getPageInfo(message.getPagedMessage().getPageNumber());

               if (!browsing && info != null && (info.isRemoved(message.getPagedMessage().getMessageNumber()) || info.getCompleteInfo() != null)) {
                  if (logger.isTraceEnabled()) {
                     boolean removed = info.isRemoved(message.getPagedMessage().getMessageNumber());
```

### ConstantValue
Condition `info != null` is always `true`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/cursor/impl/PageSubscriptionImpl.java`
#### Snippet
```java
               }

               if (info != null && info.isAck(message.getPagedMessage().getMessageNumber())) {
                  logger.trace("CursorIterator::message {} is acked, moving next", message);
                  continue;
```

### ConstantValue
Condition `info != null` is always `true`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/cursor/impl/PageSubscriptionImpl.java`
#### Snippet
```java
                  // is being changed. That's why the false is passed as a parameter here

                  if (!browsing && info != null && info.isRemoved(message.getPagedMessage().getMessageNumber())) {
                     valid = false;
                  }
```

### ConstantValue
Condition `info != null` is always `true`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/cursor/impl/PageSubscriptionImpl.java`
#### Snippet
```java

      PageCursorInfo info = getPageInfo(position);
      if (info != null) {
         logger.trace("InstallTXCallback looking up pagePosition {}, result={}", position, info);

```

### ConstantValue
Condition `info != null` is always `true`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/cursor/impl/PageSubscriptionImpl.java`
#### Snippet
```java
      PageCursorInfo info = getPageInfo(pagedMessage.getPageNumber());

      if (info != null) {
         info.incrementPendingTX();
      }
```

### ConstantValue
Condition `txDeleteCursorOnReload >= 0` is always `false`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/cursor/impl/PageSubscriptionImpl.java`
#### Snippet
```java
         }

         if (txDeleteCursorOnReload >= 0) {
            store.commit(txDeleteCursorOnReload);
         }
```

### ConstantValue
Condition `info != null` is always `true`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/cursor/impl/PageSubscriptionImpl.java`
#### Snippet
```java
         if (delivery != null) {
            PageCursorInfo info = PageSubscriptionImpl.this.getPageInfo(delivery.getPagedMessage().getPageNumber());
            if (info != null) {
               info.remove(delivery.getPagedMessage().getMessageNumber());
            }
```

### ConstantValue
Condition `info == null` is always `false`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/cursor/impl/PageSubscriptionImpl.java`
#### Snippet
```java

      // This could be null if the page file was removed
      if (info == null) {
         // This could become null if the page file was deleted, or if the queue was removed maybe?
         // it's better to diagnose it (based on support tickets) instead of NPE
```

### ConstantValue
Condition `purgeOnNoConsumers != that.purgeOnNoConsumers` is always `false`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/QueueConfig.java`
#### Snippet
```java
      if (delayBeforeDispatch != that.delayBeforeDispatch)
         return false;
      if (purgeOnNoConsumers != that.purgeOnNoConsumers)
         return false;
      if (groupRebalance != that.groupRebalance)
```

### ConstantValue
Value `autoCreated` is always 'true'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/QueueConfig.java`
#### Snippet
```java
         this.groupBuckets = ActiveMQDefaultConfiguration.getDefaultGroupBuckets();
         this.groupFirstKey = ActiveMQDefaultConfiguration.getDefaultGroupFirstKey();
         this.autoDelete = ActiveMQDefaultConfiguration.getDefaultQueueAutoDelete(autoCreated);
         this.autoDeleteDelay = ActiveMQDefaultConfiguration.getDefaultQueueAutoDeleteDelay();
         this.autoDeleteMessageCount = ActiveMQDefaultConfiguration.getDefaultQueueAutoDeleteMessageCount();
```

### ConstantValue
Value `dg` is always 'null'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/SharedNothingLiveActivation.java`
#### Snippet
```java

         if (dg == null) {
            throw ActiveMQMessageBundle.BUNDLE.noDiscoveryGroupFound(dg);
         }
         locator = (ServerLocatorInternal) ActiveMQClient.createServerLocatorWithHA(dg);
```

### ConstantValue
Condition `tx != null` is always `true`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerConsumerImpl.java`
#### Snippet
```java
         if (startedTransaction) {
            tx.rollback();
         } else if (tx != null) {
            tx.markAsRollbackOnly(e);
         }
```

### ConstantValue
Condition `tx != null` is always `true`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerConsumerImpl.java`
#### Snippet
```java
         if (startedTransaction) {
            tx.rollback();
         } else if (tx != null) {
            tx.markAsRollbackOnly(hqex);
         }
```

### ConstantValue
Condition `clientSessionFactory != null` is always `false`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/LiveOnlyActivation.java`
#### Snippet
```java
            } catch (Exception e) {
               logger.trace("Failed to connect to {}", scaleDownPolicy.getConnectors().get(0));
               if (clientSessionFactory != null) {
                  clientSessionFactory.close();
               }
```

### ConstantValue
Condition `clientSessionFactory != null` is always `false`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/LiveOnlyActivation.java`
#### Snippet
```java
               logger.trace("Failed to connect to {}", possibleLive.getA());
               nodeLocator.notifyRegistrationFailed(false);
               if (clientSessionFactory != null) {
                  clientSessionFactory.close();
               }
```

### ConstantValue
Value `failed` is always 'false'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java

               try {
                  rollback(failed, false);
               } catch (Exception e) {
                  ActiveMQServerLogger.LOGGER.unableToRollbackOnClose(e);
```

### ConstantValue
Value `theTx` is always 'null'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java
               throw new ActiveMQXAException(XAException.XA_HEURRB, "transaction has ben heuristically rolled back: " + xid);
            } else {
               logger.trace("xarollback into {}, xid={} forcing a rollback regular", theTx, xid);

               try {
```

### ConstantValue
Value `theTx` is always 'null'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java
               throw new ActiveMQXAException(XAException.XA_HEURRB, "transaction has been heuristically rolled back: " + xid);
            } else {
               logger.trace("XAcommit into {}, xid={} cannot find it", theTx, xid);

               throw new ActiveMQXAException(XAException.XAER_NOTA, "Cannot find xid in resource manager: " + xid);
```

### ConstantValue
Condition `addRecord` is always `true`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/group/impl/LocalGroupingHandler.java`
#### Snippet
```java
         }
         // Storing the record outside of any locks
         if (addRecord) {
            storageManager.addGrouping(groupBinding);
         }
```

### ConstantValue
Value `tx` is always 'null'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/plugin/impl/LoggingActiveMQServerPlugin.java`
#### Snippet
```java
            });
         } else {
            logSend(tx, message, result);
         }
      }
```

### ConstantValue
Value `tx` is always 'null'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/plugin/impl/LoggingActiveMQServerPlugin.java`
#### Snippet
```java
            });
         } else {
            logAck(tx, ref);
         }
      }
```

### ConstantValue
Value `dg` is always 'null'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/ha/ScaleDownPolicy.java`
#### Snippet
```java

         if (dg == null) {
            throw ActiveMQMessageBundle.BUNDLE.noDiscoveryGroupFound(dg);
         }
         return (ServerLocatorInternal) ActiveMQClient.createServerLocatorWithHA(dg);
```

### ConstantValue
Condition `factory == null` is always `false`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/impl/ClusterConnectionBridge.java`
#### Snippet
```java
      setSessionFactory(factory);

      if (factory == null) {
         return null;
      }
```

### ConstantValue
Value `scaleDownTargetNodeID` is always 'null'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/impl/BridgeImpl.java`
#### Snippet
```java
         fail(true, true);
      } else {
         logger.debug("Received invalid scaleDownTargetNodeID: {}", scaleDownTargetNodeID);

         fail(me.getType() == ActiveMQExceptionType.DISCONNECTED, false);
```

### ConstantValue
Condition `consistentTargets.size() > 0` is always `true`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/routing/policies/ConsistentHashPolicy.java`
#### Snippet
```java
         }

         if (consistentTargets.size() > 0) {
            Map.Entry<Integer, Target> consistentEntry = consistentTargets.floorEntry(getHash(key));

```

### ConstantValue
Condition `target != null` is always `true`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/impl/ConfigurationImpl.java`
#### Snippet
```java
               } else if (target instanceof Map) {
                  type = Object.class;
               } else if (target != null && target.getClass().isArray() && index >= 0) {
                  type = Array.get(target, index).getClass();
               } else {
```

### ConstantValue
Value `result` is always 'null'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/BasicAuthenticator.java`
#### Snippet
```java
      } else {
         if (AuditLogger.isResourceLoggingEnabled()) {
            AuditLogger.userFailedAuthenticationInAudit(result, null, null);
         }
         throw new SecurityException("Authentication failed");
```

### ConstantValue
Condition `servers != null` is always `true`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/MBeanServerFactory.java`
#### Snippet
```java
      if (this.locateExistingServerIfPossible) {
         List servers = javax.management.MBeanServerFactory.findMBeanServer(null);
         if (servers != null && servers.size() > 0) {
            this.server = (MBeanServer) servers.get(0);
         }
```

### ConstantValue
Condition `storageManager != null` is always `true`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/protocol/ServerPacketDecoder.java`
#### Snippet
```java
         Message message = ((MessagePacketI)packet).getMessage();
         if (message instanceof LargeServerMessage) {
            assert storageManager != null;
            ((LargeServerMessage) message).setStorageManager(storageManager);
         }
```

### ConstantValue
Value `synchronizationIsFinished` is always 'false'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/ReplicationStartSyncMessage.java`
#### Snippet
```java
   public ReplicationStartSyncMessage(boolean beforeTwoEighteen) {
      super(REPLICATION_START_FINISH_SYNC);
      this.beforeTwoEighteen = synchronizationIsFinished;
   }

```

### ConstantValue
Value `response` is always 'null'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/protocol/core/ServerSessionPacketHandler.java`
#### Snippet
```java
            session.requestProducerCredits(message.getAddress(), message.getCredits());
         } catch (ActiveMQIOErrorException e) {
            response = onActiveMQIOErrorExceptionWhileHandlePacket(packet, e, requiresResponse, response, this.session);
         } catch (ActiveMQXAException e) {
            response = onActiveMQXAExceptionWhileHandlePacket(packet, e, requiresResponse, response);
```

### ConstantValue
Value `response` is always 'null'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/protocol/core/ServerSessionPacketHandler.java`
#### Snippet
```java
            response = onActiveMQIOErrorExceptionWhileHandlePacket(packet, e, requiresResponse, response, this.session);
         } catch (ActiveMQXAException e) {
            response = onActiveMQXAExceptionWhileHandlePacket(packet, e, requiresResponse, response);
         } catch (ActiveMQQueueMaxConsumerLimitReached e) {
            response = onActiveMQQueueMaxConsumerLimitReachedWhileHandlePacket(packet, e, requiresResponse, response);
```

### ConstantValue
Value `response` is always 'null'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/protocol/core/ServerSessionPacketHandler.java`
#### Snippet
```java
            response = onActiveMQXAExceptionWhileHandlePacket(packet, e, requiresResponse, response);
         } catch (ActiveMQQueueMaxConsumerLimitReached e) {
            response = onActiveMQQueueMaxConsumerLimitReachedWhileHandlePacket(packet, e, requiresResponse, response);
         } catch (ActiveMQException e) {
            response = onActiveMQExceptionWhileHandlePacket(packet, e, requiresResponse, response);
```

### ConstantValue
Value `response` is always 'null'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/protocol/core/ServerSessionPacketHandler.java`
#### Snippet
```java
            response = onActiveMQQueueMaxConsumerLimitReachedWhileHandlePacket(packet, e, requiresResponse, response);
         } catch (ActiveMQException e) {
            response = onActiveMQExceptionWhileHandlePacket(packet, e, requiresResponse, response);
         } catch (Throwable t) {
            response = onCatchThrowableWhileHandlePacket(packet, t, requiresResponse, response, this.session);
```

### ConstantValue
Value `response` is always 'null'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/protocol/core/ServerSessionPacketHandler.java`
#### Snippet
```java
            response = onActiveMQExceptionWhileHandlePacket(packet, e, requiresResponse, response);
         } catch (Throwable t) {
            response = onCatchThrowableWhileHandlePacket(packet, t, requiresResponse, response, this.session);
         }
         sendResponse(packet, response, false, false);
```

### ConstantValue
Value `response` is always 'null'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/protocol/core/ServerSessionPacketHandler.java`
#### Snippet
```java
            session.receiveConsumerCredits(message.getConsumerID(), message.getCredits());
         } catch (ActiveMQIOErrorException e) {
            response = onActiveMQIOErrorExceptionWhileHandlePacket(packet, e, requiresResponse, response, this.session);
         } catch (ActiveMQXAException e) {
            response = onActiveMQXAExceptionWhileHandlePacket(packet, e, requiresResponse, response);
```

### ConstantValue
Value `response` is always 'null'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/protocol/core/ServerSessionPacketHandler.java`
#### Snippet
```java
            response = onActiveMQIOErrorExceptionWhileHandlePacket(packet, e, requiresResponse, response, this.session);
         } catch (ActiveMQXAException e) {
            response = onActiveMQXAExceptionWhileHandlePacket(packet, e, requiresResponse, response);
         } catch (ActiveMQQueueMaxConsumerLimitReached e) {
            response = onActiveMQQueueMaxConsumerLimitReachedWhileHandlePacket(packet, e, requiresResponse, response);
```

### ConstantValue
Value `response` is always 'null'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/protocol/core/ServerSessionPacketHandler.java`
#### Snippet
```java
            response = onActiveMQXAExceptionWhileHandlePacket(packet, e, requiresResponse, response);
         } catch (ActiveMQQueueMaxConsumerLimitReached e) {
            response = onActiveMQQueueMaxConsumerLimitReachedWhileHandlePacket(packet, e, requiresResponse, response);
         } catch (ActiveMQException e) {
            response = onActiveMQExceptionWhileHandlePacket(packet, e, requiresResponse, response);
```

### ConstantValue
Value `response` is always 'null'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/protocol/core/ServerSessionPacketHandler.java`
#### Snippet
```java
            response = onActiveMQQueueMaxConsumerLimitReachedWhileHandlePacket(packet, e, requiresResponse, response);
         } catch (ActiveMQException e) {
            response = onActiveMQExceptionWhileHandlePacket(packet, e, requiresResponse, response);
         } catch (Throwable t) {
            response = onCatchThrowableWhileHandlePacket(packet, t, requiresResponse, response, this.session);
```

### ConstantValue
Value `response` is always 'null'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/protocol/core/ServerSessionPacketHandler.java`
#### Snippet
```java
            response = onActiveMQExceptionWhileHandlePacket(packet, e, requiresResponse, response);
         } catch (Throwable t) {
            response = onCatchThrowableWhileHandlePacket(packet, t, requiresResponse, response, this.session);
         }
         sendResponse(packet, response, false, false);
```

### ConstantValue
Condition `response != null` is always `true` when reached
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/protocol/core/ServerSessionPacketHandler.java`
#### Snippet
```java
                                     final boolean closeChannel) {
      // don't confirm if the response is an exception
      if (confirmPacket != null && (response == null || (response != null && response.getType() != PacketImpl.EXCEPTION))) {
         channel.confirm(confirmPacket);

```

### ConstantValue
Condition `packet instanceof NullResponseMessage_V2` is always `false`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/protocol/core/ServerSessionPacketHandler.java`
#### Snippet
```java
         return;
      }
      if (packet instanceof NullResponseMessage_V2) {
         poolNullResponseV2.release((NullResponseMessage_V2) packet);
      }
```

### ConstantValue
Value `response` is always 'null'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/protocol/core/ServerSessionPacketHandler.java`
#### Snippet
```java
            }
         } catch (ActiveMQIOErrorException e) {
            response = onActiveMQIOErrorExceptionWhileHandlePacket(packet, e, requiresResponse, response, this.session);
         } catch (ActiveMQXAException e) {
            response = onActiveMQXAExceptionWhileHandlePacket(packet, e, requiresResponse, response);
```

### ConstantValue
Value `response` is always 'null'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/protocol/core/ServerSessionPacketHandler.java`
#### Snippet
```java
            response = onActiveMQIOErrorExceptionWhileHandlePacket(packet, e, requiresResponse, response, this.session);
         } catch (ActiveMQXAException e) {
            response = onActiveMQXAExceptionWhileHandlePacket(packet, e, requiresResponse, response);
         } catch (ActiveMQQueueMaxConsumerLimitReached e) {
            response = onActiveMQQueueMaxConsumerLimitReachedWhileHandlePacket(packet, e, requiresResponse, response);
```

### ConstantValue
Value `response` is always 'null'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/protocol/core/ServerSessionPacketHandler.java`
#### Snippet
```java
            response = onActiveMQXAExceptionWhileHandlePacket(packet, e, requiresResponse, response);
         } catch (ActiveMQQueueMaxConsumerLimitReached e) {
            response = onActiveMQQueueMaxConsumerLimitReachedWhileHandlePacket(packet, e, requiresResponse, response);
         } catch (ActiveMQException e) {
            response = onActiveMQExceptionWhileHandlePacket(packet, e, requiresResponse, response);
```

### ConstantValue
Value `response` is always 'null'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/protocol/core/ServerSessionPacketHandler.java`
#### Snippet
```java
            response = onActiveMQQueueMaxConsumerLimitReachedWhileHandlePacket(packet, e, requiresResponse, response);
         } catch (ActiveMQException e) {
            response = onActiveMQExceptionWhileHandlePacket(packet, e, requiresResponse, response);
         } catch (Throwable t) {
            response = onCatchThrowableWhileHandlePacket(packet, t, requiresResponse, response, this.session);
```

### ConstantValue
Value `response` is always 'null'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/protocol/core/ServerSessionPacketHandler.java`
#### Snippet
```java
            response = onActiveMQExceptionWhileHandlePacket(packet, e, requiresResponse, response);
         } catch (Throwable t) {
            response = onCatchThrowableWhileHandlePacket(packet, t, requiresResponse, response, this.session);
         }
         sendResponse(packet, response, false, false);
```

### ConstantValue
Value `response` is always 'null'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/protocol/core/ServerSessionPacketHandler.java`
#### Snippet
```java
            }
         } catch (ActiveMQIOErrorException e) {
            response = onActiveMQIOErrorExceptionWhileHandlePacket(packet, e, requiresResponse, response, this.session);
         } catch (ActiveMQXAException e) {
            response = onActiveMQXAExceptionWhileHandlePacket(packet, e, requiresResponse, response);
```

### ConstantValue
Value `response` is always 'null'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/protocol/core/ServerSessionPacketHandler.java`
#### Snippet
```java
            response = onActiveMQIOErrorExceptionWhileHandlePacket(packet, e, requiresResponse, response, this.session);
         } catch (ActiveMQXAException e) {
            response = onActiveMQXAExceptionWhileHandlePacket(packet, e, requiresResponse, response);
         } catch (ActiveMQQueueMaxConsumerLimitReached e) {
            response = onActiveMQQueueMaxConsumerLimitReachedWhileHandlePacket(packet, e, requiresResponse, response);
```

### ConstantValue
Value `response` is always 'null'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/protocol/core/ServerSessionPacketHandler.java`
#### Snippet
```java
            response = onActiveMQXAExceptionWhileHandlePacket(packet, e, requiresResponse, response);
         } catch (ActiveMQQueueMaxConsumerLimitReached e) {
            response = onActiveMQQueueMaxConsumerLimitReachedWhileHandlePacket(packet, e, requiresResponse, response);
         } catch (ActiveMQException e) {
            response = onActiveMQExceptionWhileHandlePacket(packet, e, requiresResponse, response);
```

### ConstantValue
Value `response` is always 'null'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/protocol/core/ServerSessionPacketHandler.java`
#### Snippet
```java
            response = onActiveMQQueueMaxConsumerLimitReachedWhileHandlePacket(packet, e, requiresResponse, response);
         } catch (ActiveMQException e) {
            response = onActiveMQExceptionWhileHandlePacket(packet, e, requiresResponse, response);
         } catch (Throwable t) {
            response = onCatchThrowableWhileHandlePacket(packet, t, requiresResponse, response, this.session);
```

### ConstantValue
Value `response` is always 'null'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/protocol/core/ServerSessionPacketHandler.java`
#### Snippet
```java
            response = onActiveMQExceptionWhileHandlePacket(packet, e, requiresResponse, response);
         } catch (Throwable t) {
            response = onCatchThrowableWhileHandlePacket(packet, t, requiresResponse, response, this.session);
         }
         sendResponse(packet, response, false, false);
```

### ConstantValue
Condition `control != null` is always `true`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/impl/ManagementServiceImpl.java`
#### Snippet
```java
      }
      //shouldnt ever be null
      if (control != null) {
         registerInJMX(objectName, control);
         registerInRegistry(ResourceNames.BROADCAST_GROUP + configuration.getName(), control);
```

### ConstantValue
Condition `!Mergeable.class.isAssignableFrom(actualMatch.getClass())` is always `false` when reached
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/HierarchicalObjectRepository.java`
#### Snippet
```java
      T actualMatch = null;
      for (Match<T> match : orderedMatches) {
         if (actualMatch == null || !Mergeable.class.isAssignableFrom(actualMatch.getClass())) {
            actualMatch = match.getValue();
            if (!Mergeable.class.isAssignableFrom(actualMatch.getClass())) {
```

### ConstantValue
Condition `boxedInt != null` is always `true`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/postoffice/impl/IntegerCache.java`
#### Snippet
```java
            cachedInts[index] = boxedInt;
         }
         assert boxedInt != null;
         return boxedInt;
      };
```

### ConstantValue
Condition `bindings != null` is always `true`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/postoffice/impl/CopyOnWriteBindings.java`
#### Snippet
```java
               return bindingsAndPosition;
            });
            assert bindings != null;
            if (bindings == newBindings) {
               return;
```

### ConstantValue
Condition `id.getB() != null` is always `true`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/postoffice/impl/PersistentDuplicateIDCache.java`
#### Snippet
```java
            continue;
         }
         assert id.getB() != null && id.getB().longValue() != NIL;
         if (toNotBeAdded > 0) {
            if (txID == -1) {
```

### ConstantValue
Condition `size >= 1` is always `true`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/OperationContextImpl.java`
#### Snippet
```java
         return;
      }
      assert size >= 1;
      // no need to use an iterator here, we can save that cost
      for (int i = 0; i < size; i++) {
```

### ConstantValue
Condition `directory == null` is always `false`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/deployers/impl/FileConfigurationParser.java`
#### Snippet
```java
         config.setJournalRetentionPeriod(unit, period);

         if (directory == null || directory.equals("")) {
            throw new IllegalArgumentException("journal-retention-directory=null");
         }
```

### ConstantValue
Condition `scaleDownConnectorNode != null` is always `true`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/deployers/impl/FileConfigurationParser.java`
#### Snippet
```java
         NodeList scaleDownConnectorNode = scaleDownElement.getElementsByTagName("connectors");

         if (scaleDownConnectorNode != null && scaleDownConnectorNode.getLength() > 0) {
            NodeList scaleDownConnectors = scaleDownConnectorNode.item(0).getChildNodes();
            for (int i = 0; i < scaleDownConnectors.getLength(); i++) {
```

### ConstantValue
Condition `ccAMQPConnections != null` is always `true`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/deployers/impl/FileConfigurationParser.java`
#### Snippet
```java
      NodeList ccAMQPConnections = e.getElementsByTagName("broker-connections");

      if (ccAMQPConnections != null) {
         NodeList ccAMQConnectionsURI = e.getElementsByTagName("amqp-connection");

```

### ConstantValue
Condition `ccAMQConnectionsURI != null` is always `true`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/deployers/impl/FileConfigurationParser.java`
#### Snippet
```java
         NodeList ccAMQConnectionsURI = e.getElementsByTagName("amqp-connection");

         if (ccAMQConnectionsURI != null) {
            for (int i = 0; i < ccAMQConnectionsURI.getLength(); i++) {
               Element ccNode = (Element) ccAMQConnectionsURI.item(i);
```

### ConstantValue
Condition `ccConnectionRouters != null` is always `true`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/deployers/impl/FileConfigurationParser.java`
#### Snippet
```java
      NodeList ccConnectionRouters = e.getElementsByTagName("connection-routers");

      if (ccConnectionRouters != null) {
         NodeList ccConnectionRouter = e.getElementsByTagName("connection-router");

```

### ConstantValue
Condition `ccConnectionRouter != null` is always `true`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/deployers/impl/FileConfigurationParser.java`
#### Snippet
```java
         NodeList ccConnectionRouter = e.getElementsByTagName("connection-router");

         if (ccConnectionRouter != null) {
            for (int i = 0; i < ccConnectionRouter.getLength(); i++) {
               Element ccNode = (Element) ccConnectionRouter.item(i);
```

### ConstantValue
Condition `remoteConnectorNode != null` is always `true`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/deployers/impl/FileConfigurationParser.java`
#### Snippet
```java
      NodeList remoteConnectorNode = policyNode.getElementsByTagName("excludes");

      if (remoteConnectorNode != null && remoteConnectorNode.getLength() > 0) {
         NodeList remoteConnectors = remoteConnectorNode.item(0).getChildNodes();
         for (int i = 0; i < remoteConnectors.getLength(); i++) {
```

### ConstantValue
Condition `passwordTextFederation != null` is always `true`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/deployers/impl/FileConfigurationParser.java`
#### Snippet
```java
      String passwordTextFederation = fedNode.getAttribute("password");

      if (passwordTextFederation != null && !passwordTextFederation.isEmpty()) {
         String resolvedPassword = PasswordMaskingUtil.resolveMask(mainConfig.isMaskPassword(), passwordTextFederation, mainConfig.getPasswordCodec());
         credentials.setPassword(resolvedPassword);
```

### ConstantValue
Condition `passwordTextFederation != null` is always `true`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/deployers/impl/FileConfigurationParser.java`
#### Snippet
```java
      String passwordTextFederation = upstreamNode.getAttribute("password");

      if (passwordTextFederation != null && !passwordTextFederation.isEmpty()) {
         String resolvedPassword = PasswordMaskingUtil.resolveMask(mainConfig.isMaskPassword(), passwordTextFederation, mainConfig.getPasswordCodec());
         config.getConnectionConfiguration().setPassword(resolvedPassword);
```

### ConstantValue
Condition `metadata != null` is always `true`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/codec/PersistentQueueBindingEncoding.java`
#### Snippet
```java

      String metadata = buffer.readNullableSimpleString().toString();
      if (metadata != null) {
         String[] elements = metadata.split(";");
         for (String element : elements) {
```

### ConstantValue
Condition `!rejectDuplicates` is always `true`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/postoffice/impl/PostOfficeImpl.java`
#### Snippet
```java
      }
      if (isDuplicate) {
         assert !rejectDuplicates;
         return DuplicateCheckResult.NoDuplicateNotStartedTX;
      }
```

### ConstantValue
Value `rejectDuplicates` is always 'false'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/postoffice/impl/PostOfficeImpl.java`
#### Snippet
```java
      }
      if (isDuplicate) {
         assert !rejectDuplicates;
         return DuplicateCheckResult.NoDuplicateNotStartedTX;
      }
```

### ConstantValue
Condition `server != null` is always `true`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/replication/ReplicationManager.java`
#### Snippet
```java
               ActiveMQReplicationTimeooutException exception = ActiveMQMessageBundle.BUNDLE.replicationSynchronizationTimeout(initialReplicationSyncTimeout);

               if (server != null) {
                  try {
                     ClusterManager clusterManager = server.getClusterManager();
```

### ConstantValue
Condition `attribute != null` is always `true`
in `artemis-server/src/main/java/org/apache/activemq/artemis/utils/XMLConfigurationUtil.java`
#### Snippet
```java
                                          final Validators.Validator validator) {
      String attribute = e.getAttribute(name);
      if (attribute != null && !attribute.equals("")) {
         int val = XMLUtil.parseInt(e, attribute);
         validator.validate(name, val);
```

### ConstantValue
Condition `attributeValue == null` is always `false`
in `artemis-server/src/main/java/org/apache/activemq/artemis/utils/XMLConfigurationUtil.java`
#### Snippet
```java
   public static final Boolean getBooleanAttribute(final Element e, final String name, final Boolean def) {
      String attributeValue = e.getAttribute(name);
      if (attributeValue == null || attributeValue.isEmpty()) {
         return def;
      } else {
```

### ConstantValue
Condition `page != null` is always `true`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/replication/ReplicationEndpoint.java`
#### Snippet
```java
      }

      if (page != null) {
         if (packet.isDelete()) {
            if (deletePages) {
```

### ConstantValue
Value `lineUpContext` is always 'false'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/AbstractJournalStorageManager.java`
#### Snippet
```java
            if (logger.isTraceEnabled()) {
               logger.trace("calling getContext(true).done() for txID={}, lineupContext={} syncTransactional={}... forcing call on getContext(true).done",
                  txID, lineUpContext, syncTransactional);
            }
            /**
```

### ConstantValue
Value `syncTransactional` is always 'false'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/AbstractJournalStorageManager.java`
#### Snippet
```java
            if (logger.isTraceEnabled()) {
               logger.trace("calling getContext(true).done() for txID={}, lineupContext={} syncTransactional={}... forcing call on getContext(true).done",
                  txID, lineUpContext, syncTransactional);
            }
            /**
```

### ConstantValue
Condition `!internalQueue` is always `true`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java
      }

      if (!internalQueue && reference.isDurable() && isDurable() && !reference.isPaged()) {
         storageManager.updateDeliveryCount(reference);
      }
```

### ConstantValue
Value `internalQueue` is always 'false'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java
      }

      if (!internalQueue && reference.isDurable() && isDurable() && !reference.isPaged()) {
         storageManager.updateDeliveryCount(reference);
      }
```

### ConstantValue
Value `tx` is always 'null'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java
               });
            } else {
               auditLogAck(subject, remoteAddress, ref, tx);
            }
         }
```

### ConstantValue
Condition `originalMessageQueue != null` is always `true`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java

               Long targetQueue = null;
               if (originalMessageQueue != null && !originalMessageQueue.equals(originalMessageAddress)) {
                  targetQueue = queues.get(originalMessageQueue);
                  if (targetQueue == null) {
```

## RuleId[id=MethodOverridesStaticMethod]
### MethodOverridesStaticMethod
Method `createMessage()` tries to override a static method of a superclass
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/compatible1X/ActiveMQCompatibleMessage.java`
#### Snippet
```java
   }

   public static ActiveMQMessage createMessage(final ClientMessage message,
                                               final ClientSession session,
                                               final ConnectionFactoryOptions options) {
```

### MethodOverridesStaticMethod
Method `getInstance()` tries to override a static method of a superclass
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessagePersisterV2.java`
#### Snippet
```java
   public static AMQPMessagePersisterV2 theInstance;

   public static AMQPMessagePersisterV2 getInstance() {
      if (theInstance == null) {
         theInstance = new AMQPMessagePersisterV2();
```

### MethodOverridesStaticMethod
Method `getInstance()` tries to override a static method of a superclass
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessagePersister.java`
#### Snippet
```java
   public static AMQPMessagePersister theInstance;

   public static AMQPMessagePersister getInstance() {
      if (theInstance == null) {
         theInstance = new AMQPMessagePersister();
```

### MethodOverridesStaticMethod
Method `getInstance()` tries to override a static method of a superclass
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPLargeMessagePersister.java`
#### Snippet
```java
   public static AMQPLargeMessagePersister theInstance;

   public static AMQPLargeMessagePersister getInstance() {
      if (theInstance == null) {
         theInstance = new AMQPLargeMessagePersister();
```

### MethodOverridesStaticMethod
Method `getInstance()` tries to override a static method of a superclass
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessagePersisterV3.java`
#### Snippet
```java
   public static AMQPMessagePersisterV3 theInstance;

   public static AMQPMessagePersisterV3 getInstance() {
      if (theInstance == null) {
         theInstance = new AMQPMessagePersisterV3();
```

## RuleId[id=StringConcatenationInsideStringBufferAppend]
### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-maven-plugin/src/main/java/org/apache/activemq/artemis/maven/ArtemisCreatePlugin.java`
#### Snippet
```java
   private String getCommandline(ArrayList<String> listCommands) {
      StringBuffer buffer = new StringBuffer();
      buffer.append(home.getAbsolutePath() + "/bin/artemis ");
      for (String string : listCommands) {
         buffer.append(string + " ");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-maven-plugin/src/main/java/org/apache/activemq/artemis/maven/ArtemisCreatePlugin.java`
#### Snippet
```java
      buffer.append(home.getAbsolutePath() + "/bin/artemis ");
      for (String string : listCommands) {
         buffer.append(string + " ");
      }
      return buffer.toString();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/LogicExpression.java`
#### Snippet
```java
      for (BooleanExpression expression : expressions) {
         if (count++ > 0) {
            result.append(" " + getExpressionSymbol() + " ");
         }
         result.append(expression.toString());
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `artemis-service-extensions/src/main/java/org/apache/activemq/artemis/service/extensions/xa/recovery/XARecoveryConfig.java`
#### Snippet
```java
      StringBuilder builder = new StringBuilder();

      builder.append("XARecoveryConfig [transportConfiguration=" + Arrays.toString(transportConfiguration));
      builder.append(", discoveryConfiguration=" + discoveryConfiguration);
      builder.append(", username=" + username);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `artemis-service-extensions/src/main/java/org/apache/activemq/artemis/service/extensions/xa/recovery/XARecoveryConfig.java`
#### Snippet
```java

      builder.append("XARecoveryConfig [transportConfiguration=" + Arrays.toString(transportConfiguration));
      builder.append(", discoveryConfiguration=" + discoveryConfiguration);
      builder.append(", username=" + username);
      builder.append(", password=****");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `artemis-service-extensions/src/main/java/org/apache/activemq/artemis/service/extensions/xa/recovery/XARecoveryConfig.java`
#### Snippet
```java
      builder.append("XARecoveryConfig [transportConfiguration=" + Arrays.toString(transportConfiguration));
      builder.append(", discoveryConfiguration=" + discoveryConfiguration);
      builder.append(", username=" + username);
      builder.append(", password=****");

```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `artemis-service-extensions/src/main/java/org/apache/activemq/artemis/service/extensions/xa/recovery/XARecoveryConfig.java`
#### Snippet
```java

      for (Map.Entry<String, String> entry : properties.entrySet()) {
         builder.append(", " + entry.getKey() + "=" + entry.getValue());
      }
      builder.append("]");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/SpawnedVMSupport.java`
#### Snippet
```java
            for (Process alive : aliveProcess) {
               alive.destroyForcibly();
               buffer.append(startedProcesses.get(alive) + " ");
            }
            return false;
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/ThreadDumpUtil.java`
#### Snippet
```java
            threadInfo.getThreadState());
      if (threadInfo.getLockName() != null) {
         sb.append(" on " + threadInfo.getLockName());
      }
      if (threadInfo.getLockOwnerName() != null) {
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/ThreadDumpUtil.java`
#### Snippet
```java
      }
      if (threadInfo.getLockOwnerName() != null) {
         sb.append(" owned by \"" + threadInfo.getLockOwnerName() +
               "\" Id=" + threadInfo.getLockOwnerId());
      }
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/ThreadDumpUtil.java`
#### Snippet
```java
      for (; i < threadInfo.getStackTrace().length; i++) {
         StackTraceElement ste = threadInfo.getStackTrace()[i];
         sb.append("\tat " + ste.toString());
         sb.append('\n');
         if (i == 0 && threadInfo.getLockInfo() != null) {
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/ThreadDumpUtil.java`
#### Snippet
```java
            switch (ts) {
               case BLOCKED:
                  sb.append("\t-  blocked on " + threadInfo.getLockInfo());
                  sb.append('\n');
                  break;
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/ThreadDumpUtil.java`
#### Snippet
```java
                  break;
               case WAITING:
                  sb.append("\t-  waiting on " + threadInfo.getLockInfo());
                  sb.append('\n');
                  break;
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/ThreadDumpUtil.java`
#### Snippet
```java
                  break;
               case TIMED_WAITING:
                  sb.append("\t-  waiting on " + threadInfo.getLockInfo());
                  sb.append('\n');
                  break;
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/ThreadDumpUtil.java`
#### Snippet
```java
         for (MonitorInfo mi : threadInfo.getLockedMonitors()) {
            if (mi.getLockedStackDepth() == i) {
               sb.append("\t-  locked " + mi);
               sb.append('\n');
            }
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/ThreadDumpUtil.java`
#### Snippet
```java
      LockInfo[] locks = threadInfo.getLockedSynchronizers();
      if (locks.length > 0) {
         sb.append("\n\tNumber of locked synchronizers = " + locks.length);
         sb.append('\n');
         for (LockInfo li : locks) {
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/ThreadDumpUtil.java`
#### Snippet
```java
         sb.append('\n');
         for (LockInfo li : locks) {
            sb.append("\t- " + li);
            sb.append('\n');
         }
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/ByteUtil.java`
#### Snippet
```java
               buffer.append(" ");
            }
            buffer.append(Integer.toString(i) + " */ \"");
         } else if ((i + groupSize) % groupSize == 0 && str.length() - i > groupSize) {
            buffer.append("\" + \"");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/uri/URISchema.java`
#### Snippet
```java
      StringBuffer buffer = new StringBuffer();
      for (Map.Entry<String, String> entry : query.entrySet()) {
         buffer.append(entry.getKey() + "=" + entry.getValue());
         buffer.append("\n");
      }
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalRecord.java`
#### Snippet
```java
   public String toString() {
      StringBuilder buffer = new StringBuilder();
      buffer.append("JournalRecord(add=" + addFile.getFile().getFileName());

      final ObjIntIntArrayList<JournalFile> fileUpdates = this.fileUpdates;
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalFileImpl.java`
#### Snippet
```java

      for (Entry<JournalFile, AtomicInteger> entry : negCounts.entrySet()) {
         builder.append(" file = " + entry.getKey() + " negcount value = " + entry.getValue() + "\n");
      }

```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/TypedProperties.java`
#### Snippet
```java
         while (iter.hasNext()) {
            Entry<SimpleString, PropertyValue> iterItem = iter.next();
            sb.append(iterItem.getKey() + "=");

            // it seems weird but it's right!!
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/TypedProperties.java`
#### Snippet
```java
               sb.append("NULL-value");
            } else if (theValue instanceof byte[]) {
               sb.append("[" + ByteUtil.maxString(ByteUtil.bytesToHex((byte[]) theValue, 2), 150) + ")");

               if (iterItem.getKey().toString().startsWith("_AMQ_ROUTE_TO")) {
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/TypedProperties.java`
#### Snippet
```java
                     }
                  } catch (Throwable e) {
                     sb.append("error-converting-longs=" + e.getMessage());
                  }
                  sb.append("]");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalFilesRepository.java`
#### Snippet
```java
      StringBuilder buffer = new StringBuilder();

      buffer.append("**********\nCurrent File = " + journal.getCurrentFile() + "\n");
      buffer.append("**********\nDataFiles:\n");
      for (JournalFile file : dataFiles) {
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalFilesRepository.java`
#### Snippet
```java
      buffer.append("**********\nDataFiles:\n");
      for (JournalFile file : dataFiles) {
         buffer.append(file.toString() + "\n");
      }
      buffer.append("*********\nFreeFiles:\n");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalFilesRepository.java`
#### Snippet
```java
      buffer.append("*********\nFreeFiles:\n");
      for (JournalFile file : freeFiles) {
         buffer.append(file.toString() + "\n");
      }
      return buffer.toString();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTUtil.java`
#### Snippet
```java

            if (message.variableHeader() instanceof MqttMessageIdVariableHeader) {
               log.append("(" + ((MqttMessageIdVariableHeader) message.variableHeader()).messageId() + ")");
            }

```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTUtil.java`
#### Snippet
```java
                     topicName = "<empty>";
                  }
                  log.append("(" + publishHeader.packetId() + ")")
                     .append(" topic=" + topicName)
                     .append(", qos=" + message.fixedHeader().qosLevel().value())
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTUtil.java`
#### Snippet
```java
                  }
                  log.append("(" + publishHeader.packetId() + ")")
                     .append(" topic=" + topicName)
                     .append(", qos=" + message.fixedHeader().qosLevel().value())
                     .append(", retain=" + message.fixedHeader().isRetain())
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTUtil.java`
#### Snippet
```java
                  log.append("(" + publishHeader.packetId() + ")")
                     .append(" topic=" + topicName)
                     .append(", qos=" + message.fixedHeader().qosLevel().value())
                     .append(", retain=" + message.fixedHeader().isRetain())
                     .append(", dup=" + message.fixedHeader().isDup())
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTUtil.java`
#### Snippet
```java
                     .append(" topic=" + topicName)
                     .append(", qos=" + message.fixedHeader().qosLevel().value())
                     .append(", retain=" + message.fixedHeader().isRetain())
                     .append(", dup=" + message.fixedHeader().isDup())
                     .append(", remainingLength=" + message.fixedHeader().remainingLength());
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTUtil.java`
#### Snippet
```java
                     .append(", qos=" + message.fixedHeader().qosLevel().value())
                     .append(", retain=" + message.fixedHeader().isRetain())
                     .append(", dup=" + message.fixedHeader().isDup())
                     .append(", remainingLength=" + message.fixedHeader().remainingLength());
                  for (MqttProperties.MqttProperty property : ((MqttPublishMessage)message).variableHeader().properties().listAll()) {
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTUtil.java`
#### Snippet
```java
                     .append(", retain=" + message.fixedHeader().isRetain())
                     .append(", dup=" + message.fixedHeader().isDup())
                     .append(", remainingLength=" + message.fixedHeader().remainingLength());
                  for (MqttProperties.MqttProperty property : ((MqttPublishMessage)message).variableHeader().properties().listAll()) {
                     Object value = property.value();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTUtil.java`
#### Snippet
```java
                        }
                     }
                     log.append(", " + formatCase(MqttPropertyType.valueOf(property.propertyId()).name()) + "=" + value);
                  }
                  log.append(", payload=" + getPayloadForLogging((MqttPublishMessage) message, 256));
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTUtil.java`
#### Snippet
```java
                     log.append(", " + formatCase(MqttPropertyType.valueOf(property.propertyId()).name()) + "=" + value);
                  }
                  log.append(", payload=" + getPayloadForLogging((MqttPublishMessage) message, 256));
                  break;
               case CONNECT:
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTUtil.java`
#### Snippet
```java
                  }
                  for (MqttProperties.MqttProperty property : connectHeader.properties().listAll()) {
                     log.append(", " + formatCase(MqttPropertyType.valueOf(property.propertyId()).name()) + "=" + property.value());
                  }
                  break;
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTUtil.java`
#### Snippet
```java
                     .append(", sessionPresent=").append(connackHeader.isSessionPresent());
                  for (MqttProperties.MqttProperty property : connackHeader.properties().listAll()) {
                     log.append(", " + formatCase(MqttPropertyType.valueOf(property.propertyId()).name()) + "=" + property.value());
                  }
                  break;
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTUtil.java`
#### Snippet
```java
               case SUBACK:
                  for (Integer qos : ((MqttSubAckMessage) message).payload().grantedQoSLevels()) {
                     log.append("\n\t" + qos);
                  }
                  break;
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTUtil.java`
#### Snippet
```java
               case UNSUBSCRIBE:
                  for (String topic : ((MqttUnsubscribeMessage) message).payload().topics()) {
                     log.append("\n\t" + topic);
                  }
                  break;
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalImpl.java`
#### Snippet
```java

      for (JournalFile file : filesRepository.getDataFiles()) {
         builder.append("DataFile:" + file +
                           " posCounter = " +
                           file.getPosCount() +
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalImpl.java`
#### Snippet
```java

      for (JournalFile file : filesRepository.getFreeFiles()) {
         builder.append("FreeFile:" + file + "\n");
      }

```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalImpl.java`
#### Snippet
```java

      if (currentFile != null) {
         builder.append("CurrentFile:" + currentFile + " posCounter = " + currentFile.getPosCount() + "\n");

         if (currentFile instanceof JournalFileImpl) {
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQMessage.java`
#### Snippet
```java
         sb.append("]:");
         sb.append(message.isDurable() ? "PERSISTENT" : "NON-PERSISTENT");
         sb.append("/" + message.toString());
      } else {
         sb.append("]");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/TransportConfiguration.java`
#### Snippet
```java
   public String toString() {
      StringBuilder str = new StringBuilder(TransportConfiguration.class.getSimpleName());
      str.append("(name=" + name + ", ");
      str.append("factory=" + (factoryClassName == null ? "null" : replaceWildcardChars(factoryClassName)));
      str.append(") ");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/TransportConfiguration.java`
#### Snippet
```java
      StringBuilder str = new StringBuilder(TransportConfiguration.class.getSimpleName());
      str.append("(name=" + name + ", ");
      str.append("factory=" + (factoryClassName == null ? "null" : replaceWildcardChars(factoryClassName)));
      str.append(") ");
      str.append(toStringParameters(params, extraProps));
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ClientSessionImpl.java`
#### Snippet
```java
      synchronized (metadata) {
         for (Map.Entry<String, String> entry : metadata.entrySet()) {
            buffer.append(entry.getKey() + "=" + entry.getValue() + ",");
         }
      }
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionDeleteQueueMessage.java`
#### Snippet
```java
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", queueName=" + queueName);
      return buff.toString();
   }
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionSendContinuationMessage_V2.java`
#### Snippet
```java
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", continues=" + continues);
      buff.append(", message=" + message);
      buff.append(", messageBodySize=" + messageBodySize);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionSendContinuationMessage_V2.java`
#### Snippet
```java
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", continues=" + continues);
      buff.append(", message=" + message);
      buff.append(", messageBodySize=" + messageBodySize);
      buff.append(", requiresResponse=" + requiresResponse);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionSendContinuationMessage_V2.java`
#### Snippet
```java
      buff.append(", continues=" + continues);
      buff.append(", message=" + message);
      buff.append(", messageBodySize=" + messageBodySize);
      buff.append(", requiresResponse=" + requiresResponse);
      buff.append(", correlationID=" + correlationID);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionSendContinuationMessage_V2.java`
#### Snippet
```java
      buff.append(", message=" + message);
      buff.append(", messageBodySize=" + messageBodySize);
      buff.append(", requiresResponse=" + requiresResponse);
      buff.append(", correlationID=" + correlationID);
      buff.append("]");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionSendContinuationMessage_V2.java`
#### Snippet
```java
      buff.append(", messageBodySize=" + messageBodySize);
      buff.append(", requiresResponse=" + requiresResponse);
      buff.append(", correlationID=" + correlationID);
      buff.append("]");
      return buff.toString();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionXAResponseMessage_V2.java`
#### Snippet
```java
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", error=" + error);
      buff.append(", message=" + message);
      buff.append(", responseCode=" + responseCode);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionXAResponseMessage_V2.java`
#### Snippet
```java
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", error=" + error);
      buff.append(", message=" + message);
      buff.append(", responseCode=" + responseCode);
      buff.append(", correlationID=" + correlationID);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionXAResponseMessage_V2.java`
#### Snippet
```java
      buff.append(", error=" + error);
      buff.append(", message=" + message);
      buff.append(", responseCode=" + responseCode);
      buff.append(", correlationID=" + correlationID);
      return buff.toString();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionXAResponseMessage_V2.java`
#### Snippet
```java
      buff.append(", message=" + message);
      buff.append(", responseCode=" + responseCode);
      buff.append(", correlationID=" + correlationID);
      return buff.toString();
   }
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionXASetTimeoutResponseMessage.java`
#### Snippet
```java
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", ok=" + ok);
      return buff.toString();
   }
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionForceConsumerDelivery.java`
#### Snippet
```java
   protected String getPacketString() {
      StringBuffer buf = new StringBuffer(super.getPacketString());
      buf.append(", consumerID=" + consumerID);
      buf.append(", sequence=" + sequence);
      return buf.toString();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionForceConsumerDelivery.java`
#### Snippet
```java
      StringBuffer buf = new StringBuffer(super.getPacketString());
      buf.append(", consumerID=" + consumerID);
      buf.append(", sequence=" + sequence);
      return buf.toString();
   }
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/CheckFailoverReplyMessage.java`
#### Snippet
```java
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", okToFailover=" + okToFailover);
      return buff.toString();
   }
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionReceiveLargeMessage.java`
#### Snippet
```java
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", consumerID=" + consumerID);
      buff.append(", deliveryCount=" + deliveryCount);
      buff.append(", largeMessageSize=" + largeMessageSize);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionReceiveLargeMessage.java`
#### Snippet
```java
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", consumerID=" + consumerID);
      buff.append(", deliveryCount=" + deliveryCount);
      buff.append(", largeMessageSize=" + largeMessageSize);
      buff.append(", message=" + message);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionReceiveLargeMessage.java`
#### Snippet
```java
      buff.append(", consumerID=" + consumerID);
      buff.append(", deliveryCount=" + deliveryCount);
      buff.append(", largeMessageSize=" + largeMessageSize);
      buff.append(", message=" + message);
      return buff.toString();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionReceiveLargeMessage.java`
#### Snippet
```java
      buff.append(", deliveryCount=" + deliveryCount);
      buff.append(", largeMessageSize=" + largeMessageSize);
      buff.append(", message=" + message);
      return buff.toString();
   }
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/CreateSessionResponseMessage.java`
#### Snippet
```java
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", serverVersion=" + serverVersion);
      return buff.toString();
   }
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/DisconnectMessage_V2.java`
#### Snippet
```java
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", nodeID=" + nodeID);
      buff.append(", scaleDownNodeID=" + scaleDownNodeID);
      return buff.toString();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/DisconnectMessage_V2.java`
#### Snippet
```java
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", nodeID=" + nodeID);
      buff.append(", scaleDownNodeID=" + scaleDownNodeID);
      return buff.toString();
   }
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionSendLargeMessage.java`
#### Snippet
```java
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", largeMessage=" + largeMessage);
      return buff.toString();
   }
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionQueueQueryResponseMessage_V3.java`
#### Snippet
```java
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", autoCreated=" + autoCreated);
      buff.append(", purgeOnNoConsumers=" + purgeOnNoConsumers);
      buff.append(", routingType=" + routingType);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionQueueQueryResponseMessage_V3.java`
#### Snippet
```java
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", autoCreated=" + autoCreated);
      buff.append(", purgeOnNoConsumers=" + purgeOnNoConsumers);
      buff.append(", routingType=" + routingType);
      buff.append(", maxConsumers=" + maxConsumers);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionQueueQueryResponseMessage_V3.java`
#### Snippet
```java
      buff.append(", autoCreated=" + autoCreated);
      buff.append(", purgeOnNoConsumers=" + purgeOnNoConsumers);
      buff.append(", routingType=" + routingType);
      buff.append(", maxConsumers=" + maxConsumers);
      buff.append(", exclusive=" + exclusive);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionQueueQueryResponseMessage_V3.java`
#### Snippet
```java
      buff.append(", purgeOnNoConsumers=" + purgeOnNoConsumers);
      buff.append(", routingType=" + routingType);
      buff.append(", maxConsumers=" + maxConsumers);
      buff.append(", exclusive=" + exclusive);
      buff.append(", groupRebalance=" + groupRebalance);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionQueueQueryResponseMessage_V3.java`
#### Snippet
```java
      buff.append(", routingType=" + routingType);
      buff.append(", maxConsumers=" + maxConsumers);
      buff.append(", exclusive=" + exclusive);
      buff.append(", groupRebalance=" + groupRebalance);
      buff.append(", groupRebalancePauseDispatch=" + groupRebalancePauseDispatch);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionQueueQueryResponseMessage_V3.java`
#### Snippet
```java
      buff.append(", maxConsumers=" + maxConsumers);
      buff.append(", exclusive=" + exclusive);
      buff.append(", groupRebalance=" + groupRebalance);
      buff.append(", groupRebalancePauseDispatch=" + groupRebalancePauseDispatch);
      buff.append(", groupBuckets=" + groupBuckets);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionQueueQueryResponseMessage_V3.java`
#### Snippet
```java
      buff.append(", exclusive=" + exclusive);
      buff.append(", groupRebalance=" + groupRebalance);
      buff.append(", groupRebalancePauseDispatch=" + groupRebalancePauseDispatch);
      buff.append(", groupBuckets=" + groupBuckets);
      buff.append(", groupFirstKey=" + groupFirstKey);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionQueueQueryResponseMessage_V3.java`
#### Snippet
```java
      buff.append(", groupRebalance=" + groupRebalance);
      buff.append(", groupRebalancePauseDispatch=" + groupRebalancePauseDispatch);
      buff.append(", groupBuckets=" + groupBuckets);
      buff.append(", groupFirstKey=" + groupFirstKey);
      buff.append(", lastValue=" + lastValue);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionQueueQueryResponseMessage_V3.java`
#### Snippet
```java
      buff.append(", groupRebalancePauseDispatch=" + groupRebalancePauseDispatch);
      buff.append(", groupBuckets=" + groupBuckets);
      buff.append(", groupFirstKey=" + groupFirstKey);
      buff.append(", lastValue=" + lastValue);
      buff.append(", lastValueKey=" + lastValueKey);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionQueueQueryResponseMessage_V3.java`
#### Snippet
```java
      buff.append(", groupBuckets=" + groupBuckets);
      buff.append(", groupFirstKey=" + groupFirstKey);
      buff.append(", lastValue=" + lastValue);
      buff.append(", lastValueKey=" + lastValueKey);
      buff.append(", nonDestructive=" + nonDestructive);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionQueueQueryResponseMessage_V3.java`
#### Snippet
```java
      buff.append(", groupFirstKey=" + groupFirstKey);
      buff.append(", lastValue=" + lastValue);
      buff.append(", lastValueKey=" + lastValueKey);
      buff.append(", nonDestructive=" + nonDestructive);
      buff.append(", consumersBeforeDispatch=" + consumersBeforeDispatch);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionQueueQueryResponseMessage_V3.java`
#### Snippet
```java
      buff.append(", lastValue=" + lastValue);
      buff.append(", lastValueKey=" + lastValueKey);
      buff.append(", nonDestructive=" + nonDestructive);
      buff.append(", consumersBeforeDispatch=" + consumersBeforeDispatch);
      buff.append(", delayBeforeDispatch=" + delayBeforeDispatch);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionQueueQueryResponseMessage_V3.java`
#### Snippet
```java
      buff.append(", lastValueKey=" + lastValueKey);
      buff.append(", nonDestructive=" + nonDestructive);
      buff.append(", consumersBeforeDispatch=" + consumersBeforeDispatch);
      buff.append(", delayBeforeDispatch=" + delayBeforeDispatch);
      buff.append(", autoDelete=" + autoDelete);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionQueueQueryResponseMessage_V3.java`
#### Snippet
```java
      buff.append(", nonDestructive=" + nonDestructive);
      buff.append(", consumersBeforeDispatch=" + consumersBeforeDispatch);
      buff.append(", delayBeforeDispatch=" + delayBeforeDispatch);
      buff.append(", autoDelete=" + autoDelete);
      buff.append(", autoDeleteDelay=" + autoDeleteDelay);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionQueueQueryResponseMessage_V3.java`
#### Snippet
```java
      buff.append(", consumersBeforeDispatch=" + consumersBeforeDispatch);
      buff.append(", delayBeforeDispatch=" + delayBeforeDispatch);
      buff.append(", autoDelete=" + autoDelete);
      buff.append(", autoDeleteDelay=" + autoDeleteDelay);
      buff.append(", autoDeleteMessageCount=" + autoDeleteMessageCount);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionQueueQueryResponseMessage_V3.java`
#### Snippet
```java
      buff.append(", delayBeforeDispatch=" + delayBeforeDispatch);
      buff.append(", autoDelete=" + autoDelete);
      buff.append(", autoDeleteDelay=" + autoDeleteDelay);
      buff.append(", autoDeleteMessageCount=" + autoDeleteMessageCount);
      buff.append(", defaultConsumerWindowSize=" + defaultConsumerWindowSize);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionQueueQueryResponseMessage_V3.java`
#### Snippet
```java
      buff.append(", autoDelete=" + autoDelete);
      buff.append(", autoDeleteDelay=" + autoDeleteDelay);
      buff.append(", autoDeleteMessageCount=" + autoDeleteMessageCount);
      buff.append(", defaultConsumerWindowSize=" + defaultConsumerWindowSize);
      buff.append(", ringSize=" + ringSize);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionQueueQueryResponseMessage_V3.java`
#### Snippet
```java
      buff.append(", autoDeleteDelay=" + autoDeleteDelay);
      buff.append(", autoDeleteMessageCount=" + autoDeleteMessageCount);
      buff.append(", defaultConsumerWindowSize=" + defaultConsumerWindowSize);
      buff.append(", ringSize=" + ringSize);
      buff.append(", enabled=" + enabled);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionQueueQueryResponseMessage_V3.java`
#### Snippet
```java
      buff.append(", autoDeleteMessageCount=" + autoDeleteMessageCount);
      buff.append(", defaultConsumerWindowSize=" + defaultConsumerWindowSize);
      buff.append(", ringSize=" + ringSize);
      buff.append(", enabled=" + enabled);
      buff.append(", configurationManaged=" + configurationManaged);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionQueueQueryResponseMessage_V3.java`
#### Snippet
```java
      buff.append(", defaultConsumerWindowSize=" + defaultConsumerWindowSize);
      buff.append(", ringSize=" + ringSize);
      buff.append(", enabled=" + enabled);
      buff.append(", configurationManaged=" + configurationManaged);
      return buff.toString();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionQueueQueryResponseMessage_V3.java`
#### Snippet
```java
      buff.append(", ringSize=" + ringSize);
      buff.append(", enabled=" + enabled);
      buff.append(", configurationManaged=" + configurationManaged);
      return buff.toString();
   }
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/RollbackMessage.java`
#### Snippet
```java
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", considerLastMessageAsDelivered=" + considerLastMessageAsDelivered);
      return buff.toString();
   }
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionXASetTimeoutMessage.java`
#### Snippet
```java
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", timeoutSeconds=" + timeoutSeconds);
      return buff.toString();
   }
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionXAPrepareMessage.java`
#### Snippet
```java
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", xid=" + xid);
      return buff.toString();
   }
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionExpireMessage.java`
#### Snippet
```java
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", consumerID=" + consumerID);
      buff.append(", messageID=" + messageID);
      return buff.toString();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionExpireMessage.java`
#### Snippet
```java
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", consumerID=" + consumerID);
      buff.append(", messageID=" + messageID);
      return buff.toString();
   }
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SubscribeClusterTopologyUpdatesMessage.java`
#### Snippet
```java
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", clusterConnection=" + clusterConnection);
      return buff.toString();
   }
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionQueueQueryResponseMessage.java`
#### Snippet
```java
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", address=" + address);
      buff.append(", name=" + name);
      buff.append(", consumerCount=" + consumerCount);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionQueueQueryResponseMessage.java`
#### Snippet
```java
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", address=" + address);
      buff.append(", name=" + name);
      buff.append(", consumerCount=" + consumerCount);
      buff.append(", filterString=" + filterString);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionQueueQueryResponseMessage.java`
#### Snippet
```java
      buff.append(", address=" + address);
      buff.append(", name=" + name);
      buff.append(", consumerCount=" + consumerCount);
      buff.append(", filterString=" + filterString);
      buff.append(", durable=" + durable);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionQueueQueryResponseMessage.java`
#### Snippet
```java
      buff.append(", name=" + name);
      buff.append(", consumerCount=" + consumerCount);
      buff.append(", filterString=" + filterString);
      buff.append(", durable=" + durable);
      buff.append(", exists=" + exists);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionQueueQueryResponseMessage.java`
#### Snippet
```java
      buff.append(", consumerCount=" + consumerCount);
      buff.append(", filterString=" + filterString);
      buff.append(", durable=" + durable);
      buff.append(", exists=" + exists);
      buff.append(", temporary=" + temporary);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionQueueQueryResponseMessage.java`
#### Snippet
```java
      buff.append(", filterString=" + filterString);
      buff.append(", durable=" + durable);
      buff.append(", exists=" + exists);
      buff.append(", temporary=" + temporary);
      buff.append(", messageCount=" + messageCount);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionQueueQueryResponseMessage.java`
#### Snippet
```java
      buff.append(", durable=" + durable);
      buff.append(", exists=" + exists);
      buff.append(", temporary=" + temporary);
      buff.append(", messageCount=" + messageCount);
      return buff.toString();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionQueueQueryResponseMessage.java`
#### Snippet
```java
      buff.append(", exists=" + exists);
      buff.append(", temporary=" + temporary);
      buff.append(", messageCount=" + messageCount);
      return buff.toString();
   }
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/CreateSessionMessage.java`
#### Snippet
```java
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", autoCommitAcks=" + autoCommitAcks);
      buff.append(", autoCommitSends=" + autoCommitSends);
      buff.append(", defaultAddress=" + defaultAddress);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/CreateSessionMessage.java`
#### Snippet
```java
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", autoCommitAcks=" + autoCommitAcks);
      buff.append(", autoCommitSends=" + autoCommitSends);
      buff.append(", defaultAddress=" + defaultAddress);
      buff.append(", minLargeMessageSize=" + minLargeMessageSize);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/CreateSessionMessage.java`
#### Snippet
```java
      buff.append(", autoCommitAcks=" + autoCommitAcks);
      buff.append(", autoCommitSends=" + autoCommitSends);
      buff.append(", defaultAddress=" + defaultAddress);
      buff.append(", minLargeMessageSize=" + minLargeMessageSize);
      buff.append(", name=" + name);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/CreateSessionMessage.java`
#### Snippet
```java
      buff.append(", autoCommitSends=" + autoCommitSends);
      buff.append(", defaultAddress=" + defaultAddress);
      buff.append(", minLargeMessageSize=" + minLargeMessageSize);
      buff.append(", name=" + name);
      buff.append(", password=****");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/CreateSessionMessage.java`
#### Snippet
```java
      buff.append(", defaultAddress=" + defaultAddress);
      buff.append(", minLargeMessageSize=" + minLargeMessageSize);
      buff.append(", name=" + name);
      buff.append(", password=****");
      buff.append(", preAcknowledge=" + preAcknowledge);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/CreateSessionMessage.java`
#### Snippet
```java
      buff.append(", name=" + name);
      buff.append(", password=****");
      buff.append(", preAcknowledge=" + preAcknowledge);
      buff.append(", sessionChannelID=" + sessionChannelID);
      buff.append(", username=" + username);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/CreateSessionMessage.java`
#### Snippet
```java
      buff.append(", password=****");
      buff.append(", preAcknowledge=" + preAcknowledge);
      buff.append(", sessionChannelID=" + sessionChannelID);
      buff.append(", username=" + username);
      buff.append(", version=" + version);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/CreateSessionMessage.java`
#### Snippet
```java
      buff.append(", preAcknowledge=" + preAcknowledge);
      buff.append(", sessionChannelID=" + sessionChannelID);
      buff.append(", username=" + username);
      buff.append(", version=" + version);
      buff.append(", windowSize=" + windowSize);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/CreateSessionMessage.java`
#### Snippet
```java
      buff.append(", sessionChannelID=" + sessionChannelID);
      buff.append(", username=" + username);
      buff.append(", version=" + version);
      buff.append(", windowSize=" + windowSize);
      buff.append(", xa=" + xa);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/CreateSessionMessage.java`
#### Snippet
```java
      buff.append(", username=" + username);
      buff.append(", version=" + version);
      buff.append(", windowSize=" + windowSize);
      buff.append(", xa=" + xa);
      return buff.toString();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/CreateSessionMessage.java`
#### Snippet
```java
      buff.append(", version=" + version);
      buff.append(", windowSize=" + windowSize);
      buff.append(", xa=" + xa);
      return buff.toString();
   }
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionProducerCreditsMessage.java`
#### Snippet
```java
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", address=" + address);
      buff.append(", credits=" + credits);
      return buff.toString();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionProducerCreditsMessage.java`
#### Snippet
```java
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", address=" + address);
      buff.append(", credits=" + credits);
      return buff.toString();
   }
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/ReattachSessionMessage.java`
#### Snippet
```java
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", lastConfirmedCommandID=" + lastConfirmedCommandID);
      buff.append(", name=" + name);
      return buff.toString();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/ReattachSessionMessage.java`
#### Snippet
```java
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", lastConfirmedCommandID=" + lastConfirmedCommandID);
      buff.append(", name=" + name);
      return buff.toString();
   }
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionQueueQueryResponseMessage_V2.java`
#### Snippet
```java
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", autoCreationEnabled=" + autoCreateQueues);
      return buff.toString();
   }
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionXAStartMessage.java`
#### Snippet
```java
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", xid=" + xid);
      return buff.toString();
   }
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/DisconnectConsumerMessage.java`
#### Snippet
```java
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", consumerId=" + consumerId);
      return buff.toString();
   }
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionProducerCreditsFailMessage.java`
#### Snippet
```java
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", address=" + address);
      buff.append(", credits=" + credits);
      return buff.toString();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionProducerCreditsFailMessage.java`
#### Snippet
```java
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", address=" + address);
      buff.append(", credits=" + credits);
      return buff.toString();
   }
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/CheckFailoverMessage.java`
#### Snippet
```java
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", nodeID=" + nodeID);
      return buff.toString();
   }
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionSendMessage_V2.java`
#### Snippet
```java
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", correlationID=" + correlationID);
      buff.append(", requiresResponse=" + super.isRequiresResponse());
      return buff.toString();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionSendMessage_V2.java`
#### Snippet
```java
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", correlationID=" + correlationID);
      buff.append(", requiresResponse=" + super.isRequiresResponse());
      return buff.toString();
   }
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/ReattachSessionResponseMessage.java`
#### Snippet
```java
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", lastConfirmedCommandID=" + lastConfirmedCommandID);
      buff.append(", reattached=" + reattached);
      return buff.toString();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/ReattachSessionResponseMessage.java`
#### Snippet
```java
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", lastConfirmedCommandID=" + lastConfirmedCommandID);
      buff.append(", reattached=" + reattached);
      return buff.toString();
   }
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionXAResponseMessage.java`
#### Snippet
```java
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", error=" + error);
      buff.append(", message=" + message);
      buff.append(", responseCode=" + responseCode);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionXAResponseMessage.java`
#### Snippet
```java
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", error=" + error);
      buff.append(", message=" + message);
      buff.append(", responseCode=" + responseCode);
      buff.append("]");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionXAResponseMessage.java`
#### Snippet
```java
      buff.append(", error=" + error);
      buff.append(", message=" + message);
      buff.append(", responseCode=" + responseCode);
      buff.append("]");
      return buff.toString();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionReceiveMessage.java`
#### Snippet
```java
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", consumerID=" + consumerID);
      buff.append(", deliveryCount=" + deliveryCount);
      buff.append("]");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionReceiveMessage.java`
#### Snippet
```java
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", consumerID=" + consumerID);
      buff.append(", deliveryCount=" + deliveryCount);
      buff.append("]");
      return buff.toString();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionAcknowledgeMessage.java`
#### Snippet
```java
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", consumerID=" + consumerID);
      buff.append(", messageID=" + messageID);
      buff.append(", requiresResponse=" + requiresResponse);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionAcknowledgeMessage.java`
#### Snippet
```java
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", consumerID=" + consumerID);
      buff.append(", messageID=" + messageID);
      buff.append(", requiresResponse=" + requiresResponse);
      return buff.toString();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionAcknowledgeMessage.java`
#### Snippet
```java
      buff.append(", consumerID=" + consumerID);
      buff.append(", messageID=" + messageID);
      buff.append(", requiresResponse=" + requiresResponse);
      return buff.toString();
   }
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/ClusterTopologyChangeMessage.java`
#### Snippet
```java
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", exit=" + exit);
      buff.append(", last=" + last);
      buff.append(", nodeID=" + nodeID);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/ClusterTopologyChangeMessage.java`
#### Snippet
```java
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", exit=" + exit);
      buff.append(", last=" + last);
      buff.append(", nodeID=" + nodeID);
      buff.append(", pair=" + pair);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/ClusterTopologyChangeMessage.java`
#### Snippet
```java
      buff.append(", exit=" + exit);
      buff.append(", last=" + last);
      buff.append(", nodeID=" + nodeID);
      buff.append(", pair=" + pair);
      return buff.toString();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/ClusterTopologyChangeMessage.java`
#### Snippet
```java
      buff.append(", last=" + last);
      buff.append(", nodeID=" + nodeID);
      buff.append(", pair=" + pair);
      return buff.toString();
   }
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionBindingQueryResponseMessage_V3.java`
#### Snippet
```java
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", autoCreateAddresses=" + autoCreateAddresses);
      return buff.toString();
   }
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionXAResumeMessage.java`
#### Snippet
```java
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", xid=" + xid);
      return buff.toString();
   }
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionSendMessage.java`
#### Snippet
```java
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", requiresResponse=" + requiresResponse);
      return buff.toString();
   }
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/ClusterTopologyChangeMessage_V2.java`
#### Snippet
```java
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", exit=" + exit);
      buff.append(", last=" + last);
      buff.append(", nodeID=" + nodeID);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/ClusterTopologyChangeMessage_V2.java`
#### Snippet
```java
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", exit=" + exit);
      buff.append(", last=" + last);
      buff.append(", nodeID=" + nodeID);
      buff.append(", pair=" + pair);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/ClusterTopologyChangeMessage_V2.java`
#### Snippet
```java
      buff.append(", exit=" + exit);
      buff.append(", last=" + last);
      buff.append(", nodeID=" + nodeID);
      buff.append(", pair=" + pair);
      buff.append(", backupGroupName=" + backupGroupName);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/ClusterTopologyChangeMessage_V2.java`
#### Snippet
```java
      buff.append(", last=" + last);
      buff.append(", nodeID=" + nodeID);
      buff.append(", pair=" + pair);
      buff.append(", backupGroupName=" + backupGroupName);
      buff.append(", uniqueEventID=" + uniqueEventID);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/ClusterTopologyChangeMessage_V2.java`
#### Snippet
```java
      buff.append(", nodeID=" + nodeID);
      buff.append(", pair=" + pair);
      buff.append(", backupGroupName=" + backupGroupName);
      buff.append(", uniqueEventID=" + uniqueEventID);
      return buff.toString();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/ClusterTopologyChangeMessage_V2.java`
#### Snippet
```java
      buff.append(", pair=" + pair);
      buff.append(", backupGroupName=" + backupGroupName);
      buff.append(", uniqueEventID=" + uniqueEventID);
      return buff.toString();
   }
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/CreateSessionMessage_V2.java`
#### Snippet
```java
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", metadata=" + clientID);
      return buff.toString();
   }
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionIndividualAcknowledgeMessage.java`
#### Snippet
```java
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", consumerID=" + consumerID);
      buff.append(", messageID=" + messageID);
      buff.append(", requiresResponse=" + requiresResponse);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionIndividualAcknowledgeMessage.java`
#### Snippet
```java
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", consumerID=" + consumerID);
      buff.append(", messageID=" + messageID);
      buff.append(", requiresResponse=" + requiresResponse);
      return buff.toString();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionIndividualAcknowledgeMessage.java`
#### Snippet
```java
      buff.append(", consumerID=" + consumerID);
      buff.append(", messageID=" + messageID);
      buff.append(", requiresResponse=" + requiresResponse);
      return buff.toString();
   }
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionRequestProducerCreditsMessage.java`
#### Snippet
```java
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", credits=" + credits);
      buff.append(", address=" + address);
      return buff.toString();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionRequestProducerCreditsMessage.java`
#### Snippet
```java
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", credits=" + credits);
      buff.append(", address=" + address);
      return buff.toString();
   }
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/CreateAddressMessage.java`
#### Snippet
```java
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", address=" + address);
      buff.append(", routingTypes=" + routingTypes);
      buff.append(", autoCreated=" + autoCreated);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/CreateAddressMessage.java`
#### Snippet
```java
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", address=" + address);
      buff.append(", routingTypes=" + routingTypes);
      buff.append(", autoCreated=" + autoCreated);
      return buff.toString();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/CreateAddressMessage.java`
#### Snippet
```java
      buff.append(", address=" + address);
      buff.append(", routingTypes=" + routingTypes);
      buff.append(", autoCreated=" + autoCreated);
      return buff.toString();
   }
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionReceiveContinuationMessage.java`
#### Snippet
```java
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", consumerID=" + consumerID);
      return buff.toString();
   }
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/CreateSharedQueueMessage_V2.java`
#### Snippet
```java
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", address=" + address);
      buff.append(", queueName=" + queueName);
      buff.append(", filterString=" + filterString);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/CreateSharedQueueMessage_V2.java`
#### Snippet
```java
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", address=" + address);
      buff.append(", queueName=" + queueName);
      buff.append(", filterString=" + filterString);
      buff.append(", durable=" + durable);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/CreateSharedQueueMessage_V2.java`
#### Snippet
```java
      buff.append(", address=" + address);
      buff.append(", queueName=" + queueName);
      buff.append(", filterString=" + filterString);
      buff.append(", durable=" + durable);
      buff.append(", routingType=" + routingType);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/CreateSharedQueueMessage_V2.java`
#### Snippet
```java
      buff.append(", queueName=" + queueName);
      buff.append(", filterString=" + filterString);
      buff.append(", durable=" + durable);
      buff.append(", routingType=" + routingType);
      buff.append(", maxConsumers=" + maxConsumers);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/CreateSharedQueueMessage_V2.java`
#### Snippet
```java
      buff.append(", filterString=" + filterString);
      buff.append(", durable=" + durable);
      buff.append(", routingType=" + routingType);
      buff.append(", maxConsumers=" + maxConsumers);
      buff.append(", purgeOnNoConsumers=" + purgeOnNoConsumers);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/CreateSharedQueueMessage_V2.java`
#### Snippet
```java
      buff.append(", durable=" + durable);
      buff.append(", routingType=" + routingType);
      buff.append(", maxConsumers=" + maxConsumers);
      buff.append(", purgeOnNoConsumers=" + purgeOnNoConsumers);
      buff.append(", exclusive=" + exclusive);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/CreateSharedQueueMessage_V2.java`
#### Snippet
```java
      buff.append(", routingType=" + routingType);
      buff.append(", maxConsumers=" + maxConsumers);
      buff.append(", purgeOnNoConsumers=" + purgeOnNoConsumers);
      buff.append(", exclusive=" + exclusive);
      buff.append(", groupRebalance=" + groupRebalance);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/CreateSharedQueueMessage_V2.java`
#### Snippet
```java
      buff.append(", maxConsumers=" + maxConsumers);
      buff.append(", purgeOnNoConsumers=" + purgeOnNoConsumers);
      buff.append(", exclusive=" + exclusive);
      buff.append(", groupRebalance=" + groupRebalance);
      buff.append(", groupRebalancePauseDispatch=" + groupRebalancePauseDispatch);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/CreateSharedQueueMessage_V2.java`
#### Snippet
```java
      buff.append(", purgeOnNoConsumers=" + purgeOnNoConsumers);
      buff.append(", exclusive=" + exclusive);
      buff.append(", groupRebalance=" + groupRebalance);
      buff.append(", groupRebalancePauseDispatch=" + groupRebalancePauseDispatch);
      buff.append(", groupBuckets=" + groupBuckets);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/CreateSharedQueueMessage_V2.java`
#### Snippet
```java
      buff.append(", exclusive=" + exclusive);
      buff.append(", groupRebalance=" + groupRebalance);
      buff.append(", groupRebalancePauseDispatch=" + groupRebalancePauseDispatch);
      buff.append(", groupBuckets=" + groupBuckets);
      buff.append(", groupFirstKey=" + groupFirstKey);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/CreateSharedQueueMessage_V2.java`
#### Snippet
```java
      buff.append(", groupRebalance=" + groupRebalance);
      buff.append(", groupRebalancePauseDispatch=" + groupRebalancePauseDispatch);
      buff.append(", groupBuckets=" + groupBuckets);
      buff.append(", groupFirstKey=" + groupFirstKey);
      buff.append(", lastValue=" + lastValue);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/CreateSharedQueueMessage_V2.java`
#### Snippet
```java
      buff.append(", groupRebalancePauseDispatch=" + groupRebalancePauseDispatch);
      buff.append(", groupBuckets=" + groupBuckets);
      buff.append(", groupFirstKey=" + groupFirstKey);
      buff.append(", lastValue=" + lastValue);
      buff.append(", lastValueKey=" + lastValueKey);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/CreateSharedQueueMessage_V2.java`
#### Snippet
```java
      buff.append(", groupBuckets=" + groupBuckets);
      buff.append(", groupFirstKey=" + groupFirstKey);
      buff.append(", lastValue=" + lastValue);
      buff.append(", lastValueKey=" + lastValueKey);
      buff.append(", nonDestructive=" + nonDestructive);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/CreateSharedQueueMessage_V2.java`
#### Snippet
```java
      buff.append(", groupFirstKey=" + groupFirstKey);
      buff.append(", lastValue=" + lastValue);
      buff.append(", lastValueKey=" + lastValueKey);
      buff.append(", nonDestructive=" + nonDestructive);
      buff.append(", consumersBeforeDispatch=" + consumersBeforeDispatch);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/CreateSharedQueueMessage_V2.java`
#### Snippet
```java
      buff.append(", lastValue=" + lastValue);
      buff.append(", lastValueKey=" + lastValueKey);
      buff.append(", nonDestructive=" + nonDestructive);
      buff.append(", consumersBeforeDispatch=" + consumersBeforeDispatch);
      buff.append(", delayBeforeDispatch=" + delayBeforeDispatch);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/CreateSharedQueueMessage_V2.java`
#### Snippet
```java
      buff.append(", lastValueKey=" + lastValueKey);
      buff.append(", nonDestructive=" + nonDestructive);
      buff.append(", consumersBeforeDispatch=" + consumersBeforeDispatch);
      buff.append(", delayBeforeDispatch=" + delayBeforeDispatch);
      buff.append(", autoDelete=" + autoDelete);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/CreateSharedQueueMessage_V2.java`
#### Snippet
```java
      buff.append(", nonDestructive=" + nonDestructive);
      buff.append(", consumersBeforeDispatch=" + consumersBeforeDispatch);
      buff.append(", delayBeforeDispatch=" + delayBeforeDispatch);
      buff.append(", autoDelete=" + autoDelete);
      buff.append(", autoDeleteDelay=" + autoDeleteDelay);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/CreateSharedQueueMessage_V2.java`
#### Snippet
```java
      buff.append(", consumersBeforeDispatch=" + consumersBeforeDispatch);
      buff.append(", delayBeforeDispatch=" + delayBeforeDispatch);
      buff.append(", autoDelete=" + autoDelete);
      buff.append(", autoDeleteDelay=" + autoDeleteDelay);
      buff.append(", autoDeleteMessageCount=" + autoDeleteMessageCount);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/CreateSharedQueueMessage_V2.java`
#### Snippet
```java
      buff.append(", delayBeforeDispatch=" + delayBeforeDispatch);
      buff.append(", autoDelete=" + autoDelete);
      buff.append(", autoDeleteDelay=" + autoDeleteDelay);
      buff.append(", autoDeleteMessageCount=" + autoDeleteMessageCount);
      buff.append(", ringSize=" + ringSize);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/CreateSharedQueueMessage_V2.java`
#### Snippet
```java
      buff.append(", autoDelete=" + autoDelete);
      buff.append(", autoDeleteDelay=" + autoDeleteDelay);
      buff.append(", autoDeleteMessageCount=" + autoDeleteMessageCount);
      buff.append(", ringSize=" + ringSize);
      buff.append(", enabled=" + enabled);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/CreateSharedQueueMessage_V2.java`
#### Snippet
```java
      buff.append(", autoDeleteDelay=" + autoDeleteDelay);
      buff.append(", autoDeleteMessageCount=" + autoDeleteMessageCount);
      buff.append(", ringSize=" + ringSize);
      buff.append(", enabled=" + enabled);
      buff.append(", requiresResponse=" + requiresResponse);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/CreateSharedQueueMessage_V2.java`
#### Snippet
```java
      buff.append(", autoDeleteMessageCount=" + autoDeleteMessageCount);
      buff.append(", ringSize=" + ringSize);
      buff.append(", enabled=" + enabled);
      buff.append(", requiresResponse=" + requiresResponse);
      return buff.toString();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/CreateSharedQueueMessage_V2.java`
#### Snippet
```java
      buff.append(", ringSize=" + ringSize);
      buff.append(", enabled=" + enabled);
      buff.append(", requiresResponse=" + requiresResponse);
      return buff.toString();
   }
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/Ping.java`
#### Snippet
```java
   protected String getPacketString() {
      StringBuffer buf = new StringBuffer(super.getPacketString());
      buf.append(", connectionTTL=" + connectionTTL);
      return buf.toString();
   }
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionBindingQueryMessage.java`
#### Snippet
```java
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", address=" + address);
      buff.append("]");
      return buff.toString();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/CreateSharedQueueMessage.java`
#### Snippet
```java
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", address=" + address);
      buff.append(", queueName=" + queueName);
      buff.append(", filterString=" + filterString);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/CreateSharedQueueMessage.java`
#### Snippet
```java
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", address=" + address);
      buff.append(", queueName=" + queueName);
      buff.append(", filterString=" + filterString);
      buff.append(", durable=" + durable);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/CreateSharedQueueMessage.java`
#### Snippet
```java
      buff.append(", address=" + address);
      buff.append(", queueName=" + queueName);
      buff.append(", filterString=" + filterString);
      buff.append(", durable=" + durable);
      buff.append(", requiresResponse=" + requiresResponse);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/CreateSharedQueueMessage.java`
#### Snippet
```java
      buff.append(", queueName=" + queueName);
      buff.append(", filterString=" + filterString);
      buff.append(", durable=" + durable);
      buff.append(", requiresResponse=" + requiresResponse);
      return buff.toString();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/CreateSharedQueueMessage.java`
#### Snippet
```java
      buff.append(", filterString=" + filterString);
      buff.append(", durable=" + durable);
      buff.append(", requiresResponse=" + requiresResponse);
      return buff.toString();
   }
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/DisconnectConsumerWithKillMessage.java`
#### Snippet
```java
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", nodeID=" + nodeID);
      return buff.toString();
   }
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionQueueQueryMessage.java`
#### Snippet
```java
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", queueName=" + queueName);
      return buff.toString();
   }
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/CreateQueueMessage_V2.java`
#### Snippet
```java
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", autoCreated=" + autoCreated);
      buff.append(", routingType=" + routingType);
      buff.append(", maxConsumers=" + maxConsumers);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionBindingQueryResponseMessage_V4.java`
#### Snippet
```java
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", defaultPurgeOnNoConsumers=" + defaultPurgeOnNoConsumers);
      buff.append(", defaultMaxConsumers=" + defaultMaxConsumers);
      buff.append(", defaultExclusive=" + defaultExclusive);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionBindingQueryResponseMessage_V4.java`
#### Snippet
```java
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", defaultPurgeOnNoConsumers=" + defaultPurgeOnNoConsumers);
      buff.append(", defaultMaxConsumers=" + defaultMaxConsumers);
      buff.append(", defaultExclusive=" + defaultExclusive);
      buff.append(", defaultLastValue=" + defaultLastValue);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/CreateQueueMessage_V2.java`
#### Snippet
```java
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", autoCreated=" + autoCreated);
      buff.append(", routingType=" + routingType);
      buff.append(", maxConsumers=" + maxConsumers);
      buff.append(", purgeOnNoConsumers=" + purgeOnNoConsumers);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionBindingQueryResponseMessage_V4.java`
#### Snippet
```java
      buff.append(", defaultPurgeOnNoConsumers=" + defaultPurgeOnNoConsumers);
      buff.append(", defaultMaxConsumers=" + defaultMaxConsumers);
      buff.append(", defaultExclusive=" + defaultExclusive);
      buff.append(", defaultLastValue=" + defaultLastValue);
      buff.append(", defaultLastValueKey=" + defaultLastValueKey);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionBindingQueryResponseMessage_V4.java`
#### Snippet
```java
      buff.append(", defaultMaxConsumers=" + defaultMaxConsumers);
      buff.append(", defaultExclusive=" + defaultExclusive);
      buff.append(", defaultLastValue=" + defaultLastValue);
      buff.append(", defaultLastValueKey=" + defaultLastValueKey);
      buff.append(", defaultNonDestructive=" + defaultNonDestructive);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/CreateQueueMessage_V2.java`
#### Snippet
```java
      buff.append(", autoCreated=" + autoCreated);
      buff.append(", routingType=" + routingType);
      buff.append(", maxConsumers=" + maxConsumers);
      buff.append(", purgeOnNoConsumers=" + purgeOnNoConsumers);
      buff.append(", exclusive=" + exclusive);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionBindingQueryResponseMessage_V4.java`
#### Snippet
```java
      buff.append(", defaultExclusive=" + defaultExclusive);
      buff.append(", defaultLastValue=" + defaultLastValue);
      buff.append(", defaultLastValueKey=" + defaultLastValueKey);
      buff.append(", defaultNonDestructive=" + defaultNonDestructive);
      buff.append(", defaultConsumersBeforeDispatch=" + defaultConsumersBeforeDispatch);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionBindingQueryResponseMessage_V4.java`
#### Snippet
```java
      buff.append(", defaultLastValue=" + defaultLastValue);
      buff.append(", defaultLastValueKey=" + defaultLastValueKey);
      buff.append(", defaultNonDestructive=" + defaultNonDestructive);
      buff.append(", defaultConsumersBeforeDispatch=" + defaultConsumersBeforeDispatch);
      buff.append(", defaultDelayBeforeDispatch=" + defaultDelayBeforeDispatch);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/CreateQueueMessage_V2.java`
#### Snippet
```java
      buff.append(", routingType=" + routingType);
      buff.append(", maxConsumers=" + maxConsumers);
      buff.append(", purgeOnNoConsumers=" + purgeOnNoConsumers);
      buff.append(", exclusive=" + exclusive);
      buff.append(", groupRebalance=" + groupRebalance);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionBindingQueryResponseMessage_V4.java`
#### Snippet
```java
      buff.append(", defaultLastValueKey=" + defaultLastValueKey);
      buff.append(", defaultNonDestructive=" + defaultNonDestructive);
      buff.append(", defaultConsumersBeforeDispatch=" + defaultConsumersBeforeDispatch);
      buff.append(", defaultDelayBeforeDispatch=" + defaultDelayBeforeDispatch);
      return buff.toString();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionBindingQueryResponseMessage_V4.java`
#### Snippet
```java
      buff.append(", defaultNonDestructive=" + defaultNonDestructive);
      buff.append(", defaultConsumersBeforeDispatch=" + defaultConsumersBeforeDispatch);
      buff.append(", defaultDelayBeforeDispatch=" + defaultDelayBeforeDispatch);
      return buff.toString();
   }
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/CreateQueueMessage_V2.java`
#### Snippet
```java
      buff.append(", maxConsumers=" + maxConsumers);
      buff.append(", purgeOnNoConsumers=" + purgeOnNoConsumers);
      buff.append(", exclusive=" + exclusive);
      buff.append(", groupRebalance=" + groupRebalance);
      buff.append(", groupRebalancePauseDispatch=" + groupRebalancePauseDispatch);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/CreateQueueMessage_V2.java`
#### Snippet
```java
      buff.append(", purgeOnNoConsumers=" + purgeOnNoConsumers);
      buff.append(", exclusive=" + exclusive);
      buff.append(", groupRebalance=" + groupRebalance);
      buff.append(", groupRebalancePauseDispatch=" + groupRebalancePauseDispatch);
      buff.append(", groupBuckets=" + groupBuckets);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/CreateQueueMessage_V2.java`
#### Snippet
```java
      buff.append(", exclusive=" + exclusive);
      buff.append(", groupRebalance=" + groupRebalance);
      buff.append(", groupRebalancePauseDispatch=" + groupRebalancePauseDispatch);
      buff.append(", groupBuckets=" + groupBuckets);
      buff.append(", groupFirstKey=" + groupFirstKey);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/CreateQueueMessage_V2.java`
#### Snippet
```java
      buff.append(", groupRebalance=" + groupRebalance);
      buff.append(", groupRebalancePauseDispatch=" + groupRebalancePauseDispatch);
      buff.append(", groupBuckets=" + groupBuckets);
      buff.append(", groupFirstKey=" + groupFirstKey);
      buff.append(", lastValue=" + lastValue);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/DisconnectMessage_V3.java`
#### Snippet
```java
   protected String getPacketString() {
      StringBuffer buf = new StringBuffer(super.getPacketString());
      buf.append(", nodeID=" + nodeID);
      buf.append(", reason=" + reason);
      buf.append(", targetNodeID=" + targetNodeID);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/CreateQueueMessage_V2.java`
#### Snippet
```java
      buff.append(", groupRebalancePauseDispatch=" + groupRebalancePauseDispatch);
      buff.append(", groupBuckets=" + groupBuckets);
      buff.append(", groupFirstKey=" + groupFirstKey);
      buff.append(", lastValue=" + lastValue);
      buff.append(", lastValueKey=" + lastValue);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/CreateQueueMessage_V2.java`
#### Snippet
```java
      buff.append(", groupBuckets=" + groupBuckets);
      buff.append(", groupFirstKey=" + groupFirstKey);
      buff.append(", lastValue=" + lastValue);
      buff.append(", lastValueKey=" + lastValue);
      buff.append(", nonDestructive=" + nonDestructive);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/DisconnectMessage_V3.java`
#### Snippet
```java
      StringBuffer buf = new StringBuffer(super.getPacketString());
      buf.append(", nodeID=" + nodeID);
      buf.append(", reason=" + reason);
      buf.append(", targetNodeID=" + targetNodeID);
      buf.append(", targetConnector=" + targetConnector);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/DisconnectMessage_V3.java`
#### Snippet
```java
      buf.append(", nodeID=" + nodeID);
      buf.append(", reason=" + reason);
      buf.append(", targetNodeID=" + targetNodeID);
      buf.append(", targetConnector=" + targetConnector);
      return buf.toString();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/CreateQueueMessage_V2.java`
#### Snippet
```java
      buff.append(", groupFirstKey=" + groupFirstKey);
      buff.append(", lastValue=" + lastValue);
      buff.append(", lastValueKey=" + lastValue);
      buff.append(", nonDestructive=" + nonDestructive);
      buff.append(", consumersBeforeDispatch=" + consumersBeforeDispatch);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/DisconnectMessage_V3.java`
#### Snippet
```java
      buf.append(", reason=" + reason);
      buf.append(", targetNodeID=" + targetNodeID);
      buf.append(", targetConnector=" + targetConnector);
      return buf.toString();
   }
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/CreateQueueMessage_V2.java`
#### Snippet
```java
      buff.append(", lastValue=" + lastValue);
      buff.append(", lastValueKey=" + lastValue);
      buff.append(", nonDestructive=" + nonDestructive);
      buff.append(", consumersBeforeDispatch=" + consumersBeforeDispatch);
      buff.append(", delayBeforeDispatch=" + delayBeforeDispatch);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/CreateQueueMessage_V2.java`
#### Snippet
```java
      buff.append(", lastValueKey=" + lastValue);
      buff.append(", nonDestructive=" + nonDestructive);
      buff.append(", consumersBeforeDispatch=" + consumersBeforeDispatch);
      buff.append(", delayBeforeDispatch=" + delayBeforeDispatch);
      buff.append(", autoDelete=" + autoDelete);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/CreateQueueMessage_V2.java`
#### Snippet
```java
      buff.append(", nonDestructive=" + nonDestructive);
      buff.append(", consumersBeforeDispatch=" + consumersBeforeDispatch);
      buff.append(", delayBeforeDispatch=" + delayBeforeDispatch);
      buff.append(", autoDelete=" + autoDelete);
      buff.append(", autoDeleteDelay=" + autoDeleteDelay);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/CreateQueueMessage_V2.java`
#### Snippet
```java
      buff.append(", consumersBeforeDispatch=" + consumersBeforeDispatch);
      buff.append(", delayBeforeDispatch=" + delayBeforeDispatch);
      buff.append(", autoDelete=" + autoDelete);
      buff.append(", autoDeleteDelay=" + autoDeleteDelay);
      buff.append(", autoDeleteMessageCount=" + autoDeleteMessageCount);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/CreateQueueMessage_V2.java`
#### Snippet
```java
      buff.append(", delayBeforeDispatch=" + delayBeforeDispatch);
      buff.append(", autoDelete=" + autoDelete);
      buff.append(", autoDeleteDelay=" + autoDeleteDelay);
      buff.append(", autoDeleteMessageCount=" + autoDeleteMessageCount);
      buff.append(", ringSize=" + ringSize);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/CreateQueueMessage_V2.java`
#### Snippet
```java
      buff.append(", autoDelete=" + autoDelete);
      buff.append(", autoDeleteDelay=" + autoDeleteDelay);
      buff.append(", autoDeleteMessageCount=" + autoDeleteMessageCount);
      buff.append(", ringSize=" + ringSize);
      buff.append(", enabled=" + enabled);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/CreateQueueMessage_V2.java`
#### Snippet
```java
      buff.append(", autoDeleteDelay=" + autoDeleteDelay);
      buff.append(", autoDeleteMessageCount=" + autoDeleteMessageCount);
      buff.append(", ringSize=" + ringSize);
      buff.append(", enabled=" + enabled);
      return buff.toString();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/CreateQueueMessage_V2.java`
#### Snippet
```java
      buff.append(", autoDeleteMessageCount=" + autoDeleteMessageCount);
      buff.append(", ringSize=" + ringSize);
      buff.append(", enabled=" + enabled);
      return buff.toString();
   }
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/DisconnectMessage.java`
#### Snippet
```java
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", nodeID=" + nodeID);
      return buff.toString();
   }
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionCreateConsumerMessage.java`
#### Snippet
```java
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", queueName=" + queueName);
      buff.append(", filterString=" + filterString);
      buff.append(", id=" + id);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionCreateConsumerMessage.java`
#### Snippet
```java
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", queueName=" + queueName);
      buff.append(", filterString=" + filterString);
      buff.append(", id=" + id);
      buff.append(", browseOnly=" + browseOnly);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionCreateConsumerMessage.java`
#### Snippet
```java
      buff.append(", queueName=" + queueName);
      buff.append(", filterString=" + filterString);
      buff.append(", id=" + id);
      buff.append(", browseOnly=" + browseOnly);
      buff.append(", requiresResponse=" + requiresResponse);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionCreateConsumerMessage.java`
#### Snippet
```java
      buff.append(", filterString=" + filterString);
      buff.append(", id=" + id);
      buff.append(", browseOnly=" + browseOnly);
      buff.append(", requiresResponse=" + requiresResponse);
      buff.append(", priority=" + priority);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionCreateConsumerMessage.java`
#### Snippet
```java
      buff.append(", id=" + id);
      buff.append(", browseOnly=" + browseOnly);
      buff.append(", requiresResponse=" + requiresResponse);
      buff.append(", priority=" + priority);
      return buff.toString();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionCreateConsumerMessage.java`
#### Snippet
```java
      buff.append(", browseOnly=" + browseOnly);
      buff.append(", requiresResponse=" + requiresResponse);
      buff.append(", priority=" + priority);
      return buff.toString();
   }
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionSendContinuationMessage.java`
#### Snippet
```java
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", continues=" + continues);
      buff.append(", message=" + message);
      buff.append(", messageBodySize=" + messageBodySize);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionSendContinuationMessage.java`
#### Snippet
```java
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", continues=" + continues);
      buff.append(", message=" + message);
      buff.append(", messageBodySize=" + messageBodySize);
      buff.append(", requiresResponse=" + requiresResponse);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionSendContinuationMessage.java`
#### Snippet
```java
      buff.append(", continues=" + continues);
      buff.append(", message=" + message);
      buff.append(", messageBodySize=" + messageBodySize);
      buff.append(", requiresResponse=" + requiresResponse);
      return buff.toString();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionSendContinuationMessage.java`
#### Snippet
```java
      buff.append(", message=" + message);
      buff.append(", messageBodySize=" + messageBodySize);
      buff.append(", requiresResponse=" + requiresResponse);
      return buff.toString();
   }
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/ClusterTopologyChangeMessage_V3.java`
#### Snippet
```java
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", exit=" + exit);
      buff.append(", last=" + last);
      buff.append(", nodeID=" + nodeID);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/ClusterTopologyChangeMessage_V3.java`
#### Snippet
```java
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", exit=" + exit);
      buff.append(", last=" + last);
      buff.append(", nodeID=" + nodeID);
      buff.append(", pair=" + pair);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/ClusterTopologyChangeMessage_V3.java`
#### Snippet
```java
      buff.append(", exit=" + exit);
      buff.append(", last=" + last);
      buff.append(", nodeID=" + nodeID);
      buff.append(", pair=" + pair);
      buff.append(", backupGroupName=" + backupGroupName);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/ClusterTopologyChangeMessage_V3.java`
#### Snippet
```java
      buff.append(", last=" + last);
      buff.append(", nodeID=" + nodeID);
      buff.append(", pair=" + pair);
      buff.append(", backupGroupName=" + backupGroupName);
      buff.append(", uniqueEventID=" + uniqueEventID);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/ClusterTopologyChangeMessage_V3.java`
#### Snippet
```java
      buff.append(", nodeID=" + nodeID);
      buff.append(", pair=" + pair);
      buff.append(", backupGroupName=" + backupGroupName);
      buff.append(", uniqueEventID=" + uniqueEventID);
      buff.append(", scaleDownGroupName=" + scaleDownGroupName);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/ClusterTopologyChangeMessage_V3.java`
#### Snippet
```java
      buff.append(", pair=" + pair);
      buff.append(", backupGroupName=" + backupGroupName);
      buff.append(", uniqueEventID=" + uniqueEventID);
      buff.append(", scaleDownGroupName=" + scaleDownGroupName);
      return buff.toString();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/ClusterTopologyChangeMessage_V3.java`
#### Snippet
```java
      buff.append(", backupGroupName=" + backupGroupName);
      buff.append(", uniqueEventID=" + uniqueEventID);
      buff.append(", scaleDownGroupName=" + scaleDownGroupName);
      return buff.toString();
   }
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionAddMetaDataMessageV2.java`
#### Snippet
```java
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", key=" + key);
      buff.append(", data=" + data);
      buff.append(", requiresConfirmation=" + requiresConfirmation);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionAddMetaDataMessageV2.java`
#### Snippet
```java
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", key=" + key);
      buff.append(", data=" + data);
      buff.append(", requiresConfirmation=" + requiresConfirmation);
      buff.append("]");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionAddMetaDataMessageV2.java`
#### Snippet
```java
      buff.append(", key=" + key);
      buff.append(", data=" + data);
      buff.append(", requiresConfirmation=" + requiresConfirmation);
      buff.append("]");
      return buff.toString();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionBindingQueryResponseMessage_V2.java`
#### Snippet
```java
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", autoCreateQueues=" + autoCreateQueues);
      return buff.toString();
   }
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionXARollbackMessage.java`
#### Snippet
```java
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", xid=" + xid);
      return buff.toString();
   }
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionAddMetaDataMessage.java`
#### Snippet
```java
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", key=" + key);
      buff.append(", data=" + data);
      return buff.toString();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionAddMetaDataMessage.java`
#### Snippet
```java
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", key=" + key);
      buff.append(", data=" + data);
      return buff.toString();
   }
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionXAJoinMessage.java`
#### Snippet
```java
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", xid=" + xid);
      return buff.toString();
   }
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionXAGetInDoubtXidsResponseMessage.java`
#### Snippet
```java
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", xids=" + xids);
      buff.append("]");
      return buff.toString();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/ClusterTopologyChangeMessage_V4.java`
#### Snippet
```java
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", clientVersion=" + serverVersion);
      return buff.toString();
   }
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SubscribeClusterTopologyUpdatesMessageV2.java`
#### Snippet
```java
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", clientVersion=" + clientVersion);
      return buff.toString();
   }
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionXAAfterFailedMessage.java`
#### Snippet
```java
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", xid=" + xid);
      return buff.toString();
   }
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/CreateQueueMessage.java`
#### Snippet
```java
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", address=" + address);
      buff.append(", queueName=" + queueName);
      buff.append(", filterString=" + filterString);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/CreateQueueMessage.java`
#### Snippet
```java
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", address=" + address);
      buff.append(", queueName=" + queueName);
      buff.append(", filterString=" + filterString);
      buff.append(", durable=" + durable);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/CreateQueueMessage.java`
#### Snippet
```java
      buff.append(", address=" + address);
      buff.append(", queueName=" + queueName);
      buff.append(", filterString=" + filterString);
      buff.append(", durable=" + durable);
      buff.append(", temporary=" + temporary);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/CreateQueueMessage.java`
#### Snippet
```java
      buff.append(", queueName=" + queueName);
      buff.append(", filterString=" + filterString);
      buff.append(", durable=" + durable);
      buff.append(", temporary=" + temporary);
      return buff.toString();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/CreateQueueMessage.java`
#### Snippet
```java
      buff.append(", filterString=" + filterString);
      buff.append(", durable=" + durable);
      buff.append(", temporary=" + temporary);
      return buff.toString();
   }
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionXAGetTimeoutResponseMessage.java`
#### Snippet
```java
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", timeoutSeconds=" + timeoutSeconds);
      return buff.toString();
   }
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionContinuationMessage.java`
#### Snippet
```java
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", body=" + Arrays.toString(body));
      buff.append(", continues=" + continues);
      return buff.toString();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionBindingQueryResponseMessage.java`
#### Snippet
```java
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", exists=" + exists);
      buff.append(", queueNames=" + queueNames);
      return buff.toString();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionContinuationMessage.java`
#### Snippet
```java
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", body=" + Arrays.toString(body));
      buff.append(", continues=" + continues);
      return buff.toString();
   }
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionBindingQueryResponseMessage.java`
#### Snippet
```java
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", exists=" + exists);
      buff.append(", queueNames=" + queueNames);
      return buff.toString();
   }
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/impl/Page.java`
#### Snippet
```java
      while (iter.hasNext()) {
         PagedMessage message = iter.next();
         buffer.append(message.toString() + "\n");
      }
      iter.close();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java
            Object tmpValue = value.getValue();
            if (tmpValue == null || tmpValue.toString().isEmpty()) {
               buffer.append(value.getKey() + "=*N/A*");
            } else {
               buffer.append(value.getKey() + "=" + tmpValue);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java
               buffer.append(value.getKey() + "=*N/A*");
            } else {
               buffer.append(value.getKey() + "=" + tmpValue);
            }
         }
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/impl/ClusterConnectionBridge.java`
#### Snippet
```java
            builder.append("(");
         for (int i = 0; i < includes.size(); i++) {
            builder.append("(" + ManagementHelper.HDR_ADDRESS + " LIKE '" + includes.get(i) + "%')");
            if (i < includes.size() - 1)
               builder.append(" OR ");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/impl/ClusterConnectionBridge.java`
#### Snippet
```java
            builder.append(" AND (");
         for (int i = 0; i < excludes.size(); i++) {
            builder.append("(" + ManagementHelper.HDR_ADDRESS + " NOT LIKE '" + excludes.get(i) + "%')");
            if (i < excludes.size() - 1)
               builder.append(" AND ");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/impl/ClusterConnectionBridge.java`
#### Snippet
```java
      if (!address.contains(",")) {
         if (address.startsWith("!")) {
            stringBuilder.append(ManagementHelper.HDR_ADDRESS + " NOT LIKE '" + address.substring(1, address.length()) + "%'");
         } else {
            stringBuilder.append(ManagementHelper.HDR_ADDRESS + " LIKE '" + address + "%'");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/impl/ClusterConnectionBridge.java`
#### Snippet
```java
            stringBuilder.append(ManagementHelper.HDR_ADDRESS + " NOT LIKE '" + address.substring(1, address.length()) + "%'");
         } else {
            stringBuilder.append(ManagementHelper.HDR_ADDRESS + " LIKE '" + address + "%'");
         }
         return stringBuilder.toString();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/routing/pools/DiscoveryService.java`
#### Snippet
```java
      public String toString() {
         StringBuilder stringBuilder = new StringBuilder(Entry.class.getSimpleName());
         stringBuilder.append("(nodeID=" + nodeID);
         stringBuilder.append(", connector=" + connector);
         stringBuilder.append(") ");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/routing/pools/DiscoveryService.java`
#### Snippet
```java
         StringBuilder stringBuilder = new StringBuilder(Entry.class.getSimpleName());
         stringBuilder.append("(nodeID=" + nodeID);
         stringBuilder.append(", connector=" + connector);
         stringBuilder.append(") ");
         return stringBuilder.toString();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ActiveMQServerImpl.java`
#### Snippet
```java
      try {
         operationsExecuted.append("**************************************************************************************************\n");
         operationsExecuted.append(ActiveMQMessageBundle.BUNDLE.destroyConnectionWithSessionMetadataHeader(metaKey, parameterValue) + "\n");

         Set<ServerSession> allSessions = getSessions();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ActiveMQServerImpl.java`
#### Snippet
```java
               if (value != null && value.equals(parameterValue)) {
                  sessionFound = session;
                  operationsExecuted.append(ActiveMQMessageBundle.BUNDLE.destroyConnectionWithSessionMetadataClosingConnection(sessionFound.toString()) + "\n");
                  RemotingConnection conn = session.getRemotingConnection();
                  if (conn != null) {
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ActiveMQServerImpl.java`
#### Snippet
```java

         if (sessionFound == null) {
            operationsExecuted.append(ActiveMQMessageBundle.BUNDLE.destroyConnectionWithSessionMetadataNoSessionFound(metaKey, parameterValue) + "\n");
         }

```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/ReplicationResponseMessageV2.java`
#### Snippet
```java
   protected String getPacketString() {
      StringBuffer buf = new StringBuffer(super.getPacketString());
      buf.append(", synchronizationIsFinishedAcknowledgement=" + synchronizationIsFinishedAcknowledgement);
      return buf.toString();
   }
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/QuorumVoteMessage.java`
#### Snippet
```java
   protected String getPacketString() {
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", vote=" + vote);
      buff.append(", handler=" + handler);
      return buff.toString();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/QuorumVoteMessage.java`
#### Snippet
```java
      StringBuffer buff = new StringBuffer(super.getPacketString());
      buff.append(", vote=" + vote);
      buff.append(", handler=" + handler);
      return buff.toString();
   }
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/ReplicationStartSyncMessage.java`
#### Snippet
```java
   protected String getPacketString() {
      StringBuffer buf = new StringBuffer(super.getPacketString());
      buf.append(", synchronizationIsFinished=" + synchronizationIsFinished);
      buf.append(", dataType=" + dataType);
      buf.append(", nodeID=" + nodeID);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/ReplicationStartSyncMessage.java`
#### Snippet
```java
      StringBuffer buf = new StringBuffer(super.getPacketString());
      buf.append(", synchronizationIsFinished=" + synchronizationIsFinished);
      buf.append(", dataType=" + dataType);
      buf.append(", nodeID=" + nodeID);
      buf.append(", ids=" + Arrays.toString(ids));
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/ReplicationStartSyncMessage.java`
#### Snippet
```java
      buf.append(", synchronizationIsFinished=" + synchronizationIsFinished);
      buf.append(", dataType=" + dataType);
      buf.append(", nodeID=" + nodeID);
      buf.append(", ids=" + Arrays.toString(ids));
      buf.append(", allowsAutoFailBack=" + allowsAutoFailBack);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/ReplicationStartSyncMessage.java`
#### Snippet
```java
      buf.append(", dataType=" + dataType);
      buf.append(", nodeID=" + nodeID);
      buf.append(", ids=" + Arrays.toString(ids));
      buf.append(", allowsAutoFailBack=" + allowsAutoFailBack);
      return buf.toString();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/ReplicationStartSyncMessage.java`
#### Snippet
```java
      buf.append(", nodeID=" + nodeID);
      buf.append(", ids=" + Arrays.toString(ids));
      buf.append(", allowsAutoFailBack=" + allowsAutoFailBack);
      return buf.toString();
   }
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/codec/PersistentAddressBindingEncoding.java`
#### Snippet
```java
   public String toString() {
      StringBuilder sb = new StringBuilder("PersistentAddressBindingEncoding [id=" + id);
      sb.append(", name=" + name);
      sb.append(", routingTypes={");
      for (RoutingType routingType : routingTypes) {
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/codec/PersistentAddressBindingEncoding.java`
#### Snippet
```java
      sb.append(", routingTypes={");
      for (RoutingType routingType : routingTypes) {
         sb.append(routingType.toString() + ",");
      }
      if (sb.charAt(sb.length() - 1) == ',') {
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/codec/PersistentAddressBindingEncoding.java`
#### Snippet
```java
      }
      sb.append("}");
      sb.append(", autoCreated=" + autoCreated + "]");
      return sb.toString();
   }
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/DescribeJournal.java`
#### Snippet
```java
         StringBuffer buffer = new StringBuffer();
         buffer.append(msg.isLargeMessage() ? "LargeMessage(" : "Message(");
         buffer.append("messageID=" + msg.getMessageID());
         if (msg.getUserID() != null) {
            buffer.append(";userMessageID=" + msg.getUserID().toString());
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/DescribeJournal.java`
#### Snippet
```java
         buffer.append("messageID=" + msg.getMessageID());
         if (msg.getUserID() != null) {
            buffer.append(";userMessageID=" + msg.getUserID().toString());
         }

```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/DescribeJournal.java`
#### Snippet
```java
         }

         buffer.append(";msg=" + msg.toString());

         return buffer.toString();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/DescribeJournal.java`
#### Snippet
```java
                                       List<RecordInfo> records1,
                                       List<RecordInfo> recordsToDelete) {
            bufferFailingTransactions.append("Transaction " + transactionID + " failed with these records:\n");
            for (RecordInfo info : records1) {
               bufferFailingTransactions.append("- " + describeRecord(info, safe) + "\n");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/DescribeJournal.java`
#### Snippet
```java
            bufferFailingTransactions.append("Transaction " + transactionID + " failed with these records:\n");
            for (RecordInfo info : records1) {
               bufferFailingTransactions.append("- " + describeRecord(info, safe) + "\n");
            }

```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/DescribeJournal.java`
#### Snippet
```java

            for (RecordInfo info : recordsToDelete) {
               bufferFailingTransactions.append("- " + describeRecord(info, safe) + " <marked to delete>\n");
            }

```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/messagecounter/MessageCounter.java`
#### Snippet
```java
         // append 24 comma separated hour counter values
         for (int i = 0; i < DayCounter.HOURS; i++) {
            strData.append("," + counters[i]);
         }

```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/messagecounter/MessageCounter.java`
#### Snippet
```java
      synchronized (dayCounters) {
         // first line: history day count
         ret.append(dayCounters.size() + "\n");

         // following lines: day counter data
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/messagecounter/MessageCounter.java`
#### Snippet
```java
         // following lines: day counter data
         for (DayCounter counter : dayCounters) {
            ret.append(counter.getDayCounterAsString() + "\n");
         }
      }
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/messagecounter/impl/MessageCounterHelper.java`
#### Snippet
```java
            durableStr = Boolean.toString(counter.isDestinationDurable());
         }
         ret.append("<tr bgcolor=\"#" + (i % 2 == 0 ? "FFFFFF" : "F0F0F0") + "\">");

         ret.append("<td>" + type + "</td>");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/messagecounter/impl/MessageCounterHelper.java`
#### Snippet
```java
         ret.append("<tr bgcolor=\"#" + (i % 2 == 0 ? "FFFFFF" : "F0F0F0") + "\">");

         ret.append("<td>" + type + "</td>");
         ret.append("<td>" + counter.getDestinationName() + "</td>");
         ret.append("<td>" + subscription + "</td>");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/messagecounter/impl/MessageCounterHelper.java`
#### Snippet
```java

         ret.append("<td>" + type + "</td>");
         ret.append("<td>" + counter.getDestinationName() + "</td>");
         ret.append("<td>" + subscription + "</td>");
         ret.append("<td>" + durableStr + "</td>");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/messagecounter/impl/MessageCounterHelper.java`
#### Snippet
```java
         ret.append("<td>" + type + "</td>");
         ret.append("<td>" + counter.getDestinationName() + "</td>");
         ret.append("<td>" + subscription + "</td>");
         ret.append("<td>" + durableStr + "</td>");
         ret.append("<td>" + counter.getCount() + "</td>");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/messagecounter/impl/MessageCounterHelper.java`
#### Snippet
```java
         ret.append("<td>" + counter.getDestinationName() + "</td>");
         ret.append("<td>" + subscription + "</td>");
         ret.append("<td>" + durableStr + "</td>");
         ret.append("<td>" + counter.getCount() + "</td>");
         ret.append("<td>" + MessageCounterHelper.prettify(counter.getCountDelta()) + "</td>");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/messagecounter/impl/MessageCounterHelper.java`
#### Snippet
```java
         ret.append("<td>" + subscription + "</td>");
         ret.append("<td>" + durableStr + "</td>");
         ret.append("<td>" + counter.getCount() + "</td>");
         ret.append("<td>" + MessageCounterHelper.prettify(counter.getCountDelta()) + "</td>");
         ret.append("<td>" + MessageCounterHelper.prettify(counter.getMessageCount()) + "</td>");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/messagecounter/impl/MessageCounterHelper.java`
#### Snippet
```java
         ret.append("<td>" + durableStr + "</td>");
         ret.append("<td>" + counter.getCount() + "</td>");
         ret.append("<td>" + MessageCounterHelper.prettify(counter.getCountDelta()) + "</td>");
         ret.append("<td>" + MessageCounterHelper.prettify(counter.getMessageCount()) + "</td>");
         ret.append("<td>" + MessageCounterHelper.prettify(counter.getMessageCountDelta()) + "</td>");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/messagecounter/impl/MessageCounterHelper.java`
#### Snippet
```java
         ret.append("<td>" + counter.getCount() + "</td>");
         ret.append("<td>" + MessageCounterHelper.prettify(counter.getCountDelta()) + "</td>");
         ret.append("<td>" + MessageCounterHelper.prettify(counter.getMessageCount()) + "</td>");
         ret.append("<td>" + MessageCounterHelper.prettify(counter.getMessageCountDelta()) + "</td>");
         ret.append("<td>" + MessageCounterHelper.asDate(counter.getLastAddedMessageTime()) + "</td>");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/messagecounter/impl/MessageCounterHelper.java`
#### Snippet
```java
         ret.append("<td>" + MessageCounterHelper.prettify(counter.getCountDelta()) + "</td>");
         ret.append("<td>" + MessageCounterHelper.prettify(counter.getMessageCount()) + "</td>");
         ret.append("<td>" + MessageCounterHelper.prettify(counter.getMessageCountDelta()) + "</td>");
         ret.append("<td>" + MessageCounterHelper.asDate(counter.getLastAddedMessageTime()) + "</td>");
         ret.append("<td>" + MessageCounterHelper.asDate(counter.getLastAckedMessageTime()) + "</td>");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/messagecounter/impl/MessageCounterHelper.java`
#### Snippet
```java
         ret.append("<td>" + MessageCounterHelper.prettify(counter.getMessageCount()) + "</td>");
         ret.append("<td>" + MessageCounterHelper.prettify(counter.getMessageCountDelta()) + "</td>");
         ret.append("<td>" + MessageCounterHelper.asDate(counter.getLastAddedMessageTime()) + "</td>");
         ret.append("<td>" + MessageCounterHelper.asDate(counter.getLastAckedMessageTime()) + "</td>");
         ret.append("<td>" + MessageCounterHelper.asDate(counter.getLastUpdate()) + "</td>");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/messagecounter/impl/MessageCounterHelper.java`
#### Snippet
```java
         ret.append("<td>" + MessageCounterHelper.prettify(counter.getMessageCountDelta()) + "</td>");
         ret.append("<td>" + MessageCounterHelper.asDate(counter.getLastAddedMessageTime()) + "</td>");
         ret.append("<td>" + MessageCounterHelper.asDate(counter.getLastAckedMessageTime()) + "</td>");
         ret.append("<td>" + MessageCounterHelper.asDate(counter.getLastUpdate()) + "</td>");

```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/messagecounter/impl/MessageCounterHelper.java`
#### Snippet
```java
         ret.append("<td>" + MessageCounterHelper.asDate(counter.getLastAddedMessageTime()) + "</td>");
         ret.append("<td>" + MessageCounterHelper.asDate(counter.getLastAckedMessageTime()) + "</td>");
         ret.append("<td>" + MessageCounterHelper.asDate(counter.getLastUpdate()) + "</td>");

         ret.append("</tr>\n");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/messagecounter/impl/MessageCounterHelper.java`
#### Snippet
```java
         ret.append("    <li>");
         // destination name
         ret.append((counter.isDestinationTopic() ? "Topic '" : "Queue '") + counter.getDestinationName() + "'");
         ret.append("</li>\n");

```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/messagecounter/impl/MessageCounterHelper.java`
#### Snippet
```java
         if (counter.getDestinationSubscription() != null) {
            ret.append("    <li>");
            ret.append("Subscription '" + counter.getDestinationSubscription() + "'");
            ret.append("</li>\n");
         }
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/messagecounter/impl/MessageCounterHelper.java`
#### Snippet
```java

         for (int j = 0; j < 24; j++) {
            ret.append("<th>" + j + "</th>");
         }

```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/messagecounter/impl/MessageCounterHelper.java`
#### Snippet
```java
         for (int j = 0; j < days; j++) {
            // next day counter row
            ret.append("<tr bgcolor=\"#" + (j % 2 == 0 ? "FFFFFF" : "F0F0F0") + "\">");

            // date
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/messagecounter/impl/MessageCounterHelper.java`
#### Snippet
```java

            // date
            ret.append("<td>" + tokens.nextToken() + "</td>");

            // 24 hour counters
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/messagecounter/impl/MessageCounterHelper.java`
#### Snippet
```java
                  ret.append("<td></td>");
               } else {
                  ret.append("<td>" + value + "</td>");

                  total += value;
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/messagecounter/impl/MessageCounterHelper.java`
#### Snippet
```java
            }

            ret.append("<td>" + total + "</td></tr>\n");
         }

```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/ActiveMQServerControlImpl.java`
#### Snippet
```java
            html.append("<table border=\"1\">");
            html.append("<tr><th>creation_time</th>");
            html.append("<td>" + txJson.get(TransactionDetail.KEY_CREATION_TIME) + "</td>");
            html.append("<th>xid_as_base_64</th>");
            html.append("<td colspan=\"3\">" + txJson.get(TransactionDetail.KEY_XID_AS_BASE64) + "</td></tr>");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/ActiveMQServerControlImpl.java`
#### Snippet
```java
            html.append("<td>" + txJson.get(TransactionDetail.KEY_CREATION_TIME) + "</td>");
            html.append("<th>xid_as_base_64</th>");
            html.append("<td colspan=\"3\">" + txJson.get(TransactionDetail.KEY_XID_AS_BASE64) + "</td></tr>");
            html.append("<tr><th>xid_format_id</th>");
            html.append("<td>" + txJson.get(TransactionDetail.KEY_XID_FORMAT_ID) + "</td>");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/ActiveMQServerControlImpl.java`
#### Snippet
```java
            html.append("<td colspan=\"3\">" + txJson.get(TransactionDetail.KEY_XID_AS_BASE64) + "</td></tr>");
            html.append("<tr><th>xid_format_id</th>");
            html.append("<td>" + txJson.get(TransactionDetail.KEY_XID_FORMAT_ID) + "</td>");
            html.append("<th>xid_global_txid</th>");
            html.append("<td>" + txJson.get(TransactionDetail.KEY_XID_GLOBAL_TXID) + "</td>");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/ActiveMQServerControlImpl.java`
#### Snippet
```java
            html.append("<td>" + txJson.get(TransactionDetail.KEY_XID_FORMAT_ID) + "</td>");
            html.append("<th>xid_global_txid</th>");
            html.append("<td>" + txJson.get(TransactionDetail.KEY_XID_GLOBAL_TXID) + "</td>");
            html.append("<th>xid_branch_qual</th>");
            html.append("<td>" + txJson.get(TransactionDetail.KEY_XID_BRANCH_QUAL) + "</td></tr>");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/ActiveMQServerControlImpl.java`
#### Snippet
```java
            html.append("<td>" + txJson.get(TransactionDetail.KEY_XID_GLOBAL_TXID) + "</td>");
            html.append("<th>xid_branch_qual</th>");
            html.append("<td>" + txJson.get(TransactionDetail.KEY_XID_BRANCH_QUAL) + "</td></tr>");

            html.append("<tr><th colspan=\"6\">Message List</th></tr>");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/ActiveMQServerControlImpl.java`
#### Snippet
```java

               html.append("<th>operation_type</th>");
               html.append("<td>" + msgJson.get(TransactionDetail.KEY_MSG_OP_TYPE) + "</th>");
               html.append("<th>message_type</th>");
               html.append("<td>" + msgJson.get(TransactionDetail.KEY_MSG_TYPE) + "</td></tr>");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/ActiveMQServerControlImpl.java`
#### Snippet
```java
               html.append("<td>" + msgJson.get(TransactionDetail.KEY_MSG_OP_TYPE) + "</th>");
               html.append("<th>message_type</th>");
               html.append("<td>" + msgJson.get(TransactionDetail.KEY_MSG_TYPE) + "</td></tr>");
               html.append("<tr><th>properties</th>");
               html.append("<td colspan=\"3\">" + propstr.toString() + "</td></tr>");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/ActiveMQServerControlImpl.java`
#### Snippet
```java
               html.append("<td>" + msgJson.get(TransactionDetail.KEY_MSG_TYPE) + "</td></tr>");
               html.append("<tr><th>properties</th>");
               html.append("<td colspan=\"3\">" + propstr.toString() + "</td></tr>");
            }
            html.append("</table></td></tr>");
```

## RuleId[id=IOResource]
### IOResource
'PrintWriter' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `artemis-maven-plugin/src/main/java/org/apache/activemq/artemis/maven/ArtemisAbstractPlugin.java`
#### Snippet
```java

      StringWriter writer = new StringWriter();
      final PrintWriter strPrint = new PrintWriter(writer);

      strPrint.println("Dependencies explored for " + artifact + ":");
```

### IOResource
'BufferedReader' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/SpawnedVMSupport.java`
#### Snippet
```java
         try {
            InputStreamReader isr = new InputStreamReader(is);
            BufferedReader br = new BufferedReader(isr);
            String line = null;
            while ((line = br.readLine()) != null) {
```

### IOResource
'PrintWriter' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/ByteUtil.java`
#### Snippet
```java
   public static String debugByteArray(byte[] byteArray) {
      StringWriter builder = new StringWriter();
      PrintWriter writer = new PrintWriter(builder);
      for (int i = 0; i < byteArray.length; i++) {
         writer.print("\t[" + i + "]=" + ByteUtil.byteToChar(byteArray[i]) + " / " + byteArray[i]);
```

### IOResource
'ObjectInputStream' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/referenceable/SerializableObjectRefAddr.java`
#### Snippet
```java
      ByteArrayInputStream bis = new ByteArrayInputStream(bytes);

      ObjectInputStream ois = new ObjectInputStream(bis);

      return ois.readObject();
```

### IOResource
'ObjectOutputStream' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/referenceable/SerializableObjectRefAddr.java`
#### Snippet
```java
         ByteArrayOutputStream bos = new ByteArrayOutputStream();

         ObjectOutputStream oos = new ObjectOutputStream(bos);

         oos.writeObject(content);
```

### IOResource
'BufferedReader' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/process/ProcessBuilder.java`
#### Snippet
```java
         try {
            InputStreamReader isr = new InputStreamReader(is);
            BufferedReader br = new BufferedReader(isr);
            String line;
            while ((line = br.readLine()) != null) {
```

### IOResource
'BufferedReader' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/journal/DecodeJournal.java`
#### Snippet
```java
      journal.loadInternalOnly();

      BufferedReader buffReader = new BufferedReader(reader);

      String line;
```

### IOResource
'PrintWriter' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Create.java`
#### Snippet
```java
      if (staticNode != null) {
         StringWriter stringWriter = new StringWriter();
         PrintWriter printer = new PrintWriter(stringWriter);
         printer.println("      <connectors>");
         printer.println("            <!-- Connector used to be announced through cluster connections and notifications -->");
```

### IOResource
'PrintWriter' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Create.java`
#### Snippet
```java
   private void applyAddressesAndQueues(HashMap<String, String> filters) {
      StringWriter writer = new StringWriter();
      PrintWriter printWriter = new PrintWriter(writer);
      printWriter.println();

```

### IOResource
'PrintWriter' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Create.java`
#### Snippet
```java
            String staticCluster = readTextFile(ETC_CLUSTER_STATIC_SETTINGS_TXT, filters);
            StringWriter stringWriter = new StringWriter();
            PrintWriter printWriter = new PrintWriter(stringWriter);
            int countCluster = getStaticNodes().length;
            for (int i = 0; i < countCluster; i++) {
```

### IOResource
'BufferedReader' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `artemis-cli/src/main/java/org/apache/activemq/artemis/util/ServerUtil.java`
#### Snippet
```java
         try {
            InputStreamReader isr = new InputStreamReader(is);
            BufferedReader br = new BufferedReader(isr);
            String line;
            while ((line = br.readLine()) != null) {
```

### IOResource
'ObjectOutputStream' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQObjectMessage.java`
#### Snippet
```java
            ByteArrayOutputStream baos = new ByteArrayOutputStream(1024);

            ObjectOutputStream oos = new ObjectOutputStream(baos);

            oos.writeObject(object);
```

### IOResource
'ObjectInputStream' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQMessage.java`
#### Snippet
```java
      InputStream is = ((ClientMessageInternal) message).getBodyInputStream();
      try {
         ObjectInputStream ois = new ObjectInputStream(is);
         return (T) ois.readObject();
      } catch (Exception e) {
```

### IOResource
'PrintWriter' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/RefCountMessage.java`
#### Snippet
```java
      String debugLocations() {
         StringWriter writer = new StringWriter();
         PrintWriter outWriter = new PrintWriter(writer);
         outWriter.println("Locations:");
         debugCrumbs.forEach(e -> e.printStackTrace(outWriter));
```

### IOResource
'PrintWriter' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/RoutingContextImpl.java`
#### Snippet
```java
   public String toString() {
      StringWriter stringWriter = new StringWriter();
      PrintWriter printWriter = new PrintWriter(stringWriter);
      printWriter.println("RoutingContextImpl(Address=" + this.address + ", routingType=" + this.routingType + ", PreviousAddress=" + previousAddress + " previousRoute:" + previousRoutingType + ", reusable=" + this.reusable + ", version=" + version + ")");
      for (Map.Entry<SimpleString, RouteContextList> entry : map.entrySet()) {
```

### IOResource
'RandomAccessFile' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/FileLockNodeManager.java`
#### Snippet
```java
            fileLock.createNewFile();
         }
         RandomAccessFile randomFileLock = new RandomAccessFile(fileLock, "rw");
         lockChannels[i] = randomFileLock.getChannel();
      }
```

### IOResource
'PrintWriter' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/ClusterManager.java`
#### Snippet
```java
   public String describe() {
      StringWriter str = new StringWriter();
      PrintWriter out = new PrintWriter(str);

      out.println("Information on " + this);
```

### IOResource
'PrintWriter' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/impl/ClusterConnectionImpl.java`
#### Snippet
```java
   public String describe() {
      StringWriter str = new StringWriter();
      PrintWriter out = new PrintWriter(str);

      out.println(this);
```

### IOResource
'PrintWriter' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ActiveMQServerImpl.java`
#### Snippet
```java
   public String describe() {
      StringWriter str = new StringWriter();
      PrintWriter out = new PrintWriter(str);

      out.println(ActiveMQMessageBundle.BUNDLE.serverDescribe(identity, getClusterManager().describe()));
```

### IOResource
'ObjectOutputStream' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/impl/ConfigurationImpl.java`
#### Snippet
```java
         public Configuration run() throws Exception {
            ByteArrayOutputStream bos = new ByteArrayOutputStream();
            ObjectOutputStream os = new ObjectOutputStream(bos);
            os.writeObject(ConfigurationImpl.this);
            Configuration config;
```

### IOResource
'PrintWriter' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/postoffice/impl/BindingsImpl.java`
#### Snippet
```java
      StringWriter writer = new StringWriter();

      PrintWriter out = new PrintWriter(writer);

      out.println("\n**************************************************");
```

### IOResource
'PrintWriter' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java
   public String debug() {
      StringWriter str = new StringWriter();
      PrintWriter out = new PrintWriter(str);

      out.println("queueMemorySize=" + queueMemorySize);
```

## RuleId[id=RedundantOperationOnEmptyContainer]
### RedundantOperationOnEmptyContainer
Array `filterIds` is always empty
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/AmqpSupport.java`
#### Snippet
```java
         StringBuilder ids = new StringBuilder();
         if (filterIds != null) {
            for (Object filterId : filterIds) {
               ids.append(filterId).append(" ");
            }
```

### RedundantOperationOnEmptyContainer
Collection `nodes` is always empty
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ColocatedActivation.java`
#### Snippet
```java
            }
         } else {
            nodes.clear();
            server.getScheduledPool().schedule(new Runnable() {
               @Override
```

## RuleId[id=Java8MapForEach]
### Java8MapForEach
Can be replaced with 'Map.forEach()'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/JsonUtil.java`
#### Snippet
```java
      Map<String, String> properties = new HashMap<>();
      if (jsonString != null) {
         JsonUtil.readJsonObject(jsonString).entrySet().forEach(e -> properties.put(e.getKey(), e.getValue().toString()));
      }
      return properties;
```

### Java8MapForEach
Can be replaced with 'Map.forEach()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/federation/address/FederatedAddress.java`
#### Snippet
```java
            //the queue is going away
            synchronized (this) {
               matchingDiverts.entrySet().forEach(entry -> {
                  if (entry.getKey().getDivert().getForwardAddress().equals(queue.getAddress())) {
                     final AddressInfo addressInfo = server.getPostOffice().getAddressInfo(entry.getKey().getAddress());
```

## RuleId[id=FieldMayBeStatic]
### FieldMayBeStatic
Field `mIsSuccessful` may be 'static'
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/sasl/scram/ScramClientFunctionalityImpl.java`
#### Snippet
```java
   private String mClientFirstMessageBare;

   private final boolean mIsSuccessful = false;
   private byte[] mSaltedPassword;
   private String mAuthMessage;
```

### FieldMayBeStatic
Field `intialConnectDelayMax` may be 'static'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/federation/FederationDownstream.java`
#### Snippet
```java
   private final ScheduledExecutorService scheduledExecutorService;
   private final int intialConnectDelayMultiplier = 2;
   private final int intialConnectDelayMax = 30;

   public static final String FEDERATION_DOWNSTREAM_NAME = "federation-downstream-name";
```

### FieldMayBeStatic
Field `intialConnectDelayMultiplier` may be 'static'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/federation/FederationDownstream.java`
#### Snippet
```java
   private AtomicBoolean initialized = new AtomicBoolean();
   private final ScheduledExecutorService scheduledExecutorService;
   private final int intialConnectDelayMultiplier = 2;
   private final int intialConnectDelayMax = 30;

```

### FieldMayBeStatic
Field `intialConnectDelayMultiplier` may be 'static'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/federation/FederatedQueueConsumerImpl.java`
#### Snippet
```java
   private final AtomicInteger count = new AtomicInteger();
   private final ScheduledExecutorService scheduledExecutorService;
   private final int intialConnectDelayMultiplier = 2;
   private final int intialConnectDelayMax = 30;
   private final ClientSessionCallback clientSessionCallback;
```

### FieldMayBeStatic
Field `intialConnectDelayMax` may be 'static'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/federation/FederatedQueueConsumerImpl.java`
#### Snippet
```java
   private final ScheduledExecutorService scheduledExecutorService;
   private final int intialConnectDelayMultiplier = 2;
   private final int intialConnectDelayMax = 30;
   private final ClientSessionCallback clientSessionCallback;

```

### FieldMayBeStatic
Field `noSync` may be 'static'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/replication/ReplicationEndpoint.java`
#### Snippet
```java
   private final boolean wantedFailBack;
   private final ReplicationEndpointEventListener eventListener;
   private final boolean noSync = false;
   private Channel channel;
   private boolean supportResponseBatching;
```

## RuleId[id=RedundantLengthCheck]
### RedundantLengthCheck
Redundant array length check
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Upgrade.java`
#### Snippet
```java
      doUpgrade(context, tmpFile, targetFile, bkpFile,
              oldLine -> {
                 if (keepingPrefixes.length > 0) {
                    for (String prefix : keepingPrefixes) {
                       if (oldLine.trim().startsWith(prefix)) {
```

### RedundantLengthCheck
Redundant array length check
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Upgrade.java`
#### Snippet
```java
              },
            newLine -> {
               if (keepingPrefixes.length > 0) {
                  for (String prefix : keepingPrefixes) {
                     if (newLine.trim().startsWith(prefix)) {
```

### RedundantLengthCheck
Redundant array length check
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/AmqpSupport.java`
#### Snippet
```java
    */
   public static boolean contains(Symbol[] symbols, Symbol key) {
      if (symbols == null || symbols.length == 0) {
         return false;
      }
```

### RedundantLengthCheck
Redundant array length check
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/connect/mirror/MirrorAddressFilter.java`
#### Snippet
```java

   public boolean match(SimpleString checkAddress) {
      if (denyList.length > 0) {
         for (SimpleString pattern : denyList) {
            if (checkAddress.startsWith(pattern)) {
```

### RedundantLengthCheck
Redundant array length check
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ActiveMQServerImpl.java`
#### Snippet
```java
   private void removeExtraAddressStores() throws Exception {
      SimpleString[] storeNames = pagingManager.getStoreNames();
      if (storeNames != null && storeNames.length > 0) {
         for (SimpleString storeName : storeNames) {
            if (getAddressInfo(storeName) == null) {
```

## RuleId[id=UtilityClassWithoutPrivateConstructor]
### UtilityClassWithoutPrivateConstructor
Class `ServiceUtils` has only 'static' members, and lacks a 'private' constructor
in `artemis-service-extensions/src/main/java/org/apache/activemq/artemis/service/extensions/ServiceUtils.java`
#### Snippet
```java
import org.apache.activemq.artemis.service.extensions.xa.ActiveMQXAResourceWrapperFactoryImpl;

public class ServiceUtils {

   private static ActiveMQXAResourceWrapperFactory activeMQXAResourceWrapperFactory;
```

### UtilityClassWithoutPrivateConstructor
Class `SelectorParser` has only 'static' members, and lacks a 'private' constructor
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/impl/SelectorParser.java`
#### Snippet
```java
import org.apache.activemq.artemis.selector.strict.StrictParser;

public class SelectorParser {

   private static final String CONVERT_STRING_EXPRESSIONS_PREFIX = "convert_string_expressions:";
```

### UtilityClassWithoutPrivateConstructor
Class `HQPropertiesConverter` has only 'static' members, and lacks a 'private' constructor
in `artemis-protocols/artemis-hqclient-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/hornetq/util/HQPropertiesConverter.java`
#### Snippet
```java
import org.apache.activemq.artemis.core.protocol.core.impl.PacketImpl;

public class HQPropertiesConverter {

   private static Map<SimpleString, SimpleString> hqAmqDictionary;
```

### UtilityClassWithoutPrivateConstructor
Class `Artemis` has only 'static' members, and lacks a 'private' constructor
in `artemis-boot/src/main/java/org/apache/activemq/artemis/boot/Artemis.java`
#### Snippet
```java
 * </p>
 */
public class Artemis {

   public static void main(String[] args) throws Throwable {
```

### UtilityClassWithoutPrivateConstructor
Class `StompUtils` has only 'static' members, and lacks a 'private' constructor
in `artemis-protocols/artemis-stomp-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/stomp/StompUtils.java`
#### Snippet
```java
import org.apache.activemq.artemis.reader.MessageUtil;

public class StompUtils {

   private static final String DEFAULT_MESSAGE_PRIORITY = "4";
```

### UtilityClassWithoutPrivateConstructor
Class `PersisterIDs` has only 'static' members, and lacks a 'private' constructor
in `artemis-commons/src/main/java/org/apache/activemq/artemis/core/persistence/PersisterIDs.java`
#### Snippet
```java
*/

public class PersisterIDs {

   public static final int MAX_PERSISTERS = 5;
```

### UtilityClassWithoutPrivateConstructor
Class `ExceptionUtil` has only 'static' members, and lacks a 'private' constructor
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/ExceptionUtil.java`
#### Snippet
```java
import java.util.List;

public class ExceptionUtil {
   public static Throwable getRootCause(final Throwable throwable) {
      List<Throwable> list = new ArrayList<>();
```

### UtilityClassWithoutPrivateConstructor
Class `ListUtil` has only 'static' members, and lacks a 'private' constructor
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/ListUtil.java`
#### Snippet
```java
import java.util.List;

public class ListUtil {
   public static List<String> toList(final String commaSeparatedString) {
      List<String> list = new ArrayList<>();
```

### UtilityClassWithoutPrivateConstructor
Class `FileUtil` has only 'static' members, and lacks a 'private' constructor
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/FileUtil.java`
#### Snippet
```java
import static java.nio.file.attribute.PosixFilePermission.OWNER_WRITE;

public class FileUtil {

   public static void makeExec(File file) throws IOException {
```

### UtilityClassWithoutPrivateConstructor
Class `BundleFactory` has only 'static' members, and lacks a 'private' constructor
in `artemis-commons/src/main/java/org/apache/activemq/artemis/logs/BundleFactory.java`
#### Snippet
```java
import org.slf4j.LoggerFactory;

public class BundleFactory {


```

### UtilityClassWithoutPrivateConstructor
Class `ConcurrentUtil` has only 'static' members, and lacks a 'private' constructor
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/ConcurrentUtil.java`
#### Snippet
```java
import java.util.concurrent.locks.Condition;

public class ConcurrentUtil {

   /**
```

### UtilityClassWithoutPrivateConstructor
Class `Preconditions` has only 'static' members, and lacks a 'private' constructor
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Preconditions.java`
#### Snippet
```java
package org.apache.activemq.artemis.utils;

public class Preconditions {

   /**
```

### UtilityClassWithoutPrivateConstructor
Class `IPV6Util` has only 'static' members, and lacks a 'private' constructor
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/IPV6Util.java`
#### Snippet
```java
package org.apache.activemq.artemis.utils;

public class IPV6Util {

   /**
```

### UtilityClassWithoutPrivateConstructor
Class `JsonLoader` has only 'static' members, and lacks a 'private' constructor
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/JsonLoader.java`
#### Snippet
```java
 * This is equivalent to using {@link javax.json.Json}
 */
public class JsonLoader {

   private static final JsonProvider provider = new org.apache.johnzon.core.JsonProviderImpl();
```

### UtilityClassWithoutPrivateConstructor
Class `SpawnedVMSupport` has only 'static' members, and lacks a 'private' constructor
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/SpawnedVMSupport.java`
#### Snippet
```java
import java.util.concurrent.TimeUnit;

public class SpawnedVMSupport {

   static ConcurrentHashMap<Process, String> startedProcesses = null;
```

### UtilityClassWithoutPrivateConstructor
Class `CompositeAddress` has only 'static' members, and lacks a 'private' constructor
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/CompositeAddress.java`
#### Snippet
```java
import org.apache.activemq.artemis.api.core.SimpleString;

public class CompositeAddress {

   public static final String SEPARATOR = "::";
```

### UtilityClassWithoutPrivateConstructor
Class `SelectorTranslator` has only 'static' members, and lacks a 'private' constructor
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/SelectorTranslator.java`
#### Snippet
```java
 * This makes it less trivial than a simple search and replace.
 */
public class SelectorTranslator {

   public static String convertToActiveMQFilterString(final String selectorString) {
```

### UtilityClassWithoutPrivateConstructor
Class `ThreadDumpUtil` has only 'static' members, and lacks a 'private' constructor
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/ThreadDumpUtil.java`
#### Snippet
```java
import java.lang.management.ThreadMXBean;

public final class ThreadDumpUtil {
   private static final Logger logger = LoggerFactory.getLogger(MethodHandles.lookup().lookupClass());

```

### UtilityClassWithoutPrivateConstructor
Class `Waiter` has only 'static' members, and lacks a 'private' constructor
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Waiter.java`
#### Snippet
```java
import java.util.concurrent.locks.LockSupport;

public class Waiter {

   public interface Condition {
```

### UtilityClassWithoutPrivateConstructor
Class `ObjectCleaner` has only 'static' members, and lacks a 'private' constructor
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/ObjectCleaner.java`
#### Snippet
```java
import java.lang.ref.Cleaner;

public class ObjectCleaner {

   private static final Cleaner cleaner;
```

### UtilityClassWithoutPrivateConstructor
Class `DestinationUtil` has only 'static' members, and lacks a 'private' constructor
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/DestinationUtil.java`
#### Snippet
```java
import org.apache.activemq.artemis.api.core.SimpleString;

public class DestinationUtil {

   public static final String QUEUE_QUALIFIED_PREFIX = "queue://";
```

### UtilityClassWithoutPrivateConstructor
Class `XmlProvider` has only 'static' members, and lacks a 'private' constructor
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/XmlProvider.java`
#### Snippet
```java
import org.xml.sax.SAXException;

public class XmlProvider {
   public static final String ARTEMIS_DISABLE_XXE_PROPERTY = "artemis.disableXxe";

```

### UtilityClassWithoutPrivateConstructor
Class `BooleanUtil` has only 'static' members, and lacks a 'private' constructor
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/BooleanUtil.java`
#### Snippet
```java
package org.apache.activemq.artemis.utils;

public class BooleanUtil {

   public static int toInt(boolean value) {
```

### UtilityClassWithoutPrivateConstructor
Class `Suppliers` has only 'static' members, and lacks a 'private' constructor
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Suppliers.java`
#### Snippet
```java
import static org.apache.activemq.artemis.utils.Preconditions.checkNotNull;

public class Suppliers {

   /**
```

### UtilityClassWithoutPrivateConstructor
Class `DataConstants` has only 'static' members, and lacks a 'private' constructor
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/DataConstants.java`
#### Snippet
```java
package org.apache.activemq.artemis.utils;

public final class DataConstants {

   public static final int SIZE_INT = 4;
```

### UtilityClassWithoutPrivateConstructor
Class `SchemaConstants` has only 'static' members, and lacks a 'private' constructor
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/uri/SchemaConstants.java`
#### Snippet
```java
package org.apache.activemq.artemis.utils.uri;

public class SchemaConstants {

   public static final String TCP = "tcp";
```

### UtilityClassWithoutPrivateConstructor
Class `ByteUtil` has only 'static' members, and lacks a 'private' constructor
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/ByteUtil.java`
#### Snippet
```java
import org.slf4j.Logger;

public class ByteUtil {

   public static final String NON_ASCII_STRING = "@@@@@";
```

### UtilityClassWithoutPrivateConstructor
Class `ClassloadingUtil` has only 'static' members, and lacks a 'private' constructor
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/ClassloadingUtil.java`
#### Snippet
```java
 */

public final class ClassloadingUtil {

   private static final Logger logger = LoggerFactory.getLogger(MethodHandles.lookup().lookupClass());
```

### UtilityClassWithoutPrivateConstructor
Class `BeanSupport` has only 'static' members, and lacks a 'private' constructor
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/uri/BeanSupport.java`
#### Snippet
```java
import org.apache.commons.beanutils.Converter;

public class BeanSupport {

   private static final BeanUtilsBean beanUtils = new BeanUtilsBean();
```

### UtilityClassWithoutPrivateConstructor
Class `FileIOUtil` has only 'static' members, and lacks a 'private' constructor
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/io/util/FileIOUtil.java`
#### Snippet
```java
import org.slf4j.Logger;

public class FileIOUtil {

   private static final Logger logger = LoggerFactory.getLogger(MethodHandles.lookup().lookupClass());
```

### UtilityClassWithoutPrivateConstructor
Class `URISupport` has only 'static' members, and lacks a 'private' constructor
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/uri/URISupport.java`
#### Snippet
```java
 * (copied from activemq 5)
 */
public class URISupport {

   /**
```

### UtilityClassWithoutPrivateConstructor
Class `BrokerFactory` has only 'static' members, and lacks a 'private' constructor
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/factory/BrokerFactory.java`
#### Snippet
```java
import org.apache.activemq.artemis.utils.FactoryFinder;

public class BrokerFactory {

   private static BrokerDTO createBrokerConfiguration(URI configURI,
```

### UtilityClassWithoutPrivateConstructor
Class `SecurityManagerFactory` has only 'static' members, and lacks a 'private' constructor
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/factory/security/SecurityManagerFactory.java`
#### Snippet
```java
import org.apache.activemq.artemis.utils.FactoryFinder;

public class SecurityManagerFactory {

   public static ActiveMQSecurityManager create(SecurityDTO config) throws Exception {
```

### UtilityClassWithoutPrivateConstructor
Class `ProcessBuilder` has only 'static' members, and lacks a 'private' constructor
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/process/ProcessBuilder.java`
#### Snippet
```java
import org.apache.activemq.artemis.utils.collections.ConcurrentHashSet;

public class ProcessBuilder {

   static ConcurrentHashSet<Process> processes = new ConcurrentHashSet<>();
```

### UtilityClassWithoutPrivateConstructor
Class `OptionsUtil` has only 'static' members, and lacks a 'private' constructor
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/OptionsUtil.java`
#### Snippet
```java
import io.airlift.airline.Option;

public class OptionsUtil {

   private static void findAllOptions(Set<String> options, Class<? extends Action> command) {
```

### UtilityClassWithoutPrivateConstructor
Class `ManagementFactory` has only 'static' members, and lacks a 'private' constructor
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/factory/jmx/ManagementFactory.java`
#### Snippet
```java
import java.util.List;

public class ManagementFactory {

   private static ManagementContextDTO createJmxAclConfiguration(URI configURI,
```

### UtilityClassWithoutPrivateConstructor
Class `Artemis` has only 'static' members, and lacks a 'private' constructor
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/Artemis.java`
#### Snippet
```java
 *        disrupt the order of bootstrapping on certain components (e.g. JMX being started from log4j)
 */
public class Artemis {

   public static void main(String... args) throws Exception {
```

### UtilityClassWithoutPrivateConstructor
Class `HashUtil` has only 'static' members, and lacks a 'private' constructor
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/util/HashUtil.java`
#### Snippet
```java
import org.apache.activemq.artemis.utils.PasswordMaskingUtil;

public class HashUtil {

   private static final HashProcessor HASH_PROCESSOR = PasswordMaskingUtil.getHashProcessor();
```

### UtilityClassWithoutPrivateConstructor
Class `SyncCalculation` has only 'static' members, and lacks a 'private' constructor
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/util/SyncCalculation.java`
#### Snippet
```java
 * * *
 */
public class SyncCalculation {

   //uses nanoseconds to avoid any conversion cost: useful when performing operation on mapped journal without fsync or with RAM-like devices
```

### UtilityClassWithoutPrivateConstructor
Class `XmlDataExporterUtil` has only 'static' members, and lacks a 'private' constructor
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/xml/XmlDataExporterUtil.java`
#### Snippet
```java
 * Common utility methods to help with XML message conversion
 */
public class XmlDataExporterUtil {

   public static String convertProperty(final Object value) {
```

### UtilityClassWithoutPrivateConstructor
Class `MicrosClock` has only 'static' members, and lacks a 'private' constructor
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/perf/MicrosClock.java`
#### Snippet
```java
import java.util.concurrent.TimeUnit;

public class MicrosClock {

   // no need for volatile here
```

### UtilityClassWithoutPrivateConstructor
Class `JVMArgumentParser` has only 'static' members, and lacks a 'private' constructor
in `artemis-cli/src/main/java/org/apache/activemq/artemis/util/JVMArgumentParser.java`
#### Snippet
```java
import java.util.Map;

public class JVMArgumentParser {

   public static void parseOriginalArgs(String prefix, String endOfLine, String originalLine, String[] keepingPrefixes, Map<String, String> originalArgs) {
```

### UtilityClassWithoutPrivateConstructor
Class `ServerUtil` has only 'static' members, and lacks a 'private' constructor
in `artemis-cli/src/main/java/org/apache/activemq/artemis/util/ServerUtil.java`
#### Snippet
```java
 * A tool to let clients start, stop and kill Artemis servers
 */
public class ServerUtil {

   public static Process startServer(String artemisInstance, String serverName) throws Exception {
```

### UtilityClassWithoutPrivateConstructor
Class `TransportConfigurationEncodingSupport` has only 'static' members, and lacks a 'private' constructor
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/server/config/impl/TransportConfigurationEncodingSupport.java`
#### Snippet
```java
import org.apache.activemq.artemis.utils.DataConstants;

public class TransportConfigurationEncodingSupport {

   public static List<Pair<TransportConfiguration, TransportConfiguration>> decodeConfigs(ActiveMQBuffer buffer) {
```

### UtilityClassWithoutPrivateConstructor
Class `OpenWireConstants` has only 'static' members, and lacks a 'private' constructor
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenWireConstants.java`
#### Snippet
```java
import org.apache.activemq.artemis.api.core.SimpleString;

public class OpenWireConstants {
   private static final SimpleString AMQ_PREFIX = new SimpleString("__HDR_");
   public static final SimpleString AMQ_MSG_DLQ_DELIVERY_FAILURE_CAUSE_PROPERTY = new SimpleString(AMQ_PREFIX + "dlqDeliveryFailureCause");
```

### UtilityClassWithoutPrivateConstructor
Class `OpenWireUtil` has only 'static' members, and lacks a 'private' constructor
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/util/OpenWireUtil.java`
#### Snippet
```java
import org.apache.activemq.command.XATransactionId;

public class OpenWireUtil {

   public static class OpenWireWildcardConfiguration extends WildcardConfiguration {
```

### UtilityClassWithoutPrivateConstructor
Class `MQTTReasonCodes` has only 'static' members, and lacks a 'private' constructor
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTReasonCodes.java`
#### Snippet
```java
 * Taken mainly from https://docs.oasis-open.org/mqtt/mqtt/v5.0/os/mqtt-v5.0-os.html#_Toc3901031
 */
public class MQTTReasonCodes {
   // codes specific to MQTT 3.x
   public static final byte UNACCEPTABLE_PROTOCOL_VERSION_3 = (byte) 0X01;
```

### UtilityClassWithoutPrivateConstructor
Class `OpenWireMessageConverter` has only 'static' members, and lacks a 'private' constructor
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenWireMessageConverter.java`
#### Snippet
```java
import static org.apache.activemq.command.ActiveMQDestination.QUEUE_TYPE;

public final class OpenWireMessageConverter {

   private static final Logger logger = LoggerFactory.getLogger(MethodHandles.lookup().lookupClass());
```

### UtilityClassWithoutPrivateConstructor
Class `MQTTUtil` has only 'static' members, and lacks a 'private' constructor
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTUtil.java`
#### Snippet
```java
 * A Utility Class for creating Server Side objects and converting MQTT concepts to/from Artemis.
 */
public class MQTTUtil {
   private static final Logger logger = LoggerFactory.getLogger(MethodHandles.lookup().lookupClass());

```

### UtilityClassWithoutPrivateConstructor
Class `XmlUtil` has only 'static' members, and lacks a 'private' constructor
in `artemis-dto/src/main/java/org/apache/activemq/artemis/dto/XmlUtil.java`
#### Snippet
```java
import org.apache.activemq.artemis.utils.XmlProvider;

public class XmlUtil {

   /**
```

### UtilityClassWithoutPrivateConstructor
Class `ActiveMQJMSConstants` has only 'static' members, and lacks a 'private' constructor
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/api/jms/ActiveMQJMSConstants.java`
#### Snippet
```java
 * Constants for ActiveMQ Artemis for property keys used for ActiveMQ Artemis specific extensions to JMS.
 */
public class ActiveMQJMSConstants {

   public static final String JMS_ACTIVEMQ_INPUT_STREAM = "JMS_AMQ_InputStream";
```

### UtilityClassWithoutPrivateConstructor
Class `JMSExceptionHelper` has only 'static' members, and lacks a 'private' constructor
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/JMSExceptionHelper.java`
#### Snippet
```java
import org.apache.activemq.artemis.api.core.ActiveMQInterruptedException;

public final class JMSExceptionHelper {

   public static JMSException convertFromActiveMQException(final ActiveMQInterruptedException me) {
```

### UtilityClassWithoutPrivateConstructor
Class `DefaultConnectionProperties` has only 'static' members, and lacks a 'private' constructor
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/DefaultConnectionProperties.java`
#### Snippet
```java
 * </table>
 */
public class DefaultConnectionProperties {

   public static final String AMQ_HOST = "AMQ_HOST";
```

### UtilityClassWithoutPrivateConstructor
Class `MechanismFinder` has only 'static' members, and lacks a 'private' constructor
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/sasl/MechanismFinder.java`
#### Snippet
```java
import java.util.ServiceLoader;

public class MechanismFinder {

   private static final Map<String, ServerSASLFactory> FACTORY_MAP = new HashMap<>();
```

### UtilityClassWithoutPrivateConstructor
Class `TLSEncode` has only 'static' members, and lacks a 'private' constructor
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/util/TLSEncode.java`
#### Snippet
```java

/** This can go away if Proton provides this feature. */
public class TLSEncode {

   // For now Proton requires that we create a decoder to create an encoder
```

### UtilityClassWithoutPrivateConstructor
Class `AMQPMessageBrokerAccessor` has only 'static' members, and lacks a 'private' constructor
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessageBrokerAccessor.java`
#### Snippet
```java
/** <b>Warning:</b> do not use this class outside of the broker implementation.
 *  This is exposing package methods on this package that are not meant to be used on user's application. */
public class AMQPMessageBrokerAccessor {

   /** Warning: this is a method specific to the broker. Do not use it on user's application. */
```

### UtilityClassWithoutPrivateConstructor
Class `AMQPMessageSymbolSearch` has only 'static' members, and lacks a 'private' constructor
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessageSymbolSearch.java`
#### Snippet
```java
import org.apache.qpid.proton.codec.TypeConstructor;

final class AMQPMessageSymbolSearch {

   // used to quick search for MessageAnnotations
```

### UtilityClassWithoutPrivateConstructor
Class `ExtCapability` has only 'static' members, and lacks a 'private' constructor
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/handler/ExtCapability.java`
#### Snippet
```java
import org.apache.qpid.proton.engine.Connection;

public class ExtCapability {

   public static final Symbol[] capabilities = new Symbol[]{AmqpSupport.SOLE_CONNECTION_CAPABILITY,
```

### UtilityClassWithoutPrivateConstructor
Class `AmqpSupport` has only 'static' members, and lacks a 'private' constructor
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/AmqpSupport.java`
#### Snippet
```java
 * Set of useful methods and definitions used in the AMQP protocol handling
 */
public class AmqpSupport {

   // Default thresholds/values used for granting credit to producers
```

### UtilityClassWithoutPrivateConstructor
Class `Events` has only 'static' members, and lacks a 'private' constructor
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/handler/Events.java`
#### Snippet
```java
import org.apache.qpid.proton.engine.Event;

public final class Events {

   public static void dispatch(Event event, EventHandler handler) throws Exception {
```

### UtilityClassWithoutPrivateConstructor
Class `Connection` has only 'static' members, and lacks a 'private' constructor
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/AMQPConstants.java`
#### Snippet
```java
   * http://docs.oasis-open.org/amqp/core/v1.0/amqp-core-complete-v1.0.pdf#subsection.2.7.1
   * */
   public static class Connection {

      public static final int DEFAULT_IDLE_TIMEOUT = -1;
```

### UtilityClassWithoutPrivateConstructor
Class `AMQPMirrorMessageFactory` has only 'static' members, and lacks a 'private' constructor
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/connect/mirror/AMQPMirrorMessageFactory.java`
#### Snippet
```java
 * This class is responsible for creating the internal message types used on controlling the mirror on the AMQP module.
 */
public class AMQPMirrorMessageFactory {

   /**
```

### UtilityClassWithoutPrivateConstructor
Class `AMQPContentTypeSupport` has only 'static' members, and lacks a 'private' constructor
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/converter/AMQPContentTypeSupport.java`
#### Snippet
```java
import org.apache.activemq.artemis.protocol.amqp.exceptions.ActiveMQAMQPInvalidContentTypeException;

public final class AMQPContentTypeSupport {

   private static final String UTF_8 = "UTF-8";
```

### UtilityClassWithoutPrivateConstructor
Class `AmqpCoreConverter` has only 'static' members, and lacks a 'private' constructor
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/converter/AmqpCoreConverter.java`
#### Snippet
```java
 *  For better organization of the code.
 * */
public class AmqpCoreConverter {
   public static ICoreMessage toCore(AMQPMessage message, CoreMessageObjectPools coreMessageObjectPools) throws Exception {
      return message.toCore(coreMessageObjectPools);
```

### UtilityClassWithoutPrivateConstructor
Class `AMQPMessageSupport` has only 'static' members, and lacks a 'private' constructor
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/converter/AMQPMessageSupport.java`
#### Snippet
```java
 * AMQP Message types being sent or received.
 */
public final class AMQPMessageSupport {
   public static final int NON_PERSISTENT = 1;

```

### UtilityClassWithoutPrivateConstructor
Class `JDBCFileUtils` has only 'static' members, and lacks a 'private' constructor
in `artemis-jdbc-store/src/main/java/org/apache/activemq/artemis/jdbc/store/file/JDBCFileUtils.java`
#### Snippet
```java
import static org.apache.activemq.artemis.jdbc.store.sql.PropertySQLProvider.Factory.SQLDialect.POSTGRESQL;

class JDBCFileUtils {

   static JDBCSequentialFileFactoryDriver getDBFileDriver(JDBCConnectionProvider connectionProvider, SQLProvider provider) throws SQLException {
```

### UtilityClassWithoutPrivateConstructor
Class `JDBCDataSourceUtils` has only 'static' members, and lacks a 'private' constructor
in `artemis-jdbc-store/src/main/java/org/apache/activemq/artemis/jdbc/store/drivers/JDBCDataSourceUtils.java`
#### Snippet
```java
import java.util.stream.Collectors;

public class JDBCDataSourceUtils {

   public static DataSource getDataSource(String dataSourceClassName, Map<String, Object> dataSourceProperties) {
```

### UtilityClassWithoutPrivateConstructor
Class `LoggingUtil` has only 'static' members, and lacks a 'private' constructor
in `artemis-jdbc-store/src/main/java/org/apache/activemq/artemis/jdbc/store/logging/LoggingUtil.java`
#### Snippet
```java
package org.apache.activemq.artemis.jdbc.store.logging;

public class LoggingUtil {
   public static String getID(Object object) {
      return new StringBuilder().append(object.getClass().getName()).append("@").append(Integer.toHexString(System.identityHashCode(object))).toString();
```

### UtilityClassWithoutPrivateConstructor
Class `WebServerComponentTestAccessor` has only 'static' members, and lacks a 'private' constructor
in `artemis-web/src/main/java/org/apache/activemq/artemis/component/WebServerComponentTestAccessor.java`
#### Snippet
```java
import org.eclipse.jetty.webapp.WebAppContext;

public class WebServerComponentTestAccessor {

   public static WebAppContext createWebAppContext(WebServerComponent webServerComponent, String url, String warFile, Path warDirectory, String virtualHost) {
```

### UtilityClassWithoutPrivateConstructor
Class `CoreAmqpConverter` has only 'static' members, and lacks a 'private' constructor
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/converter/CoreAmqpConverter.java`
#### Snippet
```java
import org.apache.qpid.proton.codec.ReadableBuffer.ByteBufferReader;

public class CoreAmqpConverter {

   public static AMQPMessage checkAMQP(Message message, StorageManager storageManager) throws Exception {
```

### UtilityClassWithoutPrivateConstructor
Class `WebTmpCleaner` has only 'static' members, and lacks a 'private' constructor
in `artemis-web/src/main/java/org/apache/activemq/artemis/component/WebTmpCleaner.java`
#### Snippet
```java
 * by URLClassLoader until the whole VM exits.
 */
public class WebTmpCleaner {

   private static final Logger logger = LoggerFactory.getLogger(MethodHandles.lookup().lookupClass());
```

### UtilityClassWithoutPrivateConstructor
Class `JDBCUtils` has only 'static' members, and lacks a 'private' constructor
in `artemis-jdbc-store/src/main/java/org/apache/activemq/artemis/jdbc/store/drivers/JDBCUtils.java`
#### Snippet
```java
import java.lang.invoke.MethodHandles;

public class JDBCUtils {

   private static final Logger logger = LoggerFactory.getLogger(MethodHandles.lookup().lookupClass());
```

### UtilityClassWithoutPrivateConstructor
Class `ResourceNames` has only 'static' members, and lacks a 'private' constructor
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/management/ResourceNames.java`
#### Snippet
```java
 * For example, the resource name of the "foo" queue is {@code QUEUE + "foo"}.
 */
public final class ResourceNames {
   public static final String BROKER = "broker";

```

### UtilityClassWithoutPrivateConstructor
Class `OpenSSLContextFactoryProvider` has only 'static' members, and lacks a 'private' constructor
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/spi/core/remoting/ssl/OpenSSLContextFactoryProvider.java`
#### Snippet
```java
 * Provider that loads all registered {@link OpenSSLContextFactory} services and returns the one with the highest priority.
 */
public class OpenSSLContextFactoryProvider {

   private static final OpenSSLContextFactory FACTORY;
```

### UtilityClassWithoutPrivateConstructor
Class `SSLContextFactoryProvider` has only 'static' members, and lacks a 'private' constructor
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/spi/core/remoting/ssl/SSLContextFactoryProvider.java`
#### Snippet
```java
 * This is only used to provide SSLContext, so it doesn't support OpenSSL.
 */
public class SSLContextFactoryProvider {
   private static final SSLContextFactory factory;
   static {
```

### UtilityClassWithoutPrivateConstructor
Class `BackwardsCompatibilityUtils` has only 'static' members, and lacks a 'private' constructor
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/BackwardsCompatibilityUtils.java`
#### Snippet
```java
 * When sending topology updates to clients, if these properties are used we need to convert them properly
 */
public class BackwardsCompatibilityUtils {

   private static int INITIAL_ACTIVEMQ_INCREMENTING_VERSION = 126;
```

### UtilityClassWithoutPrivateConstructor
Class `CertificateUtil` has only 'static' members, and lacks a 'private' constructor
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/CertificateUtil.java`
#### Snippet
```java
import java.lang.invoke.MethodHandles;

public class CertificateUtil {

   private static final Logger logger = LoggerFactory.getLogger(MethodHandles.lookup().lookupClass());
```

### UtilityClassWithoutPrivateConstructor
Class `TransportConfigurationUtil` has only 'static' members, and lacks a 'private' constructor
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/impl/TransportConfigurationUtil.java`
#### Snippet
```java
 */

public class TransportConfigurationUtil {

   private static final Map<String, Map<String, Object>> DEFAULTS = new HashMap<>();
```

### UtilityClassWithoutPrivateConstructor
Class `CheckDependencies` has only 'static' members, and lacks a 'private' constructor
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/CheckDependencies.java`
#### Snippet
```java
 * it could be improved to check for other cases eventually.
 */
public class CheckDependencies {

   private static final Logger logger = LoggerFactory.getLogger(MethodHandles.lookup().lookupClass());
```

### UtilityClassWithoutPrivateConstructor
Class `TransportConstants` has only 'static' members, and lacks a 'private' constructor
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/TransportConstants.java`
#### Snippet
```java
import java.lang.invoke.MethodHandles;

public class TransportConstants {

   private static final Logger logger = LoggerFactory.getLogger(MethodHandles.lookup().lookupClass());
```

### UtilityClassWithoutPrivateConstructor
Class `StringUtil` has only 'static' members, and lacks a 'private' constructor
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/utils/StringUtil.java`
#### Snippet
```java
import java.util.List;

public class StringUtil {

   /**
```

### UtilityClassWithoutPrivateConstructor
Class `PrefixUtil` has only 'static' members, and lacks a 'private' constructor
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/utils/PrefixUtil.java`
#### Snippet
```java
import org.apache.activemq.artemis.api.core.RoutingType;

public class PrefixUtil {

   public static Pair<SimpleString, EnumSet<RoutingType>> getAddressAndRoutingTypes(SimpleString address,
```

### UtilityClassWithoutPrivateConstructor
Class `SizeFormatterUtil` has only 'static' members, and lacks a 'private' constructor
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/utils/SizeFormatterUtil.java`
#### Snippet
```java
package org.apache.activemq.artemis.utils;

public class SizeFormatterUtil {


```

### UtilityClassWithoutPrivateConstructor
Class `AutoCreateUtil` has only 'static' members, and lacks a 'private' constructor
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/utils/AutoCreateUtil.java`
#### Snippet
```java
 * Utility class to create queues 'automatically'.
 */
public class AutoCreateUtil {

   private static final Logger logger = LoggerFactory.getLogger(MethodHandles.lookup().lookupClass());
```

### UtilityClassWithoutPrivateConstructor
Class `JNDIUtil` has only 'static' members, and lacks a 'private' constructor
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/utils/JNDIUtil.java`
#### Snippet
```java
import java.util.StringTokenizer;

public class JNDIUtil {

   /**
```

### UtilityClassWithoutPrivateConstructor
Class `ConfigurationHelper` has only 'static' members, and lacks a 'private' constructor
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/utils/ConfigurationHelper.java`
#### Snippet
```java
import org.apache.activemq.artemis.core.client.ActiveMQClientMessageBundle;

public class ConfigurationHelper {

   public static String getStringProperty(final String propName, final String def, final Map<String, ?> props) {
```

### UtilityClassWithoutPrivateConstructor
Class `MemorySize` has only 'static' members, and lacks a 'private' constructor
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/utils/MemorySize.java`
#### Snippet
```java
import org.apache.activemq.artemis.core.client.ActiveMQClientLogger;

public class MemorySize {

   private static final int numberOfObjects = 10000;
```

### UtilityClassWithoutPrivateConstructor
Class `SecurityFormatter` has only 'static' members, and lacks a 'private' constructor
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/utils/SecurityFormatter.java`
#### Snippet
```java
import org.apache.activemq.artemis.core.security.Role;

public class SecurityFormatter {

   public static Set<Role> createSecurity(String sendRoles,
```

### UtilityClassWithoutPrivateConstructor
Class `XidCodecSupport` has only 'static' members, and lacks a 'private' constructor
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/utils/XidCodecSupport.java`
#### Snippet
```java
import static org.apache.activemq.artemis.utils.BufferHelper.safeReadBytes;

public class XidCodecSupport {

   public static void encodeXid(final Xid xid, final ActiveMQBuffer out) {
```

### UtilityClassWithoutPrivateConstructor
Class `BufferHelper` has only 'static' members, and lacks a 'private' constructor
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/utils/BufferHelper.java`
#### Snippet
```java
 * Helper methods to read and write from ActiveMQBuffer.
 */
public class BufferHelper {

   /**
```

### UtilityClassWithoutPrivateConstructor
Class `VersionLoader` has only 'static' members, and lacks a 'private' constructor
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/utils/VersionLoader.java`
#### Snippet
```java
 * This loads the version info in from a version.properties file.
 */
public final class VersionLoader {

   public static final String VERSION_PROP_FILE_KEY = "activemq.version.property.filename";
```

### UtilityClassWithoutPrivateConstructor
Class `EmbedMessageUtil` has only 'static' members, and lacks a 'private' constructor
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/protocol/EmbedMessageUtil.java`
#### Snippet
```java
import java.lang.invoke.MethodHandles;

public class EmbedMessageUtil {


```

### UtilityClassWithoutPrivateConstructor
Class `StringPrep` has only 'static' members, and lacks a 'private' constructor
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/security/scram/StringPrep.java`
#### Snippet
```java
 * StringPrep case folding is unimplemented, as it's not required by SASLPrep.
 */
public class StringPrep {
   /**
    * A representation of sets of character classes.
```

### UtilityClassWithoutPrivateConstructor
Class `Validators` has only 'static' members, and lacks a 'private' constructor
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/impl/Validators.java`
#### Snippet
```java
 * A Validators.
 */
public final class Validators {

   public interface Validator {
```

### UtilityClassWithoutPrivateConstructor
Class `PageReadWriter` has only 'static' members, and lacks a 'private' constructor
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/impl/PageReadWriter.java`
#### Snippet
```java
import java.lang.invoke.MethodHandles;

public class PageReadWriter {


```

### UtilityClassWithoutPrivateConstructor
Class `ActivationParams` has only 'static' members, and lacks a 'private' constructor
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/ActivationParams.java`
#### Snippet
```java
package org.apache.activemq.artemis.core.server;

public class ActivationParams {

   public static final String REPLICATION_ENDPOINT = "REPLICATION_ENDPOINT";
```

### UtilityClassWithoutPrivateConstructor
Class `Factory` has only 'static' members, and lacks a 'private' constructor
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/MessageReference.java`
#### Snippet
```java
public interface MessageReference {

   final class Factory {
      public static MessageReference createReference(Message encode, final Queue queue) {
         return new MessageReferenceImpl(encode, queue);
```

### UtilityClassWithoutPrivateConstructor
Class `QueueConfigurationUtils` has only 'static' members, and lacks a 'private' constructor
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueConfigurationUtils.java`
#### Snippet
```java
import org.apache.activemq.artemis.core.settings.impl.AddressSettings;

public class QueueConfigurationUtils {

   public static void applyDynamicQueueDefaults(QueueConfiguration config, AddressSettings as) {
```

### UtilityClassWithoutPrivateConstructor
Class `QueueMetricNames` has only 'static' members, and lacks a 'private' constructor
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/metrics/QueueMetricNames.java`
#### Snippet
```java
package org.apache.activemq.artemis.core.server.metrics;

public class QueueMetricNames {

   public static final String MESSAGE_COUNT = "message.count";
```

### UtilityClassWithoutPrivateConstructor
Class `AddressMetricNames` has only 'static' members, and lacks a 'private' constructor
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/metrics/AddressMetricNames.java`
#### Snippet
```java
package org.apache.activemq.artemis.core.server.metrics;

public class AddressMetricNames {

   public static final String ROUTED_MESSAGE_COUNT = "routed.message.count";
```

### UtilityClassWithoutPrivateConstructor
Class `BrokerMetricNames` has only 'static' members, and lacks a 'private' constructor
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/metrics/BrokerMetricNames.java`
#### Snippet
```java
package org.apache.activemq.artemis.core.server.metrics;

public class BrokerMetricNames {

   public static final String CONNECTION_COUNT = "connection.count";
```

### UtilityClassWithoutPrivateConstructor
Class `MBeanInfoHelper` has only 'static' members, and lacks a 'private' constructor
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/MBeanInfoHelper.java`
#### Snippet
```java
import org.apache.activemq.artemis.api.core.management.Parameter;

public class MBeanInfoHelper {

   private static ConcurrentHashMap<Class, MBeanAttributeInfo[]> attributesInfoCache = new ConcurrentHashMap<>();
```

### UtilityClassWithoutPrivateConstructor
Class `IntegerCache` has only 'static' members, and lacks a 'private' constructor
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/postoffice/impl/IntegerCache.java`
#### Snippet
```java
import java.util.function.IntFunction;

final class IntegerCache {

   private static final boolean DISABLE_INTEGER_CACHE = Boolean.valueOf(System.getProperty("disable.integer.cache", Boolean.FALSE.toString()));
```

### UtilityClassWithoutPrivateConstructor
Class `BufferSplitter` has only 'static' members, and lacks a 'private' constructor
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/BufferSplitter.java`
#### Snippet
```java

/** this class will split a big buffer into smaller buffers */
public class BufferSplitter {


```

### UtilityClassWithoutPrivateConstructor
Class `JournalRecordIds` has only 'static' members, and lacks a 'private' constructor
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/JournalRecordIds.java`
#### Snippet
```java
 * the IDs.
 */
public final class JournalRecordIds {

   // grouping journal record type
```

### UtilityClassWithoutPrivateConstructor
Class `TransactionPropertyIndexes` has only 'static' members, and lacks a 'private' constructor
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/transaction/TransactionPropertyIndexes.java`
#### Snippet
```java
package org.apache.activemq.artemis.core.transaction;

public class TransactionPropertyIndexes {

   public static final int LARGE_MESSAGE_CONFIRMATIONS = 1;
```

### UtilityClassWithoutPrivateConstructor
Class `SecurityManagerUtil` has only 'static' members, and lacks a 'private' constructor
in `artemis-server/src/main/java/org/apache/activemq/artemis/utils/SecurityManagerUtil.java`
#### Snippet
```java
import org.apache.activemq.artemis.spi.core.security.jaas.RolePrincipal;

public class SecurityManagerUtil {

   private static final String WILDCARD = "*";
```

### UtilityClassWithoutPrivateConstructor
Class `MessageCounterHelper` has only 'static' members, and lacks a 'private' constructor
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/messagecounter/impl/MessageCounterHelper.java`
#### Snippet
```java
import org.apache.activemq.artemis.core.messagecounter.MessageCounter.DayCounter;

public class MessageCounterHelper {


```

## RuleId[id=DataFlowIssue]
### DataFlowIssue
Dereference of `bootdir.listFiles()` may produce `NullPointerException`
in `artemis-boot/src/main/java/org/apache/activemq/artemis/boot/Artemis.java`
#### Snippet
```java
            // Find the jar files in the directory..
            ArrayList<File> files = new ArrayList<>();
            for (File f : bootdir.listFiles()) {
               if (f.getName().endsWith(".jar") || f.getName().endsWith(".zip")) {
                  files.add(f);
```

### DataFlowIssue
Method invocation `toString` may produce `NullPointerException`
in `artemis-protocols/artemis-stomp-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/stomp/StompUtils.java`
#### Snippet
```java
      }
      if (message.containsProperty(Message.HDR_ROUTING_TYPE)) {
         command.addHeader(Stomp.Headers.Send.DESTINATION_TYPE, RoutingType.getType(message.getByteProperty(Message.HDR_ROUTING_TYPE.toString())).toString());
      }
      if (message.containsProperty(Message.HDR_INGRESS_TIMESTAMP)) {
```

### DataFlowIssue
Method invocation `initDecoder` may produce `NullPointerException`
in `artemis-protocols/artemis-stomp-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/stomp/StompConnection.java`
#### Snippet
```java
      if (this.version != (StompVersions.V1_0)) {
         VersionedStompFrameHandler newHandler = VersionedStompFrameHandler.getHandler(this, this.version, scheduledExecutorService, executorFactory);
         newHandler.initDecoder(this.frameHandler);
         this.frameHandler = newHandler;
      }
```

### DataFlowIssue
Method invocation `close` may produce `NullPointerException`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
      } finally {
         try {
            in.close();
         } catch (Exception exc) {
         }
```

### DataFlowIssue
Method invocation `close` may produce `NullPointerException`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
         }
         try {
            out.close();
         } catch (Exception exc) {
         }
```

### DataFlowIssue
Method invocation `close` may produce `NullPointerException`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
      } finally {
         try {
            oos.close();
         } catch (Exception e) {
         }
```

### DataFlowIssue
Method invocation `close` may produce `NullPointerException`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
         }
         try {
            gzos.close();
         } catch (Exception e) {
         }
```

### DataFlowIssue
Method invocation `close` may produce `NullPointerException`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
         }
         try {
            b64os.close();
         } catch (Exception e) {
         }
```

### DataFlowIssue
Method invocation `close` may produce `NullPointerException`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
         }
         try {
            baos.close();
         } catch (Exception e) {
         }
```

### DataFlowIssue
Method invocation `close` may produce `NullPointerException`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
         } finally {
            try {
               gzos.close();
            } catch (Exception e) {
            }
```

### DataFlowIssue
Method invocation `close` may produce `NullPointerException`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
            }
            try {
               b64os.close();
            } catch (Exception e) {
            }
```

### DataFlowIssue
Method invocation `close` may produce `NullPointerException`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
            }
            try {
               baos.close();
            } catch (Exception e) {
            }
```

### DataFlowIssue
Method invocation `close` may produce `NullPointerException`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
            } finally {
               try {
                  baos.close();
               } catch (Exception e) {
               }
```

### DataFlowIssue
Method invocation `close` may produce `NullPointerException`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
               }
               try {
                  gzis.close();
               } catch (Exception e) {
               }
```

### DataFlowIssue
Method invocation `close` may produce `NullPointerException`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
               }
               try {
                  bais.close();
               } catch (Exception e) {
               }
```

### DataFlowIssue
Method invocation `close` may produce `NullPointerException`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
      } finally {
         try {
            bis.close();
         } catch (Exception e) {
         }
```

### DataFlowIssue
Array access `DECODABET[source[srcOffset]]` may produce `NullPointerException`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
            return 3;
         } catch (Exception e) {
            System.out.println("" + source[srcOffset] + ": " + DECODABET[source[srcOffset]]);
            System.out.println("" + source[srcOffset + 1] + ": " + DECODABET[source[srcOffset + 1]]);
            System.out.println("" + source[srcOffset + 2] + ": " + DECODABET[source[srcOffset + 2]]);
```

### DataFlowIssue
Variable is already assigned to this value
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
         success = true;
      } catch (java.io.IOException e) {
         success = false;
      } finally {
         try {
```

### DataFlowIssue
Method invocation `close` may produce `NullPointerException`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
      } finally {
         try {
            bos.close();
         } catch (Exception e) {
         }
```

### DataFlowIssue
Method invocation `close` may produce `NullPointerException`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
      } finally {
         try {
            in.close();
         } catch (Exception exc) {
         }
```

### DataFlowIssue
Method invocation `close` may produce `NullPointerException`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
         }
         try {
            out.close();
         } catch (Exception exc) {
         }
```

### DataFlowIssue
Variable is already assigned to this value
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
      } catch (java.io.IOException e) {

         success = false;
      } finally {
         try {
```

### DataFlowIssue
Method invocation `close` may produce `NullPointerException`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
      } finally {
         try {
            bos.close();
         } catch (Exception e) {
         }
```

### DataFlowIssue
Variable is already assigned to this value
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
      } catch (java.io.IOException | java.lang.ClassNotFoundException e) {
         ActiveMQUtilLogger.LOGGER.failedToDeserializeObject(e);
         obj = null;
      } finally {
         try {
```

### DataFlowIssue
Method invocation `close` may produce `NullPointerException`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
      } finally {
         try {
            bais.close();
         } catch (Exception e) {
         }
```

### DataFlowIssue
Method invocation `close` may produce `NullPointerException`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
         }
         try {
            ois.close();
         } catch (Exception e) {
         }
```

### DataFlowIssue
Method invocation `add` may produce `NullPointerException`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/PriorityCollection.java`
#### Snippet
```java
      if (e == null) return false;
      Collection<E> priority = getCollection(e.getPriority(), true);
      return priority.add(e);
   }

```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/io/aio/ActiveMQFileLock.java`
#### Snippet
```java

   public ActiveMQFileLock(final LibaioFile handle) {
      super((FileChannel) null, 0, 0, false);
      this.file = handle;
   }
```

### DataFlowIssue
Variable is already assigned to this value
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/ObjIntIntArrayList.java`
#### Snippet
```java
      } catch (OutOfMemoryError outOfMemoryError) {
         // restore previous ones
         objects = oldObjects;
         ints = oldInts;
         throw outOfMemoryError;
```

### DataFlowIssue
Variable is already assigned to this value
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/ObjIntIntArrayList.java`
#### Snippet
```java
         // restore previous ones
         objects = oldObjects;
         ints = oldInts;
         throw outOfMemoryError;
      }
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/TypedProperties.java`
#### Snippet
```java
      Object value = doGetProperty(key);
      if (value == null)
         return Float.valueOf(null);
      if (value instanceof Float) {
         return ((Float) value);
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/TypedProperties.java`
#### Snippet
```java
      Object value = doGetProperty(key);
      if (value == null) {
         return Short.valueOf(null);
      } else if (value instanceof Byte) {
         return ((Byte) value).shortValue();
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/TypedProperties.java`
#### Snippet
```java
      Object value = doGetProperty(key);
      if (value == null) {
         return Integer.valueOf(null);
      } else if (value instanceof Integer) {
         return (Integer) value;
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/TypedProperties.java`
#### Snippet
```java
      Object value = doGetProperty(key);
      if (value == null) {
         return Long.valueOf(null);
      } else if (value instanceof Long) {
         return (Long) value;
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/TypedProperties.java`
#### Snippet
```java

   public Byte getByteProperty(final SimpleString key) throws ActiveMQPropertyConversionException {
      return getByteProperty(key, () -> Byte.valueOf(null));
   }

```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/TypedProperties.java`
#### Snippet
```java
      Object value = doGetProperty(key);
      if (value == null) {
         return Double.valueOf(null);
      } else if (value instanceof Float) {
         return ((Float) value).doubleValue();
```

### DataFlowIssue
Method invocation `val` may produce `NullPointerException`
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/LinkedListImpl.java`
#### Snippet
```java
                     e = getNode();

                     return e.val();
                  } else {
                     throw new NoSuchElementException();
```

### DataFlowIssue
Variable is already assigned to this value
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/ActiveMQRAManagedConnection.java`
#### Snippet
```java
      inManagedTx = false;

      inManagedTx = false;

      // I'm recreating the lock object when we return to the pool
```

### DataFlowIssue
Variable is already assigned to this value
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/InputAbstract.java`
#### Snippet
```java
         } catch (NumberFormatException e) {
            e.printStackTrace();
            value = null;
         }
      }
```

### DataFlowIssue
Method invocation `createComponents` may produce `NullPointerException`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Run.java`
#### Snippet
```java
         server = BrokerFactory.createServer(broker.server, securityManager, activateCallback);

         server.createComponents();
         server.getServer().registerActivationFailureListener(exception -> serverActivationFailed.set(exception));

```

### DataFlowIssue
Argument `Artemis.class.getResourceAsStream("banner.txt")` might be null
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/Artemis.java`
#### Snippet
```java

   public static void printBanner(PrintStream out) throws Exception {
      copy(Artemis.class.getResourceAsStream("banner.txt"), out);
   }

```

### DataFlowIssue
Variable is already assigned to this value
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/Artemis.java`
#### Snippet
```java
         builder.withGroup("user").withDescription("default file-based user management (add|rm|list|reset) (example ./artemis user list)").
                 withDefaultCommand(HelpUser.class).withCommands(ListUser.class, AddUser.class, RemoveUser.class, ResetUser.class);
         builder = builder.withCommands(Run.class, Stop.class, Kill.class, PerfJournal.class);
      } else {
         builder.withGroup("data").withDescription("data tools group (print|recover) (example ./artemis data print)").
```

### DataFlowIssue
Variable is already assigned to this value
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/Artemis.java`
#### Snippet
```java
         builder.withGroup("data").withDescription("data tools group (print|recover) (example ./artemis data print)").
            withDefaultCommand(HelpData.class).withCommands(RecoverMessages.class, PrintData.class);
         builder = builder.withCommands(Create.class, Upgrade.class);
      }

```

### DataFlowIssue
Method invocation `replaceAll` may produce `NullPointerException`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/InstallAbstract.java`
#### Snippet
```java
      // that is correct for the current platform.
      String separator = unixTarget && IS_CYGWIN ? "\n" : System.getProperty("line.separator");
      content = content.replaceAll("\\r?\\n", Matcher.quoteReplacement(separator));
      ByteArrayInputStream in = new ByteArrayInputStream(content.getBytes(encoding));
      try (FileOutputStream fout = new FileOutputStream(target)) {
```

### DataFlowIssue
Variable is already assigned to this value
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/xml/XmlDataImporter.java`
#### Snippet
```java
         }
      } else if (address.startsWith(PacketImpl.OLD_TOPIC_PREFIX.toString())) {
         routingType = RoutingType.MULTICAST;
         if (!legacyPrefixes) {
            String newaddress = address.substring(PacketImpl.OLD_TOPIC_PREFIX.length());
```

### DataFlowIssue
Argument `value` might be null
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/xml/XMLMessageImporter.java`
#### Snippet
```java
      switch (propertyType) {
         case XmlDataConstants.PROPERTY_TYPE_SHORT:
            message.putShortProperty(key, Short.parseShort(value));
            break;
         case XmlDataConstants.PROPERTY_TYPE_BOOLEAN:
```

### DataFlowIssue
Argument `value` might be null
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/xml/XMLMessageImporter.java`
#### Snippet
```java
            break;
         case XmlDataConstants.PROPERTY_TYPE_BYTE:
            message.putByteProperty(key, Byte.parseByte(value));
            break;
         case XmlDataConstants.PROPERTY_TYPE_BYTES:
```

### DataFlowIssue
Argument `value` might be null
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/xml/XMLMessageImporter.java`
#### Snippet
```java
            break;
         case XmlDataConstants.PROPERTY_TYPE_DOUBLE:
            message.putDoubleProperty(key, Double.parseDouble(value));
            break;
         case XmlDataConstants.PROPERTY_TYPE_FLOAT:
```

### DataFlowIssue
Argument `value` might be null
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/xml/XMLMessageImporter.java`
#### Snippet
```java
            break;
         case XmlDataConstants.PROPERTY_TYPE_FLOAT:
            message.putFloatProperty(key, Float.parseFloat(value));
            break;
         case XmlDataConstants.PROPERTY_TYPE_INTEGER:
```

### DataFlowIssue
Argument `value` might be null
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/xml/XMLMessageImporter.java`
#### Snippet
```java
            break;
         case XmlDataConstants.PROPERTY_TYPE_INTEGER:
            message.putIntProperty(key, Integer.parseInt(value));
            break;
         case XmlDataConstants.PROPERTY_TYPE_LONG:
```

### DataFlowIssue
Argument `value` might be null
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/xml/XMLMessageImporter.java`
#### Snippet
```java
            break;
         case XmlDataConstants.PROPERTY_TYPE_LONG:
            message.putLongProperty(key, Long.parseLong(value));
            break;
         case XmlDataConstants.PROPERTY_TYPE_SIMPLE_STRING:
```

### DataFlowIssue
Method invocation `toString` may produce `NullPointerException`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/xml/XmlDataExporter.java`
#### Snippet
```java
         xmlWriter.writeAttribute(XmlDataConstants.QUEUE_BINDING_NAME, bindingEncoding.getQueueName().toString());
         xmlWriter.writeAttribute(XmlDataConstants.QUEUE_BINDING_ID, Long.toString(bindingEncoding.getId()));
         xmlWriter.writeAttribute(XmlDataConstants.QUEUE_BINDING_ROUTING_TYPE, RoutingType.getType(bindingEncoding.getRoutingType()).toString());
         bindingsPrinted++;
      }
```

### DataFlowIssue
Method invocation `getMsg` may produce `NullPointerException`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/xml/XmlDataExporter.java`
#### Snippet
```java
         Object o = DescribeJournal.newObjectEncoding(info, storageManager);
         if (info.getUserRecordType() == JournalRecordIds.ADD_MESSAGE) {
            messages.put(info.id, ((MessageDescribe) o).getMsg().toCore());
         } else if (info.getUserRecordType() == JournalRecordIds.ADD_MESSAGE_PROTOCOL) {
            messages.put(info.id, ((MessageDescribe) o).getMsg().toCore());
```

### DataFlowIssue
Method invocation `getMsg` may produce `NullPointerException`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/xml/XmlDataExporter.java`
#### Snippet
```java
            messages.put(info.id, ((MessageDescribe) o).getMsg().toCore());
         } else if (info.getUserRecordType() == JournalRecordIds.ADD_MESSAGE_PROTOCOL) {
            messages.put(info.id, ((MessageDescribe) o).getMsg().toCore());
         } else if (info.getUserRecordType() == JournalRecordIds.ADD_LARGE_MESSAGE) {
            messages.put(info.id, ((MessageDescribe) o).getMsg());
```

### DataFlowIssue
Method invocation `getMsg` may produce `NullPointerException`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/xml/XmlDataExporter.java`
#### Snippet
```java
            messages.put(info.id, ((MessageDescribe) o).getMsg().toCore());
         } else if (info.getUserRecordType() == JournalRecordIds.ADD_LARGE_MESSAGE) {
            messages.put(info.id, ((MessageDescribe) o).getMsg());
         } else if (info.getUserRecordType() == JournalRecordIds.ADD_REF) {
            ReferenceDescribe ref = (ReferenceDescribe) o;
```

### DataFlowIssue
Dereference of `ref` may produce `NullPointerException`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/xml/XmlDataExporter.java`
#### Snippet
```java
            if (map == null) {
               HashMap<Long, ReferenceDescribe> newMap = new HashMap<>();
               newMap.put(ref.refEncoding.queueID, ref);
               messageRefs.put(info.id, newMap);
            } else {
```

### DataFlowIssue
Dereference of `ref` may produce `NullPointerException`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/xml/XmlDataExporter.java`
#### Snippet
```java
               messageRefs.put(info.id, newMap);
            } else {
               map.put(ref.refEncoding.queueID, ref);
            }
         } else if (info.getUserRecordType() == JournalRecordIds.ACKNOWLEDGE_REF) {
```

### DataFlowIssue
Dereference of `ack` may produce `NullPointerException`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/xml/XmlDataExporter.java`
#### Snippet
```java
         HashMap<Long, ReferenceDescribe> referenceDescribeHashMap = messageRefs.get(info.id);
         if (referenceDescribeHashMap != null) {
            referenceDescribeHashMap.remove(ack.refEncoding.queueID);
            if (referenceDescribeHashMap.size() == 0) {
               messages.remove(info.id);
```

### DataFlowIssue
Method invocation `getId` may produce `NullPointerException`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/xml/XmlDataExporter.java`
#### Snippet
```java
         if (info.getUserRecordType() == JournalRecordIds.QUEUE_BINDING_RECORD) {
            PersistentQueueBindingEncoding bindingEncoding = (PersistentQueueBindingEncoding) DescribeJournal.newObjectEncoding(info, null);
            queueBindings.put(bindingEncoding.getId(), bindingEncoding);
         } else if (info.getUserRecordType() == JournalRecordIds.ADDRESS_BINDING_RECORD) {
            PersistentAddressBindingEncoding bindingEncoding = (PersistentAddressBindingEncoding) DescribeJournal.newObjectEncoding(info, null);
```

### DataFlowIssue
Method invocation `getId` may produce `NullPointerException`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/xml/XmlDataExporter.java`
#### Snippet
```java
         } else if (info.getUserRecordType() == JournalRecordIds.ADDRESS_BINDING_RECORD) {
            PersistentAddressBindingEncoding bindingEncoding = (PersistentAddressBindingEncoding) DescribeJournal.newObjectEncoding(info, null);
            addressBindings.put(bindingEncoding.getId(), bindingEncoding);
         }
      }
```

### DataFlowIssue
Method invocation `receive` may produce `NullPointerException`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/Transfer.java`
#### Snippet
```java
         Message receivedMessage;
         if (receiveTimeout < 0) {
            receivedMessage = consumer.receive();
         } else if (receiveTimeout == 0) {
            receivedMessage = consumer.receiveNoWait();
```

### DataFlowIssue
Method invocation `receiveNoWait` may produce `NullPointerException`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/Transfer.java`
#### Snippet
```java
            receivedMessage = consumer.receive();
         } else if (receiveTimeout == 0) {
            receivedMessage = consumer.receiveNoWait();
         } else {
            receivedMessage = consumer.receive(receiveTimeout);
```

### DataFlowIssue
Method invocation `receive` may produce `NullPointerException`
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/Transfer.java`
#### Snippet
```java
            receivedMessage = consumer.receiveNoWait();
         } else {
            receivedMessage = consumer.receive(receiveTimeout);
         }

```

### DataFlowIssue
Unboxing of `BufferHelper.readNullableBoolean(buffer)` may produce `NullPointerException`
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/server/config/impl/ConnectionFactoryConfigurationImpl.java`
#### Snippet
```java
      enable1xPrefixes = buffer.readableBytes() > 0 ? buffer.readBoolean() : ActiveMQJMSClient.DEFAULT_ENABLE_1X_PREFIXES;

      enableSharedClientID = buffer.readableBytes() > 0 ? BufferHelper.readNullableBoolean(buffer) : ActiveMQClient.DEFAULT_ENABLED_SHARED_CLIENT_ID;

      useTopologyForLoadBalancing = buffer.readableBytes() > 0 ? BufferHelper.readNullableBoolean(buffer) : ActiveMQClient.DEFAULT_USE_TOPOLOGY_FOR_LOADBALANCING;
```

### DataFlowIssue
Unboxing of `BufferHelper.readNullableBoolean(buffer)` may produce `NullPointerException`
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/server/config/impl/ConnectionFactoryConfigurationImpl.java`
#### Snippet
```java
      enableSharedClientID = buffer.readableBytes() > 0 ? BufferHelper.readNullableBoolean(buffer) : ActiveMQClient.DEFAULT_ENABLED_SHARED_CLIENT_ID;

      useTopologyForLoadBalancing = buffer.readableBytes() > 0 ? BufferHelper.readNullableBoolean(buffer) : ActiveMQClient.DEFAULT_USE_TOPOLOGY_FOR_LOADBALANCING;
   }

```

### DataFlowIssue
Casting `cf` to `ActiveMQConnectionFactory` may produce `ClassCastException`
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/bridge/impl/JMSBridgeImpl.java`
#### Snippet
```java

         if (conn instanceof ActiveMQConnection) {
            ActiveMQConnectionFactory activeMQCF = (ActiveMQConnectionFactory) cf;
            ha = activeMQCF.isHA();

```

### DataFlowIssue
Variable is already assigned to this value
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/amq/AMQSession.java`
#### Snippet
```java
         } catch (ActiveMQQueueExistsException e) {
            // In case another thread created the queue before us but after we did the binding query
            hasQueue = true;
         }
      }
```

### DataFlowIssue
Casting `replyTo` to `Queue` will produce `ClassCastException` for any non-null value
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenWireMessageConverter.java`
#### Snippet
```java
         } else {
            // it should not happen
            MessageUtil.setJMSReplyTo(coreMessage, org.apache.activemq.artemis.jms.client.ActiveMQDestination.QUEUE_QUALIFIED_PREFIX + (((Queue) replyTo).getQueueName()));
         }
      }
```

### DataFlowIssue
Method invocation `close` may produce `NullPointerException`
in `artemis-unit-test-support/src/main/java/org/apache/activemq/artemis/utils/PortCheckRule.java`
#### Snippet
```java
      } finally {
         try {
            s.close();
         } catch (Throwable ignored) {
         }
```

### DataFlowIssue
Variable is already assigned to this value
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/api/jms/JMSFactoryType.java`
#### Snippet
```java
      switch (this) {
         case CF:
            val = 0;
            break;
         case QUEUE_CF:
```

### DataFlowIssue
Method invocation `addNegative` may produce `NullPointerException`
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalImpl.java`
#### Snippet
```java
               }

               tx.addNegative(usedFile, id);
            } catch (Throwable e) {
               logger.error("Exception during appendDeleteRecordTransactional:", e);
```

### DataFlowIssue
Method invocation `addPositive` may produce `NullPointerException`
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalImpl.java`
#### Snippet
```java
               }

               tx.addPositive(usedFile, id, encodeSize, false);
            } catch (Throwable e) {
               logger.error("Exception during appendAddRecordTransactional:", e);
```

### DataFlowIssue
Dereference of `repeatFiles` may produce `NullPointerException`
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalImpl.java`
#### Snippet
```java
            File[] repeatFiles = historyFolder.listFiles((a, name) -> name.startsWith(getFilePrefix()) && name.endsWith(file.getFileID() + "." + filesRepository.getFileExtension()));

            for (File f : repeatFiles) {
               logger.warn("File {} was partially copied before, removing the file", f);
               f.delete();
```

### DataFlowIssue
Argument `fileNames` might be null
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalImpl.java`
#### Snippet
```java
         if (journalRetentionPeriod > 0) {
            String[] fileNames = journalRetentionFolder.list(fnf);
            Arrays.sort(fileNames);

            GregorianCalendar calendar = this.calendarThreadLocal.get();
```

### DataFlowIssue
Argument `fileNames` might be null
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalImpl.java`
#### Snippet
```java
         if (journalRetentionMaxFiles > 0) {
            String[] fileNames = journalRetentionFolder.list(fnf);
            Arrays.sort(fileNames);

            if (fileNames.length > journalRetentionMaxFiles) {
```

### DataFlowIssue
Method invocation `toString` will produce `NullPointerException`
in `artemis-junit/artemis-junit-commons/src/main/java/org/apache/activemq/artemis/junit/ActiveMQDynamicProducerDelegate.java`
#### Snippet
```java
         if (address == null) {
            throw new ActiveMQClientResourceException(String.format("Error creating producer for address %s",
                                                                    address.toString()),
                                                      amqEx);
         } else {
```

### DataFlowIssue
Casting `original` to `AMQPLargeMessage` will produce `ClassCastException` for any non-null value
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPLargeMessage.java`
#### Snippet
```java

      if (original instanceof LargeServerMessageImpl) {
         this.largeBody.referenceOriginalMessage(((AMQPLargeMessage) original).largeBody);
      }
   }
```

### DataFlowIssue
`null` is returned by the method declared as @NotNull
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/handler/ExecutorNettyAdapter.java`
#### Snippet
```java
   @Override
   public ScheduledFuture<?> scheduleWithFixedDelay(Runnable command, long initialDelay, long delay, TimeUnit unit) {
      return null;
   }

```

### DataFlowIssue
`null` is returned by the method declared as @NotNull
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/handler/ExecutorNettyAdapter.java`
#### Snippet
```java
   @Override
   public <T> T invokeAny(Collection<? extends Callable<T>> tasks) throws InterruptedException, ExecutionException {
      return null;
   }

```

### DataFlowIssue
`null` is returned by the method declared as @NotNull
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/handler/ExecutorNettyAdapter.java`
#### Snippet
```java
                                                             long timeout,
                                                             TimeUnit unit) throws InterruptedException {
      return null;
   }

```

### DataFlowIssue
`null` is returned by the method declared as @NotNull
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/handler/ExecutorNettyAdapter.java`
#### Snippet
```java
   @Override
   public Iterator<EventExecutor> iterator() {
      return null;
   }

```

### DataFlowIssue
`null` is returned by the method declared as @NotNull
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/handler/ExecutorNettyAdapter.java`
#### Snippet
```java
   @Override
   public <V> ScheduledFuture<V> schedule(Callable<V> callable, long delay, TimeUnit unit) {
      return null;
   }

```

### DataFlowIssue
`null` is returned by the method declared as @NotNull
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/handler/ExecutorNettyAdapter.java`
#### Snippet
```java
   @Override
   public ScheduledFuture<?> schedule(Runnable command, long delay, TimeUnit unit) {
      return null;
   }

```

### DataFlowIssue
`null` is returned by the method declared as @NotNull
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/handler/ExecutorNettyAdapter.java`
#### Snippet
```java
   @Override
   public List<Runnable> shutdownNow() {
      return null;
   }

```

### DataFlowIssue
`null` is returned by the method declared as @NotNull
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/handler/ExecutorNettyAdapter.java`
#### Snippet
```java
   public Future<?> submit(Runnable task) {
      execute(task);
      return null;
   }

```

### DataFlowIssue
`null` is returned by the method declared as @NotNull
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/handler/ExecutorNettyAdapter.java`
#### Snippet
```java
   @Override
   public ScheduledFuture<?> scheduleAtFixedRate(Runnable command, long initialDelay, long period, TimeUnit unit) {
      return null;
   }

```

### DataFlowIssue
`null` is returned by the method declared as @NotNull
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/handler/ExecutorNettyAdapter.java`
#### Snippet
```java
   @Override
   public <T> List<java.util.concurrent.Future<T>> invokeAll(Collection<? extends Callable<T>> tasks) throws InterruptedException {
      return null;
   }

```

### DataFlowIssue
`null` is returned by the method declared as @NotNull
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/handler/ExecutorNettyAdapter.java`
#### Snippet
```java
   @Override
   public <T> Future<T> submit(Callable<T> task) {
      return null;
   }

```

### DataFlowIssue
`null` is returned by the method declared as @NotNull
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/handler/ExecutorNettyAdapter.java`
#### Snippet
```java
   public <T> Future<T> submit(Runnable task, T result) {
      execute(task);
      return null;
   }

```

### DataFlowIssue
Variable is already assigned to this value
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
               type = OBJECT_TYPE;
            } else if (AMQPMessageSupport.OCTET_STREAM_CONTENT_TYPE_SYMBOL.equals(contentType)) {
               type = BYTES_TYPE;
            } else {
               Charset charset = getCharsetForTextualContent(contentType.toString());
```

### DataFlowIssue
Variable is already assigned to this value
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
                  type = OBJECT_TYPE;
               } else {
                  type = BYTES_TYPE;
               }
            } else if (value instanceof List) {
```

### DataFlowIssue
Method invocation `addDelivery` may produce `NullPointerException`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/ProtonServerSenderContext.java`
#### Snippet
```java
                     }
                     sessionSPI.ack(tx, brokerConsumer, message);
                     tx.addDelivery(delivery, this);
                  } catch (Exception e) {
                     throw ActiveMQAMQPProtocolMessageBundle.BUNDLE.errorAcknowledgingMessage(message.toString(), e.getMessage());
```

### DataFlowIssue
Method invocation `replicationLineUp` may produce `NullPointerException`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/connect/mirror/AMQPMirrorControllerSource.java`
#### Snippet
```java
            if (tx == null) {
               // notice that if transactional, the context is lined up on beforeCommit as part of the transaction operation
               operContext.replicationLineUp();
            }
            if (logger.isDebugEnabled()) {
```

### DataFlowIssue
Method invocation `replicationLineUp` may produce `NullPointerException`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/connect/mirror/AMQPMirrorControllerSource.java`
#### Snippet
```java
         if (tx == null) {
            // notice that if transactional, the context is lined up on beforeCommit as part of the transaction operation
            operationContext.replicationLineUp();
         }
      }
```

### DataFlowIssue
Unboxing of `internalIDLong` may produce `NullPointerException`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/connect/mirror/AMQPMirrorControllerTarget.java`
#### Snippet
```java
      }

      byte[] duplicateIDBytes = ByteUtil.longToBytes(internalIDLong);

      if (duplicateIDCache.contains(duplicateIDBytes)) {
```

### DataFlowIssue
Casting `currentMirrorController` to `AMQPMirrorControllerAggregation` may produce `ClassCastException`
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/connect/AMQPBrokerConnection.java`
#### Snippet
```java
            server.installMirrorController(remoteAggregation);
         }
         ((AMQPMirrorControllerAggregation) currentMirrorController).addPartition(newPartition);
      }

```

### DataFlowIssue
Method invocation `getSession` may produce `NullPointerException`
in `artemis-web/src/main/java/org/apache/activemq/artemis/component/JolokiaFilter.java`
#### Snippet
```java
      if (AuditLogger.isAnyLoggingEnabled()) {
         try {
            HttpSession session = ((Request) servletRequest).getSession();
            Subject subject = (Subject) session.getAttribute("subject");
            AuditLogger.setCurrentCaller(subject);
```

### DataFlowIssue
Variable is already assigned to this value
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/jgroups/JChannelWrapper.java`
#### Snippet
```java

   public JChannelWrapper(JChannelManager manager, final String channelName, JChannel channel) throws Exception {
      this.refCount = 1;
      this.channelName = channelName;
      this.channel = channel;
```

### DataFlowIssue
Casting `innerVal` to `Object[]` may produce `ClassCastException`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/JsonUtil.java`
#### Snippet
```java
               }
               if (CompositeData.class.getName().equals(key)) {
                  Object[] data = (Object[]) innerVal;
                  CompositeData[] cds = new CompositeData[data.length];
                  for (int i1 = 0; i1 < data.length; i1++) {
```

### DataFlowIssue
Dereference of `data` may produce `NullPointerException`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/JsonUtil.java`
#### Snippet
```java
               if (CompositeData.class.getName().equals(key)) {
                  Object[] data = (Object[]) innerVal;
                  CompositeData[] cds = new CompositeData[data.length];
                  for (int i1 = 0; i1 < data.length; i1++) {
                     String dataConverted = convertJsonValue(data[i1], String.class).toString();
```

### DataFlowIssue
Method invocation `getTotalSize` may produce `NullPointerException`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ClientProducerImpl.java`
#### Snippet
```java
                  msgI.getBodyBuffer().resetReaderIndex();
                  msgI.getBodyBuffer().resetWriterIndex();
                  msgI.putLongProperty(Message.HDR_LARGE_BODY_SIZE, deflaterReader.getTotalSize());

                  msgI.getBodyBuffer().writeBytes(buff, 0, pos);
```

### DataFlowIssue
Method invocation `toPath` may produce `NullPointerException`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/LargeMessageControllerImpl.java`
#### Snippet
```java
         FileChannel channel = cachedChannel;
         if (cachedFile != null || !channel.isOpen()) {
            channel = FileChannel.open(cachedFile.toPath(), StandardOpenOption.CREATE, StandardOpenOption.READ, StandardOpenOption.WRITE);
            cachedChannel = channel;
         }
```

### DataFlowIssue
Method invocation `waitForTopology` may produce `NullPointerException`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ServerLocatorImpl.java`
#### Snippet
```java
      // how the sendSubscription happens.
      // in case this ever changes.
      if (topology != null && !factory.waitForTopology(config.callTimeout, TimeUnit.MILLISECONDS)) {
         factory.cleanup();
         throw ActiveMQClientMessageBundle.BUNDLE.connectionTimedOutOnReceiveTopology(discoveryGroup);
```

### DataFlowIssue
Method invocation `poll` may produce `NullPointerException`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/ChannelImpl.java`
#### Snippet
```java

      for (int i = 0; i < numberToClear; i++) {
         final Packet packet = resendCache.poll();

         if (packet == null) {
```

### DataFlowIssue
Method invocation `setResponseHandler` may produce `NullPointerException`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/ChannelImpl.java`
#### Snippet
```java
         throw new IllegalStateException(msg);
      }
      responseAsyncCache.setResponseHandler(responseHandler);
   }

```

### DataFlowIssue
Method invocation `putChannel` may produce `NullPointerException`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/ChannelImpl.java`
#### Snippet
```java
         // And switch it

         newConnection.putChannel(id, this);

         connection = newConnection;
```

### DataFlowIssue
Method invocation `add` may produce `NullPointerException`
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/ChannelImpl.java`
#### Snippet
```java

   private void addResendPacket(Packet packet) {
      resendCache.add(packet);

      if (logger.isTraceEnabled()) {
```

### DataFlowIssue
Variable is already assigned to this value
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/NettyConnector.java`
#### Snippet
```java
            if (httpRequiresSessionId && !active) {
               if (handshaking) {
                  handshaking = true;
               } else {
                  if (!handShakeFuture.await(5000)) {
```

### DataFlowIssue
Argument `getLDAPPropertyValue(ConfigKey.USER_SEARCH_MATCHING)` might be null
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/security/jaas/LDAPLoginModule.java`
#### Snippet
```java
      }

      MessageFormat userSearchMatchingFormat = new MessageFormat(getLDAPPropertyValue(ConfigKey.USER_SEARCH_MATCHING));
      boolean userSearchSubtreeBool = Boolean.parseBoolean(getLDAPPropertyValue(ConfigKey.USER_SEARCH_SUBTREE));
      boolean ignorePartialResultExceptionBool = Boolean.parseBoolean(getLDAPPropertyValue(ConfigKey.IGNORE_PARTIAL_RESULT_EXCEPTION));
```

### DataFlowIssue
Argument `getLDAPPropertyValue(ConfigKey.ROLE_SEARCH_MATCHING)` might be null
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/security/jaas/LDAPLoginModule.java`
#### Snippet
```java
      }

      MessageFormat roleSearchMatchingFormat = new MessageFormat(getLDAPPropertyValue(ConfigKey.ROLE_SEARCH_MATCHING));
      boolean roleSearchSubtreeBool = Boolean.parseBoolean(getLDAPPropertyValue(ConfigKey.ROLE_SEARCH_SUBTREE));
      boolean expandRolesBool = Boolean.parseBoolean(getLDAPPropertyValue(ConfigKey.EXPAND_ROLES));
```

### DataFlowIssue
Argument `getLDAPPropertyValue(ConfigKey.EXPAND_ROLES_MATCHING)` might be null
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/security/jaas/LDAPLoginModule.java`
#### Snippet
```java
      }
      if (expandRolesBool) {
         MessageFormat expandRolesMatchingFormat = new MessageFormat(getLDAPPropertyValue(ConfigKey.EXPAND_ROLES_MATCHING));
         while (!pendingNameExpansion.isEmpty()) {
            String name = pendingNameExpansion.remove();
```

### DataFlowIssue
Argument `configScope` might be null
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/security/jaas/LDAPLoginModule.java`
#### Snippet
```java
               final String configScope = isLoginPropertySet(ConfigKey.SASL_LOGIN_CONFIG_SCOPE) ? getLDAPPropertyValue(ConfigKey.SASL_LOGIN_CONFIG_SCOPE) : "broker-sasl-gssapi";
               try {
                  LoginContext loginContext = new LoginContext(configScope);
                  loginContext.login();
                  brokerGssapiIdentity = loginContext.getSubject();
```

### DataFlowIssue
Method invocation `replaceFirst` may produce `NullPointerException`
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/security/jaas/LDAPLoginModule.java`
#### Snippet
```java
      context.addToEnvironment(Context.SECURITY_CREDENTIALS, password);
      try {
         String baseDn = getLDAPPropertyValue(ConfigKey.CONNECTION_URL).replaceFirst(".*/", ",");
         String userDn = dn.replace(baseDn, "");
         logger.debug("Get user Attributes with dn {}", userDn);
```

### DataFlowIssue
Variable is already assigned to this value
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/security/jaas/LDAPLoginModule.java`
#### Snippet
```java
         logger.debug("User {} successfully bound.", dn);
      } catch (AuthenticationException e) {
         isValid = false;
         logger.debug("Authentication failed for dn={}", dn);
      }
```

### DataFlowIssue
Method invocation `split` may produce `NullPointerException`
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/security/jaas/LDAPLoginModule.java`
#### Snippet
```java
      codecClass = getLDAPPropertyValue(ConfigKey.PASSWORD_CODEC);
      if (isLoginPropertySet(ConfigKey.NO_CACHE_EXCEPTIONS)) {
         noCacheExceptions = Arrays.asList(getLDAPPropertyValue(ConfigKey.NO_CACHE_EXCEPTIONS).split(","));
         noCacheExceptions.replaceAll(String::trim);
      } else {
```

### DataFlowIssue
Unboxing of `BufferHelper.readNullableDouble(buffer)` may produce `NullPointerException`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/BridgeConfiguration.java`
#### Snippet
```java
      ha = buffer.readNullableBoolean();
      retryInterval = buffer.readNullableLong();
      retryIntervalMultiplier = BufferHelper.readNullableDouble(buffer);
      initialConnectAttempts = buffer.readNullableInt();
      reconnectAttempts = buffer.readNullableInt();
```

### DataFlowIssue
Method invocation `afterCompleteOperations` may produce `NullPointerException`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/impl/Page.java`
#### Snippet
```java
      }

      storageManager.afterCompleteOperations(new IOCallback() {
         @Override
         public void done() {
```

### DataFlowIssue
Variable is already assigned to this value
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/cursor/PagedReferenceImpl.java`
#### Snippet
```java
      } else {
         this.largeMessage = UNDEFINED_IS_LARGE_MESSAGE;
         this.transactionID = -2;
         this.messageID = -1;
         this.messageSize = -1;
```

### DataFlowIssue
Variable is already assigned to this value
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/cursor/PagedReferenceImpl.java`
#### Snippet
```java
         this.largeMessage = UNDEFINED_IS_LARGE_MESSAGE;
         this.transactionID = -2;
         this.messageID = -1;
         this.messageSize = -1;
         this.durable = UNDEFINED_IS_DURABLE;
```

### DataFlowIssue
Variable is already assigned to this value
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/cursor/PagedReferenceImpl.java`
#### Snippet
```java
         this.transactionID = -2;
         this.messageID = -1;
         this.messageSize = -1;
         this.durable = UNDEFINED_IS_DURABLE;
         this.deliveryTime = UNDEFINED_DELIVERY_TIME;
```

### DataFlowIssue
Variable is already assigned to this value
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/cursor/PagedReferenceImpl.java`
#### Snippet
```java
         this.messageID = -1;
         this.messageSize = -1;
         this.durable = UNDEFINED_IS_DURABLE;
         this.deliveryTime = UNDEFINED_DELIVERY_TIME;
      }
```

### DataFlowIssue
Variable is already assigned to this value
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/cursor/PagedReferenceImpl.java`
#### Snippet
```java
         this.messageSize = -1;
         this.durable = UNDEFINED_IS_DURABLE;
         this.deliveryTime = UNDEFINED_DELIVERY_TIME;
      }
   }
```

### DataFlowIssue
Variable is already assigned to this value
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/cursor/impl/PageSubscriptionImpl.java`
#### Snippet
```java
                     if (tx.deliverAfterCommit(CursorIterator.this, PageSubscriptionImpl.this, message)) {
                        valid = false;
                        ignored = false;
                     }
                  }
```

### DataFlowIssue
Method invocation `write` may produce `NullPointerException`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/FileBasedNodeManager.java`
#### Snippet
```java
         final ByteBuffer tmpBuffer = ByteBuffer.allocate(Long.BYTES).order(ByteOrder.BIG_ENDIAN);
         tmpBuffer.putLong(0, sequence);
         channel.write(tmpBuffer, 0);
         channel.force(false);
         setNodeActivationSequence(sequence);
```

### DataFlowIssue
Variable is already assigned to this value
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ReplicationBackupActivation.java`
#### Snippet
```java
                        // no need to retry here, can just restart as backup that will handle a more resilient tryActivate
                        logger.warn("Errored while attempting failover", error);
                        liveLockWithInSyncReplica = null;
                     }
                  } else {
```

### DataFlowIssue
Method invocation `getNodeId` may produce `NullPointerException`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ScaleDownHandler.java`
#### Snippet
```java

   private String getTargetNodeId(ClientSessionFactory sessionFactory) {
      return sessionFactory.getServerLocator().getTopology().getMember(sessionFactory.getConnection()).getNodeId();
   }

```

### DataFlowIssue
Casting `e` to `Exception` may produce `ClassCastException`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerConsumerImpl.java`
#### Snippet
```java
         throw e;
      } catch (Throwable e) {
         ActiveMQServerLogger.LOGGER.errorAckingMessage((Exception) e);
         ActiveMQIllegalStateException hqex = new ActiveMQIllegalStateException(e.getMessage());
         if (startedTransaction) {
```

### DataFlowIssue
Casting `e` to `Exception` may produce `ClassCastException`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerConsumerImpl.java`
#### Snippet
```java
         throw e;
      } catch (Throwable e) {
         ActiveMQServerLogger.LOGGER.errorAckingMessage((Exception) e);
         ActiveMQException activeMQIllegalStateException = new ActiveMQIllegalStateException(e.getMessage());
         if (startedTransaction) {
```

### DataFlowIssue
Variable is already assigned to this value
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/LiveOnlyActivation.java`
#### Snippet
```java
                  clientSessionFactory.close();
               }
               clientSessionFactory = null;
            }
         }
```

### DataFlowIssue
Variable is already assigned to this value
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/LiveOnlyActivation.java`
#### Snippet
```java
                  clientSessionFactory.close();
               }
               clientSessionFactory = null;
               // should I try the backup (i.e. getB()) from possibleLive?
            }
```

### DataFlowIssue
Method invocation `getNodeId` may produce `NullPointerException`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/LiveOnlyActivation.java`
#### Snippet
```java

      RemotingConnection rc = scaleDownClientSessionFactory == null ? null : scaleDownClientSessionFactory.getConnection();
      String nodeID = rc == null ? null : scaleDownClientSessionFactory.getServerLocator().getTopology().getMember(rc).getNodeId();
      if (remotingService != null) {
         remotingService.freeze(nodeID, null);
```

### DataFlowIssue
Method invocation `toString` may produce `NullPointerException`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/plugin/impl/LoggingActiveMQServerPlugin.java`
#### Snippet
```java
               @Override
               public void afterRollback(Transaction tx) {
                  LoggingActiveMQServerPluginLogger.LOGGER.rolledBackTransaction(tx, ref.toString());
               }
            });
```

### DataFlowIssue
Method invocation `getTime` may produce `NullPointerException`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/replay/ReplayManager.java`
#### Snippet
```java
               String datePortion = journal.getDatePortion(fileName);
               logger.debug("Evaluating replay for file {}, datePortion={}\n\tInterval evaluated: start({}) --- file({}) --- end({})\n\tepoch times: start({}) --- file({}) + end({})",
                             fileName, datePortion, start, new Date(fileEpochTime), end, start.getTime(), fileEpochTime, end.getTime());
            }

```

### DataFlowIssue
Method invocation `getTime` may produce `NullPointerException`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/replay/ReplayManager.java`
#### Snippet
```java
               String datePortion = journal.getDatePortion(fileName);
               logger.debug("Evaluating replay for file {}, datePortion={}\n\tInterval evaluated: start({}) --- file({}) --- end({})\n\tepoch times: start({}) --- file({}) + end({})",
                             fileName, datePortion, start, new Date(fileEpochTime), end, start.getTime(), fileEpochTime, end.getTime());
            }

```

### DataFlowIssue
Method invocation `verify` may produce `NullPointerException`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/ClusterController.java`
#### Snippet
```java
               ClusterConnectMessage msg = (ClusterConnectMessage) packet;

               if (server.getConfiguration().isSecurityEnabled() && !clusterConnection.verify(msg.getClusterUser(), msg.getClusterPassword())) {
                  clusterChannel.send(new ClusterConnectReplyMessage(false));
               } else {
```

### DataFlowIssue
Variable is already assigned to this value
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/ha/ReplicaPolicy.java`
#### Snippet
```java
      this.voteOnReplicationFailure = voteOnReplicationFailure;
      this.quorumVoteWait = quorumVoteWait;
      this.retryReplicationWait = retryReplicationWait;
   }

```

### DataFlowIssue
Argument `name` might be null
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/impl/ClusterConnectionBridge.java`
#### Snippet
```java
      this.discoveryLocator = discoveryLocator;

      idsHeaderName = Message.HDR_ROUTE_TO_IDS.concat(name);

      this.clusterConnection = clusterConnection;
```

### DataFlowIssue
Method invocation `getId` may produce `NullPointerException`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/metrics/MetricsManager.java`
#### Snippet
```java
         for (Meter meter : resourceMeters) {
            Meter removed = meterRegistry.remove(meter);
            logger.debug("Unregistered meter: {}", removed.getId());
         }
      } else {
```

### DataFlowIssue
Casting `haPolicyConfiguration` to `ReplicatedPolicyConfiguration` will produce `ClassCastException` for any non-null value
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/impl/ConfigurationImpl.java`
#### Snippet
```java
   public boolean isCheckForLiveServer() {
      if (haPolicyConfiguration instanceof ReplicaPolicyConfiguration) {
         return ((ReplicatedPolicyConfiguration) haPolicyConfiguration).isCheckForLiveServer();
      } else {
         return false;
```

### DataFlowIssue
Casting `haPolicyConfiguration` to `ReplicatedPolicyConfiguration` will produce `ClassCastException` for any non-null value
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/impl/ConfigurationImpl.java`
#### Snippet
```java
   public ConfigurationImpl setCheckForLiveServer(boolean checkForLiveServer) {
      if (haPolicyConfiguration instanceof ReplicaPolicyConfiguration) {
         ((ReplicatedPolicyConfiguration) haPolicyConfiguration).setCheckForLiveServer(checkForLiveServer);
      }

```

### DataFlowIssue
Array access `params[0]` may produce `NullPointerException`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/BasicAuthenticator.java`
#### Snippet
```java
         params = (String[]) credentials;
      }
      result = securityManager.authenticate(params[0], params[1], null, null);
      if (result != null) {
         if (AuditLogger.isResourceLoggingEnabled()) {
```

### DataFlowIssue
Variable is already assigned to this value
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/impl/ManagementServiceImpl.java`
#### Snippet
```java
      storageManager = null;

      messagingServer = null;

      registeredNames.clear();
```

### DataFlowIssue
Method invocation `getID` may produce `NullPointerException`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/security/impl/SecurityStoreImpl.java`
#### Snippet
```java

      if (AuditLogger.isResourceLoggingEnabled()) {
         AuditLogger.userFailedAuthenticationInAudit(null, e.getMessage(), connection.getID().toString());
      }

```

### DataFlowIssue
Method invocation `getRemoteAddress` may produce `NullPointerException`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/security/impl/SecurityStoreImpl.java`
#### Snippet
```java
         }
         if (AuditLogger.isResourceLoggingEnabled()) {
            AuditLogger.userSuccesfullyAuthenticatedInAudit(subject, connection.getRemoteAddress(), connection.getID().toString());
         }

```

### DataFlowIssue
Method invocation `getFile` may produce `NullPointerException`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/JGroupsFileBroadcastGroupControlImpl.java`
#### Snippet
```java
      }
      URL resource = this.getClass().getClassLoader().getResource(this.getFile());
      File file = new File(resource.getFile());
      return new String(Files.readAllBytes(Paths.get(file.getPath())));
   }
```

### DataFlowIssue
Method invocation `equals` will produce `NullPointerException`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/NettyAcceptor.java`
#### Snippet
```java
         return;
      }
      if (keyStorePath == null && TransportConstants.DEFAULT_KEYSTORE_PROVIDER.equals(keyStoreProvider)) {
         throw new IllegalArgumentException("If \"" + TransportConstants.SSL_ENABLED_PROP_NAME + "\" is true then \"" + TransportConstants.KEYSTORE_PATH_PROP_NAME + "\" must be non-null unless an alternative \"" + TransportConstants.KEYSTORE_PROVIDER_PROP_NAME + "\" has been specified.");
      }
```

### DataFlowIssue
Method invocation `hashCode` may produce `NullPointerException`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/AddressSettings.java`
#### Snippet
```java
      result = prime * result + ((expiryAddress == null) ? 0 : expiryAddress.hashCode());
      result = prime * result + ((expiryDelay == null) ? 0 : expiryDelay.hashCode());
      result = prime * result + ((minExpiryDelay == null) ? 0 : expiryDelay.hashCode());
      result = prime * result + ((maxExpiryDelay == null) ? 0 : expiryDelay.hashCode());
      result = prime * result + ((defaultLastValueQueue == null) ? 0 : defaultLastValueQueue.hashCode());
```

### DataFlowIssue
Method invocation `hashCode` may produce `NullPointerException`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/AddressSettings.java`
#### Snippet
```java
      result = prime * result + ((expiryDelay == null) ? 0 : expiryDelay.hashCode());
      result = prime * result + ((minExpiryDelay == null) ? 0 : expiryDelay.hashCode());
      result = prime * result + ((maxExpiryDelay == null) ? 0 : expiryDelay.hashCode());
      result = prime * result + ((defaultLastValueQueue == null) ? 0 : defaultLastValueQueue.hashCode());
      result = prime * result + ((defaultLastValueKey == null) ? 0 : defaultLastValueKey.hashCode());
```

### DataFlowIssue
Dereference of `holder` may produce `NullPointerException`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/OperationContextImpl.java`
#### Snippet
```java
      for (int i = 0; i < size; i++) {
         final TaskHolder holder = tasks.peek();
         if (stored < holder.storeLined || replicated < holder.replicationLined || paged < holder.pageLined) {
            // End of list here. No other task will be completed after this
            return;
```

### DataFlowIssue
Dereference of `holder` may produce `NullPointerException`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/OperationContextImpl.java`
#### Snippet
```java
      for (int i = 0; i < size; i++) {
         final StoreOnlyTaskHolder holder = storeOnlyTasks.peek();
         if (stored < holder.storeLined) {
            // fail fast: storeOnlyTasks are ordered by storeLined, there is no need to continue
            return;
```

### DataFlowIssue
Dereference of `holder` may produce `NullPointerException`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/OperationContextImpl.java`
#### Snippet
```java
         for (int i = 0; i < size; i++) {
            final TaskHolder holder = tasks.poll();
            holder.task.onError(errorCode, errorMessage);
         }
      }
```

### DataFlowIssue
Method invocation `close` may produce `NullPointerException`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/LargeBody.java`
#### Snippet
```java
         if (closeFile) {
            try {
               file.close(false, false);
            } catch (Exception ignored) {
            }
```

### DataFlowIssue
Variable is already assigned to this value
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/codec/DuplicateIDEncoding.java`
#### Snippet
```java
            bridgeRepresentation = "nodeUUID=" + uuid.toString() + " messageID=" + id;
         } catch (Throwable ignored) {
            bridgeRepresentation = null;
         }
      }
```

### DataFlowIssue
Argument `getTrimmedTextContent(child)` might be null
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/deployers/impl/FileConfigurationParser.java`
#### Snippet
```java
         final String name = child.getNodeName();
         if (DEAD_LETTER_ADDRESS_NODE_NAME.equalsIgnoreCase(name)) {
            SimpleString queueName = new SimpleString(getTrimmedTextContent(child));
            addressSettings.setDeadLetterAddress(queueName);
         } else if (EXPIRY_ADDRESS_NODE_NAME.equalsIgnoreCase(name)) {
```

### DataFlowIssue
Argument `getTrimmedTextContent(child)` might be null
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/deployers/impl/FileConfigurationParser.java`
#### Snippet
```java
            addressSettings.setDeadLetterAddress(queueName);
         } else if (EXPIRY_ADDRESS_NODE_NAME.equalsIgnoreCase(name)) {
            SimpleString queueName = new SimpleString(getTrimmedTextContent(child));
            addressSettings.setExpiryAddress(queueName);
         } else if (EXPIRY_DELAY_NODE_NAME.equalsIgnoreCase(name)) {
```

### DataFlowIssue
Argument `getTrimmedTextContent(child)` might be null
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/deployers/impl/FileConfigurationParser.java`
#### Snippet
```java
            addressSettings.setPageSizeBytes((int) pageSizeLong);
         }  else if (MAX_READ_PAGE_MESSAGES_NODE_NAME.equalsIgnoreCase(name)) {
            long maxReadPageMessages = Long.parseLong(getTrimmedTextContent(child));
            Validators.MINUS_ONE_OR_POSITIVE_INT.validate(MAX_READ_PAGE_MESSAGES_NODE_NAME, maxReadPageMessages);
            addressSettings.setMaxReadPageMessages((int)maxReadPageMessages);
```

### DataFlowIssue
Argument `value` might be null
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/deployers/impl/FileConfigurationParser.java`
#### Snippet
```java
            String value = getTrimmedTextContent(child);
            Validators.ADDRESS_FULL_MESSAGE_POLICY_TYPE.validate(ADDRESS_FULL_MESSAGE_POLICY_NODE_NAME, value);
            AddressFullMessagePolicy policy = Enum.valueOf(AddressFullMessagePolicy.class, value);
            addressSettings.setAddressFullMessagePolicy(policy);
         } else if (PAGE_FULL_MESSAGE_POLICY_NODE_NAME.equalsIgnoreCase(name)) {
```

### DataFlowIssue
Argument `value` might be null
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/deployers/impl/FileConfigurationParser.java`
#### Snippet
```java
            String value = getTrimmedTextContent(child);
            Validators.PAGE_FULL_MESSAGE_POLICY_TYPE.validate(PAGE_FULL_MESSAGE_POLICY_NODE_NAME, value);
            PageFullMessagePolicy policy = Enum.valueOf(PageFullMessagePolicy.class, value);
            addressSettings.setPageFullMessagePolicy(policy);
         } else if (LVQ_NODE_NAME.equalsIgnoreCase(name) || DEFAULT_LVQ_NODE_NAME.equalsIgnoreCase(name)) {
```

### DataFlowIssue
Argument `value` might be null
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/deployers/impl/FileConfigurationParser.java`
#### Snippet
```java
            String value = getTrimmedTextContent(child);
            Validators.SLOW_CONSUMER_POLICY_TYPE.validate(SLOW_CONSUMER_POLICY_NODE_NAME, value);
            SlowConsumerPolicy policy = Enum.valueOf(SlowConsumerPolicy.class, value);
            addressSettings.setSlowConsumerPolicy(policy);
         } else if (AUTO_CREATE_JMS_QUEUES.equalsIgnoreCase(name)) {
```

### DataFlowIssue
Argument `value` might be null
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/deployers/impl/FileConfigurationParser.java`
#### Snippet
```java
            String value = getTrimmedTextContent(child);
            Validators.DELETION_POLICY_TYPE.validate(CONFIG_DELETE_QUEUES, value);
            DeletionPolicy policy = Enum.valueOf(DeletionPolicy.class, value);
            addressSettings.setConfigDeleteQueues(policy);
         } else if (AUTO_CREATE_ADDRESSES.equalsIgnoreCase(name)) {
```

### DataFlowIssue
Argument `value` might be null
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/deployers/impl/FileConfigurationParser.java`
#### Snippet
```java
            String value = getTrimmedTextContent(child);
            Validators.DELETION_POLICY_TYPE.validate(CONFIG_DELETE_ADDRESSES, value);
            DeletionPolicy policy = Enum.valueOf(DeletionPolicy.class, value);
            addressSettings.setConfigDeleteAddresses(policy);
         } else if (CONFIG_DELETE_DIVERTS.equalsIgnoreCase(name)) {
```

### DataFlowIssue
Argument `value` might be null
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/deployers/impl/FileConfigurationParser.java`
#### Snippet
```java
            String value = getTrimmedTextContent(child);
            Validators.DELETION_POLICY_TYPE.validate(CONFIG_DELETE_DIVERTS, value);
            DeletionPolicy policy = Enum.valueOf(DeletionPolicy.class, value);
            addressSettings.setConfigDeleteDiverts(policy);
         } else if (MANAGEMENT_BROWSE_PAGE_SIZE.equalsIgnoreCase(name)) {
```

### DataFlowIssue
Argument `getTrimmedTextContent(child)` might be null
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/deployers/impl/FileConfigurationParser.java`
#### Snippet
```java
            addressSettings.setAutoCreateDeadLetterResources(XMLUtil.parseBoolean(child));
         } else if (DEAD_LETTER_QUEUE_PREFIX_NODE_NAME.equalsIgnoreCase(name)) {
            addressSettings.setDeadLetterQueuePrefix(new SimpleString(getTrimmedTextContent(child)));
         } else if (DEAD_LETTER_QUEUE_SUFFIX_NODE_NAME.equalsIgnoreCase(name)) {
            addressSettings.setDeadLetterQueueSuffix(new SimpleString(getTrimmedTextContent(child)));
```

### DataFlowIssue
Argument `getTrimmedTextContent(child)` might be null
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/deployers/impl/FileConfigurationParser.java`
#### Snippet
```java
            addressSettings.setDeadLetterQueuePrefix(new SimpleString(getTrimmedTextContent(child)));
         } else if (DEAD_LETTER_QUEUE_SUFFIX_NODE_NAME.equalsIgnoreCase(name)) {
            addressSettings.setDeadLetterQueueSuffix(new SimpleString(getTrimmedTextContent(child)));
         } else if (AUTO_CREATE_EXPIRY_RESOURCES_NODE_NAME.equalsIgnoreCase(name)) {
            addressSettings.setAutoCreateExpiryResources(XMLUtil.parseBoolean(child));
```

### DataFlowIssue
Argument `getTrimmedTextContent(child)` might be null
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/deployers/impl/FileConfigurationParser.java`
#### Snippet
```java
            addressSettings.setAutoCreateExpiryResources(XMLUtil.parseBoolean(child));
         } else if (EXPIRY_QUEUE_PREFIX_NODE_NAME.equalsIgnoreCase(name)) {
            addressSettings.setExpiryQueuePrefix(new SimpleString(getTrimmedTextContent(child)));
         } else if (EXPIRY_QUEUE_SUFFIX_NODE_NAME.equalsIgnoreCase(name)) {
            addressSettings.setExpiryQueueSuffix(new SimpleString(getTrimmedTextContent(child)));
```

### DataFlowIssue
Argument `getTrimmedTextContent(child)` might be null
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/deployers/impl/FileConfigurationParser.java`
#### Snippet
```java
            addressSettings.setExpiryQueuePrefix(new SimpleString(getTrimmedTextContent(child)));
         } else if (EXPIRY_QUEUE_SUFFIX_NODE_NAME.equalsIgnoreCase(name)) {
            addressSettings.setExpiryQueueSuffix(new SimpleString(getTrimmedTextContent(child)));
         } else if (ENABLE_METRICS.equalsIgnoreCase(name)) {
            addressSettings.setEnableMetrics(XMLUtil.parseBoolean(child));
```

### DataFlowIssue
Argument `propertyValue` might be null
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/deployers/impl/FileConfigurationParser.java`
#### Snippet
```java
               propertyValue = PasswordMaskingUtil.resolveMask(mainConfig.isMaskPassword(), propertyValue, mainConfig.getPasswordCodec());
            }
            conf.addDataSourceProperty(propertyName, propertyValue);
         }
      }
```

### DataFlowIssue
Method invocation `trim` may produce `NullPointerException`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/deployers/impl/FileConfigurationParser.java`
#### Snippet
```java
   private Map<String, Set<String>> parseSecurityRoleMapping(Node item) {
      Map<String, Set<String>> mappedRoleNames = new HashMap<>();
      String externalRoleName = getAttributeValue(item, ROLE_FROM_ATTR_NAME).trim();
      Set<String> internalRoleNames = new HashSet<>();
      Collections.addAll(internalRoleNames, getAttributeValue(item, ROLE_TO_ATTR_NAME).split(","));
```

### DataFlowIssue
Method invocation `split` may produce `NullPointerException`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/deployers/impl/FileConfigurationParser.java`
#### Snippet
```java
      String externalRoleName = getAttributeValue(item, ROLE_FROM_ATTR_NAME).trim();
      Set<String> internalRoleNames = new HashSet<>();
      Collections.addAll(internalRoleNames, getAttributeValue(item, ROLE_TO_ATTR_NAME).split(","));
      for (String internalRoleName : internalRoleNames) {
         internalRoleName = internalRoleName.trim();
```

### DataFlowIssue
Method invocation `split` may produce `NullPointerException`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/deployers/impl/FileConfigurationParser.java`
#### Snippet
```java
            final String type = getAttributeValue(child, TYPE_ATTR_NAME);
            final String roleString = getAttributeValue(child, ROLES_ATTR_NAME);
            String[] roles = roleString.split(",");
            String[] mappedRoles = getMappedRoleNames(roles, roleMappings);

```

### DataFlowIssue
Method invocation `replicationLineUp` may produce `NullPointerException`
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/replication/ReplicationManager.java`
#### Snippet
```java
      final OperationContext repliToken = OperationContextImpl.getContext(ioExecutorFactory);
      if (lineUp) {
         repliToken.replicationLineUp();
      }
      final ReplicatePacketRequest request = new ReplicatePacketRequest(packet, repliToken, done);
```

### DataFlowIssue
Argument `oldRouteToIDs` might be null
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java
      }

      ByteBuffer oldBuffer = ByteBuffer.wrap(oldRouteToIDs);

      RoutingContext routingContext = new RoutingContextImpl(tx);
```

### DataFlowIssue
Argument `server` might be null
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java

      this.address = queueConfiguration.getAddress();
      this.refCountForConsumers = queueConfiguration.isTransient() ? new TransientQueueManagerImpl(server, queueConfiguration.getName()) : new QueueManagerImpl(server, queueConfiguration.getName());

      this.addressInfo = postOffice == null ? null : postOffice.getAddressInfo(address);
```

### DataFlowIssue
Argument `server` might be null
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java

      this.address = queueConfiguration.getAddress();
      this.refCountForConsumers = queueConfiguration.isTransient() ? new TransientQueueManagerImpl(server, queueConfiguration.getName()) : new QueueManagerImpl(server, queueConfiguration.getName());

      this.addressInfo = postOffice == null ? null : postOffice.getAddressInfo(address);
```

## RuleId[id=DeprecatedIsStillUsed]
### DeprecatedIsStillUsed
Deprecated member 'TRACE' is still used
in `artemis-log-annotation-processor/src/main/java/org/apache/activemq/artemis/logs/annotation/LogMessage.java`
#### Snippet
```java
       */
      @Deprecated
      TRACE;
   }

```

### DeprecatedIsStillUsed
Deprecated member 'DURABLE_SUBSCRIBER_NAME' is still used
in `artemis-protocols/artemis-stomp-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/stomp/Stomp.java`
#### Snippet
```java

         @Deprecated
         String DURABLE_SUBSCRIBER_NAME = "durable-subscriber-name";

         String DURABLE_SUBSCRIPTION_NAME = "durable-subscription-name";
```

### DeprecatedIsStillUsed
Deprecated member 'DURABLE_SUBSCRIBER_NAME' is still used
in `artemis-protocols/artemis-stomp-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/stomp/Stomp.java`
#### Snippet
```java

         @Deprecated
         String DURABLE_SUBSCRIBER_NAME = "durable-subscriber-name";

         String DURABLE_SUBSCRIPTION_NAME = "durable-subscription-name";
```

### DeprecatedIsStillUsed
Deprecated member 'WhiteListDTO' is still used
in `artemis-dto/src/main/java/org/apache/activemq/artemis/dto/WhiteListDTO.java`
#### Snippet
```java
@XmlAccessorType(XmlAccessType.FIELD)
@Deprecated
public class WhiteListDTO {

   @XmlElementRef
```

### DeprecatedIsStillUsed
Deprecated member 'getWhiteList' is still used
in `artemis-dto/src/main/java/org/apache/activemq/artemis/dto/AuthorisationDTO.java`
#### Snippet
```java

   @Deprecated
   public WhiteListDTO getWhiteList() {
      return whitelist;
   }
```

### DeprecatedIsStillUsed
Deprecated member 'keyStorePassword' is still used
in `artemis-dto/src/main/java/org/apache/activemq/artemis/dto/WebServerDTO.java`
#### Snippet
```java
   @Deprecated
   @XmlAttribute
   private String keyStorePassword;

   @Deprecated
```

### DeprecatedIsStillUsed
Deprecated member 'trustStorePassword' is still used
in `artemis-dto/src/main/java/org/apache/activemq/artemis/dto/WebServerDTO.java`
#### Snippet
```java
   @Deprecated
   @XmlAttribute
   private String trustStorePassword;

   @Deprecated
```

### DeprecatedIsStillUsed
Deprecated member 'excludedTLSProtocols' is still used
in `artemis-dto/src/main/java/org/apache/activemq/artemis/dto/WebServerDTO.java`
#### Snippet
```java
   @Deprecated
   @XmlAttribute
   private String excludedTLSProtocols;

   @Deprecated
```

### DeprecatedIsStillUsed
Deprecated member 'apps' is still used
in `artemis-dto/src/main/java/org/apache/activemq/artemis/dto/WebServerDTO.java`
#### Snippet
```java
   @Deprecated
   @XmlElementRef
   public List<AppDTO> apps;

   @XmlElementRef(required = false)
```

### DeprecatedIsStillUsed
Deprecated member 'includedCipherSuites' is still used
in `artemis-dto/src/main/java/org/apache/activemq/artemis/dto/WebServerDTO.java`
#### Snippet
```java
   @Deprecated
   @XmlAttribute
   private String includedCipherSuites;

   @Deprecated
```

### DeprecatedIsStillUsed
Deprecated member 'includedTLSProtocols' is still used
in `artemis-dto/src/main/java/org/apache/activemq/artemis/dto/WebServerDTO.java`
#### Snippet
```java
   @Deprecated
   @XmlAttribute
   private String includedTLSProtocols;

   @Deprecated
```

### DeprecatedIsStillUsed
Deprecated member 'excludedCipherSuites' is still used
in `artemis-dto/src/main/java/org/apache/activemq/artemis/dto/WebServerDTO.java`
#### Snippet
```java
   @Deprecated
   @XmlAttribute
   private String excludedCipherSuites;

   @XmlAttribute
```

### DeprecatedIsStillUsed
Deprecated member 'clientAuth' is still used
in `artemis-dto/src/main/java/org/apache/activemq/artemis/dto/WebServerDTO.java`
#### Snippet
```java
   @Deprecated
   @XmlAttribute
   public Boolean clientAuth;

   @Deprecated
```

### DeprecatedIsStillUsed
Deprecated member 'getQueueAttributes' is still used
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQDestination.java`
#### Snippet
```java

   @Deprecated
   public QueueAttributes getQueueAttributes() {
      return QueueAttributes.fromQueueConfiguration(queueConfiguration);
   }
```

### DeprecatedIsStillUsed
Deprecated member 'address' is still used
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQDestination.java`
#### Snippet
```java
    */
   @Deprecated
   private String address;

   private String name;
```

### DeprecatedIsStillUsed
Deprecated member 'setDeliveryAnnotationsForSendBuffer' is still used
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
    */
   @Deprecated
   public final void setDeliveryAnnotationsForSendBuffer(DeliveryAnnotations deliveryAnnotations) {
      this.deliveryAnnotationsForSendBuffer = deliveryAnnotations;
   }
```

### DeprecatedIsStillUsed
Deprecated member 'ConnectionLifeCycleListener' is still used
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/spi/core/remoting/ConnectionLifeCycleListener.java`
#### Snippet
```java
 */
@Deprecated
public interface ConnectionLifeCycleListener extends BaseConnectionLifeCycleListener<String> {

}
```

### DeprecatedIsStillUsed
Deprecated member 'getDefaultMessageExpiryThreadPriority' is still used
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/config/ActiveMQDefaultConfiguration.java`
#### Snippet
```java
    */
   @Deprecated
   public static int getDefaultMessageExpiryThreadPriority() {
      return DEFAULT_MESSAGE_EXPIRY_THREAD_PRIORITY;
   }
```

### DeprecatedIsStillUsed
Deprecated member 'getSSLContext' is still used
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/spi/core/remoting/ssl/SSLContextFactory.java`
#### Snippet
```java
   @SuppressWarnings("unused")
   @Deprecated
   default SSLContext getSSLContext(Map<String, Object> configuration,
           String keystoreProvider, String keystorePath, String keystoreType, String keystorePassword,
           String truststoreProvider, String truststorePath, String truststoreType, String truststorePassword,
```

### DeprecatedIsStillUsed
Deprecated member 'STOMP_CONSUMERS_CREDIT' is still used
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/TransportConstants.java`
#### Snippet
```java

   @Deprecated
   public static final String STOMP_CONSUMERS_CREDIT = "stompConsumerCredits";

   public static final String STOMP_CONSUMER_WINDOW_SIZE = "stompConsumerWindowSize";
```

### DeprecatedIsStillUsed
Deprecated member 'PROTOCOL_PROP_NAME' is still used
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/TransportConstants.java`
#### Snippet
```java
    */
   @Deprecated
   public static final String PROTOCOL_PROP_NAME = "protocol";

   public static final String PROTOCOLS_PROP_NAME = "protocols";
```

### DeprecatedIsStillUsed
Deprecated member 'USE_NIO_GLOBAL_WORKER_POOL_PROP_NAME' is still used
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/TransportConstants.java`
#### Snippet
```java
    * @deprecated Use USE_GLOBAL_WORKER_POOL_PROP_NAME
    */
   public static final String USE_NIO_GLOBAL_WORKER_POOL_PROP_NAME = "useNioGlobalWorkerPool";

   public static final String USE_GLOBAL_WORKER_POOL_PROP_NAME = "useGlobalWorkerPool";
```

### DeprecatedIsStillUsed
Deprecated member 'STOMP_MAX_FRAME_PAYLOAD_LENGTH' is still used
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/TransportConstants.java`
#### Snippet
```java
    */
   @Deprecated
   public static final String STOMP_MAX_FRAME_PAYLOAD_LENGTH = "stompMaxFramePayloadLength";

   public static final String WEB_SOCKET_MAX_FRAME_PAYLOAD_LENGTH = "webSocketMaxFramePayloadLength";
```

### DeprecatedIsStillUsed
Deprecated member 'STOMP_ENABLE_MESSAGE_ID_DEPRECATED' is still used
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/TransportConstants.java`
#### Snippet
```java

   @Deprecated
   public static final String STOMP_ENABLE_MESSAGE_ID_DEPRECATED = "stomp-enable-message-id";

   public static final String STOMP_ENABLE_MESSAGE_ID = "stompEnableMessageId";
```

### DeprecatedIsStillUsed
Deprecated member 'NIO_REMOTING_THREADS_PROPNAME' is still used
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/TransportConstants.java`
#### Snippet
```java
    * @deprecated Use REMOTING_THREADS_PROPNAME
    */
   public static final String NIO_REMOTING_THREADS_PROPNAME = "nioRemotingThreads";

   public static final String WRITE_BUFFER_LOW_WATER_MARK_PROPNAME = "writeBufferLowWaterMark";
```

### DeprecatedIsStillUsed
Deprecated member 'STOMP_MIN_LARGE_MESSAGE_SIZE_DEPRECATED' is still used
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/TransportConstants.java`
#### Snippet
```java

   @Deprecated
   public static final String STOMP_MIN_LARGE_MESSAGE_SIZE_DEPRECATED = "stomp-min-large-message-size";

   public static final String STOMP_MIN_LARGE_MESSAGE_SIZE = "stompMinLargeMessageSize";
```

### DeprecatedIsStillUsed
Deprecated member 'CoreQueueConfiguration' is still used
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/CoreQueueConfiguration.java`
#### Snippet
```java

@Deprecated
public class CoreQueueConfiguration implements Serializable {

   private static final long serialVersionUID = 650404974977490254L;
```

### DeprecatedIsStillUsed
Deprecated member 'ConnectorService' is still used
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/ConnectorService.java`
#### Snippet
```java

@Deprecated
public interface ConnectorService extends ActiveMQComponent {

   String getName();
```

### DeprecatedIsStillUsed
Deprecated member 'listMessageCounterHistoryAsHTML' is still used
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/management/QueueControl.java`
#### Snippet
```java
   @Deprecated
   @Operation(desc = "List the message counters history HTML", impact = MBeanOperationInfo.INFO)
   String listMessageCounterHistoryAsHTML() throws Exception;

   /**
```

### DeprecatedIsStillUsed
Deprecated member 'QueueConfig' is still used
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/QueueConfig.java`
#### Snippet
```java

@Deprecated
public final class QueueConfig {

   private final long id;
```

### DeprecatedIsStillUsed
Deprecated member 'EmbeddedJMSResource' is still used
in `artemis-junit/artemis-junit-4/src/main/java/org/apache/activemq/artemis/junit/EmbeddedJMSResource.java`
#### Snippet
```java
 */
@Deprecated
public class EmbeddedJMSResource extends ExternalResource implements EmbeddedJMSOperations<EmbeddedJMSResource> {

   private static final Logger logger = LoggerFactory.getLogger(MethodHandles.lookup().lookupClass());
```

### DeprecatedIsStillUsed
Deprecated member 'DEFAULT_FAILOVER_ON_INITIAL_CONNECTION' is still used
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/client/ActiveMQClient.java`
#### Snippet
```java

   @Deprecated
   public static final boolean DEFAULT_FAILOVER_ON_INITIAL_CONNECTION = false;

   public static final boolean DEFAULT_IS_HA = false;
```

### DeprecatedIsStillUsed
Deprecated member 'messageAcknowledged' is still used
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/plugin/ActiveMQServerMessagePlugin.java`
#### Snippet
```java
    */
   @Deprecated
   default void messageAcknowledged(MessageReference ref, AckReason reason, ServerConsumer consumer) throws ActiveMQException {
      //by default call the old method for backwards compatibility
      this.messageAcknowledged(ref, reason);
```

### DeprecatedIsStillUsed
Deprecated member 'beforeDeliver' is still used
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/plugin/ActiveMQServerMessagePlugin.java`
#### Snippet
```java
    */
   @Deprecated
   default void beforeDeliver(MessageReference reference) throws ActiveMQException {

   }
```

### DeprecatedIsStillUsed
Deprecated member 'afterDeliver' is still used
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/plugin/ActiveMQServerMessagePlugin.java`
#### Snippet
```java
    */
   @Deprecated
   default void afterDeliver(MessageReference reference) throws ActiveMQException {

   }
```

### DeprecatedIsStillUsed
Deprecated member 'messageExpired' is still used
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/plugin/ActiveMQServerMessagePlugin.java`
#### Snippet
```java
    */
   @Deprecated
   default void messageExpired(MessageReference message, SimpleString messageExpiryAddress) throws ActiveMQException {

   }
```

### DeprecatedIsStillUsed
Deprecated member 'beforeDestroyQueue' is still used
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/plugin/ActiveMQServerQueuePlugin.java`
#### Snippet
```java
    */
   @Deprecated
   default void beforeDestroyQueue(SimpleString queueName, final SecurityAuth session, boolean checkConsumerCount,
                                   boolean removeConsumers, boolean autoDeleteAddress) throws ActiveMQException {

```

### DeprecatedIsStillUsed
Deprecated member 'beforeCreateConsumer' is still used
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/plugin/ActiveMQServerConsumerPlugin.java`
#### Snippet
```java
    */
   @Deprecated
   default void beforeCreateConsumer(long consumerID, SimpleString queueName, SimpleString filterString,
                                     boolean browseOnly, boolean supportLargeMessage) throws ActiveMQException {

```

### DeprecatedIsStillUsed
Deprecated member 'ActiveMQSecurityManagerImpl' is still used
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/security/ActiveMQSecurityManagerImpl.java`
#### Snippet
```java
 */
@Deprecated
public class ActiveMQSecurityManagerImpl implements ActiveMQSecurityManager {

   private final SecurityConfiguration configuration;
```

### DeprecatedIsStillUsed
Deprecated member 'coreQueueConfigurations' is still used
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/impl/ConfigurationImpl.java`
#### Snippet
```java
   @Deprecated
   // this can eventually be replaced with List<QueueConfiguration>, but to keep existing semantics it must stay as is for now
   private List<CoreQueueConfiguration> coreQueueConfigurations = new ArrayList<>();

   private List<CoreAddressConfiguration> addressConfigurations = new ArrayList<>();
```

### DeprecatedIsStillUsed
Deprecated member 'addAddressesSetting' is still used
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/impl/ConfigurationImpl.java`
#### Snippet
```java
   @Override
   @Deprecated
   public ConfigurationImpl addAddressesSetting(String key, AddressSettings addressesSetting) {
      return addAddressSetting(key, addressesSetting);
   }
```

### DeprecatedIsStillUsed
Deprecated member 'getAddressesSettings' is still used
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/impl/ConfigurationImpl.java`
#### Snippet
```java
   @Override
   @Deprecated
   public Map<String, AddressSettings> getAddressesSettings() {
      return getAddressSettings();
   }
```

### DeprecatedIsStillUsed
Deprecated member 'getMetricsPlugin' is still used
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/Configuration.java`
#### Snippet
```java

   @Deprecated
   ActiveMQMetricsPlugin getMetricsPlugin();

   MetricsConfiguration getMetricsConfiguration();
```

### DeprecatedIsStillUsed
Deprecated member 'setEnabledAsyncConnectionExecution' is still used
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/Configuration.java`
#### Snippet
```java
    */
   @Deprecated
   Configuration setEnabledAsyncConnectionExecution(boolean enabled);

   /**
```

### DeprecatedIsStillUsed
Deprecated member 'setMetricsPlugin' is still used
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/Configuration.java`
#### Snippet
```java

   @Deprecated
   Configuration setMetricsPlugin(ActiveMQMetricsPlugin plugin);

   Configuration setMetricsConfiguration(MetricsConfiguration metricsConfiguration);
```

### DeprecatedIsStillUsed
Deprecated member 'getAddressesSettings' is still used
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/Configuration.java`
#### Snippet
```java

   @Deprecated
   Map<String, AddressSettings> getAddressesSettings();

   @Deprecated
```

### DeprecatedIsStillUsed
Deprecated member 'addAddressesSetting' is still used
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/Configuration.java`
#### Snippet
```java

   @Deprecated
   Configuration addAddressesSetting(String key, AddressSettings addressesSetting);

   @Deprecated
```

### DeprecatedIsStillUsed
Deprecated member 'setAutoDeleteJmsTopics' is still used
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/AddressSettings.java`
#### Snippet
```java

   @Deprecated
   public AddressSettings setAutoDeleteJmsTopics(final boolean autoDeleteJmsTopics) {
      this.autoDeleteJmsTopics = autoDeleteJmsTopics;
      return this;
```

### DeprecatedIsStillUsed
Deprecated member 'DEFAULT_AUTO_CREATE_TOPICS' is still used
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/AddressSettings.java`
#### Snippet
```java

   @Deprecated
   public static final boolean DEFAULT_AUTO_CREATE_TOPICS = true;

   @Deprecated
```

### DeprecatedIsStillUsed
Deprecated member 'setAutoCreateJmsTopics' is still used
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/AddressSettings.java`
#### Snippet
```java

   @Deprecated
   public AddressSettings setAutoCreateJmsTopics(final boolean autoCreateJmsTopics) {
      this.autoCreateJmsTopics = autoCreateJmsTopics;
      return this;
```

### DeprecatedIsStillUsed
Deprecated member 'autoDeleteJmsQueues' is still used
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/AddressSettings.java`
#### Snippet
```java

   @Deprecated
   private Boolean autoDeleteJmsQueues = null;

   @Deprecated
```

### DeprecatedIsStillUsed
Deprecated member 'setAutoDeleteJmsQueues' is still used
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/AddressSettings.java`
#### Snippet
```java

   @Deprecated
   public AddressSettings setAutoDeleteJmsQueues(final boolean autoDeleteJmsQueues) {
      this.autoDeleteJmsQueues = autoDeleteJmsQueues;
      return this;
```

### DeprecatedIsStillUsed
Deprecated member 'autoCreateJmsQueues' is still used
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/AddressSettings.java`
#### Snippet
```java

   @Deprecated
   private Boolean autoCreateJmsQueues = null;

   @Deprecated
```

### DeprecatedIsStillUsed
Deprecated member 'autoCreateJmsTopics' is still used
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/AddressSettings.java`
#### Snippet
```java

   @Deprecated
   private Boolean autoCreateJmsTopics = null;

   @Deprecated
```

### DeprecatedIsStillUsed
Deprecated member 'DEFAULT_AUTO_DELETE_TOPICS' is still used
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/AddressSettings.java`
#### Snippet
```java

   @Deprecated
   public static final boolean DEFAULT_AUTO_DELETE_TOPICS = true;

   public static final boolean DEFAULT_AUTO_CREATE_QUEUES = true;
```

### DeprecatedIsStillUsed
Deprecated member 'DEFAULT_AUTO_CREATE_JMS_QUEUES' is still used
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/AddressSettings.java`
#### Snippet
```java

   @Deprecated
   public static final boolean DEFAULT_AUTO_CREATE_JMS_QUEUES = true;

   @Deprecated
```

### DeprecatedIsStillUsed
Deprecated member 'setAutoCreateJmsQueues' is still used
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/AddressSettings.java`
#### Snippet
```java

   @Deprecated
   public AddressSettings setAutoCreateJmsQueues(final boolean autoCreateJmsQueues) {
      this.autoCreateJmsQueues = autoCreateJmsQueues;
      return this;
```

### DeprecatedIsStillUsed
Deprecated member 'autoDeleteJmsTopics' is still used
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/AddressSettings.java`
#### Snippet
```java

   @Deprecated
   private Boolean autoDeleteJmsTopics = null;

   private Boolean autoCreateQueues = null;
```

### DeprecatedIsStillUsed
Deprecated member 'DEFAULT_AUTO_DELETE_JMS_QUEUES' is still used
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/AddressSettings.java`
#### Snippet
```java

   @Deprecated
   public static final boolean DEFAULT_AUTO_DELETE_JMS_QUEUES = true;

   @Deprecated
```

## RuleId[id=Convert2MethodRef]
### Convert2MethodRef
Lambda can be replaced with method reference
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/actors/HandlerBase.java`
#### Snippet
```java

   /** an actor could be used within an OrderedExecutor. So we need this counter to decide if there's a Handler anywhere in the stack trace */
   private static final ThreadLocal<Counter> inHandler = ThreadLocal.withInitial(() -> new Counter());

   protected static void enter() {
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/ActionContext.java`
#### Snippet
```java
   public PrintStream err;

   private static ThreadLocal<ActionContext> contextThreadLocal = ThreadLocal.withInitial(() -> new ActionContext());

   public static void setSystem(ActionContext context) {
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Run.java`
#### Snippet
```java

         server.createComponents();
         server.getServer().registerActivationFailureListener(exception -> serverActivationFailed.set(exception));

         if (properties == null) {
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Upgrade.java`
#### Snippet
```java
         try (Stream<String> originalLines = Files.lines(targetFile.toPath())) {
            originalLines.forEach(line -> {
               originalConsumer.accept(line);
            });
         }
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/check/CheckContext.java`
#### Snippet
```java
         topology = Arrays.stream(NodeInfo.from((String)managementProxy.
            invokeOperation("broker", "listNetworkTopology", null, null, 0))).
            collect(Collectors.toMap(node -> node.getId(), node -> node));
      }

```

### Convert2MethodRef
Lambda can be replaced with method reference
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/xml/XMLMessageImporter.java`
#### Snippet
```java
         logger.debug("Creating temp file {} for large message.", tempFileName);
         try (OutputStream out = new BufferedOutputStream(new FileOutputStream(tempFileName))) {
            getMessageBodyBytes(bytes -> out.write(bytes), (message.toCore().getType() == Message.TEXT_TYPE) && decodeTextMessage);
         }
         FileInputStream fileInputStream = new FileInputStream(tempFileName);
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/server/impl/JMSServerManagerImpl.java`
#### Snippet
```java
   @Override
   public String listPreparedTransactionDetailsAsHTML() throws Exception {
      return server.getActiveMQServerControl().listPreparedTransactionDetailsAsHTML((xid, tx, creation) -> new JMSTransactionDetail(xid, tx, creation));
   }

```

### Convert2MethodRef
Lambda can be replaced with method reference
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/server/impl/JMSServerManagerImpl.java`
#### Snippet
```java
   @Override
   public String listPreparedTransactionDetailsAsJSON() throws Exception {
      return server.getActiveMQServerControl().listPreparedTransactionDetailsAsJSON((xid, tx, creation) -> new JMSTransactionDetail(xid, tx, creation));
   }

```

### Convert2MethodRef
Lambda can be replaced with method reference
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalImpl.java`
#### Snippet
```java
               // Restore newRecords created during compacting
               localCompactor.getNewRecords().forEach((id, newRecord) -> {
                  records.put(id, newRecord);
               });

```

### Convert2MethodRef
Lambda can be replaced with method reference
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalImpl.java`
#### Snippet
```java
   private ThreadPoolExecutor threadPool;

   ThreadLocal<GregorianCalendar> calendarThreadLocal = ThreadLocal.withInitial(() -> new GregorianCalendar());

   /**
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `artemis-quorum-ri/src/main/java/org/apache/activemq/artemis/quorum/zookeeper/CuratorDistributedPrimitiveManager.java`
#### Snippet
```java
            case LOST:
               unavailable = true;
               listeners.forEach(listener -> listener.onUnavailableManagerEvent());
               primitives.forEach((id, primitive) -> primitive.onLost());
               break;
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/connect/AMQPBrokerConnection.java`
#### Snippet
```java
         return;
      }
      connectExecutor.execute(() -> doConnect());
   }

```

### Convert2MethodRef
Lambda can be replaced with method reference
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/connect/AMQPBrokerConnection.java`
#### Snippet
```java
               logger.debug("Reconnecting in {}, this is the {} of {}", brokerConnectConfiguration.getRetryInterval(), retryCounter, brokerConnectConfiguration.getReconnectAttempts());
            }
            reconnectFuture = scheduledExecutorService.schedule(() -> connectExecutor.execute(() -> doConnect()), brokerConnectConfiguration.getRetryInterval(), TimeUnit.MILLISECONDS);
         } else {
            retryCounter = 0;
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/cursor/impl/PageSubscriptionImpl.java`
#### Snippet
```java
               return;
            }
            localScanList = scanList.stream().toArray(i -> new PageScan[i]);
            scanList.clear();
         }
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/metrics/NettyPooledAllocatorMetrics.java`
#### Snippet
```java
       */
      final String poolArenaIndexString = Integer.toString(poolArenaIndex);
      Gauge.builder("netty.pooled.arena.threadcaches.num", poolArenaMetric, metric -> metric.numThreadCaches())
         .tags("pool_arena_type", poolArenaType, "pool_arena_index", poolArenaIndexString)
         .description("The number of thread caches backed by this arena")
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/metrics/NettyPooledAllocatorMetrics.java`
#### Snippet
```java

      FunctionCounter.builder("netty.pooled.arena.allocations.num", poolArenaMetric,
         metric -> metric.numAllocations())
         .tags("pool_arena_type", poolArenaType, "pool_arena_index", poolArenaIndexString, "size", "all")
         .description("The number of allocations done via the arena. This includes all sizes")
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/metrics/NettyPooledAllocatorMetrics.java`
#### Snippet
```java

      FunctionCounter.builder("netty.pooled.arena.allocations.num", poolArenaMetric,
         metric -> metric.numTinyAllocations())
         .tags("pool_arena_type", poolArenaType, "pool_arena_index", poolArenaIndexString, "size", "tiny")
         .description("The number of tiny allocations done via the arena")
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/metrics/NettyPooledAllocatorMetrics.java`
#### Snippet
```java

      FunctionCounter.builder("netty.pooled.arena.allocations.num", poolArenaMetric,
         metric -> metric.numSmallAllocations())
         .tags("pool_arena_type", poolArenaType, "pool_arena_index", poolArenaIndexString, "size", "small")
         .description("The number of small allocations done via the arena")
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/metrics/NettyPooledAllocatorMetrics.java`
#### Snippet
```java

      FunctionCounter.builder("netty.pooled.arena.allocations.num", poolArenaMetric,
         metric -> metric.numNormalAllocations())
         .tags("pool_arena_type", poolArenaType, "pool_arena_index", poolArenaIndexString, "size", "normal")
         .description("The number of normal allocations done via the arena")
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/metrics/NettyPooledAllocatorMetrics.java`
#### Snippet
```java

      FunctionCounter.builder("netty.pooled.arena.allocations.num", poolArenaMetric,
         metric -> metric.numHugeAllocations())
         .tags("pool_arena_type", poolArenaType, "pool_arena_index", poolArenaIndexString, "size", "huge")
         .description("The number of huge allocations done via the arena")
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/metrics/NettyPooledAllocatorMetrics.java`
#### Snippet
```java

      FunctionCounter.builder("netty.pooled.arena.deallocations.num", poolArenaMetric,
         metric -> metric.numDeallocations())
         .tags("pool_arena_type", poolArenaType, "pool_arena_index", poolArenaIndexString, "size", "all")
         .description("The number of deallocations done via the arena. This includes all sizes")
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/metrics/NettyPooledAllocatorMetrics.java`
#### Snippet
```java

      FunctionCounter.builder("netty.pooled.arena.deallocations.num", poolArenaMetric,
         metric -> metric.numTinyDeallocations())
         .tags("pool_arena_type", poolArenaType, "pool_arena_index", poolArenaIndexString, "size", "tiny")
         .description("The number of tiny deallocations done via the arena")
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/metrics/NettyPooledAllocatorMetrics.java`
#### Snippet
```java

      FunctionCounter.builder("netty.pooled.arena.deallocations.num", poolArenaMetric,
         metric -> metric.numSmallDeallocations())
         .tags("pool_arena_type", poolArenaType, "pool_arena_index", poolArenaIndexString, "size", "small")
         .description("The number of small deallocations done via the arena")
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/metrics/NettyPooledAllocatorMetrics.java`
#### Snippet
```java

      FunctionCounter.builder("netty.pooled.arena.deallocations.num", poolArenaMetric,
         metric -> metric.numNormalDeallocations())
         .tags("pool_arena_type", poolArenaType, "pool_arena_index", poolArenaIndexString, "size", "normal")
         .description("The number of normal deallocations done via the arena")
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/metrics/NettyPooledAllocatorMetrics.java`
#### Snippet
```java

      FunctionCounter.builder("netty.pooled.arena.deallocations.num", poolArenaMetric,
         metric -> metric.numHugeDeallocations())
         .tags("pool_arena_type", poolArenaType, "pool_arena_index", poolArenaIndexString, "size", "huge")
         .description("The number of huge deallocations done via the arena")
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/metrics/NettyPooledAllocatorMetrics.java`
#### Snippet
```java

      Gauge.builder("netty.pooled.arena.active.allocations.num", poolArenaMetric,
         metric -> metric.numActiveAllocations())
         .tags("pool_arena_type", poolArenaType, "pool_arena_index", poolArenaIndexString, "size", "all")
         .description("The number of currently active allocations")
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/metrics/NettyPooledAllocatorMetrics.java`
#### Snippet
```java

      Gauge.builder("netty.pooled.arena.active.allocations.num", poolArenaMetric,
         metric -> metric.numActiveTinyAllocations())
         .tags("pool_arena_type", poolArenaType, "pool_arena_index", poolArenaIndexString, "size", "tiny")
         .description("The number of currently active tiny allocations")
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/metrics/NettyPooledAllocatorMetrics.java`
#### Snippet
```java

      Gauge.builder("netty.pooled.arena.active.allocations.num", poolArenaMetric,
         metric -> metric.numActiveSmallAllocations())
         .tags("pool_arena_type", poolArenaType, "pool_arena_index", poolArenaIndexString, "size", "small")
         .description("The number of currently active small allocations")
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/metrics/NettyPooledAllocatorMetrics.java`
#### Snippet
```java

      Gauge.builder("netty.pooled.arena.active.allocations.num", poolArenaMetric,
         metric -> metric.numActiveNormalAllocations())
         .tags("pool_arena_type", poolArenaType, "pool_arena_index", poolArenaIndexString, "size", "normal")
         .description("The number of currently active normal allocations")
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/metrics/NettyPooledAllocatorMetrics.java`
#### Snippet
```java

      Gauge.builder("netty.pooled.arena.active.allocations.num", poolArenaMetric,
         metric -> metric.numActiveHugeAllocations())
         .tags("pool_arena_type", poolArenaType, "pool_arena_index", poolArenaIndexString, "size", "huge")
         .description("The number of currently active huge allocations")
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/metrics/NettyPooledAllocatorMetrics.java`
#### Snippet
```java

      Gauge.builder("netty.pooled.arena.active.allocated.num", poolArenaMetric,
         metric -> metric.numActiveBytes())
         .tags("pool_arena_type", poolArenaType, "pool_arena_index", poolArenaIndexString)
         .description("The number of active bytes that are currently allocated by the arena")
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/metrics/NettyPooledAllocatorMetrics.java`
#### Snippet
```java

      Gauge.builder("netty.pooled.arena.chunk.num", poolArenaMetric,
         metric -> metric.numChunkLists())
         .tags("pool_arena_type", poolArenaType, "pool_arena_index", poolArenaIndexString)
         .description("The number of chunk lists for the arena")
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/metrics/NettyPooledAllocatorMetrics.java`
#### Snippet
```java

      Gauge.builder("netty.pooled.arena.subpages.num", poolArenaMetric,
         metric -> metric.numTinySubpages())
         .tags("pool_arena_type", poolArenaType, "pool_arena_index", poolArenaIndexString, "size", "tiny")
         .description("The number of tiny sub-pages for the arena")
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/metrics/NettyPooledAllocatorMetrics.java`
#### Snippet
```java

      Gauge.builder("netty.pooled.arena.subpages.num", poolArenaMetric,
         metric -> metric.numSmallSubpages())
         .tags("pool_arena_type", poolArenaType, "pool_arena_index", poolArenaIndexString, "size", "small")
         .description("The number of small sub-pages for the arena")
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/metrics/NettyPooledAllocatorMetrics.java`
#### Snippet
```java
   public void bindTo(final MeterRegistry registry) {

      Gauge.builder("netty.pooled.used.memory", this.metric, metric -> metric.usedDirectMemory())
         .tags("type", "direct")
         .description("The used memory")
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/metrics/NettyPooledAllocatorMetrics.java`
#### Snippet
```java
         .baseUnit(BYTES_UNIT).register(registry);

      Gauge.builder("netty.pooled.used.memory", this.metric, metric -> metric.usedHeapMemory())
         .tags("type", "heap")
         .description("The used memory")
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/metrics/NettyPooledAllocatorMetrics.java`
#### Snippet
```java
         .baseUnit(BYTES_UNIT).register(registry);

      Gauge.builder("netty.pooled.arenas.num", this.metric, metric -> metric.numDirectArenas())
         .tags("type", "direct")
         .description("The number of arenas")
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/metrics/NettyPooledAllocatorMetrics.java`
#### Snippet
```java
         .register(registry);

      Gauge.builder("netty.pooled.arenas.num", this.metric, metric -> metric.numHeapArenas())
         .tags("type", "heap").description("The number or arenas")
         .register(registry);
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/metrics/NettyPooledAllocatorMetrics.java`
#### Snippet
```java
         .register(registry);

      Gauge.builder("netty.pooled.cachesize", this.metric, metric -> metric.tinyCacheSize())
         .tags("type", "tiny")
         .description("The cachesize used by this netty allocator")
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/metrics/NettyPooledAllocatorMetrics.java`
#### Snippet
```java
         .register(registry);

      Gauge.builder("netty.pooled.cachesize", this.metric, metric -> metric.smallCacheSize())
         .tags("type", "small")
         .description("The cachesize used by this netty allocator")
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/metrics/NettyPooledAllocatorMetrics.java`
#### Snippet
```java
         .register(registry);

      Gauge.builder("netty.pooled.cachesize", this.metric, metric -> metric.normalCacheSize())
         .tags("type", "normal")
         .description("The cachesize used by this netty allocator")
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/metrics/NettyPooledAllocatorMetrics.java`
#### Snippet
```java
         .register(registry);

      Gauge.builder("netty.pooled.threadlocalcache.num", this.metric, metric -> metric.numThreadLocalCaches())
         .description("The number of thread local caches used by this netty allocator")
         .register(registry);
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/metrics/NettyPooledAllocatorMetrics.java`
#### Snippet
```java
         .register(registry);

      Gauge.builder("netty.pooled.chunk.size", this.metric, metric -> metric.chunkSize())
         .description("The arena chunk size of this netty allocator")
         .baseUnit(BYTES_UNIT)
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/routing/pools/AbstractPool.java`
#### Snippet
```java
   @Override
   public List<Target> getTargets() {
      List<Target> targets = targetMonitors.stream().filter(targetMonitor -> targetMonitor.isTargetReady())
         .map(targetMonitor -> targetMonitor.getTarget()).collect(Collectors.toList());

```

### Convert2MethodRef
Lambda can be replaced with method reference
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/routing/pools/AbstractPool.java`
#### Snippet
```java
   public List<Target> getTargets() {
      List<Target> targets = targetMonitors.stream().filter(targetMonitor -> targetMonitor.isTargetReady())
         .map(targetMonitor -> targetMonitor.getTarget()).collect(Collectors.toList());

      if (quorumTimeout > 0 && targets.size() < quorumSize) {
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/routing/pools/AbstractPool.java`
#### Snippet
```java
         final long deadline = System.nanoTime() + quorumTimeoutNanos;
         while (targets.size() < quorumSize && (System.nanoTime() - deadline) < 0) {
            targets = targetMonitors.stream().filter(targetMonitor -> targetMonitor.isTargetReady())
               .map(targetMonitor -> targetMonitor.getTarget()).collect(Collectors.toList());

```

### Convert2MethodRef
Lambda can be replaced with method reference
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/routing/pools/AbstractPool.java`
#### Snippet
```java
         while (targets.size() < quorumSize && (System.nanoTime() - deadline) < 0) {
            targets = targetMonitors.stream().filter(targetMonitor -> targetMonitor.isTargetReady())
               .map(targetMonitor -> targetMonitor.getTarget()).collect(Collectors.toList());

            LockSupport.parkNanos(quorumParkNanos);
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/routing/pools/AbstractPool.java`
#### Snippet
```java
   @Override
   public List<Target> getAllTargets() {
      return targetMonitors.stream().map(targetMonitor -> targetMonitor.getTarget()).collect(Collectors.toList());
   }

```

### Convert2MethodRef
Lambda can be replaced with method reference
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/routing/policies/PolicyFactoryResolver.java`
#### Snippet
```java

   private PolicyFactoryResolver() {
      policyFactories.put(ConsistentHashPolicy.NAME, () -> new ConsistentHashPolicy());
      policyFactories.put(FirstElementPolicy.NAME, () -> new FirstElementPolicy());
      policyFactories.put(LeastConnectionsPolicy.NAME, () -> new LeastConnectionsPolicy());
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/routing/policies/PolicyFactoryResolver.java`
#### Snippet
```java
   private PolicyFactoryResolver() {
      policyFactories.put(ConsistentHashPolicy.NAME, () -> new ConsistentHashPolicy());
      policyFactories.put(FirstElementPolicy.NAME, () -> new FirstElementPolicy());
      policyFactories.put(LeastConnectionsPolicy.NAME, () -> new LeastConnectionsPolicy());
      policyFactories.put(RoundRobinPolicy.NAME, () -> new RoundRobinPolicy());
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/routing/policies/PolicyFactoryResolver.java`
#### Snippet
```java
      policyFactories.put(ConsistentHashPolicy.NAME, () -> new ConsistentHashPolicy());
      policyFactories.put(FirstElementPolicy.NAME, () -> new FirstElementPolicy());
      policyFactories.put(LeastConnectionsPolicy.NAME, () -> new LeastConnectionsPolicy());
      policyFactories.put(RoundRobinPolicy.NAME, () -> new RoundRobinPolicy());
      policyFactories.put(ConsistentHashModuloPolicy.NAME, () -> new ConsistentHashModuloPolicy());
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/routing/policies/PolicyFactoryResolver.java`
#### Snippet
```java
      policyFactories.put(FirstElementPolicy.NAME, () -> new FirstElementPolicy());
      policyFactories.put(LeastConnectionsPolicy.NAME, () -> new LeastConnectionsPolicy());
      policyFactories.put(RoundRobinPolicy.NAME, () -> new RoundRobinPolicy());
      policyFactories.put(ConsistentHashModuloPolicy.NAME, () -> new ConsistentHashModuloPolicy());

```

### Convert2MethodRef
Lambda can be replaced with method reference
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/routing/policies/PolicyFactoryResolver.java`
#### Snippet
```java
      policyFactories.put(LeastConnectionsPolicy.NAME, () -> new LeastConnectionsPolicy());
      policyFactories.put(RoundRobinPolicy.NAME, () -> new RoundRobinPolicy());
      policyFactories.put(ConsistentHashModuloPolicy.NAME, () -> new ConsistentHashModuloPolicy());

      loadPolicyFactories();
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ActiveMQServerImpl.java`
#### Snippet
```java

   private List<Queue> listConfiguredQueues(SimpleString address) throws Exception {
      return listQueues(address).stream().filter(queue -> queue.isConfigurationManaged()).collect(Collectors.toList());
   }

```

### Convert2MethodRef
Lambda can be replaced with method reference
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/impl/ConfigurationImpl.java`
#### Snippet
```java
   @Override
   public void registerBrokerPlugins(final List<ActiveMQServerBasePlugin> plugins) {
      plugins.forEach(plugin -> registerBrokerPlugin(plugin));
   }

```

### Convert2MethodRef
Lambda can be replaced with method reference
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/protocol/core/ServerSessionPacketHandler.java`
#### Snippet
```java
      // no confirmation window size means no resend cache hence NullResponsePackets
      // won't get cached on it because need confirmation
      poolNullResponse = new MpscPool<>(this.channel.getConfirmationWindowSize() == -1 ? MAX_CACHED_NULL_RESPONSES : 0, NullResponseMessage::reset, () -> new NullResponseMessage());
      poolNullResponseV2 = new MpscPool<>(this.channel.getConfirmationWindowSize() == -1 ? MAX_CACHED_NULL_RESPONSES : 0, NullResponseMessage_V2::reset, () -> new NullResponseMessage_V2());
   }
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/protocol/core/ServerSessionPacketHandler.java`
#### Snippet
```java
      // won't get cached on it because need confirmation
      poolNullResponse = new MpscPool<>(this.channel.getConfirmationWindowSize() == -1 ? MAX_CACHED_NULL_RESPONSES : 0, NullResponseMessage::reset, () -> new NullResponseMessage());
      poolNullResponseV2 = new MpscPool<>(this.channel.getConfirmationWindowSize() == -1 ? MAX_CACHED_NULL_RESPONSES : 0, NullResponseMessage_V2::reset, () -> new NullResponseMessage_V2());
   }

```

### Convert2MethodRef
Lambda can be replaced with method reference
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/view/predicate/ConnectionFilterPredicate.java`
#### Snippet
```java
            return matches(connection.getClientID());
         case USERS:
            return matchAny(collectFromSessions(connection.getID().toString(), s-> s.getUsername()));
         case PROTOCOL:
            return matches(connection.getProtocolName());
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/postoffice/impl/SimpleAddressManager.java`
#### Snippet
```java
   @Override
   public Stream<Binding> getBindings() {
      return nameMap.values().stream().map(pair -> pair.getA());
   }

```

### Convert2MethodRef
Lambda can be replaced with method reference
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/ActiveMQServerControlImpl.java`
#### Snippet
```java
   @Override
   public String listPreparedTransactionDetailsAsHTML() throws Exception {
      return listPreparedTransactionDetailsAsHTML((xid, tx, creation) -> new CoreTransactionDetail(xid, tx, creation));
   }

```

### Convert2MethodRef
Lambda can be replaced with method reference
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/ActiveMQServerControlImpl.java`
#### Snippet
```java
   @Override
   public String listPreparedTransactionDetailsAsJSON() throws Exception {
      return listPreparedTransactionDetailsAsJSON((xid, tx, creation) -> new CoreTransactionDetail(xid, tx, creation));
   }

```

## RuleId[id=ComparatorCombinators]
### ComparatorCombinators
Can be replaced with 'Comparator.comparingInt'
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/sasl/MechanismFinder.java`
#### Snippet
```java
   private static final Map<String, ServerSASLFactory> FACTORY_MAP = new HashMap<>();
   private static final Comparator<? super ServerSASLFactory> PRECEDENCE_COMPARATOR =
      (f1, f2) -> Integer.compare(f1.getPrecedence(), f2.getPrecedence());
   private static final String[] DEFAULT_MECHANISMS;

```

## RuleId[id=NonSerializableFieldInSerializableClass]
### NonSerializableFieldInSerializableClass
Non-serializable field 'resourceRecovery' in a Serializable class
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/ActiveMQRAManagedConnectionFactory.java`
#### Snippet
```java
    * The resource recovery if there is one
    */
   private XARecoveryConfig resourceRecovery;

   /**
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'recoveryManager' in a Serializable class
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/ActiveMQResourceAdapter.java`
#### Snippet
```java
   private String unparsedJndiParams;

   private final RecoveryManager recoveryManager;

   private boolean useAutoRecovery = true;
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'amqpError' in a Serializable class
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/exceptions/ActiveMQAMQPException.java`
#### Snippet
```java
   }

   private final Symbol amqpError;

   public ActiveMQAMQPException(Symbol amqpError, String message, Throwable e, ActiveMQExceptionType t) {
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'manager' in a Serializable class
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/JGroupsFileBroadcastEndpointFactory.java`
#### Snippet
```java
   private String channelName;

   private final JChannelManager manager = JChannelManager.getInstance();

   @Override
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'manager' in a Serializable class
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/ChannelBroadcastEndpointFactory.java`
#### Snippet
```java
   private final String channelName;

   private final JChannelManager manager;

   private static final JChannelManager singletonManager = JChannelManager.getInstance();
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'channel' in a Serializable class
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/ChannelBroadcastEndpointFactory.java`
#### Snippet
```java
   private static final Logger logger = LoggerFactory.getLogger(MethodHandles.lookup().lookupClass());

   private final JChannel channel;

   private final String channelName;
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'coreMessageObjectPools' in a Serializable class
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/ClientPacketDecoder.java`
#### Snippet
```java

   private static final long serialVersionUID = 6952614096979334582L;
   protected final CoreMessageObjectPools coreMessageObjectPools = new CoreMessageObjectPools();

   @Override
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'compositeMembers' in a Serializable class
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/ClusterConnectionConfiguration.java`
#### Snippet
```java
   private MessageLoadBalancingType messageLoadBalancingType = Enum.valueOf(MessageLoadBalancingType.class, ActiveMQDefaultConfiguration.getDefaultClusterMessageLoadBalancingType());

   private URISupport.CompositeData compositeMembers;

   private List<String> staticConnectors = Collections.emptyList();
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'connectionProvider' in a Serializable class
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/storage/DatabaseStorageConfiguration.java`
#### Snippet
```java
   private Map<String, Object> dataSourceProperties = new HashMap();

   private JDBCConnectionProvider connectionProvider;

   private SQLProvider.Factory sqlProviderFactory;
```

## RuleId[id=RedundantCollectionOperation]
### RedundantCollectionOperation
`contains` can be replaced with 'Map.containsKey()'
in `artemis-commons/src/main/java/org/apache/activemq/artemis/api/core/QueueConfiguration.java`
#### Snippet
```java

      // name is the only required value
      if (!json.keySet().contains(NAME)) {
         return null;
      }
```

### RedundantCollectionOperation
`contains` can be replaced with 'Map.containsKey()'
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/ActiveMQResourceAdapter.java`
#### Snippet
```java
      boolean known = false;

      if (!knownConnectionFactories.keySet().contains(overrideProperties)) {
         cf = newConnectionFactory(overrideProperties);
         knownConnectionFactories.put(overrideProperties, new Pair<>(cf, new AtomicInteger(1)));
```

### RedundantCollectionOperation
Unnecessary 'containsKey()' check
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/HierarchicalObjectRepository.java`
#### Snippet
```java
         if (usesWildcards(modifiedMatch)) {
            clearCache();
         } else if (modifiedMatch != null && cache.containsKey(modifiedMatch)) {
            cache.remove(modifiedMatch);
         }
```

## RuleId[id=AbstractMethodCallInConstructor]
### AbstractMethodCallInConstructor
Call to 'abstract' method `createQueue()` during object construction
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/pools/Pool.java`
#### Snippet
```java

   public Pool(int maxSize, Consumer<T> cleaner, Supplier<T> supplier) {
      internalPool = createQueue(maxSize);
      this.cleaner = cleaner;
      this.supplier = supplier;
```

### AbstractMethodCallInConstructor
Call to 'abstract' method `getDefaultOrderColumn()` during object construction
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/view/ActiveMQAbstractView.java`
#### Snippet
```java

   public ActiveMQAbstractView() {
      this.sortColumn = getDefaultOrderColumn();
      this.sortOrder = "asc";
   }
```

### AbstractMethodCallInConstructor
Call to 'abstract' method `init()` during object construction
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/AbstractJournalStorageManager.java`
#### Snippet
```java
      syncTransactional = config.isJournalSyncTransactional();

      init(config, criticalErrorListener);

      idGenerator = new BatchingIDGenerator(0, CHECKPOINT_BATCH_SIZE, this);
```

## RuleId[id=MismatchedJavadocCode]
### MismatchedJavadocCode
Method is specified to return number but the return type is list
in `artemis-junit/artemis-junit-commons/src/main/java/org/apache/activemq/artemis/junit/EmbeddedJMSOperations.java`
#### Snippet
```java
    * is assumed.
    * @param topicName the full name of the JMS Destination
    * @return the number of messages in the JMS Destination
    */
   List<Queue> getTopicQueues(String topicName);
```

### MismatchedJavadocCode
Method is specified to return list but the return type is map
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/Configuration.java`
#### Snippet
```java

   /**
    * @return a list of roles per matching
    */
   Map<String, Set<Role>> getSecurityRoles();
```

## RuleId[id=CatchMayIgnoreException]
### CatchMayIgnoreException
'catch' parameter named `ignored` is used
in `artemis-service-extensions/src/main/java/org/apache/activemq/artemis/service/extensions/xa/recovery/ActiveMQXAResourceWrapper.java`
#### Snippet
```java
               if (serverLocator != null)
                  serverLocator.close();
            } catch (Throwable ignored) {
               logger.trace(e.getMessage(), ignored);
            }
```

### CatchMayIgnoreException
Empty `catch` block
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/XPathExpression.java`
#### Snippet
```java
      try {
         XPATH_EVALUATOR_FACTORY = xpath -> new JAXPXPathEvaluator(xpath, builder);
      } catch (Throwable e) {
      }
   }
```

### CatchMayIgnoreException
Empty `catch` block
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/FileUtil.java`
#### Snippet
```java
            try {
               Thread.sleep(100);
            } catch (InterruptedException e) {
            }
            files = directory.list();
```

### CatchMayIgnoreException
Empty `catch` block
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/SpawnedVMSupport.java`
#### Snippet
```java
            try {
               alive.waitFor(5, TimeUnit.SECONDS);
            } catch (Exception e) {
            }
         }
```

### CatchMayIgnoreException
Empty `catch` block
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/TimeUtils.java`
#### Snippet
```java
         try {
            Thread.sleep(interval);
         } catch (InterruptedException e) {
         }
         timeLeft -= interval;
```

### CatchMayIgnoreException
Empty `catch` block
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/UUIDTimer.java`
#### Snippet
```java
         try {
            Thread.sleep(delay);
         } catch (InterruptedException ie) {
         }
         delay = 1L;
```

### CatchMayIgnoreException
Empty `catch` block
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/ClassloadingUtil.java`
#### Snippet
```java
         if (resource != null)
            return resource;
      } catch (Throwable t) {
      }

```

### CatchMayIgnoreException
'catch' parameter named `ignored` is used
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/ClassloadingUtil.java`
#### Snippet
```java
            properties.load(url.openStream());
         }
      } catch (Throwable ignored) {
         logger.warn(ignored.getMessage(), ignored);
      }
```

### CatchMayIgnoreException
Empty `catch` block
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
         try {
            in.close();
         } catch (Exception exc) {
         }
         try {
```

### CatchMayIgnoreException
Empty `catch` block
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
         try {
            out.close();
         } catch (Exception exc) {
         }
      } // end finally
```

### CatchMayIgnoreException
Empty `catch` block
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
         try {
            oos.close();
         } catch (Exception e) {
         }
         try {
```

### CatchMayIgnoreException
Empty `catch` block
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
         try {
            gzos.close();
         } catch (Exception e) {
         }
         try {
```

### CatchMayIgnoreException
Empty `catch` block
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
         try {
            b64os.close();
         } catch (Exception e) {
         }
         try {
```

### CatchMayIgnoreException
Empty `catch` block
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
         try {
            baos.close();
         } catch (Exception e) {
         }
      } // end finally
```

### CatchMayIgnoreException
Empty `catch` block
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
            try {
               gzos.close();
            } catch (Exception e) {
            }
            try {
```

### CatchMayIgnoreException
Empty `catch` block
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
            try {
               b64os.close();
            } catch (Exception e) {
            }
            try {
```

### CatchMayIgnoreException
Empty `catch` block
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
            try {
               baos.close();
            } catch (Exception e) {
            }
         } // end finally
```

### CatchMayIgnoreException
Empty `catch` block
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
               bis.close();
            }
         } catch (Exception e) {
         }
      } // end finally
```

### CatchMayIgnoreException
Empty `catch` block
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
               try {
                  baos.close();
               } catch (Exception e) {
               }
               try {
```

### CatchMayIgnoreException
Empty `catch` block
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
               try {
                  gzis.close();
               } catch (Exception e) {
               }
               try {
```

### CatchMayIgnoreException
Empty `catch` block
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
               try {
                  bais.close();
               } catch (Exception e) {
               }
            } // end finally
```

### CatchMayIgnoreException
Empty `catch` block
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
         try {
            bis.close();
         } catch (Exception e) {
         }
      } // end finally
```

### CatchMayIgnoreException
Empty `catch` block
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
         try {
            bos.close();
         } catch (Exception e) {
         }
      } // end finally
```

### CatchMayIgnoreException
Empty `catch` block
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
         try {
            in.close();
         } catch (Exception exc) {
         }
         try {
```

### CatchMayIgnoreException
Empty `catch` block
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
         try {
            out.close();
         } catch (Exception exc) {
         }
      } // end finally
```

### CatchMayIgnoreException
Empty `catch` block
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
         try {
            bos.close();
         } catch (Exception e) {
         }
      } // end finally
```

### CatchMayIgnoreException
Empty `catch` block
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
         try {
            bais.close();
         } catch (Exception e) {
         }
         try {
```

### CatchMayIgnoreException
Empty `catch` block
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
         try {
            ois.close();
         } catch (Exception e) {
         }
      } // end finally
```

### CatchMayIgnoreException
Empty `catch` block
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/io/nio/NIOSequentialFileFactory.java`
#### Snippet
```java
               Thread.sleep(100);
            }
         } catch (InterruptedException e) {
         }

```

### CatchMayIgnoreException
Empty `catch` block
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/ActiveMQRAConnectionMetaData.java`
#### Snippet
```java
            versionProps.load(in);
         }
      } catch (IOException e) {
      }
      JMS_VERSION_NAME = versionProps.getProperty("activemq.version.implementation.versionName", "2.0");
```

### CatchMayIgnoreException
Empty `catch` block
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/ActiveMQRAConnectionManager.java`
#### Snippet
```java
         try {
            conn.destroy();
         } catch (Throwable e) {

         }
```

### CatchMayIgnoreException
Empty `catch` block
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/ActiveMQRaUtils.java`
#### Snippet
```java
               } catch (RuntimeException e) {
                  throw e;
               } catch (Exception e) {
               }

```

### CatchMayIgnoreException
'catch' parameter named `ignored` is used
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/ActiveMQResourceAdapter.java`
#### Snippet
```java
         try {
            entry.getValue().stop();
         } catch (Exception ignored) {
            logger.debug("Ignored", ignored);
         }
```

### CatchMayIgnoreException
Empty `catch` block
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/util/SyncCalculation.java`
#### Snippet
```java
         try {
            file.close();
         } catch (Exception e) {
         }
         try {
```

### CatchMayIgnoreException
Empty `catch` block
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/util/SyncCalculation.java`
#### Snippet
```java
         try {
            file.delete();
         } catch (Exception e) {
         }
         try {
```

### CatchMayIgnoreException
Empty `catch` block
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/util/SyncCalculation.java`
#### Snippet
```java
         try {
            factory.stop();
         } catch (Exception e) {
         }
      }
```

### CatchMayIgnoreException
Empty `catch` block
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/xml/XmlDataImporter.java`
#### Snippet
```java
            // to avoid races
            session.commit();
         } catch (Throwable dontcare) {
         }
         if (!tempFileName.delete()) {
```

### CatchMayIgnoreException
'catch' parameter named `ignore` is used
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/perf/ProducerBenchmark.java`
#### Snippet
```java
         try {
            generators[i].asyncClose(() -> onClosed.complete(null)).get();
         } catch (final Throwable ignore) {
            closedGenerators[i].completeExceptionally(ignore);
         }
```

### CatchMayIgnoreException
'catch' parameter named `ignore` is used
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/bridge/impl/JMSBridgeImpl.java`
#### Snippet
```java
         try {
            stop(true);
         } catch (Throwable ignore) {
            logger.trace("Failed to stop bridge {} from {} ", bridgeName, this.getClass().getSimpleName(), ignore);
         }
```

### CatchMayIgnoreException
'catch' parameter named `ignore` is used
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/bridge/impl/JMSBridgeImpl.java`
#### Snippet
```java
         try {
            sourceConn.stop();
         } catch (Throwable ignore) {
            logger.trace("Failed to stop source connection", ignore);
         }
```

### CatchMayIgnoreException
'catch' parameter named `ignore` is used
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/bridge/impl/JMSBridgeImpl.java`
#### Snippet
```java
         try {
            delistResources(tx);
         } catch (Throwable ignore) {
            logger.trace("Failed to delist resources", ignore);
         }
```

### CatchMayIgnoreException
'catch' parameter named `ignore` is used
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/bridge/impl/JMSBridgeImpl.java`
#### Snippet
```java
            tx.rollback();
            abortedMessageCount += messages.size();
         } catch (Throwable ignore) {
            logger.trace("Failed to rollback", ignore);
         }
```

### CatchMayIgnoreException
'catch' parameter named `ignore` is used
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/bridge/impl/JMSBridgeImpl.java`
#### Snippet
```java
         try {
            sourceConn.close();
         } catch (Throwable ignore) {
            logger.trace("Failed to close source connection", ignore);
         }
```

### CatchMayIgnoreException
'catch' parameter named `ignore` is used
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/bridge/impl/JMSBridgeImpl.java`
#### Snippet
```java
            targetConn.close();
         }
      } catch (Throwable ignore) {
         logger.trace("Failed to close target connection", ignore);
      }
```

### CatchMayIgnoreException
'catch' parameter named `ignore` is used
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/bridge/impl/JMSBridgeImpl.java`
#### Snippet
```java
                     tx.rollback();
                     abortedMessageCount += messages.size();
                  } catch (Exception ignore) {
                     logger.trace("Failed to rollback", ignore);
                  }
```

### CatchMayIgnoreException
'catch' parameter named `ignore` is used
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/bridge/impl/JMSBridgeImpl.java`
#### Snippet
```java
               try {
                  sourceConn.close();
               } catch (Exception ignore) {
                  logger.trace("Failed to close source connection on bridge {}", ignore);
               }
```

### CatchMayIgnoreException
'catch' parameter named `ignore` is used
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/bridge/impl/JMSBridgeImpl.java`
#### Snippet
```java
               try {
                  targetConn.close();
               } catch (Exception ignore) {
                  logger.trace("Failed to close target connection on bridge {}", ignore);
               }
```

### CatchMayIgnoreException
Empty `catch` block
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/bridge/impl/JMSBridgeImpl.java`
#### Snippet
```java
                  start = System.currentTimeMillis();
                  this.wait(toWait);
               } catch (InterruptedException e) {
               } finally {
                  waited = System.currentTimeMillis() - start;
```

### CatchMayIgnoreException
Empty `catch` block
in `artemis-unit-test-support/src/main/java/org/apache/activemq/artemis/utils/ThreadLeakCheckRule.java`
#### Snippet
```java
         try {
            Thread.sleep(500);
         } catch (InterruptedException e) {
         }
      }
```

### CatchMayIgnoreException
Empty `catch` block
in `artemis-unit-test-support/src/main/java/org/apache/activemq/artemis/utils/ThreadLeakCheckRule.java`
#### Snippet
```java
         try {
            finalized.await(100, TimeUnit.MILLISECONDS);
         } catch (InterruptedException e) {
         }
      }
```

### CatchMayIgnoreException
Empty `catch` block
in `artemis-unit-test-support/src/main/java/org/apache/activemq/artemis/utils/ThreadLeakCheckRule.java`
#### Snippet
```java
                  try {
                     Thread.sleep(500);
                  } catch (Throwable e) {
                  }
               }
```

### CatchMayIgnoreException
Empty `catch` block
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQConnectionFactory.java`
#### Snippet
```java
         try {
            connection.close();
         } catch (JMSException me) {
         }
         throw e;
```

### CatchMayIgnoreException
Empty `catch` block
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQConnectionMetaData.java`
#### Snippet
```java
            versionProps.load(in);
         }
      } catch (IOException e) {
      }
      JMS_VERSION_NAME = versionProps.getProperty("activemq.version.implementation.versionName", "2.0");
```

### CatchMayIgnoreException
'catch' parameter named `ignored` is used
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPLargeMessage.java`
#### Snippet
```java
         try {
            reader.close();
         } catch (Exception ignored) {
            logger.debug(ignored.getMessage(), ignored);
         }
```

### CatchMayIgnoreException
Empty `catch` block
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/converter/coreWrapper/CoreStreamMessageWrapper.java`
#### Snippet
```java
            list.add(readObject());
         }
      } catch (MessageEOFException e) {
      }

```

### CatchMayIgnoreException
Empty `catch` block
in `artemis-jdbc-store/src/main/java/org/apache/activemq/artemis/jdbc/store/drivers/JDBCConnectionProvider.java`
#### Snippet
```java
         try {
            DriverManager.getConnection("jdbc:derby:;shutdown=true");
         } catch (Exception e) { }
      }

```

### CatchMayIgnoreException
'catch' parameter named `ignored` is used
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/LargeMessageControllerImpl.java`
#### Snippet
```java
         try {
            consumerInternal.flowControl(totalSize, false);
         } catch (Exception ignored) {
            // what else can we do here?
            ActiveMQClientLogger.LOGGER.errorCallingCancel(ignored);
```

### CatchMayIgnoreException
'catch' parameter named `ignored` is used
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ClientSessionImpl.java`
#### Snippet
```java
            try {
               rollback(false, false);
            } catch (Throwable ignored) {
               logger.debug("Error on rollback during end call!", ignored);
            }
```

### CatchMayIgnoreException
'catch' parameter named `ignored` is used
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/TransportConstants.java`
#### Snippet
```java
            return Integer.parseInt(variable);
         }
      } catch (Throwable ignored) {
         logger.debug(ignored.getMessage(), ignored);
      }
```

### CatchMayIgnoreException
Empty `catch` block
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ClientSessionFactoryImpl.java`
#### Snippet
```java
            try {
               transportConnection.close();
            } catch (Throwable t) {
            }
         }
```

### CatchMayIgnoreException
Empty `catch` block
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ClientSessionFactoryImpl.java`
#### Snippet
```java
            try {
               connector.close();
            } catch (Throwable t) {
            }
         }
```

### CatchMayIgnoreException
Empty `catch` block
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ClientSessionFactoryImpl.java`
#### Snippet
```java
         try {
            connector.close();
         } catch (Throwable t) {
         }
      }
```

### CatchMayIgnoreException
Empty `catch` block
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/utils/MemorySize.java`
#### Snippet
```java
         try {
            Thread.sleep(500);
         } catch (InterruptedException e) {
         }
      }
```

### CatchMayIgnoreException
Empty `catch` block
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/security/jaas/GuestLoginModule.java`
#### Snippet
```java
               passwordCallback.clearPassword();
            }
         } catch (IOException | UnsupportedCallbackException e) {
         }
      }
```

### CatchMayIgnoreException
'catch' parameter named `ignored` is used
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/impl/PagingStoreImpl.java`
#### Snippet
```java
         SequentialFile file = factory.createSequentialFile(fileName);
         return file.exists() && file.size() > 0;
      } catch (Exception ignored) {
         // never supposed to happen, but just in case
         logger.warn("PagingStoreFactory::checkPageFileExists never-throws assumption failed.", ignored);
```

### CatchMayIgnoreException
Empty `catch` block
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/cursor/impl/PageCursorProviderImpl.java`
#### Snippet
```java
               try {
                  depagedPage.close(false, false);
               } catch (Exception e) {
               }
            }
```

### CatchMayIgnoreException
'catch' parameter named `ignored` is used
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/cursor/impl/PageSubscriptionImpl.java`
#### Snippet
```java
               logger.trace("numberOfMessages = {} confirmed = {} pendingTX = {}, pageNr = {} posACK = {}",
                            getNumberOfMessages(), (confirmed.get() + 1), pendingTX, pageId, posACK);
            } catch (Throwable ignored) {
               logger.debug(ignored.getMessage(), ignored);
            }
```

### CatchMayIgnoreException
Empty `catch` block
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/FileBasedNodeManager.java`
#### Snippet
```java
               try {
                  Thread.sleep(100);
               } catch (InterruptedException e1) {
               }
               count++;
```

### CatchMayIgnoreException
'catch' parameter named `ignore` is used
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ReplicationBackupActivation.java`
#### Snippet
```java
      try {
         task.run();
      } catch (Throwable ignore) {
         logger.debug(debugErrorMessage, ignore);
      }
```

### CatchMayIgnoreException
Empty `catch` block
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/AnyLiveNodeLocatorForScaleDown.java`
#### Snippet
```java
                  }
               }
            } catch (InterruptedException e) {

            }
```

### CatchMayIgnoreException
Empty `catch` block
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/LegacyLDAPSecuritySettingPlugin.java`
#### Snippet
```java
            context.getAttributes("");
            alive = true;
         } catch (Exception e) {
         }
      }
```

### CatchMayIgnoreException
'catch' parameter named `ignored` is used
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ScaleDownHandler.java`
#### Snippet
```java
                  }
               }
            } catch (NoSuchElementException ignored) {
               logger.debug(ignored.getMessage(), ignored);
               // this could happen through paging browsing
```

### CatchMayIgnoreException
Empty `catch` block
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/AnyLiveNodeLocatorForReplication.java`
#### Snippet
```java
                  }
               }
            } catch (InterruptedException e) {

            }
```

### CatchMayIgnoreException
'catch' parameter named `ignored` is used
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ReplicationPrimaryActivation.java`
#### Snippet
```java
                         coordinatedLockAndNodeId, inSyncReplicaActivation, FAILBACK_TIMEOUT_MILLIS);
         }
      } catch (UnavailableStateException ignored) {
         logger.debug("Unavailable distributed manager while awaiting failback activation sequence: ignored", ignored);
      } finally {
```

### CatchMayIgnoreException
'catch' parameter named `ignored` is used
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/PostOfficeJournalLoader.java`
#### Snippet
```java
            try {
               storageManager.deleteMessage(msg.getMessageID());
            } catch (Exception ignored) {
               ActiveMQServerLogger.LOGGER.journalErrorDeletingMessage(msg.getMessageID(), ignored);
            }
```

### CatchMayIgnoreException
Empty `catch` block
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/impl/BridgeImpl.java`
#### Snippet
```java
               session.close();
               session = null;
            } catch (ActiveMQException dontcare) {
            }
         }
```

### CatchMayIgnoreException
Empty `catch` block
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/impl/BridgeImpl.java`
#### Snippet
```java
               sessionConsumer.close();
               sessionConsumer = null;
            } catch (ActiveMQException dontcare) {
            }
         }
```

### CatchMayIgnoreException
Empty `catch` block
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/impl/BridgeImpl.java`
#### Snippet
```java

         cleanUpSessionFactory(csf);
      } catch (Throwable dontCare) {
      }

```

### CatchMayIgnoreException
Empty `catch` block
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/impl/BridgeImpl.java`
#### Snippet
```java
      try {
         session.cleanUp(false);
      } catch (Throwable dontCare) {
      }

```

### CatchMayIgnoreException
'catch' parameter named `ignored` is used
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/impl/ClusterConnectionImpl.java`
#### Snippet
```java
                     targetLocator.close();
                  }
               } catch (Exception ignored) {
                  logger.debug(ignored.getMessage(), ignored);
               }
```

### CatchMayIgnoreException
'catch' parameter named `ignored` is used
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ActiveMQServerImpl.java`
#### Snippet
```java
               listener.onIOException(cause, message, file);
            }
         } catch (Throwable ignored) {
            logger.debug("Ignored exception {}", ignored.getMessage(), ignored);
         }
```

### CatchMayIgnoreException
'catch' parameter named `ignored` is used
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ActiveMQServerImpl.java`
#### Snippet
```java
                  }
               }
            } catch (Throwable ignored) {
               logger.debug(ignored.getMessage(), ignored);
            }
```

### CatchMayIgnoreException
Empty `catch` block
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/federation/FederationDownstream.java`
#### Snippet
```java
         clientSession = null;
         clientSessionFactory = null;
      } catch (Throwable dontCare) {
      }
      start();
```

### CatchMayIgnoreException
Empty `catch` block
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/federation/FederatedQueueConsumerImpl.java`
#### Snippet
```java
         clientSession = null;
         clientSessionFactory = null;
      } catch (Throwable dontCare) {
      }
      start();
```

### CatchMayIgnoreException
Empty `catch` block
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/federation/FederatedQueueConsumerImpl.java`
#### Snippet
```java
         try {
            clientSession.rollback();
         } catch (ActiveMQException e1) {
         }
      }
```

### CatchMayIgnoreException
Empty `catch` block
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/registry/JndiBindingRegistry.java`
#### Snippet
```java
            context.close();
         }
      } catch (NamingException e) {
      }
   }
```

### CatchMayIgnoreException
Empty `catch` block
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/registry/JndiBindingRegistry.java`
#### Snippet
```java
            context.unbind(name);
         }
      } catch (NamingException e) {
      }
   }
```

### CatchMayIgnoreException
'catch' parameter named `ignored` is used
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/CoreProtocolManager.java`
#### Snippet
```java
                        }
                     });
                  } catch (RejectedExecutionException ignored) {
                     logger.debug(ignored.getMessage(), ignored);
                     // this could happen during a shutdown and we don't care, if we lost a nodeDown during a shutdown
```

## RuleId[id=UnnecessaryToStringCall]
### UnnecessaryToStringCall
Unnecessary `toString()` call
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/SpawnedVMSupport.java`
#### Snippet
```java
               }
               empty = false;
               stringBuilder.append(f.toString());
            }
         }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `artemis-commons/src/main/java/org/apache/activemq/artemis/logs/AuditLogger.java`
#### Snippet
```java
         return prefix + Arrays.toString((Object[])value);
      } else {
         return prefix + value.toString();
      }
   }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalFileImpl.java`
#### Snippet
```java
      } catch (Exception e) {
         logger.warn("Error during method invocation", e.getMessage(), e);
         return "Error:" + e.toString();
      }
   }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/TypedProperties.java`
#### Snippet
```java
               }
            } else {
               sb.append(theValue.toString());
            }

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/LinkedListImpl.java`
#### Snippet
```java

      while (node != null) {
         str.append(node.toString());

         if (node.next != null) {
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/inflow/ActiveMQActivation.java`
#### Snippet
```java

         if (lastReceived && newNode) {
            ActiveMQRALogger.LOGGER.rebalancingConnections("nodeUp " + member.toString());
            startReconnectThread("NodeUP Connection Rebalancer");
         } else if (last) {
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/inflow/ActiveMQActivation.java`
#### Snippet
```java
         if (spec.isEnable1xPrefixes() == null) {
            if (ra.isEnable1xPrefixes() != null && ra.isEnable1xPrefixes() && !topic.startsWith(PacketImpl.OLD_TOPIC_PREFIX.toString())) {
               return PacketImpl.OLD_TOPIC_PREFIX.toString() + topic;
            }
            return topic;
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/inflow/ActiveMQActivation.java`
#### Snippet
```java
         }
         if (spec.isEnable1xPrefixes() && !topic.startsWith(PacketImpl.OLD_TOPIC_PREFIX.toString())) {
            return PacketImpl.OLD_TOPIC_PREFIX.toString() + topic;
         }
         return topic;
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/inflow/ActiveMQActivation.java`
#### Snippet
```java
         if (spec.isEnable1xPrefixes() == null) {
            if (ra.isEnable1xPrefixes() != null && ra.isEnable1xPrefixes() && !queue.startsWith(PacketImpl.OLD_QUEUE_PREFIX.toString())) {
               return PacketImpl.OLD_QUEUE_PREFIX.toString() + queue;
            }
            return queue;
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/inflow/ActiveMQActivation.java`
#### Snippet
```java
         }
         if (spec.isEnable1xPrefixes() && !queue.startsWith(PacketImpl.OLD_QUEUE_PREFIX.toString())) {
            return PacketImpl.OLD_QUEUE_PREFIX.toString() + queue;
         }
         return queue;
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/ActionAbstract.java`
#### Snippet
```java
         } catch (Exception e) {
            if (isVerbose()) {
               getActionContext().out.print("Can not get the broker url instance: " + e.toString());
            }
         }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/check/CheckAbstract.java`
#### Snippet
```java
                     context.out.println(reason);
                     if (verbose) {
                        context.out.println(e.toString());
                        e.printStackTrace(context.out);
                     }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Create.java`
#### Snippet
```java
         buffer.append("*");
      }
      getActionContext().out.println(buffer.toString());
      getActionContext().out.println();
      getActionContext().out.println(message);
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Create.java`
#### Snippet
```java
      getActionContext().out.println(message);
      getActionContext().out.println();
      getActionContext().out.println(buffer.toString());
   }

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenWireProtocolManager.java`
#### Snippet
```java
               fqqn.append(paths[i]);
            }
            mappedDestination = new ActiveMQQueue(fqqn.toString() + ( virtualTopicConfig.selectorAware ? "?" + SELECTOR_AWARE_OPTION + "=true" : "" ));
            break;
         }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `artemis-junit/artemis-junit-commons/src/main/java/org/apache/activemq/artemis/junit/ActiveMQConsumerDelegate.java`
#### Snippet
```java
         if (!session.queueQuery(queueName).isExists() && autoCreateQueue) {
            log.warn("{}: queue does not exist - creating queue: address = {}, name = {}",
                     this.getClass().getSimpleName(), queueName.toString(), queueName.toString());
            session.createAddress(queueName, RoutingType.MULTICAST, true);
            session.createQueue(new QueueConfiguration(queueName));
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `artemis-junit/artemis-junit-commons/src/main/java/org/apache/activemq/artemis/junit/ActiveMQDynamicProducerDelegate.java`
#### Snippet
```java
         if (address != null && !session.addressQuery(address).isExists() && autoCreateQueue) {
            log.warn("queue does not exist - creating queue: address = {}, name = {}", address.toString(),
                     address.toString());
            session.createQueue(new QueueConfiguration(address));
         }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `artemis-junit/artemis-junit-commons/src/main/java/org/apache/activemq/artemis/junit/ActiveMQDynamicProducerDelegate.java`
#### Snippet
```java
         if (autoCreateQueue && !session.addressQuery(targetAddress).isExists()) {
            log.warn("queue does not exist - creating queue: address = {}, name = {}", address.toString(),
                     address.toString());
            session.createQueue(new QueueConfiguration(targetAddress));
         }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `artemis-junit/artemis-junit-commons/src/main/java/org/apache/activemq/artemis/junit/ActiveMQDynamicProducerDelegate.java`
#### Snippet
```java
      } catch (ActiveMQException amqEx) {
         throw new ActiveMQClientResourceException(String.format("Queue creation failed for queue: address = %s, name = %s",
                                                                 address.toString(), address.toString()));
      }

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `artemis-junit/artemis-junit-commons/src/main/java/org/apache/activemq/artemis/junit/ActiveMQDynamicProducerDelegate.java`
#### Snippet
```java
      } catch (ActiveMQException amqEx) {
         throw new ActiveMQClientResourceException(String.format("Failed to send message to %s",
                                                                 targetAddress.toString()),
                                                   amqEx);
      }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `artemis-junit/artemis-junit-commons/src/main/java/org/apache/activemq/artemis/junit/ActiveMQProducerDelegate.java`
#### Snippet
```java
         if (!session.addressQuery(address).isExists() && autoCreateQueue) {
            log.warn("{}: queue does not exist - creating queue: address = {}, name = {}",
                     this.getClass().getSimpleName(), address.toString(), address.toString());
            session.createQueue(new QueueConfiguration(address));
         }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQMessage.java`
#### Snippet
```java
         UUID uid = (UUID)message.getUserID();

         msgID = uid == null ? null : "ID:" + uid.toString();
      }
      return msgID;
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/sasl/GSSAPIServerSASL.java`
#### Snippet
```java

      } catch (Exception outOfHere) {
         logger.info("Error on sasl input: {}", outOfHere.toString(), outOfHere);
         result = new PrincipalSASLResult(false, null);
      }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `artemis-junit/artemis-junit-commons/src/main/java/org/apache/activemq/artemis/junit/EmbeddedJMSDelegate.java`
#### Snippet
```java
         } catch (JMSException jmsEx) {
            throw new EmbeddedJMSResourceException(String.format("Failed to set body {%s} on ObjectMessage",
                                                                 body.toString()),
                                                   jmsEx);
         }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/ProtonServerSenderContext.java`
#### Snippet
```java

                  String remoteContainerId = sender.getSession().getConnection().getRemoteContainer();
                  String noLocalFilter = MessageUtil.CONNECTION_ID_PROPERTY_NAME.toString() + "<>'" + remoteContainerId + "'";

                  if (selector.endsWith(noLocalFilter)) {
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/ProtonServerSenderContext.java`
#### Snippet
```java
               if (filter != null) {
                  String remoteContainerId = sender.getSession().getConnection().getRemoteContainer();
                  String noLocalFilter = MessageUtil.CONNECTION_ID_PROPERTY_NAME.toString() + "<>'" + remoteContainerId + "'";
                  if (selector != null) {
                     selector += " AND " + noLocalFilter;
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/converter/AMQPMessageIdHelper.java`
#### Snippet
```java

      if (idObject instanceof UUID) {
         return JMS_ID_PREFIX + AMQP_UUID_PREFIX + idObject.toString();
      } else if (idObject instanceof UnsignedLong) {
         return JMS_ID_PREFIX + AMQP_ULONG_PREFIX + idObject.toString();
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/converter/AMQPMessageIdHelper.java`
#### Snippet
```java
         return JMS_ID_PREFIX + AMQP_UUID_PREFIX + idObject.toString();
      } else if (idObject instanceof UnsignedLong) {
         return JMS_ID_PREFIX + AMQP_ULONG_PREFIX + idObject.toString();
      } else if (idObject instanceof Binary) {
         ByteBuffer dup = ((Binary) idObject).asByteBuffer();
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/client/ClientRequestor.java`
#### Snippet
```java

      requestProducer = queueSession.createProducer(requestAddress);
      replyQueue = new SimpleString(requestAddress + "." + UUID.randomUUID().toString());
      queueSession.createQueue(new QueueConfiguration(replyQueue).setDurable(false).setTemporary(true));
      replyConsumer = queueSession.createConsumer(replyQueue);
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `artemis-junit/artemis-junit-commons/src/main/java/org/apache/activemq/artemis/junit/EmbeddedActiveMQDelegate.java`
#### Snippet
```java
      } catch (Exception e) {
         throw new EmbeddedActiveMQResourceException(String.format("getBoundQueues( %s ) - bindingQuery( %s ) failed",
                                                                   address.toString(), address.toString()));
      }
      if (bindingQueryResult.isExists()) {
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `artemis-junit/artemis-junit-commons/src/main/java/org/apache/activemq/artemis/junit/EmbeddedActiveMQDelegate.java`
#### Snippet
```java
      } catch (Exception e) {
         throw new EmbeddedActiveMQResourceException(String.format("getBoundQueues( %s ) - bindingQuery( %s ) failed",
                                                                   address.toString(), address.toString()));
      }
      if (bindingQueryResult.isExists()) {
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ClientMessageImpl.java`
#### Snippet
```java
   @Override
   public String toString() {
      return getClass().getSimpleName() + "[messageID=" + messageID + ", durable=" + durable + ", address=" + getAddress() + ",userID=" + (getUserID() != null ? getUserID() : "null") + ",properties=" + getProperties().toString() + "]";
   }

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/ActiveMQSessionContext.java`
#### Snippet
```java
            }
         }
         logger.trace("xaScan returning {} xids = [{}]", xidArray.length, buffer.toString());
      }

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/cursor/impl/PageSubscriptionImpl.java`
#### Snippet
```java
               confirmed +
               ", isDone=" +
               e.toString();
         }
      }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ReplicationBackupActivation.java`
#### Snippet
```java

         logger.info("Apache ActiveMQ Artemis Backup Server version {} [{}] started, awaiting connection to a live cluster member to start replication", activeMQServer.getVersion().getFullVersion(),
                      activeMQServer.toString());

         clusterController.awaitConnectionToReplicationCluster();
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerConsumerImpl.java`
#### Snippet
```java
   @Override
   public String debug() {
      String debug = toString() + "::Delivering ";
      synchronized (lock) {
         return debug + this.deliveringRefs.size();
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java
      // so please don't clog this with debug objects
      // unless you provide a special way for management to translate sessions
      return "ServerSessionImpl(" + buffer.toString() + ")";
   }

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/impl/ClusterConnectionImpl.java`
#### Snippet
```java
      ClusterConnectionBridge bridge = new ClusterConnectionBridge(this, manager, targetLocator, serverLocator, initialConnectAttempts, reconnectAttempts, retryInterval, retryIntervalMultiplier, maxRetryInterval, nodeManager.getUUID(), record.getEventUID(), record.getTargetNodeID(), record.getQueueName(), record.getQueue(), executorFactory.getExecutor(), null, null, scheduledExecutor, null, useDuplicateDetection, clusterUser, clusterPassword, server, managementService.getManagementAddress(), managementService.getManagementNotificationAddress(), record, record.getConnector(), storeAndForwardPrefix, server.getStorageManager());

      targetLocator.setIdentity("(Cluster-connection-bridge::" + bridge.toString() + "::" + this.toString() + ")");

      if (logger.isDebugEnabled()) {
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/impl/ClusterConnectionImpl.java`
#### Snippet
```java
      ClusterConnectionBridge bridge = new ClusterConnectionBridge(this, manager, targetLocator, serverLocator, initialConnectAttempts, reconnectAttempts, retryInterval, retryIntervalMultiplier, maxRetryInterval, nodeManager.getUUID(), record.getEventUID(), record.getTargetNodeID(), record.getQueueName(), record.getQueue(), executorFactory.getExecutor(), null, null, scheduledExecutor, null, useDuplicateDetection, clusterUser, clusterPassword, server, managementService.getManagementAddress(), managementService.getManagementNotificationAddress(), record, record.getConnector(), storeAndForwardPrefix, server.getStorageManager());

      targetLocator.setIdentity("(Cluster-connection-bridge::" + bridge.toString() + "::" + this.toString() + ")");

      if (logger.isDebugEnabled()) {
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ActiveMQServerImpl.java`
#### Snippet
```java
            @Override
            public ThreadFactory run() {
               return new ActiveMQThreadFactory("ActiveMQ-IO-server-" + this.toString(), false, ClientSessionFactoryImpl.class.getClassLoader());
            }
         });
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ActiveMQServerImpl.java`
#### Snippet
```java
            @Override
            public ThreadFactory run() {
               return new ActiveMQThreadFactory("ActiveMQ-PageExecutor-server-" + this.toString(), false, ClientSessionFactoryImpl.class.getClassLoader());
            }
         });
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ActiveMQServerImpl.java`
#### Snippet
```java
            @Override
            public ThreadFactory run() {
               return new ActiveMQThreadFactory("ActiveMQ-server-" + this.toString(), false, ClientSessionFactoryImpl.class.getClassLoader());
            }
         });
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ActiveMQServerImpl.java`
#### Snippet
```java
            @Override
            public ThreadFactory run() {
               return new ActiveMQThreadFactory("ActiveMQ-IO-server-" + this.toString(), false, ClientSessionFactoryImpl.class.getClassLoader());
            }
         });
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/codec/DuplicateIDEncoding.java`
#### Snippet
```java

            long id = buff.getLong();
            bridgeRepresentation = "nodeUUID=" + uuid.toString() + " messageID=" + id;
         } catch (Throwable ignored) {
            bridgeRepresentation = null;
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/DescribeJournal.java`
#### Snippet
```java
      }

      out.println(bufferFailingTransactions.toString());

      out.println("### Message Counts ###");
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java
      }

      System.out.println(str.toString());

      return str.toString();
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/ActiveMQServerControlImpl.java`
#### Snippet
```java
               html.append("<td>" + msgJson.get(TransactionDetail.KEY_MSG_TYPE) + "</td></tr>");
               html.append("<tr><th>properties</th>");
               html.append("<td colspan=\"3\">" + propstr.toString() + "</td></tr>");
            }
            html.append("</table></td></tr>");
```

## RuleId[id=AssignmentToForLoopParameter]
### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/ComparisonExpression.java`
#### Snippet
```java
            char c = like.charAt(i);
            if (escape == (0xFFFF & c) && shouldEscapeNext(like, i, c)) {
               i++;
               char t = like.charAt(i);
               regexp.append("\\x");
```

## RuleId[id=SetReplaceableByEnumSet]
### SetReplaceableByEnumSet
`HashSet<>` can be replaced with 'EnumSet'
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/FileUtil.java`
#### Snippet
```java
   public static void makeExec(File file) throws IOException {
      try {
         Files.setPosixFilePermissions(file.toPath(), new HashSet<>(Arrays.asList(OWNER_READ, OWNER_WRITE, OWNER_EXECUTE, GROUP_READ, GROUP_WRITE, GROUP_EXECUTE, OTHERS_READ, OTHERS_EXECUTE)));
      } catch (Throwable ignore) {
         // Our best effort was not good enough :)
```

## RuleId[id=InnerClassMayBeStatic]
### InnerClassMayBeStatic
Inner class `StompDecoderV11` may be 'static'
in `artemis-protocols/artemis-stomp-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/stomp/v11/StompFrameHandlerV11.java`
#### Snippet
```java
   }

   protected class StompDecoderV11 extends StompDecoder {

      protected boolean isEscaping = false;
```

### InnerClassMayBeStatic
Inner class `MessageInfo` may be 'static'
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/xml/XMLMessageImporter.java`
#### Snippet
```java
   }

   public class MessageInfo {
      public long id;
      public List<String> queues;
```

### InnerClassMayBeStatic
Inner class `SerialiserMessageListener` may be 'static'
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/Consumer.java`
#### Snippet
```java
   }

   private class SerialiserMessageListener implements MessageListener {

      private MessageSerializer messageSerializer;
```

### InnerClassMayBeStatic
Inner class `MQTTPeriodicTasks` may be 'static'
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTProtocolManagerFactory.java`
#### Snippet
```java
   }

   public class MQTTPeriodicTasks extends ActiveMQScheduledComponent {
      final ActiveMQServer server;
      public MQTTPeriodicTasks(ActiveMQServer server, ScheduledExecutorService scheduledExecutorService) {
```

### InnerClassMayBeStatic
Inner class `OutboundStore` may be 'static'
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTSessionState.java`
#### Snippet
```java
   }

   public class OutboundStore {

      private HashMap<Pair<Long, Long>, Integer> artemisToMqttMessageMap = new HashMap<>();
```

### InnerClassMayBeStatic
Inner class `WrappedRunnable` may be 'static'
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/server/impl/JMSServerManagerImpl.java`
#### Snippet
```java
   }

   private abstract class WrappedRunnable implements Runnable {

      @Override
```

### InnerClassMayBeStatic
Inner class `AddressQueryCache` may be 'static'
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPSessionCallback.java`
#### Snippet
```java


   class AddressQueryCache<T> {
      SimpleString address;
      T result;
```

### InnerClassMayBeStatic
Inner class `AMQPOutgoingController` may be 'static'
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/connect/AMQPBrokerConnection.java`
#### Snippet
```java
   }

   private class AMQPOutgoingController implements SenderController {

      final Queue queue;
```

### InnerClassMayBeStatic
Inner class `PageCursorTX` may be 'static'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/cursor/impl/PageSubscriptionImpl.java`
#### Snippet
```java
   }

   private final class PageCursorTX extends TransactionOperationAbstract {

      private final Map<PageSubscriptionImpl, List<PagePosition>> pendingPositions = new HashMap<>();
```

### InnerClassMayBeStatic
Inner class `RequestBackupVote` may be 'static'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ColocatedActivation.java`
#### Snippet
```java
   }

   class RequestBackupVote extends Vote<Pair<String, Integer>> {

      private int backupsSize;
```

### InnerClassMayBeStatic
Inner class `RefScheduled` may be 'static'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ScheduledDeliveryHandlerImpl.java`
#### Snippet
```java
   // We need a treeset ordered, but we need to order tail operations as well.
   // So, this will serve as a delegate to the object
   class RefScheduled {

      private final MessageReference ref;
```

### InnerClassMayBeStatic
Inner class `ReloadRegistry` may be 'static'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/reload/ReloadManagerImpl.java`
#### Snippet
```java
   }

   class ReloadRegistry {

      private File file;
```

### InnerClassMayBeStatic
Inner class `Entry` may be 'static'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/routing/pools/DiscoveryService.java`
#### Snippet
```java
   }

   public class Entry {
      private final String nodeID;
      private final TransportConfiguration connector;
```

### InnerClassMayBeStatic
Inner class `SslHandshakeExceptionHandler` may be 'static'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/NettyAcceptor.java`
#### Snippet
```java
    * in the log.
    */
   private class SslHandshakeExceptionHandler implements ChannelHandler {

      @Override
```

### InnerClassMayBeStatic
Inner class `QueueIterateAction` may be 'static'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java
    * This will determine the actions that could be done while iterate the queue through iterQueue
    */
   abstract class QueueIterateAction {

      public Integer expectedHits() {
```

## RuleId[id=Java9CollectionFactory]
### Java9CollectionFactory
Can be replaced with 'Map.of()' call
in `artemis-protocols/artemis-hqclient-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/hornetq/util/HQPropertiesConverter.java`
#### Snippet
```java
      d.put(new SimpleString("_HQ_LVQ_NAME"), new SimpleString("_AMQ_LVQ_NAME"));

      hqAmqDictionary = Collections.unmodifiableMap(d);

      d = new HashMap<>();
```

### Java9CollectionFactory
Can be replaced with 'Map.of()' call
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/NettyConnector.java`
#### Snippet
```java
      config.put(TransportConstants.HOST_PROP_NAME, TransportConstants.DEFAULT_HOST);
      config.put(TransportConstants.PORT_PROP_NAME, TransportConstants.DEFAULT_PORT);
      DEFAULT_CONFIG = Collections.unmodifiableMap(config);
   }

```

### Java9CollectionFactory
Can be replaced with 'List.of()' call
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/ArtemisMBeanServerBuilder.java`
#### Snippet
```java

      private final MBeanServer wrapped;
      private final List<String> guarded = Collections.unmodifiableList(Arrays.asList("invoke", "getAttribute", "getAttributes", "setAttribute", "setAttributes"));

      MBeanInvocationHandler(MBeanServer mbeanServer) {
```

### Java9CollectionFactory
Can be replaced with 'Map.of()' call
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/remoting/impl/invm/InVMConnector.java`
#### Snippet
```java
      Map<String, Object> config = new HashMap<>();
      config.put(TransportConstants.SERVER_ID_PROP_NAME, TransportConstants.DEFAULT_SERVER_ID);
      DEFAULT_CONFIG = Collections.unmodifiableMap(config);
   }

```

## RuleId[id=NestedSynchronizedStatement]
### NestedSynchronizedStatement
Nested `synchronized` statement
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/bridge/impl/JMSBridgeImpl.java`
#### Snippet
```java
                     logger.trace("{} waited enough", this);

                     synchronized (lock) {
                        if (!failed && !messages.isEmpty()) {
                           logger.trace("{} got some messages so sending batch", this);
```

### NestedSynchronizedStatement
Nested `synchronized` statement
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/bridge/impl/JMSBridgeImpl.java`
#### Snippet
```java
         this.batchTimeCheckerFinished = null;
         this.batchTimeCheckerTask = null;
         synchronized (lock) {
            started = false;
            if (!isFailureHandler) {
```

### NestedSynchronizedStatement
Nested `synchronized` statement
in `artemis-jdbc-store/src/main/java/org/apache/activemq/artemis/jdbc/store/journal/JDBCJournalImpl.java`
#### Snippet
```java
         }

         synchronized (records) {
            records.add(record);
         }
```

### NestedSynchronizedStatement
Nested `synchronized` statement
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ClientSessionImpl.java`
#### Snippet
```java

            if (resetCreditManager) {
               synchronized (producerCreditManager) {
                  producerCreditManager.reset();
               }
```

### NestedSynchronizedStatement
Nested `synchronized` statement
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ClientSessionImpl.java`
#### Snippet
```java

                     ClientConsumerInternal consumerInternal = entryx.getValue();
                     synchronized (consumerInternal) {
                        if (!consumerInternal.isClosed()) {
                           sessionContext.recreateConsumerOnServer(consumerInternal, entryx.getKey().getId(), started);
```

### NestedSynchronizedStatement
Nested `synchronized` statement
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/cursor/impl/PageSubscriptionImpl.java`
#### Snippet
```java
               }

               synchronized (redeliveries) {
                  PagedReference redelivery = redeliveries.poll();

```

### NestedSynchronizedStatement
Nested `synchronized` statement
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/impl/BridgeImpl.java`
#### Snippet
```java
         ref.handled();

         synchronized (refs) {
            refs.put(ref.getMessage().getMessageID(), ref);
         }
```

### NestedSynchronizedStatement
Nested `synchronized` statement
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/impl/ManagementServiceImpl.java`
#### Snippet
```java
            // otherwise we can get notifications arriving in wrong order / missing
            // if a notification occurs at same time as sendQueueInfoToQueue is processed
            synchronized (postOffice.getNotificationLock()) {

               // First send to any local listeners
```

## RuleId[id=StringEqualsEmptyString]
### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `artemis-maven-plugin/src/main/java/org/apache/activemq/artemis/maven/ArtemisDependencyDocPlugin.java`
#### Snippet
```java
               filter.put("X{uri}", uri);

               if (uri.equals("")) {
                  filter.put("X{fileMD}", result.getArtifact().getFile().getName());
               } else {
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `artemis-maven-plugin/src/main/java/org/apache/activemq/artemis/maven/ArtemisDependencyDocPlugin.java`
#### Snippet
```java

   private String getPackageName(org.eclipse.aether.artifact.Artifact artifact) {
      return artifact.getGroupId() + ":" + artifact.getArtifactId() + (artifact.getClassifier() != null && !artifact.getClassifier().equals("") ? ":" + artifact.getClassifier() : "");
   }

```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `artemis-log-annotation-processor/src/main/java/org/apache/activemq/artemis/logs/annotation/processor/LogAnnotationProcessor.java`
#### Snippet
```java

      tupples(message, '{', '}', (tupple) -> {
         if (!tupple.equals("")) {
            throw new IllegalArgumentException("Invalid placeholder argument {" + tupple + "} on message \'" + message + "\' as part of " + holder + "\nreplace it by {}");
         }
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/inflow/ActiveMQActivationValidationUtils.java`
#### Snippet
```java

      try {
         if (destination == null || destination.trim().equals("")) {
            propsNotSet.add(new PropertyDescriptor("destination", ActiveMQActivationSpec.class));
            errorMessages.add("Destination is mandatory.");
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/inflow/ActiveMQMessageHandler.java`
#### Snippet
```java

      // Create the message consumer
      SimpleString selectorString = selector == null || selector.trim().equals("") ? null : new SimpleString(selector);
      if (activation.isTopic() && spec.isSubscriptionDurable()) {
         SimpleString queueName = ActiveMQDestination.createQueueNameForSubscription(true, spec.getClientID(), spec.getSubscriptionName());
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/inflow/ActiveMQActivation.java`
#### Snippet
```java

         String destinationTypeString = spec.getDestinationType();
         if (destinationTypeString != null && !destinationTypeString.trim().equals("")) {
            logger.debug("Destination type defined as {}", destinationTypeString);

```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/InputAbstract.java`
#### Snippet
```java
      do {
         String input = input(propertyName, prompt, silentDefault);
         if (input == null || input.trim().equals("")) {
            input = "0";
         }
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/InputAbstract.java`
#### Snippet
```java
         inputStr = new String(chars);

         if (inputStr.trim().equals("")) {
            getActionContext().out.println("Invalid Entry!");
         } else {
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/InputAbstract.java`
#### Snippet
```java
         getActionContext().out.println(prompt);
         inputStr = scanner.nextLine();
         if (!acceptNull && inputStr.trim().equals("")) {
            getActionContext().out.println("Invalid Entry!");
         } else {
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/queue/QueueAbstract.java`
#### Snippet
```java
      String queueName = getName();

      if (requireInput && (address == null || "".equals(address.trim()))) {
         address = input("--address", "What is the name of the address? <Enter for " + queueName + ">", null, true);
      }
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/queue/QueueAbstract.java`
#### Snippet
```java
      }

      if (address == null || "".equals(address.trim())) {
         // if still null, it will use the queueName
         address = queueName;
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Create.java`
#### Snippet
```java
      filters.put("${global-max-messages}", Long.toString(globalMaxMessages));

      if (globalMaxSize == null || globalMaxSize.trim().equals("")) {
         filters.put("${global-max-section}", readTextFile(ETC_GLOBAL_MAX_DEFAULT_TXT, filters));
      } else {
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQSession.java`
#### Snippet
```java
         throw new InvalidDestinationException("Not an ActiveMQQueue:" + queue);
      }
      if ("".equals(filterString)) {
         filterString = null;
      }
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQSession.java`
#### Snippet
```java
         }

         selectorString = "".equals(selectorString) ? null : selectorString;

         SimpleString coreFilterString = null;
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQSession.java`
#### Snippet
```java
         throw new InvalidDestinationException("Not an ActiveMQTopic:" + topic);
      }
      if ("".equals(messageSelector)) {
         messageSelector = null;
      }
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQSession.java`
#### Snippet
```java
                                                  ConsumerDurability durability) throws JMSException {
      try {
         selectorString = "".equals(selectorString) ? null : selectorString;

         if (noLocal) {
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQMapMessage.java`
#### Snippet
```java
         throw ActiveMQJMSClientBundle.BUNDLE.nameCannotBeNull();
      }
      if (name.equals("")) {
         throw ActiveMQJMSClientBundle.BUNDLE.nameCannotBeEmpty();
      }
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQJMSProducer.java`
#### Snippet
```java
         throw ActiveMQJMSClientBundle.BUNDLE.nameCannotBeNull();
      }
      if (name.equals("")) {
         throw ActiveMQJMSClientBundle.BUNDLE.nameCannotBeEmpty();
      }
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQMessage.java`
#### Snippet
```java
      }

      if (name.equals("")) {
         throw new IllegalArgumentException("The name of a property must not be an empty String.");
      }
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jndi/ReadOnlyContext.java`
#### Snippet
```java
         String segment = name.substring(0, pos);
         assert segment != null;
         assert !segment.equals("");
         Object o = treeBindings.get(segment);
         if (o == null) {
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/connect/mirror/MirrorAddressFilter.java`
#### Snippet
```java
         String[] parts = filter.split(",");
         for (String part : parts) {
            if (!"".equals(part) && !"!".equals(part)) {
               if (part.startsWith("!")) {
                  denyList.add(new SimpleString(part.substring(1)));
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ServerLocatorImpl.java`
#### Snippet
```java
      interceptors.clear();

      if (interceptorList == null || interceptorList.trim().equals("")) {
         return;
      }
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/utils/XMLUtil.java`
#### Snippet
```java

      if (children.getLength() == 0) {
         if ((textContent = XMLUtil.getTextContent(n)) != null && !"".equals(textContent)) {
            sb.append(textContent).append("</").append(name).append('>');
         } else {
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/utils/XMLUtil.java`
#### Snippet
```java
         for (int i = 0; i < children.getLength(); i++) {
            String childToString = XMLUtil.elementToString(children.item(i));
            if (!"".equals(childToString)) {
               sb.append(childToString);
               hasValidChildren = true;
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/LegacyLDAPSecuritySettingPlugin.java`
#### Snippet
```java
      Hashtable<String, String> env = new Hashtable<>();
      env.put(Context.INITIAL_CONTEXT_FACTORY, initialContextFactory);
      if (connectionUsername != null && !"".equals(connectionUsername)) {
         env.put(Context.SECURITY_PRINCIPAL, connectionUsername);
      } else {
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/LegacyLDAPSecuritySettingPlugin.java`
#### Snippet
```java
         throw new NamingException("Empty username is not allowed");
      }
      if (connectionPassword != null && !"".equals(connectionPassword)) {
         env.put(Context.SECURITY_CREDENTIALS, connectionPassword);
      } else {
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/view/predicate/SessionFilterPredicate.java`
#### Snippet
```java
   @Override
   public void setField(String field) {
      if (field != null && !field.equals("")) {
         this.f = SessionField.valueOfName(field);

```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/view/predicate/QueueFilterPredicate.java`
#### Snippet
```java
   @Override
   public void setField(String field) {
      if (field != null && !field.equals("")) {
         this.f = QueueField.valueOfName(field);

```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/view/predicate/ProducerFilterPredicate.java`
#### Snippet
```java
   @Override
   public void setField(String field) {
      if (field != null && !field.equals("")) {
         this.f = ProducerField.valueOfName(field);

```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/view/predicate/ActiveMQFilterPredicate.java`
#### Snippet
```java

   public void setOperation(String operation) {
      if (operation != null && !operation.equals("")) {
         this.operation = Operation.valueOf(operation);
      }
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/view/predicate/ConsumerFilterPredicate.java`
#### Snippet
```java
   @Override
   public void setField(String field) {
      if (field != null && !field.equals("")) {
         this.f = ConsumerField.valueOfName(field);

```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/view/predicate/AddressFilterPredicate.java`
#### Snippet
```java
   @Override
   public void setField(String field) {
      if (field != null && !field.equals("")) {
         this.f = AddressField.valueOfName(field);

```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/view/predicate/ConnectionFilterPredicate.java`
#### Snippet
```java
   @Override
   public void setField(String field) {
      if (field != null && !field.equals("")) {
         this.f = ConnectionField.valueOfName(field);

```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/deployers/impl/FileConfigurationParser.java`
#### Snippet
```java
         config.setJournalRetentionPeriod(unit, period);

         if (directory == null || directory.equals("")) {
            throw new IllegalArgumentException("journal-retention-directory=null");
         }
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/utils/XMLConfigurationUtil.java`
#### Snippet
```java
                                          final Validators.Validator validator) {
      String attribute = e.getAttribute(name);
      if (attribute != null && !attribute.equals("")) {
         int val = XMLUtil.parseInt(e, attribute);
         validator.validate(name, val);
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/ActiveMQServerControlImpl.java`
#### Snippet
```java
      SimpleString filter = filterStr == null ? null : new SimpleString(filterStr);
      try {
         if (filterStr != null && !filterStr.trim().equals("")) {
            filter = new SimpleString(filterStr);
         }
```

## RuleId[id=UnnecessaryBoxing]
### UnnecessaryBoxing
Unnecessary boxing
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/ConstantExpression.java`
#### Snippet
```java

   public static ConstantExpression createFromHex(String text) {
      Number value = Long.valueOf(Long.parseLong(text.substring(2), 16));
      long l = value.longValue();
      if (Integer.MIN_VALUE <= l && l <= Integer.MAX_VALUE) {
```

### UnnecessaryBoxing
Unnecessary boxing
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/ConstantExpression.java`
#### Snippet
```java
      long l = value.longValue();
      if (Integer.MIN_VALUE <= l && l <= Integer.MAX_VALUE) {
         value = Integer.valueOf(value.intValue());
      }
      return new ConstantExpression(value);
```

### UnnecessaryBoxing
Unnecessary boxing
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/ConstantExpression.java`
#### Snippet
```java
      long l = value.longValue();
      if (Integer.MIN_VALUE <= l && l <= Integer.MAX_VALUE) {
         value = Integer.valueOf(value.intValue());
      }
      return new ConstantExpression(value);
```

### UnnecessaryBoxing
Unnecessary boxing
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/ConstantExpression.java`
#### Snippet
```java

   public static ConstantExpression createFromOctal(String text) {
      Number value = Long.valueOf(Long.parseLong(text, 8));
      long l = value.longValue();
      if (Integer.MIN_VALUE <= l && l <= Integer.MAX_VALUE) {
```

### UnnecessaryBoxing
Unnecessary boxing
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/ConstantExpression.java`
#### Snippet
```java
      long l = value.longValue();
      if (Integer.MIN_VALUE <= l && l <= Integer.MAX_VALUE) {
         value = Integer.valueOf(value.intValue());
      }
      return new ConstantExpression(value);
```

### UnnecessaryBoxing
Unnecessary boxing
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/UnaryExpression.java`
#### Snippet
```java

         if (BD_LONG_MIN_VALUE.compareTo(bd) == 0) {
            return Long.valueOf(Long.MIN_VALUE);
         }
         return bd;
```

### UnnecessaryBoxing
Unnecessary boxing
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/ComparisonExpression.java`
#### Snippet
```java
         } else if (c == '_') {
            regexp.append("."); // match one
         } else if (REGEXP_CONTROL_CHARS.contains(Character.valueOf(c))) {
            regexp.append("\\x");
            regexp.append(Integer.toHexString(0xFFFF & c));
```

### UnnecessaryBoxing
Unnecessary boxing
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/ComparisonExpression.java`
#### Snippet
```java

   static {
      REGEXP_CONTROL_CHARS.add(Character.valueOf('.'));
      REGEXP_CONTROL_CHARS.add(Character.valueOf('\\'));
      REGEXP_CONTROL_CHARS.add(Character.valueOf('['));
```

### UnnecessaryBoxing
Unnecessary boxing
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/ComparisonExpression.java`
#### Snippet
```java
   static {
      REGEXP_CONTROL_CHARS.add(Character.valueOf('.'));
      REGEXP_CONTROL_CHARS.add(Character.valueOf('\\'));
      REGEXP_CONTROL_CHARS.add(Character.valueOf('['));
      REGEXP_CONTROL_CHARS.add(Character.valueOf(']'));
```

### UnnecessaryBoxing
Unnecessary boxing
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/ComparisonExpression.java`
#### Snippet
```java
      REGEXP_CONTROL_CHARS.add(Character.valueOf('.'));
      REGEXP_CONTROL_CHARS.add(Character.valueOf('\\'));
      REGEXP_CONTROL_CHARS.add(Character.valueOf('['));
      REGEXP_CONTROL_CHARS.add(Character.valueOf(']'));
      REGEXP_CONTROL_CHARS.add(Character.valueOf('^'));
```

### UnnecessaryBoxing
Unnecessary boxing
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/ComparisonExpression.java`
#### Snippet
```java
      REGEXP_CONTROL_CHARS.add(Character.valueOf('\\'));
      REGEXP_CONTROL_CHARS.add(Character.valueOf('['));
      REGEXP_CONTROL_CHARS.add(Character.valueOf(']'));
      REGEXP_CONTROL_CHARS.add(Character.valueOf('^'));
      REGEXP_CONTROL_CHARS.add(Character.valueOf('$'));
```

### UnnecessaryBoxing
Unnecessary boxing
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/ComparisonExpression.java`
#### Snippet
```java
      REGEXP_CONTROL_CHARS.add(Character.valueOf('['));
      REGEXP_CONTROL_CHARS.add(Character.valueOf(']'));
      REGEXP_CONTROL_CHARS.add(Character.valueOf('^'));
      REGEXP_CONTROL_CHARS.add(Character.valueOf('$'));
      REGEXP_CONTROL_CHARS.add(Character.valueOf('?'));
```

### UnnecessaryBoxing
Unnecessary boxing
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/ComparisonExpression.java`
#### Snippet
```java
      REGEXP_CONTROL_CHARS.add(Character.valueOf(']'));
      REGEXP_CONTROL_CHARS.add(Character.valueOf('^'));
      REGEXP_CONTROL_CHARS.add(Character.valueOf('$'));
      REGEXP_CONTROL_CHARS.add(Character.valueOf('?'));
      REGEXP_CONTROL_CHARS.add(Character.valueOf('*'));
```

### UnnecessaryBoxing
Unnecessary boxing
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/ComparisonExpression.java`
#### Snippet
```java
      REGEXP_CONTROL_CHARS.add(Character.valueOf('^'));
      REGEXP_CONTROL_CHARS.add(Character.valueOf('$'));
      REGEXP_CONTROL_CHARS.add(Character.valueOf('?'));
      REGEXP_CONTROL_CHARS.add(Character.valueOf('*'));
      REGEXP_CONTROL_CHARS.add(Character.valueOf('+'));
```

### UnnecessaryBoxing
Unnecessary boxing
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/ComparisonExpression.java`
#### Snippet
```java
      REGEXP_CONTROL_CHARS.add(Character.valueOf('$'));
      REGEXP_CONTROL_CHARS.add(Character.valueOf('?'));
      REGEXP_CONTROL_CHARS.add(Character.valueOf('*'));
      REGEXP_CONTROL_CHARS.add(Character.valueOf('+'));
      REGEXP_CONTROL_CHARS.add(Character.valueOf('{'));
```

### UnnecessaryBoxing
Unnecessary boxing
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/ComparisonExpression.java`
#### Snippet
```java
      REGEXP_CONTROL_CHARS.add(Character.valueOf('?'));
      REGEXP_CONTROL_CHARS.add(Character.valueOf('*'));
      REGEXP_CONTROL_CHARS.add(Character.valueOf('+'));
      REGEXP_CONTROL_CHARS.add(Character.valueOf('{'));
      REGEXP_CONTROL_CHARS.add(Character.valueOf('}'));
```

### UnnecessaryBoxing
Unnecessary boxing
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/ComparisonExpression.java`
#### Snippet
```java
      REGEXP_CONTROL_CHARS.add(Character.valueOf('*'));
      REGEXP_CONTROL_CHARS.add(Character.valueOf('+'));
      REGEXP_CONTROL_CHARS.add(Character.valueOf('{'));
      REGEXP_CONTROL_CHARS.add(Character.valueOf('}'));
      REGEXP_CONTROL_CHARS.add(Character.valueOf('|'));
```

### UnnecessaryBoxing
Unnecessary boxing
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/ComparisonExpression.java`
#### Snippet
```java
      REGEXP_CONTROL_CHARS.add(Character.valueOf('+'));
      REGEXP_CONTROL_CHARS.add(Character.valueOf('{'));
      REGEXP_CONTROL_CHARS.add(Character.valueOf('}'));
      REGEXP_CONTROL_CHARS.add(Character.valueOf('|'));
      REGEXP_CONTROL_CHARS.add(Character.valueOf('('));
```

### UnnecessaryBoxing
Unnecessary boxing
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/ComparisonExpression.java`
#### Snippet
```java
      REGEXP_CONTROL_CHARS.add(Character.valueOf('{'));
      REGEXP_CONTROL_CHARS.add(Character.valueOf('}'));
      REGEXP_CONTROL_CHARS.add(Character.valueOf('|'));
      REGEXP_CONTROL_CHARS.add(Character.valueOf('('));
      REGEXP_CONTROL_CHARS.add(Character.valueOf(')'));
```

### UnnecessaryBoxing
Unnecessary boxing
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/ComparisonExpression.java`
#### Snippet
```java
      REGEXP_CONTROL_CHARS.add(Character.valueOf('}'));
      REGEXP_CONTROL_CHARS.add(Character.valueOf('|'));
      REGEXP_CONTROL_CHARS.add(Character.valueOf('('));
      REGEXP_CONTROL_CHARS.add(Character.valueOf(')'));
      REGEXP_CONTROL_CHARS.add(Character.valueOf(':'));
```

### UnnecessaryBoxing
Unnecessary boxing
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/ComparisonExpression.java`
#### Snippet
```java
      REGEXP_CONTROL_CHARS.add(Character.valueOf('|'));
      REGEXP_CONTROL_CHARS.add(Character.valueOf('('));
      REGEXP_CONTROL_CHARS.add(Character.valueOf(')'));
      REGEXP_CONTROL_CHARS.add(Character.valueOf(':'));
      REGEXP_CONTROL_CHARS.add(Character.valueOf('&'));
```

### UnnecessaryBoxing
Unnecessary boxing
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/ComparisonExpression.java`
#### Snippet
```java
      REGEXP_CONTROL_CHARS.add(Character.valueOf('('));
      REGEXP_CONTROL_CHARS.add(Character.valueOf(')'));
      REGEXP_CONTROL_CHARS.add(Character.valueOf(':'));
      REGEXP_CONTROL_CHARS.add(Character.valueOf('&'));
      REGEXP_CONTROL_CHARS.add(Character.valueOf('<'));
```

### UnnecessaryBoxing
Unnecessary boxing
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/ComparisonExpression.java`
#### Snippet
```java
      REGEXP_CONTROL_CHARS.add(Character.valueOf(')'));
      REGEXP_CONTROL_CHARS.add(Character.valueOf(':'));
      REGEXP_CONTROL_CHARS.add(Character.valueOf('&'));
      REGEXP_CONTROL_CHARS.add(Character.valueOf('<'));
      REGEXP_CONTROL_CHARS.add(Character.valueOf('>'));
```

### UnnecessaryBoxing
Unnecessary boxing
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/ComparisonExpression.java`
#### Snippet
```java
      REGEXP_CONTROL_CHARS.add(Character.valueOf(':'));
      REGEXP_CONTROL_CHARS.add(Character.valueOf('&'));
      REGEXP_CONTROL_CHARS.add(Character.valueOf('<'));
      REGEXP_CONTROL_CHARS.add(Character.valueOf('>'));
      REGEXP_CONTROL_CHARS.add(Character.valueOf('='));
```

### UnnecessaryBoxing
Unnecessary boxing
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/ComparisonExpression.java`
#### Snippet
```java
      REGEXP_CONTROL_CHARS.add(Character.valueOf('&'));
      REGEXP_CONTROL_CHARS.add(Character.valueOf('<'));
      REGEXP_CONTROL_CHARS.add(Character.valueOf('>'));
      REGEXP_CONTROL_CHARS.add(Character.valueOf('='));
      REGEXP_CONTROL_CHARS.add(Character.valueOf('!'));
```

### UnnecessaryBoxing
Unnecessary boxing
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/ComparisonExpression.java`
#### Snippet
```java
      REGEXP_CONTROL_CHARS.add(Character.valueOf('<'));
      REGEXP_CONTROL_CHARS.add(Character.valueOf('>'));
      REGEXP_CONTROL_CHARS.add(Character.valueOf('='));
      REGEXP_CONTROL_CHARS.add(Character.valueOf('!'));
   }
```

### UnnecessaryBoxing
Unnecessary boxing
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/ComparisonExpression.java`
#### Snippet
```java
      REGEXP_CONTROL_CHARS.add(Character.valueOf('>'));
      REGEXP_CONTROL_CHARS.add(Character.valueOf('='));
      REGEXP_CONTROL_CHARS.add(Character.valueOf('!'));
   }

```

### UnnecessaryBoxing
Redundant boxing, `Double.parseDouble()` call can be used instead
in `artemis-protocols/artemis-stomp-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/stomp/v11/StompFrameHandlerV11.java`
#### Snippet
```java
         if (connectionEntry != null) {
            String heartBeatToTtlModifierStr = (String) connection.getAcceptorUsed().getConfiguration().get(TransportConstants.HEART_BEAT_TO_CONNECTION_TTL_MODIFIER);
            double heartBeatToTtlModifier = heartBeatToTtlModifierStr == null ? 2 : Double.valueOf(heartBeatToTtlModifierStr);

            // the default response to the client
```

### UnnecessaryBoxing
Redundant boxing, `Long.parseLong()` call can be used instead
in `artemis-protocols/artemis-stomp-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/stomp/v11/StompFrameHandlerV11.java`
#### Snippet
```java
               clientPingResponse = clientPing;
               String ttlMaxStr = (String) connection.getAcceptorUsed().getConfiguration().get(TransportConstants.CONNECTION_TTL_MAX);
               long ttlMax = ttlMaxStr == null ? Long.MAX_VALUE : Long.valueOf(ttlMaxStr);

               String ttlMinStr = (String) connection.getAcceptorUsed().getConfiguration().get(TransportConstants.CONNECTION_TTL_MIN);
```

### UnnecessaryBoxing
Redundant boxing, `Long.parseLong()` call can be used instead
in `artemis-protocols/artemis-stomp-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/stomp/v11/StompFrameHandlerV11.java`
#### Snippet
```java

               String ttlMinStr = (String) connection.getAcceptorUsed().getConfiguration().get(TransportConstants.CONNECTION_TTL_MIN);
               long ttlMin = ttlMinStr == null ? 1000 : Long.valueOf(ttlMinStr);

               /* The connection's TTL should be one of the following:
```

### UnnecessaryBoxing
Redundant boxing, `Long.parseLong()` call can be used instead
in `artemis-protocols/artemis-stomp-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/stomp/v11/StompFrameHandlerV11.java`
#### Snippet
```java

      //client ping
      long minPingInterval = Long.valueOf(params[0]);
      //client receive ping
      long minAcceptInterval = Long.valueOf(params[1]);
```

### UnnecessaryBoxing
Redundant boxing, `Long.parseLong()` call can be used instead
in `artemis-protocols/artemis-stomp-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/stomp/v11/StompFrameHandlerV11.java`
#### Snippet
```java
      long minPingInterval = Long.valueOf(params[0]);
      //client receive ping
      long minAcceptInterval = Long.valueOf(params[1]);

      if (heartBeater == null) {
```

### UnnecessaryBoxing
Unnecessary boxing
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/ByteUtil.java`
#### Snippet
```java
         Matcher m = ONE.matcher(text);
         if (m.matches()) {
            return Long.valueOf(Long.parseLong(m.group(1)));
         }

```

### UnnecessaryBoxing
Unnecessary boxing
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/ByteUtil.java`
#### Snippet
```java
         m = KILO.matcher(text);
         if (m.matches()) {
            return Long.valueOf(Long.parseLong(m.group(1)) * 1024);
         }

```

### UnnecessaryBoxing
Unnecessary boxing
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/ByteUtil.java`
#### Snippet
```java
         m = MEGA.matcher(text);
         if (m.matches()) {
            return Long.valueOf(Long.parseLong(m.group(1)) * 1024 * 1024);
         }

```

### UnnecessaryBoxing
Unnecessary boxing
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/ByteUtil.java`
#### Snippet
```java
         m = GIGA.matcher(text);
         if (m.matches()) {
            return Long.valueOf(Long.parseLong(m.group(1)) * 1024 * 1024 * 1024);
         }

```

### UnnecessaryBoxing
Unnecessary boxing
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/TypedProperties.java`
#### Snippet
```java
      Object value = doGetProperty(key);
      if (value == null)
         return Float.valueOf(null);
      if (value instanceof Float) {
         return ((Float) value);
```

### UnnecessaryBoxing
Unnecessary boxing
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/TypedProperties.java`
#### Snippet
```java
      Object value = doGetProperty(key);
      if (value == null) {
         return Short.valueOf(null);
      } else if (value instanceof Byte) {
         return ((Byte) value).shortValue();
```

### UnnecessaryBoxing
Unnecessary boxing
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/TypedProperties.java`
#### Snippet
```java
      Object value = doGetProperty(key);
      if (value == null) {
         return Integer.valueOf(null);
      } else if (value instanceof Integer) {
         return (Integer) value;
```

### UnnecessaryBoxing
Unnecessary boxing
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/TypedProperties.java`
#### Snippet
```java
      Object value = doGetProperty(key);
      if (value == null) {
         return Long.valueOf(null);
      } else if (value instanceof Long) {
         return (Long) value;
```

### UnnecessaryBoxing
Unnecessary boxing
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/TypedProperties.java`
#### Snippet
```java

   public Byte getByteProperty(final SimpleString key) throws ActiveMQPropertyConversionException {
      return getByteProperty(key, () -> Byte.valueOf(null));
   }

```

### UnnecessaryBoxing
Unnecessary boxing
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/TypedProperties.java`
#### Snippet
```java
      Object value = doGetProperty(key);
      if (value == null) {
         return Boolean.valueOf(null);
      } else if (value instanceof Boolean) {
         return (Boolean) value;
```

### UnnecessaryBoxing
Unnecessary boxing
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/TypedProperties.java`
#### Snippet
```java
      Object value = doGetProperty(key);
      if (value == null) {
         return Double.valueOf(null);
      } else if (value instanceof Float) {
         return ((Float) value).doubleValue();
```

### UnnecessaryBoxing
Redundant boxing, `Integer.parseInt()` call can be used instead
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/ActiveMQRAConnectionMetaData.java`
#### Snippet
```java
      }
      JMS_VERSION_NAME = versionProps.getProperty("activemq.version.implementation.versionName", "2.0");
      JMS_MAJOR_VERSION = Integer.valueOf(versionProps.getProperty("activemq.version.implementation.majorVersion", "2"));
      JMS_MINOR_VERSION = Integer.valueOf(versionProps.getProperty("activemq.version.implementation.minorVersion", "0"));
   }
```

### UnnecessaryBoxing
Redundant boxing, `Integer.parseInt()` call can be used instead
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/ActiveMQRAConnectionMetaData.java`
#### Snippet
```java
      JMS_VERSION_NAME = versionProps.getProperty("activemq.version.implementation.versionName", "2.0");
      JMS_MAJOR_VERSION = Integer.valueOf(versionProps.getProperty("activemq.version.implementation.majorVersion", "2"));
      JMS_MINOR_VERSION = Integer.valueOf(versionProps.getProperty("activemq.version.implementation.minorVersion", "0"));
   }

```

### UnnecessaryBoxing
Unnecessary boxing
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/PrintData.java`
#### Snippet
```java
            encoding.decode(buff);

            Long queueID = Long.valueOf(encoding.queueID);
            Long pageNR = Long.valueOf(encoding.position.getPageNr());

```

### UnnecessaryBoxing
Unnecessary boxing
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/PrintData.java`
#### Snippet
```java

            Long queueID = Long.valueOf(encoding.queueID);
            Long pageNR = Long.valueOf(encoding.position.getPageNr());

            if (!cursorInfo.getCompletePages(queueID).add(pageNR)) {
```

### UnnecessaryBoxing
Unnecessary boxing
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/PrintData.java`
#### Snippet
```java
                        }

                        if (cursorACKs.getCompletePages(q[i]).contains(Long.valueOf(pgid))) {
                           acked = true;
                           out.print(" (PG-COMPLETE)");
```

### UnnecessaryBoxing
Redundant boxing, `Integer.parseInt()` call can be used instead
in `artemis-cli/src/main/java/org/apache/activemq/artemis/util/ServerUtil.java`
#### Snippet
```java
         TransportConfiguration transportConfiguration = session.getSessionFactory().getConnectorConfiguration();
         String port = (String) transportConfiguration.getParams().get("port");
         if (Integer.valueOf(port) == server + 61616) {
            return connection;
         }
```

### UnnecessaryBoxing
Redundant boxing, `Integer.parseInt()` call can be used instead
in `artemis-cli/src/main/java/org/apache/activemq/artemis/util/ServerUtil.java`
#### Snippet
```java
      TransportConfiguration transportConfiguration = session.getSessionFactory().getConnectorConfiguration();
      String port = (String) transportConfiguration.getParams().get("port");
      return Integer.valueOf(port) - 61616;
   }

```

### UnnecessaryBoxing
Redundant boxing, `Boolean.parseBoolean()` call can be used instead
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/util/OpenWireUtil.java`
#### Snippet
```java
      if (info.getSelector() != null) {
         if (openWireDest.getOptions()  != null) {
            if (Boolean.valueOf(openWireDest.getOptions().get(SELECTOR_AWARE_OPTION))) {
               return info.getSelector();
            }
```

### UnnecessaryBoxing
Redundant boxing, `Boolean.parseBoolean()` call can be used instead
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenWireProtocolManager.java`
#### Snippet
```java
         if (optionPair.length == 2) {
            if (SELECTOR_AWARE_OPTION.equals(optionPair[0])) {
               selectorAware = Boolean.valueOf(optionPair[1]);
            }
         }
```

### UnnecessaryBoxing
Redundant boxing, `Integer.parseInt()` call can be used instead
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenWireProtocolManager.java`
#### Snippet
```java

      public VirtualTopicConfig(String[] configuration) {
         filterPathTerminus = Integer.valueOf(configuration[1]);
         // optional config
         for (int i = 2; i < configuration.length; i++) {
```

### UnnecessaryBoxing
Redundant boxing, `Integer.parseInt()` call can be used instead
in `artemis-protocols/artemis-mqtt-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/mqtt/MQTTPublishManager.java`
#### Snippet
```java
            if (propertyName.startsWith(MQTT_USER_PROPERTY_KEY_PREFIX_SIMPLE)) {
               SimpleString[] split = propertyName.split('.');
               int position = Integer.valueOf(split[4].toString());
               String key = propertyName.subSeq(MQTT_USER_PROPERTY_KEY_PREFIX_SIMPLE.length() + split[4].length() + 1, propertyName.length()).toString();
               orderedProperties[position] = new MqttProperties.StringPair(key, message.getStringProperty(propertyName));
```

### UnnecessaryBoxing
Redundant boxing, `Integer.parseInt()` call can be used instead
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQConnectionMetaData.java`
#### Snippet
```java
      }
      JMS_VERSION_NAME = versionProps.getProperty("activemq.version.implementation.versionName", "2.0");
      JMS_MAJOR_VERSION = Integer.valueOf(versionProps.getProperty("activemq.version.implementation.majorVersion", "2"));
      JMS_MINOR_VERSION = Integer.valueOf(versionProps.getProperty("activemq.version.implementation.minorVersion", "0"));
   }
```

### UnnecessaryBoxing
Redundant boxing, `Integer.parseInt()` call can be used instead
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQConnectionMetaData.java`
#### Snippet
```java
      JMS_VERSION_NAME = versionProps.getProperty("activemq.version.implementation.versionName", "2.0");
      JMS_MAJOR_VERSION = Integer.valueOf(versionProps.getProperty("activemq.version.implementation.majorVersion", "2"));
      JMS_MINOR_VERSION = Integer.valueOf(versionProps.getProperty("activemq.version.implementation.minorVersion", "0"));
   }

```

### UnnecessaryBoxing
Unnecessary boxing
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
   @Override
   public final org.apache.activemq.artemis.api.core.Message putShortProperty(String key, short value) {
      getApplicationPropertiesMap(true).put(key, Short.valueOf(value));
      return this;
   }
```

### UnnecessaryBoxing
Unnecessary boxing
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
   @Override
   public final org.apache.activemq.artemis.api.core.Message putBooleanProperty(String key, boolean value) {
      getApplicationPropertiesMap(true).put(key, Boolean.valueOf(value));
      return this;
   }
```

### UnnecessaryBoxing
Unnecessary boxing
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
   @Override
   public final org.apache.activemq.artemis.api.core.Message putLongProperty(String key, long value) {
      getApplicationPropertiesMap(true).put(key, Long.valueOf(value));
      return this;
   }
```

### UnnecessaryBoxing
Unnecessary boxing
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
   @Override
   public final org.apache.activemq.artemis.api.core.Message putCharProperty(String key, char value) {
      getApplicationPropertiesMap(true).put(key, Character.valueOf(value));
      return this;
   }
```

### UnnecessaryBoxing
Unnecessary boxing
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
   @Override
   public final org.apache.activemq.artemis.api.core.Message putDoubleProperty(String key, double value) {
      getApplicationPropertiesMap(true).put(key, Double.valueOf(value));
      return this;
   }
```

### UnnecessaryBoxing
Unnecessary boxing
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
   @Override
   public final org.apache.activemq.artemis.api.core.Message putBooleanProperty(SimpleString key, boolean value) {
      getApplicationPropertiesMap(true).put(key.toString(), Boolean.valueOf(value));
      return this;
   }
```

### UnnecessaryBoxing
Unnecessary boxing
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
   @Override
   public final org.apache.activemq.artemis.api.core.Message putByteProperty(String key, byte value) {
      getApplicationPropertiesMap(true).put(key, Byte.valueOf(value));
      return this;
   }
```

### UnnecessaryBoxing
Unnecessary boxing
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
   @Override
   public final org.apache.activemq.artemis.api.core.Message putFloatProperty(String key, float value) {
      getApplicationPropertiesMap(true).put(key, Float.valueOf(value));
      return this;
   }
```

### UnnecessaryBoxing
Unnecessary boxing
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
   @Override
   public final org.apache.activemq.artemis.api.core.Message putIntProperty(String key, int value) {
      getApplicationPropertiesMap(true).put(key, Integer.valueOf(value));
      return this;
   }
```

### UnnecessaryBoxing
Redundant boxing, `Boolean.parseBoolean()` call can be used instead
in `artemis-jdbc-store/src/main/java/org/apache/activemq/artemis/jdbc/store/sql/PropertySQLProvider.java`
#### Snippet
```java
   @Override
   public boolean closeConnectionOnShutdown() {
      return Boolean.valueOf(sql("close-connection-on-shutdown"));
   }

```

### UnnecessaryBoxing
Redundant boxing, `Long.parseLong()` call can be used instead
in `artemis-jdbc-store/src/main/java/org/apache/activemq/artemis/jdbc/store/sql/PropertySQLProvider.java`
#### Snippet
```java
   @Override
   public long getMaxBlobSize() {
      return Long.valueOf(sql("max-blob-size"));
   }

```

### UnnecessaryBoxing
Redundant boxing, `Integer.parseInt()` call can be used instead
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/uri/schema/serverLocator/TCPServerLocatorSchema.java`
#### Snippet
```java
      Object port = params.get("port");
      if (port instanceof String) {
         return Integer.valueOf((String) port);
      }
      return port != null ? (int) port : 61616;
```

### UnnecessaryBoxing
Redundant boxing, `Boolean.parseBoolean()` call can be used instead
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/impl/ssl/SSLSupport.java`
#### Snippet
```java
         TrustManagerFactory trustMgrFactory = TrustManagerFactory.getInstance(TrustManagerFactory.getDefaultAlgorithm());
         KeyStore trustStore = SSLSupport.loadKeystore(truststoreProvider, truststoreType, truststorePath, truststorePassword);
         boolean ocsp = Boolean.valueOf(Security.getProperty("ocsp.enable"));

         boolean initialized = false;
```

### UnnecessaryBoxing
Redundant boxing, `Long.parseLong()` call can be used instead
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/utils/ConfigurationHelper.java`
#### Snippet
```java
         if (prop instanceof String) {
            try {
               return Long.valueOf((String) prop);
            } catch (NumberFormatException e) {
               ActiveMQClientLogger.LOGGER.propertyNotLong(propName, prop.getClass().getName());
```

### UnnecessaryBoxing
Redundant boxing, `Integer.parseInt()` call can be used instead
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/utils/ConfigurationHelper.java`
#### Snippet
```java
         if (prop instanceof String) {
            try {
               return Integer.valueOf((String) prop);
            } catch (NumberFormatException e) {
               ActiveMQClientLogger.LOGGER.propertyNotInteger(propName, prop.getClass().getName());
```

### UnnecessaryBoxing
Redundant boxing, `Boolean.parseBoolean()` call can be used instead
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/utils/ConfigurationHelper.java`
#### Snippet
```java
         // The resource adapter will aways send Strings, hence the conversion here
         if (prop instanceof String) {
            return Boolean.valueOf((String) prop);
         } else if (prop instanceof Boolean == false) {
            ActiveMQClientLogger.LOGGER.propertyNotBoolean(propName, prop.getClass().getName());
```

### UnnecessaryBoxing
Redundant boxing, `Integer.parseInt()` call can be used instead
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/utils/VersionLoader.java`
#### Snippet
```java
            versionProps.load(in);
            String versionName = versionProps.getProperty("activemq.version.versionName");
            int majorVersion = Integer.valueOf(versionProps.getProperty("activemq.version.majorVersion"));
            int minorVersion = Integer.valueOf(versionProps.getProperty("activemq.version.minorVersion"));
            int microVersion = Integer.valueOf(versionProps.getProperty("activemq.version.microVersion"));
```

### UnnecessaryBoxing
Redundant boxing, `Integer.parseInt()` call can be used instead
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/utils/VersionLoader.java`
#### Snippet
```java
            String versionName = versionProps.getProperty("activemq.version.versionName");
            int majorVersion = Integer.valueOf(versionProps.getProperty("activemq.version.majorVersion"));
            int minorVersion = Integer.valueOf(versionProps.getProperty("activemq.version.minorVersion"));
            int microVersion = Integer.valueOf(versionProps.getProperty("activemq.version.microVersion"));
            int[] incrementingVersions = parseCompatibleVersionList(versionProps.getProperty("activemq.version.incrementingVersion"));
```

### UnnecessaryBoxing
Redundant boxing, `Integer.parseInt()` call can be used instead
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/utils/VersionLoader.java`
#### Snippet
```java
            int majorVersion = Integer.valueOf(versionProps.getProperty("activemq.version.majorVersion"));
            int minorVersion = Integer.valueOf(versionProps.getProperty("activemq.version.minorVersion"));
            int microVersion = Integer.valueOf(versionProps.getProperty("activemq.version.microVersion"));
            int[] incrementingVersions = parseCompatibleVersionList(versionProps.getProperty("activemq.version.incrementingVersion"));
            int[] compatibleVersionArray = parseCompatibleVersionList(versionProps.getProperty("activemq.version.compatibleVersionList"));
```

### UnnecessaryBoxing
Redundant boxing, `Boolean.parseBoolean()` call can be used instead
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/reader/StreamMessageUtil.java`
#### Snippet
```java
         case DataConstants.STRING:
            String s = buff.readNullableString();
            return Boolean.valueOf(s);
         default:
            throw new IllegalStateException("Invalid conversion, type byte was " + type);
```

### UnnecessaryBoxing
Unnecessary boxing
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/filter/impl/FilterImpl.java`
#### Snippet
```java
         }
      } else if (FilterConstants.ACTIVEMQ_PRIORITY.equals(fieldName)) {
         return Integer.valueOf(msg.getPriority());
      } else if (FilterConstants.ACTIVEMQ_TIMESTAMP.equals(fieldName)) {
         return msg.getTimestamp();
```

### UnnecessaryBoxing
Redundant boxing, `Integer.parseInt()` call can be used instead
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/impl/PagingManagerImpl.java`
#### Snippet
```java
public final class PagingManagerImpl implements PagingManager {

   private static final int ARTEMIS_PAGING_COUNTER_SNAPSHOT_INTERVAL = Integer.valueOf(System.getProperty("artemis.paging.counter.snapshot.interval", "60"));

   private static final Logger logger = LoggerFactory.getLogger(MethodHandles.lookup().lookupClass());
```

### UnnecessaryBoxing
Unnecessary boxing
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/cursor/impl/PageSubscriptionImpl.java`
#### Snippet
```java
      PageCursorInfo info;
      synchronized (consumedPages) {
         info = consumedPages.remove(Long.valueOf(deletedPage.getPageId()));
      }
      if (info != null) {
```

### UnnecessaryBoxing
Unnecessary boxing
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/cursor/impl/PageSubscriptionImpl.java`
#### Snippet
```java
      info.setCompleteInfo(position);
      synchronized (consumedPages) {
         consumedPages.put(Long.valueOf(position.getPageNr()), info);
      }

```

### UnnecessaryBoxing
Redundant boxing, `Boolean.parseBoolean()` call can be used instead
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/AddressInfo.java`
#### Snippet
```java
         this.name = SimpleString.toSimpleString(jasonString.getString());
      } else if (key.equals("auto-created")) {
         this.autoCreated = Boolean.valueOf(value.toString());
      } else if (key.equals("temporary")) {
         this.temporary = Boolean.valueOf(value.toString());
```

### UnnecessaryBoxing
Redundant boxing, `Boolean.parseBoolean()` call can be used instead
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/AddressInfo.java`
#### Snippet
```java
         this.autoCreated = Boolean.valueOf(value.toString());
      } else if (key.equals("temporary")) {
         this.temporary = Boolean.valueOf(value.toString());
      } else if (key.equals("firstSeen")) {
         JsonNumber jsonNumber = (JsonNumber)value;
```

### UnnecessaryBoxing
Redundant boxing, `Integer.parseInt()` call can be used instead
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/client/ActiveMQClient.java`
#### Snippet
```java
   public static void initializeGlobalThreadPoolProperties() {

      setGlobalThreadPoolProperties(Integer.valueOf(System.getProperty(ActiveMQClient.THREAD_POOL_MAX_SIZE_PROPERTY_KEY, "" + ActiveMQClient.DEFAULT_GLOBAL_THREAD_POOL_MAX_SIZE)), Integer.valueOf(System.getProperty(ActiveMQClient.SCHEDULED_THREAD_POOL_SIZE_PROPERTY_KEY, "" + ActiveMQClient.DEFAULT_SCHEDULED_THREAD_POOL_MAX_SIZE)));
   }

```

### UnnecessaryBoxing
Redundant boxing, `Integer.parseInt()` call can be used instead
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/client/ActiveMQClient.java`
#### Snippet
```java
   public static void initializeGlobalThreadPoolProperties() {

      setGlobalThreadPoolProperties(Integer.valueOf(System.getProperty(ActiveMQClient.THREAD_POOL_MAX_SIZE_PROPERTY_KEY, "" + ActiveMQClient.DEFAULT_GLOBAL_THREAD_POOL_MAX_SIZE)), Integer.valueOf(System.getProperty(ActiveMQClient.SCHEDULED_THREAD_POOL_SIZE_PROPERTY_KEY, "" + ActiveMQClient.DEFAULT_SCHEDULED_THREAD_POOL_MAX_SIZE)));
   }

```

### UnnecessaryBoxing
Redundant boxing, `Integer.parseInt()` call can be used instead
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/routing/policies/LeastConnectionsPolicy.java`
#### Snippet
```java
      if (properties != null) {
         if (properties.containsKey(CONNECTION_COUNT_THRESHOLD)) {
            connectionCountThreshold = Integer.valueOf(properties.get(CONNECTION_COUNT_THRESHOLD));
         }
      }
```

### UnnecessaryBoxing
Unnecessary boxing
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/impl/ManagementServiceImpl.java`
#### Snippet
```java
      if (metricsManager != null) {
         metricsManager.registerBrokerGauge(builder -> {
            builder.build(BrokerMetricNames.CONNECTION_COUNT, messagingServer, metrics -> Double.valueOf(messagingServer.getConnectionCount()), ActiveMQServerControl.CONNECTION_COUNT_DESCRIPTION);
            builder.build(BrokerMetricNames.TOTAL_CONNECTION_COUNT, messagingServer, metrics -> Double.valueOf(messagingServer.getTotalConnectionCount()), ActiveMQServerControl.TOTAL_CONNECTION_COUNT_DESCRIPTION);
            builder.build(BrokerMetricNames.ADDRESS_MEMORY_USAGE, messagingServer, metrics -> Double.valueOf(messagingServerControl.getAddressMemoryUsage()), ActiveMQServerControl.ADDRESS_MEMORY_USAGE_DESCRIPTION);
```

### UnnecessaryBoxing
Unnecessary boxing
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/impl/ManagementServiceImpl.java`
#### Snippet
```java
         metricsManager.registerBrokerGauge(builder -> {
            builder.build(BrokerMetricNames.CONNECTION_COUNT, messagingServer, metrics -> Double.valueOf(messagingServer.getConnectionCount()), ActiveMQServerControl.CONNECTION_COUNT_DESCRIPTION);
            builder.build(BrokerMetricNames.TOTAL_CONNECTION_COUNT, messagingServer, metrics -> Double.valueOf(messagingServer.getTotalConnectionCount()), ActiveMQServerControl.TOTAL_CONNECTION_COUNT_DESCRIPTION);
            builder.build(BrokerMetricNames.ADDRESS_MEMORY_USAGE, messagingServer, metrics -> Double.valueOf(messagingServerControl.getAddressMemoryUsage()), ActiveMQServerControl.ADDRESS_MEMORY_USAGE_DESCRIPTION);
            builder.build(BrokerMetricNames.ADDRESS_MEMORY_USAGE_PERCENTAGE, messagingServer, metrics -> Double.valueOf(messagingServerControl.getAddressMemoryUsagePercentage()), ActiveMQServerControl.ADDRESS_MEMORY_USAGE_PERCENTAGE_DESCRIPTION);
```

### UnnecessaryBoxing
Unnecessary boxing
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/impl/ManagementServiceImpl.java`
#### Snippet
```java
            builder.build(BrokerMetricNames.CONNECTION_COUNT, messagingServer, metrics -> Double.valueOf(messagingServer.getConnectionCount()), ActiveMQServerControl.CONNECTION_COUNT_DESCRIPTION);
            builder.build(BrokerMetricNames.TOTAL_CONNECTION_COUNT, messagingServer, metrics -> Double.valueOf(messagingServer.getTotalConnectionCount()), ActiveMQServerControl.TOTAL_CONNECTION_COUNT_DESCRIPTION);
            builder.build(BrokerMetricNames.ADDRESS_MEMORY_USAGE, messagingServer, metrics -> Double.valueOf(messagingServerControl.getAddressMemoryUsage()), ActiveMQServerControl.ADDRESS_MEMORY_USAGE_DESCRIPTION);
            builder.build(BrokerMetricNames.ADDRESS_MEMORY_USAGE_PERCENTAGE, messagingServer, metrics -> Double.valueOf(messagingServerControl.getAddressMemoryUsagePercentage()), ActiveMQServerControl.ADDRESS_MEMORY_USAGE_PERCENTAGE_DESCRIPTION);
            builder.build(BrokerMetricNames.DISK_STORE_USAGE, messagingServer, metrics -> Double.valueOf(messagingServer.getDiskStoreUsage()), ActiveMQServerControl.DISK_STORE_USAGE_DESCRIPTION);
```

### UnnecessaryBoxing
Unnecessary boxing
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/impl/ManagementServiceImpl.java`
#### Snippet
```java
            builder.build(BrokerMetricNames.TOTAL_CONNECTION_COUNT, messagingServer, metrics -> Double.valueOf(messagingServer.getTotalConnectionCount()), ActiveMQServerControl.TOTAL_CONNECTION_COUNT_DESCRIPTION);
            builder.build(BrokerMetricNames.ADDRESS_MEMORY_USAGE, messagingServer, metrics -> Double.valueOf(messagingServerControl.getAddressMemoryUsage()), ActiveMQServerControl.ADDRESS_MEMORY_USAGE_DESCRIPTION);
            builder.build(BrokerMetricNames.ADDRESS_MEMORY_USAGE_PERCENTAGE, messagingServer, metrics -> Double.valueOf(messagingServerControl.getAddressMemoryUsagePercentage()), ActiveMQServerControl.ADDRESS_MEMORY_USAGE_PERCENTAGE_DESCRIPTION);
            builder.build(BrokerMetricNames.DISK_STORE_USAGE, messagingServer, metrics -> Double.valueOf(messagingServer.getDiskStoreUsage()), ActiveMQServerControl.DISK_STORE_USAGE_DESCRIPTION);
         });
```

### UnnecessaryBoxing
Unnecessary boxing
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/impl/ManagementServiceImpl.java`
#### Snippet
```java
            builder.build(BrokerMetricNames.ADDRESS_MEMORY_USAGE, messagingServer, metrics -> Double.valueOf(messagingServerControl.getAddressMemoryUsage()), ActiveMQServerControl.ADDRESS_MEMORY_USAGE_DESCRIPTION);
            builder.build(BrokerMetricNames.ADDRESS_MEMORY_USAGE_PERCENTAGE, messagingServer, metrics -> Double.valueOf(messagingServerControl.getAddressMemoryUsagePercentage()), ActiveMQServerControl.ADDRESS_MEMORY_USAGE_PERCENTAGE_DESCRIPTION);
            builder.build(BrokerMetricNames.DISK_STORE_USAGE, messagingServer, metrics -> Double.valueOf(messagingServer.getDiskStoreUsage()), ActiveMQServerControl.DISK_STORE_USAGE_DESCRIPTION);
         });
      }
```

### UnnecessaryBoxing
Unnecessary boxing
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/impl/ManagementServiceImpl.java`
#### Snippet
```java
         if (metricsManager != null) {
            metricsManager.registerAddressGauge(addressInfo.getName().toString(), builder -> {
               builder.build(AddressMetricNames.ROUTED_MESSAGE_COUNT, addressInfo, metrics -> Double.valueOf(addressInfo.getRoutedMessageCount()), AddressControl.ROUTED_MESSAGE_COUNT_DESCRIPTION);
               builder.build(AddressMetricNames.UNROUTED_MESSAGE_COUNT, addressInfo, metrics -> Double.valueOf(addressInfo.getUnRoutedMessageCount()), AddressControl.UNROUTED_MESSAGE_COUNT_DESCRIPTION);
               builder.build(AddressMetricNames.ADDRESS_SIZE, addressInfo, metrics -> Double.valueOf(addressControl.getAddressSize()), AddressControl.ADDRESS_SIZE_DESCRIPTION);
```

### UnnecessaryBoxing
Unnecessary boxing
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/impl/ManagementServiceImpl.java`
#### Snippet
```java
            metricsManager.registerAddressGauge(addressInfo.getName().toString(), builder -> {
               builder.build(AddressMetricNames.ROUTED_MESSAGE_COUNT, addressInfo, metrics -> Double.valueOf(addressInfo.getRoutedMessageCount()), AddressControl.ROUTED_MESSAGE_COUNT_DESCRIPTION);
               builder.build(AddressMetricNames.UNROUTED_MESSAGE_COUNT, addressInfo, metrics -> Double.valueOf(addressInfo.getUnRoutedMessageCount()), AddressControl.UNROUTED_MESSAGE_COUNT_DESCRIPTION);
               builder.build(AddressMetricNames.ADDRESS_SIZE, addressInfo, metrics -> Double.valueOf(addressControl.getAddressSize()), AddressControl.ADDRESS_SIZE_DESCRIPTION);
               builder.build(AddressMetricNames.PAGES_COUNT, addressInfo, metrics -> Double.valueOf(addressControl.getNumberOfPages()), AddressControl.NUMBER_OF_PAGES_DESCRIPTION);
```

### UnnecessaryBoxing
Unnecessary boxing
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/impl/ManagementServiceImpl.java`
#### Snippet
```java
               builder.build(AddressMetricNames.ROUTED_MESSAGE_COUNT, addressInfo, metrics -> Double.valueOf(addressInfo.getRoutedMessageCount()), AddressControl.ROUTED_MESSAGE_COUNT_DESCRIPTION);
               builder.build(AddressMetricNames.UNROUTED_MESSAGE_COUNT, addressInfo, metrics -> Double.valueOf(addressInfo.getUnRoutedMessageCount()), AddressControl.UNROUTED_MESSAGE_COUNT_DESCRIPTION);
               builder.build(AddressMetricNames.ADDRESS_SIZE, addressInfo, metrics -> Double.valueOf(addressControl.getAddressSize()), AddressControl.ADDRESS_SIZE_DESCRIPTION);
               builder.build(AddressMetricNames.PAGES_COUNT, addressInfo, metrics -> Double.valueOf(addressControl.getNumberOfPages()), AddressControl.NUMBER_OF_PAGES_DESCRIPTION);
            });
```

### UnnecessaryBoxing
Unnecessary boxing
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/impl/ManagementServiceImpl.java`
#### Snippet
```java
               builder.build(AddressMetricNames.UNROUTED_MESSAGE_COUNT, addressInfo, metrics -> Double.valueOf(addressInfo.getUnRoutedMessageCount()), AddressControl.UNROUTED_MESSAGE_COUNT_DESCRIPTION);
               builder.build(AddressMetricNames.ADDRESS_SIZE, addressInfo, metrics -> Double.valueOf(addressControl.getAddressSize()), AddressControl.ADDRESS_SIZE_DESCRIPTION);
               builder.build(AddressMetricNames.PAGES_COUNT, addressInfo, metrics -> Double.valueOf(addressControl.getNumberOfPages()), AddressControl.NUMBER_OF_PAGES_DESCRIPTION);
            });
         }
```

### UnnecessaryBoxing
Unnecessary boxing
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/impl/ManagementServiceImpl.java`
#### Snippet
```java
         if (metricsManager != null) {
            metricsManager.registerQueueGauge(queue.getAddress().toString(), queue.getName().toString(), (builder) -> {
               builder.build(QueueMetricNames.MESSAGE_COUNT, queue, metrics -> Double.valueOf(queue.getMessageCount()), QueueControl.MESSAGE_COUNT_DESCRIPTION);
               builder.build(QueueMetricNames.DURABLE_MESSAGE_COUNT, queue, metrics -> Double.valueOf(queue.getDurableMessageCount()), QueueControl.DURABLE_MESSAGE_COUNT_DESCRIPTION);
               builder.build(QueueMetricNames.PERSISTENT_SIZE, queue, metrics -> Double.valueOf(queue.getPersistentSize()), QueueControl.PERSISTENT_SIZE_DESCRIPTION);
```

### UnnecessaryBoxing
Unnecessary boxing
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/impl/ManagementServiceImpl.java`
#### Snippet
```java
            metricsManager.registerQueueGauge(queue.getAddress().toString(), queue.getName().toString(), (builder) -> {
               builder.build(QueueMetricNames.MESSAGE_COUNT, queue, metrics -> Double.valueOf(queue.getMessageCount()), QueueControl.MESSAGE_COUNT_DESCRIPTION);
               builder.build(QueueMetricNames.DURABLE_MESSAGE_COUNT, queue, metrics -> Double.valueOf(queue.getDurableMessageCount()), QueueControl.DURABLE_MESSAGE_COUNT_DESCRIPTION);
               builder.build(QueueMetricNames.PERSISTENT_SIZE, queue, metrics -> Double.valueOf(queue.getPersistentSize()), QueueControl.PERSISTENT_SIZE_DESCRIPTION);
               builder.build(QueueMetricNames.DURABLE_PERSISTENT_SIZE, queue, metrics -> Double.valueOf(queue.getDurablePersistentSize()), QueueControl.DURABLE_PERSISTENT_SIZE_DESCRIPTION);
```

### UnnecessaryBoxing
Unnecessary boxing
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/impl/ManagementServiceImpl.java`
#### Snippet
```java
               builder.build(QueueMetricNames.MESSAGE_COUNT, queue, metrics -> Double.valueOf(queue.getMessageCount()), QueueControl.MESSAGE_COUNT_DESCRIPTION);
               builder.build(QueueMetricNames.DURABLE_MESSAGE_COUNT, queue, metrics -> Double.valueOf(queue.getDurableMessageCount()), QueueControl.DURABLE_MESSAGE_COUNT_DESCRIPTION);
               builder.build(QueueMetricNames.PERSISTENT_SIZE, queue, metrics -> Double.valueOf(queue.getPersistentSize()), QueueControl.PERSISTENT_SIZE_DESCRIPTION);
               builder.build(QueueMetricNames.DURABLE_PERSISTENT_SIZE, queue, metrics -> Double.valueOf(queue.getDurablePersistentSize()), QueueControl.DURABLE_PERSISTENT_SIZE_DESCRIPTION);

```

### UnnecessaryBoxing
Unnecessary boxing
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/impl/ManagementServiceImpl.java`
#### Snippet
```java
               builder.build(QueueMetricNames.DURABLE_MESSAGE_COUNT, queue, metrics -> Double.valueOf(queue.getDurableMessageCount()), QueueControl.DURABLE_MESSAGE_COUNT_DESCRIPTION);
               builder.build(QueueMetricNames.PERSISTENT_SIZE, queue, metrics -> Double.valueOf(queue.getPersistentSize()), QueueControl.PERSISTENT_SIZE_DESCRIPTION);
               builder.build(QueueMetricNames.DURABLE_PERSISTENT_SIZE, queue, metrics -> Double.valueOf(queue.getDurablePersistentSize()), QueueControl.DURABLE_PERSISTENT_SIZE_DESCRIPTION);

               builder.build(QueueMetricNames.DELIVERING_MESSAGE_COUNT, queue, metrics -> Double.valueOf(queue.getDeliveringCount()), QueueControl.DELIVERING_MESSAGE_COUNT_DESCRIPTION);
```

### UnnecessaryBoxing
Unnecessary boxing
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/impl/ManagementServiceImpl.java`
#### Snippet
```java
               builder.build(QueueMetricNames.DURABLE_PERSISTENT_SIZE, queue, metrics -> Double.valueOf(queue.getDurablePersistentSize()), QueueControl.DURABLE_PERSISTENT_SIZE_DESCRIPTION);

               builder.build(QueueMetricNames.DELIVERING_MESSAGE_COUNT, queue, metrics -> Double.valueOf(queue.getDeliveringCount()), QueueControl.DELIVERING_MESSAGE_COUNT_DESCRIPTION);
               builder.build(QueueMetricNames.DELIVERING_DURABLE_MESSAGE_COUNT, queue, metrics -> Double.valueOf(queue.getDurableDeliveringCount()), QueueControl.DURABLE_DELIVERING_MESSAGE_COUNT_DESCRIPTION);
               builder.build(QueueMetricNames.DELIVERING_PERSISTENT_SIZE, queue, metrics -> Double.valueOf(queue.getDeliveringSize()), QueueControl.DELIVERING_SIZE_DESCRIPTION);
```

### UnnecessaryBoxing
Unnecessary boxing
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/impl/ManagementServiceImpl.java`
#### Snippet
```java

               builder.build(QueueMetricNames.DELIVERING_MESSAGE_COUNT, queue, metrics -> Double.valueOf(queue.getDeliveringCount()), QueueControl.DELIVERING_MESSAGE_COUNT_DESCRIPTION);
               builder.build(QueueMetricNames.DELIVERING_DURABLE_MESSAGE_COUNT, queue, metrics -> Double.valueOf(queue.getDurableDeliveringCount()), QueueControl.DURABLE_DELIVERING_MESSAGE_COUNT_DESCRIPTION);
               builder.build(QueueMetricNames.DELIVERING_PERSISTENT_SIZE, queue, metrics -> Double.valueOf(queue.getDeliveringSize()), QueueControl.DELIVERING_SIZE_DESCRIPTION);
               builder.build(QueueMetricNames.DELIVERING_DURABLE_PERSISTENT_SIZE, queue, metrics -> Double.valueOf(queue.getDurableDeliveringSize()), QueueControl.DURABLE_DELIVERING_SIZE_DESCRIPTION);
```

### UnnecessaryBoxing
Unnecessary boxing
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/impl/ManagementServiceImpl.java`
#### Snippet
```java
               builder.build(QueueMetricNames.DELIVERING_MESSAGE_COUNT, queue, metrics -> Double.valueOf(queue.getDeliveringCount()), QueueControl.DELIVERING_MESSAGE_COUNT_DESCRIPTION);
               builder.build(QueueMetricNames.DELIVERING_DURABLE_MESSAGE_COUNT, queue, metrics -> Double.valueOf(queue.getDurableDeliveringCount()), QueueControl.DURABLE_DELIVERING_MESSAGE_COUNT_DESCRIPTION);
               builder.build(QueueMetricNames.DELIVERING_PERSISTENT_SIZE, queue, metrics -> Double.valueOf(queue.getDeliveringSize()), QueueControl.DELIVERING_SIZE_DESCRIPTION);
               builder.build(QueueMetricNames.DELIVERING_DURABLE_PERSISTENT_SIZE, queue, metrics -> Double.valueOf(queue.getDurableDeliveringSize()), QueueControl.DURABLE_DELIVERING_SIZE_DESCRIPTION);

```

### UnnecessaryBoxing
Unnecessary boxing
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/impl/ManagementServiceImpl.java`
#### Snippet
```java
               builder.build(QueueMetricNames.DELIVERING_DURABLE_MESSAGE_COUNT, queue, metrics -> Double.valueOf(queue.getDurableDeliveringCount()), QueueControl.DURABLE_DELIVERING_MESSAGE_COUNT_DESCRIPTION);
               builder.build(QueueMetricNames.DELIVERING_PERSISTENT_SIZE, queue, metrics -> Double.valueOf(queue.getDeliveringSize()), QueueControl.DELIVERING_SIZE_DESCRIPTION);
               builder.build(QueueMetricNames.DELIVERING_DURABLE_PERSISTENT_SIZE, queue, metrics -> Double.valueOf(queue.getDurableDeliveringSize()), QueueControl.DURABLE_DELIVERING_SIZE_DESCRIPTION);

               builder.build(QueueMetricNames.SCHEDULED_MESSAGE_COUNT, queue, metrics -> Double.valueOf(queue.getScheduledCount()), QueueControl.SCHEDULED_MESSAGE_COUNT_DESCRIPTION);
```

### UnnecessaryBoxing
Unnecessary boxing
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/impl/ManagementServiceImpl.java`
#### Snippet
```java
               builder.build(QueueMetricNames.DELIVERING_DURABLE_PERSISTENT_SIZE, queue, metrics -> Double.valueOf(queue.getDurableDeliveringSize()), QueueControl.DURABLE_DELIVERING_SIZE_DESCRIPTION);

               builder.build(QueueMetricNames.SCHEDULED_MESSAGE_COUNT, queue, metrics -> Double.valueOf(queue.getScheduledCount()), QueueControl.SCHEDULED_MESSAGE_COUNT_DESCRIPTION);
               builder.build(QueueMetricNames.SCHEDULED_DURABLE_MESSAGE_COUNT, queue, metrics -> Double.valueOf(queue.getDurableScheduledCount()), QueueControl.DURABLE_SCHEDULED_MESSAGE_COUNT_DESCRIPTION);
               builder.build(QueueMetricNames.SCHEDULED_PERSISTENT_SIZE, queue, metrics -> Double.valueOf(queue.getScheduledSize()), QueueControl.SCHEDULED_SIZE_DESCRIPTION);
```

### UnnecessaryBoxing
Unnecessary boxing
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/impl/ManagementServiceImpl.java`
#### Snippet
```java

               builder.build(QueueMetricNames.SCHEDULED_MESSAGE_COUNT, queue, metrics -> Double.valueOf(queue.getScheduledCount()), QueueControl.SCHEDULED_MESSAGE_COUNT_DESCRIPTION);
               builder.build(QueueMetricNames.SCHEDULED_DURABLE_MESSAGE_COUNT, queue, metrics -> Double.valueOf(queue.getDurableScheduledCount()), QueueControl.DURABLE_SCHEDULED_MESSAGE_COUNT_DESCRIPTION);
               builder.build(QueueMetricNames.SCHEDULED_PERSISTENT_SIZE, queue, metrics -> Double.valueOf(queue.getScheduledSize()), QueueControl.SCHEDULED_SIZE_DESCRIPTION);
               builder.build(QueueMetricNames.SCHEDULED_DURABLE_PERSISTENT_SIZE, queue, metrics -> Double.valueOf(queue.getDurableScheduledSize()), QueueControl.DURABLE_SCHEDULED_SIZE_DESCRIPTION);
```

### UnnecessaryBoxing
Unnecessary boxing
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/impl/ManagementServiceImpl.java`
#### Snippet
```java
               builder.build(QueueMetricNames.SCHEDULED_MESSAGE_COUNT, queue, metrics -> Double.valueOf(queue.getScheduledCount()), QueueControl.SCHEDULED_MESSAGE_COUNT_DESCRIPTION);
               builder.build(QueueMetricNames.SCHEDULED_DURABLE_MESSAGE_COUNT, queue, metrics -> Double.valueOf(queue.getDurableScheduledCount()), QueueControl.DURABLE_SCHEDULED_MESSAGE_COUNT_DESCRIPTION);
               builder.build(QueueMetricNames.SCHEDULED_PERSISTENT_SIZE, queue, metrics -> Double.valueOf(queue.getScheduledSize()), QueueControl.SCHEDULED_SIZE_DESCRIPTION);
               builder.build(QueueMetricNames.SCHEDULED_DURABLE_PERSISTENT_SIZE, queue, metrics -> Double.valueOf(queue.getDurableScheduledSize()), QueueControl.DURABLE_SCHEDULED_SIZE_DESCRIPTION);

```

### UnnecessaryBoxing
Unnecessary boxing
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/impl/ManagementServiceImpl.java`
#### Snippet
```java
               builder.build(QueueMetricNames.SCHEDULED_DURABLE_MESSAGE_COUNT, queue, metrics -> Double.valueOf(queue.getDurableScheduledCount()), QueueControl.DURABLE_SCHEDULED_MESSAGE_COUNT_DESCRIPTION);
               builder.build(QueueMetricNames.SCHEDULED_PERSISTENT_SIZE, queue, metrics -> Double.valueOf(queue.getScheduledSize()), QueueControl.SCHEDULED_SIZE_DESCRIPTION);
               builder.build(QueueMetricNames.SCHEDULED_DURABLE_PERSISTENT_SIZE, queue, metrics -> Double.valueOf(queue.getDurableScheduledSize()), QueueControl.DURABLE_SCHEDULED_SIZE_DESCRIPTION);

               builder.build(QueueMetricNames.MESSAGES_ACKNOWLEDGED, queue, metrics -> Double.valueOf(queue.getMessagesAcknowledged()), QueueControl.MESSAGES_ACKNOWLEDGED_DESCRIPTION);
```

### UnnecessaryBoxing
Unnecessary boxing
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/impl/ManagementServiceImpl.java`
#### Snippet
```java
               builder.build(QueueMetricNames.SCHEDULED_DURABLE_PERSISTENT_SIZE, queue, metrics -> Double.valueOf(queue.getDurableScheduledSize()), QueueControl.DURABLE_SCHEDULED_SIZE_DESCRIPTION);

               builder.build(QueueMetricNames.MESSAGES_ACKNOWLEDGED, queue, metrics -> Double.valueOf(queue.getMessagesAcknowledged()), QueueControl.MESSAGES_ACKNOWLEDGED_DESCRIPTION);
               builder.build(QueueMetricNames.MESSAGES_ADDED, queue, metrics -> Double.valueOf(queue.getMessagesAdded()), QueueControl.MESSAGES_ADDED_DESCRIPTION);
               builder.build(QueueMetricNames.MESSAGES_KILLED, queue, metrics -> Double.valueOf(queue.getMessagesKilled()), QueueControl.MESSAGES_KILLED_DESCRIPTION);
```

### UnnecessaryBoxing
Unnecessary boxing
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/impl/ManagementServiceImpl.java`
#### Snippet
```java

               builder.build(QueueMetricNames.MESSAGES_ACKNOWLEDGED, queue, metrics -> Double.valueOf(queue.getMessagesAcknowledged()), QueueControl.MESSAGES_ACKNOWLEDGED_DESCRIPTION);
               builder.build(QueueMetricNames.MESSAGES_ADDED, queue, metrics -> Double.valueOf(queue.getMessagesAdded()), QueueControl.MESSAGES_ADDED_DESCRIPTION);
               builder.build(QueueMetricNames.MESSAGES_KILLED, queue, metrics -> Double.valueOf(queue.getMessagesKilled()), QueueControl.MESSAGES_KILLED_DESCRIPTION);
               builder.build(QueueMetricNames.MESSAGES_EXPIRED, queue, metrics -> Double.valueOf(queue.getMessagesExpired()), QueueControl.MESSAGES_EXPIRED_DESCRIPTION);
```

### UnnecessaryBoxing
Unnecessary boxing
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/impl/ManagementServiceImpl.java`
#### Snippet
```java
               builder.build(QueueMetricNames.MESSAGES_ACKNOWLEDGED, queue, metrics -> Double.valueOf(queue.getMessagesAcknowledged()), QueueControl.MESSAGES_ACKNOWLEDGED_DESCRIPTION);
               builder.build(QueueMetricNames.MESSAGES_ADDED, queue, metrics -> Double.valueOf(queue.getMessagesAdded()), QueueControl.MESSAGES_ADDED_DESCRIPTION);
               builder.build(QueueMetricNames.MESSAGES_KILLED, queue, metrics -> Double.valueOf(queue.getMessagesKilled()), QueueControl.MESSAGES_KILLED_DESCRIPTION);
               builder.build(QueueMetricNames.MESSAGES_EXPIRED, queue, metrics -> Double.valueOf(queue.getMessagesExpired()), QueueControl.MESSAGES_EXPIRED_DESCRIPTION);
               builder.build(QueueMetricNames.CONSUMER_COUNT, queue, metrics -> Double.valueOf(queue.getConsumerCount()), QueueControl.CONSUMER_COUNT_DESCRIPTION);
```

### UnnecessaryBoxing
Unnecessary boxing
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/impl/ManagementServiceImpl.java`
#### Snippet
```java
               builder.build(QueueMetricNames.MESSAGES_ADDED, queue, metrics -> Double.valueOf(queue.getMessagesAdded()), QueueControl.MESSAGES_ADDED_DESCRIPTION);
               builder.build(QueueMetricNames.MESSAGES_KILLED, queue, metrics -> Double.valueOf(queue.getMessagesKilled()), QueueControl.MESSAGES_KILLED_DESCRIPTION);
               builder.build(QueueMetricNames.MESSAGES_EXPIRED, queue, metrics -> Double.valueOf(queue.getMessagesExpired()), QueueControl.MESSAGES_EXPIRED_DESCRIPTION);
               builder.build(QueueMetricNames.CONSUMER_COUNT, queue, metrics -> Double.valueOf(queue.getConsumerCount()), QueueControl.CONSUMER_COUNT_DESCRIPTION);
            });
```

### UnnecessaryBoxing
Unnecessary boxing
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/impl/ManagementServiceImpl.java`
#### Snippet
```java
               builder.build(QueueMetricNames.MESSAGES_KILLED, queue, metrics -> Double.valueOf(queue.getMessagesKilled()), QueueControl.MESSAGES_KILLED_DESCRIPTION);
               builder.build(QueueMetricNames.MESSAGES_EXPIRED, queue, metrics -> Double.valueOf(queue.getMessagesExpired()), QueueControl.MESSAGES_EXPIRED_DESCRIPTION);
               builder.build(QueueMetricNames.CONSUMER_COUNT, queue, metrics -> Double.valueOf(queue.getConsumerCount()), QueueControl.CONSUMER_COUNT_DESCRIPTION);
            });
         }
```

### UnnecessaryBoxing
Redundant boxing, `Boolean.parseBoolean()` call can be used instead
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/postoffice/impl/IntegerCache.java`
#### Snippet
```java
final class IntegerCache {

   private static final boolean DISABLE_INTEGER_CACHE = Boolean.valueOf(System.getProperty("disable.integer.cache", Boolean.FALSE.toString()));

   // we're not interested into safe publication here: we need to scale, be fast and save "some" GC to happen
```

### UnnecessaryBoxing
Unnecessary boxing
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/QueueControlImpl.java`
#### Snippet
```java
            SimpleString groupByProperty = SimpleString.toSimpleString(groupByPropertyStr);
            if (filter == null && groupByProperty == null) {
               result.put(null, Long.valueOf(getDeliveringCount()));
            } else {
               Map<String, List<MessageReference>> deliveringMessages = queue.getDeliveringMessages();
```

### UnnecessaryBoxing
Redundant boxing, `Long.parseLong()` call can be used instead
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/ActiveMQServerControlImpl.java`
#### Snippet
```java
                  Set<ServerConsumer> serverConsumers = session.getServerConsumers();
                  for (ServerConsumer serverConsumer : serverConsumers) {
                     if (serverConsumer.sequentialID() == Long.valueOf(ID)) {
                        serverConsumer.disconnect();
                        return true;
```

## RuleId[id=ConditionCoveredByFurtherCondition]
### ConditionCoveredByFurtherCondition
Condition 'object != null' covered by subsequent condition 'object == Boolean.TRUE'
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/ConstantExpression.java`
#### Snippet
```java
      public boolean matches(Filterable message) throws FilterException {
         Object object = evaluate(message);
         return object != null && object == Boolean.TRUE;
      }
   }
```

### ConditionCoveredByFurtherCondition
Condition 'object != null' covered by subsequent condition 'object instanceof ActiveMQRAMessage'
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/ActiveMQRAMessage.java`
#### Snippet
```java
      logger.trace("equals({})", object);

      if (object != null && object instanceof ActiveMQRAMessage) {
         return message.equals(((ActiveMQRAMessage) object).message);
      } else {
```

### ConditionCoveredByFurtherCondition
Condition 'current != null' covered by subsequent condition 'current instanceof JMSException'
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/ActiveMQRASessionFactoryImpl.java`
#### Snippet
```java
         }

         if (current != null && current instanceof JMSException) {
            throw (JMSException) current;
         } else {
```

### ConditionCoveredByFurtherCondition
Condition 'ra == null' covered by subsequent condition '!(ra instanceof ActiveMQResourceAdapter)'
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/ActiveMQRAManagedConnectionFactory.java`
#### Snippet
```java
      logger.trace("setResourceAdapter({})", ra);

      if (ra == null || !(ra instanceof ActiveMQResourceAdapter)) {
         throw new ResourceException("Resource adapter is " + ra);
      }
```

### ConditionCoveredByFurtherCondition
Condition 'ra == null' covered by subsequent condition '!(ra instanceof ActiveMQResourceAdapter)'
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/inflow/ActiveMQActivationSpec.java`
#### Snippet
```java
      logger.trace("setResourceAdapter({})", ra);

      if (ra == null || !(ra instanceof ActiveMQResourceAdapter)) {
         throw new ResourceException("Resource adapter is " + ra);
      }
```

### ConditionCoveredByFurtherCondition
Condition 'end \< 0' covered by subsequent conditions
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/KMPNeedle.java`
#### Snippet
```java
    */
   public int searchInto(ReadableBuffer haystack, int start, int end) {
      if (end < 0 || start < 0 || end < start) {
         return -1;
      }
```

### ConditionCoveredByFurtherCondition
Condition 'objscheduledTime != null' covered by subsequent condition 'objscheduledTime instanceof Number'
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
         Object objdelay = getMessageAnnotation(AMQPMessageSupport.SCHEDULED_DELIVERY_DELAY);

         if (objscheduledTime != null && objscheduledTime instanceof Number) {
            this.scheduledTime = ((Number) objscheduledTime).longValue();
         } else if (objdelay != null && objdelay instanceof Number) {
```

### ConditionCoveredByFurtherCondition
Condition 'objdelay != null' covered by subsequent condition 'objdelay instanceof Number'
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
         if (objscheduledTime != null && objscheduledTime instanceof Number) {
            this.scheduledTime = ((Number) objscheduledTime).longValue();
         } else if (objdelay != null && objdelay instanceof Number) {
            this.scheduledTime = System.currentTimeMillis() + ((Number) objdelay).longValue();
         } else {
```

### ConditionCoveredByFurtherCondition
Condition 'scheduledFuture != null' covered by subsequent condition 'scheduledFuture instanceof Runnable'
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/AMQPConnectionContext.java`
#### Snippet
```java
      Future<?> scheduledFuture = scheduledFutureRef.getAndSet(null);

      if (scheduledPool instanceof ThreadPoolExecutor && scheduledFuture != null &&
         scheduledFuture != VOID_FUTURE && scheduledFuture instanceof Runnable) {
         if (!((ThreadPoolExecutor) scheduledPool).remove((Runnable) scheduledFuture) &&
```

### ConditionCoveredByFurtherCondition
Condition 'value == null' covered by subsequent condition '!(value instanceof Number)'
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/converter/AmqpCoreConverter.java`
#### Snippet
```java

      byte queueType;
      if (value == null || !(value instanceof Number)) {
         queueType = AMQPMessageSupport.QUEUE_TYPE;
      } else {
```

### ConditionCoveredByFurtherCondition
Condition 'currentMirrorController != null' covered by subsequent condition 'currentMirrorController instanceof AMQPMirrorControllerSource'
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/connect/AMQPBrokerConnection.java`
#### Snippet
```java

      // This following block is to avoid a duplicate on mirror controller
      if (currentMirrorController != null && currentMirrorController instanceof AMQPMirrorControllerSource) {
         Queue queue = checkCurrentMirror(this, (AMQPMirrorControllerSource) currentMirrorController);
         // on this case we already had a mirror installed before, we won't duplicate it
```

### ConditionCoveredByFurtherCondition
Condition 'currentMirrorController != null' covered by subsequent condition 'currentMirrorController instanceof AMQPMirrorControllerAggregation'
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/connect/AMQPBrokerConnection.java`
#### Snippet
```java
            return queue;
         }
      } else if (currentMirrorController != null && currentMirrorController instanceof AMQPMirrorControllerAggregation) {
         AMQPMirrorControllerAggregation aggregation = (AMQPMirrorControllerAggregation) currentMirrorController;

```

### ConditionCoveredByFurtherCondition
Condition 'property != null' covered by subsequent condition 'property instanceof Number'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/message/impl/CoreMessage.java`
#### Snippet
```java
      Object property = getProperties().getProperty(Message.HDR_SCHEDULED_DELIVERY_TIME);

      if (property != null && property instanceof Number) {
         return ((Number) property).longValue();
      }
```

### ConditionCoveredByFurtherCondition
Condition 'channelHandler != null' covered by subsequent condition 'channelHandler instanceof SslHandler'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/CertificateUtil.java`
#### Snippet
```java
      Certificate[] plainCerts = null;
      ChannelHandler channelHandler = channel.pipeline().get("ssl");
      if (channelHandler != null && channelHandler instanceof SslHandler) {
         SslHandler sslHandler = (SslHandler) channelHandler;
         try {
```

### ConditionCoveredByFurtherCondition
Condition 'channelHandler != null' covered by subsequent condition 'channelHandler instanceof SslHandler'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/CertificateUtil.java`
#### Snippet
```java
            NettyConnection nettyConnection = (NettyConnection) transportConnection;
            ChannelHandler channelHandler = nettyConnection.getChannel().pipeline().get(SSL_HANDLER_NAME);
            if (channelHandler != null && channelHandler instanceof SslHandler) {
               SslHandler sslHandler = (SslHandler) channelHandler;
               try {
```

### ConditionCoveredByFurtherCondition
Condition 'object != null' covered by subsequent condition 'object instanceof TransportConfigurationHelper'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/impl/TransportConfigurationUtil.java`
#### Snippet
```java
      if (!DEFAULTS.containsKey(className)) {
         Object object = instantiateObject(className);
         if (object != null && object instanceof TransportConfigurationHelper) {

            DEFAULTS.put(className, ((TransportConfigurationHelper) object).getDefaults());
```

### ConditionCoveredByFurtherCondition
Condition 'oldConnection != null' covered by subsequent condition 'oldConnection instanceof CoreRemotingConnection'
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ClientSessionFactoryImpl.java`
#### Snippet
```java

         if (getConnection() != null) {
            if (oldConnection != null && oldConnection instanceof CoreRemotingConnection) {
               // transferring old connection version into the new connection
               ((CoreRemotingConnection)connection).setChannelVersion(((CoreRemotingConnection)oldConnection).getChannelVersion());
```

### ConditionCoveredByFurtherCondition
Condition 'result != null' covered by subsequent condition 'result instanceof Number'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ScaleDownHandler.java`
#### Snippet
```java
         result = ManagementHelper.getResult(reply);
      }
      if (result != null && result instanceof Number) {
         queueID = ((Number) result).longValue();
      }
```

### ConditionCoveredByFurtherCondition
Condition 'retroQueue != null' covered by subsequent condition 'retroQueue instanceof QueueImpl'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ActiveMQServerImpl.java`
#### Snippet
```java
      if (addressSettingsRepository.getMatch(queue.getAddress().toString()).getRetroactiveMessageCount() > 0) {
         Queue retroQueue = locateQueue(ResourceNames.getRetroactiveResourceQueueName(getInternalNamingPrefix(), getConfiguration().getWildcardConfiguration().getDelimiterString(), queue.getAddress(), queue.getRoutingType()));
         if (retroQueue != null && retroQueue instanceof QueueImpl) {
            ((QueueImpl) retroQueue).rerouteMessages(queue.getName(), queue.getFilter());
         }
```

### ConditionCoveredByFurtherCondition
Condition 'transformer != null' covered by subsequent condition 'transformer instanceof RegisteredTransformer'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/DivertControlImpl.java`
#### Snippet
```java
      try {
         Transformer transformer = divert.getTransformer();
         return transformer != null && transformer instanceof RegisteredTransformer ?
            ((RegisteredTransformer)transformer).getProperties() : Collections.emptyMap();
      } finally {
```

### ConditionCoveredByFurtherCondition
Condition 'binding == null' covered by subsequent condition '!(binding instanceof LocalQueueBinding)'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/postoffice/impl/SimpleAddressManager.java`
#### Snippet
```java
      Binding binding = getBinding(realAddress);

      if (binding == null || !(binding instanceof LocalQueueBinding) || !binding.getAddress().equals(realAddress)) {
         Bindings bindings = mappings.get(realAddress);
         if (bindings != null) {
```

### ConditionCoveredByFurtherCondition
Condition 'originalRoutingType != null' covered by subsequent condition 'originalRoutingType instanceof Byte'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java

      Object originalRoutingType = ref.getMessage().getBrokerProperty(Message.HDR_ORIG_ROUTING_TYPE);
      if (originalRoutingType != null && originalRoutingType instanceof Byte) {
         copyMessage.setRoutingType(RoutingType.getType((Byte) originalRoutingType));
      }
```

## RuleId[id=UnnecessaryFullyQualifiedName]
### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.core.remoting.impl.invm` is unnecessary, and can be replaced with an import
in `artemis-cdi-client/src/main/java/org/apache/artemis/client/cdi/extension/ArtemisEmbeddedServerConfigBean.java`
#### Snippet
```java
   public Configuration create(CreationalContext<Configuration> creationalContext) {
      Map<String, Object> params = new HashMap<>();
      params.put(org.apache.activemq.artemis.core.remoting.impl.invm.TransportConstants.SERVER_ID_PROP_NAME, "1");
      return new ConfigurationImpl().setSecurityEnabled(false).setPersistenceEnabled(false).setJMXManagementEnabled(false).addAcceptorConfiguration(new TransportConfiguration(InVMAcceptorFactory.class.getName(), params));
   }
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.maven.artifact.repository` is unnecessary, and can be replaced with an import
in `artemis-maven-plugin/src/main/java/org/apache/activemq/artemis/maven/ArtemisCreatePlugin.java`
#### Snippet
```java

   @Parameter(defaultValue = "${localRepository}")
   private org.apache.maven.artifact.repository.ArtifactRepository localRepository;

   @Parameter(defaultValue = "${noServer}")
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.aether.artifact` is unnecessary and can be removed
in `artemis-maven-plugin/src/main/java/org/apache/activemq/artemis/maven/ArtemisDependencyDocPlugin.java`
#### Snippet
```java
         }

         List<org.eclipse.aether.artifact.Artifact> artifacts = explodeDependencies(newArtifact(lib));

         Collections.sort(artifacts, new Comparator<Artifact>() {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.aether.artifact` is unnecessary and can be removed
in `artemis-maven-plugin/src/main/java/org/apache/activemq/artemis/maven/ArtemisDependencyDocPlugin.java`
#### Snippet
```java
   }

   private String getPackageName(org.eclipse.aether.artifact.Artifact artifact) {
      return artifact.getGroupId() + ":" + artifact.getArtifactId() + (artifact.getClassifier() != null && !artifact.getClassifier().equals("") ? ":" + artifact.getClassifier() : "");
   }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/PropertyExpression.java`
#### Snippet
```java

   /**
    * @see java.lang.Object#toString()
    */
   @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/PropertyExpression.java`
#### Snippet
```java

   /**
    * @see java.lang.Object#equals(java.lang.Object)
    */
   @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/PropertyExpression.java`
#### Snippet
```java

   /**
    * @see java.lang.Object#equals(java.lang.Object)
    */
   @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/PropertyExpression.java`
#### Snippet
```java

   /**
    * @see java.lang.Object#hashCode()
    */
   @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/BinaryExpression.java`
#### Snippet
```java

   /**
    * @see java.lang.Object#hashCode()
    */
   @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/BinaryExpression.java`
#### Snippet
```java

   /**
    * @see java.lang.Object#toString()
    */
   @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/BinaryExpression.java`
#### Snippet
```java

   /**
    * @see java.lang.Object#equals(java.lang.Object)
    */
   @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/BinaryExpression.java`
#### Snippet
```java

   /**
    * @see java.lang.Object#equals(java.lang.Object)
    */
   @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/ConstantExpression.java`
#### Snippet
```java

   /**
    * @see java.lang.Object#hashCode()
    */
   @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/ConstantExpression.java`
#### Snippet
```java

   /**
    * @see java.lang.Object#equals(Object)
    */
   @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/ConstantExpression.java`
#### Snippet
```java

   /**
    * @see java.lang.Object#toString()
    */
   @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/UnaryExpression.java`
#### Snippet
```java

   /**
    * @see java.lang.Object#hashCode()
    */
   @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/UnaryExpression.java`
#### Snippet
```java

   /**
    * @see java.lang.Object#toString()
    */
   @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/UnaryExpression.java`
#### Snippet
```java

   /**
    * @see java.lang.Object#equals(java.lang.Object)
    */
   @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `artemis-selector/src/main/java/org/apache/activemq/artemis/selector/filter/UnaryExpression.java`
#### Snippet
```java

   /**
    * @see java.lang.Object#equals(java.lang.Object)
    */
   @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.json` is unnecessary, and can be replaced with an import
in `artemis-commons/src/main/java/org/apache/activemq/artemis/json/impl/JsonValueImpl.java`
#### Snippet
```java
         cacheValue = new JsonObjectImpl((javax.json.JsonObject) rawValue);
      } else if (rawValue.getValueType() == javax.json.JsonValue.ValueType.STRING) {
         cacheValue = new JsonStringImpl((javax.json.JsonString) rawValue);
      } else if (rawValue.getValueType() == javax.json.JsonValue.ValueType.NUMBER) {
         cacheValue = new JsonNumberImpl((javax.json.JsonNumber) rawValue);
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.json` is unnecessary, and can be replaced with an import
in `artemis-commons/src/main/java/org/apache/activemq/artemis/json/impl/JsonValueImpl.java`
#### Snippet
```java
         cacheValue = new JsonStringImpl((javax.json.JsonString) rawValue);
      } else if (rawValue.getValueType() == javax.json.JsonValue.ValueType.NUMBER) {
         cacheValue = new JsonNumberImpl((javax.json.JsonNumber) rawValue);
      } else if (rawValue.getValueType() == javax.json.JsonValue.ValueType.TRUE) {
         cacheValue = new JsonValueImpl(rawValue);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.johnzon.core` is unnecessary, and can be replaced with an import
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/JsonLoader.java`
#### Snippet
```java
public class JsonLoader {

   private static final JsonProvider provider = new org.apache.johnzon.core.JsonProviderImpl();

   public static JsonObject readObject(Reader reader) {
```

### UnnecessaryFullyQualifiedName
Qualifier `sun.misc` is unnecessary, and can be replaced with an import
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Env.java`
#### Snippet
```java
      //most common OS page size value
      int osPageSize = 4096;
      sun.misc.Unsafe instance;
      try {
         Field field = sun.misc.Unsafe.class.getDeclaredField("theUnsafe");
```

### UnnecessaryFullyQualifiedName
Qualifier `sun.misc` is unnecessary, and can be replaced with an import
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Env.java`
#### Snippet
```java
      sun.misc.Unsafe instance;
      try {
         Field field = sun.misc.Unsafe.class.getDeclaredField("theUnsafe");
         field.setAccessible(true);
         instance = (sun.misc.Unsafe) field.get((Object) null);
```

### UnnecessaryFullyQualifiedName
Qualifier `sun.misc` is unnecessary, and can be replaced with an import
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Env.java`
#### Snippet
```java
         Field field = sun.misc.Unsafe.class.getDeclaredField("theUnsafe");
         field.setAccessible(true);
         instance = (sun.misc.Unsafe) field.get((Object) null);
      } catch (Throwable t) {
         try {
```

### UnnecessaryFullyQualifiedName
Qualifier `sun.misc` is unnecessary, and can be replaced with an import
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Env.java`
#### Snippet
```java
      } catch (Throwable t) {
         try {
            Constructor<sun.misc.Unsafe> c = sun.misc.Unsafe.class.getDeclaredConstructor(new Class[0]);
            c.setAccessible(true);
            instance = c.newInstance(new Object[0]);
```

### UnnecessaryFullyQualifiedName
Qualifier `sun.misc` is unnecessary, and can be replaced with an import
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Env.java`
#### Snippet
```java
      } catch (Throwable t) {
         try {
            Constructor<sun.misc.Unsafe> c = sun.misc.Unsafe.class.getDeclaredConstructor(new Class[0]);
            c.setAccessible(true);
            instance = c.newInstance(new Object[0]);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.net` is unnecessary and can be removed
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/InetAddresses.java`
#### Snippet
```java
    *
    * <p>This function is the inverse of
    * {@link InetAddresses#toUriString(java.net.InetAddress)}.
    *
    * @param hostAddr A RFC 3986 section 3.2.2 encoded IPv4 or IPv6 address
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.utils.actors` is unnecessary and can be removed
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/actors/ArtemisExecutor.java`
#### Snippet
```java

   /**
    * This will verify if the executor is flushed with no wait (or very minimal wait if not the {@link org.apache.activemq.artemis.utils.actors.OrderedExecutor}
    * @return
    */
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
      try {
         // Set up some useful variables
         java.io.File file = new java.io.File(filename);
         byte[] buffer = null;
         int length = 0;
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
      try {
         // Set up some useful variables
         java.io.File file = new java.io.File(filename);
         byte[] buffer = null;
         int length = 0;
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java

         // Open a stream
         bis = new Base64.InputStream(new java.io.BufferedInputStream(new java.io.FileInputStream(file)), Base64.DECODE);

         // Read until done
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java

         // Open a stream
         bis = new Base64.InputStream(new java.io.BufferedInputStream(new java.io.FileInputStream(file)), Base64.DECODE);

         // Read until done
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
         System.arraycopy(buffer, 0, decodedData, 0, length);

      } catch (java.io.IOException e) {
         System.err.println("Error decoding from file " + filename);
      } finally {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
       */
      @Override
      public void write(final int theByte) throws java.io.IOException {
         // Encoding suspended?
         if (suspendEncoding) {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
               } // end if: enough to output
            } else if (decodabet[theByte & 0x7f] != Base64.WHITE_SPACE_ENC) {
               throw new java.io.IOException("Invalid character in Base64 data.");
            }
         } // end else: decoding
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
    * @since 1.4
    */
   public static String encodeObject(final java.io.Serializable serializableObject) {
      return Base64.encodeObject(serializableObject, Base64.NO_OPTIONS);
   } // end encodeObject
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
       * @throws java.io.IOException On IO Exception
       */
      public void flushBase64() throws java.io.IOException {
         if (position > 0) {
            if (encode) {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
               position = 0;
            } else {
               throw new java.io.IOException("Base64 input not properly padded.");
            }
         } // end if: buffer partially full
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
      Base64.OutputStream bos = null;
      try {
         bos = new Base64.OutputStream(new java.io.FileOutputStream(filename), Base64.DECODE);
         bos.write(dataToDecode.getBytes(Base64.PREFERRED_ENCODING));
         success = true;
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
         bos.write(dataToDecode.getBytes(Base64.PREFERRED_ENCODING));
         success = true;
      } catch (java.io.IOException e) {
         success = false;
      } finally {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
       */
      @Override
      public int read(final byte[] dest, final int off, final int len) throws java.io.IOException {
         int i;
         int b;
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
      java.io.OutputStream out = null;
      try {
         in = new Base64.InputStream(new java.io.BufferedInputStream(new java.io.FileInputStream(infile)), Base64.DECODE);
         out = new java.io.BufferedOutputStream(new java.io.FileOutputStream(outfile));
         byte[] buffer = new byte[65536]; // 64K
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
      java.io.OutputStream out = null;
      try {
         in = new Base64.InputStream(new java.io.BufferedInputStream(new java.io.FileInputStream(infile)), Base64.DECODE);
         out = new java.io.BufferedOutputStream(new java.io.FileOutputStream(outfile));
         byte[] buffer = new byte[65536]; // 64K
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
      try {
         in = new Base64.InputStream(new java.io.BufferedInputStream(new java.io.FileInputStream(infile)), Base64.DECODE);
         out = new java.io.BufferedOutputStream(new java.io.FileOutputStream(outfile));
         byte[] buffer = new byte[65536]; // 64K
         int read = -1;
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
      try {
         in = new Base64.InputStream(new java.io.BufferedInputStream(new java.io.FileInputStream(infile)), Base64.DECODE);
         out = new java.io.BufferedOutputStream(new java.io.FileOutputStream(outfile));
         byte[] buffer = new byte[65536]; // 64K
         int read = -1;
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
         } // end while: through file
         success = true;
      } catch (java.io.IOException exc) {
         exc.printStackTrace();
      } finally {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
      Base64.OutputStream bos = null;
      try {
         bos = new Base64.OutputStream(new java.io.FileOutputStream(filename), Base64.ENCODE);
         bos.write(dataToEncode);
         success = true;
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
         bos.write(dataToEncode);
         success = true;
      } catch (java.io.IOException e) {

         success = false;
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
       */
      @Override
      public void write(final byte[] theBytes, final int off, final int len) throws java.io.IOException {
         // Encoding suspended?
         if (suspendEncoding) {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
      byte[] objBytes = Base64.decode(encodedObject);

      java.io.ByteArrayInputStream bais = null;
      java.io.ObjectInputStream ois = null;
      Object obj = null;
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java

      java.io.ByteArrayInputStream bais = null;
      java.io.ObjectInputStream ois = null;
      Object obj = null;

```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java

      try {
         bais = new java.io.ByteArrayInputStream(objBytes);
         ois = new java.io.ObjectInputStream(bais);

```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
      try {
         bais = new java.io.ByteArrayInputStream(objBytes);
         ois = new java.io.ObjectInputStream(bais);

         obj = ois.readObject();
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java

         obj = ois.readObject();
      } catch (java.io.IOException | java.lang.ClassNotFoundException e) {
         ActiveMQUtilLogger.LOGGER.failedToDeserializeObject(e);
         obj = null;
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java

         obj = ois.readObject();
      } catch (java.io.IOException | java.lang.ClassNotFoundException e) {
         ActiveMQUtilLogger.LOGGER.failedToDeserializeObject(e);
         obj = null;
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
      java.io.OutputStream out = null;
      try {
         in = new Base64.InputStream(new java.io.BufferedInputStream(new java.io.FileInputStream(infile)), Base64.ENCODE);
         out = new java.io.BufferedOutputStream(new java.io.FileOutputStream(outfile));
         byte[] buffer = new byte[65536]; // 64K
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
      java.io.OutputStream out = null;
      try {
         in = new Base64.InputStream(new java.io.BufferedInputStream(new java.io.FileInputStream(infile)), Base64.ENCODE);
         out = new java.io.BufferedOutputStream(new java.io.FileOutputStream(outfile));
         byte[] buffer = new byte[65536]; // 64K
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
      try {
         in = new Base64.InputStream(new java.io.BufferedInputStream(new java.io.FileInputStream(infile)), Base64.ENCODE);
         out = new java.io.BufferedOutputStream(new java.io.FileOutputStream(outfile));
         byte[] buffer = new byte[65536]; // 64K
         int read = -1;
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
      try {
         in = new Base64.InputStream(new java.io.BufferedInputStream(new java.io.FileInputStream(infile)), Base64.ENCODE);
         out = new java.io.BufferedOutputStream(new java.io.FileOutputStream(outfile));
         byte[] buffer = new byte[65536]; // 64K
         int read = -1;
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
         } // end while: through file
         success = true;
      } catch (java.io.IOException exc) {
         exc.printStackTrace();
      } finally {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
    * @since 1.3
    */
   public static class OutputStream extends java.io.FilterOutputStream {

      private final boolean encode;
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
    * @since 2.0
    */
   public static String encodeObject(final java.io.Serializable serializableObject, final int options) {
      // Streams
      java.io.ByteArrayOutputStream baos = null;
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
   public static String encodeObject(final java.io.Serializable serializableObject, final int options) {
      // Streams
      java.io.ByteArrayOutputStream baos = null;
      java.io.OutputStream b64os = null;
      java.io.ObjectOutputStream oos = null;
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
      java.io.ByteArrayOutputStream baos = null;
      java.io.OutputStream b64os = null;
      java.io.ObjectOutputStream oos = null;
      java.util.zip.GZIPOutputStream gzos = null;

```

### UnnecessaryFullyQualifiedName
Qualifier `java.util.zip` is unnecessary, and can be replaced with an import
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
      java.io.OutputStream b64os = null;
      java.io.ObjectOutputStream oos = null;
      java.util.zip.GZIPOutputStream gzos = null;

      // Isolate options
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
      try {
         // ObjectOutputStream -> (GZIP) -> Base64 -> ByteArrayOutputStream
         baos = new java.io.ByteArrayOutputStream();
         b64os = new Base64.OutputStream(baos, Base64.ENCODE | options);

```

### UnnecessaryFullyQualifiedName
Qualifier `java.util.zip` is unnecessary, and can be replaced with an import
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
         // GZip?
         if (gzip == Base64.GZIP) {
            gzos = new java.util.zip.GZIPOutputStream(b64os);
            oos = new java.io.ObjectOutputStream(gzos);
            // end if: gzip
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
         if (gzip == Base64.GZIP) {
            gzos = new java.util.zip.GZIPOutputStream(b64os);
            oos = new java.io.ObjectOutputStream(gzos);
            // end if: gzip
         } else {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
            // end if: gzip
         } else {
            oos = new java.io.ObjectOutputStream(b64os);
         }

```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java

         oos.writeObject(serializableObject);
      } catch (java.io.IOException e) {
         ActiveMQUtilLogger.LOGGER.failedToSerializeObject(e);
         return null;
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
      // Compress?
      if (gzip == Base64.GZIP) {
         java.io.ByteArrayOutputStream baos = null;
         java.util.zip.GZIPOutputStream gzos = null;
         Base64.OutputStream b64os = null;
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util.zip` is unnecessary, and can be replaced with an import
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
      if (gzip == Base64.GZIP) {
         java.io.ByteArrayOutputStream baos = null;
         java.util.zip.GZIPOutputStream gzos = null;
         Base64.OutputStream b64os = null;

```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
         try {
            // GZip -> Base64 -> ByteArray
            baos = new java.io.ByteArrayOutputStream();
            b64os = new Base64.OutputStream(baos, Base64.ENCODE | options);
            gzos = new java.util.zip.GZIPOutputStream(b64os);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util.zip` is unnecessary, and can be replaced with an import
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
            baos = new java.io.ByteArrayOutputStream();
            b64os = new Base64.OutputStream(baos, Base64.ENCODE | options);
            gzos = new java.util.zip.GZIPOutputStream(b64os);

            gzos.write(source, off, len);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
            gzos.write(source, off, len);
            gzos.close();
         } catch (java.io.IOException e) {
            ActiveMQUtilLogger.LOGGER.failedToEncodeByteArrayToBase64Notation(e);
            return null;
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
       * @since 1.5.1
       */
      public void suspendEncoding() throws java.io.IOException {
         flushBase64();
         suspendEncoding = true;
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util.zip` is unnecessary, and can be replaced with an import
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java

         int head = bytes[0] & 0xff | bytes[1] << 8 & 0xff00;
         if (java.util.zip.GZIPInputStream.GZIP_MAGIC == head) {
            java.io.ByteArrayInputStream bais = null;
            java.util.zip.GZIPInputStream gzis = null;
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
         int head = bytes[0] & 0xff | bytes[1] << 8 & 0xff00;
         if (java.util.zip.GZIPInputStream.GZIP_MAGIC == head) {
            java.io.ByteArrayInputStream bais = null;
            java.util.zip.GZIPInputStream gzis = null;
            java.io.ByteArrayOutputStream baos = null;
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util.zip` is unnecessary, and can be replaced with an import
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
         if (java.util.zip.GZIPInputStream.GZIP_MAGIC == head) {
            java.io.ByteArrayInputStream bais = null;
            java.util.zip.GZIPInputStream gzis = null;
            java.io.ByteArrayOutputStream baos = null;
            byte[] buffer = new byte[2048];
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
            java.io.ByteArrayInputStream bais = null;
            java.util.zip.GZIPInputStream gzis = null;
            java.io.ByteArrayOutputStream baos = null;
            byte[] buffer = new byte[2048];
            int length = 0;
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java

            try {
               baos = new java.io.ByteArrayOutputStream();
               bais = new java.io.ByteArrayInputStream(bytes);
               gzis = new java.util.zip.GZIPInputStream(bais);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
            try {
               baos = new java.io.ByteArrayOutputStream();
               bais = new java.io.ByteArrayInputStream(bytes);
               gzis = new java.util.zip.GZIPInputStream(bais);

```

### UnnecessaryFullyQualifiedName
Qualifier `java.util.zip` is unnecessary, and can be replaced with an import
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
               baos = new java.io.ByteArrayOutputStream();
               bais = new java.io.ByteArrayInputStream(bytes);
               gzis = new java.util.zip.GZIPInputStream(bais);

               while ((length = gzis.read(buffer)) >= 0) {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
               bytes = baos.toByteArray();

            } catch (java.io.IOException e) {
               // Just return originally-decoded bytes
            } finally {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
       */
      @Override
      public int read() throws java.io.IOException {
         // Do we need to get data?
         if (position < 0) {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
                     } // end if: not end of stream

                  } catch (java.io.IOException e) {
                     // Only a problem if we got no data at all.
                     if (i == 0) {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
               } else {
                  // Must have broken out from above.
                  throw new java.io.IOException("Improperly padded Base64 input.");
               }

```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
         } else {
            // When JDK1.4 is more accepted, use an assertion here.
            throw new java.io.IOException("Error in Base64 code reading stream.");
         }
      } // end read
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
      try {
         // Set up some useful variables
         java.io.File file = new java.io.File(filename);
         byte[] buffer = new byte[Math.max((int) (file.length() * 1.4), 40)]; // Need max() for math on small files
         // (v2.2.1)
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
      try {
         // Set up some useful variables
         java.io.File file = new java.io.File(filename);
         byte[] buffer = new byte[Math.max((int) (file.length() * 1.4), 40)]; // Need max() for math on small files
         // (v2.2.1)
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java

         // Open a stream
         bis = new Base64.InputStream(new java.io.BufferedInputStream(new java.io.FileInputStream(file)), Base64.ENCODE);

         // Read until done
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java

         // Open a stream
         bis = new Base64.InputStream(new java.io.BufferedInputStream(new java.io.FileInputStream(file)), Base64.ENCODE);

         // Read until done
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
         encodedData = new String(buffer, 0, length, Base64.PREFERRED_ENCODING);

      } catch (java.io.IOException e) {
         System.err.println("Error encoding from file " + filename);
      } finally {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
       */
      @Override
      public void close() throws java.io.IOException {
         // 1. Ensure that pending characters are written
         flushBase64();
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
    * @since 1.3
    */
   public static class InputStream extends java.io.FilterInputStream {

      private final boolean encode; // Encoding or decoding
```

### UnnecessaryFullyQualifiedName
Qualifier `java.net` is unnecessary and can be removed
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/uri/URISupport.java`
#### Snippet
```java
    * @param query The new URI query string that should be appended to the given URI.
    * @return a new URI that is a combination of the original URI and the given query string.
    * @throws java.net.URISyntaxException
    */
   public static URI createURIWithQuery(URI uri, String query) throws URISyntaxException {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.net` is unnecessary and can be removed
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/uri/URISupport.java`
#### Snippet
```java
    * @param scheme   The new scheme value to use for the returned URI.
    * @return a new URI that is a copy of the original except that its scheme matches the supplied one.
    * @throws java.net.URISyntaxException
    */
   public static URI changeScheme(URI bindAddr, String scheme) throws URISyntaxException {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.net` is unnecessary and can be removed
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/uri/URISupport.java`
#### Snippet
```java
    * @param uri The URI whose query should be extracted and processed.
    * @return A Mapping of the URI options.
    * @throws java.net.URISyntaxException
    */
   public static Map<String, String> parseQuery(String uri) throws URISyntaxException {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.net` is unnecessary and can be removed
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/uri/URISupport.java`
#### Snippet
```java
    * @param first The index in the string of the opening parend whose close value is to be searched.
    * @return the index in the string where the closing parend is located.
    * @throws java.net.URISyntaxException fi the string does not contain a matching parend.
    */
   public static int indexOfParenthesisMatch(String str, int first) throws URISyntaxException {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.net` is unnecessary and can be removed
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/uri/URISupport.java`
#### Snippet
```java
    * @param uri The URI whose scheme value should be stripped.
    * @return The stripped URI value.
    * @throws java.net.URISyntaxException
    */
   public static URI stripScheme(URI uri) throws URISyntaxException {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.net` is unnecessary and can be removed
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/uri/URISupport.java`
#### Snippet
```java
    * @param uri The target URI that should be parsed.
    * @return a new CompositeData instance representing the parsed composite URI.
    * @throws java.net.URISyntaxException
    */
   public static CompositeData parseComposite(URI uri) throws URISyntaxException {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.net` is unnecessary and can be removed
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/uri/URISupport.java`
#### Snippet
```java
    * @param uri The URI whose query value is to be removed.
    * @return a new URI that does not contain a query value.
    * @throws java.net.URISyntaxException
    */
   public static URI removeQuery(URI uri) throws URISyntaxException {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.net` is unnecessary and can be removed
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/uri/URISupport.java`
#### Snippet
```java
    * @param params      The URI params that should be used to replace the current ones in the target.
    * @return a new URI that matches the original one but has its query options replaced with the given ones.
    * @throws java.net.URISyntaxException
    */
   public static URI createRemainingURI(URI originalURI, Map<String, String> params) throws URISyntaxException {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.net` is unnecessary and can be removed
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/uri/URISupport.java`
#### Snippet
```java
    * @param queryParameters The Key / Value mapping that will be transformed into a URI query string.
    * @return A new URI value that combines the given URI and the constructed query string.
    * @throws java.net.URISyntaxException
    */
   public static URI applyParameters(URI uri, Map<String, String> queryParameters) throws URISyntaxException {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.net` is unnecessary and can be removed
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/uri/URISupport.java`
#### Snippet
```java
    * @param optionPrefix    A string value that when not null or empty is used to prefix each query option key.
    * @return A new URI value that combines the given URI and the constructed query string.
    * @throws java.net.URISyntaxException
    */
   public static URI applyParameters(URI uri,
```

### UnnecessaryFullyQualifiedName
Qualifier `java.net` is unnecessary and can be removed
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/uri/URISupport.java`
#### Snippet
```java
    * @param uri The URI whose query should be extracted and processed.
    * @return A Mapping of the URI options.
    * @throws java.net.URISyntaxException
    */
   public static Map<String, String> parseParameters(URI uri) throws URISyntaxException {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.net` is unnecessary and can be removed
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/uri/URISupport.java`
#### Snippet
```java
    * @param options The Mapping that will create the new Query string.
    * @return a URI formatted query string.
    * @throws java.net.URISyntaxException
    */
   public static String createQueryString(Map<String, ? extends Object> options) throws URISyntaxException {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.net` is unnecessary and can be removed
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/uri/URISupport.java`
#### Snippet
```java
    * @param rc  The CompositeData instance that will be populated from the given ssp.
    * @param ssp The scheme specific part from the original string that is a composite or one or more URIs.
    * @throws java.net.URISyntaxException
    */
   private static void parseComposite(URI uri, CompositeData rc, String ssp) throws URISyntaxException {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.ra.referenceable` is unnecessary, and can be replaced with an import
in `artemis-ra/src/main/java/org/apache/activemq/artemis/jms/referenceable/ConnectionFactoryObjectFactory.java`
#### Snippet
```java
 * Should be removed on next major version change.
 */
public class ConnectionFactoryObjectFactory extends org.apache.activemq.artemis.ra.referenceable.ActiveMQRAConnectionFactoryObjectFactory {
}

```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/ActiveMQResourceAdapter.java`
#### Snippet
```java

   /**
    * @see ActiveMQRAProperties#setJgroupsChannelRefName(java.lang.String)
    */
   public void setJgroupsChannelRefName(String jgroupsChannelRefName) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.utils` is unnecessary, and can be replaced with an import
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/xml/XMLMessageImporter.java`
#### Snippet
```java
      long timestamp = 0L;
      long id = 0L;
      org.apache.activemq.artemis.utils.UUID userId = null;
      ArrayList<String> queues = new ArrayList<>();

```

### UnnecessaryFullyQualifiedName
Qualifier `jdk.internal.misc` is unnecessary, and can be replaced with an import
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/perf/MicrosClock.java`
#### Snippet
```java
   public static long now() {
      long epochSecond = offset;
      long nanoAdjustment = jdk.internal.misc.VM.getNanoTimeAdjustment(epochSecond);

      if (nanoAdjustment == -1) {
```

### UnnecessaryFullyQualifiedName
Qualifier `jdk.internal.misc` is unnecessary, and can be replaced with an import
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/perf/MicrosClock.java`
#### Snippet
```java
      if (nanoAdjustment == -1) {
         epochSecond = System.currentTimeMillis() / 1000 - 1024;
         nanoAdjustment = jdk.internal.misc.VM.getNanoTimeAdjustment(epochSecond);
         if (nanoAdjustment == -1) {
            throw new InternalError("Offset " + epochSecond + " is not in range");
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenWireRoutingHandler.java`
#### Snippet
```java
   public boolean route(OpenWireConnection openWireConnection, ConnectionInfo connectionInfo) throws Exception {
      if (!connectionInfo.isFaultTolerant()) {
         throw new java.lang.IllegalStateException("Client not fault tolerant");
      }

```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.api.core` is unnecessary, and can be replaced with an import
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenWireProtocolManager.java`
#### Snippet
```java

      final CoreMessageObjectPools objectPools = context.getConnection().getCoreMessageObjectPools();
      final org.apache.activemq.artemis.api.core.Message coreMessage = OpenWireMessageConverter.inbound(advisoryMessage, wireFormat, objectPools);

      final SimpleString address = SimpleString.toSimpleString(topic.getPhysicalName(), objectPools.getAddressStringSimpleStringPool());
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.jms.client` is unnecessary, and can be replaced with an import
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/amq/AMQConsumer.java`
#### Snippet
```java

      if (isDurable) {
         queueName = org.apache.activemq.artemis.jms.client.ActiveMQDestination.createQueueNameForSubscription(true, clientID, subscriptionName);
         if (info.getDestination().isComposite()) {
            queueName =  queueName.concat(physicalName);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.command` is unnecessary, and can be replaced with an import
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/amq/AMQConsumer.java`
#### Snippet
```java


   public org.apache.activemq.command.ActiveMQDestination getOpenwireDestination() {
      return openwireDestination;
   }
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.command` is unnecessary, and can be replaced with an import
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/amq/AMQConsumer.java`
#### Snippet
```java

   public AMQConsumer(AMQSession amqSession,
                      org.apache.activemq.command.ActiveMQDestination d,
                      ConsumerInfo info,
                      ScheduledExecutorService scheduledPool,
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.command` is unnecessary, and can be replaced with an import
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/amq/AMQConsumer.java`
#### Snippet
```java

   private final AMQSession session;
   private final org.apache.activemq.command.ActiveMQDestination openwireDestination;
   private final ConsumerInfo info;
   private final ScheduledExecutorService scheduledPool;
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary, and can be replaced with an import
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/amq/AMQSession.java`
#### Snippet
```java
      }

      List<AMQConsumer> consumersList = new java.util.LinkedList<>();

      for (ActiveMQDestination openWireDest : dests) {
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.xml.bind.annotation` is unnecessary, and can be replaced with an import
in `artemis-dto/src/main/java/org/apache/activemq/artemis/dto/package-info.java`
#### Snippet
```java
 * The JAXB POJOs for the XML configuration of ActiveMQ Artemis broker
 */
@javax.xml.bind.annotation.XmlSchema(
   namespace = "http://activemq.apache.org/schema",
   elementFormDefault = javax.xml.bind.annotation.XmlNsForm.QUALIFIED)
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.xml.bind.annotation` is unnecessary, and can be replaced with an import
in `artemis-dto/src/main/java/org/apache/activemq/artemis/dto/package-info.java`
#### Snippet
```java
@javax.xml.bind.annotation.XmlSchema(
   namespace = "http://activemq.apache.org/schema",
   elementFormDefault = javax.xml.bind.annotation.XmlNsForm.QUALIFIED)
package org.apache.activemq.artemis.dto;

```

### UnnecessaryFullyQualifiedName
Qualifier `javax.jms` is unnecessary, and can be replaced with an import
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/JMSExceptionHelper.java`
#### Snippet
```java

         case TRANSACTION_ROLLED_BACK:
            je = new javax.jms.TransactionRolledBackException(me.getMessage());
            break;

```

### UnnecessaryFullyQualifiedName
Qualifier `java.util.concurrent` is unnecessary, and can be replaced with an import
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalImpl.java`
#### Snippet
```java
                                           boolean replaceableUpdate,
                                           JournalUpdateCallback updateCallback,
                                           IOCompletion callback) throws InterruptedException, java.util.concurrent.ExecutionException {
      appendExecutor.execute(new Runnable() {
         @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary, and can be replaced with an import
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQMapMessage.java`
#### Snippet
```java
         return true;
      }
      return c.isAssignableFrom(java.util.Map.class);
   }

```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQTextMessage.java`
#### Snippet
```java
      if (text == null)
         return true;
      return c.isAssignableFrom(java.lang.String.class);
   }
}
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.jms` is unnecessary, and can be replaced with an import
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQConnectionForContext.java`
#### Snippet
```java
 * @see JMSContext
 */
public interface ActiveMQConnectionForContext extends javax.jms.Connection {

   JMSContext createContext(int sessionMode);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jndi/JNDIStorable.java`
#### Snippet
```java
    * @param out
    * @throws IOException
    * @see java.io.Externalizable#writeExternal(java.io.ObjectOutput)
    */
   public void writeObject(ObjectOutput out) throws IOException {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jndi/JNDIStorable.java`
#### Snippet
```java
    * @throws IOException
    * @throws ClassNotFoundException
    * @see java.io.Externalizable#readExternal(java.io.ObjectInput)
    */
   public void readObject(ObjectInput in) throws IOException, ClassNotFoundException {
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.jms` is unnecessary and can be removed
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQMessage.java`
#### Snippet
```java
 * instances
 */
public class ActiveMQMessage implements javax.jms.Message {

   public static final byte TYPE = org.apache.activemq.artemis.api.core.Message.DEFAULT_TYPE;
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.naming` is unnecessary and can be removed
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jndi/ActiveMQInitialContextFactory.java`
#### Snippet
```java
      Map<String, Object> data = new ConcurrentHashMap<>();

      String providerUrl = (String) environment.get(javax.naming.Context.PROVIDER_URL);
      if (providerUrl != null && !providerUrl.isEmpty()) {
         try {
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.jms` is unnecessary and can be removed
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jndi/ActiveMQInitialContextFactory.java`
#### Snippet
```java
/**
 * A factory of the ActiveMQ Artemis InitialContext which contains
 * {@link javax.jms.ConnectionFactory} instances as well as a child context called
 * <i>destinations</i> which contain all of the current active destinations, in
 * child context depending on the QoS such as transient or durable and queue or
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.naming` is unnecessary and can be removed
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jndi/ReadOnlyContext.java`
#### Snippet
```java
    * @param value
    * @return
    * @throws javax.naming.NamingException
    */
   protected Map<String, Object> internalBind(String name, Object value) throws NamingException {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.spi.core.remoting` is unnecessary and can be removed
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/client/ProtonClientProtocolManager.java`
#### Snippet
```java

/**
 * Handles proton protocol management for clients, mapping the {@link ProtonProtocolManager} to the {@link org.apache.activemq.artemis.spi.core.remoting.ClientProtocolManager} API.
 * This is currently very basic and only supports Connecting to a broker,
 * which will be useful in scenarios where the broker needs to connect to another broker through AMQP into another broker (like Interconnect) that will perform extra functionality.
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.api.core` is unnecessary, and can be replaced with an import
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPStandardMessage.java`
#### Snippet
```java

   @Override
   public Persister<org.apache.activemq.artemis.api.core.Message> getPersister() {
      return AMQPMessagePersisterV3.getInstance();
   }
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.api.core` is unnecessary, and can be replaced with an import
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPStandardMessage.java`
#### Snippet
```java

   @Override
   public final org.apache.activemq.artemis.api.core.Message copy(long newID) {
      return copy().setMessageID(newID);
   }
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.api.core` is unnecessary, and can be replaced with an import
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPStandardMessage.java`
#### Snippet
```java

   @Override
   public org.apache.activemq.artemis.api.core.Message copy() {
      ensureDataIsValid();

```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.qpid.proton.amqp.messaging` is unnecessary, and can be replaced with an import
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/ProtonServerReceiverContext.java`
#### Snippet
```java
      super.close(remoteLinkClose);
      sessionSPI.removeProducer(receiver.getName());
      org.apache.qpid.proton.amqp.messaging.Target target = (org.apache.qpid.proton.amqp.messaging.Target) receiver.getRemoteTarget();
      if (target != null && target.getDynamic() && (target.getExpiryPolicy() == TerminusExpiryPolicy.LINK_DETACH || target.getExpiryPolicy() == TerminusExpiryPolicy.SESSION_END)) {
         try {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.qpid.proton.amqp.messaging` is unnecessary, and can be replaced with an import
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/ProtonServerReceiverContext.java`
#### Snippet
```java
      super.close(remoteLinkClose);
      sessionSPI.removeProducer(receiver.getName());
      org.apache.qpid.proton.amqp.messaging.Target target = (org.apache.qpid.proton.amqp.messaging.Target) receiver.getRemoteTarget();
      if (target != null && target.getDynamic() && (target.getExpiryPolicy() == TerminusExpiryPolicy.LINK_DETACH || target.getExpiryPolicy() == TerminusExpiryPolicy.SESSION_END)) {
         try {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.qpid.proton.amqp.messaging` is unnecessary, and can be replaced with an import
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/ProtonServerReceiverContext.java`
#### Snippet
```java
   public void initialize() throws Exception {
      super.initialize();
      org.apache.qpid.proton.amqp.messaging.Target target = (org.apache.qpid.proton.amqp.messaging.Target) receiver.getRemoteTarget();

      // Match the settlement mode of the remote instead of relying on the default of MIXED.
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.qpid.proton.amqp.messaging` is unnecessary, and can be replaced with an import
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/ProtonServerReceiverContext.java`
#### Snippet
```java
   public void initialize() throws Exception {
      super.initialize();
      org.apache.qpid.proton.amqp.messaging.Target target = (org.apache.qpid.proton.amqp.messaging.Target) receiver.getRemoteTarget();

      // Match the settlement mode of the remote instead of relying on the default of MIXED.
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.qpid.proton.amqp.messaging` is unnecessary, and can be replaced with an import
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/ProtonServerReceiverContext.java`
#### Snippet
```java

   public RoutingType getRoutingType(Receiver receiver, SimpleString address) {
      org.apache.qpid.proton.amqp.messaging.Target target = (org.apache.qpid.proton.amqp.messaging.Target) receiver.getRemoteTarget();
      return target != null ? getRoutingType(target.getCapabilities(), address) : getRoutingType((Symbol[]) null, address);
   }
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.qpid.proton.amqp.messaging` is unnecessary, and can be replaced with an import
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/ProtonServerReceiverContext.java`
#### Snippet
```java

   public RoutingType getRoutingType(Receiver receiver, SimpleString address) {
      org.apache.qpid.proton.amqp.messaging.Target target = (org.apache.qpid.proton.amqp.messaging.Target) receiver.getRemoteTarget();
      return target != null ? getRoutingType(target.getCapabilities(), address) : getRoutingType((Symbol[]) null, address);
   }
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.qpid.proton.amqp.messaging` is unnecessary and can be removed
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/ProtonServerSenderContext.java`
#### Snippet
```java
            // the lifetime policy and capabilities of the new subscription.
            if (result.isExists()) {
               source = new org.apache.qpid.proton.amqp.messaging.Source();
               source.setAddress(queue.toString());
               source.setDurable(TerminusDurability.UNSETTLED_STATE);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary, and can be replaced with an import
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/ProtonServerSenderContext.java`
#### Snippet
```java
            // if dynamic we have to create the node (queue) and set the address on the target, the
            // node is temporary and  will be deleted on closing of the session
            queue = SimpleString.toSimpleString(java.util.UUID.randomUUID().toString());
            tempQueueName = queue;
            try {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary, and can be replaced with an import
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/ProtonServerSenderContext.java`
#### Snippet
```java
                     }
                  } else {
                     queue = SimpleString.toSimpleString(java.util.UUID.randomUUID().toString());
                     tempQueueName = queue;
                     try {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.reader` is unnecessary, and can be replaced with an import
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/converter/AmqpCoreConverter.java`
#### Snippet
```java
            switch (value) {
               case AMQPMessageSupport.QUEUE_TYPE:
                  org.apache.activemq.artemis.reader.MessageUtil.setJMSReplyTo(jms.getInnerMessage(), QUEUE_QUALIFIED_PREFIX + properties.getReplyTo());
                  break;
               case AMQPMessageSupport.TEMP_QUEUE_TYPE:
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.reader` is unnecessary, and can be replaced with an import
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/converter/AmqpCoreConverter.java`
#### Snippet
```java
                  break;
               case AMQPMessageSupport.TEMP_QUEUE_TYPE:
                  org.apache.activemq.artemis.reader.MessageUtil.setJMSReplyTo(jms.getInnerMessage(), TEMP_QUEUE_QUALIFED_PREFIX + properties.getReplyTo());
                  break;
               case AMQPMessageSupport.TOPIC_TYPE:
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.reader` is unnecessary, and can be replaced with an import
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/converter/AmqpCoreConverter.java`
#### Snippet
```java
                  break;
               case AMQPMessageSupport.TOPIC_TYPE:
                  org.apache.activemq.artemis.reader.MessageUtil.setJMSReplyTo(jms.getInnerMessage(), TOPIC_QUALIFIED_PREFIX + properties.getReplyTo());
                  break;
               case AMQPMessageSupport.TEMP_TOPIC_TYPE:
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.reader` is unnecessary, and can be replaced with an import
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/converter/AmqpCoreConverter.java`
#### Snippet
```java
                  break;
               case AMQPMessageSupport.TEMP_TOPIC_TYPE:
                  org.apache.activemq.artemis.reader.MessageUtil.setJMSReplyTo(jms.getInnerMessage(), TEMP_TOPIC_QUALIFED_PREFIX + properties.getReplyTo());
                  break;
               default:
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.reader` is unnecessary, and can be replaced with an import
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/converter/AmqpCoreConverter.java`
#### Snippet
```java
                  break;
               default:
                  org.apache.activemq.artemis.reader.MessageUtil.setJMSReplyTo(jms.getInnerMessage(), QUEUE_QUALIFIED_PREFIX + properties.getReplyTo());
                  break;
            }
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.api.core` is unnecessary and can be removed
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/Message.java`
#### Snippet
```java


   default org.apache.activemq.artemis.api.core.Message putExtraBytesProperty(SimpleString key, byte[] value) {
      return putBytesProperty(key, value);
   }
```

### UnnecessaryFullyQualifiedName
Qualifier `org.jgroups` is unnecessary, and can be replaced with an import
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/JGroupsBroadcastEndpoint.java`
#### Snippet
```java

      if (broadcastOpened) {
         org.jgroups.Message msg = new org.jgroups.BytesMessage();

         msg.setArray(data);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.api.core` is unnecessary and can be removed
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/client/ServerLocator.java`
#### Snippet
```java
    * <p>
    * If client's blocking calls to the server take more than this timeout, the call will throw a
    * {@link org.apache.activemq.artemis.api.core.ActiveMQException} with the code {@link org.apache.activemq.artemis.api.core.ActiveMQExceptionType#CONNECTION_TIMEDOUT}. Value
    * is in milliseconds, default value is {@link ActiveMQClient#DEFAULT_CALL_TIMEOUT}.
    *
```

### UnnecessaryFullyQualifiedName
Qualifier `org.jgroups` is unnecessary, and can be replaced with an import
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/jgroups/JGroupsReceiver.java`
#### Snippet
```java

   @Override
   public void receive(org.jgroups.Message msg) {
      logger.trace("sending message {}", msg);
      dequeue.add(msg.getArray());
```

### UnnecessaryFullyQualifiedName
Qualifier `org.jgroups` is unnecessary and can be removed
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/jgroups/JChannelWrapper.java`
#### Snippet
```java

         @Override
         public void receive(org.jgroups.Message msg) {
            if (logger.isTraceEnabled()) {
               logger.trace("{}:: Wrapper received {} on channel {}", this, msg, channelName);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.jgroups` is unnecessary and can be removed
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/jgroups/JChannelWrapper.java`
#### Snippet
```java
   }

   public void send(org.jgroups.Message msg) throws Exception {
      if (logger.isTraceEnabled()) {
         logger.trace("{}::Sending JGroups Message: Open={} on channel {} msg={}", this, channel.isOpen(), channelName, msg);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary, and can be replaced with an import
in `artemis-junit/artemis-junit-commons/src/main/java/org/apache/activemq/artemis/junit/EmbeddedActiveMQDelegate.java`
#### Snippet
```java
         throw new IllegalArgumentException("getBoundQueues( address ) - address cannot be null");
      }
      List<Queue> boundQueues = new java.util.LinkedList<>();

      BindingQueryResult bindingQueryResult = null;
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/management/AddressControl.java`
#### Snippet
```java
    * Pauses all the queues bound to this address. Messages are no longer delivered to all its bounded queues. Newly added queue will be paused too until resume is called.
    * @param persist if true, the pause state will be persisted.
    * @throws java.lang.Exception
    */
   @Operation(desc = "Pauses the queues bound to this address", impact = MBeanOperationInfo.ACTION)
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/management/AddressControl.java`
#### Snippet
```java
   /**
    * Resume all the queues bound of this address. Messages are delivered again to all its bounded queues.
    * @throws java.lang.Exception
    */
   @Operation(desc = "Resumes the queues bound to this address", impact = MBeanOperationInfo.ACTION)
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/management/AddressControl.java`
#### Snippet
```java
   /**
    * Purge all the queues bound of this address. Returns the total number of messages purged.
    * @throws java.lang.Exception
    */
   @Operation(desc = "Purges the queues bound to this address. Returns the total number of messages purged.", impact = MBeanOperationInfo.ACTION)
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/api/core/management/AddressControl.java`
#### Snippet
```java
    * Pauses all the queues bound to this address. Messages are no longer delivered to all its bounded queues.
    * Newly added queue will be paused too until resume is called.
    * @throws java.lang.Exception
    */
   @Operation(desc = "Pauses the queues bound to this address", impact = MBeanOperationInfo.ACTION)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.spi.core.remoting` is unnecessary and can be removed
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/spi/core/remoting/BufferDecoder.java`
#### Snippet
```java

   /**
    * called by the remoting system prior to {@link org.apache.activemq.artemis.spi.core.remoting.BufferHandler#bufferReceived(Object, ActiveMQBuffer)}.
    * <p>
    * The implementation should return true if there is enough data in the buffer to decode. otherwise false.
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.core.server.management` is unnecessary and can be removed
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/server/management/Notification.java`
#### Snippet
```java
 * A Notification
 *
 * @see org.apache.activemq.artemis.core.server.management.NotificationListener
 * @see NotificationType
 */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.api.core.client` is unnecessary and can be removed
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ServerLocatorImpl.java`
#### Snippet
```java

/**
 * This is the implementation of {@link org.apache.activemq.artemis.api.core.client.ServerLocator} and all
 * the proper javadoc is located on that interface.
 */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.core.protocol.core` is unnecessary and can be removed
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/Channel.java`
#### Snippet
```java

   /**
    * forces any {@link org.apache.activemq.artemis.core.protocol.core.Channel#sendBlocking(Packet, byte)} request to return with an exception.
    */
   void returnBlocking(Throwable cause);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.core.protocol.core` is unnecessary and can be removed
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/Channel.java`
#### Snippet
```java

   /**
    * Gets the {@link org.apache.activemq.artemis.core.protocol.core.ChannelHandler} that this channel should
    * forward received packets to.
    *
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.core.protocol.core` is unnecessary and can be removed
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/Channel.java`
#### Snippet
```java

   /**
    * Called by {@link org.apache.activemq.artemis.core.protocol.core.CoreRemotingConnection} when a packet is received.
    * <p>
    * This method should then call its {@link org.apache.activemq.artemis.core.protocol.core.ChannelHandler} after appropriate processing of
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.core.protocol.core` is unnecessary and can be removed
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/Channel.java`
#### Snippet
```java
    * Called by {@link org.apache.activemq.artemis.core.protocol.core.CoreRemotingConnection} when a packet is received.
    * <p>
    * This method should then call its {@link org.apache.activemq.artemis.core.protocol.core.ChannelHandler} after appropriate processing of
    * the packet
    *
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.core.protocol.core` is unnecessary and can be removed
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/Channel.java`
#### Snippet
```java

   /**
    * Sets the {@link org.apache.activemq.artemis.core.protocol.core.ChannelHandler} that this channel should
    * forward received packets to.
    *
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.core.protocol.core` is unnecessary and can be removed
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/Channel.java`
#### Snippet
```java

   /**
    * forces any {@link org.apache.activemq.artemis.core.protocol.core.Channel#sendBlocking(Packet, byte)} request to return with an exception.
    */
   void returnBlocking();
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.core.protocol.core` is unnecessary and can be removed
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/Channel.java`
#### Snippet
```java

/**
 * A channel is a way of interleaving data meant for different endpoints over the same {@link org.apache.activemq.artemis.core.protocol.core.CoreRemotingConnection}.
 * <p>
 * Any packet sent will have its channel id set to the specific channel sending so it can be routed to its correct channel
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.core.protocol.core` is unnecessary and can be removed
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/Channel.java`
#### Snippet
```java
 * <p>
 * Any packet sent will have its channel id set to the specific channel sending so it can be routed to its correct channel
 * when received by the {@link org.apache.activemq.artemis.core.protocol.core.CoreRemotingConnection}. see {@link org.apache.activemq.artemis.core.protocol.core.Packet#setChannelID(long)}.
 * <p>
 * Each Channel should will forward any packets received to its {@link org.apache.activemq.artemis.core.protocol.core.ChannelHandler}.
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.core.protocol.core` is unnecessary and can be removed
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/Channel.java`
#### Snippet
```java
 * <p>
 * Any packet sent will have its channel id set to the specific channel sending so it can be routed to its correct channel
 * when received by the {@link org.apache.activemq.artemis.core.protocol.core.CoreRemotingConnection}. see {@link org.apache.activemq.artemis.core.protocol.core.Packet#setChannelID(long)}.
 * <p>
 * Each Channel should will forward any packets received to its {@link org.apache.activemq.artemis.core.protocol.core.ChannelHandler}.
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.core.protocol.core` is unnecessary and can be removed
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/Channel.java`
#### Snippet
```java
 * when received by the {@link org.apache.activemq.artemis.core.protocol.core.CoreRemotingConnection}. see {@link org.apache.activemq.artemis.core.protocol.core.Packet#setChannelID(long)}.
 * <p>
 * Each Channel should will forward any packets received to its {@link org.apache.activemq.artemis.core.protocol.core.ChannelHandler}.
 * <p>
 * A Channel *does not* support concurrent access by more than one thread!
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary, and can be replaced with an import
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/message/impl/CoreMessage.java`
#### Snippet
```java
         return "0";
      } else {
         return new java.util.Date(timestamp).toString();
      }
   }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary, and can be replaced with an import
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/ChannelImpl.java`
#### Snippet
```java
   private Packet response;

   private final java.util.Queue<Packet> resendCache;

   private final ResponseCache responseAsyncCache;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.api.core` is unnecessary and can be removed
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/wireformat/SessionSendContinuationMessage.java`
#### Snippet
```java
    *
    * @see org.apache.activemq.artemis.api.core.client.ClientSession#setSendAcknowledgementHandler(SendAcknowledgementHandler)
    * @see org.apache.activemq.artemis.api.core.client.ClientProducer#send(org.apache.activemq.artemis.api.core.SimpleString, org.apache.activemq.artemis.api.core.Message, SendAcknowledgementHandler)
    */
   private final transient SendAcknowledgementHandler handler;
```

### UnnecessaryFullyQualifiedName
Qualifier `io.netty.util.concurrent` is unnecessary, and can be replaced with an import
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/DelegatingEventLoopGroup.java`
#### Snippet
```java

   @Override
   public io.netty.util.concurrent.ScheduledFuture<?> scheduleAtFixedRate(Runnable runnable,
                                                                          long l,
                                                                          long l1,
```

### UnnecessaryFullyQualifiedName
Qualifier `io.netty.util.concurrent` is unnecessary, and can be replaced with an import
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/DelegatingEventLoopGroup.java`
#### Snippet
```java

   @Override
   public io.netty.util.concurrent.ScheduledFuture<?> scheduleWithFixedDelay(Runnable runnable,
                                                                             long l,
                                                                             long l1,
```

### UnnecessaryFullyQualifiedName
Qualifier `io.netty.util.concurrent` is unnecessary, and can be replaced with an import
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/DelegatingEventLoopGroup.java`
#### Snippet
```java

   @Override
   public <V> io.netty.util.concurrent.ScheduledFuture<V> schedule(Callable<V> callable, long l, TimeUnit timeUnit) {
      return delegate.schedule(callable, l, timeUnit);
   }
```

### UnnecessaryFullyQualifiedName
Qualifier `io.netty.util.concurrent` is unnecessary, and can be replaced with an import
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/DelegatingEventLoopGroup.java`
#### Snippet
```java

   @Override
   public io.netty.util.concurrent.ScheduledFuture<?> schedule(Runnable runnable, long l, TimeUnit timeUnit) {
      return delegate.schedule(runnable, l, timeUnit);
   }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/remoting/Acceptor.java`
#### Snippet
```java
    * Whether this acceptor allows insecure connections.
    *
    * @throws java.lang.IllegalStateException if false @setDefaultActiveMQPrincipal
    */
   boolean isUnsecurable();
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.spi.core.remoting` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/remoting/AcceptorFactory.java`
#### Snippet
```java
 * A factory for creating acceptors.
 * <p>
 * An Acceptor is an endpoint that a {@link org.apache.activemq.artemis.spi.core.remoting.Connector} will connect to and is used by the remoting service.
 */
public interface AcceptorFactory {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.core.security` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/impl/SecurityConfiguration.java`
#### Snippet
```java
   }

   public org.apache.activemq.artemis.core.security.User getUser(String username) {
      return users.get(username);
   }
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.core.filter` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/filter/FilterUtils.java`
#### Snippet
```java

   /**
    * Returns {@code true} if {@code filter} is a {@link org.apache.activemq.artemis.core.filter.Filter#GENERIC_IGNORED_FILTER}.
    *
    * @param filter a subscription filter
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.core.filter` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/filter/FilterUtils.java`
#### Snippet
```java
    *
    * @param filter a subscription filter
    * @return {@code true} if {@code filter} is not {@code null} and is a {@link org.apache.activemq.artemis.core.filter.Filter#GENERIC_IGNORED_FILTER}
    */
   public static boolean isTopicIdentification(final Filter filter) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.api.core` is unnecessary, and can be replaced with an import
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/ActiveMQServerLogger.java`
#### Snippet
```java

   @LogMessage(id = 222059, value = "Duplicate message detected - message will not be routed. Message information:\n{}", level = LogMessage.Level.WARN)
   void duplicateMessageDetected(org.apache.activemq.artemis.api.core.Message message);

   @LogMessage(id = 222060, value = "Error while confirming large message completion on rollback for recordID={}", level = LogMessage.Level.WARN)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.api.core` is unnecessary, and can be replaced with an import
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/ActiveMQServerLogger.java`
#### Snippet
```java

   @LogMessage(id = 222110, value = "no queue IDs defined!,  originalMessage  = {}, copiedMessage = {}, props={}", level = LogMessage.Level.WARN)
   void noQueueIdDefined(org.apache.activemq.artemis.api.core.Message message, org.apache.activemq.artemis.api.core.Message messageCopy, SimpleString idsHeaderName);

   @LogMessage(id = 222111, value = "exception while invoking {} on {}", level = LogMessage.Level.TRACE)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.api.core` is unnecessary, and can be replaced with an import
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/ActiveMQServerLogger.java`
#### Snippet
```java

   @LogMessage(id = 222110, value = "no queue IDs defined!,  originalMessage  = {}, copiedMessage = {}, props={}", level = LogMessage.Level.WARN)
   void noQueueIdDefined(org.apache.activemq.artemis.api.core.Message message, org.apache.activemq.artemis.api.core.Message messageCopy, SimpleString idsHeaderName);

   @LogMessage(id = 222111, value = "exception while invoking {} on {}", level = LogMessage.Level.TRACE)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.core.server` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/ActiveMQServer.java`
#### Snippet
```java

   /**
    * @param callback {@link org.apache.activemq.artemis.core.server.PostQueueDeletionCallback}
    */
   void registerPostQueueDeletionCallback(PostQueueDeletionCallback callback);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util.concurrent` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/ActiveMQServer.java`
#### Snippet
```java
    * timeout period, {@code false} otherwise.
    * @throws InterruptedException
    * @see java.util.concurrent.CountDownLatch#await(long, java.util.concurrent.TimeUnit)
    */
   boolean waitForActivation(long timeout, TimeUnit unit) throws InterruptedException;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.core.server` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/ActiveMQServer.java`
#### Snippet
```java

   /**
    * @param callback {@link org.apache.activemq.artemis.core.server.PostQueueCreationCallback}
    */
   void unregisterPostQueueCreationCallback(PostQueueCreationCallback callback);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.core.server` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/ActiveMQServer.java`
#### Snippet
```java

   /**
    * @param callback {@link org.apache.activemq.artemis.core.server.PostQueueCreationCallback}
    */
   void registerPostQueueCreationCallback(PostQueueCreationCallback callback);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.core.server` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/ActiveMQServer.java`
#### Snippet
```java

   /**
    * @param callback {@link org.apache.activemq.artemis.core.server.PostQueueDeletionCallback}
    */
   void unregisterPostQueueDeletionCallback(PostQueueDeletionCallback callback);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.core.protocol.core.impl.wireformat` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ReplicationObserver.java`
#### Snippet
```java
   /**
    * This is a safety net in case the live sends the first {@link ReplicationLiveIsStoppingMessage}
    * with code {@link org.apache.activemq.artemis.core.protocol.core.impl.wireformat.ReplicationLiveIsStoppingMessage.LiveStopping#STOP_CALLED} and crashes before sending the second with
    * {@link org.apache.activemq.artemis.core.protocol.core.impl.wireformat.ReplicationLiveIsStoppingMessage.LiveStopping#FAIL_OVER}.
    * <p>
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.core.protocol.core.impl.wireformat` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ReplicationObserver.java`
#### Snippet
```java
    * This is a safety net in case the live sends the first {@link ReplicationLiveIsStoppingMessage}
    * with code {@link org.apache.activemq.artemis.core.protocol.core.impl.wireformat.ReplicationLiveIsStoppingMessage.LiveStopping#STOP_CALLED} and crashes before sending the second with
    * {@link org.apache.activemq.artemis.core.protocol.core.impl.wireformat.ReplicationLiveIsStoppingMessage.LiveStopping#FAIL_OVER}.
    * <p>
    * If the second message does come within this dead line, we fail over anyway.
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary, and can be replaced with an import
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerConsumerImpl.java`
#### Snippet
```java
   private final StorageManager storageManager;

   private final java.util.Deque<MessageReference> deliveringRefs = new ArrayDeque<>();

   private SessionCallback callback;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.core.server.impl` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/InVMNodeManager.java`
#### Snippet
```java
 * NodeManager used to run multiple servers in the same VM.
 * <p>
 * We use the {@link org.apache.activemq.artemis.core.server.impl.InVMNodeManager} instead of {@link org.apache.activemq.artemis.core.server.impl.FileLockNodeManager} when
 * multiple servers are run inside the same VM and File Locks can not be shared in the
 * same VM (it would cause a shared lock violation).
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.core.server.impl` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/InVMNodeManager.java`
#### Snippet
```java
 * NodeManager used to run multiple servers in the same VM.
 * <p>
 * We use the {@link org.apache.activemq.artemis.core.server.impl.InVMNodeManager} instead of {@link org.apache.activemq.artemis.core.server.impl.FileLockNodeManager} when
 * multiple servers are run inside the same VM and File Locks can not be shared in the
 * same VM (it would cause a shared lock violation).
```

### UnnecessaryFullyQualifiedName
Qualifier `java.nio.channels` is unnecessary, and can be replaced with an import
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/FileLockNodeManager.java`
#### Snippet
```java

         return lock;
      } catch (java.nio.channels.OverlappingFileLockException ex) {
         // This just means that another object on the same JVM is holding the lock
         return null;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.api.core` is unnecessary, and can be replaced with an import
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/plugin/impl/LoggingActiveMQServerPluginLogger.java`
#### Snippet
```java
   @LogMessage(id = 843016, value = "onSendError message ID: {}, message {}, session name: {} with tx: {}, session: {}, direct: {}," + " noAutoCreateQueue: {}", level = LogMessage.Level.DEBUG)
   void onSendErrorDetails(String messageID,
                           org.apache.activemq.artemis.api.core.Message message,
                           String sessionName,
                           Transaction tx,
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.api.core` is unnecessary, and can be replaced with an import
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/plugin/impl/LoggingActiveMQServerPluginLogger.java`
#### Snippet
```java

   @LogMessage(id = 843008, value = "beforeSend called with message: {}, tx: {}, session: {}, direct: {}," + " noAutoCreateQueue: {}", level = LogMessage.Level.DEBUG)
   void beforeSend(org.apache.activemq.artemis.api.core.Message message,
                   Transaction tx,
                   ServerSession session,
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.api.core` is unnecessary, and can be replaced with an import
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/plugin/impl/LoggingActiveMQServerPluginLogger.java`
#### Snippet
```java

   @LogMessage(id = 843017, value = "onMessageRouteError message: {}, with context: {}, direct: {}, rejectDuplicates: {}", level = LogMessage.Level.DEBUG)
   void onMessageRouteErrorDetails(org.apache.activemq.artemis.api.core.Message message,
                                   RoutingContext context,
                                   boolean direct,
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.api.core` is unnecessary, and can be replaced with an import
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/plugin/impl/LoggingActiveMQServerPluginLogger.java`
#### Snippet
```java

   @LogMessage(id = 843009, value = "afterSend message: {}, result: {}, transaction: {}, session: {}, connection: {}, direct: {}, noAutoCreateQueue: {}", level = LogMessage.Level.DEBUG)
   void afterSendDetails(org.apache.activemq.artemis.api.core.Message message,
                         String result,
                         Transaction tx,
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.api.core` is unnecessary, and can be replaced with an import
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/plugin/impl/LoggingActiveMQServerPluginLogger.java`
#### Snippet
```java

   @LogMessage(id = 843011, value = "afterMessageRoute message: {}, with context: {}, direct: {}, rejectDuplicates: {}", level = LogMessage.Level.DEBUG)
   void afterMessageRouteDetails(org.apache.activemq.artemis.api.core.Message message,
                                 RoutingContext context,
                                 boolean direct,
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.api.core` is unnecessary, and can be replaced with an import
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/plugin/impl/LoggingActiveMQServerPluginLogger.java`
#### Snippet
```java

   @LogMessage(id = 843010, value = "beforeMessageRoute called with message: {}, context: {}, direct: {}, rejectDuplicates: {}", level = LogMessage.Level.DEBUG)
   void beforeMessageRoute(org.apache.activemq.artemis.api.core.Message message,
                           RoutingContext context,
                           boolean direct,
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.core.server.cluster.qourum` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/qourum/QuorumVote.java`
#### Snippet
```java

   /**
    * the name of this quorum vote, used for identifying the correct {@link org.apache.activemq.artemis.core.server.cluster.qourum.QuorumVoteHandler}
    *
    * @return the name of the wuorum vote
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.core.server.cluster.qourum` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/qourum/QuorumVote.java`
#### Snippet
```java

   /**
    * called by the {@link org.apache.activemq.artemis.core.server.cluster.qourum.QuorumManager} fails to connect to a node in the quorum.
    * The QuorumVote can then decide whether or not a decision can be made with just that information however the node
    * cannot cannot be asked.
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.core.server.cluster.qourum` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/qourum/QuorumVote.java`
#### Snippet
```java

   /**
    * called by the {@link org.apache.activemq.artemis.core.server.cluster.qourum.QuorumManager} when one of the nodes in the quorum is
    * successfully connected to. The QuorumVote can then decide whether or not a decision can be made with just that information.
    *
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.core.server.cluster.qourum` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/qourum/QuorumVote.java`
#### Snippet
```java

   /**
    * called by the {@link org.apache.activemq.artemis.core.server.cluster.qourum.QuorumManager} when all the votes have been cast and received.
    *
    * @param voteTopology the topology of where the votes were sent.
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.core.server.cluster.qourum` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/qourum/QuorumVote.java`
#### Snippet
```java

   /**
    * called by the {@link org.apache.activemq.artemis.core.server.cluster.qourum.QuorumManager} when a vote can be made, either from the
    * cluster or decided by itself.
    *
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.core.protocol.core.impl.wireformat` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/qourum/SharedNothingBackupQuorum.java`
#### Snippet
```java
   /**
    * This is a safety net in case the live sends the first {@link ReplicationLiveIsStoppingMessage}
    * with code {@link org.apache.activemq.artemis.core.protocol.core.impl.wireformat.ReplicationLiveIsStoppingMessage.LiveStopping#STOP_CALLED} and crashes before sending the second with
    * {@link org.apache.activemq.artemis.core.protocol.core.impl.wireformat.ReplicationLiveIsStoppingMessage.LiveStopping#FAIL_OVER}.
    * <p>
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.core.protocol.core.impl.wireformat` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/qourum/SharedNothingBackupQuorum.java`
#### Snippet
```java
    * This is a safety net in case the live sends the first {@link ReplicationLiveIsStoppingMessage}
    * with code {@link org.apache.activemq.artemis.core.protocol.core.impl.wireformat.ReplicationLiveIsStoppingMessage.LiveStopping#STOP_CALLED} and crashes before sending the second with
    * {@link org.apache.activemq.artemis.core.protocol.core.impl.wireformat.ReplicationLiveIsStoppingMessage.LiveStopping#FAIL_OVER}.
    * <p>
    * If the second message does come within this dead line, we fail over anyway.
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary, and can be replaced with an import
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/impl/BridgeImpl.java`
#### Snippet
```java
   private final Filter filter;

   final java.util.Map<Long, MessageReference> refs = new LinkedHashMap<>();

   private final Transformer transformer;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.core.server.cluster.qourum` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/qourum/QuorumManager.java`
#### Snippet
```java
   /**
    * this will connect to a node and then cast a vote. whether or not this vote is asked of the target node is dependent
    * on {@link org.apache.activemq.artemis.core.server.cluster.qourum.Vote#isRequestServerVote()}
    */
   private final class VoteRunnable implements Runnable {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.core.server.cluster.qourum` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/qourum/QuorumManager.java`
#### Snippet
```java

   /**
    * notify the {@link org.apache.activemq.artemis.core.server.cluster.qourum.Quorum} of a topology change.
    *
    * @param eventUID
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.core.server.cluster.qourum` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/qourum/QuorumManager.java`
#### Snippet
```java

/**
 * A QourumManager can be used to register a {@link org.apache.activemq.artemis.core.server.cluster.qourum.Quorum} to receive notifications
 * about changes to the cluster. A {@link org.apache.activemq.artemis.core.server.cluster.qourum.Quorum} can then issue a vote to the
 * remaining nodes in a cluster for a specific outcome
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.core.server.cluster.qourum` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/qourum/QuorumManager.java`
#### Snippet
```java
/**
 * A QourumManager can be used to register a {@link org.apache.activemq.artemis.core.server.cluster.qourum.Quorum} to receive notifications
 * about changes to the cluster. A {@link org.apache.activemq.artemis.core.server.cluster.qourum.Quorum} can then issue a vote to the
 * remaining nodes in a cluster for a specific outcome
 */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.core.server.cluster.qourum` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/qourum/QuorumManager.java`
#### Snippet
```java

   /**
    * unregisters a {@link org.apache.activemq.artemis.core.server.cluster.qourum.Quorum}.
    *
    * @param quorum
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.core.server.cluster.qourum` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/qourum/QuorumManager.java`
#### Snippet
```java
   /**
    * called by the {@link org.apache.activemq.artemis.core.client.impl.ServerLocatorInternal} when the topology changes. we update the
    * {@code maxClusterSize} if needed and inform the {@link org.apache.activemq.artemis.core.server.cluster.qourum.Quorum}'s.
    *
    * @param topologyMember the topolgy changed
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.core.server.cluster.qourum` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/qourum/QuorumManager.java`
#### Snippet
```java

   /**
    * registers a {@link org.apache.activemq.artemis.core.server.cluster.qourum.Quorum} so that it can be notified of changes in the cluster.
    *
    * @param quorum
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.core.server.cluster.qourum` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/qourum/QuorumManager.java`
#### Snippet
```java

   /**
    * all the current registered {@link org.apache.activemq.artemis.core.server.cluster.qourum.Quorum}'s
    */
   private final Map<String, Quorum> quorums = new HashMap<>();
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.core.filter` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ActiveMQServerImpl.java`
#### Snippet
```java
    * this filter is considered a global ignore
    *
    * @deprecated Replaced by {@link org.apache.activemq.artemis.core.filter.Filter#GENERIC_IGNORED_FILTER}
    */
   @Deprecated
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/impl/ConfigurationImpl.java`
#### Snippet
```java
            return (T) convertedValue;
         }
      }, java.util.List.class);

      beanUtils.getConvertUtils().register(new Converter() {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/impl/ConfigurationImpl.java`
#### Snippet
```java
            return (T) convertedValue;
         }
      }, java.util.Set.class);

      // support 25K or 25m etc like xml config
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.api.config` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/Configuration.java`
#### Snippet
```java
   /**
    * Returns the maximum number of write requests that can be in the AIO queue at any given time. <br>
    * Default value is {@link org.apache.activemq.artemis.api.config.ActiveMQDefaultConfiguration#DEFAULT_JOURNAL_MAX_IO_AIO}.
    */
   int getJournalMaxIO_AIO();
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.api.config` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/Configuration.java`
#### Snippet
```java
    * Returns the connection time to live. <br>
    * This value overrides the connection time to live <em>sent by the client</em>. <br>
    * Default value is {@link org.apache.activemq.artemis.api.config.ActiveMQDefaultConfiguration#DEFAULT_CONNECTION_TTL_OVERRIDE}.
    */
   long getConnectionTTLOverride();
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.api.config` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/Configuration.java`
#### Snippet
```java
   /**
    * Returns the maximum number of days kept in memory for message counter. <br>
    * Default value is {@link org.apache.activemq.artemis.api.config.ActiveMQDefaultConfiguration#DEFAULT_MESSAGE_COUNTER_MAX_DAY_HISTORY}.
    */
   int getMessageCounterMaxDayHistory();
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.api.config` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/Configuration.java`
#### Snippet
```java

   /**
    * Number of files that would be acceptable to keep on a pool. Default value is {@link org.apache.activemq.artemis.api.config.ActiveMQDefaultConfiguration#DEFAULT_JOURNAL_POOL_FILES}.
    */
   Configuration setJournalPoolFiles(int poolSize);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.api.config` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/Configuration.java`
#### Snippet
```java
   /**
    * Returns the sample period (in milliseconds) to take message counter snapshot. <br>
    * Default value is {@link org.apache.activemq.artemis.api.config.ActiveMQDefaultConfiguration#DEFAULT_MESSAGE_COUNTER_SAMPLE_PERIOD}.
    */
   long getMessageCounterSamplePeriod();
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.api.config` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/Configuration.java`
#### Snippet
```java
   /**
    * Returns whether the journal is synchronized when receiving non-transactional data. <br>
    * Default value is {@link org.apache.activemq.artemis.api.config.ActiveMQDefaultConfiguration#DEFAULT_JOURNAL_SYNC_NON_TRANSACTIONAL}.
    */
   boolean isJournalSyncNonTransactional();
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.api.config` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/Configuration.java`
#### Snippet
```java
    * Returns the timeout (in milliseconds) after which transactions is removed from the resource
    * manager after it was created. <br>
    * Default value is {@link org.apache.activemq.artemis.api.config.ActiveMQDefaultConfiguration#DEFAULT_TRANSACTION_TIMEOUT}.
    */
   long getTransactionTimeout();
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.api.config` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/Configuration.java`
#### Snippet
```java
   /**
    * Returns the domain used by JMX MBeans (provided JMX management is enabled). <br>
    * Default value is {@link org.apache.activemq.artemis.api.config.ActiveMQDefaultConfiguration#DEFAULT_JMX_DOMAIN}.
    */
   String getJMXDomain();
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.api.config` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/Configuration.java`
#### Snippet
```java
   /**
    * Returns whether suppression of session-notifications is enabled for this server. <br>
    * Default value is {@link org.apache.activemq.artemis.api.config.ActiveMQDefaultConfiguration#DEFAULT_SUPPRESS_SESSION_NOTIFICATIONS}.
    */
   boolean isSuppressSessionNotifications();
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.api.config` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/Configuration.java`
#### Snippet
```java
   /**
    * Returns whether this server is using persistence and store data. <br>
    * Default value is {@link org.apache.activemq.artemis.api.config.ActiveMQDefaultConfiguration#DEFAULT_PERSISTENCE_ENABLED}.
    */
   boolean isPersistenceEnabled();
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.api.config` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/Configuration.java`
#### Snippet
```java
   /**
    * Returns the maximum number of write requests for NIO journal. <br>
    * Default value is {@link org.apache.activemq.artemis.api.config.ActiveMQDefaultConfiguration#DEFAULT_JOURNAL_MAX_IO_NIO}.
    */
   int getJournalMaxIO_NIO();
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.api.config` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/Configuration.java`
#### Snippet
```java
    * Returns the maximal number of data files before we can start deleting corrupted files instead of moving them to attic.
    * <br>
    * Default value is  {@link org.apache.activemq.artemis.api.config.ActiveMQDefaultConfiguration#DEFAULT_JOURNAL_MAX_ATTIC_FILES}.
    */
   int getJournalMaxAtticFiles();
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.api.config` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/Configuration.java`
#### Snippet
```java
    * Returns the frequency (in milliseconds) to scan transactions to detect which transactions have
    * timed out. <br>
    * Default value is {@link org.apache.activemq.artemis.api.config.ActiveMQDefaultConfiguration#DEFAULT_TRANSACTION_TIMEOUT_SCAN_PERIOD}.
    */
   long getTransactionTimeoutScanPeriod();
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.api.config` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/Configuration.java`
#### Snippet
```java
   /**
    * Returns whether the bindings directory is created on this server startup. <br>
    * Default value is {@link org.apache.activemq.artemis.api.config.ActiveMQDefaultConfiguration#DEFAULT_CREATE_BINDINGS_DIR}.
    */
   boolean isCreateBindingsDir();
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.api.config` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/Configuration.java`
#### Snippet
```java
   /**
    * Returns the maximum number of threads in the thread pool of this server. <br>
    * Default value is {@link org.apache.activemq.artemis.api.config.ActiveMQDefaultConfiguration#DEFAULT_THREAD_POOL_MAX_SIZE}.
    */
   int getThreadPoolMaxSize();
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.api.config` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/Configuration.java`
#### Snippet
```java
   /**
    * Returns the file system directory used to store bindings. <br>
    * Default value is {@link org.apache.activemq.artemis.api.config.ActiveMQDefaultConfiguration#DEFAULT_BINDINGS_DIRECTORY}.
    */
   String getBindingsDirectory();
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.api.config` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/Configuration.java`
#### Snippet
```java
   /**
    * Returns whether the whole page is read while getting message after page cache is evicted. <br>
    * Default value is {@link org.apache.activemq.artemis.api.config.ActiveMQDefaultConfiguration#DEFAULT_READ_WHOLE_PAGE}.
    */
   boolean isReadWholePage();
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.api.config` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/Configuration.java`
#### Snippet
```java
   /**
    * Returns the configured size of the authentication cache. <br>
    * Default value is {@link org.apache.activemq.artemis.api.config.ActiveMQDefaultConfiguration#DEFAULT_AUTHENTICATION_CACHE_SIZE}.
    */
   long getAuthenticationCacheSize();
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.api.config` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/Configuration.java`
#### Snippet
```java
   /**
    * Returns the size of the cache for pre-creating message IDs. <br>
    * Default value is {@link org.apache.activemq.artemis.api.config.ActiveMQDefaultConfiguration#DEFAULT_ID_CACHE_SIZE}.
    */
   int getIDCacheSize();
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.api.config` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/Configuration.java`
#### Snippet
```java
   /**
    * Returns whether wildcard routing is supported by this server. <br>
    * Default value is {@link org.apache.activemq.artemis.api.config.ActiveMQDefaultConfiguration#DEFAULT_WILDCARD_ROUTING_ENABLED}.
    */
   boolean isWildcardRoutingEnabled();
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.api.config` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/Configuration.java`
#### Snippet
```java
   /**
    * Returns the graceful shutdown timeout for this server. <br>
    * Default value is {@link org.apache.activemq.artemis.api.config.ActiveMQDefaultConfiguration#DEFAULT_GRACEFUL_SHUTDOWN_TIMEOUT}.
    */
   long getGracefulShutdownTimeout();
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.api.config` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/Configuration.java`
#### Snippet
```java
    * The max number of concurrent reads allowed on paging.
    * <p>
    * Default value is {@link org.apache.activemq.artemis.api.config.ActiveMQDefaultConfiguration#DEFAULT_MAX_CONCURRENT_PAGE_IO}.
    */
   int getPageMaxConcurrentIO();
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.api.config` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/Configuration.java`
#### Snippet
```java
    * Returns the frequency (in milliseconds) to scan messages to detect which messages have
    * expired. <br>
    * Default value is {@link org.apache.activemq.artemis.api.config.ActiveMQDefaultConfiguration#DEFAULT_MESSAGE_EXPIRY_SCAN_PERIOD}.
    */
   long getMessageExpiryScanPeriod();
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.api.config` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/Configuration.java`
#### Snippet
```java
   /**
    * Returns whether the journal directory is created on this server startup. <br>
    * Default value is {@link org.apache.activemq.artemis.api.config.ActiveMQDefaultConfiguration#DEFAULT_CREATE_JOURNAL_DIR}.
    */
   boolean isCreateJournalDir();
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.api.config` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/Configuration.java`
#### Snippet
```java
   /**
    * Returns the file system directory used to store paging files. <br>
    * Default value is {@link org.apache.activemq.artemis.api.config.ActiveMQDefaultConfiguration#DEFAULT_PAGING_DIR}.
    */
   String getPagingDirectory();
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.api.config` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/Configuration.java`
#### Snippet
```java
   /**
    * Returns the priority of the thread used to scan message expiration. <br>
    * Default value is {@link org.apache.activemq.artemis.api.config.ActiveMQDefaultConfiguration#DEFAULT_MESSAGE_EXPIRY_THREAD_PRIORITY}.
    */
   @Deprecated
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.api.config` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/Configuration.java`
#### Snippet
```java
   /**
    * Returns the interval time (in milliseconds) to invalidate security credentials. <br>
    * Default value is {@link org.apache.activemq.artemis.api.config.ActiveMQDefaultConfiguration#DEFAULT_SECURITY_INVALIDATION_INTERVAL}.
    */
   long getSecurityInvalidationInterval();
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.api.config` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/Configuration.java`
#### Snippet
```java
   /**
    * Returns the size (in bytes) of each journal files. <br>
    * Default value is {@link org.apache.activemq.artemis.api.config.ActiveMQDefaultConfiguration#DEFAULT_JOURNAL_FILE_SIZE}.
    */
   int getJournalFileSize();
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.api.config` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/Configuration.java`
#### Snippet
```java
    * Clients can bind queues to this address to receive management notifications emitted by this
    * server. <br>
    * Default value is {@link org.apache.activemq.artemis.api.config.ActiveMQDefaultConfiguration#DEFAULT_MANAGEMENT_NOTIFICATION_ADDRESS}.
    */
   SimpleString getManagementNotificationAddress();
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.api.config` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/Configuration.java`
#### Snippet
```java
   /**
    * Returns the cluster user for this server. <br>
    * Default value is {@link org.apache.activemq.artemis.api.config.ActiveMQDefaultConfiguration#DEFAULT_CLUSTER_USER}.
    */
   String getClusterUser();
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.api.config` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/Configuration.java`
#### Snippet
```java
   /**
    * Returns the cluster password for this server. <br>
    * Default value is {@link org.apache.activemq.artemis.api.config.ActiveMQDefaultConfiguration#DEFAULT_CLUSTER_PASSWORD}.
    */
   String getClusterPassword();
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.api.config` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/Configuration.java`
#### Snippet
```java
   /**
    * Returns the minimal number of journal files before compacting. <br>
    * Default value is {@link org.apache.activemq.artemis.api.config.ActiveMQDefaultConfiguration#DEFAULT_JOURNAL_COMPACT_MIN_FILES}.
    */
   int getJournalCompactMinFiles();
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.api.config` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/Configuration.java`
#### Snippet
```java
   /**
    * Returns whether this server is manageable using JMX or not. <br>
    * Default value is {@link org.apache.activemq.artemis.api.config.ActiveMQDefaultConfiguration#DEFAULT_JMX_MANAGEMENT_ENABLED}.
    */
   boolean isJMXManagementEnabled();
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.api.config` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/Configuration.java`
#### Snippet
```java
   /**
    * Returns the file system directory used to store journal log. <br>
    * Default value is {@link org.apache.activemq.artemis.api.config.ActiveMQDefaultConfiguration#DEFAULT_JOURNAL_DIR}.
    */
   String getJournalDirectory();
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.api.config` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/Configuration.java`
#### Snippet
```java
    * Returns the frequency (in milliseconds) to scan addresses and queues to detect which
    * ones should be deleted. <br>
    * Default value is {@link org.apache.activemq.artemis.api.config.ActiveMQDefaultConfiguration#DEFAULT_MESSAGE_EXPIRY_SCAN_PERIOD}.
    */
   long getAddressQueueScanPeriod();
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.api.config` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/Configuration.java`
#### Snippet
```java
   /**
    * Returns whether graceful shutdown is enabled for this server. <br>
    * Default value is {@link org.apache.activemq.artemis.api.config.ActiveMQDefaultConfiguration#DEFAULT_GRACEFUL_SHUTDOWN_ENABLED}.
    */
   boolean isGracefulShutdownEnabled();
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.api.config` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/Configuration.java`
#### Snippet
```java
   /**
    * Returns whether the journal is synchronized when receiving transactional data. <br>
    * Default value is {@link org.apache.activemq.artemis.api.config.ActiveMQDefaultConfiguration#DEFAULT_JOURNAL_SYNC_TRANSACTIONAL}.
    */
   boolean isJournalSyncTransactional();
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.api.config` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/Configuration.java`
#### Snippet
```java
   /**
    * Returns the file system directory used to store large messages. <br>
    * Default value is {@link org.apache.activemq.artemis.api.config.ActiveMQDefaultConfiguration#DEFAULT_LARGE_MESSAGES_DIR}.
    */
   String getLargeMessagesDirectory();
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.api.config` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/Configuration.java`
#### Snippet
```java

   /**
    * Number of files that would be acceptable to keep on a pool. Default value is {@link org.apache.activemq.artemis.api.config.ActiveMQDefaultConfiguration#DEFAULT_JOURNAL_POOL_FILES}.
    */
   int getJournalPoolFiles();
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.api.config` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/Configuration.java`
#### Snippet
```java
    * Returns whether delivery count is persisted before messages are delivered to the consumers. <br>
    * Default value is
    * {@link org.apache.activemq.artemis.api.config.ActiveMQDefaultConfiguration#DEFAULT_PERSIST_DELIVERY_COUNT_BEFORE_DELIVERY}
    */
   boolean isPersistDeliveryCountBeforeDelivery();
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.api.config` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/Configuration.java`
#### Snippet
```java
   /**
    * Returns the period (in milliseconds) to scan configuration files used by deployment. <br>
    * Default value is {@link org.apache.activemq.artemis.api.config.ActiveMQDefaultConfiguration#DEFAULT_FILE_DEPLOYER_SCAN_PERIOD}.
    */
   long getFileDeployerScanPeriod();
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.api.config` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/Configuration.java`
#### Snippet
```java
   /**
    * Sets whether this server is manageable using JMX or not. <br>
    * Default value is {@link org.apache.activemq.artemis.api.config.ActiveMQDefaultConfiguration#DEFAULT_JMX_MANAGEMENT_ENABLED}.
    */
   Configuration setJMXManagementEnabled(boolean enabled);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.api.config` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/Configuration.java`
#### Snippet
```java
   /**
    * Returns whether message counter is enabled for this server. <br>
    * Default value is {@link org.apache.activemq.artemis.api.config.ActiveMQDefaultConfiguration#DEFAULT_MESSAGE_COUNTER_ENABLED}.
    */
   boolean isMessageCounterEnabled();
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.api.config` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/Configuration.java`
#### Snippet
```java
   /**
    * Returns the maximum number of threads in the <em>scheduled</em> thread pool of this server. <br>
    * Default value is {@link org.apache.activemq.artemis.api.config.ActiveMQDefaultConfiguration#DEFAULT_SCHEDULED_THREAD_POOL_MAX_SIZE}.
    */
   int getScheduledThreadPoolMaxSize();
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.api.config` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/Configuration.java`
#### Snippet
```java
    * Returns whether code coming from connection is executed asynchronously or not. <br>
    * Default value is
    * {@link org.apache.activemq.artemis.api.config.ActiveMQDefaultConfiguration#DEFAULT_ASYNC_CONNECTION_EXECUTION_ENABLED}.
    */
   @Deprecated
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.api.config` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/Configuration.java`
#### Snippet
```java
   /**
    * Returns whether security is enabled for this server. <br>
    * Default value is {@link org.apache.activemq.artemis.api.config.ActiveMQDefaultConfiguration#DEFAULT_SECURITY_ENABLED}.
    */
   boolean isSecurityEnabled();
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.api.config` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/Configuration.java`
#### Snippet
```java
   /**
    * Returns the configured size of the authorization cache. <br>
    * Default value is {@link org.apache.activemq.artemis.api.config.ActiveMQDefaultConfiguration#DEFAULT_AUTHORIZATION_CACHE_SIZE}.
    */
   long getAuthorizationCacheSize();
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.api.config` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/Configuration.java`
#### Snippet
```java
   /**
    * Returns the percentage of live data before compacting the journal. <br>
    * Default value is {@link org.apache.activemq.artemis.api.config.ActiveMQDefaultConfiguration#DEFAULT_JOURNAL_COMPACT_PERCENTAGE}.
    */
   int getJournalCompactPercentage();
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.api.config` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/Configuration.java`
#### Snippet
```java
   /**
    * Returns the number of journal files to pre-create. <br>
    * Default value is {@link org.apache.activemq.artemis.api.config.ActiveMQDefaultConfiguration#DEFAULT_JOURNAL_MIN_FILES}.
    */
   int getJournalMinFiles();
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.api.config` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/Configuration.java`
#### Snippet
```java
    * Returns the management address of this server. <br>
    * Clients can send management messages to this address to manage this server. <br>
    * Default value is {@link org.apache.activemq.artemis.api.config.ActiveMQDefaultConfiguration#DEFAULT_MANAGEMENT_ADDRESS}.
    */
   SimpleString getManagementAddress();
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.api.config` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/Configuration.java`
#### Snippet
```java
   /**
    * Returns whether message ID cache is persisted. <br>
    * Default value is {@link org.apache.activemq.artemis.api.config.ActiveMQDefaultConfiguration#DEFAULT_PERSIST_ID_CACHE}.
    */
   boolean isPersistIDCache();
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.core.remoting.impl.invm` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/remoting/impl/invm/InVMConnection.java`
#### Snippet
```java
      Map<String, Object> params = new HashMap<>();

      params.put(org.apache.activemq.artemis.core.remoting.impl.invm.TransportConstants.SERVER_ID_PROP_NAME, serverID);

      return new TransportConfiguration(InVMConnectorFactory.class.getName(), params);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.core.remoting.impl.netty` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/remoting/server/impl/RemotingServiceImpl.java`
#### Snippet
```java

   private ClusterConnection lookupClusterConnection(TransportConfiguration acceptorConfig) {
      String clusterConnectionName = (String) acceptorConfig.getParams().get(org.apache.activemq.artemis.core.remoting.impl.netty.TransportConstants.CLUSTER_CONNECTION);

      ClusterConnection clusterConnection = null;
```

### UnnecessaryFullyQualifiedName
Qualifier `io.netty.util.concurrent` is unnecessary, and can be replaced with an import
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/NettyAcceptor.java`
#### Snippet
```java
               sslHandler.handshakeFuture().addListener(new GenericFutureListener<io.netty.util.concurrent.Future<Channel>>() {
                  @Override
                  public void operationComplete(final io.netty.util.concurrent.Future<Channel> future) throws Exception {
                     if (future.isSuccess()) {
                        active = true;
```

### UnnecessaryFullyQualifiedName
Qualifier `io.netty.util.concurrent` is unnecessary, and can be replaced with an import
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/NettyAcceptor.java`
#### Snippet
```java
            SslHandler sslHandler = ctx.pipeline().get(SslHandler.class);
            if (sslHandler != null) {
               sslHandler.handshakeFuture().addListener(new GenericFutureListener<io.netty.util.concurrent.Future<Channel>>() {
                  @Override
                  public void operationComplete(final io.netty.util.concurrent.Future<Channel> future) throws Exception {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary, and can be replaced with an import
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/LargeServerMessageImpl.java`
#### Snippet
```java
         return "0";
      } else {
         return new java.util.Date(timestamp).toString();
      }
   }
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.core.replication` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/StorageManager.java`
#### Snippet
```java

   /**
    * @see org.apache.activemq.artemis.core.persistence.impl.journal.JournalStorageManager#startReplication(org.apache.activemq.artemis.core.replication.ReplicationManager, org.apache.activemq.artemis.core.paging.PagingManager, String, boolean, long)
    */
   void startReplication(ReplicationManager replicationManager,
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.core.paging` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/StorageManager.java`
#### Snippet
```java

   /**
    * @see org.apache.activemq.artemis.core.persistence.impl.journal.JournalStorageManager#startReplication(org.apache.activemq.artemis.core.replication.ReplicationManager, org.apache.activemq.artemis.core.paging.PagingManager, String, boolean, long)
    */
   void startReplication(ReplicationManager replicationManager,
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.core.journal` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/replication/ReplicatedJournal.java`
#### Snippet
```java
    * @param record
    * @throws Exception
    * @see org.apache.activemq.artemis.core.journal.Journal#appendDeleteRecordTransactional(long, long, byte[])
    */
   @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.core.journal` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/replication/ReplicatedJournal.java`
#### Snippet
```java
    * @param sync
    * @throws Exception
    * @see org.apache.activemq.artemis.core.journal.Journal#appendUpdateRecord(long, byte, byte[], boolean)
    */
   @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.core.journal` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/replication/ReplicatedJournal.java`
#### Snippet
```java
    * @param record
    * @throws Exception
    * @see org.apache.activemq.artemis.core.journal.Journal#appendUpdateRecordTransactional(long, long, byte, byte[])
    */
   @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.core.journal` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/replication/ReplicatedJournal.java`
#### Snippet
```java
    * @param transactionFailure
    * @throws Exception
    * @see org.apache.activemq.artemis.core.journal.Journal#load(java.util.List, java.util.List, org.apache.activemq.artemis.core.journal.TransactionFailureCallback)
    */
   @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/replication/ReplicatedJournal.java`
#### Snippet
```java
    * @param transactionFailure
    * @throws Exception
    * @see org.apache.activemq.artemis.core.journal.Journal#load(java.util.List, java.util.List, org.apache.activemq.artemis.core.journal.TransactionFailureCallback)
    */
   @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/replication/ReplicatedJournal.java`
#### Snippet
```java
    * @param transactionFailure
    * @throws Exception
    * @see org.apache.activemq.artemis.core.journal.Journal#load(java.util.List, java.util.List, org.apache.activemq.artemis.core.journal.TransactionFailureCallback)
    */
   @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.core.journal` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/replication/ReplicatedJournal.java`
#### Snippet
```java
    * @param transactionFailure
    * @throws Exception
    * @see org.apache.activemq.artemis.core.journal.Journal#load(java.util.List, java.util.List, org.apache.activemq.artemis.core.journal.TransactionFailureCallback)
    */
   @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.core.journal` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/replication/ReplicatedJournal.java`
#### Snippet
```java
    * @param sync
    * @throws Exception
    * @see org.apache.activemq.artemis.core.journal.Journal#appendDeleteRecord(long, boolean)
    */
   @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.core.journal` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/replication/ReplicatedJournal.java`
#### Snippet
```java
    * @param record
    * @throws Exception
    * @see org.apache.activemq.artemis.core.journal.Journal#appendDeleteRecordTransactional(long, long, org.apache.activemq.artemis.core.journal.EncodingSupport)
    */
   @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.core.journal` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/replication/ReplicatedJournal.java`
#### Snippet
```java
    * @param record
    * @throws Exception
    * @see org.apache.activemq.artemis.core.journal.Journal#appendDeleteRecordTransactional(long, long, org.apache.activemq.artemis.core.journal.EncodingSupport)
    */
   @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.core.journal` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/replication/ReplicatedJournal.java`
#### Snippet
```java
    * @param sync
    * @throws Exception
    * @see org.apache.activemq.artemis.core.journal.Journal#appendCommitRecord(long, boolean)
    */
   @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.core.journal` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/replication/ReplicatedJournal.java`
#### Snippet
```java
    * @param record
    * @throws Exception
    * @see org.apache.activemq.artemis.core.journal.Journal#appendUpdateRecordTransactional(long, long, byte, org.apache.activemq.artemis.core.journal.EncodingSupport)
    */
   @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.core.journal` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/replication/ReplicatedJournal.java`
#### Snippet
```java
    * @param record
    * @throws Exception
    * @see org.apache.activemq.artemis.core.journal.Journal#appendUpdateRecordTransactional(long, long, byte, org.apache.activemq.artemis.core.journal.EncodingSupport)
    */
   @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.core.journal` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/replication/ReplicatedJournal.java`
#### Snippet
```java
    * @param sync
    * @throws Exception
    * @see org.apache.activemq.artemis.core.journal.Journal#appendPrepareRecord(long, org.apache.activemq.artemis.core.journal.EncodingSupport, boolean)
    */
   @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.core.journal` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/replication/ReplicatedJournal.java`
#### Snippet
```java
    * @param sync
    * @throws Exception
    * @see org.apache.activemq.artemis.core.journal.Journal#appendPrepareRecord(long, org.apache.activemq.artemis.core.journal.EncodingSupport, boolean)
    */
   @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.core.journal` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/replication/ReplicatedJournal.java`
#### Snippet
```java
    * @param sync
    * @throws Exception
    * @see org.apache.activemq.artemis.core.journal.Journal#appendRollbackRecord(long, boolean)
    */
   @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.core.journal` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/replication/ReplicatedJournal.java`
#### Snippet
```java
    * @param sync
    * @throws Exception
    * @see org.apache.activemq.artemis.core.journal.Journal#appendAddRecord(long, byte, org.apache.activemq.artemis.core.journal.EncodingSupport, boolean)
    */
   @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.core.journal` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/replication/ReplicatedJournal.java`
#### Snippet
```java
    * @param sync
    * @throws Exception
    * @see org.apache.activemq.artemis.core.journal.Journal#appendAddRecord(long, byte, org.apache.activemq.artemis.core.journal.EncodingSupport, boolean)
    */
   @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.core.journal` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/replication/ReplicatedJournal.java`
#### Snippet
```java
    * @param sync
    * @throws Exception
    * @see org.apache.activemq.artemis.core.journal.Journal#appendDeleteRecord(long, boolean)
    */
   @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.core.journal` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/replication/ReplicatedJournal.java`
#### Snippet
```java
    * @param record
    * @throws Exception
    * @see org.apache.activemq.artemis.core.journal.Journal#appendAddRecordTransactional(long, long, byte, org.apache.activemq.artemis.core.journal.EncodingSupport)
    */
   @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.core.journal` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/replication/ReplicatedJournal.java`
#### Snippet
```java
    * @param record
    * @throws Exception
    * @see org.apache.activemq.artemis.core.journal.Journal#appendAddRecordTransactional(long, long, byte, org.apache.activemq.artemis.core.journal.EncodingSupport)
    */
   @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.core.journal` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/replication/ReplicatedJournal.java`
#### Snippet
```java
    * @param record
    * @throws Exception
    * @see org.apache.activemq.artemis.core.journal.Journal#appendAddRecordTransactional(long, long, byte, byte[])
    */
   @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.core.journal` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/replication/ReplicatedJournal.java`
#### Snippet
```java
    * @param sync
    * @throws Exception
    * @see org.apache.activemq.artemis.core.journal.Journal#appendPrepareRecord(long, byte[], boolean)
    */
   @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.core.journal` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/replication/ReplicatedJournal.java`
#### Snippet
```java
    * @param sync
    * @throws Exception
    * @see org.apache.activemq.artemis.core.journal.Journal#appendAddRecord(long, byte, byte[], boolean)
    */
   @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.core.journal` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/replication/ReplicatedJournal.java`
#### Snippet
```java
    * @param id
    * @throws Exception
    * @see org.apache.activemq.artemis.core.journal.Journal#appendDeleteRecordTransactional(long, long)
    */
   @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.core.journal` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/replication/ReplicatedJournal.java`
#### Snippet
```java
    * @param reloadManager
    * @throws Exception
    * @see org.apache.activemq.artemis.core.journal.Journal#load(org.apache.activemq.artemis.core.journal.LoaderCallback)
    */
   @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.core.journal` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/replication/ReplicatedJournal.java`
#### Snippet
```java
    * @param reloadManager
    * @throws Exception
    * @see org.apache.activemq.artemis.core.journal.Journal#load(org.apache.activemq.artemis.core.journal.LoaderCallback)
    */
   @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.core.journal` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/replication/ReplicatedJournal.java`
#### Snippet
```java
    * @param transactionFailure
    * @throws Exception
    * @see org.apache.activemq.artemis.core.journal.Journal#load(java.util.List, java.util.List, org.apache.activemq.artemis.core.journal.TransactionFailureCallback)
    */
   @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/replication/ReplicatedJournal.java`
#### Snippet
```java
    * @param transactionFailure
    * @throws Exception
    * @see org.apache.activemq.artemis.core.journal.Journal#load(java.util.List, java.util.List, org.apache.activemq.artemis.core.journal.TransactionFailureCallback)
    */
   @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/replication/ReplicatedJournal.java`
#### Snippet
```java
    * @param transactionFailure
    * @throws Exception
    * @see org.apache.activemq.artemis.core.journal.Journal#load(java.util.List, java.util.List, org.apache.activemq.artemis.core.journal.TransactionFailureCallback)
    */
   @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.core.journal` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/replication/ReplicatedJournal.java`
#### Snippet
```java
    * @param transactionFailure
    * @throws Exception
    * @see org.apache.activemq.artemis.core.journal.Journal#load(java.util.List, java.util.List, org.apache.activemq.artemis.core.journal.TransactionFailureCallback)
    */
   @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.core.journal` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/replication/ReplicatedJournal.java`
#### Snippet
```java
    * @param sync
    * @throws Exception
    * @see org.apache.activemq.artemis.core.journal.Journal#appendUpdateRecord(long, byte, org.apache.activemq.artemis.core.journal.EncodingSupport, boolean)
    */
   @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.activemq.artemis.core.journal` is unnecessary and can be removed
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/replication/ReplicatedJournal.java`
#### Snippet
```java
    * @param sync
    * @throws Exception
    * @see org.apache.activemq.artemis.core.journal.Journal#appendUpdateRecord(long, byte, org.apache.activemq.artemis.core.journal.EncodingSupport, boolean)
    */
   @Override
```

## RuleId[id=ThrowablePrintStackTrace]
### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `artemis-maven-plugin/src/main/java/org/apache/activemq/artemis/maven/ArtemisCreatePlugin.java`
#### Snippet
```java
         outputStream = new FileOutputStream(commandLine);
      } catch (Exception e) {
         e.printStackTrace();
         throw new MojoExecutionException(e.getMessage(), e);
      }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `artemis-maven-plugin/src/main/java/org/apache/activemq/artemis/maven/ArtemisDependencyDocPlugin.java`
#### Snippet
```java
            } catch (Throwable e) {
               System.out.println("Error on " + entry.getKey());
               e.printStackTrace();
            }
         }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `artemis-log-annotation-processor/src/main/java/org/apache/activemq/artemis/logs/annotation/processor/LogAnnotationProcessor.java`
#### Snippet
```java
         }
      } catch (Exception e) {
         e.printStackTrace();
      }
      DEBUG = debugResult;
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `artemis-log-annotation-processor/src/main/java/org/apache/activemq/artemis/logs/annotation/processor/LogAnnotationProcessor.java`
#### Snippet
```java
         }
      } catch (Exception e) {
         e.printStackTrace();
         processingEnv.getMessager().printMessage(Diagnostic.Kind.ERROR, e.getMessage());
         return false;
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `artemis-boot/src/main/java/org/apache/activemq/artemis/boot/Artemis.java`
#### Snippet
```java
         urls.add(file.toURI().toURL());
      } catch (MalformedURLException e) {
         e.printStackTrace();
      }
   }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/SpawnedVMSupport.java`
#### Snippet
```java
               break;
            } catch (Throwable e) {
               e.printStackTrace();
            }
         }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/SpawnedVMSupport.java`
#### Snippet
```java
            }
         } catch (IOException ioe) {
            ioe.printStackTrace();
         }
      }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
         success = true;
      } catch (java.io.IOException exc) {
         exc.printStackTrace();
      } finally {
         try {
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/Base64.java`
#### Snippet
```java
         success = true;
      } catch (java.io.IOException exc) {
         exc.printStackTrace();
      } finally {
         try {
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/InputAbstract.java`
#### Snippet
```java
            value = Integer.parseInt(input);
         } catch (NumberFormatException e) {
            e.printStackTrace();
            value = null;
         }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Run.java`
#### Snippet
```java
         }
      } catch (Throwable t) {
         t.printStackTrace();
         serverActivationFailed.set(t);
         latchRunning.countDown();
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Run.java`
#### Snippet
```java
         }
      } catch (Exception e) {
         e.printStackTrace();
      }
   }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Configurable.java`
#### Snippet
```java

      if (!(e instanceof ActiveMQException)) {
         e.printStackTrace();
      }
      helpGroup(group, command);
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/Artemis.java`
#### Snippet
```java
            }
         } catch (Exception e) {
            e.printStackTrace();
         }
      }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/Artemis.java`
#### Snippet
```java
         // this is the CLI and System.out and System.err are common places for interacting with the user
         // this is a programming error that must be visualized and corrected
         e.printStackTrace();
         return e;
      } catch (RuntimeException | InvalidOptionsError re) {
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/util/HashUtil.java`
#### Snippet
```java
         context.err.println("Warning: Failed to calculate hash value for password using " + HASH_PROCESSOR);
         context.err.println("Reason: " + e.getMessage());
         e.printStackTrace();
      }
      return plaintext;
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/InstallAbstract.java`
#### Snippet
```java

      if (content == null) {
         new Exception(source + " not found").printStackTrace();
      }

```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/RecoverMessages.java`
#### Snippet
```java
                  }
               } catch (Exception e) {
                  e.printStackTrace();
               }
            }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/xml/XMLMessageExporter.java`
#### Snippet
```java
         encoder.close();
      } catch (ActiveMQException e) {
         e.printStackTrace();
      } finally {
         if (encoder != null) {
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/xml/XMLMessageExporter.java`
#### Snippet
```java
               encoder.close();
            } catch (ActiveMQException e) {
               e.printStackTrace();
            }
         }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/xml/XmlDataExporter.java`
#### Snippet
```java
         xmlWriter.close();
      } catch (Exception e) {
         e.printStackTrace();
      }
   }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/xml/XmlDataExporter.java`
#### Snippet
```java
         }
      } catch (Exception e) {
         e.printStackTrace();
      }
   }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Create.java`
#### Snippet
```java
            RoutingType.valueOf(routingType.toUpperCase());
         } catch (Exception e) {
            e.printStackTrace();
            System.err.println("Invalid routing type: " + routingType);
         }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/Create.java`
#### Snippet
```java
            filters.put("${journal-buffer.settings}", "");
            filters.put("${page-sync.settings}", "");
            e.printStackTrace();
            System.err.println("Couldn't perform sync calculation, using default values");
         }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/PrintData.java`
#### Snippet
```java
         return DescribeJournal.describeBindingsJournal(bindingsDirectory, out, safe, printRecords, printSurviving, reclaimed);
      } catch (Exception e) {
         e.printStackTrace();
         return null;
      }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/PrintData.java`
#### Snippet
```java
         describeJournal = DescribeJournal.describeMessagesJournal(messagesDirectory, out, safe, printRecords, printSurviving, reclaimed);
      } catch (Exception e) {
         e.printStackTrace();
         return null;
      }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/PrintData.java`
#### Snippet
```java
         printPages(describeJournal, sm, manager, out, safe, maxPages, bindingsDescribe);
      } catch (Exception e) {
         e.printStackTrace();
      } finally {
         executor.shutdownNow();
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/PrintData.java`
#### Snippet
```java
            fileLock.stop();
         } catch (Exception e) {
            e.printStackTrace();
         }
      }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/PrintData.java`
#### Snippet
```java
         }
      } catch (Exception e) {
         e.printStackTrace();
         return;
      }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/Producer.java`
#### Snippet
```java
               System.err.println("Error occurred during import.  Rolling back.");
               session.rollback();
               e.printStackTrace();
               return 0;
            }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/ProducerThread.java`
#### Snippet
```java

      } catch (Exception e) {
         e.printStackTrace();
      } finally {
         if (finished != null) {
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/ProducerThread.java`
#### Snippet
```java
               producer.close();
            } catch (JMSException e) {
               e.printStackTrace();
            }
         }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/ConsumerThread.java`
#### Snippet
```java
         consumer.close();
      } catch (Exception e) {
         e.printStackTrace();
      } finally {
         if (finished != null) {
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/ConsumerThread.java`
#### Snippet
```java
               consumer.close();
            } catch (JMSException e) {
               e.printStackTrace();
            }
         }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/ConsumerThread.java`
#### Snippet
```java

      } catch (Exception e) {
         e.printStackTrace();
      } finally {
         if (finished != null) {
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/ConsumerThread.java`
#### Snippet
```java
               consumer.close();
            } catch (JMSException e) {
               e.printStackTrace();
            }
         }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `artemis-unit-test-support/src/main/java/org/apache/activemq/artemis/utils/PortCheckRule.java`
#### Snippet
```java
         return true;
      } catch (IOException e) {
         e.printStackTrace();
         return false;
      } finally {
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `artemis-unit-test-support/src/main/java/org/apache/activemq/artemis/utils/ThreadLeakCheckRule.java`
#### Snippet
```java
                  System.out.println("             The test failed and there is a leak");
                  System.out.println("***********************************************************************");
                  failure.printStackTrace();
                  Assert.fail("Test " + testDescription + " Failed with a leak - " + failure.getMessage());
               }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jndi/ActiveMQInitialContextFactory.java`
#### Snippet
```java
            }
         } catch (Exception e) {
            e.printStackTrace();
            throw new NamingException("Invalid broker URL");
         }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jndi/ActiveMQInitialContextFactory.java`
#### Snippet
```java
               data.put(jndiName, createConnectionFactory((String) environment.get(key), jndiName));
            } catch (Exception e) {
               e.printStackTrace();
               throw new NamingException("Invalid broker URL");
            }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `artemis-jdbc-store/src/main/java/org/apache/activemq/artemis/jdbc/store/file/JDBCSequentialFileFactoryDriver.java`
#### Snippet
```java
         }
      } catch (NullPointerException npe) {
         npe.printStackTrace();
         throw npe;
      }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/message/impl/CoreMessage.java`
#### Snippet
```java
            }
         } catch (Exception e) {
            e.printStackTrace();
         }
      }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/security/jaas/LDAPLoginModule.java`
#### Snippet
```java
                  brokerGssapiIdentity = loginContext.getSubject();
               } catch (LoginException e) {
                  e.printStackTrace();
                  FailedLoginException ex = new FailedLoginException("Error contacting LDAP using GSSAPI in JAAS loginConfigScope: " + configScope);
                  ex.initCause(e);
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/SharedNothingLiveActivation.java`
#### Snippet
```java
                                 startThread.join();
                              } catch (Exception e) {
                                 e.printStackTrace();
                              }
                           }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerConnectVoteHandler.java`
#### Snippet
```java
         ActiveMQServerLogger.LOGGER.nodeNotFoundInClusterTopology(nodeid);
      } catch (Exception e) {
         e.printStackTrace();
      }
      return new ServerConnectVote(nodeid, !((Boolean) vote.getVote()), null);
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/protocol/websocket/WebSocketServerHandler.java`
#### Snippet
```java
   @Override
   public void exceptionCaught(ChannelHandlerContext ctx, Throwable cause) throws Exception {
      cause.printStackTrace();
      ctx.close();
   }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/ManagementContext.java`
#### Snippet
```java
         return new HawtioSecurityControlImpl(guardHandler, storageManager);
      } catch (NotCompliantMBeanException e) {
         e.printStackTrace();
         return null;
      }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/AddressControlImpl.java`
#### Snippet
```java
            return sendMessage(addressInfo.getName(), server, headers, type, body, durable, user, password, createMessageId);
         } catch (Exception e) {
            e.printStackTrace();
            throw new IllegalStateException(e.getMessage());
         }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/QueueControlImpl.java`
#### Snippet
```java
               AuditLogger.resumeQueueFailure(queue.getName().toString());
            }
            e.printStackTrace();
         }
      } finally {
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/nullpm/NullStorageManager.java`
#### Snippet
```java
         @Override
         public void onIOException(Throwable code, String message, String file) {
            code.printStackTrace();
         }
      });
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/LargeServerMessageImpl.java`
#### Snippet
```java
         return "LargeServerMessage[messageID=" + messageID + ",durable=" + isDurable() + ",userID=" + getUserID() + ",priority=" + this.getPriority() + ", timestamp=" + toDate(getTimestamp()) + ",expiration=" + toDate(getExpiration()) + ", durable=" + durable + ", address=" + getAddress() + ", properties=" + (properties != null ? properties.toString() : "") + "]@" + System.identityHashCode(this);
      } catch (Exception e) {
         e.printStackTrace();
         return "LargeServerMessage[messageID=" + messageID + "]";
      }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/DescribeJournal.java`
#### Snippet
```java
               return describe.getMsg().getClass().getSimpleName() + "(safe data, size=" + describe.getMsg().getPersistentSize() + ")";
            } catch (Throwable e) {
               e.printStackTrace();
               return describe.getMsg().getClass().getSimpleName() + "(safe data)";
            }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/postoffice/impl/PostOfficeImpl.java`
#### Snippet
```java
               }
            } catch (Exception e) {
               e.printStackTrace();
            }
         }
```

## RuleId[id=ReplaceNullCheck]
### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `artemis-protocols/artemis-stomp-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/stomp/StompUtils.java`
#### Snippet
```java

      String priority = headers.remove(Stomp.Headers.Send.PRIORITY);
      if (priority != null) {
         msg.setPriority(Byte.parseByte(priority));
      } else {
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElseGet()'
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/collections/TypedProperties.java`
#### Snippet
```java
         final int index = (int) b + offset;
         ByteValue value = VALUES[index];
         if (value == null) {
            return VALUES[index] = new ByteValue(b);
         } else {
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElseGet()'
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/ActiveMQRASessionFactoryImpl.java`
#### Snippet
```java
      this.tsr = tsr;

      if (cm == null) {
         this.cm = new ActiveMQRAConnectionManager();
      } else {
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/ActiveMQResourceAdapter.java`
#### Snippet
```java
      }
      val2 = overrideProperties.getReconnectAttempts() != null ? overrideProperties.getReconnectAttempts() : raProperties.getReconnectAttempts();
      if (val2 != null) {
         cf.setReconnectAttempts(val2);
      } else {
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElseGet()'
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/inflow/ActiveMQActivationSpec.java`
#### Snippet
```java
      logger.trace("getSetupInterval()");

      if (setupInterval == null) {
         return ra.getSetupInterval();
      } else {
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElseGet()'
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/inflow/ActiveMQActivationSpec.java`
#### Snippet
```java
      logger.trace("getSetupAttempts()");

      if (setupAttempts == null) {
         return ra.getSetupAttempts();
      } else {
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElseGet()'
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/messages/ProducerThread.java`
#### Snippet
```java
         } else if (payloadUrl != null) {
            messageText = readInputStream(new URL(payloadUrl).openStream(), -1, i);
         } else if (message != null) {
            messageText = message;
         } else {
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElseGet()'
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/connect/mirror/ReferenceNodeStore.java`
#### Snippet
```java
      Message message = element.getMessage();
      Long id = getID(message);
      if (id == null) {
         return element.getMessageID();
      } else {
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ActiveMQServerImpl.java`
#### Snippet
```java
         }

         if (file == null) {
            ActiveMQServerLogger.LOGGER.ioCriticalIOError(message, "NULL", cause);
         } else {
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElseGet()'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java
      Transaction tx;

      if (originalTX != null) {
         tx = originalTX;
      } else {
```

## RuleId[id=Anonymous2MethodRef]
### Anonymous2MethodRef
Anonymous new ArtemisExecutor() can be replaced with method reference
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/actors/ArtemisExecutor.java`
#### Snippet
```java
    */
   static ArtemisExecutor delegate(Executor executor) {
      return new ArtemisExecutor() {
         @Override
         public void execute(Runnable command) {
```

### Anonymous2MethodRef
Anonymous new Runnable() can be replaced with method reference
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/actors/ArtemisExecutor.java`
#### Snippet
```java
   default boolean isFlushed() {
      CountDownLatch latch = new CountDownLatch(1);
      Runnable runnable = new Runnable() {
         @Override
         public void run() {
```

### Anonymous2MethodRef
Anonymous new PrivilegedExceptionAction() can be replaced with method reference
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/inflow/ActiveMQActivation.java`
#### Snippet
```java

      try {
         tccl = AccessController.doPrivileged(new PrivilegedExceptionAction<ClassLoader>() {
            @Override
            public ClassLoader run() {
```

### Anonymous2MethodRef
Anonymous new ThreadFactory() can be replaced with method reference
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/DBOption.java`
#### Snippet
```java
      executorFactory = new OrderedExecutorFactory(executor);

      scheduledExecutorService = new ScheduledThreadPoolExecutor(configuration.getScheduledThreadPoolMaxSize(), new ThreadFactory() {
         @Override
         public Thread newThread(Runnable r) {
```

### Anonymous2MethodRef
Anonymous new Runnable() can be replaced with method reference
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalImpl.java`
#### Snippet
```java

         try {
            executor.execute(new Runnable() {

               @Override
```

### Anonymous2MethodRef
Anonymous new Runnable() can be replaced with method reference
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ClientConsumerImpl.java`
#### Snippet
```java

      //execute the future after the last onMessage call
      sessionExecutor.execute(new Runnable() {
         @Override
         public void run() {
```

### Anonymous2MethodRef
Anonymous new Runnable() can be replaced with method reference
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ClientConsumerImpl.java`
#### Snippet
```java
         // If resetting a slow consumer, we need to wait the execution
         final CountDownLatch latch = new CountDownLatch(1);
         flowControlExecutor.execute(new Runnable() {
            @Override
            public void run() {
```

### Anonymous2MethodRef
Anonymous new Runnable() can be replaced with method reference
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/impl/PageSyncTimer.java`
#### Snippet
```java
   private final long timeSync;

   private final Runnable runnable = new Runnable() {
      @Override
      public void run() {
```

### Anonymous2MethodRef
Anonymous new Runnable() can be replaced with method reference
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/SharedNothingLiveActivation.java`
#### Snippet
```java
         // Schedule for 10 seconds
         // this pool gets a 'hard' shutdown, no need to manage the Future of this Runnable.
         activeMQServer.getScheduledPool().schedule(new Runnable() {
            @Override
            public void run() {
```

### Anonymous2MethodRef
Anonymous new Runnable() can be replaced with method reference
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/BackupManager.java`
#### Snippet
```java
      /** it will re-schedule the connection after a timeout, using a scheduled executor */
      protected void retryConnection() {
         scheduledExecutor.schedule(new Runnable() {
            @Override
            public void run() {
```

### Anonymous2MethodRef
Anonymous new Runnable() can be replaced with method reference
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/remoting/impl/invm/InVMConnection.java`
#### Snippet
```java
         if (flush && flushEnabled) {
            final CountDownLatch latch = new CountDownLatch(1);
            executor.execute(new Runnable() {
               @Override
               public void run() {
```

### Anonymous2MethodRef
Anonymous new Runnable() can be replaced with method reference
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/JDBCJournalStorageManager.java`
#### Snippet
```java

      final CountDownLatch latch = new CountDownLatch(1);
      executor.execute(new Runnable() {
         @Override
         public void run() {
```

### Anonymous2MethodRef
Anonymous new Runnable() can be replaced with method reference
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/JournalStorageManager.java`
#### Snippet
```java
      final CountDownLatch latch = new CountDownLatch(1);
      try {
         executor.execute(new Runnable() {
            @Override
            public void run() {
```

### Anonymous2MethodRef
Anonymous new Runnable() can be replaced with method reference
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/AbstractJournalStorageManager.java`
#### Snippet
```java

      final CountDownLatch latch = new CountDownLatch(1);
      executor.execute(new Runnable() {
         @Override
         public void run() {
```

### Anonymous2MethodRef
Anonymous new Runnable() can be replaced with method reference
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java
   @Override
   public void close() throws Exception {
      getExecutor().execute(new Runnable() {
         @Override
         public void run() {
```

## RuleId[id=Java8MapApi]
### Java8MapApi
Can be replaced with single 'Map.computeIfAbsent' method call
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/xml/XmlDataExporter.java`
#### Snippet
```java
            Set<PagePosition> set = cursorRecords.get(encoding.queueID);

            if (set == null) {
               set = new HashSet<>();
               cursorRecords.put(encoding.queueID, set);
```

### Java8MapApi
Can be replaced with single 'Map.computeIfAbsent' method call
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/PrintData.java`
#### Snippet
```java
            Set<PagePosition> set = cursorInfo.getCursorRecords().get(encoding.queueID);

            if (set == null) {
               set = new HashSet<>();
               cursorInfo.getCursorRecords().put(encoding.queueID, set);
```

### Java8MapApi
Can be replaced with single 'Map.computeIfAbsent' method call
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/PrintData.java`
#### Snippet
```java
         Set<Long> completePagesSet = completePages.get(queueID);

         if (completePagesSet == null) {
            completePagesSet = new HashSet<>();
            completePages.put(queueID, completePagesSet);
```

### Java8MapApi
Can be replaced with single 'Map.computeIfAbsent' method call
in `artemis-jms-server/src/main/java/org/apache/activemq/artemis/jms/server/impl/JMSServerManagerImpl.java`
#### Snippet
```java
   private void addToBindings(Map<String, List<String>> map, String name, String... bindings) {
      List<String> list = map.get(name);
      if (list == null) {
         list = new ArrayList<>();
         map.put(name, list);
```

### Java8MapApi
Can be replaced with single 'Map.computeIfAbsent' method call
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/security/ActiveMQBasicSecurityManager.java`
#### Snippet
```java
         for (String user : ((String) val.getValue()).split(",")) {
            Set<String> tempRoles = invertedProps.get(user);
            if (tempRoles == null) {
               tempRoles = new HashSet<>();
               invertedProps.put(user, tempRoles);
```

### Java8MapApi
Can be replaced with single 'Map.computeIfAbsent' method call
in `artemis-server/src/main/java/org/apache/activemq/artemis/spi/core/security/jaas/ReloadableProperties.java`
#### Snippet
```java
            for (String user : userList) {
               Set<String> set = invertedValueProps.get(user);
               if (set == null) {
                  set = new HashSet<>();
                  invertedValueProps.put(user, set);
```

### Java8MapApi
Can be replaced with single 'Map.computeIfAbsent' method call
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/impl/SecurityConfiguration.java`
#### Snippet
```java

   public void addRole(final String user, final String role) {
      if (roles.get(user) == null) {
         roles.put(user, new ArrayList<String>());
      }
```

### Java8MapApi
Can be replaced with single 'Map.computeIfAbsent' method call
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/cursor/impl/PageSubscriptionImpl.java`
#### Snippet
```java
         List<PagePosition> list = pendingPositions.get(cursor);

         if (list == null) {
            list = new LinkedList<>();
            pendingPositions.put(cursor, list);
```

### Java8MapApi
Can be replaced with single 'Map.computeIfAbsent' method call
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ScheduledDeliveryHandlerImpl.java`
#### Snippet
```java
               LinkedList<MessageReference> references = refs.get(reference.getQueue());

               if (references == null) {
                  references = new LinkedList<>();
                  refs.put(reference.getQueue(), references);
```

### Java8MapApi
Can be replaced with single 'Map.computeIfAbsent' method call
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/PostOfficeJournalLoader.java`
#### Snippet
```java
         Map<Long, Map<Long, List<PageCountPending>>> perPageMap = perAddressMap.get(address);

         if (perPageMap == null) {
            perPageMap = new HashMap<>();
            perAddressMap.put(address, perPageMap);
```

### Java8MapApi
Can be replaced with single 'Map.computeIfAbsent' method call
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/PostOfficeJournalLoader.java`
#### Snippet
```java
         Map<Long, List<PageCountPending>> perQueueMap = perPageMap.get(pageID);

         if (perQueueMap == null) {
            perQueueMap = new HashMap<>();
            perPageMap.put(pageID, perQueueMap);
```

### Java8MapApi
Can be replaced with single 'Map.computeIfAbsent' method call
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/PostOfficeJournalLoader.java`
#### Snippet
```java
         List<PageCountPending> pendingCounters = perQueueMap.get(queueID);

         if (pendingCounters == null) {
            pendingCounters = new LinkedList<>();
            perQueueMap.put(queueID, pendingCounters);
```

### Java8MapApi
Can be replaced with single 'Map.computeIfAbsent' method call
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/replay/ReplayManager.java`
#### Snippet
```java
                  case JournalRecordIds.ADD_MESSAGE_BODY:
                     LinkedHashSet<JournalFile> files = largeMessageLocations.get(info.id);
                     if (files == null) {
                        files = new LinkedHashSet<>();
                        largeMessageLocations.put(info.id, files);
```

### Java8MapApi
Can be replaced with single 'Map.computeIfAbsent' method call
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/routing/policies/LeastConnectionsPolicy.java`
#### Snippet
```java
            List<Target> leastTargets = sortedTargets.get(connectionCount);

            if (leastTargets == null) {
               leastTargets = new ArrayList<>();
               sortedTargets.put(connectionCount, leastTargets);
```

### Java8MapApi
Can be replaced with single 'Map.computeIfAbsent' method call
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/JMXAccessControlList.java`
#### Snippet
```java
      public synchronized void addMethods(String prefix, String... roles) {
         List<String> rolesList = methodRoles.get(prefix);
         if (rolesList == null) {
            rolesList = new ArrayList<>();
            methodRoles.put(prefix, rolesList);
```

### Java8MapApi
Can be replaced with single 'Map.computeIfAbsent' method call
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/management/JMXAccessControlList.java`
#### Snippet
```java
      public synchronized void addMethodsPrefixes(String prefix, String... roles) {
         List<String> rolesList = methodPrefixRoles.get(prefix);
         if (rolesList == null) {
            rolesList = new ArrayList<>();
            methodPrefixRoles.put(prefix, rolesList);
```

### Java8MapApi
Can be replaced with single 'Map.computeIfAbsent' method call
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/AbstractJournalStorageManager.java`
#### Snippet
```java
         bindingsJournal.appendAddRecord(id, JournalRecordIds.KEY_VALUE_PAIR_RECORD, persistedKeyValuePair, true);
         Map<String, PersistedKeyValuePair> persistedKeyValuePairs = mapPersistedKeyValuePairs.get(persistedKeyValuePair.getMapId());
         if (persistedKeyValuePairs == null) {
            persistedKeyValuePairs = new HashMap<>();
            mapPersistedKeyValuePairs.put(persistedKeyValuePair.getMapId(), persistedKeyValuePairs);
```

### Java8MapApi
Can be replaced with single 'Map.computeIfAbsent' method call
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/AbstractJournalStorageManager.java`
#### Snippet
```java
               PersistedKeyValuePair keyValuePair = newKeyValuePairEncoding(id, buffer);
               Map<String, PersistedKeyValuePair> persistedKeyValuePairs = mapPersistedKeyValuePairs.get(keyValuePair.getMapId());
               if (persistedKeyValuePairs == null) {
                  persistedKeyValuePairs = new HashMap<>();
                  mapPersistedKeyValuePairs.put(keyValuePair.getMapId(), persistedKeyValuePairs);
```

### Java8MapApi
Can be replaced with single 'Map.computeIfAbsent' method call
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/AbstractJournalStorageManager.java`
#### Snippet
```java
                     Map<Long, AddMessageRecord> queueMessages = queueMap.get(encoding.queueID);

                     if (queueMessages == null) {
                        queueMessages = new LinkedHashMap<>();

```

### Java8MapApi
Can be replaced with single 'Map.computeIfAbsent' method call
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/AbstractJournalStorageManager.java`
#### Snippet
```java
                     List<Pair<byte[], Long>> ids = duplicateIDMap.get(encoding.address);

                     if (ids == null) {
                        ids = new ArrayList<>();

```

## RuleId[id=Convert2Lambda]
### Convert2Lambda
Anonymous new Comparator() can be replaced with lambda
in `artemis-maven-plugin/src/main/java/org/apache/activemq/artemis/maven/ArtemisDependencyDocPlugin.java`
#### Snippet
```java
         List<org.eclipse.aether.artifact.Artifact> artifacts = explodeDependencies(newArtifact(lib));

         Collections.sort(artifacts, new Comparator<Artifact>() {
            @Override
            public int compare(Artifact o1, Artifact o2) {
```

### Convert2Lambda
Anonymous new Comparator() can be replaced with lambda
in `artemis-boot/src/main/java/org/apache/activemq/artemis/boot/Artemis.java`
#### Snippet
```java

            // Sort the list by file name..
            Collections.sort(files, new Comparator<File>() {
               @Override
               public int compare(File file, File file1) {
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `artemis-protocols/artemis-stomp-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/stomp/StompProtocolManager.java`
#### Snippet
```java

      // Close the session outside of the lock on the StompConnection, otherwise it could dead lock
      this.executor.execute(new Runnable() {
         @Override
         public void run() {
```

### Convert2Lambda
Anonymous new Callable() can be replaced with lambda
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/UUIDGenerator.java`
#### Snippet
```java

      for (final NetworkInterface networkInterface : ifaces) {
         tasks.add(new Callable<byte[]>() {
            @Override
            public byte[] call() throws Exception {
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `artemis-commons/src/main/java/org/apache/activemq/artemis/utils/actors/ArtemisExecutor.java`
#### Snippet
```java
   default boolean isFlushed() {
      CountDownLatch latch = new CountDownLatch(1);
      Runnable runnable = new Runnable() {
         @Override
         public void run() {
```

### Convert2Lambda
Anonymous new FilenameFilter() can be replaced with lambda
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/io/AbstractSequentialFileFactory.java`
#### Snippet
```java
   @Override
   public List<String> listFiles(final String extension) throws Exception {
      FilenameFilter fnf = new FilenameFilter() {
         @Override
         public boolean accept(final File file, final String name) {
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `artemis-ra/src/main/java/org/apache/activemq/artemis/ra/inflow/ActiveMQActivation.java`
#### Snippet
```java
         }

         Runnable runTearDown = new Runnable() {
            @Override
            public void run() {
```

### Convert2Lambda
Anonymous new Comparator() can be replaced with lambda
in `artemis-cli/src/main/java/org/apache/activemq/artemis/cli/commands/tools/xml/XmlDataImporter.java`
#### Snippet
```java
   private void processXml() throws Exception {
      if (sort) {
         messages = new TreeSet<XMLMessageImporter.MessageInfo>(new Comparator<XMLMessageImporter.MessageInfo>() {
            @Override
            public int compare(XMLMessageImporter.MessageInfo o1, XMLMessageImporter.MessageInfo o2) {
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/amq/AMQConsumer.java`
#### Snippet
```java
                  latch.countDown();
               } else {
                  messagePullFuture = scheduledPool.schedule(new Runnable() {
                     @Override
                     public void run() {
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `artemis-protocols/artemis-openwire-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/openwire/OpenWireConnection.java`
#### Snippet
```java

      if (this.useKeepAlive) {
         ttlCheck = protocolManager.getScheduledPool().schedule(new Runnable() {
            @Override
            public void run() {
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalImpl.java`
#### Snippet
```java
      final CountDownLatch done = newLatch(1);

      filesExecutor.execute(new Runnable() {
         @Override
         public void run() {
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalImpl.java`
#### Snippet
```java

      final SimpleFuture<Boolean> result = newSyncAndCallbackResult(sync, callback);
      appendExecutor.execute(new Runnable() {
         @Override
         public void run() {
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalImpl.java`
#### Snippet
```java

      if (isAutoReclaim() && !compactorRunning.get()) {
         compactorExecutor.execute(new Runnable() {
            @Override
            public void run() {
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalImpl.java`
#### Snippet
```java
      // We can't use the executor for the compacting... or we would dead lock because of file open and creation
      // operations (that will use the executor)
      compactorExecutor.execute(new Runnable() {
         @Override
         public void run() {
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalImpl.java`
#### Snippet
```java
      final SimpleFuture<JournalTransaction> result = newSyncAndCallbackResult(sync, callback);

      appendExecutor.execute(new Runnable() {
         @Override
         public void run() {
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalImpl.java`
#### Snippet
```java
      checkJournalIsLoaded();

      appendExecutor.execute(new Runnable() {
         @Override
         public void run() {
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalImpl.java`
#### Snippet
```java


      appendExecutor.execute(new Runnable() {

         @Override
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalImpl.java`
#### Snippet
```java
      // We can't use the executor for the compacting... or we would dead lock because of file open and creation
      // operations (that will use the executor)
      compactorExecutor.execute(new Runnable() {
         @Override
         public void run() {
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalImpl.java`
#### Snippet
```java

      final SimpleFuture<JournalTransaction> result = newSyncAndCallbackResult(sync, callback);
      appendExecutor.execute(new Runnable() {
         @Override
         public void run() {
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalImpl.java`
#### Snippet
```java
                                           JournalUpdateCallback updateCallback,
                                           IOCompletion callback) throws InterruptedException, java.util.concurrent.ExecutionException {
      appendExecutor.execute(new Runnable() {
         @Override
         public void run() {
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalImpl.java`
#### Snippet
```java

         try {
            executor.execute(new Runnable() {

               @Override
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalImpl.java`
#### Snippet
```java
                                           IOCompletion callback)  {

      appendExecutor.execute(new Runnable() {
         @Override
         public void run() {
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalImpl.java`
#### Snippet
```java
      final SimpleFuture<JournalTransaction> result = newSyncAndCallbackResult(sync, callback);

      appendExecutor.execute(new Runnable() {
         @Override
         public void run() {
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `artemis-journal/src/main/java/org/apache/activemq/artemis/core/journal/impl/JournalImpl.java`
#### Snippet
```java


      appendExecutor.execute(new Runnable() {

         @Override
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQConnection.java`
#### Snippet
```java
               if (failoverListener != null) {

                  conn.failoverListenerExecutor.execute(new Runnable() {
                     @Override
                     public void run() {
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQConnection.java`
#### Snippet
```java
                  je.initCause(me);

                  new Thread(new Runnable() {
                     @Override
                     public void run() {
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `artemis-jms-client/src/main/java/org/apache/activemq/artemis/jms/client/ActiveMQConnectionForContextImpl.java`
#### Snippet
```java
public abstract class ActiveMQConnectionForContextImpl implements ActiveMQConnectionForContext {

   final Runnable closeRunnable = new Runnable() {
      @Override
      public void run() {
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/proton/ProtonServerSenderContext.java`
#### Snippet
```java

   private void flushDrain(ServerConsumerImpl serverConsumer, ProtonServerSenderContext plugSender) {
      serverConsumer.forceDelivery(1, new Runnable() {
         @Override
         public void run() {
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `artemis-jdbc-store/src/main/java/org/apache/activemq/artemis/jdbc/store/journal/JDBCJournalImpl.java`
#### Snippet
```java

   private void executeCallbacks(final List<JDBCJournalRecord> records, final boolean success) {
      Runnable r = new Runnable() {
         @Override
         public void run() {
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/Topology.java`
#### Snippet
```java

      if (copy.size() > 0) {
         executor.execute(new Runnable() {
            @Override
            public void run() {
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/Topology.java`
#### Snippet
```java
      logger.debug("{} is sending topology to {}", this, listener);

      executor.execute(new Runnable() {
         @Override
         public void run() {
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ClientConsumerImpl.java`
#### Snippet
```java

      //execute the future after the last onMessage call
      sessionExecutor.execute(new Runnable() {
         @Override
         public void run() {
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ClientConsumerImpl.java`
#### Snippet
```java
   private void sendCredits(final int credits) {
      pendingFlowControl.countUp();
      flowControlExecutor.execute(new Runnable() {
         @Override
         public void run() {
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ClientConsumerImpl.java`
#### Snippet
```java
         // If resetting a slow consumer, we need to wait the execution
         final CountDownLatch latch = new CountDownLatch(1);
         flowControlExecutor.execute(new Runnable() {
            @Override
            public void run() {
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ServerLocatorImpl.java`
#### Snippet
```java
         // to trigger the node notification to form the cluster.

         Runnable connectRunnable = new Runnable() {
            @Override
            public void run() {
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ServerLocatorImpl.java`
#### Snippet
```java

      if (executor != null) {
         executor.execute(new Runnable() {
            @Override
            public void run() {
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ClientSessionImpl.java`
#### Snippet
```java

      if (consumer != null) {
         closeExecutor.execute(new Runnable() {
            @Override
            public void run() {
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/SharedEventLoopGroup.java`
#### Snippet
```java
      synchronized (SharedEventLoopGroup.class) {
         if (channelFactoryCount.decrementAndGet() == 0) {
            shutdown.compareAndSet(null, next().scheduleAtFixedRate(new Runnable() {
               @Override
               public void run() {
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ClientSessionFactoryImpl.java`
#### Snippet
```java
            } catch (final RuntimeException e) {
               ActiveMQClientLogger.LOGGER.disconnectOnErrorDecoding(e);
               threadPool.execute(new Runnable() {
                  @Override
                  public void run() {
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ClientSessionFactoryImpl.java`
#### Snippet
```java
               cancelled = true;

               threadPool.execute(new Runnable() {
                  // Must be executed on different thread
                  @Override
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ClientSessionFactoryImpl.java`
#### Snippet
```java
            } catch (final RuntimeException e) {
               ActiveMQClientLogger.LOGGER.disconnectOnErrorDecoding(e);
               threadPool.execute(new Runnable() {
                  @Override
                  public void run() {
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ClientSessionFactoryImpl.java`
#### Snippet
```java
      // It has to use the same executor as the disconnect message is being sent through

      closeExecutor.execute(new Runnable() {
         @Override
         public void run() {
```

### Convert2Lambda
Anonymous new Comparator() can be replaced with lambda
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/utils/VersionLoader.java`
#### Snippet
```java
            }
            //We want the higher version to be the first
            Collections.sort(definedVersions, new Comparator<Version>() {
               @Override
               public int compare(Version version1, Version version2) {
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/NettyConnector.java`
#### Snippet
```java
      public void connectionException(final Object connectionID, final ActiveMQException me) {
         // Execute on different thread to avoid deadlocks
         closeExecutor.execute(new Runnable() {
            @Override
            public void run() {
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/remoting/impl/netty/NettyConnector.java`
#### Snippet
```java
         if (connections.remove(connectionID) != null) {
            // Execute on different thread to avoid deadlocks
            closeExecutor.execute(new Runnable() {
               @Override
               public void run() {
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/impl/PageSyncTimer.java`
#### Snippet
```java
   private final long timeSync;

   private final Runnable runnable = new Runnable() {
      @Override
      public void run() {
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/cursor/impl/PageCursorProviderImpl.java`
#### Snippet
```java
      scheduledCleanup.incrementAndGet();

      pagingStore.execute(new Runnable() {
         @Override
         public void run() {
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/paging/cursor/impl/PageSubscriptionImpl.java`
#### Snippet
```java
            @Override
            public void afterCommit(final Transaction tx1) {
               pageStore.execute(new Runnable() {

                  @Override
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/SharedStoreBackupActivation.java`
#### Snippet
```java

            ActiveMQServerLogger.LOGGER.backupServerScaledDown();
            Thread t = new Thread(new Runnable() {
               @Override
               public void run() {
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/SharedStoreBackupActivation.java`
#### Snippet
```java
               }
               ActiveMQServerLogger.LOGGER.awaitFailBack();
               Thread t = new Thread(new Runnable() {
                  @Override
                  public void run() {
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ColocatedActivation.java`
#### Snippet
```java
               if (!backupStarted) {
                  nodes.clear();
                  server.getScheduledPool().schedule(new Runnable() {
                     @Override
                     public void run() {
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ColocatedActivation.java`
#### Snippet
```java
         } else {
            nodes.clear();
            server.getScheduledPool().schedule(new Runnable() {
               @Override
               public void run() {
```

### Convert2Lambda
Anonymous new Comparator\>() can be replaced with lambda
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ColocatedActivation.java`
#### Snippet
```java
      public Pair<String, Integer> getDecision() {
         //sort the nodes by how many backups they have and choose the first
         Collections.sort(nodes, new Comparator<Pair<String, Integer>>() {
            @Override
            public int compare(Pair<String, Integer> o1, Pair<String, Integer> o2) {
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/SharedNothingLiveActivation.java`
#### Snippet
```java
         ExecutorService executorService = activeMQServer.getThreadPool();
         if (executorService != null) {
            executorService.execute(new Runnable() {
               @Override
               public void run() {
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/SharedNothingLiveActivation.java`
#### Snippet
```java
         // Schedule for 10 seconds
         // this pool gets a 'hard' shutdown, no need to manage the Future of this Runnable.
         activeMQServer.getScheduledPool().schedule(new Runnable() {
            @Override
            public void run() {
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/SharedNothingLiveActivation.java`
#### Snippet
```java
         replicationManager = new ReplicationManager(activeMQServer, rc, clusterConnection.getCallTimeout(), replicatedPolicy.getInitialReplicationSyncTimeout(), activeMQServer.getIOExecutorFactory());
         replicationManager.start();
         Thread t = new Thread(new Runnable() {
            @Override
            public void run() {
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/SharedNothingLiveActivation.java`
#### Snippet
```java
                           if (!isStillLive) {
                              try {
                                 Thread startThread = new Thread(new Runnable() {
                                    @Override
                                    public void run() {
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/SharedNothingBackupActivation.java`
#### Snippet
```java
               final SharedNothingBackupQuorum.BACKUP_ACTIVATION signalToStop = signal;

               Thread startThread = new Thread(new Runnable() {
                  @Override
                  public void run() {
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerConsumerImpl.java`
#### Snippet
```java
   }

   private final Runnable resumeLargeMessageRunnable = new Runnable() {
      @Override
      public void run() {
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerConsumerImpl.java`
#### Snippet
```java
                  if (transferring) {
                     // Case it's transferring (reattach), we will retry later
                     messageQueue.getExecutor().execute(new Runnable() {
                        @Override
                        public void run() {
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerConsumerImpl.java`
#### Snippet
```java

      // JBPAPP-6030 - Using the executor to avoid distributed dead locks
      messageQueue.getExecutor().execute(new Runnable() {
         @Override
         public void run() {
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ServerSessionImpl.java`
#### Snippet
```java
      if (store == null) {
         callback.sendProducerCreditsMessage(credits, address);
      } else if (!store.checkMemory(new Runnable() {
         @Override
         public void run() {
```

### Convert2Lambda
Anonymous new FilenameFilter() can be replaced with lambda
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/files/FileMoveManager.java`
#### Snippet
```java
   };

   private static final FilenameFilter notPrefix = new FilenameFilter() {
      @Override
      public boolean accept(File dir, String name) {
```

### Convert2Lambda
Anonymous new FilenameFilter() can be replaced with lambda
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/files/FileMoveManager.java`
#### Snippet
```java
   public static final String PREFIX = "oldreplica.";

   private static final FilenameFilter isPrefix = new FilenameFilter() {
      @Override
      public boolean accept(File dir, String name) {
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/group/impl/GroupHandlingAbstract.java`
#### Snippet
```java
         listeners.add(listener);
      } else {
         executor.execute(new Runnable() {
            @Override
            public void run() {
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/group/impl/GroupHandlingAbstract.java`
#### Snippet
```java
   protected void fireUnproposed(final SimpleString groupID) {

      Runnable runnable = new Runnable() {
         @Override
         public void run() {
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/group/impl/LocalGroupingHandler.java`
#### Snippet
```java
      final List<GroupBinding> list = groupMap.remove(clusterName);
      if (list != null) {
         executor.execute(new Runnable() {
            @Override
            public void run() {
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/BackupManager.java`
#### Snippet
```java
      /** it will re-schedule the connection after a timeout, using a scheduled executor */
      protected void retryConnection() {
         scheduledExecutor.schedule(new Runnable() {
            @Override
            public void run() {
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/BackupManager.java`
#### Snippet
```java
            closeLocator(backupServerLocator);
         }
         executor.execute(new Runnable() {
            @Override
            public void run() {
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/ClusterManager.java`
#### Snippet
```java
            if (exception.getType() == ActiveMQExceptionType.CLUSTER_SECURITY_EXCEPTION) {
               ActiveMQServerLogger.LOGGER.clusterManagerAuthenticationError(exception.getMessage());
               executor.execute(new Runnable() {
                  @Override
                  public void run() {
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/impl/BridgeImpl.java`
#### Snippet
```java
                                    final LargeServerMessage message,
                                    final Message originalMessage) {
      executor.execute(new Runnable() {
         @Override
         public void run() {
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/impl/BridgeImpl.java`
#### Snippet
```java
   @Override
   public void disconnect() {
      executor.execute(new Runnable() {
         @Override
         public void run() {
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/impl/ClusterConnectionImpl.java`
#### Snippet
```java
         managementService.sendNotification(notification);
      }
      executor.execute(new Runnable() {
         @Override
         public void run() {
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/cluster/impl/ClusterConnectionImpl.java`
#### Snippet
```java
         bridge.stop();

         bridge.getExecutor().execute(new Runnable() {
            @Override
            public void run() {
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/ActiveMQServerImpl.java`
#### Snippet
```java

      if (dumpInfoInterval > 0) {
         scheduledPool.scheduleWithFixedDelay(new Runnable() {
            @Override
            public void run() {
```

### Convert2Lambda
Anonymous new FilenameFilter() can be replaced with lambda
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/config/impl/ConfigurationImpl.java`
#### Snippet
```java
               File dir = new File(fileUrl);
               if (dir.exists()) {
                  String[] files = dir.list(new FilenameFilter() {
                     @Override
                     public boolean accept(File file, String s) {
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/CoreProtocolManager.java`
#### Snippet
```java
                  // What could cause deadlocks
                  try {
                     entry.connectionExecutor.execute(new Runnable() {
                        @Override
                        public void run() {
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/CoreProtocolManager.java`
#### Snippet
```java
               // if not clustered, we send a single notification to the client containing the node-id where the server is connected to
               // This is done this way so Recovery discovery could also use the node-id for non-clustered setups
               entry.connectionExecutor.execute(new Runnable() {
                  @Override
                  public void run() {
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/protocol/core/impl/CoreProtocolManager.java`
#### Snippet
```java
                     // may come from a channel itself
                     // What could cause deadlocks
                     entry.connectionExecutor.execute(new Runnable() {
                        @Override
                        public void run() {
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/remoting/impl/invm/InVMAcceptor.java`
#### Snippet
```java

            // Execute on different thread after all the packets are sent, to avoid deadlocks
            connection.getExecutor().execute(new Runnable() {
               @Override
               public void run() {
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/remoting/impl/invm/InVMConnection.java`
#### Snippet
```java
         if (flush && flushEnabled) {
            final CountDownLatch latch = new CountDownLatch(1);
            executor.execute(new Runnable() {
               @Override
               public void run() {
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/remoting/impl/invm/InVMConnector.java`
#### Snippet
```java

            // Execute on different thread to avoid deadlocks
            closeExecutor.execute(new Runnable() {
               @Override
               public void run() {
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/remoting/impl/invm/InVMConnector.java`
#### Snippet
```java
      public void connectionException(final Object connectionID, final ActiveMQException me) {
         // Execute on different thread to avoid deadlocks
         closeExecutor.execute(new Runnable() {
            @Override
            public void run() {
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/remoting/server/impl/RemotingServiceImpl.java`
#### Snippet
```java

                  if (flush) {
                     flushExecutor.execute(new Runnable() {
                        @Override
                        public void run() {
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/remoting/server/impl/RemotingServiceImpl.java`
#### Snippet
```java
                     // We can't pause the FailureCheckAndFlushThread as that would lead other clients to fail for
                     // missing pings
                     flushExecutor.execute(new Runnable() {
                        @Override
                        public void run() {
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/OperationContextImpl.java`
#### Snippet
```java
      EXECUTORS_PENDING_UPDATER.incrementAndGet(this);
      try {
         executor.execute(new Runnable() {
            @Override
            public void run() {
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/JDBCJournalStorageManager.java`
#### Snippet
```java

      final CountDownLatch latch = new CountDownLatch(1);
      executor.execute(new Runnable() {
         @Override
         public void run() {
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/JournalStorageManager.java`
#### Snippet
```java
      final CountDownLatch latch = new CountDownLatch(1);
      try {
         executor.execute(new Runnable() {
            @Override
            public void run() {
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/JournalStorageManager.java`
#### Snippet
```java
         }
      }
      Runnable deleteAction = new Runnable() {
         @Override
         public void run() {
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/replication/ReplicationEndpoint.java`
#### Snippet
```java
            message.releaseResources(true, false);
         } else {
            executor.execute(new Runnable() {
               @Override
               public void run() {
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/journal/AbstractJournalStorageManager.java`
#### Snippet
```java

      final CountDownLatch latch = new CountDownLatch(1);
      executor.execute(new Runnable() {
         @Override
         public void run() {
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java
   @Override
   public void close() throws Exception {
      getExecutor().execute(new Runnable() {
         @Override
         public void run() {
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/QueueImpl.java`
#### Snippet
```java
         final SimpleString groupIDToRemove = (SimpleString) groupID.subSequence(0, groupID.length() - getName().length() - 1);
         // using an executor so we don't want to hold anyone just because of this
         getExecutor().execute(new Runnable() {
            @Override
            public void run() {
```

### Convert2Lambda
Anonymous new Comparator\>() can be replaced with lambda
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/ActiveMQServerControlImpl.java`
#### Snippet
```java

         ArrayList<Entry<Xid, Long>> xidsSortedByCreationTime = new ArrayList<>(xids.entrySet());
         Collections.sort(xidsSortedByCreationTime, new Comparator<Entry<Xid, Long>>() {
            @Override
            public int compare(final Entry<Xid, Long> entry1, final Entry<Xid, Long> entry2) {
```

### Convert2Lambda
Anonymous new Comparator\>() can be replaced with lambda
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/ActiveMQServerControlImpl.java`
#### Snippet
```java

         ArrayList<Entry<Xid, Long>> xidsSortedByCreationTime = new ArrayList<>(xids.entrySet());
         Collections.sort(xidsSortedByCreationTime, new Comparator<Entry<Xid, Long>>() {
            @Override
            public int compare(final Entry<Xid, Long> entry1, final Entry<Xid, Long> entry2) {
```

### Convert2Lambda
Anonymous new Comparator\>() can be replaced with lambda
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/ActiveMQServerControlImpl.java`
#### Snippet
```java
         Map<Xid, Long> xids = resourceManager.getPreparedTransactionsWithCreationTime();
         ArrayList<Entry<Xid, Long>> xidsSortedByCreationTime = new ArrayList<>(xids.entrySet());
         Collections.sort(xidsSortedByCreationTime, new Comparator<Entry<Xid, Long>>() {
            @Override
            public int compare(final Entry<Xid, Long> entry1, final Entry<Xid, Long> entry2) {
```

### Convert2Lambda
Anonymous new Comparator() can be replaced with lambda
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/management/impl/ActiveMQServerControlImpl.java`
#### Snippet
```java
      try {
         final Set<SimpleString> addresses = server.getPostOffice().getAddresses();
         TreeSet<SimpleString> sortAddress = new TreeSet<>(new Comparator<SimpleString>() {
            @Override
            public int compare(SimpleString o1, SimpleString o2) {
```

## RuleId[id=UnnecessaryContinue]
### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/server/impl/PostOfficeJournalLoader.java`
#### Snippet
```java
            } catch (Throwable t) {
               ActiveMQServerLogger.LOGGER.unableToLoadMessageFromJournal(t);
               continue;
            }
         }
```

## RuleId[id=CopyConstructorMissesField]
### CopyConstructorMissesField
Copy constructor does not copy 11 fields
in `artemis-protocols/artemis-amqp-protocol/src/main/java/org/apache/activemq/artemis/protocol/amqp/broker/AMQPMessage.java`
#### Snippet
```java
   }

   protected AMQPMessage(AMQPMessage copy) {
      this(copy.messageFormat, copy.extraProperties, copy.coreMessageObjectPools);

```

### CopyConstructorMissesField
Copy constructor does not copy 5 fields
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ClientMessageImpl.java`
#### Snippet
```java
   }

   protected ClientMessageImpl(ClientMessageImpl other) {
      super(other);
   }
```

### CopyConstructorMissesField
Copy constructor does not copy 4 fields
in `artemis-core-client/src/main/java/org/apache/activemq/artemis/core/client/impl/ServerLocatorImpl.java`
#### Snippet
```java
   }

   private ServerLocatorImpl(ServerLocatorImpl locator) {
      ha = locator.ha;
      clusterConnection = locator.clusterConnection;
```

### CopyConstructorMissesField
Copy constructor does not copy fields 'autoDeleteQueuesMessageCount' and 'retroactiveMessageCount'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/settings/impl/AddressSettings.java`
#### Snippet
```java
   private transient Integer queuePrefetch = null;

   public AddressSettings(AddressSettings other) {
      this.addressFullMessagePolicy = other.addressFullMessagePolicy;
      this.maxSizeBytes = other.maxSizeBytes;
```

### CopyConstructorMissesField
Copy constructor does not copy field 'storageManager'
in `artemis-server/src/main/java/org/apache/activemq/artemis/core/persistence/impl/nullpm/NullStorageLargeServerMessage.java`
#### Snippet
```java
   }

   NullStorageLargeServerMessage(NullStorageLargeServerMessage other) {
      super(other);
   }
```

## RuleId[id=EqualsWhichDoesntCheckParameterClass]
### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `artemis-server/src/main/java/org/apache/activemq/artemis/utils/SecurityManagerUtil.java`
#### Snippet
```java

            @Override
            public boolean equals(Object other) {
               return true;
            }
```

## RuleId[id=CastCanBeRemovedNarrowingVariableType]
### CastCanBeRemovedNarrowingVariableType
Cast may be removed by changing the type of 'replyTo' to 'String'
in `artemis-protocols/artemis-stomp-protocol/src/main/java/org/apache/activemq/artemis/core/protocol/stomp/StompUtils.java`
#### Snippet
```java
      Object replyTo = headers.remove(Stomp.Headers.Send.REPLY_TO);
      if (replyTo != null) {
         msg.putStringProperty(ClientMessageImpl.REPLYTO_HEADER_NAME, SimpleString.toSimpleString((String) replyTo));
      }
      String expiration = headers.remove(Stomp.Headers.Send.EXPIRATION_TIME);
```

