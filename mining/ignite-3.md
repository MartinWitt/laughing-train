# ignite-3 
 
# Bad smells
I found 3392 bad smells with 364 repairable:
| ruleID | number | fixable |
| --- | --- | --- |
| BoundedWildcard | 645 | false |
| ReturnNull | 485 | false |
| DataFlowIssue | 483 | false |
| AssignmentToMethodParameter | 181 | false |
| UtilityClassWithoutPrivateConstructor | 175 | true |
| StaticCallOnSubclass | 124 | false |
| UnnecessaryModifier | 122 | true |
| NestedAssignment | 119 | false |
| RedundantFieldInitialization | 102 | false |
| SystemOutErr | 77 | false |
| ConstantValue | 69 | false |
| NullableProblems | 64 | false |
| UnnecessaryFullyQualifiedName | 53 | false |
| MagicConstant | 38 | false |
| FieldAccessedSynchronizedAndUnsynchronized | 33 | false |
| FieldMayBeStatic | 33 | false |
| ZeroLengthArrayInitialization | 32 | false |
| SynchronizeOnThis | 30 | false |
| EmptyMethod | 30 | false |
| NonProtectedConstructorInAbstractClass | 27 | true |
| RedundantSuppression | 21 | false |
| CommentedOutCode | 18 | false |
| AssignmentToLambdaParameter | 18 | false |
| DuplicateBranchesInSwitch | 17 | false |
| MarkedForRemoval | 16 | false |
| UnnecessaryQualifierForThis | 16 | false |
| RedundantMethodOverride | 14 | false |
| UnnecessaryLocalVariable | 14 | true |
| NonSerializableFieldInSerializableClass | 14 | false |
| IgnoreResultOfCall | 13 | false |
| DynamicRegexReplaceableByCompiledPattern | 13 | false |
| Convert2MethodRef | 13 | false |
| UnnecessarySuperQualifier | 12 | false |
| CodeBlock2Expr | 12 | true |
| UnnecessarySemicolon | 11 | false |
| PublicFieldAccessedInSynchronizedContext | 11 | false |
| NonShortCircuitBoolean | 10 | false |
| InstanceofCatchParameter | 9 | false |
| ArrayEquality | 9 | false |
| AssignmentToForLoopParameter | 9 | false |
| MethodOverloadsParentMethod | 8 | false |
| BusyWait | 8 | false |
| MissortedModifiers | 7 | false |
| TypeParameterExtendsObject | 7 | false |
| MismatchedJavadocCode | 7 | false |
| OptionalUsedAsFieldOrParameterType | 6 | false |
| DefaultAnnotationParam | 6 | false |
| ClassNameSameAsAncestorName | 5 | false |
| CaughtExceptionImmediatelyRethrown | 5 | false |
| StringBufferReplaceableByString | 5 | false |
| AbstractClassNeverImplemented | 5 | false |
| RedundantImplements | 5 | false |
| UnnecessaryToStringCall | 5 | true |
| Convert2Lambda | 5 | false |
| SizeReplaceableByIsEmpty | 4 | true |
| RegExpDuplicateCharacterInClass | 4 | false |
| IntegerMultiplicationImplicitCastToLong | 4 | false |
| EqualsAndHashcode | 4 | false |
| UnusedAssignment | 4 | false |
| SamePackageImport | 4 | false |
| IfStatementWithIdenticalBranches | 4 | false |
| ToArrayCallWithZeroLengthArrayArgument | 3 | true |
| NonSynchronizedMethodOverridesSynchronizedMethod | 3 | false |
| ConditionalBreakInInfiniteLoop | 3 | false |
| ManualMinMaxCalculation | 3 | false |
| NullArgumentToVariableArgMethod | 3 | false |
| DeprecatedIsStillUsed | 3 | false |
| AssignmentToStaticFieldFromInstanceMethod | 2 | false |
| KeySetIterationMayUseEntrySet | 2 | false |
| UnnecessaryUnboxing | 2 | false |
| UNUSED_IMPORT | 2 | false |
| AssignmentUsedAsCondition | 2 | false |
| SimplifyStreamApiCallChains | 2 | false |
| OptionalContainsCollection | 2 | false |
| TestCaseWithConstructor | 2 | false |
| WhileLoopSpinsOnField | 2 | false |
| IOResource | 2 | false |
| SetReplaceableByEnumSet | 2 | false |
| MissingDeprecatedAnnotation | 2 | false |
| ReplaceNullCheck | 2 | false |
| EqualsWhichDoesntCheckParameterClass | 2 | false |
| TestCaseWithNoTestMethods | 2 | false |
| Finalize | 1 | false |
| DoubleCheckedLocking | 1 | false |
| WhileCanBeForeach | 1 | false |
| RegExpRedundantEscape | 1 | false |
| AccessStaticViaInstance | 1 | false |
| MismatchedCollectionQueryUpdate | 1 | false |
| TypeParameterHidesVisibleType | 1 | false |
| CollectionContainsUrl | 1 | false |
| SwitchStatementWithConfusingDeclaration | 1 | false |
| UtilityClassWithPublicConstructor | 1 | false |
| ComparatorMethodParameterNotUsed | 1 | false |
| SuspiciousToArrayCall | 1 | false |
| UseCompareMethod | 1 | false |
| UseBulkOperation | 1 | false |
| AssignmentToCatchBlockParameter | 1 | false |
| StringEquality | 1 | false |
| LoopStatementsThatDontLoop | 1 | false |
| ExternalizableWithoutPublicNoArgConstructor | 1 | false |
| AssertWithSideEffects | 1 | false |
| UnnecessaryReturn | 1 | true |
| FunctionalExpressionCanBeFolded | 1 | false |
| ThrowFromFinallyBlock | 1 | false |
| SlowListContainsAll | 1 | false |
| InfiniteLoopStatement | 1 | false |
| NonFinalFieldOfException | 1 | false |
| FuseStreamOperations | 1 | false |
| NonExceptionNameEndsWithException | 1 | false |
| OptionalGetWithoutIsPresent | 1 | false |
| StringConcatenationInsideStringBufferAppend | 1 | false |
| Java8MapForEach | 1 | false |
| RedundantLengthCheck | 1 | false |
| SuspiciousMethodCalls | 1 | false |
| InnerClassMayBeStatic | 1 | true |
| UnnecessaryBoxing | 1 | false |
| Java8MapApi | 1 | false |
| UnnecessaryContinue | 1 | false |
| CopyConstructorMissesField | 1 | false |
| ThrowableNotThrown | 1 | false |
| ThreadStartInConstruction | 1 | false |
| CastCanBeRemovedNarrowingVariableType | 1 | false |
## RuleId[id=ToArrayCallWithZeroLengthArrayArgument]
### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new String\[list.size()\]'
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/StringUtils.java`
#### Snippet
```java
            list.add(str.substring(start, i));
        }
        return (String[]) list.toArray(new String[list.size()]);
    }

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new String\[list.size()\]'
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/StringUtils.java`
#### Snippet
```java
                    }

                    return (String[]) list.toArray(new String[list.size()]);
                }
            }
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new MBeanAttributeInfo\[attrs.size()\]'
in `modules/metrics/src/main/java/org/apache/ignite/internal/metrics/exporters/jmx/MetricSetMbean.java`
#### Snippet
```java
                MetricManager.class.getName(),
                metricSet.name(),
                attrs.toArray(new MBeanAttributeInfo[attrs.size()]),
                null,
                null,
```

## RuleId[id=Finalize]
### Finalize
'finalize()' should not be overridden
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/timer/HashedWheelTimer.java`
#### Snippet
```java

    @Override
    protected void finalize() throws Throwable {
        try {
            super.finalize();
```

## RuleId[id=UnnecessaryModifier]
### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/Marshaller.java`
#### Snippet
```java

public interface Marshaller {
    public static Marshaller DEFAULT = new JDKMarshaller();

    byte[] marshall(Object o);
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/Marshaller.java`
#### Snippet
```java

public interface Marshaller {
    public static Marshaller DEFAULT = new JDKMarshaller();

    byte[] marshall(Object o);
```

### UnnecessaryModifier
Modifier `private` is redundant for enum constructors
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/entity/EnumOutter.java`
#### Snippet
```java
        private final int value;

        private ErrorType(int value) {
            this.value = value;
        }
```

### UnnecessaryModifier
Modifier `private` is redundant for enum constructors
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/entity/EnumOutter.java`
#### Snippet
```java
        private final int value;

        private EntryType(int value) {
            this.value = value;
        }
```

### UnnecessaryModifier
Modifier `private` is redundant for enum constructors
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/entity/LocalFileMetaOutter.java`
#### Snippet
```java
        private final int value;

        private FileSource(int value) {
            this.value = value;
        }
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/client/src/main/java/org/apache/ignite/client/RetryPolicyContext.java`
#### Snippet
```java
     * @return Exception.
     */
    public IgniteClientConnectionException exception();
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/client/src/main/java/org/apache/ignite/client/RetryPolicyContext.java`
#### Snippet
```java
     * @return Client configuration.
     */
    public IgniteClientConfiguration configuration();

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/client/src/main/java/org/apache/ignite/client/RetryPolicyContext.java`
#### Snippet
```java
     * @return Operation type.
     */
    public ClientOperationType operation();

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/client/src/main/java/org/apache/ignite/client/RetryPolicyContext.java`
#### Snippet
```java
     * @return Zero-based iteration counter.
     */
    public int iteration();

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/client/src/main/java/org/apache/ignite/client/RetryPolicy.java`
#### Snippet
```java
     * @return {@code true} if the operation should be retried on another connection, {@code false} otherwise.
     */
    public boolean shouldRetry(RetryPolicyContext context);
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/client/src/main/java/org/apache/ignite/client/IgniteClientConfiguration.java`
#### Snippet
```java
     * @return Heartbeat interval.
     */
    public long heartbeatTimeout();

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/client/src/main/java/org/apache/ignite/client/IgniteClientConfiguration.java`
#### Snippet
```java
     * @return Heartbeat interval.
     */
    public long heartbeatInterval();

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/client/src/main/java/org/apache/ignite/client/IgniteClientAddressFinder.java`
#### Snippet
```java
     * @return Addresses of Ignite server nodes within a cluster.
     */
    public String[] getAddresses();
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/client/src/main/java/org/apache/ignite/internal/client/ClientChannel.java`
#### Snippet
```java
     * @return Future for the operation.
     */
    public <T> CompletableFuture<T> serviceAsync(
            int opCode,
            PayloadWriter payloadWriter,
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/client/src/main/java/org/apache/ignite/internal/client/ClientChannel.java`
#### Snippet
```java
     * @param listener Listener.
     */
    public void addTopologyAssignmentChangeListener(Consumer<ClientChannel> listener);
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/client/src/main/java/org/apache/ignite/internal/client/ClientChannel.java`
#### Snippet
```java
     * @return Protocol context.
     */
    public ProtocolContext protocolContext();

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/client/src/main/java/org/apache/ignite/internal/client/ClientChannel.java`
#### Snippet
```java
     * @return {@code True} channel is closed.
     */
    public boolean closed();

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/schema/src/main/java/org/apache/ignite/internal/util/Factory.java`
#### Snippet
```java
     * @return Object.
     */
    public T create();
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/schema/src/main/java/org/apache/ignite/internal/schema/mapping/ColumnMapper.java`
#### Snippet
```java
     * @return {@code this} for chaining.
     */
    public ColumnMapper add(@NotNull Column col);

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/schema/src/main/java/org/apache/ignite/internal/schema/mapping/ColumnMapper.java`
#### Snippet
```java
     * @return {@code this} for chaining.
     */
    public ColumnMapper add(int from, int to);

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/network/src/main/java/org/apache/ignite/internal/network/direct/state/DirectMessageStateItem.java`
#### Snippet
```java
     * Resets the state.
     */
    public void reset();
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/replicator/src/main/java/org/apache/ignite/internal/replicator/listener/ReplicaListener.java`
#### Snippet
```java
     * @return Listener response.
     */
    public CompletableFuture<Object> invoke(ReplicaRequest request);
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rel/set/IgniteColocatedSetOp.java`
#### Snippet
```java
    /** {@inheritDoc} */
    @Override
    public default List<Pair<RelTraitSet, List<RelTraitSet>>> deriveDistribution(
            RelTraitSet nodeTraits,
            List<RelTraitSet> inputTraits
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rel/set/IgniteColocatedSetOp.java`
#### Snippet
```java
    /** {@inheritDoc} */
    @Override
    public default AggregateType aggregateType() {
        return AggregateType.SINGLE;
    }
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rel/set/IgniteColocatedSetOp.java`
#### Snippet
```java
    /** {@inheritDoc} */
    @Override
    public default Pair<RelTraitSet, List<RelTraitSet>> passThroughDistribution(RelTraitSet nodeTraits,
            List<RelTraitSet> inTraits) {
        if (TraitUtils.distribution(nodeTraits) == IgniteDistributions.single()) {
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rel/set/IgniteReduceSetOp.java`
#### Snippet
```java
    /** {@inheritDoc} */
    @Override
    public default AggregateType aggregateType() {
        return AggregateType.REDUCE;
    }
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rel/set/IgniteReduceSetOp.java`
#### Snippet
```java
    /** {@inheritDoc} */
    @Override
    public default List<Pair<RelTraitSet, List<RelTraitSet>>> deriveDistribution(
            RelTraitSet nodeTraits,
            List<RelTraitSet> inputTraits
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rel/set/IgniteReduceSetOp.java`
#### Snippet
```java
    /** {@inheritDoc} */
    @Override
    public default Pair<RelTraitSet, List<RelTraitSet>> passThroughDistribution(RelTraitSet nodeTraits,
            List<RelTraitSet> inTraits) {
        if (TraitUtils.distribution(nodeTraits) == IgniteDistributions.single()) {
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rel/set/IgniteSetOp.java`
#### Snippet
```java

    /** Gets count of fields for aggregation for this node. Required for memory consumption calculation. */
    public int aggregateFieldsCount();

    /** Compute cost for set op. */
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rel/set/IgniteSetOp.java`
#### Snippet
```java

    /** Aggregate type. */
    public AggregateType aggregateType();
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rel/set/IgniteSetOp.java`
#### Snippet
```java
    /** {@inheritDoc} */
    @Override
    public default Pair<RelTraitSet, List<RelTraitSet>> passThroughCollation(RelTraitSet nodeTraits,
            List<RelTraitSet> inputTraits) {
        // Operation erases collation.
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rel/set/IgniteSetOp.java`
#### Snippet
```java
    /** {@inheritDoc} */
    @Override
    public default List<Pair<RelTraitSet, List<RelTraitSet>>> deriveCollation(RelTraitSet nodeTraits,
            List<RelTraitSet> inputTraits) {
        // Operation erases collation.
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rel/set/IgniteSetOp.java`
#### Snippet
```java

    /** Compute cost for set op. */
    public default RelOptCost computeSetOpCost(RelOptPlanner planner, RelMetadataQuery mq) {
        IgniteCostFactory costFactory = (IgniteCostFactory) planner.getCostFactory();

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rel/set/IgniteSetOp.java`
#### Snippet
```java
public interface IgniteSetOp extends TraitsAwareIgniteRel {
    /** ALL flag of set op. */
    public boolean all();

    /** {@inheritDoc} */
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/RangeIterable.java`
#### Snippet
```java
     * @return {@code True} if iterable can have more than one item.
     */
    public boolean multiBounds();
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/trait/TraitsAwareIgniteRel.java`
#### Snippet
```java
    /** {@inheritDoc} */
    @Override
    public default List<RelNode> derive(List<List<RelTraitSet>> inTraits) {
        return TraitUtils.derive(this, inTraits);
    }
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/trait/TraitsAwareIgniteRel.java`
#### Snippet
```java
    /** {@inheritDoc} */
    @Override
    public default DeriveMode getDeriveMode() {
        return DeriveMode.OMAKASE;
    }
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/network-api/src/main/java/org/apache/ignite/network/serialization/MessageWriter.java`
#### Snippet
```java
     * @return Whether a value was fully written.
     */
    public boolean writeMessage(String name, NetworkMessage val);

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/network-api/src/main/java/org/apache/ignite/network/serialization/MessageWriter.java`
#### Snippet
```java
     * @return Whether an array was fully written.
     */
    public boolean writeShortArray(String name, short[] val);

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/network-api/src/main/java/org/apache/ignite/network/serialization/MessageWriter.java`
#### Snippet
```java
     * @return Whether the message header has already been written.
     */
    public boolean isHeaderWritten();

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/network-api/src/main/java/org/apache/ignite/network/serialization/MessageWriter.java`
#### Snippet
```java
     * @return Whether a value was fully written.
     */
    public boolean writeIgniteUuid(String name, IgniteUuid val);

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/network-api/src/main/java/org/apache/ignite/network/serialization/MessageWriter.java`
#### Snippet
```java
     * @return Whether an array was fully written.
     */
    public boolean writeLongArray(String name, long[] val, int len);

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/network-api/src/main/java/org/apache/ignite/network/serialization/MessageWriter.java`
#### Snippet
```java
     * @return Whether a value was fully written.
     */
    public boolean writeFloat(String name, float val);

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/network-api/src/main/java/org/apache/ignite/network/serialization/MessageWriter.java`
#### Snippet
```java
     * @return Whether an array was fully written.
     */
    public <T> boolean writeObjectArray(String name, T[] arr, MessageCollectionItemType itemType);

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/network-api/src/main/java/org/apache/ignite/network/serialization/MessageWriter.java`
#### Snippet
```java
     * @return Whether a value was fully written.
     */
    public boolean writeUuid(String name, UUID val);

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/network-api/src/main/java/org/apache/ignite/network/serialization/MessageWriter.java`
#### Snippet
```java
     * @return Whether an array was fully written.
     */
    public boolean writeCharArray(String name, char[] val);

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/network-api/src/main/java/org/apache/ignite/network/serialization/MessageWriter.java`
#### Snippet
```java
     * Resets this writer.
     */
    public void reset();
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/network-api/src/main/java/org/apache/ignite/network/serialization/MessageWriter.java`
#### Snippet
```java
     * Callback called when the header of the message is written.
     */
    public void onHeaderWritten();

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/network-api/src/main/java/org/apache/ignite/network/serialization/MessageWriter.java`
#### Snippet
```java
     * @return Whether a value was fully written.
     */
    public boolean writeInt(String name, int val);

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/network-api/src/main/java/org/apache/ignite/network/serialization/MessageWriter.java`
#### Snippet
```java
     * @return Whether an array was fully written.
     */
    public boolean writeIntArray(String name, int[] val);

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/network-api/src/main/java/org/apache/ignite/network/serialization/MessageWriter.java`
#### Snippet
```java
     * @return Whether a value was fully written.
     */
    public <K, V> boolean writeMap(String name, Map<K, V> map, MessageCollectionItemType keyType,
            MessageCollectionItemType valType);

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/network-api/src/main/java/org/apache/ignite/network/serialization/MessageWriter.java`
#### Snippet
```java
     * @return Whether a value was fully written.
     */
    public <T> boolean writeList(String name, List<T> col, MessageCollectionItemType itemType);

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/network-api/src/main/java/org/apache/ignite/network/serialization/MessageWriter.java`
#### Snippet
```java
     * Callback called before an inner message is written.
     */
    public void beforeInnerMessageWrite();

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/network-api/src/main/java/org/apache/ignite/network/serialization/MessageWriter.java`
#### Snippet
```java
     * @return Whether an array was fully written.
     */
    public boolean writeDoubleArray(String name, double[] val);

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/network-api/src/main/java/org/apache/ignite/network/serialization/MessageWriter.java`
#### Snippet
```java
     * @return Whether a value was fully written.
     */
    public boolean writeChar(String name, char val);

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/network-api/src/main/java/org/apache/ignite/network/serialization/MessageWriter.java`
#### Snippet
```java
     * @return Whether a value was fully written.
     */
    public boolean writeBoolean(String name, boolean val);

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/network-api/src/main/java/org/apache/ignite/network/serialization/MessageWriter.java`
#### Snippet
```java
     * @return Whether a value was fully written.
     */
    public boolean writeDouble(String name, double val);

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/network-api/src/main/java/org/apache/ignite/network/serialization/MessageWriter.java`
#### Snippet
```java
     * @return Whether a value was fully written.
     */
    public <T> boolean writeCollection(String name, Collection<T> col, MessageCollectionItemType itemType);

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/network-api/src/main/java/org/apache/ignite/network/serialization/MessageWriter.java`
#### Snippet
```java
     * @return Whether a value was fully written.
     */
    public boolean writeShort(String name, short val);

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/network-api/src/main/java/org/apache/ignite/network/serialization/MessageWriter.java`
#### Snippet
```java
     * @return State.
     */
    public int state();

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/network-api/src/main/java/org/apache/ignite/network/serialization/MessageWriter.java`
#### Snippet
```java
     * @return Whether a value was fully written.
     */
    public <T> boolean writeSet(String name, Set<T> col, MessageCollectionItemType itemType);

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/network-api/src/main/java/org/apache/ignite/network/serialization/MessageWriter.java`
#### Snippet
```java
     * @return Whether a value was fully written.
     */
    public boolean writeByte(String name, byte val);

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/network-api/src/main/java/org/apache/ignite/network/serialization/MessageWriter.java`
#### Snippet
```java
     * @param msgCls Message type.
     */
    public void setCurrentWriteClass(Class<? extends NetworkMessage> msgCls);

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/network-api/src/main/java/org/apache/ignite/network/serialization/MessageWriter.java`
#### Snippet
```java
     * @param finished Whether the message was fully written.
     */
    public void afterInnerMessageWrite(boolean finished);

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/network-api/src/main/java/org/apache/ignite/network/serialization/MessageWriter.java`
#### Snippet
```java
     * @return Whether a value was fully written.
     */
    public boolean writeBitSet(String name, BitSet val);

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/network-api/src/main/java/org/apache/ignite/network/serialization/MessageWriter.java`
#### Snippet
```java
     * @return {@code true} if successfully. Otherwise returns {@code false}.
     */
    public boolean writeHeader(short groupType, short messageType, byte fieldCnt);

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/network-api/src/main/java/org/apache/ignite/network/serialization/MessageWriter.java`
#### Snippet
```java
     * @param buf Byte buffer.
     */
    public void setBuffer(ByteBuffer buf);

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/network-api/src/main/java/org/apache/ignite/network/serialization/MessageWriter.java`
#### Snippet
```java
     * Increments a state.
     */
    public void incrementState();

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/network-api/src/main/java/org/apache/ignite/network/serialization/MessageWriter.java`
#### Snippet
```java
     * @return Whether a value was fully written.
     */
    public boolean writeString(String name, String val);

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/network-api/src/main/java/org/apache/ignite/network/serialization/MessageWriter.java`
#### Snippet
```java
     * @return Whether an array was fully written.
     */
    public boolean writeByteArray(String name, byte[] val, long off, int len);

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/network-api/src/main/java/org/apache/ignite/network/serialization/MessageWriter.java`
#### Snippet
```java
     * @return Whether an array was fully written.
     */
    public boolean writeLongArray(String name, long[] val);

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/network-api/src/main/java/org/apache/ignite/network/serialization/MessageWriter.java`
#### Snippet
```java
     * @return Whether an array was fully written.
     */
    public boolean writeFloatArray(String name, float[] val);

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/network-api/src/main/java/org/apache/ignite/network/serialization/MessageWriter.java`
#### Snippet
```java
     * @return Whether a value was fully written.
     */
    public boolean writeLong(String name, long val);

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/network-api/src/main/java/org/apache/ignite/network/serialization/MessageWriter.java`
#### Snippet
```java
     * @return Whether an array was fully written.
     */
    public boolean writeBooleanArray(String name, boolean[] val);

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/network-api/src/main/java/org/apache/ignite/network/serialization/MessageWriter.java`
#### Snippet
```java
     * @return Whether an array was fully written.
     */
    public boolean writeByteArray(String name, byte[] val);

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/network-api/src/main/java/org/apache/ignite/network/serialization/MessageReader.java`
#### Snippet
```java
     * @return {@code int} array.
     */
    public int[] readIntArray(String name);

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/network-api/src/main/java/org/apache/ignite/network/serialization/MessageReader.java`
#### Snippet
```java
     * @return {@code int} value.
     */
    public int readInt(String name);

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/network-api/src/main/java/org/apache/ignite/network/serialization/MessageReader.java`
#### Snippet
```java
     * @return Set.
     */
    public <C extends Set<?>> C readSet(String name, MessageCollectionItemType itemType);

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/network-api/src/main/java/org/apache/ignite/network/serialization/MessageReader.java`
#### Snippet
```java
     * @return {@code double} array.
     */
    public double[] readDoubleArray(String name);

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/network-api/src/main/java/org/apache/ignite/network/serialization/MessageReader.java`
#### Snippet
```java
     * @return {@code float} value.
     */
    public float readFloat(String name);

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/network-api/src/main/java/org/apache/ignite/network/serialization/MessageReader.java`
#### Snippet
```java
     * @return {@link IgniteUuid}.
     */
    public IgniteUuid readIgniteUuid(String name);

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/network-api/src/main/java/org/apache/ignite/network/serialization/MessageReader.java`
#### Snippet
```java
     * @return {@code long} array.
     */
    public long[] readLongArray(String name);

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/network-api/src/main/java/org/apache/ignite/network/serialization/MessageReader.java`
#### Snippet
```java
     * @return Read state.
     */
    public int state();

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/network-api/src/main/java/org/apache/ignite/network/serialization/MessageReader.java`
#### Snippet
```java
     * @return Map.
     */
    public <M extends Map<?, ?>> M readMap(String name, MessageCollectionItemType keyType,
            MessageCollectionItemType valType, boolean linked);

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/network-api/src/main/java/org/apache/ignite/network/serialization/MessageReader.java`
#### Snippet
```java
     * @return Message.
     */
    public <T extends NetworkMessage> T readMessage(String name);

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/network-api/src/main/java/org/apache/ignite/network/serialization/MessageReader.java`
#### Snippet
```java
     * @return {@link BitSet}.
     */
    public BitSet readBitSet(String name);

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/network-api/src/main/java/org/apache/ignite/network/serialization/MessageReader.java`
#### Snippet
```java
     * @return {@code double} value.
     */
    public double readDouble(String name);

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/network-api/src/main/java/org/apache/ignite/network/serialization/MessageReader.java`
#### Snippet
```java
     * @param buf Byte buffer.
     */
    public void setBuffer(ByteBuffer buf);

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/network-api/src/main/java/org/apache/ignite/network/serialization/MessageReader.java`
#### Snippet
```java
     * @return {@code short} array.
     */
    public short[] readShortArray(String name);

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/network-api/src/main/java/org/apache/ignite/network/serialization/MessageReader.java`
#### Snippet
```java
     * @return {@link String}.
     */
    public String readString(String name);

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/network-api/src/main/java/org/apache/ignite/network/serialization/MessageReader.java`
#### Snippet
```java
     * Increments a read state.
     */
    public void incrementState();

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/network-api/src/main/java/org/apache/ignite/network/serialization/MessageReader.java`
#### Snippet
```java
     * @return {@code char} array.
     */
    public char[] readCharArray(String name);

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/network-api/src/main/java/org/apache/ignite/network/serialization/MessageReader.java`
#### Snippet
```java
     * @return {@code byte} array.
     */
    public byte[] readByteArray(String name);

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/network-api/src/main/java/org/apache/ignite/network/serialization/MessageReader.java`
#### Snippet
```java
     * @return {@code boolean} array.
     */
    public boolean[] readBooleanArray(String name);

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/network-api/src/main/java/org/apache/ignite/network/serialization/MessageReader.java`
#### Snippet
```java
     * @return {@code char} value.
     */
    public char readChar(String name);

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/network-api/src/main/java/org/apache/ignite/network/serialization/MessageReader.java`
#### Snippet
```java
     * @param finished Whether a message was fully read.
     */
    public void afterInnerMessageRead(boolean finished);

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/network-api/src/main/java/org/apache/ignite/network/serialization/MessageReader.java`
#### Snippet
```java
     * @param msgCls Message type.
     */
    public void setCurrentReadClass(Class<? extends NetworkMessage> msgCls);

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/network-api/src/main/java/org/apache/ignite/network/serialization/MessageReader.java`
#### Snippet
```java
     * @return Array of objects.
     */
    public <T> T[] readObjectArray(String name, MessageCollectionItemType itemType, Class<T> itemCls);

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/network-api/src/main/java/org/apache/ignite/network/serialization/MessageReader.java`
#### Snippet
```java
     * @return {@code True} if a read operation can be proceeded, {@code false} otherwise.
     */
    public boolean afterMessageRead(Class<? extends NetworkMessage> msgCls);

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/network-api/src/main/java/org/apache/ignite/network/serialization/MessageReader.java`
#### Snippet
```java
     * Resets this reader.
     */
    public void reset();
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/network-api/src/main/java/org/apache/ignite/network/serialization/MessageReader.java`
#### Snippet
```java
     * @return List.
     */
    public <C extends List<?>> C readList(String name, MessageCollectionItemType itemType);

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/network-api/src/main/java/org/apache/ignite/network/serialization/MessageReader.java`
#### Snippet
```java
     * @return {@code float} array.
     */
    public float[] readFloatArray(String name);

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/network-api/src/main/java/org/apache/ignite/network/serialization/MessageReader.java`
#### Snippet
```java
     * @return Collection.
     */
    public <C extends Collection<?>> C readCollection(String name, MessageCollectionItemType itemType);

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/network-api/src/main/java/org/apache/ignite/network/serialization/MessageReader.java`
#### Snippet
```java
     * @return Whether the last value was fully read.
     */
    public boolean isLastRead();

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/network-api/src/main/java/org/apache/ignite/network/serialization/MessageReader.java`
#### Snippet
```java
     * @return {@link UUID}.
     */
    public UUID readUuid(String name);

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/network-api/src/main/java/org/apache/ignite/network/serialization/MessageReader.java`
#### Snippet
```java
     * @return {@code long} value.
     */
    public long readLong(String name);

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/network-api/src/main/java/org/apache/ignite/network/serialization/MessageReader.java`
#### Snippet
```java
     * Callback called before an inner message is read.
     */
    public void beforeInnerMessageRead();

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/network-api/src/main/java/org/apache/ignite/network/serialization/MessageReader.java`
#### Snippet
```java
     * @return {@code boolean} value.
     */
    public boolean readBoolean(String name);

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/network-api/src/main/java/org/apache/ignite/network/serialization/MessageReader.java`
#### Snippet
```java
     * @return {@code int} value.
     */
    public int readInt(String name, int dflt);

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/network-api/src/main/java/org/apache/ignite/network/serialization/MessageReader.java`
#### Snippet
```java
     * @return {@code short} value.
     */
    public short readShort(String name);

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/network-api/src/main/java/org/apache/ignite/network/serialization/MessageReader.java`
#### Snippet
```java
     * @return {@code byte} value.
     */
    public byte readByte(String name);

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/network-api/src/main/java/org/apache/ignite/network/serialization/MessageReader.java`
#### Snippet
```java
     * @return {@code True} if a read operation is allowed to proceed, {@code false} otherwise.
     */
    public boolean beforeMessageRead();

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/mem/MemoryAllocator.java`
#### Snippet
```java
     * @return Pointer to memory or {@code 0} if failed.
     */
    public long allocateMemory(long size);

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/mem/MemoryAllocator.java`
#### Snippet
```java
     * @param addr Address of memory.
     */
    public void freeMemory(long addr);
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/transactions/src/main/java/org/apache/ignite/internal/tx/TxManager.java`
#### Snippet
```java
     */
    @Deprecated
    public LockManager lockManager();

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/transactions/src/main/java/org/apache/ignite/internal/tx/LockManager.java`
#### Snippet
```java
     */
    @TestOnly
    public boolean isEmpty();
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/transactions/src/main/java/org/apache/ignite/internal/tx/LockManager.java`
#### Snippet
```java
     */
    @TestOnly
    public Waiter waiter(LockKey key, UUID txId);

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/transactions/src/main/java/org/apache/ignite/internal/tx/LockManager.java`
#### Snippet
```java
     * @param lock Lock to release.
     */
    public void release(Lock lock);

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/transactions/src/main/java/org/apache/ignite/internal/tx/LockManager.java`
#### Snippet
```java
     * @return An iterator over a collection of locks.
     */
    public Iterator<Lock> locks(UUID txId);

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/transactions/src/main/java/org/apache/ignite/internal/tx/LockManager.java`
#### Snippet
```java
     * @throws LockException When a lock can't be taken due to possible deadlock.
     */
    public CompletableFuture<Lock> acquire(UUID txId, LockKey lockKey, LockMode lockMode);

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/transactions/src/main/java/org/apache/ignite/internal/tx/LockManager.java`
#### Snippet
```java
     */
    @TestOnly
    public Collection<UUID> queue(LockKey key);

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `modules/marshaller-common/src/main/java/org/apache/ignite/internal/util/Factory.java`
#### Snippet
```java
     * @return Object.
     */
    public T create();
}

```

## RuleId[id=MagicConstant]
### MagicConstant
Should be one of: SortedIndexStorage.GREATER, SortedIndexStorage.GREATER_OR_EQUAL, SortedIndexStorage.LESS, ... or their combination
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/replicator/PartitionReplicaListener.java`
#### Snippet
```java
                        lowerBound,
                        upperBound,
                        flags
                ));

```

### MagicConstant
Should be one of: SortedIndexStorage.GREATER, SortedIndexStorage.GREATER_OR_EQUAL, SortedIndexStorage.LESS, ... or their combination
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/replicator/PartitionReplicaListener.java`
#### Snippet
```java
                                // for correctness of taking of a range lock.
                                null,
                                flags
                        ));

```

### MagicConstant
Should be one of: SortedIndexStorage.GREATER, SortedIndexStorage.GREATER_OR_EQUAL, SortedIndexStorage.LESS, ... or their combination
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/AbstractMvTableStorageTest.java`
#### Snippet
```java
        checkStorageDestroyed((IndexStorage) storage);

        assertThrows(StorageClosedException.class, () -> storage.scan(null, null, 0));
    }

```

### MagicConstant
Should be one of: SortedIndexStorage.GREATER, SortedIndexStorage.GREATER_OR_EQUAL, SortedIndexStorage.LESS, ... or their combination
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/AbstractMvTableStorageTest.java`
#### Snippet
```java

        Cursor<RowId> getFromSortedIndexCursor = sortedIndexStorage.get(sortedIndexRow.indexColumns());
        Cursor<IndexRow> scanFromSortedIndexCursor = sortedIndexStorage.scan(null, null, 0);

        assertThat(tableStorage.destroy(), willCompleteSuccessfully());
```

### MagicConstant
Should be one of: SortedIndexStorage.GREATER, SortedIndexStorage.GREATER_OR_EQUAL, SortedIndexStorage.LESS, ... or their combination
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/AbstractMvTableStorageTest.java`
#### Snippet
```java

        Cursor<?> sortedIndexStorageGetCursor = sortedIndexStorage.get(sortedIndexRow.indexColumns());
        Cursor<?> sortedIndexStorageScanCursor = sortedIndexStorage.scan(null, null, 0);

        // Partition is out of configuration range.
```

### MagicConstant
Should be one of: SortedIndexStorage.GREATER, SortedIndexStorage.GREATER_OR_EQUAL, SortedIndexStorage.LESS, ... or their combination
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/AbstractMvTableStorageTest.java`
#### Snippet
```java
        assertThrows(StorageRebalanceException.class, () -> storage.get(mock(BinaryTuple.class)));
        assertThrows(StorageRebalanceException.class, () -> storage.remove(mock(IndexRow.class)));
        assertThrows(StorageRebalanceException.class, () -> storage.scan(null, null, 0));
    }

```

### MagicConstant
Should be one of: SortedIndexStorage.GREATER, SortedIndexStorage.GREATER_OR_EQUAL, SortedIndexStorage.LESS, ... or their combination
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/AbstractMvTableStorageTest.java`
#### Snippet
```java

        Cursor<RowId> getFromSortedIndexCursor = sortedIndexStorage.get(hashIndexRow.indexColumns());
        Cursor<IndexRow> scanFromSortedIndexCursor = sortedIndexStorage.scan(null, null, 0);

        tableStorage.destroyPartition(PARTITION_ID).get(1, SECONDS);
```

### MagicConstant
Should be one of: SortedIndexStorage.GREATER, SortedIndexStorage.GREATER_OR_EQUAL, SortedIndexStorage.LESS, ... or their combination
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/index/AbstractSortedIndexStorageTest.java`
#### Snippet
```java
        put(indexStorage, serializer.serializeRow(new Object[]{2}, rowId1));

        Cursor<IndexRow> scan = indexStorage.scan(null, null, 0);

        // index  = [0, r0] [0, r1] [1, r0] [2, r1]
```

### MagicConstant
Should be one of: SortedIndexStorage.GREATER, SortedIndexStorage.GREATER_OR_EQUAL, SortedIndexStorage.LESS, ... or their combination
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/index/AbstractSortedIndexStorageTest.java`
#### Snippet
```java
        BinaryTupleRowSerializer serializer = new BinaryTupleRowSerializer(indexStorage.indexDescriptor());

        PeekCursor<IndexRow> scan0 = indexStorage.scan(null, null, 0);
        PeekCursor<IndexRow> scan1 = indexStorage.scan(null, null, 0);

```

### MagicConstant
Should be one of: SortedIndexStorage.GREATER, SortedIndexStorage.GREATER_OR_EQUAL, SortedIndexStorage.LESS, ... or their combination
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/index/AbstractSortedIndexStorageTest.java`
#### Snippet
```java

        PeekCursor<IndexRow> scan0 = indexStorage.scan(null, null, 0);
        PeekCursor<IndexRow> scan1 = indexStorage.scan(null, null, 0);

        // index   =
```

### MagicConstant
Should be one of: SortedIndexStorage.GREATER, SortedIndexStorage.GREATER_OR_EQUAL, SortedIndexStorage.LESS, ... or their combination
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/index/AbstractSortedIndexStorageTest.java`
#### Snippet
```java
        // cursor0 = ^ no cached row
        // cursor1 = ^ no cached row
        scan0 = indexStorage.scan(null, null, 0);
        scan1 = indexStorage.scan(null, null, 0);

```

### MagicConstant
Should be one of: SortedIndexStorage.GREATER, SortedIndexStorage.GREATER_OR_EQUAL, SortedIndexStorage.LESS, ... or their combination
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/index/AbstractSortedIndexStorageTest.java`
#### Snippet
```java
        // cursor1 = ^ no cached row
        scan0 = indexStorage.scan(null, null, 0);
        scan1 = indexStorage.scan(null, null, 0);

        assertEquals(0, serializer.deserializeColumns(scan0.peek())[0]);
```

### MagicConstant
Should be one of: SortedIndexStorage.GREATER, SortedIndexStorage.GREATER_OR_EQUAL, SortedIndexStorage.LESS, ... or their combination
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/index/AbstractSortedIndexStorageTest.java`
#### Snippet
```java
        // Checking without borders.
        assertThat(
                scan(indexStorage, null, null, 0, AbstractSortedIndexStorageTest::firstArrayElement),
                contains(0, 1, 2, 3, 4)
        );
```

### MagicConstant
Should be one of: SortedIndexStorage.GREATER, SortedIndexStorage.GREATER_OR_EQUAL, SortedIndexStorage.LESS, ... or their combination
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/index/AbstractSortedIndexStorageTest.java`
#### Snippet
```java
        RowId rowId1 = new RowId(TEST_PARTITION, 0, 1);

        PeekCursor<IndexRow> scan = indexStorage.scan(null, null, 0);

        // index  =  [0, r1]
```

### MagicConstant
Should be one of: SortedIndexStorage.GREATER, SortedIndexStorage.GREATER_OR_EQUAL, SortedIndexStorage.LESS, ... or their combination
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/index/AbstractSortedIndexStorageTest.java`
#### Snippet
```java
        // Test without bounds.
        assertThat(
                scan(index1, null, null, 0),
                contains(val1080, val1090, val2080, val2090)
        );
```

### MagicConstant
Should be one of: SortedIndexStorage.GREATER, SortedIndexStorage.GREATER_OR_EQUAL, SortedIndexStorage.LESS, ... or their combination
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/index/AbstractSortedIndexStorageTest.java`
#### Snippet
```java

        assertThat(
                scan(index2, null, null, 0),
                contains(val1090, val1080, val2090, val2080)
        );
```

### MagicConstant
Should be one of: SortedIndexStorage.GREATER, SortedIndexStorage.GREATER_OR_EQUAL, SortedIndexStorage.LESS, ... or their combination
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/index/AbstractSortedIndexStorageTest.java`
#### Snippet
```java
        BinaryTupleRowSerializer serializer = new BinaryTupleRowSerializer(indexStorage.indexDescriptor());

        Cursor<IndexRow> scan = indexStorage.scan(null, null, 0);

        // index  =
```

### MagicConstant
Should be one of: SortedIndexStorage.GREATER, SortedIndexStorage.GREATER_OR_EQUAL, SortedIndexStorage.LESS, ... or their combination
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/index/AbstractSortedIndexStorageTest.java`
#### Snippet
```java
        assertThrows(NoSuchElementException.class, scan::next);

        scan = indexStorage.scan(null, null, 0);

        // index  = [0]
```

### MagicConstant
Should be one of: SortedIndexStorage.GREATER, SortedIndexStorage.GREATER_OR_EQUAL, SortedIndexStorage.LESS, ... or their combination
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/index/AbstractSortedIndexStorageTest.java`
#### Snippet
```java
        SortedIndexStorage index = createIndexStorage(shuffledRandomDefinitions());

        assertThat(scan(index, null, null, 0), is(empty()));
    }

```

### MagicConstant
Should be one of: SortedIndexStorage.GREATER, SortedIndexStorage.GREATER_OR_EQUAL, SortedIndexStorage.LESS, ... or their combination
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/index/AbstractSortedIndexStorageTest.java`
#### Snippet
```java
        BinaryTupleRowSerializer serializer = new BinaryTupleRowSerializer(indexStorage.indexDescriptor());

        PeekCursor<IndexRow> scan = indexStorage.scan(null, null, 0);

        RowId rowId = new RowId(TEST_PARTITION);
```

### MagicConstant
Should be one of: SortedIndexStorage.GREATER, SortedIndexStorage.GREATER_OR_EQUAL, SortedIndexStorage.LESS, ... or their combination
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/index/AbstractSortedIndexStorageTest.java`
#### Snippet
```java
        RowId rowId2 = new RowId(TEST_PARTITION, 0, 1);

        Cursor<IndexRow> scan = indexStorage.scan(null, null, 0);

        put(indexStorage, serializer.serializeRow(new Object[]{0}, rowId0));
```

### MagicConstant
Should be one of: SortedIndexStorage.GREATER, SortedIndexStorage.GREATER_OR_EQUAL, SortedIndexStorage.LESS, ... or their combination
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/index/AbstractSortedIndexStorageTest.java`
#### Snippet
```java
        put(indexStorage, entry2);

        try (Cursor<IndexRow> cursor = indexStorage.scan(entry2.prefix(indexSchema.size()), entry1.prefix(indexSchema.size()), 0)) {
            assertThat(cursor.stream().collect(toList()), is(empty()));
        }
```

### MagicConstant
Should be one of: SortedIndexStorage.GREATER, SortedIndexStorage.GREATER_OR_EQUAL, SortedIndexStorage.LESS, ... or their combination
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/index/AbstractSortedIndexStorageTest.java`
#### Snippet
```java
        BinaryTupleRowSerializer serializer = new BinaryTupleRowSerializer(indexStorage.indexDescriptor());

        Cursor<IndexRow> scan = indexStorage.scan(null, null, 0);

        // index  =
```

### MagicConstant
Should be one of: SortedIndexStorage.GREATER, SortedIndexStorage.GREATER_OR_EQUAL, SortedIndexStorage.LESS, ... or their combination
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/index/AbstractSortedIndexStorageTest.java`
#### Snippet
```java
        // index  =  [0]
        // cursor = ^ no cached row
        scan = indexStorage.scan(null, null, 0);

        // index  = [0]
```

### MagicConstant
Should be one of: SortedIndexStorage.GREATER, SortedIndexStorage.GREATER_OR_EQUAL, SortedIndexStorage.LESS, ... or their combination
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/index/AbstractSortedIndexStorageTest.java`
#### Snippet
```java
        put(index, row3);

        List<Object[]> actualColumns = scan(index, null, null, 0);

        assertThat(actualColumns, contains(columnValues2, columnValues1, columnValues1));
```

### MagicConstant
Should be one of: SortedIndexStorage.GREATER, SortedIndexStorage.GREATER_OR_EQUAL, SortedIndexStorage.LESS, ... or their combination
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/index/AbstractSortedIndexStorageTest.java`
#### Snippet
```java
        remove(index, row2);

        actualColumns = scan(index, null, null, 0);

        assertThat(actualColumns, contains(columnValues2, columnValues1));
```

### MagicConstant
Should be one of: SortedIndexStorage.GREATER, SortedIndexStorage.GREATER_OR_EQUAL, SortedIndexStorage.LESS, ... or their combination
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/index/AbstractSortedIndexStorageTest.java`
#### Snippet
```java
        remove(index, row2);

        actualColumns = scan(index, null, null, 0);

        assertThat(actualColumns, contains(columnValues2, columnValues1));
```

### MagicConstant
Should be one of: SortedIndexStorage.GREATER, SortedIndexStorage.GREATER_OR_EQUAL, SortedIndexStorage.LESS, ... or their combination
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/index/AbstractSortedIndexStorageTest.java`
#### Snippet
```java
        remove(index, row1);

        actualColumns = scan(index, null, null, 0);

        assertThat(actualColumns, contains((Object) columnValues2));
```

### MagicConstant
Should be one of: SortedIndexStorage.GREATER, SortedIndexStorage.GREATER_OR_EQUAL, SortedIndexStorage.LESS, ... or their combination
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/index/AbstractSortedIndexStorageTest.java`
#### Snippet
```java
        BinaryTupleRowSerializer serializer = new BinaryTupleRowSerializer(indexStorage.indexDescriptor());

        Cursor<IndexRow> scan = indexStorage.scan(null, null, 0);

        RowId rowId = new RowId(TEST_PARTITION);
```

### MagicConstant
Should be one of: SortedIndexStorage.GREATER, SortedIndexStorage.GREATER_OR_EQUAL, SortedIndexStorage.LESS, ... or their combination
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/index/AbstractSortedIndexStorageTest.java`
#### Snippet
```java
        assertThrows(NoSuchElementException.class, scan::next);

        scan = indexStorage.scan(null, null, 0);

        // index  =  [2]
```

### MagicConstant
Should be one of: SortedIndexStorage.GREATER, SortedIndexStorage.GREATER_OR_EQUAL, SortedIndexStorage.LESS, ... or their combination
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/index/AbstractSortedIndexStorageTest.java`
#### Snippet
```java
        RowId rowId1 = new RowId(TEST_PARTITION, 0, 1);

        PeekCursor<IndexRow> scan = indexStorage.scan(null, null, 0);

        put(indexStorage, serializer.serializeRow(new Object[]{0}, rowId0));
```

### MagicConstant
Should be one of: SortedIndexStorage.GREATER, SortedIndexStorage.GREATER_OR_EQUAL, SortedIndexStorage.LESS, ... or their combination
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/index/AbstractSortedIndexStorageTest.java`
#### Snippet
```java
        BinaryTupleRowSerializer serializer = new BinaryTupleRowSerializer(indexStorage.indexDescriptor());

        Cursor<IndexRow> scan = indexStorage.scan(null, null, 0);

        // index  =  [0]
```

### MagicConstant
Should be one of: SortedIndexStorage.GREATER, SortedIndexStorage.GREATER_OR_EQUAL, SortedIndexStorage.LESS, ... or their combination
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/index/AbstractSortedIndexStorageTest.java`
#### Snippet
```java
        put(indexStorage, serializer.serializeRow(new Object[]{2}, rowId));

        Cursor<IndexRow> scan = indexStorage.scan(null, null, 0);

        // index  = [0] [2]
```

### MagicConstant
Should be one of: SortedIndexStorage.GREATER, SortedIndexStorage.GREATER_OR_EQUAL, SortedIndexStorage.LESS, ... or their combination
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/index/AbstractSortedIndexStorageTest.java`
#### Snippet
```java
        put(indexStorage, serializer.serializeRow(new Object[]{2}, rowId));

        Cursor<IndexRow> scan = indexStorage.scan(null, null, 0);

        // index  = [0] [2]
```

### MagicConstant
Should be one of: SortedIndexStorage.GREATER, SortedIndexStorage.GREATER_OR_EQUAL, SortedIndexStorage.LESS, ... or their combination
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/index/AbstractSortedIndexStorageTest.java`
#### Snippet
```java
        BinaryTupleRowSerializer serializer = new BinaryTupleRowSerializer(indexStorage.indexDescriptor());

        Cursor<IndexRow> scan = indexStorage.scan(null, null, 0);

        // index  =
```

### MagicConstant
Should be one of: SortedIndexStorage.GREATER, SortedIndexStorage.GREATER_OR_EQUAL, SortedIndexStorage.LESS, ... or their combination
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/index/AbstractSortedIndexStorageTest.java`
#### Snippet
```java
        // index  =  [0]
        // cursor = ^ no cached row
        scan = indexStorage.scan(null, null, 0);

        // index  = [0]
```

### MagicConstant
Should be one of: SortedIndexStorage.GREATER, SortedIndexStorage.GREATER_OR_EQUAL, SortedIndexStorage.LESS, ... or their combination
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/index/AbstractSortedIndexStorageTest.java`
#### Snippet
```java
        RowId rowId2 = new RowId(TEST_PARTITION, 1, 0);

        Cursor<IndexRow> scan = indexStorage.scan(null, null, 0);

        // index  =  [0, r1]
```

### MagicConstant
Should be one of: SortedIndexStorage.GREATER, SortedIndexStorage.GREATER_OR_EQUAL, SortedIndexStorage.LESS, ... or their combination
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/index/AbstractSortedIndexStorageTest.java`
#### Snippet
```java
        put(index, row3);

        List<Object[]> actualColumns = scan(index, null, null, 0);

        assertThat(actualColumns, contains(columnValues2, columnValues1, columnValues1));
```

## RuleId[id=MarkedForRemoval]
### MarkedForRemoval
'java.security.AccessController' is deprecated and marked for removal
in `modules/core/src/main/java/org/apache/ignite/internal/util/GridUnsafe.java`
#### Snippet
```java
        final ByteBuffer maybeDirectBuf = ByteBuffer.allocateDirect(1);

        Field addrField = AccessController.doPrivileged(new PrivilegedAction<Field>() {
            @Override
            public Field run() {
```

### MarkedForRemoval
'java.security.AccessController' is deprecated and marked for removal
in `modules/core/src/main/java/org/apache/ignite/internal/util/GridUnsafe.java`
#### Snippet
```java
        final ByteBuffer maybeDirectBuf = ByteBuffer.allocateDirect(1);

        Field addrField = AccessController.doPrivileged(new PrivilegedAction<Field>() {
            @Override
            public Field run() {
```

### MarkedForRemoval
'java.security.AccessController' is deprecated and marked for removal
in `modules/core/src/main/java/org/apache/ignite/internal/util/GridUnsafe.java`
#### Snippet
```java
import java.nio.ByteBuffer;
import java.nio.ByteOrder;
import java.security.AccessController;
import java.security.PrivilegedAction;
import java.security.PrivilegedActionException;
```

### MarkedForRemoval
'java.security.AccessController' is deprecated and marked for removal
in `modules/core/src/main/java/org/apache/ignite/internal/util/GridUnsafe.java`
#### Snippet
```java
            Method mtd = cls.getMethod("newDirectByteBuffer", long.class, int.class, Object.class);

            AccessController.doPrivileged((PrivilegedExceptionAction<?>) () -> {
                mtd.setAccessible(true);

```

### MarkedForRemoval
'java.security.AccessController' is deprecated and marked for removal
in `modules/core/src/main/java/org/apache/ignite/internal/util/GridUnsafe.java`
#### Snippet
```java
            Method mtd = cls.getMethod("newDirectByteBuffer", long.class, int.class, Object.class);

            AccessController.doPrivileged((PrivilegedExceptionAction<?>) () -> {
                mtd.setAccessible(true);

```

### MarkedForRemoval
'java.security.AccessController' is deprecated and marked for removal
in `modules/core/src/main/java/org/apache/ignite/internal/util/GridUnsafe.java`
#### Snippet
```java
            Constructor<?> ctor = buf.getClass().getDeclaredConstructor(long.class, int.class);

            AccessController.doPrivileged((PrivilegedExceptionAction<?>) () -> {
                ctor.setAccessible(true);

```

### MarkedForRemoval
'java.security.AccessController' is deprecated and marked for removal
in `modules/core/src/main/java/org/apache/ignite/internal/util/GridUnsafe.java`
#### Snippet
```java
            Constructor<?> ctor = buf.getClass().getDeclaredConstructor(long.class, int.class);

            AccessController.doPrivileged((PrivilegedExceptionAction<?>) () -> {
                ctor.setAccessible(true);

```

### MarkedForRemoval
'java.security.AccessController' is deprecated and marked for removal
in `modules/core/src/main/java/org/apache/ignite/internal/util/GridUnsafe.java`
#### Snippet
```java
        } catch (SecurityException ignored) {
            try {
                return AccessController.doPrivileged(
                        new PrivilegedExceptionAction<Unsafe>() {
                            @Override
```

### MarkedForRemoval
'java.security.AccessController' is deprecated and marked for removal
in `modules/core/src/main/java/org/apache/ignite/internal/util/GridUnsafe.java`
#### Snippet
```java
        } catch (SecurityException ignored) {
            try {
                return AccessController.doPrivileged(
                        new PrivilegedExceptionAction<Unsafe>() {
                            @Override
```

### MarkedForRemoval
'java.lang.SecurityManager' is deprecated and marked for removal
in `modules/jdbc/src/main/java/org/apache/ignite/internal/jdbc/JdbcConnection.java`
#### Snippet
```java
        }

        SecurityManager secMgr = System.getSecurityManager();

        if (secMgr != null) {
```

### MarkedForRemoval
'getSecurityManager()' is deprecated and marked for removal
in `modules/jdbc/src/main/java/org/apache/ignite/internal/jdbc/JdbcConnection.java`
#### Snippet
```java
        }

        SecurityManager secMgr = System.getSecurityManager();

        if (secMgr != null) {
```

### MarkedForRemoval
'java.lang.SecurityManager' is deprecated and marked for removal
in `modules/jdbc/src/main/java/org/apache/ignite/internal/jdbc/JdbcConnection.java`
#### Snippet
```java

        if (secMgr != null) {
            secMgr.checkPermission(new SQLPermission(SET_NETWORK_TIMEOUT_PERM));
        }

```

### MarkedForRemoval
'getSecurityManager()' is deprecated and marked for removal
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/SystemPropertyUtil.java`
#### Snippet
```java
        String value = null;
        try {
            if (System.getSecurityManager() == null) {
                value = System.getProperty(key);
            }
```

### MarkedForRemoval
'java.security.AccessController' is deprecated and marked for removal
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/SystemPropertyUtil.java`
#### Snippet
```java
            }
            else {
                value = AccessController.doPrivileged((PrivilegedAction<String>) () -> System.getProperty(key));
            }
        }
```

### MarkedForRemoval
'java.security.AccessController' is deprecated and marked for removal
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/SystemPropertyUtil.java`
#### Snippet
```java
            }
            else {
                value = AccessController.doPrivileged((PrivilegedAction<String>) () -> System.getProperty(key));
            }
        }
```

### MarkedForRemoval
'java.security.AccessController' is deprecated and marked for removal
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/SystemPropertyUtil.java`
#### Snippet
```java
package org.apache.ignite.raft.jraft.util;

import java.security.AccessController;
import java.security.PrivilegedAction;
import org.apache.ignite.internal.logger.IgniteLogger;
```

## RuleId[id=DoubleCheckedLocking]
### DoubleCheckedLocking
Double-checked locking
in `modules/cli/src/main/java/org/apache/ignite/internal/cli/core/repl/completer/LazyDynamicCompleter.java`
#### Snippet
```java
    @Override
    public List<String> complete(String[] words) {
        if (lazyDelegate == null) {
            synchronized (lock) {
                if (lazyDelegate == null) {
```

## RuleId[id=WhileCanBeForeach]
### WhileCanBeForeach
`while` loop can be replaced with enhanced 'for'
in `modules/core/src/main/java/org/apache/ignite/internal/manager/Producer.java`
#### Snippet
```java
        Iterator<EventListener<P>> iter = eventListeners.iterator();

        while (iter.hasNext()) {
            EventListener<P> closure = iter.next();

```

## RuleId[id=CommentedOutCode]
### CommentedOutCode
Commented out code (14 lines)
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/core/ReplicatorGroupImpl.java`
#### Snippet
```java
        }

//        if (!sync) {
//            final RaftClientService client = opts.getRaftRpcService();
//
```

### CommentedOutCode
Commented out code (33 lines)
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/BytesUtil.java`
#### Snippet
```java
    }

//    /**
//     * This method has better performance than String#getBytes(Charset),
//     * See the benchmark class: Utf8Benchmark for details.
```

### CommentedOutCode
Commented out code (4 lines)
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/StorageOptionsFactory.java`
#### Snippet
```java
        Requires.requireNonNull(cls, "cls");
        Requires.requireNonNull(cfg, "cfg");
//        if (tableFormatConfigTable.putIfAbsent(cls.getName(), cfg) != null) {
//            throw new IllegalStateException("TableFormatConfig with class key [" + cls.getName()
//                                            + "] has already been registered");
```

### CommentedOutCode
Commented out code (597 lines)
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/internal/UnsafeUtil.java`
#### Snippet
```java
 * For the {@link sun.misc.Unsafe} access.
 */
//@SuppressWarnings("ConstantConditions")
//public final class UnsafeUtil {
//
```

### CommentedOutCode
Commented out code (5 lines)
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/internal/Updaters.java`
#### Snippet
```java
    public static <U> LongFieldUpdater<U> newLongFieldUpdater(final Class<? super U> tClass, final String fieldName) {
        try {
//            if (UnsafeUtil.hasUnsafe()) {
//                return new UnsafeLongFieldUpdater<>(UnsafeUtil.getUnsafeAccessor().getUnsafe(), tClass, fieldName);
//            } else {
```

### CommentedOutCode
Commented out code (5 lines)
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/internal/Updaters.java`
#### Snippet
```java
        final String fieldName) {
        try {
//            if (UnsafeUtil.hasUnsafe()) {
//                return new UnsafeIntegerFieldUpdater<>(UnsafeUtil.getUnsafeAccessor().getUnsafe(), tClass, fieldName);
//            } else {
```

### CommentedOutCode
Commented out code (5 lines)
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/internal/Updaters.java`
#### Snippet
```java
        final String fieldName) {
        try {
//            if (UnsafeUtil.hasUnsafe()) {
//                return new UnsafeReferenceFieldUpdater<>(UnsafeUtil.getUnsafeAccessor().getUnsafe(), tClass, fieldName);
//            } else {
```

### CommentedOutCode
Commented out code (5 lines)
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/entity/codec/AutoDetectDecoder.java`
#### Snippet
```java
        return V1Decoder.INSTANCE.decode(bs);

//        if (bs[0] == LogEntryV2CodecFactory.MAGIC_BYTES[0]) {
//            return V2Decoder.INSTANCE.decode(bs);
//        } else {
```

### CommentedOutCode
Commented out code (3 lines)
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/storage/impl/LogManagerImpl.java`
#### Snippet
```java
            // way we can avoid some corner cases which failed to get logs.
            // See https://github.com/baidu/braft/pull/224/commits/8ef6fdbf70d23f5a4ee147356a889e2c0fa22aac
            //            if (this.lastSnapshotId.compareTo(this.diskId) > 0) {
            //                this.diskId = this.lastSnapshotId.copy();
            //            }
```

### CommentedOutCode
Commented out code (2 lines)
in `modules/runner/src/main/java/org/apache/ignite/internal/app/IgniteImpl.java`
#### Snippet
```java

    // TODO: IGNITE-16985 Design table management flow
    // /** Placement driver manager. */
    //private final PlacementDriverManager placementDriverMgr;

```

### CommentedOutCode
Commented out code (12 lines)
in `modules/runner/src/main/java/org/apache/ignite/internal/app/IgniteImpl.java`
#### Snippet
```java

        // TODO: IGNITE-16985 Design table management flow
        // placementDriverMgr = new PlacementDriverManager(
        //         metaStorageMgr,
        //         vaultMgr,
```

### CommentedOutCode
Commented out code (4 lines)
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/RootNode.java`
#### Snippet
```java
        }
        // TODO: rework with SQL error code
        //        if (e instanceof IgniteSQLException)
        //            throw (IgniteSQLException)e;
        //        else
```

### CommentedOutCode
Commented out code (2 lines)
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/asm/ConfigurationImplAsmGenerator.java`
#### Snippet
```java

        // Constructor body.
        // super(parent);
        // this.this$0 = parent;
        constructorMtd.getBody()
```

### CommentedOutCode
Commented out code (5 lines)
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/asm/ConfigurationImplAsmGenerator.java`
#### Snippet
```java
                    );

                    // newValue = new NamedListConfiguration(this.keys, fieldName, rootKey, changer, listenOnly,
                    //      (p, k) -> new ValueConfigurationImpl(p, k, rootKey, changer, listenOnly),
                    //      (p, c) -> new ValueDirectProxy(p, c),
```

### CommentedOutCode
Commented out code (3 lines)
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/asm/InnerNodeAsmGenerator.java`
#### Snippet
```java
            String polymorphicInstanceId = polymorphicInstanceId(polymorphicExtension);

            // case "HashIndexChange":
            //     this.changePolymorphicTypeId("hashIndex");
            //     return new HashIndexNode(this);
```

### CommentedOutCode
Commented out code (3 lines)
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/asm/InnerNodeAsmGenerator.java`
#### Snippet
```java
            );

            // case "hashIndex":
            //     this.changePolymorphicTypeId("hashIndex");
            //     return new HashIndexNode(this);
```

### CommentedOutCode
Commented out code (2 lines)
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/asm/InnerNodeAsmGenerator.java`
#### Snippet
```java
                Variable tmpStrVar = constructMtd.getScope().createTempVariable(String.class);

                // this.field = (FieldType) this.field.copy();
                // if(tmpStr != null) this.field.changeTypeId(tmpStr);
                BytecodeBlock copyWithChange = new BytecodeBlock()
```

### CommentedOutCode
Commented out code (6 lines)
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/asm/InnerNodeAsmGenerator.java`
#### Snippet
```java
                        );

                // this.field = new FieldType();
                // if(tmpStr != null) this.field.changeTypeId(tmpStr);
                // else {
```

## RuleId[id=AssignmentToStaticFieldFromInstanceMethod]
### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `futures` from instance context
in `modules/api/src/testFixtures/java/org/apache/ignite/internal/testframework/IntegrationTestBase.java`
#### Snippet
```java
        String connectNodeAddr = "\"localhost:" + BASE_PORT + '\"';

        futures = IntStream.range(0, nodes())
                .mapToObj(i -> {
                    String nodeName = testNodeName(testInfo, i);
```

### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `coarseCurrentTimeMillis` from instance context
in `modules/core/src/main/java/org/apache/ignite/internal/util/FastTimestamps.java`
#### Snippet
```java
            public void run() {
                while (true) {
                    coarseCurrentTimeMillis = System.currentTimeMillis();
                    try {
                        Thread.sleep(UPDATE_FREQUENCY_MS);
```

## RuleId[id=RegExpRedundantEscape]
### RegExpRedundantEscape
Redundant character escape `\\]` in RegExp
in `modules/cli/src/main/java/org/apache/ignite/internal/cli/config/ini/IniParser.java`
#### Snippet
```java
    public static final String NO_SECTION = "NO_SECTION";

    private static final Pattern SECTION_PATTERN  = Pattern.compile("\\s*\\[([^]]*)\\]\\s*");
    private static final Pattern KEY_VALUE_PATTER = Pattern.compile("\\s*([^=]*)=(.*)");
    private static final Pattern COMMENT_LINE = Pattern.compile("^[;|#].*");
```

## RuleId[id=KeySetIterationMayUseEntrySet]
### KeySetIterationMayUseEntrySet
Iteration over `txOps.futures.keySet()` may be replaced with 'entrySet()' iteration
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/replicator/PartitionReplicaListener.java`
#### Snippet
```java
            }

            for (RequestType opType : txOps.futures.keySet()) {
                if (opType == RequestType.RW_GET || opType == RequestType.RW_GET_ALL || opType == RequestType.RW_SCAN) {
                    txReadFutures.addAll(txOps.futures.get(opType));
```

### KeySetIterationMayUseEntrySet
Iteration over `locks.keySet()` may be replaced with 'entrySet()' iteration
in `modules/transactions/src/main/java/org/apache/ignite/internal/tx/impl/HeapLockManager.java`
#### Snippet
```java
            LockMode newLockMode = null;

            for (LockMode mode : locks.keySet()) {
                assert locks.get(mode) > 0 : "Incorrect lock counter [txId=" + txId + ", mode=" + mode + "]";

```

## RuleId[id=UnnecessaryQualifierForThis]
### UnnecessaryQualifierForThis
Qualifier `Flow` on 'this' is unnecessary in this context
in `modules/cli/src/main/java/org/apache/ignite/internal/cli/core/flow/Flow.java`
#### Snippet
```java
    default <OT> Flow<I, OT> composite(Flow<O, OT> next) {
        return input -> {
            Flowable<O> outputFlowable = Flow.this.start(input);
            return next.start(outputFlowable);
        };
```

### UnnecessaryQualifierForThis
Qualifier `RepeatedTimer` on 'this' is unnecessary in this context
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/RepeatedTimer.java`
#### Snippet
```java
        final TimerTask timerTask = timeout -> {
            try {
                RepeatedTimer.this.run();
            }
            catch (final Throwable t) {
```

### UnnecessaryQualifierForThis
Qualifier `RepeatedTimer` on 'this' is unnecessary in this context
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/RepeatedTimer.java`
#### Snippet
```java
            }
            catch (final Throwable t) {
                LOG.error("Run timer task failed, taskName={}.", t, RepeatedTimer.this.name);
            }
        };
```

### UnnecessaryQualifierForThis
Qualifier `MpscSingleThreadExecutor` on 'this' is unnecessary in this context
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/concurrent/MpscSingleThreadExecutor.java`
#### Snippet
```java
     */
    public void addShutdownHook(final Runnable task) {
        execute(() -> MpscSingleThreadExecutor.this.shutdownHooks.add(task));
    }

```

### UnnecessaryQualifierForThis
Qualifier `MpscSingleThreadExecutor` on 'this' is unnecessary in this context
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/concurrent/MpscSingleThreadExecutor.java`
#### Snippet
```java
     */
    public void removeShutdownHook(final Runnable task) {
        execute(() -> MpscSingleThreadExecutor.this.shutdownHooks.remove(task));
    }

```

### UnnecessaryQualifierForThis
Qualifier `MpscSingleThreadExecutor` on 'this' is unnecessary in this context
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/concurrent/MpscSingleThreadExecutor.java`
#### Snippet
```java
    private void doStartWorker() {
        this.executor.execute(() -> {
            MpscSingleThreadExecutor.this.worker = new Worker(Thread.currentThread());

            try {
```

### UnnecessaryQualifierForThis
Qualifier `MpscSingleThreadExecutor` on 'this' is unnecessary in this context
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/concurrent/MpscSingleThreadExecutor.java`
#### Snippet
```java

            try {
                MpscSingleThreadExecutor.this.worker.run();
            }
            catch (final Throwable t) {
```

### UnnecessaryQualifierForThis
Qualifier `MpscSingleThreadExecutor` on 'this' is unnecessary in this context
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/concurrent/MpscSingleThreadExecutor.java`
#### Snippet
```java
            finally {
                for (; ; ) {
                    int oldState = MpscSingleThreadExecutor.this.state;
                    if (oldState >= ST_SHUTDOWN || STATE_UPDATER.compareAndSet(MpscSingleThreadExecutor.this, oldState, ST_SHUTDOWN)) {
                        break;
```

### UnnecessaryQualifierForThis
Qualifier `MpscSingleThreadExecutor` on 'this' is unnecessary in this context
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/concurrent/MpscSingleThreadExecutor.java`
#### Snippet
```java
                for (; ; ) {
                    int oldState = MpscSingleThreadExecutor.this.state;
                    if (oldState >= ST_SHUTDOWN || STATE_UPDATER.compareAndSet(MpscSingleThreadExecutor.this, oldState, ST_SHUTDOWN)) {
                        break;
                    }
```

### UnnecessaryQualifierForThis
Qualifier `MpscSingleThreadExecutor` on 'this' is unnecessary in this context
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/concurrent/MpscSingleThreadExecutor.java`
#### Snippet
```java
                runShutdownHooks();

                MpscSingleThreadExecutor.this.state = ST_TERMINATED;
                MpscSingleThreadExecutor.this.threadLock.release();
            }
```

### UnnecessaryQualifierForThis
Qualifier `MpscSingleThreadExecutor` on 'this' is unnecessary in this context
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/concurrent/MpscSingleThreadExecutor.java`
#### Snippet
```java

                MpscSingleThreadExecutor.this.state = ST_TERMINATED;
                MpscSingleThreadExecutor.this.threadLock.release();
            }
        });
```

### UnnecessaryQualifierForThis
Qualifier `NodeImpl` on 'this' is unnecessary in this context
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/core/NodeImpl.java`
#### Snippet
```java
        if (opts.getSnapshotTimer() == null && validateOption(opts, "snapshotTimer"))
            opts.setSnapshotTimer(JRaftUtils.createTimer(opts, name));
        snapshotTimer = new RepeatedTimer(name, NodeImpl.this.options.getSnapshotIntervalSecs() * 1000, opts.getSnapshotTimer()) {
            private volatile boolean firstSchedule = true;

```

### UnnecessaryQualifierForThis
Qualifier `NodeImpl` on 'this' is unnecessary in this context
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/core/NodeImpl.java`
#### Snippet
```java
        if (opts.getElectionTimer() == null && validateOption(opts, "electionTimer"))
            opts.setElectionTimer(JRaftUtils.createTimer(opts, name));
        electionTimer = new RepeatedTimer(name, NodeImpl.this.options.getElectionTimeoutMs(), opts.getElectionTimer()) {
            @Override
            protected void onTrigger() {
```

### UnnecessaryQualifierForThis
Qualifier `NodeImpl` on 'this' is unnecessary in this context
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/core/NodeImpl.java`
#### Snippet
```java
        }

        this.voteTimer = new RepeatedTimer(name, NodeImpl.this.options.getElectionTimeoutMs(), opts.getVoteTimer()) {
            @Override
            protected void onTrigger() {
```

### UnnecessaryQualifierForThis
Qualifier `NodeImpl` on 'this' is unnecessary in this context
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/core/NodeImpl.java`
#### Snippet
```java
        if (opts.getStepDownTimer() == null && validateOption(opts, "stepDownTimer"))
            opts.setStepDownTimer(JRaftUtils.createTimer(opts, name));
        stepDownTimer = new RepeatedTimer(name, NodeImpl.this.options.getElectionTimeoutMs() >> 1, opts.getStepDownTimer()) {
            @Override
            protected void onTrigger() {
```

### UnnecessaryQualifierForThis
Qualifier `AsyncResultSetImpl` on 'this' is unnecessary in this context
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/api/AsyncResultSetImpl.java`
#### Snippet
```java
                        curPage = page;

                        return AsyncResultSetImpl.this;
                    });
        }
```

## RuleId[id=SizeReplaceableByIsEmpty]
### SizeReplaceableByIsEmpty
`header.size() > 0` can be replaced with '!header.isEmpty()'
in `modules/cli/src/main/java/org/apache/ignite/internal/cli/sql/table/Table.java`
#### Snippet
```java

    private static String[] parseHeader(List<String> header) {
        if (header.size() > 0) {
            return header.toArray(new String[0]);
        } else {
```

### SizeReplaceableByIsEmpty
`content.size() != 0` can be replaced with '!content.isEmpty()'
in `modules/cli/src/main/java/org/apache/ignite/internal/cli/sql/table/Table.java`
#### Snippet
```java
     */
    public Table(List<String> ids, List<T> content) {
        if (content.size() != 0 && ids.size() != 0 && content.size() % ids.size() != 0) {
            throw new IllegalArgumentException("Content size should be divisible by columns count");
        }
```

### SizeReplaceableByIsEmpty
`ids.size() != 0` can be replaced with '!ids.isEmpty()'
in `modules/cli/src/main/java/org/apache/ignite/internal/cli/sql/table/Table.java`
#### Snippet
```java
     */
    public Table(List<String> ids, List<T> content) {
        if (content.size() != 0 && ids.size() != 0 && content.size() % ids.size() != 0) {
            throw new IllegalArgumentException("Content size should be divisible by columns count");
        }
```

### SizeReplaceableByIsEmpty
`cs.length() == 0` can be replaced with 'cs.isEmpty()'
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/StringUtils.java`
#### Snippet
```java

    public static boolean isEmpty(CharSequence cs) {
        return cs == null || cs.length() == 0;
    }

```

## RuleId[id=NullableProblems]
### NullableProblems
Overridden method parameters are not annotated
in `modules/api/src/main/java/org/apache/ignite/table/Tuple.java`
#### Snippet
```java
     * @throws IllegalArgumentException If column with given name doesn't exists.
     */
    long longValue(@NotNull String columnName);

    /**
```

### NullableProblems
Overridden method parameters are not annotated
in `modules/api/src/main/java/org/apache/ignite/table/Tuple.java`
#### Snippet
```java
     * @throws IllegalArgumentException If column with given name doesn't exists.
     */
    int intValue(@NotNull String columnName);

    /**
```

### NullableProblems
Overridden method parameters are not annotated
in `modules/api/src/main/java/org/apache/ignite/table/Tuple.java`
#### Snippet
```java
     * @throws IllegalArgumentException If column with given name doesn't exists.
     */
    String stringValue(@NotNull String columnName);

    /**
```

### NullableProblems
Overridden method parameters are not annotated
in `modules/api/src/main/java/org/apache/ignite/table/Tuple.java`
#### Snippet
```java
     * @throws IllegalArgumentException If column with given name doesn't exists.
     */
    BitSet bitmaskValue(@NotNull String columnName);

    /**
```

### NullableProblems
Overridden method parameters are not annotated
in `modules/api/src/main/java/org/apache/ignite/table/Tuple.java`
#### Snippet
```java
     * @throws IllegalArgumentException If column with given name doesn't exists.
     */
    <T> T value(@NotNull String columnName) throws IllegalArgumentException;

    /**
```

### NullableProblems
Overridden method parameters are not annotated
in `modules/api/src/main/java/org/apache/ignite/table/Tuple.java`
#### Snippet
```java
     * @return {@code this} for chaining.
     */
    Tuple set(@NotNull String columnName, Object value);

    /**
```

### NullableProblems
Overridden method parameters are not annotated
in `modules/api/src/main/java/org/apache/ignite/table/Tuple.java`
#### Snippet
```java
     * @return Column index, or {@code -1} when a column with given name is not present.
     */
    int columnIndex(@NotNull String columnName);

    /**
```

### NullableProblems
Overridden method parameters are not annotated
in `modules/api/src/main/java/org/apache/ignite/table/Tuple.java`
#### Snippet
```java
     * @throws IllegalArgumentException If column with given name doesn't exists.
     */
    byte byteValue(@NotNull String columnName);

    /**
```

### NullableProblems
Overridden method parameters are not annotated
in `modules/api/src/main/java/org/apache/ignite/table/Tuple.java`
#### Snippet
```java
     * @throws IllegalArgumentException If column with given name doesn't exists.
     */
    float floatValue(@NotNull String columnName);

    /**
```

### NullableProblems
Overridden method parameters are not annotated
in `modules/api/src/main/java/org/apache/ignite/table/Tuple.java`
#### Snippet
```java
     * @return Column value if this tuple contains a column with the specified name. Otherwise returns {@code defaultValue}.
     */
    <T> T valueOrDefault(@NotNull String columnName, T defaultValue);

    /**
```

### NullableProblems
Overridden method parameters are not annotated
in `modules/api/src/main/java/org/apache/ignite/table/Tuple.java`
#### Snippet
```java
     * @throws IllegalArgumentException If column with given name doesn't exists.
     */
    UUID uuidValue(@NotNull String columnName);

    /**
```

### NullableProblems
Overridden method parameters are not annotated
in `modules/api/src/main/java/org/apache/ignite/table/Tuple.java`
#### Snippet
```java
     * @throws IllegalArgumentException If column with given name doesn't exists.
     */
    BinaryObject binaryObjectValue(@NotNull String columnName);

    /**
```

### NullableProblems
Overridden method parameters are not annotated
in `modules/api/src/main/java/org/apache/ignite/table/Tuple.java`
#### Snippet
```java
     * @throws IllegalArgumentException If column with given name doesn't exists.
     */
    double doubleValue(@NotNull String columnName);

    /**
```

### NullableProblems
Overridden method parameters are not annotated
in `modules/api/src/main/java/org/apache/ignite/table/Tuple.java`
#### Snippet
```java
     * @throws IllegalArgumentException If column with given name doesn't exists.
     */
    short shortValue(@NotNull String columnName);

    /**
```

### NullableProblems
Primitive type members cannot be annotated
in `modules/core/src/main/java/org/apache/ignite/internal/util/ArrayUtils.java`
#### Snippet
```java
     * @return Concatenated array.
     */
    public static long[] concat(@Nullable long[] arr, long... longs) {
        if (nullOrEmpty(arr)) {
            return longs;
```

### NullableProblems
Primitive type members cannot be annotated
in `modules/core/src/main/java/org/apache/ignite/internal/tostring/IgniteToStringBuilder.java`
#### Snippet
```java
            Object[] addNames,
            Object[] addVals,
            @Nullable boolean[] addSens,
            int addLen) {
        assert cls != null;
```

### NullableProblems
Primitive type members cannot be annotated
in `modules/core/src/main/java/org/apache/ignite/internal/tostring/IgniteToStringBuilder.java`
#### Snippet
```java
            Object[] addNames,
            Object[] addVals,
            @Nullable boolean[] addSens,
            int addLen
    ) {
```

### NullableProblems
Overridden method parameters are not annotated
in `modules/table/src/main/java/org/apache/ignite/internal/table/InternalTable.java`
#### Snippet
```java
    Publisher<BinaryRow> scan(
            int partId,
            @NotNull HybridTimestamp readTimestamp,
            @NotNull ClusterNode recipientNode,
            @NotNull UUID indexId,
```

### NullableProblems
Overridden method parameters are not annotated
in `modules/table/src/main/java/org/apache/ignite/internal/table/InternalTable.java`
#### Snippet
```java
            int partId,
            @NotNull HybridTimestamp readTimestamp,
            @NotNull ClusterNode recipientNode,
            @NotNull UUID indexId,
            @Nullable BinaryTuplePrefix lowerBound,
```

### NullableProblems
Setter parameter for @Nullable field might be annotated @Nullable itself
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/raft/snapshot/PartitionSnapshotStorage.java`
#### Snippet
```java

    @Override
    public void setSnapshotThrottle(SnapshotThrottle snapshotThrottle) {
        this.snapshotThrottle = snapshotThrottle;
    }
```

### NullableProblems
Overridden method parameters are not annotated
in `modules/schema/src/main/java/org/apache/ignite/internal/schema/NativeType.java`
#### Snippet
```java
     * @return {@code true} if type or typeSpec doesn't match given one, {@code false} otherwise.
     */
    public boolean mismatch(@NotNull NativeType type) {
        return this != type && typeSpec != type.typeSpec;
    }
```

### NullableProblems
Overridden method parameters are not annotated
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/storage/InternalTableImpl.java`
#### Snippet
```java
    public Publisher<BinaryRow> scan(
            int partId,
            @NotNull HybridTimestamp readTimestamp,
            @NotNull ClusterNode recipientNode,
            @Nullable UUID indexId,
```

### NullableProblems
Overridden method parameters are not annotated
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/storage/InternalTableImpl.java`
#### Snippet
```java
            int partId,
            @NotNull HybridTimestamp readTimestamp,
            @NotNull ClusterNode recipientNode,
            @Nullable UUID indexId,
            @Nullable BinaryTuplePrefix lowerBound,
```

### NullableProblems
Primitive type members cannot be annotated
in `modules/network/src/main/java/org/apache/ignite/internal/network/direct/DirectMessageWriter.java`
#### Snippet
```java
    /** {@inheritDoc} */
    @Override
    public boolean writeIntArray(String name, @Nullable int[] val) {
        DirectByteBufferStream stream = state.item().stream;

```

### NullableProblems
Primitive type members cannot be annotated
in `modules/network/src/main/java/org/apache/ignite/internal/network/direct/DirectMessageWriter.java`
#### Snippet
```java
    /** {@inheritDoc} */
    @Override
    public boolean writeDoubleArray(String name, @Nullable double[] val) {
        DirectByteBufferStream stream = state.item().stream;

```

### NullableProblems
Primitive type members cannot be annotated
in `modules/network/src/main/java/org/apache/ignite/internal/network/direct/DirectMessageWriter.java`
#### Snippet
```java
    /** {@inheritDoc} */
    @Override
    public boolean writeLongArray(String name, @Nullable long[] val) {
        DirectByteBufferStream stream = state.item().stream;

```

### NullableProblems
Primitive type members cannot be annotated
in `modules/network/src/main/java/org/apache/ignite/internal/network/direct/DirectMessageWriter.java`
#### Snippet
```java
    /** {@inheritDoc} */
    @Override
    public boolean writeFloatArray(String name, @Nullable float[] val) {
        DirectByteBufferStream stream = state.item().stream;

```

### NullableProblems
Primitive type members cannot be annotated
in `modules/network/src/main/java/org/apache/ignite/internal/network/direct/DirectMessageWriter.java`
#### Snippet
```java
    /** {@inheritDoc} */
    @Override
    public boolean writeBooleanArray(String name, @Nullable boolean[] val) {
        DirectByteBufferStream stream = state.item().stream;

```

### NullableProblems
Primitive type members cannot be annotated
in `modules/network/src/main/java/org/apache/ignite/internal/network/direct/DirectMessageWriter.java`
#### Snippet
```java
    /** {@inheritDoc} */
    @Override
    public boolean writeShortArray(String name, @Nullable short[] val) {
        DirectByteBufferStream stream = state.item().stream;

```

### NullableProblems
Primitive type members cannot be annotated
in `modules/network/src/main/java/org/apache/ignite/internal/network/direct/DirectMessageWriter.java`
#### Snippet
```java
    /** {@inheritDoc} */
    @Override
    public boolean writeByteArray(String name, @Nullable byte[] val) {
        DirectByteBufferStream stream = state.item().stream;

```

### NullableProblems
Primitive type members cannot be annotated
in `modules/network/src/main/java/org/apache/ignite/internal/network/direct/DirectMessageWriter.java`
#### Snippet
```java
    /** {@inheritDoc} */
    @Override
    public boolean writeCharArray(String name, @Nullable char[] val) {
        DirectByteBufferStream stream = state.item().stream;

```

### NullableProblems
Primitive type members cannot be annotated
in `modules/network/src/main/java/org/apache/ignite/internal/network/direct/DirectMessageReader.java`
#### Snippet
```java

    /** {@inheritDoc} */
    @Nullable
    @Override
    public float[] readFloatArray(String name) {
```

### NullableProblems
Primitive type members cannot be annotated
in `modules/network/src/main/java/org/apache/ignite/internal/network/direct/DirectMessageReader.java`
#### Snippet
```java

    /** {@inheritDoc} */
    @Nullable
    @Override
    public short[] readShortArray(String name) {
```

### NullableProblems
Primitive type members cannot be annotated
in `modules/network/src/main/java/org/apache/ignite/internal/network/direct/DirectMessageReader.java`
#### Snippet
```java

    /** {@inheritDoc} */
    @Nullable
    @Override
    public byte[] readByteArray(String name) {
```

### NullableProblems
Primitive type members cannot be annotated
in `modules/network/src/main/java/org/apache/ignite/internal/network/direct/DirectMessageReader.java`
#### Snippet
```java

    /** {@inheritDoc} */
    @Nullable
    @Override
    public long[] readLongArray(String name) {
```

### NullableProblems
Primitive type members cannot be annotated
in `modules/network/src/main/java/org/apache/ignite/internal/network/direct/DirectMessageReader.java`
#### Snippet
```java

    /** {@inheritDoc} */
    @Nullable
    @Override
    public char[] readCharArray(String name) {
```

### NullableProblems
Primitive type members cannot be annotated
in `modules/network/src/main/java/org/apache/ignite/internal/network/direct/DirectMessageReader.java`
#### Snippet
```java

    /** {@inheritDoc} */
    @Nullable
    @Override
    public double[] readDoubleArray(String name) {
```

### NullableProblems
Primitive type members cannot be annotated
in `modules/network/src/main/java/org/apache/ignite/internal/network/direct/DirectMessageReader.java`
#### Snippet
```java

    /** {@inheritDoc} */
    @Nullable
    @Override
    public boolean[] readBooleanArray(String name) {
```

### NullableProblems
Primitive type members cannot be annotated
in `modules/network/src/main/java/org/apache/ignite/internal/network/direct/DirectMessageReader.java`
#### Snippet
```java

    /** {@inheritDoc} */
    @Nullable
    @Override
    public int[] readIntArray(String name) {
```

### NullableProblems
Constructor parameter for @Nullable field might be annotated @Nullable itself
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/LimitNode.java`
#### Snippet
```java
            ExecutionContext<RowT> ctx,
            Supplier<Integer> offsetNode,
            Supplier<Integer> fetchNode
    ) {
        super(ctx);
```

### NullableProblems
Primitive type members cannot be annotated
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/prepare/ddl/CreateTableCommand.java`
#### Snippet
```java
     * Set replicas count.
     */
    @Nullable
    public void replicas(int repl) {
        replicas = repl;
```

### NullableProblems
Primitive type members cannot be annotated
in `modules/metastorage/src/main/java/org/apache/ignite/internal/metastorage/server/AbstractSimpleCondition.java`
#### Snippet
```java
     * @param key Key identifies an entry which the condition will applied to.
     */
    public AbstractSimpleCondition(@NotNull byte[] key) {
        keys = new byte[][]{ key };
    }
```

### NullableProblems
Primitive type members cannot be annotated
in `modules/metastorage/src/main/java/org/apache/ignite/internal/metastorage/server/AbstractSimpleCondition.java`
#### Snippet
```java
    /** {@inheritDoc} */
    @Override
    public @NotNull byte[][] keys() {
        return keys;
    }
```

### NullableProblems
Primitive type members cannot be annotated
in `modules/metastorage/src/main/java/org/apache/ignite/internal/metastorage/server/AbstractSimpleCondition.java`
#### Snippet
```java
public abstract class AbstractSimpleCondition implements Condition {
    /** Entry key. */
    @NotNull
    private final byte[][] keys;

```

### NullableProblems
Primitive type members cannot be annotated
in `modules/metastorage/src/main/java/org/apache/ignite/internal/metastorage/server/AbstractSimpleCondition.java`
#### Snippet
```java

    /** {@inheritDoc} */
    @NotNull
    public byte[] key() {
        return keys[0];
```

### NullableProblems
Primitive type members cannot be annotated
in `modules/metastorage/src/main/java/org/apache/ignite/internal/metastorage/server/ExistenceCondition.java`
#### Snippet
```java
     * @param key  Key of entry which condition will be applied to. Can't be {@code null}.
     */
    public ExistenceCondition(@NotNull Type type, @NotNull byte[] key) {
        super(key);

```

### NullableProblems
Primitive type members cannot be annotated
in `modules/metastorage/src/main/java/org/apache/ignite/internal/metastorage/server/ValueCondition.java`
#### Snippet
```java
     * @param val  Value, which will be tested against an entry value. Can't be {@code null}.
     */
    public ValueCondition(@NotNull Type type, @NotNull byte[] key, @NotNull byte[] val) {
        super(key);

```

### NullableProblems
Primitive type members cannot be annotated
in `modules/metastorage/src/main/java/org/apache/ignite/internal/metastorage/server/ValueCondition.java`
#### Snippet
```java
     * @param val  Value, which will be tested against an entry value. Can't be {@code null}.
     */
    public ValueCondition(@NotNull Type type, @NotNull byte[] key, @NotNull byte[] val) {
        super(key);

```

### NullableProblems
Primitive type members cannot be annotated
in `modules/metastorage/src/main/java/org/apache/ignite/internal/metastorage/server/ValueCondition.java`
#### Snippet
```java

    /** Value, which will be tested against an entry value. */
    @NotNull
    private final byte[] val;

```

### NullableProblems
Primitive type members cannot be annotated
in `modules/metastorage/src/main/java/org/apache/ignite/internal/metastorage/server/AbstractCompoundCondition.java`
#### Snippet
```java
     */
    @Override
    public @NotNull byte[][] keys() {
        return keys;
    }
```

### NullableProblems
Primitive type members cannot be annotated
in `modules/metastorage/src/main/java/org/apache/ignite/internal/metastorage/server/TombstoneCondition.java`
#### Snippet
```java
     * @param key Key identifies an entry which the condition will applied to.
     */
    public TombstoneCondition(@NotNull byte[] key) {
        super(key);
    }
```

### NullableProblems
Primitive type members cannot be annotated
in `modules/metastorage/src/main/java/org/apache/ignite/internal/metastorage/server/Value.java`
#### Snippet
```java
     * @param updCntr Update counter.
     */
    public Value(@NotNull byte[] bytes, long updCntr) {
        this.bytes = bytes;
        this.updCntr = updCntr;
```

### NullableProblems
Primitive type members cannot be annotated
in `modules/metastorage/src/main/java/org/apache/ignite/internal/metastorage/server/RevisionCondition.java`
#### Snippet
```java
     * @param rev  Revision which will be tested against an entry revision. Must be positive.
     */
    public RevisionCondition(@NotNull Type type, @NotNull byte[] key, long rev) {
        super(key);

```

### NullableProblems
Primitive type members cannot be annotated
in `modules/metastorage/src/main/java/org/apache/ignite/internal/metastorage/server/persistence/RocksStorageUtils.java`
#### Snippet
```java
     * @return Array of longs.
     */
    @NotNull
    static long[] getAsLongs(byte[] bytes) {
        // Value must be divisible by a size of a long, because it's a list of longs
```

### NullableProblems
Primitive type members cannot be annotated
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/tree/io/BplusInnerIo.java`
#### Snippet
```java

    @Override
    public @Nullable byte[] insert(
            long pageAddr,
            int idx,
```

### NullableProblems
Primitive type members cannot be annotated
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/tree/io/BplusInnerIo.java`
#### Snippet
```java
            int idx,
            L row,
            @Nullable byte[] rowBytes,
            long rightId,
            boolean needRowBytes
```

### NullableProblems
Primitive type members cannot be annotated
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/tree/io/BplusInnerIo.java`
#### Snippet
```java
     * @throws IgniteInternalCheckedException If failed.
     */
    public @Nullable byte[] initNewRoot(
            long newRootPageAddr,
            long newRootId,
```

### NullableProblems
Primitive type members cannot be annotated
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/tree/io/BplusInnerIo.java`
#### Snippet
```java
            long leftChildId,
            L row,
            @Nullable byte[] rowBytes,
            long rightChildId,
            int pageSize,
```

### NullableProblems
Primitive type members cannot be annotated
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/tree/io/BplusIo.java`
#### Snippet
```java
     * @throws IgniteInternalCheckedException If failed.
     */
    public @Nullable byte[] insert(
            long pageAddr,
            int idx,
```

### NullableProblems
Primitive type members cannot be annotated
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/tree/io/BplusIo.java`
#### Snippet
```java
            int idx,
            L row,
            @Nullable byte[] rowBytes,
            long rightId,
            boolean needRowBytes
```

### NullableProblems
Primitive type members cannot be annotated
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/tree/io/BplusIo.java`
#### Snippet
```java
     * @throws IgniteInternalCheckedException If failed.
     */
    public final @Nullable byte[] store(
            long pageAddr,
            int idx,
```

### NullableProblems
Primitive type members cannot be annotated
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/tree/io/BplusIo.java`
#### Snippet
```java
            int idx,
            L row,
            @Nullable byte[] rowBytes,
            boolean needRowBytes
    ) throws IgniteInternalCheckedException {
```

### NullableProblems
Setter parameter for @Nullable field might be annotated @Nullable itself
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/store/FilePageStore.java`
#### Snippet
```java
     * @param listener New page allocation listener.
     */
    public void setPageAllocationListener(PageAllocationListener listener) {
        pageAllocationListener = listener;
    }
```

### NullableProblems
Constructor parameter for @Nullable field might be annotated @Nullable itself
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/tree/NamedListNode.java`
#### Snippet
```java
         * @param value      Node instance.
         */
        private ElementDescriptor(UUID internalId, InnerNode value) {
            this.internalId = internalId;
            this.value = value;
```

## RuleId[id=IgnoreResultOfCall]
### IgnoreResultOfCall
Result of `File.mkdirs()` is ignored
in `modules/cli/src/main/java/org/apache/ignite/internal/cli/config/StateConfig.java`
#### Snippet
```java
    private static IniFile createDefaultConfig(File file) {
        try {
            file.getParentFile().mkdirs();
            file.delete();
            file.createNewFile();
```

### IgnoreResultOfCall
Result of `File.delete()` is ignored
in `modules/cli/src/main/java/org/apache/ignite/internal/cli/config/StateConfig.java`
#### Snippet
```java
        try {
            file.getParentFile().mkdirs();
            file.delete();
            file.createNewFile();
            IniFile ini = new IniFile(file);
```

### IgnoreResultOfCall
Result of `File.createNewFile()` is ignored
in `modules/cli/src/main/java/org/apache/ignite/internal/cli/config/StateConfig.java`
#### Snippet
```java
            file.getParentFile().mkdirs();
            file.delete();
            file.createNewFile();
            IniFile ini = new IniFile(file);
            ini.store();
```

### IgnoreResultOfCall
Result of `File.mkdirs()` is ignored
in `modules/cli/src/main/java/org/apache/ignite/internal/cli/config/ini/IniConfigManager.java`
#### Snippet
```java
    private static IniFile createDefaultConfig(File file) {
        try {
            file.getParentFile().mkdirs();
            file.delete();
            file.createNewFile();
```

### IgnoreResultOfCall
Result of `File.delete()` is ignored
in `modules/cli/src/main/java/org/apache/ignite/internal/cli/config/ini/IniConfigManager.java`
#### Snippet
```java
        try {
            file.getParentFile().mkdirs();
            file.delete();
            file.createNewFile();
            IniFile ini = new IniFile(file);
```

### IgnoreResultOfCall
Result of `File.createNewFile()` is ignored
in `modules/cli/src/main/java/org/apache/ignite/internal/cli/config/ini/IniConfigManager.java`
#### Snippet
```java
            file.getParentFile().mkdirs();
            file.delete();
            file.createNewFile();
            IniFile ini = new IniFile(file);
            ini.getTopLevelSection().setProperty("current_profile", DEFAULT_PROFILE_NAME);
```

### IgnoreResultOfCall
Result of `File.mkdirs()` is ignored
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/FileOutputSignalHandler.java`
#### Snippet
```java
        }
        else {
            dir.mkdirs();
        }
    }
```

### IgnoreResultOfCall
Result of `File.mkdirs()` is ignored
in `modules/raft/src/testFixtures/java/org/apache/ignite/raft/jraft/core/TestCluster.java`
#### Snippet
```java
            }
            String serverDataPath = this.dataPath + File.separator + peer.getPeerId().toString().replace(':', '_');
            new File(serverDataPath).mkdirs();
            nodeOptions.setLogUri(serverDataPath + File.separator + "logs");
            nodeOptions.setRaftMetaUri(serverDataPath + File.separator + "meta");
```

### IgnoreResultOfCall
Result of `InetAddress.getByName()` is ignored
in `modules/client/src/main/java/org/apache/ignite/internal/client/HostAndPortRange.java`
#### Snippet
```java
                if (addrStr.lastIndexOf(':', colIdx - 1) != -1) { // IPv6 without [] and port
                    try {
                        Inet6Address.getByName(addrStr);
                        host = addrStr;
                        portFrom = dfltPortFrom;
```

### IgnoreResultOfCall
Result of `File.createNewFile()` is ignored
in `modules/runner/src/main/java/org/apache/ignite/internal/configuration/NodeBootstrapConfiguration.java`
#### Snippet
```java

            if (!file.exists()) {
                file.createNewFile();
            }

```

### IgnoreResultOfCall
Result of `RangeCursor.hasNext()` is ignored
in `modules/metastorage/src/testFixtures/java/org/apache/ignite/internal/metastorage/server/SimpleInMemoryKeyValueStorage.java`
#### Snippet
```java
                        return e;
                    } else {
                        hasNext();
                    }
                }
```

### IgnoreResultOfCall
Result of `Iterator.hasNext()` is ignored
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/AbstractMvTableStorageTest.java`
#### Snippet
```java
        assertDoesNotThrow(cursor::close);

        assertThrows(StorageRebalanceException.class, cursor::hasNext);
        assertThrows(StorageRebalanceException.class, cursor::next);

```

### IgnoreResultOfCall
Result of `Iterator.hasNext()` is ignored
in `modules/transactions/src/testFixtures/java/org/apache/ignite/internal/tx/storage/state/AbstractTxStateStorageTest.java`
#### Snippet
```java
        checkTxStateStorageMethodsWhenRebalanceInProgress(storage);

        assertThrowsIgniteInternalException(TX_STATE_STORAGE_REBALANCE_ERR, scanCursorBeforeStartRebalance::hasNext);
        assertThrowsIgniteInternalException(TX_STATE_STORAGE_REBALANCE_ERR, scanCursorBeforeStartRebalance::next);

```

## RuleId[id=UnnecessaryUnboxing]
### UnnecessaryUnboxing
Unnecessary unboxing
in `modules/distribution-zones/src/main/java/org/apache/ignite/internal/distributionzones/DistributionZoneManager.java`
#### Snippet
```java
            }

            int newScaleDown = ctx.newValue().intValue();

            // It is safe to zonesTimers.get(zoneId) in term of NPE because meta storage notifications are one-threaded
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `modules/distribution-zones/src/main/java/org/apache/ignite/internal/distributionzones/DistributionZoneManager.java`
#### Snippet
```java
            }

            int newScaleUp = ctx.newValue().intValue();

            // It is safe to zonesTimers.get(zoneId) in term of NPE because meta storage notifications are one-threaded
```

## RuleId[id=AccessStaticViaInstance]
### AccessStaticViaInstance
Static member 'org.apache.ignite.internal.tx.LockMode.supremum(org.apache.ignite.internal.tx.LockMode, org.apache.ignite.internal.tx.LockMode)' accessed via instance reference
in `modules/transactions/src/main/java/org/apache/ignite/internal/tx/impl/HeapLockManager.java`
#### Snippet
```java

                if (waiter != null) {
                    assert lockMode.supremum(lockMode, waiter.lockMode()) == waiter.lockMode() :
                            "The lock mode is not locked [mode=" + lockMode + ", locked=" + waiter.lockMode() + ']';

```

## RuleId[id=RedundantMethodOverride]
### RedundantMethodOverride
Method `toString()` only delegates to its super method
in `modules/cli/src/main/java/org/apache/ignite/internal/cli/core/exception/ConnectCommandException.java`
#### Snippet
```java

    @Override
    public String toString() {
        return super.toString();
    }
```

### RedundantMethodOverride
Method `toString()` only delegates to its super method
in `modules/cli/src/main/java/org/apache/ignite/internal/cli/core/exception/CommandExecutionException.java`
#### Snippet
```java

    @Override
    public String toString() {
        return super.toString();
    }
```

### RedundantMethodOverride
Method `onAppeared()` is identical to its super method
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/rpc/impl/IgniteRpcServer.java`
#### Snippet
```java

        service.topologyService().addEventHandler(new TopologyEventHandler() {
            @Override public void onAppeared(ClusterNode member) {
                // TODO https://issues.apache.org/jira/browse/IGNITE-14837
                // Perhaps, We can remove checking for dead nodes and replace it with SWIM node alive event
```

### RedundantMethodOverride
Method `onAppeared()` is identical to its super method
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/rpc/impl/AbstractClientService.java`
#### Snippet
```java
    }

    @Override public void onAppeared(ClusterNode member) {
        // TODO https://issues.apache.org/jira/browse/IGNITE-14837
        // Perhaps, We can remove checking for dead nodes and replace it with SWIM node alive event
```

### RedundantMethodOverride
Method `getConfigurationByPath()` only delegates to its super method
in `modules/rest/src/main/java/org/apache/ignite/internal/rest/configuration/NodeConfigurationController.java`
#### Snippet
```java
     */
    @Override
    public String getConfigurationByPath(String path) {
        return super.getConfigurationByPath(path);
    }
```

### RedundantMethodOverride
Method `updateConfiguration()` only delegates to its super method
in `modules/rest/src/main/java/org/apache/ignite/internal/rest/configuration/NodeConfigurationController.java`
#### Snippet
```java
     */
    @Override
    public CompletableFuture<Void> updateConfiguration(String updatedConfiguration) {
        return super.updateConfiguration(updatedConfiguration);
    }
```

### RedundantMethodOverride
Method `getConfiguration()` only delegates to its super method
in `modules/rest/src/main/java/org/apache/ignite/internal/rest/configuration/NodeConfigurationController.java`
#### Snippet
```java
     */
    @Override
    public String getConfiguration() {
        return super.getConfiguration();
    }
```

### RedundantMethodOverride
Method `binaryObjectValue()` only delegates to its super method
in `modules/table/src/main/java/org/apache/ignite/internal/table/MutableRowTupleAdapter.java`
#### Snippet
```java
    /** {@inheritDoc} */
    @Override
    public BinaryObject binaryObjectValue(int columnIndex) {
        return super.binaryObjectValue(columnIndex);
    }
```

### RedundantMethodOverride
Method `get()` only delegates to its super method
in `modules/table/src/testFixtures/java/org/apache/ignite/internal/table/impl/DummyInternalTableImpl.java`
#### Snippet
```java
    /** {@inheritDoc} */
    @Override
    public CompletableFuture<BinaryRow> get(BinaryRowEx keyRow, InternalTransaction tx) {
        return super.get(keyRow, tx);
    }
```

### RedundantMethodOverride
Method `readAllBytes()` only delegates to its super method
in `modules/network/src/main/java/org/apache/ignite/internal/network/serialization/marshal/UosObjectInputStream.java`
#### Snippet
```java

    @Override
    public byte[] readAllBytes() throws IOException {
        return super.readAllBytes();
    }
```

### RedundantMethodOverride
Method `visitSource()` only delegates to its super method
in `modules/bytecode/src/main/java/com/facebook/presto/bytecode/AddFakeLineNumberClassVisitor.java`
#### Snippet
```java

    @Override
    public void visitSource(String source, String debug)
    {
        super.visitSource(source, debug);
```

### RedundantMethodOverride
Method `onAppeared()` is identical to its super method
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/MailboxRegistryImpl.java`
#### Snippet
```java
    /** {@inheritDoc} */
    @Override
    public void onAppeared(ClusterNode member) {
        // NO-OP
    }
```

### RedundantMethodOverride
Method `getChildNodes()` is identical to its super method
in `modules/bytecode/src/main/java/com/facebook/presto/bytecode/instruction/InvokeInstruction.java`
#### Snippet
```java

        @Override
        public List<BytecodeNode> getChildNodes() {
            return List.of();
        }
```

### RedundantMethodOverride
Method `onAppeared()` is identical to its super method
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/ExecutionServiceImpl.java`
#### Snippet
```java
    /** {@inheritDoc} */
    @Override
    public void onAppeared(ClusterNode member) {
        // NO_OP
    }
```

## RuleId[id=ClassNameSameAsAncestorName]
### ClassNameSameAsAncestorName
Class name `Iterator` is the same as one of its superclass' names
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/Iterator.java`
#### Snippet
```java
 * @see StateMachine#onApply(Iterator)
 */
public interface Iterator extends java.util.Iterator<ByteBuffer> {

    /**
```

### ClassNameSameAsAncestorName
Class name `ConfigurationListener` is the same as one of its superclass' names
in `modules/index/src/main/java/org/apache/ignite/internal/index/IndexManager.java`
#### Snippet
```java
    }

    private class ConfigurationListener implements ConfigurationNamedListListener<TableIndexView> {
        /** {@inheritDoc} */
        @Override
```

### ClassNameSameAsAncestorName
Class name `Config` is the same as one of its superclass' names
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rule/UnionConverterRule.java`
#### Snippet
```java
     */
    @Value.Immutable
    public interface Config extends RelRule.Config {
        UnionConverterRule.Config DEFAULT = ImmutableUnionConverterRule.Config.of()
                .withDescription("UnionConverterRule")
```

### ClassNameSameAsAncestorName
Class name `Config` is the same as one of its superclass' names
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rule/SortConverterRule.java`
#### Snippet
```java
    /** Rule configuration. */
    @Value.Immutable
    public interface Config extends RelRule.Config {
        SortConverterRule.Config DEFAULT = ImmutableSortConverterRule.Config.of()
                .withOperandSupplier(b ->
```

### ClassNameSameAsAncestorName
Class name `Config` is the same as one of its superclass' names
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rule/logical/LogicalOrToUnionRule.java`
#### Snippet
```java
     */
    @Value.Immutable(singleton = false)
    public interface Config extends RuleFactoryConfig<Config> {
        Config SCAN = ImmutableLogicalOrToUnionRule.Config.builder()
                .withRuleFactory(LogicalOrToUnionRule::new)
```

## RuleId[id=RegExpDuplicateCharacterInClass]
### RegExpDuplicateCharacterInClass
Duplicate character '\\\\\\\\' inside character class
in `modules/client-handler/src/main/java/org/apache/ignite/client/handler/requests/jdbc/JdbcMetadataCatalog.java`
#### Snippet
```java
        String toRegex = ' ' + sqlPtrn;

        toRegex = toRegex.replaceAll("([\\[\\]{}()*+?.\\\\\\\\^$|])", "\\\\$1");
        toRegex = toRegex.replaceAll("([^\\\\\\\\])((?:\\\\\\\\\\\\\\\\)*)%", "$1$2.*");
        toRegex = toRegex.replaceAll("([^\\\\\\\\])((?:\\\\\\\\\\\\\\\\)*)_", "$1$2.");
```

### RegExpDuplicateCharacterInClass
Duplicate character '\\\\\\\\' inside character class
in `modules/client-handler/src/main/java/org/apache/ignite/client/handler/requests/jdbc/JdbcMetadataCatalog.java`
#### Snippet
```java

        toRegex = toRegex.replaceAll("([\\[\\]{}()*+?.\\\\\\\\^$|])", "\\\\$1");
        toRegex = toRegex.replaceAll("([^\\\\\\\\])((?:\\\\\\\\\\\\\\\\)*)%", "$1$2.*");
        toRegex = toRegex.replaceAll("([^\\\\\\\\])((?:\\\\\\\\\\\\\\\\)*)_", "$1$2.");
        toRegex = toRegex.replaceAll("([^\\\\\\\\])(\\\\\\\\(?>\\\\\\\\\\\\\\\\)*\\\\\\\\)*\\\\\\\\([_|%])", "$1$2$3");
```

### RegExpDuplicateCharacterInClass
Duplicate character '\\\\\\\\' inside character class
in `modules/client-handler/src/main/java/org/apache/ignite/client/handler/requests/jdbc/JdbcMetadataCatalog.java`
#### Snippet
```java
        toRegex = toRegex.replaceAll("([\\[\\]{}()*+?.\\\\\\\\^$|])", "\\\\$1");
        toRegex = toRegex.replaceAll("([^\\\\\\\\])((?:\\\\\\\\\\\\\\\\)*)%", "$1$2.*");
        toRegex = toRegex.replaceAll("([^\\\\\\\\])((?:\\\\\\\\\\\\\\\\)*)_", "$1$2.");
        toRegex = toRegex.replaceAll("([^\\\\\\\\])(\\\\\\\\(?>\\\\\\\\\\\\\\\\)*\\\\\\\\)*\\\\\\\\([_|%])", "$1$2$3");

```

### RegExpDuplicateCharacterInClass
Duplicate character '\\\\\\\\' inside character class
in `modules/client-handler/src/main/java/org/apache/ignite/client/handler/requests/jdbc/JdbcMetadataCatalog.java`
#### Snippet
```java
        toRegex = toRegex.replaceAll("([^\\\\\\\\])((?:\\\\\\\\\\\\\\\\)*)%", "$1$2.*");
        toRegex = toRegex.replaceAll("([^\\\\\\\\])((?:\\\\\\\\\\\\\\\\)*)_", "$1$2.");
        toRegex = toRegex.replaceAll("([^\\\\\\\\])(\\\\\\\\(?>\\\\\\\\\\\\\\\\)*\\\\\\\\)*\\\\\\\\([_|%])", "$1$2$3");

        return toRegex.substring(1);
```

## RuleId[id=IntegerMultiplicationImplicitCastToLong]
### IntegerMultiplicationImplicitCastToLong
cnt \* getItemSize(): integer multiplication implicitly cast to long
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/tree/io/BplusLeafIo.java`
#### Snippet
```java
        assertPageType(dstPageAddr);

        copyMemory(srcPageAddr, offset(srcIdx), dstPageAddr, offset(dstIdx), cnt * getItemSize());
    }

```

### IntegerMultiplicationImplicitCastToLong
8 \* (cnt - i - 1): integer multiplication implicitly cast to long
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/freelist/io/PagesListNodeIo.java`
#### Snippet
```java
            if (maskPartitionId(getAt(pageAddr, i)) == maskPartitionId(dataPageId)) {
                if (i != cnt - 1) {
                    copyMemory(pageAddr, offset(i + 1), pageAddr, offset(i), 8 * (cnt - i - 1));
                }

```

### IntegerMultiplicationImplicitCastToLong
b \<\< shift: integer shift implicitly cast to long
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/freelist/AbstractFreeList.java`
#### Snippet
```java

        for (int b = BUCKETS - 2; b > 0; b--) {
            long perPageFreeSpace = b << shift;

            long pages = bucketsSize.get(b);
```

### IntegerMultiplicationImplicitCastToLong
pagesCnt \* 8: integer multiplication implicitly cast to long
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/replacement/SegmentedLruPageList.java`
#### Snippet
```java
     */
    public static long requiredMemory(int pagesCnt) {
        return pagesCnt * 8 /* links = 2 ints per page */
                + ((pagesCnt + 63) / 8) & (~7L) /* protected flags = 1 bit per page + 8 byte align */;
    }
```

## RuleId[id=UNUSED_IMPORT]
### UNUSED_IMPORT
Unused import `import org.apache.ignite.network.NetworkAddress;`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/JRaftUtils.java`
#### Snippet
```java
import org.apache.ignite.internal.logger.Loggers;
import org.apache.ignite.internal.thread.NamedThreadFactory;
import org.apache.ignite.network.NetworkAddress;
import org.apache.ignite.raft.jraft.conf.Configuration;
import org.apache.ignite.raft.jraft.core.NodeImpl;
```

### UNUSED_IMPORT
Unused import `import org.apache.ignite.internal.thread.NamedThreadFactory;`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/disruptor/DisruptorBuilder.java`
#### Snippet
```java
import com.lmax.disruptor.dsl.Disruptor;
import com.lmax.disruptor.dsl.ProducerType;
import org.apache.ignite.internal.thread.NamedThreadFactory;
import org.apache.ignite.raft.jraft.util.Requires;

```

## RuleId[id=NestedAssignment]
### NestedAssignment
Result of assignment expression used
in `modules/cli/src/main/java/org/apache/ignite/internal/cli/config/ini/IniParser.java`
#### Snippet
```java
        map.put(NO_SECTION, currentSection);
        String line;
        while ((line = bufferedReader.readLine()) != null) {

            Matcher commentMatcher = COMMENT_LINE.matcher(line);
```

### NestedAssignment
Result of assignment expression used
in `modules/core/src/main/java/org/apache/ignite/internal/util/IgniteConcurrentMultiPairQueue.java`
#### Snippet
```java
            keysArr[keyPos] = p.getKey();

            lenSeq[keyPos] = size += p.getValue().length;

            vals[keyPos++] = p.getValue();
```

### NestedAssignment
Result of assignment expression used
in `modules/core/src/main/java/org/apache/ignite/internal/util/IgniteConcurrentMultiPairQueue.java`
#### Snippet
```java
            keysArr[keyPos] = p.getKey();

            lenSeq[keyPos] = size += p.getValue().size();

            vals[keyPos++] = (V[]) p.getValue().toArray();
```

### NestedAssignment
Result of assignment expression used
in `modules/core/src/main/java/org/apache/ignite/internal/util/io/IgniteUnsafeDataInput.java`
#### Snippet
```java
        // Increase size of buffer if needed.
        if (size > inBuf.length) {
            buf = inBuf = new byte[Math.max(inBuf.length << 1, size)]; // Grow.
        } else if (isAutoShrinkEnabled()) {
            long now = FastTimestamps.coarseCurrentTimeMillis();
```

### NestedAssignment
Result of assignment expression used
in `modules/core/src/main/java/org/apache/ignite/internal/util/io/IgniteUnsafeDataInput.java`
#### Snippet
```java
                    System.arraycopy(inBuf, 0, newInBuf, 0, off);

                    buf = inBuf = newInBuf;
                }

```

### NestedAssignment
Result of assignment expression used
in `modules/core/src/main/java/org/apache/ignite/internal/util/io/IgniteUnsafeDataInput.java`
#### Snippet
```java
        StringBuilder sbuf = new StringBuilder();

        end = pos = 0;

        while (utfLen > 0) {
```

### NestedAssignment
Result of assignment expression used
in `modules/core/src/main/java/org/apache/ignite/internal/util/io/IgniteUnsafeDataInput.java`
#### Snippet
```java
        int b;

        while ((b = read()) >= 0) {
            char c = (char) b;

```

### NestedAssignment
Result of assignment expression used
in `modules/core/src/main/java/org/apache/ignite/internal/util/ExceptionUtils.java`
#### Snippet
```java
                result.addAll(getSuppressedList(suppressed));
            }
        } while ((t = t.getCause()) != null);

        return result;
```

### NestedAssignment
Result of assignment expression used
in `modules/core/src/main/java/org/apache/ignite/internal/util/IgniteUtils.java`
#### Snippet
```java

        if (ldrMap == null) {
            ConcurrentMap<String, Class<?>> old = classCache.putIfAbsent(ldr, ldrMap = new ConcurrentHashMap<>());

            if (old != null) {
```

### NestedAssignment
Result of assignment expression used
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/conf/Configuration.java`
#### Snippet
```java
            int index;
            boolean isLearner = false;
            if ((index = peerStr.indexOf(LEARNER_POSTFIX)) > 0) {
                // It's a learner
                peerStr = peerStr.substring(0, index);
```

### NestedAssignment
Result of assignment expression used
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/StringUtils.java`
#### Snippet
```java
    public static boolean isBlank(CharSequence cs) {
        int strLen;
        if (cs != null && (strLen = cs.length()) != 0) {
            for (int i = 0; i < strLen; ++i) {
                if (!Character.isWhitespace(cs.charAt(i))) {
```

### NestedAssignment
Result of assignment expression used
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/SegmentList.java`
#### Snippet
```java
            RecycleUtil.recycle(this.segments.pollLast());
        }
        this.size = this.firstOffset = 0;
    }

```

### NestedAssignment
Result of assignment expression used
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/SegmentList.java`
#### Snippet
```java
        }
        else {
            this.firstOffset = this.size = 0;
        }
    }
```

### NestedAssignment
Result of assignment expression used
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/SegmentList.java`
#### Snippet
```java
        while (true) {
            if (lastSeg == null) {
                this.firstOffset = this.size = 0;
                return;
            }
```

### NestedAssignment
Result of assignment expression used
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/SegmentList.java`
#### Snippet
```java

        void clear() {
            this.pos = this.offset = 0;
            Arrays.fill(this.elements, null);
        }
```

### NestedAssignment
Result of assignment expression used
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/SegmentList.java`
#### Snippet
```java
        Segment(final Recyclers.Handle handle) {
            this.elements = (T[]) new Object[SEGMENT_SIZE];
            this.pos = this.offset = 0;
            this.handle = handle;
        }
```

### NestedAssignment
Result of assignment expression used
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/SegmentList.java`
#### Snippet
```java
        while (true) {
            if (firstSeg == null) {
                this.firstOffset = this.size = 0;
                return;
            }
```

### NestedAssignment
Result of assignment expression used
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/Recyclers.java`
#### Snippet
```java
            Link tail = this.tail;
            int writeIndex;
            if ((writeIndex = tail.get()) == LINK_CAPACITY) {
                this.tail = tail = tail.next = new Link();
                writeIndex = tail.get();
```

### NestedAssignment
Result of assignment expression used
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/Recyclers.java`
#### Snippet
```java
            int writeIndex;
            if ((writeIndex = tail.get()) == LINK_CAPACITY) {
                this.tail = tail = tail.next = new Link();
                writeIndex = tail.get();
            }
```

### NestedAssignment
Result of assignment expression used
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/Recyclers.java`
#### Snippet
```java
            int writeIndex;
            if ((writeIndex = tail.get()) == LINK_CAPACITY) {
                this.tail = tail = tail.next = new Link();
                writeIndex = tail.get();
            }
```

### NestedAssignment
Result of assignment expression used
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/Recyclers.java`
#### Snippet
```java
            WeakOrderQueue queue = delayedRecycled.get(stack);
            if (queue == null) {
                delayedRecycled.put(stack, queue = new WeakOrderQueue(stack, thread));
            }
            queue.add(this);
```

### NestedAssignment
Result of assignment expression used
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/Recyclers.java`
#### Snippet
```java
                throw new IllegalStateException("recycled already");
            }
            item.recycleId = item.lastRecycledId = OWN_THREAD_ID;

            int size = this.size;
```

### NestedAssignment
Result of assignment expression used
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/Recyclers.java`
#### Snippet
```java
                    return false;
                }
                this.head = head = head.next;
            }

```

### NestedAssignment
Result of assignment expression used
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/Recyclers.java`
#### Snippet
```java

        WeakOrderQueue(Stack<?> stack, Thread thread) {
            head = tail = new Link();
            owner = new WeakReference<>(thread);
            synchronized (stackLock(stack)) {
```

### NestedAssignment
Result of assignment expression used
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/concurrent/MpscSingleThreadExecutor.java`
#### Snippet
```java
        private void runAllTasks() {
            Runnable task;
            while ((task = pollTask()) != null) {
                runTask(task);
            }
```

### NestedAssignment
Result of assignment expression used
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/timer/HashedWheelTimer.java`
#### Snippet
```java
            HashedWheelTimeout next = head.next;
            if (next == null) {
                tail = this.head = null;
            }
            else {
```

### NestedAssignment
Result of assignment expression used
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/timer/HashedWheelTimer.java`
#### Snippet
```java
            timeout.bucket = this;
            if (head == null) {
                head = tail = timeout;
            }
            else {
```

### NestedAssignment
Result of assignment expression used
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/entity/Ballot.java`
#### Snippet
```java
        this.peers.clear();
        this.oldPeers.clear();
        this.quorum = this.oldQuorum = 0;
        int index = 0;
        if (conf != null) {
```

### NestedAssignment
Result of assignment expression used
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/core/Replicator.java`
#### Snippet
```java
        final long startTimeMs = Utils.nowMs();
        Replicator r;
        if ((r = (Replicator) id.lock()) == null) {
            return;
        }
```

### NestedAssignment
Result of assignment expression used
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/core/Replicator.java`
#### Snippet
```java
        this.pendingResponses.clear();
        final int rs = Math.max(this.reqSeq, this.requiredNextSeq);
        this.reqSeq = this.requiredNextSeq = rs;
        releaseReader();
    }
```

### NestedAssignment
Result of assignment expression used
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/core/Replicator.java`
#### Snippet
```java
        final long startTimeMs = Utils.nowMs();
        Replicator r;
        if ((r = (Replicator) id.lock()) == null) {
            return;
        }
```

### NestedAssignment
Result of assignment expression used
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/storage/io/MessageFile.java`
#### Snippet
```java
    private void readBytes(final byte[] bs, final InputStream input) throws IOException {
        int read;
        if ((read = input.read(bs)) != bs.length) {
            throw new IOException("Read error, expects " + bs.length + " bytes, but read " + read);
        }
```

### NestedAssignment
Result of assignment expression used
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/storage/snapshot/local/LocalSnapshotCopier.java`
#### Snippet
```java
                continue;
            }
            if ((localMeta = (LocalFileMeta) lastSnapshot.getFileMeta(fileName)) == null) {
                continue;
            }
```

### NestedAssignment
Result of assignment expression used
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/core/NodeImpl.java`
#### Snippet
```java
            if (peerId.isEmpty()) {
                LOG.info("Node {} starts to transfer leadership to any peer.", getNodeId());
                if ((peerId = this.replicatorGroup.findTheNextCandidate(this.conf)) == null) {
                    return new Status(-1, "Candidate not found for any peer");
                }
```

### NestedAssignment
Result of assignment expression used
in `modules/core/src/main/java/org/apache/ignite/internal/tostring/IgniteToStringBuilder.java`
#### Snippet
```java
            sb.app(',').app(' ');

            left = right = val;
        }

```

### NestedAssignment
Result of assignment expression used
in `modules/table/src/main/java/org/apache/ignite/internal/table/RecordViewImpl.java`
#### Snippet
```java
        // TODO: Cache marshaller for schema version or upgrade row?

        return this.marsh = marshallerFactory.apply(schemaReg.schema(schemaVersion));
    }

```

### NestedAssignment
Result of assignment expression used
in `modules/table/src/main/java/org/apache/ignite/internal/table/KeyValueViewImpl.java`
#### Snippet
```java
        // TODO: Cache marshaller for schema version or upgrade row?

        return this.marsh = marshallerFactory.apply(schemaReg.schema(schemaVersion));
    }

```

### NestedAssignment
Result of assignment expression used
in `modules/schema/src/main/java/org/apache/ignite/internal/schema/row/Row.java`
#### Snippet
```java
            }

            colocationHash = h0 = hashCalc.hash();
        }

```

### NestedAssignment
Result of assignment expression used
in `modules/schema/src/main/java/org/apache/ignite/internal/schema/registry/SchemaRegistryImpl.java`
#### Snippet
```java
        ColumnMapper mapping;

        if ((mapping = mappingCache.get(mappingKey)) != null) {
            return mapping;
        }
```

### NestedAssignment
Result of assignment expression used
in `modules/affinity/src/main/java/org/apache/ignite/internal/affinity/RendezvousAffinityFunction.java`
#### Snippet
```java

            if (nodes == null) {
                macMap.put(macs, nodes = new HashSet<>());
            }

```

### NestedAssignment
Result of assignment expression used
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/RootNode.java`
#### Snippet
```java
                    close();
                } else if (inBuff.isEmpty() && waiting == 0) {
                    int req = waiting = inBufSize;
                    context().execute(() -> source().request(req), this::onError);
                }
```

### NestedAssignment
Result of assignment expression used
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/AbstractSetOpNode.java`
#### Snippet
```java

        if (waiting == 0) {
            sources().get(curSrcIdx).request(waiting = inBufSize);
        } else if (!inLoop) {
            context().execute(this::flush, this::onError);
```

### NestedAssignment
Result of assignment expression used
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/AbstractSetOpNode.java`
#### Snippet
```java

        if (waiting == 0) {
            sources().get(curSrcIdx).request(waiting = inBufSize);
        }
    }
```

### NestedAssignment
Result of assignment expression used
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/AsyncRootNode.java`
#### Snippet
```java

        if (waiting == 0) {
            source.request(waiting = IN_BUFFER_SIZE);
        } else if (waiting == -1 && buff.isEmpty()) {
            closeAsync();
```

### NestedAssignment
Result of assignment expression used
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/AsyncRootNode.java`
#### Snippet
```java
        if (waiting == 0) {
            try {
                source.request(waiting = IN_BUFFER_SIZE);
            } catch (Exception ex) {
                onError(ex);
```

### NestedAssignment
Result of assignment expression used
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/UnionAllNode.java`
#### Snippet
```java
        checkState();

        source().request(waiting = rowsCnt);
    }

```

### NestedAssignment
Result of assignment expression used
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/ModifyNode.java`
#### Snippet
```java

        if (waiting == 0) {
            source().request(waiting = MODIFY_BATCH_SIZE);
        }
    }
```

### NestedAssignment
Result of assignment expression used
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/ModifyNode.java`
#### Snippet
```java

        if (state == State.UPDATING && waiting == 0) {
            source().request(waiting = MODIFY_BATCH_SIZE);
        }

```

### NestedAssignment
Result of assignment expression used
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/SortNode.java`
#### Snippet
```java

        if (waiting == 0) {
            source().request(waiting = inBufSize);
        } else if (!inLoop) {
            context().execute(this::flush, this::onError);
```

### NestedAssignment
Result of assignment expression used
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/SortNode.java`
#### Snippet
```java

        if (waiting == 0) {
            source().request(waiting = inBufSize);
        }
    }
```

### NestedAssignment
Result of assignment expression used
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/NestedLoopJoinNode.java`
#### Snippet
```java

            if (waitingRight == 0) {
                rightSource().request(waitingRight = inBufSize);
            }

```

### NestedAssignment
Result of assignment expression used
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/NestedLoopJoinNode.java`
#### Snippet
```java

            if (waitingLeft == 0 && leftInBuf.isEmpty()) {
                leftSource().request(waitingLeft = inBufSize);
            }

```

### NestedAssignment
Result of assignment expression used
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/NestedLoopJoinNode.java`
#### Snippet
```java

        if (waitingRight == 0) {
            rightSource().request(waitingRight = inBufSize);
        }
    }
```

### NestedAssignment
Result of assignment expression used
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/NestedLoopJoinNode.java`
#### Snippet
```java

            if (waitingRight == 0) {
                rightSource().request(waitingRight = inBufSize);
            }

```

### NestedAssignment
Result of assignment expression used
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/NestedLoopJoinNode.java`
#### Snippet
```java

            if (waitingLeft == 0 && leftInBuf.isEmpty()) {
                leftSource().request(waitingLeft = inBufSize);
            }

```

### NestedAssignment
Result of assignment expression used
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/NestedLoopJoinNode.java`
#### Snippet
```java

            if (waitingRight == 0) {
                rightSource().request(waitingRight = inBufSize);
            }

```

### NestedAssignment
Result of assignment expression used
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/NestedLoopJoinNode.java`
#### Snippet
```java

            if (waitingLeft == 0 && leftInBuf.isEmpty()) {
                leftSource().request(waitingLeft = inBufSize);
            }

```

### NestedAssignment
Result of assignment expression used
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/NestedLoopJoinNode.java`
#### Snippet
```java

            if (waitingRight == 0) {
                rightSource().request(waitingRight = inBufSize);
            }

```

### NestedAssignment
Result of assignment expression used
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/NestedLoopJoinNode.java`
#### Snippet
```java

            if (waitingLeft == 0 && leftInBuf.isEmpty()) {
                leftSource().request(waitingLeft = inBufSize);
            }

```

### NestedAssignment
Result of assignment expression used
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/NestedLoopJoinNode.java`
#### Snippet
```java

            if (waitingRight == 0) {
                rightSource().request(waitingRight = inBufSize);
            }

```

### NestedAssignment
Result of assignment expression used
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/NestedLoopJoinNode.java`
#### Snippet
```java

            if (waitingLeft == 0 && leftInBuf.isEmpty()) {
                leftSource().request(waitingLeft = inBufSize);
            }

```

### NestedAssignment
Result of assignment expression used
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/NestedLoopJoinNode.java`
#### Snippet
```java

            if (waitingRight == 0) {
                rightSource().request(waitingRight = inBufSize);
            }

```

### NestedAssignment
Result of assignment expression used
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/NestedLoopJoinNode.java`
#### Snippet
```java

            if (waitingLeft == 0 && leftInBuf.isEmpty()) {
                leftSource().request(waitingLeft = inBufSize);
            }

```

### NestedAssignment
Result of assignment expression used
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/TableSpoolNode.java`
#### Snippet
```java

        if (waiting == 0) {
            source().request(waiting = inBufSize);
        }

```

### NestedAssignment
Result of assignment expression used
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/TableSpoolNode.java`
#### Snippet
```java
            context().execute(this::doPush, this::onError);
        } else if (waiting == 0) {
            source().request(waiting = inBufSize);
        }
    }
```

### NestedAssignment
Result of assignment expression used
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/FilterNode.java`
#### Snippet
```java

        if (inBuf.isEmpty() && waiting == 0) {
            source().request(waiting = inBufSize);
        }

```

### NestedAssignment
Result of assignment expression used
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/ExpressionFactoryImpl.java`
#### Snippet
```java
        @Override
        public RowT lower() {
            return lowerRow != null ? lowerRow : (lowerRow = getRow(lowerBound));
        }

```

### NestedAssignment
Result of assignment expression used
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/ExpressionFactoryImpl.java`
#### Snippet
```java

            if (fieldSingleBounds instanceof ExactBounds) {
                fieldLowerBound = fieldUpperBound = ((ExactBounds) fieldSingleBounds).bound();
                fieldLowerInclude = fieldUpperInclude = true;
            } else if (fieldSingleBounds instanceof RangeBounds) {
```

### NestedAssignment
Result of assignment expression used
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/ExpressionFactoryImpl.java`
#### Snippet
```java
            if (fieldSingleBounds instanceof ExactBounds) {
                fieldLowerBound = fieldUpperBound = ((ExactBounds) fieldSingleBounds).bound();
                fieldLowerInclude = fieldUpperInclude = true;
            } else if (fieldSingleBounds instanceof RangeBounds) {
                RangeBounds fieldRangeBounds = (RangeBounds) fieldSingleBounds;
```

### NestedAssignment
Result of assignment expression used
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/ExpressionFactoryImpl.java`
#### Snippet
```java
        /** Clear cached rows. */
        void clearCache() {
            lowerRow = upperRow = null;
            skip = null;
        }
```

### NestedAssignment
Result of assignment expression used
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/ExpressionFactoryImpl.java`
#### Snippet
```java
        @Override
        public RowT upper() {
            return upperRow != null ? upperRow : (upperRow = getRow(upperBound));
        }

```

### NestedAssignment
Result of assignment expression used
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/ExpressionFactoryImpl.java`
#### Snippet
```java

            if (field == 0) {
                rows.add(currRow = factory.create());
            }

```

### NestedAssignment
Result of assignment expression used
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/HashAggregateNode.java`
#### Snippet
```java

        if (waiting == 0) {
            source().request(waiting = inBufSize);
        }
    }
```

### NestedAssignment
Result of assignment expression used
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/HashAggregateNode.java`
#### Snippet
```java

        if (waiting == 0) {
            source().request(waiting = inBufSize);
        } else if (!inLoop) {
            context().execute(this::flush, this::onError);
```

### NestedAssignment
Result of assignment expression used
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/Outbox.java`
#### Snippet
```java

            if (waiting == 0) {
                source().request(waiting = inBufSize);
            }
        } catch (Throwable t) {
```

### NestedAssignment
Result of assignment expression used
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/Outbox.java`
#### Snippet
```java

        if (waiting == 0) {
            source().request(waiting = inBufSize);
        } else if (waiting == -1) {
            for (RemoteDownstream<RowT> buffer : nodeBuffers.values()) {
```

### NestedAssignment
Result of assignment expression used
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/CorrelatedNestedLoopJoinNode.java`
#### Snippet
```java

            state = State.FILLING_RIGHT;
            rightSource().request(waitingRight = rightInBufferSize);
        }
    }
```

### NestedAssignment
Result of assignment expression used
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/CorrelatedNestedLoopJoinNode.java`
#### Snippet
```java
                state = State.FILLING_RIGHT;

                rightSource().request(waitingRight = rightInBufferSize);

                return;
```

### NestedAssignment
Result of assignment expression used
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/CorrelatedNestedLoopJoinNode.java`
#### Snippet
```java
                state = State.FILLING_LEFT;

                leftSource().request(waitingLeft = leftInBufferSize);

                return;
```

### NestedAssignment
Result of assignment expression used
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/CorrelatedNestedLoopJoinNode.java`
#### Snippet
```java
            state = State.FILLING_RIGHT;

            rightSource().request(waitingRight = rightInBufferSize);
        }
    }
```

### NestedAssignment
Result of assignment expression used
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/CorrelatedNestedLoopJoinNode.java`
#### Snippet
```java

                    state = State.FILLING_LEFT;
                    leftSource().request(waitingLeft = leftInBufferSize);
                }, this::onError);

```

### NestedAssignment
Result of assignment expression used
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/MergeJoinNode.java`
#### Snippet
```java

            if (waitingRight == 0) {
                rightSource().request(waitingRight = inBufSize);
            }

```

### NestedAssignment
Result of assignment expression used
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/MergeJoinNode.java`
#### Snippet
```java

            if (waitingLeft == 0) {
                leftSource().request(waitingLeft = inBufSize);
            }

```

### NestedAssignment
Result of assignment expression used
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/MergeJoinNode.java`
#### Snippet
```java

            if (waitingRight == 0) {
                rightSource().request(waitingRight = inBufSize);
            }

```

### NestedAssignment
Result of assignment expression used
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/MergeJoinNode.java`
#### Snippet
```java

            if (waitingLeft == 0) {
                leftSource().request(waitingLeft = inBufSize);
            }

```

### NestedAssignment
Result of assignment expression used
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/MergeJoinNode.java`
#### Snippet
```java

            if (waitingRight == 0) {
                rightSource().request(waitingRight = inBufSize);
            }

```

### NestedAssignment
Result of assignment expression used
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/MergeJoinNode.java`
#### Snippet
```java

            if (waitingLeft == 0) {
                leftSource().request(waitingLeft = inBufSize);
            }

```

### NestedAssignment
Result of assignment expression used
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/MergeJoinNode.java`
#### Snippet
```java

            if (waitingRight == 0) {
                rightSource().request(waitingRight = inBufSize);
            }

```

### NestedAssignment
Result of assignment expression used
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/MergeJoinNode.java`
#### Snippet
```java

            if (waitingLeft == 0) {
                leftSource().request(waitingLeft = inBufSize);
            }

```

### NestedAssignment
Result of assignment expression used
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/MergeJoinNode.java`
#### Snippet
```java

            if (waitingRight == 0) {
                rightSource().request(waitingRight = inBufSize);
            }

```

### NestedAssignment
Result of assignment expression used
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/MergeJoinNode.java`
#### Snippet
```java

            if (waitingLeft == 0) {
                leftSource().request(waitingLeft = inBufSize);
            }

```

### NestedAssignment
Result of assignment expression used
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/MergeJoinNode.java`
#### Snippet
```java

            if (waitingRight == 0) {
                rightSource().request(waitingRight = inBufSize);
            }

```

### NestedAssignment
Result of assignment expression used
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/MergeJoinNode.java`
#### Snippet
```java

            if (waitingLeft == 0) {
                leftSource().request(waitingLeft = inBufSize);
            }

```

### NestedAssignment
Result of assignment expression used
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rule/FilterSpoolMergeToSortedIndexSpoolRule.java`
#### Snippet
```java
            equalsFields.addAll(otherFields);

            searchCollation = traitCollation = TraitUtils.createCollation(equalsFields);
        } else {
            // Create search collation as a prefix of input collation.
```

### NestedAssignment
Result of assignment expression used
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/util/BaseQueryContext.java`
#### Snippet
```java
        }

        return catalogReader = new CalciteCatalogReader(
                CalciteSchema.from(rootSchema),
                CalciteSchema.from(dfltSchema).path(null),
                typeFactory(), CALCITE_CONNECTION_CONFIG);
    }

```

### NestedAssignment
Result of assignment expression used
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/trait/DistributionFunction.java`
#### Snippet
```java
        }

        return name = name0().intern();
    }

```

### NestedAssignment
Result of assignment expression used
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/externalize/RelJson.java`
#### Snippet
```java
        if (!typeName.contains(".")) {
            for (String pckg : PACKAGES) {
                if ((clazz = classForName(pckg + typeName, true)) != null) {
                    break;
                }
```

### NestedAssignment
Result of assignment expression used
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/freelist/io/PagesListMetaIo.java`
#### Snippet
```java

            if (list == null) {
                res.put(bucket, list = new LongArrayList());
            }

```

### NestedAssignment
Result of assignment expression used
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/util/PageHandler.java`
#### Snippet
```java

        try {
            if ((pageAddr = readLock(pageMem, groupId, pageId, page, lsnr)) == 0L) {
                return lockFailed;
            }
```

### NestedAssignment
Result of assignment expression used
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/util/PageHandler.java`
#### Snippet
```java
                assert PageIo.getCrc(pageAddr) == 0;

                if (releaseAfterWrite = h.releaseAfterWrite(groupId, pageId, page, pageAddr, arg, intArg)) {
                    writeUnlock(pageMem, groupId, pageId, page, pageAddr, lsnr, ok);
                }
```

### NestedAssignment
Result of assignment expression used
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/freelist/AbstractFreeList.java`
#### Snippet
```java

                    // If the data row was completely written without remainder, proceed to the next.
                    if ((written = writeWholePages(row, statHolder)) == COMPLETE) {
                        continue;
                    }
```

### NestedAssignment
Result of assignment expression used
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/freelist/AbstractFreeList.java`
#### Snippet
```java
        PagesCache pagesCache = bucketCaches.get(bucket);

        if (pagesCache == null && create && !bucketCaches.compareAndSet(bucket, null, pagesCache = new PagesCache(pageListCacheLimit))) {
            pagesCache = bucketCaches.get(bucket);
        }
```

### NestedAssignment
Result of assignment expression used
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/store/FilePageStore.java`
#### Snippet
```java
        }

        if (!NEW_DELTA_FILE_PAGE_STORE_IO_FUTURE.compareAndSet(this, null, future = new CompletableFuture<>())) {
            // Another thread started creating a delta file.
            return newDeltaFilePageStoreIoFuture;
```

### NestedAssignment
Result of assignment expression used
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/store/AbstractFilePageStoreIo.java`
#### Snippet
```java
                        while (true) {
                            try {
                                this.fileIo = fileIo = ioFactory.create(filePath, CREATE, READ, WRITE);

                                fileExists = true;
```

### NestedAssignment
Result of assignment expression used
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/freelist/PagesList.java`
#### Snippet
```java

        try {
            while ((nextId = bag.pollFreePage()) != 0L) {
                assert itemId(nextId) > 0 && itemId(nextId) <= MAX_ITEM_ID_NUM : hexLong(nextId);

```

### NestedAssignment
Result of assignment expression used
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/freelist/PagesList.java`
#### Snippet
```java
        long pageId;

        if (pagesCache != null && (pageId = pagesCache.poll()) != 0L) {
            decrementBucketSize(bucket);

```

### NestedAssignment
Result of assignment expression used
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/freelist/PagesList.java`
#### Snippet
```java

                if (stripe == null) {
                    stripes[stripeIdx] = stripe = new LongArrayList(MAX_SIZE / STRIPES_COUNT);
                }

```

### NestedAssignment
Result of assignment expression used
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/freelist/PagesList.java`
#### Snippet
```java
            }

            if ((cur = (cur + 1) % len) == init) {
                return null;
            }
```

### NestedAssignment
Result of assignment expression used
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/checkpoint/Checkpointer.java`
#### Snippet
```java
                    writePageIds,
                    updatedPartitions,
                    futures[i] = new CompletableFuture<>(),
                    workProgressDispatcher::updateHeartbeat,
                    currentCheckpointProgress,
```

### NestedAssignment
Result of assignment expression used
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/compaction/Compactor.java`
#### Snippet
```java

            for (int i = 0; i < threads; i++) {
                CompletableFuture<?> future = futures[i] = new CompletableFuture<>();

                Runnable merger = () -> {
```

### NestedAssignment
Result of assignment expression used
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/PersistentPageMemory.java`
#### Snippet
```java
            assert segment.checkpointPages == null : "Failed to begin checkpoint (it is already in progress)";

            Set<FullPageId> segmentDirtyPages = (dirtyPageIds[i] = segment.dirtyPages);

            segment.checkpointPages = new CheckpointPages(segmentDirtyPages, allowToReplace);
```

### NestedAssignment
Result of assignment expression used
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/PersistentPageMemory.java`
#### Snippet
```java
            }

            relPtr = resolveRelativePointer(seg, fullId, tag = generationTag(seg, fullId));

            // Page may have been cleared during eviction. We have nothing to do in this case.
```

### NestedAssignment
Result of assignment expression used
in `modules/transactions/src/main/java/org/apache/ignite/internal/tx/Timestamp.java`
#### Snippet
```java

            if (bytes == null) {
                ThreadLocalRandom.current().nextBytes(bytes = new byte[Byte.SIZE]);
            }

```

### NestedAssignment
Result of assignment expression used
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/SuperRoot.java`
#### Snippet
```java
            roots.remove(key);
        } else {
            roots.put(key, root = new RootInnerNode(root));

            src.descend(root.node());
```

### NestedAssignment
Result of assignment expression used
in `modules/storage-page-memory/src/main/java/org/apache/ignite/internal/storage/pagememory/mv/PersistentPageMemoryMvPartitionStorage.java`
#### Snippet
```java
        persistedIndex = meta.lastAppliedIndex();

        checkpointManager.addCheckpointListener(checkpointListener = new CheckpointListener() {
            @Override
            public void beforeCheckpointBegin(CheckpointProgress progress, @Nullable Executor exec) throws IgniteInternalCheckedException {
                // It may take some time, it's not scary because we keep a read lock here.
                syncMetadataOnCheckpoint(exec);
            }

            @Override
            public void onMarkCheckpointBegin(CheckpointProgress progress, @Nullable Executor exec) throws IgniteInternalCheckedException {
                // Should be fast, because here we only need to save the delta, reduce write lock holding time.
                syncMetadataOnCheckpoint(exec);
            }

            @Override
            public void afterCheckpointEnd(CheckpointProgress progress) {
                persistedIndex = meta.metaSnapshot(progress.id()).lastAppliedIndex();
            }
        }, dataRegion);

        blobStorage = new BlobStorage(
```

### NestedAssignment
Result of assignment expression used
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/tree/BplusTree.java`
#### Snippet
```java
                long rootId = io.getFirstPageId(pageAddr, rootLvl, partId);

                treeMeta = meta0 = new TreeMetaData(rootLvl, rootId);
            } finally {
                if (metaPageAddr == 0L) {
```

### NestedAssignment
Result of assignment expression used
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/tree/BplusTree.java`
#### Snippet
```java
            assert lowerBound != null;

            doFind(getCursor = new GetCursor(lowerBound, lowerShift, this));
        }

```

### NestedAssignment
Result of assignment expression used
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/tree/BplusTree.java`
#### Snippet
```java

        private long lock(long pageId, long page) {
            if (writing = ((pred.state() & TreeVisitorClosure.CAN_WRITE) != 0)) {
                return writeLock(pageId, page);
            } else {
```

### NestedAssignment
Result of assignment expression used
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/tree/BplusTree.java`
#### Snippet
```java
            }

            if (!(writing = (pred.state() & TreeVisitorClosure.CAN_WRITE) != 0)) {
                init(pageAddr, io, idx);
            }
```

## RuleId[id=MismatchedCollectionQueryUpdate]
### MismatchedCollectionQueryUpdate
Contents of collection `columns` are queried, but never updated
in `modules/schema/src/main/java/org/apache/ignite/internal/schema/configuration/TableValidatorImpl.java`
#### Snippet
```java
            }

            Set<String> columns = new HashSet<>(newTable.columns().namedListKeys());

            if (newTable.primaryKey() == null || nullOrEmpty(newTable.primaryKey().columns())) {
```

## RuleId[id=FieldAccessedSynchronizedAndUnsynchronized]
### FieldAccessedSynchronizedAndUnsynchronized
Field `lazyDelegate` is accessed in both synchronized and unsynchronized contexts
in `modules/cli/src/main/java/org/apache/ignite/internal/cli/core/repl/completer/LazyDynamicCompleter.java`
#### Snippet
```java
    private final Supplier<DynamicCompleter> delegateInitializer;

    private DynamicCompleter lazyDelegate;

    private final Object lock = new Object();
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `lastWakeUpTime` is accessed in both synchronized and unsynchronized contexts
in `modules/core/src/main/java/org/apache/ignite/internal/components/LongJvmPauseDetector.java`
#### Snippet
```java

    /** Last detector's wake up time. */
    private long lastWakeUpTime;

    /** Long pauses timestamps. */
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `nodeOptions` is accessed in both synchronized and unsynchronized contexts
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/RaftGroupService.java`
#### Snippet
```java
     * Node options
     */
    private NodeOptions nodeOptions;

    /**
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `serverId` is accessed in both synchronized and unsynchronized contexts
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/RaftGroupService.java`
#### Snippet
```java
     * This node serverId
     */
    private PeerId serverId;

    /**
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `groupId` is accessed in both synchronized and unsynchronized contexts
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/RaftGroupService.java`
#### Snippet
```java
     * The raft group id
     */
    private String groupId;

    /**
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `rpcOptions` is accessed in both synchronized and unsynchronized contexts
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/rpc/impl/AbstractClientService.java`
#### Snippet
```java
    protected volatile RpcClient rpcClient;
    protected ExecutorService rpcExecutor;
    protected RpcOptions rpcOptions;

    /**
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `blockPred` is accessed in both synchronized and unsynchronized contexts
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/rpc/impl/IgniteRpcClient.java`
#### Snippet
```java
    private volatile BiPredicate<Object, String> recordPred;

    private BiPredicate<Object, String> blockPred;

    private LinkedBlockingQueue<Object[]> blockedMsgs = new LinkedBlockingQueue<>();
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `blockedMsgs` is accessed in both synchronized and unsynchronized contexts
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/rpc/impl/IgniteRpcClient.java`
#### Snippet
```java
    private BiPredicate<Object, String> blockPred;

    private LinkedBlockingQueue<Object[]> blockedMsgs = new LinkedBlockingQueue<>();

    private LinkedBlockingQueue<Object[]> recordedMsgs = new LinkedBlockingQueue<>();
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `cliOptions` is accessed in both synchronized and unsynchronized contexts
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/rpc/impl/cli/CliClientServiceImpl.java`
#### Snippet
```java
public class CliClientServiceImpl extends AbstractClientService implements CliClientService {
    /** Options. */
    private CliOptions cliOptions;

    @Override
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `nodeOptions` is accessed in both synchronized and unsynchronized contexts
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/rpc/impl/core/DefaultRaftClientService.java`
#### Snippet
```java

    // cached node options
    private NodeOptions nodeOptions;

    @Override
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `node` is accessed in both synchronized and unsynchronized contexts
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/core/ReadOnlyServiceImpl.java`
#### Snippet
```java
    private RaftOptions raftOptions;

    private NodeImpl node;

    private final Lock lock = new ReentrantLock();
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `node` is accessed in both synchronized and unsynchronized contexts
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/core/FSMCallerImpl.java`
#### Snippet
```java
    private long lastAppliedTerm;
    private Closure afterShutdown;
    private NodeImpl node;
    private volatile TaskType currTask;
    private final AtomicLong applyingIndex;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `fsm` is accessed in both synchronized and unsynchronized contexts
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/core/FSMCallerImpl.java`
#### Snippet
```java

    private LogManager logManager;
    private StateMachine fsm;
    private ClosureQueue closureQueue;
    private final AtomicLong lastAppliedIndex;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `nodeId` is accessed in both synchronized and unsynchronized contexts
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/core/FSMCallerImpl.java`
#### Snippet
```java

    /** Raft node id. */
    private NodeId nodeId;

    private LogManager logManager;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `afterShutdown` is accessed in both synchronized and unsynchronized contexts
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/core/FSMCallerImpl.java`
#### Snippet
```java
    private final AtomicLong lastAppliedIndex;
    private long lastAppliedTerm;
    private Closure afterShutdown;
    private NodeImpl node;
    private volatile TaskType currTask;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `taskQueue` is accessed in both synchronized and unsynchronized contexts
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/core/FSMCallerImpl.java`
#### Snippet
```java
    private volatile RaftException error;
    private StripedDisruptor<ApplyTask> disruptor;
    private RingBuffer<ApplyTask> taskQueue;
    private volatile CountDownLatch shutdownLatch;
    private NodeMetrics nodeMetrics;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `disruptor` is accessed in both synchronized and unsynchronized contexts
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/core/FSMCallerImpl.java`
#### Snippet
```java
    private final AtomicLong applyingIndex;
    private volatile RaftException error;
    private StripedDisruptor<ApplyTask> disruptor;
    private RingBuffer<ApplyTask> taskQueue;
    private volatile CountDownLatch shutdownLatch;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `cliClientService` is accessed in both synchronized and unsynchronized contexts
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/core/CliServiceImpl.java`
#### Snippet
```java
    private CliOptions cliOptions;

    private CliClientService cliClientService;

    @Override
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `cliOptions` is accessed in both synchronized and unsynchronized contexts
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/core/CliServiceImpl.java`
#### Snippet
```java
    private static final IgniteLogger LOG = Loggers.forClass(CliServiceImpl.class);

    private CliOptions cliOptions;

    private CliClientService cliClientService;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `logManager` is accessed in both synchronized and unsynchronized contexts
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/core/NodeImpl.java`
#### Snippet
```java
    private ClosureQueue closureQueue;
    private ConfigurationManager configManager;
    private LogManager logManager;
    private FSMCaller fsmCaller;
    private BallotBox ballotBox;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `readOnlyService` is accessed in both synchronized and unsynchronized contexts
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/core/NodeImpl.java`
#### Snippet
```java
    private ReplicatorGroup replicatorGroup;
    private RaftClientService rpcClientService;
    private ReadOnlyService readOnlyService;

    /**
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `applyDisruptor` is accessed in both synchronized and unsynchronized contexts
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/core/NodeImpl.java`
#### Snippet
```java
     * Disruptor to run node service
     */
    private StripedDisruptor<LogEntryAndClosure> applyDisruptor;
    private RingBuffer<LogEntryAndClosure> applyQueue;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `fsmCaller` is accessed in both synchronized and unsynchronized contexts
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/core/NodeImpl.java`
#### Snippet
```java
    private ConfigurationManager configManager;
    private LogManager logManager;
    private FSMCaller fsmCaller;
    private BallotBox ballotBox;
    private SnapshotExecutor snapshotExecutor;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `wakingCandidate` is accessed in both synchronized and unsynchronized contexts
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/core/NodeImpl.java`
#### Snippet
```java
    private RepeatedTimer snapshotTimer;
    private ScheduledFuture<?> transferTimer;
    private ThreadId wakingCandidate;
    /**
     * Disruptor to run node service
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `snapshotExecutor` is accessed in both synchronized and unsynchronized contexts
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/core/NodeImpl.java`
#### Snippet
```java
    private FSMCaller fsmCaller;
    private BallotBox ballotBox;
    private SnapshotExecutor snapshotExecutor;
    private ReplicatorGroup replicatorGroup;
    private RaftClientService rpcClientService;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `serviceEventListener` is accessed in both synchronized and unsynchronized contexts
in `modules/raft/src/main/java/org/apache/ignite/internal/raft/server/impl/JraftServerImpl.java`
#### Snippet
```java

    /** Raft service event listener. */
    private RaftServiceEventListener serviceEventListener;

    /** The number of parallel raft groups starts. */
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `rpcServer` is accessed in both synchronized and unsynchronized contexts
in `modules/raft/src/main/java/org/apache/ignite/internal/raft/server/impl/JraftServerImpl.java`
#### Snippet
```java

    /** Server instance. */
    private IgniteRpcServer rpcServer;

    /** Started nodes. */
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `jobExecutorService` is accessed in both synchronized and unsynchronized contexts
in `modules/compute/src/main/java/org/apache/ignite/internal/compute/ComputeComponentImpl.java`
#### Snippet
```java
    private final ComputeConfiguration configuration;

    private ExecutorService jobExecutorService;

    private final ClassLoader jobClassLoader = Thread.currentThread().getContextClassLoader();
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `configuration` is accessed in both synchronized and unsynchronized contexts
in `modules/metrics/src/main/java/org/apache/ignite/internal/metrics/exporters/BasicMetricExporter.java`
#### Snippet
```java

    /** Exporter's configuration view. */
    private CfgT configuration;

    /** {@inheritDoc} */
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `stopped` is accessed in both synchronized and unsynchronized contexts
in `modules/network/src/main/java/org/apache/ignite/internal/network/netty/NettyClient.java`
#### Snippet
```java

    /** Flag indicating if {@link #stop()} has been called. */
    private boolean stopped = false;

    /**
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `markedForRemove` is accessed in both synchronized and unsynchronized contexts
in `modules/transactions/src/main/java/org/apache/ignite/internal/tx/impl/HeapLockManager.java`
#### Snippet
```java

        /** Marked for removal flag. */
        private boolean markedForRemove = false;

        public LockState(DeadlockPreventionPolicy deadlockPreventionPolicy, Executor delayedExecutor) {
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `invalid` is accessed in both synchronized and unsynchronized contexts
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/ConfigurationNode.java`
#### Snippet
```java
     * removed.
     */
    private boolean invalid;

    /**
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `val` is accessed in both synchronized and unsynchronized contexts
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/ConfigurationNode.java`
#### Snippet
```java

    /** Cached configuration value. Immutable. */
    private VIEWT val;

    /**
```

## RuleId[id=EqualsAndHashcode]
### EqualsAndHashcode
Class has `hashCode()` defined but does not define `equals()`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/metadata/cost/IgniteCost.java`
#### Snippet
```java
 * Estimated execution cost of a {@link RelNode}. Measured in abstract points.
 */
public class IgniteCost implements RelOptCost {
    /** Cost of a passing a single row through an execution node. */
    public static final double ROW_PASS_THROUGH_COST = 1;
```

### EqualsAndHashcode
Class has `equals()` defined but does not define `hashCode()`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/checkpoint/CheckpointWorkflow.java`
#### Snippet
```java
     */
    public void removeCheckpointListener(CheckpointListener listener) {
        listeners.remove(new IgniteBiTuple<CheckpointListener, DataRegion<PersistentPageMemory>>() {
            /** {@inheritDoc} */
            @Override
            public boolean equals(Object o) {
                return listener == ((IgniteBiTuple<?, ?>) o).getKey();
            }
        });
    }

```

### EqualsAndHashcode
Class has `equals()` defined but does not define `hashCode()`
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/ConfigurationListenerHolder.java`
#### Snippet
```java
     */
    public void removeListener(L listener) {
        containers.remove(new Container<>(listener, -1) {
            /** {@inheritDoc} */
            @Override
            public boolean equals(Object obj) {
                return listener == ((Container<L>) obj).listener;
            }
        });
    }

```

### EqualsAndHashcode
Class has `equals()` defined but does not define `hashCode()`
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/index/AbstractSortedIndexStorageTest.java`
#### Snippet
```java
    }

    private static final class SimpleRow<T> {
        private final T indexColumns;

```

## RuleId[id=SynchronizeOnThis]
### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `modules/core/src/main/java/org/apache/ignite/internal/util/Lazy.java`
#### Snippet
```java
        if (v == null) {
            if (supplier != EMPTY) {
                synchronized (this) {
                    if (supplier != EMPTY) {
                        v = supplier.get();
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `modules/core/src/main/java/org/apache/ignite/internal/components/LongJvmPauseDetector.java`
#### Snippet
```java
            @Override
            public void run() {
                synchronized (LongJvmPauseDetector.this) {
                    lastWakeUpTime = System.currentTimeMillis();
                }
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `modules/core/src/main/java/org/apache/ignite/internal/components/LongJvmPauseDetector.java`
#### Snippet
```java
                            log.warn("Possible too long JVM pause [duration={}ms]", pause);

                            synchronized (LongJvmPauseDetector.this) {
                                final int next = (int) (longPausesCnt % EVT_CNT);

```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `modules/core/src/main/java/org/apache/ignite/internal/components/LongJvmPauseDetector.java`
#### Snippet
```java
                            }
                        } else {
                            synchronized (LongJvmPauseDetector.this) {
                                lastWakeUpTime = now;
                            }
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/rpc/impl/IgniteRpcClient.java`
#### Snippet
```java
            recordedMsgs.add(new Object[] {request, peerId.toString(), fut.hashCode(), System.currentTimeMillis(), null});

        synchronized (this) {
            if (blockPred != null && blockPred.test(request, peerId.toString())) {
                Object[] msgData = {
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/rpc/impl/IgniteRpcClient.java`
#### Snippet
```java
        ArrayList<Object[]> msgs = new ArrayList<>();

        synchronized (this) {
            blockedMsgs.drainTo(msgs);

```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/rpc/impl/IgniteRpcClient.java`
#### Snippet
```java
        ArrayList<Object[]> msgs = new ArrayList<>();

        synchronized (this) {
            while(cnt-- > 0) {
                Object[] tmp = blockedMsgs.poll();
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/concurrent/MpscSingleThreadExecutor.java`
#### Snippet
```java
            }
            if (NOTIFY_UPDATER.getAndSet(this, NOT_NEEDED) == NEEDED) {
                synchronized (this) {
                    notifyAll();
                }
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/concurrent/MpscSingleThreadExecutor.java`
#### Snippet
```java
            if (NOTIFY_UPDATER.getAndSet(this, NOT_NEEDED) == NEEDED) {
                synchronized (this) {
                    notifyAll();
                }
            }
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/concurrent/MpscSingleThreadExecutor.java`
#### Snippet
```java

        private void notifyAndStop() {
            synchronized (this) {
                this.stop = true;
                notifyAll();
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/concurrent/MpscSingleThreadExecutor.java`
#### Snippet
```java
            synchronized (this) {
                this.stop = true;
                notifyAll();
            }
        }
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/concurrent/MpscSingleThreadExecutor.java`
#### Snippet
```java
                if (task == null) {
                    // wait task
                    synchronized (this) {
                        if (this.stop) {
                            break;
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/concurrent/MpscSingleThreadExecutor.java`
#### Snippet
```java
                            // Maybe the outer layer calls shutdown when the worker has not initialized yet,
                            // so we only wait a little while to recheck the conditions.
                            wait(1000, 10);

                            if (this.stop || isShutdown()) {
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/MetricReporter.java`
#### Snippet
```java
     */
    public void report() {
        synchronized (this) {
            report(this.registry.getGauges(this.filter), //
                this.registry.getCounters(this.filter), //
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `modules/client/src/main/java/org/apache/ignite/internal/client/ReliableChannel.java`
#### Snippet
```java
            }

            synchronized (this) {
                if (close) {
                    return CompletableFuture.completedFuture(null);
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `modules/runner/src/main/java/org/apache/ignite/internal/app/LifecycleManager.java`
#### Snippet
```java
        }

        synchronized (this) {
            startedComponents.add(component);

```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/checkpoint/Checkpointer.java`
#### Snippet
```java
        isCancelled.set(true);

        synchronized (this) {
            notifyAll();
        }
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/checkpoint/Checkpointer.java`
#### Snippet
```java

        synchronized (this) {
            notifyAll();
        }
    }
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/checkpoint/Checkpointer.java`
#### Snippet
```java
    void waitCheckpointEvent() {
        try {
            synchronized (this) {
                long remaining = NANOSECONDS.toMillis(scheduledCheckpointProgress.nextCheckpointNanos() - nanoTime());

```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/checkpoint/Checkpointer.java`
#### Snippet
```java

                    try {
                        wait(remaining);

                        remaining = NANOSECONDS.toMillis(scheduledCheckpointProgress.nextCheckpointNanos() - nanoTime());
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/checkpoint/Checkpointer.java`
#### Snippet
```java
        lastCheckpointTimestamp = checkpointStartTimestamp;

        synchronized (this) {
            CheckpointProgressImpl curr = scheduledCheckpointProgress;

```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/checkpoint/Checkpointer.java`
#### Snippet
```java
        }

        synchronized (this) {
            current = scheduledCheckpointProgress;

```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/checkpoint/Checkpointer.java`
#### Snippet
```java
            }

            notifyAll();
        }

```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/checkpoint/CheckpointWorkflow.java`
#### Snippet
```java
     */
    public void markCheckpointEnd(Checkpoint chp) throws IgniteInternalCheckedException {
        synchronized (this) {
            for (DataRegion<PersistentPageMemory> dataRegion : dataRegions) {
                dataRegion.pageMemory().finishCheckpoint();
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/ConfigurationNode.java`
#### Snippet
```java
            VIEWT newVal = ConfigurationUtil.find(keys.subList(1, keys.size()), newRootNode, true);

            synchronized (this) {
                if (cachedRootNode == oldRootNode) {
                    beforeRefreshValue(newVal, val);
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/ConfigurationNode.java`
#### Snippet
```java
            }
        } catch (KeyNotFoundException e) {
            synchronized (this) {
                invalid = true;

```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `modules/configuration/src/testFixtures/java/org/apache/ignite/internal/configuration/storage/TestConfigurationStorage.java`
#### Snippet
```java
    public CompletableFuture<Data> readDataOnRecovery() {
        return supplyAsync(() -> {
            synchronized (this) {
                if (fail) {
                    throw new StorageException("Failed to read data");
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `modules/configuration/src/testFixtures/java/org/apache/ignite/internal/configuration/storage/TestConfigurationStorage.java`
#### Snippet
```java
    public CompletableFuture<Boolean> write(Map<String, ? extends Serializable> newValues, long sentVersion) {
        return supplyAsync(() -> {
            synchronized (this) {
                if (fail) {
                    throw new StorageException("Failed to write data");
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `modules/configuration/src/testFixtures/java/org/apache/ignite/internal/configuration/storage/TestConfigurationStorage.java`
#### Snippet
```java
    public CompletableFuture<Map<String, ? extends Serializable>> readAllLatest(String prefix) {
        return supplyAsync(() -> {
            synchronized (this) {
                if (fail) {
                    throw new StorageException("Failed to read data");
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `modules/configuration/src/testFixtures/java/org/apache/ignite/internal/configuration/storage/TestConfigurationStorage.java`
#### Snippet
```java
    public CompletableFuture<Serializable> readLatest(String key) throws StorageException {
        return supplyAsync(() -> {
            synchronized (this) {
                if (fail) {
                    throw new StorageException("Failed to read data");
```

## RuleId[id=UnusedAssignment]
### UnusedAssignment
Variable `rowsToUpdate` initializer `null` is redundant
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/ModifyNode.java`
#### Snippet
```java

        List<RowT> rowsToInsert = null;
        List<RowT> rowsToUpdate = null;

        int rowSize = handler.columnCount(rows.get(0));
```

### UnusedAssignment
Variable `asyncSqlCursor` initializer `null` is redundant
in `modules/client-handler/src/main/java/org/apache/ignite/client/handler/JdbcQueryCursorHandlerImpl.java`
#### Snippet
```java
    @Override
    public CompletableFuture<JdbcMetaColumnsResult> queryMetadataAsync(JdbcQueryMetadataRequest req) {
        AsyncSqlCursor<List<Object>> asyncSqlCursor = null;
        try {
            asyncSqlCursor = resources.get(req.cursorId()).get(AsyncSqlCursor.class);
```

### UnusedAssignment
Variable `asyncSqlCursor` initializer `null` is redundant
in `modules/client-handler/src/main/java/org/apache/ignite/client/handler/JdbcQueryCursorHandlerImpl.java`
#### Snippet
```java
    @Override
    public CompletableFuture<JdbcQueryFetchResult> fetchAsync(JdbcQueryFetchRequest req) {
        AsyncSqlCursor<List<Object>> asyncSqlCursor = null;
        try {
            asyncSqlCursor = resources.get(req.cursorId()).get(AsyncSqlCursor.class);
```

### UnusedAssignment
Variable `asyncSqlCursor` initializer `null` is redundant
in `modules/client-handler/src/main/java/org/apache/ignite/client/handler/JdbcQueryCursorHandlerImpl.java`
#### Snippet
```java
    @Override
    public CompletableFuture<JdbcQueryCloseResult> closeAsync(JdbcQueryCloseRequest req) {
        AsyncSqlCursor<List<Object>> asyncSqlCursor = null;
        try {
            asyncSqlCursor = resources.remove(req.cursorId()).get(AsyncSqlCursor.class);
```

## RuleId[id=AssignmentUsedAsCondition]
### AssignmentUsedAsCondition
Assignment `releaseAfterWrite = h.releaseAfterWrite(groupId, pageId, page, pageAddr, arg, intArg)` used as condition
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/util/PageHandler.java`
#### Snippet
```java
                assert PageIo.getCrc(pageAddr) == 0;

                if (releaseAfterWrite = h.releaseAfterWrite(groupId, pageId, page, pageAddr, arg, intArg)) {
                    writeUnlock(pageMem, groupId, pageId, page, pageAddr, lsnr, ok);
                }
```

### AssignmentUsedAsCondition
Assignment `writing = ((pred.state() & TreeVisitorClosure.CAN_WRITE) != 0)` used as condition
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/tree/BplusTree.java`
#### Snippet
```java

        private long lock(long pageId, long page) {
            if (writing = ((pred.state() & TreeVisitorClosure.CAN_WRITE) != 0)) {
                return writeLock(pageId, page);
            } else {
```

## RuleId[id=UnnecessarySemicolon]
### UnnecessarySemicolon
Unnecessary semicolon `;`
in `modules/core/src/main/java/org/apache/ignite/internal/tostring/SensitiveDataLoggingPolicy.java`
#### Snippet
```java
     * Don't write sensitive information in {@code toString()} output.
     */
    NONE;
}

```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/entity/Task.java`
#### Snippet
```java

    /** Associated  task data */
    private ByteBuffer data = LogEntry.EMPTY_DATA;;
    /** task closure, called when the data is successfully committed to the raft group or failures happen. */
    private Closure done;
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/core/Replicator.java`
#### Snippet
```java
        ERROR, // error
        DESTROYED , // destroyed
        STATE_CHANGED; // state changed.
    }

```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `modules/network/src/main/java/org/apache/ignite/internal/network/configuration/NodeFinderType.java`
#### Snippet
```java
public enum NodeFinderType {
    /** Node finder with a preconfigured list of ip addresses. */
    STATIC;
}

```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `modules/transactions/src/main/java/org/apache/ignite/internal/tx/TxState.java`
#### Snippet
```java
public enum TxState {
    ABORTED,
    COMMITED;
}

```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `modules/storage-rocksdb/src/main/java/org/apache/ignite/internal/storage/rocksdb/RocksDbMetaStorage.java`
#### Snippet
```java
                var upperBound = new Slice(PARTITION_ID_PREFIX_END);
                var options = new ReadOptions().setIterateUpperBound(upperBound);
                RocksIterator it = metaColumnFamily.newIterator(options);
        ) {
            it.seek(PARTITION_ID_PREFIX);
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `modules/configuration-api/src/main/java/org/apache/ignite/configuration/annotation/ConfigurationType.java`
#### Snippet
```java

    /** Distributed configuration. */
    DISTRIBUTED;
}

```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `modules/marshaller-common/src/main/java/org/apache/ignite/internal/marshaller/BinaryMode.java`
#### Snippet
```java

    /** Timestamp. */
    TIMESTAMP;
}

```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `modules/cluster-management/src/main/java/org/apache/ignite/internal/cluster/management/raft/RocksDbClusterStateStorage.java`
#### Snippet
```java
        try (
                var batch = new WriteBatch();
                var options = new WriteOptions();
        ) {
            for (byte[] key : keys) {
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `modules/cluster-management/src/main/java/org/apache/ignite/internal/cluster/management/raft/RocksDbClusterStateStorage.java`
#### Snippet
```java
        try (
                var batch = new WriteBatch();
                var options = new WriteOptions();
        ) {
            byte[] endKey = RocksUtils.incrementPrefix(prefix);
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `modules/network-annotation-processor/src/main/java/org/apache/ignite/internal/network/processor/messages/MessageImplGenerator.java`
#### Snippet
```java
        COLLECTION,
        MESSAGE,
        MAP;
    }
}
```

## RuleId[id=SimplifyStreamApiCallChains]
### SimplifyStreamApiCallChains
Can be replaced with '.values().stream()'
in `modules/core/src/main/java/org/apache/ignite/internal/util/IgniteConcurrentMultiPairQueue.java`
#### Snippet
```java
     */
    public IgniteConcurrentMultiPairQueue(Map<K, ? extends Collection<V>> items) {
        int pairCnt = (int) items.entrySet().stream().map(Map.Entry::getValue).filter(not(Collection::isEmpty)).count();

        vals = (V[][]) new Object[pairCnt][];
```

### SimplifyStreamApiCallChains
''stream().forEach()'' can be replaced with 'forEach()'' (may change semantics)
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/RecyclableByteBufferList.java`
#### Snippet
```java
    public static ByteString concatenate(List<ByteBuffer> buffers) {
        final ByteBuffer combined = ByteBuffer.allocate(buffers.stream().mapToInt(Buffer::remaining).sum());
        buffers.stream().forEach(b -> combined.put(b.duplicate()));
        return new ByteString(combined);
    }
```

## RuleId[id=TypeParameterHidesVisibleType]
### TypeParameterHidesVisibleType
Type parameter `T` hides type parameter 'T'
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/DynamicProperty.java`
#### Snippet
```java
            /** {@inheritDoc} */
            @Override
            public <T> T unwrap(Class<T> clazz) {
                assert level == keys.size();

```

## RuleId[id=MethodOverloadsParentMethod]
### MethodOverloadsParentMethod
Method `getPredicates()` overloads a compatible method of a superclass, when overriding might have been intended
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/metadata/IgniteMdPredicates.java`
#### Snippet
```java
     * RelMdPredicates#getPredicates(org.apache.calcite.rel.RelNode, org.apache.calcite.rel.metadata.RelMetadataQuery)}.
     */
    public RelOptPredicateList getPredicates(ProjectableFilterableTableScan rel, RelMetadataQuery mq) {
        RexNode predicate = rel.pushUpPredicate();
        if (predicate == null) {
```

### MethodOverloadsParentMethod
Method `getRowCount()` overloads a compatible method of a superclass, when overriding might have been intended
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/metadata/IgniteMdRowCount.java`
#### Snippet
```java
     * Estimation of row count for Aggregate operator.
     */
    public double getRowCount(IgniteAggregate rel, RelMetadataQuery mq) {
        return rel.estimateRowCount(mq);
    }
```

### MethodOverloadsParentMethod
Method `getRowCount()` overloads a compatible method of a superclass, when overriding might have been intended
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/metadata/IgniteMdRowCount.java`
#### Snippet
```java
     * Estimation of row count for Limit operator.
     */
    public double getRowCount(IgniteLimit rel, RelMetadataQuery mq) {
        return rel.estimateRowCount(mq);
    }
```

### MethodOverloadsParentMethod
Method `getRowCount()` overloads a compatible method of a superclass, when overriding might have been intended
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/metadata/IgniteMdRowCount.java`
#### Snippet
```java
     * could filter out some rows, hence we need to estimate it differently.
     */
    public double getRowCount(IgniteSortedIndexSpool rel, RelMetadataQuery mq) {
        return rel.estimateRowCount(mq);
    }
```

### MethodOverloadsParentMethod
Method `getSelectivity()` overloads a compatible method of a superclass, when overriding might have been intended
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/metadata/IgniteMdSelectivity.java`
#### Snippet
```java
     * TODO Documentation https://issues.apache.org/jira/browse/IGNITE-15859
     */
    public Double getSelectivity(IgniteSortedIndexSpool rel, RelMetadataQuery mq, RexNode predicate) {
        if (predicate != null) {
            return mq.getSelectivity(rel.getInput(),
```

### MethodOverloadsParentMethod
Method `getSelectivity()` overloads a compatible method of a superclass, when overriding might have been intended
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/metadata/IgniteMdSelectivity.java`
#### Snippet
```java
     * TODO Documentation https://issues.apache.org/jira/browse/IGNITE-15859
     */
    public Double getSelectivity(AbstractIndexScan rel, RelMetadataQuery mq, RexNode predicate) {
        if (predicate != null) {
            return getSelectivity((ProjectableFilterableTableScan) rel, mq, predicate);
```

### MethodOverloadsParentMethod
Method `getSelectivity()` overloads a compatible method of a superclass, when overriding might have been intended
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/metadata/IgniteMdSelectivity.java`
#### Snippet
```java
     * TODO Documentation https://issues.apache.org/jira/browse/IGNITE-15859
     */
    public Double getSelectivity(IgniteHashIndexSpool rel, RelMetadataQuery mq, RexNode predicate) {
        if (predicate != null) {
            return mq.getSelectivity(rel.getInput(),
```

### MethodOverloadsParentMethod
Method `getSelectivity()` overloads a compatible method of a superclass, when overriding might have been intended
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/metadata/IgniteMdSelectivity.java`
#### Snippet
```java
     * TODO Documentation https://issues.apache.org/jira/browse/IGNITE-15859
     */
    public Double getSelectivity(ProjectableFilterableTableScan rel, RelMetadataQuery mq, RexNode predicate) {
        if (predicate == null) {
            return RelMdUtil.guessSelectivity(rel.condition());
```

## RuleId[id=OptionalContainsCollection]
### OptionalContainsCollection
'Optional' contains array `String[]`
in `modules/cli/src/main/java/org/apache/ignite/internal/cli/core/repl/completer/DynamicCompleterRegistry.java`
#### Snippet
```java
            }

            Optional<String[]> commandsMatch = conf.commands().stream().filter(command -> samePrefix(words, command)).findFirst();
            return commandsMatch.isPresent() && canBeAppliedCommandMatch(words);
        }
```

### OptionalContainsCollection
'Optional' contains collection `List`
in `modules/network/src/main/java/org/apache/ignite/internal/network/serialization/ClassDescriptorFactory.java`
#### Snippet
```java

    @SuppressWarnings("CodeBlock2Expr")
    private Optional<List<FieldDescriptor>> maybeSerialPersistentFields(Class<?> clazz) {
        if (!Classes.isSerializable(clazz)) {
            return Optional.empty();
```

## RuleId[id=NonSynchronizedMethodOverridesSynchronizedMethod]
### NonSynchronizedMethodOverridesSynchronizedMethod
Unsynchronized method `mark()` overrides synchronized method
in `modules/core/src/main/java/org/apache/ignite/internal/util/io/IgniteUnsafeDataInput.java`
#### Snippet
```java
    /** {@inheritDoc} */
    @Override
    public void mark(int readLimit) {
        if (in != null) {
            in.mark(readLimit);
```

### NonSynchronizedMethodOverridesSynchronizedMethod
Unsynchronized method `setSeed()` overrides synchronized method
in `modules/core/src/testFixtures/java/org/apache/ignite/internal/util/IgniteRandom.java`
#### Snippet
```java
    /** {@inheritDoc} */
    @Override
    public void setSeed(long seed) {
        rnd = (seed ^ 0x5DEECE66DL) & ((1L << 48) - 1);
    }
```

### NonSynchronizedMethodOverridesSynchronizedMethod
Unsynchronized method `fillInStackTrace()` overrides synchronized method
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/error/RetryAgainException.java`
#### Snippet
```java

    @Override
    public Throwable fillInStackTrace() {
        return this;
    }
```

## RuleId[id=CollectionContainsUrl]
### CollectionContainsUrl
Map `nodeNameToNodeUrl` may contain URL objects
in `modules/cli/src/main/java/org/apache/ignite/internal/cli/core/repl/registry/impl/NodeNameRegistryImpl.java`
#### Snippet
```java
    private final PhysicalTopologyCall physicalTopologyCall;

    private volatile Map<String, URL> nodeNameToNodeUrl = Map.of();

    private ScheduledExecutorService executor;
```

## RuleId[id=SwitchStatementWithConfusingDeclaration]
### SwitchStatementWithConfusingDeclaration
Local variable `startMs` declared in one 'switch' branch and used in another
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/storage/impl/LogManagerImpl.java`
#### Snippet
```java
                        break;
                    case TRUNCATE_PREFIX:
                        long startMs = Utils.monotonicMs();
                        try {
                            final TruncatePrefixClosure tpc = (TruncatePrefixClosure) done;
```

## RuleId[id=PublicFieldAccessedInSynchronizedContext]
### PublicFieldAccessedInSynchronizedContext
Non-private field `this.rpcClient` accessed in synchronized context
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/rpc/impl/AbstractClientService.java`
#### Snippet
```java
    @Override
    public synchronized void shutdown() {
        if (this.rpcClient != null) {
            this.rpcClient.shutdown();
            this.rpcClient = null;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.rpcClient` accessed in synchronized context
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/rpc/impl/AbstractClientService.java`
#### Snippet
```java
    public synchronized void shutdown() {
        if (this.rpcClient != null) {
            this.rpcClient.shutdown();
            this.rpcClient = null;
        }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.rpcClient` accessed in synchronized context
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/rpc/impl/AbstractClientService.java`
#### Snippet
```java
        if (this.rpcClient != null) {
            this.rpcClient.shutdown();
            this.rpcClient = null;
        }
    }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.rpcClient` accessed in synchronized context
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/rpc/impl/AbstractClientService.java`
#### Snippet
```java
    @Override
    public synchronized boolean init(final RpcOptions rpcOptions) {
        if (this.rpcClient != null) {
            return true;
        }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.rpcOptions` accessed in synchronized context
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/rpc/impl/AbstractClientService.java`
#### Snippet
```java
            return true;
        }
        this.rpcOptions = rpcOptions;
        return initRpcClient(this.rpcOptions.getRpcProcessorThreadPoolSize());
    }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.rpcOptions` accessed in synchronized context
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/rpc/impl/AbstractClientService.java`
#### Snippet
```java
        }
        this.rpcOptions = rpcOptions;
        return initRpcClient(this.rpcOptions.getRpcProcessorThreadPoolSize());
    }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.rpcOptions` accessed in synchronized context
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/rpc/impl/cli/CliClientServiceImpl.java`
#### Snippet
```java
        boolean ret = super.init(rpcOptions);
        if (ret) {
            this.cliOptions = (CliOptions) this.rpcOptions;
        }
        return ret;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `partitionMap` accessed in synchronized context
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/storage/InternalTableImpl.java`
#### Snippet
```java
            Int2ObjectMap<RaftGroupService> newPartitionMap = new Int2ObjectOpenHashMap<>(partitions);

            newPartitionMap.putAll(partitionMap);

            oldSrvc = newPartitionMap.put(p, raftGrpSvc);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `partitionMap` accessed in synchronized context
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/storage/InternalTableImpl.java`
#### Snippet
```java
            oldSrvc = newPartitionMap.put(p, raftGrpSvc);

            partitionMap = newPartitionMap;
        }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.members` accessed in synchronized context
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/NamedListConfiguration.java`
#### Snippet
```java
            @Nullable NamedListView<VIEWT> oldValue
    ) {
        Map<String, ConfigurationProperty<?>> oldValues = this.members;
        Map<String, ConfigurationProperty<?>> newValues = new LinkedHashMap<>();

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `this.members` accessed in synchronized context
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/NamedListConfiguration.java`
#### Snippet
```java
        }

        this.members = newValues;
    }

```

## RuleId[id=RedundantSuppression]
### RedundantSuppression
Redundant suppression
in `modules/client/src/main/java/org/apache/ignite/internal/client/ReliableChannel.java`
#### Snippet
```java

            if (hostAddrs.length == 0) {
                //noinspection NonPrivateFieldAccessedInSynchronizedContext
                throw new IgniteException(CONFIGURATION_ERR, "Empty addresses");
            }
```

### RedundantSuppression
Redundant suppression
in `modules/client/src/main/java/org/apache/ignite/internal/client/sql/ClientColumnOrigin.java`
#### Snippet
```java
        int schemaNameIdx = unpacker.tryUnpackInt(-1);

        //noinspection ConstantConditions
        this.schemaName = schemaNameIdx == -1
                ? unpacker.unpackString()
```

### RedundantSuppression
Redundant suppression
in `modules/client/src/main/java/org/apache/ignite/internal/client/sql/ClientColumnOrigin.java`
#### Snippet
```java
        int tableNameIdx = unpacker.tryUnpackInt(-1);

        //noinspection ConstantConditions
        this.tableName = tableNameIdx == -1
                ? unpacker.unpackString()
```

### RedundantSuppression
Redundant suppression
in `modules/core/src/main/java/org/apache/ignite/internal/util/io/IgniteUnsafeDataInput.java`
#### Snippet
```java
    @SuppressWarnings("NullableProblems")
    @Override
    public void readFully(byte[] b, int off, int len) throws IOException {
        fromStream(len);

```

### RedundantSuppression
Redundant suppression
in `modules/core/src/main/java/org/apache/ignite/internal/util/io/IgniteUnsafeDataInput.java`
#### Snippet
```java
    @SuppressWarnings("NullableProblems")
    @Override
    public int read(byte[] b, int off, int len) throws IOException {
        if (b == null) {
            throw new NullPointerException();
```

### RedundantSuppression
Redundant suppression
in `modules/core/src/main/java/org/apache/ignite/internal/util/subscription/OrderedMergePublisher.java`
#### Snippet
```java
        /** Error. */
        @SuppressWarnings({"unused", "FieldMayBeFinal"})
        private Throwable error;

        /** Cancelled flag. */
```

### RedundantSuppression
Redundant suppression
in `modules/core/src/main/java/org/apache/ignite/internal/util/subscription/OrderedMergePublisher.java`
#### Snippet
```java
        /** Cancelled flag. */
        @SuppressWarnings({"unused", "FieldMayBeFinal"})
        private boolean cancelled;

        /** Number of requested rows. */
```

### RedundantSuppression
Redundant suppression
in `modules/core/src/main/java/org/apache/ignite/internal/util/subscription/OrderedMergePublisher.java`
#### Snippet
```java
        /** Number of requested rows. */
        @SuppressWarnings({"unused", "FieldMayBeFinal"})
        private long requested;

        /** Number of emitted rows (guarded by {@link #guardCntr}). */
```

### RedundantSuppression
Redundant suppression
in `modules/network/src/main/java/org/apache/ignite/internal/network/serialization/marshal/DefaultUserObjectMarshaller.java`
#### Snippet
```java
            builtInContainerMarshallers.writeBuiltInMap((Map<?, ?>) object, descriptor, output, context);
        } else if (descriptor.isArray()) {
            //noinspection ConstantConditions
            builtInContainerMarshallers.writeGenericRefArray((Object[]) object, descriptor, output, context);
        } else if (descriptor.isExternalizable()) {
```

### RedundantSuppression
Redundant suppression
in `modules/network/src/main/java/org/apache/ignite/internal/network/serialization/marshal/DefaultUserObjectMarshaller.java`
#### Snippet
```java
            externalizableMarshaller.writeExternalizable((Externalizable) object, descriptor, output, context);
        } else if (descriptor.isProxy()) {
            //noinspection ConstantConditions
            proxyMarshaller.writeProxy(object, output, context);
        } else {
```

### RedundantSuppression
Redundant suppression
in `modules/network/src/main/java/org/apache/ignite/internal/network/serialization/marshal/UosObjectInputStream.java`
#### Snippet
```java
    @SuppressWarnings("NullableProblems")
    @Override
    public int read(byte[] buf) throws IOException {
        return input.read(buf);
    }
```

### RedundantSuppression
Redundant suppression
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/core/FSMCallerImpl.java`
#### Snippet
```java

    @SuppressWarnings("ConstantConditions")
    private long runApplyTask(final ApplyTask task, long maxCommittedIndex, final boolean endOfBatch) {
        CountDownLatch shutdown = null;
        if (task.type == TaskType.COMMITTED) {
```

### RedundantSuppression
Redundant suppression
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/core/NodeImpl.java`
#### Snippet
```java
                    break;
                case STAGE_NONE:
                    // noinspection ConstantConditions
                    Requires.requireTrue(false, "Can't reach here");
                    break;
```

### RedundantSuppression
Redundant suppression
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/core/Replicator.java`
#### Snippet
```java
        else {
            id.unlock();
            // noinspection ConstantConditions
            Requires.requireTrue(false, "Unknown error code for replicator: " + errorCode);
        }
```

### RedundantSuppression
Redundant suppression
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/concurrent/ConcurrentHashSet.java`
#### Snippet
```java
    @SuppressWarnings("SuspiciousMethodCalls")
    @Override
    public boolean contains(Object o) {
        return map.containsKey(o);
    }
```

### RedundantSuppression
Redundant suppression
in `modules/rocksdb-common/src/main/java/org/apache/ignite/internal/rocksdb/flush/RocksDbFlusher.java`
#### Snippet
```java
     */
    @SuppressWarnings("AssignmentOrReturnOfFieldWithMutableType")
    public void init(RocksDB db, List<ColumnFamilyHandle> columnFamilyHandles) {
        this.db = db;

```

### RedundantSuppression
Redundant suppression
in `modules/schema/src/testFixtures/java/org/apache/ignite/internal/schema/testutils/definition/index/HashIndexDefinitionImpl.java`
#### Snippet
```java
    @SuppressWarnings("AssignmentOrReturnOfFieldWithMutableType")
    @Override
    public List<IndexColumnDefinition> columns() {
        return columns;
    }
```

### RedundantSuppression
Redundant suppression
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/schema/TableDescriptorImpl.java`
#### Snippet
```java

    @SuppressWarnings("AssertWithSideEffects")
    private ColumnDescriptor injectDefault(ColumnDescriptor desc) {
        assert Commons.implicitPkEnabled() && Commons.IMPLICIT_PK_COL_NAME.equals(desc.name()) : desc;

```

### RedundantSuppression
Redundant suppression
in `modules/storage-rocksdb/src/main/java/org/apache/ignite/internal/storage/rocksdb/RocksDbMvPartitionStorage.java`
#### Snippet
```java
    public void addWriteCommitted(RowId rowId, @Nullable BinaryRow row, HybridTimestamp commitTimestamp) throws StorageException {
        busy(() -> {
            @SuppressWarnings("resource") WriteBatchWithIndex writeBatch = requireWriteBatch();

            ByteBuffer keyBuf = prepareHeapKeyBuf(rowId);
```

### RedundantSuppression
Redundant suppression
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/raft/snapshot/PartitionSnapshotStorageFactory.java`
#### Snippet
```java
     */
    @SuppressWarnings("AssignmentOrReturnOfFieldWithMutableType")
    public PartitionSnapshotStorageFactory(
            TopologyService topologyService,
            OutgoingSnapshotsManager outgoingSnapshotsManager,
```

### RedundantSuppression
Redundant suppression
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/raft/snapshot/outgoing/SnapshotMetaUtils.java`
#### Snippet
```java

        if (!config.isStable()) {
            //noinspection ConstantConditions
            metaBuilder
                    .oldPeersList(config.oldPeers())
```

## RuleId[id=UtilityClassWithPublicConstructor]
### UtilityClassWithPublicConstructor
Class `StringUtils` has only 'static' members, and a 'public' constructor
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/StringUtils.java`
#### Snippet
```java
import java.util.List;

public class StringUtils {
    public static final String EMPTY = "";
    public static final String[] EMPTY_STRING_ARRAY = new String[0];
```

## RuleId[id=OptionalUsedAsFieldOrParameterType]
### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'dumpClassPath'
in `modules/bytecode/src/main/java/com/facebook/presto/bytecode/ClassGenerator.java`
#### Snippet
```java
    }

    public ClassGenerator dumpClassFilesTo(Optional<Path> dumpClassPath)
    {
        return new ClassGenerator(classLoader, fakeLineNumbers, runAsmVerifier, dumpRawBytecode, output, dumpClassPath);
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'dumpClassPath'
in `modules/bytecode/src/main/java/com/facebook/presto/bytecode/ClassGenerator.java`
#### Snippet
```java
    private final boolean dumpRawBytecode;
    private final Writer output;
    private final Optional<Path> dumpClassPath;

    public static ClassGenerator classGenerator(ClassLoader parentClassLoader)
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'dumpClassPath'
in `modules/bytecode/src/main/java/com/facebook/presto/bytecode/ClassGenerator.java`
#### Snippet
```java
            boolean dumpRawBytecode,
            Writer output,
            Optional<Path> dumpClassPath)
    {
        this.classLoader = requireNonNull(classLoader, "classLoader is null");
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'suffix'
in `modules/bytecode/src/main/java/com/facebook/presto/bytecode/BytecodeUtils.java`
#### Snippet
```java
    }

    public static ParameterizedType makeClassName(String baseName, Optional<String> suffix) {
        String className = baseName
            + "_" + suffix.orElseGet(() -> Instant.now().atZone(UTC).format(TIMESTAMP_FORMAT))
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'thisType'
in `modules/bytecode/src/main/java/com/facebook/presto/bytecode/Scope.java`
#### Snippet
```java

    // This can only be constructed by a method definition
    Scope(Optional<ParameterizedType> thisType, Iterable<Parameter> parameters) {
        if (thisType.isPresent()) {
            thisVariable = new Variable("this", thisType.get());
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'overrideClassLoader'
in `modules/bytecode/src/main/java/com/facebook/presto/bytecode/DynamicClassLoader.java`
#### Snippet
```java
    private final ConcurrentMap<String, byte[]> pendingClasses = new ConcurrentHashMap<>();
    private final Map<Long, MethodHandle> callSiteBindings;
    private final Optional<ClassLoader> overrideClassLoader;

    public DynamicClassLoader(ClassLoader parentClassLoader)
```

## RuleId[id=SystemOutErr]
### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `modules/cli/src/main/java/org/apache/ignite/internal/cli/ReplManager.java`
#### Snippet
```java
                        CallExecutionPipeline.builder(executor)
                                .inputProvider(() -> new StringCallInput(line))
                                .output(System.out)
                                .errOutput(System.err)
                                .exceptionHandlers(new DefaultExceptionHandlers())
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `modules/cli/src/main/java/org/apache/ignite/internal/cli/ReplManager.java`
#### Snippet
```java
                                .inputProvider(() -> new StringCallInput(line))
                                .output(System.out)
                                .errOutput(System.err)
                                .exceptionHandlers(new DefaultExceptionHandlers())
                                .exceptionHandlers(exceptionHandlers)
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `modules/cli/src/main/java/org/apache/ignite/internal/cli/Main.java`
#### Snippet
```java
    private static void enterRepl(MicronautFactory micronautFactory) throws Exception {
        VersionProvider versionProvider = micronautFactory.create(VersionProvider.class);
        System.out.println(banner(versionProvider));

        ReplManager replManager = micronautFactory.create(ReplManager.class);
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `modules/cli/src/main/java/org/apache/ignite/internal/cli/Main.java`
#### Snippet
```java
                    exitCode = executeCommand(args, micronautFactory);
                } catch (Exception e) {
                    System.err.println("Error occurred during command execution");
                }
            } else {
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `modules/cli/src/main/java/org/apache/ignite/internal/cli/Main.java`
#### Snippet
```java
            }
        } catch (Exception e) {
            System.err.println("Error occurred during initialization");
        } finally {
            AnsiConsole.systemUninstall();
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `modules/cli/src/main/java/org/apache/ignite/internal/cli/core/call/CallExecutionPipelineBuilder.java`
#### Snippet
```java
    private PrintWriter output = wrapOutputStream(System.out);

    private PrintWriter errOutput = wrapOutputStream(System.err);

    private Decorator<T, TerminalOutput> decorator = new DefaultDecorator<>();
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `modules/cli/src/main/java/org/apache/ignite/internal/cli/core/call/CallExecutionPipelineBuilder.java`
#### Snippet
```java
    private Supplier<I> inputProvider;

    private PrintWriter output = wrapOutputStream(System.out);

    private PrintWriter errOutput = wrapOutputStream(System.err);
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `modules/cli/src/main/java/org/apache/ignite/internal/cli/core/call/AsyncCallExecutionPipelineBuilder.java`
#### Snippet
```java
    private PrintWriter output = wrapOutputStream(System.out);

    private PrintWriter errOutput = wrapOutputStream(System.err);

    private Decorator<T, TerminalOutput> decorator = new DefaultDecorator<>();
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `modules/cli/src/main/java/org/apache/ignite/internal/cli/core/call/AsyncCallExecutionPipelineBuilder.java`
#### Snippet
```java
    private Supplier<I> inputProvider;

    private PrintWriter output = wrapOutputStream(System.out);

    private PrintWriter errOutput = wrapOutputStream(System.err);
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `modules/cli/src/main/java/org/apache/ignite/internal/cli/core/repl/executor/ReplExecutor.java`
#### Snippet
```java
                    repl.getPipeline(executor, exceptionHandlers, line).runPipeline();
                } catch (Throwable t) {
                    exceptionHandlers.handleException(System.err::println, t);
                }
            }
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `modules/cli/src/main/java/org/apache/ignite/internal/cli/core/repl/executor/ReplExecutor.java`
#### Snippet
```java
            reader.getHistory().save();
        } catch (Throwable t) {
            exceptionHandlers.handleException(System.err::println, t);
        }
    }
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `modules/core/src/main/java/org/apache/ignite/internal/util/IgniteUtils.java`
#### Snippet
```java
            log.warn(reason, err);
        } else {
            System.err.println("[" + LocalDateTime.now().format(SHORT_DATE_FMT) + "] (err) " + reason);

            err.printStackTrace(System.err);
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `modules/core/src/main/java/org/apache/ignite/internal/util/IgniteUtils.java`
#### Snippet
```java
            System.err.println("[" + LocalDateTime.now().format(SHORT_DATE_FMT) + "] (err) " + reason);

            err.printStackTrace(System.err);
        }
    }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `modules/core/src/testFixtures/java/org/apache/ignite/internal/testframework/junit/DumpThreadsOnTimeout.java`
#### Snippet
```java

        for (ThreadInfo info : infos) {
            System.out.println(info);
        }
    }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `modules/core/src/testFixtures/java/org/apache/ignite/internal/testframework/junit/DumpThreadsOnTimeout.java`
#### Snippet
```java
        if (isJunitMethodTimeout(throwable)) {
            // We take throwable.getMessage() because TimeoutExtension puts a description of 'what has timed out' to it.
            System.out.println(category + " " + throwable.getMessage() + ", dumping threads");

            dumpThreadsToStdout();
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/MetricReporter.java`
#### Snippet
```java
            this.registry = registry;
            this.prefix = "";
            this.output = System.out;
            this.locale = Locale.getDefault();
            this.clock = Clock.defaultClock();
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `modules/raft/src/testFixtures/java/org/apache/ignite/raft/jraft/test/TestUtils.java`
#### Snippet
```java
        ThreadInfo[] infos = bean.dumpAllThreads(true, true);
        for (ThreadInfo info : infos)
            System.out.println(info);
    }

```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `modules/runner/src/main/java/org/apache/ignite/app/IgniteRunner.java`
#### Snippet
```java
            start(args).get();
        } catch (ExecutionException | InterruptedException e) {
            System.out.println("Error when starting the node: " + e.getMessage());

            e.printStackTrace(System.out);
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `modules/runner/src/main/java/org/apache/ignite/app/IgniteRunner.java`
#### Snippet
```java
            System.out.println("Error when starting the node: " + e.getMessage());

            e.printStackTrace(System.out);

            System.exit(1);
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `modules/schema/src/testFixtures/java/org/apache/ignite/internal/schema/marshaller/RecordMarshallerTest.java`
#### Snippet
```java
        long seed = System.currentTimeMillis();

        System.out.println("Using seed: " + seed + "L;");

        rnd = new Random(seed);
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/RexExecutorImpl.java`
#### Snippet
```java

        if (CalciteSystemProperty.DEBUG.value()) {
            Util.debugCode(System.out, code);
        }

```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/util/Commons.java`
#### Snippet
```java

        if (debug) {
            Util.debugCode(System.out, body);
        }

```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `examples/src/main/java/org/apache/ignite/example/tx/TransactionsExample.java`
#### Snippet
```java
        //--------------------------------------------------------------------------------------

        System.out.println("\nConnecting to server...");

        try (IgniteClient client = IgniteClient.builder()
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `examples/src/main/java/org/apache/ignite/example/tx/TransactionsExample.java`
#### Snippet
```java
            accounts.put(null, key, new Account("John", "Doe", 1000.0d));

            System.out.println("\nInitial balance: " + accounts.get(null, key).balance);

            //--------------------------------------------------------------------------------------
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `examples/src/main/java/org/apache/ignite/example/tx/TransactionsExample.java`
#### Snippet
```java
            });

            System.out.println("\nBalance after the sync transaction: " + accounts.get(null, key).balance);

            //--------------------------------------------------------------------------------------
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `examples/src/main/java/org/apache/ignite/example/tx/TransactionsExample.java`
#### Snippet
```java
            fut.join();

            System.out.println("\nBalance after the async transaction: " + accounts.get(null, key).balance);
        } finally {
            System.out.println("\nDropping the table...");
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `examples/src/main/java/org/apache/ignite/example/tx/TransactionsExample.java`
#### Snippet
```java
            System.out.println("\nBalance after the async transaction: " + accounts.get(null, key).balance);
        } finally {
            System.out.println("\nDropping the table...");

            try (
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `examples/src/main/java/org/apache/ignite/example/table/KeyValueViewExample.java`
#### Snippet
```java
        //--------------------------------------------------------------------------------------

        System.out.println("\nConnecting to server...");

        try (IgniteClient client = IgniteClient.builder()
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `examples/src/main/java/org/apache/ignite/example/table/KeyValueViewExample.java`
#### Snippet
```java
            //--------------------------------------------------------------------------------------

            System.out.println("\nInserting a key-value pair into the 'accounts' table...");

            Tuple key = Tuple.create()
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `examples/src/main/java/org/apache/ignite/example/table/KeyValueViewExample.java`
#### Snippet
```java
            //--------------------------------------------------------------------------------------

            System.out.println("\nRetrieving a value using KeyValueView API...");

            value = kvView.get(null, key);
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `examples/src/main/java/org/apache/ignite/example/table/KeyValueViewExample.java`
#### Snippet
```java
            value = kvView.get(null, key);

            System.out.println(
                    "\nRetrieved value:\n"
                            + "    Account Number: " + key.intValue("accountNumber") + '\n'
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `examples/src/main/java/org/apache/ignite/example/table/KeyValueViewExample.java`
#### Snippet
```java
                            + "    Balance: $" + value.doubleValue("balance"));
        } finally {
            System.out.println("\nDropping the table...");

            try (
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `examples/src/main/java/org/apache/ignite/example/table/RecordViewPojoExample.java`
#### Snippet
```java
        //--------------------------------------------------------------------------------------

        System.out.println("\nConnecting to server...");

        try (IgniteClient client = IgniteClient.builder()
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `examples/src/main/java/org/apache/ignite/example/table/RecordViewPojoExample.java`
#### Snippet
```java
            //--------------------------------------------------------------------------------------

            System.out.println("\nInserting a record into the 'accounts' table...");

            Account newAccount = new Account(
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `examples/src/main/java/org/apache/ignite/example/table/RecordViewPojoExample.java`
#### Snippet
```java
            //--------------------------------------------------------------------------------------

            System.out.println("\nRetrieving a record using RecordView API...");

            Account account = accounts.get(null, new Account(123456));
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `examples/src/main/java/org/apache/ignite/example/table/RecordViewPojoExample.java`
#### Snippet
```java
            Account account = accounts.get(null, new Account(123456));

            System.out.println(
                    "\nRetrieved record:\n"
                        + "    Account Number: " + account.accountNumber + '\n'
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `examples/src/main/java/org/apache/ignite/example/table/RecordViewPojoExample.java`
#### Snippet
```java
                        + "    Balance: $" + account.balance);
        } finally {
            System.out.println("\nDropping the table...");

            try (
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `examples/src/main/java/org/apache/ignite/example/sql/SqlApiExample.java`
#### Snippet
```java
        //
        for (var row : resultSet.currentPage()) {
            System.out.println("    "
                    + row.stringValue(1) + ", "
                    + row.stringValue(2) + ", "
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `examples/src/main/java/org/apache/ignite/example/sql/SqlApiExample.java`
#### Snippet
```java
        //--------------------------------------------------------------------------------------

        System.out.println("\nConnecting to server...");

        try (IgniteClient client = IgniteClient.builder()
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `examples/src/main/java/org/apache/ignite/example/sql/SqlApiExample.java`
#### Snippet
```java
                //--------------------------------------------------------------------------------------

                System.out.println("\nPopulating 'CITIES' table...");

                try (Statement stmt = client.sql().createStatement("INSERT INTO CITIES (ID, NAME) VALUES (?, ?)")) {
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `examples/src/main/java/org/apache/ignite/example/sql/SqlApiExample.java`
#### Snippet
```java
                    }

                    System.out.println("\nAdded cities: " + rowsAdded);
                }

```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `examples/src/main/java/org/apache/ignite/example/sql/SqlApiExample.java`
#### Snippet
```java
                //--------------------------------------------------------------------------------------

                System.out.println("\nPopulating 'ACCOUNTS' table...");

                long rowsAdded = Arrays.stream(ses.executeBatch(null,
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `examples/src/main/java/org/apache/ignite/example/sql/SqlApiExample.java`
#### Snippet
```java
                        .sum();

                System.out.println("\nAdded accounts: " + rowsAdded);

                //--------------------------------------------------------------------------------------
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `examples/src/main/java/org/apache/ignite/example/sql/SqlApiExample.java`
#### Snippet
```java
                //--------------------------------------------------------------------------------------

                System.out.println("\nAll accounts:");

                try (ResultSet<SqlRow> rs = ses.execute(null,
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `examples/src/main/java/org/apache/ignite/example/sql/SqlApiExample.java`
#### Snippet
```java
                        SqlRow row = rs.next();

                        System.out.println("    "
                                + row.stringValue(1) + ", "
                                + row.stringValue(2) + ", "
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `examples/src/main/java/org/apache/ignite/example/sql/SqlApiExample.java`
#### Snippet
```java
                //--------------------------------------------------------------------------------------

                System.out.println("\nAccounts with balance lower than 1,500:");

                Statement statement = client.sql().statementBuilder()
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `examples/src/main/java/org/apache/ignite/example/sql/SqlApiExample.java`
#### Snippet
```java
                        AccountInfo row = rs.next();

                        System.out.println("    "
                                + row.firstName + ", "
                                + row.lastName + ", "
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `examples/src/main/java/org/apache/ignite/example/sql/SqlApiExample.java`
#### Snippet
```java
                //--------------------------------------------------------------------------------------

                System.out.println("\nDeleting one of the accounts...");

                try (ResultSet<SqlRow> rs = ses.execute(null, "DELETE FROM ACCOUNTS WHERE ACCOUNT_ID = ?", 1)) {
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `examples/src/main/java/org/apache/ignite/example/sql/SqlApiExample.java`
#### Snippet
```java

                try (ResultSet<SqlRow> rs = ses.execute(null, "DELETE FROM ACCOUNTS WHERE ACCOUNT_ID = ?", 1)) {
                    System.out.println("\n Removed accounts: " + rs.affectedRows());
                }

```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `examples/src/main/java/org/apache/ignite/example/sql/SqlApiExample.java`
#### Snippet
```java
                //--------------------------------------------------------------------------------------

                System.out.println("\nAll accounts:");

                // Async way.
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `examples/src/main/java/org/apache/ignite/example/sql/SqlApiExample.java`
#### Snippet
```java
                stmt.close();

                System.out.println("\nDropping the tables...");

                ses.execute(null, "DROP TABLE ACCOUNTS").close();
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `examples/src/main/java/org/apache/ignite/example/sql/jdbc/SqlJdbcExample.java`
#### Snippet
```java
        //--------------------------------------------------------------------------------------

        System.out.println("\nConnecting to server...");

        try (Connection conn = DriverManager.getConnection("jdbc:ignite:thin://127.0.0.1:10800/")) {
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `examples/src/main/java/org/apache/ignite/example/sql/jdbc/SqlJdbcExample.java`
#### Snippet
```java
            //--------------------------------------------------------------------------------------

            System.out.println("\nPopulating 'CITIES' table...");

            try (PreparedStatement stmt = conn.prepareStatement("INSERT INTO CITIES (ID, NAME) VALUES (?, ?)")) {
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `examples/src/main/java/org/apache/ignite/example/sql/jdbc/SqlJdbcExample.java`
#### Snippet
```java
            //--------------------------------------------------------------------------------------

            System.out.println("\nPopulating 'ACCOUNTS' table...");

            try (PreparedStatement stmt = conn.prepareStatement(
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `examples/src/main/java/org/apache/ignite/example/sql/jdbc/SqlJdbcExample.java`
#### Snippet
```java
            //--------------------------------------------------------------------------------------

            System.out.println("\nAll accounts:");

            try (Statement stmt = conn.createStatement()) {
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `examples/src/main/java/org/apache/ignite/example/sql/jdbc/SqlJdbcExample.java`
#### Snippet
```java
                                + "INNER JOIN CITIES c on c.ID = a.CITY_ID ORDER BY a.ACCOUNT_ID")) {
                    while (rs.next()) {
                        System.out.println("    "
                                + rs.getString(1) + ", "
                                + rs.getString(2) + ", "
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `examples/src/main/java/org/apache/ignite/example/sql/jdbc/SqlJdbcExample.java`
#### Snippet
```java
            //--------------------------------------------------------------------------------------

            System.out.println("\nAccounts with balance lower than 1,500:");

            try (Statement stmt = conn.createStatement()) {
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `examples/src/main/java/org/apache/ignite/example/sql/jdbc/SqlJdbcExample.java`
#### Snippet
```java
                                + "ORDER BY a.ACCOUNT_ID")) {
                    while (rs.next()) {
                        System.out.println("    "
                                + rs.getString(1) + ", "
                                + rs.getString(2) + ", "
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `examples/src/main/java/org/apache/ignite/example/sql/jdbc/SqlJdbcExample.java`
#### Snippet
```java
            //--------------------------------------------------------------------------------------

            System.out.println("\nDeleting one of the accounts...");

            try (PreparedStatement stmt = conn.prepareStatement("DELETE FROM ACCOUNTS WHERE ACCOUNT_ID = ?")) {
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `examples/src/main/java/org/apache/ignite/example/sql/jdbc/SqlJdbcExample.java`
#### Snippet
```java
            //--------------------------------------------------------------------------------------

            System.out.println("\nAll accounts:");

            try (Statement stmt = conn.createStatement()) {
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `examples/src/main/java/org/apache/ignite/example/sql/jdbc/SqlJdbcExample.java`
#### Snippet
```java
                                + "INNER JOIN CITIES c on c.ID = a.CITY_ID ORDER BY a.ACCOUNT_ID")) {
                    while (rs.next()) {
                        System.out.println("    "
                                + rs.getString(1) + ", "
                                + rs.getString(2) + ", "
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `examples/src/main/java/org/apache/ignite/example/sql/jdbc/SqlJdbcExample.java`
#### Snippet
```java
            }

            System.out.println("\nDropping the tables...");

            try (Statement stmt = conn.createStatement()) {
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `examples/src/main/java/org/apache/ignite/example/table/KeyValueViewPojoExample.java`
#### Snippet
```java
        //--------------------------------------------------------------------------------------

        System.out.println("\nConnecting to server...");

        try (IgniteClient client = IgniteClient.builder()
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `examples/src/main/java/org/apache/ignite/example/table/KeyValueViewPojoExample.java`
#### Snippet
```java
            //--------------------------------------------------------------------------------------

            System.out.println("\nInserting a key-value pair into the 'accounts' table...");

            AccountKey key = new AccountKey(123456);
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `examples/src/main/java/org/apache/ignite/example/table/KeyValueViewPojoExample.java`
#### Snippet
```java
            //--------------------------------------------------------------------------------------

            System.out.println("\nRetrieving a value using KeyValueView API...");

            value = kvView.get(null, key);
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `examples/src/main/java/org/apache/ignite/example/table/RecordViewExample.java`
#### Snippet
```java
        //--------------------------------------------------------------------------------------

        System.out.println("\nConnecting to server...");

        try (IgniteClient client = IgniteClient.builder()
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `examples/src/main/java/org/apache/ignite/example/table/KeyValueViewPojoExample.java`
#### Snippet
```java
            value = kvView.get(null, key);

            System.out.println(
                    "\nRetrieved value:\n"
                        + "    Account Number: " + key.accountNumber + '\n'
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `examples/src/main/java/org/apache/ignite/example/table/RecordViewExample.java`
#### Snippet
```java
            //--------------------------------------------------------------------------------------

            System.out.println("\nInserting a record into the 'accounts' table...");

            Tuple newAccountTuple = Tuple.create()
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `examples/src/main/java/org/apache/ignite/example/table/KeyValueViewPojoExample.java`
#### Snippet
```java
                        + "    Balance: $" + value.balance);
        } finally {
            System.out.println("\nDropping the table...");

            try (
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `examples/src/main/java/org/apache/ignite/example/table/RecordViewExample.java`
#### Snippet
```java
            //--------------------------------------------------------------------------------------

            System.out.println("\nRetrieving a record using RecordView API...");

            Tuple accountNumberTuple = Tuple.create().set("accountNumber", 123456);
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `examples/src/main/java/org/apache/ignite/example/table/RecordViewExample.java`
#### Snippet
```java
            Tuple accountTuple = accounts.get(null, accountNumberTuple);

            System.out.println(
                    "\nRetrieved record:\n"
                            + "    Account Number: " + accountTuple.intValue("accountNumber") + '\n'
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `examples/src/main/java/org/apache/ignite/example/table/RecordViewExample.java`
#### Snippet
```java
                            + "    Balance: $" + accountTuple.doubleValue("balance"));
        } finally {
            System.out.println("\nDropping the table...");

            try (
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `examples/src/main/java/org/apache/ignite/example/storage/StorageEngineExample.java`
#### Snippet
```java
        //--------------------------------------------------------------------------------------

        System.out.println("\nConnecting to server...");

        try (Connection conn = DriverManager.getConnection("jdbc:ignite:thin://127.0.0.1:10800/")) {
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `examples/src/main/java/org/apache/ignite/example/storage/StorageEngineExample.java`
#### Snippet
```java
            //--------------------------------------------------------------------------------------

            System.out.println("\nPopulating 'ACCOUNTS' table...");

            try (PreparedStatement stmt = conn.prepareStatement(
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `examples/src/main/java/org/apache/ignite/example/storage/StorageEngineExample.java`
#### Snippet
```java
            //--------------------------------------------------------------------------------------

            System.out.println("\nAll accounts:");

            try (Statement stmt = conn.createStatement()) {
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `examples/src/main/java/org/apache/ignite/example/storage/StorageEngineExample.java`
#### Snippet
```java
                )) {
                    while (rs.next()) {
                        System.out.println("    "
                                + rs.getString(1) + ", "
                                + rs.getString(2) + ", "
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `examples/src/main/java/org/apache/ignite/example/storage/StorageEngineExample.java`
#### Snippet
```java
            }
        } finally {
            System.out.println("\nDropping the table...");

            try (
```

## RuleId[id=DynamicRegexReplaceableByCompiledPattern]
### DynamicRegexReplaceableByCompiledPattern
`split()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `modules/cli/src/main/java/org/apache/ignite/internal/cli/core/repl/executor/IgnitePicocliCommands.java`
#### Snippet
```java
        Help cmdhelp = new picocli.CommandLine.Help(spec);
        String description = AttributedString.stripAnsi(spec.usageMessage().sectionMap().get("description").render(cmdhelp));
        return new ArrayList<>(Arrays.asList(description.split("\\r?\\n")));
    }

```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/core/FSMCallerImpl.java`
#### Snippet
```java
        public String metricName() {
            if (this.metricName == null) {
                this.metricName = "fsm-" + name().toLowerCase().replaceAll("_", "-");
            }
            return this.metricName;
```

### DynamicRegexReplaceableByCompiledPattern
`split()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/replicator/TablePartitionId.java`
#### Snippet
```java
     */
    public static TablePartitionId fromString(String str) {
        String[] parts = str.split("_part_");

        return new TablePartitionId(UUID.fromString(parts[0]), Integer.valueOf(parts[1]));
```

### DynamicRegexReplaceableByCompiledPattern
`matches()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `modules/bytecode/src/main/java/com/facebook/presto/bytecode/instruction/InvokeInstruction.java`
#### Snippet
```java
        }

        checkArgument(!name.matches(".*[.;\\[/<>].*"), "invalid name: %s", name);
    }
}
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/asm/SchemaClassesInfo.java`
#### Snippet
```java
        String schemaClassName = schemaClass.getPackageName() + "." + schemaClass.getSimpleName();

        return schemaClassName.replaceAll("ConfigurationSchema$", "");
    }

```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/util/ConfigurationUtil.java`
#### Snippet
```java
     */
    public static String escape(String key) {
        return key.replaceAll("([.\\\\])", "\\\\$1");
    }

```

### DynamicRegexReplaceableByCompiledPattern
`split()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/util/ConfigurationUtil.java`
#### Snippet
```java
     */
    public static List<String> split(String keys) {
        String[] split = keys.split("(?<!\\\\)[.]", -1);

        for (int i = 0; i < split.length; i++) {
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/util/ConfigurationUtil.java`
#### Snippet
```java
     */
    public static String unescape(String key) {
        return key.replaceAll("\\\\([.\\\\])", "$1");
    }

```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `modules/client-handler/src/main/java/org/apache/ignite/client/handler/requests/jdbc/JdbcMetadataCatalog.java`
#### Snippet
```java
        String toRegex = ' ' + sqlPtrn;

        toRegex = toRegex.replaceAll("([\\[\\]{}()*+?.\\\\\\\\^$|])", "\\\\$1");
        toRegex = toRegex.replaceAll("([^\\\\\\\\])((?:\\\\\\\\\\\\\\\\)*)%", "$1$2.*");
        toRegex = toRegex.replaceAll("([^\\\\\\\\])((?:\\\\\\\\\\\\\\\\)*)_", "$1$2.");
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `modules/client-handler/src/main/java/org/apache/ignite/client/handler/requests/jdbc/JdbcMetadataCatalog.java`
#### Snippet
```java

        toRegex = toRegex.replaceAll("([\\[\\]{}()*+?.\\\\\\\\^$|])", "\\\\$1");
        toRegex = toRegex.replaceAll("([^\\\\\\\\])((?:\\\\\\\\\\\\\\\\)*)%", "$1$2.*");
        toRegex = toRegex.replaceAll("([^\\\\\\\\])((?:\\\\\\\\\\\\\\\\)*)_", "$1$2.");
        toRegex = toRegex.replaceAll("([^\\\\\\\\])(\\\\\\\\(?>\\\\\\\\\\\\\\\\)*\\\\\\\\)*\\\\\\\\([_|%])", "$1$2$3");
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `modules/client-handler/src/main/java/org/apache/ignite/client/handler/requests/jdbc/JdbcMetadataCatalog.java`
#### Snippet
```java
        toRegex = toRegex.replaceAll("([\\[\\]{}()*+?.\\\\\\\\^$|])", "\\\\$1");
        toRegex = toRegex.replaceAll("([^\\\\\\\\])((?:\\\\\\\\\\\\\\\\)*)%", "$1$2.*");
        toRegex = toRegex.replaceAll("([^\\\\\\\\])((?:\\\\\\\\\\\\\\\\)*)_", "$1$2.");
        toRegex = toRegex.replaceAll("([^\\\\\\\\])(\\\\\\\\(?>\\\\\\\\\\\\\\\\)*\\\\\\\\)*\\\\\\\\([_|%])", "$1$2$3");

```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `modules/client-handler/src/main/java/org/apache/ignite/client/handler/requests/jdbc/JdbcMetadataCatalog.java`
#### Snippet
```java
        toRegex = toRegex.replaceAll("([^\\\\\\\\])((?:\\\\\\\\\\\\\\\\)*)%", "$1$2.*");
        toRegex = toRegex.replaceAll("([^\\\\\\\\])((?:\\\\\\\\\\\\\\\\)*)_", "$1$2.");
        toRegex = toRegex.replaceAll("([^\\\\\\\\])(\\\\\\\\(?>\\\\\\\\\\\\\\\\)*\\\\\\\\)*\\\\\\\\([_|%])", "$1$2$3");

        return toRegex.substring(1);
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `modules/configuration-annotation-processor/src/main/java/org/apache/ignite/internal/configuration/processor/ConfigurationProcessorUtils.java`
#### Snippet
```java
        return ClassName.get(
                schemaClassName.packageName(),
                schemaClassName.simpleName().replaceAll("Schema$", "")
        );
    }
```

## RuleId[id=ComparatorMethodParameterNotUsed]
### ComparatorMethodParameterNotUsed
Comparator never returns negative value
in `modules/api/src/main/java/org/apache/ignite/deployment/version/Version.java`
#### Snippet
```java

        @Override
        public int compareTo(Version o) {
            if (o == LATEST) {
                return 0;
```

## RuleId[id=SamePackageImport]
### SamePackageImport
Unnecessary import from the same package `import org.apache.ignite.internal.cli.core.repl.completer.DynamicCompleter;`
in `modules/cli/src/main/java/org/apache/ignite/internal/cli/core/repl/completer/StringDynamicCompleter.java`
#### Snippet
```java
import java.util.Set;
import java.util.stream.Collectors;
import org.apache.ignite.internal.cli.core.repl.completer.DynamicCompleter;

/**
```

### SamePackageImport
Unnecessary import from the same package `import org.apache.ignite.internal.cli.config.Config;`
in `modules/cli/src/testFixtures/java/org/apache/ignite/internal/cli/config/TestStateConfigHelper.java`
#### Snippet
```java

import org.apache.ignite.internal.cli.commands.cliconfig.TestConfigManagerHelper;
import org.apache.ignite.internal.cli.config.Config;
import org.apache.ignite.internal.cli.config.StateConfig;

```

### SamePackageImport
Unnecessary import from the same package `import org.apache.ignite.internal.cli.config.StateConfig;`
in `modules/cli/src/testFixtures/java/org/apache/ignite/internal/cli/config/TestStateConfigHelper.java`
#### Snippet
```java
import org.apache.ignite.internal.cli.commands.cliconfig.TestConfigManagerHelper;
import org.apache.ignite.internal.cli.config.Config;
import org.apache.ignite.internal.cli.config.StateConfig;

/**
```

### SamePackageImport
Unnecessary import from the same package `import org.apache.ignite.network.scalecube.ScaleCubeClusterServiceFactory;`
in `modules/network/src/testFixtures/java/org/apache/ignite/network/scalecube/TestScaleCubeClusterServiceFactory.java`
#### Snippet
```java
import io.scalecube.cluster.ClusterConfig;
import org.apache.ignite.internal.network.configuration.ClusterMembershipView;
import org.apache.ignite.network.scalecube.ScaleCubeClusterServiceFactory;

/**
```

## RuleId[id=SuspiciousToArrayCall]
### SuspiciousToArrayCall
Array of type 'java.util.concurrent.Flow.Publisher\[\]' expected, 'java.util.concurrent.Flow.Publisher\[\]' found
in `modules/core/src/main/java/org/apache/ignite/internal/util/SubscriptionUtils.java`
#### Snippet
```java
        source.forEachRemaining(pubList::add);

        return orderedMerge(comparator, prefetch, pubList.<Publisher<T>>toArray(Publisher[]::new));
    }

```

## RuleId[id=NonProtectedConstructorInAbstractClass]
### NonProtectedConstructorInAbstractClass
Constructor `EventParameters()` of an abstract class should not be declared 'public'
in `modules/core/src/main/java/org/apache/ignite/internal/manager/EventParameters.java`
#### Snippet
```java
     * @param causalityToken Causality token.
     */
    public EventParameters(long causalityToken) {
        this.causalityToken = causalityToken;
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `RpcRequestProcessor()` of an abstract class should not be declared 'public'
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/rpc/RpcRequestProcessor.java`
#### Snippet
```java
    public abstract Message processRequest(final T request, final RpcRequestClosure done);

    public RpcRequestProcessor(Executor executor, RaftMessagesFactory msgFactory) {
        this.executor = executor;
        this.msgFactory = msgFactory;
```

### NonProtectedConstructorInAbstractClass
Constructor `BaseCliRequestProcessor()` of an abstract class should not be declared 'public'
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/rpc/impl/cli/BaseCliRequestProcessor.java`
#### Snippet
```java
    protected static final IgniteLogger LOG = Loggers.forClass(BaseCliRequestProcessor.class);

    public BaseCliRequestProcessor(Executor executor, RaftMessagesFactory msgFactory) {
        super(executor, msgFactory);
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `CommandClosureImpl()` of an abstract class should not be declared 'public'
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/rpc/impl/ActionRequestProcessor.java`
#### Snippet
```java
         * @param command The command.
         */
        public CommandClosureImpl(T command) {
            this.command = command;
        }
```

### NonProtectedConstructorInAbstractClass
Constructor `NodeRequestProcessor()` of an abstract class should not be declared 'public'
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/rpc/impl/core/NodeRequestProcessor.java`
#### Snippet
```java
 */
public abstract class NodeRequestProcessor<T extends Message> extends RpcRequestProcessor<T> {
    public NodeRequestProcessor(Executor executor, RaftMessagesFactory msgFactory) {
        super(executor, msgFactory);
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `RepeatedTimer()` of an abstract class should not be declared 'public'
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/RepeatedTimer.java`
#### Snippet
```java
    }

    public RepeatedTimer(final String name, final int timeoutMs, final Timer timer) {
        super();
        this.name = name;
```

### NonProtectedConstructorInAbstractClass
Constructor `RepeatedTimer()` of an abstract class should not be declared 'public'
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/RepeatedTimer.java`
#### Snippet
```java
    }

    public RepeatedTimer(final String name, final int timeoutMs) {
        this(name, timeoutMs, new HashedWheelTimer(new NamedThreadFactory(name, true, LOG), 1, TimeUnit.MILLISECONDS, 2048));
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `LongHeldDetectingReadWriteLock()` of an abstract class should not be declared 'public'
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/concurrent/LongHeldDetectingReadWriteLock.java`
#### Snippet
```java
    private final Lock wLock;

    public LongHeldDetectingReadWriteLock(long maxBlockingTimeToReport, TimeUnit unit) {
        this(false, maxBlockingTimeToReport, unit);
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `LongHeldDetectingReadWriteLock()` of an abstract class should not be declared 'public'
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/concurrent/LongHeldDetectingReadWriteLock.java`
#### Snippet
```java
    }

    public LongHeldDetectingReadWriteLock(boolean fair, long maxBlockingTimeToReport, TimeUnit unit) {
        final RwLock rwLock = new RwLock(fair);
        final long maxBlockingNanos = unit.toNanos(maxBlockingTimeToReport);
```

### NonProtectedConstructorInAbstractClass
Constructor `StableClosure()` of an abstract class should not be declared 'public'
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/storage/LogManager.java`
#### Snippet
```java
        }

        public StableClosure(final List<LogEntry> entries) {
            super();
            setEntries(entries);
```

### NonProtectedConstructorInAbstractClass
Constructor `StableClosure()` of an abstract class should not be declared 'public'
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/storage/LogManager.java`
#### Snippet
```java
        protected int nEntries;

        public StableClosure() {
            // NO-OP
        }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractConfigurationController()` of an abstract class should not be declared 'public'
in `modules/rest/src/main/java/org/apache/ignite/internal/rest/configuration/AbstractConfigurationController.java`
#### Snippet
```java
    private final ConfigurationPresentation<String> cfgPresentation;

    public AbstractConfigurationController(ConfigurationPresentation<String> cfgPresentation) {
        this.cfgPresentation = cfgPresentation;
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractRowTupleAdapter()` of an abstract class should not be declared 'public'
in `modules/table/src/main/java/org/apache/ignite/internal/table/AbstractRowTupleAdapter.java`
#### Snippet
```java
     * @param row Row.
     */
    public AbstractRowTupleAdapter(@NotNull Row row) {
        this.row = row;
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractMetric()` of an abstract class should not be declared 'public'
in `modules/metrics/src/main/java/org/apache/ignite/internal/metrics/AbstractMetric.java`
#### Snippet
```java
     * @param desc Description.
     */
    public AbstractMetric(String name, @Nullable String desc) {
        assert name != null;
        assert !name.isEmpty();
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractIgniteSpool()` of an abstract class should not be declared 'public'
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rel/AbstractIgniteSpool.java`
#### Snippet
```java
     * AbstractIgniteSpool
     */
    public AbstractIgniteSpool(
            RelOptCluster cluster,
            RelTraitSet traits,
```

### NonProtectedConstructorInAbstractClass
Constructor `IgniteAbstractSqlAlterTable()` of an abstract class should not be declared 'public'
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/sql/IgniteAbstractSqlAlterTable.java`
#### Snippet
```java

    /** Constructor. */
    public IgniteAbstractSqlAlterTable(SqlParserPos pos, boolean ifExists, SqlIdentifier tblName) {
        super(OPERATOR, pos);
        this.ifExists = ifExists;
```

### NonProtectedConstructorInAbstractClass
Constructor `IgniteAbstractSqlAlterZone()` of an abstract class should not be declared 'public'
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/sql/IgniteAbstractSqlAlterZone.java`
#### Snippet
```java

    /** Constructor. */
    public IgniteAbstractSqlAlterZone(SqlParserPos pos, SqlIdentifier name, boolean ifExists) {
        super(OPERATOR, pos);
        this.name = name;
```

### NonProtectedConstructorInAbstractClass
Constructor `StorageScanNode()` of an abstract class should not be declared 'public'
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/StorageScanNode.java`
#### Snippet
```java
     * @param requiredColumns Optional set of column of interest.
     */
    public StorageScanNode(
            ExecutionContext<RowT> ctx,
            RowHandler.RowFactory<RowT> rowFactory,
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractSimpleCondition()` of an abstract class should not be declared 'public'
in `modules/metastorage/src/main/java/org/apache/ignite/internal/metastorage/server/AbstractSimpleCondition.java`
#### Snippet
```java
     * @param key Key identifies an entry which the condition will applied to.
     */
    public AbstractSimpleCondition(@NotNull byte[] key) {
        keys = new byte[][]{ key };
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractCompoundCondition()` of an abstract class should not be declared 'public'
in `modules/metastorage/src/main/java/org/apache/ignite/internal/metastorage/server/AbstractCompoundCondition.java`
#### Snippet
```java
     * @param rightCondition right condition.
     */
    public AbstractCompoundCondition(Condition leftCondition, Condition rightCondition) {
        this.leftCondition = leftCondition;
        this.rightCondition = rightCondition;
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractClusterService()` of an abstract class should not be declared 'public'
in `modules/network-api/src/main/java/org/apache/ignite/network/AbstractClusterService.java`
#### Snippet
```java
     * @param messagingService Messaging service.
     */
    public AbstractClusterService(
            ClusterLocalConfiguration context,
            TopologyService topologyService,
```

### NonProtectedConstructorInAbstractClass
Constructor `NonFragmentableDataPageReader()` of an abstract class should not be declared 'public'
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/datapage/NonFragmentableDataPageReader.java`
#### Snippet
```java
     * @param statisticsHolder used to track statistics about operations
     */
    public NonFragmentableDataPageReader(PageMemory pageMemory, int groupId, IoStatisticsHolder statisticsHolder) {
        this.pageMemory = pageMemory;
        this.groupId = groupId;
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractFreeList()` of an abstract class should not be declared 'public'
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/freelist/AbstractFreeList.java`
#### Snippet
```java
     * @throws IgniteInternalCheckedException If failed.
     */
    public AbstractFreeList(
            int grpId,
            int partId,
```

### NonProtectedConstructorInAbstractClass
Constructor `DataStructure()` of an abstract class should not be declared 'public'
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/datastructure/DataStructure.java`
#### Snippet
```java
     *      PageIdAllocator#FLAG_AUX}.
     */
    public DataStructure(
            String name,
            int grpId,
```

### NonProtectedConstructorInAbstractClass
Constructor `IgniteAbstractTransactionImpl()` of an abstract class should not be declared 'public'
in `modules/transactions/src/main/java/org/apache/ignite/internal/tx/impl/IgniteAbstractTransactionImpl.java`
#### Snippet
```java
     * @param id The id.
     */
    public IgniteAbstractTransactionImpl(TxManager txManager, @NotNull UUID id) {
        this.txManager = txManager;
        this.id = id;
```

### NonProtectedConstructorInAbstractClass
Constructor `DynamicConfiguration()` of an abstract class should not be declared 'public'
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/DynamicConfiguration.java`
#### Snippet
```java
     * @param listenOnly Only adding listeners mode, without the ability to get or update the property value.
     */
    public DynamicConfiguration(
            List<String> prefix,
            String key,
```

### NonProtectedConstructorInAbstractClass
Constructor `ConfigurationChanger()` of an abstract class should not be declared 'public'
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/ConfigurationChanger.java`
#### Snippet
```java
     * @throws IllegalArgumentException If the configuration type of the root keys is not equal to the storage type.
     */
    public ConfigurationChanger(
            Notificator notificator,
            Collection<RootKey<?, ?>> rootKeys,
```

## RuleId[id=TestCaseWithConstructor]
### TestCaseWithConstructor
Initialization logic in constructor `BaseIgniteAbstractTest()` instead of 'setup()' life cycle method
in `modules/core/src/testFixtures/java/org/apache/ignite/internal/testframework/BaseIgniteAbstractTest.java`
#### Snippet
```java
     */
    @SuppressWarnings("AssignmentToStaticFieldFromInstanceMethod")
    protected BaseIgniteAbstractTest() {
        log = Loggers.forClass(getClass());
    }
```

### TestCaseWithConstructor
Initialization logic in constructor `AbstractSortedIndexStorageTest()` instead of 'setup()' life cycle method
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/index/AbstractSortedIndexStorageTest.java`
#### Snippet
```java
    private TablesConfiguration tablesCfg;

    protected AbstractSortedIndexStorageTest() {
        long seed = System.currentTimeMillis();

```

## RuleId[id=CaughtExceptionImmediatelyRethrown]
### CaughtExceptionImmediatelyRethrown
Caught exception `e` is immediately rethrown
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/store/FilePageStoreManager.java`
#### Snippet
```java
                }
            }
        } catch (IgniteInternalCheckedException e) {
            // TODO: IGNITE-16899 By analogy with 2.0, fail a node

```

### CaughtExceptionImmediatelyRethrown
Caught exception `e` is immediately rethrown
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/store/FilePageStoreManager.java`
#### Snippet
```java

            return pageId(partId, flags, pageIdx);
        } catch (IgniteInternalCheckedException e) {
            // TODO: IGNITE-16899 By analogy with 2.0, fail a node

```

### CaughtExceptionImmediatelyRethrown
Caught exception `e` is immediately rethrown
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/store/FilePageStoreManager.java`
#### Snippet
```java

            return pageStore;
        } catch (IgniteInternalCheckedException e) {
            // TODO: IGNITE-16899 By analogy with 2.0, fail a node

```

### CaughtExceptionImmediatelyRethrown
Caught exception `e` is immediately rethrown
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/store/FilePageStoreManager.java`
#### Snippet
```java

            pageStore.read(pageId, pageBuf, keepCrc);
        } catch (IgniteInternalCheckedException e) {
            // TODO: IGNITE-16899 By analogy with 2.0, fail a node

```

### CaughtExceptionImmediatelyRethrown
Caught exception `e` is immediately rethrown
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/tree/BplusTree.java`
#### Snippet
```java
                        readUnlock(pageId, page, pageAddr);
                    }
                } catch (CorruptedDataStructureException e) {
                    throw e;
                } catch (RuntimeException | AssertionError e) {
```

## RuleId[id=AssignmentToMethodParameter]
### AssignmentToMethodParameter
Assignment to method parameter `e`
in `modules/api/src/main/java/org/apache/ignite/lang/IgniteException.java`
#### Snippet
```java
        Objects.requireNonNull(e);

        e = ExceptionUtils.unwrapCause(e);

        if (e instanceof IgniteException) {
```

### AssignmentToMethodParameter
Assignment to method parameter `arr`
in `modules/core/src/main/java/org/apache/ignite/internal/util/ArrayUtils.java`
#### Snippet
```java
            len += len >>> 1; // len *= 1.5
            len = Math.max(len, idx + 1);
            arr = Arrays.copyOf(arr, len);
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `fromIdx`
in `modules/core/src/main/java/org/apache/ignite/internal/util/ArrayUtils.java`
#### Snippet
```java
    public static void clearTail(Object[] arr, int fromIdx) {
        while (fromIdx < arr.length && arr[fromIdx] != null) {
            arr[fromIdx++] = null;
        }
    }
```

### AssignmentToMethodParameter
Assignment to method parameter `nanos`
in `modules/core/src/main/java/org/apache/ignite/internal/util/TemporalTypeUtils.java`
#### Snippet
```java
        switch (precision) {
            case 0:
                nanos = 0;
                break;
            case 1:
```

### AssignmentToMethodParameter
Assignment to method parameter `nanos`
in `modules/core/src/main/java/org/apache/ignite/internal/util/TemporalTypeUtils.java`
#### Snippet
```java
                break;
            case 1:
                nanos = (nanos / 100_000_000) * 100_000_000; // 100ms precision.
                break;
            case 2:
```

### AssignmentToMethodParameter
Assignment to method parameter `nanos`
in `modules/core/src/main/java/org/apache/ignite/internal/util/TemporalTypeUtils.java`
#### Snippet
```java
                break;
            case 2:
                nanos = (nanos / 10_000_000) * 10_000_000; // 10ms precision.
                break;
            case 3: {
```

### AssignmentToMethodParameter
Assignment to method parameter `nanos`
in `modules/core/src/main/java/org/apache/ignite/internal/util/TemporalTypeUtils.java`
#### Snippet
```java
                break;
            case 3: {
                nanos = (nanos / 1_000_000) * 1_000_000; // 1ms precision.
                break;
            }
```

### AssignmentToMethodParameter
Assignment to method parameter `nanos`
in `modules/core/src/main/java/org/apache/ignite/internal/util/TemporalTypeUtils.java`
#### Snippet
```java
            }
            case 4: {
                nanos = (nanos / 100_000) * 100_000; // 100us precision.
                break;
            }
```

### AssignmentToMethodParameter
Assignment to method parameter `nanos`
in `modules/core/src/main/java/org/apache/ignite/internal/util/TemporalTypeUtils.java`
#### Snippet
```java
            }
            case 5: {
                nanos = (nanos / 10_000) * 10_000; // 10us precision.
                break;
            }
```

### AssignmentToMethodParameter
Assignment to method parameter `nanos`
in `modules/core/src/main/java/org/apache/ignite/internal/util/TemporalTypeUtils.java`
#### Snippet
```java
            }
            case 6: {
                nanos = (nanos / 1_000) * 1_000; // 1us precision.
                break;
            }
```

### AssignmentToMethodParameter
Assignment to method parameter `nanos`
in `modules/core/src/main/java/org/apache/ignite/internal/util/TemporalTypeUtils.java`
#### Snippet
```java
            }
            case 7: {
                nanos = (nanos / 100) * 100; // 100ns precision.
                break;
            }
```

### AssignmentToMethodParameter
Assignment to method parameter `nanos`
in `modules/core/src/main/java/org/apache/ignite/internal/util/TemporalTypeUtils.java`
#### Snippet
```java
            }
            case 8: {
                nanos = (nanos / 10) * 10; // 10ns precision.
                break;
            }
```

### AssignmentToMethodParameter
Assignment to method parameter `l`
in `modules/core/src/main/java/org/apache/ignite/internal/util/ByteUtils.java`
#### Snippet
```java
        for (int i = Long.BYTES - 1; i >= 0; i--) {
            bytes[off + i] = (byte) l;
            l >>>= 8;
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `hash`
in `modules/core/src/main/java/org/apache/ignite/internal/util/HashUtils.java`
#### Snippet
```java
     */
    private static long fmix64(long hash) {
        hash ^= (hash >>> 33);
        hash *= 0xff51afd7ed558ccdL;
        hash ^= (hash >>> 33);
```

### AssignmentToMethodParameter
Assignment to method parameter `hash`
in `modules/core/src/main/java/org/apache/ignite/internal/util/HashUtils.java`
#### Snippet
```java
    private static long fmix64(long hash) {
        hash ^= (hash >>> 33);
        hash *= 0xff51afd7ed558ccdL;
        hash ^= (hash >>> 33);
        hash *= 0xc4ceb9fe1a85ec53L;
```

### AssignmentToMethodParameter
Assignment to method parameter `hash`
in `modules/core/src/main/java/org/apache/ignite/internal/util/HashUtils.java`
#### Snippet
```java
        hash ^= (hash >>> 33);
        hash *= 0xff51afd7ed558ccdL;
        hash ^= (hash >>> 33);
        hash *= 0xc4ceb9fe1a85ec53L;
        hash ^= (hash >>> 33);
```

### AssignmentToMethodParameter
Assignment to method parameter `hash`
in `modules/core/src/main/java/org/apache/ignite/internal/util/HashUtils.java`
#### Snippet
```java
        hash *= 0xff51afd7ed558ccdL;
        hash ^= (hash >>> 33);
        hash *= 0xc4ceb9fe1a85ec53L;
        hash ^= (hash >>> 33);
        return hash;
```

### AssignmentToMethodParameter
Assignment to method parameter `hash`
in `modules/core/src/main/java/org/apache/ignite/internal/util/HashUtils.java`
#### Snippet
```java
        hash ^= (hash >>> 33);
        hash *= 0xc4ceb9fe1a85ec53L;
        hash ^= (hash >>> 33);
        return hash;
    }
```

### AssignmentToMethodParameter
Assignment to method parameter `tag`
in `modules/core/src/main/java/org/apache/ignite/internal/util/OffheapReadWriteLock.java`
#### Snippet
```java
     */
    public void init(long lock, int tag) {
        tag &= 0xFFFF;

        assert tag != 0;
```

### AssignmentToMethodParameter
Assignment to method parameter `size`
in `modules/core/src/main/java/org/apache/ignite/internal/util/io/IgniteUnsafeDataOutput.java`
#### Snippet
```java
        }

        size = off + size;

        maxOff = Math.max(maxOff, size);
```

### AssignmentToMethodParameter
Assignment to method parameter `utfLen`
in `modules/core/src/main/java/org/apache/ignite/internal/util/io/IgniteUnsafeDataInput.java`
#### Snippet
```java

            if (avail >= 3 || (long) avail == utfLen) {
                utfLen -= readUtfSpan(sbuf, utfLen);
            } else {
                // shift and refill buffer manually
```

### AssignmentToMethodParameter
Assignment to method parameter `n`
in `modules/core/src/main/java/org/apache/ignite/internal/util/io/IgniteUnsafeDataInput.java`
#### Snippet
```java
    public int skipBytes(int n) {
        if (off + n > max) {
            n = max - off;
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `index`
in `modules/core/src/main/java/org/apache/ignite/internal/util/CollectionUtils.java`
#### Snippet
```java
                for (List<T> list : lists) {
                    if (index >= list.size()) {
                        index -= list.size();
                    } else {
                        return list.get(index);
```

### AssignmentToMethodParameter
Assignment to method parameter `t`
in `modules/core/src/main/java/org/apache/ignite/internal/util/ExceptionUtils.java`
#### Snippet
```java
                result.addAll(getSuppressedList(suppressed));
            }
        } while ((t = t.getCause()) != null);

        return result;
```

### AssignmentToMethodParameter
Assignment to method parameter `e`
in `modules/core/src/main/java/org/apache/ignite/internal/util/ExceptionUtils.java`
#### Snippet
```java
    public static Throwable unwrapCause(Throwable e) {
        while ((e instanceof CompletionException || e instanceof ExecutionException) && e.getCause() != null) {
            e = e.getCause();
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `throwable`
in `modules/core/src/main/java/org/apache/ignite/internal/util/ExceptionUtils.java`
#### Snippet
```java
        while (throwable != null && !list.contains(throwable)) {
            list.add(throwable);
            throwable = getCause(throwable);
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `mtdNames`
in `modules/core/src/main/java/org/apache/ignite/internal/util/ExceptionUtils.java`
#### Snippet
```java
        if (cause == null) {
            if (mtdNames == null) {
                mtdNames = CAUSE_METHOD_NAMES;
            }

```

### AssignmentToMethodParameter
Assignment to method parameter `h`
in `modules/core/src/main/java/org/apache/ignite/internal/util/IgniteUtils.java`
#### Snippet
```java
        // Spread bits to regularize both segment and index locations,
        // using variant of single-word Wang/Jenkins hash.
        h += (h << 15) ^ 0xffffcd7d;
        h ^= (h >>> 10);
        h += (h << 3);
```

### AssignmentToMethodParameter
Assignment to method parameter `h`
in `modules/core/src/main/java/org/apache/ignite/internal/util/IgniteUtils.java`
#### Snippet
```java
        // using variant of single-word Wang/Jenkins hash.
        h += (h << 15) ^ 0xffffcd7d;
        h ^= (h >>> 10);
        h += (h << 3);
        h ^= (h >>> 6);
```

### AssignmentToMethodParameter
Assignment to method parameter `h`
in `modules/core/src/main/java/org/apache/ignite/internal/util/IgniteUtils.java`
#### Snippet
```java
        h += (h << 15) ^ 0xffffcd7d;
        h ^= (h >>> 10);
        h += (h << 3);
        h ^= (h >>> 6);
        h += (h << 2) + (h << 14);
```

### AssignmentToMethodParameter
Assignment to method parameter `h`
in `modules/core/src/main/java/org/apache/ignite/internal/util/IgniteUtils.java`
#### Snippet
```java
        h ^= (h >>> 10);
        h += (h << 3);
        h ^= (h >>> 6);
        h += (h << 2) + (h << 14);

```

### AssignmentToMethodParameter
Assignment to method parameter `h`
in `modules/core/src/main/java/org/apache/ignite/internal/util/IgniteUtils.java`
#### Snippet
```java
        h += (h << 3);
        h ^= (h >>> 6);
        h += (h << 2) + (h << 14);

        return h ^ (h >>> 16);
```

### AssignmentToMethodParameter
Assignment to method parameter `ldr`
in `modules/core/src/main/java/org/apache/ignite/internal/util/IgniteUtils.java`
#### Snippet
```java

        if (ldr == null) {
            ldr = igniteClassLoader;
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `ptr1`
in `modules/core/src/main/java/org/apache/ignite/internal/util/GridUnsafe.java`
#### Snippet
```java
            }

            ptr1 += 8;
            ptr2 += 8;
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `ptr2`
in `modules/core/src/main/java/org/apache/ignite/internal/util/GridUnsafe.java`
#### Snippet
```java

            ptr1 += 8;
            ptr2 += 8;
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `ptr1`
in `modules/core/src/main/java/org/apache/ignite/internal/util/GridUnsafe.java`
#### Snippet
```java
            }

            ptr1++;
            ptr2++;
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `ptr2`
in `modules/core/src/main/java/org/apache/ignite/internal/util/GridUnsafe.java`
#### Snippet
```java

            ptr1++;
            ptr2++;
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `obj`
in `modules/core/src/testFixtures/java/org/apache/ignite/internal/testframework/IgniteTestUtils.java`
#### Snippet
```java

                try {
                    obj = findField(cls, obj, fieldName);
                } catch (NoSuchFieldException e) {
                    // Resolve inner class, if not an inner field.
```

### AssignmentToMethodParameter
Assignment to method parameter `obj`
in `modules/core/src/testFixtures/java/org/apache/ignite/internal/testframework/IgniteTestUtils.java`
#### Snippet
```java
                    }

                    obj = innerCls;
                }
            }
```

### AssignmentToMethodParameter
Assignment to method parameter `cnt`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/rpc/impl/IgniteRpcClient.java`
#### Snippet
```java

        synchronized (this) {
            while(cnt-- > 0) {
                Object[] tmp = blockedMsgs.poll();

```

### AssignmentToMethodParameter
Assignment to method parameter `t`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/LogScheduledThreadPoolExecutor.java`
#### Snippet
```java
            }
            catch (final ExecutionException ee) {
                t = ee.getCause();
            }
            catch (final InterruptedException ie) {
```

### AssignmentToMethodParameter
Assignment to method parameter `length`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/CRC64.java`
#### Snippet
```java
    @Override
    public void update(final byte[] buffer, final int offset, int length) {
        for (int i = offset; length > 0; length--) {
            update(buffer[i++]);
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `maxCommittedIndex`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/core/FSMCallerImpl.java`
#### Snippet
```java
        if (task.type == TaskType.COMMITTED) {
            if (task.committedIndex > maxCommittedIndex) {
                maxCommittedIndex = task.committedIndex;
            }
            task.reset();
```

### AssignmentToMethodParameter
Assignment to method parameter `maxCommittedIndex`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/core/FSMCallerImpl.java`
#### Snippet
```java
                this.currTask = TaskType.COMMITTED;
                doCommitted(maxCommittedIndex);
                maxCommittedIndex = -1L; // reset maxCommittedIndex
            }
            final long startMs = Utils.monotonicMs();
```

### AssignmentToMethodParameter
Assignment to method parameter `maxCommittedIndex`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/core/FSMCallerImpl.java`
#### Snippet
```java
                this.currTask = TaskType.COMMITTED;
                doCommitted(maxCommittedIndex);
                maxCommittedIndex = -1L; // reset maxCommittedIndex
            }
            this.currTask = TaskType.IDLE;
```

### AssignmentToMethodParameter
Assignment to method parameter `t`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/LogThreadPoolExecutor.java`
#### Snippet
```java
            }
            catch (final ExecutionException ee) {
                t = ee.getCause();
            }
            catch (final InterruptedException ie) {
```

### AssignmentToMethodParameter
Assignment to method parameter `index`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/SegmentList.java`
#### Snippet
```java

    public T get(int index) {
        index += this.firstOffset;
        return this.segments.get(index >> SEGMENT_SHIFT).get(index & (SEGMENT_SIZE - 1));
    }
```

### AssignmentToMethodParameter
Assignment to method parameter `len`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/Bytes.java`
#### Snippet
```java

        if (off + len > b.length) {
            len = b.length - off;
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `ticksPerWheel`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/timer/HashedWheelTimer.java`
#### Snippet
```java
        }

        ticksPerWheel = normalizeTicksPerWheel(ticksPerWheel);
        HashedWheelBucket[] wheel = new HashedWheelBucket[ticksPerWheel];
        for (int i = 0; i < wheel.length; i++) {
```

### AssignmentToMethodParameter
Assignment to method parameter `v`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/entity/Checksum.java`
#### Snippet
```java
        if (factors != null && !factors.isEmpty()) {
            for (final Checksum factor : factors) {
                v = checksum(v, factor.checksum());
            }
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `timeoutMillis`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/entity/Task.java`
#### Snippet
```java
            final long start = System.nanoTime();
            closures.add(t.join(timeoutMillis));
            timeoutMillis -= TimeUnit.NANOSECONDS.toMillis(System.nanoTime() - start);
            if (timeoutMillis <= 0) {
                throw new TimeoutException("joined timeout");
```

### AssignmentToMethodParameter
Assignment to method parameter `prevLogIndex`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/core/Replicator.java`
#### Snippet
```java
                // request so that follower would do nothing besides updating its
                // leader timestamp.
                prevLogIndex = 0;
            }
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `offset`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/storage/io/LocalDirReader.java`
#### Snippet
```java
                    else {
                        buf.expandAtMost((int) (maxCount - totalRead));
                        offset += nread;
                    }
                }
```

### AssignmentToMethodParameter
Assignment to method parameter `uri`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/storage/snapshot/remote/RemoteFileCopier.java`
#### Snippet
```java
            return false;
        }
        uri = uri.substring(prefixSize);
        final int slasPos = uri.indexOf('/');
        final String peerId = uri.substring(0, slasPos);
```

### AssignmentToMethodParameter
Assignment to method parameter `uri`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/storage/snapshot/remote/RemoteFileCopier.java`
#### Snippet
```java
        final int slasPos = uri.indexOf('/');
        final String peerId = uri.substring(0, slasPos);
        uri = uri.substring(slasPos + 1);

        try {
```

### AssignmentToMethodParameter
Assignment to method parameter `cls`
in `modules/core/src/main/java/org/apache/ignite/internal/tostring/IgniteToStringBuilder.java`
#### Snippet
```java

        if (cls == null) {
            cls = val.getClass();
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `first`
in `modules/core/src/main/java/org/apache/ignite/internal/tostring/IgniteToStringBuilder.java`
#### Snippet
```java
                    buf.app(", ");
                } else {
                    first = false;
                }

```

### AssignmentToMethodParameter
Assignment to method parameter `t`
in `modules/raft/src/main/java/org/apache/ignite/internal/raft/RaftGroupServiceImpl.java`
#### Snippet
```java
    private static boolean recoverable(Throwable t) {
        if (t instanceof ExecutionException || t instanceof CompletionException) {
            t = t.getCause();
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `totalBatchSize`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/raft/snapshot/outgoing/OutgoingSnapshot.java`
#### Snippet
```java
                batch.add(rowEntry);

                totalBatchSize += rowSizeInBytes(rowEntry.rowVersions());
            }
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `t`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/TableManager.java`
#### Snippet
```java
    ) {
        if (t instanceof CompletionException || t instanceof ExecutionException) {
            t = t.getCause();
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `err`
in `modules/client/src/main/java/org/apache/ignite/internal/client/ReliableChannel.java`
#### Snippet
```java
    private static IgniteClientConnectionException unwrapConnectionException(Throwable err) {
        while (err instanceof CompletionException) {
            err = err.getCause();
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `err`
in `modules/client/src/main/java/org/apache/ignite/internal/client/compute/ClientCompute.java`
#### Snippet
```java
    private <R> R handleMissingTable(String tableName, R res, Throwable err) {
        if (err instanceof CompletionException) {
            err = err.getCause();
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `key`
in `modules/schema/src/main/java/org/apache/ignite/internal/schema/SchemaManager.java`
#### Snippet
```java
        assert pos != -1 : "Unexpected key: " + key;

        key = key.substring(pos + 1);
        return Integer.parseInt(key);
    }
```

### AssignmentToMethodParameter
Assignment to method parameter `ver`
in `modules/schema/src/main/java/org/apache/ignite/internal/schema/registry/SchemaRegistryImpl.java`
#### Snippet
```java
        if (ver == 0) {
            // Use last version (any version may be used) for 0 version, that mean row doesn't contain value.
            ver = schemaCache.lastKey();
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `obj`
in `modules/schema/src/main/java/org/apache/ignite/internal/schema/marshaller/reflection/ColumnBinding.java`
#### Snippet
```java
        @Override
        protected void write0(RowAssembler writer, Object obj) throws Throwable {
            obj = getterHnd.invoke(obj);

            if (obj == null) {
```

### AssignmentToMethodParameter
Assignment to method parameter `e`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/storage/InternalTableImpl.java`
#### Snippet
```java
    private RuntimeException wrapReplicationException(Throwable e) {
        if (e instanceof CompletionException) {
            e = e.getCause();
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `enumClass`
in `modules/network/src/main/java/org/apache/ignite/internal/network/serialization/Classes.java`
#### Snippet
```java
        if (!enumClass.isEnum()) {
            // this is needed for enums where members are represented with anonymous classes
            enumClass = enumClass.getSuperclass();
        }
        return enumClass;
```

### AssignmentToMethodParameter
Assignment to method parameter `val`
in `modules/network/src/main/java/org/apache/ignite/internal/network/direct/stream/DirectByteBufferStreamImplV1.java`
#### Snippet
```java

        if (lastFinished) {
            val++;

            int pos = buf.position();
```

### AssignmentToMethodParameter
Assignment to method parameter `val`
in `modules/network/src/main/java/org/apache/ignite/internal/network/direct/stream/DirectByteBufferStreamImplV1.java`
#### Snippet
```java
                GridUnsafe.putByte(heapArr, baseOff + pos++, b);

                val >>>= 7;
            }

```

### AssignmentToMethodParameter
Assignment to method parameter `val`
in `modules/network/src/main/java/org/apache/ignite/internal/network/direct/stream/DirectByteBufferStreamImplV1.java`
#### Snippet
```java

        if (lastFinished) {
            val++;

            int pos = buf.position();
```

### AssignmentToMethodParameter
Assignment to method parameter `val`
in `modules/network/src/main/java/org/apache/ignite/internal/network/direct/stream/DirectByteBufferStreamImplV1.java`
#### Snippet
```java
                GridUnsafe.putByte(heapArr, baseOff + pos++, b);

                val >>>= 7;
            }

```

### AssignmentToMethodParameter
Assignment to method parameter `fieldValue`
in `modules/network/src/main/java/org/apache/ignite/internal/network/serialization/marshal/StructuredObjectMarshaller.java`
#### Snippet
```java
        if (mergedField.typesAreDifferent()) {
            // TODO: IGNITE-16564 - special handling for numeric values
            fieldValue = mergedField.convertToLocalType(fieldValue);
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `n`
in `modules/bytecode/src/main/java/com/facebook/presto/bytecode/ParameterizedType.java`
#### Snippet
```java
    private static String toInternalIdentifier(Class<?> n) {
        if (n.isArray()) {
            n = n.getComponentType();
            if (n.isPrimitive()) {
                if (n == Byte.TYPE) {
```

### AssignmentToMethodParameter
Assignment to method parameter `i`
in `modules/affinity/src/main/java/org/apache/ignite/internal/affinity/RendezvousAffinityFunction.java`
#### Snippet
```java
     */
    public static int safeAbs(int i) {
        i = Math.abs(i);

        return i < 0 ? 0 : i;
```

### AssignmentToMethodParameter
Assignment to method parameter `dst`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/QueryContext.java`
#### Snippet
```java
    private static List<Object> build(List<Object> dst, Object[] src) {
        if (dst == null) {
            dst = new ArrayList<>();
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `required`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rel/IgniteLimit.java`
#### Snippet
```java

        if (relCollation.satisfies(requiredCollation)) {
            required = required.replace(relCollation);
        } else if (!requiredCollation.satisfies(relCollation)) {
            return null;
```

### AssignmentToMethodParameter
Assignment to method parameter `nodeTraits`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rel/IgniteCorrelatedNestedLoopJoin.java`
#### Snippet
```java
        // We preserve left edge collation only if batch size == 1
        if (variablesSet.size() == 1) {
            nodeTraits = nodeTraits.replace(leftCollation);
        } else {
            nodeTraits = nodeTraits.replace(RelCollations.EMPTY);
```

### AssignmentToMethodParameter
Assignment to method parameter `nodeTraits`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rel/IgniteCorrelatedNestedLoopJoin.java`
#### Snippet
```java
            nodeTraits = nodeTraits.replace(leftCollation);
        } else {
            nodeTraits = nodeTraits.replace(RelCollations.EMPTY);
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `pw`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rel/AbstractIndexScan.java`
#### Snippet
```java
    @Override
    protected RelWriter explainTerms0(RelWriter pw) {
        pw = pw.item("index", idxName)
                .item("type", type.name())
                .itemIf("searchBounds", searchBounds, searchBounds != null);
```

### AssignmentToMethodParameter
Assignment to method parameter `groupSets`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rel/agg/IgniteReduceAggregateBase.java`
#### Snippet
```java
        this.groupSet = groupSet;
        if (groupSets == null) {
            groupSets = List.of(groupSet);
        }
        this.groupSets = groupSets;
```

### AssignmentToMethodParameter
Assignment to method parameter `translatedOperands`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/ReflectiveCallNotNullImplementor.java`
#### Snippet
```java
    public Expression implement(RexToLixTranslator translator,
            RexCall call, List<Expression> translatedOperands) {
        translatedOperands =
                ConverterUtils.fromInternal(method.getParameterTypes(), translatedOperands);
        translatedOperands =
```

### AssignmentToMethodParameter
Assignment to method parameter `translatedOperands`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/ReflectiveCallNotNullImplementor.java`
#### Snippet
```java
        translatedOperands =
                ConverterUtils.fromInternal(method.getParameterTypes(), translatedOperands);
        translatedOperands =
                ConverterUtils.convertAssignableTypes(method.getParameterTypes(), translatedOperands);
        final Expression callExpr;
```

### AssignmentToMethodParameter
Assignment to method parameter `e`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/ExecutionServiceImpl.java`
#### Snippet
```java
    private static RuntimeException convertDdlException(Throwable e) {
        if (e instanceof CompletionException) {
            e = e.getCause();
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `e`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/ExecutionServiceImpl.java`
#### Snippet
```java
            assert e.getCause() != null;
            // Cut off upper configuration error`s as uninformative.
            e = e.getCause();
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `storageType`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/RexExecutorImpl.java`
#### Snippet
```java
                        rowType.getFieldList().get(idx).getType();

                storageType = ((JavaTypeFactory) typeFactory).getJavaClass(fieldType);
            }

```

### AssignmentToMethodParameter
Assignment to method parameter `cnt`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/AbstractSetOpNode.java`
#### Snippet
```java
                    }

                    cnt -= availableRows;
                } else {
                    availableRows = cnt;
```

### AssignmentToMethodParameter
Assignment to method parameter `cnt`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/AbstractSetOpNode.java`
#### Snippet
```java
                    decrementAvailableRows(cntrs, availableRows);

                    cnt = 0;
                }

```

### AssignmentToMethodParameter
Assignment to method parameter `desiredType`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/ExpressionFactoryImpl.java`
#### Snippet
```java

            if (desiredType == null) {
                desiredType = fieldType;
                fieldType = Object.class;
            } else if (fieldType != java.sql.Date.class
```

### AssignmentToMethodParameter
Assignment to method parameter `type`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/ExpressionFactoryImpl.java`
#### Snippet
```java
    private Scalar compile(List<RexNode> nodes, RelDataType type, boolean biInParams) {
        if (type == null) {
            type = emptyType;
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `rowsCnt`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/LimitNode.java`
#### Snippet
```java

        if (offset > 0 && rowsProcessed == 0) {
            rowsCnt = offset + rowsCnt;
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `rowsCnt`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/LimitNode.java`
#### Snippet
```java

        if (fetch > 0) {
            rowsCnt = Math.min(rowsCnt, (fetch + offset) - rowsProcessed);
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `expression`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/RexImpTable.java`
#### Snippet
```java
            final Primitive primitive = Primitive.of(expression.getType());
            if (primitive != null) {
                expression = Expressions.box(expression, primitive);
            }

```

### AssignmentToMethodParameter
Assignment to method parameter `columnDescriptors`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/schema/TableDescriptorImpl.java`
#### Snippet
```java

        if (implicitKeyFound) {
            columnDescriptors = columnDescriptors.stream().map(desc -> descriptorsMap.get(desc.name())).collect(Collectors.toList());
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `targetType`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/prepare/IgniteTypeCoercion.java`
#### Snippet
```java
        // does not support deriving JavaType yet.
        if (RelDataTypeFactoryImpl.isJavaType(targetType)) {
            targetType = ((JavaTypeFactory) factory).toSql(targetType);
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `targetType`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/prepare/IgniteTypeCoercion.java`
#### Snippet
```java
        // does not support deriving JavaType yet.
        if (RelDataTypeFactoryImpl.isJavaType(targetType)) {
            targetType = ((JavaTypeFactory) factory).toSql(targetType);
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `scope`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/prepare/IgniteSqlValidator.java`
#### Snippet
```java

            if (newScope != null) {
                scope = newScope;
            }

```

### AssignmentToMethodParameter
Assignment to method parameter `condition`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/util/RexUtils.java`
#### Snippet
```java
        }

        condition = RexUtil.toCnf(builder(cluster), condition);

        Int2ObjectMap<List<RexCall>> fieldsToPredicates = mapPredicatesToFields(condition, cluster);
```

### AssignmentToMethodParameter
Assignment to method parameter `collation`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/util/RexUtils.java`
#### Snippet
```java
            fields.addAll(lastFields);

            collation = TraitUtils.createCollation(fields);
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `condition`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/util/RexUtils.java`
#### Snippet
```java
        }

        condition = RexUtil.toCnf(builder(cluster), condition);

        Int2ObjectMap<List<RexCall>> fieldsToPredicates = mapPredicatesToFields(condition, cluster);
```

### AssignmentToMethodParameter
Assignment to method parameter `condition`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/util/RexUtils.java`
#### Snippet
```java
            @Nullable ImmutableBitSet requiredColumns
    ) {
        condition = RexUtil.toCnf(builder(cluster), condition);

        Int2ObjectMap<List<RexCall>> fieldsToPredicates = mapPredicatesToFields(condition, cluster);
```

### AssignmentToMethodParameter
Assignment to method parameter `correlate`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/prepare/IgnitePlanner.java`
#### Snippet
```java

                        try {
                            correlate = correlate.copy(
                                    correlate.getTraitSet(),
                                    correlate.getLeft(),
```

### AssignmentToMethodParameter
Assignment to method parameter `rel`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/trait/TraitUtils.java`
#### Snippet
```java

                if (old != null && old != rel) {
                    rel = planner.register(rel, old);
                }

```

### AssignmentToMethodParameter
Assignment to method parameter `rel`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/trait/TraitUtils.java`
#### Snippet
```java
                old = rel;

                rel = convertTrait(planner, fromTrait, toTrait, rel);

                assert rel == null || rel.getTraitSet().getTrait(i).satisfies(toTrait);
```

### AssignmentToMethodParameter
Assignment to method parameter `assignment0`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/metadata/ColocationGroup.java`
#### Snippet
```java
        if (assignment0.size() > assignment1.size()) {
            List<NodeWithTerm> tmp = assignment0;
            assignment0 = assignment1;
            assignment1 = tmp;
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `assignment1`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/metadata/ColocationGroup.java`
#### Snippet
```java
            List<NodeWithTerm> tmp = assignment0;
            assignment0 = assignment1;
            assignment1 = tmp;
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `node`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/externalize/RelJson.java`
#### Snippet
```java
    private Object toJson(RexNode node) {
        // removes calls to SEARCH and the included Sarg and converts them to comparisons
        node = RexUtil.expandSearch(Commons.cluster().getRexBuilder(), null, node);

        Map<String, Object> map;
```

### AssignmentToMethodParameter
Assignment to method parameter `z`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/FullPageId.java`
#### Snippet
```java
     */
    private static long mix64(long z) {
        z = (z ^ (z >>> 32)) * 0x4cd6944c5cc20b6dL;
        z = (z ^ (z >>> 29)) * 0xfc12c5b19d3259e9L;

```

### AssignmentToMethodParameter
Assignment to method parameter `z`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/FullPageId.java`
#### Snippet
```java
    private static long mix64(long z) {
        z = (z ^ (z >>> 32)) * 0x4cd6944c5cc20b6dL;
        z = (z ^ (z >>> 29)) * 0xfc12c5b19d3259e9L;

        return z ^ (z >>> 32);
```

### AssignmentToMethodParameter
Assignment to method parameter `k`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/FullPageId.java`
#### Snippet
```java
     */
    private static int mix32(int k) {
        k = (k ^ (k >>> 16)) * 0x85ebca6b;
        k = (k ^ (k >>> 13)) * 0xc2b2ae35;

```

### AssignmentToMethodParameter
Assignment to method parameter `k`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/FullPageId.java`
#### Snippet
```java
    private static int mix32(int k) {
        k = (k ^ (k >>> 16)) * 0x85ebca6b;
        k = (k ^ (k >>> 13)) * 0xc2b2ae35;

        return k ^ (k >>> 16);
```

### AssignmentToMethodParameter
Assignment to method parameter `cnt`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/tree/io/BplusInnerIo.java`
#### Snippet
```java
        assert srcIdx != dstIdx || srcPageAddr != dstPageAddr;

        cnt *= getItemSize() + PARTITIONLESS_LINK_SIZE_BYTES; // From items to bytes.

        if (dstIdx > srcIdx) {
```

### AssignmentToMethodParameter
Assignment to method parameter `rowBytes`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/tree/io/BplusInnerIo.java`
#### Snippet
```java
        assertPageType(pageAddr);

        rowBytes = super.insert(pageAddr, idx, row, rowBytes, rightId, needRowBytes);

        // Setup reference to the right page on split.
```

### AssignmentToMethodParameter
Assignment to method parameter `rowBytes`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/tree/io/BplusInnerIo.java`
#### Snippet
```java
        setCount(newRootPageAddr, 1);
        setLeft(newRootPageAddr, 0, leftChildId);
        rowBytes = store(newRootPageAddr, 0, row, rowBytes, needRowBytes);
        setRight(newRootPageAddr, 0, rightChildId);

```

### AssignmentToMethodParameter
Assignment to method parameter `cnt`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/tree/io/BplusIo.java`
#### Snippet
```java
        assertPageType(pageAddr);

        cnt--;

        copyItems(pageAddr, pageAddr, idx + 1, idx, cnt - idx, false);
```

### AssignmentToMethodParameter
Assignment to method parameter `cnt`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/tree/io/BplusIo.java`
#### Snippet
```java
        initNewPage(fwdPageAddr, fwdId, pageSize);

        cnt -= mid;

        copyItems(pageAddr, fwdPageAddr, mid, 0, cnt, true);
```

### AssignmentToMethodParameter
Assignment to method parameter `rowBytes`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/tree/io/BplusIo.java`
#### Snippet
```java

            if (needRowBytes) {
                rowBytes = getBytes(pageAddr, off, getItemSize());
            }
        } else {
```

### AssignmentToMethodParameter
Assignment to method parameter `itemId`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/io/AbstractDataPageIo.java`
#### Snippet
```java
                assert indirectId >= directCnt;

                itemId = directItemIndex(getItem(pageAddr, indirectId));

                assert itemId < directCnt;
```

### AssignmentToMethodParameter
Assignment to method parameter `dataOff`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/io/AbstractDataPageIo.java`
#### Snippet
```java

        if (!isEnoughSpace(entryFullSize, dataOff, directCnt, indirectCnt)) {
            dataOff = compactDataEntries(pageAddr, directCnt, pageSize);

            assert isEnoughSpace(entryFullSize, dataOff, directCnt, indirectCnt);
```

### AssignmentToMethodParameter
Assignment to method parameter `show`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/io/AbstractDataPageIo.java`
#### Snippet
```java
            payloadLen &= ~FRAGMENTED_FLAG; // We are fragmented and have a link.
        } else {
            show &= ~SHOW_LINK; // We are not fragmented, never have a link.
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `tailsOff`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/freelist/io/PagesListMetaIo.java`
#### Snippet
```java
            putLong(pageAddr, off + 2, tails[tailsOff].tailId);

            tailsOff++;

            off += ITEM_SIZE;
```

### AssignmentToMethodParameter
Assignment to method parameter `init`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/util/PageHandler.java`
#### Snippet
```java
                doInitPage(pageMem, groupId, pageId, pageAddr, init);
            } else {
                init = pageMem.ioRegistry().resolve(pageAddr);
            }

```

### AssignmentToMethodParameter
Assignment to method parameter `init`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/util/PageHandler.java`
#### Snippet
```java
                    doInitPage(pageMem, groupId, pageId, pageAddr, init);
                } else {
                    init = pageMem.ioRegistry().resolve(pageAddr);
                }

```

### AssignmentToMethodParameter
Assignment to method parameter `pageIdx`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/inmemory/VolatilePageMemory.java`
#### Snippet
```java
         */
        private long absolute(int pageIdx) {
            pageIdx &= IDX_MASK;

            long off = ((long) pageIdx) * sysPageSize;
```

### AssignmentToMethodParameter
Assignment to method parameter `pageIdx`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/inmemory/VolatilePageMemory.java`
#### Snippet
```java
         */
        private int sequenceNumber(int pageIdx) {
            pageIdx &= IDX_MASK;

            return pagesInPrevSegments + pageIdx;
```

### AssignmentToMethodParameter
Assignment to method parameter `tstamp`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/PageHeader.java`
#### Snippet
```java
     */
    public static void writeTimestamp(final long absPtr, long tstamp) {
        tstamp &= 0xFFFFFFFFFFFFFF00L;

        putLongVolatile(null, absPtr, tstamp | 0x01);
```

### AssignmentToMethodParameter
Assignment to method parameter `written`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/freelist/AbstractFreeList.java`
#### Snippet
```java

            // If the full row does not fit into this page write only a fragment.
            written = (written == 0 && oldFreeSpace >= rowSize) ? addRowFull(pageId, pageAddr, io, row, rowSize) :
                    addRowFragment(pageId, pageAddr, io, row, written, rowSize);

```

### AssignmentToMethodParameter
Assignment to method parameter `written`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/freelist/AbstractFreeList.java`
#### Snippet
```java

                    // If the data row was completely written without remainder, proceed to the next.
                    if ((written = writeWholePages(row, statHolder)) == COMPLETE) {
                        continue;
                    }
```

### AssignmentToMethodParameter
Assignment to method parameter `written`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/freelist/AbstractFreeList.java`
#### Snippet
```java
                }

                written = writeRowHnd.addRow(pageId, pageAddr, io, row, written);

                assert written == COMPLETE;
```

### AssignmentToMethodParameter
Assignment to method parameter `written`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/freelist/AbstractFreeList.java`
#### Snippet
```java
        }

        written = write(pageId, writeRowHnd, initIo, row, written, FAIL_I, statHolder);

        assert written != FAIL_I; // We can't fail here.
```

### AssignmentToMethodParameter
Assignment to method parameter `pageId`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/freelist/AbstractFreeList.java`
#### Snippet
```java
                    if (oldBucket != newBucket) {
                        // It is possible that page was concurrently taken for put, in this case put will handle bucket change.
                        pageId = maskPartId ? PageIdUtils.maskPartitionId(pageId) : pageId;

                        putIsNeeded = removeDataPage(pageId, pageAddr, io, oldBucket, statHolder);
```

### AssignmentToMethodParameter
Assignment to method parameter `written`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/freelist/AbstractFreeList.java`
#### Snippet
```java
                IoStatisticsHolder statHolder
        ) throws IgniteInternalCheckedException {
            written = addRow(pageId, pageAddr, iox, row, written);

            putPage(((AbstractDataPageIo<Storable>) iox).getFreeSpace(pageAddr), pageId, pageAddr, statHolder);
```

### AssignmentToMethodParameter
Assignment to method parameter `fileIo`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/store/AbstractFilePageStoreIo.java`
#### Snippet
```java
                while (true) {
                    try {
                        fileIo = null;

                        fileIo = ioFactory.create(filePath, CREATE, READ, WRITE);
```

### AssignmentToMethodParameter
Assignment to method parameter `fileIo`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/store/AbstractFilePageStoreIo.java`
#### Snippet
```java
                        fileIo = null;

                        fileIo = ioFactory.create(filePath, CREATE, READ, WRITE);

                        fileExists = true;
```

### AssignmentToMethodParameter
Assignment to method parameter `nextPageId`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/freelist/PagesList.java`
#### Snippet
```java
                    io.resetCount(pageAddr);

                    nextPageId = io.getNextMetaPageId(pageAddr);
                } finally {
                    writeUnlock(pageId, page, pageAddr, true);
```

### AssignmentToMethodParameter
Assignment to method parameter `nextId`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/freelist/PagesList.java`
#### Snippet
```java

                    // Reread next page ID and go for retry.
                    nextId = io.getNextId(pageAddr);
                } finally {
                    if (curAddr != 0L) {
```

### AssignmentToMethodParameter
Assignment to method parameter `nextPageId`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/freelist/PagesList.java`
#### Snippet
```java
                        if (written == 0) {
                            if (nextPageId == 0L) {
                                nextPageId = allocatePageNoReuse();

                                if (curPage != 0L) {
```

### AssignmentToMethodParameter
Assignment to method parameter `nextPageId`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/freelist/PagesList.java`
#### Snippet
```java
                            }

                            nextPageId = curIo.getNextMetaPageId(curAddr);
                        } else {
                            tailIdx += written;
```

### AssignmentToMethodParameter
Assignment to method parameter `pageId`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/PersistentPageMemory.java`
#### Snippet
```java
     */
    public static int segmentIndex(int grpId, long pageId, int segments) {
        pageId = effectivePageId(pageId);

        // Take a prime number larger than total number of partitions.
```

### AssignmentToMethodParameter
Assignment to method parameter `begin`
in `modules/binary-tuple/src/main/java/org/apache/ignite/internal/binarytuple/BinaryTupleParser.java`
#### Snippet
```java
        if (buffer.hasArray()) {
            bytes = buffer.array();
            begin += buffer.arrayOffset();
        } else {
            bytes = bytesValue(begin, end);
```

### AssignmentToMethodParameter
Assignment to method parameter `begin`
in `modules/binary-tuple/src/main/java/org/apache/ignite/internal/binarytuple/BinaryTupleParser.java`
#### Snippet
```java
        } else {
            bytes = bytesValue(begin, end);
            begin = 0;
        }
        return new BigInteger(bytes, begin, len);
```

### AssignmentToMethodParameter
Assignment to method parameter `begin`
in `modules/binary-tuple/src/main/java/org/apache/ignite/internal/binarytuple/BinaryTupleParser.java`
#### Snippet
```java
        if (buffer.hasArray()) {
            bytes = buffer.array();
            begin += buffer.arrayOffset();
        } else {
            bytes = bytesValue(begin, end);
```

### AssignmentToMethodParameter
Assignment to method parameter `begin`
in `modules/binary-tuple/src/main/java/org/apache/ignite/internal/binarytuple/BinaryTupleParser.java`
#### Snippet
```java
        } else {
            bytes = bytesValue(begin, end);
            begin = 0;
        }
        return new String(bytes, begin, len, StandardCharsets.UTF_8);
```

### AssignmentToMethodParameter
Assignment to method parameter `totalValueSize`
in `modules/binary-tuple/src/main/java/org/apache/ignite/internal/binarytuple/BinaryTupleBuilder.java`
#### Snippet
```java
    private int estimateBufferCapacity(int totalValueSize) {
        if (totalValueSize < 0) {
            totalValueSize = Integer.max(numElements * 8, DEFAULT_BUFFER_SIZE);
        }
        return valueBase + totalValueSize;
```

### AssignmentToMethodParameter
Assignment to method parameter `count`
in `modules/client-common/src/main/java/org/apache/ignite/internal/client/proto/ClientMessageUnpacker.java`
#### Snippet
```java
                case FIXMAP: {
                    int mapLen = code & 0x0f;
                    count += mapLen * 2;
                    break;
                }
```

### AssignmentToMethodParameter
Assignment to method parameter `count`
in `modules/client-common/src/main/java/org/apache/ignite/internal/client/proto/ClientMessageUnpacker.java`
#### Snippet
```java
                case FIXARRAY: {
                    int arrayLen = code & 0x0f;
                    count += arrayLen;
                    break;
                }
```

### AssignmentToMethodParameter
Assignment to method parameter `count`
in `modules/client-common/src/main/java/org/apache/ignite/internal/client/proto/ClientMessageUnpacker.java`
#### Snippet
```java

                case ARRAY16:
                    count += readLength16();
                    break;

```

### AssignmentToMethodParameter
Assignment to method parameter `count`
in `modules/client-common/src/main/java/org/apache/ignite/internal/client/proto/ClientMessageUnpacker.java`
#### Snippet
```java

                case ARRAY32:
                    count += readLength32();
                    break;

```

### AssignmentToMethodParameter
Assignment to method parameter `count`
in `modules/client-common/src/main/java/org/apache/ignite/internal/client/proto/ClientMessageUnpacker.java`
#### Snippet
```java

                case MAP16:
                    count += readLength16() * 2;
                    break;

```

### AssignmentToMethodParameter
Assignment to method parameter `count`
in `modules/client-common/src/main/java/org/apache/ignite/internal/client/proto/ClientMessageUnpacker.java`
#### Snippet
```java

                case MAP32:
                    count += readLength32() * 2;
                    break;

```

### AssignmentToMethodParameter
Assignment to method parameter `count`
in `modules/client-common/src/main/java/org/apache/ignite/internal/client/proto/ClientMessageUnpacker.java`
#### Snippet
```java
            }

            count--;
        }
    }
```

### AssignmentToMethodParameter
Assignment to method parameter `namedList`
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/util/ConfigurationUtil.java`
#### Snippet
```java
                node.construct(key, EMPTY_CFG_SRC, true);

                namedList = node.traverseChild(key, namedListNodeVisitor(), true);

                for (String namedListKey : namedList.namedListKeys()) {
```

### AssignmentToMethodParameter
Assignment to method parameter `err`
in `modules/client-handler/src/main/java/org/apache/ignite/client/handler/ClientInboundMessageHandler.java`
#### Snippet
```java

    private void writeErrorCore(Throwable err, ClientMessagePacker packer) {
        err = ExceptionUtils.unwrapCause(err);

        if (err instanceof IgniteException) {
```

### AssignmentToMethodParameter
Assignment to method parameter `t`
in `modules/placement-driver/src/main/java/org/apache/ignite/internal/raft/client/TopologyAwareRaftGroupService.java`
#### Snippet
```java
    private static boolean recoverable(Throwable t) {
        if (t instanceof ExecutionException || t instanceof CompletionException) {
            t = t.getCause();
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `schemaFieldType`
in `modules/configuration-api/src/main/java/org/apache/ignite/configuration/validation/ValidatorChecker.java`
#### Snippet
```java
            }

            schemaFieldType = schemaFieldType.getSuperclass();
        }
    }
```

### AssignmentToMethodParameter
Assignment to method parameter `schemaFieldType`
in `modules/configuration-api/src/main/java/org/apache/ignite/configuration/validation/ValidatorChecker.java`
#### Snippet
```java
            switch (schemaFieldType.getSimpleName()) {
                case "boolean":
                    schemaFieldType = Boolean.class;
                    break;

```

### AssignmentToMethodParameter
Assignment to method parameter `schemaFieldType`
in `modules/configuration-api/src/main/java/org/apache/ignite/configuration/validation/ValidatorChecker.java`
#### Snippet
```java

                case "byte":
                    schemaFieldType = Byte.class;
                    break;

```

### AssignmentToMethodParameter
Assignment to method parameter `schemaFieldType`
in `modules/configuration-api/src/main/java/org/apache/ignite/configuration/validation/ValidatorChecker.java`
#### Snippet
```java

                case "short":
                    schemaFieldType = Short.class;
                    break;

```

### AssignmentToMethodParameter
Assignment to method parameter `schemaFieldType`
in `modules/configuration-api/src/main/java/org/apache/ignite/configuration/validation/ValidatorChecker.java`
#### Snippet
```java

                case "int":
                    schemaFieldType = Integer.class;
                    break;

```

### AssignmentToMethodParameter
Assignment to method parameter `schemaFieldType`
in `modules/configuration-api/src/main/java/org/apache/ignite/configuration/validation/ValidatorChecker.java`
#### Snippet
```java

                case "long":
                    schemaFieldType = Long.class;
                    break;

```

### AssignmentToMethodParameter
Assignment to method parameter `schemaFieldType`
in `modules/configuration-api/src/main/java/org/apache/ignite/configuration/validation/ValidatorChecker.java`
#### Snippet
```java

                case "float":
                    schemaFieldType = Float.class;
                    break;

```

### AssignmentToMethodParameter
Assignment to method parameter `schemaFieldType`
in `modules/configuration-api/src/main/java/org/apache/ignite/configuration/validation/ValidatorChecker.java`
#### Snippet
```java

                case "double":
                    schemaFieldType = Double.class;
                    break;

```

### AssignmentToMethodParameter
Assignment to method parameter `schemaFieldType`
in `modules/configuration-api/src/main/java/org/apache/ignite/configuration/validation/ValidatorChecker.java`
#### Snippet
```java

                case "char":
                    schemaFieldType = Character.class;
                    break;

```

### AssignmentToMethodParameter
Assignment to method parameter `dataOff`
in `modules/storage-page-memory/src/main/java/org/apache/ignite/internal/storage/pagememory/index/freelist/io/IndexColumnsDataIo.java`
#### Snippet
```java
        putShort(pageAddr, dataOff, (short) payloadSize);

        dataOff += Short.BYTES;

        putInt(pageAddr, dataOff + IndexColumns.SIZE_OFFSET, row.valueSize());
```

### AssignmentToMethodParameter
Assignment to method parameter `type`
in `modules/configuration-annotation-processor/src/main/java/org/apache/ignite/internal/configuration/processor/ConfigurationProcessor.java`
#### Snippet
```java
    private boolean isValidValueAnnotationFieldType(TypeMirror type) {
        if (type.getKind() == TypeKind.ARRAY) {
            type = ((ArrayType) type).getComponentType();
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `startIdx`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/tree/BplusTree.java`
#### Snippet
```java

            if (startIdx == -1) {
                startIdx = findLowerBound(pageAddr, io, cnt);
            }

```

### AssignmentToMethodParameter
Assignment to method parameter `cnt`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/tree/BplusTree.java`
#### Snippet
```java
            }

            cnt = findUpperBound(pageAddr, io, startIdx, cnt);

            for (int i = startIdx; i < cnt; i++) {
```

### AssignmentToMethodParameter
Assignment to method parameter `idx`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/tree/BplusTree.java`
#### Snippet
```java
        private boolean isChild(Tail<L> prnt, Tail<L> child, int idx, int cnt, boolean right) {
            if (right && cnt != 0) {
                idx++;
            }

```

### AssignmentToMethodParameter
Assignment to method parameter `t`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/tree/BplusTree.java`
#### Snippet
```java
                }

                t = t.down;
            }
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `startIdx`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/tree/BplusTree.java`
#### Snippet
```java
        boolean fillFromBuffer0(long pageAddr, BplusIo<L> io, int startIdx, int cnt) throws IgniteInternalCheckedException {
            if (startIdx == -1) {
                startIdx = findLowerBound(pageAddr, io, cnt);
            }

```

### AssignmentToMethodParameter
Assignment to method parameter `cnt`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/tree/BplusTree.java`
#### Snippet
```java
                assert idx < 0;

                cnt = fix(idx);

                nextPageId = 0; // The End.
```

### AssignmentToMethodParameter
Assignment to method parameter `minRow`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/tree/BplusTree.java`
#### Snippet
```java
                        }

                        minRow = io.getLookupRow(this, pageAddr, i);
                    }

```

### AssignmentToMethodParameter
Assignment to method parameter `minRow`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/tree/BplusTree.java`
#### Snippet
```java
                    validateDownKeys(leftId, minRow, lvl - 1);

                    minRow = row;
                }

```

### AssignmentToMethodParameter
Assignment to method parameter `low`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/tree/BplusTree.java`
#### Snippet
```java
            //noinspection Duplicates
            if (cmp < 0) {
                low = mid + 1;
            } else if (cmp > 0) {
                high = mid - 1;
```

### AssignmentToMethodParameter
Assignment to method parameter `lvl`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/tree/BplusTree.java`
#### Snippet
```java

            if (lvl < 0) {
                lvl = io.getRootLevel(pageAddr);
            }

```

### AssignmentToMethodParameter
Assignment to method parameter `startIdx`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/tree/BplusTree.java`
#### Snippet
```java
            if (startIdx == -1) {
                if (lowerBound != null) {
                    startIdx = findLowerBound(pageAddr, io, cnt);
                } else {
                    startIdx = 0;
```

### AssignmentToMethodParameter
Assignment to method parameter `startIdx`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/tree/BplusTree.java`
#### Snippet
```java
                    startIdx = findLowerBound(pageAddr, io, cnt);
                } else {
                    startIdx = 0;
                }
            }
```

### AssignmentToMethodParameter
Assignment to method parameter `cnt`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/tree/BplusTree.java`
#### Snippet
```java

            if (upperBound != null && cnt != startIdx) {
                cnt = findUpperBound(pageAddr, io, startIdx, cnt);
            }

```

### AssignmentToMethodParameter
Assignment to method parameter `idx`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/tree/BplusTree.java`
#### Snippet
```java

        if (idx < 0) {
            idx = -idx - 1;
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `cnt`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/tree/BplusTree.java`
#### Snippet
```java
                assert idx < 0;

                cnt = fix(idx);

                nextPageId = 0; // The End.
```

### AssignmentToMethodParameter
Assignment to method parameter `io`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/tree/BplusTree.java`
#### Snippet
```java
                        try {
                            if (io.isLeaf()) {
                                io = latestInnerIo();
                            }

```

### AssignmentToMethodParameter
Assignment to method parameter `fwdId`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/tree/BplusTree.java`
#### Snippet
```java
                                }

                                fwdId = inner(io).getLeft(fwdPageAddr, 0, partId);
                            } finally {
                                readUnlock(fwdId0, fwdPage, fwdPageAddr);
```

## RuleId[id=ReturnNull]
### ReturnNull
Return of `null`
in `modules/api/src/main/java/org/apache/ignite/tx/IgniteTransactions.java`
#### Snippet
```java
        runInTransaction(tx -> {
            clo.accept(tx);
            return null;
        }, options);
    }
```

### ReturnNull
Return of `null`
in `modules/api/src/main/java/org/apache/ignite/table/TupleImpl.java`
#### Snippet
```java
            @Override
            public Object next() {
                return hasNext() ? colValues.get(cur++) : null;
            }
        };
```

### ReturnNull
Return of `null`
in `modules/cli/src/main/java/org/apache/ignite/internal/cli/sql/SqlSchema.java`
#### Snippet
```java
            }
        }
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `modules/cli/src/main/java/org/apache/ignite/internal/cli/sql/table/Table.java`
#### Snippet
```java
            return new Table<>(ids, content);
        } catch (SQLException e) {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `modules/cli/src/main/java/org/apache/ignite/internal/cli/call/connect/ConnectCall.java`
#### Snippet
```java
        } catch (MalformedURLException ignored) {
            // Shouldn't happen ever since we are now connected to this URL
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `modules/cli/src/main/java/org/apache/ignite/internal/cli/core/repl/registry/impl/JdbcUrlRegistryImpl.java`
#### Snippet
```java
        } catch (Exception e) {
            log.warn("Couldn't fetch jdbc url of " + nodeUrl + " node: ", e);
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `modules/cli/src/main/java/org/apache/ignite/internal/cli/core/repl/registry/impl/JdbcUrlRegistryImpl.java`
#### Snippet
```java
        } catch (MalformedURLException ignored) {
            // Shouldn't happen ever since we are now connected to this URL
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `modules/cli/src/main/java/org/apache/ignite/internal/cli/core/repl/executor/ReplExecutor.java`
#### Snippet
```java
        });
        // Workaround for jline issue where TailTipWidgets will produce NPE when passed a bracket
        registry.setScriptDescription(cmdLine -> null);
        return widgets;
    }
```

### ReturnNull
Return of `null`
in `modules/cli/src/main/java/org/apache/ignite/internal/cli/core/repl/executor/IgnitePicocliCommands.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `modules/cli/src/main/java/org/apache/ignite/internal/cli/core/repl/executor/IgnitePicocliCommands.java`
#### Snippet
```java
        Arrays.stream(args).map(Object::toString).forEach(arguments::add);
        cmd.execute(arguments.toArray(new String[0]));
        return null;
    }

```

### ReturnNull
Return of `null`
in `modules/cli/src/main/java/org/apache/ignite/internal/cli/core/repl/executor/IgnitePicocliCommands.java`
#### Snippet
```java
        CommandLine sub = findSubcommandLine(args, args.size());
        if (sub == null) {
            return null;
        }
        CommandSpec spec = sub.getCommandSpec();
```

### ReturnNull
Return of `null`
in `modules/cli/src/main/java/org/apache/ignite/internal/cli/commands/cluster/ClusterUrlMixin.java`
#### Snippet
```java

    public String getClusterUrl() {
        return clusterUrl != null ? clusterUrl.toString() : null;
    }
}
```

### ReturnNull
Return of `null`
in `modules/core/src/main/java/org/apache/ignite/lang/IgniteStringFormatter.java`
#### Snippet
```java
    public static String format(final String messagePattern, final Object... params) {
        if (messagePattern == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `modules/core/src/main/java/org/apache/ignite/lang/ErrorGroup.java`
#### Snippet
```java
    public static String extractCauseMessage(String errorMessage) {
        if (errorMessage == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `modules/core/src/main/java/org/apache/ignite/internal/util/OffheapReadWriteLock.java`
#### Snippet
```java

            if (!checkTag(state, tag)) {
                return null;
            }

```

### ReturnNull
Return of `null`
in `modules/core/src/main/java/org/apache/ignite/internal/util/OffheapReadWriteLock.java`
#### Snippet
```java

                if (!checkTag(state, tag)) {
                    return null;
                }

```

### ReturnNull
Return of `null`
in `modules/core/src/main/java/org/apache/ignite/internal/thread/IgniteThread.java`
#### Snippet
```java

        return thread.getClass() == IgniteThread.class || thread instanceof IgniteThread
            ? ((IgniteThread) thread) : null;
    }

```

### ReturnNull
Return of `null`
in `modules/core/src/main/java/org/apache/ignite/internal/util/ExceptionUtils.java`
#### Snippet
```java
    public static Throwable getCause(Throwable throwable, String[] mtdNames) {
        if (throwable == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `modules/core/src/main/java/org/apache/ignite/internal/util/ExceptionUtils.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `modules/core/src/main/java/org/apache/ignite/internal/util/ExceptionUtils.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `modules/core/src/main/java/org/apache/ignite/internal/util/ExceptionUtils.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `modules/core/src/main/java/org/apache/ignite/internal/util/GridUnsafe.java`
#### Snippet
```java
                mtd.setAccessible(true);

                return null;
            });

```

### ReturnNull
Return of `null`
in `modules/core/src/main/java/org/apache/ignite/internal/util/GridUnsafe.java`
#### Snippet
```java
                ctor.setAccessible(true);

                return null;
            });

```

### ReturnNull
Return of `null`
in `modules/core/src/main/java/org/apache/ignite/internal/causality/VersionedValue.java`
#### Snippet
```java
        }

        return defaultValRef == null ? null : defaultValRef.get();
    }

```

### ReturnNull
Return of `null`
in `modules/jdbc/src/main/java/org/apache/ignite/internal/jdbc/JdbcStatement.java`
#### Snippet
```java
        ensureNotClosed();

        return null;
    }

```

### ReturnNull
Return of `null`
in `modules/jdbc/src/main/java/org/apache/ignite/internal/jdbc/JdbcStatement.java`
#### Snippet
```java

        if (resSets == null || curRes >= resSets.size()) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `modules/jdbc/src/main/java/org/apache/ignite/internal/jdbc/JdbcStatement.java`
#### Snippet
```java

        if (!rs.isQuery()) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `modules/jdbc/src/main/java/org/apache/ignite/internal/jdbc/IgniteJdbcDriver.java`
#### Snippet
```java
    public Connection connect(String url, Properties props) throws SQLException {
        if (!acceptsURL(url)) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `modules/jdbc/src/main/java/org/apache/ignite/internal/jdbc/ConnectionPropertiesImpl.java`
#### Snippet
```java
        } else {
            if (ArrayUtils.nullOrEmpty(getAddresses())) {
                return null;
            }

```

### ReturnNull
Return of `null`
in `modules/jdbc/src/main/java/org/apache/ignite/internal/jdbc/ConnectionPropertiesImpl.java`
#### Snippet
```java
         */
        Long value() {
            return val != null ? val.longValue() : null;
        }
    }
```

### ReturnNull
Return of `null`
in `modules/jdbc/src/main/java/org/apache/ignite/internal/jdbc/ConnectionPropertiesImpl.java`
#### Snippet
```java
         */
        Integer value() {
            return val != null ? val.intValue() : null;
        }
    }
```

### ReturnNull
Return of `null`
in `modules/jdbc/src/main/java/org/apache/ignite/internal/jdbc/ConnectionPropertiesImpl.java`
#### Snippet
```java
        @Override
        String valueObject() {
            return val != null ? String.valueOf(val) : null;
        }

```

### ReturnNull
Return of `null`
in `modules/core/src/testFixtures/java/org/apache/ignite/internal/testframework/IgniteTestUtils.java`
#### Snippet
```java
        assert false;

        return null;
    }

```

### ReturnNull
Return of `null`
in `modules/core/src/testFixtures/java/org/apache/ignite/internal/testframework/IgniteTestUtils.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `modules/core/src/testFixtures/java/org/apache/ignite/internal/testframework/IgniteTestUtils.java`
#### Snippet
```java
            run.run();

            return null;
        }, threadNum, threadName);
    }
```

### ReturnNull
Return of `null`
in `modules/core/src/testFixtures/java/org/apache/ignite/internal/testframework/IgniteTestUtils.java`
#### Snippet
```java
            }

            return null;
        }, threadName);
    }
```

### ReturnNull
Return of `null`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/rpc/RpcProcessor.java`
#### Snippet
```java
     */
    default Executor executor() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/rpc/RpcProcessor.java`
#### Snippet
```java
     */
    default ExecutorSelector executorSelector() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/Status.java`
#### Snippet
```java
     */
    public String getErrorMsg() {
        return this.state == null ? null : this.state.msg;
    }
}
```

### ReturnNull
Return of `null`
in `modules/jdbc/src/main/java/org/apache/ignite/internal/jdbc/JdbcConnection.java`
#### Snippet
```java
        ensureNotClosed();

        return null;
    }

```

### ReturnNull
Return of `null`
in `modules/jdbc/src/main/java/org/apache/ignite/internal/jdbc/JdbcConnection.java`
#### Snippet
```java
        ensureNotClosed();

        return null;
    }

```

### ReturnNull
Return of `null`
in `modules/jdbc/src/main/java/org/apache/ignite/internal/jdbc/JdbcConnection.java`
#### Snippet
```java
        ensureNotClosed();

        return null;
    }

```

### ReturnNull
Return of `null`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/rpc/impl/cli/ChangePeersRequestProcessor.java`
#### Snippet
```java
            }
        });
        return null;
    }

```

### ReturnNull
Return of `null`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/rpc/impl/cli/SnapshotRequestProcessor.java`
#### Snippet
```java
        LOG.info("Receive SnapshotRequest to {} from {}", ctx.node.getNodeId(), request.peerId());
        ctx.node.snapshot(done);
        return null;
    }

```

### ReturnNull
Return of `null`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/rpc/impl/cli/RemoveLearnersRequestProcessor.java`
#### Snippet
```java
        });

        return null;
    }

```

### ReturnNull
Return of `null`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/rpc/impl/cli/RemovePeerRequestProcessor.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/rpc/impl/cli/AddPeerRequestProcessor.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/rpc/impl/cli/AddLearnersRequestProcessor.java`
#### Snippet
```java
        });

        return null;
    }

```

### ReturnNull
Return of `null`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/rpc/impl/cli/GetLeaderRequestProcessor.java`
#### Snippet
```java
        final IgniteCliRpcRequestClosure done) {
        // ignore
        return null;
    }

```

### ReturnNull
Return of `null`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/rpc/impl/cli/ChangePeersAsyncRequestProcessor.java`
#### Snippet
```java
            }
        });
        return null;
    }

```

### ReturnNull
Return of `null`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/rpc/impl/cli/ResetLearnersRequestProcessor.java`
#### Snippet
```java
        });

        return null;
    }

```

### ReturnNull
Return of `null`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/rpc/impl/core/ReadIndexRequestProcessor.java`
#### Snippet
```java

        });
        return null;
    }

```

### ReturnNull
Return of `null`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/core/IteratorImpl.java`
#### Snippet
```java
    public Closure done() {
        if (this.currentIndex < this.firstClosureIndex) {
            return null;
        }
        return this.closures.get((int) (this.currentIndex - this.firstClosureIndex));
```

### ReturnNull
Return of `null`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/rpc/impl/core/AppendEntriesRequestProcessor.java`
#### Snippet
```java
            }

            return null;
        }
        else {
```

### ReturnNull
Return of `null`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/rpc/impl/core/AppendEntriesRequestProcessor.java`
#### Snippet
```java

        if (groupContexts == null) {
            return null;
        }
        return groupContexts.get(pair);
```

### ReturnNull
Return of `null`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/core/IteratorWrapper.java`
#### Snippet
```java
    public ByteBuffer getData() {
        final LogEntry entry = this.impl.entry();
        return entry != null ? entry.getData() : null;
    }

```

### ReturnNull
Return of `null`
in `modules/jdbc/src/main/java/org/apache/ignite/internal/jdbc/JdbcResultSet.java`
#### Snippet
```java
        Object val = getValue(colIdx);

        return val == null ? null : String.valueOf(val);
    }

```

### ReturnNull
Return of `null`
in `modules/jdbc/src/main/java/org/apache/ignite/internal/jdbc/JdbcResultSet.java`
#### Snippet
```java
        BigDecimal val = getBigDecimal(colIdx);

        return val == null ? null : val.setScale(scale, RoundingMode.HALF_UP);
    }

```

### ReturnNull
Return of `null`
in `modules/jdbc/src/main/java/org/apache/ignite/internal/jdbc/JdbcResultSet.java`
#### Snippet
```java

            if (val == null) {
                return null;
            }

```

### ReturnNull
Return of `null`
in `modules/jdbc/src/main/java/org/apache/ignite/internal/jdbc/JdbcResultSet.java`
#### Snippet
```java

        if (val == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `modules/jdbc/src/main/java/org/apache/ignite/internal/jdbc/JdbcResultSet.java`
#### Snippet
```java
        ensureNotClosed();

        return null;
    }

```

### ReturnNull
Return of `null`
in `modules/jdbc/src/main/java/org/apache/ignite/internal/jdbc/JdbcResultSet.java`
#### Snippet
```java

        if (val == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `modules/jdbc/src/main/java/org/apache/ignite/internal/jdbc/JdbcResultSet.java`
#### Snippet
```java

        if (val == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `modules/jdbc/src/main/java/org/apache/ignite/internal/jdbc/JdbcResultSet.java`
#### Snippet
```java

        if (val == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `modules/jdbc/src/main/java/org/apache/ignite/internal/jdbc/JdbcResultSet.java`
#### Snippet
```java
        ensureNotClosed();

        return null;
    }

```

### ReturnNull
Return of `null`
in `modules/jdbc/src/main/java/org/apache/ignite/internal/jdbc/JdbcResultSet.java`
#### Snippet
```java

        if (val == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `modules/jdbc/src/main/java/org/apache/ignite/internal/jdbc/JdbcResultSet.java`
#### Snippet
```java

        if (val == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/core/ReplicatorGroupImpl.java`
#### Snippet
```java

        if (maxIndex == -1L) {
            return null;
        }
        else {
```

### ReturnNull
Return of `null`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/BytesUtil.java`
#### Snippet
```java
    public static byte[] hexStringToByteArray(final String s) {
        if (s == null) {
            return null;
        }
        final int len = s.length();
```

### ReturnNull
Return of `null`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/BytesUtil.java`
#### Snippet
```java
    public static String toHex(final byte[] bytes) {
        if (bytes == null) {
            return null;
        }
        final char[] hexChars = new char[bytes.length * 2];
```

### ReturnNull
Return of `null`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/Utils.java`
#### Snippet
```java
    public static Future<?> runClosureInThread(ExecutorService executor, final Closure done) {
        if (done == null) {
            return null;
        }
        return runClosureInThread(executor, done, Status.OK());
```

### ReturnNull
Return of `null`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/Utils.java`
#### Snippet
```java
        final Status status) {
        if (done == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/StringUtils.java`
#### Snippet
```java

        if (str == null) {
            return null;
        }
        int len = str.length();
```

### ReturnNull
Return of `null`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/StringUtils.java`
#### Snippet
```java
    private static String[] splitWorker(String str, char separatorChar, boolean preserveAllTokens) {
        if (str == null) {
            return null;
        }
        else {
```

### ReturnNull
Return of `null`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/ThreadId.java`
#### Snippet
```java
    public Object lock() {
        if (this.destroyed) {
            return null;
        }
        try {
```

### ReturnNull
Return of `null`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/ThreadId.java`
#### Snippet
```java
            while (!this.lock.tryLock(TRY_LOCK_TIMEOUT_MS, TimeUnit.MILLISECONDS)) {
                if (this.destroyed) {
                    return null;
                }
            }
```

### ReturnNull
Return of `null`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/ThreadId.java`
#### Snippet
```java
        catch (final InterruptedException e) {
            Thread.currentThread().interrupt(); // reset
            return null;
        }
        // Got the lock, double checking state.
```

### ReturnNull
Return of `null`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/ThreadId.java`
#### Snippet
```java
            // should release lock
            this.lock.unlock();
            return null;
        }
        return this.data;
```

### ReturnNull
Return of `null`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/SegmentList.java`
#### Snippet
```java
            return this.segments.peekFirst();
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/SegmentList.java`
#### Snippet
```java
    public T peekLast() {
        Segment<T> lastSeg = getLast();
        return lastSeg == null ? null : lastSeg.peekLast();
    }

```

### ReturnNull
Return of `null`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/SegmentList.java`
#### Snippet
```java
            return this.segments.peekLast();
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/SegmentList.java`
#### Snippet
```java
    public T peekFirst() {
        Segment<T> firstSeg = getFirst();
        return firstSeg == null ? null : firstSeg.peekFirst();
    }

```

### ReturnNull
Return of `null`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/Recyclers.java`
#### Snippet
```java
            if (size == 0) {
                if (!scavenge()) {
                    return null;
                }
                size = this.size;
```

### ReturnNull
Return of `null`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/timer/HashedWheelTimer.java`
#### Snippet
```java
            HashedWheelTimeout head = this.head;
            if (head == null) {
                return null;
            }
            HashedWheelTimeout next = head.next;
```

### ReturnNull
Return of `null`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/entity/EnumOutter.java`
#### Snippet
```java
                    return ENTRY_TYPE_CONFIGURATION;
                default:
                    return null;
            }
        }
```

### ReturnNull
Return of `null`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/entity/EnumOutter.java`
#### Snippet
```java
                    return ERROR_TYPE_META;
                default:
                    return null;
            }
        }
```

### ReturnNull
Return of `null`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/entity/LocalFileMetaOutter.java`
#### Snippet
```java
                    return FILE_SOURCE_REFERENCE;
                default:
                    return null;
            }
        }
```

### ReturnNull
Return of `null`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/entity/codec/AutoDetectDecoder.java`
#### Snippet
```java
    public LogEntry decode(final byte[] bs) {
        if (bs == null || bs.length < 1) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/entity/Ballot.java`
#### Snippet
```java
                }
            }
            return null;
        }

```

### ReturnNull
Return of `null`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/entity/PeerId.java`
#### Snippet
```java
            return peer;
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/core/Replicator.java`
#### Snippet
```java
            LOG.error("Fail to init sending channel to {}.", opts.getPeerId());
            // Return and it will be retried later.
            return null;
        }

```

### ReturnNull
Return of `null`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/core/Replicator.java`
#### Snippet
```java
    Future<Message> getRpcInFly() {
        if (this.rpcInFly == null) {
            return null;
        }
        return this.rpcInFly.rpcFuture;
```

### ReturnNull
Return of `null`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/entity/codec/v1/V1Decoder.java`
#### Snippet
```java
    public LogEntry decode(final byte[] content) {
        if (content == null || content.length == 0) {
            return null;
        }
        if (content[0] != LogEntryV1CodecFactory.MAGIC) {
```

### ReturnNull
Return of `null`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/entity/codec/v1/V1Decoder.java`
#### Snippet
```java
        if (content[0] != LogEntryV1CodecFactory.MAGIC) {
            // Corrupted log
            return null;
        }
        LogEntry log = new LogEntry();
```

### ReturnNull
Return of `null`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/storage/io/MessageFile.java`
#### Snippet
```java

        if (!file.exists()) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/storage/impl/VolatileLogStorage.java`
#### Snippet
```java
        try {
            if (!hasAnyEntries() || index < firstLogIndex) {
                return null;
            }

```

### ReturnNull
Return of `null`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/storage/impl/RocksDbSpillout.java`
#### Snippet
```java
        try {
            if (index < this.firstLogIndex || index > this.lastLogIndex) {
                return null;
            }

```

### ReturnNull
Return of `null`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/storage/impl/RocksDbSpillout.java`
#### Snippet
```java
                    LOG.error("Bad log entry format for index={}, the log data is: {}.", index, BytesUtil.toHex(bs));
                    // invalid data remove? TODO https://issues.apache.org/jira/browse/IGNITE-14832
                    return null;
                }
            }
```

### ReturnNull
Return of `null`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/storage/impl/RocksDbSpillout.java`
#### Snippet
```java
            this.useLock.unlock();
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/storage/impl/LocalLogStorage.java`
#### Snippet
```java
        try {
            if (index < getFirstLogIndex()) {
                return null;
            }

```

### ReturnNull
Return of `null`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/storage/snapshot/local/LocalSnapshotReader.java`
#### Snippet
```java
        if (this.peerId == null || this.peerId.isEmpty()) {
            LOG.error("Peer ID is not specified");
            return null;
        }
        if (this.readerId == 0) {
```

### ReturnNull
Return of `null`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/storage/snapshot/local/LocalSnapshotReader.java`
#### Snippet
```java
            if (!reader.open()) {
                LOG.error("Open snapshot {} failed.", this.path);
                return null;
            }
            this.readerId = FileService.getInstance().addReader(reader);
```

### ReturnNull
Return of `null`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/storage/snapshot/local/LocalSnapshotReader.java`
#### Snippet
```java
            if (this.readerId < 0) {
                LOG.error("Fail to add reader to file_service.");
                return null;
            }
        }
```

### ReturnNull
Return of `null`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/storage/snapshot/local/LocalSnapshotReader.java`
#### Snippet
```java
            return this.metaTable.getMeta();
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/storage/snapshot/remote/RemoteFileCopier.java`
#### Snippet
```java
            if (!file.delete()) {
                LOG.error("Fail to delete destPath: {}.", destPath);
                return null;
            }
        }
```

### ReturnNull
Return of `null`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/storage/snapshot/local/LocalSnapshotStorage.java`
#### Snippet
```java
        final SnapshotCopier copier = startToCopyFrom(uri, opts);
        if (copier == null) {
            return null;
        }
        try {
```

### ReturnNull
Return of `null`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/storage/snapshot/local/LocalSnapshotStorage.java`
#### Snippet
```java
            Thread.currentThread().interrupt();
            LOG.error("Join on snapshot copier was interrupted.");
            return null;
        }
        final SnapshotReader reader = copier.getReader();
```

### ReturnNull
Return of `null`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/storage/snapshot/local/LocalSnapshotStorage.java`
#### Snippet
```java
        if (lsIndex == 0) {
            LOG.warn("No data for snapshot reader {}.", this.path);
            return null;
        }
        final String snapshotPath = getSnapshotPath(lsIndex);
```

### ReturnNull
Return of `null`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/storage/snapshot/local/LocalSnapshotStorage.java`
#### Snippet
```java
            LOG.error("Fail to init reader for path {}.", snapshotPath);
            unref(lsIndex);
            return null;
        }
        return reader;
```

### ReturnNull
Return of `null`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/storage/snapshot/local/LocalSnapshotStorage.java`
#### Snippet
```java
        if (!copier.init(uri, opts)) {
            LOG.error("Fail to init copier to {}.", uri);
            return null;
        }
        copier.start();
```

### ReturnNull
Return of `null`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/storage/impl/RocksDBLogStorage.java`
#### Snippet
```java
        try {
            if (this.hasLoadFirstLogIndex && index < this.firstLogIndex) {
                return null;
            }
            final byte[] keyBytes = getKeyBytes(index);
```

### ReturnNull
Return of `null`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/storage/impl/RocksDBLogStorage.java`
#### Snippet
```java
                    LOG.error("Bad log entry format for index={}, the log data is: {}.", index, BytesUtil.toHex(bs));
                    // invalid data remove? TODO https://issues.apache.org/jira/browse/IGNITE-14832
                    return null;
                }
            }
```

### ReturnNull
Return of `null`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/storage/impl/RocksDBLogStorage.java`
#### Snippet
```java
            this.readLock.unlock();
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `modules/raft/src/main/java/org/apache/ignite/internal/raft/util/OptimizedStream.java`
#### Snippet
```java

        if ((flag & BYTE_BUFFER_NOT_NULL_FLAG) == 0) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/core/NodeImpl.java`
#### Snippet
```java
            // update configuration after _log_manager updated its memory status
            checkAndSetConfiguration(true);
            return null;
        }
        finally {
```

### ReturnNull
Return of `null`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/core/NodeImpl.java`
#### Snippet
```java
            return logEntry;
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/core/NodeImpl.java`
#### Snippet
```java
            }
            this.snapshotExecutor.installSnapshot(request, raftOptions.getRaftMessagesFactory().installSnapshotResponse(), done);
            return null;
        }
        finally {
```

### ReturnNull
Return of `null`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/core/NodeImpl.java`
#### Snippet
```java
                return this.conf.getConf().copy();
            }
            return null;
        }
        finally {
```

### ReturnNull
Return of `null`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/core/NodeImpl.java`
#### Snippet
```java
        this.readLock.lock();
        try {
            return this.leaderId.isEmpty() ? null : this.leaderId;
        }
        finally {
```

### ReturnNull
Return of `null`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/core/NodeImpl.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `modules/raft/src/main/java/org/apache/ignite/internal/raft/storage/impl/RocksDbSharedLogStorage.java`
#### Snippet
```java
        try {
            if (this.hasLoadFirstLogIndex && index < this.firstLogIndex) {
                return null;
            }

```

### ReturnNull
Return of `null`
in `modules/raft/src/main/java/org/apache/ignite/internal/raft/storage/impl/RocksDbSharedLogStorage.java`
#### Snippet
```java
                    LOG.error("Bad log entry format for index={}, the log data is: {}.", index, BytesUtil.toHex(bs));
                    // invalid data remove? TODO https://issues.apache.org/jira/browse/IGNITE-14832
                    return null;
                }
            }
```

### ReturnNull
Return of `null`
in `modules/raft/src/main/java/org/apache/ignite/internal/raft/storage/impl/RocksDbSharedLogStorage.java`
#### Snippet
```java
            this.useLock.unlock();
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/storage/impl/LogManagerImpl.java`
#### Snippet
```java
        try {
            if (index > this.lastLogIndex || index < this.firstLogIndex) {
                return null;
            }
            final LogEntry entry = getEntryFromMemory(index);
```

### ReturnNull
Return of `null`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/storage/impl/LogManagerImpl.java`
#### Snippet
```java
    public ConfigurationEntry checkAndSetConfiguration(final ConfigurationEntry current) {
        if (current == null) {
            return null;
        }
        this.readLock.lock();
```

### ReturnNull
Return of `null`
in `modules/rest/src/main/java/org/apache/ignite/internal/rest/cluster/TopologyController.java`
#### Snippet
```java
    private static NodeMetadataDto toNodeMetadataDto(NodeMetadata metadata) {
        if (metadata == null) {
            return null;
        }
        return new NodeMetadataDto(metadata.restHost(), metadata.httpPort(), metadata.httpsPort());
```

### ReturnNull
Return of `null`
in `modules/table/src/main/java/org/apache/ignite/binary/BinaryObjects.java`
#### Snippet
```java
    @Contract("null -> null")
    public static <T> BinaryObject serialize(T obj) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `modules/table/src/main/java/org/apache/ignite/binary/BinaryObjects.java`
#### Snippet
```java
     */
    public static BinaryObject wrap(byte[] data) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `modules/table/src/main/java/org/apache/ignite/binary/BinaryObjects.java`
#### Snippet
```java
     */
    public static <T> T deserialize(BinaryObject binaryObject, Class<T> targetCls) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `modules/table/src/main/java/org/apache/ignite/internal/table/TableRow.java`
#### Snippet
```java
     */
    public static Tuple valueTuple(Row row) {
        return row.hasValue() ? new ValueRowChunk(row) : null;
    }

```

### ReturnNull
Return of `null`
in `modules/table/src/main/java/org/apache/ignite/internal/table/AbstractRowTupleAdapter.java`
#### Snippet
```java
            @Override
            public Object next() {
                return hasNext() ? value(cur++) : null;
            }
        };
```

### ReturnNull
Return of `null`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/HashIndexLocker.java`
#### Snippet
```java
        BinaryTuple key = indexRowResolver.apply(tableRow);

        return lockManager.acquire(txId, new LockKey(indexId, key.byteBuffer()), modificationMode).thenApply(lock -> null);
    }

```

### ReturnNull
Return of `null`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/gc/MvGc.java`
#### Snippet
```java
            runnable.run();

            return null;
        });
    }
```

### ReturnNull
Return of `null`
in `modules/raft/src/testFixtures/java/org/apache/ignite/raft/jraft/core/TestCluster.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `modules/raft/src/testFixtures/java/org/apache/ignite/raft/jraft/core/TestCluster.java`
#### Snippet
```java
            return (MockStateMachine) leader.getOptions().getFsm();
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `modules/raft/src/testFixtures/java/org/apache/ignite/raft/jraft/core/TestCluster.java`
#### Snippet
```java
                }
            }
            return null;
        }
        finally {
```

### ReturnNull
Return of `null`
in `modules/table/src/main/java/org/apache/ignite/internal/table/RecordViewImpl.java`
#### Snippet
```java
    private R unmarshal(BinaryRow binaryRow) {
        if (binaryRow == null || !binaryRow.hasValue()) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/SortedIndexLocker.java`
#### Snippet
```java
        return acquireLockNextKey(txId, peekCursor).thenApply(indexRow -> {
            if (indexRow == null) {
                return null;
            }

```

### ReturnNull
Return of `null`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/raft/snapshot/PartitionAccessImpl.java`
#### Snippet
```java
            storageUpdateHandler.addToIndexes(row, rowId);

            return null;
        });
    }
```

### ReturnNull
Return of `null`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/raft/snapshot/PartitionAccessImpl.java`
#### Snippet
```java
            storageUpdateHandler.addToIndexes(row, rowId);

            return null;
        });
    }
```

### ReturnNull
Return of `null`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/StorageUpdateHandler.java`
#### Snippet
```java
            }

            return null;
        });
    }
```

### ReturnNull
Return of `null`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/StorageUpdateHandler.java`
#### Snippet
```java
            addToIndexes(row, rowId);

            return null;
        });
    }
```

### ReturnNull
Return of `null`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/StorageUpdateHandler.java`
#### Snippet
```java
            onReplication.run();

            return null;
        });
    }
```

### ReturnNull
Return of `null`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/StorageUpdateHandler.java`
#### Snippet
```java
            }

            return null;
        });
    }
```

### ReturnNull
Return of `null`
in `modules/table/src/main/java/org/apache/ignite/internal/table/KeyValueViewImpl.java`
#### Snippet
```java

        return tbl.getAndDelete(keyRow, (InternalTransaction) tx)
                       .thenApply(r -> r == null ? null : NullableValue.of(unmarshalNullableValue(r)));
    }

```

### ReturnNull
Return of `null`
in `modules/table/src/main/java/org/apache/ignite/internal/table/KeyValueViewImpl.java`
#### Snippet
```java

        return tbl.getAndReplace(row, (InternalTransaction) tx)
                       .thenApply(r -> r == null ? null : NullableValue.of(unmarshalNullableValue(r)));
    }

```

### ReturnNull
Return of `null`
in `modules/table/src/main/java/org/apache/ignite/internal/table/KeyValueViewImpl.java`
#### Snippet
```java

        return tbl.getAndUpsert(row, (InternalTransaction) tx)
                       .thenApply(r -> r == null ? null : NullableValue.of(unmarshalNullableValue(r)));
    }

```

### ReturnNull
Return of `null`
in `modules/table/src/main/java/org/apache/ignite/internal/table/KeyValueViewImpl.java`
#### Snippet
```java
        BinaryRowEx keyRow = marshal(Objects.requireNonNull(key));

        return tbl.get(keyRow, (InternalTransaction) tx).thenApply(r -> r == null ? null : NullableValue.of(unmarshalNullableValue(r)));
    }

```

### ReturnNull
Return of `null`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/raft/PartitionListener.java`
#### Snippet
```java
                storage.lastApplied(config.index(), config.term());

                return null;
            });
        } finally {
```

### ReturnNull
Return of `null`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/raft/PartitionListener.java`
#### Snippet
```java
            storage.lastApplied(maxLastAppliedIndex, maxLastAppliedTerm);

            return null;
        });

```

### ReturnNull
Return of `null`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/raft/PartitionListener.java`
#### Snippet
```java
            storage.lastApplied(commandIndex, commandTerm);

            return null;
        });
    }
```

### ReturnNull
Return of `null`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/raft/PartitionListener.java`
#### Snippet
```java
                storage.lastApplied(commandIndex, commandTerm);

                return null;
            });

```

### ReturnNull
Return of `null`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/raft/PartitionListener.java`
#### Snippet
```java
                storage.lastApplied(commandIndex, commandTerm);

                return null;
            });
        }
```

### ReturnNull
Return of `null`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/raft/PartitionListener.java`
#### Snippet
```java
                storage.lastApplied(commandIndex, commandTerm);

                return null;
            });
        } else {
```

### ReturnNull
Return of `null`
in `modules/table/src/main/java/org/apache/ignite/internal/table/KeyValueBinaryViewImpl.java`
#### Snippet
```java
    private Tuple unmarshalValue(BinaryRow row) {
        if (row == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `modules/table/src/testFixtures/java/org/apache/ignite/internal/table/impl/TestTupleBuilder.java`
#### Snippet
```java
    @Override
    public BitSet bitmaskValue(int columnIndex) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `modules/table/src/testFixtures/java/org/apache/ignite/internal/table/impl/TestTupleBuilder.java`
#### Snippet
```java
    @Override
    public String stringValue(int columnIndex) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `modules/table/src/testFixtures/java/org/apache/ignite/internal/table/impl/TestTupleBuilder.java`
#### Snippet
```java
    @Override
    public Instant timestampValue(int columnIndex) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `modules/table/src/testFixtures/java/org/apache/ignite/internal/table/impl/TestTupleBuilder.java`
#### Snippet
```java
    @Override
    public LocalDate dateValue(int columnIndex) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `modules/table/src/testFixtures/java/org/apache/ignite/internal/table/impl/TestTupleBuilder.java`
#### Snippet
```java
    @Override
    public UUID uuidValue(int columnIndex) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `modules/table/src/testFixtures/java/org/apache/ignite/internal/table/impl/TestTupleBuilder.java`
#### Snippet
```java
    @Override
    public LocalTime timeValue(int columnIndex) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `modules/table/src/testFixtures/java/org/apache/ignite/internal/table/impl/TestTupleBuilder.java`
#### Snippet
```java
    @Override
    public BinaryObject binaryObjectValue(int columnIndex) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `modules/table/src/testFixtures/java/org/apache/ignite/internal/table/impl/TestTupleBuilder.java`
#### Snippet
```java
    @Override
    public LocalDateTime datetimeValue(int columnIndex) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `modules/vault/src/main/java/org/apache/ignite/internal/vault/VaultManager.java`
#### Snippet
```java
     */
    public CompletableFuture<String> name() {
        return vaultSvc.get(NODE_NAME).thenApply(name -> name == null ? null : new String(name.value(), UTF_8));
    }
}
```

### ReturnNull
Return of `null`
in `modules/table/src/testFixtures/java/org/apache/ignite/internal/table/impl/DummyInternalTableImpl.java`
#### Snippet
```java
    @Override
    public @NotNull String name() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `modules/vault/src/testFixtures/java/org/apache/ignite/internal/vault/inmemory/InMemoryVaultService.java`
#### Snippet
```java
                byte[] value = storage.get(key);

                return value == null ? null : new VaultEntry(key, storage.get(key));
            }
        });
```

### ReturnNull
Return of `null`
in `modules/vault/src/main/java/org/apache/ignite/internal/vault/persistence/PersistentVaultService.java`
#### Snippet
```java
                byte[] value = db.get(key.bytes());

                return value == null ? null : new VaultEntry(key, value);
            } catch (RocksDBException e) {
                throw new IgniteInternalException("Unable to read data from RocksDB", e);
```

### ReturnNull
Return of `null`
in `modules/client/src/main/java/org/apache/ignite/internal/client/IgniteClientConfigurationImpl.java`
#### Snippet
```java
    @Override
    public String[] addresses() {
        return addresses == null ? null : addresses.clone();
    }

```

### ReturnNull
Return of `null`
in `modules/client/src/main/java/org/apache/ignite/internal/client/ClientUtils.java`
#### Snippet
```java
        if (opCode < 0) {
            // No-op.
            return null;
        }

```

### ReturnNull
Return of `null`
in `modules/client/src/main/java/org/apache/ignite/internal/client/ClientUtils.java`
#### Snippet
```java
        switch (opCode) {
            case ClientOp.HEARTBEAT:
                return null;

            case ClientOp.TABLES_GET:
```

### ReturnNull
Return of `null`
in `modules/client/src/main/java/org/apache/ignite/internal/client/ClientUtils.java`
#### Snippet
```java

            case ClientOp.SCHEMAS_GET:
                return null;

            case ClientOp.TUPLE_UPSERT:
```

### ReturnNull
Return of `null`
in `modules/client/src/main/java/org/apache/ignite/internal/client/ClientUtils.java`
#### Snippet
```java

            case ClientOp.JDBC_EXEC:
                return null;

            case ClientOp.JDBC_NEXT:
```

### ReturnNull
Return of `null`
in `modules/client/src/main/java/org/apache/ignite/internal/client/ClientUtils.java`
#### Snippet
```java

            case ClientOp.JDBC_NEXT:
                return null;

            case ClientOp.JDBC_EXEC_BATCH:
```

### ReturnNull
Return of `null`
in `modules/client/src/main/java/org/apache/ignite/internal/client/ClientUtils.java`
#### Snippet
```java

            case ClientOp.JDBC_EXEC_BATCH:
                return null;

            case ClientOp.JDBC_CURSOR_CLOSE:
```

### ReturnNull
Return of `null`
in `modules/client/src/main/java/org/apache/ignite/internal/client/ClientUtils.java`
#### Snippet
```java

            case ClientOp.JDBC_CURSOR_CLOSE:
                return null;

            case ClientOp.JDBC_TABLE_META:
```

### ReturnNull
Return of `null`
in `modules/client/src/main/java/org/apache/ignite/internal/client/ClientUtils.java`
#### Snippet
```java

            case ClientOp.JDBC_TABLE_META:
                return null;

            case ClientOp.JDBC_COLUMN_META:
```

### ReturnNull
Return of `null`
in `modules/client/src/main/java/org/apache/ignite/internal/client/ClientUtils.java`
#### Snippet
```java

            case ClientOp.JDBC_COLUMN_META:
                return null;

            case ClientOp.JDBC_SCHEMAS_META:
```

### ReturnNull
Return of `null`
in `modules/client/src/main/java/org/apache/ignite/internal/client/ClientUtils.java`
#### Snippet
```java

            case ClientOp.JDBC_SCHEMAS_META:
                return null;

            case ClientOp.JDBC_PK_META:
```

### ReturnNull
Return of `null`
in `modules/client/src/main/java/org/apache/ignite/internal/client/ClientUtils.java`
#### Snippet
```java

            case ClientOp.JDBC_PK_META:
                return null;

            case ClientOp.JDBC_QUERY_META:
```

### ReturnNull
Return of `null`
in `modules/client/src/main/java/org/apache/ignite/internal/client/ClientUtils.java`
#### Snippet
```java

            case ClientOp.JDBC_QUERY_META:
                return null;

            case ClientOp.TX_BEGIN:
```

### ReturnNull
Return of `null`
in `modules/client/src/main/java/org/apache/ignite/internal/client/ClientUtils.java`
#### Snippet
```java
            case ClientOp.TX_COMMIT:
            case ClientOp.TX_ROLLBACK:
                return null; // Commit/rollback use owning connection and bypass retry mechanism.

            case ClientOp.JDBC_SQL_EXEC_PS_BATCH:
```

### ReturnNull
Return of `null`
in `modules/client/src/main/java/org/apache/ignite/internal/client/ClientUtils.java`
#### Snippet
```java

            case ClientOp.JDBC_SQL_EXEC_PS_BATCH:
                return null;

            case ClientOp.COMPUTE_EXECUTE:
```

### ReturnNull
Return of `null`
in `modules/client/src/main/java/org/apache/ignite/internal/client/ClientUtils.java`
#### Snippet
```java

            case ClientOp.CLUSTER_GET_NODES:
                return null;

            case ClientOp.SQL_EXEC:
```

### ReturnNull
Return of `null`
in `modules/client/src/main/java/org/apache/ignite/internal/client/ClientUtils.java`
#### Snippet
```java

            case ClientOp.SQL_CURSOR_CLOSE:
                return null;

            case ClientOp.PARTITION_ASSIGNMENT_GET:
```

### ReturnNull
Return of `null`
in `modules/client/src/main/java/org/apache/ignite/internal/client/ClientUtils.java`
#### Snippet
```java

            case ClientOp.PARTITION_ASSIGNMENT_GET:
                return null;

            // Do not return null from default arm intentionally, so we don't forget to update this when new ClientOp values are added.
```

### ReturnNull
Return of `null`
in `modules/client/src/main/java/org/apache/ignite/internal/client/TcpClientChannel.java`
#### Snippet
```java
                                    }

                                    return null;
                                });
                    }
```

### ReturnNull
Return of `null`
in `modules/client/src/main/java/org/apache/ignite/internal/client/TcpClientChannel.java`
#### Snippet
```java
        return pendingReq.thenApplyAsync(payload -> {
            if (payload == null) {
                return null;
            }

```

### ReturnNull
Return of `null`
in `modules/client/src/main/java/org/apache/ignite/internal/client/TcpClientChannel.java`
#### Snippet
```java
            if (payloadReader == null) {
                payload.close();
                return null;
            }

```

### ReturnNull
Return of `null`
in `modules/client/src/main/java/org/apache/ignite/internal/client/table/ClientTables.java`
#### Snippet
```java

        return ch.serviceAsync(ClientOp.TABLE_GET, w -> w.out().packString(name),
                r -> r.in().tryUnpackNil() ? null : new ClientTable(ch, r.in().unpackUuid(), name));
    }
}
```

### ReturnNull
Return of `null`
in `modules/client/src/main/java/org/apache/ignite/internal/client/sql/ClientAsyncResultSet.java`
#### Snippet
```java
    private static Object readValue(BinaryTupleReader in, int idx, ColumnMetadata col) {
        if (in.hasNullValue(idx)) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `modules/client/src/main/java/org/apache/ignite/internal/client/ReliableChannel.java`
#### Snippet
```java
                    log.warn("Failed to establish connection to " + chCfg.getAddress() + ": " + err.getMessage(), err);

                    return null;
                });

```

### ReturnNull
Return of `null`
in `modules/client/src/main/java/org/apache/ignite/internal/client/table/ClientTuple.java`
#### Snippet
```java

    private static <T> T convertValue(T val) {
        return val == NULL_OBJ ? null : val;
    }
}
```

### ReturnNull
Return of `null`
in `modules/client/src/main/java/org/apache/ignite/internal/client/table/ClientTuple.java`
#### Snippet
```java
            @Override
            public Object next() {
                return cur < vals.length ? vals[cur++] : null;
            }
        };
```

### ReturnNull
Return of `null`
in `modules/table/src/testFixtures/java/org/apache/ignite/internal/table/TxAbstractTest.java`
#### Snippet
```java

                        if (balance < 0) {
                            return tx.rollbackAsync().thenApply(ignored -> null);
                        }

```

### ReturnNull
Return of `null`
in `modules/client/src/main/java/org/apache/ignite/internal/marshaller/ClientMarshallerReader.java`
#### Snippet
```java
    public UUID readUuid() {
        var idx = index++;
        return unpacker.hasNullValue(idx) ? null : unpacker.uuidValue(idx);
    }

```

### ReturnNull
Return of `null`
in `modules/client/src/main/java/org/apache/ignite/internal/marshaller/ClientMarshallerReader.java`
#### Snippet
```java
    public LocalDateTime readDateTime() {
        var idx = index++;
        return unpacker.hasNullValue(idx) ? null : unpacker.dateTimeValue(idx);
    }
}
```

### ReturnNull
Return of `null`
in `modules/client/src/main/java/org/apache/ignite/internal/marshaller/ClientMarshallerReader.java`
#### Snippet
```java
    public BigInteger readBigInt() {
        var idx = index++;
        return unpacker.hasNullValue(idx) ? null : unpacker.numberValue(idx);
    }

```

### ReturnNull
Return of `null`
in `modules/client/src/main/java/org/apache/ignite/internal/marshaller/ClientMarshallerReader.java`
#### Snippet
```java
    public byte[] readBytes() {
        var idx = index++;
        return unpacker.hasNullValue(idx) ? null : unpacker.bytesValue(idx);
    }

```

### ReturnNull
Return of `null`
in `modules/client/src/main/java/org/apache/ignite/internal/marshaller/ClientMarshallerReader.java`
#### Snippet
```java
    public BigDecimal readBigDecimal(int scale) {
        var idx = index++;
        return unpacker.hasNullValue(idx) ? null : unpacker.decimalValue(idx, scale);
    }

```

### ReturnNull
Return of `null`
in `modules/client/src/main/java/org/apache/ignite/internal/marshaller/ClientMarshallerReader.java`
#### Snippet
```java
    public BitSet readBitSet() {
        var idx = index++;
        return unpacker.hasNullValue(idx) ? null : unpacker.bitmaskValue(idx);
    }

```

### ReturnNull
Return of `null`
in `modules/client/src/main/java/org/apache/ignite/internal/marshaller/ClientMarshallerReader.java`
#### Snippet
```java
    public Integer readIntBoxed() {
        var idx = index++;
        return unpacker.hasNullValue(idx) ? null : unpacker.intValue(idx);
    }

```

### ReturnNull
Return of `null`
in `modules/client/src/main/java/org/apache/ignite/internal/marshaller/ClientMarshallerReader.java`
#### Snippet
```java
    public String readString() {
        var idx = index++;
        return unpacker.hasNullValue(idx) ? null : unpacker.stringValue(idx);
    }

```

### ReturnNull
Return of `null`
in `modules/client/src/main/java/org/apache/ignite/internal/marshaller/ClientMarshallerReader.java`
#### Snippet
```java
    public Short readShortBoxed() {
        var idx = index++;
        return unpacker.hasNullValue(idx) ? null : unpacker.shortValue(idx);
    }

```

### ReturnNull
Return of `null`
in `modules/client/src/main/java/org/apache/ignite/internal/marshaller/ClientMarshallerReader.java`
#### Snippet
```java
    public Float readFloatBoxed() {
        var idx = index++;
        return unpacker.hasNullValue(idx) ? null : unpacker.floatValue(idx);
    }

```

### ReturnNull
Return of `null`
in `modules/client/src/main/java/org/apache/ignite/internal/marshaller/ClientMarshallerReader.java`
#### Snippet
```java
    public Byte readByteBoxed() {
        var idx = index++;
        return unpacker.hasNullValue(idx) ? null : unpacker.byteValue(idx);
    }

```

### ReturnNull
Return of `null`
in `modules/client/src/main/java/org/apache/ignite/internal/marshaller/ClientMarshallerReader.java`
#### Snippet
```java
    public Double readDoubleBoxed() {
        var idx = index++;
        return unpacker.hasNullValue(idx) ? null : unpacker.doubleValue(idx);
    }

```

### ReturnNull
Return of `null`
in `modules/client/src/main/java/org/apache/ignite/internal/marshaller/ClientMarshallerReader.java`
#### Snippet
```java
    public LocalTime readTime() {
        var idx = index++;
        return unpacker.hasNullValue(idx) ? null : unpacker.timeValue(idx);
    }

```

### ReturnNull
Return of `null`
in `modules/client/src/main/java/org/apache/ignite/internal/marshaller/ClientMarshallerReader.java`
#### Snippet
```java
    public LocalDate readDate() {
        var idx = index++;
        return unpacker.hasNullValue(idx) ? null : unpacker.dateValue(idx);
    }

```

### ReturnNull
Return of `null`
in `modules/client/src/main/java/org/apache/ignite/internal/marshaller/ClientMarshallerReader.java`
#### Snippet
```java
    public Long readLongBoxed() {
        var idx = index++;
        return unpacker.hasNullValue(idx) ? null : unpacker.longValue(idx);
    }

```

### ReturnNull
Return of `null`
in `modules/client/src/main/java/org/apache/ignite/internal/marshaller/ClientMarshallerReader.java`
#### Snippet
```java
    public Instant readTimestamp() {
        var idx = index++;
        return unpacker.hasNullValue(idx) ? null : unpacker.timestampValue(idx);
    }

```

### ReturnNull
Return of `null`
in `modules/runner/src/main/java/org/apache/ignite/internal/app/EnvironmentDefaultValueProvider.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `modules/runner/src/main/java/org/apache/ignite/internal/configuration/storage/LocalConfigurationStorage.java`
#### Snippet
```java
    public CompletableFuture<Serializable> readLatest(String key) {
        return vaultMgr.get(new ByteArray(LOC_PREFIX + key))
                .thenApply(entry -> entry == null ? null : fromBytes(entry.value()))
                .exceptionally(e -> {
                    throw new StorageException("Exception while reading vault entry", e);
```

### ReturnNull
Return of `null`
in `modules/runner/src/main/java/org/apache/ignite/internal/configuration/storage/DistributedConfigurationStorage.java`
#### Snippet
```java
                    byte[] value = entry.value();

                    return value == null ? null : ConfigurationSerializationUtil.fromBytes(value);
                })
                .exceptionally(e -> {
```

### ReturnNull
Return of `null`
in `modules/runner/src/main/java/org/apache/ignite/internal/app/IgniteImpl.java`
#### Snippet
```java
            return new NetworkAddress(host, port);
        } else {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `modules/schema/src/main/java/org/apache/ignite/internal/schema/NativeTypeSpec.java`
#### Snippet
```java
     */
    public static NativeTypeSpec fromObject(Object val) {
        return val != null ? fromClass(val.getClass()) : null;
    }

```

### ReturnNull
Return of `null`
in `modules/schema/src/main/java/org/apache/ignite/internal/schema/NativeTypeSpec.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `modules/schema/src/main/java/org/apache/ignite/internal/schema/mapping/ColumnMapperImpl.java`
#### Snippet
```java
    @Override
    public Column mappedColumn(int idx) {
        return idx < cols.length ? cols[idx] : null;
    }
}
```

### ReturnNull
Return of `null`
in `modules/schema/src/main/java/org/apache/ignite/internal/schema/SchemaManager.java`
#### Snippet
```java
        try {
            return registriesVv.get(causalityToken)
                    .thenApply(regs -> inBusyLock(busyLock, () -> tableId == null ? null : regs.get(tableId)));
        } finally {
            busyLock.leaveBusy();
```

### ReturnNull
Return of `null`
in `modules/schema/src/main/java/org/apache/ignite/internal/schema/NativeTypes.java`
#### Snippet
```java

        if (spec == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `modules/schema/src/main/java/org/apache/ignite/internal/schema/NativeTypes.java`
#### Snippet
```java
                assert false : "Unexpected type: " + spec;

                return null;
        }
    }
```

### ReturnNull
Return of `null`
in `modules/schema/src/main/java/org/apache/ignite/internal/schema/mapping/ColumnMapping.java`
#### Snippet
```java
        @Override
        public Column mappedColumn(int idx) {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `modules/schema/src/main/java/org/apache/ignite/internal/schema/marshaller/MarshallerUtil.java`
#### Snippet
```java
            return new ObjectFactory<>(targetCls);
        } else {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `modules/client/src/main/java/org/apache/ignite/internal/client/table/ClientTable.java`
#### Snippet
```java
        resFut.handle((tuple, err) -> {
            in.close();
            return null;
        });

```

### ReturnNull
Return of `null`
in `modules/schema/src/main/java/org/apache/ignite/internal/schema/marshaller/schema/SchemaSerializerImpl.java`
#### Snippet
```java

        if (!isPresent) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `modules/schema/src/testFixtures/java/org/apache/ignite/internal/schema/testutils/builder/SchemaBuilders.java`
#### Snippet
```java
        // TODO IGNITE-13749: implement schema generation from classes.

        return null;
    }

```

### ReturnNull
Return of `null`
in `modules/metrics/src/main/java/org/apache/ignite/internal/metrics/AbstractMetricSource.java`
#### Snippet
```java
        MetricSet metricSet = bldr.build();

        return HOLDER_FIELD_UPD.compareAndSet(this, null, hldr) ? metricSet : null;
    }

```

### ReturnNull
Return of `null`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/storage/InternalTableImpl.java`
#### Snippet
```java
                    }

                    return null;
                });
            }
```

### ReturnNull
Return of `null`
in `modules/network/src/main/java/org/apache/ignite/internal/network/netty/NettyUtils.java`
#### Snippet
```java
     */
    public static <T> CompletableFuture<T> toCompletableFuture(Future<T> future) {
        return toCompletableFuture(future, fut -> null);
    }

```

### ReturnNull
Return of `null`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/replicator/PartitionReplicaListener.java`
#### Snippet
```java
        return resolveReadResult(readResult, timestamp, () -> {
            if (readResult.newestCommitTimestamp() == null) {
                return null;
            }

```

### ReturnNull
Return of `null`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/replicator/PartitionReplicaListener.java`
#### Snippet
```java
                .logical(tmstmp.getLogical())
                .build()
                : null;
    }

```

### ReturnNull
Return of `null`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/replicator/PartitionReplicaListener.java`
#### Snippet
```java
                                }

                                return null;
                            });
                });
```

### ReturnNull
Return of `null`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/replicator/PartitionReplicaListener.java`
#### Snippet
```java
                                rowIdLock.get2().forEach(lock -> lockManager.release(lock.txId(), lock.lockKey(), lock.lockMode()));

                                return null;
                            });
                });
```

### ReturnNull
Return of `null`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/replicator/PartitionReplicaListener.java`
#### Snippet
```java
        return resolveReadResult(readResult, timestamp, () -> {
            if (readResult.newestCommitTimestamp() == null) {
                return null;
            }

```

### ReturnNull
Return of `null`
in `modules/network/src/main/java/org/apache/ignite/internal/network/direct/stream/DirectByteBufferStreamImplV1.java`
#### Snippet
```java

            if (!lastFinished) {
                return null;
            }

```

### ReturnNull
Return of `null`
in `modules/network/src/main/java/org/apache/ignite/internal/network/direct/stream/DirectByteBufferStreamImplV1.java`
#### Snippet
```java
                    lastFinished = true;

                    return null;

                case 0:
```

### ReturnNull
Return of `null`
in `modules/network/src/main/java/org/apache/ignite/internal/network/direct/stream/DirectByteBufferStreamImplV1.java`
#### Snippet
```java
            tmpArrOff += remaining;

            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `modules/network/src/main/java/org/apache/ignite/internal/network/direct/stream/DirectByteBufferStreamImplV1.java`
#### Snippet
```java
        long[] arr = readLongArray();

        return arr != null ? BitSet.valueOf(arr) : null;
    }

```

### ReturnNull
Return of `null`
in `modules/network/src/main/java/org/apache/ignite/internal/network/direct/stream/DirectByteBufferStreamImplV1.java`
#### Snippet
```java
        byte[] arr = readByteArray();

        return arr != null ? new String(arr, UTF_8) : null;
    }

```

### ReturnNull
Return of `null`
in `modules/network/src/main/java/org/apache/ignite/internal/network/direct/stream/DirectByteBufferStreamImplV1.java`
#### Snippet
```java

                if (!lastFinished || isNull) {
                    return null;
                }

```

### ReturnNull
Return of `null`
in `modules/network/src/main/java/org/apache/ignite/internal/network/direct/stream/DirectByteBufferStreamImplV1.java`
#### Snippet
```java

                if (!lastFinished) {
                    return null;
                }

```

### ReturnNull
Return of `null`
in `modules/network/src/main/java/org/apache/ignite/internal/network/direct/stream/DirectByteBufferStreamImplV1.java`
#### Snippet
```java

                if (!lastFinished) {
                    return null;
                }

```

### ReturnNull
Return of `null`
in `modules/network/src/main/java/org/apache/ignite/internal/network/direct/stream/DirectByteBufferStreamImplV1.java`
#### Snippet
```java

                if (!lastFinished || isNull) {
                    return null;
                }

```

### ReturnNull
Return of `null`
in `modules/network/src/main/java/org/apache/ignite/internal/network/direct/stream/DirectByteBufferStreamImplV1.java`
#### Snippet
```java

                if (!lastFinished) {
                    return null;
                }

```

### ReturnNull
Return of `null`
in `modules/network/src/main/java/org/apache/ignite/internal/network/direct/stream/DirectByteBufferStreamImplV1.java`
#### Snippet
```java

            if (!lastFinished) {
                return null;
            }

```

### ReturnNull
Return of `null`
in `modules/network/src/main/java/org/apache/ignite/internal/network/direct/stream/DirectByteBufferStreamImplV1.java`
#### Snippet
```java
                    lastFinished = true;

                    return null;

                case 0:
```

### ReturnNull
Return of `null`
in `modules/network/src/main/java/org/apache/ignite/internal/network/direct/stream/DirectByteBufferStreamImplV1.java`
#### Snippet
```java
            return arr;
        } else {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `modules/network/src/main/java/org/apache/ignite/internal/network/direct/stream/DirectByteBufferStreamImplV1.java`
#### Snippet
```java

            if (!lastFinished) {
                return null;
            }

```

### ReturnNull
Return of `null`
in `modules/network/src/main/java/org/apache/ignite/internal/network/direct/stream/DirectByteBufferStreamImplV1.java`
#### Snippet
```java

                    if (!lastFinished) {
                        return null;
                    }

```

### ReturnNull
Return of `null`
in `modules/network/src/main/java/org/apache/ignite/internal/network/direct/stream/DirectByteBufferStreamImplV1.java`
#### Snippet
```java

                if (!lastFinished) {
                    return null;
                }

```

### ReturnNull
Return of `null`
in `modules/network/src/main/java/org/apache/ignite/internal/network/direct/stream/DirectByteBufferStreamImplV1.java`
#### Snippet
```java

            if (!lastFinished) {
                return null;
            }

```

### ReturnNull
Return of `null`
in `modules/network/src/main/java/org/apache/ignite/internal/network/direct/stream/DirectByteBufferStreamImplV1.java`
#### Snippet
```java

                if (!lastFinished) {
                    return null;
                }

```

### ReturnNull
Return of `null`
in `modules/network/src/main/java/org/apache/ignite/internal/network/direct/stream/DirectByteBufferStreamImplV1.java`
#### Snippet
```java

                if (!lastFinished || isNull) {
                    return null;
                }

```

### ReturnNull
Return of `null`
in `modules/network/src/main/java/org/apache/ignite/internal/network/direct/stream/DirectByteBufferStreamImplV1.java`
#### Snippet
```java

                if (!lastFinished) {
                    return null;
                }

```

### ReturnNull
Return of `null`
in `modules/network/src/main/java/org/apache/ignite/internal/network/direct/stream/DirectByteBufferStreamImplV1.java`
#### Snippet
```java

                if (!lastFinished) {
                    return null;
                }

```

### ReturnNull
Return of `null`
in `modules/network/src/main/java/org/apache/ignite/internal/network/direct/stream/DirectByteBufferStreamImplV1.java`
#### Snippet
```java

                if (!lastFinished) {
                    return null;
                }

```

### ReturnNull
Return of `null`
in `modules/bytecode/src/main/java/com/facebook/presto/bytecode/BytecodeVisitor.java`
#### Snippet
```java
            visitAnnotation(fieldDefinition, annotationDefinition);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `modules/bytecode/src/main/java/com/facebook/presto/bytecode/BytecodeVisitor.java`
#### Snippet
```java
        }
        methodDefinition.getBody().accept(null, this);
        return null;
    }

```

### ReturnNull
Return of `null`
in `modules/bytecode/src/main/java/com/facebook/presto/bytecode/BytecodeVisitor.java`
#### Snippet
```java
            visitMethod(classDefinition, methodDefinition);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `modules/bytecode/src/main/java/com/facebook/presto/bytecode/BytecodeVisitor.java`
#### Snippet
```java
            child.accept(node, this);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `modules/bytecode/src/main/java/com/facebook/presto/bytecode/BytecodeVisitor.java`
#### Snippet
```java
    public T visitAnnotation(Object parent, AnnotationDefinition annotationDefinition)
    {
        return null;
    }

```

### ReturnNull
Return of `null`
in `modules/bytecode/src/main/java/com/facebook/presto/bytecode/ClassInfo.java`
#### Snippet
```java
    public ClassInfo getSuperclass() {
        if (superClass == null) {
            return null;
        }
        return loader.loadClassInfo(superClass);
```

### ReturnNull
Return of `null`
in `modules/bytecode/src/main/java/com/facebook/presto/bytecode/expression/CastBytecodeExpression.java`
#### Snippet
```java
                return double.class;
            default:
                return null;
        }
    }
```

### ReturnNull
Return of `null`
in `modules/bytecode/src/main/java/com/facebook/presto/bytecode/instruction/Constant.java`
#### Snippet
```java
        @Override
        public Object getValue() {
            return null;
        }

```

### ReturnNull
Return of `null`
in `modules/network/src/main/java/org/apache/ignite/internal/network/serialization/marshal/BuiltInNonContainerMarshallers.java`
#### Snippet
```java
        addSingle(map, DECIMAL, BuiltInMarshalling::writeBigDecimal, BuiltInMarshalling::readBigDecimal);
        addSingle(map, BIT_SET, BuiltInMarshalling::writeBitSet, BuiltInMarshalling::readBitSet);
        addSingle(map, NULL, (obj, output) -> {}, input -> null);
        addSingle(map, CLASS, (obj, out, ctx) -> BuiltInMarshalling.writeClass(obj, out), BuiltInMarshalling::readClass);

```

### ReturnNull
Return of `null`
in `modules/replicator/src/main/java/org/apache/ignite/internal/replicator/ReplicaService.java`
#### Snippet
```java
                                }

                                return null;
                            });

```

### ReturnNull
Return of `null`
in `modules/replicator/src/main/java/org/apache/ignite/internal/replicator/ReplicaManager.java`
#### Snippet
```java
            return ((TimestampAware) request).timestamp();
        } else {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `modules/replicator/src/main/java/org/apache/ignite/internal/replicator/ReplicaManager.java`
#### Snippet
```java
                                        );

                                        return null;
                                    }
                            );
```

### ReturnNull
Return of `null`
in `modules/replicator/src/main/java/org/apache/ignite/internal/replicator/ReplicaManager.java`
#### Snippet
```java
                    clusterNetSvc.messagingService().respond(senderConsistentId, msg, correlationId);

                    return null;
                });
            } finally {
```

### ReturnNull
Return of `null`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rel/IgniteLimit.java`
#### Snippet
```java
    public Pair<RelTraitSet, List<RelTraitSet>> passThroughTraits(RelTraitSet required) {
        if (required.getConvention() != IgniteConvention.INSTANCE) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rel/IgniteLimit.java`
#### Snippet
```java

        if (TraitUtils.distributionEnabled(this) && TraitUtils.distribution(required) != IgniteDistributions.single()) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rel/IgniteLimit.java`
#### Snippet
```java
            required = required.replace(relCollation);
        } else if (!requiredCollation.satisfies(relCollation)) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rel/IgniteLimit.java`
#### Snippet
```java

        if (childTraits.getConvention() != IgniteConvention.INSTANCE) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rel/IgniteLimit.java`
#### Snippet
```java

        if (TraitUtils.distributionEnabled(this) && TraitUtils.distribution(childTraits) != IgniteDistributions.single()) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rel/IgniteLimit.java`
#### Snippet
```java

        if (!TraitUtils.collation(childTraits).satisfies(TraitUtils.collation(traitSet))) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rel/IgniteRel.java`
#### Snippet
```java
    default Pair<RelTraitSet, List<RelTraitSet>> deriveTraits(
            RelTraitSet childTraits, int childId) {
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rel/IgniteRel.java`
#### Snippet
```java
    default Pair<RelTraitSet, List<RelTraitSet>> passThroughTraits(
            RelTraitSet required) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rel/IgniteSort.java`
#### Snippet
```java

        if (isEnforcer() || childTraits.getConvention() != IgniteConvention.INSTANCE) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rel/IgniteSort.java`
#### Snippet
```java
    public Pair<RelTraitSet, List<RelTraitSet>> passThroughTraits(RelTraitSet required) {
        if (isEnforcer() || required.getConvention() != IgniteConvention.INSTANCE) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rel/IgniteSort.java`
#### Snippet
```java

        if (!requiredCollation.satisfies(relCollation)) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rel/AbstractIndexScan.java`
#### Snippet
```java
            if (searchBounds != null) {
                selectivity = mq.getSelectivity(this, RexUtil.composeConjunction(builder,
                        Commons.transform(searchBounds, b -> b == null ? null : b.condition())));

                cost = Math.log(rows) * IgniteCost.ROW_COMPARISON_COST;
```

### ReturnNull
Return of `null`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rel/agg/IgniteReduceAggregateBase.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rel/agg/IgniteColocatedAggregateBase.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rel/set/IgniteColocatedSetOp.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rel/set/IgniteReduceSetOp.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/ExecutionContext.java`
#### Snippet
```java
    @Override
    public QueryProvider getQueryProvider() {
        return null; // TODO
    }

```

### ReturnNull
Return of `null`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/ExecutionServiceImpl.java`
#### Snippet
```java
                                                }

                                                return null;
                                            })
                            );
```

### ReturnNull
Return of `null`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/ExecutionServiceImpl.java`
#### Snippet
```java
                        });

                        return compoundCancelFut.thenCombine(finalStepFut, (none1, none2) -> null);
                    });

```

### ReturnNull
Return of `null`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/ExecutionServiceImpl.java`
#### Snippet
```java
                this.close(true);

                return null;
            });

```

### ReturnNull
Return of `null`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/IgniteSqlFunctions.java`
#### Snippet
```java
    /** CAST(DECIMAL AS VARCHAR). */
    public static String toString(BigDecimal x) {
        return x == null ? null : x.toPlainString();
    }

```

### ReturnNull
Return of `null`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/IgniteSqlFunctions.java`
#### Snippet
```java
    /** CAST(VARCHAR AS VARBINARY). */
    public static ByteString toByteString(String s) {
        return s == null ? null : new ByteString(s.getBytes(Commons.typeFactory().getDefaultCharset()));
    }

```

### ReturnNull
Return of `null`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/IgniteSqlFunctions.java`
#### Snippet
```java
    public static BigDecimal toBigDecimal(Object o, int precision, int scale) {
        if (o == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/IgniteSqlFunctions.java`
#### Snippet
```java
    public static BigDecimal toBigDecimal(Number num, int precision, int scale) {
        if (num == null) {
            return null;
        }
        // There are some values of "long" that cannot be represented as "double".
```

### ReturnNull
Return of `null`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/IgniteSqlFunctions.java`
#### Snippet
```java
    public static BigDecimal toBigDecimal(String s, int precision, int scale) {
        if (s == null) {
            return null;
        }
        BigDecimal decimal = new BigDecimal(s.trim());
```

### ReturnNull
Return of `null`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/IgniteSqlFunctions.java`
#### Snippet
```java
    /** CAST(VARBINARY AS VARCHAR). */
    public static String toString(ByteString b) {
        return b == null ? null : new String(b.getBytes(), Commons.typeFactory().getDefaultCharset());
    }

```

### ReturnNull
Return of `null`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/ConverterUtils.java`
#### Snippet
```java
                return type.isNullable() && !forceNotNull ? Long.class : long.class;
            default:
                return null; // we don't care; use the default storage type
        }
    }
```

### ReturnNull
Return of `null`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/agg/Accumulators.java`
#### Snippet
```java
        @Override
        public Object end() {
            return cnt > 0 ? sum / cnt : null;
        }

```

### ReturnNull
Return of `null`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/agg/Accumulators.java`
#### Snippet
```java
        @Override
        public Object end() {
            return cnt.compareTo(BigDecimal.ZERO) == 0 ? null : sum.divide(cnt, MathContext.DECIMAL64);
        }

```

### ReturnNull
Return of `null`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/agg/Accumulators.java`
#### Snippet
```java
        /** {@inheritDoc} */
        @Override public Object end() {
            return empty ? null : acc.end();
        }

```

### ReturnNull
Return of `null`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/agg/Accumulators.java`
#### Snippet
```java
        @Override
        public Object end() {
            return empty ? null : val;
        }

```

### ReturnNull
Return of `null`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/agg/Accumulators.java`
#### Snippet
```java
        @Override
        public Object end() {
            return empty ? null : val;
        }

```

### ReturnNull
Return of `null`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/agg/AccumulatorsFactory.java`
#### Snippet
```java

        if (Void.class == toType) {
            return o -> null;
        }

```

### ReturnNull
Return of `null`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/RexToLixTranslator.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/ExpressionFactoryImpl.java`
#### Snippet
```java
    ) {
        if (calls.isEmpty()) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/ExpressionFactoryImpl.java`
#### Snippet
```java
    public Comparator<RowT> comparator(RelCollation collation) {
        if (collation == null || collation.getFieldCollations().isEmpty()) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/ExpressionFactoryImpl.java`
#### Snippet
```java
                }

                return correlates == null ? null : correlates::get;
            } finally {
                correlates = null;
```

### ReturnNull
Return of `null`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rule/FilterSpoolMergeToHashIndexSpoolRule.java`
#### Snippet
```java
            assert b == null || b instanceof ExactBounds : b;

            return b == null ? null : ((ExactBounds) b).bound();
        });

```

### ReturnNull
Return of `null`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rule/HashAggregateConverterRule.java`
#### Snippet
```java
                LogicalAggregate agg) {
            if (complexDistinctAgg(agg.getAggCallList()) || HintUtils.isExpandDistinctAggregate(agg)) {
                return null;
            }

```

### ReturnNull
Return of `null`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rule/HashAggregateConverterRule.java`
#### Snippet
```java
                LogicalAggregate agg) {
            if (HintUtils.isExpandDistinctAggregate(agg)) {
                return null;
            }

```

### ReturnNull
Return of `null`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/RexImpTable.java`
#### Snippet
```java
        Expression implementSafe(final RexToLixTranslator translator,
                final RexCall call, final List<Expression> argValueList) {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/RexImpTable.java`
#### Snippet
```java
        Expression implementSafe(final RexToLixTranslator translator,
                final RexCall call, final List<Expression> argValueList) {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/type/IgniteTypeFactory.java`
#### Snippet
```java
                return List.class;
            default:
                return null;
        }
    }
```

### ReturnNull
Return of `null`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/type/IgniteTypeFactory.java`
#### Snippet
```java
                return List.class;
            default:
                return null;
        }
    }
```

### ReturnNull
Return of `null`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/util/TypeUtils.java`
#### Snippet
```java
            default:
                assert false : "Unexpected type of result: " + type.getSqlTypeName();
                return null;
        }
    }
```

### ReturnNull
Return of `null`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/schema/ColumnDescriptorImpl.java`
#### Snippet
```java
 */
public class ColumnDescriptorImpl implements ColumnDescriptor {
    private static final Supplier<Object> NULL_SUPPLIER = () -> null;

    private final boolean nullable;
```

### ReturnNull
Return of `null`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/schema/TableDescriptorImpl.java`
#### Snippet
```java
    @Override
    public ColumnDescriptor columnDescriptor(String fieldName) {
        return fieldName == null ? null : descriptorsMap.get(fieldName);
    }

```

### ReturnNull
Return of `null`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/schema/TableDescriptorImpl.java`
#### Snippet
```java
    @Override
    public ColumnDescriptor columnDescriptor(int idx) {
        return idx < 0 || idx >= descriptors.length ? null : descriptors[idx];
    }

```

### ReturnNull
Return of `null`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/prepare/IgniteSqlValidator.java`
#### Snippet
```java

        if (table == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/prepare/IgniteSqlValidator.java`
#### Snippet
```java

        if (desc == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/util/RexUtils.java`
#### Snippet
```java
    ) {
        if (condition == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/util/RexUtils.java`
#### Snippet
```java

        if (nullOrEmpty(fieldsToPredicates)) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/util/RexUtils.java`
#### Snippet
```java

            if (nullOrEmpty(collFldPreds)) {
                return null; // Partial condition implies index scan, which is not supported.
            }

```

### ReturnNull
Return of `null`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/util/RexUtils.java`
#### Snippet
```java

            if (columnPred == null) {
                return null; // Non-equality conditions are not expected.
            }

```

### ReturnNull
Return of `null`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/util/RexUtils.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/util/RexUtils.java`
#### Snippet
```java
            @Nullable Mappings.TargetMapping mapping) {
        if (nullOrEmpty(idxCond)) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/util/RexUtils.java`
#### Snippet
```java

        if (leftRef != null) {
            return idxOpSupports(removeCast(rightOp)) ? leftRef : null;
        }

```

### ReturnNull
Return of `null`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/util/RexUtils.java`
#### Snippet
```java

        if (rightRef != null) {
            return idxOpSupports(removeCast(leftOp)) ? rightRef : null;
        }

```

### ReturnNull
Return of `null`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/util/RexUtils.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/util/RexUtils.java`
#### Snippet
```java

        if (nullOrEmpty(fieldsToPredicates)) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/util/RexUtils.java`
#### Snippet
```java
            for (RexCall pred : collFldPreds) {
                if (!pred.isA(HASH_SEARCH_OPS)) {
                    return null;
                }

```

### ReturnNull
Return of `null`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/prepare/AbstractMultiStepPlan.java`
#### Snippet
```java
    public ColocationGroup target(Fragment fragment) {
        if (fragment.rootFragment()) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/prepare/AbstractMultiStepPlan.java`
#### Snippet
```java

        if (nullOrEmpty(remotes)) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/prepare/ddl/ColumnDefinition.java`
#### Snippet
```java
     */
    public Integer scale() {
        return type.getScale() != RelDataType.SCALE_NOT_SPECIFIED ? type.getScale() : null;
    }
}
```

### ReturnNull
Return of `null`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/prepare/ddl/ColumnDefinition.java`
#### Snippet
```java
     */
    public Integer precision() {
        return type.getPrecision() != RelDataType.PRECISION_NOT_SPECIFIED ? type.getPrecision() : null;
    }

```

### ReturnNull
Return of `null`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/schema/IgniteTableImpl.java`
#### Snippet
```java
                    }

                    return null;
                });
    }
```

### ReturnNull
Return of `null`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/schema/IgniteTableImpl.java`
#### Snippet
```java
        @Override
        public List<ImmutableBitSet> getKeys() {
            return null; // TODO
        }

```

### ReturnNull
Return of `null`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/metadata/IgniteMdPercentageOriginalRows.java`
#### Snippet
```java
        Double left = mq.getPercentageOriginalRows(rel.getLeft());
        if (left == null) {
            return null;
        }
        Double right = mq.getPercentageOriginalRows(rel.getRight());
```

### ReturnNull
Return of `null`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/metadata/IgniteMdPercentageOriginalRows.java`
#### Snippet
```java
        Double right = mq.getPercentageOriginalRows(rel.getRight());
        if (right == null) {
            return null;
        }
        return left * right;
```

### ReturnNull
Return of `null`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/metadata/IgniteMdPercentageOriginalRows.java`
#### Snippet
```java
        if (rel.getInputs().size() > 1) {
            // No generic formula available for multiple inputs.
            return null;
        }

```

### ReturnNull
Return of `null`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/metadata/IgniteMdPercentageOriginalRows.java`
#### Snippet
```java
        Double childPercentage = mq.getPercentageOriginalRows(child);
        if (childPercentage == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/metadata/IgniteMdPercentageOriginalRows.java`
#### Snippet
```java
                quotientForPercentage(mq.getRowCount(rel), mq.getRowCount(child));
        if (relPercentage == null) {
            return null;
        }
        double percent = relPercentage * childPercentage;
```

### ReturnNull
Return of `null`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/metadata/IgniteMdPercentageOriginalRows.java`
#### Snippet
```java
        // physical rel
        if ((percent < 0.0) || (percent > 1.0)) {
            return null;
        }
        return relPercentage * childPercentage;
```

### ReturnNull
Return of `null`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/metadata/IgniteMdPercentageOriginalRows.java`
#### Snippet
```java
            Double denominator) {
        if ((numerator == null) || (denominator == null)) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/trait/TraitUtils.java`
#### Snippet
```java
            RelTraitSet requiredTraits) {
        if (requiredTraits.getConvention() != convention || rel.getInputs().isEmpty()) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/externalize/RelJsonReader.java`
#### Snippet
```java

            if (list == null) {
                return null;
            }

```

### ReturnNull
Return of `null`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/externalize/RelJsonReader.java`
#### Snippet
```java

            return RelOptTableImpl.create(null, table.getRowType(Commons.typeFactory()), tableName,
                    table, c -> null);
        }

```

### ReturnNull
Return of `null`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/externalize/RelJson.java`
#### Snippet
```java
    private RexWindowBound toRexWindowBound(RelInput input, Map<String, Object> map) {
        if (map == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/externalize/RelJson.java`
#### Snippet
```java
    List<SearchBounds> toSearchBoundList(RelInput input, List<Map<String, Object>> bounds) {
        if (bounds == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/externalize/RelJson.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/externalize/RelJson.java`
#### Snippet
```java
    private SearchBounds toSearchBound(RelInput input, Map<String, Object> map) {
        if (map == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/externalize/RelJson.java`
#### Snippet
```java
            return AvaticaUtils.instantiatePlugin(SqlOperator.class, cls);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/externalize/RelJson.java`
#### Snippet
```java
    private List<RexFieldCollation> toRexFieldCollationList(RelInput relInput, List<Map<String, Object>> order) {
        if (order == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/externalize/RelJson.java`
#### Snippet
```java
        RexBuilder rexBuilder = cluster.getRexBuilder();
        if (o == null) {
            return null;
        } else if (o instanceof Map) {
            Map map = (Map) o;
```

### ReturnNull
Return of `null`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/inmemory/VolatilePageMemory.java`
#### Snippet
```java
            // No more memory is available.
            if (region == null) {
                return null;
            }

```

### ReturnNull
Return of `null`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/freelist/PagesList.java`
#### Snippet
```java

                if (!stripesChanged) {
                    return null;
                }
            }
```

### ReturnNull
Return of `null`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/compaction/Compactor.java`
#### Snippet
```java

                        if (deltaFileToCompaction == null) {
                            return null;
                        }

```

### ReturnNull
Return of `null`
in `modules/page-memory/src/testFixtures/java/org/apache/ignite/internal/pagememory/persistence/TestPageReadWriteManager.java`
#### Snippet
```java
    @Override
    public PageStore write(int grpId, long pageId, ByteBuffer pageBuf, boolean calculateCrc) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/BaseMvPartitionStorageTest.java`
#### Snippet
```java
            storage.addWriteCommitted(rowId, row, commitTimestamp);

            return null;
        });
    }
```

### ReturnNull
Return of `null`
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/BaseMvPartitionStorageTest.java`
#### Snippet
```java
            storage.commitWrite(rowId, tsExact);

            return null;
        });
    }
```

### ReturnNull
Return of `null`
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/index/AbstractHashIndexStorageTest.java`
#### Snippet
```java
            indexStorage.remove(row);

            return null;
        });
    }
```

### ReturnNull
Return of `null`
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/index/AbstractHashIndexStorageTest.java`
#### Snippet
```java
            indexStorage.put(row);

            return null;
        });
    }
```

### ReturnNull
Return of `null`
in `modules/binary-tuple/src/main/java/org/apache/ignite/internal/binarytuple/BinaryTupleReader.java`
#### Snippet
```java
    public BigDecimal decimalValue(int index, int scale) {
        seek(index);
        return begin == 0 ? null : new BigDecimal(numberValue(begin, end), scale);
    }

```

### ReturnNull
Return of `null`
in `modules/binary-tuple/src/main/java/org/apache/ignite/internal/binarytuple/BinaryTupleReader.java`
#### Snippet
```java
    public String stringValue(int index) {
        seek(index);
        return begin == 0 ? null : stringValue(begin, end);
    }

```

### ReturnNull
Return of `null`
in `modules/binary-tuple/src/main/java/org/apache/ignite/internal/binarytuple/BinaryTupleReader.java`
#### Snippet
```java
    public BigInteger numberValue(int index) {
        seek(index);
        return begin == 0 ? null : numberValue(begin, end);
    }

```

### ReturnNull
Return of `null`
in `modules/binary-tuple/src/main/java/org/apache/ignite/internal/binarytuple/BinaryTupleReader.java`
#### Snippet
```java
    public Byte byteValueBoxed(int index) {
        seek(index);
        return begin == 0 ? null : byteValue(begin, end);
    }

```

### ReturnNull
Return of `null`
in `modules/binary-tuple/src/main/java/org/apache/ignite/internal/binarytuple/BinaryTupleReader.java`
#### Snippet
```java
    public BitSet bitmaskValue(int index) {
        seek(index);
        return begin == 0 ? null : bitmaskValue(begin, end);
    }

```

### ReturnNull
Return of `null`
in `modules/binary-tuple/src/main/java/org/apache/ignite/internal/binarytuple/BinaryTupleReader.java`
#### Snippet
```java
    public Double doubleValueBoxed(int index) {
        seek(index);
        return begin == 0 ? null : doubleValue(begin, end);
    }

```

### ReturnNull
Return of `null`
in `modules/binary-tuple/src/main/java/org/apache/ignite/internal/binarytuple/BinaryTupleReader.java`
#### Snippet
```java
    public Long longValueBoxed(int index) {
        seek(index);
        return begin == 0 ? null : longValue(begin, end);
    }

```

### ReturnNull
Return of `null`
in `modules/binary-tuple/src/main/java/org/apache/ignite/internal/binarytuple/BinaryTupleReader.java`
#### Snippet
```java
    public UUID uuidValue(int index) {
        seek(index);
        return begin == 0 ? null : uuidValue(begin, end);
    }

```

### ReturnNull
Return of `null`
in `modules/binary-tuple/src/main/java/org/apache/ignite/internal/binarytuple/BinaryTupleReader.java`
#### Snippet
```java
    public Duration durationValue(int index) {
        seek(index);
        return begin == 0 ? null : durationValue(begin, end);
    }

```

### ReturnNull
Return of `null`
in `modules/binary-tuple/src/main/java/org/apache/ignite/internal/binarytuple/BinaryTupleReader.java`
#### Snippet
```java
    public LocalTime timeValue(int index) {
        seek(index);
        return begin == 0 ? null : timeValue(begin, end);
    }

```

### ReturnNull
Return of `null`
in `modules/binary-tuple/src/main/java/org/apache/ignite/internal/binarytuple/BinaryTupleReader.java`
#### Snippet
```java
    public Instant timestampValue(int index) {
        seek(index);
        return begin == 0 ? null : timestampValue(begin, end);
    }

```

### ReturnNull
Return of `null`
in `modules/binary-tuple/src/main/java/org/apache/ignite/internal/binarytuple/BinaryTupleReader.java`
#### Snippet
```java
    public Float floatValueBoxed(int index) {
        seek(index);
        return begin == 0 ? null : floatValue(begin, end);
    }

```

### ReturnNull
Return of `null`
in `modules/binary-tuple/src/main/java/org/apache/ignite/internal/binarytuple/BinaryTupleReader.java`
#### Snippet
```java
    public LocalDate dateValue(int index) {
        seek(index);
        return begin == 0 ? null : dateValue(begin, end);
    }

```

### ReturnNull
Return of `null`
in `modules/binary-tuple/src/main/java/org/apache/ignite/internal/binarytuple/BinaryTupleReader.java`
#### Snippet
```java
    public Period periodValue(int index) {
        seek(index);
        return begin == 0 ? null : periodValue(begin, end);
    }

```

### ReturnNull
Return of `null`
in `modules/binary-tuple/src/main/java/org/apache/ignite/internal/binarytuple/BinaryTupleReader.java`
#### Snippet
```java
    public byte[] bytesValue(int index) {
        seek(index);
        return begin == 0 ? null : bytesValue(begin, end);
    }

```

### ReturnNull
Return of `null`
in `modules/binary-tuple/src/main/java/org/apache/ignite/internal/binarytuple/BinaryTupleReader.java`
#### Snippet
```java
    public Integer intValueBoxed(int index) {
        seek(index);
        return begin == 0 ? null : intValue(begin,  end);
    }

```

### ReturnNull
Return of `null`
in `modules/binary-tuple/src/main/java/org/apache/ignite/internal/binarytuple/BinaryTupleReader.java`
#### Snippet
```java
    public LocalDateTime dateTimeValue(int index) {
        seek(index);
        return begin == 0 ? null : dateTimeValue(begin, end);
    }

```

### ReturnNull
Return of `null`
in `modules/binary-tuple/src/main/java/org/apache/ignite/internal/binarytuple/BinaryTupleReader.java`
#### Snippet
```java
    public Short shortValueBoxed(int index) {
        seek(index);
        return begin == 0 ? null : shortValue(begin, end);
    }

```

### ReturnNull
Return of `null`
in `modules/transactions/src/main/java/org/apache/ignite/internal/tx/impl/ReadOnlyTransactionImpl.java`
#### Snippet
```java
    @Override
    public ReplicationGroupId commitPartition() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `modules/transactions/src/main/java/org/apache/ignite/internal/tx/impl/ReadOnlyTransactionImpl.java`
#### Snippet
```java
    public IgniteBiTuple<ClusterNode, Long> enlist(ReplicationGroupId replicationGroupId, IgniteBiTuple<ClusterNode, Long> nodeAndTerm) {
        // TODO: IGNITE-17666 Close cursor tx finish and do it on the first finish invocation only.
        return null;
    }

```

### ReturnNull
Return of `null`
in `modules/transactions/src/main/java/org/apache/ignite/internal/tx/impl/ReadOnlyTransactionImpl.java`
#### Snippet
```java
    @Override
    public IgniteBiTuple<ClusterNode, Long> enlistedNodeAndTerm(ReplicationGroupId replicationGroupId) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/AbstractMvPartitionStorageTest.java`
#### Snippet
```java
            addWrite(rowId, binaryRow2, newTransactionId());

            return null;
        });

```

### ReturnNull
Return of `null`
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/AbstractMvPartitionStorageTest.java`
#### Snippet
```java
            assertThat(storage.persistedIndex(), is(lessThanOrEqualTo(1L)));

            return null;
        });

```

### ReturnNull
Return of `null`
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/AbstractMvPartitionStorageTest.java`
#### Snippet
```java
            addWrite(higherRowId, binaryRow, txId);

            return null;
        });

```

### ReturnNull
Return of `null`
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/AbstractMvPartitionStorageTest.java`
#### Snippet
```java
            storage.committedGroupConfiguration(firstConfig);

            return null;
        });

```

### ReturnNull
Return of `null`
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/AbstractMvPartitionStorageTest.java`
#### Snippet
```java
            storage.committedGroupConfiguration(secondConfig);

            return null;
        });

```

### ReturnNull
Return of `null`
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/AbstractMvPartitionStorageTest.java`
#### Snippet
```java
            addWrite(rowId, null, newTransactionId());

            return null;
        });

```

### ReturnNull
Return of `null`
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/AbstractMvPartitionStorageTest.java`
#### Snippet
```java
            addWrite(rowId, null, newTransactionId());

            return null;
        });

```

### ReturnNull
Return of `null`
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/AbstractMvPartitionStorageTest.java`
#### Snippet
```java

            commitWrite(rowId, first);
            return null;
        });

```

### ReturnNull
Return of `null`
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/AbstractMvPartitionStorageTest.java`
#### Snippet
```java

            commitWrite(rowId, second);
            return null;
        });

```

### ReturnNull
Return of `null`
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/AbstractMvPartitionStorageTest.java`
#### Snippet
```java
            addWrite(rowId, binaryRow3, newTransactionId());

            return null;
        });

```

### ReturnNull
Return of `null`
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/AbstractMvPartitionStorageTest.java`
#### Snippet
```java
            commitWrite(rowId, commitTs);

            return null;
        });

```

### ReturnNull
Return of `null`
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/AbstractMvPartitionStorageTest.java`
#### Snippet
```java
            addWrite(rowId, binaryRow2, txId2);

            return null;
        });

```

### ReturnNull
Return of `null`
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/AbstractMvPartitionStorageTest.java`
#### Snippet
```java
            commitWrite(higherRowId, clock.now());

            return null;
        });

```

### ReturnNull
Return of `null`
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/AbstractMvPartitionStorageTest.java`
#### Snippet
```java
            storage.committedGroupConfiguration(configToSave);

            return null;
        });

```

### ReturnNull
Return of `null`
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/AbstractMvPartitionStorageTest.java`
#### Snippet
```java
            addWrite(higherRowId, binaryRow2, newTransactionId());

            return null;
        });

```

### ReturnNull
Return of `null`
in `modules/transactions/src/testFixtures/java/org/apache/ignite/internal/tx/storage/state/test/TestTxStateTableStorage.java`
#### Snippet
```java
    @Override
    public TableConfiguration configuration() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/AbstractMvTableStorageTest.java`
#### Snippet
```java
            mvPartitionStorage.committedGroupConfiguration(raftGroupConfig);

            return null;
        });

```

### ReturnNull
Return of `null`
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/AbstractMvTableStorageTest.java`
#### Snippet
```java
        int partId = PARTITION_ID;

        assertThrows(StorageClosedException.class, () -> storage.runConsistently(() -> null));

        assertThrows(StorageClosedException.class, storage::flush);
```

### ReturnNull
Return of `null`
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/AbstractMvTableStorageTest.java`
#### Snippet
```java
            storage1.put(new IndexRowImpl(tuple, rowId1));

            return null;
        });

```

### ReturnNull
Return of `null`
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/AbstractMvTableStorageTest.java`
#### Snippet
```java
            storage2.put(new IndexRowImpl(tuple, rowId2));

            return null;
        });

```

### ReturnNull
Return of `null`
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/AbstractMvTableStorageTest.java`
#### Snippet
```java
            assertThrows(StorageRebalanceException.class, () -> storage.pollForVacuum(clock.now()));

            return null;
        });
    }
```

### ReturnNull
Return of `null`
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/AbstractMvTableStorageTest.java`
#### Snippet
```java
                sortedIndexStorage.put(sortedIndexRow);

                return null;
            });
        }
```

### ReturnNull
Return of `null`
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/AbstractMvTableStorageTest.java`
#### Snippet
```java
            mvPartitionStorage.addWriteCommitted(rowId, binaryRow, clock.now());

            return null;
        });

```

### ReturnNull
Return of `null`
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/AbstractMvTableStorageTest.java`
#### Snippet
```java
            sortedIndexStorage.put(sortedIndexRow);

            return null;
        });

```

### ReturnNull
Return of `null`
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/AbstractMvTableStorageTest.java`
#### Snippet
```java
            finalMvPartitionStorage.lastApplied(REBALANCE_IN_PROGRESS, REBALANCE_IN_PROGRESS);

            return null;
        });

```

### ReturnNull
Return of `null`
in `modules/transactions/src/main/java/org/apache/ignite/internal/tx/storage/state/rocksdb/TxStateRocksDbStorage.java`
#### Snippet
```java
            }

            return null;
        });
    }
```

### ReturnNull
Return of `null`
in `modules/transactions/src/main/java/org/apache/ignite/internal/tx/storage/state/rocksdb/TxStateRocksDbStorage.java`
#### Snippet
```java
                byte[] txMetaBytes = db.get(txIdToKey(txId));

                return txMetaBytes == null ? null : fromBytes(txMetaBytes);
            } catch (RocksDBException e) {
                throw new IgniteInternalException(
```

### ReturnNull
Return of `null`
in `modules/transactions/src/main/java/org/apache/ignite/internal/tx/storage/state/rocksdb/TxStateRocksDbStorage.java`
#### Snippet
```java
                this.lastAppliedTerm = lastAppliedTerm;

                return null;
            } catch (RocksDBException e) {
                throw new IgniteInternalException(
```

### ReturnNull
Return of `null`
in `modules/transactions/src/main/java/org/apache/ignite/internal/tx/storage/state/rocksdb/TxStateRocksDbStorage.java`
#### Snippet
```java
                db.delete(txIdToKey(txId));

                return null;
            } catch (RocksDBException e) {
                throw new IgniteInternalException(
```

### ReturnNull
Return of `null`
in `modules/transactions/src/main/java/org/apache/ignite/internal/tx/storage/state/rocksdb/TxStateRocksDbStorage.java`
#### Snippet
```java
                db.put(txIdToKey(txId), toBytes(txMeta));

                return null;
            } catch (RocksDBException e) {
                throw new IgniteInternalException(
```

### ReturnNull
Return of `null`
in `modules/client-common/src/main/java/org/apache/ignite/internal/jdbc/proto/event/ClientMessageUtils.java`
#### Snippet
```java
        }

        return null;
    }
}
```

### ReturnNull
Return of `null`
in `modules/client-common/src/main/java/org/apache/ignite/internal/client/proto/ClientMessageDecoder.java`
#### Snippet
```java
    protected Object decode(ChannelHandlerContext ctx, ByteBuf in) throws Exception {
        if (!readMagic(in)) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `modules/client-common/src/main/java/org/apache/ignite/internal/jdbc/proto/event/JdbcColumnMeta.java`
#### Snippet
```java
     */
    public String defaultValue() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `modules/client-common/src/main/java/org/apache/ignite/internal/client/proto/ClientBinaryTupleUtils.java`
#### Snippet
```java
    public static Object readObject(BinaryTupleReader reader, int index) {
        if (reader.hasNullValue(index)) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `modules/client-common/src/main/java/org/apache/ignite/internal/client/proto/ClientMessageUnpacker.java`
#### Snippet
```java

        if (tryUnpackNil()) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `modules/client-common/src/main/java/org/apache/ignite/internal/client/proto/ClientMessageUnpacker.java`
#### Snippet
```java

        if (tryUnpackNil()) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/DynamicConfiguration.java`
#### Snippet
```java
    @Nullable
    public Class<?>[] internalConfigTypes() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/ConfigurationChanger.java`
#### Snippet
```java
            RootKey<?, ?> rootKey = rootKeys.get(key);

            return rootKey == null ? null : new RootInnerNode(rootKey, createRootNode(rootKey));
        };
    }
```

### ReturnNull
Return of `null`
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/tree/InnerNode.java`
#### Snippet
```java
     */
    public String getInjectedNameFieldValue() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/tree/InnerNode.java`
#### Snippet
```java
    @Nullable
    public Class<?>[] internalSchemaTypes() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/util/KeysTrackingConfigurationVisitor.java`
#### Snippet
```java
        node.traverseChildren(this, true);

        return null;
    }

```

### ReturnNull
Return of `null`
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/util/KeysTrackingConfigurationVisitor.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/util/KeysTrackingConfigurationVisitor.java`
#### Snippet
```java
     */
    protected T doVisitLeafNode(String key, Serializable val) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/util/ConfigurationFlattener.java`
#### Snippet
```java
                    // Deletion of nonexistent element.
                    if (oldNamedElement == null && newNamedElement == null) {
                        return null;
                    }

```

### ReturnNull
Return of `null`
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/util/ConfigurationFlattener.java`
#### Snippet
```java
                    }

                    return null;
                });
            }
```

### ReturnNull
Return of `null`
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/util/ConfigurationUtil.java`
#### Snippet
```java
                            assert false : e;

                            return null;
                        }
                    }
```

### ReturnNull
Return of `null`
in `modules/client-handler/src/main/java/org/apache/ignite/client/handler/ClientInboundMessageHandler.java`
#### Snippet
```java
        switch (opCode) {
            case ClientOp.HEARTBEAT:
                return null;

            case ClientOp.TABLES_GET:
```

### ReturnNull
Return of `null`
in `modules/client-handler/src/main/java/org/apache/ignite/client/handler/JdbcQueryEventHandlerImpl.java`
#### Snippet
```java
                            //we can do nothing about this.
                        }
                        return null;
                    })
                    .filter(Objects::nonNull)
```

### ReturnNull
Return of `null`
in `modules/client-handler/src/main/java/org/apache/ignite/client/handler/requests/table/ClientSchemasGetRequest.java`
#### Snippet
```java
        }

        return null;
    }
}
```

### ReturnNull
Return of `null`
in `modules/client-handler/src/main/java/org/apache/ignite/client/handler/requests/table/ClientTablePartitionAssignmentGetRequest.java`
#### Snippet
```java
        if (assignment == null) {
            out.packArrayHeader(0);
            return null;
        }

```

### ReturnNull
Return of `null`
in `modules/client-handler/src/main/java/org/apache/ignite/client/handler/requests/table/ClientTablePartitionAssignmentGetRequest.java`
#### Snippet
```java
        }

        return null;
    }
}
```

### ReturnNull
Return of `null`
in `modules/client-handler/src/main/java/org/apache/ignite/client/handler/requests/cluster/ClientClusterGetNodesRequest.java`
#### Snippet
```java

        // Null future indicates synchronous completion.
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `modules/storage-rocksdb/src/main/java/org/apache/ignite/internal/storage/rocksdb/index/RocksDbHashIndexStorage.java`
#### Snippet
```java
                writeBatch.delete(indexCf.handle(), rocksKey(row));

                return null;
            } catch (RocksDBException e) {
                throw new StorageException("Unable to remove data from hash index. Index ID: " + descriptor.id(), e);
```

### ReturnNull
Return of `null`
in `modules/storage-rocksdb/src/main/java/org/apache/ignite/internal/storage/rocksdb/index/RocksDbHashIndexStorage.java`
#### Snippet
```java
                writeBatch.put(indexCf.handle(), rocksKey(row), BYTE_EMPTY_ARRAY);

                return null;
            } catch (RocksDBException e) {
                throw new StorageException("Unable to insert data into hash index. Index ID: " + descriptor.id(), e);
```

### ReturnNull
Return of `null`
in `modules/storage-rocksdb/src/main/java/org/apache/ignite/internal/storage/rocksdb/index/RocksDbHashIndexStorage.java`
#### Snippet
```java
                indexCf.db().deleteRange(indexCf.handle(), writeOptions, constantPrefix, rangeEnd);

                return null;
            } catch (RocksDBException e) {
                throw new StorageException("Unable to remove data from hash index. Index ID: " + descriptor.id(), e);
```

### ReturnNull
Return of `null`
in `modules/storage-rocksdb/src/main/java/org/apache/ignite/internal/storage/rocksdb/RocksDbMvPartitionStorage.java`
#### Snippet
```java
            }

            return null;
        });
    }
```

### ReturnNull
Return of `null`
in `modules/storage-rocksdb/src/main/java/org/apache/ignite/internal/storage/rocksdb/RocksDbMvPartitionStorage.java`
#### Snippet
```java
                    RocksUtils.checkIterator(it);

                    return null;
                }

```

### ReturnNull
Return of `null`
in `modules/storage-rocksdb/src/main/java/org/apache/ignite/internal/storage/rocksdb/RocksDbMvPartitionStorage.java`
#### Snippet
```java
                if (previousValue == null) {
                    //the chain doesn't contain an uncommitted write intent
                    return null;
                }

```

### ReturnNull
Return of `null`
in `modules/storage-rocksdb/src/main/java/org/apache/ignite/internal/storage/rocksdb/RocksDbMvPartitionStorage.java`
#### Snippet
```java
                savePendingLastApplied(requireWriteBatch(), lastAppliedIndex, lastAppliedTerm);

                return null;
            } catch (RocksDBException e) {
                throw new StorageException(e);
```

### ReturnNull
Return of `null`
in `modules/storage-rocksdb/src/main/java/org/apache/ignite/internal/storage/rocksdb/RocksDbMvPartitionStorage.java`
#### Snippet
```java

                if (readResult.isEmpty()) {
                    return null;
                }

```

### ReturnNull
Return of `null`
in `modules/storage-rocksdb/src/main/java/org/apache/ignite/internal/storage/rocksdb/RocksDbMvPartitionStorage.java`
#### Snippet
```java
                if (valueBytes == null) {
                    // The chain doesn't contain an uncommitted write intent.
                    return null;
                }

```

### ReturnNull
Return of `null`
in `modules/storage-rocksdb/src/main/java/org/apache/ignite/internal/storage/rocksdb/RocksDbMvPartitionStorage.java`
#### Snippet
```java
                }

                return null;
            } catch (RocksDBException e) {
                throw new StorageException("Failed to commit row into storage", e);
```

### ReturnNull
Return of `null`
in `modules/storage-rocksdb/src/main/java/org/apache/ignite/internal/storage/rocksdb/RocksDbMvPartitionStorage.java`
#### Snippet
```java
                saveGroupConfiguration(requireWriteBatch(), config);

                return null;
            } catch (RocksDBException e) {
                throw new StorageException(e);
```

### ReturnNull
Return of `null`
in `modules/placement-driver/src/main/java/org/apache/ignite/internal/placementdriver/LeaseUpdater.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `modules/placement-driver/src/main/java/org/apache/ignite/internal/placementdriver/TopologyTracker.java`
#### Snippet
```java

        if (logicalTopologySnap0 == null || CollectionUtils.nullOrEmpty(logicalTopologySnap0.nodes())) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `modules/placement-driver/src/main/java/org/apache/ignite/internal/placementdriver/TopologyTracker.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `modules/storage-rocksdb/src/main/java/org/apache/ignite/internal/storage/rocksdb/index/RocksDbSortedIndexStorage.java`
#### Snippet
```java
                writeBatch.put(indexCf.handle(), rocksKey(row), BYTE_EMPTY_ARRAY);

                return null;
            } catch (RocksDBException e) {
                throw new StorageException("Unable to insert data into sorted index. Index ID: " + descriptor.id(), e);
```

### ReturnNull
Return of `null`
in `modules/storage-rocksdb/src/main/java/org/apache/ignite/internal/storage/rocksdb/index/RocksDbSortedIndexStorage.java`
#### Snippet
```java
                writeBatch.delete(indexCf.handle(), rocksKey(row));

                return null;
            } catch (RocksDBException e) {
                throw new StorageException("Unable to remove data from sorted index. Index ID: " + descriptor.id(), e);
```

### ReturnNull
Return of `null`
in `modules/storage-rocksdb/src/main/java/org/apache/ignite/internal/storage/rocksdb/index/RocksDbSortedIndexStorage.java`
#### Snippet
```java
                        }

                        return null;
                    }

```

### ReturnNull
Return of `null`
in `modules/storage-rocksdb/src/main/java/org/apache/ignite/internal/storage/rocksdb/index/RocksDbSortedIndexStorage.java`
#### Snippet
```java
                        RocksUtils.checkIterator(it);

                        return null;
                    } else {
                        return mapper.apply(ByteBuffer.wrap(it.key()).order(ORDER));
```

### ReturnNull
Return of `null`
in `modules/marshaller-common/src/main/java/org/apache/ignite/internal/marshaller/MarshallerUtil.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `modules/marshaller-common/src/main/java/org/apache/ignite/internal/marshaller/MarshallerColumn.java`
#### Snippet
```java
public class MarshallerColumn {
    /** Default "default value supplier". */
    private static final Supplier<Object> NULL_SUPPLIER = () -> null;

    /**
```

### ReturnNull
Return of `null`
in `modules/cluster-management/src/main/java/org/apache/ignite/internal/cluster/management/LocalStateStorage.java`
#### Snippet
```java
    CompletableFuture<LocalState> getLocalState() {
        return vault.get(CMG_STATE_VAULT_KEY)
                .thenApply(entry -> entry == null ? null : ByteUtils.fromBytes(entry.value()));
    }

```

### ReturnNull
Return of `null`
in `modules/storage-page-memory/src/main/java/org/apache/ignite/internal/storage/pagememory/AbstractPageMemoryTableStorage.java`
#### Snippet
```java
            mvPartitionStorages = new MvPartitionStorages(tableCfg.value());

            return null;
        });
    }
```

### ReturnNull
Return of `null`
in `modules/storage-page-memory/src/main/java/org/apache/ignite/internal/storage/pagememory/AbstractPageMemoryTableStorage.java`
#### Snippet
```java
                                mvPartitionStorage.lastAppliedOnRebalance(0, 0);

                                return null;
                            });

```

### ReturnNull
Return of `null`
in `modules/storage-page-memory/src/main/java/org/apache/ignite/internal/storage/pagememory/AbstractPageMemoryTableStorage.java`
#### Snippet
```java
                                mvPartitionStorage.lastAppliedOnRebalance(REBALANCE_IN_PROGRESS, REBALANCE_IN_PROGRESS);

                                return null;
                            })
                    );
```

### ReturnNull
Return of `null`
in `modules/storage-page-memory/src/main/java/org/apache/ignite/internal/storage/pagememory/AbstractPageMemoryTableStorage.java`
#### Snippet
```java
                mvPartitionStorage.committedGroupConfigurationOnRebalance(groupConfig);

                return null;
            });

```

### ReturnNull
Return of `null`
in `modules/storage-page-memory/src/main/java/org/apache/ignite/internal/storage/pagememory/PersistentPageMemoryTableStorage.java`
#### Snippet
```java
                );

                return null;
            });
        });
```

### ReturnNull
Return of `null`
in `modules/storage-page-memory/src/main/java/org/apache/ignite/internal/storage/pagememory/mv/AbstractPartitionTimestampCursor.java`
#### Snippet
```java

            if (result.isEmpty()) {
                return null;
            }

```

### ReturnNull
Return of `null`
in `modules/storage-page-memory/src/main/java/org/apache/ignite/internal/storage/pagememory/mv/VolatilePageMemoryMvPartitionStorage.java`
#### Snippet
```java
            this.lastAppliedTerm = lastAppliedTerm;

            return null;
        });
    }
```

### ReturnNull
Return of `null`
in `modules/storage-page-memory/src/main/java/org/apache/ignite/internal/storage/pagememory/mv/VolatilePageMemoryMvPartitionStorage.java`
#### Snippet
```java
            groupConfig = Arrays.copyOf(config, config.length);

            return null;
        });
    }
```

### ReturnNull
Return of `null`
in `modules/storage-page-memory/src/main/java/org/apache/ignite/internal/storage/pagememory/mv/VolatilePageMemoryMvPartitionStorage.java`
#### Snippet
```java

            byte[] currentConfig = groupConfig;
            return currentConfig == null ? null : Arrays.copyOf(currentConfig, currentConfig.length);
        });
    }
```

### ReturnNull
Return of `null`
in `modules/storage-page-memory/src/main/java/org/apache/ignite/internal/storage/pagememory/mv/PersistentPageMemoryMvPartitionStorage.java`
#### Snippet
```java
            committedGroupConfigurationBusy(config);

            return null;
        });
    }
```

### ReturnNull
Return of `null`
in `modules/storage-page-memory/src/main/java/org/apache/ignite/internal/storage/pagememory/mv/PersistentPageMemoryMvPartitionStorage.java`
#### Snippet
```java
            lastAppliedBusy(lastAppliedIndex, lastAppliedTerm);

            return null;
        });
    }
```

### ReturnNull
Return of `null`
in `modules/storage-page-memory/src/main/java/org/apache/ignite/internal/storage/pagememory/mv/PersistentPageMemoryMvPartitionStorage.java`
#### Snippet
```java

                    if (configFirstPageId == BlobStorage.NO_PAGE_ID) {
                        return null;
                    }

```

### ReturnNull
Return of `null`
in `modules/storage-page-memory/src/main/java/org/apache/ignite/internal/storage/pagememory/index/hash/PageMemoryHashIndexStorage.java`
#### Snippet
```java
                hashIndexTree.invoke(hashIndexRow, null, insert);

                return null;
            } catch (IgniteInternalCheckedException e) {
                throw new StorageException("Failed to put value into index", e);
```

### ReturnNull
Return of `null`
in `modules/storage-page-memory/src/main/java/org/apache/ignite/internal/storage/pagememory/index/hash/PageMemoryHashIndexStorage.java`
#### Snippet
```java
                remove.afterCompletion();

                return null;
            } catch (IgniteInternalCheckedException e) {
                throw new StorageException("Failed to remove value from index", e);
```

### ReturnNull
Return of `null`
in `modules/storage-page-memory/src/main/java/org/apache/ignite/internal/storage/pagememory/mv/AbstractPageMemoryMvPartitionStorage.java`
#### Snippet
```java
                    addWriteCommitted.afterCompletion();

                    return null;
                } catch (IgniteInternalCheckedException e) {
                    throwStorageExceptionIfItCause(e);
```

### ReturnNull
Return of `null`
in `modules/storage-page-memory/src/main/java/org/apache/ignite/internal/storage/pagememory/mv/AbstractPageMemoryMvPartitionStorage.java`
#### Snippet
```java
                }

                return null;
            } catch (Exception e) {
                throw new StorageException(
```

### ReturnNull
Return of `null`
in `modules/storage-page-memory/src/main/java/org/apache/ignite/internal/storage/pagememory/mv/AbstractPageMemoryMvPartitionStorage.java`
#### Snippet
```java

            try (Cursor<VersionChain> cursor = versionChainTree.find(new VersionChainKey(lowerBound), null)) {
                return cursor.hasNext() ? cursor.next().rowId() : null;
            } catch (Exception e) {
                throw new StorageException("Error occurred while trying to read a row id", e);
```

### ReturnNull
Return of `null`
in `modules/storage-page-memory/src/main/java/org/apache/ignite/internal/storage/pagememory/mv/AbstractPageMemoryMvPartitionStorage.java`
#### Snippet
```java
                    commitWrite.afterCompletion();

                    return null;
                } catch (IgniteInternalCheckedException e) {
                    throwStorageExceptionIfItCause(e);
```

### ReturnNull
Return of `null`
in `modules/storage-page-memory/src/main/java/org/apache/ignite/internal/storage/pagememory/mv/AbstractPageMemoryMvPartitionStorage.java`
#### Snippet
```java
                // Garbage collection queue is empty.
                if (head == null) {
                    return null;
                }

```

### ReturnNull
Return of `null`
in `modules/storage-page-memory/src/main/java/org/apache/ignite/internal/storage/pagememory/mv/AbstractPageMemoryMvPartitionStorage.java`
#### Snippet
```java
                // There are no versions in the garbage collection queue before watermark.
                if (rowTimestamp.compareTo(lowWatermark) > 0) {
                    return null;
                }

```

### ReturnNull
Return of `null`
in `modules/storage-page-memory/src/main/java/org/apache/ignite/internal/storage/pagememory/index/sorted/PageMemorySortedIndexStorage.java`
#### Snippet
```java
                    }

                    return null;
                }

```

### ReturnNull
Return of `null`
in `modules/storage-page-memory/src/main/java/org/apache/ignite/internal/storage/pagememory/index/sorted/PageMemorySortedIndexStorage.java`
#### Snippet
```java

                    if (nextTreeRow == null || (upper != null && compareRows(nextTreeRow, upper) >= 0)) {
                        return null;
                    }

```

### ReturnNull
Return of `null`
in `modules/storage-page-memory/src/main/java/org/apache/ignite/internal/storage/pagememory/index/sorted/PageMemorySortedIndexStorage.java`
#### Snippet
```java
                sortedIndexTree.invoke(sortedIndexRow, null, insert);

                return null;
            } catch (IgniteInternalCheckedException e) {
                throw new StorageException("Failed to put value into index", e);
```

### ReturnNull
Return of `null`
in `modules/storage-page-memory/src/main/java/org/apache/ignite/internal/storage/pagememory/index/sorted/PageMemorySortedIndexStorage.java`
#### Snippet
```java
                remove.afterCompletion();

                return null;
            } catch (IgniteInternalCheckedException e) {
                throw new StorageException("Failed to remove value from index", e);
```

### ReturnNull
Return of `null`
in `modules/distribution-zones/src/main/java/org/apache/ignite/internal/distributionzones/DistributionZoneManager.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/index/AbstractSortedIndexStorageTest.java`
#### Snippet
```java
            indexStorage.put(row);

            return null;
        });
    }
```

### ReturnNull
Return of `null`
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/index/AbstractSortedIndexStorageTest.java`
#### Snippet
```java
            indexStorage.remove(row);

            return null;
        });
    }
```

### ReturnNull
Return of `null`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/tree/BplusTree.java`
#### Snippet
```java
            }

            return null; // Succeeded.
        }

```

### ReturnNull
Return of `null`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/tree/BplusTree.java`
#### Snippet
```java
            insertSimple(pageAddr, io, idx);

            return null;
        }

```

### ReturnNull
Return of `null`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/tree/BplusTree.java`
#### Snippet
```java

                            if (nextPageId == 0) {
                                return null;
                            }

```

### ReturnNull
Return of `null`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/tree/BplusTree.java`
#### Snippet
```java
                        assert res == TRUE : res;

                        return null; // We've just moved link up to root, nothing to return here.
                    }

```

## RuleId[id=AssignmentToLambdaParameter]
### AssignmentToLambdaParameter
Assignment to lambda parameter `err`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/rpc/impl/IgniteRpcClient.java`
#### Snippet
```java

                if (err instanceof ExecutionException)
                    err = new RemotingException(err);
                else if (err instanceof TimeoutException) // Translate timeout exception.
                    err = new InvokeTimeoutException();
```

### AssignmentToLambdaParameter
Assignment to lambda parameter `err`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/rpc/impl/IgniteRpcClient.java`
#### Snippet
```java
                    err = new RemotingException(err);
                else if (err instanceof TimeoutException) // Translate timeout exception.
                    err = new InvokeTimeoutException();

                Throwable finalErr = err;
```

### AssignmentToLambdaParameter
Assignment to lambda parameter `actions`
in `modules/raft/src/main/java/org/apache/ignite/internal/raft/server/impl/RaftServiceEventListener.java`
#### Snippet
```java
        subscriptions.compute(groupId, (id, actions) -> {
            if (actions == null) {
                actions = new HashSet<>();
            }

```

### AssignmentToLambdaParameter
Assignment to lambda parameter `nodeActions`
in `modules/raft/src/main/java/org/apache/ignite/internal/raft/server/impl/RaftServiceEventListener.java`
#### Snippet
```java

                if (nodeActions == null) {
                    nodeActions = new HashSet<>();
                }

```

### AssignmentToLambdaParameter
Assignment to lambda parameter `e`
in `modules/rest/src/main/java/org/apache/ignite/internal/rest/configuration/hocon/HoconPresentation.java`
#### Snippet
```java
                .exceptionally(e -> {
                    if (e instanceof CompletionException) {
                        e = e.getCause();
                    }

```

### AssignmentToLambdaParameter
Assignment to lambda parameter `txOps`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/replicator/PartitionReplicaListener.java`
#### Snippet
```java
        txCleanupReadyFutures.compute(txId, (id, txOps) -> {
            if (txOps == null) {
                txOps = new TxCleanupReadyFutureList();
            }

```

### AssignmentToLambdaParameter
Assignment to lambda parameter `txOps`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/replicator/PartitionReplicaListener.java`
#### Snippet
```java
        txCleanupReadyFutures.compute(request.txId(), (id, txOps) -> {
            if (txOps == null) {
                txOps = new TxCleanupReadyFutureList();
            }

```

### AssignmentToLambdaParameter
Assignment to lambda parameter `throwable`
in `modules/replicator/src/main/java/org/apache/ignite/internal/replicator/ReplicaService.java`
#### Snippet
```java
            if (throwable != null) {
                if (throwable instanceof CompletionException) {
                    throwable = throwable.getCause();
                }

```

### AssignmentToLambdaParameter
Assignment to lambda parameter `fut`
in `modules/replicator/src/main/java/org/apache/ignite/internal/replicator/ReplicaService.java`
#### Snippet
```java
                                        .build();

                                fut = messagingService.invoke(targetNodeConsistentId, awaitReplicaReq, RPC_TIMEOUT)
                                        .whenComplete((response0, throwable0) -> {
                                            pendingInvokes.remove(targetNodeConsistentId);
```

### AssignmentToLambdaParameter
Assignment to lambda parameter `throwable0`
in `modules/replicator/src/main/java/org/apache/ignite/internal/replicator/ReplicaService.java`
#### Snippet
```java
                                if (throwable0 != null) {
                                    if (throwable0 instanceof CompletionException) {
                                        throwable0 = throwable0.getCause();
                                    }

```

### AssignmentToLambdaParameter
Assignment to lambda parameter `replicaFut`
in `modules/replicator/src/main/java/org/apache/ignite/internal/replicator/ReplicaManager.java`
#### Snippet
```java
                    replicas.compute(request.groupId(), (replicationGroupId, replicaFut) -> {
                        if (replicaFut == null) {
                            replicaFut = new CompletableFuture<>();
                        }

```

### AssignmentToLambdaParameter
Assignment to lambda parameter `inRow`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/ExecutionServiceImpl.java`
#### Snippet
```java

                AsyncRootNode<RowT, List<Object>> rootNode = new AsyncRootNode<>(node, inRow -> {
                    inRow = internalTypeConverter.apply(inRow);

                    int rowSize = ectx.rowHandler().columnCount(inRow);
```

### AssignmentToLambdaParameter
Assignment to lambda parameter `entries`
in `modules/metastorage/src/testFixtures/java/org/apache/ignite/internal/metastorage/server/SimpleInMemoryKeyValueStorage.java`
#### Snippet
```java
                (rev, entries) -> {
                    if (entries == null) {
                        entries = new TreeMap<>(CMP);
                    }

```

### AssignmentToLambdaParameter
Assignment to lambda parameter `reentrantLockLockHolder`
in `modules/storage-api/src/main/java/org/apache/ignite/internal/storage/util/ReentrantLockByRowId.java`
#### Snippet
```java
        LockHolder<ReentrantLock> lockHolder = lockHolderByRowId.compute(rowId, (rowId1, reentrantLockLockHolder) -> {
            if (reentrantLockLockHolder == null) {
                reentrantLockLockHolder = new LockHolder<>(new ReentrantLock());
            }

```

### AssignmentToLambdaParameter
Assignment to lambda parameter `operation`
in `modules/storage-api/src/main/java/org/apache/ignite/internal/storage/util/MvPartitionStorages.java`
#### Snippet
```java
            StorageOperation storageOperation = operationByPartitionId.compute(partitionId, (partId, operation) -> {
                if (operation == null) {
                    operation = new CloseStorageOperation();
                }

```

### AssignmentToLambdaParameter
Assignment to lambda parameter `e`
in `modules/cluster-management/src/main/java/org/apache/ignite/internal/cluster/management/ClusterInitializer.java`
#### Snippet
```java
                        } else {
                            if (e instanceof CompletionException) {
                                e = e.getCause();
                            }

```

### AssignmentToLambdaParameter
Assignment to lambda parameter `e`
in `modules/cluster-management/src/main/java/org/apache/ignite/internal/cluster/management/ClusterManagementGroupManager.java`
#### Snippet
```java

                                if (e instanceof CompletionException) {
                                    e = e.getCause();
                                }

```

### AssignmentToLambdaParameter
Assignment to lambda parameter `e`
in `modules/cluster-management/src/main/java/org/apache/ignite/internal/cluster/management/ClusterManagementGroupManager.java`
#### Snippet
```java
                                } else {
                                    if (e instanceof CompletionException) {
                                        e = e.getCause();
                                    }

```

## RuleId[id=UnnecessaryLocalVariable]
### UnnecessaryLocalVariable
Local variable `pair` is redundant
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/rpc/impl/core/AppendEntriesRequestProcessor.java`
#### Snippet
```java
            Map<String, PeerPair> pairs = this.pairConstants.computeIfAbsent(peerId, k -> new HashMap<>());

            PeerPair pair = pairs.computeIfAbsent(serverId, k -> new PeerPair(peerId, serverId));
            return pair;
        }
```

### UnnecessaryLocalVariable
Local variable `src` is redundant
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/SegmentList.java`
#### Snippet
```java

    private Object[] coll2Array(final Collection<T> coll) {
        Object[] src = coll.toArray();

        return src;
```

### UnnecessaryLocalVariable
Local variable `invoke` is redundant
in `modules/table/src/testFixtures/java/org/apache/ignite/internal/table/impl/DummyInternalTableImpl.java`
#### Snippet
```java
            lenient().doAnswer(
                    invocationOnMock -> {
                        CompletableFuture<Object> invoke = replicaListener.invoke(invocationOnMock.getArgument(1));
                        return invoke;
                    }
```

### UnnecessaryLocalVariable
Local variable `acc1` is redundant
in `modules/table/src/testFixtures/java/org/apache/ignite/internal/table/TxAbstractTest.java`
#### Snippet
```java

                        try {
                            long acc1 = finalI;

                            double amount = 100 + r.nextInt(500);
```

### UnnecessaryLocalVariable
Local variable `changeStateFuture` is redundant
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/replicator/PartitionReplicaListener.java`
#### Snippet
```java
        }

        CompletableFuture<Object> changeStateFuture = raftClient.run(finishTxCmdBldr.build()).whenComplete((o, throwable) -> {
            fut.complete(new TxMeta(commit ? TxState.COMMITED : TxState.ABORTED, aggregatedGroupIds, commitTimestamp));

```

### UnnecessaryLocalVariable
Local variable `convPub` is redundant
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/StorageScanNode.java`
#### Snippet
```java
     */
    protected Publisher<RowT> convertPublisher(Publisher<BinaryRow> pub) {
        Publisher<RowT> convPub = downstream -> {
            // BinaryRow -> RowT converter.
            Subscriber<BinaryRow> subs = new Subscriber<>() {
```

### UnnecessaryLocalVariable
Local variable `field` is redundant
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/ExpressionFactoryImpl.java`
#### Snippet
```java
            Expression row2 = list.append("row2", this.row2);

            Expression field = Expressions.call(
                    IgniteMethod.ROW_HANDLER_BI_GET.method(), hnd,
                    Expressions.constant(index), row1, row2);
```

### UnnecessaryLocalVariable
Local variable `field` is redundant
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/ExpressionFactoryImpl.java`
#### Snippet
```java
            Expression row = list.append("row", this.row);

            Expression field = Expressions.call(hnd,
                    IgniteMethod.ROW_HANDLER_GET.method(),
                    Expressions.constant(index), row);
```

### UnnecessaryLocalVariable
Local variable `exchangeId` is redundant
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/prepare/FragmentSplitter.java`
#### Snippet
```java
        long targetFragmentId = curr.id;
        long sourceFragmentId = IdGenerator.nextId();
        long exchangeId = sourceFragmentId;

        IgniteReceiver receiver = new IgniteReceiver(cluster, traits, rowType, exchangeId, sourceFragmentId);
```

### UnnecessaryLocalVariable
Local variable `exchangeId` is redundant
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/prepare/Splitter.java`
#### Snippet
```java
        long targetFragmentId = curr.id;
        long sourceFragmentId = IdGenerator.nextId();
        long exchangeId = sourceFragmentId;

        IgniteReceiver receiver = new IgniteReceiver(cluster, rel.getTraitSet(), rel.getRowType(), exchangeId, sourceFragmentId);
```

### UnnecessaryLocalVariable
Local variable `arr` is redundant
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/util/PageUtils.java`
#### Snippet
```java
            assert !buf.isReadOnly();

            byte[] arr = buf.array();

            srcBase = arr;
```

### UnnecessaryLocalVariable
Local variable `oom` is redundant
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/inmemory/VolatilePageMemory.java`
#### Snippet
```java

        if (relPtr == INVALID_REL_PTR) {
            IgniteOutOfMemoryException oom = new IgniteOutOfMemoryException("Out of memory in data region ["
                    + "name=" + dataRegionConfigView.name()
                    + ", initSize=" + IgniteUtils.readableSize(dataRegionConfigView.initSize(), false)
```

### UnnecessaryLocalVariable
Local variable `toNotify` is redundant
in `modules/transactions/src/main/java/org/apache/ignite/internal/tx/impl/HeapLockManager.java`
#### Snippet
```java
            }

            List<WaiterImpl> toNotify = unlockCompatibleWaiters();

            return toNotify;
```

### UnnecessaryLocalVariable
Local variable `booleans` is redundant
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/util/ConfigurationSerializationUtil.java`
#### Snippet
```java

    private static byte[] compressBooleanArray(boolean[] value) {
        boolean[] booleans = value;

        int payloadSize = (booleans.length + Byte.SIZE - 1) / Byte.SIZE;
```

## RuleId[id=UseCompareMethod]
### UseCompareMethod
Expression can be replaced with 'Integer.compare'
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/GroupPartitionId.java`
#### Snippet
```java
        }

        if (getPartitionId() < o.getPartitionId()) {
            return -1;
        }
```

## RuleId[id=BusyWait]
### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `modules/core/src/main/java/org/apache/ignite/internal/util/FastTimestamps.java`
#### Snippet
```java
                    coarseCurrentTimeMillis = System.currentTimeMillis();
                    try {
                        Thread.sleep(UPDATE_FREQUENCY_MS);
                    } catch (InterruptedException e) {
                        break;
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `modules/core/src/main/java/org/apache/ignite/internal/components/LongJvmPauseDetector.java`
#### Snippet
```java
                while (true) {
                    try {
                        Thread.sleep(PRECISION);

                        final long now = System.currentTimeMillis();
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/timer/HashedWheelTimer.java`
#### Snippet
```java

                try {
                    Thread.sleep(sleepTimeMs);
                }
                catch (InterruptedException ignored) {
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `modules/raft/src/testFixtures/java/org/apache/ignite/raft/jraft/core/TestCluster.java`
#### Snippet
```java
            }
            else {
                Thread.sleep(10);
            }
        }
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `modules/raft/src/testFixtures/java/org/apache/ignite/raft/jraft/core/TestCluster.java`
#### Snippet
```java

                if (wait) {
                    Thread.sleep(10);

                    continue;
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `modules/table/src/testFixtures/java/org/apache/ignite/internal/table/impl/DummyInternalTableImpl.java`
#### Snippet
```java

                try {
                    Thread.sleep(1_000);
                } catch (InterruptedException e) {
                    LOG.warn("The sfe time updater thread is interrupted");
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/session/SessionManager.java`
#### Snippet
```java
                    blockingSectionBegin();
                    try {
                        Thread.sleep(expirationCheckPeriod);
                    } finally {
                        blockingSectionEnd();
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `modules/placement-driver/src/main/java/org/apache/ignite/internal/placementdriver/LeaseUpdater.java`
#### Snippet
```java

                try {
                    Thread.sleep(UPDATE_LEASE_MS);
                } catch (InterruptedException e) {
                    LOG.warn("Lease updater is interrupted");
```

## RuleId[id=UseBulkOperation]
### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `modules/core/src/main/java/org/apache/ignite/internal/thread/StripedThreadPoolExecutor.java`
#### Snippet
```java
        for (ExecutorService exec : execs) {
            for (Runnable r : exec.shutdownNow()) {
                res.add(r);
            }
        }
```

## RuleId[id=AssignmentToCatchBlockParameter]
### AssignmentToCatchBlockParameter
Assignment to 'catch' block parameter `e`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/store/AbstractFilePageStoreIo.java`
#### Snippet
```java
                        e0.addSuppressed(e);

                        e = e0;
                    }
                }
```

## RuleId[id=StringEquality]
### StringEquality
String values are compared using `==`, not 'equals()'
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/trait/DistributionFunction.java`
#### Snippet
```java
     */
    public static boolean satisfy(DistributionFunction f0, DistributionFunction f1) {
        if (f0 == f1 || f0.name() == f1.name()) {
            return true;
        }
```

## RuleId[id=ConditionalBreakInInfiniteLoop]
### ConditionalBreakInInfiniteLoop
Conditional break inside loop
in `modules/core/src/main/java/org/apache/ignite/internal/util/subscription/ConcatenatedPublisher.java`
#### Snippet
```java
            long requestAmount = 0L;

            for (; ; ) {
                Subscription currentSub = (Subscription) CURRENT.getAcquire(this);

```

### ConditionalBreakInInfiniteLoop
Conditional break inside loop
in `modules/core/src/main/java/org/apache/ignite/internal/util/subscription/OrderedMergePublisher.java`
#### Snippet
```java
            long emitted = this.emitted;

            for (; ; ) {
                long requested = (long) REQUESTED.getAcquire(this);

```

### ConditionalBreakInInfiniteLoop
Conditional break inside loop
in `modules/raft/src/testFixtures/java/org/apache/ignite/raft/jraft/core/TestCluster.java`
#### Snippet
```java
        Node node;

        while (true) {
            node = getLeader();

```

## RuleId[id=LoopStatementsThatDontLoop]
### LoopStatementsThatDontLoop
`while` statement does not loop
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/StringUtils.java`
#### Snippet
```java
                boolean lastMatch = false;

                while (true) {
                    while (i < len) {
                        if (str.charAt(i) == separatorChar) {
```

## RuleId[id=StaticCallOnSubclass]
### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/api/src/main/java/org/apache/ignite/network/ClusterNode.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(ClusterNode.class, this);
    }
}
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/api/src/main/java/org/apache/ignite/deployment/UnitStatus.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(this);
    }

```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/api/src/main/java/org/apache/ignite/table/TupleImpl.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(Tuple.class.getSimpleName(), colNames, colValues);
    }
}
```

### StaticCallOnSubclass
Static method `nullWriter()` declared in class 'java.io.Writer' but referenced via subclass 'java.io.PrintWriter'
in `modules/cli/src/main/java/org/apache/ignite/internal/cli/core/exception/ExceptionWriter.java`
#### Snippet
```java

    static ExceptionWriter nullWriter() {
        return fromPrintWriter(new PrintWriter(PrintWriter.nullWriter()));
    }
}
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/core/src/main/java/org/apache/ignite/lang/IgniteTuple3.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(IgniteTuple3.class, this);
    }
}
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/core/src/main/java/org/apache/ignite/internal/hlc/HybridTimestamp.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(HybridTimestamp.class, this);
    }
}
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/core/src/main/java/org/apache/ignite/lang/JavaLoggerFormatter.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(JavaLoggerFormatter.class, this);
    }

```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/core/src/main/java/org/apache/ignite/internal/hlc/HybridClockImpl.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(HybridClock.class, this);
    }
}
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/core/src/main/java/org/apache/ignite/lang/IgniteBiTuple.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(IgniteBiTuple.class, this);
    }
}
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/core/src/main/java/org/apache/ignite/internal/util/IgniteSpinReadWriteLock.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(IgniteSpinReadWriteLock.class, this);
    }
}
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/core/src/main/java/org/apache/ignite/internal/util/IgniteStripedLock.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(IgniteStripedLock.class, this, "concurrency", locks.length);
    }
}
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/core/src/main/java/org/apache/ignite/internal/util/IgniteConcurrentMultiPairQueue.java`
#### Snippet
```java
        @Override
        public String toString() {
            return S.toString(Result.class, this);
        }
    }
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/core/src/main/java/org/apache/ignite/internal/util/worker/IgniteWorker.java`
#### Snippet
```java
        Thread runner = this.runner;

        return S.toString(IgniteWorker.class, this,
                "hashCode", hashCode(),
                "interrupted", (runner != null ? runner.isInterrupted() : "unknown"),
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/core/src/main/java/org/apache/ignite/internal/thread/IgniteThread.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(IgniteThread.class, this, "name", getName());
    }
}
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/core/src/main/java/org/apache/ignite/internal/thread/StripedThreadPoolExecutor.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(StripedThreadPoolExecutor.class, this);
    }
}
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/core/src/main/java/org/apache/ignite/internal/components/LongJvmPauseDetector.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(LongJvmPauseDetector.class, this);
    }
}
```

### StaticCallOnSubclass
Static method `setSensitiveDataLoggingPolicySupplier()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/core/src/testFixtures/java/org/apache/ignite/internal/testframework/BaseIgniteAbstractTest.java`
#### Snippet
```java
    /* Init test env. */
    static {
        S.setSensitiveDataLoggingPolicySupplier(() ->
                SensitiveDataLoggingPolicy.valueOf(getString(IGNITE_SENSITIVE_DATA_LOGGING, "hash").toUpperCase()));
    }
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/jdbc/src/main/java/org/apache/ignite/internal/jdbc/JdbcQueryExecuteResponse.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(JdbcQueryExecuteResponse.class, this);
    }

```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/rpc/impl/IgniteRpcServer.java`
#### Snippet
```java
            } catch (RejectedExecutionException e) {
                // The rejection is ok if an executor has been stopped, otherwise it shouldn't happen.
                LOG.warn("A request execution was rejected [sender={} req={} reason={}]", sender, S.toString(message), e.getMessage());
            }
        }
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/rpc/impl/IgniteRpcClient.java`
#### Snippet
```java
                blockedMsgs.add(msgData);
    
                LOG.info("Blocked message to={} id={} msg={}", peerId.toString(), msgData[2], S.toString(request));

                return fut;
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/rpc/impl/IgniteRpcClient.java`
#### Snippet
```java
            Runnable r = (Runnable) msg[4];
    
            LOG.info("Unblocked message to={} id={} msg={}", msg[1], msg[2], S.toString(msg[0]));
            
            r.run();
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/ExponentialBackoffTimeoutStrategy.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(ExponentialBackoffTimeoutStrategy.class, this);
    }
}
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/raft/src/main/java/org/apache/ignite/internal/raft/RaftGroupServiceImpl.java`
#### Snippet
```java
                        if (LOG.isTraceEnabled()) {
                            LOG.trace("sendWithRetry resp={} from={} to={} err={}",
                                    S.toString(resp),
                                    cluster.topologyService().localMember().address(),
                                    peer.consistentId(),
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/raft/RaftGroupConfiguration.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(RaftGroupConfiguration.class, this);
    }
}
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/raft/snapshot/PartitionKey.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(PartitionKey.class, this);
    }
}
```

### StaticCallOnSubclass
Static method `getByName()` declared in class 'java.net.InetAddress' but referenced via subclass 'java.net.Inet6Address'
in `modules/client/src/main/java/org/apache/ignite/internal/client/HostAndPortRange.java`
#### Snippet
```java
                if (addrStr.lastIndexOf(':', colIdx - 1) != -1) { // IPv6 without [] and port
                    try {
                        Inet6Address.getByName(addrStr);
                        host = addrStr;
                        portFrom = dfltPortFrom;
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/client/src/main/java/org/apache/ignite/internal/client/sql/ClientColumnOrigin.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(ClientColumnOrigin.class,  this);
    }
}
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/schema/src/main/java/org/apache/ignite/internal/schema/DecimalNativeType.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(DecimalNativeType.class.getSimpleName(), "name", spec(), "precision", precision, "scale", scale);
    }
}
```

### StaticCallOnSubclass
Static method `arrayToString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/schema/src/main/java/org/apache/ignite/internal/schema/Columns.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.arrayToString(cols);
    }
}
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/schema/src/main/java/org/apache/ignite/internal/schema/SchemaDescriptor.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(SchemaDescriptor.class, this);
    }
}
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/schema/src/main/java/org/apache/ignite/internal/schema/NativeType.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(NativeType.class.getSimpleName(),
                "name", typeSpec.name(),
                "sizeInBytes", size,
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/schema/src/main/java/org/apache/ignite/internal/schema/VarlenNativeType.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(VarlenNativeType.class.getSimpleName(), "name", spec(), "len", len);
    }
}
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/schema/src/main/java/org/apache/ignite/internal/schema/NumberNativeType.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(NumberNativeType.class.getSimpleName(), "typeSpec", spec(), "precision", precision());
    }
}
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/schema/src/main/java/org/apache/ignite/internal/schema/TemporalNativeType.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(TemporalNativeType.class.getSimpleName(), "name", spec(), "precision", precision);
    }
}
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/schema/src/main/java/org/apache/ignite/internal/schema/Column.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(Column.class, this);
    }
}
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/schema/src/main/java/org/apache/ignite/internal/schema/BitmaskNativeType.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(BitmaskNativeType.class.getSimpleName(), "bits", bits, "typeSpec", spec(), "len", sizeInBytes());
    }
}
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/schema/src/main/java/org/apache/ignite/internal/schema/NativeTypeSpec.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(NativeTypeSpec.class.getSimpleName(),
                "name", name(),
                "fixed", fixedLength());
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/schema/src/testFixtures/java/org/apache/ignite/internal/schema/testutils/definition/ColumnDefinitionImpl.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(ColumnDefinitionImpl.class, this);
    }
}
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/schema/src/testFixtures/java/org/apache/ignite/internal/schema/testutils/definition/AbstractSchemaObject.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString("SchemaObject",
                "name", name,
                "class", getClass().getName());
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/schema/src/testFixtures/java/org/apache/ignite/internal/schema/testutils/definition/index/SortedIndexDefinitionImpl.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(SortedIndexDefinition.class, this,
                "type", type(),
                "name", name());
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/schema/src/testFixtures/java/org/apache/ignite/internal/schema/testutils/definition/index/SortedIndexColumnDefinitionImpl.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(SortedIndexColumnDefinitionImpl.class, this);
    }
}
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/schema/src/testFixtures/java/org/apache/ignite/internal/schema/testutils/definition/index/HashIndexDefinitionImpl.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(HashIndexDefinitionImpl.class, this,
                "type", type(),
                "name", name());
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/schema/src/testFixtures/java/org/apache/ignite/internal/schema/testutils/definition/index/IndexColumnDefinitionImpl.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(IndexColumnDefinitionImpl.class.getSimpleName(), "name", name());
    }
}
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/schema/src/testFixtures/java/org/apache/ignite/internal/schema/testutils/definition/TableDefinitionImpl.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(TableDefinitionImpl.class, this);
    }
}
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/schema/src/testFixtures/java/org/apache/ignite/internal/schema/testutils/definition/index/PrimaryKeyDefinitionImpl.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(PrimaryKeyDefinitionImpl.class, this,
                "name", name(),
                "cols", columns(),
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/network/src/main/java/org/apache/ignite/internal/network/netty/DefaultRecoveryDescriptorProvider.java`
#### Snippet
```java
        @Override
        public String toString() {
            return S.toString(ChannelKey.class, this);
        }
    }
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/network/src/main/java/org/apache/ignite/internal/network/recovery/RecoveryDescriptor.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(RecoveryDescriptor.class, this);
    }
}
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/affinity/src/main/java/org/apache/ignite/internal/affinity/Assignment.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(Assignment.class, this);
    }
}
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/raft-api/src/main/java/org/apache/ignite/internal/raft/RaftNodeId.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(RaftNodeId.class, this);
    }
}
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/raft-api/src/main/java/org/apache/ignite/internal/raft/Peer.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(Peer.class, this);
    }
}
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/raft-api/src/main/java/org/apache/ignite/internal/raft/PeersAndLearners.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(PeersAndLearners.class, this);
    }
}
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/raft-api/src/main/java/org/apache/ignite/internal/raft/service/CommittedConfiguration.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(CommittedConfiguration.class, this);
    }
}
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/api/ResultSetMetadataImpl.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(ResultSetMetadataImpl.class, this);
    }
}
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/api/ColumnMetadataImpl.java`
#### Snippet
```java
        @Override
        public String toString() {
            return S.toString(ColumnOriginImpl.class,  this);
        }
    }
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/api/ColumnMetadataImpl.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(ColumnMetadataImpl.class,  this);
    }

```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/TxAttributes.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(this);
    }
}
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/MailboxRegistryImpl.java`
#### Snippet
```java
        @Override
        public String toString() {
            return S.toString(MailboxKey.class, this);
        }
    }
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/MailboxRegistryImpl.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(MailboxRegistryImpl.class, this);
    }

```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/type/IgniteCustomTypeSpec.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(IgniteCustomTypeSpec.class, this);
    }
}
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/prepare/Fragment.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(Fragment.class, this, "root", RelOptUtil.toString(root));
    }
}
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/prepare/bounds/ExactBounds.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(ExactBounds.class, this);
    }
}
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/prepare/bounds/MultiBounds.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(MultiBounds.class, this);
    }
}
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/session/SessionId.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(this);
    }
}
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/prepare/bounds/RangeBounds.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(RangeBounds.class, this);
    }
}
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/metadata/cost/IgniteCost.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(IgniteCost.class, this);
    }
}
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/property/Property.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(Property.class, this);
    }
}
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/metastorage/src/main/java/org/apache/ignite/internal/metastorage/impl/EntryImpl.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(this);
    }
}
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/io/DataPagePayload.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(DataPagePayload.class, this);
    }
}
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/io/IoVersions.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(IoVersions.class, this);
    }
}
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/mem/unsafe/UnsafeChunk.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(UnsafeChunk.class, this);
    }
}
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/GroupPartitionId.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(GroupPartitionId.class, this);
    }

```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/ReplaceCandidate.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(ReplaceCandidate.class, this, "relPtr", hexLong(relPtr));
    }
}
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/PartitionMeta.java`
#### Snippet
```java
        @Override
        public String toString() {
            return S.toString(PartitionMetaSnapshot.class, this);
        }
    }
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/PartitionMeta.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(PartitionMeta.class, this);
    }

```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/freelist/PagesList.java`
#### Snippet
```java
        @Override
        public String toString() {
            return S.toString(SingletonReuseBag.class, this, "pageId", hexLong(pageId));
        }
    }
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/storage-api/src/main/java/org/apache/ignite/internal/storage/TxIdMismatchException.java`
#### Snippet
```java
     */
    public TxIdMismatchException(UUID expectedTxId, UUID conflictingTxId) {
        super(S.toString(
                "Mismatched transaction id",
                "expectedTxId", expectedTxId, false,
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/storage-api/src/main/java/org/apache/ignite/internal/storage/index/HashIndexDescriptor.java`
#### Snippet
```java
        @Override
        public String toString() {
            return S.toString(this);
        }
    }
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/storage-api/src/main/java/org/apache/ignite/internal/storage/DataStorageManager.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(DataStorageManager.class, this);
    }
}
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/storage-api/src/main/java/org/apache/ignite/internal/storage/index/SortedIndexDescriptor.java`
#### Snippet
```java
        @Override
        public String toString() {
            return S.toString(this);
        }
    }
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/BaseMvStoragesTest.java`
#### Snippet
```java
        @Override
        public String toString() {
            return S.toString(TestValue.class, this);
        }
    }
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/BaseMvStoragesTest.java`
#### Snippet
```java
        @Override
        public String toString() {
            return S.toString(TestKey.class, this);
        }
    }
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/transactions/src/main/java/org/apache/ignite/internal/tx/Lock.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(this);
    }
}
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/transactions/src/main/java/org/apache/ignite/internal/tx/TxMeta.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(TxMeta.class, this);
    }

```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/transactions/src/main/java/org/apache/ignite/internal/tx/LockKey.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(this);
    }
}
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/transactions/src/main/java/org/apache/ignite/internal/tx/impl/HeapLockManager.java`
#### Snippet
```java
        @Override
        public String toString() {
            return S.toString(WaiterImpl.class, this, "isDone", fut.isDone());
        }
    }
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/client-common/src/main/java/org/apache/ignite/internal/jdbc/proto/event/JdbcQueryFetchRequest.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(JdbcQueryFetchRequest.class, this);
    }
}
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/client-common/src/main/java/org/apache/ignite/internal/jdbc/proto/event/JdbcMetaColumnsRequest.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(JdbcMetaColumnsRequest.class, this);
    }
}
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/client-common/src/main/java/org/apache/ignite/internal/jdbc/proto/event/JdbcBatchExecuteResult.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(JdbcBatchExecuteResult.class, this);
    }
}
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/client-common/src/main/java/org/apache/ignite/internal/jdbc/proto/event/JdbcBatchExecuteRequest.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(JdbcBatchExecuteRequest.class, this);
    }
}
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/client-common/src/main/java/org/apache/ignite/internal/jdbc/proto/event/JdbcMetaSchemasRequest.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(JdbcMetaSchemasRequest.class, this);
    }
}
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/client-common/src/main/java/org/apache/ignite/internal/jdbc/proto/event/Response.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(Response.class, this);
    }
}
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/client-common/src/main/java/org/apache/ignite/internal/jdbc/proto/event/JdbcQueryFetchResult.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(JdbcQueryFetchResult.class, this);
    }
}
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/client-common/src/main/java/org/apache/ignite/internal/jdbc/proto/event/JdbcQueryCloseRequest.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(JdbcQueryCloseRequest.class, this);
    }
}
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/client-common/src/main/java/org/apache/ignite/internal/jdbc/proto/event/JdbcQueryMetadataRequest.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(JdbcQueryMetadataRequest.class, this);
    }
}
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/client-common/src/main/java/org/apache/ignite/internal/jdbc/proto/event/JdbcMetaTablesResult.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(JdbcMetaTablesResult.class, this);
    }
}
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/client-common/src/main/java/org/apache/ignite/internal/jdbc/proto/event/JdbcMetaColumnsResult.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(JdbcMetaColumnsResult.class, this);
    }
}
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/client-common/src/main/java/org/apache/ignite/internal/jdbc/proto/event/JdbcPrimaryKeyMeta.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(JdbcPrimaryKeyMeta.class, this);
    }
}
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/client-common/src/main/java/org/apache/ignite/internal/jdbc/proto/event/JdbcMetaTablesRequest.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(JdbcMetaTablesRequest.class, this);
    }
}
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/client-common/src/main/java/org/apache/ignite/internal/jdbc/proto/event/JdbcQueryCloseResult.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(JdbcQueryCloseResult.class, this);
    }
}
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/client-common/src/main/java/org/apache/ignite/internal/jdbc/proto/event/JdbcQueryExecuteResult.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(JdbcQueryExecuteResult.class, this);
    }
}
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/client-common/src/main/java/org/apache/ignite/internal/jdbc/proto/event/JdbcBatchPreparedStmntRequest.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(JdbcBatchPreparedStmntRequest.class, this);
    }
}
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/client-common/src/main/java/org/apache/ignite/internal/jdbc/proto/event/JdbcQuerySingleResult.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(JdbcQuerySingleResult.class, this);
    }
}
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/client-common/src/main/java/org/apache/ignite/internal/jdbc/proto/event/JdbcTableMeta.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(JdbcTableMeta.class, this);
    }
}
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/client-common/src/main/java/org/apache/ignite/internal/jdbc/proto/event/JdbcMetaPrimaryKeysRequest.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(JdbcMetaPrimaryKeysRequest.class, this);
    }
}
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/client-common/src/main/java/org/apache/ignite/internal/jdbc/proto/event/JdbcMetaSchemasResult.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(JdbcMetaSchemasResult.class, this);
    }
}
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/client-common/src/main/java/org/apache/ignite/internal/jdbc/proto/event/JdbcQueryExecuteRequest.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(JdbcQueryExecuteRequest.class, this);
    }
}
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/client-common/src/main/java/org/apache/ignite/internal/jdbc/proto/event/JdbcMetaPrimaryKeysResult.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(JdbcMetaPrimaryKeysResult.class, this);
    }
}
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/client-common/src/main/java/org/apache/ignite/internal/jdbc/proto/event/JdbcColumnMeta.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(JdbcColumnMeta.class, this);
    }

```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/client-common/src/main/java/org/apache/ignite/internal/client/proto/ProtocolVersion.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(ProtocolVersion.class, this);
    }
}
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/DynamicProperty.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(DynamicProperty.class, this, "key", key, "value", value());
    }
}
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/direct/KeyPathNode.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(KeyPathNode.class, this);
    }
}
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/client-handler/src/main/java/org/apache/ignite/client/handler/ClientContext.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(ClientContext.class, this);
    }
}
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/metastorage-api/src/main/java/org/apache/ignite/internal/metastorage/EntryEvent.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(this);
    }
}
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/metastorage-api/src/main/java/org/apache/ignite/internal/metastorage/WatchEvent.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(this);
    }
}
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/storage-rocksdb/src/main/java/org/apache/ignite/internal/storage/rocksdb/GcRowVersion.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(GcRowVersion.class, this);
    }
}
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/configuration-api/src/main/java/org/apache/ignite/configuration/RootKey.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(RootKey.class, this);
    }
}
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/configuration-api/src/main/java/org/apache/ignite/configuration/validation/ValidationIssue.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(ValidationIssue.class, this);
    }
}
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/cluster-management/src/main/java/org/apache/ignite/internal/cluster/management/topology/api/LogicalTopologySnapshot.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(LogicalTopologySnapshot.class, this);
    }
}
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/storage-page-memory/src/main/java/org/apache/ignite/internal/storage/pagememory/mv/RowVersion.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(RowVersion.class, this);
    }
}
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/storage-page-memory/src/main/java/org/apache/ignite/internal/storage/pagememory/mv/VersionChain.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(VersionChain.class, this);
    }
}
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/storage-page-memory/src/main/java/org/apache/ignite/internal/storage/pagememory/index/meta/IndexMeta.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(IndexMeta.class, this, "metaPageId", IgniteUtils.hexLong(metaPageId));
    }
}
```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/tree/BplusTree.java`
#### Snippet
```java
    @Override
    public String toString() {
        return S.toString(BplusTree.class, this);
    }

```

### StaticCallOnSubclass
Static method `toString()` declared in class 'org.apache.ignite.internal.tostring.IgniteToStringBuilder' but referenced via subclass 'org.apache.ignite.internal.tostring.S'
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/tree/BplusTree.java`
#### Snippet
```java
        @Override
        public String toString() {
            return S.toString(TreeMetaData.class, this);
        }
    }
```

### StaticCallOnSubclass
Static method `getPageId()` declared in class 'org.apache.ignite.internal.pagememory.io.PageIo' but referenced via subclass 'org.apache.ignite.internal.pagememory.tree.io.BplusIo'
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/tree/BplusTree.java`
#### Snippet
```java

            try {
                long realPageId = BplusIo.getPageId(pageAddr);

                if (realPageId != pageId) {
```

## RuleId[id=ExternalizableWithoutPublicNoArgConstructor]
### ExternalizableWithoutPublicNoArgConstructor
Externalizable class `IgniteBiTuple`> has no 'public' no-arg constructor
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/checkpoint/CheckpointWorkflow.java`
#### Snippet
```java
     */
    public void removeCheckpointListener(CheckpointListener listener) {
        listeners.remove(new IgniteBiTuple<CheckpointListener, DataRegion<PersistentPageMemory>>() {
            /** {@inheritDoc} */
            @Override
```

## RuleId[id=AssertWithSideEffects]
### AssertWithSideEffects
`assert` has side effects: realPagesArrSize++
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/checkpoint/CheckpointWorkflow.java`
#### Snippet
```java

            for (FullPageId dirtyPage : dataRegionDirtyPages.dirtyPages) {
                assert realPagesArrSize++ != dataRegionsDirtyPages.dirtyPageCount :
                        "Incorrect estimated dirty pages number: " + dataRegionsDirtyPages.dirtyPageCount;

```

## RuleId[id=DuplicateBranchesInSwitch]
### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `modules/client/src/main/java/org/apache/ignite/internal/client/ClientUtils.java`
#### Snippet
```java

            case ClientOp.SCHEMAS_GET:
                return null;

            case ClientOp.TUPLE_UPSERT:
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `modules/client/src/main/java/org/apache/ignite/internal/client/ClientUtils.java`
#### Snippet
```java

            case ClientOp.JDBC_EXEC:
                return null;

            case ClientOp.JDBC_NEXT:
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `modules/client/src/main/java/org/apache/ignite/internal/client/ClientUtils.java`
#### Snippet
```java

            case ClientOp.JDBC_NEXT:
                return null;

            case ClientOp.JDBC_EXEC_BATCH:
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `modules/client/src/main/java/org/apache/ignite/internal/client/ClientUtils.java`
#### Snippet
```java

            case ClientOp.JDBC_EXEC_BATCH:
                return null;

            case ClientOp.JDBC_CURSOR_CLOSE:
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `modules/client/src/main/java/org/apache/ignite/internal/client/ClientUtils.java`
#### Snippet
```java

            case ClientOp.JDBC_CURSOR_CLOSE:
                return null;

            case ClientOp.JDBC_TABLE_META:
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `modules/client/src/main/java/org/apache/ignite/internal/client/ClientUtils.java`
#### Snippet
```java

            case ClientOp.JDBC_TABLE_META:
                return null;

            case ClientOp.JDBC_COLUMN_META:
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `modules/client/src/main/java/org/apache/ignite/internal/client/ClientUtils.java`
#### Snippet
```java

            case ClientOp.JDBC_COLUMN_META:
                return null;

            case ClientOp.JDBC_SCHEMAS_META:
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `modules/client/src/main/java/org/apache/ignite/internal/client/ClientUtils.java`
#### Snippet
```java

            case ClientOp.JDBC_SCHEMAS_META:
                return null;

            case ClientOp.JDBC_PK_META:
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `modules/client/src/main/java/org/apache/ignite/internal/client/ClientUtils.java`
#### Snippet
```java

            case ClientOp.JDBC_PK_META:
                return null;

            case ClientOp.JDBC_QUERY_META:
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `modules/client/src/main/java/org/apache/ignite/internal/client/ClientUtils.java`
#### Snippet
```java

            case ClientOp.JDBC_QUERY_META:
                return null;

            case ClientOp.TX_BEGIN:
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `modules/client/src/main/java/org/apache/ignite/internal/client/ClientUtils.java`
#### Snippet
```java

            case ClientOp.JDBC_SQL_EXEC_PS_BATCH:
                return null;

            case ClientOp.COMPUTE_EXECUTE:
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `modules/client/src/main/java/org/apache/ignite/internal/client/ClientUtils.java`
#### Snippet
```java

            case ClientOp.CLUSTER_GET_NODES:
                return null;

            case ClientOp.SQL_EXEC:
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `modules/client/src/main/java/org/apache/ignite/internal/client/ClientUtils.java`
#### Snippet
```java

            case ClientOp.SQL_CURSOR_CLOSE:
                return null;

            case ClientOp.PARTITION_ASSIGNMENT_GET:
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `modules/client/src/main/java/org/apache/ignite/internal/client/ClientUtils.java`
#### Snippet
```java

            case ClientOp.PARTITION_ASSIGNMENT_GET:
                return null;

            // Do not return null from default arm intentionally, so we don't forget to update this when new ClientOp values are added.
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/RexImpTable.java`
#### Snippet
```java
                return TimeUnit.YEAR.multiplier.longValue();
            case QUARTER:
                return TimeUnit.YEAR.multiplier.longValue();
            case YEAR:
            case DECADE:
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/RexImpTable.java`
#### Snippet
```java
            case CENTURY:
            case MILLENNIUM:
                return 1L;
            default:
                throw Util.unexpected(unit);
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `modules/client-common/src/main/java/org/apache/ignite/internal/client/proto/ClientMessageUnpacker.java`
#### Snippet
```java

                case FIXEXT1:
                    skipBytes(2);
                    break;

```

## RuleId[id=StringBufferReplaceableByString]
### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `modules/bytecode/src/main/java/com/facebook/presto/bytecode/FieldDefinition.java`
#### Snippet
```java
    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("FieldDefinition");
        sb.append("{access=").append(access);
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `modules/bytecode/src/main/java/com/facebook/presto/bytecode/MethodDefinition.java`
#### Snippet
```java
        ParameterizedType returnType,
        List<ParameterizedType> parameterTypes) {
        StringBuilder sb = new StringBuilder();
        sb.append("(");
        sb.append(parameterTypes.stream().map(ParameterizedType::toString).collect(Collectors.joining("")));
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `modules/bytecode/src/main/java/com/facebook/presto/bytecode/MethodDefinition.java`
#### Snippet
```java
        ParameterizedType returnType,
        List<ParameterizedType> parameterTypes) {
        StringBuilder sb = new StringBuilder();
        sb.append("(");
        sb.append(parameterTypes.stream().map(ParameterizedType::getType).collect(Collectors.joining("")));
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `modules/bytecode/src/main/java/com/facebook/presto/bytecode/MethodDefinition.java`
#### Snippet
```java

    public String toSourceString() {
        StringBuilder sb = new StringBuilder();
        sb.append(access.stream().map(Access::toString).collect(Collectors.joining(" ")))
            .append(' ');
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `modules/bytecode/src/main/java/com/facebook/presto/bytecode/ClassDefinition.java`
#### Snippet
```java
    @Override
    public String toString() {
        final StringBuilder sb = new StringBuilder();
        sb.append("ClassDefinition");
        sb.append("{access=").append(access);
```

## RuleId[id=UnnecessaryReturn]
### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `modules/storage-api/src/main/java/org/apache/ignite/internal/storage/util/ReentrantLockByRowId.java`
#### Snippet
```java

        if (lockedRowIds == null) {
            return;
        } else if (lockedRowIds instanceof RowId) {
            releaseLock0(((RowId) lockedRowIds), true);
```

## RuleId[id=NonShortCircuitBoolean]
### NonShortCircuitBoolean
Non-short-circuit boolean expression `res &= exec.awaitTermination(timeout, unit)`
in `modules/core/src/main/java/org/apache/ignite/internal/thread/StripedThreadPoolExecutor.java`
#### Snippet
```java

        for (ExecutorService exec : execs) {
            res &= exec.awaitTermination(timeout, unit);
        }

```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `hasNullables |= nullable`
in `modules/schema/src/main/java/org/apache/ignite/internal/schema/BinaryTupleSchema.java`
#### Snippet
```java
            boolean nullable = column.nullable();
            elements[i] = new Element(column.type(), nullable);
            hasNullables |= nullable;
        }

```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `hasNullables |= nullable`
in `modules/schema/src/main/java/org/apache/ignite/internal/schema/BinaryTupleSchema.java`
#### Snippet
```java
            boolean nullable = column.nullable();
            elements[i] = new Element(column.type(), nullable);
            hasNullables |= nullable;
        }

```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `lowerInclude |= rangeBounds.lowerInclude()`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/util/RexUtils.java`
#### Snippet
```java
                            if (lowerBound != null && lowerBound != nullBound) {
                                lowerBound = leastOrGreatest(builder, !ascDir, lowerBound, rangeBounds.lowerBound());
                                lowerInclude |= rangeBounds.lowerInclude();
                            } else {
                                lowerBound = rangeBounds.lowerBound();
```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `upperInclude |= rangeBounds.upperInclude()`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/util/RexUtils.java`
#### Snippet
```java
                            if (upperBound != null && upperBound != nullBound) {
                                upperBound = leastOrGreatest(builder, ascDir, upperBound, rangeBounds.upperBound());
                                upperInclude |= rangeBounds.upperInclude();
                            } else {
                                upperBound = rangeBounds.upperBound();
```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `lowerInclude |= includeBound`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/util/RexUtils.java`
#### Snippet
```java
                        } else {
                            lowerBound = leastOrGreatest(builder, lessCondition, lowerBound, val);
                            lowerInclude |= includeBound;
                            lowerCond = lessOrGreater(builder, lessCondition, lowerInclude, ref, lowerBound);
                        }
```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `upperInclude |= includeBound`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/util/RexUtils.java`
#### Snippet
```java
                        } else {
                            upperBound = leastOrGreatest(builder, lessCondition, upperBound, val);
                            upperInclude |= includeBound;
                            upperCond = lessOrGreater(builder, lessCondition, upperInclude, ref, upperBound);
                        }
```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `modified |= removed`
in `modules/metastorage/src/main/java/org/apache/ignite/internal/metastorage/server/persistence/RocksDbKeyValueStorage.java`
#### Snippet
```java
                        }

                        modified |= removed;

                        break;
```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `modified |= doRemove(op.key(), curRev)`
in `modules/metastorage/src/testFixtures/java/org/apache/ignite/internal/metastorage/server/SimpleInMemoryKeyValueStorage.java`
#### Snippet
```java

                    case REMOVE:
                        modified |= doRemove(op.key(), curRev);

                        break;
```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `modified |= doRemove(op.key(), curRev)`
in `modules/metastorage/src/testFixtures/java/org/apache/ignite/internal/metastorage/server/SimpleInMemoryKeyValueStorage.java`
#### Snippet
```java

                            case REMOVE:
                                modified |= doRemove(op.key(), curRev);

                                break;
```

## RuleId[id=ManualMinMaxCalculation]
### ManualMinMaxCalculation
Can be replaced with 'Math.max()' call
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/Utils.java`
#### Snippet
```java
     */
    public static ByteBuffer expandByteBufferAtLeast(final ByteBuffer buf, final int minLength) {
        final int newCapacity = minLength > RAFT_DATA_BUF_SIZE ? minLength : RAFT_DATA_BUF_SIZE;
        final ByteBuffer newBuf = ByteBuffer.allocate(buf.capacity() + newCapacity);
        buf.flip();
```

### ManualMinMaxCalculation
Can be replaced with 'Math.min()' call
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/storage/snapshot/ThroughputSnapshotThrottle.java`
#### Snippet
```java
                else {
                    // otherwise, read the data in the next cycle.
                    availableSize = bytes > limitPerCycle ? limitPerCycle : bytes;
                    this.currThroughputBytes = availableSize;
                    this.lastThroughputCheckTimeUs = calculateCheckTimeUs(nowUs);
```

### ManualMinMaxCalculation
Can be replaced with 'Math.max()' call
in `modules/affinity/src/main/java/org/apache/ignite/internal/affinity/RendezvousAffinityFunction.java`
#### Snippet
```java
        i = Math.abs(i);

        return i < 0 ? 0 : i;
    }
}
```

## RuleId[id=AbstractClassNeverImplemented]
### AbstractClassNeverImplemented
Abstract class `GridUnsafe` has no concrete subclass
in `modules/core/src/main/java/org/apache/ignite/internal/util/GridUnsafe.java`
#### Snippet
```java
 * </ul>
 */
public abstract class GridUnsafe {
    /** Native byte order. */
    public static final ByteOrder NATIVE_BYTE_ORDER = ByteOrder.nativeOrder();
```

### AbstractClassNeverImplemented
Abstract class `IgniteMinusBase` has no concrete subclass
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rel/set/IgniteMinusBase.java`
#### Snippet
```java
 * Base class for physical MINUS (EXCEPT) set op.
 */
public abstract class IgniteMinusBase extends Minus implements TraitsAwareIgniteRel {
    /** Count of counter fields used to aggregate results. */
    protected static final int COUNTER_FIELDS_CNT = 2;
```

### AbstractClassNeverImplemented
Abstract class `NonFragmentableDataPageReader` has no concrete subclass
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/datapage/NonFragmentableDataPageReader.java`
#### Snippet
```java
 * Contains logic for reading values (that cannot be fragmented, that it, occupy more than one page) from page memory data pages.
 */
public abstract class NonFragmentableDataPageReader<T> {
    private final PageMemory pageMemory;
    private final int groupId;
```

### AbstractClassNeverImplemented
Abstract class `DirectConfigurationProxy` has no concrete subclass
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/direct/DirectConfigurationProxy.java`
#### Snippet
```java
 * {@link DirectPropertyProxy} implementation for leaves.
 */
public abstract class DirectConfigurationProxy<VIEWT, CHANGET extends VIEWT>
        extends DirectPropertyProxy<VIEWT>
        implements ConfigurationTree<VIEWT, CHANGET> {
```

### AbstractClassNeverImplemented
Abstract class `BusyRocksIteratorAdapter` has no concrete subclass
in `modules/rocksdb-common/src/main/java/org/apache/ignite/internal/rocksdb/BusyRocksIteratorAdapter.java`
#### Snippet
```java
 * Adapter from a {@link RocksIterator} to a {@link Cursor} that also handles stopping of the storage.
 */
public abstract class BusyRocksIteratorAdapter<T> extends RocksIteratorAdapter<T> {
    /** Busy lock. */
    private final IgniteSpinBusyLock busyLock;
```

## RuleId[id=BoundedWildcard]
### BoundedWildcard
Can generalize to `? extends List`
in `modules/api/src/main/java/org/apache/ignite/sql/BatchedArguments.java`
#### Snippet
```java
     * @return Batch query arguments.
     */
    public static BatchedArguments of(List<List<Object>> args) {
        BatchedArguments arguments = new BatchedArguments();

```

### BoundedWildcard
Can generalize to `? super Transaction`
in `modules/api/src/main/java/org/apache/ignite/tx/IgniteTransactions.java`
#### Snippet
```java
     */
    default <T> CompletableFuture<T> runInTransactionAsync(
            Function<Transaction, CompletableFuture<T>> clo,
            @Nullable TransactionOptions options) {
        Objects.requireNonNull(clo);
```

### BoundedWildcard
Can generalize to `? extends CompletableFuture`
in `modules/api/src/main/java/org/apache/ignite/tx/IgniteTransactions.java`
#### Snippet
```java
     */
    default <T> CompletableFuture<T> runInTransactionAsync(
            Function<Transaction, CompletableFuture<T>> clo,
            @Nullable TransactionOptions options) {
        Objects.requireNonNull(clo);
```

### BoundedWildcard
Can generalize to `? super Transaction`
in `modules/api/src/main/java/org/apache/ignite/tx/IgniteTransactions.java`
#### Snippet
```java
     * @throws TransactionException If a transaction can't be finished successfully.
     */
    default void runInTransaction(Consumer<Transaction> clo, @Nullable TransactionOptions options) throws TransactionException {
        Objects.requireNonNull(clo);

```

### BoundedWildcard
Can generalize to `? super Transaction`
in `modules/api/src/main/java/org/apache/ignite/tx/IgniteTransactions.java`
#### Snippet
```java
     * @throws TransactionException If a transaction can't be finished successfully.
     */
    default <T> T runInTransaction(Function<Transaction, T> clo, @Nullable TransactionOptions options) throws TransactionException {
        Objects.requireNonNull(clo);

```

### BoundedWildcard
Can generalize to `? super String`
in `modules/api/src/main/java/org/apache/ignite/table/TupleImpl.java`
#### Snippet
```java
     * @param columnValues  List of columns values.
     */
    private TupleImpl(Map<String, Integer> columnMapping, List<String> columnNames, List<Object> columnValues) {
        this.colMapping = columnMapping;
        this.colNames = columnNames;
```

### BoundedWildcard
Can generalize to `? super String`
in `modules/cli/src/main/java/org/apache/ignite/internal/cli/sql/SqlQueryResult.java`
#### Snippet
```java
     */
    public TerminalOutput getResult(TableDecorator tableDecorator,
                                    Decorator<String, TerminalOutput> messageDecorator) {
        if (table != null) {
            return tableDecorator.decorate(table);
```

### BoundedWildcard
Can generalize to `? extends T`
in `modules/cli/src/main/java/org/apache/ignite/internal/cli/core/call/AbstractCallExecutionPipeline.java`
#### Snippet
```java
    }

    int handleResult(CallOutput<T> callOutput) {
        if (callOutput.hasError()) {
            return handleException(callOutput.errorCause());
```

### BoundedWildcard
Can generalize to `? super I`
in `modules/cli/src/main/java/org/apache/ignite/internal/cli/core/call/SingleCallExecutionPipeline.java`
#### Snippet
```java

    SingleCallExecutionPipeline(
            Call<I, T> call,
            PrintWriter output,
            PrintWriter errOutput,
```

### BoundedWildcard
Can generalize to `? super ProgressTracker`
in `modules/cli/src/main/java/org/apache/ignite/internal/cli/core/call/AsyncCallExecutionPipeline.java`
#### Snippet
```java

    AsyncCallExecutionPipeline(
            Function<ProgressTracker, AsyncCall<I, T>> callFactory,
            ProgressBarBuilder progressBarBuilder,
            PrintWriter output,
```

### BoundedWildcard
Can generalize to `? extends AsyncCall`
in `modules/cli/src/main/java/org/apache/ignite/internal/cli/core/call/AsyncCallExecutionPipeline.java`
#### Snippet
```java

    AsyncCallExecutionPipeline(
            Function<ProgressTracker, AsyncCall<I, T>> callFactory,
            ProgressBarBuilder progressBarBuilder,
            PrintWriter output,
```

### BoundedWildcard
Can generalize to `? super String`
in `modules/cli/src/main/java/org/apache/ignite/internal/cli/core/flow/question/QuestionAnswer.java`
#### Snippet
```java
    private final AnswerAction<I, O> action;

    public QuestionAnswer(Predicate<String> answerTester, AnswerAction<I, O> action) {
        this.answer = answerTester;
        this.action = action;
```

### BoundedWildcard
Can generalize to `? super I`
in `modules/cli/src/main/java/org/apache/ignite/internal/cli/core/flow/question/QuestionAnswer.java`
#### Snippet
```java
    private final AnswerAction<I, O> action;

    public QuestionAnswer(Predicate<String> answerTester, AnswerAction<I, O> action) {
        this.answer = answerTester;
        this.action = action;
```

### BoundedWildcard
Can generalize to `? extends O`
in `modules/cli/src/main/java/org/apache/ignite/internal/cli/core/flow/question/QuestionAnswer.java`
#### Snippet
```java
    private final AnswerAction<I, O> action;

    public QuestionAnswer(Predicate<String> answerTester, AnswerAction<I, O> action) {
        this.answer = answerTester;
        this.action = action;
```

### BoundedWildcard
Can generalize to `? extends QuestionAnswer`
in `modules/cli/src/main/java/org/apache/ignite/internal/cli/core/flow/question/QuestionAsker.java`
#### Snippet
```java
     * @return value of answer.
     */
    public <I, O> O askQuestion(String question, I input, List<QuestionAnswer<I, O>> answers) {
        String userAnswer = readWriter.readAnswer(question);

```

### BoundedWildcard
Can generalize to `? extends DynamicCompleter`
in `modules/cli/src/main/java/org/apache/ignite/internal/cli/core/repl/completer/LazyDynamicCompleter.java`
#### Snippet
```java

    /** Default constructor. Supplier will be called once it is needed. */
    public LazyDynamicCompleter(Supplier<DynamicCompleter> delegateInitializer) {
        this.delegateInitializer = delegateInitializer;
    }
```

### BoundedWildcard
Can generalize to `? extends O`
in `modules/cli/src/main/java/org/apache/ignite/internal/cli/core/flow/builder/Flows.java`
#### Snippet
```java
     * @return new {@link FlowBuilder}.
     */
    public static <I, O> FlowBuilder<I, O> acceptQuestion(String question, Supplier<O> onAccept) {
        return question(question,
                List.of(new AcceptedQuestionAnswer<>((a, i) -> onAccept.get()),
```

### BoundedWildcard
Can generalize to `? extends T`
in `modules/cli/src/main/java/org/apache/ignite/internal/cli/core/flow/builder/Flows.java`
#### Snippet
```java
    }

    private static <T> Flowable<T> fromOutput(CallOutput<T> output) {
        return DefaultFlowable
                .<T>builder()
```

### BoundedWildcard
Can generalize to `? super CIT`
in `modules/cli/src/main/java/org/apache/ignite/internal/cli/core/flow/builder/Flows.java`
#### Snippet
```java
     * @return new {@link Flow} will call action.
     */
    public static <I, CIT extends CallInput, T> Flow<I, T> fromCall(Call<CIT, T> call, Function<I, CIT> mapper) {
        return flowable -> fromOutput(call.execute(mapper.apply(flowable.value())));
    }
```

### BoundedWildcard
Can generalize to `? super I`
in `modules/cli/src/main/java/org/apache/ignite/internal/cli/core/flow/builder/Flows.java`
#### Snippet
```java
     * @return new {@link Flow} will call action.
     */
    public static <I, CIT extends CallInput, T> Flow<I, T> fromCall(Call<CIT, T> call, Function<I, CIT> mapper) {
        return flowable -> fromOutput(call.execute(mapper.apply(flowable.value())));
    }
```

### BoundedWildcard
Can generalize to `? extends CIT`
in `modules/cli/src/main/java/org/apache/ignite/internal/cli/core/flow/builder/Flows.java`
#### Snippet
```java
     * @return new {@link Flow} will call action.
     */
    public static <I, CIT extends CallInput, T> Flow<I, T> fromCall(Call<CIT, T> call, Function<I, CIT> mapper) {
        return flowable -> fromOutput(call.execute(mapper.apply(flowable.value())));
    }
```

### BoundedWildcard
Can generalize to `? super I`
in `modules/cli/src/main/java/org/apache/ignite/internal/cli/core/flow/builder/Flows.java`
#### Snippet
```java
     * @return new {@link Flow} with action {@param function}.
     */
    public static <I, O> Flow<I, O> mono(Function<I, O> function) {
        return input -> Flowable.process(() -> function.apply(input.value()));
    }
```

### BoundedWildcard
Can generalize to `? extends O`
in `modules/cli/src/main/java/org/apache/ignite/internal/cli/core/flow/builder/Flows.java`
#### Snippet
```java
     * @return new {@link Flow} with action {@param function}.
     */
    public static <I, O> Flow<I, O> mono(Function<I, O> function) {
        return input -> Flowable.process(() -> function.apply(input.value()));
    }
```

### BoundedWildcard
Can generalize to `? super I`
in `modules/cli/src/main/java/org/apache/ignite/internal/cli/core/flow/builder/Flows.java`
#### Snippet
```java
     * @return new {@link Flow} will call action.
     */
    public static <I extends CallInput, T> Flow<I, T> fromCall(Call<I, T> call) {
        return flowable -> fromOutput(call.execute(flowable.value()));
    }
```

### BoundedWildcard
Can generalize to `? super O`
in `modules/cli/src/main/java/org/apache/ignite/internal/cli/core/flow/builder/FlowBuilderImpl.java`
#### Snippet
```java

    @Override
    public <QT> FlowBuilder<I, QT> question(Function<O, String> questionText, List<QuestionAnswer<O, QT>> answers) {
        return then(input -> Flowable.success(
                QuestionAskerFactory.newQuestionAsker().askQuestion(questionText.apply(input.value()), input.value(), answers)));
```

### BoundedWildcard
Can generalize to `? super Class`
in `modules/cli/src/main/java/org/apache/ignite/internal/cli/core/flow/builder/FlowBuilderImpl.java`
#### Snippet
```java
     * @return input flowable
     */
    private Flowable<O> printResult(Flowable<O> input, Function<Class<O>, Decorator<O, TerminalOutput>> decoratorProvider) {
        if (input.hasResult()) {
            // Workaround for the scroll truncation issue in windows terminal
```

### BoundedWildcard
Can generalize to `? extends Decorator`
in `modules/cli/src/main/java/org/apache/ignite/internal/cli/core/flow/builder/FlowBuilderImpl.java`
#### Snippet
```java
     * @return input flowable
     */
    private Flowable<O> printResult(Flowable<O> input, Function<Class<O>, Decorator<O, TerminalOutput>> decoratorProvider) {
        if (input.hasResult()) {
            // Workaround for the scroll truncation issue in windows terminal
```

### BoundedWildcard
Can generalize to `? super O`
in `modules/cli/src/main/java/org/apache/ignite/internal/cli/core/flow/builder/FlowBuilderImpl.java`
#### Snippet
```java

    @Override
    public <OT> FlowBuilder<I, O> ifThen(Predicate<O> tester, Flow<O, OT> flow) {
        return then(input -> {
            if (tester.test(input.value())) {
```

### BoundedWildcard
Can generalize to `? super O`
in `modules/cli/src/main/java/org/apache/ignite/internal/cli/core/flow/builder/FlowBuilderImpl.java`
#### Snippet
```java

    @Override
    public <OT> FlowBuilder<I, OT> flatMap(Function<O, FlowBuilder<O, OT>> mapper) {
        return then(it -> mapper.apply(it.value()).build().start(it));
    }
```

### BoundedWildcard
Can generalize to `? extends FlowBuilder`
in `modules/cli/src/main/java/org/apache/ignite/internal/cli/core/flow/builder/FlowBuilderImpl.java`
#### Snippet
```java

    @Override
    public <OT> FlowBuilder<I, OT> flatMap(Function<O, FlowBuilder<O, OT>> mapper) {
        return then(it -> mapper.apply(it.value()).build().start(it));
    }
```

### BoundedWildcard
Can generalize to `? super String`
in `modules/cli/src/main/java/org/apache/ignite/internal/cli/core/repl/completer/hocon/HoconDynamicCompleter.java`
#### Snippet
```java


    private void walkAndAdd(String keyPrefix, Set<String> keySet, List<String> result) {
        keySet.forEach(key -> {
            if (!leafs.contains(keyPrefix + key)) {
```

### BoundedWildcard
Can generalize to `? super Boolean`
in `modules/cli/src/main/java/org/apache/ignite/internal/cli/core/exception/handler/EndOfFileExceptionHandler.java`
#### Snippet
```java
     * @param endAction handle action.
     */
    public EndOfFileExceptionHandler(Consumer<Boolean> endAction) {
        this.endAction = endAction;
    }
```

### BoundedWildcard
Can generalize to `? super Throwable`
in `modules/cli/src/main/java/org/apache/ignite/internal/cli/core/exception/ExceptionHandlers.java`
#### Snippet
```java
    }

    public ExceptionHandlers(ExceptionHandler<Throwable> defaultHandler) {
        this.defaultHandler = defaultHandler;
    }
```

### BoundedWildcard
Can generalize to `? extends CompleterFilter`
in `modules/cli/src/main/java/org/apache/ignite/internal/cli/core/repl/executor/IgnitePicocliCommands.java`
#### Snippet
```java

    /** Default constructor. */
    public IgnitePicocliCommands(CommandLine cmd, DynamicCompleterRegistry completerRegistry, List<CompleterFilter> completerFilters) {
        this.cmd = cmd;
        this.completerFilters = completerFilters;
```

### BoundedWildcard
Can generalize to `? super Candidate`
in `modules/cli/src/main/java/org/apache/ignite/internal/cli/commands/sql/SqlCompleter.java`
#### Snippet
```java
    }

    private static void addCandidate(String string, List<Candidate> candidates) {
        candidates.add(new Candidate(string));
        candidates.add(new Candidate(string.toLowerCase()));
```

### BoundedWildcard
Can generalize to `? extends T`
in `modules/core/src/main/java/org/apache/ignite/internal/util/FilteringIterator.java`
#### Snippet
```java
     */
    public FilteringIterator(
            @NotNull Iterator<T> delegate,
            @NotNull Predicate<T> pred
    ) {
```

### BoundedWildcard
Can generalize to `? super T`
in `modules/core/src/main/java/org/apache/ignite/internal/util/FilteringIterator.java`
#### Snippet
```java
    public FilteringIterator(
            @NotNull Iterator<T> delegate,
            @NotNull Predicate<T> pred
    ) {
        this.delegate = delegate;
```

### BoundedWildcard
Can generalize to `? extends @Nullable T`
in `modules/core/src/main/java/org/apache/ignite/internal/util/Lazy.java`
#### Snippet
```java
     * @param supplier A supplier of the value.
     */
    public Lazy(Supplier<@Nullable T> supplier) {
        this.supplier = supplier;
    }
```

### BoundedWildcard
Can generalize to `? extends T`
in `modules/core/src/main/java/org/apache/ignite/internal/util/CursorUtils.java`
#### Snippet
```java
        private final Function<T, U> mapper;

        MapCursor(Cursor<T> cursor, Function<T, U> mapper) {
            this.cursor = cursor;
            this.mapper = mapper;
```

### BoundedWildcard
Can generalize to `? super T`
in `modules/core/src/main/java/org/apache/ignite/internal/util/CursorUtils.java`
#### Snippet
```java
        private final Function<T, U> mapper;

        MapCursor(Cursor<T> cursor, Function<T, U> mapper) {
            this.cursor = cursor;
            this.mapper = mapper;
```

### BoundedWildcard
Can generalize to `? extends U`
in `modules/core/src/main/java/org/apache/ignite/internal/util/CursorUtils.java`
#### Snippet
```java
        private final Function<T, U> mapper;

        MapCursor(Cursor<T> cursor, Function<T, U> mapper) {
            this.cursor = cursor;
            this.mapper = mapper;
```

### BoundedWildcard
Can generalize to `? extends TinT`
in `modules/core/src/main/java/org/apache/ignite/internal/util/TransformingIterator.java`
#### Snippet
```java
     * TODO Documentation https://issues.apache.org/jira/browse/IGNITE-15859
     */
    public TransformingIterator(Iterator<TinT> delegate, Function<TinT, ToutT> transformation) {
        this.delegate = delegate;
        this.transformation = transformation;
```

### BoundedWildcard
Can generalize to `? super TinT`
in `modules/core/src/main/java/org/apache/ignite/internal/util/TransformingIterator.java`
#### Snippet
```java
     * TODO Documentation https://issues.apache.org/jira/browse/IGNITE-15859
     */
    public TransformingIterator(Iterator<TinT> delegate, Function<TinT, ToutT> transformation) {
        this.delegate = delegate;
        this.transformation = transformation;
```

### BoundedWildcard
Can generalize to `? extends ToutT`
in `modules/core/src/main/java/org/apache/ignite/internal/util/TransformingIterator.java`
#### Snippet
```java
     * TODO Documentation https://issues.apache.org/jira/browse/IGNITE-15859
     */
    public TransformingIterator(Iterator<TinT> delegate, Function<TinT, ToutT> transformation) {
        this.delegate = delegate;
        this.transformation = transformation;
```

### BoundedWildcard
Can generalize to `? extends Publisher`
in `modules/core/src/main/java/org/apache/ignite/internal/util/SubscriptionUtils.java`
#### Snippet
```java
     * @return The publisher will combine all of the passed sources into a single one with sorting guaranties.
     */
    public static <T> Publisher<T> orderedMerge(Comparator<T> comparator, int prefetch, Iterator<Publisher<? extends T>> source) {
        List<Publisher<? extends T>> pubList = new ArrayList<>();
        source.forEachRemaining(pubList::add);
```

### BoundedWildcard
Can generalize to `? extends IgniteBiTuple`
in `modules/core/src/main/java/org/apache/ignite/internal/util/IgniteConcurrentMultiPairQueue.java`
#### Snippet
```java
     * @param items Items.
     */
    public IgniteConcurrentMultiPairQueue(Collection<IgniteBiTuple<K, V[]>> items) {
        int pairCnt = (int) items.stream().map(Map.Entry::getValue).filter(k -> k.length > 0).count();

```

### BoundedWildcard
Can generalize to `? super K`
in `modules/core/src/main/java/org/apache/ignite/internal/util/IgniteConcurrentMultiPairQueue.java`
#### Snippet
```java
     * @return {@code true} if not empty result, or {@code false} if this queue is empty.
     */
    public boolean next(Result<K, V> res) {
        int absPos = pos.getAndIncrement();

```

### BoundedWildcard
Can generalize to `? super V`
in `modules/core/src/main/java/org/apache/ignite/internal/util/IgniteConcurrentMultiPairQueue.java`
#### Snippet
```java
     * @return {@code true} if not empty result, or {@code false} if this queue is empty.
     */
    public boolean next(Result<K, V> res) {
        int absPos = pos.getAndIncrement();

```

### BoundedWildcard
Can generalize to `? extends Publisher`
in `modules/core/src/main/java/org/apache/ignite/internal/util/subscription/ConcatenatedPublisher.java`
#### Snippet
```java
        ConcatenatedSubscriber(
                Subscriber<? super T> downstream,
                Iterator<Publisher<? extends T>> sources
        ) {
            this.downstream = downstream;
```

### BoundedWildcard
Can generalize to `? extends T`
in `modules/core/src/main/java/org/apache/ignite/internal/util/CollectionUtils.java`
#### Snippet
```java
     * @return Immutable set of elements of the first without the second.
     */
    public static <T> Set<T> difference(@Nullable Set<T> a, @Nullable Set<T> b) {
        if (nullOrEmpty(a)) {
            return Set.of();
```

### BoundedWildcard
Can generalize to `? extends T`
in `modules/core/src/main/java/org/apache/ignite/internal/util/CollectionUtils.java`
#### Snippet
```java
     */
    @SafeVarargs
    public static <T> Set<T> union(@Nullable Set<T> set, @Nullable T... ts) {
        if (nullOrEmpty(set)) {
            return ts == null || ts.length == 0 ? Set.of() : Set.of(ts);
```

### BoundedWildcard
Can generalize to `? super UUID`
in `modules/core/src/main/java/org/apache/ignite/internal/util/ExceptionUtils.java`
#### Snippet
```java
     */
    private static <T extends Exception> T withCauseInternal(
            IgniteTetraFunction<UUID, Integer, String, Throwable, T> supplier,
            int defaultCode,
            Throwable t
```

### BoundedWildcard
Can generalize to `? super Integer`
in `modules/core/src/main/java/org/apache/ignite/internal/util/ExceptionUtils.java`
#### Snippet
```java
     */
    private static <T extends Exception> T withCauseInternal(
            IgniteTetraFunction<UUID, Integer, String, Throwable, T> supplier,
            int defaultCode,
            Throwable t
```

### BoundedWildcard
Can generalize to `? super String`
in `modules/core/src/main/java/org/apache/ignite/internal/util/ExceptionUtils.java`
#### Snippet
```java
     */
    private static <T extends Exception> T withCauseInternal(
            IgniteTetraFunction<UUID, Integer, String, Throwable, T> supplier,
            int defaultCode,
            Throwable t
```

### BoundedWildcard
Can generalize to `? super Throwable`
in `modules/core/src/main/java/org/apache/ignite/internal/util/ExceptionUtils.java`
#### Snippet
```java
     */
    private static <T extends Exception> T withCauseInternal(
            IgniteTetraFunction<UUID, Integer, String, Throwable, T> supplier,
            int defaultCode,
            Throwable t
```

### BoundedWildcard
Can generalize to `? super UUID`
in `modules/core/src/main/java/org/apache/ignite/internal/util/ExceptionUtils.java`
#### Snippet
```java
     * @return New exception with the given cause.
     */
    public static <T extends Exception> T withCauseAndCode(IgniteTriFunction<UUID, Integer, Throwable, T> supplier, int code, Throwable t) {
        return withCauseInternal((traceId, c, message, cause) -> supplier.apply(traceId, code, t), code, t);
    }
```

### BoundedWildcard
Can generalize to `? super Integer`
in `modules/core/src/main/java/org/apache/ignite/internal/util/ExceptionUtils.java`
#### Snippet
```java
     * @return New exception with the given cause.
     */
    public static <T extends Exception> T withCauseAndCode(IgniteTriFunction<UUID, Integer, Throwable, T> supplier, int code, Throwable t) {
        return withCauseInternal((traceId, c, message, cause) -> supplier.apply(traceId, code, t), code, t);
    }
```

### BoundedWildcard
Can generalize to `? super Throwable`
in `modules/core/src/main/java/org/apache/ignite/internal/util/ExceptionUtils.java`
#### Snippet
```java
     * @return New exception with the given cause.
     */
    public static <T extends Exception> T withCauseAndCode(IgniteTriFunction<UUID, Integer, Throwable, T> supplier, int code, Throwable t) {
        return withCauseInternal((traceId, c, message, cause) -> supplier.apply(traceId, code, t), code, t);
    }
```

### BoundedWildcard
Can generalize to `? super UUID`
in `modules/core/src/main/java/org/apache/ignite/internal/util/ExceptionUtils.java`
#### Snippet
```java
     */
    public static <T extends Exception> T withCauseAndCode(
            IgniteTetraFunction<UUID, Integer, String, Throwable, T> supplier,
            int code,
            String message,
```

### BoundedWildcard
Can generalize to `? super Integer`
in `modules/core/src/main/java/org/apache/ignite/internal/util/ExceptionUtils.java`
#### Snippet
```java
     */
    public static <T extends Exception> T withCauseAndCode(
            IgniteTetraFunction<UUID, Integer, String, Throwable, T> supplier,
            int code,
            String message,
```

### BoundedWildcard
Can generalize to `? super String`
in `modules/core/src/main/java/org/apache/ignite/internal/util/ExceptionUtils.java`
#### Snippet
```java
     */
    public static <T extends Exception> T withCauseAndCode(
            IgniteTetraFunction<UUID, Integer, String, Throwable, T> supplier,
            int code,
            String message,
```

### BoundedWildcard
Can generalize to `? super Throwable`
in `modules/core/src/main/java/org/apache/ignite/internal/util/ExceptionUtils.java`
#### Snippet
```java
     */
    public static <T extends Exception> T withCauseAndCode(
            IgniteTetraFunction<UUID, Integer, String, Throwable, T> supplier,
            int code,
            String message,
```

### BoundedWildcard
Can generalize to `? super UUID`
in `modules/core/src/main/java/org/apache/ignite/internal/util/ExceptionUtils.java`
#### Snippet
```java
     * @return New exception with the given cause.
     */
    public static <T extends Exception> T withCause(IgniteTriFunction<UUID, Integer, Throwable, T> supplier, int defaultCode, Throwable t) {
        return withCauseInternal((traceId, code, message, cause) -> supplier.apply(traceId, code, t), defaultCode, t);
    }
```

### BoundedWildcard
Can generalize to `? super Integer`
in `modules/core/src/main/java/org/apache/ignite/internal/util/ExceptionUtils.java`
#### Snippet
```java
     * @return New exception with the given cause.
     */
    public static <T extends Exception> T withCause(IgniteTriFunction<UUID, Integer, Throwable, T> supplier, int defaultCode, Throwable t) {
        return withCauseInternal((traceId, code, message, cause) -> supplier.apply(traceId, code, t), defaultCode, t);
    }
```

### BoundedWildcard
Can generalize to `? super Throwable`
in `modules/core/src/main/java/org/apache/ignite/internal/util/ExceptionUtils.java`
#### Snippet
```java
     * @return New exception with the given cause.
     */
    public static <T extends Exception> T withCause(IgniteTriFunction<UUID, Integer, Throwable, T> supplier, int defaultCode, Throwable t) {
        return withCauseInternal((traceId, code, message, cause) -> supplier.apply(traceId, code, t), defaultCode, t);
    }
```

### BoundedWildcard
Can generalize to `? super UUID`
in `modules/core/src/main/java/org/apache/ignite/internal/util/ExceptionUtils.java`
#### Snippet
```java
     */
    public static <T extends Exception> T withCause(
            IgniteTetraFunction<UUID, Integer, String, Throwable, T> supplier,
            int defaultCode,
            String message,
```

### BoundedWildcard
Can generalize to `? super Integer`
in `modules/core/src/main/java/org/apache/ignite/internal/util/ExceptionUtils.java`
#### Snippet
```java
     */
    public static <T extends Exception> T withCause(
            IgniteTetraFunction<UUID, Integer, String, Throwable, T> supplier,
            int defaultCode,
            String message,
```

### BoundedWildcard
Can generalize to `? super String`
in `modules/core/src/main/java/org/apache/ignite/internal/util/ExceptionUtils.java`
#### Snippet
```java
     */
    public static <T extends Exception> T withCause(
            IgniteTetraFunction<UUID, Integer, String, Throwable, T> supplier,
            int defaultCode,
            String message,
```

### BoundedWildcard
Can generalize to `? super Throwable`
in `modules/core/src/main/java/org/apache/ignite/internal/util/ExceptionUtils.java`
#### Snippet
```java
     */
    public static <T extends Exception> T withCause(
            IgniteTetraFunction<UUID, Integer, String, Throwable, T> supplier,
            int defaultCode,
            String message,
```

### BoundedWildcard
Can generalize to `? extends T`
in `modules/core/src/main/java/org/apache/ignite/internal/future/OrderingFuture.java`
#### Snippet
```java
     * @return Adapting future.
     */
    public static <T> OrderingFuture<T> adapt(CompletableFuture<T> adaptee) {
        var future = new OrderingFuture<T>();

```

### BoundedWildcard
Can generalize to `? super U`
in `modules/core/src/main/java/org/apache/ignite/internal/future/OrderingFuture.java`
#### Snippet
```java
        private final Function<? super T, ? extends CompletableFuture<U>> mapper;

        private ThenComposeToCompletable(CompletableFuture<U> resultFuture, Function<? super T, ? extends CompletableFuture<U>> mapper) {
            this.resultFuture = resultFuture;
            this.mapper = mapper;
```

### BoundedWildcard
Can generalize to `? extends IgniteWorker`
in `modules/core/src/main/java/org/apache/ignite/internal/util/IgniteUtils.java`
#### Snippet
```java
     * @param log Logger.
     */
    public static void awaitForWorkersStop(Collection<IgniteWorker> workers, boolean cancel, @Nullable IgniteLogger log) {
        if (cancel) {
            workers.forEach(IgniteWorker::cancel);
```

### BoundedWildcard
Can generalize to `? super T`
in `modules/core/src/main/java/org/apache/ignite/internal/util/IgniteUtils.java`
#### Snippet
```java
     * @return Optional containing element (if present).
     */
    public static <T> Optional<T> findAny(Collection<T> collection, @Nullable Predicate<T> predicate) {
        if (!collection.isEmpty()) {
            for (Iterator<T> it = collection.iterator(); it.hasNext(); ) {
```

### BoundedWildcard
Can generalize to `? extends T`
in `modules/core/src/main/java/org/apache/ignite/internal/util/IgniteUtils.java`
#### Snippet
```java
     * @return Filtered list.
     */
    public static <T> List<T> filter(Collection<T> collection, Predicate<T> predicate) {
        List<T> result = new ArrayList<>();

```

### BoundedWildcard
Can generalize to `? super T`
in `modules/core/src/main/java/org/apache/ignite/internal/util/IgniteUtils.java`
#### Snippet
```java
     * @return Filtered list.
     */
    public static <T> List<T> filter(Collection<T> collection, Predicate<T> predicate) {
        List<T> result = new ArrayList<>();

```

### BoundedWildcard
Can generalize to `? extends T`
in `modules/core/src/main/java/org/apache/ignite/internal/util/IgniteUtils.java`
#### Snippet
```java
     * @param <T> Future's result type.
     */
    public static <T> void cancelOrConsume(CompletableFuture<T> future, Consumer<T> consumer) {
        future.cancel(true);

```

### BoundedWildcard
Can generalize to `? super T`
in `modules/core/src/main/java/org/apache/ignite/internal/util/IgniteUtils.java`
#### Snippet
```java
     * @param <T> Future's result type.
     */
    public static <T> void cancelOrConsume(CompletableFuture<T> future, Consumer<T> consumer) {
        future.cancel(true);

```

### BoundedWildcard
Can generalize to `? super String`
in `modules/core/src/main/java/org/apache/ignite/internal/util/IgniteUtils.java`
#### Snippet
```java
            String clsName,
            @Nullable ClassLoader ldr,
            Predicate<String> clsFilter
    ) throws ClassNotFoundException {
        assert clsName != null;
```

### BoundedWildcard
Can generalize to `? extends WithSystemProperty`
in `modules/core/src/testFixtures/java/org/apache/ignite/internal/testframework/SystemPropertiesExtension.java`
#### Snippet
```java
     * @return List of the previous property values.
     */
    private static List<Property> replaceProperties(List<WithSystemProperty> newProps) {
        // List of system properties to set when test is finished.
        var oldProps = new ArrayList<Property>(newProps.size());
```

### BoundedWildcard
Can generalize to `? extends T`
in `modules/core/src/testFixtures/java/org/apache/ignite/internal/testframework/flow/TestFlowUtils.java`
#### Snippet
```java
     * Creates a Publisher that provides data from a given cursor.
     */
    public static <T> Publisher<T> fromCursor(Cursor<T> cursor) {
        return subscriber -> subscriber.onSubscribe(new Subscription() {
            private long demand;
```

### BoundedWildcard
Can generalize to `? super T`
in `modules/core/src/main/java/org/apache/ignite/internal/causality/VersionedValue.java`
#### Snippet
```java
    public CompletableFuture<T> update(
            long causalityToken,
            BiFunction<T, Throwable, CompletableFuture<T>> updater
    ) {
        long actualToken0 = this.actualToken;
```

### BoundedWildcard
Can generalize to `? super Throwable`
in `modules/core/src/main/java/org/apache/ignite/internal/causality/VersionedValue.java`
#### Snippet
```java
    public CompletableFuture<T> update(
            long causalityToken,
            BiFunction<T, Throwable, CompletableFuture<T>> updater
    ) {
        long actualToken0 = this.actualToken;
```

### BoundedWildcard
Can generalize to `? extends CompletableFuture`
in `modules/core/src/main/java/org/apache/ignite/internal/causality/VersionedValue.java`
#### Snippet
```java
    public CompletableFuture<T> update(
            long causalityToken,
            BiFunction<T, Throwable, CompletableFuture<T>> updater
    ) {
        long actualToken0 = this.actualToken;
```

### BoundedWildcard
Can generalize to `? extends JdbcColumnMeta`
in `modules/jdbc/src/main/java/org/apache/ignite/internal/jdbc/JdbcResultSetMetadata.java`
#### Snippet
```java
     * @param meta Metadata.
     */
    JdbcResultSetMetadata(List<JdbcColumnMeta> meta) {
        assert meta != null;

```

### BoundedWildcard
Can generalize to `? extends T`
in `modules/core/src/testFixtures/java/org/apache/ignite/internal/testframework/IgniteTestUtils.java`
#### Snippet
```java
     * @return Future with task result.
     */
    public static <T> CompletableFuture<T> runAsync(final Callable<T> task, String threadName) {
        final NamedThreadFactory thrFactory = new NamedThreadFactory(threadName, LOG);

```

### BoundedWildcard
Can generalize to `? extends Callable`
in `modules/core/src/testFixtures/java/org/apache/ignite/internal/testframework/IgniteTestUtils.java`
#### Snippet
```java
     * @throws Exception If failed.
     */
    public static long runMultiThreaded(Iterable<Callable<?>> calls, ThreadFactory threadFactory) throws Exception {
        Collection<Thread> threads = new ArrayList<>();

```

### BoundedWildcard
Can generalize to `? super Message`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/rpc/impl/IgniteRpcClient.java`
#### Snippet
```java
    }

    public void send(PeerId peerId, Object request, CompletableFuture<Message> fut, long timeout) {
        ClusterNode targetNode = service.topologyService().getByConsistentId(peerId.getConsistentId());

```

### BoundedWildcard
Can generalize to `? super String`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/rpc/impl/IgniteRpcClient.java`
#### Snippet
```java

    /** {@inheritDoc} */
    @Override public void blockMessages(BiPredicate<Object, String> predicate) {
        this.blockPred = predicate;
    }
```

### BoundedWildcard
Can generalize to `? super String`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/rpc/impl/IgniteRpcClient.java`
#### Snippet
```java

    /** {@inheritDoc} */
    @Override public void recordMessages(BiPredicate<Object, String> predicate) {
        this.recordPred = predicate;
    }
```

### BoundedWildcard
Can generalize to `? extends Closure`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/core/IteratorImpl.java`
#### Snippet
```java
    private RaftException error;

    public IteratorImpl(final StateMachine fsm, final LogManager logManager, final List<Closure> closures,
        final long firstClosureIndex, final long lastAppliedIndex, final long committedIndex,
        final AtomicLong applyingIndex, NodeOptions options) {
```

### BoundedWildcard
Can generalize to `? extends PeerId`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/conf/Configuration.java`
#### Snippet
```java
    }

    public void setPeers(final List<PeerId> peers) {
        this.peers.clear();
        for (final PeerId peer : peers) {
```

### BoundedWildcard
Can generalize to `? extends PeerId`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/conf/Configuration.java`
#### Snippet
```java
     * @return the total added count
     */
    public int addLearners(final Iterable<PeerId> learners) {
        int ret = 0;
        if (learners != null) {
```

### BoundedWildcard
Can generalize to `? extends PeerId`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/conf/Configuration.java`
#### Snippet
```java
    }

    public void appendPeers(final Collection<PeerId> set) {
        this.peers.addAll(set);
    }
```

### BoundedWildcard
Can generalize to `? extends PeerId`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/conf/Configuration.java`
#### Snippet
```java
     * @param learners learners
     */
    public Configuration(final Iterable<PeerId> conf, final Iterable<PeerId> learners) {
        Requires.requireNonNull(conf, "conf");
        for (final PeerId peer : conf) {
```

### BoundedWildcard
Can generalize to `? extends TaskClosure`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/core/FSMCallerImpl.java`
#### Snippet
```java
    }

    private void onTaskCommitted(final List<TaskClosure> closures) {
        for (int i = 0, size = closures.size(); i < size; i++) {
            final TaskClosure done = closures.get(i);
```

### BoundedWildcard
Can generalize to `? super String`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/core/CliServiceImpl.java`
#### Snippet
```java
    @Override
    public Status rebalance(final Set<String> balanceGroupIds, final Configuration conf,
        final Map<String, PeerId> rebalancedLeaderIds) {
        Requires.requireNonNull(balanceGroupIds, "Null balance group ids");
        Requires.requireTrue(!balanceGroupIds.isEmpty(), "Empty balance group ids");
```

### BoundedWildcard
Can generalize to `? super PeerId`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/core/CliServiceImpl.java`
#### Snippet
```java
    @Override
    public Status rebalance(final Set<String> balanceGroupIds, final Configuration conf,
        final Map<String, PeerId> rebalancedLeaderIds) {
        Requires.requireNonNull(balanceGroupIds, "Null balance group ids");
        Requires.requireTrue(!balanceGroupIds.isEmpty(), "Empty balance group ids");
```

### BoundedWildcard
Can generalize to `? extends PeerId`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/core/CliServiceImpl.java`
#### Snippet
```java
    }

    private void checkPeers(final Collection<PeerId> peers) {
        for (final PeerId peer : peers) {
            Requires.requireNonNull(peer, "Null peer in collection");
```

### BoundedWildcard
Can generalize to `? extends ByteBuffer`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/RecyclableByteBufferList.java`
#### Snippet
```java
     * @param buffers Buffers.
     */
    public static ByteString concatenate(List<ByteBuffer> buffers) {
        final ByteBuffer combined = ByteBuffer.allocate(buffers.stream().mapToInt(Buffer::remaining).sum());
        buffers.stream().forEach(b -> combined.put(b.duplicate()));
```

### BoundedWildcard
Can generalize to `? super T`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/SegmentList.java`
#### Snippet
```java
        }

        int removeFromFirstWhen(final Predicate<T> predicate) {
            int removed = 0;
            for (int i = this.offset; i < this.pos; i++) {
```

### BoundedWildcard
Can generalize to `? super T`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/SegmentList.java`
#### Snippet
```java
        }

        int removeFromLastWhen(final Predicate<T> predicate) {
            int removed = 0;
            for (int i = this.pos - 1; i >= this.offset; i--) {
```

### BoundedWildcard
Can generalize to `? super Timeout`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/timer/HashedWheelTimer.java`
#### Snippet
```java
         * Clear this bucket and return all not expired / cancelled {@link Timeout}s.
         */
        public void clearTimeouts(Set<Timeout> set) {
            for (; ; ) {
                HashedWheelTimeout timeout = pollTimeout();
```

### BoundedWildcard
Can generalize to `? extends Task`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/entity/Task.java`
#### Snippet
```java
     * @throws TimeoutException if timeout
     */
    public static List<Closure> joinAll(final List<Task> tasks, long timeoutMillis) throws InterruptedException,
        TimeoutException {
        final List<Closure> closures = new ArrayList<>(tasks.size());
```

### BoundedWildcard
Can generalize to `? extends Task`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/entity/Task.java`
#### Snippet
```java
     * @throws InterruptedException if the current thread is interrupted while waiting
     */
    public static List<Closure> joinAll(final List<Task> tasks) throws InterruptedException {
        final List<Closure> closures = new ArrayList<>(tasks.size());
        for (final Task t : tasks) {
```

### BoundedWildcard
Can generalize to `? extends UnfoundPeerId`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/entity/Ballot.java`
#### Snippet
```java
    }

    private UnfoundPeerId findPeer(final PeerId peerId, final List<UnfoundPeerId> peers, final int posHint) {
        if (posHint < 0 || posHint >= peers.size() || !peers.get(posHint).peerId.equals(peerId)) {
            for (final UnfoundPeerId ufp : peers) {
```

### BoundedWildcard
Can generalize to `? extends Counter`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/MetricReporter.java`
#### Snippet
```java
    }

    private void printCounter(final Map.Entry<String, Counter> entry) {
        this.output.printf(this.locale, "             count = %d%n", entry.getValue().getCount());
    }
```

### BoundedWildcard
Can generalize to `? extends LogEntry`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/storage/impl/EntryCountBudget.java`
#### Snippet
```java

    @Override
    public void onAppended(List<LogEntry> entries) {
        if (firstIndex == NO_INDEX) {
            firstIndex = entries.get(0).getId().getIndex();
```

### BoundedWildcard
Can generalize to `? extends LogEntry`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/storage/impl/VolatileLogStorage.java`
#### Snippet
```java

    @Override
    public int appendEntries(final List<LogEntry> entries) {
        if (entries == null || entries.isEmpty()) {
            return 0;
```

### BoundedWildcard
Can generalize to `? extends LogEntry`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/storage/impl/RocksDbSpillout.java`
#### Snippet
```java

    @Override
    public void appendEntries(List<LogEntry> entries) {
        if (entries == null || entries.isEmpty()) {
            return;
```

### BoundedWildcard
Can generalize to `? extends LogEntry`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/storage/impl/LocalLogStorage.java`
#### Snippet
```java

    @Override
    public int appendEntries(final List<LogEntry> entries) {
        if (entries == null || entries.isEmpty()) {
            return 0;
```

### BoundedWildcard
Can generalize to `? extends LogEntry`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/storage/impl/OnHeapLogs.java`
#### Snippet
```java

    @Override
    public void appendEntries(List<LogEntry> entries) {
        for (LogEntry entry : entries) {
            appendEntry(entry);
```

### BoundedWildcard
Can generalize to `? super Closure`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/closure/ClosureQueueImpl.java`
#### Snippet
```java

    @Override
    public long popClosureUntil(final long endIndex, final List<Closure> closures,
        final List<TaskClosure> taskClosures) {
        closures.clear();
```

### BoundedWildcard
Can generalize to `? super TaskClosure`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/closure/ClosureQueueImpl.java`
#### Snippet
```java
    @Override
    public long popClosureUntil(final long endIndex, final List<Closure> closures,
        final List<TaskClosure> taskClosures) {
        closures.clear();
        if (taskClosures != null) {
```

### BoundedWildcard
Can generalize to `? extends LogEntry`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/storage/impl/RocksDBLogStorage.java`
#### Snippet
```java

    @Override
    public int appendEntries(final List<LogEntry> entries) {
        if (entries == null || entries.isEmpty()) {
            return 0;
```

### BoundedWildcard
Can generalize to `? extends PeerId`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/core/NodeImpl.java`
#### Snippet
```java

    // in read_lock
    private List<PeerId> getAliveNodes(final Collection<PeerId> peers, final long monotonicNowMs) {
        final int leaderLeaseTimeoutMs = this.options.getLeaderLeaseTimeoutMs();
        final List<PeerId> alivePeers = new ArrayList<>();
```

### BoundedWildcard
Can generalize to `? extends PeerId`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/core/NodeImpl.java`
#### Snippet
```java
     * @param peerIds peer nodes in the same Raft group
     */
    private int getMaxPriorityOfNodes(final List<PeerId> peerIds) {
        Requires.requireNonNull(peerIds, "Null peer list");

```

### BoundedWildcard
Can generalize to `? extends PeerId`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/core/NodeImpl.java`
#### Snippet
```java
     * @return {@code True} if a majority of nodes are alive.
     */
    private boolean checkDeadNodes0(final List<PeerId> peers, final long monotonicNowMs, final boolean checkReplicator,
        final Configuration deadNodes) {
        final int leaderLeaseTimeoutMs = this.options.getLeaderLeaseTimeoutMs();
```

### BoundedWildcard
Can generalize to `? extends LogEntryAndClosure`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/core/NodeImpl.java`
#### Snippet
```java
    }

    private void executeApplyingTasks(final List<LogEntryAndClosure> tasks) {
        this.writeLock.lock();
        try {
```

### BoundedWildcard
Can generalize to `? extends PeerId`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/core/NodeImpl.java`
#### Snippet
```java
    }

    private void stopReplicator(final Collection<PeerId> keep, final Collection<PeerId> drop) {
        if (drop != null) {
            for (final PeerId peer : drop) {
```

### BoundedWildcard
Can generalize to `? extends PeerId`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/core/NodeImpl.java`
#### Snippet
```java
    }

    private void checkPeers(final List<PeerId> peers) {
        Requires.requireNonNull(peers, "Null peers");
        Requires.requireTrue(!peers.isEmpty(), "Empty peers");
```

### BoundedWildcard
Can generalize to `? extends RepeatedTimer`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/core/NodeImpl.java`
#### Snippet
```java
    }

    private void destroyAllTimers(final List<RepeatedTimer> timers) {
        for (final RepeatedTimer timer : timers) {
            timer.destroy();
```

### BoundedWildcard
Can generalize to `? extends PeerId`
in `modules/raft/src/main/java/org/apache/ignite/internal/raft/server/impl/RaftGroupEventsListenerAdapter.java`
#### Snippet
```java
    }

    private static Set<Peer> peerIdsToPeers(Collection<PeerId> ids) {
        return ids.stream()
                .map(id -> new Peer(id.getConsistentId(), id.getIdx()))
```

### BoundedWildcard
Can generalize to `? extends PeerId`
in `modules/raft/src/main/java/org/apache/ignite/internal/raft/server/impl/JraftServerImpl.java`
#### Snippet
```java
        }

        private static List<String> peersIdsToStrings(Collection<PeerId> peerIds) {
            return peerIds.stream().map(PeerId::toString).collect(toUnmodifiableList());
        }
```

### BoundedWildcard
Can generalize to `? extends T`
in `modules/core/src/main/java/org/apache/ignite/internal/tostring/IgniteToStringBuilder.java`
#### Snippet
```java
     */
    public static <T extends Number & Comparable<? super T>> String compact(
            Collection<T> col,
            Function<T, T> nextValFun
    ) {
```

### BoundedWildcard
Can generalize to `? super T`
in `modules/core/src/main/java/org/apache/ignite/internal/tostring/IgniteToStringBuilder.java`
#### Snippet
```java
    public static <T extends Number & Comparable<? super T>> String compact(
            Collection<T> col,
            Function<T, T> nextValFun
    ) {
        assert nonNull(col);
```

### BoundedWildcard
Can generalize to `? extends T`
in `modules/core/src/main/java/org/apache/ignite/internal/tostring/IgniteToStringBuilder.java`
#### Snippet
```java
    public static <T extends Number & Comparable<? super T>> String compact(
            Collection<T> col,
            Function<T, T> nextValFun
    ) {
        assert nonNull(col);
```

### BoundedWildcard
Can generalize to `? extends LogEntry`
in `modules/raft/src/main/java/org/apache/ignite/internal/raft/storage/impl/RocksDbSharedLogStorage.java`
#### Snippet
```java
    /** {@inheritDoc} */
    @Override
    public int appendEntries(List<LogEntry> entries) {
        if (entries == null || entries.isEmpty()) {
            return 0;
```

### BoundedWildcard
Can generalize to `? extends LogEntry`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/storage/impl/LogManagerImpl.java`
#### Snippet
```java

    @SuppressWarnings("NonAtomicOperationOnVolatileField")
    private boolean checkAndResolveConflict(final List<LogEntry> entries, final StableClosure done) {
        final LogEntry firstLogEntry = ArrayDeque.peekFirst(entries);
        if (firstLogEntry.getId().getIndex() == 0) {
```

### BoundedWildcard
Can generalize to `? extends Supplier`
in `modules/rest/src/main/java/org/apache/ignite/internal/rest/RestComponent.java`
#### Snippet
```java
     * Creates a new instance of REST module.
     */
    public RestComponent(List<Supplier<RestFactory>> restFactories, RestConfiguration restConfiguration) {
        this.restFactories = restFactories;
        this.restConfiguration = restConfiguration;
```

### BoundedWildcard
Can generalize to `? extends ClusterNode`
in `modules/rest/src/main/java/org/apache/ignite/internal/rest/cluster/TopologyController.java`
#### Snippet
```java
    }

    private static List<ClusterNodeDto> toClusterNodeDtos(Collection<ClusterNode> nodes) {
        return nodes.stream().map(TopologyController::toClusterNodeDto).collect(toList());
    }
```

### BoundedWildcard
Can generalize to `? extends AuthenticationProviderConfigDto`
in `modules/rest/src/main/java/org/apache/ignite/internal/rest/cluster/ClusterManagementController.java`
#### Snippet
```java
    }

    private List<AuthenticationProviderConfig> authnProviders(List<AuthenticationProviderConfigDto> providers) {
        if (providers == null) {
            return Collections.emptyList();
```

### BoundedWildcard
Can generalize to `? super BinaryRow`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/TableSchemaAwareIndexStorage.java`
#### Snippet
```java
            UUID indexId,
            IndexStorage storage,
            Function<BinaryRow, BinaryTuple> indexRowResolver
    ) {
        this.indexId = indexId;
```

### BoundedWildcard
Can generalize to `? extends BinaryTuple`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/TableSchemaAwareIndexStorage.java`
#### Snippet
```java
            UUID indexId,
            IndexStorage storage,
            Function<BinaryRow, BinaryTuple> indexRowResolver
    ) {
        this.indexId = indexId;
```

### BoundedWildcard
Can generalize to `? super BinaryRow`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/HashIndexLocker.java`
#### Snippet
```java
     */
    public HashIndexLocker(UUID indexId, boolean unique, LockManager lockManager,
            Function<BinaryRow, BinaryTuple> indexRowResolver) {
        this.indexId = indexId;
        this.modificationMode = unique ? LockMode.X : LockMode.IX;
```

### BoundedWildcard
Can generalize to `? extends BinaryTuple`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/HashIndexLocker.java`
#### Snippet
```java
     */
    public HashIndexLocker(UUID indexId, boolean unique, LockManager lockManager,
            Function<BinaryRow, BinaryTuple> indexRowResolver) {
        this.indexId = indexId;
        this.modificationMode = unique ? LockMode.X : LockMode.IX;
```

### BoundedWildcard
Can generalize to `? super BinaryRow`
in `modules/index/src/main/java/org/apache/ignite/internal/index/IndexManager.java`
#### Snippet
```java
            private final Function<BinaryRow, BinaryTuple> delegate;

            private VersionedConverter(int version, Function<BinaryRow, BinaryTuple> delegate) {
                this.version = version;
                this.delegate = delegate;
```

### BoundedWildcard
Can generalize to `? extends BinaryTuple`
in `modules/index/src/main/java/org/apache/ignite/internal/index/IndexManager.java`
#### Snippet
```java
            private final Function<BinaryRow, BinaryTuple> delegate;

            private VersionedConverter(int version, Function<BinaryRow, BinaryTuple> delegate) {
                this.version = version;
                this.delegate = delegate;
```

### BoundedWildcard
Can generalize to `? extends RaftGroupService`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/PartitionMover.java`
#### Snippet
```java
     * Constructor.
     */
    PartitionMover(IgniteSpinBusyLock busyLock, Supplier<RaftGroupService> raftGroupServiceSupplier) {
        this.busyLock = busyLock;
        this.raftGroupServiceSupplier = raftGroupServiceSupplier;
```

### BoundedWildcard
Can generalize to `? super PeerId`
in `modules/raft/src/testFixtures/java/org/apache/ignite/raft/jraft/core/TestCluster.java`
#### Snippet
```java
     * @throws InterruptedException
     */
    public void ensureSame(Predicate<PeerId> filter) throws InterruptedException {
        this.lock.lock();

```

### BoundedWildcard
Can generalize to `? extends TestPeer`
in `modules/raft/src/testFixtures/java/org/apache/ignite/raft/jraft/core/TestCluster.java`
#### Snippet
```java
        String name,
        String dataPath,
        List<TestPeer> peers,
        LinkedHashSet<TestPeer> learners,
        int electionTimeoutMs,
```

### BoundedWildcard
Can generalize to `? super NodeOptions`
in `modules/raft/src/testFixtures/java/org/apache/ignite/raft/jraft/core/TestCluster.java`
#### Snippet
```java
        LinkedHashSet<TestPeer> learners,
        int electionTimeoutMs,
        @Nullable Consumer<NodeOptions> optsClo,
        TestInfo testInfo
    ) {
```

### BoundedWildcard
Can generalize to `? extends TestPeer`
in `modules/raft/src/testFixtures/java/org/apache/ignite/raft/jraft/core/TestCluster.java`
#### Snippet
```java
    }

    public void setLearners(LinkedHashSet<TestPeer> learners) {
        this.learners = learners.stream().map(TestPeer::getPeerId).collect(Collectors.toCollection(LinkedHashSet::new));
    }
```

### BoundedWildcard
Can generalize to `? extends R`
in `modules/table/src/main/java/org/apache/ignite/internal/table/RecordViewImpl.java`
#### Snippet
```java
     * @return Binary rows collection.
     */
    private Collection<BinaryRowEx> marshalKeys(Collection<R> recs) {
        RecordMarshaller<R> marsh = marshaller(schemaReg.lastSchemaVersion());

```

### BoundedWildcard
Can generalize to `? extends R`
in `modules/table/src/main/java/org/apache/ignite/internal/table/RecordViewImpl.java`
#### Snippet
```java
     * @return Binary rows collection.
     */
    private Collection<BinaryRowEx> marshal(Collection<R> recs) {
        RecordMarshaller<R> marsh = marshaller(schemaReg.lastSchemaVersion());

```

### BoundedWildcard
Can generalize to `? extends Tuple`
in `modules/table/src/main/java/org/apache/ignite/internal/table/RecordBinaryViewImpl.java`
#### Snippet
```java
     * @return List of binary rows.
     */
    private Collection<BinaryRowEx> mapToBinary(Collection<Tuple> rows, boolean key) {
        Collection<BinaryRowEx> mapped = new ArrayList<>(rows.size());

```

### BoundedWildcard
Can generalize to `? super BinaryRow`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/SortedIndexLocker.java`
#### Snippet
```java
     */
    public SortedIndexLocker(UUID indexId, LockManager lockManager, SortedIndexStorage storage,
            Function<BinaryRow, BinaryTuple> indexRowResolver) {
        this.indexId = indexId;
        this.lockManager = lockManager;
```

### BoundedWildcard
Can generalize to `? extends BinaryTuple`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/SortedIndexLocker.java`
#### Snippet
```java
     */
    public SortedIndexLocker(UUID indexId, LockManager lockManager, SortedIndexStorage storage,
            Function<BinaryRow, BinaryTuple> indexRowResolver) {
        this.indexId = indexId;
        this.lockManager = lockManager;
```

### BoundedWildcard
Can generalize to `? extends IndexRow`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/SortedIndexLocker.java`
#### Snippet
```java
     * @return A future representing a locked index row or {@code null} if the cursor has no rows more.
     */
    private CompletableFuture<IndexRow> acquireLockNextKey(UUID txId, PeekCursor<IndexRow> peekCursor) {
        if (!peekCursor.hasNext()) { // No upper bound or not found. Lock INF+ and exit loop.
            return lockManager.acquire(txId, new LockKey(indexId, POSITIVE_INF.byteBuffer()), LockMode.S)
```

### BoundedWildcard
Can generalize to `? super Collection`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/StorageUpdateHandler.java`
#### Snippet
```java
            Map<UUID, ByteBuffer> rowsToUpdate,
            TablePartitionId commitPartitionId,
            @Nullable Consumer<Collection<RowId>> onReplication,
            @Nullable HybridTimestamp lowWatermark
    ) {
```

### BoundedWildcard
Can generalize to `? super RowId`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/StorageUpdateHandler.java`
#### Snippet
```java
            TablePartitionId commitPartitionId,
            @Nullable ByteBuffer rowBuffer,
            @Nullable Consumer<RowId> onReplication,
            @Nullable HybridTimestamp lowWatermark
    ) {
```

### BoundedWildcard
Can generalize to `? extends Map`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/StorageUpdateHandler.java`
#### Snippet
```java
            int partitionId,
            PartitionDataStorage storage,
            Supplier<Map<UUID, TableSchemaAwareIndexStorage>> indexes,
            DataStorageConfiguration dsCfg
    ) {
```

### BoundedWildcard
Can generalize to `? extends ReadResult`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/StorageUpdateHandler.java`
#### Snippet
```java
     * @param previousValues Cursor with previous version of the row.
     */
    private void tryRemoveFromIndexes(BinaryRow rowToRemove, RowId rowId, Cursor<ReadResult> previousValues) {
        TableSchemaAwareIndexStorage[] indexes = this.indexes.get().values().toArray(new TableSchemaAwareIndexStorage[0]);

```

### BoundedWildcard
Can generalize to `? extends K`
in `modules/table/src/main/java/org/apache/ignite/internal/table/KeyValueViewImpl.java`
#### Snippet
```java
     * @return Binary rows.
     */
    private Collection<BinaryRowEx> marshal(Collection<K> keys) {
        if (keys.isEmpty()) {
            return Collections.emptyList();
```

### BoundedWildcard
Can generalize to `? super Throwable`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/raft/PartitionListener.java`
#### Snippet
```java

    @Override
    public void onSnapshotSave(Path path, Consumer<Throwable> doneClo) {
        // The max index here is required for local recovery and a possible scenario
        // of false node failure when we actually have all required data. This might happen because we use the minimal index
```

### BoundedWildcard
Can generalize to `? super HybridTimestamp`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/raft/PartitionListener.java`
#### Snippet
```java
            StorageUpdateHandler storageUpdateHandler,
            TxStateStorage txStateStorage,
            PendingComparableValuesTracker<HybridTimestamp> safeTime
    ) {
        this.storage = partitionDataStorage;
```

### BoundedWildcard
Can generalize to `? extends CommandClosure`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/raft/PartitionListener.java`
#### Snippet
```java

    @Override
    public void onRead(Iterator<CommandClosure<ReadCommand>> iterator) {
        iterator.forEachRemaining((CommandClosure<? extends ReadCommand> clo) -> {
            Command command = clo.command();
```

### BoundedWildcard
Can generalize to `? extends CommandClosure`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/raft/PartitionListener.java`
#### Snippet
```java

    @Override
    public void onWrite(Iterator<CommandClosure<WriteCommand>> iterator) {
        iterator.forEachRemaining((CommandClosure<? extends WriteCommand> clo) -> {
            Command command = clo.command();
```

### BoundedWildcard
Can generalize to `? extends Tuple`
in `modules/table/src/main/java/org/apache/ignite/internal/table/KeyValueBinaryViewImpl.java`
#### Snippet
```java
     * @return Rows.
     */
    private List<BinaryRowEx> marshalKeys(Collection<Tuple> keys) {
        if (keys.isEmpty()) {
            return Collections.emptyList();
```

### BoundedWildcard
Can generalize to `? super SnapshotMvDataResponse.ResponseEntry`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/raft/snapshot/outgoing/OutgoingSnapshot.java`
#### Snippet
```java
    }

    private long tryProcessRowFromPartition(List<SnapshotMvDataResponse.ResponseEntry> batch, long totalBatchSize,
            SnapshotMvDataRequest request) {
        if (batchIsFull(request, totalBatchSize) || finishedMvData()) {
```

### BoundedWildcard
Can generalize to `? extends ByteBuffer`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/raft/snapshot/outgoing/OutgoingSnapshot.java`
#### Snippet
```java
    }

    private static long rowSizeInBytes(List<ByteBuffer> rowVersions) {
        long sum = 0;

```

### BoundedWildcard
Can generalize to `? super SnapshotMvDataResponse.ResponseEntry`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/raft/snapshot/outgoing/OutgoingSnapshot.java`
#### Snippet
```java

    private long fillWithOutOfOrderRows(
            List<SnapshotMvDataResponse.ResponseEntry> rowEntries,
            long totalBytesBefore,
            SnapshotMvDataRequest request
```

### BoundedWildcard
Can generalize to `? super TxMeta`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/replicator/PlacementDriver.java`
#### Snippet
```java
     * @param request Request.
     */
    private void sendAndRetry(CompletableFuture<TxMeta> resFut, ReplicationGroupId replicaGrp, TxStateReplicaRequest request) {
        ClusterNode nodeToSend = primaryReplicaMapping.get(replicaGrp).stream()
                .map(clusterNodeResolver)
```

### BoundedWildcard
Can generalize to `? super String`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/replicator/PlacementDriver.java`
#### Snippet
```java
     * @param replicaService Replication service.
     */
    public PlacementDriver(ReplicaService replicaService, Function<String, ClusterNode> clusterNodeResolver) {
        this.replicaService = replicaService;
        this.clusterNodeResolver = clusterNodeResolver;
```

### BoundedWildcard
Can generalize to `? extends ClusterNode`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/replicator/PlacementDriver.java`
#### Snippet
```java
     * @param replicaService Replication service.
     */
    public PlacementDriver(ReplicaService replicaService, Function<String, ClusterNode> clusterNodeResolver) {
        this.replicaService = replicaService;
        this.clusterNodeResolver = clusterNodeResolver;
```

### BoundedWildcard
Can generalize to `? extends BinaryRow`
in `modules/table/src/testFixtures/java/org/apache/ignite/internal/table/impl/DummySchemaManagerImpl.java`
#### Snippet
```java
    /** {@inheritDoc} */
    @Override
    public Collection<Row> resolve(Collection<BinaryRow> rows) {
        return rows.stream().filter(Objects::nonNull).map(this::resolve).collect(Collectors.toList());
    }
```

### BoundedWildcard
Can generalize to `? super HybridTimestamp`
in `modules/table/src/testFixtures/java/org/apache/ignite/internal/table/impl/DummyInternalTableImpl.java`
#### Snippet
```java
        PendingComparableValuesTracker<HybridTimestamp> safeTime;

        public SafeTimeUpdater(PendingComparableValuesTracker<HybridTimestamp> safeTime) {
            this.safeTime = safeTime;
        }
```

### BoundedWildcard
Can generalize to `? extends T`
in `modules/table/src/main/java/org/apache/ignite/internal/utils/RebalanceUtil.java`
#### Snippet
```java
     * @return Result of the intersection.
     */
    public static <T> Set<T> intersect(Set<T> op1, Set<T> op2) {
        return op1.stream().filter(op2::contains).collect(Collectors.toSet());
    }
```

### BoundedWildcard
Can generalize to `? extends T`
in `modules/table/src/main/java/org/apache/ignite/internal/utils/RebalanceUtil.java`
#### Snippet
```java
     * @return Result of the addition.
     */
    public static <T> Set<T> union(Set<T> op1, Set<T> op2) {
        var res = new HashSet<>(op1);

```

### BoundedWildcard
Can generalize to `? extends T`
in `modules/table/src/main/java/org/apache/ignite/internal/utils/RebalanceUtil.java`
#### Snippet
```java
     * @return Result of the subtraction.
     */
    public static <T> Set<T> subtract(Set<T> minuend, Set<T> subtrahend) {
        return minuend.stream().filter(v -> !subtrahend.contains(v)).collect(Collectors.toSet());
    }
```

### BoundedWildcard
Can generalize to `? extends T`
in `modules/client/src/main/java/org/apache/ignite/internal/client/TcpClientChannel.java`
#### Snippet
```java
     * @return Future for the operation.
     */
    private <T> CompletableFuture<T> receiveAsync(ClientRequestFuture pendingReq, PayloadReader<T> payloadReader) {
        return pendingReq.thenApplyAsync(payload -> {
            if (payload == null) {
```

### BoundedWildcard
Can generalize to `? extends CompletableFuture`
in `modules/client/src/main/java/org/apache/ignite/internal/client/ClientFutureUtils.java`
#### Snippet
```java

    private static <T> void doWithRetryAsync(
            Supplier<CompletableFuture<T>> func,
            @Nullable Predicate<T> validator,
            Predicate<RetryContext> retryPredicate,
```

### BoundedWildcard
Can generalize to `? super T`
in `modules/client/src/main/java/org/apache/ignite/internal/client/ClientFutureUtils.java`
#### Snippet
```java
    private static <T> void doWithRetryAsync(
            Supplier<CompletableFuture<T>> func,
            @Nullable Predicate<T> validator,
            Predicate<RetryContext> retryPredicate,
            CompletableFuture<T> resFut,
```

### BoundedWildcard
Can generalize to `? super RetryContext`
in `modules/client/src/main/java/org/apache/ignite/internal/client/ClientFutureUtils.java`
#### Snippet
```java
            Supplier<CompletableFuture<T>> func,
            @Nullable Predicate<T> validator,
            Predicate<RetryContext> retryPredicate,
            CompletableFuture<T> resFut,
            RetryContext ctx) {
```

### BoundedWildcard
Can generalize to `? super T`
in `modules/client/src/main/java/org/apache/ignite/internal/client/ClientFutureUtils.java`
#### Snippet
```java
            @Nullable Predicate<T> validator,
            Predicate<RetryContext> retryPredicate,
            CompletableFuture<T> resFut,
            RetryContext ctx) {
        func.get().whenComplete((res, err) -> {
```

### BoundedWildcard
Can generalize to `? extends TableImpl`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/TableManager.java`
#### Snippet
```java
     * @param tables Tables to stop.
     */
    private void cleanUpTablesResources(Map<UUID, TableImpl> tables) {
        for (TableImpl table : tables.values()) {
            table.beforeClose();
```

### BoundedWildcard
Can generalize to `? extends Assignment`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/TableManager.java`
#### Snippet
```java
    }

    private static PeersAndLearners configurationFromAssignments(Collection<Assignment> assignments) {
        var peers = new HashSet<String>();
        var learners = new HashSet<String>();
```

### BoundedWildcard
Can generalize to `? super ClientSchema`
in `modules/client/src/main/java/org/apache/ignite/internal/client/table/PartitionAwarenessProvider.java`
#### Snippet
```java
    private final @Nullable Function<ClientSchema, Integer> hashFunc;

    private PartitionAwarenessProvider(@Nullable ClientChannel channel, @Nullable Function<ClientSchema, Integer> hashFunc) {
        assert (channel == null) ^ (hashFunc == null) : "One must be null, another not null: channel, hashFunc";

```

### BoundedWildcard
Can generalize to `? extends ClientChannel`
in `modules/client/src/main/java/org/apache/ignite/internal/client/ReliableChannel.java`
#### Snippet
```java
        }

        private boolean isFutureInProgressOrDoneAndChannelOpen(@Nullable CompletableFuture<ClientChannel> f) {
            if (f == null || f.isCompletedExceptionally()) {
                return false;
```

### BoundedWildcard
Can generalize to `? extends R`
in `modules/client/src/main/java/org/apache/ignite/internal/client/table/ClientRecordSerializer.java`
#### Snippet
```java
    void writeRecs(
            @Nullable Transaction tx,
            @NotNull Collection<R> recs,
            ClientSchema schema,
            PayloadOutputChannel out,
```

### BoundedWildcard
Can generalize to `? extends Tuple`
in `modules/client/src/main/java/org/apache/ignite/internal/client/table/ClientTupleSerializer.java`
#### Snippet
```java
    void writeTuples(
            @Nullable Transaction tx,
            @NotNull Collection<Tuple> tuples,
            ClientSchema schema,
            PayloadOutputChannel out,
```

### BoundedWildcard
Can generalize to `? extends Tuple`
in `modules/table/src/testFixtures/java/org/apache/ignite/internal/table/TxAbstractTest.java`
#### Snippet
```java
     * @param expected Expected values.
     */
    private void validateBalance(Collection<Tuple> rows, double... expected) {
        List<Tuple> rows0 = new ArrayList<>(rows);

```

### BoundedWildcard
Can generalize to `? extends ClusterNode`
in `modules/client/src/main/java/org/apache/ignite/internal/client/compute/ClientCompute.java`
#### Snippet
```java
    /** {@inheritDoc} */
    @Override
    public <R> Map<ClusterNode, CompletableFuture<R>> broadcast(Set<ClusterNode> nodes, String jobClassName, Object... args) {
        Objects.requireNonNull(nodes);
        Objects.requireNonNull(jobClassName);
```

### BoundedWildcard
Can generalize to `? super CompletableFuture`
in `modules/runner/src/main/java/org/apache/ignite/internal/recovery/RecoveryCompletionFutureFactory.java`
#### Snippet
```java
    public static CompletableFuture<Void> create(
            ConfigurationManager clusterCfgMgr,
            Function<CompletableFuture<Void>, ConfigurationStorageRevisionListener> listenerProvider
    ) {
        CompletableFuture<Void> configCatchUpFuture = new CompletableFuture<>();
```

### BoundedWildcard
Can generalize to `? extends ConfigurationStorageRevisionListener`
in `modules/runner/src/main/java/org/apache/ignite/internal/recovery/RecoveryCompletionFutureFactory.java`
#### Snippet
```java
    public static CompletableFuture<Void> create(
            ConfigurationManager clusterCfgMgr,
            Function<CompletableFuture<Void>, ConfigurationStorageRevisionListener> listenerProvider
    ) {
        CompletableFuture<Void> configCatchUpFuture = new CompletableFuture<>();
```

### BoundedWildcard
Can generalize to `? extends ConfigurationModule`
in `modules/runner/src/main/java/org/apache/ignite/internal/configuration/ConfigurationModules.java`
#### Snippet
```java
    }

    private static CompoundModule compoundOfType(ConfigurationType type, Collection<ConfigurationModule> modules) {
        List<ConfigurationModule> modulesOfGivenType = modules.stream()
                .filter(module -> module.type() == type)
```

### BoundedWildcard
Can generalize to `? super ClientSchema`
in `modules/client/src/main/java/org/apache/ignite/internal/client/table/ClientTable.java`
#### Snippet
```java
    public <T> CompletableFuture<T> doSchemaOutOpAsync(
            int opCode,
            BiConsumer<ClientSchema, PayloadOutputChannel> writer,
            Function<ClientMessageUnpacker, T> reader,
            @Nullable PartitionAwarenessProvider provider) {
```

### BoundedWildcard
Can generalize to `? super PayloadOutputChannel`
in `modules/client/src/main/java/org/apache/ignite/internal/client/table/ClientTable.java`
#### Snippet
```java
    public <T> CompletableFuture<T> doSchemaOutOpAsync(
            int opCode,
            BiConsumer<ClientSchema, PayloadOutputChannel> writer,
            Function<ClientMessageUnpacker, T> reader,
            @Nullable PartitionAwarenessProvider provider) {
```

### BoundedWildcard
Can generalize to `? super ClientMessageUnpacker`
in `modules/client/src/main/java/org/apache/ignite/internal/client/table/ClientTable.java`
#### Snippet
```java
            int opCode,
            BiConsumer<ClientSchema, PayloadOutputChannel> writer,
            Function<ClientMessageUnpacker, T> reader,
            @Nullable PartitionAwarenessProvider provider) {

```

### BoundedWildcard
Can generalize to `? extends T`
in `modules/client/src/main/java/org/apache/ignite/internal/client/table/ClientTable.java`
#### Snippet
```java
            int opCode,
            BiConsumer<ClientSchema, PayloadOutputChannel> writer,
            Function<ClientMessageUnpacker, T> reader,
            @Nullable PartitionAwarenessProvider provider) {

```

### BoundedWildcard
Can generalize to `? super ClientSchema`
in `modules/client/src/main/java/org/apache/ignite/internal/client/table/ClientTable.java`
#### Snippet
```java
    public <T> CompletableFuture<T> doSchemaOutOpAsync(
            int opCode,
            BiConsumer<ClientSchema, PayloadOutputChannel> writer,
            Function<ClientMessageUnpacker, T> reader) {
        return getLatestSchema()
```

### BoundedWildcard
Can generalize to `? super PayloadOutputChannel`
in `modules/client/src/main/java/org/apache/ignite/internal/client/table/ClientTable.java`
#### Snippet
```java
    public <T> CompletableFuture<T> doSchemaOutOpAsync(
            int opCode,
            BiConsumer<ClientSchema, PayloadOutputChannel> writer,
            Function<ClientMessageUnpacker, T> reader) {
        return getLatestSchema()
```

### BoundedWildcard
Can generalize to `? super ClientMessageUnpacker`
in `modules/client/src/main/java/org/apache/ignite/internal/client/table/ClientTable.java`
#### Snippet
```java
            int opCode,
            BiConsumer<ClientSchema, PayloadOutputChannel> writer,
            Function<ClientMessageUnpacker, T> reader) {
        return getLatestSchema()
                .thenCompose(schema ->
```

### BoundedWildcard
Can generalize to `? extends T`
in `modules/client/src/main/java/org/apache/ignite/internal/client/table/ClientTable.java`
#### Snippet
```java
            int opCode,
            BiConsumer<ClientSchema, PayloadOutputChannel> writer,
            Function<ClientMessageUnpacker, T> reader) {
        return getLatestSchema()
                .thenCompose(schema ->
```

### BoundedWildcard
Can generalize to `? super ClientSchema`
in `modules/client/src/main/java/org/apache/ignite/internal/client/table/ClientTable.java`
#### Snippet
```java
    <T> CompletableFuture<T> doSchemaOutInOpAsync(
            int opCode,
            BiConsumer<ClientSchema, PayloadOutputChannel> writer,
            BiFunction<ClientSchema, ClientMessageUnpacker, T> reader,
            @Nullable T defaultValue,
```

### BoundedWildcard
Can generalize to `? super PayloadOutputChannel`
in `modules/client/src/main/java/org/apache/ignite/internal/client/table/ClientTable.java`
#### Snippet
```java
    <T> CompletableFuture<T> doSchemaOutInOpAsync(
            int opCode,
            BiConsumer<ClientSchema, PayloadOutputChannel> writer,
            BiFunction<ClientSchema, ClientMessageUnpacker, T> reader,
            @Nullable T defaultValue,
```

### BoundedWildcard
Can generalize to `? super ClientSchema`
in `modules/client/src/main/java/org/apache/ignite/internal/client/table/ClientTable.java`
#### Snippet
```java
    private <T> CompletionStage<T> loadSchemaAndReadData(
            Object data,
            BiFunction<ClientSchema, ClientMessageUnpacker, T> fn
    ) {
        if (!(data instanceof IgniteBiTuple)) {
```

### BoundedWildcard
Can generalize to `? super ClientMessageUnpacker`
in `modules/client/src/main/java/org/apache/ignite/internal/client/table/ClientTable.java`
#### Snippet
```java
    private <T> CompletionStage<T> loadSchemaAndReadData(
            Object data,
            BiFunction<ClientSchema, ClientMessageUnpacker, T> fn
    ) {
        if (!(data instanceof IgniteBiTuple)) {
```

### BoundedWildcard
Can generalize to `? super ClientSchema`
in `modules/client/src/main/java/org/apache/ignite/internal/client/table/ClientTable.java`
#### Snippet
```java
    <T> CompletableFuture<T> doSchemaOutInOpAsync(
            int opCode,
            BiConsumer<ClientSchema, PayloadOutputChannel> writer,
            BiFunction<ClientSchema, ClientMessageUnpacker, T> reader,
            @Nullable T defaultValue
```

### BoundedWildcard
Can generalize to `? super PayloadOutputChannel`
in `modules/client/src/main/java/org/apache/ignite/internal/client/table/ClientTable.java`
#### Snippet
```java
    <T> CompletableFuture<T> doSchemaOutInOpAsync(
            int opCode,
            BiConsumer<ClientSchema, PayloadOutputChannel> writer,
            BiFunction<ClientSchema, ClientMessageUnpacker, T> reader,
            @Nullable T defaultValue
```

### BoundedWildcard
Can generalize to `? super ClientSchema`
in `modules/client/src/main/java/org/apache/ignite/internal/client/table/ClientTable.java`
#### Snippet
```java
            ClientSchema knownSchema,
            ClientMessageUnpacker in,
            BiFunction<ClientSchema, ClientMessageUnpacker, T> fn,
            @Nullable T defaultValue
    ) {
```

### BoundedWildcard
Can generalize to `? super ClientMessageUnpacker`
in `modules/client/src/main/java/org/apache/ignite/internal/client/table/ClientTable.java`
#### Snippet
```java
            ClientSchema knownSchema,
            ClientMessageUnpacker in,
            BiFunction<ClientSchema, ClientMessageUnpacker, T> fn,
            @Nullable T defaultValue
    ) {
```

### BoundedWildcard
Can generalize to `? extends BinaryRow`
in `modules/schema/src/main/java/org/apache/ignite/internal/schema/registry/SchemaRegistryImpl.java`
#### Snippet
```java
    /** {@inheritDoc} */
    @Override
    public Collection<Row> resolve(Collection<BinaryRow> binaryRows) {
        final SchemaDescriptor curSchema = waitLatestSchema();

```

### BoundedWildcard
Can generalize to `? extends CompletableFuture`
in `modules/schema/src/main/java/org/apache/ignite/internal/schema/registry/SchemaRegistryImpl.java`
#### Snippet
```java
     */
    public SchemaRegistryImpl(
            IntFunction<CompletableFuture<SchemaDescriptor>> history,
            Supplier<CompletableFuture<Integer>> latestVersionStore,
            SchemaDescriptor initialSchema
```

### BoundedWildcard
Can generalize to `? extends CompletableFuture`
in `modules/schema/src/main/java/org/apache/ignite/internal/schema/registry/SchemaRegistryImpl.java`
#### Snippet
```java
    public SchemaRegistryImpl(
            IntFunction<CompletableFuture<SchemaDescriptor>> history,
            Supplier<CompletableFuture<Integer>> latestVersionStore,
            SchemaDescriptor initialSchema
    ) {
```

### BoundedWildcard
Can generalize to `? extends ColumnDefinition`
in `modules/schema/src/testFixtures/java/org/apache/ignite/internal/schema/testutils/builder/TableDefinitionBuilderImpl.java`
#### Snippet
```java
    /** {@inheritDoc} */
    @Override
    public TableDefinitionBuilderImpl columns(List<ColumnDefinition> columns) {
        for (ColumnDefinition column : columns) {
            if (this.columns.put(column.name(), column) != null) {
```

### BoundedWildcard
Can generalize to `? extends ColumnDefinition`
in `modules/schema/src/testFixtures/java/org/apache/ignite/internal/schema/testutils/definition/TableDefinitionImpl.java`
#### Snippet
```java
            String schemaName,
            String tableName,
            LinkedHashMap<String, ColumnDefinition> colMap,
            PrimaryKeyDefinition primaryKeyDefinition
    ) {
```

### BoundedWildcard
Can generalize to `? extends Metric`
in `modules/metrics/src/main/java/org/apache/ignite/internal/metrics/CompositeAwareIterator.java`
#### Snippet
```java
     * @param delegate Collection of metrics that can contain composite metrics.
     */
    public CompositeAwareIterator(Iterator<Metric> delegate) {
        this.delegate = delegate;
    }
```

### BoundedWildcard
Can generalize to `? extends MetricSet`
in `modules/metrics/src/main/java/org/apache/ignite/internal/metrics/MetricRegistry.java`
#### Snippet
```java
     * @param metricSets New map of metric sets that should be saved to new version of metric snapshot.
     */
    private void updateMetricSnapshot(Map<String, MetricSet> metricSets) {
        assert lock.isHeldByCurrentThread() : "Access to shared state from an incorrect thread " + Thread.currentThread().getName();

```

### BoundedWildcard
Can generalize to `? extends Column`
in `modules/schema/src/testFixtures/java/org/apache/ignite/internal/schema/marshaller/RecordMarshallerTest.java`
#### Snippet
```java
     * @param allColumns Columns to test.
     */
    public static void ensureAllTypesChecked(Stream<Column> allColumns) {
        Set<NativeTypeSpec> testedTypes = allColumns.map(c -> c.type().spec())
                .collect(Collectors.toSet());
```

### BoundedWildcard
Can generalize to `? extends MemoryUsage`
in `modules/metrics/src/main/java/org/apache/ignite/internal/metrics/sources/JvmMetricSource.java`
#### Snippet
```java
         * @param timeout Cache timeout in millis.
         */
        private CachedMemoryUsage(Supplier<MemoryUsage> source, long timeout) {
            this.source = source;
            this.timeout = timeout;
```

### BoundedWildcard
Can generalize to `? super String`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/storage/InternalTableImpl.java`
#### Snippet
```java
            Int2ObjectMap<RaftGroupService> partMap,
            int partitions,
            Function<String, ClusterNode> clusterNodeResolver,
            TxManager txManager,
            MvTableStorage tableStorage,
```

### BoundedWildcard
Can generalize to `? extends ClusterNode`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/storage/InternalTableImpl.java`
#### Snippet
```java
            Int2ObjectMap<RaftGroupService> partMap,
            int partitions,
            Function<String, ClusterNode> clusterNodeResolver,
            TxManager txManager,
            MvTableStorage tableStorage,
```

### BoundedWildcard
Can generalize to `? super TablePartitionId`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/storage/InternalTableImpl.java`
#### Snippet
```java
            BinaryRowEx row,
            InternalTransaction tx,
            IgniteTetraFunction<TablePartitionId, InternalTransaction, ReplicationGroupId, Long, ReplicaRequest> op
    ) {
        // Check whether proposed tx is read-only. Complete future exceptionally if true.
```

### BoundedWildcard
Can generalize to `? super InternalTransaction`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/storage/InternalTableImpl.java`
#### Snippet
```java
            BinaryRowEx row,
            InternalTransaction tx,
            IgniteTetraFunction<TablePartitionId, InternalTransaction, ReplicationGroupId, Long, ReplicaRequest> op
    ) {
        // Check whether proposed tx is read-only. Complete future exceptionally if true.
```

### BoundedWildcard
Can generalize to `? super ReplicationGroupId`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/storage/InternalTableImpl.java`
#### Snippet
```java
            BinaryRowEx row,
            InternalTransaction tx,
            IgniteTetraFunction<TablePartitionId, InternalTransaction, ReplicationGroupId, Long, ReplicaRequest> op
    ) {
        // Check whether proposed tx is read-only. Complete future exceptionally if true.
```

### BoundedWildcard
Can generalize to `? super Long`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/storage/InternalTableImpl.java`
#### Snippet
```java
            BinaryRowEx row,
            InternalTransaction tx,
            IgniteTetraFunction<TablePartitionId, InternalTransaction, ReplicationGroupId, Long, ReplicaRequest> op
    ) {
        // Check whether proposed tx is read-only. Complete future exceptionally if true.
```

### BoundedWildcard
Can generalize to `? extends ReplicaRequest`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/storage/InternalTableImpl.java`
#### Snippet
```java
            BinaryRowEx row,
            InternalTransaction tx,
            IgniteTetraFunction<TablePartitionId, InternalTransaction, ReplicationGroupId, Long, ReplicaRequest> op
    ) {
        // Check whether proposed tx is read-only. Complete future exceptionally if true.
```

### BoundedWildcard
Can generalize to `? super TablePartitionId`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/storage/InternalTableImpl.java`
#### Snippet
```java
            InternalTransaction tx,
            int partId,
            BiFunction<TablePartitionId, Long, ReplicaRequest> requestFunction,
            int attempts
    ) {
```

### BoundedWildcard
Can generalize to `? super Long`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/storage/InternalTableImpl.java`
#### Snippet
```java
            InternalTransaction tx,
            int partId,
            BiFunction<TablePartitionId, Long, ReplicaRequest> requestFunction,
            int attempts
    ) {
```

### BoundedWildcard
Can generalize to `? extends ReplicaRequest`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/storage/InternalTableImpl.java`
#### Snippet
```java
            InternalTransaction tx,
            int partId,
            BiFunction<TablePartitionId, Long, ReplicaRequest> requestFunction,
            int attempts
    ) {
```

### BoundedWildcard
Can generalize to `? extends T`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/storage/InternalTableImpl.java`
#### Snippet
```java
     * @return The future.
     */
    private <T> CompletableFuture<T> postEnlist(CompletableFuture<T> fut, boolean implicit, InternalTransaction tx0) {
        return fut.handle((BiFunction<T, Throwable, CompletableFuture<T>>) (r, e) -> {
            if (e != null) {
```

### BoundedWildcard
Can generalize to `? extends BinaryRowEx`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/storage/InternalTableImpl.java`
#### Snippet
```java
     * @return Partition -%gt; rows mapping.
     */
    private Int2ObjectOpenHashMap<List<BinaryRow>> mapRowsToPartitions(Collection<BinaryRowEx> rows) {
        Int2ObjectOpenHashMap<List<BinaryRow>> keyRowsByPartition = new Int2ObjectOpenHashMap<>();

```

### BoundedWildcard
Can generalize to `? super TablePartitionId`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/storage/InternalTableImpl.java`
#### Snippet
```java
            Collection<BinaryRowEx> keyRows,
            InternalTransaction tx,
            IgniteFiveFunction<TablePartitionId, Collection<BinaryRow>, InternalTransaction, ReplicationGroupId, Long, ReplicaRequest> op,
            Function<CompletableFuture<Object>[], CompletableFuture<T>> reducer
    ) {
```

### BoundedWildcard
Can generalize to `? super Collection`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/storage/InternalTableImpl.java`
#### Snippet
```java
            Collection<BinaryRowEx> keyRows,
            InternalTransaction tx,
            IgniteFiveFunction<TablePartitionId, Collection<BinaryRow>, InternalTransaction, ReplicationGroupId, Long, ReplicaRequest> op,
            Function<CompletableFuture<Object>[], CompletableFuture<T>> reducer
    ) {
```

### BoundedWildcard
Can generalize to `? super InternalTransaction`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/storage/InternalTableImpl.java`
#### Snippet
```java
            Collection<BinaryRowEx> keyRows,
            InternalTransaction tx,
            IgniteFiveFunction<TablePartitionId, Collection<BinaryRow>, InternalTransaction, ReplicationGroupId, Long, ReplicaRequest> op,
            Function<CompletableFuture<Object>[], CompletableFuture<T>> reducer
    ) {
```

### BoundedWildcard
Can generalize to `? super ReplicationGroupId`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/storage/InternalTableImpl.java`
#### Snippet
```java
            Collection<BinaryRowEx> keyRows,
            InternalTransaction tx,
            IgniteFiveFunction<TablePartitionId, Collection<BinaryRow>, InternalTransaction, ReplicationGroupId, Long, ReplicaRequest> op,
            Function<CompletableFuture<Object>[], CompletableFuture<T>> reducer
    ) {
```

### BoundedWildcard
Can generalize to `? super Long`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/storage/InternalTableImpl.java`
#### Snippet
```java
            Collection<BinaryRowEx> keyRows,
            InternalTransaction tx,
            IgniteFiveFunction<TablePartitionId, Collection<BinaryRow>, InternalTransaction, ReplicationGroupId, Long, ReplicaRequest> op,
            Function<CompletableFuture<Object>[], CompletableFuture<T>> reducer
    ) {
```

### BoundedWildcard
Can generalize to `? extends ReplicaRequest`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/storage/InternalTableImpl.java`
#### Snippet
```java
            Collection<BinaryRowEx> keyRows,
            InternalTransaction tx,
            IgniteFiveFunction<TablePartitionId, Collection<BinaryRow>, InternalTransaction, ReplicationGroupId, Long, ReplicaRequest> op,
            Function<CompletableFuture<Object>[], CompletableFuture<T>> reducer
    ) {
```

### BoundedWildcard
Can generalize to `? extends CompletableFuture`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/storage/InternalTableImpl.java`
#### Snippet
```java
            InternalTransaction tx,
            IgniteFiveFunction<TablePartitionId, Collection<BinaryRow>, InternalTransaction, ReplicationGroupId, Long, ReplicaRequest> op,
            Function<CompletableFuture<Object>[], CompletableFuture<T>> reducer
    ) {
        // Check whether proposed tx is read-only. Complete future exceptionally if true.
```

### BoundedWildcard
Can generalize to `? super Long`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/storage/InternalTableImpl.java`
#### Snippet
```java
         */
        PartitionScanPublisher(
                BiFunction<Long, Integer, CompletableFuture<Collection<BinaryRow>>> retrieveBatch,
                Function<CompletableFuture<Void>, CompletableFuture<Void>> onClose
        ) {
```

### BoundedWildcard
Can generalize to `? super Integer`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/storage/InternalTableImpl.java`
#### Snippet
```java
         */
        PartitionScanPublisher(
                BiFunction<Long, Integer, CompletableFuture<Collection<BinaryRow>>> retrieveBatch,
                Function<CompletableFuture<Void>, CompletableFuture<Void>> onClose
        ) {
```

### BoundedWildcard
Can generalize to `? extends CompletableFuture`>
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/storage/InternalTableImpl.java`
#### Snippet
```java
         */
        PartitionScanPublisher(
                BiFunction<Long, Integer, CompletableFuture<Collection<BinaryRow>>> retrieveBatch,
                Function<CompletableFuture<Void>, CompletableFuture<Void>> onClose
        ) {
```

### BoundedWildcard
Can generalize to `? super CompletableFuture`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/storage/InternalTableImpl.java`
#### Snippet
```java
        PartitionScanPublisher(
                BiFunction<Long, Integer, CompletableFuture<Collection<BinaryRow>>> retrieveBatch,
                Function<CompletableFuture<Void>, CompletableFuture<Void>> onClose
        ) {
            this.retrieveBatch = retrieveBatch;
```

### BoundedWildcard
Can generalize to `? extends CompletableFuture`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/storage/InternalTableImpl.java`
#### Snippet
```java
        PartitionScanPublisher(
                BiFunction<Long, Integer, CompletableFuture<Collection<BinaryRow>>> retrieveBatch,
                Function<CompletableFuture<Void>, CompletableFuture<Void>> onClose
        ) {
            this.retrieveBatch = retrieveBatch;
```

### BoundedWildcard
Can generalize to `? extends T`
in `modules/network/src/main/java/org/apache/ignite/internal/network/netty/NettyUtils.java`
#### Snippet
```java
    public static <T, R, F extends Future<R>> CompletableFuture<T> toCompletableFuture(
            @Schedule F nettyFuture,
            Function<F, T> mapper,
            Supplier<? extends CompletableFuture<T>> completableFutureFactory
    ) {
```

### BoundedWildcard
Can generalize to `? extends ClusterNode`
in `modules/compute/src/main/java/org/apache/ignite/internal/compute/IgniteComputeImpl.java`
#### Snippet
```java
    @Override
    public <R> Map<ClusterNode, CompletableFuture<R>> broadcast(
            Set<ClusterNode> nodes,
            Class<? extends ComputeJob<R>> jobClass,
            Object... args
```

### BoundedWildcard
Can generalize to `? extends ClusterNode`
in `modules/compute/src/main/java/org/apache/ignite/internal/compute/IgniteComputeImpl.java`
#### Snippet
```java
    /** {@inheritDoc} */
    @Override
    public <R> Map<ClusterNode, CompletableFuture<R>> broadcast(Set<ClusterNode> nodes, String jobClassName, Object... args) {
        Objects.requireNonNull(nodes);
        Objects.requireNonNull(jobClassName);
```

### BoundedWildcard
Can generalize to `? super InNetworkObject`
in `modules/network/src/main/java/org/apache/ignite/internal/network/netty/MessageHandler.java`
#### Snippet
```java
     * @param serializationService Serialization service.
     */
    public MessageHandler(Consumer<InNetworkObject> messageListener, String consistentId,
            PerSessionSerializationService serializationService) {
        this.messageListener = messageListener;
```

### BoundedWildcard
Can generalize to `? extends NetworkAddress`
in `modules/network/src/main/java/org/apache/ignite/network/scalecube/ScaleCubeClusterServiceFactory.java`
#### Snippet
```java
     * @return List of ScaleCube's {@link Address}.
     */
    private static List<Address> parseAddresses(List<NetworkAddress> addresses) {
        return addresses.stream()
                .map(addr -> Address.create(addr.host(), addr.port()))
```

### BoundedWildcard
Can generalize to `? super Channel`
in `modules/network/src/main/java/org/apache/ignite/internal/network/netty/NettyServer.java`
#### Snippet
```java
     * @param fut       Future.
     */
    private void tryBind(ServerBootstrap bootstrap, int port, int endPort, int startPort, CompletableFuture<Channel> fut) {
        if (port > endPort) {
            fut.completeExceptionally(new IllegalStateException("No available port in range [" + startPort + "-" + endPort + ']'));
```

### BoundedWildcard
Can generalize to `? super BinaryRow`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/replicator/PartitionReplicaListener.java`
#### Snippet
```java
            HybridTimestamp timestamp,
            int batchSize,
            List<BinaryRow> result
    ) {
        if (result.size() >= batchSize || !indexCursor.hasNext()) {
```

### BoundedWildcard
Can generalize to `? extends Map`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/replicator/PartitionReplicaListener.java`
#### Snippet
```java
            int partId,
            UUID tableId,
            Supplier<Map<UUID, IndexLocker>> indexesLockers,
            Lazy<TableSchemaAwareIndexStorage> pkIndexStorage,
            Supplier<Map<UUID, TableSchemaAwareIndexStorage>> secondaryIndexStorages,
```

### BoundedWildcard
Can generalize to `? extends TableSchemaAwareIndexStorage`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/replicator/PartitionReplicaListener.java`
#### Snippet
```java
            UUID tableId,
            Supplier<Map<UUID, IndexLocker>> indexesLockers,
            Lazy<TableSchemaAwareIndexStorage> pkIndexStorage,
            Supplier<Map<UUID, TableSchemaAwareIndexStorage>> secondaryIndexStorages,
            HybridClock hybridClock,
```

### BoundedWildcard
Can generalize to `? extends Map`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/replicator/PartitionReplicaListener.java`
#### Snippet
```java
            Supplier<Map<UUID, IndexLocker>> indexesLockers,
            Lazy<TableSchemaAwareIndexStorage> pkIndexStorage,
            Supplier<Map<UUID, TableSchemaAwareIndexStorage>> secondaryIndexStorages,
            HybridClock hybridClock,
            PendingComparableValuesTracker<HybridTimestamp> safeTime,
```

### BoundedWildcard
Can generalize to `? super HybridTimestamp`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/replicator/PartitionReplicaListener.java`
#### Snippet
```java
            Supplier<Map<UUID, TableSchemaAwareIndexStorage>> secondaryIndexStorages,
            HybridClock hybridClock,
            PendingComparableValuesTracker<HybridTimestamp> safeTime,
            TxStateStorage txStateStorage,
            PlacementDriver placementDriver,
```

### BoundedWildcard
Can generalize to `? super Peer`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/replicator/PartitionReplicaListener.java`
#### Snippet
```java
            PlacementDriver placementDriver,
            StorageUpdateHandler storageUpdateHandler,
            Function<Peer, Boolean> isLocalPeerChecker,
            CompletableFuture<SchemaRegistry> schemaFut
    ) {
```

### BoundedWildcard
Can generalize to `? extends SchemaRegistry`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/replicator/PartitionReplicaListener.java`
#### Snippet
```java
            StorageUpdateHandler storageUpdateHandler,
            Function<Peer, Boolean> isLocalPeerChecker,
            CompletableFuture<SchemaRegistry> schemaFut
    ) {
        this.mvDataStorage = mvDataStorage;
```

### BoundedWildcard
Can generalize to `? super @Nullable RowId`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/replicator/PartitionReplicaListener.java`
#### Snippet
```java
            BinaryRow binaryRow,
            UUID txId,
            BiFunction<@Nullable RowId, @Nullable BinaryRow, CompletableFuture<T>> action
    ) {
        IndexLocker pkLocker = indexesLockers.get().get(pkIndexStorage.get().id());
```

### BoundedWildcard
Can generalize to `? super @Nullable BinaryRow`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/replicator/PartitionReplicaListener.java`
#### Snippet
```java
            BinaryRow binaryRow,
            UUID txId,
            BiFunction<@Nullable RowId, @Nullable BinaryRow, CompletableFuture<T>> action
    ) {
        IndexLocker pkLocker = indexesLockers.get().get(pkIndexStorage.get().id());
```

### BoundedWildcard
Can generalize to `? extends CompletableFuture`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/replicator/PartitionReplicaListener.java`
#### Snippet
```java
            BinaryRow binaryRow,
            UUID txId,
            BiFunction<@Nullable RowId, @Nullable BinaryRow, CompletableFuture<T>> action
    ) {
        IndexLocker pkLocker = indexesLockers.get().get(pkIndexStorage.get().id());
```

### BoundedWildcard
Can generalize to `? super BinaryRow`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/replicator/PartitionReplicaListener.java`
#### Snippet
```java
            Cursor<IndexRow> indexCursor,
            int batchSize,
            List<BinaryRow> result,
            Function<IndexRow, Boolean> isUpperBoundAchieved
    ) {
```

### BoundedWildcard
Can generalize to `? super IndexRow`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/replicator/PartitionReplicaListener.java`
#### Snippet
```java
            int batchSize,
            List<BinaryRow> result,
            Function<IndexRow, Boolean> isUpperBoundAchieved
    ) {
        if (result.size() == batchSize) { // Batch is full, exit loop.
```

### BoundedWildcard
Can generalize to `? super BinaryRow`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/replicator/PartitionReplicaListener.java`
#### Snippet
```java
            Cursor<RowId> indexCursor,
            int batchSize,
            List<BinaryRow> result
    ) {
        if (result.size() >= batchSize || !indexCursor.hasNext()) {
```

### BoundedWildcard
Can generalize to `? extends CompletableFuture`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/replicator/PartitionReplicaListener.java`
#### Snippet
```java
     * @return A future object representing the result of the given operation.
     */
    private <T> CompletableFuture<T> appendTxCommand(UUID txId, RequestType cmdType, Supplier<CompletableFuture<T>> op) {
        var fut = new CompletableFuture<T>();

```

### BoundedWildcard
Can generalize to `? extends IndexRow`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/replicator/PartitionReplicaListener.java`
#### Snippet
```java

    private CompletableFuture<Void> continueReadOnlyIndexScan(
            Cursor<IndexRow> cursor,
            HybridTimestamp timestamp,
            int batchSize,
```

### BoundedWildcard
Can generalize to `? super BinaryRow`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/replicator/PartitionReplicaListener.java`
#### Snippet
```java
            HybridTimestamp timestamp,
            int batchSize,
            List<BinaryRow> result
    ) {
        if (result.size() >= batchSize || !cursor.hasNext()) {
```

### BoundedWildcard
Can generalize to `? extends BinaryRow`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/replicator/PartitionReplicaListener.java`
#### Snippet
```java
            ReadResult readResult,
            HybridTimestamp timestamp,
            Supplier<BinaryRow> lastCommitted
    ) {
        return resolveTxState(
```

### BoundedWildcard
Can generalize to `? extends T`
in `modules/network/src/main/java/org/apache/ignite/internal/network/direct/state/DirectMessageState.java`
#### Snippet
```java
     * @param factory Item factory.
     */
    public DirectMessageState(Class<T> cls, Supplier<T> factory) {
        this.factory = factory;

```

### BoundedWildcard
Can generalize to `? extends ReadResult`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/replicator/PartitionReplicaListener.java`
#### Snippet
```java
     * @param writeIntents Write intents.
     */
    private static void checkWriteIntentsBelongSameTx(Collection<ReadResult> writeIntents) {
        ReadResult writeIntent = findAny(writeIntents).orElseThrow();

```

### BoundedWildcard
Can generalize to `? extends ClassDescriptor`
in `modules/network/src/main/java/org/apache/ignite/internal/network/serialization/MapBackedIdIndexedDescriptors.java`
#### Snippet
```java
    private final Int2ObjectMap<ClassDescriptor> descriptorsById;

    public MapBackedIdIndexedDescriptors(Int2ObjectMap<ClassDescriptor> descriptorsById) {
        this.descriptorsById = descriptorsById;
    }
```

### BoundedWildcard
Can generalize to `? extends T`
in `modules/network/src/main/java/org/apache/ignite/internal/network/direct/DirectMessageReader.java`
#### Snippet
```java
    /** {@inheritDoc} */
    @Override
    public <T> T[] readObjectArray(String name, MessageCollectionItemType itemType, Class<T> itemCls) {
        DirectByteBufferStream stream = state.item().stream;

```

### BoundedWildcard
Can generalize to `? extends ClassDescriptor`
in `modules/network/src/main/java/org/apache/ignite/internal/network/serialization/ClassNameMapBackedClassIndexedDescriptors.java`
#### Snippet
```java
    private final Map<String, ClassDescriptor> descriptorsByClass;

    public ClassNameMapBackedClassIndexedDescriptors(Map<String, ClassDescriptor> descriptorsByClass) {
        this.descriptorsByClass = descriptorsByClass;
    }
```

### BoundedWildcard
Can generalize to `? extends ClassDescriptor`
in `modules/network/src/main/java/org/apache/ignite/internal/network/serialization/ClassDescriptorMerger.java`
#### Snippet
```java
    }

    private static Set<String> classNames(List<ClassDescriptor> localLineage) {
        return localLineage.stream()
                .map(ClassDescriptor::className)
```

### BoundedWildcard
Can generalize to `? extends FieldDescriptor`
in `modules/network/src/main/java/org/apache/ignite/internal/network/serialization/ClassDescriptorMerger.java`
#### Snippet
```java
 */
class ClassDescriptorMerger {
    static List<MergedField> mergeFields(List<FieldDescriptor> localFields, List<FieldDescriptor> remoteFields) {
        List<MergedField> mergedFields = new ArrayList<>();

```

### BoundedWildcard
Can generalize to `? extends FieldDescriptor`
in `modules/network/src/main/java/org/apache/ignite/internal/network/serialization/ClassDescriptorMerger.java`
#### Snippet
```java
 */
class ClassDescriptorMerger {
    static List<MergedField> mergeFields(List<FieldDescriptor> localFields, List<FieldDescriptor> remoteFields) {
        List<MergedField> mergedFields = new ArrayList<>();

```

### BoundedWildcard
Can generalize to `? extends Collection`
in `modules/network/src/main/java/org/apache/ignite/internal/network/direct/stream/DirectByteBufferStreamImplV1.java`
#### Snippet
```java
            MessageCollectionItemType itemType,
            MessageReader reader,
            IntFunction<Collection<Object>> ctor
    ) {
        if (readSize == -1) {
```

### BoundedWildcard
Can generalize to `? extends ClassDescriptorMessage`
in `modules/network/src/main/java/org/apache/ignite/internal/network/serialization/PerSessionSerializationService.java`
#### Snippet
```java
     * @param remoteDescriptors Remote descriptors.
     */
    public void mergeDescriptors(Collection<ClassDescriptorMessage> remoteDescriptors) {
        List<ClassDescriptorMessage> leftToProcess = remoteDescriptors.stream()
                .filter(classMessage -> !knownMergedDescriptor(classMessage.descriptorId()))
```

### BoundedWildcard
Can generalize to `? super K`
in `modules/network/src/main/java/org/apache/ignite/internal/network/serialization/marshal/BuiltInMarshalling.java`
#### Snippet
```java
            Map<K, V> map,
            IgniteDataOutput output,
            ValueWriter<K> keyWriter,
            ValueWriter<V> valueWriter,
            MarshallingContext context
```

### BoundedWildcard
Can generalize to `? super V`
in `modules/network/src/main/java/org/apache/ignite/internal/network/serialization/marshal/BuiltInMarshalling.java`
#### Snippet
```java
            IgniteDataOutput output,
            ValueWriter<K> keyWriter,
            ValueWriter<V> valueWriter,
            MarshallingContext context
    ) throws IOException, MarshalException {
```

### BoundedWildcard
Can generalize to `? extends T`
in `modules/network/src/main/java/org/apache/ignite/internal/network/serialization/marshal/BuiltInMarshalling.java`
#### Snippet
```java

    static <T> void writeCollection(
            Collection<T> collection,
            IgniteDataOutput output,
            ValueWriter<T> valueWriter,
```

### BoundedWildcard
Can generalize to `? extends T`
in `modules/network/src/main/java/org/apache/ignite/internal/network/serialization/marshal/BuiltInMarshalling.java`
#### Snippet
```java
    }

    private static <T> void fillRefArrayFrom(IgniteDataInput input, T[] array, ValueReader<T> valueReader, UnmarshallingContext context)
            throws IOException, UnmarshalException {
        for (int i = 0; i < array.length; i++) {
```

### BoundedWildcard
Can generalize to `? super T`
in `modules/network/src/main/java/org/apache/ignite/internal/network/serialization/marshal/BuiltInMarshalling.java`
#### Snippet
```java
    private static <T> void writeRandomAccessListElements(
            IgniteDataOutput output,
            ValueWriter<T> valueWriter,
            MarshallingContext context,
            List<T> list
```

### BoundedWildcard
Can generalize to `? extends T`
in `modules/network/src/main/java/org/apache/ignite/internal/network/serialization/marshal/BuiltInMarshalling.java`
#### Snippet
```java
            ValueWriter<T> valueWriter,
            MarshallingContext context,
            List<T> list
    ) throws IOException, MarshalException {
        //noinspection ForLoopReplaceableByForEach
```

### BoundedWildcard
Can generalize to `? super T`
in `modules/network/src/main/java/org/apache/ignite/internal/network/serialization/marshal/BuiltInMarshalling.java`
#### Snippet
```java
    }

    private static <T> void writeRefArray(T[] array, IgniteDataOutput output, ValueWriter<T> valueWriter, MarshallingContext context)
            throws IOException, MarshalException {
        writeLength(array.length, output);
```

### BoundedWildcard
Can generalize to `? super T`
in `modules/network/src/main/java/org/apache/ignite/internal/network/serialization/marshal/BuiltInMarshalling.java`
#### Snippet
```java
            IgniteDataInput input,
            IntFunction<T[]> arrayFactory,
            ValueReader<T> valueReader,
            UnmarshallingContext context
    ) throws IOException, UnmarshalException {
```

### BoundedWildcard
Can generalize to `? extends FieldDescriptor`
in `modules/network/src/main/java/org/apache/ignite/internal/network/serialization/ClassDescriptor.java`
#### Snippet
```java
    }

    private static Map<String, FieldDescriptor> fieldsByNameMap(List<FieldDescriptor> fields) {
        return fields.stream()
                .collect(toUnmodifiableMap(FieldDescriptor::name, Function.identity()));
```

### BoundedWildcard
Can generalize to `? extends FieldDescriptor`
in `modules/network/src/main/java/org/apache/ignite/internal/network/serialization/ClassDescriptor.java`
#### Snippet
```java
    }

    private static Object2IntMap<String> primitiveFieldDataOffsetsMap(List<FieldDescriptor> fields) {
        Object2IntMap<String> map = new Object2IntOpenHashMap<>();

```

### BoundedWildcard
Can generalize to `? extends FieldDescriptor`
in `modules/network/src/main/java/org/apache/ignite/internal/network/serialization/ClassDescriptor.java`
#### Snippet
```java
    }

    private static int computeObjectFieldsCount(List<FieldDescriptor> fields) {
        return (int) fields.stream()
                .filter(fieldDesc -> !fieldDesc.isPrimitive())
```

### BoundedWildcard
Can generalize to `? extends FieldDescriptor`
in `modules/network/src/main/java/org/apache/ignite/internal/network/serialization/ClassDescriptor.java`
#### Snippet
```java
    }

    private static int computePrimitiveFieldsDataSize(List<FieldDescriptor> fields) {
        int accumulatedBytes = 0;
        for (FieldDescriptor fieldDesc : fields) {
```

### BoundedWildcard
Can generalize to `? extends FieldDescriptor`
in `modules/network/src/main/java/org/apache/ignite/internal/network/serialization/ClassDescriptor.java`
#### Snippet
```java
    }

    private Object2IntMap<String> computeObjectFieldIndices(List<FieldDescriptor> fields) {
        Object2IntMap<String> map = new Object2IntOpenHashMap<>();

```

### BoundedWildcard
Can generalize to `? extends FieldDescriptor`
in `modules/network/src/main/java/org/apache/ignite/internal/network/serialization/ClassDescriptor.java`
#### Snippet
```java
    }

    private static int computeFieldNullsBitmapSize(List<FieldDescriptor> fields) {
        int count = 0;
        for (FieldDescriptor fieldDescriptor : fields) {
```

### BoundedWildcard
Can generalize to `? extends FieldDescriptor`
in `modules/network/src/main/java/org/apache/ignite/internal/network/serialization/ClassDescriptor.java`
#### Snippet
```java
    }

    private static Object2IntMap<String> computeFieldNullsBitmapIndices(List<FieldDescriptor> fields) {
        Object2IntMap<String> map = new Object2IntOpenHashMap<>();

```

### BoundedWildcard
Can generalize to `? extends T`
in `modules/bytecode/src/main/java/com/facebook/presto/bytecode/ClassGenerator.java`
#### Snippet
```java
    }

    public <T> Class<? extends T> defineClass(ClassDefinition classDefinition, Class<T> superType)
    {
        Map<String, Class<?>> classes = defineClasses(List.of(classDefinition));
```

### BoundedWildcard
Can generalize to `? extends Collection`
in `modules/affinity/src/main/java/org/apache/ignite/internal/affinity/RendezvousAffinityFunction.java`
#### Snippet
```java
            List<String> nodes,
            int replicas,
            Map<String, Collection<String>> neighborhoodCache,
            boolean exclNeighbors,
            BiPredicate<String, T> nodeFilter,
```

### BoundedWildcard
Can generalize to `? super String`
in `modules/affinity/src/main/java/org/apache/ignite/internal/affinity/RendezvousAffinityFunction.java`
#### Snippet
```java
            Map<String, Collection<String>> neighborhoodCache,
            boolean exclNeighbors,
            BiPredicate<String, T> nodeFilter,
            IntFunction<T> aggregator
    ) {
```

### BoundedWildcard
Can generalize to `? super T`
in `modules/affinity/src/main/java/org/apache/ignite/internal/affinity/RendezvousAffinityFunction.java`
#### Snippet
```java
            Map<String, Collection<String>> neighborhoodCache,
            boolean exclNeighbors,
            BiPredicate<String, T> nodeFilter,
            IntFunction<T> aggregator
    ) {
```

### BoundedWildcard
Can generalize to `? extends T`
in `modules/affinity/src/main/java/org/apache/ignite/internal/affinity/RendezvousAffinityFunction.java`
#### Snippet
```java
            boolean exclNeighbors,
            BiPredicate<String, T> nodeFilter,
            IntFunction<T> aggregator
    ) {
        if (nodes.size() <= 1) {
```

### BoundedWildcard
Can generalize to `? super String`
in `modules/bytecode/src/main/java/com/facebook/presto/bytecode/BytecodeUtils.java`
#### Snippet
```java
    }

    public static ParameterizedType makeClassName(String baseName, Optional<String> suffix) {
        String className = baseName
            + "_" + suffix.orElseGet(() -> Instant.now().atZone(UTC).format(TIMESTAMP_FORMAT))
```

### BoundedWildcard
Can generalize to `? extends ClassDefinition`
in `modules/bytecode/src/main/java/com/facebook/presto/bytecode/ClassInfoLoader.java`
#### Snippet
```java

public class ClassInfoLoader {
    public static ClassInfoLoader createClassInfoLoader(Collection<ClassDefinition> classDefinitions,
        ClassLoader classLoader) {
        Map<ParameterizedType, ClassNode> classNodes = new HashMap<>();
```

### BoundedWildcard
Can generalize to `? extends ParameterizedType`
in `modules/bytecode/src/main/java/com/facebook/presto/bytecode/MethodDefinition.java`
#### Snippet
```java
    public static String genericMethodSignature(
        ParameterizedType returnType,
        List<ParameterizedType> parameterTypes) {
        StringBuilder sb = new StringBuilder();
        sb.append("(");
```

### BoundedWildcard
Can generalize to `? extends ParameterizedType`
in `modules/bytecode/src/main/java/com/facebook/presto/bytecode/MethodDefinition.java`
#### Snippet
```java
    public static String methodDescription(
        ParameterizedType returnType,
        List<ParameterizedType> parameterTypes) {
        StringBuilder sb = new StringBuilder();
        sb.append("(");
```

### BoundedWildcard
Can generalize to `? super Integer`
in `modules/network/src/main/java/org/apache/ignite/internal/network/serialization/marshal/BuiltInNonContainerMarshallers.java`
#### Snippet
```java

    private static <T> void addSingle(
            Map<Integer, BuiltInMarshaller<?>> map,
            BuiltInType type,
            ValueWriter<T> writer,
```

### BoundedWildcard
Can generalize to `? super BuiltInMarshaller`
in `modules/network/src/main/java/org/apache/ignite/internal/network/serialization/marshal/BuiltInNonContainerMarshallers.java`
#### Snippet
```java

    private static <T> void addSingle(
            Map<Integer, BuiltInMarshaller<?>> map,
            BuiltInType type,
            ValueWriter<T> writer,
```

### BoundedWildcard
Can generalize to `? extends T`
in `modules/network/src/main/java/org/apache/ignite/internal/network/serialization/marshal/BuiltInNonContainerMarshallers.java`
#### Snippet
```java
    }

    private static <T> ValueReader<T> contextless(ContextlessValueReader<T> reader) {
        return (in, ctx) -> reader.read(in);
    }
```

### BoundedWildcard
Can generalize to `? super T`
in `modules/network/src/main/java/org/apache/ignite/internal/network/serialization/marshal/BuiltInNonContainerMarshallers.java`
#### Snippet
```java
        private final ValueReader<T> reader;

        private BuiltInMarshaller(ValueWriter<T> writer, ValueReader<T> reader) {
            this.writer = writer;
            this.reader = reader;
```

### BoundedWildcard
Can generalize to `? super T`
in `modules/network/src/main/java/org/apache/ignite/internal/network/serialization/marshal/BuiltInNonContainerMarshallers.java`
#### Snippet
```java
    }

    private static <T> ValueWriter<T> contextless(ContextlessValueWriter<T> writer) {
        return (obj, out, ctx) -> writer.write(obj, out);
    }
```

### BoundedWildcard
Can generalize to `? super Integer`
in `modules/network/src/main/java/org/apache/ignite/internal/network/serialization/marshal/BuiltInNonContainerMarshallers.java`
#### Snippet
```java

    private static <T> void addPrimitiveAndWrapper(
            Map<Integer, BuiltInMarshaller<?>> map,
            BuiltInType primitiveType,
            BuiltInType wrapperType,
```

### BoundedWildcard
Can generalize to `? super BuiltInMarshaller`
in `modules/network/src/main/java/org/apache/ignite/internal/network/serialization/marshal/BuiltInNonContainerMarshallers.java`
#### Snippet
```java

    private static <T> void addPrimitiveAndWrapper(
            Map<Integer, BuiltInMarshaller<?>> map,
            BuiltInType primitiveType,
            BuiltInType wrapperType,
```

### BoundedWildcard
Can generalize to `? super Property`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/api/SessionBuilderImpl.java`
#### Snippet
```java
     * @param props Initial properties.
     */
    SessionBuilderImpl(QueryProcessor qryProc, Map<Property<?>, Object> props) {
        this.qryProc = qryProc;
        this.props = props;
```

### BoundedWildcard
Can generalize to `? extends List`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/api/SessionImpl.java`
#### Snippet
```java
    }

    private static void validateDmlResult(AsyncCursor.BatchedResult<List<Object>> page) {
        if (page == null
                || page.items() == null
```

### BoundedWildcard
Can generalize to `? extends IgniteRel`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rel/IgniteExchange.java`
#### Snippet
```java
    /** {@inheritDoc} */
    @Override
    public IgniteRel clone(RelOptCluster cluster, List<IgniteRel> inputs) {
        return new IgniteExchange(cluster, getTraitSet(), sole(inputs), distribution);
    }
```

### BoundedWildcard
Can generalize to `? extends IgniteRel`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rel/IgniteLimit.java`
#### Snippet
```java
    /** {@inheritDoc} */
    @Override
    public IgniteRel clone(RelOptCluster cluster, List<IgniteRel> inputs) {
        return new IgniteLimit(cluster, getTraitSet(), sole(inputs), offset, fetch);
    }
```

### BoundedWildcard
Can generalize to `? extends IgniteRel`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rel/IgniteProject.java`
#### Snippet
```java
    /** {@inheritDoc} */
    @Override
    public IgniteRel clone(RelOptCluster cluster, List<IgniteRel> inputs) {
        return new IgniteProject(cluster, getTraitSet(), sole(inputs), getProjects(), getRowType());
    }
```

### BoundedWildcard
Can generalize to `? extends IgniteRel`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rel/IgniteHashIndexSpool.java`
#### Snippet
```java

    @Override
    public IgniteRel clone(RelOptCluster cluster, List<IgniteRel> inputs) {
        return new IgniteHashIndexSpool(cluster, getTraitSet(), inputs.get(0), searchRow, cond, allowNulls);
    }
```

### BoundedWildcard
Can generalize to `? extends Map`>>
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/SqlQueryProcessor.java`
#### Snippet
```java
            TxManager txManager,
            DistributionZoneManager distributionZoneManager,
            Supplier<Map<String, Map<String, Class<?>>>> dataStorageFieldsSupplier,
            ReplicaService replicaService,
            HybridClock clock
```

### BoundedWildcard
Can generalize to `? extends IgniteRel`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rel/IgniteSender.java`
#### Snippet
```java
    /** {@inheritDoc} */
    @Override
    public IgniteRel clone(RelOptCluster cluster, List<IgniteRel> inputs) {
        return new IgniteSender(cluster, getTraitSet(), sole(inputs), exchangeId(), targetFragmentId(), distribution());
    }
```

### BoundedWildcard
Can generalize to `? extends IgniteRel`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rel/IgniteTableSpool.java`
#### Snippet
```java

    @Override
    public IgniteRel clone(RelOptCluster cluster, List<IgniteRel> inputs) {
        return new IgniteTableSpool(cluster, getTraitSet(), readType, inputs.get(0));
    }
```

### BoundedWildcard
Can generalize to `? extends IgniteRel`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rel/IgniteTableModify.java`
#### Snippet
```java
    /** {@inheritDoc} */
    @Override
    public IgniteRel clone(RelOptCluster cluster, List<IgniteRel> inputs) {
        return new IgniteTableModify(cluster, getTraitSet(), getTable(), sole(inputs),
                getOperation(), getUpdateColumnList(), getSourceExpressionList(), isFlattened());
```

### BoundedWildcard
Can generalize to `? extends IgniteRel`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rel/IgniteNestedLoopJoin.java`
#### Snippet
```java
    /** {@inheritDoc} */
    @Override
    public IgniteRel clone(RelOptCluster cluster, List<IgniteRel> inputs) {
        return new IgniteNestedLoopJoin(cluster, getTraitSet(), inputs.get(0), inputs.get(1), getCondition(),
                getVariablesSet(), getJoinType());
```

### BoundedWildcard
Can generalize to `? extends IgniteRel`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rel/IgniteTrimExchange.java`
#### Snippet
```java
    /** {@inheritDoc} */
    @Override
    public IgniteRel clone(RelOptCluster cluster, List<IgniteRel> inputs) {
        return new IgniteTrimExchange(sourceId, cluster, getTraitSet(), sole(inputs), getDistribution());
    }
```

### BoundedWildcard
Can generalize to `? extends IgniteRel`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rel/IgniteSortedIndexSpool.java`
#### Snippet
```java
     */
    @Override
    public IgniteRel clone(RelOptCluster cluster, List<IgniteRel> inputs) {
        return new IgniteSortedIndexSpool(cluster, getTraitSet(), inputs.get(0), collation, condition, searchBounds);
    }
```

### BoundedWildcard
Can generalize to `? extends IgniteRel`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rel/IgniteCorrelatedNestedLoopJoin.java`
#### Snippet
```java
    /** {@inheritDoc} */
    @Override
    public IgniteRel clone(RelOptCluster cluster, List<IgniteRel> inputs) {
        return new IgniteCorrelatedNestedLoopJoin(cluster, getTraitSet(), inputs.get(0), inputs.get(1), getCondition(),
                getVariablesSet(), getJoinType());
```

### BoundedWildcard
Can generalize to `? extends IgniteRel`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rel/IgniteFilter.java`
#### Snippet
```java
    /** {@inheritDoc} */
    @Override
    public IgniteRel clone(RelOptCluster cluster, List<IgniteRel> inputs) {
        return new IgniteFilter(cluster, getTraitSet(), sole(inputs), getCondition());
    }
```

### BoundedWildcard
Can generalize to `? extends IgniteRel`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rel/IgniteSort.java`
#### Snippet
```java
    /** {@inheritDoc} */
    @Override
    public IgniteRel clone(RelOptCluster cluster, List<IgniteRel> inputs) {
        return new IgniteSort(cluster, getTraitSet(), sole(inputs), collation, offset, fetch);
    }
```

### BoundedWildcard
Can generalize to `? extends IgniteRel`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rel/IgniteMergeJoin.java`
#### Snippet
```java
    /** {@inheritDoc} */
    @Override
    public IgniteRel clone(RelOptCluster cluster, List<IgniteRel> inputs) {
        return new IgniteMergeJoin(cluster, getTraitSet(), inputs.get(0), inputs.get(1), getCondition(),
                getVariablesSet(), getJoinType(), leftCollation, rightCollation);
```

### BoundedWildcard
Can generalize to `? extends IgniteRel`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rel/agg/IgniteColocatedSortAggregate.java`
#### Snippet
```java
    /** {@inheritDoc} */
    @Override
    public IgniteRel clone(RelOptCluster cluster, List<IgniteRel> inputs) {
        return new IgniteColocatedSortAggregate(cluster, getTraitSet().replace(collation), sole(inputs),
                getGroupSet(), getGroupSets(), getAggCallList());
```

### BoundedWildcard
Can generalize to `? extends IgniteRel`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rel/agg/IgniteMapSortAggregate.java`
#### Snippet
```java
    /** {@inheritDoc} */
    @Override
    public IgniteRel clone(RelOptCluster cluster, List<IgniteRel> inputs) {
        return new IgniteMapSortAggregate(
                cluster,
```

### BoundedWildcard
Can generalize to `? extends IgniteRel`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rel/agg/IgniteReduceSortAggregate.java`
#### Snippet
```java
    /** {@inheritDoc} */
    @Override
    public IgniteRel clone(RelOptCluster cluster, List<IgniteRel> inputs) {
        return new IgniteReduceSortAggregate(
                cluster,
```

### BoundedWildcard
Can generalize to `? extends IgniteRel`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rel/agg/IgniteMapHashAggregate.java`
#### Snippet
```java
    /** {@inheritDoc} */
    @Override
    public IgniteRel clone(RelOptCluster cluster, List<IgniteRel> inputs) {
        return new IgniteMapHashAggregate(cluster, getTraitSet(), sole(inputs),
                getGroupSet(), getGroupSets(), getAggCallList());
```

### BoundedWildcard
Can generalize to `? extends IgniteRel`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rel/set/IgniteReduceIntersect.java`
#### Snippet
```java
    /** {@inheritDoc} */
    @Override
    public IgniteReduceIntersect clone(RelOptCluster cluster, List<IgniteRel> inputs) {
        return new IgniteReduceIntersect(cluster, getTraitSet(), sole(inputs), all, rowType);
    }
```

### BoundedWildcard
Can generalize to `? extends IgniteRel`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rel/agg/IgniteReduceHashAggregate.java`
#### Snippet
```java
    /** {@inheritDoc} */
    @Override
    public IgniteRel clone(RelOptCluster cluster, List<IgniteRel> inputs) {
        return new IgniteReduceHashAggregate(cluster, getTraitSet(), sole(inputs),
                groupSet, groupSets, aggCalls, rowType);
```

### BoundedWildcard
Can generalize to `? extends IgniteRel`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rel/agg/IgniteColocatedHashAggregate.java`
#### Snippet
```java
    /** {@inheritDoc} */
    @Override
    public IgniteRel clone(RelOptCluster cluster, List<IgniteRel> inputs) {
        return new IgniteColocatedHashAggregate(cluster, getTraitSet(), sole(inputs),
                getGroupSet(), getGroupSets(), getAggCallList());
```

### BoundedWildcard
Can generalize to `? extends IgniteRel`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rel/set/IgniteReduceMinus.java`
#### Snippet
```java
    /** {@inheritDoc} */
    @Override
    public IgniteReduceMinus clone(RelOptCluster cluster, List<IgniteRel> inputs) {
        return new IgniteReduceMinus(cluster, getTraitSet(), sole(inputs), all, rowType);
    }
```

### BoundedWildcard
Can generalize to `? super RowT`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/RowConverter.java`
#### Snippet
```java
            ExecutionContext<RowT> ectx,
            BinaryTupleSchema binarySchema,
            RowHandler<RowT> handler,
            BinaryTupleBuilder tupleBuilder,
            RowT searchRow
```

### BoundedWildcard
Can generalize to `? super Throwable`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/ExecutionContext.java`
#### Snippet
```java
     * @return a {@link CompletableFuture} representing pending task
     */
    public CompletableFuture<?> submit(RunnableX task, Consumer<Throwable> onError) {
        assert !isCancelled() : "Call submit after execution was cancelled.";

```

### BoundedWildcard
Can generalize to `? super Throwable`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/ExecutionContext.java`
#### Snippet
```java
     * @param task Query task.
     */
    public void execute(RunnableX task, Consumer<Throwable> onError) {
        if (isCancelled()) {
            return;
```

### BoundedWildcard
Can generalize to `? extends Iterable`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/TableFunctionScan.java`
#### Snippet
```java
     */
    public TableFunctionScan(
            Supplier<Iterable<Object[]>> dataSupplier,
            RowFactory<RowT> rowFactory
    ) {
```

### BoundedWildcard
Can generalize to `? extends RowT`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/TableFunctionScan.java`
#### Snippet
```java
    public TableFunctionScan(
            Supplier<Iterable<Object[]>> dataSupplier,
            RowFactory<RowT> rowFactory
    ) {
        this.dataSupplier = dataSupplier;
```

### BoundedWildcard
Can generalize to `? extends Parameter`
in `modules/bytecode/src/main/java/com/facebook/presto/bytecode/ClassDefinition.java`
#### Snippet
```java
    public MethodDefinition declareConstructor(
        EnumSet<Access> access,
        Collection<Parameter> parameters) {
        return declareMethod(access, "<init>", ParameterizedType.type(void.class), List.copyOf(parameters));
    }
```

### BoundedWildcard
Can generalize to `? extends ParameterizedType`
in `modules/bytecode/src/main/java/com/facebook/presto/bytecode/ClassDefinition.java`
#### Snippet
```java
    public static String genericClassSignature(
        ParameterizedType classType,
        List<ParameterizedType> interfaceTypes) {

        return Stream.concat(Stream.of(classType), interfaceTypes.stream())
```

### BoundedWildcard
Can generalize to `? extends RowT`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/RuntimeSortedIndex.java`
#### Snippet
```java
         * @param upperInclude {@code True} for inclusive upper bound.
         */
        CursorImpl(List<RowT> rows, @Nullable RowT lower, @Nullable RowT upper, boolean lowerInclude, boolean upperInclude) {
            this.rows = rows;
            this.upper = upper;
```

### BoundedWildcard
Can generalize to `? extends RowT`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/RuntimeSortedIndex.java`
#### Snippet
```java
         * @return Lower bound position in the list.
         */
        private int lowerBound(List<RowT> rows, RowT bound, boolean includeBound) {
            int low = 0;
            int high = rows.size() - 1;
```

### BoundedWildcard
Can generalize to `? extends Iterator`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/AsyncWrapper.java`
#### Snippet
```java
     * @param exec An executor to delegate execution.
     */
    public AsyncWrapper(CompletableFuture<Iterator<T>> initFut, Executor exec) {
        this.cursorFut = initFut;
        this.exec = exec;
```

### BoundedWildcard
Can generalize to `? extends RelDataType`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/RowHandler.java`
#### Snippet
```java
     * TODO Documentation https://issues.apache.org/jira/browse/IGNITE-15859
     */
    default RowFactory<RowT> factory(IgniteTypeFactory typeFactory, List<RelDataType> fieldTypes) {
        Type[] types = new Type[fieldTypes.size()];
        for (int i = 0; i < fieldTypes.size(); i++) {
```

### BoundedWildcard
Can generalize to `? extends RowT`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/RuntimeHashIndex.java`
#### Snippet
```java
         * @param searchRow Search row.
         */
        IndexScan(Supplier<RowT> searchRow, @Nullable Predicate<RowT> filter) {
            this.searchRow = searchRow;
            this.filter = filter;
```

### BoundedWildcard
Can generalize to `? extends NodeWithTerm`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/ExecutionServiceImpl.java`
#### Snippet
```java
                }

                private void enlist(UUID tableId, List<NodeWithTerm> assignments) {
                    if (assignments.isEmpty()) {
                        return;
```

### BoundedWildcard
Can generalize to `? extends RexNode`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/RexExecutorImpl.java`
#### Snippet
```java
    private static String compile(
            RexBuilder rexBuilder,
            List<RexNode> constExps,
            RexToLixTranslator.InputGetter getter,
            RelDataType rowType
```

### BoundedWildcard
Can generalize to `? extends Expression`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/ConverterUtils.java`
#### Snippet
```java
     */
    static List<Expression> convertAssignableTypes(Class<?>[] targetTypes,
            List<Expression> arguments) {
        final List<Expression> list = new ArrayList<>();
        if (targetTypes.length == arguments.size()) {
```

### BoundedWildcard
Can generalize to `? extends Expression`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/ConverterUtils.java`
#### Snippet
```java

    static List<Expression> fromInternal(Class<?>[] targetTypes,
            List<Expression> expressions) {
        final List<Expression> list = new ArrayList<>();
        if (targetTypes.length == expressions.size()) {
```

### BoundedWildcard
Can generalize to `? super RowT`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/RootNode.java`
#### Snippet
```java
     * @param converter Output rows converter.
     */
    public RootNode(ExecutionContext<RowT> ctx, Function<RowT, RowT> converter) {
        super(ctx);

```

### BoundedWildcard
Can generalize to `? extends RowT`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/RootNode.java`
#### Snippet
```java
     * @param converter Output rows converter.
     */
    public RootNode(ExecutionContext<RowT> ctx, Function<RowT, RowT> converter) {
        super(ctx);

```

### BoundedWildcard
Can generalize to `? super RowT`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/RootNode.java`
#### Snippet
```java
     * @param onClose Runnable.
     */
    public RootNode(ExecutionContext<RowT> ctx, Function<RowT, RowT> converter, Runnable onClose) {
        super(ctx);

```

### BoundedWildcard
Can generalize to `? extends RowT`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/RootNode.java`
#### Snippet
```java
     * @param onClose Runnable.
     */
    public RootNode(ExecutionContext<RowT> ctx, Function<RowT, RowT> converter, Runnable onClose) {
        super(ctx);

```

### BoundedWildcard
Can generalize to `? extends RelDataType`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/agg/AccumulatorsFactory.java`
#### Snippet
```java
    }

    private static Function<Object, Object> cast0(Pair<RelDataType, RelDataType> types) {
        IgniteTypeFactory typeFactory = Commons.typeFactory();

```

### BoundedWildcard
Can generalize to `? extends RelDataType`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/agg/AccumulatorsFactory.java`
#### Snippet
```java
    }

    private static Function<Object, Object> cast0(Pair<RelDataType, RelDataType> types) {
        IgniteTypeFactory typeFactory = Commons.typeFactory();

```

### BoundedWildcard
Can generalize to `? extends PartitionWithTerm`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/TableScanNode.java`
#### Snippet
```java
            RowHandler.RowFactory<RowT> rowFactory,
            IgniteTable schemaTable,
            Collection<PartitionWithTerm> partsWithTerms,
            @Nullable Predicate<RowT> filters,
            @Nullable Function<RowT, RowT> rowTransformer,
```

### BoundedWildcard
Can generalize to `? super RowT`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/ProjectNode.java`
#### Snippet
```java
     * @param prj Projection.
     */
    public ProjectNode(ExecutionContext<RowT> ctx, Function<RowT, RowT> prj) {
        super(ctx);

```

### BoundedWildcard
Can generalize to `? extends RowT`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/ProjectNode.java`
#### Snippet
```java
     * @param prj Projection.
     */
    public ProjectNode(ExecutionContext<RowT> ctx, Function<RowT, RowT> prj) {
        super(ctx);

```

### BoundedWildcard
Can generalize to `? super InRowT`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/AsyncRootNode.java`
#### Snippet
```java
     * @param converter A converter to convert rows from an internal format to desired output format.
     */
    public AsyncRootNode(AbstractNode<InRowT> source, Function<InRowT, OutRowT> converter) {
        this.source = source;
        this.converter = converter;
```

### BoundedWildcard
Can generalize to `? extends OutRowT`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/AsyncRootNode.java`
#### Snippet
```java
     * @param converter A converter to convert rows from an internal format to desired output format.
     */
    public AsyncRootNode(AbstractNode<InRowT> source, Function<InRowT, OutRowT> converter) {
        this.source = source;
        this.converter = converter;
```

### BoundedWildcard
Can generalize to `? extends RexNode`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/RexToLixTranslator.java`
#### Snippet
```java
     */
    private void implementRecursively(final RexToLixTranslator currentTranslator,
            final List<RexNode> operandList, final ParameterExpression valueVariable, int pos) {
        final BlockBuilder currentBlockBuilder = currentTranslator.getBlockBuilder();
        final List<Type> storageTypes = ConverterUtils.internalTypes(operandList);
```

### BoundedWildcard
Can generalize to `? super RowT`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/ModifyNode.java`
#### Snippet
```java
    private static <RowT> void injectDefaults(
            TableDescriptor tableDescriptor,
            RowHandler<RowT> handler,
            List<RowT> rows
    ) {
```

### BoundedWildcard
Can generalize to `? extends RowT`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/ModifyNode.java`
#### Snippet
```java
            TableDescriptor tableDescriptor,
            RowHandler<RowT> handler,
            List<RowT> rows
    ) {
        for (RowT row : rows) {
```

### BoundedWildcard
Can generalize to `? extends RowT`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/ModifyNode.java`
#### Snippet
```java

    /** See {@link #mapping(TableDescriptor, List)}. */
    private void inlineUpdates(int offset, List<RowT> rows) {
        if (mapping == null) {
            // the node inserts or deletes rows only
```

### BoundedWildcard
Can generalize to `? extends RowT`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/IndexScanNode.java`
#### Snippet
```java
    }

    private Publisher<RowT> partitionPublisher(PartitionWithTerm partWithTerm, @Nullable RangeCondition<RowT> cond) {
        Publisher<BinaryRow> pub;
        TxAttributes txAttributes = context().txAttributes();
```

### BoundedWildcard
Can generalize to `? extends PartitionWithTerm`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/IndexScanNode.java`
#### Snippet
```java
    }

    private Publisher<RowT> indexPublisher(Collection<PartitionWithTerm> partsWithTerms, @Nullable RangeCondition<RowT> cond) {
        Iterator<Publisher<? extends RowT>> it = new TransformingIterator<>(
                partsWithTerms.iterator(),
```

### BoundedWildcard
Can generalize to `? extends BinaryRow`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/StorageScanNode.java`
#### Snippet
```java
     * @return Proxy publisher with conversion from {@code BinaryRow} to {@code RowT}.
     */
    protected Publisher<RowT> convertPublisher(Publisher<BinaryRow> pub) {
        Publisher<RowT> convPub = downstream -> {
            // BinaryRow -> RowT converter.
```

### BoundedWildcard
Can generalize to `? super RowT`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/StorageScanNode.java`
#### Snippet
```java
            RowHandler.RowFactory<RowT> rowFactory,
            IgniteTable schemaTable,
            @Nullable Predicate<RowT> filters,
            @Nullable Function<RowT, RowT> rowTransformer,
            @Nullable BitSet requiredColumns
```

### BoundedWildcard
Can generalize to `? super RowT`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/StorageScanNode.java`
#### Snippet
```java
            IgniteTable schemaTable,
            @Nullable Predicate<RowT> filters,
            @Nullable Function<RowT, RowT> rowTransformer,
            @Nullable BitSet requiredColumns
    ) {
```

### BoundedWildcard
Can generalize to `? extends RowT`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/StorageScanNode.java`
#### Snippet
```java
            IgniteTable schemaTable,
            @Nullable Predicate<RowT> filters,
            @Nullable Function<RowT, RowT> rowTransformer,
            @Nullable BitSet requiredColumns
    ) {
```

### BoundedWildcard
Can generalize to `? super RowT`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/SortNode.java`
#### Snippet
```java
     */
    public SortNode(ExecutionContext<RowT> ctx,
            Comparator<RowT> comp,
            @Nullable Supplier<Integer> offset,
            @Nullable Supplier<Integer> fetch) {
```

### BoundedWildcard
Can generalize to `? extends RowT`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/NestedLoopJoinNode.java`
#### Snippet
```java
                ExecutionContext<RowT> ctx,
                BiPredicate<RowT, RowT> cond,
                RowHandler.RowFactory<RowT> leftRowFactory,
                RowHandler.RowFactory<RowT> rightRowFactory
        ) {
```

### BoundedWildcard
Can generalize to `? extends RowT`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/NestedLoopJoinNode.java`
#### Snippet
```java
                BiPredicate<RowT, RowT> cond,
                RowHandler.RowFactory<RowT> leftRowFactory,
                RowHandler.RowFactory<RowT> rightRowFactory
        ) {
            super(ctx, cond);
```

### BoundedWildcard
Can generalize to `? extends RowT`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/NestedLoopJoinNode.java`
#### Snippet
```java
                ExecutionContext<RowT> ctx,
                BiPredicate<RowT, RowT> cond,
                RowHandler.RowFactory<RowT> leftRowFactory
        ) {
            super(ctx, cond);
```

### BoundedWildcard
Can generalize to `? extends RowT`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/NestedLoopJoinNode.java`
#### Snippet
```java
                ExecutionContext<RowT> ctx,
                BiPredicate<RowT, RowT> cond,
                RowHandler.RowFactory<RowT> rightRowFactory
        ) {
            super(ctx, cond);
```

### BoundedWildcard
Can generalize to `? extends List`>
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/SortAggregateNode.java`
#### Snippet
```java
            AggregateType type,
            ImmutableBitSet grpSet,
            Supplier<List<AccumulatorWrapper<RowT>>> accFactory,
            RowFactory<RowT> rowFactory,
            Comparator<RowT> comp
```

### BoundedWildcard
Can generalize to `? super RowT`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/FilterNode.java`
#### Snippet
```java
     * @param pred Predicate.
     */
    public FilterNode(ExecutionContext<RowT> ctx, Predicate<RowT> pred) {
        super(ctx);

```

### BoundedWildcard
Can generalize to `? extends RowT`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/ExpressionFactoryImpl.java`
#### Snippet
```java
                boolean lowerInclude,
                boolean upperInclude,
                RowFactory<RowT> factory
        ) {
            this.lowerBound = lowerBound;
```

### BoundedWildcard
Can generalize to `? extends RowT`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/ExpressionFactoryImpl.java`
#### Snippet
```java
         * Constructor.
         */
        private ValuesImpl(SingleScalar scalar, RowFactory<RowT> factory) {
            this.scalar = scalar;
            this.factory = factory;
```

### BoundedWildcard
Can generalize to `? extends RexNode`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/ExpressionFactoryImpl.java`
#### Snippet
```java
    }

    private String digest(List<RexNode> nodes, RelDataType type, boolean biParam) {
        StringBuilder b = new StringBuilder();

```

### BoundedWildcard
Can generalize to `? super RangeCondition`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/ExpressionFactoryImpl.java`
#### Snippet
```java
     */
    private void expandBounds(
            List<RangeCondition<RowT>> ranges,
            List<SearchBounds> searchBounds,
            RelDataType rowType,
```

### BoundedWildcard
Can generalize to `? extends SearchBounds`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/ExpressionFactoryImpl.java`
#### Snippet
```java
    private void expandBounds(
            List<RangeCondition<RowT>> ranges,
            List<SearchBounds> searchBounds,
            RelDataType rowType,
            RowFactory<RowT> rowFactory,
```

### BoundedWildcard
Can generalize to `? extends RexNode`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/ExpressionFactoryImpl.java`
#### Snippet
```java
        }

        public Function1<String, InputGetter> build(Iterable<RexNode> nodes) {
            try {
                for (RexNode node : nodes) {
```

### BoundedWildcard
Can generalize to `? extends RowT`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/ExpressionFactoryImpl.java`
#### Snippet
```java
         * Constructor.
         */
        private ValueImpl(SingleScalar scalar, RowFactory<RowT> factory) {
            this.scalar = scalar;
            this.factory = factory;
```

### BoundedWildcard
Can generalize to `? extends RelFieldCollation`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/ExpressionFactoryImpl.java`
#### Snippet
```java
    /** {@inheritDoc} */
    @Override
    public Comparator<RowT> comparator(List<RelFieldCollation> left, List<RelFieldCollation> right, ImmutableBitSet equalNulls) {
        if (nullOrEmpty(left) || nullOrEmpty(right) || left.size() != right.size()) {
            throw new IllegalArgumentException("Both inputs should be non-empty and have the same size: left="
```

### BoundedWildcard
Can generalize to `? extends RelFieldCollation`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/ExpressionFactoryImpl.java`
#### Snippet
```java
    /** {@inheritDoc} */
    @Override
    public Comparator<RowT> comparator(List<RelFieldCollation> left, List<RelFieldCollation> right, ImmutableBitSet equalNulls) {
        if (nullOrEmpty(left) || nullOrEmpty(right) || left.size() != right.size()) {
            throw new IllegalArgumentException("Both inputs should be non-empty and have the same size: left="
```

### BoundedWildcard
Can generalize to `? super RowT`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/ExpressionFactoryImpl.java`
#### Snippet
```java
        RangeIterableImpl(
                List<RangeCondition<RowT>> ranges,
                @Nullable Comparator<RowT> comparator
        ) {
            this.ranges = ranges;
```

### BoundedWildcard
Can generalize to `? extends RexLiteral`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/ExpressionFactoryImpl.java`
#### Snippet
```java
    /** {@inheritDoc} */
    @Override
    public Iterable<RowT> values(List<RexLiteral> values, RelDataType rowType) {
        RowHandler<RowT> handler = ctx.rowHandler();
        RowFactory<RowT> factory = handler.factory(typeFactory, rowType);
```

### BoundedWildcard
Can generalize to `? extends RowT`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/ExpressionFactoryImpl.java`
#### Snippet
```java
         * @param factory Row factory.
         */
        private ProjectImpl(SingleScalar scalar, RowFactory<RowT> factory) {
            this.scalar = scalar;
            this.factory = factory;
```

### BoundedWildcard
Can generalize to `? super RowT`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/IndexSpoolNode.java`
#### Snippet
```java
    private IndexSpoolNode(
            ExecutionContext<RowT> ctx,
            RuntimeIndex<RowT> idx,
            ScanNode<RowT> scan
    ) {
```

### BoundedWildcard
Can generalize to `? extends List`>
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/HashAggregateNode.java`
#### Snippet
```java
    public HashAggregateNode(
            ExecutionContext<RowT> ctx, AggregateType type, List<ImmutableBitSet> grpSets,
            Supplier<List<AccumulatorWrapper<RowT>>> accFactory, RowFactory<RowT> rowFactory) {
        super(ctx);

```

### BoundedWildcard
Can generalize to `? extends RowT`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/HashAggregateNode.java`
#### Snippet
```java
    public HashAggregateNode(
            ExecutionContext<RowT> ctx, AggregateType type, List<ImmutableBitSet> grpSets,
            Supplier<List<AccumulatorWrapper<RowT>>> accFactory, RowFactory<RowT> rowFactory) {
        super(ctx);

```

### BoundedWildcard
Can generalize to `? super RowT`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/Inbox.java`
#### Snippet
```java
            MailboxRegistry registry,
            Collection<String> srcNodeNames,
            @Nullable Comparator<RowT> comp,
            long exchangeId,
            long srcFragmentId
```

### BoundedWildcard
Can generalize to `? super RowT`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/CorrelatedNestedLoopJoinNode.java`
#### Snippet
```java
     * @param rightRowFactory Right row factory.
     */
    public CorrelatedNestedLoopJoinNode(ExecutionContext<RowT> ctx, BiPredicate<RowT, RowT> cond,
            Set<CorrelationId> correlationIds, JoinRelType joinType, RowFactory<RowT> rightRowFactory) {
        super(ctx);
```

### BoundedWildcard
Can generalize to `? super RowT`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/CorrelatedNestedLoopJoinNode.java`
#### Snippet
```java
     * @param rightRowFactory Right row factory.
     */
    public CorrelatedNestedLoopJoinNode(ExecutionContext<RowT> ctx, BiPredicate<RowT, RowT> cond,
            Set<CorrelationId> correlationIds, JoinRelType joinType, RowFactory<RowT> rightRowFactory) {
        super(ctx);
```

### BoundedWildcard
Can generalize to `? extends RowT`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/MergeJoinNode.java`
#### Snippet
```java
                ExecutionContext<RowT> ctx,
                Comparator<RowT> comp,
                RowHandler.RowFactory<RowT> leftRowFactory
        ) {
            super(ctx, comp);
```

### BoundedWildcard
Can generalize to `? extends RowT`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/MergeJoinNode.java`
#### Snippet
```java
                ExecutionContext<RowT> ctx,
                Comparator<RowT> comp,
                RowHandler.RowFactory<RowT> rightRowFactory
        ) {
            super(ctx, comp);
```

### BoundedWildcard
Can generalize to `? extends RowT`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/MergeJoinNode.java`
#### Snippet
```java
         */
        private FullOuterJoin(ExecutionContext<RowT> ctx, Comparator<RowT> comp,
                RowHandler.RowFactory<RowT> leftRowFactory, RowHandler.RowFactory<RowT> rightRowFactory) {
            super(ctx, comp);

```

### BoundedWildcard
Can generalize to `? extends RowT`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/MergeJoinNode.java`
#### Snippet
```java
         */
        private FullOuterJoin(ExecutionContext<RowT> ctx, Comparator<RowT> comp,
                RowHandler.RowFactory<RowT> leftRowFactory, RowHandler.RowFactory<RowT> rightRowFactory) {
            super(ctx, comp);

```

### BoundedWildcard
Can generalize to `? extends RexNode`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rule/LogicalScanConverterRule.java`
#### Snippet
```java
    /** Creates column mapping regarding the projection. */
    public static Mappings.TargetMapping createMapping(
            List<RexNode> projects,
            ImmutableBitSet requiredColumns,
            int tableRowSize
```

### BoundedWildcard
Can generalize to `? extends T`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rule/AbstractIgniteConverterRule.java`
#### Snippet
```java
     * TODO Documentation https://issues.apache.org/jira/browse/IGNITE-15859
     */
    protected AbstractIgniteConverterRule(Class<T> clazz, String descriptionPrefix) {
        super(Config.INSTANCE
                .withConversion(clazz, Convention.NONE, IgniteConvention.INSTANCE, descriptionPrefix));
```

### BoundedWildcard
Can generalize to `? extends RexNode`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rule/logical/LogicalOrToUnionRule.java`
#### Snippet
```java
     * @param operands Operands from OR expression.
     */
    private boolean idxCollationCheck(RelOptRuleCall call, List<RexNode> operands) {
        final IgniteLogicalTableScan scan = call.rel(0);

```

### BoundedWildcard
Can generalize to `? super T`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/util/HashFunctionFactoryImpl.java`
#### Snippet
```java
        private final RowHandler<T> rowHandler;

        SimpleHashFunction(int[] fields, RowHandler<T> rowHandler) {
            this.fields = fields;
            this.rowHandler = rowHandler;
```

### BoundedWildcard
Can generalize to `? super T`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/util/HashFunctionFactoryImpl.java`
#### Snippet
```java
        private final RowHandler<T> rowHandler;

        TypesAwareHashFunction(int[] fields, NativeType[] fieldTypes, RowHandler<T> rowHandler) {
            this.fields = fields;
            this.fieldTypes = fieldTypes;
```

### BoundedWildcard
Can generalize to `? extends AggregateCall`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/util/PlanUtils.java`
#### Snippet
```java
     * @return {@code true} If found, {@code false} otherwise.
     */
    public static boolean complexDistinctAgg(List<AggregateCall> aggCalls) {
        for (AggregateCall call : aggCalls) {
            if (call.isDistinct()
```

### BoundedWildcard
Can generalize to `? extends Expression`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/RexImpTable.java`
#### Snippet
```java
        @Override
        Expression implementSafe(RexToLixTranslator translator,
                RexCall call, List<Expression> argValueList) {
            final Expression expression;
            Class clazz = method.getDeclaringClass();
```

### BoundedWildcard
Can generalize to `? extends Expression`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/RexImpTable.java`
#### Snippet
```java
        @Override
        Expression implementSafe(final RexToLixTranslator translator,
                final RexCall call, final List<Expression> argValueList) {
            return Expressions.equal(argValueList.get(0), TRUE_EXPR);
        }
```

### BoundedWildcard
Can generalize to `? extends Expression`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/RexImpTable.java`
#### Snippet
```java
        @Override
        Expression implementSafe(RexToLixTranslator translator,
                RexCall call, List<Expression> argValueList) {
            final Expression argValue = argValueList.get(0);

```

### BoundedWildcard
Can generalize to `? extends Expression`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/RexImpTable.java`
#### Snippet
```java
        @Override
        Expression implementSafe(RexToLixTranslator translator,
                RexCall call, List<Expression> argValueList) {
            return EnumUtils.call(
                    null,
```

### BoundedWildcard
Can generalize to `? extends RexToLixTranslator.Result`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/RexImpTable.java`
#### Snippet
```java
        @Override
        public RexToLixTranslator.Result implement(final RexToLixTranslator translator,
                final RexCall call, final List<RexToLixTranslator.Result> arguments) {
            final List<Expression> argIsNullList = new ArrayList<>();
            for (RexToLixTranslator.Result result : arguments) {
```

### BoundedWildcard
Can generalize to `? extends Expression`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/RexImpTable.java`
#### Snippet
```java
        @Override
        Expression implementSafe(final RexToLixTranslator translator,
                final RexCall call, final List<Expression> argValueList) {
            Expression operand = argValueList.get(0);
            final RelDataType type = call.operands.get(0).getType();
```

### BoundedWildcard
Can generalize to `? extends Expression`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/RexImpTable.java`
#### Snippet
```java
        @Override
        Expression implementSafe(final RexToLixTranslator translator,
                final RexCall call, final List<Expression> argValueList) {
            final RexNode operand0 = call.getOperands().get(0);
            Expression trop0 = argValueList.get(0);
```

### BoundedWildcard
Can generalize to `? extends Expression`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/RexImpTable.java`
#### Snippet
```java
        @Override
        Expression implementSafe(final RexToLixTranslator translator,
                final RexCall call, final List<Expression> argValueList) {
            return Expressions.equal(argValueList.get(0), FALSE_EXPR);
        }
```

### BoundedWildcard
Can generalize to `? extends RexToLixTranslator.Result`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/RexImpTable.java`
#### Snippet
```java
                final RexToLixTranslator translator,
                final RexCall call,
                final List<RexToLixTranslator.Result> arguments) {
            final List<Expression> argIsNullList = new ArrayList<>();
            final List<Expression> argValueList = new ArrayList<>();
```

### BoundedWildcard
Can generalize to `? extends Expression`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/RexImpTable.java`
#### Snippet
```java
        @Override
        Expression implementSafe(final RexToLixTranslator translator,
                final RexCall call, final List<Expression> argValueList) {
            final boolean strict = !translator.conformance.allowExtendedTrim();
            final Object value = translator.getLiteralValue(argValueList.get(0));
```

### BoundedWildcard
Can generalize to `? extends Expression`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/RexImpTable.java`
#### Snippet
```java
        @Override
        Expression implementSafe(final RexToLixTranslator translator,
                final RexCall call, final List<Expression> argValueList) {
            assert call.getOperands().size() == 1;
            return argValueList.get(0);
```

### BoundedWildcard
Can generalize to `? extends Expression`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/RexImpTable.java`
#### Snippet
```java
        }

        private Expression callBackupMethodAnyType(List<Expression> expressions) {
            final String backupMethodNameForAnyType =
                    backupMethodName + METHOD_POSTFIX_FOR_ANY_TYPE;
```

### BoundedWildcard
Can generalize to `? extends RexToLixTranslator.Result`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/RexImpTable.java`
#### Snippet
```java
        @Override
        public RexToLixTranslator.Result implement(final RexToLixTranslator translator,
                final RexCall call, final List<RexToLixTranslator.Result> arguments) {
            final List<Expression> argIsNullList = new ArrayList<>();
            for (RexToLixTranslator.Result result : arguments) {
```

### BoundedWildcard
Can generalize to `? extends Expression`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/RexImpTable.java`
#### Snippet
```java
        @Override
        Expression implementSafe(final RexToLixTranslator translator,
                final RexCall call, final List<Expression> argValueList) {
            final TimeUnitRange timeUnitRange =
                    (TimeUnitRange) translator.getLiteralValue(argValueList.get(0));
```

### BoundedWildcard
Can generalize to `? extends Expression`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/RexImpTable.java`
#### Snippet
```java
        @Override
        Expression implementSafe(RexToLixTranslator translator,
                final RexCall call, final List<Expression> argValueList) {
            SqlKind kind = call.getOperator().getKind();
            final BlockBuilder blockBuilder = translator.getBlockBuilder();
```

### BoundedWildcard
Can generalize to `? extends Expression`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/RexImpTable.java`
#### Snippet
```java
        /** {@inheritDoc} */
        @Override Expression implementSafe(RexToLixTranslator translator,
                RexCall call, List<Expression> argValueList) {
            assert argValueList.size() == 2;
            // Add extra parameter (caseSensitive boolean flag), required by SqlFunctions#posixRegex.
```

### BoundedWildcard
Can generalize to `? extends Expression`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/RexImpTable.java`
#### Snippet
```java
        @Override
        Expression implementSafe(final RexToLixTranslator translator,
                final RexCall call, final List<Expression> argValueList) {
            switch (call.getOperands().size()) {
                case 1:
```

### BoundedWildcard
Can generalize to `? extends Expression`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/RexImpTable.java`
#### Snippet
```java
        @Override
        Expression implementSafe(final RexToLixTranslator translator,
                final RexCall call, final List<Expression> argValueList) {
            return Expressions.call(BuiltInMethod.NOT.method, argValueList);
        }
```

### BoundedWildcard
Can generalize to `? extends Expression`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/RexImpTable.java`
#### Snippet
```java
        @Override
        Expression implementSafe(final RexToLixTranslator translator,
                final RexCall call, final List<Expression> argValueList) {
            return Expressions.notEqual(argValueList.get(0), TRUE_EXPR);
        }
```

### BoundedWildcard
Can generalize to `? extends Expression`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/RexImpTable.java`
#### Snippet
```java
        @Override
        Expression implementSafe(final RexToLixTranslator translator,
                final RexCall call, final List<Expression> argValueList) {
            return Expressions.equal(argValueList.get(0), NULL_EXPR);
        }
```

### BoundedWildcard
Can generalize to `? extends Expression`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/RexImpTable.java`
#### Snippet
```java

        private Expression implementRecurse(RexToLixTranslator translator,
                final List<Expression> argValueList) {
            if (argValueList.size() == 1) {
                return argValueList.get(0);
```

### BoundedWildcard
Can generalize to `? extends Expression`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/RexImpTable.java`
#### Snippet
```java
        @Override
        Expression implementSafe(final RexToLixTranslator translator,
                final RexCall call, final List<Expression> argValueList) {
            return Expressions.notEqual(argValueList.get(0), NULL_EXPR);
        }
```

### BoundedWildcard
Can generalize to `? extends Expression`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/RexImpTable.java`
#### Snippet
```java
        @Override
        Expression implementSafe(final RexToLixTranslator translator,
                final RexCall call, final List<Expression> argValueList) {
            return Expressions.notEqual(argValueList.get(0), FALSE_EXPR);
        }
```

### BoundedWildcard
Can generalize to `? extends Expression`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/RexImpTable.java`
#### Snippet
```java
        @Override
        Expression implementSafe(final RexToLixTranslator translator,
                final RexCall call, final List<Expression> argValueList) {
            assert call.getOperands().size() == 1;
            final RelDataType sourceType = call.getOperands().get(0).getType();
```

### BoundedWildcard
Can generalize to `? super ClusterNode`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/util/NodeLeaveHandler.java`
#### Snippet
```java
     * TODO Documentation https://issues.apache.org/jira/browse/IGNITE-15859
     */
    public NodeLeaveHandler(Consumer<ClusterNode> onDisappeared) {
        this.onDisappeared = onDisappeared;
    }
```

### BoundedWildcard
Can generalize to `? extends RelHint`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/util/HintUtils.java`
#### Snippet
```java
     * TODO Documentation https://issues.apache.org/jira/browse/IGNITE-15859
     */
    public static boolean containsDisabledRules(List<RelHint> hints) {
        return hints.stream()
                .anyMatch(h -> "DISABLE_RULE".equals(h.hintName) && !h.listOptions.isEmpty());
```

### BoundedWildcard
Can generalize to `? extends RelHint`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/util/HintUtils.java`
#### Snippet
```java
     * TODO Documentation https://issues.apache.org/jira/browse/IGNITE-15859
     */
    public static Set<String> disabledRules(List<RelHint> hints) {
        if (nullOrEmpty(hints)) {
            return Collections.emptySet();
```

### BoundedWildcard
Can generalize to `? super Class`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/util/Primitives.java`
#### Snippet
```java

    private static void add(
            Map<Class<?>, Class<?>> forward,
            Map<Class<?>, Class<?>> backward,
            Class<?> key,
```

### BoundedWildcard
Can generalize to `? super Class`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/util/Primitives.java`
#### Snippet
```java

    private static void add(
            Map<Class<?>, Class<?>> forward,
            Map<Class<?>, Class<?>> backward,
            Class<?> key,
```

### BoundedWildcard
Can generalize to `? super Class`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/util/Primitives.java`
#### Snippet
```java
    private static void add(
            Map<Class<?>, Class<?>> forward,
            Map<Class<?>, Class<?>> backward,
            Class<?> key,
            Class<?> value) {
```

### BoundedWildcard
Can generalize to `? super Class`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/util/Primitives.java`
#### Snippet
```java
    private static void add(
            Map<Class<?>, Class<?>> forward,
            Map<Class<?>, Class<?>> backward,
            Class<?> key,
            Class<?> value) {
```

### BoundedWildcard
Can generalize to `? extends RelDataType`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/type/IgniteTypeFactory.java`
#### Snippet
```java
    }

    private boolean allEquals(List<RelDataType> types) {
        assert types.size() > 1;

```

### BoundedWildcard
Can generalize to `? extends List`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/trait/Partitioned.java`
#### Snippet
```java
     * TODO Documentation https://issues.apache.org/jira/browse/IGNITE-15859
     */
    public Partitioned(List<List<String>> assignments, RowHashFunction<RowT> partFun) {
        this.assignments = assignments;
        this.partFun = partFun;
```

### BoundedWildcard
Can generalize to `? super RowT`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/trait/Partitioned.java`
#### Snippet
```java
     * TODO Documentation https://issues.apache.org/jira/browse/IGNITE-15859
     */
    public Partitioned(List<List<String>> assignments, RowHashFunction<RowT> partFun) {
        this.assignments = assignments;
        this.partFun = partFun;
```

### BoundedWildcard
Can generalize to `? extends ResultSetMetadata`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/prepare/LazyResultSetMetadata.java`
#### Snippet
```java
    private volatile ResultSetMetadata metadata;

    public LazyResultSetMetadata(Supplier<ResultSetMetadata> columnMetadataProvider) {
        this.columnMetadataProvider = columnMetadataProvider;
    }
```

### BoundedWildcard
Can generalize to `? extends Fragment`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/prepare/QueryTemplate.java`
#### Snippet
```java
    }

    private List<Fragment> replace(List<Fragment> fragments, Fragment fragment, List<Fragment> replacement) {
        assert !nullOrEmpty(replacement);

```

### BoundedWildcard
Can generalize to `? extends Fragment`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/prepare/QueryTemplate.java`
#### Snippet
```java
    }

    private List<Fragment> replace(List<Fragment> fragments, Fragment fragment, List<Fragment> replacement) {
        assert !nullOrEmpty(replacement);

```

### BoundedWildcard
Can generalize to `? extends Fragment`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/prepare/QueryTemplate.java`
#### Snippet
```java

    @NotNull
    private List<Fragment> map(List<Fragment> fragments, MappingQueryContext ctx, RelMetadataQuery mq) {
        List<Fragment> frgs = new ArrayList<>();

```

### BoundedWildcard
Can generalize to `? extends IgniteSchema`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/schema/SqlSchemaManagerImpl.java`
#### Snippet
```java
     * @param schemas Ignite schemas.
     */
    private SchemaPlus rebuild(Map<String, IgniteSchema> schemas) {
        SchemaPlus newCalciteSchema = Frameworks.createRootSchema(false);

```

### BoundedWildcard
Can generalize to `? super RuleSet`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/prepare/PlanningContext.java`
#### Snippet
```java
     * Set rules filter.
     */
    public void rulesFilter(Function<RuleSet, RuleSet> rulesFilter) {
        this.rulesFilter = rulesFilter;
    }
```

### BoundedWildcard
Can generalize to `? extends RuleSet`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/prepare/PlanningContext.java`
#### Snippet
```java
     * Set rules filter.
     */
    public void rulesFilter(Function<RuleSet, RuleSet> rulesFilter) {
        this.rulesFilter = rulesFilter;
    }
```

### BoundedWildcard
Can generalize to `? extends RexNode`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/util/RexUtils.java`
#### Snippet
```java
     * TODO Documentation https://issues.apache.org/jira/browse/IGNITE-15859
     */
    public static Set<CorrelationId> extractCorrelationIds(List<RexNode> nodes) {
        final Set<CorrelationId> cors = new HashSet<>();

```

### BoundedWildcard
Can generalize to `? extends RexNode`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/util/RexUtils.java`
#### Snippet
```java
     * TODO Documentation https://issues.apache.org/jira/browse/IGNITE-15859
     */
    public static boolean hasCorrelation(List<RexNode> nodes) {
        try {
            RexVisitor<Void> v = new RexVisitorImpl<Void>(true) {
```

### BoundedWildcard
Can generalize to `? extends RexCall`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/util/RexUtils.java`
#### Snippet
```java
    private static @Nullable SearchBounds createBounds(
            @Nullable RelFieldCollation fc, // Can be null for EQUALS condition.
            List<RexCall> collFldPreds,
            RelOptCluster cluster,
            RelDataType fldType,
```

### BoundedWildcard
Can generalize to `? extends RexNode`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/util/RexUtils.java`
#### Snippet
```java
     */
    @Deprecated(forRemoval = true)
    public static List<RexNode> asBound(RelOptCluster cluster, Iterable<RexNode> idxCond, RelDataType rowType,
            @Nullable Mappings.TargetMapping mapping) {
        if (nullOrEmpty(idxCond)) {
```

### BoundedWildcard
Can generalize to `? extends RexNode`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/util/RexUtils.java`
#### Snippet
```java
     * TODO Documentation https://issues.apache.org/jira/browse/IGNITE-15859
     */
    public static IntSet notNullKeys(List<RexNode> row) {
        if (nullOrEmpty(row)) {
            return IntSets.EMPTY_SET;
```

### BoundedWildcard
Can generalize to `? extends List`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/prepare/PrepareServiceImpl.java`
#### Snippet
```java
    private ResultSetMetadata resultSetMetadata(
            RelDataType rowType,
            @Nullable List<List<String>> origins
    ) {
        return new LazyResultSetMetadata(
```

### BoundedWildcard
Can generalize to `? super RowT`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/schema/IgniteTableImpl.java`
#### Snippet
```java

    private static <RowT> CompletableFuture<List<RowT>> handleInsertResults(
            RowHandler<RowT> handler,
            CompletableFuture<List<RowT>>[] futs
    ) {
```

### BoundedWildcard
Can generalize to `? extends T`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/util/Commons.java`
#### Snippet
```java
     * @return {@code true} if there is a permutation of all {@code elems} that is prefix of {@code seq}.
     */
    public static <T> boolean isPrefix(List<T> seq, Collection<T> elems) {
        Set<T> elems0 = new HashSet<>(elems);

```

### BoundedWildcard
Can generalize to `? extends T`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/util/Commons.java`
#### Snippet
```java
     * @return {@code true} if there is a permutation of all {@code elems} that is prefix of {@code seq}.
     */
    public static <T> boolean isPrefix(List<T> seq, Collection<T> elems) {
        Set<T> elems0 = new HashSet<>(elems);

```

### BoundedWildcard
Can generalize to `? extends T`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/util/Commons.java`
#### Snippet
```java
     * @return A List of unique entries that presented in both the given set and the given list.
     */
    public static <T> List<T> intersect(Set<T> set, List<T> list) {
        if (nullOrEmpty(set) || nullOrEmpty(list)) {
            return Collections.emptyList();
```

### BoundedWildcard
Can generalize to `? extends T`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/util/Commons.java`
#### Snippet
```java
     * Combines two lists.
     */
    public static <T> List<T> combine(List<T> left, List<T> right) {
        Set<T> set = new HashSet<>(left.size() + right.size());

```

### BoundedWildcard
Can generalize to `? extends T`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/util/Commons.java`
#### Snippet
```java
     * Combines two lists.
     */
    public static <T> List<T> combine(List<T> left, List<T> right) {
        Set<T> set = new HashSet<>(left.size() + right.size());

```

### BoundedWildcard
Can generalize to `? extends Comparator`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/util/Commons.java`
#### Snippet
```java
     * TODO Documentation https://issues.apache.org/jira/browse/IGNITE-15859
     */
    public static <T> Comparator<T> compoundComparator(Iterable<Comparator<T>> cmps) {
        return (r1, r2) -> {
            for (Comparator<T> cmp : cmps) {
```

### BoundedWildcard
Can generalize to `? extends T`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/util/Commons.java`
#### Snippet
```java
     * Transforms a given list using map function.
     */
    public static <T, R> List<R> transform(@NotNull List<T> src, @NotNull Function<T, R> mapFun) {
        if (nullOrEmpty(src)) {
            return Collections.emptyList();
```

### BoundedWildcard
Can generalize to `? super T`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/util/Commons.java`
#### Snippet
```java
     * Transforms a given list using map function.
     */
    public static <T, R> List<R> transform(@NotNull List<T> src, @NotNull Function<T, R> mapFun) {
        if (nullOrEmpty(src)) {
            return Collections.emptyList();
```

### BoundedWildcard
Can generalize to `? extends R`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/util/Commons.java`
#### Snippet
```java
     * Transforms a given list using map function.
     */
    public static <T, R> List<R> transform(@NotNull List<T> src, @NotNull Function<T, R> mapFun) {
        if (nullOrEmpty(src)) {
            return Collections.emptyList();
```

### BoundedWildcard
Can generalize to `? extends List`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/util/Commons.java`
#### Snippet
```java
     * TODO Documentation https://issues.apache.org/jira/browse/IGNITE-15859
     */
    public static <T> List<T> flat(List<List<? extends T>> src) {
        return src.stream().flatMap(List::stream).collect(Collectors.toList());
    }
```

### BoundedWildcard
Can generalize to `? extends RelTraitDef`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/trait/TraitUtils.java`
#### Snippet
```java
     * @return {@code true} if distribution found, {@code false} otherwise.
     */
    public static boolean distributionPresent(ImmutableList<RelTraitDef> traitDefs) {
        for (RelTraitDef<?> trait : traitDefs) {
            if (trait instanceof DistributionTraitDef) {
```

### BoundedWildcard
Can generalize to `? extends List`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/trait/TraitUtils.java`
#### Snippet
```java
    private static boolean fillRecursive(
            RelTraitSet outTraits,
            List<List<RelTraitSet>> inTraits,
            Set<Pair<RelTraitSet, List<RelTraitSet>>> result,
            RelTraitSet[] combination,
```

### BoundedWildcard
Can generalize to `? super Pair`>
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/trait/TraitUtils.java`
#### Snippet
```java
            RelTraitSet outTraits,
            List<List<RelTraitSet>> inTraits,
            Set<Pair<RelTraitSet, List<RelTraitSet>>> result,
            RelTraitSet[] combination,
            int idx
```

### BoundedWildcard
Can generalize to `? extends RexNode`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/trait/TraitUtils.java`
#### Snippet
```java
     * ProjectDistribution. TODO Documentation https://issues.apache.org/jira/browse/IGNITE-15859
     */
    public static IgniteDistribution projectDistribution(IgniteDistribution distribution, List<RexNode> projects,
            RelDataType inputRowType) {
        if (distribution.getType() != HASH_DISTRIBUTED) {
```

### BoundedWildcard
Can generalize to `? super ClusterNode`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/metadata/MappingServiceImpl.java`
#### Snippet
```java
    /** {@inheritDoc} */
    @Override
    public List<String> executionNodes(boolean single, @Nullable Predicate<ClusterNode> nodeFilter) {
        List<ClusterNode> nodes = new ArrayList<>(topSrvc.allMembers());

```

### BoundedWildcard
Can generalize to `? extends T`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/property/PropertiesHolderImpl.java`
#### Snippet
```java
    /** {@inheritDoc} */
    @Override
    public <T> @Nullable T getOrDefault(Property<T> prop, @Nullable T defaultValue) {
        var val = get(prop);

```

### BoundedWildcard
Can generalize to `? extends RelNode`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/externalize/RelJsonWriter.java`
#### Snippet
```java
    }

    private List<Object> explainInputs(List<RelNode> inputs) {
        final List<Object> list = relJson.list();
        for (RelNode input : inputs) {
```

### BoundedWildcard
Can generalize to `? extends Pair`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/externalize/RelJsonWriter.java`
#### Snippet
```java
    }

    private void explain_(RelNode rel, List<Pair<String, Object>> values) {
        final Map<String, Object> map = relJson.map();

```

### BoundedWildcard
Can generalize to `? extends List`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/metadata/FragmentMapping.java`
#### Snippet
```java
     * TODO Documentation https://issues.apache.org/jira/browse/IGNITE-15859
     */
    public FragmentMapping finalize(Supplier<List<String>> nodesSource) {
        if (colocationGroups.isEmpty()) {
            return this;
```

### BoundedWildcard
Can generalize to `? super IgniteUuid`
in `modules/metastorage/src/main/java/org/apache/ignite/internal/metastorage/impl/CursorPublisher.java`
#### Snippet
```java
     * @param createCursorSupplier Factory that creates a remote Meta Storage cursor (provided with a cursor ID).
     */
    CursorPublisher(MetaStorageServiceContext context, Function<IgniteUuid, WriteCommand> createCursorSupplier) {
        this.context = context;
        this.createCursorSupplier = createCursorSupplier;
```

### BoundedWildcard
Can generalize to `? extends WriteCommand`
in `modules/metastorage/src/main/java/org/apache/ignite/internal/metastorage/impl/CursorPublisher.java`
#### Snippet
```java
     * @param createCursorSupplier Factory that creates a remote Meta Storage cursor (provided with a cursor ID).
     */
    CursorPublisher(MetaStorageServiceContext context, Function<IgniteUuid, WriteCommand> createCursorSupplier) {
        this.context = context;
        this.createCursorSupplier = createCursorSupplier;
```

### BoundedWildcard
Can generalize to `? extends CompletableFuture`
in `modules/metastorage/src/main/java/org/apache/ignite/internal/metastorage/impl/MetaStorageRaftGroupEventsListener.java`
#### Snippet
```java
    }

    private CompletableFuture<Void> updateConfigUnderLock(Supplier<CompletableFuture<Void>> action) {
        if (!busyLock.enterBusy()) {
            LOG.info("Skipping Meta Storage configuration update because the node is stopping");
```

### BoundedWildcard
Can generalize to `? extends MetaStorageServiceImpl`
in `modules/metastorage/src/main/java/org/apache/ignite/internal/metastorage/impl/MetaStorageRaftGroupEventsListener.java`
#### Snippet
```java
            ClusterService clusterService,
            LogicalTopologyService logicalTopologyService,
            CompletableFuture<MetaStorageServiceImpl> metaStorageSvcFut
    ) {
        this.busyLock = busyLock;
```

### BoundedWildcard
Can generalize to `? extends Map`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/externalize/RelJsonReader.java`
#### Snippet
```java
    }

    private void readRels(List<Map<String, Object>> jsonRels) {
        for (Map<String, Object> jsonRel : jsonRels) {
            readRel(jsonRel);
```

### BoundedWildcard
Can generalize to `? super String`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/metadata/ColocationGroup.java`
#### Snippet
```java
            List<NodeWithTerm> assignment0, 
            List<NodeWithTerm> assignment1,
            Predicate<String> filter, 
            int p
    ) throws ColocationMappingException {
```

### BoundedWildcard
Can generalize to `? extends NodeWithTerm`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/metadata/ColocationGroup.java`
#### Snippet
```java
    }

    private List<NodeWithTerm> filterByNodeNames(List<NodeWithTerm> assignment, Predicate<String> filter) {
        List<NodeWithTerm> res = new ArrayList<>(assignment.size());

```

### BoundedWildcard
Can generalize to `? super String`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/metadata/ColocationGroup.java`
#### Snippet
```java
    }

    private List<NodeWithTerm> filterByNodeNames(List<NodeWithTerm> assignment, Predicate<String> filter) {
        List<NodeWithTerm> res = new ArrayList<>(assignment.size());

```

### BoundedWildcard
Can generalize to `? extends ImmutableList`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/metadata/IgniteMdCollation.java`
#### Snippet
```java
     */
    public static List<RelCollation> values(RelMetadataQuery mq,
            RelDataType rowType, ImmutableList<ImmutableList<RexLiteral>> tuples) {
        Util.discard(mq); // for future use
        final List<RelCollation> list = new ArrayList<>();
```

### BoundedWildcard
Can generalize to `? extends Entry`
in `modules/metastorage/src/main/java/org/apache/ignite/internal/metastorage/server/WatchProcessor.java`
#### Snippet
```java
     * Notifies registered watch about an update event.
     */
    public void notifyWatches(List<Entry> updatedEntries) {
        // Revision must be the same for all entries.
        long newRevision = updatedEntries.get(0).revision();
```

### BoundedWildcard
Can generalize to `? super Throwable`
in `modules/metastorage/src/main/java/org/apache/ignite/internal/metastorage/server/raft/MetaStorageLearnerListener.java`
#### Snippet
```java

    @Override
    public void onSnapshotSave(Path path, Consumer<Throwable> doneClo) {
        storage.snapshot(path).whenComplete((unused, throwable) -> doneClo.accept(throwable));
    }
```

### BoundedWildcard
Can generalize to `? extends CommandClosure`
in `modules/metastorage/src/main/java/org/apache/ignite/internal/metastorage/server/raft/MetaStorageLearnerListener.java`
#### Snippet
```java

    @Override
    public void onWrite(Iterator<CommandClosure<WriteCommand>> iter) {
        while (iter.hasNext()) {
            CommandClosure<WriteCommand> clo = iter.next();
```

### BoundedWildcard
Can generalize to `? extends WriteCommand`
in `modules/metastorage/src/main/java/org/apache/ignite/internal/metastorage/server/raft/MetaStorageWriteHandler.java`
#### Snippet
```java
     * the command requires external processing.
     */
    boolean handleWriteCommand(CommandClosure<WriteCommand> clo) {
        WriteCommand command = clo.command();

```

### BoundedWildcard
Can generalize to `? extends Map`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/externalize/RelJson.java`
#### Snippet
```java
    }

    RelCollation toCollation(List<Map<String, Object>> jsonFieldCollations) {
        if (jsonFieldCollations == null) {
            return RelCollations.EMPTY;
```

### BoundedWildcard
Can generalize to `? extends Map`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/externalize/RelJson.java`
#### Snippet
```java
    }

    List<SearchBounds> toSearchBoundList(RelInput input, List<Map<String, Object>> bounds) {
        if (bounds == null) {
            return null;
```

### BoundedWildcard
Can generalize to `? extends Map`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/externalize/RelJson.java`
#### Snippet
```java
    }

    private List<RexFieldCollation> toRexFieldCollationList(RelInput relInput, List<Map<String, Object>> order) {
        if (order == null) {
            return null;
```

### BoundedWildcard
Can generalize to `? super String`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/externalize/RelJson.java`
#### Snippet
```java
    }

    private static void register(Map<String, Enum<?>> map, Class<? extends Enum> aclass) {
        String preffix = aclass.getSimpleName() + "#";
        for (Enum enumConstant : aclass.getEnumConstants()) {
```

### BoundedWildcard
Can generalize to `? super Enum`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/externalize/RelJson.java`
#### Snippet
```java
    }

    private static void register(Map<String, Enum<?>> map, Class<? extends Enum> aclass) {
        String preffix = aclass.getSimpleName() + "#";
        for (Enum enumConstant : aclass.getEnumConstants()) {
```

### BoundedWildcard
Can generalize to `? super Throwable`
in `modules/metastorage/src/main/java/org/apache/ignite/internal/metastorage/server/raft/MetaStorageListener.java`
#### Snippet
```java

    @Override
    public void onSnapshotSave(Path path, Consumer<Throwable> doneClo) {
        storage.snapshot(path).whenComplete((unused, throwable) -> {
            doneClo.accept(throwable);
```

### BoundedWildcard
Can generalize to `? extends CommandClosure`
in `modules/metastorage/src/main/java/org/apache/ignite/internal/metastorage/server/raft/MetaStorageListener.java`
#### Snippet
```java

    @Override
    public void onRead(Iterator<CommandClosure<ReadCommand>> iter) {
        while (iter.hasNext()) {
            CommandClosure<ReadCommand> clo = iter.next();
```

### BoundedWildcard
Can generalize to `? extends CommandClosure`
in `modules/metastorage/src/main/java/org/apache/ignite/internal/metastorage/server/raft/MetaStorageListener.java`
#### Snippet
```java

    @Override
    public void onWrite(Iterator<CommandClosure<WriteCommand>> iter) {
        while (iter.hasNext()) {
            CommandClosure<WriteCommand> clo = iter.next();
```

### BoundedWildcard
Can generalize to `? extends Entry`
in `modules/metastorage/src/main/java/org/apache/ignite/internal/metastorage/impl/MetaStorageServiceImpl.java`
#### Snippet
```java
    }

    private static Map<ByteArray, Entry> multipleEntryResult(List<Entry> entries) {
        Map<ByteArray, Entry> res = IgniteUtils.newHashMap(entries.size());

```

### BoundedWildcard
Can generalize to `? extends U`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/io/AbstractDataPageIo.java`
#### Snippet
```java
     * @throws IgniteInternalCheckedException In case of error in closure body.
     */
    public <U> List<U> forAllItems(long pageAddr, Closure<U> c) throws IgniteInternalCheckedException {
        assertPageType(pageAddr);

```

### BoundedWildcard
Can generalize to `? extends Publisher`
in `modules/metastorage/src/main/java/org/apache/ignite/internal/metastorage/impl/MetaStorageManagerImpl.java`
#### Snippet
```java
        private final CompletableFuture<Publisher<T>> future;

        CompletableFuturePublisher(CompletableFuture<Publisher<T>> future) {
            this.future = future;
        }
```

### BoundedWildcard
Can generalize to `? extends Entry`
in `modules/metastorage/src/main/java/org/apache/ignite/internal/metastorage/impl/MetaStorageManagerImpl.java`
#### Snippet
```java
     * Saves processed Meta Storage revision and corresponding entries to the Vault.
     */
    private void saveUpdatedEntriesToVault(long revision, Collection<Entry> updatedEntries) {
        assert revision > appliedRevision : "Remote revision " + revision + " is greater than applied revision " + appliedRevision;

```

### BoundedWildcard
Can generalize to `? super Integer`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/freelist/io/PagesListMetaIo.java`
#### Snippet
```java
     * @param res Results map.
     */
    public void getBucketsData(long pageAddr, Map<Integer, LongArrayList> res) {
        int cnt = getCount(pageAddr);

```

### BoundedWildcard
Can generalize to `? extends Operation`
in `modules/metastorage/src/main/java/org/apache/ignite/internal/metastorage/server/persistence/RocksDbKeyValueStorage.java`
#### Snippet
```java
    }

    private void applyOperations(Collection<Operation> ops) throws RocksDBException {
        long curRev = rev + 1;

```

### BoundedWildcard
Can generalize to `? super FullPageId`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/RobinHoodBackwardShiftHashMap.java`
#### Snippet
```java
    /** {@inheritDoc} */
    @Override
    public void forEach(BiConsumer<FullPageId, Long> act) {
        for (int i = 0; i < numBuckets; i++) {
            if (isEmptyAt(i)) {
```

### BoundedWildcard
Can generalize to `? super Long`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/RobinHoodBackwardShiftHashMap.java`
#### Snippet
```java
    /** {@inheritDoc} */
    @Override
    public void forEach(BiConsumer<FullPageId, Long> act) {
        for (int i = 0; i < numBuckets; i++) {
            if (isEmptyAt(i)) {
```

### BoundedWildcard
Can generalize to `? super List`
in `modules/metastorage/src/testFixtures/java/org/apache/ignite/internal/metastorage/server/SimpleInMemoryKeyValueStorage.java`
#### Snippet
```java
            byte[] key,
            List<Long> revs,
            Map<byte[], List<Long>> compactedKeysIdx,
            Map<Long, NavigableMap<byte[], Value>> compactedRevsIdx
    ) {
```

### BoundedWildcard
Can generalize to `? super Long`
in `modules/metastorage/src/testFixtures/java/org/apache/ignite/internal/metastorage/server/SimpleInMemoryKeyValueStorage.java`
#### Snippet
```java
            List<Long> revs,
            Map<byte[], List<Long>> compactedKeysIdx,
            Map<Long, NavigableMap<byte[], Value>> compactedRevsIdx
    ) {
        Long lastRev = lastRevision(revs);
```

### BoundedWildcard
Can generalize to `? super T`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/freelist/AbstractFreeList.java`
#### Snippet
```java
                long pageId,
                long pageAddr,
                AbstractDataPageIo<T> io,
                T row,
                int rowSize
```

### BoundedWildcard
Can generalize to `? extends T`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/freelist/AbstractFreeList.java`
#### Snippet
```java
        private T next;

        CachedIterator(Iterator<T> it) {
            this.it = it;
        }
```

### BoundedWildcard
Can generalize to `? super T`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/freelist/AbstractFreeList.java`
#### Snippet
```java
                long pageId,
                long pageAddr,
                AbstractDataPageIo<T> io,
                T row,
                int written,
```

### BoundedWildcard
Can generalize to `? extends Path`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/store/FilePageStore.java`
#### Snippet
```java
     */
    public CompletableFuture<DeltaFilePageStoreIo> getOrCreateNewDeltaFile(
            IntFunction<Path> deltaFilPathFunction,
            Supplier<int[]> pageIndexesSupplier
    ) {
```

### BoundedWildcard
Can generalize to `? extends FilePageStore`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/store/FilePageStoreManager.java`
#### Snippet
```java

    private static void stopGroupFilePageStores(
            List<FilePageStore> partitionPageStores,
            boolean cleanFiles
    ) throws IgniteInternalCheckedException {
```

### BoundedWildcard
Can generalize to `? extends ByteBuffer`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/replacement/DelayedDirtyPageWrite.java`
#### Snippet
```java
    public DelayedDirtyPageWrite(
            WriteDirtyPage flushDirtyPage,
            ThreadLocal<ByteBuffer> byteBufThreadLoc,
            // TODO: IGNITE-17017 Move to common config
            int pageSize,
```

### BoundedWildcard
Can generalize to `? super GroupPartitionId`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/checkpoint/CheckpointPagesWriter.java`
#### Snippet
```java
            CheckpointMetricsTracker tracker,
            IgniteConcurrentMultiPairQueue<PersistentPageMemory, FullPageId> writePageIds,
            ConcurrentMap<GroupPartitionId, LongAdder> updatedPartitions,
            CompletableFuture<?> doneFut,
            Runnable updateHeartbeat,
```

### BoundedWildcard
Can generalize to `? extends ByteBuffer`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/checkpoint/CheckpointPagesWriter.java`
#### Snippet
```java
            CompletableFuture<?> doneFut,
            Runnable updateHeartbeat,
            ThreadLocal<ByteBuffer> threadBuf,
            CheckpointProgressImpl checkpointProgress,
            WriteDirtyPage pageWriter,
```

### BoundedWildcard
Can generalize to `? super PersistentPageMemory`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/checkpoint/CheckpointPagesWriter.java`
#### Snippet
```java
    private PageStoreWriter createPageStoreWriter(
            PersistentPageMemory pageMemory,
            Map<PersistentPageMemory, List<FullPageId>> pagesToRetry
    ) {
        return (fullPageId, buf, tag) -> {
```

### BoundedWildcard
Can generalize to `? extends FilePageStore`
in `modules/page-memory/src/testFixtures/java/org/apache/ignite/internal/pagememory/persistence/checkpoint/CheckpointTestUtils.java`
#### Snippet
```java
     */
    public static FilePageStoreManager createFilePageStoreManager(
            Map<GroupPartitionId, FilePageStore> stores
    ) throws Exception {
        FilePageStoreManager manager = mock(FilePageStoreManager.class);
```

### BoundedWildcard
Can generalize to `? extends SortedIndexColumnDescriptor`
in `modules/storage-api/src/main/java/org/apache/ignite/internal/storage/index/SortedIndexDescriptor.java`
#### Snippet
```java
    }

    private static BinaryTupleSchema createSchema(List<SortedIndexColumnDescriptor> columns) {
        Element[] elements = columns.stream()
                .map(columnDescriptor -> new Element(columnDescriptor.type(), columnDescriptor.nullable()))
```

### BoundedWildcard
Can generalize to `? extends ReadResult`
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/BaseMvStoragesTest.java`
#### Snippet
```java
    }

    protected static List<IgniteBiTuple<TestKey, TestValue>> drainToList(Cursor<ReadResult> cursor) {
        try (cursor) {
            return cursor.stream().map(BaseMvStoragesTest::unwrap).collect(Collectors.toList());
```

### BoundedWildcard
Can generalize to `? super T`
in `modules/storage-api/src/main/java/org/apache/ignite/internal/storage/util/MvPartitionStorages.java`
#### Snippet
```java
     * @throws StorageRebalanceException If the storage does not exist or another operation is already in progress.
     */
    public CompletableFuture<Void> abortRebalance(int partitionId, Function<T, CompletableFuture<Void>> abortRebalanceStorageFunction) {
        operationByPartitionId.compute(partitionId, (partId, operation) -> {
            checkStorageExistsForRebalance(partitionId);
```

### BoundedWildcard
Can generalize to `? extends CompletableFuture`
in `modules/storage-api/src/main/java/org/apache/ignite/internal/storage/util/MvPartitionStorages.java`
#### Snippet
```java
     * @throws StorageRebalanceException If the storage does not exist or another operation is already in progress.
     */
    public CompletableFuture<Void> abortRebalance(int partitionId, Function<T, CompletableFuture<Void>> abortRebalanceStorageFunction) {
        operationByPartitionId.compute(partitionId, (partId, operation) -> {
            checkStorageExistsForRebalance(partitionId);
```

### BoundedWildcard
Can generalize to `? super T`
in `modules/storage-api/src/main/java/org/apache/ignite/internal/storage/util/MvPartitionStorages.java`
#### Snippet
```java
     * @throws StorageRebalanceException If rebalancing is already in progress.
     */
    public CompletableFuture<Void> startRebalance(int partitionId, Function<T, CompletableFuture<Void>> startRebalanceStorageFunction) {
        operationByPartitionId.compute(partitionId, (partId, operation) -> {
            checkStorageExistsForRebalance(partitionId);
```

### BoundedWildcard
Can generalize to `? extends CompletableFuture`
in `modules/storage-api/src/main/java/org/apache/ignite/internal/storage/util/MvPartitionStorages.java`
#### Snippet
```java
     * @throws StorageRebalanceException If rebalancing is already in progress.
     */
    public CompletableFuture<Void> startRebalance(int partitionId, Function<T, CompletableFuture<Void>> startRebalanceStorageFunction) {
        operationByPartitionId.compute(partitionId, (partId, operation) -> {
            checkStorageExistsForRebalance(partitionId);
```

### BoundedWildcard
Can generalize to `? super T`
in `modules/storage-api/src/main/java/org/apache/ignite/internal/storage/util/MvPartitionStorages.java`
#### Snippet
```java
     * @throws StorageRebalanceException If storage rebalancing has not started.
     */
    public CompletableFuture<Void> finishRebalance(int partitionId, Function<T, CompletableFuture<Void>> finishRebalanceStorageFunction) {
        operationByPartitionId.compute(partitionId, (partId, operation) -> {
            checkStorageExistsForRebalance(partitionId);
```

### BoundedWildcard
Can generalize to `? extends CompletableFuture`
in `modules/storage-api/src/main/java/org/apache/ignite/internal/storage/util/MvPartitionStorages.java`
#### Snippet
```java
     * @throws StorageRebalanceException If storage rebalancing has not started.
     */
    public CompletableFuture<Void> finishRebalance(int partitionId, Function<T, CompletableFuture<Void>> finishRebalanceStorageFunction) {
        operationByPartitionId.compute(partitionId, (partId, operation) -> {
            checkStorageExistsForRebalance(partitionId);
```

### BoundedWildcard
Can generalize to `? super T`
in `modules/storage-api/src/main/java/org/apache/ignite/internal/storage/util/MvPartitionStorages.java`
#### Snippet
```java
     * @throws StorageRebalanceException If the storage is in the process of rebalancing.
     */
    public CompletableFuture<Void> destroy(int partitionId, Function<T, CompletableFuture<Void>> destroyStorageFunction) {
        DestroyStorageOperation destroyOp = (DestroyStorageOperation) operationByPartitionId.compute(partitionId, (partId, operation) -> {
            checkStorageExists(partitionId);
```

### BoundedWildcard
Can generalize to `? extends CompletableFuture`
in `modules/storage-api/src/main/java/org/apache/ignite/internal/storage/util/MvPartitionStorages.java`
#### Snippet
```java
     * @throws StorageRebalanceException If the storage is in the process of rebalancing.
     */
    public CompletableFuture<Void> destroy(int partitionId, Function<T, CompletableFuture<Void>> destroyStorageFunction) {
        DestroyStorageOperation destroyOp = (DestroyStorageOperation) operationByPartitionId.compute(partitionId, (partId, operation) -> {
            checkStorageExists(partitionId);
```

### BoundedWildcard
Can generalize to `? extends T`
in `modules/storage-api/src/main/java/org/apache/ignite/internal/storage/util/MvPartitionStorages.java`
#### Snippet
```java
     * @throws StorageException If the creation of the storage after its destruction is already planned.
     */
    public CompletableFuture<MvPartitionStorage> create(int partitionId, IntFunction<T> createStorageFunction) {
        StorageOperation storageOperation = operationByPartitionId.compute(partitionId, (partId, operation) -> {
            if (operation instanceof DestroyStorageOperation) {
```

### BoundedWildcard
Can generalize to `? super T`
in `modules/storage-api/src/main/java/org/apache/ignite/internal/storage/util/MvPartitionStorages.java`
#### Snippet
```java
     * @throws StorageRebalanceException If the storage is in the process of rebalancing.
     */
    public CompletableFuture<Void> clear(int partitionId, Function<T, CompletableFuture<Void>> clearStorageFunction) {
        operationByPartitionId.compute(partitionId, (partId, operation) -> {
            checkStorageExists(partitionId);
```

### BoundedWildcard
Can generalize to `? extends CompletableFuture`
in `modules/storage-api/src/main/java/org/apache/ignite/internal/storage/util/MvPartitionStorages.java`
#### Snippet
```java
     * @throws StorageRebalanceException If the storage is in the process of rebalancing.
     */
    public CompletableFuture<Void> clear(int partitionId, Function<T, CompletableFuture<Void>> clearStorageFunction) {
        operationByPartitionId.compute(partitionId, (partId, operation) -> {
            checkStorageExists(partitionId);
```

### BoundedWildcard
Can generalize to `? extends ColumnDescriptor`
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/index/impl/BinaryTupleRowSerializer.java`
#### Snippet
```java
    }

    private BinaryTupleRowSerializer(List<ColumnDescriptor> schema) {
        this.schema = schema;

```

### BoundedWildcard
Can generalize to `? extends ReplicationGroupId`
in `modules/transactions/src/main/java/org/apache/ignite/internal/tx/TxMeta.java`
#### Snippet
```java
     * @param commitTimestamp Commit timestamp.
     */
    public TxMeta(TxState txState, List<ReplicationGroupId> enlistedPartitions, @Nullable HybridTimestamp commitTimestamp) {
        this.txState = txState;
        this.enlistedPartitions = enlistedPartitions;
```

### BoundedWildcard
Can generalize to `? extends IgniteTuple3`
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/AbstractMvTableStorageTest.java`
#### Snippet
```java
            HashIndexStorage hashIndexStorage,
            SortedIndexStorage sortedIndexStorage,
            List<IgniteTuple3<RowId, BinaryRow, HybridTimestamp>> rows
    ) {
        for (IgniteTuple3<RowId, BinaryRow, HybridTimestamp> row : rows) {
```

### BoundedWildcard
Can generalize to `? extends ReadResult`
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/AbstractMvTableStorageTest.java`
#### Snippet
```java
    }

    private static List<byte[]> toListOfByteArrays(Cursor<ReadResult> cursor) {
        try (cursor) {
            return cursor.stream().map(ReadResult::binaryRow).map(BinaryRow::bytes).collect(toList());
```

### BoundedWildcard
Can generalize to `? extends IgniteTuple3`
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/AbstractMvTableStorageTest.java`
#### Snippet
```java
            HashIndexStorage hashIndexStorage,
            SortedIndexStorage sortedIndexStorage,
            List<IgniteTuple3<RowId, BinaryRow, HybridTimestamp>> rows
    ) {
        for (IgniteTuple3<RowId, BinaryRow, HybridTimestamp> row : rows) {
```

### BoundedWildcard
Can generalize to `? extends IgniteTuple3`
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/AbstractMvTableStorageTest.java`
#### Snippet
```java
            HashIndexStorage hashIndexStorage,
            SortedIndexStorage sortedIndexStorage,
            List<IgniteTuple3<RowId, BinaryRow, HybridTimestamp>> rows
    ) {
        assertThat(rows, hasSize(greaterThanOrEqualTo(2)));
```

### BoundedWildcard
Can generalize to `? extends T`
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/AbstractMvTableStorageTest.java`
#### Snippet
```java
    }

    private static <T> List<T> getAll(Cursor<T> cursor) {
        try (cursor) {
            return cursor.stream().collect(toList());
```

### BoundedWildcard
Can generalize to `? extends JdbcColumnMeta`
in `modules/client-common/src/main/java/org/apache/ignite/internal/jdbc/proto/event/JdbcMetaColumnsResult.java`
#### Snippet
```java
     * @param meta Columns metadata.
     */
    public JdbcMetaColumnsResult(Collection<JdbcColumnMeta> meta) {
        Objects.requireNonNull(meta);

```

### BoundedWildcard
Can generalize to `? extends JdbcPrimaryKeyMeta`
in `modules/client-common/src/main/java/org/apache/ignite/internal/jdbc/proto/event/JdbcMetaPrimaryKeysResult.java`
#### Snippet
```java
     * @param meta Column metadata.
     */
    public JdbcMetaPrimaryKeysResult(Collection<JdbcPrimaryKeyMeta> meta) {
        Objects.requireNonNull(meta);

```

### BoundedWildcard
Can generalize to `? extends IgniteBiTuple`
in `modules/transactions/src/testFixtures/java/org/apache/ignite/internal/tx/storage/state/AbstractTxStateStorageTest.java`
#### Snippet
```java
     * @param rows Rows.
     */
    protected static void fillStorage(TxStateStorage storage, List<IgniteBiTuple<UUID, TxMeta>> rows) {
        assertThat(rows, hasSize(greaterThanOrEqualTo(2)));

```

### BoundedWildcard
Can generalize to `? super String`
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/DynamicConfiguration.java`
#### Snippet
```java
     */
    protected <P extends ConfigurationProperty<?>> void addMember(
            Map<String, ConfigurationProperty<?>> members,
            P member
    ) {
```

### BoundedWildcard
Can generalize to `? super ConfigurationProperty`
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/DynamicConfiguration.java`
#### Snippet
```java
     */
    protected <P extends ConfigurationProperty<?>> void addMember(
            Map<String, ConfigurationProperty<?>> members,
            P member
    ) {
```

### BoundedWildcard
Can generalize to `? super CHANGET`
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/DynamicConfiguration.java`
#### Snippet
```java
    /** {@inheritDoc} */
    @Override
    public final CompletableFuture<Void> change(Consumer<CHANGET> change) {
        Objects.requireNonNull(change, "Configuration consumer cannot be null.");

```

### BoundedWildcard
Can generalize to `? super List`
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/NamedListConfiguration.java`
#### Snippet
```java
            DynamicConfigurationChanger changer,
            boolean listenOnly,
            BiFunction<List<String>, String, T> cfgCreator,
            BiFunction<List<KeyPathNode>, DynamicConfigurationChanger, T> directProxyCreator,
            T anyConfig
```

### BoundedWildcard
Can generalize to `? super String`
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/NamedListConfiguration.java`
#### Snippet
```java
            DynamicConfigurationChanger changer,
            boolean listenOnly,
            BiFunction<List<String>, String, T> cfgCreator,
            BiFunction<List<KeyPathNode>, DynamicConfigurationChanger, T> directProxyCreator,
            T anyConfig
```

### BoundedWildcard
Can generalize to `? extends T`
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/NamedListConfiguration.java`
#### Snippet
```java
            DynamicConfigurationChanger changer,
            boolean listenOnly,
            BiFunction<List<String>, String, T> cfgCreator,
            BiFunction<List<KeyPathNode>, DynamicConfigurationChanger, T> directProxyCreator,
            T anyConfig
```

### BoundedWildcard
Can generalize to `? extends T`
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/NamedListConfiguration.java`
#### Snippet
```java
            boolean listenOnly,
            BiFunction<List<String>, String, T> cfgCreator,
            BiFunction<List<KeyPathNode>, DynamicConfigurationChanger, T> directProxyCreator,
            T anyConfig
    ) {
```

### BoundedWildcard
Can generalize to `? super String`
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/SuperRoot.java`
#### Snippet
```java
     * @param roots       Map of roots belonging to this super root.
     */
    public SuperRoot(Function<String, RootInnerNode> nodeCreator, Map<RootKey<?, ?>, InnerNode> roots) {
        this.nodeCreator = nodeCreator;

```

### BoundedWildcard
Can generalize to `? extends RootInnerNode`
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/SuperRoot.java`
#### Snippet
```java
     * @param roots       Map of roots belonging to this super root.
     */
    public SuperRoot(Function<String, RootInnerNode> nodeCreator, Map<RootKey<?, ?>, InnerNode> roots) {
        this.nodeCreator = nodeCreator;

```

### BoundedWildcard
Can generalize to `? extends V`
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/tree/OrderedMap.java`
#### Snippet
```java
     * @param other Source of keys/values to copy from.
     */
    OrderedMap(OrderedMap<V> other) {
        map = new HashMap<>(other.map);
        orderedKeys = new ArrayList<>(other.orderedKeys);
```

### BoundedWildcard
Can generalize to `? super Class`
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/ConfigurationRegistry.java`
#### Snippet
```java
     */
    private static <A extends Annotation> void addDefaultValidator(
            Map<Class<? extends Annotation>, Set<Validator<?, ?>>> validators,
            Class<A> annotatopnType,
            Validator<A, ?> validator
```

### BoundedWildcard
Can generalize to `? extends A`
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/ConfigurationRegistry.java`
#### Snippet
```java
    private static <A extends Annotation> void addDefaultValidator(
            Map<Class<? extends Annotation>, Set<Validator<?, ?>>> validators,
            Class<A> annotatopnType,
            Validator<A, ?> validator
    ) {
```

### BoundedWildcard
Can generalize to `? super SuperRootChange`
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/ConfigurationRegistry.java`
#### Snippet
```java
     * @return Future that is completed on change completion.
     */
    public CompletableFuture<Void> change(Consumer<SuperRootChange> change) {
        return change(new ConfigurationSource() {
            @Override
```

### BoundedWildcard
Can generalize to `? extends RootKey`
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/ConfigurationRegistry.java`
#### Snippet
```java
     */
    public ConfigurationRegistry(
            Collection<RootKey<?, ?>> rootKeys,
            Set<Validator<?, ?>> validators,
            ConfigurationStorage storage,
```

### BoundedWildcard
Can generalize to `? extends RootKey`
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/ConfigurationRegistry.java`
#### Snippet
```java
     * @return set of all schema classes
     */
    private static Set<Class<?>> collectAllSchemas(Collection<RootKey<?, ?>> rootKeys,
            Collection<Class<?>> internalSchemaExtensions,
            Collection<Class<?>> polymorphicSchemaExtensions) {
```

### BoundedWildcard
Can generalize to `? super N`
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/tree/NamedListNode.java`
#### Snippet
```java
    /** {@inheritDoc} */
    @Override
    public NamedListChange<N, N> create(int index, String key, Consumer<N> valConsumer) {
        Objects.requireNonNull(key, "key");
        Objects.requireNonNull(valConsumer, "valConsumer");
```

### BoundedWildcard
Can generalize to `? super N`
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/tree/NamedListNode.java`
#### Snippet
```java
    /** {@inheritDoc} */
    @Override
    public NamedListChange<N, N> createOrUpdate(String key, Consumer<N> valConsumer) {
        Objects.requireNonNull(key, "key");
        Objects.requireNonNull(valConsumer, "valConsumer");
```

### BoundedWildcard
Can generalize to `? super N`
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/tree/NamedListNode.java`
#### Snippet
```java
    /** {@inheritDoc} */
    @Override
    public NamedListChange<N, N> create(String key, Consumer<N> valConsumer) {
        Objects.requireNonNull(key, "key");
        Objects.requireNonNull(valConsumer, "valConsumer");
```

### BoundedWildcard
Can generalize to `? extends InnerNode`
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/tree/NamedListNode.java`
#### Snippet
```java
     * @param typeIdFieldName  Field name with {@link PolymorphicId}.
     */
    public NamedListNode(String syntheticKeyName, Supplier<InnerNode> valSupplier, @Nullable String typeIdFieldName) {
        this.syntheticKeyName = syntheticKeyName;
        this.valSupplier = valSupplier;
```

### BoundedWildcard
Can generalize to `? super N`
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/tree/NamedListNode.java`
#### Snippet
```java
    /** {@inheritDoc} */
    @Override
    public NamedListChange<N, N> createAfter(String precedingKey, String key, Consumer<N> valConsumer) {
        Objects.requireNonNull(precedingKey, "precedingKey");
        Objects.requireNonNull(key, "key");
```

### BoundedWildcard
Can generalize to `? super N`
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/tree/NamedListNode.java`
#### Snippet
```java
    /** {@inheritDoc} */
    @Override
    public NamedListChange<N, N> update(String key, Consumer<N> valConsumer) {
        Objects.requireNonNull(key, "key");
        Objects.requireNonNull(valConsumer, "valConsumer");
```

### BoundedWildcard
Can generalize to `? extends T`
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/util/KeysTrackingConfigurationVisitor.java`
#### Snippet
```java
     * @return Closure result.
     */
    protected final T withTracking(String key, boolean escape, boolean leaf, Supplier<T> closure) {
        int prevPos = startVisit(key, escape, leaf);

```

### BoundedWildcard
Can generalize to `? super String`
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/util/ConfigurationFlattener.java`
#### Snippet
```java
         * @param resMap Map with the result.
         */
        FlattenerVisitor(Deque<InnerNode> oldInnerNodesStack, Map<String, Serializable> resMap) {
            this.oldInnerNodesStack = oldInnerNodesStack;
            this.resMap = resMap;
```

### BoundedWildcard
Can generalize to `? super Serializable`
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/util/ConfigurationFlattener.java`
#### Snippet
```java
         * @param resMap Map with the result.
         */
        FlattenerVisitor(Deque<InnerNode> oldInnerNodesStack, Map<String, Serializable> resMap) {
            this.oldInnerNodesStack = oldInnerNodesStack;
            this.resMap = resMap;
```

### BoundedWildcard
Can generalize to `? extends Number`
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/validation/RangeValidator.java`
#### Snippet
```java
    /** {@inheritDoc} */
    @Override
    public void validate(Range annotation, ValidationContext<Number> ctx) {
        long longValue = ctx.getNewValue().longValue();

```

### BoundedWildcard
Can generalize to `? super List`
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/direct/DirectNamedListProxy.java`
#### Snippet
```java
            List<KeyPathNode> keys,
            DynamicConfigurationChanger changer,
            BiFunction<List<KeyPathNode>, DynamicConfigurationChanger, T> creator
    ) {
        super(keys, changer);
```

### BoundedWildcard
Can generalize to `? super DynamicConfigurationChanger`
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/direct/DirectNamedListProxy.java`
#### Snippet
```java
            List<KeyPathNode> keys,
            DynamicConfigurationChanger changer,
            BiFunction<List<KeyPathNode>, DynamicConfigurationChanger, T> creator
    ) {
        super(keys, changer);
```

### BoundedWildcard
Can generalize to `? extends T`
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/direct/DirectNamedListProxy.java`
#### Snippet
```java
            List<KeyPathNode> keys,
            DynamicConfigurationChanger changer,
            BiFunction<List<KeyPathNode>, DynamicConfigurationChanger, T> creator
    ) {
        super(keys, changer);
```

### BoundedWildcard
Can generalize to `? extends Number`
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/validation/PowerOfTwoValidator.java`
#### Snippet
```java
    /** {@inheritDoc} */
    @Override
    public void validate(PowerOfTwo annotation, ValidationContext<Number> ctx) {
        if (!isPow2(ctx.getNewValue().longValue())) {
            ctx.addIssue(new ValidationIssue(
```

### BoundedWildcard
Can generalize to `? extends NamedListView`
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/validation/ExceptKeysValidator.java`
#### Snippet
```java
    /** {@inheritDoc} */
    @Override
    public void validate(ExceptKeys annotation, ValidationContext<NamedListView<?>> ctx) {
        NamedListView<?> nameList = ctx.getNewValue();

```

### BoundedWildcard
Can generalize to `? super RootKey`
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/validation/ValidationContextImpl.java`
#### Snippet
```java
            SuperRoot oldRoots,
            SuperRoot newRoots,
            Function<RootKey<?, ?>, InnerNode> otherRoots,
            VIEWT val,
            String currentKey,
```

### BoundedWildcard
Can generalize to `? super ValidationIssue`
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/validation/ValidationContextImpl.java`
#### Snippet
```java
            String currentKey,
            List<String> currentPath,
            List<ValidationIssue> issues
    ) {
        this.oldRoots = oldRoots;
```

### BoundedWildcard
Can generalize to `? extends DynamicConfiguration`
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/notifications/ConfigurationNotificationUtils.java`
#### Snippet
```java
     */
    static Collection<DynamicConfiguration<InnerNode, ?>> mergeAnyConfigs(
            Collection<DynamicConfiguration<InnerNode, ?>> anyConfigs,
            @Nullable DynamicConfiguration<InnerNode, ?> anyConfig
    ) {
```

### BoundedWildcard
Can generalize to `? super String`
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/util/ConfigurationUtil.java`
#### Snippet
```java
     * @param val  Value to be inserted.
     */
    private static void insert(Map<String, Object> map, List<String> keys, int idx, Serializable val) {
        String key = keys.get(idx);

```

### BoundedWildcard
Can generalize to `? extends KeyPathNode`
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/util/ConfigurationUtil.java`
#### Snippet
```java
    }

    private static String joinPath(List<KeyPathNode> path) {
        return path.stream().map(pathNode -> pathNode.key).map(ConfigurationUtil::escape).collect(joining(KEY_SEPARATOR));
    }
```

### BoundedWildcard
Can generalize to `? extends K`
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/util/ConfigurationUtil.java`
#### Snippet
```java
     * @return New list.
     */
    public static <K> List<K> appendKey(List<K> prefix, K key) {
        if (prefix.isEmpty()) {
            return List.of(key);
```

### BoundedWildcard
Can generalize to `? extends RootKey`
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/util/ConfigurationUtil.java`
#### Snippet
```java
     * @throws IllegalArgumentException If the configuration type of the root keys is not equal to the storage type.
     */
    public static void checkConfigurationType(Collection<RootKey<?, ?>> rootKeys, ConfigurationStorage storage) {
        for (RootKey<?, ?> key : rootKeys) {
            if (key.type() != storage.type()) {
```

### BoundedWildcard
Can generalize to `? extends FieldDefinition`
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/asm/ConfigurationImplAsmGenerator.java`
#### Snippet
```java
     */
    private void addCfgAddMembersMethod(
            Map<String, FieldDefinition> fieldDefs,
            FieldDefinition polymorphicTypeIdFieldDef
    ) {
```

### BoundedWildcard
Can generalize to `? extends FieldDefinition`
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/asm/ConfigurationImplAsmGenerator.java`
#### Snippet
```java
     * @param polymorphicTypeIdFieldDef Identification field for the polymorphic configuration instance.
     */
    private void addCfgRemoveMembersMethod(Map<String, FieldDefinition> fieldDefs, FieldDefinition polymorphicTypeIdFieldDef) {
        MethodDefinition removeMembersMtd = cfgImplClassDef.declareMethod(
                EnumSet.of(PUBLIC),
```

### BoundedWildcard
Can generalize to `? extends FieldDefinition`
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/asm/ConfigurationImplAsmGenerator.java`
#### Snippet
```java
     */
    private void addConfigurationImplConstructor(
            Map<String, FieldDefinition> fieldDefs,
            @Nullable FieldDefinition internalConfigTypesFieldDef
    ) {
```

### BoundedWildcard
Can generalize to `? super MemberKey`
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/validation/ValidationUtil.java`
#### Snippet
```java
            SuperRoot newRoots,
            Function<RootKey<?, ?>, InnerNode> otherRoots,
            Map<MemberKey, Map<Annotation, Set<Validator<?, ?>>>> memberAnnotationsCache,
            List<? extends Validator<?, ?>> validators
    ) {
```

### BoundedWildcard
Can generalize to `? extends CompletableFuture`
in `modules/client-handler/src/main/java/org/apache/ignite/client/handler/ClientHandlerModule.java`
#### Snippet
```java
            NettyBootstrapFactory bootstrapFactory,
            IgniteSql sql,
            Supplier<CompletableFuture<UUID>> clusterIdSupplier) {
        assert igniteTables != null;
        assert registry != null;
```

### BoundedWildcard
Can generalize to `? extends SqlRow`
in `modules/client-handler/src/main/java/org/apache/ignite/client/handler/requests/sql/ClientSqlCommon.java`
#### Snippet
```java
 */
class ClientSqlCommon {
    static void packCurrentPage(ClientMessagePacker out, AsyncResultSet<SqlRow> asyncResultSet) {
        ResultSetMetadata meta = asyncResultSet.metadata();
        assert meta != null : "Metadata can't be null when row set is present.";
```

### BoundedWildcard
Can generalize to `? extends Tuple`
in `modules/client-handler/src/main/java/org/apache/ignite/client/handler/requests/table/ClientTableCommon.java`
#### Snippet
```java
    public static void writeTuplesNullable(
            ClientMessagePacker packer,
            Collection<Tuple> tuples,
            TuplePart part,
            SchemaRegistry schemaRegistry,
```

### BoundedWildcard
Can generalize to `? extends Tuple`
in `modules/client-handler/src/main/java/org/apache/ignite/client/handler/requests/table/ClientTableCommon.java`
#### Snippet
```java
    public static void writeTuples(
            ClientMessagePacker packer,
            Collection<Tuple> tuples,
            TuplePart part,
            SchemaRegistry schemaRegistry,
```

### BoundedWildcard
Can generalize to `? extends ColumnFamilyHandle`
in `modules/rocksdb-common/src/main/java/org/apache/ignite/internal/rocksdb/flush/RocksDbFlusher.java`
#### Snippet
```java
     */
    @SuppressWarnings("AssignmentOrReturnOfFieldWithMutableType")
    public void init(RocksDB db, List<ColumnFamilyHandle> columnFamilyHandles) {
        this.db = db;

```

### BoundedWildcard
Can generalize to `? extends Assignment`
in `modules/placement-driver/src/main/java/org/apache/ignite/internal/placementdriver/LeaseUpdater.java`
#### Snippet
```java
     * @return Cluster node, or {@code null} if no node in assignments can be the leaseholder.
     */
    private ClusterNode nextLeaseHolder(Set<Assignment> assignments) {
        //TODO: IGNITE-18879 Implement more intellectual algorithm to choose a node.
        for (Assignment assignment : assignments) {
```

### BoundedWildcard
Can generalize to `? super ByteBuffer`
in `modules/storage-rocksdb/src/main/java/org/apache/ignite/internal/storage/rocksdb/index/RocksDbSortedIndexStorage.java`
#### Snippet
```java
            byte @Nullable [] lowerBound,
            byte @Nullable [] upperBound,
            Function<ByteBuffer, T> mapper
    ) {
        Slice upperBoundSlice = upperBound == null ? new Slice(partitionStorage.partitionEndPrefix()) : new Slice(upperBound);
```

### BoundedWildcard
Can generalize to `? extends T`
in `modules/storage-rocksdb/src/main/java/org/apache/ignite/internal/storage/rocksdb/index/RocksDbSortedIndexStorage.java`
#### Snippet
```java
            byte @Nullable [] lowerBound,
            byte @Nullable [] upperBound,
            Function<ByteBuffer, T> mapper
    ) {
        Slice upperBoundSlice = upperBound == null ? new Slice(partitionStorage.partitionEndPrefix()) : new Slice(upperBound);
```

### BoundedWildcard
Can generalize to `? super ClusterNode`
in `modules/placement-driver/src/main/java/org/apache/ignite/internal/raft/client/TopologyAwareRaftGroupService.java`
#### Snippet
```java
         * @param onLeaderElectedCallback A callback closure.
         */
        public synchronized void setOnLeaderElectedCallback(BiConsumer<ClusterNode, Long> onLeaderElectedCallback) {
            this.onLeaderElectedCallback = onLeaderElectedCallback;
        }
```

### BoundedWildcard
Can generalize to `? super Long`
in `modules/placement-driver/src/main/java/org/apache/ignite/internal/raft/client/TopologyAwareRaftGroupService.java`
#### Snippet
```java
         * @param onLeaderElectedCallback A callback closure.
         */
        public synchronized void setOnLeaderElectedCallback(BiConsumer<ClusterNode, Long> onLeaderElectedCallback) {
            this.onLeaderElectedCallback = onLeaderElectedCallback;
        }
```

### BoundedWildcard
Can generalize to `? extends CompletableFuture`>
in `modules/placement-driver/src/main/java/org/apache/ignite/internal/placementdriver/PlacementDriverManager.java`
#### Snippet
```java
            ClusterService clusterService,
            RaftConfiguration raftConfiguration,
            Supplier<CompletableFuture<Set<String>>> placementDriverNodesNamesProvider,
            LogicalTopologyService logicalTopologyService,
            ScheduledExecutorService raftClientExecutor,
```

### BoundedWildcard
Can generalize to `? super TopologyAwareRaftGroupService`
in `modules/placement-driver/src/main/java/org/apache/ignite/internal/placementdriver/PlacementDriverManager.java`
#### Snippet
```java
    }

    private void withRaftClientIfPresent(Consumer<TopologyAwareRaftGroupService> closure) {
        raftClientFuture.thenAccept(client -> {
            if (client != null) {
```

### BoundedWildcard
Can generalize to `? extends ValidationIssue`
in `modules/configuration-api/src/main/java/org/apache/ignite/configuration/validation/ConfigurationValidationException.java`
#### Snippet
```java
    }

    private static String createMessageFromIssues(List<ValidationIssue> issues) {
        return "Validation did not pass for keys: "
                + issues.stream().map(issue -> "[" + issue.key() + ", " + issue.message() + "]").collect(Collectors.joining(", "));
```

### BoundedWildcard
Can generalize to `? extends CommandClosure`
in `modules/cluster-management/src/main/java/org/apache/ignite/internal/cluster/management/raft/CmgRaftGroupListener.java`
#### Snippet
```java

    @Override
    public void onWrite(Iterator<CommandClosure<WriteCommand>> iterator) {
        while (iterator.hasNext()) {
            CommandClosure<WriteCommand> clo = iterator.next();
```

### BoundedWildcard
Can generalize to `? extends CommandClosure`
in `modules/cluster-management/src/main/java/org/apache/ignite/internal/cluster/management/raft/CmgRaftGroupListener.java`
#### Snippet
```java

    @Override
    public void onRead(Iterator<CommandClosure<ReadCommand>> iterator) {
        while (iterator.hasNext()) {
            CommandClosure<ReadCommand> clo = iterator.next();
```

### BoundedWildcard
Can generalize to `? super Throwable`
in `modules/cluster-management/src/main/java/org/apache/ignite/internal/cluster/management/raft/CmgRaftGroupListener.java`
#### Snippet
```java

    @Override
    public void onSnapshotSave(Path path, Consumer<Throwable> doneClo) {
        storage.snapshot(path)
                .whenComplete((unused, throwable) -> doneClo.accept(throwable));
```

### BoundedWildcard
Can generalize to `? extends ClusterNode`
in `modules/cluster-management/src/main/java/org/apache/ignite/internal/cluster/management/raft/ValidationManager.java`
#### Snippet
```java
    }

    void removeValidatedNodes(Collection<ClusterNode> nodes) {
        Set<String> validatedNodeIds = storage.getValidatedNodes().stream()
                .map(ClusterNode::id)
```

### BoundedWildcard
Can generalize to `? extends T`
in `modules/cluster-management/src/main/java/org/apache/ignite/internal/cluster/management/raft/RocksDbClusterStateStorage.java`
#### Snippet
```java

    @Override
    public <T> Cursor<T> getWithPrefix(byte[] prefix, BiFunction<byte[], byte[], T> entryTransformer) {
        byte[] upperBound = RocksUtils.incrementPrefix(prefix);

```

### BoundedWildcard
Can generalize to `? extends ClusterNode`
in `modules/cluster-management/src/main/java/org/apache/ignite/internal/cluster/management/topology/LogicalTopologyImpl.java`
#### Snippet
```java

    @Override
    public void removeNodes(Set<ClusterNode> nodesToRemove) {
        LogicalTopologySnapshot snapshot = readLogicalTopology();

```

### BoundedWildcard
Can generalize to `? super LogicalTopologyEventListener`
in `modules/cluster-management/src/main/java/org/apache/ignite/internal/cluster/management/topology/LogicalTopologyImpl.java`
#### Snippet
```java
    }

    private void notifyListeners(Consumer<LogicalTopologyEventListener> action, String methodName) {
        for (LogicalTopologyEventListener listener : listeners) {
            try {
```

### BoundedWildcard
Can generalize to `? extends ClusterNode`
in `modules/cluster-management/src/main/java/org/apache/ignite/internal/cluster/management/ClusterInitializer.java`
#### Snippet
```java

    private static CompletableFuture<Void> allOf(
            Collection<ClusterNode> nodes,
            Function<ClusterNode, CompletableFuture<?>> futureProducer
    ) {
```

### BoundedWildcard
Can generalize to `? super ClusterNode`
in `modules/cluster-management/src/main/java/org/apache/ignite/internal/cluster/management/ClusterInitializer.java`
#### Snippet
```java
    private static CompletableFuture<Void> allOf(
            Collection<ClusterNode> nodes,
            Function<ClusterNode, CompletableFuture<?>> futureProducer
    ) {
        CompletableFuture<?>[] futures = nodes.stream().map(futureProducer).toArray(CompletableFuture[]::new);
```

### BoundedWildcard
Can generalize to `? super MockNode`
in `modules/cluster-management/src/testFixtures/java/org/apache/ignite/internal/cluster/management/BaseItClusterManagementTest.java`
#### Snippet
```java
    }

    protected static MockNode addNodeToCluster(Collection<MockNode> cluster, TestInfo testInfo, Path workDir) {
        var node = new MockNode(
                testInfo,
```

### BoundedWildcard
Can generalize to `? extends MockNode`
in `modules/cluster-management/src/testFixtures/java/org/apache/ignite/internal/cluster/management/BaseItClusterManagementTest.java`
#### Snippet
```java
    }

    protected static void stopNodes(Collection<MockNode> nodes) throws Exception {
        IgniteUtils.closeAll(Stream.concat(
                nodes.stream().map(node -> node::beforeNodeStop),
```

### BoundedWildcard
Can generalize to `? extends T`
in `modules/cluster-management/src/testFixtures/java/org/apache/ignite/internal/cluster/management/raft/TestClusterStateStorage.java`
#### Snippet
```java

    @Override
    public <T> Cursor<T> getWithPrefix(byte[] prefix, BiFunction<byte[], byte[], T> entryTransformer) {
        lock.readLock().lock();

```

### BoundedWildcard
Can generalize to `? extends FieldDefinition`
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/asm/InnerNodeAsmGenerator.java`
#### Snippet
```java
     */
    private MethodDefinition addNodeChangePolymorphicTypeIdMethod(
            Map<String, FieldDefinition> fieldDefs,
            FieldDefinition polymorphicTypeIdFieldDef
    ) {
```

### BoundedWildcard
Can generalize to `? extends FieldDefinition`
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/asm/InnerNodeAsmGenerator.java`
#### Snippet
```java
     */
    private void addNodeTraverseChildMethod(
            Map<String, FieldDefinition> fieldDefs,
            Map<Class<?>, List<Field>> polymorphicFieldsByExtension,
            @Nullable FieldDefinition polymorphicTypeIdFieldDef
```

### BoundedWildcard
Can generalize to `? extends FieldDefinition`
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/asm/InnerNodeAsmGenerator.java`
#### Snippet
```java
     */
    private void addNodeTraverseChildrenMethod(
            Map<String, FieldDefinition> fieldDefs,
            Map<Class<?>, List<Field>> polymorphicFieldsByExtension,
            @Nullable FieldDefinition polymorphicTypeIdFieldDef
```

### BoundedWildcard
Can generalize to `? super MethodDefinition`
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/asm/InnerNodeAsmGenerator.java`
#### Snippet
```java
            ClassDefinition classDef,
            Field schemaField,
            Function<MethodDefinition, BytecodeExpression> getFieldCodeFun,
            @Nullable Function<MethodDefinition, BytecodeExpression> getPolymorphicTypeIdFieldFun
    ) {
```

### BoundedWildcard
Can generalize to `? extends BytecodeExpression`
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/asm/InnerNodeAsmGenerator.java`
#### Snippet
```java
            ClassDefinition classDef,
            Field schemaField,
            Function<MethodDefinition, BytecodeExpression> getFieldCodeFun,
            @Nullable Function<MethodDefinition, BytecodeExpression> getPolymorphicTypeIdFieldFun
    ) {
```

### BoundedWildcard
Can generalize to `? super MethodDefinition`
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/asm/InnerNodeAsmGenerator.java`
#### Snippet
```java
    private void enrichWithPolymorphicTypeCheck(
            Field schemaField,
            @Nullable Function<MethodDefinition, BytecodeExpression> getPolymorphicTypeIdFieldFun,
            MethodDefinition method,
            BytecodeBlock bytecodeBlock
```

### BoundedWildcard
Can generalize to `? extends BytecodeExpression`
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/asm/InnerNodeAsmGenerator.java`
#### Snippet
```java
    private void enrichWithPolymorphicTypeCheck(
            Field schemaField,
            @Nullable Function<MethodDefinition, BytecodeExpression> getPolymorphicTypeIdFieldFun,
            MethodDefinition method,
            BytecodeBlock bytecodeBlock
```

### BoundedWildcard
Can generalize to `? super MethodDefinition`
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/asm/InnerNodeAsmGenerator.java`
#### Snippet
```java
            ClassDefinition classDef,
            Field schemaField,
            Function<MethodDefinition, BytecodeExpression> getFieldCodeFun,
            BiFunction<MethodDefinition, BytecodeExpression, BytecodeExpression> setFieldCodeFun,
            @Nullable Function<MethodDefinition, BytecodeExpression> getPolymorphicTypeIdFieldFun
```

### BoundedWildcard
Can generalize to `? extends BytecodeExpression`
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/asm/InnerNodeAsmGenerator.java`
#### Snippet
```java
            ClassDefinition classDef,
            Field schemaField,
            Function<MethodDefinition, BytecodeExpression> getFieldCodeFun,
            BiFunction<MethodDefinition, BytecodeExpression, BytecodeExpression> setFieldCodeFun,
            @Nullable Function<MethodDefinition, BytecodeExpression> getPolymorphicTypeIdFieldFun
```

### BoundedWildcard
Can generalize to `? super MethodDefinition`
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/asm/InnerNodeAsmGenerator.java`
#### Snippet
```java
            Field schemaField,
            Function<MethodDefinition, BytecodeExpression> getFieldCodeFun,
            BiFunction<MethodDefinition, BytecodeExpression, BytecodeExpression> setFieldCodeFun,
            @Nullable Function<MethodDefinition, BytecodeExpression> getPolymorphicTypeIdFieldFun
    ) {
```

### BoundedWildcard
Can generalize to `? super BytecodeExpression`
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/asm/InnerNodeAsmGenerator.java`
#### Snippet
```java
            Field schemaField,
            Function<MethodDefinition, BytecodeExpression> getFieldCodeFun,
            BiFunction<MethodDefinition, BytecodeExpression, BytecodeExpression> setFieldCodeFun,
            @Nullable Function<MethodDefinition, BytecodeExpression> getPolymorphicTypeIdFieldFun
    ) {
```

### BoundedWildcard
Can generalize to `? extends BytecodeExpression`
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/asm/InnerNodeAsmGenerator.java`
#### Snippet
```java
            Field schemaField,
            Function<MethodDefinition, BytecodeExpression> getFieldCodeFun,
            BiFunction<MethodDefinition, BytecodeExpression, BytecodeExpression> setFieldCodeFun,
            @Nullable Function<MethodDefinition, BytecodeExpression> getPolymorphicTypeIdFieldFun
    ) {
```

### BoundedWildcard
Can generalize to `? extends FieldDefinition`
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/asm/InnerNodeAsmGenerator.java`
#### Snippet
```java
    private void addNodeConstructor(
            Map<Class<?>, FieldDefinition> specFields,
            Map<String, FieldDefinition> fieldDefs,
            @Nullable FieldDefinition internalSchemaTypesFieldDef
    ) {
```

### BoundedWildcard
Can generalize to `? extends FieldDefinition`
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/asm/InnerNodeAsmGenerator.java`
#### Snippet
```java
     */
    private void addNodeConstructMethod(
            Map<String, FieldDefinition> fieldDefs,
            Map<Class<?>, List<Field>> polymorphicFieldsByExtension,
            @Nullable FieldDefinition polymorphicTypeIdFieldDef,
```

### BoundedWildcard
Can generalize to `? extends FieldDefinition`
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/asm/InnerNodeAsmGenerator.java`
#### Snippet
```java
     */
    private void addNodeConstructDefaultMethod(
            Map<Class<?>, FieldDefinition> specFields,
            Map<String, FieldDefinition> fieldDefs,
            Map<Class<?>, List<Field>> polymorphicFieldsByExtension,
```

### BoundedWildcard
Can generalize to `? extends FieldDefinition`
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/asm/InnerNodeAsmGenerator.java`
#### Snippet
```java
    private void addNodeConstructDefaultMethod(
            Map<Class<?>, FieldDefinition> specFields,
            Map<String, FieldDefinition> fieldDefs,
            Map<Class<?>, List<Field>> polymorphicFieldsByExtension,
            @Nullable FieldDefinition polymorphicTypeIdFieldDef
```

### BoundedWildcard
Can generalize to `? super HybridTimestamp`
in `modules/storage-page-memory/src/main/java/org/apache/ignite/internal/storage/pagememory/mv/ReadRowVersion.java`
#### Snippet
```java
    }

    private long readFullOrInitiateReadFragmented(long link, long pageAddr, DataPagePayload payload, Predicate<HybridTimestamp> loadValue) {
        firstFragmentLink = link;

```

### BoundedWildcard
Can generalize to `? super VersionChainKey`
in `modules/storage-page-memory/src/main/java/org/apache/ignite/internal/storage/pagememory/mv/io/VersionChainLeafIo.java`
#### Snippet
```java

    @Override
    public void visit(BplusTree<VersionChainKey, ?> tree, long pageAddr, Consumer<VersionChainKey> c) {
        int partitionId = getPartitionId(pageAddr);

```

### BoundedWildcard
Can generalize to `? super HashIndexRowKey`
in `modules/storage-page-memory/src/main/java/org/apache/ignite/internal/storage/pagememory/index/hash/io/HashIndexTreeLeafIo.java`
#### Snippet
```java

    @Override
    public void visit(BplusTree<HashIndexRowKey, ?> tree, long pageAddr, Consumer<HashIndexRowKey> c) {
        HashIndexTree hashIndexTree = (HashIndexTree) tree;

```

### BoundedWildcard
Can generalize to `? super VersionChain`
in `modules/storage-page-memory/src/main/java/org/apache/ignite/internal/storage/pagememory/mv/AbstractPageMemoryMvPartitionStorage.java`
#### Snippet
```java
     *      the version chain is located. If the version chain is not found, then {@code null} will be passed to the function.
     */
    <T> @Nullable T findVersionChain(RowId rowId, Function<VersionChain, T> mapper) {
        try {
            return versionChainTree.findOne(new VersionChainKey(rowId), new TreeRowMapClosure<>() {
```

### BoundedWildcard
Can generalize to `? super SortedIndexRowKey`
in `modules/storage-page-memory/src/main/java/org/apache/ignite/internal/storage/pagememory/index/sorted/io/SortedIndexTreeLeafIo.java`
#### Snippet
```java

    @Override
    public void visit(BplusTree<SortedIndexRowKey, ?> tree, long pageAddr, Consumer<SortedIndexRowKey> c) {
        SortedIndexTree sortedIndexTree = (SortedIndexTree) tree;

```

### BoundedWildcard
Can generalize to `? extends T`
in `modules/storage-page-memory/src/main/java/org/apache/ignite/internal/storage/pagememory/index/sorted/PageMemorySortedIndexStorage.java`
#### Snippet
```java
     * @param mapper Conversion function.
     */
    private <T, R> Cursor<R> convertCursor(Cursor<T> cursor, Function<T, R> mapper) {
        return new Cursor<>() {
            @Override
```

### BoundedWildcard
Can generalize to `? super T`
in `modules/storage-page-memory/src/main/java/org/apache/ignite/internal/storage/pagememory/index/sorted/PageMemorySortedIndexStorage.java`
#### Snippet
```java
     * @param mapper Conversion function.
     */
    private <T, R> Cursor<R> convertCursor(Cursor<T> cursor, Function<T, R> mapper) {
        return new Cursor<>() {
            @Override
```

### BoundedWildcard
Can generalize to `? extends R`
in `modules/storage-page-memory/src/main/java/org/apache/ignite/internal/storage/pagememory/index/sorted/PageMemorySortedIndexStorage.java`
#### Snippet
```java
     * @param mapper Conversion function.
     */
    private <T, R> Cursor<R> convertCursor(Cursor<T> cursor, Function<T, R> mapper) {
        return new Cursor<>() {
            @Override
```

### BoundedWildcard
Can generalize to `? extends MessageClass`
in `modules/network-annotation-processor/src/main/java/org/apache/ignite/internal/network/processor/messages/MessageFactoryGenerator.java`
#### Snippet
```java
     * @return {@code TypeSpec} of the generated message factory
     */
    public TypeSpec generateMessageFactory(Collection<MessageClass> messages) {
        ClassName factoryName = messageGroup.messageFactoryClassName();

```

### BoundedWildcard
Can generalize to `? extends ExecutableElement`
in `modules/network-annotation-processor/src/main/java/org/apache/ignite/internal/network/processor/messages/MessageBuilderGenerator.java`
#### Snippet
```java
    }

    private List<MethodSpec> generateGettersAndSetters(MessageClass message, List<ExecutableElement> fields) {
        List<MethodSpec> methods = new ArrayList<>();

```

### BoundedWildcard
Can generalize to `? extends ExecutableElement`
in `modules/network-annotation-processor/src/main/java/org/apache/ignite/internal/network/processor/messages/MessageBuilderGenerator.java`
#### Snippet
```java
    }

    private List<MethodSpec> generateByteArrayGettersAndSetters(MessageClass message, List<ExecutableElement> fields) {
        List<MethodSpec> methods = new ArrayList<>();

```

### BoundedWildcard
Can generalize to `? extends MessageClass`
in `modules/network-annotation-processor/src/main/java/org/apache/ignite/internal/network/processor/TransferableObjectProcessor.java`
#### Snippet
```java
     * </ol>
     */
    private void validateMessages(Collection<MessageClass> messages) {
        var typeUtils = new TypeUtils(processingEnv);

```

### BoundedWildcard
Can generalize to `? extends MessageClass`
in `modules/network-annotation-processor/src/main/java/org/apache/ignite/internal/network/processor/TransferableObjectProcessor.java`
#### Snippet
```java
     * </ol>
     */
    private void generateSerializers(Collection<MessageClass> annotatedMessages, MessageGroupWrapper messageGroup) {
        List<MessageClass> serializableMessages = annotatedMessages.stream()
                .filter(MessageClass::isAutoSerializable)
```

### BoundedWildcard
Can generalize to `? extends MessageClass`
in `modules/network-annotation-processor/src/main/java/org/apache/ignite/internal/network/processor/TransferableObjectProcessor.java`
#### Snippet
```java
    }

    private Set<MessageClass> mergeMessages(IncrementalCompilationConfig currentConfig, Collection<MessageClass> messages) {
        Elements elementUtils = processingEnv.getElementUtils();

```

### BoundedWildcard
Can generalize to `? extends MessageClass`
in `modules/network-annotation-processor/src/main/java/org/apache/ignite/internal/network/processor/TransferableObjectProcessor.java`
#### Snippet
```java
    }

    private void updateConfig(Collection<MessageClass> messages, MessageGroupWrapper messageGroup) {
        List<ClassName> messageClassNames = messages.stream()
                .map(MessageClass::className)
```

### BoundedWildcard
Can generalize to `? extends VariableElement`
in `modules/configuration-annotation-processor/src/main/java/org/apache/ignite/internal/configuration/processor/ConfigurationProcessorUtils.java`
#### Snippet
```java
     */
    public static List<VariableElement> collectFieldsWithAnnotation(
            Collection<VariableElement> fields,
            Class<? extends Annotation> annotationClass
    ) {
```

### BoundedWildcard
Can generalize to `? extends MessageClass`
in `modules/network-annotation-processor/src/main/java/org/apache/ignite/internal/network/processor/serialization/RegistryInitializerGenerator.java`
#### Snippet
```java
     * @return {@code TypeSpec} of the generated registry initializer
     */
    public TypeSpec generateRegistryInitializer(Map<MessageClass, TypeSpec> messageFactories) {
        String initializerName = messageGroup.groupName() + "SerializationRegistryInitializer";

```

### BoundedWildcard
Can generalize to `? extends VariableElement`
in `modules/configuration-annotation-processor/src/main/java/org/apache/ignite/internal/configuration/processor/ConfigurationProcessor.java`
#### Snippet
```java
     */
    private static Collection<Name> findDuplicates(
            Collection<VariableElement> fields1,
            Collection<VariableElement> fields2
    ) {
```

### BoundedWildcard
Can generalize to `? extends VariableElement`
in `modules/configuration-annotation-processor/src/main/java/org/apache/ignite/internal/configuration/processor/ConfigurationProcessor.java`
#### Snippet
```java
    private static Collection<Name> findDuplicates(
            Collection<VariableElement> fields1,
            Collection<VariableElement> fields2
    ) {
        if (fields1.isEmpty() || fields2.isEmpty()) {
```

### BoundedWildcard
Can generalize to `? extends VariableElement`
in `modules/configuration-annotation-processor/src/main/java/org/apache/ignite/internal/configuration/processor/ConfigurationProcessor.java`
#### Snippet
```java
     */
    private void createPojoBindings(
            Collection<VariableElement> fields,
            ClassName schemaClassName,
            TypeSpec.Builder configurationInterfaceBuilder,
```

### BoundedWildcard
Can generalize to `? super ColumnDefinition`
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/index/AbstractSortedIndexStorageTest.java`
#### Snippet
```java
    }

    private List<ColumnDefinition> shuffledDefinitions(Predicate<ColumnDefinition> filter) {
        List<ColumnDefinition> shuffledDefinitions = ALL_TYPES_COLUMN_DEFINITIONS.stream()
                .filter(filter)
```

### BoundedWildcard
Can generalize to `? extends T`
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/index/AbstractSortedIndexStorageTest.java`
#### Snippet
```java
            @Nullable BinaryTuplePrefix upperBound,
            @MagicConstant(flagsFromClass = SortedIndexStorage.class) int flags,
            Function<Object[], T> mapper
    ) {
        var serializer = new BinaryTupleRowSerializer(index.indexDescriptor());
```

### BoundedWildcard
Can generalize to `? extends IndexRow`
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/index/AbstractSortedIndexStorageTest.java`
#### Snippet
```java
    }

    private static <T> List<T> getRemaining(Cursor<IndexRow> scanCursor, Function<IndexRow, T> mapper) {
        List<T> result = new ArrayList<>();

```

### BoundedWildcard
Can generalize to `? super IndexRow`
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/index/AbstractSortedIndexStorageTest.java`
#### Snippet
```java
    }

    private static <T> List<T> getRemaining(Cursor<IndexRow> scanCursor, Function<IndexRow, T> mapper) {
        List<T> result = new ArrayList<>();

```

### BoundedWildcard
Can generalize to `? extends T`
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/index/AbstractSortedIndexStorageTest.java`
#### Snippet
```java
    }

    private static <T> List<T> getRemaining(Cursor<IndexRow> scanCursor, Function<IndexRow, T> mapper) {
        List<T> result = new ArrayList<>();

```

### BoundedWildcard
Can generalize to `? super IndexRow`
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/index/AbstractSortedIndexStorageTest.java`
#### Snippet
```java
        }

        private static <T> SimpleRow<T> of(IndexRow indexRow, Function<IndexRow, T> mapper) {
            return new SimpleRow<>(mapper.apply(indexRow), indexRow.rowId());
        }
```

### BoundedWildcard
Can generalize to `? extends T`
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/index/AbstractSortedIndexStorageTest.java`
#### Snippet
```java
        }

        private static <T> SimpleRow<T> of(IndexRow indexRow, Function<IndexRow, T> mapper) {
            return new SimpleRow<>(mapper.apply(indexRow), indexRow.rowId());
        }
```

### BoundedWildcard
Can generalize to `? extends ColumnDefinition`
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/index/AbstractSortedIndexStorageTest.java`
#### Snippet
```java
     * Creates a Sorted Index using the given columns.
     */
    private SortedIndexStorage createIndexStorage(List<ColumnDefinition> indexSchema) {
        SortedIndexDefinitionBuilder indexDefinitionBuilder = SchemaBuilders.sortedIndex("TEST_IDX");

```

### BoundedWildcard
Can generalize to `? extends DynamicConfiguration`
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/notifications/ConfigurationNotifier.java`
#### Snippet
```java
            InnerNode newInnerNode,
            DynamicConfiguration<InnerNode, ?> config,
            Collection<DynamicConfiguration<InnerNode, ?>> anyConfigs,
            ConfigurationNotificationContext ctx
    ) {
```

### BoundedWildcard
Can generalize to `? extends DynamicConfiguration`
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/notifications/ConfigurationNotifier.java`
#### Snippet
```java
            InnerNode innerNode,
            DynamicConfiguration<InnerNode, ?> config,
            Collection<DynamicConfiguration<InnerNode, ?>> anyConfigs,
            ConfigurationNotificationContext ctx
    ) {
```

### BoundedWildcard
Can generalize to `? super ConfigurationNotificationEvent`
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/notifications/ConfigurationNotifier.java`
#### Snippet
```java
            @Nullable Object newValue,
            ConfigurationNotificationContext notificationCtx,
            BiFunction<L, ConfigurationNotificationEvent, CompletableFuture<?>> invokeListener
    ) {
        // Lazy set.
```

### BoundedWildcard
Can generalize to `? extends CompletableFuture`
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/notifications/ConfigurationNotifier.java`
#### Snippet
```java
            @Nullable Object newValue,
            ConfigurationNotificationContext notificationCtx,
            BiFunction<L, ConfigurationNotificationEvent, CompletableFuture<?>> invokeListener
    ) {
        // Lazy set.
```

### BoundedWildcard
Can generalize to `? super L`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/tree/BplusTree.java`
#### Snippet
```java
         * @param idx Replacement index.
         */
        private void replaceRowInPage(BplusIo<L> io, long pageAddr, int idx) throws IgniteInternalCheckedException {
            io.store(pageAddr, idx, row, null, false);

```

### BoundedWildcard
Can generalize to `? extends R`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/tree/BplusTree.java`
#### Snippet
```java
                boolean lowIncl,
                boolean upIncl,
                @Nullable TreeRowMapClosure<L, T, R> treeRowClosure,
                @Nullable Object arg
        ) {
```

### BoundedWildcard
Can generalize to `? extends R`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/tree/BplusTree.java`
#### Snippet
```java
        private GetOne(
                @Nullable L row,
                @Nullable TreeRowMapClosure<L, T, R> treeRowClosure,
                @Nullable Object arg,
                boolean findLast
```

### BoundedWildcard
Can generalize to `? extends IgniteTuple3`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/tree/BplusTree.java`
#### Snippet
```java
     *      that order as they were locked by destroy method. We unlock them in reverse order and unlock in direct order.
     */
    private void temporaryReleaseLock(Deque<IgniteTuple3<Long, Long, Long>> lockedPages) {
        lockedPages.iterator().forEachRemaining(t -> writeUnlock(t.get1(), t.get2(), t.get3(), true));

```

### BoundedWildcard
Can generalize to `? super L`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/tree/BplusTree.java`
#### Snippet
```java
        }

        private void insertSimple(long pageAddr, BplusIo<L> io, int idx) throws IgniteInternalCheckedException {
            io.insert(pageAddr, idx, row, null, rightId, false);

```

## RuleId[id=MissortedModifiers]
### MissortedModifiers
Missorted modifiers `final static`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/BytesUtil.java`
#### Snippet
```java
    }

    private final static char[] HEX_ARRAY = "0123456789ABCDEF".toCharArray();

    /**
```

### MissortedModifiers
Missorted modifiers `transient final`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/ByteBufferCollector.java`
#### Snippet
```java
    }

    private transient final Recyclers.Handle handle;

    // TODO asch fixme is it safe to have static recyclers ? IGNITE-14832
```

### MissortedModifiers
Missorted modifiers `static abstract`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/ThreadHelper.java`
#### Snippet
```java
    }

    public static abstract class Spinner {
        public abstract void onSpinWait();
    }
```

### MissortedModifiers
Missorted modifiers `transient final`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/RecyclableByteBufferList.java`
#### Snippet
```java
    }

    private transient final Recyclers.Handle handle;

    private static final Recyclers<RecyclableByteBufferList> recyclers = new Recyclers<RecyclableByteBufferList>(512) {
```

### MissortedModifiers
Missorted modifiers `final static`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/SegmentList.java`
#### Snippet
```java
     * @param <T>
     */
    private final static class Segment<T> implements Recyclable {
        private static final Recyclers<Segment<?>> recyclers = new Recyclers<Segment<?>>(16_382 / SEGMENT_SIZE) {

```

### MissortedModifiers
Missorted modifiers `static abstract`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/concurrent/DefaultExecutorChooserFactory.java`
#### Snippet
```java
    }

    private static abstract class AbstractExecutorChooser implements ExecutorChooser {

        protected final AtomicInteger idx = new AtomicInteger();
```

### MissortedModifiers
Missorted modifiers `protected @InjectConfiguration("mock.tables.foo = {}")`
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/BaseMvPartitionStorageTest.java`
#### Snippet
```java
    protected static final BinaryRow TABLE_ROW2 = binaryRow(KEY, new TestValue(30, "bar"));

    protected @InjectConfiguration("mock.tables.foo = {}") TablesConfiguration tablesCfg;

    protected StorageEngine engine;
```

## RuleId[id=FunctionalExpressionCanBeFolded]
### FunctionalExpressionCanBeFolded
Method reference can be replaced with qualifier
in `modules/core/src/main/java/org/apache/ignite/internal/causality/VersionedValue.java`
#### Snippet
```java

            CompletableFuture<CompletableFuture<T>> f0 = future
                    .handle(updater::apply)
                    .handle((fut, e) -> e == null ? fut : failedFuture(e));

```

## RuleId[id=WhileLoopSpinsOnField]
### WhileLoopSpinsOnField
`while` loop spins on field
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/tree/BplusTree.java`
#### Snippet
```java
            doVisit(this);

            while (nextPageId != 0) {
                nextPage();
            }
```

### WhileLoopSpinsOnField
`while` loop spins on field
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/tree/BplusTree.java`
#### Snippet
```java
         */
        public T find() throws IgniteInternalCheckedException {
            while (retry) {
                row = row0;

```

## RuleId[id=DefaultAnnotationParam]
### DefaultAnnotationParam
Redundant default parameter value assignment
in `modules/rest-api/src/main/java/org/apache/ignite/internal/rest/api/cluster/authentication/AuthenticationProviderConfigDto.java`
#### Snippet
```java
 */
@JsonTypeInfo(use = JsonTypeInfo.Id.NAME,
        include = JsonTypeInfo.As.PROPERTY,
        property = "type")
@JsonSubTypes({
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `modules/rest-api/src/main/java/org/apache/ignite/internal/rest/api/deployment/DeploymentCodeApi.java`
#### Snippet
```java
            description = "Unit undeployed successfully.",
            //DO NOT Remove redundant parameter. It will BREAK generated spec.
            content = @Content(mediaType = APPLICATION_JSON, schema = @Schema(implementation = Void.class))
    )
    @ApiResponse(responseCode = "404",
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `modules/rest-api/src/main/java/org/apache/ignite/internal/rest/api/deployment/DeploymentCodeApi.java`
#### Snippet
```java
            description = "Unit undeployed successfully.",
            //DO NOT Remove redundant parameter. It will BREAK generated spec.
            content = @Content(mediaType = APPLICATION_JSON, schema = @Schema(implementation = Void.class))
    )
    @ApiResponse(responseCode = "404",
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rule/logical/LogicalOrToUnionRule.java`
#### Snippet
```java
     * TODO Documentation https://issues.apache.org/jira/browse/IGNITE-15859
     */
    @Value.Immutable(singleton = false)
    public interface Config extends RuleFactoryConfig<Config> {
        Config SCAN = ImmutableLogicalOrToUnionRule.Config.builder()
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rule/logical/FilterScanMergeRule.java`
#### Snippet
```java
     */
    @SuppressWarnings("ClassNameSameAsAncestorName")
    @Value.Immutable(singleton = false)
    public interface Config extends RuleFactoryConfig<Config> {
        Config DEFAULT = ImmutableFilterScanMergeRule.Config.builder()
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rule/logical/ProjectScanMergeRule.java`
#### Snippet
```java
     */
    @SuppressWarnings("ClassNameSameAsAncestorName")
    @Value.Immutable(singleton = false)
    public interface Config extends RuleFactoryConfig<Config> {
        Config DEFAULT = ImmutableProjectScanMergeRule.Config.builder()
```

## RuleId[id=ThrowFromFinallyBlock]
### ThrowFromFinallyBlock
`throw` inside 'finally' block
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/PersistentPageMemory.java`
#### Snippet
```java
                LOG.debug("Failed to unlock page [fullPageId={}, binPage={}]", fullId, toHexString(page, systemPageSize()));

                throw ex;
            }
        }
```

## RuleId[id=IfStatementWithIdenticalBranches]
### IfStatementWithIdenticalBranches
Common parts with variables can be extracted from 'if'
in `modules/core/src/main/java/org/apache/ignite/internal/util/ArrayUtils.java`
#### Snippet
```java
        assert idx >= 0 && idx < len : idx + " < " + len;

        if (idx < len >>> 1) {
            T[] res = Arrays.copyOfRange(arr, 1, len);

```

### IfStatementWithIdenticalBranches
Common parts with variables can be extracted from 'if'
in `modules/core/src/main/java/org/apache/ignite/internal/util/ArrayUtils.java`
#### Snippet
```java
        assert idx >= 0 && idx < len : idx + " < " + len;

        if (idx < len >>> 1) {
            long[] res = Arrays.copyOfRange(arr, 1, len);

```

### IfStatementWithIdenticalBranches
Common part can be extracted removing branch
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/rpc/impl/AbstractClientService.java`
#### Snippet
```java
                        Status status = Status.OK();
                        Message msg;
                        if (result instanceof ErrorResponse) {
                            status = handleErrorResponse((ErrorResponse) result);
                            msg = (Message) result;
```

### IfStatementWithIdenticalBranches
Common part can be extracted from 'if'
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/storage/impl/LogManagerImpl.java`
#### Snippet
```java
    private boolean checkAndResolveConflict(final List<LogEntry> entries, final StableClosure done) {
        final LogEntry firstLogEntry = ArrayDeque.peekFirst(entries);
        if (firstLogEntry.getId().getIndex() == 0) {
            // Node is currently the leader and |entries| are from the user who
            // don't know the correct indexes the logs should assign to. So we have
```

## RuleId[id=UnnecessarySuperQualifier]
### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/LogScheduledThreadPoolExecutor.java`
#### Snippet
```java
        }
        if (t != null) {
            LOG.error("Uncaught exception in pool: {}, {}.", t, this.name, super.toString());
        }
    }
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/LogScheduledThreadPoolExecutor.java`
#### Snippet
```java
    protected void terminated() {
        super.terminated();
        LOG.info("ThreadPool is terminated: {}, {}.", this.name, super.toString());
    }
}
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/LogThreadPoolExecutor.java`
#### Snippet
```java
        }
        if (t != null) {
            LOG.error("Uncaught exception in pool: {}, {}.", t, this.name, super.toString());
        }
    }
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/LogThreadPoolExecutor.java`
#### Snippet
```java
    protected void terminated() {
        super.terminated();
        LOG.info("ThreadPool is terminated: {}, {}.", this.name, super.toString());
    }
}
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `modules/file-io/src/main/java/org/apache/ignite/internal/fileio/AsyncFileIo.java`
#### Snippet
```java

            // Release waiter and allow next operation to begin.
            super.complete(res);
        }

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `modules/file-io/src/main/java/org/apache/ignite/internal/fileio/AsyncFileIo.java`
#### Snippet
```java
        @Override
        public void failed(Throwable exc, AsyncFileIo attach) {
            super.completeExceptionally(exc);
        }
    }
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/store/DeltaFilePageStoreIo.java`
#### Snippet
```java

        try {
            super.read(pageId, pageOff, pageBuf, keepCrc);

            return true;
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `modules/storage-page-memory/src/main/java/org/apache/ignite/internal/storage/pagememory/mv/RowVersionFreeList.java`
#### Snippet
```java
     */
    public void removeDataRowByLink(long link) throws IgniteInternalCheckedException {
        super.removeDataRowByLink(link, statHolder);
    }

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `modules/storage-page-memory/src/main/java/org/apache/ignite/internal/storage/pagememory/mv/RowVersionFreeList.java`
#### Snippet
```java
     */
    public void insertDataRow(RowVersion row) throws IgniteInternalCheckedException {
        super.insertDataRow(row, statHolder);
    }

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `modules/storage-page-memory/src/main/java/org/apache/ignite/internal/storage/pagememory/mv/RowVersionFreeList.java`
#### Snippet
```java
     */
    public void saveMetadata() throws IgniteInternalCheckedException {
        super.saveMetadata(statHolder);
    }
}
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `modules/storage-page-memory/src/main/java/org/apache/ignite/internal/storage/pagememory/index/freelist/IndexColumnsFreeList.java`
#### Snippet
```java
     */
    public void removeDataRowByLink(long link) throws IgniteInternalCheckedException {
        super.removeDataRowByLink(link, statHolder);
    }
}
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `modules/storage-page-memory/src/main/java/org/apache/ignite/internal/storage/pagememory/index/freelist/IndexColumnsFreeList.java`
#### Snippet
```java
     */
    public void insertDataRow(IndexColumns row) throws IgniteInternalCheckedException {
        super.insertDataRow(row, statHolder);
    }

```

## RuleId[id=SlowListContainsAll]
### SlowListContainsAll
Call to 'list.containsAll(collection)' may have poor performance
in `modules/schema/src/main/java/org/apache/ignite/internal/schema/configuration/index/IndexValidatorImpl.java`
#### Snippet
```java
            if (collocationColumns.isEmpty()) {
                ctx.addIssue(new ValidationIssue(indexView.name(), "Unique index is not supported for tables without primary key"));
            } else if (!indexedColumns.containsAll(collocationColumns)) {
                ctx.addIssue(new ValidationIssue(indexView.name(), "Unique index must include all colocation columns"));
            }
```

## RuleId[id=InfiniteLoopStatement]
### InfiniteLoopStatement
`while` statement cannot complete without throwing an exception
in `modules/table/src/testFixtures/java/org/apache/ignite/internal/table/impl/DummyInternalTableImpl.java`
#### Snippet
```java
        @Override
        public void run() {
            while (true) {
                safeTime.update(CLOCK.now());

```

## RuleId[id=CodeBlock2Expr]
### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `modules/cli/src/main/java/org/apache/ignite/internal/cli/commands/sql/help/SqlHelpCommand.java`
#### Snippet
```java
                    .map(IgniteSqlCommand::getSyntax)
                    .or(() -> {
                        return Optional.ofNullable(self.getParent())
                                .map(it -> it.getSubcommands().get(command).getUsageMessage());
                    })
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `modules/table/src/testFixtures/java/org/apache/ignite/internal/table/TxAbstractTest.java`
#### Snippet
```java

        CompletableFuture<Double> fut0 = igniteTransactions.runInTransactionAsync(tx -> {
            return view.getAsync(tx, makeKey(2))
                    .thenCompose(val2 -> {
                        double prev = val2.doubleValue("balance"); // val2 is null - NPE is thrown here
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `modules/runner/src/main/java/org/apache/ignite/internal/app/IgniteImpl.java`
#### Snippet
```java
                    }, startupExecutor)
                    // Moving to the common pool on purpose to close the startup pool and proceed user's code in the common pool.
                    .whenCompleteAsync((res, ex) -> {
                        startupExecutor.shutdownNow();
                    });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `modules/metrics/src/main/java/org/apache/ignite/internal/metrics/exporters/jmx/MetricSetMbean.java`
#### Snippet
```java
        List<MBeanAttributeInfo> attrs = new ArrayList<>();

        iter.forEachRemaining(metric -> {
            attrs.add(new MBeanAttributeInfo(
                    metric.name(),
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `modules/network/src/main/java/org/apache/ignite/internal/network/serialization/PerSessionSerializationService.java`
#### Snippet
```java
                    List<FieldDescriptorMessage> fields = descriptor.fields().stream()
                            .map(d -> {
                                return MSG_FACTORY.fieldDescriptorMessage()
                                        .name(d.name())
                                        .typeDescriptorId(d.typeDescriptorId())
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `modules/replicator/src/main/java/org/apache/ignite/internal/replicator/ReplicaService.java`
#### Snippet
```java

                                fut = messagingService.invoke(targetNodeConsistentId, awaitReplicaReq, RPC_TIMEOUT)
                                        .whenComplete((response0, throwable0) -> {
                                            pendingInvokes.remove(targetNodeConsistentId);
                                        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/util/RexUtils.java`
#### Snippet
```java
            // It's more effective to put equality conditions in the collation first.
            fieldsToPredicates.int2ObjectEntrySet()
                    .forEach(entry -> {
                        (entry.getValue().stream().anyMatch(v -> v.getOperator().getKind() == EQUALS) ? fields : lastFields)
                                .add(entry.getIntKey());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `modules/metastorage/src/main/java/org/apache/ignite/internal/metastorage/server/raft/MetaStorageListener.java`
#### Snippet
```java
    @Override
    public void onSnapshotSave(Path path, Consumer<Throwable> doneClo) {
        storage.snapshot(path).whenComplete((unused, throwable) -> {
            doneClo.accept(throwable);
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `modules/client-handler/src/main/java/org/apache/ignite/client/handler/requests/jdbc/JdbcMetadataCatalog.java`
#### Snippet
```java

        return tables.tablesAsync().thenApply(tablesList -> {
            return tablesList.stream()
                    .filter(t -> matches(DEFAULT_SCHEMA_NAME, schemaNameRegex))
                    .filter(t -> matches(t.name(), tlbNameRegex))
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `modules/client-handler/src/main/java/org/apache/ignite/client/handler/requests/jdbc/JdbcMetadataCatalog.java`
#### Snippet
```java

        return tables.tablesAsync().thenApply(tablesList -> {
            return tablesList.stream()
                    .filter(t -> matches(DEFAULT_SCHEMA_NAME, schemaNameRegex))
                    .filter(t -> matches(t.name(), tlbNameRegex))
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `modules/cluster-management/src/main/java/org/apache/ignite/internal/cluster/management/DistributedConfigurationUpdater.java`
#### Snippet
```java
            Authentication authentication) {
        return authConfiguration.change(authChange -> {
            authChange.changeProviders(providers -> {
                authentication.providers().forEach(provider -> {
                    providers.create(provider.name(), cfg -> applyProviderChange(cfg, provider));
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `modules/cluster-management/src/main/java/org/apache/ignite/internal/cluster/management/DistributedConfigurationUpdater.java`
#### Snippet
```java
        return authConfiguration.change(authChange -> {
            authChange.changeProviders(providers -> {
                authentication.providers().forEach(provider -> {
                    providers.create(provider.name(), cfg -> applyProviderChange(cfg, provider));
                });
```

## RuleId[id=EmptyMethod]
### EmptyMethod
Method only calls its super
in `modules/cli/src/main/java/org/apache/ignite/internal/cli/core/exception/CommandExecutionException.java`
#### Snippet
```java

    @Override
    public String toString() {
        return super.toString();
    }
```

### EmptyMethod
Method only calls its super
in `modules/cli/src/main/java/org/apache/ignite/internal/cli/core/exception/ConnectCommandException.java`
#### Snippet
```java

    @Override
    public String toString() {
        return super.toString();
    }
```

### EmptyMethod
All implementations of this method are empty
in `modules/cli/src/main/java/org/apache/ignite/internal/cli/core/repl/terminal/TerminalCustomizer.java`
#### Snippet
```java
     * @param terminal provided terminal instance.
     */
    void customize(Terminal terminal);
}

```

### EmptyMethod
The method is empty
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/DynamicConfiguration.java`
#### Snippet
```java
     * @param members  Configuration members (leaves and nodes).
     */
    protected void removeMembers(VIEWT oldValue, Map<String, ConfigurationProperty<?>> members) {
        // No-op.
    }
```

### EmptyMethod
The method is empty
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/DynamicConfiguration.java`
#### Snippet
```java
     * @param members  Configuration members (leaves and nodes).
     */
    protected void addMembers(VIEWT newValue, Map<String, ConfigurationProperty<?>> members) {
        // No-op.
    }
```

### EmptyMethod
The method is empty
in `modules/metrics/src/main/java/org/apache/ignite/internal/metrics/AbstractMetricSource.java`
#### Snippet
```java
     * @param holder Metric instances holder.
     */
    protected void cleanup(T holder) {
        // No-op.
    }
```

### EmptyMethod
Method only calls its super
in `modules/network/src/main/java/org/apache/ignite/internal/network/serialization/marshal/UosObjectInputStream.java`
#### Snippet
```java

    @Override
    public byte[] readAllBytes() throws IOException {
        return super.readAllBytes();
    }
```

### EmptyMethod
All implementations of this method are empty
in `modules/network-api/src/main/java/org/apache/ignite/network/serialization/MessageReader.java`
#### Snippet
```java
     * @param msgCls Message type.
     */
    public void setCurrentReadClass(Class<? extends NetworkMessage> msgCls);

    /**
```

### EmptyMethod
All implementations of this method are empty
in `modules/network-api/src/main/java/org/apache/ignite/network/serialization/MessageWriter.java`
#### Snippet
```java
     * @param msgCls Message type.
     */
    public void setCurrentWriteClass(Class<? extends NetworkMessage> msgCls);

    /**
```

### EmptyMethod
All implementations of this method are empty
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/evict/PageEvictionTracker.java`
#### Snippet
```java
     * @throws IgniteInternalCheckedException In case of page memory error.
     */
    void touchPage(long pageId) throws IgniteInternalCheckedException;

    /**
```

### EmptyMethod
All implementations of this method are empty
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/evict/PageEvictionTracker.java`
#### Snippet
```java
     * @throws IgniteInternalCheckedException In case of page memory error.
     */
    void evictDataPage() throws IgniteInternalCheckedException;

    /**
```

### EmptyMethod
All implementations of this method are empty
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/evict/PageEvictionTracker.java`
#### Snippet
```java
     * @throws IgniteInternalCheckedException In case of page memory error.
     */
    void forgetPage(long pageId) throws IgniteInternalCheckedException;
}

```

### EmptyMethod
All implementations of this method are empty
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/metric/IoStatisticsHolder.java`
#### Snippet
```java
     * @param pageAddr Address of page.
     */
    void trackLogicalRead(long pageAddr);

    /**
```

### EmptyMethod
All implementations of this method are empty
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/metric/IoStatisticsHolder.java`
#### Snippet
```java
     * @param pageAddr Start address of page.
     */
    void trackPhysicalAndLogicalRead(long pageAddr);

    /**
```

### EmptyMethod
All implementations of this method are empty
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/PersistentPageMemory.java`
#### Snippet
```java
         * @param pageMemoryImpl Page memory.
         */
        void apply(long page, FullPageId fullPageId, PersistentPageMemory pageMemoryImpl);
    }

```

### EmptyMethod
The method is empty
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/checkpoint/CheckpointWorkflow.java`
#### Snippet
```java
     * Starts a checkpoint workflow.
     */
    public void start() {
        // No-op.
    }
```

### EmptyMethod
The method is empty
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/tree/BplusTree.java`
#### Snippet
```java
     * processes to acquire it.
     */
    protected void temporaryReleaseLock() {
        // No-op.
    }
```

### EmptyMethod
The method is empty
in `modules/placement-driver/src/main/java/org/apache/ignite/internal/placementdriver/AssignmentsTracker.java`
#### Snippet
```java
     * Triggers to renew leases forcibly. The method wakes up the monitor of {@link LeaseUpdater}.
     */
    private void triggerToRenewLeases() {
        //TODO: IGNITE-18879 Implement lease maintenance.
    }
```

### EmptyMethod
The method is empty
in `modules/placement-driver/src/main/java/org/apache/ignite/internal/placementdriver/TopologyTracker.java`
#### Snippet
```java
     * Triggers to renew leases forcibly. The method wakes up the monitor of {@link LeaseUpdater}.
     */
    private void triggerToRenewLeases() {
        //TODO: IGNITE-18879 Implement lease maintenance.
    }
```

### EmptyMethod
The method is empty
in `modules/raft/src/main/java/org/apache/ignite/internal/raft/storage/impl/RocksDbSharedLogStorage.java`
#### Snippet
```java
     */
    @SuppressWarnings("RedundantThrows")
    protected void onSync() throws IOException, InterruptedException {
    }

```

### EmptyMethod
The method is empty
in `modules/raft/src/main/java/org/apache/ignite/internal/raft/storage/impl/RocksDbSharedLogStorage.java`
#### Snippet
```java
     */
    @SuppressWarnings("unused")
    protected void onReset(long nextLogIndex) {
    }

```

### EmptyMethod
The method is empty
in `modules/raft/src/main/java/org/apache/ignite/internal/raft/storage/impl/RocksDbSharedLogStorage.java`
#### Snippet
```java
     */
    @SuppressWarnings("unused")
    protected void onTruncatePrefix(long startIndex, long firstIndexKept) throws RocksDBException,
            IOException {
    }
```

### EmptyMethod
The method is empty
in `modules/raft/src/main/java/org/apache/ignite/internal/raft/storage/impl/RocksDbSharedLogStorage.java`
#### Snippet
```java
     */
    @SuppressWarnings("unused")
    protected void onTruncateSuffix(long lastIndexKept) throws RocksDBException, IOException {
    }

```

### EmptyMethod
The method is empty
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/storage/impl/RocksDBLogStorage.java`
#### Snippet
```java
     * @param nextLogIndex next log index
     */
    protected void onReset(final long nextLogIndex) {
    }

```

### EmptyMethod
The method is empty
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/storage/impl/RocksDBLogStorage.java`
#### Snippet
```java
     * @param firstIndexKept the first index to kept
     */
    protected void onTruncatePrefix(final long startIndex, final long firstIndexKept) throws RocksDBException,
        IOException {
    }
```

### EmptyMethod
The method is empty
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/storage/impl/RocksDBLogStorage.java`
#### Snippet
```java
     * Called when sync data into file system.
     */
    protected void onSync() throws IOException, InterruptedException {
    }

```

### EmptyMethod
The method is empty
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/storage/impl/RocksDBLogStorage.java`
#### Snippet
```java
     * @param lastIndexKept the last index to kept
     */
    protected void onTruncateSuffix(final long lastIndexKept) throws RocksDBException, IOException {
    }

```

### EmptyMethod
All implementations of this method are empty
in `modules/rest-api/src/main/java/org/apache/ignite/internal/rest/api/configuration/NodeConfigurationApi.java`
#### Snippet
```java
    })
    @Get
    String getConfiguration();

    /**
```

### EmptyMethod
The method is empty
in `modules/rocksdb-common/src/main/java/org/apache/ignite/internal/rocksdb/BusyRocksIteratorAdapter.java`
#### Snippet
```java
     * Handles busy lock acquiring success.
     */
    protected void handeBusySuccess() {
        // No-op.
    }
```

### EmptyMethod
The method is empty
in `modules/table/src/testFixtures/java/org/apache/ignite/internal/table/TxAbstractTest.java`
#### Snippet
```java

    @Test
    public void testLockedTooLong() {
        // TODO asch IGNITE-15936 if lock can't be acquired until timeout tx should be rolled back.
    }
```

## RuleId[id=RedundantFieldInitialization]
### RedundantFieldInitialization
Field initialization to `false` is redundant
in `modules/api/src/main/java/org/apache/ignite/tx/TransactionOptions.java`
#### Snippet
```java

    /** Read-only transaction. */
    private boolean readOnly = false;

    /**
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `modules/api/src/main/java/org/apache/ignite/tx/TransactionOptions.java`
#### Snippet
```java
public class TransactionOptions {
    /** Transaction timeout. */
    private long timeoutMillis = 0;

    /** Read-only transaction. */
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `modules/api/src/main/java/org/apache/ignite/table/TupleImpl.java`
#### Snippet
```java
        return new Iterator<>() {
            /** Current column index. */
            private int cur = 0;

            /** {@inheritDoc} */
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `modules/api/src/main/java/org/apache/ignite/table/mapper/MapperBuilder.java`
#### Snippet
```java

    /** Flag indicates, if all unmapped object fields should be mapped to the columns with same names automatically. */
    private boolean automapFlag = false;

    /** {@code True} if the {@link #build()} method was called, {@code false} otherwise. */
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `modules/core/src/main/java/org/apache/ignite/internal/util/HashCalculator.java`
#### Snippet
```java
 */
public class HashCalculator {
    private int hash = 0;

    /**
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `modules/core/src/main/java/org/apache/ignite/internal/util/IgniteIntList.java`
#### Snippet
```java
    public IgniteIntIterator iterator() {
        return new IgniteIntIterator() {
            int cur = 0;

            @Override
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `modules/core/src/main/java/org/apache/ignite/internal/util/CollectionUtils.java`
#### Snippet
```java
        return () -> new Iterator<>() {
            /** Current index at {@code iterables}. */
            int idx = 0;

            /** Current iterator. */
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `modules/core/src/main/java/org/apache/ignite/internal/util/CollectionUtils.java`
#### Snippet
```java
        return new Iterator<>() {
            /** Current index at {@code iterators}. */
            int idx = 0;

            /** Current iterator. */
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `modules/core/src/main/java/org/apache/ignite/internal/causality/VersionedValue.java`
#### Snippet
```java
     * See {@link #update(long, BiFunction)}.
     */
    private volatile CompletableFuture<T> updaterFuture = null;

    /**
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/RaftGroupService.java`
#### Snippet
```java
    private static final IgniteLogger LOG = Loggers.forClass(RaftGroupService.class);

    private volatile boolean started = false;

    /**
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/core/BallotBox.java`
#### Snippet
```java
    private ClosureQueue closureQueue;
    private final StampedLock stampedLock = new StampedLock();
    private long lastCommittedIndex = 0;
    private long pendingIndex; // Par. 3.6 prevent commits from previous terms
    private final SegmentList<Ballot> pendingMetaQueue = new SegmentList<>(false);
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/CRC64.java`
#### Snippet
```java
        0x14DEA25F3AF9026DL, 0x562E43B4931334FEL, 0x913F6188692D6F4BL, 0xD3CF8063C0C759D8L, 0x5DEDC41A34BBEEB2L,
        0x1F1D25F19D51D821L, 0xD80C07CD676F8394L, 0x9AFCE626CE85B507L};
    private long crc = 0;

    @Override
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/RecyclableByteBufferList.java`
#### Snippet
```java
    private static final int DEFAULT_INITIAL_CAPACITY = 8;

    private int capacity = 0;

    /**
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/CountDownEvent.java`
#### Snippet
```java
public class CountDownEvent {

    private int state = 0;
    private final Lock lock = new ReentrantLock();
    private final Condition busyCond = this.lock.newCondition();
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/concurrent/MpscSingleThreadExecutor.java`
#### Snippet
```java
        final Thread thread;
        volatile int notifyNeeded = NOT_NEEDED;
        boolean stop = false;

        private Worker(Thread thread) {
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/concurrent/AdjustableSemaphore.java`
#### Snippet
```java

    private final ResizeableSemaphore semaphore = new ResizeableSemaphore(0);
    private volatile int maxPermits = 0;

    public AdjustableSemaphore() {
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/core/Replicator.java`
#### Snippet
```java
    private volatile long lastRpcSendTimestamp;
    private volatile long heartbeatCounter = 0;
    private volatile long probeCounter = 0;
    private volatile long appendEntriesCounter = 0;
    private volatile long installSnapshotCounter = 0;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/core/Replicator.java`
#### Snippet
```java
    private volatile long probeCounter = 0;
    private volatile long appendEntriesCounter = 0;
    private volatile long installSnapshotCounter = 0;
    protected Stat statInfo = new Stat();
    private ScheduledFuture<?> blockTimer;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/core/Replicator.java`
#### Snippet
```java
    // Next sending log index
    private volatile long nextIndex;
    private int consecutiveErrorTimes = 0;
    private boolean hasSucceeded;
    private long timeoutNowIndex;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/core/Replicator.java`
#### Snippet
```java
    private volatile long heartbeatCounter = 0;
    private volatile long probeCounter = 0;
    private volatile long appendEntriesCounter = 0;
    private volatile long installSnapshotCounter = 0;
    protected Stat statInfo = new Stat();
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/core/Replicator.java`
#### Snippet
```java
    private int reqSeq = 0;
    // Response sequence
    private int requiredNextSeq = 0;
    // Replicator state reset version
    private int version = 0;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/core/Replicator.java`
#### Snippet
```java
    private int requiredNextSeq = 0;
    // Replicator state reset version
    private int version = 0;

    // Pending response queue;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/core/Replicator.java`
#### Snippet
```java

    // Request sequence
    private int reqSeq = 0;
    // Response sequence
    private int requiredNextSeq = 0;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/core/Replicator.java`
#### Snippet
```java
    private long timeoutNowIndex;
    private volatile long lastRpcSendTimestamp;
    private volatile long heartbeatCounter = 0;
    private volatile long probeCounter = 0;
    private volatile long appendEntriesCounter = 0;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/option/RpcOptions.java`
#### Snippet
```java
     * Whether to enable checksum for RPC.
     */
    private boolean enableRpcChecksum = false;

    /**
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/option/BootstrapOptions.java`
#### Snippet
```java

    // Whether to enable metrics for node.
    private boolean enableMetrics = false;

    /**
```

### RedundantFieldInitialization
Field initialization to `0L` is redundant
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/option/BootstrapOptions.java`
#### Snippet
```java
    // The index of the last index which the dumping snapshot contains
    // Default: 0
    private long lastLogIndex = 0L;

    // The specific StateMachine which is going to dump the first snapshot
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/option/RaftOptions.java`
#### Snippet
```java
     * Sync log meta, snapshot meta and raft meta
     */
    private boolean syncMeta = false;

    /**
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/option/RaftOptions.java`
#### Snippet
```java
     * File service check hole switch, default disable
     */
    private boolean fileCheckHole = false;

    /**
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/option/RaftOptions.java`
#### Snippet
```java
     *
     */
    private boolean enableLogEntryChecksum = false;

    /**
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/storage/LogManager.java`
#### Snippet
```java
    abstract class StableClosure implements Closure {

        protected long firstLogIndex = 0;
        protected List<LogEntry> entries;
        protected int nEntries;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/storage/impl/VolatileLogStorage.java`
#### Snippet
```java
    private volatile long lastSpiltLogIndex = -1;

    private volatile boolean initialized = false;

    public VolatileLogStorage(LogStorageBudget inMemoryBudget, Logs inMemoryLogs, Logs spiltOnDisk) {
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/storage/impl/RocksDbSpillout.java`
#### Snippet
```java
    private volatile long firstLogIndex = 1;

    private volatile long lastLogIndex = 0;

    /** Constructor. */
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/storage/impl/RocksDbSpillout.java`
#### Snippet
```java

    /** Flag indicating whether storage is stopped. Guarded by readWriteLock. */
    private boolean stopped = false;

    /** Executor that handles prefix truncation. */
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/option/NodeOptions.java`
#### Snippet
```java

    /** */
    private boolean sharedPools = false;

    public NodeOptions() {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/option/NodeOptions.java`
#### Snippet
```java
    // only if they has the same filename and the same checksum (stored in file meta).
    // Default: false
    private boolean filterBeforeCopyRemote = false;

    // If non-null, we will pass this throughput_snapshot_throttle to SnapshotExecutor
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/option/NodeOptions.java`
#### Snippet
```java
     * Whether to enable metrics for node.
     */
    private boolean enableMetrics = false; // TODO asch https://issues.apache.org/jira/browse/IGNITE-14847

    /**
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/option/NodeOptions.java`
#### Snippet
```java
    // If true, RPCs through raft_cli will be denied.
    // Default: false
    private boolean disableCli = false;

    /**
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/option/NodeOptions.java`
#### Snippet
```java
    //
    // Default: 0
    private int snapshotLogIndexMargin = 0;

    // We will regard a adding peer as caught up if the margin between the
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/storage/impl/LocalLogStorage.java`
#### Snippet
```java

    private volatile long firstLogIndex = 1;
    private volatile long lastLogIndex = 0;

    private volatile boolean initialized = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/storage/impl/LocalLogStorage.java`
#### Snippet
```java
    private volatile long lastLogIndex = 0;

    private volatile boolean initialized = false;

    public LocalLogStorage(final RaftOptions raftOptions) {
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/storage/snapshot/remote/CopySession.java`
#### Snippet
```java
    private final SnapshotThrottle snapshotThrottle;
    private final RaftOptions raftOptions;
    private int retryTimes = 0;
    private boolean finished;
    private ByteBufferCollector destBuf;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `modules/raft/src/main/java/org/apache/ignite/internal/raft/storage/impl/RocksDbSharedLogStorage.java`
#### Snippet
```java

    /** Flag indicating whether storage is stopped. Guarded by readWriteLock. */
    private boolean stopped = false;

    /** Executor that handles prefix truncation. */
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `modules/raft/src/testFixtures/java/org/apache/ignite/raft/jraft/core/MockStateMachine.java`
#### Snippet
```java

    private final Lock lock = new ReentrantLock();
    private volatile int onStartFollowingTimes = 0;
    private volatile int onStopFollowingTimes = 0;
    private volatile long appliedIndex = -1;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `modules/raft/src/testFixtures/java/org/apache/ignite/raft/jraft/core/MockStateMachine.java`
#### Snippet
```java
    private final Lock lock = new ReentrantLock();
    private volatile int onStartFollowingTimes = 0;
    private volatile int onStopFollowingTimes = 0;
    private volatile long appliedIndex = -1;
    private volatile long snapshotIndex = -1L;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `modules/rest/src/main/java/org/apache/ignite/internal/rest/authentication/DelegatingAuthenticationProvider.java`
#### Snippet
```java
    private final ReadWriteLock rwLock = new ReentrantReadWriteLock();
    private final List<Authenticator> authenticators = new ArrayList<>();
    private boolean authEnabled = false;

    @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/raft/snapshot/outgoing/OutgoingSnapshot.java`
#### Snippet
```java
    private RowId lastRowId;

    private boolean startedToReadMvPartition = false;

    private Cursor<IgniteBiTuple<UUID, TxMeta>> txDataCursor;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/raft/snapshot/outgoing/OutgoingSnapshot.java`
#### Snippet
```java
    private boolean finishedTxData = false;

    private volatile boolean closed = false;

    /**
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/raft/snapshot/outgoing/OutgoingSnapshot.java`
#### Snippet
```java
     * This becomes {@code true} as soon as we exhaust TX data in the partition.
     */
    private boolean finishedTxData = false;

    private volatile boolean closed = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `modules/client/src/main/java/org/apache/ignite/internal/client/SslConfigurationBuilder.java`
#### Snippet
```java
    private static final String DEFAULT_KEYSTORE_TYPE = "PKCS12";

    private boolean enabled = false;

    private ClientAuthenticationMode clientAuth = ClientAuthenticationMode.NONE;
```

### RedundantFieldInitialization
Field initialization to `0L` is redundant
in `modules/runner/src/main/java/org/apache/ignite/internal/configuration/storage/LocalFileConfigurationStorage.java`
#### Snippet
```java
    private final InFlightFutures futureTracker = new InFlightFutures();

    private long lastRevision = 0L;

    /**
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `modules/schema/src/main/java/org/apache/ignite/internal/schema/BinaryRowConverter.java`
#### Snippet
```java
        // See if there are any NULL values and estimate total data size.
        var stats = new Sink() {
            boolean hasNulls = false;
            int estimatedValueSize = 0;

```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `modules/schema/src/main/java/org/apache/ignite/internal/schema/BinaryRowConverter.java`
#### Snippet
```java
        var stats = new Sink() {
            boolean hasNulls = false;
            int estimatedValueSize = 0;

            @Override
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `modules/client/src/main/java/org/apache/ignite/internal/client/table/ClientTable.java`
#### Snippet
```java
    private final Object latestSchemaLock = new Object();

    private volatile List<String> partitionAssignment = null;

    private volatile long partitionAssignmentVersion = -1;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `modules/schema/src/main/java/org/apache/ignite/internal/schema/configuration/TablesConfigurationSchema.java`
#### Snippet
```java
    /** Global integer id counter. Used as an auto-increment counter to generate integer identifiers for table. */
    @Value(hasDefault = true)
    public int globalIdCounter = 0;

    /** List of configured tables. */
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `modules/schema/src/main/java/org/apache/ignite/internal/schema/configuration/ColumnConfigurationSchema.java`
#### Snippet
```java
    @Value(hasDefault = true)
    @Immutable
    public boolean nullable = false;

    /** Default value. */
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `modules/schema/src/main/java/org/apache/ignite/internal/schema/configuration/ColumnTypeConfigurationSchema.java`
#### Snippet
```java
    @Immutable
    @Range(min = 0)
    public int length = 0;

    /** Precision. */
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `modules/schema/src/main/java/org/apache/ignite/internal/schema/configuration/ColumnTypeConfigurationSchema.java`
#### Snippet
```java
    @Immutable
    @Range(min = 0)
    public int precision = 0;

    /** Scale. */
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `modules/schema/src/main/java/org/apache/ignite/internal/schema/configuration/ColumnTypeConfigurationSchema.java`
#### Snippet
```java
    @Immutable
    @Range(min = 0)
    public int scale = 0;
}

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `modules/schema/src/main/java/org/apache/ignite/internal/schema/configuration/index/TableIndexConfigurationSchema.java`
#### Snippet
```java
    /** Has default value flag. */
    @Value(hasDefault = true)
    public boolean uniq = false;
}

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `modules/metrics/src/main/java/org/apache/ignite/internal/metrics/CompositeAwareIterator.java`
#### Snippet
```java

    /** Iterator for single composite metric, iterating over the group of scalar ones. */
    private Iterator<Metric> compositeMetricIterator = null;

    /**
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `modules/metrics/src/main/java/org/apache/ignite/internal/metrics/DistributionMetric.java`
#### Snippet
```java

    /** List of scalar metrics. */
    private volatile List<Metric> scalarMetrics = null;

    /**
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `modules/network/src/main/java/org/apache/ignite/internal/network/netty/NettyClient.java`
#### Snippet
```java
    /** Client channel. */
    @Nullable
    private volatile Channel channel = null;

    /** Flag indicating if {@link #stop()} has been called. */
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `modules/network/src/main/java/org/apache/ignite/internal/network/netty/NettyClient.java`
#### Snippet
```java

    /** Flag indicating if {@link #stop()} has been called. */
    private boolean stopped = false;

    /**
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `modules/network/src/main/java/org/apache/ignite/internal/network/netty/NettyClient.java`
#### Snippet
```java
    /** Future that resolves when the client finished the handshake. */
    @Nullable
    private volatile OrderingFuture<NettySender> senderFuture = null;

    /** Client channel. */
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `modules/network/src/main/java/org/apache/ignite/internal/network/netty/OutboundEncoder.java`
#### Snippet
```java
        /** Whether the message was fully written. */
        private boolean finished = false;
        private boolean descriptorsFinished = false;

        /**
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `modules/network/src/main/java/org/apache/ignite/internal/network/netty/OutboundEncoder.java`
#### Snippet
```java

        /** Whether the message was fully written. */
        private boolean finished = false;
        private boolean descriptorsFinished = false;

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `modules/network/src/main/java/org/apache/ignite/internal/network/serialization/marshal/UosIgniteOutputStream.java`
#### Snippet
```java
 */
class UosIgniteOutputStream extends IgniteUnsafeDataOutput {
    private boolean occupied = false;

    public UosIgniteOutputStream(int size) {
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `modules/network/src/main/java/org/apache/ignite/internal/network/serialization/marshal/MarshallingContext.java`
#### Snippet
```java
    private final Object2IntMap<Object> objectsToIds = new Object2IntOpenCustomHashMap<>(new IdentityHashStrategy());

    private int nextObjectId = 0;

    private Object objectCurrentlyWrittenWithWriteObject;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/AsyncWrapper.java`
#### Snippet
```java
    private CompletableFuture<BatchedResult<T>> requestChainTail = CompletableFuture.completedFuture(null);

    private volatile boolean cancelled = false;

    private volatile boolean firstRequest = true;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/ExecutionServiceImpl.java`
#### Snippet
```java
        private final CompletableFuture<AsyncRootNode<RowT, List<Object>>> root;

        private volatile Long rootFragmentId = null;


```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/AbstractSetOpNode.java`
#### Snippet
```java

        /** Processed rows count in current set. */
        protected int rowsCnt = 0;

        protected Grouping(ExecutionContext<RowT> ctx, RowFactory<RowT> rowFactory, AggregateType type, boolean all) {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/AsyncRootNode.java`
#### Snippet
```java
    private final Queue<PendingRequest<OutRowT>> pendingRequests = new ConcurrentLinkedQueue<>();

    private volatile boolean closed = false;

    /**
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/Inbox.java`
#### Snippet
```java
        private int lastEnqueued = -1;
        private int lastRequested = -1;
        private @Nullable Batch<RowT> curr = null;

        /**
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/Inbox.java`
#### Snippet
```java
         * and then immediately reset to null to prevent the same state from being sent once again.
         */
        private @Nullable SharedState sharedStateHolder = null;

        private RemoteSource(BatchRequester batchRequester) {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/prepare/FragmentSplitter.java`
#### Snippet
```java
    private FragmentProto curr;

    private boolean correlated = false;

    public FragmentSplitter(RelNode cutPoint) {
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/prepare/Splitter.java`
#### Snippet
```java
    private FragmentProto curr;

    private boolean correlated = false;

    /**
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/prepare/PlannerHelper.java`
#### Snippet
```java
    private static class JoinSizeFinder extends RelHomogeneousShuttle {
        private int countOfSources = 0;
        private int maxCountOfSourcesInSubQuery = 0;

        /** {@inheritDoc} */
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/prepare/PlannerHelper.java`
#### Snippet
```java
     */
    private static class JoinSizeFinder extends RelHomogeneousShuttle {
        private int countOfSources = 0;
        private int maxCountOfSourcesInSubQuery = 0;

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `modules/metastorage/src/main/java/org/apache/ignite/internal/metastorage/impl/CursorSubscription.java`
#### Snippet
```java

    /** Flag indicating that either the whole data range has been exhausted or the subscription has been cancelled. */
    private boolean isDone = false;

    /** Cached response from the Meta Storage leader. {@code null} if no requests have been issued yet. */
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `modules/metastorage/src/main/java/org/apache/ignite/internal/metastorage/impl/MetaStorageRaftGroupEventsListener.java`
#### Snippet
```java
     * <p>Multi-threaded access is guarded by {@code serializationFutureMux}.
     */
    private CompletableFuture<Void> serializationFuture = null;

    private final Object serializationFutureMux = new Object();
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/mem/unsafe/UnsafeMemoryProvider.java`
#### Snippet
```java

    /** Number of used data regions in {@link #regions} list. */
    private int used = 0;

    /** Memory allocator. */
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/util/GradualTaskExecutor.java`
#### Snippet
```java
    private final InFlightFutures inFlightFutures = new InFlightFutures();

    private volatile boolean cancelled = false;

    public GradualTaskExecutor(ExecutorService executor) {
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/datapage/ReadPageMemoryRowValue.java`
#### Snippet
```java
     * Number of bytes written to {@link #allValueBytes}.
     */
    private int transferredBytes = 0;

    @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `modules/metastorage/src/testFixtures/java/org/apache/ignite/internal/metastorage/server/SimpleInMemoryKeyValueStorage.java`
#### Snippet
```java
    private final Object mux = new Object();

    private boolean areWatchesEnabled = false;

    private final WatchProcessor watchProcessor = new WatchProcessor(this::get);
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/configuration/schema/PageMemoryCheckpointConfigurationSchema.java`
#### Snippet
```java
    @Range(min = 0)
    @Value(hasDefault = true)
    public long logReadLockThresholdTimeout = 0;

    /** Use an asynchronous file I/O operations provider. */
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `modules/binary-tuple/src/main/java/org/apache/ignite/internal/binarytuple/BinaryTupleReader.java`
#### Snippet
```java

    /** End offset of the current element. */
    private int end = 0;

    /**
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `modules/binary-tuple/src/main/java/org/apache/ignite/internal/binarytuple/BinaryTupleReader.java`
#### Snippet
```java
public class BinaryTupleReader extends BinaryTupleParser implements BinaryTupleParser.Sink {
    /** Start offset of the current element. */
    private int begin = 0;

    /** End offset of the current element. */
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `modules/binary-tuple/src/main/java/org/apache/ignite/internal/binarytuple/BinaryTupleBuilder.java`
#### Snippet
```java

    /** Current element. */
    private int elementIndex = 0;

    /** Number of elements in the tuple. */
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `modules/binary-tuple/src/main/java/org/apache/ignite/internal/binarytuple/BinaryTupleBuilder.java`
#### Snippet
```java

    /** Flag indicating if any NULL values were really put here. */
    private boolean hasNullValues = false;

    /**
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `modules/transactions/src/main/java/org/apache/ignite/internal/tx/impl/HeapLockManager.java`
#### Snippet
```java

        /** Marked for removal flag. */
        private boolean markedForRemove = false;

        public LockState(DeadlockPreventionPolicy deadlockPreventionPolicy, Executor delayedExecutor) {
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/DynamicProperty.java`
#### Snippet
```java
        ConfigurationSource src = new ConfigurationSource() {
            /** Current index in the {@code keys}. */
            private int level = 0;

            /** {@inheritDoc} */
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/DynamicConfiguration.java`
#### Snippet
```java
        ConfigurationSource src = new ConfigurationSource() {
            /** Current index in the {@code keys}. */
            private int level = 0;

            /** {@inheritDoc} */
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/tree/InnerNode.java`
#### Snippet
```java

    /** Immutability flag. */
    private boolean immutable = false;

    /**
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/tree/NamedListNode.java`
#### Snippet
```java

    /** Immutability flag. */
    private boolean immutable = false;

    /**
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `modules/configuration/src/testFixtures/java/org/apache/ignite/internal/configuration/storage/TestConfigurationStorage.java`
#### Snippet
```java

    /** Should fail on every operation. Guarded by {@code this}. */
    private boolean fail = false;

    /**
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `modules/configuration/src/testFixtures/java/org/apache/ignite/internal/configuration/storage/TestConfigurationStorage.java`
#### Snippet
```java

    /** Storage version. Guarded by {@code this}. */
    private long version = 0;

    /** Should fail on every operation. Guarded by {@code this}. */
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `modules/placement-driver/src/main/java/org/apache/ignite/internal/raft/client/TopologyAwareRaftGroupService.java`
#### Snippet
```java
    private static class ServerEventHandler {
        /** A term of last elected leader. */
        private long term = 0;

        /** A leader elected callback. */
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `modules/cluster-management/src/testFixtures/java/org/apache/ignite/internal/cluster/management/raft/TestClusterStateStorage.java`
#### Snippet
```java
    private final ReadWriteLock lock = new ReentrantReadWriteLock();

    private volatile boolean isStarted = false;

    @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `modules/cluster-management/src/main/java/org/apache/ignite/internal/cluster/management/ClusterManagementGroupManager.java`
#### Snippet
```java
     * the cluster.
     */
    private volatile boolean attemptedCompleteJoinOnStart = false;

    /** Constructor. */
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/tree/BplusTree.java`
#### Snippet
```java
        private final @Nullable TreeRowMapClosure<L, T, R> treeRowClosure;

        private @Nullable Boolean hasNext = null;

        /**
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/tree/BplusTree.java`
#### Snippet
```java
        private long currentPageId;

        private boolean finished = false;

        private DestroyTreeTask(LongListReuseBag bag, @Nullable Consumer<L> actOnEachElement, int rootLevel, long rootPageId) {
```

## RuleId[id=RedundantImplements]
### RedundantImplements
Redundant interface declaration `List`>
in `modules/api/src/main/java/org/apache/ignite/sql/BatchedArguments.java`
#### Snippet
```java
 * TODO: add named arguments support.
 */
public class BatchedArguments extends ArrayList<List<Object>> implements List<List<Object>> {
    /**
     * Creates batched arguments.
```

### RedundantImplements
Redundant interface declaration `Completer`
in `modules/cli/src/main/java/org/apache/ignite/internal/cli/core/repl/executor/IgnitePicocliCommands.java`
#### Snippet
```java
    }

    private class IgnitePicocliCompleter extends ArgumentCompleter implements Completer {

        public IgnitePicocliCompleter() {
```

### RedundantImplements
Redundant interface declaration `SchemaObjectBuilder`
in `modules/schema/src/testFixtures/java/org/apache/ignite/internal/schema/testutils/builder/PrimaryKeyDefinitionBuilderImpl.java`
#### Snippet
```java
 * Primary key builder.
 */
class PrimaryKeyDefinitionBuilderImpl implements SchemaObjectBuilder, PrimaryKeyDefinitionBuilder {
    /** Index columns. */
    @IgniteToStringInclude
```

### RedundantImplements
Redundant interface declaration `IgniteRel`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rel/IgniteSortedIndexSpool.java`
#### Snippet
```java
 * Relational operator that returns the sorted contents of a table and allow to lookup rows by specified bounds.
 */
public class IgniteSortedIndexSpool extends AbstractIgniteSpool implements IgniteRel {
    private final RelCollation collation;

```

### RedundantImplements
Redundant interface declaration `NetworkMessage`
in `modules/transactions/src/main/java/org/apache/ignite/internal/tx/message/TxFinishResponse.java`
#### Snippet
```java
 */
@Transferable(value = TxMessageGroup.TX_FINISH_RESPONSE)
public interface TxFinishResponse extends NetworkMessage, TimestampAware, Serializable {
    /**
     * Returns the error message.
```

## RuleId[id=InstanceofCatchParameter]
### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `modules/core/src/main/java/org/apache/ignite/internal/util/worker/IgniteWorker.java`
#### Snippet
```java
            log.warn("Runtime error caught during ignite runnable execution [worker={}]", e, this);

            if (e instanceof Error) {
                throw e;
            }
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `modules/core/src/main/java/org/apache/ignite/internal/util/IgniteUtils.java`
#### Snippet
```java
            // or something wrong happened. Anyway, let's try to be over-diagnosing
            if (log != null) {
                if (e instanceof AtomicMoveNotSupportedException) {
                    log.info("Atomic move not supported. Falling back to non-atomic move [reason={}]", e.getMessage());
                } else {
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `t`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/concurrent/MpscSingleThreadExecutor.java`
#### Snippet
```java
                this.state = ST_TERMINATED;

                if (!(t instanceof Exception)) {
                    // Also rethrow as it may be an OOME for example
                    throw new RuntimeException(t);
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `modules/network/src/main/java/org/apache/ignite/network/DefaultMessagingService.java`
#### Snippet
```java
                    LOG.error("onMessage() failed while processing {} from {}", e, obj.message(), obj.consistentId());

                    if (e instanceof Error) {
                        throw e;
                    }
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `ex`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/message/MessageServiceImpl.java`
#### Snippet
```java
                    messagingSrvc.send(node, msg).join();
                } catch (Exception ex) {
                    if (ex instanceof IgniteInternalCheckedException) {
                        throw (IgniteInternalCheckedException) ex;
                    }
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/store/AbstractFilePageStoreIo.java`
#### Snippet
```java
                destBuf.position(bufPos);

                if (e instanceof ClosedByInterruptException) {
                    interrupted = true;

```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/store/AbstractFilePageStoreIo.java`
#### Snippet
```java
                }
            } catch (IOException e) {
                if (e instanceof ClosedChannelException) {
                    try {
                        if (e instanceof ClosedByInterruptException) {
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/store/AbstractFilePageStoreIo.java`
#### Snippet
```java
                if (e instanceof ClosedChannelException) {
                    try {
                        if (e instanceof ClosedByInterruptException) {
                            interrupted = true;

```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/checkpoint/CheckpointWorkflow.java`
#### Snippet
```java
                throw new IgniteInternalCheckedException(
                        "Failed to perform pages array parallel sort",
                        e instanceof ExecutionException ? e.getCause() : e
                );
            }
```

## RuleId[id=ArrayEquality]
### ArrayEquality
Array objects are compared using `==`, not 'Arrays.equals()'
in `modules/core/src/main/java/org/apache/ignite/internal/util/IgniteIntList.java`
#### Snippet
```java
        }

        if (idx == 0 || arr == that.arr) {
            return true;
        }
```

### ArrayEquality
Array objects are compared using `==`, not 'Arrays.equals()'
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/BytesUtil.java`
#### Snippet
```java
            final int offset2, final int length2) {
            // short circuit equal case
            if (buffer1 == buffer2 && offset1 == offset2 && length1 == length2) {
                return 0;
            }
```

### ArrayEquality
Array objects are compared using `!=`, not 'Arrays.equals()'
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/storage/impl/RocksDBLogStorage.java`
#### Snippet
```java
                this.db.put(this.defaultHandle, this.writeOptions, getKeyBytes(logIndex), newValueBytes);
                writeCtx.joinAll();
                if (newValueBytes != valueBytes) {
                    doSync();
                }
```

### ArrayEquality
Array objects are compared using `!=`, not 'Arrays.equals()'
in `modules/raft/src/main/java/org/apache/ignite/internal/raft/storage/impl/RocksDbSharedLogStorage.java`
#### Snippet
```java
                this.db.put(this.dataHandle, this.writeOptions, createKey(logIndex), newValueBytes);
                writeCtx.joinAll();
                if (newValueBytes != valueBytes) {
                    doSync();
                }
```

### ArrayEquality
Array objects are compared using `==`, not 'Arrays.equals()'
in `modules/metastorage/src/main/java/org/apache/ignite/internal/metastorage/server/Value.java`
#### Snippet
```java
     */
    public boolean tombstone() {
        return bytes == TOMBSTONE;
    }
}
```

### ArrayEquality
Array objects are compared using `==`, not 'Arrays.equals()'
in `modules/metastorage/src/main/java/org/apache/ignite/internal/metastorage/server/persistence/RocksStorageUtils.java`
#### Snippet
```java
        LONG_ARRAY_HANDLE.set(bytes, 0, updateCounter);

        bytes[Long.BYTES] = (byte) (value == TOMBSTONE ? 0 : 1);

        System.arraycopy(value, 0, bytes, Long.BYTES + Byte.BYTES, value.length);
```

### ArrayEquality
Array objects are compared using `==`, not 'Arrays.equals()'
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/inmemory/VolatilePageMemory.java`
#### Snippet
```java
     */
    private synchronized Segment addSegment(Segment[] oldRef) {
        if (segments == oldRef) {
            DirectMemoryRegion region = directMemoryProvider.nextRegion();

```

### ArrayEquality
Array objects are compared using `!=`, not 'Arrays.equals()'
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/tree/BplusTree.java`
#### Snippet
```java
                results = null;
            } else {
                if (results != OBJECT_EMPTY_ARRAY) {
                    assert results.length > 0; // Otherwise it makes no sense to create an array.

```

### ArrayEquality
Array objects are compared using `==`, not 'Arrays.equals()'
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/tree/BplusTree.java`
#### Snippet
```java
            }

            if (results == OBJECT_EMPTY_ARRAY) {
                results = (R[]) new Object[cnt0];
            }
```

## RuleId[id=NonFinalFieldOfException]
### NonFinalFieldOfException
Non-final field `issues` of exception class
in `modules/configuration-api/src/main/java/org/apache/ignite/configuration/validation/ConfigurationValidationException.java`
#### Snippet
```java
public class ConfigurationValidationException extends RuntimeException {
    /** List of configuration validation issues. */
    private List<ValidationIssue> issues;

    /**
```

## RuleId[id=FuseStreamOperations]
### FuseStreamOperations
Stream may be extended replacing 'toArray'
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/asm/ConfigurationAsmGenerator.java`
#### Snippet
```java
        List<ParameterizedType> result = Stream.concat(Stream.of(schemaClass), schemaExtensions.stream())
                .map(cls -> typeFromJavaClassName(configurationClassName(cls)))
                .collect(toCollection(ArrayList::new));

        return result.toArray(new ParameterizedType[0]);
```

## RuleId[id=ZeroLengthArrayInitialization]
### ZeroLengthArrayInitialization
Allocation of zero length array
in `modules/cli/src/main/java/org/apache/ignite/internal/cli/sql/table/Table.java`
#### Snippet
```java
    private static String[] parseHeader(List<String> header) {
        if (header.size() > 0) {
            return header.toArray(new String[0]);
        } else {
            return new String[] { "EMPTY" };
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `modules/cli/src/main/java/org/apache/ignite/internal/cli/sql/table/Table.java`
#### Snippet
```java
        List<Object[]> collect = content.stream()
                .map(row -> new ArrayList<>(row.getValues()))
                .map(strings -> strings.toArray(new Object[0]))
                .collect(Collectors.toList());

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `modules/cli/src/main/java/org/apache/ignite/internal/cli/sql/table/Table.java`
#### Snippet
```java
                .collect(Collectors.toList());

        return collect.toArray(new Object[0][0]);
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `modules/cli/src/main/java/org/apache/ignite/internal/cli/core/repl/executor/RegistryCommandExecutor.java`
#### Snippet
```java
        try {
            String[] args = new ArrayList<>(parser.parse(input.getString(), 0, ParseContext.SPLIT_LINE).words())
                    .toArray(new String[0]);
            commandLine.execute(args);
            return DefaultCallOutput.empty();
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `modules/cli/src/main/java/org/apache/ignite/internal/cli/core/repl/completer/DynamicCompleterRegistry.java`
#### Snippet
```java
                return input -> {
                    List<String> candidates = factory.getDynamicCompleter(input).complete(input);
                    return List.of(conf.getFilter().filter(input, candidates.toArray(new String[0])));
                };
            } else {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `modules/cli/src/main/java/org/apache/ignite/internal/cli/core/repl/executor/IgnitePicocliCommands.java`
#### Snippet
```java

            // let picocli generate completion candidates for the token where the cursor is at
            String[] words = commandLine.words().toArray(new String[0]);
            List<CharSequence> cs = new ArrayList<>();
            AutoComplete.complete(cmd.getCommandSpec(),
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `modules/cli/src/main/java/org/apache/ignite/internal/cli/core/repl/executor/IgnitePicocliCommands.java`
#### Snippet
```java
        arguments.add(command);
        Arrays.stream(args).map(Object::toString).forEach(arguments::add);
        cmd.execute(arguments.toArray(new String[0]));
        return null;
    }
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `modules/core/src/main/java/org/apache/ignite/internal/manager/Producer.java`
#### Snippet
```java
        }

        return allOf(futures.toArray(new CompletableFuture[0]));
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/StorageUpdateHandler.java`
#### Snippet
```java
     */
    private void tryRemoveFromIndexes(BinaryRow rowToRemove, RowId rowId, Cursor<ReadResult> previousValues) {
        TableSchemaAwareIndexStorage[] indexes = this.indexes.get().values().toArray(new TableSchemaAwareIndexStorage[0]);

        ByteBuffer[] indexValues = new ByteBuffer[indexes.length];
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/TableManager.java`
#### Snippet
```java
            beforeTablesVvComplete.clear();

            return allOf(futures.toArray(new CompletableFuture[]{}))
                    .orTimeout(TABLES_COMPLETE_TIMEOUT, TimeUnit.SECONDS)
                    .whenComplete((v, e) -> {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `modules/schema/src/main/java/org/apache/ignite/internal/schema/configuration/PrimaryKeyConfigurationSchema.java`
#### Snippet
```java
    @Value(hasDefault = true)
    @Immutable
    public String[] colocationColumns = new String[0];
}

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/replicator/PartitionReplicaListener.java`
#### Snippet
```java
        }

        return allOf(resolutionFuts.toArray(new CompletableFuture[0])).thenCompose(unused -> {
            ArrayList<BinaryRow> rows = new ArrayList<>(count);

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/replicator/PartitionReplicaListener.java`
#### Snippet
```java
            }

            return allOf(resolutionFuts.toArray(new CompletableFuture[0])).thenApply(unused1 -> {
                ArrayList<BinaryRow> result = new ArrayList<>(resolutionFuts.size());

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/replicator/PartitionReplicaListener.java`
#### Snippet
```java
    private static CompletableFuture<Void> allOffFuturesExceptionIgnored(List<CompletableFuture<?>> txFutures,
            TxCleanupReplicaRequest request) {
        return allOf(txFutures.toArray(new CompletableFuture<?>[0]))
                .exceptionally(e -> {
                    assert !request.commit() :
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `modules/network/src/main/java/org/apache/ignite/internal/network/configuration/NodeFinderConfigurationSchema.java`
#### Snippet
```java
    /** Addresses of nodes in the cluster in a host:port format. This is a part of StaticNodeFinder configuration. */
    @Value(hasDefault = true)
    public final String[] netClusterNodes = new String[0];
}

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/ExecutionServiceImpl.java`
#### Snippet
```java

                            cancelFuts.add(
                                    CompletableFuture.allOf(entry.getValue().toArray(new CompletableFuture[0]))
                                            .handle((none2, t) -> {
                                                try {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/ExecutionServiceImpl.java`
#### Snippet
```java
                        }

                        var compoundCancelFut = CompletableFuture.allOf(cancelFuts.toArray(new CompletableFuture[0]));
                        var finalStepFut = compoundCancelFut.thenRun(() -> {
                            queryManagerMap.remove(ctx.queryId());
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `modules/metastorage/src/main/java/org/apache/ignite/internal/metastorage/server/persistence/RocksDbKeyValueStorage.java`
#### Snippet
```java

        try {
            Entry[] entries = getAll(Arrays.asList(condition.keys())).toArray(new Entry[]{});

            boolean branch = condition.test(entries);
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `modules/metastorage/src/main/java/org/apache/ignite/internal/metastorage/server/persistence/RocksDbKeyValueStorage.java`
#### Snippet
```java
                }

                Entry[] entries = getAll(Arrays.asList(currIf.cond().keys())).toArray(new Entry[]{});

                Statement branch = (currIf.cond().test(entries)) ? currIf.andThen() : currIf.orElse();
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `modules/metastorage/src/testFixtures/java/org/apache/ignite/internal/metastorage/server/SimpleInMemoryKeyValueStorage.java`
#### Snippet
```java
            Collection<Entry> e = getAll(Arrays.asList(condition.keys()));

            boolean branch = condition.test(e.toArray(new Entry[]{}));

            Collection<Operation> ops = branch ? success : failure;
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `modules/metastorage/src/testFixtures/java/org/apache/ignite/internal/metastorage/server/SimpleInMemoryKeyValueStorage.java`
#### Snippet
```java
                Collection<Entry> e = getAll(Arrays.asList(currIf.cond().keys()));

                Statement branch = (currIf.cond().test(e.toArray(new Entry[]{}))) ? currIf.andThen() : currIf.orElse();

                if (branch.isTerminal()) {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/store/DeltaFilePageStoreIoHeader.java`
#### Snippet
```java

        if (arrayLen == 0) {
            return new DeltaFilePageStoreIoHeader(version, index, pageSize, new int[0]);
        }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `modules/transactions/src/main/java/org/apache/ignite/internal/tx/impl/ReadWriteTransactionImpl.java`
#### Snippet
```java
        // TODO: https://issues.apache.org/jira/browse/IGNITE-17688 Add proper exception handling.
        CompletableFuture<Void> mainFinishFut = CompletableFuture
                .allOf(enlistedResults.toArray(new CompletableFuture[0]))
                .thenCompose(
                        ignored -> {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/AbstractMvTableStorageTest.java`
#### Snippet
```java
        assertThrows(
                StorageRebalanceException.class,
                () -> tableStorage.finishRebalancePartition(PARTITION_ID, 100, 500, new byte[0])
        );

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/AbstractMvTableStorageTest.java`
#### Snippet
```java
        assertThrows(
                IllegalArgumentException.class,
                () -> tableStorage.finishRebalancePartition(getPartitionIdOutOfRange(), 100, 500, new byte[0])
        );

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/AbstractMvTableStorageTest.java`
#### Snippet
```java
        assertThrows(
                StorageRebalanceException.class,
                () -> tableStorage.finishRebalancePartition(1, 100, 500, new byte[0])
        );

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/AbstractMvTableStorageTest.java`
#### Snippet
```java
        storage.runConsistently(() -> {
            assertThrows(StorageRebalanceException.class, () -> storage.lastApplied(100, 500));
            assertThrows(StorageRebalanceException.class, () -> storage.committedGroupConfiguration(new byte[0]));

            assertThrows(
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/AbstractMvTableStorageTest.java`
#### Snippet
```java
            assertThrows(
                    StorageRebalanceException.class,
                    () -> storage.committedGroupConfiguration(new byte[0])
            );

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `modules/transactions/src/testFixtures/java/org/apache/ignite/internal/tx/storage/state/AbstractTxStateStorageTest.java`
#### Snippet
```java
            assertThat(
                    scan.stream().collect(toList()),
                    containsInAnyOrder(expRows.toArray(new IgniteBiTuple[0]))
            );
        }
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/asm/ConfigurationAsmGenerator.java`
#### Snippet
```java
                .collect(toCollection(ArrayList::new));

        return result.toArray(new ParameterizedType[0]);
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `modules/client-handler/src/main/java/org/apache/ignite/client/handler/JdbcQueryEventHandlerImpl.java`
#### Snippet
```java
        }

        return CompletableFuture.allOf(results.toArray(new CompletableFuture[0])).thenApply(none -> {
            var actualResults = results.stream().map(CompletableFuture::join).collect(Collectors.toList());

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `modules/network-annotation-processor/src/main/java/org/apache/ignite/internal/network/processor/IncrementalCompilationConfig.java`
#### Snippet
```java
        String firstSimpleName = split[1];

        String[] simpleNames = split.length > 2 ? Arrays.copyOfRange(split, 2, split.length) : new String[0];

        return ClassName.get(packageName, firstSimpleName, simpleNames);
```

## RuleId[id=NullArgumentToVariableArgMethod]
### NullArgumentToVariableArgMethod
Confusing argument `null`, unclear if a varargs or non-varargs call is desired
in `modules/core/src/main/java/org/apache/ignite/internal/util/ExceptionUtils.java`
#### Snippet
```java

        try {
            mtd = throwable.getClass().getMethod(mtdName, null);
        } catch (NoSuchMethodException | SecurityException ignored) {
            // exception ignored
```

### NullArgumentToVariableArgMethod
Confusing argument `null`, unclear if a varargs or non-varargs call is desired
in `modules/core/src/main/java/org/apache/ignite/internal/util/ExceptionUtils.java`
#### Snippet
```java

        try {
            causeMtd = Throwable.class.getMethod("getCause", null);
        } catch (Exception ignored) {
            causeMtd = null;
```

### NullArgumentToVariableArgMethod
Confusing argument `null`, unclear if a varargs or non-varargs call is desired
in `modules/core/src/main/java/org/apache/ignite/internal/util/ExceptionUtils.java`
#### Snippet
```java
        for (String methodName : CAUSE_METHOD_NAMES) {
            try {
                Method mtd = cls.getMethod(methodName, null);

                if (mtd != null && Throwable.class.isAssignableFrom(mtd.getReturnType())) {
```

## RuleId[id=TypeParameterExtendsObject]
### TypeParameterExtendsObject
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `modules/bytecode/src/main/java/com/facebook/presto/bytecode/expression/BytecodeExpressions.java`
#### Snippet
```java
    public static BytecodeExpression invokeDynamic(
        Method bootstrapMethod,
        Collection<? extends Object> bootstrapArgs,
        String methodName,
        ParameterizedType returnType,
```

### TypeParameterExtendsObject
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `modules/bytecode/src/main/java/com/facebook/presto/bytecode/expression/BytecodeExpressions.java`
#### Snippet
```java
    public static BytecodeExpression invokeDynamic(
        Method bootstrapMethod,
        Collection<? extends Object> bootstrapArgs,
        String methodName,
        Class<?> returnType,
```

### TypeParameterExtendsObject
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `modules/bytecode/src/main/java/com/facebook/presto/bytecode/expression/BytecodeExpressions.java`
#### Snippet
```java
    public static BytecodeExpression invokeDynamic(
        Method bootstrapMethod,
        Collection<? extends Object> bootstrapArgs,
        String methodName,
        MethodType methodType,
```

### TypeParameterExtendsObject
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `modules/bytecode/src/main/java/com/facebook/presto/bytecode/expression/BytecodeExpressions.java`
#### Snippet
```java
    public static BytecodeExpression invokeDynamic(
        Method bootstrapMethod,
        Collection<? extends Object> bootstrapArgs,
        String methodName,
        MethodType methodType,
```

### TypeParameterExtendsObject
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `modules/bytecode/src/main/java/com/facebook/presto/bytecode/expression/BytecodeExpressions.java`
#### Snippet
```java
    public static BytecodeExpression invokeDynamic(
        Method bootstrapMethod,
        Collection<? extends Object> bootstrapArgs,
        String methodName,
        Class<?> returnType,
```

### TypeParameterExtendsObject
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `modules/bytecode/src/main/java/com/facebook/presto/bytecode/expression/BytecodeExpressions.java`
#### Snippet
```java
    public static BytecodeExpression invokeDynamic(
        Method bootstrapMethod,
        Collection<? extends Object> bootstrapArgs,
        String methodName,
        ParameterizedType returnType,
```

### TypeParameterExtendsObject
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `modules/bytecode/src/main/java/com/facebook/presto/bytecode/expression/BytecodeExpressions.java`
#### Snippet
```java
    public static BytecodeExpression invokeDynamic(
        Method bootstrapMethod,
        Collection<? extends Object> bootstrapArgs,
        String methodName,
        ParameterizedType returnType,
```

## RuleId[id=NonExceptionNameEndsWithException]
### NonExceptionNameEndsWithException
Non-exception class name `RaftException` ends with 'Exception'
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/error/RaftException.java`
#### Snippet
```java
 * Internal RAFT exception.
 */
public class RaftException extends Throwable {
    private static final long serialVersionUID = -1533343555230409704L;

```

## RuleId[id=OptionalGetWithoutIsPresent]
### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `modules/bytecode/src/main/java/com/facebook/presto/bytecode/ClassGenerator.java`
#### Snippet
```java
        Map<String, Class<?>> classes = defineClasses(List.of(classDefinition));

        return classes.values().stream().findFirst().get().asSubclass(superType);
    }

```

## RuleId[id=ConstantValue]
### ConstantValue
Condition `cmgNodeNames == null` is always `false`
in `modules/api/src/main/java/org/apache/ignite/InitParametersBuilder.java`
#### Snippet
```java
            throw new IllegalStateException("Meta storage node names is not set.");
        }
        if (cmgNodeNames == null) {
            throw new IllegalStateException("CMG node names is not set.");
        }
```

### ConstantValue
Condition `targetType.getDeclaredField(fieldName) == null` is always `false` when reached
in `modules/api/src/main/java/org/apache/ignite/table/mapper/MapperBuilder.java`
#### Snippet
```java
    private String requireValidField(String fieldName) {
        try {
            if (fieldName == null || targetType.getDeclaredField(fieldName) == null) {
                throw new IllegalArgumentException("Mapping for a column already exists: " + fieldName);
            }
```

### ConstantValue
Value `fieldName` is always 'null'
in `modules/api/src/main/java/org/apache/ignite/table/mapper/MapperBuilder.java`
#### Snippet
```java
        try {
            if (fieldName == null || targetType.getDeclaredField(fieldName) == null) {
                throw new IllegalArgumentException("Mapping for a column already exists: " + fieldName);
            }
        } catch (NoSuchFieldException e) {
```

### ConstantValue
Condition `idx != 0` is always `true`
in `modules/core/src/main/java/org/apache/ignite/internal/util/IgniteIntList.java`
#### Snippet
```java
        }

        if (!last && idx != 0 && size != 0) {
            System.arraycopy(arr, idx - size, arr, 0, size);
        }
```

### ConstantValue
Condition `mtd != null` is always `true`
in `modules/core/src/main/java/org/apache/ignite/internal/util/ExceptionUtils.java`
#### Snippet
```java
                Method mtd = cls.getMethod(methodName, null);

                if (mtd != null && Throwable.class.isAssignableFrom(mtd.getReturnType())) {
                    return true;
                }
```

### ConstantValue
Condition `field != null` is always `true`
in `modules/core/src/main/java/org/apache/ignite/internal/util/ExceptionUtils.java`
#### Snippet
```java
            Field field = cls.getField("detail");

            if (field != null) {
                return true;
            }
```

### ConstantValue
Condition `i < len` is always `false`
in `modules/core/src/main/java/org/apache/ignite/internal/util/GridUnsafe.java`
#### Snippet
```java
    public static void copyOffheapOffheap(long srcAddr, long dstAddr, long len) {
        if (len <= PER_BYTE_THRESHOLD) {
            for (int i = 0; i < len; i++) {
                UNSAFE.putByte(dstAddr + i, UNSAFE.getByte(srcAddr + i));
            }
```

### ConstantValue
Condition `i < len` is always `false`
in `modules/core/src/main/java/org/apache/ignite/internal/util/GridUnsafe.java`
#### Snippet
```java
    public static void copyOffheapHeap(long srcAddr, Object dstBase, long dstOff, long len) {
        if (len <= PER_BYTE_THRESHOLD) {
            for (int i = 0; i < len; i++) {
                UNSAFE.putByte(dstBase, dstOff + i, UNSAFE.getByte(srcAddr + i));
            }
```

### ConstantValue
Condition `directBufCtor == null` is always `false`
in `modules/core/src/main/java/org/apache/ignite/internal/util/GridUnsafe.java`
#### Snippet
```java
                }

                if (directBufCtor == null) {
                    throw e;
                }
```

### ConstantValue
Condition `nioAccessObj == null` is always `false`
in `modules/core/src/main/java/org/apache/ignite/internal/util/GridUnsafe.java`
#### Snippet
```java
                }

                if (nioAccessObj == null || directBufMtd == null) {
                    throw e;
                }
```

### ConstantValue
Condition `i < len` is always `false`
in `modules/core/src/main/java/org/apache/ignite/internal/util/GridUnsafe.java`
#### Snippet
```java
    public static void copyHeapOffheap(Object srcBase, long srcOff, long dstAddr, long len) {
        if (len <= PER_BYTE_THRESHOLD) {
            for (int i = 0; i < len; i++) {
                UNSAFE.putByte(dstAddr + i, UNSAFE.getByte(srcBase, srcOff + i));
            }
```

### ConstantValue
Condition `i < len` is always `false`
in `modules/core/src/main/java/org/apache/ignite/internal/util/GridUnsafe.java`
#### Snippet
```java
    public static void copyMemory(Object srcBase, long srcOff, Object dstBase, long dstOff, long len) {
        if (len <= PER_BYTE_THRESHOLD && srcBase != null && dstBase != null) {
            for (int i = 0; i < len; i++) {
                UNSAFE.putByte(dstBase, dstOff + i, UNSAFE.getByte(srcBase, srcOff + i));
            }
```

### ConstantValue
Condition `causalityToken <= actualToken0` is always `false`
in `modules/core/src/main/java/org/apache/ignite/internal/causality/VersionedValue.java`
#### Snippet
```java

        try {
            if (causalityToken <= actualToken0) {
                return getValueForPreviousToken(causalityToken);
            }
```

### ConstantValue
Condition `eqSymPos == token.length()` is always `false`
in `modules/jdbc/src/main/java/org/apache/ignite/internal/jdbc/ConnectionPropertiesImpl.java`
#### Snippet
```java
                }

                if (eqSymPos == token.length()) {
                    throw new SQLException("Invalid parameter format (key and value cannot be empty): " + token);
                }
```

### ConstantValue
Value `res` is always 'null'
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/rpc/impl/IgniteRpcClient.java`
#### Snippet
```java
        fut.orTimeout(timeoutMs, TimeUnit.MILLISECONDS).
            whenComplete((res, err) -> {
                assert !(res == null && err == null) : res + " " + err;

                if (err == null && recordPred != null && recordPred.test(res, this.toString()))
```

### ConstantValue
Value `err` is always 'null'
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/rpc/impl/IgniteRpcClient.java`
#### Snippet
```java
        fut.orTimeout(timeoutMs, TimeUnit.MILLISECONDS).
            whenComplete((res, err) -> {
                assert !(res == null && err == null) : res + " " + err;

                if (err == null && recordPred != null && recordPred.test(res, this.toString()))
```

### ConstantValue
Condition `lastMatch` is always `true` when reached
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/StringUtils.java`
#### Snippet
```java
                    }

                    if (match || preserveAllTokens && lastMatch) {
                        list.add(str.substring(start, i));
                    }
```

### ConstantValue
Condition `tmp != null` is always `true`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/storage/snapshot/SnapshotExecutorImpl.java`
#### Snippet
```java
        if (snapshotStorage instanceof LocalSnapshotStorage) {
            final LocalSnapshotStorage tmp = (LocalSnapshotStorage) this.snapshotStorage;
            if (tmp != null && !tmp.hasServerPeerId()) {
                tmp.setServerPeerId(opts.getPeerId());
            }
```

### ConstantValue
Condition `cd != null` is always `true`
in `modules/core/src/main/java/org/apache/ignite/internal/tostring/IgniteToStringBuilder.java`
#### Snippet
```java
            ClassDescriptor cd = getClassDescriptor(cls);

            assert cd != null;

            buf.app(cd.getSimpleClassName());
```

### ConstantValue
Condition `clearId != null` is always `true`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/storage/impl/LogManagerImpl.java`
#### Snippet
```java
            this.writeLock.unlock();
        }
        if (clearId != null) {
            clearMemoryLogs(clearId);
        }
```

### ConstantValue
Condition `clearId != null` is always `true`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/storage/impl/LogManagerImpl.java`
#### Snippet
```java
            this.writeLock.unlock();
        }
        if (clearId != null) {
            clearMemoryLogs(clearId);
        }
```

### ConstantValue
Value `sslEnabled` is always 'true'
in `modules/rest/src/main/java/org/apache/ignite/internal/rest/RestComponent.java`
#### Snippet
```java
                    "micronaut.server.dual-protocol", dualProtocol,
                    "micronaut.server.ssl.port", sslPort,
                    "micronaut.server.ssl.enabled", sslEnabled,
                    "micronaut.server.ssl.key-store.path", "file:" + keyStore.path(),
                    "micronaut.server.ssl.key-store.password", keyStore.password(),
```

### ConstantValue
Value `columnName` is always 'null'
in `modules/index/src/main/java/org/apache/ignite/internal/index/IndexManager.java`
#### Snippet
```java

        for (var columnName : indexView.columns().namedListKeys()) {
            IndexColumnView columnView = indexView.columns().get(columnName);

            //TODO IGNITE-15141: Make null-order configurable.
```

### ConstantValue
Value `columnName` is always 'null'
in `modules/index/src/main/java/org/apache/ignite/internal/index/IndexManager.java`
#### Snippet
```java
            boolean nullsFirst = !columnView.asc();

            indexedColumns.add(columnName);
            collations.add(ColumnCollation.get(columnView.asc(), nullsFirst));
        }
```

### ConstantValue
Value `indexView` is always 'null'
in `modules/index/src/main/java/org/apache/ignite/internal/index/IndexManager.java`
#### Snippet
```java
                    convert((SortedIndexView) indexView)
            );
        } else if (indexView instanceof HashIndexView) {
            return new HashIndex(
                    indexView.id(),
```

### ConstantValue
Condition `indexView != null` is always `false`
in `modules/index/src/main/java/org/apache/ignite/internal/index/IndexManager.java`
#### Snippet
```java
        }

        throw new AssertionError("Unknown index type [type=" + (indexView != null ? indexView.getClass() : null) + ']');
    }

```

### ConstantValue
Condition `schema != null` is always `true`
in `modules/table/src/testFixtures/java/org/apache/ignite/internal/table/impl/DummySchemaManagerImpl.java`
#### Snippet
```java
     */
    public DummySchemaManagerImpl(@NotNull SchemaDescriptor schema) {
        assert schema != null;

        this.schema = schema;
```

### ConstantValue
Condition is always true
in `modules/table/src/testFixtures/java/org/apache/ignite/internal/table/TxAbstractTest.java`
#### Snippet
```java
            fut.join();

            if (true) {
                throw new IllegalArgumentException();
            }
```

### ConstantValue
Condition is always true
in `modules/table/src/testFixtures/java/org/apache/ignite/internal/table/TxAbstractTest.java`
#### Snippet
```java
        assertThrows(IgniteException.class, () -> igniteTransactions.runInTransaction(tx -> {
            accounts.recordView().upsertAll(tx, List.of(makeValue(3, 300.), makeValue(4, 400.)));
            if (true) {
                throw new IgniteException();
            }
```

### ConstantValue
Value `defaultValueView` is always 'null'
in `modules/schema/src/main/java/org/apache/ignite/internal/schema/configuration/ConfigurationToSchemaDescriptorConverter.java`
#### Snippet
```java
        if (defaultValueView instanceof NullValueDefaultView) {
            defaultValueProvider = DefaultValueProvider.constantProvider(null);
        } else if (defaultValueView instanceof FunctionCallDefaultView) {
            defaultValueProvider = DefaultValueProvider.forValueGenerator(
                    DefaultValueGenerator.valueOf(
```

### ConstantValue
Value `defaultValueView` is always 'null'
in `modules/schema/src/main/java/org/apache/ignite/internal/schema/configuration/ConfigurationToSchemaDescriptorConverter.java`
#### Snippet
```java
                    )
            );
        } else if (defaultValueView instanceof ConstantValueDefaultView) {
            defaultValueProvider = DefaultValueProvider.constantProvider(
                    fromString(((ConstantValueDefaultView) defaultValueView).defaultValue(), type)
```

### ConstantValue
Value `indexView` is always 'null'
in `modules/schema/src/main/java/org/apache/ignite/internal/schema/configuration/index/IndexValidatorImpl.java`
#### Snippet
```java
            // we need modifiable list
            indexedColumns = new ArrayList<>(Arrays.asList(index0.columnNames()));
        } else if (indexView instanceof SortedIndexView) {
            var index0 = (SortedIndexView) indexView;

```

### ConstantValue
Value `defaultValueProvider` is always 'null'
in `modules/schema/src/testFixtures/java/org/apache/ignite/internal/schema/testutils/SchemaConfigurationConverter.java`
#### Snippet
```java
        var defaultValueProvider = colView.defaultValueProvider();

        if (defaultValueProvider instanceof NullValueDefaultView) {
            valueSupplier = DefaultValueDefinition.nullValue();
        } else if (defaultValueProvider instanceof FunctionCallDefaultView) {
```

### ConstantValue
Value `defaultValueProvider` is always 'null'
in `modules/schema/src/testFixtures/java/org/apache/ignite/internal/schema/testutils/SchemaConfigurationConverter.java`
#### Snippet
```java
        if (defaultValueProvider instanceof NullValueDefaultView) {
            valueSupplier = DefaultValueDefinition.nullValue();
        } else if (defaultValueProvider instanceof FunctionCallDefaultView) {
            valueSupplier = DefaultValueDefinition.functionCall(((FunctionCallDefaultView) defaultValueProvider).functionName());
        } else if (defaultValueProvider instanceof ConstantValueDefaultView) {
```

### ConstantValue
Value `defaultValueProvider` is always 'null'
in `modules/schema/src/testFixtures/java/org/apache/ignite/internal/schema/testutils/SchemaConfigurationConverter.java`
#### Snippet
```java
        } else if (defaultValueProvider instanceof FunctionCallDefaultView) {
            valueSupplier = DefaultValueDefinition.functionCall(((FunctionCallDefaultView) defaultValueProvider).functionName());
        } else if (defaultValueProvider instanceof ConstantValueDefaultView) {
            valueSupplier = DefaultValueDefinition.constant(
                    convertDefaultFromConfiguration(((ConstantValueDefaultView) defaultValueProvider).defaultValue(), type)
```

### ConstantValue
Value `e` is always 'null'
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/storage/InternalTableImpl.java`
#### Snippet
```java
            } else {
                if (res == null || res.leader() == null) {
                    throw withCause(TransactionException::new, REPLICA_UNAVAILABLE_ERR, e);
                } else {
                    return clusterNodeResolver.apply(res.leader().consistentId());
```

### ConstantValue
Condition `exclNeighbors` is always `false`
in `modules/affinity/src/main/java/org/apache/ignite/internal/affinity/RendezvousAffinityFunction.java`
#### Snippet
```java
                    res.add(node);

                    if (exclNeighbors) {
                        allNeighbors.addAll(neighborhoodCache.get(node));
                    }
```

### ConstantValue
Condition `hashDistribution == null || !haveSingle` is always `true`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rel/set/IgniteColocatedSetOp.java`
#### Snippet
```java
        }

        assert hashDistribution == null || !haveSingle;

        if (haveSingle) {
```

### ConstantValue
Condition `!haveSingle` is always `true` when reached
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rel/set/IgniteColocatedSetOp.java`
#### Snippet
```java
        }

        assert hashDistribution == null || !haveSingle;

        if (haveSingle) {
```

### ConstantValue
Value `haveSingle` is always 'false'
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rel/set/IgniteColocatedSetOp.java`
#### Snippet
```java
        }

        assert hashDistribution == null || !haveSingle;

        if (haveSingle) {
```

### ConstantValue
Value `requiredColumns` is always 'null'
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rel/logical/IgniteLogicalIndexScan.java`
#### Snippet
```java
                        : null;
            } else {
                searchBounds = buildHashIndexConditions(cluster, tbl, collation, cond, requiredColumns);
            }
        } else if (index.type() == Type.SORTED) {
```

### ConstantValue
Condition `exceptions == null` is always `false`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/ReflectiveCallNotNullImplementor.java`
#### Snippet
```java
    private boolean containsCheckedException(Method method) {
        Class[] exceptions = method.getExceptionTypes();
        if (exceptions == null || exceptions.length == 0) {
            return false;
        }
```

### ConstantValue
Condition `inBuf.isEmpty()` is always `true`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/Outbox.java`
#### Snippet
```java
        }

        assert inBuf.isEmpty();

        if (waiting == 0) {
```

### ConstantValue
Condition `requested > 0` is always `true`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/CorrelatedNestedLoopJoinNode.java`
#### Snippet
```java
                state = State.END;

                if (requested > 0) {
                    downstream().end();
                }
```

### ConstantValue
Condition `right == null` is always `true` when reached
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/MergeJoinNode.java`
#### Snippet
```java
                            }

                            if (left != null && right == null) {
                                if (!leftMatched) {
                                    row = handler.concat(left, rightRowFactory.create());
```

### ConstantValue
Condition `op instanceof SqlUserDefinedTableMacro` is always `false`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/RexImpTable.java`
#### Snippet
```java
                    || (returnType == callValue.getType())
                    || (op instanceof SqlTableFunction)
                    || (op instanceof SqlUserDefinedTableMacro)
                    || (op instanceof SqlUserDefinedTableFunction);
            final Expression convertedCallValue =
```

### ConstantValue
Condition `op instanceof SqlUserDefinedTableFunction` is always `false`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/RexImpTable.java`
#### Snippet
```java
                    || (op instanceof SqlTableFunction)
                    || (op instanceof SqlUserDefinedTableMacro)
                    || (op instanceof SqlUserDefinedTableFunction);
            final Expression convertedCallValue =
                    noConvert
```

### ConstantValue
Condition `ex instanceof IgniteInternalCheckedException` is always `false`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/message/MessageServiceImpl.java`
#### Snippet
```java
                    messagingSrvc.send(node, msg).join();
                } catch (Exception ex) {
                    if (ex instanceof IgniteInternalCheckedException) {
                        throw (IgniteInternalCheckedException) ex;
                    }
```

### ConstantValue
Condition `row != null` is always `true`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/io/AbstractDataPageIo.java`
#### Snippet
```java
        int itemId = addItem(pageAddr, fullEntrySize, directCnt, indirectCnt, dataOff, pageSize);

        if (row != null) {
            setLinkByPageId(row, pageId, itemId);
        }
```

### ConstantValue
Condition `bucket <= Short.MAX_VALUE` is always `true` when reached
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/freelist/io/PagesListMetaIo.java`
#### Snippet
```java
        for (int i = 0; i < cnt; i++) {
            int bucket = getShort(pageAddr, off);
            assert bucket >= 0 && bucket <= Short.MAX_VALUE : bucket;

            long tailId = PageUtils.getLong(pageAddr, off + 2);
```

### ConstantValue
Condition `pagesCntr != null` is always `true` when reached
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/PagePool.java`
#### Snippet
```java
        }

        if (relPtr != INVALID_REL_PTR && pagesCntr != null) {
            pagesCntr.incrementAndGet();
        }
```

### ConstantValue
Condition `pagesCntr != null` is always `true`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/PagePool.java`
#### Snippet
```java
        int resCntr = 0;

        if (pagesCntr != null) {
            resCntr = pagesCntr.decrementAndGet();
        }
```

### ConstantValue
Result of `res << SEG_BITS` is always '0'
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/inmemory/VolatilePageMemory.java`
#### Snippet
```java
        long res = 0;

        res = (res << SEG_BITS) | (segIdx & SEG_MASK);
        res = (res << IDX_BITS) | (pageIdx & IDX_MASK);

```

### ConstantValue
Condition `dumpBucketsInfo` is always `false`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/freelist/AbstractFreeList.java`
#### Snippet
```java
            }

            if (dumpBucketsInfo) {
                Stripe[] stripes = getBucket(b);

```

### ConstantValue
Condition `bucket >= 0` is always `true`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/freelist/AbstractFreeList.java`
#### Snippet
```java
        int bucket = freeSpace >>> shift;

        assert bucket >= 0 && bucket < BUCKETS : bucket;

        if (!allowReuse && isReuseBucket(bucket)) {
```

### ConstantValue
Value `deltaFileHeader` is always 'null'
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/store/FilePageStoreFactory.java`
#### Snippet
```java
                    DeltaFilePageStoreIoHeader deltaFileHeader = DeltaFilePageStoreIoHeader.readHeader(deltaFileIo, headerBuffer.rewind());

                    assert deltaFileHeader != null : deltaFileHeader;

                    deltaFileIos[i] = createDeltaFilePageStoreIo(deltaFilePath, deltaFileHeader);
```

### ConstantValue
Condition `fileIo != null` is always `true`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/store/AbstractFilePageStoreIo.java`
#### Snippet
```java
                this.filePath = newFilePath;

                if (fileIo != null) {
                    reinit(fileIo);
                }
```

### ConstantValue
Condition `!dirty` is always `true`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/replacement/RandomLruPageReplacementPolicy.java`
#### Snippet
```java
                final boolean storMeta = isStoreMetadataPage(absPageAddr);

                if (pageTs < cleanTs && !dirty && !storMeta) {
                    cleanAddr = rndAddr;

```

### ConstantValue
Value `dirty` is always 'false'
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/replacement/RandomLruPageReplacementPolicy.java`
#### Snippet
```java
                final boolean storMeta = isStoreMetadataPage(absPageAddr);

                if (pageTs < cleanTs && !dirty && !storMeta) {
                    cleanAddr = rndAddr;

```

### ConstantValue
Condition `pageTs < dirtyTs && dirty && !storMeta` is always `false`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/replacement/RandomLruPageReplacementPolicy.java`
#### Snippet
```java

                    cleanTs = pageTs;
                } else if (pageTs < dirtyTs && dirty && !storMeta) {
                    dirtyAddr = rndAddr;

```

### ConstantValue
Condition `dirty` is always `false`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/replacement/RandomLruPageReplacementPolicy.java`
#### Snippet
```java

                    cleanTs = pageTs;
                } else if (pageTs < dirtyTs && dirty && !storMeta) {
                    dirtyAddr = rndAddr;

```

### ConstantValue
Condition `dirtyAddr != INVALID_REL_PTR` is always `false`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/replacement/RandomLruPageReplacementPolicy.java`
#### Snippet
```java
                if (cleanAddr != INVALID_REL_PTR) {
                    relRmvAddr = cleanAddr;
                } else if (dirtyAddr != INVALID_REL_PTR) {
                    relRmvAddr = dirtyAddr;
                } else {
```

### ConstantValue
Condition `!(indexConfig instanceof HashIndexView)` is always `false`
in `modules/storage-api/src/main/java/org/apache/ignite/internal/storage/index/HashIndexDescriptor.java`
#### Snippet
```java
        }

        if (!(indexConfig instanceof HashIndexView)) {
            throw new StorageException(String.format(
                    "Index \"%s\" is not configured as a Hash Index. Actual type: %s",
```

### ConstantValue
Condition `!(indexConfig instanceof SortedIndexView)` is always `false`
in `modules/storage-api/src/main/java/org/apache/ignite/internal/storage/index/SortedIndexDescriptor.java`
#### Snippet
```java
        }

        if (!(indexConfig instanceof SortedIndexView)) {
            throw new StorageException(String.format(
                    "Index \"%s\" is not configured as a Sorted Index. Actual type: %s",
```

### ConstantValue
Value `indexCfgView` is always 'null'
in `modules/storage-page-memory/src/main/java/org/apache/ignite/internal/storage/pagememory/mv/AbstractPageMemoryMvPartitionStorage.java`
#### Snippet
```java
                    if (indexCfgView instanceof HashIndexView) {
                        hashIndexes.put(indexCfgView.id(), createOrRestoreHashIndex(indexMeta));
                    } else if (indexCfgView instanceof SortedIndexView) {
                        sortedIndexes.put(indexCfgView.id(), createOrRestoreSortedIndex(indexMeta));
                    } else {
```

### ConstantValue
Condition `indexCfgView == null` is always `true`
in `modules/storage-page-memory/src/main/java/org/apache/ignite/internal/storage/pagememory/mv/AbstractPageMemoryMvPartitionStorage.java`
#### Snippet
```java
                        sortedIndexes.put(indexCfgView.id(), createOrRestoreSortedIndex(indexMeta));
                    } else {
                        assert indexCfgView == null;

                        //TODO: IGNITE-17626 Drop the index synchronously.
```

### ConstantValue
Condition `newOwner instanceof PersistentPageMemoryDataStorageView` is always `false`
in `modules/storage-page-memory/src/main/java/org/apache/ignite/internal/storage/pagememory/configuration/PageMemoryDataRegionValidatorImpl.java`
#### Snippet
```java
                );
            }
        } else if (newOwner instanceof PersistentPageMemoryDataStorageView) {
            PersistentPageMemoryStorageEngineView engineConfig = ctx.getNewRoot(PersistentPageMemoryStorageEngineConfiguration.KEY);

```

### ConstantValue
Condition `idx >= 0` is always `true`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/tree/BplusTree.java`
#### Snippet
```java
            }

            assert idx >= 0 && idx < cnt : idx;

            // Need to do inner replace when we remove the rightmost element and the leaf has a forward page,
```

### ConstantValue
Condition `res == RETRY` is always `true`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/tree/BplusTree.java`
#### Snippet
```java

            for (; ; ) {
                if (res == RETRY) {
                    x.checkLockRetry();
                }
```

## RuleId[id=StringConcatenationInsideStringBufferAppend]
### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `modules/core/src/main/java/org/apache/ignite/lang/IgniteStringFormatter.java`
#### Snippet
```java
            sbuf.append(s);
        } catch (Throwable t) {
            sbuf.append("Failed toString() invocation on an object of type [cls=" + o.getClass().getName()
                    + ", errMsg=" + t.getClass().getName()
                    + ", errMsg=" + t.getMessage() + ']');
```

## RuleId[id=IOResource]
### IOResource
'ObjectInputStream' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/JDKMarshaller.java`
#### Snippet
```java
        try {
            ByteArrayInputStream bais = new ByteArrayInputStream(raw);
            ObjectInputStream oos = new ObjectInputStream(bais);
            return (T) oos.readObject();
        }
```

### IOResource
'PrintWriter' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `modules/client-handler/src/main/java/org/apache/ignite/client/handler/JdbcQueryEventHandlerImpl.java`
#### Snippet
```java
        String message = ExceptionUtils.unwrapCause(t).getMessage();
        StringWriter sw = new StringWriter();
        PrintWriter pw = new PrintWriter(sw);

        pw.print(message);
```

## RuleId[id=Java8MapForEach]
### Java8MapForEach
Can be replaced with 'Map.forEach()'
in `modules/transactions/src/main/java/org/apache/ignite/internal/tx/impl/HeapLockManager.java`
#### Snippet
```java
            intendedLocks.addAll(other.intendedLocks);

            other.locks.entrySet().forEach(entry -> addLock(entry.getKey(), entry.getValue()));

            recalculate();
```

## RuleId[id=RedundantLengthCheck]
### RedundantLengthCheck
Redundant array length check
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/ReflectiveCallNotNullImplementor.java`
#### Snippet
```java
    private boolean containsCheckedException(Method method) {
        Class[] exceptions = method.getExceptionTypes();
        if (exceptions == null || exceptions.length == 0) {
            return false;
        }
```

## RuleId[id=FieldMayBeStatic]
### FieldMayBeStatic
Field `dualProtocol` may be 'static'
in `modules/rest/src/main/java/org/apache/ignite/internal/rest/configuration/RestConfigurationSchema.java`
#### Snippet
```java
    /** The dual protocol (http/https) configuration. */
    @Value(hasDefault = true)
    public final boolean dualProtocol = false;

    /** HTTP to HTTPS redirection. */
```

### FieldMayBeStatic
Field `httpToHttpsRedirection` may be 'static'
in `modules/rest/src/main/java/org/apache/ignite/internal/rest/configuration/RestConfigurationSchema.java`
#### Snippet
```java
    /** HTTP to HTTPS redirection. */
    @Value(hasDefault = true)
    public final boolean httpToHttpsRedirection = false;

    /** SSL configuration. */
```

### FieldMayBeStatic
Field `port` may be 'static'
in `modules/rest/src/main/java/org/apache/ignite/internal/rest/configuration/RestConfigurationSchema.java`
#### Snippet
```java
    @Range(min = 1024, max = 0xFFFF)
    @Value(hasDefault = true)
    public final int port = 10300;

    /** TCP port range. */
```

### FieldMayBeStatic
Field `portRange` may be 'static'
in `modules/rest/src/main/java/org/apache/ignite/internal/rest/configuration/RestConfigurationSchema.java`
#### Snippet
```java
    @Range(min = 0)
    @Value(hasDefault = true)
    public final int portRange = 100;

    /** The dual protocol (http/https) configuration. */
```

### FieldMayBeStatic
Field `port` may be 'static'
in `modules/rest/src/main/java/org/apache/ignite/internal/rest/configuration/RestSslConfigurationSchema.java`
#### Snippet
```java
    @Range(min = 1024, max = 0xFFFF)
    @Value(hasDefault = true)
    public final int port = 10400;

    /** SSL port range. */
```

### FieldMayBeStatic
Field `portRange` may be 'static'
in `modules/rest/src/main/java/org/apache/ignite/internal/rest/configuration/RestSslConfigurationSchema.java`
#### Snippet
```java
    @Range(min = 0)
    @Value(hasDefault = true)
    public final int portRange = 100;
}

```

### FieldMayBeStatic
Field `threadPoolStopTimeoutMillis` may be 'static'
in `modules/compute/src/main/java/org/apache/ignite/internal/compute/configuration/ComputeConfigurationSchema.java`
#### Snippet
```java
    @Range(min = 1)
    @Value(hasDefault = true)
    public final long threadPoolStopTimeoutMillis = 10_000;
}

```

### FieldMayBeStatic
Field `clientAuth` may be 'static'
in `modules/network/src/main/java/org/apache/ignite/internal/network/configuration/AbstractSslConfigurationSchema.java`
#### Snippet
```java
    @OneOf({"none", "optional", "require"})
    @Value(hasDefault = true)
    public final String clientAuth = "none";

    /** SSL keystore configuration. */
```

### FieldMayBeStatic
Field `soKeepAlive` may be 'static'
in `modules/network/src/main/java/org/apache/ignite/internal/network/configuration/OutboundConfigurationSchema.java`
#### Snippet
```java
    /** Keep-alive flag. */
    @Value(hasDefault = true)
    public final boolean soKeepAlive = true;

    /** Socket close linger value. */
```

### FieldMayBeStatic
Field `enabled` may be 'static'
in `modules/network/src/main/java/org/apache/ignite/internal/network/configuration/AbstractSslConfigurationSchema.java`
#### Snippet
```java
    /** Enable/disable SSL. */
    @Value(hasDefault = true)
    public final boolean enabled = false;

    /**
```

### FieldMayBeStatic
Field `soLinger` may be 'static'
in `modules/network/src/main/java/org/apache/ignite/internal/network/configuration/OutboundConfigurationSchema.java`
#### Snippet
```java
    @Range(min = 0, max = 0xFFFF)
    @Value(hasDefault = true)
    public final int soLinger = 0;

    /** TCP no delay flag. */
```

### FieldMayBeStatic
Field `tcpNoDelay` may be 'static'
in `modules/network/src/main/java/org/apache/ignite/internal/network/configuration/OutboundConfigurationSchema.java`
#### Snippet
```java
    /** TCP no delay flag. */
    @Value(hasDefault = true)
    public final boolean tcpNoDelay = true;
}

```

### FieldMayBeStatic
Field `failurePingInterval` may be 'static'
in `modules/network/src/main/java/org/apache/ignite/internal/network/configuration/ClusterMembershipConfigurationSchema.java`
#### Snippet
```java
    /** Failure detector ping interval. */
    @Value(hasDefault = true)
    public final int failurePingInterval = 1_000;

    /** ScaleCube-specific configuration. */
```

### FieldMayBeStatic
Field `membershipSyncInterval` may be 'static'
in `modules/network/src/main/java/org/apache/ignite/internal/network/configuration/ClusterMembershipConfigurationSchema.java`
#### Snippet
```java
    /** Periodic membership data sync interval. */
    @Value(hasDefault = true)
    public final int membershipSyncInterval = 30_000;

    /** Failure detector ping interval. */
```

### FieldMayBeStatic
Field `gossipInterval` may be 'static'
in `modules/network/src/main/java/org/apache/ignite/internal/network/configuration/ScaleCubeConfigurationSchema.java`
#### Snippet
```java
     */
    @Value(hasDefault = true)
    public final int gossipInterval = 200;
}

```

### FieldMayBeStatic
Field `membershipSuspicionMultiplier` may be 'static'
in `modules/network/src/main/java/org/apache/ignite/internal/network/configuration/ScaleCubeConfigurationSchema.java`
#### Snippet
```java
     */
    @Value(hasDefault = true)
    public final int membershipSuspicionMultiplier = 5;

    /**
```

### FieldMayBeStatic
Field `failurePingRequestMembers` may be 'static'
in `modules/network/src/main/java/org/apache/ignite/internal/network/configuration/ScaleCubeConfigurationSchema.java`
#### Snippet
```java
     */
    @Value(hasDefault = true)
    public final int failurePingRequestMembers = 3;

    /**
```

### FieldMayBeStatic
Field `shutdownQuietPeriod` may be 'static'
in `modules/network/src/main/java/org/apache/ignite/internal/network/configuration/NetworkConfigurationSchema.java`
#### Snippet
```java
    @Range(min = 0)
    @Value(hasDefault = true)
    public final long shutdownQuietPeriod = 0;

    /**
```

### FieldMayBeStatic
Field `shutdownTimeout` may be 'static'
in `modules/network/src/main/java/org/apache/ignite/internal/network/configuration/NetworkConfigurationSchema.java`
#### Snippet
```java
    @Range(min = 0)
    @Value(hasDefault = true)
    public final long shutdownTimeout = 15_000;

    /** Server configuration. */
```

### FieldMayBeStatic
Field `portRange` may be 'static'
in `modules/network/src/main/java/org/apache/ignite/internal/network/configuration/NetworkConfigurationSchema.java`
#### Snippet
```java
    @Range(min = 0)
    @Value(hasDefault = true)
    public final int portRange = 0;

    /**
```

### FieldMayBeStatic
Field `port` may be 'static'
in `modules/network/src/main/java/org/apache/ignite/internal/network/configuration/NetworkConfigurationSchema.java`
#### Snippet
```java
    @Range(min = 1024, max = 0xFFFF)
    @Value(hasDefault = true)
    public final int port = 47500;

    /** Network port range. */
```

### FieldMayBeStatic
Field `soReuseAddr` may be 'static'
in `modules/network/src/main/java/org/apache/ignite/internal/network/configuration/InboundConfigurationSchema.java`
#### Snippet
```java
    /** Reuse address flag. */
    @Value(hasDefault = true)
    public final boolean soReuseAddr = true;

    /** Keep-alive flag. */
```

### FieldMayBeStatic
Field `soLinger` may be 'static'
in `modules/network/src/main/java/org/apache/ignite/internal/network/configuration/InboundConfigurationSchema.java`
#### Snippet
```java
    @Range(min = 0, max = 0xFFFF)
    @Value(hasDefault = true)
    public final int soLinger = 0;

    /** TCP no delay flag. */
```

### FieldMayBeStatic
Field `tcpNoDelay` may be 'static'
in `modules/network/src/main/java/org/apache/ignite/internal/network/configuration/InboundConfigurationSchema.java`
#### Snippet
```java
    /** TCP no delay flag. */
    @Value(hasDefault = true)
    public final boolean tcpNoDelay = true;
}

```

### FieldMayBeStatic
Field `soBacklog` may be 'static'
in `modules/network/src/main/java/org/apache/ignite/internal/network/configuration/InboundConfigurationSchema.java`
#### Snippet
```java
    @Range(min = 0)
    @Value(hasDefault = true)
    public final int soBacklog = 128;

    /** Reuse address flag. */
```

### FieldMayBeStatic
Field `soKeepAlive` may be 'static'
in `modules/network/src/main/java/org/apache/ignite/internal/network/configuration/InboundConfigurationSchema.java`
#### Snippet
```java
    /** Keep-alive flag. */
    @Value(hasDefault = true)
    public final boolean soKeepAlive = true;

    /** Socket close linger value. */
```

### FieldMayBeStatic
Field `enabled` may be 'static'
in `modules/security/src/main/java/org/apache/ignite/internal/configuration/AuthenticationConfigurationSchema.java`
#### Snippet
```java
    /** Enabled. */
    @Value(hasDefault = true)
    public final boolean enabled = false;

    /** Auth configurations. */
```

### FieldMayBeStatic
Field `connectTimeout` may be 'static'
in `modules/client-handler/src/main/java/org/apache/ignite/client/handler/configuration/ClientConnectorConfigurationSchema.java`
#### Snippet
```java
    @Range(min = 0)
    @Value(hasDefault = true)
    public final int connectTimeout = 5000;

    /** Idle timeout. */
```

### FieldMayBeStatic
Field `port` may be 'static'
in `modules/client-handler/src/main/java/org/apache/ignite/client/handler/configuration/ClientConnectorConfigurationSchema.java`
#### Snippet
```java
    @Range(min = 1024, max = 0xFFFF)
    @Value(hasDefault = true)
    public final int port = 10800;

    /** TCP port range. */
```

### FieldMayBeStatic
Field `portRange` may be 'static'
in `modules/client-handler/src/main/java/org/apache/ignite/client/handler/configuration/ClientConnectorConfigurationSchema.java`
#### Snippet
```java
    @Range(min = 0)
    @Value(hasDefault = true)
    public final int portRange = 100;

    /** Connect timeout. */
```

### FieldMayBeStatic
Field `idleTimeout` may be 'static'
in `modules/client-handler/src/main/java/org/apache/ignite/client/handler/configuration/ClientConnectorConfigurationSchema.java`
#### Snippet
```java
    @Range(min = 0)
    @Value(hasDefault = true)
    public final long idleTimeout = 0;

    /** Server exception stack trace visibility. */
```

### FieldMayBeStatic
Field `sendServerExceptionStackTraceToClient` may be 'static'
in `modules/client-handler/src/main/java/org/apache/ignite/client/handler/configuration/ClientConnectorConfigurationSchema.java`
#### Snippet
```java
    /** Server exception stack trace visibility. */
    @Value(hasDefault = true)
    public final boolean sendServerExceptionStackTraceToClient = false;

    /** SSL configuration schema. */
```

### FieldMayBeStatic
Field `deploymentLocation` may be 'static'
in `modules/code-deployment/src/main/java/org/apache/ignite/internal/deployunit/configuration/DeploymentConfigurationSchema.java`
#### Snippet
```java
     */
    @Value(hasDefault = true)
    public final String deploymentLocation = "deployment";
}

```

## RuleId[id=UtilityClassWithoutPrivateConstructor]
### UtilityClassWithoutPrivateConstructor
Class `IgnitionManager` has only 'static' members, and lacks a 'private' constructor
in `modules/api/src/main/java/org/apache/ignite/IgnitionManager.java`
#### Snippet
```java
 * Service loader based implementation of an entry point for handling grid lifecycle.
 */
public class IgnitionManager {
    /**
     * Loaded Ignition instance.
```

### UtilityClassWithoutPrivateConstructor
Class `Main` has only 'static' members, and lacks a 'private' constructor
in `modules/cli/src/main/java/org/apache/ignite/internal/cli/Main.java`
#### Snippet
```java
 * Ignite cli entry point.
 */
public class Main {
    /**
     * Entry point.
```

### UtilityClassWithoutPrivateConstructor
Class `QuestionAskerFactory` has only 'static' members, and lacks a 'private' constructor
in `modules/cli/src/main/java/org/apache/ignite/internal/cli/core/flow/question/QuestionAskerFactory.java`
#### Snippet
```java
 */
//Tech Debt: IGNITE-17484
public class QuestionAskerFactory {
    private static volatile QuestionWriterReader questionWriterReader;

```

### UtilityClassWithoutPrivateConstructor
Class `CommandLineContextProvider` has only 'static' members, and lacks a 'private' constructor
in `modules/cli/src/main/java/org/apache/ignite/internal/cli/core/repl/context/CommandLineContextProvider.java`
#### Snippet
```java
 */
//Tech Debt: IGNITE-17484
public class CommandLineContextProvider {

    private static volatile CommandLineContext context;
```

### UtilityClassWithoutPrivateConstructor
Class `UiString` has only 'static' members, and lacks a 'private' constructor
in `modules/cli/src/main/java/org/apache/ignite/internal/cli/core/style/element/UiString.java`
#### Snippet
```java

/** Can format the string with UI Elements. */
public class UiString {
    /** Accepts the String template with UI Elements. */
    public static String format(String template, UiElement... elements) {
```

### UtilityClassWithoutPrivateConstructor
Class `UiElements` has only 'static' members, and lacks a 'private' constructor
in `modules/cli/src/main/java/org/apache/ignite/internal/cli/core/style/element/UiElements.java`
#### Snippet
```java

/** Defines all UI Elements that are used in the CLI. */
public class UiElements {
    public static UiElement url(String content) {
        return new MarkedUiElement(content, Style.UNDERLINE);
```

### UtilityClassWithoutPrivateConstructor
Class `StateConfig` has only 'static' members, and lacks a 'private' constructor
in `modules/cli/src/main/java/org/apache/ignite/internal/cli/config/StateConfig.java`
#### Snippet
```java
 * Config file which stores information between application restarts, but not part of the config.
 */
public class StateConfig {

    /**
```

### UtilityClassWithoutPrivateConstructor
Class `CliLoggers` has only 'static' members, and lacks a 'private' constructor
in `modules/cli/src/main/java/org/apache/ignite/internal/cli/logger/CliLoggers.java`
#### Snippet
```java
 * {@link CliLoggers#forName(String)} methods will redirect their output to the console when commands are started with the {@code -v} flag.
 */
public class CliLoggers {
    private static PrintWriter output;

```

### UtilityClassWithoutPrivateConstructor
Class `Constants` has only 'static' members, and lacks a 'private' constructor
in `modules/cli/src/main/java/org/apache/ignite/internal/cli/commands/Options.java`
#### Snippet
```java

    /** Constants for all options. */
    public static final class Constants {
        /** Cluster endpoint URL option long name. */
        public static final String CLUSTER_URL_OPTION = "--cluster-endpoint-url";
```

### UtilityClassWithoutPrivateConstructor
Class `SqlMetaData` has only 'static' members, and lacks a 'private' constructor
in `modules/cli/src/main/java/org/apache/ignite/internal/cli/commands/sql/SqlMetaData.java`
#### Snippet
```java
 */

public class SqlMetaData {

    /**
```

### UtilityClassWithoutPrivateConstructor
Class `TestStateConfigHelper` has only 'static' members, and lacks a 'private' constructor
in `modules/cli/src/testFixtures/java/org/apache/ignite/internal/cli/config/TestStateConfigHelper.java`
#### Snippet
```java
 * Test factory for application state config.
 */
public class TestStateConfigHelper {
    public static final String EMPTY = "empty.ini";
    public static final String LAST_CONNECTED_DEFAULT = "last_connected_default.ini";
```

### UtilityClassWithoutPrivateConstructor
Class `TestConfigManagerHelper` has only 'static' members, and lacks a 'private' constructor
in `modules/cli/src/testFixtures/java/org/apache/ignite/internal/cli/commands/cliconfig/TestConfigManagerHelper.java`
#### Snippet
```java
 * Test factory for {@link ConfigManager}.
 */
public class TestConfigManagerHelper {
    private static final String EMPTY = "empty.ini";
    private static final String ONE_SECTION_WITH_DEFAULT_PROFILE = "one_section_with_default_profile.ini";
```

### UtilityClassWithoutPrivateConstructor
Class `IgniteStringFormatter` has only 'static' members, and lacks a 'private' constructor
in `modules/core/src/main/java/org/apache/ignite/lang/IgniteStringFormatter.java`
#### Snippet
```java
 * <p>will return the string "File name is C:\file.zip".
 */
public final class IgniteStringFormatter {
    /** Left brace. */
    private static final char DELIM_START = '{';
```

### UtilityClassWithoutPrivateConstructor
Class `ArrayUtils` has only 'static' members, and lacks a 'private' constructor
in `modules/core/src/main/java/org/apache/ignite/internal/util/ArrayUtils.java`
#### Snippet
```java
 */
@SuppressWarnings("SwitchStatementWithTooFewBranches")
public final class ArrayUtils {
    /** Empty array of byte. */
    public static final byte[] BYTE_EMPTY_ARRAY = new byte[0];
```

### UtilityClassWithoutPrivateConstructor
Class `TemporalTypeUtils` has only 'static' members, and lacks a 'private' constructor
in `modules/core/src/main/java/org/apache/ignite/internal/util/TemporalTypeUtils.java`
#### Snippet
```java
 * Helper class for temporal type conversions.
 */
public class TemporalTypeUtils {
    /**
     * Normalize nanoseconds regarding the precision.
```

### UtilityClassWithoutPrivateConstructor
Class `FeatureChecker` has only 'static' members, and lacks a 'private' constructor
in `modules/core/src/main/java/org/apache/ignite/internal/util/FeatureChecker.java`
#### Snippet
```java
 * Class extracted for fields from GridUnsafe to be absolutely independent with current and future static block initialization effects.
 */
public class FeatureChecker {
    /** Required options to run on Java 9, 10, 11. */
    public static final String JAVA_9_10_11_OPTIONS = "--add-exports=java.base/jdk.internal.misc=ALL-UNNAMED\n"
```

### UtilityClassWithoutPrivateConstructor
Class `CursorUtils` has only 'static' members, and lacks a 'private' constructor
in `modules/core/src/main/java/org/apache/ignite/internal/util/CursorUtils.java`
#### Snippet
```java
 * Utility class for working with cursors.
 */
public class CursorUtils {
    /** Empty cursor instance. */
    private static final Cursor<?> EMPTY = Cursor.fromBareIterator(Collections.emptyIterator());
```

### UtilityClassWithoutPrivateConstructor
Class `ByteUtils` has only 'static' members, and lacks a 'private' constructor
in `modules/core/src/main/java/org/apache/ignite/internal/util/ByteUtils.java`
#### Snippet
```java
 * Utility class provides various method for manipulating with bytes.
 */
public class ByteUtils {
    /**
     * Constructs {@code long} from byte array in Big Endian order.
```

### UtilityClassWithoutPrivateConstructor
Class `SubscriptionUtils` has only 'static' members, and lacks a 'private' constructor
in `modules/core/src/main/java/org/apache/ignite/internal/util/SubscriptionUtils.java`
#### Snippet
```java
 * Java Flow API utility methods.
 */
public class SubscriptionUtils {
    /**
     * Creates a thread-safe publisher wrapper for a chain of multiple publishers. Generally, it consumes the next source after the previous
```

### UtilityClassWithoutPrivateConstructor
Class `GarbageCollector` has only 'static' members, and lacks a 'private' constructor
in `modules/core/src/main/java/org/apache/ignite/lang/ErrorGroups.java`
#### Snippet
```java
     * Garbage collector error group.
     */
    public static class GarbageCollector {
        /** Garbage collector error group. */
        public static final ErrorGroup GC_ERR_GROUP = ErrorGroup.newGroup("GC", 14);
```

### UtilityClassWithoutPrivateConstructor
Class `MetaStorage` has only 'static' members, and lacks a 'private' constructor
in `modules/core/src/main/java/org/apache/ignite/lang/ErrorGroups.java`
#### Snippet
```java

    /** Meta storage error group. */
    public static class MetaStorage {
        /** Meta storage error group. */
        public static final ErrorGroup META_STORAGE_ERR_GROUP = ErrorGroup.newGroup("META", 5);
```

### UtilityClassWithoutPrivateConstructor
Class `Transactions` has only 'static' members, and lacks a 'private' constructor
in `modules/core/src/main/java/org/apache/ignite/lang/ErrorGroups.java`
#### Snippet
```java

    /** Transactions error group. */
    public static class Transactions {
        /** Transactions error group. */
        public static final ErrorGroup TX_ERR_GROUP = ErrorGroup.newGroup("TX", 7);
```

### UtilityClassWithoutPrivateConstructor
Class `Client` has only 'static' members, and lacks a 'private' constructor
in `modules/core/src/main/java/org/apache/ignite/lang/ErrorGroups.java`
#### Snippet
```java

    /** Client error group. */
    public static class Client {
        /** Client error group. */
        public static final ErrorGroup CLIENT_ERR_GROUP = ErrorGroup.newGroup("CLIENT", 3);
```

### UtilityClassWithoutPrivateConstructor
Class `Table` has only 'static' members, and lacks a 'private' constructor
in `modules/core/src/main/java/org/apache/ignite/lang/ErrorGroups.java`
#### Snippet
```java

    /** Tables error group. */
    public static class Table {
        /** Table error group. */
        public static final ErrorGroup TABLE_ERR_GROUP = ErrorGroup.newGroup("TBL", 2);
```

### UtilityClassWithoutPrivateConstructor
Class `Index` has only 'static' members, and lacks a 'private' constructor
in `modules/core/src/main/java/org/apache/ignite/lang/ErrorGroups.java`
#### Snippet
```java

    /** Index error group. */
    public static class Index {
        /** Index error group. */
        public static final ErrorGroup INDEX_ERR_GROUP = ErrorGroup.newGroup("IDX", 6);
```

### UtilityClassWithoutPrivateConstructor
Class `Sql` has only 'static' members, and lacks a 'private' constructor
in `modules/core/src/main/java/org/apache/ignite/lang/ErrorGroups.java`
#### Snippet
```java

    /** SQL error group. */
    public static class Sql {
        /** SQL error group. */
        public static final ErrorGroup SQL_ERR_GROUP = ErrorGroup.newGroup("SQL", 4);
```

### UtilityClassWithoutPrivateConstructor
Class `Common` has only 'static' members, and lacks a 'private' constructor
in `modules/core/src/main/java/org/apache/ignite/lang/ErrorGroups.java`
#### Snippet
```java
public class ErrorGroups {
    /** Common error group. */
    public static class Common {
        /** Unknown error group. */
        public static final ErrorGroup COMMON_ERR_GROUP = ErrorGroup.newGroup("CMN", 1);
```

### UtilityClassWithoutPrivateConstructor
Class `Network` has only 'static' members, and lacks a 'private' constructor
in `modules/core/src/main/java/org/apache/ignite/lang/ErrorGroups.java`
#### Snippet
```java

    /** Network error group. */
    public static class Network {
        /** Network error group. */
        public static final ErrorGroup NETWORK_ERR_GROUP = ErrorGroup.newGroup("NETWORK", 11);
```

### UtilityClassWithoutPrivateConstructor
Class `DistributionZones` has only 'static' members, and lacks a 'private' constructor
in `modules/core/src/main/java/org/apache/ignite/lang/ErrorGroups.java`
#### Snippet
```java

    /** Distribution zones error group. */
    public static class DistributionZones {
        /** Distribution zones group. */
        public static final ErrorGroup DISTRIBUTION_ZONES_ERR_GROUP = ErrorGroup.newGroup("DISTRZONES", 10);
```

### UtilityClassWithoutPrivateConstructor
Class `NodeConfiguration` has only 'static' members, and lacks a 'private' constructor
in `modules/core/src/main/java/org/apache/ignite/lang/ErrorGroups.java`
#### Snippet
```java
     * Node configuration error group.
     */
    public static class NodeConfiguration {
        /**
         * Node configuration error group.
```

### UtilityClassWithoutPrivateConstructor
Class `Replicator` has only 'static' members, and lacks a 'private' constructor
in `modules/core/src/main/java/org/apache/ignite/lang/ErrorGroups.java`
#### Snippet
```java

    /** Replicator error group. */
    public static class Replicator {
        /** Replicator error group. */
        public static final ErrorGroup REPLICATOR_ERR_GROUP = ErrorGroup.newGroup("REP", 8);
```

### UtilityClassWithoutPrivateConstructor
Class `Storage` has only 'static' members, and lacks a 'private' constructor
in `modules/core/src/main/java/org/apache/ignite/lang/ErrorGroups.java`
#### Snippet
```java

    /** Storage error group. */
    public static class Storage {
        /** Storage error group. */
        public static final ErrorGroup STORAGE_ERR_GROUP = ErrorGroup.newGroup("STORAGE", 9);
```

### UtilityClassWithoutPrivateConstructor
Class `CodeDeployment` has only 'static' members, and lacks a 'private' constructor
in `modules/core/src/main/java/org/apache/ignite/lang/ErrorGroups.java`
#### Snippet
```java
     * Code deployment error group.
     */
    public static class CodeDeployment {
        /**
         * Code deployment error group.
```

### UtilityClassWithoutPrivateConstructor
Class `Loggers` has only 'static' members, and lacks a 'private' constructor
in `modules/core/src/main/java/org/apache/ignite/internal/logger/Loggers.java`
#### Snippet
```java
 * This class contains different static factory methods to create an instance of logger.
 */
public final class Loggers {
    /**
     * Creates logger for given class with system logger as a backend.
```

### UtilityClassWithoutPrivateConstructor
Class `ExceptionUtils` has only 'static' members, and lacks a 'private' constructor
in `modules/core/src/main/java/org/apache/ignite/internal/util/ExceptionUtils.java`
#### Snippet
```java
 * Exception utils.
 */
public final class ExceptionUtils {
    /**
     * The names of methods commonly used to access a wrapped exception.
```

### UtilityClassWithoutPrivateConstructor
Class `IgniteUtils` has only 'static' members, and lacks a 'private' constructor
in `modules/core/src/main/java/org/apache/ignite/internal/util/IgniteUtils.java`
#### Snippet
```java
 * Collection of utility methods used throughout the system.
 */
public class IgniteUtils {
    /** Byte bit-mask. */
    private static final int MASK = 0xf;
```

### UtilityClassWithoutPrivateConstructor
Class `TestFlowUtils` has only 'static' members, and lacks a 'private' constructor
in `modules/core/src/testFixtures/java/org/apache/ignite/internal/testframework/flow/TestFlowUtils.java`
#### Snippet
```java
 * Utility methods for extracting data from {@link Publisher}s.
 */
public class TestFlowUtils {
    /**
     * Subscribes to the given publisher, collecting all values into a list.
```

### UtilityClassWithoutPrivateConstructor
Class `JunitExtensionTestUtils` has only 'static' members, and lacks a 'private' constructor
in `modules/core/src/testFixtures/java/org/apache/ignite/internal/testframework/JunitExtensionTestUtils.java`
#### Snippet
```java
 * @see <a href="https://junit.org/junit5/docs/current/user-guide/#testkit">JUnit Platform Test Kit</a>
 */
class JunitExtensionTestUtils {
    /**
     * Executes the given test class on the Jupiter test engine.
```

### UtilityClassWithoutPrivateConstructor
Class `IgniteTestUtils` has only 'static' members, and lacks a 'private' constructor
in `modules/core/src/testFixtures/java/org/apache/ignite/internal/testframework/IgniteTestUtils.java`
#### Snippet
```java
 * Utility class for tests.
 */
public final class IgniteTestUtils {
    private static final IgniteLogger LOG = Loggers.forClass(IgniteTestUtils.class);

```

### UtilityClassWithoutPrivateConstructor
Class `RpcActionMessageGroup` has only 'static' members, and lacks a 'private' constructor
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/RaftMessageGroup.java`
#### Snippet
```java
     * Message types for Ignite actions.
     */
    public static final class RpcActionMessageGroup {
        /**
         * Message type for {@link ActionRequest}.
```

### UtilityClassWithoutPrivateConstructor
Class `RpcRequestsMessageGroup` has only 'static' members, and lacks a 'private' constructor
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/RaftMessageGroup.java`
#### Snippet
```java
     * Logical message subgroup for messages declared in {@link RpcRequests}.
     */
    public static final class RpcRequestsMessageGroup {
        /** */
        public static final short PING_REQUEST = 3000;
```

### UtilityClassWithoutPrivateConstructor
Class `RaftOutterMessageGroup` has only 'static' members, and lacks a 'private' constructor
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/RaftMessageGroup.java`
#### Snippet
```java
     * Logical message subgroup for messages declared in {@link RaftOutter}.
     */
    public static final class RaftOutterMessageGroup {
        /** */
        public static final short ENTRY_META = 2000;
```

### UtilityClassWithoutPrivateConstructor
Class `RpcClientMessageGroup` has only 'static' members, and lacks a 'private' constructor
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/RaftMessageGroup.java`
#### Snippet
```java
     * Logical message subgroup for messages declared in {@link CliRequests}.
     */
    public static final class RpcClientMessageGroup {
        /** */
        public static final short ADD_PEER_REQUEST = 1000;
```

### UtilityClassWithoutPrivateConstructor
Class `ElectionPriority` has only 'static' members, and lacks a 'private' constructor
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/core/ElectionPriority.java`
#### Snippet
```java
 * ElectionPriority Type
 */
public class ElectionPriority {

    /**
```

### UtilityClassWithoutPrivateConstructor
Class `Bits` has only 'static' members, and lacks a 'private' constructor
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/Bits.java`
#### Snippet
```java
 * Bits util.
 */
public class Bits {
    public static int getInt(final byte[] b, final int off) {
        return HeapByteBufUtil.getInt(b, off);
```

### UtilityClassWithoutPrivateConstructor
Class `ExecutorServiceHelper` has only 'static' members, and lacks a 'private' constructor
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/ExecutorServiceHelper.java`
#### Snippet
```java
 *
 */
public final class ExecutorServiceHelper {

    private static final IgniteLogger LOG = Loggers.forClass(ExecutorServiceHelper.class);
```

### UtilityClassWithoutPrivateConstructor
Class `Utils` has only 'static' members, and lacks a 'private' constructor
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/Utils.java`
#### Snippet
```java
 * Helper methods for jraft.
 */
public final class Utils {
    private static final IgniteLogger LOG = Loggers.forClass(Utils.class);

```

### UtilityClassWithoutPrivateConstructor
Class `ThreadPoolMetricRegistry` has only 'static' members, and lacks a 'private' constructor
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/ThreadPoolMetricRegistry.java`
#### Snippet
```java
 *
 */
public class ThreadPoolMetricRegistry {

    private static final MetricRegistry metricRegistry = new MetricRegistry();
```

### UtilityClassWithoutPrivateConstructor
Class `StringUtils` has only 'static' members, and lacks a 'private' constructor
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/StringUtils.java`
#### Snippet
```java
import java.util.List;

public class StringUtils {
    public static final String EMPTY = "";
    public static final String[] EMPTY_STRING_ARRAY = new String[0];
```

### UtilityClassWithoutPrivateConstructor
Class `Platform` has only 'static' members, and lacks a 'private' constructor
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/Platform.java`
#### Snippet
```java
 *
 */
public class Platform {

    private static final IgniteLogger LOG = Loggers.forClass(Platform.class);
```

### UtilityClassWithoutPrivateConstructor
Class `Mpsc` has only 'static' members, and lacks a 'private' constructor
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/Mpsc.java`
#### Snippet
```java
 * Mpsc factory.
 */
public final class Mpsc {
    private static final int MPSC_CHUNK_SIZE = 1024;
    private static final int MIN_MAX_MPSC_CAPACITY = MPSC_CHUNK_SIZE << 1;
```

### UtilityClassWithoutPrivateConstructor
Class `Updaters` has only 'static' members, and lacks a 'private' constructor
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/internal/Updaters.java`
#### Snippet
```java
 * Sometime instead of reflection, better performance.
 */
public class Updaters {

    /**
```

### UtilityClassWithoutPrivateConstructor
Class `TestUtils` has only 'static' members, and lacks a 'private' constructor
in `modules/raft/src/testFixtures/java/org/apache/ignite/raft/jraft/test/TestUtils.java`
#### Snippet
```java
 * Test helper
 */
public class TestUtils {
    public static ConfigurationEntry getConfEntry(String confStr, String oldConfStr) {
        ConfigurationEntry entry = new ConfigurationEntry();
```

### UtilityClassWithoutPrivateConstructor
Class `AuthenticatorFactory` has only 'static' members, and lacks a 'private' constructor
in `modules/rest/src/main/java/org/apache/ignite/internal/rest/authentication/AuthenticatorFactory.java`
#### Snippet
```java

/** Factory for {@link Authenticator}. */
class AuthenticatorFactory {

    static Authenticator create(AuthenticationProviderView view) {
```

### UtilityClassWithoutPrivateConstructor
Class `SnapshotMetaUtils` has only 'static' members, and lacks a 'private' constructor
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/raft/snapshot/outgoing/SnapshotMetaUtils.java`
#### Snippet
```java
 * Utils to build {@link SnapshotMeta} instances.
 */
public class SnapshotMetaUtils {
    /**
     * Builds a {@link SnapshotMeta} corresponding to RAFT state (term, configuration) at the given log index.
```

### UtilityClassWithoutPrivateConstructor
Class `RebalanceUtil` has only 'static' members, and lacks a 'private' constructor
in `modules/table/src/main/java/org/apache/ignite/internal/utils/RebalanceUtil.java`
#### Snippet
```java
 * Util class for methods needed for the rebalance process.
 */
public class RebalanceUtil {

    /** Logger. */
```

### UtilityClassWithoutPrivateConstructor
Class `ClientUtils` has only 'static' members, and lacks a 'private' constructor
in `modules/client/src/main/java/org/apache/ignite/internal/client/ClientUtils.java`
#### Snippet
```java
 * Client utilities.
 */
public class ClientUtils {
    /**
     * Waits for async operation completion.
```

### UtilityClassWithoutPrivateConstructor
Class `ClientFutureUtils` has only 'static' members, and lacks a 'private' constructor
in `modules/client/src/main/java/org/apache/ignite/internal/client/ClientFutureUtils.java`
#### Snippet
```java
 * Future utils.
 */
class ClientFutureUtils {
    static <T> @Nullable T getNowSafe(CompletableFuture<T> fut) {
        try {
```

### UtilityClassWithoutPrivateConstructor
Class `RecoveryCompletionFutureFactory` has only 'static' members, and lacks a 'private' constructor
in `modules/runner/src/main/java/org/apache/ignite/internal/recovery/RecoveryCompletionFutureFactory.java`
#### Snippet
```java
 * Creates a future that completes when local recovery is finished.
 */
public class RecoveryCompletionFutureFactory {
    /**
     * Create recovery completion future.
```

### UtilityClassWithoutPrivateConstructor
Class `SchemaUtils` has only 'static' members, and lacks a 'private' constructor
in `modules/schema/src/main/java/org/apache/ignite/internal/schema/SchemaUtils.java`
#### Snippet
```java
 * Stateless schema utils that produces helper methods for schema preparation.
 */
public class SchemaUtils {
    /**
     * Creates schema descriptor for the table with specified configuration.
```

### UtilityClassWithoutPrivateConstructor
Class `DefaultValueGenerators` has only 'static' members, and lacks a 'private' constructor
in `modules/schema/src/testFixtures/java/org/apache/ignite/internal/schema/testutils/definition/DefaultValueGenerators.java`
#### Snippet
```java
 * (i.e. could be specified as a default in column definition).
 */
public class DefaultValueGenerators {
    /** Generator that generates random UUID string. */
    public static final String GEN_RANDOM_UUID = "GEN_RANDOM_UUID";
```

### UtilityClassWithoutPrivateConstructor
Class `SchemaConfigurationConverter` has only 'static' members, and lacks a 'private' constructor
in `modules/schema/src/testFixtures/java/org/apache/ignite/internal/schema/testutils/SchemaConfigurationConverter.java`
#### Snippet
```java
 * Configuration to schema and vice versa converter.
 */
public class SchemaConfigurationConverter {
    /**
     * Types map.
```

### UtilityClassWithoutPrivateConstructor
Class `ComputeMessageTypes` has only 'static' members, and lacks a 'private' constructor
in `modules/compute/src/main/java/org/apache/ignite/internal/compute/ComputeMessageTypes.java`
#### Snippet
```java
 */
@MessageGroup(groupType = 6, groupName = "ComputeMessages")
public class ComputeMessageTypes {
    /**
     * Type for {@link ExecuteRequest}.
```

### UtilityClassWithoutPrivateConstructor
Class `NodeFinderFactory` has only 'static' members, and lacks a 'private' constructor
in `modules/network/src/main/java/org/apache/ignite/network/NodeFinderFactory.java`
#### Snippet
```java
 * {@link NodeFinder} factory.
 */
public class NodeFinderFactory {
    /**
     * Creates a {@link NodeFinder} based on the provided configuration.
```

### UtilityClassWithoutPrivateConstructor
Class `NetworkMessageTypes` has only 'static' members, and lacks a 'private' constructor
in `modules/network/src/main/java/org/apache/ignite/internal/network/NetworkMessageTypes.java`
#### Snippet
```java
 */
@MessageGroup(groupName = "NetworkMessages", groupType = 1)
public class NetworkMessageTypes {
    /**
     * Type for {@link InvokeRequest}.
```

### UtilityClassWithoutPrivateConstructor
Class `NettyUtils` has only 'static' members, and lacks a 'private' constructor
in `modules/network/src/main/java/org/apache/ignite/internal/network/netty/NettyUtils.java`
#### Snippet
```java
 * Netty utilities.
 */
public class NettyUtils {
    /**
     * Convert a Netty {@link Future} to a {@link CompletableFuture}.
```

### UtilityClassWithoutPrivateConstructor
Class `DirectMarshallingUtils` has only 'static' members, and lacks a 'private' constructor
in `modules/network/src/main/java/org/apache/ignite/internal/network/direct/DirectMarshallingUtils.java`
#### Snippet
```java
 * Direct marshalling utils.
 */
public class DirectMarshallingUtils {
    /**
     * Reads a {@code short} from a byte buffer in an order defined by the {@link DirectByteBufferStream} implementation.
```

### UtilityClassWithoutPrivateConstructor
Class `PipelineUtils` has only 'static' members, and lacks a 'private' constructor
in `modules/network/src/main/java/org/apache/ignite/internal/network/netty/PipelineUtils.java`
#### Snippet
```java

/** Pipeline utils. */
public class PipelineUtils {
    /** {@link ChunkedWriteHandler}'s name. */
    private static final String CHUNKED_WRITE_HANDLER_NAME = "chunked-write-handler";
```

### UtilityClassWithoutPrivateConstructor
Class `BuiltInTypeIds` has only 'static' members, and lacks a 'private' constructor
in `modules/network/src/main/java/org/apache/ignite/internal/network/serialization/BuiltInTypeIds.java`
#### Snippet
```java
 * most of them are defined directly on enum members in {@link BuiltInType}.
 */
public class BuiltInTypeIds {
    public static final int BYTE = 0;
    public static final int SHORT = 2;
```

### UtilityClassWithoutPrivateConstructor
Class `Primitives` has only 'static' members, and lacks a 'private' constructor
in `modules/network/src/main/java/org/apache/ignite/internal/network/serialization/Primitives.java`
#### Snippet
```java
 * Utils to work with primitives.
 */
public class Primitives {
    /**
     * Returns number of bytes a value of the given primtive type takes (1 for byte, 8 for long and so on).
```

### UtilityClassWithoutPrivateConstructor
Class `FlaggedObjectIds` has only 'static' members, and lacks a 'private' constructor
in `modules/network/src/main/java/org/apache/ignite/internal/network/serialization/marshal/FlaggedObjectIds.java`
#### Snippet
```java
 * Such representation of a FlaggedObjectId using primitive long is chosen in a pursue of lower memory allocation rate.
 */
class FlaggedObjectIds {
    private static final long LOWER_32_BITS_MASK = 0xFFFFFFFFL;

```

### UtilityClassWithoutPrivateConstructor
Class `ClassDescriptorMerger` has only 'static' members, and lacks a 'private' constructor
in `modules/network/src/main/java/org/apache/ignite/internal/network/serialization/ClassDescriptorMerger.java`
#### Snippet
```java
 * Merges {@link ClassDescriptor} components.
 */
class ClassDescriptorMerger {
    static List<MergedField> mergeFields(List<FieldDescriptor> localFields, List<FieldDescriptor> remoteFields) {
        List<MergedField> mergedFields = new ArrayList<>();
```

### UtilityClassWithoutPrivateConstructor
Class `LittleEndianBits` has only 'static' members, and lacks a 'private' constructor
in `modules/network/src/main/java/org/apache/ignite/internal/network/serialization/marshal/LittleEndianBits.java`
#### Snippet
```java
 * Little endian byte order is used.
 */
class LittleEndianBits {
    static boolean getBoolean(byte[] b, int off) {
        return b[off] != 0;
```

### UtilityClassWithoutPrivateConstructor
Class `TestMessageTypes` has only 'static' members, and lacks a 'private' constructor
in `modules/network/src/testFixtures/java/org/apache/ignite/internal/network/messages/TestMessageTypes.java`
#### Snippet
```java
 */
@MessageGroup(groupName = "TestMessages", groupType = 2)
public class TestMessageTypes {
    /** Type of {@link SerializationOrderMessage}. */
    public static final short SERIALIZATION_ORDER_MESSAGE = 1;
```

### UtilityClassWithoutPrivateConstructor
Class `ClusterServiceTestUtils` has only 'static' members, and lacks a 'private' constructor
in `modules/network/src/testFixtures/java/org/apache/ignite/utils/ClusterServiceTestUtils.java`
#### Snippet
```java
 * Test utils that provide sort of cluster service mock that manages required node configuration internally.
 */
public class ClusterServiceTestUtils {
    private static final TestScaleCubeClusterServiceFactory SERVICE_FACTORY = new TestScaleCubeClusterServiceFactory();

```

### UtilityClassWithoutPrivateConstructor
Class `AffinityUtils` has only 'static' members, and lacks a 'private' constructor
in `modules/affinity/src/main/java/org/apache/ignite/internal/affinity/AffinityUtils.java`
#### Snippet
```java
 * Stateless affinity utils that produces helper methods for an affinity assignments calculation.
 */
public class AffinityUtils {
    /**
     * Calculates affinity assignments.
```

### UtilityClassWithoutPrivateConstructor
Class `QueryProperty` has only 'static' members, and lacks a 'private' constructor
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/QueryProperty.java`
#### Snippet
```java
 * Enumerates the properties which might be used during query execution.
 */
public class QueryProperty {
    public static final Property<Long> QUERY_TIMEOUT = new Property<>("query_timeout", Long.class);
    public static final Property<String> DEFAULT_SCHEMA = new Property<>("default_schema", String.class);
```

### UtilityClassWithoutPrivateConstructor
Class `Stubs` has only 'static' members, and lacks a 'private' constructor
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/Stubs.java`
#### Snippet
```java

/** Stubs. */
public class Stubs {
    public static int intFoo(Object... args) {
        return args == null ? 0 : args.length;
```

### UtilityClassWithoutPrivateConstructor
Class `RowConverter` has only 'static' members, and lacks a 'private' constructor
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/RowConverter.java`
#### Snippet
```java
 * Helper class provides method to convert binary tuple to rows and vice-versa.
 */
public final class RowConverter {
    /**
     * Creates binary tuple schema for index rows.
```

### UtilityClassWithoutPrivateConstructor
Class `PlanUtils` has only 'static' members, and lacks a 'private' constructor
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/util/PlanUtils.java`
#### Snippet
```java
 * Plan util methods.
 */
public class PlanUtils {
    /**
     * Return {@code true} if observes AGGREGATE and DISTINCT simultaneously.
```

### UtilityClassWithoutPrivateConstructor
Class `TypeUtils` has only 'static' members, and lacks a 'private' constructor
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/util/TypeUtils.java`
#### Snippet
```java
 * TODO Documentation https://issues.apache.org/jira/browse/IGNITE-15859
 */
public class TypeUtils {
    private static final Set<SqlTypeName> CONVERTABLE_TYPES = EnumSet.of(
            SqlTypeName.DATE,
```

### UtilityClassWithoutPrivateConstructor
Class `IgniteDistributions` has only 'static' members, and lacks a 'private' constructor
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/trait/IgniteDistributions.java`
#### Snippet
```java
 * TODO Documentation https://issues.apache.org/jira/browse/IGNITE-15859
 */
public class IgniteDistributions {
    private static final IgniteDistribution BROADCAST = canonize(new DistributionTrait(DistributionFunction.broadcast()));

```

### UtilityClassWithoutPrivateConstructor
Class `SqlQueryMessageGroup` has only 'static' members, and lacks a 'private' constructor
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/message/SqlQueryMessageGroup.java`
#### Snippet
```java
 */
@MessageGroup(groupType = GROUP_TYPE, groupName = "SqlQueryMessages")
public final class SqlQueryMessageGroup {
    public static final short GROUP_TYPE = 4;

```

### UtilityClassWithoutPrivateConstructor
Class `IgnitePrograms` has only 'static' members, and lacks a 'private' constructor
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/prepare/IgnitePrograms.java`
#### Snippet
```java
 * TODO Documentation https://issues.apache.org/jira/browse/IGNITE-15859
 */
public class IgnitePrograms {
    /**
     * Returns heuristic planer based program with given rules.
```

### UtilityClassWithoutPrivateConstructor
Class `RexUtils` has only 'static' members, and lacks a 'private' constructor
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/util/RexUtils.java`
#### Snippet
```java
 * TODO Documentation https://issues.apache.org/jira/browse/IGNITE-15859
 */
public class RexUtils {
    /** Maximum amount of search bounds tuples per scan. */
    public static final int MAX_SEARCH_BOUNDS_COMPLEXITY = 100;
```

### UtilityClassWithoutPrivateConstructor
Class `TraitUtils` has only 'static' members, and lacks a 'private' constructor
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/trait/TraitUtils.java`
#### Snippet
```java
 * TraitUtils. TODO Documentation https://issues.apache.org/jira/browse/IGNITE-15859
 */
public class TraitUtils {
    /**
     * Enforce. TODO Documentation https://issues.apache.org/jira/browse/IGNITE-15859
```

### UtilityClassWithoutPrivateConstructor
Class `IgniteMetadata` has only 'static' members, and lacks a 'private' constructor
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/metadata/IgniteMetadata.java`
#### Snippet
```java
 * Utility class, holding metadata related interfaces and metadata providers.
 */
public class IgniteMetadata {
    public static final RelMetadataProvider METADATA_PROVIDER =
            ChainedRelMetadataProvider.of(
```

### UtilityClassWithoutPrivateConstructor
Class `VolcanoUtils` has only 'static' members, and lacks a 'private' constructor
in `modules/sql-engine/src/main/java/org/apache/calcite/plan/volcano/VolcanoUtils.java`
#### Snippet
```java

/** Utils for access to package-private fields. */
public class VolcanoUtils {
    /**
     * Return cost of best known plan.
```

### UtilityClassWithoutPrivateConstructor
Class `RocksStorageUtils` has only 'static' members, and lacks a 'private' constructor
in `modules/metastorage/src/main/java/org/apache/ignite/internal/metastorage/server/persistence/RocksStorageUtils.java`
#### Snippet
```java
 * Utility class for {@link RocksDbKeyValueStorage}.
 */
class RocksStorageUtils {
    /**
     * VarHandle that gives the access to the elements of a {@code byte[]} array viewed as if it were a {@code long[]} array. Byte order
```

### UtilityClassWithoutPrivateConstructor
Class `PartitionlessLinks` has only 'static' members, and lacks a 'private' constructor
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/util/PartitionlessLinks.java`
#### Snippet
```java
 * @see PageIdUtils#pageId(int, byte, int)
 */
public class PartitionlessLinks {
    /** Number of bytes a partitionless link takes in storage. */
    public static final int PARTITIONLESS_LINK_SIZE_BYTES = 6;
```

### UtilityClassWithoutPrivateConstructor
Class `PageIdUtils` has only 'static' members, and lacks a 'private' constructor
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/util/PageIdUtils.java`
#### Snippet
```java
 * @see FullPageId
 */
public final class PageIdUtils {
    /** Represents an absent or missing link. */
    public static final long NULL_LINK = 0;
```

### UtilityClassWithoutPrivateConstructor
Class `PageUtils` has only 'static' members, and lacks a 'private' constructor
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/util/PageUtils.java`
#### Snippet
```java
 * Page utils.
 */
public class PageUtils {
    /**
     * Reads a byte from the memory.
```

### UtilityClassWithoutPrivateConstructor
Class `PageHeader` has only 'static' members, and lacks a 'private' constructor
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/PageHeader.java`
#### Snippet
```java
 * Page header.
 */
public class PageHeader {
    /** Page marker. */
    public static final long PAGE_MARKER = 0x0000000000000001L;
```

### UtilityClassWithoutPrivateConstructor
Class `PageStoreUtils` has only 'static' members, and lacks a 'private' constructor
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/store/PageStoreUtils.java`
#### Snippet
```java
 * Utility class for page stores.
 */
class PageStoreUtils {
    /**
     * Checks the page sizes in bytes of the file.
```

### UtilityClassWithoutPrivateConstructor
Class `CheckpointTestUtils` has only 'static' members, and lacks a 'private' constructor
in `modules/page-memory/src/testFixtures/java/org/apache/ignite/internal/pagememory/persistence/checkpoint/CheckpointTestUtils.java`
#### Snippet
```java
 * Useful class for testing a checkpoint.
 */
public class CheckpointTestUtils {
    /**
     * Returns new instance of {@link CheckpointReadWriteLock}.
```

### UtilityClassWithoutPrivateConstructor
Class `StorageUtils` has only 'static' members, and lacks a 'private' constructor
in `modules/storage-api/src/main/java/org/apache/ignite/internal/storage/util/StorageUtils.java`
#### Snippet
```java
 * Helper class for storages.
 */
public class StorageUtils {
    /**
     * Creates an error message about missing multi-version partition storage.
```

### UtilityClassWithoutPrivateConstructor
Class `BinaryTupleCommon` has only 'static' members, and lacks a 'private' constructor
in `modules/binary-tuple/src/main/java/org/apache/ignite/internal/binarytuple/BinaryTupleCommon.java`
#### Snippet
```java
 * Common binary tuple constants and utils.
 */
public class BinaryTupleCommon {
    /** Size of a tuple header, in bytes. */
    public static final int HEADER_SIZE = 1;
```

### UtilityClassWithoutPrivateConstructor
Class `TxMessageGroup` has only 'static' members, and lacks a 'private' constructor
in `modules/transactions/src/main/java/org/apache/ignite/internal/tx/message/TxMessageGroup.java`
#### Snippet
```java
 */
@MessageGroup(groupType = 5, groupName = "TxMessages")
public class TxMessageGroup {
    /**
     * Message type for {@link TxFinishReplicaRequest}.
```

### UtilityClassWithoutPrivateConstructor
Class `ClientMessageUtils` has only 'static' members, and lacks a 'private' constructor
in `modules/client-common/src/main/java/org/apache/ignite/internal/jdbc/proto/event/ClientMessageUtils.java`
#### Snippet
```java
 * Client message utils class.
 */
public class ClientMessageUtils {
    /**
     * Packs a string or null if string is null.
```

### UtilityClassWithoutPrivateConstructor
Class `ClientDataType` has only 'static' members, and lacks a 'private' constructor
in `modules/client-common/src/main/java/org/apache/ignite/internal/client/proto/ClientDataType.java`
#### Snippet
```java
 * Client data types.
 */
public class ClientDataType {
    /** Byte. */
    public static final int INT8 = 1;
```

### UtilityClassWithoutPrivateConstructor
Class `ResponseFlags` has only 'static' members, and lacks a 'private' constructor
in `modules/client-common/src/main/java/org/apache/ignite/internal/client/proto/ResponseFlags.java`
#### Snippet
```java
 * Response flags utils.
 */
public class ResponseFlags {
    /** Partitions assignment flag. */
    private static final int PARTITION_ASSIGNMENT_FLAG = 1;
```

### UtilityClassWithoutPrivateConstructor
Class `ClientMsgPackType` has only 'static' members, and lacks a 'private' constructor
in `modules/client-common/src/main/java/org/apache/ignite/internal/client/proto/ClientMsgPackType.java`
#### Snippet
```java
 * Ignite-specific extension type codes.
 */
public class ClientMsgPackType {
    /** UUID. */
    public static final byte UUID = 3;
```

### UtilityClassWithoutPrivateConstructor
Class `ClientMessageCommon` has only 'static' members, and lacks a 'private' constructor
in `modules/client-common/src/main/java/org/apache/ignite/internal/client/proto/ClientMessageCommon.java`
#### Snippet
```java
 * Common client message constants.
 */
public class ClientMessageCommon {
    /** Message header size. */
    public static final int HEADER_SIZE = 4;
```

### UtilityClassWithoutPrivateConstructor
Class `ClientOp` has only 'static' members, and lacks a 'private' constructor
in `modules/client-common/src/main/java/org/apache/ignite/internal/client/proto/ClientOp.java`
#### Snippet
```java
 * Client operation codes.
 */
public class ClientOp {
    /** Heartbeat. */
    public static final int HEARTBEAT = 1;
```

### UtilityClassWithoutPrivateConstructor
Class `ServerMessageType` has only 'static' members, and lacks a 'private' constructor
in `modules/client-common/src/main/java/org/apache/ignite/internal/client/proto/ServerMessageType.java`
#### Snippet
```java
 * Server to client message types.
 */
public class ServerMessageType {
    /** Response to a request (initiated by the client). */
    public static final int RESPONSE = 0;
```

### UtilityClassWithoutPrivateConstructor
Class `ClientColumnTypeConverter` has only 'static' members, and lacks a 'private' constructor
in `modules/client-common/src/main/java/org/apache/ignite/internal/client/proto/ClientColumnTypeConverter.java`
#### Snippet
```java
 * SQL column type utils.
 */
public class ClientColumnTypeConverter {
    /**
     * Converts column type to wire code.
```

### UtilityClassWithoutPrivateConstructor
Class `ClientBinaryTupleUtils` has only 'static' members, and lacks a 'private' constructor
in `modules/client-common/src/main/java/org/apache/ignite/internal/client/proto/ClientBinaryTupleUtils.java`
#### Snippet
```java
 * Client binary tuple utils.
 */
public class ClientBinaryTupleUtils {
    /**
     * Reads a binary tuple column and sets the value in the specified tuple.
```

### UtilityClassWithoutPrivateConstructor
Class `TypeUtils` has only 'static' members, and lacks a 'private' constructor
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/TypeUtils.java`
#### Snippet
```java
 * Helper methods for working with reflection API.
 */
public class TypeUtils {
    /**
     * Returns the boxed version of the given class, if it is a primitive.
```

### UtilityClassWithoutPrivateConstructor
Class `ConfigurationSerializationUtil` has only 'static' members, and lacks a 'private' constructor
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/util/ConfigurationSerializationUtil.java`
#### Snippet
```java
 * Utility class to help serializing  deserializing configuration values - primitives, strings, or arrays of primitives or strings.
 */
public class ConfigurationSerializationUtil {
    private static final byte BOOLEAN = 1;

```

### UtilityClassWithoutPrivateConstructor
Class `HoconConverter` has only 'static' members, and lacks a 'private' constructor
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/hocon/HoconConverter.java`
#### Snippet
```java
 * Hocon converter.
 */
public class HoconConverter {
    /**
     * Converts configuration subtree to a HOCON {@link ConfigValue} instance.
```

### UtilityClassWithoutPrivateConstructor
Class `ConfigurationFlattener` has only 'static' members, and lacks a 'private' constructor
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/util/ConfigurationFlattener.java`
#### Snippet
```java

/** Utility class that has {@link ConfigurationFlattener#createFlattenedUpdatesMap(SuperRoot, SuperRoot)} method. */
public class ConfigurationFlattener {
    /**
     * Convert a traversable tree to a map of qualified keys to values.
```

### UtilityClassWithoutPrivateConstructor
Class `ConfigurationUtil` has only 'static' members, and lacks a 'private' constructor
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/util/ConfigurationUtil.java`
#### Snippet
```java
 * Utility class for configuration.
 */
public class ConfigurationUtil {
    /** Configuration source that copies values without modifying tham. */
    public static final ConfigurationSource EMPTY_CFG_SRC = new ConfigurationSource() {
```

### UtilityClassWithoutPrivateConstructor
Class `ValidationUtil` has only 'static' members, and lacks a 'private' constructor
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/validation/ValidationUtil.java`
#### Snippet
```java
 * Utility class for configuration validation.
 */
public class ValidationUtil {
    /**
     * Validate configuration changes.
```

### UtilityClassWithoutPrivateConstructor
Class `ClientTransactionBeginRequest` has only 'static' members, and lacks a 'private' constructor
in `modules/client-handler/src/main/java/org/apache/ignite/client/handler/requests/tx/ClientTransactionBeginRequest.java`
#### Snippet
```java
 * Client transaction begin request.
 */
public class ClientTransactionBeginRequest {
    /**
     * Processes the request.
```

### UtilityClassWithoutPrivateConstructor
Class `ClientTransactionRollbackRequest` has only 'static' members, and lacks a 'private' constructor
in `modules/client-handler/src/main/java/org/apache/ignite/client/handler/requests/tx/ClientTransactionRollbackRequest.java`
#### Snippet
```java
 * Client transaction rollback request.
 */
public class ClientTransactionRollbackRequest {
    /**
     * Processes the request.
```

### UtilityClassWithoutPrivateConstructor
Class `ClientTransactionCommitRequest` has only 'static' members, and lacks a 'private' constructor
in `modules/client-handler/src/main/java/org/apache/ignite/client/handler/requests/tx/ClientTransactionCommitRequest.java`
#### Snippet
```java
 * Client transaction commit request.
 */
public class ClientTransactionCommitRequest {
    /**
     * Processes the request.
```

### UtilityClassWithoutPrivateConstructor
Class `ClientSqlCursorCloseRequest` has only 'static' members, and lacks a 'private' constructor
in `modules/client-handler/src/main/java/org/apache/ignite/client/handler/requests/sql/ClientSqlCursorCloseRequest.java`
#### Snippet
```java
 * Client SQL cursor close request.
 */
public class ClientSqlCursorCloseRequest {
    /**
     * Processes the request.
```

### UtilityClassWithoutPrivateConstructor
Class `ClientSqlCursorNextPageRequest` has only 'static' members, and lacks a 'private' constructor
in `modules/client-handler/src/main/java/org/apache/ignite/client/handler/requests/sql/ClientSqlCursorNextPageRequest.java`
#### Snippet
```java
 * Client SQL cursor next page request.
 */
public class ClientSqlCursorNextPageRequest {
    /**
     * Processes the request.
```

### UtilityClassWithoutPrivateConstructor
Class `TestValidationUtil` has only 'static' members, and lacks a 'private' constructor
in `modules/configuration/src/testFixtures/java/org/apache/ignite/internal/configuration/validation/TestValidationUtil.java`
#### Snippet
```java
 * Useful class for testing {@link Validator}.
 */
public class TestValidationUtil {
    /**
     * Creates mock {@link ValidationContext}.
```

### UtilityClassWithoutPrivateConstructor
Class `ClientSqlCommon` has only 'static' members, and lacks a 'private' constructor
in `modules/client-handler/src/main/java/org/apache/ignite/client/handler/requests/sql/ClientSqlCommon.java`
#### Snippet
```java
 * Common SQL request handling logic.
 */
class ClientSqlCommon {
    static void packCurrentPage(ClientMessagePacker out, AsyncResultSet<SqlRow> asyncResultSet) {
        ResultSetMetadata meta = asyncResultSet.metadata();
```

### UtilityClassWithoutPrivateConstructor
Class `ClientJdbcTableMetadataRequest` has only 'static' members, and lacks a 'private' constructor
in `modules/client-handler/src/main/java/org/apache/ignite/client/handler/requests/jdbc/ClientJdbcTableMetadataRequest.java`
#### Snippet
```java
 * Client jdbc table metadata request handler.
 */
public class ClientJdbcTableMetadataRequest {
    /**
     * Processes remote {@code JdbcMetaTablesRequest}.
```

### UtilityClassWithoutPrivateConstructor
Class `ClientJdbcColumnMetadataRequest` has only 'static' members, and lacks a 'private' constructor
in `modules/client-handler/src/main/java/org/apache/ignite/client/handler/requests/jdbc/ClientJdbcColumnMetadataRequest.java`
#### Snippet
```java
 * Client jdbc column metadata request handler.
 */
public class ClientJdbcColumnMetadataRequest {
    /**
     * Processes remote {@code JdbcMetaColumnsRequest}.
```

### UtilityClassWithoutPrivateConstructor
Class `ClientSqlExecuteRequest` has only 'static' members, and lacks a 'private' constructor
in `modules/client-handler/src/main/java/org/apache/ignite/client/handler/requests/sql/ClientSqlExecuteRequest.java`
#### Snippet
```java
 * Client SQL execute request.
 */
public class ClientSqlExecuteRequest {
    /**
     * Processes the request.
```

### UtilityClassWithoutPrivateConstructor
Class `ClientJdbcExecuteRequest` has only 'static' members, and lacks a 'private' constructor
in `modules/client-handler/src/main/java/org/apache/ignite/client/handler/requests/jdbc/ClientJdbcExecuteRequest.java`
#### Snippet
```java
 * Client jdbc request handler.
 */
public class ClientJdbcExecuteRequest {
    /**
     * Processes remote {@code JdbcQueryExecuteRequest}.
```

### UtilityClassWithoutPrivateConstructor
Class `ClientJdbcFetchRequest` has only 'static' members, and lacks a 'private' constructor
in `modules/client-handler/src/main/java/org/apache/ignite/client/handler/requests/jdbc/ClientJdbcFetchRequest.java`
#### Snippet
```java
 * Client jdbc request handler.
 */
public class ClientJdbcFetchRequest {
    /**
     * Processes remote {@code JdbcQueryFetchRequest}.
```

### UtilityClassWithoutPrivateConstructor
Class `ClientJdbcSchemasMetadataRequest` has only 'static' members, and lacks a 'private' constructor
in `modules/client-handler/src/main/java/org/apache/ignite/client/handler/requests/jdbc/ClientJdbcSchemasMetadataRequest.java`
#### Snippet
```java
 * Client jdbc schema metadata request handler.
 */
public class ClientJdbcSchemasMetadataRequest {
    /**
     * Processes remote {@code JdbcMetaSchemasRequest}.
```

### UtilityClassWithoutPrivateConstructor
Class `ClientJdbcExecuteBatchRequest` has only 'static' members, and lacks a 'private' constructor
in `modules/client-handler/src/main/java/org/apache/ignite/client/handler/requests/jdbc/ClientJdbcExecuteBatchRequest.java`
#### Snippet
```java
 * Client jdbc request handler.
 */
public class ClientJdbcExecuteBatchRequest {
    /**
     * Processes remote {@code JdbcBatchExecuteRequest}.
```

### UtilityClassWithoutPrivateConstructor
Class `ClientJdbcPreparedStmntBatchRequest` has only 'static' members, and lacks a 'private' constructor
in `modules/client-handler/src/main/java/org/apache/ignite/client/handler/requests/jdbc/ClientJdbcPreparedStmntBatchRequest.java`
#### Snippet
```java
 * Client jdbc prepared statement sql execute request handler.
 */
public class ClientJdbcPreparedStmntBatchRequest {
    /**
     * Processes remote {@code BatchPreparedStmntRequest}.
```

### UtilityClassWithoutPrivateConstructor
Class `ClientJdbcCloseRequest` has only 'static' members, and lacks a 'private' constructor
in `modules/client-handler/src/main/java/org/apache/ignite/client/handler/requests/jdbc/ClientJdbcCloseRequest.java`
#### Snippet
```java
 * Client jdbc close request handler.
 */
public class ClientJdbcCloseRequest {
    /**
     * Processes remote {@code JdbcQueryCloseRequest}.
```

### UtilityClassWithoutPrivateConstructor
Class `ClientJdbcPrimaryKeyMetadataRequest` has only 'static' members, and lacks a 'private' constructor
in `modules/client-handler/src/main/java/org/apache/ignite/client/handler/requests/jdbc/ClientJdbcPrimaryKeyMetadataRequest.java`
#### Snippet
```java
 * Client jdbc primary key metadata request handler.
 */
public class ClientJdbcPrimaryKeyMetadataRequest {
    /**
     * Processes remote {@code JdbcMetaPrimaryKeysRequest}.
```

### UtilityClassWithoutPrivateConstructor
Class `ClientJdbcQueryMetadataRequest` has only 'static' members, and lacks a 'private' constructor
in `modules/client-handler/src/main/java/org/apache/ignite/client/handler/requests/jdbc/ClientJdbcQueryMetadataRequest.java`
#### Snippet
```java
 * Client jdbc query metadata request handler.
 */
public class ClientJdbcQueryMetadataRequest {
    /**
     * Processes remote {@code JdbcQueryMetadataRequest}.
```

### UtilityClassWithoutPrivateConstructor
Class `ClientTupleUpsertAllRequest` has only 'static' members, and lacks a 'private' constructor
in `modules/client-handler/src/main/java/org/apache/ignite/client/handler/requests/table/ClientTupleUpsertAllRequest.java`
#### Snippet
```java
 * Client tuple upsert all request.
 */
public class ClientTupleUpsertAllRequest {
    /**
     * Processes the request.
```

### UtilityClassWithoutPrivateConstructor
Class `ClientTupleGetRequest` has only 'static' members, and lacks a 'private' constructor
in `modules/client-handler/src/main/java/org/apache/ignite/client/handler/requests/table/ClientTupleGetRequest.java`
#### Snippet
```java
 * Client tuple get request.
 */
public class ClientTupleGetRequest {
    /**
     * Processes the request.
```

### UtilityClassWithoutPrivateConstructor
Class `ClientTupleDeleteAllRequest` has only 'static' members, and lacks a 'private' constructor
in `modules/client-handler/src/main/java/org/apache/ignite/client/handler/requests/table/ClientTupleDeleteAllRequest.java`
#### Snippet
```java
 * Client tuple delete all request.
 */
public class ClientTupleDeleteAllRequest {
    /**
     * Processes the request.
```

### UtilityClassWithoutPrivateConstructor
Class `ClientTupleDeleteRequest` has only 'static' members, and lacks a 'private' constructor
in `modules/client-handler/src/main/java/org/apache/ignite/client/handler/requests/table/ClientTupleDeleteRequest.java`
#### Snippet
```java
 * Client tuple delete request.
 */
public class ClientTupleDeleteRequest {
    /**
     * Processes the request.
```

### UtilityClassWithoutPrivateConstructor
Class `ClientTableGetRequest` has only 'static' members, and lacks a 'private' constructor
in `modules/client-handler/src/main/java/org/apache/ignite/client/handler/requests/table/ClientTableGetRequest.java`
#### Snippet
```java
 * Client table retrieval request.
 */
public class ClientTableGetRequest {
    /**
     * Processes the request.
```

### UtilityClassWithoutPrivateConstructor
Class `ClientSchemasGetRequest` has only 'static' members, and lacks a 'private' constructor
in `modules/client-handler/src/main/java/org/apache/ignite/client/handler/requests/table/ClientSchemasGetRequest.java`
#### Snippet
```java
 * Client schemas retrieval request.
 */
public class ClientSchemasGetRequest {
    /**
     * Processes the request.
```

### UtilityClassWithoutPrivateConstructor
Class `ClientTupleReplaceExactRequest` has only 'static' members, and lacks a 'private' constructor
in `modules/client-handler/src/main/java/org/apache/ignite/client/handler/requests/table/ClientTupleReplaceExactRequest.java`
#### Snippet
```java
 * Client tuple replace request.
 */
public class ClientTupleReplaceExactRequest {
    /**
     * Processes the request.
```

### UtilityClassWithoutPrivateConstructor
Class `ClientTablePartitionAssignmentGetRequest` has only 'static' members, and lacks a 'private' constructor
in `modules/client-handler/src/main/java/org/apache/ignite/client/handler/requests/table/ClientTablePartitionAssignmentGetRequest.java`
#### Snippet
```java
 * Client partition assignment retrieval request.
 */
public class ClientTablePartitionAssignmentGetRequest {
    /**
     * Processes the request.
```

### UtilityClassWithoutPrivateConstructor
Class `ClientTupleInsertAllRequest` has only 'static' members, and lacks a 'private' constructor
in `modules/client-handler/src/main/java/org/apache/ignite/client/handler/requests/table/ClientTupleInsertAllRequest.java`
#### Snippet
```java
 * Client tuple insert all request.
 */
public class ClientTupleInsertAllRequest {
    /**
     * Processes the request.
```

### UtilityClassWithoutPrivateConstructor
Class `ClientTupleDeleteExactRequest` has only 'static' members, and lacks a 'private' constructor
in `modules/client-handler/src/main/java/org/apache/ignite/client/handler/requests/table/ClientTupleDeleteExactRequest.java`
#### Snippet
```java
 * Client tuple delete exact request.
 */
public class ClientTupleDeleteExactRequest {
    /**
     * Processes the request.
```

### UtilityClassWithoutPrivateConstructor
Class `ClientTupleDeleteAllExactRequest` has only 'static' members, and lacks a 'private' constructor
in `modules/client-handler/src/main/java/org/apache/ignite/client/handler/requests/table/ClientTupleDeleteAllExactRequest.java`
#### Snippet
```java
 * Client tuple delete all exact request.
 */
public class ClientTupleDeleteAllExactRequest {
    /**
     * Processes the request.
```

### UtilityClassWithoutPrivateConstructor
Class `ClientTablesGetRequest` has only 'static' members, and lacks a 'private' constructor
in `modules/client-handler/src/main/java/org/apache/ignite/client/handler/requests/table/ClientTablesGetRequest.java`
#### Snippet
```java
 * Client tables retrieval request.
 */
public class ClientTablesGetRequest {
    /**
     * Processes the request.
```

### UtilityClassWithoutPrivateConstructor
Class `ClientTupleReplaceRequest` has only 'static' members, and lacks a 'private' constructor
in `modules/client-handler/src/main/java/org/apache/ignite/client/handler/requests/table/ClientTupleReplaceRequest.java`
#### Snippet
```java
 * Client tuple replace request.
 */
public class ClientTupleReplaceRequest {
    /**
     * Processes the request.
```

### UtilityClassWithoutPrivateConstructor
Class `ClientTupleGetAndUpsertRequest` has only 'static' members, and lacks a 'private' constructor
in `modules/client-handler/src/main/java/org/apache/ignite/client/handler/requests/table/ClientTupleGetAndUpsertRequest.java`
#### Snippet
```java
 * Client tuple get and upsert request.
 */
public class ClientTupleGetAndUpsertRequest {
    /**
     * Processes the request.
```

### UtilityClassWithoutPrivateConstructor
Class `ClientTupleGetAndDeleteRequest` has only 'static' members, and lacks a 'private' constructor
in `modules/client-handler/src/main/java/org/apache/ignite/client/handler/requests/table/ClientTupleGetAndDeleteRequest.java`
#### Snippet
```java
 * Client tuple get and delete request.
 */
public class ClientTupleGetAndDeleteRequest {
    /**
     * Processes the request.
```

### UtilityClassWithoutPrivateConstructor
Class `ClientTupleGetAllRequest` has only 'static' members, and lacks a 'private' constructor
in `modules/client-handler/src/main/java/org/apache/ignite/client/handler/requests/table/ClientTupleGetAllRequest.java`
#### Snippet
```java
 * Client tuple get all request.
 */
public class ClientTupleGetAllRequest {
    /**
     * Processes the request.
```

### UtilityClassWithoutPrivateConstructor
Class `ClientTupleUpsertRequest` has only 'static' members, and lacks a 'private' constructor
in `modules/client-handler/src/main/java/org/apache/ignite/client/handler/requests/table/ClientTupleUpsertRequest.java`
#### Snippet
```java
 * Client tuple upsert request.
 */
public class ClientTupleUpsertRequest {
    /**
     * Processes the request.
```

### UtilityClassWithoutPrivateConstructor
Class `ClientTupleContainsKeyRequest` has only 'static' members, and lacks a 'private' constructor
in `modules/client-handler/src/main/java/org/apache/ignite/client/handler/requests/table/ClientTupleContainsKeyRequest.java`
#### Snippet
```java
 * Client tuple contains key request.
 */
public class ClientTupleContainsKeyRequest {
    /**
     * Processes the request.
```

### UtilityClassWithoutPrivateConstructor
Class `ClientTupleGetAndReplaceRequest` has only 'static' members, and lacks a 'private' constructor
in `modules/client-handler/src/main/java/org/apache/ignite/client/handler/requests/table/ClientTupleGetAndReplaceRequest.java`
#### Snippet
```java
 * Client tuple get and replace request.
 */
public class ClientTupleGetAndReplaceRequest {
    /**
     * Processes the request.
```

### UtilityClassWithoutPrivateConstructor
Class `ClientTupleInsertRequest` has only 'static' members, and lacks a 'private' constructor
in `modules/client-handler/src/main/java/org/apache/ignite/client/handler/requests/table/ClientTupleInsertRequest.java`
#### Snippet
```java
 * Client tuple insert request.
 */
public class ClientTupleInsertRequest {
    /**
     * Processes the request.
```

### UtilityClassWithoutPrivateConstructor
Class `ClientClusterGetNodesRequest` has only 'static' members, and lacks a 'private' constructor
in `modules/client-handler/src/main/java/org/apache/ignite/client/handler/requests/cluster/ClientClusterGetNodesRequest.java`
#### Snippet
```java
 * Cluster nodes request.
 */
public class ClientClusterGetNodesRequest {
    /**
     * Processes the request.
```

### UtilityClassWithoutPrivateConstructor
Class `ClientComputeExecuteColocatedRequest` has only 'static' members, and lacks a 'private' constructor
in `modules/client-handler/src/main/java/org/apache/ignite/client/handler/requests/compute/ClientComputeExecuteColocatedRequest.java`
#### Snippet
```java
 * Compute execute colocated request.
 */
public class ClientComputeExecuteColocatedRequest {
    /**
     * Processes the request.
```

### UtilityClassWithoutPrivateConstructor
Class `ClientComputeExecuteRequest` has only 'static' members, and lacks a 'private' constructor
in `modules/client-handler/src/main/java/org/apache/ignite/client/handler/requests/compute/ClientComputeExecuteRequest.java`
#### Snippet
```java
 * Compute execute request.
 */
public class ClientComputeExecuteRequest {
    /**
     * Processes the request.
```

### UtilityClassWithoutPrivateConstructor
Class `ClientTableCommon` has only 'static' members, and lacks a 'private' constructor
in `modules/client-handler/src/main/java/org/apache/ignite/client/handler/requests/table/ClientTableCommon.java`
#### Snippet
```java
 * Common table functionality.
 */
public class ClientTableCommon {
    /**
     * Writes a schema.
```

### UtilityClassWithoutPrivateConstructor
Class `RocksUtils` has only 'static' members, and lacks a 'private' constructor
in `modules/rocksdb-common/src/main/java/org/apache/ignite/internal/rocksdb/RocksUtils.java`
#### Snippet
```java
 * RocksDB utility functions.
 */
public class RocksUtils {
    /**
     * Iterates over the given iterator passing key-value pairs to the given consumer and checks the iterator's status afterwards.
```

### UtilityClassWithoutPrivateConstructor
Class `DeployUnitMessageTypes` has only 'static' members, and lacks a 'private' constructor
in `modules/code-deployment/src/main/java/org/apache/ignite/internal/deployunit/message/DeployUnitMessageTypes.java`
#### Snippet
```java
 */
@MessageGroup(groupType = 10, groupName = "DeploymentUnit")
public class DeployUnitMessageTypes {
    /**
     * Message type for {@link DeployUnitRequest}.
```

### UtilityClassWithoutPrivateConstructor
Class `ColumnFamilyUtils` has only 'static' members, and lacks a 'private' constructor
in `modules/storage-rocksdb/src/main/java/org/apache/ignite/internal/storage/rocksdb/ColumnFamilyUtils.java`
#### Snippet
```java
 * Utilities for converting partition IDs and index names into Column Family names and vice versa.
 */
class ColumnFamilyUtils {
    /** Name of the meta column family matches default columns family, meaning that it always exist when new table is created. */
    static final String META_CF_NAME = new String(RocksDB.DEFAULT_COLUMN_FAMILY, StandardCharsets.UTF_8);
```

### UtilityClassWithoutPrivateConstructor
Class `ValidatorChecker` has only 'static' members, and lacks a 'private' constructor
in `modules/configuration-api/src/main/java/org/apache/ignite/configuration/validation/ValidatorChecker.java`
#### Snippet
```java
 * Helper class for the default {@link Validator#canValidate(Class, Class, boolean)} implementation.
 */
class ValidatorChecker {
    /**
     * Pattern for configuration schema class names. Used to replace the suffix with something else.
```

### UtilityClassWithoutPrivateConstructor
Class `CmgMessageGroup` has only 'static' members, and lacks a 'private' constructor
in `modules/cluster-management/src/main/java/org/apache/ignite/internal/cluster/management/network/messages/CmgMessageGroup.java`
#### Snippet
```java
 */
@MessageGroup(groupType = 7, groupName = "CmgMessages")
public class CmgMessageGroup {
    /**
     * Message type for {@link CmgInitMessage}.
```

### UtilityClassWithoutPrivateConstructor
Class `DistributionZonesUtil` has only 'static' members, and lacks a 'private' constructor
in `modules/distribution-zones/src/main/java/org/apache/ignite/internal/distributionzones/DistributionZonesUtil.java`
#### Snippet
```java
 * Util class for Distribution Zones flow.
 */
public class DistributionZonesUtil {
    /** Key prefix for zone's data nodes. */
    private static final String DISTRIBUTION_ZONE_DATA_NODES_PREFIX = "distributionZone.dataNodes.";
```

### UtilityClassWithoutPrivateConstructor
Class `HybridTimestamps` has only 'static' members, and lacks a 'private' constructor
in `modules/storage-page-memory/src/main/java/org/apache/ignite/internal/storage/pagememory/mv/HybridTimestamps.java`
#### Snippet
```java
 * Code to work with {@link HybridTimestamp}s.
 */
public class HybridTimestamps {
    /** Physical time component to store for {@code null} hybrid timestamp values. */
    private static final long NULL_PHYSICAL_TIME = 0L;
```

### UtilityClassWithoutPrivateConstructor
Class `InlineUtils` has only 'static' members, and lacks a 'private' constructor
in `modules/storage-page-memory/src/main/java/org/apache/ignite/internal/storage/pagememory/index/InlineUtils.java`
#### Snippet
```java
 * {@link FreeList}.
 */
public class InlineUtils {
    /** Maximum inline size for a {@link BinaryTuple}, in bytes. */
    public static final int MAX_BINARY_TUPLE_INLINE_SIZE = 2 * KiB;
```

### UtilityClassWithoutPrivateConstructor
Class `ConfigurationProcessorUtils` has only 'static' members, and lacks a 'private' constructor
in `modules/configuration-annotation-processor/src/main/java/org/apache/ignite/internal/configuration/processor/ConfigurationProcessorUtils.java`
#### Snippet
```java
 * Annotation processing utilities.
 */
class ConfigurationProcessorUtils {
    /**
     * Returns {@link ClassName} for configuration class public interface.
```

### UtilityClassWithoutPrivateConstructor
Class `TransactionsExample` has only 'static' members, and lacks a 'private' constructor
in `examples/src/main/java/org/apache/ignite/example/tx/TransactionsExample.java`
#### Snippet
```java
 * </ol>
 */
public class TransactionsExample {
    /**
     * Main method of the example.
```

### UtilityClassWithoutPrivateConstructor
Class `KeyValueViewExample` has only 'static' members, and lacks a 'private' constructor
in `examples/src/main/java/org/apache/ignite/example/table/KeyValueViewExample.java`
#### Snippet
```java
 * </ol>
 */
public class KeyValueViewExample {
    /**
     * Main method of the example.
```

### UtilityClassWithoutPrivateConstructor
Class `RecordViewPojoExample` has only 'static' members, and lacks a 'private' constructor
in `examples/src/main/java/org/apache/ignite/example/table/RecordViewPojoExample.java`
#### Snippet
```java
 * </ol>
 */
public class RecordViewPojoExample {
    /**
     * Main method of the example.
```

### UtilityClassWithoutPrivateConstructor
Class `SqlApiExample` has only 'static' members, and lacks a 'private' constructor
in `examples/src/main/java/org/apache/ignite/example/sql/SqlApiExample.java`
#### Snippet
```java
 * Examples of using SQL API.
 */
public class SqlApiExample {
    /**
     * Main method of the example.
```

### UtilityClassWithoutPrivateConstructor
Class `SqlJdbcExample` has only 'static' members, and lacks a 'private' constructor
in `examples/src/main/java/org/apache/ignite/example/sql/jdbc/SqlJdbcExample.java`
#### Snippet
```java
 * </ol>
 */
public class SqlJdbcExample {
    /**
     * Main method of the example.
```

### UtilityClassWithoutPrivateConstructor
Class `RocksDbStorageExample` has only 'static' members, and lacks a 'private' constructor
in `examples/src/main/java/org/apache/ignite/example/storage/RocksDbStorageExample.java`
#### Snippet
```java
 * </ol>
 */
public class RocksDbStorageExample {
    /**
     * Main method of the example.
```

### UtilityClassWithoutPrivateConstructor
Class `KeyValueViewPojoExample` has only 'static' members, and lacks a 'private' constructor
in `examples/src/main/java/org/apache/ignite/example/table/KeyValueViewPojoExample.java`
#### Snippet
```java
 * </ol>
 */
public class KeyValueViewPojoExample {
    /**
     * Main method of the example.
```

### UtilityClassWithoutPrivateConstructor
Class `RecordViewExample` has only 'static' members, and lacks a 'private' constructor
in `examples/src/main/java/org/apache/ignite/example/table/RecordViewExample.java`
#### Snippet
```java
 * </ol>
 */
public class RecordViewExample {
    /**
     * Main method of the example.
```

### UtilityClassWithoutPrivateConstructor
Class `PersistentPageMemoryStorageExample` has only 'static' members, and lacks a 'private' constructor
in `examples/src/main/java/org/apache/ignite/example/storage/PersistentPageMemoryStorageExample.java`
#### Snippet
```java
 * </ol>
 */
public class PersistentPageMemoryStorageExample {
    /**
     * Main method of the example.
```

### UtilityClassWithoutPrivateConstructor
Class `VolatilePageMemoryStorageExample` has only 'static' members, and lacks a 'private' constructor
in `examples/src/main/java/org/apache/ignite/example/storage/VolatilePageMemoryStorageExample.java`
#### Snippet
```java
 * </ol>
 */
public class VolatilePageMemoryStorageExample {
    /**
     * Main method of the example.
```

### UtilityClassWithoutPrivateConstructor
Class `ConfigurationNotifier` has only 'static' members, and lacks a 'private' constructor
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/notifications/ConfigurationNotifier.java`
#### Snippet
```java
 * Class for notifying configuration listeners.
 */
public class ConfigurationNotifier {
    /**
     * Recursive notification of all configuration listeners.
```

## RuleId[id=DataFlowIssue]
### DataFlowIssue
Dereference of `cliVerInfo` may produce `NullPointerException`
in `modules/cli/src/main/java/org/apache/ignite/internal/cli/VersionProvider.java`
#### Snippet
```java
    @Override
    public String[] getVersion() {
        return new String[]{"Apache Ignite CLI ver. " + cliVerInfo.ver};
    }
}
```

### DataFlowIssue
Condition `e.getCause() instanceof ApiException` is redundant and can be replaced with a null check
in `modules/cli/src/main/java/org/apache/ignite/internal/cli/core/exception/handler/ClusterNotInitializedExceptionHandler.java`
#### Snippet
```java
    @Override
    public int handle(ExceptionWriter err, IgniteCliApiException e) {
        if (e.getCause() instanceof ApiException) {
            ApiException apiException = (ApiException) e.getCause();
            if (apiException.getCode() == 404) {
```

### DataFlowIssue
Condition `e.getCause() instanceof ApiException` is redundant and can be replaced with a null check
in `modules/cli/src/main/java/org/apache/ignite/internal/cli/core/exception/handler/IgniteCliApiExceptionHandler.java`
#### Snippet
```java
        ErrorComponentBuilder errorComponentBuilder = ErrorUiComponent.builder();

        if (e.getCause() instanceof ApiException) {
            ApiException cause = (ApiException) e.getCause();
            Throwable apiCause = cause.getCause();
```

### DataFlowIssue
Argument `resourceAsStream` might be null
in `modules/cli/src/testFixtures/java/org/apache/ignite/internal/cli/commands/cliconfig/TestConfigManagerHelper.java`
#### Snippet
```java
                FileChannel dest = fileOutputStream.getChannel();
                InputStream resourceAsStream = TestConfigManagerHelper.class.getClassLoader().getResourceAsStream(resource);
                ReadableByteChannel src = Channels.newChannel(resourceAsStream);
                dest.transferFrom(src, 0, Integer.MAX_VALUE);
                tempFile.deleteOnExit();
```

### DataFlowIssue
Method invocation `addTicks` may produce `NullPointerException`
in `modules/core/src/main/java/org/apache/ignite/internal/hlc/HybridClockImpl.java`
#### Snippet
```java
            HybridTimestamp maxLatestTime = HybridTimestamp.max(now, requestTime, latestTime);

            HybridTimestamp newLatestTime = maxLatestTime.addTicks(1);

            if (LATEST_TIME.compareAndSet(this, latestTime, newLatestTime)) {
```

### DataFlowIssue
Dereference of `k` may produce `NullPointerException`
in `modules/core/src/main/java/org/apache/ignite/internal/util/IgniteConcurrentMultiPairQueue.java`
#### Snippet
```java
     */
    public IgniteConcurrentMultiPairQueue(Collection<IgniteBiTuple<K, V[]>> items) {
        int pairCnt = (int) items.stream().map(Map.Entry::getValue).filter(k -> k.length > 0).count();

        vals = (V[][]) new Object[pairCnt][];
```

### DataFlowIssue
Method invocation `addTicks` may produce `NullPointerException`
in `modules/core/src/testFixtures/java/org/apache/ignite/internal/TestHybridClock.java`
#### Snippet
```java
            HybridTimestamp now = new HybridTimestamp(currentTimeMillisSupplier.getAsLong(), -1);

            HybridTimestamp newLatestTime = HybridTimestamp.max(now, requestTime, latestTime).addTicks(1);

            if (LATEST_TIME.compareAndSet(this, latestTime, newLatestTime)) {
```

### DataFlowIssue
Method invocation `toUpperCase` may produce `NullPointerException`
in `modules/core/src/testFixtures/java/org/apache/ignite/internal/testframework/BaseIgniteAbstractTest.java`
#### Snippet
```java
    static {
        S.setSensitiveDataLoggingPolicySupplier(() ->
                SensitiveDataLoggingPolicy.valueOf(getString(IGNITE_SENSITIVE_DATA_LOGGING, "hash").toUpperCase()));
    }

```

### DataFlowIssue
Variable is already assigned to this value
in `modules/core/src/main/java/org/apache/ignite/internal/util/GridUnsafe.java`
#### Snippet
```java
            } catch (Exception e) {
                nioAccessObj = null;
                directBufMtd = null;

                try {
```

### DataFlowIssue
Unboxing of `connTimeout.value()` may produce `NullPointerException`
in `modules/jdbc/src/main/java/org/apache/ignite/internal/jdbc/ConnectionPropertiesImpl.java`
#### Snippet
```java
    @Override
    public int getConnectionTimeout() {
        return connTimeout.value();
    }

```

### DataFlowIssue
Method invocation `term` may produce `NullPointerException`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/core/Replicator.java`
#### Snippet
```java
            }
            r.consecutiveErrorTimes = 0;
            if (response.term() > r.options.getTerm()) {
                if (isLogDebugEnabled) {
                    sb.append(" fail, greater term ") //
```

### DataFlowIssue
Method invocation `toUpperCase` may produce `NullPointerException`
in `modules/core/src/main/java/org/apache/ignite/internal/tostring/IgniteToStringBuilder.java`
#### Snippet
```java
                /** Sensitive data logging policy. */
                final SensitiveDataLoggingPolicy sensitiveDataLoggingPolicy =
                        SensitiveDataLoggingPolicy.valueOf(getString(IGNITE_SENSITIVE_DATA_LOGGING, "hash").toUpperCase());

                /** {@inheritDoc} */
```

### DataFlowIssue
Method reference argument might be null
in `modules/rest/src/main/java/org/apache/ignite/internal/rest/authentication/DelegatingAuthenticationProvider.java`
#### Snippet
```java
        return providers.namedListKeys().stream()
                .map(providers::get)
                .map(AuthenticatorFactory::create)
                .collect(Collectors.toList());
    }
```

### DataFlowIssue
Method invocation `keyColumns` may produce `NullPointerException`
in `modules/table/src/main/java/org/apache/ignite/internal/table/TableRow.java`
#### Snippet
```java
        @Override
        public int columnCount() {
            return tuple != null ? tuple.columnCount() : schema().keyColumns().length();
        }

```

### DataFlowIssue
Method invocation `isKeyColumn` may produce `NullPointerException`
in `modules/table/src/main/java/org/apache/ignite/internal/table/TableRow.java`
#### Snippet
```java
            final Column col = super.rowColumnByName(columnName);

            if (schema().isKeyColumn(col.schemaIndex())) {
                throw new IllegalArgumentException("Invalid column name: columnName=" + columnName);
            }
```

### DataFlowIssue
Method invocation `keyColumns` may produce `NullPointerException`
in `modules/table/src/main/java/org/apache/ignite/internal/table/TableRow.java`
#### Snippet
```java
        @Override
        protected Column rowColumnByIndex(int columnIndex) {
            Objects.checkIndex(columnIndex, schema().keyColumns().length());

            return schema().column(columnIndex);
```

### DataFlowIssue
Method invocation `column` may produce `NullPointerException`
in `modules/table/src/main/java/org/apache/ignite/internal/table/TableRow.java`
#### Snippet
```java
            Objects.checkIndex(columnIndex, schema().keyColumns().length());

            return schema().column(columnIndex);
        }
    }
```

### DataFlowIssue
Method invocation `column` may produce `NullPointerException`
in `modules/table/src/main/java/org/apache/ignite/internal/table/TableRow.java`
#### Snippet
```java
            Objects.requireNonNull(columnName);

            var col = schema().column(columnName);

            return col == null || !schema().isKeyColumn(col.schemaIndex()) ? -1 : col.schemaIndex();
```

### DataFlowIssue
Method invocation `isKeyColumn` may produce `NullPointerException`
in `modules/table/src/main/java/org/apache/ignite/internal/table/TableRow.java`
#### Snippet
```java
            var col = schema().column(columnName);

            return col == null || !schema().isKeyColumn(col.schemaIndex()) ? -1 : col.schemaIndex();
        }

```

### DataFlowIssue
Method invocation `valueColumns` may produce `NullPointerException`
in `modules/table/src/main/java/org/apache/ignite/internal/table/TableRow.java`
#### Snippet
```java
        @Override
        protected Column rowColumnByIndex(int columnIndex) {
            Objects.checkIndex(columnIndex, schema().valueColumns().length());

            return schema().column(columnIndex + schema().keyColumns().length());
```

### DataFlowIssue
Method invocation `column` may produce `NullPointerException`
in `modules/table/src/main/java/org/apache/ignite/internal/table/TableRow.java`
#### Snippet
```java
            Objects.checkIndex(columnIndex, schema().valueColumns().length());

            return schema().column(columnIndex + schema().keyColumns().length());
        }
    }
```

### DataFlowIssue
Method invocation `keyColumns` may produce `NullPointerException`
in `modules/table/src/main/java/org/apache/ignite/internal/table/TableRow.java`
#### Snippet
```java
            Objects.checkIndex(columnIndex, schema().valueColumns().length());

            return schema().column(columnIndex + schema().keyColumns().length());
        }
    }
```

### DataFlowIssue
Method invocation `column` may produce `NullPointerException`
in `modules/table/src/main/java/org/apache/ignite/internal/table/TableRow.java`
#### Snippet
```java
            Objects.requireNonNull(columnName);

            var col = schema().column(columnName);

            return col == null || schema().isKeyColumn(col.schemaIndex()) ? -1 :
```

### DataFlowIssue
Method invocation `isKeyColumn` may produce `NullPointerException`
in `modules/table/src/main/java/org/apache/ignite/internal/table/TableRow.java`
#### Snippet
```java
            var col = schema().column(columnName);

            return col == null || schema().isKeyColumn(col.schemaIndex()) ? -1 :
                    col.schemaIndex() - schema().keyColumns().length();
        }
```

### DataFlowIssue
Method invocation `keyColumns` may produce `NullPointerException`
in `modules/table/src/main/java/org/apache/ignite/internal/table/TableRow.java`
#### Snippet
```java

            return col == null || schema().isKeyColumn(col.schemaIndex()) ? -1 :
                    col.schemaIndex() - schema().keyColumns().length();
        }

```

### DataFlowIssue
Method invocation `valueColumns` may produce `NullPointerException`
in `modules/table/src/main/java/org/apache/ignite/internal/table/TableRow.java`
#### Snippet
```java
        @Override
        public int columnCount() {
            return tuple != null ? tuple.columnCount() : schema().valueColumns().length();
        }

```

### DataFlowIssue
Method invocation `isKeyColumn` may produce `NullPointerException`
in `modules/table/src/main/java/org/apache/ignite/internal/table/TableRow.java`
#### Snippet
```java
            final Column col = super.rowColumnByName(columnName);

            if (!schema().isKeyColumn(col.schemaIndex())) {
                throw new IllegalArgumentException("Invalid column name: columnName=" + columnName);
            }
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `modules/table/src/main/java/org/apache/ignite/internal/table/InternalTable.java`
#### Snippet
```java
            @NotNull ClusterNode recipientNode
    ) {
        return scan(partId, readTimestamp, recipientNode, null, null, null, 0, null);
    }

```

### DataFlowIssue
Method invocation `stringValue` may produce `NullPointerException`
in `modules/table/src/main/java/org/apache/ignite/internal/table/AbstractRowTupleAdapter.java`
#### Snippet
```java
        Column col = rowColumnByIndex(columnIndex);

        return row.stringValue(col.schemaIndex());
    }

```

### DataFlowIssue
Method invocation `shortValue` may produce `NullPointerException`
in `modules/table/src/main/java/org/apache/ignite/internal/table/AbstractRowTupleAdapter.java`
#### Snippet
```java
        Column col = rowColumnByName(columnName);

        return row.shortValue(col.schemaIndex());
    }

```

### DataFlowIssue
Method invocation `timeValue` may produce `NullPointerException`
in `modules/table/src/main/java/org/apache/ignite/internal/table/AbstractRowTupleAdapter.java`
#### Snippet
```java
        Column col = rowColumnByName(columnName);

        return row.timeValue(col.schemaIndex());
    }

```

### DataFlowIssue
Method invocation `intValue` may produce `NullPointerException`
in `modules/table/src/main/java/org/apache/ignite/internal/table/AbstractRowTupleAdapter.java`
#### Snippet
```java
        Column col = rowColumnByName(columnName);

        return row.intValue(col.schemaIndex());
    }

```

### DataFlowIssue
Method invocation `schema` may produce `NullPointerException`
in `modules/table/src/main/java/org/apache/ignite/internal/table/AbstractRowTupleAdapter.java`
#### Snippet
```java
        Objects.requireNonNull(columnName);

        var col = row.schema().column(IgniteNameUtils.parseSimpleName(columnName));

        return col == null ? -1 : col.schemaIndex();
```

### DataFlowIssue
Method invocation `uuidValue` may produce `NullPointerException`
in `modules/table/src/main/java/org/apache/ignite/internal/table/AbstractRowTupleAdapter.java`
#### Snippet
```java
        Column col = rowColumnByIndex(columnIndex);

        return row.uuidValue(col.schemaIndex());
    }

```

### DataFlowIssue
Method invocation `schema` may produce `NullPointerException`
in `modules/table/src/main/java/org/apache/ignite/internal/table/AbstractRowTupleAdapter.java`
#### Snippet
```java
            @Override
            public boolean hasNext() {
                return cur < row.schema().length();
            }

```

### DataFlowIssue
Method invocation `dateValue` may produce `NullPointerException`
in `modules/table/src/main/java/org/apache/ignite/internal/table/AbstractRowTupleAdapter.java`
#### Snippet
```java
        Column col = rowColumnByName(columnName);

        return row.dateValue(col.schemaIndex());
    }

```

### DataFlowIssue
Method invocation `bitmaskValue` may produce `NullPointerException`
in `modules/table/src/main/java/org/apache/ignite/internal/table/AbstractRowTupleAdapter.java`
#### Snippet
```java
        Column col = rowColumnByName(columnName);

        return row.bitmaskValue(col.schemaIndex());
    }

```

### DataFlowIssue
Method invocation `uuidValue` may produce `NullPointerException`
in `modules/table/src/main/java/org/apache/ignite/internal/table/AbstractRowTupleAdapter.java`
#### Snippet
```java
        Column col = rowColumnByName(columnName);

        return row.uuidValue(col.schemaIndex());
    }

```

### DataFlowIssue
Method invocation `floatValue` may produce `NullPointerException`
in `modules/table/src/main/java/org/apache/ignite/internal/table/AbstractRowTupleAdapter.java`
#### Snippet
```java
        Column col = rowColumnByIndex(columnIndex);

        return row.floatValue(col.schemaIndex());
    }

```

### DataFlowIssue
Method invocation `schema` may produce `NullPointerException`
in `modules/table/src/main/java/org/apache/ignite/internal/table/AbstractRowTupleAdapter.java`
#### Snippet
```java
        Objects.requireNonNull(columnName);

        final Column col = row.schema().column(IgniteNameUtils.parseSimpleName(columnName));

        return col == null ? defaultValue : (T) col.type().spec().objectValue(row, col.schemaIndex());
```

### DataFlowIssue
Method invocation `floatValue` may produce `NullPointerException`
in `modules/table/src/main/java/org/apache/ignite/internal/table/AbstractRowTupleAdapter.java`
#### Snippet
```java
        Column col = rowColumnByName(columnName);

        return row.floatValue(col.schemaIndex());
    }

```

### DataFlowIssue
Method invocation `longValue` may produce `NullPointerException`
in `modules/table/src/main/java/org/apache/ignite/internal/table/AbstractRowTupleAdapter.java`
#### Snippet
```java
        Column col = rowColumnByIndex(columnIndex);

        return row.longValue(col.schemaIndex());
    }

```

### DataFlowIssue
Method invocation `bytesValue` may produce `NullPointerException`
in `modules/table/src/main/java/org/apache/ignite/internal/table/AbstractRowTupleAdapter.java`
#### Snippet
```java
        Column col = rowColumnByIndex(columnIndex);

        return BinaryObjects.wrap(row.bytesValue(col.schemaIndex()));
    }

```

### DataFlowIssue
Method invocation `byteValue` may produce `NullPointerException`
in `modules/table/src/main/java/org/apache/ignite/internal/table/AbstractRowTupleAdapter.java`
#### Snippet
```java
        Column col = rowColumnByName(columnName);

        return row.byteValue(col.schemaIndex());
    }

```

### DataFlowIssue
Method invocation `schema` may produce `NullPointerException`
in `modules/table/src/main/java/org/apache/ignite/internal/table/AbstractRowTupleAdapter.java`
#### Snippet
```java
    @Override
    public SchemaDescriptor schema() {
        return row.schema();
    }

```

### DataFlowIssue
Method invocation `doubleValue` may produce `NullPointerException`
in `modules/table/src/main/java/org/apache/ignite/internal/table/AbstractRowTupleAdapter.java`
#### Snippet
```java
        Column col = rowColumnByIndex(columnIndex);

        return row.doubleValue(col.schemaIndex());
    }

```

### DataFlowIssue
Method invocation `dateTimeValue` may produce `NullPointerException`
in `modules/table/src/main/java/org/apache/ignite/internal/table/AbstractRowTupleAdapter.java`
#### Snippet
```java
        Column col = rowColumnByName(columnName);

        return row.dateTimeValue(col.schemaIndex());
    }

```

### DataFlowIssue
Method invocation `shortValue` may produce `NullPointerException`
in `modules/table/src/main/java/org/apache/ignite/internal/table/AbstractRowTupleAdapter.java`
#### Snippet
```java
        Column col = rowColumnByIndex(columnIndex);

        return row.shortValue(col.schemaIndex());
    }

```

### DataFlowIssue
Method invocation `timestampValue` may produce `NullPointerException`
in `modules/table/src/main/java/org/apache/ignite/internal/table/AbstractRowTupleAdapter.java`
#### Snippet
```java
        Column col = rowColumnByName(columnName);

        return row.timestampValue(col.schemaIndex());
    }

```

### DataFlowIssue
Method invocation `schema` may produce `NullPointerException`
in `modules/table/src/main/java/org/apache/ignite/internal/table/AbstractRowTupleAdapter.java`
#### Snippet
```java
        Objects.requireNonNull(columnName);

        final Column col = row.schema().column(IgniteNameUtils.parseSimpleName(columnName));

        if (col == null) {
```

### DataFlowIssue
Method invocation `stringValue` may produce `NullPointerException`
in `modules/table/src/main/java/org/apache/ignite/internal/table/AbstractRowTupleAdapter.java`
#### Snippet
```java
        Column col = rowColumnByName(columnName);

        return row.stringValue(col.schemaIndex());
    }

```

### DataFlowIssue
Method invocation `dateTimeValue` may produce `NullPointerException`
in `modules/table/src/main/java/org/apache/ignite/internal/table/AbstractRowTupleAdapter.java`
#### Snippet
```java
        Column col = rowColumnByIndex(columnIndex);

        return row.dateTimeValue(col.schemaIndex());
    }

```

### DataFlowIssue
Method invocation `timeValue` may produce `NullPointerException`
in `modules/table/src/main/java/org/apache/ignite/internal/table/AbstractRowTupleAdapter.java`
#### Snippet
```java
        Column col = rowColumnByIndex(columnIndex);

        return row.timeValue(col.schemaIndex());
    }

```

### DataFlowIssue
Method invocation `doubleValue` may produce `NullPointerException`
in `modules/table/src/main/java/org/apache/ignite/internal/table/AbstractRowTupleAdapter.java`
#### Snippet
```java
        Column col = rowColumnByName(columnName);

        return row.doubleValue(col.schemaIndex());
    }

```

### DataFlowIssue
Method invocation `timestampValue` may produce `NullPointerException`
in `modules/table/src/main/java/org/apache/ignite/internal/table/AbstractRowTupleAdapter.java`
#### Snippet
```java
        Column col = rowColumnByIndex(columnIndex);

        return row.timestampValue(col.schemaIndex());
    }

```

### DataFlowIssue
Method invocation `bitmaskValue` may produce `NullPointerException`
in `modules/table/src/main/java/org/apache/ignite/internal/table/AbstractRowTupleAdapter.java`
#### Snippet
```java
        Column col = rowColumnByIndex(columnIndex);

        return row.bitmaskValue(col.schemaIndex());
    }

```

### DataFlowIssue
Method invocation `bytesValue` may produce `NullPointerException`
in `modules/table/src/main/java/org/apache/ignite/internal/table/AbstractRowTupleAdapter.java`
#### Snippet
```java
        Column col = rowColumnByName(columnName);

        return BinaryObjects.wrap(row.bytesValue(col.schemaIndex()));
    }

```

### DataFlowIssue
Method invocation `schema` may produce `NullPointerException`
in `modules/table/src/main/java/org/apache/ignite/internal/table/AbstractRowTupleAdapter.java`
#### Snippet
```java
     */
    protected Column rowColumnByIndex(int columnIndex) {
        Objects.checkIndex(columnIndex, row.schema().length());

        return row.schema().column(columnIndex);
```

### DataFlowIssue
Method invocation `dateValue` may produce `NullPointerException`
in `modules/table/src/main/java/org/apache/ignite/internal/table/AbstractRowTupleAdapter.java`
#### Snippet
```java
        Column col = rowColumnByIndex(columnIndex);

        return row.dateValue(col.schemaIndex());
    }

```

### DataFlowIssue
Method invocation `longValue` may produce `NullPointerException`
in `modules/table/src/main/java/org/apache/ignite/internal/table/AbstractRowTupleAdapter.java`
#### Snippet
```java
        Column col = rowColumnByName(columnName);

        return row.longValue(col.schemaIndex());
    }

```

### DataFlowIssue
Method invocation `intValue` may produce `NullPointerException`
in `modules/table/src/main/java/org/apache/ignite/internal/table/AbstractRowTupleAdapter.java`
#### Snippet
```java
        Column col = rowColumnByIndex(columnIndex);

        return row.intValue(col.schemaIndex());
    }

```

### DataFlowIssue
Method invocation `byteValue` may produce `NullPointerException`
in `modules/table/src/main/java/org/apache/ignite/internal/table/AbstractRowTupleAdapter.java`
#### Snippet
```java
        Column col = rowColumnByIndex(columnIndex);

        return row.byteValue(col.schemaIndex());
    }

```

### DataFlowIssue
Method invocation `hasValue` may produce `NullPointerException`
in `modules/table/src/main/java/org/apache/ignite/internal/table/AbstractRowTupleAdapter.java`
#### Snippet
```java
    @Override
    public int columnCount() {
        return row.hasValue() ? row.schema().length() : row.schema().keyColumns().length();
    }

```

### DataFlowIssue
Method invocation `id` may produce `NullPointerException`
in `modules/index/src/main/java/org/apache/ignite/internal/index/IndexManager.java`
#### Snippet
```java
     */
    private CompletableFuture<?> onIndexDrop(ConfigurationNotificationEvent<TableIndexView> evt) {
        UUID idxId = evt.oldValue().id();

        if (!busyLock.enterBusy()) {
```

### DataFlowIssue
Method invocation `tableId` may produce `NullPointerException`
in `modules/index/src/main/java/org/apache/ignite/internal/index/IndexManager.java`
#### Snippet
```java

        try {
            return tableManager.tableAsync(evt.storageRevision(), evt.oldValue().tableId())
                    .thenAccept(table -> {
                        if (table != null) { // in case of DROP TABLE the table will be removed first
```

### DataFlowIssue
Method invocation `id` may produce `NullPointerException`
in `modules/index/src/main/java/org/apache/ignite/internal/index/IndexManager.java`
#### Snippet
```java
    private CompletableFuture<?> onIndexCreate(ConfigurationNotificationEvent<TableIndexView> evt) {
        if (!busyLock.enterBusy()) {
            UUID idxId = evt.newValue().id();

            fireEvent(IndexEvent.CREATE,
```

### DataFlowIssue
Method invocation `tableId` may produce `NullPointerException`
in `modules/index/src/main/java/org/apache/ignite/internal/index/IndexManager.java`
#### Snippet
```java

        try {
            UUID tableId = evt.newValue().tableId();

            return createIndexLocally(
```

### DataFlowIssue
Argument `evt.newValue()` might be null
in `modules/index/src/main/java/org/apache/ignite/internal/index/IndexManager.java`
#### Snippet
```java
                    evt.storageRevision(),
                    tableId,
                    evt.newValue());
        } finally {
            busyLock.leaveBusy();
```

### DataFlowIssue
Method invocation `indexColumns` may produce `NullPointerException`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/SortedIndexLocker.java`
#### Snippet
```java
            IndexRow nextRow = peekCursor.peek();

            return lockManager.acquire(txId, new LockKey(indexId, nextRow.indexColumns().byteBuffer()), LockMode.S)
                    .thenCompose(ignore -> {
                        if (!peekCursor.hasNext() || !nextRow.rowId().equals(peekCursor.peek().rowId())) {
```

### DataFlowIssue
Method invocation `rowId` may produce `NullPointerException`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/SortedIndexLocker.java`
#### Snippet
```java
            return lockManager.acquire(txId, new LockKey(indexId, nextRow.indexColumns().byteBuffer()), LockMode.S)
                    .thenCompose(ignore -> {
                        if (!peekCursor.hasNext() || !nextRow.rowId().equals(peekCursor.peek().rowId())) {
                            lockManager.release(txId, new LockKey(indexId, nextRow.indexColumns().byteBuffer()), LockMode.S);

```

### DataFlowIssue
Method invocation `peersList` may produce `NullPointerException`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/raft/snapshot/incoming/IncomingSnapshotCopier.java`
#### Snippet
```java

            RaftGroupConfiguration raftGroupConfig = new RaftGroupConfiguration(
                    meta.peersList(),
                    meta.learnersList(),
                    meta.oldPeersList(),
```

### DataFlowIssue
Method invocation `handle` may produce `NullPointerException`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/raft/snapshot/incoming/IncomingSnapshotCopier.java`
#### Snippet
```java
                });

        joinFuture = rebalanceFuture.handle((unused, throwable) -> completeRebalance(throwable)).thenCompose(Function.identity());
    }

```

### DataFlowIssue
Method invocation `id` may produce `NullPointerException`
in `modules/table/src/testFixtures/java/org/apache/ignite/internal/table/impl/DummyInternalTableImpl.java`
#### Snippet
```java
        PendingComparableValuesTracker<HybridTimestamp> safeTime = new PendingComparableValuesTracker<>(new HybridTimestamp(1, 0));
        PartitionDataStorage partitionDataStorage = new TestPartitionDataStorage(mvPartStorage);
        Supplier<Map<UUID, TableSchemaAwareIndexStorage>> indexes = () -> Map.of(pkStorage.get().id(), pkStorage.get());

        DataStorageConfiguration dsCfg = mock(DataStorageConfiguration.class);
```

### DataFlowIssue
Argument `pkStorage.get()` might be null
in `modules/table/src/testFixtures/java/org/apache/ignite/internal/table/impl/DummyInternalTableImpl.java`
#### Snippet
```java
        PendingComparableValuesTracker<HybridTimestamp> safeTime = new PendingComparableValuesTracker<>(new HybridTimestamp(1, 0));
        PartitionDataStorage partitionDataStorage = new TestPartitionDataStorage(mvPartStorage);
        Supplier<Map<UUID, TableSchemaAwareIndexStorage>> indexes = () -> Map.of(pkStorage.get().id(), pkStorage.get());

        DataStorageConfiguration dsCfg = mock(DataStorageConfiguration.class);
```

### DataFlowIssue
`null` is returned by the method declared as @NotNull
in `modules/table/src/testFixtures/java/org/apache/ignite/internal/table/impl/DummyInternalTableImpl.java`
#### Snippet
```java
    @Override
    public @NotNull String name() {
        return null;
    }

```

### DataFlowIssue
Method invocation `name` may produce `NullPointerException`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/TableManager.java`
#### Snippet
```java
    private CompletableFuture<?> onTableDelete(ConfigurationNotificationEvent<TableView> ctx) {
        if (!busyLock.enterBusy()) {
            String tblName = ctx.oldValue().name();
            UUID tblId = ((ExtendedTableView) ctx.oldValue()).id();

```

### DataFlowIssue
Method invocation `id` may produce `NullPointerException`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/TableManager.java`
#### Snippet
```java
        if (!busyLock.enterBusy()) {
            String tblName = ctx.oldValue().name();
            UUID tblId = ((ExtendedTableView) ctx.oldValue()).id();

            fireEvent(
```

### DataFlowIssue
Method invocation `name` may produce `NullPointerException`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/TableManager.java`
#### Snippet
```java
            dropTableLocally(
                    ctx.storageRevision(),
                    ctx.oldValue().name(),
                    ((ExtendedTableView) ctx.oldValue()).id(),
                    ByteUtils.fromBytes(((ExtendedTableView) ctx.oldValue()).assignments())
```

### DataFlowIssue
Method invocation `id` may produce `NullPointerException`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/TableManager.java`
#### Snippet
```java
                    ctx.storageRevision(),
                    ctx.oldValue().name(),
                    ((ExtendedTableView) ctx.oldValue()).id(),
                    ByteUtils.fromBytes(((ExtendedTableView) ctx.oldValue()).assignments())
            );
```

### DataFlowIssue
Method invocation `assignments` may produce `NullPointerException`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/TableManager.java`
#### Snippet
```java
                            TableConfiguration tableCfg = tables.get(tableView.name());

                            byte[] assignmentsBytes = ((ExtendedTableConfiguration) tableCfg).assignments().value();

                            List<Set<Assignment>> tableAssignments = ByteUtils.fromBytes(assignmentsBytes);
```

### DataFlowIssue
Method invocation `createMvTable` may produce `NullPointerException`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/TableManager.java`
#### Snippet
```java
     */
    protected MvTableStorage createTableStorage(TableConfiguration tableCfg, TablesConfiguration tablesCfg) {
        MvTableStorage tableStorage = dataStorageMgr.engine(tableCfg.dataStorage()).createMvTable(tableCfg, tablesCfg);

        tableStorage.start();
```

### DataFlowIssue
Argument `leaderWithTerm.leader()` might be null
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/TableManager.java`
#### Snippet
```java

                    // run update of raft configuration if this node is a leader
                    if (isLocalPeer(leaderWithTerm.leader())) {
                        LOG.info("Current node={} is the leader of partition raft group={}. "
                                        + "Initiate rebalance process for partition={}, table={}",
```

### DataFlowIssue
Method invocation `id` may produce `NullPointerException`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/TableManager.java`
#### Snippet
```java
        ExtendedTableConfiguration tblCfg = assignmentsCtx.config(ExtendedTableConfiguration.class);

        UUID tblId = tblCfg.id().value();

        long causalityToken = assignmentsCtx.storageRevision();
```

### DataFlowIssue
Unboxing of `replicasCtx.oldValue()` may produce `NullPointerException`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/TableManager.java`
#### Snippet
```java

        try {
            if (replicasCtx.oldValue() != null && replicasCtx.oldValue() > 0) {
                TableConfiguration tblCfg = replicasCtx.config(TableConfiguration.class);

```

### DataFlowIssue
Method invocation `name` may produce `NullPointerException`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/TableManager.java`
#### Snippet
```java

                LOG.info("Received update for replicas number [table={}, oldNumber={}, newNumber={}]",
                        tblCfg.name().value(), replicasCtx.oldValue(), replicasCtx.newValue());

                int partCnt = tblCfg.partitions().value();
```

### DataFlowIssue
Unboxing of `replicasCtx.newValue()` may produce `NullPointerException`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/TableManager.java`
#### Snippet
```java
                int partCnt = tblCfg.partitions().value();

                int newReplicas = replicasCtx.newValue();

                CompletableFuture<?>[] futures = new CompletableFuture<?>[partCnt];
```

### DataFlowIssue
Method invocation `name` may produce `NullPointerException`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/TableManager.java`
#### Snippet
```java
    private CompletableFuture<?> onTableCreate(ConfigurationNotificationEvent<TableView> ctx) {
        if (!busyLock.enterBusy()) {
            String tblName = ctx.newValue().name();
            UUID tblId = ((ExtendedTableView) ctx.newValue()).id();

```

### DataFlowIssue
Method invocation `id` may produce `NullPointerException`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/TableManager.java`
#### Snippet
```java
        if (!busyLock.enterBusy()) {
            String tblName = ctx.newValue().name();
            UUID tblId = ((ExtendedTableView) ctx.newValue()).id();

            fireEvent(TableEvent.CREATE,
```

### DataFlowIssue
Method invocation `name` may produce `NullPointerException`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/TableManager.java`
#### Snippet
```java
            return createTableLocally(
                    ctx.storageRevision(),
                    ctx.newValue().name(),
                    ((ExtendedTableView) ctx.newValue()).id(),
                    ctx.newValue().partitions()
```

### DataFlowIssue
Method invocation `id` may produce `NullPointerException`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/TableManager.java`
#### Snippet
```java
                    ctx.storageRevision(),
                    ctx.newValue().name(),
                    ((ExtendedTableView) ctx.newValue()).id(),
                    ctx.newValue().partitions()
            );
```

### DataFlowIssue
Method invocation `toCharArray` may produce `NullPointerException`
in `modules/client/src/main/java/org/apache/ignite/internal/client/io/netty/NettyClientConnectionMultiplexer.java`
#### Snippet
```java
        KeyStore ks = KeyStore.getInstance(ssl.keyStoreType());

        char[] ksPassword = ssl.keyStorePassword() == null ? null : ssl.keyStorePassword().toCharArray();
        if (ssl.keyStorePath() != null) {
            try (InputStream is = Files.newInputStream(Path.of(ssl.keyStorePath()))) {
```

### DataFlowIssue
Argument `ssl.keyStorePath()` might be null
in `modules/client/src/main/java/org/apache/ignite/internal/client/io/netty/NettyClientConnectionMultiplexer.java`
#### Snippet
```java
        char[] ksPassword = ssl.keyStorePassword() == null ? null : ssl.keyStorePassword().toCharArray();
        if (ssl.keyStorePath() != null) {
            try (InputStream is = Files.newInputStream(Path.of(ssl.keyStorePath()))) {
                ks.load(is, ksPassword);
            }
```

### DataFlowIssue
Method invocation `toCharArray` may produce `NullPointerException`
in `modules/client/src/main/java/org/apache/ignite/internal/client/io/netty/NettyClientConnectionMultiplexer.java`
#### Snippet
```java
            throws KeyStoreException, IOException, NoSuchAlgorithmException, CertificateException {
        KeyStore ts = KeyStore.getInstance(ssl.trustStoreType());
        char[] tsPassword = ssl.trustStorePassword() == null ? null : ssl.trustStorePassword().toCharArray();
        if (ssl.trustStorePath() != null) {
            try (InputStream is = Files.newInputStream(Path.of(ssl.trustStorePath()))) {
```

### DataFlowIssue
Argument `ssl.trustStorePath()` might be null
in `modules/client/src/main/java/org/apache/ignite/internal/client/io/netty/NettyClientConnectionMultiplexer.java`
#### Snippet
```java
        char[] tsPassword = ssl.trustStorePassword() == null ? null : ssl.trustStorePassword().toCharArray();
        if (ssl.trustStorePath() != null) {
            try (InputStream is = Files.newInputStream(Path.of(ssl.trustStorePath()))) {
                ts.load(is, tsPassword);
            }
```

### DataFlowIssue
Method invocation `enabled` may produce `NullPointerException`
in `modules/client/src/main/java/org/apache/ignite/internal/client/io/netty/NettyClientConnectionMultiplexer.java`
#### Snippet
```java

    private void setupSsl(SocketChannel ch, IgniteClientConfiguration clientCfg) {
        if (clientCfg.ssl() == null || !clientCfg.ssl().enabled()) {
            return;
        }
```

### DataFlowIssue
Argument `ssl` might be null
in `modules/client/src/main/java/org/apache/ignite/internal/client/io/netty/NettyClientConnectionMultiplexer.java`
#### Snippet
```java
        try {
            SslConfiguration ssl = clientCfg.ssl();
            SslContextBuilder builder = SslContextBuilder.forClient().trustManager(loadTrustManagerFactory(ssl));

            ClientAuth clientAuth = toNettyClientAuth(ssl.clientAuthenticationMode());
```

### DataFlowIssue
Method invocation `get` may produce `NullPointerException`
in `modules/client/src/main/java/org/apache/ignite/internal/client/ReliableChannel.java`
#### Snippet
```java

        if (idx != -1) {
            currDfltHolder = holders.get(idx);
        }

```

### DataFlowIssue
Argument `internalTx.readTimestamp()` might be null
in `modules/table/src/testFixtures/java/org/apache/ignite/internal/table/TxAbstractTest.java`
#### Snippet
```java
    private CompletableFuture<List<Tuple>> scan(InternalTable internalTable, InternalTransaction internalTx) {
        Flow.Publisher<BinaryRow> pub = internalTx != null && internalTx.isReadOnly()
                ? internalTable.scan(0, internalTx.readTimestamp(), internalTable.leaderAssignment(0))
                : internalTable.scan(0, internalTx);

```

### DataFlowIssue
Method invocation `value` may produce `NullPointerException`
in `modules/schema/src/main/java/org/apache/ignite/internal/schema/SchemaManager.java`
#### Snippet
```java

        try {
            ExtendedTableView tblCfg = (ExtendedTableView) ctx.config(ExtendedTableConfiguration.class).value();

            int newSchemaVersion = tblCfg.schemaId();
```

### DataFlowIssue
Method invocation `getClass` will produce `NullPointerException`
in `modules/schema/src/main/java/org/apache/ignite/internal/schema/configuration/ConfigurationToSchemaDescriptorConverter.java`
#### Snippet
```java
            );
        } else {
            throw new IllegalStateException("Unknown value supplier class " + defaultValueView.getClass().getName());
        }

```

### DataFlowIssue
Method invocation `columns` may produce `NullPointerException`
in `modules/schema/src/main/java/org/apache/ignite/internal/schema/configuration/TableValidatorImpl.java`
#### Snippet
```java
            TableView newTable = newTables.get(tableName);

            if (newTable.columns() == null || newTable.columns().size() == 0) {
                ctx.addIssue(new ValidationIssue(newTable.name(), "Table should include at least one column"));

```

### DataFlowIssue
Switch label `"NUMBER"` is unreachable
in `modules/schema/src/main/java/org/apache/ignite/internal/schema/configuration/ColumnTypeValidatorImpl.java`
#### Snippet
```java
    private void validatePrecision(ValidationContext<ColumnTypeView> ctx, ColumnTypeView typeView) {
        switch (typeView.type()) {
            case "NUMBER":
            case "DECIMAL":
                if (typeView.precision() <= 0) {
```

### DataFlowIssue
Switch label `"DECIMAL"` is unreachable
in `modules/schema/src/main/java/org/apache/ignite/internal/schema/configuration/ColumnTypeValidatorImpl.java`
#### Snippet
```java
        switch (typeView.type()) {
            case "NUMBER":
            case "DECIMAL":
                if (typeView.precision() <= 0) {
                    ColumnView columnView = ctx.getNewOwner();
```

### DataFlowIssue
Switch label `"TIME"` is unreachable
in `modules/schema/src/main/java/org/apache/ignite/internal/schema/configuration/ColumnTypeValidatorImpl.java`
#### Snippet
```java
                break;

            case "TIME":
            case "DATETIME":
            case "TIMESTAMP":
```

### DataFlowIssue
Switch label `"DATETIME"` is unreachable
in `modules/schema/src/main/java/org/apache/ignite/internal/schema/configuration/ColumnTypeValidatorImpl.java`
#### Snippet
```java

            case "TIME":
            case "DATETIME":
            case "TIMESTAMP":
                if (typeView.precision() < 0 || typeView.precision() > 9) {
```

### DataFlowIssue
Switch label `"TIMESTAMP"` is unreachable
in `modules/schema/src/main/java/org/apache/ignite/internal/schema/configuration/ColumnTypeValidatorImpl.java`
#### Snippet
```java
            case "TIME":
            case "DATETIME":
            case "TIMESTAMP":
                if (typeView.precision() < 0 || typeView.precision() > 9) {
                    ColumnView columnView = ctx.getNewOwner();
```

### DataFlowIssue
Switch label `"STRING"` is unreachable
in `modules/schema/src/main/java/org/apache/ignite/internal/schema/configuration/ColumnTypeValidatorImpl.java`
#### Snippet
```java
    private void validateLength(ValidationContext<ColumnTypeView> ctx, ColumnTypeView typeView) {
        switch (typeView.type()) {
            case "STRING":
            case "BYTES":
            case "BITMASK":
```

### DataFlowIssue
Switch label `"BYTES"` is unreachable
in `modules/schema/src/main/java/org/apache/ignite/internal/schema/configuration/ColumnTypeValidatorImpl.java`
#### Snippet
```java
        switch (typeView.type()) {
            case "STRING":
            case "BYTES":
            case "BITMASK":
                if (typeView.length() <= 0) {
```

### DataFlowIssue
Switch label `"BITMASK"` is unreachable
in `modules/schema/src/main/java/org/apache/ignite/internal/schema/configuration/ColumnTypeValidatorImpl.java`
#### Snippet
```java
            case "STRING":
            case "BYTES":
            case "BITMASK":
                if (typeView.length() <= 0) {
                    ColumnView columnView = ctx.getNewOwner();
```

### DataFlowIssue
Method invocation `tableId` may produce `NullPointerException`
in `modules/schema/src/main/java/org/apache/ignite/internal/schema/configuration/index/IndexValidatorImpl.java`
#### Snippet
```java
            TableIndexView idxView = indexView.get(key);

            UUID tableId = idxView.tableId();

            TableView tableView = getByInternalId(tablesView, tableId);
```

### DataFlowIssue
Method invocation `name` will produce `NullPointerException`
in `modules/schema/src/main/java/org/apache/ignite/internal/schema/configuration/index/IndexValidatorImpl.java`
#### Snippet
```java
            indexedColumns = new ArrayList<>(index0.columns().namedListKeys());
        } else {
            ctx.addIssue(new ValidationIssue(indexView.name(), "Index type is not supported [type=" + indexView.type() + "]"));

            // no further validation required
```

### DataFlowIssue
Method reference argument might be null
in `modules/schema/src/testFixtures/java/org/apache/ignite/internal/schema/testutils/SchemaConfigurationConverter.java`
#### Snippet
```java
                List<SortedIndexColumnDefinition> sortedIndexColumnDefinitions = sortedIndexColumns.namedListKeys().stream()
                        .map(sortedIndexColumns::get)
                        .map(SchemaConfigurationConverter::convert)
                        .collect(Collectors.toList());

```

### DataFlowIssue
Unboxing of `old.get2()` may produce `NullPointerException`
in `modules/metrics/src/main/java/org/apache/ignite/internal/metrics/MetricRegistry.java`
#### Snippet
```java
        IgniteBiTuple<Map<String, MetricSet>, Long> old = metricSnapshot;

        metricSnapshot = new IgniteBiTuple<>(unmodifiableMap(metricSets), old.get2() + 1);
    }

```

### DataFlowIssue
Method invocation `exporterName` may produce `NullPointerException`
in `modules/metrics/src/main/java/org/apache/ignite/internal/metrics/MetricManager.java`
#### Snippet
```java
        @Override
        public CompletableFuture<?> onCreate(ConfigurationNotificationEvent<ExporterView> ctx) {
            checkAndStartExporter(ctx.newValue().exporterName(), ctx.newValue());

            return CompletableFuture.completedFuture(null);
```

### DataFlowIssue
Method invocation `exporterName` may produce `NullPointerException`
in `modules/metrics/src/main/java/org/apache/ignite/internal/metrics/MetricManager.java`
#### Snippet
```java
        @Override
        public CompletableFuture<?> onDelete(ConfigurationNotificationEvent<ExporterView> ctx) {
            var removed = enabledMetricExporters.remove(ctx.oldValue().exporterName());

            if (removed != null) {
```

### DataFlowIssue
Method invocation `exporterName` may produce `NullPointerException`
in `modules/metrics/src/main/java/org/apache/ignite/internal/metrics/MetricManager.java`
#### Snippet
```java
        @Override
        public CompletableFuture<?> onUpdate(ConfigurationNotificationEvent<ExporterView> ctx) {
            MetricExporter exporter = enabledMetricExporters.get(ctx.newValue().exporterName());

            if (exporter != null) {
```

### DataFlowIssue
Method invocation `values` may produce `NullPointerException`
in `modules/metrics/src/main/java/org/apache/ignite/internal/metrics/exporters/jmx/JmxExporter.java`
#### Snippet
```java
        super.start(metricsProvider, configuration);

        for (MetricSet metricSet : metricsProvider.metrics().get1().values()) {
            register(metricSet);
        }
```

### DataFlowIssue
Method invocation `getClass` may produce `NullPointerException`
in `modules/metrics/src/main/java/org/apache/ignite/internal/metrics/exporters/jmx/MetricSetMbean.java`
#### Snippet
```java
        }

        throw new AttributeNotFoundException("Unknown metric class " + metric.getClass());
    }

```

### DataFlowIssue
Method invocation `commitPartition` may produce `NullPointerException`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/storage/InternalTableImpl.java`
#### Snippet
```java

        if (primaryReplicaAndTerm != null) {
            TablePartitionId commitPart = (TablePartitionId) tx.commitPartition();

            ReplicaRequest request = op.apply(commitPart, tx0, partGroupId, primaryReplicaAndTerm.get2());
```

### DataFlowIssue
Argument `primaryReplicaAndTerm.get1()` might be null
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/storage/InternalTableImpl.java`
#### Snippet
```java

            try {
                fut = replicaSvc.invoke(primaryReplicaAndTerm.get1(), request);
            } catch (PrimaryReplicaMissException e) {
                throw new TransactionException(e);
```

### DataFlowIssue
Argument `primaryReplicaAndTerm.get1()` might be null
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/storage/InternalTableImpl.java`
#### Snippet
```java
                            try {
                                return replicaSvc.invoke(
                                        primaryReplicaAndTerm.get1(),
                                        requestFunction.apply((TablePartitionId) tx.commitPartition(), primaryReplicaAndTerm.get2())
                                );
```

### DataFlowIssue
Method invocation `consistentId` may produce `NullPointerException`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/storage/InternalTableImpl.java`
#### Snippet
```java
                    throw withCause(TransactionException::new, REPLICA_UNAVAILABLE_ERR, e);
                } else {
                    return clusterNodeResolver.apply(res.leader().consistentId());
                }
            }
```

### DataFlowIssue
Method invocation `consistentId` may produce `NullPointerException`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/storage/InternalTableImpl.java`
#### Snippet
```java
        }

        return clusterNodeResolver.apply(raftGroupService.leader().consistentId());
    }

```

### DataFlowIssue
Method invocation `commitPartition` may produce `NullPointerException`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/storage/InternalTableImpl.java`
#### Snippet
```java

            if (primaryReplicaAndTerm != null) {
                TablePartitionId commitPart = (TablePartitionId) tx.commitPartition();

                ReplicaRequest request = op.apply(commitPart, partToRows.getValue(), tx0, partGroupId, primaryReplicaAndTerm.get2());
```

### DataFlowIssue
Argument `primaryReplicaAndTerm.get1()` might be null
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/storage/InternalTableImpl.java`
#### Snippet
```java

                try {
                    fut = replicaSvc.invoke(primaryReplicaAndTerm.get1(), request);
                } catch (PrimaryReplicaMissException e) {
                    throw new TransactionException(e);
```

### DataFlowIssue
Method invocation `consistentId` may produce `NullPointerException`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/storage/InternalTableImpl.java`
#### Snippet
```java

            return tx.enlist(partGroupId,
                    new IgniteBiTuple<>(clusterNodeResolver.apply(primaryPeerAndTerm.leader().consistentId()), primaryPeerAndTerm.term()));
        });
    }
```

### DataFlowIssue
Method invocation `consistentId` may produce `NullPointerException`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/storage/InternalTableImpl.java`
#### Snippet
```java
                .sorted(Comparator.comparingInt(Int2ObjectOpenHashMap.Entry::getIntKey))
                .map(Map.Entry::getValue)
                .map(service -> service.leader().consistentId())
                .collect(Collectors.toList());
    }
```

### DataFlowIssue
Argument `primaryReplicaAndTerm.get1()` might be null
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/storage/InternalTableImpl.java`
#### Snippet
```java

            try {
                fut = replicaSvc.invoke(primaryReplicaAndTerm.get1(), request);
            } catch (PrimaryReplicaMissException e) {
                throw new TransactionException(e);
```

### DataFlowIssue
Method invocation `consistentId` may produce `NullPointerException`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/storage/InternalTableImpl.java`
#### Snippet
```java
        for (CompletableFuture<LeaderWithTerm> fut : futs) {
            LeaderWithTerm leaderWithTerm = fut.join();
            ClusterNode primaryNode = clusterNodeResolver.apply(leaderWithTerm.leader().consistentId());

            primaryReplicas.add(new PrimaryReplica(primaryNode, leaderWithTerm.term()));
```

### DataFlowIssue
Argument `tx.readTimestamp()` might be null
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/storage/InternalTableImpl.java`
#### Snippet
```java
            } else {
                return evaluateReadOnlyRecipientNode(partitionId(firstRow))
                        .thenCompose(recipientNode -> getAll(keyRows, tx.readTimestamp(), recipientNode));
            }
        } else {
```

### DataFlowIssue
Argument `tx.readTimestamp()` might be null
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/storage/InternalTableImpl.java`
#### Snippet
```java
        if (tx != null && tx.isReadOnly()) {
            return evaluateReadOnlyRecipientNode(partitionId(keyRow))
                    .thenCompose(recipientNode -> get(keyRow, tx.readTimestamp(), recipientNode));
        } else {
            return enlistInTx(
```

### DataFlowIssue
Method invocation `close` may produce `NullPointerException`
in `modules/network/src/main/java/org/apache/ignite/internal/network/netty/NettyClient.java`
#### Snippet
```java
                    .handle((sender, throwable) ->
                            channel == null
                                    ? CompletableFuture.<Void>completedFuture(null) : NettyUtils.toCompletableFuture(channel.close()))
                    .thenCompose(Function.identity());
        }
```

### DataFlowIssue
Method invocation `close` may produce `NullPointerException`
in `modules/network/src/main/java/org/apache/ignite/internal/network/netty/NettyServer.java`
#### Snippet
```java
            return serverStartFuture.handle((unused, throwable) -> {
                if (channel != null) {
                    channel.close();
                }

```

### DataFlowIssue
Method invocation `localAddress` may produce `NullPointerException`
in `modules/network/src/main/java/org/apache/ignite/internal/network/netty/NettyServer.java`
#### Snippet
```java
     */
    public SocketAddress address() {
        return channel.localAddress();
    }

```

### DataFlowIssue
Method invocation `get` may produce `NullPointerException`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/replicator/PartitionReplicaListener.java`
#### Snippet
```java
     */
    private CompletableFuture<BinaryRow> resolveRowByPkForReadOnly(BinaryRow searchKey, HybridTimestamp ts) {
        try (Cursor<RowId> cursor = pkIndexStorage.get().get(searchKey)) {
            List<ReadResult> candidates = new ArrayList<>();

```

### DataFlowIssue
Argument `writeIntent.commitTableId()` might be null
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/replicator/PartitionReplicaListener.java`
#### Snippet
```java

                return resolveTxState(
                                new TablePartitionId(writeIntent.commitTableId(), writeIntent.commitPartitionId()),
                                writeIntent.transactionId(),
                                ts)
```

### DataFlowIssue
Method invocation `id` may produce `NullPointerException`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/replicator/PartitionReplicaListener.java`
#### Snippet
```java
            BiFunction<@Nullable RowId, @Nullable BinaryRow, CompletableFuture<T>> action
    ) {
        IndexLocker pkLocker = indexesLockers.get().get(pkIndexStorage.get().id());

        assert pkLocker != null;
```

### DataFlowIssue
Method invocation `get` may produce `NullPointerException`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/replicator/PartitionReplicaListener.java`
#### Snippet
```java
        return pkLocker.locksForLookup(txId, binaryRow)
                .thenCompose(ignored -> {
                    try (Cursor<RowId> cursor = pkIndexStorage.get().get(binaryRow)) {
                        for (RowId rowId : cursor) {
                            BinaryRow row = resolveReadResult(mvDataStorage.read(rowId, HybridTimestamp.MAX_VALUE), txId);
```

### DataFlowIssue
Method invocation `consistentId` may produce `NullPointerException`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/replicator/PartitionReplicaListener.java`
#### Snippet
```java
                        return txStateFut.thenApply(txMeta -> new LeaderOrTxState(null, txMeta));
                    } else {
                        return completedFuture(new LeaderOrTxState(leader.consistentId(), null));
                    }
                });
```

### DataFlowIssue
Method invocation `uuid` may produce `NullPointerException`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/replicator/PartitionReplicaListener.java`
#### Snippet
```java

                            return applyUpdateCommand(
                                    updateCommand(commitPartitionId, validatedRowId.get1().uuid(), newRow.byteBuffer(), txId))
                                    .thenApply(ignored -> validatedRowId)
                                    .thenApply(rowIdLock -> {
```

### DataFlowIssue
Method invocation `forEach` may produce `NullPointerException`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/replicator/PartitionReplicaListener.java`
#### Snippet
```java
                                    .thenApply(rowIdLock -> {
                                        // Release short term locks.
                                        rowIdLock.get2().forEach(lock -> lockManager.release(lock.txId(), lock.lockKey(), lock.lockMode()));

                                        return true;
```

### DataFlowIssue
Method invocation `forEach` may produce `NullPointerException`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/replicator/PartitionReplicaListener.java`
#### Snippet
```java
                                for (CompletableFuture<IgniteBiTuple<RowId, Collection<Lock>>> insertLockFut : insertLockFuts) {
                                    insertLockFut.join().get2()
                                            .forEach(lock -> lockManager.release(lock.txId(), lock.lockKey(), lock.lockMode()));
                                }

```

### DataFlowIssue
Method invocation `uuid` may produce `NullPointerException`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/replicator/PartitionReplicaListener.java`
#### Snippet
```java
                        RowId lockedRow = rowIdFuts[futNum++].join().get1();

                        rowsToUpdate.put(lockedRow.uuid(), row.byteBuffer());
                    }

```

### DataFlowIssue
Method invocation `forEach` may produce `NullPointerException`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/replicator/PartitionReplicaListener.java`
#### Snippet
```java
                                for (CompletableFuture<IgniteBiTuple<RowId, Collection<Lock>>> rowIdFut : rowIdFuts) {
                                    rowIdFut.join().get2()
                                            .forEach(lock -> lockManager.release(lock.txId(), lock.lockKey(), lock.lockMode()));
                                }

```

### DataFlowIssue
Method invocation `forEach` may produce `NullPointerException`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/replicator/PartitionReplicaListener.java`
#### Snippet
```java
                            .thenApply(rowIdLock -> {
                                // Release short term locks.
                                rowIdLock.get2().forEach(lock -> lockManager.release(lock.txId(), lock.lockKey(), lock.lockMode()));

                                return true;
```

### DataFlowIssue
Method invocation `forEach` may produce `NullPointerException`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/replicator/PartitionReplicaListener.java`
#### Snippet
```java
                            .thenApply(rowIdLock -> {
                                // Release short term locks.
                                rowIdLock.get2().forEach(lock -> lockManager.release(lock.txId(), lock.lockKey(), lock.lockMode()));

                                return null;
```

### DataFlowIssue
Method invocation `forEach` may produce `NullPointerException`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/replicator/PartitionReplicaListener.java`
#### Snippet
```java
                            .thenApply(rowIdLock -> {
                                // Release short term locks.
                                rowIdLock.get2().forEach(lock -> lockManager.release(lock.txId(), lock.lockKey(), lock.lockMode()));

                                return row;
```

### DataFlowIssue
Method invocation `forEach` may produce `NullPointerException`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/replicator/PartitionReplicaListener.java`
#### Snippet
```java
                            .thenApply(rowIdLock -> {
                                // Release short term locks.
                                rowIdLock.get2().forEach(lock -> lockManager.release(lock.txId(), lock.lockKey(), lock.lockMode()));

                                return row;
```

### DataFlowIssue
Method invocation `forEach` may produce `NullPointerException`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/replicator/PartitionReplicaListener.java`
#### Snippet
```java
                            .thenApply(rowIdLock -> {
                                // Release short term locks.
                                rowIdLock.get2().forEach(lock -> lockManager.release(lock.txId(), lock.lockKey(), lock.lockMode()));

                                return true;
```

### DataFlowIssue
Argument `readResult.commitTableId()` might be null
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/replicator/PartitionReplicaListener.java`
#### Snippet
```java
    ) {
        return resolveTxState(
                        new TablePartitionId(readResult.commitTableId(), readResult.commitPartitionId()),
                        readResult.transactionId(),
                        timestamp)
```

### DataFlowIssue
Method invocation `equals` may produce `NullPointerException`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/replicator/PartitionReplicaListener.java`
#### Snippet
```java

        for (ReadResult wi : writeIntents) {
            assert wi.transactionId().equals(writeIntent.transactionId())
                    : "Unexpected write intent, tx1=" + writeIntent.transactionId() + ", tx2=" + wi.transactionId();

```

### DataFlowIssue
Method invocation `equals` may produce `NullPointerException`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/replicator/PartitionReplicaListener.java`
#### Snippet
```java
                    : "Unexpected write intent, tx1=" + writeIntent.transactionId() + ", tx2=" + wi.transactionId();

            assert wi.commitTableId().equals(writeIntent.commitTableId())
                    : "Unexpected write intent, commitTableId1=" + writeIntent.commitTableId() + ", commitTableId2=" + wi.commitTableId();

```

### DataFlowIssue
Method invocation `compareTo` may produce `NullPointerException`
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/replicator/PartitionReplicaListener.java`
#### Snippet
```java
                        return true;
                    } else if (txMeta.txState() == TxState.COMMITED) {
                        if (txMeta.commitTimestamp().compareTo(timestamp) <= 0) {
                            return false;
                        } else {
```

### DataFlowIssue
Variable is already assigned to this value
in `modules/network/src/main/java/org/apache/ignite/internal/network/direct/stream/DirectByteBufferStreamImplV1.java`
#### Snippet
```java
            switch (len) {
                case -1:
                    lastFinished = true;

                    return null;
```

### DataFlowIssue
Variable is already assigned to this value
in `modules/network/src/main/java/org/apache/ignite/internal/network/direct/stream/DirectByteBufferStreamImplV1.java`
#### Snippet
```java

                case 0:
                    lastFinished = true;

                    return creator.of(0);
```

### DataFlowIssue
Variable is already assigned to this value
in `modules/network/src/main/java/org/apache/ignite/internal/network/direct/stream/DirectByteBufferStreamImplV1.java`
#### Snippet
```java
            switch (len) {
                case -1:
                    lastFinished = true;

                    return null;
```

### DataFlowIssue
Variable is already assigned to this value
in `modules/network/src/main/java/org/apache/ignite/internal/network/direct/stream/DirectByteBufferStreamImplV1.java`
#### Snippet
```java

                case 0:
                    lastFinished = true;

                    return creator.of(0);
```

### DataFlowIssue
Method invocation `writeMessage` may produce `NullPointerException`
in `modules/network/src/main/java/org/apache/ignite/internal/network/direct/stream/DirectByteBufferStreamImplV1.java`
#### Snippet
```java
                    writer.setBuffer(buf);

                    lastFinished = msgSerializer.writeMessage(msg, writer);

                    if (lastFinished) {
```

### DataFlowIssue
Method invocation `klass` may produce `NullPointerException`
in `modules/network/src/main/java/org/apache/ignite/internal/network/direct/stream/DirectByteBufferStreamImplV1.java`
#### Snippet
```java

        try {
            reader.setCurrentReadClass(msgDeserializer.klass());

            reader.setBuffer(buf);
```

### DataFlowIssue
Method invocation `getType` may produce `NullPointerException`
in `modules/bytecode/src/main/java/com/facebook/presto/bytecode/ArrayOpCode.java`
#### Snippet
```java

    public int getAtype() {
        return atype.getType();
    }

```

### DataFlowIssue
Unboxing of `o1.get1()` may produce `NullPointerException`
in `modules/affinity/src/main/java/org/apache/ignite/internal/affinity/RendezvousAffinityFunction.java`
#### Snippet
```java
        @Override
        public int compare(IgniteBiTuple<Long, String> o1, IgniteBiTuple<Long, String> o2) {
            return o1.get1() < o2.get1() ? -1 : o1.get1() > o2.get1() ? 1 :
                    o1.get2().compareTo(o2.get2());
        }
```

### DataFlowIssue
Unboxing of `o2.get1()` may produce `NullPointerException`
in `modules/affinity/src/main/java/org/apache/ignite/internal/affinity/RendezvousAffinityFunction.java`
#### Snippet
```java
        @Override
        public int compare(IgniteBiTuple<Long, String> o1, IgniteBiTuple<Long, String> o2) {
            return o1.get1() < o2.get1() ? -1 : o1.get1() > o2.get1() ? 1 :
                    o1.get2().compareTo(o2.get2());
        }
```

### DataFlowIssue
Method invocation `compareTo` may produce `NullPointerException`
in `modules/affinity/src/main/java/org/apache/ignite/internal/affinity/RendezvousAffinityFunction.java`
#### Snippet
```java
        public int compare(IgniteBiTuple<Long, String> o1, IgniteBiTuple<Long, String> o2) {
            return o1.get1() < o2.get1() ? -1 : o1.get1() > o2.get1() ? 1 :
                    o1.get2().compareTo(o2.get2());
        }
    }
```

### DataFlowIssue
Argument `o2.get2()` might be null
in `modules/affinity/src/main/java/org/apache/ignite/internal/affinity/RendezvousAffinityFunction.java`
#### Snippet
```java
        public int compare(IgniteBiTuple<Long, String> o1, IgniteBiTuple<Long, String> o2) {
            return o1.get1() < o2.get1() ? -1 : o1.get1() > o2.get1() ? 1 :
                    o1.get2().compareTo(o2.get2());
        }
    }
```

### DataFlowIssue
Argument `errorMessageTemplate` might be null
in `modules/bytecode/src/main/java/com/facebook/presto/bytecode/BytecodeUtils.java`
#### Snippet
```java
        Object... errorMessageArgs) {
        if (!expression) {
            throw new IllegalStateException(String.format(errorMessageTemplate, errorMessageArgs));
        }
    }
```

### DataFlowIssue
Argument `is` might be null
in `modules/bytecode/src/main/java/com/facebook/presto/bytecode/ClassInfoLoader.java`
#### Snippet
```java
            String classFileName = type.getClassName() + ".class";
            try (InputStream is = classLoader.getResourceAsStream(classFileName)) {
                classReader = new ClassReader(is);
            }
            catch (IOException e) {
```

### DataFlowIssue
Argument `instance.getType().getArrayComponentType()` might be null
in `modules/bytecode/src/main/java/com/facebook/presto/bytecode/expression/GetElementBytecodeExpression.java`
#### Snippet
```java

        checkArgument(index.getType().getPrimitiveType() == int.class, "index must be int type, but is " + index.getType());
        this.arrayLoadInstruction = getArrayOpCode(instance.getType().getArrayComponentType()).getLoad();
    }

```

### DataFlowIssue
Method invocation `equals` may produce `NullPointerException`
in `modules/bytecode/src/main/java/com/facebook/presto/bytecode/expression/SetArrayElementBytecodeExpression.java`
#### Snippet
```java
        ParameterizedType componentType = instance.getType().getArrayComponentType();
        checkArgument(index.getType().getPrimitiveType() == int.class, "index must be int type, but is " + index.getType());
        checkArgument(componentType.equals(value.getType()), "value must be %s type, but is %s", componentType, value.getType());

        this.arrayStoreInstruction = getArrayOpCode(componentType).getStore();
```

### DataFlowIssue
Argument `sourceClass` might be null
in `modules/bytecode/src/main/java/com/facebook/presto/bytecode/expression/CastBytecodeExpression.java`
#### Snippet
```java
                        Class<?> sourceClass = sourceType.getPrimitiveType();
                        return block
                                .invokeStatic(BytecodeUtils.wrap(sourceClass), "valueOf", BytecodeUtils.wrap(sourceClass), sourceClass)
                                .checkCast(targetType);
                }
```

### DataFlowIssue
Method invocation `getSimpleName` may produce `NullPointerException`
in `modules/bytecode/src/main/java/com/facebook/presto/bytecode/expression/CastBytecodeExpression.java`
#### Snippet
```java
                    case PRIMITIVE:
                        checkArgument(unwrapPrimitiveType(sourceType) == targetType.getPrimitiveType(), "Type %s can not be cast to %s", sourceType, targetType);
                        return block.invokeVirtual(sourceType, targetType.getPrimitiveType().getSimpleName() + "Value", targetType);
                    case BOXED_PRIMITVE:
                        checkArgument(sourceType.equals(targetType), "Type %s can not be cast to %s", sourceType, targetType);
```

### DataFlowIssue
Argument `targetType.getPrimitiveType()` might be null
in `modules/bytecode/src/main/java/com/facebook/presto/bytecode/expression/CastBytecodeExpression.java`
#### Snippet
```java
                        checkArgument(OBJECT_TYPE.equals(sourceType), "Type %s can not be cast to %s", sourceType, targetType);
                        return block
                                .checkCast(BytecodeUtils.wrap(targetType.getPrimitiveType()))
                                .invokeVirtual(BytecodeUtils.wrap(targetType.getPrimitiveType()), targetType.getPrimitiveType().getSimpleName() + "Value", targetType.getPrimitiveType());
                    case BOXED_PRIMITVE:
```

### DataFlowIssue
Argument `value.getPrimitiveType()` might be null
in `modules/bytecode/src/main/java/com/facebook/presto/bytecode/instruction/Constant.java`
#### Snippet
```java
        public void accept(MethodVisitor visitor, MethodGenerationContext generationContext) {
            if (value.isPrimitive()) {
                Class<?> wrapper = BytecodeUtils.wrap(value.getPrimitiveType());
                getStaticInstruction(wrapper, "TYPE", Class.class).accept(visitor, generationContext);
            }
```

### DataFlowIssue
The call to 'checkArgument' always fails, according to its method contracts
in `modules/bytecode/src/main/java/com/facebook/presto/bytecode/BytecodeBlock.java`
#### Snippet
```java
                    break;
                default:
                    checkArgument(false, "Unknown type '%s'", variable.getType());
            }
        }
```

### DataFlowIssue
Argument `providers.get(it)` might be null
in `modules/security/src/main/java/org/apache/ignite/internal/configuration/AuthenticationProvidersValidatorImpl.java`
#### Snippet
```java
        NamedListView<AuthenticationProviderView> providers = ctx.getNewValue();
        providers.namedListKeys()
                .forEach(it -> validateProvider(it, providers.get(it), ctx));

    }
```

### DataFlowIssue
Unboxing of `correlationId` may produce `NullPointerException`
in `modules/replicator/src/main/java/org/apache/ignite/internal/replicator/ReplicaManager.java`
#### Snippet
```java
                    }

                    clusterNetSvc.messagingService().respond(senderConsistentId, msg, correlationId);

                    return null;
```

### DataFlowIssue
Unboxing of `props.get(QueryProperty.QUERY_TIMEOUT)` may produce `NullPointerException`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/api/SessionImpl.java`
#### Snippet
```java
    @Override
    public long defaultQueryTimeout(TimeUnit timeUnit) {
        return timeUnit.convert(props.get(QueryProperty.QUERY_TIMEOUT), TimeUnit.MILLISECONDS);
    }

```

### DataFlowIssue
Method invocation `longValue` may produce `NullPointerException`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rel/IgniteReceiver.java`
#### Snippet
```java
                input.getTraitSet().replace(IgniteConvention.INSTANCE),
                input.getRowType("rowType"),
                ((Number) input.get("exchangeId")).longValue(),
                ((Number) input.get("sourceFragmentId")).longValue(),
                input.getCollation()
```

### DataFlowIssue
Method invocation `longValue` may produce `NullPointerException`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rel/IgniteReceiver.java`
#### Snippet
```java
                input.getRowType("rowType"),
                ((Number) input.get("exchangeId")).longValue(),
                ((Number) input.get("sourceFragmentId")).longValue(),
                input.getCollation()
        );
```

### DataFlowIssue
Unboxing of `mq.getSelectivity(this, null)` may produce `NullPointerException`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rel/ProjectableFilterableTableScan.java`
#### Snippet
```java
    @Override
    public double estimateRowCount(RelMetadataQuery mq) {
        return table.getRowCount() * mq.getSelectivity(this, null);
    }

```

### DataFlowIssue
Method invocation `getRowType` may produce `NullPointerException`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rel/ProjectableFilterableTableScan.java`
#### Snippet
```java
            return RexUtil.createStructType(Commons.typeFactory(getCluster()), projects);
        } else {
            return table.unwrap(IgniteTable.class).getRowType(Commons.typeFactory(getCluster()), requiredColumns);
        }
    }
```

### DataFlowIssue
Method invocation `getRowType` may produce `NullPointerException`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rel/ProjectableFilterableTableScan.java`
#### Snippet
```java

        Mappings.TargetMapping mapping = RexUtils.inversePermutation(projects,
                tbl.getRowType(typeFactory, requiredColumns), true);

        RexShuttle shuttle = new RexShuttle() {
```

### DataFlowIssue
Method invocation `id` may produce `NullPointerException`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rel/ProjectableFilterableTableScan.java`
#### Snippet
```java
        return explainTerms0(pw
                .item("table", table.getQualifiedName())
                .itemIf("tableId", table.unwrap(IgniteTable.class).id().toString(),
                        pw.getDetailLevel() == ALL_ATTRIBUTES)
                .itemIf("tableVer", table.unwrap(IgniteTable.class).version(),
```

### DataFlowIssue
Method invocation `version` may produce `NullPointerException`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rel/ProjectableFilterableTableScan.java`
#### Snippet
```java
                .itemIf("tableId", table.unwrap(IgniteTable.class).id().toString(),
                        pw.getDetailLevel() == ALL_ATTRIBUTES)
                .itemIf("tableVer", table.unwrap(IgniteTable.class).version(),
                        pw.getDetailLevel() == ALL_ATTRIBUTES)
        );
```

### DataFlowIssue
Unboxing of `mq.getSelectivity(this, null)` may produce `NullPointerException`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rel/IgniteHashIndexSpool.java`
#### Snippet
```java
    @Override
    public double estimateRowCount(RelMetadataQuery mq) {
        return mq.getRowCount(getInput()) * mq.getSelectivity(this, null);
    }

```

### DataFlowIssue
Method invocation `longValue` may produce `NullPointerException`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rel/IgniteSender.java`
#### Snippet
```java
                        .replace(IgniteConvention.INSTANCE),
                input.getInput(),
                ((Number) input.get("exchangeId")).longValue(),
                ((Number) input.get("targetFragmentId")).longValue(),
                (IgniteDistribution) input.getDistribution());
```

### DataFlowIssue
Method invocation `longValue` may produce `NullPointerException`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rel/IgniteSender.java`
#### Snippet
```java
                input.getInput(),
                ((Number) input.get("exchangeId")).longValue(),
                ((Number) input.get("targetFragmentId")).longValue(),
                (IgniteDistribution) input.getDistribution());
    }
```

### DataFlowIssue
Method invocation `id` may produce `NullPointerException`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rel/IgniteTableModify.java`
#### Snippet
```java
    public RelWriter explainTerms(RelWriter pw) {
        return super.explainTerms(pw)
                .itemIf("tableId", getTable().unwrap(IgniteTable.class).id().toString(),
                        pw.getDetailLevel() == ALL_ATTRIBUTES)
                .itemIf("tableVer", getTable().unwrap(IgniteTable.class).version(),
```

### DataFlowIssue
Method invocation `version` may produce `NullPointerException`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rel/IgniteTableModify.java`
#### Snippet
```java
                .itemIf("tableId", getTable().unwrap(IgniteTable.class).id().toString(),
                        pw.getDetailLevel() == ALL_ATTRIBUTES)
                .itemIf("tableVer", getTable().unwrap(IgniteTable.class).version(),
                        pw.getDetailLevel() == ALL_ATTRIBUTES);
    }
```

### DataFlowIssue
Argument `input.getIntegerList("variablesSet")` might be null
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rel/IgniteNestedLoopJoin.java`
#### Snippet
```java
                input.getInputs().get(1),
                input.getExpression("condition"),
                Set.copyOf(Commons.transform(input.getIntegerList("variablesSet"), CorrelationId::new)),
                input.getEnum("joinType", JoinRelType.class));
    }
```

### DataFlowIssue
Unboxing of `mq.getSelectivity(this, null)` may produce `NullPointerException`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rel/IgniteSortedIndexSpool.java`
#### Snippet
```java
    @Override
    public double estimateRowCount(RelMetadataQuery mq) {
        return mq.getRowCount(getInput()) * mq.getSelectivity(this, null);
    }

```

### DataFlowIssue
Argument `input.getIntegerList("variablesSet")` might be null
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rel/IgniteCorrelatedNestedLoopJoin.java`
#### Snippet
```java
                input.getInputs().get(1),
                input.getExpression("condition"),
                Set.copyOf(Commons.transform(input.getIntegerList("variablesSet"), CorrelationId::new)),
                input.getEnum("joinType", JoinRelType.class));
    }
```

### DataFlowIssue
Method invocation `get` may produce `NullPointerException`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rel/IgniteCorrelatedNestedLoopJoin.java`
#### Snippet
```java
                    baseTraits.getKey(),
                    List.of(
                            baseTraits.getValue().get(0),
                            baseTraits.getValue().get(1)
                    )
```

### DataFlowIssue
Unboxing of `mq.getSelectivity(this, getCondition())` may produce `NullPointerException`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rel/IgniteCorrelatedNestedLoopJoin.java`
#### Snippet
```java
    public double estimateRowCount(RelMetadataQuery mq) {
        // condition selectivity already counted within the external filter
        return super.estimateRowCount(mq) / mq.getSelectivity(this, getCondition());
    }
}
```

### DataFlowIssue
Unboxing of `mq.getSelectivity(this, RexUtil.composeConjunction(builder, Commons.transfor...` may produce `NullPointerException`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rel/AbstractIndexScan.java`
#### Snippet
```java

            if (searchBounds != null) {
                selectivity = mq.getSelectivity(this, RexUtil.composeConjunction(builder,
                        Commons.transform(searchBounds, b -> b == null ? null : b.condition())));

                cost = Math.log(rows) * IgniteCost.ROW_COMPARISON_COST;
```

### DataFlowIssue
Argument `input.getIntegerList("variablesSet")` might be null
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rel/IgniteMergeJoin.java`
#### Snippet
```java
                input.getInputs().get(1),
                input.getExpression("condition"),
                Set.copyOf(Commons.transform(input.getIntegerList("variablesSet"), CorrelationId::new)),
                input.getEnum("joinType", JoinRelType.class),
                ((RelInputEx) input).getCollation("leftCollation"),
```

### DataFlowIssue
Condition `typeFactory instanceof IgniteTypeFactory` is redundant and can be replaced with a null check
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rel/set/IgniteMapSetOp.java`
#### Snippet
```java
        RelDataTypeFactory typeFactory = Commons.typeFactory(getCluster());

        assert typeFactory instanceof IgniteTypeFactory;

        RelDataTypeFactory.Builder builder = new RelDataTypeFactory.Builder(typeFactory);
```

### DataFlowIssue
Method invocation `getIndex` may produce `NullPointerException`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rel/logical/IgniteLogicalIndexScan.java`
#### Snippet
```java
        IgniteTable tbl = table.unwrap(IgniteTable.class);
        IgniteTypeFactory typeFactory = Commons.typeFactory(cluster);
        IgniteIndex index = tbl.getIndex(idxName);
        RelCollation collation = TraitUtils.createCollation(index.columns(), index.collations(), tbl.descriptor());

```

### DataFlowIssue
Unboxing of `first(collation.getKeys())` may produce `NullPointerException`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/RuntimeSortedIndex.java`
#### Snippet
```java
    @Override
    public Cursor<RowT> find(RowT lower, RowT upper, boolean lowerInclude, boolean upperInclude) {
        int firstCol = first(collation.getKeys());

        Object lowerBound = (lower == null) ? null : ectx.rowHandler().get(firstCol, lower);
```

### DataFlowIssue
Method invocation `unwrap` may produce `NullPointerException`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/ExecutionServiceImpl.java`
#### Snippet
```java

                private void enlist(SourceAwareIgniteRel rel) {
                    UUID tableId = rel.getTable().unwrap(IgniteTable.class).id();
                    List<NodeWithTerm> assignments = fragment.mapping().findGroup(rel.sourceId()).assignments().stream()
                            .map(l -> l.get(0))
```

### DataFlowIssue
Method invocation `id` may produce `NullPointerException`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/ExecutionServiceImpl.java`
#### Snippet
```java

                private void enlist(SourceAwareIgniteRel rel) {
                    UUID tableId = rel.getTable().unwrap(IgniteTable.class).id();
                    List<NodeWithTerm> assignments = fragment.mapping().findGroup(rel.sourceId()).assignments().stream()
                            .map(l -> l.get(0))
```

### DataFlowIssue
Method invocation `id` may produce `NullPointerException`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/ExecutionServiceImpl.java`
#### Snippet
```java
                @Override
                public IgniteRel visit(IgniteTableModify rel) {
                    UUID tableId = rel.getTable().unwrap(IgniteTable.class).id();
                    List<NodeWithTerm> assignments = fragment.mapping().updatingTableAssignments();

```

### DataFlowIssue
Unboxing of `cmd.dataNodesAutoAdjustScaleDown()` may produce `NullPointerException`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/ddl/DdlCommandHandler.java`
#### Snippet
```java

        if (cmd.dataNodesAutoAdjustScaleDown() != null) {
            zoneCfgBuilder.dataNodesAutoAdjustScaleDown(cmd.dataNodesAutoAdjustScaleDown());
        }

```

### DataFlowIssue
Unboxing of `cmd.dataNodesAutoAdjust()` may produce `NullPointerException`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/ddl/DdlCommandHandler.java`
#### Snippet
```java

        if (cmd.dataNodesAutoAdjust() != null) {
            zoneCfgBuilder.dataNodesAutoAdjust(cmd.dataNodesAutoAdjust());
        }

```

### DataFlowIssue
Unboxing of `cmd.dataNodesAutoAdjustScaleUp()` may produce `NullPointerException`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/ddl/DdlCommandHandler.java`
#### Snippet
```java

        if (cmd.dataNodesAutoAdjustScaleUp() != null) {
            zoneCfgBuilder.dataNodesAutoAdjustScaleUp(cmd.dataNodesAutoAdjustScaleUp());
        }

```

### DataFlowIssue
Method invocation `name` may produce `NullPointerException`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/ddl/DdlCommandHandler.java`
#### Snippet
```java

        for (String colOrder : cols.namedListKeys()) {
            colNames.put(cols.get(colOrder).name(), colOrder);
        }

```

### DataFlowIssue
Unboxing of `cmd.dataNodesAutoAdjust()` may produce `NullPointerException`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/ddl/DdlCommandHandler.java`
#### Snippet
```java

        if (cmd.dataNodesAutoAdjust() != null) {
            zoneCfgBuilder.dataNodesAutoAdjust(cmd.dataNodesAutoAdjust());
        }

```

### DataFlowIssue
Unboxing of `cmd.dataNodesAutoAdjustScaleUp()` may produce `NullPointerException`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/ddl/DdlCommandHandler.java`
#### Snippet
```java

        if (cmd.dataNodesAutoAdjustScaleUp() != null) {
            zoneCfgBuilder.dataNodesAutoAdjustScaleUp(cmd.dataNodesAutoAdjustScaleUp());
        }

```

### DataFlowIssue
Unboxing of `cmd.dataNodesAutoAdjustScaleDown()` may produce `NullPointerException`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/ddl/DdlCommandHandler.java`
#### Snippet
```java

        if (cmd.dataNodesAutoAdjustScaleDown() != null) {
            zoneCfgBuilder.dataNodesAutoAdjustScaleDown(cmd.dataNodesAutoAdjustScaleDown());
        }

```

### DataFlowIssue
Unboxing of `TypeUtils.toInternal(LocalTime.now(), LocalTime.class)` may produce `NullPointerException`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/IgniteSqlFunctions.java`
#### Snippet
```java

    public static int currentTime(DataContext ctx) {
        return (int) TypeUtils.toInternal(LocalTime.now(), LocalTime.class);
    }

```

### DataFlowIssue
Argument `toPrimitive.primitiveName` might be null
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/ConverterUtils.java`
#### Snippet
```java
                        return Expressions.call(
                                SqlFunctions.class,
                                "to" + SqlFunctions.initcap(toPrimitive.primitiveName),
                                operand);
                    default:
```

### DataFlowIssue
Argument `toPrimitive.boxClass` might be null
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/ConverterUtils.java`
#### Snippet
```java
                        // Generate "Short.parseShort(x)".
                        return Expressions.call(
                                toPrimitive.boxClass,
                                "parse" + SqlFunctions.initcap(toPrimitive.primitiveName),
                                operand);
```

### DataFlowIssue
Argument `toPrimitive.primitiveName` might be null
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/ConverterUtils.java`
#### Snippet
```java
                        return Expressions.call(
                                toPrimitive.boxClass,
                                "parse" + SqlFunctions.initcap(toPrimitive.primitiveName),
                                operand);
                }
```

### DataFlowIssue
Argument `toBox.primitiveName` might be null
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/ConverterUtils.java`
#### Snippet
```java
                        return Expressions.call(
                                SqlFunctions.class,
                                "to" + SqlFunctions.initcap(toBox.primitiveName) + "Boxed",
                                operand);
                    default:
```

### DataFlowIssue
Argument `toBox.boxClass` might be null
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/ConverterUtils.java`
#### Snippet
```java
                        // Generate "Short.valueOf(x)".
                        return Expressions.call(
                                toBox.boxClass,
                                "valueOf",
                                operand);
```

### DataFlowIssue
Argument `toPrimitive.primitiveName` might be null
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/ConverterUtils.java`
#### Snippet
```java
                return Expressions.call(
                        SqlFunctions.class,
                        "to" + SqlFunctions.initcap(toPrimitive.primitiveName),
                        operand);
            }
```

### DataFlowIssue
Argument `fromPrimitive.boxClass` might be null
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/ConverterUtils.java`
#### Snippet
```java
                        // Generate "Integer.toString(x)"
                        return Expressions.call(
                                fromPrimitive.boxClass,
                                "toString",
                                operand);
```

### DataFlowIssue
Dereference of `Primitive.ofBox(targetType)` may produce `NullPointerException`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/ConverterUtils.java`
#### Snippet
```java
            // E.g. operand is "int", target is "Long", generate "(long) operand".
            return Expressions.convert_(operand,
                    Primitive.ofBox(targetType).primitiveClass);
        }
        return operand;
```

### DataFlowIssue
Method invocation `getRowType` may produce `NullPointerException`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/LogicalRelImplementor.java`
#### Snippet
```java
        IgniteTypeFactory typeFactory = ctx.getTypeFactory();
        ImmutableBitSet requiredColumns = rel.requiredColumns();
        RelDataType rowType = tbl.getRowType(typeFactory, requiredColumns);

        IgniteIndex idx = tbl.getIndex(rel.indexName());
```

### DataFlowIssue
Argument `idx.collations()` might be null
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/LogicalRelImplementor.java`
#### Snippet
```java

            if (idx.collations() != null) {
                searchRowComparator = expressionFactory.comparator(TraitUtils.createCollation(idx.collations()));
            }

```

### DataFlowIssue
Argument `txAttributes.time()` might be null
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/TableScanNode.java`
#### Snippet
```java

            if (txAttributes.readOnly()) {
                pub = physTable.scan(partWithTerm.partId(), txAttributes.time(), context().localNode());
            } else {
                PrimaryReplica recipient = new PrimaryReplica(context().localNode(), partWithTerm.term());
```

### DataFlowIssue
Expression `first(sources())` might evaluate to null but is returned by the method declared as @NotNull
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/SingleNode.java`
#### Snippet
```java

    default @NotNull Node<RowT> source() {
        return first(sources());
    }
}
```

### DataFlowIssue
Dereference of `cntrsMap` may produce `NullPointerException`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/AbstractSetOpNode.java`
#### Snippet
```java
            int[] cntrsMap = (int[]) hnd.get(1, row);

            int[] cntrs = groups.computeIfAbsent(grpKey, k -> new int[cntrsMap.length]);

            assert cntrs.length == cntrsMap.length;
```

### DataFlowIssue
Dereference of `cntrsMap` may produce `NullPointerException`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/AbstractSetOpNode.java`
#### Snippet
```java
            int[] cntrs = groups.computeIfAbsent(grpKey, k -> new int[cntrsMap.length]);

            assert cntrs.length == cntrsMap.length;

            for (int i = 0; i < cntrsMap.length; i++) {
```

### DataFlowIssue
Dereference of `interval` may produce `NullPointerException`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/RexToLixTranslator.java`
#### Snippet
```java
                                        BuiltInMethod.INTERVAL_YEAR_MONTH_TO_STRING.method,
                                        operand,
                                        Expressions.constant(interval.timeUnitRange)));
                        break;
                    case INTERVAL_DAY:
```

### DataFlowIssue
Dereference of `interval` may produce `NullPointerException`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/RexToLixTranslator.java`
#### Snippet
```java
                                        BuiltInMethod.INTERVAL_DAY_TIME_TO_STRING.method,
                                        operand,
                                        Expressions.constant(interval.timeUnitRange),
                                        Expressions.constant(
                                                interval.getFractionalSecondPrecision(
```

### DataFlowIssue
Dereference of `sourceType.getSqlTypeName().getFamily()` may produce `NullPointerException`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/RexToLixTranslator.java`
#### Snippet
```java
            case INTERVAL_MINUTE_SECOND:
            case INTERVAL_SECOND:
                switch (sourceType.getSqlTypeName().getFamily()) {
                    case CHARACTER:
                        SqlIntervalQualifier intervalQualifier = targetType.getIntervalQualifier();
```

### DataFlowIssue
Method invocation `isYearMonth` may produce `NullPointerException`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/RexToLixTranslator.java`
#### Snippet
```java
                        SqlIntervalQualifier intervalQualifier = targetType.getIntervalQualifier();

                        Method method = intervalQualifier.isYearMonth()
                                ? IgniteMethod.PARSE_INTERVAL_YEAR_MONTH.method()
                                : IgniteMethod.PARSE_INTERVAL_DAY_TIME.method();
```

### DataFlowIssue
Dereference of `sourceType.getSqlTypeName().getFamily()` may produce `NullPointerException`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/RexToLixTranslator.java`
#### Snippet
```java
            case BINARY:
            case VARBINARY:
                switch (sourceType.getSqlTypeName().getFamily()) {
                    case CHARACTER:
                        convert = Expressions.call(IgniteMethod.STRING_TO_BYTESTRING.method(), operand);
```

### DataFlowIssue
Argument `operand` might be null
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/RexToLixTranslator.java`
#### Snippet
```java

        if (convert == null) {
            convert = ConverterUtils.convert(operand, typeFactory.getJavaClass(targetType));
        }

```

### DataFlowIssue
Dereference of `sourceType.getSqlTypeName().getFamily()` may produce `NullPointerException`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/RexToLixTranslator.java`
#### Snippet
```java
            case INTERVAL_MINUTE_SECOND:
            case INTERVAL_SECOND:
                switch (sourceType.getSqlTypeName().getFamily()) {
                    case NUMERIC:
                        final BigDecimal multiplier = targetType.getSqlTypeName().getEndUnit().multiplier;
```

### DataFlowIssue
Method invocation `toString` may produce `NullPointerException`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/RexToLixTranslator.java`
#### Snippet
```java
                        Therefore, BigDecimal must be converted to a string to avoid this.
                         */
                        Expressions.constant(bd.toString()),
                        Expressions.constant(type.getPrecision()),
                        Expressions.constant(type.getScale())
```

### DataFlowIssue
Method invocation `getClass` may produce `NullPointerException`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/RexToLixTranslator.java`
#### Snippet
```java
            case SYMBOL:
                value2 = literal.getValueAs(Enum.class);
                javaClass = value2.getClass();
                break;
            default:
```

### DataFlowIssue
Dereference of `targetType.getSqlTypeName().getFamily()` may produce `NullPointerException`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/RexToLixTranslator.java`
#### Snippet
```java
            RelDataType targetType,
            Expression operand) {
        switch (targetType.getSqlTypeName().getFamily()) {
            case NUMERIC:
                switch (sourceType.getSqlTypeName()) {
```

### DataFlowIssue
Argument `e.left` might be null
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/RexToLixTranslator.java`
#### Snippet
```java
        final List<Expression> list = new ArrayList<>();
        for (Pair<RexNode, ? extends Type> e : Pair.zip(operandList, storageTypes)) {
            list.add(translate(e.left, nullAs, e.right));
        }

```

### DataFlowIssue
Method invocation `isEmpty` may produce `NullPointerException`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/NestedLoopJoinNode.java`
#### Snippet
```java

            if (requested > 0 && waitingLeft == NOT_WAITING && waitingRight == NOT_WAITING && left == null
                    && leftInBuf.isEmpty() && rightNotMatchedIndexes.isEmpty()) {
                requested = 0;
                downstream().end();
```

### DataFlowIssue
Method invocation `isEmpty` may produce `NullPointerException`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/NestedLoopJoinNode.java`
#### Snippet
```java

            if (requested > 0 && waitingLeft == NOT_WAITING && waitingRight == NOT_WAITING && left == null
                    && leftInBuf.isEmpty() && rightNotMatchedIndexes.isEmpty()) {
                requested = 0;
                downstream().end();
```

### DataFlowIssue
Method invocation `size` may produce `NullPointerException`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/SortAggregateNode.java`
#### Snippet
```java
                    ? (List<Accumulator>) handler.get(handler.columnCount(row) - 1, row) : Collections.emptyList();

            for (int i = 0; i < accums.size(); i++) {
                AccumulatorWrapper<RowT> wrapper = accumWrps.get(i);

```

### DataFlowIssue
Unboxing of `handler.get(0, row)` may produce `NullPointerException`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/HashAggregateNode.java`
#### Snippet
```java

        private void addOnReducer(RowT row) {
            byte targetGrpId = (byte) handler.get(0, row);

            if (targetGrpId != grpId) {
```

### DataFlowIssue
Method invocation `get` may produce `NullPointerException`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/HashAggregateNode.java`
#### Snippet
```java
            for (int i = 0; i < wrappers.size(); i++) {
                AccumulatorWrapper<RowT> wrapper = wrappers.get(i);
                Accumulator accum = accums.get(i);

                wrapper.apply(accum);
```

### DataFlowIssue
Method invocation `iterator` may produce `NullPointerException`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/Inbox.java`
#### Snippet
```java
    @SuppressWarnings("LabeledStatement")
    private void pushOrdered() throws Exception {
        if (!checkAllBuffsReady(remoteSources.iterator())) {
            for (RemoteSource<RowT> remote : remoteSources) {
                remote.requestNextBatchIfNeeded();
```

### DataFlowIssue
Dereference of `heap.poll()` may produce `NullPointerException`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/Inbox.java`
#### Snippet
```java
                checkState();

                RemoteSource<RowT> source = heap.poll().right;

                requested--;
```

### DataFlowIssue
Method invocation `remove` may produce `NullPointerException`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/Inbox.java`
#### Snippet
```java

                requested--;
                downstream().push(source.remove());

                State state = source.check();
```

### DataFlowIssue
Method invocation `isEmpty` may produce `NullPointerException`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/Inbox.java`
#### Snippet
```java
        inLoop = true;
        try {
            while (requested > 0 && !remoteSources.isEmpty()) {
                checkState();

```

### DataFlowIssue
Dereference of `remoteSources` may produce `NullPointerException`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/Inbox.java`
#### Snippet
```java
        }

        for (RemoteSource<?> source : remoteSources) {
            source.requestNextBatchIfNeeded();
        }
```

### DataFlowIssue
Method invocation `size` may produce `NullPointerException`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/MergeJoinNode.java`
#### Snippet
```java
                        }

                        if (rightIdx >= rightMaterialization.size()) {
                            rightIdx = 0;
                            left = null;
```

### DataFlowIssue
Method invocation `size` may produce `NullPointerException`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/MergeJoinNode.java`
#### Snippet
```java
                        }

                        if (rightIdx >= rightMaterialization.size()) {
                            rightIdx = 0;
                            left = null;
```

### DataFlowIssue
Method invocation `size` may produce `NullPointerException`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/MergeJoinNode.java`
#### Snippet
```java
                        }
                    } else {
                        if (rightIdx >= rightMaterialization.size()) {
                            rightIdx = 0;
                            left = null;
```

### DataFlowIssue
Method invocation `size` may produce `NullPointerException`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/rel/MergeJoinNode.java`
#### Snippet
```java
                        }
                    } else {
                        if (rightIdx >= rightMaterialization.size()) {
                            rightIdx = 0;
                            left = null;
```

### DataFlowIssue
Method invocation `getIndex` may produce `NullPointerException`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rule/LogicalScanConverterRule.java`
#### Snippet
```java
                    RelOptCluster cluster = rel.getCluster();
                    IgniteTable table = rel.getTable().unwrap(IgniteTable.class);
                    IgniteIndex index = table.getIndex(rel.indexName());

                    RelDistribution distribution = table.distribution();
```

### DataFlowIssue
Method invocation `distribution` may produce `NullPointerException`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rule/LogicalScanConverterRule.java`
#### Snippet
```java
                    IgniteTable table = rel.getTable().unwrap(IgniteTable.class);

                    RelDistribution distribution = table.distribution();
                    if (rel.requiredColumns() != null) {
                        Mappings.TargetMapping mapping = createMapping(
```

### DataFlowIssue
Method invocation `indexes` may produce `NullPointerException`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rule/logical/ExposeIndexRule.java`
#### Snippet
```java
    private static boolean preMatch(IgniteLogicalTableScan scan) {
        // has indexes to expose
        return !scan.getTable().unwrap(IgniteTable.class).indexes().isEmpty();
    }

```

### DataFlowIssue
Method invocation `indexes` may produce `NullPointerException`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rule/logical/ExposeIndexRule.java`
#### Snippet
```java
        ImmutableBitSet requiredCols = scan.requiredColumns();

        List<IgniteLogicalIndexScan> indexes = igniteTable.indexes().keySet().stream()
                .map(idxName -> igniteTable.toRel(cluster, optTable, idxName, proj, condition, requiredCols))
                .collect(Collectors.toList());
```

### DataFlowIssue
Method invocation `getRowType` may produce `NullPointerException`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rule/logical/LogicalOrToUnionRule.java`
#### Snippet
```java
        IgniteTable tbl = scan.getTable().unwrap(IgniteTable.class);
        IgniteTypeFactory typeFactory = Commons.typeFactory(scan.getCluster());
        int fieldCnt = tbl.getRowType(typeFactory).getFieldCount();

        BitSet idxsFirstFields = new BitSet(fieldCnt);
```

### DataFlowIssue
Argument `call.getPlanner().getExecutor()` might be null
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rule/logical/FilterScanMergeRule.java`
#### Snippet
```java
        // We need to replace RexLocalRef with RexInputRef because "simplify" doesn't understand local refs.
        condition = RexUtils.replaceLocalRefs(condition);
        condition = new RexSimplify(builder, RelOptPredicateList.EMPTY, call.getPlanner().getExecutor())
                .simplifyUnknownAsFalse(condition);

```

### DataFlowIssue
Method invocation `getRowType` may produce `NullPointerException`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rule/logical/ProjectScanMergeRule.java`
#### Snippet
```java

            Mappings.TargetMapping targetMapping = Commons.trimmingMapping(
                    tbl.getRowType(typeFactory).getFieldCount(), requiredColumns);

            projects = new RexShuttle() {
```

### DataFlowIssue
Method invocation `getRowType` may produce `NullPointerException`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/rule/logical/ProjectScanMergeRule.java`
#### Snippet
```java
        }

        if (RexUtils.isIdentity(projects, tbl.getRowType(typeFactory, requiredColumns), true)) {
            projects = null;
        }
```

### DataFlowIssue
Dereference of `p` may produce `NullPointerException`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/RexImpTable.java`
#### Snippet
```java
        if (Primitive.is(type)) {
            Primitive p = Primitive.of(type);
            return Expressions.constant(p.defaultValue, type);
        }
        return Expressions.constant(null, type);
```

### DataFlowIssue
Dereference of `Primitive.ofBox(x.getType())` may produce `NullPointerException`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/RexImpTable.java`
#### Snippet
```java
                        case NOT_POSSIBLE:
                            return ConverterUtils.convert(x,
                                    Primitive.ofBox(x.getType()).primitiveClass);
                        default:
                            // No-Op.
```

### DataFlowIssue
Dereference of `EMPTY_CONTEXT.config().getTraitDefs()` may produce `NullPointerException`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/util/BaseQueryContext.java`
#### Snippet
```java

        // Dummy planner must contain all trait definitions to create singleton cluster with all default traits.
        for (RelTraitDef<?> def : EMPTY_CONTEXT.config().getTraitDefs()) {
            DUMMY_PLANNER.addRelTraitDef(def);
        }
```

### DataFlowIssue
Method invocation `root` may produce `NullPointerException`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/prepare/QueryTemplate.java`
#### Snippet
```java

        Exception ex = null;
        RelMetadataQuery mq = first(fragments).root().getCluster().getMetadataQuery();
        for (int i = 0; i < 3; i++) {
            try {
```

### DataFlowIssue
Method reference invocation `Column::columnOrder` may produce `NullPointerException`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/schema/SqlSchemaManagerImpl.java`
#### Snippet
```java
        List<ColumnDescriptor> colDescriptors = descriptor.columnNames().stream()
                .map(descriptor::column)
                .sorted(Comparator.comparingInt(Column::columnOrder))
                .map(col -> new ColumnDescriptorImpl(
                        col.name(),
```

### DataFlowIssue
Argument `Commons.context(rel).config().getCostFactory()` might be null
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/prepare/IgnitePrograms.java`
#### Snippet
```java

            final HepPlanner hepPlanner = new HepPlanner(builder.build(), Commons.context(rel), true,
                    null, Commons.context(rel).config().getCostFactory());

            hepPlanner.setExecutor(planner.getExecutor());
```

### DataFlowIssue
Method invocation `unwrap` may produce `NullPointerException`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/prepare/IgniteSqlValidator.java`
#### Snippet
```java
        final SqlValidatorTable table = getCatalogReader().getTable(((SqlIdentifier) call.getTargetTable()).names);

        table.unwrap(IgniteTable.class).descriptor().deleteRowType((IgniteTypeFactory) typeFactory)
                .getFieldNames().stream()
                .map(name -> new SqlIdentifier(name, SqlParserPos.ZERO))
```

### DataFlowIssue
Method invocation `descriptor` may produce `NullPointerException`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/prepare/IgniteSqlValidator.java`
#### Snippet
```java
        final SqlValidatorTable table = getCatalogReader().getTable(((SqlIdentifier) call.getTargetTable()).names);

        table.unwrap(IgniteTable.class).descriptor().deleteRowType((IgniteTypeFactory) typeFactory)
                .getFieldNames().stream()
                .map(name -> new SqlIdentifier(name, SqlParserPos.ZERO))
```

### DataFlowIssue
Method invocation `compareTo` may produce `NullPointerException`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/prepare/IgniteSqlValidator.java`
#### Snippet
```java
            BigDecimal offFetchLimit = ((SqlLiteral) n).bigDecimalValue();

            if (offFetchLimit.compareTo(DEC_INT_MAX) > 0 || offFetchLimit.compareTo(BigDecimal.ZERO) < 0) {
                throw newValidationError(n, IgniteResource.INSTANCE.correctIntegerLimit(nodeName));
            }
```

### DataFlowIssue
Method invocation `unwrap` may produce `NullPointerException`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/prepare/IgniteSqlValidator.java`
#### Snippet
```java
                new SqlIdentifier(deriveAlias(targetTable, 0), SqlParserPos.ZERO);

        table.unwrap(IgniteTable.class).descriptor().selectForUpdateRowType((IgniteTypeFactory) typeFactory)
                .getFieldNames().stream()
                .map(name -> alias.plus(name, SqlParserPos.ZERO))
```

### DataFlowIssue
Argument `sarg` might be null
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/util/RexUtils.java`
#### Snippet
```java
                Sarg<?> sarg = ((RexLiteral) pred.operands.get(1)).getValueAs(Sarg.class);

                List<SearchBounds> bounds = expandSargToBounds(fc, cluster, fldType, prevComplexity, sarg, ref);

                if (bounds == null) {
```

### DataFlowIssue
Method invocation `descriptor` may produce `NullPointerException`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/prepare/IgniteSqlValidator.java`
#### Snippet
```java
                new SqlIdentifier(deriveAlias(targetTable, 0), SqlParserPos.ZERO);

        table.unwrap(IgniteTable.class).descriptor().selectForUpdateRowType((IgniteTypeFactory) typeFactory)
                .getFieldNames().stream()
                .map(name -> alias.plus(name, SqlParserPos.ZERO))
```

### DataFlowIssue
Method invocation `getDirection` may produce `NullPointerException`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/util/RexUtils.java`
#### Snippet
```java
                    if (bounds.get(0) instanceof RangeBounds && collFldPreds.size() > 1) {
                        // Try to merge bounds.
                        boolean ascDir = !fc.getDirection().isDescending();
                        RangeBounds rangeBounds = (RangeBounds) bounds.get(0);
                        if (rangeBounds.lowerBound() != null) {
```

### DataFlowIssue
Method invocation `getDirection` may produce `NullPointerException`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/util/RexUtils.java`
#### Snippet
```java

            // Range bounds.
            boolean lowerBoundBelow = !fc.getDirection().isDescending();
            boolean includeBound = op.kind == GREATER_THAN_OR_EQUAL || op.kind == LESS_THAN_OR_EQUAL;
            boolean lessCondition = false;
```

### DataFlowIssue
Unboxing of `((RexLiteral) n).getValueAs(Integer.class)` may produce `NullPointerException`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/util/RexUtils.java`
#### Snippet
```java
        try {
            if (n.isA(SqlKind.LITERAL)) {
                return ((RexLiteral) n).getValueAs(Integer.class);
            } else {
                return def;
```

### DataFlowIssue
Argument `ctx.config().getTraitDefs()` might be null
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/prepare/PrepareServiceImpl.java`
#### Snippet
```java

    private CompletableFuture<QueryPlan> prepareQuery(SqlNode sqlNode, PlanningContext ctx) {
        boolean distributed = distributionPresent(ctx.config().getTraitDefs());

        Class[] paramTypes = ctx.parameters().length == 0
```

### DataFlowIssue
Unboxing of `v` may produce `NullPointerException`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/prepare/IgnitePlanner.java`
#### Snippet
```java
                    node = visitChild(node, 1, correlate.getRight());
                } finally {
                    removeMap.compute(corId, (k, v) -> v == 1 ? null : v - 1);
                }

```

### DataFlowIssue
Method invocation `plannerTimeout` may produce `NullPointerException`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/prepare/IgnitePlanner.java`
#### Snippet
```java
            PlanningContext ctx = getContext().unwrap(PlanningContext.class);

            long timeout = ctx.plannerTimeout();

            if (timeout > 0) {
```

### DataFlowIssue
Method invocation `distribution` may produce `NullPointerException`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/metadata/IgniteMdDistribution.java`
#### Snippet
```java
     */
    public IgniteDistribution distribution(TableScan rel, RelMetadataQuery mq) {
        return rel.getTable().unwrap(IgniteTable.class).distribution();
    }

```

### DataFlowIssue
Method invocation `isInfinite` may produce `NullPointerException`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/metadata/IgniteMdCumulativeCost.java`
#### Snippet
```java

        RelOptCost leftCost = mq.getCumulativeCost(left);
        if (leftCost.isInfinite()) {
            return leftCost;
        }
```

### DataFlowIssue
Method invocation `isInfinite` may produce `NullPointerException`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/metadata/IgniteMdCumulativeCost.java`
#### Snippet
```java

        RelOptCost rightCost = mq.getCumulativeCost(right);
        if (rightCost.isInfinite()) {
            return rightCost;
        }
```

### DataFlowIssue
Method invocation `isInfinite` may produce `NullPointerException`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/metadata/IgniteMdCumulativeCost.java`
#### Snippet
```java
        RelOptCost cost = mq.getNonCumulativeCost(rel);

        if (cost.isInfinite()) {
            return cost;
        }
```

### DataFlowIssue
Method invocation `isInfinite` may produce `NullPointerException`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/metadata/IgniteMdCumulativeCost.java`
#### Snippet
```java
        for (RelNode input : rel.getInputs()) {
            RelOptCost inputCost = mq.getCumulativeCost(input);
            if (inputCost.isInfinite()) {
                return inputCost;
            }
```

### DataFlowIssue
Unboxing of `mq.getSelectivity(rel, rel.getCondition())` may produce `NullPointerException`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/metadata/IgniteMdRowCount.java`
#### Snippet
```java
        ImmutableIntList rightKeys = joinInfo.rightKeys;

        double selectivity = mq.getSelectivity(rel, rel.getCondition());

        if (nullOrEmpty(leftKeys) || nullOrEmpty(rightKeys)) {
```

### DataFlowIssue
Method invocation `satisfies` may produce `NullPointerException`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/trait/TraitUtils.java`
#### Snippet
```java
        RelTrait fromTrait = rel.getTraitSet().getTrait(converter);

        if (fromTrait.satisfies(toTrait)) {
            return rel;
        }
```

### DataFlowIssue
Method invocation `colocationGroup` may produce `NullPointerException`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/metadata/IgniteMdFragmentMapping.java`
#### Snippet
```java
        // from replica that is not primary anymore.
        List<NodeWithTerm> assignments = rel.getTable().unwrap(IgniteTable.class)
                .colocationGroup(ctx).assignments().stream()
                .map(CollectionUtils::first)
                .collect(Collectors.toList());
```

### DataFlowIssue
Method invocation `isLt` may produce `NullPointerException`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/metadata/IgniteMdFragmentMapping.java`
#### Snippet
```java
            RelOptCost rightVarCost = mq.getCumulativeCost(rightVar);

            if (leftVarCost.isLt(rightVarCost)) {
                throw new NodeMappingException("Failed to calculate physical distribution", left, e);
            } else {
```

### DataFlowIssue
Method invocation `colocationGroup` may produce `NullPointerException`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/metadata/IgniteMdFragmentMapping.java`
#### Snippet
```java

    private static FragmentMapping getFragmentMapping(long sourceId, ProjectableFilterableTableScan rel, MappingQueryContext ctx) {
        ColocationGroup group = rel.getTable().unwrap(IgniteTable.class).colocationGroup(ctx);

        return FragmentMapping.create(sourceId, group);
```

### DataFlowIssue
Expression `first(groups)` might evaluate to null but is returned by the method declared as @NotNull
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/metadata/FragmentMapping.java`
#### Snippet
```java
        }

        return first(groups);
    }
}
```

### DataFlowIssue
Method invocation `prune` may produce `NullPointerException`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/metadata/FragmentMapping.java`
#### Snippet
```java
        }

        return new FragmentMapping(updatingTableAssignments, List.of(first(colocationGroups).prune(rel)));
    }

```

### DataFlowIssue
Argument `firstNotNull(first, second)` might be null
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/metadata/FragmentMapping.java`
#### Snippet
```java
            return new FragmentMapping(updatingTableAssignments, List.of());
        } else if (first == null || second == null) {
            return new FragmentMapping(updatingTableAssignments, List.of(firstNotNull(first, second)));
        } else {
            return new FragmentMapping(updatingTableAssignments, List.of(first.colocate(second)));
```

### DataFlowIssue
Method invocation `stream` may produce `NullPointerException`
in `modules/metastorage/src/main/java/org/apache/ignite/internal/metastorage/impl/MetaStorageRaftGroupEventsListener.java`
#### Snippet
```java

    private static boolean isPeer(RaftGroupService raftService, ClusterNode node) {
        return raftService.peers().stream().anyMatch(peer -> peer.consistentId().equals(node.name()));
    }

```

### DataFlowIssue
Method invocation `consistentId` may produce `NullPointerException`
in `modules/metastorage/src/main/java/org/apache/ignite/internal/metastorage/impl/MetaStorageRaftGroupEventsListener.java`
#### Snippet
```java
        return metaStorageSvcFut.thenCompose(service -> service.raftGroupService().refreshAndGetLeaderWithTerm()
                .thenCompose(leaderWithTerm -> {
                    String leaderName = leaderWithTerm.leader().consistentId();

                    return leaderName.equals(nodeName) ? action.apply(service, leaderWithTerm.term()) : completedFuture(null);
```

### DataFlowIssue
Method invocation `stream` may produce `NullPointerException`
in `modules/metastorage/src/main/java/org/apache/ignite/internal/metastorage/impl/MetaStorageRaftGroupEventsListener.java`
#### Snippet
```java
                    RaftGroupService raftService = service.raftGroupService();

                    Set<String> peers = raftService.peers().stream().map(Peer::consistentId).collect(toSet());

                    Set<String> learners = validatedNodes.stream()
```

### DataFlowIssue
Dereference of `leftCollations` may produce `NullPointerException`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/metadata/IgniteMdCollation.java`
#### Snippet
```java
            case RIGHT:
            case FULL:
                for (RelCollation collation : leftCollations) {
                    for (RelFieldCollation field : collation.getFieldCollations()) {
                        if (!(RelFieldCollation.NullDirection.LAST == field.nullDirection)) {
```

### DataFlowIssue
Argument `leftCollations` might be null
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/metadata/IgniteMdCollation.java`
#### Snippet
```java

        final List<RelCollation> leftCollations = mq.collations(left);
        assert RelCollations.contains(leftCollations, leftKeys)
                : "cannot merge join: left input is not sorted on left keys";
        if (!joinType.projectsRight()) {
```

### DataFlowIssue
Argument `rightCollations` might be null
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/metadata/IgniteMdCollation.java`
#### Snippet
```java

        final List<RelCollation> rightCollations = mq.collations(right);
        assert RelCollations.contains(rightCollations, rightKeys)
                : "cannot merge join: right input is not sorted on right keys";
        final int leftFieldCount = left.getRowType().getFieldCount();
```

### DataFlowIssue
Method invocation `key` may produce `NullPointerException`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/prepare/ddl/DdlSqlToCommandConverter.java`
#### Snippet
```java
        for (SqlNode optionNode : createZoneNode.createOptionList().getList()) {
            IgniteSqlZoneOption option = (IgniteSqlZoneOption) optionNode;
            IgniteSqlZoneOptionEnum optionName = option.key().symbolValue(IgniteSqlZoneOptionEnum.class);

            if (!knownOptionNames.remove(optionName)) {
```

### DataFlowIssue
Method invocation `key` may produce `NullPointerException`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/prepare/ddl/DdlSqlToCommandConverter.java`
#### Snippet
```java
                IgniteSqlCreateTableOption option = (IgniteSqlCreateTableOption) optionNode;

                assert option.key().isSimple() : option.key();

                String optionKey = option.key().getSimple().toUpperCase();
```

### DataFlowIssue
Method reference invocation `SqlIdentifier::getSimple` may produce `NullPointerException`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/prepare/ddl/DdlSqlToCommandConverter.java`
#### Snippet
```java
                .flatMap(l -> l.getList().stream())
                .map(SqlIdentifier.class::cast)
                .map(SqlIdentifier::getSimple)
                .filter(dedupSetPk::add)
                .collect(Collectors.toList());
```

### DataFlowIssue
Method reference invocation `SqlIdentifier::getSimple` may produce `NullPointerException`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/prepare/ddl/DdlSqlToCommandConverter.java`
#### Snippet
```java
                : createTblNode.colocationColumns().getList().stream()
                        .map(SqlIdentifier.class::cast)
                        .map(SqlIdentifier::getSimple)
                        .collect(Collectors.toList());

```

### DataFlowIssue
Method invocation `getKind` may produce `NullPointerException`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/prepare/ddl/DdlSqlToCommandConverter.java`
#### Snippet
```java
            boolean desc = false;

            if (col.getKind() == SqlKind.DESCENDING) {
                col = ((SqlCall) col).getOperandList().get(0);

```

### DataFlowIssue
Method invocation `key` may produce `NullPointerException`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/prepare/ddl/DdlSqlToCommandConverter.java`
#### Snippet
```java
        for (SqlNode optionNode : alterZoneSet.alterOptionsList().getList()) {
            IgniteSqlZoneOption option = (IgniteSqlZoneOption) optionNode;
            IgniteSqlZoneOptionEnum optionName = option.key().symbolValue(IgniteSqlZoneOptionEnum.class);

            if (!knownOptionNames.remove(optionName)) {
```

### DataFlowIssue
Argument `clazz` might be null
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/externalize/RelJson.java`
#### Snippet
```java
        }

        assert RelNode.class.isAssignableFrom(clazz);

        Constructor<RelNode> constructor;
```

### DataFlowIssue
Casting `distribution` to `Map` may produce `ClassCastException`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/externalize/RelJson.java`
#### Snippet
```java
        }

        Map<String, Object> map = (Map<String, Object>) distribution;
        String tableIdStr = (String) map.get("tableId");

```

### DataFlowIssue
Argument `classForName(cls, false)` might be null
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/externalize/RelJson.java`
#### Snippet
```java
            String cls = (String) map.get("class");
            String name = map.get("name").toString();
            return Util.enumVal((Class<T>) classForName(cls, false), name);
        }

```

### DataFlowIssue
Argument `data.get(rocksKey)` might be null
in `modules/metastorage/src/main/java/org/apache/ignite/internal/metastorage/server/persistence/RocksDbKeyValueStorage.java`
#### Snippet
```java
        byte[] rocksKey = keyToRocksKey(lastRev, key);

        Value value = bytesToValue(data.get(rocksKey));

        if (value.tombstone()) {
```

### DataFlowIssue
Argument `data.get(REVISION_KEY)` might be null
in `modules/metastorage/src/main/java/org/apache/ignite/internal/metastorage/server/persistence/RocksDbKeyValueStorage.java`
#### Snippet
```java
            snapshotManager.restoreSnapshot(path);

            currentRevision = bytesToLong(data.get(REVISION_KEY));

            rev = currentRevision;
```

### DataFlowIssue
Argument `data.get(UPDATE_COUNTER_KEY)` might be null
in `modules/metastorage/src/main/java/org/apache/ignite/internal/metastorage/server/persistence/RocksDbKeyValueStorage.java`
#### Snippet
```java
            rev = currentRevision;

            updCntr = bytesToLong(data.get(UPDATE_COUNTER_KEY));
        } catch (Exception e) {
            throw new MetaStorageException(RESTORING_STORAGE_ERR, "Failed to restore snapshot", e);
```

### DataFlowIssue
Method invocation `addForRecycle` may produce `NullPointerException`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/freelist/AbstractFreeList.java`
#### Snippet
```java
            }

            reuseList.addForRecycle(bag);
        } catch (AssertionError e) {
            throw corruptedFreeListException(e);
```

### DataFlowIssue
Method invocation `takeRecycledPage` may produce `NullPointerException`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/freelist/AbstractFreeList.java`
#### Snippet
```java
                pageId = takeEmptyPage(REUSE_BUCKET, row.ioVersions(), statHolder);
            } else {
                pageId = reuseList.takeRecycledPage();

                if (pageId != 0) {
```

### DataFlowIssue
Method invocation `close` may produce `NullPointerException`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/store/AbstractFilePageStoreIo.java`
#### Snippet
```java
                // Ensure the file is closed even if not initialized yet.
                if (fileIo != null) {
                    fileIo.close();
                }

```

### DataFlowIssue
Method invocation `force` may produce `NullPointerException`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/store/AbstractFilePageStoreIo.java`
#### Snippet
```java
            }

            fileIo.force();

            fileIo.close();
```

### DataFlowIssue
Method invocation `close` may produce `NullPointerException`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/store/AbstractFilePageStoreIo.java`
#### Snippet
```java
            fileIo.force();

            fileIo.close();

            fileIo = null;
```

### DataFlowIssue
Method invocation `size` may produce `NullPointerException`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/store/AbstractFilePageStoreIo.java`
#### Snippet
```java
                    throw new IgniteInternalDataIntegrityViolationException("Failed to read page (CRC validation failed) "
                            + "[id=" + hexLong(pageId) + ", off=" + pageOff
                            + ", filePath=" + filePath + ", fileSize=" + fileIo.size()
                            + ", savedCrc=" + hexInt(savedCrc32) + ", curCrc=" + hexInt(curCrc32)
                            + ", page=" + toHexString(pageBuf) + "]");
```

### DataFlowIssue
Method invocation `writeFully` may produce `NullPointerException`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/store/AbstractFilePageStoreIo.java`
#### Snippet
```java
                    long pageOff = pageOffset(pageId);

                    fileIo.writeFully(pageBuf, pageOff);

                    PageIo.setCrc(pageBuf, 0);
```

### DataFlowIssue
Method invocation `addForRecycle` may produce `NullPointerException`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/freelist/PagesList.java`
#### Snippet
```java

            if (recycleId != 0L) {
                reuseList.addForRecycle(new SingletonReuseBag(recycleId));
            }

```

### DataFlowIssue
Method invocation `addForRecycle` may produce `NullPointerException`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/freelist/PagesList.java`
#### Snippet
```java
                    assert !isReuseBucket(bucket);

                    reuseList.addForRecycle(new SingletonReuseBag(recycleId));
                }

```

### DataFlowIssue
Argument `getBucketCache(bucket, true)` might be null
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/freelist/PagesList.java`
#### Snippet
```java
        }

        if (bag == null && onheapListCachingEnabled && putDataPage(getBucketCache(bucket, true), dataId, dataAddr, bucket)) {
            // Successfully put page to the onheap pages list cache.
            log.debug("Put page to pages list cache [list={}, bucket={}, dataId={}]", name(), bucket, dataId);
```

### DataFlowIssue
Method invocation `fail` may produce `NullPointerException`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/checkpoint/Checkpointer.java`
#### Snippet
```java
            } catch (Exception e) {
                if (currentCheckpointProgress != null) {
                    currentCheckpointProgress.fail(e);
                }

```

### DataFlowIssue
Method invocation `syncedPagesCounter` may produce `NullPointerException`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/checkpoint/Checkpointer.java`
#### Snippet
```java
        }

        currentCheckpointProgress.syncedPagesCounter().addAndGet(pagesWritten.intValue());
    }

```

### DataFlowIssue
Unboxing of `lastLongPause.get1()` may produce `NullPointerException`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/checkpoint/Checkpointer.java`
#### Snippet
```java
                IgniteBiTuple<Long, Long> lastLongPause = pauseDetector.getLastLongPause();

                if (lastLongPause != null && tracker.checkpointStartTime() < lastLongPause.get1()) {
                    return lastLongPause.get2();
                }
```

### DataFlowIssue
Unboxing of `lastLongPause.get2()` may produce `NullPointerException`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/checkpoint/Checkpointer.java`
#### Snippet
```java

                if (lastLongPause != null && tracker.checkpointStartTime() < lastLongPause.get1()) {
                    return lastLongPause.get2();
                }

```

### DataFlowIssue
Argument `pagesToWrite.getPartitionView(pageMemory, pageId.groupId(), pageId.partitionId())` might be null
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/checkpoint/CheckpointManager.java`
#### Snippet
```java
        CompletableFuture<DeltaFilePageStoreIo> deltaFilePageStoreFuture = filePageStore.getOrCreateNewDeltaFile(
                index -> filePageStoreManager.tmpDeltaFilePageStorePath(pageId.groupId(), pageId.partitionId(), index),
                () -> pageIndexesForDeltaFilePageStore(pagesToWrite.getPartitionView(pageMemory, pageId.groupId(), pageId.partitionId()))
        );

```

### DataFlowIssue
Unboxing of `val.get1()` may produce `NullPointerException`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/checkpoint/ReentrantReadWriteLockWithTracking.java`
#### Snippet
```java
            IgniteBiTuple<Integer, Long> val = readLockHolderTs.get();

            int cntr = val.get1();

            if (--cntr == 0) {
```

### DataFlowIssue
Unboxing of `val.get2()` may produce `NullPointerException`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/checkpoint/ReentrantReadWriteLockWithTracking.java`
#### Snippet
```java

            if (--cntr == 0) {
                long timeout = coarseCurrentTimeMillis() - val.get2();

                if (timeout > readLockThreshold) {
```

### DataFlowIssue
Unboxing of `val.get1()` may produce `NullPointerException`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/checkpoint/ReentrantReadWriteLockWithTracking.java`
#### Snippet
```java
            IgniteBiTuple<Integer, Long> val = readLockHolderTs.get();

            int cntr = val.get1();

            if (cntr == 0) {
```

### DataFlowIssue
Function may return null, but it's not allowed here
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/checkpoint/CheckpointWorkflow.java`
#### Snippet
```java
        return listeners.stream()
                .filter(tuple -> tuple.getValue() == null || dataRegions.contains(tuple.getValue()))
                .map(IgniteBiTuple::getKey)
                .collect(toUnmodifiableList());
    }
```

### DataFlowIssue
Method invocation `releaseFreePage` may produce `NullPointerException`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/PersistentPageMemory.java`
#### Snippet
```java

    private void releaseCheckpointBufferPage(long tmpBufPtr) {
        checkpointPool.releaseFreePage(tmpBufPtr);
    }

```

### DataFlowIssue
Method invocation `absolute` may produce `NullPointerException`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/PersistentPageMemory.java`
#### Snippet
```java

            if (tmpBufPtr != INVALID_REL_PTR) {
                zeroMemory(checkpointPool.absolute(tmpBufPtr) + PAGE_OVERHEAD, pageSize());

                tempBufferPointer(absPtr, INVALID_REL_PTR);
```

### DataFlowIssue
Method invocation `readLock` may produce `NullPointerException`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/PersistentPageMemory.java`
#### Snippet
```java

        for (Segment seg : segments) {
            seg.readLock().lock();

            try {
```

### DataFlowIssue
Method invocation `writeLock` may produce `NullPointerException`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/PersistentPageMemory.java`
#### Snippet
```java

            for (Segment segment : segments) {
                segment.writeLock().lock();

                try {
```

### DataFlowIssue
Dereference of `seg` may produce `NullPointerException`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/PersistentPageMemory.java`
#### Snippet
```java

        for (Segment seg : segments) {
            res.addAll(seg.dirtyPages);
        }

```

### DataFlowIssue
Method invocation `acquiredPages` may produce `NullPointerException`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/PersistentPageMemory.java`
#### Snippet
```java

        for (Segment seg : segments) {
            total += seg.acquiredPages();
        }

```

### DataFlowIssue
Method invocation `close` may produce `NullPointerException`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/PersistentPageMemory.java`
#### Snippet
```java
            if (segments != null) {
                for (Segment seg : segments) {
                    seg.close();
                }
            }
```

### DataFlowIssue
Dereference of `checkpointPool` may produce `NullPointerException`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/PersistentPageMemory.java`
#### Snippet
```java
     */
    public FullPageId pullPageFromCpBuffer() {
        long idx = getLong(checkpointPool.lastAllocatedIdxPtr);

        long lastIdx = ThreadLocalRandom.current().nextLong(idx / 2, idx);
```

### DataFlowIssue
Method invocation `relative` may produce `NullPointerException`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/PersistentPageMemory.java`
#### Snippet
```java
            assert (lastIdx & SEGMENT_INDEX_MASK) == 0L;

            long relative = checkpointPool.relative(lastIdx);

            long freePageAbsPtr = checkpointPool.absolute(relative);
```

### DataFlowIssue
Method invocation `absolute` may produce `NullPointerException`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/PersistentPageMemory.java`
#### Snippet
```java
            long relative = checkpointPool.relative(lastIdx);

            long freePageAbsPtr = checkpointPool.absolute(relative);

            FullPageId fullPageId = fullPageId(freePageAbsPtr);
```

### DataFlowIssue
Method invocation `pages` may produce `NullPointerException`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/PersistentPageMemory.java`
#### Snippet
```java

        for (Segment segment : segments) {
            res += segment.pages();
        }

```

### DataFlowIssue
Method invocation `borrowOrAllocateFreePage` may produce `NullPointerException`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/PersistentPageMemory.java`
#### Snippet
```java
        // Create a buffer copy if the page is scheduled for a checkpoint.
        if (isInCheckpoint(fullId) && tempBufferPointer(absPtr) == INVALID_REL_PTR) {
            long tmpRelPtr = checkpointPool.borrowOrAllocateFreePage(tag(fullId.pageId()));

            if (tmpRelPtr == INVALID_REL_PTR) {
```

### DataFlowIssue
Method invocation `absolute` may produce `NullPointerException`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/PersistentPageMemory.java`
#### Snippet
```java
            PageHeader.acquirePage(absPtr);

            long tmpAbsPtr = checkpointPool.absolute(tmpRelPtr);

            copyMemory(
```

### DataFlowIssue
Dereference of `seg` may produce `NullPointerException`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/PersistentPageMemory.java`
#### Snippet
```java
        synchronized (segmentsLock) {
            for (Segment seg : segments) {
                seg.checkpointPages = null;
            }
        }
```

### DataFlowIssue
Method invocation `writeLock` may produce `NullPointerException`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/PersistentPageMemory.java`
#### Snippet
```java
    public void onGroupDestroyed(int grpId) {
        for (Segment seg : segments) {
            seg.writeLock().lock();

            try {
```

### DataFlowIssue
Method invocation `absolute` may produce `NullPointerException`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/PersistentPageMemory.java`
#### Snippet
```java
                tempBufferPointer(absPtr, INVALID_REL_PTR);

                long tmpAbsPtr = checkpointPool.absolute(tmpRelPtr);

                copyInBuffer(tmpAbsPtr, buf);
```

### DataFlowIssue
Argument `indexColumnView` might be null
in `modules/storage-api/src/main/java/org/apache/ignite/internal/storage/index/SortedIndexDescriptor.java`
#### Snippet
```java
                    IndexColumnView indexColumnView = indexColumns.get(columnName);

                    return new SortedIndexColumnDescriptor(columnView, indexColumnView);
                })
                .collect(toUnmodifiableList());
```

### DataFlowIssue
Switch label `HASH_INDEX_TYPE` is unreachable
in `modules/storage-api/src/main/java/org/apache/ignite/internal/storage/engine/MvTableStorage.java`
#### Snippet
```java

        switch (indexConfig.type().value()) {
            case HASH_INDEX_TYPE:
                return getOrCreateHashIndex(partitionId, indexId);
            case SORTED_INDEX_TYPE:
```

### DataFlowIssue
Switch label `SORTED_INDEX_TYPE` is unreachable
in `modules/storage-api/src/main/java/org/apache/ignite/internal/storage/engine/MvTableStorage.java`
#### Snippet
```java
            case HASH_INDEX_TYPE:
                return getOrCreateHashIndex(partitionId, indexId);
            case SORTED_INDEX_TYPE:
                return getOrCreateSortedIndex(partitionId, indexId);
            default:
```

### DataFlowIssue
Method invocation `byteBuffer` may produce `NullPointerException`
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/AbstractMvPartitionStorageConcurrencyTest.java`
#### Snippet
```java

            runRace(
                    () -> assertRemoveRow(pollForVacuum(HybridTimestamp.MAX_VALUE).binaryRow().byteBuffer(), rows),
                    () -> assertRemoveRow(pollForVacuum(HybridTimestamp.MAX_VALUE).binaryRow().byteBuffer(), rows)
            );
```

### DataFlowIssue
Method invocation `byteBuffer` may produce `NullPointerException`
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/AbstractMvPartitionStorageConcurrencyTest.java`
#### Snippet
```java
            runRace(
                    () -> assertRemoveRow(pollForVacuum(HybridTimestamp.MAX_VALUE).binaryRow().byteBuffer(), rows),
                    () -> assertRemoveRow(pollForVacuum(HybridTimestamp.MAX_VALUE).binaryRow().byteBuffer(), rows)
            );

```

### DataFlowIssue
Method invocation `name` may produce `NullPointerException`
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/BaseMvStoragesTest.java`
#### Snippet
```java
                    Column column = schemaDescriptor.column(indexColumnDescriptor.name());

                    assertNotNull(column, column.name());

                    return column.schemaIndex();
```

### DataFlowIssue
Unboxing of `hasNext` may produce `NullPointerException`
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/impl/TestMvPartitionStorage.java`
#### Snippet
```java
            advanceIfNeeded();

            return hasNext;
        }

```

### DataFlowIssue
Unboxing of `hasNext` may produce `NullPointerException`
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/impl/TestMvPartitionStorage.java`
#### Snippet
```java
            advanceIfNeeded();

            if (!hasNext) {
                throw new NoSuchElementException();
            }
```

### DataFlowIssue
Argument `versionChain` might be null
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/impl/TestMvPartitionStorage.java`
#### Snippet
```java
            hasNext = null;

            return versionChainToReadResult(versionChain, false);
        }

```

### DataFlowIssue
Argument `firstCommit.ts` might be null
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/impl/TestMvPartitionStorage.java`
#### Snippet
```java
        boolean hasWriteIntent = chainHead.ts == null;

        if (hasWriteIntent && timestamp.compareTo(firstCommit.ts) > 0) {
            // It's the latest commit in chain, query ts is greater than commit ts and there is a write-intent.
            // So we just return write-intent.
```

### DataFlowIssue
Method invocation `compareTo` may produce `NullPointerException`
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/impl/TestMvPartitionStorage.java`
#### Snippet
```java
        VersionChain dequeuedVersionChain = it.next();

        if (dequeuedVersionChain.ts.compareTo(lowWatermark) > 0) {
            return null;
        }
```

### DataFlowIssue
Method invocation `getKey` may produce `NullPointerException`
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/index/impl/TestSortedIndexStorage.java`
#### Snippet
```java
            if (hasNext != null) {
                if (hasNext) {
                    return new IndexRowImpl(new BinaryTuple(descriptor.binaryTupleSchema(), currentEntry.getKey()), rowId);
                }

```

### DataFlowIssue
Unboxing of `this.hasNext` may produce `NullPointerException`
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/index/impl/TestSortedIndexStorage.java`
#### Snippet
```java
            advanceIfNeeded();

            boolean hasNext = this.hasNext;

            if (!hasNext) {
```

### DataFlowIssue
Method invocation `getKey` may produce `NullPointerException`
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/index/impl/TestSortedIndexStorage.java`
#### Snippet
```java
            this.hasNext = null;

            return new IndexRowImpl(new BinaryTuple(descriptor.binaryTupleSchema(), currentEntry.getKey()), rowId);
        }

```

### DataFlowIssue
Unboxing of `hasNext` may produce `NullPointerException`
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/index/impl/TestSortedIndexStorage.java`
#### Snippet
```java
            advanceIfNeeded();

            return hasNext;
        }

```

### DataFlowIssue
Method invocation `bytes` may produce `NullPointerException`
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/AbstractMvTableStorageTest.java`
#### Snippet
```java
            assertThat(
                    toListOfByteArrays(mvPartitionStorage.scanVersions(row.get1())),
                    containsInAnyOrder(row.get2().bytes())
            );

```

### DataFlowIssue
Method reference invocation `BinaryRow::bytes` may produce `NullPointerException`
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/AbstractMvTableStorageTest.java`
#### Snippet
```java
    private static List<byte[]> toListOfByteArrays(Cursor<ReadResult> cursor) {
        try (cursor) {
            return cursor.stream().map(ReadResult::binaryRow).map(BinaryRow::bytes).collect(toList());
        }
    }
```

### DataFlowIssue
Method invocation `partitionId` may produce `NullPointerException`
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/AbstractMvTableStorageTest.java`
#### Snippet
```java
            mvPartitionStorage.runConsistently(() -> {
                if ((finalI % 2) == 0) {
                    mvPartitionStorage.addWrite(rowId, binaryRow, UUID.randomUUID(), UUID.randomUUID(), rowId.partitionId());

                    mvPartitionStorage.commitWrite(rowId, timestamp);
```

### DataFlowIssue
Method invocation `get1` may produce `NullPointerException`
in `modules/transactions/src/main/java/org/apache/ignite/internal/tx/impl/HeapLockManager.java`
#### Snippet
```java
            IgniteBiTuple<CompletableFuture<Void>, LockMode> futureTuple = state.tryAcquire(txId, lockMode);

            if (futureTuple.get1() == null) {
                continue; // Obsolete state.
            }
```

### DataFlowIssue
Argument `txMeta` might be null
in `modules/transactions/src/testFixtures/java/org/apache/ignite/internal/tx/storage/state/AbstractTxStateStorageTest.java`
#### Snippet
```java
            TxMeta txMeta = storage.get(txIds.get(i));
            TxMeta txMetaExpected = new TxMeta(TxState.COMMITED, generateEnlistedPartitions(i), generateTimestamp(txIds.get(i)));
            assertTxMetaEquals(txMetaExpected, txMeta);
        }

```

### DataFlowIssue
Argument `txMeta` might be null
in `modules/transactions/src/testFixtures/java/org/apache/ignite/internal/tx/storage/state/AbstractTxStateStorageTest.java`
#### Snippet
```java
                TxMeta txMeta = storage.get(txIds.get(i));
                TxMeta txMetaExpected = new TxMeta(TxState.COMMITED, generateEnlistedPartitions(i), generateTimestamp(txIds.get(i)));
                assertTxMetaEquals(txMetaExpected, txMeta);
            }
        }
```

### DataFlowIssue
Argument `storage.get(txId)` might be null
in `modules/transactions/src/testFixtures/java/org/apache/ignite/internal/tx/storage/state/AbstractTxStateStorageTest.java`
#### Snippet
```java
        assertFalse(storage.compareAndSet(txId, TxState.ABORTED, txMetaNullTimestamp0, 3, 2));

        assertTxMetaEquals(storage.get(txId), txMeta1);

        assertTrue(storage.compareAndSet(txId, txMeta1.txState(), txMeta2, 3, 2));
```

### DataFlowIssue
Argument `storage.get(txId)` might be null
in `modules/transactions/src/testFixtures/java/org/apache/ignite/internal/tx/storage/state/AbstractTxStateStorageTest.java`
#### Snippet
```java
        assertFalse(storage.compareAndSet(txId, TxState.ABORTED, txMetaNullTimestamp2, 3, 2));

        assertTxMetaEquals(storage.get(txId), txMeta2);
    }

```

### DataFlowIssue
@Nullable method 'visitLeafNode' always returns a non-null value
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/ConfigurationNode.java`
#### Snippet
```java

            /** {@inheritDoc} */
            @Nullable
            @Override
            public List<KeyPathNode> visitLeafNode(String key, Serializable val) {
```

### DataFlowIssue
Method invocation `toString` may produce `NullPointerException`
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/ConfigurationNode.java`
#### Snippet
```java

                // This is important, node is added as a resolved named list entry here.
                res.add(new KeyPathNode(innerNode.internalId().toString(), false));

                if (keys.size() == idx) {
```

### DataFlowIssue
Argument `node.getInnerNode(namedListKey)` might be null
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/ConfigurationChanger.java`
#### Snippet
```java
                if (node.makeImmutable()) {
                    for (String namedListKey : node.namedListKeys()) {
                        makeImmutable(node.getInnerNode(namedListKey));
                    }
                }
```

### DataFlowIssue
Method invocation `accept` may produce `NullPointerException`
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/tree/ConverterToMapVisitor.java`
#### Snippet
```java

        for (String subkey : node.namedListKeys()) {
            node.getInnerNode(subkey).accept(subkey, this);

            ((Map<String, Object>) list.get(list.size() - 1)).put(node.syntheticKeyName(), subkey);
```

### DataFlowIssue
Method invocation `internalId` may produce `NullPointerException`
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/tree/NamedListNode.java`
#### Snippet
```java

            element.internalId = internalId;
            element.value.internalId(internalId);

            reverseIdMap.put(internalId, key);
```

### DataFlowIssue
Method invocation `construct` may produce `NullPointerException`
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/tree/NamedListNode.java`
#### Snippet
```java

                    if (polymorphicTypeId != null) {
                        polymorphicInnerNode.construct(typeIdFieldName, new LeafConfigurationSource(polymorphicTypeId), true);
                    } else {
                        // check if the Type ID node has already been created by the 'setDefaults' method
```

### DataFlowIssue
Method invocation `traverseChild` may produce `NullPointerException`
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/tree/NamedListNode.java`
#### Snippet
```java
                    } else {
                        // check if the Type ID node has already been created by the 'setDefaults' method
                        Object typeIdNode = polymorphicInnerNode.traverseChild(typeIdFieldName, leafNodeVisitor(), true);

                        if (typeIdNode == null) {
```

### DataFlowIssue
Method invocation `construct` may produce `NullPointerException`
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/tree/NamedListNode.java`
#### Snippet
```java

                    if (polymorphicTypeId != null) {
                        polymorphicInnerNode.construct(typeIdFieldName, new LeafConfigurationSource(polymorphicTypeId), true);
                    }
                }
```

### DataFlowIssue
Method invocation `setInjectedNameFieldValue` may produce `NullPointerException`
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/tree/NamedListNode.java`
#### Snippet
```java
            InnerNode value = element.value;

            value.setInjectedNameFieldValue(key);

            src.descend(value);
```

### DataFlowIssue
Method invocation `copy` may produce `NullPointerException`
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/tree/NamedListNode.java`
#### Snippet
```java
         */
        public ElementDescriptor copy() {
            return new ElementDescriptor(internalId, value.copy());
        }

```

### DataFlowIssue
Method invocation `setInjectedNameFieldValue` may produce `NullPointerException`
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/tree/NamedListNode.java`
#### Snippet
```java
        reverseIdMap.put(element.internalId, newKey);

        element.value.setInjectedNameFieldValue(newKey);

        return this;
```

### DataFlowIssue
Method invocation `getName` may produce `NullPointerException`
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/asm/ConfigurationAsmGenerator.java`
#### Snippet
```java
                constantString(syntheticKeyName),
                newNamedListElementLambda(fieldClassNames.nodeClassName),
                isPolymorphicConfig(fieldType) ? constantString(polymorphicIdField(fieldType).getName()) : constantNull(String.class)
        );
    }
```

### DataFlowIssue
Argument `node.getInnerNode(namedListKey)` might be null
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/util/KeysTrackingConfigurationVisitor.java`
#### Snippet
```java

            try {
                doVisitInnerNode(namedListKey, node.getInnerNode(namedListKey));
            } finally {
                endVisit(prevPos);
```

### DataFlowIssue
Argument `namedList.getInnerNode(namedListKey)` might be null
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/util/ConfigurationUtil.java`
#### Snippet
```java
                        namedList.construct(namedListKey, EMPTY_CFG_SRC, true);

                        addDefaults(namedList.getInnerNode(namedListKey));
                    }
                }
```

### DataFlowIssue
Method invocation `internalId` may produce `NullPointerException`
in `modules/configuration/src/testFixtures/java/org/apache/ignite/internal/configuration/testframework/ConfigurationExtension.java`
#### Snippet
```java
            InnerNode root = superRoot.getRoot(rootKey);

            root.internalId(UUID.randomUUID());
            root.setInjectedNameFieldValue(annotation.name());
        }
```

### DataFlowIssue
@Nullable method 'findSchemaField' always returns a non-null value
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/validation/ValidationUtil.java`
#### Snippet
```java
    }

    private static @Nullable Field findSchemaField(InnerNode innerNode, String schemaFieldName) throws NoSuchFieldException {
        Class<?> schemaType = innerNode.schemaType();

```

### DataFlowIssue
Method invocation `getDeclaredFields` may produce `NullPointerException`
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/validation/ValidationUtil.java`
#### Snippet
```java
            // Linear search to not fight with NoSuchFieldException.
            for (Class<?> internalSchemaType : innerNode.internalSchemaTypes()) {
                for (Field field : internalSchemaType.getDeclaredFields()) {
                    if (field.getName().equals(schemaFieldName)) {
                        return field;
```

### DataFlowIssue
Method invocation `version` may produce `NullPointerException`
in `modules/client-handler/src/main/java/org/apache/ignite/client/handler/requests/table/ClientTableCommon.java`
#### Snippet
```java
            }

            assert schema.version() == ((SchemaAware) tuple).schema().version();

            packer.packBoolean(true);
```

### DataFlowIssue
Method invocation `version` may produce `NullPointerException`
in `modules/client-handler/src/main/java/org/apache/ignite/client/handler/requests/table/ClientTableCommon.java`
#### Snippet
```java
        for (Tuple tuple : tuples) {
            assert tuple != null;
            assert schema.version() == ((SchemaAware) tuple).schema().version();

            writeTuple(packer, tuple, skipHeader, part);
```

### DataFlowIssue
Unboxing of `correlationId` may produce `NullPointerException`
in `modules/code-deployment/src/main/java/org/apache/ignite/internal/deployunit/DeploymentManagerImpl.java`
#### Snippet
```java
                (message, senderConsistentId, correlationId) -> {
                    if (message instanceof DeployUnitRequest) {
                        processDeployRequest((DeployUnitRequest) message, senderConsistentId, correlationId);
                    } else if (message instanceof UndeployUnitRequest) {
                        processUndeployRequest((UndeployUnitRequest) message, senderConsistentId, correlationId);
```

### DataFlowIssue
Unboxing of `correlationId` may produce `NullPointerException`
in `modules/code-deployment/src/main/java/org/apache/ignite/internal/deployunit/DeploymentManagerImpl.java`
#### Snippet
```java
                        processDeployRequest((DeployUnitRequest) message, senderConsistentId, correlationId);
                    } else if (message instanceof UndeployUnitRequest) {
                        processUndeployRequest((UndeployUnitRequest) message, senderConsistentId, correlationId);
                    }
                });
```

### DataFlowIssue
Switch label `ROCKSDB_CLOCK_CACHE` is unreachable
in `modules/storage-rocksdb/src/main/java/org/apache/ignite/internal/storage/rocksdb/RocksDbDataRegion.java`
#### Snippet
```java

        switch (dataRegionView.cache().toLowerCase(Locale.ROOT)) {
            case ROCKSDB_CLOCK_CACHE:
                cache = new ClockCache(totalCacheSize, dataRegionView.numShardBits(), false);

```

### DataFlowIssue
Switch label `ROCKSDB_LRU_CACHE` is unreachable
in `modules/storage-rocksdb/src/main/java/org/apache/ignite/internal/storage/rocksdb/RocksDbDataRegion.java`
#### Snippet
```java
                break;

            case ROCKSDB_LRU_CACHE:
                cache = new LRUCache(totalCacheSize, dataRegionView.numShardBits(), false);

```

### DataFlowIssue
Unboxing of `this.hasNext` may produce `NullPointerException`
in `modules/storage-rocksdb/src/main/java/org/apache/ignite/internal/storage/rocksdb/index/RocksDbSortedIndexStorage.java`
#### Snippet
```java
                    advanceIfNeeded();

                    boolean hasNext = this.hasNext;

                    if (!hasNext) {
```

### DataFlowIssue
Argument `key` might be null
in `modules/storage-rocksdb/src/main/java/org/apache/ignite/internal/storage/rocksdb/index/RocksDbSortedIndexStorage.java`
#### Snippet
```java
                    this.hasNext = null;

                    return mapper.apply(ByteBuffer.wrap(key).order(ORDER));
                });
            }
```

### DataFlowIssue
Argument `key` might be null
in `modules/storage-rocksdb/src/main/java/org/apache/ignite/internal/storage/rocksdb/index/RocksDbSortedIndexStorage.java`
#### Snippet
```java
                    if (hasNext != null) {
                        if (hasNext) {
                            return mapper.apply(ByteBuffer.wrap(key).order(ORDER));
                        }

```

### DataFlowIssue
Dereference of `peers()` may produce `NullPointerException`
in `modules/placement-driver/src/main/java/org/apache/ignite/internal/raft/client/TopologyAwareRaftGroupService.java`
#### Snippet
```java
            @Override
            public void onNodeJoined(ClusterNode appearedNode, LogicalTopologySnapshot newTopology) {
                for (Peer peer : peers()) {
                    if (serverEventHandler.isSubscribed() && appearedNode.name().equals(peer.consistentId())) {
                        LOG.info("New peer will be sending a leader elected notification [grpId={}, consistentId={}]", groupId(),
```

### DataFlowIssue
Method invocation `consistentId` may produce `NullPointerException`
in `modules/placement-driver/src/main/java/org/apache/ignite/internal/raft/client/TopologyAwareRaftGroupService.java`
#### Snippet
```java
                                                .thenAcceptAsync(leaderWithTerm -> {
                                                    if (leaderWithTerm.leader() != null
                                                            && appearedNode.name().equals(leaderWithTerm.leader().consistentId())) {
                                                        serverEventHandler.onLeaderElected(appearedNode, leaderWithTerm.term());
                                                    }
```

### DataFlowIssue
Method invocation `size` may produce `NullPointerException`
in `modules/placement-driver/src/main/java/org/apache/ignite/internal/raft/client/TopologyAwareRaftGroupService.java`
#### Snippet
```java
        assert !serverEventHandler.isSubscribed() : "The node already subscribed";

        int peers = peers().size();

        var futs = new CompletableFuture[peers];
```

### DataFlowIssue
Method invocation `get` may produce `NullPointerException`
in `modules/placement-driver/src/main/java/org/apache/ignite/internal/raft/client/TopologyAwareRaftGroupService.java`
#### Snippet
```java

        for (int i = 0; i < peers; i++) {
            Peer peer = peers().get(i);

            ClusterNode node = clusterService.topologyService().getByConsistentId(peer.consistentId());
```

### DataFlowIssue
Method invocation `consistentId` may produce `NullPointerException`
in `modules/placement-driver/src/main/java/org/apache/ignite/internal/raft/client/TopologyAwareRaftGroupService.java`
#### Snippet
```java
                    if (leaderWithTerm.leader() != null) {
                        serverEventHandler.onLeaderElected(
                                clusterService.topologyService().getByConsistentId(leaderWithTerm.leader().consistentId()),
                                leaderWithTerm.term()
                        );
```

### DataFlowIssue
Method invocation `size` may produce `NullPointerException`
in `modules/placement-driver/src/main/java/org/apache/ignite/internal/raft/client/TopologyAwareRaftGroupService.java`
#### Snippet
```java

        var peers = peers();
        var futs = new CompletableFuture[peers.size()];

        for (int i = 0; i < peers.size(); i++) {
```

### DataFlowIssue
Method invocation `id` may produce `NullPointerException`
in `modules/placement-driver/src/main/java/org/apache/ignite/internal/placementdriver/AssignmentsTracker.java`
#### Snippet
```java
            ExtendedTableConfiguration tblCfg = assignmentsCtx.config(ExtendedTableConfiguration.class);

            UUID tblId = tblCfg.id().value();

            LOG.debug("Table assignments configuration update for placement driver [revision={}, tblId={}]",
```

### DataFlowIssue
Argument `mode` might be null
in `modules/marshaller-common/src/main/java/org/apache/ignite/internal/marshaller/Marshaller.java`
#### Snippet
```java
        assert !mapper.targetType().isPrimitive() : "Non-nullable types are not allowed.";

        return new SimpleMarshaller(FieldAccessor.createIdentityAccessor(col.name(), 0, mode));
    }

```

### DataFlowIssue
Method invocation `set` may produce `NullPointerException`
in `modules/marshaller-common/src/main/java/org/apache/ignite/internal/marshaller/FieldAccessor.java`
#### Snippet
```java
            final float val = reader.readFloat();

            varHandle.set(obj, val);
        }
    }
```

### DataFlowIssue
Method invocation `set` may produce `NullPointerException`
in `modules/marshaller-common/src/main/java/org/apache/ignite/internal/marshaller/FieldAccessor.java`
#### Snippet
```java
            final int val = reader.readInt();

            varHandle.set(obj, val);
        }
    }
```

### DataFlowIssue
Method invocation `set` may produce `NullPointerException`
in `modules/marshaller-common/src/main/java/org/apache/ignite/internal/marshaller/FieldAccessor.java`
#### Snippet
```java
            final byte val = reader.readByte();

            varHandle.set(obj, val);
        }
    }
```

### DataFlowIssue
Method invocation `get` may produce `NullPointerException`
in `modules/marshaller-common/src/main/java/org/apache/ignite/internal/marshaller/FieldAccessor.java`
#### Snippet
```java
        @Override
        protected void write0(MarshallerWriter writer, Object obj) {
            final double val = (double) varHandle.get(obj);

            writer.writeDouble(val);
```

### DataFlowIssue
Method invocation `get` may produce `NullPointerException`
in `modules/marshaller-common/src/main/java/org/apache/ignite/internal/marshaller/FieldAccessor.java`
#### Snippet
```java
        @Override
        protected void write0(MarshallerWriter writer, Object obj) {
            final short val = (short) varHandle.get(obj);

            writer.writeShort(val);
```

### DataFlowIssue
Method invocation `set` may produce `NullPointerException`
in `modules/marshaller-common/src/main/java/org/apache/ignite/internal/marshaller/FieldAccessor.java`
#### Snippet
```java

    public void set(Object obj, Object val) {
        varHandle.set(obj, val);
    }

```

### DataFlowIssue
Method invocation `get` may produce `NullPointerException`
in `modules/marshaller-common/src/main/java/org/apache/ignite/internal/marshaller/FieldAccessor.java`
#### Snippet
```java
            assert writer != null;

            Object val = varHandle.get(obj);

            if (val == null) {
```

### DataFlowIssue
Method invocation `get` may produce `NullPointerException`
in `modules/marshaller-common/src/main/java/org/apache/ignite/internal/marshaller/FieldAccessor.java`
#### Snippet
```java
        @Override
        protected void write0(MarshallerWriter writer, Object obj) {
            final byte val = (byte) varHandle.get(obj);

            writer.writeByte(val);
```

### DataFlowIssue
Method invocation `get` may produce `NullPointerException`
in `modules/marshaller-common/src/main/java/org/apache/ignite/internal/marshaller/FieldAccessor.java`
#### Snippet
```java

    public Object get(Object obj) {
        return varHandle.get(obj);
    }

```

### DataFlowIssue
Method invocation `set` may produce `NullPointerException`
in `modules/marshaller-common/src/main/java/org/apache/ignite/internal/marshaller/FieldAccessor.java`
#### Snippet
```java
            final long val = reader.readLong();

            varHandle.set(obj, val);
        }
    }
```

### DataFlowIssue
Method invocation `set` may produce `NullPointerException`
in `modules/marshaller-common/src/main/java/org/apache/ignite/internal/marshaller/FieldAccessor.java`
#### Snippet
```java
            Object val = readRefValue(reader, colIdx, mode, scale);

            varHandle.set(obj, val);
        }
    }
```

### DataFlowIssue
Method invocation `set` may produce `NullPointerException`
in `modules/marshaller-common/src/main/java/org/apache/ignite/internal/marshaller/FieldAccessor.java`
#### Snippet
```java
            final double val = reader.readDouble();

            varHandle.set(obj, val);
        }
    }
```

### DataFlowIssue
Method invocation `get` may produce `NullPointerException`
in `modules/marshaller-common/src/main/java/org/apache/ignite/internal/marshaller/FieldAccessor.java`
#### Snippet
```java
        @Override
        protected void write0(MarshallerWriter writer, Object obj) {
            final float val = (float) varHandle.get(obj);

            writer.writeFloat(val);
```

### DataFlowIssue
Method invocation `get` may produce `NullPointerException`
in `modules/marshaller-common/src/main/java/org/apache/ignite/internal/marshaller/FieldAccessor.java`
#### Snippet
```java
        @Override
        protected void write0(MarshallerWriter writer, Object obj) {
            final long val = (long) varHandle.get(obj);

            writer.writeLong(val);
```

### DataFlowIssue
Method invocation `get` may produce `NullPointerException`
in `modules/marshaller-common/src/main/java/org/apache/ignite/internal/marshaller/FieldAccessor.java`
#### Snippet
```java
     */
    Object value(Object obj) {
        return varHandle.get(Objects.requireNonNull(obj));
    }

```

### DataFlowIssue
Method invocation `set` may produce `NullPointerException`
in `modules/marshaller-common/src/main/java/org/apache/ignite/internal/marshaller/FieldAccessor.java`
#### Snippet
```java
            final short val = reader.readShort();

            varHandle.set(obj, val);
        }
    }
```

### DataFlowIssue
Method invocation `get` may produce `NullPointerException`
in `modules/marshaller-common/src/main/java/org/apache/ignite/internal/marshaller/FieldAccessor.java`
#### Snippet
```java
        @Override
        protected void write0(MarshallerWriter writer, Object obj) {
            final int val = (int) varHandle.get(obj);

            writer.writeInt(val);
```

### DataFlowIssue
Method invocation `delete` may produce `NullPointerException`
in `modules/cluster-management/src/main/java/org/apache/ignite/internal/cluster/management/raft/RocksDbClusterStateStorage.java`
#### Snippet
```java
    public void remove(byte[] key) {
        try {
            db.delete(key);
        } catch (RocksDBException e) {
            throw new IgniteInternalException("Unable to remove data from Rocks DB", e);
```

### DataFlowIssue
Method invocation `newIterator` may produce `NullPointerException`
in `modules/cluster-management/src/main/java/org/apache/ignite/internal/cluster/management/raft/RocksDbClusterStateStorage.java`
#### Snippet
```java
        ReadOptions readOptions = new ReadOptions().setIterateUpperBound(upperBoundSlice);

        RocksIterator it = db.newIterator(readOptions);

        it.seek(prefix);
```

### DataFlowIssue
Method invocation `get` may produce `NullPointerException`
in `modules/cluster-management/src/main/java/org/apache/ignite/internal/cluster/management/raft/RocksDbClusterStateStorage.java`
#### Snippet
```java
    public byte @Nullable [] get(byte[] key) {
        try {
            return db.get(key);
        } catch (RocksDBException e) {
            throw new IgniteInternalException("Unable to get data from Rocks DB", e);
```

### DataFlowIssue
Method invocation `put` may produce `NullPointerException`
in `modules/cluster-management/src/main/java/org/apache/ignite/internal/cluster/management/raft/RocksDbClusterStateStorage.java`
#### Snippet
```java
    public void put(byte[] key, byte[] value) {
        try {
            db.put(key, value);
        } catch (RocksDBException e) {
            throw new IgniteInternalException("Unable to put data into Rocks DB", e);
```

### DataFlowIssue
Method invocation `write` may produce `NullPointerException`
in `modules/cluster-management/src/main/java/org/apache/ignite/internal/cluster/management/raft/RocksDbClusterStateStorage.java`
#### Snippet
```java
            }

            db.write(options, batch);
        } catch (RocksDBException e) {
            throw new IgniteInternalException("Unable to remove data from Rocks DB", e);
```

### DataFlowIssue
Method invocation `write` may produce `NullPointerException`
in `modules/cluster-management/src/main/java/org/apache/ignite/internal/cluster/management/raft/RocksDbClusterStateStorage.java`
#### Snippet
```java
            batch.put(key, value);

            db.write(options, batch);
        } catch (RocksDBException e) {
            throw new IgniteInternalException("Unable to replace data in Rocks DB", e);
```

### DataFlowIssue
Argument `options` might be null
in `modules/cluster-management/src/main/java/org/apache/ignite/internal/cluster/management/raft/RocksDbClusterStateStorage.java`
#### Snippet
```java

        try {
            db = RocksDB.open(options, dbPath.toString());

            ColumnFamily defaultCf = ColumnFamily.wrap(db, db.getDefaultColumnFamily());
```

### DataFlowIssue
Method invocation `getDefaultColumnFamily` may produce `NullPointerException`
in `modules/cluster-management/src/main/java/org/apache/ignite/internal/cluster/management/raft/RocksDbClusterStateStorage.java`
#### Snippet
```java
            db = RocksDB.open(options, dbPath.toString());

            ColumnFamily defaultCf = ColumnFamily.wrap(db, db.getDefaultColumnFamily());

            snapshotManager = new RocksSnapshotManager(db, List.of(fullRange(defaultCf)), snapshotExecutor);
```

### DataFlowIssue
Method invocation `handle` may produce `NullPointerException`
in `modules/cluster-management/src/main/java/org/apache/ignite/internal/cluster/management/ClusterManagementGroupManager.java`
#### Snippet
```java
                // case we need to re-create the service.
                raftService = raftService
                        .handle((service, e) -> {
                            if (service != null && service.nodeNames().equals(state.cmgNodes())) {
                                LOG.info("ClusterStateMessage received, but the CMG service is already started");
```

### DataFlowIssue
Method invocation `cancel` may produce `NullPointerException`
in `modules/cluster-management/src/main/java/org/apache/ignite/internal/cluster/management/ClusterManagementGroupManager.java`
#### Snippet
```java
            try {
                if (raftService != null) {
                    raftService.cancel(true);

                    raftService = null;
```

### DataFlowIssue
Method invocation `getName` may produce `NullPointerException`
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/asm/InnerNodeAsmGenerator.java`
#### Snippet
```java
        MethodDefinition internalIdMtd = innerNodeClassDef.declareMethod(
                EnumSet.of(PUBLIC),
                internalIdField.getName(),
                type(UUID.class)
        );
```

### DataFlowIssue
Method invocation `getName` may produce `NullPointerException`
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/asm/InnerNodeAsmGenerator.java`
#### Snippet
```java
        }

        FieldDefinition polymorphicTypeIdFieldDef = fieldDefs.get(polymorphicIdField(schemaClass).getName());

        // Creates view and change methods for specific polymorphic instance schema.
```

### DataFlowIssue
Argument `ctx.config(VolatilePageMemoryDataRegionConfiguration.class)` might be null
in `modules/storage-page-memory/src/main/java/org/apache/ignite/internal/storage/pagememory/VolatilePageMemoryStorageEngine.java`
#### Snippet
```java
            @Override
            public CompletableFuture<?> onCreate(ConfigurationNotificationEvent<VolatilePageMemoryDataRegionView> ctx) {
                addDataRegion(ctx.config(VolatilePageMemoryDataRegionConfiguration.class));

                return completedFuture(null);
```

### DataFlowIssue
Method invocation `start` may produce `NullPointerException`
in `modules/storage-page-memory/src/main/java/org/apache/ignite/internal/storage/pagememory/PersistentPageMemoryStorageEngine.java`
#### Snippet
```java
            );

            filePageStoreManager.start();
        } catch (IgniteInternalCheckedException e) {
            throw new StorageException("Error starting file page store manager", e);
```

### DataFlowIssue
Method invocation `start` may produce `NullPointerException`
in `modules/storage-page-memory/src/main/java/org/apache/ignite/internal/storage/pagememory/PersistentPageMemoryStorageEngine.java`
#### Snippet
```java
            );

            checkpointManager.start();
        } catch (IgniteInternalCheckedException e) {
            throw new StorageException("Error starting checkpoint manager", e);
```

### DataFlowIssue
Argument `ctx.config(PersistentPageMemoryDataRegionConfiguration.class)` might be null
in `modules/storage-page-memory/src/main/java/org/apache/ignite/internal/storage/pagememory/PersistentPageMemoryStorageEngine.java`
#### Snippet
```java
            @Override
            public CompletableFuture<?> onCreate(ConfigurationNotificationEvent<PersistentPageMemoryDataRegionView> ctx) {
                addDataRegion(ctx.config(PersistentPageMemoryDataRegionConfiguration.class));

                return completedFuture(null);
```

### DataFlowIssue
Method invocation `limit` may produce `NullPointerException`
in `modules/storage-page-memory/src/main/java/org/apache/ignite/internal/storage/pagememory/mv/RowVersion.java`
#### Snippet
```java
        assert value != null;

        return headerSize() + value.limit();
    }

```

### DataFlowIssue
Method invocation `isMarkedToDestroy` may produce `NullPointerException`
in `modules/storage-page-memory/src/main/java/org/apache/ignite/internal/storage/pagememory/PersistentPageMemoryTableStorage.java`
#### Snippet
```java
            FilePageStore filePageStore = dataRegion.filePageStoreManager().getStore(groupPartitionId);

            assert !filePageStore.isMarkedToDestroy() : IgniteStringFormatter.format(
                    "Should not be marked for deletion: [tableName={}, tableId={}, partitionId={}]",
                    tableView.name(),
```

### DataFlowIssue
Method invocation `markToDestroy` may produce `NullPointerException`
in `modules/storage-page-memory/src/main/java/org/apache/ignite/internal/storage/pagememory/PersistentPageMemoryTableStorage.java`
#### Snippet
```java

    private CompletableFuture<Void> destroyPartitionPhysically(GroupPartitionId groupPartitionId) {
        dataRegion.filePageStoreManager().getStore(groupPartitionId).markToDestroy();

        dataRegion.pageMemory().invalidate(groupPartitionId.getGroupId(), groupPartitionId.getPartitionId());
```

### DataFlowIssue
Method invocation `addForRecycle` may produce `NullPointerException`
in `modules/storage-page-memory/src/main/java/org/apache/ignite/internal/storage/pagememory/mv/BlobStorage.java`
#### Snippet
```java
    private void freePagesStartingWith(long pageId) throws IgniteInternalCheckedException {
        if (pageId != NO_PAGE_ID) {
            reuseList.addForRecycle(recycleAndCollectPagesStartingWith(pageId));
        }
    }
```

### DataFlowIssue
Dereference of `state.bytes` may produce `NullPointerException`
in `modules/storage-page-memory/src/main/java/org/apache/ignite/internal/storage/pagememory/mv/BlobStorage.java`
#### Snippet
```java

            int capacityForBytes = blobIo.getCapacityForFragmentBytes(pageSize(), state.isFirstPage());
            int fragmentLength = Math.min(capacityForBytes, state.bytes.length - state.bytesOffset);

            blobIo.getFragmentBytes(pageAddr, state.isFirstPage(), state.bytes, state.bytesOffset, fragmentLength);
```

### DataFlowIssue
Dereference of `state.bytes` may produce `NullPointerException`
in `modules/storage-page-memory/src/main/java/org/apache/ignite/internal/storage/pagememory/mv/BlobStorage.java`
#### Snippet
```java
            int newBytesOffset = state.bytesOffset + fragmentLength;

            if (newBytesOffset < state.bytes.length) {
                assert nextPageId != NO_PAGE_ID;

```

### DataFlowIssue
Method invocation `rowId` may produce `NullPointerException`
in `modules/storage-page-memory/src/main/java/org/apache/ignite/internal/storage/pagememory/mv/RemoveWriteOnGcInvokeClosure.java`
#### Snippet
```java
                        "Error updating the next link: [rowId={}, timestamp={}, rowLink={}, nextLink={}, {}]",
                        e,
                        newRow.rowId(), timestamp, toUpdate.link(), result.nextLink(), storage.createStorageInfo()
                );
            }
```

### DataFlowIssue
Method invocation `hasNext` may produce `NullPointerException`
in `modules/storage-page-memory/src/main/java/org/apache/ignite/internal/storage/pagememory/mv/AbstractPartitionTimestampCursor.java`
#### Snippet
```java

            while (true) {
                if (!cursor.hasNext()) {
                    iterationExhausted = true;

```

### DataFlowIssue
Method invocation `isEmpty` may produce `NullPointerException`
in `modules/storage-page-memory/src/main/java/org/apache/ignite/internal/storage/pagememory/mv/AbstractPartitionTimestampCursor.java`
#### Snippet
```java
            );

            if (result.isEmpty()) {
                return null;
            }
```

### DataFlowIssue
Method invocation `checkpointTimeoutLock` may produce `NullPointerException`
in `modules/storage-page-memory/src/main/java/org/apache/ignite/internal/storage/pagememory/mv/PersistentPageMemoryMvPartitionStorage.java`
#### Snippet
```java

        checkpointManager = tableStorage.engine().checkpointManager();
        checkpointTimeoutLock = checkpointManager.checkpointTimeoutLock();

        DataRegion<PersistentPageMemory> dataRegion = tableStorage.dataRegion();
```

### DataFlowIssue
Method invocation `compareTo` may produce `NullPointerException`
in `modules/storage-page-memory/src/main/java/org/apache/ignite/internal/storage/pagememory/mv/gc/io/GcIo.java`
#### Snippet
```java
        HybridTimestamp readTimestamp = HybridTimestamps.readTimestamp(pageAddr, offset + ROW_TIMESTAMP_OFFSET);

        int cmp = readTimestamp.compareTo(rowVersion.getTimestamp());

        if (cmp != 0) {
```

### DataFlowIssue
Argument `row.value()` might be null
in `modules/storage-page-memory/src/main/java/org/apache/ignite/internal/storage/pagememory/mv/io/RowVersionDataIo.java`
#### Snippet
```java
            pageBuf.putInt(row.valueSize());

            putValueBufferIntoPage(pageBuf, row.value(), 0, payloadSize - row.headerSize());
        } else {
            // non-first fragment
```

### DataFlowIssue
Argument `row.value()` might be null
in `modules/storage-page-memory/src/main/java/org/apache/ignite/internal/storage/pagememory/mv/io/RowVersionDataIo.java`
#### Snippet
```java
            assert rowOff >= row.headerSize();

            putValueBufferIntoPage(pageBuf, row.value(), rowOff - row.headerSize(), payloadSize);
        }
    }
```

### DataFlowIssue
Argument `row.value()` might be null
in `modules/storage-page-memory/src/main/java/org/apache/ignite/internal/storage/pagememory/mv/io/RowVersionDataIo.java`
#### Snippet
```java
        addr += Integer.BYTES;

        putByteBuffer(addr, 0, row.value());
    }

```

### DataFlowIssue
Argument `rowVersion.value()` might be null
in `modules/storage-page-memory/src/main/java/org/apache/ignite/internal/storage/pagememory/mv/AbstractPageMemoryMvPartitionStorage.java`
#### Snippet
```java
        }

        return new ByteBufferRow(rowVersion.value());
    }

```

### DataFlowIssue
Argument `curCommit.value()` might be null
in `modules/storage-page-memory/src/main/java/org/apache/ignite/internal/storage/pagememory/mv/AbstractPageMemoryMvPartitionStorage.java`
#### Snippet
```java
                    row = null;
                } else {
                    row = new ByteBufferRow(curCommit.value());
                }

```

### DataFlowIssue
Argument `rowVersion.value()` might be null
in `modules/storage-page-memory/src/main/java/org/apache/ignite/internal/storage/pagememory/mv/AbstractPageMemoryMvPartitionStorage.java`
#### Snippet
```java
                return ReadResult.empty(rowId);
            } else {
                BinaryRow row = new ByteBufferRow(rowVersion.value());

                return ReadResult.createFromCommitted(rowId, row, rowVersion.timestamp());
```

### DataFlowIssue
Argument `rowVersion.value()` might be null
in `modules/storage-page-memory/src/main/java/org/apache/ignite/internal/storage/pagememory/mv/AbstractPageMemoryMvPartitionStorage.java`
#### Snippet
```java
            }
        } else {
            BinaryRow row = rowVersion.isTombstone() ? null : new ByteBufferRow(rowVersion.value());

            return ReadResult.createFromWriteIntent(
```

### DataFlowIssue
Argument `treeRow` might be null
in `modules/storage-page-memory/src/main/java/org/apache/ignite/internal/storage/pagememory/index/sorted/PageMemorySortedIndexStorage.java`
#### Snippet
```java
                if (hasNext != null) {
                    if (hasNext) {
                        return toIndexRowImpl(treeRow);
                    }

```

### DataFlowIssue
Unboxing of `this.hasNext` may produce `NullPointerException`
in `modules/storage-page-memory/src/main/java/org/apache/ignite/internal/storage/pagememory/index/sorted/PageMemorySortedIndexStorage.java`
#### Snippet
```java
                    advanceIfNeeded();

                    boolean hasNext = this.hasNext;

                    if (!hasNext) {
```

### DataFlowIssue
Argument `treeRow` might be null
in `modules/storage-page-memory/src/main/java/org/apache/ignite/internal/storage/pagememory/index/sorted/PageMemorySortedIndexStorage.java`
#### Snippet
```java
                    this.hasNext = null;

                    return toIndexRowImpl(treeRow);
                } catch (IgniteInternalCheckedException e) {
                    throw new StorageException("Error while advancing the cursor", e);
```

### DataFlowIssue
Method invocation `zoneId` may produce `NullPointerException`
in `modules/distribution-zones/src/main/java/org/apache/ignite/internal/distributionzones/DistributionZoneManager.java`
#### Snippet
```java
        @Override
        public CompletableFuture<?> onDelete(ConfigurationNotificationEvent<DistributionZoneView> ctx) {
            int zoneId = ctx.oldValue().zoneId();

            zonesState.get(zoneId).stopTimers();
```

### DataFlowIssue
Method invocation `zoneId` may produce `NullPointerException`
in `modules/distribution-zones/src/main/java/org/apache/ignite/internal/distributionzones/DistributionZoneManager.java`
#### Snippet
```java
    private ConfigurationListener<Integer> onUpdateScaleDown() {
        return ctx -> {
            int zoneId = ctx.config(DistributionZoneConfiguration.class).zoneId().value();

            if (ctx.oldValue() == null) {
```

### DataFlowIssue
Method invocation `intValue` may produce `NullPointerException`
in `modules/distribution-zones/src/main/java/org/apache/ignite/internal/distributionzones/DistributionZoneManager.java`
#### Snippet
```java
            }

            int newScaleDown = ctx.newValue().intValue();

            // It is safe to zonesTimers.get(zoneId) in term of NPE because meta storage notifications are one-threaded
```

### DataFlowIssue
Method invocation `zoneId` may produce `NullPointerException`
in `modules/distribution-zones/src/main/java/org/apache/ignite/internal/distributionzones/DistributionZoneManager.java`
#### Snippet
```java
        @Override
        public CompletableFuture<?> onCreate(ConfigurationNotificationEvent<DistributionZoneView> ctx) {
            int zoneId = ctx.newValue().zoneId();

            ZoneState zoneState = new ZoneState(executor);
```

### DataFlowIssue
Method invocation `zoneId` may produce `NullPointerException`
in `modules/distribution-zones/src/main/java/org/apache/ignite/internal/distributionzones/DistributionZoneManager.java`
#### Snippet
```java
    private ConfigurationListener<Integer> onUpdateScaleUp() {
        return ctx -> {
            int zoneId = ctx.config(DistributionZoneConfiguration.class).zoneId().value();

            if (ctx.oldValue() == null) {
```

### DataFlowIssue
Method invocation `intValue` may produce `NullPointerException`
in `modules/distribution-zones/src/main/java/org/apache/ignite/internal/distributionzones/DistributionZoneManager.java`
#### Snippet
```java
            }

            int newScaleUp = ctx.newValue().intValue();

            // It is safe to zonesTimers.get(zoneId) in term of NPE because meta storage notifications are one-threaded
```

### DataFlowIssue
Argument `scan0.peek()` might be null
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/index/AbstractSortedIndexStorageTest.java`
#### Snippet
```java
        scan1 = indexStorage.scan(null, null, 0);

        assertEquals(0, serializer.deserializeColumns(scan0.peek())[0]);
        assertEquals(0, serializer.deserializeColumns(scan1.peek())[0]);

```

### DataFlowIssue
Argument `scan1.peek()` might be null
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/index/AbstractSortedIndexStorageTest.java`
#### Snippet
```java

        assertEquals(0, serializer.deserializeColumns(scan0.peek())[0]);
        assertEquals(0, serializer.deserializeColumns(scan1.peek())[0]);

        // index   = [0]
```

### DataFlowIssue
Argument `scan0.peek()` might be null
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/index/AbstractSortedIndexStorageTest.java`
#### Snippet
```java
        // cursor0 =    ^ cached [0]
        assertTrue(scan0.hasNext());
        assertEquals(0, serializer.deserializeColumns(scan0.peek())[0]);

        // index   = [0]
```

### DataFlowIssue
Method invocation `rowId` may produce `NullPointerException`
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/index/AbstractSortedIndexStorageTest.java`
#### Snippet
```java
        IndexRow actualRow = getSingle(index, row.indexColumns());

        assertThat(actualRow.rowId(), is(equalTo(row.rowId())));
    }

```

### DataFlowIssue
Method invocation `rowId` may produce `NullPointerException`
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/index/AbstractSortedIndexStorageTest.java`
#### Snippet
```java

        assertThat(
                getSingle(indexStorage, entry1.indexColumns()).rowId(),
                is(equalTo(entry1.rowId()))
        );
```

### DataFlowIssue
Method invocation `rowId` may produce `NullPointerException`
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/index/AbstractSortedIndexStorageTest.java`
#### Snippet
```java

        assertThat(
                getSingle(indexStorage, entry2.indexColumns()).rowId(),
                is(equalTo(entry2.rowId()))
        );
```

### DataFlowIssue
Method invocation `touchMembers` may produce `NullPointerException`
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/notifications/ConfigurationNotifier.java`
#### Snippet
```java
                    NamedListConfiguration<?, InnerNode, ?> namedListCfg = namedDynamicConfig(config, key);

                    Map<String, ConfigurationProperty<?>> namedListCfgMembers = namedListCfg.touchMembers();

                    // Lazy initialization.
```

### DataFlowIssue
Method invocation `specificNode` may produce `NullPointerException`
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/notifications/ConfigurationNotifier.java`
#### Snippet
```java
                                )),
                                null,
                                newVal.specificNode(),
                                ctx,
                                ConfigurationNamedListListener::onCreate
```

### DataFlowIssue
Method invocation `specificNode` may produce `NullPointerException`
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/notifications/ConfigurationNotifier.java`
#### Snippet
```java
                                        anyCfg -> extendedListeners(namedDynamicConfig(anyCfg, key), ctx.notificationNum)
                                )),
                                oldVal.specificNode(),
                                null,
                                ctx,
```

### DataFlowIssue
Method invocation `specificNode` may produce `NullPointerException`
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/notifications/ConfigurationNotifier.java`
#### Snippet
```java
                                        anyCfg -> extendedListeners(namedDynamicConfig(anyCfg, key), ctx.notificationNum)
                                )),
                                oldVal.specificNode(),
                                newVal.specificNode(),
                                ctx,
```

### DataFlowIssue
Method invocation `specificNode` may produce `NullPointerException`
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/notifications/ConfigurationNotifier.java`
#### Snippet
```java
                                )),
                                oldVal.specificNode(),
                                newVal.specificNode(),
                                ctx,
                                (listener, event) -> listener.onRename(entry.getKey(), entry.getValue(), event)
```

### DataFlowIssue
Method invocation `specificNode` may produce `NullPointerException`
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/notifications/ConfigurationNotifier.java`
#### Snippet
```java
                                        anyCfg -> extendedListeners(namedDynamicConfig(anyCfg, key), ctx.notificationNum)
                                )),
                                oldVal.specificNode(),
                                newVal.specificNode(),
                                ctx,
```

### DataFlowIssue
Method invocation `specificNode` may produce `NullPointerException`
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/notifications/ConfigurationNotifier.java`
#### Snippet
```java
                                )),
                                oldVal.specificNode(),
                                newVal.specificNode(),
                                ctx,
                                ConfigurationNamedListListener::onUpdate
```

### DataFlowIssue
Method invocation `touchMembers` may produce `NullPointerException`
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/notifications/ConfigurationNotifier.java`
#### Snippet
```java

                // Initialize named list configuration.
                namedListCfg.touchMembers();

                for (String name : newNamedList.namedListKeys()) {
```

### DataFlowIssue
Method invocation `specificNode` may produce `NullPointerException`
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/notifications/ConfigurationNotifier.java`
#### Snippet
```java
                            )),
                            null,
                            namedInnerNode.specificNode(),
                            ctx,
                            ConfigurationNamedListListener::onCreate
```

### DataFlowIssue
Dereference of `results` may produce `NullPointerException`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/tree/BplusTree.java`
#### Snippet
```java

        private boolean advance() {
            if (++row < results.length && results[row] != null) {
                clearLastResult(); // Allow to GC the last returned row.

```

### DataFlowIssue
Array access `results[last]` may produce `NullPointerException`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/tree/BplusTree.java`
#### Snippet
```java
            int last = row - 1;

            assert results[last] != null;

            results[last] = null;
```

### DataFlowIssue
Method invocation `addForRecycle` may produce `NullPointerException`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/tree/BplusTree.java`
#### Snippet
```java

    private void addForRecycle(LongListReuseBag bag) throws IgniteInternalCheckedException {
        reuseList.addForRecycle(bag);

        assert bag.isEmpty() : bag.size();
```

### DataFlowIssue
Dereference of `results` may produce `NullPointerException`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/tree/BplusTree.java`
#### Snippet
```java
            } else {
                if (results != OBJECT_EMPTY_ARRAY) {
                    assert results.length > 0; // Otherwise it makes no sense to create an array.

                    // Fake clear.
```

### DataFlowIssue
Array access `results[row]` may produce `NullPointerException`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/tree/BplusTree.java`
#### Snippet
```java
            }

            R r = results[row];

            assert r != null;
```

### DataFlowIssue
Unboxing of `t.get1()` may produce `NullPointerException`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/tree/BplusTree.java`
#### Snippet
```java
     */
    private void temporaryReleaseLock(Deque<IgniteTuple3<Long, Long, Long>> lockedPages) {
        lockedPages.iterator().forEachRemaining(t -> writeUnlock(t.get1(), t.get2(), t.get3(), true));

        temporaryReleaseLock();
```

### DataFlowIssue
Unboxing of `t.get2()` may produce `NullPointerException`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/tree/BplusTree.java`
#### Snippet
```java
     */
    private void temporaryReleaseLock(Deque<IgniteTuple3<Long, Long, Long>> lockedPages) {
        lockedPages.iterator().forEachRemaining(t -> writeUnlock(t.get1(), t.get2(), t.get3(), true));

        temporaryReleaseLock();
```

### DataFlowIssue
Unboxing of `t.get3()` may produce `NullPointerException`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/tree/BplusTree.java`
#### Snippet
```java
     */
    private void temporaryReleaseLock(Deque<IgniteTuple3<Long, Long, Long>> lockedPages) {
        lockedPages.iterator().forEachRemaining(t -> writeUnlock(t.get1(), t.get2(), t.get3(), true));

        temporaryReleaseLock();
```

### DataFlowIssue
Unboxing of `t.get1()` may produce `NullPointerException`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/tree/BplusTree.java`
#### Snippet
```java
        temporaryReleaseLock();

        lockedPages.descendingIterator().forEachRemaining(t -> writeLock(t.get1(), t.get2()));
    }

```

### DataFlowIssue
Unboxing of `t.get2()` may produce `NullPointerException`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/tree/BplusTree.java`
#### Snippet
```java
        temporaryReleaseLock();

        lockedPages.descendingIterator().forEachRemaining(t -> writeLock(t.get1(), t.get2()));
    }

```

### DataFlowIssue
Argument `results` might be null
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/tree/BplusTree.java`
#### Snippet
```java
                    R result = treeRowClosure != null ? treeRowClosure.map(treeRow) : (R) treeRow;

                    results = set(results, resCnt++, result);

                    lastRow = treeRow;
```

## RuleId[id=DeprecatedIsStillUsed]
### DeprecatedIsStillUsed
Deprecated member 'initJavaLoggerProps' is still used
in `modules/cli/src/main/java/org/apache/ignite/internal/cli/Main.java`
#### Snippet
```java
     */
    @Deprecated
    private static void initJavaLoggerProps() {
        InputStream propsFile = Main.class.getResourceAsStream("/cli.java.util.logging.properties");

```

### DeprecatedIsStillUsed
Deprecated member 'checkConnection' is still used
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/rpc/RpcClient.java`
#### Snippet
```java
     * @deprecated // TODO asch remove IGNITE-14832
     */
    boolean checkConnection(PeerId peerId);

    /**
```

### DeprecatedIsStillUsed
Deprecated member 'enlistResultFuture' is still used
in `modules/transactions/src/main/java/org/apache/ignite/internal/tx/InternalTransaction.java`
#### Snippet
```java
     */
    @Deprecated
    void enlistResultFuture(CompletableFuture<?> resultFuture);

    /**
```

## RuleId[id=Convert2MethodRef]
### Convert2MethodRef
Lambda can be replaced with method reference
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/core/NodeImpl.java`
#### Snippet
```java
                NamedThreadFactory.threadPrefix(opts.getServerName(), "JRaft-FSMCaller-Disruptor"),
                opts.getRaftOptions().getDisruptorBufferSize(),
                () -> new FSMCallerImpl.ApplyTask(),
                opts.getStripes()));
        }
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/core/NodeImpl.java`
#### Snippet
```java
                NamedThreadFactory.threadPrefix(opts.getServerName(), "JRaft-NodeImpl-Disruptor"),
                opts.getRaftOptions().getDisruptorBufferSize(),
                () -> new NodeImpl.LogEntryAndClosure(),
                opts.getStripes()));
        }
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/core/NodeImpl.java`
#### Snippet
```java
                NamedThreadFactory.threadPrefix(opts.getServerName(), "JRaft-ReadOnlyService-Disruptor"),
                opts.getRaftOptions().getDisruptorBufferSize(),
                () -> new ReadOnlyServiceImpl.ReadIndexEvent(),
                opts.getStripes()));
        }
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/core/NodeImpl.java`
#### Snippet
```java
                NamedThreadFactory.threadPrefix(opts.getServerName(), "JRaft-LogManager-Disruptor"),
                opts.getRaftOptions().getDisruptorBufferSize(),
                () -> new LogManagerImpl.StableClosureEvent(),
                opts.getStripes()));
        }
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `modules/table/src/testFixtures/java/org/apache/ignite/internal/table/impl/DummyInternalTableImpl.java`
#### Snippet
```java
                () -> Map.of(pkLocker.id(), pkLocker),
                pkStorage,
                () -> Map.of(),
                CLOCK,
                safeTime,
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/TableManager.java`
#### Snippet
```java

        // TODO: IGNITE-16288 directIndexIds should use async configuration API
        var table = new TableImpl(internalTable, lockMgr, () -> CompletableFuture.supplyAsync(() -> directIndexIds()));

        tablesByIdVv.update(causalityToken, (previous, e) -> inBusyLock(busyLock, () -> {
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `modules/table/src/testFixtures/java/org/apache/ignite/internal/table/TxAbstractTest.java`
#### Snippet
```java

        tx.commit();
        assertThrows(Exception.class, () -> tx2.commit());
    }

```

### Convert2MethodRef
Lambda can be replaced with method reference
in `modules/table/src/testFixtures/java/org/apache/ignite/internal/table/TxAbstractTest.java`
#### Snippet
```java
        igniteTransactions.beginAsync()
                .thenCompose(tx -> accounts.keyValueView().getAsync(tx, makeKey(1))
                        .thenCombine(accounts.recordView().getAsync(tx, makeKey(2)), (v1, v2) -> new Pair<>(v1, v2))
                        .thenCompose(pair -> allOf(
                                accounts.keyValueView().putAsync(
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `modules/table/src/testFixtures/java/org/apache/ignite/internal/table/TxAbstractTest.java`
#### Snippet
```java
        igniteTransactions.beginAsync()
                .thenCompose(tx -> accounts.recordView().getAsync(tx, makeKey(1))
                        .thenCombine(accounts.recordView().getAsync(tx, makeKey(2)), (v1, v2) -> new Pair<>(v1, v2))
                        .thenCompose(pair -> allOf(
                                accounts.recordView().upsertAsync(
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `modules/metrics/src/main/java/org/apache/ignite/internal/metrics/MetricManager.java`
#### Snippet
```java
                .stream()
                .map(Provider::get)
                .collect(Collectors.toMap(e -> e.name(), Function.identity()));
    }

```

### Convert2MethodRef
Lambda can be replaced with method reference
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/replicator/PartitionReplicaListener.java`
#### Snippet
```java
        txTimestampUpdateMap.compute(txStateReq.txId(), (uuid, fut) -> {
            if (fut != null) {
                fut.thenAccept(txMeta -> txStateFut.complete(txMeta));
            } else {
                TxMeta txMeta = txStateStorage.get(txStateReq.txId());
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/AbstractMvPartitionStorageTest.java`
#### Snippet
```java
            assertFalse(cursor.hasNext());

            assertThrows(NoSuchElementException.class, () -> cursor.next());

            assertThat(res, containsInAnyOrder(value1, value2));
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/AbstractMvPartitionStorageTest.java`
#### Snippet
```java
            assertFalse(cursor.hasNext());

            assertThrows(NoSuchElementException.class, () -> cursor.next());

            assertThrows(IllegalStateException.class, () -> cursor.committed(commitTs1));
```

## RuleId[id=NonSerializableFieldInSerializableClass]
### NonSerializableFieldInSerializableClass
Non-serializable field 'parent' in a Serializable class
in `modules/core/src/main/java/org/apache/ignite/internal/util/subscription/OrderedMergePublisher.java`
#### Snippet
```java
        static final class OrderedMergeSubscriber<T> extends AtomicReference<Subscription> implements Subscriber<T>, Subscription {
            /** Parent subscription. */
            private final OrderedMergeSubscription<T> parent;

            /** Prefetch size. */
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'tail' in a Serializable class
in `modules/core/src/main/java/org/apache/ignite/internal/tostring/StringBuilderLimitedLength.java`
#### Snippet
```java

    /** Additional string builder to get tail of message. */
    private CircularStringBuilder tail;

    /**
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'dfltVal' in a Serializable class
in `modules/jdbc/src/main/java/org/apache/ignite/internal/jdbc/ConnectionPropertiesImpl.java`
#### Snippet
```java

        /** Default value. */
        protected Object dfltVal;

        /**
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'owner' in a Serializable class
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/NonReentrantLock.java`
#### Snippet
```java
    private static final long serialVersionUID = -833780837233068610L;

    private Thread owner;

    @Override
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'status' in a Serializable class
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/error/RaftException.java`
#### Snippet
```java
     * Error status
     */
    private Status status = Status.OK();

    public RaftException(EnumOutter.ErrorType type) {
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'correlations' in a Serializable class
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/SharedState.java`
#### Snippet
```java
    private static final long serialVersionUID = 42L;

    private Object[] correlations = new Object[16];

    /**
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'fields' in a Serializable class
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/agg/GroupKey.java`
#### Snippet
```java
    public static final GroupKey EMPTY_GRP_KEY = new GroupKey(OBJECT_EMPTY_ARRAY);

    private final Object[] fields;

    public GroupKey(Object[] fields) {
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'holder' in a Serializable class
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/agg/Accumulators.java`
#### Snippet
```java
     */
    private static class AnyVal implements Accumulator {
        private Object holder;

        public static final Supplier<Accumulator> FACTORY = AnyVal::new;
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'method' in a Serializable class
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/util/IgniteMethod.java`
#### Snippet
```java
    RAND_UUID(UUID.class, "randomUUID");

    private final Method method;

    /**
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'fragment' in a Serializable class
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/metadata/FragmentMappingException.java`
#### Snippet
```java
 */
public class FragmentMappingException extends RuntimeException {
    private final Fragment fragment;

    private final RelNode node;
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'readLockHolderTs' in a Serializable class
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/checkpoint/ReentrantReadWriteLockWithTracking.java`
#### Snippet
```java
        private static final long serialVersionUID = 0L;

        private final ThreadLocal<IgniteBiTuple<Integer, Long>> readLockHolderTs = withInitial(() -> new IgniteBiTuple<>(0, 0L));

        private final IgniteLogger log;
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'log' in a Serializable class
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/checkpoint/ReentrantReadWriteLockWithTracking.java`
#### Snippet
```java
        private final ThreadLocal<IgniteBiTuple<Integer, Long>> readLockHolderTs = withInitial(() -> new IgniteBiTuple<>(0, 0L));

        private final IgniteLogger log;

        private final long readLockThreshold;
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'checkpointPages' in a Serializable class
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/PersistentPageMemory.java`
#### Snippet
```java
        /** Wrapper of pages of current checkpoint. */
        @Nullable
        private volatile CheckpointPages checkpointPages;

        /** Maximum number of dirty pages. */
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'pool' in a Serializable class
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/PersistentPageMemory.java`
#### Snippet
```java

        /** Page pool. */
        private final PagePool pool;

        /** Page replacement policy. */
```

## RuleId[id=MismatchedJavadocCode]
### MismatchedJavadocCode
Method is specified to return number but the return type is array
in `modules/api/src/main/java/org/apache/ignite/sql/Session.java`
#### Snippet
```java
     * @param dmlQuery DML query template.
     * @param batch Batch of query arguments.
     * @return Number of rows affected by each query in the batch.
     * @throws SqlBatchException If the batch fails.
     */
```

### MismatchedJavadocCode
Method is specified to return number but the return type is array
in `modules/api/src/main/java/org/apache/ignite/sql/Session.java`
#### Snippet
```java
     * @param dmlStatement DML statement to execute.
     * @param batch Batch of query arguments.
     * @return Number of rows affected by each query in the batch.
     * @throws SqlBatchException If the batch fails.
     */
```

### MismatchedJavadocCode
Method is specified to return number but the return type is array
in `modules/core/src/main/java/org/apache/ignite/internal/util/ByteUtils.java`
#### Snippet
```java
     * @param bytes Bytes array to write result to.
     * @param off Offset in the target array to write result to.
     * @return Number of bytes overwritten in {@code bytes} array.
     */
    public static byte[] putLongToBytes(long l, byte[] bytes, int off) {
```

### MismatchedJavadocCode
Method is specified to return number but the return type is array
in `modules/core/src/main/java/org/apache/ignite/internal/util/ByteUtils.java`
#### Snippet
```java
     * @param bytes Bytes array to write result to.
     * @param off Offset in the target array to write result to.
     * @return Number of bytes overwritten in {@code bytes} array.
     */
    public static byte[] putUuidToBytes(UUID uuid, byte[] bytes, int off) {
```

### MismatchedJavadocCode
Method is specified to return list but the return type is set
in `modules/schema/src/testFixtures/java/org/apache/ignite/internal/schema/testutils/definition/TableDefinition.java`
#### Snippet
```java
     * Returns key columns.
     *
     * @return List of columns.
     */
    Set<String> keyColumns();
```

### MismatchedJavadocCode
Method is specified to return list but the return type is map
in `modules/metrics/src/main/java/org/apache/ignite/internal/metrics/MetricManager.java`
#### Snippet
```java
     * Load exporters by {@link ServiceLoader} mechanism.
     *
     * @return list of loaded exporters.
     */
    public static Map<String, MetricExporter> loadExporters() {
```

### MismatchedJavadocCode
Method is specified to return list but the return type is array
in `modules/storage-rocksdb/src/main/java/org/apache/ignite/internal/storage/rocksdb/RocksDbMetaStorage.java`
#### Snippet
```java
     * Returns a list of partition IDs that exist in the associated storage.
     *
     * @return list of partition IDs
     */
    int[] getPartitionIds() {
```

## RuleId[id=AssignmentToForLoopParameter]
### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `modules/core/src/main/java/org/apache/ignite/internal/util/OffheapReadWriteLock.java`
#### Snippet
```java
                } else {
                    // Retry CAS, do not count as spin cycle.
                    i--;
                }
            }
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `modules/core/src/main/java/org/apache/ignite/internal/util/OffheapReadWriteLock.java`
#### Snippet
```java
                } else {
                    // Retry CAS, do not count as spin cycle.
                    i--;
                }
            }
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `modules/core/src/main/java/org/apache/ignite/internal/util/OffheapReadWriteLock.java`
#### Snippet
```java
                    } else {
                        // Retry CAS, do not count as spin cycle.
                        i--;
                    }
                }
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/CRC64.java`
#### Snippet
```java
    public void update(final byte[] buffer, final int offset, int length) {
        for (int i = offset; length > 0; length--) {
            update(buffer[i++]);
        }
    }
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `modules/schema/src/main/java/org/apache/ignite/internal/schema/marshaller/MarshallerUtil.java`
#### Snippet
```java
            } else if (Character.isHighSurrogate(ch)) {
                cnt += 4;
                ++i;
            } else {
                cnt += 3;
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/RexImpTable.java`
#### Snippet
```java
                                    false);
                    for (int i = 0; i < argValueList.size(); i++) {
                        Expression key = argValueList.get(i++);
                        Expression value = argValueList.get(i);
                        blockBuilder.add(
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/io/AbstractDataPageIo.java`
#### Snippet
```java

                    if (offNext + nextSize == off) {
                        i--;

                        off = offNext;
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `idx`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/RobinHoodBackwardShiftHashMap.java`
#### Snippet
```java
            list.add(valAt);

            idx--; //Need recheck current cell because of backward shift
        }

```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/replacement/RandomLruPageReplacementPolicy.java`
#### Snippet
```java

                if (relRmvAddr == rndAddr || pinned || skip || dirty) {
                    i--;

                    continue;
```

## RuleId[id=UnnecessaryToStringCall]
### UnnecessaryToStringCall
Unnecessary `toString()` call
in `modules/core/src/main/java/org/apache/ignite/lang/IgniteUuid.java`
#### Snippet
```java
    @Override
    public String toString() {
        return shortString() + '-' + gid.toString();
    }
}
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `modules/jdbc/src/main/java/org/apache/ignite/internal/jdbc/ConnectionPropertiesImpl.java`
#### Snippet
```java
                if (val.doubleValue() < range[0].doubleValue()) {
                    throw new SQLException("Property cannot be lower than " + range[0].toString() + " [name=" + name
                            + ", value=" + val.toString() + ']', SqlStateCode.CLIENT_CONNECTION_FAILED);
                }

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `modules/jdbc/src/main/java/org/apache/ignite/internal/jdbc/ConnectionPropertiesImpl.java`
#### Snippet
```java
                if (val.doubleValue() > range[1].doubleValue()) {
                    throw new SQLException("Property cannot be upper than " + range[1].toString() + " [name=" + name
                            + ", value=" + val.toString() + ']', SqlStateCode.CLIENT_CONNECTION_FAILED);
                }
            }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/SegmentList.java`
#### Snippet
```java
                }
            }
            return "Segment [elements=" + b.toString() + ", offset=" + this.offset + ", pos=" + this.pos + "]";
        }

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/prepare/IgniteSqlToRelConvertor.java`
#### Snippet
```java
                        SqlValidatorUtil.getTargetField(
                                targetRowType, typeFactory, id, catalogReader, targetTable);
                assert field != null : "column " + id.toString() + " not found";
                targetColumnNameList.add(field.getName());
            }
```

## RuleId[id=SetReplaceableByEnumSet]
### SetReplaceableByEnumSet
`HashSet<>` can be replaced with 'EnumSet'
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/externalize/RelJson.java`
#### Snippet
```java
        for (Map<String, Object> o : order) {
            RexNode expr = toRex(relInput, o.get("expr"));
            Set<SqlKind> directions = new HashSet<>();
            if (toEnum(o.get("direction")) == Direction.DESCENDING) {
                directions.add(SqlKind.DESCENDING);
```

### SetReplaceableByEnumSet
`HashSet<>` can be replaced with 'EnumSet'
in `modules/transactions/src/main/java/org/apache/ignite/internal/tx/impl/HeapLockManager.java`
#### Snippet
```java
         * TODO: IGNITE-18350 Abandon the collection in favor of BitSet.
         */
        private final Set<LockMode> intendedLocks = new HashSet<>();

        /** Locked future. */
```

## RuleId[id=SuspiciousMethodCalls]
### SuspiciousMethodCalls
'Map, Object\>' may not contain keys of type 'String'
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/api/SessionBuilderImpl.java`
#### Snippet
```java
    @Override
    public @Nullable Object property(String name) {
        return props.get(name);
    }

```

## RuleId[id=InnerClassMayBeStatic]
### InnerClassMayBeStatic
Inner class `OnErrorClosure` may be 'static'
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/core/FSMCallerImpl.java`
#### Snippet
```java
     * Closure runs with an error.
     */
    public class OnErrorClosure implements Closure {
        private RaftException error;

```

## RuleId[id=UnnecessaryBoxing]
### UnnecessaryBoxing
Redundant boxing, `Integer.parseInt()` call can be used instead
in `modules/table/src/main/java/org/apache/ignite/internal/table/distributed/replicator/TablePartitionId.java`
#### Snippet
```java
        String[] parts = str.split("_part_");

        return new TablePartitionId(UUID.fromString(parts[0]), Integer.valueOf(parts[1]));
    }

```

## RuleId[id=MissingDeprecatedAnnotation]
### MissingDeprecatedAnnotation
Missing '@Deprecated' annotation
in `modules/core/src/main/java/org/apache/ignite/internal/util/IgniteUtils.java`
#### Snippet
```java
     * @deprecated Calls to this method should never be committed to master.
     */
    public static void dumpStack(IgniteLogger log, String msg, Object... params) {
        String reason = "Dumping stack";

```

### MissingDeprecatedAnnotation
Missing '@Deprecated' annotation
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/rpc/RpcClient.java`
#### Snippet
```java
     * @deprecated // TODO asch remove IGNITE-14832
     */
    boolean checkConnection(PeerId peerId);

    /**
```

## RuleId[id=UnnecessaryFullyQualifiedName]
### UnnecessaryFullyQualifiedName
Qualifier `org.apache.ignite` is unnecessary and can be removed
in `modules/api/src/main/java/org/apache/ignite/InitParametersBuilder.java`
#### Snippet
```java
import org.apache.ignite.security.AuthenticationConfig;

/** Builder of {@link org.apache.ignite.InitParameters}. */
public class InitParametersBuilder {
    private String destinationNodeName;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.ignite.sql.async` is unnecessary and can be removed
in `modules/api/src/main/java/org/apache/ignite/sql/SyncResultSetAdapter.java`
#### Snippet
```java

/**
 * Synchronous wrapper over {@link org.apache.ignite.sql.async.AsyncResultSet}.
 */
class SyncResultSetAdapter<T> implements ResultSet<T> {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.ignite.table.mapper` is unnecessary and can be removed
in `modules/api/src/main/java/org/apache/ignite/table/Table.java`
#### Snippet
```java
 * @see RecordView
 * @see KeyValueView
 * @see org.apache.ignite.table.mapper.Mapper
 */
public interface Table {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `modules/api/src/main/java/org/apache/ignite/table/TupleImpl.java`
#### Snippet
```java
     * @throws ClassNotFoundException If failed.
     */
    private void readObject(java.io.ObjectInputStream in) throws IOException, ClassNotFoundException {
        in.defaultReadObject();

```

### UnnecessaryFullyQualifiedName
Qualifier `picocli.CommandLine` is unnecessary and can be removed
in `modules/cli/src/main/java/org/apache/ignite/internal/cli/core/repl/executor/IgnitePicocliCommands.java`
#### Snippet
```java
    public List<String> commandInfo(String command) {
        CommandSpec spec = cmd.getSubcommands().get(command).getCommandSpec();
        Help cmdhelp = new picocli.CommandLine.Help(spec);
        String description = AttributedString.stripAnsi(spec.usageMessage().sectionMap().get("description").render(cmdhelp));
        return new ArrayList<>(Arrays.asList(description.split("\\r?\\n")));
```

### UnnecessaryFullyQualifiedName
Qualifier `picocli.CommandLine` is unnecessary and can be removed
in `modules/cli/src/main/java/org/apache/ignite/internal/cli/core/repl/executor/IgnitePicocliCommands.java`
#### Snippet
```java
        }
        CommandSpec spec = sub.getCommandSpec();
        Help cmdhelp = new picocli.CommandLine.Help(spec);
        List<AttributedString> main = new ArrayList<>();
        Map<String, List<AttributedString>> options = new HashMap<>();
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util.logging` is unnecessary and can be removed
in `modules/core/src/main/java/org/apache/ignite/lang/JavaLoggerFormatter.java`
#### Snippet
```java

    /**
     * Convert {@linkplain java.util.logging.Level  java.util.logging levels} to {@linkplain System.Logger.Level System logger levels}.
     *
     * @param severity Severity
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util.logging` is unnecessary and can be removed
in `modules/core/src/main/java/org/apache/ignite/lang/JavaLoggerFormatter.java`
#### Snippet
```java
     *
     * @param severity Severity
     * @return {@link System.Logger.Level} according to {@link java.util.logging.Level} int value.
     * @see System.Logger.Level
     */
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `modules/core/src/main/java/org/apache/ignite/internal/util/io/IgniteDataInput.java`
#### Snippet
```java
     * @param   readLimit   the maximum limit of bytes that can be read before
     *                      the mark position becomes invalid.
     * @see     java.io.InputStream#reset()
     */
    void mark(int readLimit);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.nio` is unnecessary and can be removed
in `modules/core/src/main/java/org/apache/ignite/internal/util/GridUnsafe.java`
#### Snippet
```java
    public static final long BOOLEAN_ARR_OFF = UNSAFE.arrayBaseOffset(boolean[].class);

    /** {@link java.nio.Buffer#address} field offset. */
    private static final long DIRECT_BUF_ADDR_OFF = bufferAddressOffset();

```

### UnnecessaryFullyQualifiedName
Qualifier `sun.misc` is unnecessary and can be removed
in `modules/core/src/main/java/org/apache/ignite/internal/util/GridUnsafe.java`
#### Snippet
```java

/**
 * Wrapper for the {@link sun.misc.Unsafe} class.
 *
 * <p>All memory access operations have the following properties:
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.ignite.raft.jraft.storage.impl` is unnecessary and can be removed
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/core/LogStorageBudgetFactory.java`
#### Snippet
```java

/**
 * Factory for {@link org.apache.ignite.raft.jraft.storage.impl.LogStorageBudget} instances.
 */
public interface LogStorageBudgetFactory {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary, and can be replaced with an import
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/util/ArrayDeque.java`
#### Snippet
```java
 * @param <E>
 */
public class ArrayDeque<E> extends java.util.ArrayList<E> {

    private static final long serialVersionUID = -4929318149975955629L;
```

### UnnecessaryFullyQualifiedName
Qualifier `java.net` is unnecessary and can be removed
in `modules/raft/src/main/java/org/apache/ignite/internal/raft/RaftGroupServiceImpl.java`
#### Snippet
```java

    /**
     * Checks if an error is recoverable, for example, {@link java.net.ConnectException}.
     *
     * @param t The throwable.
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util.concurrent` is unnecessary and can be removed
in `modules/client/src/main/java/org/apache/ignite/client/IgniteClient.java`
#### Snippet
```java
         * <p>When <code>null</code> (default), {@link ForkJoinPool#commonPool()} is used.
         *
         * <p>When async client operation completes, corresponding {@link java.util.concurrent.CompletableFuture} continuations
         * (such as {@link java.util.concurrent.CompletableFuture#thenApply(Function)}) will be invoked using this executor.
         *
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util.concurrent` is unnecessary and can be removed
in `modules/client/src/main/java/org/apache/ignite/client/IgniteClient.java`
#### Snippet
```java
         *
         * <p>When async client operation completes, corresponding {@link java.util.concurrent.CompletableFuture} continuations
         * (such as {@link java.util.concurrent.CompletableFuture#thenApply(Function)}) will be invoked using this executor.
         *
         * <p>Server responses are handled by a dedicated network thread. To ensure optimal performance,
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary, and can be replaced with an import
in `modules/schema/src/testFixtures/java/org/apache/ignite/internal/schema/SchemaTestUtils.java`
#### Snippet
```java

            case UUID:
                return new java.util.UUID(rnd.nextLong(), rnd.nextLong());

            case STRING:
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.ignite.internal.network.serialization` is unnecessary and can be removed
in `modules/network/src/main/java/org/apache/ignite/internal/network/serialization/SpecialSerializationMethods.java`
#### Snippet
```java
     * Invokes {@code writeObject()} on the target object. Should only be used if the target descriptor supports
     * the method (that is, its serialization type is
     * {@link org.apache.ignite.internal.network.serialization.SerializationType#SERIALIZABLE} and the class
     * actually has writeObject() method).
     * If any of these conditions fail, a {@link NullPointerException} will be thrown.
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.ignite.internal.network.serialization` is unnecessary and can be removed
in `modules/network/src/main/java/org/apache/ignite/internal/network/serialization/SpecialSerializationMethods.java`
#### Snippet
```java
     * Invokes {@code writeReplace()} on the target object. Should only be used if the target descriptor supports
     * the method (that is, its serialization type is
     * {@link org.apache.ignite.internal.network.serialization.SerializationType#SERIALIZABLE} or
     * {@link org.apache.ignite.internal.network.serialization.SerializationType#EXTERNALIZABLE} and the class
     * actually has writeReplace() method).
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.ignite.internal.network.serialization` is unnecessary and can be removed
in `modules/network/src/main/java/org/apache/ignite/internal/network/serialization/SpecialSerializationMethods.java`
#### Snippet
```java
     * the method (that is, its serialization type is
     * {@link org.apache.ignite.internal.network.serialization.SerializationType#SERIALIZABLE} or
     * {@link org.apache.ignite.internal.network.serialization.SerializationType#EXTERNALIZABLE} and the class
     * actually has writeReplace() method).
     * If any of these conditions fail, a {@link NullPointerException} will be thrown.
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.ignite.internal.network.serialization` is unnecessary and can be removed
in `modules/network/src/main/java/org/apache/ignite/internal/network/serialization/SpecialSerializationMethods.java`
#### Snippet
```java
     * Invokes {@code readObject()} on the target object. Should only be used if the target descriptor supports
     * the method (that is, its serialization type is
     * {@link org.apache.ignite.internal.network.serialization.SerializationType#SERIALIZABLE} and the class
     * actually has readObject() method).
     * If any of these conditions fail, a {@link NullPointerException} will be thrown.
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.ignite.internal.network.serialization` is unnecessary and can be removed
in `modules/network/src/main/java/org/apache/ignite/internal/network/serialization/SpecialSerializationMethods.java`
#### Snippet
```java
     * Invokes {@code readResolve()} on the target object. Should only be used if the target descriptor supports
     * the method (that is, its serialization type is
     * {@link org.apache.ignite.internal.network.serialization.SerializationType#SERIALIZABLE} or
     * {@link org.apache.ignite.internal.network.serialization.SerializationType#EXTERNALIZABLE} and the class
     * actually has readResolve() method).
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.ignite.internal.network.serialization` is unnecessary and can be removed
in `modules/network/src/main/java/org/apache/ignite/internal/network/serialization/SpecialSerializationMethods.java`
#### Snippet
```java
     * the method (that is, its serialization type is
     * {@link org.apache.ignite.internal.network.serialization.SerializationType#SERIALIZABLE} or
     * {@link org.apache.ignite.internal.network.serialization.SerializationType#EXTERNALIZABLE} and the class
     * actually has readResolve() method).
     * If any of these conditions fail, a {@link NullPointerException} will be thrown.
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `modules/network/src/main/java/org/apache/ignite/internal/network/serialization/ClassDescriptor.java`
#### Snippet
```java

    /**
     * Returns {@code true} if this is the descriptor of {@link java.util.Collections#singletonList(Object)} type.
     *
     * @return {@code true} if this is the descriptor of {@link java.util.Collections#singletonList(Object)} type
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `modules/network/src/main/java/org/apache/ignite/internal/network/serialization/ClassDescriptor.java`
#### Snippet
```java
     * Returns {@code true} if this is the descriptor of {@link java.util.Collections#singletonList(Object)} type.
     *
     * @return {@code true} if this is the descriptor of {@link java.util.Collections#singletonList(Object)} type
     */
    public boolean isSingletonList() {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.calcite` is unnecessary and can be removed
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/RexExecutorImpl.java`
#### Snippet
```java
     * Implementation of {@link org.apache.calcite.adapter.enumerable.RexToLixTranslator.InputGetter} that reads the
     * values of input fields by calling
     * <code>{@link org.apache.calcite.DataContext#get}("inputRecord")</code>.
     */
    private static class DataContextInputGetter implements RexToLixTranslator.InputGetter {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.sql` is unnecessary, and can be replaced with an import
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/ConverterUtils.java`
#### Snippet
```java

    private static boolean representAsInternalType(Type type) {
        return type == java.sql.Date.class
                || type == java.sql.Time.class
                || type == java.sql.Timestamp.class;
```

### UnnecessaryFullyQualifiedName
Qualifier `java.sql` is unnecessary, and can be replaced with an import
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/ConverterUtils.java`
#### Snippet
```java
    private static boolean representAsInternalType(Type type) {
        return type == java.sql.Date.class
                || type == java.sql.Time.class
                || type == java.sql.Timestamp.class;
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.sql` is unnecessary, and can be replaced with an import
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/ConverterUtils.java`
#### Snippet
```java
        return type == java.sql.Date.class
                || type == java.sql.Time.class
                || type == java.sql.Timestamp.class;
    }

```

### UnnecessaryFullyQualifiedName
Qualifier `java.sql` is unnecessary, and can be replaced with an import
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/ConverterUtils.java`
#### Snippet
```java
            return operand;
        }
        if (targetType == java.sql.Date.class) {
            // E.g. from "int" or "Integer" to "java.sql.Date",
            // generate "SqlFunctions.internalToDate".
```

### UnnecessaryFullyQualifiedName
Qualifier `java.sql` is unnecessary, and can be replaced with an import
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/ConverterUtils.java`
#### Snippet
```java
                return Expressions.call(BuiltInMethod.INTERNAL_TO_DATE.method, operand);
            }
        } else if (targetType == java.sql.Time.class) {
            // E.g. from "int" or "Integer" to "java.sql.Time",
            // generate "SqlFunctions.internalToTime".
```

### UnnecessaryFullyQualifiedName
Qualifier `java.sql` is unnecessary, and can be replaced with an import
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/ConverterUtils.java`
#### Snippet
```java
                return Expressions.call(BuiltInMethod.INTERNAL_TO_TIME.method, operand);
            }
        } else if (targetType == java.sql.Timestamp.class) {
            // E.g. from "long" or "Long" to "java.sql.Timestamp",
            // generate "SqlFunctions.internalToTimestamp".
```

### UnnecessaryFullyQualifiedName
Qualifier `java.sql` is unnecessary, and can be replaced with an import
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/ConverterUtils.java`
#### Snippet
```java
    private static Expression toInternal(Expression operand,
            Type fromType, Type targetType) {
        if (fromType == java.sql.Date.class) {
            if (targetType == int.class) {
                return Expressions.call(BuiltInMethod.DATE_TO_INT.method, operand);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.sql` is unnecessary, and can be replaced with an import
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/ConverterUtils.java`
#### Snippet
```java
                return Expressions.call(BuiltInMethod.DATE_TO_INT_OPTIONAL.method, operand);
            }
        } else if (fromType == java.sql.Time.class) {
            if (targetType == int.class) {
                return Expressions.call(BuiltInMethod.TIME_TO_INT.method, operand);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.sql` is unnecessary, and can be replaced with an import
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/ConverterUtils.java`
#### Snippet
```java
                return Expressions.call(BuiltInMethod.TIME_TO_INT_OPTIONAL.method, operand);
            }
        } else if (fromType == java.sql.Timestamp.class) {
            if (targetType == long.class) {
                return Expressions.call(BuiltInMethod.TIMESTAMP_TO_LONG.method, operand);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.sql` is unnecessary, and can be replaced with an import
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/ExpressionFactoryImpl.java`
#### Snippet
```java
                desiredType = fieldType;
                fieldType = Object.class;
            } else if (fieldType != java.sql.Date.class
                    && fieldType != java.sql.Time.class
                    && fieldType != java.sql.Timestamp.class) {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.sql` is unnecessary, and can be replaced with an import
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/ExpressionFactoryImpl.java`
#### Snippet
```java
                fieldType = Object.class;
            } else if (fieldType != java.sql.Date.class
                    && fieldType != java.sql.Time.class
                    && fieldType != java.sql.Timestamp.class) {
                fieldType = Object.class;
```

### UnnecessaryFullyQualifiedName
Qualifier `java.sql` is unnecessary, and can be replaced with an import
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/ExpressionFactoryImpl.java`
#### Snippet
```java
            } else if (fieldType != java.sql.Date.class
                    && fieldType != java.sql.Time.class
                    && fieldType != java.sql.Timestamp.class) {
                fieldType = Object.class;
            }
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.calcite.schema` is unnecessary, and can be replaced with an import
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/RexImpTable.java`
#### Snippet
```java
    public RexCallImplementor get(final SqlOperator operator) {
        if (operator instanceof SqlUserDefinedFunction) {
            org.apache.calcite.schema.Function udf =
                    ((SqlUserDefinedFunction) operator).getFunction();
            if (!(udf instanceof ImplementableFunction)) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.calcite.rel` is unnecessary and can be removed
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/prepare/IgnitePlanner.java`
#### Snippet
```java

    /**
     * Walks over a tree of relational expressions, replacing each {@link org.apache.calcite.rel.RelNode} with a
     * 'slimmed down' relational expression that projects only the columns required by its consumer.
     *
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.calcite.sql.parser` is unnecessary and can be removed
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/util/Commons.java`
#### Snippet
```java
     * @param parserCfg Parser config.
     * @return Parsed query.
     * @throws org.apache.calcite.sql.parser.SqlParseException on parse error.
     */
    public static SqlNodeList parse(Reader reader, SqlParser.Config parserCfg) throws SqlParseException {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.calcite.rel.metadata` is unnecessary and can be removed
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/metadata/IgniteMdPredicates.java`
#### Snippet
```java
    /**
     * See {@link
     * RelMdPredicates#getPredicates(org.apache.calcite.rel.RelNode, org.apache.calcite.rel.metadata.RelMetadataQuery)}.
     */
    public RelOptPredicateList getPredicates(ProjectableFilterableTableScan rel, RelMetadataQuery mq) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.calcite.rel.core` is unnecessary and can be removed
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/metadata/IgniteMdCollation.java`
#### Snippet
```java

    /**
     * Helper method to determine a {@link org.apache.calcite.rel.core.Values}'s collation.
     *
     * <p>We actually under-report the collations. A Values with 0 or 1 rows - an
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.calcite.rel.core` is unnecessary and can be removed
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/metadata/IgniteMdCollation.java`
#### Snippet
```java

    /**
     * Helper method to determine a {@link org.apache.calcite.rel.core.Sort}'s collation.
     */
    public static List<RelCollation> sort(RelCollation collation) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.calcite.rel.core` is unnecessary and can be removed
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/metadata/IgniteMdCollation.java`
#### Snippet
```java

    /**
     * Helper method to determine a {@link org.apache.calcite.rel.core.Calc}'s collation.
     */
    public static List<RelCollation> calc(RelMetadataQuery mq, RelNode input,
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.calcite.rel.metadata` is unnecessary and can be removed
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/metadata/IgniteMdCollation.java`
#### Snippet
```java
/**
 * Implementation class for {@link RelMetadataQuery#collations(RelNode)} method call. See {@link
 * org.apache.calcite.rel.metadata.RelMdCollation}
 */
@SuppressWarnings("unused") // actually all methods are used by runtime generated classes
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.calcite.rel.core` is unnecessary and can be removed
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/metadata/IgniteMdCollation.java`
#### Snippet
```java

    /**
     * Helper method to determine a {@link org.apache.calcite.rel.core.Match}'s collation.
     */
    public static List<RelCollation> match(RelMetadataQuery mq, RelNode input,
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.calcite.rel.core` is unnecessary and can be removed
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/metadata/IgniteMdCollation.java`
#### Snippet
```java

    /**
     * Helper method to determine a {@link org.apache.calcite.rel.core.Window}'s collation.
     *
     * <p>A Window projects the fields of its input first, followed by the output
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.calcite.rel.core` is unnecessary and can be removed
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/metadata/IgniteMdCollation.java`
#### Snippet
```java
     *
     * <p>{@link org.apache.calcite.rel.core.Union},
     * {@link org.apache.calcite.rel.core.Intersect}, {@link org.apache.calcite.rel.core.Minus}, {@link org.apache.calcite.rel.core.Join},
     * {@link org.apache.calcite.rel.core.Correlate} do not in general return sorted results (but implementations using particular
     * algorithms may).
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.calcite.rel.metadata` is unnecessary and can be removed
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/metadata/IgniteMdCollation.java`
#### Snippet
```java
     * @param rel Relational expression
     * @return Relational expression's collations
     * @see org.apache.calcite.rel.metadata.RelMetadataQuery#collations(RelNode)
     */
    public ImmutableList<RelCollation> collations(RelNode rel,
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.calcite.rel.core` is unnecessary and can be removed
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/metadata/IgniteMdCollation.java`
#### Snippet
```java

    /**
     * Helper method to determine a {@link org.apache.calcite.rel.core.Filter}'s collation.
     */
    public static List<RelCollation> filter(RelMetadataQuery mq, RelNode input) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.calcite.rel.core` is unnecessary and can be removed
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/metadata/IgniteMdCollation.java`
#### Snippet
```java

    /**
     * Helper method to determine a {@link org.apache.calcite.rel.core.TableScan}'s collation.
     */
    public static List<RelCollation> table(RelOptTable table) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.ignite.client.handler` is unnecessary and can be removed
in `modules/client-handler/src/test/java/ClientResourceRegistryTest.java`
#### Snippet
```java

/**
 * Tests for {@link org.apache.ignite.client.handler.ClientResourceRegistry}.
 */
public class ClientResourceRegistryTest {
```

## RuleId[id=ReplaceNullCheck]
### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElseGet()'
in `modules/raft/src/main/java/org/apache/ignite/raft/jraft/core/Replicator.java`
#### Snippet
```java
                RpcResponseClosure<AppendEntriesResponse> heartbeatDone;
                // Prefer passed-in closure.
                if (heartBeatClosure != null) {
                    heartbeatDone = heartBeatClosure;
                }
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElseGet()'
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/ConverterUtils.java`
#### Snippet
```java
        // IgniteCustomType: call runtime conversion routines.
        var toCustomType = CustomTypesConversion.INSTANCE.tryConvert(operand, toType);
        if (toCustomType != null) {
            return toCustomType;
        } else {
```

## RuleId[id=Java8MapApi]
### Java8MapApi
Can be replaced with single 'Map.computeIfAbsent' method call
in `modules/affinity/src/main/java/org/apache/ignite/internal/affinity/RendezvousAffinityFunction.java`
#### Snippet
```java
            Collection<String> nodes = macMap.get(macs);

            if (nodes == null) {
                macMap.put(macs, nodes = new HashSet<>());
            }
```

## RuleId[id=Convert2Lambda]
### Convert2Lambda
Anonymous new PrivilegedAction() can be replaced with lambda
in `modules/core/src/main/java/org/apache/ignite/internal/util/GridUnsafe.java`
#### Snippet
```java
        final ByteBuffer maybeDirectBuf = ByteBuffer.allocateDirect(1);

        Field addrField = AccessController.doPrivileged(new PrivilegedAction<Field>() {
            @Override
            public Field run() {
```

### Convert2Lambda
Anonymous new PrivilegedExceptionAction() can be replaced with lambda
in `modules/core/src/main/java/org/apache/ignite/internal/util/GridUnsafe.java`
#### Snippet
```java
            try {
                return AccessController.doPrivileged(
                        new PrivilegedExceptionAction<Unsafe>() {
                            @Override
                            public Unsafe run() throws Exception {
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `modules/table/src/testFixtures/java/org/apache/ignite/internal/table/TxAbstractTest.java`
#### Snippet
```java
        for (int i = 0; i < threads.length; i++) {
            long finalI = i;
            threads[i] = new Thread(new Runnable() {
                @Override
                public void run() {
```

### Convert2Lambda
Anonymous new Thread.UncaughtExceptionHandler() can be replaced with lambda
in `modules/table/src/testFixtures/java/org/apache/ignite/internal/table/TxAbstractTest.java`
#### Snippet
```java

            threads[i].setName("Worker-" + i);
            threads[i].setUncaughtExceptionHandler(new Thread.UncaughtExceptionHandler() {
                @Override
                public void uncaughtException(Thread t, Throwable e) {
```

### Convert2Lambda
Anonymous new Function() can be replaced with lambda
in `modules/sql-engine/src/main/java/org/apache/ignite/internal/sql/engine/exec/exp/agg/AccumulatorsFactory.java`
#### Snippet
```java
                    Commons.transform(Pair.zip(inTypes, outTypes), AccumulatorsFactory::cast);

            return new Function<Object[], Object[]>() {
                @Override
                public Object[] apply(Object[] args) {
```

## RuleId[id=UnnecessaryContinue]
### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `modules/raft/src/testFixtures/java/org/apache/ignite/raft/jraft/core/TestCluster.java`
#### Snippet
```java
                    Thread.sleep(10);

                    continue;
                }
                else
```

## RuleId[id=CopyConstructorMissesField]
### CopyConstructorMissesField
Copy constructor does not copy field 'immutable'
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/tree/NamedListNode.java`
#### Snippet
```java
     * @param node Other node.
     */
    private NamedListNode(NamedListNode<N> node) {
        syntheticKeyName = node.syntheticKeyName;
        valSupplier = node.valSupplier;
```

## RuleId[id=ThrowableNotThrown]
### ThrowableNotThrown
Result of `assertThrowsWithCause()` not thrown
in `modules/table/src/testFixtures/java/org/apache/ignite/internal/table/TxAbstractTest.java`
#### Snippet
```java
        assertFalse(futUpd2.isDone());

        assertThrowsWithCause(() -> table.upsertAll(tx2, rows), LockException.class);
    }

```

## RuleId[id=EqualsWhichDoesntCheckParameterClass]
### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `modules/page-memory/src/main/java/org/apache/ignite/internal/pagememory/persistence/checkpoint/CheckpointWorkflow.java`
#### Snippet
```java
            /** {@inheritDoc} */
            @Override
            public boolean equals(Object o) {
                return listener == ((IgniteBiTuple<?, ?>) o).getKey();
            }
```

### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `modules/configuration/src/main/java/org/apache/ignite/internal/configuration/ConfigurationListenerHolder.java`
#### Snippet
```java
            /** {@inheritDoc} */
            @Override
            public boolean equals(Object obj) {
                return listener == ((Container<L>) obj).listener;
            }
```

## RuleId[id=ThreadStartInConstruction]
### ThreadStartInConstruction
Call to `start()` during object construction
in `modules/table/src/testFixtures/java/org/apache/ignite/internal/table/impl/DummyInternalTableImpl.java`
#### Snippet
```java
        safeTimeUpdaterThread = new Thread(new SafeTimeUpdater(safeTime), "safe-time-updater");

        safeTimeUpdaterThread.start();
    }

```

## RuleId[id=CastCanBeRemovedNarrowingVariableType]
### CastCanBeRemovedNarrowingVariableType
Cast may be removed by changing the type of 'removedSortedIndexStorage' to 'TestSortedIndexStorage'
in `modules/storage-api/src/testFixtures/java/org/apache/ignite/internal/storage/impl/TestMvTableStorage.java`
#### Snippet
```java

            if (removedSortedIndexStorage != null) {
                ((TestSortedIndexStorage) removedSortedIndexStorage).destroy();
            }
        }
```

## RuleId[id=TestCaseWithNoTestMethods]
### TestCaseWithNoTestMethods
Test class `IntegrationTestBase` has no tests
in `modules/api/src/testFixtures/java/org/apache/ignite/internal/testframework/IntegrationTestBase.java`
#### Snippet
```java
@MicronautTest(rebuildContext = true)
@ExtendWith(WorkDirectoryExtension.class)
public class IntegrationTestBase extends BaseIgniteAbstractTest {
    /** Correct ignite cluster url. */
    protected static final String NODE_URL = "http://localhost:10300";
```

### TestCaseWithNoTestMethods
Test class `BaseItClusterManagementTest` has no tests
in `modules/cluster-management/src/testFixtures/java/org/apache/ignite/internal/cluster/management/BaseItClusterManagementTest.java`
#### Snippet
```java
 */
@ExtendWith(ConfigurationExtension.class)
public class BaseItClusterManagementTest extends BaseIgniteAbstractTest {
    private static final int PORT_BASE = 10000;

```

