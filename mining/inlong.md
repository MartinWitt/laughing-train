# inlong 
 
# Bad smells
I found 6683 bad smells with 258 repairable:
| ruleID | number | fixable |
| --- | --- | --- |
| JavadocDeclaration | 1691 | false |
| FieldMayBeFinal | 599 | false |
| DuplicatedCode | 563 | false |
| SpringJavaAutowiredFieldsWarningInspection | 501 | false |
| Deprecation | 388 | false |
| UnusedAssignment | 344 | false |
| NullableProblems | 334 | false |
| StringBufferReplaceableByString | 330 | false |
| UNCHECKED_WARNING | 312 | false |
| FieldCanBeLocal | 197 | false |
| DataFlowIssue | 106 | false |
| UnnecessaryToStringCall | 95 | true |
| UnstableApiUsage | 65 | false |
| ArraysAsListWithZeroOrOneArgument | 63 | false |
| ConstantValue | 58 | false |
| UnnecessaryLocalVariable | 57 | true |
| AutoCloseableResource | 52 | false |
| IgnoreResultOfCall | 46 | false |
| BusyWait | 44 | false |
| NonFinalFieldInEnum | 39 | false |
| PlaceholderCountMatchesArgumentCount | 36 | false |
| SpringBootApplicationProperties | 36 | false |
| UnnecessaryModifier | 35 | true |
| JavadocLinkAsPlainText | 33 | false |
| TrivialIf | 31 | false |
| CharsetObjectCanBeUsed | 27 | false |
| JavadocReference | 26 | false |
| MismatchedJavadocCode | 21 | false |
| RegExpRedundantEscape | 20 | false |
| RedundantCast | 20 | false |
| Lombok | 20 | false |
| OptionalGetWithoutIsPresent | 20 | false |
| DeprecatedIsStillUsed | 19 | false |
| UnnecessaryReturn | 18 | true |
| OptionalUsedAsFieldOrParameterType | 17 | false |
| CommentedOutCode | 16 | false |
| DuplicateBranchesInSwitch | 15 | false |
| IfStatementWithIdenticalBranches | 15 | false |
| UnnecessarySemicolon | 14 | false |
| SimplifyStreamApiCallChains | 14 | false |
| UnnecessaryStringEscape | 14 | true |
| SynchronizationOnLocalVariableOrMethodParameter | 13 | false |
| DuplicateCondition | 10 | false |
| RedundantMethodOverride | 10 | false |
| NonStrictComparisonCanBeEquality | 10 | true |
| RedundantArrayCreation | 10 | true |
| SynchronizeOnNonFinalField | 10 | false |
| EmptyStatementBody | 9 | false |
| IntegerMultiplicationImplicitCastToLong | 9 | false |
| MismatchedCollectionQueryUpdate | 9 | false |
| DuplicateExpressions | 9 | false |
| IOStreamConstructor | 9 | false |
| SuspiciousMethodCalls | 9 | false |
| AccessStaticViaInstance | 7 | false |
| StringOperationCanBeSimplified | 7 | false |
| ProtectedMemberInFinalClass | 7 | true |
| UseBulkOperation | 7 | false |
| RefusedBequest | 7 | false |
| TrivialStringConcatenation | 6 | false |
| OptionalIsPresent | 6 | false |
| InnerClassMayBeStatic | 6 | true |
| SpringTransactionalMethodCallsInspection | 6 | false |
| ToArrayCallWithZeroLengthArrayArgument | 5 | true |
| SpringJavaInjectionPointsAutowiringInspection | 5 | false |
| DuplicateThrows | 5 | false |
| WriteOnlyObject | 5 | false |
| UnnecessaryCallToStringValueOf | 5 | false |
| InfiniteLoopStatement | 5 | false |
| StringConcatenationInsideStringBufferAppend | 5 | false |
| SwitchStatementWithTooFewBranches | 5 | false |
| CollectionAddAllCanBeReplacedWithConstructor | 5 | false |
| PointlessBitwiseExpression | 4 | false |
| ArrayHashCode | 4 | false |
| StringEquality | 4 | false |
| ConditionCoveredByFurtherCondition | 4 | false |
| UnnecessaryContinue | 4 | false |
| WrapperTypeMayBePrimitive | 3 | false |
| PointlessArithmeticExpression | 3 | false |
| FinalStaticMethod | 3 | false |
| RedundantTypeArguments | 3 | false |
| EqualsBetweenInconvertibleTypes | 3 | false |
| UNUSED_IMPORT | 3 | false |
| MathRoundingWithIntArgument | 3 | false |
| JavaReflectionInvocation | 3 | false |
| IntegerDivisionInFloatingPointContext | 3 | false |
| SimplifiableConditionalExpression | 3 | false |
| ManualMinMaxCalculation | 3 | false |
| DefaultAnnotationParam | 3 | false |
| PointlessNullCheck | 3 | false |
| LoopConditionNotUpdatedInsideLoop | 2 | false |
| DoubleCheckedLocking | 2 | false |
| NotNullFieldNotInitialized | 2 | false |
| ManualArrayToCollectionCopy | 2 | false |
| ObviousNullCheck | 2 | false |
| ReplaceAllDot | 2 | false |
| ComparatorMethodParameterNotUsed | 2 | false |
| InstantiationOfUtilityClass | 2 | false |
| RedundantFileCreation | 2 | false |
| RegExpSimplifiable | 2 | false |
| NegativeIntConstantInLongContext | 2 | false |
| NullArgumentToVariableArgMethod | 2 | false |
| CatchMayIgnoreException | 2 | false |
| RegExpSingleCharAlternation | 2 | false |
| CopyConstructorMissesField | 2 | false |
| UastIncorrectHttpHeaderInspection | 1 | false |
| RegExpDuplicateCharacterInClass | 1 | false |
| RedundantExplicitClose | 1 | false |
| StringConcatenationInLoops | 1 | false |
| SuspiciousNameCombination | 1 | false |
| SpringPropertySource | 1 | false |
| SwitchStatementWithConfusingDeclaration | 1 | false |
| ImplicitArrayToString | 1 | false |
| CaughtExceptionImmediatelyRethrown | 1 | false |
| PointlessBooleanExpression | 1 | true |
| LoopStatementsThatDontLoop | 1 | false |
| SillyAssignment | 1 | false |
| MalformedFormatString | 1 | false |
| ThrowFromFinallyBlock | 1 | false |
| SlowListContainsAll | 1 | false |
| ConditionalExpressionWithIdenticalBranches | 1 | false |
| InfiniteRecursion | 1 | false |
| NonAtomicOperationOnVolatileField | 1 | false |
| RedundantLengthCheck | 1 | false |
| RedundantCollectionOperation | 1 | false |
| EqualsWhichDoesntCheckParameterClass | 1 | false |
| CastCanBeRemovedNarrowingVariableType | 1 | false |
## RuleId[id=ToArrayCallWithZeroLengthArrayArgument]
### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new String\[pkNameList.size()\]'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/jdbc/src/main/java/org/apache/inlong/sort/jdbc/internal/JdbcMultiBatchingOutputFormat.java`
#### Snippet
```java
                    .withDialect(jdbcOptions.getDialect())
                    .withFieldNames(filedNames)
                    .withKeyFields(pkNameList.toArray(new String[pkNameList.size()]))
                    .build();
            statementExecutorFactory = ctx -> (JdbcExec) JdbcMultiBatchingComm.createBufferReduceExecutor(
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new HttpHost\[httpHosts.size()\]'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/elasticsearch-6/src/main/java/org/apache/inlong/sort/elasticsearch6/Elasticsearch6ApiCallBridge.java`
#### Snippet
```java
    public RestHighLevelClient createClient(Map<String, String> clientConfig) {
        RestClientBuilder builder =
                RestClient.builder(httpHosts.toArray(new HttpHost[httpHosts.size()]));
        restClientFactory.configureRestClientBuilder(builder);
        RestHighLevelClient rhlClient = new RestHighLevelClient(builder);
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Segment\[accum.size()\]'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/tools/StoreRepairAdmin.java`
#### Snippet
```java
                validateSegments("DATA", accum);
            }
            segments = new FileSegmentList(accum.toArray(new Segment[accum.size()]));
            logger.info(new StringBuilder(512)
                    .append("[Data Repair] Loaded DATA ")
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Segment\[accum.size()\]'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/disk/MsgFileStore.java`
#### Snippet
```java
        }
        if (segType == SegmentType.DATA) {
            this.dataSegments = new FileSegmentList(accum.toArray(new Segment[accum.size()]));
        } else {
            this.indexSegments = new FileSegmentList(accum.toArray(new Segment[accum.size()]));
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Segment\[accum.size()\]'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/disk/MsgFileStore.java`
#### Snippet
```java
            this.dataSegments = new FileSegmentList(accum.toArray(new Segment[accum.size()]));
        } else {
            this.indexSegments = new FileSegmentList(accum.toArray(new Segment[accum.size()]));
        }
        logger.info(sBuilder.append("[File Store] Loaded ")
```

## RuleId[id=WrapperTypeMayBePrimitive]
### WrapperTypeMayBePrimitive
Type may be primitive
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/util/TemporalConversions.java`
#### Snippet
```java
        }
        if (obj instanceof Duration) {
            Long value = ((Duration) obj).toNanos();
            if (value >= 0 && value <= NANOSECONDS_PER_DAY) {
                return LocalTime.ofNanoOfDay(value);
```

### WrapperTypeMayBePrimitive
Type may be primitive
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/jdbc/src/main/java/org/apache/inlong/sort/jdbc/internal/JdbcMultiBatchingOutputFormat.java`
#### Snippet
```java
            }
            JdbcExec jdbcStatementExecutor;
            Boolean flushFlag = false;
            Exception tableException = null;
            try {
```

### WrapperTypeMayBePrimitive
Type may be primitive
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/jdbc/src/main/java/org/apache/inlong/sort/jdbc/internal/JdbcMultiBatchingOutputFormat.java`
#### Snippet
```java
                getAndSetPkNamesFromDb(tableIdentifier);
                jdbcStatementExecutor = getOrCreateStatementExecutor(tableIdentifier);
                Long totalDataSize = 0L;
                for (GenericRowData record : tableIdRecordList) {
                    totalDataSize = totalDataSize + record.toString().getBytes(StandardCharsets.UTF_8).length;
```

## RuleId[id=UnnecessaryModifier]
### UnnecessaryModifier
Modifier `private` is redundant for enum constructors
in `inlong-sdk/sort-sdk/src/main/java/org/apache/inlong/sdk/sort/api/ReportApi.java`
#### Snippet
```java
    private final int value;

    private ReportApi(int value) {
        this.value = value;
    }
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/SendMessageCallback.java`
#### Snippet
```java

    /* Invoked when a message is confirmed by TDBus. */
    public void onMessageAck(SendResult result);

    /* Invoked when a message transportation interrupted by an exception. */
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/SendMessageCallback.java`
#### Snippet
```java

    /* Invoked when a message transportation interrupted by an exception. */
    public void onException(Throwable e);
}

```

### UnnecessaryModifier
Modifier `private` is redundant for enum constructors
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/LoadBalance.java`
#### Snippet
```java
    private int index;

    private LoadBalance(String name, int index) {
        this.name = name;
        this.index = index;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/ISenderBuilder.java`
#### Snippet
```java
     * @return
     */
    public MessageSender build();
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/MessageSender.java`
#### Snippet
```java
     *                     and each element of extraAttrMap contains a pair like attrKey,attrValue
     */
    public SendResult sendMessage(byte[] body, String groupId, String streamId, long dt, String msgUUID,
            long timeout, TimeUnit timeUnit, Map<String, String> extraAttrMap);

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/MessageSender.java`
#### Snippet
```java
     */
    @Deprecated
    public SendResult sendMessage(byte[] body, String attributes, String msgUUID,
            long timeout, TimeUnit timeUnit);

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/MessageSender.java`
#### Snippet
```java
     *                     element of extraAttrMap contains a pair like attrKey,attrValue
     */
    public void asyncSendMessage(SendMessageCallback callback,
            List<byte[]> bodyList, String groupId, String streamId, long dt, String msgUUID,
            long timeout, TimeUnit timeUnit,
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/MessageSender.java`
#### Snippet
```java
     * @param body     The data will be sent
     */
    public void asyncSendMessage(SendMessageCallback callback,
            byte[] body, String groupId, String streamId, long dt, String msgUUID,
            long timeout, TimeUnit timeUnit) throws ProxysdkException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/MessageSender.java`
#### Snippet
```java
     */
    @Deprecated
    public void asyncSendMessage(SendMessageCallback callback,
            byte[] body, String attributes, String msgUUID,
            long timeout, TimeUnit timeUnit) throws ProxysdkException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/MessageSender.java`
#### Snippet
```java
     *             
     */
    public SendResult sendMessage(byte[] body, String groupId, String streamId, long dt, String msgUUID,
            long timeout, TimeUnit timeUnit);

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/MessageSender.java`
#### Snippet
```java
     *                     and each element of extraAttrMap contains a pair like attrKey,attrValue
     */
    public SendResult sendMessage(List<byte[]> bodyList, String groupId, String streamId, long dt, String msgUUID,
            long timeout, TimeUnit timeUnit, Map<String, String> extraAttrMap);

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/MessageSender.java`
#### Snippet
```java
     * @param bodyList The data will be sent,which is a collection consisting of byte arrays
     */
    public SendResult sendMessage(List<byte[]> bodyList, String groupId, String streamId, long dt, String msgUUID,
            long timeout, TimeUnit timeUnit);

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/MessageSender.java`
#### Snippet
```java
     * @param bodyList The data will be sent,which is a collection consisting of byte arrays
     */
    public void asyncSendMessage(SendMessageCallback callback,
            List<byte[]> bodyList, String groupId, String streamId, long dt, String msgUUID,
            long timeout, TimeUnit timeUnit) throws ProxysdkException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/MessageSender.java`
#### Snippet
```java
     *                     and each element of extraAttrMap contains a pair like attrKey,attrValue
     */
    public void asyncSendMessage(SendMessageCallback callback,
            byte[] body, String groupId, String streamId, long dt, String msgUUID,
            long timeout, TimeUnit timeUnit,
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/MessageSender.java`
#### Snippet
```java
            SendMessageCallback callback) throws ProxysdkException;

    public void close();
}

```

### UnnecessaryModifier
Modifier `private` is redundant for enum constructors
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mongodb-cdc/src/main/java/org/apache/inlong/sort/cdc/mongodb/table/filter/MongoDBRowKind.java`
#### Snippet
```java
    private final byte value;

    private MongoDBRowKind(String shortString, byte value) {
        this.shortString = shortString;
        this.value = value;
```

### UnnecessaryModifier
Modifier `abstract` is redundant for interface methods
in `inlong-agent/agent-common/src/main/java/org/apache/inlong/agent/db/Db.java`
#### Snippet
```java
public interface Db extends Closeable {

    abstract KeyValueEntity get(String key);

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `inlong-common/src/main/java/org/apache/inlong/common/msg/AttributeConstants.java`
#### Snippet
```java
    // calculate the end-to-end message delay; if this field does not exist in the request,
    // it will be added by the Bus with the current time
    public static final String MSG_RPT_TIME = "rtms";
}

```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `inlong-common/src/main/java/org/apache/inlong/common/msg/AttributeConstants.java`
#### Snippet
```java
    // calculate the end-to-end message delay; if this field does not exist in the request,
    // it will be added by the Bus with the current time
    public static final String MSG_RPT_TIME = "rtms";
}

```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `inlong-common/src/main/java/org/apache/inlong/common/msg/AttributeConstants.java`
#### Snippet
```java
    // calculate the end-to-end message delay; if this field does not exist in the request,
    // it will be added by the Bus with the current time
    public static final String MSG_RPT_TIME = "rtms";
}

```

### UnnecessaryModifier
Modifier `private` is redundant for enum constructors
in `inlong-common/src/main/java/org/apache/inlong/common/msg/InLongMsgAttrBuilder.java`
#### Snippet
```java
        private final String value;

        private PartitionUnit(String value) {
            this.value = value;
        }
```

### UnnecessaryModifier
Modifier `private` is redundant for enum constructors
in `inlong-common/src/main/java/org/apache/inlong/common/msg/InLongMsgAttrBuilder.java`
#### Snippet
```java
        private final String value;

        private TimeType(String value) {
            this.value = value;
        }
```

### UnnecessaryModifier
Modifier `private` is redundant for enum constructors
in `inlong-common/src/main/java/org/apache/inlong/common/msg/InLongMsg.java`
#### Snippet
```java
        private final int value;

        private Version(int value) {
            this.value = value;
        }
```

### UnnecessaryModifier
Modifier `private` is redundant for enum constructors
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/entry/TopicStatus.java`
#### Snippet
```java
    private int value = 0;

    private TopicStatus(int value) {
        this.value = value;
    }
```

### UnnecessaryModifier
Modifier `private` is redundant for enum constructors
in `inlong-manager/manager-pojo/src/main/java/org/apache/inlong/manager/pojo/sink/redis/RedisClusterMode.java`
#### Snippet
```java
    private final String key;

    private RedisClusterMode(String key) {
        this.key = key;
    }
```

### UnnecessaryModifier
Modifier `private` is redundant for enum constructors
in `inlong-manager/manager-pojo/src/main/java/org/apache/inlong/manager/pojo/sink/redis/RedisDataType.java`
#### Snippet
```java
    private final HashSet<RedisSchemaMapMode> mapModes;

    private RedisDataType(RedisSchemaMapMode... modes) {
        this.mapModes = new HashSet<>(Arrays.asList(modes));
    }
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `inlong-manager/manager-client/src/main/java/org/apache/inlong/manager/client/api/DataNode.java`
#### Snippet
```java
     * @return whether succeed
     */
    public Boolean update(DataNodeRequest request);

    /**
```

### UnnecessaryModifier
Modifier `private` is redundant for enum constructors
in `inlong-manager/manager-common/src/main/java/org/apache/inlong/manager/common/tool/excel/ExcelCellDataTransfer.java`
#### Snippet
```java
    protected static final Logger LOGGER = LoggerFactory.getLogger(ExcelCellDataTransfer.class);

    private ExcelCellDataTransfer() {
    }

```

### UnnecessaryModifier
Modifier `private` is redundant for enum constructors
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/config/pojo/CacheType.java`
#### Snippet
```java
     * @param value
     */
    private CacheType(String value) {
        this.value = value;
    }
```

### UnnecessaryModifier
Modifier `private` is redundant for enum constructors
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/config/pojo/DataType.java`
#### Snippet
```java
     * @param value
     */
    private DataType(String value) {
        this.value = value;
    }
```

### UnnecessaryModifier
Modifier `private` is redundant for enum constructors
in `inlong-sort-standalone/sort-standalone-common/src/main/java/org/apache/inlong/sort/standalone/config/pojo/type/CacheType.java`
#### Snippet
```java
     * @param value
     */
    private CacheType(String value) {
        this.value = value;
    }
```

### UnnecessaryModifier
Modifier `private` is redundant for enum constructors
in `inlong-sort-standalone/sort-standalone-common/src/main/java/org/apache/inlong/sort/standalone/config/pojo/type/SortType.java`
#### Snippet
```java
     * @param value
     */
    private SortType(String value) {
        this.value = value;
    }
```

### UnnecessaryModifier
Modifier `private` is redundant for enum constructors
in `inlong-sort-standalone/sort-standalone-common/src/main/java/org/apache/inlong/sort/standalone/config/pojo/type/DataType.java`
#### Snippet
```java
     * @param value
     */
    private DataType(String value) {
        this.value = value;
    }
```

### UnnecessaryModifier
Modifier `private` is redundant for enum constructors
in `inlong-sort-standalone/sort-standalone-common/src/main/java/org/apache/inlong/sort/standalone/config/holder/AckPolicy.java`
#### Snippet
```java
     * @param value
     */
    private AckPolicy(int value) {
        this.value = value;
    }
```

## RuleId[id=PointlessArithmeticExpression]
### PointlessArithmeticExpression
`1 * 60 * 1000` can be replaced with '60 \* 1000'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/TBaseConstants.java`
#### Snippet
```java

    public static final long CFG_DEF_META_FORCE_UPDATE_PERIOD = 3 * 60 * 1000;
    public static final long CFG_MIN_META_FORCE_UPDATE_PERIOD = 1 * 60 * 1000;
    public static final long CFG_STATS_MIN_SNAPSHOT_PERIOD_MS = 2000;

```

### PointlessArithmeticExpression
`1 * 3600 * 1000` can be replaced with '3600 \* 1000'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/utils/DataStoreUtils.java`
#### Snippet
```java
    public static final int STORE_DATA_TOKER_BEGIN_VALUE = 0x2C998B8;
    public static final int STORE_DATA_TOKER_BLANK_VALUE = 0x2C99B5E;
    public static final long MAX_FILE_ROLL_CHECK_DURATION = 1 * 3600 * 1000;
    public static final long MAX_FILE_NO_WRITE_DURATION = 2 * 24 * 3600 * 1000;
    public static final long MAX_FILE_VALID_DURATION = 168 * 3600L * 1000L;
```

### PointlessArithmeticExpression
`1000 * 1` can be replaced with '1000'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/heartbeat/HeartbeatManager.java`
#### Snippet
```java
                            }
                        }
                        Thread.sleep(1000 * 1);
                    } catch (Throwable t) {
                        logger.error(new StringBuilder(256)
```

## RuleId[id=LoopConditionNotUpdatedInsideLoop]
### LoopConditionNotUpdatedInsideLoop
Variable 'count' is not updated inside loop
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/example/UdpClientExample.java`
#### Snippet
```java
        try {
            int count = 0;
            while (count < sentCount) {
                if (count % 1000 == 0) {
                    long seqId = idGenerator.getNextInt();
```

### LoopConditionNotUpdatedInsideLoop
Variable 'sentCount' is not updated inside loop
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/example/UdpClientExample.java`
#### Snippet
```java
        try {
            int count = 0;
            while (count < sentCount) {
                if (count % 1000 == 0) {
                    long seqId = idGenerator.getNextInt();
```

## RuleId[id=DoubleCheckedLocking]
### DoubleCheckedLocking
Double-checked locking
in `inlong-agent/agent-core/src/main/java/org/apache/inlong/agent/core/HeartbeatManager.java`
#### Snippet
```java

    public static HeartbeatManager getInstance(AgentManager agentManager) {
        if (heartbeatManager == null) {
            synchronized (HeartbeatManager.class) {
                if (heartbeatManager == null) {
```

### DoubleCheckedLocking
Double-checked locking
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/message/SequentialID.java`
#### Snippet
```java
    public static synchronized SequentialID getInstance() {

        if (uniqueSequentialID == null) {
            synchronized (SequentialID.class) {
                if (uniqueSequentialID == null) {
```

## RuleId[id=EmptyStatementBody]
### EmptyStatementBody
`else` statement has empty body
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/utils/EncryptUtil.java`
#### Snippet
```java
            if (file.exists()) {
                len = (int) file.length();
            } else {
                // error
            }
```

### EmptyStatementBody
`while` statement has empty body
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/utils/EncryptUtil.java`
#### Snippet
```java
            DataInputStream dis = new DataInputStream(new BufferedInputStream(new FileInputStream(path)));
            byte[] content = new byte[len];
            while (dis.read(content) != -1) {

            }
```

### EmptyStatementBody
`else` statement has empty body
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/utils/EncryptUtil.java`
#### Snippet
```java
            if (file.exists()) {
                len = (int) file.length();
            } else {
                // error
            }
```

### EmptyStatementBody
`while` statement has empty body
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/utils/EncryptUtil.java`
#### Snippet
```java
            DataInputStream dis = new DataInputStream(new BufferedInputStream(new FileInputStream(path)));
            byte[] content = new byte[len];
            while (dis.read(content) != -1) {

            }
```

### EmptyStatementBody
`if` statement has empty body
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/pulsar/src/main/java/org/apache/inlong/sort/pulsar/table/DynamicPulsarSerializationSchema.java`
#### Snippet
```java
        final RowData valueRow = createProjectedRow(consumedRow, kind, valueFieldGetters);
        if (upsertMode) {
            if (kind == RowKind.DELETE || kind == RowKind.UPDATE_BEFORE) {
                // transform the message as the tombstone message
            } else {
```

### EmptyStatementBody
`else` statement has empty body
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mongodb-cdc/src/main/java/org/apache/inlong/sort/cdc/mongodb/table/MongoDBTableSource.java`
#### Snippet
```java
        } else if (StringUtils.isNotEmpty(collection)) {
            collectionList = collection;
        } else {
            // Watching all changes on the cluster by default, we do nothing here
        }
```

### EmptyStatementBody
`if` statement has empty body
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/disk/FileSegment.java`
#### Snippet
```java
    @Override
    public void read(ByteBuffer bf, long absOffset) throws IOException {
        if (this.isExpired()) {
            // Todo: conduct file closed and expired cases.
        }
```

### EmptyStatementBody
`if` statement has empty body
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/disk/FileSegment.java`
#### Snippet
```java
    @Override
    public void relRead(final ByteBuffer bf, long relOffset) throws IOException {
        if (this.isExpired()) {
            // Todo: conduct file closed and expired cases.
        }
```

### EmptyStatementBody
`if` statement has empty body
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/listener/consume/apply/ApproveConsumeProcessListener.java`
#### Snippet
```java
        } else if (MQType.PULSAR.equals(mqType) || MQType.TDMQ_PULSAR.equals(mqType)) {
            this.createPulsarSubscription(entity);
        } else if (MQType.KAFKA.equals(mqType)) {
            // Kafka consumers do not need to register in advance
        } else {
```

## RuleId[id=CommentedOutCode]
### CommentedOutCode
Commented out code (2 lines)
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/context/SdkSinkContext.java`
#### Snippet
```java
                metricItem.wholeDuration.addAndGet(wholeDuration * count);
            }
            // LOG.info("addSendTrueMetric,bid:{},result:{},sendTime:{},count:{},metric:{}",
            // bid, result, sendTime, currentRecord.getCount(), JSON.toJSONString(metricItemSet.getItemMap()));
        } else {
```

### CommentedOutCode
Commented out code (2 lines)
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/context/SdkSinkContext.java`
#### Snippet
```java
            metricItem.sendFailCount.addAndGet(count);
            metricItem.sendFailSize.addAndGet(size);
            // LOG.info("addSendFalseMetric,bid:{},result:{},sendTime:{},count:{},metric:{}",
            // bid, result, sendTime, currentRecord.getCount(), JSON.toJSONString(metricItemSet.getItemMap()));
        }
```

### CommentedOutCode
Commented out code (2 lines)
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/context/SdkSinkContext.java`
#### Snippet
```java
        metricItem.sendCount.addAndGet(count);
        metricItem.sendSize.addAndGet(size);
        // LOG.info("addSendMetric,bid:{},count:{},metric:{}",
        // bid, currentRecord.getCount(), JSON.toJSONString(metricItemSet.getItemMap()));
    }
```

### CommentedOutCode
Commented out code (3 lines)
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/utils/ProxyUtils.java`
#### Snippet
```java
        }
        if (!clientConfig.isLocalVisit()) {
            // if(!clientConfig.isNeedDataEncry()) {
            // throw new IllegalArgumentException("OutNetwork visit isNeedDataEncry must be true!");
            // }
```

### CommentedOutCode
Commented out code (27 lines)
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/network/TcpChannelGroup.java`
#### Snippet
```java
        }
    }
    // public static void main(String[] args) {
    // String[] data = new String[]{"10.56.81.205:46801",
    // "10.56.81.211:46801",
```

### CommentedOutCode
Commented out code (16 lines)
in `inlong-common/src/main/java/org/apache/inlong/common/msg/InLongMsgAttrBuilder.java`
#### Snippet
```java
                .println("\nAttention !!!! m=0 is contained by m=100, so just use m=100");

        // long time = System.currentTimeMillis();
        // byte[] data0 = ("id," + time + ",other,data").getBytes();
        // String attr = InLongMsgProtocolFactory.getProtololM100().setSpliter(",")
```

### CommentedOutCode
Commented out code (2 lines)
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/tools/cli/CliProducer.java`
#### Snippet
```java
        addCommandOption(CliArgDef.MESSAGES);
        addCommandOption(CliArgDef.MSGDATASIZE);
        // addCommandOption(CliArgDef.PAYLOADFILE);
        // addCommandOption(CliArgDef.PAYLOADDELIM);
        addCommandOption(CliArgDef.PRDTOPIC);
```

### CommentedOutCode
Commented out code (2 lines)
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/zookeeper/RecoverableZooKeeper.java`
#### Snippet
```java
                            throw e;
                        }
                        // logger.debug("Node " + path +
                        // " already exists and this is not a " + "retry");
                        throw e;
```

### CommentedOutCode
Commented out code (2 lines)
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/action/screen/config/BrokerList.java`
#### Snippet
```java
        String strPageNum = req.getParameter("page_num");
        String strPageSize = req.getParameter("page_size");
        // String strTopicName = req.getParameter("topicName");
        // String strConsumeGroup = req.getParameter("consumeGroup");
        int pageNum = TStringUtils.isNotEmpty(strPageNum)
```

### CommentedOutCode
Commented out code (4 lines)
in `inlong-tubemq/tubemq-example/src/main/java/org/apache/inlong/tubemq/example/MessageProducerExample.java`
#### Snippet
```java
                // Or
                // 5.2.2 Send message synchronous, not recommended
                // MessageSentResult result = messageProducer.sendMessage(message);
                // if (!result.isSuccess()) {
                // logger.error("Sync-send message failed!" + result.getErrMsg());
```

### CommentedOutCode
Commented out code (5 lines)
in `inlong-tubemq/tubemq-example/src/main/java/org/apache/inlong/tubemq/example/MAMessageProducerExample.java`
#### Snippet
```java
                    // Or
                    // 2.2 Send message synchronous, not recommended
                    // MessageSentResult result = producer.sendMessage(message);
                    // totalSentCnt.incrementAndGet();
                    // if (!result.isSuccess()) {
```

### CommentedOutCode
Commented out code (3 lines)
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/group/InlongGroupOperator4Pulsar.java`
#### Snippet
```java

        // TODO get the cluster from inlong_cluster by entity.getInlongClusterTag()
        // groupInfo.setTenant();
        // groupInfo.setAdminUrl();
        // groupInfo.setServiceUrl();
```

### CommentedOutCode
Commented out code (2 lines)
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/pulsar/PulsarHandler.java`
#### Snippet
```java
                    .build();
            this.baseBuilder = client.newProducer();
            // Map<String, Object> builderConf = new HashMap<>();
            // builderConf.putAll(context.getParameters());
            this.baseBuilder
```

### CommentedOutCode
Commented out code (2 lines)
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/hive/HiveSinkContext.java`
#### Snippet
```java
        metricItem.sendCount.addAndGet(count);
        metricItem.sendSize.addAndGet(size);
        // LOG.info("addSendMetric,bid:{},count:{},metric:{}",
        // bid, currentRecord.getCount(), JSON.toJSONString(metricItemSet.getItemMap()));
    }
```

### CommentedOutCode
Commented out code (2 lines)
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/hive/HiveSinkContext.java`
#### Snippet
```java
                metricItem.wholeDuration.addAndGet(wholeDuration * count);
            }
            // LOG.info("addSendTrueMetric,bid:{},result:{},sendTime:{},count:{},metric:{}",
            // bid, result, sendTime, currentRecord.getCount(), JSON.toJSONString(metricItemSet.getItemMap()));
        } else {
```

### CommentedOutCode
Commented out code (2 lines)
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/hive/HiveSinkContext.java`
#### Snippet
```java
            metricItem.sendFailCount.addAndGet(count);
            metricItem.sendFailSize.addAndGet(size);
            // LOG.info("addSendFalseMetric,bid:{},result:{},sendTime:{},count:{},metric:{}",
            // bid, result, sendTime, currentRecord.getCount(), JSON.toJSONString(metricItemSet.getItemMap()));
        }
```

## RuleId[id=RegExpRedundantEscape]
### RegExpRedundantEscape
Redundant character escape `\\-` in RegExp
in `inlong-sdk/sort-sdk/src/main/java/org/apache/inlong/sdk/sort/util/StringUtil.java`
#### Snippet
```java
            return "";
        } else {
            if (dateStr.matches("\\d{1,4}\\-\\d{1,2}\\-\\d{1,2}\\s+\\d{1,2}:\\d{1,2}:\\d{1,2}\\.\\d{1,3}")) {
                inputFormat = "yyyy-MM-dd HH:mm:ss.SSS";
            } else if (dateStr.matches("\\d{4}\\-\\d{1,2}\\-\\d{1,2} +\\d{1,2}:\\d{1,2}")) {
```

### RegExpRedundantEscape
Redundant character escape `\\-` in RegExp
in `inlong-sdk/sort-sdk/src/main/java/org/apache/inlong/sdk/sort/util/StringUtil.java`
#### Snippet
```java
            return "";
        } else {
            if (dateStr.matches("\\d{1,4}\\-\\d{1,2}\\-\\d{1,2}\\s+\\d{1,2}:\\d{1,2}:\\d{1,2}\\.\\d{1,3}")) {
                inputFormat = "yyyy-MM-dd HH:mm:ss.SSS";
            } else if (dateStr.matches("\\d{4}\\-\\d{1,2}\\-\\d{1,2} +\\d{1,2}:\\d{1,2}")) {
```

### RegExpRedundantEscape
Redundant character escape `\\-` in RegExp
in `inlong-sdk/sort-sdk/src/main/java/org/apache/inlong/sdk/sort/util/StringUtil.java`
#### Snippet
```java
            if (dateStr.matches("\\d{1,4}\\-\\d{1,2}\\-\\d{1,2}\\s+\\d{1,2}:\\d{1,2}:\\d{1,2}\\.\\d{1,3}")) {
                inputFormat = "yyyy-MM-dd HH:mm:ss.SSS";
            } else if (dateStr.matches("\\d{4}\\-\\d{1,2}\\-\\d{1,2} +\\d{1,2}:\\d{1,2}")) {
                inputFormat = "yyyy-MM-dd HH:mm:ss";
            } else if (dateStr.matches("\\d{4}\\-\\d{1,2}\\-\\d{1,2} +\\d{1,2}:\\d{1,2}")) {
```

### RegExpRedundantEscape
Redundant character escape `\\-` in RegExp
in `inlong-sdk/sort-sdk/src/main/java/org/apache/inlong/sdk/sort/util/StringUtil.java`
#### Snippet
```java
            if (dateStr.matches("\\d{1,4}\\-\\d{1,2}\\-\\d{1,2}\\s+\\d{1,2}:\\d{1,2}:\\d{1,2}\\.\\d{1,3}")) {
                inputFormat = "yyyy-MM-dd HH:mm:ss.SSS";
            } else if (dateStr.matches("\\d{4}\\-\\d{1,2}\\-\\d{1,2} +\\d{1,2}:\\d{1,2}")) {
                inputFormat = "yyyy-MM-dd HH:mm:ss";
            } else if (dateStr.matches("\\d{4}\\-\\d{1,2}\\-\\d{1,2} +\\d{1,2}:\\d{1,2}")) {
```

### RegExpRedundantEscape
Redundant character escape `\\-` in RegExp
in `inlong-sdk/sort-sdk/src/main/java/org/apache/inlong/sdk/sort/util/StringUtil.java`
#### Snippet
```java
            } else if (dateStr.matches("\\d{4}\\-\\d{1,2}\\-\\d{1,2} +\\d{1,2}:\\d{1,2}")) {
                inputFormat = "yyyy-MM-dd HH:mm:ss";
            } else if (dateStr.matches("\\d{4}\\-\\d{1,2}\\-\\d{1,2} +\\d{1,2}:\\d{1,2}")) {
                inputFormat = "yyyy-MM-dd HH:mm";
            } else if (dateStr.matches("\\d{4}\\-\\d{1,2}\\-\\d{1,2} +\\d{1,2}")) {
```

### RegExpRedundantEscape
Redundant character escape `\\-` in RegExp
in `inlong-sdk/sort-sdk/src/main/java/org/apache/inlong/sdk/sort/util/StringUtil.java`
#### Snippet
```java
            } else if (dateStr.matches("\\d{4}\\-\\d{1,2}\\-\\d{1,2} +\\d{1,2}:\\d{1,2}")) {
                inputFormat = "yyyy-MM-dd HH:mm:ss";
            } else if (dateStr.matches("\\d{4}\\-\\d{1,2}\\-\\d{1,2} +\\d{1,2}:\\d{1,2}")) {
                inputFormat = "yyyy-MM-dd HH:mm";
            } else if (dateStr.matches("\\d{4}\\-\\d{1,2}\\-\\d{1,2} +\\d{1,2}")) {
```

### RegExpRedundantEscape
Redundant character escape `\\-` in RegExp
in `inlong-sdk/sort-sdk/src/main/java/org/apache/inlong/sdk/sort/util/StringUtil.java`
#### Snippet
```java
            } else if (dateStr.matches("\\d{4}\\-\\d{1,2}\\-\\d{1,2} +\\d{1,2}:\\d{1,2}")) {
                inputFormat = "yyyy-MM-dd HH:mm";
            } else if (dateStr.matches("\\d{4}\\-\\d{1,2}\\-\\d{1,2} +\\d{1,2}")) {
                inputFormat = "yyyy-MM-dd HH";
            } else if (dateStr.matches("\\d{4}\\-\\d{1,2}\\-\\d{1,2} +\\d{1,2}")) {
```

### RegExpRedundantEscape
Redundant character escape `\\-` in RegExp
in `inlong-sdk/sort-sdk/src/main/java/org/apache/inlong/sdk/sort/util/StringUtil.java`
#### Snippet
```java
            } else if (dateStr.matches("\\d{4}\\-\\d{1,2}\\-\\d{1,2} +\\d{1,2}:\\d{1,2}")) {
                inputFormat = "yyyy-MM-dd HH:mm";
            } else if (dateStr.matches("\\d{4}\\-\\d{1,2}\\-\\d{1,2} +\\d{1,2}")) {
                inputFormat = "yyyy-MM-dd HH";
            } else if (dateStr.matches("\\d{4}\\-\\d{1,2}\\-\\d{1,2} +\\d{1,2}")) {
```

### RegExpRedundantEscape
Redundant character escape `\\-` in RegExp
in `inlong-sdk/sort-sdk/src/main/java/org/apache/inlong/sdk/sort/util/StringUtil.java`
#### Snippet
```java
            } else if (dateStr.matches("\\d{4}\\-\\d{1,2}\\-\\d{1,2} +\\d{1,2}")) {
                inputFormat = "yyyy-MM-dd HH";
            } else if (dateStr.matches("\\d{4}\\-\\d{1,2}\\-\\d{1,2} +\\d{1,2}")) {
                inputFormat = "yyyy-MM-dd";
            } else if (dateStr.matches("\\d{1,4}/\\d{1,2}/\\d{1,2}\\s+\\d{1,2}:\\d{1,2}:\\d{1,2}\\.\\d{1,3}")) {
```

### RegExpRedundantEscape
Redundant character escape `\\-` in RegExp
in `inlong-sdk/sort-sdk/src/main/java/org/apache/inlong/sdk/sort/util/StringUtil.java`
#### Snippet
```java
            } else if (dateStr.matches("\\d{4}\\-\\d{1,2}\\-\\d{1,2} +\\d{1,2}")) {
                inputFormat = "yyyy-MM-dd HH";
            } else if (dateStr.matches("\\d{4}\\-\\d{1,2}\\-\\d{1,2} +\\d{1,2}")) {
                inputFormat = "yyyy-MM-dd";
            } else if (dateStr.matches("\\d{1,4}/\\d{1,2}/\\d{1,2}\\s+\\d{1,2}:\\d{1,2}:\\d{1,2}\\.\\d{1,3}")) {
```

### RegExpRedundantEscape
Redundant character escape `\\{` in RegExp
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/elasticsearch-base/src/main/java/org/apache/inlong/sort/elasticsearch/table/IndexGeneratorFactory.java`
#### Snippet
```java
    private static class IndexHelper {

        private static final Pattern dynamicIndexPattern = Pattern.compile("\\{[^\\{\\}]+\\}?");
        private static final Pattern dynamicIndexTimeExtractPattern =
                Pattern.compile(".*\\{.+\\|.*\\}.*");
```

### RegExpRedundantEscape
Redundant character escape `\\}` in RegExp
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/elasticsearch-base/src/main/java/org/apache/inlong/sort/elasticsearch/table/IndexGeneratorFactory.java`
#### Snippet
```java
    private static class IndexHelper {

        private static final Pattern dynamicIndexPattern = Pattern.compile("\\{[^\\{\\}]+\\}?");
        private static final Pattern dynamicIndexTimeExtractPattern =
                Pattern.compile(".*\\{.+\\|.*\\}.*");
```

### RegExpRedundantEscape
Redundant character escape `\\}` in RegExp
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/elasticsearch-base/src/main/java/org/apache/inlong/sort/elasticsearch/table/IndexGeneratorFactory.java`
#### Snippet
```java
    private static class IndexHelper {

        private static final Pattern dynamicIndexPattern = Pattern.compile("\\{[^\\{\\}]+\\}?");
        private static final Pattern dynamicIndexTimeExtractPattern =
                Pattern.compile(".*\\{.+\\|.*\\}.*");
```

### RegExpRedundantEscape
Redundant character escape `\\}` in RegExp
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/elasticsearch-base/src/main/java/org/apache/inlong/sort/elasticsearch/table/IndexGeneratorFactory.java`
#### Snippet
```java
        private static final Pattern dynamicIndexPattern = Pattern.compile("\\{[^\\{\\}]+\\}?");
        private static final Pattern dynamicIndexTimeExtractPattern =
                Pattern.compile(".*\\{.+\\|.*\\}.*");
        private static final List<LogicalTypeRoot> supportedTypes = new ArrayList<>();
        private static final Map<LogicalTypeRoot, String> defaultFormats = new HashMap<>();
```

### RegExpRedundantEscape
Redundant character escape `\\}` in RegExp
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/hive/HdfsIdConfig.java`
#### Snippet
```java
    public static final String PATTERN_MINUTE = "{yyyyMMddHHmm}";
    public static final String REGEX_DAY = "\\{yyyyMMdd\\}";
    public static final String REGEX_HOUR = "\\{yyyyMMddHH\\}";
    public static final String REGEX_MINUTE = "\\{yyyyMMddHHmm\\}";
    public static final long HOUR_MS = 60L * 60 * 1000;
```

### RegExpRedundantEscape
Redundant character escape `\\}` in RegExp
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/hive/HdfsIdConfig.java`
#### Snippet
```java
    public static final String PATTERN_HOUR = "{yyyyMMddHH}";
    public static final String PATTERN_MINUTE = "{yyyyMMddHHmm}";
    public static final String REGEX_DAY = "\\{yyyyMMdd\\}";
    public static final String REGEX_HOUR = "\\{yyyyMMddHH\\}";
    public static final String REGEX_MINUTE = "\\{yyyyMMddHHmm\\}";
```

### RegExpRedundantEscape
Redundant character escape `\\}` in RegExp
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/hive/HdfsIdConfig.java`
#### Snippet
```java
    public static final String REGEX_DAY = "\\{yyyyMMdd\\}";
    public static final String REGEX_HOUR = "\\{yyyyMMddHH\\}";
    public static final String REGEX_MINUTE = "\\{yyyyMMddHHmm\\}";
    public static final long HOUR_MS = 60L * 60 * 1000;
    public static final int SEPARATOR_LENGTH = 1;
```

### RegExpRedundantEscape
Redundant character escape `\\}` in RegExp
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/elasticsearch/EsIdConfig.java`
#### Snippet
```java
    public static final String REGEX_DAY = "\\{yyyyMMdd\\}";
    public static final String REGEX_HOUR = "\\{yyyyMMddHH\\}";
    public static final String REGEX_MINUTE = "\\{yyyyMMddHHmm\\}";
    private static ThreadLocal<SimpleDateFormat> FORMAT_DAY = new ThreadLocal<SimpleDateFormat>() {

```

### RegExpRedundantEscape
Redundant character escape `\\}` in RegExp
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/elasticsearch/EsIdConfig.java`
#### Snippet
```java
    public static final String PATTERN_MINUTE = "{yyyyMMddHHmm}";
    public static final String REGEX_DAY = "\\{yyyyMMdd\\}";
    public static final String REGEX_HOUR = "\\{yyyyMMddHH\\}";
    public static final String REGEX_MINUTE = "\\{yyyyMMddHHmm\\}";
    private static ThreadLocal<SimpleDateFormat> FORMAT_DAY = new ThreadLocal<SimpleDateFormat>() {
```

### RegExpRedundantEscape
Redundant character escape `\\}` in RegExp
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/elasticsearch/EsIdConfig.java`
#### Snippet
```java
    public static final String PATTERN_HOUR = "{yyyyMMddHH}";
    public static final String PATTERN_MINUTE = "{yyyyMMddHHmm}";
    public static final String REGEX_DAY = "\\{yyyyMMdd\\}";
    public static final String REGEX_HOUR = "\\{yyyyMMddHH\\}";
    public static final String REGEX_MINUTE = "\\{yyyyMMddHHmm\\}";
```

## RuleId[id=UastIncorrectHttpHeaderInspection]
### UastIncorrectHttpHeaderInspection
Unknown HTTP header
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/action/screen/Webapi.java`
#### Snippet
```java
                requestContext.put("sb", strCallbackFun + "(" + sBuffer.toString() + ")");
                requestContext.getResp().addHeader("Content-type", "text/plain");
                requestContext.getResp().addHeader("charset", TBaseConstants.META_DEFAULT_CHARSET_NAME);
            }
        } catch (Throwable e) {
```

## RuleId[id=SpringJavaInjectionPointsAutowiringInspection]
### SpringJavaInjectionPointsAutowiringInspection
Autowired members must be defined in valid Spring bean (@Component\|@Service\|...)
in `inlong-manager/manager-test/src/main/java/org/apache/inlong/manager/test/BaseTest.java`
#### Snippet
```java
public class BaseTest {

    @Autowired
    private DataSource dataSource;

```

### SpringJavaInjectionPointsAutowiringInspection
Autowired members must be defined in valid Spring bean (@Component\|@Service\|...)
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sink/oracle/OracleResourceOperator.java`
#### Snippet
```java
    private static final Logger LOG = LoggerFactory.getLogger(OracleResourceOperator.class);

    @Autowired
    private StreamSinkService sinkService;

```

### SpringJavaInjectionPointsAutowiringInspection
Autowired members must be defined in valid Spring bean (@Component\|@Service\|...)
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sink/oracle/OracleResourceOperator.java`
#### Snippet
```java
    private StreamSinkService sinkService;

    @Autowired
    private StreamSinkFieldEntityMapper fieldEntityMapper;

```

### SpringJavaInjectionPointsAutowiringInspection
Autowired members must be defined in valid Spring bean (@Component\|@Service\|...)
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sink/greenplum/GreenplumResourceOperator.java`
#### Snippet
```java
    private StreamSinkService sinkService;

    @Autowired
    private StreamSinkFieldEntityMapper fieldEntityMapper;

```

### SpringJavaInjectionPointsAutowiringInspection
Autowired members must be defined in valid Spring bean (@Component\|@Service\|...)
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sink/greenplum/GreenplumResourceOperator.java`
#### Snippet
```java
    public static final String GREENPLUM_DEFAULT_SCHEMA = "public";

    @Autowired
    private StreamSinkService sinkService;

```

## RuleId[id=NotNullFieldNotInitialized]
### NotNullFieldNotInitialized
Not-null fields must be initialized
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kudu/src/main/java/org/apache/inlong/sort/kudu/common/Kudu.java`
#### Snippet
```java
    @Nonnull
    private final Map<String, String> properties;
    @Nonnull
    private String table;
    @Nonnull
```

### NotNullFieldNotInitialized
Not-null fields must be initialized
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kudu/src/main/java/org/apache/inlong/sort/kudu/common/Kudu.java`
#### Snippet
```java
    @Nonnull
    private String table;
    @Nonnull
    private String masters;

```

## RuleId[id=Deprecation]
### Deprecation
'SslContextFactory()' is deprecated
in `inlong-sdk/sdk-common/src/main/java/org/apache/inlong/sdk/commons/admin/AdminHttpSource.java`
#### Snippet
```java
        FlumeBeanConfigurator.setConfigurationFields(httpConfiguration, sourceContext);
        ServerConnector connector = getSslContextSupplier().get().map(sslContext -> {
            SslContextFactory sslCtxFactory = new SslContextFactory();
            sslCtxFactory.setSslContext(sslContext);
            sslCtxFactory.setExcludeProtocols(getExcludeProtocols().toArray(new String[]{}));
```

### Deprecation
'propagate(java.lang.Throwable)' is deprecated
in `inlong-sdk/sdk-common/src/main/java/org/apache/inlong/sdk/commons/admin/AdminHttpSource.java`
#### Snippet
```java
        } catch (Exception ex) {
            LOG.error("Error while starting HTTPSource. Exception follows.", ex);
            Throwables.propagate(ex);
        }
        Preconditions.checkArgument(srv.isRunning());
```

### Deprecation
'propagate(java.lang.Throwable)' is deprecated
in `inlong-sdk/sdk-common/src/main/java/org/apache/inlong/sdk/commons/admin/AdminHttpSource.java`
#### Snippet
```java
        } catch (ClassNotFoundException ex) {
            LOG.error("Error while configuring HTTPSource. Exception follows.", ex);
            Throwables.propagate(ex);
        } catch (ClassCastException ex) {
            LOG.error("Deserializer is not an instance of HTTPSourceHandler."
```

### Deprecation
'propagate(java.lang.Throwable)' is deprecated
in `inlong-sdk/sdk-common/src/main/java/org/apache/inlong/sdk/commons/admin/AdminHttpSource.java`
#### Snippet
```java
            LOG.error("Deserializer is not an instance of HTTPSourceHandler."
                    + "Deserializer must implement HTTPSourceHandler.");
            Throwables.propagate(ex);
        } catch (Exception ex) {
            LOG.error("Error configuring HTTPSource!", ex);
```

### Deprecation
'propagate(java.lang.Throwable)' is deprecated
in `inlong-sdk/sdk-common/src/main/java/org/apache/inlong/sdk/commons/admin/AdminHttpSource.java`
#### Snippet
```java
        } catch (Exception ex) {
            LOG.error("Error configuring HTTPSource!", ex);
            Throwables.propagate(ex);
        }
        if (sourceCounter == null) {
```

### Deprecation
'org.apache.http.params.HttpParams' is deprecated
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/utils/ServiceDiscoveryUtils.java`
#### Snippet
```java
        log.info("Begin to get configure from manager {}, param is {}", url, params);
        CloseableHttpClient httpClient;
        HttpParams myParams = new BasicHttpParams();
        HttpConnectionParams.setConnectionTimeout(myParams, proxyClientConfig.getManagerConnectionTimeout());
        HttpConnectionParams.setSoTimeout(myParams, proxyClientConfig.getManagerSocketTimeout());
```

### Deprecation
'org.apache.http.params.BasicHttpParams' is deprecated
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/utils/ServiceDiscoveryUtils.java`
#### Snippet
```java
        log.info("Begin to get configure from manager {}, param is {}", url, params);
        CloseableHttpClient httpClient;
        HttpParams myParams = new BasicHttpParams();
        HttpConnectionParams.setConnectionTimeout(myParams, proxyClientConfig.getManagerConnectionTimeout());
        HttpConnectionParams.setSoTimeout(myParams, proxyClientConfig.getManagerSocketTimeout());
```

### Deprecation
'org.apache.http.params.HttpConnectionParams' is deprecated
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/utils/ServiceDiscoveryUtils.java`
#### Snippet
```java
        CloseableHttpClient httpClient;
        HttpParams myParams = new BasicHttpParams();
        HttpConnectionParams.setConnectionTimeout(myParams, proxyClientConfig.getManagerConnectionTimeout());
        HttpConnectionParams.setSoTimeout(myParams, proxyClientConfig.getManagerSocketTimeout());
        if (proxyClientConfig.isLocalVisit()) {
```

### Deprecation
'org.apache.http.params.HttpConnectionParams' is deprecated
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/utils/ServiceDiscoveryUtils.java`
#### Snippet
```java
        HttpParams myParams = new BasicHttpParams();
        HttpConnectionParams.setConnectionTimeout(myParams, proxyClientConfig.getManagerConnectionTimeout());
        HttpConnectionParams.setSoTimeout(myParams, proxyClientConfig.getManagerSocketTimeout());
        if (proxyClientConfig.isLocalVisit()) {
            httpClient = new DefaultHttpClient(myParams);
```

### Deprecation
'org.apache.http.impl.client.DefaultHttpClient' is deprecated
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/utils/ServiceDiscoveryUtils.java`
#### Snippet
```java
        HttpConnectionParams.setSoTimeout(myParams, proxyClientConfig.getManagerSocketTimeout());
        if (proxyClientConfig.isLocalVisit()) {
            httpClient = new DefaultHttpClient(myParams);
        } else {
            try {
```

### Deprecation
'JsonParser()' is deprecated
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/utils/ServiceDiscoveryUtils.java`
#### Snippet
```java
            if (Utils.isNotBlank(returnStr) && response.getStatusLine().getStatusCode() == 200) {
                log.info("Get configure from manager is " + returnStr);
                JsonParser jsonParser = new JsonParser();
                JsonObject jb = jsonParser.parse(returnStr).getAsJsonObject();
                if (jb == null) {
```

### Deprecation
'parse(java.lang.String)' is deprecated
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/utils/ServiceDiscoveryUtils.java`
#### Snippet
```java
                log.info("Get configure from manager is " + returnStr);
                JsonParser jsonParser = new JsonParser();
                JsonObject jb = jsonParser.parse(returnStr).getAsJsonObject();
                if (jb == null) {
                    log.warn("ServiceDiscovery updated manager ip failed, returnStr = {} jb is "
```

### Deprecation
'getConnectionManager()' is deprecated
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/utils/ServiceDiscoveryUtils.java`
#### Snippet
```java
            }
            if (httpClient != null) {
                httpClient.getConnectionManager().shutdown();
            }
        }
```

### Deprecation
'JsonParser()' is deprecated
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/http/InternalHttpSender.java`
#### Snippet
```java
            .newCachedThreadPool();
    private CloseableHttpClient httpClient;
    private final JsonParser jsonParser = new JsonParser();
    private boolean bShutDown = false;

```

### Deprecation
'parse(java.lang.String)' is deprecated
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/http/InternalHttpSender.java`
#### Snippet
```java
            if (Utils.isNotBlank(returnStr) && response.getStatusLine().getStatusCode() == 200) {
                logger.debug("Get configure from manager is " + returnStr);
                JsonObject jsonRes = jsonParser.parse(returnStr).getAsJsonObject();

                if (jsonRes.has("code")) {
```

### Deprecation
'hmacSha1(java.lang.String, java.lang.String)' is deprecated
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/Utils.java`
#### Snippet
```java
    public static String generateSignature(String secureId, long timestamp, int randomValue, String secureKey) {
        Base64 base64 = new Base64();
        byte[] baseStr = base64.encode(HmacUtils.hmacSha1(secureKey, secureId + timestamp + randomValue));
        String result = "";
        try {
```

### Deprecation
'getClusterId()' is deprecated
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/config/ProxyConfigManager.java`
#### Snippet
```java

        int clusterId = -1;
        if (ObjectUtils.isNotEmpty(proxyCluster.getClusterId())) {
            clusterId = proxyCluster.getClusterId();
        }
```

### Deprecation
'getClusterId()' is deprecated
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/config/ProxyConfigManager.java`
#### Snippet
```java
        int clusterId = -1;
        if (ObjectUtils.isNotEmpty(proxyCluster.getClusterId())) {
            clusterId = proxyCluster.getClusterId();
        }
        int load = ConfigConstants.LOAD_THRESHOLD;
```

### Deprecation
'org.apache.http.params.HttpParams' is deprecated
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/config/ProxyConfigManager.java`
#### Snippet
```java
            HttpPost httpPost = null;
            String returnStr = null;
            HttpParams myParams = new BasicHttpParams();
            HttpConnectionParams.setConnectionTimeout(myParams, 10000);
            HttpConnectionParams.setSoTimeout(myParams, clientConfig.getManagerSocketTimeout());
```

### Deprecation
'org.apache.http.params.BasicHttpParams' is deprecated
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/config/ProxyConfigManager.java`
#### Snippet
```java
            HttpPost httpPost = null;
            String returnStr = null;
            HttpParams myParams = new BasicHttpParams();
            HttpConnectionParams.setConnectionTimeout(myParams, 10000);
            HttpConnectionParams.setSoTimeout(myParams, clientConfig.getManagerSocketTimeout());
```

### Deprecation
'org.apache.http.params.HttpConnectionParams' is deprecated
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/config/ProxyConfigManager.java`
#### Snippet
```java
            String returnStr = null;
            HttpParams myParams = new BasicHttpParams();
            HttpConnectionParams.setConnectionTimeout(myParams, 10000);
            HttpConnectionParams.setSoTimeout(myParams, clientConfig.getManagerSocketTimeout());
            CloseableHttpClient httpClient = null;
```

### Deprecation
'org.apache.http.params.HttpConnectionParams' is deprecated
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/config/ProxyConfigManager.java`
#### Snippet
```java
            HttpParams myParams = new BasicHttpParams();
            HttpConnectionParams.setConnectionTimeout(myParams, 10000);
            HttpConnectionParams.setSoTimeout(myParams, clientConfig.getManagerSocketTimeout());
            CloseableHttpClient httpClient = null;
            if (this.clientConfig.isLocalVisit()) {
```

### Deprecation
'org.apache.http.impl.client.DefaultHttpClient' is deprecated
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/config/ProxyConfigManager.java`
#### Snippet
```java
            CloseableHttpClient httpClient = null;
            if (this.clientConfig.isLocalVisit()) {
                httpClient = new DefaultHttpClient(myParams);
            } else {
                try {
```

### Deprecation
'getConnectionManager()' is deprecated
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/config/ProxyConfigManager.java`
#### Snippet
```java
                }
                if (httpClient != null) {
                    httpClient.getConnectionManager().shutdown();
                }
            }
```

### Deprecation
'parse(java.lang.String)' is deprecated
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/config/ProxyConfigManager.java`
#### Snippet
```java
            return null;
        }
        JsonObject pubKeyConf = jsonParser.parse(returnStr).getAsJsonObject();
        if (pubKeyConf == null) {
            LOGGER.info("No public key information returned from manager");
```

### Deprecation
'JsonParser()' is deprecated
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/config/ProxyConfigManager.java`
#### Snippet
```java
    private final ClientMgr clientManager;
    private final ReentrantReadWriteLock rw = new ReentrantReadWriteLock();
    private final JsonParser jsonParser = new JsonParser();
    private final Gson gson = new Gson();
    private final HashRing hashRing = HashRing.getInstance();
```

### Deprecation
'equals(java.lang.Object, java.lang.Object)' is deprecated
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/Sender.java`
#### Snippet
```java
        // used by sync send
        for (SyncMessageCallable syncCallBack : syncCallables.values()) {
            if (ObjectUtils.equals(client, syncCallBack.getClient())) {
                return false;
            }
```

### Deprecation
'org.apache.commons.lang3.StringEscapeUtils' is deprecated
in `inlong-sort/sort-flink/base/src/main/java/org/apache/inlong/sort/base/dirty/sink/s3/S3DirtySink.java`
#### Snippet
```java
        String content = null;
        try {
            content = StringUtils.join(values, StringEscapeUtils.unescapeJava(s3Options.getLineDelimiter()));
            s3Helper.upload(identifier, content);
            LOGGER.info("Write {} records to s3 of identifier: {}", values.size(), identifier);
```

### Deprecation
'COLUMN_BLACKLIST' is deprecated
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/util/ColumnFilterUtil.java`
#### Snippet
```java
import io.debezium.relational.history.TableChanges;

import static io.debezium.relational.RelationalDatabaseConnectorConfig.COLUMN_BLACKLIST;
import static io.debezium.relational.RelationalDatabaseConnectorConfig.COLUMN_WHITELIST;
import static io.debezium.relational.RelationalDatabaseConnectorConfig.COLUMN_EXCLUDE_LIST;
```

### Deprecation
'COLUMN_WHITELIST' is deprecated
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/util/ColumnFilterUtil.java`
#### Snippet
```java

import static io.debezium.relational.RelationalDatabaseConnectorConfig.COLUMN_BLACKLIST;
import static io.debezium.relational.RelationalDatabaseConnectorConfig.COLUMN_WHITELIST;
import static io.debezium.relational.RelationalDatabaseConnectorConfig.COLUMN_EXCLUDE_LIST;
import static io.debezium.relational.RelationalDatabaseConnectorConfig.COLUMN_INCLUDE_LIST;
```

### Deprecation
'COLUMN_BLACKLIST' is deprecated
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/util/ColumnFilterUtil.java`
#### Snippet
```java
            Configuration configuration, ColumnFilterMode columnFilterMode) {

        String columnExcludeList = configuration.getFallbackStringProperty(COLUMN_EXCLUDE_LIST, COLUMN_BLACKLIST);
        String columnIncludeList = configuration.getFallbackStringProperty(COLUMN_INCLUDE_LIST, COLUMN_WHITELIST);

```

### Deprecation
'COLUMN_WHITELIST' is deprecated
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/util/ColumnFilterUtil.java`
#### Snippet
```java

        String columnExcludeList = configuration.getFallbackStringProperty(COLUMN_EXCLUDE_LIST, COLUMN_BLACKLIST);
        String columnIncludeList = configuration.getFallbackStringProperty(COLUMN_INCLUDE_LIST, COLUMN_WHITELIST);

        Tables.ColumnNameFilter columnFilter;
```

### Deprecation
'getYear()' is deprecated
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/util/TemporalConversions.java`
#### Snippet
```java
            java.sql.Timestamp timestamp = (java.sql.Timestamp) obj;
            return LocalDateTime.of(
                    timestamp.getYear() + 1900,
                    timestamp.getMonth() + 1,
                    timestamp.getDate(),
```

### Deprecation
'getMonth()' is deprecated
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/util/TemporalConversions.java`
#### Snippet
```java
            return LocalDateTime.of(
                    timestamp.getYear() + 1900,
                    timestamp.getMonth() + 1,
                    timestamp.getDate(),
                    timestamp.getHours(),
```

### Deprecation
'getDate()' is deprecated
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/util/TemporalConversions.java`
#### Snippet
```java
                    timestamp.getYear() + 1900,
                    timestamp.getMonth() + 1,
                    timestamp.getDate(),
                    timestamp.getHours(),
                    timestamp.getMinutes(),
```

### Deprecation
'getHours()' is deprecated
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/util/TemporalConversions.java`
#### Snippet
```java
                    timestamp.getMonth() + 1,
                    timestamp.getDate(),
                    timestamp.getHours(),
                    timestamp.getMinutes(),
                    timestamp.getSeconds(),
```

### Deprecation
'getMinutes()' is deprecated
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/util/TemporalConversions.java`
#### Snippet
```java
                    timestamp.getDate(),
                    timestamp.getHours(),
                    timestamp.getMinutes(),
                    timestamp.getSeconds(),
                    timestamp.getNanos());
```

### Deprecation
'getSeconds()' is deprecated
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/util/TemporalConversions.java`
#### Snippet
```java
                    timestamp.getHours(),
                    timestamp.getMinutes(),
                    timestamp.getSeconds(),
                    timestamp.getNanos());
        }
```

### Deprecation
'getYear()' is deprecated
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/util/TemporalConversions.java`
#### Snippet
```java
            int nanosOfSecond = (int) (millis * NANOSECONDS_PER_MILLISECOND);
            return LocalDateTime.of(
                    date.getYear() + 1900,
                    date.getMonth() + 1,
                    date.getDate(),
```

### Deprecation
'getMonth()' is deprecated
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/util/TemporalConversions.java`
#### Snippet
```java
            return LocalDateTime.of(
                    date.getYear() + 1900,
                    date.getMonth() + 1,
                    date.getDate(),
                    date.getHours(),
```

### Deprecation
'getDate()' is deprecated
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/util/TemporalConversions.java`
#### Snippet
```java
                    date.getYear() + 1900,
                    date.getMonth() + 1,
                    date.getDate(),
                    date.getHours(),
                    date.getMinutes(),
```

### Deprecation
'getHours()' is deprecated
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/util/TemporalConversions.java`
#### Snippet
```java
                    date.getMonth() + 1,
                    date.getDate(),
                    date.getHours(),
                    date.getMinutes(),
                    date.getSeconds(),
```

### Deprecation
'getMinutes()' is deprecated
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/util/TemporalConversions.java`
#### Snippet
```java
                    date.getDate(),
                    date.getHours(),
                    date.getMinutes(),
                    date.getSeconds(),
                    nanosOfSecond);
```

### Deprecation
'getSeconds()' is deprecated
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/util/TemporalConversions.java`
#### Snippet
```java
                    date.getHours(),
                    date.getMinutes(),
                    date.getSeconds(),
                    nanosOfSecond);
        }
```

### Deprecation
'getYear()' is deprecated
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/util/TemporalConversions.java`
#### Snippet
```java
        if (obj instanceof java.util.Date) {
            java.util.Date date = (java.util.Date) obj;
            return LocalDate.of(date.getYear() + 1900, date.getMonth() + 1, date.getDate());
        }
        if (obj instanceof Long) {
```

### Deprecation
'getMonth()' is deprecated
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/util/TemporalConversions.java`
#### Snippet
```java
        if (obj instanceof java.util.Date) {
            java.util.Date date = (java.util.Date) obj;
            return LocalDate.of(date.getYear() + 1900, date.getMonth() + 1, date.getDate());
        }
        if (obj instanceof Long) {
```

### Deprecation
'getDate()' is deprecated
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/util/TemporalConversions.java`
#### Snippet
```java
        if (obj instanceof java.util.Date) {
            java.util.Date date = (java.util.Date) obj;
            return LocalDate.of(date.getYear() + 1900, date.getMonth() + 1, date.getDate());
        }
        if (obj instanceof Long) {
```

### Deprecation
'getHours()' is deprecated
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/util/TemporalConversions.java`
#### Snippet
```java
            int nanosOfSecond = (int) (millis * NANOSECONDS_PER_MILLISECOND);
            return LocalTime.of(
                    time.getHours(), time.getMinutes(), time.getSeconds(), nanosOfSecond);
        }
        if (obj instanceof java.sql.Timestamp) {
```

### Deprecation
'getMinutes()' is deprecated
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/util/TemporalConversions.java`
#### Snippet
```java
            int nanosOfSecond = (int) (millis * NANOSECONDS_PER_MILLISECOND);
            return LocalTime.of(
                    time.getHours(), time.getMinutes(), time.getSeconds(), nanosOfSecond);
        }
        if (obj instanceof java.sql.Timestamp) {
```

### Deprecation
'getSeconds()' is deprecated
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/util/TemporalConversions.java`
#### Snippet
```java
            int nanosOfSecond = (int) (millis * NANOSECONDS_PER_MILLISECOND);
            return LocalTime.of(
                    time.getHours(), time.getMinutes(), time.getSeconds(), nanosOfSecond);
        }
        if (obj instanceof java.sql.Timestamp) {
```

### Deprecation
'getHours()' is deprecated
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/util/TemporalConversions.java`
#### Snippet
```java
            java.sql.Timestamp timestamp = (java.sql.Timestamp) obj;
            return LocalTime.of(
                    timestamp.getHours(),
                    timestamp.getMinutes(),
                    timestamp.getSeconds(),
```

### Deprecation
'getMinutes()' is deprecated
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/util/TemporalConversions.java`
#### Snippet
```java
            return LocalTime.of(
                    timestamp.getHours(),
                    timestamp.getMinutes(),
                    timestamp.getSeconds(),
                    timestamp.getNanos());
```

### Deprecation
'getSeconds()' is deprecated
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/util/TemporalConversions.java`
#### Snippet
```java
                    timestamp.getHours(),
                    timestamp.getMinutes(),
                    timestamp.getSeconds(),
                    timestamp.getNanos());
        }
```

### Deprecation
'getHours()' is deprecated
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/util/TemporalConversions.java`
#### Snippet
```java
            int nanosOfSecond = (int) (millis * NANOSECONDS_PER_MILLISECOND);
            return LocalTime.of(
                    date.getHours(), date.getMinutes(), date.getSeconds(), nanosOfSecond);
        }
        if (obj instanceof Duration) {
```

### Deprecation
'getMinutes()' is deprecated
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/util/TemporalConversions.java`
#### Snippet
```java
            int nanosOfSecond = (int) (millis * NANOSECONDS_PER_MILLISECOND);
            return LocalTime.of(
                    date.getHours(), date.getMinutes(), date.getSeconds(), nanosOfSecond);
        }
        if (obj instanceof Duration) {
```

### Deprecation
'getSeconds()' is deprecated
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/util/TemporalConversions.java`
#### Snippet
```java
            int nanosOfSecond = (int) (millis * NANOSECONDS_PER_MILLISECOND);
            return LocalTime.of(
                    date.getHours(), date.getMinutes(), date.getSeconds(), nanosOfSecond);
        }
        if (obj instanceof Duration) {
```

### Deprecation
'defaultValue(T)' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/hive/src/main/java/org/apache/inlong/sort/hive/HiveOptions.java`
#### Snippet
```java
    public static final ConfigOption<Integer> TABLE_EXEC_HIVE_INFER_SOURCE_PARALLELISM_MAX =
            key("table.exec.hive.infer-source-parallelism.max")
                    .defaultValue(1000)
                    .withDescription("Sets max infer parallelism for source operator.");

```

### Deprecation
'defaultValue(T)' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/hive/src/main/java/org/apache/inlong/sort/hive/HiveOptions.java`
#### Snippet
```java
    public static final ConfigOption<Boolean> TABLE_EXEC_HIVE_FALLBACK_MAPRED_READER =
            key("table.exec.hive.fallback-mapred-reader")
                    .defaultValue(false)
                    .withDescription(
                            "If it is false, using flink native vectorized reader to read orc files; "
```

### Deprecation
'defaultValue(T)' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/hive/src/main/java/org/apache/inlong/sort/hive/HiveOptions.java`
#### Snippet
```java
    public static final ConfigOption<Boolean> TABLE_EXEC_HIVE_INFER_SOURCE_PARALLELISM =
            key("table.exec.hive.infer-source-parallelism")
                    .defaultValue(true)
                    .withDescription(
                            "If is false, parallelism of source are set by config.\n"
```

### Deprecation
'org.apache.flink.table.descriptors.DescriptorProperties' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/hive/src/main/java/org/apache/inlong/sort/hive/HiveValidator.java`
#### Snippet
```java

    @Override
    public void validate(DescriptorProperties properties) {
        properties.validateValue(CONNECTOR, CONNECTOR_TYPE_VALUE_HIVE, false);
        properties.validateString(CONNECTOR_HIVE_DATABASE, false, 1);
```

### Deprecation
'org.apache.flink.table.descriptors.DescriptorProperties' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/hive/src/main/java/org/apache/inlong/sort/hive/table/HiveTableInlongFactory.java`
#### Snippet
```java
    public DynamicTableSource createDynamicTableSource(Context context) {
        Map<String, String> options = context.getCatalogTable().getOptions();
        final DescriptorProperties properties = new DescriptorProperties();
        properties.putProperties(options);

```

### Deprecation
'org.apache.flink.table.descriptors.DescriptorProperties' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/hive/src/main/java/org/apache/inlong/sort/hive/table/HiveTableInlongFactory.java`
#### Snippet
```java
    public DynamicTableSource createDynamicTableSource(Context context) {
        Map<String, String> options = context.getCatalogTable().getOptions();
        final DescriptorProperties properties = new DescriptorProperties();
        properties.putProperties(options);

```

### Deprecation
'org.apache.flink.table.api.TableSchema' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/jdbc/src/main/java/org/apache/inlong/sort/jdbc/table/AbstractJdbcDialect.java`
#### Snippet
```java

    @Override
    public void validate(TableSchema schema) throws ValidationException {
        for (int i = 0; i < schema.getFieldCount(); i++) {
            DataType dt = schema.getFieldDataType(i).get();
```

### Deprecation
'org.apache.flink.table.api.TableSchema' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/jdbc/src/main/java/org/apache/inlong/sort/jdbc/table/JdbcDynamicTableSink.java`
#### Snippet
```java
    private final JdbcExecutionOptions executionOptions;
    private final JdbcDmlOptions dmlOptions;
    private final TableSchema tableSchema;
    private final String dialectName;

```

### Deprecation
'org.apache.flink.table.api.TableSchema' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/jdbc/src/main/java/org/apache/inlong/sort/jdbc/table/JdbcDynamicTableSink.java`
#### Snippet
```java
            JdbcExecutionOptions executionOptions,
            JdbcDmlOptions dmlOptions,
            TableSchema tableSchema,
            boolean appendMode,
            boolean multipleSink,
```

### Deprecation
'writeUsingOutputFormat(org.apache.flink.api.common.io.OutputFormat)' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/hive/src/main/java/org/apache/inlong/sort/hive/HiveTableSink.java`
#### Snippet
```java
        return dataStream
                .map((MapFunction<RowData, Row>) value -> (Row) converter.toExternal(value))
                .writeUsingOutputFormat(builder.build())
                .setParallelism(parallelism);
    }
```

### Deprecation
'org.apache.flink.table.api.TableSchema' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/hive/src/main/java/org/apache/inlong/sort/hive/HiveTableSink.java`
#### Snippet
```java
    private final CatalogTable catalogTable;
    private final ObjectIdentifier identifier;
    private final TableSchema tableSchema;
    private final String hiveVersion;
    private final HiveShim hiveShim;
```

### Deprecation
'getSchema()' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/hive/src/main/java/org/apache/inlong/sort/hive/HiveTableSink.java`
#### Snippet
```java
                        "Hive version is not defined");
        hiveShim = HiveShimLoader.loadHiveShim(hiveVersion);
        tableSchema = TableSchemaUtils.getPhysicalSchema(table.getSchema());
        this.configuredParallelism = configuredParallelism;
        this.inlongMetric = inlongMetric;
```

### Deprecation
'org.apache.flink.table.api.TableSchema' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/jdbc/src/main/java/org/apache/inlong/sort/jdbc/table/JdbcDynamicTableFactory.java`
#### Snippet
```java
        validateSinkMultiple(multipleSink, sinkMultipleFormat, databasePattern, schemaPattern, tablePattern);
        JdbcOptions jdbcOptions = getJdbcOptions(config);
        TableSchema physicalSchema =
                TableSchemaUtils.getPhysicalSchema(context.getCatalogTable().getSchema());
        boolean appendMode = config.get(SINK_APPEND_MODE);
```

### Deprecation
'getSchema()' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/jdbc/src/main/java/org/apache/inlong/sort/jdbc/table/JdbcDynamicTableFactory.java`
#### Snippet
```java
        JdbcOptions jdbcOptions = getJdbcOptions(config);
        TableSchema physicalSchema =
                TableSchemaUtils.getPhysicalSchema(context.getCatalogTable().getSchema());
        boolean appendMode = config.get(SINK_APPEND_MODE);
        String inlongMetric = config.getOptional(INLONG_METRIC).orElse(null);
```

### Deprecation
'org.apache.flink.table.api.TableSchema' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/jdbc/src/main/java/org/apache/inlong/sort/jdbc/table/JdbcDynamicTableFactory.java`
#### Snippet
```java
        helper.validate();
        validateConfigOptions(config);
        TableSchema physicalSchema =
                TableSchemaUtils.getPhysicalSchema(context.getCatalogTable().getSchema());
        return new JdbcDynamicTableSource(
```

### Deprecation
'getSchema()' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/jdbc/src/main/java/org/apache/inlong/sort/jdbc/table/JdbcDynamicTableFactory.java`
#### Snippet
```java
        validateConfigOptions(config);
        TableSchema physicalSchema =
                TableSchemaUtils.getPhysicalSchema(context.getCatalogTable().getSchema());
        return new JdbcDynamicTableSource(
                getJdbcOptions(helper.getOptions()),
```

### Deprecation
'org.apache.flink.table.api.TableSchema' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/jdbc/src/main/java/org/apache/inlong/sort/jdbc/table/JdbcDynamicTableFactory.java`
#### Snippet
```java
    }

    private JdbcDmlOptions getJdbcDmlOptions(JdbcOptions jdbcOptions, TableSchema schema) {
        String[] keyFields =
                schema.getPrimaryKey()
```

### Deprecation
'org.apache.flink.connector.jdbc.internal.executor.InsertOrUpdateJdbcExecutor' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/jdbc/src/main/java/org/apache/inlong/sort/jdbc/internal/TableJdbcUpsertOutputFormat.java`
#### Snippet
```java
                                ctx.getExecutionConfig().isObjectReuseEnabled()))
                .orElseGet(
                        () -> new InsertOrUpdateJdbcExecutor<>(
                                parseNamedStatement(
                                        opt.getDialect()
```

### Deprecation
'org.apache.flink.table.api.TableSchema' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kudu/src/main/java/org/apache/inlong/sort/kudu/sink/KuduWriter.java`
#### Snippet
```java
    }

    private DataType[] getExpectedDataTypes(TableSchema flinkSchema) {
        return flinkSchema.getFieldDataTypes();
    }
```

### Deprecation
'org.apache.flink.table.api.TableSchema' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kudu/src/main/java/org/apache/inlong/sort/kudu/sink/KuduWriter.java`
#### Snippet
```java
    }

    private String[] getExpectedFieldNames(TableSchema flinkSchema) {
        return flinkSchema.getFieldNames();
    }
```

### Deprecation
'org.apache.flink.table.descriptors.DescriptorProperties' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kudu/src/main/java/org/apache/inlong/sort/kudu/common/Kudu.java`
#### Snippet
```java
    @Override
    protected Map<String, String> toConnectorProperties() {
        DescriptorProperties descriptorProperties = new DescriptorProperties();

        descriptorProperties.putString(KuduValidator.CONNECTOR_TABLE, table);
```

### Deprecation
'org.apache.flink.table.descriptors.DescriptorProperties' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kudu/src/main/java/org/apache/inlong/sort/kudu/common/Kudu.java`
#### Snippet
```java
    @Override
    protected Map<String, String> toConnectorProperties() {
        DescriptorProperties descriptorProperties = new DescriptorProperties();

        descriptorProperties.putString(KuduValidator.CONNECTOR_TABLE, table);
```

### Deprecation
'defaultSocketReadTimeoutMs(long)' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kudu/src/main/java/org/apache/inlong/sort/kudu/sink/AbstractKuduSinkFunction.java`
#### Snippet
```java
        builder.defaultAdminOperationTimeoutMs(configuration.getLong(DEFAULT_ADMIN_OPERATION_TIMEOUT_IN_MS));
        builder.defaultOperationTimeoutMs(configuration.getLong(DEFAULT_OPERATION_TIMEOUT_IN_MS));
        builder.defaultSocketReadTimeoutMs(configuration.getLong(DEFAULT_SOCKET_READ_TIMEOUT_IN_MS));

        return builder
```

### Deprecation
'org.apache.flink.table.descriptors.DescriptorProperties' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kudu/src/main/java/org/apache/inlong/sort/kudu/common/KuduValidator.java`
#### Snippet
```java

    @Override
    public void validate(DescriptorProperties properties) {
        super.validate(properties);

```

### Deprecation
'org.apache.flink.table.api.TableSchema' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kudu/src/main/java/org/apache/inlong/sort/kudu/table/KuduDynamicTableFactory.java`
#### Snippet
```java
    @Override
    public DynamicTableSink createDynamicTableSink(Context context) {
        TableSchema schema = context.getCatalogTable().getSchema();
        FactoryUtil.TableFactoryHelper helper =
                FactoryUtil.createTableFactoryHelper(this, context);
```

### Deprecation
'fromDataTypeToLegacyInfo(org.apache.flink.table.types.DataType)' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/jdbc/src/main/java/org/apache/inlong/sort/jdbc/converter/AbstractJdbcRowConverter.java`
#### Snippet
```java
                if (type.getTypeRoot() != LogicalTypeRoot.TIMESTAMP_WITH_LOCAL_TIME_ZONE) {
                    sqlType = JdbcTypeUtil.typeInformationToSqlType(
                            TypeConversions.fromDataTypeToLegacyInfo(
                                    TypeConversions.fromLogicalToDataType(type)));
                }
```

### Deprecation
'getSchema()' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kudu/src/main/java/org/apache/inlong/sort/kudu/table/KuduDynamicTableFactory.java`
#### Snippet
```java
    @Override
    public DynamicTableSink createDynamicTableSink(Context context) {
        TableSchema schema = context.getCatalogTable().getSchema();
        FactoryUtil.TableFactoryHelper helper =
                FactoryUtil.createTableFactoryHelper(this, context);
```

### Deprecation
'org.apache.flink.table.descriptors.DescriptorProperties' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kudu/src/main/java/org/apache/inlong/sort/kudu/table/KuduDynamicTableFactory.java`
#### Snippet
```java

    private Configuration getConfiguration(
            DescriptorProperties descriptorProperties) {
        Map<String, String> properties =
                descriptorProperties.getPropertiesWithPrefix(KuduValidator.CONNECTOR_PROPERTIES);
```

### Deprecation
'org.apache.flink.table.api.TableSchema' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kudu/src/main/java/org/apache/inlong/sort/kudu/table/KuduDynamicTableFactory.java`
#### Snippet
```java

    private KuduTableInfo getKuduTableInfo(Context context) {
        TableSchema schema = context.getCatalogTable().getSchema();
        FactoryUtil.TableFactoryHelper helper =
                FactoryUtil.createTableFactoryHelper(this, context);
```

### Deprecation
'getSchema()' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kudu/src/main/java/org/apache/inlong/sort/kudu/table/KuduDynamicTableFactory.java`
#### Snippet
```java

    private KuduTableInfo getKuduTableInfo(Context context) {
        TableSchema schema = context.getCatalogTable().getSchema();
        FactoryUtil.TableFactoryHelper helper =
                FactoryUtil.createTableFactoryHelper(this, context);
```

### Deprecation
'org.apache.flink.table.descriptors.DescriptorProperties' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kudu/src/main/java/org/apache/inlong/sort/kudu/table/KuduDynamicTableFactory.java`
#### Snippet
```java
            DynamicTableSinkFactory {

    private void validateProperties(DescriptorProperties descriptorProperties) {
        new SchemaValidator(true, false, false).validate(descriptorProperties);
        new KuduValidator().validate(descriptorProperties);
```

### Deprecation
'org.apache.flink.table.api.TableSchema' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/doris/src/main/java/org/apache/inlong/sort/doris/table/DorisDynamicTableSink.java`
#### Snippet
```java
    private final DorisReadOptions readOptions;
    private final DorisExecutionOptions executionOptions;
    private final TableSchema tableSchema;
    private final boolean multipleSink;
    private final String sinkMultipleFormat;
```

### Deprecation
'org.apache.flink.table.api.TableSchema' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/doris/src/main/java/org/apache/inlong/sort/doris/table/DorisDynamicTableSink.java`
#### Snippet
```java
            DorisReadOptions readOptions,
            DorisExecutionOptions executionOptions,
            TableSchema tableSchema,
            boolean multipleSink,
            String sinkMultipleFormat,
```

### Deprecation
'getSchema()' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/doris/src/main/java/org/apache/inlong/sort/doris/table/DorisDynamicTableFactory.java`
#### Snippet
```java
        final ReadableConfig options = helper.getOptions();
        // derive the produced data type (excluding computed columns) from the catalog table
        final DataType producedDataType = context.getCatalogTable().getSchema().toPhysicalRowDataType();
        TableSchema physicalSchema = TableSchemaUtils.getPhysicalSchema(context.getCatalogTable().getSchema());
        // create and return dynamic table source
```

### Deprecation
'org.apache.flink.table.api.TableSchema' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/doris/src/main/java/org/apache/inlong/sort/doris/table/DorisDynamicTableFactory.java`
#### Snippet
```java
        // derive the produced data type (excluding computed columns) from the catalog table
        final DataType producedDataType = context.getCatalogTable().getSchema().toPhysicalRowDataType();
        TableSchema physicalSchema = TableSchemaUtils.getPhysicalSchema(context.getCatalogTable().getSchema());
        // create and return dynamic table source
        return new DorisDynamicTableSource(
```

### Deprecation
'getSchema()' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/doris/src/main/java/org/apache/inlong/sort/doris/table/DorisDynamicTableFactory.java`
#### Snippet
```java
        // derive the produced data type (excluding computed columns) from the catalog table
        final DataType producedDataType = context.getCatalogTable().getSchema().toPhysicalRowDataType();
        TableSchema physicalSchema = TableSchemaUtils.getPhysicalSchema(context.getCatalogTable().getSchema());
        // create and return dynamic table source
        return new DorisDynamicTableSource(
```

### Deprecation
'org.apache.flink.table.api.TableSchema' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/doris/src/main/java/org/apache/inlong/sort/doris/table/DorisDynamicTableFactory.java`
#### Snippet
```java

        Properties streamLoadProp = getStreamLoadProp(context.getCatalogTable().getOptions());
        TableSchema physicalSchema = TableSchemaUtils.getPhysicalSchema(context.getCatalogTable().getSchema());
        String databasePattern = helper.getOptions().getOptional(SINK_MULTIPLE_DATABASE_PATTERN).orElse(null);
        String tablePattern = helper.getOptions().getOptional(SINK_MULTIPLE_TABLE_PATTERN).orElse(null);
```

### Deprecation
'getSchema()' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/doris/src/main/java/org/apache/inlong/sort/doris/table/DorisDynamicTableFactory.java`
#### Snippet
```java

        Properties streamLoadProp = getStreamLoadProp(context.getCatalogTable().getOptions());
        TableSchema physicalSchema = TableSchemaUtils.getPhysicalSchema(context.getCatalogTable().getSchema());
        String databasePattern = helper.getOptions().getOptional(SINK_MULTIPLE_DATABASE_PATTERN).orElse(null);
        String tablePattern = helper.getOptions().getOptional(SINK_MULTIPLE_TABLE_PATTERN).orElse(null);
```

### Deprecation
'org.apache.flink.table.api.TableSchema' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/hbase/src/main/java/org/apache/inlong/sort/hbase/HBase2DynamicTableFactory.java`
#### Snippet
```java
        final ReadableConfig tableOptions = helper.getOptions();

        TableSchema tableSchema = context.getCatalogTable().getSchema();
        Map<String, String> options = context.getCatalogTable().getOptions();

```

### Deprecation
'getSchema()' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/hbase/src/main/java/org/apache/inlong/sort/hbase/HBase2DynamicTableFactory.java`
#### Snippet
```java
        final ReadableConfig tableOptions = helper.getOptions();

        TableSchema tableSchema = context.getCatalogTable().getSchema();
        Map<String, String> options = context.getCatalogTable().getOptions();

```

### Deprecation
'org.apache.flink.table.api.TableSchema' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/hbase/src/main/java/org/apache/inlong/sort/hbase/HBase2DynamicTableFactory.java`
#### Snippet
```java
        final ReadableConfig tableOptions = helper.getOptions();

        TableSchema tableSchema = context.getCatalogTable().getSchema();
        Map<String, String> options = context.getCatalogTable().getOptions();

```

### Deprecation
'getSchema()' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/hbase/src/main/java/org/apache/inlong/sort/hbase/HBase2DynamicTableFactory.java`
#### Snippet
```java
        final ReadableConfig tableOptions = helper.getOptions();

        TableSchema tableSchema = context.getCatalogTable().getSchema();
        Map<String, String> options = context.getCatalogTable().getOptions();

```

### Deprecation
'defaultSocketReadTimeoutMs(long)' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kudu/src/main/java/org/apache/inlong/sort/kudu/source/KuduLookupFunction.java`
#### Snippet
```java
        builder.defaultAdminOperationTimeoutMs(configuration.getLong(DEFAULT_ADMIN_OPERATION_TIMEOUT_IN_MS));
        builder.defaultOperationTimeoutMs(configuration.getLong(DEFAULT_OPERATION_TIMEOUT_IN_MS));
        builder.defaultSocketReadTimeoutMs(configuration.getLong(DEFAULT_SOCKET_READ_TIMEOUT_IN_MS));

        return builder
```

### Deprecation
'getSchema()' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kafka/src/main/java/org/apache/inlong/sort/kafka/table/KafkaDynamicTableFactory.java`
#### Snippet
```java

        final DataType physicalDataType =
                context.getCatalogTable().getSchema().toPhysicalRowDataType();

        validateSinkMultipleFormatAndPhysicalDataType(physicalDataType, valueEncodingFormat, sinkMultipleFormat);
```

### Deprecation
'getSchema()' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kafka/src/main/java/org/apache/inlong/sort/kafka/table/KafkaDynamicTableFactory.java`
#### Snippet
```java

        final DataType physicalDataType =
                context.getCatalogTable().getSchema().toPhysicalRowDataType();

        final int[] keyProjection = createKeyFormatProjection(tableOptions, physicalDataType);
```

### Deprecation
'getSchema()' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kafka/src/main/java/org/apache/inlong/sort/kafka/table/KafkaDynamicTableFactory.java`
#### Snippet
```java
    private static void validatePKConstraints(
            ObjectIdentifier tableName, CatalogTable catalogTable, Format format) {
        if (catalogTable.getSchema().getPrimaryKey().isPresent()
                && format.getChangelogMode().containsOnly(RowKind.INSERT)) {
            Configuration options = Configuration.fromMap(catalogTable.getOptions());
```

### Deprecation
'org.apache.flink.table.api.TableSchema' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kafka/src/main/java/org/apache/inlong/sort/kafka/table/UpsertKafkaDynamicTableFactory.java`
#### Snippet
```java
        // Validate the option data type.
        helper.validateExcept(KafkaOptions.PROPERTIES_PREFIX, DIRTY_PREFIX);
        TableSchema schema = context.getCatalogTable().getSchema();
        validateSink(tableOptions, keyEncodingFormat, valueEncodingFormat, schema);

```

### Deprecation
'getSchema()' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kafka/src/main/java/org/apache/inlong/sort/kafka/table/UpsertKafkaDynamicTableFactory.java`
#### Snippet
```java
        // Validate the option data type.
        helper.validateExcept(KafkaOptions.PROPERTIES_PREFIX, DIRTY_PREFIX);
        TableSchema schema = context.getCatalogTable().getSchema();
        validateSink(tableOptions, keyEncodingFormat, valueEncodingFormat, schema);

```

### Deprecation
'org.apache.flink.table.api.TableSchema' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kafka/src/main/java/org/apache/inlong/sort/kafka/table/UpsertKafkaDynamicTableFactory.java`
#### Snippet
```java

    private Tuple2<int[], int[]> createKeyValueProjections(CatalogTable catalogTable) {
        TableSchema schema = catalogTable.getSchema();
        // primary key should validated earlier
        List<String> keyFields = schema.getPrimaryKey().get().getColumns();
```

### Deprecation
'getSchema()' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kafka/src/main/java/org/apache/inlong/sort/kafka/table/UpsertKafkaDynamicTableFactory.java`
#### Snippet
```java

    private Tuple2<int[], int[]> createKeyValueProjections(CatalogTable catalogTable) {
        TableSchema schema = catalogTable.getSchema();
        // primary key should validated earlier
        List<String> keyFields = schema.getPrimaryKey().get().getColumns();
```

### Deprecation
'org.apache.flink.table.api.TableSchema' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kafka/src/main/java/org/apache/inlong/sort/kafka/table/UpsertKafkaDynamicTableFactory.java`
#### Snippet
```java

    private static void validateSource(
            ReadableConfig tableOptions, Format keyFormat, Format valueFormat, TableSchema schema) {
        validateTopic(tableOptions);
        validateFormat(keyFormat, valueFormat, tableOptions);
```

### Deprecation
'org.apache.flink.table.api.TableSchema' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kafka/src/main/java/org/apache/inlong/sort/kafka/table/UpsertKafkaDynamicTableFactory.java`
#### Snippet
```java
        // Validate the option data type.
        helper.validateExcept(KafkaOptions.PROPERTIES_PREFIX, DIRTY_PREFIX);
        TableSchema schema = context.getCatalogTable().getSchema();
        validateSource(tableOptions, keyDecodingFormat, valueDecodingFormat, schema);

```

### Deprecation
'getSchema()' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kafka/src/main/java/org/apache/inlong/sort/kafka/table/UpsertKafkaDynamicTableFactory.java`
#### Snippet
```java
        // Validate the option data type.
        helper.validateExcept(KafkaOptions.PROPERTIES_PREFIX, DIRTY_PREFIX);
        TableSchema schema = context.getCatalogTable().getSchema();
        validateSource(tableOptions, keyDecodingFormat, valueDecodingFormat, schema);

```

### Deprecation
'org.apache.flink.table.api.TableSchema' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kafka/src/main/java/org/apache/inlong/sort/kafka/table/UpsertKafkaDynamicTableFactory.java`
#### Snippet
```java
    }

    private static void validatePKConstraints(TableSchema schema) {
        if (!schema.getPrimaryKey().isPresent()) {
            throw new ValidationException(
```

### Deprecation
'org.apache.flink.table.api.TableSchema' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kafka/src/main/java/org/apache/inlong/sort/kafka/table/UpsertKafkaDynamicTableFactory.java`
#### Snippet
```java

    private static void validateSink(
            ReadableConfig tableOptions, Format keyFormat, Format valueFormat, TableSchema schema) {
        validateTopic(tableOptions);
        validateFormat(keyFormat, valueFormat, tableOptions);
```

### Deprecation
'org.apache.flink.table.api.TableSchema' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/redis/src/main/java/org/apache/inlong/sort/redis/common/schema/impl/AbstractRedisSchema.java`
#### Snippet
```java

    public AbstractRedisSchema(ResolvedSchema resolvedSchema) {
        TableSchema physicalSchema = TableSchemaUtils.getPhysicalSchema(
                TableSchema.fromResolvedSchema(resolvedSchema));
        int valueIndex = getValueIndex();
```

### Deprecation
'org.apache.flink.table.api.TableSchema' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/redis/src/main/java/org/apache/inlong/sort/redis/common/schema/impl/AbstractRedisSchema.java`
#### Snippet
```java
    public AbstractRedisSchema(ResolvedSchema resolvedSchema) {
        TableSchema physicalSchema = TableSchemaUtils.getPhysicalSchema(
                TableSchema.fromResolvedSchema(resolvedSchema));
        int valueIndex = getValueIndex();
        int[] projection = IntStream.range(valueIndex, physicalSchema.getFieldCount()).toArray();
```

### Deprecation
'org.apache.flink.streaming.util.serialization.KeyedSerializationSchema' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kafka/src/main/java/org/apache/inlong/sort/kafka/FlinkKafkaProducer.java`
#### Snippet
```java
     *
     * <p>If a partitioner is not provided, written records will be partitioned by the attached key
     * of each record (as determined by {@link KeyedSerializationSchema#serializeKey(Object)}). If
     * written records do not have a key (i.e., {@link
     * KeyedSerializationSchema#serializeKey(Object)} returns {@code null}), they will be
```

### Deprecation
'org.apache.flink.streaming.util.serialization.KeyedSerializationSchema' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kafka/src/main/java/org/apache/inlong/sort/kafka/FlinkKafkaProducer.java`
#### Snippet
```java
     * of each record (as determined by {@link KeyedSerializationSchema#serializeKey(Object)}). If
     * written records do not have a key (i.e., {@link
     * KeyedSerializationSchema#serializeKey(Object)} returns {@code null}), they will be
     * distributed to Kafka partitions in a round-robin fashion.
     *
```

### Deprecation
'org.apache.flink.streaming.util.serialization.KeyedSerializationSchema' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kafka/src/main/java/org/apache/inlong/sort/kafka/FlinkKafkaProducer.java`
#### Snippet
```java
     * @param customPartitioner A serializable partitioner for assigning messages to Kafka
     *         partitions. If a partitioner is not provided, records will be partitioned by the key of
     *         each record (determined by {@link KeyedSerializationSchema#serializeKey(Object)}). If the
     *         keys are {@code null}, then records will be distributed to Kafka partitions in a
     *         round-robin fashion.
```

### Deprecation
'org.apache.flink.streaming.util.serialization.KeyedSerializationSchema' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kafka/src/main/java/org/apache/inlong/sort/kafka/FlinkKafkaProducer.java`
#### Snippet
```java
    private FlinkKafkaProducer(
            String defaultTopic,
            KeyedSerializationSchema<IN> keyedSchema,
            FlinkKafkaPartitioner<IN> customPartitioner,
            KafkaSerializationSchema<IN> kafkaSchema,
```

### Deprecation
'org.apache.flink.streaming.util.serialization.KeyedSerializationSchema' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kafka/src/main/java/org/apache/inlong/sort/kafka/FlinkKafkaProducer.java`
#### Snippet
```java
     */
    @Nullable
    private final KeyedSerializationSchema<IN> keyedSchema;
    /**
     * (Serializable) serialization schema for serializing records to {@link ProducerRecord
```

### Deprecation
'org.apache.flink.table.api.TableSchema' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/pulsar/src/main/java/org/apache/inlong/sort/pulsar/table/UpsertPulsarDynamicTableFactory.java`
#### Snippet
```java
        // Validate the option data type.
        helper.validateExcept(PulsarTableOptions.PROPERTIES_PREFIX);
        TableSchema schema = context.getCatalogTable().getSchema();
        validateTableOptions(tableOptions, keyEncodingFormat, valueEncodingFormat, schema);

```

### Deprecation
'getSchema()' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/pulsar/src/main/java/org/apache/inlong/sort/pulsar/table/UpsertPulsarDynamicTableFactory.java`
#### Snippet
```java
        // Validate the option data type.
        helper.validateExcept(PulsarTableOptions.PROPERTIES_PREFIX);
        TableSchema schema = context.getCatalogTable().getSchema();
        validateTableOptions(tableOptions, keyEncodingFormat, valueEncodingFormat, schema);

```

### Deprecation
'org.apache.flink.table.api.TableSchema' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/pulsar/src/main/java/org/apache/inlong/sort/pulsar/table/UpsertPulsarDynamicTableFactory.java`
#### Snippet
```java

    private Tuple2<int[], int[]> createKeyValueProjections(CatalogTable catalogTable) {
        TableSchema schema = catalogTable.getSchema();
        // primary key should validated earlier
        List<String> keyFields = schema.getPrimaryKey().get().getColumns();
```

### Deprecation
'getSchema()' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/pulsar/src/main/java/org/apache/inlong/sort/pulsar/table/UpsertPulsarDynamicTableFactory.java`
#### Snippet
```java

    private Tuple2<int[], int[]> createKeyValueProjections(CatalogTable catalogTable) {
        TableSchema schema = catalogTable.getSchema();
        // primary key should validated earlier
        List<String> keyFields = schema.getPrimaryKey().get().getColumns();
```

### Deprecation
'org.apache.flink.table.api.TableSchema' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/pulsar/src/main/java/org/apache/inlong/sort/pulsar/table/UpsertPulsarDynamicTableFactory.java`
#### Snippet
```java
                .orElse(tableOptions.get(VALUE_FORMAT));
        helper.validateExcept(PulsarTableOptions.PROPERTIES_PREFIX, valueFormatPrefix);
        TableSchema schema = context.getCatalogTable().getSchema();
        validateTableOptions(tableOptions, keyDecodingFormat, valueDecodingFormat, schema);

```

### Deprecation
'getSchema()' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/pulsar/src/main/java/org/apache/inlong/sort/pulsar/table/UpsertPulsarDynamicTableFactory.java`
#### Snippet
```java
                .orElse(tableOptions.get(VALUE_FORMAT));
        helper.validateExcept(PulsarTableOptions.PROPERTIES_PREFIX, valueFormatPrefix);
        TableSchema schema = context.getCatalogTable().getSchema();
        validateTableOptions(tableOptions, keyDecodingFormat, valueDecodingFormat, schema);

```

### Deprecation
'org.apache.flink.table.api.TableSchema' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/pulsar/src/main/java/org/apache/inlong/sort/pulsar/table/UpsertPulsarDynamicTableFactory.java`
#### Snippet
```java
            Format keyFormat,
            Format valueFormat,
            TableSchema schema) {
        validateTopic(tableOptions);
        validateFormat(keyFormat, valueFormat, tableOptions);
```

### Deprecation
'org.apache.flink.table.api.TableSchema' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/pulsar/src/main/java/org/apache/inlong/sort/pulsar/table/UpsertPulsarDynamicTableFactory.java`
#### Snippet
```java
    }

    private static void validatePKConstraints(TableSchema schema) {
        if (!schema.getPrimaryKey().isPresent()) {
            throw new ValidationException("'upsert-pulsar' tables require to define a PRIMARY KEY constraint. "
```

### Deprecation
'getSchema()' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/pulsar/src/main/java/org/apache/inlong/sort/pulsar/table/PulsarDynamicTableFactory.java`
#### Snippet
```java
        validatePKConstraints(context.getObjectIdentifier(), context.getCatalogTable(), valueEncodingFormat);

        final DataType physicalDataType = context.getCatalogTable().getSchema().toPhysicalRowDataType();

        final int[] keyProjection = createKeyFormatProjection(tableOptions, physicalDataType);
```

### Deprecation
'getSchema()' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/pulsar/src/main/java/org/apache/inlong/sort/pulsar/table/PulsarDynamicTableFactory.java`
#### Snippet
```java

    private static void validatePKConstraints(ObjectIdentifier tableName, CatalogTable catalogTable, Format format) {
        if (catalogTable.getSchema().getPrimaryKey().isPresent()
                && format.getChangelogMode().containsOnly(RowKind.INSERT)) {
            Configuration options = Configuration.fromMap(catalogTable.getOptions());
```

### Deprecation
'getSchema()' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/pulsar/src/main/java/org/apache/inlong/sort/pulsar/table/PulsarDynamicTableFactory.java`
#### Snippet
```java
                .getStartupOptions(tableOptions);

        final DataType physicalDataType = context.getCatalogTable().getSchema().toPhysicalRowDataType();

        final int[] keyProjection = createKeyFormatProjection(tableOptions, physicalDataType);
```

### Deprecation
'getSchema()' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/tubemq/src/main/java/org/apache/inlong/sort/tubemq/table/TubeMQDynamicTableFactory.java`
#### Snippet
```java
    private static void validatePKConstraints(
            ObjectIdentifier tableName, CatalogTable catalogTable, Format format) {
        if (catalogTable.getSchema().getPrimaryKey().isPresent()
                && format.getChangelogMode().containsOnly(RowKind.INSERT)) {
            Configuration options = Configuration.fromMap(catalogTable.getOptions());
```

### Deprecation
'getSchema()' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/tubemq/src/main/java/org/apache/inlong/sort/tubemq/table/TubeMQDynamicTableFactory.java`
#### Snippet
```java
        final Configuration properties = getTubeMQProperties(context.getCatalogTable().getOptions());

        final DataType physicalDataType = context.getCatalogTable().getSchema().toPhysicalRowDataType();

        return createTubeMQTableSource(
```

### Deprecation
'noDefaultValue()' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/pulsar/src/main/java/org/apache/inlong/sort/pulsar/internal/PulsarMetadataReader.java`
#### Snippet
```java
    public static final ConfigOption<String> AUTHENTICATION_TOKEN =
            ConfigOptions.key("authentication-token")
                    .noDefaultValue()
                    .withDescription("Authentication token for connection.");

```

### Deprecation
Overrides deprecated method in 'org.apache.flink.table.factories.CatalogFactory'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/iceberg/src/main/java/org/apache/inlong/sort/iceberg/FlinkCatalogFactory.java`
#### Snippet
```java

    @Override
    public Catalog createCatalog(String name, Map<String, String> properties) {
        return createCatalog(name, properties, clusterHadoopConf());
    }
```

### Deprecation
Overrides deprecated method in 'org.apache.flink.table.factories.CatalogFactory'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/iceberg/src/main/java/org/apache/inlong/sort/iceberg/FlinkCatalogFactory.java`
#### Snippet
```java

    @Override
    public List<String> supportedProperties() {
        return ImmutableList.of("*");
    }
```

### Deprecation
Overrides deprecated method in 'org.apache.flink.table.factories.CatalogFactory'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/iceberg/src/main/java/org/apache/inlong/sort/iceberg/FlinkCatalogFactory.java`
#### Snippet
```java

    @Override
    public Map<String, String> requiredContext() {
        Map<String, String> context = Maps.newHashMap();
        context.put(TYPE, "iceberg");
```

### Deprecation
'org.apache.flink.table.api.TableSchema' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/iceberg/src/main/java/org/apache/inlong/sort/iceberg/IcebergTableSink.java`
#### Snippet
```java

    private final TableLoader tableLoader;
    private final TableSchema tableSchema;

    private final CatalogTable catalogTable;
```

### Deprecation
'org.apache.flink.table.api.constraints.UniqueConstraint' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/iceberg/src/main/java/org/apache/inlong/sort/iceberg/IcebergTableSink.java`
#### Snippet
```java

        List<String> equalityColumns = tableSchema.getPrimaryKey()
                .map(UniqueConstraint::getColumns)
                .orElseGet(ImmutableList::of);

```

### Deprecation
'org.apache.flink.table.api.TableSchema' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/iceberg/src/main/java/org/apache/inlong/sort/iceberg/IcebergTableSink.java`
#### Snippet
```java

    public IcebergTableSink(TableLoader tableLoader,
            TableSchema tableSchema,
            CatalogTable catalogTable,
            CatalogLoader catalogLoader,
```

### Deprecation
'org.apache.flink.table.api.TableSchema' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/iceberg/src/main/java/org/apache/inlong/sort/iceberg/FlinkDynamicTableFactory.java`
#### Snippet
```java
        CatalogTable catalogTable = loadCatalogTable(context);
        Map<String, String> tableProps = catalogTable.getOptions();
        TableSchema tableSchema = TableSchemaUtils.getPhysicalSchema(catalogTable.getSchema());

        TableLoader tableLoader;
```

### Deprecation
'getSchema()' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/iceberg/src/main/java/org/apache/inlong/sort/iceberg/FlinkDynamicTableFactory.java`
#### Snippet
```java
        CatalogTable catalogTable = loadCatalogTable(context);
        Map<String, String> tableProps = catalogTable.getOptions();
        TableSchema tableSchema = TableSchemaUtils.getPhysicalSchema(catalogTable.getSchema());

        TableLoader tableLoader;
```

### Deprecation
'org.apache.flink.table.api.TableSchema' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/iceberg/src/main/java/org/apache/inlong/sort/iceberg/FlinkDynamicTableFactory.java`
#### Snippet
```java
        CatalogTable catalogTable = loadCatalogTable(context);
        Map<String, String> tableProps = catalogTable.getOptions();
        TableSchema tableSchema = TableSchemaUtils.getPhysicalSchema(catalogTable.getSchema());
        ActionsProvider actionsLoader = createActionLoader(context.getClassLoader(), tableProps);
        // Build the dirty data side-output
```

### Deprecation
'getSchema()' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/iceberg/src/main/java/org/apache/inlong/sort/iceberg/FlinkDynamicTableFactory.java`
#### Snippet
```java
        CatalogTable catalogTable = loadCatalogTable(context);
        Map<String, String> tableProps = catalogTable.getOptions();
        TableSchema tableSchema = TableSchemaUtils.getPhysicalSchema(catalogTable.getSchema());
        ActionsProvider actionsLoader = createActionLoader(context.getClassLoader(), tableProps);
        // Build the dirty data side-output
```

### Deprecation
'org.apache.flink.streaming.api.TimeCharacteristic' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/iceberg/src/main/java/org/apache/inlong/sort/iceberg/sink/multiple/IcebergProcessFunction.java`
#### Snippet
```java
         *
         * <p>This might be {@code null}, for example if the time characteristic of your program is
         * set to {@link org.apache.flink.streaming.api.TimeCharacteristic#ProcessingTime}.
         */
        public abstract Long timestamp();
```

### Deprecation
'org.apache.flink.runtime.state.CheckpointListener' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/pulsar/src/main/java/org/apache/inlong/sort/pulsar/internal/FlinkPulsarSource.java`
#### Snippet
```java
        implements
            ResultTypeQueryable<T>,
            CheckpointListener,
            CheckpointedFunction {

```

### Deprecation
'org.apache.flink.table.api.TableSchema' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/iceberg/src/main/java/org/apache/inlong/sort/iceberg/FlinkCatalog.java`
#### Snippet
```java
        Preconditions.checkArgument(table instanceof CatalogTable, "The Table should be a CatalogTable.");

        TableSchema schema = table.getSchema();
        schema.getTableColumns().forEach(column -> {
            if (!FlinkCompatibilityUtil.isPhysicalColumn(column)) {
```

### Deprecation
'getSchema()' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/iceberg/src/main/java/org/apache/inlong/sort/iceberg/FlinkCatalog.java`
#### Snippet
```java
        Preconditions.checkArgument(table instanceof CatalogTable, "The Table should be a CatalogTable.");

        TableSchema schema = table.getSchema();
        schema.getTableColumns().forEach(column -> {
            if (!FlinkCompatibilityUtil.isPhysicalColumn(column)) {
```

### Deprecation
'getSchema()' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/iceberg/src/main/java/org/apache/inlong/sort/iceberg/FlinkCatalog.java`
#### Snippet
```java
        validateFlinkTable(table);

        Schema icebergSchema = FlinkSchemaUtil.convert(table.getSchema());
        PartitionSpec spec = toPartitionSpec(((CatalogTable) table).getPartitionKeys(), icebergSchema);

```

### Deprecation
'org.apache.flink.table.api.TableSchema' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/iceberg/src/main/java/org/apache/inlong/sort/iceberg/FlinkCatalog.java`
#### Snippet
```java

    static CatalogTable toCatalogTable(Table table) {
        TableSchema schema = FlinkSchemaUtil.toSchema(table.schema());
        List<String> partitionKeys = toPartitionKeys(table.spec(), table.schema());

```

### Deprecation
'org.apache.flink.table.catalog.CatalogTableImpl' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/iceberg/src/main/java/org/apache/inlong/sort/iceberg/FlinkCatalog.java`
#### Snippet
```java
        // Let's re-loading table from Iceberg catalog when creating source/sink operators.
        // Iceberg does not have Table comment, so pass a null (Default comment value in Flink).
        return new CatalogTableImpl(schema, partitionKeys, table.properties(), null);
    }

```

### Deprecation
'getSchema()' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/iceberg/src/main/java/org/apache/inlong/sort/iceberg/FlinkCatalog.java`
#### Snippet
```java
        // For current Flink Catalog API, support for adding/removing/renaming columns cannot be done by comparing
        // CatalogTable instances, unless the Flink schema contains Iceberg column IDs.
        if (!table.getSchema().equals(newTable.getSchema())) {
            throw new UnsupportedOperationException("Altering schema is not supported yet.");
        }
```

### Deprecation
'getSchema()' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/iceberg/src/main/java/org/apache/inlong/sort/iceberg/FlinkCatalog.java`
#### Snippet
```java
        // For current Flink Catalog API, support for adding/removing/renaming columns cannot be done by comparing
        // CatalogTable instances, unless the Flink schema contains Iceberg column IDs.
        if (!table.getSchema().equals(newTable.getSchema())) {
            throw new UnsupportedOperationException("Altering schema is not supported yet.");
        }
```

### Deprecation
'org.apache.flink.table.api.TableSchema' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/iceberg/src/main/java/org/apache/inlong/sort/iceberg/sink/FlinkSink.java`
#### Snippet
```java
        }

        public Builder tableSchema(TableSchema newTableSchema) {
            this.tableSchema = newTableSchema;
            return this;
```

### Deprecation
'org.apache.flink.table.api.TableSchema' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/iceberg/src/main/java/org/apache/inlong/sort/iceberg/sink/FlinkSink.java`
#### Snippet
```java
        private TableLoader tableLoader;
        private Table table;
        private TableSchema tableSchema;
        private ActionsProvider actionProvider;
        private boolean overwrite = false;
```

### Deprecation
'org.apache.flink.table.api.TableSchema' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/iceberg/src/main/java/org/apache/inlong/sort/iceberg/sink/FlinkSink.java`
#### Snippet
```java
    }

    static RowType toFlinkRowType(Schema schema, TableSchema requestedSchema) {
        if (requestedSchema != null) {
            // Convert the flink schema to iceberg schema firstly, then reassign ids to match the existing iceberg
```

### Deprecation
'org.apache.flink.table.api.TableSchema' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/iceberg/src/main/java/org/apache/inlong/sort/iceberg/sink/FlinkSink.java`
#### Snippet
```java
    /**
     * Initialize a {@link Builder} to export the data from input data stream with {@link Row}s into iceberg table.
     * We use {@link RowData} inside the sink connector, so users need to provide a {@link TableSchema} for builder to
     * convert those {@link Row}s to a {@link RowData} DataStream.
     *
```

### Deprecation
'org.apache.flink.table.api.TableSchema' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/iceberg/src/main/java/org/apache/inlong/sort/iceberg/sink/FlinkSink.java`
#### Snippet
```java
     * @return {@link Builder} to connect the iceberg table.
     */
    public static Builder forRow(DataStream<Row> input, TableSchema tableSchema) {
        RowType rowType = (RowType) tableSchema.toRowDataType().getLogicalType();
        DataType[] fieldDataTypes = tableSchema.getFieldDataTypes();
```

### Deprecation
'org.apache.inlong.sort.cdc.mysql.MySqlSource' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/mysql/table/MySqlTableSource.java`
#### Snippet
```java
            return SourceProvider.of(parallelSource);
        } else {
            org.apache.inlong.sort.cdc.mysql.MySqlSource.Builder<RowData> builder =
                    org.apache.inlong.sort.cdc.mysql.MySqlSource.<RowData>builder()
                            .hostname(hostname)
```

### Deprecation
'org.apache.inlong.sort.cdc.mysql.MySqlSource.Builder' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/mysql/table/MySqlTableSource.java`
#### Snippet
```java
            return SourceProvider.of(parallelSource);
        } else {
            org.apache.inlong.sort.cdc.mysql.MySqlSource.Builder<RowData> builder =
                    org.apache.inlong.sort.cdc.mysql.MySqlSource.<RowData>builder()
                            .hostname(hostname)
```

### Deprecation
'org.apache.inlong.sort.cdc.mysql.MySqlSource' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/mysql/table/MySqlTableSource.java`
#### Snippet
```java
        } else {
            org.apache.inlong.sort.cdc.mysql.MySqlSource.Builder<RowData> builder =
                    org.apache.inlong.sort.cdc.mysql.MySqlSource.<RowData>builder()
                            .hostname(hostname)
                            .port(port)
```

### Deprecation
'getConfig()' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/mysql/debezium/DebeziumUtils.java`
#### Snippet
```java
        String bigIntUnsignedHandlingModeStr =
                dbzMySqlConfig
                        .getConfig()
                        .getString(MySqlConnectorConfig.BIGINT_UNSIGNED_HANDLING_MODE);
        MySqlConnectorConfig.BigIntUnsignedHandlingMode bigIntUnsignedHandlingMode =
```

### Deprecation
'getConfig()' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/mysql/debezium/DebeziumUtils.java`
#### Snippet
```java

        boolean timeAdjusterEnabled =
                dbzMySqlConfig.getConfig().getBoolean(MySqlConnectorConfig.ENABLE_TIME_ADJUSTER);
        return new MySqlValueConverters(
                decimalMode,
```

### Deprecation
'org.apache.flink.table.api.TableSchema' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/starrocks/src/main/java/org/apache/inlong/sort/starrocks/table/sink/StarRocksDynamicTableSink.java`
#### Snippet
```java
public class StarRocksDynamicTableSink implements DynamicTableSink {

    private transient TableSchema flinkSchema;
    private StarRocksSinkOptions sinkOptions;
    private final boolean multipleSink;
```

### Deprecation
'org.apache.flink.table.api.TableSchema' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/starrocks/src/main/java/org/apache/inlong/sort/starrocks/table/sink/StarRocksDynamicTableSink.java`
#### Snippet
```java

    public StarRocksDynamicTableSink(StarRocksSinkOptions sinkOptions,
            TableSchema schema,
            boolean multipleSink,
            String sinkMultipleFormat,
```

### Deprecation
'org.apache.flink.table.api.TableSchema' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/starrocks/src/main/java/org/apache/inlong/sort/starrocks/table/sink/StarRocksDynamicTableSinkFactory.java`
#### Snippet
```java
        StarRocksSinkOptions sinkOptions = new StarRocksSinkOptions(options, context.getCatalogTable().getOptions());
        sinkOptions.enableUpsertDelete();
        TableSchema physicalSchema = TableSchemaUtils.getPhysicalSchema(context.getCatalogTable().getSchema());
        boolean multipleSink = helper.getOptions().get(SINK_MULTIPLE_ENABLE);
        String sinkMultipleFormat = helper.getOptions().getOptional(SINK_MULTIPLE_FORMAT).orElse(null);
```

### Deprecation
'getSchema()' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/starrocks/src/main/java/org/apache/inlong/sort/starrocks/table/sink/StarRocksDynamicTableSinkFactory.java`
#### Snippet
```java
        StarRocksSinkOptions sinkOptions = new StarRocksSinkOptions(options, context.getCatalogTable().getOptions());
        sinkOptions.enableUpsertDelete();
        TableSchema physicalSchema = TableSchemaUtils.getPhysicalSchema(context.getCatalogTable().getSchema());
        boolean multipleSink = helper.getOptions().get(SINK_MULTIPLE_ENABLE);
        String sinkMultipleFormat = helper.getOptions().getOptional(SINK_MULTIPLE_FORMAT).orElse(null);
```

### Deprecation
'org.apache.flink.table.api.TableSchema' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/filesystem/src/main/java/org/apache/inlong/sort/filesystem/AbstractFileSystemTable.java`
#### Snippet
```java

    DataType getFormatDataType() {
        TableSchema.Builder builder = TableSchema.builder();
        schema.getTableColumns()
                .forEach(
```

### Deprecation
'org.apache.flink.table.api.TableSchema' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/filesystem/src/main/java/org/apache/inlong/sort/filesystem/AbstractFileSystemTable.java`
#### Snippet
```java

    DataType getFormatDataType() {
        TableSchema.Builder builder = TableSchema.builder();
        schema.getTableColumns()
                .forEach(
```

### Deprecation
'org.apache.flink.table.api.TableSchema' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/filesystem/src/main/java/org/apache/inlong/sort/filesystem/AbstractFileSystemTable.java`
#### Snippet
```java
    final ObjectIdentifier tableIdentifier;
    final Configuration tableOptions;
    final TableSchema schema;
    final List<String> partitionKeys;
    final Path path;
```

### Deprecation
'getSchema()' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/filesystem/src/main/java/org/apache/inlong/sort/filesystem/AbstractFileSystemTable.java`
#### Snippet
```java
        this.tableOptions = new Configuration();
        context.getCatalogTable().getOptions().forEach(tableOptions::setString);
        this.schema = TableSchemaUtils.getPhysicalSchema(context.getCatalogTable().getSchema());
        this.partitionKeys = context.getCatalogTable().getPartitionKeys();
        this.path = new Path(tableOptions.get(PATH));
```

### Deprecation
'org.apache.flink.table.factories.FileSystemFormatFactory' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/filesystem/src/main/java/org/apache/inlong/sort/filesystem/FileSystemTableFactory.java`
#### Snippet
```java
    }

    private FileSystemFormatFactory discoverFormatFactory(Context context) {
        if (formatFactoryExists(context, FileSystemFormatFactory.class)) {
            Configuration options = Configuration.fromMap(context.getCatalogTable().getOptions());
```

### Deprecation
'org.apache.flink.table.factories.FileSystemFormatFactory' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/filesystem/src/main/java/org/apache/inlong/sort/filesystem/FileSystemTableFactory.java`
#### Snippet
```java

    private FileSystemFormatFactory discoverFormatFactory(Context context) {
        if (formatFactoryExists(context, FileSystemFormatFactory.class)) {
            Configuration options = Configuration.fromMap(context.getCatalogTable().getOptions());
            String identifier = options.get(FactoryUtil.FORMAT);
```

### Deprecation
'org.apache.flink.table.factories.FileSystemFormatFactory' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/filesystem/src/main/java/org/apache/inlong/sort/filesystem/FileSystemTableFactory.java`
#### Snippet
```java
            return FactoryUtil.discoverFactory(
                    Thread.currentThread().getContextClassLoader(),
                    FileSystemFormatFactory.class,
                    identifier);
        } else {
```

### Deprecation
'org.apache.flink.table.api.TableSchema' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/starrocks/src/main/java/org/apache/inlong/sort/starrocks/table/sink/StarRocksDynamicSinkFunction.java`
#### Snippet
```java

    public StarRocksDynamicSinkFunction(StarRocksSinkOptions sinkOptions,
            TableSchema schema,
            StarRocksIRowTransformer<T> rowTransformer,
            boolean multipleSink,
```

### Deprecation
'org.apache.flink.table.api.TableSchema' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/filesystem/src/main/java/org/apache/inlong/sort/filesystem/FileSystemTableSink.java`
#### Snippet
```java

            @Override
            public TableSchema getSchema() {
                return schema;
            }
```

### Deprecation
'org.apache.flink.table.factories.FileSystemFormatFactory' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/filesystem/src/main/java/org/apache/inlong/sort/filesystem/FileSystemTableSink.java`
#### Snippet
```java
    private final DecodingFormat<DeserializationSchema<RowData>> deserializationFormat;
    @Nullable
    private final FileSystemFormatFactory formatFactory;

    // For Writing
```

### Deprecation
'org.apache.flink.table.factories.FileSystemFormatFactory' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/filesystem/src/main/java/org/apache/inlong/sort/filesystem/FileSystemTableSink.java`
#### Snippet
```java
    }

    private FileSystemFormatFactory.ReaderContext createReaderContext() {
        return new FileSystemFormatFactory.ReaderContext() {

```

### Deprecation
'org.apache.flink.table.factories.FileSystemFormatFactory' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/filesystem/src/main/java/org/apache/inlong/sort/filesystem/FileSystemTableSink.java`
#### Snippet
```java
            @Nullable DecodingFormat<BulkFormat<RowData, FileSourceSplit>> bulkReaderFormat,
            @Nullable DecodingFormat<DeserializationSchema<RowData>> deserializationFormat,
            @Nullable FileSystemFormatFactory formatFactory,
            @Nullable EncodingFormat<BulkWriter.Factory<RowData>> bulkWriterFormat,
            @Nullable EncodingFormat<SerializationSchema<RowData>> serializationFormat,
```

### Deprecation
'org.apache.flink.table.factories.FileSystemFormatFactory' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/filesystem/src/main/java/org/apache/inlong/sort/filesystem/FileSystemTableSink.java`
#### Snippet
```java

    private FileSystemFormatFactory.ReaderContext createReaderContext() {
        return new FileSystemFormatFactory.ReaderContext() {

            @Override
```

### Deprecation
'writeUsingOutputFormat(org.apache.flink.api.common.io.OutputFormat)' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/filesystem/src/main/java/org/apache/inlong/sort/filesystem/FileSystemTableSink.java`
#### Snippet
```java
                        .build());
        return inputStream
                .writeUsingOutputFormat(builder.build())
                .setParallelism(parallelism)
                .name("Filesystem");
```

### Deprecation
'org.apache.flink.table.api.TableSchema' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/starrocks/src/main/java/org/apache/inlong/sort/starrocks/manager/StarRocksSinkManager.java`
#### Snippet
```java

    public StarRocksSinkManager(StarRocksSinkOptions sinkOptions,
            TableSchema flinkSchema,
            StarRocksJdbcConnectionProvider jdbcConnProvider,
            StarRocksQueryVisitor starrocksQueryVisitor,
```

### Deprecation
'org.apache.flink.table.api.TableSchema' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/starrocks/src/main/java/org/apache/inlong/sort/starrocks/manager/StarRocksSinkManager.java`
#### Snippet
```java
    }

    protected void init(TableSchema schema) {
        if (!multipleSink) {
            validateTableStructure(schema);
```

### Deprecation
'org.apache.flink.table.api.TableSchema' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/starrocks/src/main/java/org/apache/inlong/sort/starrocks/manager/StarRocksSinkManager.java`
#### Snippet
```java
    }

    private void validateTableStructure(TableSchema flinkSchema) {
        if (null == flinkSchema) {
            return;
```

### Deprecation
'org.apache.flink.table.api.constraints.UniqueConstraint' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/starrocks/src/main/java/org/apache/inlong/sort/starrocks/manager/StarRocksSinkManager.java`
#### Snippet
```java
            return;
        }
        Optional<UniqueConstraint> constraint = flinkSchema.getPrimaryKey();
        List<Map<String, Object>> rows = starrocksQueryVisitor.getTableColumnsMetaData();
        if (rows == null || rows.isEmpty()) {
```

### Deprecation
'org.apache.flink.table.api.TableColumn' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/starrocks/src/main/java/org/apache/inlong/sort/starrocks/manager/StarRocksSinkManager.java`
#### Snippet
```java
                                    .collect(Collectors.joining(",")));
        }
        List<TableColumn> flinkCols = flinkSchema.getTableColumns();
        for (int i = 0; i < rows.size(); i++) {
            String starrocksField = rows.get(i).get("COLUMN_NAME").toString().toLowerCase();
```

### Deprecation
'org.apache.flink.table.api.TableColumn' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/starrocks/src/main/java/org/apache/inlong/sort/starrocks/manager/StarRocksSinkManager.java`
#### Snippet
```java
            String starrocksField = rows.get(i).get("COLUMN_NAME").toString().toLowerCase();
            String starrocksType = rows.get(i).get("DATA_TYPE").toString().toLowerCase();
            List<TableColumn> matchedFlinkCols = flinkCols.stream()
                    .filter(col -> col.getName().toLowerCase().equals(starrocksField)
                            && (!typesMap.containsKey(starrocksType) || typesMap.get(starrocksType)
```

### Deprecation
'org.apache.flink.table.api.TableSchema' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/starrocks/src/main/java/org/apache/inlong/sort/starrocks/manager/StarRocksSinkManager.java`
#### Snippet
```java

    public StarRocksSinkManager(StarRocksSinkOptions sinkOptions,
            TableSchema flinkSchema,
            boolean multipleSink,
            SchemaUpdateExceptionPolicy schemaUpdatePolicy,
```

### Deprecation
'STORE_ONLY_MONITORED_TABLES_DDL' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/oracle-cdc/src/main/java/org/apache/inlong/sort/cdc/oracle/debezium/internal/FlinkDatabaseSchemaHistory.java`
#### Snippet
```java
        this.instanceName = config.getString(DATABASE_HISTORY_INSTANCE_NAME);
        this.listener = listener;
        this.storeOnlyMonitoredTablesDdl = config.getBoolean(STORE_ONLY_MONITORED_TABLES_DDL);
        this.skipUnparseableDDL = config.getBoolean(SKIP_UNPARSEABLE_DDL_STATEMENTS);
        this.useCatalogBeforeSchema = useCatalogBeforeSchema;
```

### Deprecation
'STORE_ONLY_MONITORED_TABLES_DDL' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mongodb-cdc/src/main/java/org/apache/inlong/sort/cdc/mongodb/debezium/internal/FlinkDatabaseSchemaHistory.java`
#### Snippet
```java
        this.instanceName = config.getString(DATABASE_HISTORY_INSTANCE_NAME);
        this.listener = listener;
        this.storeOnlyMonitoredTablesDdl = config.getBoolean(STORE_ONLY_MONITORED_TABLES_DDL);
        this.skipUnparseableDDL = config.getBoolean(SKIP_UNPARSEABLE_DDL_STATEMENTS);
        this.useCatalogBeforeSchema = useCatalogBeforeSchema;
```

### Deprecation
'org.apache.flink.table.api.TableSchema' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/elasticsearch-6/src/main/java/org/apache/inlong/sort/elasticsearch6/table/Elasticsearch6DynamicSinkFactory.java`
#### Snippet
```java
    @Override
    public DynamicTableSink createDynamicTableSink(Context context) {
        TableSchema tableSchema = context.getCatalogTable().getSchema();
        ElasticsearchValidationUtils.validatePrimaryKey(tableSchema);
        final FactoryUtil.TableFactoryHelper helper =
```

### Deprecation
'getSchema()' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/elasticsearch-6/src/main/java/org/apache/inlong/sort/elasticsearch6/table/Elasticsearch6DynamicSinkFactory.java`
#### Snippet
```java
    @Override
    public DynamicTableSink createDynamicTableSink(Context context) {
        TableSchema tableSchema = context.getCatalogTable().getSchema();
        ElasticsearchValidationUtils.validatePrimaryKey(tableSchema);
        final FactoryUtil.TableFactoryHelper helper =
```

### Deprecation
'bulkAsync(org.elasticsearch.action.bulk.BulkRequest, org.elasticsearch.action.ActionListener, org.apache.http.Header...)' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/elasticsearch-6/src/main/java/org/apache/inlong/sort/elasticsearch6/Elasticsearch6ApiCallBridge.java`
#### Snippet
```java
    public BulkProcessor.Builder createBulkProcessorBuilder(
            RestHighLevelClient client, BulkProcessor.Listener listener) {
        return BulkProcessor.builder(client::bulkAsync, listener);
    }

```

### Deprecation
'ping(org.apache.http.Header...)' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/elasticsearch-6/src/main/java/org/apache/inlong/sort/elasticsearch6/Elasticsearch6ApiCallBridge.java`
#### Snippet
```java
        }

        if (!client.ping()) {
            throw new RuntimeException("There are no reachable Elasticsearch nodes!");
        }
```

### Deprecation
'org.apache.flink.table.api.TableSchema' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/elasticsearch-7/src/main/java/org/apache/inlong/sort/elasticsearch7/table/Elasticsearch7DynamicSinkFactory.java`
#### Snippet
```java
    @Override
    public DynamicTableSink createDynamicTableSink(Context context) {
        TableSchema tableSchema = context.getCatalogTable().getSchema();
        ElasticsearchValidationUtils.validatePrimaryKey(tableSchema);

```

### Deprecation
'getSchema()' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/elasticsearch-7/src/main/java/org/apache/inlong/sort/elasticsearch7/table/Elasticsearch7DynamicSinkFactory.java`
#### Snippet
```java
    @Override
    public DynamicTableSink createDynamicTableSink(Context context) {
        TableSchema tableSchema = context.getCatalogTable().getSchema();
        ElasticsearchValidationUtils.validatePrimaryKey(tableSchema);

```

### Deprecation
'org.apache.flink.table.api.TableSchema' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/elasticsearch-6/src/main/java/org/apache/inlong/sort/elasticsearch6/table/Elasticsearch6DynamicSink.java`
#### Snippet
```java

    private final EncodingFormat<SerializationSchema<RowData>> format;
    private final TableSchema schema;
    private final Elasticsearch6Configuration config;
    private final String inlongMetric;
```

### Deprecation
'org.apache.flink.table.api.TableSchema' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/elasticsearch-6/src/main/java/org/apache/inlong/sort/elasticsearch6/table/Elasticsearch6DynamicSink.java`
#### Snippet
```java
            EncodingFormat<SerializationSchema<RowData>> format,
            Elasticsearch6Configuration config,
            TableSchema schema,
            String inlongMetric,
            String auditHostAndPorts,
```

### Deprecation
'getFieldTypes()' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/elasticsearch-6/src/main/java/org/apache/inlong/sort/elasticsearch6/table/Elasticsearch6DynamicSink.java`
#### Snippet
```java
                            schema, config.getRoutingField().orElse(null)),
                    dirtySinkHelper,
                    new TableSchemaFactory(schema.getFieldNames(), schema.getFieldTypes()),
                    multipleFormat,
                    indexPattern,
```

### Deprecation
'org.apache.flink.table.api.TableSchema' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/elasticsearch-6/src/main/java/org/apache/inlong/sort/elasticsearch6/table/Elasticsearch6DynamicSink.java`
#### Snippet
```java
            EncodingFormat<SerializationSchema<RowData>> format,
            Elasticsearch6Configuration config,
            TableSchema schema,
            ElasticSearchBuilderProvider builderProvider,
            String inlongMetric,
```

### Deprecation
'org.apache.flink.table.api.TableSchema' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/elasticsearch-base/src/main/java/org/apache/inlong/sort/elasticsearch/table/RoutingExtractor.java`
#### Snippet
```java

    public static Function<RowData, String> createRoutingExtractor(
            TableSchema schema,
            @Nullable String filedName) {
        if (filedName == null) {
```

### Deprecation
'org.apache.flink.table.api.TableColumn' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/elasticsearch-base/src/main/java/org/apache/inlong/sort/elasticsearch/table/RoutingExtractor.java`
#### Snippet
```java
            return null;
        }
        List<TableColumn> tableColumns = schema.getTableColumns();
        for (int i = 0; i < schema.getFieldCount(); i++) {
            TableColumn column = tableColumns.get(i);
```

### Deprecation
'org.apache.flink.table.api.TableColumn' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/elasticsearch-base/src/main/java/org/apache/inlong/sort/elasticsearch/table/RoutingExtractor.java`
#### Snippet
```java
        List<TableColumn> tableColumns = schema.getTableColumns();
        for (int i = 0; i < schema.getFieldCount(); i++) {
            TableColumn column = tableColumns.get(i);
            if (column.getName().equals(filedName)) {
                RowData.FieldGetter fieldGetter = RowData.createFieldGetter(
```

### Deprecation
'org.apache.flink.table.api.TableSchema' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/elasticsearch-base/src/main/java/org/apache/inlong/sort/elasticsearch/table/TableSchemaFactory.java`
#### Snippet
```java
    }

    public TableSchema getSchema() {
        return new TableSchema(fieldNames, fieldTypes);
    }
```

### Deprecation
'TableSchema(java.lang.String\[\], org.apache.flink.api.common.typeinfo.TypeInformation\[\])' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/elasticsearch-base/src/main/java/org/apache/inlong/sort/elasticsearch/table/TableSchemaFactory.java`
#### Snippet
```java

    public TableSchema getSchema() {
        return new TableSchema(fieldNames, fieldTypes);
    }
}
```

### Deprecation
'org.apache.flink.table.api.TableSchema' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/elasticsearch-base/src/main/java/org/apache/inlong/sort/elasticsearch/table/TableSchemaFactory.java`
#### Snippet
```java

    public TableSchema getSchema() {
        return new TableSchema(fieldNames, fieldTypes);
    }
}
```

### Deprecation
'org.apache.flink.table.api.TableSchema' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/elasticsearch-base/src/main/java/org/apache/inlong/sort/elasticsearch/table/ElasticsearchValidationUtils.java`
#### Snippet
```java
     * LogicalTypeRoot#RAW} type.
     */
    public static void validatePrimaryKey(TableSchema schema) {
        schema.getPrimaryKey()
                .ifPresent(
```

### Deprecation
'getFieldTypes()' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/elasticsearch-7/src/main/java/org/apache/inlong/sort/elasticsearch7/table/Elasticsearch7DynamicSink.java`
#### Snippet
```java
                            schema, config.getRoutingField().orElse(null)),
                    dirtySinkHelper,
                    new TableSchemaFactory(schema.getFieldNames(), schema.getFieldTypes()),
                    multipleFormat,
                    indexPattern,
```

### Deprecation
'org.apache.flink.table.api.TableSchema' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/elasticsearch-7/src/main/java/org/apache/inlong/sort/elasticsearch7/table/Elasticsearch7DynamicSink.java`
#### Snippet
```java

    private final EncodingFormat<SerializationSchema<RowData>> format;
    private final TableSchema schema;
    private final Elasticsearch7Configuration config;
    private final String inlongMetric;
```

### Deprecation
'org.apache.flink.table.api.TableSchema' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/elasticsearch-7/src/main/java/org/apache/inlong/sort/elasticsearch7/table/Elasticsearch7DynamicSink.java`
#### Snippet
```java
            EncodingFormat<SerializationSchema<RowData>> format,
            Elasticsearch7Configuration config,
            TableSchema schema,
            String inlongMetric,
            String auditHostAndPorts,
```

### Deprecation
'org.apache.flink.table.api.TableSchema' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/elasticsearch-7/src/main/java/org/apache/inlong/sort/elasticsearch7/table/Elasticsearch7DynamicSink.java`
#### Snippet
```java
            EncodingFormat<SerializationSchema<RowData>> format,
            Elasticsearch7Configuration config,
            TableSchema schema,
            ElasticSearchBuilderProvider builderProvider,
            String inlongMetric,
```

### Deprecation
'org.apache.flink.table.api.TableColumn' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/elasticsearch-base/src/main/java/org/apache/inlong/sort/elasticsearch/table/KeyExtractor.java`
#### Snippet
```java
        public int index;

        public ColumnWithIndex(TableColumn column, int index) {
            this.column = column;
            this.index = index;
```

### Deprecation
'org.apache.flink.table.api.TableColumn' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/elasticsearch-base/src/main/java/org/apache/inlong/sort/elasticsearch/table/KeyExtractor.java`
#### Snippet
```java
    private static class ColumnWithIndex {

        public TableColumn column;
        public int index;

```

### Deprecation
'org.apache.flink.table.api.TableSchema' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/elasticsearch-base/src/main/java/org/apache/inlong/sort/elasticsearch/table/KeyExtractor.java`
#### Snippet
```java

    public static Function<RowData, String> createKeyExtractor(
            TableSchema schema, String keyDelimiter) {
        return schema.getPrimaryKey()
                .map(
```

### Deprecation
'org.apache.flink.table.api.TableColumn' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/elasticsearch-base/src/main/java/org/apache/inlong/sort/elasticsearch/table/KeyExtractor.java`
#### Snippet
```java
                        key -> {
                            Map<String, ColumnWithIndex> namesToColumns = new HashMap<>();
                            List<TableColumn> tableColumns = schema.getTableColumns();
                            for (int i = 0; i < schema.getFieldCount(); i++) {
                                TableColumn column = tableColumns.get(i);
```

### Deprecation
'org.apache.flink.table.api.TableColumn' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/elasticsearch-base/src/main/java/org/apache/inlong/sort/elasticsearch/table/KeyExtractor.java`
#### Snippet
```java
                            List<TableColumn> tableColumns = schema.getTableColumns();
                            for (int i = 0; i < schema.getFieldCount(); i++) {
                                TableColumn column = tableColumns.get(i);
                                namesToColumns.put(
                                        column.getName(), new ColumnWithIndex(column, i));
```

### Deprecation
'org.apache.flink.table.api.TableSchema' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/elasticsearch-base/src/main/java/org/apache/inlong/sort/elasticsearch/table/MultipleElasticsearchSinkFunctionBase.java`
#### Snippet
```java
            String rawIndex = jsonDynamicSchemaFormat.parse(rootNode, indexPattern);
            if (!indexGeneratorMap.containsKey(rawIndex)) {
                TableSchema schema = tableSchemaFactory.getSchema();
                IndexGenerator indexGenerator = IndexGeneratorFactory.createIndexGenerator(rawIndex, schema);
                indexGeneratorMap.put(rawIndex, indexGenerator);
```

### Deprecation
'org.elasticsearch.client.transport.TransportClient' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/elasticsearch-base/src/main/java/org/apache/inlong/sort/elasticsearch/ElasticsearchSinkBase.java`
#### Snippet
```java
     * The config map that contains configuration for the bulk flushing behaviours.
     *
     * <p>For {@link org.elasticsearch.client.transport.TransportClient} based implementations, this
     * config map would also contain Elasticsearch-shipped configuration, and therefore this config
     * map would also be forwarded when creating the Elasticsearch client.
```

### Deprecation
'org.apache.flink.table.api.TableSchema' is deprecated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/elasticsearch-base/src/main/java/org/apache/inlong/sort/elasticsearch/table/IndexGeneratorFactory.java`
#### Snippet
```java
    }

    public static IndexGenerator createIndexGenerator(String index, TableSchema schema) {
        final IndexHelper indexHelper = new IndexHelper();
        if (indexHelper.checkIsDynamicIndex(index)) {
```

### Deprecation
'DATA' is deprecated
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/Metadata.java`
#### Snippet
```java
            case OP_TYPE:
            case DATA_CANAL:
            case DATA:
            case DATA_DEBEZIUM:
            case COLLECTION_NAME:
```

### Deprecation
'DATA_BYTES' is deprecated
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/Metadata.java`
#### Snippet
```java
                metadataType = "ARRAY<MAP<STRING, STRING>>";
                break;
            case DATA_BYTES:
            case DATA_BYTES_DEBEZIUM:
            case DATA_BYTES_CANAL:
```

### Deprecation
'isNumber(java.lang.String)' is deprecated
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/util/ParameterTool.java`
#### Snippet
```java
            if (i >= args.length) {
                map.put(key, NO_VALUE_KEY);
            } else if (NumberUtils.isNumber(args[i])) {
                map.put(key, args[i]);
                i += 1;
```

### Deprecation
'DATA' is deprecated
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/load/KafkaLoadNode.java`
#### Snippet
```java
                metadataKey = "value.type";
                break;
            case DATA:
            case DATA_CANAL:
                metadataKey = "value.data_canal";
```

### Deprecation
'DATA' is deprecated
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/load/KafkaLoadNode.java`
#### Snippet
```java
                MetaField.DATABASE_NAME, MetaField.SQL_TYPE, MetaField.PK_NAMES, MetaField.TS,
                MetaField.OP_TS, MetaField.IS_DDL, MetaField.MYSQL_TYPE, MetaField.BATCH_ID,
                MetaField.UPDATE_BEFORE, MetaField.DATA_CANAL, MetaField.DATA);
    }
}
```

### Deprecation
'DATA' is deprecated
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/extract/PostgresExtractNode.java`
#### Snippet
```java
    public Set<MetaField> supportedMetaFields() {
        return EnumSet.of(MetaField.PROCESS_TIME, MetaField.TABLE_NAME, MetaField.DATABASE_NAME, MetaField.SCHEMA_NAME,
                MetaField.OP_TS, MetaField.OP_TYPE, MetaField.DATA, MetaField.DATA_BYTES, MetaField.DATA_CANAL,
                MetaField.DATA_BYTES_CANAL, MetaField.DATA_DEBEZIUM, MetaField.DATA_BYTES_DEBEZIUM, MetaField.IS_DDL,
                MetaField.TS, MetaField.SQL_TYPE, MetaField.PK_NAMES);
```

### Deprecation
'DATA_BYTES' is deprecated
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/extract/PostgresExtractNode.java`
#### Snippet
```java
    public Set<MetaField> supportedMetaFields() {
        return EnumSet.of(MetaField.PROCESS_TIME, MetaField.TABLE_NAME, MetaField.DATABASE_NAME, MetaField.SCHEMA_NAME,
                MetaField.OP_TS, MetaField.OP_TYPE, MetaField.DATA, MetaField.DATA_BYTES, MetaField.DATA_CANAL,
                MetaField.DATA_BYTES_CANAL, MetaField.DATA_DEBEZIUM, MetaField.DATA_BYTES_DEBEZIUM, MetaField.IS_DDL,
                MetaField.TS, MetaField.SQL_TYPE, MetaField.PK_NAMES);
```

### Deprecation
'DATA' is deprecated
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/extract/PostgresExtractNode.java`
#### Snippet
```java
                metadataKey = "meta.op_type";
                break;
            case DATA:
            case DATA_BYTES:
                metadataKey = "meta.data";
```

### Deprecation
'DATA_BYTES' is deprecated
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/extract/PostgresExtractNode.java`
#### Snippet
```java
                break;
            case DATA:
            case DATA_BYTES:
                metadataKey = "meta.data";
                break;
```

### Deprecation
'DATA' is deprecated
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/extract/OracleExtractNode.java`
#### Snippet
```java
                metadataKey = "meta.op_type";
                break;
            case DATA:
            case DATA_BYTES:
                metadataKey = "meta.data";
```

### Deprecation
'DATA_BYTES' is deprecated
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/extract/OracleExtractNode.java`
#### Snippet
```java
                break;
            case DATA:
            case DATA_BYTES:
                metadataKey = "meta.data";
                break;
```

### Deprecation
'DATA' is deprecated
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/extract/OracleExtractNode.java`
#### Snippet
```java
    public Set<MetaField> supportedMetaFields() {
        return EnumSet.of(MetaField.PROCESS_TIME, MetaField.TABLE_NAME, MetaField.DATABASE_NAME,
                MetaField.SCHEMA_NAME, MetaField.OP_TS, MetaField.OP_TYPE, MetaField.DATA, MetaField.DATA_BYTES,
                MetaField.DATA_CANAL, MetaField.DATA_BYTES_CANAL, MetaField.IS_DDL, MetaField.TS,
                MetaField.SQL_TYPE, MetaField.ORACLE_TYPE, MetaField.PK_NAMES);
```

### Deprecation
'DATA_BYTES' is deprecated
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/extract/OracleExtractNode.java`
#### Snippet
```java
    public Set<MetaField> supportedMetaFields() {
        return EnumSet.of(MetaField.PROCESS_TIME, MetaField.TABLE_NAME, MetaField.DATABASE_NAME,
                MetaField.SCHEMA_NAME, MetaField.OP_TS, MetaField.OP_TYPE, MetaField.DATA, MetaField.DATA_BYTES,
                MetaField.DATA_CANAL, MetaField.DATA_BYTES_CANAL, MetaField.IS_DDL, MetaField.TS,
                MetaField.SQL_TYPE, MetaField.ORACLE_TYPE, MetaField.PK_NAMES);
```

### Deprecation
'DATA' is deprecated
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/extract/MySqlExtractNode.java`
#### Snippet
```java
                metadataKey = "meta.op_type";
                break;
            case DATA:
            case DATA_BYTES:
            case DATA_CANAL:
```

### Deprecation
'DATA_BYTES' is deprecated
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/extract/MySqlExtractNode.java`
#### Snippet
```java
                break;
            case DATA:
            case DATA_BYTES:
            case DATA_CANAL:
            case DATA_BYTES_CANAL:
```

### Deprecation
'DATA' is deprecated
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/extract/MySqlExtractNode.java`
#### Snippet
```java
                MetaField.TS, MetaField.SQL_TYPE, MetaField.MYSQL_TYPE, MetaField.PK_NAMES,
                MetaField.BATCH_ID, MetaField.UPDATE_BEFORE, MetaField.DATA_BYTES_DEBEZIUM,
                MetaField.DATA_DEBEZIUM, MetaField.DATA_BYTES_CANAL, MetaField.DATA, MetaField.DATA_BYTES);
    }
}
```

### Deprecation
'DATA_BYTES' is deprecated
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/extract/MySqlExtractNode.java`
#### Snippet
```java
                MetaField.TS, MetaField.SQL_TYPE, MetaField.MYSQL_TYPE, MetaField.PK_NAMES,
                MetaField.BATCH_ID, MetaField.UPDATE_BEFORE, MetaField.DATA_BYTES_DEBEZIUM,
                MetaField.DATA_DEBEZIUM, MetaField.DATA_BYTES_CANAL, MetaField.DATA, MetaField.DATA_BYTES);
    }
}
```

### Deprecation
'org.apache.flink.table.descriptors.DescriptorProperties' is deprecated
in `inlong-sort/sort-formats/format-kv/src/main/java/org/apache/inlong/sort/formats/kv/KvValidator.java`
#### Snippet
```java

    @Override
    public void validate(DescriptorProperties properties) {
        super.validate(properties);

```

### Deprecation
'org.apache.flink.table.descriptors.DescriptorProperties' is deprecated
in `inlong-sort/sort-formats/format-csv/src/main/java/org/apache/inlong/sort/formats/csv/CsvValidator.java`
#### Snippet
```java

    @Override
    public void validate(DescriptorProperties properties) {
        super.validate(properties);

```

### Deprecation
'org.apache.flink.table.descriptors.DescriptorProperties' is deprecated
in `inlong-sort/sort-formats/format-kv/src/main/java/org/apache/inlong/sort/formats/kv/Kv.java`
#### Snippet
```java
    @Override
    protected Map<String, String> toFormatProperties() {
        final DescriptorProperties properties = new DescriptorProperties();
        properties.putProperties(internalProperties);
        return properties.asMap();
```

### Deprecation
'org.apache.flink.table.descriptors.DescriptorProperties' is deprecated
in `inlong-sort/sort-formats/format-kv/src/main/java/org/apache/inlong/sort/formats/kv/Kv.java`
#### Snippet
```java
    @Override
    protected Map<String, String> toFormatProperties() {
        final DescriptorProperties properties = new DescriptorProperties();
        properties.putProperties(internalProperties);
        return properties.asMap();
```

### Deprecation
'org.apache.flink.table.descriptors.DescriptorProperties' is deprecated
in `inlong-sort/sort-formats/format-kv/src/main/java/org/apache/inlong/sort/formats/kv/Kv.java`
#### Snippet
```java
    public static final String FORMAT_TYPE_VALUE = "tdkv";

    private DescriptorProperties internalProperties =
            new DescriptorProperties(true);

```

### Deprecation
'org.apache.flink.table.descriptors.DescriptorProperties' is deprecated
in `inlong-sort/sort-formats/format-kv/src/main/java/org/apache/inlong/sort/formats/kv/Kv.java`
#### Snippet
```java

    private DescriptorProperties internalProperties =
            new DescriptorProperties(true);

    public Kv() {
```

### Deprecation
'org.apache.flink.table.descriptors.DescriptorProperties' is deprecated
in `inlong-sort/sort-formats/format-csv/src/main/java/org/apache/inlong/sort/formats/csv/Csv.java`
#### Snippet
```java
    public static final String FORMAT_TYPE_VALUE = "tdcsv";

    private DescriptorProperties internalProperties =
            new DescriptorProperties(true);

```

### Deprecation
'org.apache.flink.table.descriptors.DescriptorProperties' is deprecated
in `inlong-sort/sort-formats/format-csv/src/main/java/org/apache/inlong/sort/formats/csv/Csv.java`
#### Snippet
```java

    private DescriptorProperties internalProperties =
            new DescriptorProperties(true);

    public Csv() {
```

### Deprecation
'org.apache.flink.table.descriptors.DescriptorProperties' is deprecated
in `inlong-sort/sort-formats/format-csv/src/main/java/org/apache/inlong/sort/formats/csv/Csv.java`
#### Snippet
```java
    @Override
    protected Map<String, String> toFormatProperties() {
        final DescriptorProperties properties = new DescriptorProperties();
        properties.putProperties(internalProperties);
        return properties.asMap();
```

### Deprecation
'org.apache.flink.table.descriptors.DescriptorProperties' is deprecated
in `inlong-sort/sort-formats/format-csv/src/main/java/org/apache/inlong/sort/formats/csv/Csv.java`
#### Snippet
```java
    @Override
    protected Map<String, String> toFormatProperties() {
        final DescriptorProperties properties = new DescriptorProperties();
        properties.putProperties(internalProperties);
        return properties.asMap();
```

### Deprecation
'org.apache.flink.table.descriptors.DescriptorProperties' is deprecated
in `inlong-sort/sort-formats/format-kv/src/main/java/org/apache/inlong/sort/formats/kv/KvFormatFactory.java`
#### Snippet
```java
    }

    private static DescriptorProperties getValidatedProperties(
            Map<String, String> properties) {
        DescriptorProperties descriptorProperties =
```

### Deprecation
'org.apache.flink.table.descriptors.DescriptorProperties' is deprecated
in `inlong-sort/sort-formats/format-kv/src/main/java/org/apache/inlong/sort/formats/kv/KvFormatFactory.java`
#### Snippet
```java
    private static DescriptorProperties getValidatedProperties(
            Map<String, String> properties) {
        DescriptorProperties descriptorProperties =
                new DescriptorProperties(true);
        descriptorProperties.putProperties(properties);
```

### Deprecation
'org.apache.flink.table.descriptors.DescriptorProperties' is deprecated
in `inlong-sort/sort-formats/format-kv/src/main/java/org/apache/inlong/sort/formats/kv/KvFormatFactory.java`
#### Snippet
```java
            Map<String, String> properties) {
        DescriptorProperties descriptorProperties =
                new DescriptorProperties(true);
        descriptorProperties.putProperties(properties);

```

### Deprecation
'org.apache.flink.table.descriptors.DescriptorProperties' is deprecated
in `inlong-sort/sort-formats/format-kv/src/main/java/org/apache/inlong/sort/formats/kv/KvFormatFactory.java`
#### Snippet
```java

    private static KvSerializationSchema buildSerializationSchema(
            DescriptorProperties descriptorProperties,
            RowFormatInfo rowFormatInfo) {
        for (FormatInfo formatInfo : rowFormatInfo.getFieldFormatInfos()) {
```

### Deprecation
'org.apache.flink.table.descriptors.DescriptorProperties' is deprecated
in `inlong-sort/sort-formats/format-kv/src/main/java/org/apache/inlong/sort/formats/kv/KvFormatFactory.java`
#### Snippet
```java

    private static KvDeserializationSchema buildDeserializationSchema(
            DescriptorProperties descriptorProperties,
            RowFormatInfo rowFormatInfo) {
        for (FormatInfo formatInfo : rowFormatInfo.getFieldFormatInfos()) {
```

### Deprecation
'org.apache.flink.table.descriptors.DescriptorProperties' is deprecated
in `inlong-sort/sort-formats/format-kv/src/main/java/org/apache/inlong/sort/formats/kv/KvFormatFactory.java`
#### Snippet
```java
    public KvSerializationSchema createSerializationSchema(
            Map<String, String> properties) {
        final DescriptorProperties descriptorProperties =
                getValidatedProperties(properties);

```

### Deprecation
'org.apache.flink.table.descriptors.DescriptorProperties' is deprecated
in `inlong-sort/sort-formats/format-kv/src/main/java/org/apache/inlong/sort/formats/kv/KvFormatFactory.java`
#### Snippet
```java
    public TableFormatSerializer createFormatSerializer(
            Map<String, String> properties) {
        final DescriptorProperties descriptorProperties =
                getValidatedProperties(properties);

```

### Deprecation
'org.apache.flink.table.descriptors.DescriptorProperties' is deprecated
in `inlong-sort/sort-formats/format-kv/src/main/java/org/apache/inlong/sort/formats/kv/KvFormatFactory.java`
#### Snippet
```java
            Map<String, String> properties,
            int[] fields) {
        final DescriptorProperties descriptorProperties =
                getValidatedProperties(properties);

```

### Deprecation
'org.apache.flink.table.descriptors.DescriptorProperties' is deprecated
in `inlong-sort/sort-formats/format-kv/src/main/java/org/apache/inlong/sort/formats/kv/KvFormatFactory.java`
#### Snippet
```java
            Map<String, String> properties,
            int[] fields) {
        final DescriptorProperties descriptorProperties =
                getValidatedProperties(properties);

```

### Deprecation
'org.apache.flink.table.descriptors.DescriptorProperties' is deprecated
in `inlong-sort/sort-formats/format-kv/src/main/java/org/apache/inlong/sort/formats/kv/KvFormatFactory.java`
#### Snippet
```java
    public TableFormatDeserializer createFormatDeserializer(
            Map<String, String> properties) {
        final DescriptorProperties descriptorProperties =
                getValidatedProperties(properties);

```

### Deprecation
'org.apache.flink.table.descriptors.DescriptorProperties' is deprecated
in `inlong-sort/sort-formats/format-kv/src/main/java/org/apache/inlong/sort/formats/kv/KvFormatFactory.java`
#### Snippet
```java
    public KvDeserializationSchema createDeserializationSchema(
            Map<String, String> properties) {
        final DescriptorProperties descriptorProperties =
                getValidatedProperties(properties);

```

### Deprecation
'org.apache.flink.table.descriptors.DescriptorProperties' is deprecated
in `inlong-sort/sort-formats/format-csv/src/main/java/org/apache/inlong/sort/formats/csv/CsvFormatFactory.java`
#### Snippet
```java

    private static CsvDeserializationSchema buildDeserializationSchema(
            DescriptorProperties descriptorProperties,
            RowFormatInfo rowFormatInfo) {
        for (FormatInfo formatInfo : rowFormatInfo.getFieldFormatInfos()) {
```

### Deprecation
'org.apache.flink.table.descriptors.DescriptorProperties' is deprecated
in `inlong-sort/sort-formats/format-csv/src/main/java/org/apache/inlong/sort/formats/csv/CsvFormatFactory.java`
#### Snippet
```java
    public TableFormatSerializer createFormatSerializer(
            Map<String, String> properties) {
        final DescriptorProperties descriptorProperties =
                getValidatedProperties(properties);

```

### Deprecation
'org.apache.flink.table.descriptors.DescriptorProperties' is deprecated
in `inlong-sort/sort-formats/format-csv/src/main/java/org/apache/inlong/sort/formats/csv/CsvFormatFactory.java`
#### Snippet
```java
            Map<String, String> properties,
            int[] fields) {
        final DescriptorProperties descriptorProperties =
                getValidatedProperties(properties);

```

### Deprecation
'org.apache.flink.table.descriptors.DescriptorProperties' is deprecated
in `inlong-sort/sort-formats/format-csv/src/main/java/org/apache/inlong/sort/formats/csv/CsvFormatFactory.java`
#### Snippet
```java
    public TableFormatDeserializer createFormatDeserializer(
            Map<String, String> properties) {
        final DescriptorProperties descriptorProperties =
                getValidatedProperties(properties);

```

### Deprecation
'org.apache.flink.table.descriptors.DescriptorProperties' is deprecated
in `inlong-sort/sort-formats/format-csv/src/main/java/org/apache/inlong/sort/formats/csv/CsvFormatFactory.java`
#### Snippet
```java
            Map<String, String> properties,
            int[] fields) {
        final DescriptorProperties descriptorProperties =
                getValidatedProperties(properties);

```

### Deprecation
'org.apache.flink.table.descriptors.DescriptorProperties' is deprecated
in `inlong-sort/sort-formats/format-csv/src/main/java/org/apache/inlong/sort/formats/csv/CsvFormatFactory.java`
#### Snippet
```java
    public CsvSerializationSchema createSerializationSchema(
            Map<String, String> properties) {
        final DescriptorProperties descriptorProperties =
                getValidatedProperties(properties);

```

### Deprecation
'org.apache.flink.table.descriptors.DescriptorProperties' is deprecated
in `inlong-sort/sort-formats/format-csv/src/main/java/org/apache/inlong/sort/formats/csv/CsvFormatFactory.java`
#### Snippet
```java

    private static CsvSerializationSchema buildSerializationSchema(
            DescriptorProperties descriptorProperties,
            RowFormatInfo rowFormatInfo) {
        for (FormatInfo formatInfo : rowFormatInfo.getFieldFormatInfos()) {
```

### Deprecation
'org.apache.flink.table.descriptors.DescriptorProperties' is deprecated
in `inlong-sort/sort-formats/format-csv/src/main/java/org/apache/inlong/sort/formats/csv/CsvFormatFactory.java`
#### Snippet
```java
    }

    public static DescriptorProperties getValidatedProperties(
            Map<String, String> properties) {
        DescriptorProperties descriptorProperties = new DescriptorProperties(true);
```

### Deprecation
'org.apache.flink.table.descriptors.DescriptorProperties' is deprecated
in `inlong-sort/sort-formats/format-csv/src/main/java/org/apache/inlong/sort/formats/csv/CsvFormatFactory.java`
#### Snippet
```java
    public static DescriptorProperties getValidatedProperties(
            Map<String, String> properties) {
        DescriptorProperties descriptorProperties = new DescriptorProperties(true);
        descriptorProperties.putProperties(properties);

```

### Deprecation
'org.apache.flink.table.descriptors.DescriptorProperties' is deprecated
in `inlong-sort/sort-formats/format-csv/src/main/java/org/apache/inlong/sort/formats/csv/CsvFormatFactory.java`
#### Snippet
```java
    public static DescriptorProperties getValidatedProperties(
            Map<String, String> properties) {
        DescriptorProperties descriptorProperties = new DescriptorProperties(true);
        descriptorProperties.putProperties(properties);

```

### Deprecation
'org.apache.flink.table.descriptors.DescriptorProperties' is deprecated
in `inlong-sort/sort-formats/format-csv/src/main/java/org/apache/inlong/sort/formats/csv/CsvFormatFactory.java`
#### Snippet
```java
    public CsvDeserializationSchema createDeserializationSchema(
            Map<String, String> properties) {
        final DescriptorProperties descriptorProperties =
                getValidatedProperties(properties);

```

### Deprecation
'org.apache.flink.table.runtime.types.TypeInfoDataTypeConverter' is deprecated
in `inlong-sort/sort-formats/format-json/src/main/java/org/apache/inlong/sort/formats/json/canal/CanalJsonSerializationSchema.java`
#### Snippet
```java
                        .collect(Collectors.toList());

        return (RowTypeInfo) TypeInfoDataTypeConverter.fromDataTypeToTypeInfo(
                DataTypeUtils.appendRowFields(root, metadataFields));
    }
```

### Deprecation
'org.apache.flink.table.descriptors.DescriptorProperties' is deprecated
in `inlong-sort/sort-formats/format-base/src/main/java/org/apache/inlong/sort/formats/base/TableFormatUtils.java`
#### Snippet
```java
     */
    public static RowFormatInfo deriveRowFormatInfo(
            DescriptorProperties descriptorProperties) {
        TableSchema tableSchema =
                deriveSchema(descriptorProperties.asMap());
```

### Deprecation
'org.apache.flink.table.api.TableSchema' is deprecated
in `inlong-sort/sort-formats/format-base/src/main/java/org/apache/inlong/sort/formats/base/TableFormatUtils.java`
#### Snippet
```java
    public static RowFormatInfo deriveRowFormatInfo(
            DescriptorProperties descriptorProperties) {
        TableSchema tableSchema =
                deriveSchema(descriptorProperties.asMap());

```

### Deprecation
'org.apache.flink.table.descriptors.DescriptorProperties' is deprecated
in `inlong-sort/sort-formats/format-base/src/main/java/org/apache/inlong/sort/formats/base/TableFormatUtils.java`
#### Snippet
```java
     */
    public static RowFormatInfo getRowFormatInfo(
            DescriptorProperties descriptorProperties) {
        if (descriptorProperties.containsKey(FORMAT_SCHEMA)) {
            return deserializeRowFormatInfo(descriptorProperties);
```

### Deprecation
'org.apache.flink.table.descriptors.DescriptorProperties' is deprecated
in `inlong-sort/sort-formats/format-base/src/main/java/org/apache/inlong/sort/formats/base/TableFormatUtils.java`
#### Snippet
```java
     * @param descriptorProperties The properties of the descriptor.
     */
    public static void validateSchema(DescriptorProperties descriptorProperties) {
        final boolean defineSchema = descriptorProperties.containsKey(FORMAT_SCHEMA);
        final boolean deriveSchema = descriptorProperties.containsKey(FORMAT_DERIVE_SCHEMA);
```

### Deprecation
'org.apache.flink.table.descriptors.DescriptorProperties' is deprecated
in `inlong-sort/sort-formats/format-base/src/main/java/org/apache/inlong/sort/formats/base/TableFormatUtils.java`
#### Snippet
```java
     */
    public static RowFormatInfo deserializeRowFormatInfo(
            DescriptorProperties descriptorProperties) {
        try {
            String schema = descriptorProperties.getString(FORMAT_SCHEMA);
```

### Deprecation
'org.apache.flink.table.descriptors.DescriptorProperties' is deprecated
in `inlong-sort/sort-formats/format-inlongmsg-csv/src/main/java/org/apache/inlong/sort/formats/inlongmsgcsv/InLongMsgCsv.java`
#### Snippet
```java
    public static final String FORMAT_TYPE_VALUE = "inlongmsgcsv";

    private DescriptorProperties internalProperties =
            new DescriptorProperties(true);

```

### Deprecation
'org.apache.flink.table.descriptors.DescriptorProperties' is deprecated
in `inlong-sort/sort-formats/format-inlongmsg-csv/src/main/java/org/apache/inlong/sort/formats/inlongmsgcsv/InLongMsgCsv.java`
#### Snippet
```java

    private DescriptorProperties internalProperties =
            new DescriptorProperties(true);

    public InLongMsgCsv() {
```

### Deprecation
'org.apache.flink.table.descriptors.DescriptorProperties' is deprecated
in `inlong-sort/sort-formats/format-inlongmsg-csv/src/main/java/org/apache/inlong/sort/formats/inlongmsgcsv/InLongMsgCsv.java`
#### Snippet
```java
    @Override
    protected Map<String, String> toFormatProperties() {
        final DescriptorProperties properties = new DescriptorProperties();
        properties.putProperties(internalProperties);
        return properties.asMap();
```

### Deprecation
'org.apache.flink.table.descriptors.DescriptorProperties' is deprecated
in `inlong-sort/sort-formats/format-inlongmsg-csv/src/main/java/org/apache/inlong/sort/formats/inlongmsgcsv/InLongMsgCsv.java`
#### Snippet
```java
    @Override
    protected Map<String, String> toFormatProperties() {
        final DescriptorProperties properties = new DescriptorProperties();
        properties.putProperties(internalProperties);
        return properties.asMap();
```

### Deprecation
'org.apache.flink.table.descriptors.DescriptorProperties' is deprecated
in `inlong-sort/sort-formats/format-inlongmsg-csv/src/main/java/org/apache/inlong/sort/formats/inlongmsgcsv/InLongMsgCsvFormatFactory.java`
#### Snippet
```java
    public InLongMsgCsvMixedFormatConverter createMixedFormatConverter(
            Map<String, String> properties) {
        final DescriptorProperties descriptorProperties =
                new DescriptorProperties(true);
        descriptorProperties.putProperties(properties);
```

### Deprecation
'org.apache.flink.table.descriptors.DescriptorProperties' is deprecated
in `inlong-sort/sort-formats/format-inlongmsg-csv/src/main/java/org/apache/inlong/sort/formats/inlongmsgcsv/InLongMsgCsvFormatFactory.java`
#### Snippet
```java
            Map<String, String> properties) {
        final DescriptorProperties descriptorProperties =
                new DescriptorProperties(true);
        descriptorProperties.putProperties(properties);

```

### Deprecation
'org.apache.flink.table.descriptors.DescriptorProperties' is deprecated
in `inlong-sort/sort-formats/format-inlongmsg-csv/src/main/java/org/apache/inlong/sort/formats/inlongmsgcsv/InLongMsgCsvFormatFactory.java`
#### Snippet
```java
    public InLongMsgCsvMixedFormatDeserializer createMixedFormatDeserializer(
            Map<String, String> properties) {
        final DescriptorProperties descriptorProperties =
                new DescriptorProperties(true);
        descriptorProperties.putProperties(properties);
```

### Deprecation
'org.apache.flink.table.descriptors.DescriptorProperties' is deprecated
in `inlong-sort/sort-formats/format-inlongmsg-csv/src/main/java/org/apache/inlong/sort/formats/inlongmsgcsv/InLongMsgCsvFormatFactory.java`
#### Snippet
```java
            Map<String, String> properties) {
        final DescriptorProperties descriptorProperties =
                new DescriptorProperties(true);
        descriptorProperties.putProperties(properties);

```

### Deprecation
'org.apache.flink.table.descriptors.DescriptorProperties' is deprecated
in `inlong-sort/sort-formats/format-inlongmsg-csv/src/main/java/org/apache/inlong/sort/formats/inlongmsgcsv/InLongMsgCsvFormatFactory.java`
#### Snippet
```java
    public TableFormatDeserializer createFormatDeserializer(
            Map<String, String> properties) {
        final DescriptorProperties descriptorProperties =
                new DescriptorProperties(true);
        descriptorProperties.putProperties(properties);
```

### Deprecation
'org.apache.flink.table.descriptors.DescriptorProperties' is deprecated
in `inlong-sort/sort-formats/format-inlongmsg-csv/src/main/java/org/apache/inlong/sort/formats/inlongmsgcsv/InLongMsgCsvFormatFactory.java`
#### Snippet
```java
            Map<String, String> properties) {
        final DescriptorProperties descriptorProperties =
                new DescriptorProperties(true);
        descriptorProperties.putProperties(properties);

```

### Deprecation
'org.apache.flink.table.descriptors.DescriptorProperties' is deprecated
in `inlong-sort/sort-formats/format-inlongmsg-base/src/main/java/org/apache/inlong/sort/formats/inlongmsg/InLongMsgMixedValidator.java`
#### Snippet
```java

    @Override
    public void validate(DescriptorProperties properties) {
        super.validate(properties);

```

### Deprecation
'org.apache.flink.table.descriptors.DescriptorProperties' is deprecated
in `inlong-sort/sort-formats/format-inlongmsg-base/src/main/java/org/apache/inlong/sort/formats/inlongmsg/InLongMsgValidator.java`
#### Snippet
```java

    @Override
    public void validate(DescriptorProperties properties) {
        super.validate(properties);

```

### Deprecation
'org.apache.flink.table.descriptors.DescriptorProperties' is deprecated
in `inlong-sort/sort-formats/format-inlongmsg-base/src/main/java/org/apache/inlong/sort/formats/inlongmsg/InLongMsgUtils.java`
#### Snippet
```java

    public static RowFormatInfo getDataFormatInfo(
            DescriptorProperties descriptorProperties) {
        if (descriptorProperties.containsKey(TableFormatConstants.FORMAT_SCHEMA)) {
            return TableFormatUtils.deserializeRowFormatInfo(descriptorProperties);
```

### Deprecation
'org.apache.flink.table.api.TableSchema' is deprecated
in `inlong-sort/sort-formats/format-inlongmsg-base/src/main/java/org/apache/inlong/sort/formats/inlongmsg/InLongMsgUtils.java`
#### Snippet
```java
            return TableFormatUtils.deserializeRowFormatInfo(descriptorProperties);
        } else {
            TableSchema tableSchema =
                    deriveSchema(descriptorProperties.asMap());

```

### Deprecation
'getRestartJobs()' is deprecated
in `inlong-agent/agent-core/src/main/java/org/apache/inlong/agent/core/job/JobManager.java`
#### Snippet
```java
     */
    private void startJobs() {
        List<JobProfile> profileList = jobProfileDb.getRestartJobs();
        for (JobProfile profile : profileList) {
            LOGGER.info("init starting job from db {}", profile.toJsonStr());
```

### Deprecation
'parse(java.io.Reader)' is deprecated
in `inlong-agent/agent-common/src/main/java/org/apache/inlong/agent/conf/AbstractConfiguration.java`
#### Snippet
```java
                reader = new InputStreamReader(inputStream, StandardCharsets.UTF_8);
                if (isJson) {
                    JsonElement tmpElement = JSON_PARSER.parse(reader).getAsJsonObject();
                    updateConfig(new HashMap<>(10), 0, tmpElement);
                } else {
```

### Deprecation
'parse(java.lang.String)' is deprecated
in `inlong-agent/agent-common/src/main/java/org/apache/inlong/agent/conf/AbstractConfiguration.java`
#### Snippet
```java
     */
    public void loadJsonStrResource(String jsonStr) {
        JsonElement tmpElement = JSON_PARSER.parse(jsonStr);
        updateConfig(new HashMap<>(10), 0, tmpElement);
    }
```

### Deprecation
'JsonParser()' is deprecated
in `inlong-agent/agent-common/src/main/java/org/apache/inlong/agent/conf/AbstractConfiguration.java`
#### Snippet
```java

    private static final Logger LOGGER = LoggerFactory.getLogger(AbstractConfiguration.class);
    private static final JsonParser JSON_PARSER = new JsonParser();

    private final Map<String, JsonPrimitive> configStorage = new HashMap<>();
```

### Deprecation
'MAX_COPY_THREADS' is deprecated
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/sources/reader/MongoDBReader.java`
#### Snippet
```java
        setEngineConfigIfNecessary(jobConf, builder,
                JOB_MONGO_BACKOFF_INITIAL_DELAY, CONNECT_BACKOFF_INITIAL_DELAY_MS);
        setEngineConfigIfNecessary(jobConf, builder, JOB_MONGO_INITIAL_SYNC_MAX_THREADS, MAX_COPY_THREADS);
        setEngineConfigIfNecessary(jobConf, builder,
                JOB_MONGO_SSL_INVALID_HOSTNAME_ALLOWED, SSL_ALLOW_INVALID_HOSTNAMES);
```

### Deprecation
'MAX_COPY_THREADS' is deprecated
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/sources/reader/MongoDBReader.java`
#### Snippet
```java
import static io.debezium.connector.mongodb.MongoDbConnectorConfig.FIELD_RENAMES;
import static io.debezium.connector.mongodb.MongoDbConnectorConfig.HOSTS;
import static io.debezium.connector.mongodb.MongoDbConnectorConfig.MAX_COPY_THREADS;
import static io.debezium.connector.mongodb.MongoDbConnectorConfig.MAX_FAILED_CONNECTIONS;
import static io.debezium.connector.mongodb.MongoDbConnectorConfig.MONGODB_POLL_INTERVAL_MS;
```

### Deprecation
'WRITE_BUFFER_HIGH_WATER_MARK' is deprecated
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/source/SimpleTcpSource.java`
#### Snippet
```java
        bootstrap.childOption(ChannelOption.SO_RCVBUF, receiveBufferSize);
        bootstrap.childOption(ChannelOption.SO_SNDBUF, sendBufferSize);
        bootstrap.childOption(ChannelOption.WRITE_BUFFER_HIGH_WATER_MARK, highWaterMark);

        bootstrap.channel(EventLoopUtil.getServerSocketChannelClass(workerGroup));
```

### Deprecation
'org.apache.commons.cli.GnuParser' is deprecated
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/node/Application.java`
#### Snippet
```java
            options.addOption(option);

            CommandLineParser parser = new GnuParser();
            CommandLine commandLine = parser.parse(options, args);

```

### Deprecation
'org.apache.flume.node.PollingPropertiesFileConfigurationProvider' is deprecated
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/node/Application.java`
#### Snippet
```java
            if (reload) {
                EventBus eventBus = new EventBus(agentName + "-event-bus");
                PollingPropertiesFileConfigurationProvider configurationProvider;
                configurationProvider = new PollingPropertiesFileConfigurationProvider(
                        agentName, configurationFile, eventBus, 30);
```

### Deprecation
'org.apache.flume.node.PollingPropertiesFileConfigurationProvider' is deprecated
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/node/Application.java`
#### Snippet
```java
                EventBus eventBus = new EventBus(agentName + "-event-bus");
                PollingPropertiesFileConfigurationProvider configurationProvider;
                configurationProvider = new PollingPropertiesFileConfigurationProvider(
                        agentName, configurationFile, eventBus, 30);
                components.add(configurationProvider);
```

### Deprecation
'org.apache.flume.node.PropertiesFileConfigurationProvider' is deprecated
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/node/Application.java`
#### Snippet
```java
                eventBus.register(application);
            } else {
                PropertiesFileConfigurationProvider configurationProvider;
                configurationProvider = new PropertiesFileConfigurationProvider(
                        agentName, configurationFile);
```

### Deprecation
'org.apache.flume.node.PropertiesFileConfigurationProvider' is deprecated
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/node/Application.java`
#### Snippet
```java
            } else {
                PropertiesFileConfigurationProvider configurationProvider;
                configurationProvider = new PropertiesFileConfigurationProvider(
                        agentName, configurationFile);
                application = new Application();
```

### Deprecation
'propagate(java.lang.Throwable)' is deprecated
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/node/Application.java`
#### Snippet
```java
                } catch (InterruptedException e) {
                    logger.error("Interrupted while waiting for channel to start.", e);
                    Throwables.propagate(e);
                }
            }
```

### Deprecation
'create(org.elasticsearch.action.admin.indices.create.CreateIndexRequest, org.elasticsearch.client.RequestOptions)' is deprecated
in `inlong-audit/audit-store/src/main/java/org/apache/inlong/audit/service/ElasticsearchService.java`
#### Snippet
```java
                .put("index.number_of_replicas", esConfig.getReplicaNum()));
        createIndexRequest.mapping("_doc", generateBuilder());
        CreateIndexResponse response = client.indices().create(createIndexRequest, RequestOptions.DEFAULT);
        boolean res = response.isAcknowledged();
        if (res) {
```

### Deprecation
'exists(org.elasticsearch.action.admin.indices.get.GetIndexRequest, org.elasticsearch.client.RequestOptions)' is deprecated
in `inlong-audit/audit-store/src/main/java/org/apache/inlong/audit/service/ElasticsearchService.java`
#### Snippet
```java
        GetIndexRequest getIndexRequest = new GetIndexRequest();
        getIndexRequest.indices(index);
        return client.indices().exists(getIndexRequest, RequestOptions.DEFAULT);
    }

```

### Deprecation
'getLevenshteinDistance(java.lang.CharSequence, java.lang.CharSequence)' is deprecated
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/utils/TStringUtils.java`
#### Snippet
```java

    public static int getLevenshteinDistance(String s, String t) {
        return StringUtils.getLevenshteinDistance(s, t);
    }

```

### Deprecation
'org.apache.inlong.tubemq.server.master.web.handler.WebAdminGroupCtrlHandler' is deprecated
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/action/screen/Webapi.java`
#### Snippet
```java
        registerHandler(new WebMasterInfoHandler(this.master));
        registerHandler(new WebOtherInfoHandler(this.master));
        registerHandler(new WebAdminGroupCtrlHandler(this.master));
        registerHandler(new WebAdminTopicAuthHandler(this.master));
        registerHandler(new WebAdminFlowRuleHandler(this.master));
```

### Deprecation
'org.apache.inlong.tubemq.server.master.web.handler.WebAdminTopicAuthHandler' is deprecated
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/action/screen/Webapi.java`
#### Snippet
```java
        registerHandler(new WebOtherInfoHandler(this.master));
        registerHandler(new WebAdminGroupCtrlHandler(this.master));
        registerHandler(new WebAdminTopicAuthHandler(this.master));
        registerHandler(new WebAdminFlowRuleHandler(this.master));
    }
```

### Deprecation
'org.apache.inlong.tubemq.server.master.web.handler.WebAdminFlowRuleHandler' is deprecated
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/action/screen/Webapi.java`
#### Snippet
```java
        registerHandler(new WebAdminGroupCtrlHandler(this.master));
        registerHandler(new WebAdminTopicAuthHandler(this.master));
        registerHandler(new WebAdminFlowRuleHandler(this.master));
    }

```

### Deprecation
'org.springframework.beans.factory.annotation.RequiredAnnotationBeanPostProcessor' is deprecated
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/simplemvc/RequestDispatcher.java`
#### Snippet
```java
                beanFactory.addBeanPostProcessor(commonProcessor);

                RequiredAnnotationBeanPostProcessor requiredProcessor =
                        new RequiredAnnotationBeanPostProcessor();
                requiredProcessor.setBeanFactory(beanFactory);
```

### Deprecation
'org.springframework.beans.factory.annotation.RequiredAnnotationBeanPostProcessor' is deprecated
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/simplemvc/RequestDispatcher.java`
#### Snippet
```java

                RequiredAnnotationBeanPostProcessor requiredProcessor =
                        new RequiredAnnotationBeanPostProcessor();
                requiredProcessor.setBeanFactory(beanFactory);
                beanFactory.addBeanPostProcessor(requiredProcessor);
```

### Deprecation
'COMPSCONSUMEGROUP' is deprecated
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebOtherInfoHandler.java`
#### Snippet
```java
        if (inGroupNameSet.isEmpty()) {
            if (!WebParameterUtils.getStringParamValue(req,
                    WebFieldDef.COMPSCONSUMEGROUP, false, null, sBuffer, result)) {
                WebParameterUtils.buildFailResult(sBuffer, result.getErrMsg());
                return sBuffer;
```

### Deprecation
'CONSUMEGROUP' is deprecated
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebOtherInfoHandler.java`
#### Snippet
```java
                WebFieldDef.GROUPNAME, true, null, sBuffer, result)) {
            if (!WebParameterUtils.getStringParamValue(req,
                    WebFieldDef.CONSUMEGROUP, true, null, sBuffer, result)) {
                WebParameterUtils.buildFailResult(sBuffer, result.getErrMsg());
                return sBuffer;
```

### Deprecation
'org.apache.flink.table.descriptors.DescriptorProperties' is deprecated
in `inlong-tubemq/tubemq-connectors/tubemq-connector-flink/src/main/java/org/apache/flink/connectors/tubemq/TubemqValidator.java`
#### Snippet
```java

    @Override
    public void validate(DescriptorProperties properties) {
        super.validate(properties);

```

### Deprecation
'defaultValue(T)' is deprecated
in `inlong-tubemq/tubemq-connectors/tubemq-connector-flink/src/main/java/org/apache/flink/connectors/tubemq/TubemqOptions.java`
#### Snippet
```java
    public static final ConfigOption<String> MESSAGE_NOT_FOUND_WAIT_PERIOD =
            ConfigOptions.key("message.not.found.wait.period")
                    .defaultValue("350ms")
                    .withDescription("The time of waiting period if tubemq broker return message not found.");
}
```

### Deprecation
'defaultValue(T)' is deprecated
in `inlong-tubemq/tubemq-connectors/tubemq-connector-flink/src/main/java/org/apache/flink/connectors/tubemq/TubemqOptions.java`
#### Snippet
```java
    public static final ConfigOption<Integer> MAX_RETRIES =
            ConfigOptions.key("max.retries")
                    .defaultValue(5)
                    .withDescription("The maximum number of retries when an "
                            + "exception is caught.");
```

### Deprecation
'noDefaultValue()' is deprecated
in `inlong-tubemq/tubemq-connectors/tubemq-connector-flink/src/main/java/org/apache/flink/connectors/tubemq/TubemqOptions.java`
#### Snippet
```java
    public static final ConfigOption<String> SESSION_KEY =
            ConfigOptions.key("session.key")
                    .noDefaultValue()
                    .withDescription("The session key for this consumer group at startup.");

```

### Deprecation
'noDefaultValue()' is deprecated
in `inlong-tubemq/tubemq-connectors/tubemq-connector-flink/src/main/java/org/apache/flink/connectors/tubemq/TubemqOptions.java`
#### Snippet
```java
    public static final ConfigOption<String> TID =
            ConfigOptions.key("topic.tid")
                    .noDefaultValue()
                    .withDescription("The tid owned this topic.");

```

### Deprecation
'defaultValue(T)' is deprecated
in `inlong-tubemq/tubemq-connectors/tubemq-connector-flink/src/main/java/org/apache/flink/connectors/tubemq/TubemqOptions.java`
#### Snippet
```java
    public static final ConfigOption<Boolean> BOOTSTRAP_FROM_MAX =
            ConfigOptions.key("bootstrap.from.max")
                    .defaultValue(false)
                    .withDescription("True if consuming from the most recent "
                            + "position when the tubemq source starts.. It only takes "
```

### Deprecation
'defaultValue(T)' is deprecated
in `inlong-tubemq/tubemq-connectors/tubemq-connector-flink/src/main/java/org/apache/flink/connectors/tubemq/TubemqOptions.java`
#### Snippet
```java
    public static final ConfigOption<String> SOURCE_MAX_IDLE_TIME =
            ConfigOptions.key("source.task.max.idle.time")
                    .defaultValue("5min")
                    .withDescription("The max time of the source marked as temporarily idle.");

```

### Deprecation
'org.apache.flink.table.descriptors.DescriptorProperties' is deprecated
in `inlong-tubemq/tubemq-connectors/tubemq-connector-flink/src/main/java/org/apache/flink/connectors/tubemq/Tubemq.java`
#### Snippet
```java
    @Override
    protected Map<String, String> toConnectorProperties() {
        DescriptorProperties descriptorProperties = new DescriptorProperties();

        if (topic != null) {
```

### Deprecation
'org.apache.flink.table.descriptors.DescriptorProperties' is deprecated
in `inlong-tubemq/tubemq-connectors/tubemq-connector-flink/src/main/java/org/apache/flink/connectors/tubemq/Tubemq.java`
#### Snippet
```java
    @Override
    protected Map<String, String> toConnectorProperties() {
        DescriptorProperties descriptorProperties = new DescriptorProperties();

        if (topic != null) {
```

### Deprecation
'org.apache.flink.table.api.TableSchema' is deprecated
in `inlong-tubemq/tubemq-connectors/tubemq-connector-flink/src/main/java/org/apache/flink/connectors/tubemq/TubemqTableSink.java`
#### Snippet
```java
     * The schema of the table.
     */
    private final TableSchema schema;

    /**
```

### Deprecation
'org.apache.flink.table.api.TableSchema' is deprecated
in `inlong-tubemq/tubemq-connectors/tubemq-connector-flink/src/main/java/org/apache/flink/connectors/tubemq/TubemqTableSink.java`
#### Snippet
```java
    public TubemqTableSink(
            SerializationSchema<Row> serializationSchema,
            TableSchema schema,
            String topic,
            String masterAddress,
```

### Deprecation
Overrides deprecated method in 'org.apache.flink.table.sinks.TableSink'
in `inlong-tubemq/tubemq-connectors/tubemq-connector-flink/src/main/java/org/apache/flink/connectors/tubemq/TubemqTableSink.java`
#### Snippet
```java

    @Override
    public String[] getFieldNames() {
        return schema.getFieldNames();
    }
```

### Deprecation
Overrides deprecated method in 'org.apache.flink.table.sinks.TableSink'
in `inlong-tubemq/tubemq-connectors/tubemq-connector-flink/src/main/java/org/apache/flink/connectors/tubemq/TubemqTableSink.java`
#### Snippet
```java

    @Override
    public TypeInformation<?>[] getFieldTypes() {
        return schema.getFieldTypes();
    }
```

### Deprecation
'getFieldTypes()' is deprecated
in `inlong-tubemq/tubemq-connectors/tubemq-connector-flink/src/main/java/org/apache/flink/connectors/tubemq/TubemqTableSink.java`
#### Snippet
```java
    @Override
    public TypeInformation<?>[] getFieldTypes() {
        return schema.getFieldTypes();
    }

```

### Deprecation
Overrides deprecated method in 'org.apache.flink.table.sinks.TableSink'
in `inlong-tubemq/tubemq-connectors/tubemq-connector-flink/src/main/java/org/apache/flink/connectors/tubemq/TubemqTableSink.java`
#### Snippet
```java

    @Override
    public TypeInformation<Row> getOutputType() {
        return schema.toRowType();
    }
```

### Deprecation
'toRowType()' is deprecated
in `inlong-tubemq/tubemq-connectors/tubemq-connector-flink/src/main/java/org/apache/flink/connectors/tubemq/TubemqTableSink.java`
#### Snippet
```java
    @Override
    public TypeInformation<Row> getOutputType() {
        return schema.toRowType();
    }

```

### Deprecation
'org.apache.flink.table.sinks.StreamTableSink' is deprecated
in `inlong-tubemq/tubemq-connectors/tubemq-connector-flink/src/main/java/org/apache/flink/connectors/tubemq/TubemqTableSink.java`
#### Snippet
```java

/**
 * Tubemq {@link org.apache.flink.table.sinks.StreamTableSink}.
 */
public class TubemqTableSink implements AppendStreamTableSink<Row> {
```

### Deprecation
'org.apache.flink.table.sinks.AppendStreamTableSink' is deprecated
in `inlong-tubemq/tubemq-connectors/tubemq-connector-flink/src/main/java/org/apache/flink/connectors/tubemq/TubemqTableSink.java`
#### Snippet
```java
 * Tubemq {@link org.apache.flink.table.sinks.StreamTableSink}.
 */
public class TubemqTableSink implements AppendStreamTableSink<Row> {

    /**
```

### Deprecation
Overrides deprecated method in 'org.apache.flink.table.sinks.TableSink'
in `inlong-tubemq/tubemq-connectors/tubemq-connector-flink/src/main/java/org/apache/flink/connectors/tubemq/TubemqTableSink.java`
#### Snippet
```java

    @Override
    public TubemqTableSink configure(String[] fieldNames, TypeInformation<?>[] fieldTypes) {
        if (!Arrays.equals(getFieldNames(), fieldNames)
                || !Arrays.equals(getFieldTypes(), fieldTypes)) {
```

### Deprecation
'org.apache.flink.table.sources.RowtimeAttributeDescriptor' is deprecated
in `inlong-tubemq/tubemq-connectors/tubemq-connector-flink/src/main/java/org/apache/flink/connectors/tubemq/TubemqTableSource.java`
#### Snippet
```java

    @Override
    public List<RowtimeAttributeDescriptor> getRowtimeAttributeDescriptors() {
        return rowtimeAttributeDescriptors;
    }
```

### Deprecation
'getFieldType(java.lang.String)' is deprecated
in `inlong-tubemq/tubemq-connectors/tubemq-connector-flink/src/main/java/org/apache/flink/connectors/tubemq/TubemqTableSource.java`
#### Snippet
```java
            Optional<String> proctimeAttribute) {
        return proctimeAttribute.map((attribute) -> {
            Optional<TypeInformation<?>> tpe = schema.getFieldType(attribute);
            if (!tpe.isPresent()) {
                throw new ValidationException("Proc time attribute '"
```

### Deprecation
'org.apache.flink.table.api.Types' is deprecated
in `inlong-tubemq/tubemq-connectors/tubemq-connector-flink/src/main/java/org/apache/flink/connectors/tubemq/TubemqTableSource.java`
#### Snippet
```java
                throw new ValidationException("Proc time attribute '"
                        + attribute + "' isn't present in TableSchema.");
            } else if (tpe.get() != Types.SQL_TIMESTAMP()) {
                throw new ValidationException("Proc time attribute '"
                        + attribute + "' isn't of type SQL_TIMESTAMP.");
```

### Deprecation
'org.apache.flink.table.api.TableSchema' is deprecated
in `inlong-tubemq/tubemq-connectors/tubemq-connector-flink/src/main/java/org/apache/flink/connectors/tubemq/TubemqTableSource.java`
#### Snippet
```java
    public TubemqTableSource(
            DeserializationSchema<Row> deserializationSchema,
            TableSchema schema,
            Optional<String> proctimeAttribute,
            List<RowtimeAttributeDescriptor> rowtimeAttributeDescriptors,
```

### Deprecation
'org.apache.flink.table.sources.RowtimeAttributeDescriptor' is deprecated
in `inlong-tubemq/tubemq-connectors/tubemq-connector-flink/src/main/java/org/apache/flink/connectors/tubemq/TubemqTableSource.java`
#### Snippet
```java
            TableSchema schema,
            Optional<String> proctimeAttribute,
            List<RowtimeAttributeDescriptor> rowtimeAttributeDescriptors,
            Map<String, String> fieldMapping,
            String masterAddress,
```

### Deprecation
'org.apache.flink.table.sources.RowtimeAttributeDescriptor' is deprecated
in `inlong-tubemq/tubemq-connectors/tubemq-connector-flink/src/main/java/org/apache/flink/connectors/tubemq/TubemqTableSource.java`
#### Snippet
```java
     * Descriptors for rowtime attributes.
     */
    private final List<RowtimeAttributeDescriptor> rowtimeAttributeDescriptors;

    /**
```

### Deprecation
'org.apache.flink.table.api.TableSchema' is deprecated
in `inlong-tubemq/tubemq-connectors/tubemq-connector-flink/src/main/java/org/apache/flink/connectors/tubemq/TubemqTableSource.java`
#### Snippet
```java
     * The schema of the table.
     */
    private final TableSchema schema;

    /**
```

### Deprecation
'org.apache.flink.table.api.TableSchema' is deprecated
in `inlong-tubemq/tubemq-connectors/tubemq-connector-flink/src/main/java/org/apache/flink/connectors/tubemq/TubemqTableSource.java`
#### Snippet
```java

    @Override
    public TableSchema getTableSchema() {
        return schema;
    }
```

### Deprecation
Overrides deprecated method in 'org.apache.flink.table.sources.TableSource'
in `inlong-tubemq/tubemq-connectors/tubemq-connector-flink/src/main/java/org/apache/flink/connectors/tubemq/TubemqTableSource.java`
#### Snippet
```java

    @Override
    public TableSchema getTableSchema() {
        return schema;
    }
```

### Deprecation
'org.apache.flink.table.sources.RowtimeAttributeDescriptor' is deprecated
in `inlong-tubemq/tubemq-connectors/tubemq-connector-flink/src/main/java/org/apache/flink/connectors/tubemq/TubemqTableSource.java`
#### Snippet
```java
    }

    private List<RowtimeAttributeDescriptor> validateRowTimeAttributeDescriptors(
            List<RowtimeAttributeDescriptor> attributeDescriptors) {
        checkNotNull(attributeDescriptors);
```

### Deprecation
'org.apache.flink.table.sources.RowtimeAttributeDescriptor' is deprecated
in `inlong-tubemq/tubemq-connectors/tubemq-connector-flink/src/main/java/org/apache/flink/connectors/tubemq/TubemqTableSource.java`
#### Snippet
```java

    private List<RowtimeAttributeDescriptor> validateRowTimeAttributeDescriptors(
            List<RowtimeAttributeDescriptor> attributeDescriptors) {
        checkNotNull(attributeDescriptors);

```

### Deprecation
'org.apache.flink.table.sources.RowtimeAttributeDescriptor' is deprecated
in `inlong-tubemq/tubemq-connectors/tubemq-connector-flink/src/main/java/org/apache/flink/connectors/tubemq/TubemqTableSource.java`
#### Snippet
```java
        checkNotNull(attributeDescriptors);

        for (RowtimeAttributeDescriptor desc : attributeDescriptors) {
            String name = desc.getAttributeName();
            Optional<TypeInformation<?>> tpe = schema.getFieldType(name);
```

### Deprecation
'getFieldType(java.lang.String)' is deprecated
in `inlong-tubemq/tubemq-connectors/tubemq-connector-flink/src/main/java/org/apache/flink/connectors/tubemq/TubemqTableSource.java`
#### Snippet
```java
        for (RowtimeAttributeDescriptor desc : attributeDescriptors) {
            String name = desc.getAttributeName();
            Optional<TypeInformation<?>> tpe = schema.getFieldType(name);
            if (!tpe.isPresent()) {
                throw new ValidationException("Row time attribute '"
```

### Deprecation
'org.apache.flink.table.api.Types' is deprecated
in `inlong-tubemq/tubemq-connectors/tubemq-connector-flink/src/main/java/org/apache/flink/connectors/tubemq/TubemqTableSource.java`
#### Snippet
```java
                throw new ValidationException("Row time attribute '"
                        + name + "' is not present.");
            } else if (tpe.get() != Types.SQL_TIMESTAMP()) {
                throw new ValidationException("Row time attribute '"
                        + name + "' is not of type SQL_TIMESTAMP.");
```

### Deprecation
'org.apache.flink.table.sources.StreamTableSource' is deprecated
in `inlong-tubemq/tubemq-connectors/tubemq-connector-flink/src/main/java/org/apache/flink/connectors/tubemq/TubemqTableSource.java`
#### Snippet
```java

/**
 * TubeMQ {@link StreamTableSource}.
 */
public class TubemqTableSource
```

### Deprecation
'org.apache.flink.table.sources.StreamTableSource' is deprecated
in `inlong-tubemq/tubemq-connectors/tubemq-connector-flink/src/main/java/org/apache/flink/connectors/tubemq/TubemqTableSource.java`
#### Snippet
```java
public class TubemqTableSource
        implements
            StreamTableSource<Row>,
            DefinedProctimeAttribute,
            DefinedRowtimeAttributes,
```

### Deprecation
'org.apache.flink.table.sources.DefinedProctimeAttribute' is deprecated
in `inlong-tubemq/tubemq-connectors/tubemq-connector-flink/src/main/java/org/apache/flink/connectors/tubemq/TubemqTableSource.java`
#### Snippet
```java
        implements
            StreamTableSource<Row>,
            DefinedProctimeAttribute,
            DefinedRowtimeAttributes,
            DefinedFieldMapping {
```

### Deprecation
'org.apache.flink.table.sources.DefinedRowtimeAttributes' is deprecated
in `inlong-tubemq/tubemq-connectors/tubemq-connector-flink/src/main/java/org/apache/flink/connectors/tubemq/TubemqTableSource.java`
#### Snippet
```java
            StreamTableSource<Row>,
            DefinedProctimeAttribute,
            DefinedRowtimeAttributes,
            DefinedFieldMapping {

```

### Deprecation
'org.apache.flink.table.sources.DefinedFieldMapping' is deprecated
in `inlong-tubemq/tubemq-connectors/tubemq-connector-flink/src/main/java/org/apache/flink/connectors/tubemq/TubemqTableSource.java`
#### Snippet
```java
            DefinedProctimeAttribute,
            DefinedRowtimeAttributes,
            DefinedFieldMapping {

    /**
```

### Deprecation
'SCHEMA_TYPE' is deprecated
in `inlong-tubemq/tubemq-connectors/tubemq-connector-flink/src/main/java/org/apache/flink/connectors/tubemq/TubemqTableSourceSinkFactory.java`
#### Snippet
```java
import static org.apache.flink.table.descriptors.Schema.SCHEMA_NAME;
import static org.apache.flink.table.descriptors.Schema.SCHEMA_PROCTIME;
import static org.apache.flink.table.descriptors.Schema.SCHEMA_TYPE;
import static org.apache.flink.table.descriptors.StreamTableDescriptorValidator.UPDATE_MODE;
import static org.apache.flink.table.descriptors.StreamTableDescriptorValidator.UPDATE_MODE_VALUE_APPEND;
```

### Deprecation
'org.apache.flink.table.descriptors.DescriptorProperties' is deprecated
in `inlong-tubemq/tubemq-connectors/tubemq-connector-flink/src/main/java/org/apache/flink/connectors/tubemq/TubemqTableSourceSinkFactory.java`
#### Snippet
```java
    }

    private void validateProperties(DescriptorProperties descriptorProperties) {
        new SchemaValidator(true, false, false).validate(descriptorProperties);
        new TubemqValidator().validate(descriptorProperties);
```

### Deprecation
'SCHEMA_TYPE' is deprecated
in `inlong-tubemq/tubemq-connectors/tubemq-connector-flink/src/main/java/org/apache/flink/connectors/tubemq/TubemqTableSourceSinkFactory.java`
#### Snippet
```java

        // schema
        properties.add(SCHEMA + ".#." + SCHEMA_TYPE);
        properties.add(SCHEMA + ".#." + SCHEMA_NAME);
        properties.add(SCHEMA + ".#." + SCHEMA_FROM);
```

### Deprecation
'org.apache.flink.table.factories.StreamTableSourceFactory' is deprecated
in `inlong-tubemq/tubemq-connectors/tubemq-connector-flink/src/main/java/org/apache/flink/connectors/tubemq/TubemqTableSourceSinkFactory.java`
#### Snippet
```java
public class TubemqTableSourceSinkFactory
        implements
            StreamTableSourceFactory<Row>,
            StreamTableSinkFactory<Row> {

```

### Deprecation
'org.apache.flink.table.factories.StreamTableSinkFactory' is deprecated
in `inlong-tubemq/tubemq-connectors/tubemq-connector-flink/src/main/java/org/apache/flink/connectors/tubemq/TubemqTableSourceSinkFactory.java`
#### Snippet
```java
        implements
            StreamTableSourceFactory<Row>,
            StreamTableSinkFactory<Row> {

    private static final String SPLIT_COMMA = ",";
```

### Deprecation
'org.apache.flink.table.descriptors.DescriptorProperties' is deprecated
in `inlong-tubemq/tubemq-connectors/tubemq-connector-flink/src/main/java/org/apache/flink/connectors/tubemq/TubemqTableSourceSinkFactory.java`
#### Snippet
```java

    private Configuration getConfiguration(
            DescriptorProperties descriptorProperties) {
        Map<String, String> properties =
                descriptorProperties.getPropertiesWithPrefix(TubemqValidator.CONNECTOR_PROPERTIES);
```

### Deprecation
'org.apache.flink.table.sinks.StreamTableSink' is deprecated
in `inlong-tubemq/tubemq-connectors/tubemq-connector-flink/src/main/java/org/apache/flink/connectors/tubemq/TubemqTableSourceSinkFactory.java`
#### Snippet
```java

    @Override
    public StreamTableSink<Row> createStreamTableSink(
            Map<String, String> properties) {
        final SerializationSchema<Row> serializationSchema =
```

### Deprecation
Overrides deprecated method in 'org.apache.flink.table.factories.StreamTableSinkFactory'
in `inlong-tubemq/tubemq-connectors/tubemq-connector-flink/src/main/java/org/apache/flink/connectors/tubemq/TubemqTableSourceSinkFactory.java`
#### Snippet
```java

    @Override
    public StreamTableSink<Row> createStreamTableSink(
            Map<String, String> properties) {
        final SerializationSchema<Row> serializationSchema =
```

### Deprecation
'org.apache.flink.table.descriptors.DescriptorProperties' is deprecated
in `inlong-tubemq/tubemq-connectors/tubemq-connector-flink/src/main/java/org/apache/flink/connectors/tubemq/TubemqTableSourceSinkFactory.java`
#### Snippet
```java
                getSerializationSchema(properties);

        final DescriptorProperties descriptorProperties =
                new DescriptorProperties(true);
        descriptorProperties.putProperties(properties);
```

### Deprecation
'org.apache.flink.table.descriptors.DescriptorProperties' is deprecated
in `inlong-tubemq/tubemq-connectors/tubemq-connector-flink/src/main/java/org/apache/flink/connectors/tubemq/TubemqTableSourceSinkFactory.java`
#### Snippet
```java

        final DescriptorProperties descriptorProperties =
                new DescriptorProperties(true);
        descriptorProperties.putProperties(properties);

```

### Deprecation
'org.apache.flink.table.api.TableSchema' is deprecated
in `inlong-tubemq/tubemq-connectors/tubemq-connector-flink/src/main/java/org/apache/flink/connectors/tubemq/TubemqTableSourceSinkFactory.java`
#### Snippet
```java
        validateProperties(descriptorProperties);

        final TableSchema tableSchema =
                descriptorProperties.getTableSchema(SCHEMA);
        final String topic =
```

### Deprecation
'org.apache.flink.table.sources.StreamTableSource' is deprecated
in `inlong-tubemq/tubemq-connectors/tubemq-connector-flink/src/main/java/org/apache/flink/connectors/tubemq/TubemqTableSourceSinkFactory.java`
#### Snippet
```java

    @Override
    public StreamTableSource<Row> createStreamTableSource(
            Map<String, String> properties) {
        final DeserializationSchema<Row> deserializationSchema =
```

### Deprecation
Overrides deprecated method in 'org.apache.flink.table.factories.StreamTableSourceFactory'
in `inlong-tubemq/tubemq-connectors/tubemq-connector-flink/src/main/java/org/apache/flink/connectors/tubemq/TubemqTableSourceSinkFactory.java`
#### Snippet
```java

    @Override
    public StreamTableSource<Row> createStreamTableSource(
            Map<String, String> properties) {
        final DeserializationSchema<Row> deserializationSchema =
```

### Deprecation
'org.apache.flink.table.descriptors.DescriptorProperties' is deprecated
in `inlong-tubemq/tubemq-connectors/tubemq-connector-flink/src/main/java/org/apache/flink/connectors/tubemq/TubemqTableSourceSinkFactory.java`
#### Snippet
```java
                getDeserializationSchema(properties);

        final DescriptorProperties descriptorProperties =
                new DescriptorProperties(true);
        descriptorProperties.putProperties(properties);
```

### Deprecation
'org.apache.flink.table.descriptors.DescriptorProperties' is deprecated
in `inlong-tubemq/tubemq-connectors/tubemq-connector-flink/src/main/java/org/apache/flink/connectors/tubemq/TubemqTableSourceSinkFactory.java`
#### Snippet
```java

        final DescriptorProperties descriptorProperties =
                new DescriptorProperties(true);
        descriptorProperties.putProperties(properties);

```

### Deprecation
'org.apache.flink.table.api.TableSchema' is deprecated
in `inlong-tubemq/tubemq-connectors/tubemq-connector-flink/src/main/java/org/apache/flink/connectors/tubemq/TubemqTableSourceSinkFactory.java`
#### Snippet
```java
        validateProperties(descriptorProperties);

        final TableSchema schema =
                descriptorProperties.getTableSchema(SCHEMA);
        final Optional<String> proctimeAttribute =
```

### Deprecation
'org.apache.flink.table.sources.RowtimeAttributeDescriptor' is deprecated
in `inlong-tubemq/tubemq-connectors/tubemq-connector-flink/src/main/java/org/apache/flink/connectors/tubemq/TubemqTableSourceSinkFactory.java`
#### Snippet
```java
        final Optional<String> proctimeAttribute =
                SchemaValidator.deriveProctimeAttribute(descriptorProperties);
        final List<RowtimeAttributeDescriptor> rowtimeAttributeDescriptors =
                SchemaValidator.deriveRowtimeAttributes(descriptorProperties);
        final Map<String, String> fieldMapping =
```

### Deprecation
'setCellType(org.apache.poi.ss.usermodel.CellType)' is deprecated
in `inlong-manager/manager-common/src/main/java/org/apache/inlong/manager/common/tool/excel/ExcelTool.java`
#### Snippet
```java
        String cellValue;
        if (cell != null) {
            cell.setCellType(CellType.STRING);
            cellValue = cell.getStringCellValue();
            if (!StringUtils.isEmpty(cellValue)) {
```

### Deprecation
'setClusterId(java.lang.Integer)' is deprecated
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/cluster/InlongClusterServiceImpl.java`
#### Snippet
```java

        // all cluster nodes belong to the same clusterId
        response.setClusterId(nodeEntities.get(0).getParentId());

        // TODO consider the data proxy load and re-balance
```

### Deprecation
'exists(org.elasticsearch.action.admin.indices.get.GetIndexRequest, org.elasticsearch.client.RequestOptions)' is deprecated
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sink/es/ElasticsearchApi.java`
#### Snippet
```java
        GetIndexRequest getIndexRequest = new GetIndexRequest();
        getIndexRequest.indices(indexName);
        return getEsClient().indices().exists(getIndexRequest, RequestOptions.DEFAULT);
    }

```

### Deprecation
'METASTOREURIS' is deprecated
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sink/hudi/HudiCatalogClient.java`
#### Snippet
```java
        this.warehouse = warehouse;
        hiveConf = new HiveConf();
        hiveConf.setVar(HiveConf.ConfVars.METASTOREURIS, uri);
        hiveConf.setBoolVar(HiveConf.ConfVars.METASTORE_EXECUTE_SET_UGI, false);
    }
```

### Deprecation
'METASTORE_EXECUTE_SET_UGI' is deprecated
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sink/hudi/HudiCatalogClient.java`
#### Snippet
```java
        hiveConf = new HiveConf();
        hiveConf.setVar(HiveConf.ConfVars.METASTOREURIS, uri);
        hiveConf.setBoolVar(HiveConf.ConfVars.METASTORE_EXECUTE_SET_UGI, false);
    }

```

### Deprecation
'METASTOREURIS' is deprecated
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sink/hudi/HudiCatalogClient.java`
#### Snippet
```java
        this.dbName = dbName;
        hiveConf = new HiveConf();
        hiveConf.setVar(HiveConf.ConfVars.METASTOREURIS, uri);
        hiveConf.setBoolVar(HiveConf.ConfVars.METASTORE_EXECUTE_SET_UGI, false);
    }
```

### Deprecation
'METASTORE_EXECUTE_SET_UGI' is deprecated
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sink/hudi/HudiCatalogClient.java`
#### Snippet
```java
        hiveConf = new HiveConf();
        hiveConf.setVar(HiveConf.ConfVars.METASTOREURIS, uri);
        hiveConf.setBoolVar(HiveConf.ConfVars.METASTORE_EXECUTE_SET_UGI, false);
    }

```

### Deprecation
'SSL_ENABLED' is deprecated
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/http/SimpleHttpSource.java`
#### Snippet
```java

            // SSL related config
            sslEnabled = context.getBoolean(HTTPSourceConfigurationConstants.SSL_ENABLED, false);
            if (sslEnabled) {
                LOG.debug("SSL configuration enabled");
```

### Deprecation
'SSL_KEYSTORE' is deprecated
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/http/SimpleHttpSource.java`
#### Snippet
```java
            if (sslEnabled) {
                LOG.debug("SSL configuration enabled");
                keyStorePath = context.getString(HTTPSourceConfigurationConstants.SSL_KEYSTORE);
                Preconditions.checkArgument(keyStorePath != null && !keyStorePath.isEmpty(),
                        "Keystore is required for SSL Conifguration");
```

### Deprecation
'SSL_KEYSTORE_PASSWORD' is deprecated
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/http/SimpleHttpSource.java`
#### Snippet
```java
                        "Keystore is required for SSL Conifguration");
                keyStorePassword =
                        context.getString(HTTPSourceConfigurationConstants.SSL_KEYSTORE_PASSWORD);
                Preconditions.checkArgument(keyStorePassword != null,
                        "Keystore password is required for SSL Configuration");
```

### Deprecation
'propagate(java.lang.Throwable)' is deprecated
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/http/SimpleHttpSource.java`
#### Snippet
```java
        } catch (Exception ex) {
            LOG.error("Error configuring HTTPSource!", ex);
            Throwables.propagate(ex);
        }
    }
```

### Deprecation
'SslContextFactory(java.lang.String)' is deprecated
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/http/SimpleHttpSource.java`
#### Snippet
```java
            Connector[] connectors = new Connector[1];
            if (sslEnabled) {
                SslContextFactory sslContextFactory = new SslContextFactory(keyStorePath);
                sslContextFactory.setKeyStorePassword(keyStorePassword);
                SslConnectionFactory ssl = new SslConnectionFactory(sslContextFactory,
```

### Deprecation
'propagate(java.lang.Throwable)' is deprecated
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/http/SimpleHttpSource.java`
#### Snippet
```java
        } catch (ClassNotFoundException ex) {
            LOG.error("Error while configuring HTTPSource. Exception follows.", ex);
            Throwables.propagate(ex);
        } catch (ClassCastException ex) {
            LOG.error("Deserializer is not an instance of HTTPSourceHandler."
```

### Deprecation
'propagate(java.lang.Throwable)' is deprecated
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/http/SimpleHttpSource.java`
#### Snippet
```java
            LOG.error("Deserializer is not an instance of HTTPSourceHandler."
                    + "Deserializer must implement HTTPSourceHandler.");
            Throwables.propagate(ex);
        } catch (Exception ex) {
            LOG.error("Error while starting HTTPSource. Exception follows.", ex);
```

### Deprecation
'propagate(java.lang.Throwable)' is deprecated
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/http/SimpleHttpSource.java`
#### Snippet
```java
        } catch (Exception ex) {
            LOG.error("Error while starting HTTPSource. Exception follows.", ex);
            Throwables.propagate(ex);
        }
        Preconditions.checkArgument(srv.isRunning());
```

### Deprecation
'org.apache.commons.cli.GnuParser' is deprecated
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/node/Application.java`
#### Snippet
```java
            options.addOption(option);

            CommandLineParser parser = new GnuParser();
            CommandLine commandLine = parser.parse(options, args);

```

### Deprecation
'org.apache.flume.node.PollingPropertiesFileConfigurationProvider' is deprecated
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/node/Application.java`
#### Snippet
```java
                if (reload) {
                    EventBus eventBus = new EventBus(agentName + "-event-bus");
                    PollingPropertiesFileConfigurationProvider configurationProvider;
                    configurationProvider = new PollingPropertiesFileConfigurationProvider(
                            agentName, configurationFile, eventBus, 30);
```

### Deprecation
'org.apache.flume.node.PollingPropertiesFileConfigurationProvider' is deprecated
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/node/Application.java`
#### Snippet
```java
                    EventBus eventBus = new EventBus(agentName + "-event-bus");
                    PollingPropertiesFileConfigurationProvider configurationProvider;
                    configurationProvider = new PollingPropertiesFileConfigurationProvider(
                            agentName, configurationFile, eventBus, 30);
                    components.add(configurationProvider);
```

### Deprecation
'org.apache.flume.node.PropertiesFileConfigurationProvider' is deprecated
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/node/Application.java`
#### Snippet
```java
                    eventBus.register(application);
                } else {
                    PropertiesFileConfigurationProvider configurationProvider;
                    configurationProvider = new PropertiesFileConfigurationProvider(agentName, configurationFile);
                    application = new Application();
```

### Deprecation
'org.apache.flume.node.PropertiesFileConfigurationProvider' is deprecated
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/node/Application.java`
#### Snippet
```java
                } else {
                    PropertiesFileConfigurationProvider configurationProvider;
                    configurationProvider = new PropertiesFileConfigurationProvider(agentName, configurationFile);
                    application = new Application();
                    application.handleConfigurationEvent(configurationProvider.getConfiguration());
```

### Deprecation
'propagate(java.lang.Throwable)' is deprecated
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/node/Application.java`
#### Snippet
```java
                } catch (InterruptedException e) {
                    LOGGER.error("interrupted while waiting for channel to start: ", e);
                    Throwables.propagate(e);
                }
            }
```

### Deprecation
'equals(java.lang.Object, java.lang.Object)' is deprecated
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/config/ConfigManager.java`
#### Snippet
```java
            if (addElseRemove) {
                String oldValue = tmpHolder.put(entry.getKey(), entry.getValue());
                if (!ObjectUtils.equals(oldValue, entry.getValue())) {
                    changed = true;
                }
```

### Deprecation
'WRITE_BUFFER_HIGH_WATER_MARK' is deprecated
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/SimpleTcpSource.java`
#### Snippet
```java
        bootstrap.childOption(ChannelOption.SO_SNDBUF, sendBufferSize);
        // serverBootstrap.childOption("child.trafficClass", trafficClass);
        bootstrap.childOption(ChannelOption.WRITE_BUFFER_HIGH_WATER_MARK, highWaterMark);
        bootstrap.channel(EventLoopUtil.getServerSocketChannelClass(workerGroup));
        EventLoopUtil.enableTriggeredMode(bootstrap);
```

### Deprecation
'WRITE_BUFFER_HIGH_WATER_MARK' is deprecated
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source2/SimpleTcpSource.java`
#### Snippet
```java
        bootstrap.childOption(ChannelOption.SO_RCVBUF, maxRcvBufferSize);
        bootstrap.childOption(ChannelOption.SO_SNDBUF, maxSendBufferSize);
        bootstrap.childOption(ChannelOption.WRITE_BUFFER_HIGH_WATER_MARK, highWaterMark);
        bootstrap.channel(EventLoopUtil.getServerSocketChannelClass(workerGroup));
        EventLoopUtil.enableTriggeredMode(bootstrap);
```

## RuleId[id=FinalStaticMethod]
### FinalStaticMethod
'static' method declared `final`
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/utils/CheckSum.java`
#### Snippet
```java
    }

    public static final int crc32(byte[] array, int offset, int length) {
        CRC32 crc32 = new CRC32();
        crc32.update(array, offset, length);
```

### FinalStaticMethod
'static' method declared `final`
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/utils/CheckSum.java`
#### Snippet
```java
public class CheckSum {

    public static final int crc32(byte[] array) {
        return crc32(array, 0, array.length);
    }
```

### FinalStaticMethod
'static' method declared `final`
in `inlong-manager/manager-pojo/src/main/java/org/apache/inlong/manager/pojo/sink/kudu/KuduType.java`
#### Snippet
```java
     * Get type by Kudu type name
     */
    public static final KuduType forKuduType(String kuduType) {
        for (KuduType type : values()) {
            if (type.getKuduType() == kuduType) {
```

## RuleId[id=TrivialStringConcatenation]
### TrivialStringConcatenation
Empty string used in concatenation
in `inlong-sdk/sort-sdk/src/main/java/org/apache/inlong/sdk/sort/util/Utils.java`
#### Snippet
```java
        bytes[2] = (byte) ((ipInt >>> 8) & 0xFF);
        bytes[3] = (byte) (ipInt & 0xFF);
        return "" + (bytes[0] & 0x00FF) + '.' + (bytes[1] & 0x00FF) + '.' + (bytes[2] & 0x00FF)
                + '.' + (bytes[3] & 0x00FF);
    }
```

### TrivialStringConcatenation
Empty string used in concatenation
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kudu/src/main/java/org/apache/inlong/sort/kudu/sink/KuduWriter.java`
#### Snippet
```java
                    Long time = consumeTime.f0;
                    Integer count = consumeTime.f1;
                    detailTimes.add(time + "");
                    detailNums.add(count + "");
                    summaryTime += time;
```

### TrivialStringConcatenation
Empty string used in concatenation
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kudu/src/main/java/org/apache/inlong/sort/kudu/sink/KuduWriter.java`
#### Snippet
```java
                    Integer count = consumeTime.f1;
                    detailTimes.add(time + "");
                    detailNums.add(count + "");
                    summaryTime += time;
                    summaryNum += count;
```

### TrivialStringConcatenation
Empty string used in concatenation
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/utils/PluginUtils.java`
#### Snippet
```java
        if (jobConf.hasKey(JOB_RETRY_TIME)) {
            LOGGER.info("job {} is retry job with specific time, update file time to {}"
                    + "", jobConf.toJsonStr(), jobConf.get(JOB_RETRY_TIME));
            patterns.forEach(pattern -> pattern.updateDateFormatRegex(jobConf.get(JOB_RETRY_TIME)));
        }
```

### TrivialStringConcatenation
Empty string used in concatenation
in `inlong-common/src/main/java/org/apache/inlong/common/util/MaskDataUtils.java`
#### Snippet
```java
     */
    private static int mask(StringBuilder builder, char maskChar, int startPos, int endPos) {
        final String masked = "" + maskChar + maskChar + maskChar + maskChar + maskChar + maskChar;
        builder.replace(startPos, endPos, masked);
        return startPos + 6;
```

### TrivialStringConcatenation
Empty string used in concatenation
in `inlong-manager/manager-common/src/main/java/org/apache/inlong/manager/common/util/SHAUtils.java`
#### Snippet
```java
            retString = sb.toString();
        } catch (NoSuchAlgorithmException e) {
            log.error("" + e);
        }

```

## RuleId[id=RedundantTypeArguments]
### RedundantTypeArguments
Explicit type arguments can be inferred
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/configuration/ConfigOption.java`
#### Snippet
```java
    @Deprecated
    public Iterable<String> deprecatedKeys() {
        return fallbackKeys == EMPTY ? Collections.<String>emptyList()
                : Arrays.stream(fallbackKeys)
                        .filter(FallbackKey::isDeprecated)
```

### RedundantTypeArguments
Explicit type arguments can be inferred
in `inlong-sort/sort-formats/format-json/src/main/java/org/apache/inlong/sort/formats/json/canal/CanalJsonDecodingFormat.java`
#### Snippet
```java
                                        .filter(rm -> rm.key.equals(k))
                                        .findFirst()
                                        .<IllegalStateException>orElseThrow(IllegalStateException::new))
                        .collect(Collectors.toList());
        final List<DataTypes.Field> metadataFields =
```

### RedundantTypeArguments
Explicit type arguments can be inferred
in `inlong-sort/sort-formats/format-json/src/main/java/org/apache/inlong/sort/formats/json/debezium/DebeziumJsonDecodingFormat.java`
#### Snippet
```java
                                        .filter(rm -> rm.key.equals(k))
                                        .findFirst()
                                        .<IllegalStateException>orElseThrow(IllegalStateException::new))
                        .collect(Collectors.toList());

```

## RuleId[id=NullableProblems]
### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/base/src/main/java/org/apache/inlong/sort/base/util/concurrent/ExecutorThreadFactory.java`
#### Snippet
```java
    private final String namePrefix;
    private final int threadPriority;
    @Nullable
    private final UncaughtExceptionHandler exceptionHandler;

```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `inlong-sort/sort-flink/base/src/main/java/org/apache/inlong/sort/base/util/concurrent/ExecutorThreadFactory.java`
#### Snippet
```java
    }

    public Thread newThread(Runnable runnable) {
        Thread t = new Thread(this.group, runnable, this.namePrefix + this.threadNumber.getAndIncrement());
        t.setDaemon(true);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `inlong-sort/sort-flink/base/src/main/java/org/apache/inlong/sort/base/util/concurrent/ThreadUtils.java`
#### Snippet
```java
            private final AtomicLong threadEpoch = new AtomicLong(0L);

            public Thread newThread(Runnable r) {
                String threadName;
                if (pattern.contains("%d")) {
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/base/src/main/java/org/apache/inlong/sort/base/dirty/DirtyData.java`
#### Snippet
```java
     * The row type of data, it is only used for 'RowData'
     */
    private @Nullable final LogicalType rowType;
    /**
     * The real dirty data
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/base/src/main/java/org/apache/inlong/sort/base/dirty/DirtySinkHelper.java`
#### Snippet
```java
    private static final DateTimeFormatter DATE_TIME_FORMAT = DateTimeFormatter.ofPattern("yyyy-MM-dd HH:mm:ss");
    private DirtyOptions dirtyOptions;
    private final @Nullable DirtySink<T> dirtySink;

    public DirtySinkHelper(DirtyOptions dirtyOptions, @Nullable DirtySink<T> dirtySink) {
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/source/meta/split/SnapshotSplitState.java`
#### Snippet
```java
public class SnapshotSplitState extends SourceSplitState {

    @Nullable
    private Offset highWatermark;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/source/meta/split/StreamSplit.java`
#### Snippet
```java
    private final Map<TableId, TableChange> tableSchemas;
    private final int totalFinishedSplitSize;
    @Nullable
    transient byte[] serializedFormCache;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/source/meta/split/ChangeEventRecords.java`
#### Snippet
```java
    @Nullable
    private String splitId;
    @Nullable
    private Iterator<SourceRecords> recordsForCurrentSplit;
    @Nullable
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/source/meta/split/ChangeEventRecords.java`
#### Snippet
```java
    @Nullable
    private Iterator<SourceRecords> recordsForCurrentSplit;
    @Nullable
    private final Iterator<SourceRecords> recordsForSplit;
    private final Set<String> finishedSnapshotSplits;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/source/meta/split/ChangeEventRecords.java`
#### Snippet
```java
public final class ChangeEventRecords implements RecordsWithSplitIds<SourceRecords> {

    @Nullable
    private String splitId;
    @Nullable
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/source/meta/split/StreamSplitState.java`
#### Snippet
```java
    @Nullable
    private Offset startingOffset;
    @Nullable
    private Offset endingOffset;
    private final Map<TableId, TableChange> tableSchemas;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/source/meta/split/StreamSplitState.java`
#### Snippet
```java
public class StreamSplitState extends SourceSplitState {

    @Nullable
    private Offset startingOffset;
    @Nullable
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/source/meta/split/SnapshotSplit.java`
#### Snippet
```java
    private final Object[] splitEnd;
    /** The high watermark is not null when the split read finished. */
    @Nullable
    private final Offset highWatermark;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/source/meta/split/SnapshotSplit.java`
#### Snippet
```java
    @Nullable
    private final Object[] splitStart;
    @Nullable
    private final Object[] splitEnd;
    /** The high watermark is not null when the split read finished. */
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/source/meta/split/SnapshotSplit.java`
#### Snippet
```java
    private final Offset highWatermark;

    @Nullable
    transient byte[] serializedFormCache;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/source/meta/split/SnapshotSplit.java`
#### Snippet
```java
    private final Map<TableId, TableChange> tableSchemas;

    @Nullable
    private final Object[] splitStart;
    @Nullable
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/source/reader/IncrementalSourceSplitReader.java`
#### Snippet
```java
    @Nullable
    private Fetcher<SourceRecords, SourceSplitBase> currentFetcher;
    @Nullable
    private String currentSplitId;
    private final DataSourceDialect<C> dataSourceDialect;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/source/reader/IncrementalSourceSplitReader.java`
#### Snippet
```java
    private final int subtaskId;

    @Nullable
    private Fetcher<SourceRecords, SourceSplitBase> currentFetcher;
    @Nullable
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/source/assigner/state/PendingSplitsState.java`
#### Snippet
```java
     * repeated serialization cheap. This field is used by {@link PendingSplitsStateSerializer}.
     */
    @Nullable
    transient byte[] serializedFormCache;
}
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/source/assigner/SnapshotSplitAssigner.java`
#### Snippet
```java
    private boolean isTableIdCaseSensitive;

    @Nullable
    private Long checkpointIdToFinish;
    private final DataSourceDialect<C> dialect;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/source/assigner/splitter/ChunkRange.java`
#### Snippet
```java
public class ChunkRange {

    private final @Nullable Object chunkStart;
    private final @Nullable Object chunkEnd;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/source/assigner/splitter/ChunkRange.java`
#### Snippet
```java

    private final @Nullable Object chunkStart;
    private final @Nullable Object chunkEnd;

    /**
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/debezium/internal/SchemaRecord.java`
#### Snippet
```java
    private final HistoryRecord historyRecord;

    @Nullable
    private final Document tableChangeDoc;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/debezium/internal/SchemaRecord.java`
#### Snippet
```java
public class SchemaRecord {

    @Nullable
    private final HistoryRecord historyRecord;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/hive/src/main/java/org/apache/inlong/sort/hive/filesystem/AbstractStreamingWriter.java`
#### Snippet
```java
    private transient long currentWatermark;

    @Nullable
    private transient SinkMetricData metricData;
    private transient ListState<MetricState> metricStateListState;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/hive/src/main/java/org/apache/inlong/sort/hive/filesystem/AbstractStreamingWriter.java`
#### Snippet
```java
    private @Nullable final String auditHostAndPorts;
    private final DirtyOptions dirtyOptions;
    private @Nullable final DirtySink<Object> dirtySink;

    // --------------------------- runtime fields -----------------------------
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/hive/src/main/java/org/apache/inlong/sort/hive/filesystem/AbstractStreamingWriter.java`
#### Snippet
```java

    private @Nullable final String inlongMetric;
    private @Nullable final String auditHostAndPorts;
    private final DirtyOptions dirtyOptions;
    private @Nullable final DirtySink<Object> dirtySink;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/hive/src/main/java/org/apache/inlong/sort/hive/filesystem/AbstractStreamingWriter.java`
#### Snippet
```java
    private final StreamingFileSink.BucketsBuilder<IN, String, ? extends StreamingFileSink.BucketsBuilder<IN, String, ?>> bucketsBuilder;

    private @Nullable final String inlongMetric;
    private @Nullable final String auditHostAndPorts;
    private final DirtyOptions dirtyOptions;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/jdbc/src/main/java/org/apache/inlong/sort/jdbc/table/JdbcDynamicTableSink.java`
#### Snippet
```java

    private final DirtyOptions dirtyOptions;
    private @Nullable final DirtySink<Object> dirtySink;

    public JdbcDynamicTableSink(
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/hive/src/main/java/org/apache/inlong/sort/hive/HiveTableSink.java`
#### Snippet
```java
    private final String hiveVersion;
    private final HiveShim hiveShim;
    @Nullable
    private final Integer configuredParallelism;
    private LinkedHashMap<String, String> staticPartitionSpec = new LinkedHashMap<>();
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/hive/src/main/java/org/apache/inlong/sort/hive/HiveTableSink.java`
#### Snippet
```java
    private final String auditHostAndPorts;
    private final DirtyOptions dirtyOptions;
    private @Nullable final DirtySink<Object> dirtySink;

    public HiveTableSink(
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kudu/src/main/java/org/apache/inlong/sort/kudu/common/Kudu.java`
#### Snippet
```java
    @Nonnull
    private final Map<String, String> properties;
    @Nonnull
    private String table;
    @Nonnull
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kudu/src/main/java/org/apache/inlong/sort/kudu/common/Kudu.java`
#### Snippet
```java
public class Kudu extends ConnectorDescriptor {

    @Nonnull
    private final Map<String, String> properties;
    @Nonnull
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kudu/src/main/java/org/apache/inlong/sort/kudu/common/Kudu.java`
#### Snippet
```java
    @Nonnull
    private String table;
    @Nonnull
    private String masters;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kudu/src/main/java/org/apache/inlong/sort/kudu/table/KuduDynamicTableSink.java`
#### Snippet
```java
     * The names of the key fields of the upsert stream consumed by this sink.
     */
    @Nullable
    private String[] keyFieldNames;
    private final boolean ignoreAllChangeLog;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/doris/src/main/java/org/apache/inlong/sort/doris/table/DorisDynamicTableSink.java`
#### Snippet
```java
    private final Integer parallelism;
    private final DirtyOptions dirtyOptions;
    private @Nullable final DirtySink<Object> dirtySink;

    public DorisDynamicTableSink(DorisOptions options,
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/jdbc/src/main/java/org/apache/inlong/sort/jdbc/internal/JdbcBatchingOutputFormat.java`
#### Snippet
```java

    private final DirtyOptions dirtyOptions;
    private @Nullable final DirtySink<Object> dirtySink;

    public JdbcBatchingOutputFormat(
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/hbase/src/main/java/org/apache/inlong/sort/hbase/sink/HBaseDynamicTableSink.java`
#### Snippet
```java
    private final String inlongAudit;
    private final DirtyOptions dirtyOptions;
    private @Nullable final DirtySink<Object> dirtySink;

    public HBaseDynamicTableSink(
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/hbase/src/main/java/org/apache/inlong/sort/hbase/sink/HBaseSinkFunction.java`
#### Snippet
```java
    private Long rowSize = 0L;
    private final DirtyOptions dirtyOptions;
    private @Nullable final DirtySink<Object> dirtySink;

    public HBaseSinkFunction(
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kafka/src/main/java/org/apache/inlong/sort/kafka/DynamicKafkaSerializationSchema.java`
#### Snippet
```java
    private JsonDynamicSchemaFormat jsonDynamicSchemaFormat;
    private final DirtyOptions dirtyOptions;
    private final @Nullable DirtySink<Object> dirtySink;
    private int[] partitions;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kafka/src/main/java/org/apache/inlong/sort/kafka/DynamicKafkaSerializationSchema.java`
#### Snippet
```java
    private static final Logger LOG = LoggerFactory.getLogger(DynamicKafkaSerializationSchema.class);

    private final @Nullable FlinkKafkaPartitioner<RowData> partitioner;

    private final String topic;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kafka/src/main/java/org/apache/inlong/sort/kafka/DynamicKafkaSerializationSchema.java`
#### Snippet
```java
    private final String topic;

    private final @Nullable SerializationSchema<RowData> keySerialization;

    private final SerializationSchema<RowData> valueSerialization;
```

### NullableProblems
Primitive type members cannot be annotated
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/jdbc/src/main/java/org/apache/inlong/sort/jdbc/internal/JdbcMultiBatchingOutputFormat.java`
#### Snippet
```java
            @Nonnull JdbcExecutionOptions executionOptions,
            @Nonnull JdbcDmlOptions dmlOptions,
            @Nonnull boolean appendMode,
            @Nonnull JdbcOptions jdbcOptions,
            String sinkMultipleFormat,
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kafka/src/main/java/org/apache/inlong/sort/kafka/KafkaDynamicSink.java`
#### Snippet
```java
     */
    private final String auditHostAndPorts;
    private @Nullable final String sinkMultipleFormat;
    private final DirtyOptions dirtyOptions;
    private @Nullable final DirtySink<Object> dirtySink;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kafka/src/main/java/org/apache/inlong/sort/kafka/KafkaDynamicSink.java`
#### Snippet
```java
     * Parallelism of the physical Kafka producer. *
     */
    protected final @Nullable Integer parallelism;
    /**
     * CatalogTable for KAFKA_IGNORE_ALL_CHANGELOG
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kafka/src/main/java/org/apache/inlong/sort/kafka/KafkaDynamicSink.java`
#### Snippet
```java
    private @Nullable final String sinkMultipleFormat;
    private final DirtyOptions dirtyOptions;
    private @Nullable final DirtySink<Object> dirtySink;
    /**
     * Metadata that is appended at the end of a physical sink row.
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kafka/src/main/java/org/apache/inlong/sort/kafka/KafkaDynamicSink.java`
#### Snippet
```java
     * Prefix that needs to be removed from fields when constructing the physical data type.
     */
    protected final @Nullable String keyPrefix;
    /**
     * The Kafka topic to write to.
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kafka/src/main/java/org/apache/inlong/sort/kafka/KafkaDynamicSink.java`
#### Snippet
```java
     * Optional format for encoding keys to Kafka.
     */
    protected final @Nullable EncodingFormat<SerializationSchema<RowData>> keyEncodingFormat;
    /**
     * Format for encoding values to Kafka.
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kafka/src/main/java/org/apache/inlong/sort/kafka/KafkaDynamicSink.java`
#### Snippet
```java
     * Partitioner to select Kafka partition for each item.
     */
    protected final @Nullable FlinkKafkaPartitioner<RowData> partitioner;

    // --------------------------------------------------------------------------------------------
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kafka/src/main/java/org/apache/inlong/sort/kafka/table/DynamicKafkaDeserializationSchema.java`
#### Snippet
```java

    private final DirtyOptions dirtyOptions;
    private final @Nullable DirtySink<String> dirtySink;
    private SourceMetricData metricData;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kafka/src/main/java/org/apache/inlong/sort/kafka/table/DynamicKafkaDeserializationSchema.java`
#### Snippet
```java
    private static final Logger LOG = LoggerFactory.getLogger(DynamicKafkaDeserializationSchema.class);

    private final @Nullable DeserializationSchema<RowData> keyDeserialization;

    private final DeserializationSchema<RowData> valueDeserialization;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kafka/src/main/java/org/apache/inlong/sort/kafka/table/KafkaDynamicSource.java`
#### Snippet
```java
     * Optional format for decoding keys from Kafka.
     */
    protected final @Nullable DecodingFormat<DeserializationSchema<RowData>> keyDecodingFormat;

    /**
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kafka/src/main/java/org/apache/inlong/sort/kafka/table/KafkaDynamicSource.java`
#### Snippet
```java
     * Watermark strategy that is used to generate per-partition watermark.
     */
    protected @Nullable WatermarkStrategy<RowData> watermarkStrategy;

    // --------------------------------------------------------------------------------------------
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kafka/src/main/java/org/apache/inlong/sort/kafka/table/KafkaDynamicSource.java`
#### Snippet
```java
     * Prefix that needs to be removed from fields when constructing the physical data type.
     */
    protected final @Nullable String keyPrefix;

    // --------------------------------------------------------------------------------------------
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kafka/src/main/java/org/apache/inlong/sort/kafka/table/KafkaDynamicSource.java`
#### Snippet
```java

    private final DirtyOptions dirtyOptions;
    private @Nullable final DirtySink<String> dirtySink;

    private static final ObjectMapper MAPPER = new ObjectMapper();
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kafka/src/main/java/org/apache/inlong/sort/kafka/FlinkKafkaProducer.java`
#### Snippet
```java
     * Kafka.
     */
    @Nullable
    private final KeyedSerializationSchema<IN> keyedSchema;
    /**
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kafka/src/main/java/org/apache/inlong/sort/kafka/FlinkKafkaProducer.java`
#### Snippet
```java
    public static class KafkaTransactionState {

        @Nullable
        final String transactionalId;
        final long producerId;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kafka/src/main/java/org/apache/inlong/sort/kafka/FlinkKafkaProducer.java`
#### Snippet
```java
     * Errors encountered in the async producer are stored here.
     */
    @Nullable
    protected transient volatile Exception asyncException;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kafka/src/main/java/org/apache/inlong/sort/kafka/FlinkKafkaProducer.java`
#### Snippet
```java
     * ProducerRecords}.
     */
    @Nullable
    private final KafkaSerializationSchema<IN> kafkaSchema;
    /**
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kafka/src/main/java/org/apache/inlong/sort/kafka/FlinkKafkaProducer.java`
#### Snippet
```java
     * The callback than handles error propagation or logging callbacks.
     */
    @Nullable
    protected transient Callback callback;
    /**
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kafka/src/main/java/org/apache/inlong/sort/kafka/FlinkKafkaProducer.java`
#### Snippet
```java
     * User-provided partitioner for assigning an object to a Kafka partition for each topic.
     */
    @Nullable
    private final FlinkKafkaPartitioner<IN> flinkKafkaPartitioner;
    /**
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/pulsar/src/main/java/org/apache/inlong/sort/pulsar/table/DynamicPulsarDeserializationSchema.java`
#### Snippet
```java
                }
            };
    @Nullable
    private final DeserializationSchema<RowData> keyDeserialization;
    private final DeserializationSchema<RowData> valueDeserialization;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/pulsar/src/main/java/org/apache/inlong/sort/pulsar/table/DynamicPulsarSerializationSchema.java`
#### Snippet
```java
    private static final long serialVersionUID = 1L;

    private final @Nullable SerializationSchema<RowData> keySerialization;

    private final SerializationSchema<RowData> valueSerialization;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/pulsar/src/main/java/org/apache/inlong/sort/pulsar/table/PulsarDynamicTableSource.java`
#### Snippet
```java

    /** Watermark strategy that is used to generate per-partition watermark. */
    protected @Nullable WatermarkStrategy<RowData> watermarkStrategy;

    // --------------------------------------------------------------------------------------------
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/pulsar/src/main/java/org/apache/inlong/sort/pulsar/table/PulsarDynamicTableSource.java`
#### Snippet
```java

    /** Prefix that needs to be removed from fields when constructing the physical data type. */
    @Nullable
    protected final String keyPrefix;
    // --------------------------------------------------------------------------------------------
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/pulsar/src/main/java/org/apache/inlong/sort/pulsar/table/PulsarDynamicTableSource.java`
#### Snippet
```java

    /** Optional format for decoding keys from Pulsar. */
    protected final @Nullable DecodingFormat<DeserializationSchema<RowData>> keyDecodingFormat;

    /** Format for decoding values from Pulsar. */
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/pulsar/src/main/java/org/apache/inlong/sort/pulsar/table/PulsarDynamicTableSink.java`
#### Snippet
```java
     * Prefix that needs to be removed from fields when constructing the physical data type.
     */
    protected final @Nullable String keyPrefix;
    /**
     * Flag to determine sink mode. In upsert mode sink transforms the delete/update-before message to
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/pulsar/src/main/java/org/apache/inlong/sort/pulsar/table/PulsarDynamicTableSink.java`
#### Snippet
```java
     * Optional format for encoding keys to Pulsar.
     */
    protected final @Nullable EncodingFormat<SerializationSchema<RowData>> keyEncodingFormat;
    /**
     * Sink format for encoding records to pulsar.
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/pulsar/src/main/java/org/apache/inlong/sort/pulsar/table/PulsarDynamicTableSink.java`
#### Snippet
```java
     * Parallelism of the physical Pulsar producer.
     **/
    protected final @Nullable Integer parallelism;
    /**
     * Sink commit semantic.
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/tubemq/src/main/java/org/apache/inlong/sort/tubemq/table/TubeMQTableSource.java`
#### Snippet
```java
     * Watermark strategy that is used to generate per-partition watermark.
     */
    @Nullable
    private WatermarkStrategy<RowData> watermarkStrategy;

```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/iceberg/src/main/java/org/apache/inlong/sort/iceberg/sink/trick/CharSequenceSet.java`
#### Snippet
```java

    @Override
    public boolean addAll(Collection<? extends CharSequence> charSequences) {
        if (charSequences != null) {
            return Iterables.addAll(
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/iceberg/src/main/java/org/apache/inlong/sort/iceberg/sink/trick/CharSequenceSet.java`
#### Snippet
```java
    @Override
    @SuppressWarnings("CollectionUndefinedEquality")
    public boolean containsAll(Collection<?> objects) {
        if (objects != null) {
            return Iterables.all(objects, this::contains);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/iceberg/src/main/java/org/apache/inlong/sort/iceberg/sink/trick/CharSequenceSet.java`
#### Snippet
```java

    @Override
    public boolean retainAll(Collection<?> objects) {
        if (objects != null) {
            return Iterables.removeAll(wrapperSet, objects);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/iceberg/src/main/java/org/apache/inlong/sort/iceberg/sink/trick/CharSequenceSet.java`
#### Snippet
```java

    @Override
    public boolean removeAll(Collection<?> objects) {
        if (objects != null) {
            return Iterables.removeAll(wrapperSet, objects);
```

### NullableProblems
Getter for @Nullable field might be annotated @Nullable itself
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/iceberg/src/main/java/org/apache/inlong/sort/iceberg/sink/multiple/IcebergSingleStreamWriter.java`
#### Snippet
```java
    }

    public RowType getFlinkRowType() {
        return flinkRowType;
    }
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/mysql/source/split/MySqlSnapshotSplitState.java`
#### Snippet
```java
public class MySqlSnapshotSplitState extends MySqlSplitState {

    @Nullable
    private BinlogOffset highWatermark;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/mysql/source/split/MySqlRecords.java`
#### Snippet
```java
    @Nullable
    private String splitId;
    @Nullable
    private Iterator<SourceRecord> recordsForCurrentSplit;
    @Nullable
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/mysql/source/split/MySqlRecords.java`
#### Snippet
```java
    @Nullable
    private Iterator<SourceRecord> recordsForCurrentSplit;
    @Nullable
    private final Iterator<SourceRecord> recordsForSplit;
    private final Set<String> finishedSnapshotSplits;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/mysql/source/split/MySqlRecords.java`
#### Snippet
```java
public final class MySqlRecords implements RecordsWithSplitIds<SourceRecord> {

    @Nullable
    private String splitId;
    @Nullable
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/mysql/source/split/MySqlBinlogSplitState.java`
#### Snippet
```java

    private final Map<TableId, TableChange> tableSchemas;
    @Nullable
    private BinlogOffset startingOffset;
    @Nullable
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/mysql/source/split/MySqlBinlogSplitState.java`
#### Snippet
```java
    @Nullable
    private BinlogOffset startingOffset;
    @Nullable
    private BinlogOffset endingOffset;
    private Map<TableId, String> tableDdls;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/mysql/source/split/MySqlBinlogSplit.java`
#### Snippet
```java
    private final int totalFinishedSplitSize;
    private final boolean isSuspended;
    @Nullable
    transient byte[] serializedFormCache;
    private final Map<TableId, String> tableDdls;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/mysql/source/split/MySqlSnapshotSplit.java`
#### Snippet
```java
    @Nullable
    private final Object[] splitStart;
    @Nullable
    private final Object[] splitEnd;
    /**
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/mysql/source/split/MySqlSnapshotSplit.java`
#### Snippet
```java
     * The high watermark is not bull when the split read finished.
     */
    @Nullable
    private final BinlogOffset highWatermark;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/mysql/source/split/MySqlSnapshotSplit.java`
#### Snippet
```java
    private final Map<TableId, TableChange> tableSchemas;

    @Nullable
    private final Object[] splitStart;
    @Nullable
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/mysql/source/split/MySqlSnapshotSplit.java`
#### Snippet
```java
    private final BinlogOffset highWatermark;

    @Nullable
    transient byte[] serializedFormCache;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/mysql/source/config/MySqlSourceConfig.java`
#### Snippet
```java
    private final List<String> databaseList;
    private final List<String> tableList;
    @Nullable
    private final ServerIdRange serverIdRange;
    private final StartupOptions startupOptions;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/mysql/source/reader/MySqlSplitReader.java`
#### Snippet
```java
    private final MySqlSourceReaderContext context;

    @Nullable
    private DebeziumReader<SourceRecord, MySqlSplit> currentReader;
    @Nullable
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/mysql/source/reader/MySqlSplitReader.java`
#### Snippet
```java
    @Nullable
    private DebeziumReader<SourceRecord, MySqlSplit> currentReader;
    @Nullable
    private String currentSplitId;

```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/mysql/source/offset/BinlogOffset.java`
#### Snippet
```java
     */
    @Override
    public int compareTo(BinlogOffset that) {
        // the NO_STOPPING_OFFSET is the max offset
        if (NO_STOPPING_OFFSET.equals(that) && NO_STOPPING_OFFSET.equals(this)) {
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/mysql/source/assigners/ChunkRange.java`
#### Snippet
```java
class ChunkRange {

    private final @Nullable Object chunkStart;
    private final @Nullable Object chunkEnd;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/mysql/source/assigners/ChunkRange.java`
#### Snippet
```java

    private final @Nullable Object chunkStart;
    private final @Nullable Object chunkEnd;

    /**
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/mysql/source/assigners/state/PendingSplitsState.java`
#### Snippet
```java
     * repeated serialization cheap. This field is used by {@link PendingSplitsStateSerializer}.
     */
    @Nullable
    transient byte[] serializedFormCache;
}
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/mysql/source/assigners/MySqlSnapshotSplitAssigner.java`
#### Snippet
```java
    private Object lock;

    @Nullable
    private Long checkpointIdToFinish;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/filesystem/src/main/java/org/apache/inlong/sort/filesystem/stream/AbstractStreamingWriter.java`
#### Snippet
```java
    private final String inlongAudit;
    private final DirtyOptions dirtyOptions;
    private @Nullable final DirtySink<Object> dirtySink;

    private transient ListState<MetricState> metricStateListState;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/debezium/DebeziumSourceFunction.java`
#### Snippet
```java
     * The specific binlog offset to read from when the first startup.
     */
    private final @Nullable DebeziumOffset specificOffset;

    /**
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/filesystem/src/main/java/org/apache/inlong/sort/filesystem/FileSystemTableSink.java`
#### Snippet
```java
    private LinkedHashMap<String, String> staticPartitions = new LinkedHashMap<>();

    @Nullable
    private final Integer configuredParallelism;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/filesystem/src/main/java/org/apache/inlong/sort/filesystem/FileSystemTableSink.java`
#### Snippet
```java
    @Nullable
    private final DecodingFormat<DeserializationSchema<RowData>> deserializationFormat;
    @Nullable
    private final FileSystemFormatFactory formatFactory;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/filesystem/src/main/java/org/apache/inlong/sort/filesystem/FileSystemTableSink.java`
#### Snippet
```java

    // For compaction reading
    @Nullable
    private final DecodingFormat<BulkFormat<RowData, FileSourceSplit>> bulkReaderFormat;
    @Nullable
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/filesystem/src/main/java/org/apache/inlong/sort/filesystem/FileSystemTableSink.java`
#### Snippet
```java
    private final String inlongAudit;
    private final DirtyOptions dirtyOptions;
    private @Nullable final DirtySink<Object> dirtySink;

    FileSystemTableSink(
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/filesystem/src/main/java/org/apache/inlong/sort/filesystem/FileSystemTableSink.java`
#### Snippet
```java
    @Nullable
    private final DecodingFormat<BulkFormat<RowData, FileSourceSplit>> bulkReaderFormat;
    @Nullable
    private final DecodingFormat<DeserializationSchema<RowData>> deserializationFormat;
    @Nullable
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/filesystem/src/main/java/org/apache/inlong/sort/filesystem/FileSystemTableSink.java`
#### Snippet
```java

    // For Writing
    @Nullable
    private final EncodingFormat<BulkWriter.Factory<RowData>> bulkWriterFormat;
    @Nullable
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/filesystem/src/main/java/org/apache/inlong/sort/filesystem/FileSystemTableSink.java`
#### Snippet
```java
    @Nullable
    private final EncodingFormat<BulkWriter.Factory<RowData>> bulkWriterFormat;
    @Nullable
    private final EncodingFormat<SerializationSchema<RowData>> serializationFormat;

```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/oracle-cdc/src/main/java/org/apache/inlong/sort/cdc/oracle/source/meta/offset/RedoLogOffset.java`
#### Snippet
```java

    @Override
    public int compareTo(Offset offset) {
        RedoLogOffset that = (RedoLogOffset) offset;
        // the NO_STOPPING_OFFSET is the max offset
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/oracle-cdc/src/main/java/org/apache/inlong/sort/cdc/oracle/source/config/OracleSourceConfig.java`
#### Snippet
```java
    private static final long serialVersionUID = 1L;

    @Nullable
    private String url;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/oracle-cdc/src/main/java/org/apache/inlong/sort/cdc/oracle/source/config/OracleSourceConfigFactory.java`
#### Snippet
```java
    private static final String DRIVER_ClASS_NAME = "oracle.jdbc.OracleDriver";

    @Nullable
    private String url;
    private List<String> schemaList;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mongodb-cdc/src/main/java/org/apache/inlong/sort/cdc/mongodb/source/config/MongoDBSourceConfig.java`
#### Snippet
```java
    @Nullable
    private final String username;
    @Nullable
    private final String password;
    @Nullable
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mongodb-cdc/src/main/java/org/apache/inlong/sort/cdc/mongodb/source/config/MongoDBSourceConfig.java`
#### Snippet
```java
    @Nullable
    private final List<String> databaseList;
    @Nullable
    private final List<String> collectionList;
    private final String connectionString;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mongodb-cdc/src/main/java/org/apache/inlong/sort/cdc/mongodb/source/config/MongoDBSourceConfig.java`
#### Snippet
```java
    @Nullable
    private final String password;
    @Nullable
    private final List<String> databaseList;
    @Nullable
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mongodb-cdc/src/main/java/org/apache/inlong/sort/cdc/mongodb/source/config/MongoDBSourceConfig.java`
#### Snippet
```java

    private final String hosts;
    @Nullable
    private final String username;
    @Nullable
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mongodb-cdc/src/main/java/org/apache/inlong/sort/cdc/mongodb/source/offset/ChangeStreamDescriptor.java`
#### Snippet
```java
    private final Pattern databaseRegex;

    @Nullable
    private final Pattern namespaceRegex;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mongodb-cdc/src/main/java/org/apache/inlong/sort/cdc/mongodb/source/offset/ChangeStreamDescriptor.java`
#### Snippet
```java
    private static final long serialVersionUID = 1L;

    @Nullable
    private final String database;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mongodb-cdc/src/main/java/org/apache/inlong/sort/cdc/mongodb/source/offset/ChangeStreamDescriptor.java`
#### Snippet
```java
    private final String database;

    @Nullable
    private final String collection;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mongodb-cdc/src/main/java/org/apache/inlong/sort/cdc/mongodb/source/offset/ChangeStreamDescriptor.java`
#### Snippet
```java
    private final String collection;

    @Nullable
    private final Pattern databaseRegex;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/oracle-cdc/src/main/java/org/apache/inlong/sort/cdc/oracle/debezium/DebeziumSourceFunction.java`
#### Snippet
```java
     * The specific binlog offset to read from when the first startup.
     */
    private final @Nullable DebeziumOffset specificOffset;

    /**
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mongodb-cdc/src/main/java/org/apache/inlong/sort/cdc/mongodb/source/offset/ChangeStreamOffset.java`
#### Snippet
```java

    @Override
    public int compareTo(Offset offset) {
        if (offset == null) {
            return -1;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mongodb-cdc/src/main/java/org/apache/inlong/sort/cdc/mongodb/DebeziumSourceFunction.java`
#### Snippet
```java
     * The specific binlog offset to read from when the first startup.
     */
    private final @Nullable DebeziumOffset specificOffset;

    /**
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mongodb-cdc/src/main/java/org/apache/inlong/sort/cdc/mongodb/debezium/internal/SchemaRecord.java`
#### Snippet
```java
    private final HistoryRecord historyRecord;

    @Nullable
    private final Document tableChangeDoc;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mongodb-cdc/src/main/java/org/apache/inlong/sort/cdc/mongodb/debezium/internal/SchemaRecord.java`
#### Snippet
```java
public class SchemaRecord {

    @Nullable
    private final HistoryRecord historyRecord;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/postgres-cdc/src/main/java/org/apache/inlong/sort/cdc/postgres/connection/PostgreSQLJdbcConnectionOptions.java`
#### Snippet
```java
    @Nullable
    protected final String username;
    @Nullable
    protected final String password;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/postgres-cdc/src/main/java/org/apache/inlong/sort/cdc/postgres/connection/PostgreSQLJdbcConnectionOptions.java`
#### Snippet
```java
    protected final String url;
    protected final String driverName;
    @Nullable
    protected final String username;
    @Nullable
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/postgres-cdc/src/main/java/org/apache/inlong/sort/cdc/postgres/DebeziumSourceFunction.java`
#### Snippet
```java
     * The specific binlog offset to read from when the first startup.
     */
    private final @Nullable DebeziumOffset specificOffset;

    /**
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/sqlserver-cdc/src/main/java/org/apache/inlong/sort/cdc/sqlserver/table/DebeziumSourceFunction.java`
#### Snippet
```java
     * The specific binlog offset to read from when the first startup.
     */
    private final @Nullable DebeziumOffset specificOffset;

    /**
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/FieldInfo.java`
#### Snippet
```java
     * It will be null if the field is a meta field
     */
    @Nullable
    @JsonProperty("formatInfo")
    private FormatInfo formatInfo;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/LookupOptions.java`
#### Snippet
```java
     * Lookup async
     */
    @Nullable
    @JsonProperty("lookupAsync")
    private Boolean lookupAsync;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/LookupOptions.java`
#### Snippet
```java
     * Lookup cache max rows
     */
    @Nullable
    @JsonProperty("lookupCacheMaxRows")
    private Long lookupCacheMaxRows;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/LookupOptions.java`
#### Snippet
```java
     * Lookup max retries
     */
    @Nullable
    @JsonProperty("lookupMaxRetries")
    private Integer lookupMaxRetries;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/LookupOptions.java`
#### Snippet
```java
     * Lookup cache ttl
     */
    @Nullable
    @JsonProperty("lookupCacheTtl")
    private Long lookupCacheTtl;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/ExtractNode.java`
#### Snippet
```java
    @JsonProperty("fields")
    private List<FieldInfo> fields;
    @Nullable
    @JsonProperty("watermarkField")
    @JsonInclude(Include.NON_NULL)
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/ExtractNode.java`
#### Snippet
```java
    @JsonInclude(Include.NON_NULL)
    private WatermarkField watermarkField;
    @Nullable
    @JsonInclude(Include.NON_NULL)
    @JsonProperty("properties")
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/LoadNode.java`
#### Snippet
```java
    @JsonInclude(Include.NON_NULL)
    private FilterStrategy filterStrategy;
    @Nullable
    @JsonInclude(Include.NON_NULL)
    @JsonProperty("properties")
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/LoadNode.java`
#### Snippet
```java
    @JsonProperty("fieldRelations")
    private List<FieldRelation> fieldRelations;
    @Nullable
    @JsonInclude(Include.NON_NULL)
    @JsonProperty("sinkParallelism")
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/load/IcebergLoadNode.java`
#### Snippet
```java

    @JsonProperty("dbName")
    @Nonnull
    private String dbName;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/load/IcebergLoadNode.java`
#### Snippet
```java

    @JsonProperty("tableName")
    @Nonnull
    private String tableName;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/load/KuduLoadNode.java`
#### Snippet
```java

    @JsonProperty("masters")
    @Nonnull
    private String masters;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/load/KuduLoadNode.java`
#### Snippet
```java

    @JsonProperty("tableName")
    @Nonnull
    private String tableName;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/load/DorisLoadNode.java`
#### Snippet
```java
    private String primaryKey;

    @Nullable
    @JsonProperty("sinkMultipleEnable")
    private Boolean sinkMultipleEnable = false;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/load/DorisLoadNode.java`
#### Snippet
```java

    @JsonProperty("feNodes")
    @Nonnull
    private String feNodes;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/load/DorisLoadNode.java`
#### Snippet
```java
    private Boolean sinkMultipleEnable = false;

    @Nullable
    @JsonProperty("sinkMultipleFormat")
    private Format sinkMultipleFormat;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/load/DorisLoadNode.java`
#### Snippet
```java
    private String databasePattern;

    @Nullable
    @JsonProperty("tablePattern")
    private String tablePattern;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/load/DorisLoadNode.java`
#### Snippet
```java

    @JsonProperty("tableIdentifier")
    @Nullable
    private String tableIdentifier;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/load/DorisLoadNode.java`
#### Snippet
```java

    @JsonProperty("password")
    @Nonnull
    private String password;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/load/DorisLoadNode.java`
#### Snippet
```java

    @JsonProperty("username")
    @Nonnull
    private String userName;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/load/DorisLoadNode.java`
#### Snippet
```java
    private Format sinkMultipleFormat;

    @Nullable
    @JsonProperty("databasePattern")
    private String databasePattern;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/load/ClickHouseLoadNode.java`
#### Snippet
```java

    @JsonProperty("passWord")
    @Nonnull
    private String password;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/load/ClickHouseLoadNode.java`
#### Snippet
```java

    @JsonProperty("url")
    @Nonnull
    private String url;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/load/ClickHouseLoadNode.java`
#### Snippet
```java

    @JsonProperty("tableName")
    @Nonnull
    private String tableName;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/load/ClickHouseLoadNode.java`
#### Snippet
```java

    @JsonProperty("userName")
    @Nonnull
    private String userName;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/load/ElasticsearchLoadNode.java`
#### Snippet
```java
    private int version;

    @Nullable
    @JsonProperty("sinkMultipleEnable")
    private Boolean sinkMultipleEnable = false;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/load/ElasticsearchLoadNode.java`
#### Snippet
```java

    @JsonProperty("index")
    @Nonnull
    private String index;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/load/ElasticsearchLoadNode.java`
#### Snippet
```java
    private Format sinkMultipleFormat;

    @Nullable
    @JsonProperty("indexPattern")
    private String indexPattern;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/load/ElasticsearchLoadNode.java`
#### Snippet
```java
    private Boolean sinkMultipleEnable = false;

    @Nullable
    @JsonProperty("sinkMultipleFormat")
    private Format sinkMultipleFormat;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/load/ElasticsearchLoadNode.java`
#### Snippet
```java

    @JsonProperty("username")
    @Nonnull
    private String username;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/load/ElasticsearchLoadNode.java`
#### Snippet
```java

    @JsonProperty("password")
    @Nonnull
    private String password;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/load/ElasticsearchLoadNode.java`
#### Snippet
```java

    @JsonProperty("hosts")
    @Nonnull
    private String hosts;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/load/StarRocksLoadNode.java`
#### Snippet
```java

    @JsonProperty("database-name")
    @Nullable
    private String databaseName;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/load/StarRocksLoadNode.java`
#### Snippet
```java

    @JsonProperty("load-url")
    @Nonnull
    private String loadUrl;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/load/StarRocksLoadNode.java`
#### Snippet
```java
    private String primaryKey;

    @Nullable
    @JsonProperty("sinkMultipleEnable")
    private Boolean sinkMultipleEnable = false;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/load/StarRocksLoadNode.java`
#### Snippet
```java

    @JsonProperty("username")
    @Nonnull
    private String username;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/load/StarRocksLoadNode.java`
#### Snippet
```java
    private Format sinkMultipleFormat;

    @Nullable
    @JsonProperty("databasePattern")
    private String databasePattern;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/load/StarRocksLoadNode.java`
#### Snippet
```java
    private Boolean sinkMultipleEnable = false;

    @Nullable
    @JsonProperty("sinkMultipleFormat")
    private Format sinkMultipleFormat;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/load/StarRocksLoadNode.java`
#### Snippet
```java

    @JsonProperty("password")
    @Nonnull
    private String password;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/load/StarRocksLoadNode.java`
#### Snippet
```java

    @JsonProperty("jdbc-url")
    @Nonnull
    private String jdbcUrl;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/load/StarRocksLoadNode.java`
#### Snippet
```java
    private String databasePattern;

    @Nullable
    @JsonProperty("tablePattern")
    private String tablePattern;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/load/HiveLoadNode.java`
#### Snippet
```java

    @JsonProperty("database")
    @Nonnull
    private String database;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/load/HiveLoadNode.java`
#### Snippet
```java

    @JsonProperty("tableName")
    @Nonnull
    private String tableName;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/load/FileSystemLoadNode.java`
#### Snippet
```java

    @JsonProperty("format")
    @Nonnull
    private String format;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/load/FileSystemLoadNode.java`
#### Snippet
```java

    @JsonProperty("path")
    @Nonnull
    private String path;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/load/HudiLoadNode.java`
#### Snippet
```java

    @JsonProperty("tableName")
    @Nonnull
    private String tableName;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/load/HudiLoadNode.java`
#### Snippet
```java

    @JsonProperty("dbName")
    @Nonnull
    private String dbName;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/extract/DorisExtractNode.java`
#### Snippet
```java

    @JsonProperty("tableIdentifier")
    @Nonnull
    private String tableIdentifier;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/extract/DorisExtractNode.java`
#### Snippet
```java

    @JsonProperty("feNodes")
    @Nonnull
    private String feNodes;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/extract/DorisExtractNode.java`
#### Snippet
```java

    @JsonProperty("password")
    @Nonnull
    private String password;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/extract/DorisExtractNode.java`
#### Snippet
```java

    @JsonProperty("username")
    @Nonnull
    private String userName;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/extract/FileSystemExtractNode.java`
#### Snippet
```java

    @JsonProperty("path")
    @Nonnull
    private String path;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/extract/FileSystemExtractNode.java`
#### Snippet
```java

    @JsonProperty("format")
    @Nonnull
    private String format;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/load/KafkaLoadNode.java`
#### Snippet
```java
    @JsonProperty("sinkPartitioner")
    private String sinkPartitioner;
    @Nullable
    @JsonProperty("partitionPattern")
    private String partitionPattern;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/load/KafkaLoadNode.java`
#### Snippet
```java
    @JsonProperty("bootstrapServers")
    private String bootstrapServers;
    @Nonnull
    @JsonProperty("format")
    private Format format;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/load/KafkaLoadNode.java`
#### Snippet
```java
    @JsonProperty("topic")
    private String topic;
    @Nonnull
    @JsonProperty("bootstrapServers")
    private String bootstrapServers;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/load/KafkaLoadNode.java`
#### Snippet
```java
    private static final long serialVersionUID = -558158965060708408L;

    @Nonnull
    @JsonProperty("topic")
    private String topic;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/load/KafkaLoadNode.java`
#### Snippet
```java
    @JsonProperty("primaryKey")
    private String primaryKey;
    @Nullable
    @JsonProperty("topicPattern")
    private String topicPattern;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/load/KafkaLoadNode.java`
#### Snippet
```java
    @JsonProperty("sinkMultipleFormat")
    private Format sinkMultipleFormat;
    @Nullable
    @JsonProperty("sinkPartitioner")
    private String sinkPartitioner;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/load/KafkaLoadNode.java`
#### Snippet
```java
    @JsonProperty("topicPattern")
    private String topicPattern;
    @Nullable
    @JsonProperty("sinkMultipleFormat")
    private Format sinkMultipleFormat;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/extract/TubeMQExtractNode.java`
#### Snippet
```java
    private static final long serialVersionUID = -2544747886429528474L;

    @Nonnull
    @JsonProperty("masterRpc")
    private String masterRpc;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/extract/HudiExtractNode.java`
#### Snippet
```java

    @JsonProperty("dbName")
    @Nonnull
    private String dbName;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/extract/HudiExtractNode.java`
#### Snippet
```java

    @JsonProperty("tableName")
    @Nonnull
    private String tableName;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/extract/TubeMQExtractNode.java`
#### Snippet
```java
    private String masterRpc;

    @Nonnull
    @JsonProperty("topic")
    private String topic;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/extract/TubeMQExtractNode.java`
#### Snippet
```java
    private String topic;

    @Nonnull
    @JsonProperty("format")
    private String format;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/extract/TubeMQExtractNode.java`
#### Snippet
```java
    private String format;

    @Nonnull
    @JsonProperty("groupId")
    private String groupId;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/extract/PulsarExtractNode.java`
#### Snippet
```java
    @JsonProperty("adminUrl")
    private String adminUrl;
    @Nonnull
    @JsonProperty("serviceUrl")
    private String serviceUrl;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/extract/PulsarExtractNode.java`
#### Snippet
```java
    @JsonProperty("serviceUrl")
    private String serviceUrl;
    @Nonnull
    @JsonProperty("format")
    private Format format;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/extract/PulsarExtractNode.java`
#### Snippet
```java
    private static final long serialVersionUID = 1L;

    @Nonnull
    @JsonProperty("topic")
    private String topic;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/extract/SqlServerExtractNode.java`
#### Snippet
```java

    @JsonProperty("hostname")
    @Nonnull
    private String hostname;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/extract/SqlServerExtractNode.java`
#### Snippet
```java

    @JsonProperty(value = "schemaName", defaultValue = "dbo")
    @Nonnull
    private String schemaName;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/extract/SqlServerExtractNode.java`
#### Snippet
```java

    @JsonProperty("password")
    @Nonnull
    private String password;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/extract/SqlServerExtractNode.java`
#### Snippet
```java

    @JsonProperty("port")
    @Nonnull
    private Integer port;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/extract/SqlServerExtractNode.java`
#### Snippet
```java

    @JsonProperty("database")
    @Nonnull
    private String database;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/extract/SqlServerExtractNode.java`
#### Snippet
```java

    @JsonProperty("tableName")
    @Nonnull
    private String tableName;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/extract/SqlServerExtractNode.java`
#### Snippet
```java

    @JsonProperty("username")
    @Nonnull
    private String username;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/extract/KafkaExtractNode.java`
#### Snippet
```java
    @JsonProperty("bootstrapServers")
    private String bootstrapServers;
    @Nonnull
    @JsonProperty("format")
    private Format format;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/extract/KafkaExtractNode.java`
#### Snippet
```java
    private static final long serialVersionUID = 1L;

    @Nonnull
    @JsonProperty("topic")
    private String topic;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/extract/KafkaExtractNode.java`
#### Snippet
```java
    @JsonProperty("topic")
    private String topic;
    @Nonnull
    @JsonProperty("bootstrapServers")
    private String bootstrapServers;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/extract/OracleExtractNode.java`
#### Snippet
```java
    @JsonProperty("password")
    private String password;
    @Nonnull
    @JsonProperty("database")
    private String database;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/extract/OracleExtractNode.java`
#### Snippet
```java
    @JsonProperty("username")
    private String username;
    @Nonnull
    @JsonProperty("password")
    private String password;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/extract/OracleExtractNode.java`
#### Snippet
```java
    @JsonProperty("schemaName")
    private String schemaName;
    @Nonnull
    @JsonProperty("tableName")
    private String tableName;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/extract/OracleExtractNode.java`
#### Snippet
```java
    @JsonProperty("database")
    private String database;
    @Nonnull
    @JsonProperty("schemaName")
    private String schemaName;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/extract/OracleExtractNode.java`
#### Snippet
```java
    @JsonProperty("hostname")
    private String hostname;
    @Nonnull
    @JsonProperty("username")
    private String username;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/extract/OracleExtractNode.java`
#### Snippet
```java
    @JsonProperty("primaryKey")
    private String primaryKey;
    @Nonnull
    @JsonProperty("hostname")
    private String hostname;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/extract/OracleExtractNode.java`
#### Snippet
```java

    @JsonInclude(JsonInclude.Include.NON_NULL)
    @Nullable
    @JsonProperty("primaryKey")
    private String primaryKey;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/extract/OracleExtractNode.java`
#### Snippet
```java
    @JsonProperty(value = "port", defaultValue = "1521")
    private Integer port;
    @Nullable
    @JsonProperty("scanStartupMode")
    @JsonInclude(JsonInclude.Include.NON_NULL)
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/extract/MySqlExtractNode.java`
#### Snippet
```java
    @JsonProperty("serverTimeZone")
    private String serverTimeZone;
    @Nonnull
    @JsonProperty("extractMode")
    private ExtractMode extractMode;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/transformation/function/IntervalFunction.java`
#### Snippet
```java
public class IntervalFunction implements Function {

    @Nonnull
    @JsonProperty("interval")
    private final StringConstantParam interval;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/transformation/function/IntervalFunction.java`
#### Snippet
```java
    private final StringConstantParam interval;

    @Nonnull
    @JsonProperty("timeUnit")
    private final TimeUnitConstantParam timeUnit;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/transformation/function/AddFunction.java`
#### Snippet
```java
    @JsonProperty("leftField")
    private final FunctionParam leftField;
    @Nonnull
    @JsonProperty("rightField")
    private final FunctionParam rightField;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/transformation/function/AddFunction.java`
#### Snippet
```java
public class AddFunction implements Function {

    @Nonnull
    @JsonProperty("leftField")
    private final FunctionParam leftField;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/extract/MySqlExtractNode.java`
#### Snippet
```java
    private String primaryKey;
    @JsonProperty("tableNames")
    @Nonnull
    private List<String> tableNames;
    @JsonProperty("hostname")
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/extract/RedisExtractNode.java`
#### Snippet
```java
     * The sentinels connect to redis info used for {@link RedisMode#SENTINEL}
     */
    @Nullable
    @JsonProperty("sentinelsInfo")
    private String sentinelsInfo;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/extract/RedisExtractNode.java`
#### Snippet
```java
     * The host connect to redis only used for {@link RedisMode#STANDALONE}
     */
    @Nullable
    @JsonProperty("host")
    private String host;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/extract/RedisExtractNode.java`
#### Snippet
```java
     * The cluster node infos connect to redis only used for {@link RedisMode#CLUSTER}
     */
    @Nullable
    @JsonProperty("clusterNodes")
    private String clusterNodes;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/extract/RedisExtractNode.java`
#### Snippet
```java
     * The redis deploy mode connect to redis see also {@link RedisMode}
     */
    @Nonnull
    @JsonProperty("redisMode")
    private RedisMode redisMode;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/extract/RedisExtractNode.java`
#### Snippet
```java
     * The additional key connect to redis only used for [Hash|Sorted-Set] data type
     */
    @Nullable
    @JsonProperty("additionalKey")
    private String additionalKey;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/extract/RedisExtractNode.java`
#### Snippet
```java
     * The timeout connect to redis
     */
    @Nullable
    @JsonProperty("timeout")
    private Integer timeout;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/extract/RedisExtractNode.java`
#### Snippet
```java
     * The redis command connect to redis see also {@link RedisCommand}
     */
    @Nonnull
    @JsonProperty("command")
    private RedisCommand command;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/extract/RedisExtractNode.java`
#### Snippet
```java
    @JsonProperty("minIdle")
    private Integer minIdle;
    @Nullable
    @JsonProperty("primaryKey")
    private String primaryKey;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/extract/RedisExtractNode.java`
#### Snippet
```java
     * The soTimeout connect to redis
     */
    @Nullable
    @JsonProperty("soTimeout")
    private Integer soTimeout;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/extract/RedisExtractNode.java`
#### Snippet
```java
     * The minIdle connect to redis
     */
    @Nullable
    @JsonProperty("minIdle")
    private Integer minIdle;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/extract/RedisExtractNode.java`
#### Snippet
```java
     * The database connect to redis used for {@link RedisMode#STANDALONE} and {@link RedisMode#SENTINEL}
     */
    @Nullable
    @JsonProperty("database")
    private Integer database;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/extract/RedisExtractNode.java`
#### Snippet
```java
     * The master name connect to redis only used for {@link RedisMode#SENTINEL}
     */
    @Nullable
    @JsonProperty("masterName")
    private String masterName;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/extract/RedisExtractNode.java`
#### Snippet
```java
     * The maxTotal connect to redis
     */
    @Nullable
    @JsonProperty("maxTotal")
    private Integer maxTotal;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/extract/RedisExtractNode.java`
#### Snippet
```java
     * The password connect to redis
     */
    @Nullable
    @JsonProperty("password")
    private String password;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/extract/RedisExtractNode.java`
#### Snippet
```java
     * The maxIdle connect to redis
     */
    @Nullable
    @JsonProperty("maxIdle")
    private Integer maxIdle;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/extract/RedisExtractNode.java`
#### Snippet
```java
     * The lookup options for connector redis see also {@link LookupOptions}
     */
    @Nullable
    @JsonProperty("lookupOptions")
    private LookupOptions lookupOptions;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/extract/RedisExtractNode.java`
#### Snippet
```java
     * The port connect to redis only used for {@link RedisMode#STANDALONE}
     */
    @Nullable
    @JsonProperty("port")
    private Integer port;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/transformation/function/BetweenFunction.java`
#### Snippet
```java
    @JsonProperty("end")
    private final FunctionParam end;
    @Nonnull
    @JsonProperty("logicOperator")
    private final LogicOperator logicOperator;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/transformation/function/BetweenFunction.java`
#### Snippet
```java
    @JsonProperty("field")
    private final FunctionParam field;
    @Nonnull
    @JsonProperty("start")
    private final FunctionParam start;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/transformation/function/BetweenFunction.java`
#### Snippet
```java
    @JsonProperty("start")
    private final FunctionParam start;
    @Nonnull
    @JsonProperty("end")
    private final FunctionParam end;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/transformation/function/BetweenFunction.java`
#### Snippet
```java
public class BetweenFunction implements FilterFunction {

    @Nonnull
    @JsonProperty("field")
    private final FunctionParam field;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/transformation/function/SubtractFunction.java`
#### Snippet
```java
    @JsonProperty("leftField")
    private final FunctionParam leftField;
    @Nonnull
    @JsonProperty("rightField")
    private final FunctionParam rightField;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/transformation/function/SubtractFunction.java`
#### Snippet
```java
public class SubtractFunction implements Function {

    @Nonnull
    @JsonProperty("leftField")
    private final FunctionParam leftField;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-formats/format-kv/src/main/java/org/apache/inlong/sort/formats/kv/KvSerializationSchema.java`
#### Snippet
```java
     * The literal represented null values, default "".
     */
    @Nullable
    private final String nullLiteral;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-formats/format-kv/src/main/java/org/apache/inlong/sort/formats/kv/KvSerializationSchema.java`
#### Snippet
```java
     * The delimiter between entries.
     */
    @Nonnull
    private final Character entryDelimiter;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-formats/format-kv/src/main/java/org/apache/inlong/sort/formats/kv/KvSerializationSchema.java`
#### Snippet
```java
     * The delimiter between key and value.
     */
    @Nonnull
    private final Character kvDelimiter;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-formats/format-kv/src/main/java/org/apache/inlong/sort/formats/kv/KvSerializationSchema.java`
#### Snippet
```java
     * Quote character. Null if quoting is disabled.
     */
    @Nullable
    private final Character quoteChar;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-formats/format-kv/src/main/java/org/apache/inlong/sort/formats/kv/KvSerializationSchema.java`
#### Snippet
```java
     * The charset of the text.
     */
    @Nonnull
    private final String charset;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-formats/format-kv/src/main/java/org/apache/inlong/sort/formats/kv/KvSerializationSchema.java`
#### Snippet
```java
     * Escape character. Null if escaping is disabled.
     */
    @Nullable
    private final Character escapeChar;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-formats/format-kv/src/main/java/org/apache/inlong/sort/formats/kv/KvDeserializationSchema.java`
#### Snippet
```java
     * Format information describing the result type.
     */
    @Nonnull
    private final RowFormatInfo rowFormatInfo;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-formats/format-kv/src/main/java/org/apache/inlong/sort/formats/kv/KvDeserializationSchema.java`
#### Snippet
```java
     * The charset of the text.
     */
    @Nonnull
    private final String charset;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-formats/format-kv/src/main/java/org/apache/inlong/sort/formats/kv/KvDeserializationSchema.java`
#### Snippet
```java
     * The literal represented null values, default "".
     */
    @Nullable
    private final String nullLiteral;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-formats/format-kv/src/main/java/org/apache/inlong/sort/formats/kv/KvDeserializationSchema.java`
#### Snippet
```java
     * Escape character. Null if escaping is disabled.
     */
    @Nullable
    private final Character escapeChar;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-formats/format-kv/src/main/java/org/apache/inlong/sort/formats/kv/KvDeserializationSchema.java`
#### Snippet
```java
     * The delimiter between entries.
     */
    @Nonnull
    private final Character entryDelimiter;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-formats/format-kv/src/main/java/org/apache/inlong/sort/formats/kv/KvDeserializationSchema.java`
#### Snippet
```java
     * The delimiter between key and value.
     */
    @Nonnull
    private final Character kvDelimiter;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-formats/format-kv/src/main/java/org/apache/inlong/sort/formats/kv/KvDeserializationSchema.java`
#### Snippet
```java
     * Quote character. Null if quoting is disabled.
     */
    @Nullable
    private final Character quoteChar;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-formats/format-csv/src/main/java/org/apache/inlong/sort/formats/csv/CsvDeserializationSchema.java`
#### Snippet
```java
     * The charset of the text.
     */
    @Nonnull
    private final String charset;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-formats/format-csv/src/main/java/org/apache/inlong/sort/formats/csv/CsvDeserializationSchema.java`
#### Snippet
```java
     * Escape character. Null if escaping is disabled.
     */
    @Nullable
    private final Character escapeChar;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-formats/format-csv/src/main/java/org/apache/inlong/sort/formats/csv/CsvDeserializationSchema.java`
#### Snippet
```java
     * The delimiter between fields.
     */
    @Nonnull
    private final Character delimiter;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-formats/format-csv/src/main/java/org/apache/inlong/sort/formats/csv/CsvDeserializationSchema.java`
#### Snippet
```java
     * Format information describing the result type.
     */
    @Nonnull
    private final RowFormatInfo rowFormatInfo;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-formats/format-csv/src/main/java/org/apache/inlong/sort/formats/csv/CsvDeserializationSchema.java`
#### Snippet
```java
     * Quote character. Null if quoting is disabled.
     */
    @Nullable
    private final Character quoteChar;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-formats/format-csv/src/main/java/org/apache/inlong/sort/formats/csv/CsvDeserializationSchema.java`
#### Snippet
```java
     * The literal represented null values, default "".
     */
    @Nullable
    private final String nullLiteral;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-formats/format-csv/src/main/java/org/apache/inlong/sort/formats/csv/CsvSerializationSchema.java`
#### Snippet
```java
     * Escape character. Null if escaping is disabled.
     */
    @Nullable
    private final Character escapeChar;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-formats/format-csv/src/main/java/org/apache/inlong/sort/formats/csv/CsvSerializationSchema.java`
#### Snippet
```java
     * Quote character. Null if quoting is disabled.
     */
    @Nullable
    private final Character quoteChar;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-formats/format-csv/src/main/java/org/apache/inlong/sort/formats/csv/CsvSerializationSchema.java`
#### Snippet
```java
     * The literal represented null values, default "".
     */
    @Nullable
    private final String nullLiteral;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-formats/format-csv/src/main/java/org/apache/inlong/sort/formats/csv/CsvSerializationSchema.java`
#### Snippet
```java
     * The charset for the text.
     */
    @Nonnull
    private final String charset;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-formats/format-csv/src/main/java/org/apache/inlong/sort/formats/csv/CsvSerializationSchema.java`
#### Snippet
```java
     * The delimiter between fields.
     */
    @Nonnull
    private final Character delimiter;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-formats/format-json/src/main/java/org/apache/inlong/sort/formats/json/canal/CanalJsonDecodingFormat.java`
#### Snippet
```java
    // --------------------------------------------------------------------------------------------

    private final @Nullable String database;

    private final @Nullable String table;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-formats/format-json/src/main/java/org/apache/inlong/sort/formats/json/canal/CanalJsonDecodingFormat.java`
#### Snippet
```java
    private final @Nullable String database;

    private final @Nullable String table;

    private final boolean ignoreParseErrors;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-formats/format-json/src/main/java/org/apache/inlong/sort/formats/json/canal/CanalJsonDeserializationSchema.java`
#### Snippet
```java

    /** Only read changelogs from the specific table. */
    private final @Nullable String table;

    /** Flag indicating whether to ignore invalid fields/rows (default: throw an exception). */
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-formats/format-json/src/main/java/org/apache/inlong/sort/formats/json/canal/CanalJsonDeserializationSchema.java`
#### Snippet
```java

    /** Only read changelogs from the specific database. */
    private final @Nullable String database;

    /** Only read changelogs from the specific table. */
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-formats/format-common/src/main/java/org/apache/inlong/sort/formats/common/MapTypeInfo.java`
#### Snippet
```java
     */
    @JsonProperty(FIELD_VALUE_TYPE)
    @Nonnull
    private final TypeInfo valueTypeInfo;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-formats/format-common/src/main/java/org/apache/inlong/sort/formats/common/MapTypeInfo.java`
#### Snippet
```java
     */
    @JsonProperty(FIELD_KEY_TYPE)
    @Nonnull
    private final TypeInfo keyTypeInfo;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-formats/format-json/src/main/java/org/apache/inlong/sort/formats/json/canal/CanalJsonEnhancedDeserializationSchema.java`
#### Snippet
```java
     * Only read changelogs from the specific table.
     */
    private final @Nullable String table;

    /**
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-formats/format-json/src/main/java/org/apache/inlong/sort/formats/json/canal/CanalJsonEnhancedDeserializationSchema.java`
#### Snippet
```java
     * Only read changelogs from the specific database.
     */
    private final @Nullable String database;

    /**
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-formats/format-common/src/main/java/org/apache/inlong/sort/formats/common/MapFormatInfo.java`
#### Snippet
```java
     */
    @JsonProperty(FIELD_VALUE_FORMAT)
    @Nonnull
    private final FormatInfo valueFormatInfo;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-formats/format-common/src/main/java/org/apache/inlong/sort/formats/common/MapFormatInfo.java`
#### Snippet
```java
     */
    @JsonProperty(FIELD_KEY_FORMAT)
    @Nonnull
    private final FormatInfo keyFormatInfo;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-formats/format-common/src/main/java/org/apache/inlong/sort/formats/common/ArrayFormatInfo.java`
#### Snippet
```java
     */
    @JsonProperty(FIELD_ELEMENT_FORMAT)
    @Nonnull
    private final FormatInfo elementFormatInfo;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-formats/format-json/src/main/java/org/apache/inlong/sort/formats/json/canal/CanalJsonEnhancedDecodingFormat.java`
#### Snippet
```java
    // Canal-specific attributes
    // --------------------------------------------------------------------------------------------
    @Nullable
    private final String database;
    @Nullable
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-formats/format-json/src/main/java/org/apache/inlong/sort/formats/json/canal/CanalJsonEnhancedDecodingFormat.java`
#### Snippet
```java
    @Nullable
    private final String database;
    @Nullable
    private final String table;
    private final boolean ignoreParseErrors;
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-formats/format-common/src/main/java/org/apache/inlong/sort/formats/common/TimeFormatInfo.java`
#### Snippet
```java
    private static final int DEFAULT_PRECISION_FOR_TIMESTAMP = 2;
    @JsonProperty(FIELD_FORMAT)
    @Nonnull
    private final String format;
    @JsonIgnore
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-formats/format-common/src/main/java/org/apache/inlong/sort/formats/common/TimeFormatInfo.java`
#### Snippet
```java
    private final String format;
    @JsonIgnore
    @Nullable
    private final SimpleDateFormat simpleDateFormat;
    @JsonProperty("precision")
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-formats/format-common/src/main/java/org/apache/inlong/sort/formats/common/ArrayTypeInfo.java`
#### Snippet
```java
     */
    @JsonProperty(FIELD_ELEMENT_TYPE)
    @Nonnull
    private final TypeInfo elementTypeInfo;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-formats/format-common/src/main/java/org/apache/inlong/sort/formats/common/TimestampFormatInfo.java`
#### Snippet
```java

    @JsonIgnore
    @Nullable
    private final SimpleDateFormat simpleDateFormat;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-formats/format-common/src/main/java/org/apache/inlong/sort/formats/common/TimestampFormatInfo.java`
#### Snippet
```java

    @JsonProperty(FIELD_FORMAT)
    @Nonnull
    private final String format;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-formats/format-common/src/main/java/org/apache/inlong/sort/formats/common/RowTypeInfo.java`
#### Snippet
```java

    @JsonProperty(FIELD_FIELD_TYPES)
    @Nonnull
    private final TypeInfo[] fieldTypeInfos;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-formats/format-common/src/main/java/org/apache/inlong/sort/formats/common/RowTypeInfo.java`
#### Snippet
```java

    @JsonProperty(FIELD_FIELD_NAMES)
    @Nonnull
    private final String[] fieldNames;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-formats/format-common/src/main/java/org/apache/inlong/sort/formats/common/RowFormatInfo.java`
#### Snippet
```java

    @JsonProperty(FIELD_FIELD_NAMES)
    @Nonnull
    private final String[] fieldNames;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-formats/format-common/src/main/java/org/apache/inlong/sort/formats/common/RowFormatInfo.java`
#### Snippet
```java

    @JsonProperty(FIELD_FIELD_FORMATS)
    @Nonnull
    private final FormatInfo[] fieldFormatInfos;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-formats/format-common/src/main/java/org/apache/inlong/sort/formats/common/DateFormatInfo.java`
#### Snippet
```java

    @JsonIgnore
    @Nullable
    private final SimpleDateFormat simpleDateFormat;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-formats/format-common/src/main/java/org/apache/inlong/sort/formats/common/DateFormatInfo.java`
#### Snippet
```java

    @JsonProperty(FIELD_FORMAT)
    @Nonnull
    private final String format;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-formats/format-inlongmsg-csv/src/main/java/org/apache/inlong/sort/formats/inlongmsgcsv/InLongMsgCsvMixedFormatConverter.java`
#### Snippet
```java
     * The schema of the rows.
     */
    @Nonnull
    private final RowFormatInfo rowFormatInfo;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-formats/format-inlongmsg-csv/src/main/java/org/apache/inlong/sort/formats/inlongmsgcsv/InLongMsgCsvMixedFormatConverter.java`
#### Snippet
```java
     * The name of the attributes field.
     */
    @Nonnull
    private final String attributesFieldName;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-formats/format-inlongmsg-csv/src/main/java/org/apache/inlong/sort/formats/inlongmsgcsv/InLongMsgCsvMixedFormatConverter.java`
#### Snippet
```java
     * The name of the time field.
     */
    @Nonnull
    private final String timeFieldName;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-formats/format-inlongmsg-csv/src/main/java/org/apache/inlong/sort/formats/inlongmsgcsv/InLongMsgCsvFormatDeserializer.java`
#### Snippet
```java
     * The delimiter between fields.
     */
    @Nonnull
    private final Character delimiter;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-formats/format-inlongmsg-csv/src/main/java/org/apache/inlong/sort/formats/inlongmsgcsv/InLongMsgCsvFormatDeserializer.java`
#### Snippet
```java
     * The charset of the text.
     */
    @Nonnull
    private final String charset;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-formats/format-inlongmsg-csv/src/main/java/org/apache/inlong/sort/formats/inlongmsgcsv/InLongMsgCsvFormatDeserializer.java`
#### Snippet
```java
     * The name of the attributes field.
     */
    @Nonnull
    private final String attributesFieldName;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-formats/format-inlongmsg-csv/src/main/java/org/apache/inlong/sort/formats/inlongmsgcsv/InLongMsgCsvFormatDeserializer.java`
#### Snippet
```java
     * Format information describing the result type.
     */
    @Nonnull
    private final RowFormatInfo rowFormatInfo;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-formats/format-inlongmsg-csv/src/main/java/org/apache/inlong/sort/formats/inlongmsgcsv/InLongMsgCsvFormatDeserializer.java`
#### Snippet
```java
     * Escape character. Null if escaping is disabled.
     */
    @Nullable
    private final Character escapeChar;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-formats/format-inlongmsg-csv/src/main/java/org/apache/inlong/sort/formats/inlongmsgcsv/InLongMsgCsvFormatDeserializer.java`
#### Snippet
```java
     * The literal represented null values, default "".
     */
    @Nullable
    private final String nullLiteral;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-formats/format-inlongmsg-csv/src/main/java/org/apache/inlong/sort/formats/inlongmsgcsv/InLongMsgCsvFormatDeserializer.java`
#### Snippet
```java
     * Quote character. Null if quoting is disabled.
     */
    @Nullable
    private final Character quoteChar;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-formats/format-inlongmsg-csv/src/main/java/org/apache/inlong/sort/formats/inlongmsgcsv/InLongMsgCsvFormatDeserializer.java`
#### Snippet
```java
     * The name of the time field.
     */
    @Nonnull
    private final String timeFieldName;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-formats/format-inlongmsg-csv/src/main/java/org/apache/inlong/sort/formats/inlongmsgcsv/InLongMsgCsvMixedFormatDeserializer.java`
#### Snippet
```java
     * Escape character. Null if escaping is disabled.
     */
    @Nullable
    private final Character escapeChar;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-sort/sort-formats/format-inlongmsg-csv/src/main/java/org/apache/inlong/sort/formats/inlongmsgcsv/InLongMsgCsvMixedFormatDeserializer.java`
#### Snippet
```java
     * Quote character. Null if quoting is disabled.
     */
    @Nullable
    private final Character quoteChar;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-formats/format-inlongmsg-csv/src/main/java/org/apache/inlong/sort/formats/inlongmsgcsv/InLongMsgCsvMixedFormatDeserializer.java`
#### Snippet
```java
     * The charset of the text.
     */
    @Nonnull
    private final String charset;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-formats/format-inlongmsg-csv/src/main/java/org/apache/inlong/sort/formats/inlongmsgcsv/InLongMsgCsvMixedFormatDeserializer.java`
#### Snippet
```java
     * The delimiter between fields.
     */
    @Nonnull
    private final Character delimiter;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-sort/sort-formats/format-inlongmsg-base/src/main/java/org/apache/inlong/sort/formats/inlongmsg/AbstractInLongMsgFormatDeserializer.java`
#### Snippet
```java
     * True if ignore errors in the deserialization.
     */
    @Nonnull
    protected final Boolean ignoreErrors;

```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `inlong-agent/agent-common/src/main/java/org/apache/inlong/agent/common/AgentThreadFactory.java`
#### Snippet
```java

    @Override
    public Thread newThread(Runnable r) {
        Thread t = new Thread(r,
                threadType + "-" + NAMED_THREAD_PLACEHOLDER + "-" + mThreadNum.getAndIncrement());
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/base/NamedThreadFactory.java`
#### Snippet
```java

    @Override
    public Thread newThread(Runnable r) {
        Thread t = new Thread(r, threadType + "-running-thread-" + mThreadNum.getAndIncrement());
        LOGGER.debug("{} created", t.getName());
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/base/HighPriorityThreadFactory.java`
#### Snippet
```java
    }

    public Thread newThread(Runnable r) {
        Thread t = new Thread(this.group, r, this.namePrefix + this.threadNumber.getAndIncrement(), 0L);
        t.setDaemon(this.isDaemon);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/netty/ByteBufferOutputStream.java`
#### Snippet
```java

    @Override
    public void write(byte[] b, int off, int len) {
        ByteBuffer buffer = buffers.get(buffers.size() - 1);
        int remaining = buffer.remaining();
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/netty/ByteBufferInputStream.java`
#### Snippet
```java
     */
    @Override
    public int read(byte[] b, int off, int len) throws IOException {
        if (len == 0) {
            return 0;
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/client/CallFuture.java`
#### Snippet
```java

    @Override
    public T get(long timeout, TimeUnit unit)
            throws InterruptedException, ExecutionException, TimeoutException {
        if (latch.await(timeout, unit)) {
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/producer/ProducerManager.java`
#### Snippet
```java

                    @Override
                    public Thread newThread(Runnable r) {
                        Thread t = new Thread(r, new StringBuilder(256)
                                .append("Producer-Heartbeat-Thread-")
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/tools/cli/CliProducer.java`
#### Snippet
```java

                    @Override
                    public Thread newThread(Runnable runnable) {
                        return new Thread(runnable, "sender_" + producerMap.size());
                    }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/TubeBroker.java`
#### Snippet
```java

                    @Override
                    public Thread newThread(Runnable r) {
                        Thread t = new Thread(r, "Broker Heartbeat Thread");
                        t.setPriority(Thread.MAX_PRIORITY);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/BaseMessageConsumer.java`
#### Snippet
```java

                    @Override
                    public Thread newThread(Runnable r) {
                        Thread t = new Thread(r, new StringBuilder(512)
                                .append("Master-Heartbeat-Thread-")
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/SimpleClientBalanceConsumer.java`
#### Snippet
```java

                    @Override
                    public Thread newThread(Runnable r) {
                        Thread t = new Thread(r, new StringBuilder(512)
                                .append("Master-Heartbeat-Thread-")
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/MessageStoreManager.java`
#### Snippet
```java

                    @Override
                    public Thread newThread(Runnable r) {
                        return new Thread(r, "Broker Log Mem Flush Thread");
                    }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/MessageStoreManager.java`
#### Snippet
```java

                    @Override
                    public Thread newThread(Runnable r) {
                        return new Thread(r, "Broker Log Disk Flush Thread");
                    }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/MessageStoreManager.java`
#### Snippet
```java

                    @Override
                    public Thread newThread(Runnable r) {
                        return new Thread(r, "Broker Log Clear Thread");
                    }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/metamanage/metastore/impl/zkimpl/ZKMetaConfigMapperImpl.java`
#### Snippet
```java

                    @Override
                    public Thread newThread(Runnable r) {
                        return new Thread(r, "Master selector thread");
                    }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/metamanage/DefaultMetaDataService.java`
#### Snippet
```java

                        @Override
                        public Thread newThread(Runnable r) {
                            return new Thread(r, "Master Status Check");
                        }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `inlong-tubemq/tubemq-example/src/main/java/org/apache/inlong/tubemq/example/MAMessageProducerExample.java`
#### Snippet
```java

                    @Override
                    public Thread newThread(Runnable runnable) {
                        return new Thread(runnable);
                    }
```

### NullableProblems
Not annotated method overrides method annotated with @NonNullApi
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/repository/MasterRepository.java`
#### Snippet
```java
     * @return
     */
    List<MasterEntry> findAll();

    /**
```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-tubemq/tubemq-connectors/tubemq-connector-flink/src/main/java/org/apache/flink/connectors/tubemq/Tubemq.java`
#### Snippet
```java
    private String topic;

    @Nullable
    private String master;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.NotNull' instead of '@javax.annotation.Nonnull'
in `inlong-tubemq/tubemq-connectors/tubemq-connector-flink/src/main/java/org/apache/flink/connectors/tubemq/Tubemq.java`
#### Snippet
```java
    private String tids;

    @Nonnull
    private Map<String, String> properties;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-tubemq/tubemq-connectors/tubemq-connector-flink/src/main/java/org/apache/flink/connectors/tubemq/Tubemq.java`
#### Snippet
```java
    private String group;

    @Nullable
    private String tids;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-tubemq/tubemq-connectors/tubemq-connector-flink/src/main/java/org/apache/flink/connectors/tubemq/Tubemq.java`
#### Snippet
```java
    private boolean consumerRole = true;

    @Nullable
    private String topic;

```

### NullableProblems
Primitive type members cannot be annotated
in `inlong-tubemq/tubemq-connectors/tubemq-connector-flink/src/main/java/org/apache/flink/connectors/tubemq/Tubemq.java`
#### Snippet
```java
public class Tubemq extends ConnectorDescriptor {

    @Nullable
    private boolean consumerRole = true;

```

### NullableProblems
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `inlong-tubemq/tubemq-connectors/tubemq-connector-flink/src/main/java/org/apache/flink/connectors/tubemq/Tubemq.java`
#### Snippet
```java
    private String master;

    @Nullable
    private String group;

```

### NullableProblems
Not annotated method overrides method annotated with @EverythingIsNonNull
in `inlong-manager/manager-client/src/main/java/org/apache/inlong/manager/client/api/service/AuthInterceptor.java`
#### Snippet
```java

    @Override
    public Response intercept(Chain chain) throws IOException {
        Request oldRequest = chain.request();
        HttpUrl.Builder builder = oldRequest.url()
```

### NullableProblems
Not annotated method overrides method annotated with @ElementTypesAreNonnullByDefault
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/source/SourceSnapshotOperator.java`
#### Snippet
```java

                        @Override
                        public ConcurrentHashMap<Integer, Integer> load(String agentIp) {
                            List<StreamSourceEntity> sourceEntities = sourceMapper.selectByAgentIp(agentIp);
                            if (CollectionUtils.isEmpty(sourceEntities)) {
```

### NullableProblems
Not annotated parameter overrides @ElementTypesAreNonnullByDefault parameter
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/source/SourceSnapshotOperator.java`
#### Snippet
```java

                        @Override
                        public ConcurrentHashMap<Integer, Integer> load(String agentIp) {
                            List<StreamSourceEntity> sourceEntities = sourceMapper.selectByAgentIp(agentIp);
                            if (CollectionUtils.isEmpty(sourceEntities)) {
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/base/HighPriorityThreadFactory.java`
#### Snippet
```java
    }

    public Thread newThread(Runnable r) {
        Thread t = new Thread(this.group, r, this.namePrefix + this.threadNumber.getAndIncrement(), 0L);
        t.setDaemon(this.isDaemon);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/base/NamedThreadFactory.java`
#### Snippet
```java

    @Override
    public Thread newThread(Runnable r) {
        Thread t = new Thread(r, threadType + "-running-thread-" + mThreadNum.getAndIncrement());
        LOGGER.debug("{} created", t.getName());
```

### NullableProblems
Not annotated method overrides method annotated with @ElementTypesAreNonnullByDefault
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/common/MsgDedupHandler.java`
#### Snippet
```java

                            @Override
                            public Long load(String key) {
                                return System.currentTimeMillis();
                            }
```

### NullableProblems
Not annotated parameter overrides @ElementTypesAreNonnullByDefault parameter
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/common/MsgDedupHandler.java`
#### Snippet
```java

                            @Override
                            public Long load(String key) {
                                return System.currentTimeMillis();
                            }
```

## RuleId[id=EqualsBetweenInconvertibleTypes]
### EqualsBetweenInconvertibleTypes
`equals` between objects of inconvertible types 'StreamSplit' and 'StreamSplit'
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/source/meta/split/StreamSplit.java`
#### Snippet
```java
            return false;
        }
        if (!super.equals(o)) {
            return false;
        }
```

### EqualsBetweenInconvertibleTypes
`equals` between objects of inconvertible types 'ChangeStreamOffset' and 'Offset'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mongodb-cdc/src/main/java/org/apache/inlong/sort/cdc/mongodb/source/reader/fetch/MongoDBStreamFetchTask.java`
#### Snippet
```java

    private boolean isBoundedRead() {
        return !NO_STOPPING_OFFSET.equals(streamSplit.getEndingOffset());
    }
}
```

### EqualsBetweenInconvertibleTypes
`equals` between objects of inconvertible types 'String' and 'int'
in `inlong-audit/audit-sdk/src/main/java/org/apache/inlong/audit/util/IpPort.java`
#### Snippet
```java
        try {
            IpPort ctp = (IpPort) o;
            if (ip != null && ip.equals(ctp.port) && port == ctp.port) {
                return true;
            }
```

## RuleId[id=DuplicateCondition]
### DuplicateCondition
Duplicate condition `dateStr.matches("\\d{4}\\-\\d{1,2}\\-\\d{1,2} +\\d{1,2}:\\d{1,2}")`
in `inlong-sdk/sort-sdk/src/main/java/org/apache/inlong/sdk/sort/util/StringUtil.java`
#### Snippet
```java
            if (dateStr.matches("\\d{1,4}\\-\\d{1,2}\\-\\d{1,2}\\s+\\d{1,2}:\\d{1,2}:\\d{1,2}\\.\\d{1,3}")) {
                inputFormat = "yyyy-MM-dd HH:mm:ss.SSS";
            } else if (dateStr.matches("\\d{4}\\-\\d{1,2}\\-\\d{1,2} +\\d{1,2}:\\d{1,2}")) {
                inputFormat = "yyyy-MM-dd HH:mm:ss";
            } else if (dateStr.matches("\\d{4}\\-\\d{1,2}\\-\\d{1,2} +\\d{1,2}:\\d{1,2}")) {
```

### DuplicateCondition
Duplicate condition `dateStr.matches("\\d{4}\\-\\d{1,2}\\-\\d{1,2} +\\d{1,2}:\\d{1,2}")`
in `inlong-sdk/sort-sdk/src/main/java/org/apache/inlong/sdk/sort/util/StringUtil.java`
#### Snippet
```java
            } else if (dateStr.matches("\\d{4}\\-\\d{1,2}\\-\\d{1,2} +\\d{1,2}:\\d{1,2}")) {
                inputFormat = "yyyy-MM-dd HH:mm:ss";
            } else if (dateStr.matches("\\d{4}\\-\\d{1,2}\\-\\d{1,2} +\\d{1,2}:\\d{1,2}")) {
                inputFormat = "yyyy-MM-dd HH:mm";
            } else if (dateStr.matches("\\d{4}\\-\\d{1,2}\\-\\d{1,2} +\\d{1,2}")) {
```

### DuplicateCondition
Duplicate condition `dateStr.matches("\\d{4}\\-\\d{1,2}\\-\\d{1,2} +\\d{1,2}")`
in `inlong-sdk/sort-sdk/src/main/java/org/apache/inlong/sdk/sort/util/StringUtil.java`
#### Snippet
```java
            } else if (dateStr.matches("\\d{4}\\-\\d{1,2}\\-\\d{1,2} +\\d{1,2}:\\d{1,2}")) {
                inputFormat = "yyyy-MM-dd HH:mm";
            } else if (dateStr.matches("\\d{4}\\-\\d{1,2}\\-\\d{1,2} +\\d{1,2}")) {
                inputFormat = "yyyy-MM-dd HH";
            } else if (dateStr.matches("\\d{4}\\-\\d{1,2}\\-\\d{1,2} +\\d{1,2}")) {
```

### DuplicateCondition
Duplicate condition `dateStr.matches("\\d{4}\\-\\d{1,2}\\-\\d{1,2} +\\d{1,2}")`
in `inlong-sdk/sort-sdk/src/main/java/org/apache/inlong/sdk/sort/util/StringUtil.java`
#### Snippet
```java
            } else if (dateStr.matches("\\d{4}\\-\\d{1,2}\\-\\d{1,2} +\\d{1,2}")) {
                inputFormat = "yyyy-MM-dd HH";
            } else if (dateStr.matches("\\d{4}\\-\\d{1,2}\\-\\d{1,2} +\\d{1,2}")) {
                inputFormat = "yyyy-MM-dd";
            } else if (dateStr.matches("\\d{1,4}/\\d{1,2}/\\d{1,2}\\s+\\d{1,2}:\\d{1,2}:\\d{1,2}\\.\\d{1,3}")) {
```

### DuplicateCondition
Duplicate condition `dateStr.matches("\\d{4}/\\d{1,2}/\\d{1,2} +\\d{1,2}:\\d{1,2}")`
in `inlong-sdk/sort-sdk/src/main/java/org/apache/inlong/sdk/sort/util/StringUtil.java`
#### Snippet
```java
            } else if (dateStr.matches("\\d{1,4}/\\d{1,2}/\\d{1,2}\\s+\\d{1,2}:\\d{1,2}:\\d{1,2}\\.\\d{1,3}")) {
                inputFormat = "yyyy/MM/dd HH:mm:ss.SSS";
            } else if (dateStr.matches("\\d{4}/\\d{1,2}/\\d{1,2} +\\d{1,2}:\\d{1,2}")) {
                inputFormat = "yyyy/MM/dd HH:mm:ss";
            } else if (dateStr.matches("\\d{4}/\\d{1,2}/\\d{1,2} +\\d{1,2}:\\d{1,2}")) {
```

### DuplicateCondition
Duplicate condition `dateStr.matches("\\d{4}/\\d{1,2}/\\d{1,2} +\\d{1,2}:\\d{1,2}")`
in `inlong-sdk/sort-sdk/src/main/java/org/apache/inlong/sdk/sort/util/StringUtil.java`
#### Snippet
```java
            } else if (dateStr.matches("\\d{4}/\\d{1,2}/\\d{1,2} +\\d{1,2}:\\d{1,2}")) {
                inputFormat = "yyyy/MM/dd HH:mm:ss";
            } else if (dateStr.matches("\\d{4}/\\d{1,2}/\\d{1,2} +\\d{1,2}:\\d{1,2}")) {
                inputFormat = "yyyy/MM/dd HH:mm";
            } else if (dateStr.matches("\\d{4}/\\d{1,2}/\\d{1,2} +\\d{1,2}")) {
```

### DuplicateCondition
Duplicate condition `dateStr.matches("\\d{4}/\\d{1,2}/\\d{1,2} +\\d{1,2}")`
in `inlong-sdk/sort-sdk/src/main/java/org/apache/inlong/sdk/sort/util/StringUtil.java`
#### Snippet
```java
            } else if (dateStr.matches("\\d{4}/\\d{1,2}/\\d{1,2} +\\d{1,2}:\\d{1,2}")) {
                inputFormat = "yyyy/MM/dd HH:mm";
            } else if (dateStr.matches("\\d{4}/\\d{1,2}/\\d{1,2} +\\d{1,2}")) {
                inputFormat = "yyyy/MM/dd HH";
            } else if (dateStr.matches("\\d{4}/\\d{1,2}/\\d{1,2} +\\d{1,2}")) {
```

### DuplicateCondition
Duplicate condition `dateStr.matches("\\d{4}/\\d{1,2}/\\d{1,2} +\\d{1,2}")`
in `inlong-sdk/sort-sdk/src/main/java/org/apache/inlong/sdk/sort/util/StringUtil.java`
#### Snippet
```java
            } else if (dateStr.matches("\\d{4}/\\d{1,2}/\\d{1,2} +\\d{1,2}")) {
                inputFormat = "yyyy/MM/dd HH";
            } else if (dateStr.matches("\\d{4}/\\d{1,2}/\\d{1,2} +\\d{1,2}")) {
                inputFormat = "yyyy/MM/dd";
            }
```

### DuplicateCondition
Duplicate condition `Objects.equals(ghostDdlChange, that.ghostDdlChange)`
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/mysql/table/MySqlTableSource.java`
#### Snippet
```java
                && Objects.equals(includeSchemaChange, that.includeSchemaChange)
                && Objects.equals(includeIncremental, that.includeIncremental)
                && Objects.equals(ghostDdlChange, that.ghostDdlChange)
                && Objects.equals(ghostDdlChange, that.ghostDdlChange);
    }
```

### DuplicateCondition
Duplicate condition `Objects.equals(ghostDdlChange, that.ghostDdlChange)`
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/mysql/table/MySqlTableSource.java`
#### Snippet
```java
                && Objects.equals(includeIncremental, that.includeIncremental)
                && Objects.equals(ghostDdlChange, that.ghostDdlChange)
                && Objects.equals(ghostDdlChange, that.ghostDdlChange);
    }

```

## RuleId[id=IgnoreResultOfCall]
### IgnoreResultOfCall
Result of `File.mkdirs()` is ignored
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/utils/ServiceDiscoveryUtils.java`
#### Snippet
```java
        File localPath = new File(path);
        if (!localPath.getParentFile().exists()) {
            localPath.getParentFile().mkdirs();
        }

```

### IgnoreResultOfCall
Result of `CountDownLatch.await()` is ignored
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/SyncMessageCallable.java`
#### Snippet
```java
        try {
            ChannelFuture channelFuture = client.write(encodeObject);
            awaitLatch.await(timeout, timeUnit);
        } catch (Exception e) {
            logger.error("SendResult call", e);
```

### IgnoreResultOfCall
Result of `CountDownLatch.await()` is ignored
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/NettyClient.java`
#### Snippet
```java
                });
                // Wait until the connection is close.
                awaitLatch.await(configure.getRequestTimeoutMillis(),
                        TimeUnit.MILLISECONDS);
                // Return if close this connection fail.
```

### IgnoreResultOfCall
Result of `CountDownLatch.await()` is ignored
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/NettyClient.java`
#### Snippet
```java
        try {
            // Wait until the connection is built.
            awaitLatch.await(configure.getConnectTimeoutMillis(),
                    TimeUnit.MILLISECONDS);
        } catch (Exception e) {
```

### IgnoreResultOfCall
Result of `Throwable.toString()` is ignored
in `inlong-sort/sort-core/src/main/java/org/apache/inlong/sort/function/EncryptFunction.java`
#### Snippet
```java
            return sb.toString();
        } catch (Exception e) {
            e.toString();
        }
        return null;
```

### IgnoreResultOfCall
Result of `File.mkdir()` is ignored
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/config/ProxyConfigManager.java`
#### Snippet
```java
            File file = new File(clientConfig.getConfStoreBasePath() + entry.getUserName() + ".pubKey");
            if (!file.getParentFile().exists()) {
                file.getParentFile().mkdir();
            }
            if (!file.exists()) {
```

### IgnoreResultOfCall
Result of `File.createNewFile()` is ignored
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/config/ProxyConfigManager.java`
#### Snippet
```java
            }
            if (!file.exists()) {
                file.createNewFile();
            }
            fos = new FileOutputStream(file);
```

### IgnoreResultOfCall
Result of `File.mkdirs()` is ignored
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/config/ProxyConfigManager.java`
#### Snippet
```java
            if (!file.getParentFile().exists()) {
                // try to create parent
                file.getParentFile().mkdirs();
            }
            LOGGER.info("try to write {}} to local cache {}", entry, configCachePath);
```

### IgnoreResultOfCall
Result of `File.mkdirs()` is ignored
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/config/ProxyConfigManager.java`
#### Snippet
```java
            } else {
                if (!localManagerIpsFile.getParentFile().exists()) {
                    localManagerIpsFile.getParentFile().mkdirs();
                }
                localManagerIps = "";
```

### IgnoreResultOfCall
Result of `ExecutorService.awaitTermination()` is ignored
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/debezium/internal/FlinkOffsetBackingStore.java`
#### Snippet
```java
            // Best effort wait for any get() and set() tasks (and caller's callbacks) to complete.
            try {
                executor.awaitTermination(30, TimeUnit.SECONDS);
            } catch (InterruptedException e) {
                Thread.currentThread().interrupt();
```

### IgnoreResultOfCall
Result of `Thread.interrupted()` is ignored
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/pulsar/src/main/java/org/apache/inlong/sort/pulsar/internal/PulsarFetcher.java`
#### Snippet
```java
            // this allows the joining on reader threads (on best effort) to happen in
            // case the initial interrupt already
            Thread.interrupted();

            // make sure that in any case (completion, abort, error), all spawned threads are stopped
```

### IgnoreResultOfCall
Result of `File.mkdirs()` is ignored
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/iceberg/src/main/java/org/apache/inlong/sort/iceberg/sink/collections/FileIOUtils.java`
#### Snippet
```java
    public static void mkdir(File directory) throws IOException {
        if (directory != null && !directory.exists()) {
            directory.mkdirs();
        }

```

### IgnoreResultOfCall
Result of `File.delete()` is ignored
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/iceberg/src/main/java/org/apache/inlong/sort/iceberg/sink/collections/FileIOUtils.java`
#### Snippet
```java
        if (directory != null && directory.exists()) {
            try (Stream<Path> files = Files.walk(directory.toPath())) {
                files.sorted(Comparator.reverseOrder()).map(Path::toFile).forEach(File::delete);
            }
            directory.delete();
```

### IgnoreResultOfCall
Result of `File.delete()` is ignored
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/iceberg/src/main/java/org/apache/inlong/sort/iceberg/sink/collections/FileIOUtils.java`
#### Snippet
```java
                files.sorted(Comparator.reverseOrder()).map(Path::toFile).forEach(File::delete);
            }
            directory.delete();
            if (directory.exists()) {
                throw new IOException("Unable to delete directory " + directory);
```

### IgnoreResultOfCall
Result of `ExecutorService.awaitTermination()` is ignored
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/debezium/DebeziumSourceFunction.java`
#### Snippet
```java

        if (executor != null) {
            executor.awaitTermination(Long.MAX_VALUE, TimeUnit.SECONDS);
        }

```

### IgnoreResultOfCall
Result of `ExecutorService.awaitTermination()` is ignored
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/oracle-cdc/src/main/java/org/apache/inlong/sort/cdc/oracle/debezium/DebeziumSourceFunction.java`
#### Snippet
```java

        if (executor != null) {
            executor.awaitTermination(Long.MAX_VALUE, TimeUnit.SECONDS);
        }

```

### IgnoreResultOfCall
Result of `ExecutorService.awaitTermination()` is ignored
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mongodb-cdc/src/main/java/org/apache/inlong/sort/cdc/mongodb/DebeziumSourceFunction.java`
#### Snippet
```java

        if (executor != null) {
            executor.awaitTermination(Long.MAX_VALUE, TimeUnit.SECONDS);
        }

```

### IgnoreResultOfCall
Result of `ExecutorService.awaitTermination()` is ignored
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mongodb-cdc/src/main/java/org/apache/inlong/sort/cdc/mongodb/debezium/internal/FlinkOffsetBackingStore.java`
#### Snippet
```java
            // Best effort wait for any get() and set() tasks (and caller's callbacks) to complete.
            try {
                executor.awaitTermination(30, TimeUnit.SECONDS);
            } catch (InterruptedException e) {
                Thread.currentThread().interrupt();
```

### IgnoreResultOfCall
Result of `ExecutorService.awaitTermination()` is ignored
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/postgres-cdc/src/main/java/org/apache/inlong/sort/cdc/postgres/DebeziumSourceFunction.java`
#### Snippet
```java

        if (executor != null) {
            executor.awaitTermination(Long.MAX_VALUE, TimeUnit.SECONDS);
        }

```

### IgnoreResultOfCall
Result of `ExecutorService.awaitTermination()` is ignored
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/sqlserver-cdc/src/main/java/org/apache/inlong/sort/cdc/sqlserver/table/DebeziumSourceFunction.java`
#### Snippet
```java

        if (executor != null) {
            executor.awaitTermination(Long.MAX_VALUE, TimeUnit.SECONDS);
        }

```

### IgnoreResultOfCall
Result of `File.delete()` is ignored
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/util/ParameterTool.java`
#### Snippet
```java
        if (file.exists()) {
            if (overwrite) {
                file.delete();
            } else {
                throw new RuntimeException(
```

### IgnoreResultOfCall
Result of `File.createNewFile()` is ignored
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/sources/snapshot/AbstractSnapshot.java`
#### Snippet
```java
                    LOGGER.info("create dir {} result {}", parentDir, success);
                }
                file.createNewFile();
            }
            FileInputStream fis = new FileInputStream(file);
```

### IgnoreResultOfCall
Result of `File.delete()` is ignored
in `inlong-audit/audit-sdk/src/main/java/org/apache/inlong/audit/send/SenderManager.java`
#### Snippet
```java
            }
            if (file.length() > auditConfig.getMaxFileSize()) {
                file.delete();
                return;
            }
```

### IgnoreResultOfCall
Result of `File.delete()` is ignored
in `inlong-audit/audit-sdk/src/main/java/org/apache/inlong/audit/send/SenderManager.java`
#### Snippet
```java
            objectStream.close();
            inputStream.close();
            file.delete();
        } catch (IOException | ClassNotFoundException e) {
            logger.error("check audit file error: ", e);
```

### IgnoreResultOfCall
Result of `LineNumberReader.skip()` is ignored
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/sources/reader/file/FileReaderOperator.java`
#### Snippet
```java
    private void storeRocksDB() {
        try (LineNumberReader lineNumberReader = new LineNumberReader(new FileReader(file.getPath()))) {
            lineNumberReader.skip(Long.MAX_VALUE);
            int seekPosition = lineNumberReader.getLineNumber();

```

### IgnoreResultOfCall
Result of `ConfigManager.getInstance()` is ignored
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/node/Application.java`
#### Snippet
```java
            Application application;

            ConfigManager.getInstance();

            File configurationFile = new File(commandLine.getOptionValue('f'));
```

### IgnoreResultOfCall
Result of `File.delete()` is ignored
in `inlong-audit/audit-common/src/main/java/org/apache/inlong/audit/file/holder/PropertiesConfigHolder.java`
#### Snippet
```java

            FileUtils.copyFile(tmpNewFile, sourceFile);
            tmpNewFile.delete();
            isSuccess = true;
            getFileChanged().set(true);
```

### IgnoreResultOfCall
Result of `FilterInputStream.skip()` is ignored
in `inlong-common/src/main/java/org/apache/inlong/common/msg/InLongMsg.java`
#### Snippet
```java
                        int len = input.readInt();
                        int pos = input.getPosition();
                        input.skip(len);
                        return ByteBuffer.wrap(uncompressdata, pos, len);
                    } catch (IOException e) {
```

### IgnoreResultOfCall
Result of `FilterInputStream.skip()` is ignored
in `inlong-common/src/main/java/org/apache/inlong/common/msg/InLongMsg.java`
#### Snippet
```java
                bodyBuffer = ByteBuffer.wrap(parsedInput.getData(), pos, len - 1);
            }
            parsedInput.skip(len - 1);

            while (bodyBuffer.remaining() > 0) {
```

### IgnoreResultOfCall
Result of `DataInputStream.read()` is ignored
in `inlong-common/src/main/java/org/apache/inlong/common/msg/InLongMsg.java`
#### Snippet
```java
                        len = input.readInt();
                        byte[] res = new byte[len];
                        input.read(res);
                        return res;
                    } catch (IOException e) {
```

### IgnoreResultOfCall
Result of `FilterInputStream.skip()` is ignored
in `inlong-common/src/main/java/org/apache/inlong/common/msg/InLongMsg.java`
#### Snippet
```java
                    new DataByteBuffer(cnt, ByteBuffer.wrap(
                            parsedInput.getData(), pos, len)));
            parsedInput.skip(len);
        }
    }
```

### IgnoreResultOfCall
Result of `ExecutorService.awaitTermination()` is ignored
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/benchemark/RcpService4BenchmarkClient.java`
#### Snippet
```java
        }
        executorService.shutdown();
        executorService.awaitTermination(Long.MAX_VALUE, TimeUnit.MILLISECONDS);
    }
}
```

### IgnoreResultOfCall
Result of `ExecutorService.awaitTermination()` is ignored
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/tools/StoreRepairAdmin.java`
#### Snippet
```java
        executor.shutdown();
        try {
            executor.awaitTermination(30 * 1000, TimeUnit.MILLISECONDS);
        } catch (InterruptedException e) {
            //
```

### IgnoreResultOfCall
Result of `StringBuilder.toString()` is ignored
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/TubeBroker.java`
#### Snippet
```java
                .append(",brokerDefaultConfInfo=").append(response.getBrokerDefaultConfInfo())
                .append(",brokerTopicSetConfList=")
                .append(response.getBrokerTopicSetConfInfoList().toString()).toString();
        strBuff.delete(0, strBuff.length());
        metadataManager.updateBrokerTopicConfigMap(response.getCurBrokerConfId(),
```

### IgnoreResultOfCall
Result of `File.mkdir()` is ignored
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/tools/cli/CliMetaDataBRU.java`
#### Snippet
```java
            logger.info("Store address is " + file.getAbsolutePath());
            if (!file.getParentFile().exists()) {
                file.getParentFile().mkdir();
            }
            if (!file.exists()) {
```

### IgnoreResultOfCall
Result of `File.createNewFile()` is ignored
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/tools/cli/CliMetaDataBRU.java`
#### Snippet
```java
            }
            if (!file.exists()) {
                file.createNewFile();
            }
            fos = new FileOutputStream(file);
```

### IgnoreResultOfCall
Result of `Condition.awaitNanos()` is ignored
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/MessageStore.java`
#### Snippet
```java
            startTime = System.currentTimeMillis();
            while (hasFlushBeenTriggered.get()) {
                flushWriteCacheCondition.awaitNanos(FLUSH_CONDITION_WAIT_DLT_NS);
                if (System.currentTimeMillis() - startTime > 2000) {
                    logger.warn(new StringBuilder(512)
```

### IgnoreResultOfCall
Result of `File.delete()` is ignored
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/disk/FileSegment.java`
#### Snippet
```java
                    .append("[File Store] delete file ")
                    .append(file.getAbsoluteFile()).toString());
            this.file.delete();
        } catch (Throwable ee) {
            if (ee instanceof IOException) {
```

### IgnoreResultOfCall
Result of `File.delete()` is ignored
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/MessageStoreManager.java`
#### Snippet
```java
        if (targetFile.exists()) {
            if (targetFile.isFile()) {
                targetFile.delete();
            } else if (targetFile.isDirectory()) {
                File[] files = targetFile.listFiles();
```

### IgnoreResultOfCall
Result of `File.delete()` is ignored
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/MessageStoreManager.java`
#### Snippet
```java
                }
            }
            targetFile.delete();
        }
    }
```

### IgnoreResultOfCall
Result of `TreeSet.comparator()` is ignored
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/utils/WebParameterUtils.java`
#### Snippet
```java
                }
            }
            valItemSet.comparator();
            result.setSuccResult(valItemSet);
        } else {
```

### IgnoreResultOfCall
Result of `TreeSet.comparator()` is ignored
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/utils/WebParameterUtils.java`
#### Snippet
```java
                valItemSet.add(defValue);
            }
            valItemSet.comparator();
            result.setSuccResult(valItemSet);
        } else {
```

### IgnoreResultOfCall
Result of `TreeSet.comparator()` is ignored
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/utils/WebParameterUtils.java`
#### Snippet
```java
                    sBuffer.delete(0, sBuffer.length());
                }
                newFilterCondSet.comparator();
            }
            result.setSuccResult(newFilterCondSet);
```

### IgnoreResultOfCall
Result of `InputStream.read()` is ignored
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/simplemvc/MultipartHttpServletRequest.java`
#### Snippet
```java
            int dataSize = (int) dataPart.getSize();
            byte[] buffer = new byte[dataSize];
            dataPart.getInputStream().read(buffer, 0, dataSize);
            String dataValue = new String(buffer, StandardCharsets.UTF_8);
            cachedParams.put(dataKey, dataValue);
```

### IgnoreResultOfCall
Result of `File.mkdirs()` is ignored
in `inlong-manager/manager-plugins/src/main/java/org/apache/inlong/manager/plugin/util/FlinkUtils.java`
#### Snippet
```java
        File file = new File(configJobDirectory);
        if (!file.exists()) {
            file.mkdirs();
        }
        String filePath = configJobDirectory + File.separator + configFileName;
```

### IgnoreResultOfCall
Result of `File.delete()` is ignored
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/config/holder/PropertiesConfigHolder.java`
#### Snippet
```java

            FileUtils.copyFile(tmpNewFile, sourceFile);
            tmpNewFile.delete();
            isSuccess = true;
            getFileChanged().set(true);
```

## RuleId[id=AccessStaticViaInstance]
### AccessStaticViaInstance
Static member 'org.apache.inlong.sdk.dataproxy.SendResult.OK' accessed via instance reference
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/example/SendMsgThread.java`
#### Snippet
```java
                        TimeUnit.MILLISECONDS);
                long endTime = System.currentTimeMillis();
                if (result == result.OK) {
                    logger.info("this msg is ok time {}", endTime - startTime);
                } else {
```

### AccessStaticViaInstance
Static member 'org.apache.inlong.sort.cdc.mongodb.debezium.internal.FlinkDatabaseSchemaHistory.latestTables' accessed via instance reference
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mongodb-cdc/src/main/java/org/apache/inlong/sort/cdc/mongodb/debezium/internal/FlinkDatabaseSchemaHistory.java`
#### Snippet
```java

        // recover
        this.latestTables = new ConcurrentHashMap<>();
        for (SchemaRecord schemaRecord : DatabaseHistoryUtil.retrieveHistory(instanceName)) {
            // validate here
```

### AccessStaticViaInstance
Static member 'java.lang.Thread.sleep(long)' accessed via instance reference
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/PulsarSink.java`
#### Snippet
```java
        while (eventQueue.size() != 0 && waitCount++ < 10) {
            try {
                Thread.currentThread().sleep(1000);
            } catch (InterruptedException e) {
                logger.info("Stop thread has been interrupt!");
```

### AccessStaticViaInstance
Static member 'java.lang.Thread.sleep(long)' accessed via instance reference
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/PulsarSink.java`
#### Snippet
```java
                    if (PulsarSink.this.overflow) {
                        PulsarSink.this.overflow = false;
                        Thread.currentThread().sleep(10);
                    }
                    if (!resendQueue.isEmpty()) {
```

### AccessStaticViaInstance
Static member 'org.apache.flume.interceptor.InterceptorBuilderFactory.newInstance(java.lang.String)' accessed via instance reference
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/channel/FailoverChannelProcessor.java`
#### Snippet
```java
            }
            try {
                Interceptor.Builder builder = factory.newInstance(type);
                builder.configure(interceptorContext);
                interceptors.add(builder.build());
```

### AccessStaticViaInstance
Static member 'java.lang.Thread.sleep(long)' accessed via instance reference
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/TubeSink.java`
#### Snippet
```java
        while (eventQueue.size() != 0 && waitCount++ < 10) {
            try {
                Thread.currentThread().sleep(1000);
            } catch (InterruptedException e) {
                logger.info("Stop thread has been interrupt!");
```

### AccessStaticViaInstance
Static member 'org.apache.flume.interceptor.InterceptorBuilderFactory.newInstance(java.lang.String)' accessed via instance reference
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/channel/FailoverChannelProcessor.java`
#### Snippet
```java
            }
            try {
                Interceptor.Builder builder = factory.newInstance(type);
                builder.configure(interceptorContext);
                interceptors.add(builder.build());
```

## RuleId[id=RedundantMethodOverride]
### RedundantMethodOverride
Method `onMessageAck()` is identical to its super method
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/threads/MetricWorkerThread.java`
#### Snippet
```java

        @Override
        public void onMessageAck(SendResult result) {

        }
```

### RedundantMethodOverride
Method `getKeyIndex()` is identical to its super method
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/redis/src/main/java/org/apache/inlong/sort/redis/common/schema/impl/HashStaticKvPairSchema.java`
#### Snippet
```java

    @Override
    public int getKeyIndex() {
        return 0;
    }
```

### RedundantMethodOverride
Method `getKeyIndex()` is identical to its super method
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/redis/src/main/java/org/apache/inlong/sort/redis/common/schema/impl/HashDynamicSchema.java`
#### Snippet
```java

    @Override
    public int getKeyIndex() {
        return 0;
    }
```

### RedundantMethodOverride
Method `snapshotState()` is identical to its super method
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/elasticsearch-base/src/main/java/org/apache/inlong/sort/elasticsearch/table/ElasticsearchSinkFunctionBase.java`
#### Snippet
```java

    @Override
    public void snapshotState(FunctionSnapshotContext context) {
    }

```

### RedundantMethodOverride
Method `initializeState()` is identical to its super method
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/elasticsearch-base/src/main/java/org/apache/inlong/sort/elasticsearch/table/ElasticsearchSinkFunctionBase.java`
#### Snippet
```java

    @Override
    public void initializeState(FunctionInitializationContext context) {
    }

```

### RedundantMethodOverride
Method `snapshotState()` is identical to its super method
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/elasticsearch-base/src/main/java/org/apache/inlong/sort/elasticsearch/table/MultipleElasticsearchSinkFunctionBase.java`
#### Snippet
```java

    @Override
    public void snapshotState(FunctionSnapshotContext context) {
    }

```

### RedundantMethodOverride
Method `initializeState()` is identical to its super method
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/elasticsearch-base/src/main/java/org/apache/inlong/sort/elasticsearch/table/MultipleElasticsearchSinkFunctionBase.java`
#### Snippet
```java

    @Override
    public void initializeState(FunctionInitializationContext context) {
    }

```

### RedundantMethodOverride
Method `name()` is identical to its super method
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/listener/source/SourceRestartListener.java`
#### Snippet
```java

    @Override
    public String name() {
        return getClass().getSimpleName();
    }
```

### RedundantMethodOverride
Method `name()` is identical to its super method
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/listener/source/SourceDeleteListener.java`
#### Snippet
```java

    @Override
    public String name() {
        return getClass().getSimpleName();
    }
```

### RedundantMethodOverride
Method `name()` is identical to its super method
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/listener/source/SourceStopListener.java`
#### Snippet
```java

    @Override
    public String name() {
        return getClass().getSimpleName();
    }
```

## RuleId[id=RegExpDuplicateCharacterInClass]
### RegExpDuplicateCharacterInClass
Duplicate character '/' inside character class
in `inlong-manager/manager-pojo/src/main/java/org/apache/inlong/manager/pojo/sink/mysql/MySQLSinkDTO.java`
#### Snippet
```java
            return jdbcUrl;
        }
        String pattern = "jdbc:mysql://(?<host>[a-zA-Z0-9-//.]+):(?<port>[0-9]+)?(?<ext>)";
        Pattern namePattern = Pattern.compile(pattern);
        Matcher dataMatcher = namePattern.matcher(jdbcUrl);
```

## RuleId[id=IntegerMultiplicationImplicitCastToLong]
### IntegerMultiplicationImplicitCastToLong
randSleepTime \* 1000: integer multiplication implicitly cast to long
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/HttpProxySender.java`
#### Snippet
```java
                int rand = ThreadLocalRandom.current().nextInt(0, 600);
                int randSleepTime = proxyClientConfig.getProxyHttpUpdateIntervalMinutes() * 60 + rand;
                TimeUnit.MILLISECONDS.sleep(randSleepTime * 1000);
                if (proxyConfigManager != null) {
                    ProxyConfigEntry proxyConfigEntry = proxyConfigManager.getGroupIdConfigure();
```

### IntegerMultiplicationImplicitCastToLong
sleepTimeSec \* 1000: integer multiplication implicitly cast to long
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/config/ProxyConfigManager.java`
#### Snippet
```java
                }
                LOGGER.info("sleep time {}", sleepTimeSec);
                Thread.sleep(sleepTimeSec * 1000);
            } catch (Throwable e2) {
                //
```

### IntegerMultiplicationImplicitCastToLong
1000 \* i: integer multiplication implicitly cast to long
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/jdbc/src/main/java/org/apache/inlong/sort/jdbc/internal/JdbcBatchingOutputFormat.java`
#### Snippet
```java
                }
                try {
                    Thread.sleep(1000 * i);
                } catch (InterruptedException ex) {
                    Thread.currentThread().interrupt();
```

### IntegerMultiplicationImplicitCastToLong
1000 \* retryTimes: integer multiplication implicitly cast to long
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/jdbc/src/main/java/org/apache/inlong/sort/jdbc/internal/JdbcMultiBatchingOutputFormat.java`
#### Snippet
```java
                            updateOneExecutor(true, tableIdentifier);
                            try {
                                Thread.sleep(1000 * retryTimes);
                            } catch (InterruptedException ex) {
                                Thread.currentThread().interrupt();
```

### IntegerMultiplicationImplicitCastToLong
getRuntimeContext().getNumberOfParallelSubtasks() \* kafkaProducersPoolSize: integer multiplication implicitly cast to long
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kafka/src/main/java/org/apache/inlong/sort/kafka/FlinkKafkaProducer.java`
#### Snippet
```java
            if (getRuntimeContext().getNumberOfParallelSubtasks() > nextTransactionalIdHint.lastParallelism) {
                nextFreeTransactionalId +=
                        getRuntimeContext().getNumberOfParallelSubtasks() * kafkaProducersPoolSize;
            }

```

### IntegerMultiplicationImplicitCastToLong
1000 \* retry: integer multiplication implicitly cast to long
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/redis/src/main/java/org/apache/inlong/sort/redis/source/RedisRowDataLookupFunction.java`
#### Snippet
```java
                }
                try {
                    Thread.sleep(1000 * retry);
                } catch (InterruptedException e1) {
                    throw new RuntimeException(e1);
```

### IntegerMultiplicationImplicitCastToLong
splitContext.getChunkSizeMB() \* 1024 \* 1024: integer multiplication implicitly cast to long
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mongodb-cdc/src/main/java/org/apache/inlong/sort/cdc/mongodb/source/assigners/splitters/SampleBucketSplitStrategy.java`
#### Snippet
```java
    @Override
    public Collection<SnapshotSplit> split(SplitContext splitContext) {
        long chunkSizeInBytes = splitContext.getChunkSizeMB() * 1024 * 1024;

        long sizeInBytes = splitContext.getSizeInBytes();
```

### IntegerMultiplicationImplicitCastToLong
this.intervalSec \* 1000: integer multiplication implicitly cast to long
in `inlong-common/src/main/java/org/apache/inlong/common/monitor/StatRunner.java`
#### Snippet
```java
        while (!this.shutDownFlag) {
            try {
                Thread.sleep((long) (this.intervalSec * 1000));
                Iterator iterator = this.moniterNames.iterator();

```

### IntegerMultiplicationImplicitCastToLong
cacheClusterReselectIntervalMinutes \* 60 \* 1000: integer multiplication implicitly cast to long
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/RandomCacheClusterSelector.java`
#### Snippet
```java
                context.getString(KEY_CACHE_CLUSTER_RESELECT_INTERVAL_MINUTES),
                DEFAULT_CACHE_CLUSTER_RESELECT_INTERVAL_MINUTES);
        long cacheClusterReselectIntervalMs = cacheClusterReselectIntervalMinutes * 60 * 1000;
        boolean needReselectCacheCluster = (System.currentTimeMillis()
                - lastCacheClusterSelectTime) > cacheClusterReselectIntervalMs;
```

## RuleId[id=UNUSED_IMPORT]
### UNUSED_IMPORT
Unused import `import static org.apache.inlong.common.metric.MetricItemMBean.DOMAIN_SEPARATOR;`
in `inlong-sdk/sort-sdk/src/main/java/org/apache/inlong/sdk/sort/metrics/SortSdkPrometheusMetricListener.java`
#### Snippet
```java
import javax.management.ObjectName;

import static org.apache.inlong.common.metric.MetricItemMBean.DOMAIN_SEPARATOR;
import static org.apache.inlong.common.metric.MetricRegister.JMX_DOMAIN;
import static org.apache.inlong.sdk.sort.metrics.SortSdkMetricItem.*;
```

### UNUSED_IMPORT
Unused import `import static org.apache.inlong.sort.base.Constants.INLONG_AUDIT;`
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/jdbc/src/main/java/org/apache/inlong/sort/jdbc/table/JdbcDynamicTableFactory.java`
#### Snippet
```java

import static org.apache.inlong.sort.base.Constants.DIRTY_PREFIX;
import static org.apache.inlong.sort.base.Constants.INLONG_AUDIT;
import static org.apache.inlong.sort.base.Constants.INLONG_METRIC;
import static org.apache.inlong.sort.base.Constants.INLONG_AUDIT;
```

### UNUSED_IMPORT
Unused import `import static org.apache.inlong.sort.base.Constants.DATASOURCE_PARTITION_MAP;`
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kafka/src/main/java/org/apache/inlong/sort/kafka/table/KafkaDynamicTableFactory.java`
#### Snippet
```java
import static org.apache.flink.table.factories.FactoryUtil.SINK_PARALLELISM;
import static org.apache.inlong.sort.base.Constants.*;
import static org.apache.inlong.sort.base.Constants.DATASOURCE_PARTITION_MAP;
import static org.apache.inlong.sort.kafka.table.KafkaOptions.KAFKA_IGNORE_ALL_CHANGELOG;

```

## RuleId[id=MismatchedCollectionQueryUpdate]
### MismatchedCollectionQueryUpdate
Contents of collection `dimensionKeys` are updated, but never queried
in `inlong-sdk/sort-sdk/src/main/java/org/apache/inlong/sdk/sort/metrics/SortSdkPrometheusMetricListener.java`
#### Snippet
```java
    private SortSdkMetricItem metricItem;
    private Map<String, AtomicLong> metricValueMap = new ConcurrentHashMap<>();
    private List<String> dimensionKeys = new ArrayList<>();

    public SortSdkPrometheusMetricListener() {
```

### MismatchedCollectionQueryUpdate
Contents of collection `CONSUMER_STARTUP_MODE_ENUMS` are updated, but never queried
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/tubemq/src/main/java/org/apache/inlong/sort/tubemq/table/TubeMQOptions.java`
#### Snippet
```java
    // Validation
    // --------------------------------------------------------------------------------------------
    private static final Set<String> CONSUMER_STARTUP_MODE_ENUMS = new HashSet<>(Arrays.asList(
            CONSUMER_FROM_MAX_OFFSET_ALWAYS,
            CONSUMER_FROM_LATEST_OFFSET,
```

### MismatchedCollectionQueryUpdate
Contents of collection `compatibleClasses` are queried, but never updated
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/util/InstantiationUtil.java`
#### Snippet
```java

        // initialize this set to support compatible manually
        private static final Set<String> compatibleClasses = new HashSet<>();

        public FailureTolerantObjectInputStream(InputStream in, ClassLoader cl) throws IOException {
```

### MismatchedCollectionQueryUpdate
Contents of collection `dimensions` are updated, but never queried
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/sinks/SenderManager.java`
#### Snippet
```java
    // metric
    private AgentMetricItemSet metricItemSet;
    private Map<String, String> dimensions;
    private TaskPositionManager taskPositionManager;
    private int ioThreadNum;
```

### MismatchedCollectionQueryUpdate
Contents of collection `metaInfoSet` are updated, but never queried
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/RmtDataCache.java`
#### Snippet
```java
    private final AtomicLong topicMetaInfoId =
            new AtomicLong(TBaseConstants.META_VALUE_UNDEFINED);
    private final Set<String> metaInfoSet = new TreeSet<>();
    private ConcurrentHashMap<String, Tuple2<Partition, Integer>> configuredPartInfoMap =
            new ConcurrentHashMap<>();
```

### MismatchedCollectionQueryUpdate
Contents of collection `tempParam` are updated, but never queried
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/common/BaseResult.java`
#### Snippet
```java
    private int startRow;
    private int endRow;
    private Map tempParam;
    private Object removeObject;
    // for ajax
```

### MismatchedCollectionQueryUpdate
Contents of collection `rsvGroupNameSet` are updated, but never queried
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebAdminFlowRuleHandler.java`
#### Snippet
```java
    private static final String blankFlowCtrlRules = "[]";
    private static final List<Integer> allowedPriorityVal = Arrays.asList(1, 2, 3);
    private static final Set<String> rsvGroupNameSet =
            new HashSet<>(Arrays.asList(TServerConstants.TOKEN_DEFAULT_FLOW_CONTROL));

```

### MismatchedCollectionQueryUpdate
Contents of collection `consumerMap` are updated, but never queried
in `inlong-tubemq/tubemq-example/src/main/java/org/apache/inlong/tubemq/example/MessagePushConsumerExample.java`
#### Snippet
```java
            new MsgSendReceiveStats(false);
    private static MessageSessionFactory sessionFactory;
    private static final Map<String, PushMessageConsumer> consumerMap = new HashMap<>();

    /**
```

### MismatchedCollectionQueryUpdate
Contents of collection `dimensions` are updated, but never queried
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/pulsar/PulsarFederationSink.java`
#### Snippet
```java
    private PulsarFederationSinkContext context;
    private List<PulsarFederationWorker> workers = new ArrayList<>();
    private Map<String, String> dimensions;

    /**
```

## RuleId[id=PointlessBitwiseExpression]
### PointlessBitwiseExpression
`i ^ 0xFFFFFFFF` can be replaced with '\~i'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/utils/WritableComparator.java`
#### Snippet
```java
            i |= bytes[(start + 1 + idx)] & 0xFF;
        }
        return isNegative ? i ^ 0xFFFFFFFF : i;
    }

```

### PointlessBitwiseExpression
`((long) (readInt(bytes, start)) << 32) + ((long) (readInt(bytes, start + 4))) & 0xFFFFFFFF` can be replaced with '((long) (readInt(bytes, start)) \<\< 32) + ((long) (readInt(bytes, start + 4)))'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/utils/WritableComparator.java`
#### Snippet
```java

    public static long readLong(byte[] bytes, int start) {
        return ((long) (readInt(bytes, start)) << 32) + ((long) (readInt(bytes, start + 4))) & 0xFFFFFFFF;
    }

```

### PointlessBitwiseExpression
`-1L ^ -1L << sequenceBits` can be replaced with '\~(-1L \<\< sequenceBits)'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/utils/IdWorker.java`
#### Snippet
```java
    private static final long workerIdShift = sequenceBits;
    private static final long timestampLeftShift = sequenceBits + workerIdBits;
    private static final long sequenceMask = -1L ^ -1L << sequenceBits;
    private final long workerId;
    private long sequence = 0L;
```

### PointlessBitwiseExpression
`-1L ^ -1L << workerIdBits` can be replaced with '\~(-1L \<\< workerIdBits)'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/utils/IdWorker.java`
#### Snippet
```java
    private static final long twepoch = 1350282310830L;
    private static final long workerIdBits = 10L;
    private static final long maxWorkerId = -1L ^ -1L << workerIdBits;
    private static final long sequenceBits = 12L;
    private static final long workerIdShift = sequenceBits;
```

## RuleId[id=RedundantExplicitClose]
### RedundantExplicitClose
Redundant 'close()'
in `inlong-audit/audit-sdk/src/main/java/org/apache/inlong/audit/util/Config.java`
#### Snippet
```java
            }
            dockerId = dockerID.substring(dockerID.length() - DOCKERID_LENGTH);
            in.close();
        } catch (Exception ex) {
            logger.error(ex.toString());
```

## RuleId[id=DuplicateThrows]
### DuplicateThrows
There is a more general exception, 'java.lang.Exception', in the throws list already.
in `inlong-sdk/sdk-common/src/main/java/org/apache/inlong/sdk/commons/admin/AdminJsonHandler.java`
#### Snippet
```java
    @Override
    public List<Event> getEvents(HttpServletRequest request, HttpServletResponse response)
            throws HTTPBadRequestException, Exception {
        List<Event> events = this.requestHandler.getEvents(request);
        for (Event event : events) {
```

### DuplicateThrows
There is a more general exception, 'java.io.IOException', in the throws list already.
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/zookeeper/ZooKeeperWatcher.java`
#### Snippet
```java
    private String masterAddressZNode;

    public ZooKeeperWatcher(ZKConfig conf) throws ZooKeeperConnectionException, IOException {
        this(conf, null, false);
    }
```

### DuplicateThrows
There is a more general exception, 'java.io.IOException', in the throws list already.
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/zookeeper/ZooKeeperWatcher.java`
#### Snippet
```java
     */
    public ZooKeeperWatcher(ZKConfig conf, Abortable abortable, boolean canCreateBaseZNode)
            throws IOException, ZooKeeperConnectionException {
        this.conf = conf;
        // Capture a stack trace now. Will print it out later if problem so we
```

### DuplicateThrows
There is a more general exception, 'java.io.IOException', in the throws list already.
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/zookeeper/ZooKeeperWatcher.java`
#### Snippet
```java
     * instance.
     */
    public ZooKeeperWatcher(ZKConfig conf, Abortable abortable) throws ZooKeeperConnectionException,
            IOException {
        this(conf, abortable, false);
```

### DuplicateThrows
There is a more general exception, 'org.apache.zookeeper.KeeperException', in the throws list already.
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/zookeeper/ZKUtil.java`
#### Snippet
```java
     */
    public static boolean setData(ZooKeeperWatcher zkw, String zkNode, byte[] data, int expectedVersion)
            throws KeeperException, KeeperException.NoNodeException {
        try {
            return zkw.getRecoverableZooKeeper().setData(zkNode, data, expectedVersion) != null;
```

## RuleId[id=StringConcatenationInLoops]
### StringConcatenationInLoops
String concatenation `+` in loop
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/jdbc/src/main/java/org/apache/inlong/sort/jdbc/internal/JdbcMultiBatchingOutputFormat.java`
#### Snippet
```java
            for (Map.Entry<String, Exception> entry : tableExceptionMap.entrySet()) {
                LOG.error("Writing table:{} get err:{}", entry.getKey(), entry.getValue().getMessage());
                tableErr = tableErr + entry.getKey() + ",";
            }
            throw new RuntimeException(tableErr.substring(0, tableErr.length() - 1));
```

## RuleId[id=UnusedAssignment]
### UnusedAssignment
Variable `topicHashId` initializer `0` is redundant
in `inlong-sdk/sort-sdk/src/main/java/org/apache/inlong/sdk/sort/api/SortClientConfig.java`
#### Snippet
```java
        int containerHashId = Math.abs(this.containerId.hashCode()) % this.consumerSubsetSize;
        for (InLongTopic topic : totalTopics) {
            int topicHashId = 0;
            if (this.consumerSubsetType == ConsumerSubsetType.CLUSTER) {
                String hashString = topic.getInLongCluster().getClusterId();
```

### UnusedAssignment
Variable `compressBytes` initializer `null` is redundant
in `inlong-sdk/sdk-common/src/main/java/org/apache/inlong/sdk/commons/protocol/EventUtils.java`
#### Snippet
```java
        byte[] srcBytes = objs.build().toByteArray();
        // compress
        byte[] compressBytes = null;
        switch (compressedType) {
            case INLONG_SNAPPY:
```

### UnusedAssignment
Variable `compressedBytes` initializer `null` is redundant
in `inlong-sdk/sdk-common/src/main/java/org/apache/inlong/sdk/commons/protocol/EventUtils.java`
#### Snippet
```java
        MessageObjs objs = objsBuilder.build();
        byte[] srcBytes = objs.toByteArray();
        byte[] compressedBytes = null;
        switch (compressedType) {
            case INLONG_SNAPPY:
```

### UnusedAssignment
Variable `srcBytes` initializer `null` is redundant
in `inlong-sdk/sdk-common/src/main/java/org/apache/inlong/sdk/commons/protocol/EventUtils.java`
#### Snippet
```java
        // decompress
        byte[] compressBytes = packObject.getCompressBytes().toByteArray();
        byte[] srcBytes = null;
        switch (header.getCompressType()) {
            case INLONG_SNAPPY:
```

### UnusedAssignment
Variable `srcBytes` initializer `null` is redundant
in `inlong-sdk/sdk-common/src/main/java/org/apache/inlong/sdk/commons/protocol/EventUtils.java`
#### Snippet
```java
            INLONG_COMPRESSED_TYPE compressedType, byte[] msgBody) throws IOException {
        // uncompress
        byte[] srcBytes = null;
        switch (compressedType) {
            case INLONG_SNAPPY:
```

### UnusedAssignment
Variable `events` initializer `Collections.emptyList()` is redundant
in `inlong-sdk/sdk-common/src/main/java/org/apache/inlong/sdk/commons/admin/AdminHttpSource.java`
#### Snippet
```java
        public void doPost(HttpServletRequest request, HttpServletResponse response)
                throws IOException {
            List<Event> events = Collections.emptyList(); // create empty list
            try {
                events = handler.getEvents(request, response);
```

### UnusedAssignment
Variable `proxyIPServiceURL` initializer `""` is redundant
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/ProxyClientConfig.java`
#### Snippet
```java

    private String managerIpLocalPath = System.getProperty("user.dir") + "/.inlong/.managerIps";
    private String proxyIPServiceURL = "";
    private int proxyUpdateIntervalMinutes;
    private int proxyUpdateMaxRetry;
```

### UnusedAssignment
Variable `managerIP` initializer `""` is redundant
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/ProxyClientConfig.java`
#### Snippet
```java
    private int asyncCallbackSize;
    private int managerPort = 8099;
    private String managerIP = "";

    private String managerIpLocalPath = System.getProperty("user.dir") + "/.inlong/.managerIps";
```

### UnusedAssignment
Variable `managerPort` initializer `8099` is redundant
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/ProxyClientConfig.java`
#### Snippet
```java
    private int syncThreadPoolSize;
    private int asyncCallbackSize;
    private int managerPort = 8099;
    private String managerIP = "";

```

### UnusedAssignment
Variable `isLocalVisit` initializer `true` is redundant
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/ProxyClientConfig.java`
#### Snippet
```java
    private String groupId;
    private boolean isFile = false;
    private boolean isLocalVisit = true;
    private boolean isNeedDataEncry = false;
    private boolean needAuthentication = false;
```

### UnusedAssignment
Variable `responseInfo` initializer `null` is redundant
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/SdkProxyChannelManager.java`
#### Snippet
```java
        nettyBuffer.readBytes(responseBytes);
        // unpack
        ResponseInfo responseInfo = null;
        try {
            responseInfo = ResponseInfo.parseFrom(responseBytes);
```

### UnusedAssignment
Variable `destAddr` initializer `null` is redundant
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/SdkProxyChannelManager.java`
#### Snippet
```java
     */
    public static InetSocketAddress parseInetSocketAddress(Channel channel) {
        InetSocketAddress destAddr = null;
        if (channel.getRemoteAddress() instanceof InetSocketAddress) {
            destAddr = (InetSocketAddress) channel.getRemoteAddress();
```

### UnusedAssignment
Variable `sizeSemaphore` initializer `null` is redundant
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/channel/SizeSemaphore.java`
#### Snippet
```java
    private int maxSize = 0;
    private int leftSize = 0;
    private Semaphore sizeSemaphore = null;
    private AtomicInteger leftSemaphore = new AtomicInteger(0);

```

### UnusedAssignment
Variable `leftSize` initializer `0` is redundant
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/channel/SizeSemaphore.java`
#### Snippet
```java

    private int maxSize = 0;
    private int leftSize = 0;
    private Semaphore sizeSemaphore = null;
    private AtomicInteger leftSemaphore = new AtomicInteger(0);
```

### UnusedAssignment
Variable `maxSize` initializer `0` is redundant
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/channel/SizeSemaphore.java`
#### Snippet
```java
    public static final int ONEKB = 1024;

    private int maxSize = 0;
    private int leftSize = 0;
    private Semaphore sizeSemaphore = null;
```

### UnusedAssignment
Variable `auditFormatInterval` initializer `60000L` is redundant
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/context/SinkContext.java`
#### Snippet
```java
    protected final long processInterval;
    protected final long reloadInterval;
    protected long auditFormatInterval = 60000L;
    // metric
    protected final SdkMetricItemSet metricItemSet;
```

### UnusedAssignment
Variable `ipv6Address` initializer `null` is redundant
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/utils/IpUtils.java`
#### Snippet
```java
        /* loop to Ethernet cards, find a valid ip */
        final Enumeration<NetworkInterface> enumeration = NetworkInterface.getNetworkInterfaces();
        InetAddress ipv6Address = null;
        while (enumeration.hasMoreElements()) {
            final NetworkInterface networkInterface = enumeration.nextElement();
```

### UnusedAssignment
Variable `ipv6Address` initializer `null` is redundant
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/utils/IpUtils.java`
#### Snippet
```java
            /* loop to Ethernet cards, find a valid ip */
            final Enumeration<NetworkInterface> enumeration = NetworkInterface.getNetworkInterfaces();
            InetAddress ipv6Address = null;
            while (enumeration.hasMoreElements()) {
                final NetworkInterface networkInterface = enumeration.nextElement();
```

### UnusedAssignment
Variable `encryptInfo` initializer `null` is redundant
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/config/EncryptConfigEntry.java`
#### Snippet
```java

    public EncryptInfo getRsaEncryptInfo() {
        EncryptInfo encryptInfo = null;
        long visitTime = this.lastUpdateTime.get();
        if (rsaEncryptedKey != null && (System.currentTimeMillis() - visitTime) <= 3 * 60 * 1000) {
```

### UnusedAssignment
The value `null` assigned to `encryptInfo` is never used
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/config/EncryptConfigEntry.java`
#### Snippet
```java
                return encryptInfo;
            }
            encryptInfo = null;
        }
        synchronized (this.lastUpdateTime) {
```

### UnusedAssignment
Variable `userName` initializer `""` is redundant
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/config/EncryptConfigEntry.java`
#### Snippet
```java

    private static final Logger logger = LoggerFactory.getLogger(EncryptConfigEntry.class);
    private String userName = "";
    private String version;
    private String pubKey;
```

### UnusedAssignment
The value `br.readLine()` assigned to `line` is never used
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/example/SendMsgThread.java`
#### Snippet
```java
            BufferedReader br = new BufferedReader(reader);
            String line = null;
            while ((line = br.readLine()) != null) {

                long startTime = System.currentTimeMillis();
```

### UnusedAssignment
Variable `messageSender` initializer `null` is redundant
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/example/SendMsgThread.java`
#### Snippet
```java

    private static final Logger logger = LoggerFactory.getLogger(SendMsgThread.class);
    private DefaultMessageSender messageSender = null;

    public SendMsgThread(DefaultMessageSender messageSender) {
```

### UnusedAssignment
Variable `proxyConfig` initializer `null` is redundant
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/example/HttpClientExample.java`
#### Snippet
```java
            boolean isLocalVisit, boolean isReadProxyIPFromLocal,
            String configBasePath) {
        ProxyClientConfig proxyConfig = null;
        HttpProxySender sender = null;
        try {
```

### UnusedAssignment
Variable `ip` initializer `null` is redundant
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/SequentialID.java`
#### Snippet
```java

    private static final long maxId = 2000000000;
    private String ip = null;
    private AtomicLong id = new AtomicLong(0);

```

### UnusedAssignment
Variable `dataProxyConfig` initializer `null` is redundant
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/example/TcpClientExample.java`
#### Snippet
```java
            String netTag, String dataProxyGroup, boolean isLocalVisit, boolean isReadProxyIPFromLocal,
            String configBasePath, int msgType) {
        ProxyClientConfig dataProxyConfig = null;
        DefaultMessageSender messageSender = null;
        try {
```

### UnusedAssignment
Variable `readLine` initializer `null` is redundant
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/utils/EncryptUtil.java`
#### Snippet
```java
        try {
            BufferedReader br = new BufferedReader(new FileReader(path));
            String readLine = null;
            StringBuilder sb = new StringBuilder();
            while ((readLine = br.readLine()) != null) {
```

### UnusedAssignment
Variable `cipher` initializer `null` is redundant
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/utils/EncryptUtil.java`
#### Snippet
```java
            throw new Exception("public key is null");
        }
        Cipher cipher = null;
        try {

```

### UnusedAssignment
Variable `readLine` initializer `null` is redundant
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/utils/EncryptUtil.java`
#### Snippet
```java
        try {
            BufferedReader br = new BufferedReader(new FileReader(path));
            String readLine = null;
            StringBuilder sb = new StringBuilder();
            while ((readLine = br.readLine()) != null) {
```

### UnusedAssignment
Variable `cipher` initializer `null` is redundant
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/utils/EncryptUtil.java`
#### Snippet
```java
            throw new Exception("private key is null");
        }
        Cipher cipher = null;
        try {
            cipher = Cipher.getInstance("RSA");
```

### UnusedAssignment
Variable `bShutDown` initializer `false` is redundant
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/threads/TimeoutScanThread.java`
#### Snippet
```java
    private final ClientMgr clientMgr;
    private final ConcurrentHashMap<Channel, TimeScanObject> timeoutChannelStat = new ConcurrentHashMap<>();
    private volatile boolean bShutDown = false;
    private long printCount = 0;

```

### UnusedAssignment
Variable `printCount` initializer `0` is redundant
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/threads/TimeoutScanThread.java`
#### Snippet
```java
    private final ConcurrentHashMap<Channel, TimeScanObject> timeoutChannelStat = new ConcurrentHashMap<>();
    private volatile boolean bShutDown = false;
    private long printCount = 0;

    public TimeoutScanThread(ConcurrentHashMap<Channel, ConcurrentHashMap<String, QueueObject>> callbacks,
```

### UnusedAssignment
Variable `newValue` initializer `""` is redundant
in `inlong-sort/sort-core/src/main/java/org/apache/inlong/sort/function/EncryptFunction.java`
#### Snippet
```java
    public String eval(String field, String key, String encrypt) {
        if (field != null) {
            String newValue = "";
            EncryptionType encryptionType = EncryptionType.getInstance(encrypt);
            switch (encryptionType) {
```

### UnusedAssignment
Variable `p` initializer `null` is redundant
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/config/ProxyConfigManager.java`
#### Snippet
```java
    private void storePubKeyEntry(EncryptConfigEntry entry) {
        FileOutputStream fos = null;
        ObjectOutputStream p = null;
        rw.writeLock().lock();
        try {
```

### UnusedAssignment
Variable `header` initializer `""` is redundant
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/config/ProxyConfigManager.java`
#### Snippet
```java
            return null;
        }
        String header = "";
        header = url.substring(0, headerIdx + 3);
        String tmpUrl = url.substring(headerIdx + 3);
```

### UnusedAssignment
Variable `tailer` initializer `""` is redundant
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/config/ProxyConfigManager.java`
#### Snippet
```java
            return null;
        }
        String tailer = "";
        tailer = tmpUrl.substring(tailerIdx);
        String[] managerIps = localManagerIpList.split(",");
```

### UnusedAssignment
Variable `is` initializer `null` is redundant
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/config/ProxyConfigManager.java`
#### Snippet
```java
        EncryptConfigEntry entry;
        FileInputStream fis = null;
        ObjectInputStream is = null;
        rw.readLock().lock();
        try {
```

### UnusedAssignment
Variable `returnStr` initializer `null` is redundant
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/config/ProxyConfigManager.java`
#### Snippet
```java
        while (true) {
            HttpPost httpPost = null;
            String returnStr = null;
            HttpParams myParams = new BasicHttpParams();
            HttpConnectionParams.setConnectionTimeout(myParams, 10000);
```

### UnusedAssignment
Variable `httpClient` initializer `null` is redundant
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/config/ProxyConfigManager.java`
#### Snippet
```java
            HttpConnectionParams.setConnectionTimeout(myParams, 10000);
            HttpConnectionParams.setSoTimeout(myParams, clientConfig.getManagerSocketTimeout());
            CloseableHttpClient httpClient = null;
            if (this.clientConfig.isLocalVisit()) {
                httpClient = new DefaultHttpClient(myParams);
```

### UnusedAssignment
Variable `proxyConfigEntry` initializer `null` is redundant
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/Sender.java`
#### Snippet
```java
        this.threadPool = Executors.newCachedThreadPool();
        this.clientMgr = new ClientMgr(configure, this, selfDefineFactory);
        ProxyConfigEntry proxyConfigEntry = null;
        try {
            proxyConfigEntry = this.clientMgr.getGroupIdConfigureInfo();
```

### UnusedAssignment
Variable `message` initializer `null` is redundant
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/Sender.java`
#### Snippet
```java
    public String syncSendMessageIndex(EncodeObject encodeObject, String msgUUID, long timeout, TimeUnit timeUnit) {
        try {
            SendResult message = null;
            NettyClient client = chooseProxy.get(encodeObject.getMessageId());
            String proxyip = encodeObject.getProxyIp();
```

### UnusedAssignment
Variable `message` initializer `null` is redundant
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/Sender.java`
#### Snippet
```java
                Utils.getLocalIp(), encodeObject.getDt(), encodeObject.getPackageTime(), encodeObject.getRealCnt());
        NettyClient client = clientMgr.getClient(clientMgr.getLoadBalance(), encodeObject);
        SendResult message = null;
        try {
            message = syncSendInternalMessage(client, encodeObject, msgUUID, timeout, timeUnit);
```

### UnusedAssignment
Variable `bShutDown` initializer `false` is redundant
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/ClientMgr.java`
#### Snippet
```java

        private final int[] random = {17, 19, 23, 31, 37};
        private boolean bShutDown = false;

        public SendHBThread() {
```

### UnusedAssignment
Variable `isSuccess` initializer `false` is redundant
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/ClientMgr.java`
#### Snippet
```java

        List<HostInfo> replaceHostLists = getRealHosts(badHostLists, aliveConnections);
        boolean isSuccess = false;
        for (HostInfo hostInfo : replaceHostLists) {
            isSuccess = initConnection(hostInfo);
```

### UnusedAssignment
Variable `getters` initializer `fieldGetters` is redundant
in `inlong-sort/sort-flink/base/src/main/java/org/apache/inlong/sort/base/dirty/sink/log/LogDirtySink.java`
#### Snippet
```java
        switch (format) {
            case "csv":
                RowData.FieldGetter[] getters = fieldGetters;
                if (rowType != null) {
                    getters = FormatUtils.parseFieldGetters(rowType);
```

### UnusedAssignment
Variable `dataIt` initializer `null` is redundant
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/source/reader/IncrementalSourceSplitReader.java`
#### Snippet
```java
    public RecordsWithSplitIds<SourceRecords> fetch() throws IOException {
        checkSplitOrStartNext();
        Iterator<SourceRecords> dataIt = null;
        try {
            dataIt = currentFetcher.pollSplitRecords();
```

### UnusedAssignment
The value `jdbcConnectionPoolFactory` assigned to `jdbcConnectionPoolFactory` is never used
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/relational/connection/JdbcConnectionPools.java`
#### Snippet
```java
    public static synchronized JdbcConnectionPools getInstance(JdbcConnectionPoolFactory jdbcConnectionPoolFactory) {
        if (instance == null) {
            jdbcConnectionPoolFactory = jdbcConnectionPoolFactory;
            instance = new JdbcConnectionPools();
        }
```

### UnusedAssignment
Variable `table` initializer `null` is redundant
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/debezium/history/FlinkJsonTableChangeSerializer.java`
#### Snippet
```java
        TableChangeType type = TableChangeType.valueOf(document.getString("type"));
        TableId id = TableId.parse(document.getString("id"), useCatalogBeforeSchema);
        Table table = null;

        if (type == TableChangeType.CREATE || type == TableChangeType.ALTER) {
```

### UnusedAssignment
Variable `valueOfMethod` initializer `null` is redundant
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/hudi/src/main/java/org/apache/inlong/sort/hudi/metric/HudiMetricsConfig.java`
#### Snippet
```java
                Class<T> clazz,
                String property) throws InvocationTargetException, IllegalAccessException {
            Method valueOfMethod = null;
            try {
                valueOfMethod = clazz.getMethod("valueOf", String.class);
```

### UnusedAssignment
Variable `mutation` initializer `null` is redundant
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/hbase/src/main/java/org/apache/inlong/sort/hbase/sink/HBaseSinkFunction.java`
#### Snippet
```java
        RowData rowData = (RowData) value;
        if (RowKind.UPDATE_BEFORE != rowData.getRowKind()) {
            Mutation mutation = null;
            try {
                mutation = Preconditions.checkNotNull(mutationConverter.convertToMutation(value));
```

### UnusedAssignment
The value `Optional.empty()` assigned to `outputFlusher` is never used
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/redis/src/main/java/org/apache/inlong/sort/redis/sink/AbstractRedisSinkFunction.java`
#### Snippet
```java
        if (this.batchSize == 1 || this.flushIntervalInMillis == 0) {
            LOG.info("Flush records immediately.");
            outputFlusher = Optional.empty();
        } else {
            String threadName = DEFAULT_OUTPUT_FLUSH_THREAD_NAME + " for "
```

### UnusedAssignment
Variable `lastParallelism` initializer `0` is redundant
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kafka/src/main/java/org/apache/inlong/sort/kafka/FlinkKafkaProducer.java`
#### Snippet
```java
    public static class NextTransactionalIdHint {

        public int lastParallelism = 0;
        public long nextFreeTransactionalId = 0;

```

### UnusedAssignment
Variable `nextFreeTransactionalId` initializer `0` is redundant
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kafka/src/main/java/org/apache/inlong/sort/kafka/FlinkKafkaProducer.java`
#### Snippet
```java

        public int lastParallelism = 0;
        public long nextFreeTransactionalId = 0;

        public NextTransactionalIdHint() {
```

### UnusedAssignment
Variable `topics` initializer `null` is redundant
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/pulsar/src/main/java/org/apache/inlong/sort/pulsar/table/PulsarDynamicTableFactory.java`
#### Snippet
```java

    private List<String> generateTopic(ObjectIdentifier table, ReadableConfig tableOptions) {
        List<String> topics = null;
        if (tableOptions.get(GENERIC)) {
            topics = tableOptions.getOptional(TOPIC).orElse(null);
```

### UnusedAssignment
Variable `partNum` initializer `1` is redundant
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/pulsar/src/main/java/org/apache/inlong/sort/pulsar/internal/PulsarMetadataReader.java`
#### Snippet
```java
        HashSet<String> allTopics = new HashSet<>();
        for (String topic : topics) {
            int partNum = 1;
            try {
                List<String> partitions = client.getPartitionsForTopic(topic).get();
```

### UnusedAssignment
Variable `failOnDataLoss` initializer `true` is redundant
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/pulsar/src/main/java/org/apache/inlong/sort/pulsar/internal/PulsarFetcher.java`
#### Snippet
```java

    /** Failed or not when data loss. **/
    private boolean failOnDataLoss = true;

    private boolean useEarliestWhenDataLoss;
```

### UnusedAssignment
Variable `runningThreads` initializer `0` is redundant
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/pulsar/src/main/java/org/apache/inlong/sort/pulsar/internal/PulsarFetcher.java`
#### Snippet
```java
            // make sure that in any case (completion, abort, error), all spawned threads are stopped
            try {
                int runningThreads = 0;
                do { // check whether threads are alive and cancel them
                    runningThreads = 0;
```

### UnusedAssignment
Variable `jsonNode` initializer `null` is redundant
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/iceberg/src/main/java/org/apache/inlong/sort/iceberg/sink/multiple/DynamicSchemaHandleOperator.java`
#### Snippet
```java
    @Override
    public void processElement(StreamRecord<RowData> element) throws Exception {
        JsonNode jsonNode = null;
        try {
            jsonNode = dynamicSchemaFormat.deserialize(element.getValue().getBinary(0));
```

### UnusedAssignment
Variable `buffer` initializer `null` is redundant
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/iceberg/src/main/java/org/apache/inlong/sort/iceberg/sink/collections/PartitionGroupBuffer.java`
#### Snippet
```java
        TypeInformation<RowData> valueTypeInfo = InternalTypeInfo.of(FlinkSchemaUtil.convert(rowSchema));

        KVBuffer<Tuple2<String, Long>, RowData> buffer = null;
        if (BufferType.ROCKSDB.equals(type)) {
            buffer = new RocksDBKVBuffer<>(
```

### UnusedAssignment
Variable `buffer` initializer `null` is redundant
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/iceberg/src/main/java/org/apache/inlong/sort/iceberg/sink/collections/PartitionGroupBuffer.java`
#### Snippet
```java
                        Arrays.toString(equalityFieldIndex)));

        KVBuffer<Tuple2<String, RowData>, RowData> buffer = null;
        if (BufferType.ROCKSDB.equals(type)) {
            TypeInformation<Tuple2<String, RowData>> keyTypeInfo = new TupleTypeInfo(
```

### UnusedAssignment
Variable `key` initializer `null` is redundant
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/iceberg/src/main/java/org/apache/inlong/sort/iceberg/sink/collections/RocksDBDAO.java`
#### Snippet
```java
                throw new IllegalStateException("next() called on rocksDB with no more valid entries");
            }
            T key = null;
            R val = null;
            try {
```

### UnusedAssignment
Variable `val` initializer `null` is redundant
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/iceberg/src/main/java/org/apache/inlong/sort/iceberg/sink/collections/RocksDBDAO.java`
#### Snippet
```java
            }
            T key = null;
            R val = null;
            try {
                key = dao.deserializeKey(iterator.key());
```

### UnusedAssignment
Variable `table` initializer `null` is redundant
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mongodb-cdc/src/main/java/org/apache/inlong/sort/cdc/mongodb/debezium/history/FlinkJsonTableChangeSerializer.java`
#### Snippet
```java
        TableChangeType type = TableChangeType.valueOf(document.getString("type"));
        TableId id = TableId.parse(document.getString("id"), useCatalogBeforeSchema);
        Table table = null;

        if (type == TableChangeType.CREATE || type == TableChangeType.ALTER) {
```

### UnusedAssignment
Variable `count` initializer `0L` is redundant
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/postgres-cdc/src/main/java/org/apache/inlong/sort/cdc/postgres/manager/PostgreSQLQueryVisitor.java`
#### Snippet
```java
     */
    public Long getQueryCount(String sql) {
        Long count = 0L;
        try {
            if (LOG.isDebugEnabled()) {
```

### UnusedAssignment
Variable `sourceMultipleEnable` initializer `false` is redundant
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mongodb-cdc/src/main/java/org/apache/inlong/sort/cdc/mongodb/debezium/table/MongoDBConnectorDeserializationSchema.java`
#### Snippet
```java
    private final AppendMetadataCollector appendMetadataCollector;

    private boolean sourceMultipleEnable = false;

    /**
```

### UnusedAssignment
Variable `isFinished` initializer `false` is redundant
in `inlong-agent/agent-core/src/main/java/org/apache/inlong/agent/core/job/JobWrapper.java`
#### Snippet
```java
     */
    private void checkAllTasksStateAndWait() throws Exception {
        boolean isFinished = false;

        long checkInterval = agentConf.getLong(
```

### UnusedAssignment
Variable `parseValue` initializer `-1` is redundant
in `inlong-agent/agent-common/src/main/java/org/apache/inlong/agent/utils/AgentDbUtils.java`
#### Snippet
```java
    private static int parseInt(String parseStr) {

        int parseValue = -1;

        if (parseStr.startsWith(HEX_PREFIX)) {
```

### UnusedAssignment
Variable `dataValue` initializer `null` is redundant
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/sources/reader/SqlReader.java`
#### Snippet
```java
                List<String> lineColumns = new ArrayList<>();
                for (int i = 1; i <= columnCount; i++) {
                    String dataValue = null;
                    /* handle special blob value, encode with base64, BLOB=2004 */
                    int typeCode = columnTypeCodes[i - 1];
```

### UnusedAssignment
Variable `destAddr` initializer `null` is redundant
in `inlong-audit/audit-sdk/src/main/java/org/apache/inlong/audit/util/IpPort.java`
#### Snippet
```java
     */
    public static InetSocketAddress parseInetSocketAddress(Channel channel) {
        InetSocketAddress destAddr = null;
        if (channel.remoteAddress() instanceof InetSocketAddress) {
            destAddr = (InetSocketAddress) channel.remoteAddress();
```

### UnusedAssignment
Variable `control` initializer `1000` is redundant
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/utils/LogCounter.java`
#### Snippet
```java

    private int start = 10;
    private int control = 1000;
    private int reset = 60 * 1000;

```

### UnusedAssignment
Variable `start` initializer `10` is redundant
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/utils/LogCounter.java`
#### Snippet
```java
    private AtomicInteger counter = new AtomicInteger(0);

    private int start = 10;
    private int control = 1000;
    private int reset = 60 * 1000;
```

### UnusedAssignment
Variable `reset` initializer `60 * 1000` is redundant
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/utils/LogCounter.java`
#### Snippet
```java
    private int start = 10;
    private int control = 1000;
    private int reset = 60 * 1000;

    private long lastLogTime = System.currentTimeMillis();
```

### UnusedAssignment
Variable `line` initializer `null` is redundant
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/sources/reader/file/FileReaderOperator.java`
#### Snippet
```java

    private List<String> readFromLine(int lineNum) throws IOException {
        String line = null;
        List<String> lines = new ArrayList<>();
        BufferedReader reader = null;
```

### UnusedAssignment
Variable `position` initializer `0` is redundant
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/sources/reader/file/FileReaderOperator.java`
#### Snippet
```java

    public File file;
    public int position = 0;
    public int bytePosition = 0;
    private long readEndpoint = Long.MAX_VALUE;
```

### UnusedAssignment
Variable `line` initializer `null` is redundant
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/sources/reader/file/FileReaderOperator.java`
#### Snippet
```java

    private List<String> readFromPos(int pos) throws IOException {
        String line = null;
        List<String> lines = new ArrayList<>();
        RandomAccessFile input = null;
```

### UnusedAssignment
Variable `line` initializer `null` is redundant
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/sources/reader/file/FileReaderOperator.java`
#### Snippet
```java
        try {
            LOGGER.info("get start line {}", lineNum);
            String line = null;
            List<String> lines = new ArrayList<>();
            reader = new BufferedReader(new InputStreamReader(new FileInputStream(file),
```

### UnusedAssignment
Variable `maxConnections` initializer `Integer.MAX_VALUE` is redundant
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/source/ServerMessageFactory.java`
#### Snippet
```java
    private ServiceDecoder serviceDecoder;
    private String messageHandlerName;
    private int maxConnections = Integer.MAX_VALUE;
    private int maxMsgLength;
    private String name;
```

### UnusedAssignment
Variable `pulsarClient` initializer `null` is redundant
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/pulsar/PulsarClientService.java`
#### Snippet
```java

    private PulsarClient initPulsarClient(String pulsarUrl) throws Exception {
        PulsarClient pulsarClient = null;
        ClientBuilder builder = PulsarClient.builder();
        if (pulsarEnableTokenAuth && StringUtils.isNotEmpty(pulsarTokenAuth)) {
```

### UnusedAssignment
Variable `streamId` initializer `""` is redundant
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/pulsar/PulsarClientService.java`
#### Snippet
```java
        Map<String, String> proMap = new HashMap<>();
        proMap.put("auditIp", localIp);
        String streamId = "";
        String groupId = "";
        if (event.getHeaders().containsKey(AttributeConstants.INLONG_STREAM_ID)) {
```

### UnusedAssignment
Variable `groupId` initializer `""` is redundant
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/pulsar/PulsarClientService.java`
#### Snippet
```java
        proMap.put("auditIp", localIp);
        String streamId = "";
        String groupId = "";
        if (event.getHeaders().containsKey(AttributeConstants.INLONG_STREAM_ID)) {
            streamId = event.getHeaders().get(AttributeConstants.INLONG_STREAM_ID);
```

### UnusedAssignment
Variable `enableBatch` initializer `true` is redundant
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/pulsar/PulsarClientService.java`
#### Snippet
```java
    private Integer sendTimeout; // in millsec
    private Integer clientOpTimeout;
    private boolean enableBatch = true;
    private boolean blockIfQueueFull = true;
    private int maxPendingMessages = 10000;
```

### UnusedAssignment
Variable `maxBatchingMessages` initializer `1000` is redundant
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/pulsar/PulsarClientService.java`
#### Snippet
```java
    private boolean blockIfQueueFull = true;
    private int maxPendingMessages = 10000;
    private int maxBatchingMessages = 1000;
    public ConcurrentHashMap<String, Producer> producerInfoMap;
    public PulsarClient pulsarClient;
```

### UnusedAssignment
Variable `blockIfQueueFull` initializer `true` is redundant
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/pulsar/PulsarClientService.java`
#### Snippet
```java
    private Integer clientOpTimeout;
    private boolean enableBatch = true;
    private boolean blockIfQueueFull = true;
    private int maxPendingMessages = 10000;
    private int maxBatchingMessages = 1000;
```

### UnusedAssignment
Variable `localIp` initializer `"127.0.0.1"` is redundant
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/pulsar/PulsarClientService.java`
#### Snippet
```java
    public String pulsarTokenAuth;

    private String localIp = "127.0.0.1";

    /**
```

### UnusedAssignment
Variable `maxPendingMessages` initializer `10000` is redundant
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/pulsar/PulsarClientService.java`
#### Snippet
```java
    private boolean enableBatch = true;
    private boolean blockIfQueueFull = true;
    private int maxPendingMessages = 10000;
    private int maxBatchingMessages = 1000;
    public ConcurrentHashMap<String, Producer> producerInfoMap;
```

### UnusedAssignment
Variable `fac` initializer `null` is redundant
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/source/SimpleTcpSource.java`
#### Snippet
```java
        logger.info("load msgFactory=" + msgFactoryName + " and serviceDecoderName="
                + serviceDecoderName);
        ChannelInitializer fac = null;

        try {
```

### UnusedAssignment
Variable `streamId` initializer `""` is redundant
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/TubeSink.java`
#### Snippet
```java
            Message message = new Message(topic, event.getBody());
            message.setAttrKeyVal("auditIp", localIp);
            String streamId = "";
            String groupId = "";
            if (event.getHeaders().containsKey(org.apache.inlong.audit.consts.AttributeConstants.INLONG_STREAM_ID)) {
```

### UnusedAssignment
Variable `groupId` initializer `""` is redundant
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/TubeSink.java`
#### Snippet
```java
            message.setAttrKeyVal("auditIp", localIp);
            String streamId = "";
            String groupId = "";
            if (event.getHeaders().containsKey(org.apache.inlong.audit.consts.AttributeConstants.INLONG_STREAM_ID)) {
                streamId = event.getHeaders().get(org.apache.inlong.audit.consts.AttributeConstants.INLONG_STREAM_ID);
```

### UnusedAssignment
Variable `instance` initializer `null` is redundant
in `inlong-audit/audit-common/src/main/java/org/apache/inlong/audit/file/ConfigManager.java`
#### Snippet
```java
    public static List<MQInfo> mqInfoList = new ArrayList<>();

    private static ConfigManager instance = null;

    private static String DEFAULT_CONFIG_PROPERTIES = "application.properties";
```

### UnusedAssignment
The value `false` assigned to `maskedThisCharacter` is never used
in `inlong-common/src/main/java/org/apache/inlong/common/util/MaskDataUtils.java`
#### Snippet
```java
        int length = stringBuilder.length();
        for (pos = 0; pos < length; pos++) {
            maskedThisCharacter = false;
            newPos = maskData(stringBuilder, '*', pos, length);
            maskedThisCharacter = newPos != pos;
```

### UnusedAssignment
Variable `bShutDown` initializer `false` is redundant
in `inlong-common/src/main/java/org/apache/inlong/common/monitor/MonitorIndexExt.java`
#### Snippet
```java
                Thread {

        private boolean bShutDown = false;

        public IndexCollectThread() {
```

### UnusedAssignment
Variable `control` initializer `1000` is redundant
in `inlong-common/src/main/java/org/apache/inlong/common/monitor/LogCounter.java`
#### Snippet
```java

    private int start = 10;
    private int control = 1000;
    private int reset = 60 * 1000;

```

### UnusedAssignment
Variable `reset` initializer `60 * 1000` is redundant
in `inlong-common/src/main/java/org/apache/inlong/common/monitor/LogCounter.java`
#### Snippet
```java
    private int start = 10;
    private int control = 1000;
    private int reset = 60 * 1000;

    private long lastLogTime = System.currentTimeMillis();
```

### UnusedAssignment
Variable `start` initializer `10` is redundant
in `inlong-common/src/main/java/org/apache/inlong/common/monitor/LogCounter.java`
#### Snippet
```java
    private AtomicInteger counter = new AtomicInteger(0);

    private int start = 10;
    private int control = 1000;
    private int reset = 60 * 1000;
```

### UnusedAssignment
Variable `finalAttrMap` initializer `commonAttrMap` is redundant
in `inlong-common/src/main/java/org/apache/inlong/common/msg/InLongMsg.java`
#### Snippet
```java
            attr2Rawdata = new LinkedHashMap<String, DataByteBuffer>(
                    this.msgcnt * 10 / 7);
            Map<String, String> finalAttrMap = commonAttrMap;

            // skip compress flag
```

### UnusedAssignment
The value `0L` assigned to `cnt` is never used
in `inlong-common/src/main/java/org/apache/inlong/common/monitor/StatRunner.java`
#### Snippet
```java
                while (iterator.hasNext()) {
                    str = (String) iterator.next();
                    cnt = 0L;
                    synchronized (this.counterGroup) {
                        cnt = this.counterGroup.get(str);
```

### UnusedAssignment
Variable `bShutDown` initializer `false` is redundant
in `inlong-common/src/main/java/org/apache/inlong/common/monitor/MonitorIndex.java`
#### Snippet
```java
                Thread {

        private boolean bShutDown = false;

        public IndexCollectThread() {
```

### UnusedAssignment
Variable `protocolVersion` initializer `TBaseConstants.META_VALUE_UNDEFINED` is redundant
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/RequestWrapper.java`
#### Snippet
```java
    private int flagId = TBaseConstants.META_VALUE_UNDEFINED;
    private int serviceType = TBaseConstants.META_VALUE_UNDEFINED;
    private int protocolVersion = TBaseConstants.META_VALUE_UNDEFINED;
    private int protocolType = TBaseConstants.META_VALUE_UNDEFINED;
    private int serialNo;
```

### UnusedAssignment
Variable `flagId` initializer `TBaseConstants.META_VALUE_UNDEFINED` is redundant
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/RequestWrapper.java`
#### Snippet
```java

    private static final long serialVersionUID = -2469749661773014443L;
    private int flagId = TBaseConstants.META_VALUE_UNDEFINED;
    private int serviceType = TBaseConstants.META_VALUE_UNDEFINED;
    private int protocolVersion = TBaseConstants.META_VALUE_UNDEFINED;
```

### UnusedAssignment
Variable `serviceType` initializer `TBaseConstants.META_VALUE_UNDEFINED` is redundant
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/RequestWrapper.java`
#### Snippet
```java
    private static final long serialVersionUID = -2469749661773014443L;
    private int flagId = TBaseConstants.META_VALUE_UNDEFINED;
    private int serviceType = TBaseConstants.META_VALUE_UNDEFINED;
    private int protocolVersion = TBaseConstants.META_VALUE_UNDEFINED;
    private int protocolType = TBaseConstants.META_VALUE_UNDEFINED;
```

### UnusedAssignment
Variable `maxConnAllowedFailCount` initializer `5` is redundant
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/RemoteConErrStats.java`
#### Snippet
```java

    private long statisticDuration = 60000;
    private int maxConnAllowedFailCount = 5;
    private final AtomicLong errCounter = new AtomicLong(0);
    private final AtomicLong lastTimeStamp = new AtomicLong(0);
```

### UnusedAssignment
Variable `statisticDuration` initializer `60000` is redundant
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/RemoteConErrStats.java`
#### Snippet
```java
public class RemoteConErrStats {

    private long statisticDuration = 60000;
    private int maxConnAllowedFailCount = 5;
    private final AtomicLong errCounter = new AtomicLong(0);
```

### UnusedAssignment
Variable `flagId` initializer `0` is redundant
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/ResponseWrapper.java`
#### Snippet
```java

    private int serialNo;
    private int flagId = 0;
    private int serviceType;
    private int protocolVersion;
```

### UnusedAssignment
Variable `errorClass` initializer `null` is redundant
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/ResponseWrapper.java`
#### Snippet
```java
        this.serviceType = serviceType;
        this.protocolVersion = locVersion;
        String errorClass = null;
        String error = null;
        if (exception.getCause() != null
```

### UnusedAssignment
Variable `error` initializer `null` is redundant
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/ResponseWrapper.java`
#### Snippet
```java
        this.protocolVersion = locVersion;
        String errorClass = null;
        String error = null;
        if (exception.getCause() != null
                && exception.getCause() instanceof StandbyException) {
```

### UnusedAssignment
Variable `protocolType` initializer `RpcProtocol.RPC_PROTOCOL_TCP` is redundant
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/netty/NettyRpcServer.java`
#### Snippet
```java
    private class NettyServerHandler extends ChannelInboundHandlerAdapter {

        private int protocolType = RpcProtocol.RPC_PROTOCOL_TCP;

        public NettyServerHandler(int protocolType) {
```

### UnusedAssignment
Variable `invokeTimes` initializer `1000000` is redundant
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/benchemark/RcpService4BenchmarkClient.java`
#### Snippet
```java
    private final SimpleService simpleService;
    private int threadNum = 10;
    private int invokeTimes = 1000000;

    /**
```

### UnusedAssignment
Variable `threadNum` initializer `10` is redundant
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/benchemark/RcpService4BenchmarkClient.java`
#### Snippet
```java
    private final NettyClientFactory clientFactory = new NettyClientFactory();
    private final SimpleService simpleService;
    private int threadNum = 10;
    private int invokeTimes = 1000000;

```

### UnusedAssignment
Variable `responseWrapper` initializer `null` is redundant
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/protocol/RpcProtocol.java`
#### Snippet
```java
    @Override
    public void handleRequest(final RequestContext context, final String rmtAddress) throws Exception {
        ResponseWrapper responseWrapper = null;
        RequestWrapper requestWrapper = context.getRequest();
        if (System.currentTimeMillis() - context.getReceiveTime() > requestWrapper.getTimeout()) {
```

### UnusedAssignment
Variable `method` initializer `null` is redundant
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/protocol/RpcProtocol.java`
#### Snippet
```java
                    RPC_PROTOCOL_VERSION, new ServiceStoppingException("service is stopping...")));
        }
        Method method = null;
        StringBuilder sBuilder = new StringBuilder(512);
        try {
```

### UnusedAssignment
Variable `errorClass` initializer `null` is redundant
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/protocol/RpcProtocol.java`
#### Snippet
```java
                            RPC_PROTOCOL_VERSION, requestWrapper.getMethodId(), result);
        } catch (Throwable e2) {
            String errorClass = null;
            String errorInfo = null;
            if (e2.getCause() != null && e2.getCause() instanceof StandbyException) {
```

### UnusedAssignment
Variable `errorInfo` initializer `null` is redundant
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/protocol/RpcProtocol.java`
#### Snippet
```java
        } catch (Throwable e2) {
            String errorClass = null;
            String errorInfo = null;
            if (e2.getCause() != null && e2.getCause() instanceof StandbyException) {
                errorClass = e2.getCause().getClass().getName();
```

### UnusedAssignment
Variable `javaVersion` initializer `""` is redundant
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/utils/MixedUtils.java`
#### Snippet
```java

    // java version cache
    private static String javaVersion = "";

    static {
```

### UnusedAssignment
Variable `dataStartDltInSize` initializer `Long.MAX_VALUE` is redundant
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/policies/SSDCtrlResult.java`
#### Snippet
```java
public class SSDCtrlResult {

    public long dataStartDltInSize = Long.MAX_VALUE;
    public long dataEndDLtInSz = 0;

```

### UnusedAssignment
Variable `dataEndDLtInSz` initializer `0` is redundant
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/policies/SSDCtrlResult.java`
#### Snippet
```java

    public long dataStartDltInSize = Long.MAX_VALUE;
    public long dataEndDLtInSz = 0;

    public SSDCtrlResult(long dataStartDltInSize, long dataEndDLtInSz) {
```

### UnusedAssignment
Variable `freqLtInMs` initializer `0` is redundant
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/policies/FlowCtrlResult.java`
#### Snippet
```java

    public long dataLtInSize = Integer.MAX_VALUE;
    public int freqLtInMs = 0;

    public FlowCtrlResult(long dataLtInSize, int freqLtInMs) {
```

### UnusedAssignment
Variable `dataLtInSize` initializer `Integer.MAX_VALUE` is redundant
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/policies/FlowCtrlResult.java`
#### Snippet
```java
public class FlowCtrlResult {

    public long dataLtInSize = Integer.MAX_VALUE;
    public int freqLtInMs = 0;

```

### UnusedAssignment
Variable `topicStoreNum` initializer `1` is redundant
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/cluster/TopicInfo.java`
#### Snippet
```java
    private String simpleValue;
    private int partitionNum;
    private int topicStoreNum = 1;
    private boolean acceptPublish;
    private boolean acceptSubscribe;
```

### UnusedAssignment
Variable `overTLS` initializer `false` is redundant
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/cluster/ProducerInfo.java`
#### Snippet
```java
    private final String host;
    private String producerId;
    private boolean overTLS = false;

    public ProducerInfo(String producerId, Set<String> topicSet, String host, boolean overTLS) {
```

### UnusedAssignment
Variable `type` initializer `0` is redundant
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/policies/FlowCtrlItem.java`
#### Snippet
```java
public class FlowCtrlItem {

    private int type = 0; // 0: current limit, 1: frequency limit, 2: SSD transfer 3: request frequency control
    private int startTime = TBaseConstants.META_VALUE_UNDEFINED;
    private int endTime = TBaseConstants.META_VALUE_UNDEFINED;
```

### UnusedAssignment
The value `TClientConstants.CFG_MIN_META_QUERY_WAIT_PERIOD_MS` assigned to `this.partMetaInfoCheckPeriodMs` is never used
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/config/ConsumerConfig.java`
#### Snippet
```java
    public void setPartMetaInfoCheckPeriodMs(long partMetaInfoCheckPeriodMs) {
        if (partMetaInfoCheckPeriodMs < TClientConstants.CFG_MIN_META_QUERY_WAIT_PERIOD_MS) {
            this.partMetaInfoCheckPeriodMs = TClientConstants.CFG_MIN_META_QUERY_WAIT_PERIOD_MS;
        }
        this.partMetaInfoCheckPeriodMs = partMetaInfoCheckPeriodMs;
```

### UnusedAssignment
Variable `lastConsumed` initializer `false` is redundant
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/FetchContext.java`
#### Snippet
```java
    private Partition partition;
    private long usedToken;
    private boolean lastConsumed = false;
    private boolean success = false;
    private int errCode = 0;
```

### UnusedAssignment
Variable `maxOffset` initializer `TBaseConstants.META_VALUE_UNDEFINED` is redundant
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/ConsumeOffsetInfo.java`
#### Snippet
```java
    private String partitionKey;
    private long currOffset = TBaseConstants.META_VALUE_UNDEFINED;
    private long maxOffset = TBaseConstants.META_VALUE_UNDEFINED;
    private long updateTime = TBaseConstants.META_VALUE_UNDEFINED;

```

### UnusedAssignment
Variable `updateTime` initializer `TBaseConstants.META_VALUE_UNDEFINED` is redundant
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/ConsumeOffsetInfo.java`
#### Snippet
```java
    private long currOffset = TBaseConstants.META_VALUE_UNDEFINED;
    private long maxOffset = TBaseConstants.META_VALUE_UNDEFINED;
    private long updateTime = TBaseConstants.META_VALUE_UNDEFINED;

    public ConsumeOffsetInfo(String partitionKey, long currOffset, long maxOffset) {
```

### UnusedAssignment
Variable `currOffset` initializer `TBaseConstants.META_VALUE_UNDEFINED` is redundant
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/ConsumeOffsetInfo.java`
#### Snippet
```java

    private String partitionKey;
    private long currOffset = TBaseConstants.META_VALUE_UNDEFINED;
    private long maxOffset = TBaseConstants.META_VALUE_UNDEFINED;
    private long updateTime = TBaseConstants.META_VALUE_UNDEFINED;
```

### UnusedAssignment
Variable `selectResult` initializer `null` is redundant
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/SimplePullMessageConsumer.java`
#### Snippet
```java
            throw new TubeClientException("Please complete topic's Subscribe call first!");
        }
        PartitionSelectResult selectResult = null;
        long startTime = System.currentTimeMillis();
        while (true) {
```

### UnusedAssignment
Variable `roundPartition` initializer `null` is redundant
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/producer/RoundRobinPartitionRouter.java`
#### Snippet
```java
            }
        }
        Partition roundPartition = null;
        int partSize = partitions.size();
        for (int i = 0; i < partSize; i++) {
```

### UnusedAssignment
Variable `partition` initializer `null` is redundant
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/producer/MessageSentResult.java`
#### Snippet
```java
    private final Message message;
    private long messageId = TBaseConstants.META_VALUE_UNDEFINED;
    private Partition partition = null;
    private long appendTime = TBaseConstants.META_VALUE_UNDEFINED;
    private long appendOffset = TBaseConstants.META_VALUE_UNDEFINED;
```

### UnusedAssignment
Variable `messageId` initializer `TBaseConstants.META_VALUE_UNDEFINED` is redundant
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/producer/MessageSentResult.java`
#### Snippet
```java
    private final String errMsg;
    private final Message message;
    private long messageId = TBaseConstants.META_VALUE_UNDEFINED;
    private Partition partition = null;
    private long appendTime = TBaseConstants.META_VALUE_UNDEFINED;
```

### UnusedAssignment
The value `null` assigned to `partSelectResult` is never used
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/MessageFetchManager.java`
#### Snippet
```java
                                .getBaseConsumer()
                                .pushReqReleasePartition(partition.getPartitionKey(), usedToken, isLastConsumed);
                        partSelectResult = null;
                        break;
                    }
```

### UnusedAssignment
The value `null` assigned to `partSelectResult` is never used
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/MessageFetchManager.java`
#### Snippet
```java
                                .getBaseConsumer().pushReqReleasePartition(partition.getPartitionKey(),
                                        usedToken, result);
                        partSelectResult = null;
                        continue;
                    }
```

### UnusedAssignment
Variable `haveProcessingThread` initializer `false` is redundant
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/MessageFetchManager.java`
#### Snippet
```java

    private boolean waitAllFetchRequestHolds(long waitTimeInMills) {
        boolean haveProcessingThread = false;
        long startWaitTime = System.currentTimeMillis();
        do {
```

### UnusedAssignment
Variable `sendNum` initializer `0L` is redundant
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/producer/qltystats/BrokerStatsDltTuple.java`
#### Snippet
```java
public class BrokerStatsDltTuple {

    private long sendNum = 0L;
    private long succRecvNum = 0L;

```

### UnusedAssignment
Variable `succRecvNum` initializer `0L` is redundant
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/producer/qltystats/BrokerStatsDltTuple.java`
#### Snippet
```java

    private long sendNum = 0L;
    private long succRecvNum = 0L;

    public BrokerStatsDltTuple(long succRecvNum, long sendNum) {
```

### UnusedAssignment
Variable `dltSendNum` initializer `0` is redundant
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/producer/qltystats/BrokerStatsItemSet.java`
#### Snippet
```java
    private AtomicLong sendNumSnapshot = new AtomicLong();
    private AtomicLong recSucNumSnapshot = new AtomicLong();
    private long dltSendNum = 0;
    private long dltRecSucNum = 0;

```

### UnusedAssignment
Variable `dltRecSucNum` initializer `0` is redundant
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/producer/qltystats/BrokerStatsItemSet.java`
#### Snippet
```java
    private AtomicLong recSucNumSnapshot = new AtomicLong();
    private long dltSendNum = 0;
    private long dltRecSucNum = 0;

    public BrokerStatsItemSet() {
```

### UnusedAssignment
Variable `objArray` initializer `null` is redundant
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/policies/FlowCtrlRuleHandler.java`
#### Snippet
```java
            throw new Exception("Parsing error, flowCtrlInfo value is blank!");
        }
        JsonArray objArray = null;
        try {
            objArray = JsonParser.parseString(flowCtrlInfo).getAsJsonArray();
```

### UnusedAssignment
Variable `timeHour` initializer `0` is redundant
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/policies/FlowCtrlRuleHandler.java`
#### Snippet
```java
                    .append(") of flowCtrlInfo value!").toString());
        }
        int timeHour = 0;
        int timeMin = 0;
        String[] startItems = strTimeVal.split(TokenConstants.ATTR_SEP);
```

### UnusedAssignment
Variable `timeMin` initializer `0` is redundant
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/policies/FlowCtrlRuleHandler.java`
#### Snippet
```java
        }
        int timeHour = 0;
        int timeMin = 0;
        String[] startItems = strTimeVal.split(TokenConstants.ATTR_SEP);
        if ((startItems.length != 2)
```

### UnusedAssignment
Variable `isDescSort` initializer `true` is redundant
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/producer/qltystats/DefaultBrokerRcvQltyStats.java`
#### Snippet
```java

        // Descending sort or ascending sort
        private boolean isDescSort = true;

        public BrokerStatsDltTupleComparator(boolean isDescSort) {
```

### UnusedAssignment
Variable `avgSuccRecNumThreshold` initializer `0L` is redundant
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/producer/qltystats/DefaultBrokerRcvQltyStats.java`
#### Snippet
```java
            return;
        }
        long avgSuccRecNumThreshold = 0L;
        if (filteredBrokerListSize <= 3) {
            totalSuccRecNum -= lstData.get(0).getValue().getSuccRecvNum();
```

### UnusedAssignment
Variable `result` initializer `null` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/tools/cli/CliBrokerAdmin.java`
#### Snippet
```java
            }
        }
        JsonObject result = null;
        Map<String, String> inParamMap = new HashMap<>();
        String brokerUrl = "http://" + brokerAddr + "/broker.htm";
```

### UnusedAssignment
Variable `lastPackConsumed` initializer `false` is redundant
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/RmtDataCache.java`
#### Snippet
```java
        try {
            waitPartitions(partitionKeys, inUseWaitPeriodMs);
            boolean lastPackConsumed = false;
            for (Map.Entry<BrokerInfo, List<Partition>> entry : unRegisterInfoMap.entrySet()) {
                for (Partition partition : entry.getValue()) {
```

### UnusedAssignment
Variable `needWait` initializer `false` is redundant
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/RmtDataCache.java`
#### Snippet
```java

    private void waitPartitions(List<String> partitionKeys, long inUseWaitPeriodMs) {
        boolean needWait = false;
        long startWaitTime = System.currentTimeMillis();
        do {
```

### UnusedAssignment
Variable `timeout1` initializer `null` is redundant
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/RmtDataCache.java`
#### Snippet
```java
        // add timeout expired check
        if (!timeouts.isEmpty()) {
            Timeout timeout1 = null;
            List<String> partKeys = new ArrayList<>(timeouts.keySet());
            for (String keyId : partKeys) {
```

### UnusedAssignment
Variable `lastPackConsumed` initializer `false` is redundant
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/RmtDataCache.java`
#### Snippet
```java
            boolean isWaitTimeoutRollBack, ProcessResult result,
            StringBuilder sBuffer) {
        boolean lastPackConsumed = false;
        List<String> partitionKeys = new ArrayList<>();
        partitionKeys.add(partitionKey);
```

### UnusedAssignment
Variable `frozenTime` initializer `null` is redundant
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/RmtDataCache.java`
#### Snippet
```java

    public void relAllFrozenPartitions() {
        Long frozenTime = null;
        List<String> partKeys = new ArrayList<>(partitionFrozenMap.keySet());
        for (String partKey : partKeys) {
```

### UnusedAssignment
Variable `createTime` initializer `0L` is redundant
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/RmtDataCache.java`
#### Snippet
```java

        private String indexId;
        private long createTime = 0L;

        public TimeoutTask(final String indexId) {
```

### UnusedAssignment
Variable `roundIndex` initializer `0` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/tools/cli/CliProducer.java`
#### Snippet
```java
            int topicAndCondCnt = topicSendRounds.size();
            long sentCount = 0;
            int roundIndex = 0;
            while (msgCount < 0 || sentCount < msgCount) {
                roundIndex = (int) (sentCount++ % topicAndCondCnt);
```

### UnusedAssignment
Variable `hostname` initializer `"0.0.0.0"` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/web/WebServer.java`
#### Snippet
```java
public class WebServer implements Server {

    private String hostname = "0.0.0.0";
    private int port = 8080;
    private org.eclipse.jetty.server.Server srv;
```

### UnusedAssignment
Variable `port` initializer `8080` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/web/WebServer.java`
#### Snippet
```java

    private String hostname = "0.0.0.0";
    private int port = 8080;
    private org.eclipse.jetty.server.Server srv;
    private TubeBroker broker;
```

### UnusedAssignment
Variable `fetchRunners` initializer `null` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/tools/cli/CliConsumer.java`
#### Snippet
```java
    private static class TupleValue {

        public Thread[] fetchRunners = null;

        public TupleValue(PullMessageConsumer consumer, long msgCount, int fetchThreadCnt) {
```

### UnusedAssignment
Variable `partitionId` initializer `TBaseConstants.META_VALUE_UNDEFINED` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/utils/GroupOffsetInfo.java`
#### Snippet
```java
public class GroupOffsetInfo {

    public int partitionId = TBaseConstants.META_VALUE_UNDEFINED;
    public long offsetMin = TBaseConstants.META_VALUE_UNDEFINED;
    public long offsetMax = TBaseConstants.META_VALUE_UNDEFINED;
```

### UnusedAssignment
Variable `attrLen` initializer `0` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/utils/DataStoreUtils.java`
#### Snippet
```java
        dataBuilder.setPayLoadData(ByteString.copyFrom(payLoadData));
        // get statistic data
        int attrLen = 0;
        String attribute = null;
        if (MessageFlagUtils.hasAttribute(flag)) {
```

### UnusedAssignment
Variable `dataMax` initializer `0L` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/utils/TopicPubStoreInfo.java`
#### Snippet
```java
    public long offsetMax = 0L;
    public long dataMin = 0L;
    public long dataMax = 0L;

    public TopicPubStoreInfo(String topicName, int storeId, int partitionId,
```

### UnusedAssignment
Variable `dataMin` initializer `0L` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/utils/TopicPubStoreInfo.java`
#### Snippet
```java
    public long offsetMin = 0L;
    public long offsetMax = 0L;
    public long dataMin = 0L;
    public long dataMax = 0L;

```

### UnusedAssignment
Variable `offsetMin` initializer `0L` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/utils/TopicPubStoreInfo.java`
#### Snippet
```java
    public int storeId = TBaseConstants.META_VALUE_UNDEFINED;
    public int partitionId = TBaseConstants.META_VALUE_UNDEFINED;
    public long offsetMin = 0L;
    public long offsetMax = 0L;
    public long dataMin = 0L;
```

### UnusedAssignment
Variable `partitionId` initializer `TBaseConstants.META_VALUE_UNDEFINED` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/utils/TopicPubStoreInfo.java`
#### Snippet
```java
    public String topicName = null;
    public int storeId = TBaseConstants.META_VALUE_UNDEFINED;
    public int partitionId = TBaseConstants.META_VALUE_UNDEFINED;
    public long offsetMin = 0L;
    public long offsetMax = 0L;
```

### UnusedAssignment
Variable `storeId` initializer `TBaseConstants.META_VALUE_UNDEFINED` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/utils/TopicPubStoreInfo.java`
#### Snippet
```java

    public String topicName = null;
    public int storeId = TBaseConstants.META_VALUE_UNDEFINED;
    public int partitionId = TBaseConstants.META_VALUE_UNDEFINED;
    public long offsetMin = 0L;
```

### UnusedAssignment
Variable `offsetMax` initializer `0L` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/utils/TopicPubStoreInfo.java`
#### Snippet
```java
    public int partitionId = TBaseConstants.META_VALUE_UNDEFINED;
    public long offsetMin = 0L;
    public long offsetMax = 0L;
    public long dataMin = 0L;
    public long dataMax = 0L;
```

### UnusedAssignment
Variable `topicName` initializer `null` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/utils/TopicPubStoreInfo.java`
#### Snippet
```java
public class TopicPubStoreInfo {

    public String topicName = null;
    public int storeId = TBaseConstants.META_VALUE_UNDEFINED;
    public int partitionId = TBaseConstants.META_VALUE_UNDEFINED;
```

### UnusedAssignment
Variable `jsonObject` initializer `null` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/offset/OffsetHistoryInfo.java`
#### Snippet
```java
     */
    public static boolean parseRecordInfo(String jsonData, ProcessResult result) {
        JsonObject jsonObject = null;
        try {
            jsonObject = JsonParser.parseString(jsonData).getAsJsonObject();
```

### UnusedAssignment
Variable `firstCreate` initializer `false` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/offset/offsetstorage/OffsetStorageInfo.java`
#### Snippet
```java
    private final AtomicLong offset = new AtomicLong(0);
    private long messageId;
    private boolean firstCreate = false;
    private boolean modified = false;

```

### UnusedAssignment
Variable `newOffset` initializer `-1` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/offset/offsetstorage/ZkOffsetStorage.java`
#### Snippet
```java
        sb.delete(0, sb.length());
        for (final OffsetStorageInfo info : infoList) {
            long newOffset = -1;
            long msgId = -1;
            synchronized (info) {
```

### UnusedAssignment
Variable `msgId` initializer `-1` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/offset/offsetstorage/ZkOffsetStorage.java`
#### Snippet
```java
        for (final OffsetStorageInfo info : infoList) {
            long newOffset = -1;
            long msgId = -1;
            synchronized (info) {
                if (!info.isModified()) {
```

### UnusedAssignment
Variable `isSuccess` initializer `false` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/mem/GetCacheMsgResult.java`
#### Snippet
```java
public class GetCacheMsgResult {

    public boolean isSuccess = false;
    public int retCode = -1;
    public String errInfo;
```

### UnusedAssignment
Variable `retCode` initializer `-1` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/mem/GetCacheMsgResult.java`
#### Snippet
```java

    public boolean isSuccess = false;
    public int retCode = -1;
    public String errInfo;
    public long startOffset;
```

### UnusedAssignment
Variable `msgRspB2C` initializer `null` is redundant
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/BaseMessageConsumer.java`
#### Snippet
```java
        long startTime = System.currentTimeMillis();
        // Response from broker
        ClientBroker.GetMessageResponseB2C msgRspB2C = null;
        try {
            msgRspB2C =
```

### UnusedAssignment
Variable `selectResult` initializer `null` is redundant
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/SimpleClientBalanceConsumer.java`
#### Snippet
```java
            return result.isSuccess();
        }
        PartitionSelectResult selectResult = null;
        long startTime = System.currentTimeMillis();
        while (true) {
```

### UnusedAssignment
Variable `msgRspB2C` initializer `null` is redundant
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/SimpleClientBalanceConsumer.java`
#### Snippet
```java
        long startTime = System.currentTimeMillis();
        // Response from broker
        ClientBroker.GetMessageResponseB2C msgRspB2C = null;
        try {
            msgRspB2C =
```

### UnusedAssignment
Variable `minStart` initializer `0` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/disk/FileSegmentList.java`
#### Snippet
```java
            return null;
        }
        int minStart = 0;
        for (minStart = 0; minStart < curViews.length; minStart++) {
            if (curViews[minStart] == null
```

### UnusedAssignment
Variable `minStart` initializer `0` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/disk/FileSegmentList.java`
#### Snippet
```java
            return null;
        }
        int minStart = 0;
        for (minStart = 0; minStart < curViews.length; minStart++) {
            if (curViews[minStart] == null
```

### UnusedAssignment
Variable `mid` initializer `0` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/disk/FileSegmentList.java`
#### Snippet
```java
            return before;
        }
        int mid = 0;
        Segment found = null;
        // Use dichotomy to find the first segment that contains the specified timestamp
```

### UnusedAssignment
Variable `found` initializer `null` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/disk/FileSegmentList.java`
#### Snippet
```java
        }
        int mid = 0;
        Segment found = null;
        // Use dichotomy to find the first segment that contains the specified timestamp
        while (minStart <= hight) {
```

### UnusedAssignment
Variable `lastReadOffset` initializer `-2` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/disk/GetMessageResult.java`
#### Snippet
```java
    public String errInfo;
    public long reqOffset;
    public int lastReadOffset = -2;
    public long lastRdDataOffset;
    public int totalMsgSize;
```

### UnusedAssignment
Variable `retCode` initializer `-1` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/disk/GetMessageResult.java`
#### Snippet
```java

    public boolean isSuccess;
    public int retCode = -1;
    public String errInfo;
    public long reqOffset;
```

### UnusedAssignment
Variable `lastWritePos` initializer `0` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/mem/MsgMemStore.java`
#### Snippet
```java
            long reqRcvTime) {
        // #lizard forgives
        Integer lastWritePos = 0;
        boolean hasMsg = false;
        // judge memory contains the given offset or not.
```

### UnusedAssignment
Variable `lastDataRdOff` initializer `lstRdDataOffset` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/mem/MsgMemStore.java`
#### Snippet
```java
        int currIndexOffset;
        int currDataOffset;
        long lastDataRdOff = lstRdDataOffset;
        int startReadOff = (int) (lstRdIndexOffset - this.writeIndexStartPos);
        this.writeLock.lock();
```

### UnusedAssignment
Variable `cPartitionId` initializer `0` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/mem/MsgMemStore.java`
#### Snippet
```java
        // fetch data by index.
        int readedSize = 0;
        int cPartitionId = 0;
        long cDataPos = 0L;
        int cDataSize = 0;
```

### UnusedAssignment
Variable `cDataPos` initializer `0L` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/mem/MsgMemStore.java`
#### Snippet
```java
        int readedSize = 0;
        int cPartitionId = 0;
        long cDataPos = 0L;
        int cDataSize = 0;
        int cKeyCode = 0;
```

### UnusedAssignment
Variable `cDataSize` initializer `0` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/mem/MsgMemStore.java`
#### Snippet
```java
        int cPartitionId = 0;
        long cDataPos = 0L;
        int cDataSize = 0;
        int cKeyCode = 0;
        long cTimeRecv = 0L;
```

### UnusedAssignment
Variable `cKeyCode` initializer `0` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/mem/MsgMemStore.java`
#### Snippet
```java
        long cDataPos = 0L;
        int cDataSize = 0;
        int cKeyCode = 0;
        long cTimeRecv = 0L;
        int cDataOffset = 0;
```

### UnusedAssignment
Variable `cTimeRecv` initializer `0L` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/mem/MsgMemStore.java`
#### Snippet
```java
        int cDataSize = 0;
        int cKeyCode = 0;
        long cTimeRecv = 0L;
        int cDataOffset = 0;
        ByteBuffer tmpIndexRdBuf = this.cachedIndexSegment.asReadOnlyBuffer();
```

### UnusedAssignment
Variable `cDataOffset` initializer `0` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/mem/MsgMemStore.java`
#### Snippet
```java
        int cKeyCode = 0;
        long cTimeRecv = 0L;
        int cDataOffset = 0;
        ByteBuffer tmpIndexRdBuf = this.cachedIndexSegment.asReadOnlyBuffer();
        ByteBuffer tmpDataRdBuf = this.cacheDataSegment.asReadOnlyBuffer();
```

### UnusedAssignment
Variable `writeIndexStartPos` initializer `-1` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/mem/MsgMemStore.java`
#### Snippet
```java
    private final ByteBuffer cacheDataSegment;
    private final int maxIndexCacheSize;
    private long writeIndexStartPos = -1;
    private final ByteBuffer cachedIndexSegment;
    private final int maxAllowedMsgCount;
```

### UnusedAssignment
Variable `writeDataStartPos` initializer `-1` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/mem/MsgMemStore.java`
#### Snippet
```java
    // where messages in memory will sink to disk
    private final int maxDataCacheSize;
    private long writeDataStartPos = -1;
    private final ByteBuffer cacheDataSegment;
    private final int maxIndexCacheSize;
```

### UnusedAssignment
Variable `dataStore` initializer `null` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/BrokerServiceServer.java`
#### Snippet
```java
        // query data from store manager.
        boolean isGetStore = false;
        MessageStore dataStore = null;
        try {
            dataStore = this.storeManager.getOrCreateMessageStore(topicName, partitionId);
```

### UnusedAssignment
Variable `dataStore` initializer `null` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/BrokerServiceServer.java`
#### Snippet
```java
            }
            heartbeatManager.regConsumerNode(getHeartbeatNodeId(clientId, partStr), clientId, partStr);
            MessageStore dataStore = null;
            try {
                dataStore = this.storeManager.getOrCreateMessageStore(topicName, request.getPartitionId());
```

### UnusedAssignment
Variable `createTime` initializer `System.currentTimeMillis()` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/nodeinfo/ConsumerNodeInfo.java`
#### Snippet
```java
    private final AtomicInteger qryPriorityId =
            new AtomicInteger(TBaseConstants.META_VALUE_UNDEFINED);
    private long createTime = System.currentTimeMillis();

    /**
```

### UnusedAssignment
Variable `isSupportLimit` initializer `false` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/nodeinfo/ConsumerNodeInfo.java`
#### Snippet
```java
    private String addrRcvFrom;
    private String rmtAddrInfo;
    private boolean isSupportLimit = false;
    private long nextStatTime = 0L;
    private long lastGetTime = 0L;
```

### UnusedAssignment
Variable `index` initializer `0` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/web/BrokerAdminServlet.java`
#### Snippet
```java
        Map<String, ConcurrentHashMap<Integer, MessageStore>> messageTopicStores =
                broker.getStoreManager().getMessageStores();
        int index = 0;
        int recordId = 0;
        for (Map.Entry<String, ConcurrentHashMap<Integer, MessageStore>> entry : messageTopicStores.entrySet()) {
```

### UnusedAssignment
Variable `adjOffset` initializer `-1` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/web/BrokerAdminServlet.java`
#### Snippet
```java
    private List<Tuple3<String, Integer, Long>> buildOffsetResetInfo(
            Map<String, Map<Integer, Tuple2<Long, Long>>> topicPartOffsetMap) {
        long adjOffset = -1;
        MessageStore store = null;
        List<Tuple3<String, Integer, Long>> result = new ArrayList<>();
```

### UnusedAssignment
Variable `index` initializer `0` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/web/BrokerAdminServlet.java`
#### Snippet
```java
        boolean needRefresh = (boolean) result.getRetData();
        // query data
        int index = 0;
        int recordId = 0;
        WebParameterUtils.buildSuccessWithDataRetBegin(sBuffer);
```

### UnusedAssignment
Variable `maxAllowRdSize` initializer `262144` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/MessageStore.java`
#### Snippet
```java
    private volatile int writeCacheFlushIntvl;
    private final AtomicLong maxFileValidDurMs = new AtomicLong(0);
    private int maxAllowRdSize = 262144;
    private final AtomicInteger memMaxIndexReadCnt = new AtomicInteger(6000);
    private final AtomicInteger fileMaxIndexReadCnt = new AtomicInteger(8000);
```

### UnusedAssignment
Variable `lastOffset` initializer `0L` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/MessageStore.java`
#### Snippet
```java
     */
    public long getIndexMaxOffset() {
        long lastOffset = 0L;
        if (tubeConfig.isEnableMemStore()) {
            this.writeCacheMutex.readLock().lock();
```

### UnusedAssignment
Variable `appendSuss` initializer `true` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/MessageStore.java`
#### Snippet
```java
        indexBuffer.flip();
        appendResult.putReceivedInfo(messageId, receivedTime);
        boolean appendSuss = true;
        long startTime = System.currentTimeMillis();
        if (this.tubeConfig.isEnableMemStore()) {
```

### UnusedAssignment
Variable `result` initializer `0` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/MessageStore.java`
#### Snippet
```java
                    .append(this.storeKey).toString());
        }
        int result = 0;
        boolean inMemCache = false;
        int maxIndexReadLength = memMaxIndexReadCnt.get();
```

### UnusedAssignment
Variable `lastOffset` initializer `0L` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/MessageStore.java`
#### Snippet
```java
     */
    public long getDataMaxOffset() {
        long lastOffset = 0L;
        if (tubeConfig.isEnableMemStore()) {
            this.writeCacheMutex.readLock().lock();
```

### UnusedAssignment
Variable `next` initializer `0L` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/disk/FileSegment.java`
#### Snippet
```java
        }
        long validBytes = 0L;
        long next = 0L;
        long itemRead = 0L;
        int itemMsglen = 0;
```

### UnusedAssignment
Variable `itemRead` initializer `0L` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/disk/FileSegment.java`
#### Snippet
```java
        long validBytes = 0L;
        long next = 0L;
        long itemRead = 0L;
        int itemMsglen = 0;
        int itemMsgToken = 0;
```

### UnusedAssignment
Variable `itemMsglen` initializer `0` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/disk/FileSegment.java`
#### Snippet
```java
        long next = 0L;
        long itemRead = 0L;
        int itemMsglen = 0;
        int itemMsgToken = 0;
        int itemCheckSum = 0;
```

### UnusedAssignment
Variable `itemMsgToken` initializer `0` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/disk/FileSegment.java`
#### Snippet
```java
        long itemRead = 0L;
        int itemMsglen = 0;
        int itemMsgToken = 0;
        int itemCheckSum = 0;
        long itemNext = 0L;
```

### UnusedAssignment
Variable `itemCheckSum` initializer `0` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/disk/FileSegment.java`
#### Snippet
```java
        int itemMsglen = 0;
        int itemMsgToken = 0;
        int itemCheckSum = 0;
        long itemNext = 0L;
        final ByteBuffer itemBuf = ByteBuffer.allocate(DataStoreUtils.STORE_DATA_HEADER_LEN);
```

### UnusedAssignment
Variable `itemNext` initializer `0L` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/disk/FileSegment.java`
#### Snippet
```java
        int itemMsgToken = 0;
        int itemCheckSum = 0;
        long itemNext = 0L;
        final ByteBuffer itemBuf = ByteBuffer.allocate(DataStoreUtils.STORE_DATA_HEADER_LEN);
        do {
```

### UnusedAssignment
Variable `next` initializer `0L` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/disk/FileSegment.java`
#### Snippet
```java
        }
        long validBytes = 0L;
        long next = 0L;
        long itemRead = 0L;
        int itemMsgPartId = 0;
```

### UnusedAssignment
Variable `itemRead` initializer `0L` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/disk/FileSegment.java`
#### Snippet
```java
        long validBytes = 0L;
        long next = 0L;
        long itemRead = 0L;
        int itemMsgPartId = 0;
        long itemMsgOffset = 0L;
```

### UnusedAssignment
Variable `itemMsgPartId` initializer `0` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/disk/FileSegment.java`
#### Snippet
```java
        long next = 0L;
        long itemRead = 0L;
        int itemMsgPartId = 0;
        long itemMsgOffset = 0L;
        int itemMsglen = 0;
```

### UnusedAssignment
Variable `itemMsgOffset` initializer `0L` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/disk/FileSegment.java`
#### Snippet
```java
        long itemRead = 0L;
        int itemMsgPartId = 0;
        long itemMsgOffset = 0L;
        int itemMsglen = 0;
        int itemKeyCode = 0;
```

### UnusedAssignment
Variable `itemMsglen` initializer `0` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/disk/FileSegment.java`
#### Snippet
```java
        int itemMsgPartId = 0;
        long itemMsgOffset = 0L;
        int itemMsglen = 0;
        int itemKeyCode = 0;
        long itemTimeRecv = 0;
```

### UnusedAssignment
Variable `itemNext` initializer `0L` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/disk/FileSegment.java`
#### Snippet
```java
        int itemKeyCode = 0;
        long itemTimeRecv = 0;
        long itemNext = 0L;
        final ByteBuffer itemBuf = ByteBuffer.allocate(DataStoreUtils.STORE_INDEX_HEAD_LEN);
        do {
```

### UnusedAssignment
The value `itemBuf.getInt()` assigned to `itemKeyCode` is never used
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/disk/FileSegment.java`
#### Snippet
```java
                itemMsgOffset = itemBuf.getLong();
                itemMsglen = itemBuf.getInt();
                itemKeyCode = itemBuf.getInt();
                itemTimeRecv = itemBuf.getLong();
                itemNext = validBytes + DataStoreUtils.STORE_INDEX_HEAD_LEN;
```

### UnusedAssignment
The value `itemBuf.getLong()` assigned to `itemTimeRecv` is never used
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/disk/FileSegment.java`
#### Snippet
```java
                itemMsglen = itemBuf.getInt();
                itemKeyCode = itemBuf.getInt();
                itemTimeRecv = itemBuf.getLong();
                itemNext = validBytes + DataStoreUtils.STORE_INDEX_HEAD_LEN;
                if ((itemMsgPartId < 0)
```

### UnusedAssignment
Variable `mutable` initializer `false` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/disk/FileSegment.java`
#### Snippet
```java
    private final AtomicLong flushedSize;
    private final SegmentType segmentType;
    private volatile boolean mutable = false;
    private long expiredTime = 0;
    private final AtomicBoolean expired = new AtomicBoolean(false);
```

### UnusedAssignment
Variable `needAuthToken` initializer `false` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/webbase/WebMethodMapper.java`
#### Snippet
```java
        public Object webHandler;
        public boolean onlyMasterOp = false;
        public boolean needAuthToken = false;

        public WebApiRegInfo(Method method,
```

### UnusedAssignment
Variable `onlyMasterOp` initializer `false` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/webbase/WebMethodMapper.java`
#### Snippet
```java
        public Method method;
        public Object webHandler;
        public boolean onlyMasterOp = false;
        public boolean needAuthToken = false;

```

### UnusedAssignment
Variable `msgStore` initializer `null` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/MessageStoreManager.java`
#### Snippet
```java
                    @Override
                    public MessageStore call() throws Exception {
                        MessageStore msgStore = null;
                        try {
                            msgStore = new MessageStore(messageStoreManager,
```

### UnusedAssignment
Variable `store` initializer `null` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/MessageStoreManager.java`
#### Snippet
```java
    @Override
    public void getTopicPublishInfos(Map<String, OffsetHistoryInfo> groupOffsetMap) {
        MessageStore store = null;
        Map<Integer, MessageStore> storeMap;
        Map<String, Map<Integer, OffsetCsmRecord>> topicOffsetMap;
```

### UnusedAssignment
Variable `store` initializer `null` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/MessageStoreManager.java`
#### Snippet
```java
    public Map<String, Map<Integer, TopicPubStoreInfo>> getTopicPublishInfos(
            Set<String> topicSet) {
        MessageStore store = null;
        TopicMetadata topicMetadata = null;
        Set<String> qryTopicSet = new HashSet<>();
```

### UnusedAssignment
Variable `topicMetadata` initializer `null` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/MessageStoreManager.java`
#### Snippet
```java
            Set<String> topicSet) {
        MessageStore store = null;
        TopicMetadata topicMetadata = null;
        Set<String> qryTopicSet = new HashSet<>();
        Map<String, Map<Integer, TopicPubStoreInfo>> topicPubStoreInfoMap = new HashMap<>();
```

### UnusedAssignment
Variable `topicMetadata` initializer `null` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/MessageStoreManager.java`
#### Snippet
```java

    private Set<File> getLogDirSet(final BrokerConfig tubeConfig) throws IOException {
        TopicMetadata topicMetadata = null;
        final Set<String> paths = new HashSet<>();
        paths.add(tubeConfig.getPrimaryPath());
```

### UnusedAssignment
Variable `timeoutTime` initializer `0L` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/heartbeat/TimeoutInfo.java`
#### Snippet
```java
public class TimeoutInfo {

    private long timeoutTime = 0L;
    private String secondKey = "";
    private String thirdKey = "";
```

### UnusedAssignment
Variable `inValidTokenCheckTimeMs` initializer `120000` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/aaaserver/SimpleCertificateBrokerHandler.java`
#### Snippet
```java
            LoggerFactory.getLogger(SimpleCertificateBrokerHandler.class);
    private static final int MAX_VISIT_TOKEN_SIZE = 6; // at least 3 items
    private long inValidTokenCheckTimeMs = 120000; // 2 minutes
    private final TubeBroker tubeBroker;
    private final AtomicReference<List<Long>> visitTokenList =
```

### UnusedAssignment
The value `authInfo.getOthParams().trim()` assigned to `othParams` is never used
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/aaaserver/SimpleCertificateMasterHandler.java`
#### Snippet
```java
        inUserName = authInfo.getUserName().trim();
        if (TStringUtils.isNotBlank(authInfo.getOthParams())) {
            othParams = authInfo.getOthParams().trim();
        }
        if (TStringUtils.isBlank(authInfo.getSignature())) {
```

### UnusedAssignment
Variable `midPos` initializer `0` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/disk/MsgFileStore.java`
#### Snippet
```java
            return recordSeg.getStart() + endPos * DataStoreUtils.STORE_INDEX_HEAD_LEN;
        }
        long midPos = 0;
        long startPos = 0;
        long firstLowPos = 0;
```

### UnusedAssignment
Variable `dataRealLimit` initializer `0` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/disk/MsgFileStore.java`
#### Snippet
```java
        String errInfo = "Ok";
        boolean result = true;
        int dataRealLimit = 0;
        int curIndexOffset = 0;
        int readedOffset = 0;
```

### UnusedAssignment
The value `brokerRegMap.remove(nodeId)` assigned to `timeoutInfo` is never used
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/heartbeat/HeartbeatManager.java`
#### Snippet
```java
            return false;
        }
        timeoutInfo = brokerRegMap.remove(nodeId);
        return true;
    }
```

### UnusedAssignment
Variable `curIndexOffset` initializer `0` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/disk/MsgFileStore.java`
#### Snippet
```java
        boolean result = true;
        int dataRealLimit = 0;
        int curIndexOffset = 0;
        int readedOffset = 0;
        Segment recordSeg = null;
```

### UnusedAssignment
Variable `curIndexPartitionId` initializer `0` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/disk/MsgFileStore.java`
#### Snippet
```java
        int readedOffset = 0;
        Segment recordSeg = null;
        int curIndexPartitionId = 0;
        long curIndexDataOffset = 0L;
        int curIndexDataSize = 0;
```

### UnusedAssignment
Variable `curIndexDataOffset` initializer `0L` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/disk/MsgFileStore.java`
#### Snippet
```java
        Segment recordSeg = null;
        int curIndexPartitionId = 0;
        long curIndexDataOffset = 0L;
        int curIndexDataSize = 0;
        int curIndexKeyCode = 0;
```

### UnusedAssignment
Variable `curIndexDataSize` initializer `0` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/disk/MsgFileStore.java`
#### Snippet
```java
        int curIndexPartitionId = 0;
        long curIndexDataOffset = 0L;
        int curIndexDataSize = 0;
        int curIndexKeyCode = 0;
        long recvTimeInMillsec = 0L;
```

### UnusedAssignment
Variable `curIndexKeyCode` initializer `0` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/disk/MsgFileStore.java`
#### Snippet
```java
        long curIndexDataOffset = 0L;
        int curIndexDataSize = 0;
        int curIndexKeyCode = 0;
        long recvTimeInMillsec = 0L;
        long maxDataLimitOffset = 0L;
```

### UnusedAssignment
Variable `recvTimeInMillsec` initializer `0L` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/disk/MsgFileStore.java`
#### Snippet
```java
        int curIndexDataSize = 0;
        int curIndexKeyCode = 0;
        long recvTimeInMillsec = 0L;
        long maxDataLimitOffset = 0L;
        long lastRdDataOffset = 0L;
```

### UnusedAssignment
Variable `maxDataLimitOffset` initializer `0L` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/disk/MsgFileStore.java`
#### Snippet
```java
        int curIndexKeyCode = 0;
        long recvTimeInMillsec = 0L;
        long maxDataLimitOffset = 0L;
        long lastRdDataOffset = 0L;
        final StringBuilder sBuilder = new StringBuilder(512);
```

### UnusedAssignment
Variable `curIndexDataOffset` initializer `0L` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/disk/MsgFileStore.java`
#### Snippet
```java
            long curDataMinOffset, ByteBuffer readBuffer) {
        int curIndexPartitionId = 0;
        long curIndexDataOffset = 0L;
        int curIndexDataSize = 0;
        int curIndexKeyCode = 0;
```

### UnusedAssignment
Variable `curIndexDataSize` initializer `0` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/disk/MsgFileStore.java`
#### Snippet
```java
        int curIndexPartitionId = 0;
        long curIndexDataOffset = 0L;
        int curIndexDataSize = 0;
        int curIndexKeyCode = 0;
        long recvTimeInMillsec = 0L;
```

### UnusedAssignment
Variable `recvTimeInMillsec` initializer `0L` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/disk/MsgFileStore.java`
#### Snippet
```java
        int curIndexDataSize = 0;
        int curIndexKeyCode = 0;
        long recvTimeInMillsec = 0L;
        try {
            readBuffer.clear();
```

### UnusedAssignment
The value `readBuffer.getInt()` assigned to `curIndexPartitionId` is never used
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/disk/MsgFileStore.java`
#### Snippet
```java
            recordSeg.relRead(readBuffer, relReadPos);
            readBuffer.flip();
            curIndexPartitionId = readBuffer.getInt();
            curIndexDataOffset = readBuffer.getLong();
            curIndexDataSize = readBuffer.getInt();
```

### UnusedAssignment
The value `readBuffer.getInt()` assigned to `curIndexKeyCode` is never used
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/disk/MsgFileStore.java`
#### Snippet
```java
            curIndexDataOffset = readBuffer.getLong();
            curIndexDataSize = readBuffer.getInt();
            curIndexKeyCode = readBuffer.getInt();
            recvTimeInMillsec = readBuffer.getLong();
            // skip when mismatch condition
```

### UnusedAssignment
The value `1` assigned to `this.nextPage` is never used
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/common/QueryResult.java`
#### Snippet
```java
    public void setTotalPage(int totalPage) {
        if (totalPage <= 1) {
            this.currentPage = this.lastPage = this.nextPage = 1;
        } else {
            this.totalPage = totalPage;
```

### UnusedAssignment
Variable `brokerInfoMap` initializer `null` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/action/screen/Master.java`
#### Snippet
```java
    private void innGetBrokerInfo(final HttpServletRequest req,
            StringBuilder sBuilder, boolean isOldRet) {
        Map<Integer, BrokerInfo> brokerInfoMap = null;
        BrokerRunManager brokerRunManager = master.getBrokerRunManager();
        String brokerIds = req.getParameter("ids");
```

### UnusedAssignment
Variable `currPartSize` initializer `0` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/action/screen/Master.java`
#### Snippet
```java
        Map<String, Map<String, Map<String, Partition>>> currentSubInfoMap =
                master.getCurrentSubInfoMap();
        int currPartSize = 0;
        Set<String> topicSet;
        List<Partition> partList;
```

### UnusedAssignment
Variable `brokerCount` initializer `0` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebMasterInfoHandler.java`
#### Snippet
```java
        BrokerRunManager brokerRunManager = master.getBrokerRunManager();
        int totalCount = 0;
        int brokerCount = 0;
        int totalCfgNumPartCount = 0;
        int totalRunNumPartCount = 0;
```

### UnusedAssignment
Variable `totalCfgNumPartCount` initializer `0` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebMasterInfoHandler.java`
#### Snippet
```java
        int totalCount = 0;
        int brokerCount = 0;
        int totalCfgNumPartCount = 0;
        int totalRunNumPartCount = 0;
        int totalCfgTopicStoreCount = 0;
```

### UnusedAssignment
Variable `totalRunNumPartCount` initializer `0` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebMasterInfoHandler.java`
#### Snippet
```java
        int brokerCount = 0;
        int totalCfgNumPartCount = 0;
        int totalRunNumPartCount = 0;
        int totalCfgTopicStoreCount = 0;
        int totalRunTopicStoreCount = 0;
```

### UnusedAssignment
Variable `totalCfgTopicStoreCount` initializer `0` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebMasterInfoHandler.java`
#### Snippet
```java
        int totalCfgNumPartCount = 0;
        int totalRunNumPartCount = 0;
        int totalCfgTopicStoreCount = 0;
        int totalRunTopicStoreCount = 0;
        boolean isSrvAcceptPublish = false;
```

### UnusedAssignment
Variable `totalRunTopicStoreCount` initializer `0` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebMasterInfoHandler.java`
#### Snippet
```java
        int totalRunNumPartCount = 0;
        int totalCfgTopicStoreCount = 0;
        int totalRunTopicStoreCount = 0;
        boolean isSrvAcceptPublish = false;
        boolean isSrvAcceptSubscribe = false;
```

### UnusedAssignment
Variable `isSrvAcceptPublish` initializer `false` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebMasterInfoHandler.java`
#### Snippet
```java
        int totalCfgTopicStoreCount = 0;
        int totalRunTopicStoreCount = 0;
        boolean isSrvAcceptPublish = false;
        boolean isSrvAcceptSubscribe = false;
        boolean enableAuthControl = false;
```

### UnusedAssignment
Variable `isSrvAcceptSubscribe` initializer `false` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebMasterInfoHandler.java`
#### Snippet
```java
        int totalRunTopicStoreCount = 0;
        boolean isSrvAcceptPublish = false;
        boolean isSrvAcceptSubscribe = false;
        boolean enableAuthControl = false;
        TopicPropGroup topicProps;
```

### UnusedAssignment
Variable `enableAuthControl` initializer `false` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebMasterInfoHandler.java`
#### Snippet
```java
        boolean isSrvAcceptPublish = false;
        boolean isSrvAcceptSubscribe = false;
        boolean enableAuthControl = false;
        TopicPropGroup topicProps;
        TopicCtrlEntity authEntity;
```

### UnusedAssignment
Variable `decTmpParamVal` initializer `null` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/utils/WebParameterUtils.java`
#### Snippet
```java
            return null;
        }
        String decTmpParamVal = null;
        try {
            decTmpParamVal = URLDecoder.decode(tmpParamValue,
```

### UnusedAssignment
Variable `validDuration` initializer `0` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/utils/WebParameterUtils.java`
#### Snippet
```java
            }
        }
        long validDuration = 0;
        try {
            if (timeUnit.endsWith("s")) {
```

### UnusedAssignment
Variable `action` initializer `null` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/simplemvc/RequestDispatcher.java`
#### Snippet
```java
                String actionKey = clazz.getName().substring(
                        actionPath.length() + 1).replaceAll("\\.", "/");
                Action action = null;
                if (config.isSpringSupported()) {
                    action =
```

### UnusedAssignment
Variable `topicCnt` initializer `0` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebOtherInfoHandler.java`
#### Snippet
```java
                topicPSInfoManager.getGroupSetWithSubTopic(inGroupNameSet, topicNameSet);
        int totalCnt = 0;
        int topicCnt = 0;
        ConsumerInfoHolder consumerHolder = master.getConsumerHolder();
        WebParameterUtils.buildSuccessWithDataRetBegin(sBuffer);
```

### UnusedAssignment
Variable `startIndex` initializer `0` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/TMaster.java`
#### Snippet
```java
                    / masterConfig.getRebalanceParallel();
            // start processor to do balance;
            int startIndex = 0;
            int endIndex = 0;
            // set parallel balance signal
```

### UnusedAssignment
Variable `endIndex` initializer `0` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/TMaster.java`
#### Snippet
```java
            // start processor to do balance;
            int startIndex = 0;
            int endIndex = 0;
            // set parallel balance signal
            final long startBalanceTime = System.currentTimeMillis();
```

### UnusedAssignment
Variable `startIndex` initializer `0` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/TMaster.java`
#### Snippet
```java
                    / masterConfig.getRebalanceParallel();
            // start processor to do balance;
            int startIndex = 0;
            int endIndex = 0;
            // set parallel balance signal
```

### UnusedAssignment
Variable `endIndex` initializer `0` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/TMaster.java`
#### Snippet
```java
            // start processor to do balance;
            int startIndex = 0;
            int endIndex = 0;
            // set parallel balance signal
            curCltBalanceParal.set(masterConfig.getRebalanceParallel());
```

### UnusedAssignment
Variable `count` initializer `0` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/TMaster.java`
#### Snippet
```java
            return result;
        }
        int count = 0;
        int statusId = 0;
        Tuple2<Boolean, TopicInfo> topicSubInfo = new Tuple2<>();
```

### UnusedAssignment
Variable `statusId` initializer `0` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/TMaster.java`
#### Snippet
```java
        }
        int count = 0;
        int statusId = 0;
        Tuple2<Boolean, TopicInfo> topicSubInfo = new Tuple2<>();
        Set<String> fbdTopicSet =
```

### UnusedAssignment
Variable `itemCount` initializer `0` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebTopicDeployHandler.java`
#### Snippet
```java
        // build query result
        int totalCnt = 0;
        int itemCount = 0;
        int totalCfgNumPartCount = 0;
        int totalRunNumPartCount = 0;
```

### UnusedAssignment
Variable `totalCfgNumPartCount` initializer `0` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebTopicDeployHandler.java`
#### Snippet
```java
        int totalCnt = 0;
        int itemCount = 0;
        int totalCfgNumPartCount = 0;
        int totalRunNumPartCount = 0;
        boolean isSrvAcceptPublish = false;
```

### UnusedAssignment
Variable `totalRunNumPartCount` initializer `0` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebTopicDeployHandler.java`
#### Snippet
```java
        int itemCount = 0;
        int totalCfgNumPartCount = 0;
        int totalRunNumPartCount = 0;
        boolean isSrvAcceptPublish = false;
        boolean isSrvAcceptSubscribe = false;
```

### UnusedAssignment
Variable `isSrvAcceptPublish` initializer `false` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebTopicDeployHandler.java`
#### Snippet
```java
        int totalCfgNumPartCount = 0;
        int totalRunNumPartCount = 0;
        boolean isSrvAcceptPublish = false;
        boolean isSrvAcceptSubscribe = false;
        String strManageStatus;
```

### UnusedAssignment
Variable `isSrvAcceptSubscribe` initializer `false` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebTopicDeployHandler.java`
#### Snippet
```java
        int totalRunNumPartCount = 0;
        boolean isSrvAcceptPublish = false;
        boolean isSrvAcceptSubscribe = false;
        String strManageStatus;
        TopicCtrlEntity ctrlEntity;
```

### UnusedAssignment
Variable `itemCount` initializer `0` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebTopicDeployHandler.java`
#### Snippet
```java
        // build query result
        int totalCnt = 0;
        int itemCount = 0;
        int condStatusId = 1;
        int maxMsgSizeInMB = 0;
```

### UnusedAssignment
Variable `condStatusId` initializer `1` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebTopicDeployHandler.java`
#### Snippet
```java
        int totalCnt = 0;
        int itemCount = 0;
        int condStatusId = 1;
        int maxMsgSizeInMB = 0;
        int totalCfgNumPartCount = 0;
```

### UnusedAssignment
Variable `maxMsgSizeInMB` initializer `0` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebTopicDeployHandler.java`
#### Snippet
```java
        int itemCount = 0;
        int condStatusId = 1;
        int maxMsgSizeInMB = 0;
        int totalCfgNumPartCount = 0;
        int totalRunNumPartCount = 0;
```

### UnusedAssignment
Variable `totalCfgNumPartCount` initializer `0` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebTopicDeployHandler.java`
#### Snippet
```java
        int condStatusId = 1;
        int maxMsgSizeInMB = 0;
        int totalCfgNumPartCount = 0;
        int totalRunNumPartCount = 0;
        boolean enableAuthCtrl;
```

### UnusedAssignment
Variable `totalRunNumPartCount` initializer `0` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebTopicDeployHandler.java`
#### Snippet
```java
        int maxMsgSizeInMB = 0;
        int totalCfgNumPartCount = 0;
        int totalRunNumPartCount = 0;
        boolean enableAuthCtrl;
        boolean isSrvAcceptPublish = false;
```

### UnusedAssignment
Variable `isSrvAcceptPublish` initializer `false` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebTopicDeployHandler.java`
#### Snippet
```java
        int totalRunNumPartCount = 0;
        boolean enableAuthCtrl;
        boolean isSrvAcceptPublish = false;
        boolean isSrvAcceptSubscribe = false;
        String strManageStatus;
```

### UnusedAssignment
Variable `isSrvAcceptSubscribe` initializer `false` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebTopicDeployHandler.java`
#### Snippet
```java
        boolean enableAuthCtrl;
        boolean isSrvAcceptPublish = false;
        boolean isSrvAcceptSubscribe = false;
        String strManageStatus;
        TopicCtrlEntity ctrlEntity;
```

### UnusedAssignment
Variable `brokerTotalCfgCnt` initializer `0` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebTopicDeployHandler.java`
#### Snippet
```java
        // set statistic data items
        int recordCount = 0;
        int brokerTotalCfgCnt = 0;
        int storeTotalCfgCnt = 0;
        int partTotalCfgCnt = 0;
```

### UnusedAssignment
Variable `storeTotalCfgCnt` initializer `0` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebTopicDeployHandler.java`
#### Snippet
```java
        int recordCount = 0;
        int brokerTotalCfgCnt = 0;
        int storeTotalCfgCnt = 0;
        int partTotalCfgCnt = 0;
        int tmpPartTtlCount = 0;
```

### UnusedAssignment
Variable `partTotalCfgCnt` initializer `0` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebTopicDeployHandler.java`
#### Snippet
```java
        int brokerTotalCfgCnt = 0;
        int storeTotalCfgCnt = 0;
        int partTotalCfgCnt = 0;
        int tmpPartTtlCount = 0;
        int brokerTotalRunCnt = 0;
```

### UnusedAssignment
Variable `tmpPartTtlCount` initializer `0` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebTopicDeployHandler.java`
#### Snippet
```java
        int storeTotalCfgCnt = 0;
        int partTotalCfgCnt = 0;
        int tmpPartTtlCount = 0;
        int brokerTotalRunCnt = 0;
        int storeTotalRunCnt = 0;
```

### UnusedAssignment
Variable `brokerTotalRunCnt` initializer `0` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebTopicDeployHandler.java`
#### Snippet
```java
        int partTotalCfgCnt = 0;
        int tmpPartTtlCount = 0;
        int brokerTotalRunCnt = 0;
        int storeTotalRunCnt = 0;
        int partTotalRunCnt = 0;
```

### UnusedAssignment
Variable `storeTotalRunCnt` initializer `0` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebTopicDeployHandler.java`
#### Snippet
```java
        int tmpPartTtlCount = 0;
        int brokerTotalRunCnt = 0;
        int storeTotalRunCnt = 0;
        int partTotalRunCnt = 0;
        int brokerAccPubTotalCnt = 0;
```

### UnusedAssignment
Variable `partTotalRunCnt` initializer `0` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebTopicDeployHandler.java`
#### Snippet
```java
        int brokerTotalRunCnt = 0;
        int storeTotalRunCnt = 0;
        int partTotalRunCnt = 0;
        int brokerAccPubTotalCnt = 0;
        int brokerAccSubTotalCnt = 0;
```

### UnusedAssignment
Variable `brokerAccPubTotalCnt` initializer `0` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebTopicDeployHandler.java`
#### Snippet
```java
        int storeTotalRunCnt = 0;
        int partTotalRunCnt = 0;
        int brokerAccPubTotalCnt = 0;
        int brokerAccSubTotalCnt = 0;
        int storeAccPubTotalCnt = 0;
```

### UnusedAssignment
Variable `brokerAccSubTotalCnt` initializer `0` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebTopicDeployHandler.java`
#### Snippet
```java
        int partTotalRunCnt = 0;
        int brokerAccPubTotalCnt = 0;
        int brokerAccSubTotalCnt = 0;
        int storeAccPubTotalCnt = 0;
        int storeAccSubTotalCnt = 0;
```

### UnusedAssignment
Variable `storeAccPubTotalCnt` initializer `0` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebTopicDeployHandler.java`
#### Snippet
```java
        int brokerAccPubTotalCnt = 0;
        int brokerAccSubTotalCnt = 0;
        int storeAccPubTotalCnt = 0;
        int storeAccSubTotalCnt = 0;
        int partAccPubTotalCnt = 0;
```

### UnusedAssignment
Variable `storeAccSubTotalCnt` initializer `0` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebTopicDeployHandler.java`
#### Snippet
```java
        int brokerAccSubTotalCnt = 0;
        int storeAccPubTotalCnt = 0;
        int storeAccSubTotalCnt = 0;
        int partAccPubTotalCnt = 0;
        int partAccSubTotalCnt = 0;
```

### UnusedAssignment
Variable `partAccPubTotalCnt` initializer `0` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebTopicDeployHandler.java`
#### Snippet
```java
        int storeAccPubTotalCnt = 0;
        int storeAccSubTotalCnt = 0;
        int partAccPubTotalCnt = 0;
        int partAccSubTotalCnt = 0;
        boolean hasRunConfig = false;
```

### UnusedAssignment
Variable `partAccSubTotalCnt` initializer `0` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebTopicDeployHandler.java`
#### Snippet
```java
        int storeAccSubTotalCnt = 0;
        int partAccPubTotalCnt = 0;
        int partAccSubTotalCnt = 0;
        boolean hasRunConfig = false;
        boolean topicSrvAccPubStatus = false;
```

### UnusedAssignment
Variable `hasRunConfig` initializer `false` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebTopicDeployHandler.java`
#### Snippet
```java
        int partAccPubTotalCnt = 0;
        int partAccSubTotalCnt = 0;
        boolean hasRunConfig = false;
        boolean topicSrvAccPubStatus = false;
        boolean topicSrvAccSubStatus = false;
```

### UnusedAssignment
Variable `topicSrvAccPubStatus` initializer `false` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebTopicDeployHandler.java`
#### Snippet
```java
        int partAccSubTotalCnt = 0;
        boolean hasRunConfig = false;
        boolean topicSrvAccPubStatus = false;
        boolean topicSrvAccSubStatus = false;
        boolean enableAuthControl = false;
```

### UnusedAssignment
Variable `topicSrvAccSubStatus` initializer `false` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebTopicDeployHandler.java`
#### Snippet
```java
        boolean hasRunConfig = false;
        boolean topicSrvAccPubStatus = false;
        boolean topicSrvAccSubStatus = false;
        boolean enableAuthControl = false;
        // build query result
```

### UnusedAssignment
Variable `enableAuthControl` initializer `false` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebTopicDeployHandler.java`
#### Snippet
```java
        boolean topicSrvAccPubStatus = false;
        boolean topicSrvAccSubStatus = false;
        boolean enableAuthControl = false;
        // build query result
        TopicPropGroup topicProps;
```

### UnusedAssignment
Variable `topicCnt` initializer `0` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebTopicDeployHandler.java`
#### Snippet
```java
        // build query result
        int dataCount = 0;
        int topicCnt = 0;
        WebParameterUtils.buildSuccessWithDataRetBegin(sBuffer);
        for (Map.Entry<Integer, Set<String>> entry : brokerTopicConfigMap.entrySet()) {
```

### UnusedAssignment
Variable `topicTotalCfgCnt` initializer `0` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebTopicDeployHandler.java`
#### Snippet
```java
        // build query result
        int recordCount = 0;
        int topicTotalCfgCnt = 0;
        int storeTotalCfgCnt = 0;
        int partTotalCfgCnt = 0;
```

### UnusedAssignment
Variable `storeTotalCfgCnt` initializer `0` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebTopicDeployHandler.java`
#### Snippet
```java
        int recordCount = 0;
        int topicTotalCfgCnt = 0;
        int storeTotalCfgCnt = 0;
        int partTotalCfgCnt = 0;
        int topicAccPubTotalCnt = 0;
```

### UnusedAssignment
Variable `partTotalCfgCnt` initializer `0` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebTopicDeployHandler.java`
#### Snippet
```java
        int topicTotalCfgCnt = 0;
        int storeTotalCfgCnt = 0;
        int partTotalCfgCnt = 0;
        int topicAccPubTotalCnt = 0;
        int topicAccSubTotalCnt = 0;
```

### UnusedAssignment
Variable `topicAccPubTotalCnt` initializer `0` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebTopicDeployHandler.java`
#### Snippet
```java
        int storeTotalCfgCnt = 0;
        int partTotalCfgCnt = 0;
        int topicAccPubTotalCnt = 0;
        int topicAccSubTotalCnt = 0;
        int storeAccPubTotalCnt = 0;
```

### UnusedAssignment
Variable `topicAccSubTotalCnt` initializer `0` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebTopicDeployHandler.java`
#### Snippet
```java
        int partTotalCfgCnt = 0;
        int topicAccPubTotalCnt = 0;
        int topicAccSubTotalCnt = 0;
        int storeAccPubTotalCnt = 0;
        int storeAccSubTotalCnt = 0;
```

### UnusedAssignment
Variable `storeAccPubTotalCnt` initializer `0` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebTopicDeployHandler.java`
#### Snippet
```java
        int topicAccPubTotalCnt = 0;
        int topicAccSubTotalCnt = 0;
        int storeAccPubTotalCnt = 0;
        int storeAccSubTotalCnt = 0;
        int partAccPubTotalCnt = 0;
```

### UnusedAssignment
Variable `storeAccSubTotalCnt` initializer `0` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebTopicDeployHandler.java`
#### Snippet
```java
        int topicAccSubTotalCnt = 0;
        int storeAccPubTotalCnt = 0;
        int storeAccSubTotalCnt = 0;
        int partAccPubTotalCnt = 0;
        int partAccSubTotalCnt = 0;
```

### UnusedAssignment
Variable `partAccPubTotalCnt` initializer `0` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebTopicDeployHandler.java`
#### Snippet
```java
        int storeAccPubTotalCnt = 0;
        int storeAccSubTotalCnt = 0;
        int partAccPubTotalCnt = 0;
        int partAccSubTotalCnt = 0;
        int tmpPartTtlCount = 0;
```

### UnusedAssignment
Variable `partAccSubTotalCnt` initializer `0` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebTopicDeployHandler.java`
#### Snippet
```java
        int storeAccSubTotalCnt = 0;
        int partAccPubTotalCnt = 0;
        int partAccSubTotalCnt = 0;
        int tmpPartTtlCount = 0;
        String strManageStatus;
```

### UnusedAssignment
Variable `tmpPartTtlCount` initializer `0` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebTopicDeployHandler.java`
#### Snippet
```java
        int partAccPubTotalCnt = 0;
        int partAccSubTotalCnt = 0;
        int tmpPartTtlCount = 0;
        String strManageStatus;
        BrokerRunStatusInfo runStatusInfo;
```

### UnusedAssignment
Variable `condStatusId` initializer `0` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebAdminGroupCtrlHandler.java`
#### Snippet
```java
        // build return result
        int totalCnt = 0;
        int condStatusId = 0;
        String itemFilterStr;
        WebParameterUtils.buildSuccessWithDataRetBegin(sBuffer);
```

### UnusedAssignment
Variable `searched` initializer `1` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/balance/DefaultLoadBalancer.java`
#### Snippet
```java
            Map<String, Map<String, Map<String, Partition>>> oldClusterState,
            List<String> filterList) {
        int searched = 1;
        int consumerSize = consumerList.size();
        for (Partition partition : partitionToAssignMap.values()) {
```

### UnusedAssignment
Variable `consumer` initializer `null` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/balance/DefaultLoadBalancer.java`
#### Snippet
```java
        int consumerSize = consumerList.size();
        for (Partition partition : partitionToAssignMap.values()) {
            ConsumerInfo consumer = null;
            int indeId = RANDOM.nextInt(consumerSize);
            do {
```

### UnusedAssignment
Variable `validDuration` initializer `0` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/metamanage/metastore/dao/entity/TopicPropGroup.java`
#### Snippet
```java

    private Tuple2<CleanPolType, Long> parseDelPolicy(String delPolicy) {
        long validDuration = 0;
        String[] tmpStrs = delPolicy.split(",");
        String validValStr = tmpStrs[1];
```

### UnusedAssignment
Variable `result` initializer `-1` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/nodemanage/nodebroker/BrokerSyncData.java`
#### Snippet
```java
    private int calculateConfigCrc32Value(String brokerConfInfo,
            Map<String, String> topicConfInfoMap) {
        int result = -1;
        int capacity = 0;
        List<String> topicConfInfoLst =
```

### UnusedAssignment
Variable `startTime` initializer `TBaseConstants.META_VALUE_UNDEFINED` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/nodemanage/nodeconsumer/ConsumerInfo.java`
#### Snippet
```java
    private String addrRcvFrom;
    private boolean overTLS = false;
    private long startTime = TBaseConstants.META_VALUE_UNDEFINED;
    private int sourceCount = TBaseConstants.META_VALUE_UNDEFINED;
    private int nodeId = TBaseConstants.META_VALUE_UNDEFINED;
```

### UnusedAssignment
Variable `overTLS` initializer `false` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/nodemanage/nodeconsumer/ConsumerInfo.java`
#### Snippet
```java
    private final Map<String, TreeSet<String>> topicConditions;
    private String addrRcvFrom;
    private boolean overTLS = false;
    private long startTime = TBaseConstants.META_VALUE_UNDEFINED;
    private int sourceCount = TBaseConstants.META_VALUE_UNDEFINED;
```

### UnusedAssignment
Variable `sourceCount` initializer `TBaseConstants.META_VALUE_UNDEFINED` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/nodemanage/nodeconsumer/ConsumerInfo.java`
#### Snippet
```java
    private boolean overTLS = false;
    private long startTime = TBaseConstants.META_VALUE_UNDEFINED;
    private int sourceCount = TBaseConstants.META_VALUE_UNDEFINED;
    private int nodeId = TBaseConstants.META_VALUE_UNDEFINED;
    // for band consume type node
```

### UnusedAssignment
Variable `roundIndex` initializer `0` is redundant
in `inlong-tubemq/tubemq-example/src/main/java/org/apache/inlong/tubemq/example/MessageProducerExample.java`
#### Snippet
```java
        // 5. send message to server
        long sentCount = 0;
        int roundIndex = 0;
        int targetCnt = buildTopicFilterTuples.size();
        while (msgCount < 0 || sentCount < msgCount) {
```

### UnusedAssignment
Variable `roundIndex` initializer `0` is redundant
in `inlong-tubemq/tubemq-example/src/main/java/org/apache/inlong/tubemq/example/MAMessageProducerExample.java`
#### Snippet
```java
            // send message to server
            long sentCount = 0;
            int roundIndex = 0;
            int targetCnt = topicFilterTuples.size();
            while (msgCount < 0 || sentCount < msgCount) {
```

### UnusedAssignment
Variable `value` initializer `0` is redundant
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/entry/TopicStatus.java`
#### Snippet
```java
    ADDING(0), SUCCESS(1), FAILED(2), RETRY(3);

    private int value = 0;

    private TopicStatus(int value) {
```

### UnusedAssignment
Variable `end` initializer `0` is redundant
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/NodeServiceImpl.java`
#### Snippet
```java
        }
        int begin = 0;
        int end = 0;
        do {
            end = Math.min(clusterEntry.getReloadBrokerSize() + begin, needReloadList.size());
```

### UnusedAssignment
Variable `es` initializer `null` is redundant
in `inlong-tubemq/tubemq-connectors/tubemq-connector-flume/src/main/java/org/apache/flume/sink/tubemq/TubemqSink.java`
#### Snippet
```java
         */
        private EventStat fetchEventStat() throws Exception {
            EventStat es = null;
            if (!resendQueue.isEmpty()) {
                es = resendQueue.poll();
```

### UnusedAssignment
Variable `user` initializer `null` is redundant
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/user/UserServiceImpl.java`
#### Snippet
```java
        Collection<Session> sessions = sessionDAO.getActiveSessions();
        if (sessions.size() >= 1) {
            UserInfo user = null;
            for (Session onlineSession : sessions) {
                Object attribute = onlineSession.getAttribute(DefaultSubjectContext.PRINCIPALS_SESSION_KEY);
```

### UnusedAssignment
Variable `data` initializer `null` is redundant
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/stream/InlongStreamServiceImpl.java`
#### Snippet
```java
            throw new BusinessException(ErrorCodeEnum.INVALID_PARAMETER, "Can not properly read update file");
        }
        List<StreamField> data = null;
        try {
            data = ExcelTool.read(inputStream, StreamField.class);
```

### UnusedAssignment
Variable `compressBytes` initializer `null` is redundant
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/common/DefaultEventHandler.java`
#### Snippet
```java
        byte[] srcBytes = objs.build().toByteArray();
        // compress
        byte[] compressBytes = null;
        switch (compressType) {
            case INLONG_SNAPPY:
```

### UnusedAssignment
Variable `leftSize` initializer `0` is redundant
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/utils/SizeSemaphore.java`
#### Snippet
```java

    private int maxSize = 0;
    private int leftSize = 0;
    private Semaphore sizeSemaphore = null;
    private AtomicInteger leftSemaphore = new AtomicInteger(0);
```

### UnusedAssignment
Variable `maxSize` initializer `0` is redundant
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/utils/SizeSemaphore.java`
#### Snippet
```java
    public static final int ONEKB = 1024;

    private int maxSize = 0;
    private int leftSize = 0;
    private Semaphore sizeSemaphore = null;
```

### UnusedAssignment
Variable `sizeSemaphore` initializer `null` is redundant
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/utils/SizeSemaphore.java`
#### Snippet
```java
    private int maxSize = 0;
    private int leftSize = 0;
    private Semaphore sizeSemaphore = null;
    private AtomicInteger leftSemaphore = new AtomicInteger(0);

```

### UnusedAssignment
Variable `endPos` initializer `0` is redundant
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/common/TubeProducerHolder.java`
#### Snippet
```java
        // create producer
        int startPos = 0;
        int endPos = 0;
        Set<String> subTopicSet = new HashSet<>();
        for (Integer dltCnt : topicGroupCnt) {
```

### UnusedAssignment
Variable `maxConnections` initializer `Integer.MAX_VALUE` is redundant
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/ServerMessageFactory.java`
#### Snippet
```java
    private String messageHandlerName;

    private int maxConnections = Integer.MAX_VALUE;

    private int maxMsgLength;
```

### UnusedAssignment
Variable `maxConnections` initializer `Integer.MAX_VALUE` is redundant
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/SourceContext.java`
#### Snippet
```java
    protected int hostPort;
    protected int maxThreads = 32;
    protected int maxConnections = Integer.MAX_VALUE;
    protected int maxMsgLength;
    // metric
```

### UnusedAssignment
Variable `maxThreads` initializer `32` is redundant
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/SourceContext.java`
#### Snippet
```java
    protected String hostIp;
    protected int hostPort;
    protected int maxThreads = 32;
    protected int maxConnections = Integer.MAX_VALUE;
    protected int maxMsgLength;
```

### UnusedAssignment
Variable `fac` initializer `null` is redundant
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/tcp/InlongTcpSource.java`
#### Snippet
```java
                .append(" and serviceDecoderName=").append(serviceDecoderName).toString());

        ChannelInitializer fac = null;
        try {
            Class<? extends ChannelInitializer> clazz = (Class<? extends ChannelInitializer>) Class
```

### UnusedAssignment
Variable `fac` initializer `null` is redundant
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/BaseSource.java`
#### Snippet
```java
        logger.info(new StringBuffer("load msgFactory=").append(msgFactoryName)
                .append(" and serviceDecoderName=").append(serviceDecoderName).toString());
        ChannelInitializer fac = null;
        try {
            ServiceDecoder serviceDecoder = (ServiceDecoder) Class.forName(serviceDecoderName).newInstance();
```

### UnusedAssignment
Variable `msgList` initializer `null` is redundant
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/DefaultServiceDecoder.java`
#### Snippet
```java
        // extract data from bodyData and if message type is 5, convert data into list.
        int calCnt = 0;
        List<ProxyMessage> msgList = null;
        ByteBuffer bodyBuffer = ByteBuffer.wrap(bodyData);
        if (MsgType.MSG_MULTI_BODY.equals(msgType)) {
```

### UnusedAssignment
Variable `strAttr` initializer `null` is redundant
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/DefaultServiceDecoder.java`
#### Snippet
```java
        cb.skipBytes(9 + bodyLen + BIN_HB_ATTRLEN_SIZE);
        // extract common attr
        String strAttr = null;
        if (attrLen != 0) {
            byte[] attrData = new byte[attrLen];
```

### UnusedAssignment
Variable `filterEmptyMsg` initializer `false` is redundant
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/ServerMessageHandler.java`
#### Snippet
```java
    private int maxConnections = Integer.MAX_VALUE;

    private boolean filterEmptyMsg = false;

    private final boolean isCompressed;
```

### UnusedAssignment
Variable `maxConnections` initializer `Integer.MAX_VALUE` is redundant
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/ServerMessageHandler.java`
#### Snippet
```java
    private final ChannelGroup allChannels;

    private int maxConnections = Integer.MAX_VALUE;

    private boolean filterEmptyMsg = false;
```

### UnusedAssignment
Variable `resultMap` initializer `null` is redundant
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/ServerMessageHandler.java`
#### Snippet
```java
            }
            // parse message
            Map<String, Object> resultMap = null;
            final long msgRcvTime = System.currentTimeMillis();
            try {
```

### UnusedAssignment
Variable `newTotalLen` initializer `0` is redundant
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/ServerMessageHandler.java`
#### Snippet
```java
        int attrLen = orgBuf.getShort(BIN_MSG_BODY_OFFSET + dataLen);

        int newTotalLen = 0;
        String strAttr;
        if (attrLen != 0) {
```

### UnusedAssignment
Variable `fac` initializer `null` is redundant
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source2/BaseSource.java`
#### Snippet
```java
     */
    public ChannelInitializer getChannelInitializerFactory() {
        ChannelInitializer fac = null;
        logger.info(this.getName() + " load msgFactory=" + msgFactoryName);
        try {
```

### UnusedAssignment
Variable `backBody` initializer `null` is redundant
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/SimpleMessageHandler.java`
#### Snippet
```java
                            .equals(msgType)) {
                byte[] backAttr = mapJoiner.join(commonAttrMap).getBytes(StandardCharsets.UTF_8);
                byte[] backBody = null;

                if (backAttr != null && !new String(backAttr, StandardCharsets.UTF_8).isEmpty()) {
```

### UnusedAssignment
Variable `newTotalLen` initializer `0` is redundant
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/SimpleMessageHandler.java`
#### Snippet
```java
        int attrLen = orgBuf.getShort(BIN_MSG_BODY_OFFSET + dataLen);

        int newTotalLen = 0;
        String strAttr;
        if (attrLen != 0) {
```

### UnusedAssignment
Variable `resultMap` initializer `null` is redundant
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/SimpleMessageHandler.java`
#### Snippet
```java
                return;
            }
            Map<String, Object> resultMap = null;
            final long msgRcvTime = System.currentTimeMillis();
            try {
```

### UnusedAssignment
Variable `filterEmptyMsg` initializer `false` is redundant
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/SimpleMessageHandler.java`
#### Snippet
```java
    private final ChannelGroup allChannels;
    private int maxConnections = Integer.MAX_VALUE;
    private boolean filterEmptyMsg = false;
    private final boolean isCompressed;
    private final ChannelProcessor processor;
```

### UnusedAssignment
Variable `maxConnections` initializer `Integer.MAX_VALUE` is redundant
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/SimpleMessageHandler.java`
#### Snippet
```java
    private BaseSource source;
    private final ChannelGroup allChannels;
    private int maxConnections = Integer.MAX_VALUE;
    private boolean filterEmptyMsg = false;
    private final boolean isCompressed;
```

### UnusedAssignment
Variable `leftSize` initializer `0` is redundant
in `inlong-sort-standalone/sort-standalone-common/src/main/java/org/apache/inlong/sort/standalone/utils/SizeSemaphore.java`
#### Snippet
```java

    private int maxSize = 0;
    private int leftSize = 0;
    private Semaphore sizeSemaphore = null;
    private AtomicInteger leftSemaphore = new AtomicInteger(0);
```

### UnusedAssignment
Variable `maxSize` initializer `0` is redundant
in `inlong-sort-standalone/sort-standalone-common/src/main/java/org/apache/inlong/sort/standalone/utils/SizeSemaphore.java`
#### Snippet
```java
    public static final int ONEKB = 1024;

    private int maxSize = 0;
    private int leftSize = 0;
    private Semaphore sizeSemaphore = null;
```

### UnusedAssignment
Variable `sizeSemaphore` initializer `null` is redundant
in `inlong-sort-standalone/sort-standalone-common/src/main/java/org/apache/inlong/sort/standalone/utils/SizeSemaphore.java`
#### Snippet
```java
    private int maxSize = 0;
    private int leftSize = 0;
    private Semaphore sizeSemaphore = null;
    private AtomicInteger leftSemaphore = new AtomicInteger(0);

```

### UnusedAssignment
The value `0` assigned to `outputFileSize` is never used
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/hive/PartitionCreateRunnable.java`
#### Snippet
```java
        if (concatInFiles.size() > 0) {
            this.concatInFiles2OuttmpFile(idFile, concatInFiles, fs);
            outputFileSize = 0;
            concatInFiles.clear();
        }
```

### UnusedAssignment
Variable `dispatchQueue` initializer `new LinkedBlockingQueue<>()` is redundant
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/hive/HiveSinkContext.java`
#### Snippet
```java
    private String nodeId;
    private Map<String, HdfsIdConfig> idConfigMap = new ConcurrentHashMap<>();
    private LinkedBlockingQueue<DispatchProfile> dispatchQueue = new LinkedBlockingQueue<>();
    // hdfs config
    private String hdfsPath;
```

### UnusedAssignment
Variable `strContext` initializer `null` is redundant
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/clickhouse/DefaultEventHandler.java`
#### Snippet
```java
        String delimeter = idConfig.getSeparator();
        char cDelimeter = delimeter.charAt(0);
        String strContext = null;
        // for tab separator
        byte[] bodyBytes = event.getBody();
```

### UnusedAssignment
Variable `strContext` initializer `null` is redundant
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/elasticsearch/DefaultEvent2IndexRequestHandler.java`
#### Snippet
```java
        String delimeter = idConfig.getSeparator();
        char cDelimeter = delimeter.charAt(0);
        String strContext = null;
        // for tab separator
        byte[] bodyBytes = event.getBody();
```

### UnusedAssignment
Variable `client` initializer `null` is redundant
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/source/sortsdk/SortSdkSource.java`
#### Snippet
```java
            String configType = CommonPropertiesHolder
                    .getString(SortSourceConfigType.KEY_TYPE, SortSourceConfigType.MANAGER.name());
            SortClient client = null;
            if (SortClusterConfigType.FILE.name().equalsIgnoreCase(configType)) {
                LOG.info("Create sort sdk client in file way:{}", configType);
```

## RuleId[id=MathRoundingWithIntArgument]
### MathRoundingWithIntArgument
`floor()` with argument of type 'int'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/paramcheck/PBParameterUtils.java`
#### Snippet
```java
        int maxBrokerCount =
                brokerRunManager.getSubTopicMaxBrokerCount(inConsumerInfo.getTopicSet());
        int curBClientRate = (int) Math.floor(maxBrokerCount / inConsumerInfo.getSourceCount());
        if (curBClientRate > allowRate) {
            int minClientCnt = maxBrokerCount / allowRate;
```

### MathRoundingWithIntArgument
`floor()` with argument of type 'int'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/balance/DefaultLoadBalancer.java`
#### Snippet
```java
                int maxBrokerCount =
                        brokerRunManager.getSubTopicMaxBrokerCount(topicSet);
                int curBClientRate = (int) Math.floor(maxBrokerCount / newConsumerList.size());
                if (curBClientRate > allowRate) {
                    int minClientCnt = maxBrokerCount / allowRate;
```

### MathRoundingWithIntArgument
`floor()` with argument of type 'int'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/balance/DefaultLoadBalancer.java`
#### Snippet
```java
            int maxBrokerCount =
                    brokerRunManager.getSubTopicMaxBrokerCount(topicSet);
            int curBClientRate = (int) Math.floor(maxBrokerCount / consumerList.size());
            if (curBClientRate > allowRate) {
                int minClientCnt = maxBrokerCount / allowRate;
```

## RuleId[id=WriteOnlyObject]
### WriteOnlyObject
Write-only object
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/config/ProxyClusterConfigHolder.java`
#### Snippet
```java
        List<ProxyInfo> proxys = new ArrayList<>(currentProxyMap.size());
        for (Entry<String, ProxyClusterResult> entry : currentProxyMap.entrySet()) {
            ProxyInfo proxyInfo = new ProxyInfo();
            proxyInfo.setClusterId(entry.getKey());
            proxyInfo.setMd5(entry.getValue().getMd5());
```

### WriteOnlyObject
Write-only object
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/utils/ServiceStatusHolder.java`
#### Snippet
```java

    private static final AtomicLong curWriteIOExcptCnt = new AtomicLong(0);
    private static final AtomicLong lastWriteStatsTime =
            new AtomicLong(System.currentTimeMillis());
    private static final AtomicBoolean isPauseWrite = new AtomicBoolean(false);
```

### WriteOnlyObject
Write-only object
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/utils/ServiceStatusHolder.java`
#### Snippet
```java
    private static long statsDurationMs = 120000;
    private static final AtomicLong curReadIOExcptCnt = new AtomicLong(0);
    private static final AtomicLong lastReadStatsTime =
            new AtomicLong(System.currentTimeMillis());
    private static final AtomicBoolean isPauseRead = new AtomicBoolean(false);
```

### WriteOnlyObject
Write-only object
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/MessageStore.java`
#### Snippet
```java
    private final AtomicInteger memMaxIndexReadCnt = new AtomicInteger(6000);
    private final AtomicInteger fileMaxIndexReadCnt = new AtomicInteger(8000);
    private final AtomicInteger memMaxFilterIndexReadCnt = new AtomicInteger(memMaxIndexReadCnt.get() * 2);
    private final AtomicInteger fileMaxFilterIndexReadCnt = new AtomicInteger(fileMaxIndexReadCnt.get() * 3);
    private final AtomicInteger fileLowReqMaxFilterIndexReadCnt = new AtomicInteger(fileMaxIndexReadCnt.get() * 10);
```

### WriteOnlyObject
Write-only object
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/metamanage/metastore/impl/bdbimpl/BdbMetaConfigMapperImpl.java`
#### Snippet
```java
        Set<String> tmp = new HashSet<>();
        for (ReplicationNode node : replicationGroup.getNodes()) {
            MasterNodeInfo masterNodeInfo =
                    new MasterNodeInfo(replicationGroup.getName(),
                            node.getName(), node.getHostName(), node.getPort());
```

## RuleId[id=SuspiciousNameCombination]
### SuspiciousNameCombination
'y' should probably not be assigned to 'x'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/utils/Bytes.java`
#### Snippet
```java
                            n += 16;
                        } else {
                            x = y;
                        }

```

## RuleId[id=JavadocReference]
### JavadocReference
Cannot resolve symbol `MessageObjs`
in `inlong-sdk/sort-sdk/src/main/java/org/apache/inlong/sdk/sort/impl/decode/MessageDeserializer.java`
#### Snippet
```java
     *
     * @param msgBytes byte[]
     * @return {@link MessageObjs}
     */
    private List<InLongMessage> decodePB(
```

### JavadocReference
Cannot resolve symbol `MessageObjs`
in `inlong-sdk/sort-sdk/src/main/java/org/apache/inlong/sdk/sort/impl/decode/MessageDeserializer.java`
#### Snippet
```java
     * transform MessageObjs to SortSdkMessage
     *
     * @param messageObjs {@link MessageObjs}
     * @return {@link List}
     */
```

### JavadocReference
Cannot resolve symbol `DebeziumSourceFunction`
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/util/DatabaseHistoryUtil.java`
#### Snippet
```java

/**
 * Util to safely visit schema history between {@link DatabaseHistory} and {@link DebeziumSourceFunction}.
 */
public class DatabaseHistoryUtil {
```

### JavadocReference
Cannot resolve symbol `DebeziumSourceFunction`
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/debezium/internal/FlinkOffsetBackingStore.java`
#### Snippet
```java
 * <p>The {@link #OFFSET_STATE_VALUE} in the {@link WorkerConfig} is the raw position and offset
 * data in JSON format. It is set into the config when recovery from failover by {@link
 * DebeziumSourceFunction} before startup the {@link DebeziumEngine}. If it is not a restoration,
 * the {@link #OFFSET_STATE_VALUE} is empty. {@link DebeziumEngine} relies on the {@link
 * OffsetBackingStore} for failover recovery.</p>
```

### JavadocReference
Cannot resolve symbol `DebeziumSourceFunction`
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/debezium/internal/FlinkOffsetBackingStore.java`
#### Snippet
```java
 * OffsetBackingStore} for failover recovery.</p>
 *
 * @see DebeziumSourceFunction
 */
public class FlinkOffsetBackingStore implements OffsetBackingStore {
```

### JavadocReference
Cannot resolve symbol `com.ververica.cdc.connectors.mysql.source.split.MySqlSchemalessSnapshotSplit`
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/mysql/source/split/MySqlSchemalessSnapshotSplit.java`
#### Snippet
```java
/**
 * The MySqlSnapshotSplit without schema information to reduce the memory usage.
 * ref {@link com.ververica.cdc.connectors.mysql.source.split.MySqlSchemalessSnapshotSplit}
 */
public class MySqlSchemalessSnapshotSplit extends MySqlSnapshotSplit {
```

### JavadocReference
Cannot resolve symbol `MySqlSource`
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/mysql/source/config/MySqlSourceConfigFactory.java`
#### Snippet
```java

    /**
     * Whether the {@link MySqlSource} should output the schema changes or not.
     */
    public MySqlSourceConfigFactory includeSchemaChanges(boolean includeSchemaChanges) {
```

### JavadocReference
Cannot resolve symbol `MySqlSource`
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/mysql/source/config/MySqlSourceConfigFactory.java`
#### Snippet
```java

    /**
     * Whether the {@link MySqlSource} should scan the newly added tables or not.
     */
    public MySqlSourceConfigFactory scanNewlyAddedTableEnabled(boolean scanNewlyAddedTableEnabled) {
```

### JavadocReference
Cannot resolve symbol `DebeziumReader`
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/mysql/source/utils/RecordUtils.java`
#### Snippet
```java

    /**
     * Return the timestamp when the change event is fetched in {@link DebeziumReader}.
     *
     * <p>The field `ts_ms` in {@link SourceRecord} data struct is the time when the record fetched
```

### JavadocReference
Cannot resolve symbol `MySqlSourceEnumerator`
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/mysql/source/assigners/state/SnapshotPendingSplitsState.java`
#### Snippet
```java

    /**
     * The offsets of finished (snapshot) splits that the {@link MySqlSourceEnumerator} has received
     * from {@link MySqlSplitReader}s.
     */
```

### JavadocReference
Cannot resolve symbol `MySqlSplitReader`
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/mysql/source/assigners/state/SnapshotPendingSplitsState.java`
#### Snippet
```java
    /**
     * The offsets of finished (snapshot) splits that the {@link MySqlSourceEnumerator} has received
     * from {@link MySqlSplitReader}s.
     */
    private final Map<String, BinlogOffset> splitFinishedOffsets;
```

### JavadocReference
Cannot resolve symbol `MySqlSourceEnumerator`
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/mysql/source/assigners/state/SnapshotPendingSplitsState.java`
#### Snippet
```java

    /**
     * The snapshot splits that the {@link MySqlSourceEnumerator} has assigned to {@link
     * MySqlSplitReader}s.
     */
```

### JavadocReference
Cannot resolve symbol `MySqlSplitReader`
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/mysql/source/assigners/state/SnapshotPendingSplitsState.java`
#### Snippet
```java
    /**
     * The snapshot splits that the {@link MySqlSourceEnumerator} has assigned to {@link
     * MySqlSplitReader}s.
     */
    private final Map<String, MySqlSchemalessSnapshotSplit> assignedSplits;
```

### JavadocReference
Cannot resolve symbol `MySqlSplitState`
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/mysql/debezium/EmbeddedFlinkDatabaseHistory.java`
#### Snippet
```java
 * A {@link DatabaseHistory} implementation which store the latest table schema in Flink state.
 *
 * <p>It stores/recovers history using data offered by {@link MySqlSplitState}.</p>
 */
public class EmbeddedFlinkDatabaseHistory implements DatabaseHistory {
```

### JavadocReference
Cannot resolve symbol `DebeziumSourceFunction`
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mongodb-cdc/src/main/java/org/apache/inlong/sort/cdc/mongodb/debezium/utils/DatabaseHistoryUtil.java`
#### Snippet
```java

/**
 * Util to safely visit schema history between {@link DatabaseHistory} and {@link DebeziumSourceFunction}.
 */
public class DatabaseHistoryUtil {
```

### JavadocReference
Symbol `org.apache.flink.streaming.connectors.elasticsearch.RequestIndexer` is inaccessible from here
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/elasticsearch-base/src/main/java/org/apache/inlong/sort/elasticsearch/RequestIndexer.java`
#### Snippet
```java
/**
 * The base interface for access elasticsearch
 * Copies from {@link org.apache.flink.streaming.connectors.elasticsearch.RequestIndexer} and restructure
 *
 * @param <Request>
```

### JavadocReference
Symbol `org.apache.flink.streaming.connectors.elasticsearch.ActionRequestFailureHandler` is inaccessible from here
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/elasticsearch-base/src/main/java/org/apache/inlong/sort/elasticsearch/ActionRequestFailureHandler.java`
#### Snippet
```java
 * should be handled, e.g. dropping them, reprocessing malformed documents,
 * or simply requesting them to be sent to Elasticsearch again if the failure is only temporary.
 * Copies from {@link org.apache.flink.streaming.connectors.elasticsearch.ActionRequestFailureHandler}
 *
 * @param <ActionRequest>
```

### JavadocReference
Symbol `org.apache.flink.streaming.connectors.elasticsearch.util.IgnoringFailureHandler` is inaccessible from here
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/elasticsearch-base/src/main/java/org/apache/inlong/sort/elasticsearch/utils/IgnoringFailureHandler.java`
#### Snippet
```java
/**
 * Ignores all kinds of failures and drops the affected ActionRequest.
 * Copies from {@link org.apache.flink.streaming.connectors.elasticsearch.util.IgnoringFailureHandler}
 *
 * @param <ActionRequest>
```

### JavadocReference
Symbol `org.apache.flink.streaming.connectors.elasticsearch.util.NoOpFailureHandler` is inaccessible from here
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/elasticsearch-base/src/main/java/org/apache/inlong/sort/elasticsearch/utils/NoOpFailureHandler.java`
#### Snippet
```java
/**
 * An ActionRequestFailureHandler that simply fails the sink on any failures.
 * Copies from {@link org.apache.flink.streaming.connectors.elasticsearch.util.NoOpFailureHandler}
 *
 * @param <ActionRequest>
```

### JavadocReference
Symbol `org.elasticsearch.client.transport.TransportClient` is inaccessible from here
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/elasticsearch-base/src/main/java/org/apache/inlong/sort/elasticsearch/ElasticsearchSinkBase.java`
#### Snippet
```java
     * The config map that contains configuration for the bulk flushing behaviours.
     *
     * <p>For {@link org.elasticsearch.client.transport.TransportClient} based implementations, this
     * config map would also contain Elasticsearch-shipped configuration, and therefore this config
     * map would also be forwarded when creating the Elasticsearch client.
```

### JavadocReference
Cannot resolve symbol `METRICS_GROUP_STREAM_NODE`
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/InlongMetric.java`
#### Snippet
```java
 * The class is the abstract of Inlong Metric.
 * We agree that the key of the inlong metric report is
 * {@link org.apache.inlong.sort.configuration.Constants#METRICS_GROUP_STREAM_NODE},
 * and its value is format by `groupId&streamId&nodeId`.
 * If node implements this interface, we will inject the key and value into the corresponding Sort-Connectors
```

### JavadocReference
Cannot resolve symbol `MessageObj`
in `inlong-sort/sort-formats/format-inlongmsg-pb/src/main/java/org/apache/inlong/sort/formats/inlongmsgpb/InLongMsgPbDeserializationSchema.java`
#### Snippet
```java
/**
 * InLongMsg pb format deserialization schema.
 * Used to deserialize {@link MessageObj} msg.
 */
public class InLongMsgPbDeserializationSchema implements DeserializationSchema<RowData> {
```

### JavadocReference
Cannot resolve symbol `OracleSinkDTO`
in `inlong-manager/manager-pojo/src/main/java/org/apache/inlong/manager/pojo/sink/sqlserver/SQLServerSinkDTO.java`
#### Snippet
```java
     * Get SqlServer table info
     *
     * @param sqlServerSink SqlServer sink dto,{@link OracleSinkDTO}
     * @param columnList SqlServer column info list,{@link OracleColumnInfo}
     * @return {@link SQLServerTableInfo}
```

### JavadocReference
Cannot resolve symbol `OracleColumnInfo`
in `inlong-manager/manager-pojo/src/main/java/org/apache/inlong/manager/pojo/sink/sqlserver/SQLServerSinkDTO.java`
#### Snippet
```java
     *
     * @param sqlServerSink SqlServer sink dto,{@link OracleSinkDTO}
     * @param columnList SqlServer column info list,{@link OracleColumnInfo}
     * @return {@link SQLServerTableInfo}
     */
```

### JavadocReference
Cannot resolve symbol `request`
in `inlong-manager/manager-client/src/main/java/org/apache/inlong/manager/client/api/inner/client/DataNodeClient.java`
#### Snippet
```java
     * Paging query nodes according to conditions.
     *
     * @param request page request conditions
     * @return node list
     */
```

### JavadocReference
Cannot resolve symbol `allClusterList`
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/RandomCacheClusterSelector.java`
#### Snippet
```java
    /**
     * select
     * @param allClusterList
     * @return
     */
```

## RuleId[id=ManualArrayToCollectionCopy]
### ManualArrayToCollectionCopy
Manual array to collection copy
in `inlong-sdk/sort-sdk/src/main/java/org/apache/inlong/sdk/sort/util/Utils.java`
#### Snippet
```java
    public static <T> Set<T> toSet(T... values) {
        Set<T> result = new HashSet<T>();
        for (T v : values) {
            result.add(v);
        }
```

### ManualArrayToCollectionCopy
Manual array to collection copy
in `inlong-sdk/sort-sdk/src/main/java/org/apache/inlong/sdk/sort/util/Utils.java`
#### Snippet
```java
    public static <T> List<T> toList(T... values) {
        List<T> result = new ArrayList<T>();
        for (T v : values) {
            result.add(v);
        }
```

## RuleId[id=SpringPropertySource]
### SpringPropertySource
Cannot resolve file 'application.properties'
in `inlong-audit/audit-store/src/main/java/org/apache/inlong/audit/config/DataServerConfig.java`
#### Snippet
```java
@MapperScan(basePackages = "org.apache.inlong.audit.db.dao")
@PropertySources({
        @PropertySource("classpath:application.properties"),
})

```

## RuleId[id=UnnecessarySemicolon]
### UnnecessarySemicolon
Unnecessary semicolon `;`
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/FileCallback.java`
#### Snippet
```java
    }

    ;

    public void onMessageAck(SendResult result) {
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/FileCallback.java`
#### Snippet
```java
    }

    ;

    /* Invoked when a message transportation interrupted by an exception. */
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/FileCallback.java`
#### Snippet
```java
    }

    ;
}

```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/network/TcpChannel.java`
#### Snippet
```java
     */
    public void acquireUninterruptibly() {
        packToken.acquireUninterruptibly();;
    }

```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/debezium/DebeziumDeserializationSchema.java`
#### Snippet
```java
    default void deserialize(SourceRecord record, Collector<T> out, Boolean isStreamingPhase) throws Exception {
        deserialize(record, out);
    };

}
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/redis/src/main/java/org/apache/inlong/sort/redis/common/config/RedisDataType.java`
#### Snippet
```java
     */
    BITMAP,
    ;
}

```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/redis/src/main/java/org/apache/inlong/sort/redis/common/config/SchemaMappingMode.java`
#### Snippet
```java
     * </code>
     */
    STATIC_KV_PAIR;
}

```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `inlong-manager/manager-pojo/src/main/java/org/apache/inlong/manager/pojo/sink/redis/RedisSchemaMapMode.java`
#### Snippet
```java
    DYNAMIC,
    STATIC_PREFIX_MATCH,
    STATIC_KV_PAIR;
}

```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `inlong-manager/manager-common/src/main/java/org/apache/inlong/manager/common/enums/TimeStaticsDim.java`
#### Snippet
```java
     * Year of time statics dim
     */
    YEAR;
}

```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `inlong-manager/manager-common/src/main/java/org/apache/inlong/manager/common/enums/AuditQuerySource.java`
#### Snippet
```java
     * CLICKHOUSE source of query
     */
    CLICKHOUSE;
}

```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `inlong-manager/manager-common/src/main/java/org/apache/inlong/manager/common/enums/ProcessStatus.java`
#### Snippet
```java
     * Terminated
     */
    TERMINATED;

}
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sink/hudi/HudiFileFormat.java`
#### Snippet
```java
    HUDI_LOG,
    HFILE,
    ORC;

}
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/OrderBatchPackProfileV0.java`
#### Snippet
```java
    public static OrderBatchPackProfileV0 create(SinkRspEvent event) {
        Map<String, String> headers = event.getHeaders();
        String inlongGroupId = headers.get(AttributeConstants.GROUP_ID);;
        String inlongStreamId = headers.get(AttributeConstants.STREAM_ID);
        String uid = InlongId.generateUid(inlongGroupId, inlongStreamId);
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/clickhouse/ClickHouseSinkContext.java`
#### Snippet
```java
    private void initIdConfig(Map<String, ClickHouseIdConfig> newIdConfigMap) throws SQLException {
        try (Connection conn = DriverManager.getConnection(jdbcUrl, jdbcUsername, jdbcPassword);
                Statement stat = conn.createStatement();) {
            for (Entry<String, ClickHouseIdConfig> entry : newIdConfigMap.entrySet()) {
                // parse field list
```

## RuleId[id=SimplifyStreamApiCallChains]
### SimplifyStreamApiCallChains
Can be replaced with 'String.join'
in `inlong-sort/sort-core/src/main/java/org/apache/inlong/sort/parser/impl/FlinkSqlParser.java`
#### Snippet
```java
                            Constants.STREAM_ID + "=" + streamInfo.getStreamId(),
                            Constants.NODE_ID + "=" + node.getId())
                            .collect(Collectors.joining("&")));
            // METRICS_AUDIT_PROXY_HOSTS depends on INLONG_GROUP_STREAM_NODE
            if (StringUtils.isNotEmpty(groupInfo.getProperties().get(Constants.METRICS_AUDIT_PROXY_HOSTS.key()))) {
```

### SimplifyStreamApiCallChains
'Arrays.asList().stream()' can be replaced with 'Arrays.stream()'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/pulsar/src/main/java/org/apache/inlong/sort/pulsar/internal/PulsarMetadataReader.java`
#### Snippet
```java
                        return Collections.singletonList(TopicName.get(e.getValue()).toString());
                    case PulsarOptions.TOPIC_MULTI_OPTION_KEY:
                        return Arrays.asList(e.getValue().split(",")).stream()
                                .filter(s -> !s.isEmpty())
                                .map(t -> TopicName.get(t).toString())
```

### SimplifyStreamApiCallChains
Can be replaced with 'java.util.HashSet' constructor
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/iceberg/src/main/java/org/apache/inlong/sort/iceberg/sink/FlinkSink.java`
#### Snippet
```java
            DataStream<RowData> inputWithRateLimit = appendWithRateLimit(input);
            DataStream<RowData> inputWithMiniBatch = appendWithMiniBatchGroup(
                    inputWithRateLimit, flinkRowType, equalityFieldIds.stream().collect(Collectors.toSet()));

            IcebergProcessOperator<RowData, WriteResult> streamWriter = createStreamWriter(
```

### SimplifyStreamApiCallChains
Can be replaced with 'String.join'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/mysql/source/utils/StatementUtils.java`
#### Snippet
```java
        } else {
            final String orderBy =
                    pkRowType.getFieldNames().stream().collect(Collectors.joining(", "));
            return buildSelectWithBoundaryRowLimits(
                    tableId,
```

### SimplifyStreamApiCallChains
'Arrays.asList().stream()' can be replaced with 'Arrays.stream()'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/starrocks/src/main/java/org/apache/inlong/sort/starrocks/manager/StarRocksStreamLoadVisitor.java`
#### Snippet
```java
                    || StarRocksSinkOptions.StreamLoadFormat.CSV.equals(sinkOptions.getStreamLoadFormat()))) {
                String cols = String.join(",",
                        Arrays.asList(fieldNames).stream().map(f -> String.format("`%s`", f.trim().replace("`", "")))
                                .collect(Collectors.toList()));
                if (cols.length() > 0 && sinkOptions.supportUpsertDelete()) {
```

### SimplifyStreamApiCallChains
Can be replaced with 'Collectors.joining'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/starrocks/src/main/java/org/apache/inlong/sort/starrocks/manager/StarRocksStreamLoadVisitor.java`
#### Snippet
```java
                String cols = String.join(",",
                        Arrays.asList(fieldNames).stream().map(f -> String.format("`%s`", f.trim().replace("`", "")))
                                .collect(Collectors.toList()));
                if (cols.length() > 0 && sinkOptions.supportUpsertDelete()) {
                    cols += String.format(",%s", StarRocksSinkOP.COLUMN_KEY);
```

### SimplifyStreamApiCallChains
'Arrays.asList().stream()' can be replaced with 'Arrays.stream()'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/starrocks/src/main/java/org/apache/inlong/sort/starrocks/manager/StarRocksSinkManager.java`
#### Snippet
```java
                    "Fields count of " + this.sinkOptions.getTableName() + " mismatch. \nflinkSchema["
                            + flinkSchema.getFieldNames().length + "]:"
                            + Arrays.asList(flinkSchema.getFieldNames()).stream().collect(Collectors.joining(","))
                            + "\n realTab[" + rows.size() + "]:"
                            + rows.stream().map((r) -> String.valueOf(r.get("COLUMN_NAME")))
```

### SimplifyStreamApiCallChains
Can be replaced with 'String.join'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/starrocks/src/main/java/org/apache/inlong/sort/starrocks/manager/StarRocksSinkManager.java`
#### Snippet
```java
                    "Fields count of " + this.sinkOptions.getTableName() + " mismatch. \nflinkSchema["
                            + flinkSchema.getFieldNames().length + "]:"
                            + Arrays.asList(flinkSchema.getFieldNames()).stream().collect(Collectors.joining(","))
                            + "\n realTab[" + rows.size() + "]:"
                            + rows.stream().map((r) -> String.valueOf(r.get("COLUMN_NAME")))
```

### SimplifyStreamApiCallChains
Can be replaced with 'String.join'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/oracle-cdc/src/main/java/org/apache/inlong/sort/cdc/oracle/source/utils/OracleUtils.java`
#### Snippet
```java
        } else {
            final String orderBy =
                    pkRowType.getFieldNames().stream().collect(Collectors.joining(", "));
            return buildSelectWithBoundaryRowLimits(
                    tableId,
```

### SimplifyStreamApiCallChains
The 'filter().findAny().isPresent()' chain can be replaced with 'anyMatch()'
in `inlong-agent/agent-core/src/main/java/org/apache/inlong/agent/core/trigger/TriggerManager.java`
#### Snippet
```java
                            boolean alreadyExistTask = job.exist(tasks -> tasks.stream()
                                    .filter(task -> !task.getJobConf().hasKey(JobConstants.JOB_TRIGGER))
                                    .filter(task -> subTaskFile.equals(
                                            task.getJobConf().get(JobConstants.JOB_DIR_FILTER_PATTERNS, "")))
                                    .findAny().isPresent());
                            if (!alreadyExistTask) {
                                LOGGER.info("Trigger job {} add new task file {}, total task {}",
```

### SimplifyStreamApiCallChains
'!Stream.anyMatch(...)' can be replaced with 'noneMatch()'
in `inlong-agent/agent-core/src/main/java/org/apache/inlong/agent/core/trigger/TriggerManager.java`
#### Snippet
```java
        // repeat check
        if (!triggerProfileDB.getTriggers().stream()
                .anyMatch(profile -> profile.getTriggerId().equals(triggerId))) {
            return;
        }
```

### SimplifyStreamApiCallChains
Can be replaced with 'java.util.ArrayList' constructor
in `inlong-agent/agent-common/src/main/java/org/apache/inlong/agent/utils/PathUtils.java`
#### Snippet
```java
                .map(PathUtils::findRootPath)
                .collect(Collectors.toSet())
                .stream()
                .collect(Collectors.toList());
        Set<String> mergedRootDirs = new HashSet<>();
        for (int i = 0; i < rootDirs.size(); i++) {
```

### SimplifyStreamApiCallChains
The 'filter().findAny().isPresent()' chain can be replaced with 'anyMatch()'
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/trigger/PathPattern.java`
#### Snippet
```java
        File file = new File(path);
        return whiteList.stream()
                .filter(whiteRegex -> whiteRegex.match(file))
                .findAny()
                .isPresent();
    }

```

### SimplifyStreamApiCallChains
The 'filter().findAny().isPresent()' chain can be replaced with 'anyMatch()'
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/trigger/DirectoryTrigger.java`
#### Snippet
```java
            try {
                boolean isSuitable = trigger.pathPatterns.stream()
                        .filter(pathPattern -> pathPattern.suitable(path.toString())).findAny().isPresent();
                if (isSuitable) {
                    LOGGER.info("path {} is suitable for trigger_{}.",
```

## RuleId[id=StringOperationCanBeSimplified]
### StringOperationCanBeSimplified
Call to `toString()` is redundant
in `inlong-sdk/sort-sdk/src/main/java/org/apache/inlong/sdk/sort/util/TimeUtil.java`
#### Snippet
```java
                .map(s -> {
                    try {
                        LocalDateTime time = LocalDateTime.parse(s.toString(), DATE_FORMAT);
                        return LocalDateTime.from(time).atZone(ZoneId.systemDefault()).toInstant().toEpochMilli();
                    } catch (Throwable t) {
```

### StringOperationCanBeSimplified
Call to `toString()` is redundant
in `inlong-sdk/sort-sdk/src/main/java/org/apache/inlong/sdk/sort/util/TimeUtil.java`
#### Snippet
```java
                .map(s -> {
                    try {
                        LocalDateTime time = LocalDateTime.parse(s.toString(), DATE_FORMAT);
                        return LocalDateTime.from(time).atZone(ZoneId.systemDefault()).toInstant().toEpochMilli();
                    } catch (Throwable t) {
```

### StringOperationCanBeSimplified
Call to `substring()` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/utils/WebParameterUtils.java`
#### Snippet
```java
        if (Character.isLetter(timeUnit.charAt(0))) {
            result.setSuccResult(sBuffer.append("delete,")
                    .append(validValStr.substring(0, validValStr.length() - 1))
                    .append(timeUnit).toString());
        } else {
```

### StringOperationCanBeSimplified
Call to `substring()` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/simplemvc/RequestContext.java`
#### Snippet
```java
        if (lastSlashIndex >= 0) {
            requestTarget = new StringBuilder(512)
                    .append(requestTarget.substring(0, lastSlashIndex)).append("/")
                    .append(TStringUtils.toCamelCase(requestTarget.substring(lastSlashIndex + 1))).toString();
        } else {
```

### StringOperationCanBeSimplified
Unnecessary string length argument
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/simplemvc/RequestDispatcher.java`
#### Snippet
```java
        for (File file : fileList) {
            String filePath = file.toURI().getPath();
            String relativePath = filePath.substring(templatePath.length(), filePath.length());
            if (relativePath.startsWith("/")) {
                relativePath = relativePath.substring(1);
```

### StringOperationCanBeSimplified
Call to `toString()` is redundant
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/admin/ProxyServiceAdminEventHandler.java`
#### Snippet
```java
                    + JMX_TYPE + PROPERTY_EQUAL + MBEAN_TYPE + PROPERTY_SEPARATOR
                    + "*";
            ObjectName objName = new ObjectName(beanName.toString());
            final MBeanServer mbs = ManagementFactory.getPlatformMBeanServer();
            Set<ObjectInstance> mbeans = mbs.queryMBeans(objName, null);
```

### StringOperationCanBeSimplified
Call to `toString()` is redundant
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/admin/ConsumerServiceAdminEventHandler.java`
#### Snippet
```java
                    + JMX_TYPE + PROPERTY_EQUAL + MBEAN_TYPE + PROPERTY_SEPARATOR
                    + "*";
            ObjectName objName = new ObjectName(beanName.toString());
            final MBeanServer mbs = ManagementFactory.getPlatformMBeanServer();
            Set<ObjectInstance> mbeans = mbs.queryMBeans(objName, null);
```

## RuleId[id=PlaceholderCountMatchesArgumentCount]
### PlaceholderCountMatchesArgumentCount
More arguments provided (3) than placeholders specified (0)
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/SdkProxyChannelManager.java`
#### Snippet
```java
        SdkProfile tProfile = this.profileMap.remove(sdkPackId);
        if (tProfile == null) {
            LOG.error("proxyClusterId:%s,Can not find MessageDispatchProfile by sdkPackId:%d,from:%s",
                    proxyClusterId, sdkPackId, String.valueOf(e.getChannel().getRemoteAddress()));
            this.setChannelException(e.getChannel());
```

### PlaceholderCountMatchesArgumentCount
Fewer arguments provided (0) than placeholders specified (1)
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
            tx.commit();
        } catch (Exception e) {
            LOG.error("Put event failed:{}", e);
            try {
                tx.rollback();
```

### PlaceholderCountMatchesArgumentCount
Fewer arguments provided (0) than placeholders specified (1)
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
            tx.commit();
        } catch (Exception e) {
            LOG.error("Put event failed:{}", e);
            try {
                tx.rollback();
```

### PlaceholderCountMatchesArgumentCount
More arguments provided (2) than placeholders specified (1)
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/utils/ServiceDiscoveryUtils.java`
#### Snippet
```java
                JsonObject jb = jsonParser.parse(returnStr).getAsJsonObject();
                if (jb == null) {
                    log.warn("ServiceDiscovery updated manager ip failed, returnStr = {} jb is "
                            + "null ", returnStr, jb);
                    return null;
                }
```

### PlaceholderCountMatchesArgumentCount
Fewer arguments provided (0) than placeholders specified (1)
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/config/EncryptConfigEntry.java`
#### Snippet
```java
                return rsaEncryptedKey;
            } catch (Exception e) {
                logger.error("RSA Encrypt error {}", e);
                return null;
            }
```

### PlaceholderCountMatchesArgumentCount
Fewer arguments provided (0) than placeholders specified (1)
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/config/EncryptConfigEntry.java`
#### Snippet
```java
                    return new EncryptInfo(this.version, this.rsaEncryptedKey, this.aesKey);
                } catch (Throwable e) {
                    logger.error("getRsaEncryptInfo failure, RSA Encrypt error {}", e);
                    return null;
                }
```

### PlaceholderCountMatchesArgumentCount
Fewer arguments provided (0) than placeholders specified (1)
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/example/TcpClientExample.java`
#### Snippet
```java
            messageSender.setMsgtype(msgType);
        } catch (Exception e) {
            logger.error("getMessageSender has exception e = {}", e);
        }
        return messageSender;
```

### PlaceholderCountMatchesArgumentCount
Fewer arguments provided (0) than placeholders specified (1)
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/codec/ProtocolEncoder.java`
#### Snippet
```java
            buf = constructBody(body, object, totalLength, cnt);
        } catch (Exception e) {
            logger.error("writeToBuf7 has {}", e);
        }
        return buf;
```

### PlaceholderCountMatchesArgumentCount
Fewer arguments provided (0) than placeholders specified (1)
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/utils/EncryptUtil.java`
#### Snippet
```java
            return cipher.doFinal(plainText);
        } catch (Exception e) {
            logger.error("aesEncrypt error {}", e);
            return null;
        }
```

### PlaceholderCountMatchesArgumentCount
Fewer arguments provided (0) than placeholders specified (1)
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/utils/EncryptUtil.java`
#### Snippet
```java
            kg = KeyGenerator.getInstance(AES);
        } catch (NoSuchAlgorithmException e) {
            logger.error("generate Aes key error {}", e);
        }

```

### PlaceholderCountMatchesArgumentCount
Fewer arguments provided (0) than placeholders specified (1)
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/utils/EncryptUtil.java`
#### Snippet
```java
            return cipher.doFinal(cipherText);
        } catch (Exception e) {
            logger.error("aesDecrypt error {}", e);
            return null;
        }
```

### PlaceholderCountMatchesArgumentCount
Fewer arguments provided (0) than placeholders specified (1)
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/example/UdpClientExample.java`
#### Snippet
```java
            logger.info("send = [{}/{}]", ip, port);
        } catch (InterruptedException e) {
            logger.info("send has exception e = {}", e);
        }
        return true;
```

### PlaceholderCountMatchesArgumentCount
Fewer arguments provided (0) than placeholders specified (1)
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/example/UdpClientExample.java`
#### Snippet
```java
            channel = bootstrap.bind(0).sync().channel();
        } catch (Exception e) {
            logger.error("Connection has exception e = {}", e);
        }
        return channel;
```

### PlaceholderCountMatchesArgumentCount
Fewer arguments provided (0) than placeholders specified (1)
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/sinks/SenderManager.java`
#### Snippet
```java
            resendQueue.put(batchMessageCallBack);
        } catch (Throwable throwable) {
            LOGGER.error("putInResendQueue e = {}", throwable);
        }
    }
```

### PlaceholderCountMatchesArgumentCount
Fewer arguments provided (0) than placeholders specified (1)
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/sources/reader/file/FileReaderOperator.java`
#### Snippet
```java
            }
        } catch (Exception e) {
            LOGGER.error("getStartPositon err {}", e);
        } finally {
            if (reader != null) {
```

### PlaceholderCountMatchesArgumentCount
Fewer arguments provided (0) than placeholders specified (1)
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/PulsarSink.java`
#### Snippet
```java
                tx.rollback();
            } catch (Throwable e) {
                logger.error("Pulsar Sink transaction rollback exception = {}", e);
            }
        } finally {
```

### PlaceholderCountMatchesArgumentCount
Fewer arguments provided (0) than placeholders specified (1)
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/PulsarSink.java`
#### Snippet
```java
            }
        } catch (Throwable throwable) {
            logger.error("resendEvent e = {}", throwable);
        }
    }
```

### PlaceholderCountMatchesArgumentCount
Fewer arguments provided (0) than placeholders specified (1)
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/pulsar/PulsarClientService.java`
#### Snippet
```java
                    .create();
        } catch (PulsarClientException e) {
            logger.error("create pulsar client has error e = {}", e);
        }
        return producer;
```

### PlaceholderCountMatchesArgumentCount
More arguments provided (1) than placeholders specified (0)
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/source/SimpleTcpSource.java`
#### Snippet
```java
            maxConnections = context.getInteger(CONNECTIONS, DEFAULT_MAX_CONNECTIONS);
        } catch (NumberFormatException e) {
            logger.warn("BaseSource\'s \"connections\" property must specify an integer value.",
                    context.getString(CONNECTIONS));
        }
```

### PlaceholderCountMatchesArgumentCount
Fewer arguments provided (0) than placeholders specified (1)
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/KafkaSink.java`
#### Snippet
```java
            }
        } catch (Throwable throwable) {
            logger.error("resendEvent e = {}", throwable);
        }
    }
```

### PlaceholderCountMatchesArgumentCount
Fewer arguments provided (0) than placeholders specified (1)
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/KafkaSink.java`
#### Snippet
```java
                tx.rollback();
            } catch (Throwable e) {
                logger.error("Kafka Sink transaction rollback exception = {}", e);
            }
        } finally {
```

### PlaceholderCountMatchesArgumentCount
Fewer arguments provided (0) than placeholders specified (1)
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/TubeSink.java`
#### Snippet
```java
            }
        } catch (Throwable throwable) {
            logger.error("resendEvent e = {}", throwable);
        }
    }
```

### PlaceholderCountMatchesArgumentCount
Fewer arguments provided (0) than placeholders specified (1)
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/TubeSink.java`
#### Snippet
```java
                    + "maybe zkstr/zkroot set error, please re-check");
        } catch (Throwable e) {
            logger.error("create connnection error in tubesink, "
                    + "maybe tube master set error/shutdown in progress, please re-check. ex2 {}", e);
            throw new FlumeException("connect to meta error2, "
                    + "maybe tube master set error/shutdown in progress, please re-check");
```

### PlaceholderCountMatchesArgumentCount
Fewer arguments provided (1) than placeholders specified (2)
in `inlong-audit/audit-common/src/main/java/org/apache/inlong/audit/file/holder/PropertiesConfigHolder.java`
#### Snippet
```java
            }
        } catch (UnsupportedEncodingException e) {
            LOG.error("fail to load properties, file ={}, and e= {}", getFileName(), e);
        } catch (Exception e) {
            LOG.error("fail to load properties, file ={}, and e= {}", getFileName(), e);
```

### PlaceholderCountMatchesArgumentCount
Fewer arguments provided (1) than placeholders specified (2)
in `inlong-audit/audit-common/src/main/java/org/apache/inlong/audit/file/holder/PropertiesConfigHolder.java`
#### Snippet
```java
            LOG.error("fail to load properties, file ={}, and e= {}", getFileName(), e);
        } catch (Exception e) {
            LOG.error("fail to load properties, file ={}, and e= {}", getFileName(), e);
        } finally {
            if (null != inStream) {
```

### PlaceholderCountMatchesArgumentCount
Fewer arguments provided (0) than placeholders specified (1)
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/netty/NettyRpcServer.java`
#### Snippet
```java
        public void exceptionCaught(ChannelHandlerContext ctx, Throwable e) {
            if (!(e.getCause() instanceof IOException)) {
                logger.error("catch some exception not IOException {}", e);
            }
            ctx.fireExceptionCaught(e);
```

### PlaceholderCountMatchesArgumentCount
Fewer arguments provided (0) than placeholders specified (1)
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/NodeServiceImpl.java`
#### Snippet
```java
            masterRepository.saveAndFlush(masterEntry);
        } catch (Exception e) {
            log.error("save masterEntry {} to db fail with ex ", e);
            throw new RuntimeException(e.getMessage());
        }
```

### PlaceholderCountMatchesArgumentCount
More arguments provided (1) than placeholders specified (0)
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/utils/MessageUtils.java`
#### Snippet
```java
            if (logCounter.shouldPrint()) {
                if (remoteChannel == null) {
                    logger.warn("remoteChannel == null, discard it!", remoteChannel);
                } else {
                    logger.warn("Unknown msgType message from {}, discard it!", remoteChannel);
```

### PlaceholderCountMatchesArgumentCount
More arguments provided (1) than placeholders specified (0)
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/SimpleUdpSource.java`
#### Snippet
```java
            maxThreads = context.getInteger(ConfigConstants.MAX_THREADS, 32);
        } catch (NumberFormatException e) {
            logger.warn("Simple UDP Source max-threads property must specify an integer value.",
                    context.getString(ConfigConstants.MAX_THREADS));
        }
```

### PlaceholderCountMatchesArgumentCount
Fewer arguments provided (0) than placeholders specified (1)
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/BaseSource.java`
#### Snippet
```java
                allChannels.close().awaitUninterruptibly();
            } catch (Exception e) {
                logger.warn("Simple Source netty server stop ex, {}", e);
            } finally {
                allChannels.clear();
```

### PlaceholderCountMatchesArgumentCount
Fewer arguments provided (0) than placeholders specified (1)
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/BaseSource.java`
#### Snippet
```java
                channelFuture.channel().closeFuture().sync();
            } catch (InterruptedException e) {
                logger.warn("Simple Source netty server stop ex, {}", e);
            }
        }
```

### PlaceholderCountMatchesArgumentCount
More arguments provided (1) than placeholders specified (0)
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/BaseSource.java`
#### Snippet
```java
            maxConnections = context.getInteger(CONNECTIONS, DEFAULT_MAX_CONNECTIONS);
        } catch (NumberFormatException e) {
            logger.warn("BaseSource\'s \"connections\" property must specify an integer value.",
                    context.getString(CONNECTIONS));
        }
```

### PlaceholderCountMatchesArgumentCount
Fewer arguments provided (0) than placeholders specified (1)
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/tcp/InlongTcpChannelHandler.java`
#### Snippet
```java
            ctx.channel().close();
        } catch (Exception ex) {
            LOG.error("channelInactive has exception e = {}", ex);
        }
        sourceContext.getAllChannels().remove(ctx.channel());
```

### PlaceholderCountMatchesArgumentCount
Fewer arguments provided (0) than placeholders specified (1)
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/tcp/InlongTcpChannelHandler.java`
#### Snippet
```java
    @Override
    public void exceptionCaught(ChannelHandlerContext ctx, Throwable cause) {
        LOG.error("exception caught cause = {}", cause);
        ctx.fireExceptionCaught(cause);
        if (ctx.channel() != null) {
```

### PlaceholderCountMatchesArgumentCount
Fewer arguments provided (0) than placeholders specified (1)
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/ServerMessageHandler.java`
#### Snippet
```java
                }
            } catch (MessageIDException ex) {
                logger.error("MessageIDException ex = {}", ex);
                throw new IOException(ex.getCause());
            }
```

### PlaceholderCountMatchesArgumentCount
Fewer arguments provided (0) than placeholders specified (1)
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/SimpleMessageHandler.java`
#### Snippet
```java
    @Override
    public void exceptionCaught(ChannelHandlerContext ctx, Throwable cause) throws Exception {
        logger.error("exception caught cause = {}", cause);
        ctx.fireExceptionCaught(cause);
        ctx.close();
```

## RuleId[id=UnnecessaryCallToStringValueOf]
### UnnecessaryCallToStringValueOf
Unnecessary `String.valueOf()` call
in `inlong-sdk/sort-sdk/src/main/java/org/apache/inlong/sdk/sort/entity/MessageRecord.java`
#### Snippet
```java
        return "MessageRecord{"
                + "msgKey='" + msgKey
                + ", message=" + String.valueOf(msgs)
                + ", offset='" + offset
                + ", recTime=" + recTime
```

### UnnecessaryCallToStringValueOf
Unnecessary `String.valueOf()` call
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/SdkProxyChannelManager.java`
#### Snippet
```java
        if (packVersion != SdkSinkContext.PACK_VERSION) {
            LOG.error("proxyClusterId:{},Error result from:{},error pack version:{}",
                    proxyClusterId, String.valueOf(e.getChannel().getRemoteAddress()), packVersion);
            this.setChannelException(e.getChannel());
            return;
```

### UnnecessaryCallToStringValueOf
Unnecessary `String.valueOf()` call
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/SdkProxyChannelManager.java`
#### Snippet
```java
        } catch (Exception ex) {
            LOG.error("proxyClusterId:{},Error result from:{},parseFrom exception:{}",
                    proxyClusterId, String.valueOf(e.getChannel().getRemoteAddress()), ex);
            this.setChannelException(e.getChannel());
            return;
```

### UnnecessaryCallToStringValueOf
Unnecessary `String.valueOf()` call
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/SdkProxyChannelManager.java`
#### Snippet
```java
        if (tProfile == null) {
            LOG.error("proxyClusterId:%s,Can not find MessageDispatchProfile by sdkPackId:%d,from:%s",
                    proxyClusterId, sdkPackId, String.valueOf(e.getChannel().getRemoteAddress()));
            this.setChannelException(e.getChannel());
            return;
```

### UnnecessaryCallToStringValueOf
Unnecessary `String.valueOf()` call
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/fileconfig/AbstractFileConfig.java`
#### Snippet
```java
                }
                int port = Integer.parseInt(hostPortItem[1].trim());
                auditAddrs.add(hostName + TokenConstants.ATTR_SEP + String.valueOf(port));
            }
            Collections.sort(auditAddrs);
```

## RuleId[id=ObviousNullCheck]
### ObviousNullCheck
Redundant null-check: a value of primitive type 'char' is never null
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/deserialization/KvDeserializationInfo.java`
#### Snippet
```java
            @JsonProperty("entry_splitter") char entrySplitter,
            @JsonProperty("kv_splitter") char kvSplitter) {
        this.entrySplitter = checkNotNull(entrySplitter);
        this.kvSplitter = checkNotNull(kvSplitter);
    }
```

### ObviousNullCheck
Redundant null-check: a value of primitive type 'char' is never null
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/deserialization/KvDeserializationInfo.java`
#### Snippet
```java
            @JsonProperty("kv_splitter") char kvSplitter) {
        this.entrySplitter = checkNotNull(entrySplitter);
        this.kvSplitter = checkNotNull(kvSplitter);
    }

```

## RuleId[id=DuplicatedCode]
### DuplicatedCode
Duplicated code
in `inlong-agent/agent-common/src/main/java/org/apache/inlong/agent/metrics/AgentPrometheusMetricListener.java`
#### Snippet
```java
            addCounterMetricFamily(M_JOB_RUNNING_COUNT, itemValue, idCounter);
            addCounterMetricFamily(M_JOB_FATAL_COUNT, itemValue, idCounter);

            addCounterMetricFamily(M_TASK_RUNNING_COUNT, itemValue, idCounter);
            addCounterMetricFamily(M_TASK_RETRYING_COUNT, itemValue, idCounter);
            addCounterMetricFamily(M_TASK_FATAL_COUNT, itemValue, idCounter);

            addCounterMetricFamily(M_SINK_SUCCESS_COUNT, itemValue, idCounter);
            addCounterMetricFamily(M_SINK_FAIL_COUNT, itemValue, idCounter);

            addCounterMetricFamily(M_SOURCE_SUCCESS_COUNT, itemValue, idCounter);
            addCounterMetricFamily(M_SOURCE_FAIL_COUNT, itemValue, idCounter);

            addCounterMetricFamily(M_PLUGIN_READ_COUNT, itemValue, idCounter);
            addCounterMetricFamily(M_PLUGIN_SEND_COUNT, itemValue, idCounter);
            addCounterMetricFamily(M_PLUGIN_READ_FAIL_COUNT, itemValue, idCounter);
            addCounterMetricFamily(M_PLUGIN_SEND_FAIL_COUNT, itemValue, idCounter);
```

### DuplicatedCode
Duplicated code
in `inlong-agent/agent-common/src/main/java/org/apache/inlong/agent/metrics/AgentPrometheusMetricListener.java`
#### Snippet
```java
            for (Entry<String, MetricValue> entry : itemValue.getMetrics().entrySet()) {
                String fieldName = entry.getValue().name;
                AtomicLong metricValue = this.metricValueMap.get(fieldName);
                if (metricValue != null) {
                    long fieldValue = entry.getValue().value;
                    metricValue.addAndGet(fieldValue);
                }
            }
            // id dimension
            String dimensionKey = itemValue.getKey();
            MetricItemValue dimensionMetricValue = this.dimensionMetricValueMap.get(dimensionKey);
```

### DuplicatedCode
Duplicated code
in `inlong-agent/agent-common/src/main/java/org/apache/inlong/agent/metrics/AgentPrometheusMetricListener.java`
#### Snippet
```java
                this.dimensionMetricValueMap.putIfAbsent(dimensionKey, dimensionMetricValue);
                dimensionMetricValue = this.dimensionMetricValueMap.get(dimensionKey);
                dimensionMetricValue.getDimensions().putAll(itemValue.getDimensions());
                // add prometheus label name
                for (Entry<String, String> entry : itemValue.getDimensions().entrySet()) {
                    if (!this.dimensionKeys.contains(entry.getKey())) {
                        this.dimensionKeys.add(entry.getKey());
                    }
                }
```

### DuplicatedCode
Duplicated code
in `inlong-agent/agent-common/src/main/java/org/apache/inlong/agent/metrics/AgentPrometheusMetricListener.java`
#### Snippet
```java
            for (Entry<String, MetricValue> entry : itemValue.getMetrics().entrySet()) {
                String fieldName = entry.getValue().name;
                MetricValue metricValue = dimensionMetricValue.getMetrics().get(fieldName);
                if (metricValue == null) {
                    metricValue = MetricValue.of(fieldName, entry.getValue().value);
                    dimensionMetricValue.getMetrics().put(metricValue.name, metricValue);
                    continue;
                }
                metricValue.value += entry.getValue().value;
            }
```

### DuplicatedCode
Duplicated code
in `inlong-agent/agent-common/src/main/java/org/apache/inlong/agent/metrics/AgentPrometheusMetricListener.java`
#### Snippet
```java
        long value = 0L;
        Map<String, MetricValue> metricValueMap = itemValue.getMetrics();
        MetricValue metricValue = metricValueMap.get(defaultDimension);
        if (metricValue != null) {
            value = metricValue.value;
        }
        idCounter.addMetric(labelValues, value);
```

### DuplicatedCode
Duplicated code
in `inlong-agent/agent-common/src/main/java/org/apache/inlong/agent/pojo/JobProfileDto.java`
#### Snippet
```java
        Proxy proxy = new Proxy();
        Manager manager = new Manager();
        AgentConfiguration agentConf = AgentConfiguration.getAgentConf();
        manager.setHost(agentConf.get(AGENT_MANAGER_VIP_HTTP_HOST));
        manager.setPort(agentConf.get(AGENT_MANAGER_VIP_HTTP_PORT));
        proxy.setInlongGroupId(dataConfigs.getInlongGroupId());
        proxy.setInlongStreamId(dataConfigs.getInlongStreamId());
        proxy.setManager(manager);
```

### DuplicatedCode
Duplicated code
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/sinks/KafkaSink.java`
#### Snippet
```java
            ProxyMessage proxyMessage = new ProxyMessage(message);
            // add proxy message to cache.
            cache.compute(proxyMessage.getBatchKey(),
                    (s, packProxyMessage) -> {
                        if (packProxyMessage == null) {
                            packProxyMessage =
                                    new PackProxyMessage(jobInstanceId, jobConf, inlongGroupId, inlongStreamId);
                            packProxyMessage.generateExtraMap(proxyMessage.getDataKey());
                            packProxyMessage.addTopicAndDataTime(topic, System.currentTimeMillis());
                        }
                        // add message to package proxy
                        packProxyMessage.addProxyMessage(proxyMessage);
                        return packProxyMessage;
                    });
            // increment the count of successful sinks
            sinkMetric.sinkSuccessCount.incrementAndGet();
```

### DuplicatedCode
Duplicated code
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/sources/AbstractSource.java`
#### Snippet
```java
        inlongGroupId = conf.get(PROXY_INLONG_GROUP_ID, DEFAULT_PROXY_INLONG_GROUP_ID);
        inlongStreamId = conf.get(PROXY_INLONG_STREAM_ID, DEFAULT_PROXY_INLONG_STREAM_ID);
        // register metric
        this.dimensions = new HashMap<>();
        dimensions.put(KEY_PLUGIN_ID, this.getClass().getSimpleName());
        dimensions.put(KEY_INLONG_GROUP_ID, inlongGroupId);
        dimensions.put(KEY_INLONG_STREAM_ID, inlongStreamId);
```

### DuplicatedCode
Duplicated code
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/sources/reader/BinlogReader.java`
#### Snippet
```java
        Pair<String, String> message = binlogMessagesQueue.poll();
        Map<String, String> header = new HashMap<>(DEFAULT_MAP_CAPACITY);
        header.put(PROXY_KEY_DATA, message.getKey());
        return new DefaultMessage(message.getValue().getBytes(StandardCharsets.UTF_8), header);
```

### DuplicatedCode
Duplicated code
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/sources/reader/BinlogReader.java`
#### Snippet
```java
                .notifying((records, committer) -> {
                    try {
                        for (ChangeEvent<String, String> record : records) {
                            DebeziumFormat debeziumFormat = GSON.fromJson(record.value(), DebeziumFormat.class);
                            binlogMessagesQueue.put(Pair.of(debeziumFormat.getSource().getTable(), record.value()));
                            committer.markProcessed(record);
                        }
                        committer.markBatchFinished();
                        long dataSize = records.stream().mapToLong(r -> r.value().length()).sum();
                        AuditUtils.add(AuditUtils.AUDIT_ID_AGENT_READ_SUCCESS, inlongGroupId, inlongStreamId,
                                System.currentTimeMillis(), records.size(), dataSize);
                        readerMetric.pluginReadSuccessCount.addAndGet(records.size());
                        readerMetric.pluginReadCount.addAndGet(records.size());
                    } catch (Exception e) {
                        readerMetric.pluginReadFailCount.addAndGet(records.size());
                        readerMetric.pluginReadCount.addAndGet(records.size());
                        LOGGER.error("parse binlog message error", e);
                    }
                })
```

### DuplicatedCode
Duplicated code
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/sources/reader/SQLServerReader.java`
#### Snippet
```java
                        for (ChangeEvent<String, String> record : records) {
                            DebeziumFormat debeziumFormat = GSON
                                    .fromJson(record.value(), DebeziumFormat.class);
                            sqlServerMessageQueue.put(Pair.of(debeziumFormat.getSource().getTable(), record.value()));
                            committer.markProcessed(record);
                        }
                        committer.markBatchFinished();
                        long dataSize = records.stream().mapToLong(c -> c.value().length()).sum();
                        AuditUtils.add(AuditUtils.AUDIT_ID_AGENT_READ_SUCCESS, inlongGroupId, inlongStreamId,
                                System.currentTimeMillis(), records.size(), dataSize);
                        readerMetric.pluginReadSuccessCount.addAndGet(records.size());
                        readerMetric.pluginReadCount.addAndGet(records.size());
```

### DuplicatedCode
Duplicated code
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/sources/reader/BinlogReader.java`
#### Snippet
```java
        props.setProperty("tombstones.on.delete", "false");
        props.setProperty("converters", "datetime");
        props.setProperty("datetime.type", "org.apache.inlong.agent.plugin.utils.BinlogTimeConverter");
        props.setProperty("datetime.format.date", "yyyy-MM-dd");
        props.setProperty("datetime.format.time", "HH:mm:ss");
        props.setProperty("datetime.format.datetime", "yyyy-MM-dd HH:mm:ss");
        props.setProperty("datetime.format.timestamp", "yyyy-MM-dd HH:mm:ss");
```

### DuplicatedCode
Duplicated code
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/sources/reader/MongoDBReader.java`
#### Snippet
```java
        setEngineConfigIfNecessary(jobConf, builder, JOB_MONGO_HOSTS, HOSTS);
        setEngineConfigIfNecessary(jobConf, builder, JOB_MONGO_USER, USER);
        setEngineConfigIfNecessary(jobConf, builder, JOB_MONGO_PASSWORD, PASSWORD);
        setEngineConfigIfNecessary(jobConf, builder, JOB_MONGO_DATABASE_INCLUDE_LIST, DATABASE_INCLUDE_LIST);
        setEngineConfigIfNecessary(jobConf, builder, JOB_MONGO_DATABASE_EXCLUDE_LIST, DATABASE_EXCLUDE_LIST);
        setEngineConfigIfNecessary(jobConf, builder, JOB_MONGO_COLLECTION_INCLUDE_LIST, COLLECTION_INCLUDE_LIST);
        setEngineConfigIfNecessary(jobConf, builder, JOB_MONGO_COLLECTION_EXCLUDE_LIST, COLLECTION_EXCLUDE_LIST);
        setEngineConfigIfNecessary(jobConf, builder, JOB_MONGO_FIELD_EXCLUDE_LIST, FIELD_EXCLUDE_LIST);
        setEngineConfigIfNecessary(jobConf, builder, JOB_MONGO_SNAPSHOT_MODE, SNAPSHOT_MODE);
        setEngineConfigIfNecessary(jobConf, builder, JOB_MONGO_CAPTURE_MODE, CAPTURE_MODE);
        setEngineConfigIfNecessary(jobConf, builder, JOB_MONGO_CONNECT_TIMEOUT_MS, CONNECT_TIMEOUT_MS);
```

### DuplicatedCode
Duplicated code
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/sources/reader/OracleReader.java`
#### Snippet
```java
        Pair<String, String> message = oracleMessageQueue.poll();
        if (Objects.isNull(message)) {
            return null;
        }
        Map<String, String> header = new HashMap<>(DEFAULT_MAP_CAPACITY);
        header.put(PROXY_KEY_DATA, message.getKey());
        return new DefaultMessage(GsonUtil.toJson(message.getValue()).getBytes(StandardCharsets.UTF_8), header);
```

### DuplicatedCode
Duplicated code
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/sources/reader/OracleReader.java`
#### Snippet
```java
        userName = jobConf.get(JOB_DATABASE_USER);
        password = jobConf.get(JOB_DATABASE_PASSWORD);
        hostName = jobConf.get(JOB_DATABASE_HOSTNAME);
        port = jobConf.get(JOB_DATABASE_PORT);
        dbName = jobConf.get(JOB_DATABASE_DBNAME);
        serverName = jobConf.get(JOB_DATABASE_SERVER_NAME);
        instanceId = jobConf.getInstanceId();
        offsetFlushIntervalMs = jobConf.get(JOB_DATABASE_STORE_OFFSET_INTERVAL_MS, "100000");
        offsetStoreFileName = jobConf.get(JOB_DATABASE_STORE_HISTORY_FILENAME,
                tryToInitAndGetHistoryPath()) + "/offset.dat" + instanceId;
```

### DuplicatedCode
Duplicated code
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/sources/reader/OracleReader.java`
#### Snippet
```java
        oracleMessageQueue = new LinkedBlockingQueue<>(jobConf.getInt(JOB_DATABASE_QUEUE_SIZE, 1000));
        finished = false;

        databaseStoreHistoryName = jobConf.get(JOB_DATABASE_STORE_HISTORY_FILENAME,
                tryToInitAndGetHistoryPath()) + "/history.dat" + jobConf.getInstanceId();
        offset = jobConf.get(JOB_DATABASE_OFFSETS, "");
        specificOffsetFile = jobConf.get(JOB_DATABASE_OFFSET_SPECIFIC_OFFSET_FILE, "");
        specificOffsetPos = jobConf.get(JOB_DATABASE_OFFSET_SPECIFIC_OFFSET_POS, "-1");
```

### DuplicatedCode
Duplicated code
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/sources/reader/OracleReader.java`
#### Snippet
```java
        DebeziumEngine<ChangeEvent<String, String>> engine = DebeziumEngine.create(
                io.debezium.engine.format.Json.class)
                .using(props)
                .notifying((records, committer) -> {
                    try {
                        for (ChangeEvent<String, String> record : records) {
                            DebeziumFormat debeziumFormat = GSON
                                    .fromJson(record.value(), DebeziumFormat.class);
                            oracleMessageQueue.put(Pair.of(debeziumFormat.getSource().getTable(), record.value()));
                            committer.markProcessed(record);
                        }
                        committer.markBatchFinished();
                        long dataSize = records.stream().mapToLong(c -> c.value().length()).sum();
                        AuditUtils.add(AuditUtils.AUDIT_ID_AGENT_READ_SUCCESS, inlongGroupId, inlongStreamId,
                                System.currentTimeMillis(), records.size(), dataSize);
                        readerMetric.pluginReadSuccessCount.addAndGet(records.size());
                        readerMetric.pluginReadCount.addAndGet(records.size());
                    } catch (Exception e) {
                        readerMetric.pluginReadFailCount.addAndGet(records.size());
                        readerMetric.pluginReadCount.addAndGet(records.size());
                        LOGGER.error("parse binlog message error", e);
                    }
                })
                .using((success, message, error) -> {
```

### DuplicatedCode
Duplicated code
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/sources/reader/OracleReader.java`
#### Snippet
```java
props.setProperty("database.hostname", hostName);
        props.setProperty("database.port", port);
        props.setProperty("database.user", userName);
        props.setProperty("database.password", password);
        props.setProperty("database.dbname", dbName);
        props.setProperty("database.server.name", serverName);
        props.setProperty("offset.flush.interval.ms", offsetFlushIntervalMs);
        props.setProperty("database.snapshot.mode", snapshotMode);
        props.setProperty("key.converter.schemas.enable", "false");
        props.setProperty("value.converter.schemas.enable", "false");
        props.setProperty("snapshot.mode", snapshotMode);
        props.setProperty("offset.storage.file.filename", offsetStoreFileName);
        props.setProperty("database.history.file.filename", databaseStoreHistoryName);
        if (SnapshotModeConstants.SPECIFIC_OFFSETS.equals(snapshotMode)) {
            Preconditions.checkNotNull(JOB_DATABASE_OFFSET_SPECIFIC_OFFSET_FILE,
                    JOB_DATABASE_OFFSET_SPECIFIC_OFFSET_FILE + " cannot be null");
            Preconditions.checkNotNull(JOB_DATABASE_OFFSET_SPECIFIC_OFFSET_POS,
                    JOB_DATABASE_OFFSET_SPECIFIC_OFFSET_POS + " cannot be null");
            props.setProperty("offset.storage", InLongFileOffsetBackingStore.class.getCanonicalName());
            props.setProperty(InLongFileOffsetBackingStore.OFFSET_STATE_VALUE,
                    serializeOffset(instanceId, specificOffsetFile, specificOffsetPos));
            props.setProperty("database.history", InLongDatabaseHistory.class.getCanonicalName());
        } else {
            props.setProperty("offset.storage", FileOffsetBackingStore.class.getCanonicalName());
            props.setProperty("database.history", FileDatabaseHistory.class.getCanonicalName());
        }
        props.setProperty("tombstones.on.delete", "false");
        props.setProperty("converters", "datetime");
        props.setProperty("datetime.type", "org.apache.inlong.agent.plugin.utils.BinlogTimeConverter");
        props.setProperty("datetime.format.date", "yyyy-MM-dd");
        props.setProperty("datetime.format.time", "HH:mm:ss");
        props.setProperty("datetime.format.datetime", "yyyy-MM-dd HH:mm:ss");
        props.setProperty("datetime.format.timestamp", "yyyy-MM-dd HH:mm:ss");
```

### DuplicatedCode
Duplicated code
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/sources/reader/RedisReader.java`
#### Snippet
```java
                        out.beginObject();
                        out.name("key").value(new String(kv.getKey()));
                        out.name("value").beginArray();
                        for (byte[] bytes : kv.getValue()) {
                            out.value(new String(bytes));
                        }
                        out.endArray();
                        out.endObject();
```

### DuplicatedCode
Duplicated code
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/utils/InLongFileOffsetBackingStore.java`
#### Snippet
```java
        start();

        Map<String, ?> conf = config.originals();
        if (!conf.containsKey(OFFSET_STATE_VALUE)) {
            // a normal startup from clean state, not need to initialize the offset
            return;
        }

        String stateJson = (String) conf.get(OFFSET_STATE_VALUE);
        DebeziumOffsetSerializer serializer = new DebeziumOffsetSerializer();
        DebeziumOffset debeziumOffset;
```

### DuplicatedCode
Duplicated code
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/utils/InLongFileOffsetBackingStore.java`
#### Snippet
```java
        final String engineName = (String) conf.get(EmbeddedEngine.ENGINE_NAME.name());
        Converter keyConverter = new JsonConverter();
        Converter valueConverter = new JsonConverter();
        keyConverter.configure(config.originals(), true);
        Map<String, Object> valueConfigs = new HashMap<>(conf);
        valueConfigs.put("schemas.enable", false);
        valueConverter.configure(valueConfigs, true);
        OffsetStorageWriter offsetWriter =
                new OffsetStorageWriter(
                        this,
                        // must use engineName as namespace to align with Debezium Engine
                        // implementation
                        engineName,
                        keyConverter,
                        valueConverter);

        offsetWriter.offset(debeziumOffset.sourcePartition, debeziumOffset.sourceOffset);
```

### DuplicatedCode
Duplicated code
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/utils/MetaDataUtils.java`
#### Snippet
```java
        if (Objects.isNull(jobProfile) || !jobProfile.hasKey(JOB_FILE_PROPERTIES)) {
            return null;
        }
        String property = jobProfile.get(JOB_FILE_PROPERTIES);
        Type type = new TypeToken<HashMap<Integer, String>>() {
        }.getType();
        Map<String, String> properties = GSON.fromJson(property, type);
```

### DuplicatedCode
Duplicated code
in `inlong-audit/audit-common/src/main/java/org/apache/inlong/audit/file/ConfigManager.java`
#### Snippet
```java
                String url = "http://" + host + ConfigConstants.MANAGER_PATH + ConfigConstants.MANAGER_GET_CONFIG_PATH;
                LOG.info("start to request {} to get config info", url);
                httpPost = new HttpPost(url);
                httpPost.addHeader(HttpHeaders.CONNECTION, "close");

                // request body
                AuditConfigRequest request = new AuditConfigRequest();
                request.setClusterTag(clusterTag);
                StringEntity stringEntity = new StringEntity(gson.toJson(request));
                stringEntity.setContentType("application/json");
                httpPost.setEntity(stringEntity);

                // request with post
                LOG.info("start to request {} to get config info with params {}", url, request);
                CloseableHttpResponse response = httpClient.execute(httpPost);
                String returnStr = EntityUtils.toString(response.getEntity());
                // get groupId <-> topic and m value.

                RemoteConfigJson configJson = gson.fromJson(returnStr, RemoteConfigJson.class);
```

### DuplicatedCode
Duplicated code
in `inlong-audit/audit-common/src/main/java/org/apache/inlong/audit/file/holder/PropertiesConfigHolder.java`
#### Snippet
```java
            File targetFile = new File(getNextBackupFileName());
            File tmpNewFile = new File(getFileName() + ".tmp");

            if (sourceFile.exists()) {
                FileUtils.copyFile(sourceFile, targetFile);
            }

            List<String> lines = getStringListFromHolder(tmpHolder);
            FileUtils.writeLines(tmpNewFile, lines);

            FileUtils.copyFile(tmpNewFile, sourceFile);
            tmpNewFile.delete();
            isSuccess = true;
            getFileChanged().set(true);
```

### DuplicatedCode
Duplicated code
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/channel/FailoverChannelProcessor.java`
#### Snippet
```java
List<Interceptor> interceptors = Lists.newLinkedList();

        String interceptorListStr = context.getString("interceptors", "");
        if (interceptorListStr.isEmpty()) {
            return;
        }
        String[] interceptorNames = interceptorListStr.split("\\s+");

        Context interceptorContexts = new Context(context.getSubProperties("interceptors."));

        // run through and instantiate all the interceptors specified in the Context
        InterceptorBuilderFactory factory = new InterceptorBuilderFactory();
        for (String interceptorName : interceptorNames) {
            Context interceptorContext =
                    new Context(interceptorContexts.getSubProperties(interceptorName + "."));
            String type = interceptorContext.getString("type");
            if (type == null) {
                LOG.error("Type not specified for interceptor " + interceptorName);
                throw new FlumeException("Interceptor.Type not specified for " + interceptorName);
            }
            try {
                Interceptor.Builder builder = factory.newInstance(type);
                builder.configure(interceptorContext);
                interceptors.add(builder.build());
            } catch (ClassNotFoundException e) {
                LOG.error("Builder class not found. Exception follows.", e);
                throw new FlumeException("Interceptor.Builder not found.", e);
            } catch (InstantiationException e) {
                LOG.error("Could not instantiate Builder. Exception follows.", e);
                throw new FlumeException("Interceptor.Builder not constructable.", e);
            } catch (IllegalAccessException e) {
                LOG.error("Unable to access Builder. Exception follows.", e);
                throw new FlumeException("Unable to access Interceptor.Builder.", e);
            }
        }

        interceptorChain.setInterceptors(interceptors);
```

### DuplicatedCode
Duplicated code
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/channel/FailoverChannelProcessor.java`
#### Snippet
```java
        Preconditions.checkNotNull(events, "Event list must not be null");
        events = interceptorChain.intercept(events);
        Map<Channel, List<Event>> reqChannelQueue = new LinkedHashMap<Channel, List<Event>>();
        Map<Channel, List<Event>> optChannelQueue = new LinkedHashMap<Channel, List<Event>>();

        long tMsgCounterL = 1L;
```

### DuplicatedCode
Duplicated code
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/channel/FailoverChannelProcessor.java`
#### Snippet
```java
            List<Channel> reqChannels = selector.getRequiredChannels(event);

            for (Channel ch : reqChannels) {
                List<Event> eventQueue = reqChannelQueue
                        .computeIfAbsent(ch, k -> new ArrayList<Event>());// reqChannelQueue
                eventQueue.add(event);
            }

            List<Channel> optChannels = selector.getOptionalChannels(event);

            for (Channel ch : optChannels) {
                List<Event> eventQueue = optChannelQueue
                        .computeIfAbsent(ch, k -> new ArrayList<Event>());// optChannelQueue

                eventQueue.add(event);
            }
```

### DuplicatedCode
Duplicated code
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/channel/FailoverChannelProcessor.java`
#### Snippet
```java
boolean success = true;
        // Process memory channels
        for (Map.Entry<Channel, List<Event>> entry : reqChannelQueue.entrySet()) {
            Channel reqChannel = entry.getKey();
            Transaction tx = reqChannel.getTransaction();
            Preconditions.checkNotNull(tx, "Transaction object must not be null");
            try {
                tx.begin();
                List<Event> batch = entry.getValue();
                for (Event event : batch) {
                    reqChannel.put(event);
                }
                tx.commit();
            } catch (Throwable t) {
                success = false;
                tx.rollback();
                if (!(t instanceof ChannelException)) {
                    LOG.error("Unable to put batch on required " + "channel: " + reqChannel, t);
                    if (t instanceof Error) {
                        throw (Error) t;
                    }
                }
                break;
            } finally {
                tx.close();
            }
        }

        if (!success) {
            // Process file channels
            for (Map.Entry<Channel, List<Event>> entry : optChannelQueue.entrySet()) {
                Channel optChannel = entry.getKey();
                Transaction tx = optChannel.getTransaction();
                Preconditions.checkNotNull(tx, "Transaction object must not be null");
                try {
                    tx.begin();

                    List<Event> batch = entry.getValue();

                    for (Event event : batch) {
                        optChannel.put(event);
                    }

                    tx.commit();

                } catch (Throwable t) {
                    tx.rollback();
                    if (t instanceof Error) {
                        LOG.error("Error while writing to optChannel channel: " + optChannel, t);
                        throw (Error) t;
                    } else {
                        throw new ChannelException(
                                "Unable to put batch on optChannel " + "channel: " + optChannel, t);
                    }
                } finally {
                    tx.close();
                }
            }
        }
```

### DuplicatedCode
Duplicated code
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/channel/FailoverChannelProcessor.java`
#### Snippet
```java
List<Channel> optionalChannels = selector.getOptionalChannels(event);
            for (Channel optChannel : optionalChannels) {
                Transaction tx = null;
                try {
                    tx = optChannel.getTransaction();
                    tx.begin();

                    optChannel.put(event);

                    tx.commit();
                } catch (Throwable t) {
                    if (logPrinter.shouldPrint()) {
                        LOG.error(
                                "FailoverChannelProcessor Unable to put event on optionalChannel:",
                                t);
                    }
                    if (tx != null) {
                        try {
                            tx.rollback();
                        } catch (Throwable e) {
                            if (logPrinter.shouldPrint()) {
                                LOG.error("FailoverChannelProcessor Transaction rollback exception",
                                        e);
                            }
                        }
                    }
                    if (t instanceof Error) {
                        if (logPrinter.shouldPrint()) {
                            LOG.error("FailoverChannelProcessor Error while writing event to "
                                    + "optionalChannels: " + optChannel, t);
                        }
                        throw (Error) t;
                    } else {
                        throw new ChannelException(
                                "FailoverChannelProcessor Unable to put event on "
                                        + "optionalChannels: " + optChannel,
                                t);
                    }
                } finally {
                    if (tx != null) {
                        tx.close();
                    }
                }
            }
```

### DuplicatedCode
Duplicated code
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/node/Application.java`
#### Snippet
```java
            if (keys.contains(CONF_MONITOR_CLASS)) {
                String monitorType = systemProps.getProperty(CONF_MONITOR_CLASS);
                Class<? extends MonitorService> klass;
                try {
                    // Is it a known type?
                    klass = MonitoringType.valueOf(
                            monitorType.toUpperCase(Locale.ENGLISH)).getMonitorClass();
                } catch (Exception e) {
                    // Not a known type, use FQCN
                    klass = (Class<? extends MonitorService>) Class.forName(monitorType);
                }
                this.monitorServer = klass.getDeclaredConstructor().newInstance();
                Context context = new Context();
                for (String key : keys) {
                    if (key.startsWith(CONF_MONITOR_PREFIX)) {
                        context.put(key.substring(CONF_MONITOR_PREFIX.length()),
                                systemProps.getProperty(key));
                    }
                }
                monitorServer.configure(context);
                monitorServer.start();
            }
```

### DuplicatedCode
Duplicated code
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/node/Application.java`
#### Snippet
```java
            SSLUtil.initGlobalSSLParameters();

            Options options = new Options();

            Option option = new Option("n", "name", true, "the name of this agent");
            option.setRequired(true);
            options.addOption(option);

            option = new Option("f", "conf-file", true,
                    "specify a config file (required if -z missing)");
            option.setRequired(false);
            options.addOption(option);

            option = new Option(null, "no-reload-conf", false,
                    "do not reload config file if changed");
            options.addOption(option);
```

### DuplicatedCode
Duplicated code
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/node/Application.java`
#### Snippet
```java
            List<LifecycleAware> components = Lists.newArrayList();

            if (reload) {
                EventBus eventBus = new EventBus(agentName + "-event-bus");
                PollingPropertiesFileConfigurationProvider configurationProvider;
                configurationProvider = new PollingPropertiesFileConfigurationProvider(
                        agentName, configurationFile, eventBus, 30);
                components.add(configurationProvider);
                application = new Application(components);
                eventBus.register(application);
            } else {
                PropertiesFileConfigurationProvider configurationProvider;
                configurationProvider = new PropertiesFileConfigurationProvider(
                        agentName, configurationFile);
                application = new Application();
                application.handleConfigurationEvent(configurationProvider.getConfiguration());
            }
```

### DuplicatedCode
Duplicated code
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/KafkaSink.java`
#### Snippet
```java
        this.canSend = false;

        if (sinkThreadPool != null) {
            for (Thread thread : sinkThreadPool) {
                if (thread != null) {
                    thread.interrupt();
                }
            }
            sinkThreadPool = null;
        }
        super.stop();
        if (!scheduledExecutorService.isShutdown()) {
            scheduledExecutorService.shutdown();
        }
        sinkCounter.stop();
```

### DuplicatedCode
Duplicated code
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/KafkaSink.java`
#### Snippet
```java
        try {
            if (es == null || es.getEvent() == null) {
                return;
            }
            if (!resendQueue.offer(es)) {
                FailoverChannelProcessorHolder.getChannelProcessor().processEvent(es.getEvent());
            }
        } catch (Throwable throwable) {
            logger.error("resendEvent e = {}", throwable);
        }
```

### DuplicatedCode
Duplicated code
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/KafkaSink.java`
#### Snippet
```java
                } catch (Throwable t) {
                    if (t instanceof PulsarClientException) {
                        String message = t.getMessage();
                        if (message != null && (message.contains("No available queue for topic")
                                || message.contains("The brokers of topic are all forbidden"))) {
                            logger.info("IllegalTopicMap.put " + topic);
                            continue;
                        } else {
                            try {
                                Thread.sleep(100);
                            } catch (InterruptedException e) {
                                // ignore..
                            }
                        }
                    }
                    resendEvent(eventStat, decrementFlag);
                }
```

### DuplicatedCode
Duplicated code
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/PulsarSink.java`
#### Snippet
```java
        this.canTake = false;
        int waitCount = 0;
        while (eventQueue.size() != 0 && waitCount++ < 10) {
            try {
                Thread.currentThread().sleep(1000);
            } catch (InterruptedException e) {
                logger.info("Stop thread has been interrupt!");
                break;
            }
        }
        this.canSend = false;

        if (sinkThreadPool != null) {
            for (Thread thread : sinkThreadPool) {
                if (thread != null) {
                    thread.interrupt();
                }
            }
            sinkThreadPool = null;
        }
        super.stop();
        if (!scheduledExecutorService.isShutdown()) {
            scheduledExecutorService.shutdown();
        }
        sinkCounter.stop();
```

### DuplicatedCode
Duplicated code
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/source/SimpleTcpSource.java`
#### Snippet
```java
        acceptorThreadFactory = new DefaultThreadFactory("tcpSource-nettyBoss-threadGroup");

        this.acceptorGroup = EventLoopUtil.newEventLoopGroup(
                acceptorThreads, false, acceptorThreadFactory);

        this.workerGroup = EventLoopUtil
                .newEventLoopGroup(maxThreads, enableBusyWait,
                        new DefaultThreadFactory("tcpSource-nettyWorker-threadGroup"));

        bootstrap = new ServerBootstrap();
        bootstrap.childOption(ChannelOption.ALLOCATOR, ByteBufAllocator.DEFAULT);
        bootstrap.childOption(ChannelOption.TCP_NODELAY, tcpNoDelay);
        bootstrap.childOption(ChannelOption.SO_KEEPALIVE, keepAlive);
        bootstrap.childOption(ChannelOption.SO_RCVBUF, receiveBufferSize);
        bootstrap.childOption(ChannelOption.SO_SNDBUF, sendBufferSize);
        bootstrap.childOption(ChannelOption.WRITE_BUFFER_HIGH_WATER_MARK, highWaterMark);

        bootstrap.channel(EventLoopUtil.getServerSocketChannelClass(workerGroup));
        EventLoopUtil.enableTriggeredMode(bootstrap);
        bootstrap.group(acceptorGroup, workerGroup);

        logger.info("load msgFactory=" + msgFactoryName + " and serviceDecoderName="
                + serviceDecoderName);
```

### DuplicatedCode
Duplicated code
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source2/SimpleTcpSource.java`
#### Snippet
```java
        bootstrap = new ServerBootstrap();
        bootstrap.childOption(ChannelOption.ALLOCATOR, ByteBufAllocator.DEFAULT);
        bootstrap.childOption(ChannelOption.TCP_NODELAY, tcpNoDelay);
        bootstrap.childOption(ChannelOption.SO_KEEPALIVE, tcpKeepAlive);
        bootstrap.childOption(ChannelOption.SO_RCVBUF, maxRcvBufferSize);
        bootstrap.childOption(ChannelOption.SO_SNDBUF, maxSendBufferSize);
        bootstrap.childOption(ChannelOption.WRITE_BUFFER_HIGH_WATER_MARK, highWaterMark);
        bootstrap.channel(EventLoopUtil.getServerSocketChannelClass(workerGroup));
        EventLoopUtil.enableTriggeredMode(bootstrap);
        bootstrap.group(acceptorGroup, workerGroup);
```

### DuplicatedCode
Duplicated code
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/source/SimpleTcpSource.java`
#### Snippet
```java
        tcpNoDelay = context.getBoolean(ConfigConstants.TCP_NO_DELAY, true);

        keepAlive = context.getBoolean(ConfigConstants.KEEP_ALIVE, true);
        highWaterMark = context.getInteger(ConfigConstants.HIGH_WATER_MARK,
                HIGH_WATER_MARK_DEFAULT_VALUE);
        receiveBufferSize = context.getInteger(ConfigConstants.RECEIVE_BUFFER_SIZE,
                RECEIVE_BUFFER_DEFAULT_SIZE);
        if (receiveBufferSize > RECEIVE_BUFFER_MAX_SIZE) {
            receiveBufferSize = RECEIVE_BUFFER_MAX_SIZE;
        }
        Preconditions.checkArgument(receiveBufferSize > BUFFER_SIZE_MUST_THAN,
                "receiveBufferSize must be > 0");

        sendBufferSize = context.getInteger(ConfigConstants.SEND_BUFFER_SIZE, SEND_BUFFER_DEFAULT_SIZE);
        if (sendBufferSize > SEND_BUFFER_MAX_SIZE) {
            sendBufferSize = SEND_BUFFER_MAX_SIZE;
        }
        Preconditions.checkArgument(sendBufferSize > BUFFER_SIZE_MUST_THAN,
                "sendBufferSize must be > 0");
```

### DuplicatedCode
Duplicated code
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/BaseSource.java`
#### Snippet
```java
        receiveBufferSize = context.getInteger(ConfigConstants.RECEIVE_BUFFER_SIZE, RECEIVE_BUFFER_DEFAULT_SIZE);
        if (receiveBufferSize > RECEIVE_BUFFER_MAX_SIZE) {
            receiveBufferSize = RECEIVE_BUFFER_MAX_SIZE;
        }
        Preconditions.checkArgument(receiveBufferSize > BUFFER_SIZE_MUST_THAN,
                "receiveBufferSize must be > 0");

        sendBufferSize = context.getInteger(ConfigConstants.SEND_BUFFER_SIZE, SEND_BUFFER_DEFAULT_SIZE);
        if (sendBufferSize > SEND_BUFFER_MAX_SIZE) {
            sendBufferSize = SEND_BUFFER_MAX_SIZE;
        }
        Preconditions.checkArgument(sendBufferSize > BUFFER_SIZE_MUST_THAN,
                "sendBufferSize must be > 0");
```

### DuplicatedCode
Duplicated code
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/utils/EventLoopUtil.java`
#### Snippet
```java
        if (!Epoll.isAvailable()) {
            return new NioEventLoopGroup(nThreads, threadFactory);
        } else if (!enableBusyWait) {
            return new EpollEventLoopGroup(nThreads, threadFactory);
        } else {
            EpollEventLoopGroup eventLoopGroup = new EpollEventLoopGroup(nThreads, threadFactory, () -> {
                return (selectSupplier, hasTasks) -> {
                    return -3;
                };
            });
            return eventLoopGroup;
        }
```

### DuplicatedCode
Duplicated code
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/utils/EventLoopUtil.java`
#### Snippet
```java
        Objects.requireNonNull(future, "future cannot be null");

        CompletableFuture<Void> adapter = new CompletableFuture<>();
        if (future.isDone()) {
            if (future.isSuccess()) {
                adapter.complete(null);
            } else {
                adapter.completeExceptionally(future.cause());
            }
        } else {
            future.addListener(f -> {
                if (f.isSuccess()) {
                    adapter.complete(null);
                } else {
                    adapter.completeExceptionally(f.cause());
                }
            });
        }
        return adapter;
```

### DuplicatedCode
Duplicated code
in `inlong-audit/audit-sdk/src/main/java/org/apache/inlong/audit/util/IpPort.java`
#### Snippet
```java
        if (splits.length == 2) {
            String strIp = splits[0];
            String strPort = splits[1];
            int port = NumberUtils.toInt(strPort, 0);
            if (port > 0) {
                return new IpPort(strIp, port);
            }
        }
        return null;
```

### DuplicatedCode
Duplicated code
in `inlong-audit/audit-store/src/main/java/org/apache/inlong/audit/service/ElasticsearchService.java`
#### Snippet
```java
        esPo.setLogTs(new Date(msgBody.getLogTs()));
        esPo.setAuditId(msgBody.getAuditId());
        esPo.setCount(msgBody.getCount());
        esPo.setDelay(msgBody.getDelay());
        esPo.setInlongGroupId(msgBody.getInlongGroupId());
        esPo.setInlongStreamId(msgBody.getInlongStreamId());
        esPo.setSize(msgBody.getSize());
```

### DuplicatedCode
Duplicated code
in `inlong-common/src/main/java/org/apache/inlong/common/msg/InLongMsg.java`
#### Snippet
```java
                    int msgItemLen = bodyBuffer.getInt();
                    if (msgItemLen <= 0 || msgItemLen > singleTotalLen) {
                        return;
                    }

                    byte[] record = new byte[1 + 4 + msgItemLen];
                    record[0] = 0;
                    record[1] = (byte) ((msgItemLen >> 24) & 0xFF);
                    record[2] = (byte) ((msgItemLen >> 16) & 0xFF);
                    record[3] = (byte) ((msgItemLen >> 8) & 0xFF);
                    record[4] = (byte) (msgItemLen & 0xFF);
                    bodyBuffer.get(record, 1 + 4, msgItemLen);

                    // single attribute length
                    int singleAttrLen = bodyBuffer.getInt();
                    if (singleAttrLen <= 0 || singleAttrLen > singleTotalLen) {
                        return;
                    }
                    byte[] attrBuf = new byte[singleAttrLen];
                    bodyBuffer.get(attrBuf, 0, singleAttrLen);
```

### DuplicatedCode
Duplicated code
in `inlong-common/src/main/java/org/apache/inlong/common/msg/InLongMsg.java`
#### Snippet
```java
            final DataInputBuffer input = new DataInputBuffer();
            byte[] array = rawdata.array();
            int pos = rawdata.position();
            int rem = rawdata.remaining() - 1;
            int compress = array[pos];

            if (compress == 1) {
                byte[] uncompressdata = new byte[Snappy.uncompressedLength(
                        array, pos + 1, rem)];
                int len = Snappy.uncompress(array, pos + 1, rem,
                        uncompressdata, 0);
                input.reset(uncompressdata, len);
            } else {
                input.reset(array, pos + 1, rem);
            }
```

### DuplicatedCode
Duplicated code
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/admin/ProxyServiceAdminEventHandler.java`
#### Snippet
```java
            String beanName = JMX_DOMAIN + DOMAIN_SEPARATOR
                    + JMX_TYPE + PROPERTY_EQUAL + MBEAN_TYPE + PROPERTY_SEPARATOR
                    + JMX_NAME + PROPERTY_EQUAL + sourceName;

            ObjectName objName = new ObjectName(beanName);
            final MBeanServer mbs = ManagementFactory.getPlatformMBeanServer();
            ObjectInstance mbean = mbs.getObjectInstance(objName);
            LOG.info("getObjectInstance for type:{},name:{},result:{}", MBEAN_TYPE, sourceName, mbean);
            String className = mbean.getClassName();
            Class<?> clazz = ClassUtils.getClass(className);
```

### DuplicatedCode
Duplicated code
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/admin/ProxyServiceAdminEventHandler.java`
#### Snippet
```java
            String beanName = JMX_DOMAIN + DOMAIN_SEPARATOR
                    + JMX_TYPE + PROPERTY_EQUAL + MBEAN_TYPE + PROPERTY_SEPARATOR
                    + "*";
            ObjectName objName = new ObjectName(beanName.toString());
            final MBeanServer mbs = ManagementFactory.getPlatformMBeanServer();
            Set<ObjectInstance> mbeans = mbs.queryMBeans(objName, null);
            LOG.info("queryMBeans for type:{},result:{}", MBEAN_TYPE, mbeans);
```

### DuplicatedCode
Duplicated code
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/config/RemoteConfigManager.java`
#### Snippet
```java
            newConfig.put(builder.toString(), channel.getType());
            for (Entry<String, String> entry : channel.getParams().entrySet()) {
                builder.setLength(0);
                builder.append(prefix).append(entry.getKey());
                newConfig.put(builder.toString(), entry.getValue());
            }
```

### DuplicatedCode
Duplicated code
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/config/pojo/IdTopicConfig.java`
#### Snippet
```java
        if (StringUtils.isBlank(inlongGroupId)) {
            if (StringUtils.isBlank(inlongStreamId)) {
                return "";
            } else {
                return inlongStreamId;
            }
        } else {
            if (StringUtils.isBlank(inlongStreamId)) {
                return inlongGroupId;
            } else {
                return inlongGroupId + "." + inlongStreamId;
            }
        }
```

### DuplicatedCode
Duplicated code
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/dispatch/DispatchManager.java`
#### Snippet
```java
        String eventUid = event.getUid();
        long dispatchTime = event.getMsgTime() - event.getMsgTime() % MINUTE_MS;
        String dispatchKey = eventUid + "." + dispatchTime;
        // find dispatch profile
        DispatchProfile dispatchProfile = this.profileCache.get(dispatchKey);
        if (dispatchProfile == null) {
            dispatchProfile = new DispatchProfile(eventUid, event.getInlongGroupId(), event.getInlongStreamId(),
                    dispatchTime);
            this.profileCache.put(dispatchKey, dispatchProfile);
        }
        // add event
        boolean addResult = dispatchProfile.addEvent(event, maxPackCount, maxPackSize);
```

### DuplicatedCode
Duplicated code
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/dispatch/DispatchManager.java`
#### Snippet
```java
        String dispatchKey = eventUid + "." + dispatchTime;
        //
        DispatchProfile dispatchProfile = this.profileCache.get(dispatchKey);
        if (dispatchProfile == null) {
            dispatchProfile = new DispatchProfile(eventUid, event.getInlongGroupId(), event.getInlongStreamId(),
                    dispatchTime);
            this.profileCache.put(dispatchKey, dispatchProfile);
        }
        //
        boolean addResult = dispatchProfile.addEvent(event, maxPackCount, maxPackSize);
```

### DuplicatedCode
Duplicated code
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/dispatch/DispatchManager.java`
#### Snippet
```java
        long currentTime = System.currentTimeMillis();
        long createThreshold = currentTime - dispatchTimeout;
        List<String> removeKeys = new ArrayList<>();
        long eventCount = 0;
        for (Entry<String, DispatchProfile> entry : this.profileCache.entrySet()) {
            DispatchProfile dispatchProfile = entry.getValue();
            eventCount += dispatchProfile.getCount();
            if (!dispatchProfile.isTimeout(createThreshold)) {
                continue;
            }
            removeKeys.add(entry.getKey());
        }
```

### DuplicatedCode
Duplicated code
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/dispatch/DispatchManager.java`
#### Snippet
```java
        for (Entry<String, DispatchProfile> entry : this.profileCache.entrySet()) {
            DispatchProfile dispatchProfile = entry.getValue();
            eventCount += dispatchProfile.getCount();
            if (!dispatchProfile.isTimeout(createThreshold)) {
                continue;
            }
            removeKeys.add(entry.getKey());
        }
```

### DuplicatedCode
Duplicated code
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/dispatch/DispatchProfile.java`
#### Snippet
```java
        long eventLength = event.getBody().length;
        if (count >= maxPackCount || (count > 0 && size + eventLength > maxPackSize)) {
            return false;
        }
        this.events.add(event);
        this.count++;
        this.size += eventLength;
        return true;
```

### DuplicatedCode
Duplicated code
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/metrics/audit/AuditUtils.java`
#### Snippet
```java
        String strLogTime = headers.get(Constants.HEADER_KEY_MSG_TIME);
        if (strLogTime == null) {
            strLogTime = headers.get(AttributeConstants.DATA_TIME);
        }
        if (strLogTime == null) {
            return System.currentTimeMillis();
        }
        long logTime = NumberUtils.toLong(strLogTime, 0);
        if (logTime == 0) {
            logTime = System.currentTimeMillis();
        }
        return logTime;
```

### DuplicatedCode
Duplicated code
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/metrics/prometheus/PrometheusMetricListener.java`
#### Snippet
```java
for (MetricItemValue itemValue : itemValues) {
            // total
            for (Entry<String, MetricValue> entry : itemValue.getMetrics().entrySet()) {
                String fieldName = entry.getValue().name;
                AtomicLong metricValue = this.metricValueMap.get(fieldName);
                if (metricValue != null) {
                    long fieldValue = entry.getValue().value;
                    metricValue.addAndGet(fieldValue);
                }
            }
            // id dimension
            String dimensionKey = itemValue.getKey();
            MetricItemValue dimensionMetricValue = this.dimensionMetricValueMap.get(dimensionKey);
            if (dimensionMetricValue == null) {
                dimensionMetricValue = new MetricItemValue(dimensionKey, new ConcurrentHashMap<String, String>(),
                        new ConcurrentHashMap<String, MetricValue>());
                this.dimensionMetricValueMap.putIfAbsent(dimensionKey, dimensionMetricValue);
                dimensionMetricValue = this.dimensionMetricValueMap.get(dimensionKey);
                dimensionMetricValue.getDimensions().putAll(itemValue.getDimensions());
                // add prometheus label name
                for (Entry<String, String> entry : itemValue.getDimensions().entrySet()) {
                    if (!this.dimensionKeys.contains(entry.getKey())) {
                        this.dimensionKeys.add(entry.getKey());
                    }
                }
            }
            // count
            for (Entry<String, MetricValue> entry : itemValue.getMetrics().entrySet()) {
                String fieldName = entry.getValue().name;
                MetricValue metricValue = dimensionMetricValue.getMetrics().get(fieldName);
                if (metricValue == null) {
                    metricValue = MetricValue.of(fieldName, entry.getValue().value);
                    dimensionMetricValue.getMetrics().put(metricValue.name, metricValue);
                    continue;
                }
                metricValue.value += entry.getValue().value;
            }
        }
```

### DuplicatedCode
Duplicated code
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/metrics/prometheus/PrometheusMetricListener.java`
#### Snippet
```java
        totalCounter.addMetric(Arrays.asList(M_READ_SUCCESS_COUNT), metricItem.readSuccessCount.get());
        totalCounter.addMetric(Arrays.asList(M_READ_SUCCESS_SIZE), metricItem.readSuccessSize.get());
        totalCounter.addMetric(Arrays.asList(M_READ_FAIL_COUNT), metricItem.readFailCount.get());
        totalCounter.addMetric(Arrays.asList(M_READ_FAIL_SIZE), metricItem.readFailSize.get());
        // send
        totalCounter.addMetric(Arrays.asList(M_SEND_COUNT), metricItem.sendCount.get());
        totalCounter.addMetric(Arrays.asList(M_SEND_SIZE), metricItem.sendSize.get());
        totalCounter.addMetric(Arrays.asList(M_SEND_SUCCESS_COUNT), metricItem.sendSuccessCount.get());
        totalCounter.addMetric(Arrays.asList(M_SEND_SUCCESS_SIZE), metricItem.sendSuccessSize.get());
        totalCounter.addMetric(Arrays.asList(M_SEND_FAIL_COUNT), metricItem.sendFailCount.get());
        totalCounter.addMetric(Arrays.asList(M_SEND_FAIL_SIZE), metricItem.sendFailSize.get());
        // duration
        totalCounter.addMetric(Arrays.asList(M_SINK_DURATION), metricItem.sinkDuration.get());
        totalCounter.addMetric(Arrays.asList(M_NODE_DURATION), metricItem.nodeDuration.get());
        totalCounter.addMetric(Arrays.asList(M_WHOLE_DURATION), metricItem.wholeDuration.get());
        List<MetricFamilySamples> mfs = new ArrayList<>();
        mfs.add(totalCounter);
```

### DuplicatedCode
Duplicated code
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/metrics/prometheus/PrometheusMetricListener.java`
#### Snippet
```java
        for (Entry<String, MetricItemValue> entry : this.dimensionMetricValueMap.entrySet()) {
            MetricItemValue itemValue = entry.getValue();
            // read
            addCounterMetricFamily(M_READ_SUCCESS_COUNT, itemValue, idCounter);
            addCounterMetricFamily(M_READ_SUCCESS_SIZE, itemValue, idCounter);
            addCounterMetricFamily(M_READ_FAIL_COUNT, itemValue, idCounter);
            addCounterMetricFamily(M_READ_FAIL_SIZE, itemValue, idCounter);
            // send
            addCounterMetricFamily(M_SEND_COUNT, itemValue, idCounter);
            addCounterMetricFamily(M_SEND_SIZE, itemValue, idCounter);
            addCounterMetricFamily(M_SEND_SUCCESS_COUNT, itemValue, idCounter);
            addCounterMetricFamily(M_SEND_SUCCESS_SIZE, itemValue, idCounter);
            addCounterMetricFamily(M_SEND_FAIL_COUNT, itemValue, idCounter);
            addCounterMetricFamily(M_SEND_FAIL_SIZE, itemValue, idCounter);
            // duration
            addCounterMetricFamily(M_SINK_DURATION, itemValue, idCounter);
            addCounterMetricFamily(M_NODE_DURATION, itemValue, idCounter);
            addCounterMetricFamily(M_WHOLE_DURATION, itemValue, idCounter);
        }
        mfs.add(idCounter);
        return mfs;
```

### DuplicatedCode
Duplicated code
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/metrics/prometheus/PrometheusMetricListener.java`
#### Snippet
```java
        List<String> labelValues = new ArrayList<>(this.dimensionKeys.size());
        labelValues.add(defaultDemension);
        Map<String, String> dimensions = itemValue.getDimensions();
        for (String key : this.dimensionKeys) {
            String labelValue = dimensions.getOrDefault(key, "-");
            labelValues.add(labelValue);
        }
        long value = 0L;
        Map<String, MetricValue> metricValueMap = itemValue.getMetrics();
        MetricValue metricValue = metricValueMap.get(defaultDemension);
        if (metricValue != null) {
            value = metricValue.value;
        }
        idCounter.addMetric(labelValues, value);
```

### DuplicatedCode
Duplicated code
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/common/DefaultEventHandler.java`
#### Snippet
```java
        MessageObjs.Builder objs = MessageObjs.newBuilder();
        for (ProxyEvent event : events) {
            MessageObj.Builder builder = MessageObj.newBuilder();
            builder.setMsgTime(event.getMsgTime());
            builder.setSourceIp(event.getSourceIp());
            event.getHeaders().forEach((key, value) -> {
                builder.addParams(MapFieldEntry.newBuilder().setKey(key).setValue(value));
            });
            builder.setBody(ByteString.copyFrom(event.getBody()));
            objs.addMsgs(builder.build());
        }
        byte[] srcBytes = objs.build().toByteArray();
        // compress
        byte[] compressBytes = null;
        switch (compressType) {
            case INLONG_SNAPPY:
                compressBytes = Snappy.compress(srcBytes);
                break;
            case INLONG_GZ:
                compressBytes = GzipUtils.compress(srcBytes);
                break;
            case INLONG_NO_COMPRESS:
            default:
                compressBytes = srcBytes;
                break;
        }
        return compressBytes;
```

### DuplicatedCode
Duplicated code
in `inlong-sdk/sdk-common/src/main/java/org/apache/inlong/sdk/commons/protocol/EventUtils.java`
#### Snippet
```java
        byte[] compressedBytes = null;
        switch (compressedType) {
            case INLONG_SNAPPY:
                compressedBytes = Snappy.compress(srcBytes);
                break;
            case INLONG_GZ:
                compressedBytes = GzipUtils.compress(srcBytes);
                break;
            case INLONG_NO_COMPRESS:
            default:
                compressedBytes = srcBytes;
                break;
        }
```

### DuplicatedCode
Duplicated code
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/BatchPackManager.java`
#### Snippet
```java
        if (!needOutputOvertimeData.getAndSet(false)) {
            return;
        }
        LOG.debug("start to outputOvertimeData profileCacheSize:{},dispatchQueueSize:{}",
                profileCache.size(), dispatchQueue.size());
        long currentTime = System.currentTimeMillis();
        long createThreshold = currentTime - dispatchTimeout;
        List<String> removeKeys = new ArrayList<>();
        long eventCount = 0;
```

### DuplicatedCode
Duplicated code
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/MessageQueueZoneSinkContext.java`
#### Snippet
```java
        Map<String, String> dimensions = new HashMap<>();
        dimensions.put(DataProxyMetricItem.KEY_CLUSTER_ID, this.getProxyClusterId());
        dimensions.put(DataProxyMetricItem.KEY_SOURCE_ID, "-");
        dimensions.put(DataProxyMetricItem.KEY_SOURCE_DATA_ID, "-");
        // metric
        fillInlongId(currentRecord, dimensions);
        dimensions.put(DataProxyMetricItem.KEY_SINK_ID, mqName);
        dimensions.put(DataProxyMetricItem.KEY_SINK_DATA_ID, topic);
```

### DuplicatedCode
Duplicated code
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/OrderBatchPackProfileV0.java`
#### Snippet
```java
        int binTotalLen = 1 + 4 + 2 + 2;
        if (null != backattrs) {
            binTotalLen += backattrs.length();
        }
        ByteBuf binBuffer = ByteBufAllocator.DEFAULT.buffer(4 + binTotalLen);
        binBuffer.writeInt(binTotalLen);
        binBuffer.writeByte(msgType.getValue());
```

### DuplicatedCode
Duplicated code
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/OrderBatchPackProfileV0.java`
#### Snippet
```java
        long uniqVal = Long.parseLong(sequenceId);
        byte[] uniq = new byte[4];
        uniq[0] = (byte) ((uniqVal >> 24) & 0xFF);
        uniq[1] = (byte) ((uniqVal >> 16) & 0xFF);
        uniq[2] = (byte) ((uniqVal >> 8) & 0xFF);
        uniq[3] = (byte) (uniqVal & 0xFF);
        binBuffer.writeBytes(uniq);

        if (null != backattrs) {
            binBuffer.writeShort(backattrs.length());
            binBuffer.writeBytes(backattrs.getBytes(StandardCharsets.UTF_8));
        } else {
            binBuffer.writeShort(0x0);
        }
        binBuffer.writeShort(0xee01);
        return binBuffer;
```

### DuplicatedCode
Duplicated code
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/SimpleMessageHandler.java`
#### Snippet
```java
                byte[] uniq = new byte[4];
                uniq[0] = (byte) ((uniqVal >> 24) & 0xFF);
                uniq[1] = (byte) ((uniqVal >> 16) & 0xFF);
                uniq[2] = (byte) ((uniqVal >> 8) & 0xFF);
                uniq[3] = (byte) (uniqVal & 0xFF);
                binBuffer.writeBytes(uniq);

                if (null != backattrs) {
                    binBuffer.writeShort(backattrs.length());
                    binBuffer.writeBytes(backattrs.getBytes(StandardCharsets.UTF_8));
                } else {
                    binBuffer.writeShort(0x0);
                }

                binBuffer.writeShort(0xee01);
```

### DuplicatedCode
Duplicated code
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/kafka/KafkaHandler.java`
#### Snippet
```java
            IdTopicConfig idConfig = sinkContext.getIdTopicHolder().getIdConfig(event.getUid());
            if (idConfig == null) {
                sinkContext.addSendResultMetric(event, clusterName, event.getUid(), false, 0);
                sinkContext.getDispatchQueue().release(event.getSize());
                return false;
            }
            String baseTopic = idConfig.getTopicName();
            if (baseTopic == null) {
                sinkContext.addSendResultMetric(event, clusterName, event.getUid(), false, 0);
                sinkContext.getDispatchQueue().release(event.getSize());
                return false;
            }
```

### DuplicatedCode
Duplicated code
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/kafka/KafkaHandler.java`
#### Snippet
```java
        EventHandler handler = handlerLocal.get();
        if (handler == null) {
            handler = this.sinkContext.createEventHandler();
            handlerLocal.set(handler);
        }
        // headers
        Map<String, String> headers = handler.parseHeader(idConfig, event, sinkContext.getNodeId(),
                sinkContext.getCompressType());
        // compress
        byte[] bodyBytes = handler.parseBody(idConfig, event, sinkContext.getCompressType());
        // metric
        sinkContext.addSendMetric(event, clusterName, topic, bodyBytes.length);
```

### DuplicatedCode
Duplicated code
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/kafka/KafkaHandler.java`
#### Snippet
```java
        ProducerRecord<String, byte[]> producerRecord = new ProducerRecord<>(topic, bodyBytes);
        // add headers
        headers.forEach((key, value) -> {
            producerRecord.headers().add(key, value.getBytes());
        });

        // callback
        Callback callback = new Callback() {

            @Override
            public void onCompletion(RecordMetadata arg0, Exception ex) {
                if (ex != null) {
                    LOG.error("Send fail:{}", ex.getMessage());
                    LOG.error(ex.getMessage(), ex);
                    if (event.isResend()) {
                        sinkContext.processSendFail(event, clusterName, topic, sendTime);
                    } else {
                        event.fail();
                    }
                } else {
                    sinkContext.addSendResultMetric(event, clusterName, topic, true, sendTime);
                    sinkContext.getDispatchQueue().release(event.getSize());
                    event.ack();
                }
            }
        };
        producer.send(producerRecord, callback);
```

### DuplicatedCode
Duplicated code
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/kafka/KafkaHandler.java`
#### Snippet
```java
        Map<String, String> headers = event.getProperties();
        if (MapUtils.isEmpty(headers)) {
            headers = event.getSimpleProfile().getHeaders();
        }
        // body
        byte[] bodyBytes = event.getSimpleProfile().getBody();
        // metric
        sinkContext.addSendMetric(event, clusterName, topic, bodyBytes.length);
        // sendAsync
        long sendTime = System.currentTimeMillis();
```

### DuplicatedCode
Duplicated code
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/pulsar/PulsarHandler.java`
#### Snippet
```java
        for (Entry<String, Producer<byte[]>> entry : this.producerMap.entrySet()) {
            try {
                entry.getValue().close();
            } catch (PulsarClientException e) {
                LOG.error(e.getMessage(), e);
            }
        }
        try {
            this.client.close();
        } catch (PulsarClientException e) {
            LOG.error(e.getMessage(), e);
        }
```

### DuplicatedCode
Duplicated code
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/pulsar/PulsarHandler.java`
#### Snippet
```java
                    LOG.info("create new producer success:{}", producer.getProducerName());
                    Producer<byte[]> oldProducer = this.producerMap.putIfAbsent(producerTopic, producer);
                    if (oldProducer != null) {
                        producer.close();
                        LOG.info("close producer success:{}", producer.getProducerName());
                        producer = oldProducer;
                    }
```

### DuplicatedCode
Duplicated code
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/pulsar/PulsarHandler.java`
#### Snippet
```java
        CompletableFuture<MessageId> future = producer.newMessage().properties(headers)
                .value(bodyBytes).sendAsync();
        // callback
        future.whenCompleteAsync((msgId, ex) -> {
            if (ex != null) {
                LOG.error("Send fail:{}", ex.getMessage());
                LOG.error(ex.getMessage(), ex);
                sinkContext.processSendFail(event, clusterName, producerTopic, sendTime);
            } else {
                sinkContext.addSendResultMetric(event, clusterName, producerTopic, true, sendTime);
                sinkContext.getDispatchQueue().release(event.getSize());
                event.ack();
            }
        });
```

### DuplicatedCode
Duplicated code
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/tube/TubeHandler.java`
#### Snippet
```java
        long sendTime = System.currentTimeMillis();
        MessageSentCallback callback = new MessageSentCallback() {

            @Override
            public void onMessageSent(MessageSentResult result) {
                sinkContext.addSendResultMetric(event, clusterName, topic, true, sendTime);
                sinkContext.getDispatchQueue().release(event.getSize());
                event.ack();
            }

            @Override
            public void onException(Throwable ex) {
                LOG.error("Send fail:{}", ex.getMessage());
                LOG.error(ex.getMessage(), ex);
                sinkContext.processSendFail(event, clusterName, topic, sendTime);
            }
        };
        producer.sendMessage(message, callback);
```

### DuplicatedCode
Duplicated code
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/DefaultServiceDecoder.java`
#### Snippet
```java
            if (StringUtils.isNotEmpty(rtMs)) {
                if (StringUtils.isEmpty(strAttr)) {
                    newTotalLen += rtMs.length();
                    strAttr = rtMs;
                } else {
                    newTotalLen += AttributeConstants.SEPARATOR.length() + rtMs.length();
                    strAttr = strAttr + AttributeConstants.SEPARATOR + rtMs;
                }
            }
```

### DuplicatedCode
Duplicated code
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/ServerMessageHandler.java`
#### Snippet
```java
final int BIN_MSG_TOTALLEN_OFFSET = 0;
        final int BIN_MSG_TOTALLEN_SIZE = 4;
        final int BIN_MSG_BODYLEN_SIZE = 4;
        final int BIN_MSG_EXTEND_OFFSET = 9;
        final int BIN_MSG_BODYLEN_OFFSET = 21;
        final int BIN_MSG_BODY_OFFSET = BIN_MSG_BODYLEN_SIZE + BIN_MSG_BODYLEN_OFFSET;
        final int BIN_MSG_ATTRLEN_SIZE = 2;
        final int BIN_MSG_FORMAT_SIZE = 29;
        final int BIN_MSG_MAGIC_SIZE = 2;
        final int BIN_MSG_MAGIC = 0xEE01;

        ByteBuffer orgBuf = ByteBuffer.wrap(orgBinMsg);
        int totalLen = orgBuf.getInt(BIN_MSG_TOTALLEN_OFFSET);
        int dataLen = orgBuf.getInt(BIN_MSG_BODYLEN_OFFSET);
        int attrLen = orgBuf.getShort(BIN_MSG_BODY_OFFSET + dataLen);

        int newTotalLen = 0;
        String strAttr;
        if (attrLen != 0) {
            newTotalLen = totalLen + extraAttr.length() + "&".length();
            strAttr = "&" + extraAttr;
        } else {
            newTotalLen = totalLen + extraAttr.length();
            strAttr = extraAttr;
        }

        ByteBuffer dataBuf = ByteBuffer.allocate(newTotalLen + BIN_MSG_TOTALLEN_SIZE);
        dataBuf
                .put(orgBuf.array(), 0, dataLen + (BIN_MSG_FORMAT_SIZE - BIN_MSG_MAGIC_SIZE) + attrLen);
        dataBuf
                .putShort(dataLen + (BIN_MSG_FORMAT_SIZE - BIN_MSG_ATTRLEN_SIZE - BIN_MSG_MAGIC_SIZE),
                        (short) (strAttr.length() + attrLen));

        System.arraycopy(strAttr.getBytes(StandardCharsets.UTF_8), 0, dataBuf.array(),
                dataLen + (BIN_MSG_FORMAT_SIZE - BIN_MSG_MAGIC_SIZE) + attrLen,
                strAttr.length());
        int extendField = orgBuf.getShort(BIN_MSG_EXTEND_OFFSET);
        dataBuf.putShort(BIN_MSG_EXTEND_OFFSET, (short) (extendField | 0x4));
        dataBuf.putInt(0, newTotalLen);
        dataBuf.putShort(newTotalLen + BIN_MSG_TOTALLEN_SIZE - BIN_MSG_MAGIC_SIZE,
                (short) BIN_MSG_MAGIC);
        return dataBuf.array();
```

### DuplicatedCode
Duplicated code
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/ServerMessageHandler.java`
#### Snippet
```java
        if (allChannels.size() - 1 >= maxConnections) {
            ctx.channel().disconnect();
            ctx.channel().close();
            logger.warn("{} refuse to connect = {} , connections = {}, maxConnections = {}",
                    source.getName(), ctx.channel(), (allChannels.size() - 1), maxConnections);
            return;
        }
        // check illegal ip
        if (ConfigManager.getInstance().needChkIllegalIP()) {
            String strRemoteIp = AddressUtils.getChannelRemoteIP(ctx.channel());
            if (strRemoteIp != null
                    && ConfigManager.getInstance().isIllegalIP(strRemoteIp)) {
                ctx.channel().disconnect();
                ctx.channel().close();
                logger.error(strRemoteIp + " is Illegal IP, so refuse it !");
                return;
            }
        }
        // add legal channel
        allChannels.add(ctx.channel());
        ctx.fireChannelActive();
        logger.info("{} added new channel, current connections = {}, maxConnections = {}",
                source.getName(), (allChannels.size() - 1), maxConnections);
```

### DuplicatedCode
Duplicated code
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/ServerMessageHandler.java`
#### Snippet
```java
                        String enableTrans =
                                (configManager.getGroupIdEnableMappingProperties() == null)
                                        ? null
                                        : configManager.getGroupIdEnableMappingProperties().get(groupIdNum);
                        if (("TRUE".equalsIgnoreCase(enableTrans)
                                && "TRUE".equalsIgnoreCase(num2name))) {
                            String extraAttr = "groupId=" + groupId + "&" + "streamId=" + streamId;
                            message.setData(newBinMsg(message.getData(), extraAttr));
                        }
```

### DuplicatedCode
Duplicated code
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/ServerMessageHandler.java`
#### Snippet
```java
                String syncSend = commonAttrMap.get(AttributeConstants.MESSAGE_SYNC_SEND);
                if (StringUtils.isNotEmpty(syncSend)) {
                    headers.put(AttributeConstants.MESSAGE_SYNC_SEND, syncSend);
                }
                String proxySend = commonAttrMap.get(AttributeConstants.MESSAGE_PROXY_SEND);
                if (StringUtils.isNotEmpty(proxySend)) {
                    headers.put(AttributeConstants.MESSAGE_PROXY_SEND, proxySend);
                }
                String partitionKey = commonAttrMap.get(AttributeConstants.MESSAGE_PARTITION_KEY);
                if (StringUtils.isNotEmpty(partitionKey)) {
                    headers.put(AttributeConstants.MESSAGE_PARTITION_KEY, partitionKey);
                }
```

### DuplicatedCode
Duplicated code
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/SimpleMessageHandler.java`
#### Snippet
```java
                headers.put(ConfigConstants.FILE_CHECK_DATA,
                        "true");
                for (ProxyMessage message : msgList) {
                    byte[] body = message.getData();
                    Event event = EventBuilder.withBody(body, headers);
                    try {
                        processor.processEvent(event);
                        this.addMetric(true, body.length, event);
                    } catch (Throwable ex) {
                        logger.error("Error writing to controller,data will discard.", ex);
                        this.addMetric(false, body.length, event);
                        throw new ChannelException(
                                "Process Controller Event error can't write event to channel.");
                    }
                }
```

### DuplicatedCode
Duplicated code
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/SimpleTcpSource.java`
#### Snippet
```java
            int cnt = 0;
            Channel channel;
            String strRemoteIP;
            long startTime = System.currentTimeMillis();
            Iterator<Channel> iterator = allChannels.iterator();
            while (iterator.hasNext()) {
                channel = iterator.next();
                strRemoteIP = AddressUtils.getChannelRemoteIP(channel);
                if (strRemoteIP == null) {
                    continue;
                }
                if (ConfigManager.getInstance().isIllegalIP(strRemoteIP)) {
                    channel.disconnect();
                    channel.close();
                    allChannels.remove(channel);
                    cnt++;
                    logger.error(strRemoteIP + " is Illegal IP, so disconnect it !");
                }
            }
```

### DuplicatedCode
Duplicated code
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source2/InLongMessageHandler.java`
#### Snippet
```java
        int binTotalLen = 1 + 4 + 2 + 2;
        if (null != attrs) {
            binTotalLen += attrs.length();
        }
        // allocate buffer and write fields
        ByteBuf binBuffer = ByteBufAllocator.DEFAULT.buffer(4 + binTotalLen);
        binBuffer.writeInt(binTotalLen);
        binBuffer.writeByte(MsgType.MSG_BIN_MULTI_BODY.getValue());
```

### DuplicatedCode
Duplicated code
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source2/InLongMessageHandler.java`
#### Snippet
```java
        int attrsLen = 0;
        int bodyLen = 0;
        if (attrs != null) {
            attrsLen = attrs.length();
        }
        // backTotalLen = mstType + bodyLen + body + attrsLen + attrs
        int backTotalLen = 1 + 4 + bodyLen + 4 + attrsLen;
        ByteBuf buffer = ByteBufAllocator.DEFAULT.buffer(4 + backTotalLen);
        buffer.writeInt(backTotalLen);
        buffer.writeByte(msgType.getValue());
        buffer.writeInt(bodyLen);
        buffer.writeInt(attrsLen);
        if (attrsLen > 0) {
            buffer.writeBytes(attrs.getBytes(StandardCharsets.UTF_8));
        }
        return buffer;
```

### DuplicatedCode
Duplicated code
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source2/InLongMessageHandler.java`
#### Snippet
```java
        if (loadValue == 0 || loadValue == (-1)) {
            loadValue = 0xffff;
        }
        // allocate buffer and write fields
        ByteBuf binBuffer = ByteBufAllocator.DEFAULT.buffer(4 + binTotalLen);
        binBuffer.writeInt(binTotalLen);
        binBuffer.writeByte(MsgType.MSG_BIN_HEARTBEAT.getValue());
        binBuffer.writeInt((int) (System.currentTimeMillis() / 1000));
        binBuffer.writeByte(version);
        binBuffer.writeInt(2);
        binBuffer.writeShort(loadValue);
```

### DuplicatedCode
Duplicated code
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source2/SimpleTcpSource.java`
#### Snippet
```java
        try {
            if (srcHost == null) {
                channelFuture = bootstrap.bind(new InetSocketAddress(srcPort)).sync();
            } else {
                channelFuture = bootstrap.bind(new InetSocketAddress(srcHost, srcPort)).sync();
            }
        } catch (Exception e) {
            logger.error("Source {} bind ({}:{}) error, program will exit! e = {}",
                    this.getName(), srcHost, srcPort, e);
            System.exit(-1);
        }
        ConfigManager.getInstance().addSourceReportInfo(
                srcHost, String.valueOf(srcPort), getProtocolName().toUpperCase());
        logger.info("Source {} started at ({}:{})!", this.getName(), srcHost, srcPort);
```

### DuplicatedCode
Duplicated code
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source2/v0msg/CodecBinMsg.java`
#### Snippet
```java
        if (bodyLen <= 0) {
            if (bodyLen == 0) {
                source.fileMetricEventInc(StatConstants.EVENT_NOBODY);
                this.errCode = DataProxyErrCode.BODY_LENGTH_ZERO;
            } else {
                source.fileMetricEventInc(StatConstants.EVENT_NEGBODY);
                this.errCode = DataProxyErrCode.BODY_LENGTH_LESS_ZERO;
            }
            return false;
        }
```

### DuplicatedCode
Duplicated code
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source2/v0msg/CodecBinMsg.java`
#### Snippet
```java
        if (strBuff.length() > 0) {
            if (StringUtils.isNotBlank(origAttr)) {
                strBuff.append(AttributeConstants.SEPARATOR).append(origAttr);
            }
            totalDataLen += strBuff.length() - origAttr.length();
            origAttr = strBuff.toString();
            strBuff.delete(0, strBuff.length());
        }
        return true;
```

### DuplicatedCode
Duplicated code
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/utils/BufferQueue.java`
#### Snippet
```java
        boolean cidResult = currentTokens.tryAcquire(sizeInByte);
        if (!cidResult) {
            return false;
        }
        if (this.globalTokens == null) {
            return true;
        }
        boolean globalResult = this.globalTokens.tryAcquire(sizeInByte);
        if (globalResult) {
            return true;
        }
        currentTokens.release(sizeInByte);
        return false;
```

### DuplicatedCode
Duplicated code
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/utils/SizeSemaphore.java`
#### Snippet
```java
        int sizeInKb = (int) (sizeInByte / leftSize);
        int sizeChange = (int) (sizeInByte % leftSize);
        if (leftSemaphore.get() - sizeChange < 0) {
            boolean result = sizeSemaphore.tryAcquire(sizeInKb + 1);
            if (result) {
                leftSemaphore.addAndGet(-sizeChange + leftSize);
            }
            return result;
        } else {
            boolean result = sizeSemaphore.tryAcquire(sizeInKb);
            if (result) {
                leftSemaphore.addAndGet(-sizeChange);
            }
            return result;
        }
```

### DuplicatedCode
Duplicated code
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/utils/SizeSemaphore.java`
#### Snippet
```java
        int sizeInKb = (int) (sizeInByte / leftSize);
        int sizeChange = (int) (sizeInByte % leftSize);
        if (leftSemaphore.get() - sizeChange < 0) {
            sizeSemaphore.acquireUninterruptibly(sizeInKb + 1);
            leftSemaphore.addAndGet(-sizeChange + leftSize);
        } else {
            sizeSemaphore.acquireUninterruptibly(sizeInKb);
            leftSemaphore.addAndGet(-sizeChange);
        }
```

### DuplicatedCode
Duplicated code
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/utils/SizeSemaphore.java`
#### Snippet
```java
        int sizeInKb = (int) (sizeInByte / leftSize);
        int sizeChange = (int) (sizeInByte % leftSize);
        if (leftSemaphore.get() + sizeChange > leftSize) {
            sizeSemaphore.release(sizeInKb + 1);
            leftSemaphore.addAndGet(sizeChange - leftSize);
        } else {
            sizeSemaphore.release(sizeInKb);
            leftSemaphore.addAndGet(sizeChange);
        }
```

### DuplicatedCode
Duplicated code
in `inlong-manager/manager-client/src/main/java/org/apache/inlong/manager/client/api/util/InlongGroupTransfer.java`
#### Snippet
```java
        if (MapUtils.isNotEmpty(flinkSortConf.getProperties())) {
            InlongGroupExtInfo flinkProperties = new InlongGroupExtInfo();
            flinkProperties.setKeyName(InlongConstants.SORT_PROPERTIES);
            try {
                flinkProperties.setKeyValue(JsonUtils.toJsonString(flinkSortConf.getProperties()));
            } catch (Exception e) {
                throw new RuntimeException("get json for sort properties error: " + e.getMessage());
            }
            extInfos.add(flinkProperties);
        }
        return extInfos;
```

### DuplicatedCode
Duplicated code
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/utils/EncryptUtil.java`
#### Snippet
```java
        cipher.init(Cipher.ENCRYPT_MODE, publicKey);
        int inputLen = data.length;
        ByteArrayOutputStream out = new ByteArrayOutputStream();
        int offSet = 0;
        byte[] cache;
        int i = 0;

        while (inputLen - offSet > 0) {
            if (inputLen - offSet > MAX_ENCRYPT_BLOCK) {
                cache = cipher.doFinal(data, offSet, MAX_ENCRYPT_BLOCK);
            } else {
                cache = cipher.doFinal(data, offSet, inputLen - offSet);
            }
            out.write(cache, 0, cache.length);
            i++;
            offSet = i * MAX_ENCRYPT_BLOCK;
        }
        byte[] encryptedData = out.toByteArray();
        out.close();
        return encryptedData;
```

### DuplicatedCode
Duplicated code
in `inlong-manager/manager-common/src/main/java/org/apache/inlong/manager/common/util/RSAUtils.java`
#### Snippet
```java
        int inputLen = data.length;
        ByteArrayOutputStream out = new ByteArrayOutputStream();
        int offSet = 0;
        byte[] cache;
        int i = 0;
        // encrypt data by block
        while (inputLen - offSet > 0) {
            if (inputLen - offSet > MAX_ENCRYPT_BLOCK) {
                cache = cipher.doFinal(data, offSet, MAX_ENCRYPT_BLOCK);
            } else {
                cache = cipher.doFinal(data, offSet, inputLen - offSet);
            }
            out.write(cache, 0, cache.length);
            i++;
            offSet = i * MAX_ENCRYPT_BLOCK;
        }
        byte[] encryptedData = out.toByteArray();
        out.close();
        return encryptedData;
```

### DuplicatedCode
Duplicated code
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/utils/EncryptUtil.java`
#### Snippet
```java
        cipher.init(Cipher.DECRYPT_MODE, privateKey);
        int inputLen = encryptedData.length;
        ByteArrayOutputStream out = new ByteArrayOutputStream();
        int offSet = 0;
        byte[] cache;
        int i = 0;

        while (inputLen - offSet > 0) {
            if (inputLen - offSet > MAX_DECRYPT_BLOCK) {
                cache = cipher.doFinal(encryptedData, offSet, MAX_DECRYPT_BLOCK);
            } else {
                cache = cipher.doFinal(encryptedData, offSet, inputLen - offSet);
            }
            out.write(cache, 0, cache.length);
            i++;
            offSet = i * MAX_DECRYPT_BLOCK;
        }
        byte[] decryptedData = out.toByteArray();
        out.close();
        return decryptedData;
```

### DuplicatedCode
Duplicated code
in `inlong-manager/manager-plugins/src/main/java/org/apache/inlong/manager/plugin/listener/DeleteSortListener.java`
#### Snippet
```java
        GroupResourceProcessForm groupResourceProcessForm = (GroupResourceProcessForm) processForm;
        InlongGroupInfo inlongGroupInfo = groupResourceProcessForm.getGroupInfo();
        List<InlongGroupExtInfo> extList = inlongGroupInfo.getExtList();
        log.info("inlong group ext info: {}", extList);

        Map<String, String> kvConf = new HashMap<>();
        extList.forEach(groupExtInfo -> kvConf.put(groupExtInfo.getKeyName(), groupExtInfo.getKeyValue()));
        String sortExt = kvConf.get(InlongConstants.SORT_PROPERTIES);
        if (StringUtils.isNotEmpty(sortExt)) {
            Map<String, String> result = JsonUtils.OBJECT_MAPPER.convertValue(
                    JsonUtils.OBJECT_MAPPER.readTree(sortExt), new TypeReference<Map<String, String>>() {
                    });
            kvConf.putAll(result);
        }

        String jobId = kvConf.get(InlongConstants.SORT_JOB_ID);
```

### DuplicatedCode
Duplicated code
in `inlong-manager/manager-plugins/src/main/java/org/apache/inlong/manager/plugin/poller/SortStatusPoller.java`
#### Snippet
```java
                Map<String, String> kvConf = new HashMap<>();
                extList.forEach(groupExtInfo -> kvConf.put(groupExtInfo.getKeyName(), groupExtInfo.getKeyValue()));
                String sortExt = kvConf.get(InlongConstants.SORT_PROPERTIES);
                if (StringUtils.isNotEmpty(sortExt)) {
                    Map<String, String> result = JsonUtils.OBJECT_MAPPER.convertValue(
                            JsonUtils.OBJECT_MAPPER.readTree(sortExt), new TypeReference<Map<String, String>>() {
                            });
                    kvConf.putAll(result);
                }

                String jobId = kvConf.get(InlongConstants.SORT_JOB_ID);
```

### DuplicatedCode
Duplicated code
in `inlong-manager/manager-plugins/src/main/java/org/apache/inlong/manager/plugin/listener/StartupSortListener.java`
#### Snippet
```java
        String sortExt = kvConf.get(InlongConstants.SORT_PROPERTIES);
        if (StringUtils.isNotEmpty(sortExt)) {
            Map<String, String> result = JsonUtils.OBJECT_MAPPER.convertValue(
                    JsonUtils.OBJECT_MAPPER.readTree(sortExt), new TypeReference<Map<String, String>>() {
                    });
            kvConf.putAll(result);
        }
```

### DuplicatedCode
Duplicated code
in `inlong-manager/manager-plugins/src/main/java/org/apache/inlong/manager/plugin/listener/DeleteSortListener.java`
#### Snippet
```java
        FlinkInfo flinkInfo = new FlinkInfo();
        flinkInfo.setJobId(jobId);
        String sortUrl = kvConf.get(InlongConstants.SORT_URL);
        flinkInfo.setEndpoint(sortUrl);

        FlinkService flinkService = new FlinkService(flinkInfo.getEndpoint());
        FlinkOperation flinkOperation = new FlinkOperation(flinkService);
```

### DuplicatedCode
Duplicated code
in `inlong-manager/manager-plugins/src/main/java/org/apache/inlong/manager/plugin/listener/DeleteStreamListener.java`
#### Snippet
```java
        Map<String, String> kvConf = new HashMap<>();
        groupExtList.forEach(groupExtInfo -> kvConf.put(groupExtInfo.getKeyName(), groupExtInfo.getKeyValue()));
        streamExtList.forEach(extInfo -> kvConf.put(extInfo.getKeyName(), extInfo.getKeyValue()));

        final String groupId = streamInfo.getInlongGroupId();
        final String streamId = streamInfo.getInlongStreamId();
        String sortExt = kvConf.get(InlongConstants.SORT_PROPERTIES);
        if (StringUtils.isNotEmpty(sortExt)) {
            Map<String, String> result = JsonUtils.OBJECT_MAPPER.convertValue(
                    JsonUtils.OBJECT_MAPPER.readTree(sortExt), new TypeReference<Map<String, String>>() {
                    });
            kvConf.putAll(result);
        }

        String jobId = kvConf.get(InlongConstants.SORT_JOB_ID);
```

### DuplicatedCode
Duplicated code
in `inlong-manager/manager-plugins/src/main/java/org/apache/inlong/manager/plugin/listener/SuspendStreamListener.java`
#### Snippet
```java
        streamExtList.forEach(extInfo -> {
            kvConf.put(extInfo.getKeyName(), extInfo.getKeyValue());
        });

        final String groupId = streamInfo.getInlongGroupId();
        final String streamId = streamInfo.getInlongStreamId();
        String sortExt = kvConf.get(InlongConstants.SORT_PROPERTIES);
        if (StringUtils.isNotEmpty(sortExt)) {
            Map<String, String> result = JsonUtils.OBJECT_MAPPER.convertValue(
                    JsonUtils.OBJECT_MAPPER.readTree(sortExt), new TypeReference<Map<String, String>>() {
                    });
            kvConf.putAll(result);
        }

        String jobId = kvConf.get(InlongConstants.SORT_JOB_ID);
```

### DuplicatedCode
Duplicated code
in `inlong-manager/manager-plugins/src/main/java/org/apache/inlong/manager/plugin/listener/RestartSortListener.java`
#### Snippet
```java
        ProcessForm processForm = context.getProcessForm();
        String groupId = processForm.getInlongGroupId();
        if (!(processForm instanceof GroupResourceProcessForm)) {
            String message = String.format("process form was not GroupResourceProcessForm for groupId [%s]", groupId);
            log.error(message);
            return ListenerResult.fail(message);
        }

        GroupResourceProcessForm groupResourceProcessForm = (GroupResourceProcessForm) processForm;
        InlongGroupInfo inlongGroupInfo = groupResourceProcessForm.getGroupInfo();
        List<InlongGroupExtInfo> extList = inlongGroupInfo.getExtList();
        log.info("inlong group ext info: {}", extList);

        Map<String, String> kvConf = new HashMap<>();
        extList.forEach(groupExtInfo -> kvConf.put(groupExtInfo.getKeyName(), groupExtInfo.getKeyValue()));
        String sortExt = kvConf.get(InlongConstants.SORT_PROPERTIES);
        if (StringUtils.isNotEmpty(sortExt)) {
            Map<String, String> result = JsonUtils.OBJECT_MAPPER.convertValue(
                    JsonUtils.OBJECT_MAPPER.readTree(sortExt), new TypeReference<Map<String, String>>() {
                    });
            kvConf.putAll(result);
        }

        String jobId = kvConf.get(InlongConstants.SORT_JOB_ID);
        if (StringUtils.isBlank(jobId)) {
            String message = String.format("sort job id is empty for groupId [%s]", groupId);
            return ListenerResult.fail(message);
        }
```

### DuplicatedCode
Duplicated code
in `inlong-manager/manager-plugins/src/main/java/org/apache/inlong/manager/plugin/listener/RestartSortListener.java`
#### Snippet
```java
        String dataflow = kvConf.get(InlongConstants.DATAFLOW);
        if (StringUtils.isEmpty(dataflow)) {
            String message = String.format("dataflow is empty for groupId [%s]", groupId);
            log.error(message);
            return ListenerResult.fail(message);
        }

        FlinkInfo flinkInfo = new FlinkInfo();
```

### DuplicatedCode
Duplicated code
in `inlong-manager/manager-plugins/src/main/java/org/apache/inlong/manager/plugin/listener/RestartStreamListener.java`
#### Snippet
```java
        FlinkInfo flinkInfo = new FlinkInfo();
        flinkInfo.setJobId(jobId);
        String jobName = Constants.SORT_JOB_NAME_GENERATOR.apply(processForm);
        flinkInfo.setJobName(jobName);
        String sortUrl = kvConf.get(InlongConstants.SORT_URL);
        flinkInfo.setEndpoint(sortUrl);

        FlinkService flinkService = new FlinkService(flinkInfo.getEndpoint());
        FlinkOperation flinkOperation = new FlinkOperation(flinkService);
```

### DuplicatedCode
Duplicated code
in `inlong-manager/manager-plugins/src/main/java/org/apache/inlong/manager/plugin/listener/RestartSortListener.java`
#### Snippet
```java
        flinkInfo.setJobId(jobId);
        String jobName = Constants.SORT_JOB_NAME_GENERATOR.apply(processForm);
        flinkInfo.setJobName(jobName);
        String sortUrl = kvConf.get(InlongConstants.SORT_URL);
        flinkInfo.setEndpoint(sortUrl);

        FlinkService flinkService = new FlinkService(flinkInfo.getEndpoint());
        FlinkOperation flinkOperation = new FlinkOperation(flinkService);
```

### DuplicatedCode
Duplicated code
in `inlong-manager/manager-plugins/src/main/java/org/apache/inlong/manager/plugin/listener/RestartStreamListener.java`
#### Snippet
```java
        ProcessForm processForm = context.getProcessForm();
        StreamResourceProcessForm streamResourceProcessForm = (StreamResourceProcessForm) processForm;
        InlongGroupInfo groupInfo = streamResourceProcessForm.getGroupInfo();
        List<InlongGroupExtInfo> groupExtList = groupInfo.getExtList();
        log.info("inlong group :{} ext info: {}", groupInfo.getInlongGroupId(), groupExtList);
        InlongStreamInfo streamInfo = streamResourceProcessForm.getStreamInfo();
        List<InlongStreamExtInfo> streamExtList = streamInfo.getExtList();
        log.info("inlong stream :{} ext info: {}", streamInfo.getInlongStreamId(), streamExtList);

        Map<String, String> kvConf = new HashMap<>();
        groupExtList.forEach(groupExtInfo -> kvConf.put(groupExtInfo.getKeyName(), groupExtInfo.getKeyValue()));
```

### DuplicatedCode
Duplicated code
in `inlong-manager/manager-plugins/src/main/java/org/apache/inlong/manager/plugin/listener/RestartStreamListener.java`
#### Snippet
```java
        final String groupId = streamInfo.getInlongGroupId();
        final String streamId = streamInfo.getInlongStreamId();
        String sortExt = kvConf.get(InlongConstants.SORT_PROPERTIES);
        if (StringUtils.isNotEmpty(sortExt)) {
            Map<String, String> result = JsonUtils.OBJECT_MAPPER.convertValue(
                    JsonUtils.OBJECT_MAPPER.readTree(sortExt), new TypeReference<Map<String, String>>() {
                    });
            kvConf.putAll(result);
        }

        String jobId = kvConf.get(InlongConstants.SORT_JOB_ID);
        if (StringUtils.isBlank(jobId)) {
            String message = String.format("sort job id is empty for groupId [%s] streamId [%s]", groupId, streamId);
            return ListenerResult.fail(message);
        }
```

### DuplicatedCode
Duplicated code
in `inlong-manager/manager-pojo/src/main/java/org/apache/inlong/manager/pojo/queue/tubemq/TubeBrokerInfo.java`
#### Snippet
```java
        if (data != null) {
            configurableList = new ArrayList<>();
            workingList = new ArrayList<>();
            idleList = new ArrayList<>();
            needReloadList = new ArrayList<>();
            for (BrokerInfo brokerInfo : data) {
                if (brokerInfo.isConfigurable()) {
                    configurableList.add(brokerInfo);
                }
                if (brokerInfo.isWorking()) {
                    workingList.add(brokerInfo);
                }
                if (brokerInfo.isIdle()) {
                    idleList.add(brokerInfo);
                }
                if (brokerInfo.isConfChanged) {
                    needReloadList.add(brokerInfo.getBrokerId());
                }
            }
        }
```

### DuplicatedCode
Duplicated code
in `inlong-manager/manager-pojo/src/main/java/org/apache/inlong/manager/pojo/sort/standalone/SortSourceClusterInfo.java`
#### Snippet
```java
                JsonObject jo = GSON.fromJson(extParams, JsonObject.class);
                extParamsMap = new HashMap<>();
                jo.keySet().forEach(k -> {
                    JsonElement element = jo.get(k);
                    if (element.isJsonPrimitive()) {
                        extParamsMap.put(k, element.getAsString());
                    } else {
                        extParamsMap.put(k, element.toString());
                    }
                });
```

### DuplicatedCode
Duplicated code
in `inlong-manager/manager-pojo/src/main/java/org/apache/inlong/manager/pojo/sort/util/ExtractNodeUtils.java`
#### Snippet
```java
        Format format;
        DataTypeEnum dataType = DataTypeEnum.forType(serializationType);
        switch (dataType) {
            case CSV:
                if (StringUtils.isNumeric(separatorStr)) {
                    char dataSeparator = (char) Integer.parseInt(separatorStr);
                    separatorStr = Character.toString(dataSeparator);
                }
                CsvFormat csvFormat = new CsvFormat(separatorStr);
                csvFormat.setIgnoreParseErrors(ignoreParseErrors);
                format = csvFormat;
                break;
            case AVRO:
                format = new AvroFormat();
                break;
            case JSON:
                JsonFormat jsonFormat = new JsonFormat();
                jsonFormat.setIgnoreParseErrors(ignoreParseErrors);
                format = jsonFormat;
                break;
            case CANAL:
                format = new CanalJsonFormat();
                break;
            case DEBEZIUM_JSON:
                DebeziumJsonFormat debeziumJsonFormat = new DebeziumJsonFormat();
                debeziumJsonFormat.setIgnoreParseErrors(ignoreParseErrors);
                format = debeziumJsonFormat;
                break;
            case RAW:
                format = new RawFormat();
                break;
            default:
                throw new IllegalArgumentException(String.format("Unsupported dataType=%s", dataType));
        }
        if (wrapWithInlongMsg) {
            Format innerFormat = format;
            format = new InLongMsgFormat(innerFormat, false);
        }
        return format;
```

### DuplicatedCode
Duplicated code
in `inlong-manager/manager-pojo/src/main/java/org/apache/inlong/manager/pojo/sort/util/NodeRelationUtils.java`
#### Snippet
```java
        JoinMode joinMode = joinerDefinition.getJoinMode();
        String leftNode = getNodeName(joinerDefinition.getLeftNode());
        String rightNode = getNodeName(joinerDefinition.getRightNode());
        List<String> preNodes = Lists.newArrayList(leftNode, rightNode);
        List<StreamField> leftJoinFields = joinerDefinition.getLeftJoinFields();
        List<StreamField> rightJoinFields = joinerDefinition.getRightJoinFields();
        List<FilterFunction> filterFunctions = Lists.newArrayList();
        for (int index = 0; index < leftJoinFields.size(); index++) {
            StreamField leftField = leftJoinFields.get(index);
            StreamField rightField = rightJoinFields.get(index);
            LogicOperator operator;
            if (index != 0) {
                operator = AndOperator.getInstance();
            } else {
                operator = EmptyOperator.getInstance();
            }
            filterFunctions.add(createFilterFunction(leftField, rightField, operator));
        }
        Map<String, List<FilterFunction>> joinConditions = new HashMap<>();
        joinConditions.put(rightNode, filterFunctions);
        switch (joinMode) {
            case LEFT_JOIN:
                return new LeftOuterJoinNodeRelation(preNodes, nodeRelation.getOutputs(), joinConditions);
            case INNER_JOIN:
                return new InnerJoinNodeRelation(preNodes, nodeRelation.getOutputs(), joinConditions);
            case RIGHT_JOIN:
                return new RightOuterJoinNodeRelation(preNodes, nodeRelation.getOutputs(), joinConditions);
            default:
                throw new IllegalArgumentException(String.format("Unsupported join mode=%s for inlong", joinMode));
        }
```

### DuplicatedCode
Duplicated code
in `inlong-manager/manager-pojo/src/main/java/org/apache/inlong/manager/pojo/sort/util/NodeRelationUtils.java`
#### Snippet
```java
        String leftNode = getNodeName(joinerDefinition.getLeftNode());
        String rightNode = getNodeName(joinerDefinition.getRightNode());
        List<String> preNodes = Lists.newArrayList(leftNode, rightNode);
        List<StreamField> leftJoinFields = joinerDefinition.getLeftJoinFields();
        List<StreamField> rightJoinFields = joinerDefinition.getRightJoinFields();
        List<FilterFunction> filterFunctions = Lists.newArrayList();
        for (int index = 0; index < leftJoinFields.size(); index++) {
            StreamField leftField = leftJoinFields.get(index);
            StreamField rightField = rightJoinFields.get(index);
            LogicOperator operator;
            if (index != 0) {
                operator = AndOperator.getInstance();
            } else {
                operator = EmptyOperator.getInstance();
            }
            filterFunctions.add(createFilterFunction(leftField, rightField, operator));
        }
```

### DuplicatedCode
Duplicated code
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/cluster/InlongClusterServiceImpl.java`
#### Snippet
```java
                if (!opInfo.getAccountType().equals(UserTypeEnum.ADMIN.getCode())) {
                    List<String> inCharges = Arrays.asList(tagEntity.getInCharges().split(InlongConstants.COMMA));
                    if (!inCharges.contains(opInfo.getName())) {
                        continue;
                    }
                }
                filterResult.add(tagEntity);
```

### DuplicatedCode
Duplicated code
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/cluster/InlongClusterServiceImpl.java`
#### Snippet
```java
        String updateTags = Joiner.on(",").join(tagSet);
        entity.setClusterTags(updateTags);
        entity.setModifier(operator);
        if (InlongConstants.AFFECTED_ONE_ROW != clusterMapper.updateById(entity)) {
            LOGGER.error("cluster has already updated with name={}, type={}, curVersion={}", entity.getName(),
                    entity.getType(), entity.getVersion());
            throw new BusinessException(ErrorCodeEnum.CONFIG_EXPIRED);
        }
```

### DuplicatedCode
Duplicated code
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/cluster/InlongClusterServiceImpl.java`
#### Snippet
```java
                String updateTags = Joiner.on(",").join(tagSet);
                entity.setClusterTags(updateTags);
                entity.setModifier(opInfo.getName());
                if (InlongConstants.AFFECTED_ONE_ROW != clusterMapper.updateById(entity)) {
                    throw new BusinessException(ErrorCodeEnum.CONFIG_EXPIRED,
                            String.format("cluster has already updated with name=%s, type=%s, curVersion=%s",
                                    entity.getName(), entity.getType(), entity.getVersion()));
                }
```

### DuplicatedCode
Duplicated code
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/cluster/InlongClusterServiceImpl.java`
#### Snippet
```java
                InlongClusterEntity entity = clusterMapper.selectById(id);
                String errMsg = String.format(
                        "Current user does not have permission to bind or unbind cluster tag for cluster by id=%s, name=%s",
                        entity.getId(), entity.getName());
                userService.checkUser(entity.getInCharges(), opInfo.getName(), errMsg);
                Set<String> tagSet = Sets.newHashSet(entity.getClusterTags().split(InlongConstants.COMMA));
```

### DuplicatedCode
Duplicated code
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/cluster/InlongClusterServiceImpl.java`
#### Snippet
```java
        if (CollectionUtils.isNotEmpty(nodeEntities)) {
            String errMsg = String.format("there are undeleted nodes under the cluster [%s], "
                    + "please delete the node first", entity.getName());
            throw new BusinessException(errMsg);
        }

        entity.setIsDeleted(entity.getId());
        entity.setModifier(operator);
        if (InlongConstants.AFFECTED_ONE_ROW != clusterMapper.updateById(entity)) {
            LOGGER.error("cluster has already updated with name={}, type={}, curVersion={}", entity.getName(),
                    entity.getType(), entity.getVersion());
            throw new BusinessException(ErrorCodeEnum.CONFIG_EXPIRED);
        }
```

### DuplicatedCode
Duplicated code
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/cluster/InlongClusterServiceImpl.java`
#### Snippet
```java
        Preconditions.expectEquals(entity.getType(), request.getType(),
                ErrorCodeEnum.INVALID_PARAMETER, "type not allowed modify");
        // check record version
        Preconditions.expectEquals(entity.getVersion(), request.getVersion(),
                ErrorCodeEnum.CONFIG_EXPIRED,
                String.format("record has expired with record version=%d, request version=%d",
                        entity.getVersion(), request.getVersion()));
        // check protocol type
        if (StringUtils.isBlank(request.getProtocolType())) {
            request.setProtocolType(entity.getProtocolType());
        }
        // check cluster node if exist
        InlongClusterNodeEntity exist = clusterNodeMapper.selectByUniqueKey(request);
```

### DuplicatedCode
Duplicated code
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/core/impl/AgentServiceImpl.java`
#### Snippet
```java
            bindSet.stream().flatMap(clusterNode -> {
                ClusterPageRequest pageRequest = new ClusterPageRequest();
                pageRequest.setParentId(cluster.getId());
                pageRequest.setType(ClusterType.AGENT);
                pageRequest.setKeyword(clusterNode);
                return clusterNodeMapper.selectByCondition(pageRequest).stream();
            }).filter(Objects::nonNull)
                    .forEach(entity -> {
```

### DuplicatedCode
Duplicated code
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/core/impl/AgentServiceImpl.java`
#### Snippet
```java
                        Set<String> groupSet = new HashSet<>();
                        AgentClusterNodeDTO agentClusterNodeDTO = new AgentClusterNodeDTO();
                        if (StringUtils.isNotBlank(entity.getExtParams())) {
                            agentClusterNodeDTO = AgentClusterNodeDTO.getFromJson(entity.getExtParams());
                            String agentGroup = agentClusterNodeDTO.getAgentGroup();
                            groupSet = StringUtils.isBlank(agentGroup) ? groupSet
                                    : Sets.newHashSet(agentGroup.split(InlongConstants.COMMA));
                        }
```

### DuplicatedCode
Duplicated code
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/group/InlongGroupServiceImpl.java`
#### Snippet
```java
        InlongGroupOperator instance = groupOperatorFactory.getInstance(entity.getMqType());
        InlongGroupInfo groupInfo = instance.getFromEntity(entity);

        // get all ext info
        List<InlongGroupExtEntity> extEntityList = groupExtMapper.selectByGroupId(groupId);
        List<InlongGroupExtInfo> extList = CommonBeanUtils.copyListProperties(extEntityList, InlongGroupExtInfo::new);
        groupInfo.setExtList(extList);
        BaseSortConf sortConf = buildSortConfig(extList);
        groupInfo.setSortConf(sortConf);
```

### DuplicatedCode
Duplicated code
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/group/InlongGroupServiceImpl.java`
#### Snippet
```java
        List<InlongGroupBriefInfo> briefInfos = CommonBeanUtils.copyListProperties(entityPage,
                InlongGroupBriefInfo::new);

        // list all related sources
        if (request.isListSources() && CollectionUtils.isNotEmpty(briefInfos)) {
            Set<String> groupIds = briefInfos.stream().map(InlongGroupBriefInfo::getInlongGroupId)
                    .collect(Collectors.toSet());
            List<StreamSourceEntity> sourceEntities = streamSourceMapper.selectByGroupIds(new ArrayList<>(groupIds));
            Map<String, List<StreamSource>> sourceMap = Maps.newHashMap();
            sourceEntities.forEach(sourceEntity -> {
                StreamSourceOperator operation = sourceOperatorFactory.getInstance(sourceEntity.getSourceType());
                StreamSource source = operation.getFromEntity(sourceEntity);
                sourceMap.computeIfAbsent(sourceEntity.getInlongGroupId(), k -> Lists.newArrayList()).add(source);
            });
            briefInfos.forEach(group -> {
                List<StreamSource> sources = sourceMap.getOrDefault(group.getInlongGroupId(), Lists.newArrayList());
                group.setStreamSources(sources);
            });
        }
```

### DuplicatedCode
Duplicated code
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/heartbeat/HeartbeatManager.java`
#### Snippet
```java
        String[] ports = heartbeat.getPort().split(InlongConstants.COMMA);
        String[] ips = heartbeat.getIp().split(InlongConstants.COMMA);
        String protocolType = heartbeat.getProtocolType();
        String[] protocolTypes = null;
        if (StringUtils.isNotBlank(protocolType) && ports.length > 1) {
            protocolTypes = protocolType.split(InlongConstants.COMMA);
            if (protocolTypes.length < ports.length) {
                protocolTypes = null;
            }
        }
```

### DuplicatedCode
Duplicated code
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/heartbeat/HeartbeatManager.java`
#### Snippet
```java
            HeartbeatMsg heartbeatMsg = JsonUtils.parseObject(JsonUtils.toJsonByte(heartbeat), HeartbeatMsg.class);
            assert heartbeatMsg != null;
            heartbeatMsg.setPort(ports[i].trim());
            heartbeatMsg.setIp(ips[i].trim());
            if (protocolTypes != null) {
                heartbeatMsg.setProtocolType(protocolTypes[i]);
            } else {
                heartbeatMsg.setProtocolType(protocolType);
            }
```

### DuplicatedCode
Duplicated code
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/listener/GroupTaskListenerFactory.java`
#### Snippet
```java
        if (!(plugin instanceof ProcessPlugin)) {
            return;
        }
        ProcessPlugin processPlugin = (ProcessPlugin) plugin;
        List<SourceOperateListener> pluginSourceOperateListeners = processPlugin.createSourceOperateListeners();
        if (CollectionUtils.isNotEmpty(pluginSourceOperateListeners)) {
            sourceOperateListeners.addAll(processPlugin.createSourceOperateListeners());
        }
        List<QueueOperateListener> pluginQueueOperateListeners = processPlugin.createQueueOperateListeners();
        if (CollectionUtils.isNotEmpty(pluginQueueOperateListeners)) {
            queueOperateListeners.addAll(pluginQueueOperateListeners);
        }
        List<SortOperateListener> pluginSortOperateListeners = processPlugin.createSortOperateListeners();
        if (CollectionUtils.isNotEmpty(pluginSortOperateListeners)) {
            sortOperateListeners.addAll(pluginSortOperateListeners);
        }
```

### DuplicatedCode
Duplicated code
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/listener/consume/apply/ApproveConsumeProcessListener.java`
#### Snippet
```java
        String groupId = entity.getInlongGroupId();
        InlongGroupEntity groupEntity = groupMapper.selectByGroupId(groupId);
        Preconditions.expectNotNull(groupEntity, "inlong group not found for groupId=" + groupId);
        String mqResource = groupEntity.getMqResource();
        Preconditions.expectNotBlank(mqResource, ErrorCodeEnum.INVALID_PARAMETER,
                "mq resource cannot empty for groupId=" + groupId);

        String clusterTag = groupEntity.getInlongClusterTag();
```

### DuplicatedCode
Duplicated code
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/node/DataNodeServiceImpl.java`
#### Snippet
```java
        chkUnmodifiableParams(curEntity, request);
        // Check whether the data node name exists with the same name and type
        if (request.getName() != null) {
            if (StringUtils.isBlank(request.getName())) {
                throw new BusinessException(ErrorCodeEnum.INVALID_PARAMETER,
                        "the name changed of data node is blank!");
            }
            DataNodeEntity existEntity =
                    dataNodeMapper.selectByUniqueKey(request.getName(), request.getType());
            if (existEntity != null && !existEntity.getId().equals(request.getId())) {
                throw new BusinessException(ErrorCodeEnum.RECORD_DUPLICATE,
                        String.format("data node already exist for name=%s, type=%s, required id=%s, exist id=%s",
                                request.getName(), request.getType(), request.getId(), existEntity.getId()));
            }
        }
        DataNodeOperator dataNodeOperator = operatorFactory.getInstance(request.getType());
```

### DuplicatedCode
Duplicated code
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/repository/DataProxyConfigRepository.java`
#### Snippet
```java
            Map<String, InlongClusterEntity> clusterMap = new HashMap<>();
            ClusterPageRequest clusterRequest = new ClusterPageRequest();
            List<InlongClusterEntity> clusters = clusterMapper.selectByCondition(clusterRequest);
            clusters.forEach((v) -> {
                clusterMap.put(v.getName(), v);
            });
            // prepare stream sink
            SinkPageRequest request = new SinkPageRequest();
            request.setInlongGroupId(inlongGroupId);
            List<StreamSinkEntity> streamSinks = streamSinkMapper.selectByCondition(request);
            List<StreamSinkEntity> newStreamSinks = new ArrayList<>();
```

### DuplicatedCode
Duplicated code
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sink/ck/ClickHouseResourceOperator.java`
#### Snippet
```java
        if (sinkInfo == null) {
            LOGGER.warn("sink info was null, skip to create resource");
            return;
        }

        if (SinkStatus.CONFIG_SUCCESSFUL.getCode().equals(sinkInfo.getStatus())) {
            LOGGER.warn("sink resource [" + sinkInfo.getId() + "] already success, skip to create");
            return;
        } else if (InlongConstants.DISABLE_CREATE_RESOURCE.equals(sinkInfo.getEnableCreateResource())) {
            LOGGER.warn("create resource was disabled, skip to create for [" + sinkInfo.getId() + "]");
            return;
        }

        this.createTable(sinkInfo);
```

### DuplicatedCode
Duplicated code
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sink/greenplum/GreenplumJdbcUtils.java`
#### Snippet
```java
        try (Statement statement = conn.createStatement();
                ResultSet resultSet = statement.executeQuery(checkTableSql)) {
            if (null != resultSet && resultSet.next()) {
                int size = resultSet.getInt(1);
                if (size > 0) {
                    result = true;
                }
            }
        }
        LOG.info("check table exist for username={} table={}, result={}", schemaName, tableName, result);
        return result;
```

### DuplicatedCode
Duplicated code
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sink/greenplum/GreenplumJdbcUtils.java`
#### Snippet
```java
        try (Statement statement = conn.createStatement();
                ResultSet resultSet = statement.executeQuery(checkColumnSql)) {
            if (Objects.nonNull(resultSet) && resultSet.next()) {
                int count = resultSet.getInt(1);
                if (count > 0) {
                    result = true;
                }
            }
        }
        LOG.info("check column exist for table={}, column={}, result={}", tableName, column, result);
        return result;
```

### DuplicatedCode
Duplicated code
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sink/greenplum/GreenplumSqlBuilder.java`
#### Snippet
```java
        final List<String> sqls = Lists.newArrayList();
        final StringBuilder createSql = new StringBuilder()
                .append("CREATE TABLE ").append(table.getSchemaName())
                .append(".\"")
                .append(table.getTableName())
                .append("\"")
                .append(buildCreateColumnsSql(table));
        sqls.add(createSql.toString());

        // column comments
        sqls.addAll(getColumnsComment(table.getSchemaName(), table.getTableName(), table.getColumns()));
        // table comment
        if (StringUtils.isNotEmpty(table.getComment())) {
            sqls.add(getTableComment(table));
        }
        LOGGER.info("create table sql : {}", sqls);
        return sqls;
```

### DuplicatedCode
Duplicated code
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sink/greenplum/GreenplumSqlBuilder.java`
#### Snippet
```java
        final List<String> resultList = Lists.newArrayList();
        final StringBuilder sqlBuilder = new StringBuilder();

        columnList.forEach(columnInfo -> {
            sqlBuilder.append("ALTER TABLE \"")
                    .append(schemaName)
                    .append("\".\"")
                    .append(tableName)
                    .append("\" ADD \"")
                    .append(columnInfo.getName())
                    .append("\" ")
                    .append(columnInfo.getType())
                    .append(" ");
            resultList.add(sqlBuilder.toString());
            sqlBuilder.delete(0, sqlBuilder.length());
        });
        resultList.addAll(getColumnsComment(schemaName, tableName, columnList));
        LOGGER.info("add columns sql={}", resultList);
        return resultList;
```

### DuplicatedCode
Duplicated code
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sink/greenplum/GreenplumSqlBuilder.java`
#### Snippet
```java
        final List<String> columnList = getColumnsInfo(table.getColumns());
        final StringBuilder sql = new StringBuilder()
                .append(" (")
                .append(StringUtils.join(columnList, ","));
        if (!StringUtils.isEmpty(table.getPrimaryKey())) {
            sql.append(", PRIMARY KEY (")
                    .append(table.getPrimaryKey())
                    .append(")");
        }
        sql.append(") ");
```

### DuplicatedCode
Duplicated code
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sink/greenplum/GreenplumSqlBuilder.java`
#### Snippet
```java
        final List<String> columnList = new ArrayList<>();
        final StringBuilder columnBuilder = new StringBuilder();

        columns.forEach(columnInfo -> {
            columnBuilder.append("\"")
                    .append(columnInfo.getName())
                    .append("\" ")
                    .append(columnInfo.getType());
            columnList.add(columnBuilder.toString());
            columnBuilder.delete(0, columnBuilder.length());
        });
        return columnList;
```

### DuplicatedCode
Duplicated code
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sink/greenplum/GreenplumSqlBuilder.java`
#### Snippet
```java
            if (StringUtils.isNotBlank(columnInfo.getComment())) {
                StringBuilder commSql = new StringBuilder();
                commSql.append("COMMENT ON COLUMN \"")
                        .append(schemaName)
                        .append("\".\"")
                        .append(tableName)
                        .append("\".\"")
                        .append(columnInfo.getName())
                        .append("\" IS \'")
                        .append(columnInfo.getComment())
                        .append("\' ;");
                commentList.add(commSql.toString());
            }
```

### DuplicatedCode
Duplicated code
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sink/greenplum/GreenplumSqlBuilder.java`
#### Snippet
```java
        StringBuilder sql = new StringBuilder().append(
                "SELECT A.COLUMN_NAME,A.UDT_NAME,C.DESCRIPTION FROM INFORMATION_SCHEMA.COLUMNS A")
                .append(" LEFT JOIN   (SELECT PC.OID AS OOID,PN.NSPNAME,PC.RELNAME")
                .append(" FROM PG_CLASS PC LEFT OUTER JOIN PG_NAMESPACE PN ON PC.RELNAMESPACE = PN.OID ")
                .append(" WHERE PN.NSPNAME ='")
                .append(schemaName)
                .append("' AND PC.RELNAME = '")
                .append(tableName)
                .append("') B   ON A.TABLE_SCHEMA = B.NSPNAME AND A.TABLE_NAME = B.RELNAME")
                .append(" LEFT JOIN PG_CATALOG.PG_DESCRIPTION C ")
                .append("ON B.OOID = C.OBJOID AND A.ORDINAL_POSITION = C.OBJSUBID")
                .append(" WHERE A.TABLE_SCHEMA = '")
                .append(schemaName)
                .append("' AND A.TABLE_NAME = '")
                .append(tableName)
                .append("'  ORDER BY  C.OBJSUBID ;");
        LOGGER.info("desc table sql={}", sql);
        return sql.toString();
```

### DuplicatedCode
Duplicated code
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sink/hive/HiveJdbcUtils.java`
#### Snippet
```java
        try (Connection conn = getConnection(url, user, password)) {
            HiveDatabaseMetaData metaData = (HiveDatabaseMetaData) conn.getMetaData();
            ResultSet rs = metaData.getTables(dbName, dbName, null, new String[]{METADATA_TYPE});
            List<String> tables = new ArrayList<>();
            while (rs.next()) {
                String tableName = rs.getString(COLUMN_LABEL);
                tables.add(tableName);
            }
            rs.close();

            return tables;
        }
```

### DuplicatedCode
Duplicated code
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sink/hudi/HudiResourceOperator.java`
#### Snippet
```java
        if (sinkInfo == null) {
            LOGGER.warn("sink info was null, skip to create resource");
            return;
        }

        if (SinkStatus.CONFIG_SUCCESSFUL.getCode().equals(sinkInfo.getStatus())) {
            LOGGER.warn("sink resource [" + sinkInfo.getId() + "] already success, skip to create");
            return;
        } else if (InlongConstants.DISABLE_CREATE_RESOURCE.equals(sinkInfo.getEnableCreateResource())) {
            LOGGER.warn("create resource was disabled, skip to create for [" + sinkInfo.getId() + "]");
            return;
        }

        this.createTableIfAbsent(sinkInfo);
```

### DuplicatedCode
Duplicated code
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sink/mysql/MySQLJdbcUtils.java`
#### Snippet
```java
        conn.setAutoCommit(false);
        try (Statement stmt = conn.createStatement()) {
            for (String entry : sqls) {
                stmt.execute(entry);
            }
            conn.commit();
            LOGGER.info("execute sql [{}] success", sqls);
        } finally {
            conn.setAutoCommit(true);
        }
```

### DuplicatedCode
Duplicated code
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sink/mysql/MySQLSqlBuilder.java`
#### Snippet
```java
        final List<String> columnInfoList = getColumnsInfo(columnList);
        final List<String> resultList = new ArrayList<>();
        final StringBuilder sqlBuilder = new StringBuilder();
        columnInfoList.forEach(columnInfo -> {
            sqlBuilder.append("ALTER TABLE ")
                    .append(dbName)
                    .append(".")
                    .append(tableName)
                    .append(" ADD COLUMN ")
                    .append(columnInfo)
                    .append(";");
            resultList.add(sqlBuilder.toString());
            sqlBuilder.delete(0, sqlBuilder.length());
        });
        return resultList;
```

### DuplicatedCode
Duplicated code
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/sink/AbstractSinkOperator.java`
#### Snippet
```java
        if (CollectionUtils.isEmpty(fieldList)) {
            return;
        }

        int size = fieldList.size();
        List<StreamSinkFieldEntity> entityList = new ArrayList<>(size);
        String groupId = request.getInlongGroupId();
        String streamId = request.getInlongStreamId();
        String sinkType = request.getSinkType();
        Integer sinkId = request.getId();
```

### DuplicatedCode
Duplicated code
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/sink/StreamSinkServiceImpl.java`
#### Snippet
```java
        boolean streamSuccess = StreamStatus.CONFIG_SUCCESSFUL.getCode().equals(streamEntity.getStatus());
        if (streamSuccess || StreamStatus.CONFIG_FAILED.getCode().equals(streamEntity.getStatus())) {
            boolean enableCreateResource = InlongConstants.ENABLE_CREATE_RESOURCE.equals(
                    request.getEnableCreateResource());
            SinkStatus nextStatus = request.getStartProcess() ? SinkStatus.CONFIG_ING : SinkStatus.NEW;
            if (!enableCreateResource) {
                nextStatus = SinkStatus.CONFIG_SUCCESSFUL;
            }
            StreamSinkEntity sinkEntity = sinkMapper.selectByPrimaryKey(id);
            sinkEntity.setStatus(nextStatus.getCode());
            sinkMapper.updateStatus(sinkEntity);
        }
```

### DuplicatedCode
Duplicated code
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/sink/StreamSinkServiceImpl.java`
#### Snippet
```java
        InlongGroupEntity entity = groupMapper.selectByGroupId(request.getInlongGroupId());
        if (entity == null) {
            throw new BusinessException(ErrorCodeEnum.GROUP_NOT_FOUND,
                    String.format("InlongGroup does not exist with InlongGroupId=%s", request.getInlongGroupId()));
        }
        // only the person in charges can query
        userService.checkUser(entity.getInCharges(), opInfo.getName(),
                ErrorCodeEnum.GROUP_PERMISSION_DENIED.getMessage());
        // check group status
        GroupStatus curState = GroupStatus.forCode(entity.getStatus());
        if (GroupStatus.notAllowedUpdate(curState)) {
            throw new BusinessException(String.format(ErrorCodeEnum.OPT_NOT_ALLOWED_BY_STATUS.getMessage(), curState));
        }
```

### DuplicatedCode
Duplicated code
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/sink/StreamSinkServiceImpl.java`
#### Snippet
```java
        if (groupEntity == null) {
            throw new BusinessException(ErrorCodeEnum.GROUP_NOT_FOUND,
                    String.format("InlongGroup does not exist with InlongGroupId=%s", sinkEntity.getInlongGroupId()));
        }
        // only the person in charges can query
        userService.checkUser(groupEntity.getInCharges(), opInfo.getName(),
                ErrorCodeEnum.GROUP_PERMISSION_DENIED.getMessage());
        // Check if group status can be modified
        GroupStatus curState = GroupStatus.forCode(groupEntity.getStatus());
        if (GroupStatus.notAllowedUpdate(curState)) {
            throw new BusinessException(String.format(ErrorCodeEnum.OPT_NOT_ALLOWED_BY_STATUS.getMessage(), curState));
        }
```

### DuplicatedCode
Duplicated code
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/sink/StreamSinkServiceImpl.java`
#### Snippet
```java
        if (request.getId() == null) {
            throw new BusinessException(ErrorCodeEnum.ID_IS_EMPTY);
        }
        StreamSinkEntity curEntity = sinkMapper.selectByPrimaryKey(request.getId());
        if (curEntity == null) {
            throw new BusinessException(ErrorCodeEnum.SINK_INFO_NOT_FOUND);
        }
        chkUnmodifiableParams(curEntity, request);
```

### DuplicatedCode
Duplicated code
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/sink/StreamSinkServiceImpl.java`
#### Snippet
```java
        SinkStatus nextStatus = null;
        boolean enableConfig = StreamStatus.CONFIG_SUCCESSFUL.getCode().equals(streamEntity.getStatus())
                || StreamStatus.CONFIG_FAILED.getCode().equals(streamEntity.getStatus());
        if (enableConfig) {
            boolean enableCreateResource = InlongConstants.ENABLE_CREATE_RESOURCE.equals(
                    request.getEnableCreateResource());
            nextStatus = enableCreateResource ? SinkStatus.CONFIG_ING : SinkStatus.CONFIG_SUCCESSFUL;
        }
        StreamSinkOperator sinkOperator = operatorFactory.getInstance(request.getSinkType());
```

### DuplicatedCode
Duplicated code
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/sink/StreamSinkServiceImpl.java`
#### Snippet
```java
        InlongGroupEntity curGroupEntity = groupMapper.selectByGroupId(curEntity.getInlongGroupId());
        if (curGroupEntity == null) {
            throw new BusinessException(ErrorCodeEnum.ILLEGAL_RECORD_FIELD_VALUE,
                    String.format("InlongGroup does not exist with InlongGroupId=%s", curEntity.getInlongGroupId()));
        }
        // only the person in charges can query
        userService.checkUser(curGroupEntity.getInCharges(), opInfo.getName(),
                ErrorCodeEnum.GROUP_PERMISSION_DENIED.getMessage());
        // Check if group status can be modified
        GroupStatus curState = GroupStatus.forCode(curEntity.getStatus());
```

### DuplicatedCode
Duplicated code
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/sink/StreamSinkServiceImpl.java`
#### Snippet
```java
            if (cols.length < PARSE_FIELD_CSV_MIN_COLUMNS) {
                throw new BusinessException(ErrorCodeEnum.INVALID_PARAMETER,
                        "At least two fields are required, line number is " + (i + 1));
            }
            String fieldName = cols[0];
            if (!PATTERN_NORMAL_CHARACTERS.matcher(fieldName).matches()) {
                throw new BusinessException(ErrorCodeEnum.INVALID_PARAMETER, "Field names in line " + (i + 1) +
                        " can only contain letters, underscores or numbers");
            }
            String fieldType = cols[1];
```

### DuplicatedCode
Duplicated code
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/sink/StreamSinkServiceImpl.java`
#### Snippet
```java
            sinkField.setFieldType(realDataType);
            // get field comment
            List<String> columnSpecs = definition.getColumnSpecs();
            if (CollectionUtils.isNotEmpty(columnSpecs)) {
                int commentIndex = -1;
                for (int csIndex = 0; csIndex < columnSpecs.size(); csIndex++) {
                    String spec = columnSpecs.get(csIndex);
                    if (spec.toUpperCase().startsWith("COMMENT")) {
                        commentIndex = csIndex;
                        break;
                    }
                }
                String comment = null;
                if (-1 != commentIndex && columnSpecs.size() > commentIndex + 1) {
                    comment = columnSpecs.get(commentIndex + 1).replaceAll("['\"]", "");
                }
                sinkField.setFieldComment(comment);
            }

            fields.add(sinkField);
```

### DuplicatedCode
Duplicated code
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/sink/StreamSinkServiceImpl.java`
#### Snippet
```java
        Preconditions.expectEquals(curEntity.getVersion(), request.getVersion(),
                ErrorCodeEnum.CONFIG_EXPIRED,
                String.format("record has expired with record version=%d, request version=%d",
                        curEntity.getVersion(), request.getVersion()));
        if (StringUtils.isNotBlank(request.getInlongGroupId())
                && !curEntity.getInlongGroupId().equals(request.getInlongGroupId())) {
            throw new BusinessException(ErrorCodeEnum.INVALID_PARAMETER,
                    "InlongGroupId not allowed modify");
        }
        if (StringUtils.isNotBlank(request.getInlongStreamId())
                && !curEntity.getInlongStreamId().equals(request.getInlongStreamId())) {
            throw new BusinessException(ErrorCodeEnum.INVALID_PARAMETER,
                    "InlongStreamId not allowed modify");
        }
```

### DuplicatedCode
Duplicated code
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/sink/ck/ClickHouseSinkOperator.java`
#### Snippet
```java
        List<SinkField> fieldList = request.getSinkFieldList();
        LOGGER.debug("begin to save es sink fields={}", fieldList);
        if (CollectionUtils.isEmpty(fieldList)) {
            return;
        }
        int size = fieldList.size();
        List<StreamSinkFieldEntity> entityList = new ArrayList<>(size);
        String groupId = request.getInlongGroupId();
        String streamId = request.getInlongStreamId();
        String sinkType = request.getSinkType();
        Integer sinkId = request.getId();
```

### DuplicatedCode
Duplicated code
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/sink/hudi/HudiSinkOperator.java`
#### Snippet
```java
            if (info.getPrecision() == null || info.getScale() == null) {
                String errorMsg = String.format("precision or scale not specified for decimal field (%s)",
                        field.getFieldName());
                LOGGER.error("field info check error: {}", errorMsg);
                throw new BusinessException(errorMsg);
            }
            if (info.getPrecision() < info.getScale()) {
                String errorMsg = String.format(
                        "precision (%d) must be greater or equal than scale (%d) for decimal field (%s)",
                        info.getPrecision(), info.getScale(), field.getFieldName());
                LOGGER.error("field info check error: {}", errorMsg);
                throw new BusinessException(errorMsg);
            }
```

### DuplicatedCode
Duplicated code
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/source/AbstractSourceOperator.java`
#### Snippet
```java
        StreamSourceEntity curEntity = CommonBeanUtils.copyProperties(request, StreamSourceEntity::new);
        curEntity.setPreviousStatus(curState.getCode());
        curEntity.setStatus(nextState.getCode());
        int rowCount = sourceMapper.updateByPrimaryKeySelective(curEntity);
        if (rowCount != InlongConstants.AFFECTED_ONE_ROW) {
            LOGGER.error("source has already updated with groupId={}, streamId={}, name={}, curVersion={}",
                    curEntity.getInlongGroupId(), curEntity.getInlongStreamId(), curEntity.getSourceName(),
                    curEntity.getVersion());
            throw new BusinessException(ErrorCodeEnum.CONFIG_EXPIRED);
        }
```

### DuplicatedCode
Duplicated code
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/source/AbstractSourceOperator.java`
#### Snippet
```java
        if (CollectionUtils.isEmpty(infoList)) {
            return;
        }
        infoList.forEach(streamField -> streamField.setId(null));
        List<InlongStreamFieldEntity> list = CommonBeanUtils.copyListProperties(infoList,
                InlongStreamFieldEntity::new);
        for (InlongStreamFieldEntity entity : list) {
            entity.setInlongGroupId(groupId);
            entity.setInlongStreamId(streamId);
            entity.setIsDeleted(InlongConstants.UN_DELETED);
        }
        streamFieldMapper.insertAll(list);
```

### DuplicatedCode
Duplicated code
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/source/StreamSourceServiceImpl.java`
#### Snippet
```java
        GroupStatus status = GroupStatus.forCode(groupEntity.getStatus());
        if (GroupStatus.notAllowedUpdate(status)) {
            throw new BusinessException(ErrorCodeEnum.OPT_NOT_ALLOWED_BY_STATUS,
                    String.format(ErrorCodeEnum.OPT_NOT_ALLOWED_BY_STATUS.getMessage(), status));
        }
        // According to the source type, save source information
        StreamSourceOperator sourceOperator = operatorFactory.getInstance(request.getSourceType());
        // Remove id in sourceField when save
        List<StreamField> streamFields = request.getFieldList();
        if (CollectionUtils.isNotEmpty(streamFields)) {
            streamFields.forEach(streamField -> streamField.setId(null));
        }
```

### DuplicatedCode
Duplicated code
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/source/StreamSourceServiceImpl.java`
#### Snippet
```java
        if (GroupStatus.notAllowedUpdate(status)) {
            throw new BusinessException(ErrorCodeEnum.OPT_NOT_ALLOWED_BY_STATUS,
                    String.format(ErrorCodeEnum.OPT_NOT_ALLOWED_BY_STATUS.getMessage(), status));
        }
        StreamSourceOperator sourceOperator = operatorFactory.getInstance(request.getSourceType());
        // Remove id in sourceField when save
        List<StreamField> streamFields = request.getFieldList();
        if (CollectionUtils.isNotEmpty(streamFields)) {
            streamFields.forEach(streamField -> streamField.setId(null));
        }
```

### DuplicatedCode
Duplicated code
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/source/StreamSourceServiceImpl.java`
#### Snippet
```java
        Map<String, Page<StreamSourceEntity>> sourceMap = Maps.newHashMap();
        for (StreamSourceEntity entity : entityList) {
            sourceMap.computeIfAbsent(entity.getSourceType(), k -> new Page<>()).add(entity);
        }
        List<StreamSource> responseList = Lists.newArrayList();
        for (Map.Entry<String, Page<StreamSourceEntity>> entry : sourceMap.entrySet()) {
            StreamSourceOperator sourceOperator = operatorFactory.getInstance(entry.getKey());
            PageResult<? extends StreamSource> pageInfo = sourceOperator.getPageInfo(entry.getValue());
            if (null != pageInfo && CollectionUtils.isNotEmpty(pageInfo.getList())) {
                responseList.addAll(pageInfo.getList());
            }
        }
```

### DuplicatedCode
Duplicated code
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/source/StreamSourceServiceImpl.java`
#### Snippet
```java
        SourceStatus curStatus = SourceStatus.forCode(entity.getStatus());
        SourceStatus nextStatus = SourceStatus.TO_BE_ISSUED_DELETE;
        // if source is frozen|failed|new, or if it is a template source or auto push source, delete directly
        if (curStatus == SourceStatus.SOURCE_STOP || curStatus == SourceStatus.SOURCE_FAILED
                || curStatus == SourceStatus.SOURCE_NEW || isTemplateSource
                || SourceType.AUTO_PUSH.equals(entity.getSourceType())) {
            nextStatus = SourceStatus.SOURCE_DISABLE;
        }
```

### DuplicatedCode
Duplicated code
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/source/StreamSourceServiceImpl.java`
#### Snippet
```java
        StreamSourceEntity entity = sourceMapper.selectByIdForUpdate(id);
        Preconditions.expectNotNull(entity, ErrorCodeEnum.SOURCE_INFO_NOT_FOUND.getMessage());
        InlongGroupEntity groupEntity = groupMapper.selectByGroupId(entity.getInlongGroupId());
        if (groupEntity == null) {
            throw new BusinessException(ErrorCodeEnum.GROUP_NOT_FOUND,
                    String.format("InlongGroup does not exist with InlongGroupId=%s", entity.getInlongGroupId()));
        }
        // only the person in charges can query
        userService.checkUser(groupEntity.getInCharges(), operator, ErrorCodeEnum.GROUP_PERMISSION_DENIED.getMessage());

        StreamSourceOperator sourceOperator = operatorFactory.getInstance(entity.getSourceType());
        SourceRequest sourceRequest = new SourceRequest();
        CommonBeanUtils.copyProperties(entity, sourceRequest, true);
```

### DuplicatedCode
Duplicated code
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/stream/InlongStreamProcessService.java`
#### Snippet
```java
        if (sync) {
            WorkflowResult workflowResult = workflowService.start(processName, operator, processForm);
            ProcessStatus processStatus = workflowResult.getProcessInfo().getStatus();
            return processStatus == ProcessStatus.COMPLETED;
        } else {
            EXECUTOR_SERVICE.execute(() -> workflowService.start(processName, operator, processForm));
            return true;
        }
```

### DuplicatedCode
Duplicated code
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/stream/InlongStreamServiceImpl.java`
#### Snippet
```java
        List<StreamField> streamFields = getStreamFields(groupId, streamId);
        streamInfo.setFieldList(streamFields);
        // load ext infos
        List<InlongStreamExtEntity> extEntities = streamExtMapper.selectByRelatedId(groupId, streamId);
        List<InlongStreamExtInfo> extInfos = CommonBeanUtils.copyListProperties(extEntities, InlongStreamExtInfo::new);
        streamInfo.setExtList(extInfos);
```

### DuplicatedCode
Duplicated code
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/transform/StreamTransformServiceImpl.java`
#### Snippet
```java
        InlongGroupEntity groupEntity = groupMapper.selectByGroupId(request.getInlongGroupId());
        if (groupEntity == null) {
            throw new BusinessException(ErrorCodeEnum.GROUP_NOT_FOUND,
                    String.format("InlongGroup does not exist with InlongGroupId=%s", request.getInlongGroupId()));
        }
        // only the person in charges can query
        userService.checkUser(groupEntity.getInCharges(), opInfo.getName(),
                ErrorCodeEnum.GROUP_PERMISSION_DENIED.getMessage());
        // check inlong group status
        GroupStatus status = GroupStatus.forCode(groupEntity.getStatus());
        if (GroupStatus.notAllowedUpdate(status)) {
            throw new BusinessException(ErrorCodeEnum.OPT_NOT_ALLOWED_BY_STATUS,
                    String.format(ErrorCodeEnum.OPT_NOT_ALLOWED_BY_STATUS.getMessage(), status));
        }
```

### DuplicatedCode
Duplicated code
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/transform/StreamTransformServiceImpl.java`
#### Snippet
```java
        Map<Integer, List<StreamField>> fieldInfoMap = fieldEntities.stream()
                .map(transformFieldEntity -> {
                    StreamField fieldInfo = CommonBeanUtils.copyProperties(transformFieldEntity, StreamField::new);
                    fieldInfo.setFieldType(transformFieldEntity.getFieldType());
                    fieldInfo.setId(transformFieldEntity.getRankNum());
                    return Pair.of(transformFieldEntity.getTransformId(), fieldInfo);
                }).collect(Collectors.groupingBy(Pair::getLeft,
                        Collectors.mapping(Pair::getRight, Collectors.toList())));
```

### DuplicatedCode
Duplicated code
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/user/UserServiceImpl.java`
#### Snippet
```java
            if (Objects.equals(groupEntity.getCreator(), user)) {
                groupEntity.setCreator("admin");
            }
            Set<String> inChargeSet = Sets.newHashSet(groupEntity.getInCharges().split(InlongConstants.COMMA));
            inChargeSet.remove(user);
            String updateInCharge = Joiner.on(",").join(inChargeSet);
            groupEntity.setInCharges(updateInCharge);
            groupEntity.setModifier(operator);
```

### DuplicatedCode
Duplicated code
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/workflow/group/CreateGroupWorkflowDefinition.java`
#### Snippet
```java
        WorkflowProcess process = new WorkflowProcess();
        process.setName(getProcessName().name());
        process.setType(getProcessName().getDisplayName());
        process.setDisplayName(getProcessName().getDisplayName());
        process.setFormClass(GroupResourceProcessForm.class);
        process.setVersion(1);
        process.setHidden(1);

        // Set up the listener
        process.addListener(initGroupListener);
        process.addListener(initGroupCompleteListener);
        process.addListener(initGroupFailedListener);

        // Start node
        StartEvent startEvent = new StartEvent();
        process.setStartEvent(startEvent);

        // Init MQ
        ServiceTask initMQTask = new ServiceTask();
```

### DuplicatedCode
Duplicated code
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/workflow/group/DeleteGroupWorkflowDefinition.java`
#### Snippet
```java
        deleteSortTask.setServiceTaskType(ServiceTaskType.DELETE_SORT);
        deleteSortTask.setListenerFactory(groupTaskListenerFactory);
        process.addTask(deleteSortTask);

        // No need to delete the sink because we should not affect the existing data in the sink

        // End node
        EndEvent endEvent = new EndEvent();
        process.setEndEvent(endEvent);

        startEvent.addNext(deleteSourceTask);
        deleteSourceTask.addNext(deleteMQTask);
        deleteMQTask.addNext(deleteSortTask);
        deleteSortTask.addNext(endEvent);

        return process;
```

### DuplicatedCode
Duplicated code
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/workflow/stream/CreateStreamWorkflowDefinition.java`
#### Snippet
```java
        initSourceTask.setListenerFactory(streamTaskListenerFactory);
        process.addTask(initSourceTask);

        // End node
        EndEvent endEvent = new EndEvent();
        process.setEndEvent(endEvent);

        // Task dependency order: 1.MQ -> 2.Sink -> 3.Sort -> 4.Source
        // To ensure that after some tasks fail, data will not start to be collected by source or consumed by sort
        startEvent.addNext(initMQTask);
        initMQTask.addNext(initSinkTask);
        initSinkTask.addNext(initSortTask);
        initSortTask.addNext(initSourceTask);
```

### DuplicatedCode
Duplicated code
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/workflow/group/RestartGroupWorkflowDefinition.java`
#### Snippet
```java
        restartSourceTask.setServiceTaskType(ServiceTaskType.RESTART_SOURCE);
        restartSourceTask.setListenerFactory(taskListenerFactory);
        process.addTask(restartSourceTask);

        // End node
        EndEvent endEvent = new EndEvent();
        process.setEndEvent(endEvent);

        startEvent.addNext(restartSortTask);
        restartSortTask.addNext(restartSourceTask);
        restartSourceTask.addNext(endEvent);

        return process;
```

### DuplicatedCode
Duplicated code
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/workflow/group/SuspendGroupWorkflowDefinition.java`
#### Snippet
```java
        stopSortTask.setServiceTaskType(ServiceTaskType.STOP_SORT);
        stopSortTask.setListenerFactory(groupTaskListenerFactory);
        process.addTask(stopSortTask);

        // End node
        EndEvent endEvent = new EndEvent();
        process.setEndEvent(endEvent);

        startEvent.addNext(stopSourceTask);
        stopSourceTask.addNext(stopSortTask);
        stopSortTask.addNext(endEvent);

        return process;
```

### DuplicatedCode
Duplicated code
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/workflow/stream/CreateStreamWorkflowDefinition.java`
#### Snippet
```java
        WorkflowProcess process = new WorkflowProcess();
        process.setName(getProcessName().name());
        process.setType(getProcessName().getDisplayName());
        process.setDisplayName(getProcessName().getDisplayName());
        process.setFormClass(StreamResourceProcessForm.class);
        process.setVersion(1);
        process.setHidden(1);

        // Set up the listener
        process.addListener(initStreamListener);
        process.addListener(initStreamFailedListener);
        process.addListener(initStreamCompleteListener);

        // Start node
        StartEvent startEvent = new StartEvent();
        process.setStartEvent(startEvent);

        // Init MQ
        ServiceTask initMQTask = new ServiceTask();
```

### DuplicatedCode
Duplicated code
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/DefaultMessageSender.java`
#### Snippet
```java
            if (isCompressEnd) {
                attrs.append("&cp=snappy");
                return sender.syncSendMessage(new EncodeObject(body, attrs.toString(),
                        idGenerator.getNextId(), this.getMsgtype(), true, groupId),
                        msgUUID, timeout, timeUnit);
            } else {
                return sender.syncSendMessage(new EncodeObject(body, attrs.toString(),
                        idGenerator.getNextId(), this.getMsgtype(), false, groupId), msgUUID,
                        timeout, timeUnit);
            }
```

### DuplicatedCode
Duplicated code
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/DefaultMessageSender.java`
#### Snippet
```java
            if (isCompressEnd) {
                attrs.append("&cp=snappy");
                sender.asyncSendMessage(new EncodeObject(body, attrs.toString(),
                        idGenerator.getNextId(), this.getMsgtype(), true, groupId),
                        callback, msgUUID, timeout, timeUnit);
            } else {
                sender.asyncSendMessage(new EncodeObject(body, attrs.toString(), idGenerator.getNextId(),
                        this.getMsgtype(), false, groupId),
                        callback, msgUUID, timeout, timeUnit);
            }
```

### DuplicatedCode
Duplicated code
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/codec/ProtocolEncoder.java`
#### Snippet
```java
            if (object.isCompress()) {
                body = processCompress(body);
            }
            String endAttr = object.getCommonattr();
            if (object.isEncrypt()) {
                EncryptConfigEntry encryptEntry = object.getEncryptEntry();
                if (encryptEntry != null) {
                    if (Utils.isNotBlank(endAttr)) {
                        endAttr = endAttr + "&";
                    }
                    EncryptInfo encryptInfo = encryptEntry.getRsaEncryptInfo();
                    endAttr = endAttr + "_userName=" + object.getUserName()
                            + "&_encyVersion=" + encryptInfo.getVersion()
                            + "&_encyAesKey=" + encryptInfo.getRsaEncryptedKey();
                    body = EncryptUtil.aesEncrypt(body, encryptInfo.getAesKey());
                }
            }
```

### DuplicatedCode
Duplicated code
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/codec/ProtocolEncoder.java`
#### Snippet
```java
            if (Utils.isNotBlank(object.getMsgUUID())) {
                if (Utils.isNotBlank(endAttr)) {
                    endAttr = endAttr + "&";
                }
                endAttr = endAttr + "msgUUID=" + object.getMsgUUID();
            }

            int msgType = 7;
            if (object.isEncrypt()) {
                msgType |= FLAG_ALLOW_ENCRYPT;
            }
            if (object.isCompress()) {
                msgType |= FLAG_ALLOW_COMPRESS;
            }
            totalLength = totalLength + body.length + endAttr.getBytes("utf8").length;
            buf = ByteBufAllocator.DEFAULT.buffer(4 + totalLength);
            buf.writeInt(totalLength);
            buf.writeByte(msgType);
            buf.writeShort(object.getGroupIdNum());
            buf.writeShort(object.getStreamIdNum());
            String bitStr = object.isSupportLF() ? "1" : "0";
            bitStr += (object.getMessageKey().equals("minute")) ? "1" : "0";
            bitStr += (object.getMessageKey().equals("file")) ? "1" : "0";
            bitStr += !object.isGroupIdTransfer() ? "1" : "0";
            bitStr += object.isReport() ? "1" : "0";
            bitStr += "0";
            buf.writeShort(Integer.parseInt(bitStr, 2));
            buf.writeInt((int) object.getDt());

            buf.writeShort(cnt);
            buf.writeInt(Integer.valueOf(object.getMessageId()));

            buf.writeInt(body.length);
            buf.writeBytes(body);

            buf.writeShort(endAttr.getBytes("utf8").length);
            buf.writeBytes(endAttr.getBytes("utf8"));
            buf.writeShort(0xee01);
```

### DuplicatedCode
Duplicated code
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/codec/ProtocolEncoder.java`
#### Snippet
```java
int totalLength = 1 + 2 + 2 + 2 + 4 + 2 + 4 + 4 + 2 + 2;
            byte[] body = null;
            int cnt = 1;

            if (object.getBodylist() != null && object.getBodylist().size() != 0) {
                if (object.getCnt() > 0) {
                    cnt = object.getCnt();
                } else {
                    cnt = object.getBodylist().size();
                }

                ByteArrayOutputStream out = new ByteArrayOutputStream();
                Iterator<byte[]> iter = object.getBodylist().iterator();

                if (object.isSupportLF()) {
                    ByteArrayOutputStream data = new ByteArrayOutputStream();
                    int len = object.getBodylist().size();
                    for (int i = 0; i < len - 1; i++) {
                        data.write(object.getBodylist().get(i));
                        data.write("\n".getBytes("utf8"));
                    }
                    data.write(object.getBodylist().get(len - 1));
                    ByteBuffer databuffer = ByteBuffer.allocate(4);
                    databuffer.putInt(data.toByteArray().length);
                    out.write(databuffer.array());
                    out.write(data.toByteArray());
                } else {
                    while (iter.hasNext()) {
                        byte[] entry = iter.next();
                        ByteBuffer databuffer = ByteBuffer.allocate(4);
                        databuffer.putInt(entry.length);
                        out.write(databuffer.array());
                        out.write(entry);
                    }
                }
                body = out.toByteArray();
            }
            // send single message one time
            if (object.getBodyBytes() != null && object.getBodyBytes().length != 0) {
                ByteArrayOutputStream out = new ByteArrayOutputStream();

                ByteBuffer databuffer = ByteBuffer.allocate(4);
                databuffer.putInt(object.getBodyBytes().length);
                out.write(databuffer.array());
                out.write(object.getBodyBytes());
                body = out.toByteArray();
            }
```

### DuplicatedCode
Duplicated code
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/codec/ProtocolEncoder.java`
#### Snippet
```java
            if (object.getBodyBytes() != null && object.getBodyBytes().length != 0) {
                ByteArrayOutputStream out = new ByteArrayOutputStream();
                ByteBuffer byteBuffer = ByteBuffer.allocate(4);
                byteBuffer.putInt(object.getBodyBytes().length);
                out.write(byteBuffer.array());
                out.write(object.getBodyBytes());
                body = out.toByteArray();
            }
```

### DuplicatedCode
Duplicated code
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/codec/ProtocolEncoder.java`
#### Snippet
```java
                String msgAttrs = object.getAttributes();
                if (object.isCompress()) {
                    body = processCompress(body);
                }
                if (object.isEncrypt()) {
                    EncryptConfigEntry encryptEntry = object.getEncryptEntry();
                    if (encryptEntry != null) {
                        if (Utils.isNotBlank(msgAttrs)) {
                            msgAttrs = msgAttrs + "&";
                        }
                        EncryptInfo encryptInfo = encryptEntry.getRsaEncryptInfo();
                        msgAttrs = msgAttrs + "_userName=" + object.getUserName()
                                + "&_encyVersion=" + encryptInfo.getVersion()
                                + "&_encyAesKey=" + encryptInfo.getRsaEncryptedKey();
                        body = EncryptUtil.aesEncrypt(body, encryptInfo.getAesKey());
                    }
                }
                if (Utils.isNotBlank(object.getMsgUUID())) {
                    if (Utils.isNotBlank(msgAttrs)) {
                        msgAttrs = msgAttrs + "&";
                    }
                    msgAttrs = msgAttrs + "msgUUID=" + object.getMsgUUID();
                }
```

### DuplicatedCode
Duplicated code
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/codec/ProtocolEncoder.java`
#### Snippet
```java
                if (object.isEncrypt()) {
                    msgType |= FLAG_ALLOW_ENCRYPT;
                }
                totalLength = totalLength + body.length + msgAttrs.getBytes("utf8").length;
                buf = ByteBufAllocator.DEFAULT.buffer(4 + totalLength);
                buf.writeInt(totalLength);
                buf.writeByte(msgType);
                buf.writeInt(body.length);
                buf.writeBytes(body);
                buf.writeInt(msgAttrs.getBytes("utf8").length);
                buf.writeBytes(msgAttrs.getBytes("utf8"));
```

### DuplicatedCode
Duplicated code
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/codec/ProtocolEncoder.java`
#### Snippet
```java
        try {
            ByteArrayOutputStream out = new ByteArrayOutputStream();
            out.write(body);
            int guessLen = Snappy.maxCompressedLength(out.size());
            byte[] tmpData = new byte[guessLen];
            int len = Snappy.compress(out.toByteArray(), 0, out.size(),
                    tmpData, 0);
            body = new byte[len];
            System.arraycopy(tmpData, 0, body, 0, len);
        } catch (IOException e) {
            logger.error("{}", e.getMessage());
            e.printStackTrace();
        }
        return body;
```

### DuplicatedCode
Duplicated code
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/config/ProxyConfigManager.java`
#### Snippet
```java
            if (!file.getParentFile().exists()) {
                file.getParentFile().mkdir();
            }
            if (!file.exists()) {
                file.createNewFile();
            }
            fos = new FileOutputStream(file);
            p = new ObjectOutputStream(fos);
            p.writeObject(entry);
            p.flush();
```

### DuplicatedCode
Duplicated code
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/ClientMgr.java`
#### Snippet
```java
                for (Map.Entry<HostInfo, NettyClient> entry : clientMapData.entrySet()) {
                    NettyClient client = entry.getValue();
                    HostInfo hostInfo = entry.getKey();
                    if (client != null && client.getChannel() != null
                            && client.getChannel().id().equals(channel.id())) {
                        // logger.info("channel" + channel + "; Load:" + load);
                        if (!channelLoadMapData.containsKey(hostInfo)) {
                            channelLoadMapData.put(hostInfo, new int[ConfigConstants.CYCLE]);
                        }
                        if ((loadCycle - 1) >= 0) {
                            channelLoadMapData.get(hostInfo)[loadCycle - 1] = loadvalue;
                        } else {
                            return;
                        }
                        break;
                    }
                }
```

### DuplicatedCode
Duplicated code
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/ClientMgr.java`
#### Snippet
```java
        for (Map.Entry<HostInfo, int[]> entry : channelLoadMapData.entrySet()) {
            HostInfo key = entry.getKey();
            int[] value = entry.getValue();
            int numerator = 0;
            int denominator = 0;
            for (int i = 0; i < value.length; i++) {
                if (value[i] > 0) {
                    numerator = numerator + value[i] * weight[i];
                    denominator = denominator + weight[i];
                }
            }
            int sum = numerator / denominator;
            loadData.put(key, sum);
        }
```

### DuplicatedCode
Duplicated code
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/ClientMgr.java`
#### Snippet
```java
            List<Map.Entry<HostInfo, Integer>> listData = new ArrayList<>(loadData.entrySet());
            Collections.sort(listData, new Comparator<Map.Entry<HostInfo, Integer>>() {

                @Override
                public int compare(Map.Entry<HostInfo, Integer> o1, Map.Entry<HostInfo, Integer> o2) {
                    if (o2.getValue() != null && o1.getValue() != null && o1.getValue() > o2.getValue()) {
                        return -1;
                    } else {
                        return 1;
                    }
                }
            });
```

### DuplicatedCode
Duplicated code
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/ClientMgr.java`
#### Snippet
```java
            int realSize = this.realSize - clientMap.size();
            if (realSize > 0) {
                List<HostInfo> hostInfoList = new ArrayList<>(proxyInfoList);
                hostInfoList.removeAll(clientMap.keySet());
                List<HostInfo> replaceHost = getRealHosts(hostInfoList, realSize);
                for (HostInfo hostInfo : replaceHost) {
                    initConnection(hostInfo);
                }
            }
```

### DuplicatedCode
Duplicated code
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/Sender.java`
#### Snippet
```java
        if (encodeObject.getMsgtype() == 7) {
            int groupIdnum = 0;
            int streamIdnum = 0;
            if (encodeObject.getGroupId().equals(clientMgr.getGroupId())) {
                groupIdnum = clientMgr.getGroupIdNum();
                streamIdnum = clientMgr.getStreamIdMap().get(encodeObject.getStreamId()) != null
                        ? clientMgr.getStreamIdMap().get(encodeObject.getStreamId())
                        : 0;
            }
            encodeObject.setGroupIdNum(groupIdnum);
            encodeObject.setStreamIdNum(streamIdnum);
            if (groupIdnum == 0 || streamIdnum == 0) {
                encodeObject.setGroupIdTransfer(false);
            }
        }
        if (this.configure.isNeedDataEncry()) {
            encodeObject.setEncryptEntry(true, configure.getUserName(), clientMgr.getEncryptConfigEntry());
        } else {
            encodeObject.setEncryptEntry(false, null, null);
        }
        encodeObject.setMsgUUID(msgUUID);
        SyncMessageCallable callable = new SyncMessageCallable(client, encodeObject, timeout, timeUnit);
        syncCallables.put(encodeObject.getMessageId(), callable);

        Future<SendResult> future = threadPool.submit(callable);
        return future.get(timeout, timeUnit);
```

### DuplicatedCode
Duplicated code
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/Sender.java`
#### Snippet
```java
            SyncMessageCallable syncMessageCallable = syncCallables.remove(encodeObject.getMessageId());
            if (syncMessageCallable != null) {
                NettyClient tmpClient = syncMessageCallable.getClient();
                if (tmpClient != null) {
                    Channel curChannel = tmpClient.getChannel();
                    if (curChannel != null) {
                        LOGGER.error("channel maybe busy {}", curChannel);
                        scanThread.addTimeoutChannel(curChannel);
                    }
                }
            }
```

### DuplicatedCode
Duplicated code
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/Sender.java`
#### Snippet
```java
        int size = 1;
        if (isFile) {
            if (encodeObject.getBodyBytes() != null) {
                size = encodeObject.getBodyBytes().length;
            } else {
                for (byte[] bytes : encodeObject.getBodylist()) {
                    size = size + bytes.length;
                }
            }
            if (currentBufferSize.addAndGet(size) >= asyncCallbackMaxSize) {
                currentBufferSize.addAndGet(-size);
                throw new ProxysdkException("ASYNC_CALLBACK_BUFFER_FULL");
            }

        } else {
            if (currentBufferSize.incrementAndGet() >= asyncCallbackMaxSize) {
                currentBufferSize.decrementAndGet();
                throw new ProxysdkException("ASYNC_CALLBACK_BUFFER_FULL");
            }
        }
```

### DuplicatedCode
Duplicated code
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/Sender.java`
#### Snippet
```java
        if (encodeObject.getMsgtype() == 7) {
            int groupIdnum = 0;
            int streamIdnum = 0;
            if ((clientMgr.getGroupId().length() != 0) && (encodeObject.getGroupId().equals(clientMgr.getGroupId()))) {
                groupIdnum = clientMgr.getGroupIdNum();
                streamIdnum = (clientMgr.getStreamIdMap().get(encodeObject.getStreamId()) != null)
                        ? clientMgr.getStreamIdMap().get(encodeObject.getStreamId())
                        : 0;
            }
            encodeObject.setGroupIdNum(groupIdnum);
            encodeObject.setStreamIdNum(streamIdnum);
            if (groupIdnum == 0 || streamIdnum == 0) {
                encodeObject.setGroupIdTransfer(false);
            }
        }
        if (this.configure.isNeedDataEncry()) {
            encodeObject.setEncryptEntry(true, configure.getUserName(), clientMgr.getEncryptConfigEntry());
        } else {
            encodeObject.setEncryptEntry(false, null, null);
        }
        encodeObject.setMsgUUID(msgUUID);
        client.write(encodeObject);
```

### DuplicatedCode
Duplicated code
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/Utils.java`
#### Snippet
```java
        byte[] ret = new byte[4];
        try {
            String[] ipArr = ipAddr.split("\\.");
            ret[0] = (byte) (Integer.parseInt(ipArr[0]) & 0xFF);
            ret[1] = (byte) (Integer.parseInt(ipArr[1]) & 0xFF);
            ret[2] = (byte) (Integer.parseInt(ipArr[2]) & 0xFF);
            ret[3] = (byte) (Integer.parseInt(ipArr[3]) & 0xFF);
            return ret;
        } catch (Exception e) {
            throw new IllegalArgumentException(ipAddr + " is invalid IP");
        }
```

### DuplicatedCode
Duplicated code
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/Utils.java`
#### Snippet
```java
        int addr = bytes[3] & 0xFF;
        addr |= ((bytes[2] << 8) & 0xFF00);
        addr |= ((bytes[1] << 16) & 0xFF0000);
        addr |= ((bytes[0] << 24) & 0xFF000000);
        return addr;
```

### DuplicatedCode
Duplicated code
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/Utils.java`
#### Snippet
```java
        if (list == null || list.isEmpty()) {
            return "";
        }
        StringBuilder sb = new StringBuilder();
        Iterator itr = list.iterator();
        sb.append(itr.next());
        while (itr.hasNext()) {
            sb.append(ch).append(itr.next());
        }
        return sb.toString();
```

### DuplicatedCode
Duplicated code
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
        SdkEvent sdkEvent = new SdkEvent();
        sdkEvent.setInlongGroupId(groupId);
        sdkEvent.setInlongStreamId(streamId);
        sdkEvent.setBody(body);
        sdkEvent.setMsgTime(dt);
        sdkEvent.setSourceIp(localIp);
        if (extraAttrMap != null) {
            sdkEvent.setHeaders(extraAttrMap);
        }
```

### DuplicatedCode
Duplicated code
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
        SendMessageCallback callback = new SendMessageCallback() {

            @Override
            public void onMessageAck(SendResult result) {
                refResult.set(SendResult.OK);
                latch.countDown();
            }

            @Override
            public void onException(Throwable e) {
                LOG.error(e.getMessage(), e);
                refResult.set(SendResult.CONNECTION_BREAK);
                latch.countDown();
            }
        };
        CallbackProfile profile = new CallbackProfile(sdkEvent, callback);
```

### DuplicatedCode
Duplicated code
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
        try {
            boolean success = latch.await(timeout, timeUnit);
            if (!success) {
                refResult.set(SendResult.TIMEOUT);
            }
        } catch (Exception e) {
            LOG.error(e.getMessage(), e);
            refResult.set(SendResult.UNKOWN_ERROR);
        }
        return refResult.get();
```

### DuplicatedCode
Duplicated code
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/ProxySdkSink.java`
#### Snippet
```java
        Channel channel = getChannel();
        Transaction tx = channel.getTransaction();
        tx.begin();
        try {
            Event event = channel.take();
            if (event == null) {
                tx.commit();
                return Status.BACKOFF;
            }
            if (!(event instanceof ProfileEvent)) {
                tx.commit();
                this.context.addSendFailMetric();
                return Status.READY;
            }
            //
            ProfileEvent profileEvent = (ProfileEvent) event;
            this.dispatchManager.addEvent(profileEvent);
            tx.commit();
            return Status.READY;
        } catch (Throwable t) {
            LOG.error("Process event failed!" + this.getName(), t);
            try {
                tx.rollback();
            } catch (Throwable e) {
                LOG.error("Channel take transaction rollback exception:" + getName(), e);
            }
            return Status.BACKOFF;
        } finally {
            tx.close();
        }
```

### DuplicatedCode
Duplicated code
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/channel/BufferQueueChannel.java`
#### Snippet
```java
        if (globalBufferQueueSizeKb != null) {
            return globalBufferQueueSizeKb;
        }
        synchronized (LOG) {
            if (globalBufferQueueSizeKb != null) {
                return globalBufferQueueSizeKb;
            }
            int maxBufferQueueSizeKb = context.getInteger(KEY_MAX_BUFFERQUEUE_SIZE_KB, DEFAULT_MAX_BUFFERQUEUE_SIZE_KB);
            globalBufferQueueSizeKb = new SizeSemaphore(maxBufferQueueSizeKb, SizeSemaphore.ONEKB);
            return globalBufferQueueSizeKb;
        }
```

### DuplicatedCode
Duplicated code
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/config/ContextProxyClusterConfigLoader.java`
#### Snippet
```java
            GetProxyConfigBySdkResponse configResponse = JSON.parseObject(jsonString,
                    GetProxyConfigBySdkResponse.class);
            // result
            for (Entry<String, ProxyClusterResult> entry : configResponse.getData().entrySet()) {
                for (InlongStreamConfig stream : entry.getValue().getConfig().getInlongStreamList()) {
                    if (StringUtils.equals(inlongGroupId, stream.getInlongGroupId())
                            && StringUtils.equals(inlongStreamId, stream.getInlongStreamId())) {
                        return entry.getValue();
                    }
                }
            }
            return null;
```

### DuplicatedCode
Duplicated code
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/config/FileProxyClusterConfigLoader.java`
#### Snippet
```java
            for (Entry<String, ProxyClusterResult> entry : configResponse.getData().entrySet()) {
                for (InlongStreamConfig stream : entry.getValue().getConfig().getInlongStreamList()) {
                    if (StringUtils.equals(inlongGroupId, stream.getInlongGroupId())
                            && StringUtils.equals(inlongStreamId, stream.getInlongStreamId())) {
                        return entry.getValue();
                    }
                }
            }
            return null;
```

### DuplicatedCode
Duplicated code
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/config/ProxyClusterConfigHolder.java`
#### Snippet
```java
        reloadTimer = new Timer(true);
        TimerTask task = new TimerTask() {

            /**
             * run
             */
            public void run() {
                try {
                    reload();
                } catch (Throwable e) {
                    LOG.error(e.getMessage(), e);
                }
            }
        };
        reloadTimer.schedule(task, new Date(System.currentTimeMillis() + reloadInterval), reloadInterval);
```

### DuplicatedCode
Duplicated code
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/context/SdkSinkContext.java`
#### Snippet
```java
        Map<String, String> dimensions = new HashMap<>();
        dimensions.put(SdkMetricItem.KEY_NODE_ID, this.nodeId);
        dimensions.put(SdkMetricItem.KEY_NODE_IP, this.nodeIp);
        // metric
        fillInlongId(currentRecord, dimensions);
        dimensions.put(SdkMetricItem.KEY_SINK_ID, proxyClusterId);
        long msgTime = currentRecord.getDispatchTime();
        long auditFormatTime = msgTime - msgTime % auditFormatInterval;
        dimensions.put(SdkMetricItem.KEY_MESSAGE_TIME, String.valueOf(auditFormatTime));
        SdkMetricItem metricItem = this.getMetricItemSet().findMetricItem(dimensions);
        long count = currentRecord.getCount();
        long size = currentRecord.getSize();
```

### DuplicatedCode
Duplicated code
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/context/SdkSinkContext.java`
#### Snippet
```java
            if (sendTime > 0) {
                long currentTime = System.currentTimeMillis();
                long sinkDuration = currentTime - sendTime;
                long nodeDuration = currentTime - NumberUtils.toLong(Constants.HEADER_KEY_SOURCE_TIME, msgTime);
                long wholeDuration = currentTime - msgTime;
                metricItem.sinkDuration.addAndGet(sinkDuration * count);
                metricItem.nodeDuration.addAndGet(nodeDuration * count);
                metricItem.wholeDuration.addAndGet(wholeDuration * count);
            }
```

### DuplicatedCode
Duplicated code
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/dispatch/DispatchManager.java`
#### Snippet
```java
        LOG.debug("start to outputOvertimeData profileCacheSize:{},dispatchQueueSize:{}",
                profileCache.size(), dispatchQueue.size());
        long currentTime = System.currentTimeMillis();
        long createThreshold = currentTime - dispatchTimeout;
        List<String> removeKeys = new ArrayList<>();
        long eventCount = 0;
        for (Entry<String, DispatchProfile> entry : this.profileCache.entrySet()) {
            DispatchProfile dispatchProfile = entry.getValue();
            eventCount += dispatchProfile.getCount();
            if (!dispatchProfile.isTimeout(createThreshold)) {
                continue;
            }
            removeKeys.add(entry.getKey());
        }
```

### DuplicatedCode
Duplicated code
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/metrics/SdkMetricItem.java`
#### Snippet
```java
        if (result) {
            metricItem.sendSuccessCount.incrementAndGet();
            metricItem.sendSuccessSize.addAndGet(currentRecord.getBody().length);
            if (sendTime > 0) {
                long currentTime = System.currentTimeMillis();
                long sinkDuration = currentTime - sendTime;
                long nodeDuration = currentTime - NumberUtils.toLong(Constants.HEADER_KEY_SOURCE_TIME, msgTime);
                long wholeDuration = currentTime - msgTime;
                metricItem.sinkDuration.addAndGet(sinkDuration);
                metricItem.nodeDuration.addAndGet(nodeDuration);
                metricItem.wholeDuration.addAndGet(wholeDuration);
            }
        } else {
            metricItem.sendFailCount.incrementAndGet();
            metricItem.sendFailSize.addAndGet(currentRecord.getBody().length);
        }
```

### DuplicatedCode
Duplicated code
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/utils/EncryptUtil.java`
#### Snippet
```java
        try {
            BufferedReader br = new BufferedReader(new FileReader(path));
            String readLine = null;
            StringBuilder sb = new StringBuilder();
            while ((readLine = br.readLine()) != null) {
                sb.append(readLine);
            }
            br.close();
            return sb.toString();
        } catch (IOException e) {
            throw new Exception("key error");
        } catch (NullPointerException e) {
            throw new Exception("npe error");
        }
```

### DuplicatedCode
Duplicated code
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/utils/EncryptUtil.java`
#### Snippet
```java
        try {
            File file = new File(path);
            int len = 0;
            if (file.exists()) {
                len = (int) file.length();
            } else {
                // error
            }

            DataInputStream dis = new DataInputStream(new BufferedInputStream(new FileInputStream(path)));
            byte[] content = new byte[len];
            while (dis.read(content) != -1) {

            }

            dis.close();
            return content;
        } catch (IOException e) {
            throw new Exception("key error");
        } catch (NullPointerException e) {
            throw new Exception("npe error");
        }
```

### DuplicatedCode
Duplicated code
in `inlong-sdk/sdk-common/src/main/java/org/apache/inlong/sdk/commons/admin/AdminTask.java`
#### Snippet
```java
        if (this.materializedConfiguration != null) {
            LOG.info("Shutting down configuration: {}", this.materializedConfiguration);
            for (Entry<String, SourceRunner> entry : this.materializedConfiguration.getSourceRunners().entrySet()) {
                try {
                    LOG.info("Stopping Source " + entry.getKey());
                    supervisor.unsupervise(entry.getValue());
                } catch (Exception e) {
                    LOG.error("Error while stopping {}", entry.getValue(), e);
                }
            }

            for (Entry<String, SinkRunner> entry : this.materializedConfiguration.getSinkRunners().entrySet()) {
                try {
                    LOG.info("Stopping Sink " + entry.getKey());
                    supervisor.unsupervise(entry.getValue());
                } catch (Exception e) {
                    LOG.error("Error while stopping {}", entry.getValue(), e);
                }
            }

            for (Entry<String, Channel> entry : this.materializedConfiguration.getChannels().entrySet()) {
                try {
                    LOG.info("Stopping Channel " + entry.getKey());
                    supervisor.unsupervise(entry.getValue());
                } catch (Exception e) {
                    LOG.error("Error while stopping {}", entry.getValue(), e);
                }
            }
        }
```

### DuplicatedCode
Duplicated code
in `inlong-sdk/sdk-common/src/main/java/org/apache/inlong/sdk/commons/admin/AdminTask.java`
#### Snippet
```java
LOG.info("Starting new configuration:{}", materializedConfiguration);

        this.materializedConfiguration = materializedConfiguration;

        for (Entry<String, Channel> entry : materializedConfiguration.getChannels().entrySet()) {
            try {
                LOG.info("Starting Channel " + entry.getKey());
                supervisor.supervise(entry.getValue(),
                        new SupervisorPolicy.AlwaysRestartPolicy(), LifecycleState.START);
            } catch (Exception e) {
                LOG.error("Error while starting {}", entry.getValue(), e);
            }
        }

        /*
         * Wait for all channels to start.
         */
        for (Channel ch : materializedConfiguration.getChannels().values()) {
            while (ch.getLifecycleState() != LifecycleState.START
                    && !supervisor.isComponentInErrorState(ch)) {
                try {
                    LOG.info("Waiting for channel: " + ch.getName() + " to start. Sleeping for 500 ms");
                    Thread.sleep(500);
                } catch (InterruptedException e) {
                    LOG.error("Interrupted while waiting for channel to start.", e);
                }
            }
        }

        for (Entry<String, SinkRunner> entry : materializedConfiguration.getSinkRunners().entrySet()) {
            try {
                LOG.info("Starting Sink " + entry.getKey());
                supervisor.supervise(entry.getValue(),
                        new SupervisorPolicy.AlwaysRestartPolicy(), LifecycleState.START);
            } catch (Exception e) {
                LOG.error("Error while starting {}", entry.getValue(), e);
            }
        }

        for (Entry<String, SourceRunner> entry : materializedConfiguration.getSourceRunners().entrySet()) {
            try {
                LOG.info("Starting Source " + entry.getKey());
                supervisor.supervise(entry.getValue(),
                        new SupervisorPolicy.AlwaysRestartPolicy(), LifecycleState.START);
            } catch (Exception e) {
                LOG.error("Error while starting {}", entry.getValue(), e);
            }
        }
```

### DuplicatedCode
Duplicated code
in `inlong-sdk/sort-sdk/src/main/java/org/apache/inlong/sdk/sort/fetcher/kafka/AckOffsetOnRebalance.java`
#### Snippet
```java
        List<Long> removeOffsets = new ArrayList<>();
        ackOffsetMap.forEach((topicPartition, tpOffsetMap) -> {
            // get the remove list
            long commitOffset = -1;
            for (Long ackOffset : tpOffsetMap.keySet()) {
                if (!tpOffsetMap.get(ackOffset)) {
                    break;
                }
                removeOffsets.add(ackOffset);
                commitOffset = ackOffset;
            }
            // the first haven't ack, do nothing
            if (CollectionUtils.isEmpty(removeOffsets)) {
                return;
            }

            // remove offset and commit offset
            removeOffsets.forEach(tpOffsetMap::remove);
            removeOffsets.clear();
            commitOffsetMap.put(topicPartition, new OffsetAndMetadata(commitOffset));
        });
```

### DuplicatedCode
Duplicated code
in `inlong-sdk/sort-sdk/src/main/java/org/apache/inlong/sdk/sort/fetcher/kafka/KafkaMultiTopicsFetcher.java`
#### Snippet
```java
        Properties properties = new Properties();
        properties.put(ConsumerConfig.BOOTSTRAP_SERVERS_CONFIG, bootstrapServers);
        properties.put(ConsumerConfig.GROUP_ID_CONFIG, context.getConfig().getSortTaskId());
        properties.put(ConsumerConfig.KEY_DESERIALIZER_CLASS_CONFIG,
                ByteArrayDeserializer.class.getName());
        properties.put(ConsumerConfig.VALUE_DESERIALIZER_CLASS_CONFIG,
                ByteArrayDeserializer.class.getName());
        properties.put(ConsumerConfig.RECEIVE_BUFFER_CONFIG,
                context.getConfig().getKafkaSocketRecvBufferSize());
        properties.put(ConsumerConfig.ENABLE_AUTO_COMMIT_CONFIG, false);
        SortClientConfig.ConsumeStrategy offsetResetStrategy = context.getConfig().getOffsetResetStrategy();
        if (offsetResetStrategy == SortClientConfig.ConsumeStrategy.lastest
                || offsetResetStrategy == SortClientConfig.ConsumeStrategy.lastest_absolutely) {
            properties.put(ConsumerConfig.AUTO_OFFSET_RESET_CONFIG, "latest");
        } else if (offsetResetStrategy == SortClientConfig.ConsumeStrategy.earliest
                || offsetResetStrategy == SortClientConfig.ConsumeStrategy.earliest_absolutely) {
            properties.put(ConsumerConfig.AUTO_OFFSET_RESET_CONFIG, "earliest");
        } else {
            properties.put(ConsumerConfig.AUTO_OFFSET_RESET_CONFIG, "none");
        }
        properties.put(ConsumerConfig.FETCH_MAX_BYTES_CONFIG,
                context.getConfig().getKafkaFetchSizeBytes());
        properties.put(ConsumerConfig.FETCH_MAX_WAIT_MS_CONFIG,
                context.getConfig().getKafkaFetchWaitMs());
```

### DuplicatedCode
Duplicated code
in `inlong-sdk/sort-sdk/src/main/java/org/apache/inlong/sdk/sort/fetcher/kafka/KafkaMultiTopicsFetcher.java`
#### Snippet
```java
                    List<InLongMessage> inLongMessages = deserializer
                            .deserialize(context, topic, getMsgHeaders(msg.headers()), msg.value());
                    context.addConsumeSuccess(topic, msg.partition(), inLongMessages.size(), msg.value().length,
                            fetchTimeCost);
                    int originSize = inLongMessages.size();
                    inLongMessages = interceptor.intercept(inLongMessages);
                    if (inLongMessages.isEmpty()) {
                        ack(offsetKey);
                        continue;
                    }
                    int filterSize = originSize - inLongMessages.size();
                    context.addConsumeFilter(topic, msg.partition(), filterSize);
```

### DuplicatedCode
Duplicated code
in `inlong-sdk/sort-sdk/src/main/java/org/apache/inlong/sdk/sort/fetcher/pulsar/PulsarMultiTopicsFetcher.java`
#### Snippet
```java
                List<InLongMessage> inLongMessages = deserializer
                        .deserialize(context, topic, msg.getProperties(), msg.getData());
                context.addConsumeSuccess(topic, -1, inLongMessages.size(), msg.getData().length,
                        fetchTimeCost);
                int originSize = inLongMessages.size();
                // intercept
                inLongMessages = interceptor.intercept(inLongMessages);
                if (inLongMessages.isEmpty()) {
                    ack(offsetKey);
                    continue;
                }
                int filterSize = originSize - inLongMessages.size();
                context.addConsumeFilter(topic, -1, filterSize);

                List<MessageRecord> msgs = new ArrayList<>();
```

### DuplicatedCode
Duplicated code
in `inlong-sdk/sort-sdk/src/main/java/org/apache/inlong/sdk/sort/manager/InlongMultiTopicManager.java`
#### Snippet
```java
        List<TopicFetcher> fetchers = pulsarFetchers.computeIfAbsent(clusterId, k -> new ArrayList<>());
        if (CollectionUtils.isNotEmpty(fetchers)) {
            fetchers.forEach(fetcher -> fetcher.updateTopics(topics));
            return;
        }
        InLongTopic topic = topics.stream().findFirst().get();
```

### DuplicatedCode
Duplicated code
in `inlong-sdk/sort-sdk/src/main/java/org/apache/inlong/sdk/sort/manager/InlongMultiTopicManager.java`
#### Snippet
```java
            if (stopAssign) {
                logger.warn("assign is stopped");
                return;
            }
            // get sortTask conf from manager
            if (queryConsumeConfig != null) {
                long start = System.currentTimeMillis();
                context.addRequestManager();
                ConsumeConfig consumeConfig = queryConsumeConfig
                        .queryCurrentConsumeConfig(context.getConfig().getSortTaskId());
                if (consumeConfig != null) {
                    List<InLongTopic> topicSubset = context.getConfig().getConsumerSubset(consumeConfig.getTopics());
                    handleUpdatedConsumeConfig(topicSubset);
                } else {
                    logger.warn("subscribedInfo is null");
                    context.addRequestManagerFail(System.currentTimeMillis() - start);
                }
            } else {
                logger.error("subscribedMetaDataInfo is null");
            }
```

### DuplicatedCode
Duplicated code
in `inlong-sdk/sort-sdk/src/main/java/org/apache/inlong/sdk/sort/util/StringUtil.java`
#### Snippet
```java
Map<String, String> fields = new HashMap<>();

        StringBuilder stringBuilder = new StringBuilder();

        String key = "";
        String value;

        int state = STATE_KEY;

        /*
         * The state when entering escaping and quoting. When we exit escaping or quoting, we should restore this state.
         */
        int kvState = STATE_KEY;

        for (int i = 0; i < text.length(); ++i) {
            char ch = text.charAt(i);

            if (ch == kvDelimiter) {
                switch (state) {
                    case STATE_KEY:
                        key = stringBuilder.toString();
                        stringBuilder.setLength(0);
                        state = STATE_VALUE;
                        break;
                    case STATE_VALUE:
                        throw new IllegalArgumentException("Unexpected token " + ch + " at position " + i + ".");
                    case STATE_ESCAPING:
                        stringBuilder.append(ch);
                        state = kvState;
                        break;
                    case STATE_QUOTING:
                        stringBuilder.append(ch);
                        break;
                }
            } else if (ch == entryDelimiter) {
                switch (state) {
                    case STATE_KEY:
                        throw new IllegalArgumentException("Unexpected token " + ch + " at position " + i + ".");
                    case STATE_VALUE:
                        value = stringBuilder.toString();
                        fields.put(key, value);

                        stringBuilder.setLength(0);
                        state = STATE_KEY;
                        break;
                    case STATE_ESCAPING:
                        stringBuilder.append(ch);
                        state = kvState;
                        break;
                    case STATE_QUOTING:
                        stringBuilder.append(ch);
                        break;
                }
            } else if (escapeChar != null && ch == escapeChar) {
                switch (state) {
                    case STATE_KEY:
                    case STATE_VALUE:
                        kvState = state;
                        state = STATE_ESCAPING;
                        break;
                    case STATE_ESCAPING:
                        stringBuilder.append(ch);
                        state = kvState;
                        break;
                    case STATE_QUOTING:
                        stringBuilder.append(ch);
                        break;
                }
            } else if (quoteChar != null && ch == quoteChar) {
                switch (state) {
                    case STATE_KEY:
                    case STATE_VALUE:
                        kvState = state;
                        state = STATE_QUOTING;
                        break;
                    case STATE_ESCAPING:
                        stringBuilder.append(ch);
                        state = kvState;
                        break;
                    case STATE_QUOTING:
                        state = kvState;
                        break;
                }
            } else {
                stringBuilder.append(ch);
            }
        }

        switch (state) {
            case STATE_KEY:
                throw new IllegalArgumentException("Dangling key.");
            case STATE_VALUE:
                value = stringBuilder.toString();
                fields.put(key, value);
                return fields;
            case STATE_ESCAPING:
                throw new IllegalArgumentException("Not closed escaping.");
            case STATE_QUOTING:
                throw new IllegalArgumentException("Not closed quoting.");
            default:
                throw new IllegalStateException();
        }
```

### DuplicatedCode
Duplicated code
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/SinkContext.java`
#### Snippet
```java
        String inlongGroupId = currentRecord.getInlongGroupId();
        inlongGroupId = (StringUtils.isBlank(inlongGroupId)) ? "-" : inlongGroupId;
        String inlongStreamId = currentRecord.getInlongStreamId();
        inlongStreamId = (StringUtils.isBlank(inlongStreamId)) ? "-" : inlongStreamId;
        dimensions.put(SortMetricItem.KEY_INLONG_GROUP_ID, inlongGroupId);
        dimensions.put(SortMetricItem.KEY_INLONG_STREAM_ID, inlongStreamId);
```

### DuplicatedCode
Duplicated code
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/clickhouse/ClickHouseSinkContext.java`
#### Snippet
```java
        Map<String, String> dimensions = new HashMap<>();
        dimensions.put(SortMetricItem.KEY_CLUSTER_ID, this.getClusterId());
        dimensions.put(SortMetricItem.KEY_TASK_NAME, this.getTaskName());
        dimensions.put(SortMetricItem.KEY_SOURCE_ID, "-");
        dimensions.put(SortMetricItem.KEY_SOURCE_DATA_ID, "-");
        dimensions.put(SortMetricItem.KEY_SINK_ID, this.getSinkName());
        dimensions.put(SortMetricItem.KEY_SINK_DATA_ID, "-");
        dimensions.put(SortMetricItem.KEY_INLONG_GROUP_ID, currentRecord.getInlongGroupId());
        dimensions.put(SortMetricItem.KEY_INLONG_STREAM_ID, currentRecord.getInlongStreamId());
```

### DuplicatedCode
Duplicated code
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/clickhouse/ClickHouseSinkContext.java`
#### Snippet
```java
        Map<String, String> dimensions = new HashMap<>();
        dimensions.put(SortMetricItem.KEY_CLUSTER_ID, this.getClusterId());
        dimensions.put(SortMetricItem.KEY_TASK_NAME, this.getTaskName());
        dimensions.put(SortMetricItem.KEY_SOURCE_ID, "-");
        dimensions.put(SortMetricItem.KEY_SOURCE_DATA_ID, "-");
        dimensions.put(SortMetricItem.KEY_SINK_ID, this.getSinkName());
        dimensions.put(SortMetricItem.KEY_SINK_DATA_ID, errorMsg);
```

### DuplicatedCode
Duplicated code
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/clickhouse/DefaultEventHandler.java`
#### Snippet
```java
        String delimeter = idConfig.getSeparator();
        char cDelimeter = delimeter.charAt(0);
        String strContext = null;
        // for tab separator
        byte[] bodyBytes = event.getBody();
        int msgLength = event.getBody().length;
        int contentOffset = idConfig.getContentOffset();
        if (contentOffset > 0 && msgLength >= 1) {
            strContext = new String(bodyBytes, contentOffset, msgLength - contentOffset, Charset.defaultCharset());
        } else {
            strContext = new String(bodyBytes, Charset.defaultCharset());
        }
        // unescape
        List<String> columnValues = UnescapeHelper.toFiledList(strContext, cDelimeter);
```

### DuplicatedCode
Duplicated code
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/cls/ClsIdConfig.java`
#### Snippet
```java
        if (fieldList == null) {
            this.fieldList = new ArrayList<>();
            if (fieldNames != null) {
                String[] fieldNameArray = fieldNames.split("\\s+");
                this.fieldList.addAll(Arrays.asList(fieldNameArray));
            }
        }
        return fieldList;
```

### DuplicatedCode
Duplicated code
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/cls/ClsSinkContext.java`
#### Snippet
```java
        Optional.ofNullable(sinkContext.getInteger(KEY_TOTAL_SIZE_IN_BYTES))
                .ifPresent(config::setTotalSizeInBytes);
        Optional.ofNullable(sinkContext.getInteger(KEY_MAX_SEND_THREAD_COUNT))
                .ifPresent(config::setSendThreadCount);
        Optional.ofNullable(sinkContext.getInteger(KEY_MAX_BLOCK_SEC))
                .ifPresent(config::setMaxBlockMs);
        Optional.ofNullable(sinkContext.getInteger(KEY_MAX_BATCH_SIZE))
                .ifPresent(config::setBatchSizeThresholdInBytes);
        Optional.ofNullable(sinkContext.getInteger(KEY_MAX_BATCH_COUNT))
                .ifPresent(config::setBatchCountThreshold);
```

### DuplicatedCode
Duplicated code
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/cls/ClsSinkContext.java`
#### Snippet
```java
                long sinkDuration = currentTime - sendTime;
                long nodeDuration = currentTime - currentRecord.getFetchTime();
                long wholeDuration = currentTime - currentRecord.getRawLogTime();
                metricItem.sinkDuration.addAndGet(sinkDuration);
                metricItem.nodeDuration.addAndGet(nodeDuration);
                metricItem.wholeDuration.addAndGet(wholeDuration);
```

### DuplicatedCode
Duplicated code
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/cls/ClsSinkContext.java`
#### Snippet
```java
        Map<String, String> dimensions = new HashMap<>();
        dimensions.put(SortMetricItem.KEY_CLUSTER_ID, this.getClusterId());
        dimensions.put(SortMetricItem.KEY_TASK_NAME, this.getTaskName());
        // metric
        fillInlongId(currentRecord, dimensions);
        dimensions.put(SortMetricItem.KEY_SINK_ID, this.getSinkName());
        dimensions.put(SortMetricItem.KEY_SINK_DATA_ID, bid);
```

### DuplicatedCode
Duplicated code
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/elasticsearch/EsIdConfig.java`
#### Snippet
```java
        String result = indexNamePattern;
        if (result.indexOf(PATTERN_MINUTE) >= 0) {
            String strHour = FORMAT_MINUTE.get().format(dtDate);
            result = result.replaceAll(REGEX_MINUTE, strHour);
        }
        if (result.indexOf(PATTERN_HOUR) >= 0) {
            String strHour = FORMAT_HOUR.get().format(dtDate);
            result = result.replaceAll(REGEX_HOUR, strHour);
        }
        if (result.indexOf(PATTERN_DAY) >= 0) {
            String strHour = FORMAT_DAY.get().format(dtDate);
            result = result.replaceAll(REGEX_DAY, strHour);
        }
```

### DuplicatedCode
Duplicated code
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/elasticsearch/EsSinkContext.java`
#### Snippet
```java
        Map<String, String> dimensions = new HashMap<>();
        dimensions.put(SortMetricItem.KEY_CLUSTER_ID, this.getClusterId());
        dimensions.put(SortMetricItem.KEY_SINK_ID, this.getSinkName());
        long msgTime = System.currentTimeMillis();
        long auditFormatTime = msgTime - msgTime % CommonPropertiesHolder.getAuditFormatInterval();
        dimensions.put(SortMetricItem.KEY_MESSAGE_TIME, String.valueOf(auditFormatTime));
        SortMetricItem metricItem = this.getMetricItemSet().findMetricItem(dimensions);
        metricItem.readFailCount.incrementAndGet();
```

### DuplicatedCode
Duplicated code
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/kafka/DefaultEvent2KafkaRecordHandler.java`
#### Snippet
```java
        if (idConfig == null) {
            context.addSendResultMetric(event, context.getTaskName(), false, System.currentTimeMillis());
            LOG.error("Can not find the id config:{}", uid);
            return null;
        }
        String delimiter = idConfig.getSeparator();
        byte separator = (byte) delimiter.charAt(0);
        outMsg.reset();
        switch (idConfig.getDataType()) {
            case TEXT:
                currentDate.setTime(event.getRawLogTime());
                String ftime = dateFormat.format(currentDate);
                outMsg.write(ftime.getBytes());
                outMsg.write(separator);
                String extinfo = getExtInfo(event);
                outMsg.write(extinfo.getBytes());
                outMsg.write(separator);
                break;
            case PB:
            case JCE:
            case UNKNOWN:
                break;
            default:
                break;
        }
        outMsg.write(event.getBody());
        byte[] msgContent = outMsg.toByteArray();
```

### DuplicatedCode
Duplicated code
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/kafka/KafkaFederationSinkContext.java`
#### Snippet
```java
            CacheClusterConfig clusterConfig = new CacheClusterConfig();
            clusterConfig.setClusterName(this.taskName);
            clusterConfig.setParams(this.sortTaskConfig.getSinkParams());
            List<CacheClusterConfig> newClusterConfigList = new ArrayList<>();
            newClusterConfigList.add(clusterConfig);
            this.idConfigMap = newIdConfigMap;
            this.clusterConfigList = newClusterConfigList;
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/configuration/ConfigOption.java`
#### Snippet
```java
        final Stream<FallbackKey> newFallbackKeys = Arrays.stream(fallbackKeys)
                .map(FallbackKey::createFallbackKey);
        final Stream<FallbackKey> currentAlternativeKeys = Arrays.stream(this.fallbackKeys);

        // put fallback keys first so that they are prioritized
        final FallbackKey[] mergedAlternativeKeys = Stream
                .concat(newFallbackKeys, currentAlternativeKeys)
                .toArray(FallbackKey[]::new);
        return new ConfigOption<>(key, description, defaultValue, mergedAlternativeKeys);
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/extract/HudiExtractNode.java`
#### Snippet
```java
        extList.forEach(ext -> {
            String keyName = ext.get(EXTEND_ATTR_KEY_NAME);
            if (StringUtils.isNoneBlank(keyName) &&
                    keyName.startsWith(DDL_ATTR_PREFIX)) {
                String ddlKeyName = keyName.substring(DDL_ATTR_PREFIX.length());
                String ddlValue = ext.get(EXTEND_ATTR_VALUE_NAME);
                options.put(ddlKeyName, ddlValue);
            }
        });

        String path = String.format("%s/%s.db/%s", warehouse, dbName, tableName);
        options.put(HUDI_OPTION_DEFAULT_PATH, path);
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/load/MySqlLoadNode.java`
#### Snippet
```java
        Map<String, String> options = super.tableOptions();
        options.put("connector", "jdbc-inlong");
        options.put("url", url);
        options.put("username", username);
        options.put("password", password);
        options.put("table-name", tableName);
        return options;
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/load/RedisLoadNode.java`
#### Snippet
```java
        if (database != null) {
            options.put(DATABASE, String.valueOf(database));
        }
        if (maxIdle != null) {
            options.put(MAX_IDLE, String.valueOf(maxIdle));
        }
        if (maxRetries != null) {
            options.put(SINK_MAX_RETRIES, String.valueOf(maxRetries));
        }
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/base/src/main/java/org/apache/inlong/sort/base/dirty/DirtySinkHelper.java`
#### Snippet
```java
        if (pattern == null) {
            return null;
        }

        Map<String, String> paramMap = new HashMap<>(6);
        paramMap.put(SYSTEM_TIME_KEY, DATE_TIME_FORMAT.format(LocalDateTime.now()));
        paramMap.put(DIRTY_TYPE_KEY, dirtyType.format());
        paramMap.put(DIRTY_MESSAGE_KEY, dirtyMessage);
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/base/src/main/java/org/apache/inlong/sort/base/dirty/DirtySinkHelper.java`
#### Snippet
```java
        Matcher matcher = REGEX_PATTERN.matcher(pattern);
        StringBuffer sb = new StringBuffer();
        while (matcher.find()) {
            String keyText = matcher.group(1);
            String replacement = paramMap.get(keyText);
            if (replacement == null) {
                continue;
            }
            matcher.appendReplacement(sb, replacement);
        }
        matcher.appendTail(sb);
        return sb.toString();
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/base/src/main/java/org/apache/inlong/sort/base/metric/sub/SinkTableMetricData.java`
#### Snippet
```java
        if (sinkMetricData == null || schemaInfoArray == null) {
            return null;
        }
        // build sub sink metric data
        Map<String, String> labels = sinkMetricData.getLabels();
        String metricGroupLabels = labels.entrySet().stream().map(entry -> entry.getKey() + "=" + entry.getValue())
                .collect(Collectors.joining(DELIMITER));
        StringBuilder labelBuilder = new StringBuilder(metricGroupLabels);
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/base/src/main/java/org/apache/inlong/sort/base/metric/sub/SinkTableMetricData.java`
#### Snippet
```java
        String identify = buildSchemaIdentify(database, schema, table);
        SinkMetricData subSinkMetricData;
        if (subSinkMetricMap.containsKey(identify)) {
            subSinkMetricData = subSinkMetricMap.get(identify);
        } else {
            subSinkMetricData = buildSubSinkMetricData(new String[]{database, schema, table}, this);
            subSinkMetricMap.put(identify, subSinkMetricData);
        }
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/base/src/main/java/org/apache/inlong/sort/base/metric/sub/SinkTableMetricData.java`
#### Snippet
```java
        String identify = buildSchemaIdentify(database, null, table);
        SinkMetricData subSinkMetricData;
        if (subSinkMetricMap.containsKey(identify)) {
            subSinkMetricData = subSinkMetricMap.get(identify);
        } else {
            subSinkMetricData = buildSubSinkMetricData(new String[]{database, table}, this);
            subSinkMetricMap.put(identify, subSinkMetricData);
        }
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/base/src/main/java/org/apache/inlong/sort/base/metric/sub/SinkTableMetricData.java`
#### Snippet
```java
        if (StringUtils.isBlank(database) || StringUtils.isBlank(table)) {
            invokeDirty(rowCount, rowSize);
            return;
        }
        String identify = buildSchemaIdentify(database, null, table);
        SinkMetricData subSinkMetricData;
        if (subSinkMetricMap.containsKey(identify)) {
            subSinkMetricData = subSinkMetricMap.get(identify);
        } else {
            subSinkMetricData = buildSubSinkMetricData(new String[]{database, table}, this);
            subSinkMetricMap.put(identify, subSinkMetricData);
        }
        // sink metric and sub sink metric output metrics
        this.invokeDirty(rowCount, rowSize);
        subSinkMetricData.invokeDirty(rowCount, rowSize);
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/config/JdbcSourceConfigFactory.java`
#### Snippet
```java
        switch (startupOptions.startupMode) {
            case INITIAL:
            case LATEST_OFFSET:
                break;
            default:
                throw new UnsupportedOperationException(
                        "Unsupported startup mode: " + startupOptions.startupMode);
        }
        this.startupOptions = startupOptions;
        return this;
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/debezium/history/FlinkJsonTableChangeSerializer.java`
#### Snippet
```java
        Document document = Document.create();

        document.set("defaultCharsetName", table.defaultCharsetName());
        document.set("primaryKeyColumnNames", Array.create(table.primaryKeyColumnNames()));

        List<Document> columns =
                table.columns().stream().map(this::toDocument).collect(Collectors.toList());

        document.setArray("columns", Array.create(columns));

        return document;
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/debezium/history/FlinkJsonTableChangeSerializer.java`
#### Snippet
```java
        Document document = Document.create();

        document.setString("name", column.name());
        document.setNumber("jdbcType", column.jdbcType());

        if (column.nativeType() != Column.UNSET_INT_VALUE) {
            document.setNumber("nativeType", column.nativeType());
        }

        document.setString("typeName", column.typeName());
        document.setString("typeExpression", column.typeExpression());
        document.setString("charsetName", column.charsetName());

        if (column.length() != Column.UNSET_INT_VALUE) {
            document.setNumber("length", column.length());
        }

        column.scale().ifPresent(s -> document.setNumber("scale", s));

        document.setNumber("position", column.position());
        document.setBoolean("optional", column.isOptional());
        document.setBoolean("autoIncremented", column.isAutoIncremented());
        document.setBoolean("generated", column.isGenerated());

        // BEGIN FLINK MODIFICATION
        document.setArray("enumValues", column.enumValues().toArray());
        // END FLINK MODIFICATION

        return document;
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/debezium/history/FlinkJsonTableChangeSerializer.java`
#### Snippet
```java
        TableChanges tableChanges = new TableChanges();

        for (Entry entry : array) {
            TableChange change =
                    fromDocument(entry.getValue().asDocument(), useCatalogBeforeSchema);

            if (change.getType() == TableChangeType.CREATE) {
                tableChanges.create(change.getTable());
            } else if (change.getType() == TableChangeType.ALTER) {
                tableChanges.alter(change.getTable());
            } else if (change.getType() == TableChangeType.DROP) {
                tableChanges.drop(change.getTable());
            }
        }

        return tableChanges;
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/debezium/history/FlinkJsonTableChangeSerializer.java`
#### Snippet
```java
TableEditor editor =
                Table.editor()
                        .tableId(id)
                        .setDefaultCharsetName(document.getString("defaultCharsetName"));

        document.getArray("columns")
                .streamValues()
                .map(Value::asDocument)
                .map(
                        v -> {
                            ColumnEditor columnEditor =
                                    Column.editor()
                                            .name(v.getString("name"))
                                            .jdbcType(v.getInteger("jdbcType"));

                            Integer nativeType = v.getInteger("nativeType");
                            if (nativeType != null) {
                                columnEditor.nativeType(nativeType);
                            }

                            columnEditor
                                    .type(v.getString("typeName"), v.getString("typeExpression"))
                                    .charsetName(v.getString("charsetName"));

                            Integer length = v.getInteger("length");
                            if (length != null) {
                                columnEditor.length(length);
                            }

                            Integer scale = v.getInteger("scale");
                            if (scale != null) {
                                columnEditor.scale(scale);
                            }

                            columnEditor
                                    .position(v.getInteger("position"))
                                    .optional(v.getBoolean("optional"))
                                    .autoIncremented(v.getBoolean("autoIncremented"))
                                    .generated(v.getBoolean("generated"));

                            // BEGIN FLINK MODIFICATION
                            Array enumValues = v.getArray("enumValues");
                            if (enumValues != null && !enumValues.isEmpty()) {
                                columnEditor.enumValues(
                                        enumValues
                                                .streamValues()
                                                .map(Value::asString)
                                                .collect(Collectors.toList()));
                            }
                            // END FLINK MODIFICATION

                            return columnEditor.create();
                        })
                .forEach(editor::addColumn);

        editor.setPrimaryKeyNames(
                document.getArray("primaryKeyColumnNames")
                        .streamValues()
                        .map(Value::asString)
                        .collect(Collectors.toList()));

        return editor.create();
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/debezium/history/FlinkJsonTableChangeSerializer.java`
#### Snippet
```java
        TableChangeType type = TableChangeType.valueOf(document.getString("type"));
        TableId id = TableId.parse(document.getString("id"), useCatalogBeforeSchema);
        Table table = null;

        if (type == TableChangeType.CREATE || type == TableChangeType.ALTER) {
            table = fromDocument(id, document.getDocument("table"));
        } else {
            table = Table.editor().tableId(id).create();
        }
        return new TableChange(type, table);
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/debezium/internal/FlinkDatabaseSchemaHistory.java`
#### Snippet
```java
        this.instanceName = config.getString(DATABASE_HISTORY_INSTANCE_NAME);
        this.listener = listener;
        this.storeOnlyMonitoredTablesDdl = config.getBoolean(STORE_ONLY_MONITORED_TABLES_DDL);
        this.skipUnparseableDDL = config.getBoolean(SKIP_UNPARSEABLE_DDL_STATEMENTS);
        this.useCatalogBeforeSchema = useCatalogBeforeSchema;

        // recover
        latestTables = new ConcurrentHashMap<>(6);
        for (SchemaRecord schemaRecord : DatabaseHistoryUtil.retrieveHistory(instanceName)) {
            // validate here
            TableChange tableChange =
                    FlinkJsonTableChangeSerializer.fromDocument(
                            schemaRecord.toDocument(), useCatalogBeforeSchema);
            latestTables.put(tableChange.getId(), schemaRecord);
        }
        // register
        DatabaseHistoryUtil.registerHistory(instanceName, latestTables.values());
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/debezium/internal/FlinkDatabaseSchemaHistory.java`
#### Snippet
```java
        for (TableChange change : changes) {
            switch (change.getType()) {
                case CREATE:
                case ALTER:
                    latestTables.put(
                            change.getId(),
                            new SchemaRecord(tableChangesSerializer.toDocument(change)));
                    break;
                case DROP:
                    latestTables.remove(change.getId());
                    break;
                default:
                    // impossible
                    throw new RuntimeException(
                            String.format("Unknown change type: %s.", change.getType()));
            }
        }
        listener.onChangeApplied(
                new HistoryRecord(source, position, databaseName, schemaName, ddl, changes));
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/debezium/internal/FlinkOffsetBackingStore.java`
#### Snippet
```java
        if (executor != null) {
            executor.shutdown();
            // Best effort wait for any get() and set() tasks (and caller's callbacks) to complete.
            try {
                executor.awaitTermination(30, TimeUnit.SECONDS);
            } catch (InterruptedException e) {
                Thread.currentThread().interrupt();
            }
            if (!executor.shutdownNow().isEmpty()) {
                throw new ConnectException(
                        "Failed to stop FlinkOffsetBackingStore. Exiting without cleanly "
                                + "shutting down pending tasks and/or callbacks.");
            }
            executor = null;
        }
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/debezium/internal/FlinkOffsetBackingStore.java`
#### Snippet
```java
        return executor.submit(
                () -> {
                    for (Map.Entry<ByteBuffer, ByteBuffer> entry : values.entrySet()) {
                        data.put(entry.getKey(), entry.getValue());
                    }
                    if (callback != null) {
                        callback.onCompletion(null, null);
                    }
                    return null;
                });
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/debezium/internal/Handover.java`
#### Snippet
```java
        synchronized (lock) {
            while (next == null && error == null) {
                lock.wait();
            }
            List<ChangeEvent<SourceRecord, SourceRecord>> n = next;
            if (n != null) {
                next = null;
                lock.notifyAll();
                return n;
            } else {
                ExceptionUtils.rethrowException(error, error.getMessage());

                // this statement cannot be reached since the above method always throws an
                // exception this is only here to silence the compiler and any warnings
                return Collections.emptyList();
            }
        }
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/debezium/internal/Handover.java`
#### Snippet
```java
        checkNotNull(element);

        synchronized (lock) {
            while (next != null && !wakeupProducer) {
                lock.wait();
            }

            wakeupProducer = false;

            // an error marks this as closed for the producer
            if (error != null) {
                ExceptionUtils.rethrow(error, error.getMessage());
            } else {
                // if there is no error, then this is open and can accept this element
                next = element;
                lock.notifyAll();
            }
        }
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/debezium/table/AppendMetadataCollector.java`
#### Snippet
```java
        for (int i = 0; i < metadataConverters.length; i++) {
            Object meta = metadataConverters[i].read(inputRecord, tableSchema, physicalRow);
            metaRow.setField(i, meta);
        }
        if (migrateAll) {
            // all data are put into meta row, set physicalRow to empty
            physicalRow = new GenericRowData(0);
        }
        RowData outRow = new JoinedRowData(physicalRow.getRowKind(), physicalRow, metaRow);
        outputCollector.collect(outRow);
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/debezium/table/RowDataDebeziumDeserializeSchema.java`
#### Snippet
```java
            @Override
            public Object convert(Object dbzObj, Schema schema) {
                if (dbzObj instanceof Boolean) {
                    return dbzObj;
                } else if (dbzObj instanceof Byte) {
                    return (byte) dbzObj == 1;
                } else if (dbzObj instanceof Short) {
                    return (short) dbzObj == 1;
                } else {
                    return Boolean.parseBoolean(dbzObj.toString());
                }
            }
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/debezium/table/RowDataDebeziumDeserializeSchema.java`
#### Snippet
```java
                    switch (schema.name()) {
                        case MicroTime.SCHEMA_NAME:
                            return (int) ((long) dbzObj / 1000);
                        case NanoTime.SCHEMA_NAME:
                            return (int) ((long) dbzObj / 1000_000);
                        default:
                            break;
                    }
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/debezium/table/RowDataDebeziumDeserializeSchema.java`
#### Snippet
```java
                if (dbzObj instanceof Long) {
                    switch (schema.name()) {
                        case Timestamp.SCHEMA_NAME:
                            return TimestampData.fromEpochMillis((Long) dbzObj);
                        case MicroTimestamp.SCHEMA_NAME:
                            long micro = (long) dbzObj;
                            return TimestampData.fromEpochMillis(
                                    micro / 1000, (int) (micro % 1000 * 1000));
                        case NanoTimestamp.SCHEMA_NAME:
                            long nano = (long) dbzObj;
                            return TimestampData.fromEpochMillis(
                                    nano / 1000_000, (int) (nano % 1000_000));
                        default:
                            break;
                    }
                }
                LocalDateTime localDateTime =
                        TemporalConversions.toLocalDateTime(dbzObj, serverTimeZone);
                return TimestampData.fromLocalDateTime(localDateTime);
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/debezium/table/RowDataDebeziumDeserializeSchema.java`
#### Snippet
```java
            @Override
            public Object convert(Object dbzObj, Schema schema) {
                if (dbzObj instanceof String) {
                    String str = (String) dbzObj;
                    // TIMESTAMP_LTZ type is encoded in string type
                    Instant instant = Instant.parse(str);
                    return TimestampData.fromLocalDateTime(
                            LocalDateTime.ofInstant(instant, serverTimeZone));
                }
                throw new IllegalArgumentException(
                        "Unable to convert to TimestampData from unexpected value '"
                                + dbzObj
                                + "' of type "
                                + dbzObj.getClass().getName());
            }
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/debezium/table/RowDataDebeziumDeserializeSchema.java`
#### Snippet
```java
            @Override
            public Object convert(Object dbzObj, Schema schema) {
                if (dbzObj instanceof byte[]) {
                    return dbzObj;
                } else if (dbzObj instanceof ByteBuffer) {
                    ByteBuffer byteBuffer = (ByteBuffer) dbzObj;
                    byte[] bytes = new byte[byteBuffer.remaining()];
                    byteBuffer.get(bytes);
                    return bytes;
                } else {
                    throw new UnsupportedOperationException(
                            "Unsupported BYTES value type: " + dbzObj.getClass().getSimpleName());
                }
            }
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/debezium/table/RowDataDebeziumDeserializeSchema.java`
#### Snippet
```java
                BigDecimal bigDecimal;
                if (dbzObj instanceof byte[]) {
                    // decimal.handling.mode=precise
                    bigDecimal = Decimal.toLogical(schema, (byte[]) dbzObj);
                } else if (dbzObj instanceof String) {
                    // decimal.handling.mode=string
                    bigDecimal = new BigDecimal((String) dbzObj);
                } else if (dbzObj instanceof Double) {
                    // decimal.handling.mode=double
                    bigDecimal = BigDecimal.valueOf((Double) dbzObj);
                } else {
                    if (VariableScaleDecimal.LOGICAL_NAME.equals(schema.name())) {
                        SpecialValueDecimal decimal =
                                VariableScaleDecimal.toLogical((Struct) dbzObj);
                        bigDecimal = decimal.getDecimalValue().orElse(BigDecimal.ZERO);
                    } else {
                        // fallback to string
                        bigDecimal = new BigDecimal(dbzObj.toString());
                    }
                }
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/debezium/table/RowDataDebeziumDeserializeSchema.java`
#### Snippet
```java
                @Override
                public Object convert(Object dbzObj, Schema schema) throws Exception {
                    Struct struct = (Struct) dbzObj;
                    int arity = fieldNames.length;
                    GenericRowData row = new GenericRowData(arity);
                    for (int i = 0; i < arity; i++) {
                        String fieldName = fieldNames[i];
                        Field field = schema.field(fieldName);
                        if (field == null) {
                            row.setField(i, null);
                        } else {
                            Object fieldValue = struct.getWithoutDefault(fieldName);
                            Schema fieldSchema = schema.field(fieldName).schema();
                            Object convertedField =
                                    convertField(fieldConverters[i], fieldValue, fieldSchema);
                            row.setField(i, convertedField);
                        }
                    }
                    return row;
                }
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/debezium/table/RowDataDebeziumDeserializeSchema.java`
#### Snippet
```java
                        String fieldName = field.name();
                        Object fieldValue = struct.getWithoutDefault(fieldName);
                        Schema fieldSchema = schema.field(fieldName).schema();
                        String schemaName = fieldSchema.name();
                        if (schemaName != null) {
                            fieldValue = getValueWithSchema(fieldValue, schemaName);
                        }
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/debezium/table/RowDataDebeziumDeserializeSchema.java`
#### Snippet
```java
        if (appendSource) {
            physicalRow.setRowKind(RowKind.INSERT);
        }
        if (!hasMetadata) {
            collector.collect(physicalRow);
            return;
        }

        appendMetadataCollector.inputRecord = inRecord;
        appendMetadataCollector.outputCollector = collector;
        appendMetadataCollector.collect(physicalRow, tableChange);
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/relational/JdbcSourceEventDispatcher.java`
#### Snippet
```java
        boolean anyNonfilteredEvent = false;
        if (dataCollectionIds == null || dataCollectionIds.isEmpty()) {
            anyNonfilteredEvent = true;
        } else {
            for (TableId dataCollectionId : dataCollectionIds) {
                if (filter.isIncluded(dataCollectionId)) {
                    anyNonfilteredEvent = true;
                    break;
                }
            }
        }
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/relational/JdbcSourceEventDispatcher.java`
#### Snippet
```java
                String fileName = sourceInfo.getString(BINLOG_FILENAME_OFFSET_KEY);
                Long pos = sourceInfo.getInt64(BINLOG_POSITION_OFFSET_KEY);
                Long serverId = sourceInfo.getInt64(SERVER_ID_KEY);
                source.put(SERVER_ID_KEY, serverId);
                source.put(BINLOG_FILENAME_OFFSET_KEY, fileName);
                source.put(BINLOG_POSITION_OFFSET_KEY, pos);
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/relational/JdbcSourceEventDispatcher.java`
#### Snippet
```java
            HistoryRecord historyRecord =
                    new HistoryRecord(
                            source,
                            event.getOffset(),
                            event.getDatabase(),
                            null,
                            event.getDdl(),
                            event.getTableChanges());
            String historyStr = DOCUMENT_WRITER.write(historyRecord.document());

            Struct value = new Struct(schemaChangeValueSchema);
            value.put(HistoryRecord.Fields.SOURCE, event.getSource());
            value.put(HISTORY_RECORD_FIELD, historyStr);
            return value;
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/relational/JdbcSourceEventDispatcher.java`
#### Snippet
```java
            historizedSchema.applySchemaChange(event);
            if (connectorConfig.isSchemaChangesHistoryEnabled()) {
                try {
                    final String topicName = topicSelector.getPrimaryTopic();
                    final Integer partition = 0;
                    final Struct key = schemaChangeRecordKey(event);
                    final Struct value = schemaChangeRecordValue(event);
                    final SourceRecord record =
                            new SourceRecord(
                                    event.getPartition(),
                                    event.getOffset(),
                                    topicName,
                                    partition,
                                    schemaChangeKeySchema,
                                    key,
                                    schemaChangeValueSchema,
                                    value);
                    queue.enqueue(new DataChangeEvent(record));
                } catch (IOException e) {
                    throw new IllegalStateException(
                            String.format("dispatch schema change event %s error ", event), e);
                }
            }
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/relational/connection/JdbcConnectionFactory.java`
#### Snippet
```java
        int i = 0;
        while (i < connectRetryTimes) {
            try {
                return dataSource.getConnection();
            } catch (SQLException e) {
                if (i < connectRetryTimes - 1) {
                    try {
                        Thread.sleep(300);
                    } catch (InterruptedException ie) {
                        throw new FlinkRuntimeException(
                                "Failed to get connection, interrupted while doing another attempt",
                                ie);
                    }
                    LOG.warn("Get connection failed, retry times {}", i + 1);
                } else {
                    LOG.error("Get connection failed after retry {} times", i + 1);
                    throw new FlinkRuntimeException(e);
                }
            }
            i++;
        }
        return dataSource.getConnection();
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/source/assigner/state/PendingSplitsStateSerializer.java`
#### Snippet
```java
        switch (version) {
            case 1:
            case 2:
                return deserializeLegacyPendingSplitsState(serialized);
            case 3:
            case 4:
                return deserializePendingSplitsState(version, serialized);
            default:
                throw new IOException("Unknown version: " + version);
        }
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/source/assigner/state/PendingSplitsStateSerializer.java`
#### Snippet
```java
        final List<SchemalessSnapshotSplit> remainingSchemalessSplits = new ArrayList<>();
        final Map<String, SchemalessSnapshotSplit> assignedSchemalessSnapshotSplits =
                new HashMap<>();
        final Map<TableId, TableChanges.TableChange> tableSchemas = new HashMap<>();
        remainingSplits.forEach(
                split -> {
                    tableSchemas.putAll(split.getTableSchemas());
                    remainingSchemalessSplits.add(split.toSchemalessSnapshotSplit());
                });
        assignedSnapshotSplits
                .entrySet()
                .forEach(
                        entry -> {
                            tableSchemas.putAll(entry.getValue().getTableSchemas());
                            assignedSchemalessSnapshotSplits.put(
                                    entry.getKey(), entry.getValue().toSchemalessSnapshotSplit());
                        });
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/source/assigner/state/PendingSplitsStateSerializer.java`
#### Snippet
```java
        List<TableId> tableIds = new ArrayList<>();
        final int size = in.readInt();
        for (int i = 0; i < size; i++) {
            String tableIdStr = in.readUTF();
            tableIds.add(TableId.parse(tableIdStr));
        }
        return tableIds;
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/source/enumerator/IncrementalSourceEnumerator.java`
#### Snippet
```java
        if (finishedSnapshotSplitMeta == null) {
            final List<FinishedSnapshotSplitInfo> finishedSnapshotSplitInfos =
                    splitAssigner.getFinishedSplitInfos();
            if (finishedSnapshotSplitInfos.isEmpty()) {
                LOG.error(
                        "The assigner offer empty finished split information, this should not happen");
                throw new FlinkRuntimeException(
                        "The assigner offer empty finished split information, this should not happen");
            }
            finishedSnapshotSplitMeta =
                    Lists.partition(
                            finishedSnapshotSplitInfos, sourceConfig.getSplitMetaGroupSize());
        }
        final int requestMetaGroupId = requestEvent.getRequestMetaGroupId();
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/source/meta/offset/Offset.java`
#### Snippet
```java
        Object obj = values.get(key);
        if (obj == null) {
            return 0L;
        }
        if (obj instanceof Number) {
            return ((Number) obj).longValue();
        }
        try {
            return Long.parseLong(obj.toString());
        } catch (NumberFormatException e) {
            throw new ConnectException(
                    "Source offset '"
                            + key
                            + "' parameter value "
                            + obj
                            + " could not be converted to a long");
        }
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/source/meta/split/SourceSplitSerializer.java`
#### Snippet
```java
            if (snapshotSplit.serializedFormCache != null) {
                return snapshotSplit.serializedFormCache;
            }

            final DataOutputSerializer out = SERIALIZER_CACHE.get();
            out.writeInt(SNAPSHOT_SPLIT_FLAG);
            out.writeUTF(snapshotSplit.getTableId().toString());
            out.writeUTF(snapshotSplit.splitId());
            out.writeUTF(snapshotSplit.getSplitKeyType().asSerializableString());

            final Object[] splitStart = snapshotSplit.getSplitStart();
            final Object[] splitEnd = snapshotSplit.getSplitEnd();
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/source/meta/split/SourceSplitSerializer.java`
#### Snippet
```java
            final DataOutputSerializer out = SERIALIZER_CACHE.get();
            out.writeInt(METRIC_SPLIT_FLAG);
            out.writeLong(metricSplit.getNumBytesIn());
            out.writeLong(metricSplit.getNumRecordsIn());
            writeReadPhaseMetric(metricSplit.getReadPhaseMetricMap(), out);
            writeTableMetrics(metricSplit.getTableMetricMap(), out);
            final byte[] result = out.getCopyOfBuffer();
            out.clear();
            return result;
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/source/meta/split/SourceSplitSerializer.java`
#### Snippet
```java
        FlinkJsonTableChangeSerializer jsonSerializer = new FlinkJsonTableChangeSerializer();
        DocumentWriter documentWriter = DocumentWriter.defaultWriter();
        final int size = tableSchemas.size();
        out.writeInt(size);
        for (Map.Entry<TableId, TableChange> entry : tableSchemas.entrySet()) {
            out.writeUTF(entry.getKey().toString());
            final String tableChangeStr =
                    documentWriter.write(jsonSerializer.toDocument(entry.getValue()));
            final byte[] tableChangeBytes = tableChangeStr.getBytes(StandardCharsets.UTF_8);
            out.writeInt(tableChangeBytes.length);
            out.write(tableChangeBytes);
        }
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/source/meta/split/SourceSplitSerializer.java`
#### Snippet
```java
        Map<String, Long> readPhaseMetrics = new HashMap<>();
        if (in.available() > 0) {
            final int size = in.readInt();
            for (int i = 0; i < size; i++) {
                readPhaseMetrics.put(in.readUTF(), in.readLong());
            }
        }
        return readPhaseMetrics;
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/source/metrics/SourceReaderMetrics.java`
#### Snippet
```java
        if (sourceTableMetricData != null) {
            // node level metric data
            sourceTableMetricData.getNumBytesIn().inc(rowDataSize);
            sourceTableMetricData.getNumRecordsIn().inc(rowCountSize);

            // register read phase metric data and table level metric data
            if (readPhaseMetricMap != null && tableMetricMap != null) {
                MetricState metricState = new MetricState();
                metricState.setMetrics(readPhaseMetricMap);
                Map<String, MetricState> subMetricStateMap = new HashMap<>();
                tableMetricMap.entrySet().stream().filter(v -> v.getValue() != null).forEach(entry -> {
                    MetricState subMetricState = new MetricState();
                    subMetricState.setMetrics(ImmutableMap
                            .of(NUM_RECORDS_IN, entry.getValue().getNumRecordsIn(), NUM_BYTES_IN,
                                    entry.getValue().getNumBytesIn()));
                    subMetricStateMap.put(entry.getKey(), subMetricState);
                });
                metricState.setSubMetricStateMap(subMetricStateMap);
                sourceTableMetricData.registerSubMetricsGroup(metricState);
            }
        }
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/source/reader/IncrementalSourceRecordEmitter.java`
#### Snippet
```java
            HistoryRecord historyRecord = getHistoryRecord(element);
            Array tableChanges =
                    historyRecord.document().getArray(HistoryRecord.Fields.TABLE_CHANGES);
            TableChanges changes = TABLE_CHANGE_SERIALIZER.deserialize(tableChanges, true);
            for (TableChanges.TableChange tableChange : changes) {
                splitState.asStreamSplitState().recordSchema(tableChange.getId(), tableChange);
            }
            if (includeSchemaChanges) {
                emitElement(element, output);
            }
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/source/reader/external/JdbcSourceFetchTaskContext.java`
#### Snippet
```java
        return snapshotRecords.stream()
                .map(
                        record -> {
                            Envelope envelope = Envelope.fromSchema(record.valueSchema());
                            Struct value = (Struct) record.value();
                            Struct updateAfter = value.getStruct(Envelope.FieldName.AFTER);
                            // set message timestamp (source.ts_ms) to 0L
                            Struct source = value.getStruct(Envelope.FieldName.SOURCE);
                            source.put(Envelope.FieldName.TIMESTAMP, 0L);
                            // extend the fetch timestamp(ts_ms)
                            Instant fetchTs =
                                    Instant.ofEpochMilli(
                                            value.getInt64(Envelope.FieldName.TIMESTAMP));
                            SourceRecord sourceRecord =
                                    new SourceRecord(
                                            record.sourcePartition(),
                                            record.sourceOffset(),
                                            record.topic(),
                                            record.kafkaPartition(),
                                            record.keySchema(),
                                            record.key(),
                                            record.valueSchema(),
                                            envelope.read(updateAfter, source, fetchTs));
                            return sourceRecord;
                        })
                .collect(Collectors.toList());
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/util/RecordUtils.java`
#### Snippet
```java
        Struct value = (Struct) dataRecord.value();
        Struct source = value.getStruct(Envelope.FieldName.SOURCE);
        String dbName = source.getString(AbstractSourceInfo.DATABASE_NAME_KEY);
        String schemaName = source.getString(AbstractSourceInfo.SCHEMA_NAME_KEY);
        String tableName = source.getString(AbstractSourceInfo.TABLE_NAME_KEY);
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/doris/src/main/java/org/apache/inlong/sort/doris/table/DorisDynamicSchemaOutputFormat.java`
#### Snippet
```java
        MetricOption metricOption = MetricOption.builder()
                .withInlongLabels(inlongMetric)
                .withAuditAddress(auditHostAndPorts)
                .withInitRecords(metricState != null ? metricState.getMetricValue(NUM_RECORDS_OUT) : 0L)
                .withInitBytes(metricState != null ? metricState.getMetricValue(NUM_BYTES_OUT) : 0L)
                .withInitDirtyRecords(metricState != null ? metricState.getMetricValue(DIRTY_RECORDS_OUT) : 0L)
                .withInitDirtyBytes(metricState != null ? metricState.getMetricValue(DIRTY_BYTES_OUT) : 0L)
                .withRegisterMetric(MetricOption.RegisteredMetric.ALL)
                .build();
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/doris/src/main/java/org/apache/inlong/sort/doris/table/DorisDynamicSchemaOutputFormat.java`
#### Snippet
```java
        if (this.inlongMetric != null) {
            this.metricStateListState = context.getOperatorStateStore().getUnionListState(
                    new ListStateDescriptor<>(
                            INLONG_METRIC_STATE_NAME, TypeInformation.of(new TypeHint<MetricState>() {
                            })));
        }
        if (context.isRestored()) {
            metricState = MetricStateUtils.restoreMetricState(metricStateListState,
                    getRuntimeContext().getIndexOfThisSubtask(), getRuntimeContext().getNumberOfParallelSubtasks());
        }
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/doris/src/main/java/org/apache/inlong/sort/doris/table/DorisDynamicTableFactory.java`
#### Snippet
```java
        if (multipleSink) {
            if (StringUtils.isBlank(databasePattern)) {
                throw new ValidationException(
                        "The option 'sink.multiple.database-pattern'"
                                + " is not allowed blank when the option 'sink.multiple.enable' is 'true'");
            }
            if (StringUtils.isBlank(tablePattern)) {
                throw new ValidationException(
                        "The option 'sink.multiple.table-pattern' "
                                + "is not allowed blank when the option 'sink.multiple.enable' is 'true'");
            }
            if (StringUtils.isBlank(sinkMultipleFormat)) {
                throw new ValidationException(
                        "The option 'sink.multiple.format' "
                                + "is not allowed blank when the option 'sink.multiple.enable' is 'true'");
            }
            DynamicSchemaFormatFactory.getFormat(sinkMultipleFormat);
            Set<String> supportFormats = DynamicSchemaFormatFactory.SUPPORT_FORMATS.keySet();
            if (!supportFormats.contains(sinkMultipleFormat)) {
                throw new ValidationException(String.format(
                        "Unsupported value '%s' for '%s'. "
                                + "Supported values are %s.",
                        sinkMultipleFormat, SINK_MULTIPLE_FORMAT.key(), supportFormats));
            }
            if (physicalDataType.getLogicalType() instanceof VarBinaryType) {
                throw new ValidationException(
                        "Only supports 'BYTES' or 'VARBINARY(n)' of PhysicalDataType "
                                + "when the option 'sink.multiple.enable' is 'true'");
            }
        }
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/elasticsearch-6/src/main/java/org/apache/inlong/sort/elasticsearch6/Elasticsearch6ApiCallBridge.java`
#### Snippet
```java
        BackoffPolicy backoffPolicy;
        if (flushBackoffPolicy != null) {
            switch (flushBackoffPolicy.getBackoffType()) {
                case CONSTANT:
                    backoffPolicy =
                            BackoffPolicy.constantBackoff(
                                    new TimeValue(flushBackoffPolicy.getDelayMillis()),
                                    flushBackoffPolicy.getMaxRetryCount());
                    break;
                case EXPONENTIAL:
                default:
                    backoffPolicy =
                            BackoffPolicy.exponentialBackoff(
                                    new TimeValue(flushBackoffPolicy.getDelayMillis()),
                                    flushBackoffPolicy.getMaxRetryCount());
            }
        } else {
            backoffPolicy = BackoffPolicy.noBackoff();
        }

        builder.setBackoffPolicy(backoffPolicy);
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/elasticsearch-6/src/main/java/org/apache/inlong/sort/elasticsearch6/Elasticsearch6ApiCallBridge.java`
#### Snippet
```java
        builder.setConcurrentRequests(options.getConcurrentRequests());
        if (options.getFlushMaxActions() != null) {
            builder.setBulkActions(options.getFlushMaxActions());
        }
        if (options.getFlushMaxSizeMb() != null) {
            final ByteSizeUnit sizeUnit;
            if (options.getFlushMaxSizeMb() == -1) {
                // bulk size can be disabled with -1, however the ByteSizeValue constructor accepts -1
                // only with BYTES as the size unit
                sizeUnit = ByteSizeUnit.BYTES;
            } else {
                sizeUnit = ByteSizeUnit.MB;
            }
            builder.setBulkSize(new ByteSizeValue(options.getFlushMaxSizeMb(), sizeUnit));
        }
        if (options.getFlushInterval() != null) {
            if (options.getFlushInterval() == -1) {
                builder.setFlushInterval(null);
            } else {
                builder.setFlushInterval(TimeValue.timeValueMillis(options.getFlushInterval()));
            }
        }
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/elasticsearch-6/src/main/java/org/apache/inlong/sort/elasticsearch6/ElasticsearchSink.java`
#### Snippet
```java
            if (response.hasFailures()) {
                BulkItemResponse itemResponse;
                Throwable failure;
                int restStatus;
                try {
                    for (int i = 0; i < response.getItems().length; i++) {
                        itemResponse = response.getItems()[i];
                        failure = getCallBridge().extractFailureCauseFromBulkItemResponse(itemResponse);
                        if (failure != null) {
                            restStatus = itemResponse.getFailure().getStatus() != null ? itemResponse.getFailure()
                                    .getStatus().getStatus() : -1;
                            getFailureHandler().onFailure(request.requests().get(i),
                                    failure, restStatus, getFailureRequestIndexer());
                        }
                    }
                } catch (Throwable t) {
                    // fail the sink and skip the rest of the items
                    // if the failure handler decides to throw an exception
                    getFailureThrowable().compareAndSet(null, t);
                }
            }
            if (flushOnCheckpoint()) {
                getPendingRequests().getAndAdd(-request.numberOfActions());
            }
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/elasticsearch-6/src/main/java/org/apache/inlong/sort/elasticsearch6/ElasticsearchSink.java`
#### Snippet
```java
            try {
                for (DocWriteRequest<?> writeRequest : request.requests()) {
                    getFailureHandler().onFailure(writeRequest, failure, -1, getFailureRequestIndexer());
                }
            } catch (Throwable t) {
                // fail the sink and skip the rest of the items
                // if the failure handler decides to throw an exception
                getFailureThrowable().compareAndSet(null, t);
            }
            if (flushOnCheckpoint()) {
                getPendingRequests().getAndAdd(-request.numberOfActions());
            }
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/elasticsearch-6/src/main/java/org/apache/inlong/sort/elasticsearch6/table/Elasticsearch6Configuration.java`
#### Snippet
```java
        try {
            HttpHost httpHost = HttpHost.create(host);
            if (httpHost.getPort() < 0) {
                throw new ValidationException(
                        String.format(
                                "Could not parse host '%s' in option '%s'. It should follow the format 'http://host_name:port'. Missing port.",
                                host, HOSTS_OPTION.key()));
            }

            if (httpHost.getSchemeName() == null) {
                throw new ValidationException(
                        String.format(
                                "Could not parse host '%s' in option '%s'. It should follow the format 'http://host_name:port'. Missing scheme.",
                                host, HOSTS_OPTION.key()));
            }
            return httpHost;
        } catch (Exception e) {
            throw new ValidationException(
                    String.format(
                            "Could not parse host '%s' in option '%s'. It should follow the format 'http://host_name:port'.",
                            host, HOSTS_OPTION.key()),
                    e);
        }
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/elasticsearch-6/src/main/java/org/apache/inlong/sort/elasticsearch6/table/Elasticsearch6Configuration.java`
#### Snippet
```java
        final ActionRequestFailureHandler<DocWriteRequest<?>> failureHandler;
        String value = config.get(FAILURE_HANDLER_OPTION);
        switch (value.toUpperCase()) {
            case "FAIL":
                failureHandler = new NoOpFailureHandler<>();
                break;
            case "IGNORE":
                failureHandler = new IgnoringFailureHandler<>();
                break;
            case "RETRY-REJECTED":
                failureHandler = new RetryRejectedExecutionFailureHandler();
                break;
            default:
                try {
                    Class<?> failureHandlerClass = Class.forName(value, false, getClassLoader());
                    failureHandler = (ActionRequestFailureHandler<DocWriteRequest<?>>) InstantiationUtil
                            .instantiate(failureHandlerClass);
                } catch (ClassNotFoundException e) {
                    throw new ValidationException(
                            "Could not instantiate the failure handler class: " + value, e);
                }
                break;
        }
        return failureHandler;
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/elasticsearch-6/src/main/java/org/apache/inlong/sort/elasticsearch6/table/Elasticsearch6DynamicSink.java`
#### Snippet
```java
return () -> {
            SerializationSchema<RowData> format =
                    this.format.createRuntimeEncoder(context, schema.toRowDataType());

            final ElasticsearchSinkFunction<RowData, DocWriteRequest<?>> upsertFunction =
                    createSinkFunction(format, context);

            final ElasticsearchSink.Builder<RowData> builder =
                    builderProvider.createBuilder(config.getHosts(), upsertFunction);
            builder.setFailureHandler(config.getFailureHandler());
            builder.setBulkFlushMaxActions(config.getBulkFlushMaxActions());
            builder.setBulkFlushMaxSizeMb((int) (config.getBulkFlushMaxByteSize() >> 20));
            builder.setBulkFlushInterval(config.getBulkFlushInterval());
            builder.setBulkFlushBackoff(config.isBulkFlushBackoffEnabled());
            builder.setInLongMetric(inlongMetric);
            builder.setDirtySinkHelper(dirtySinkHelper);
            builder.setAuditHostAndPorts(auditHostAndPorts);
            config.getBulkFlushBackoffType().ifPresent(builder::setBulkFlushBackoffType);
            config.getBulkFlushBackoffRetries().ifPresent(builder::setBulkFlushBackoffRetries);
            config.getBulkFlushBackoffDelay().ifPresent(builder::setBulkFlushBackoffDelay);
            // we must overwrite the default factory which is defined with a lambda because of a bug
            // in shading lambda serialization shading see FLINK-18006
            if (config.getUsername().isPresent()
                    && config.getPassword().isPresent()
                    && !StringUtils.isNullOrWhitespaceOnly(config.getUsername().get())
                    && !StringUtils.isNullOrWhitespaceOnly(config.getPassword().get())) {
                builder.setRestClientFactory(
                        new AuthRestClientFactory(
                                config.getPathPrefix().orElse(null),
                                config.getUsername().get(),
                                config.getPassword().get()));
            } else {
                builder.setRestClientFactory(
                        new DefaultRestClientFactory(config.getPathPrefix().orElse(null)));
            }
            builder.setMultipleSink(multipleSink);
            final ElasticsearchSink<RowData> sink = builder.build();
            if (config.isDisableFlushOnCheckpoint()) {
                sink.disableFlushOnCheckpoint();
            }
            return sink;
        };
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/elasticsearch-6/src/main/java/org/apache/inlong/sort/elasticsearch6/table/Elasticsearch6DynamicSink.java`
#### Snippet
```java
            if (pathPrefix != null) {
                restClientBuilder.setPathPrefix(pathPrefix);
            }
            if (credentialsProvider == null) {
                credentialsProvider = new BasicCredentialsProvider();
                credentialsProvider.setCredentials(
                        AuthScope.ANY, new UsernamePasswordCredentials(username, password));
            }
            restClientBuilder.setHttpClientConfigCallback(
                    httpAsyncClientBuilder -> httpAsyncClientBuilder.setDefaultCredentialsProvider(
                            credentialsProvider));
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/elasticsearch-6/src/main/java/org/apache/inlong/sort/elasticsearch6/table/Elasticsearch6DynamicSinkFactory.java`
#### Snippet
```java
        TableSchema tableSchema = context.getCatalogTable().getSchema();
        ElasticsearchValidationUtils.validatePrimaryKey(tableSchema);
        final FactoryUtil.TableFactoryHelper helper =
                FactoryUtil.createTableFactoryHelper(this, context);
        final EncodingFormat<SerializationSchema<RowData>> format =
                helper.discoverEncodingFormat(SerializationFormatFactory.class, FORMAT_OPTION);
        helper.validateExcept(DIRTY_PREFIX);
        Configuration configuration = new Configuration();
        context.getCatalogTable().getOptions().forEach(configuration::setString);
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/elasticsearch-6/src/main/java/org/apache/inlong/sort/elasticsearch6/table/Elasticsearch6DynamicSinkFactory.java`
#### Snippet
```java
        validate(config, configuration);
        String inlongMetric = helper.getOptions().getOptional(INLONG_METRIC).orElse(null);
        String auditHostAndPorts = helper.getOptions().getOptional(INLONG_AUDIT).orElse(null);
        final DirtyOptions dirtyOptions = DirtyOptions.fromConfig(helper.getOptions());
        final DirtySink<Object> dirtySink = DirtySinkFactoryUtils.createDirtySink(context, dirtyOptions);
        final DirtySinkHelper<Object> dirtySinkHelper = new DirtySinkHelper<>(dirtyOptions, dirtySink);
        final boolean multipleSink = helper.getOptions().getOptional(SINK_MULTIPLE_ENABLE).orElse(false);
        final String indexPattern = helper.getOptions().getOptional(SINK_MULTIPLE_INDEX_PATTERN).orElse(null);
        final String multipleFormat = helper.getOptions().getOptional(SINK_MULTIPLE_FORMAT).orElse(null);
        final SchemaUpdateExceptionPolicy schemaUpdateExceptionPolicy =
                helper.getOptions().getOptional(SINK_MULTIPLE_SCHEMA_UPDATE_POLICY).orElse(null);
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/elasticsearch-6/src/main/java/org/apache/inlong/sort/elasticsearch6/table/Elasticsearch6DynamicSinkFactory.java`
#### Snippet
```java
config.getFailureHandler(); // checks if we can instantiate the custom failure handler
        config.getHosts(); // validate hosts
        validate(
                config.getIndex().length() >= 1,
                () -> String.format("'%s' must not be empty", INDEX_OPTION.key()));
        int maxActions = config.getBulkFlushMaxActions();
        validate(
                maxActions == -1 || maxActions >= 1,
                () -> String.format(
                        "'%s' must be at least 1. Got: %s",
                        BULK_FLUSH_MAX_ACTIONS_OPTION.key(), maxActions));
        long maxSize = config.getBulkFlushMaxByteSize();
        long mb1 = 1024 * 1024;
        validate(
                maxSize == -1 || (maxSize >= mb1 && maxSize % mb1 == 0),
                () -> String.format(
                        "'%s' must be in MB granularity. Got: %s",
                        BULK_FLASH_MAX_SIZE_OPTION.key(),
                        originalConfiguration
                                .get(BULK_FLASH_MAX_SIZE_OPTION)
                                .toHumanReadableString()));
        validate(
                config.getBulkFlushBackoffRetries().map(retries -> retries >= 1).orElse(true),
                () -> String.format(
                        "'%s' must be at least 1. Got: %s",
                        BULK_FLUSH_BACKOFF_MAX_RETRIES_OPTION.key(),
                        config.getBulkFlushBackoffRetries().get()));
        if (config.getUsername().isPresent()
                && !StringUtils.isNullOrWhitespaceOnly(config.getUsername().get())) {
            validate(
                    config.getPassword().isPresent()
                            && !StringUtils.isNullOrWhitespaceOnly(config.getPassword().get()),
                    () -> String.format(
                            "'%s' and '%s' must be set at the same time. Got: username '%s' and password '%s'",
                            USERNAME_OPTION.key(),
                            PASSWORD_OPTION.key(),
                            config.getUsername().get(),
                            config.getPassword().orElse("")));
        }
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/elasticsearch-6/src/main/java/org/apache/inlong/sort/elasticsearch6/utils/DirtySinkFailureHandler.java`
#### Snippet
```java
        if (ExceptionUtils.findThrowable(failure, ElasticsearchException.class).isPresent()) {
            String dirtyData;
            if (request instanceof UpdateRequest) {
                dirtyData = ((UpdateRequest) request).doc().source().utf8ToString();
            } else if (request instanceof IndexRequest) {
                dirtyData = ((IndexRequest) request).source().utf8ToString();
            } else {
                dirtyData = request.id();
            }
            LOGGER.error("Action request handle error", failure);
            LOGGER.debug("The dirty data: {}", dirtyData);
            dirtySinkHelper.invoke(dirtyData, DirtyType.DOCUMENT_PARSE_ERROR, failure);
            if (sinkMetricData != null) {
                sinkMetricData.invokeDirty(1, dirtyData.getBytes(StandardCharsets.UTF_8).length);
            }
        } else {
            throw failure;
        }
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/elasticsearch-base/src/main/java/org/apache/inlong/sort/elasticsearch/ElasticsearchSinkBase.java`
#### Snippet
```java
        MetricOption metricOption = MetricOption.builder()
                .withInlongLabels(inlongMetric)
                .withAuditAddress(auditHostAndPorts)
                .withInitRecords(metricState != null ? metricState.getMetricValue(NUM_RECORDS_OUT) : 0L)
                .withInitBytes(metricState != null ? metricState.getMetricValue(NUM_BYTES_OUT) : 0L)
                .withInitDirtyRecords(metricState != null ? metricState.getMetricValue(DIRTY_RECORDS_OUT) : 0L)
                .withInitDirtyBytes(metricState != null ? metricState.getMetricValue(DIRTY_BYTES_OUT) : 0L)
                .withRegisterMetric(RegisteredMetric.ALL)
                .build();
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/filesystem/src/main/java/org/apache/inlong/sort/filesystem/stream/AbstractStreamingWriter.java`
#### Snippet
```java
        super.open();
        MetricOption metricOption = MetricOption.builder()
                .withInlongLabels(inlongMetric)
                .withAuditAddress(inlongAudit)
                .withInitRecords(metricState != null ? metricState.getMetricValue(NUM_RECORDS_OUT) : 0L)
                .withInitBytes(metricState != null ? metricState.getMetricValue(NUM_BYTES_OUT) : 0L)
                .withInitDirtyRecords(metricState != null ? metricState.getMetricValue(DIRTY_RECORDS_OUT) : 0L)
                .withInitDirtyBytes(metricState != null ? metricState.getMetricValue(DIRTY_BYTES_OUT) : 0L)
                .withRegisterMetric(RegisteredMetric.ALL)
                .build();
        if (metricOption != null) {
            sinkMetricData = new SinkMetricData(metricOption, getRuntimeContext().getMetricGroup());
        }
        if (dirtySink != null) {
            dirtySink.open(new Configuration());
        }
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/filesystem/src/main/java/org/apache/inlong/sort/filesystem/stream/AbstractStreamingWriter.java`
#### Snippet
```java
        helper =
                new StreamingFileSinkHelper<>(
                        buckets,
                        context.isRestored(),
                        context.getOperatorStateStore(),
                        getRuntimeContext().getProcessingTimeService(),
                        bucketCheckInterval);

        currentWatermark = Long.MIN_VALUE;
        if (this.inlongMetric != null) {
            this.metricStateListState = context.getOperatorStateStore().getUnionListState(
                    new ListStateDescriptor<>(
                            INLONG_METRIC_STATE_NAME, TypeInformation.of(new TypeHint<MetricState>() {
                            })));
        }
        if (context.isRestored()) {
            metricState = MetricStateUtils.restoreMetricState(metricStateListState,
                    getRuntimeContext().getIndexOfThisSubtask(), getRuntimeContext().getNumberOfParallelSubtasks());
        }
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/filesystem/src/main/java/org/apache/inlong/sort/filesystem/stream/AbstractStreamingWriter.java`
#### Snippet
```java
            if (dirtySink != null) {
                DirtyData.Builder<Object> builder = DirtyData.builder();
                try {
                    builder.setData(element.getValue())
                            .setDirtyType(DirtyType.UNDEFINED)
                            .setLabels(dirtyOptions.getLabels())
                            .setLogTag(dirtyOptions.getLogTag())
                            .setDirtyMessage(e.getMessage())
                            .setIdentifier(dirtyOptions.getIdentifier());
                    dirtySink.invoke(builder.build());
                } catch (Exception ex) {
                    if (!dirtyOptions.ignoreSideOutputErrors()) {
                        throw new RuntimeException(ex);
                    }
                    LOGGER.warn("Dirty sink failed", ex);
                }
            }
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/filesystem/src/main/java/org/apache/inlong/sort/filesystem/stream/StreamingFileWriter.java`
#### Snippet
```java
        super.commitUpToCheckpoint(checkpointId);

        NavigableMap<Long, Set<String>> headPartitions =
                this.newPartitions.headMap(checkpointId, true);
        Set<String> partitions = new HashSet<>(committablePartitions);
        committablePartitions.clear();
        headPartitions.values().forEach(partitions::addAll);
        headPartitions.clear();

        output.collect(
                new StreamRecord<>(
                        new PartitionCommitInfo(
                                checkpointId,
                                getRuntimeContext().getIndexOfThisSubtask(),
                                getRuntimeContext().getNumberOfParallelSubtasks(),
                                new ArrayList<>(partitions))));
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/filesystem/src/main/java/org/apache/inlong/sort/filesystem/stream/StreamingSink.java`
#### Snippet
```java
        CompactFileWriter<T> writer = new CompactFileWriter<>(bucketCheckInterval, bucketsBuilder, inlongMetric,
                inlongAudit, dirtyOptions, dirtySink);

        SupplierWithException<FileSystem, IOException> fsSupplier =
                (SupplierWithException<FileSystem, IOException> & Serializable) () -> fsFactory.create(path.toUri());

        CompactCoordinator coordinator = new CompactCoordinator(fsSupplier, targetFileSize);

        SingleOutputStreamOperator<CoordinatorOutput> coordinatorOp =
                inputStream
                        .transform(
                                "streaming-writer",
                                TypeInformation.of(CoordinatorInput.class),
                                writer)
                        .setParallelism(parallelism)
                        .transform(
                                "compact-coordinator",
                                TypeInformation.of(CoordinatorOutput.class),
                                coordinator)
                        .setParallelism(1)
                        .setMaxParallelism(1);

        CompactWriter.Factory<T> writerFactory =
                CompactBucketWriter.factory(
                        (SupplierWithException<BucketWriter<T, String>, IOException> & Serializable) bucketsBuilder::createBucketWriter);

        CompactOperator<T> compacter =
                new CompactOperator<>(fsSupplier, readFactory, writerFactory);
        return coordinatorOp
                .broadcast()
                .transform(
                        "compact-operator",
                        TypeInformation.of(PartitionCommitInfo.class),
                        compacter)
                .setParallelism(parallelism);
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/filesystem/src/main/java/org/apache/inlong/sort/filesystem/stream/StreamingSink.java`
#### Snippet
```java
        DataStream<?> stream = writer;
        if (partitionKeys.size() > 0 && options.contains(SINK_PARTITION_COMMIT_POLICY_KIND)) {
            PartitionCommitter committer =
                    new PartitionCommitter(
                            locationPath, identifier, partitionKeys, msFactory, fsFactory, options);
            stream =
                    writer.transform(
                            PartitionCommitter.class.getSimpleName(), Types.VOID, committer)
                            .setParallelism(1)
                            .setMaxParallelism(1);
        }

        return stream.addSink(new DiscardingSink<>()).name("end").setParallelism(1);
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/hbase/src/main/java/org/apache/inlong/sort/hbase/HBase2DynamicTableFactory.java`
#### Snippet
```java
        final ReadableConfig tableOptions = helper.getOptions();

        TableSchema tableSchema = context.getCatalogTable().getSchema();
        Map<String, String> options = context.getCatalogTable().getOptions();

        validatePrimaryKey(tableSchema);

        String tableName = tableOptions.get(TABLE_NAME);
        Configuration hbaseConf = getHBaseConfiguration(options);
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/hbase/src/main/java/org/apache/inlong/sort/hbase/sink/HBaseSinkFunction.java`
#### Snippet
```java
            this.runtimeContext = getRuntimeContext();
            MetricOption metricOption = MetricOption.builder()
                    .withInlongLabels(inlongMetric)
                    .withAuditAddress(inlongAudit)
                    .withInitRecords(metricState != null ? metricState.getMetricValue(NUM_RECORDS_OUT) : 0L)
                    .withInitBytes(metricState != null ? metricState.getMetricValue(NUM_BYTES_OUT) : 0L)
                    .withInitDirtyRecords(metricState != null ? metricState.getMetricValue(DIRTY_RECORDS_OUT) : 0L)
                    .withInitDirtyBytes(metricState != null ? metricState.getMetricValue(DIRTY_BYTES_OUT) : 0L)
                    .withRegisterMetric(RegisteredMetric.ALL)
                    .build();
            if (metricOption != null) {
                sinkMetricData = new SinkMetricData(metricOption, runtimeContext.getMetricGroup());
            }
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/iceberg/src/main/java/org/apache/inlong/sort/iceberg/sink/BaseDeltaTaskWriter.java`
#### Snippet
```java
        switch (row.getRowKind()) {
            case INSERT:
            case UPDATE_AFTER:
                if (upsert) {
                    writer.deleteKey(keyProjection.wrap(row));
                }
                writer.write(row);
                break;

            case UPDATE_BEFORE:
                if (upsert) {
                    break; // UPDATE_BEFORE is not necessary for UPDATE, we do nothing to prevent delete one row twice
                }
                writer.delete(row);
                break;
            case DELETE:
                if (upsert) { // https://github.com/apache/iceberg/pull/6753/files
                    writer.deleteKey(keyProjection.wrap(row));
                } else {
                    writer.delete(row);
                }
                break;

            default:
                throw new UnsupportedOperationException("Unknown row kind: " + row.getRowKind());
        }
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/iceberg/src/main/java/org/apache/inlong/sort/iceberg/sink/multiple/IcebergMultipleStreamWriter.java`
#### Snippet
```java
        this.runtimeContext = getRuntimeContext();
        MetricOption metricOption = MetricOption.builder()
                .withInlongLabels(inlongMetric)
                .withAuditAddress(auditHostAndPorts)
                .withInitRecords(metricState != null ? metricState.getMetricValue(NUM_RECORDS_OUT) : 0L)
                .withInitBytes(metricState != null ? metricState.getMetricValue(NUM_BYTES_OUT) : 0L)
                .withInitDirtyRecords(metricState != null ? metricState.getMetricValue(DIRTY_RECORDS_OUT) : 0L)
                .withInitDirtyBytes(metricState != null ? metricState.getMetricValue(DIRTY_BYTES_OUT) : 0L)
                .withRegisterMetric(MetricOption.RegisteredMetric.ALL)
                .build();
        if (metricOption != null) {
            sinkMetricData = new SinkTableMetricData(metricOption, runtimeContext.getMetricGroup());
            sinkMetricData.registerSubMetricsGroup(metricState);
        }
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/iceberg/src/main/java/org/apache/inlong/sort/iceberg/sink/multiple/IcebergSingleFileCommiter.java`
#### Snippet
```java
        int numFiles = 0;
        for (WriteResult result : pendingResults.values()) {
            Preconditions.checkState(result.referencedDataFiles().length == 0,
                    "Should have no referenced data files.");

            numFiles += result.dataFiles().length;
            Arrays.stream(result.dataFiles()).forEach(dynamicOverwrite::addFile);
        }
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/jdbc/src/main/java/org/apache/inlong/sort/jdbc/internal/JdbcBatchingOutputFormat.java`
#### Snippet
```java
        if (dirtySink != null) {
            DirtyData.Builder<Object> builder = DirtyData.builder();
            try {
                builder.setData(dirtyData)
                        .setDirtyType(dirtyType)
                        .setLabels(dirtyOptions.getLabels())
                        .setLogTag(dirtyOptions.getLogTag())
                        .setDirtyMessage(e.getMessage())
                        .setIdentifier(dirtyOptions.getIdentifier());
                dirtySink.invoke(builder.build());
            } catch (Exception ex) {
                if (!dirtyOptions.ignoreSideOutputErrors()) {
                    throw new RuntimeException(ex);
                }
                LOG.warn("Dirty sink failed", ex);
            }
        }
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/jdbc/src/main/java/org/apache/inlong/sort/jdbc/internal/JdbcBatchingOutputFormat.java`
#### Snippet
```java
        Field executorType;
        if (exec instanceof TableBufferReducedStatementExecutor) {
            executorType = TableBufferReducedStatementExecutor.class.getDeclaredField("upsertExecutor");
        } else if (exec instanceof TableBufferedStatementExecutor) {
            executorType = TableBufferedStatementExecutor.class.getDeclaredField("statementExecutor");
        } else {
            throw new RuntimeException("table enhance failed, can't enhance " + exec.getClass());
        }
        executorType.setAccessible(true);
        TableSimpleStatementExecutor executor = (TableSimpleStatementExecutor) executorType.get(exec);
        // get the factory and rowconverter to initialize TableMetricStatementExecutor.
        Field statementFactory = TableSimpleStatementExecutor.class.getDeclaredField("stmtFactory");
        Field rowConverter = TableSimpleStatementExecutor.class.getDeclaredField("converter");
        statementFactory.setAccessible(true);
        rowConverter.setAccessible(true);
        final StatementFactory stmtFactory = (StatementFactory) statementFactory.get(executor);
        final JdbcRowConverter converter = (JdbcRowConverter) rowConverter.get(executor);
        TableMetricStatementExecutor newExecutor =
                new TableMetricStatementExecutor(stmtFactory, converter, dirtySinkHelper, sinkMetricData);
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/jdbc/src/main/java/org/apache/inlong/sort/jdbc/internal/JdbcBatchingOutputFormat.java`
#### Snippet
```java
            closed = true;

            if (this.scheduledFuture != null) {
                scheduledFuture.cancel(false);
                this.scheduler.shutdown();
            }

            if (batchCount > 0) {
                try {
                    flush();
                } catch (Exception e) {
                    LOG.warn("Writing records to JDBC failed.", e);
                    throw new RuntimeException("Writing records to JDBC failed.", e);
                }
            }
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/jdbc/src/main/java/org/apache/inlong/sort/jdbc/internal/JdbcMultiBatchingComm.java`
#### Snippet
```java
        checkArgument(opt.getKeyFields().isPresent());
        JdbcDialect dialect = opt.getDialect();
        String tableName = opt.getTableName();
        String[] pkNames = opt.getKeyFields().get();
        int[] pkFields =
                Arrays.stream(pkNames)
                        .mapToInt(Arrays.asList(opt.getFieldNames())::indexOf)
                        .toArray();
        LogicalType[] pkTypes =
                Arrays.stream(pkFields).mapToObj(f -> fieldTypes[f]).toArray(LogicalType[]::new);
        final TypeSerializer<RowData> typeSerializer =
                rowDataTypeInfo.createSerializer(ctx.getExecutionConfig());
        final Function<RowData, RowData> valueTransform =
                ctx.getExecutionConfig().isObjectReuseEnabled()
                        ? typeSerializer::copy
                        : Function.identity();

        return new TableBufferReducedStatementExecutor(
                createUpsertRowExecutor(
                        dialect,
                        tableName,
                        opt.getFieldNames(),
                        fieldTypes,
                        pkFields,
                        pkNames,
                        pkTypes),
                createDeleteExecutor(dialect, tableName, pkNames, pkTypes),
                createRowKeyExtractor(fieldTypes, pkFields),
                valueTransform);
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/jdbc/src/main/java/org/apache/inlong/sort/jdbc/internal/JdbcMultiBatchingComm.java`
#### Snippet
```java
        final String existStmt = dialect.getRowExistsStatement(tableName, pkNames);
        final String insertStmt = dialect.getInsertIntoStatement(tableName, fieldNames);
        final String updateStmt = dialect.getUpdateStatement(tableName, fieldNames, pkNames);
        return new TableInsertOrUpdateStatementExecutor(
                connection -> FieldNamedPreparedStatement.prepareStatement(
                        connection, existStmt, pkNames),
                connection -> FieldNamedPreparedStatement.prepareStatement(
                        connection, insertStmt, fieldNames),
                connection -> FieldNamedPreparedStatement.prepareStatement(
                        connection, updateStmt, fieldNames),
                dialect.getRowConverter(RowType.of(pkTypes)),
                dialect.getRowConverter(RowType.of(fieldTypes)),
                dialect.getRowConverter(RowType.of(fieldTypes)),
                createRowKeyExtractor(fieldTypes, pkFields));
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/jdbc/src/main/java/org/apache/inlong/sort/jdbc/internal/JdbcMultiBatchingComm.java`
#### Snippet
```java
        final RowData.FieldGetter[] fieldGetters = new RowData.FieldGetter[pkFields.length];
        for (int i = 0; i < pkFields.length; i++) {
            fieldGetters[i] = createFieldGetter(logicalTypes[pkFields[i]], pkFields[i]);
        }
        return row -> getPrimaryKey(row, fieldGetters);
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kafka/src/main/java/org/apache/inlong/sort/kafka/DynamicKafkaSerializationSchema.java`
#### Snippet
```java
        final int arity = fieldGetters.length;
        final GenericRowData genericRowData = new GenericRowData(kind, arity);
        for (int fieldPos = 0; fieldPos < arity; fieldPos++) {
            genericRowData.setField(fieldPos, fieldGetters[fieldPos].getFieldOrNull(consumedRow));
        }
        return genericRowData;
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kafka/src/main/java/org/apache/inlong/sort/kafka/FlinkKafkaConsumerBase.java`
#### Snippet
```java
        checkNotNull(watermarkStrategy);

        try {
            ClosureCleaner.clean(
                    watermarkStrategy, ExecutionConfig.ClosureCleanerLevel.RECURSIVE, true);
            this.watermarkStrategy = new SerializedValue<>(watermarkStrategy);
        } catch (Exception e) {
            throw new IllegalArgumentException(
                    "The given WatermarkStrategy is not serializable", e);
        }

        return this;
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kafka/src/main/java/org/apache/inlong/sort/kafka/FlinkKafkaConsumerBase.java`
#### Snippet
```java
        checkNotNull(assigner);

        if (this.watermarkStrategy != null) {
            throw new IllegalStateException("Some watermark strategy has already been set.");
        }

        try {
            ClosureCleaner.clean(assigner, ExecutionConfig.ClosureCleanerLevel.RECURSIVE, true);
            final WatermarkStrategy<T> wms =
                    new AssignerWithPunctuatedWatermarksAdapter.Strategy<>(assigner);

            return assignTimestampsAndWatermarks(wms);
        } catch (Exception e) {
            throw new IllegalArgumentException("The given assigner is not serializable", e);
        }
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kafka/src/main/java/org/apache/inlong/sort/kafka/FlinkKafkaConsumerBase.java`
#### Snippet
```java
        checkNotNull(assigner);

        if (this.watermarkStrategy != null) {
            throw new IllegalStateException("Some watermark strategy has already been set.");
        }

        try {
            ClosureCleaner.clean(assigner, ExecutionConfig.ClosureCleanerLevel.RECURSIVE, true);
            final WatermarkStrategy<T> wms =
                    new AssignerWithPeriodicWatermarksAdapter.Strategy<>(assigner);

            return assignTimestampsAndWatermarks(wms);
        } catch (Exception e) {
            throw new IllegalArgumentException("The given assigner is not serializable", e);
        }
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kafka/src/main/java/org/apache/inlong/sort/kafka/FlinkKafkaConsumerBase.java`
#### Snippet
```java
        cancel();

        joinDiscoveryLoopThread();

        Exception exception = null;
        if (partitionDiscoverer != null) {
            try {
                partitionDiscoverer.close();
            } catch (Exception e) {
                exception = e;
            }
        }

        try {
            super.close();
        } catch (Exception e) {
            exception = ExceptionUtils.firstOrSuppressed(e, exception);
        }

        if (exception != null) {
            throw exception;
        }
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kafka/src/main/java/org/apache/inlong/sort/kafka/KafkaDynamicSink.java`
#### Snippet
```java
        final List<LogicalType> physicalChildren = physicalDataType.getLogicalType().getChildren();

        final RowData.FieldGetter[] keyFieldGetters =
                Arrays.stream(keyProjection)
                        .mapToObj(
                                targetField -> RowData.createFieldGetter(
                                        physicalChildren.get(targetField), targetField))
                        .toArray(RowData.FieldGetter[]::new);

        final RowData.FieldGetter[] valueFieldGetters =
                Arrays.stream(valueProjection)
                        .mapToObj(
                                targetField -> RowData.createFieldGetter(
                                        physicalChildren.get(targetField), targetField))
                        .toArray(RowData.FieldGetter[]::new);

        // determine the positions of metadata in the consumed row
        final int[] metadataPositions =
                Stream.of(WritableMetadata.values())
                        .mapToInt(
                                m -> {
                                    final int pos = metadataKeys.indexOf(m.key);
                                    if (pos < 0) {
                                        return -1;
                                    }
                                    return physicalChildren.size() + pos;
                                })
                        .toArray();

        // check if metadata is used at all
        final boolean hasMetadata = metadataKeys.size() > 0;
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kafka/src/main/java/org/apache/inlong/sort/kafka/KafkaDynamicSink.java`
#### Snippet
```java
        if (format == null) {
            return null;
        }
        DataType physicalFormatDataType =
                DataTypeUtils.projectRow(this.physicalDataType, projection);
        if (prefix != null) {
            physicalFormatDataType = DataTypeUtils.stripRowPrefix(physicalFormatDataType, prefix);
        }
        return format.createRuntimeEncoder(context, physicalFormatDataType);
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kafka/src/main/java/org/apache/inlong/sort/kafka/table/KafkaDynamicSource.java`
#### Snippet
```java
        final Map<String, DataType> metadataMap = new LinkedHashMap<>();

        // according to convention, the order of the final row must be
        // PHYSICAL + FORMAT METADATA + CONNECTOR METADATA
        // where the format metadata has highest precedence

        // add value format metadata with prefix
        valueDecodingFormat
                .listReadableMetadata()
                .forEach((key, value) -> metadataMap.put(VALUE_METADATA_PREFIX + key, value));

        // add connector metadata
        Stream.of(ReadableMetadata.values())
                .forEachOrdered(m -> metadataMap.putIfAbsent(m.key, m.dataType));

        return metadataMap;
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kafka/src/main/java/org/apache/inlong/sort/kafka/table/KafkaDynamicSource.java`
#### Snippet
```java
        final List<String> formatMetadataKeys =
                metadataKeys.stream()
                        .filter(k -> k.startsWith(VALUE_METADATA_PREFIX))
                        .collect(Collectors.toList());
        final List<String> connectorMetadataKeys = new ArrayList<>(metadataKeys);
        connectorMetadataKeys.removeAll(formatMetadataKeys);

        // push down format metadata
        final Map<String, DataType> formatMetadata = valueDecodingFormat.listReadableMetadata();
        if (formatMetadata.size() > 0) {
            final List<String> requestedFormatMetadataKeys =
                    formatMetadataKeys.stream()
                            .map(k -> k.substring(VALUE_METADATA_PREFIX.length()))
                            .collect(Collectors.toList());
            valueDecodingFormat.applyReadableMetadata(requestedFormatMetadataKeys);
        }

        this.metadataKeys = connectorMetadataKeys;
        this.producedDataType = producedDataType;
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kafka/src/main/java/org/apache/inlong/sort/kafka/table/KafkaDynamicSource.java`
#### Snippet
```java
        if (format == null) {
            return null;
        }
        DataType physicalFormatDataType =
                DataTypeUtils.projectRow(this.physicalDataType, projection);
        if (prefix != null) {
            physicalFormatDataType = DataTypeUtils.stripRowPrefix(physicalFormatDataType, prefix);
        }
        return format.createRuntimeDecoder(context, physicalFormatDataType);
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kafka/src/main/java/org/apache/inlong/sort/kafka/table/KafkaDynamicTableFactory.java`
#### Snippet
```java
        final Optional<DecodingFormat<DeserializationSchema<RowData>>> keyDecodingFormat =
                helper.discoverOptionalDecodingFormat(
                        DeserializationFormatFactory.class, KEY_FORMAT);
        keyDecodingFormat.ifPresent(
                format -> {
                    if (!format.getChangelogMode().containsOnly(RowKind.INSERT)) {
                        throw new ValidationException(
                                String.format(
                                        "A key format should only deal with INSERT-only records. "
                                                + "But %s has a changelog mode of %s.",
                                        helper.getOptions().get(KEY_FORMAT),
                                        format.getChangelogMode()));
                    }
                });
        return keyDecodingFormat;
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kafka/src/main/java/org/apache/inlong/sort/kafka/table/KafkaDynamicTableFactory.java`
#### Snippet
```java
        final Optional<EncodingFormat<SerializationSchema<RowData>>> keyEncodingFormat =
                helper.discoverOptionalEncodingFormat(SerializationFormatFactory.class, KEY_FORMAT);
        keyEncodingFormat.ifPresent(
                format -> {
                    if (!format.getChangelogMode().containsOnly(RowKind.INSERT)) {
                        throw new ValidationException(
                                String.format(
                                        "A key format should only deal with INSERT-only records. "
                                                + "But %s has a changelog mode of %s.",
                                        helper.getOptions().get(KEY_FORMAT),
                                        format.getChangelogMode()));
                    }
                });
        return keyEncodingFormat;
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kafka/src/main/java/org/apache/inlong/sort/kafka/table/UpsertKafkaDynamicTableFactory.java`
#### Snippet
```java
        final Set<ConfigOption<?>> options = new HashSet<>();
        options.add(PROPS_BOOTSTRAP_SERVERS);
        options.add(TOPIC);
        options.add(KEY_FORMAT);
        options.add(VALUE_FORMAT);
        return options;
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kafka/src/main/java/org/apache/inlong/sort/kafka/table/UpsertKafkaDynamicTableFactory.java`
#### Snippet
```java
        FactoryUtil.TableFactoryHelper helper = FactoryUtil.createTableFactoryHelper(this, context);

        ReadableConfig tableOptions = helper.getOptions();
        DecodingFormat<DeserializationSchema<RowData>> keyDecodingFormat =
                helper.discoverDecodingFormat(DeserializationFormatFactory.class, KEY_FORMAT);
        DecodingFormat<DeserializationSchema<RowData>> valueDecodingFormat =
                helper.discoverDecodingFormat(DeserializationFormatFactory.class, VALUE_FORMAT);
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kafka/src/main/java/org/apache/inlong/sort/kafka/table/UpsertKafkaDynamicTableFactory.java`
#### Snippet
```java
        TableSchema schema = catalogTable.getSchema();
        // primary key should validated earlier
        List<String> keyFields = schema.getPrimaryKey().get().getColumns();
        DataType physicalDataType = schema.toPhysicalRowDataType();

        Configuration tableOptions = Configuration.fromMap(catalogTable.getOptions());
        // upsert-kafka will set key.fields to primary key fields by default
        tableOptions.set(KEY_FIELDS, keyFields);

        int[] keyProjection = createKeyFormatProjection(tableOptions, physicalDataType);
        int[] valueProjection = createValueFormatProjection(tableOptions, physicalDataType);

        return Tuple2.of(keyProjection, valueProjection);
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kudu/src/main/java/org/apache/inlong/sort/kudu/sink/AbstractKuduSinkFunction.java`
#### Snippet
```java
        if (this.inLongMetric != null) {
            this.metricStateListState = context.getOperatorStateStore().getUnionListState(
                    new ListStateDescriptor<>(
                            INLONG_METRIC_STATE_NAME, TypeInformation.of(new TypeHint<MetricState>() {
                            })));
        }

        if (context.isRestored()) {
            metricState = MetricStateUtils.restoreMetricState(metricStateListState,
                    getRuntimeContext().getIndexOfThisSubtask(), getRuntimeContext().getNumberOfParallelSubtasks());
        }
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kudu/src/main/java/org/apache/inlong/sort/kudu/sink/KuduAsyncSinkFunction.java`
#### Snippet
```java
        KuduClient client = buildKuduClient();
        KuduTable kuduTable;
        try {
            String tableName = kuduTableInfo.getTableName();
            kuduTable = client.openTable(tableName);
            checkState(client.tableExists(tableName), "Can not find table with name:{} in kudu.", tableName);
        } catch (KuduException e) {
            LOG.error("Error on Open kudu table", e);
            throw new RuntimeException(e);
        }

        // check table schema
        Schema schema = kuduTable.getSchema();
        try {
            checkSchema(kuduTableInfo.getFieldNames(), kuduTableInfo.getDataTypes(), schema);
        } catch (Exception e) {
            LOG.error("The provided schema is invalid!", e);
            throw new RuntimeException(e);
        }
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mongodb-cdc/src/main/java/org/apache/inlong/sort/cdc/mongodb/DebeziumSourceFunction.java`
#### Snippet
```java
OperatorStateStore stateStore = context.getOperatorStateStore();
        this.offsetState =
                stateStore.getUnionListState(
                        new ListStateDescriptor<>(
                                OFFSETS_STATE_NAME,
                                PrimitiveArrayTypeInfo.BYTE_PRIMITIVE_ARRAY_TYPE_INFO));
        this.schemaRecordsState =
                stateStore.getUnionListState(
                        new ListStateDescriptor<>(
                                HISTORY_RECORDS_STATE_NAME, BasicTypeInfo.STRING_TYPE_INFO));

        if (this.inlongMetric != null) {
            this.metricStateListState =
                    stateStore.getUnionListState(
                            new ListStateDescriptor<>(
                                    INLONG_METRIC_STATE_NAME, TypeInformation.of(new TypeHint<MetricState>() {
                                    })));
        }

        if (context.isRestored()) {
            restoreOffsetState();
            restoreHistoryRecordsState();
            metricState = MetricStateUtils.restoreMetricState(metricStateListState,
                    getRuntimeContext().getIndexOfThisSubtask(), getRuntimeContext().getNumberOfParallelSubtasks());
        } else {
            if (specificOffset != null) {
                byte[] serializedOffset =
                        DebeziumOffsetSerializer.INSTANCE.serialize(specificOffset);
                restoredOffsetState = new String(serializedOffset, StandardCharsets.UTF_8);
                LOG.info(
                        "Consumer subtask {} starts to read from specified offset {}.",
                        getRuntimeContext().getIndexOfThisSubtask(),
                        restoredOffsetState);
            } else {
                LOG.info(
                        "Consumer subtask {} has no restore state.",
                        getRuntimeContext().getIndexOfThisSubtask());
            }
        }
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mongodb-cdc/src/main/java/org/apache/inlong/sort/cdc/mongodb/DebeziumSourceFunction.java`
#### Snippet
```java
        DocumentReader reader = DocumentReader.defaultReader();
        ConcurrentLinkedQueue<SchemaRecord> historyRecords = new ConcurrentLinkedQueue<>();
        int recordsCount = 0;
        boolean firstEntry = true;
        for (String record : schemaRecordsState.get()) {
            if (firstEntry) {
                // we store the engine instance name in the first element
                this.engineInstanceName = record;
                firstEntry = false;
            } else {
                // Put the records into the state. The database history should read, reorganize and
                // register the state.
                historyRecords.add(new SchemaRecord(reader.read(record)));
                recordsCount++;
            }
        }
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mongodb-cdc/src/main/java/org/apache/inlong/sort/cdc/mongodb/DebeziumSourceFunction.java`
#### Snippet
```java
        if (handover.hasError()) {
            LOG.debug("snapshotState() called on closed source");
            throw new FlinkRuntimeException(
                    "Call snapshotState() on closed source, checkpoint failed.");
        } else {
            snapshotOffsetState(functionSnapshotContext.getCheckpointId());
            snapshotHistoryRecordsState();
            if (sourceMetricData != null && metricStateListState != null) {
                MetricStateUtils.snapshotMetricStateForSourceMetricData(metricStateListState, sourceMetricData,
                        getRuntimeContext().getIndexOfThisSubtask());
            }
        }
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mongodb-cdc/src/main/java/org/apache/inlong/sort/cdc/mongodb/DebeziumSourceFunction.java`
#### Snippet
```java
        offsetState.clear();

        final DebeziumChangeFetcher<?> fetcher = this.debeziumChangeFetcher;

        byte[] serializedOffset = null;
        if (fetcher == null) {
            // the fetcher has not yet been initialized, which means we need to return the
            // originally restored offsets
            if (restoredOffsetState != null) {
                serializedOffset = restoredOffsetState.getBytes(StandardCharsets.UTF_8);
            }
        } else {
            byte[] currentState = fetcher.snapshotCurrentState();
            if (currentState == null && restoredOffsetState != null) {
                // the fetcher has been initialized, but has not yet received any data,
                // which means we need to return the originally restored offsets.
                serializedOffset = restoredOffsetState.getBytes(StandardCharsets.UTF_8);
            } else {
                serializedOffset = currentState;
            }
        }

        if (serializedOffset != null) {
            offsetState.add(serializedOffset);
            // the map cannot be asynchronously updated, because only one checkpoint call
            // can happen on this function at a time: either snapshotState() or
            // notifyCheckpointComplete()
            pendingOffsetsToCommit.put(checkpointId, serializedOffset);
            // truncate the map of pending offsets to commit, to prevent infinite growth
            while (pendingOffsetsToCommit.size() > MAX_NUM_PENDING_CHECKPOINTS) {
                pendingOffsetsToCommit.remove(0);
            }
        }
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mongodb-cdc/src/main/java/org/apache/inlong/sort/cdc/mongodb/DebeziumSourceFunction.java`
#### Snippet
```java
        schemaRecordsState.clear();

        if (engineInstanceName != null) {
            schemaRecordsState.add(engineInstanceName);
            Collection<SchemaRecord> records = DatabaseHistoryUtil.retrieveHistory(engineInstanceName);
            DocumentWriter writer = DocumentWriter.defaultWriter();
            for (SchemaRecord record : records) {
                schemaRecordsState.add(writer.write(record.toDocument()));
            }
        }
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mongodb-cdc/src/main/java/org/apache/inlong/sort/cdc/mongodb/DebeziumSourceFunction.java`
#### Snippet
```java
        final Method getMetricGroupMethod =
                getRuntimeContext().getClass().getMethod("getMetricGroup");
        getMetricGroupMethod.setAccessible(true);
        final MetricGroup metricGroup =
                (MetricGroup) getMetricGroupMethod.invoke(getRuntimeContext());

        metricGroup.gauge(
                "currentFetchEventTimeLag",
                (Gauge<Long>) () -> debeziumChangeFetcher.getFetchDelay());
        metricGroup.gauge(
                "currentEmitEventTimeLag",
                (Gauge<Long>) () -> debeziumChangeFetcher.getEmitDelay());
        metricGroup.gauge(
                "sourceIdleTime", (Gauge<Long>) () -> debeziumChangeFetcher.getIdleTime());
        MetricOption metricOption = MetricOption.builder()
                .withInlongLabels(inlongMetric)
                .withAuditAddress(inlongAudit)
                .withInitRecords(metricState != null ? metricState.getMetricValue(NUM_RECORDS_IN) : 0L)
                .withInitBytes(metricState != null ? metricState.getMetricValue(NUM_BYTES_IN) : 0L)
                .withRegisterMetric(RegisteredMetric.ALL)
                .build();
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mongodb-cdc/src/main/java/org/apache/inlong/sort/cdc/mongodb/DebeziumSourceFunction.java`
#### Snippet
```java
        properties.setProperty("name", "engine");
        properties.setProperty("offset.storage", FlinkOffsetBackingStore.class.getCanonicalName());
        if (restoredOffsetState != null) {
            // restored from state
            properties.setProperty(FlinkOffsetBackingStore.OFFSET_STATE_VALUE, restoredOffsetState);
        }
        // DO NOT include schema change, e.g. DDL
        properties.setProperty("include.schema.changes", "false");
        // disable the offset flush totally
        properties.setProperty("offset.flush.interval.ms", String.valueOf(Long.MAX_VALUE));
        // disable tombstones
        properties.setProperty("tombstones.on.delete", "false");
        if (engineInstanceName == null) {
            // not restore from recovery
            engineInstanceName = UUID.randomUUID().toString();
        }
        // history instance name to initialize FlinkDatabaseHistory
        properties.setProperty(
                FlinkDatabaseHistory.DATABASE_HISTORY_INSTANCE_NAME, engineInstanceName);
        // we have to use a persisted DatabaseHistory implementation, otherwise, recovery can't
        // continue to read binlog
        // see
        // https://stackoverflow.com/questions/57147584/debezium-error-schema-isnt-know-to-this-connector
        // and https://debezium.io/blog/2018/03/16/note-on-database-history-topic-configuration/
        properties.setProperty("database.history", determineDatabase().getCanonicalName());

        // we have to filter out the heartbeat events, otherwise the deserializer will fail
        String dbzHeartbeatPrefix =
                properties.getProperty(
                        Heartbeat.HEARTBEAT_TOPICS_PREFIX.name(),
                        Heartbeat.HEARTBEAT_TOPICS_PREFIX.defaultValueAsString());
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mongodb-cdc/src/main/java/org/apache/inlong/sort/cdc/mongodb/DebeziumSourceFunction.java`
#### Snippet
```java
                                    Struct value = (Struct) record.value();
                                    Struct ns = value.getStruct(MongoDBEnvelope.NAMESPACE_FIELD);
                                    if (null == ns) {
                                        ns = value.getStruct(RecordUtils.DOCUMENT_TO_FIELD);
                                    }
                                    String dbName = ns.getString(MongoDBEnvelope.NAMESPACE_DATABASE_FIELD);
                                    String collectionName =
                                            ns.getString(MongoDBEnvelope.NAMESPACE_COLLECTION_FIELD);
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mongodb-cdc/src/main/java/org/apache/inlong/sort/cdc/mongodb/DebeziumSourceFunction.java`
#### Snippet
```java
        this.engine =
                DebeziumEngine.create(Connect.class)
                        .using(properties)
                        .notifying(changeConsumer)
                        .using(OffsetCommitPolicy.always())
                        .using(
                                (success, message, error) -> {
                                    if (success) {
                                        // Close the handover and prepare to exit.
                                        handover.close();
                                    } else {
                                        handover.reportError(error);
                                    }
                                })
                        .build();

        // run the engine asynchronously
        executor.execute(engine);
        debeziumStarted = true;

        // start the real debezium consumer
        debeziumChangeFetcher.runFetchLoop();
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mongodb-cdc/src/main/java/org/apache/inlong/sort/cdc/mongodb/DebeziumSourceFunction.java`
#### Snippet
```java
        if (!debeziumStarted) {
            LOG.debug("notifyCheckpointComplete() called when engine is not started.");
            return;
        }

        final DebeziumChangeFetcher<T> fetcher = this.debeziumChangeFetcher;
        if (fetcher == null) {
            LOG.debug("notifyCheckpointComplete() called on uninitialized source");
            return;
        }

        try {
            final int posInMap = pendingOffsetsToCommit.indexOf(checkpointId);
            if (posInMap == -1) {
                LOG.warn(
                        "Consumer subtask {} received confirmation for unknown checkpoint id {}",
                        getRuntimeContext().getIndexOfThisSubtask(),
                        checkpointId);
                return;
            }

            byte[] serializedOffsets = (byte[]) pendingOffsetsToCommit.remove(posInMap);

            // remove older checkpoints in map
            for (int i = 0; i < posInMap; i++) {
                pendingOffsetsToCommit.remove(0);
            }

            if (serializedOffsets == null || serializedOffsets.length == 0) {
                LOG.debug(
                        "Consumer subtask {} has empty checkpoint state.",
                        getRuntimeContext().getIndexOfThisSubtask());
                return;
            }

            DebeziumOffset offset =
                    DebeziumOffsetSerializer.INSTANCE.deserialize(serializedOffsets);
            changeConsumer.commitOffset(offset);
        } catch (Exception e) {
            // ignore exception if we are no longer running
            LOG.warn("Ignore error when committing offset to database.", e);
        }
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mongodb-cdc/src/main/java/org/apache/inlong/sort/cdc/mongodb/DebeziumSourceFunction.java`
#### Snippet
```java
        try {
            if (engine != null) {
                engine.close();
            }
        } catch (IOException e) {
            ExceptionUtils.rethrow(e);
        } finally {
            if (executor != null) {
                executor.shutdownNow();
            }

            debeziumStarted = false;

            if (handover != null) {
                handover.close();
            }
        }
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mongodb-cdc/src/main/java/org/apache/inlong/sort/cdc/mongodb/MongoDBSource.java`
#### Snippet
```java
            StringBuilder sb = new StringBuilder(MONGODB_SCHEME).append("://");

            if (StringUtils.isNotEmpty(username) && StringUtils.isNotEmpty(password)) {
                sb.append(encodeValue(username))
                        .append(":")
                        .append(encodeValue(password))
                        .append("@");
            }

            sb.append(checkNotNull(hosts));

            if (StringUtils.isNotEmpty(connectionOptions)) {
                sb.append("/?").append(connectionOptions);
            }

            return new ConnectionString(sb.toString());
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mongodb-cdc/src/main/java/org/apache/inlong/sort/cdc/mongodb/debezium/internal/DebeziumChangeConsumer.java`
#### Snippet
```java
        if (currentCommitter == null) {
            LOG.info(
                    "commitOffset() called on Debezium change consumer which doesn't receive records yet.");
            return;
        }

        // only the offset is used
        SourceRecord recordWrapper =
                new SourceRecord(
                        offset.sourcePartition,
                        adjustSourceOffset((Map<String, Object>) offset.sourceOffset),
                        "DUMMY",
                        Schema.BOOLEAN_SCHEMA,
                        true);
        EmbeddedEngineChangeEvent<SourceRecord, SourceRecord> changeEvent =
                new EmbeddedEngineChangeEvent<>(null, recordWrapper, recordWrapper);
        currentCommitter.markProcessed(changeEvent);
        currentCommitter.markBatchFinished();
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mongodb-cdc/src/main/java/org/apache/inlong/sort/cdc/mongodb/debezium/internal/DebeziumChangeConsumer.java`
#### Snippet
```java
        if (sourceOffset.containsKey(LAST_COMPLETELY_PROCESSED_LSN_KEY)) {
            String value = sourceOffset.get(LAST_COMPLETELY_PROCESSED_LSN_KEY).toString();
            sourceOffset.put(LAST_COMPLETELY_PROCESSED_LSN_KEY, Long.parseLong(value));
        }
        if (sourceOffset.containsKey(LAST_COMMIT_LSN_KEY)) {
            String value = sourceOffset.get(LAST_COMMIT_LSN_KEY).toString();
            sourceOffset.put(LAST_COMMIT_LSN_KEY, Long.parseLong(value));
        }
        return sourceOffset;
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mongodb-cdc/src/main/java/org/apache/inlong/sort/cdc/mongodb/debezium/internal/DebeziumChangeFetcher.java`
#### Snippet
```java
        synchronized (checkpointLock) {
            T record;
            while ((record = records.poll()) != null) {
                emitDelay =
                        isInDbSnapshotPhase ? 0L : System.currentTimeMillis() - messageTimestamp;
                sourceContext.collect(record);
            }
            // update offset to state
            debeziumOffset.setSourcePartition(sourcePartition);
            debeziumOffset.setSourceOffset(sourceOffset);
        }
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mongodb-cdc/src/main/java/org/apache/inlong/sort/cdc/mongodb/debezium/internal/DebeziumChangeFetcher.java`
#### Snippet
```java
        Schema schema = record.valueSchema();
        Struct value = (Struct) record.value();
        if (schema.field(Envelope.FieldName.SOURCE) == null) {
            return;
        }

        Struct source = value.getStruct(Envelope.FieldName.SOURCE);
        if (source.schema().field(Envelope.FieldName.TIMESTAMP) == null) {
            return;
        }

        Long tsMs = source.getInt64(Envelope.FieldName.TIMESTAMP);
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mongodb-cdc/src/main/java/org/apache/inlong/sort/cdc/mongodb/debezium/internal/DebeziumChangeFetcher.java`
#### Snippet
```java
        Struct value = (Struct) record.value();
        if (value != null) {
            Struct source = value.getStruct(Envelope.FieldName.SOURCE);
            SnapshotRecord snapshotRecord = SnapshotRecord.fromSource(source);
            // even if it is the last record of snapshot, i.e. SnapshotRecord.LAST
            // we can still recover from checkpoint and continue to read the binlog,
            // because the checkpoint contains binlog position
            return SnapshotRecord.TRUE == snapshotRecord;
        }
        return false;
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mongodb-cdc/src/main/java/org/apache/inlong/sort/cdc/mongodb/source/reader/MongoDBRecordEmitter.java`
#### Snippet
```java
        long now = System.currentTimeMillis();
        // record the latest process time
        sourceReaderMetrics.recordProcessTime(now);
        Long messageTimestamp = getMessageTimestamp(element);

        if (messageTimestamp != null && messageTimestamp > 0L) {
            // report fetch delay
            Long fetchTimestamp = getFetchTimestamp(element);
            if (fetchTimestamp != null && fetchTimestamp >= messageTimestamp) {
                sourceReaderMetrics.recordFetchDelay(fetchTimestamp - messageTimestamp);
            }
            // report emit delay
            sourceReaderMetrics.recordEmitDelay(now - messageTimestamp);
        }
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mongodb-cdc/src/main/java/org/apache/inlong/sort/cdc/mongodb/source/utils/MetaDataUtils.java`
#### Snippet
```java
        String opType;
        switch (rowData.getRowKind()) {
            case DELETE:
            case UPDATE_BEFORE:
                opType = "d";
                break;
            case INSERT:
            case UPDATE_AFTER:
                opType = "c";
                break;
            default:
                throw new IllegalStateException("the record only have states in DELETE, "
                        + "UPDATE_BEFORE, INSERT and UPDATE_AFTER");
        }
        return opType;
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mongodb-cdc/src/main/java/org/apache/inlong/sort/cdc/mongodb/source/utils/MetaDataUtils.java`
#### Snippet
```java
        String opType;
        switch (rowData.getRowKind()) {
            case DELETE:
            case UPDATE_BEFORE:
                opType = "DELETE";
                break;
            case INSERT:
            case UPDATE_AFTER:
                opType = "INSERT";
                break;
            default:
                throw new IllegalStateException("the record only have states in DELETE, "
                        + "UPDATE_BEFORE, INSERT and UPDATE_AFTER");
        }
        return opType;
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mongodb-cdc/src/main/java/org/apache/inlong/sort/cdc/mongodb/source/utils/MetaDataUtils.java`
#### Snippet
```java
        GenericRowData data = (GenericRowData) rowData;
        Map<String, Object> field = (Map<String, Object>) data.getField(0);
        Map<String, String> mongoDbType = (Map<String, String>) data.getField(1);

        String database = getMetaData(record, MongoDBEnvelope.NAMESPACE_DATABASE_FIELD);
        String table = getMetaData(record, MongoDBEnvelope.NAMESPACE_COLLECTION_FIELD);
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mongodb-cdc/src/main/java/org/apache/inlong/sort/cdc/mongodb/table/MongoDBTableSource.java`
#### Snippet
```java
            Optional.ofNullable(databaseList).ifPresent(builder::databaseList);
            Optional.ofNullable(collectionList).ifPresent(builder::collectionList);
            Optional.ofNullable(username).ifPresent(builder::username);
            Optional.ofNullable(password).ifPresent(builder::password);
            Optional.ofNullable(connectionOptions).ifPresent(builder::connectionOptions);
            Optional.ofNullable(copyExisting).ifPresent(builder::copyExisting);
            Optional.ofNullable(batchSize).ifPresent(builder::batchSize);
            Optional.ofNullable(pollMaxBatchSize).ifPresent(builder::pollMaxBatchSize);
            Optional.ofNullable(pollAwaitTimeMillis).ifPresent(builder::pollAwaitTimeMillis);
            Optional.ofNullable(heartbeatIntervalMillis)
                    .ifPresent(builder::heartbeatIntervalMillis);
            Optional.ofNullable(splitMetaGroupSize).ifPresent(builder::splitMetaGroupSize);
            Optional.ofNullable(splitSizeMB).ifPresent(builder::splitSizeMB);
            Optional.ofNullable(inlongMetric).ifPresent(builder::inlongMetric);
            Optional.ofNullable(inlongAudit).ifPresent(builder::inlongAudit);
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/debezium/DebeziumSourceFunction.java`
#### Snippet
```java
                            @Override
                            public void deserialize(SourceRecord record, Collector<T> out) throws Exception {
                                deserializer.deserialize(record, new CallbackCollector<>(inputRow -> {
                                    if (sourceMetricData != null) {
                                        sourceMetricData.outputMetricsWithEstimate(record.value());
                                    }
                                    out.collect(inputRow);
                                }));
                            }
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/debezium/DebeziumSourceFunction.java`
#### Snippet
```java
        boolean isCompatibleWithLegacy =
                FlinkDatabaseHistory.isCompatible(DatabaseHistoryUtil.retrieveHistory(engineInstanceName));
        if (LEGACY_IMPLEMENTATION_VALUE.equals(properties.get(LEGACY_IMPLEMENTATION_KEY))) {
            // specifies the legacy implementation but the state may be incompatible
            if (isCompatibleWithLegacy) {
                return FlinkDatabaseHistory.class;
            } else {
                throw new IllegalStateException(
                        "The configured option 'debezium.internal.implementation' is 'legacy', "
                                + "but the state of source is incompatible with this implementation, "
                                + "you should remove the the option.");
            }
        } else if (FlinkDatabaseSchemaHistory.isCompatible(DatabaseHistoryUtil.retrieveHistory(engineInstanceName))) {
            // tries the non-legacy first
            return FlinkDatabaseSchemaHistory.class;
        } else if (isCompatibleWithLegacy) {
            // fallback to legacy if possible
            return FlinkDatabaseHistory.class;
        } else {
            // impossible
            throw new IllegalStateException("Can't determine which DatabaseHistory to use.");
        }
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/debezium/internal/DebeziumChangeFetcher.java`
#### Snippet
```java
        try {
            // begin snapshot database phase
            if (isInDbSnapshotPhase) {
                List<ChangeEvent<SourceRecord, SourceRecord>> events = handover.pollNext();

                synchronized (checkpointLock) {
                    LOG.info(
                            "Database snapshot phase can't perform checkpoint, acquired Checkpoint lock.");
                    handleBatch(events);
                    while (isRunning && isInDbSnapshotPhase) {
                        handleBatch(handover.pollNext());
                    }
                }
                LOG.info("Received record from streaming binlog phase, released checkpoint lock.");
            }

            // begin streaming binlog phase
            while (isRunning) {
                // If the handover is closed or has errors, exit.
                // If there is no streaming phase, the handover will be closed by the engine.
                handleBatch(handover.pollNext());
            }
        } catch (Handover.ClosedException e) {
            // ignore
        }
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/debezium/internal/DebeziumChangeFetcher.java`
#### Snippet
```java
        if (CollectionUtils.isEmpty(changeEvents)) {
            return;
        }
        this.processTime = System.currentTimeMillis();

        for (ChangeEvent<SourceRecord, SourceRecord> event : changeEvents) {
            SourceRecord record = event.value();
            updateMessageTimestamp(record);
            fetchDelay = isInDbSnapshotPhase ? 0L : processTime - messageTimestamp;

            if (isHeartbeatEvent(record)) {
                // keep offset update
                synchronized (checkpointLock) {
                    debeziumOffset.setSourcePartition(record.sourcePartition());
                    debeziumOffset.setSourceOffset(record.sourceOffset());
                }
                // drop heartbeat events
                continue;
            }

            deserialization.deserialize(record, debeziumCollector, getTableChange(record));

            if (!isSnapshotRecord(record)) {
                LOG.debug("Snapshot phase finishes.");
                isInDbSnapshotPhase = false;
            }

            // emit the actual records. this also updates offset state atomically
            emitRecordsUnderCheckpointLock(
                    debeziumCollector.records, record.sourcePartition(), record.sourceOffset());
        }
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/mysql/debezium/task/MySqlSnapshotSplitReadTask.java`
#### Snippet
```java
            ColumnUtils.ColumnArray columnArray = ColumnUtils.toArray(rs, table);
            long rows = 0;
            Threads.Timer logTimer = getTableScanLogTimer();

            while (rs.next()) {
                rows++;
                final Object[] row = new Object[columnArray.getGreatestColumnPosition()];
                for (int i = 0; i < columnArray.getColumns().length; i++) {
                    Column actualColumn = table.columns().get(i);
                    row[columnArray.getColumns()[i].position() - 1] =
                            readField(rs, i + 1, actualColumn, table);
                }
                if (logTimer.expired()) {
                    long stop = clock.currentTimeInMillis();
                    LOG.info(
                            "Exported {} records for split '{}' after {}",
                            rows,
                            snapshotSplit.splitId(),
                            Strings.duration(stop - exportStart));
                    snapshotProgressListener.rowsScanned(table.id(), rows);
                    logTimer = getTableScanLogTimer();
                }
                dispatcher.dispatchSnapshotEvent(
                        table.id(),
                        getChangeRecordEmitter(snapshotContext, table.id(), row),
                        snapshotReceiver);
            }
            LOG.info(
                    "Finished exporting {} records for split '{}', total duration '{}'",
                    rows,
                    snapshotSplit.splitId(),
                    Strings.duration(clock.currentTimeInMillis() - exportStart));
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/mysql/source/assigners/ChunkSplitter.java`
#### Snippet
```java
        final String splitColumnName = splitColumn.name();
        final Object[] minMaxOfSplitColumn = queryMinMax(jdbc, tableId, splitColumnName);
        final Object min = minMaxOfSplitColumn[0];
        final Object max = minMaxOfSplitColumn[1];
        if (min == null || max == null || min.equals(max)) {
            // empty table, or only one row, return full table scan as a chunk
            return Collections.singletonList(ChunkRange.all());
        }

        final int chunkSize = sourceConfig.getSplitSize();
        final double distributionFactorUpper = sourceConfig.getDistributionFactorUpper();
        final double distributionFactorLower = sourceConfig.getDistributionFactorLower();
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/mysql/source/assigners/ChunkSplitter.java`
#### Snippet
```java
        if (isEvenlySplitColumn(splitColumn)) {
            long approximateRowCnt = queryApproximateRowCnt(jdbc, tableId);
            double distributionFactor =
                    calculateDistributionFactor(tableId, min, max, approximateRowCnt);

            boolean dataIsEvenlyDistributed =
                    doubleCompare(distributionFactor, distributionFactorLower) >= 0
                            && doubleCompare(distributionFactor, distributionFactorUpper) <= 0;

            if (dataIsEvenlyDistributed) {
                // the minimum dynamic chunk size is at least 1
                final int dynamicChunkSize = Math.max((int) (distributionFactor * chunkSize), 1);
                return splitEvenlySizedChunks(
                        tableId, min, max, approximateRowCnt, dynamicChunkSize);
            } else {
                return splitUnevenlySizedChunks(
                        jdbc, tableId, splitColumnName, min, max, chunkSize);
            }
        } else {
            return splitUnevenlySizedChunks(jdbc, tableId, splitColumnName, min, max, chunkSize);
        }
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/mysql/source/assigners/ChunkSplitter.java`
#### Snippet
```java
        if (approximateRowCnt <= chunkSize) {
            // there is no more than one chunk, return full table as a chunk
            return Collections.singletonList(ChunkRange.all());
        }

        final List<ChunkRange> splits = new ArrayList<>();
        Object chunkStart = null;
        Object chunkEnd = ObjectUtils.plus(min, chunkSize);
        while (ObjectUtils.compare(chunkEnd, max) <= 0) {
            splits.add(ChunkRange.of(chunkStart, chunkEnd));
            chunkStart = chunkEnd;
            chunkEnd = ObjectUtils.plus(chunkEnd, chunkSize);
        }
        // add the ending split
        splits.add(ChunkRange.of(chunkStart, null));
        return splits;
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/mysql/source/assigners/ChunkSplitter.java`
#### Snippet
```java
        LOG.info(
                "Use unevenly-sized chunks for table {}, the chunk size is {}", tableId, chunkSize);
        final List<ChunkRange> splits = new ArrayList<>();
        Object chunkStart = null;
        Object chunkEnd = nextChunkEnd(jdbc, min, tableId, splitColumnName, max, chunkSize);
        int count = 0;
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/mysql/source/assigners/ChunkSplitter.java`
#### Snippet
```java
        if (!min.getClass().equals(max.getClass())) {
            throw new IllegalStateException(
                    String.format(
                            "Unsupported operation type, the MIN value type %s is different with MAX value type %s.",
                            min.getClass().getSimpleName(), max.getClass().getSimpleName()));
        }
        if (approximateRowCnt == 0) {
            return Double.MAX_VALUE;
        }
        BigDecimal difference = ObjectUtils.minus(max, min);
        // factor = (max - min + 1) / rowCount
        final BigDecimal subRowCnt = difference.add(BigDecimal.valueOf(1));
        double distributionFactor =
                subRowCnt.divide(new BigDecimal(approximateRowCnt), 4, ROUND_CEILING).doubleValue();
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/mysql/source/utils/StatementUtils.java`
#### Snippet
```java
final String condition;

        if (isFirstSplit && isLastSplit) {
            condition = null;
        } else if (isFirstSplit) {
            final StringBuilder sql = new StringBuilder();
            addPrimaryKeyColumnsToCondition(pkRowType, sql, " <= ?");
            if (isScanningData) {
                sql.append(" AND NOT (");
                addPrimaryKeyColumnsToCondition(pkRowType, sql, " = ?");
                sql.append(")");
            }
            condition = sql.toString();
        } else if (isLastSplit) {
            final StringBuilder sql = new StringBuilder();
            addPrimaryKeyColumnsToCondition(pkRowType, sql, " >= ?");
            condition = sql.toString();
        } else {
            final StringBuilder sql = new StringBuilder();
            addPrimaryKeyColumnsToCondition(pkRowType, sql, " >= ?");
            if (isScanningData) {
                sql.append(" AND NOT (");
                addPrimaryKeyColumnsToCondition(pkRowType, sql, " = ?");
                sql.append(")");
            }
            sql.append(" AND ");
            addPrimaryKeyColumnsToCondition(pkRowType, sql, " <= ?");
            condition = sql.toString();
        }

        if (isScanningData) {
            return buildSelectWithRowLimits(
                    tableId, limitSize, "*", Optional.ofNullable(condition), Optional.empty());
        } else {
            final String orderBy =
                    pkRowType.getFieldNames().stream().collect(Collectors.joining(", "));
            return buildSelectWithBoundaryRowLimits(
                    tableId,
                    limitSize,
                    getPrimaryKeyColumnsProjection(pkRowType),
                    getMaxPrimaryKeyColumnsProjection(pkRowType),
                    Optional.ofNullable(condition),
                    orderBy);
        }
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/mysql/source/utils/StatementUtils.java`
#### Snippet
```java
        try {
            final PreparedStatement statement = initStatement(jdbc, sql, fetchSize);
            if (isFirstSplit && isLastSplit) {
                return statement;
            }
            if (isFirstSplit) {
                for (int i = 0; i < primaryKeyNum; i++) {
                    statement.setObject(i + 1, splitEnd[i]);
                    statement.setObject(i + 1 + primaryKeyNum, splitEnd[i]);
                }
            } else if (isLastSplit) {
                for (int i = 0; i < primaryKeyNum; i++) {
                    statement.setObject(i + 1, splitStart[i]);
                }
            } else {
                for (int i = 0; i < primaryKeyNum; i++) {
                    statement.setObject(i + 1, splitStart[i]);
                    statement.setObject(i + 1 + primaryKeyNum, splitEnd[i]);
                    statement.setObject(i + 1 + 2 * primaryKeyNum, splitEnd[i]);
                }
            }
            return statement;
        } catch (Exception e) {
            throw new RuntimeException("Failed to build the split data read statement.", e);
        }
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/mysql/source/utils/StatementUtils.java`
#### Snippet
```java
        StringBuilder sql = new StringBuilder();
        for (Iterator<String> fieldNamesIt = pkRowType.getFieldNames().iterator(); fieldNamesIt.hasNext();) {
            sql.append(fieldNamesIt.next());
            if (fieldNamesIt.hasNext()) {
                sql.append(" , ");
            }
        }
        return sql.toString();
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/mysql/source/utils/StatementUtils.java`
#### Snippet
```java
        StringBuilder sql = new StringBuilder();
        for (Iterator<String> fieldNamesIt = pkRowType.getFieldNames().iterator(); fieldNamesIt.hasNext();) {
            sql.append("MAX(" + fieldNamesIt.next() + ")");
            if (fieldNamesIt.hasNext()) {
                sql.append(" , ");
            }
        }
        return sql.toString();
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/mysql/table/MySqlReadableMetadata.java`
#### Snippet
```java
                    if (tableSchema == null) {
                        return null;
                    }
                    Map<StringData, Integer> mysqlType = new HashMap<>();
                    final Table table = tableSchema.getTable();
                    table.columns()
                            .forEach(
                                    column -> {
                                        mysqlType.put(
                                                StringData.fromString(column.name()),
                                                column.jdbcType());
                                    });

                    return new GenericMapData(mysqlType);
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/postgres-cdc/src/main/java/org/apache/inlong/sort/cdc/postgres/table/PostgreSQLReadableMetaData.java`
#### Snippet
```java
                    Map<StringData, Integer> postgresType = new HashMap<>();
                    final Table table = tableSchema.getTable();
                    table.columns().forEach(column -> {
                        postgresType.put(StringData.fromString(column.name()), column.jdbcType());
                    });

                    return new GenericMapData(postgresType);
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/mysql/table/MySqlTableInlongSourceFactory.java`
#### Snippet
```java
            validateIntegerOption(SCAN_INCREMENTAL_SNAPSHOT_CHUNK_SIZE, splitSize, 1);
            validateIntegerOption(CHUNK_META_GROUP_SIZE, splitMetaGroupSize, 1);
            validateIntegerOption(SCAN_SNAPSHOT_FETCH_SIZE, fetchSize, 1);
            validateIntegerOption(CONNECTION_POOL_SIZE, connectionPoolSize, 1);
            validateIntegerOption(CONNECT_MAX_RETRIES, connectMaxRetries, 0);
            validateDistributionFactorUpper(distributionFactorUpper);
            validateDistributionFactorLower(distributionFactorLower);
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/mysql/utils/MetaDataUtils.java`
#### Snippet
```java
        if (tableSchema == null) {
            return null;
        }
        Map<String, Integer> sqlType = new LinkedHashMap<>();
        final Table table = tableSchema.getTable();
        table.columns().forEach(
                column -> sqlType.put(column.name(), column.jdbcType()));
        return sqlType;
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/mysql/utils/MetaDataUtils.java`
#### Snippet
```java
        String opType;
        final Envelope.Operation op = Envelope.operationFor(record);
        if (op == Envelope.Operation.CREATE || op == Envelope.Operation.READ) {
            opType = "INSERT";
        } else if (op == Envelope.Operation.DELETE) {
            opType = "DELETE";
        } else {
            opType = "UPDATE";
        }
        return opType;
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/sqlserver-cdc/src/main/java/org/apache/inlong/sort/cdc/sqlserver/SqlServerSource.java`
#### Snippet
```java
            props.setProperty("database.server.name", DATABASE_SERVER_NAME);
            props.setProperty("database.hostname", checkNotNull(hostname));
            props.setProperty("database.user", checkNotNull(username));
            props.setProperty("database.password", checkNotNull(password));
            props.setProperty("database.port", String.valueOf(port));
            props.setProperty("database.history.skip.unparseable.ddl", String.valueOf(true));
            props.setProperty("database.dbname", checkNotNull(database));
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/oracle-cdc/src/main/java/org/apache/inlong/sort/cdc/oracle/OracleSource.java`
#### Snippet
```java
            props.setProperty("database.hostname", checkNotNull(hostname));
            props.setProperty("database.user", checkNotNull(username));
            props.setProperty("database.password", checkNotNull(password));
            props.setProperty("database.port", String.valueOf(port));
            props.setProperty("database.history.skip.unparseable.ddl", String.valueOf(true));
            props.setProperty("database.dbname", checkNotNull(database));
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/oracle-cdc/src/main/java/org/apache/inlong/sort/cdc/oracle/debezium/DebeziumSourceFunction.java`
#### Snippet
```java
new DebeziumDeserializationSchema<T>() {

                            @Override
                            public void deserialize(SourceRecord record, Collector<T> out) throws Exception {
                                deserializer.deserialize(record, new CallbackCollector<>(inputRow -> {
                                    if (sourceMetricData != null) {
                                        sourceMetricData.outputMetricsWithEstimate(record.value());
                                    }
                                    out.collect(inputRow);
                                }));
                            }

                            @Override
                            public void deserialize(SourceRecord record, Collector<T> out,
                                    TableChange tableSchema) throws Exception {
                                deserializer.deserialize(record, new CallbackCollector<>(inputRow -> {
                                    if (sourceMetricData != null && record != null && sourceMultipleEnable) {
                                        Struct value = (Struct) record.value();
                                        Struct source = value.getStruct(Envelope.FieldName.SOURCE);
                                        String dbName = source.getString(AbstractSourceInfo.DATABASE_NAME_KEY);
                                        String schemaName = source.getString(AbstractSourceInfo.SCHEMA_NAME_KEY);
                                        String tableName = source.getString(AbstractSourceInfo.TABLE_NAME_KEY);
                                        SnapshotRecord snapshotRecord = SnapshotRecord.fromSource(source);
                                        boolean isSnapshotRecord = (SnapshotRecord.TRUE == snapshotRecord);
                                        sourceMetricData
                                                .outputMetricsWithEstimate(dbName, schemaName, tableName,
                                                        isSnapshotRecord, value);
                                    } else if (sourceMetricData != null && record != null) {
                                        sourceMetricData.outputMetricsWithEstimate(record.value());
                                    }
                                    out.collect(inputRow);
                                }), tableSchema);
                            }

                            @Override
                            public TypeInformation<T> getProducedType() {
                                return deserializer.getProducedType();
                            }
                        }
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/oracle-cdc/src/main/java/org/apache/inlong/sort/cdc/oracle/source/reader/fetch/OracleStreamFetchTask.java`
#### Snippet
```java
            Map<String, String> offsetStrMap = new HashMap<>();
            for (Map.Entry<String, ?> entry : offset.entrySet()) {
                offsetStrMap.put(
                        entry.getKey(),
                        entry.getValue() == null ? null : entry.getValue().toString());
            }
            return new RedoLogOffset(offsetStrMap);
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/oracle-cdc/src/main/java/org/apache/inlong/sort/cdc/oracle/table/OracleDeserializationConverterFactory.java`
#### Snippet
```java
        switch (type.getTypeRoot()) {
            case BOOLEAN:
                return createBooleanConverter();
            case TINYINT:
                return createByteConverter();
            case SMALLINT:
                return createShortConverter();
            case INTEGER:
                return createIntegerConverter();
            case BIGINT:
                return createLongConverter();
            case FLOAT:
                return createFloatConverter();
            case DOUBLE:
                return createDoubleConverter();
            default:
                // fallback to default converter
                return Optional.empty();
        }
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/oracle-cdc/src/main/java/org/apache/inlong/sort/cdc/oracle/table/OracleDeserializationConverterFactory.java`
#### Snippet
```java
                    @Override
                    public Object convert(Object dbzObj, Schema schema) throws Exception {
                        if (VariableScaleDecimal.LOGICAL_NAME.equals(schema.name())) {
                            SpecialValueDecimal decimal =
                                    VariableScaleDecimal.toLogical((Struct) dbzObj);
                            return converter.convert(
                                    decimal.getDecimalValue().orElse(BigDecimal.ZERO),
                                    schema);
                        }
                        return converter.convert(dbzObj, schema);
                    }
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/oracle-cdc/src/main/java/org/apache/inlong/sort/cdc/oracle/table/OracleDeserializationConverterFactory.java`
#### Snippet
```java
                    @Override
                    public Object convert(Object dbzObj, Schema schema) {
                        if (dbzObj instanceof Boolean) {
                            return dbzObj;
                        } else if (dbzObj instanceof Byte) {
                            return (byte) dbzObj != 0;
                        } else if (dbzObj instanceof Short) {
                            return (short) dbzObj != 0;
                        } else if (dbzObj instanceof BigDecimal) {
                            return ((BigDecimal) dbzObj).shortValue() != 0;
                        } else {
                            return Boolean.parseBoolean(dbzObj.toString());
                        }
                    }
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/oracle-cdc/src/main/java/org/apache/inlong/sort/cdc/oracle/table/OracleDeserializationConverterFactory.java`
#### Snippet
```java
                    @Override
                    public Object convert(Object dbzObj, Schema schema) {
                        if (dbzObj instanceof Byte) {
                            return ((Byte) dbzObj).shortValue();
                        } else if (dbzObj instanceof Short) {
                            return dbzObj;
                        } else if (dbzObj instanceof BigDecimal) {
                            return ((BigDecimal) dbzObj).shortValue();
                        } else {
                            return Short.parseShort(dbzObj.toString());
                        }
                    }
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/oracle-cdc/src/main/java/org/apache/inlong/sort/cdc/oracle/table/OracleDeserializationConverterFactory.java`
#### Snippet
```java
                    @Override
                    public Object convert(Object dbzObj, Schema schema) {
                        if (dbzObj instanceof Integer) {
                            return dbzObj;
                        } else if (dbzObj instanceof Long) {
                            return ((Long) dbzObj).intValue();
                        } else if (dbzObj instanceof BigDecimal) {
                            return ((BigDecimal) dbzObj).intValue();
                        } else {
                            return Integer.parseInt(dbzObj.toString());
                        }
                    }
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/oracle-cdc/src/main/java/org/apache/inlong/sort/cdc/oracle/table/OracleDeserializationConverterFactory.java`
#### Snippet
```java
                    @Override
                    public Object convert(Object dbzObj, Schema schema) {
                        if (dbzObj instanceof Integer) {
                            return ((Integer) dbzObj).longValue();
                        } else if (dbzObj instanceof Long) {
                            return dbzObj;
                        } else if (dbzObj instanceof BigDecimal) {
                            return ((BigDecimal) dbzObj).longValue();
                        } else {
                            return Long.parseLong(dbzObj.toString());
                        }
                    }
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/oracle-cdc/src/main/java/org/apache/inlong/sort/cdc/oracle/table/OracleDeserializationConverterFactory.java`
#### Snippet
```java
                    @Override
                    public Object convert(Object dbzObj, Schema schema) {
                        if (dbzObj instanceof Float) {
                            return dbzObj;
                        } else if (dbzObj instanceof Double) {
                            return ((Double) dbzObj).floatValue();
                        } else if (dbzObj instanceof BigDecimal) {
                            return ((BigDecimal) dbzObj).floatValue();
                        } else {
                            return Float.parseFloat(dbzObj.toString());
                        }
                    }
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/oracle-cdc/src/main/java/org/apache/inlong/sort/cdc/oracle/table/OracleDeserializationConverterFactory.java`
#### Snippet
```java
                    @Override
                    public Object convert(Object dbzObj, Schema schema) {
                        if (dbzObj instanceof Float) {
                            return ((Float) dbzObj).doubleValue();
                        } else if (dbzObj instanceof Double) {
                            return dbzObj;
                        } else if (dbzObj instanceof BigDecimal) {
                            return ((BigDecimal) dbzObj).doubleValue();
                        } else {
                            return Double.parseDouble(dbzObj.toString());
                        }
                    }
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/oracle-cdc/src/main/java/org/apache/inlong/sort/cdc/oracle/table/OracleReadableMetaData.java`
#### Snippet
```java
        Struct messageStruct = (Struct) record.value();
        Struct sourceStruct = messageStruct.getStruct(FieldName.SOURCE);
        // tableName
        String tableName = getMetaData(record, AbstractSourceInfo.TABLE_NAME_KEY);
        // databaseName
        String databaseName = getMetaData(record, AbstractSourceInfo.DATABASE_NAME_KEY);
        // schemaName
        String schemaName = getMetaData(record, AbstractSourceInfo.SCHEMA_NAME_KEY);
        // opTs
        long opTs = (Long) sourceStruct.get(AbstractSourceInfo.TIMESTAMP_KEY);
        // ts
        long ts = (Long) messageStruct.get(FieldName.TIMESTAMP);
        // actual data
        GenericRowData data = rowData;
        Map<String, Object> field = (Map<String, Object>) data.getField(0);
        List<Map<String, Object>> dataList = new ArrayList<>();
        dataList.add(field);
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/oracle-cdc/src/main/java/org/apache/inlong/sort/cdc/oracle/table/OracleTableSourceFactory.java`
#### Snippet
```java
        final ReadableConfig config = helper.getOptions();
        String hostname = config.get(HOSTNAME);
        String username = config.get(USERNAME);
        String password = config.get(PASSWORD);
        String databaseName = config.get(DATABASE_NAME);
        String tableName = config.get(TABLE_NAME);
        String schemaName = config.get(SCHEMA_NAME);
        int port = config.get(PORT);
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/postgres-cdc/src/main/java/org/apache/inlong/sort/cdc/postgres/table/PostgreSQLTableFactory.java`
#### Snippet
```java
        final FactoryUtil.TableFactoryHelper helper =
                FactoryUtil.createTableFactoryHelper(this, context);
        helper.validateExcept(DEBEZIUM_OPTIONS_PREFIX);

        final ReadableConfig config = helper.getOptions();
        String hostname = config.get(HOSTNAME);
        String username = config.get(USERNAME);
        String password = config.get(PASSWORD);
        String databaseName = config.get(DATABASE_NAME);
        String schemaName = config.get(SCHEMA_NAME);
        String tableName = config.get(TABLE_NAME);
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/pulsar/src/main/java/org/apache/inlong/sort/pulsar/internal/FlinkPulsarSource.java`
#### Snippet
```java
        if (this.deserializer != null) {
            DynamicPulsarDeserializationSchema dynamicKafkaDeserializationSchema =
                    (DynamicPulsarDeserializationSchema) deserializer;
            dynamicKafkaDeserializationSchema.setMetricData(sourceMetricData);

            this.deserializer.open(
                    RuntimeContextInitializationContextAdapters.deserializationAdapter(
                            getRuntimeContext(),
                            metricGroup -> metricGroup.addGroup("user")));
        }
        this.taskIndex = getRuntimeContext().getIndexOfThisSubtask();
        this.numParallelTasks = getRuntimeContext().getNumberOfParallelSubtasks();

        this.metadataReader = createMetadataReader();

        ownedTopicStarts = new HashMap<>();
        excludeStartMessageIds = new HashSet<>();
        Set<TopicRange> allTopics = metadataReader.discoverTopicChanges();
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/pulsar/src/main/java/org/apache/inlong/sort/pulsar/internal/FlinkPulsarSource.java`
#### Snippet
```java
boolean usingRestoredState = (startupMode != StartupMode.EXTERNAL_SUBSCRIPTION) || stateSubEqualexternalSub;

        if (restoredState != null && usingRestoredState) {
            allTopicOffsets.entrySet().stream()
                    .filter(e -> !restoredState.containsKey(e.getKey()))
                    .forEach(e -> restoredState.put(e.getKey(), e.getValue()));

            SerializableRange subTaskRange = metadataReader.getRange();
            restoredState.entrySet().stream()
                    .filter(
                            e -> SourceSinkUtils.belongsTo(
                                    e.getKey().getTopic(),
                                    subTaskRange,
                                    numParallelTasks,
                                    taskIndex))
                    .forEach(
                            e -> {
                                TopicRange tr =
                                        new TopicRange(
                                                e.getKey().getTopic(),
                                                subTaskRange.getPulsarRange());
                                ownedTopicStarts.put(tr, e.getValue());
                                excludeStartMessageIds.add(e.getKey());
                            });

            Set<TopicRange> goneTopics =
                    Sets.difference(restoredState.keySet(), allTopics).stream()
                            .filter(
                                    k -> SourceSinkUtils.belongsTo(
                                            k.getTopic(),
                                            subTaskRange,
                                            numParallelTasks,
                                            taskIndex))
                            .map(k -> new TopicRange(k.getTopic(), subTaskRange.getPulsarRange()))
                            .collect(Collectors.toSet());

            for (TopicRange goneTopic : goneTopics) {
                log.warn(goneTopic + " is removed from subscription since " +
                        "it no longer matches with topics settings.");
                ownedTopicStarts.remove(goneTopic);
            }

            log.info("Source {} will start reading {} topics in restored state {}",
                    taskIndex, ownedTopicStarts.size(), StringUtils.join(ownedTopicStarts.entrySet()));
        } else {
            ownedTopicStarts.putAll(
                    allTopicOffsets.entrySet().stream()
                            .collect(Collectors.toMap(Map.Entry::getKey, Map.Entry::getValue)));
            if (ownedTopicStarts.isEmpty()) {
                log.info("Source {} initially has no topics to read from.", taskIndex);
            } else {
                log.info("Source {} will start reading {} topics from initialized positions: {}",
                        taskIndex, ownedTopicStarts.size(), ownedTopicStarts);
            }
        }
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/pulsar/src/main/java/org/apache/inlong/sort/pulsar/internal/FlinkPulsarSource.java`
#### Snippet
```java
        AtomicReference<Exception> discoveryLoopErrorRef = new AtomicReference<>();
        createAndStartDiscoveryLoop(discoveryLoopErrorRef);

        pulsarFetcher.runFetchLoop();

        joinDiscoveryLoopThread();

        Exception discoveryLoopError = discoveryLoopErrorRef.get();
        if (discoveryLoopError != null) {
            throw new RuntimeException(discoveryLoopError);
        }
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/pulsar/src/main/java/org/apache/inlong/sort/pulsar/internal/FlinkPulsarSource.java`
#### Snippet
```java
        discoveryLoopThread = new Thread(
                () -> {
                    try {
                        while (running) {
                            Set<TopicRange> added = metadataReader.discoverTopicChanges();

                            if (running && !added.isEmpty()) {
                                pulsarFetcher.addDiscoveredTopics(added);
                            }

                            if (running && discoveryIntervalMillis != -1) {
                                Thread.sleep(discoveryIntervalMillis);
                            }
                        }
                    } catch (PulsarMetadataReader.ClosedException e) {
                        // break out while and do nothing
                    } catch (InterruptedException e) {
                        // break out while and do nothing
                    } catch (Exception e) {
                        discoveryLoopErrorRef.set(e);
                    } finally {
                        if (running) {
                            // calling cancel will also let the fetcher loop escape
                            // (if not running, cancel() was already called)
                            cancel();
                        }
                    }
                }, "Pulsar topic discovery for source " + taskIndex);
        discoveryLoopThread.start();
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/pulsar/src/main/java/org/apache/inlong/sort/pulsar/internal/FlinkPulsarSource.java`
#### Snippet
```java
        running = false;

        if (discoveryLoopThread != null) {
            discoveryLoopThread.interrupt();
        }

        if (pulsarFetcher != null) {
            try {
                pulsarFetcher.cancel();
            } catch (Exception e) {
                log.error("Failed to cancel the Pulsar Fetcher {}", ExceptionUtils.stringifyException(e));
                throw new RuntimeException(e);
            }
        }
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/pulsar/src/main/java/org/apache/inlong/sort/pulsar/internal/FlinkPulsarSource.java`
#### Snippet
```java
            if (!iterator.hasNext()) {
                iterator = tryMigrateState(stateStore);
            }
            while (iterator.hasNext()) {
                final Tuple2<TopicSubscription, MessageId> tuple2 = iterator.next();
                final SerializableRange range =
                        tuple2.f0.getRange() != null ? tuple2.f0.getRange() : SerializableRange.ofFullRange();
                final TopicRange topicRange =
                        new TopicRange(tuple2.f0.getTopic(), range.getPulsarRange());
                restoredState.put(topicRange, tuple2.f1);
                String subscriptionName = tuple2.f0.getSubscriptionName();
                if (!stateSubEqualexternalSub && StringUtils.equals(subscriptionName, externalSubscriptionName)) {
                    stateSubEqualexternalSub = true;
                    log.info("Source restored state with subscriptionName {}", subscriptionName);
                }
            }
            log.info("Source subtask {} restored state {}",
                    taskIndex,
                    StringUtils.join(restoredState.entrySet()));
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/pulsar/src/main/java/org/apache/inlong/sort/pulsar/internal/FlinkPulsarSource.java`
#### Snippet
```java
        TypeSerializer<?>[] fieldSerializers =
                new TypeSerializer<?>[]{
                        TopicSubscriptionSerializer.INSTANCE,
                        MessageIdSerializer.INSTANCE
                };
        @SuppressWarnings("unchecked")
        Class<Tuple2<TopicSubscription, MessageId>> tupleClass =
                (Class<Tuple2<TopicSubscription, MessageId>>) (Class<?>) Tuple2.class;
        return new TupleSerializer<>(tupleClass, fieldSerializers);
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/pulsar/src/main/java/org/apache/inlong/sort/pulsar/internal/FlinkPulsarSource.java`
#### Snippet
```java
log.info("restore old state version {}", oldStateVersion);
        PulsarSourceStateSerializer stateSerializer =
                new PulsarSourceStateSerializer(getRuntimeContext().getExecutionConfig());
        // Since stateStore.getUnionListState gets the data of a state point,
        // it can only be registered once and will fail to register again,
        // so it only allows the user to set a version number.
        ListState<?> rawStates = stateStore.getUnionListState(new ListStateDescriptor<>(
                OFFSETS_STATE_NAME,
                stateSerializer.getSerializer(oldStateVersion)));

        ListState<String> oldUnionSubscriptionNameStates =
                stateStore.getUnionListState(
                        new ListStateDescriptor<>(
                                OFFSETS_STATE_NAME + "_subName",
                                TypeInformation.of(new TypeHint<String>() {
                                })));
        final Iterator<String> subNameIterator = oldUnionSubscriptionNameStates.get().iterator();

        Iterator<?> tuple2s = rawStates.get().iterator();
        log.info("restore old state has data {}", tuple2s.hasNext());
        final List<Tuple2<TopicSubscription, MessageId>> records = new ArrayList<>();
        while (tuple2s.hasNext()) {
            final Object next = tuple2s.next();
            Tuple2<TopicSubscription, MessageId> tuple2 = stateSerializer.deserialize(oldStateVersion, next);

            String subName = tuple2.f0.getSubscriptionName();
            if (subNameIterator.hasNext()) {
                subName = subNameIterator.next();
            }
            final TopicSubscription topicSubscription = TopicSubscription.builder()
                    .topic(tuple2.f0.getTopic())
                    .range(tuple2.f0.getRange())
                    .subscriptionName(subName)
                    .build();
            final Tuple2<TopicSubscription, MessageId> record = Tuple2.of(topicSubscription, tuple2.f1);
            log.info("migrationState {}", record);
            records.add(record);
        }
        rawStates.clear();
        oldUnionSubscriptionNameStates.clear();
        return records.listIterator();
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/pulsar/src/main/java/org/apache/inlong/sort/pulsar/internal/FlinkPulsarSource.java`
#### Snippet
```java
unionOffsetStates.clear();

            PulsarFetcher<T> fetcher = this.pulsarFetcher;

            if (fetcher == null) {
                // the fetcher has not yet been initialized, which means we need to return the
                // originally restored offsets or the assigned partitions
                for (Map.Entry<TopicRange, MessageId> entry : ownedTopicStarts.entrySet()) {
                    final TopicSubscription topicSubscription = TopicSubscription.builder()
                            .topic(entry.getKey().getTopic())
                            .range(entry.getKey().getRange())
                            .subscriptionName(getSubscriptionName())
                            .build();
                    unionOffsetStates.add(Tuple2.of(topicSubscription, entry.getValue()));
                }
                pendingOffsetsToCommit.put(context.getCheckpointId(), restoredState);
            } else {
                Map<TopicRange, MessageId> currentOffsets = fetcher.snapshotCurrentState();
                pendingOffsetsToCommit.put(context.getCheckpointId(), currentOffsets);
                for (Map.Entry<TopicRange, MessageId> entry : currentOffsets.entrySet()) {
                    final TopicSubscription topicSubscription = TopicSubscription.builder()
                            .topic(entry.getKey().getTopic())
                            .range(entry.getKey().getRange())
                            .subscriptionName(getSubscriptionName())
                            .build();
                    unionOffsetStates.add(Tuple2.of(topicSubscription, entry.getValue()));
                }

                int exceed = pendingOffsetsToCommit.size() - MAX_NUM_PENDING_CHECKPOINTS;
                Iterator<Long> iterator = pendingOffsetsToCommit.keySet().iterator();

                while (iterator.hasNext() && exceed > 0) {
                    iterator.next();
                    iterator.remove();
                }
            }
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/pulsar/src/main/java/org/apache/inlong/sort/pulsar/internal/FlinkPulsarSource.java`
#### Snippet
```java
        if (!running) {
            log.info("notifyCheckpointComplete() called on closed source");
            return;
        }

        PulsarFetcher<T> fetcher = this.pulsarFetcher;

        if (fetcher == null) {
            log.info("notifyCheckpointComplete() called on uninitialized source");
            return;
        }

        log.debug("Source {} received confirmation for unknown checkpoint id {}",
                taskIndex, checkpointId);
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/pulsar/src/main/java/org/apache/inlong/sort/pulsar/internal/FlinkPulsarSource.java`
#### Snippet
```java
            if (!pendingOffsetsToCommit.containsKey(checkpointId)) {
                log.warn("Source {} received confirmation for unknown checkpoint id {}",
                        taskIndex, checkpointId);
                return;
            }

            Map<TopicRange, MessageId> offset = pendingOffsetsToCommit.get(checkpointId);

            // remove older checkpoints in map
            Iterator<Long> iterator = pendingOffsetsToCommit.keySet().iterator();
            while (iterator.hasNext()) {
                Long key = iterator.next();
                iterator.remove();
                if (Objects.equals(key, checkpointId)) {
                    break;
                }
            }

            if (offset == null || offset.size() == 0) {
                log.debug("Source {} has empty checkpoint state", taskIndex);
                return;
            }
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/pulsar/src/main/java/org/apache/inlong/sort/pulsar/table/PulsarDynamicTableFactory.java`
#### Snippet
```java
        FactoryUtil.TableFactoryHelper helper = FactoryUtil.createTableFactoryHelper(this, context);
        ReadableConfig tableOptions = helper.getOptions();

        List<String> topics = generateTopic(context.getObjectIdentifier(), tableOptions);
        if (topics != null && !topics.isEmpty()) {
            ((Configuration) tableOptions).set(TOPIC, Collections.singletonList(topics.get(0)));
        }
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/pulsar/src/main/java/org/apache/inlong/sort/pulsar/table/PulsarDynamicTableFactory.java`
#### Snippet
```java
        final DataType physicalDataType = context.getCatalogTable().getSchema().toPhysicalRowDataType();

        final int[] keyProjection = createKeyFormatProjection(tableOptions, physicalDataType);

        final int[] valueProjection = createValueFormatProjection(tableOptions, physicalDataType);

        final String keyPrefix = tableOptions.getOptional(KEY_FIELDS_PREFIX).orElse(null);

        String adminUrl = tableOptions.get(ADMIN_URL);
        String serviceUrl = tableOptions.get(SERVICE_URL);
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/pulsar/src/main/java/org/apache/inlong/sort/pulsar/table/PulsarDynamicTableFactory.java`
#### Snippet
```java
        final int[] keyProjection = createKeyFormatProjection(tableOptions, physicalDataType);

        final int[] valueProjection = createValueFormatProjection(tableOptions, physicalDataType);

        final String keyPrefix = tableOptions.getOptional(KEY_FIELDS_PREFIX).orElse(null);

        String adminUrl = tableOptions.get(ADMIN_URL);
        String serverUrl = tableOptions.get(SERVICE_URL);
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/redis/src/main/java/org/apache/inlong/sort/redis/sink/RedisPlainSinkFunction.java`
#### Snippet
```java
        for (Tuple3<Boolean, String, String> row : rows) {
            String key = row.f1;
            String value = row.f2;
            if (row.f0) {
                if (expireTime != null) {
                    redisCommandsContainer.setex(key, value, expireTime);
                } else {
                    redisCommandsContainer.set(key, value);
                }
            } else {
                redisCommandsContainer.del(key);
            }
        }
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/tubemq/src/main/java/org/apache/inlong/sort/tubemq/FlinkTubeMQConsumer.java`
#### Snippet
```java
        TypeInformation<Tuple2<String, Long>> typeInformation =
                new TupleTypeInfo<>(STRING_TYPE_INFO, LONG_TYPE_INFO);
        ListStateDescriptor<Tuple2<String, Long>> stateDescriptor =
                new ListStateDescriptor<>(TUBE_OFFSET_STATE, typeInformation);

        OperatorStateStore stateStore = context.getOperatorStateStore();
        offsetsState = stateStore.getListState(stateDescriptor);
        currentOffsets = new HashMap<>();
        if (context.isRestored()) {
            for (Tuple2<String, Long> tubeOffset : offsetsState.get()) {
                currentOffsets.put(tubeOffset.f0, tubeOffset.f1);
            }
            LOG.info("Successfully restore the offsets {}.", currentOffsets);
        } else {
            LOG.info("No restore offsets.");
        }
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/tubemq/src/main/java/org/apache/inlong/sort/tubemq/FlinkTubeMQConsumer.java`
#### Snippet
```java
        consumerConfig.setMsgNotFoundWaitPeriodMs(messageNotFoundWaitPeriod.toMillis());

        final int numTasks = getRuntimeContext().getNumberOfParallelSubtasks();
        messageSessionFactory = new TubeSingleSessionFactory(consumerConfig);
        messagePullConsumer = messageSessionFactory.createPullConsumer(consumerConfig);
        messagePullConsumer.subscribe(topic, tidSet);
        messagePullConsumer.completeSubscribe(sessionKey, numTasks, true, currentOffsets);

        running = true;
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/tubemq/src/main/java/org/apache/inlong/sort/tubemq/FlinkTubeMQConsumer.java`
#### Snippet
```java
        cancel();

        if (messagePullConsumer != null) {
            try {
                messagePullConsumer.shutdown();
            } catch (Throwable t) {
                LOG.warn("Could not properly shutdown the tubemq pull consumer.", t);
            }
        }

        if (messageSessionFactory != null) {
            try {
                messageSessionFactory.shutdown();
            } catch (Throwable t) {
                LOG.warn("Could not properly shutdown the tubemq session factory.", t);
            }
        }

        super.close();

        LOG.info("Closed the tubemq source.");
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/tubemq/src/main/java/org/apache/inlong/sort/tubemq/table/DynamicTubeMQDeserializationSchema.java`
#### Snippet
```java
        if (metadataConverters.length == 0) {
            out.collect(physicalRow);
            return;
        }
        final int physicalArity = physicalRow.getArity();
        final int metadataArity = metadataConverters.length;
        final GenericRowData producedRow =
                new GenericRowData(physicalRow.getRowKind(), physicalArity + metadataArity);
        for (int physicalPos = 0; physicalPos < physicalArity; physicalPos++) {
            producedRow.setField(physicalPos, physicalRow.getField(physicalPos));
        }
        for (int metadataPos = 0; metadataPos < metadataArity; metadataPos++) {
            producedRow.setField(
                    physicalArity + metadataPos, metadataConverters[metadataPos].read(head));
        }
        out.collect(producedRow);
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-formats/format-json/src/main/java/org/apache/inlong/sort/formats/json/canal/CanalJsonEnhancedDeserializationSchema.java`
#### Snippet
```java
        final int physicalArity = physicalRow.getArity();
        final int metadataArity = metadataConverters.length;
        final GenericRowData producedRow =
                new GenericRowData(physicalRow.getRowKind(), physicalArity + metadataArity);
        for (int physicalPos = 0; physicalPos < physicalArity; physicalPos++) {
            producedRow.setField(physicalPos, physicalRow.getField(physicalPos));
        }
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-formats/format-inlongmsg-base/src/main/java/org/apache/inlong/sort/formats/inlongmsg/InLongMsgDeserializationSchema.java`
#### Snippet
```java
        final GenericRowData producedRow =
                new GenericRowData(physicalRow.getRowKind(), physicalArity + metadataArity);
        for (int physicalPos = 0; physicalPos < physicalArity; physicalPos++) {
            producedRow.setField(physicalPos, physicalRow.getField(physicalPos));
        }
        for (int metadataPos = 0; metadataPos < metadataArity; metadataPos++) {
            producedRow.setField(
                    physicalArity + metadataPos, metadataConverters[metadataPos].read(head));
        }
        out.collect(producedRow);
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-formats/format-common/src/main/java/org/apache/inlong/sort/formats/common/DateFormatInfo.java`
#### Snippet
```java
        switch (format) {
            case "MICROS": {
                long millis = date.getTime();
                long micros = TimeUnit.MILLISECONDS.toMicros(millis);
                return Long.toString(micros);
            }
            case "MILLIS": {
                long millis = date.getTime();
                return Long.toString(millis);
            }
            case "SECONDS": {
                long millis = date.getTime();
                long seconds = TimeUnit.MILLISECONDS.toSeconds(millis);
                return Long.toString(seconds);
            }
            default: {
                if (simpleDateFormat == null) {
                    throw new IllegalStateException();
                }

                return simpleDateFormat.format(date);
            }
        }
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-formats/format-csv/src/main/java/org/apache/inlong/sort/formats/csv/CsvFormatFactory.java`
#### Snippet
```java
        descriptorProperties.getOptionalString(TableFormatConstants.FORMAT_CHARSET)
                .ifPresent(builder::setCharset);

        descriptorProperties.getOptionalCharacter(TableFormatConstants.FORMAT_DELIMITER)
                .ifPresent(builder::setDelimiter);

        descriptorProperties.getOptionalCharacter(TableFormatConstants.FORMAT_ESCAPE_CHARACTER)
                .ifPresent(builder::setEscapeCharacter);

        descriptorProperties.getOptionalCharacter(TableFormatConstants.FORMAT_QUOTE_CHARACTER)
                .ifPresent(builder::setQuoteCharacter);

        descriptorProperties.getOptionalString(TableFormatConstants.FORMAT_NULL_LITERAL)
                .ifPresent(builder::setNullLiteral);

        return builder.build();
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-formats/format-inlongmsg-base/src/main/java/org/apache/inlong/sort/formats/inlongmsg/InLongMsgDecodingFormat.java`
#### Snippet
```java
        final MetadataConverter[] metadataConverters = Arrays.stream(ReadableMetadata.values())
                .filter(metadata -> metadataKeys.contains(metadata.key))
                .map(metadata -> metadata.converter)
                .toArray(MetadataConverter[]::new);
        final List<ReadableMetadata> readableMetadata =
                metadataKeys.stream()
                        .map(
                                k -> Stream.of(ReadableMetadata.values())
                                        .filter(rm -> rm.key.equals(k))
                                        .findFirst()
                                        .orElseThrow(IllegalStateException::new))
                        .collect(Collectors.toList());
        final List<DataTypes.Field> metadataFields =
                readableMetadata.stream()
                        .map(m -> DataTypes.FIELD(m.key, m.dataType))
                        .collect(Collectors.toList());
        final DataType producedDataType =
                DataTypeUtils.appendRowFields(physicalDataType, metadataFields);
        final TypeInformation<RowData> producedTypeInfo =
                context.createTypeInformation(producedDataType);
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-formats/format-json/src/main/java/org/apache/inlong/sort/formats/json/canal/CanalJsonEnhancedDecodingFormat.java`
#### Snippet
```java
        final List<ReadableMetadata> readableMetadata = metadataKeys.stream()
                .map(k -> Stream.of(ReadableMetadata.values())
                        .filter(rm -> rm.key.equals(k))
                        .findFirst()
                        .orElseThrow(IllegalStateException::new))
                .collect(Collectors.toList());
        final List<DataTypes.Field> metadataFields = readableMetadata.stream()
                .map(m -> DataTypes.FIELD(m.key, m.dataType))
                .collect(Collectors.toList());
        final DataType producedDataType = DataTypeUtils.appendRowFields(physicalDataType, metadataFields);
        final TypeInformation<RowData> producedTypeInfo = context.createTypeInformation(producedDataType);
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-formats/format-inlongmsg-base/src/main/java/org/apache/inlong/sort/formats/inlongmsg/InLongMsgDecodingFormat.java`
#### Snippet
```java
        final Map<String, DataType> metadataMap = new LinkedHashMap<>();

        // add inner format metadata with prefix
        innerDecodingFormat
                .listReadableMetadata()
                .forEach((key, value) -> metadataMap.putIfAbsent(innerFormatMetaPrefix + key, value));

        // add format metadata
        Stream.of(ReadableMetadata.values())
                .forEachOrdered(m -> metadataMap.putIfAbsent(m.key, m.dataType));

        return metadataMap;
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-formats/format-inlongmsg-base/src/main/java/org/apache/inlong/sort/formats/inlongmsg/InLongMsgDecodingFormat.java`
#### Snippet
```java
        final List<String> innerFormatMetadataKeys =
                metadataKeys.stream()
                        .filter(k -> k.startsWith(innerFormatMetaPrefix))
                        .collect(Collectors.toList());
        final List<String> formatMetadataKeys = new ArrayList<>(metadataKeys);
        formatMetadataKeys.removeAll(innerFormatMetadataKeys);
        this.metadataKeys = formatMetadataKeys;

        // push down inner format metadata
        final Map<String, DataType> formatMetadata = innerDecodingFormat.listReadableMetadata();
        if (formatMetadata.size() > 0) {
            final List<String> requestedFormatMetadataKeys =
                    innerFormatMetadataKeys.stream()
                            .map(k -> k.substring(innerFormatMetaPrefix.length()))
                            .collect(Collectors.toList());
            innerDecodingFormat.applyReadableMetadata(requestedFormatMetadataKeys);
        }
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-formats/format-inlongmsg-base/src/main/java/org/apache/inlong/sort/formats/inlongmsg/InLongMsgFormatFactory.java`
#### Snippet
```java
        validateDecodingFormatOptions(formatOptions);

        final DeserializationFormatFactory innerFactory = FactoryUtil.discoverFactory(
                context.getClassLoader(),
                DeserializationFormatFactory.class,
                formatOptions.get(INNER_FORMAT));
        Configuration allOptions = Configuration.fromMap(context.getCatalogTable().getOptions());
        String innerFormatMetaPrefix = formatOptions.get(INNER_FORMAT) + ".";
        String innerFormatPrefix = INLONG_PREFIX + innerFormatMetaPrefix;
        DecodingFormat<DeserializationSchema<RowData>> innerFormat =
                innerFactory.createDecodingFormat(context, new DelegatingConfiguration(allOptions, innerFormatPrefix));
        boolean ignoreErrors = formatOptions.get(IGNORE_PARSE_ERRORS);
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-formats/format-inlongmsg-base/src/main/java/org/apache/inlong/sort/formats/inlongmsg/InLongMsgMixedValidator.java`
#### Snippet
```java
        super.validate(properties);

        properties.validateString(TableFormatConstants.FORMAT_DELIMITER, true, 1, 1);
        properties.validateString(TableFormatConstants.FORMAT_ENTRY_DELIMITER, true, 1, 1);
        properties.validateString(TableFormatConstants.FORMAT_KV_DELIMITER, true, 1, 1);
        properties.validateString(TableFormatConstants.FORMAT_ESCAPE_CHARACTER, true, 1, 1);
        properties.validateString(TableFormatConstants.FORMAT_QUOTE_CHARACTER, true, 1, 1);
        properties.validateBoolean(TableFormatConstants.FORMAT_IGNORE_ERRORS, true);
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-formats/format-json/src/main/java/org/apache/inlong/sort/formats/json/canal/CanalJsonDecodingFormat.java`
#### Snippet
```java
        final List<ReadableMetadata> readableMetadata =
                metadataKeys.stream()
                        .map(
                                k -> Stream.of(ReadableMetadata.values())
                                        .filter(rm -> rm.key.equals(k))
                                        .findFirst()
                                        .<IllegalStateException>orElseThrow(IllegalStateException::new))
                        .collect(Collectors.toList());
        final List<DataTypes.Field> metadataFields =
                readableMetadata.stream()
                        .map(m -> DataTypes.FIELD(m.key, m.dataType))
                        .collect(Collectors.toList());
        final DataType producedDataType =
                DataTypeUtils.appendRowFields(physicalDataType, metadataFields);
        final TypeInformation<RowData> producedTypeInfo =
                context.createTypeInformation(producedDataType);
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-formats/format-json/src/main/java/org/apache/inlong/sort/formats/json/canal/CanalJsonDeserializationSchema.java`
#### Snippet
```java
if (message == null || message.length == 0) {
            return;
        }
        try {
            final JsonNode root = jsonDeserializer.deserializeToJsonNode(message);
            if (database != null) {
                if (!databasePattern
                        .matcher(root.get(ReadableMetadata.DATABASE.key).asText())
                        .matches()) {
                    return;
                }
            }
            if (table != null) {
                if (!tablePattern
                        .matcher(root.get(ReadableMetadata.TABLE.key).asText())
                        .matches()) {
                    return;
                }
            }
            final GenericRowData row = (GenericRowData) jsonDeserializer.convertToRowData(root);
            String type = row.getString(2).toString(); // "type" field
            if (OP_INSERT.equals(type)) {
                // "data" field is an array of row, contains inserted rows
                ArrayData data = row.getArray(0);
                for (int i = 0; i < data.size(); i++) {
                    GenericRowData insert = (GenericRowData) data.getRow(i, fieldCount);
                    insert.setRowKind(RowKind.INSERT);
                    emitRow(row, insert, out);
                }
            } else if (OP_UPDATE.equals(type)) {
                // "data" field is an array of row, contains new rows
                ArrayData data = row.getArray(0);
                // "old" field is an array of row, contains old values
                ArrayData old = row.getArray(1);
                for (int i = 0; i < data.size(); i++) {
                    // the underlying JSON deserialization schema always produce GenericRowData.
                    GenericRowData after = (GenericRowData) data.getRow(i, fieldCount);
                    GenericRowData before = (GenericRowData) old.getRow(i, fieldCount);
                    final JsonNode oldField = root.get(FIELD_OLD);
                    for (int f = 0; f < fieldCount; f++) {
                        if (before.isNullAt(f) && oldField.findValue(fieldNames.get(f)) == null) {
                            // fields in "old" (before) means the fields are changed
                            // fields not in "old" (before) means the fields are not changed
                            // so we just copy the not changed fields into before
                            before.setField(f, after.getField(f));
                        }
                    }
                    before.setRowKind(RowKind.UPDATE_BEFORE);
                    after.setRowKind(RowKind.UPDATE_AFTER);
                    emitRow(row, before, out);
                    emitRow(row, after, out);
                }
            } else if (OP_DELETE.equals(type)) {
                // "data" field is an array of row, contains deleted rows
                ArrayData data = row.getArray(0);
                for (int i = 0; i < data.size(); i++) {
                    GenericRowData insert = (GenericRowData) data.getRow(i, fieldCount);
                    insert.setRowKind(RowKind.DELETE);
                    emitRow(row, insert, out);
                }
            } else if (OP_CREATE.equals(type)) {
                // "data" field is null and "type" is "CREATE" which means
                // this is a DDL change event, and we should skip it.
                return;
            } else {
                if (!ignoreParseErrors) {
                    throw new IOException(
                            format(
                                    "Unknown \"type\" value \"%s\". The Canal JSON message is '%s'",
                                    type, new String(message)));
                }
            }
        } catch (Throwable t) {
            // a big try catch to protect the processing.
            if (!ignoreParseErrors) {
                throw new IOException(
                        format("Corrupt Canal JSON message '%s'.", new String(message)), t);
            }
        }
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-formats/format-json/src/main/java/org/apache/inlong/sort/formats/json/debezium/DebeziumJsonDeserializationSchema.java`
#### Snippet
```java
        final int metadataArity = metadataConverters.length;

        final GenericRowData producedRow = new GenericRowData(physicalRow.getRowKind(), physicalArity + 1);

        for (int physicalPos = 0; physicalPos < physicalArity; physicalPos++) {
            producedRow.setField(physicalPos + 1, physicalRow.getField(physicalPos));
        }

        // Put metadata in the first field of the emitted RowData
        Map<StringData, StringData> metadataMap = new HashMap<>();
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-formats/format-json/src/main/java/org/apache/inlong/sort/formats/json/canal/CanalJsonDeserializationSchema.java`
#### Snippet
```java
        final GenericRowData producedRow = new GenericRowData(physicalRow.getRowKind(), physicalArity + 1);

        for (int physicalPos = 0; physicalPos < physicalArity; physicalPos++) {
            producedRow.setField(physicalPos + 1, physicalRow.getField(physicalPos));
        }

        // Put metadata in the first field of the emitted RowData
        Map<StringData, StringData> metadataMap = new HashMap<>();
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-formats/format-json/src/main/java/org/apache/inlong/sort/formats/json/canal/CanalJsonEnhancedSerializationSchema.java`
#### Snippet
```java
        switch (rowKind) {
            case INSERT:
            case UPDATE_AFTER:
                return OP_INSERT;
            case UPDATE_BEFORE:
            case DELETE:
                return OP_DELETE;
            default:
                throw new UnsupportedOperationException(
                        "Unsupported operation '" + rowKind + "' for row kind.");
        }
```

### DuplicatedCode
Duplicated code
in `inlong-sort/sort-formats/format-kv/src/main/java/org/apache/inlong/sort/formats/kv/KvFormatFactory.java`
#### Snippet
```java
        descriptorProperties.getOptionalString(TableFormatConstants.FORMAT_CHARSET)
                .ifPresent(builder::setCharset);

        descriptorProperties.getOptionalCharacter(TableFormatConstants.FORMAT_ENTRY_DELIMITER)
                .ifPresent(builder::setEntryDelimiter);

        descriptorProperties.getOptionalCharacter(TableFormatConstants.FORMAT_KV_DELIMITER)
                .ifPresent(builder::setKvDelimiter);

        descriptorProperties.getOptionalCharacter(TableFormatConstants.FORMAT_ESCAPE_CHARACTER)
                .ifPresent(builder::setEscapeCharacter);

        descriptorProperties.getOptionalCharacter(TableFormatConstants.FORMAT_QUOTE_CHARACTER)
                .ifPresent(builder::setQuoteCharacter);

        descriptorProperties.getOptionalString(TableFormatConstants.FORMAT_NULL_LITERAL)
                .ifPresent(builder::setNullLiteral);

        return builder.build();
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/resources/assets/lib/DataTables/DataTables-1.10.11/css/dataTables.bootstrap.css`
#### Snippet
```java
table.dataTable {
    clear: both;
    margin-top: 6px !important;
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/resources/assets/lib/DataTables/DataTables-1.10.11/css/dataTables.foundation.css`
#### Snippet
```java
table.dataTable {
    clear: both;
    margin: 0.5em 0 !important;
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/resources/assets/lib/DataTables/DataTables-1.10.11/css/dataTables.jqueryui.css`
#### Snippet
```java
.dataTables_wrapper .dataTables_paginate .paginate_button {
    box-sizing: border-box;
    display: inline-block;
    min-width: 1.5em;
    padding: 0.5em 1em;
    margin-left: 2px;
    text-align: center;
    text-decoration: none !important;
    cursor: pointer;
    *cursor: hand;
    color: #333 !important;
    border: 1px solid transparent;
    border-radius: 2px;
}
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/resources/assets/lib/DataTables/DataTables-1.10.11/css/dataTables.jqueryui.css`
#### Snippet
```java
.dataTables_wrapper .dataTables_processing {
    position: absolute;
    top: 50%;
    left: 50%;
    width: 100%;
    height: 40px;
    margin-left: -50%;
    margin-top: -25px;
    padding-top: 20px;
    text-align: center;
    font-size: 1.2em;
    background-color: white;
    background: -webkit-gradient(linear, left top, right top, color-stop(0%, rgba(255, 255, 255, 0)), color-stop(25%, rgba(255, 255, 255, 0.9)), color-stop(75%, rgba(255, 255, 255, 0.9)), color-stop(100%, rgba(255, 255, 255, 0)));
    background: -webkit-linear-gradient(left, rgba(255, 255, 255, 0) 0%, rgba(255, 255, 255, 0.9) 25%, rgba(255, 255, 255, 0.9) 75%, rgba(255, 255, 255, 0) 100%);
    background: -moz-linear-gradient(left, rgba(255, 255, 255, 0) 0%, rgba(255, 255, 255, 0.9) 25%, rgba(255, 255, 255, 0.9) 75%, rgba(255, 255, 255, 0) 100%);
    background: -ms-linear-gradient(left, rgba(255, 255, 255, 0) 0%, rgba(255, 255, 255, 0.9) 25%, rgba(255, 255, 255, 0.9) 75%, rgba(255, 255, 255, 0) 100%);
    background: -o-linear-gradient(left, rgba(255, 255, 255, 0) 0%, rgba(255, 255, 255, 0.9) 25%, rgba(255, 255, 255, 0.9) 75%, rgba(255, 255, 255, 0) 100%);
    background: linear-gradient(to right, rgba(255, 255, 255, 0) 0%, rgba(255, 255, 255, 0.9) 25%, rgba(255, 255, 255, 0.9) 75%, rgba(255, 255, 255, 0) 100%);
}
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/resources/assets/lib/DataTables/DataTables-1.10.11/css/dataTables.jqueryui.css`
#### Snippet
```java
.dataTables_wrapper .dataTables_paginate .fg-button {
    box-sizing: border-box;
    display: inline-block;
    min-width: 1.5em;
    padding: 0.5em;
    margin-left: 2px;
    text-align: center;
    text-decoration: none !important;
    cursor: pointer;
    *cursor: hand;
    border: 1px solid transparent;
}
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/BaseMessageConsumer.java`
#### Snippet
```java
        if (this.subStatus.get() >= 0) {
            if (this.subStatus.get() == 0) {
                throw new TubeClientException("Duplicated completeSubscribe call!");
            } else {
                throw new TubeClientException("Subscribe has finished!");
            }
        }
        if (!subStatus.compareAndSet(-1, 0)) {
            throw new TubeClientException("Duplicated completeSubscribe call!");
        }
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/BaseMessageConsumer.java`
#### Snippet
```java
        clientStatsInfo.selfPrintStatsInfo(true, true, strBuffer);
        logger.info(strBuffer
                .append("[SHUTDOWN_CONSUMER] Partitions unregistered,  consumer :")
                .append(this.consumerId).toString());
        strBuffer.delete(0, strBuffer.length());
        try {
            masterService.consumerCloseClientC2M(createMasterCloseRequest(),
                    AddressUtils.getLocalAddress(), consumerConfig.isTlsEnable());
        } catch (Throwable e) {
            strBuffer.delete(0, strBuffer.length());
            logger.warn(strBuffer
                    .append("[SHUTDOWN_CONSUMER] call closeRequest failure, error is ")
                    .append(e.getMessage()).toString());
            strBuffer.delete(0, strBuffer.length());
        }
        logger.info(strBuffer.append("[SHUTDOWN_CONSUMER] Client closed, consumer : ")
                .append(this.consumerId).toString());
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/BaseMessageConsumer.java`
#### Snippet
```java
        this.lastHeartbeatTime2Master = System.currentTimeMillis();
        this.heartService2Master.scheduleWithFixedDelay(new HeartTask2MasterWorker(),
                0, consumerConfig.getHeartbeatPeriodMs(), TimeUnit.MILLISECONDS);
        // to broker
        this.lastHeartbeatTime2Broker = System.currentTimeMillis();
        this.heartBeatThread2Broker = new Thread(new HeartTask2BrokerWorker());
        heartBeatThread2Broker
                .setName(strBuffer.append("Broker-Heartbeat-Thread-").append(consumerId).toString());
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/BaseMessageConsumer.java`
#### Snippet
```java
        ClientBroker.GetMessageRequestC2B.Builder builder =
                ClientBroker.GetMessageRequestC2B.newBuilder();
        builder.setClientId(this.consumerId);
        builder.setGroupName(this.consumerConfig.getConsumerGroup());
        builder.setTopicName(partition.getTopic());
        builder.setEscFlowCtrl(rmtDataCache.isCurGroupInFlowCtrl());
        builder.setPartitionId(partition.getPartitionId());
        builder.setLastPackConsumed(isLastConsumed);
        builder.setManualCommitOffset(false);
        return builder.build();
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/BaseMessageConsumer.java`
#### Snippet
```java
        ClientBroker.CommitOffsetRequestC2B.Builder builder =
                ClientBroker.CommitOffsetRequestC2B.newBuilder();
        builder.setClientId(this.consumerId);
        builder.setGroupName(this.consumerConfig.getConsumerGroup());
        builder.setTopicName(partition.getTopic());
        builder.setPartitionId(partition.getPartitionId());
        builder.setLastPackConsumed(isConsumed);
        return builder.build();
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/BaseMessageConsumer.java`
#### Snippet
```java
        StringBuilder strBuffer = new StringBuilder(512);
        strBuffer.append("Unregister info:");
        for (Map.Entry<BrokerInfo, List<PartitionSelectResult>> entry : unRegisterInfoMap.entrySet()) {
            for (PartitionSelectResult partResult : entry.getValue()) {
                try {
                    getBrokerService(partResult.getPartition().getBroker())
                            .consumerRegisterC2B(createBrokerUnregisterRequest(partResult.getPartition(),
                                    partResult.isLastPackConsumed()),
                                    AddressUtils.getLocalAddress(), consumerConfig.isTlsEnable());
                } catch (Throwable e) {
                    logger.error(new StringBuilder(512)
                            .append("Disconnect to Broker error! broker:")
                            .append(partResult.getPartition().getBroker().toString()).toString(), e);
                }
                strBuffer.append(partResult.getPartition().toString());
                strBuffer.append("\n");
            }
        }
        logger.info(strBuffer.toString());
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/BaseMessageConsumer.java`
#### Snippet
```java
        final StringBuilder strBuffer = new StringBuilder(512);
        List<String> strTopicCondList = new ArrayList<>();
        if ((topicCondMap != null) && (!topicCondMap.isEmpty())) {
            for (Map.Entry<String, TopicProcessor> entry : topicCondMap.entrySet()) {
                if (entry.getKey() == null || entry.getValue() == null) {
                    continue;
                }
                Set<String> condSet = entry.getValue().getFilterConds();
                if (condSet != null && !condSet.isEmpty()) {
                    int i = 0;
                    strBuffer.append(entry.getKey()).append(TokenConstants.SEGMENT_SEP);
                    for (String condStr : condSet) {
                        if (i++ > 0) {
                            strBuffer.append(TokenConstants.ARRAY_SEP);
                        }
                        strBuffer.append(condStr);
                    }
                    strTopicCondList.add(strBuffer.toString());
                    strBuffer.delete(0, strBuffer.length());
                }
            }
        }
        return strTopicCondList;
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/BaseMessageConsumer.java`
#### Snippet
```java
        ClientBroker.RegisterRequestC2B.Builder builder =
                ClientBroker.RegisterRequestC2B.newBuilder();
        builder.setClientId(consumerId);
        builder.setGroupName(this.consumerConfig.getConsumerGroup());
        builder.setOpType(RpcConstants.MSG_OPTYPE_REGISTER);
        builder.setTopicName(partition.getTopic());
        builder.setPartitionId(partition.getPartitionId());
        builder.setQryPriorityId(rmtDataCache.getQryPriorityId());
        builder.setReadStatus(getGroupInitReadStatus(rmtDataCache.bookPartition(partition.getPartitionKey())));
        TopicProcessor topicProcessor =
                this.consumeSubInfo.getTopicProcessor(partition.getTopic());
        if (topicProcessor != null && topicProcessor.getFilterConds() != null) {
            builder.addAllFilterCondStr(topicProcessor.getFilterConds());
        }
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/BaseMessageConsumer.java`
#### Snippet
```java
        ClientBroker.RegisterRequestC2B.Builder builder =
                ClientBroker.RegisterRequestC2B.newBuilder();
        builder.setClientId(consumerId);
        builder.setGroupName(this.consumerConfig.getConsumerGroup());
        builder.setOpType(RpcConstants.MSG_OPTYPE_UNREGISTER);
        builder.setTopicName(partition.getTopic());
        builder.setPartitionId(partition.getPartitionId());
        if (isLastConsumered) {
            builder.setReadStatus(0);
        } else {
            builder.setReadStatus(1);
        }
        builder.setAuthInfo(genBrokerAuthenticInfo(partition.getBrokerId(), true));
        return builder.build();
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/BaseMessageConsumer.java`
#### Snippet
```java
        ClientBroker.HeartBeatRequestC2B.Builder builder =
                ClientBroker.HeartBeatRequestC2B.newBuilder();
        builder.setClientId(consumerId);
        builder.setGroupName(this.consumerConfig.getConsumerGroup());
        builder.setReadStatus(getGroupInitReadStatus(false));
        builder.setQryPriorityId(rmtDataCache.getQryPriorityId());
        builder.addAllPartitionInfo(partitionList);
        builder.setAuthInfo(genBrokerAuthenticInfo(brokerId, false));
        return builder.build();
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/BaseMessageConsumer.java`
#### Snippet
```java
        ClientMaster.MasterCertificateInfo.Builder authInfoBuilder = null;
        if (this.consumerConfig.isEnableUserAuthentic()) {
            authInfoBuilder = ClientMaster.MasterCertificateInfo.newBuilder();
            if (rmtDataCache.markAndGetAuthStatus(force)) {
                authInfoBuilder.setAuthInfo(authenticateHandler
                        .genMasterAuthenticateToken(consumerConfig.getUsrName(),
                                consumerConfig.getUsrPassWord()));
            } else {
                authInfoBuilder.setAuthorizedToken(authAuthorizedTokenRef.get());
            }
        }
        if (authInfoBuilder != null) {
            return authInfoBuilder.build();
        }
        return null;
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/BaseMessageConsumer.java`
#### Snippet
```java
        ClientBroker.AuthorizedInfo.Builder authInfoBuilder =
                ClientBroker.AuthorizedInfo.newBuilder();
        authInfoBuilder.setVisitAuthorizedToken(visitToken.get());
        if (this.consumerConfig.isEnableUserAuthentic()) {
            if (rmtDataCache.markAndGetBrokerAuthStatus(brokerId, force)) {
                authInfoBuilder.setAuthAuthorizedToken(authenticateHandler
                        .genBrokerAuthenticateToken(consumerConfig.getUsrName(),
                                consumerConfig.getUsrPassWord()));
            }
        }
        return authInfoBuilder.build();
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/BaseMessageConsumer.java`
#### Snippet
```java
        if (inAuthorizedTokenInfo != null) {
            visitToken.set(inAuthorizedTokenInfo.getVisitAuthorizedToken());
            if (inAuthorizedTokenInfo.hasAuthAuthorizedToken()) {
                String inAuthAuthorizedToken = inAuthorizedTokenInfo.getAuthAuthorizedToken();
                if (TStringUtils.isNotBlank(inAuthAuthorizedToken)) {
                    String curAuthAuthorizedToken = authAuthorizedTokenRef.get();
                    if (!inAuthAuthorizedToken.equals(curAuthAuthorizedToken)) {
                        authAuthorizedTokenRef.set(inAuthAuthorizedToken);
                    }
                }
            }
        }
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/BaseMessageConsumer.java`
#### Snippet
```java
        int readStatus = TBaseConstants.CONSUME_MODEL_READ_NORMAL;
        switch (consumerConfig.getConsumePosition()) {
            case CONSUMER_FROM_LATEST_OFFSET: {
                if (isFistReg) {
                    readStatus = TBaseConstants.CONSUME_MODEL_READ_FROM_MAX;
                    logger.info("[Consume From Max Offset]" + consumerId);
                }
                break;
            }
            case CONSUMER_FROM_MAX_OFFSET_ALWAYS: {
                if (isFistReg) {
                    readStatus = TBaseConstants.CONSUME_MODEL_READ_FROM_MAX_ALWAYS;
                    logger.info("[Consume From Max Offset Always]" + consumerId);
                }
                break;
            }
            default: {
                readStatus = TBaseConstants.CONSUME_MODEL_READ_NORMAL;
            }
        }
        return readStatus;
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/BaseMessageConsumer.java`
#### Snippet
```java
int msgSize = 0;
                    int msgCount = 0;
                    // Convert the message payload data
                    List<Message> tmpMessageList =
                            DataConverterUtil.convertMessage(topic, msgRspB2C.getMessagesList());
                    boolean isEscLimit =
                            (msgRspB2C.hasEscFlowCtrl() && msgRspB2C.getEscFlowCtrl());
                    // Filter the message based on its content
                    // Calculate the message size and do some flow control
                    boolean needFilter = false;
                    Set<String> topicFilterSet = null;
                    TopicProcessor topicProcessor = consumeSubInfo.getTopicProcessor(topic);
                    if (topicProcessor != null) {
                        topicFilterSet = topicProcessor.getFilterConds();
                        if (topicFilterSet != null && !topicFilterSet.isEmpty()) {
                            needFilter = true;
                        }
                    }
                    List<Message> messageList = new ArrayList<>();
                    for (Message message : tmpMessageList) {
                        if (message == null) {
                            continue;
                        }
                        if (needFilter && (TStringUtils.isBlank(message.getMsgType())
                                || !topicFilterSet.contains(message.getMsgType()))) {
                            continue;
                        }
                        msgCount++;
                        messageList.add(message);
                        msgSize += message.getData().length;
                    }
                    // Set the process result of current stage. Process the result based on the response
                    long dataDltVal = msgRspB2C.hasCurrDataDlt()
                            ? msgRspB2C.getCurrDataDlt()
                            : -1;
                    long currOffset = msgRspB2C.hasCurrOffset()
                            ? msgRspB2C.getCurrOffset()
                            : TBaseConstants.META_VALUE_UNDEFINED;
                    long maxOffset = msgRspB2C.hasMaxOffset()
                            ? msgRspB2C.getMaxOffset()
                            : TBaseConstants.META_VALUE_UNDEFINED;
                    boolean isRequireSlow =
                            (msgRspB2C.hasRequireSlow() && msgRspB2C.getRequireSlow());
                    rmtDataCache
                            .setPartitionContextInfo(partitionKey, currOffset, 1,
                                    msgRspB2C.getErrCode(), isEscLimit, msgSize, 0,
                                    dataDltVal, isRequireSlow, maxOffset);
                    taskContext.setSuccessProcessResult(currOffset,
                            strBuffer.append(partitionKey).append(TokenConstants.ATTR_SEP)
                                    .append(taskContext.getUsedToken()).toString(),
                            messageList, maxOffset);
                    strBuffer.delete(0, strBuffer.length());
                    clientStatsInfo.bookSuccGetMsg(dltTime,
                            topic, partitionKey, msgCount, msgSize);
                    break;
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/BaseMessageConsumer.java`
#### Snippet
```java
                    long defDltTime =
                            msgRspB2C.hasMinLimitTime()
                                    ? msgRspB2C.getMinLimitTime()
                                    : consumerConfig.getMsgNotFoundWaitPeriodMs();
                    rmtDataCache.errRspRelease(partitionKey, topic,
                            taskContext.getUsedToken(), false, TBaseConstants.META_VALUE_UNDEFINED,
                            0, msgRspB2C.getErrCode(), false, 0,
                            defDltTime, isFilterConsume(topic), TBaseConstants.META_VALUE_UNDEFINED,
                            TBaseConstants.META_VALUE_UNDEFINED);
                    taskContext.setFailProcessResult(msgRspB2C.getErrCode(), msgRspB2C.getErrMsg());
                    break;
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/BaseMessageConsumer.java`
#### Snippet
```java
                    long limitDlt = 300;
                    switch (msgRspB2C.getErrCode()) {
                        case TErrCodeConstants.FORBIDDEN: {
                            limitDlt = 2000;
                            break;
                        }
                        case TErrCodeConstants.SERVICE_UNAVAILABLE: {
                            limitDlt = 300;
                            break;
                        }
                        case TErrCodeConstants.MOVED: {
                            limitDlt = 200;
                            break;
                        }
                        case TErrCodeConstants.NOT_FOUND: {
                            limitDlt = consumerConfig.getMsgNotFoundWaitPeriodMs();
                            break;
                        }
                        default: {
                            //
                        }
                    }
                    rmtDataCache.errRspRelease(partitionKey, topic,
                            taskContext.getUsedToken(), false, TBaseConstants.META_VALUE_UNDEFINED,
                            0, msgRspB2C.getErrCode(), false, 0,
                            limitDlt, isFilterConsume(topic), -1, TBaseConstants.META_VALUE_UNDEFINED);
                    taskContext.setFailProcessResult(msgRspB2C.getErrCode(), msgRspB2C.getErrMsg());
                    break;
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/BaseMessageConsumer.java`
#### Snippet
```java
                long currentTime = System.currentTimeMillis();
                if ((currentTime - lastHeartbeatTime2Master) > consumerConfig.getHeartbeatPeriodMs() * 2) {
                    logger.warn(strBuffer.append(consumerId)
                            .append(" heartbeat interval to master is too long,please check! Total time : ")
                            .append(currentTime - lastHeartbeatTime2Master).toString());
                    strBuffer.delete(0, strBuffer.length());
                }
                lastHeartbeatTime2Master = currentTime;
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/BaseMessageConsumer.java`
#### Snippet
```java
            lastHeartbeatTime2Master = System.currentTimeMillis();
            heartbeatRetryTimes++;
            if (!isShutdown()
                    && heartbeatRetryTimes > consumerConfig.getMaxHeartBeatRetryTimes()) {
                logger.warn(sBuilder.append("Adjust HeartbeatPeriod for ").append(reason)
                        .append(", sleep ").append(consumerConfig.getHeartbeatPeriodAfterFail())
                        .append(" Ms").toString());
                sBuilder.delete(0, sBuilder.length());
                ThreadUtils.sleep(consumerConfig.getHeartbeatPeriodAfterFail());
            }
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/BaseMessageConsumer.java`
#### Snippet
```java
                                for (Partition partition : partitions) {
                                    partStrSet.add(partition.toString());
                                }
                                ClientBroker.HeartBeatResponseB2C heartBeatResponseV2 =
                                        getBrokerService(brokerInfo).consumerHeartbeatC2B(
                                                createBrokerHeartBeatRequest(brokerInfo.getBrokerId(), partStrSet),
                                                AddressUtils.getLocalAddress(), consumerConfig.isTlsEnable());
                                // When response is success
                                if (heartBeatResponseV2 == null) {
                                    clientStatsInfo.bookHB2BrokerTimeout();
                                    continue;
                                }
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/BaseMessageConsumer.java`
#### Snippet
```java
                                                final int index =
                                                        strFailInfo.indexOf(TokenConstants.ATTR_SEP);
                                                if (index < 0) {
                                                    logger.error(strBuffer
                                                            .append("Parse Heartbeat response error : ")
                                                            .append("invalid response, ")
                                                            .append(strFailInfo).toString());
                                                    strBuffer.delete(0, strBuffer.length());
                                                    continue;
                                                }
                                                int errorCode =
                                                        Integer.parseInt(strFailInfo.substring(0, index));
                                                Partition failPartition =
                                                        new Partition(strFailInfo.substring(index + 1));
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/RmtDataCache.java`
#### Snippet
```java
        if (opsTaskInfo == null) {
            return;
        }
        // update group flowctrl info
        if (opsTaskInfo.hasGroupFlowCheckId()
                && opsTaskInfo.getGroupFlowCheckId() >= 0
                && opsTaskInfo.getGroupFlowCheckId() != groupFlowCtrlRuleHandler.getFlowCtrlId()) {
            try {
                groupFlowCtrlRuleHandler.updateFlowCtrlInfo(
                        opsTaskInfo.getQryPriorityId(),
                        opsTaskInfo.getGroupFlowCheckId(),
                        opsTaskInfo.getGroupFlowControlInfo(), strBuff);
            } catch (Exception e1) {
                logger.warn("[Remote Data Cache] found parse group flowCtrl rules failure", e1);
            }
        }
        // update default flowctrl info
        if (opsTaskInfo.hasDefFlowCheckId()
                && opsTaskInfo.getDefFlowCheckId() >= 0
                && opsTaskInfo.getDefFlowCheckId() != defFlowCtrlRuleHandler.getFlowCtrlId()) {
            try {
                defFlowCtrlRuleHandler.updateFlowCtrlInfo(
                        TBaseConstants.META_VALUE_UNDEFINED,
                        opsTaskInfo.getDefFlowCheckId(),
                        opsTaskInfo.getDefFlowControlInfo(), strBuff);
            } catch (Exception e1) {
                logger.warn("[Remote Data Cache] found parse default flowCtrl rules failure", e1);
            }
        }
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/RmtDataCache.java`
#### Snippet
```java
            ConcurrentLinkedQueue<Partition> oldPartitionList =
                    topicPartitionConMap.get(partitionExt.getTopic());
            if (oldPartitionList != null) {
                oldPartitionList.remove(partitionExt);
                if (oldPartitionList.isEmpty()) {
                    topicPartitionConMap.remove(partitionExt.getTopic());
                }
            }
            ConcurrentLinkedQueue<Partition> regMapPartitionList =
                    brokerPartitionConMap.get(partitionExt.getBroker());
            if (regMapPartitionList != null) {
                regMapPartitionList.remove(partitionExt);
                if (regMapPartitionList.isEmpty()) {
                    brokerPartitionConMap.remove(partitionExt.getBroker());
                }
            }
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/SimpleClientBalanceConsumer.java`
#### Snippet
```java
        if (result == null) {
            throw new TubeClientException("Illegal parameter: parameter result is null!");
        }
        final StringBuilder sBuffer = new StringBuilder(512);
        if (TStringUtils.isBlank(partitionKey)) {
            result.setFailResult(TErrCodeConstants.BAD_REQUEST,
                    "Parameter partitionKey is blank!");
            return result.isSuccess();
        }
        if (isShutdown()) {
            result.setFailResult(TErrCodeConstants.CLIENT_SHUTDOWN,
                    "The client is not started or closed!");
            return result.isSuccess();
        }
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/SimpleClientBalanceConsumer.java`
#### Snippet
```java
        String keyId = sBuilder.append(strConfirmContextItems[0].trim())
                .append(TokenConstants.ATTR_SEP).append(strConfirmContextItems[1].trim())
                .append(TokenConstants.ATTR_SEP).append(strConfirmContextItems[2].trim()).toString();
        sBuilder.delete(0, sBuilder.length());
        String topicName = strConfirmContextItems[1].trim();
        long timeStamp = Long.parseLong(strConfirmContextItems[3]);
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/producer/ProducerManager.java`
#### Snippet
```java
            AtomicInteger curPubCnt = this.publishTopics.get(topic);
            if (curPubCnt == null) {
                AtomicInteger tmpPubCnt = new AtomicInteger(0);
                curPubCnt = this.publishTopics.putIfAbsent(topic, tmpPubCnt);
                if (curPubCnt == null) {
                    curPubCnt = tmpPubCnt;
                }
            }
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/producer/qltystats/BrokerStatsItemSet.java`
#### Snippet
```java
        long tmpSendNum = sendNum.get();
        long dltNum = tmpSendNum - sendNumSnapshot.get();
        sendNumSnapshot.set(tmpSendNum);
        if (dltNum < 0) {
            dltNum += Long.MAX_VALUE;
        }
        dltSendNum = dltNum;
        return dltNum;
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/producer/qltystats/DefaultBrokerRcvQltyStats.java`
#### Snippet
```java
        AtomicLong curBrokerNum = brokerCurSentReqNum.get(brokerId);
        if (curBrokerNum == null) {
            AtomicLong tmpCurBrokerNum = new AtomicLong(0);
            curBrokerNum = brokerCurSentReqNum.putIfAbsent(brokerId, tmpCurBrokerNum);
            if (curBrokerNum == null) {
                curBrokerNum = tmpCurBrokerNum;
            }
        }
        curBrokerNum.incrementAndGet();
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-connectors/tubemq-connector-flink/src/main/java/org/apache/flink/connectors/tubemq/TubemqTableSourceSinkFactory.java`
#### Snippet
```java
        properties.add(SCHEMA + ".#." + SCHEMA_TYPE);
        properties.add(SCHEMA + ".#." + SCHEMA_NAME);
        properties.add(SCHEMA + ".#." + SCHEMA_FROM);

        // time attributes
        properties.add(SCHEMA + ".#." + SCHEMA_PROCTIME);
        properties.add(SCHEMA + ".#." + ROWTIME_TIMESTAMPS_TYPE);
        properties.add(SCHEMA + ".#." + ROWTIME_TIMESTAMPS_FROM);
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/cluster/BrokerInfo.java`
#### Snippet
```java
        if (!this.host.equals(o.host)) {
            return this.host.compareTo(o.host);
        } else if (this.port != o.port) {
            return this.port > o.port ? 1 : -1;
        }
        return 0;
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/metric/impl/ESTHistogram.java`
#### Snippet
```java
        long p99Cnt = 0L;
        long p999Cnt = 0L;
        long p9999Cnt = 0L;
        int maxValIndex = -1;
        int match99Index = 0;
        int match999Index = 0;
        int match9999Index = 0;
        long curCnt;
        long maxValue;
        long minValue;
        if (snapShot) {
            curCnt = this.count.getAndResetValue();
            maxValue = this.max.getAndResetValue();
            minValue = this.min.getAndResetValue();
        } else {
            curCnt = this.count.getValue();
            maxValue = this.max.getValue();
            minValue = this.min.getValue();
        }
        if (curCnt > 0) {
            p99Cnt = (long) (curCnt * 0.99);
            p999Cnt = (long) (curCnt * 0.999);
            p9999Cnt = (long) (curCnt * 0.9999);
        }
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/metric/impl/ESTHistogram.java`
#### Snippet
```java
                maxValIndex = i;
                // calculate p99 value
                if (p99Cnt > 0) {
                    p99Cnt -= bucketItemVal;
                    if (p99Cnt <= 0) {
                        match99Index = i;
                    }
                }
                // calculate p999 value
                if (p999Cnt > 0) {
                    p999Cnt -= bucketItemVal;
                    if (p999Cnt <= 0) {
                        match999Index = i;
                    }
                }
                // calculate p9999 value
                if (p9999Cnt > 0) {
                    p9999Cnt -= bucketItemVal;
                    if (p9999Cnt <= 0) {
                        match9999Index = i;
                    }
                }
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-example/src/main/java/org/apache/inlong/tubemq/example/MAMessageProducerExample.java`
#### Snippet
```java
        final String masterServers = args[0];
        final String pubTopicAndFilterItems = args[1];
        final long msgCount = Long.parseLong(args[2]);
        int pkgSize = 1024;
        if (args.length > 3) {
            pkgSize = MixedUtils.mid(Integer.parseInt(args[3]), 1, 1024 * 1024);
        }
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-example/src/main/java/org/apache/inlong/tubemq/example/MAMessageProducerExample.java`
#### Snippet
```java
            long sentCount = 0;
            int roundIndex = 0;
            int targetCnt = topicFilterTuples.size();
            while (msgCount < 0 || sentCount < msgCount) {
                // 1 Rotate to get the attribute information to be sent
                roundIndex = (int) (sentCount++ % targetCnt);
                Tuple2<String, String> target = topicFilterTuples.get(roundIndex);

                // 2 send message
                try {
                    // 2.1 Send data asynchronously, recommended
                    producer.sendMessage(MixedUtils.buildMessage(target.getF0(),
                            target.getF1(), bodyData, sentCount), new DefaultSendCallback());
                    // Or
                    // 2.2 Send message synchronous, not recommended
                    // MessageSentResult result = producer.sendMessage(message);
                    // totalSentCnt.incrementAndGet();
                    // if (!result.isSuccess()) {
                    // logger.error("Sync-send message failed!" + result.getErrMsg());
                    // }
                } catch (TubeClientException | InterruptedException e) {
                    logger.error("Send message failed!", e);
                }
                // 3 Cool sending
                // Attention: only used in the test link, to solve the problem of
                // frequent sending failures caused by insufficient test resources.
                MixedUtils.coolSending(sentCount);
            }
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-example/src/main/java/org/apache/inlong/tubemq/example/MessagePullConsumerExample.java`
#### Snippet
```java
        final String masterServers = args[0];
        final String subTopicAndFiterItems = args[1];
        final String groupName = args[2];
        final int consumeCount = Integer.parseInt(args[3]);
        int fetchThreadCnt = 3;
        if (args.length > 4) {
            fetchThreadCnt = MixedUtils.mid(Integer.parseInt(args[4]),
                    1, Runtime.getRuntime().availableProcessors());
        }
        final Map<String, TreeSet<String>> topicAndFiltersMap =
                MixedUtils.parseTopicParam(subTopicAndFiterItems);
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-example/src/main/java/org/apache/inlong/tubemq/example/MessagePullSetConsumerExample.java`
#### Snippet
```java
        consumer1.completeSubscribe(sessionKeyFst,
                sourceCountFst, isSelectBig, partOffsetMapFst);

        // 3.4. initial and start fetch threads
        Thread[] fetchRunners1 = new Thread[fetchThreadCnt];
        for (int i = 0; i < fetchRunners1.length; i++) {
            fetchRunners1[i] = new Thread(new FetchRequestRunner(
                    consumer1, partOffsetMapFst, consumeCount), "_fetch_runner_" + i);
        }
        for (Thread thread : fetchRunners1) {
            thread.start();
        }
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/BrokerConfig.java`
#### Snippet
```java
            if (TStringUtils.isBlank(brokerSect.get("visitName"))) {
                throw new IllegalArgumentException(new StringBuilder(256)
                        .append("visitName is null or Blank in ")
                        .append(SECT_TOKEN_BROKER).append(" section!").toString());
            }
            if (TStringUtils.isBlank(brokerSect.get("visitPassword"))) {
                throw new IllegalArgumentException(new StringBuilder(256)
                        .append("visitPassword is null or Blank in ").append(SECT_TOKEN_BROKER)
                        .append(" section!").toString());
            }
            this.visitName = brokerSect.get("visitName").trim();
            this.visitPassword = brokerSect.get("visitPassword").trim();
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/BrokerServiceServer.java`
#### Snippet
```java
            rpcTLSWriteConfig.put(RpcConstants.TLS_KEYSTORE_PATH,
                    tlsConfig.getTlsKeyStorePath());
            rpcTLSWriteConfig.put(RpcConstants.TLS_KEYSTORE_PASSWORD,
                    tlsConfig.getTlsKeyStorePassword());
            rpcTLSWriteConfig.put(RpcConstants.TLS_TWO_WAY_AUTHENTIC,
                    tlsConfig.isTlsTwoWayAuthEnable());
            if (tlsConfig.isTlsTwoWayAuthEnable()) {
                rpcTLSWriteConfig.put(RpcConstants.TLS_TRUSTSTORE_PATH,
                        tlsConfig.getTlsTrustStorePath());
                rpcTLSWriteConfig.put(RpcConstants.TLS_TRUSTSTORE_PASSWORD,
                        tlsConfig.getTlsTrustStorePassword());
            }
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/BrokerServiceServer.java`
#### Snippet
```java
        ProcessResult result = new ProcessResult();
        StringBuilder strBuffer = new StringBuilder(512);
        // get and check clientId field
        if (!PBParameterUtils.checkClientId(request.getClientId(), strBuffer, result)) {
            builder.setErrCode(result.getErrCode());
            builder.setErrMsg(result.getErrMsg());
            return builder.build();
        }
        final String clientId = (String) result.getRetData();
        // get and check groupName field
        if (!PBParameterUtils.checkGroupName(request.getGroupName(), strBuffer, result)) {
            builder.setErrCode(result.getErrCode());
            builder.setErrMsg(result.getErrMsg());
            return builder.build();
        }
        final String groupName = (String) result.getRetData();
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/BrokerServiceServer.java`
#### Snippet
```java
        if (!PBParameterUtils.checkClientId(request.getClientId(), strBuffer, result)) {
            builder.setErrCode(result.getErrCode());
            builder.setErrMsg(result.getErrMsg());
            return builder.build();
        }
        final String clientId = (String) result.getRetData();
        // get and check groupName field
        if (!PBParameterUtils.checkGroupName(request.getGroupName(), strBuffer, result)) {
            builder.setErrCode(result.getErrCode());
            builder.setErrMsg(result.getErrMsg());
            return builder.build();
        }
        final String groupName = (String) result.getRetData();
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/BrokerServiceServer.java`
#### Snippet
```java
        if (!this.started.get()) {
            builder.setErrCode(TErrCodeConstants.SERVICE_UNAVAILABLE);
            builder.setErrMsg("StoreService temporary unavailable!");
            return builder.build();
        }
        if (!serverAuthHandler.identityValidUserInfo(request.getAuthInfo(), false, result)) {
            builder.setErrCode(result.getErrCode());
            builder.setErrMsg(result.getErrMsg());
            return builder.build();
        }
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/TubeBroker.java`
#### Snippet
```java
        builder.setReadStatusRpt(ServiceStatusHolder.getReadServiceReportStatus());
        builder.setWriteStatusRpt(ServiceStatusHolder.getWriteServiceReportStatus());
        builder.setCurBrokerConfId(metadataManager.getBrokerMetadataConfId());
        builder.setConfCheckSumId(metadataManager.getBrokerConfCheckSumId());
        FlowCtrlRuleHandler flowCtrlRuleHandler =
                metadataManager.getFlowCtrlRuleHandler();
        builder.setFlowCheckId(flowCtrlRuleHandler.getFlowCtrlId());
        builder.setQryPriorityId(flowCtrlRuleHandler.getQryPriorityId());
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/disk/FileSegmentList.java`
#### Snippet
```java
        final Segment[] curViews = segmentList.get();
        if (curViews.length == 0) {
            return null;
        }
        int minStart = 0;
        for (minStart = 0; minStart < curViews.length; minStart++) {
            if (curViews[minStart] == null
                    || curViews[minStart].isExpired()) {
                continue;
            }
            break;
        }
        if (minStart >= curViews.length) {
            minStart = curViews.length - 1;
        }
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/disk/MsgFileStore.java`
#### Snippet
```java
        for (int i = 0; i < segments.size() - 1; i++) {
            final Segment curr = segments.get(i);
            final Segment next = segments.get(i + 1);
            if (curr.getStart() + curr.getCachedSize() != next.getStart()) {
                throw new IllegalStateException(new StringBuilder(512)
                        .append("The following ").append(segTypeStr)
                        .append(" segments don't validate: ")
                        .append(curr.getFile().getAbsolutePath()).append(", ")
                        .append(next.getFile().getAbsolutePath()).toString());
            }
        }
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/offset/DefaultOffsetManager.java`
#### Snippet
```java
        if (regInfoMap != null) {
            for (Map.Entry<String, Set<Integer>> entry : topicPartMap.entrySet()) {
                if (entry.getKey() == null
                        || entry.getValue() == null
                        || entry.getValue().isEmpty()) {
                    continue;
                }
                for (Integer partitionId : entry.getValue()) {
                    String offsetCacheKey = getOffsetCacheKey(entry.getKey(), partitionId);
                    regInfoMap.remove(offsetCacheKey);
                }
            }
            if (regInfoMap.isEmpty()) {
                cfmOffsetMap.remove(group);
            }
        }
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/offset/OffsetHistoryInfo.java`
#### Snippet
```java
        final int storeId = partitionId < TBaseConstants.META_STORE_INS_BASE
                ? 0
                : partitionId / TBaseConstants.META_STORE_INS_BASE;
        Map<Integer, OffsetCsmRecord> storeOffsetMap = histOffsetMap.get(topicName);
        if (storeOffsetMap == null) {
            Map<Integer, OffsetCsmRecord> tmpMap = new HashMap<>();
            storeOffsetMap = histOffsetMap.putIfAbsent(topicName, tmpMap);
            if (storeOffsetMap == null) {
                storeOffsetMap = tmpMap;
            }
        }
        OffsetCsmRecord offsetCsmRecord = storeOffsetMap.get(storeId);
        if (offsetCsmRecord == null) {
            OffsetCsmRecord tmpRecord = new OffsetCsmRecord(storeId);
            offsetCsmRecord = storeOffsetMap.putIfAbsent(storeId, tmpRecord);
            if (offsetCsmRecord == null) {
                offsetCsmRecord = tmpRecord;
            }
        }
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/stats/BrokerSrvStatsHolder.java`
#### Snippet
```java
        long curSnapshotTime = lstSnapshotTime.get();
        // Avoid frequent snapshots
        if ((System.currentTimeMillis() - curSnapshotTime) >= TBaseConstants.CFG_STATS_MIN_SNAPSHOT_PERIOD_MS) {
            if (lstSnapshotTime.compareAndSet(curSnapshotTime, System.currentTimeMillis())) {
                switchableSets[getIndex(writableIndex.incrementAndGet())].resetSinceTime();
                return true;
            }
        }
        return false;
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/stats/prometheus/BrokerPromMetricService.java`
#### Snippet
```java
        final List<MetricFamilySamples> mfs = new ArrayList<>();
        if (!started) {
            return mfs;
        }
        // service status metric data
        Map<String, Long> statsMap = new LinkedHashMap<>();
        StringBuilder strBuff = new StringBuilder(512);
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/stats/prometheus/BrokerPromMetricService.java`
#### Snippet
```java
        strBuff.delete(0, strBuff.length());
        statsMap.clear();
        WebCallStatsHolder.snapShort(statsMap);
        for (Map.Entry<String, Long> entry : statsMap.entrySet()) {
            webAPICounter.addMetric(Arrays.asList(entry.getKey()), entry.getValue());
        }
        mfs.add(webAPICounter);
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/web/BrokerAdminServlet.java`
#### Snippet
```java
        ProcessResult result = new ProcessResult();
        if (!WebParameterUtils.getStringParamValue(req,
                WebFieldDef.COMPSGROUPNAME, false, null, strBuff, result)) {
            WebParameterUtils.buildFailResult(strBuff, result.getErrMsg());
            return;
        }
        Set<String> groupNameSet = (Set<String>) result.getRetData();
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/web/BrokerAdminServlet.java`
#### Snippet
```java
        ProcessResult result = new ProcessResult();
        if (!WebParameterUtils.getStringParamValue(req,
                WebFieldDef.COMPSTOPICNAME, false, null, sBuilder, result)) {
            WebParameterUtils.buildFailResult(sBuilder, result.getErrMsg());
            return;
        }
        Set<String> topicNameSet = (Set<String>) result.getRetData();
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/web/BrokerAdminServlet.java`
#### Snippet
```java
        ProcessResult result = new ProcessResult();
        if (!WebParameterUtils.getStringParamValue(req,
                WebFieldDef.TOPICNAME, true, null, sBuffer, result)) {
            WebParameterUtils.buildFailResult(sBuffer, result.getErrMsg());
            return;
        }
        final String topicName = (String) result.getRetData();
        if (!WebParameterUtils.getStringParamValue(req,
                WebFieldDef.GROUPNAME, true, null, sBuffer, result)) {
            WebParameterUtils.buildFailResult(sBuffer, result.getErrMsg());
            return;
        }
        final String groupName = (String) result.getRetData();
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/web/BrokerAdminServlet.java`
#### Snippet
```java
        if (!WebParameterUtils.getStringParamValue(req,
                WebFieldDef.GROUPNAME, true, null, sBuffer, result)) {
            WebParameterUtils.buildFailResult(sBuffer, result.getErrMsg());
            return;
        }
        final String groupName = (String) result.getRetData();
        // get modify user
        if (!WebParameterUtils.getStringParamValue(req,
                WebFieldDef.MODIFYUSER, true, null, sBuffer, result)) {
            WebParameterUtils.buildFailResult(sBuffer, result.getErrMsg());
            return;
        }
        final String modifyUser = (String) result.getRetData();
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/web/BrokerAdminServlet.java`
#### Snippet
```java
            for (String group : onlineGroups) {
                if (itemCnt++ > 0) {
                    sBuffer.append(",");
                }
                sBuffer.append("\"").append(group).append("\"");
            }
            sBuffer.append("],\"groupCount\":").append(itemCnt).append("}");
            totalCnt++;
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/web/BrokerAdminServlet.java`
#### Snippet
```java
            for (Map.Entry<String, Map<Integer, GroupOffsetInfo>> entry1 : topicPartMap.entrySet()) {
                if (topicCnt++ > 0) {
                    sBuffer.append(",");
                }
                Map<Integer, GroupOffsetInfo> partOffMap = entry1.getValue();
                sBuffer.append("{\"topicName\":\"").append(entry1.getKey())
                        .append("\",\"offsets\":[");
                int partCnt = 0;
                for (Map.Entry<Integer, GroupOffsetInfo> entry2 : partOffMap.entrySet()) {
                    if (partCnt++ > 0) {
                        sBuffer.append(",");
                    }
                    GroupOffsetInfo offsetInfo = entry2.getValue();
                    offsetInfo.buildOffsetInfo(sBuffer);
                }
                sBuffer.append("],\"partCount\":").append(partCnt).append("}");
            }
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/web/BrokerAdminServlet.java`
#### Snippet
```java
        ProcessResult result = new ProcessResult();
        // get group list
        if (!WebParameterUtils.getStringParamValue(req,
                WebFieldDef.COMPSGROUPNAME, true, null, sBuffer, result)) {
            WebParameterUtils.buildFailResult(sBuffer, result.getErrMsg());
            return;
        }
        final Set<String> groupNameSet = (Set<String>) result.getRetData();
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/web/BrokerAdminServlet.java`
#### Snippet
```java
        Throwable qryThrow;
        GetMessageResult getMessageResult;
        // locate partitionId and filter-item
        msgTypeCode = groupName.hashCode();
        partitionId = Math.abs(msgTypeCode) % TServerConstants.OFFSET_HISTORY_NUMPARTS;
        // build filter conditions
        Set<String> filterCodes = new HashSet<>();
        filterCodes.add(groupName);
        // build consumer node information
        ConsumerNodeInfo consumerNodeInfo = new ConsumerNodeInfo(broker.getStoreManager(),
                groupName, "offsetConsumer", filterCodes, "", System.currentTimeMillis(), "", "");
        // query records from storage
        int qryRetryCount = 0;
        long itemInitOffset = requestOffset;
        int maxTransferSize = broker.getStoreManager().getMaxMsgTransferSize();
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/web/BrokerAdminServlet.java`
#### Snippet
```java
            qryThrow = null;
            try {
                getMessageResult = msgStore.getMessages(303, itemInitOffset,
                        partitionId, consumerNodeInfo, TServerConstants.OFFSET_HISTORY_NAME,
                        maxTransferSize, recordStamp);
            } catch (Throwable e2) {
                qryThrow = e2;
                continue;
            }
            // check query result
            if (getMessageResult.transferedMessageList == null
                    || getMessageResult.transferedMessageList.isEmpty()) {
                itemInitOffset += getMessageResult.lastReadOffset;
                continue;
            }
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/web/BrokerAdminServlet.java`
#### Snippet
```java
        final String srcGroupName = (String) result.getRetData();
        // get source consume group's topic set cloned to target group
        if (!WebParameterUtils.getStringParamValue(req,
                WebFieldDef.COMPSTOPICNAME, false, null, sBuffer, result)) {
            WebParameterUtils.buildFailResult(sBuffer, result.getErrMsg());
            return;
        }
        Set<String> srcTopicNameSet = (Set<String>) result.getRetData();
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/web/BrokerAdminServlet.java`
#### Snippet
```java
                if (partStoreMap != null) {
                    for (Entry<Integer, MessageStore> subEntry : partStoreMap.entrySet()) {
                        MessageStore msgStore = subEntry.getValue();
                        if (msgStore == null) {
                            continue;
                        }
                        if (index++ > 0) {
                            sBuffer.append(",");
                        }
                        sBuffer.append("{\"storeId\":").append(subEntry.getKey())
                                .append(",\"msgStore\":");
                        msgStore.getMsgStoreStatsInfo(needRefresh, sBuffer);
                        sBuffer.append("}");
                    }
                }
                sBuffer.append("]}");
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/aaaserver/SimpleCertificateMasterHandler.java`
#### Snippet
```java
        ClientMaster.AuthenticateInfo authInfo = certificateInfo.getAuthInfo();
        if (authInfo == null) {
            result.setFailResult(TErrCodeConstants.CERTIFICATE_FAILURE,
                    "Illegal value: AuthenticateInfo is null!");
            return result.isSuccess();
        }
        if (TStringUtils.isBlank(authInfo.getUserName())) {
            result.setFailResult(TErrCodeConstants.CERTIFICATE_FAILURE,
                    "Illegal value: authInfo.userName is Blank!");
            return result.isSuccess();
        }
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/fileconfig/AbstractFileConfig.java`
#### Snippet
```java
        final Profile.Section zkeeperSect = iniConf.get(SECT_TOKEN_ZKEEPER);
        if (zkeeperSect == null) {
            throw new IllegalArgumentException(new StringBuilder(256)
                    .append(SECT_TOKEN_ZKEEPER).append(" configure section is required!").toString());
        }
        Set<String> configKeySet = zkeeperSect.keySet();
        if (configKeySet.isEmpty()) {
            throw new IllegalArgumentException(new StringBuilder(256)
                    .append("Empty configure item in ").append(SECT_TOKEN_ZKEEPER)
                    .append(" section!").toString());
        }
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/fileconfig/AbstractFileConfig.java`
#### Snippet
```java
        if (TStringUtils.isNotBlank(zkeeperSect.get("zkServerAddr"))) {
            zkConfig.setZkServerAddr(zkeeperSect.get("zkServerAddr").trim());
        }
        if (TStringUtils.isNotBlank(zkeeperSect.get("zkNodeRoot"))) {
            zkConfig.setZkNodeRoot(zkeeperSect.get("zkNodeRoot").trim());
        }
        if (TStringUtils.isNotBlank(zkeeperSect.get("zkSessionTimeoutMs"))) {
            zkConfig.setZkSessionTimeoutMs(getInt(zkeeperSect, "zkSessionTimeoutMs"));
        }
        if (TStringUtils.isNotBlank(zkeeperSect.get("zkConnectionTimeoutMs"))) {
            zkConfig.setZkConnectionTimeoutMs(getInt(zkeeperSect, "zkConnectionTimeoutMs"));
        }
        if (TStringUtils.isNotBlank(zkeeperSect.get("zkSyncTimeMs"))) {
            zkConfig.setZkSyncTimeMs(getInt(zkeeperSect, "zkSyncTimeMs"));
        }
        if (TStringUtils.isNotBlank(zkeeperSect.get("zkCommitPeriodMs"))) {
            zkConfig.setZkCommitPeriodMs(getLong(zkeeperSect, "zkCommitPeriodMs"));
        }
        if (TStringUtils.isNotBlank(zkeeperSect.get("zkCommitFailRetries"))) {
            zkConfig.setZkCommitFailRetries(getInt(zkeeperSect, "zkCommitFailRetries"));
        }
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/fileconfig/BdbMetaConfig.java`
#### Snippet
```java
        switch (metaLocalSyncPolicy) {
            case 1:
                return Durability.SyncPolicy.SYNC;
            case 2:
                return Durability.SyncPolicy.NO_SYNC;
            case 3:
                return Durability.SyncPolicy.WRITE_NO_SYNC;
            default:
                return Durability.SyncPolicy.SYNC;
        }
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/utils/WebParameterUtils.java`
#### Snippet
```java
            if (timeUnit.endsWith("s")) {
                validDuration = Long.parseLong(validValStr.substring(0, validValStr.length() - 1)) * 1000;
            } else if (timeUnit.endsWith("m")) {
                validDuration = Long.parseLong(validValStr.substring(0, validValStr.length() - 1)) * 60000;
            } else if (timeUnit.endsWith("h")) {
                validDuration = Long.parseLong(validValStr.substring(0, validValStr.length() - 1)) * 3600000;
            } else {
                validDuration = Long.parseLong(validValStr) * 3600000;
            }
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/utils/WebParameterUtils.java`
#### Snippet
```java
        if (!getStringParamValue(paramCntr, fieldDef,
                required, null, sBuffer, result)) {
            return result.isSuccess();
        }
        String paramValue = (String) result.getRetData();
        if (paramValue == null) {
            result.setSuccResult(defValue);
            return result.isSuccess();
        }
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/utils/WebParameterUtils.java`
#### Snippet
```java
String paramValue = req.getParameter(fieldDef.name);
        if (paramValue == null) {
            paramValue = req.getParameter(fieldDef.shortName);
        }
        if (TStringUtils.isNotBlank(paramValue)) {
            // Cleanup value extra characters
            paramValue = escDoubleQuotes(paramValue.trim());
        }
        // Check if the parameter exists
        if (TStringUtils.isBlank(paramValue)) {
            if (required) {
                result.setFailResult(new StringBuilder(512)
                        .append("Parameter ").append(fieldDef.name)
                        .append(" is missing or value is blank!").toString());
            } else {
                result.setSuccResult(defValue);
            }
            return result.isSuccess();
        }
        try {
            paramValue = URLDecoder.decode(paramValue,
                    TBaseConstants.META_DEFAULT_CHARSET_NAME);
        } catch (UnsupportedEncodingException e) {
            result.setFailResult(new StringBuilder(512)
                    .append("Parameter ").append(fieldDef.name)
                    .append(" decode error, exception is ")
                    .append(e.toString()).toString());
        }
        if (TStringUtils.isBlank(paramValue)) {
            if (required) {
                result.setFailResult(new StringBuilder(512).append("Parameter ")
                        .append(fieldDef.name).append("'s value is blank!").toString());
            } else {
                result.setSuccResult(defValue);
            }
            return result.isSuccess();
        }
        if (fieldDef.valMaxLen != TBaseConstants.META_VALUE_UNDEFINED) {
            if (paramValue.length() > fieldDef.valMaxLen) {
                result.setFailResult(new StringBuilder(512)
                        .append("Parameter ").append(fieldDef.name)
                        .append("'s length over max allowed length (")
                        .append(fieldDef.valMaxLen).append(")!").toString());
                return result.isSuccess();
            }
        }
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/zookeeper/RecoverableZooKeeper.java`
#### Snippet
```java
        RetryCounter retryCounter = retryCounterFactory.create();
        while (true) {
            try {
                return zk.exists(path, watcher);
            } catch (KeeperException e) {
                ensureConnectivity(e);

                switch (e.code()) {
                    case CONNECTIONLOSS:
                    case SESSIONEXPIRED:
                    case OPERATIONTIMEOUT:
                        retryOrThrow(retryCounter, e, "exists");
                        break;

                    default:
                        throw e;
                }
            }
            retryCounter.sleepUntilNextRetry();
            retryCounter.useRetry();
        }
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/zookeeper/RecoverableZooKeeper.java`
#### Snippet
```java
        RetryCounter retryCounter = retryCounterFactory.create();
        while (true) {
            try {
                return zk.getChildren(path, watcher);
            } catch (KeeperException e) {
                ensureConnectivity(e);

                switch (e.code()) {
                    case CONNECTIONLOSS:
                    case SESSIONEXPIRED:
                    case OPERATIONTIMEOUT:
                        retryOrThrow(retryCounter, e, "getClientLst");
                        break;

                    default:
                        throw e;
                }
            }
            retryCounter.sleepUntilNextRetry();
            retryCounter.useRetry();
        }
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/zookeeper/RecoverableZooKeeper.java`
#### Snippet
```java
        RetryCounter retryCounter = retryCounterFactory.create();
        while (true) {
            try {
                byte[] revData = zk.getData(path, watcher, stat);
                return revData;
            } catch (KeeperException e) {
                ensureConnectivity(e);

                switch (e.code()) {
                    case CONNECTIONLOSS:
                    case SESSIONEXPIRED:
                    case OPERATIONTIMEOUT:
                        retryOrThrow(retryCounter, e, "getData");
                        break;

                    default:
                        throw e;
                }
            }
            retryCounter.sleepUntilNextRetry();
            retryCounter.useRetry();
        }
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/MasterConfig.java`
#### Snippet
```java
        final Profile.Section repSect = iniConf.get(SECT_TOKEN_META_BDB);
        if (repSect == null) {
            return null;
        }
        Set<String> configKeySet = repSect.keySet();
        if (configKeySet.isEmpty()) {
            throw new IllegalArgumentException(new StringBuilder(256)
                    .append("Empty configure item in ").append(SECT_TOKEN_META_BDB)
                    .append(" section!").toString());
        }
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/MasterConfig.java`
#### Snippet
```java
        if (TStringUtils.isNotBlank(repSect.get("repGroupName"))) {
            tmpMetaConfig.setRepGroupName(repSect.get("repGroupName").trim());
        }
        if (TStringUtils.isBlank(repSect.get("repNodeName"))) {
            getSimilarConfigField(SECT_TOKEN_META_BDB, configKeySet, "repNodeName");
        } else {
            tmpMetaConfig.setRepNodeName(repSect.get("repNodeName").trim());
        }
        if (TStringUtils.isNotBlank(repSect.get("repNodePort"))) {
            tmpMetaConfig.setRepNodePort(getInt(repSect, "repNodePort"));
        }
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/MasterConfig.java`
#### Snippet
```java
        if (TStringUtils.isNotBlank(repSect.get("metaDataPath"))) {
            tmpMetaConfig.setMetaDataPath(repSect.get("metaDataPath").trim());
        }
        if (TStringUtils.isNotBlank(repSect.get("repHelperHost"))) {
            tmpMetaConfig.setRepHelperHost(repSect.get("repHelperHost").trim());
        }
        if (TStringUtils.isNotBlank(repSect.get("metaLocalSyncPolicy"))) {
            tmpMetaConfig.setMetaLocalSyncPolicy(getInt(repSect, "metaLocalSyncPolicy"));
        }
        if (TStringUtils.isNotBlank(repSect.get("metaReplicaSyncPolicy"))) {
            tmpMetaConfig.setMetaReplicaSyncPolicy(getInt(repSect, "metaReplicaSyncPolicy"));
        }
        if (TStringUtils.isNotBlank(repSect.get("repReplicaAckPolicy"))) {
            tmpMetaConfig.setRepReplicaAckPolicy(getInt(repSect, "repReplicaAckPolicy"));
        }
        if (TStringUtils.isNotBlank(repSect.get("repStatusCheckTimeoutMs"))) {
            tmpMetaConfig.setRepStatusCheckTimeoutMs(getLong(repSect, "repStatusCheckTimeoutMs"));
        }
        return tmpMetaConfig;
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/balance/DefaultLoadBalancer.java`
#### Snippet
```java
                GroupResCtrlEntity offsetResetGroupEntity =
                        defMetaDataService.getGroupCtrlConf(group);
                int confAllowBClientRate = (offsetResetGroupEntity != null
                        && offsetResetGroupEntity.getAllowedBrokerClientRate() > 0)
                                ? offsetResetGroupEntity.getAllowedBrokerClientRate()
                                : -2;
                int allowRate = confAllowBClientRate > 0
                        ? confAllowBClientRate
                        : consumerHolder.getDefResourceRate();
                int maxBrokerCount =
                        brokerRunManager.getSubTopicMaxBrokerCount(topicSet);
                int curBClientRate = (int) Math.floor(maxBrokerCount / newConsumerList.size());
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/bdbstore/bdbentitys/BdbBrokerConfEntity.java`
#### Snippet
```java
        StringBuilder sBuilder = new StringBuilder(512);
        this.brokerAddress = sBuilder.append(this.brokerIp)
                .append(TokenConstants.ATTR_SEP)
                .append(this.brokerPort).toString();
        sBuilder.delete(0, sBuilder.length());
        this.brokerSimpleInfo = sBuilder.append(this.brokerId)
                .append(TokenConstants.ATTR_SEP).append(this.brokerIp)
                .append(TokenConstants.ATTR_SEP).append(" ").toString();
        sBuilder.delete(0, sBuilder.length());
        this.brokerFullInfo = sBuilder.append(this.brokerId)
                .append(TokenConstants.ATTR_SEP).append(this.brokerIp)
                .append(TokenConstants.ATTR_SEP).append(this.brokerPort).toString();
        sBuilder.delete(0, sBuilder.length());
        this.brokerTLSSimpleInfo = sBuilder.append(this.brokerId)
                .append(TokenConstants.ATTR_SEP).append(this.brokerIp)
                .append(TokenConstants.ATTR_SEP).append(" ").toString();
        sBuilder.delete(0, sBuilder.length());
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/bdbstore/bdbentitys/BdbClusterSettingEntity.java`
#### Snippet
```java
        if (TStringUtils.isNotBlank(creater)) {
            this.attributes =
                    TStringUtils.setAttrValToAttributes(this.attributes,
                            TStoreConstants.TOKEN_CREATE_USER, creater);
        }
        if (createDate != null) {
            String dataStr = DateTimeConvertUtils.date2yyyyMMddHHmmss(createDate);
            this.attributes =
                    TStringUtils.setAttrValToAttributes(this.attributes,
                            TStoreConstants.TOKEN_CREATE_DATE, dataStr);
        }
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/metamanage/metastore/dao/entity/BrokerConfEntity.java`
#### Snippet
```java
        boolean changed = false;
        // check and set dataVerId info
        if (dataVerId != TBaseConstants.META_VALUE_UNDEFINED
                && this.getDataVerId() != dataVerId) {
            changed = true;
            this.setDataVersionId(dataVerId);
        }
        // check and set brokerPort info
        if (brokerPort != TBaseConstants.META_VALUE_UNDEFINED
                && this.brokerPort != brokerPort) {
            changed = true;
            this.brokerPort = brokerPort;
        }
        // check and set brokerTLSPort info
        if (brokerTlsPort != TBaseConstants.META_VALUE_UNDEFINED
                && this.brokerTLSPort != brokerTlsPort) {
            changed = true;
            this.brokerTLSPort = brokerTlsPort;
        }
        // check and set brokerWebPort info
        if (brokerWebPort != TBaseConstants.META_VALUE_UNDEFINED
                && this.brokerWebPort != brokerWebPort) {
            changed = true;
            this.brokerWebPort = brokerWebPort;
        }
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/metamanage/metastore/dao/entity/TopicCtrlEntity.java`
#### Snippet
```java
        boolean changed = false;
        // check and set brokerPort info
        if (dataVerId != TBaseConstants.META_VALUE_UNDEFINED
                && this.getDataVerId() != dataVerId) {
            changed = true;
            this.setDataVersionId(dataVerId);
        }
        // check and set topicNameId info
        if (topicNameId != TBaseConstants.META_VALUE_UNDEFINED
                && this.topicNameId != topicNameId) {
            changed = true;
            this.topicNameId = topicNameId;
        }
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/metamanage/metastore/impl/AbsConsumeCtrlMapperImpl.java`
#### Snippet
```java
        if (topicSet == null || topicSet.isEmpty()) {
            return Collections.emptyMap();
        }
        GroupConsumeCtrlEntity entity;
        ConcurrentHashSet<String> keySet;
        List<GroupConsumeCtrlEntity> itemRet;
        Map<String, List<GroupConsumeCtrlEntity>> result = new HashMap<>();
        for (String topicName : topicSet) {
            keySet = topic2RecordCache.get(topicName);
            if (keySet == null || keySet.isEmpty()) {
                result.put(topicName, Collections.emptyList());
                continue;
            }
            itemRet = new ArrayList<>();
            for (String recordKey : keySet) {
                if (recordKey == null) {
                    continue;
                }
                entity = consumeCtrlCache.get(recordKey);
                if (entity != null) {
                    itemRet.add(entity);
                }
            }
            result.put(topicName, itemRet);
        }
        return result;
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/metamanage/metastore/impl/AbsConsumeCtrlMapperImpl.java`
#### Snippet
```java
        if (groupSet != null && !groupSet.isEmpty()) {
            groupKeySet = new HashSet<>();
            for (String group : groupSet) {
                recSet = group2RecordCache.get(group);
                if (recSet != null && !recSet.isEmpty()) {
                    groupKeySet.addAll(recSet);
                }
            }
            if (groupKeySet.isEmpty()) {
                return Collections.emptySet();
            }
        }
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/metamanage/metastore/impl/AbsConsumeCtrlMapperImpl.java`
#### Snippet
```java
        if (groupKeySet != null || topicKeySet != null) {
            if (groupKeySet == null) {
                totalMatchedSet = new HashSet<>(topicKeySet);
            } else {
                if (topicKeySet == null) {
                    totalMatchedSet = new HashSet<>(groupKeySet);
                } else {
                    totalMatchedSet = new HashSet<>();
                    for (String record : groupKeySet) {
                        if (topicKeySet.contains(record)) {
                            totalMatchedSet.add(record);
                        }
                    }
                }
            }
        }
        return totalMatchedSet;
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/metamanage/metastore/impl/AbsConsumeCtrlMapperImpl.java`
#### Snippet
```java
        consumeCtrlCache.put(entity.getRecordKey(), entity);
        // add topic index map
        ConcurrentHashSet<String> keySet =
                topic2RecordCache.get(entity.getTopicName());
        if (keySet == null) {
            ConcurrentHashSet<String> tmpSet = new ConcurrentHashSet<>();
            keySet = topic2RecordCache.putIfAbsent(entity.getTopicName(), tmpSet);
            if (keySet == null) {
                keySet = tmpSet;
            }
        }
        keySet.add(entity.getRecordKey());
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/metamanage/metastore/impl/AbsConsumeCtrlMapperImpl.java`
#### Snippet
```java
        if (curEntity == null) {
            return;
        }
        // add topic index
        ConcurrentHashSet<String> keySet =
                topic2RecordCache.get(curEntity.getTopicName());
        if (keySet != null) {
            keySet.remove(recordKey);
            if (keySet.isEmpty()) {
                topic2RecordCache.remove(curEntity.getTopicName(), new ConcurrentHashSet<>());
            }
        }
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/metamanage/metastore/impl/AbsMetaConfigMapperImpl.java`
#### Snippet
```java
                    if (curEntity != null) {
                        if (curEntity.isValidTopicStatus()) {
                            result.setFailResult(DataOpErrCode.DERR_EXISTED.getCode(),
                                    strBuff.append("Existed record found for brokerId-topicName(")
                                            .append(curEntity.getRecordKey()).append(")!").toString());
                        } else {
                            result.setFailResult(DataOpErrCode.DERR_EXISTED.getCode(),
                                    strBuff.append("Softly deleted record found for brokerId-topicName(")
                                            .append(curEntity.getRecordKey())
                                            .append("), please resume or remove it first!").toString());
                        }
                        strBuff.delete(0, strBuff.length());
                        return result.isSuccess();
                    }
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/metamanage/metastore/impl/AbsTopicDeployMapperImpl.java`
#### Snippet
```java
        keySet.add(entity.getRecordKey());
        // add brokerId topic map
        keySet = brokerId2TopicNameCache.get(entity.getBrokerId());
        if (keySet == null) {
            ConcurrentHashSet<String> tmpSet = new ConcurrentHashSet<>();
            keySet = brokerId2TopicNameCache.putIfAbsent(entity.getBrokerId(), tmpSet);
            if (keySet == null) {
                keySet = tmpSet;
            }
        }
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/metamanage/metastore/impl/AbsTopicDeployMapperImpl.java`
#### Snippet
```java
        keySet = brokerId2RecordCache.get(entity.getBrokerId());
        if (keySet == null) {
            ConcurrentHashSet<String> tmpSet = new ConcurrentHashSet<>();
            keySet = brokerId2RecordCache.putIfAbsent(entity.getBrokerId(), tmpSet);
            if (keySet == null) {
                keySet = tmpSet;
            }
        }
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/nodemanage/nodebroker/BrokerTopicInfoView.java`
#### Snippet
```java
        ConcurrentHashSet<String> curTopicSet = brokerIdIndexMap.get(brokerId);
        if (curTopicSet == null) {
            ConcurrentHashSet<String> newTopicSet = new ConcurrentHashSet<>();
            curTopicSet = brokerIdIndexMap.putIfAbsent(brokerId, newTopicSet);
            if (curTopicSet == null) {
                curTopicSet = newTopicSet;
            }
        }
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/nodemanage/nodebroker/DefBrokerRunManager.java`
#### Snippet
```java
        BrokerRunStatusInfo runStatusInfo =
                brokerRunSyncManageMap.get(brokerId);
        if (runStatusInfo == null) {
            logger.info(sBuffer.append("Get Broker run-info failure, brokerId=")
                    .append(brokerId).append(", please check the implement first!")
                    .toString());
            sBuffer.delete(0, sBuffer.length());
            return;
        }
        Tuple4<Long, Integer, String, List<String>> retTuple =
                runStatusInfo.getNeedSyncData();
        builder.setCurBrokerConfId(retTuple.getF0());
        builder.setConfCheckSumId(retTuple.getF1());
        Tuple2<Boolean, Boolean> autoFbdTuple =
                brokerAbnHolder.getBrokerAutoFbdStatus(brokerId);
        builder.setStopWrite(autoFbdTuple.getF0());
        builder.setStopRead(autoFbdTuple.getF1());
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/nodemanage/nodebroker/TopicPSInfoManager.java`
#### Snippet
```java
        for (String topic : topicSet) {
            ConcurrentHashSet<String> groupSet = topicSubInfoMap.get(topic);
            if (groupSet == null) {
                ConcurrentHashSet<String> tmpGroupSet =
                        new ConcurrentHashSet<>();
                groupSet = topicSubInfoMap.putIfAbsent(topic, tmpGroupSet);
                if (groupSet == null) {
                    groupSet = tmpGroupSet;
                }
            }
            groupSet.add(groupName);
        }
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/nodemanage/nodeconsumer/ConsumerInfoHolder.java`
#### Snippet
```java
                        if (groupSet == null) {
                            ConcurrentHashSet<String> tmpGroupSet = new ConcurrentHashSet<>();
                            groupSet = topicGroupMap.putIfAbsent(topicName, tmpGroupSet);
                            if (groupSet == null) {
                                groupSet = tmpGroupSet;
                            }
                        }
                        groupSet.add(group);
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebAdminFlowRuleHandler.java`
#### Snippet
```java
        GroupResCtrlEntity qryEntity = new GroupResCtrlEntity();
        // get queried operation info, for createUser, modifyUser, dataVersionId
        if (!WebParameterUtils.getQueriedOperateInfo(req, qryEntity, sBuffer, result)) {
            WebParameterUtils.buildFailResult(sBuffer, result.getErrMsg());
            return sBuffer;
        }
        // get group list
        if (!WebParameterUtils.getStringParamValue(req,
                WebFieldDef.COMPSGROUPNAME, false, null, sBuffer, result)) {
            WebParameterUtils.buildFailResult(sBuffer, result.getErrMsg());
            return sBuffer;
        }
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebAdminFlowRuleHandler.java`
#### Snippet
```java
        if (!WebParameterUtils.getQryPriorityIdParameter(req,
                false, TBaseConstants.META_VALUE_UNDEFINED,
                TServerConstants.QRY_PRIORITY_MIN_VALUE, sBuffer, result)) {
            WebParameterUtils.buildFailResult(sBuffer, result.getErrMsg());
            return sBuffer;
        }
        int inQryPriorityId = (int) result.getRetData();
        // get flowCtrlEnable's statusId info
        if (!WebParameterUtils.getFlowCtrlStatusParamValue(req, false, null, sBuffer, result)) {
            WebParameterUtils.buildFailResult(sBuffer, result.getErrMsg());
            return sBuffer;
        }
        EnableStatus flowCtrlEnable = (EnableStatus) result.getRetData();
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebAdminFlowRuleHandler.java`
#### Snippet
```java
        if (!WebParameterUtils.getAUDBaseInfo(req, false, null, sBuffer, result)) {
            WebParameterUtils.buildFailResult(sBuffer, result.getErrMsg());
            return sBuffer;
        }
        BaseEntity opEntity = (BaseEntity) result.getRetData();
        // get group list
        if (!WebParameterUtils.getStringParamValue(req,
                WebFieldDef.COMPSGROUPNAME, true, null, sBuffer, result)) {
            WebParameterUtils.buildFailResult(sBuffer, result.getErrMsg());
            return sBuffer;
        }
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebAdminFlowRuleHandler.java`
#### Snippet
```java
        if (!WebParameterUtils.getAUDBaseInfo(req, isAddOp, null, sBuffer, result)) {
            WebParameterUtils.buildFailResult(sBuffer, result.getErrMsg());
            return sBuffer;
        }
        BaseEntity opEntity = (BaseEntity) result.getRetData();
        // get group list
        if (!WebParameterUtils.getStringParamValue(req,
                WebFieldDef.COMPSGROUPNAME, true, null, sBuffer, result)) {
            WebParameterUtils.buildFailResult(sBuffer, result.getErrMsg());
            return sBuffer;
        }
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebGroupResCtrlHandler.java`
#### Snippet
```java
        EnableStatus flowCtrlEnable = (EnableStatus) result.getRetData();
        // get and flow control rule info
        int flowRuleCnt = WebParameterUtils.getAndCheckFlowRules(req,
                (isAddOp ? TServerConstants.BLANK_FLOWCTRL_RULES : null), sBuffer, result);
        if (!result.isSuccess()) {
            WebParameterUtils.buildFailResult(sBuffer, result.getErrMsg());
            return sBuffer;
        }
        String flowCtrlInfo = (String) result.getRetData();
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebAdminFlowRuleHandler.java`
#### Snippet
```java
        int totalCnt = 0;
        WebParameterUtils.buildSuccessWithDataRetBegin(sBuffer);
        for (GroupProcessResult entry : retInfo) {
            if (totalCnt++ > 0) {
                sBuffer.append(",");
            }
            sBuffer.append("{\"groupName\":\"").append(entry.getGroupName()).append("\"")
                    .append(",\"success\":").append(entry.isSuccess())
                    .append(",\"errCode\":").append(entry.getErrCode())
                    .append(",\"errInfo\":\"").append(entry.getErrMsg()).append("\"}");
        }
        WebParameterUtils.buildSuccessWithDataRetEnd(sBuffer, totalCnt);
        return sBuffer;
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebAdminGroupCtrlHandler.java`
#### Snippet
```java
        GroupConsumeCtrlEntity entity = new GroupConsumeCtrlEntity();
        // get queried operation info, for createUser, modifyUser, dataVersionId
        if (!WebParameterUtils.getQueriedOperateInfo(req, entity, sBuffer, result)) {
            WebParameterUtils.buildFailResult(sBuffer, result.getErrMsg());
            return sBuffer;
        }
        // get group list
        if (!WebParameterUtils.getStringParamValue(req,
                WebFieldDef.COMPSGROUPNAME, false, null, sBuffer, result)) {
            WebParameterUtils.buildFailResult(sBuffer, result.getErrMsg());
            return sBuffer;
        }
        Set<String> groupNameSet = (Set<String>) result.getRetData();
        // check and get topicName field
        if (!WebParameterUtils.getStringParamValue(req,
                WebFieldDef.COMPSTOPICNAME, false, null, sBuffer, result)) {
            WebParameterUtils.buildFailResult(sBuffer, result.getErrMsg());
            return sBuffer;
        }
        Set<String> topicNameSet = (Set<String>) result.getRetData();
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebAdminGroupCtrlHandler.java`
#### Snippet
```java
        GroupConsumeCtrlEntity qryEntity = new GroupConsumeCtrlEntity();
        // get queried operation info, for createUser, modifyUser, dataVersionId
        if (!WebParameterUtils.getQueriedOperateInfo(req, qryEntity, sBuffer, result)) {
            WebParameterUtils.buildFailResult(sBuffer, result.getErrMsg());
            return sBuffer;
        }
        // get group list
        if (!WebParameterUtils.getStringParamValue(req,
                WebFieldDef.COMPSGROUPNAME, false, null, sBuffer, result)) {
            WebParameterUtils.buildFailResult(sBuffer, result.getErrMsg());
            return sBuffer;
        }
        final Set<String> groupNameSet = (Set<String>) result.getRetData();
        // check and get topicName field
        if (!WebParameterUtils.getStringParamValue(req,
                WebFieldDef.COMPSTOPICNAME, false, null, sBuffer, result)) {
            WebParameterUtils.buildFailResult(sBuffer, result.getErrMsg());
            return sBuffer;
        }
        final Set<String> topicNameSet = (Set<String>) result.getRetData();
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebAdminGroupCtrlHandler.java`
#### Snippet
```java
        EnableStatus filterEnable = (EnableStatus) result.getRetData();
        // get filterConds info
        if (!WebParameterUtils.getFilterCondSet(req, false, true, sBuffer, result)) {
            WebParameterUtils.buildFailResult(sBuffer, result.getErrMsg());
            return sBuffer;
        }
        Set<String> filterCondSet = (Set<String>) result.getRetData();
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebAdminGroupCtrlHandler.java`
#### Snippet
```java
        if (!WebParameterUtils.getAUDBaseInfo(req, true, null, sBuffer, result)) {
            WebParameterUtils.buildFailResult(sBuffer, result.getErrMsg());
            return sBuffer;
        }
        BaseEntity opEntity = (BaseEntity) result.getRetData();
        // get group list
        if (!WebParameterUtils.getStringParamValue(req,
                WebFieldDef.COMPSGROUPNAME, true, null, sBuffer, result)) {
            WebParameterUtils.buildFailResult(sBuffer, result.getErrMsg());
            return sBuffer;
        }
        Set<String> groupNameSet = (Set<String>) result.getRetData();
        // check and get topicName field
        if (!WebParameterUtils.getAndValidTopicNameInfo(req,
                defMetaDataService, true, null, sBuffer, result)) {
            WebParameterUtils.buildFailResult(sBuffer, result.getErrMsg());
            return sBuffer;
        }
        Set<String> topicNameSet = (Set<String>) result.getRetData();
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebAdminGroupCtrlHandler.java`
#### Snippet
```java
        if (!WebParameterUtils.getAUDBaseInfo(req, false, null, sBuffer, result)) {
            WebParameterUtils.buildFailResult(sBuffer, result.getErrMsg());
            return sBuffer;
        }
        BaseEntity opEntity = (BaseEntity) result.getRetData();
        // check and get topicName field
        if (!WebParameterUtils.getStringParamValue(req,
                WebFieldDef.COMPSTOPICNAME, true, null, sBuffer, result)) {
            WebParameterUtils.buildFailResult(sBuffer, result.getErrMsg());
            return sBuffer;
        }
        Set<String> topicNameSet = (Set<String>) result.getRetData();
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebAdminGroupCtrlHandler.java`
#### Snippet
```java
        List<Map<String, String>> groupJsonArray =
                (List<Map<String, String>>) result.getRetData();
        GroupConsumeCtrlEntity itemEntity;
        Map<String, GroupConsumeCtrlEntity> addRecordMap = new HashMap<>();
        Set<String> configuredTopicSet =
                defMetaDataService.getDeployedTopicSet();
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebAdminGroupCtrlHandler.java`
#### Snippet
```java
            if (!WebParameterUtils.getAUDBaseInfo(itemValueMap,
                    isAddOp, defOpEntity, sBuffer, result)) {
                return result.isSuccess();
            }
            final BaseEntity itemOpEntity = (BaseEntity) result.getRetData();
            // get group configure info
            if (!WebParameterUtils.getStringParamValue(itemValueMap,
                    WebFieldDef.GROUPNAME, true, "", sBuffer, result)) {
                return result.isSuccess();
            }
            final String groupName = (String) result.getRetData();
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebAdminGroupCtrlHandler.java`
#### Snippet
```java
            if (!WebParameterUtils.getStringParamValue(itemValueMap,
                    WebFieldDef.TOPICNAME, true, "", sBuffer, result)) {
                return result.isSuccess();
            }
            String topicName = (String) result.getRetData();
            if (!configuredTopicSet.contains(topicName)) {
                result.setFailResult(sBuffer
                        .append(WebFieldDef.TOPICNAME.name)
                        .append(" ").append(topicName)
                        .append(" is not configure, please configure first!").toString());
                sBuffer.delete(0, sBuffer.length());
                return result.isSuccess();
            }
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebAdminGroupCtrlHandler.java`
#### Snippet
```java
            if (!WebParameterUtils.getAUDBaseInfo(itemValueMap,
                    true, defOpEntity, sBuffer, result)) {
                return result.isSuccess();
            }
            final BaseEntity itemOpEntity = (BaseEntity) result.getRetData();
            // get group configure info
            if (!WebParameterUtils.getStringParamValue(itemValueMap,
                    WebFieldDef.GROUPNAME, true, "", sBuffer, result)) {
                return result.isSuccess();
            }
            String groupName = (String) result.getRetData();
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebAdminGroupCtrlHandler.java`
#### Snippet
```java
        if (addRecordMap.isEmpty()) {
            result.setFailResult(sBuffer
                    .append("Not found record info in ")
                    .append(WebFieldDef.GROUPJSONSET.name)
                    .append(" parameter!").toString());
            sBuffer.delete(0, sBuffer.length());
            return result.isSuccess();
        }
        result.setSuccResult(addRecordMap);
        return result.isSuccess();
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebAdminTopicAuthHandler.java`
#### Snippet
```java
        TopicCtrlEntity qryEntity = new TopicCtrlEntity();
        // get queried operation info, for createUser, modifyUser, dataVersionId
        if (!WebParameterUtils.getQueriedOperateInfo(req, qryEntity, sBuffer, result)) {
            WebParameterUtils.buildFailResult(sBuffer, result.getErrMsg());
            return sBuffer;
        }
        // check and get topicName field
        if (!WebParameterUtils.getStringParamValue(req,
                WebFieldDef.COMPSTOPICNAME, false, null, sBuffer, result)) {
            WebParameterUtils.buildFailResult(sBuffer, result.getErrMsg());
            return sBuffer;
        }
        Set<String> topicNameSet = (Set<String>) result.getRetData();
        // query matched records
        Map<String, TopicCtrlEntity> topicCtrlMap =
                defMetaDataService.getTopicCtrlConf(topicNameSet, qryEntity);
        // build query result
        int totalCnt = 0;
        WebParameterUtils.buildSuccessWithDataRetBegin(sBuffer);
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebAdminTopicAuthHandler.java`
#### Snippet
```java
        if (addRecordMap.isEmpty()) {
            result.setFailResult(sBuffer
                    .append("Not found record in ")
                    .append(WebFieldDef.TOPICJSONSET.name)
                    .append(" parameter!").toString());
            sBuffer.delete(0, sBuffer.length());
            return result.isSuccess();
        }
        result.setSuccResult(addRecordMap);
        return result.isSuccess();
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebAdminTopicAuthHandler.java`
#### Snippet
```java
        int totalCnt = 0;
        WebParameterUtils.buildSuccessWithDataRetBegin(sBuffer);
        for (TopicProcessResult entry : retInfo) {
            if (entry == null) {
                continue;
            }
            if (totalCnt++ > 0) {
                sBuffer.append(",");
            }
            sBuffer.append("{\"topicName\":\"").append(entry.getTopicName()).append("\"")
                    .append(",\"success\":").append(entry.isSuccess())
                    .append(",\"errCode\":").append(entry.getErrCode())
                    .append(",\"errInfo\":\"").append(entry.getErrMsg()).append("\"}");
        }
        WebParameterUtils.buildSuccessWithDataRetEnd(sBuffer, totalCnt);
        return sBuffer;
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebBrokerConfHandler.java`
#### Snippet
```java
        BrokerConfEntity qryEntity = new BrokerConfEntity();
        // get queried operation info, for createUser, modifyUser, dataVersionId
        if (!WebParameterUtils.getQueriedOperateInfo(req, qryEntity, sBuffer, result)) {
            WebParameterUtils.buildFailResult(sBuffer, result.getErrMsg());
            return sBuffer;
        }
        // check and get brokerId field
        if (!WebParameterUtils.getIntParamValue(req,
                WebFieldDef.COMPSBROKERID, false, sBuffer, result)) {
            WebParameterUtils.buildFailResult(sBuffer, result.getErrMsg());
            return sBuffer;
        }
        final Set<Integer> brokerIds = (Set<Integer>) result.getRetData();
        // get brokerIp info
        if (!WebParameterUtils.getStringParamValue(req,
                WebFieldDef.COMPBROKERIP, false, null, sBuffer, result)) {
            WebParameterUtils.buildFailResult(sBuffer, result.getErrMsg());
            return sBuffer;
        }
        final Set<String> brokerIpSet = (Set<String>) result.getRetData();
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebBrokerConfHandler.java`
#### Snippet
```java
        if (!WebParameterUtils.getIntParamValue(req, WebFieldDef.BROKERPORT,
                false, TBaseConstants.META_VALUE_UNDEFINED, 1, sBuffer, result)) {
            WebParameterUtils.buildFailResult(sBuffer, result.getErrMsg());
            return sBuffer;
        }
        final int brokerPort = (int) result.getRetData();
        // get brokerTlsPort field
        if (!WebParameterUtils.getIntParamValue(req, WebFieldDef.BROKERTLSPORT,
                false, TBaseConstants.META_VALUE_UNDEFINED, 1, sBuffer, result)) {
            WebParameterUtils.buildFailResult(sBuffer, result.getErrMsg());
            return sBuffer;
        }
        final int brokerTlsPort = (int) result.getRetData();
        // get brokerWebPort field
        if (!WebParameterUtils.getIntParamValue(req, WebFieldDef.BROKERWEBPORT,
                false, TBaseConstants.META_VALUE_UNDEFINED, 1, sBuffer, result)) {
            WebParameterUtils.buildFailResult(sBuffer, result.getErrMsg());
            return sBuffer;
        }
        final int brokerWebPort = (int) result.getRetData();
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebBrokerConfHandler.java`
#### Snippet
```java
        if (!WebParameterUtils.getIntParamValue(req,
                WebFieldDef.COMPSBROKERID, true, sBuffer, result)) {
            WebParameterUtils.buildFailResult(sBuffer, result.getErrMsg());
            return sBuffer;
        }
        Set<Integer> brokerIds = (Set<Integer>) result.getRetData();
        List<BrokerProcessResult> retInfo = new ArrayList<>();
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebBrokerConfHandler.java`
#### Snippet
```java
        if (!WebParameterUtils.getAUDBaseInfo(req, false, null, sBuffer, result)) {
            WebParameterUtils.buildFailResult(sBuffer, result.getErrMsg());
            return sBuffer;
        }
        BaseEntity opEntity = (BaseEntity) result.getRetData();
        // check and get brokerId field
        if (!WebParameterUtils.getIntParamValue(req,
                WebFieldDef.COMPSBROKERID, true, sBuffer, result)) {
            WebParameterUtils.buildFailResult(sBuffer, result.getErrMsg());
            return sBuffer;
        }
        Set<Integer> brokerIds = (Set<Integer>) result.getRetData();
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebMasterInfoHandler.java`
#### Snippet
```java
        if (!WebParameterUtils.getIntParamValue(req,
                WebFieldDef.COMPSBROKERID, false, sBuffer, result)) {
            WebParameterUtils.buildFailResult(sBuffer, result.getErrMsg());
            return sBuffer;
        }
        Set<Integer> brokerIds = (Set<Integer>) result.getRetData();
        // check and get topicName field
        if (!WebParameterUtils.getStringParamValue(req,
                WebFieldDef.COMPSTOPICNAME, false, null, sBuffer, result)) {
            WebParameterUtils.buildFailResult(sBuffer, result.getErrMsg());
            return sBuffer;
        }
        Set<String> topicNameSet = (Set<String>) result.getRetData();
        // query topic configure info
        Map<String, List<TopicDeployEntity>> topicConfMap =
                defMetaDataService.getTopicConfMapByTopicAndBrokerIds(topicNameSet, brokerIds);
        BrokerRunManager brokerRunManager = master.getBrokerRunManager();
        int totalCount = 0;
        int brokerCount = 0;
        int totalCfgNumPartCount = 0;
        int totalRunNumPartCount = 0;
        int totalCfgTopicStoreCount = 0;
        int totalRunTopicStoreCount = 0;
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebMasterInfoHandler.java`
#### Snippet
```java
        boolean isSrvAcceptPublish = false;
        boolean isSrvAcceptSubscribe = false;
        boolean enableAuthControl = false;
        TopicPropGroup topicProps;
        TopicCtrlEntity authEntity;
        BrokerConfEntity brokerConfEntity;
        Tuple3<Boolean, Boolean, TopicInfo> topicInfoTuple = new Tuple3<>();
        WebParameterUtils.buildSuccessWithDataRetBegin(sBuffer);
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebTopicCtrlHandler.java`
#### Snippet
```java
            if (!WebParameterUtils.getAUDBaseInfo(itemConfMap,
                    isAddOp, defOpEntity, sBuffer, result)) {
                return result.isSuccess();
            }
            final BaseEntity itemOpEntity = (BaseEntity) result.getRetData();
            // get topicName configure info
            if (!WebParameterUtils.getStringParamValue(itemConfMap,
                    WebFieldDef.TOPICNAME, true, "", sBuffer, result)) {
                return result.isSuccess();
            }
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebTopicDeployHandler.java`
#### Snippet
```java
        int recordCount = 0;
        int topicTotalCfgCnt = 0;
        int storeTotalCfgCnt = 0;
        int partTotalCfgCnt = 0;
        int topicAccPubTotalCnt = 0;
        int topicAccSubTotalCnt = 0;
        int storeAccPubTotalCnt = 0;
        int storeAccSubTotalCnt = 0;
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebTopicDeployHandler.java`
#### Snippet
```java
            if (recordCount++ > 0) {
                sBuffer.append(",");
            }
            // reset the value of data items
            brokerTotalCfgCnt = 0;
            brokerTotalRunCnt = 0;
            storeTotalCfgCnt = 0;
            storeTotalRunCnt = 0;
            partTotalCfgCnt = 0;
            partTotalRunCnt = 0;
            brokerAccPubTotalCnt = 0;
            brokerAccSubTotalCnt = 0;
            storeAccPubTotalCnt = 0;
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebTopicDeployHandler.java`
#### Snippet
```java
        TopicDeployEntity qryEntity = new TopicDeployEntity();
        // get queried operation info, for createUser, modifyUser, dataVersionId
        if (!WebParameterUtils.getQueriedOperateInfo(req, qryEntity, sBuffer, result)) {
            WebParameterUtils.buildFailResult(sBuffer, result.getErrMsg());
            return sBuffer;
        }
        // check and get topicName field
        if (!WebParameterUtils.getStringParamValue(req,
                WebFieldDef.COMPSTOPICNAME, false, null, sBuffer, result)) {
            WebParameterUtils.buildFailResult(sBuffer, result.getErrMsg());
            return sBuffer;
        }
        final Set<String> topicNameSet = (Set<String>) result.getRetData();
        // check and get brokerId field
        if (!WebParameterUtils.getIntParamValue(req,
                WebFieldDef.COMPSBROKERID, false, sBuffer, result)) {
            WebParameterUtils.buildFailResult(sBuffer, result.getErrMsg());
            return sBuffer;
        }
        final Set<Integer> brokerIdSet = (Set<Integer>) result.getRetData();
        // get brokerPort field
        if (!WebParameterUtils.getIntParamValue(req, WebFieldDef.BROKERPORT,
                false, TBaseConstants.META_VALUE_UNDEFINED, 1, sBuffer, result)) {
            WebParameterUtils.buildFailResult(sBuffer, result.getErrMsg());
            return sBuffer;
        }
        final int brokerPort = (int) result.getRetData();
        // get and valid topicProps info
        if (!WebParameterUtils.getTopicPropInfo(req, null, sBuffer, result)) {
            WebParameterUtils.buildFailResult(sBuffer, result.getErrMsg());
            return sBuffer;
        }
        TopicPropGroup topicProps = (TopicPropGroup) result.getRetData();
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebTopicDeployHandler.java`
#### Snippet
```java
        if (!WebParameterUtils.getTopicStatusParamValue(req,
                false, TopicStatus.STATUS_TOPIC_UNDEFINED, sBuffer, result)) {
            WebParameterUtils.buildFailResult(sBuffer, result.getErrMsg());
            return sBuffer;
        }
        TopicStatus topicStatus = (TopicStatus) result.getRetData();
        qryEntity.updModifyInfo(qryEntity.getDataVerId(),
                TBaseConstants.META_VALUE_UNDEFINED,
                brokerPort, null, topicStatus, topicProps);
        Map<String, List<TopicDeployEntity>> topicDeployInfoMap =
                defMetaDataService.getTopicDeployInfoMap(topicNameSet, brokerIdSet, qryEntity);
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebTopicDeployHandler.java`
#### Snippet
```java
        boolean isSrvAcceptPublish = false;
        boolean isSrvAcceptSubscribe = false;
        String strManageStatus;
        TopicCtrlEntity ctrlEntity;
        BrokerConfEntity brokerConfEntity;
        List<GroupConsumeCtrlEntity> groupCtrlInfoLst;
        Tuple3<Boolean, Boolean, TopicInfo> topicInfoTuple = new Tuple3<>();
        BrokerRunManager brokerRunManager = master.getBrokerRunManager();
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebTopicDeployHandler.java`
#### Snippet
```java
            totalCfgNumPartCount = 0;
            totalRunNumPartCount = 0;
            isSrvAcceptPublish = false;
            isSrvAcceptSubscribe = false;
            ctrlEntity = defMetaDataService.getTopicCtrlByTopicName(entry.getKey());
            if (ctrlEntity == null) {
                continue;
            }
            if (totalCnt++ > 0) {
                sBuffer.append(",");
            }
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebTopicDeployHandler.java`
#### Snippet
```java
if (brokerConfEntity != null) {
                    strManageStatus = brokerConfEntity.getManageStatus().getDescription();
                }
                brokerRunManager.getPubBrokerTopicInfo(
                        entity.getBrokerId(), entity.getTopicName(), topicInfoTuple);
                if (topicInfoTuple.getF2() == null) {
                    sBuffer.append("\"acceptPublish\":\"-\"").append(",\"acceptSubscribe\":\"-\"")
                            .append(",\"numPartitions\":\"-\"").append(",\"brokerManageStatus\":\"-\"");
                } else {
                    if (topicInfoTuple.getF0()) {
                        sBuffer.append("\"acceptPublish\":")
                                .append(topicInfoTuple.getF2().isAcceptPublish());
                        if (topicInfoTuple.getF2().isAcceptPublish()) {
                            isSrvAcceptPublish = true;
                        }
                    } else {
                        sBuffer.append("\"acceptPublish\":false");
                    }
                    if (topicInfoTuple.getF1()) {
                        sBuffer.append(",\"acceptSubscribe\":")
                                .append(topicInfoTuple.getF2().isAcceptSubscribe());
                        if (topicInfoTuple.getF2().isAcceptSubscribe()) {
                            isSrvAcceptSubscribe = true;
                        }
                    } else {
                        sBuffer.append(",\"acceptSubscribe\":false");
                    }
                    totalRunNumPartCount +=
                            topicInfoTuple.getF2().getPartitionNum() * topicInfoTuple.getF2().getTopicStoreNum();
                    sBuffer.append(",\"numPartitions\":")
                            .append(topicInfoTuple.getF2().getPartitionNum())
                            .append(",\"numTopicStores\":")
                            .append(topicInfoTuple.getF2().getTopicStoreNum())
                            .append(",\"brokerManageStatus\":\"").append(strManageStatus).append("\"");
                }
                sBuffer.append("}}");
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/tools/cli/CliBrokerAdmin.java`
#### Snippet
```java
        CommandLine cli = parser.parse(options, args);
        if (cli == null) {
            throw new ParseException("Parse args failure");
        }
        if (cli.hasOption(CliArgDef.VERSION.longOpt)) {
            version();
        }
        if (cli.hasOption(CliArgDef.HELP.longOpt)) {
            help();
        }
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/tools/cli/CliConsumer.java`
#### Snippet
```java
        String rpcTimeoutStr = cli.getOptionValue(CliArgDef.RPCTIMEOUT.longOpt);
        if (TStringUtils.isNotBlank(rpcTimeoutStr)) {
            rpcTimeoutMs = Long.parseLong(rpcTimeoutStr);
        }
        String clientCntStr = cli.getOptionValue(CliArgDef.CLIENTCOUNT.longOpt);
        if (TStringUtils.isNotBlank(clientCntStr)) {
            clientCount = Integer.parseInt(clientCntStr);
        }
        String printIntMsStr = cli.getOptionValue(CliArgDef.OUTPUTINTERVAL.longOpt);
        if (TStringUtils.isNotBlank(printIntMsStr)) {
            printIntervalMs = Long.parseLong(printIntMsStr);
            if (printIntervalMs < 5000) {
                throw new Exception("Invalid "
                        + CliArgDef.OUTPUTINTERVAL.longOpt
                        + " parameter value!");
            }
        }
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/tools/cli/CliConsumer.java`
#### Snippet
```java
                    PushMessageConsumer consumer1 =
                            msgSessionFactory.createPushConsumer(consumerConfig);
                    for (Map.Entry<String, TreeSet<String>> entry : topicAndFiltersMap.entrySet()) {
                        consumer1.subscribe(entry.getKey(), entry.getValue(), msgListener);
                        TOPIC_COUNT_MAP.put(entry.getKey(), new AtomicLong(0));
                    }
                    consumer1.completeSubscribe();
                    consumerMap.put(consumer1, null);
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/tools/cli/CliConsumer.java`
#### Snippet
```java
                    PullMessageConsumer consumer2 =
                            msgSessionFactory.createPullConsumer(consumerConfig);
                    for (Map.Entry<String, TreeSet<String>> entry : topicAndFiltersMap.entrySet()) {
                        consumer2.subscribe(entry.getKey(), entry.getValue());
                        TOPIC_COUNT_MAP.put(entry.getKey(), new AtomicLong(0));
                    }
                    consumer2.completeSubscribe();
                    consumerMap.put(consumer2,
                            new TupleValue(consumer2, msgCount, fetchThreadCnt));
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/tools/cli/CliMetaDataBRU.java`
#### Snippet
```java
        Map<String, ClusterSettingEntity> storedSettingMap =
                (Map<String, ClusterSettingEntity>) readObjectFromFile(
                        backupAndRecoveryPath, storeFileNameClusterConf);
        if (storedSettingMap == null) {
            logger.error(strBuff.append("  read configure file ")
                    .append(backupAndRecoveryPath).append("/")
                    .append(storeFileNameClusterConf).append(" failure!").toString());
            strBuff.delete(0, strBuff.length());
            return false;
        }
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/tools/cli/CliMetaDataBRU.java`
#### Snippet
```java
                int qryPriorityId = jsonItem.get("qryPriorityId").getAsInt();
                boolean flowCtrlEnable = jsonItem.get("flowCtrlEnable").getAsBoolean();
                int flowRuleCnt = jsonItem.get("flowCtrlRuleCount").getAsInt();
                JsonArray flowCtrlInfoArray = jsonItem.get("flowCtrlInfo").getAsJsonArray();
                String flowCtrlInfoStr = flowCtrlInfoArray.toString();
                EnableStatus flowCtrlStatus =
                        flowCtrlEnable ? EnableStatus.STATUS_ENABLE : EnableStatus.STATUS_DISABLE;
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/tools/cli/CliMetaDataBRU.java`
#### Snippet
```java
        Map<String, BrokerConfEntity> storedBrokerConfigMap =
                (Map<String, BrokerConfEntity>) readObjectFromFile(
                        backupAndRecoveryPath, storeFileNameBrokerConf);
        if (storedBrokerConfigMap == null) {
            logger.error(strBuff.append("  read configure file ")
                    .append(backupAndRecoveryPath).append("/")
                    .append(storeFileNameBrokerConf).append(" failure!").toString());
            strBuff.delete(0, strBuff.length());
            return false;
        }
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/tools/cli/CliMetaDataBRU.java`
#### Snippet
```java
                inParamMap.put("brokerJsonSet", "[" + strBuff.toString() + "]");
                strBuff.delete(0, strBuff.length());
                inParamMap.put("createUser", curOperator);
                inParamMap.put("modifyUser", curOperator);
                if (!writeDataToMaster("admin_batch_add_broker_configure",
                        authToken, inParamMap, strBuff, result)) {
                    return false;
                }
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/tools/cli/CliMetaDataBRU.java`
#### Snippet
```java
        Map<String, TopicCtrlEntity> storedTopicCtrlMap =
                (Map<String, TopicCtrlEntity>) readObjectFromFile(
                        backupAndRecoveryPath, storeFileNameTopicCtrl);
        if (storedTopicCtrlMap == null) {
            logger.error(strBuff.append("  read configure file ")
                    .append(backupAndRecoveryPath).append("/")
                    .append(storeFileNameTopicCtrl).append(" failure!").toString());
            strBuff.delete(0, strBuff.length());
            return false;
        }
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/tools/cli/CliMetaDataBRU.java`
#### Snippet
```java
                inParamMap.put("topicCtrlJsonSet", "[" + strBuff.toString() + "]");
                strBuff.delete(0, strBuff.length());
                inParamMap.put("createUser", curOperator);
                inParamMap.put("modifyUser", curOperator);
                if (!writeDataToMaster("admin_batch_add_topic_control_info",
                        authToken, inParamMap, strBuff, result)) {
                    return false;
                }
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/tools/cli/CliMetaDataBRU.java`
#### Snippet
```java
        Map<String, TopicDeployEntity> storedTopicDeployMap =
                (Map<String, TopicDeployEntity>) readObjectFromFile(
                        backupAndRecoveryPath, storeFileNameTopicDeploy);
        if (storedTopicDeployMap == null) {
            logger.error(strBuff.append("  read configure file ")
                    .append(backupAndRecoveryPath).append("/")
                    .append(storeFileNameTopicDeploy).append(" failure!").toString());
            strBuff.delete(0, strBuff.length());
            return false;
        }
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/tools/cli/CliMetaDataBRU.java`
#### Snippet
```java
                inParamMap.put("topicJsonSet", "[" + strBuff.toString() + "]");
                strBuff.delete(0, strBuff.length());
                inParamMap.put("createUser", curOperator);
                inParamMap.put("modifyUser", curOperator);
                if (!writeDataToMaster("admin_bath_add_topic_deploy_info",
                        authToken, inParamMap, strBuff, result)) {
                    return false;
                }
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/tools/cli/CliMetaDataBRU.java`
#### Snippet
```java
                inParamMap.put("topicJsonSet", "[" + strBuff.toString() + "]");
                strBuff.delete(0, strBuff.length());
                inParamMap.put("createUser", curOperator);
                inParamMap.put("modifyUser", curOperator);
                if (!writeDataToMaster("admin_batch_update_topic_deploy_info",
                        authToken, inParamMap, strBuff, result)) {
                    return false;
                }
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/tools/cli/CliMetaDataBRU.java`
#### Snippet
```java
        Map<String, GroupResCtrlEntity> storedGroupCtrlMap =
                (Map<String, GroupResCtrlEntity>) readObjectFromFile(
                        backupAndRecoveryPath, storeFileNameGroupCtrl);
        if (storedGroupCtrlMap == null) {
            logger.error(strBuff.append("  read configure file ")
                    .append(backupAndRecoveryPath).append("/")
                    .append(storeFileNameGroupCtrl).append(" failure!").toString());
            strBuff.delete(0, strBuff.length());
            return false;
        }
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/tools/cli/CliMetaDataBRU.java`
#### Snippet
```java
        Map<String, GroupConsumeCtrlEntity> storedGroupCsmMap =
                (Map<String, GroupConsumeCtrlEntity>) readObjectFromFile(
                        backupAndRecoveryPath, storeFileNameCsmCtrl);
        if (storedGroupCsmMap == null) {
            logger.error(strBuff.append("  read configure file ")
                    .append(backupAndRecoveryPath).append("/")
                    .append(storeFileNameCsmCtrl).append(" failure!").toString());
            strBuff.delete(0, strBuff.length());
            return false;
        }
```

### DuplicatedCode
Duplicated code
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/tools/cli/CliMetaDataBRU.java`
#### Snippet
```java
                inParamMap.put("groupCsmJsonSet", "[" + strBuff.toString() + "]");
                strBuff.delete(0, strBuff.length());
                inParamMap.put("createUser", curOperator);
                inParamMap.put("modifyUser", curOperator);
                if (!writeDataToMaster("admin_batch_add_group_csmctrl_info",
                        authToken, inParamMap, strBuff, result)) {
                    return false;
                }
```

## RuleId[id=ProtectedMemberInFinalClass]
### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/iceberg/src/main/java/org/apache/inlong/sort/iceberg/sink/collections/RocksDBKVBuffer.java`
#### Snippet
```java
    private transient boolean closed = false;
    private transient Thread shutdownThread = null;
    protected String diskMapPath;
    protected final TypeSerializer<T> keySerializer;
    protected final TypeSerializer<R> valueSerializer;
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/iceberg/src/main/java/org/apache/inlong/sort/iceberg/sink/collections/RocksDBKVBuffer.java`
#### Snippet
```java
    protected final TypeSerializer<T> keySerializer;
    protected final TypeSerializer<R> valueSerializer;
    protected final String rocksDbStoragePath;

    public RocksDBKVBuffer(
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/iceberg/src/main/java/org/apache/inlong/sort/iceberg/sink/collections/RocksDBKVBuffer.java`
#### Snippet
```java
    protected String diskMapPath;
    protected final TypeSerializer<T> keySerializer;
    protected final TypeSerializer<R> valueSerializer;
    protected final String rocksDbStoragePath;

```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/iceberg/src/main/java/org/apache/inlong/sort/iceberg/sink/collections/RocksDBKVBuffer.java`
#### Snippet
```java
    private transient Thread shutdownThread = null;
    protected String diskMapPath;
    protected final TypeSerializer<T> keySerializer;
    protected final TypeSerializer<R> valueSerializer;
    protected final String rocksDbStoragePath;
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/postgres-cdc/src/main/java/org/apache/inlong/sort/cdc/postgres/debezium/internal/ColumnImpl.java`
#### Snippet
```java
    }

    protected ColumnImpl(String columnName, int position, int jdbcType, int nativeType, String typeName,
            String typeExpression, String charsetName, String defaultCharsetName, int columnLength, Integer columnScale,
            List<String> enumValues, boolean optional, boolean autoIncremented, boolean generated, Object defaultValue,
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/postgres-cdc/src/main/java/org/apache/inlong/sort/cdc/postgres/debezium/internal/ColumnImpl.java`
#### Snippet
```java
    private final List<String> enumValues;

    protected ColumnImpl(String columnName, int position, int jdbcType, int componentType, String typeName,
            String typeExpression, String charsetName, String defaultCharsetName, int columnLength, Integer columnScale,
            boolean optional, boolean autoIncremented, boolean generated) {
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/postgres-cdc/src/main/java/org/apache/inlong/sort/cdc/postgres/debezium/internal/ColumnImpl.java`
#### Snippet
```java
    }

    protected ColumnImpl(String columnName, int position, int jdbcType, int nativeType, String typeName,
            String typeExpression, String charsetName, String defaultCharsetName, int columnLength, Integer columnScale,
            boolean optional, boolean autoIncremented, boolean generated, Object defaultValue,
```

## RuleId[id=SwitchStatementWithConfusingDeclaration]
### SwitchStatementWithConfusingDeclaration
Local variable `instantTime` declared in one 'switch' branch and used in another
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/debezium/table/RowDataDebeziumDeserializeSchema.java`
#### Snippet
```java
                break;
            case Timestamp.SCHEMA_NAME:
                Instant instantTime = Instant.ofEpochMilli((Long) fieldValue);
                fieldValue = LocalDateTime.ofInstant(instantTime, ZONE_UTC).toString();
                break;
```

## RuleId[id=TrivialIf]
### TrivialIf
`if` statement can be simplified
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/NettyClient.java`
#### Snippet
```java
        NettyClient other = (NettyClient) obj;
        if (channel == null) {
            if (other.channel != null) {
                return false;
            }
```

### TrivialIf
`if` statement can be simplified
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/NettyClient.java`
#### Snippet
```java
                return false;
            }
        } else if (!channel.equals(other.channel)) {
            return false;
        }
```

### TrivialIf
`if` statement can be simplified
in `inlong-sort/sort-flink/base/src/main/java/org/apache/inlong/sort/base/dirty/sink/s3/S3DirtySink.java`
#### Snippet
```java
            return false;
        }
        if (System.currentTimeMillis() - lastExecutetime < s3Options.getBatchIntervalMs()) {
            return false;
        }
```

### TrivialIf
`if` statement can be simplified
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/mysql/source/utils/RecordUtils.java`
#### Snippet
```java
    public static boolean isSchemaChangeEvent(SourceRecord sourceRecord) {
        Schema keySchema = sourceRecord.keySchema();
        if (keySchema != null && SCHEMA_CHANGE_EVENT_KEY_NAME.equalsIgnoreCase(keySchema.name())) {
            return true;
        }
```

### TrivialIf
`if` statement can be simplified
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/mysql/source/utils/RecordUtils.java`
#### Snippet
```java
    public static boolean isHighWatermarkEvent(SourceRecord record) {
        Optional<SignalEventDispatcher.WatermarkKind> watermarkKind = getWatermarkKind(record);
        if (watermarkKind.isPresent() && watermarkKind.get() == SignalEventDispatcher.WatermarkKind.HIGH) {
            return true;
        }
```

### TrivialIf
`if` statement can be simplified
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/mysql/source/utils/RecordUtils.java`
#### Snippet
```java
    public static boolean isEndWatermarkEvent(SourceRecord record) {
        Optional<SignalEventDispatcher.WatermarkKind> watermarkKind = getWatermarkKind(record);
        if (watermarkKind.isPresent() && watermarkKind.get() == SignalEventDispatcher.WatermarkKind.BINLOG_END) {
            return true;
        }
```

### TrivialIf
`if` statement can be simplified
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/mysql/source/utils/RecordUtils.java`
#### Snippet
```java
    public static boolean isLowWatermarkEvent(SourceRecord record) {
        Optional<SignalEventDispatcher.WatermarkKind> watermarkKind = getWatermarkKind(record);
        if (watermarkKind.isPresent() && watermarkKind.get() == SignalEventDispatcher.WatermarkKind.LOW) {
            return true;
        }
```

### TrivialIf
`if` statement can be simplified
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mongodb-cdc/src/main/java/org/apache/inlong/sort/cdc/mongodb/source/reader/fetch/MongoDBFetchTaskContext.java`
#### Snippet
```java

        // keyValue -> [lower, upper)
        if (compareBsonValue(lowerBound, keyValue) <= 0
                && compareBsonValue(keyValue, upperBound) < 0) {
            return true;
```

### TrivialIf
`if` statement can be simplified
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mongodb-cdc/src/main/java/org/apache/inlong/sort/cdc/mongodb/debezium/utils/RecordUtils.java`
#### Snippet
```java
     */
    public static boolean isDMLOperation(OperationType op) {
        if (OperationType.INSERT.equals(op) || OperationType.DELETE.equals(op)
                || OperationType.UPDATE.equals(op) || OperationType.REPLACE.equals(op)) {
            return true;
```

### TrivialIf
`if` statement can be simplified
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/utils/InLongDatabaseHistory.java`
#### Snippet
```java
        if (var1.hasNext()) {
            SchemaRecord record = (SchemaRecord) var1.next();
            if (!record.isHistoryRecord()) {
                return false;
            }
```

### TrivialIf
`if` statement can be simplified
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/utils/LogCounter.java`
#### Snippet
```java
        }

        if (counter.incrementAndGet() > start && counter.get() % control != 0) {
            return false;
        }
```

### TrivialIf
`if` statement can be simplified
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/sources/reader/file/FileReaderOperator.java`
#### Snippet
```java
                AgentConstants.AGENT_JOB_STORE_TIME, AgentConstants.DEFAULT_JOB_STORE_TIME);

        if (System.currentTimeMillis() - jobStoreTime > storeTime) {
            this.firstStored = false;
        } else {
```

### TrivialIf
`if` statement can be simplified
in `inlong-common/src/main/java/org/apache/inlong/common/pojo/agent/DataConfig.java`
#### Snippet
```java
            return true;
        }
        if (reportType == DIRECT_SEND_TO_MQ && CollectionUtils.isNotEmpty(mqClusters) && mqClusters.stream()
                .allMatch(MQClusterInfo::isValid) && topicInfo.isValid()) {
            return true;
```

### TrivialIf
`if` statement can be simplified
in `inlong-common/src/main/java/org/apache/inlong/common/pojo/dataproxy/MQClusterInfo.java`
#### Snippet
```java

    public boolean isValid() {
        if (StringUtils.isBlank(mqType) || StringUtils.isBlank(url)) {
            return false;
        }
```

### TrivialIf
`if` statement can be simplified
in `inlong-common/src/main/java/org/apache/inlong/common/monitor/LogCounter.java`
#### Snippet
```java
        }

        if (counter.incrementAndGet() > start && counter.get() % control != 0) {
            return false;
        }
```

### TrivialIf
`if` statement can be simplified
in `inlong-common/src/main/java/org/apache/inlong/common/msg/InLongMsg.java`
#### Snippet
```java
                + BIN_MSG_ATTRLEN_SIZE + attrLen) & 0xFFFF);

        if ((totalLen + BIN_MSG_TOTALLEN_SIZE != (bodyLen + attrLen + BIN_MSG_FORMAT_SIZE))
                || (msgMagic != BIN_MSG_MAGIC)) {
            return false;
```

### TrivialIf
`if` statement can be simplified
in `inlong-common/src/main/java/org/apache/inlong/common/msg/InLongMsg.java`
#### Snippet
```java
    public boolean isfull() {
        checkMode(true);
        if (datalen >= capacity) {
            return true;
        }
```

### TrivialIf
`if` statement can be simplified
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/RemoteConErrStats.java`
#### Snippet
```java

    public boolean isExpiredRecord(long currentTIme) {
        if (currentTIme - lastTimeStamp.get() > this.statisticDuration * 10) {
            return true;
        }
```

### TrivialIf
`if` statement can be simplified
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/RemoteConErrStats.java`
#### Snippet
```java
        }
        boolean isForbidden = false;
        if (this.errCounter.incrementAndGet() > this.maxConnAllowedFailCount) {
            isForbidden = true;
        }
```

### TrivialIf
`if` statement can be simplified
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/Message.java`
#### Snippet
```java
        }
        if (this.topic == null) {
            if (other.topic != null) {
                return false;
            }
```

### TrivialIf
`if` statement can be simplified
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/Message.java`
#### Snippet
```java
                return false;
            }
        } else if (!this.topic.equals(other.topic)) {
            return false;
        }
```

### TrivialIf
`if` statement can be simplified
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/cluster/TopicInfo.java`
#### Snippet
```java
            return false;
        }
        if (this.acceptSubscribe != other.acceptSubscribe) {
            return false;
        }
```

### TrivialIf
`if` statement can be simplified
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/cluster/BrokerInfo.java`
#### Snippet
```java
            return false;
        }
        if (this.port != other.port) {
            return false;
        }
```

### TrivialIf
`if` statement can be simplified
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/cluster/Partition.java`
#### Snippet
```java
        }
        if (topic == null) {
            if (other.topic != null) {
                return false;
            }
```

### TrivialIf
`if` statement can be simplified
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/cluster/Partition.java`
#### Snippet
```java
                return false;
            }
        } else if (!topic.equals(other.topic)) {
            return false;
        }
```

### TrivialIf
`if` statement can be simplified
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/policies/FlowCtrlItem.java`
#### Snippet
```java
        }
        final FlowCtrlItem other = (FlowCtrlItem) obj;
        if (this.type != other.type
                || this.startTime != other.startTime
                || this.endTime != other.endTime
```

### TrivialIf
`if` statement can be simplified
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/producer/SimpleMessageProducer.java`
#### Snippet
```java
            return false;
        }
        if (this.producerManager.getTopicPartition(topic) == null) {
            return false;
        }
```

### TrivialIf
`if` statement can be simplified
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/metadata/TopicMetadata.java`
#### Snippet
```java
            return false;
        }
        if (this.minMemCacheSize != other.minMemCacheSize) {
            return false;
        }
```

### TrivialIf
`if` statement can be simplified
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/simplemvc/WebFilter.java`
#### Snippet
```java
    private static boolean isMultipartFormData(HttpServletRequest req) {
        String contentType = req.getContentType();
        if (contentType != null && MimeTypes.Type.MULTIPART_FORM_DATA.is(
                HttpFields.valueParameters(contentType, null))) {
            return true;
```

### TrivialIf
`if` statement can be simplified
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/TMaster.java`
#### Snippet
```java
                DataConverterUtil.convertSubInfo(request.getSubscribeInfoList());
        boolean isNotAllocated = true;
        if (CollectionUtils.isNotEmpty(subscribeList)
                || ((request.hasNotAllocated() && !request.getNotAllocated()))) {
            isNotAllocated = false;
```

### TrivialIf
`if` statement can be simplified
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/utils/ConfStringUtils.java`
#### Snippet
```java
     */
    public static boolean isValidPort(int port) {
        if (port < 0 || port > 65535) {
            return false;
        }
```

## RuleId[id=NonStrictComparisonCanBeEquality]
### NonStrictComparisonCanBeEquality
Can be replaced with equality
in `inlong-audit/audit-sdk/src/main/java/org/apache/inlong/audit/send/SenderGroup.java`
#### Snippet
```java
        SenderChannel channel = null;
        try {
            if (channels.size() <= 0) {
                logger.error("channels is empty");
                dataBuf.release();
```

### NonStrictComparisonCanBeEquality
Can be replaced with equality
in `inlong-audit/audit-sdk/src/main/java/org/apache/inlong/audit/send/SenderManager.java`
#### Snippet
```java
     */
    private void sendData(byte[] data) {
        if (data == null || data.length <= 0) {
            logger.warn("send data is empty!");
            return;
```

### NonStrictComparisonCanBeEquality
Can be replaced with equality
in `inlong-common/src/main/java/org/apache/inlong/common/metric/MetricUtils.java`
#### Snippet
```java
    public static String getDimensionsKey(Map<String, String> dimensionMap) {
        StringBuilder builder = new StringBuilder();
        if (dimensionMap.size() <= 0) {
            return "";
        }
```

### NonStrictComparisonCanBeEquality
Can be replaced with equality
in `inlong-common/src/main/java/org/apache/inlong/common/metric/MetricObserver.java`
#### Snippet
```java
            List<MetricListener> listenerList = parseDomain(domainMap);
            // no listener
            if (listenerList == null || listenerList.size() <= 0) {
                continue;
            }
```

### NonStrictComparisonCanBeEquality
Can be replaced with equality
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/BrokerServiceServer.java`
#### Snippet
```java
        final byte[] msgData = request.getData().toByteArray();
        final int dataLength = msgData.length;
        if (dataLength <= 0) {
            builder.setErrCode(TErrCodeConstants.BAD_REQUEST);
            builder.setErrMsg("data length is zero!");
```

### NonStrictComparisonCanBeEquality
Can be replaced with equality
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/balance/DefaultLoadBalancer.java`
#### Snippet
```java
                    }
                }
            } while (searched >= 2);
            if (searched == 0) {
                return;
```

### NonStrictComparisonCanBeEquality
Can be replaced with equality
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sink/starrocks/StarRocksSqlBuilder.java`
#### Snippet
```java
            result.append("DUPLICATE KEY (").append(StringUtils.join(sortKeyList, ",")).append(") ");
        }
        if (distributeList.size() <= 0 && columns.size() > 0) {
            distributeList.add("`" + columns.get(0).getFieldName() + "` ");
        }
```

### NonStrictComparisonCanBeEquality
Can be replaced with equality
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/http/MessageFilter.java`
#### Snippet
```java
        }
        // check body length
        if (body.length() <= 0) {
            returnRspPackage(resp, req.getCharacterEncoding(),
                    DataProxyErrCode.EMPTY_MSG.getErrCode(),
```

### NonStrictComparisonCanBeEquality
Can be replaced with equality
in `inlong-sort-standalone/sort-standalone-common/src/main/java/org/apache/inlong/sort/standalone/utils/UnescapeHelper.java`
#### Snippet
```java
    public static List<String> toFiledList(String fieldValues, char separator) {
        List<String> fields = new ArrayList<String>();
        if (fieldValues.length() <= 0) {
            return fields;
        }
```

### NonStrictComparisonCanBeEquality
Can be replaced with equality
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/kafka/PartitionerSelector.java`
#### Snippet
```java
            List<PartitionInfo> availablePartitions = cluster.availablePartitionsForTopic(topic);
            // if no available partition, return 0;
            if (availablePartitions == null || availablePartitions.size() <= 0) {
                return prevPartition;
            }
```

## RuleId[id=OptionalUsedAsFieldOrParameterType]
### OptionalUsedAsFieldOrParameterType
`Optional`> used as type for parameter 'customPartitioner'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kafka/src/main/java/org/apache/inlong/sort/kafka/FlinkKafkaProducer.java`
#### Snippet
```java
            KeyedSerializationSchema<IN> serializationSchema,
            Properties producerConfig,
            Optional<FlinkKafkaPartitioner<IN>> customPartitioner,
            boolean multipleSink) {
        this(
```

### OptionalUsedAsFieldOrParameterType
`Optional`> used as type for parameter 'customPartitioner'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kafka/src/main/java/org/apache/inlong/sort/kafka/FlinkKafkaProducer.java`
#### Snippet
```java
            SerializationSchema<IN> serializationSchema,
            Properties producerConfig,
            Optional<FlinkKafkaPartitioner<IN>> customPartitioner,
            boolean multipleSink) {
        this(
```

### OptionalUsedAsFieldOrParameterType
`Optional`> used as type for parameter 'customPartitioner'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kafka/src/main/java/org/apache/inlong/sort/kafka/FlinkKafkaProducer.java`
#### Snippet
```java
            KeyedSerializationSchema<IN> serializationSchema,
            Properties producerConfig,
            Optional<FlinkKafkaPartitioner<IN>> customPartitioner,
            FlinkKafkaProducer.Semantic semantic,
            int kafkaProducersPoolSize,
```

### OptionalUsedAsFieldOrParameterType
`Optional`>> used as type for parameter 'keyEncodingFormat'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/pulsar/src/main/java/org/apache/inlong/sort/pulsar/table/PulsarDynamicTableFactory.java`
#### Snippet
```java
    private PulsarDynamicTableSink createPulsarTableSink(ReadableConfig tableOptions, List<String> topics,
            String adminUrl, String serverUrl,
            Optional<EncodingFormat<SerializationSchema<RowData>>> keyEncodingFormat,
            EncodingFormat<SerializationSchema<RowData>> valueEncodingFormat,
            Properties properties, DataType physicalDataType,
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'proctimeAttribute'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/tubemq/src/main/java/org/apache/inlong/sort/tubemq/table/TubeMQTableSource.java`
#### Snippet
```java
     * field is defined.
     */
    private final Optional<String> proctimeAttribute;
    /**
     * status of error
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'proctimeAttribute'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/tubemq/src/main/java/org/apache/inlong/sort/tubemq/table/TubeMQTableSource.java`
#### Snippet
```java
            TreeSet<String> tidSet, String consumerGroup, String sessionKey,
            Configuration configuration, @Nullable WatermarkStrategy<RowData> watermarkStrategy,
            Optional<String> proctimeAttribute, Boolean ignoreErrors, Boolean innerFormat) {

        Preconditions.checkNotNull(physicalDataType, "Physical data type must not be null.");
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'condition'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/mysql/source/utils/StatementUtils.java`
#### Snippet
```java
            int limit,
            String projection,
            Optional<String> condition,
            Optional<String> orderBy) {
        final StringBuilder sql = new StringBuilder("SELECT ");
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'orderBy'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/mysql/source/utils/StatementUtils.java`
#### Snippet
```java
            String projection,
            Optional<String> condition,
            Optional<String> orderBy) {
        final StringBuilder sql = new StringBuilder("SELECT ");
        sql.append(projection).append(" FROM ");
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'condition'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/mysql/source/utils/StatementUtils.java`
#### Snippet
```java
            String projection,
            String maxColumnProjection,
            Optional<String> condition,
            String orderBy) {
        final StringBuilder sql = new StringBuilder("SELECT ");
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'converterOptional'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/oracle-cdc/src/main/java/org/apache/inlong/sort/cdc/oracle/table/OracleDeserializationConverterFactory.java`
#### Snippet
```java

    private static Optional<DeserializationRuntimeConverter> wrapNumericConverter(
            Optional<DeserializationRuntimeConverter> converterOptional) {
        return converterOptional.map(
                converter -> new DeserializationRuntimeConverter() {
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'condition'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/oracle-cdc/src/main/java/org/apache/inlong/sort/cdc/oracle/source/utils/OracleUtils.java`
#### Snippet
```java
            String projection,
            String maxColumnProjection,
            Optional<String> condition,
            String orderBy) {
        final StringBuilder sql = new StringBuilder("SELECT ");
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'condition'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/oracle-cdc/src/main/java/org/apache/inlong/sort/cdc/oracle/source/utils/OracleUtils.java`
#### Snippet
```java
            int limit,
            String projection,
            Optional<String> condition,
            Optional<String> orderBy) {
        final StringBuilder sql = new StringBuilder("SELECT ");
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'orderBy'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/oracle-cdc/src/main/java/org/apache/inlong/sort/cdc/oracle/source/utils/OracleUtils.java`
#### Snippet
```java
            String projection,
            Optional<String> condition,
            Optional<String> orderBy) {
        final StringBuilder sql = new StringBuilder("SELECT ");
        sql.append(projection).append(" FROM ");
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'converterOptional'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/postgres-cdc/src/main/java/org/apache/inlong/sort/cdc/postgres/table/PostgreSQLDeserializationConverterFactory.java`
#### Snippet
```java

    private static Optional<DeserializationRuntimeConverter> wrapNumericConverter(
            Optional<DeserializationRuntimeConverter> converterOptional) {
        return converterOptional.map(
                converter -> new DeserializationRuntimeConverter() {
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'proctimeAttribute'
in `inlong-tubemq/tubemq-connectors/tubemq-connector-flink/src/main/java/org/apache/flink/connectors/tubemq/TubemqTableSource.java`
#### Snippet
```java

    private Optional<String> validateProcTimeAttribute(
            Optional<String> proctimeAttribute) {
        return proctimeAttribute.map((attribute) -> {
            Optional<TypeInformation<?>> tpe = schema.getFieldType(attribute);
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'proctimeAttribute'
in `inlong-tubemq/tubemq-connectors/tubemq-connector-flink/src/main/java/org/apache/flink/connectors/tubemq/TubemqTableSource.java`
#### Snippet
```java
            DeserializationSchema<Row> deserializationSchema,
            TableSchema schema,
            Optional<String> proctimeAttribute,
            List<RowtimeAttributeDescriptor> rowtimeAttributeDescriptors,
            Map<String, String> fieldMapping,
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'proctimeAttribute'
in `inlong-tubemq/tubemq-connectors/tubemq-connector-flink/src/main/java/org/apache/flink/connectors/tubemq/TubemqTableSource.java`
#### Snippet
```java
     * field is defined.
     */
    private final Optional<String> proctimeAttribute;

    /**
```

## RuleId[id=CharsetObjectCanBeUsed]
### CharsetObjectCanBeUsed
StandardCharsets.UTF_8 can be used instead
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/example/SendMsgThread.java`
#### Snippet
```java

                long startTime = System.currentTimeMillis();
                SendResult result = messageSender.sendMessage("hhhh".getBytes("utf8"),
                        "b_test", "n_test1", 0, String.valueOf(System.currentTimeMillis()), 1,
                        TimeUnit.MILLISECONDS);
```

### CharsetObjectCanBeUsed
StandardCharsets.UTF_8 can be used instead
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/example/TcpClientExample.java`
#### Snippet
```java
        SendResult result = null;
        try {
            result = sender.sendMessage(messageBody.getBytes("utf8"), inlongGroupId, inlongStreamId,
                    0, String.valueOf(dt), 20, TimeUnit.SECONDS);
        } catch (UnsupportedEncodingException e) {
```

### CharsetObjectCanBeUsed
StandardCharsets.UTF_8 can be used instead
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/codec/ProtocolEncoder.java`
#### Snippet
```java
                msgType |= FLAG_ALLOW_AUTH;
            }
            int totalLength = 1 + 4 + 1 + 4 + 2 + endAttr.getBytes("utf8").length + 2;
            buf = ByteBufAllocator.DEFAULT.buffer(4 + totalLength);
            buf.writeInt(totalLength);
```

### CharsetObjectCanBeUsed
StandardCharsets.UTF_8 can be used instead
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/codec/ProtocolEncoder.java`
#### Snippet
```java
            buf.writeByte(1);
            buf.writeInt(0);
            buf.writeShort(endAttr.getBytes("utf8").length);
            if (endAttr.getBytes("utf8").length > 0) {
                buf.writeBytes(endAttr.getBytes("utf8"));
```

### CharsetObjectCanBeUsed
StandardCharsets.UTF_8 can be used instead
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/codec/ProtocolEncoder.java`
#### Snippet
```java
            buf.writeInt(0);
            buf.writeShort(endAttr.getBytes("utf8").length);
            if (endAttr.getBytes("utf8").length > 0) {
                buf.writeBytes(endAttr.getBytes("utf8"));
            }
```

### CharsetObjectCanBeUsed
StandardCharsets.UTF_8 can be used instead
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/codec/ProtocolEncoder.java`
#### Snippet
```java
            buf.writeShort(endAttr.getBytes("utf8").length);
            if (endAttr.getBytes("utf8").length > 0) {
                buf.writeBytes(endAttr.getBytes("utf8"));
            }
            buf.writeShort(0xee01);
```

### CharsetObjectCanBeUsed
StandardCharsets.UTF_8 can be used instead
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/codec/ProtocolEncoder.java`
#### Snippet
```java
                msgType |= FLAG_ALLOW_COMPRESS;
            }
            totalLength = totalLength + body.length + endAttr.getBytes("utf8").length;
            buf = ByteBufAllocator.DEFAULT.buffer(4 + totalLength);
            buf.writeInt(totalLength);
```

### CharsetObjectCanBeUsed
StandardCharsets.UTF_8 can be used instead
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/codec/ProtocolEncoder.java`
#### Snippet
```java
            buf.writeBytes(body);

            buf.writeShort(endAttr.getBytes("utf8").length);
            buf.writeBytes(endAttr.getBytes("utf8"));
            buf.writeShort(0xee01);
```

### CharsetObjectCanBeUsed
StandardCharsets.UTF_8 can be used instead
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/codec/ProtocolEncoder.java`
#### Snippet
```java

            buf.writeShort(endAttr.getBytes("utf8").length);
            buf.writeBytes(endAttr.getBytes("utf8"));
            buf.writeShort(0xee01);
        }
```

### CharsetObjectCanBeUsed
StandardCharsets.UTF_8 can be used instead
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/codec/ProtocolEncoder.java`
#### Snippet
```java
                    byte[] entry = iter.next();
                    out.write(entry);
                    out.write("\n".getBytes("utf8"));
                }
                body = out.toByteArray();
```

### CharsetObjectCanBeUsed
StandardCharsets.UTF_8 can be used instead
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/codec/ProtocolEncoder.java`
#### Snippet
```java
                    msgType |= FLAG_ALLOW_ENCRYPT;
                }
                totalLength = totalLength + body.length + msgAttrs.getBytes("utf8").length;
                buf = ByteBufAllocator.DEFAULT.buffer(4 + totalLength);
                buf.writeInt(totalLength);
```

### CharsetObjectCanBeUsed
StandardCharsets.UTF_8 can be used instead
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/codec/ProtocolEncoder.java`
#### Snippet
```java
                buf.writeInt(body.length);
                buf.writeBytes(body);
                buf.writeInt(msgAttrs.getBytes("utf8").length);
                buf.writeBytes(msgAttrs.getBytes("utf8"));
            }
```

### CharsetObjectCanBeUsed
StandardCharsets.UTF_8 can be used instead
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/codec/ProtocolEncoder.java`
#### Snippet
```java
                buf.writeBytes(body);
                buf.writeInt(msgAttrs.getBytes("utf8").length);
                buf.writeBytes(msgAttrs.getBytes("utf8"));
            }
        } catch (Exception e) {
```

### CharsetObjectCanBeUsed
StandardCharsets.UTF_8 can be used instead
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/codec/ProtocolEncoder.java`
#### Snippet
```java
                    msgType |= FLAG_ALLOW_ENCRYPT;
                }
                totalLength = totalLength + body.length + msgAttrs.getBytes("utf8").length;
                buf = ByteBufAllocator.DEFAULT.buffer(4 + totalLength);
                buf.writeInt(totalLength);
```

### CharsetObjectCanBeUsed
StandardCharsets.UTF_8 can be used instead
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/codec/ProtocolEncoder.java`
#### Snippet
```java
                buf.writeInt(body.length);
                buf.writeBytes(body);
                buf.writeInt(msgAttrs.getBytes("utf8").length);
                buf.writeBytes(msgAttrs.getBytes("utf8"));
            }
```

### CharsetObjectCanBeUsed
StandardCharsets.UTF_8 can be used instead
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/codec/ProtocolEncoder.java`
#### Snippet
```java
                buf.writeBytes(body);
                buf.writeInt(msgAttrs.getBytes("utf8").length);
                buf.writeBytes(msgAttrs.getBytes("utf8"));
            }
        } catch (Exception e) {
```

### CharsetObjectCanBeUsed
StandardCharsets.UTF_8 can be used instead
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/codec/ProtocolEncoder.java`
#### Snippet
```java
                    for (int i = 0; i < len - 1; i++) {
                        data.write(object.getBodylist().get(i));
                        data.write("\n".getBytes("utf8"));
                    }
                    data.write(object.getBodylist().get(len - 1));
```

### CharsetObjectCanBeUsed
StandardCharsets.UTF_8 can be used instead
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/example/UdpClientExample.java`
#### Snippet
```java
                    for (int i = 0; i < len - 1; i++) {
                        data.write(object.getBodylist().get(i));
                        data.write("\n".getBytes("utf8"));
                    }
                    data.write(object.getBodylist().get(len - 1));
```

### CharsetObjectCanBeUsed
StandardCharsets.UTF_8 can be used instead
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/example/UdpClientExample.java`
#### Snippet
```java
                }
                totalLength = totalLength + body.length
                        + endAttr.getBytes("utf8").length;
                buf = ByteBufAllocator.DEFAULT.buffer(4 + totalLength);
                buf.writeInt(totalLength);
```

### CharsetObjectCanBeUsed
StandardCharsets.UTF_8 can be used instead
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/example/UdpClientExample.java`
#### Snippet
```java
                buf.writeBytes(body);

                buf.writeShort(endAttr.getBytes("utf8").length);
                buf.writeBytes(endAttr.getBytes("utf8"));
                buf.writeShort(0xee01);
```

### CharsetObjectCanBeUsed
StandardCharsets.UTF_8 can be used instead
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/example/UdpClientExample.java`
#### Snippet
```java

                buf.writeShort(endAttr.getBytes("utf8").length);
                buf.writeBytes(endAttr.getBytes("utf8"));
                buf.writeShort(0xee01);
            }
```

### CharsetObjectCanBeUsed
StandardCharsets.UTF_8 can be used instead
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/example/UdpClientExample.java`
#### Snippet
```java
            String attr) throws UnsupportedEncodingException {
        EncodeObject encodeObject =
                new EncodeObject(getRandomString(5).getBytes("UTF-8"), msgType,
                        isCompress,
                        isReport, isGroupIdTransfer, dt, seqId, groupId, streamId, attr);
```

### CharsetObjectCanBeUsed
StandardCharsets.UTF_8 can be used instead
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/config/ProxyConfigManager.java`
#### Snippet
```java
                    return null;
                }
                localManagerIps = new String(serialized, "UTF-8");
            } else {
                if (!localManagerIpsFile.getParentFile().exists()) {
```

### CharsetObjectCanBeUsed
StandardCharsets.UTF_8 can be used instead
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/mysql/debezium/task/MySqlSnapshotSplitReadTask.java`
#### Snippet
```java
        try {
            return MySqlValueConverters.containsZeroValuesInDatePart(
                    (new String(b.getBytes(1, (int) (b.length())), "UTF-8")), column, table)
                            ? null
                            : rs.getTimestamp(fieldNo, Calendar.getInstance());
```

### CharsetObjectCanBeUsed
StandardCharsets.UTF_8 can be used instead
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/mysql/debezium/task/MySqlSnapshotSplitReadTask.java`
#### Snippet
```java
        try {
            return MySqlValueConverters.stringToLocalDate(
                    new String(b.getBytes(1, (int) (b.length())), "UTF-8"), column, table);
        } catch (UnsupportedEncodingException e) {
            LOG.error("Could not read MySQL TIME value as UTF-8");
```

### CharsetObjectCanBeUsed
StandardCharsets.UTF_8 can be used instead
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/mysql/debezium/task/MySqlSnapshotSplitReadTask.java`
#### Snippet
```java
        try {
            return MySqlValueConverters.stringToDuration(
                    new String(b.getBytes(1, (int) (b.length())), "UTF-8"));
        } catch (UnsupportedEncodingException e) {
            LOG.error("Could not read MySQL TIME value as UTF-8");
```

### CharsetObjectCanBeUsed
StandardCharsets.UTF_8 can be used instead
in `inlong-agent/agent-common/src/main/java/org/apache/inlong/agent/utils/HttpManager.java`
#### Snippet
```java
            HttpPost post = getHttpPost(url);
            post.addHeader(BasicAuth.BASIC_AUTH_HEADER, BasicAuth.genBasicAuthCredential(secretId, secretKey));
            StringEntity stringEntity = new StringEntity(toJsonStr(dto), Charset.forName("UTF-8"));
            stringEntity.setContentType(AGENT_HTTP_APPLICATION_JSON);
            post.setEntity(stringEntity);
```

## RuleId[id=ReplaceAllDot]
### ReplaceAllDot
File.separator is used as a regex; will not work on Windows
in `inlong-agent/agent-common/src/main/java/org/apache/inlong/agent/utils/PathUtils.java`
#### Snippet
```java
    public static boolean antPathIncluded(String dirStr, String patternStr) {
        // todo:Determines whether a path is a possible prefix of the path expression
        List<String> dirArr = Stream.of(dirStr.split(File.separator)).collect(Collectors.toList());
        List<String> patternDirArr = Stream.of(patternStr.split(File.separator)).collect(Collectors.toList());
        int everything = patternDirArr.indexOf("**");
```

### ReplaceAllDot
File.separator is used as a regex; will not work on Windows
in `inlong-agent/agent-common/src/main/java/org/apache/inlong/agent/utils/PathUtils.java`
#### Snippet
```java
        // todo:Determines whether a path is a possible prefix of the path expression
        List<String> dirArr = Stream.of(dirStr.split(File.separator)).collect(Collectors.toList());
        List<String> patternDirArr = Stream.of(patternStr.split(File.separator)).collect(Collectors.toList());
        int everything = patternDirArr.indexOf("**");
        if (everything != -1) {
```

## RuleId[id=ImplicitArrayToString]
### ImplicitArrayToString
Implicit call to 'toString()' on array returned by call to `data.getDataRows()`
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/starrocks/src/main/java/org/apache/inlong/sort/starrocks/table/sink/StarRocksDynamicSinkFunction.java`
#### Snippet
```java
                        || null == data.getDataRows()) {
                    LOG.warn(String.format("json row data not fullfilled. {database: %s, table: %s, dataRows: %s}",
                            data.getDatabase(), data.getTable(), data.getDataRows()));
                    return;
                }
```

## RuleId[id=AutoCloseableResource]
### AutoCloseableResource
'PulsarClient' used without 'try'-with-resources statement
in `inlong-sdk/sort-sdk/src/main/java/org/apache/inlong/sdk/sort/fetcher/pulsar/PulsarTopicFetcherBuilderImpl.java`
#### Snippet
```java
                .orElseThrow(() -> new IllegalStateException("context is null"));
        Optional.ofNullable(pulsarClient)
                .orElseThrow(() -> new IllegalStateException("pulsar client is null"));
        deserializer = Optional.ofNullable(deserializer).orElse(new MessageDeserializer());
        if (CollectionUtils.isNotEmpty(topics)) {
```

### AutoCloseableResource
'DefaultHttpClient' used without 'try'-with-resources statement
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/utils/ServiceDiscoveryUtils.java`
#### Snippet
```java
        HttpConnectionParams.setSoTimeout(myParams, proxyClientConfig.getManagerSocketTimeout());
        if (proxyClientConfig.isLocalVisit()) {
            httpClient = new DefaultHttpClient(myParams);
        } else {
            try {
```

### AutoCloseableResource
'CloseableHttpClient' used without 'try'-with-resources statement
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/utils/ServiceDiscoveryUtils.java`
#### Snippet
```java
                        SSLConnectionSocketFactory.getDefaultHostnameVerifier());
                httpClient = HttpClients.custom().setDefaultHeaders(headers)
                        .setDefaultRequestConfig(requestConfig).setSSLSocketFactory(sslsf).build();
            } catch (Throwable t) {
                log.error("Create Https client failed: ", t);
```

### AutoCloseableResource
'RowDataDeltaWriter' used without 'try'-with-resources statement
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/iceberg/src/main/java/org/apache/inlong/sort/iceberg/sink/BaseDeltaTaskWriter.java`
#### Snippet
```java
    @Override
    public void write(RowData row) throws IOException {
        RowDataDeltaWriter writer = route(row);

        switch (row.getRowKind()) {
```

### AutoCloseableResource
'RowDataDeltaWriter' used without 'try'-with-resources statement
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/iceberg/src/main/java/org/apache/inlong/sort/iceberg/sink/trick/BaseDeltaTaskWriter.java`
#### Snippet
```java
    @Override
    public void write(RowData row) throws IOException {
        BaseDeltaTaskWriter.RowDataDeltaWriter writer = route(row);

        switch (row.getRowKind()) {
```

### AutoCloseableResource
'FileIO' used without 'try'-with-resources statement
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/iceberg/src/main/java/org/apache/inlong/sort/iceberg/sink/multiple/IcebergSingleFileCommiter.java`
#### Snippet
```java
        for (ManifestFile manifest : manifests) {
            try {
                table.io().deleteFile(manifest.path());
            } catch (Exception e) {
                // The flink manifests cleaning failure shouldn't abort the completed checkpoint.
```

### AutoCloseableResource
'ColumnFamilyHandle' used without 'try'-with-resources statement
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/iceberg/src/main/java/org/apache/inlong/sort/iceberg/sink/collections/RocksDBDAO.java`
#### Snippet
```java
                throw new RuntimeException(e);
            }
            managedHandlesMap.remove(columnFamilyName);
            return null;
        });
```

### AutoCloseableResource
'ServerConnector' used without 'try'-with-resources statement
in `inlong-agent/agent-core/src/main/java/org/apache/inlong/agent/core/conf/ConfigJetty.java`
#### Snippet
```java
    private void initJetty() throws Exception {
        // not using resource try to avoid AutoClosable's close() on the given stream
        ServerConnector connector = new ServerConnector(this.server);
        connector.setPort(conf.getInt(
                AgentConstants.AGENT_HTTP_PORT, AgentConstants.DEFAULT_AGENT_HTTP_PORT));
```

### AutoCloseableResource
'Options' used without 'try'-with-resources statement
in `inlong-agent/agent-common/src/main/java/org/apache/inlong/agent/db/RocksDbImp.java`
#### Snippet
```java
        storePath = finalPath.getAbsolutePath();
        RocksDB.loadLibrary();
        final Options options = new Options();
        options.setCreateIfMissing(true);
        try {
```

### AutoCloseableResource
'KafkaConsumer' used without 'try'-with-resources statement
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/sources/KafkaSource.java`
#### Snippet
```java
            props.put(JOB_KAFKA_AUTO_RESETE, map.get(JOB_KAFKA_AUTO_COMMIT_OFFSET_RESET));
        }
        KafkaConsumer<String, String> consumer = new KafkaConsumer<>(props);
        List<PartitionInfo> partitionInfoList = consumer.partitionsFor(conf.get(JOB_KAFKA_TOPIC));
        String allPartitionOffsets = map.get(JOB_KAFKA_OFFSET);
```

### AutoCloseableResource
'Stream' used without 'try'-with-resources statement
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/trigger/DirectoryTrigger.java`
#### Snippet
```java
                        WatchKey watchKey = path.register(watchService, StandardWatchEventKinds.ENTRY_CREATE);
                        tobeAddedWatchers.add(watchKey);
                        Files.list(path).forEach(subPath -> registerAllSubDir(trigger, subPath.toAbsolutePath(),
                                tobeAddedWatchers, registerSubFile));
                    } else if (registerSubFile) {
```

### AutoCloseableResource
'Stream' used without 'try'-with-resources statement
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/sources/reader/file/FileReaderOperator.java`
#### Snippet
```java
            if (!jobConf.get(JOB_FILE_MONITOR_STATUS, JOB_FILE_MONITOR_DEFAULT_STATUS)
                    .equals(JOB_FILE_MONITOR_DEFAULT_STATUS)) {
                readEndpoint = Files.lines(file.toPath()).count();
            }

```

### AutoCloseableResource
'Producer' used without 'try'-with-resources statement
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/PulsarSink.java`
#### Snippet
```java
        long startTime = System.currentTimeMillis();
        if (topic != null) {
            pulsarClientService.initTopicProducer(topic);
        }
        logger.info(getName() + " initTopic cost: "
```

### AutoCloseableResource
'DataInputBuffer' used without 'try'-with-resources statement
in `inlong-common/src/main/java/org/apache/inlong/common/msg/InLongMsg.java`
#### Snippet
```java

        try {
            final DataInputBuffer input = new DataInputBuffer();
            byte[] array = rawdata.array();
            int pos = rawdata.position();
```

### AutoCloseableResource
'DataInputBuffer' used without 'try'-with-resources statement
in `inlong-common/src/main/java/org/apache/inlong/common/msg/InLongMsg.java`
#### Snippet
```java
    public static Iterator<byte[]> getIterator(ByteBuffer rawdata) {
        try {
            final DataInputBuffer input = new DataInputBuffer();
            byte[] array = rawdata.array();
            int pos = rawdata.position();
```

### AutoCloseableResource
'Cursor' used without 'try'-with-resources statement
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/core/impl/SortConfigLoaderImpl.java`
#### Snippet
```java
    @Override
    public List<SortTaskInfo> loadAllTask() {
        Cursor<SortTaskInfo> cursor = streamSinkEntityMapper.selectAllTasks();
        List<SortTaskInfo> allTasks = new ArrayList<>();
        cursor.forEach(allTasks::add);
```

### AutoCloseableResource
'Cursor' used without 'try'-with-resources statement
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/core/impl/SortConfigLoaderImpl.java`
#### Snippet
```java
    @Override
    public List<SortSourceClusterInfo> loadAllClusters() {
        Cursor<SortSourceClusterInfo> cursor = clusterEntityMapper.selectAllClusters();
        List<SortSourceClusterInfo> allClusters = new ArrayList<>();
        cursor.forEach(allClusters::add);
```

### AutoCloseableResource
'Cursor' used without 'try'-with-resources statement
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/core/impl/SortConfigLoaderImpl.java`
#### Snippet
```java
    @Override
    public List<SortFieldInfo> loadAllFields() {
        Cursor<SortFieldInfo> cursor = streamSinkFieldEntityMapper.selectAllFields();
        List<SortFieldInfo> allFields = new ArrayList<>();
        cursor.forEach(allFields::add);
```

### AutoCloseableResource
'Cursor' used without 'try'-with-resources statement
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/core/impl/SortConfigLoaderImpl.java`
#### Snippet
```java
    @Override
    public List<InlongGroupExtEntity> loadGroupBackupInfo(String keyName) {
        Cursor<InlongGroupExtEntity> cursor = inlongGroupExtEntityMapper.selectByKeyName(keyName);
        List<InlongGroupExtEntity> groupBackupInfo = new ArrayList<>();
        cursor.forEach(groupBackupInfo::add);
```

### AutoCloseableResource
'Cursor' used without 'try'-with-resources statement
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/core/impl/SortConfigLoaderImpl.java`
#### Snippet
```java
    @Override
    public List<StreamSinkEntity> loadAllStreamSinkEntity() {
        Cursor<StreamSinkEntity> cursor = streamSinkEntityMapper.selectAllStreamSinks();
        List<StreamSinkEntity> allStreamSinks = new ArrayList<>();
        cursor.forEach(allStreamSinks::add);
```

### AutoCloseableResource
'Cursor' used without 'try'-with-resources statement
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/core/impl/SortConfigLoaderImpl.java`
#### Snippet
```java
    @Override
    public List<SortSourceStreamSinkInfo> loadAllStreamSinks() {
        Cursor<SortSourceStreamSinkInfo> cursor = streamSinkEntityMapper.selectAllStreams();
        List<SortSourceStreamSinkInfo> allStreamSinks = new ArrayList<>();
        cursor.forEach(allStreamSinks::add);
```

### AutoCloseableResource
'Cursor' used without 'try'-with-resources statement
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/core/impl/SortConfigLoaderImpl.java`
#### Snippet
```java
    @Override
    public List<DataNodeEntity> loadAllDataNodeEntity() {
        Cursor<DataNodeEntity> cursor = dataNodeEntityMapper.selectAllDataNodes();
        List<DataNodeEntity> allDataNodes = new ArrayList<>();
        cursor.forEach(allDataNodes::add);
```

### AutoCloseableResource
'Cursor' used without 'try'-with-resources statement
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/core/impl/SortConfigLoaderImpl.java`
#### Snippet
```java
    @Override
    public List<SortSourceStreamInfo> loadAllStreams() {
        Cursor<SortSourceStreamInfo> cursor = inlongStreamEntityMapper.selectAllStreams();
        List<SortSourceStreamInfo> allStreams = new ArrayList<>();
        cursor.forEach(allStreams::add);
```

### AutoCloseableResource
'Cursor' used without 'try'-with-resources statement
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/core/impl/SortConfigLoaderImpl.java`
#### Snippet
```java
    @Override
    public List<InlongStreamExtEntity> loadStreamBackupInfo(String keyName) {
        Cursor<InlongStreamExtEntity> cursor = inlongStreamExtEntityMapper.selectByKeyName(keyName);
        List<InlongStreamExtEntity> streamBackupInfo = new ArrayList<>();
        cursor.forEach(streamBackupInfo::add);
```

### AutoCloseableResource
'Cursor' used without 'try'-with-resources statement
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/core/impl/SortConfigLoaderImpl.java`
#### Snippet
```java
    @Override
    public List<SortSourceGroupInfo> loadAllGroup() {
        Cursor<SortSourceGroupInfo> cursor = inlongGroupEntityMapper.selectAllGroups();
        List<SortSourceGroupInfo> allGroups = new ArrayList<>();
        cursor.forEach(allGroups::add);
```

### AutoCloseableResource
'Page' used without 'try'-with-resources statement
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/core/impl/WorkflowEventServiceImpl.java`
#### Snippet
```java
    @Override
    public PageResult<EventLogResponse> list(EventLogRequest query) {
        PageHelper.startPage(query.getPageNum(), query.getPageSize());
        Page<WorkflowEventLogEntity> page = (Page<WorkflowEventLogEntity>) queryService.listEventLog(query);

```

### AutoCloseableResource
'Page' used without 'try'-with-resources statement
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/core/impl/WorkflowApproverServiceImpl.java`
#### Snippet
```java
    @Override
    public PageResult<ApproverResponse> listByCondition(ApproverPageRequest request) {
        PageHelper.startPage(request.getPageNum(), request.getPageSize());

        Page<WorkflowApproverEntity> page = (Page<WorkflowApproverEntity>) approverMapper.selectByCondition(request);
```

### AutoCloseableResource
'Page' used without 'try'-with-resources statement
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/node/DataNodeServiceImpl.java`
#### Snippet
```java
    @Override
    public PageResult<DataNodeInfo> list(DataNodePageRequest request) {
        PageHelper.startPage(request.getPageNum(), request.getPageSize());
        Page<DataNodeEntity> entityPage = (Page<DataNodeEntity>) dataNodeMapper.selectByCondition(request);
        List<DataNodeInfo> list = entityPage.stream()
```

### AutoCloseableResource
'Page' used without 'try'-with-resources statement
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/sink/StreamSinkServiceImpl.java`
#### Snippet
```java
        UserInfo userInfo = userService.getByName(operator);
        boolean isAdmin = UserTypeEnum.ADMIN.getCode().equals(userInfo.getAccountType());
        PageHelper.startPage(request.getPageNum(), request.getPageSize());
        OrderFieldEnum.checkOrderField(request);
        OrderTypeEnum.checkOrderType(request);
```

### AutoCloseableResource
'Page' used without 'try'-with-resources statement
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/sink/StreamSinkServiceImpl.java`
#### Snippet
```java
                }
            }
            sinkMap.computeIfAbsent(streamSink.getSinkType(), k -> new Page<>()).add(streamSink);
        }
        List<StreamSink> responseList = Lists.newArrayList();
```

### AutoCloseableResource
'Page' used without 'try'-with-resources statement
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/sink/StreamSinkServiceImpl.java`
#### Snippet
```java
        Map<String, Page<StreamSinkEntity>> sinkMap = Maps.newHashMap();
        for (StreamSinkEntity streamSink : sinkEntityList) {
            sinkMap.computeIfAbsent(streamSink.getSinkType(), k -> new Page<>()).add(streamSink);
        }
        List<StreamSink> filterResult = Lists.newArrayList();
```

### AutoCloseableResource
'Page' used without 'try'-with-resources statement
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/user/UserServiceImpl.java`
#### Snippet
```java
    @Override
    public PageResult<UserInfo> list(UserRequest request) {
        PageHelper.startPage(request.getPageNum(), request.getPageSize());
        Page<UserEntity> entityPage = (Page<UserEntity>) userMapper.selectByCondition(request);
        List<UserInfo> userList = CommonBeanUtils.copyListProperties(entityPage, UserInfo::new);
```

### AutoCloseableResource
'Page' used without 'try'-with-resources statement
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/group/InlongGroupServiceImpl.java`
#### Snippet
```java
            request.setPageSize(MAX_PAGE_SIZE);
        }
        PageHelper.startPage(request.getPageNum(), request.getPageSize());
        OrderFieldEnum.checkOrderField(request);
        OrderTypeEnum.checkOrderType(request);
```

### AutoCloseableResource
'Page' used without 'try'-with-resources statement
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/source/StreamSourceServiceImpl.java`
#### Snippet
```java
        Preconditions.expectNotBlank(request.getInlongGroupId(), ErrorCodeEnum.GROUP_ID_IS_EMPTY);

        PageHelper.startPage(request.getPageNum(), request.getPageSize());
        OrderFieldEnum.checkOrderField(request);
        OrderTypeEnum.checkOrderType(request);
```

### AutoCloseableResource
'Page' used without 'try'-with-resources statement
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/source/StreamSourceServiceImpl.java`
#### Snippet
```java
        Map<String, Page<StreamSourceEntity>> sourceMap = Maps.newHashMap();
        for (StreamSourceEntity entity : entityList) {
            sourceMap.computeIfAbsent(entity.getSourceType(), k -> new Page<>()).add(entity);
        }
        List<StreamSource> responseList = Lists.newArrayList();
```

### AutoCloseableResource
'Page' used without 'try'-with-resources statement
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/source/StreamSourceServiceImpl.java`
#### Snippet
```java
            throw new BusinessException(ErrorCodeEnum.GROUP_ID_IS_EMPTY);
        }
        PageHelper.startPage(request.getPageNum(), request.getPageSize());
        OrderFieldEnum.checkOrderField(request);
        OrderTypeEnum.checkOrderType(request);
```

### AutoCloseableResource
'Page' used without 'try'-with-resources statement
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/source/StreamSourceServiceImpl.java`
#### Snippet
```java
        Map<String, Page<StreamSourceEntity>> sourceMap = Maps.newHashMap();
        for (StreamSourceEntity entity : filteredEntitys) {
            sourceMap.computeIfAbsent(entity.getSourceType(), k -> new Page<>()).add(entity);
        }
        List<StreamSource> responseList = Lists.newArrayList();
```

### AutoCloseableResource
'Page' used without 'try'-with-resources statement
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/consume/InlongConsumeServiceImpl.java`
#### Snippet
```java
            request.setPageSize(MAX_PAGE_SIZE);
        }
        PageHelper.startPage(request.getPageNum(), request.getPageSize());
        OrderFieldEnum.checkOrderField(request);
        OrderTypeEnum.checkOrderType(request);
```

### AutoCloseableResource
'Page' used without 'try'-with-resources statement
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/stream/InlongStreamServiceImpl.java`
#### Snippet
```java
        // the person in charge of the inlong group has the authority of all inlong streams,
        // so do not filter by in charge person
        PageHelper.startPage(request.getPageNum(), request.getPageSize());
        OrderFieldEnum.checkOrderField(request);
        OrderTypeEnum.checkOrderType(request);
```

### AutoCloseableResource
'Page' used without 'try'-with-resources statement
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/stream/InlongStreamServiceImpl.java`
#### Snippet
```java
        LOGGER.debug("begin to list inlong stream page by {}", request);

        PageHelper.startPage(request.getPageNum(), request.getPageSize());
        OrderFieldEnum.checkOrderField(request);
        OrderTypeEnum.checkOrderType(request);
```

### AutoCloseableResource
'Page' used without 'try'-with-resources statement
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/cluster/InlongClusterServiceImpl.java`
#### Snippet
```java
    @Override
    public PageResult<ClusterInfo> list(ClusterPageRequest request) {
        PageHelper.startPage(request.getPageNum(), request.getPageSize());
        Page<InlongClusterEntity> entityPage = (Page<InlongClusterEntity>) clusterMapper.selectByCondition(request);
        List<ClusterInfo> list = entityPage.stream()
```

### AutoCloseableResource
'Page' used without 'try'-with-resources statement
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/cluster/InlongClusterServiceImpl.java`
#### Snippet
```java
        String message = "Current user does not have permission to get cluster node list";
        userService.checkUser(cluster.getInCharges(), currentUser, message);
        PageHelper.startPage(request.getPageNum(), request.getPageSize());
        Page<InlongClusterNodeEntity> entityPage =
                (Page<InlongClusterNodeEntity>) clusterNodeMapper.selectByCondition(request);
```

### AutoCloseableResource
'Page' used without 'try'-with-resources statement
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/cluster/InlongClusterServiceImpl.java`
#### Snippet
```java
    @Override
    public PageResult<ClusterTagResponse> listTag(ClusterTagPageRequest request) {
        PageHelper.startPage(request.getPageNum(), request.getPageSize());
        Page<InlongClusterTagEntity> entityPage = (Page<InlongClusterTagEntity>) clusterTagMapper
                .selectByCondition(request);
```

### AutoCloseableResource
'Page' used without 'try'-with-resources statement
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/heartbeat/HeartbeatServiceImpl.java`
#### Snippet
```java

    private PageResult<StreamHeartbeatResponse> listStreamHeartbeatOpt(HeartbeatPageRequest request) {
        PageHelper.startPage(request.getPageNum(), request.getPageSize());
        Page<StreamHeartbeatEntity> entityPage =
                (Page<StreamHeartbeatEntity>) streamHeartbeatMapper.selectByCondition(request);
```

### AutoCloseableResource
'Page' used without 'try'-with-resources statement
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/heartbeat/HeartbeatServiceImpl.java`
#### Snippet
```java

    private PageResult<ComponentHeartbeatResponse> listComponentHeartbeatOpt(HeartbeatPageRequest request) {
        PageHelper.startPage(request.getPageNum(), request.getPageSize());
        Page<ComponentHeartbeatEntity> entityPage =
                (Page<ComponentHeartbeatEntity>) componentHeartbeatMapper.selectByCondition(request);
```

### AutoCloseableResource
'Page' used without 'try'-with-resources statement
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/heartbeat/HeartbeatServiceImpl.java`
#### Snippet
```java

    private PageResult<GroupHeartbeatResponse> listGroupHeartbeatOpt(HeartbeatPageRequest request) {
        PageHelper.startPage(request.getPageNum(), request.getPageSize());
        Page<GroupHeartbeatEntity> entityPage = (Page<GroupHeartbeatEntity>) groupHeartbeatMapper.selectByCondition(
                request);
```

### AutoCloseableResource
'Page' used without 'try'-with-resources statement
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/workflow/WorkflowServiceImpl.java`
#### Snippet
```java
    @Override
    public PageResult<ProcessResponse> listProcess(ProcessRequest query) {
        PageHelper.startPage(query.getPageNum(), query.getPageSize());
        Page<WorkflowProcessEntity> result = (Page<WorkflowProcessEntity>) queryService.listProcessEntity(query);
        PageInfo<ProcessResponse> pageInfo = result.toPageInfo(entity -> {
```

### AutoCloseableResource
'Page' used without 'try'-with-resources statement
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/workflow/WorkflowServiceImpl.java`
#### Snippet
```java
                    .statusSet(Collections.singleton(TaskStatus.PENDING))
                    .build();
            PageHelper.startPage(0, 100);
            pageResult.getList().forEach(this.addCurrentTask(taskQuery));
        }
```

### AutoCloseableResource
'Page' used without 'try'-with-resources statement
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/workflow/WorkflowServiceImpl.java`
#### Snippet
```java
    @Override
    public PageResult<TaskResponse> listTask(TaskRequest query) {
        PageHelper.startPage(query.getPageNum(), query.getPageSize());
        Page<WorkflowTaskEntity> result = (Page<WorkflowTaskEntity>) queryService.listTaskEntity(query);

```

### AutoCloseableResource
'Page' used without 'try'-with-resources statement
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/workflow/WorkflowServiceImpl.java`
#### Snippet
```java

        // Paging query process instance, construct process execution log
        PageHelper.startPage(query.getPageNum(), query.getPageSize());
        Page<WorkflowProcessEntity> entityPage = (Page<WorkflowProcessEntity>) queryService.listProcessEntity(
                processRequest);
```

### AutoCloseableResource
'Page' used without 'try'-with-resources statement
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/transform/StreamTransformServiceImpl.java`
#### Snippet
```java
        String streamId = request.getInlongStreamId();
        LOGGER.debug("begin to fetch transform info by groupId={} and streamId={} ", groupId, streamId);
        PageHelper.startPage(request.getPageNum(), request.getPageSize());
        PageResult<TransformResponse> pageResponse = new PageResult<>();
        InlongGroupEntity groupEntity = groupMapper.selectByGroupId(groupId);
```

### AutoCloseableResource
'ServerConnector' used without 'try'-with-resources statement
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/http/SimpleHttpSource.java`
#### Snippet
```java
                LOG.info("sslEnabled {}", sslEnabled);
            } else {
                ServerConnector connector = new ServerConnector(srv);
                connector.setReuseAddress(true);
                connector.setIdleTimeout(maxIdelTime);
```

## RuleId[id=ComparatorMethodParameterNotUsed]
### ComparatorMethodParameterNotUsed
Comparator does not return 0 for equal elements
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/ClientMgr.java`
#### Snippet
```java
                        return -1;
                    } else {
                        return 1;
                    }
                }
```

### ComparatorMethodParameterNotUsed
Comparator does not return 0 for equal elements
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/ClientMgr.java`
#### Snippet
```java
                        return -1;
                    } else {
                        return 1;
                    }
                }
```

## RuleId[id=RedundantCast]
### RedundantCast
Casting `map` to `ConcurrentMap` is redundant
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/utils/ConcurrentHashSet.java`
#### Snippet
```java
    @Override
    public boolean add(E o) {
        Boolean answer = ((ConcurrentMap<E, Boolean>) map).putIfAbsent(o, Boolean.TRUE);
        return answer == null;
    }
```

### RedundantCast
Casting `Preconditions.checkNotNull(...)` to `String` is redundant
in `inlong-sort/sort-flink/base/src/main/java/org/apache/inlong/sort/base/util/concurrent/ExecutorThreadFactory.java`
#### Snippet
```java
    ExecutorThreadFactory(String poolName, int threadPriority, @Nullable UncaughtExceptionHandler exceptionHandler) {
        this.threadNumber = new AtomicInteger(1);
        this.namePrefix = (String) Preconditions.checkNotNull(poolName, "poolName") + "-thread-";
        this.threadPriority = threadPriority;
        this.exceptionHandler = exceptionHandler;
```

### RedundantCast
Casting `null` to `SecurityManager` is redundant
in `inlong-sort/sort-flink/base/src/main/java/org/apache/inlong/sort/base/security/FlinkSecurityManager.java`
#### Snippet
```java

    public static void forceProcessExit(int exitCode) {
        System.setSecurityManager((SecurityManager) null);
        if (flinkSecurityManager != null && flinkSecurityManager.haltOnSystemExit) {
            Runtime.getRuntime().halt(exitCode);
```

### RedundantCast
Casting `configuration.get(...)` to `UserSystemExitMode` is redundant
in `inlong-sort/sort-flink/base/src/main/java/org/apache/inlong/sort/base/security/FlinkSecurityManager.java`
#### Snippet
```java
    static FlinkSecurityManager fromConfiguration(Configuration configuration) {
        UserSystemExitMode userSystemExitMode =
                (UserSystemExitMode) configuration.get(ClusterOptions.INTERCEPT_USER_SYSTEM_EXIT);
        boolean haltOnSystemExit = (Boolean) configuration.get(ClusterOptions.HALT_ON_FATAL_ERROR);
        if (userSystemExitMode == UserSystemExitMode.DISABLED && !haltOnSystemExit) {
```

### RedundantCast
Casting `configuration.get(...)` to `Boolean` is redundant
in `inlong-sort/sort-flink/base/src/main/java/org/apache/inlong/sort/base/security/FlinkSecurityManager.java`
#### Snippet
```java
        UserSystemExitMode userSystemExitMode =
                (UserSystemExitMode) configuration.get(ClusterOptions.INTERCEPT_USER_SYSTEM_EXIT);
        boolean haltOnSystemExit = (Boolean) configuration.get(ClusterOptions.HALT_ON_FATAL_ERROR);
        if (userSystemExitMode == UserSystemExitMode.DISABLED && !haltOnSystemExit) {
            return null;
```

### RedundantCast
Casting `Preconditions.checkNotNull(...)` to `UserSystemExitMode` is redundant
in `inlong-sort/sort-flink/base/src/main/java/org/apache/inlong/sort/base/security/FlinkSecurityManager.java`
#### Snippet
```java
            SecurityManager originalSecurityManager) {
        this.monitorUserSystemExit = new InheritableThreadLocal();
        this.userSystemExitMode = (UserSystemExitMode) Preconditions.checkNotNull(userSystemExitMode);
        this.haltOnSystemExit = haltOnSystemExit;
        this.originalSecurityManager = originalSecurityManager;
```

### RedundantCast
Casting `val` to `byte[]` is redundant
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/jdbc/src/main/java/org/apache/inlong/sort/jdbc/converter/AbstractJdbcRowConverter.java`
#### Snippet
```java
            case BINARY:
            case VARBINARY:
                return val -> (byte[]) val;
            case ARRAY:
            case ROW:
```

### RedundantCast
Casting `key` to `T` is redundant
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/iceberg/src/main/java/org/apache/inlong/sort/iceberg/sink/collections/RocksDBKVBuffer.java`
#### Snippet
```java
        R value = get(key);
        if (value != null) {
            lazyGetRocksDb().delete(ROCKSDB_COL_FAMILY, (T) key);
        }
        return value;
```

### RedundantCast
Casting `HISTORY.getOrDefault(...)` to `Collection` is redundant
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/utils/DatabaseHistoryUtil.java`
#### Snippet
```java
                        engineName));
            } else {
                return (Collection) HISTORY.getOrDefault(engineName, Collections.emptyList());
            }
        }
```

### RedundantCast
Casting `msgId` to `MessageIdImpl` is redundant
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/pulsar/PulsarClientService.java`
#### Snippet
```java
        producer.newMessage().properties(proMap).value(event.getBody())
                .sendAsync().thenAccept((msgId) -> {
                    sendMessageCallBack.handleMessageSendSuccess((MessageIdImpl) msgId, es);

                }).exceptionally((e) -> {
```

### RedundantCast
Casting `this.counters.get(...)` to `AtomicLong` is redundant
in `inlong-common/src/main/java/org/apache/inlong/common/monitor/CounterGroupExt.java`
#### Snippet
```java
        }

        return (AtomicLong) this.counters.get(name);
    }

```

### RedundantCast
Casting `this.counters.get(...)` to `AtomicLong` is redundant
in `inlong-common/src/main/java/org/apache/inlong/common/monitor/CounterGroup.java`
#### Snippet
```java
        }

        return (AtomicLong) this.counters.get(name);
    }

```

### RedundantCast
Casting `entry.getKey()` to `String` is redundant
in `inlong-common/src/main/java/org/apache/inlong/common/monitor/CounterGroupExt.java`
#### Snippet
```java
            while (ite.hasNext()) {
                Entry<String, AtomicLong> entry = (Entry) ite.next();
                this.addAndGet((String) entry.getKey(), ((AtomicLong) entry.getValue()).get());
            }

```

### RedundantCast
Casting `entry.getKey()` to `String` is redundant
in `inlong-common/src/main/java/org/apache/inlong/common/monitor/CounterGroup.java`
#### Snippet
```java
            while (iterator.hasNext()) {
                Entry<String, AtomicLong> entry = (Entry) iterator.next();
                this.addAndGet((String) entry.getKey(), ((AtomicLong) entry.getValue()).get());
            }

```

### RedundantCast
Casting `entry.getValue()` to `AtomicLong` is redundant
in `inlong-common/src/main/java/org/apache/inlong/common/monitor/CounterGroupExt.java`
#### Snippet
```java
            while (ite.hasNext()) {
                Entry<String, AtomicLong> entry = (Entry) ite.next();
                this.addAndGet((String) entry.getKey(), ((AtomicLong) entry.getValue()).get());
            }

```

### RedundantCast
Casting `entry.getValue()` to `AtomicLong` is redundant
in `inlong-common/src/main/java/org/apache/inlong/common/monitor/CounterGroup.java`
#### Snippet
```java
            while (iterator.hasNext()) {
                Entry<String, AtomicLong> entry = (Entry) iterator.next();
                this.addAndGet((String) entry.getKey(), ((AtomicLong) entry.getValue()).get());
            }

```

### RedundantCast
Casting `entry.getKey()` to `String` is redundant
in `inlong-common/src/main/java/org/apache/inlong/common/metric/MetricObserver.java`
#### Snippet
```java
            while (var4.hasNext()) {
                Entry<String, String> entry = (Entry) var4.next();
                String key = (String) entry.getKey();
                if (key.startsWith(prefix)) {
                    String name = key.substring(prefix.length());
```

### RedundantCast
Casting `(this.intervalSec * 1000)` to `long` is redundant
in `inlong-common/src/main/java/org/apache/inlong/common/monitor/StatRunner.java`
#### Snippet
```java
        while (!this.shutDownFlag) {
            try {
                Thread.sleep((long) (this.intervalSec * 1000));
                Iterator iterator = this.moniterNames.iterator();

```

### RedundantCast
Casting `counters.get(...)` to `Long` is redundant
in `inlong-common/src/main/java/org/apache/inlong/common/monitor/StatRunner.java`
#### Snippet
```java
                while (iterator.hasNext()) {
                    str = (String) iterator.next();
                    cnt = (Long) counters.get(str);
                    logger.info("{}.{}={}", new Object[]{this.name, str, cnt});
                }
```

### RedundantCast
Casting `((TBaseConstants.META_MAX_MESSAGE_DATA_SIZE_UPPER_LIMIT + TBaseConstants.META_MB_UNIT_SIZE * 8) / RP ...` to `int` is redundant
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/RpcConstants.java`
#### Snippet
```java
    public static final int RPC_MAX_BUFFER_SIZE = 8192;
    public static final int MAX_FRAME_MAX_LIST_SIZE =
            (int) ((TBaseConstants.META_MAX_MESSAGE_DATA_SIZE_UPPER_LIMIT
                    + TBaseConstants.META_MB_UNIT_SIZE * 8) / RPC_MAX_BUFFER_SIZE);

```

## RuleId[id=JavadocDeclaration]
### JavadocDeclaration
`@throws` tag description is missing
in `inlong-sdk/sort-sdk/src/main/java/org/apache/inlong/sdk/sort/api/TopicFetcher.java`
#### Snippet
```java
     * Ack message by the given msgOffset.
     * @param msgOffset Offset of message.
     * @throws Exception
     */
    void ack(String msgOffset) throws Exception;
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-sdk/sort-sdk/src/main/java/org/apache/inlong/sdk/sort/impl/SortClientImpl.java`
#### Snippet
```java
     * @param msgKey String
     * @param msgOffset String
     * @throws Exception
     */
    @Override
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-sdk/sort-sdk/src/main/java/org/apache/inlong/sdk/sort/impl/SortClientImpl.java`
#### Snippet
```java
     *
     * @return true/false
     * @throws Throwable
     */
    @Override
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sdk/sort-sdk/src/main/java/org/apache/inlong/sdk/sort/api/SortClientConfig.java`
#### Snippet
```java
    /**
     * get the type of topic manager
     * @return
     */
    public TopicType getTopicType() {
```

### JavadocDeclaration
`@param sortSdkParams` tag description is missing
in `inlong-sdk/sort-sdk/src/main/java/org/apache/inlong/sdk/sort/api/SortClientConfig.java`
#### Snippet
```java
    /**
     * setParameters
     * @param sortSdkParams
     */
    public void setParameters(Map<String, String> sortSdkParams) {
```

### JavadocDeclaration
`@param topicType` tag description is missing
in `inlong-sdk/sort-sdk/src/main/java/org/apache/inlong/sdk/sort/api/SortClientConfig.java`
#### Snippet
```java
    /**
     * Set type of topic manager
     * @param topicType
     */
    public void setTopicManagerType(TopicType topicType) {
```

### JavadocDeclaration
`@param getUrl` tag description is missing
in `inlong-sdk/sort-sdk/src/main/java/org/apache/inlong/sdk/sort/impl/QueryConsumeConfigImpl.java`
#### Snippet
```java
     * params error FAIL = -1; common error
     *
     * @param  getUrl
     * @param  response      ManagerResponse
     * @param  respCodeValue int
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sdk/sort-sdk/src/main/java/org/apache/inlong/sdk/sort/metrics/SortSdkMetricItemSet.java`
#### Snippet
```java
     * snapshot
     *
     * @return
     */
    @Override
```

### JavadocDeclaration
`@param domain` tag description is missing
in `inlong-sdk/sort-sdk/src/main/java/org/apache/inlong/sdk/sort/metrics/SortSdkPrometheusMetricListener.java`
#### Snippet
```java
     * snapshot
     *
     * @param domain
     * @param metrics
     */
```

### JavadocDeclaration
`@param metrics` tag description is missing
in `inlong-sdk/sort-sdk/src/main/java/org/apache/inlong/sdk/sort/metrics/SortSdkPrometheusMetricListener.java`
#### Snippet
```java
     *
     * @param domain
     * @param metrics
     */
    public void snapshot(String domain, Map<String, MetricValue> metrics) {
```

### JavadocDeclaration
`@param args` tag description is missing
in `inlong-sdk/sdk-common/src/main/java/org/apache/inlong/sdk/commons/admin/AdminServiceRegister.java`
#### Snippet
```java
     * main
     * 
     * @param args
     */
    public static void main(String[] args) {
```

### JavadocDeclaration
`@param response` tag description is missing
in `inlong-sdk/sdk-common/src/main/java/org/apache/inlong/sdk/commons/admin/AbstractAdminEventHandler.java`
#### Snippet
```java
     * outputResponse
     *
     * @param response
     * @param outputString
     */
```

### JavadocDeclaration
`@param outputString` tag description is missing
in `inlong-sdk/sdk-common/src/main/java/org/apache/inlong/sdk/commons/admin/AbstractAdminEventHandler.java`
#### Snippet
```java
     *
     * @param response
     * @param outputString
     */
    public void outputResponse(HttpServletResponse response, String outputString) {
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sdk/sdk-common/src/main/java/org/apache/inlong/sdk/commons/admin/PropertiesConfigurationProvider.java`
#### Snippet
```java
     * getFlumeConfiguration
     * 
     * @return
     */
    @Override
```

### JavadocDeclaration
`@param rootName` tag description is missing
in `inlong-sdk/sdk-common/src/main/java/org/apache/inlong/sdk/commons/admin/PropertiesConfigurationProvider.java`
#### Snippet
```java
     * PropertiesConfigurationProvider
     * 
     * @param rootName
     * @param flumeConf
     */
```

### JavadocDeclaration
`@param flumeConf` tag description is missing
in `inlong-sdk/sdk-common/src/main/java/org/apache/inlong/sdk/commons/admin/PropertiesConfigurationProvider.java`
#### Snippet
```java
     * 
     * @param rootName
     * @param flumeConf
     */
    public PropertiesConfigurationProvider(String rootName, Map<String, String> flumeConf) {
```

### JavadocDeclaration
`@param cmd` tag description is missing
in `inlong-sdk/sdk-common/src/main/java/org/apache/inlong/sdk/commons/admin/AdminEventHandler.java`
#### Snippet
```java
     * process
     * 
     * @param cmd
     * @param event
     * @param response
```

### JavadocDeclaration
`@param event` tag description is missing
in `inlong-sdk/sdk-common/src/main/java/org/apache/inlong/sdk/commons/admin/AdminEventHandler.java`
#### Snippet
```java
     * 
     * @param cmd
     * @param event
     * @param response
     */
```

### JavadocDeclaration
`@param response` tag description is missing
in `inlong-sdk/sdk-common/src/main/java/org/apache/inlong/sdk/commons/admin/AdminEventHandler.java`
#### Snippet
```java
     * @param cmd
     * @param event
     * @param response
     */
    void process(String cmd, Event event, HttpServletResponse response);
```

### JavadocDeclaration
`@param resultCode` tag description is missing
in `inlong-sdk/sdk-common/src/main/java/org/apache/inlong/sdk/commons/protocol/ProxyPackEvent.java`
#### Snippet
```java
    /**
     * acknowledge
     * @param resultCode
     */
    public void acknowledge(ResultCode resultCode) {
```

### JavadocDeclaration
`@param inlongGroupId` tag description is missing
in `inlong-sdk/sdk-common/src/main/java/org/apache/inlong/sdk/commons/protocol/ProxyPackEvent.java`
#### Snippet
```java
    /**
     * Constructor
     * @param inlongGroupId
     * @param inlongStreamId
     * @param events
```

### JavadocDeclaration
`@param inlongStreamId` tag description is missing
in `inlong-sdk/sdk-common/src/main/java/org/apache/inlong/sdk/commons/protocol/ProxyPackEvent.java`
#### Snippet
```java
     * Constructor
     * @param inlongGroupId
     * @param inlongStreamId
     * @param events
     * @param callback
```

### JavadocDeclaration
`@param events` tag description is missing
in `inlong-sdk/sdk-common/src/main/java/org/apache/inlong/sdk/commons/protocol/ProxyPackEvent.java`
#### Snippet
```java
     * @param inlongGroupId
     * @param inlongStreamId
     * @param events
     * @param callback
     */
```

### JavadocDeclaration
`@param callback` tag description is missing
in `inlong-sdk/sdk-common/src/main/java/org/apache/inlong/sdk/commons/protocol/ProxyPackEvent.java`
#### Snippet
```java
     * @param inlongStreamId
     * @param events
     * @param callback
     */
    public ProxyPackEvent(String inlongGroupId, String inlongStreamId, List<ProxyEvent> events,
```

### JavadocDeclaration
`@param data` tag description is missing
in `inlong-sdk/sdk-common/src/main/java/org/apache/inlong/sdk/commons/utils/GzipUtils.java`
#### Snippet
```java
     * compress
     * 
     * @param  data
     * @return      byte array
     */
```

### JavadocDeclaration
`@param bytes` tag description is missing
in `inlong-sdk/sdk-common/src/main/java/org/apache/inlong/sdk/commons/utils/GzipUtils.java`
#### Snippet
```java
     * decompress
     * 
     * @param  bytes
     * @return       byte array
     */
```

### JavadocDeclaration
`@param inlongGroupId` tag description is missing
in `inlong-sdk/sdk-common/src/main/java/org/apache/inlong/sdk/commons/protocol/InlongId.java`
#### Snippet
```java
     * generateUid
     * 
     * @param  inlongGroupId
     * @param  inlongStreamId
     * @return
```

### JavadocDeclaration
`@param inlongStreamId` tag description is missing
in `inlong-sdk/sdk-common/src/main/java/org/apache/inlong/sdk/commons/protocol/InlongId.java`
#### Snippet
```java
     * 
     * @param  inlongGroupId
     * @param  inlongStreamId
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sdk/sdk-common/src/main/java/org/apache/inlong/sdk/commons/protocol/InlongId.java`
#### Snippet
```java
     * @param  inlongGroupId
     * @param  inlongStreamId
     * @return
     */
    public static String generateUid(String inlongGroupId, String inlongStreamId) {
```

### JavadocDeclaration
`@param inlongGroupId` tag description is missing
in `inlong-sdk/sdk-common/src/main/java/org/apache/inlong/sdk/commons/protocol/SdkEvent.java`
#### Snippet
```java
     * Constructor
     * 
     * @param inlongGroupId
     * @param inlongStreamId
     * @param body
```

### JavadocDeclaration
`@param inlongStreamId` tag description is missing
in `inlong-sdk/sdk-common/src/main/java/org/apache/inlong/sdk/commons/protocol/SdkEvent.java`
#### Snippet
```java
     * 
     * @param inlongGroupId
     * @param inlongStreamId
     * @param body
     */
```

### JavadocDeclaration
`@param body` tag description is missing
in `inlong-sdk/sdk-common/src/main/java/org/apache/inlong/sdk/commons/protocol/SdkEvent.java`
#### Snippet
```java
     * @param inlongGroupId
     * @param inlongStreamId
     * @param body
     */
    public SdkEvent(String inlongGroupId, String inlongStreamId, String body) {
```

### JavadocDeclaration
`@param inlongGroupId` tag description is missing
in `inlong-sdk/sdk-common/src/main/java/org/apache/inlong/sdk/commons/protocol/SdkEvent.java`
#### Snippet
```java
     * Constructor
     * 
     * @param inlongGroupId
     * @param inlongStreamId
     * @param body
```

### JavadocDeclaration
`@param inlongStreamId` tag description is missing
in `inlong-sdk/sdk-common/src/main/java/org/apache/inlong/sdk/commons/protocol/SdkEvent.java`
#### Snippet
```java
     * 
     * @param inlongGroupId
     * @param inlongStreamId
     * @param body
     */
```

### JavadocDeclaration
`@param body` tag description is missing
in `inlong-sdk/sdk-common/src/main/java/org/apache/inlong/sdk/commons/protocol/SdkEvent.java`
#### Snippet
```java
     * @param inlongGroupId
     * @param inlongStreamId
     * @param body
     */
    public SdkEvent(String inlongGroupId, String inlongStreamId, byte[] body) {
```

### JavadocDeclaration
`@param inlongGroupId` tag description is missing
in `inlong-sdk/sdk-common/src/main/java/org/apache/inlong/sdk/commons/protocol/SortEvent.java`
#### Snippet
```java
     * Constructor
     * 
     * @param inlongGroupId
     * @param inlongStreamId
     * @param body
```

### JavadocDeclaration
`@param inlongStreamId` tag description is missing
in `inlong-sdk/sdk-common/src/main/java/org/apache/inlong/sdk/commons/protocol/SortEvent.java`
#### Snippet
```java
     * 
     * @param inlongGroupId
     * @param inlongStreamId
     * @param body
     * @param msgTime
```

### JavadocDeclaration
`@param body` tag description is missing
in `inlong-sdk/sdk-common/src/main/java/org/apache/inlong/sdk/commons/protocol/SortEvent.java`
#### Snippet
```java
     * @param inlongGroupId
     * @param inlongStreamId
     * @param body
     * @param msgTime
     * @param sourceIp
```

### JavadocDeclaration
`@param msgTime` tag description is missing
in `inlong-sdk/sdk-common/src/main/java/org/apache/inlong/sdk/commons/protocol/SortEvent.java`
#### Snippet
```java
     * @param inlongStreamId
     * @param body
     * @param msgTime
     * @param sourceIp
     */
```

### JavadocDeclaration
`@param sourceIp` tag description is missing
in `inlong-sdk/sdk-common/src/main/java/org/apache/inlong/sdk/commons/protocol/SortEvent.java`
#### Snippet
```java
     * @param body
     * @param msgTime
     * @param sourceIp
     */
    public SortEvent(String inlongGroupId, String inlongStreamId, byte[] body, long msgTime, String sourceIp) {
```

### JavadocDeclaration
`@param inlongGroupId` tag description is missing
in `inlong-sdk/sdk-common/src/main/java/org/apache/inlong/sdk/commons/protocol/SortEvent.java`
#### Snippet
```java
     * Constructor
     * 
     * @param inlongGroupId
     * @param inlongStreamId
     * @param obj
```

### JavadocDeclaration
`@param inlongStreamId` tag description is missing
in `inlong-sdk/sdk-common/src/main/java/org/apache/inlong/sdk/commons/protocol/SortEvent.java`
#### Snippet
```java
     * 
     * @param inlongGroupId
     * @param inlongStreamId
     * @param obj
     */
```

### JavadocDeclaration
`@param obj` tag description is missing
in `inlong-sdk/sdk-common/src/main/java/org/apache/inlong/sdk/commons/protocol/SortEvent.java`
#### Snippet
```java
     * @param inlongGroupId
     * @param inlongStreamId
     * @param obj
     */
    public SortEvent(String inlongGroupId, String inlongStreamId, MessageObj obj) {
```

### JavadocDeclaration
`@param context` tag description is missing
in `inlong-sdk/sdk-common/src/main/java/org/apache/inlong/sdk/commons/admin/AdminJsonHandler.java`
#### Snippet
```java
     * configure
     * 
     * @param context
     */
    @Override
```

### JavadocDeclaration
`@param inlongGroupId` tag description is missing
in `inlong-sdk/sdk-common/src/main/java/org/apache/inlong/sdk/commons/protocol/ProxyEvent.java`
#### Snippet
```java
     * Constructor
     * 
     * @param inlongGroupId
     * @param inlongStreamId
     * @param obj
```

### JavadocDeclaration
`@param inlongStreamId` tag description is missing
in `inlong-sdk/sdk-common/src/main/java/org/apache/inlong/sdk/commons/protocol/ProxyEvent.java`
#### Snippet
```java
     * 
     * @param inlongGroupId
     * @param inlongStreamId
     * @param obj
     */
```

### JavadocDeclaration
`@param obj` tag description is missing
in `inlong-sdk/sdk-common/src/main/java/org/apache/inlong/sdk/commons/protocol/ProxyEvent.java`
#### Snippet
```java
     * @param inlongGroupId
     * @param inlongStreamId
     * @param obj
     */
    public ProxyEvent(String inlongGroupId, String inlongStreamId, MessageObj obj) {
```

### JavadocDeclaration
`@param inlongGroupId` tag description is missing
in `inlong-sdk/sdk-common/src/main/java/org/apache/inlong/sdk/commons/protocol/ProxyEvent.java`
#### Snippet
```java
     * Constructor
     * 
     * @param inlongGroupId
     * @param inlongStreamId
     * @param body
```

### JavadocDeclaration
`@param inlongStreamId` tag description is missing
in `inlong-sdk/sdk-common/src/main/java/org/apache/inlong/sdk/commons/protocol/ProxyEvent.java`
#### Snippet
```java
     * 
     * @param inlongGroupId
     * @param inlongStreamId
     * @param body
     * @param msgTime
```

### JavadocDeclaration
`@param body` tag description is missing
in `inlong-sdk/sdk-common/src/main/java/org/apache/inlong/sdk/commons/protocol/ProxyEvent.java`
#### Snippet
```java
     * @param inlongGroupId
     * @param inlongStreamId
     * @param body
     * @param msgTime
     * @param sourceIp
```

### JavadocDeclaration
`@param msgTime` tag description is missing
in `inlong-sdk/sdk-common/src/main/java/org/apache/inlong/sdk/commons/protocol/ProxyEvent.java`
#### Snippet
```java
     * @param inlongStreamId
     * @param body
     * @param msgTime
     * @param sourceIp
     */
```

### JavadocDeclaration
`@param sourceIp` tag description is missing
in `inlong-sdk/sdk-common/src/main/java/org/apache/inlong/sdk/commons/protocol/ProxyEvent.java`
#### Snippet
```java
     * @param body
     * @param msgTime
     * @param sourceIp
     */
    public ProxyEvent(String inlongGroupId, String inlongStreamId, byte[] body, long msgTime, String sourceIp) {
```

### JavadocDeclaration
`@param compressedType` tag description is missing
in `inlong-sdk/sdk-common/src/main/java/org/apache/inlong/sdk/commons/protocol/EventUtils.java`
#### Snippet
```java
     * encodeCacheMessageBody
     * 
     * @param  compressedType
     * @param  events
     * @return byte array
```

### JavadocDeclaration
`@param events` tag description is missing
in `inlong-sdk/sdk-common/src/main/java/org/apache/inlong/sdk/commons/protocol/EventUtils.java`
#### Snippet
```java
     * 
     * @param  compressedType
     * @param  events
     * @return byte array
     * @throws IOException
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-sdk/sdk-common/src/main/java/org/apache/inlong/sdk/commons/protocol/EventUtils.java`
#### Snippet
```java
     * @param  events
     * @return byte array
     * @throws IOException
     */
    public static byte[] encodeCacheMessageBody(INLONG_COMPRESSED_TYPE compressedType, List<ProxyEvent> events)
```

### JavadocDeclaration
`@param inlongGroupId` tag description is missing
in `inlong-sdk/sdk-common/src/main/java/org/apache/inlong/sdk/commons/protocol/EventUtils.java`
#### Snippet
```java
     * encode
     * 
     * @param  inlongGroupId
     * @param  inlongStreamId
     * @param  compressedType
```

### JavadocDeclaration
`@param inlongStreamId` tag description is missing
in `inlong-sdk/sdk-common/src/main/java/org/apache/inlong/sdk/commons/protocol/EventUtils.java`
#### Snippet
```java
     * 
     * @param  inlongGroupId
     * @param  inlongStreamId
     * @param  compressedType
     * @param  events
```

### JavadocDeclaration
`@param compressedType` tag description is missing
in `inlong-sdk/sdk-common/src/main/java/org/apache/inlong/sdk/commons/protocol/EventUtils.java`
#### Snippet
```java
     * @param  inlongGroupId
     * @param  inlongStreamId
     * @param  compressedType
     * @param  events
     * @return MessagePack
```

### JavadocDeclaration
`@param events` tag description is missing
in `inlong-sdk/sdk-common/src/main/java/org/apache/inlong/sdk/commons/protocol/EventUtils.java`
#### Snippet
```java
     * @param  inlongStreamId
     * @param  compressedType
     * @param  events
     * @return MessagePack
     * @throws IOException
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-sdk/sdk-common/src/main/java/org/apache/inlong/sdk/commons/protocol/EventUtils.java`
#### Snippet
```java
     * @param  events
     * @return MessagePack
     * @throws IOException
     */
    public static MessagePack encodeSdkEvents(String inlongGroupId, String inlongStreamId,
```

### JavadocDeclaration
`@param packObject` tag description is missing
in `inlong-sdk/sdk-common/src/main/java/org/apache/inlong/sdk/commons/protocol/EventUtils.java`
#### Snippet
```java
     * decodeSdkPack
     * 
     * @param  packObject
     * @return List,ProxyEvent
     * @throws IOException
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-sdk/sdk-common/src/main/java/org/apache/inlong/sdk/commons/protocol/EventUtils.java`
#### Snippet
```java
     * @param  packObject
     * @return List,ProxyEvent
     * @throws IOException
     */
    public static List<ProxyEvent> decodeSdkPack(MessagePack packObject) throws IOException {
```

### JavadocDeclaration
`@param inlongGroupId` tag description is missing
in `inlong-sdk/sdk-common/src/main/java/org/apache/inlong/sdk/commons/protocol/EventUtils.java`
#### Snippet
```java
     * decodeCacheMessageBody
     * 
     * @param  inlongGroupId
     * @param  inlongStreamId
     * @param  compressedType
```

### JavadocDeclaration
`@param inlongStreamId` tag description is missing
in `inlong-sdk/sdk-common/src/main/java/org/apache/inlong/sdk/commons/protocol/EventUtils.java`
#### Snippet
```java
     * 
     * @param  inlongGroupId
     * @param  inlongStreamId
     * @param  compressedType
     * @param  msgBody
```

### JavadocDeclaration
`@param compressedType` tag description is missing
in `inlong-sdk/sdk-common/src/main/java/org/apache/inlong/sdk/commons/protocol/EventUtils.java`
#### Snippet
```java
     * @param  inlongGroupId
     * @param  inlongStreamId
     * @param  compressedType
     * @param  msgBody
     * @return List,SortEvent
```

### JavadocDeclaration
`@param msgBody` tag description is missing
in `inlong-sdk/sdk-common/src/main/java/org/apache/inlong/sdk/commons/protocol/EventUtils.java`
#### Snippet
```java
     * @param  inlongStreamId
     * @param  compressedType
     * @param  msgBody
     * @return List,SortEvent
     * @throws IOException
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-sdk/sdk-common/src/main/java/org/apache/inlong/sdk/commons/protocol/EventUtils.java`
#### Snippet
```java
     * @param  msgBody
     * @return List,SortEvent
     * @throws IOException
     */
    public static List<SortEvent> decodeCacheMessageBody(String inlongGroupId, String inlongStreamId,
```

### JavadocDeclaration
`@param materializedConfiguration` tag description is missing
in `inlong-sdk/sdk-common/src/main/java/org/apache/inlong/sdk/commons/admin/AdminTask.java`
#### Snippet
```java
     * startAllComponents
     * 
     * @param materializedConfiguration
     */
    private void startAllComponents(MaterializedConfiguration materializedConfiguration) {
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-sdk/sdk-common/src/main/java/org/apache/inlong/sdk/commons/admin/AdminTask.java`
#### Snippet
```java
     * 
     * @return             Map
     * @throws IOException
     */
    private Map<String, String> generateFlumeConfiguration() throws IOException {
```

### JavadocDeclaration
`@param context` tag description is missing
in `inlong-sdk/sdk-common/src/main/java/org/apache/inlong/sdk/commons/admin/AdminTask.java`
#### Snippet
```java
     * Constructor
     * 
     * @param context
     */
    public AdminTask(Context context) {
```

### JavadocDeclaration
`@param args` tag description is missing
in `inlong-sdk/sdk-common/src/main/java/org/apache/inlong/sdk/commons/admin/AdminTask.java`
#### Snippet
```java
     * main
     * 
     * @param args
     */
    public static void main(String[] args) {
```

### JavadocDeclaration
`@param conf` tag description is missing
in `inlong-sdk/sdk-common/src/main/java/org/apache/inlong/sdk/commons/admin/AdminTask.java`
#### Snippet
```java
     * handleConfigurationEvent
     * 
     * @param conf
     */
    @Subscribe
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/ISenderBuilder.java`
#### Snippet
```java
     * build
     * 
     * @return
     */
    public MessageSender build();
```

### JavadocDeclaration
`@param inlongGroupId` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/MessageSender.java`
#### Snippet
```java
     * Sending timeout is configured by MessageSenderBuilder.<br>
     * Message time will be current time.<br>
     * @param inlongGroupId
     * @param inlongStreamId
     * @param bodyList
```

### JavadocDeclaration
`@param inlongStreamId` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/MessageSender.java`
#### Snippet
```java
     * Message time will be current time.<br>
     * @param inlongGroupId
     * @param inlongStreamId
     * @param bodyList
     * @param callback callback can be null
```

### JavadocDeclaration
`@param bodyList` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/MessageSender.java`
#### Snippet
```java
     * @param inlongGroupId
     * @param inlongStreamId
     * @param bodyList
     * @param callback callback can be null
     * @throws ProxysdkException
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/MessageSender.java`
#### Snippet
```java
     * @param bodyList
     * @param callback callback can be null
     * @throws ProxysdkException
     */
    void asyncSendMessage(String inlongGroupId, String inlongStreamId, List<byte[]> bodyList,
```

### JavadocDeclaration
`@param inlongGroupId` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/MessageSender.java`
#### Snippet
```java
     * Sending timeout is configured by MessageSenderBuilder.<br>
     * Message time will be current time.<br>
     * @param inlongGroupId
     * @param inlongStreamId
     * @param body
```

### JavadocDeclaration
`@param inlongStreamId` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/MessageSender.java`
#### Snippet
```java
     * Message time will be current time.<br>
     * @param inlongGroupId
     * @param inlongStreamId
     * @param body
     * @param callback callback can be null
```

### JavadocDeclaration
`@param body` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/MessageSender.java`
#### Snippet
```java
     * @param inlongGroupId
     * @param inlongStreamId
     * @param body
     * @param callback callback can be null
     * @throws ProxysdkException
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/MessageSender.java`
#### Snippet
```java
     * @param body
     * @param callback callback can be null
     * @throws ProxysdkException
     */
    void asyncSendMessage(String inlongGroupId, String inlongStreamId, byte[] body, SendMessageCallback callback)
```

### JavadocDeclaration
`@param ctx` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/SdkSenderClientHandler.java`
#### Snippet
```java
     * channelDisconnected
     * 
     * @param  ctx
     * @param  e
     * @throws Exception
```

### JavadocDeclaration
`@param e` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/SdkSenderClientHandler.java`
#### Snippet
```java
     * 
     * @param  ctx
     * @param  e
     * @throws Exception
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/SdkSenderClientHandler.java`
#### Snippet
```java
     * @param  ctx
     * @param  e
     * @throws Exception
     */
    @Override
```

### JavadocDeclaration
`@param manager` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/SdkSenderClientHandler.java`
#### Snippet
```java
     * Constructor
     * 
     * @param manager
     */
    public SdkSenderClientHandler(SdkProxyChannelManager manager) {
```

### JavadocDeclaration
`@param ctx` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/SdkSenderClientHandler.java`
#### Snippet
```java
     * exceptionCaught
     * 
     * @param  ctx
     * @param  e
     * @throws Exception
```

### JavadocDeclaration
`@param e` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/SdkSenderClientHandler.java`
#### Snippet
```java
     * 
     * @param  ctx
     * @param  e
     * @throws Exception
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/SdkSenderClientHandler.java`
#### Snippet
```java
     * @param  ctx
     * @param  e
     * @throws Exception
     */
    @Override
```

### JavadocDeclaration
`@param ctx` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/SdkSenderClientHandler.java`
#### Snippet
```java
     * channelConnected
     * 
     * @param  ctx
     * @param  e
     * @throws Exception
```

### JavadocDeclaration
`@param e` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/SdkSenderClientHandler.java`
#### Snippet
```java
     * 
     * @param  ctx
     * @param  e
     * @throws Exception
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/SdkSenderClientHandler.java`
#### Snippet
```java
     * @param  ctx
     * @param  e
     * @throws Exception
     */
    @Override
```

### JavadocDeclaration
`@param ctx` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/SdkSenderClientHandler.java`
#### Snippet
```java
     * messageReceived
     * 
     * @param  ctx
     * @param  e
     * @throws Exception
```

### JavadocDeclaration
`@param e` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/SdkSenderClientHandler.java`
#### Snippet
```java
     * 
     * @param  ctx
     * @param  e
     * @throws Exception
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/SdkSenderClientHandler.java`
#### Snippet
```java
     * @param  ctx
     * @param  e
     * @throws Exception
     */
    @Override
```

### JavadocDeclaration
`@param ctx` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/SdkSenderClientHandler.java`
#### Snippet
```java
     * channelClosed
     * 
     * @param  ctx
     * @param  e
     * @throws Exception
```

### JavadocDeclaration
`@param e` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/SdkSenderClientHandler.java`
#### Snippet
```java
     * 
     * @param  ctx
     * @param  e
     * @throws Exception
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/SdkSenderClientHandler.java`
#### Snippet
```java
     * @param  ctx
     * @param  e
     * @throws Exception
     */
    @Override
```

### JavadocDeclaration
`@param ctx` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/SdkSenderClientHandler.java`
#### Snippet
```java
     * writeComplete
     * 
     * @param  ctx
     * @param  e
     * @throws Exception
```

### JavadocDeclaration
`@param e` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/SdkSenderClientHandler.java`
#### Snippet
```java
     * 
     * @param  ctx
     * @param  e
     * @throws Exception
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/SdkSenderClientHandler.java`
#### Snippet
```java
     * @param  ctx
     * @param  e
     * @throws Exception
     */
    @Override
```

### JavadocDeclaration
`@param ctx` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/SdkSenderClientHandler.java`
#### Snippet
```java
     * getChannel
     * 
     * @param  ctx
     * @param  e
     * @return
```

### JavadocDeclaration
`@param e` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/SdkSenderClientHandler.java`
#### Snippet
```java
     * 
     * @param  ctx
     * @param  e
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/SdkSenderClientHandler.java`
#### Snippet
```java
     * @param  ctx
     * @param  e
     * @return
     */
    private Channel getChannel(ChannelHandlerContext ctx, ChannelEvent e) {
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/MessageSenderBuilder.java`
#### Snippet
```java
     * build
     * 
     * @return
     */
    public MessageSender build() {
```

### JavadocDeclaration
`@param value` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/config/LoaderType.java`
#### Snippet
```java
     * Constructor
     * 
     * @param value
     */
    LoaderType(String value) {
```

### JavadocDeclaration
`@param manager` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/SdkChannelWorker.java`
#### Snippet
```java
     * Constructor
     * 
     * @param manager
     */
    public SdkChannelWorker(SdkProxyChannelManager manager, int index) {
```

### JavadocDeclaration
`@param currentRecord` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/SdkChannelWorker.java`
#### Snippet
```java
     * checkSendResult
     * 
     * @param  currentRecord
     * @param  result
     * @param  ipPort
```

### JavadocDeclaration
`@param result` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/SdkChannelWorker.java`
#### Snippet
```java
     * 
     * @param  currentRecord
     * @param  result
     * @param  ipPort
     * @param  tProfile
```

### JavadocDeclaration
`@param ipPort` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/SdkChannelWorker.java`
#### Snippet
```java
     * @param  currentRecord
     * @param  result
     * @param  ipPort
     * @param  tProfile
     * @return
```

### JavadocDeclaration
`@param tProfile` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/SdkChannelWorker.java`
#### Snippet
```java
     * @param  result
     * @param  ipPort
     * @param  tProfile
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/SdkChannelWorker.java`
#### Snippet
```java
     * @param  ipPort
     * @param  tProfile
     * @return
     */
    private boolean checkSendResult(DispatchProfile currentRecord, TcpResult result, IpPort ipPort,
```

### JavadocDeclaration
`@param context` tag description is missing
in `inlong-sdk/sdk-common/src/main/java/org/apache/inlong/sdk/commons/admin/AdminHttpSource.java`
#### Snippet
```java
     * handleDeprecatedParameter
     *
     * @param context
     * @param newParam
     * @param oldParam
```

### JavadocDeclaration
`@param newParam` tag description is missing
in `inlong-sdk/sdk-common/src/main/java/org/apache/inlong/sdk/commons/admin/AdminHttpSource.java`
#### Snippet
```java
     *
     * @param context
     * @param newParam
     * @param oldParam
     */
```

### JavadocDeclaration
`@param oldParam` tag description is missing
in `inlong-sdk/sdk-common/src/main/java/org/apache/inlong/sdk/commons/admin/AdminHttpSource.java`
#### Snippet
```java
     * @param context
     * @param newParam
     * @param oldParam
     */
    private void handleDeprecatedParameter(Context context, String newParam, String oldParam) {
```

### JavadocDeclaration
`@param context` tag description is missing
in `inlong-sdk/sdk-common/src/main/java/org/apache/inlong/sdk/commons/admin/AdminHttpSource.java`
#### Snippet
```java
     * configure
     *
     * @param context
     */
    @Override
```

### JavadocDeclaration
`@param context` tag description is missing
in `inlong-sdk/sdk-common/src/main/java/org/apache/inlong/sdk/commons/admin/AdminHttpSource.java`
#### Snippet
```java
     * configureSsl
     *
     * @param context
     */
    @Override
```

### JavadocDeclaration
`@param request` tag description is missing
in `inlong-sdk/sdk-common/src/main/java/org/apache/inlong/sdk/commons/admin/AdminHttpSource.java`
#### Snippet
```java
         * doPost
         *
         * @param request
         * @param response
         * @throws IOException
```

### JavadocDeclaration
`@param response` tag description is missing
in `inlong-sdk/sdk-common/src/main/java/org/apache/inlong/sdk/commons/admin/AdminHttpSource.java`
#### Snippet
```java
         *
         * @param request
         * @param response
         * @throws IOException
         */
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-sdk/sdk-common/src/main/java/org/apache/inlong/sdk/commons/admin/AdminHttpSource.java`
#### Snippet
```java
         * @param request
         * @param response
         * @throws IOException
         */
        @Override
```

### JavadocDeclaration
`@param request` tag description is missing
in `inlong-sdk/sdk-common/src/main/java/org/apache/inlong/sdk/commons/admin/AdminHttpSource.java`
#### Snippet
```java
         * doGet
         *
         * @param request
         * @param response
         * @throws IOException
```

### JavadocDeclaration
`@param response` tag description is missing
in `inlong-sdk/sdk-common/src/main/java/org/apache/inlong/sdk/commons/admin/AdminHttpSource.java`
#### Snippet
```java
         *
         * @param request
         * @param response
         * @throws IOException
         */
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-sdk/sdk-common/src/main/java/org/apache/inlong/sdk/commons/admin/AdminHttpSource.java`
#### Snippet
```java
         * @param request
         * @param response
         * @throws IOException
         */
        @Override
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/ProxySdkSink.java`
#### Snippet
```java
     * 
     * @return                        Status
     * @throws EventDeliveryException
     */
    @Override
```

### JavadocDeclaration
`@param context` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/ProxySdkSink.java`
#### Snippet
```java
     * configure
     * 
     * @param context
     */
    @Override
```

### JavadocDeclaration
`@param tProfile` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/SdkProxyChannelManager.java`
#### Snippet
```java
     * onMessageOK
     * 
     * @param tProfile
     * @param channel
     */
```

### JavadocDeclaration
`@param channel` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/SdkProxyChannelManager.java`
#### Snippet
```java
     * 
     * @param tProfile
     * @param channel
     */
    private void onMessageOK(SdkProfile tProfile, Channel channel) {
```

### JavadocDeclaration
`@param ctx` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/SdkProxyChannelManager.java`
#### Snippet
```java
     * onMessageReceived
     * 
     * @param ctx
     * @param e
     */
```

### JavadocDeclaration
`@param e` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/SdkProxyChannelManager.java`
#### Snippet
```java
     * 
     * @param ctx
     * @param e
     */
    public void onMessageReceived(ChannelHandlerContext ctx, MessageEvent e) {
```

### JavadocDeclaration
`@param tProfile` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/SdkProxyChannelManager.java`
#### Snippet
```java
     * removeWaitCompletedProfile
     * 
     * @param tProfile
     */
    public void removeWaitCompletedProfile(SdkProfile tProfile) {
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/SdkProxyChannelManager.java`
#### Snippet
```java
     * 
     * @return                      DispatchProfile
     * @throws InterruptedException
     */
    public DispatchProfile takeDispatchQueue() throws InterruptedException {
```

### JavadocDeclaration
`@param tProfile` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/SdkProxyChannelManager.java`
#### Snippet
```java
     * putWaitCompletedProfile
     * 
     * @param tProfile
     */
    public void putWaitCompletedProfile(SdkProfile tProfile) {
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/SdkProxyChannelManager.java`
#### Snippet
```java
     * nextPackId
     * 
     * @return
     */
    public long nextPackId() {
```

### JavadocDeclaration
`@param profile` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/SdkProxyChannelManager.java`
#### Snippet
```java
     * offerDispatchQueue
     * 
     * @param  profile
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/SdkProxyChannelManager.java`
#### Snippet
```java
     * 
     * @param  profile
     * @return
     */
    public boolean offerDispatchQueue(DispatchProfile profile) {
```

### JavadocDeclaration
`@param channel` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/SdkProxyChannelManager.java`
#### Snippet
```java
     * clearChannel
     * 
     * @param channel
     */
    public void setChannelException(Channel channel) {
```

### JavadocDeclaration
`@param channel` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/SdkProxyChannelManager.java`
#### Snippet
```java
     * parseInetSocketAddress
     * 
     * @param  channel
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/SdkProxyChannelManager.java`
#### Snippet
```java
     * 
     * @param  channel
     * @return
     */
    public static InetSocketAddress parseInetSocketAddress(Channel channel) {
```

### JavadocDeclaration
`@param proxyClusterId` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/SdkProxyChannelManager.java`
#### Snippet
```java
     * Constructor
     * 
     * @param proxyClusterId
     * @param context
     */
```

### JavadocDeclaration
`@param context` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/SdkProxyChannelManager.java`
#### Snippet
```java
     * 
     * @param proxyClusterId
     * @param context
     */
    public SdkProxyChannelManager(String proxyClusterId, SdkSinkContext context) {
```

### JavadocDeclaration
`@param context` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
     * configure
     * 
     * @param context
     */
    @Override
```

### JavadocDeclaration
`@param body` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
     * sendMessage
     * 
     * @param  body
     * @param  groupId
     * @param  streamId
```

### JavadocDeclaration
`@param groupId` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
     * 
     * @param  body
     * @param  groupId
     * @param  streamId
     * @param  dt
```

### JavadocDeclaration
`@param streamId` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
     * @param  body
     * @param  groupId
     * @param  streamId
     * @param  dt
     * @param  msgUUID
```

### JavadocDeclaration
`@param dt` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
     * @param  groupId
     * @param  streamId
     * @param  dt
     * @param  msgUUID
     * @param  timeout
```

### JavadocDeclaration
`@param msgUUID` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
     * @param  streamId
     * @param  dt
     * @param  msgUUID
     * @param  timeout
     * @param  timeUnit
```

### JavadocDeclaration
`@param timeout` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
     * @param  dt
     * @param  msgUUID
     * @param  timeout
     * @param  timeUnit
     * @return          SendResult
```

### JavadocDeclaration
`@param timeUnit` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
     * @param  msgUUID
     * @param  timeout
     * @param  timeUnit
     * @return          SendResult
     */
```

### JavadocDeclaration
`@param events` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
     * putAll
     * 
     * @param events
     */
    private void putAll(List<CallbackProfile> events) {
```

### JavadocDeclaration
`@param callback` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
     * asyncSendMessage
     * 
     * @param  callback
     * @param  body
     * @param  groupId
```

### JavadocDeclaration
`@param body` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
     * 
     * @param  callback
     * @param  body
     * @param  groupId
     * @param  streamId
```

### JavadocDeclaration
`@param groupId` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
     * @param  callback
     * @param  body
     * @param  groupId
     * @param  streamId
     * @param  dt
```

### JavadocDeclaration
`@param streamId` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
     * @param  body
     * @param  groupId
     * @param  streamId
     * @param  dt
     * @param  msgUUID
```

### JavadocDeclaration
`@param dt` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
     * @param  groupId
     * @param  streamId
     * @param  dt
     * @param  msgUUID
     * @param  timeout
```

### JavadocDeclaration
`@param msgUUID` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
     * @param  streamId
     * @param  dt
     * @param  msgUUID
     * @param  timeout
     * @param  timeUnit
```

### JavadocDeclaration
`@param timeout` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
     * @param  dt
     * @param  msgUUID
     * @param  timeout
     * @param  timeUnit
     * @param  extraAttrMap
```

### JavadocDeclaration
`@param timeUnit` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
     * @param  msgUUID
     * @param  timeout
     * @param  timeUnit
     * @param  extraAttrMap
     * @throws ProxysdkException
```

### JavadocDeclaration
`@param extraAttrMap` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
     * @param  timeout
     * @param  timeUnit
     * @param  extraAttrMap
     * @throws ProxysdkException
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
     * @param  timeUnit
     * @param  extraAttrMap
     * @throws ProxysdkException
     */
    @Override
```

### JavadocDeclaration
`@param inlongGroupId` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
     * asyncSendMessage
     * 
     * @param  inlongGroupId
     * @param  inlongStreamId
     * @param  body
```

### JavadocDeclaration
`@param inlongStreamId` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
     * 
     * @param  inlongGroupId
     * @param  inlongStreamId
     * @param  body
     * @param  callback
```

### JavadocDeclaration
`@param body` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
     * @param  inlongGroupId
     * @param  inlongStreamId
     * @param  body
     * @param  callback
     * @throws ProxysdkException
```

### JavadocDeclaration
`@param callback` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
     * @param  inlongStreamId
     * @param  body
     * @param  callback
     * @throws ProxysdkException
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
     * @param  body
     * @param  callback
     * @throws ProxysdkException
     */
    @Override
```

### JavadocDeclaration
`@param bodyList` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
     * sendMessage
     * 
     * @param  bodyList
     * @param  groupId
     * @param  streamId
```

### JavadocDeclaration
`@param groupId` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
     * 
     * @param  bodyList
     * @param  groupId
     * @param  streamId
     * @param  dt
```

### JavadocDeclaration
`@param streamId` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
     * @param  bodyList
     * @param  groupId
     * @param  streamId
     * @param  dt
     * @param  msgUUID
```

### JavadocDeclaration
`@param dt` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
     * @param  groupId
     * @param  streamId
     * @param  dt
     * @param  msgUUID
     * @param  timeout
```

### JavadocDeclaration
`@param msgUUID` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
     * @param  streamId
     * @param  dt
     * @param  msgUUID
     * @param  timeout
     * @param  timeUnit
```

### JavadocDeclaration
`@param timeout` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
     * @param  dt
     * @param  msgUUID
     * @param  timeout
     * @param  timeUnit
     * @param  extraAttrMap
```

### JavadocDeclaration
`@param timeUnit` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
     * @param  msgUUID
     * @param  timeout
     * @param  timeUnit
     * @param  extraAttrMap
     * @return              SendResult
```

### JavadocDeclaration
`@param extraAttrMap` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
     * @param  timeout
     * @param  timeUnit
     * @param  extraAttrMap
     * @return              SendResult
     */
```

### JavadocDeclaration
`@param name` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
     * Constructor
     * 
     * @param name
     */
    public PbProtocolMessageSender(String name) {
```

### JavadocDeclaration
`@param callback` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
     * asyncSendMessage
     * 
     * @param      callback
     * @param      body
     * @param      attributes
```

### JavadocDeclaration
`@param body` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
     * 
     * @param      callback
     * @param      body
     * @param      attributes
     * @param      msgUUID
```

### JavadocDeclaration
`@param attributes` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
     * @param      callback
     * @param      body
     * @param      attributes
     * @param      msgUUID
     * @param      timeout
```

### JavadocDeclaration
`@param msgUUID` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
     * @param      body
     * @param      attributes
     * @param      msgUUID
     * @param      timeout
     * @param      timeUnit
```

### JavadocDeclaration
`@param timeout` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
     * @param      attributes
     * @param      msgUUID
     * @param      timeout
     * @param      timeUnit
     * @throws     ProxysdkException
```

### JavadocDeclaration
`@param timeUnit` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
     * @param      msgUUID
     * @param      timeout
     * @param      timeUnit
     * @throws     ProxysdkException
     * @deprecated
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
     * @param      timeout
     * @param      timeUnit
     * @throws     ProxysdkException
     * @deprecated
     */
```

### JavadocDeclaration
`@param callback` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
     * asyncSendMessage
     * 
     * @param  callback
     * @param  bodyList
     * @param  groupId
```

### JavadocDeclaration
`@param bodyList` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
     * 
     * @param  callback
     * @param  bodyList
     * @param  groupId
     * @param  streamId
```

### JavadocDeclaration
`@param groupId` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
     * @param  callback
     * @param  bodyList
     * @param  groupId
     * @param  streamId
     * @param  dt
```

### JavadocDeclaration
`@param streamId` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
     * @param  bodyList
     * @param  groupId
     * @param  streamId
     * @param  dt
     * @param  msgUUID
```

### JavadocDeclaration
`@param dt` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
     * @param  groupId
     * @param  streamId
     * @param  dt
     * @param  msgUUID
     * @param  timeout
```

### JavadocDeclaration
`@param msgUUID` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
     * @param  streamId
     * @param  dt
     * @param  msgUUID
     * @param  timeout
     * @param  timeUnit
```

### JavadocDeclaration
`@param timeout` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
     * @param  dt
     * @param  msgUUID
     * @param  timeout
     * @param  timeUnit
     * @param  extraAttrMap
```

### JavadocDeclaration
`@param timeUnit` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
     * @param  msgUUID
     * @param  timeout
     * @param  timeUnit
     * @param  extraAttrMap
     * @throws ProxysdkException
```

### JavadocDeclaration
`@param extraAttrMap` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
     * @param  timeout
     * @param  timeUnit
     * @param  extraAttrMap
     * @throws ProxysdkException
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
     * @param  timeUnit
     * @param  extraAttrMap
     * @throws ProxysdkException
     */
    @Override
```

### JavadocDeclaration
`@param body` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
     * sendMessage
     * 
     * @param      body
     * @param      attributes
     * @param      msgUUID
```

### JavadocDeclaration
`@param attributes` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
     * 
     * @param      body
     * @param      attributes
     * @param      msgUUID
     * @param      timeout
```

### JavadocDeclaration
`@param msgUUID` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
     * @param      body
     * @param      attributes
     * @param      msgUUID
     * @param      timeout
     * @param      timeUnit
```

### JavadocDeclaration
`@param timeout` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
     * @param      attributes
     * @param      msgUUID
     * @param      timeout
     * @param      timeUnit
     * @return                SendResult
```

### JavadocDeclaration
`@param timeUnit` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
     * @param      msgUUID
     * @param      timeout
     * @param      timeUnit
     * @return                SendResult
     * @deprecated
```

### JavadocDeclaration
`@param callback` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
     * asyncSendMessage
     * 
     * @param  callback
     * @param  bodyList
     * @param  groupId
```

### JavadocDeclaration
`@param bodyList` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
     * 
     * @param  callback
     * @param  bodyList
     * @param  groupId
     * @param  streamId
```

### JavadocDeclaration
`@param groupId` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
     * @param  callback
     * @param  bodyList
     * @param  groupId
     * @param  streamId
     * @param  dt
```

### JavadocDeclaration
`@param configList` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/config/pojo/ProxyClusterConfig.java`
#### Snippet
```java
     * generateMd5
     * 
     * @param  configList
     * @return
     */
```

### JavadocDeclaration
`@param streamId` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
     * @param  bodyList
     * @param  groupId
     * @param  streamId
     * @param  dt
     * @param  msgUUID
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/config/pojo/ProxyClusterConfig.java`
#### Snippet
```java
     * 
     * @param  configList
     * @return
     */
    public static String generateMd5(List<ProxyClusterConfig> configList) {
```

### JavadocDeclaration
`@param dt` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
     * @param  groupId
     * @param  streamId
     * @param  dt
     * @param  msgUUID
     * @param  timeout
```

### JavadocDeclaration
`@param config` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/config/pojo/ProxyClusterConfig.java`
#### Snippet
```java
     * generateMd5
     * 
     * @param  config
     * @return
     */
```

### JavadocDeclaration
`@param msgUUID` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
     * @param  streamId
     * @param  dt
     * @param  msgUUID
     * @param  timeout
     * @param  timeUnit
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/config/pojo/ProxyClusterConfig.java`
#### Snippet
```java
     * 
     * @param  config
     * @return
     */
    public static String generateMd5(ProxyClusterConfig config) {
```

### JavadocDeclaration
`@param timeout` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
     * @param  dt
     * @param  msgUUID
     * @param  timeout
     * @param  timeUnit
     * @throws ProxysdkException
```

### JavadocDeclaration
`@param timeUnit` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
     * @param  msgUUID
     * @param  timeout
     * @param  timeUnit
     * @throws ProxysdkException
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/config/pojo/ProxyClusterConfig.java`
#### Snippet
```java
     * getExample
     * 
     * @return
     */
    public static ProxyClusterConfig getExample() {
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
     * @param  timeout
     * @param  timeUnit
     * @throws ProxysdkException
     */
    @Override
```

### JavadocDeclaration
`@param callback` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
     * asyncSendMessage
     * 
     * @param  callback
     * @param  body
     * @param  groupId
```

### JavadocDeclaration
`@param body` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
     * 
     * @param  callback
     * @param  body
     * @param  groupId
     * @param  streamId
```

### JavadocDeclaration
`@param groupId` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
     * @param  callback
     * @param  body
     * @param  groupId
     * @param  streamId
     * @param  dt
```

### JavadocDeclaration
`@param streamId` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
     * @param  body
     * @param  groupId
     * @param  streamId
     * @param  dt
     * @param  msgUUID
```

### JavadocDeclaration
`@param dt` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
     * @param  groupId
     * @param  streamId
     * @param  dt
     * @param  msgUUID
     * @param  timeout
```

### JavadocDeclaration
`@param msgUUID` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
     * @param  streamId
     * @param  dt
     * @param  msgUUID
     * @param  timeout
     * @param  timeUnit
```

### JavadocDeclaration
`@param timeout` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
     * @param  dt
     * @param  msgUUID
     * @param  timeout
     * @param  timeUnit
     * @throws ProxysdkException
```

### JavadocDeclaration
`@param timeUnit` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
     * @param  msgUUID
     * @param  timeout
     * @param  timeUnit
     * @throws ProxysdkException
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
     * @param  timeout
     * @param  timeUnit
     * @throws ProxysdkException
     */
    @Override
```

### JavadocDeclaration
`@param event` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
     * put
     * 
     * @param event
     */
    private void put(CallbackProfile event) {
```

### JavadocDeclaration
`@param inlongGroupId` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
     * asyncSendMessage
     * 
     * @param  inlongGroupId
     * @param  inlongStreamId
     * @param  bodyList
```

### JavadocDeclaration
`@param inlongStreamId` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
     * 
     * @param  inlongGroupId
     * @param  inlongStreamId
     * @param  bodyList
     * @param  callback
```

### JavadocDeclaration
`@param bodyList` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
     * @param  inlongGroupId
     * @param  inlongStreamId
     * @param  bodyList
     * @param  callback
     * @throws ProxysdkException
```

### JavadocDeclaration
`@param callback` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
     * @param  inlongStreamId
     * @param  bodyList
     * @param  callback
     * @throws ProxysdkException
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
     * @param  bodyList
     * @param  callback
     * @throws ProxysdkException
     */
    @Override
```

### JavadocDeclaration
`@param bodyList` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
     * sendMessage
     * 
     * @param  bodyList
     * @param  groupId
     * @param  streamId
```

### JavadocDeclaration
`@param groupId` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
     * 
     * @param  bodyList
     * @param  groupId
     * @param  streamId
     * @param  dt
```

### JavadocDeclaration
`@param streamId` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
     * @param  bodyList
     * @param  groupId
     * @param  streamId
     * @param  dt
     * @param  msgUUID
```

### JavadocDeclaration
`@param dt` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
     * @param  groupId
     * @param  streamId
     * @param  dt
     * @param  msgUUID
     * @param  timeout
```

### JavadocDeclaration
`@param msgUUID` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
     * @param  streamId
     * @param  dt
     * @param  msgUUID
     * @param  timeout
     * @param  timeUnit
```

### JavadocDeclaration
`@param timeout` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
     * @param  dt
     * @param  msgUUID
     * @param  timeout
     * @param  timeUnit
     * @return          SendResult
```

### JavadocDeclaration
`@param timeUnit` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
     * @param  msgUUID
     * @param  timeout
     * @param  timeUnit
     * @return          SendResult
     */
```

### JavadocDeclaration
`@param body` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
     * sendMessage
     * 
     * @param  body
     * @param  groupId
     * @param  streamId
```

### JavadocDeclaration
`@param groupId` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
     * 
     * @param  body
     * @param  groupId
     * @param  streamId
     * @param  dt
```

### JavadocDeclaration
`@param streamId` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
     * @param  body
     * @param  groupId
     * @param  streamId
     * @param  dt
     * @param  msgUUID
```

### JavadocDeclaration
`@param dt` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
     * @param  groupId
     * @param  streamId
     * @param  dt
     * @param  msgUUID
     * @param  timeout
```

### JavadocDeclaration
`@param msgUUID` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
     * @param  streamId
     * @param  dt
     * @param  msgUUID
     * @param  timeout
     * @param  timeUnit
```

### JavadocDeclaration
`@param timeout` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
     * @param  dt
     * @param  msgUUID
     * @param  timeout
     * @param  timeUnit
     * @param  extraAttrMap
```

### JavadocDeclaration
`@param timeUnit` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
     * @param  msgUUID
     * @param  timeout
     * @param  timeUnit
     * @param  extraAttrMap
     * @return              SendResult
```

### JavadocDeclaration
`@param extraAttrMap` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
     * @param  timeout
     * @param  timeUnit
     * @param  extraAttrMap
     * @return              SendResult
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/config/pojo/GetProxyConfigByStreamResponse.java`
#### Snippet
```java
     * getExample
     * 
     * @return
     */
    public static GetProxyConfigByStreamResponse getExample() {
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/config/pojo/ProxyClusterResult.java`
#### Snippet
```java
     * getExample
     * 
     * @return
     */
    public static ProxyClusterResult getExample() {
```

### JavadocDeclaration
`@param value` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/config/pojo/ErrorCode.java`
#### Snippet
```java
     * Constructor
     * 
     * @param value
     */
    ErrorCode(int value) {
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/config/pojo/GetProxyConfigByAgentResponse.java`
#### Snippet
```java
     * getExample
     * 
     * @return
     */
    public static GetProxyConfigByAgentResponse getExample() {
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/config/pojo/GetProxyConfigBySdkResponse.java`
#### Snippet
```java
     * getExample
     * 
     * @return
     */
    public static GetProxyConfigBySdkResponse getExample() {
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/config/pojo/GetProxyConfigBySdkRequest.java`
#### Snippet
```java
     * getExample
     * 
     * @return
     */
    public static GetProxyConfigBySdkRequest getExample() {
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/config/ManagerProxyClusterConfigLoader.java`
#### Snippet
```java
     * constructHttpClient
     * 
     * @return
     */
    private static synchronized CloseableHttpClient constructHttpClient() {
```

### JavadocDeclaration
`@param inlongGroupId` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/config/ManagerProxyClusterConfigLoader.java`
#### Snippet
```java
     * loadByStream
     * 
     * @param  inlongGroupId
     * @param  inlongStreamId
     * @return
```

### JavadocDeclaration
`@param inlongStreamId` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/config/ManagerProxyClusterConfigLoader.java`
#### Snippet
```java
     * 
     * @param  inlongGroupId
     * @param  inlongStreamId
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/config/ManagerProxyClusterConfigLoader.java`
#### Snippet
```java
     * @param  inlongGroupId
     * @param  inlongStreamId
     * @return
     */
    @Override
```

### JavadocDeclaration
`@param context` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/config/ManagerProxyClusterConfigLoader.java`
#### Snippet
```java
     * configure
     * 
     * @param context
     */
    @Override
```

### JavadocDeclaration
`@param proxys` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/config/ManagerProxyClusterConfigLoader.java`
#### Snippet
```java
     * loadByClusterIds
     * 
     * @param  proxys
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/config/ManagerProxyClusterConfigLoader.java`
#### Snippet
```java
     * 
     * @param  proxys
     * @return
     */
    @Override
```

### JavadocDeclaration
`@param maxSizeKb` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/channel/BufferQueue.java`
#### Snippet
```java
     * Constructor
     * 
     * @param maxSizeKb
     * @param globalTokens
     */
```

### JavadocDeclaration
`@param globalTokens` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/channel/BufferQueue.java`
#### Snippet
```java
     * 
     * @param maxSizeKb
     * @param globalTokens
     */
    public BufferQueue(int maxSizeKb, SizeSemaphore globalTokens) {
```

### JavadocDeclaration
`@param maxSizeKb` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/channel/BufferQueue.java`
#### Snippet
```java
     * Constructor
     * 
     * @param maxSizeKb
     */
    public BufferQueue(int maxSizeKb) {
```

### JavadocDeclaration
`@param maxSize` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/channel/SizeSemaphore.java`
#### Snippet
```java
     * Constructor
     * 
     * @param maxSize
     * @param leftSize
     */
```

### JavadocDeclaration
`@param leftSize` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/channel/SizeSemaphore.java`
#### Snippet
```java
     * 
     * @param maxSize
     * @param leftSize
     */
    public SizeSemaphore(int maxSize, int leftSize) {
```

### JavadocDeclaration
`@param bufferQueue` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/channel/ProfileTransaction.java`
#### Snippet
```java
     * Constructor
     * 
     * @param bufferQueue
     */
    public ProfileTransaction(BufferQueue<CallbackProfile> bufferQueue) {
```

### JavadocDeclaration
`@param event` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/channel/ProfileTransaction.java`
#### Snippet
```java
     * doTake
     * 
     * @param event
     */
    public void doTake(CallbackProfile event) {
```

### JavadocDeclaration
`@param event` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/channel/ProfileTransaction.java`
#### Snippet
```java
     * doPut
     * 
     * @param event
     */
    public void doPut(CallbackProfile event) {
```

### JavadocDeclaration
`@param dispatchProfile` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/context/SdkProfile.java`
#### Snippet
```java
     * Constructor
     * 
     * @param dispatchProfile
     * @param sdkPackId
     */
```

### JavadocDeclaration
`@param sdkPackId` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/context/SdkProfile.java`
#### Snippet
```java
     * 
     * @param dispatchProfile
     * @param sdkPackId
     */
    public SdkProfile(DispatchProfile dispatchProfile, long sdkPackId) {
```

### JavadocDeclaration
`@param proxys` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/config/FileProxyClusterConfigLoader.java`
#### Snippet
```java
     * loadByClusterIds
     * 
     * @param  proxys
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/config/FileProxyClusterConfigLoader.java`
#### Snippet
```java
     * 
     * @param  proxys
     * @return
     */
    @Override
```

### JavadocDeclaration
`@param inlongGroupId` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/config/FileProxyClusterConfigLoader.java`
#### Snippet
```java
     * loadByStream
     * 
     * @param  inlongGroupId
     * @param  inlongStreamId
     * @return
```

### JavadocDeclaration
`@param inlongStreamId` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/config/FileProxyClusterConfigLoader.java`
#### Snippet
```java
     * 
     * @param  inlongGroupId
     * @param  inlongStreamId
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/config/FileProxyClusterConfigLoader.java`
#### Snippet
```java
     * @param  inlongGroupId
     * @param  inlongStreamId
     * @return
     */
    @Override
```

### JavadocDeclaration
`@param context` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/config/FileProxyClusterConfigLoader.java`
#### Snippet
```java
     * configure
     * 
     * @param context
     */
    @Override
```

### JavadocDeclaration
`@param context` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/config/ProxyClusterConfigHolder.java`
#### Snippet
```java
     * configure
     * 
     * @param context
     */
    private void configure(Context context) {
```

### JavadocDeclaration
`@param clusterId` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/config/ProxyClusterConfigHolder.java`
#### Snippet
```java
     * getConfigByClusterId
     * 
     * @param  clusterId
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/config/ProxyClusterConfigHolder.java`
#### Snippet
```java
     * 
     * @param  clusterId
     * @return
     */
    public static ProxyClusterConfig getConfigByClusterId(String clusterId) {
```

### JavadocDeclaration
`@param inlongGroupId` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/config/ProxyClusterConfigHolder.java`
#### Snippet
```java
     * getConfigByStream
     * 
     * @param  inlongGroupId
     * @param  inlongStreamId
     * @return
```

### JavadocDeclaration
`@param inlongStreamId` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/config/ProxyClusterConfigHolder.java`
#### Snippet
```java
     * 
     * @param  inlongGroupId
     * @param  inlongStreamId
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/config/ProxyClusterConfigHolder.java`
#### Snippet
```java
     * @param  inlongGroupId
     * @param  inlongStreamId
     * @return
     */
    public static ProxyClusterConfig getConfigByStream(String inlongGroupId, String inlongStreamId) {
```

### JavadocDeclaration
`@param event` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/context/CallbackProfile.java`
#### Snippet
```java
     * Constructor
     * 
     * @param event
     * @param callback
     */
```

### JavadocDeclaration
`@param callback` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/context/CallbackProfile.java`
#### Snippet
```java
     * 
     * @param event
     * @param callback
     */
    public CallbackProfile(SdkEvent event, SendMessageCallback callback) {
```

### JavadocDeclaration
`@param body` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/context/CallbackProfile.java`
#### Snippet
```java
     * setBody
     * 
     * @param body
     */
    @Override
```

### JavadocDeclaration
`@param headers` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/context/CallbackProfile.java`
#### Snippet
```java
     * setHeaders
     * 
     * @param headers
     */
    @Override
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/context/CallbackProfile.java`
#### Snippet
```java
     * getBody
     * 
     * @return
     */
    @Override
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/context/CallbackProfile.java`
#### Snippet
```java
     * getHeaders
     * 
     * @return
     */
    @Override
```

### JavadocDeclaration
`@param headers` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/context/ProfileEvent.java`
#### Snippet
```java
     * setHeaders
     * 
     * @param headers
     */
    @Override
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/context/ProfileEvent.java`
#### Snippet
```java
     * getHeaders
     * 
     * @return
     */
    @Override
```

### JavadocDeclaration
`@param profile` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/context/ProfileEvent.java`
#### Snippet
```java
     * Constructor
     * 
     * @param profile
     * @param transaction
     */
```

### JavadocDeclaration
`@param transaction` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/context/ProfileEvent.java`
#### Snippet
```java
     * 
     * @param profile
     * @param transaction
     */
    public ProfileEvent(CallbackProfile profile, ProfileTransaction transaction) {
```

### JavadocDeclaration
`@param body` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/context/ProfileEvent.java`
#### Snippet
```java
     * setBody
     * 
     * @param body
     */
    @Override
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/context/ProfileEvent.java`
#### Snippet
```java
     * getBody
     * 
     * @return
     */
    @Override
```

### JavadocDeclaration
`@param event` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/channel/BufferQueueChannel.java`
#### Snippet
```java
     * put
     * 
     * @param  event
     * @throws ChannelException
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/channel/BufferQueueChannel.java`
#### Snippet
```java
     * 
     * @param  event
     * @throws ChannelException
     */
    @Override
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/channel/BufferQueueChannel.java`
#### Snippet
```java
     * getGlobalBufferQueueSizeKb
     *
     * @return
     */
    public static SizeSemaphore getGlobalBufferQueueSizeKb(Context context) {
```

### JavadocDeclaration
`@param context` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/channel/BufferQueueChannel.java`
#### Snippet
```java
     * configure
     * 
     * @param context
     */
    @Override
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/channel/BufferQueueChannel.java`
#### Snippet
```java
     * getTransaction
     * 
     * @return
     */
    @Override
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/channel/BufferQueueChannel.java`
#### Snippet
```java
     * 
     * @return                  Event
     * @throws ChannelException
     */
    @Override
```

### JavadocDeclaration
`@param inlongGroupId` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/context/InlongId.java`
#### Snippet
```java
     * generateUid
     * 
     * @param  inlongGroupId
     * @param  inlongStreamId
     * @return
```

### JavadocDeclaration
`@param inlongStreamId` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/context/InlongId.java`
#### Snippet
```java
     * 
     * @param  inlongGroupId
     * @param  inlongStreamId
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/context/InlongId.java`
#### Snippet
```java
     * @param  inlongGroupId
     * @param  inlongStreamId
     * @return
     */
    public static String generateUid(String inlongGroupId, String inlongStreamId) {
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/metrics/SdkMetricItemSet.java`
#### Snippet
```java
     * createItem
     * 
     * @return
     */
    @Override
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/metrics/SdkMetricItemSet.java`
#### Snippet
```java
     * snapshot
     * 
     * @return
     */
    public List<MetricItem> snapshot() {
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/metrics/SdkMetricItemSet.java`
#### Snippet
```java
     * getItemMap
     * 
     * @return
     */
    public Map<String, SdkMetricItem> getItemMap() {
```

### JavadocDeclaration
`@param name` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/metrics/SdkMetricItemSet.java`
#### Snippet
```java
     * Constructor
     * 
     * @param name
     */
    public SdkMetricItemSet(String name) {
```

### JavadocDeclaration
`@param event` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/context/SinkContext.java`
#### Snippet
```java
     * fillAuditFormatTime
     *
     * @param event
     * @param dimensions
     */
```

### JavadocDeclaration
`@param dimensions` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/context/SinkContext.java`
#### Snippet
```java
     *
     * @param event
     * @param dimensions
     */
    public void fillAuditFormatTime(Event event, Map<String, String> dimensions) {
```

### JavadocDeclaration
`@param context` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/context/SinkContext.java`
#### Snippet
```java
     * Constructor
     * 
     * @param context
     * @param channel
     */
```

### JavadocDeclaration
`@param channel` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/context/SinkContext.java`
#### Snippet
```java
     * 
     * @param context
     * @param channel
     */
    public SinkContext(Context context, Channel channel) {
```

### JavadocDeclaration
`@param msgTime` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/context/SinkContext.java`
#### Snippet
```java
     * getAuditFormatTime
     *
     * @param  msgTime
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/context/SinkContext.java`
#### Snippet
```java
     *
     * @param  msgTime
     * @return
     */
    public long getAuditFormatTime(long msgTime) {
```

### JavadocDeclaration
`@param sendIp` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/network/TcpResult.java`
#### Snippet
```java
     * Constructor
     * 
     * @param sendIp
     * @param sendPort
     * @param result
```

### JavadocDeclaration
`@param sendPort` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/network/TcpResult.java`
#### Snippet
```java
     * 
     * @param sendIp
     * @param sendPort
     * @param result
     * @param errorMsg
```

### JavadocDeclaration
`@param result` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/network/TcpResult.java`
#### Snippet
```java
     * @param sendIp
     * @param sendPort
     * @param result
     * @param errorMsg
     */
```

### JavadocDeclaration
`@param errorMsg` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/network/TcpResult.java`
#### Snippet
```java
     * @param sendPort
     * @param result
     * @param errorMsg
     */
    public TcpResult(String sendIp, int sendPort, boolean result, String errorMsg) {
```

### JavadocDeclaration
`@param ipPort` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/network/TcpResult.java`
#### Snippet
```java
     * Constructor
     * 
     * @param ipPort
     * @param result
     * @param errorMsg
```

### JavadocDeclaration
`@param result` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/network/TcpResult.java`
#### Snippet
```java
     * 
     * @param ipPort
     * @param result
     * @param errorMsg
     */
```

### JavadocDeclaration
`@param errorMsg` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/network/TcpResult.java`
#### Snippet
```java
     * @param ipPort
     * @param result
     * @param errorMsg
     */
    public TcpResult(IpPort ipPort, boolean result, String errorMsg) {
```

### JavadocDeclaration
`@param ctx` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/network/ByteArrayToBinaryEncoder.java`
#### Snippet
```java
     * encode
     * 
     * @param  ctx
     * @param  channel
     * @param  msg
```

### JavadocDeclaration
`@param channel` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/network/ByteArrayToBinaryEncoder.java`
#### Snippet
```java
     * 
     * @param  ctx
     * @param  channel
     * @param  msg
     * @return
```

### JavadocDeclaration
`@param msg` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/network/ByteArrayToBinaryEncoder.java`
#### Snippet
```java
     * @param  ctx
     * @param  channel
     * @param  msg
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/network/ByteArrayToBinaryEncoder.java`
#### Snippet
```java
     * @param  channel
     * @param  msg
     * @return
     */
    @Override
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/network/IpPort.java`
#### Snippet
```java
     * hashCode
     * 
     * @return
     */
    @Override
```

### JavadocDeclaration
`@param addr` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/network/IpPort.java`
#### Snippet
```java
     * Constructor
     * 
     * @param addr
     */
    public IpPort(InetSocketAddress addr) {
```

### JavadocDeclaration
`@param o` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/network/IpPort.java`
#### Snippet
```java
     * equals
     * 
     * @param  o
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/network/IpPort.java`
#### Snippet
```java
     * 
     * @param  o
     * @return
     */
    @Override
```

### JavadocDeclaration
`@param ip` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/network/IpPort.java`
#### Snippet
```java
     * getIpPortKey
     * 
     * @param  ip
     * @param  port
     * @return
```

### JavadocDeclaration
`@param port` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/network/IpPort.java`
#### Snippet
```java
     * 
     * @param  ip
     * @param  port
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/network/IpPort.java`
#### Snippet
```java
     * @param  ip
     * @param  port
     * @return
     */
    public static String getIpPortKey(String ip, int port) {
```

### JavadocDeclaration
`@param ipPort` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/network/IpPort.java`
#### Snippet
```java
     * parseIpPort
     * 
     * @param  ipPort
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/network/IpPort.java`
#### Snippet
```java
     * 
     * @param  ipPort
     * @return
     */
    public static IpPort parseIpPort(String ipPort) {
```

### JavadocDeclaration
`@param ip` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/network/IpPort.java`
#### Snippet
```java
     * Constructor
     * 
     * @param ip
     * @param port
     */
```

### JavadocDeclaration
`@param port` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/network/IpPort.java`
#### Snippet
```java
     * 
     * @param ip
     * @param port
     */
    public IpPort(String ip, int port) {
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/network/IpPort.java`
#### Snippet
```java
     * toString
     * 
     * @return
     */
    public String toString() {
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/network/TcpChannel.java`
#### Snippet
```java
     * tryAcquire
     * 
     * @return
     */
    public boolean tryAcquire() {
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/network/TcpChannel.java`
#### Snippet
```java
     * toString
     * 
     * @return
     */
    @Override
```

### JavadocDeclaration
`@param channel` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/network/TcpChannel.java`
#### Snippet
```java
     * Constructor
     * 
     * @param channel
     * @param ipPort
     */
```

### JavadocDeclaration
`@param ipPort` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/network/TcpChannel.java`
#### Snippet
```java
     * 
     * @param channel
     * @param ipPort
     */
    public TcpChannel(Channel channel, IpPort ipPort) {
```

### JavadocDeclaration
`@param createThreshold` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/dispatch/DispatchProfile.java`
#### Snippet
```java
     * isTimeout
     * 
     * @param  createThreshold
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/dispatch/DispatchProfile.java`
#### Snippet
```java
     * 
     * @param  createThreshold
     * @return
     */
    public boolean isTimeout(long createThreshold) {
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/dispatch/DispatchProfile.java`
#### Snippet
```java
     * getDispatchTime
     * 
     * @return
     */
    public long getDispatchTime() {
```

### JavadocDeclaration
`@param uid` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/dispatch/DispatchProfile.java`
#### Snippet
```java
     * Constructor
     * 
     * @param uid
     * @param inlongGroupId
     * @param inlongStreamId
```

### JavadocDeclaration
`@param inlongGroupId` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/dispatch/DispatchProfile.java`
#### Snippet
```java
     * 
     * @param uid
     * @param inlongGroupId
     * @param inlongStreamId
     * @param dispatchTime
```

### JavadocDeclaration
`@param inlongStreamId` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/dispatch/DispatchProfile.java`
#### Snippet
```java
     * @param uid
     * @param inlongGroupId
     * @param inlongStreamId
     * @param dispatchTime
     */
```

### JavadocDeclaration
`@param dispatchTime` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/dispatch/DispatchProfile.java`
#### Snippet
```java
     * @param inlongGroupId
     * @param inlongStreamId
     * @param dispatchTime
     */
    public DispatchProfile(String uid, String inlongGroupId, String inlongStreamId, long dispatchTime) {
```

### JavadocDeclaration
`@param event` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/dispatch/DispatchProfile.java`
#### Snippet
```java
     * addEvent
     * 
     * @param  event
     * @param  maxPackCount
     * @param  maxPackSize
```

### JavadocDeclaration
`@param maxPackCount` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/dispatch/DispatchProfile.java`
#### Snippet
```java
     * 
     * @param  event
     * @param  maxPackCount
     * @param  maxPackSize
     * @return
```

### JavadocDeclaration
`@param maxPackSize` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/dispatch/DispatchProfile.java`
#### Snippet
```java
     * @param  event
     * @param  maxPackCount
     * @param  maxPackSize
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/dispatch/DispatchProfile.java`
#### Snippet
```java
     * @param  maxPackCount
     * @param  maxPackSize
     * @return
     */
    public boolean addEvent(ProfileEvent event, long maxPackCount, long maxPackSize) {
```

### JavadocDeclaration
`@param event` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/metrics/SdkMetricItem.java`
#### Snippet
```java
     * fillInlongId
     *
     * @param event
     * @param dimensions
     */
```

### JavadocDeclaration
`@param dimensions` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/metrics/SdkMetricItem.java`
#### Snippet
```java
     *
     * @param event
     * @param dimensions
     */
    public static void fillInlongId(Event event, Map<String, String> dimensions) {
```

### JavadocDeclaration
`@param currentRecord` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/metrics/SdkMetricItem.java`
#### Snippet
```java
     * reportDurations
     * 
     * @param currentRecord
     * @param result
     * @param sendTime
```

### JavadocDeclaration
`@param result` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/metrics/SdkMetricItem.java`
#### Snippet
```java
     * 
     * @param currentRecord
     * @param result
     * @param sendTime
     * @param dimensions
```

### JavadocDeclaration
`@param sendTime` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/metrics/SdkMetricItem.java`
#### Snippet
```java
     * @param currentRecord
     * @param result
     * @param sendTime
     * @param dimensions
     * @param msgTime
```

### JavadocDeclaration
`@param dimensions` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/metrics/SdkMetricItem.java`
#### Snippet
```java
     * @param result
     * @param sendTime
     * @param dimensions
     * @param msgTime
     * @param metricItemSet
```

### JavadocDeclaration
`@param msgTime` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/metrics/SdkMetricItem.java`
#### Snippet
```java
     * @param sendTime
     * @param dimensions
     * @param msgTime
     * @param metricItemSet
     */
```

### JavadocDeclaration
`@param metricItemSet` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/metrics/SdkMetricItem.java`
#### Snippet
```java
     * @param dimensions
     * @param msgTime
     * @param metricItemSet
     */
    public static void reportDurations(
```

### JavadocDeclaration
`@param headers` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/metrics/SdkMetricItem.java`
#### Snippet
```java
     * getInlongStreamId
     *
     * @param  headers
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/metrics/SdkMetricItem.java`
#### Snippet
```java
     *
     * @param  headers
     * @return
     */
    public static String getInlongStreamId(Map<String, String> headers) {
```

### JavadocDeclaration
`@param headers` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/metrics/SdkMetricItem.java`
#### Snippet
```java
     * getInlongGroupId
     *
     * @param  headers
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/metrics/SdkMetricItem.java`
#### Snippet
```java
     *
     * @param  headers
     * @return
     */
    public static String getInlongGroupId(Map<String, String> headers) {
```

### JavadocDeclaration
`@param event` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/metrics/SdkMetricItem.java`
#### Snippet
```java
     * getLogTime
     *
     * @param  event
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/metrics/SdkMetricItem.java`
#### Snippet
```java
     *
     * @param  event
     * @return
     */
    public static long getLogTime(Event event) {
```

### JavadocDeclaration
`@param headers` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/metrics/SdkMetricItem.java`
#### Snippet
```java
     * getLogTime
     *
     * @param  headers
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/metrics/SdkMetricItem.java`
#### Snippet
```java
     *
     * @param  headers
     * @return
     */
    public static long getLogTime(Map<String, String> headers) {
```

### JavadocDeclaration
`@param event` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/dispatch/DispatchManager.java`
#### Snippet
```java
     * addEvent
     * 
     * @param event
     */
    public void addEvent(ProfileEvent event) {
```

### JavadocDeclaration
Tag `return` is not allowed here
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/dispatch/DispatchManager.java`
#### Snippet
```java
     * outputOvertimeData
     * 
     * @return
     */
    public void outputOvertimeData() {
```

### JavadocDeclaration
`@param context` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/dispatch/DispatchManager.java`
#### Snippet
```java
     * Constructor
     * 
     * @param context
     * @param dispatchQueue
     */
```

### JavadocDeclaration
`@param dispatchQueue` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/dispatch/DispatchManager.java`
#### Snippet
```java
     * 
     * @param context
     * @param dispatchQueue
     */
    public DispatchManager(Context context, LinkedBlockingQueue<DispatchProfile> dispatchQueue) {
```

### JavadocDeclaration
`@param context` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/context/SdkSinkContext.java`
#### Snippet
```java
     * Constructor
     *
     * @param context
     * @param channel
     */
```

### JavadocDeclaration
`@param channel` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/context/SdkSinkContext.java`
#### Snippet
```java
     *
     * @param context
     * @param channel
     */
    public SdkSinkContext(Context context, Channel channel) {
```

### JavadocDeclaration
`@param uid` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/context/SdkSinkContext.java`
#### Snippet
```java
     * getProxyClusterId
     * 
     * @param  uid
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/context/SdkSinkContext.java`
#### Snippet
```java
     * 
     * @param  uid
     * @return
     */
    public String getProxyClusterId(String uid) {
```

### JavadocDeclaration
`@param currentRecord` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/context/SdkSinkContext.java`
#### Snippet
```java
     * addSendResultMetric
     * 
     * @param currentRecord
     * @param proxyClusterId
     * @param result
```

### JavadocDeclaration
`@param proxyClusterId` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/context/SdkSinkContext.java`
#### Snippet
```java
     * 
     * @param currentRecord
     * @param proxyClusterId
     * @param result
     * @param sendTime
```

### JavadocDeclaration
`@param result` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/context/SdkSinkContext.java`
#### Snippet
```java
     * @param currentRecord
     * @param proxyClusterId
     * @param result
     * @param sendTime
     */
```

### JavadocDeclaration
`@param sendTime` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/context/SdkSinkContext.java`
#### Snippet
```java
     * @param proxyClusterId
     * @param result
     * @param sendTime
     */
    public void addSendResultMetric(DispatchProfile currentRecord, String proxyClusterId, boolean result,
```

### JavadocDeclaration
`@param currentRecord` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/context/SdkSinkContext.java`
#### Snippet
```java
     * addSendMetric
     * 
     * @param currentRecord
     * @param proxyClusterId
     */
```

### JavadocDeclaration
`@param proxyClusterId` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/context/SdkSinkContext.java`
#### Snippet
```java
     * 
     * @param currentRecord
     * @param proxyClusterId
     */
    public void addSendMetric(DispatchProfile currentRecord, String proxyClusterId) {
```

### JavadocDeclaration
`@param currentRecord` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/context/SdkSinkContext.java`
#### Snippet
```java
     * fillInlongId
     * 
     * @param currentRecord
     * @param dimensions
     */
```

### JavadocDeclaration
`@param dimensions` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/context/SdkSinkContext.java`
#### Snippet
```java
     * 
     * @param currentRecord
     * @param dimensions
     */
    public static void fillInlongId(DispatchProfile currentRecord, Map<String, String> dimensions) {
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/DefaultMessageSender.java`
#### Snippet
```java
     * @param bodyList list of messages
     * @param callback callback can be null
     * @throws ProxysdkException
     */
    @Override
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/DefaultMessageSender.java`
#### Snippet
```java
     * @param callback callback can be null
     * @param isProxySend true: dataproxy doesn't return response message until data is sent to MQ
     * @throws ProxysdkException
     */
    public void asyncSendMessage(String inlongGroupId, String inlongStreamId, byte[] body, SendMessageCallback callback,
```

### JavadocDeclaration
`@param timeout` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/DefaultMessageSender.java`
#### Snippet
```java
     * @param dt data report timestamp
     * @param msgUUID msg uuid
     * @param timeout
     * @param timeUnit
     * @param extraAttrMap extra attributes
```

### JavadocDeclaration
`@param timeUnit` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/DefaultMessageSender.java`
#### Snippet
```java
     * @param msgUUID msg uuid
     * @param timeout
     * @param timeUnit
     * @param extraAttrMap extra attributes
     * @param isProxySend true: dataproxy doesn't return response message until data is sent to MQ
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/DefaultMessageSender.java`
#### Snippet
```java
     * @param extraAttrMap extra attributes
     * @param isProxySend true: dataproxy doesn't return response message until data is sent to MQ
     * @throws ProxysdkException
     */
    public void asyncSendMessage(SendMessageCallback callback, byte[] body, String groupId, String streamId, long dt,
```

### JavadocDeclaration
`@param timeout` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/DefaultMessageSender.java`
#### Snippet
```java
     * @param dt data report timestamp
     * @param msgUUID msg uuid
     * @param timeout
     * @param timeUnit
     * @param isProxySend true: dataproxy doesn't return response message until data is sent to MQ
```

### JavadocDeclaration
`@param timeUnit` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/DefaultMessageSender.java`
#### Snippet
```java
     * @param msgUUID msg uuid
     * @param timeout
     * @param timeUnit
     * @param isProxySend true: dataproxy doesn't return response message until data is sent to MQ
     * @throws ProxysdkException
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/DefaultMessageSender.java`
#### Snippet
```java
     * @param timeUnit
     * @param isProxySend true: dataproxy doesn't return response message until data is sent to MQ
     * @throws ProxysdkException
     */
    public void asyncSendMessage(SendMessageCallback callback, byte[] body, String groupId, String streamId, long dt,
```

### JavadocDeclaration
`@param timeout` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/DefaultMessageSender.java`
#### Snippet
```java
     * @param dt data report timestamp
     * @param msgUUID msg uuid
     * @param timeout
     * @param timeUnit
     * @param extraAttrMap extra attributes
```

### JavadocDeclaration
`@param timeUnit` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/DefaultMessageSender.java`
#### Snippet
```java
     * @param msgUUID msg uuid
     * @param timeout
     * @param timeUnit
     * @param extraAttrMap extra attributes
     * @param isProxySend true: dataproxy doesn't return response message until data is sent to MQ
```

### JavadocDeclaration
`@param timeout` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/DefaultMessageSender.java`
#### Snippet
```java
     * @param dt data report time
     * @param msgUUID msg uuid
     * @param timeout
     * @param timeUnit
     * @param extraAttrMap extra attributes
```

### JavadocDeclaration
`@param timeUnit` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/DefaultMessageSender.java`
#### Snippet
```java
     * @param msgUUID msg uuid
     * @param timeout
     * @param timeUnit
     * @param extraAttrMap extra attributes
     * @param isProxySend true: dataproxy doesn't return response message until data is sent to MQ
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/DefaultMessageSender.java`
#### Snippet
```java
     * @param extraAttrMap extra attributes
     * @param isProxySend true: dataproxy doesn't return response message until data is sent to MQ
     * @throws ProxysdkException
     */
    public void asyncSendMessage(SendMessageCallback callback,
```

### JavadocDeclaration
`@param timeout` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/DefaultMessageSender.java`
#### Snippet
```java
     * @param dt data report timestamp
     * @param msgUUID msg uuid
     * @param timeout
     * @param timeUnit
     * @param extraAttrMap extra attributes
```

### JavadocDeclaration
`@param timeUnit` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/DefaultMessageSender.java`
#### Snippet
```java
     * @param msgUUID msg uuid
     * @param timeout
     * @param timeUnit
     * @param extraAttrMap extra attributes
     * @param isProxySend true: dataproxy doesn't return response message until data is sent to MQ
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/DefaultMessageSender.java`
#### Snippet
```java
     * @param callback callback can be null
     * @param isProxySend true: dataproxy doesn't return response message until data is sent to MQ
     * @throws ProxysdkException
     */
    public void asyncSendMessage(String inlongGroupId, String inlongStreamId, List<byte[]> bodyList,
```

### JavadocDeclaration
`@param timeout` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/DefaultMessageSender.java`
#### Snippet
```java
     * @param dt data report time
     * @param msgUUID msg uuid
     * @param timeout
     * @param timeUnit
     * @param isProxySend true: dataproxy doesn't return response message until data is sent to MQ
```

### JavadocDeclaration
`@param timeUnit` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/DefaultMessageSender.java`
#### Snippet
```java
     * @param msgUUID msg uuid
     * @param timeout
     * @param timeUnit
     * @param isProxySend true: dataproxy doesn't return response message until data is sent to MQ
     * @throws ProxysdkException
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/DefaultMessageSender.java`
#### Snippet
```java
     * @param timeUnit
     * @param isProxySend true: dataproxy doesn't return response message until data is sent to MQ
     * @throws ProxysdkException
     */
    public void asyncSendMessage(SendMessageCallback callback, List<byte[]> bodyList,
```

### JavadocDeclaration
`@param timeout` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/DefaultMessageSender.java`
#### Snippet
```java
     * @param dt data report timestamp
     * @param msgUUID msg uuid
     * @param timeout
     * @param timeUnit
     * @param isProxySend true: dataproxy doesn't return response message until data is sent to MQ
```

### JavadocDeclaration
`@param timeUnit` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/DefaultMessageSender.java`
#### Snippet
```java
     * @param msgUUID msg uuid
     * @param timeout
     * @param timeUnit
     * @param isProxySend true: dataproxy doesn't return response message until data is sent to MQ
     * @return SendResult.OK means success
```

### JavadocDeclaration
`@param inlongGroupId` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/DefaultMessageSender.java`
#### Snippet
```java
     * asyncSendMessage
     *
     * @param inlongGroupId
     * @param inlongStreamId
     * @param body
```

### JavadocDeclaration
`@param inlongStreamId` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/DefaultMessageSender.java`
#### Snippet
```java
     *
     * @param inlongGroupId
     * @param inlongStreamId
     * @param body
     * @param callback
```

### JavadocDeclaration
`@param body` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/DefaultMessageSender.java`
#### Snippet
```java
     * @param inlongGroupId
     * @param inlongStreamId
     * @param body
     * @param callback
     * @throws ProxysdkException
```

### JavadocDeclaration
`@param callback` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/DefaultMessageSender.java`
#### Snippet
```java
     * @param inlongStreamId
     * @param body
     * @param callback
     * @throws ProxysdkException
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/DefaultMessageSender.java`
#### Snippet
```java
     * @param body
     * @param callback
     * @throws ProxysdkException
     */
    @Override
```

### JavadocDeclaration
`@param timeout` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/DefaultMessageSender.java`
#### Snippet
```java
     * @param dt data report timestamp
     * @param msgUUID msg uuid
     * @param timeout
     * @param timeUnit
     * @param isProxySend true: dataproxy doesn't return response message until data is sent to MQ
```

### JavadocDeclaration
`@param timeUnit` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/DefaultMessageSender.java`
#### Snippet
```java
     * @param msgUUID msg uuid
     * @param timeout
     * @param timeUnit
     * @param isProxySend true: dataproxy doesn't return response message until data is sent to MQ
     * @return SendResult.OK means success
```

### JavadocDeclaration
`@param bodyList` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/utils/ProxyUtils.java`
#### Snippet
```java
     * check body valid
     *
     * @param bodyList
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/utils/ProxyUtils.java`
#### Snippet
```java
     *
     * @param bodyList
     * @return
     */
    public static boolean isBodyValid(List<byte[]> bodyList) {
```

### JavadocDeclaration
`@param clientConfig` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/utils/ProxyUtils.java`
#### Snippet
```java
     * valid client config
     *
     * @param clientConfig
     */
    public static void validClientConfig(ProxyClientConfig clientConfig) {
```

### JavadocDeclaration
`@param body` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/utils/ProxyUtils.java`
#### Snippet
```java
     * check body valid
     *
     * @param body
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/utils/ProxyUtils.java`
#### Snippet
```java
     *
     * @param body
     * @return
     */
    public static boolean isBodyValid(byte[] body) {
```

### JavadocDeclaration
`@param bytes` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/utils/IpUtils.java`
#### Snippet
```java
     * bytesToInt
     * 
     * @param  bytes
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/utils/IpUtils.java`
#### Snippet
```java
     * 
     * @param  bytes
     * @return
     */
    public static int bytesToInt(byte[] bytes) {
```

### JavadocDeclaration
`@param localHost` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/utils/IpUtils.java`
#### Snippet
```java
     * normalizeHostAddress
     * 
     * @param  localHost
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/utils/IpUtils.java`
#### Snippet
```java
     * 
     * @param  localHost
     * @return
     */
    public static String normalizeHostAddress(final InetAddress localHost) {
```

### JavadocDeclaration
`@param ipAddr` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/utils/IpUtils.java`
#### Snippet
```java
     * toBytes
     * 
     * @param  ipAddr
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/utils/IpUtils.java`
#### Snippet
```java
     * 
     * @param  ipAddr
     * @return
     */
    public static byte[] toBytes(String ipAddr) {
```

### JavadocDeclaration
`@param ipAddr` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/utils/IpUtils.java`
#### Snippet
```java
     * ipToInt
     * 
     * @param  ipAddr
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/utils/IpUtils.java`
#### Snippet
```java
     * 
     * @param  ipAddr
     * @return
     */
    public static int ipToInt(String ipAddr) {
```

### JavadocDeclaration
`@param ipInt` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/utils/IpUtils.java`
#### Snippet
```java
     * intToIp
     * 
     * @param  ipInt
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/utils/IpUtils.java`
#### Snippet
```java
     * 
     * @param  ipInt
     * @return
     */
    public static String intToIp(int ipInt) {
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/utils/IpUtils.java`
#### Snippet
```java
     * getLocalAddressBytes
     * 
     * @return
     *
     * @throws Exception
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/utils/IpUtils.java`
#### Snippet
```java
     * @return
     *
     * @throws Exception
     */
    public static byte[] getLocalAddressBytes() throws Exception {
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/utils/IpUtils.java`
#### Snippet
```java
     * getLocalAddress
     * 
     * @return
     *
     * @throws Exception
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/utils/IpUtils.java`
#### Snippet
```java
     * @return
     *
     * @throws Exception
     */
    public static String getLocalAddress() {
```

### JavadocDeclaration
Exception is not declared to be thrown by method getLocalAddress
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/utils/IpUtils.java`
#### Snippet
```java
     * @return
     *
     * @throws Exception
     */
    public static String getLocalAddress() {
```

### JavadocDeclaration
`@param dataBuf` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/network/TcpChannelGroup.java`
#### Snippet
```java
     * send
     * 
     * @param  dataBuf
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/network/TcpChannelGroup.java`
#### Snippet
```java
     * 
     * @param  dataBuf
     * @return
     */
    public TcpResult send(ChannelBuffer dataBuf) {
```

### JavadocDeclaration
`@param ipList` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/network/TcpChannelGroup.java`
#### Snippet
```java
     * updateConfig
     * 
     * @param ipList
     */
    public void updateConfig(Set<IpPort> ipList) {
```

### JavadocDeclaration
`@param tcpChannel` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/network/TcpChannelGroup.java`
#### Snippet
```java
     * reconnect
     * 
     * @param tcpChannel
     */
    private void reconnect(TcpChannel tcpChannel) {
```

### JavadocDeclaration
`@param bid` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/network/TcpChannelGroup.java`
#### Snippet
```java
     * Constructor
     * 
     * @param bid
     * @param senderThreadNum
     * @param decoder
```

### JavadocDeclaration
`@param senderThreadNum` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/network/TcpChannelGroup.java`
#### Snippet
```java
     * 
     * @param bid
     * @param senderThreadNum
     * @param decoder
     * @param clientHandler
```

### JavadocDeclaration
`@param decoder` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/network/TcpChannelGroup.java`
#### Snippet
```java
     * @param bid
     * @param senderThreadNum
     * @param decoder
     * @param clientHandler
     */
```

### JavadocDeclaration
`@param clientHandler` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/network/TcpChannelGroup.java`
#### Snippet
```java
     * @param senderThreadNum
     * @param decoder
     * @param clientHandler
     */
    public TcpChannelGroup(String bid, int senderThreadNum, ChannelUpstreamHandler decoder,
```

### JavadocDeclaration
`@param channel` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/network/TcpChannelGroup.java`
#### Snippet
```java
     * releaseChannel
     * 
     * @param channel
     */
    public void releaseChannel(Channel channel) {
```

### JavadocDeclaration
`@param channel` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/network/TcpChannelGroup.java`
#### Snippet
```java
     * mark exception channel
     * 
     * @param channel
     */
    public void exceptionChannel(Channel channel) {
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/network/TcpChannelGroup.java`
#### Snippet
```java
     * getTcpChannel
     * 
     * @return
     */
    private TcpChannel getTcpChannel(LinkedBlockingQueue<TcpChannel> channelQueue) {
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/metric/MetricTimeNumSummary.java`
#### Snippet
```java
     * get summary time
     *
     * @return
     */
    public long getSummaryTime() {
```

### JavadocDeclaration
`@param timeout` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/http/InternalHttpSender.java`
#### Snippet
```java
     * http client
     *
     * @param timeout
     * @param timeUnit
     * @return
```

### JavadocDeclaration
`@param timeUnit` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/http/InternalHttpSender.java`
#### Snippet
```java
     *
     * @param timeout
     * @param timeUnit
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/http/InternalHttpSender.java`
#### Snippet
```java
     * @param timeout
     * @param timeUnit
     * @return
     */
    private synchronized CloseableHttpClient constructHttpClient(long timeout, TimeUnit timeUnit) {
```

### JavadocDeclaration
`@param bodies` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/http/InternalHttpSender.java`
#### Snippet
```java
     * send message with host info
     *
     * @param bodies
     * @param groupId
     * @param streamId
```

### JavadocDeclaration
`@param groupId` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/http/InternalHttpSender.java`
#### Snippet
```java
     *
     * @param bodies
     * @param groupId
     * @param streamId
     * @param dt
```

### JavadocDeclaration
`@param streamId` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/http/InternalHttpSender.java`
#### Snippet
```java
     * @param bodies
     * @param groupId
     * @param streamId
     * @param dt
     * @param timeout
```

### JavadocDeclaration
`@param dt` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/http/InternalHttpSender.java`
#### Snippet
```java
     * @param groupId
     * @param streamId
     * @param dt
     * @param timeout
     * @param timeUnit
```

### JavadocDeclaration
`@param timeout` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/http/InternalHttpSender.java`
#### Snippet
```java
     * @param streamId
     * @param dt
     * @param timeout
     * @param timeUnit
     * @return
```

### JavadocDeclaration
`@param timeUnit` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/http/InternalHttpSender.java`
#### Snippet
```java
     * @param dt
     * @param timeout
     * @param timeUnit
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/http/InternalHttpSender.java`
#### Snippet
```java
     * @param timeout
     * @param timeUnit
     * @return
     */
    public SendResult sendMessageWithHostInfo(List<String> bodies, String groupId, String streamId, long dt,
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/http/InternalHttpSender.java`
#### Snippet
```java
     * close
     *
     * @throws Exception
     */
    public void close() throws Exception {
```

### JavadocDeclaration
`@param bodies` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/http/InternalHttpSender.java`
#### Snippet
```java
     * send request by http
     *
     * @param bodies
     * @param groupId
     * @param streamId
```

### JavadocDeclaration
`@param groupId` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/http/InternalHttpSender.java`
#### Snippet
```java
     *
     * @param bodies
     * @param groupId
     * @param streamId
     * @param dt
```

### JavadocDeclaration
`@param streamId` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/http/InternalHttpSender.java`
#### Snippet
```java
     * @param bodies
     * @param groupId
     * @param streamId
     * @param dt
     * @param timeout
```

### JavadocDeclaration
`@param dt` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/http/InternalHttpSender.java`
#### Snippet
```java
     * @param groupId
     * @param streamId
     * @param dt
     * @param timeout
     * @param timeUnit
```

### JavadocDeclaration
`@param timeout` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/http/InternalHttpSender.java`
#### Snippet
```java
     * @param streamId
     * @param dt
     * @param timeout
     * @param timeUnit
     * @param hostInfo
```

### JavadocDeclaration
`@param timeUnit` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/http/InternalHttpSender.java`
#### Snippet
```java
     * @param dt
     * @param timeout
     * @param timeUnit
     * @param hostInfo
     * @return
```

### JavadocDeclaration
`@param hostInfo` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/http/InternalHttpSender.java`
#### Snippet
```java
     * @param timeout
     * @param timeUnit
     * @param hostInfo
     * @return
     *
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/http/InternalHttpSender.java`
#### Snippet
```java
     * @param timeUnit
     * @param hostInfo
     * @return
     *
     * @throws Exception
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/http/InternalHttpSender.java`
#### Snippet
```java
     * @return
     *
     * @throws Exception
     */
    private SendResult sendByHttp(List<String> bodies, String groupId, String streamId, long dt,
```

### JavadocDeclaration
`@param bodies` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/http/InternalHttpSender.java`
#### Snippet
```java
     * construct header
     *
     * @param bodies
     * @param groupId
     * @param streamId
```

### JavadocDeclaration
`@param groupId` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/http/InternalHttpSender.java`
#### Snippet
```java
     *
     * @param bodies
     * @param groupId
     * @param streamId
     * @param dt
```

### JavadocDeclaration
`@param streamId` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/http/InternalHttpSender.java`
#### Snippet
```java
     * @param bodies
     * @param groupId
     * @param streamId
     * @param dt
     * @return
```

### JavadocDeclaration
`@param dt` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/http/InternalHttpSender.java`
#### Snippet
```java
     * @param groupId
     * @param streamId
     * @param dt
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/http/InternalHttpSender.java`
#### Snippet
```java
     * @param streamId
     * @param dt
     * @return
     */
    private ArrayList<BasicNameValuePair> getHeaders(List<String> bodies,
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/utils/EncryptUtil.java`
#### Snippet
```java
     * @param publicKey public key
     * @param data data
     * @return
     *
     * @throws Exception exception
```

### JavadocDeclaration
`@param plainText` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/utils/EncryptUtil.java`
#### Snippet
```java
     * AES encrypt
     *
     * @param plainText
     * @param aesKey
     * @return
```

### JavadocDeclaration
`@param aesKey` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/utils/EncryptUtil.java`
#### Snippet
```java
     *
     * @param plainText
     * @param aesKey
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/utils/EncryptUtil.java`
#### Snippet
```java
     * @param plainText
     * @param aesKey
     * @return
     */
    public static byte[] aesEncrypt(byte[] plainText, byte[] aesKey) {
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/utils/EncryptUtil.java`
#### Snippet
```java
     * @return whether success
     *
     * @throws Exception
     */
    public static String loadPrivateKeyByFileText(String path) throws Exception {
```

### JavadocDeclaration
`@param cipherText` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/utils/EncryptUtil.java`
#### Snippet
```java
     * AES decrypt
     *
     * @param cipherText
     * @param aesKey
     * @return des decrypt
```

### JavadocDeclaration
`@param aesKey` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/utils/EncryptUtil.java`
#### Snippet
```java
     *
     * @param cipherText
     * @param aesKey
     * @return des decrypt
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/utils/EncryptUtil.java`
#### Snippet
```java
     * @param privateKey key
     * @param encryptedData data
     * @return
     *
     * @throws Exception exception
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/utils/EncryptUtil.java`
#### Snippet
```java
     * @param publicKey public key
     * @param cipherData cipher data
     * @return
     *
     * @throws Exception exception
```

### JavadocDeclaration
`@param groupId` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/HttpProxySender.java`
#### Snippet
```java
     *
     * @param bodies   list of bodies
     * @param groupId
     * @param streamId
     * @param dt
```

### JavadocDeclaration
`@param streamId` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/HttpProxySender.java`
#### Snippet
```java
     * @param bodies   list of bodies
     * @param groupId
     * @param streamId
     * @param dt
     * @param timeout
```

### JavadocDeclaration
`@param dt` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/HttpProxySender.java`
#### Snippet
```java
     * @param groupId
     * @param streamId
     * @param dt
     * @param timeout
     * @param timeUnit
```

### JavadocDeclaration
`@param timeout` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/HttpProxySender.java`
#### Snippet
```java
     * @param streamId
     * @param dt
     * @param timeout
     * @param timeUnit
     * @return
```

### JavadocDeclaration
`@param timeUnit` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/HttpProxySender.java`
#### Snippet
```java
     * @param dt
     * @param timeout
     * @param timeUnit
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/HttpProxySender.java`
#### Snippet
```java
     * @param timeout
     * @param timeUnit
     * @return
     */
    public SendResult sendMessage(List<String> bodies, String groupId, String streamId, long dt,
```

### JavadocDeclaration
`@param body` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/HttpProxySender.java`
#### Snippet
```java
     * send by http
     *
     * @param body
     * @param groupId
     * @param streamId
```

### JavadocDeclaration
`@param groupId` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/HttpProxySender.java`
#### Snippet
```java
     *
     * @param body
     * @param groupId
     * @param streamId
     * @param dt
```

### JavadocDeclaration
`@param streamId` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/HttpProxySender.java`
#### Snippet
```java
     * @param body
     * @param groupId
     * @param streamId
     * @param dt
     * @param timeout
```

### JavadocDeclaration
`@param dt` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/HttpProxySender.java`
#### Snippet
```java
     * @param groupId
     * @param streamId
     * @param dt
     * @param timeout
     * @param timeUnit
```

### JavadocDeclaration
`@param timeout` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/HttpProxySender.java`
#### Snippet
```java
     * @param streamId
     * @param dt
     * @param timeout
     * @param timeUnit
     * @return
```

### JavadocDeclaration
`@param timeUnit` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/HttpProxySender.java`
#### Snippet
```java
     * @param dt
     * @param timeout
     * @param timeUnit
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/HttpProxySender.java`
#### Snippet
```java
     * @param timeout
     * @param timeUnit
     * @return
     */
    public SendResult sendMessage(String body, String groupId, String streamId, long dt,
```

### JavadocDeclaration
`@param bodies` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/HttpProxySender.java`
#### Snippet
```java
     * async sender
     *
     * @param bodies
     * @param groupId
     * @param streamId
```

### JavadocDeclaration
`@param groupId` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/HttpProxySender.java`
#### Snippet
```java
     *
     * @param bodies
     * @param groupId
     * @param streamId
     * @param dt
```

### JavadocDeclaration
`@param streamId` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/HttpProxySender.java`
#### Snippet
```java
     * @param bodies
     * @param groupId
     * @param streamId
     * @param dt
     * @param timeout
```

### JavadocDeclaration
`@param dt` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/HttpProxySender.java`
#### Snippet
```java
     * @param groupId
     * @param streamId
     * @param dt
     * @param timeout
     * @param timeUnit
```

### JavadocDeclaration
`@param timeout` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/HttpProxySender.java`
#### Snippet
```java
     * @param streamId
     * @param dt
     * @param timeout
     * @param timeUnit
     * @param callback
```

### JavadocDeclaration
`@param timeUnit` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/HttpProxySender.java`
#### Snippet
```java
     * @param dt
     * @param timeout
     * @param timeUnit
     * @param callback
     */
```

### JavadocDeclaration
`@param callback` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/HttpProxySender.java`
#### Snippet
```java
     * @param timeout
     * @param timeUnit
     * @param callback
     */
    public void asyncSendMessage(List<String> bodies, String groupId, String streamId, long dt,
```

### JavadocDeclaration
`@param configure` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/HttpProxySender.java`
#### Snippet
```java
     * get proxy list
     *
     * @param configure
     * @throws Exception
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/HttpProxySender.java`
#### Snippet
```java
     *
     * @param configure
     * @throws Exception
     */
    private void initTDMClientAndRequest(ProxyClientConfig configure) throws Exception {
```

### JavadocDeclaration
`@param body` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/HttpProxySender.java`
#### Snippet
```java
     * async send single message.
     *
     * @param body
     * @param groupId
     * @param streamId
```

### JavadocDeclaration
`@param groupId` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/HttpProxySender.java`
#### Snippet
```java
     *
     * @param body
     * @param groupId
     * @param streamId
     * @param dt
```

### JavadocDeclaration
`@param streamId` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/HttpProxySender.java`
#### Snippet
```java
     * @param body
     * @param groupId
     * @param streamId
     * @param dt
     * @param timeout
```

### JavadocDeclaration
`@param dt` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/HttpProxySender.java`
#### Snippet
```java
     * @param groupId
     * @param streamId
     * @param dt
     * @param timeout
     * @param timeUnit
```

### JavadocDeclaration
`@param timeout` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/HttpProxySender.java`
#### Snippet
```java
     * @param streamId
     * @param dt
     * @param timeout
     * @param timeUnit
     * @param callback
```

### JavadocDeclaration
`@param timeUnit` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/HttpProxySender.java`
#### Snippet
```java
     * @param dt
     * @param timeout
     * @param timeUnit
     * @param callback
     */
```

### JavadocDeclaration
`@param callback` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/HttpProxySender.java`
#### Snippet
```java
     * @param timeout
     * @param timeUnit
     * @param callback
     */
    public void asyncSendMessage(String body, String groupId, String streamId, long dt,
```

### JavadocDeclaration
`@param channel` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/threads/TimeoutScanThread.java`
#### Snippet
```java
     * add timeout channel
     *
     * @param channel
     */
    public void addTimeoutChannel(Channel channel) {
```

### JavadocDeclaration
`@param channel` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/threads/TimeoutScanThread.java`
#### Snippet
```java
     * check message id
     *
     * @param channel
     * @param messageIdCallbacks
     */
```

### JavadocDeclaration
`@param messageIdCallbacks` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/threads/TimeoutScanThread.java`
#### Snippet
```java
     *
     * @param channel
     * @param messageIdCallbacks
     */
    private void checkMessageIdBasedCallbacks(Channel channel,
```

### JavadocDeclaration
`@param channel` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/threads/TimeoutScanThread.java`
#### Snippet
```java
     * reset channel timeout
     *
     * @param channel
     */
    public void resetTimeoutChannel(Channel channel) {
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sort/sort-core/src/main/java/org/apache/inlong/sort/function/EncryptFunction.java`
#### Snippet
```java
     *
     * @param plaintextBytes it is data to be encoded
     * @return
     */
    public static String encryptBASE64(byte[] plaintextBytes) throws Exception {
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sort/sort-core/src/main/java/org/apache/inlong/sort/function/EncryptFunction.java`
#### Snippet
```java
     * @param content it is content to be encrypted
     * @param key the key of encryption
     * @return
     */
    public static String encryptAES(String content, String key) {
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sort/sort-core/src/main/java/org/apache/inlong/sort/function/EncryptFunction.java`
#### Snippet
```java
     * @param data it is data to be encrypted
     * @param key  the key of encryption
     * @return
     */
    public static String encrypt3DES(String data, String key) {
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sort/sort-core/src/main/java/org/apache/inlong/sort/function/EncryptFunction.java`
#### Snippet
```java
     * @param key the key of encryption
     * @param method encryption algorithm
     * @return
     */
    public static String encrypt(String data, String key, String method) {
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sort/sort-core/src/main/java/org/apache/inlong/sort/function/EncryptFunction.java`
#### Snippet
```java
     * build 3DesKey
     * @param keyStr the key of encryption
     * @return
     */
    public static byte[] build3DesKey(String keyStr) {
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/config/ProxyConfigManager.java`
#### Snippet
```java
     *
     * @return proxyConfigEntry
     * @throws Exception
     */
    public ProxyConfigEntry getGroupIdConfigure() throws Exception {
```

### JavadocDeclaration
`@param proxyEntry` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/config/ProxyConfigManager.java`
#### Snippet
```java
     * compare proxy list
     *
     * @param proxyEntry
     */
    private void compareProxyList(ProxyConfigEntry proxyEntry) {
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/config/ProxyConfigManager.java`
#### Snippet
```java
     * try to read cache of proxy entry
     *
     * @return
     */
    private ProxyConfigEntry tryToReadCacheProxyEntry(String configCachePath) {
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/config/ProxyConfigManager.java`
#### Snippet
```java
     * request proxyHost list from manager, update ClientMgr.proxyHostList and channels
     *
     * @throws Exception
     */
    public void doProxyEntryQueryWork() throws Exception {
```

### JavadocDeclaration
`@param commonAttr` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/Sender.java`
#### Snippet
```java
     * whether is validate
     *
     * @param commonAttr
     * @param oldAttr
     * @return
```

### JavadocDeclaration
`@param oldAttr` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/Sender.java`
#### Snippet
```java
     *
     * @param commonAttr
     * @param oldAttr
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/Sender.java`
#### Snippet
```java
     * @param commonAttr
     * @param oldAttr
     * @return
     */
    private boolean isNotValidateAttr(String commonAttr, String oldAttr) {
```

### JavadocDeclaration
`@param client` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/Sender.java`
#### Snippet
```java
     * check whether clientChannel is idle; if idle, need send hb to keep alive
     *
     * @param client
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/Sender.java`
#### Snippet
```java
     *
     * @param client
     * @return
     */
    public boolean isIdleClient(NettyClient client) {
```

### JavadocDeclaration
`@param encodeObject` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/Sender.java`
#### Snippet
```java
     * sync send
     *
     * @param encodeObject
     * @param msgUUID
     * @param timeout
```

### JavadocDeclaration
`@param msgUUID` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/Sender.java`
#### Snippet
```java
     *
     * @param encodeObject
     * @param msgUUID
     * @param timeout
     * @param timeUnit
```

### JavadocDeclaration
`@param timeout` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/Sender.java`
#### Snippet
```java
     * @param encodeObject
     * @param msgUUID
     * @param timeout
     * @param timeUnit
     * @return
```

### JavadocDeclaration
`@param timeUnit` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/Sender.java`
#### Snippet
```java
     * @param msgUUID
     * @param timeout
     * @param timeUnit
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/Sender.java`
#### Snippet
```java
     * @param timeout
     * @param timeUnit
     * @return
     */
    public String syncSendMessageIndex(EncodeObject encodeObject, String msgUUID, long timeout, TimeUnit timeUnit) {
```

### JavadocDeclaration
`@param encodeObject` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/Sender.java`
#### Snippet
```java
     * Meanwhile, update this send channel timeout info(including increase or reset), according to the sendResult
     *
     * @param encodeObject
     * @param msgUUID
     * @param timeout
```

### JavadocDeclaration
`@param msgUUID` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/Sender.java`
#### Snippet
```java
     *
     * @param encodeObject
     * @param msgUUID
     * @param timeout
     * @param timeUnit
```

### JavadocDeclaration
`@param timeout` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/Sender.java`
#### Snippet
```java
     * @param encodeObject
     * @param msgUUID
     * @param timeout
     * @param timeUnit
     * @return
```

### JavadocDeclaration
`@param timeUnit` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/Sender.java`
#### Snippet
```java
     * @param msgUUID
     * @param timeout
     * @param timeUnit
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/Sender.java`
#### Snippet
```java
     * @param timeout
     * @param timeUnit
     * @return
     */
    public SendResult syncSendMessage(EncodeObject encodeObject, String msgUUID, long timeout, TimeUnit timeUnit) {
```

### JavadocDeclaration
`@param attr` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/Sender.java`
#### Snippet
```java
     * validate attribute
     *
     * @param attr
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/Sender.java`
#### Snippet
```java
     *
     * @param attr
     * @return
     */
    private boolean validAttribute(String attr) {
```

### JavadocDeclaration
`@param encodeObject` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/Sender.java`
#### Snippet
```java
     * async send message index
     *
     * @param encodeObject
     * @param callback
     * @param msgUUID
```

### JavadocDeclaration
`@param callback` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/Sender.java`
#### Snippet
```java
     *
     * @param encodeObject
     * @param callback
     * @param msgUUID
     * @param timeout
```

### JavadocDeclaration
`@param msgUUID` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/Sender.java`
#### Snippet
```java
     * @param encodeObject
     * @param callback
     * @param msgUUID
     * @param timeout
     * @param timeUnit
```

### JavadocDeclaration
`@param timeout` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/Sender.java`
#### Snippet
```java
     * @param callback
     * @param msgUUID
     * @param timeout
     * @param timeUnit
     * @throws ProxysdkException
```

### JavadocDeclaration
`@param timeUnit` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/Sender.java`
#### Snippet
```java
     * @param msgUUID
     * @param timeout
     * @param timeUnit
     * @throws ProxysdkException
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/Sender.java`
#### Snippet
```java
     * @param timeout
     * @param timeUnit
     * @throws ProxysdkException
     */
    public void asyncSendMessageIndex(EncodeObject encodeObject, FileCallback callback, String msgUUID, long timeout,
```

### JavadocDeclaration
`@param url` tag description is missing
in `inlong-sort/sort-flink/base/src/main/java/org/apache/inlong/sort/base/util/JdbcUrlUtils.java`
#### Snippet
```java
    /**
     * see https://su18.org/post/jdbc-connection-url-attack/
     * @param url
     * @return url after filtering out the invalid property
     */
```

### JavadocDeclaration
Exception is not declared to be thrown by method snapshotMetricStateForSourceSubMetricData
in `inlong-sort/sort-flink/base/src/main/java/org/apache/inlong/sort/base/util/MetricStateUtils.java`
#### Snippet
```java
     * @param subtaskIndex subtask index
     * @param metricState state of source metric data
     * @throws Exception throw exception when add sub metric state
     */
    private static void snapshotMetricStateForSourceSubMetricData(SourceMetricData sourceMetricData,
```

### JavadocDeclaration
`@param pattern` tag description is missing
in `inlong-sort/sort-flink/base/src/main/java/org/apache/inlong/sort/base/dirty/DirtySinkHelper.java`
#### Snippet
```java
     * replace ${SYSTEM_TIME} with real time
     *
     * @param pattern
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sort/sort-flink/base/src/main/java/org/apache/inlong/sort/base/dirty/DirtySinkHelper.java`
#### Snippet
```java
     *
     * @param pattern
     * @return
     */
    public static String regexReplace(String pattern, DirtyType dirtyType, String dirtyMessage) {
```

### JavadocDeclaration
`@param host` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/ClientMgr.java`
#### Snippet
```java
     * create conn, as DataConn or HBConn
     *
     * @param host
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/ClientMgr.java`
#### Snippet
```java
     *
     * @param host
     * @return
     */
    private boolean initConnection(HostInfo host) {
```

### JavadocDeclaration
`@param dataRecord` tag description is missing
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/util/RecordUtils.java`
#### Snippet
```java
    /**
     * Get table id from source record
     * @param dataRecord
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/util/RecordUtils.java`
#### Snippet
```java
     * Get table id from source record
     * @param dataRecord
     * @return
     */
    public static TableId getTableId(SourceRecord dataRecord) {
```

### JavadocDeclaration
`@param source` tag description is missing
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/util/RecordUtils.java`
#### Snippet
```java
    /**
     * Whether the source belong Mysql Connector
     * @param source
     * @return true if the source belong Mysql Connector
     */
```

### JavadocDeclaration
`@param sourceRecord` tag description is missing
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/util/RecordUtils.java`
#### Snippet
```java
    /**
     * Whether the source Record is a schema change event.
     * @param sourceRecord
     * @return ture if the source Record is a schema change event.
     */
```

### JavadocDeclaration
`@param column` tag description is missing
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/util/RecordUtils.java`
#### Snippet
```java
     * According column's type, get it's logicalType
     * Refer: https://ververica.github.io/flink-cdc-connectors/master/content/connectors/oracle-cdc.html?from_wecom=1
     * @param column
     * @param struct
     * @return
```

### JavadocDeclaration
`@param struct` tag description is missing
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/util/RecordUtils.java`
#### Snippet
```java
     * Refer: https://ververica.github.io/flink-cdc-connectors/master/content/connectors/oracle-cdc.html?from_wecom=1
     * @param column
     * @param struct
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/util/RecordUtils.java`
#### Snippet
```java
     * @param column
     * @param struct
     * @return
     */
    public static LogicalType convertLogicType(Column column, Struct struct) {
```

### JavadocDeclaration
`@param fieldValue` tag description is missing
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/debezium/table/RowDataDebeziumDeserializeSchema.java`
#### Snippet
```java
     * extract the data with the format provided by debezium
     *
     * @param fieldValue
     * @param schemaName
     * @return the extracted data with schema
```

### JavadocDeclaration
`@param schemaName` tag description is missing
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/debezium/table/RowDataDebeziumDeserializeSchema.java`
#### Snippet
```java
     *
     * @param fieldValue
     * @param schemaName
     * @return the extracted data with schema
     */
```

### JavadocDeclaration
`@param jdbcField` tag description is missing
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/jdbc/src/main/java/org/apache/inlong/sort/jdbc/converter/AbstractJdbcRowConverter.java`
#### Snippet
```java
         * Convert a jdbc field object of {@link ResultSet} to the internal data structure object.
         *
         * @param jdbcField
         */
        Object deserialize(Object jdbcField) throws SQLException;
```

### JavadocDeclaration
`@param record` tag description is missing
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/jdbc/src/main/java/org/apache/inlong/sort/jdbc/internal/JdbcBatchingOutputFormat.java`
#### Snippet
```java
    /**
     * update before is used for metric computing only
     * @param record
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/jdbc/src/main/java/org/apache/inlong/sort/jdbc/internal/JdbcBatchingOutputFormat.java`
#### Snippet
```java
     * update before is used for metric computing only
     * @param record
     * @return
     */
    private boolean isValidRowKind(In record) {
```

### JavadocDeclaration
`@param props` tag description is missing
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kafka/src/main/java/org/apache/inlong/sort/kafka/FlinkKafkaConsumer.java`
#### Snippet
```java
     * @param deserializer The de-/serializer used to convert between Kafka's byte messages and
     *         Flink's objects.
     * @param props
     */
    public FlinkKafkaConsumer(
```

### JavadocDeclaration
`@param props` tag description is missing
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kafka/src/main/java/org/apache/inlong/sort/kafka/FlinkKafkaConsumer.java`
#### Snippet
```java
     * @param valueDeserializer The de-/serializer used to convert between Kafka's byte messages and
     *         Flink's objects.
     * @param props
     */
    public FlinkKafkaConsumer(
```

### JavadocDeclaration
`@param props` tag description is missing
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kafka/src/main/java/org/apache/inlong/sort/kafka/FlinkKafkaConsumer.java`
#### Snippet
```java
     * @param deserializer The keyed de-/serializer used to convert between Kafka's byte messages
     *         and Flink's objects.
     * @param props
     */
    public FlinkKafkaConsumer(
```

### JavadocDeclaration
`@param props` tag description is missing
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kafka/src/main/java/org/apache/inlong/sort/kafka/FlinkKafkaConsumer.java`
#### Snippet
```java
     * @param valueDeserializer The de-/serializer used to convert between Kafka's byte messages and
     *         Flink's objects.
     * @param props
     */
    public FlinkKafkaConsumer(
```

### JavadocDeclaration
`@param props` tag description is missing
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kafka/src/main/java/org/apache/inlong/sort/kafka/FlinkKafkaConsumer.java`
#### Snippet
```java
     * @param deserializer The keyed de-/serializer used to convert between Kafka's byte messages
     *         and Flink's objects.
     * @param props
     */
    public FlinkKafkaConsumer(
```

### JavadocDeclaration
`@param props` tag description is missing
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kafka/src/main/java/org/apache/inlong/sort/kafka/FlinkKafkaConsumer.java`
#### Snippet
```java
     * @param deserializer The keyed de-/serializer used to convert between Kafka's byte messages
     *         and Flink's objects.
     * @param props
     */
    public FlinkKafkaConsumer(
```

### JavadocDeclaration
`@param values` tag description is missing
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/doris/src/main/java/org/apache/inlong/sort/doris/table/DorisDynamicSchemaOutputFormat.java`
#### Snippet
```java
     * format data to csv or json
     *
     * @param values
     * @param format
     * @return string
```

### JavadocDeclaration
`@param format` tag description is missing
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/doris/src/main/java/org/apache/inlong/sort/doris/table/DorisDynamicSchemaOutputFormat.java`
#### Snippet
```java
     *
     * @param values
     * @param format
     * @return string
     * @throws JsonProcessingException
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/doris/src/main/java/org/apache/inlong/sort/doris/table/DorisDynamicSchemaOutputFormat.java`
#### Snippet
```java
     * @param format
     * @return string
     * @throws JsonProcessingException
     */
    private String serialize(String tableIdentifier, List values, String format) throws JsonProcessingException {
```

### JavadocDeclaration
`@param data` tag description is missing
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/redis/src/main/java/org/apache/inlong/sort/redis/common/mapper/RedisMapper.java`
#### Snippet
```java
     * The default implementation returns an empty Optional.
     *
     * @param data
     * @return Optional
     */
```

### JavadocDeclaration
`@param data` tag description is missing
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/redis/src/main/java/org/apache/inlong/sort/redis/common/mapper/RedisMapper.java`
#### Snippet
```java
     * The default implementation returns an empty Optional.
     *
     * @param data
     * @return Optional
     */
```

### JavadocDeclaration
`@param transaction` tag description is missing
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kafka/src/main/java/org/apache/inlong/sort/kafka/FlinkKafkaProducer.java`
#### Snippet
```java
     * Flush pending records.
     *
     * @param transaction
     */
    private void flush(FlinkKafkaProducer.KafkaTransactionState transaction)
```

### JavadocDeclaration
PulsarAdminException is not declared to be thrown by method getTopicPartitions
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/pulsar/src/main/java/org/apache/inlong/sort/pulsar/internal/PulsarMetadataReader.java`
#### Snippet
```java
     *
     * @return allTopicPartitions
     * @throws PulsarAdminException pulsarAdminException
     */
    public Set<String> getTopicPartitions() throws PulsarClientException {
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/pulsar/src/main/java/org/apache/inlong/sort/pulsar/internal/PulsarMetadataReader.java`
#### Snippet
```java
     *
     * @return set of topic ranges this subTask should consume
     * @throws PulsarAdminException
     */
    public Set<TopicRange> getTopicPartitionRanges() throws PulsarClientException {
```

### JavadocDeclaration
PulsarAdminException is not declared to be thrown by method getTopicPartitionRanges
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/pulsar/src/main/java/org/apache/inlong/sort/pulsar/internal/PulsarMetadataReader.java`
#### Snippet
```java
     *
     * @return set of topic ranges this subTask should consume
     * @throws PulsarAdminException
     */
    public Set<TopicRange> getTopicPartitionRanges() throws PulsarClientException {
```

### JavadocDeclaration
`@param oldSchema` tag description is missing
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/iceberg/src/main/java/org/apache/inlong/sort/iceberg/sink/multiple/SchemaChangeUtils.java`
#### Snippet
```java
     * TODO: currently only support add column
     *
     * @param oldSchema
     * @param newSchema
     * @return
```

### JavadocDeclaration
`@param newSchema` tag description is missing
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/iceberg/src/main/java/org/apache/inlong/sort/iceberg/sink/multiple/SchemaChangeUtils.java`
#### Snippet
```java
     *
     * @param oldSchema
     * @param newSchema
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/iceberg/src/main/java/org/apache/inlong/sort/iceberg/sink/multiple/SchemaChangeUtils.java`
#### Snippet
```java
     * @param oldSchema
     * @param newSchema
     * @return
     */
    static List<TableChange> diffSchema(Schema oldSchema, Schema newSchema) {
```

### JavadocDeclaration
Wrong tag `note`
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/iceberg/src/main/java/org/apache/inlong/sort/iceberg/sink/collections/RocksDBKVBuffer.java`
#### Snippet
```java
     * Perform a range search according to the prefix of the key, and the KV data of all keys matching the
     * prefix will be returned
     * @note This stream must be closed after use, otherwise it will cause a memory leak
     *
     * @param keyPrefix key prefix binary data
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/iceberg/src/main/java/org/apache/inlong/sort/iceberg/sink/collections/RocksDBKVBuffer.java`
#### Snippet
```java
     *
     * @param keyPrefix key prefix binary data
     * @return
     */
    @Override
```

### JavadocDeclaration
`@param key` tag description is missing
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/iceberg/src/main/java/org/apache/inlong/sort/iceberg/sink/collections/KVBuffer.java`
#### Snippet
```java
     * is removed.  (The buffer can contain at most one such mapping.)
     *
     * @param key
     * @return the previous value associated with <tt>key</tt>, or
     *         <tt>null</tt> if there was no mapping for <tt>key</tt>.
```

### JavadocDeclaration
`@param key` tag description is missing
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/iceberg/src/main/java/org/apache/inlong/sort/iceberg/sink/collections/KVBuffer.java`
#### Snippet
```java
     * or {@code null} if this buffer contains no mapping for the key.
     *
     * @param key
     * @return the value to which the specified key is mapped
     */
```

### JavadocDeclaration
`@param input` tag description is missing
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/iceberg/src/main/java/org/apache/inlong/sort/iceberg/sink/FlinkSink.java`
#### Snippet
```java
         * subtask (rather than processed multiple times).
         *
         * @param input
         * @return
         */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/iceberg/src/main/java/org/apache/inlong/sort/iceberg/sink/FlinkSink.java`
#### Snippet
```java
         *
         * @param input
         * @return
         */
        private DataStream<RowData> appendWithMiniBatchGroup(DataStream<RowData> input,
```

### JavadocDeclaration
`@param inlongMetric` tag description is missing
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/iceberg/src/main/java/org/apache/inlong/sort/iceberg/sink/FlinkSink.java`
#### Snippet
```java
        /**
         * Add metric output for iceberg writer
         * @param inlongMetric
         * @param auditHostAndPorts
         * @return
```

### JavadocDeclaration
`@param auditHostAndPorts` tag description is missing
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/iceberg/src/main/java/org/apache/inlong/sort/iceberg/sink/FlinkSink.java`
#### Snippet
```java
         * Add metric output for iceberg writer
         * @param inlongMetric
         * @param auditHostAndPorts
         * @return
         */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/iceberg/src/main/java/org/apache/inlong/sort/iceberg/sink/FlinkSink.java`
#### Snippet
```java
         * @param inlongMetric
         * @param auditHostAndPorts
         * @return
         */
        public Builder metric(String inlongMetric, String auditHostAndPorts) {
```

### JavadocDeclaration
Wrong tag `note`
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/iceberg/src/main/java/org/apache/inlong/sort/iceberg/sink/collections/RocksDBDAO.java`
#### Snippet
```java
    /**
     * Perform a prefix search and return stream of key-value pairs retrieved.
     * @note This stream must be closed after use, otherwise it will cause a memory leak
     *
     * @param columnFamilyName Column Family Name
```

### JavadocDeclaration
`@param splitToRead` tag description is missing
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/mysql/debezium/reader/DebeziumReader.java`
#### Snippet
```java
     * Add to split to read, this should call only the when reader is idle.
     *
     * @param splitToRead
     */
    void submitSplit(Split splitToRead);
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/oracle-cdc/src/main/java/io/debezium/connector/oracle/logminer/LogMinerQueryBuilder.java`
#### Snippet
```java
     * @param columnName the column in which the reg-ex patterns are to be applied against
     * @param inclusion should be {@code true} when passing inclusion patterns, {@code false} otherwise
     * @return
     */
    private static String listOfPatternsToSql(List<Pattern> patterns, String columnName, boolean inclusion) {
```

### JavadocDeclaration
`@param fieldValue` tag description is missing
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/oracle-cdc/src/main/java/org/apache/inlong/sort/cdc/oracle/debezium/table/RowDataDebeziumDeserializeSchema.java`
#### Snippet
```java
     * extract the data with the format provided by debezium
     *
     * @param fieldValue
     * @param schemaName
     * @return the extracted data with schema
```

### JavadocDeclaration
`@param schemaName` tag description is missing
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/oracle-cdc/src/main/java/org/apache/inlong/sort/cdc/oracle/debezium/table/RowDataDebeziumDeserializeSchema.java`
#### Snippet
```java
     *
     * @param fieldValue
     * @param schemaName
     * @return the extracted data with schema
     */
```

### JavadocDeclaration
`@param dataRecord` tag description is missing
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mongodb-cdc/src/main/java/org/apache/inlong/sort/cdc/mongodb/debezium/utils/RecordUtils.java`
#### Snippet
```java
    /**
     * Get table id from source record
     * @param dataRecord
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mongodb-cdc/src/main/java/org/apache/inlong/sort/cdc/mongodb/debezium/utils/RecordUtils.java`
#### Snippet
```java
     * Get table id from source record
     * @param dataRecord
     * @return
     */
    public static TableId getTableId(SourceRecord dataRecord) {
```

### JavadocDeclaration
`@param typeString` tag description is missing
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/postgres-cdc/src/main/java/org/apache/inlong/sort/cdc/postgres/table/PostgreSQLDataType.java`
#### Snippet
```java
     * convert string type to PostgreSQLDataType instance
     *
     * @param typeString
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/postgres-cdc/src/main/java/org/apache/inlong/sort/cdc/postgres/table/PostgreSQLDataType.java`
#### Snippet
```java
     *
     * @param typeString
     * @return
     */
    public static PostgreSQLDataType fromString(String typeString) {
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/postgres-cdc/src/main/java/org/apache/inlong/sort/cdc/postgres/manager/PostgreSQLQueryVisitor.java`
#### Snippet
```java
     * query PostgreSQL server version
     *
     * @return
     */
    public String getPostgreSQLVersion() {
```

### JavadocDeclaration
`@param query` tag description is missing
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/postgres-cdc/src/main/java/org/apache/inlong/sort/cdc/postgres/manager/PostgreSQLQueryVisitor.java`
#### Snippet
```java
     * execute sql and return result set
     *
     * @param query
     * @param args
     * @return
```

### JavadocDeclaration
`@param args` tag description is missing
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/postgres-cdc/src/main/java/org/apache/inlong/sort/cdc/postgres/manager/PostgreSQLQueryVisitor.java`
#### Snippet
```java
     *
     * @param query
     * @param args
     * @return
     * @throws ClassNotFoundException
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/postgres-cdc/src/main/java/org/apache/inlong/sort/cdc/postgres/manager/PostgreSQLQueryVisitor.java`
#### Snippet
```java
     * @param query
     * @param args
     * @return
     * @throws ClassNotFoundException
     * @throws SQLException
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/postgres-cdc/src/main/java/org/apache/inlong/sort/cdc/postgres/manager/PostgreSQLQueryVisitor.java`
#### Snippet
```java
     * @param args
     * @return
     * @throws ClassNotFoundException
     * @throws SQLException
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/postgres-cdc/src/main/java/org/apache/inlong/sort/cdc/postgres/manager/PostgreSQLQueryVisitor.java`
#### Snippet
```java
     * @return
     * @throws ClassNotFoundException
     * @throws SQLException
     */
    private List<Map<String, Object>> executeQuery(String query, String... args)
```

### JavadocDeclaration
`@param schema` tag description is missing
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/postgres-cdc/src/main/java/org/apache/inlong/sort/cdc/postgres/manager/PostgreSQLQueryVisitor.java`
#### Snippet
```java
     * query PostgreSQL table meta data, such as column name, data type, constraint type, etc.
     *
     * @param schema
     * @param table
     * @return
```

### JavadocDeclaration
`@param table` tag description is missing
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/postgres-cdc/src/main/java/org/apache/inlong/sort/cdc/postgres/manager/PostgreSQLQueryVisitor.java`
#### Snippet
```java
     *
     * @param schema
     * @param table
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/postgres-cdc/src/main/java/org/apache/inlong/sort/cdc/postgres/manager/PostgreSQLQueryVisitor.java`
#### Snippet
```java
     * @param schema
     * @param table
     * @return
     */
    public List<Map<String, Object>> getTableColumnsMetaData(String schema, String table) {
```

### JavadocDeclaration
`@param schema` tag description is missing
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/postgres-cdc/src/main/java/org/apache/inlong/sort/cdc/postgres/manager/PostgreSQLQueryVisitor.java`
#### Snippet
```java
     * get a map which key is column name and value is {@link PostgreSQLDataType}
     *
     * @param schema
     * @param table
     * @return
```

### JavadocDeclaration
`@param table` tag description is missing
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/postgres-cdc/src/main/java/org/apache/inlong/sort/cdc/postgres/manager/PostgreSQLQueryVisitor.java`
#### Snippet
```java
     *
     * @param schema
     * @param table
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/postgres-cdc/src/main/java/org/apache/inlong/sort/cdc/postgres/manager/PostgreSQLQueryVisitor.java`
#### Snippet
```java
     * @param schema
     * @param table
     * @return
     */
    public Map<String, PostgreSQLDataType> getFieldMapping(String schema, String table) {
```

### JavadocDeclaration
`@param sql` tag description is missing
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/postgres-cdc/src/main/java/org/apache/inlong/sort/cdc/postgres/manager/PostgreSQLQueryVisitor.java`
#### Snippet
```java
     * execute sql query and return result count
     *
     * @param sql
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/postgres-cdc/src/main/java/org/apache/inlong/sort/cdc/postgres/manager/PostgreSQLQueryVisitor.java`
#### Snippet
```java
     *
     * @param sql
     * @return
     */
    public Long getQueryCount(String sql) {
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/postgres-cdc/src/main/java/org/apache/inlong/sort/cdc/postgres/table/PostgreSQLReadableMetaData.java`
#### Snippet
```java
     * @param tableSchema table schema
     * @param rowData row data with canal json or debezium json
     * @return
     */
    private static StringData getCanalData(SourceRecord record, TableChange tableSchema, GenericRowData rowData) {
```

### JavadocDeclaration
`@param auditHostAndPorts` tag description is missing
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/elasticsearch-6/src/main/java/org/apache/inlong/sort/elasticsearch6/ElasticsearchSink.java`
#### Snippet
```java
         * set auditHostAndPorts for reporting metrics
         *
         * @param auditHostAndPorts
         */
        public void setAuditHostAndPorts(String auditHostAndPorts) {
```

### JavadocDeclaration
`@param inlongMetric` tag description is missing
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/elasticsearch-6/src/main/java/org/apache/inlong/sort/elasticsearch6/ElasticsearchSink.java`
#### Snippet
```java
         * set InLongMetric for reporting metrics
         *
         * @param inlongMetric
         */
        public void setInLongMetric(String inlongMetric) {
```

### JavadocDeclaration
`@param auditHostAndPorts` tag description is missing
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/elasticsearch-7/src/main/java/org/apache/inlong/sort/elasticsearch7/ElasticsearchSink.java`
#### Snippet
```java
         * Set audit hosts and ports
         *
         * @param auditHostAndPorts
         */
        public void setAuditHostAndPorts(String auditHostAndPorts) {
```

### JavadocDeclaration
`@param inlongMetric` tag description is missing
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/elasticsearch-7/src/main/java/org/apache/inlong/sort/elasticsearch7/ElasticsearchSink.java`
#### Snippet
```java
         * set InLongMetric for reporting metrics
         *
         * @param inlongMetric
         */
        public void setInLongMetric(String inlongMetric) {
```

### JavadocDeclaration
Class/method reference, quoted text, or HTML link are expected after @see tag
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/format/RawFormat.java`
#### Snippet
```java
 * The Raw format
 *
 * @see <a herf="https://nightlies.apache.org/flink/flink-docs-release-1.13/zh/docs/connectors/table/formats/raw/">
 *         Raw Format</a>
 */
```

### JavadocDeclaration
Class/method reference, quoted text, or HTML link are expected after @see tag
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/format/JsonFormat.java`
#### Snippet
```java
 * The Json format
 *
 * @see <a herf="https://nightlies.apache.org/flink/flink-docs-release-1.13/zh/docs/connectors/table/formats/json/">
 *         Json Format</a>
 */
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-agent/agent-core/src/main/java/org/apache/inlong/agent/core/conf/ConfigServlet.java`
#### Snippet
```java
     * @param response HttpServletResponse
     * @param result ResponseResult
     * @throws IOException
     */
    public void responseToJson(HttpServletResponse response,
```

### JavadocDeclaration
`@param jobInstancId` tag description is missing
in `inlong-agent/agent-core/src/main/java/org/apache/inlong/agent/core/job/JobManager.java`
#### Snippet
```java
     * delete job profile and stop job thread
     *
     * @param jobInstancId
     */
    public boolean deleteJob(String jobInstancId, boolean isFrozen) {
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-agent/agent-core/src/main/java/org/apache/inlong/agent/core/job/JobWrapper.java`
#### Snippet
```java
     *
     * @param predicate a function to test whether running jobs met some conditions
     * @return
     */
    public boolean exist(Predicate<Collection<Task>> predicate) {
```

### JavadocDeclaration
`@param patternStrs` tag description is missing
in `inlong-agent/agent-common/src/main/java/org/apache/inlong/agent/utils/PathUtils.java`
#### Snippet
```java
     * </ul>
     *
     * @param patternStrs
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-agent/agent-common/src/main/java/org/apache/inlong/agent/utils/PathUtils.java`
#### Snippet
```java
     *
     * @param patternStrs
     * @return
     */
    public static Set<String> findCommonRootPath(Collection<String> patternStrs) {
```

### JavadocDeclaration
`@param patternStr` tag description is missing
in `inlong-agent/agent-common/src/main/java/org/apache/inlong/agent/utils/PathUtils.java`
#### Snippet
```java
     * </ul>
     *
     * @param patternStr
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-agent/agent-common/src/main/java/org/apache/inlong/agent/utils/PathUtils.java`
#### Snippet
```java
     *
     * @param patternStr
     * @return
     */
    public static String findRootPath(String patternStr) {
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-agent/agent-common/src/main/java/org/apache/inlong/agent/common/Service.java`
#### Snippet
```java
     * start service
     *
     * @throws Exception
     */
    void start() throws Exception;
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-agent/agent-common/src/main/java/org/apache/inlong/agent/common/Service.java`
#### Snippet
```java
     * stop service
     *
     * @throws Exception
     */
    void stop() throws Exception;
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-agent/agent-common/src/main/java/org/apache/inlong/agent/common/Service.java`
#### Snippet
```java
     * join and wait until getting signal
     *
     * @throws Exception
     */
    void join() throws Exception;
```

### JavadocDeclaration
`@param uniqueName` tag description is missing
in `inlong-agent/agent-common/src/main/java/org/apache/inlong/agent/common/AgentThreadFactory.java`
#### Snippet
```java
     * Replace a unique name for thread factory created thread, replace this name for placeholder
     *
     * @param uniqueName
     */
    public static void nameThread(String uniqueName) {
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-agent/agent-common/src/main/java/org/apache/inlong/agent/plugin/Trigger.java`
#### Snippet
```java
     * init trigger by trigger profile
     *
     * @throws IOException
     */
    void init(TriggerProfile profile) throws IOException;
```

### JavadocDeclaration
`@param sourceFileName` tag description is missing
in `inlong-agent/agent-common/src/main/java/org/apache/inlong/agent/plugin/Sink.java`
#### Snippet
```java
     * set source file name where the message is generated
     *
     * @param sourceFileName
     */
    void setSourceName(String sourceFileName);
```

### JavadocDeclaration
`@param name` tag description is missing
in `inlong-agent/agent-common/src/main/java/org/apache/inlong/agent/metrics/AgentMetricItemSet.java`
#### Snippet
```java
     * Constructor
     *
     * @param name
     */
    public AgentMetricItemSet(String name) {
```

### JavadocDeclaration
Wrong tag `Deprecated`
in `inlong-agent/agent-common/src/main/java/org/apache/inlong/agent/db/JobProfileDb.java`
#### Snippet
```java
     * get all restart jobs from db
     *
     * @Deprecated Use {@link JobProfileDb#getJobsByState(Set)}
     */
    @Deprecated
```

### JavadocDeclaration
`@param jobProfile` tag description is missing
in `inlong-agent/agent-common/src/main/java/org/apache/inlong/agent/db/JobProfileDb.java`
#### Snippet
```java
     * update job profile
     *
     * @param jobProfile
     */
    public void updateJobProfile(JobProfile jobProfile) {
```

### JavadocDeclaration
Wrong tag `Deprecated`
in `inlong-agent/agent-common/src/main/java/org/apache/inlong/agent/db/JobProfileDb.java`
#### Snippet
```java
     * @param stateSearchKey state search key.
     * @return list of job profile.
     * @Deprecated Use {@link JobProfileDb#getJobsByState(Set)}
     */
    @Deprecated
```

### JavadocDeclaration
`@param batchMessageCallBack` tag description is missing
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/sinks/SenderManager.java`
#### Snippet
```java
     * put the data into resend queue and will be resent later.
     *
     * @param batchMessageCallBack
     */
    private void putInResendQueue(AgentSenderCallback batchMessageCallBack) {
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/sources/reader/AbstractReader.java`
#### Snippet
```java
     * @param file specific offset file
     * @param pos specific offset pos
     * @return
     */
    public String serializeOffset(final String server, final String file,
```

### JavadocDeclaration
`@param jobConf` tag description is missing
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/sources/reader/MqttReader.java`
#### Snippet
```java
     * set global params value
     *
     * @param jobConf
     */
    private void setGlobalParamsValue(JobProfile jobConf) {
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/sources/snapshot/SnapshotBase.java`
#### Snippet
```java
    /**
     * get snapshot of the job
     * @return
     */
    String getSnapshot();
```

### JavadocDeclaration
`@param maxNum` tag description is missing
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/trigger/PathPattern.java`
#### Snippet
```java
     * Research all children files with {@link PathPattern#rootDir} matched whiteList and filtered by blackList.
     *
     * @param maxNum
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/trigger/PathPattern.java`
#### Snippet
```java
     *
     * @param maxNum
     * @return
     */
    public Collection<File> walkSuitableFiles(int maxNum) {
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-audit/audit-sdk/src/main/java/org/apache/inlong/audit/send/SenderChannel.java`
#### Snippet
```java
     * connect channel
     *
     * @return
     */
    public boolean connect() {
```

### JavadocDeclaration
`@param ipPort` tag description is missing
in `inlong-audit/audit-sdk/src/main/java/org/apache/inlong/audit/send/SenderChannel.java`
#### Snippet
```java
     * Constructor
     *
     * @param ipPort
     */
    public SenderChannel(IpPort ipPort, int maxSynchRequest, SenderManager senderManager) {
```

### JavadocDeclaration
`@param channel` tag description is missing
in `inlong-audit/audit-sdk/src/main/java/org/apache/inlong/audit/send/SenderChannel.java`
#### Snippet
```java
     * check channeel connect
     *
     * @param channel
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-audit/audit-sdk/src/main/java/org/apache/inlong/audit/send/SenderChannel.java`
#### Snippet
```java
     *
     * @param channel
     * @return
     */
    private boolean checkConnect(Channel channel) {
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-audit/audit-sdk/src/main/java/org/apache/inlong/audit/send/SenderChannel.java`
#### Snippet
```java
     * Try acquire channel
     *
     * @return
     */
    public boolean tryAcquire() {
```

### JavadocDeclaration
`@param ipPort` tag description is missing
in `inlong-audit/audit-sdk/src/main/java/org/apache/inlong/audit/util/SenderResult.java`
#### Snippet
```java
     * Constructor
     *
     * @param ipPort
     * @param result
     */
```

### JavadocDeclaration
`@param result` tag description is missing
in `inlong-audit/audit-sdk/src/main/java/org/apache/inlong/audit/util/SenderResult.java`
#### Snippet
```java
     *
     * @param ipPort
     * @param result
     */
    public SenderResult(IpPort ipPort, boolean result) {
```

### JavadocDeclaration
`@param sendIp` tag description is missing
in `inlong-audit/audit-sdk/src/main/java/org/apache/inlong/audit/util/SenderResult.java`
#### Snippet
```java
     * Constructor
     *
     * @param sendIp
     * @param sendPort
     * @param result
```

### JavadocDeclaration
`@param sendPort` tag description is missing
in `inlong-audit/audit-sdk/src/main/java/org/apache/inlong/audit/util/SenderResult.java`
#### Snippet
```java
     *
     * @param sendIp
     * @param sendPort
     * @param result
     */
```

### JavadocDeclaration
`@param result` tag description is missing
in `inlong-audit/audit-sdk/src/main/java/org/apache/inlong/audit/util/SenderResult.java`
#### Snippet
```java
     * @param sendIp
     * @param sendPort
     * @param result
     */
    public SenderResult(String sendIp, int sendPort, boolean result) {
```

### JavadocDeclaration
`@param dataBuf` tag description is missing
in `inlong-audit/audit-sdk/src/main/java/org/apache/inlong/audit/send/SenderGroup.java`
#### Snippet
```java
     * send data
     *
     * @param dataBuf
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-audit/audit-sdk/src/main/java/org/apache/inlong/audit/send/SenderGroup.java`
#### Snippet
```java
     *
     * @param dataBuf
     * @return
     */
    public SenderResult send(ByteBuf dataBuf) {
```

### JavadocDeclaration
`@param senderManager` tag description is missing
in `inlong-audit/audit-sdk/src/main/java/org/apache/inlong/audit/send/SenderGroup.java`
#### Snippet
```java
     * constructor
     *
     * @param senderManager
     */
    public SenderGroup(SenderManager senderManager) {
```

### JavadocDeclaration
`@param ipLists` tag description is missing
in `inlong-audit/audit-sdk/src/main/java/org/apache/inlong/audit/send/SenderGroup.java`
#### Snippet
```java
     * update config
     *
     * @param ipLists
     */
    public void updateConfig(Set<String> ipLists) {
```

### JavadocDeclaration
`@param channel` tag description is missing
in `inlong-audit/audit-sdk/src/main/java/org/apache/inlong/audit/util/IpPort.java`
#### Snippet
```java
     * parse InetSocketAddress
     *
     * @param channel
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-audit/audit-sdk/src/main/java/org/apache/inlong/audit/util/IpPort.java`
#### Snippet
```java
     *
     * @param channel
     * @return
     */
    public static InetSocketAddress parseInetSocketAddress(Channel channel) {
```

### JavadocDeclaration
`@param ip` tag description is missing
in `inlong-audit/audit-sdk/src/main/java/org/apache/inlong/audit/util/IpPort.java`
#### Snippet
```java
     * Constructor
     *
     * @param ip
     * @param port
     */
```

### JavadocDeclaration
`@param port` tag description is missing
in `inlong-audit/audit-sdk/src/main/java/org/apache/inlong/audit/util/IpPort.java`
#### Snippet
```java
     *
     * @param ip
     * @param port
     */
    public IpPort(String ip, int port) {
```

### JavadocDeclaration
`@param ip` tag description is missing
in `inlong-audit/audit-sdk/src/main/java/org/apache/inlong/audit/util/IpPort.java`
#### Snippet
```java
     * get IpPort by key
     *
     * @param ip
     * @param port
     * @return
```

### JavadocDeclaration
`@param port` tag description is missing
in `inlong-audit/audit-sdk/src/main/java/org/apache/inlong/audit/util/IpPort.java`
#### Snippet
```java
     *
     * @param ip
     * @param port
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-audit/audit-sdk/src/main/java/org/apache/inlong/audit/util/IpPort.java`
#### Snippet
```java
     * @param ip
     * @param port
     * @return
     */
    public static String getIpPortKey(String ip, int port) {
```

### JavadocDeclaration
`@param addr` tag description is missing
in `inlong-audit/audit-sdk/src/main/java/org/apache/inlong/audit/util/IpPort.java`
#### Snippet
```java
     * Constructor
     *
     * @param addr
     */
    public IpPort(InetSocketAddress addr) {
```

### JavadocDeclaration
`@param ipPort` tag description is missing
in `inlong-audit/audit-sdk/src/main/java/org/apache/inlong/audit/util/IpPort.java`
#### Snippet
```java
     * parse sIpPort
     *
     * @param ipPort
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-audit/audit-sdk/src/main/java/org/apache/inlong/audit/util/IpPort.java`
#### Snippet
```java
     *
     * @param ipPort
     * @return
     */
    public static IpPort parseIpPort(String ipPort) {
```

### JavadocDeclaration
`@param url` tag description is missing
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/pulsar/CreatePulsarClientCallBack.java`
#### Snippet
```java
    /**
     * call after create pulsar client success
     * @param url
     */
    void handleCreateClientSuccess(String url);
```

### JavadocDeclaration
`@param url` tag description is missing
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/pulsar/CreatePulsarClientCallBack.java`
#### Snippet
```java
    /**
     * call after create pulsar client has exception
     * @param url
     */
    void handleCreateClientException(String url);
```

### JavadocDeclaration
`@param future` tag description is missing
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/utils/EventLoopUtil.java`
#### Snippet
```java
     * get CompletableFuture by Future
     *
     * @param future
     * @return the CompletableFuture
     */
```

### JavadocDeclaration
`@param source` tag description is missing
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/source/ServerMessageFactory.java`
#### Snippet
```java
     * get server factory
     *
     * @param source
     * @param allChannels
     * @param serviceDecoder
```

### JavadocDeclaration
`@param allChannels` tag description is missing
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/source/ServerMessageFactory.java`
#### Snippet
```java
     *
     * @param source
     * @param allChannels
     * @param serviceDecoder
     * @param messageHandlerName
```

### JavadocDeclaration
`@param serviceDecoder` tag description is missing
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/source/ServerMessageFactory.java`
#### Snippet
```java
     * @param source
     * @param allChannels
     * @param serviceDecoder
     * @param messageHandlerName
     * @param maxMsgLength
```

### JavadocDeclaration
`@param messageHandlerName` tag description is missing
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/source/ServerMessageFactory.java`
#### Snippet
```java
     * @param allChannels
     * @param serviceDecoder
     * @param messageHandlerName
     * @param maxMsgLength
     * @param maxCons
```

### JavadocDeclaration
`@param maxMsgLength` tag description is missing
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/source/ServerMessageFactory.java`
#### Snippet
```java
     * @param serviceDecoder
     * @param messageHandlerName
     * @param maxMsgLength
     * @param maxCons
     * @param name
```

### JavadocDeclaration
`@param maxCons` tag description is missing
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/source/ServerMessageFactory.java`
#### Snippet
```java
     * @param messageHandlerName
     * @param maxMsgLength
     * @param maxCons
     * @param name
     */
```

### JavadocDeclaration
`@param name` tag description is missing
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/source/ServerMessageFactory.java`
#### Snippet
```java
     * @param maxMsgLength
     * @param maxCons
     * @param name
     */
    public ServerMessageFactory(AbstractSource source,
```

### JavadocDeclaration
`@param context` tag description is missing
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/PulsarSink.java`
#### Snippet
```java
    /**
     * configure
     * @param context
     */
    public void configure(Context context) {
```

### JavadocDeclaration
`@param es` tag description is missing
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/PulsarSink.java`
#### Snippet
```java
    /**
     * Resend the data and store the data in the memory cache.
     * @param es
     * @param isDecrement
     */
```

### JavadocDeclaration
`@param isDecrement` tag description is missing
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/PulsarSink.java`
#### Snippet
```java
     * Resend the data and store the data in the memory cache.
     * @param es
     * @param isDecrement
     */
    private void resendEvent(EventStat es, boolean isDecrement) {
```

### JavadocDeclaration
`@param topic` tag description is missing
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/pulsar/PulsarClientService.java`
#### Snippet
```java
     * send message
     *
     * @param topic
     * @param event
     * @param sendMessageCallBack
```

### JavadocDeclaration
`@param event` tag description is missing
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/pulsar/PulsarClientService.java`
#### Snippet
```java
     *
     * @param topic
     * @param event
     * @param sendMessageCallBack
     * @param es
```

### JavadocDeclaration
`@param sendMessageCallBack` tag description is missing
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/pulsar/PulsarClientService.java`
#### Snippet
```java
     * @param topic
     * @param event
     * @param sendMessageCallBack
     * @param es
     * @return
```

### JavadocDeclaration
`@param es` tag description is missing
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/pulsar/PulsarClientService.java`
#### Snippet
```java
     * @param event
     * @param sendMessageCallBack
     * @param es
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/pulsar/PulsarClientService.java`
#### Snippet
```java
     * @param sendMessageCallBack
     * @param es
     * @return
     */
    public boolean sendMessage(String topic, Event event,
```

### JavadocDeclaration
`@param callBack` tag description is missing
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/pulsar/PulsarClientService.java`
#### Snippet
```java
     * init connection
     *
     * @param callBack
     */
    public void initCreateConnection(CreatePulsarClientCallBack callBack) {
```

### JavadocDeclaration
`@param context` tag description is missing
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/pulsar/PulsarClientService.java`
#### Snippet
```java
     * pulsar client service
     *
     * @param context
     */
    public PulsarClientService(Context context) {
```

### JavadocDeclaration
`@param args` tag description is missing
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/node/Application.java`
#### Snippet
```java
    /**
     * main
     * @param args
     */
    public static void main(String[] args) {
```

### JavadocDeclaration
`@param es` tag description is missing
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/TubeSink.java`
#### Snippet
```java
     * Resend the data and store the data in the memory cache.
     *
     * @param es
     * @param isDecrement
     */
```

### JavadocDeclaration
`@param isDecrement` tag description is missing
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/TubeSink.java`
#### Snippet
```java
     *
     * @param es
     * @param isDecrement
     */
    private void resendEvent(EventStat es, boolean isDecrement) {
```

### JavadocDeclaration
`@param topic` tag description is missing
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/TubeSink.java`
#### Snippet
```java
     * Currently, all topics are published by the same producer. If needed, extend it to multi producers.
     *
     * @param topic
     * @throws TubeClientException
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/TubeSink.java`
#### Snippet
```java
     *
     * @param topic
     * @throws TubeClientException
     */
    private void initTopicProducer(String topic) throws TubeClientException {
```

### JavadocDeclaration
`@param body` tag description is missing
in `inlong-audit/audit-store/src/main/java/org/apache/inlong/audit/service/consume/BaseConsume.java`
#### Snippet
```java
    /**
     * handleMessage
     * @param body
     * @throws Exception
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-audit/audit-store/src/main/java/org/apache/inlong/audit/service/consume/BaseConsume.java`
#### Snippet
```java
     * handleMessage
     * @param body
     * @throws Exception
     */
    protected void handleMessage(String body) throws Exception {
```

### JavadocDeclaration
`@param insertServiceList` tag description is missing
in `inlong-audit/audit-store/src/main/java/org/apache/inlong/audit/service/consume/KafkaConsume.java`
#### Snippet
```java
     * Constructor
     *
     * @param insertServiceList
     * @param storeConfig
     * @param mqConfig
```

### JavadocDeclaration
`@param storeConfig` tag description is missing
in `inlong-audit/audit-store/src/main/java/org/apache/inlong/audit/service/consume/KafkaConsume.java`
#### Snippet
```java
     *
     * @param insertServiceList
     * @param storeConfig
     * @param mqConfig
     */
```

### JavadocDeclaration
`@param mqConfig` tag description is missing
in `inlong-audit/audit-store/src/main/java/org/apache/inlong/audit/service/consume/KafkaConsume.java`
#### Snippet
```java
     * @param insertServiceList
     * @param storeConfig
     * @param mqConfig
     */
    public KafkaConsume(List<InsertData> insertServiceList, StoreConfig storeConfig, MessageQueueConfig mqConfig) {
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-audit/audit-common/src/main/java/org/apache/inlong/audit/file/ConfigHolder.java`
#### Snippet
```java
     * file name with base path.
     *
     * @return
     */
    public String getFilePath() {
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-audit/audit-store/src/main/java/org/apache/inlong/audit/service/AuditMsgConsumerServer.java`
#### Snippet
```java
     * getInsertServiceList
     *
     * @return
     */
    private List<InsertData> getInsertServiceList() {
```

### JavadocDeclaration
`@param insertServiceList` tag description is missing
in `inlong-audit/audit-store/src/main/java/org/apache/inlong/audit/service/consume/TubeConsume.java`
#### Snippet
```java
    /**
     * Constructor
     * @param insertServiceList
     * @param storeConfig
     * @param mqConfig
```

### JavadocDeclaration
`@param storeConfig` tag description is missing
in `inlong-audit/audit-store/src/main/java/org/apache/inlong/audit/service/consume/TubeConsume.java`
#### Snippet
```java
     * Constructor
     * @param insertServiceList
     * @param storeConfig
     * @param mqConfig
     */
```

### JavadocDeclaration
`@param mqConfig` tag description is missing
in `inlong-audit/audit-store/src/main/java/org/apache/inlong/audit/service/consume/TubeConsume.java`
#### Snippet
```java
     * @param insertServiceList
     * @param storeConfig
     * @param mqConfig
     */
    public TubeConsume(List<InsertData> insertServiceList, StoreConfig storeConfig,
```

### JavadocDeclaration
`@param insertServiceList` tag description is missing
in `inlong-audit/audit-store/src/main/java/org/apache/inlong/audit/service/consume/PulsarConsume.java`
#### Snippet
```java
    /**
     * Constructor
     * @param insertServiceList
     * @param storeConfig
     * @param mqConfig
```

### JavadocDeclaration
`@param storeConfig` tag description is missing
in `inlong-audit/audit-store/src/main/java/org/apache/inlong/audit/service/consume/PulsarConsume.java`
#### Snippet
```java
     * Constructor
     * @param insertServiceList
     * @param storeConfig
     * @param mqConfig
     */
```

### JavadocDeclaration
`@param mqConfig` tag description is missing
in `inlong-audit/audit-store/src/main/java/org/apache/inlong/audit/service/consume/PulsarConsume.java`
#### Snippet
```java
     * @param insertServiceList
     * @param storeConfig
     * @param mqConfig
     */
    public PulsarConsume(List<InsertData> insertServiceList, StoreConfig storeConfig,
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-audit/audit-common/src/main/java/org/apache/inlong/audit/file/ConfigManager.java`
#### Snippet
```java
     * get instance for manager
     *
     * @return
     */
    public static ConfigManager getInstance(String fileName, boolean needToCheckChanged) {
```

### JavadocDeclaration
`@param tmpHolder` tag description is missing
in `inlong-audit/audit-common/src/main/java/org/apache/inlong/audit/file/holder/PropertiesConfigHolder.java`
#### Snippet
```java
    /**
     * load from holder
     * @param tmpHolder
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-audit/audit-common/src/main/java/org/apache/inlong/audit/file/holder/PropertiesConfigHolder.java`
#### Snippet
```java
     * load from holder
     * @param tmpHolder
     * @return
     */
    public boolean loadFromHolderToFile(Map<String, String> tmpHolder) {
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-audit/audit-common/src/main/java/org/apache/inlong/audit/file/holder/PropertiesConfigHolder.java`
#### Snippet
```java
     * holder
     *
     * @return
     */
    public Map<String, String> forkHolder() {
```

### JavadocDeclaration
`@param msgBody` tag description is missing
in `inlong-audit/audit-store/src/main/java/org/apache/inlong/audit/service/ElasticsearchService.java`
#### Snippet
```java
    /**
     * insert
     * @param msgBody
     */
    @Override
```

### JavadocDeclaration
`@param key` tag description is missing
in `inlong-common/src/main/java/org/apache/inlong/common/metric/MetricItemValue.java`
#### Snippet
```java
     * Constructor
     * 
     * @param key
     * @param dimensions
     * @param metrics
```

### JavadocDeclaration
`@param dimensions` tag description is missing
in `inlong-common/src/main/java/org/apache/inlong/common/metric/MetricItemValue.java`
#### Snippet
```java
     * 
     * @param key
     * @param dimensions
     * @param metrics
     */
```

### JavadocDeclaration
`@param metrics` tag description is missing
in `inlong-common/src/main/java/org/apache/inlong/common/metric/MetricItemValue.java`
#### Snippet
```java
     * @param key
     * @param dimensions
     * @param metrics
     */
    public MetricItemValue(String key, Map<String, String> dimensions, Map<String, MetricValue> metrics) {
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-common/src/main/java/org/apache/inlong/common/metric/MetricItemValue.java`
#### Snippet
```java
     * toString
     * 
     * @return
     */
    public String toString() {
```

### JavadocDeclaration
`@param character` tag description is missing
in `inlong-common/src/main/java/org/apache/inlong/common/util/MaskDataUtils.java`
#### Snippet
```java
     * whether a character is a delimiter
     *
     * @param character
     * @return true or false
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-common/src/main/java/org/apache/inlong/common/msg/InLongMsgAttrBuilder.java`
#### Snippet
```java
        /**
         * buildAttr
         * @return
         *
         * @throws Exception
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-common/src/main/java/org/apache/inlong/common/msg/InLongMsgAttrBuilder.java`
#### Snippet
```java
         * @return
         *
         * @throws Exception
         */
        public String buildAttr() throws Exception {
```

### JavadocDeclaration
`@param args` tag description is missing
in `inlong-common/src/main/java/org/apache/inlong/common/msg/InLongMsgAttrBuilder.java`
#### Snippet
```java
    /**
     *  main
     * @param args
     * @throws Exception
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-common/src/main/java/org/apache/inlong/common/msg/InLongMsgAttrBuilder.java`
#### Snippet
```java
     *  main
     * @param args
     * @throws Exception
     */
    public static void main(String[] args) throws Exception {
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-common/src/main/java/org/apache/inlong/common/msg/InLongMsgAttrBuilder.java`
#### Snippet
```java
        /**
         * buildAttr
         * @return
         *
         * @throws Exception
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-common/src/main/java/org/apache/inlong/common/msg/InLongMsgAttrBuilder.java`
#### Snippet
```java
         * @return
         *
         * @throws Exception
         */
        public String buildAttr() throws Exception {
```

### JavadocDeclaration
`@param value` tag description is missing
in `inlong-common/src/main/java/org/apache/inlong/common/metric/MetricValue.java`
#### Snippet
```java
     * Constructor
     * 
     * @param value
     */
    private MetricValue(String name, long value) {
```

### JavadocDeclaration
`@param value` tag description is missing
in `inlong-common/src/main/java/org/apache/inlong/common/metric/MetricValue.java`
#### Snippet
```java
     * of
     * 
     * @param  value
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-common/src/main/java/org/apache/inlong/common/metric/MetricValue.java`
#### Snippet
```java
     * 
     * @param  value
     * @return
     */
    public static MetricValue of(String name, long value) {
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-common/src/main/java/org/apache/inlong/common/metric/MetricListenerRunnable.java`
#### Snippet
```java
     *
     * @return                              MetricItemValue List
     * @throws InstanceNotFoundException
     * @throws AttributeNotFoundException
     * @throws ReflectionException
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-common/src/main/java/org/apache/inlong/common/metric/MetricListenerRunnable.java`
#### Snippet
```java
     * @return                              MetricItemValue List
     * @throws InstanceNotFoundException
     * @throws AttributeNotFoundException
     * @throws ReflectionException
     * @throws MBeanException
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-common/src/main/java/org/apache/inlong/common/metric/MetricListenerRunnable.java`
#### Snippet
```java
     * @throws InstanceNotFoundException
     * @throws AttributeNotFoundException
     * @throws ReflectionException
     * @throws MBeanException
     * @throws MalformedObjectNameException
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-common/src/main/java/org/apache/inlong/common/metric/MetricListenerRunnable.java`
#### Snippet
```java
     * @throws AttributeNotFoundException
     * @throws ReflectionException
     * @throws MBeanException
     * @throws MalformedObjectNameException
     * @throws ClassNotFoundException
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-common/src/main/java/org/apache/inlong/common/metric/MetricListenerRunnable.java`
#### Snippet
```java
     * @throws ReflectionException
     * @throws MBeanException
     * @throws MalformedObjectNameException
     * @throws ClassNotFoundException
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-common/src/main/java/org/apache/inlong/common/metric/MetricListenerRunnable.java`
#### Snippet
```java
     * @throws MBeanException
     * @throws MalformedObjectNameException
     * @throws ClassNotFoundException
     */
    @SuppressWarnings("unchecked")
```

### JavadocDeclaration
`@param domain` tag description is missing
in `inlong-common/src/main/java/org/apache/inlong/common/metric/MetricListenerRunnable.java`
#### Snippet
```java
     * Constructor
     *
     * @param domain
     * @param listenerList
     */
```

### JavadocDeclaration
`@param listenerList` tag description is missing
in `inlong-common/src/main/java/org/apache/inlong/common/metric/MetricListenerRunnable.java`
#### Snippet
```java
     *
     * @param domain
     * @param listenerList
     */
    public MetricListenerRunnable(String domain, List<MetricListener> listenerList) {
```

### JavadocDeclaration
`@param key` tag description is missing
in `inlong-common/src/main/java/org/apache/inlong/common/monitor/MonitorIndexExt.java`
#### Snippet
```java
    /**
     * incrementAndGet
     * @param key
     */
    public void incrementAndGet(String key) {
```

### JavadocDeclaration
`@param attr` tag description is missing
in `inlong-common/src/main/java/org/apache/inlong/common/msg/InLongMsg.java`
#### Snippet
```java
    /**
     * add msg
     * @param attr
     * @param data
     * @return
```

### JavadocDeclaration
`@param data` tag description is missing
in `inlong-common/src/main/java/org/apache/inlong/common/msg/InLongMsg.java`
#### Snippet
```java
     * add msg
     * @param attr
     * @param data
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-common/src/main/java/org/apache/inlong/common/msg/InLongMsg.java`
#### Snippet
```java
     * @param attr
     * @param data
     * @return
     */
    public boolean addMsg(String attr, ByteBuffer data) {
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-common/src/main/java/org/apache/inlong/common/msg/InLongMsg.java`
#### Snippet
```java
     * capacity: 4096, compress: true, version: 1
     *
     * @return
     */
    public static InLongMsg newInLongMsg() {
```

### JavadocDeclaration
`@param rawdata` tag description is missing
in `inlong-common/src/main/java/org/apache/inlong/common/msg/InLongMsg.java`
#### Snippet
```java
    /**
     * getIterator
     * @param rawdata
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-common/src/main/java/org/apache/inlong/common/msg/InLongMsg.java`
#### Snippet
```java
     * getIterator
     * @param rawdata
     * @return
     */
    public static Iterator<byte[]> getIterator(ByteBuffer rawdata) {
```

### JavadocDeclaration
`@param key` tag description is missing
in `inlong-common/src/main/java/org/apache/inlong/common/monitor/MonitorIndex.java`
#### Snippet
```java
    /**
     * addAndGet
     * @param key
     * @param cnt
     * @param packcnt
```

### JavadocDeclaration
`@param cnt` tag description is missing
in `inlong-common/src/main/java/org/apache/inlong/common/monitor/MonitorIndex.java`
#### Snippet
```java
     * addAndGet
     * @param key
     * @param cnt
     * @param packcnt
     * @param packsize
```

### JavadocDeclaration
`@param packcnt` tag description is missing
in `inlong-common/src/main/java/org/apache/inlong/common/monitor/MonitorIndex.java`
#### Snippet
```java
     * @param key
     * @param cnt
     * @param packcnt
     * @param packsize
     * @param failcnt
```

### JavadocDeclaration
`@param packsize` tag description is missing
in `inlong-common/src/main/java/org/apache/inlong/common/monitor/MonitorIndex.java`
#### Snippet
```java
     * @param cnt
     * @param packcnt
     * @param packsize
     * @param failcnt
     */
```

### JavadocDeclaration
`@param failcnt` tag description is missing
in `inlong-common/src/main/java/org/apache/inlong/common/monitor/MonitorIndex.java`
#### Snippet
```java
     * @param packcnt
     * @param packsize
     * @param failcnt
     */
    public void addAndGet(String key, int cnt, int packcnt, long packsize, int failcnt) {
```

### JavadocDeclaration
`@param value` tag description is missing
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/netty/NettyRpcServer.java`
#### Snippet
```java
         * prepare and write the message into an list of byte buffers
         *
         * @param value
         * @param status
         * @param errorClass
```

### JavadocDeclaration
`@param status` tag description is missing
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/netty/NettyRpcServer.java`
#### Snippet
```java
         *
         * @param value
         * @param status
         * @param errorClass
         * @param error
```

### JavadocDeclaration
`@param errorClass` tag description is missing
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/netty/NettyRpcServer.java`
#### Snippet
```java
         * @param value
         * @param status
         * @param errorClass
         * @param error
         * @return
```

### JavadocDeclaration
`@param error` tag description is missing
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/netty/NettyRpcServer.java`
#### Snippet
```java
         * @param status
         * @param errorClass
         * @param error
         * @return
         */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/netty/NettyRpcServer.java`
#### Snippet
```java
         * @param errorClass
         * @param error
         * @return
         */
        protected List<ByteBuffer> prepareResponse(Object value, int rmtVersion,
```

### JavadocDeclaration
`@param conf` tag description is missing
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/netty/NettyRpcServer.java`
#### Snippet
```java
     * create a server with rpc config info
     *
     * @param conf
     * @throws Exception
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/netty/NettyRpcServer.java`
#### Snippet
```java
     *
     * @param conf
     * @throws Exception
     */
    public NettyRpcServer(RpcConfig conf) throws Exception {
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/RpcServiceFactory.java`
#### Snippet
```java
     * shutdown and recycle the connection resources
     *
     * @throws Exception
     */
    public void destroy() throws Exception {
```

### JavadocDeclaration
`@param strPartInfoList` tag description is missing
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/utils/DataConverterUtil.java`
#### Snippet
```java
     * convert string info to @link Partition
     *
     * @param strPartInfoList
     * @return return a list of Partition
     */
```

### JavadocDeclaration
`@param topicName` tag description is missing
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/utils/DataConverterUtil.java`
#### Snippet
```java
     * to a list of @link Message
     *
     * @param topicName
     * @param transferedMessageList return a list of @link Message
     */
```

### JavadocDeclaration
`@param overTLS` tag description is missing
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/cluster/Partition.java`
#### Snippet
```java
     * get the partition full string info
     *
     * @param overTLS
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/cluster/Partition.java`
#### Snippet
```java
     *
     * @param overTLS
     * @return
     */
    public String getPartitionFullStr(boolean overTLS) {
```

### JavadocDeclaration
`@param broker` tag description is missing
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/cluster/Partition.java`
#### Snippet
```java
     * create a Partition with full broker and part info string
     *
     * @param broker
     * @param partStr
     */
```

### JavadocDeclaration
`@param partStr` tag description is missing
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/cluster/Partition.java`
#### Snippet
```java
     *
     * @param broker
     * @param partStr
     */
    public Partition(BrokerInfo broker,
```

### JavadocDeclaration
`@param broker` tag description is missing
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/cluster/Partition.java`
#### Snippet
```java
     * create a Partition with broker info , topic and partitionId
     *
     * @param broker
     * @param topic
     * @param partitionId
```

### JavadocDeclaration
`@param topic` tag description is missing
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/cluster/Partition.java`
#### Snippet
```java
     *
     * @param broker
     * @param topic
     * @param partitionId
     */
```

### JavadocDeclaration
`@param partitionId` tag description is missing
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/cluster/Partition.java`
#### Snippet
```java
     * @param broker
     * @param topic
     * @param partitionId
     */
    public Partition(final BrokerInfo broker,
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/producer/SimpleMessageProducer.java`
#### Snippet
```java
     * @param topic topic name
     * @return if accept message
     * @throws TubeClientException
     */
    @Override
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/producer/SimpleMessageProducer.java`
#### Snippet
```java
     * @param topicSet topic names
     * @return successful published topic names
     * @throws TubeClientException
     */
    @Override
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/producer/SimpleMessageProducer.java`
#### Snippet
```java
     *
     * @return published topic set
     * @throws TubeClientException
     */
    @Override
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/producer/SimpleMessageProducer.java`
#### Snippet
```java
     *
     * @param topic topic name
     * @throws TubeClientException
     */
    @Override
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/BaseMessageConsumer.java`
#### Snippet
```java
     * @param registerInfoMap broker partition mapping
     * @param unRegPartitions unregister partition list
     * @throws InterruptedException
     */
    private void registerPartitions(Map<BrokerInfo, List<Partition>> registerInfoMap,
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/BaseMessageConsumer.java`
#### Snippet
```java
     * Complete subscription.
     *
     * @throws TubeClientException
     */
    @Override
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/BaseMessageConsumer.java`
#### Snippet
```java
     *
     * @return consumer id
     * @throws Exception
     */
    private String generateConsumerID() throws Exception {
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/BaseMessageConsumer.java`
#### Snippet
```java
     * Start heartbeat thread and rebalance thread.
     *
     * @throws TubeClientException
     */
    private void startMasterAndBrokerThreads() throws TubeClientException {
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/BaseMessageConsumer.java`
#### Snippet
```java
     * @param messageListener message listener
     * @return message consumer
     * @throws TubeClientException
     */
    protected MessageConsumer subscribe(String topic,
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/BaseMessageConsumer.java`
#### Snippet
```java
     * Shutdown current consumer.
     *
     * @throws Throwable
     */
    @Override
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/BaseMessageConsumer.java`
#### Snippet
```java
     * @param consumerConfig consumer configuration
     * @param isPullConsume  if this is a pull consumer
     * @throws TubeClientException
     */
    public BaseMessageConsumer(final InnerSessionFactory sessionFactory,
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/SimpleClientBalanceConsumer.java`
#### Snippet
```java
     *
     * @return consumer id
     * @throws Exception
     */
    private String generateConsumerID() throws Exception {
```

### JavadocDeclaration
`@param consumerId` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/BrokerServiceServer.java`
#### Snippet
```java
     * Get consumer's register time.
     *
     * @param consumerId
     * @param partitionStr
     * @return
```

### JavadocDeclaration
`@param partitionStr` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/BrokerServiceServer.java`
#### Snippet
```java
     *
     * @param consumerId
     * @param partitionStr
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/BrokerServiceServer.java`
#### Snippet
```java
     * @param consumerId
     * @param partitionStr
     * @return
     */
    public Long getConsumerRegisterTime(String consumerId, String partitionStr) {
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/BrokerServiceServer.java`
#### Snippet
```java
     * Start broker service
     *
     * @throws Exception
     */
    @Override
```

### JavadocDeclaration
HeartbeatException is not declared to be thrown by method updBrokerNode
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/heartbeat/HeartbeatManager.java`
#### Snippet
```java
     *
     * @param nodeId the id of node to be updated
     * @throws HeartbeatException if the timeout info of the node is not found
     */
    public boolean updBrokerNode(String nodeId, String createId,
```

### JavadocDeclaration
InterruptedException is not declared to be thrown by method createPersistent
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/zookeeper/ZKUtil.java`
#### Snippet
```java
     * @param createParents if true all parent dirs are created as well and no {@link
     *                      NodeExistsException} is thrown in case the path already exists
     * @throws InterruptedException     if operation was interrupted, or a required reconnection got
     *                                  interrupted
     * @throws IllegalArgumentException if called from anything except the ZooKeeper event thread
```

### JavadocDeclaration
`@param req` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/action/screen/Master.java`
#### Snippet
```java
     * Get broker info
     *
     * @param req
     * @param sBuilder
     * @param isOldRet
```

### JavadocDeclaration
`@param sBuilder` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/action/screen/Master.java`
#### Snippet
```java
     *
     * @param req
     * @param sBuilder
     * @param isOldRet
     * @return
```

### JavadocDeclaration
`@param isOldRet` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/action/screen/Master.java`
#### Snippet
```java
     * @param req
     * @param sBuilder
     * @param isOldRet
     * @return
     */
```

### JavadocDeclaration
Tag `return` is not allowed here
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/action/screen/Master.java`
#### Snippet
```java
     * @param sBuilder
     * @param isOldRet
     * @return
     */
    private void innGetBrokerInfo(final HttpServletRequest req,
```

### JavadocDeclaration
`@param req` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/action/screen/Master.java`
#### Snippet
```java
     * Get topic publish info
     *
     * @param req
     * @param sBuilder
     * @return
```

### JavadocDeclaration
`@param sBuilder` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/action/screen/Master.java`
#### Snippet
```java
     *
     * @param req
     * @param sBuilder
     * @return
     */
```

### JavadocDeclaration
Tag `return` is not allowed here
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/action/screen/Master.java`
#### Snippet
```java
     * @param req
     * @param sBuilder
     * @return
     */
    private void getTopicPubInfo(final HttpServletRequest req, StringBuilder sBuilder) {
```

### JavadocDeclaration
`@param req` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/action/screen/Master.java`
#### Snippet
```java
     * Get producer list info
     *
     * @param req
     * @param sBuilder
     * @return
```

### JavadocDeclaration
`@param sBuilder` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/action/screen/Master.java`
#### Snippet
```java
     *
     * @param req
     * @param sBuilder
     * @return
     */
```

### JavadocDeclaration
Tag `return` is not allowed here
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/action/screen/Master.java`
#### Snippet
```java
     * @param req
     * @param sBuilder
     * @return
     */
    private void getProducerListInfo(final HttpServletRequest req, StringBuilder sBuilder) {
```

### JavadocDeclaration
`@param req` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/action/screen/Master.java`
#### Snippet
```java
     * Get consumer subscription info
     *
     * @param req
     * @param sBuilder
     * @return
```

### JavadocDeclaration
`@param sBuilder` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/action/screen/Master.java`
#### Snippet
```java
     *
     * @param req
     * @param sBuilder
     * @return
     */
```

### JavadocDeclaration
Tag `return` is not allowed here
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/action/screen/Master.java`
#### Snippet
```java
     * @param req
     * @param sBuilder
     * @return
     */
    private void getConsumerSubInfo(final HttpServletRequest req, StringBuilder sBuilder) {
```

### JavadocDeclaration
`@param req` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/action/screen/Master.java`
#### Snippet
```java
     * Get consumer list info
     *
     * @param req
     * @param sBuilder
     * @return
```

### JavadocDeclaration
`@param sBuilder` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/action/screen/Master.java`
#### Snippet
```java
     *
     * @param req
     * @param sBuilder
     * @return
     */
```

### JavadocDeclaration
Tag `return` is not allowed here
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/action/screen/Master.java`
#### Snippet
```java
     * @param req
     * @param sBuilder
     * @return
     */
    private void getConsumerListInfo(final HttpServletRequest req, StringBuilder sBuilder) {
```

### JavadocDeclaration
`@param sBuilder` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/action/screen/Master.java`
#### Snippet
```java
     * Get un-balanced group info
     *
     * @param sBuilder
     * @return
     */
```

### JavadocDeclaration
Tag `return` is not allowed here
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/action/screen/Master.java`
#### Snippet
```java
     *
     * @param sBuilder
     * @return
     */
    private void getUnbalanceGroupInfo(StringBuilder sBuilder) {
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/utils/WebParameterUtils.java`
#### Snippet
```java
     * @param required    denote whether the parameter is must required
     * @return a list of linked hash map represent the json array
     * @throws Exception
     */
    public static List<Map<String, String>> checkAndGetJsonArray(String paramName,
```

### JavadocDeclaration
`@param req` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/simplemvc/MultipartHttpServletRequest.java`
#### Snippet
```java
     * If one day there's need to received serialized POJO or some kinds of binary stream, please compose a
     * full powered function to replace this one, as this is ONLY for backward compatibility.
     * @param req
     */
    private void extractParametersFromMultipartFormData(HttpServletRequest req) throws IOException, ServletException {
```

### JavadocDeclaration
`@param req` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebBrokerConfHandler.java`
#### Snippet
```java
     * Add default config to brokers in batch
     *
     * @param req
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebBrokerConfHandler.java`
#### Snippet
```java
     *
     * @param req
     * @return
     */
    private StringBuilder innBatchAddOrUpdBrokerConfInfo(HttpServletRequest req,
```

### JavadocDeclaration
`@param withTopic` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebBrokerConfHandler.java`
#### Snippet
```java
     * Private method to add topic info
     *
     * @param withTopic
     * @param sBuffer
     * @param topicConfEntityMap
```

### JavadocDeclaration
`@param sBuffer` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebBrokerConfHandler.java`
#### Snippet
```java
     *
     * @param withTopic
     * @param sBuffer
     * @param topicConfEntityMap
     * @return
```

### JavadocDeclaration
`@param topicConfEntityMap` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebBrokerConfHandler.java`
#### Snippet
```java
     * @param withTopic
     * @param sBuffer
     * @param topicConfEntityMap
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebBrokerConfHandler.java`
#### Snippet
```java
     * @param sBuffer
     * @param topicConfEntityMap
     * @return
     */
    private StringBuilder addTopicInfo(Boolean withTopic, StringBuilder sBuffer,
```

### JavadocDeclaration
`@param qryTopicSet` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebBrokerConfHandler.java`
#### Snippet
```java
     * Check if the record is valid
     *
     * @param qryTopicSet
     * @param topicStatus
     * @param isInclude
```

### JavadocDeclaration
`@param topicStatus` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebBrokerConfHandler.java`
#### Snippet
```java
     *
     * @param qryTopicSet
     * @param topicStatus
     * @param isInclude
     * @param topicConfEntityMap
```

### JavadocDeclaration
`@param isInclude` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebBrokerConfHandler.java`
#### Snippet
```java
     * @param qryTopicSet
     * @param topicStatus
     * @param isInclude
     * @param topicConfEntityMap
     * @return
```

### JavadocDeclaration
`@param topicConfEntityMap` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebBrokerConfHandler.java`
#### Snippet
```java
     * @param topicStatus
     * @param isInclude
     * @param topicConfEntityMap
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebBrokerConfHandler.java`
#### Snippet
```java
     * @param isInclude
     * @param topicConfEntityMap
     * @return
     */
    private boolean isValidRecord(Set<String> qryTopicSet, Boolean isInclude,
```

### JavadocDeclaration
`@param sBuilder` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/bdbstore/bdbentitys/BdbGroupFlowCtrlEntity.java`
#### Snippet
```java
     * Serialize config field to json format
     *
     * @param sBuilder
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/bdbstore/bdbentitys/BdbGroupFlowCtrlEntity.java`
#### Snippet
```java
     *
     * @param sBuilder
     * @return
     */
    public StringBuilder toJsonString(final StringBuilder sBuilder) {
```

### JavadocDeclaration
`@param sBuilder` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/bdbstore/bdbentitys/BdbTopicConfEntity.java`
#### Snippet
```java
     * Serialize field to json format
     *
     * @param sBuilder
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/bdbstore/bdbentitys/BdbTopicConfEntity.java`
#### Snippet
```java
     *
     * @param sBuilder
     * @return
     */
    public StringBuilder toJsonString(final StringBuilder sBuilder) {
```

### JavadocDeclaration
`@param request` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/TMaster.java`
#### Snippet
```java
     * Broker heartbeat request with master
     *
     * @param request
     * @param rmtAddress
     * @param overtls
```

### JavadocDeclaration
`@param rmtAddress` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/TMaster.java`
#### Snippet
```java
     *
     * @param request
     * @param rmtAddress
     * @param overtls
     * @return heartbeat response
```

### JavadocDeclaration
`@param overtls` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/TMaster.java`
#### Snippet
```java
     * @param request
     * @param rmtAddress
     * @param overtls
     * @return heartbeat response
     * @throws Exception
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/TMaster.java`
#### Snippet
```java
     * @param overtls
     * @return heartbeat response
     * @throws Exception
     */
    @Override
```

### JavadocDeclaration
`@param group` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/TMaster.java`
#### Snippet
```java
     * Generate consumer id
     *
     * @param group
     * @param consumerId
     * @return
```

### JavadocDeclaration
`@param consumerId` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/TMaster.java`
#### Snippet
```java
     *
     * @param group
     * @param consumerId
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/TMaster.java`
#### Snippet
```java
     * @param group
     * @param consumerId
     * @return
     */
    private String getConsumerKey(String group, String consumerId) {
```

### JavadocDeclaration
`@param strBuffer` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/TMaster.java`
#### Snippet
```java
     * get need balance group list
     *
     * @param strBuffer
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/TMaster.java`
#### Snippet
```java
     *
     * @param strBuffer
     * @return
     */
    private List<String> getNeedToBalanceGroups(final StringBuilder strBuffer) {
```

### JavadocDeclaration
`@param request` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/TMaster.java`
#### Snippet
```java
     * Broker close request with master
     *
     * @param request
     * @param rmtAddress
     * @param overtls
```

### JavadocDeclaration
`@param rmtAddress` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/TMaster.java`
#### Snippet
```java
     *
     * @param request
     * @param rmtAddress
     * @param overtls
     * @return close response
```

### JavadocDeclaration
`@param overtls` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/TMaster.java`
#### Snippet
```java
     * @param request
     * @param rmtAddress
     * @param overtls
     * @return close response
     * @throws Throwable
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/TMaster.java`
#### Snippet
```java
     * @param overtls
     * @return close response
     * @throws Throwable
     */
    @Override
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/TMaster.java`
#### Snippet
```java
     * Get master config
     *
     * @return
     */
    public MasterConfig getMasterConfig() {
```

### JavadocDeclaration
`@param masterConfig` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/TMaster.java`
#### Snippet
```java
     * constructor
     *
     * @param masterConfig
     * @throws Exception
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/TMaster.java`
#### Snippet
```java
     *
     * @param masterConfig
     * @throws Exception
     */
    public TMaster(MasterConfig masterConfig) throws Exception {
```

### JavadocDeclaration
`@param masterSubInfoMap` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/TMaster.java`
#### Snippet
```java
     * check if master subscribe info consist consumer subscribe info
     *
     * @param masterSubInfoMap
     * @param consumerSubInfoList
     * @return
```

### JavadocDeclaration
`@param consumerSubInfoList` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/TMaster.java`
#### Snippet
```java
     *
     * @param masterSubInfoMap
     * @param consumerSubInfoList
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/TMaster.java`
#### Snippet
```java
     * @param masterSubInfoMap
     * @param consumerSubInfoList
     * @return
     */
    private boolean checkIfConsist(Map<String, Map<String, Partition>> masterSubInfoMap,
```

### JavadocDeclaration
`@param request` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/TMaster.java`
#### Snippet
```java
     * Consumer register request with master
     *
     * @param request
     * @param rmtAddress
     * @param overtls
```

### JavadocDeclaration
`@param rmtAddress` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/TMaster.java`
#### Snippet
```java
     *
     * @param request
     * @param rmtAddress
     * @param overtls
     * @return register response
```

### JavadocDeclaration
`@param overtls` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/TMaster.java`
#### Snippet
```java
     * @param request
     * @param rmtAddress
     * @param overtls
     * @return register response
     * @throws Exception
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/TMaster.java`
#### Snippet
```java
     * @param overtls
     * @return register response
     * @throws Exception
     */
    @Override
```

### JavadocDeclaration
`@param request` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/TMaster.java`
#### Snippet
```java
     * Producer heartbeat request with master
     *
     * @param request
     * @param rmtAddress
     * @param overtls
```

### JavadocDeclaration
`@param rmtAddress` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/TMaster.java`
#### Snippet
```java
     *
     * @param request
     * @param rmtAddress
     * @param overtls
     * @return heartbeat response
```

### JavadocDeclaration
`@param overtls` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/TMaster.java`
#### Snippet
```java
     * @param request
     * @param rmtAddress
     * @param overtls
     * @return heartbeat response
     * @throws Exception
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/TMaster.java`
#### Snippet
```java
     * @param overtls
     * @return heartbeat response
     * @throws Exception
     */
    @Override
```

### JavadocDeclaration
`@param authAuthorizedToken` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/TMaster.java`
#### Snippet
```java
     * get client authorized info
     *
     * @param authAuthorizedToken
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/TMaster.java`
#### Snippet
```java
     *
     * @param authAuthorizedToken
     * @return
     */
    private MasterAuthorizedInfo.Builder genAuthorizedInfo(String authAuthorizedToken, boolean isBroker) {
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/TMaster.java`
#### Snippet
```java
     * check bdb meta-data path, create it if not exist
     *
     * @throws Exception
     */
    private void chkAndCreateBdbMetaDataPath() throws Exception {
```

### JavadocDeclaration
`@param request` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/TMaster.java`
#### Snippet
```java
     * Consumer close request with master
     *
     * @param request
     * @param rmtAddress
     * @param overtls
```

### JavadocDeclaration
`@param rmtAddress` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/TMaster.java`
#### Snippet
```java
     *
     * @param request
     * @param rmtAddress
     * @param overtls
     * @return close response
```

### JavadocDeclaration
`@param overtls` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/TMaster.java`
#### Snippet
```java
     * @param request
     * @param rmtAddress
     * @param overtls
     * @return close response
     * @throws Exception
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/TMaster.java`
#### Snippet
```java
     * @param overtls
     * @return close response
     * @throws Exception
     */
    @Override
```

### JavadocDeclaration
`@param authAuthorizedToken` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/TMaster.java`
#### Snippet
```java
     * get broker authorized info
     *
     * @param authAuthorizedToken
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/TMaster.java`
#### Snippet
```java
     *
     * @param authAuthorizedToken
     * @return
     */
    private MasterBrokerAuthorizedInfo.Builder genBrokerAuthorizedInfo(String authAuthorizedToken) {
```

### JavadocDeclaration
`@param producerId` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/TMaster.java`
#### Snippet
```java
     * Get producer topic partition info
     *
     * @param producerId
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/TMaster.java`
#### Snippet
```java
     *
     * @param producerId
     * @return
     */
    private Tuple3<Long, Integer, Map<String, String>> getTopicConfigureInfos(String producerId,
```

### JavadocDeclaration
`@param request` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/TMaster.java`
#### Snippet
```java
     * Client balance consumer register request with master
     *
     * @param request
     * @param rmtAddress
     * @param overtls
```

### JavadocDeclaration
`@param rmtAddress` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/TMaster.java`
#### Snippet
```java
     *
     * @param request
     * @param rmtAddress
     * @param overtls
     * @return register response
```

### JavadocDeclaration
`@param overtls` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/TMaster.java`
#### Snippet
```java
     * @param request
     * @param rmtAddress
     * @param overtls
     * @return register response
     * @throws Exception
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/TMaster.java`
#### Snippet
```java
     * @param overtls
     * @return register response
     * @throws Exception
     */
    @Override
```

### JavadocDeclaration
`@param request` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/TMaster.java`
#### Snippet
```java
     * Producer register request to master
     *
     * @param request
     * @param rmtAddress
     * @param overtls
```

### JavadocDeclaration
`@param rmtAddress` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/TMaster.java`
#### Snippet
```java
     *
     * @param request
     * @param rmtAddress
     * @param overtls
     * @return register response
```

### JavadocDeclaration
`@param overtls` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/TMaster.java`
#### Snippet
```java
     * @param request
     * @param rmtAddress
     * @param overtls
     * @return register response
     * @throws Exception
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/TMaster.java`
#### Snippet
```java
     * @param overtls
     * @return register response
     * @throws Exception
     */
    @Override
```

### JavadocDeclaration
`@param request` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/TMaster.java`
#### Snippet
```java
     * Client balance consumer get partition meta information with master
     *
     * @param request
     * @param rmtAddress
     * @param overtls
```

### JavadocDeclaration
`@param rmtAddress` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/TMaster.java`
#### Snippet
```java
     *
     * @param request
     * @param rmtAddress
     * @param overtls
     * @return close response
```

### JavadocDeclaration
`@param overtls` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/TMaster.java`
#### Snippet
```java
     * @param request
     * @param rmtAddress
     * @param overtls
     * @return close response
     * @throws Exception
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/TMaster.java`
#### Snippet
```java
     * @param overtls
     * @return close response
     * @throws Exception
     */
    @Override
```

### JavadocDeclaration
`@param request` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/TMaster.java`
#### Snippet
```java
     * Client balance consumer heartbeat request with master
     *
     * @param request
     * @param rmtAddress
     * @param overtls
```

### JavadocDeclaration
`@param rmtAddress` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/TMaster.java`
#### Snippet
```java
     *
     * @param request
     * @param rmtAddress
     * @param overtls
     * @return heartbeat response
```

### JavadocDeclaration
`@param overtls` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/TMaster.java`
#### Snippet
```java
     * @param request
     * @param rmtAddress
     * @param overtls
     * @return heartbeat response
     * @throws Throwable
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/TMaster.java`
#### Snippet
```java
     * @param overtls
     * @return heartbeat response
     * @throws Throwable
     */
    @Override
```

### JavadocDeclaration
`@param request` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/TMaster.java`
#### Snippet
```java
     * Producer close request with master
     *
     * @param request
     * @param rmtAddress
     * @param overtls
```

### JavadocDeclaration
`@param rmtAddress` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/TMaster.java`
#### Snippet
```java
     *
     * @param request
     * @param rmtAddress
     * @param overtls
     * @return close response
```

### JavadocDeclaration
`@param overtls` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/TMaster.java`
#### Snippet
```java
     * @param request
     * @param rmtAddress
     * @param overtls
     * @return close response
     * @throws Exception
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/TMaster.java`
#### Snippet
```java
     * @param overtls
     * @return close response
     * @throws Exception
     */
    @Override
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/TMaster.java`
#### Snippet
```java
     * Getter
     *
     * @return
     */
    public ConcurrentHashMap<String, Map<String, Map<String, Partition>>> getCurrentSubInfoMap() {
```

### JavadocDeclaration
`@param master` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/TMaster.java`
#### Snippet
```java
     * Start balance chore
     *
     * @param master
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/TMaster.java`
#### Snippet
```java
     *
     * @param master
     * @return
     */
    private Thread startBalancerChore(final TMaster master) {
```

### JavadocDeclaration
`@param request` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/TMaster.java`
#### Snippet
```java
     * Broker register request with master
     *
     * @param request
     * @param rmtAddress
     * @param overtls
```

### JavadocDeclaration
`@param rmtAddress` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/TMaster.java`
#### Snippet
```java
     *
     * @param request
     * @param rmtAddress
     * @param overtls
     * @return register response
```

### JavadocDeclaration
`@param overtls` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/TMaster.java`
#### Snippet
```java
     * @param request
     * @param rmtAddress
     * @param overtls
     * @return register response
     * @throws Exception
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/TMaster.java`
#### Snippet
```java
     * @param overtls
     * @return register response
     * @throws Exception
     */
    @Override
```

### JavadocDeclaration
`@param request` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/TMaster.java`
#### Snippet
```java
     * Consumer heartbeat request with master
     *
     * @param request
     * @param rmtAddress
     * @param overtls
```

### JavadocDeclaration
`@param rmtAddress` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/TMaster.java`
#### Snippet
```java
     *
     * @param request
     * @param rmtAddress
     * @param overtls
     * @return heartbeat response
```

### JavadocDeclaration
`@param overtls` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/TMaster.java`
#### Snippet
```java
     * @param request
     * @param rmtAddress
     * @param overtls
     * @return heartbeat response
     * @throws Throwable
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/TMaster.java`
#### Snippet
```java
     * @param overtls
     * @return heartbeat response
     * @throws Throwable
     */
    @Override
```

### JavadocDeclaration
`@param req` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebTopicDeployHandler.java`
#### Snippet
```java
     * Internal method to perform topic deploy status change
     *
     * @param req
     * @param chgType
     * @return
```

### JavadocDeclaration
`@param chgType` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebTopicDeployHandler.java`
#### Snippet
```java
     *
     * @param req
     * @param chgType
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebTopicDeployHandler.java`
#### Snippet
```java
     * @param req
     * @param chgType
     * @return
     */
    private StringBuilder innModifyTopicDeployStatusInfo(HttpServletRequest req,
```

### JavadocDeclaration
`@param sBuilder` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/bdbstore/bdbentitys/BdbClusterSettingEntity.java`
#### Snippet
```java
     * Serialize field to json format
     *
     * @param sBuilder
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/bdbstore/bdbentitys/BdbClusterSettingEntity.java`
#### Snippet
```java
     *
     * @param sBuilder
     * @return
     */
    public StringBuilder toJsonString(final StringBuilder sBuilder) {
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/metamanage/MetaDataService.java`
#### Snippet
```java
     * Transfer master role to other replica nodes
     *
     * @throws Exception
     */
    void transferMaster() throws Exception;
```

### JavadocDeclaration
`@param partitions` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/balance/DefaultLoadBalancer.java`
#### Snippet
```java
     * Round robin assign partitions
     *
     * @param partitions
     * @param consumers
     * @return
```

### JavadocDeclaration
`@param consumers` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/balance/DefaultLoadBalancer.java`
#### Snippet
```java
     *
     * @param partitions
     * @param consumers
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/balance/DefaultLoadBalancer.java`
#### Snippet
```java
     * @param partitions
     * @param consumers
     * @return
     */
    @Override
```

### JavadocDeclaration
`@param partitionToAssignMap` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/balance/DefaultLoadBalancer.java`
#### Snippet
```java
     * Random assign partition
     *
     * @param partitionToAssignMap
     * @param consumerList
     * @param clusterState
```

### JavadocDeclaration
`@param consumerList` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/balance/DefaultLoadBalancer.java`
#### Snippet
```java
     *
     * @param partitionToAssignMap
     * @param consumerList
     * @param clusterState
     * @param oldClusterState
```

### JavadocDeclaration
`@param clusterState` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/balance/DefaultLoadBalancer.java`
#### Snippet
```java
     * @param partitionToAssignMap
     * @param consumerList
     * @param clusterState
     * @param oldClusterState
     * @param filterList
```

### JavadocDeclaration
`@param oldClusterState` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/balance/DefaultLoadBalancer.java`
#### Snippet
```java
     * @param consumerList
     * @param clusterState
     * @param oldClusterState
     * @param filterList
     */
```

### JavadocDeclaration
`@param filterList` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/balance/DefaultLoadBalancer.java`
#### Snippet
```java
     * @param clusterState
     * @param oldClusterState
     * @param filterList
     */
    private void randomAssign(Map<String, Partition> partitionToAssignMap,
```

### JavadocDeclaration
`@param clusterState` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/balance/DefaultLoadBalancer.java`
#### Snippet
```java
     * Reset balance
     *
     * @param clusterState
     * @param consumerHolder
     * @param brokerRunManager
```

### JavadocDeclaration
`@param consumerHolder` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/balance/DefaultLoadBalancer.java`
#### Snippet
```java
     *
     * @param clusterState
     * @param consumerHolder
     * @param brokerRunManager
     * @param groupSet
```

### JavadocDeclaration
`@param brokerRunManager` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/balance/DefaultLoadBalancer.java`
#### Snippet
```java
     * @param clusterState
     * @param consumerHolder
     * @param brokerRunManager
     * @param groupSet
     * @param defMetaDataService
```

### JavadocDeclaration
`@param groupSet` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/balance/DefaultLoadBalancer.java`
#### Snippet
```java
     * @param consumerHolder
     * @param brokerRunManager
     * @param groupSet
     * @param defMetaDataService
     * @param strBuffer
```

### JavadocDeclaration
`@param defMetaDataService` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/balance/DefaultLoadBalancer.java`
#### Snippet
```java
     * @param brokerRunManager
     * @param groupSet
     * @param defMetaDataService
     * @param strBuffer
     * @return
```

### JavadocDeclaration
`@param strBuffer` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/balance/DefaultLoadBalancer.java`
#### Snippet
```java
     * @param groupSet
     * @param defMetaDataService
     * @param strBuffer
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/balance/DefaultLoadBalancer.java`
#### Snippet
```java
     * @param defMetaDataService
     * @param strBuffer
     * @return
     */
    @Override
```

### JavadocDeclaration
`@param consumerHolder` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/balance/DefaultLoadBalancer.java`
#### Snippet
```java
     * Reset
     *
     * @param consumerHolder
     * @param brokerRunManager
     * @param groupSet
```

### JavadocDeclaration
`@param brokerRunManager` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/balance/DefaultLoadBalancer.java`
#### Snippet
```java
     *
     * @param consumerHolder
     * @param brokerRunManager
     * @param groupSet
     * @param defMetaDataService
```

### JavadocDeclaration
`@param groupSet` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/balance/DefaultLoadBalancer.java`
#### Snippet
```java
     * @param consumerHolder
     * @param brokerRunManager
     * @param groupSet
     * @param defMetaDataService
     * @param strBuffer
```

### JavadocDeclaration
`@param defMetaDataService` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/balance/DefaultLoadBalancer.java`
#### Snippet
```java
     * @param brokerRunManager
     * @param groupSet
     * @param defMetaDataService
     * @param strBuffer
     * @return
```

### JavadocDeclaration
`@param strBuffer` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/balance/DefaultLoadBalancer.java`
#### Snippet
```java
     * @param groupSet
     * @param defMetaDataService
     * @param strBuffer
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/balance/DefaultLoadBalancer.java`
#### Snippet
```java
     * @param defMetaDataService
     * @param strBuffer
     * @return
     */
    @Override
```

### JavadocDeclaration
`@param clusterState` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/balance/DefaultLoadBalancer.java`
#### Snippet
```java
     * Load balance
     *
     * @param clusterState
     * @param consumerHolder
     * @param brokerRunManager
```

### JavadocDeclaration
`@param consumerHolder` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/balance/DefaultLoadBalancer.java`
#### Snippet
```java
     *
     * @param clusterState
     * @param consumerHolder
     * @param brokerRunManager
     * @param groupSet
```

### JavadocDeclaration
`@param brokerRunManager` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/balance/DefaultLoadBalancer.java`
#### Snippet
```java
     * @param clusterState
     * @param consumerHolder
     * @param brokerRunManager
     * @param groupSet
     * @param defMetaDataService
```

### JavadocDeclaration
`@param groupSet` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/balance/DefaultLoadBalancer.java`
#### Snippet
```java
     * @param consumerHolder
     * @param brokerRunManager
     * @param groupSet
     * @param defMetaDataService
     * @param strBuffer
```

### JavadocDeclaration
`@param defMetaDataService` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/balance/DefaultLoadBalancer.java`
#### Snippet
```java
     * @param brokerRunManager
     * @param groupSet
     * @param defMetaDataService
     * @param strBuffer
     * @return
```

### JavadocDeclaration
`@param strBuffer` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/balance/DefaultLoadBalancer.java`
#### Snippet
```java
     * @param groupSet
     * @param defMetaDataService
     * @param strBuffer
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/balance/DefaultLoadBalancer.java`
#### Snippet
```java
     * @param defMetaDataService
     * @param strBuffer
     * @return
     */
    @Override
```

### JavadocDeclaration
`@param consumerHolder` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/balance/DefaultLoadBalancer.java`
#### Snippet
```java
     * Assign consumer partitions
     *
     * @param consumerHolder
     * @param brokerRunManager
     * @param groupSet
```

### JavadocDeclaration
`@param brokerRunManager` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/balance/DefaultLoadBalancer.java`
#### Snippet
```java
     *
     * @param consumerHolder
     * @param brokerRunManager
     * @param groupSet
     * @param defMetaDataService
```

### JavadocDeclaration
`@param groupSet` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/balance/DefaultLoadBalancer.java`
#### Snippet
```java
     * @param consumerHolder
     * @param brokerRunManager
     * @param groupSet
     * @param defMetaDataService
     * @param strBuffer
```

### JavadocDeclaration
`@param defMetaDataService` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/balance/DefaultLoadBalancer.java`
#### Snippet
```java
     * @param brokerRunManager
     * @param groupSet
     * @param defMetaDataService
     * @param strBuffer
     * @return
```

### JavadocDeclaration
`@param strBuffer` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/balance/DefaultLoadBalancer.java`
#### Snippet
```java
     * @param groupSet
     * @param defMetaDataService
     * @param strBuffer
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/balance/DefaultLoadBalancer.java`
#### Snippet
```java
     * @param defMetaDataService
     * @param strBuffer
     * @return
     */
    @Override
```

### JavadocDeclaration
`@param masterConfig` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/metamanage/metastore/impl/zkimpl/ZKMetaConfigMapperImpl.java`
#### Snippet
```java
    /**
     * Constructor of ZKMetaConfigMapperImpl.
     * @param masterConfig
     */
    public ZKMetaConfigMapperImpl(MasterConfig masterConfig) {
```

### JavadocDeclaration
`@param qryGroupSet` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/nodemanage/nodebroker/TopicPSInfoManager.java`
#### Snippet
```java
     * is empty, then all online consumer groups will be taken.
     *
     * @param qryGroupSet
     * @param subTopicSet
     * @return online groups
```

### JavadocDeclaration
`@param subTopicSet` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/nodemanage/nodebroker/TopicPSInfoManager.java`
#### Snippet
```java
     *
     * @param qryGroupSet
     * @param subTopicSet
     * @return online groups
     */
```

### JavadocDeclaration
`@param masterConfig` tag description is missing
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/metamanage/metastore/impl/bdbimpl/BdbMetaConfigMapperImpl.java`
#### Snippet
```java
    /**
     * Constructor of BdbMetaConfigMapperImpl.
     * @param masterConfig
     */
    public BdbMetaConfigMapperImpl(MasterConfig masterConfig) {
```

### JavadocDeclaration
`@param url` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/MasterServiceImpl.java`
#### Snippet
```java
     * query master to get node info
     *
     * @param url
     * @return query info
     */
```

### JavadocDeclaration
`@param regionId` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/RegionService.java`
#### Snippet
```java
     * delete region and set the brokers in the region to be default region
     *
     * @param regionId
     * @param clusterId
     * @return
```

### JavadocDeclaration
`@param clusterId` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/RegionService.java`
#### Snippet
```java
     *
     * @param regionId
     * @param clusterId
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/RegionService.java`
#### Snippet
```java
     * @param regionId
     * @param clusterId
     * @return
     */
    TubeMQResult deleteRegion(long regionId, long clusterId);
```

### JavadocDeclaration
`@param regionEntry` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/RegionService.java`
#### Snippet
```java
     * create new region with brokers
     *
     * @param regionEntry
     * @param brokerIdList
     * @return
```

### JavadocDeclaration
`@param brokerIdList` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/RegionService.java`
#### Snippet
```java
     *
     * @param regionEntry
     * @param brokerIdList
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/RegionService.java`
#### Snippet
```java
     * @param regionEntry
     * @param brokerIdList
     * @return
     */
    TubeMQResult createNewRegion(RegionEntry regionEntry,
```

### JavadocDeclaration
`@param regionId` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/RegionService.java`
#### Snippet
```java
     * if no regionId is passed return all regions inside the cluster
     *
     * @param regionId
     * @param clusterId
     * @return
```

### JavadocDeclaration
`@param clusterId` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/RegionService.java`
#### Snippet
```java
     *
     * @param regionId
     * @param clusterId
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/RegionService.java`
#### Snippet
```java
     * @param regionId
     * @param clusterId
     * @return
     */
    List<RegionEntry> queryRegion(Long regionId, Long clusterId);
```

### JavadocDeclaration
`@param newRegionEntry` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/RegionService.java`
#### Snippet
```java
     * need to check if other region contains the same brokers
     *
     * @param newRegionEntry
     * @param brokerIdList
     * @param clusterId
```

### JavadocDeclaration
`@param brokerIdList` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/RegionService.java`
#### Snippet
```java
     *
     * @param newRegionEntry
     * @param brokerIdList
     * @param clusterId
     * @return
```

### JavadocDeclaration
`@param clusterId` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/RegionService.java`
#### Snippet
```java
     * @param newRegionEntry
     * @param brokerIdList
     * @param clusterId
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/RegionService.java`
#### Snippet
```java
     * @param brokerIdList
     * @param clusterId
     * @return
     */
    TubeMQResult updateRegion(RegionEntry newRegionEntry, List<Long> brokerIdList, long clusterId);
```

### JavadocDeclaration
`@param clusterId` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/ClusterService.java`
#### Snippet
```java
     * delete cluster by id
     *
     * @param clusterId
     */
    void deleteCluster(Long clusterId);
```

### JavadocDeclaration
`@param clusterName` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/ClusterService.java`
#### Snippet
```java
     * get one cluster by cluster name
     *
     * @param clusterName
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/ClusterService.java`
#### Snippet
```java
     *
     * @param clusterName
     * @return
     */
    ClusterEntry getOneCluster(String clusterName);
```

### JavadocDeclaration
`@param req` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/ClusterService.java`
#### Snippet
```java
     * add cluster and the master node in the cluster
     *
     * @param req
     * @return
     */
```

### JavadocDeclaration
Tag `return` is not allowed here
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/ClusterService.java`
#### Snippet
```java
     *
     * @param req
     * @return
     */
    void addClusterAndMasterNode(AddClusterReq req);
```

### JavadocDeclaration
`@param clusterDto` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/ClusterService.java`
#### Snippet
```java
     * modify cluster with cluster name or id
     *
     * @param clusterDto
     * @return
     */
```

### JavadocDeclaration
`@param masterEntry` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/TopicService.java`
#### Snippet
```java
     * get topic config info
     *
     * @param masterEntry
     * @param topic
     * @return
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/ClusterService.java`
#### Snippet
```java
     *
     * @param clusterDto
     * @return
     */
    TubeMQResult modifyCluster(ClusterDto clusterDto);
```

### JavadocDeclaration
`@param topic` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/TopicService.java`
#### Snippet
```java
     *
     * @param masterEntry
     * @param topic
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/ClusterService.java`
#### Snippet
```java
     * get all clusters
     *
     * @return
     */
    List<ClusterEntry> getAllClusters();
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/TopicService.java`
#### Snippet
```java
     * @param masterEntry
     * @param topic
     * @return
     */
    TubeHttpTopicInfoList requestTopicConfigInfo(MasterEntry masterEntry, String topic);
```

### JavadocDeclaration
`@param clusterId` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/ClusterService.java`
#### Snippet
```java
     * get one cluster
     *
     * @param clusterId
     * @return
     */
```

### JavadocDeclaration
`@param masterEntry` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/TopicService.java`
#### Snippet
```java
     * get consumer group run info
     *
     * @param masterEntry
     * @param group
     * @return
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/ClusterService.java`
#### Snippet
```java
     *
     * @param clusterId
     * @return
     */
    ClusterEntry getOneCluster(long clusterId);
```

### JavadocDeclaration
`@param group` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/TopicService.java`
#### Snippet
```java
     *
     * @param masterEntry
     * @param group
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/TopicService.java`
#### Snippet
```java
     * @param masterEntry
     * @param group
     * @return
     */
    TubeHttpGroupDetailInfo requestGroupRunInfo(MasterEntry masterEntry, String group);
```

### JavadocDeclaration
`@param req` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/TopicService.java`
#### Snippet
```java
     * rebalance group
     *
     * @param req
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/TopicService.java`
#### Snippet
```java
     *
     * @param req
     * @return
     */
    TubeMQResult rebalanceGroup(RebalanceGroupReq req);
```

### JavadocDeclaration
`@param req` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/TopicService.java`
#### Snippet
```java
     * clone offset to other groups
     *
     * @param req
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/TopicService.java`
#### Snippet
```java
     *
     * @param req
     * @return
     */
    TubeMQResult cloneOffsetToOtherGroups(CloneOffsetReq req);
```

### JavadocDeclaration
`@param req` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/TopicService.java`
#### Snippet
```java
     * query if a group exist with a topic
     *
     * @param req
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/TopicService.java`
#### Snippet
```java
     *
     * @param req
     * @return
     */
    TubeMQResult queryGroupExist(QueryConsumerGroupReq req);
```

### JavadocDeclaration
`@param req` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/TopicService.java`
#### Snippet
```java
     * query offset given topic and group name
     *
     * @param req
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/TopicService.java`
#### Snippet
```java
     *
     * @param req
     * @return
     */
    TubeMQResult queryOffset(QueryOffsetReq req);
```

### JavadocDeclaration
`@param url` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/MasterService.java`
#### Snippet
```java
     * request master with request url, return action result (success or fail)
     *
     * @param url
     * @return
     */
```

### JavadocDeclaration
`@param req` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/TopicService.java`
#### Snippet
```java
     * delete offset given topic and broker
     *
     * @param req
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/MasterService.java`
#### Snippet
```java
     *
     * @param url
     * @return
     */
    TubeMQResult requestMaster(String url);
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/TopicService.java`
#### Snippet
```java
     *
     * @param req
     * @return
     */
    TubeMQResult deleteOffset(DeleteOffsetReq req);
```

### JavadocDeclaration
`@param clusterId` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/MasterService.java`
#### Snippet
```java
     * delete master by cluster id
     *
     * @param clusterId
     */
    void deleteMaster(Long clusterId);
```

### JavadocDeclaration
`@param clusterId` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/MasterService.java`
#### Snippet
```java
    /**
     * get master node in one cluster
     * @param clusterId
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/MasterService.java`
#### Snippet
```java
     * get master node in one cluster
     * @param clusterId
     * @return
     */
    List<MasterEntry> getMasterNodes(Long clusterId);
```

### JavadocDeclaration
`@param masterIp` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/MasterService.java`
#### Snippet
```java
    /**
     * get master in master ip
     * @param masterIp
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/MasterService.java`
#### Snippet
```java
     * get master in master ip
     * @param masterIp
     * @return
     */
    List<MasterEntry> getMasterNodes(String masterIp);
```

### JavadocDeclaration
`@param masterIp` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/MasterService.java`
#### Snippet
```java
     * check whether the master node is alive
     *
     * @param masterIp
     * @param masterPort
     * @return
```

### JavadocDeclaration
`@param masterPort` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/MasterService.java`
#### Snippet
```java
     *
     * @param masterIp
     * @param masterPort
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/MasterService.java`
#### Snippet
```java
     * @param masterIp
     * @param masterPort
     * @return
     */
    TubeMQResult checkMasterNodeStatus(String masterIp, Integer masterPort);
```

### JavadocDeclaration
`@param queryBody` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/MasterService.java`
#### Snippet
```java
     * use queryBody to generate queryUrl for master query
     *
     * @param queryBody
     * @return
     *
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/MasterService.java`
#### Snippet
```java
     *
     * @param queryBody
     * @return
     *
     * @throws Exception exception
```

### JavadocDeclaration
`@param url` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/MasterService.java`
#### Snippet
```java
     * query master with query url, return the information returned by master
     *
     * @param url
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/MasterService.java`
#### Snippet
```java
     *
     * @param url
     * @return
     */
    String queryMaster(String url);
```

### JavadocDeclaration
`@param clusterId` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/MasterService.java`
#### Snippet
```java
     * get one master node
     *
     * @param clusterId
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/MasterService.java`
#### Snippet
```java
     *
     * @param clusterId
     * @return
     */
    MasterEntry getMasterNode(Long clusterId);
```

### JavadocDeclaration
`@param req` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/MasterService.java`
#### Snippet
```java
     * get the master node in the cluster
     *
     * @param req
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/MasterService.java`
#### Snippet
```java
     *
     * @param req
     * @return
     */
    MasterEntry getMasterNode(BaseReq req);
```

### JavadocDeclaration
`@param req` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/MasterService.java`
#### Snippet
```java
     * request master with baseReq, return action result (success or fail)
     *
     * @param req
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/MasterService.java`
#### Snippet
```java
     *
     * @param req
     * @return
     */
    TubeMQResult baseRequestMaster(BaseReq req);
```

### JavadocDeclaration
`@param brokerIdList` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/BrokerService.java`
#### Snippet
```java
     * update brokers to be in a region
     *
     * @param brokerIdList
     * @param regionId
     * @param clusterId
```

### JavadocDeclaration
`@param regionId` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/BrokerService.java`
#### Snippet
```java
     *
     * @param brokerIdList
     * @param regionId
     * @param clusterId
     */
```

### JavadocDeclaration
`@param clusterId` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/BrokerService.java`
#### Snippet
```java
     * @param brokerIdList
     * @param regionId
     * @param clusterId
     */
    void updateBrokersRegion(List<Long> brokerIdList, Long regionId, Long clusterId);
```

### JavadocDeclaration
`@param brokerIp` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/BrokerService.java`
#### Snippet
```java
     * delete offset
     *
     * @param brokerIp
     * @param brokerWebPort
     * @param req
```

### JavadocDeclaration
`@param brokerWebPort` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/BrokerService.java`
#### Snippet
```java
     *
     * @param brokerIp
     * @param brokerWebPort
     * @param req
     * @return
```

### JavadocDeclaration
`@param req` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/BrokerService.java`
#### Snippet
```java
     * @param brokerIp
     * @param brokerWebPort
     * @param req
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/BrokerService.java`
#### Snippet
```java
     * @param brokerWebPort
     * @param req
     * @return
     */
    TubeMQResult deleteOffset(String brokerIp, int brokerWebPort,
```

### JavadocDeclaration
`@param regionId` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/BrokerService.java`
#### Snippet
```java
     * get all broker id list in a region
     *
     * @param regionId
     * @param cluster
     * @return
```

### JavadocDeclaration
`@param cluster` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/BrokerService.java`
#### Snippet
```java
     *
     * @param regionId
     * @param cluster
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/BrokerService.java`
#### Snippet
```java
     * @param regionId
     * @param cluster
     * @return
     */
    List<Long> getBrokerIdListInRegion(long regionId, long cluster);
```

### JavadocDeclaration
`@param brokerIp` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/BrokerService.java`
#### Snippet
```java
     * clone offset from one group to another
     *
     * @param brokerIp
     * @param brokerWebPort
     * @param req
```

### JavadocDeclaration
`@param brokerWebPort` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/BrokerService.java`
#### Snippet
```java
     *
     * @param brokerIp
     * @param brokerWebPort
     * @param req
     * @return
```

### JavadocDeclaration
`@param req` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/BrokerService.java`
#### Snippet
```java
     * @param brokerIp
     * @param brokerWebPort
     * @param req
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/BrokerService.java`
#### Snippet
```java
     * @param brokerWebPort
     * @param req
     * @return
     */
    TubeMQResult cloneOffset(String brokerIp, int brokerWebPort, CloneOffsetReq req);
```

### JavadocDeclaration
`@param brokerIdList` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/BrokerService.java`
#### Snippet
```java
     * check if all the brokers exist in this cluster
     *
     * @param brokerIdList
     * @param clusterId
     * @return
```

### JavadocDeclaration
`@param clusterId` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/BrokerService.java`
#### Snippet
```java
     *
     * @param brokerIdList
     * @param clusterId
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/BrokerService.java`
#### Snippet
```java
     * @param brokerIdList
     * @param clusterId
     * @return
     */
    boolean checkIfBrokersAllExist(List<Long> brokerIdList, long clusterId);
```

### JavadocDeclaration
`@param regionId` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/BrokerService.java`
#### Snippet
```java
     * reset the brokers in a region to be default region
     *
     * @param regionId
     * @param clusterId
     */
```

### JavadocDeclaration
`@param clusterId` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/BrokerService.java`
#### Snippet
```java
     *
     * @param regionId
     * @param clusterId
     */
    void resetBrokerRegions(long regionId, long clusterId);
```

### JavadocDeclaration
`@param req` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/NodeServiceImpl.java`
#### Snippet
```java
     * clone source broker to generate brokers with the same config and copy the topics in it.
     *
     * @param req
     * @return
     *
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/NodeServiceImpl.java`
#### Snippet
```java
     *
     * @param req
     * @return
     *
     * @throws Exception exception
```

### JavadocDeclaration
`@param masterEntry` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/NodeServiceImpl.java`
#### Snippet
```java
     * reload broker list, cannot exceed maxConfigurableBrokerSize each time.
     *
     * @param masterEntry
     * @param needReloadList
     */
```

### JavadocDeclaration
`@param needReloadList` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/NodeServiceImpl.java`
#### Snippet
```java
     *
     * @param masterEntry
     * @param needReloadList
     */
    @Override
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/NodeServiceImpl.java`
#### Snippet
```java
     *
     * @param masterEntry - node entry
     * @return
     */
    @Override
```

### JavadocDeclaration
`@param req` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/NodeServiceImpl.java`
#### Snippet
```java
     * if no broker is is provided, topics will be cloned to all brokers in cluster
     *
     * @param req
     * @return
     *
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/NodeServiceImpl.java`
#### Snippet
```java
     *
     * @param req
     * @return
     *
     * @throws Exception exception
```

### JavadocDeclaration
`@param isSuccess` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/NodeServiceImpl.java`
#### Snippet
```java
     * otherwise complete it with exception.
     *
     * @param isSuccess
     * @param topics
     * @param pendingTopic
```

### JavadocDeclaration
`@param topics` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/NodeServiceImpl.java`
#### Snippet
```java
     *
     * @param isSuccess
     * @param topics
     * @param pendingTopic
     */
```

### JavadocDeclaration
`@param pendingTopic` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/NodeServiceImpl.java`
#### Snippet
```java
     * @param isSuccess
     * @param topics
     * @param pendingTopic
     */
    private void handleAddingResult(boolean isSuccess, Set<String> topics,
```

### JavadocDeclaration
`@param masterEntry` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/NodeService.java`
#### Snippet
```java
     * query broker status
     *
     * @param masterEntry
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/NodeService.java`
#### Snippet
```java
     *
     * @param masterEntry
     * @return
     */
    TubeHttpBrokerInfoList requestBrokerStatus(MasterEntry masterEntry);
```

### JavadocDeclaration
`@param masterEntry` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/NodeService.java`
#### Snippet
```java
     * add topics to brokers
     *
     * @param masterEntry
     * @param brokerIds
     * @param addTopicReqs
```

### JavadocDeclaration
`@param brokerIds` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/NodeService.java`
#### Snippet
```java
     *
     * @param masterEntry
     * @param brokerIds
     * @param addTopicReqs
     * @return
```

### JavadocDeclaration
`@param addTopicReqs` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/NodeService.java`
#### Snippet
```java
     * @param masterEntry
     * @param brokerIds
     * @param addTopicReqs
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/NodeService.java`
#### Snippet
```java
     * @param brokerIds
     * @param addTopicReqs
     * @return
     */
    TubeMQResult addTopicsToBrokers(MasterEntry masterEntry, List<Integer> brokerIds,
```

### JavadocDeclaration
`@param masterDto` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/NodeService.java`
#### Snippet
```java
     * modify master node
     *
     * @param masterDto
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/NodeService.java`
#### Snippet
```java
     *
     * @param masterDto
     * @return
     */
    TubeMQResult modifyMasterNode(MasterDto masterDto);
```

### JavadocDeclaration
`@param req` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/NodeService.java`
#### Snippet
```java
     * add one topic to brokers
     *
     * @param req
     * @param masterEntry
     * @return
```

### JavadocDeclaration
`@param masterEntry` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/NodeService.java`
#### Snippet
```java
     *
     * @param req
     * @param masterEntry
     * @return
     *
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/NodeService.java`
#### Snippet
```java
     * @param req
     * @param masterEntry
     * @return
     *
     * @throws Exception exception
```

### JavadocDeclaration
`@param clusterId` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/NodeService.java`
#### Snippet
```java
    /**
     * update broker status
     * @param clusterId
     * @param pendingTopic
     */
```

### JavadocDeclaration
`@param pendingTopic` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/NodeService.java`
#### Snippet
```java
     * update broker status
     * @param clusterId
     * @param pendingTopic
     */
    void updateBrokerStatus(int clusterId, Map<String, TopicFuture> pendingTopic);
```

### JavadocDeclaration
`@param req` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/NodeService.java`
#### Snippet
```java
     * clone brokers with topic in it
     *
     * @param req
     * @return
     *
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/NodeService.java`
#### Snippet
```java
     *
     * @param req
     * @return
     *
     * @throws Exception exception
```

### JavadocDeclaration
`@param masterEntry` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/NodeService.java`
#### Snippet
```java
     * add one node to node repository
     *
     * @param masterEntry
     * @return
     */
```

### JavadocDeclaration
Tag `return` is not allowed here
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/NodeService.java`
#### Snippet
```java
     *
     * @param masterEntry
     * @return
     */
    void addNode(MasterEntry masterEntry);
```

### JavadocDeclaration
`@param req` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/NodeService.java`
#### Snippet
```java
     * clone topic to brokers
     *
     * @param req
     * @return
     *
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/NodeService.java`
#### Snippet
```java
     *
     * @param req
     * @return
     *
     * @throws Exception exception
```

### JavadocDeclaration
`@param masterEntry` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/NodeService.java`
#### Snippet
```java
     * config topics to brokers with max brokers limit
     *
     * @param masterEntry
     * @param topics
     * @param brokerList
```

### JavadocDeclaration
`@param topics` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/NodeService.java`
#### Snippet
```java
     *
     * @param masterEntry
     * @param topics
     * @param brokerList
     * @param maxBrokers
```

### JavadocDeclaration
`@param brokerList` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/NodeService.java`
#### Snippet
```java
     * @param masterEntry
     * @param topics
     * @param brokerList
     * @param maxBrokers
     * @return
```

### JavadocDeclaration
`@param maxBrokers` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/NodeService.java`
#### Snippet
```java
     * @param topics
     * @param brokerList
     * @param maxBrokers
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/interfaces/NodeService.java`
#### Snippet
```java
     * @param brokerList
     * @param maxBrokers
     * @return
     */
    boolean configBrokersForTopics(MasterEntry masterEntry,
```

### JavadocDeclaration
`@param ex` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/controller/ManagerControllerAdvice.java`
#### Snippet
```java
     * handling exception, and return json format string.
     *
     * @param ex
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/controller/ManagerControllerAdvice.java`
#### Snippet
```java
     *
     * @param ex
     * @return
     */
    @ExceptionHandler(Exception.class)
```

### JavadocDeclaration
`@param req` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/controller/group/GroupController.java`
#### Snippet
```java
     * add groups in one batch
     *
     * @param req
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/controller/group/GroupController.java`
#### Snippet
```java
     *
     * @param req
     * @return
     */
    private TubeMQResult batchAddGroup(BatchAddGroupAuthReq req) {
```

### JavadocDeclaration
`@param req` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/controller/group/GroupController.java`
#### Snippet
```java
    /**
     * query group exist
     * @param req
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/controller/group/GroupController.java`
#### Snippet
```java
     * query group exist
     * @param req
     * @return
     */
    private TubeMQResult queryGroupExist(QueryConsumerGroupReq req) {
```

### JavadocDeclaration
`@param req` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/controller/group/GroupController.java`
#### Snippet
```java
     * query the black list for certain topic
     *
     * @param req
     * @return
     *
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/controller/group/GroupController.java`
#### Snippet
```java
     *
     * @param req
     * @return
     *
     * @throws Exception the exception
```

### JavadocDeclaration
`@param req` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/controller/group/GroupController.java`
#### Snippet
```java
     * query the consumer group for certain topic
     *
     * @param req
     * @return
     *
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/controller/group/GroupController.java`
#### Snippet
```java
     *
     * @param req
     * @return
     *
     * @throws Exception the exception
```

### JavadocDeclaration
`@param req` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/controller/topic/TopicWebController.java`
#### Snippet
```java
     * query consumer auth control, shows all consumer groups
     *
     * @param req
     * @return
     *
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/controller/topic/TopicWebController.java`
#### Snippet
```java
     *
     * @param req
     * @return
     *
     * @throws Exception the exception
```

### JavadocDeclaration
`@param req` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/controller/topic/TopicWebController.java`
#### Snippet
```java
     * query topic config info
     *
     * @param req
     * @return
     *
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/controller/topic/TopicWebController.java`
#### Snippet
```java
     *
     * @param req
     * @return
     *
     * @throws Exception the exception
```

### JavadocDeclaration
`@param req` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/controller/region/RegionController.java`
#### Snippet
```java
     * query regions in cluster
     *
     * @param req
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/controller/region/RegionController.java`
#### Snippet
```java
     *
     * @param req
     * @return
     */
    private TubeMQResult queryRegion(QueryRegionReq req) {
```

### JavadocDeclaration
`@param req` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/controller/region/RegionController.java`
#### Snippet
```java
     * delete region
     *
     * @param req
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/controller/region/RegionController.java`
#### Snippet
```java
     *
     * @param req
     * @return
     */
    private TubeMQResult deleteRegion(DeleteRegionReq req) {
```

### JavadocDeclaration
`@param businessName` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/repository/TopicRepository.java`
#### Snippet
```java
     * get one topicEntry by business name
     *
     * @param businessName
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/repository/TopicRepository.java`
#### Snippet
```java
     *
     * @param businessName
     * @return
     */
    TopicEntry findByBusinessName(String businessName);
```

### JavadocDeclaration
`@param clusterId` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/repository/MasterRepository.java`
#### Snippet
```java
     * find all nodes in cluster
     *
     * @param clusterId
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/repository/MasterRepository.java`
#### Snippet
```java
     *
     * @param clusterId
     * @return
     */
    List<MasterEntry> findMasterEntriesByClusterIdEquals(long clusterId);
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/repository/MasterRepository.java`
#### Snippet
```java
     * delete master by cluster id
     *
     * @return
     */
    Integer deleteByClusterId(Long clusterId);
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/repository/MasterRepository.java`
#### Snippet
```java
     * find all nodes in ip
     *
     * @return
     */
    List<MasterEntry> findMasterEntryByIpEquals(String masterIp);
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/repository/MasterRepository.java`
#### Snippet
```java
     * find all nodes
     *
     * @return
     */
    List<MasterEntry> findAll();
```

### JavadocDeclaration
`@param clusterId` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/repository/MasterRepository.java`
#### Snippet
```java
     * find master By clusterId
     *
     * @param clusterId
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/repository/MasterRepository.java`
#### Snippet
```java
     *
     * @param clusterId
     * @return
     */
    MasterEntry findMasterEntryByClusterIdEquals(long clusterId);
```

### JavadocDeclaration
`@param clusterId` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/repository/ClusterRepository.java`
#### Snippet
```java
     * delete cluster by cluster id
     *
     * @param clusterId
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/repository/ClusterRepository.java`
#### Snippet
```java
     *
     * @param clusterId
     * @return
     */
    Integer deleteByClusterId(Long clusterId);
```

### JavadocDeclaration
`@param clusterName` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/repository/ClusterRepository.java`
#### Snippet
```java
    /**
     * find clusterEntry by clusterName
     * @param clusterName
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/repository/ClusterRepository.java`
#### Snippet
```java
     * find clusterEntry by clusterName
     * @param clusterName
     * @return
     */
    ClusterEntry findClusterEntryByClusterName(String clusterName);
```

### JavadocDeclaration
`@param clusterId` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/repository/ClusterRepository.java`
#### Snippet
```java
     * find clusterEntry by clusterId
     *
     * @param clusterId
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/repository/ClusterRepository.java`
#### Snippet
```java
     *
     * @param clusterId
     * @return
     */
    ClusterEntry findClusterEntryByClusterId(long clusterId);
```

### JavadocDeclaration
`@param clusterDto` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/controller/cluster/ClusterController.java`
#### Snippet
```java
     * change cluster info
     *
     * @param clusterDto
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/controller/cluster/ClusterController.java`
#### Snippet
```java
     *
     * @param clusterDto
     * @return
     */
    private TubeMQResult changeCluster(ClusterDto clusterDto) {
```

### JavadocDeclaration
`@param clusterId` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/controller/cluster/ClusterController.java`
#### Snippet
```java
     * query cluster info, if no clusterId is passed, return all clusters
     *
     * @param clusterId
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/controller/cluster/ClusterController.java`
#### Snippet
```java
     *
     * @param clusterId
     * @return
     */
    @RequestMapping(value = "", method = RequestMethod.GET, produces = MediaType.APPLICATION_JSON_VALUE)
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/controller/cluster/ClusterController.java`
#### Snippet
```java
     * get all cluster info
     *
     * @return
     */
    private TubeMQResult queryAllClusterVo() {
```

### JavadocDeclaration
`@param clusterId` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/controller/cluster/ClusterController.java`
#### Snippet
```java
     * query Consumer group count
     *
     * @param clusterId
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/controller/cluster/ClusterController.java`
#### Snippet
```java
     *
     * @param clusterId
     * @return
     */
    public int getConsumerGroupCount(Integer clusterId) {
```

### JavadocDeclaration
`@param clusterId` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/controller/cluster/ClusterController.java`
#### Snippet
```java
     * query borker size
     *
     * @param clusterId
     */
    public int getBrokerSize(Integer clusterId) {
```

### JavadocDeclaration
`@param clusterId` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/controller/cluster/ClusterController.java`
#### Snippet
```java
     * query topic and partition count
     *
     * @param clusterId
     */
    public ClusterVo getTopicAndPartitionCount(Integer clusterId) {
```

### JavadocDeclaration
`@param clusterId` tag description is missing
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/controller/cluster/ClusterController.java`
#### Snippet
```java
     * get all count
     *
     * @param clusterId
     */
    public ClusterVo getCountInCluster(Integer clusterId) {
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-tubemq/tubemq-connectors/tubemq-connector-flume/src/main/java/org/apache/flume/sink/tubemq/TubemqSink.java`
#### Snippet
```java
     * Create producer
     *
     * @throws FlumeException
     */
    private void createConnection() throws FlumeException {
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-tubemq/tubemq-connectors/tubemq-connector-flume/src/main/java/org/apache/flume/sink/tubemq/TubemqSink.java`
#### Snippet
```java
         *
         * @return EventStat
         * @throws Exception
         */
        private EventStat fetchEventStat() throws Exception {
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-tubemq/tubemq-connectors/tubemq-connector-flume/src/main/java/org/apache/flume/sink/tubemq/TubemqSink.java`
#### Snippet
```java
     * @param topic - topic name
     * @return MessageProducer
     * @throws TubeClientException
     */
    private MessageProducer getProducer(String topic) throws TubeClientException {
```

### JavadocDeclaration
`@param groupInfo` tag description is missing
in `inlong-manager/manager-pojo/src/main/java/org/apache/inlong/manager/pojo/group/InlongGroupUtils.java`
#### Snippet
```java
     * Check if group is batch task.
     *
     * @param groupInfo
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-manager/manager-pojo/src/main/java/org/apache/inlong/manager/pojo/group/InlongGroupUtils.java`
#### Snippet
```java
     *
     * @param groupInfo
     * @return
     */
    public static boolean isBatchTask(InlongGroupInfo groupInfo) {
```

### JavadocDeclaration
`@param options` tag description is missing
in `inlong-manager/manager-pojo/src/main/java/org/apache/inlong/manager/pojo/sort/util/ExtractNodeUtils.java`
#### Snippet
```java
     * Parse LookupOptions
     *
     * @param options
     * @return LookupOptions
     */
```

### JavadocDeclaration
`@param clusterRequest` tag description is missing
in `inlong-manager/manager-client/src/main/java/org/apache/inlong/manager/client/api/InlongCluster.java`
#### Snippet
```java
     * Save DATA_PROXY|PULSAR|TUBE cluster
     *
     * @param clusterRequest
     * @return clusterInfo
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-manager/manager-client/src/main/java/org/apache/inlong/manager/client/api/InlongClient.java`
#### Snippet
```java
     *
     * @return cluster Api
     * @throws Exception
     */
    InlongCluster cluster() throws Exception;
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-manager/manager-client/src/main/java/org/apache/inlong/manager/client/api/InlongGroup.java`
#### Snippet
```java
     *         when rerun = 0
     * @return group info
     * @throws Exception
     */
    InlongGroupContext reset(int rerun, int resetFinalStatus) throws Exception;
```

### JavadocDeclaration
`@param streamSink` tag description is missing
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/sink/StreamSinkOperator.java`
#### Snippet
```java
     * Parse stream sink to id params
     *
     * @param streamSink
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/sink/StreamSinkOperator.java`
#### Snippet
```java
     *
     * @param streamSink
     * @return
     */
    Map<String, String> parse2IdParams(StreamSinkEntity streamSink, List<String> fields);
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/cluster/PulsarClusterOperator.java`
#### Snippet
```java
     *
     * @param pulsarInfo pulsar cluster info
     * @return
     */
    private Boolean testConnectAdminUrl(PulsarClusterInfo pulsarInfo) {
```

### JavadocDeclaration
IOException is not declared to be thrown by method getMappingInfo
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sink/es/ElasticsearchApi.java`
#### Snippet
```java
     * @param fieldsInfo The fields info of Elasticsearch
     * @return String list of fields translation
     * @throws IOException The exception may throws
     */
    private List<String> getMappingInfo(List<ElasticsearchFieldInfo> fieldsInfo) {
```

### JavadocDeclaration
`@param schemaName` tag description is missing
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sink/greenplum/GreenplumSqlBuilder.java`
#### Snippet
```java
     * Build SQL to check whether the schema exists.
     *
     * @param schemaName
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sink/greenplum/GreenplumSqlBuilder.java`
#### Snippet
```java
     *
     * @param schemaName
     * @return
     */
    public static String getCheckSchema(final String schemaName) {
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sink/sqlserver/SQLServerSqlBuilder.java`
#### Snippet
```java
     *
     * @param schemaName SQLServer schema name
     * @return
     */
    public static String buildCreateSchemaSql(String schemaName) {
```

### JavadocDeclaration
`@param schemaName` tag description is missing
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sink/sqlserver/SQLServerSqlBuilder.java`
#### Snippet
```java
     * Build SQL to check whether the schema exists.
     *
     * @param schemaName
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sink/sqlserver/SQLServerSqlBuilder.java`
#### Snippet
```java
     *
     * @param schemaName
     * @return
     */
    public static String getCheckSchema(String schemaName) {
```

### JavadocDeclaration
`@param schemaName` tag description is missing
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sink/postgresql/PostgreSQLSqlBuilder.java`
#### Snippet
```java
     * Build SQL to check whether the schema exists.
     *
     * @param schemaName
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sink/postgresql/PostgreSQLSqlBuilder.java`
#### Snippet
```java
     *
     * @param schemaName
     * @return
     */
    public static String getCheckSchema(final String schemaName) {
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/heartbeat/HeartbeatManager.java`
#### Snippet
```java
     * @param oldHB last heartbeat msg
     * @param newHB current heartbeat msg
     * @return
     */
    private static boolean heartbeatConfigModified(HeartbeatMsg oldHB, HeartbeatMsg newHB) {
```

### JavadocDeclaration
`@param totalCount` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/BatchPackProfileCallback.java`
#### Snippet
```java
    /**
     * Constructor
     * @param totalCount
     * @param callback
     */
```

### JavadocDeclaration
`@param callback` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/BatchPackProfileCallback.java`
#### Snippet
```java
     * Constructor
     * @param totalCount
     * @param callback
     */
    public BatchPackProfileCallback(int totalCount, SourceCallback callback) {
```

### JavadocDeclaration
`@param eventCount` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/BatchPackProfileCallback.java`
#### Snippet
```java
    /**
     * ack
     * @param eventCount
     */
    public void ack(int eventCount) {
```

### JavadocDeclaration
`@param event` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/MessageQueueClusterProducer.java`
#### Snippet
```java
     * send
     * 
     * @param event
     */
    public boolean send(BatchPackProfile event) {
```

### JavadocDeclaration
`@param workerName` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/MessageQueueClusterProducer.java`
#### Snippet
```java
     * Constructor
     * 
     * @param workerName
     * @param config
     * @param context
```

### JavadocDeclaration
`@param event` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/MessageQueueZoneProducer.java`
#### Snippet
```java
     * send
     * 
     * @param event
     */
    public boolean send(BatchPackProfile event) {
```

### JavadocDeclaration
`@param workerName` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/MessageQueueZoneProducer.java`
#### Snippet
```java
     * Constructor
     * 
     * @param workerName
     * @param context
     */
```

### JavadocDeclaration
`@param context` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/MessageQueueZoneProducer.java`
#### Snippet
```java
     * 
     * @param workerName
     * @param context
     */
    public MessageQueueZoneProducer(String workerName, MessageQueueZoneSinkContext context) {
```

### JavadocDeclaration
`@param config` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/MessageQueueClusterProducer.java`
#### Snippet
```java
     * 
     * @param workerName
     * @param config
     * @param context
     */
```

### JavadocDeclaration
`@param context` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/MessageQueueClusterProducer.java`
#### Snippet
```java
     * @param workerName
     * @param config
     * @param context
     */
    public MessageQueueClusterProducer(String workerName, CacheClusterConfig config,
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/MessageQueueClusterProducer.java`
#### Snippet
```java
     * getLifecycleState
     * 
     * @return
     */
    @Override
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/BatchPackProfile.java`
#### Snippet
```java
    /**
     * isResend
     * @return
     */
    public boolean isResend() {
```

### JavadocDeclaration
`@param event` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/BatchPackProfile.java`
#### Snippet
```java
     * addEvent
     * 
     * @param  event
     * @param  maxPackCount
     * @param  maxPackSize
```

### JavadocDeclaration
`@param maxPackCount` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/BatchPackProfile.java`
#### Snippet
```java
     * 
     * @param  event
     * @param  maxPackCount
     * @param  maxPackSize
     * @return
```

### JavadocDeclaration
`@param maxPackSize` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/BatchPackProfile.java`
#### Snippet
```java
     * @param  event
     * @param  maxPackCount
     * @param  maxPackSize
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/BatchPackProfile.java`
#### Snippet
```java
     * @param  maxPackCount
     * @param  maxPackSize
     * @return
     */
    public boolean addEvent(ProxyEvent event, long maxPackCount, long maxPackSize) {
```

### JavadocDeclaration
`@param createThreshold` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/BatchPackProfile.java`
#### Snippet
```java
     * isTimeout
     * 
     * @param  createThreshold
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/BatchPackProfile.java`
#### Snippet
```java
     * 
     * @param  createThreshold
     * @return
     */
    public boolean isTimeout(long createThreshold) {
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/BatchPackProfile.java`
#### Snippet
```java
     * getDispatchTime
     * 
     * @return
     */
    public long getDispatchTime() {
```

### JavadocDeclaration
`@param uid` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/BatchPackProfile.java`
#### Snippet
```java
     * Constructor
     * 
     * @param uid
     * @param inlongGroupId
     * @param inlongStreamId
```

### JavadocDeclaration
`@param inlongGroupId` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/BatchPackProfile.java`
#### Snippet
```java
     * 
     * @param uid
     * @param inlongGroupId
     * @param inlongStreamId
     * @param dispatchTime
```

### JavadocDeclaration
`@param inlongStreamId` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/BatchPackProfile.java`
#### Snippet
```java
     * @param uid
     * @param inlongGroupId
     * @param inlongStreamId
     * @param dispatchTime
     */
```

### JavadocDeclaration
`@param dispatchTime` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/BatchPackProfile.java`
#### Snippet
```java
     * @param inlongGroupId
     * @param inlongStreamId
     * @param dispatchTime
     */
    public BatchPackProfile(String uid, String inlongGroupId, String inlongStreamId, long dispatchTime) {
```

### JavadocDeclaration
Tag `return` is not allowed here
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/BatchPackProfile.java`
#### Snippet
```java
    /**
     * fail
     * @return
     */
    public void fail() {
```

### JavadocDeclaration
`@param allClusterList` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/AllCacheClusterSelector.java`
#### Snippet
```java
    /**
     * select
     * @param allClusterList
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/AllCacheClusterSelector.java`
#### Snippet
```java
     * select
     * @param allClusterList
     * @return
     */
    @Override
```

### JavadocDeclaration
`@param event` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/MessageQueueHandler.java`
#### Snippet
```java
    /**
     * send
     * @param event
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/MessageQueueHandler.java`
#### Snippet
```java
     * send
     * @param event
     * @return
     */
    boolean send(BatchPackProfile event);
```

### JavadocDeclaration
`@param config` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/MessageQueueHandler.java`
#### Snippet
```java
    /**
     * init
     * @param config
     * @param sinkContext
     */
```

### JavadocDeclaration
`@param sinkContext` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/MessageQueueHandler.java`
#### Snippet
```java
     * init
     * @param config
     * @param sinkContext
     */
    void init(CacheClusterConfig config, MessageQueueZoneSinkContext sinkContext);
```

### JavadocDeclaration
`@param allClusterList` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/RandomCacheClusterSelector.java`
#### Snippet
```java
    /**
     * select
     * @param allClusterList
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/RandomCacheClusterSelector.java`
#### Snippet
```java
     * select
     * @param allClusterList
     * @return
     */
    @Override
```

### JavadocDeclaration
`@param event` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/SimpleBatchPackProfileV0.java`
#### Snippet
```java
    /**
     * create
     * @param event
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/SimpleBatchPackProfileV0.java`
#### Snippet
```java
     * create
     * @param event
     * @return
     */
    public static SimpleBatchPackProfileV0 create(Event event) {
```

### JavadocDeclaration
`@param uid` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/SimpleBatchPackProfileV0.java`
#### Snippet
```java
    /**
     * Constructor
     * @param uid
     * @param inlongGroupId
     * @param inlongStreamId
```

### JavadocDeclaration
`@param inlongGroupId` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/SimpleBatchPackProfileV0.java`
#### Snippet
```java
     * Constructor
     * @param uid
     * @param inlongGroupId
     * @param inlongStreamId
     * @param dispatchTime
```

### JavadocDeclaration
`@param inlongStreamId` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/SimpleBatchPackProfileV0.java`
#### Snippet
```java
     * @param uid
     * @param inlongGroupId
     * @param inlongStreamId
     * @param dispatchTime
     */
```

### JavadocDeclaration
`@param dispatchTime` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/SimpleBatchPackProfileV0.java`
#### Snippet
```java
     * @param inlongGroupId
     * @param inlongStreamId
     * @param dispatchTime
     */
    public SimpleBatchPackProfileV0(String uid, String inlongGroupId, String inlongStreamId, long dispatchTime) {
```

### JavadocDeclaration
`@param backattrs` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/OrderBatchPackProfileV0.java`
#### Snippet
```java
     * Convert String to ByteBuf
     *
     * @param backattrs
     * @param msgType message type
     * @param sequenceId sequence Id
```

### JavadocDeclaration
`@param uid` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/OrderBatchPackProfileV0.java`
#### Snippet
```java
    /**
     * Constructor
     * @param uid
     * @param inlongGroupId
     * @param inlongStreamId
```

### JavadocDeclaration
`@param inlongGroupId` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/OrderBatchPackProfileV0.java`
#### Snippet
```java
     * Constructor
     * @param uid
     * @param inlongGroupId
     * @param inlongStreamId
     * @param dispatchTime
```

### JavadocDeclaration
`@param inlongStreamId` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/OrderBatchPackProfileV0.java`
#### Snippet
```java
     * @param uid
     * @param inlongGroupId
     * @param inlongStreamId
     * @param dispatchTime
     */
```

### JavadocDeclaration
`@param dispatchTime` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/OrderBatchPackProfileV0.java`
#### Snippet
```java
     * @param inlongGroupId
     * @param inlongStreamId
     * @param dispatchTime
     */
    public OrderBatchPackProfileV0(String uid, String inlongGroupId, String inlongStreamId, long dispatchTime) {
```

### JavadocDeclaration
`@param event` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/OrderBatchPackProfileV0.java`
#### Snippet
```java
    /**
     * create
     * @param event
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/OrderBatchPackProfileV0.java`
#### Snippet
```java
     * create
     * @param event
     * @return
     */
    public static OrderBatchPackProfileV0 create(SinkRspEvent event) {
```

### JavadocDeclaration
`@param context` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/MessageQueueZoneSink.java`
#### Snippet
```java
     * configure
     * 
     * @param context
     */
    @Override
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/MessageQueueZoneSink.java`
#### Snippet
```java
     * 
     * @return                        Status
     * @throws EventDeliveryException
     */
    @Override
```

### JavadocDeclaration
Tag `return` is not allowed here
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/BatchPackManager.java`
#### Snippet
```java
     * outputOvertimeData
     * 
     * @return
     */
    public void outputOvertimeData() {
```

### JavadocDeclaration
`@param context` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/BatchPackManager.java`
#### Snippet
```java
     * Constructor
     * 
     * @param context
     * @param dispatchQueue
     */
```

### JavadocDeclaration
`@param dispatchQueue` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/BatchPackManager.java`
#### Snippet
```java
     * 
     * @param context
     * @param dispatchQueue
     */
    public BatchPackManager(Context context, BufferQueue<BatchPackProfile> dispatchQueue) {
```

### JavadocDeclaration
`@param packEvent` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/BatchPackManager.java`
#### Snippet
```java
    /**
     * addPackEvent
     * @param packEvent
     */
    public void addPackEvent(ProxyPackEvent packEvent) {
```

### JavadocDeclaration
`@param event` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/BatchPackManager.java`
#### Snippet
```java
    /**
     * addEvent
     * @param event
     */
    public void addEvent(ProxyEvent event) {
```

### JavadocDeclaration
`@param event` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/BatchPackManager.java`
#### Snippet
```java
    /**
     * addSimpleEvent
     * @param event
     */
    public void addSimpleEvent(SimpleEvent event) {
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/common/SinkContext.java`
#### Snippet
```java
    /**
     * createBufferQueue
     * @return
     */
    public static BufferQueue<BatchPackProfile> createBufferQueue() {
```

### JavadocDeclaration
`@param maxSize` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/utils/SizeSemaphore.java`
#### Snippet
```java
     * Constructor
     * 
     * @param maxSize
     * @param leftSize
     */
```

### JavadocDeclaration
`@param leftSize` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/utils/SizeSemaphore.java`
#### Snippet
```java
     * 
     * @param maxSize
     * @param leftSize
     */
    public SizeSemaphore(int maxSize, int leftSize) {
```

### JavadocDeclaration
`@param port` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/utils/ConfStringUtils.java`
#### Snippet
```java
    /**
     * isValidPort
     * @param port
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/utils/ConfStringUtils.java`
#### Snippet
```java
     * isValidPort
     * @param port
     * @return
     */
    public static boolean isValidPort(int port) {
```

### JavadocDeclaration
`@param ip` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/utils/ConfStringUtils.java`
#### Snippet
```java
    /**
     * isValidIp
     * @param ip
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/utils/ConfStringUtils.java`
#### Snippet
```java
     * isValidIp
     * @param ip
     * @return
     */
    public static boolean isValidIp(String ip) {
```

### JavadocDeclaration
`@param maxSizeKb` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/utils/BufferQueue.java`
#### Snippet
```java
     * Constructor
     * 
     * @param maxSizeKb
     * @param globalTokens
     */
```

### JavadocDeclaration
`@param globalTokens` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/utils/BufferQueue.java`
#### Snippet
```java
     * 
     * @param maxSizeKb
     * @param globalTokens
     */
    public BufferQueue(int maxSizeKb, SizeSemaphore globalTokens) {
```

### JavadocDeclaration
`@param maxSizeKb` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/utils/BufferQueue.java`
#### Snippet
```java
     * Constructor
     * 
     * @param maxSizeKb
     */
    public BufferQueue(int maxSizeKb) {
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/tube/TubeHandler.java`
#### Snippet
```java
    /**
     * initTubeConfig
     * @return
     *
     * @throws Exception
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/tube/TubeHandler.java`
#### Snippet
```java
     * @return
     *
     * @throws Exception
     */
    private TubeClientConfig initTubeConfig() throws Exception {
```

### JavadocDeclaration
`@param config` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/tube/TubeHandler.java`
#### Snippet
```java
    /**
     * init
     * @param config
     * @param sinkContext
     */
```

### JavadocDeclaration
`@param sinkContext` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/tube/TubeHandler.java`
#### Snippet
```java
     * init
     * @param config
     * @param sinkContext
     */
    @Override
```

### JavadocDeclaration
`@param context` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/admin/ProxyServiceAdminEventHandler.java`
#### Snippet
```java
     * configure
     *
     * @param context
     */
    @Override
```

### JavadocDeclaration
`@param cmd` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/admin/ProxyServiceAdminEventHandler.java`
#### Snippet
```java
     * process
     *
     * @param cmd
     * @param event
     * @param response
```

### JavadocDeclaration
`@param event` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/admin/ProxyServiceAdminEventHandler.java`
#### Snippet
```java
     *
     * @param cmd
     * @param event
     * @param response
     */
```

### JavadocDeclaration
`@param response` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/admin/ProxyServiceAdminEventHandler.java`
#### Snippet
```java
     * @param cmd
     * @param event
     * @param response
     */
    @Override
```

### JavadocDeclaration
`@param cmd` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/admin/ProxyServiceAdminEventHandler.java`
#### Snippet
```java
     * processAll
     *
     * @param cmd
     * @param event
     * @param response
```

### JavadocDeclaration
`@param event` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/admin/ProxyServiceAdminEventHandler.java`
#### Snippet
```java
     *
     * @param cmd
     * @param event
     * @param response
     */
```

### JavadocDeclaration
`@param response` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/admin/ProxyServiceAdminEventHandler.java`
#### Snippet
```java
     * @param cmd
     * @param event
     * @param response
     */
    private void processAll(String cmd, Event event, HttpServletResponse response) {
```

### JavadocDeclaration
`@param cmd` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/admin/ProxyServiceAdminEventHandler.java`
#### Snippet
```java
     * processOne
     *
     * @param cmd
     * @param sourceName
     * @param response
```

### JavadocDeclaration
`@param sourceName` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/admin/ProxyServiceAdminEventHandler.java`
#### Snippet
```java
     *
     * @param cmd
     * @param sourceName
     * @param response
     */
```

### JavadocDeclaration
`@param response` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/admin/ProxyServiceAdminEventHandler.java`
#### Snippet
```java
     * @param cmd
     * @param sourceName
     * @param response
     */
    private void processOne(String cmd, String sourceName, HttpServletResponse response) {
```

### JavadocDeclaration
`@param event` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/kafka/KafkaHandler.java`
#### Snippet
```java
    /**
     * send
     * @param event
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/kafka/KafkaHandler.java`
#### Snippet
```java
     * send
     * @param event
     * @return
     */
    @Override
```

### JavadocDeclaration
`@param config` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/kafka/KafkaHandler.java`
#### Snippet
```java
    /**
     * init
     * @param config
     * @param sinkContext
     */
```

### JavadocDeclaration
`@param sinkContext` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/kafka/KafkaHandler.java`
#### Snippet
```java
     * init
     * @param config
     * @param sinkContext
     */
    @Override
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/common/TubeProducerHolder.java`
#### Snippet
```java
     * @return  the producer
     *          if topic is illegal, return null
     * @throws  TubeClientException
     */
    public MessageProducer getProducer(String topicName) throws TubeClientException {
```

### JavadocDeclaration
`@param commonProperties` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/config/DefaultManagerIpListParser.java`
#### Snippet
```java
     * setCommonProperties
     * 
     * @param commonProperties
     */
    @Override
```

### JavadocDeclaration
`@param value` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/config/pojo/CacheType.java`
#### Snippet
```java
     * convert
     *
     * @param  value
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/config/pojo/CacheType.java`
#### Snippet
```java
     *
     * @param  value
     * @return
     */
    public static CacheType convert(String value) {
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/config/pojo/CacheType.java`
#### Snippet
```java
     * value
     *
     * @return
     */
    public String value() {
```

### JavadocDeclaration
`@param value` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/config/pojo/CacheType.java`
#### Snippet
```java
     * Constructor
     * 
     * @param value
     */
    private CacheType(String value) {
```

### JavadocDeclaration
`@param inlongGroupId` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/config/pojo/IdTopicConfig.java`
#### Snippet
```java
    /**
     * generateUid
     * @param  inlongGroupId
     * @param  inlongStreamId
     * @return
```

### JavadocDeclaration
`@param inlongStreamId` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/config/pojo/IdTopicConfig.java`
#### Snippet
```java
     * generateUid
     * @param  inlongGroupId
     * @param  inlongStreamId
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/config/pojo/IdTopicConfig.java`
#### Snippet
```java
     * @param  inlongGroupId
     * @param  inlongStreamId
     * @return
     */
    public static String generateUid(String inlongGroupId, String inlongStreamId) {
```

### JavadocDeclaration
`@param value` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/config/pojo/DataType.java`
#### Snippet
```java
     * Constructor
     * 
     * @param value
     */
    private DataType(String value) {
```

### JavadocDeclaration
`@param value` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/config/pojo/DataType.java`
#### Snippet
```java
     * convert
     *
     * @param  value
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/config/pojo/DataType.java`
#### Snippet
```java
     *
     * @param  value
     * @return
     */
    public static DataType convert(String value) {
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/config/pojo/DataType.java`
#### Snippet
```java
     * value
     *
     * @return
     */
    public String value() {
```

### JavadocDeclaration
`@param event` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/pulsar/PulsarHandler.java`
#### Snippet
```java
    /**
     * send
     * @param event
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/pulsar/PulsarHandler.java`
#### Snippet
```java
     * send
     * @param event
     * @return
     */
    @Override
```

### JavadocDeclaration
`@param config` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/pulsar/PulsarHandler.java`
#### Snippet
```java
    /**
     * init
     * @param config
     * @param sinkContext
     */
```

### JavadocDeclaration
`@param sinkContext` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/pulsar/PulsarHandler.java`
#### Snippet
```java
     * init
     * @param config
     * @param sinkContext
     */
    public void init(CacheClusterConfig config, MessageQueueZoneSinkContext sinkContext) {
```

### JavadocDeclaration
`@param context` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/config/CommonConfigHolder.java`
#### Snippet
```java
     * getStringFromContext
     *
     * @param context
     * @param key
     * @param defaultValue
```

### JavadocDeclaration
`@param key` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/config/CommonConfigHolder.java`
#### Snippet
```java
     *
     * @param context
     * @param key
     * @param defaultValue
     * @return
```

### JavadocDeclaration
`@param defaultValue` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/config/CommonConfigHolder.java`
#### Snippet
```java
     * @param context
     * @param key
     * @param defaultValue
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/config/CommonConfigHolder.java`
#### Snippet
```java
     * @param key
     * @param defaultValue
     * @return
     */
    public static String getStringFromContext(Context context, String key, String defaultValue) {
```

### JavadocDeclaration
`@param context` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/config/loader/ManagerCacheClusterConfigLoader.java`
#### Snippet
```java
     * configure
     * 
     * @param context
     */
    @Override
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/config/loader/ManagerCacheClusterConfigLoader.java`
#### Snippet
```java
     * load
     * 
     * @return
     */
    @Override
```

### JavadocDeclaration
`@param context` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/config/holder/CacheClusterConfigHolder.java`
#### Snippet
```java
     * configure
     * 
     * @param context
     */
    @Override
```

### JavadocDeclaration
`@param context` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/config/loader/ManagerIdTopicConfigLoader.java`
#### Snippet
```java
    /**
     * configure
     * @param context
     */
    @Override
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/config/loader/ManagerIdTopicConfigLoader.java`
#### Snippet
```java
    /**
     * load
     * @return
     */
    @Override
```

### JavadocDeclaration
`@param context` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/config/loader/ContextIdTopicConfigLoader.java`
#### Snippet
```java
     * configure
     * 
     * @param context
     */
    @Override
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/config/loader/ContextIdTopicConfigLoader.java`
#### Snippet
```java
     * load
     * 
     * @return
     */
    @Override
```

### JavadocDeclaration
`@param context` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/config/holder/IdTopicConfigHolder.java`
#### Snippet
```java
    /**
     * configure
     * @param context
     */
    @Override
```

### JavadocDeclaration
`@param uid` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/config/holder/IdTopicConfigHolder.java`
#### Snippet
```java
    /**
     * getTopic
     * @param  uid
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/config/holder/IdTopicConfigHolder.java`
#### Snippet
```java
     * getTopic
     * @param  uid
     * @return
     */
    public String getTopic(String uid) {
```

### JavadocDeclaration
`@param context` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/config/loader/ContextCacheClusterConfigLoader.java`
#### Snippet
```java
     * configure
     * 
     * @param context
     */
    @Override
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/config/loader/ContextCacheClusterConfigLoader.java`
#### Snippet
```java
     * load
     * 
     * @return
     */
    @Override
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/config/RemoteConfigManager.java`
#### Snippet
```java
     * getZone
     *
     * @return
     */
    public String getZone() {
```

### JavadocDeclaration
`@param source` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/ServerMessageFactory.java`
#### Snippet
```java
     * get server factory
     *
     * @param source
     * @param allChannels
     * @param protocol
```

### JavadocDeclaration
`@param allChannels` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/ServerMessageFactory.java`
#### Snippet
```java
     *
     * @param source
     * @param allChannels
     * @param protocol
     * @param serviceDecoder
```

### JavadocDeclaration
`@param protocol` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/ServerMessageFactory.java`
#### Snippet
```java
     * @param source
     * @param allChannels
     * @param protocol
     * @param serviceDecoder
     * @param messageHandlerName
```

### JavadocDeclaration
`@param serviceDecoder` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/ServerMessageFactory.java`
#### Snippet
```java
     * @param allChannels
     * @param protocol
     * @param serviceDecoder
     * @param messageHandlerName
     * @param topic
```

### JavadocDeclaration
`@param messageHandlerName` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/ServerMessageFactory.java`
#### Snippet
```java
     * @param protocol
     * @param serviceDecoder
     * @param messageHandlerName
     * @param topic
     * @param attr
```

### JavadocDeclaration
`@param topic` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/ServerMessageFactory.java`
#### Snippet
```java
     * @param serviceDecoder
     * @param messageHandlerName
     * @param topic
     * @param attr
     * @param filterEmptyMsg
```

### JavadocDeclaration
`@param attr` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/ServerMessageFactory.java`
#### Snippet
```java
     * @param messageHandlerName
     * @param topic
     * @param attr
     * @param filterEmptyMsg
     * @param maxCons
```

### JavadocDeclaration
`@param filterEmptyMsg` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/ServerMessageFactory.java`
#### Snippet
```java
     * @param topic
     * @param attr
     * @param filterEmptyMsg
     * @param maxCons
     * @param isCompressed
```

### JavadocDeclaration
`@param maxCons` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/ServerMessageFactory.java`
#### Snippet
```java
     * @param attr
     * @param filterEmptyMsg
     * @param maxCons
     * @param isCompressed
     * @param monitorIndex
```

### JavadocDeclaration
`@param isCompressed` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/ServerMessageFactory.java`
#### Snippet
```java
     * @param filterEmptyMsg
     * @param maxCons
     * @param isCompressed
     * @param monitorIndex
     * @param monitorIndexExt
```

### JavadocDeclaration
`@param monitorIndex` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/ServerMessageFactory.java`
#### Snippet
```java
     * @param maxCons
     * @param isCompressed
     * @param monitorIndex
     * @param monitorIndexExt
     * @param name
```

### JavadocDeclaration
`@param monitorIndexExt` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/ServerMessageFactory.java`
#### Snippet
```java
     * @param isCompressed
     * @param monitorIndex
     * @param monitorIndexExt
     * @param name
     */
```

### JavadocDeclaration
`@param name` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/ServerMessageFactory.java`
#### Snippet
```java
     * @param monitorIndex
     * @param monitorIndexExt
     * @param name
     */
    public ServerMessageFactory(BaseSource source, ChannelGroup allChannels, String protocol,
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/ServiceDecoder.java`
#### Snippet
```java
     * @param channel      the channel
     * @return Map         the message map
     * @throws Exception
     */
    Map<String, Object> extractData(ByteBuf cb, String strRemoteIP,
```

### JavadocDeclaration
`@param sourceContext` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/tcp/InlongTcpChannelPipelineFactory.java`
#### Snippet
```java
     * get server factory
     *
     * @param sourceContext
     */
    public InlongTcpChannelPipelineFactory(SourceContext sourceContext, String protocolType) {
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/SourceContext.java`
#### Snippet
```java
     * isRejectService
     * 
     * @return
     */
    public boolean isRejectService() {
```

### JavadocDeclaration
`@param source` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/SourceContext.java`
#### Snippet
```java
     * Constructor
     * 
     * @param source
     * @param allChannels
     * @param context
```

### JavadocDeclaration
`@param allChannels` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/SourceContext.java`
#### Snippet
```java
     * 
     * @param source
     * @param allChannels
     * @param context
     */
```

### JavadocDeclaration
`@param context` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/SourceContext.java`
#### Snippet
```java
     * @param source
     * @param allChannels
     * @param context
     */
    public SourceContext(AbstractSource source, ChannelGroup allChannels, Context context) {
```

### JavadocDeclaration
`@param context` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/SourceContext.java`
#### Snippet
```java
     * getHostPort
     * 
     * @param  context
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/SourceContext.java`
#### Snippet
```java
     * 
     * @param  context
     * @return
     */
    private int getHostPort(Context context) {
```

### JavadocDeclaration
`@param isRejectService` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/SourceContext.java`
#### Snippet
```java
     * setRejectService
     * 
     * @param isRejectService
     */
    public void setRejectService(boolean isRejectService) {
```

### JavadocDeclaration
`@param context` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/SourceContext.java`
#### Snippet
```java
     * getHostIp
     * 
     * @param  context
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/SourceContext.java`
#### Snippet
```java
     * 
     * @param  context
     * @return
     */
    private String getHostIp(Context context) {
```

### JavadocDeclaration
`@param ctx` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/tcp/InlongTcpSourceCallback.java`
#### Snippet
```java
    /**
     * Constructor
     * @param ctx
     * @param header
     */
```

### JavadocDeclaration
`@param header` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/tcp/InlongTcpSourceCallback.java`
#### Snippet
```java
     * Constructor
     * @param ctx
     * @param header
     */
    public InlongTcpSourceCallback(ChannelHandlerContext ctx, MessagePackHeader header) {
```

### JavadocDeclaration
`@param resultCode` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/tcp/InlongTcpSourceCallback.java`
#### Snippet
```java
    /**
     * callback
     * @param resultCode
     */
    @Override
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/tcp/InlongTcpSource.java`
#### Snippet
```java
     * getProtocolName
     * 
     * @return
     */
    public String getProtocolName() {
```

### JavadocDeclaration
`@param context` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/tcp/InlongTcpSource.java`
#### Snippet
```java
     * configure
     * 
     * @param context
     */
    @Override
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/BaseSource.java`
#### Snippet
```java
    /**
     * channel factory
     * @return
     */
    public ChannelInitializer getChannelInitializerFactory() {
```

### JavadocDeclaration
`@param event` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/channel/ProxyTransaction.java`
#### Snippet
```java
     * doTake
     *
     * @param event
     */
    public void doTake(ProxyEvent event) {
```

### JavadocDeclaration
`@param event` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/channel/ProxyTransaction.java`
#### Snippet
```java
     * doPut
     *
     * @param event
     */
    public void doPut(ProxyEvent event) {
```

### JavadocDeclaration
`@param countSemaphore` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/channel/ProxyTransaction.java`
#### Snippet
```java
     * Constructor
     *
     * @param countSemaphore
     * @param bufferQueue
     */
```

### JavadocDeclaration
`@param bufferQueue` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/channel/ProxyTransaction.java`
#### Snippet
```java
     *
     * @param countSemaphore
     * @param bufferQueue
     */
    public ProxyTransaction(Semaphore countSemaphore, BufferQueue<ProxyEvent> bufferQueue) {
```

### JavadocDeclaration
`@param event` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/channel/BufferQueueChannel.java`
#### Snippet
```java
     * put
     *
     * @param  event
     * @throws ChannelException
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/channel/BufferQueueChannel.java`
#### Snippet
```java
     *
     * @param  event
     * @throws ChannelException
     */
    @Override
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/channel/BufferQueueChannel.java`
#### Snippet
```java
     *
     * @return Event
     * @throws ChannelException
     */
    @Override
```

### JavadocDeclaration
`@param context` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/channel/BufferQueueChannel.java`
#### Snippet
```java
     * configure
     *
     * @param context
     */
    @Override
```

### JavadocDeclaration
`@param ctx` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/tcp/InlongTcpChannelHandler.java`
#### Snippet
```java
    /**
     * processAndResponse
     * @param ctx
     * @param packObject
     * @param events
```

### JavadocDeclaration
`@param packObject` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/tcp/InlongTcpChannelHandler.java`
#### Snippet
```java
     * processAndResponse
     * @param ctx
     * @param packObject
     * @param events
     * @throws Exception
```

### JavadocDeclaration
`@param events` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/tcp/InlongTcpChannelHandler.java`
#### Snippet
```java
     * @param ctx
     * @param packObject
     * @param events
     * @throws Exception
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/tcp/InlongTcpChannelHandler.java`
#### Snippet
```java
     * @param packObject
     * @param events
     * @throws Exception
     */
    private void processAndResponse(ChannelHandlerContext ctx, MessagePack packObject, List<ProxyEvent> events)
```

### JavadocDeclaration
`@param ctx` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/tcp/InlongTcpChannelHandler.java`
#### Snippet
```java
     * channelActive
     *
     * @param  ctx
     * @throws Exception
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/tcp/InlongTcpChannelHandler.java`
#### Snippet
```java
     *
     * @param  ctx
     * @throws Exception
     */
    @Override
```

### JavadocDeclaration
Exception is not declared to be thrown by method channelActive
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/tcp/InlongTcpChannelHandler.java`
#### Snippet
```java
     *
     * @param  ctx
     * @throws Exception
     */
    @Override
```

### JavadocDeclaration
`@param ctx` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/tcp/InlongTcpChannelHandler.java`
#### Snippet
```java
     * channelRead
     * 
     * @param  ctx
     * @param  msg
     * @throws Exception
```

### JavadocDeclaration
`@param msg` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/tcp/InlongTcpChannelHandler.java`
#### Snippet
```java
     * 
     * @param  ctx
     * @param  msg
     * @throws Exception
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/tcp/InlongTcpChannelHandler.java`
#### Snippet
```java
     * @param  ctx
     * @param  msg
     * @throws Exception
     */
    @Override
```

### JavadocDeclaration
`@param sourceContext` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/tcp/InlongTcpChannelHandler.java`
#### Snippet
```java
     * Constructor
     * 
     * @param sourceContext
     */
    public InlongTcpChannelHandler(SourceContext sourceContext) {
```

### JavadocDeclaration
`@param ctx` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/tcp/InlongTcpChannelHandler.java`
#### Snippet
```java
     * channelInactive
     *
     * @param  ctx
     * @throws Exception
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/tcp/InlongTcpChannelHandler.java`
#### Snippet
```java
     *
     * @param  ctx
     * @throws Exception
     */
    @Override
```

### JavadocDeclaration
Exception is not declared to be thrown by method channelInactive
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/tcp/InlongTcpChannelHandler.java`
#### Snippet
```java
     *
     * @param  ctx
     * @throws Exception
     */
    @Override
```

### JavadocDeclaration
`@param ctx` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/tcp/InlongTcpChannelHandler.java`
#### Snippet
```java
    /**
     * processAndWaitingSave
     * @param ctx
     * @param packObject
     * @param events
```

### JavadocDeclaration
`@param packObject` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/tcp/InlongTcpChannelHandler.java`
#### Snippet
```java
     * processAndWaitingSave
     * @param ctx
     * @param packObject
     * @param events
     * @throws Exception
```

### JavadocDeclaration
`@param events` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/tcp/InlongTcpChannelHandler.java`
#### Snippet
```java
     * @param ctx
     * @param packObject
     * @param events
     * @throws Exception
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/tcp/InlongTcpChannelHandler.java`
#### Snippet
```java
     * @param packObject
     * @param events
     * @throws Exception
     */
    private void processAndWaitingSave(ChannelHandlerContext ctx, MessagePack packObject, List<ProxyEvent> events)
```

### JavadocDeclaration
`@param ctx` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/tcp/InlongTcpChannelHandler.java`
#### Snippet
```java
     * responsePackage
     *
     * @param  ctx
     * @param  code
     * @throws Exception
```

### JavadocDeclaration
`@param code` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/tcp/InlongTcpChannelHandler.java`
#### Snippet
```java
     *
     * @param  ctx
     * @param  code
     * @throws Exception
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/tcp/InlongTcpChannelHandler.java`
#### Snippet
```java
     * @param  ctx
     * @param  code
     * @throws Exception
     */
    private void responsePackage(ChannelHandlerContext ctx, ResultCode code, MessagePack packObject)
```

### JavadocDeclaration
`@param ctx` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/tcp/InlongTcpChannelHandler.java`
#### Snippet
```java
     * exceptionCaught
     * 
     * @param  ctx
     * @param  cause
     * @throws Exception
```

### JavadocDeclaration
`@param cause` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/tcp/InlongTcpChannelHandler.java`
#### Snippet
```java
     * 
     * @param  ctx
     * @param  cause
     * @throws Exception
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/tcp/InlongTcpChannelHandler.java`
#### Snippet
```java
     * @param  ctx
     * @param  cause
     * @throws Exception
     */
    @Override
```

### JavadocDeclaration
Exception is not declared to be thrown by method exceptionCaught
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/tcp/InlongTcpChannelHandler.java`
#### Snippet
```java
     * @param  ctx
     * @param  cause
     * @throws Exception
     */
    @Override
```

### JavadocDeclaration
`@param result` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/tcp/InlongTcpChannelHandler.java`
#### Snippet
```java
     * addMetric
     * 
     * @param result
     * @param size
     * @param event
```

### JavadocDeclaration
`@param size` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/tcp/InlongTcpChannelHandler.java`
#### Snippet
```java
     * 
     * @param result
     * @param size
     * @param event
     */
```

### JavadocDeclaration
`@param event` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/tcp/InlongTcpChannelHandler.java`
#### Snippet
```java
     * @param result
     * @param size
     * @param event
     */
    private void addMetric(boolean result, long size, Event event) {
```

### JavadocDeclaration
`@param name` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/metrics/DataProxyMetricItemSet.java`
#### Snippet
```java
     * Constructor
     * 
     * @param name
     */
    public DataProxyMetricItemSet(String name) {
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/metrics/DataProxyMetricItemSet.java`
#### Snippet
```java
     * createItem
     * 
     * @return
     */
    @Override
```

### JavadocDeclaration
`@param headers` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/metrics/DataProxyMetricItem.java`
#### Snippet
```java
     * getInlongStreamId
     *
     * @param  headers
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/metrics/DataProxyMetricItem.java`
#### Snippet
```java
     *
     * @param  headers
     * @return
     */
    public static String getInlongStreamId(Map<String, String> headers) {
```

### JavadocDeclaration
`@param event` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/metrics/DataProxyMetricItem.java`
#### Snippet
```java
     * fillAuditFormatTime
     *
     * @param event
     * @param dimensions
     */
```

### JavadocDeclaration
`@param dimensions` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/metrics/DataProxyMetricItem.java`
#### Snippet
```java
     *
     * @param event
     * @param dimensions
     */
    public static void fillAuditFormatTime(Event event, Map<String, String> dimensions) {
```

### JavadocDeclaration
`@param event` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/metrics/DataProxyMetricItem.java`
#### Snippet
```java
     * fillInlongId
     *
     * @param event
     * @param dimensions
     */
```

### JavadocDeclaration
`@param dimensions` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/metrics/DataProxyMetricItem.java`
#### Snippet
```java
     *
     * @param event
     * @param dimensions
     */
    public static void fillInlongId(Event event, Map<String, String> dimensions) {
```

### JavadocDeclaration
`@param headers` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/metrics/DataProxyMetricItem.java`
#### Snippet
```java
     * getInlongGroupId
     *
     * @param  headers
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/metrics/DataProxyMetricItem.java`
#### Snippet
```java
     *
     * @param  headers
     * @return
     */
    public static String getInlongGroupId(Map<String, String> headers) {
```

### JavadocDeclaration
`@param context` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/channel/FailoverChannelProcessor.java`
#### Snippet
```java
     * The Context of the associated Source is passed.
     *
     * @param context
     */
    @Override
```

### JavadocDeclaration
`@param source` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source2/InLongMessageFactory.java`
#### Snippet
```java
     * get server factory
     *
     * @param source
     */
    public InLongMessageFactory(BaseSource source) {
```

### JavadocDeclaration
`@param domain` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/metrics/prometheus/PrometheusMetricListener.java`
#### Snippet
```java
     * snapshot
     *
     * @param domain
     * @param itemValues
     */
```

### JavadocDeclaration
`@param itemValues` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/metrics/prometheus/PrometheusMetricListener.java`
#### Snippet
```java
     *
     * @param domain
     * @param itemValues
     */
    @Override
```

### JavadocDeclaration
`@param defaultDemension` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/metrics/prometheus/PrometheusMetricListener.java`
#### Snippet
```java
     * addCounterMetricFamily
     *
     * @param defaultDemension
     * @param itemValue
     * @param idCounter
```

### JavadocDeclaration
`@param itemValue` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/metrics/prometheus/PrometheusMetricListener.java`
#### Snippet
```java
     *
     * @param defaultDemension
     * @param itemValue
     * @param idCounter
     */
```

### JavadocDeclaration
`@param idCounter` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/metrics/prometheus/PrometheusMetricListener.java`
#### Snippet
```java
     * @param defaultDemension
     * @param itemValue
     * @param idCounter
     */
    private void addCounterMetricFamily(String defaultDemension, MetricItemValue itemValue,
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/metrics/prometheus/PrometheusMetricListener.java`
#### Snippet
```java
     * collect
     *
     * @return
     */
    @Override
```

### JavadocDeclaration
`@param eventCount` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/dispatch/DispatchProfileCallback.java`
#### Snippet
```java
    /**
     * ack
     * @param eventCount
     */
    public void ack(int eventCount) {
```

### JavadocDeclaration
`@param totalCount` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/dispatch/DispatchProfileCallback.java`
#### Snippet
```java
    /**
     * Constructor
     * @param totalCount
     * @param callback
     */
```

### JavadocDeclaration
`@param callback` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/dispatch/DispatchProfileCallback.java`
#### Snippet
```java
     * Constructor
     * @param totalCount
     * @param callback
     */
    public DispatchProfileCallback(int totalCount, SourceCallback callback) {
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source2/BaseSource.java`
#### Snippet
```java
    /**
     * channel factory
     * @return
     */
    public ChannelInitializer getChannelInitializerFactory() {
```

### JavadocDeclaration
`@param context` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source2/BaseSource.java`
#### Snippet
```java
     * getHostIp
     *
     * @param  context
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source2/BaseSource.java`
#### Snippet
```java
     *
     * @param  context
     * @return
     */
    private String getHostIp(Context context) {
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source2/BaseSource.java`
#### Snippet
```java
     * isRejectService
     *
     * @return
     */
    public boolean isRejectService() {
```

### JavadocDeclaration
`@param context` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source2/BaseSource.java`
#### Snippet
```java
     * getHostPort
     *
     * @param  context
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source2/BaseSource.java`
#### Snippet
```java
     *
     * @param  context
     * @return
     */
    private int getHostPort(Context context) {
```

### JavadocDeclaration
Tag `return` is not allowed here
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/dispatch/DispatchProfile.java`
#### Snippet
```java
    /**
     * fail
     * @return
     */
    public void fail() {
```

### JavadocDeclaration
`@param uid` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/dispatch/DispatchProfile.java`
#### Snippet
```java
     * Constructor
     * 
     * @param uid
     * @param inlongGroupId
     * @param inlongStreamId
```

### JavadocDeclaration
`@param inlongGroupId` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/dispatch/DispatchProfile.java`
#### Snippet
```java
     * 
     * @param uid
     * @param inlongGroupId
     * @param inlongStreamId
     * @param dispatchTime
```

### JavadocDeclaration
`@param inlongStreamId` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/dispatch/DispatchProfile.java`
#### Snippet
```java
     * @param uid
     * @param inlongGroupId
     * @param inlongStreamId
     * @param dispatchTime
     */
```

### JavadocDeclaration
`@param dispatchTime` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/dispatch/DispatchProfile.java`
#### Snippet
```java
     * @param inlongGroupId
     * @param inlongStreamId
     * @param dispatchTime
     */
    public DispatchProfile(String uid, String inlongGroupId, String inlongStreamId, long dispatchTime) {
```

### JavadocDeclaration
`@param createThreshold` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/dispatch/DispatchProfile.java`
#### Snippet
```java
     * isTimeout
     * 
     * @param  createThreshold
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/dispatch/DispatchProfile.java`
#### Snippet
```java
     * 
     * @param  createThreshold
     * @return
     */
    public boolean isTimeout(long createThreshold) {
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/dispatch/DispatchProfile.java`
#### Snippet
```java
    /**
     * isResend
     * @return
     */
    public boolean isResend() {
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/dispatch/DispatchProfile.java`
#### Snippet
```java
     * getDispatchTime
     * 
     * @return
     */
    public long getDispatchTime() {
```

### JavadocDeclaration
`@param event` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/dispatch/DispatchProfile.java`
#### Snippet
```java
     * addEvent
     * 
     * @param  event
     * @param  maxPackCount
     * @param  maxPackSize
```

### JavadocDeclaration
`@param maxPackCount` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/dispatch/DispatchProfile.java`
#### Snippet
```java
     * 
     * @param  event
     * @param  maxPackCount
     * @param  maxPackSize
     * @return
```

### JavadocDeclaration
`@param maxPackSize` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/dispatch/DispatchProfile.java`
#### Snippet
```java
     * @param  event
     * @param  maxPackCount
     * @param  maxPackSize
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/dispatch/DispatchProfile.java`
#### Snippet
```java
     * @param  maxPackCount
     * @param  maxPackSize
     * @return
     */
    public boolean addEvent(ProxyEvent event, long maxPackCount, long maxPackSize) {
```

### JavadocDeclaration
`@param commonHeaders` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/SimpleMessageHandler.java`
#### Snippet
```java
     * processProxyMessageList
     * 
     * @param commonHeaders
     * @param proxyMessages
     */
```

### JavadocDeclaration
`@param proxyMessages` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/SimpleMessageHandler.java`
#### Snippet
```java
     * 
     * @param commonHeaders
     * @param proxyMessages
     */
    private void processProxyMessageList(Map<String, String> commonHeaders,
```

### JavadocDeclaration
`@param result` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/SimpleMessageHandler.java`
#### Snippet
```java
     * addMetric
     * 
     * @param result
     * @param size
     * @param event
```

### JavadocDeclaration
`@param size` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/SimpleMessageHandler.java`
#### Snippet
```java
     * 
     * @param result
     * @param size
     * @param event
     */
```

### JavadocDeclaration
`@param event` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/SimpleMessageHandler.java`
#### Snippet
```java
     * @param result
     * @param size
     * @param event
     */
    private void addMetric(boolean result, long size, Event event) {
```

### JavadocDeclaration
`@param commonHeaders` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/SimpleMessageHandler.java`
#### Snippet
```java
     * parseProxyMessage2Event
     * 
     * @param  commonHeaders
     * @param  proxyMessage
     * @return
```

### JavadocDeclaration
`@param proxyMessage` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/SimpleMessageHandler.java`
#### Snippet
```java
     * 
     * @param  commonHeaders
     * @param  proxyMessage
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/SimpleMessageHandler.java`
#### Snippet
```java
     * @param  commonHeaders
     * @param  proxyMessage
     * @return
     */
    private Event parseProxyMessage2Event(Map<String, String> commonHeaders, ProxyMessage proxyMessage) {
```

### JavadocDeclaration
`@param commonAttrMap` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/SimpleMessageHandler.java`
#### Snippet
```java
     * formatMessagesAndSend
     * 
     * @param  commonAttrMap
     * @param  messageMap
     * @param  strRemoteIP
```

### JavadocDeclaration
`@param messageMap` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/SimpleMessageHandler.java`
#### Snippet
```java
     * 
     * @param  commonAttrMap
     * @param  messageMap
     * @param  strRemoteIP
     * @param  msgType
```

### JavadocDeclaration
`@param strRemoteIP` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/SimpleMessageHandler.java`
#### Snippet
```java
     * @param  commonAttrMap
     * @param  messageMap
     * @param  strRemoteIP
     * @param  msgType
     * @throws MessageIDException
```

### JavadocDeclaration
`@param msgType` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/SimpleMessageHandler.java`
#### Snippet
```java
     * @param  messageMap
     * @param  strRemoteIP
     * @param  msgType
     * @throws MessageIDException
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/SimpleMessageHandler.java`
#### Snippet
```java
     * @param  strRemoteIP
     * @param  msgType
     * @throws MessageIDException
     */
    private void formatMessagesAndSend(Map<String, String> commonAttrMap,
```

### JavadocDeclaration
`@param source` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/SimpleMessageHandler.java`
#### Snippet
```java
    /**
     * SimpleMessageHandler
     * @param source
     * @param serProcessor
     * @param allChannels
```

### JavadocDeclaration
`@param serProcessor` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/SimpleMessageHandler.java`
#### Snippet
```java
     * SimpleMessageHandler
     * @param source
     * @param serProcessor
     * @param allChannels
     * @param topic
```

### JavadocDeclaration
`@param allChannels` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/SimpleMessageHandler.java`
#### Snippet
```java
     * @param source
     * @param serProcessor
     * @param allChannels
     * @param topic
     * @param attr
```

### JavadocDeclaration
`@param topic` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/SimpleMessageHandler.java`
#### Snippet
```java
     * @param serProcessor
     * @param allChannels
     * @param topic
     * @param attr
     * @param filterEmptyMsg
```

### JavadocDeclaration
`@param attr` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/SimpleMessageHandler.java`
#### Snippet
```java
     * @param allChannels
     * @param topic
     * @param attr
     * @param filterEmptyMsg
     * @param maxMsgLength
```

### JavadocDeclaration
`@param filterEmptyMsg` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/SimpleMessageHandler.java`
#### Snippet
```java
     * @param topic
     * @param attr
     * @param filterEmptyMsg
     * @param maxMsgLength
     * @param maxCons
```

### JavadocDeclaration
`@param maxMsgLength` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/SimpleMessageHandler.java`
#### Snippet
```java
     * @param attr
     * @param filterEmptyMsg
     * @param maxMsgLength
     * @param maxCons
     * @param isCompressed
```

### JavadocDeclaration
`@param maxCons` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/SimpleMessageHandler.java`
#### Snippet
```java
     * @param filterEmptyMsg
     * @param maxMsgLength
     * @param maxCons
     * @param isCompressed
     * @param protocolType
```

### JavadocDeclaration
`@param isCompressed` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/SimpleMessageHandler.java`
#### Snippet
```java
     * @param maxMsgLength
     * @param maxCons
     * @param isCompressed
     * @param protocolType
     */
```

### JavadocDeclaration
`@param protocolType` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/SimpleMessageHandler.java`
#### Snippet
```java
     * @param maxCons
     * @param isCompressed
     * @param protocolType
     */
    public SimpleMessageHandler(BaseSource source, ServiceDecoder serProcessor,
```

### JavadocDeclaration
`@param packEvent` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/dispatch/DispatchManager.java`
#### Snippet
```java
    /**
     * addPackEvent
     * @param packEvent
     */
    public void addPackEvent(ProxyPackEvent packEvent) {
```

### JavadocDeclaration
Tag `return` is not allowed here
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/dispatch/DispatchManager.java`
#### Snippet
```java
     * outputOvertimeData
     * 
     * @return
     */
    public void outputOvertimeData() {
```

### JavadocDeclaration
`@param event` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/dispatch/DispatchManager.java`
#### Snippet
```java
     * addEvent
     * 
     * @param event
     */
    public void addEvent(ProxyEvent event) {
```

### JavadocDeclaration
`@param context` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/dispatch/DispatchManager.java`
#### Snippet
```java
     * Constructor
     * 
     * @param context
     * @param dispatchQueues
     */
```

### JavadocDeclaration
`@param dispatchQueues` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/dispatch/DispatchManager.java`
#### Snippet
```java
     * 
     * @param context
     * @param dispatchQueues
     */
    public DispatchManager(Context context, ArrayList<LinkedBlockingQueue<DispatchProfile>> dispatchQueues) {
```

### JavadocDeclaration
`@param clazz` tag description is missing
in `inlong-sort-standalone/sort-standalone-common/src/main/java/org/apache/inlong/sort/standalone/utils/InlongLoggerFactory.java`
#### Snippet
```java
     * getLogger
     * 
     * @param  clazz
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sort-standalone/sort-standalone-common/src/main/java/org/apache/inlong/sort/standalone/utils/InlongLoggerFactory.java`
#### Snippet
```java
     * 
     * @param  clazz
     * @return
     */
    public static Logger getLogger(Class<?> clazz) {
```

### JavadocDeclaration
`@param className` tag description is missing
in `inlong-sort-standalone/sort-standalone-common/src/main/java/org/apache/inlong/sort/standalone/utils/InlongLoggerFactory.java`
#### Snippet
```java
     * getClassNamePrefix
     * 
     * @param  className
     * @param  layer
     * @return
```

### JavadocDeclaration
`@param layer` tag description is missing
in `inlong-sort-standalone/sort-standalone-common/src/main/java/org/apache/inlong/sort/standalone/utils/InlongLoggerFactory.java`
#### Snippet
```java
     * 
     * @param  className
     * @param  layer
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sort-standalone/sort-standalone-common/src/main/java/org/apache/inlong/sort/standalone/utils/InlongLoggerFactory.java`
#### Snippet
```java
     * @param  className
     * @param  layer
     * @return
     */
    public static String getClassNamePrefix(String className, int layer) {
```

### JavadocDeclaration
`@param maxSize` tag description is missing
in `inlong-sort-standalone/sort-standalone-common/src/main/java/org/apache/inlong/sort/standalone/utils/SizeSemaphore.java`
#### Snippet
```java
     * Constructor
     * 
     * @param maxSize
     * @param leftSize
     */
```

### JavadocDeclaration
`@param leftSize` tag description is missing
in `inlong-sort-standalone/sort-standalone-common/src/main/java/org/apache/inlong/sort/standalone/utils/SizeSemaphore.java`
#### Snippet
```java
     * 
     * @param maxSize
     * @param leftSize
     */
    public SizeSemaphore(int maxSize, int leftSize) {
```

### JavadocDeclaration
`@param inlongGroupId` tag description is missing
in `inlong-sort-standalone/sort-standalone-common/src/main/java/org/apache/inlong/sort/standalone/config/pojo/InlongId.java`
#### Snippet
```java
     * generateUid
     * 
     * @param  inlongGroupId
     * @param  inlongStreamId
     * @return
```

### JavadocDeclaration
`@param inlongStreamId` tag description is missing
in `inlong-sort-standalone/sort-standalone-common/src/main/java/org/apache/inlong/sort/standalone/config/pojo/InlongId.java`
#### Snippet
```java
     * 
     * @param  inlongGroupId
     * @param  inlongStreamId
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sort-standalone/sort-standalone-common/src/main/java/org/apache/inlong/sort/standalone/config/pojo/InlongId.java`
#### Snippet
```java
     * @param  inlongGroupId
     * @param  inlongStreamId
     * @return
     */
    public static String generateUid(String inlongGroupId, String inlongStreamId) {
```

### JavadocDeclaration
`@param maxSizeKb` tag description is missing
in `inlong-sort-standalone/sort-standalone-common/src/main/java/org/apache/inlong/sort/standalone/utils/BufferQueue.java`
#### Snippet
```java
     * Constructor
     * 
     * @param maxSizeKb
     * @param globalTokens
     */
```

### JavadocDeclaration
`@param globalTokens` tag description is missing
in `inlong-sort-standalone/sort-standalone-common/src/main/java/org/apache/inlong/sort/standalone/utils/BufferQueue.java`
#### Snippet
```java
     * 
     * @param maxSizeKb
     * @param globalTokens
     */
    public BufferQueue(int maxSizeKb, SizeSemaphore globalTokens) {
```

### JavadocDeclaration
`@param maxSizeKb` tag description is missing
in `inlong-sort-standalone/sort-standalone-common/src/main/java/org/apache/inlong/sort/standalone/utils/BufferQueue.java`
#### Snippet
```java
     * Constructor
     * 
     * @param maxSizeKb
     */
    public BufferQueue(int maxSizeKb) {
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sort-standalone/sort-standalone-common/src/main/java/org/apache/inlong/sort/standalone/config/pojo/type/CacheType.java`
#### Snippet
```java
     * value
     *
     * @return
     */
    public String value() {
```

### JavadocDeclaration
`@param value` tag description is missing
in `inlong-sort-standalone/sort-standalone-common/src/main/java/org/apache/inlong/sort/standalone/config/pojo/type/CacheType.java`
#### Snippet
```java
     * convert
     *
     * @param  value
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sort-standalone/sort-standalone-common/src/main/java/org/apache/inlong/sort/standalone/config/pojo/type/CacheType.java`
#### Snippet
```java
     *
     * @param  value
     * @return
     */
    public static CacheType convert(String value) {
```

### JavadocDeclaration
`@param value` tag description is missing
in `inlong-sort-standalone/sort-standalone-common/src/main/java/org/apache/inlong/sort/standalone/config/pojo/type/CacheType.java`
#### Snippet
```java
     * Constructor
     * 
     * @param value
     */
    private CacheType(String value) {
```

### JavadocDeclaration
`@param value` tag description is missing
in `inlong-sort-standalone/sort-standalone-common/src/main/java/org/apache/inlong/sort/standalone/config/pojo/type/SortType.java`
#### Snippet
```java
     * convert
     *
     * @param  value
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sort-standalone/sort-standalone-common/src/main/java/org/apache/inlong/sort/standalone/config/pojo/type/SortType.java`
#### Snippet
```java
     *
     * @param  value
     * @return
     */
    public static SortType convert(String value) {
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sort-standalone/sort-standalone-common/src/main/java/org/apache/inlong/sort/standalone/config/pojo/type/SortType.java`
#### Snippet
```java
     * value
     *
     * @return
     */
    public String value() {
```

### JavadocDeclaration
`@param value` tag description is missing
in `inlong-sort-standalone/sort-standalone-common/src/main/java/org/apache/inlong/sort/standalone/config/pojo/type/SortType.java`
#### Snippet
```java
     * Constructor
     * 
     * @param value
     */
    private SortType(String value) {
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sort-standalone/sort-standalone-common/src/main/java/org/apache/inlong/sort/standalone/config/pojo/type/DataType.java`
#### Snippet
```java
     * value
     *
     * @return
     */
    public String value() {
```

### JavadocDeclaration
`@param value` tag description is missing
in `inlong-sort-standalone/sort-standalone-common/src/main/java/org/apache/inlong/sort/standalone/config/pojo/type/DataType.java`
#### Snippet
```java
     * convert
     *
     * @param  value
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sort-standalone/sort-standalone-common/src/main/java/org/apache/inlong/sort/standalone/config/pojo/type/DataType.java`
#### Snippet
```java
     *
     * @param  value
     * @return
     */
    public static DataType convert(String value) {
```

### JavadocDeclaration
`@param value` tag description is missing
in `inlong-sort-standalone/sort-standalone-common/src/main/java/org/apache/inlong/sort/standalone/config/pojo/type/DataType.java`
#### Snippet
```java
     * Constructor
     * 
     * @param value
     */
    private DataType(String value) {
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sort-standalone/sort-standalone-common/src/main/java/org/apache/inlong/sort/standalone/config/loader/SortClusterConfigLoader.java`
#### Snippet
```java
     * load
     * 
     * @return
     */
    SortClusterConfig load();
```

### JavadocDeclaration
`@param value` tag description is missing
in `inlong-sort-standalone/sort-standalone-common/src/main/java/org/apache/inlong/sort/standalone/config/holder/AckPolicy.java`
#### Snippet
```java
    /**
     * getAckPolicy
     * @param value
     * @return AckPolicy
     */
```

### JavadocDeclaration
`@param name` tag description is missing
in `inlong-sort-standalone/sort-standalone-common/src/main/java/org/apache/inlong/sort/standalone/config/holder/AckPolicy.java`
#### Snippet
```java
    /**
     * getAckPolicy
     * @param name
     * @return AckPolicy
     */
```

### JavadocDeclaration
`@param value` tag description is missing
in `inlong-sort-standalone/sort-standalone-common/src/main/java/org/apache/inlong/sort/standalone/config/holder/AckPolicy.java`
#### Snippet
```java
    /**
     * Constructor
     * @param value
     */
    private AckPolicy(int value) {
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sort-standalone/sort-standalone-common/src/main/java/org/apache/inlong/sort/standalone/config/loader/CommonPropertiesLoader.java`
#### Snippet
```java
     * load
     * 
     * @return
     */
    Map<String, String> load();
```

### JavadocDeclaration
`@param ctx` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source2/InLongMessageHandler.java`
#### Snippet
```java
    /**
     * processAndWaitingSave
     * @param ctx
     * @param packObject
     * @param events
```

### JavadocDeclaration
`@param packObject` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source2/InLongMessageHandler.java`
#### Snippet
```java
     * processAndWaitingSave
     * @param ctx
     * @param packObject
     * @param events
     * @throws Exception
```

### JavadocDeclaration
`@param events` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source2/InLongMessageHandler.java`
#### Snippet
```java
     * @param ctx
     * @param packObject
     * @param events
     * @throws Exception
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source2/InLongMessageHandler.java`
#### Snippet
```java
     * @param packObject
     * @param events
     * @throws Exception
     */
    private void processAndWaitingSave(ChannelHandlerContext ctx,
```

### JavadocDeclaration
`@param ctx` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source2/InLongMessageHandler.java`
#### Snippet
```java
    /**
     * processAndResponse
     * @param ctx
     * @param packObject
     * @param events
```

### JavadocDeclaration
`@param packObject` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source2/InLongMessageHandler.java`
#### Snippet
```java
     * processAndResponse
     * @param ctx
     * @param packObject
     * @param events
     * @throws Exception
```

### JavadocDeclaration
`@param events` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source2/InLongMessageHandler.java`
#### Snippet
```java
     * @param ctx
     * @param packObject
     * @param events
     * @throws Exception
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source2/InLongMessageHandler.java`
#### Snippet
```java
     * @param packObject
     * @param events
     * @throws Exception
     */
    private void processAndResponse(ChannelHandlerContext ctx,
```

### JavadocDeclaration
`@param result` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source2/InLongMessageHandler.java`
#### Snippet
```java
     * addMetric
     *
     * @param result
     * @param size
     * @param event
```

### JavadocDeclaration
`@param size` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source2/InLongMessageHandler.java`
#### Snippet
```java
     *
     * @param result
     * @param size
     * @param event
     */
```

### JavadocDeclaration
`@param event` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source2/InLongMessageHandler.java`
#### Snippet
```java
     * @param result
     * @param size
     * @param event
     */
    private void addMetric(boolean result, long size, Event event) {
```

### JavadocDeclaration
`@param ctx` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source2/InLongMessageHandler.java`
#### Snippet
```java
     * responsePackage
     *
     * @param  ctx
     * @param  code
     * @throws Exception
```

### JavadocDeclaration
`@param code` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source2/InLongMessageHandler.java`
#### Snippet
```java
     *
     * @param  ctx
     * @param  code
     * @throws Exception
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source2/InLongMessageHandler.java`
#### Snippet
```java
     * @param  ctx
     * @param  code
     * @throws Exception
     */
    private void responsePackage(ChannelHandlerContext ctx,
```

### JavadocDeclaration
`@param context` tag description is missing
in `inlong-sort-standalone/sort-standalone-common/src/main/java/org/apache/inlong/sort/standalone/config/holder/CommonPropertiesHolder.java`
#### Snippet
```java
     * getStringFromContext
     * 
     * @param  context
     * @param  key
     * @param  defaultValue
```

### JavadocDeclaration
`@param key` tag description is missing
in `inlong-sort-standalone/sort-standalone-common/src/main/java/org/apache/inlong/sort/standalone/config/holder/CommonPropertiesHolder.java`
#### Snippet
```java
     * 
     * @param  context
     * @param  key
     * @param  defaultValue
     * @return
```

### JavadocDeclaration
`@param defaultValue` tag description is missing
in `inlong-sort-standalone/sort-standalone-common/src/main/java/org/apache/inlong/sort/standalone/config/holder/CommonPropertiesHolder.java`
#### Snippet
```java
     * @param  context
     * @param  key
     * @param  defaultValue
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sort-standalone/sort-standalone-common/src/main/java/org/apache/inlong/sort/standalone/config/holder/CommonPropertiesHolder.java`
#### Snippet
```java
     * @param  key
     * @param  defaultValue
     * @return
     */
    public static String getStringFromContext(Context context, String key, String defaultValue) {
```

### JavadocDeclaration
`@param context` tag description is missing
in `inlong-sort-standalone/sort-standalone-common/src/main/java/org/apache/inlong/sort/standalone/config/loader/ClassResourceSortClusterConfigLoader.java`
#### Snippet
```java
     * configure
     * 
     * @param context
     */
    @Override
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sort-standalone/sort-standalone-common/src/main/java/org/apache/inlong/sort/standalone/config/holder/CommonPropertiesHolder.java`
#### Snippet
```java
     * getAuditFormatInterval
     *
     * @return
     */
    public static long getAuditFormatInterval() {
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sort-standalone/sort-standalone-common/src/main/java/org/apache/inlong/sort/standalone/config/loader/ClassResourceSortClusterConfigLoader.java`
#### Snippet
```java
     * load
     * 
     * @return
     */
    @Override
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sort-standalone/sort-standalone-common/src/main/java/org/apache/inlong/sort/standalone/config/holder/CommonPropertiesHolder.java`
#### Snippet
```java
     * getClusterId
     * 
     * @return
     */
    public static String getClusterId() {
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sort-standalone/sort-standalone-common/src/main/java/org/apache/inlong/sort/standalone/config/holder/SortClusterConfigHolder.java`
#### Snippet
```java
     * getClusterConfig
     * 
     * @return
     */
    public static SortClusterConfig getClusterConfig() {
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sort-standalone/sort-standalone-common/src/main/java/org/apache/inlong/sort/standalone/config/holder/SortClusterConfigHolder.java`
#### Snippet
```java
     * getInstance
     * 
     * @return
     */
    private static SortClusterConfigHolder get() {
```

### JavadocDeclaration
`@param sortTaskName` tag description is missing
in `inlong-sort-standalone/sort-standalone-common/src/main/java/org/apache/inlong/sort/standalone/config/holder/SortClusterConfigHolder.java`
#### Snippet
```java
     * getTaskConfig
     * 
     * @param  sortTaskName
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sort-standalone/sort-standalone-common/src/main/java/org/apache/inlong/sort/standalone/config/holder/SortClusterConfigHolder.java`
#### Snippet
```java
     * 
     * @param  sortTaskName
     * @return
     */
    public static SortTaskConfig getTaskConfig(String sortTaskName) {
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sort-standalone/sort-standalone-common/src/main/java/org/apache/inlong/sort/standalone/metrics/SortMetricItemSet.java`
#### Snippet
```java
     * getItemMap
     * 
     * @return
     */
    public Map<String, SortMetricItem> getItemMap() {
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sort-standalone/sort-standalone-common/src/main/java/org/apache/inlong/sort/standalone/metrics/SortMetricItemSet.java`
#### Snippet
```java
     * snapshot
     * 
     * @return
     */
    public List<MetricItem> snapshot() {
```

### JavadocDeclaration
`@param name` tag description is missing
in `inlong-sort-standalone/sort-standalone-common/src/main/java/org/apache/inlong/sort/standalone/metrics/SortMetricItemSet.java`
#### Snippet
```java
     * Constructor
     * 
     * @param name
     */
    public SortMetricItemSet(String name) {
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sort-standalone/sort-standalone-common/src/main/java/org/apache/inlong/sort/standalone/metrics/SortMetricItemSet.java`
#### Snippet
```java
     * createItem
     * 
     * @return
     */
    @Override
```

### JavadocDeclaration
`@param sortTaskId` tag description is missing
in `inlong-sort-standalone/sort-standalone-common/src/main/java/org/apache/inlong/sort/standalone/config/loader/ClassResourceQueryConsumeConfig.java`
#### Snippet
```java
     * queryCurrentConsumeConfig
     *
     * @param  sortTaskId
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sort-standalone/sort-standalone-common/src/main/java/org/apache/inlong/sort/standalone/config/loader/ClassResourceQueryConsumeConfig.java`
#### Snippet
```java
     *
     * @param  sortTaskId
     * @return
     */
    @Override
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sort-standalone/sort-standalone-common/src/main/java/org/apache/inlong/sort/standalone/config/loader/ManagerSortClusterConfigLoader.java`
#### Snippet
```java
     * constructHttpClient
     * 
     * @return
     */
    private static synchronized CloseableHttpClient constructHttpClient() {
```

### JavadocDeclaration
`@param context` tag description is missing
in `inlong-sort-standalone/sort-standalone-common/src/main/java/org/apache/inlong/sort/standalone/config/loader/ManagerSortClusterConfigLoader.java`
#### Snippet
```java
     * configure
     * 
     * @param context
     */
    @Override
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sort-standalone/sort-standalone-common/src/main/java/org/apache/inlong/sort/standalone/config/loader/ManagerSortClusterConfigLoader.java`
#### Snippet
```java
     * load
     * 
     * @return
     */
    @Override
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/PropertiesConfigurationProvider.java`
#### Snippet
```java
     * getFlumeConfiguration
     * 
     * @return
     */
    @Override
```

### JavadocDeclaration
`@param agentName` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/PropertiesConfigurationProvider.java`
#### Snippet
```java
     * PropertiesConfigurationProvider
     * 
     * @param agentName
     * @param flumeConf
     */
```

### JavadocDeclaration
`@param flumeConf` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/PropertiesConfigurationProvider.java`
#### Snippet
```java
     * 
     * @param agentName
     * @param flumeConf
     */
    public PropertiesConfigurationProvider(String agentName, Map<String, String> flumeConf) {
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sort-standalone/sort-standalone-common/src/main/java/org/apache/inlong/sort/standalone/config/loader/ClassResourceCommonPropertiesLoader.java`
#### Snippet
```java
     * load
     * 
     * @return
     */
    @Override
```

### JavadocDeclaration
`@param fileName` tag description is missing
in `inlong-sort-standalone/sort-standalone-common/src/main/java/org/apache/inlong/sort/standalone/config/loader/ClassResourceCommonPropertiesLoader.java`
#### Snippet
```java
     * loadProperties
     * 
     * @param  fileName
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sort-standalone/sort-standalone-common/src/main/java/org/apache/inlong/sort/standalone/config/loader/ClassResourceCommonPropertiesLoader.java`
#### Snippet
```java
     * 
     * @param  fileName
     * @return
     */
    protected Map<String, String> loadProperties(String fileName) {
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/cls/ClsSink.java`
#### Snippet
```java
     * Process.
     * @return Status
     * @throws EventDeliveryException
     */
    @Override
```

### JavadocDeclaration
`@param event` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/hive/IEventFormatHandler.java`
#### Snippet
```java
     * format
     * 
     * @param  event
     * @param  idConfig
     * @return
```

### JavadocDeclaration
`@param idConfig` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/hive/IEventFormatHandler.java`
#### Snippet
```java
     * 
     * @param  event
     * @param  idConfig
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/hive/IEventFormatHandler.java`
#### Snippet
```java
     * @param  event
     * @param  idConfig
     * @return
     */
    byte[] format(ProfileEvent event, HdfsIdConfig idConfig);
```

### JavadocDeclaration
`@param headers` tag description is missing
in `inlong-sort-standalone/sort-standalone-common/src/main/java/org/apache/inlong/sort/standalone/metrics/SortMetricItem.java`
#### Snippet
```java
     * getLogTime
     *
     * @param  headers
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sort-standalone/sort-standalone-common/src/main/java/org/apache/inlong/sort/standalone/metrics/SortMetricItem.java`
#### Snippet
```java
     *
     * @param  headers
     * @return
     */
    public static long getLogTime(Map<String, String> headers) {
```

### JavadocDeclaration
`@param headers` tag description is missing
in `inlong-sort-standalone/sort-standalone-common/src/main/java/org/apache/inlong/sort/standalone/metrics/SortMetricItem.java`
#### Snippet
```java
     * getInlongGroupId
     *
     * @param  headers
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sort-standalone/sort-standalone-common/src/main/java/org/apache/inlong/sort/standalone/metrics/SortMetricItem.java`
#### Snippet
```java
     *
     * @param  headers
     * @return
     */
    public static String getInlongGroupId(Map<String, String> headers) {
```

### JavadocDeclaration
`@param msgTime` tag description is missing
in `inlong-sort-standalone/sort-standalone-common/src/main/java/org/apache/inlong/sort/standalone/metrics/SortMetricItem.java`
#### Snippet
```java
     * getAuditFormatTime
     *
     * @param  msgTime
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sort-standalone/sort-standalone-common/src/main/java/org/apache/inlong/sort/standalone/metrics/SortMetricItem.java`
#### Snippet
```java
     *
     * @param  msgTime
     * @return
     */
    public static long getAuditFormatTime(long msgTime) {
```

### JavadocDeclaration
`@param event` tag description is missing
in `inlong-sort-standalone/sort-standalone-common/src/main/java/org/apache/inlong/sort/standalone/metrics/SortMetricItem.java`
#### Snippet
```java
     * getLogTime
     *
     * @param  event
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sort-standalone/sort-standalone-common/src/main/java/org/apache/inlong/sort/standalone/metrics/SortMetricItem.java`
#### Snippet
```java
     *
     * @param  event
     * @return
     */
    public static long getLogTime(Event event) {
```

### JavadocDeclaration
`@param headers` tag description is missing
in `inlong-sort-standalone/sort-standalone-common/src/main/java/org/apache/inlong/sort/standalone/metrics/SortMetricItem.java`
#### Snippet
```java
     * getInlongStreamId
     *
     * @param  headers
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sort-standalone/sort-standalone-common/src/main/java/org/apache/inlong/sort/standalone/metrics/SortMetricItem.java`
#### Snippet
```java
     *
     * @param  headers
     * @return
     */
    public static String getInlongStreamId(Map<String, String> headers) {
```

### JavadocDeclaration
`@param event` tag description is missing
in `inlong-sort-standalone/sort-standalone-common/src/main/java/org/apache/inlong/sort/standalone/metrics/SortMetricItem.java`
#### Snippet
```java
     * fillAuditFormatTime
     *
     * @param event
     * @param dimensions
     */
```

### JavadocDeclaration
`@param dimensions` tag description is missing
in `inlong-sort-standalone/sort-standalone-common/src/main/java/org/apache/inlong/sort/standalone/metrics/SortMetricItem.java`
#### Snippet
```java
     *
     * @param event
     * @param dimensions
     */
    public static void fillAuditFormatTime(Event event, Map<String, String> dimensions) {
```

### JavadocDeclaration
`@param event` tag description is missing
in `inlong-sort-standalone/sort-standalone-common/src/main/java/org/apache/inlong/sort/standalone/metrics/SortMetricItem.java`
#### Snippet
```java
     * fillInlongId
     *
     * @param event
     * @param dimensions
     */
```

### JavadocDeclaration
`@param dimensions` tag description is missing
in `inlong-sort-standalone/sort-standalone-common/src/main/java/org/apache/inlong/sort/standalone/metrics/SortMetricItem.java`
#### Snippet
```java
     *
     * @param event
     * @param dimensions
     */
    public static void fillInlongId(Event event, Map<String, String> dimensions) {
```

### JavadocDeclaration
`@param taskName` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/SortTask.java`
#### Snippet
```java
     * Constructor
     * 
     * @param taskName
     */
    public SortTask(String taskName) {
```

### JavadocDeclaration
`@param materializedConfiguration` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/SortTask.java`
#### Snippet
```java
     * startAllComponents
     * 
     * @param materializedConfiguration
     */
    private void startAllComponents(MaterializedConfiguration materializedConfiguration) {
```

### JavadocDeclaration
`@param conf` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/SortTask.java`
#### Snippet
```java
     * handleConfigurationEvent
     * 
     * @param conf
     */
    @Subscribe
```

### JavadocDeclaration
`@param sinkName` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/SinkContext.java`
#### Snippet
```java
     * Constructor
     * 
     * @param sinkName
     * @param context
     * @param channel
```

### JavadocDeclaration
`@param context` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/SinkContext.java`
#### Snippet
```java
     * 
     * @param sinkName
     * @param context
     * @param channel
     */
```

### JavadocDeclaration
`@param channel` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/SinkContext.java`
#### Snippet
```java
     * @param sinkName
     * @param context
     * @param channel
     */
    public SinkContext(String sinkName, Context context, Channel channel) {
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/SinkContext.java`
#### Snippet
```java
    /**
     * createBufferQueue
     * @return
     */
    public static <U> BufferQueue<U> createBufferQueue() {
```

### JavadocDeclaration
`@param currentRecord` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/SinkContext.java`
#### Snippet
```java
     * fillInlongId
     *
     * @param currentRecord
     * @param dimensions
     */
```

### JavadocDeclaration
`@param dimensions` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/SinkContext.java`
#### Snippet
```java
     *
     * @param currentRecord
     * @param dimensions
     */
    public static void fillInlongId(ProfileEvent currentRecord, Map<String, String> dimensions) {
```

### JavadocDeclaration
`@param context` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/hive/WriteHdfsFileRunnable.java`
#### Snippet
```java
     * Constructor
     * 
     * @param context
     * @param idFile
     * @param profile
```

### JavadocDeclaration
`@param idFile` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/hive/WriteHdfsFileRunnable.java`
#### Snippet
```java
     * 
     * @param context
     * @param idFile
     * @param profile
     */
```

### JavadocDeclaration
`@param profile` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/hive/WriteHdfsFileRunnable.java`
#### Snippet
```java
     * @param context
     * @param idFile
     * @param profile
     */
    public WriteHdfsFileRunnable(HiveSinkContext context, HdfsIdFile idFile, DispatchProfile profile) {
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/cls/ClsChannelWorker.java`
#### Snippet
```java
     * @param rowEvent Row event.
     * @param tx Transaction
     * @throws ProducerException
     * @throws InterruptedException
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/cls/ClsChannelWorker.java`
#### Snippet
```java
     * @param tx Transaction
     * @throws ProducerException
     * @throws InterruptedException
     */
    private void send(Event rowEvent, Transaction tx) throws ProducerException, InterruptedException {
```

### JavadocDeclaration
`@param msgTime` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/hive/HdfsIdConfig.java`
#### Snippet
```java
     * parsePartitionPath
     * 
     * @param  msgTime
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/hive/HdfsIdConfig.java`
#### Snippet
```java
     * 
     * @param  msgTime
     * @return
     */
    public String parsePartitionPath(long msgTime) {
```

### JavadocDeclaration
`@param msgTime` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/hive/HdfsIdConfig.java`
#### Snippet
```java
     * parsePartitionField
     * 
     * @param  msgTime
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/hive/HdfsIdConfig.java`
#### Snippet
```java
     * 
     * @param  msgTime
     * @return
     */
    public String parsePartitionField(long msgTime) {
```

### JavadocDeclaration
`@param msgTime` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/hive/HdfsIdConfig.java`
#### Snippet
```java
     * parseMsgTimeField
     * 
     * @param  msgTime
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/hive/HdfsIdConfig.java`
#### Snippet
```java
     * 
     * @param  msgTime
     * @return
     */
    public String parseMsgTimeField(long msgTime) {
```

### JavadocDeclaration
`@param context` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/hive/HdfsIdFile.java`
#### Snippet
```java
     * Constructor
     * 
     * @param  context
     * @param  idConfig
     * @param  strIdRootPath
```

### JavadocDeclaration
`@param idConfig` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/hive/HdfsIdFile.java`
#### Snippet
```java
     * 
     * @param  context
     * @param  idConfig
     * @param  strIdRootPath
     * @throws IOException
```

### JavadocDeclaration
`@param strIdRootPath` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/hive/HdfsIdFile.java`
#### Snippet
```java
     * @param  context
     * @param  idConfig
     * @param  strIdRootPath
     * @throws IOException
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/hive/HdfsIdFile.java`
#### Snippet
```java
     * @param  idConfig
     * @param  strIdRootPath
     * @throws IOException
     */
    public HdfsIdFile(HiveSinkContext context, HdfsIdConfig idConfig, String strIdRootPath) throws IOException {
```

### JavadocDeclaration
`@param context` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/hive/HdfsIdFile.java`
#### Snippet
```java
     * getFileName
     * 
     * @param  context
     * @param  fileTime
     * @return
```

### JavadocDeclaration
`@param fileTime` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/hive/HdfsIdFile.java`
#### Snippet
```java
     * 
     * @param  context
     * @param  fileTime
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/hive/HdfsIdFile.java`
#### Snippet
```java
     * @param  context
     * @param  fileTime
     * @return
     */
    public static String getFileName(HiveSinkContext context, long fileTime) {
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/hive/HdfsIdFile.java`
#### Snippet
```java
     * close
     * 
     * @throws IOException
     */
    public void close() {
```

### JavadocDeclaration
IOException is not declared to be thrown by method close
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/hive/HdfsIdFile.java`
#### Snippet
```java
     * close
     * 
     * @throws IOException
     */
    public void close() {
```

### JavadocDeclaration
`@param event` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/hive/DefaultEventFormatHandler.java`
#### Snippet
```java
     * 3. keep rawData;<br>
     * 
     * @param  event
     * @param  idConfig
     * @return
```

### JavadocDeclaration
`@param idConfig` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/hive/DefaultEventFormatHandler.java`
#### Snippet
```java
     * 
     * @param  event
     * @param  idConfig
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/hive/DefaultEventFormatHandler.java`
#### Snippet
```java
     * @param  event
     * @param  idConfig
     * @return
     */
    @Override
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/kafka/KafkaProducerCluster.java`
#### Snippet
```java
     * @param  profileEvent data to send
     * @return              boolean
     * @throws IOException
     */
    public boolean send(ProfileEvent profileEvent, Transaction tx) throws IOException {
```

### JavadocDeclaration
`@param idFile` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/hive/PartitionCreateRunnable.java`
#### Snippet
```java
     * process
     * 
     * @param  idFile
     * @throws IOException
     * @throws FileNotFoundException
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/hive/PartitionCreateRunnable.java`
#### Snippet
```java
     * 
     * @param  idFile
     * @throws IOException
     * @throws FileNotFoundException
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/hive/PartitionCreateRunnable.java`
#### Snippet
```java
     * @param  idFile
     * @throws IOException
     * @throws FileNotFoundException
     */
    private void process(HdfsIdFile idFile) throws FileNotFoundException, IOException {
```

### JavadocDeclaration
`@param context` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/hive/PartitionCreateRunnable.java`
#### Snippet
```java
     * Constructor
     * 
     * @param context
     * @param idConfig
     * @param strPartitionValue
```

### JavadocDeclaration
`@param idConfig` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/hive/PartitionCreateRunnable.java`
#### Snippet
```java
     * 
     * @param context
     * @param idConfig
     * @param strPartitionValue
     * @param partitionTime
```

### JavadocDeclaration
`@param strPartitionValue` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/hive/PartitionCreateRunnable.java`
#### Snippet
```java
     * @param context
     * @param idConfig
     * @param strPartitionValue
     * @param partitionTime
     * @param isForce
```

### JavadocDeclaration
`@param partitionTime` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/hive/PartitionCreateRunnable.java`
#### Snippet
```java
     * @param idConfig
     * @param strPartitionValue
     * @param partitionTime
     * @param isForce
     */
```

### JavadocDeclaration
`@param isForce` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/hive/PartitionCreateRunnable.java`
#### Snippet
```java
     * @param strPartitionValue
     * @param partitionTime
     * @param isForce
     */
    public PartitionCreateRunnable(HiveSinkContext context, HdfsIdConfig idConfig, String strPartitionValue,
```

### JavadocDeclaration
`@param idFile` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/hive/PartitionCreateRunnable.java`
#### Snippet
```java
     * concatInFiles2OuttmpFile
     * 
     * @param  idFile
     * @param  concatInFiles
     * @param  fs
```

### JavadocDeclaration
`@param concatInFiles` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/hive/PartitionCreateRunnable.java`
#### Snippet
```java
     * 
     * @param  idFile
     * @param  concatInFiles
     * @param  fs
     * @throws IOException
```

### JavadocDeclaration
`@param fs` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/hive/PartitionCreateRunnable.java`
#### Snippet
```java
     * @param  idFile
     * @param  concatInFiles
     * @param  fs
     * @throws IOException
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/hive/PartitionCreateRunnable.java`
#### Snippet
```java
     * @param  concatInFiles
     * @param  fs
     * @throws IOException
     */
    private void concatInFiles2OuttmpFile(HdfsIdFile idFile, List<Path> concatInFiles, DistributedFileSystem fs)
```

### JavadocDeclaration
`@param idFile` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/hive/PartitionCreateRunnable.java`
#### Snippet
```java
     * canArchive
     * 
     * @param  idFile
     * @param  rootPath
     * @return                       boolean
```

### JavadocDeclaration
`@param rootPath` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/hive/PartitionCreateRunnable.java`
#### Snippet
```java
     * 
     * @param  idFile
     * @param  rootPath
     * @return                       boolean
     * @throws IOException
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/hive/PartitionCreateRunnable.java`
#### Snippet
```java
     * @param  rootPath
     * @return                       boolean
     * @throws IOException
     * @throws FileNotFoundException
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/hive/PartitionCreateRunnable.java`
#### Snippet
```java
     * @return                       boolean
     * @throws IOException
     * @throws FileNotFoundException
     */
    private boolean canArchive(HdfsIdFile idFile, Path rootPath) throws FileNotFoundException, IOException {
```

### JavadocDeclaration
`@param idParam` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/kafka/KafkaIdConfig.java`
#### Snippet
```java
     * Constructor
     * 
     * @param idParam
     */
    public KafkaIdConfig(Map<String, String> idParam) {
```

### JavadocDeclaration
`@param context` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/hive/PartitionLeaderElectionRunnable.java`
#### Snippet
```java
     * Constructor
     * 
     * @param context
     */
    public PartitionLeaderElectionRunnable(HiveSinkContext context) {
```

### JavadocDeclaration
`@param idConfig` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/hive/PartitionLeaderElectionRunnable.java`
#### Snippet
```java
     * hasToken
     * 
     * @param  idConfig
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/hive/PartitionLeaderElectionRunnable.java`
#### Snippet
```java
     * 
     * @param  idConfig
     * @return
     */
    private boolean hasToken(HdfsIdConfig idConfig) {
```

### JavadocDeclaration
`@param context` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/kafka/IEvent2KafkaRecordHandler.java`
#### Snippet
```java
     * parse
     * 
     * @param  context
     * @param  event
     * @return             ProducerRecord
```

### JavadocDeclaration
`@param event` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/kafka/IEvent2KafkaRecordHandler.java`
#### Snippet
```java
     * 
     * @param  context
     * @param  event
     * @return             ProducerRecord
     * @throws IOException
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/kafka/IEvent2KafkaRecordHandler.java`
#### Snippet
```java
     * @param  event
     * @return             ProducerRecord
     * @throws IOException
     */
    ProducerRecord<String, byte[]> parse(KafkaFederationSinkContext context, ProfileEvent event) throws IOException;
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/hive/HiveSink.java`
#### Snippet
```java
     * writeHdfsFile
     * 
     * @throws JsonProcessingException
     */
    private void writeHdfsFile() throws JsonProcessingException {
```

### JavadocDeclaration
`@param context` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/hive/HiveSink.java`
#### Snippet
```java
     * configure
     * 
     * @param context
     */
    @Override
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/hive/HiveSink.java`
#### Snippet
```java
     * 
     * @return                        Status
     * @throws EventDeliveryException
     */
    @Override
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/kafka/KafkaProducerFederation.java`
#### Snippet
```java
     * @param  tx           transaction
     * @return              send result
     * @throws IOException
     */
    public boolean send(ProfileEvent profileEvent, Transaction tx) throws IOException {
```

### JavadocDeclaration
`@param event` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/kafka/DefaultEvent2KafkaRecordHandler.java`
#### Snippet
```java
     * getExtInfo
     * 
     * @param  event
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/kafka/DefaultEvent2KafkaRecordHandler.java`
#### Snippet
```java
     * 
     * @param  event
     * @return
     */
    public String getExtInfo(ProfileEvent event) {
```

### JavadocDeclaration
`@param context` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/kafka/DefaultEvent2KafkaRecordHandler.java`
#### Snippet
```java
     * parse
     * 
     * @param  context
     * @param  event
     * @return             ProducerRecord
```

### JavadocDeclaration
`@param event` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/kafka/DefaultEvent2KafkaRecordHandler.java`
#### Snippet
```java
     * 
     * @param  context
     * @param  event
     * @return             ProducerRecord
     * @throws IOException
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/kafka/DefaultEvent2KafkaRecordHandler.java`
#### Snippet
```java
     * @param  event
     * @return             ProducerRecord
     * @throws IOException
     */
    @Override
```

### JavadocDeclaration
`@param currentRecord` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/kafka/KafkaFederationSinkContext.java`
#### Snippet
```java
     * addSendResultMetric
     * 
     * @param currentRecord
     * @param topic
     * @param result
```

### JavadocDeclaration
`@param topic` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/kafka/KafkaFederationSinkContext.java`
#### Snippet
```java
     * 
     * @param currentRecord
     * @param topic
     * @param result
     * @param sendTime
```

### JavadocDeclaration
`@param result` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/kafka/KafkaFederationSinkContext.java`
#### Snippet
```java
     * @param currentRecord
     * @param topic
     * @param result
     * @param sendTime
     */
```

### JavadocDeclaration
`@param sendTime` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/kafka/KafkaFederationSinkContext.java`
#### Snippet
```java
     * @param topic
     * @param result
     * @param sendTime
     */
    public void addSendResultMetric(ProfileEvent currentRecord, String topic, boolean result, long sendTime) {
```

### JavadocDeclaration
`@param currentRecord` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/kafka/KafkaFederationSinkContext.java`
#### Snippet
```java
     * addSendMetric
     * 
     * @param currentRecord
     * @param topic
     */
```

### JavadocDeclaration
`@param topic` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/kafka/KafkaFederationSinkContext.java`
#### Snippet
```java
     * 
     * @param currentRecord
     * @param topic
     */
    public void addSendMetric(ProfileEvent currentRecord, String topic) {
```

### JavadocDeclaration
`@param sinkName` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/hive/HiveSinkContext.java`
#### Snippet
```java
     * Constructor
     * 
     * @param sinkName
     * @param context
     * @param channel
```

### JavadocDeclaration
`@param context` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/hive/HiveSinkContext.java`
#### Snippet
```java
     * 
     * @param sinkName
     * @param context
     * @param channel
     */
```

### JavadocDeclaration
`@param channel` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/hive/HiveSinkContext.java`
#### Snippet
```java
     * @param sinkName
     * @param context
     * @param channel
     */
    public HiveSinkContext(String sinkName, Context context, Channel channel,
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/hive/HiveSinkContext.java`
#### Snippet
```java
     * 
     * @return                        Connection
     * @throws SQLException
     * @throws ClassNotFoundException
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/hive/HiveSinkContext.java`
#### Snippet
```java
     * @return                        Connection
     * @throws SQLException
     * @throws ClassNotFoundException
     */
    public Connection getHiveConnection() throws SQLException, ClassNotFoundException {
```

### JavadocDeclaration
`@param currentRecord` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/hive/HiveSinkContext.java`
#### Snippet
```java
     * addSendMetric
     * 
     * @param currentRecord
     * @param bid
     */
```

### JavadocDeclaration
`@param bid` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/hive/HiveSinkContext.java`
#### Snippet
```java
     * 
     * @param currentRecord
     * @param bid
     */
    public void addSendMetric(DispatchProfile currentRecord, String bid) {
```

### JavadocDeclaration
`@param currentRecord` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/hive/HiveSinkContext.java`
#### Snippet
```java
     * addSendResultMetric
     * 
     * @param currentRecord
     * @param bid
     * @param result
```

### JavadocDeclaration
`@param bid` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/hive/HiveSinkContext.java`
#### Snippet
```java
     * 
     * @param currentRecord
     * @param bid
     * @param result
     * @param sendTime
```

### JavadocDeclaration
`@param result` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/hive/HiveSinkContext.java`
#### Snippet
```java
     * @param currentRecord
     * @param bid
     * @param result
     * @param sendTime
     */
```

### JavadocDeclaration
`@param sendTime` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/hive/HiveSinkContext.java`
#### Snippet
```java
     * @param bid
     * @param result
     * @param sendTime
     */
    public void addSendResultMetric(DispatchProfile currentRecord, String bid, boolean result, long sendTime) {
```

### JavadocDeclaration
`@param context` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/pulsar/PulsarFederationSink.java`
#### Snippet
```java
     * configure
     * 
     * @param context
     */
    @Override
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/pulsar/PulsarFederationSink.java`
#### Snippet
```java
     * 
     * @return                        Status
     * @throws EventDeliveryException
     */
    @Override
```

### JavadocDeclaration
`@param uid` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/hive/HiveSinkContext.java`
#### Snippet
```java
     * getTopic
     * 
     * @param  uid
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/hive/HiveSinkContext.java`
#### Snippet
```java
     * 
     * @param  uid
     * @return
     */
    public HdfsIdConfig getIdConfig(String uid) {
```

### JavadocDeclaration
`@param currentRecord` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/hive/HiveSinkContext.java`
#### Snippet
```java
     * fillInlongId
     * 
     * @param currentRecord
     * @param dimensions
     */
```

### JavadocDeclaration
`@param dimensions` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/hive/HiveSinkContext.java`
#### Snippet
```java
     * 
     * @param currentRecord
     * @param dimensions
     */
    public static void fillInlongId(DispatchProfile currentRecord, Map<String, String> dimensions) {
```

### JavadocDeclaration
`@param context` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/pulsar/IEvent2PulsarRecordHandler.java`
#### Snippet
```java
     * parse
     * 
     * @param  context
     * @param  event
     * @return             ProducerRecord
```

### JavadocDeclaration
`@param event` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/pulsar/IEvent2PulsarRecordHandler.java`
#### Snippet
```java
     * 
     * @param  context
     * @param  event
     * @return             ProducerRecord
     * @throws IOException
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/pulsar/IEvent2PulsarRecordHandler.java`
#### Snippet
```java
     * @param  event
     * @return             ProducerRecord
     * @throws IOException
     */
    byte[] parse(PulsarFederationSinkContext context, ProfileEvent event) throws IOException;
```

### JavadocDeclaration
`@param idParam` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/pulsar/PulsarIdConfig.java`
#### Snippet
```java
     * Constructor
     * 
     * @param idParam
     */
    public PulsarIdConfig(Map<String, String> idParam) {
```

### JavadocDeclaration
`@param profileEvent` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/pulsar/PulsarProducerFederation.java`
#### Snippet
```java
     * send
     * 
     * @param  profileEvent
     * @param  tx
     * @return              boolean
```

### JavadocDeclaration
`@param tx` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/pulsar/PulsarProducerFederation.java`
#### Snippet
```java
     * 
     * @param  profileEvent
     * @param  tx
     * @return              boolean
     * @throws IOException
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/pulsar/PulsarProducerFederation.java`
#### Snippet
```java
     * @param  tx
     * @return              boolean
     * @throws IOException
     */
    public boolean send(ProfileEvent profileEvent, Transaction tx) throws IOException {
```

### JavadocDeclaration
`@param workerName` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/pulsar/PulsarProducerFederation.java`
#### Snippet
```java
     * Constructor
     * 
     * @param workerName
     * @param context
     */
```

### JavadocDeclaration
`@param context` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/pulsar/PulsarProducerFederation.java`
#### Snippet
```java
     * 
     * @param workerName
     * @param context
     */
    public PulsarProducerFederation(String workerName, PulsarFederationSinkContext context) {
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/cls/ClsSinkContext.java`
#### Snippet
```java
     * Reload id params.
     *
     * @throws JsonProcessingException
     */
    private void reloadIdParams() throws JsonProcessingException {
```

### JavadocDeclaration
`@param context` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/pulsar/DefaultEvent2PulsarRecordHandler.java`
#### Snippet
```java
     * parse
     * 
     * @param  context
     * @param  event
     * @return             byte array
```

### JavadocDeclaration
`@param event` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/pulsar/DefaultEvent2PulsarRecordHandler.java`
#### Snippet
```java
     * 
     * @param  context
     * @param  event
     * @return             byte array
     * @throws IOException
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/pulsar/DefaultEvent2PulsarRecordHandler.java`
#### Snippet
```java
     * @param  event
     * @return             byte array
     * @throws IOException
     */
    @Override
```

### JavadocDeclaration
`@param event` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/pulsar/DefaultEvent2PulsarRecordHandler.java`
#### Snippet
```java
     * getExtInfo
     * 
     * @param  event
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/pulsar/DefaultEvent2PulsarRecordHandler.java`
#### Snippet
```java
     * 
     * @param  event
     * @return
     */
    public String getExtInfo(ProfileEvent event) {
```

### JavadocDeclaration
`@param sinkName` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/pulsar/PulsarFederationWorker.java`
#### Snippet
```java
     * Constructor
     * 
     * @param sinkName
     * @param workerIndex
     * @param context
```

### JavadocDeclaration
`@param workerIndex` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/pulsar/PulsarFederationWorker.java`
#### Snippet
```java
     * 
     * @param sinkName
     * @param workerIndex
     * @param context
     */
```

### JavadocDeclaration
`@param context` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/pulsar/PulsarFederationWorker.java`
#### Snippet
```java
     * @param sinkName
     * @param workerIndex
     * @param context
     */
    public PulsarFederationWorker(String sinkName, int workerIndex, PulsarFederationSinkContext context) {
```

### JavadocDeclaration
`@param currentRecord` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/pulsar/PulsarFederationWorker.java`
#### Snippet
```java
     * fillTopic
     * 
     * @param currentRecord
     */
    private String fillTopic(Event currentRecord) {
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/pulsar/PulsarFederationSinkContext.java`
#### Snippet
```java
     * getCacheClusters
     * 
     * @return
     */
    public List<CacheClusterConfig> getCacheClusters() {
```

### JavadocDeclaration
`@param sinkName` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/pulsar/PulsarFederationSinkContext.java`
#### Snippet
```java
     * Constructor
     * 
     * @param sinkName
     * @param context
     * @param channel
```

### JavadocDeclaration
`@param context` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/pulsar/PulsarFederationSinkContext.java`
#### Snippet
```java
     * 
     * @param sinkName
     * @param context
     * @param channel
     */
```

### JavadocDeclaration
`@param channel` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/pulsar/PulsarFederationSinkContext.java`
#### Snippet
```java
     * @param sinkName
     * @param context
     * @param channel
     */
    public PulsarFederationSinkContext(String sinkName, Context context, Channel channel) {
```

### JavadocDeclaration
`@param currentRecord` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/pulsar/PulsarFederationSinkContext.java`
#### Snippet
```java
     * addSendMetric
     * 
     * @param currentRecord
     * @param topic
     */
```

### JavadocDeclaration
`@param topic` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/pulsar/PulsarFederationSinkContext.java`
#### Snippet
```java
     * 
     * @param currentRecord
     * @param topic
     */
    public void addSendMetric(ProfileEvent currentRecord, String topic) {
```

### JavadocDeclaration
`@param currentRecord` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/pulsar/PulsarFederationSinkContext.java`
#### Snippet
```java
     * addSendResultMetric
     * 
     * @param currentRecord
     * @param topic
     * @param result
```

### JavadocDeclaration
`@param topic` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/pulsar/PulsarFederationSinkContext.java`
#### Snippet
```java
     * 
     * @param currentRecord
     * @param topic
     * @param result
     * @param sendTime
```

### JavadocDeclaration
`@param result` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/pulsar/PulsarFederationSinkContext.java`
#### Snippet
```java
     * @param currentRecord
     * @param topic
     * @param result
     * @param sendTime
     */
```

### JavadocDeclaration
`@param sendTime` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/pulsar/PulsarFederationSinkContext.java`
#### Snippet
```java
     * @param topic
     * @param result
     * @param sendTime
     */
    public void addSendResultMetric(ProfileEvent currentRecord, String topic, boolean result, long sendTime) {
```

### JavadocDeclaration
`@param idConfig` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/clickhouse/IEventHandler.java`
#### Snippet
```java
     * parse
     * 
     * @param  idConfig
     * @param  event
     * @return
```

### JavadocDeclaration
`@param event` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/clickhouse/IEventHandler.java`
#### Snippet
```java
     * 
     * @param  idConfig
     * @param  event
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/clickhouse/IEventHandler.java`
#### Snippet
```java
     * @param  idConfig
     * @param  event
     * @return
     */
    Map<String, String> parse(ClickHouseIdConfig idConfig, ProfileEvent event);
```

### JavadocDeclaration
`@param idConfig` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/clickhouse/IEventHandler.java`
#### Snippet
```java
    /**
     * setValue
     * @param idConfig
     * @param columnValueMap
     * @param pstat
```

### JavadocDeclaration
`@param columnValueMap` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/clickhouse/IEventHandler.java`
#### Snippet
```java
     * setValue
     * @param idConfig
     * @param columnValueMap
     * @param pstat
     * @throws SQLException
```

### JavadocDeclaration
`@param pstat` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/clickhouse/IEventHandler.java`
#### Snippet
```java
     * @param idConfig
     * @param columnValueMap
     * @param pstat
     * @throws SQLException
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/clickhouse/IEventHandler.java`
#### Snippet
```java
     * @param columnValueMap
     * @param pstat
     * @throws SQLException
     */
    void setValue(ClickHouseIdConfig idConfig, Map<String, String> columnValueMap, PreparedStatement pstat)
```

### JavadocDeclaration
`@param indexName` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/elasticsearch/EsIndexRequest.java`
#### Snippet
```java
     * Constructor
     * 
     * @param indexName
     * @param event
     */
```

### JavadocDeclaration
`@param event` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/elasticsearch/EsIndexRequest.java`
#### Snippet
```java
     * 
     * @param indexName
     * @param event
     */
    public EsIndexRequest(String indexName, ProfileEvent event) {
```

### JavadocDeclaration
`@param context` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/elasticsearch/EsSink.java`
#### Snippet
```java
     * configure
     * 
     * @param context
     */
    @Override
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/elasticsearch/EsSink.java`
#### Snippet
```java
     * 
     * @return                        Status
     * @throws EventDeliveryException
     */
    @Override
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/clickhouse/ClickHouseChannelWorker.java`
#### Snippet
```java
    /**
     * reconnect
     * @throws SQLException
     */
    private void reconnect() throws SQLException {
```

### JavadocDeclaration
`@param context` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/clickhouse/ClickHouseChannelWorker.java`
#### Snippet
```java
     * Constructor
     * 
     * @param context
     * @param workerIndex
     */
```

### JavadocDeclaration
`@param workerIndex` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/clickhouse/ClickHouseChannelWorker.java`
#### Snippet
```java
     * 
     * @param context
     * @param workerIndex
     */
    public ClickHouseChannelWorker(ClickHouseSinkContext context, int workerIndex) {
```

### JavadocDeclaration
`@param idConfig` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/clickhouse/DefaultEventHandler.java`
#### Snippet
```java
    /**
     * setValue
     * @param idConfig
     * @param columnValueMap
     * @param pstat
```

### JavadocDeclaration
`@param columnValueMap` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/clickhouse/DefaultEventHandler.java`
#### Snippet
```java
     * setValue
     * @param idConfig
     * @param columnValueMap
     * @param pstat
     * @throws SQLException 
```

### JavadocDeclaration
`@param pstat` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/clickhouse/DefaultEventHandler.java`
#### Snippet
```java
     * @param idConfig
     * @param columnValueMap
     * @param pstat
     * @throws SQLException 
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/clickhouse/DefaultEventHandler.java`
#### Snippet
```java
     * @param columnValueMap
     * @param pstat
     * @throws SQLException 
     */
    public void setValue(ClickHouseIdConfig idConfig, Map<String, String> columnValueMap, PreparedStatement pstat)
```

### JavadocDeclaration
`@param event` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/clickhouse/DefaultEventHandler.java`
#### Snippet
```java
     * getExtInfo
     * 
     * @param  event
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/clickhouse/DefaultEventHandler.java`
#### Snippet
```java
     * 
     * @param  event
     * @return
     */
    public static String getExtInfo(ProfileEvent event) {
```

### JavadocDeclaration
`@param idConfig` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/clickhouse/DefaultEventHandler.java`
#### Snippet
```java
    /**
     * parse
     * @param idConfig
     * @param event
     * @return
```

### JavadocDeclaration
`@param event` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/clickhouse/DefaultEventHandler.java`
#### Snippet
```java
     * parse
     * @param idConfig
     * @param event
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/clickhouse/DefaultEventHandler.java`
#### Snippet
```java
     * @param idConfig
     * @param event
     * @return
     */
    @Override
```

### JavadocDeclaration
`@param fieldValue` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/clickhouse/DefaultEventHandler.java`
#### Snippet
```java
    /**
     * parseDate
     * @param fieldValue
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/clickhouse/DefaultEventHandler.java`
#### Snippet
```java
     * parseDate
     * @param fieldValue
     * @return
     */
    private Date parseDate(String fieldValue) {
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/clickhouse/ClickHouseSink.java`
#### Snippet
```java
     * 
     * @return                        Status
     * @throws EventDeliveryException
     */
    @Override
```

### JavadocDeclaration
`@param context` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/clickhouse/ClickHouseSink.java`
#### Snippet
```java
     * configure
     * 
     * @param context
     */
    @Override
```

### JavadocDeclaration
`@param profileEvent` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/pulsar/PulsarProducerCluster.java`
#### Snippet
```java
     * send
     * 
     * @param  profileEvent
     * @param  tx
     * @return              boolean
```

### JavadocDeclaration
`@param tx` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/pulsar/PulsarProducerCluster.java`
#### Snippet
```java
     * 
     * @param  profileEvent
     * @param  tx
     * @return              boolean
     * @throws IOException
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/pulsar/PulsarProducerCluster.java`
#### Snippet
```java
     * @param  tx
     * @return              boolean
     * @throws IOException
     */
    public boolean send(ProfileEvent profileEvent, Transaction tx) throws IOException {
```

### JavadocDeclaration
`@param workerName` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/pulsar/PulsarProducerCluster.java`
#### Snippet
```java
     * Constructor
     * 
     * @param workerName
     * @param config
     * @param context
```

### JavadocDeclaration
`@param config` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/pulsar/PulsarProducerCluster.java`
#### Snippet
```java
     * 
     * @param workerName
     * @param config
     * @param context
     */
```

### JavadocDeclaration
`@param context` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/pulsar/PulsarProducerCluster.java`
#### Snippet
```java
     * @param workerName
     * @param config
     * @param context
     */
    public PulsarProducerCluster(String workerName, CacheClusterConfig config, PulsarFederationSinkContext context) {
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/pulsar/PulsarProducerCluster.java`
#### Snippet
```java
     * getLifecycleState
     * 
     * @return
     */
    @Override
```

### JavadocDeclaration
`@param msgTime` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/elasticsearch/EsIdConfig.java`
#### Snippet
```java
     * parseIndexName
     * 
     * @param  msgTime
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/elasticsearch/EsIdConfig.java`
#### Snippet
```java
     * 
     * @param  msgTime
     * @return
     */
    public String parseIndexName(long msgTime) {
```

### JavadocDeclaration
`@param context` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/elasticsearch/IEvent2IndexRequestHandler.java`
#### Snippet
```java
     * parse
     * 
     * @param  context
     * @param  event
     * @return
```

### JavadocDeclaration
`@param event` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/elasticsearch/IEvent2IndexRequestHandler.java`
#### Snippet
```java
     * 
     * @param  context
     * @param  event
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/elasticsearch/IEvent2IndexRequestHandler.java`
#### Snippet
```java
     * @param  context
     * @param  event
     * @return
     */
    EsIndexRequest parse(EsSinkContext context, ProfileEvent event);
```

### JavadocDeclaration
`@param context` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/elasticsearch/EsSinkFactory.java`
#### Snippet
```java
     * createEsOutputChannel
     * 
     * @param  context
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/elasticsearch/EsSinkFactory.java`
#### Snippet
```java
     * 
     * @param  context
     * @return
     */
    public static EsOutputChannel createEsOutputChannel(EsSinkContext context) {
```

### JavadocDeclaration
`@param builder` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/elasticsearch/EsSinkFactory.java`
#### Snippet
```java
     * createRestHighLevelClient
     *
     * @param  builder
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/elasticsearch/EsSinkFactory.java`
#### Snippet
```java
     *
     * @param  builder
     * @return
     */
    public static RestHighLevelClient createRestHighLevelClient(RestClientBuilder builder) {
```

### JavadocDeclaration
`@param context` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/elasticsearch/EsChannelWorker.java`
#### Snippet
```java
     * Constructor
     * 
     * @param context
     * @param workerIndex
     */
```

### JavadocDeclaration
`@param workerIndex` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/elasticsearch/EsChannelWorker.java`
#### Snippet
```java
     * 
     * @param context
     * @param workerIndex
     */
    public EsChannelWorker(EsSinkContext context, int workerIndex) {
```

### JavadocDeclaration
`@param context` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/elasticsearch/DefaultEvent2IndexRequestHandler.java`
#### Snippet
```java
     * parse
     * 
     * @param  context
     * @param  event
     * @return
```

### JavadocDeclaration
`@param event` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/elasticsearch/DefaultEvent2IndexRequestHandler.java`
#### Snippet
```java
     * 
     * @param  context
     * @param  event
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/elasticsearch/DefaultEvent2IndexRequestHandler.java`
#### Snippet
```java
     * @param  context
     * @param  event
     * @return
     */
    @Override
```

### JavadocDeclaration
`@param event` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/elasticsearch/DefaultEvent2IndexRequestHandler.java`
#### Snippet
```java
     * getExtInfo
     * 
     * @param  event
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/elasticsearch/DefaultEvent2IndexRequestHandler.java`
#### Snippet
```java
     * 
     * @param  event
     * @return
     */
    public static String getExtInfo(ProfileEvent event) {
```

### JavadocDeclaration
`@param context` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/elasticsearch/EsCallbackListener.java`
#### Snippet
```java
     * Constructor
     * 
     * @param context
     */
    public EsCallbackListener(EsSinkContext context) {
```

### JavadocDeclaration
`@param executionId` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/elasticsearch/EsCallbackListener.java`
#### Snippet
```java
     * afterBulk
     * 
     * @param executionId
     * @param request
     * @param failure
```

### JavadocDeclaration
`@param request` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/elasticsearch/EsCallbackListener.java`
#### Snippet
```java
     * 
     * @param executionId
     * @param request
     * @param failure
     */
```

### JavadocDeclaration
`@param failure` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/elasticsearch/EsCallbackListener.java`
#### Snippet
```java
     * @param executionId
     * @param request
     * @param failure
     */
    @Override
```

### JavadocDeclaration
`@param executionId` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/elasticsearch/EsCallbackListener.java`
#### Snippet
```java
     * afterBulk
     * 
     * @param executionId
     * @param request
     * @param response
```

### JavadocDeclaration
`@param request` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/elasticsearch/EsCallbackListener.java`
#### Snippet
```java
     * 
     * @param executionId
     * @param request
     * @param response
     */
```

### JavadocDeclaration
`@param response` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/elasticsearch/EsCallbackListener.java`
#### Snippet
```java
     * @param executionId
     * @param request
     * @param response
     */
    @Override
```

### JavadocDeclaration
`@param executionId` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/elasticsearch/EsCallbackListener.java`
#### Snippet
```java
     * beforeBulk
     * 
     * @param executionId
     * @param request
     */
```

### JavadocDeclaration
`@param request` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/elasticsearch/EsCallbackListener.java`
#### Snippet
```java
     * 
     * @param executionId
     * @param request
     */
    @Override
```

### JavadocDeclaration
`@param cmd` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/admin/ConsumerServiceAdminEventHandler.java`
#### Snippet
```java
     * processAll
     * 
     * @param cmd
     * @param event
     * @param response
```

### JavadocDeclaration
`@param event` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/admin/ConsumerServiceAdminEventHandler.java`
#### Snippet
```java
     * 
     * @param cmd
     * @param event
     * @param response
     */
```

### JavadocDeclaration
`@param response` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/admin/ConsumerServiceAdminEventHandler.java`
#### Snippet
```java
     * @param cmd
     * @param event
     * @param response
     */
    private void processAll(String cmd, Event event, HttpServletResponse response) {
```

### JavadocDeclaration
`@param cmd` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/admin/ConsumerServiceAdminEventHandler.java`
#### Snippet
```java
     * processOne
     * 
     * @param cmd
     * @param taskName
     * @param response
```

### JavadocDeclaration
`@param taskName` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/admin/ConsumerServiceAdminEventHandler.java`
#### Snippet
```java
     * 
     * @param cmd
     * @param taskName
     * @param response
     */
```

### JavadocDeclaration
`@param response` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/admin/ConsumerServiceAdminEventHandler.java`
#### Snippet
```java
     * @param cmd
     * @param taskName
     * @param response
     */
    private void processOne(String cmd, String taskName, HttpServletResponse response) {
```

### JavadocDeclaration
`@param context` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/admin/ConsumerServiceAdminEventHandler.java`
#### Snippet
```java
     * configure
     * 
     * @param context
     */
    @Override
```

### JavadocDeclaration
`@param cmd` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/admin/ConsumerServiceAdminEventHandler.java`
#### Snippet
```java
     * process
     * 
     * @param cmd
     * @param event
     * @param response
```

### JavadocDeclaration
`@param event` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/admin/ConsumerServiceAdminEventHandler.java`
#### Snippet
```java
     * 
     * @param cmd
     * @param event
     * @param response
     */
```

### JavadocDeclaration
`@param response` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/admin/ConsumerServiceAdminEventHandler.java`
#### Snippet
```java
     * @param cmd
     * @param event
     * @param response
     */
    @Override
```

### JavadocDeclaration
`@param headers` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/channel/ProfileEvent.java`
#### Snippet
```java
    /**
     * Constructor
     * @param headers
     * @param body
     */
```

### JavadocDeclaration
`@param body` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/channel/ProfileEvent.java`
#### Snippet
```java
     * Constructor
     * @param headers
     * @param body
     */
    public ProfileEvent(Map<String, String> headers, byte[] body) {
```

### JavadocDeclaration
`@param sdkMessage` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/channel/ProfileEvent.java`
#### Snippet
```java
     * Constructor
     * 
     * @param sdkMessage
     * @param cacheRecord
     */
```

### JavadocDeclaration
`@param cacheRecord` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/channel/ProfileEvent.java`
#### Snippet
```java
     * 
     * @param sdkMessage
     * @param cacheRecord
     */
    public ProfileEvent(InLongMessage sdkMessage, CacheMessageRecord cacheRecord) {
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/elasticsearch/EsOutputChannel.java`
#### Snippet
```java
     * initEsclient
     * 
     * @return
     */
    private boolean initEsclient() {
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/elasticsearch/EsOutputChannel.java`
#### Snippet
```java
     * send
     * 
     * @throws InterruptedException
     */
    public void send() throws InterruptedException {
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/elasticsearch/EsOutputChannel.java`
#### Snippet
```java
     * initBulkprocessIfNeed
     * 
     * @return
     */
    private boolean initBulkprocessIfNeed() {
```

### JavadocDeclaration
`@param context` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/elasticsearch/EsOutputChannel.java`
#### Snippet
```java
     * Constructor
     * 
     * @param context
     */
    public EsOutputChannel(EsSinkContext context) {
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/elasticsearch/EsOutputChannel.java`
#### Snippet
```java
     * getBulkProcessor
     *
     * @return
     */
    public BulkProcessor getBulkProcessor() {
```

### JavadocDeclaration
`@param sinkName` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/elasticsearch/EsSinkContext.java`
#### Snippet
```java
     * Constructor
     * 
     * @param sinkName
     * @param context
     * @param channel
```

### JavadocDeclaration
`@param context` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/elasticsearch/EsSinkContext.java`
#### Snippet
```java
     * 
     * @param sinkName
     * @param context
     * @param channel
     * @param dispatchQueue
```

### JavadocDeclaration
`@param channel` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/elasticsearch/EsSinkContext.java`
#### Snippet
```java
     * @param sinkName
     * @param context
     * @param channel
     * @param dispatchQueue
     */
```

### JavadocDeclaration
`@param dispatchQueue` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/elasticsearch/EsSinkContext.java`
#### Snippet
```java
     * @param context
     * @param channel
     * @param dispatchQueue
     */
    public EsSinkContext(String sinkName, Context context, Channel channel,
```

### JavadocDeclaration
`@param indexRequest` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/elasticsearch/EsSinkContext.java`
#### Snippet
```java
     * offerDispatchQueue
     * 
     * @param  indexRequest
     * @return
     */
```

### JavadocDeclaration
Tag `return` is not allowed here
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/elasticsearch/EsSinkContext.java`
#### Snippet
```java
     * 
     * @param  indexRequest
     * @return
     */
    public void offerDispatchQueue(EsIndexRequest indexRequest) {
```

### JavadocDeclaration
`@param indexRequest` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/elasticsearch/EsSinkContext.java`
#### Snippet
```java
     * backDispatchQueue
     * 
     * @param  indexRequest
     * @return
     */
```

### JavadocDeclaration
Tag `return` is not allowed here
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/elasticsearch/EsSinkContext.java`
#### Snippet
```java
     * 
     * @param  indexRequest
     * @return
     */
    public void backDispatchQueue(EsIndexRequest indexRequest) {
```

### JavadocDeclaration
`@param currentRecord` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/elasticsearch/EsSinkContext.java`
#### Snippet
```java
     * addSendMetric
     * 
     * @param currentRecord
     * @param bid
     */
```

### JavadocDeclaration
`@param bid` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/elasticsearch/EsSinkContext.java`
#### Snippet
```java
     * 
     * @param currentRecord
     * @param bid
     */
    public void addSendMetric(ProfileEvent currentRecord, String bid) {
```

### JavadocDeclaration
`@param indexRequest` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/elasticsearch/EsSinkContext.java`
#### Snippet
```java
     * releaseDispatchQueue
     * 
     * @param  indexRequest
     * @return
     */
```

### JavadocDeclaration
`@param msgRecord` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/channel/CacheMessageRecord.java`
#### Snippet
```java
     * Constructor
     * 
     * @param msgRecord
     * @param client
     */
```

### JavadocDeclaration
Tag `return` is not allowed here
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/elasticsearch/EsSinkContext.java`
#### Snippet
```java
     * 
     * @param  indexRequest
     * @return
     */
    public void releaseDispatchQueue(EsIndexRequest indexRequest) {
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/elasticsearch/EsSinkContext.java`
#### Snippet
```java
     * takeDispatchQueue
     * 
     * @return
     */
    public EsIndexRequest takeDispatchQueue() {
```

### JavadocDeclaration
`@param client` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/channel/CacheMessageRecord.java`
#### Snippet
```java
     * 
     * @param msgRecord
     * @param client
     */
    public CacheMessageRecord(MessageRecord msgRecord, SortClient client, AckPolicy ackPolicy) {
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/channel/CacheMessageRecord.java`
#### Snippet
```java
    /**
     * getToken
     * @return
     */
    public Integer getToken() {
```

### JavadocDeclaration
`@param currentRecord` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/elasticsearch/EsSinkContext.java`
#### Snippet
```java
     * addSendResultMetric
     * 
     * @param currentRecord
     * @param bid
     * @param result
```

### JavadocDeclaration
`@param bid` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/elasticsearch/EsSinkContext.java`
#### Snippet
```java
     * 
     * @param currentRecord
     * @param bid
     * @param result
     * @param sendTime
```

### JavadocDeclaration
`@param result` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/elasticsearch/EsSinkContext.java`
#### Snippet
```java
     * @param currentRecord
     * @param bid
     * @param result
     * @param sendTime
     */
```

### JavadocDeclaration
`@param sendTime` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/elasticsearch/EsSinkContext.java`
#### Snippet
```java
     * @param bid
     * @param result
     * @param sendTime
     */
    public void addSendResultMetric(ProfileEvent currentRecord, String bid, boolean result, long sendTime) {
```

### JavadocDeclaration
`@param uid` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/elasticsearch/EsSinkContext.java`
#### Snippet
```java
     * getIdConfig
     * 
     * @param  uid
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/elasticsearch/EsSinkContext.java`
#### Snippet
```java
     * 
     * @param  uid
     * @return
     */
    public EsIdConfig getIdConfig(String uid) {
```

### JavadocDeclaration
`@param bufferQueue` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/channel/ProfileTransaction.java`
#### Snippet
```java
     * Constructor
     * 
     * @param bufferQueue
     */
    public ProfileTransaction(BufferQueue<ProfileEvent> bufferQueue) {
```

### JavadocDeclaration
`@param event` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/channel/ProfileTransaction.java`
#### Snippet
```java
     * doTake
     * 
     * @param event
     */
    public void doTake(ProfileEvent event) {
```

### JavadocDeclaration
`@param event` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/channel/ProfileTransaction.java`
#### Snippet
```java
     * doPut
     * 
     * @param event
     */
    public void doPut(ProfileEvent event) {
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/channel/BufferQueueChannel.java`
#### Snippet
```java
     * getTransaction
     * 
     * @return
     */
    @Override
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/channel/BufferQueueChannel.java`
#### Snippet
```java
     * 
     * @return                  Event
     * @throws ChannelException
     */
    @Override
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/channel/BufferQueueChannel.java`
#### Snippet
```java
     * getGlobalBufferQueueSizeKb
     *
     * @return
     */
    public static SizeSemaphore getGlobalBufferQueueSizeKb(Context context) {
```

### JavadocDeclaration
`@param context` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/channel/BufferQueueChannel.java`
#### Snippet
```java
     * configure
     * 
     * @param context
     */
    @Override
```

### JavadocDeclaration
`@param event` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/channel/BufferQueueChannel.java`
#### Snippet
```java
     * put
     * 
     * @param  event
     * @throws ChannelException
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/channel/BufferQueueChannel.java`
#### Snippet
```java
     * 
     * @param  event
     * @throws ChannelException
     */
    @Override
```

### JavadocDeclaration
`@param event` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/dispatch/DispatchProfile.java`
#### Snippet
```java
     * addEvent
     * 
     * @param  event
     * @param  maxPackCount
     * @param  maxPackSize
```

### JavadocDeclaration
`@param maxPackCount` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/dispatch/DispatchProfile.java`
#### Snippet
```java
     * 
     * @param  event
     * @param  maxPackCount
     * @param  maxPackSize
     * @return
```

### JavadocDeclaration
`@param maxPackSize` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/dispatch/DispatchProfile.java`
#### Snippet
```java
     * @param  event
     * @param  maxPackCount
     * @param  maxPackSize
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/dispatch/DispatchProfile.java`
#### Snippet
```java
     * @param  maxPackCount
     * @param  maxPackSize
     * @return
     */
    public boolean addEvent(ProfileEvent event, long maxPackCount, long maxPackSize) {
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/dispatch/DispatchProfile.java`
#### Snippet
```java
     * getDispatchTime
     * 
     * @return
     */
    public long getDispatchTime() {
```

### JavadocDeclaration
`@param createThreshold` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/dispatch/DispatchProfile.java`
#### Snippet
```java
     * isTimeout
     * 
     * @param  createThreshold
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/dispatch/DispatchProfile.java`
#### Snippet
```java
     * 
     * @param  createThreshold
     * @return
     */
    public boolean isTimeout(long createThreshold) {
```

### JavadocDeclaration
`@param uid` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/dispatch/DispatchProfile.java`
#### Snippet
```java
     * Constructor
     * 
     * @param uid
     * @param inlongGroupId
     * @param inlongStreamId
```

### JavadocDeclaration
`@param inlongGroupId` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/dispatch/DispatchProfile.java`
#### Snippet
```java
     * 
     * @param uid
     * @param inlongGroupId
     * @param inlongStreamId
     * @param dispatchTime
```

### JavadocDeclaration
`@param inlongStreamId` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/dispatch/DispatchProfile.java`
#### Snippet
```java
     * @param uid
     * @param inlongGroupId
     * @param inlongStreamId
     * @param dispatchTime
     */
```

### JavadocDeclaration
`@param dispatchTime` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/dispatch/DispatchProfile.java`
#### Snippet
```java
     * @param inlongGroupId
     * @param inlongStreamId
     * @param dispatchTime
     */
    public DispatchProfile(String uid, String inlongGroupId, String inlongStreamId, long dispatchTime) {
```

### JavadocDeclaration
Tag `return` is not allowed here
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/dispatch/DispatchManager.java`
#### Snippet
```java
     * outputOvertimeData
     * 
     * @return
     */
    public void outputOvertimeData() {
```

### JavadocDeclaration
`@param event` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/dispatch/DispatchManager.java`
#### Snippet
```java
     * addEvent
     * 
     * @param event
     */
    public void addEvent(ProfileEvent event) {
```

### JavadocDeclaration
`@param context` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/dispatch/DispatchManager.java`
#### Snippet
```java
     * Constructor
     * 
     * @param context
     * @param dispatchQueue
     */
```

### JavadocDeclaration
`@param dispatchQueue` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/dispatch/DispatchManager.java`
#### Snippet
```java
     * 
     * @param context
     * @param dispatchQueue
     */
    public DispatchManager(Context context, LinkedBlockingQueue<DispatchProfile> dispatchQueue) {
```

### JavadocDeclaration
`@param uid` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/clickhouse/ClickHouseSinkContext.java`
#### Snippet
```java
     * getIdConfig
     * 
     * @param  uid
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/clickhouse/ClickHouseSinkContext.java`
#### Snippet
```java
     * 
     * @param  uid
     * @return
     */
    public ClickHouseIdConfig getIdConfig(String uid) {
```

### JavadocDeclaration
`@param currentRecord` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/clickhouse/ClickHouseSinkContext.java`
#### Snippet
```java
     * addSendSuccessMetric
     * 
     * @param currentRecord
     * @param sendTime
     */
```

### JavadocDeclaration
`@param sendTime` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/clickhouse/ClickHouseSinkContext.java`
#### Snippet
```java
     * 
     * @param currentRecord
     * @param sendTime
     */
    public void addSendSuccessMetric(DispatchProfile currentRecord, long sendTime) {
```

### JavadocDeclaration
`@param newIdConfigMap` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/clickhouse/ClickHouseSinkContext.java`
#### Snippet
```java
    /**
     * initIdConfig
     * @param newIdConfigMap
     * @throws SQLException
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/clickhouse/ClickHouseSinkContext.java`
#### Snippet
```java
     * initIdConfig
     * @param newIdConfigMap
     * @throws SQLException
     */
    private void initIdConfig(Map<String, ClickHouseIdConfig> newIdConfigMap) throws SQLException {
```

### JavadocDeclaration
`@param errorMsg` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/clickhouse/ClickHouseSinkContext.java`
#### Snippet
```java
    /**
     * addReadFailMetric
     * @param errorMsg
     * @param event
     */
```

### JavadocDeclaration
`@param event` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/clickhouse/ClickHouseSinkContext.java`
#### Snippet
```java
     * addReadFailMetric
     * @param errorMsg
     * @param event
     */
    public void addSendFailMetric(String errorMsg, ProfileEvent event) {
```

### JavadocDeclaration
`@param sinkName` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/clickhouse/ClickHouseSinkContext.java`
#### Snippet
```java
     * Constructor
     * 
     * @param sinkName
     * @param context
     * @param channel
```

### JavadocDeclaration
`@param context` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/clickhouse/ClickHouseSinkContext.java`
#### Snippet
```java
     * 
     * @param sinkName
     * @param context
     * @param channel
     */
```

### JavadocDeclaration
`@param channel` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/clickhouse/ClickHouseSinkContext.java`
#### Snippet
```java
     * @param sinkName
     * @param context
     * @param channel
     */
    public ClickHouseSinkContext(String sinkName, Context context, Channel channel,
```

### JavadocDeclaration
`@param currentRecord` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/clickhouse/ClickHouseSinkContext.java`
#### Snippet
```java
     * addSendMetric
     * 
     * @param currentRecord
     */
    public void addSendMetric(DispatchProfile currentRecord) {
```

### JavadocDeclaration
`@param errorMsg` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/clickhouse/ClickHouseSinkContext.java`
#### Snippet
```java
    /**
     * addReadFailMetric
     * @param errorMsg
     * @param currentRecord
     */
```

### JavadocDeclaration
`@param currentRecord` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/clickhouse/ClickHouseSinkContext.java`
#### Snippet
```java
     * addReadFailMetric
     * @param errorMsg
     * @param currentRecord
     */
    public void addSendFailMetric(String errorMsg, DispatchProfile currentRecord) {
```

### JavadocDeclaration
`@return` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/metrics/prometheus/PrometheusMetricListener.java`
#### Snippet
```java
     * collect
     * 
     * @return
     */
    @Override
```

### JavadocDeclaration
`@param domain` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/metrics/prometheus/PrometheusMetricListener.java`
#### Snippet
```java
     * snapshot
     * 
     * @param domain
     * @param itemValues
     */
```

### JavadocDeclaration
`@param itemValues` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/metrics/prometheus/PrometheusMetricListener.java`
#### Snippet
```java
     * 
     * @param domain
     * @param itemValues
     */
    @Override
```

### JavadocDeclaration
`@param defaultDemension` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/metrics/prometheus/PrometheusMetricListener.java`
#### Snippet
```java
     * addCounterMetricFamily
     * 
     * @param defaultDemension
     * @param itemValue
     * @param idCounter
```

### JavadocDeclaration
`@param itemValue` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/metrics/prometheus/PrometheusMetricListener.java`
#### Snippet
```java
     * 
     * @param defaultDemension
     * @param itemValue
     * @param idCounter
     */
```

### JavadocDeclaration
`@param idCounter` tag description is missing
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/metrics/prometheus/PrometheusMetricListener.java`
#### Snippet
```java
     * @param defaultDemension
     * @param itemValue
     * @param idCounter
     */
    private void addCounterMetricFamily(String defaultDemension, MetricItemValue itemValue,
```

## RuleId[id=FieldMayBeFinal]
### FieldMayBeFinal
Field `timeMap` may be 'final'
in `inlong-sdk/sort-sdk/src/main/java/org/apache/inlong/sdk/sort/util/Utils.java`
#### Snippet
```java
    public static final long DAY_IN_MILLIS = 24 * 60 * 60 * 1000;

    private static ConcurrentHashMap<String, AtomicLong> timeMap = new ConcurrentHashMap<String, AtomicLong>();

    public static double toKB(long bytes) {
```

### FieldMayBeFinal
Field `consumer` may be 'final'
in `inlong-sdk/sort-sdk/src/main/java/org/apache/inlong/sdk/sort/fetcher/pulsar/PulsarSeeker.java`
#### Snippet
```java
    private static final Logger LOGGER = LoggerFactory.getLogger(PulsarSeeker.class);
    private long seekTime = -1;
    private Consumer<byte[]> consumer;
    private String topic;

```

### FieldMayBeFinal
Field `consumer` may be 'final'
in `inlong-sdk/sort-sdk/src/main/java/org/apache/inlong/sdk/sort/fetcher/kafka/KafkaSeeker.java`
#### Snippet
```java
    private long seekTime = -1;
    private String topic;
    private KafkaConsumer<byte[], byte[]> consumer;

    public KafkaSeeker(KafkaConsumer<byte[], byte[]> consumer) {
```

### FieldMayBeFinal
Field `tubeConsumerCreator` may be 'final'
in `inlong-sdk/sort-sdk/src/main/java/org/apache/inlong/sdk/sort/fetcher/tube/TubeSingleTopicFetcher.java`
#### Snippet
```java
    private PullMessageConsumer messageConsumer;
    private volatile Thread fetchThread;
    private TubeConsumerCreator tubeConsumerCreator;

    public TubeSingleTopicFetcher(
```

### FieldMayBeFinal
Field `metricValueMap` may be 'final'
in `inlong-sdk/sort-sdk/src/main/java/org/apache/inlong/sdk/sort/metrics/SortSdkPrometheusMetricListener.java`
#### Snippet
```java

    private SortSdkMetricItem metricItem;
    private Map<String, AtomicLong> metricValueMap = new ConcurrentHashMap<>();
    private List<String> dimensionKeys = new ArrayList<>();

```

### FieldMayBeFinal
Field `dimensionKeys` may be 'final'
in `inlong-sdk/sort-sdk/src/main/java/org/apache/inlong/sdk/sort/metrics/SortSdkPrometheusMetricListener.java`
#### Snippet
```java
    private SortSdkMetricItem metricItem;
    private Map<String, AtomicLong> metricValueMap = new ConcurrentHashMap<>();
    private List<String> dimensionKeys = new ArrayList<>();

    public SortSdkPrometheusMetricListener() {
```

### FieldMayBeFinal
Field `metricItem` may be 'final'
in `inlong-sdk/sort-sdk/src/main/java/org/apache/inlong/sdk/sort/metrics/SortSdkPrometheusMetricListener.java`
#### Snippet
```java
    private static final Logger LOG = LoggerFactory.getLogger(SortSdkPrometheusMetricListener.class);

    private SortSdkMetricItem metricItem;
    private Map<String, AtomicLong> metricValueMap = new ConcurrentHashMap<>();
    private List<String> dimensionKeys = new ArrayList<>();
```

### FieldMayBeFinal
Field `consumerSize` may be 'final'
in `inlong-sdk/sort-sdk/src/main/java/org/apache/inlong/sdk/sort/manager/InlongMultiTopicManager.java`
#### Snippet
```java

    private boolean stopAssign = false;
    private int consumerSize;

    public InlongMultiTopicManager(ClientContext context, QueryConsumeConfig queryConsumeConfig) {
```

### FieldMayBeFinal
Field `pulsarClient` may be 'final'
in `inlong-sdk/sort-sdk/src/main/java/org/apache/inlong/sdk/sort/fetcher/pulsar/PulsarSingleTopicFetcher.java`
#### Snippet
```java
    private final ConcurrentHashMap<String, MessageId> offsetCache = new ConcurrentHashMap<>();
    private Consumer<byte[]> consumer;
    private PulsarClient pulsarClient;

    public PulsarSingleTopicFetcher(
```

### FieldMayBeFinal
Field `pulsarClient` may be 'final'
in `inlong-sdk/sort-sdk/src/main/java/org/apache/inlong/sdk/sort/fetcher/pulsar/PulsarMultiTopicsFetcher.java`
#### Snippet
```java
    private PulsarConsumer currentConsumer;
    private List<PulsarConsumer> toBeRemovedConsumers = new LinkedList<>();
    private PulsarClient pulsarClient;

    public PulsarMultiTopicsFetcher(
```

### FieldMayBeFinal
Field `handlerMap` may be 'final'
in `inlong-sdk/sdk-common/src/main/java/org/apache/inlong/sdk/commons/admin/AdminJsonHandler.java`
#### Snippet
```java
    private Context context;
    private JSONHandler requestHandler;
    private Map<String, AdminEventHandler> handlerMap = new HashMap<>();

    /**
```

### FieldMayBeFinal
Field `name` may be 'final'
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/LoadBalance.java`
#### Snippet
```java
    WEIGHT_ROBIN("weight robin", 4);

    private String name;
    private int index;

```

### FieldMayBeFinal
Field `index` may be 'final'
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/LoadBalance.java`
#### Snippet
```java

    private String name;
    private int index;

    private LoadBalance(String name, int index) {
```

### FieldMayBeFinal
Field `context` may be 'final'
in `inlong-sdk/sdk-common/src/main/java/org/apache/inlong/sdk/commons/admin/AdminTask.java`
#### Snippet
```java
    public static final String FLUME_ROOT = "admin";

    private Context context;
    private final LifecycleSupervisor supervisor;
    private MaterializedConfiguration materializedConfiguration;
```

### FieldMayBeFinal
Field `manager` may be 'final'
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/SdkSenderClientHandler.java`
#### Snippet
```java
    public static final Logger LOG = LoggerFactory.getLogger(SdkSenderClientHandler.class);

    private SdkProxyChannelManager manager;

    /**
```

### FieldMayBeFinal
Field `value` may be 'final'
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/config/LoaderType.java`
#### Snippet
```java
    File("file"), Context("context"), Manager("manager"), Plugin("plugin");

    private String value;

    /**
```

### FieldMayBeFinal
Field `totalBuffer` may be 'final'
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/SdkChannelWorker.java`
#### Snippet
```java

    // package buffer
    private ChannelBuffer totalBuffer = ChannelBuffers.dynamicBuffer();

    /**
```

### FieldMayBeFinal
Field `context` may be 'final'
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/SdkChannelWorker.java`
#### Snippet
```java
    // manager
    private final SdkProxyChannelManager manager;
    private SdkSinkContext context;
    private LifecycleState status;

```

### FieldMayBeFinal
Field `dispatchQueue` may be 'final'
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/ProxySdkSink.java`
#### Snippet
```java
    private SdkSinkContext context;
    private DispatchManager dispatchManager;
    private LinkedBlockingQueue<DispatchProfile> dispatchQueue = new LinkedBlockingQueue<>();
    //
    protected Timer sinkTimer;
```

### FieldMayBeFinal
Field `takeCounter` may be 'final'
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/SdkProxyChannelManager.java`
#### Snippet
```java
    private AtomicLong offerCounter = new AtomicLong(0);
    private ConcurrentHashMap<String, AtomicLong> offerGroupCounter = new ConcurrentHashMap<>();
    private AtomicLong takeCounter = new AtomicLong(0);
    private Timer reloadTimer;
    private List<SdkChannelWorker> workers = new ArrayList<>();
```

### FieldMayBeFinal
Field `proxyClusterId` may be 'final'
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/SdkProxyChannelManager.java`
#### Snippet
```java

    // proxyClusterId
    private String proxyClusterId;
    private SdkSinkContext context;
    //
```

### FieldMayBeFinal
Field `profileMap` may be 'final'
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/SdkProxyChannelManager.java`
#### Snippet
```java
    //
    private AtomicLong sdkPackId = new AtomicLong(RandomUtils.nextLong());
    private ConcurrentHashMap<Long, SdkProfile> profileMap = new ConcurrentHashMap<>();

    /**
```

### FieldMayBeFinal
Field `sdkPackId` may be 'final'
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/SdkProxyChannelManager.java`
#### Snippet
```java
    private TcpChannelGroup sender;
    //
    private AtomicLong sdkPackId = new AtomicLong(RandomUtils.nextLong());
    private ConcurrentHashMap<Long, SdkProfile> profileMap = new ConcurrentHashMap<>();

```

### FieldMayBeFinal
Field `proxyDispatchQueue` may be 'final'
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/SdkProxyChannelManager.java`
#### Snippet
```java
    private SdkSinkContext context;
    //
    private LinkedBlockingQueue<DispatchProfile> proxyDispatchQueue = new LinkedBlockingQueue<>();
    private AtomicLong offerCounter = new AtomicLong(0);
    private ConcurrentHashMap<String, AtomicLong> offerGroupCounter = new ConcurrentHashMap<>();
```

### FieldMayBeFinal
Field `offerCounter` may be 'final'
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/SdkProxyChannelManager.java`
#### Snippet
```java
    //
    private LinkedBlockingQueue<DispatchProfile> proxyDispatchQueue = new LinkedBlockingQueue<>();
    private AtomicLong offerCounter = new AtomicLong(0);
    private ConcurrentHashMap<String, AtomicLong> offerGroupCounter = new ConcurrentHashMap<>();
    private AtomicLong takeCounter = new AtomicLong(0);
```

### FieldMayBeFinal
Field `context` may be 'final'
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/SdkProxyChannelManager.java`
#### Snippet
```java
    // proxyClusterId
    private String proxyClusterId;
    private SdkSinkContext context;
    //
    private LinkedBlockingQueue<DispatchProfile> proxyDispatchQueue = new LinkedBlockingQueue<>();
```

### FieldMayBeFinal
Field `workers` may be 'final'
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/SdkProxyChannelManager.java`
#### Snippet
```java
    private AtomicLong takeCounter = new AtomicLong(0);
    private Timer reloadTimer;
    private List<SdkChannelWorker> workers = new ArrayList<>();
    private TcpChannelGroup sender;
    //
```

### FieldMayBeFinal
Field `offerGroupCounter` may be 'final'
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/SdkProxyChannelManager.java`
#### Snippet
```java
    private LinkedBlockingQueue<DispatchProfile> proxyDispatchQueue = new LinkedBlockingQueue<>();
    private AtomicLong offerCounter = new AtomicLong(0);
    private ConcurrentHashMap<String, AtomicLong> offerGroupCounter = new ConcurrentHashMap<>();
    private AtomicLong takeCounter = new AtomicLong(0);
    private Timer reloadTimer;
```

### FieldMayBeFinal
Field `name` may be 'final'
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java
    public static final Logger LOG = LoggerFactory.getLogger(PbProtocolMessageSender.class);

    private String name;
    private String localIp;
    private LifecycleState lifecycleState;
```

### FieldMayBeFinal
Field `localIp` may be 'final'
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/PbProtocolMessageSender.java`
#### Snippet
```java

    private String name;
    private String localIp;
    private LifecycleState lifecycleState;
    private Context context;
```

### FieldMayBeFinal
Field `value` may be 'final'
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/config/pojo/ErrorCode.java`
#### Snippet
```java
    SUCC(0), NOUPDATE(1), FAIL(-1), REQ_PARAMS_ERROR(-101);

    private int value;

    /**
```

### FieldMayBeFinal
Field `LOG` may be 'final'
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/config/ManagerProxyClusterConfigLoader.java`
#### Snippet
```java
public class ManagerProxyClusterConfigLoader implements ProxyClusterConfigLoader {

    private static Logger LOG = LoggerFactory.getLogger(ManagerProxyClusterConfigLoader.class);
    private Context context;
    private CloseableHttpClient httpClient;
```

### FieldMayBeFinal
Field `leftSemaphore` may be 'final'
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/channel/SizeSemaphore.java`
#### Snippet
```java
    private int leftSize = 0;
    private Semaphore sizeSemaphore = null;
    private AtomicInteger leftSemaphore = new AtomicInteger(0);

    /**
```

### FieldMayBeFinal
Field `putList` may be 'final'
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/channel/ProfileTransaction.java`
#### Snippet
```java
    private List<CallbackProfile> takeList = new ArrayList<>();
    private AtomicLong takeCounter = new AtomicLong(0);
    private List<CallbackProfile> putList = new ArrayList<>();

    /**
```

### FieldMayBeFinal
Field `takeList` may be 'final'
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/channel/ProfileTransaction.java`
#### Snippet
```java

    private BufferQueue<CallbackProfile> bufferQueue;
    private List<CallbackProfile> takeList = new ArrayList<>();
    private AtomicLong takeCounter = new AtomicLong(0);
    private List<CallbackProfile> putList = new ArrayList<>();
```

### FieldMayBeFinal
Field `bufferQueue` may be 'final'
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/channel/ProfileTransaction.java`
#### Snippet
```java
    public static final Logger LOG = LoggerFactory.getLogger(ProfileTransaction.class);

    private BufferQueue<CallbackProfile> bufferQueue;
    private List<CallbackProfile> takeList = new ArrayList<>();
    private AtomicLong takeCounter = new AtomicLong(0);
```

### FieldMayBeFinal
Field `takeCounter` may be 'final'
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/channel/ProfileTransaction.java`
#### Snippet
```java
    private BufferQueue<CallbackProfile> bufferQueue;
    private List<CallbackProfile> takeList = new ArrayList<>();
    private AtomicLong takeCounter = new AtomicLong(0);
    private List<CallbackProfile> putList = new ArrayList<>();

```

### FieldMayBeFinal
Field `LOG` may be 'final'
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/config/FileProxyClusterConfigLoader.java`
#### Snippet
```java
public class FileProxyClusterConfigLoader implements ProxyClusterConfigLoader {

    private static Logger LOG = LoggerFactory.getLogger(FileProxyClusterConfigLoader.class);
    private Context context;

```

### FieldMayBeFinal
Field `putCounter` may be 'final'
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/channel/BufferQueueChannel.java`
#### Snippet
```java
    protected Timer channelTimer;
    private AtomicLong takeCounter = new AtomicLong(0);
    private AtomicLong putCounter = new AtomicLong(0);

    /**
```

### FieldMayBeFinal
Field `currentTransaction` may be 'final'
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/channel/BufferQueueChannel.java`
#### Snippet
```java
    private static SizeSemaphore globalBufferQueueSizeKb;
    private BufferQueue<CallbackProfile> bufferQueue;
    private ThreadLocal<ProfileTransaction> currentTransaction = new ThreadLocal<ProfileTransaction>();
    protected Timer channelTimer;
    private AtomicLong takeCounter = new AtomicLong(0);
```

### FieldMayBeFinal
Field `takeCounter` may be 'final'
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/channel/BufferQueueChannel.java`
#### Snippet
```java
    private ThreadLocal<ProfileTransaction> currentTransaction = new ThreadLocal<ProfileTransaction>();
    protected Timer channelTimer;
    private AtomicLong takeCounter = new AtomicLong(0);
    private AtomicLong putCounter = new AtomicLong(0);

```

### FieldMayBeFinal
Field `packToken` may be 'final'
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/network/TcpChannel.java`
#### Snippet
```java
    private boolean hasException = false;
    private boolean reconnectFail = false;
    private Semaphore packToken;

    /**
```

### FieldMayBeFinal
Field `createTime` may be 'final'
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/dispatch/DispatchProfile.java`
#### Snippet
```java
    private final String uid;
    private List<ProfileEvent> events = new ArrayList<>();
    private long createTime = System.currentTimeMillis();
    private long count = 0;
    private long size = 0;
```

### FieldMayBeFinal
Field `dispatchTime` may be 'final'
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/dispatch/DispatchProfile.java`
#### Snippet
```java
    private long count = 0;
    private long size = 0;
    private long dispatchTime;
    private long sendTime;

```

### FieldMayBeFinal
Field `needOutputOvertimeData` may be 'final'
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/dispatch/DispatchManager.java`
#### Snippet
```java
    private ConcurrentHashMap<String, DispatchProfile> profileCache = new ConcurrentHashMap<>();
    // needOutputOvertimeData
    private AtomicBoolean needOutputOvertimeData = new AtomicBoolean(false);

    /**
```

### FieldMayBeFinal
Field `dispatchQueue` may be 'final'
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/dispatch/DispatchManager.java`
#### Snippet
```java
    public static final long MINUTE_MS = 60L * 1000;

    private LinkedBlockingQueue<DispatchProfile> dispatchQueue;
    private final long dispatchTimeout;
    private final long maxPackCount;
```

### FieldMayBeFinal
Field `profileCache` may be 'final'
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/dispatch/DispatchManager.java`
#### Snippet
```java
    private final long maxPackCount;
    private final long maxPackSize;
    private ConcurrentHashMap<String, DispatchProfile> profileCache = new ConcurrentHashMap<>();
    // needOutputOvertimeData
    private AtomicBoolean needOutputOvertimeData = new AtomicBoolean(false);
```

### FieldMayBeFinal
Field `channelLists` may be 'final'
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/network/TcpChannelGroup.java`
#### Snippet
```java
    private List<LinkedBlockingQueue<TcpChannel>> channelQueues = new ArrayList<>();
    // for close
    private List<List<TcpChannel>> channelLists = new ArrayList<>();
    private int mIndex = 0;
    // for callback
```

### FieldMayBeFinal
Field `channelMap` may be 'final'
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/network/TcpChannelGroup.java`
#### Snippet
```java
    private int mIndex = 0;
    // for callback
    private ConcurrentHashMap<Object, TcpChannel> channelMap = new ConcurrentHashMap<>();
    private AtomicLong channelId = new AtomicLong(0);

```

### FieldMayBeFinal
Field `channelQueues` may be 'final'
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/network/TcpChannelGroup.java`
#### Snippet
```java
    private Set<IpPort> currentIpLists = new HashSet<>();
    // for send
    private List<LinkedBlockingQueue<TcpChannel>> channelQueues = new ArrayList<>();
    // for close
    private List<List<TcpChannel>> channelLists = new ArrayList<>();
```

### FieldMayBeFinal
Field `channelId` may be 'final'
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/network/TcpChannelGroup.java`
#### Snippet
```java
    // for callback
    private ConcurrentHashMap<Object, TcpChannel> channelMap = new ConcurrentHashMap<>();
    private AtomicLong channelId = new AtomicLong(0);

    /**
```

### FieldMayBeFinal
Field `client` may be 'final'
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/network/TcpChannelGroup.java`
#### Snippet
```java
    private String bid;
    private int senderThreadNum;
    private ClientBootstrap client = new ClientBootstrap();
    private Set<IpPort> currentIpLists = new HashSet<>();
    // for send
```

### FieldMayBeFinal
Field `senderThreadNum` may be 'final'
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/network/TcpChannelGroup.java`
#### Snippet
```java

    private String bid;
    private int senderThreadNum;
    private ClientBootstrap client = new ClientBootstrap();
    private Set<IpPort> currentIpLists = new HashSet<>();
```

### FieldMayBeFinal
Field `bid` may be 'final'
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/network/TcpChannelGroup.java`
#### Snippet
```java
    public static final Logger LOG = LoggerFactory.getLogger(TcpChannelGroup.class);

    private String bid;
    private int senderThreadNum;
    private ClientBootstrap client = new ClientBootstrap();
```

### FieldMayBeFinal
Field `arraySed` may be 'final'
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/utils/ServiceDiscoveryUtils.java`
#### Snippet
```java
    private static final String GET_MANAGER_IP_LIST_API = "/inlong/manager/openapi/agent/getManagerIpList";
    private static String latestManagerIPList = "";
    private static String arraySed = ",";

    /**
```

### FieldMayBeFinal
Field `lastUpdateTime` may be 'final'
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/config/EncryptConfigEntry.java`
#### Snippet
```java
    private byte[] aesKey;
    private String rsaEncryptedKey;
    private AtomicLong lastUpdateTime = new AtomicLong(0);

    public EncryptConfigEntry(final String userName, final String version, final String pubKey) {
```

### FieldMayBeFinal
Field `id` may be 'final'
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/SequentialID.java`
#### Snippet
```java
    private static final long maxId = 2000000000;
    private String ip = null;
    private AtomicLong id = new AtomicLong(0);

    public SequentialID(String theIp) {
```

### FieldMayBeFinal
Field `count` may be 'final'
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/TimeScanObject.java`
#### Snippet
```java
public class TimeScanObject {

    private AtomicInteger count = new AtomicInteger(0);
    private AtomicLong time = new AtomicLong(0);

```

### FieldMayBeFinal
Field `time` may be 'final'
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/TimeScanObject.java`
#### Snippet
```java

    private AtomicInteger count = new AtomicInteger(0);
    private AtomicLong time = new AtomicLong(0);

    public TimeScanObject() {
```

### FieldMayBeFinal
Field `bootstrap` may be 'final'
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/NettyClient.java`
#### Snippet
```java
    private ConnState connState;
    private ProxyClientConfig configure;
    private Bootstrap bootstrap;
    private String serverIP;
    private int serverPort;
```

### FieldMayBeFinal
Field `serverPort` may be 'final'
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/NettyClient.java`
#### Snippet
```java
    private Bootstrap bootstrap;
    private String serverIP;
    private int serverPort;

    public String getServerIP() {
```

### FieldMayBeFinal
Field `configure` may be 'final'
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/NettyClient.java`
#### Snippet
```java

    private ConnState connState;
    private ProxyClientConfig configure;
    private Bootstrap bootstrap;
    private String serverIP;
```

### FieldMayBeFinal
Field `nodeList` may be 'final'
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/HashRing.java`
#### Snippet
```java
    private int virtualNode = ConfigConstants.DEFAULT_VIRTUAL_NODE;
    private TreeMap<String, HostInfo> virtualNode2RealNode;
    private List<HostInfo> nodeList;

    private HashRing() {
```

### FieldMayBeFinal
Field `virtualNode2RealNode` may be 'final'
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/HashRing.java`
#### Snippet
```java
    private static volatile HashRing instance;
    private int virtualNode = ConfigConstants.DEFAULT_VIRTUAL_NODE;
    private TreeMap<String, HostInfo> virtualNode2RealNode;
    private List<HostInfo> nodeList;

```

### FieldMayBeFinal
Field `idGenerator` may be 'final'
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/example/UdpClientExample.java`
#### Snippet
```java
    private static final Logger logger = LoggerFactory.getLogger(UdpClientExample.class);

    private static SequentialID idGenerator = new SequentialID(Utils.getLocalIp());

    private static SecureRandom random = new SecureRandom();
```

### FieldMayBeFinal
Field `random` may be 'final'
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/example/UdpClientExample.java`
#### Snippet
```java
    private static SequentialID idGenerator = new SequentialID(Utils.getLocalIp());

    private static SecureRandom random = new SecureRandom();

    public static void main(String[] args) {
```

### FieldMayBeFinal
Field `description` may be 'final'
in `inlong-sort/sort-flink/base/src/main/java/org/apache/inlong/sort/base/sink/SchemaUpdateExceptionPolicy.java`
#### Snippet
```java
    THROW_WITH_STOP("Throw exception to stop flink job when meet schema update.");

    private String description;

    SchemaUpdateExceptionPolicy(String description) {
```

### FieldMayBeFinal
Field `sparkEngineEnable` may be 'final'
in `inlong-sort/sort-flink/base/src/main/java/org/apache/inlong/sort/base/sink/MultipleSinkOption.java`
#### Snippet
```java
    private final String format;

    private boolean sparkEngineEnable;

    private final SchemaUpdateExceptionPolicy schemaUpdatePolicy;
```

### FieldMayBeFinal
Field `description` may be 'final'
in `inlong-sort/sort-flink/base/src/main/java/org/apache/inlong/sort/base/sink/TableChange.java`
#### Snippet
```java
    final class UnknownColumnChange implements ColumnChange {

        private String description;

        public UnknownColumnChange(String description) {
```

### FieldMayBeFinal
Field `ipManager` may be 'final'
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/ClientMgr.java`
#### Snippet
```java
    private int realSize;
    private SendHBThread sendHBThread;
    private ProxyConfigManager ipManager;
    private int groupIdNum = 0;
    private String groupId = "";
```

### FieldMayBeFinal
Field `loadBalance` may be 'final'
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/ClientMgr.java`
#### Snippet
```java
    private int loadThreshold;
    private int loadCycle = 0;
    private LoadBalance loadBalance;

    public ClientMgr(ProxyClientConfig configure, Sender sender) throws Exception {
```

### FieldMayBeFinal
Field `sender` may be 'final'
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/ClientMgr.java`
#### Snippet
```java
    private int currentIndex = 0;
    private ProxyClientConfig configure;
    private Sender sender;
    private int aliveConnections;
    private int realSize;
```

### FieldMayBeFinal
Field `sendHBThread` may be 'final'
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/ClientMgr.java`
#### Snippet
```java
    private int aliveConnections;
    private int realSize;
    private SendHBThread sendHBThread;
    private ProxyConfigManager ipManager;
    private int groupIdNum = 0;
```

### FieldMayBeFinal
Field `configure` may be 'final'
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/ClientMgr.java`
#### Snippet
```java
    private Bootstrap bootstrap;
    private int currentIndex = 0;
    private ProxyClientConfig configure;
    private Sender sender;
    private int aliveConnections;
```

### FieldMayBeFinal
Field `bootstrap` may be 'final'
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/ClientMgr.java`
#### Snippet
```java
    private final ReentrantReadWriteLock fsLock = new ReentrantReadWriteLock(true);
    private List<HostInfo> proxyInfoList = new ArrayList<>();
    private Bootstrap bootstrap;
    private int currentIndex = 0;
    private ProxyClientConfig configure;
```

### FieldMayBeFinal
Field `ipPorts` may be 'final'
in `inlong-sort/sort-flink/base/src/main/java/org/apache/inlong/sort/base/metric/MetricOption.java`
#### Snippet
```java
    private Map<String, String> labels;
    private HashSet<String> ipPortList;
    private String ipPorts;
    private RegisteredMetric registeredMetric;
    private long initRecords;
```

### FieldMayBeFinal
Field `labels` may be 'final'
in `inlong-sort/sort-flink/base/src/main/java/org/apache/inlong/sort/base/metric/MetricOption.java`
#### Snippet
```java
    private static final long serialVersionUID = 1L;

    private Map<String, String> labels;
    private HashSet<String> ipPortList;
    private String ipPorts;
```

### FieldMayBeFinal
Field `migrateAll` may be 'final'
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/debezium/table/AppendMetadataCollector.java`
#### Snippet
```java
    public transient SourceRecord inputRecord;
    public transient Collector<RowData> outputCollector;
    private boolean migrateAll;

    public AppendMetadataCollector(MetadataConverter[] metadataConverters, boolean migrateAll) {
```

### FieldMayBeFinal
Field `jobConfWrapper` may be 'final'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/hive/src/main/java/org/apache/inlong/sort/hive/HadoopFileSystemFactory.java`
#### Snippet
```java
    private static final long serialVersionUID = 1L;

    private JobConfWrapper jobConfWrapper;

    HadoopFileSystemFactory(JobConf jobConf) {
```

### FieldMayBeFinal
Field `migrateAll` may be 'final'
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/debezium/table/RowDataDebeziumDeserializeSchema.java`
#### Snippet
```java
    private final RowValidator rowKindValidator;

    private boolean migrateAll;

    private ZoneId serverTimeZone;
```

### FieldMayBeFinal
Field `serverTimeZone` may be 'final'
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/debezium/table/RowDataDebeziumDeserializeSchema.java`
#### Snippet
```java
    private boolean migrateAll;

    private ZoneId serverTimeZone;

    public final ObjectMapper objectMapper = new ObjectMapper();
```

### FieldMayBeFinal
Field `sd` may be 'final'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/hive/src/main/java/org/apache/inlong/sort/hive/HiveTableMetaStoreFactory.java`
#### Snippet
```java

        private HiveMetastoreClientWrapper client;
        private StorageDescriptor sd;

        private HiveTableMetaStore() throws TException {
```

### FieldMayBeFinal
Field `client` may be 'final'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/hive/src/main/java/org/apache/inlong/sort/hive/HiveTableMetaStoreFactory.java`
#### Snippet
```java
    private class HiveTableMetaStore implements TableMetaStore {

        private HiveMetastoreClientWrapper client;
        private StorageDescriptor sd;

```

### FieldMayBeFinal
Field `reportPeriodSeconds` may be 'final'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/hudi/src/main/java/org/apache/inlong/sort/hudi/metric/InLongHudiAuditReporter.java`
#### Snippet
```java

    private HudiAuditReporter hudiAuditReporter;
    private Integer reportPeriodSeconds;

    public InLongHudiAuditReporter(Properties props, MetricRegistry registry) {
```

### FieldMayBeFinal
Field `byteMetricNames` may be 'final'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/hudi/src/main/java/org/apache/inlong/sort/hudi/metric/HudiAuditReporter.java`
#### Snippet
```java
    private static final Logger LOG = LoggerFactory.getLogger(HudiAuditReporter.class);
    private final LinkedHashMap<String, String> labels;
    private Set<String> byteMetricNames;
    private Set<String> recordMetricNames;
    private AuditOperator auditOperator;
```

### FieldMayBeFinal
Field `recordMetricNames` may be 'final'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/hudi/src/main/java/org/apache/inlong/sort/hudi/metric/HudiAuditReporter.java`
#### Snippet
```java
    private final LinkedHashMap<String, String> labels;
    private Set<String> byteMetricNames;
    private Set<String> recordMetricNames;
    private AuditOperator auditOperator;

```

### FieldMayBeFinal
Field `executionOptionsBuilder` may be 'final'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/jdbc/src/main/java/org/apache/inlong/sort/jdbc/internal/JdbcBatchingOutputFormat.java`
#### Snippet
```java
        private String inlongMetric;
        private String auditHostAndPorts;
        private JdbcExecutionOptions.Builder executionOptionsBuilder =
                JdbcExecutionOptions.builder();
        private DirtyOptions dirtyOptions;
```

### FieldMayBeFinal
Field `multipleSink` may be 'final'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kafka/src/main/java/org/apache/inlong/sort/kafka/KafkaDynamicSink.java`
#### Snippet
```java
    protected DataType consumedDataType;

    private boolean multipleSink;

    /**
```

### FieldMayBeFinal
Field `jdbcOptions` may be 'final'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/jdbc/src/main/java/org/apache/inlong/sort/jdbc/internal/JdbcMultiBatchingOutputFormat.java`
#### Snippet
```java
    private transient JsonDynamicSchemaFormat jsonDynamicSchemaFormat;
    private JdbcDmlOptions dmlOptions;
    private JdbcOptions jdbcOptions;
    private boolean appendMode;
    private transient Map<String, JdbcExec> jdbcExecMap = new HashMap<>();
```

### FieldMayBeFinal
Field `appendMode` may be 'final'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/jdbc/src/main/java/org/apache/inlong/sort/jdbc/internal/JdbcMultiBatchingOutputFormat.java`
#### Snippet
```java
    private JdbcDmlOptions dmlOptions;
    private JdbcOptions jdbcOptions;
    private boolean appendMode;
    private transient Map<String, JdbcExec> jdbcExecMap = new HashMap<>();
    private transient Map<String, SimpleJdbcConnectionProvider> connectionExecProviderMap = new HashMap<>();
```

### FieldMayBeFinal
Field `dmlOptions` may be 'final'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/jdbc/src/main/java/org/apache/inlong/sort/jdbc/internal/JdbcMultiBatchingOutputFormat.java`
#### Snippet
```java
    private transient RuntimeContext runtimeContext;
    private transient JsonDynamicSchemaFormat jsonDynamicSchemaFormat;
    private JdbcDmlOptions dmlOptions;
    private JdbcOptions jdbcOptions;
    private boolean appendMode;
```

### FieldMayBeFinal
Field `databasePattern` may be 'final'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kafka/src/main/java/org/apache/inlong/sort/kafka/partitioner/InLongFixedPartitionPartitioner.java`
#### Snippet
```java
    private final String DEFAULT_PARTITION = "DEFAULT_PARTITION";

    private String databasePattern;
    private String tablePattern;

```

### FieldMayBeFinal
Field `tablePattern` may be 'final'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kafka/src/main/java/org/apache/inlong/sort/kafka/partitioner/InLongFixedPartitionPartitioner.java`
#### Snippet
```java

    private String databasePattern;
    private String tablePattern;

    private final static String DELIMITER1 = "&";
```

### FieldMayBeFinal
Field `inlongMetric` may be 'final'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kafka/src/main/java/org/apache/inlong/sort/kafka/FlinkKafkaConsumerBase.java`
#### Snippet
```java
     * Metric for InLong
     */
    private String inlongMetric;
    /**
     * audit host and ports
```

### FieldMayBeFinal
Field `auditKeys` may be 'final'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kafka/src/main/java/org/apache/inlong/sort/kafka/FlinkKafkaConsumerBase.java`
#### Snippet
```java
    private SourceMetricData sourceMetricData;

    private String auditKeys;

    // ------------------------------------------------------------------------
```

### FieldMayBeFinal
Field `inlongAudit` may be 'final'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kafka/src/main/java/org/apache/inlong/sort/kafka/FlinkKafkaConsumerBase.java`
#### Snippet
```java
     * audit host and ports
     */
    private String inlongAudit;

    private SourceMetricData sourceMetricData;
```

### FieldMayBeFinal
Field `dirtySinkHelper` may be 'final'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/doris/src/main/java/org/apache/inlong/sort/doris/table/DorisDynamicSchemaOutputFormat.java`
#### Snippet
```java
    private String columns;
    private final LogicalType[] logicalTypes;
    private DirtySinkHelper<Object> dirtySinkHelper;
    private transient Schema schema;

```

### FieldMayBeFinal
Field `redisCommand` may be 'final'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/redis/src/main/java/org/apache/inlong/sort/redis/common/mapper/RedisCommandDescription.java`
#### Snippet
```java
    private static final long serialVersionUID = 1L;

    private RedisCommand redisCommand;

    /**
```

### FieldMayBeFinal
Field `additionalTTL` may be 'final'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/redis/src/main/java/org/apache/inlong/sort/redis/common/mapper/RedisCommandDescription.java`
#### Snippet
```java
     * <p>For {@link RedisCommand#SETEX}, we need key, value and time to live (TTL).
     */
    private Integer additionalTTL;

    /**
```

### FieldMayBeFinal
Field `redisDataType` may be 'final'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/redis/src/main/java/org/apache/inlong/sort/redis/common/mapper/RedisCommand.java`
#### Snippet
```java
     * The {@link RedisDataType} this command belongs to.
     */
    private RedisDataType redisDataType;

    RedisCommand(RedisDataType redisDataType) {
```

### FieldMayBeFinal
Field `additionalKey` may be 'final'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/redis/src/main/java/org/apache/inlong/sort/redis/common/mapper/RedisCommandDescription.java`
#### Snippet
```java
     * {@link #getAdditionalKey()} used as set name for {@link RedisDataType#SORTED_SET}
     */
    private String additionalKey;

    /**
```

### FieldMayBeFinal
Field `rowSize` may be 'final'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kafka/src/main/java/org/apache/inlong/sort/kafka/FlinkKafkaProducer.java`
#### Snippet
```java
    private SinkTopicMetricData sinkMetricData;
    private Long dataSize = 0L;
    private Long rowSize = 0L;

    private transient ListState<MetricState> metricStateListState;
```

### FieldMayBeFinal
Field `multipleSink` may be 'final'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kafka/src/main/java/org/apache/inlong/sort/kafka/FlinkKafkaProducer.java`
#### Snippet
```java
    protected transient volatile Exception asyncException;

    private boolean multipleSink;
    /**
     * sink metric data
```

### FieldMayBeFinal
Field `valueDataType` may be 'final'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/pulsar/src/main/java/org/apache/inlong/sort/pulsar/table/DynamicPulsarSerializationSchema.java`
#### Snippet
```java
    private int numParallelInstances;

    private DataType valueDataType;

    private String valueFormatType;
```

### FieldMayBeFinal
Field `valueFormatType` may be 'final'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/pulsar/src/main/java/org/apache/inlong/sort/pulsar/table/DynamicPulsarSerializationSchema.java`
#### Snippet
```java
    private DataType valueDataType;

    private String valueFormatType;

    private volatile Schema<RowData> schema;
```

### FieldMayBeFinal
Field `delayMilliseconds` may be 'final'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/pulsar/src/main/java/org/apache/inlong/sort/pulsar/table/DynamicPulsarSerializationSchema.java`
#### Snippet
```java
     * delay milliseconds message.
     */
    private long delayMilliseconds;

    DynamicPulsarSerializationSchema(
```

### FieldMayBeFinal
Field `useEarliestWhenDataLoss` may be 'final'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/pulsar/src/main/java/org/apache/inlong/sort/pulsar/internal/PulsarFetcher.java`
#### Snippet
```java
    private boolean failOnDataLoss = true;

    private boolean useEarliestWhenDataLoss;

    /** topic poison state */
```

### FieldMayBeFinal
Field `poisonState` may be 'final'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/pulsar/src/main/java/org/apache/inlong/sort/pulsar/internal/PulsarFetcher.java`
#### Snippet
```java

    /** topic poison state */
    private PoisonState poisonState;

    // ------------------------------------------------------------------------
```

### FieldMayBeFinal
Field `partitionGroupBuffer` may be 'final'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/iceberg/src/main/java/org/apache/inlong/sort/iceberg/sink/IcebergMiniBatchGroupOperator.java`
#### Snippet
```java

    private transient StreamRecordCollector<RowData> collector;
    private PartitionGroupBuffer partitionGroupBuffer;

    public IcebergMiniBatchGroupOperator(PartitionGroupBuffer partitionGroupBuffer) {
```

### FieldMayBeFinal
Field `inlongMetric` may be 'final'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/pulsar/src/main/java/org/apache/inlong/sort/pulsar/internal/FlinkPulsarSourceWithoutAdmin.java`
#### Snippet
```java
     * Metric for InLong
     */
    private String inlongMetric;
    /**
     * audit host and ports
```

### FieldMayBeFinal
Field `subscriptionPosition` may be 'final'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/pulsar/src/main/java/org/apache/inlong/sort/pulsar/internal/FlinkPulsarSourceWithoutAdmin.java`
#### Snippet
```java
     * Only relevant when startup mode is {@link StartupMode#EXTERNAL_SUBSCRIPTION}.
     */
    private MessageId subscriptionPosition = MessageId.latest;

    // TODO: remove this when MessageId is serializable itself.
```

### FieldMayBeFinal
Field `inlongAuditKeys` may be 'final'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/pulsar/src/main/java/org/apache/inlong/sort/pulsar/internal/FlinkPulsarSourceWithoutAdmin.java`
#### Snippet
```java
    private String inlongAudit;

    private String inlongAuditKeys;

    private SourceMetricData sourceMetricData;
```

### FieldMayBeFinal
Field `inlongAudit` may be 'final'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/pulsar/src/main/java/org/apache/inlong/sort/pulsar/internal/FlinkPulsarSourceWithoutAdmin.java`
#### Snippet
```java
     * audit host and ports
     */
    private String inlongAudit;

    private String inlongAuditKeys;
```

### FieldMayBeFinal
Field `tableId` may be 'final'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/iceberg/src/main/java/org/apache/inlong/sort/iceberg/sink/multiple/MultipleWriteResult.java`
#### Snippet
```java
public class MultipleWriteResult {

    private TableIdentifier tableId;

    private WriteResult writeResult;
```

### FieldMayBeFinal
Field `writeResult` may be 'final'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/iceberg/src/main/java/org/apache/inlong/sort/iceberg/sink/multiple/MultipleWriteResult.java`
#### Snippet
```java
    private TableIdentifier tableId;

    private WriteResult writeResult;

    public MultipleWriteResult(TableIdentifier tableId, WriteResult writeResult) {
```

### FieldMayBeFinal
Field `multipleSink` may be 'final'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/iceberg/src/main/java/org/apache/inlong/sort/iceberg/sink/multiple/IcebergSingleStreamWriter.java`
#### Snippet
```java
    private final DirtyOptions dirtyOptions;
    private @Nullable final DirtySink<Object> dirtySink;
    private boolean multipleSink;

    public IcebergSingleStreamWriter(
```

### FieldMayBeFinal
Field `flinkActions` may be 'final'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/iceberg/src/main/java/org/apache/inlong/sort/iceberg/sink/multiple/IcebergSingleFileCommiter.java`
#### Snippet
```java

    // compact file action
    private ActionsProvider flinkActions;
    private transient RewriteDataFiles compactAction;
    private ReadableConfig tableOptions;
```

### FieldMayBeFinal
Field `tableOptions` may be 'final'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/iceberg/src/main/java/org/apache/inlong/sort/iceberg/sink/multiple/IcebergSingleFileCommiter.java`
#### Snippet
```java
    private ActionsProvider flinkActions;
    private transient RewriteDataFiles compactAction;
    private ReadableConfig tableOptions;

    public IcebergSingleFileCommiter(
```

### FieldMayBeFinal
Field `keyPrefix` may be 'final'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/iceberg/src/main/java/org/apache/inlong/sort/iceberg/sink/collections/RocksDBDAO.java`
#### Snippet
```java
    private static class ConditionalIteratorWrapper<T, R> extends IteratorWrapper<T, R> {

        private byte[] keyPrefix;

        public ConditionalIteratorWrapper(final RocksIterator iterator, final RocksDBDAO<T, R> dao, byte[] keyPrefix) {
```

### FieldMayBeFinal
Field `tableDdls` may be 'final'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/mysql/source/split/MySqlBinlogSplitState.java`
#### Snippet
```java
    @Nullable
    private BinlogOffset endingOffset;
    private Map<TableId, String> tableDdls;

    public MySqlBinlogSplitState(MySqlBinlogSplit split) {
```

### FieldMayBeFinal
Field `target` may be 'final'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/mysql/source/events/WakeupReaderEvent.java`
#### Snippet
```java

    private static final long serialVersionUID = 1L;
    private WakeUpTarget target;

    public WakeupReaderEvent(WakeUpTarget target) {
```

### FieldMayBeFinal
Field `ghostDdlChange` may be 'final'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/mysql/source/reader/MySqlRecordEmitter.java`
#### Snippet
```java

    private boolean includeIncremental;
    private boolean ghostDdlChange;
    private String ghostTableRegex;

```

### FieldMayBeFinal
Field `ghostTableRegex` may be 'final'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/mysql/source/reader/MySqlRecordEmitter.java`
#### Snippet
```java
    private boolean includeIncremental;
    private boolean ghostDdlChange;
    private String ghostTableRegex;

    public MySqlRecordEmitter(
```

### FieldMayBeFinal
Field `includeIncremental` may be 'final'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/mysql/source/reader/MySqlRecordEmitter.java`
#### Snippet
```java
    private volatile long snapProcessTime = 0L;

    private boolean includeIncremental;
    private boolean ghostDdlChange;
    private String ghostTableRegex;
```

### FieldMayBeFinal
Field `sourceReaderMetrics` may be 'final'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/mysql/source/reader/MySqlSourceReader.java`
#### Snippet
```java
    private final MySqlSourceReaderContext mySqlSourceReaderContext;
    private MySqlBinlogSplit suspendedBinlogSplit;
    private MySqlSourceReaderMetrics sourceReaderMetrics;

    public MySqlSourceReader(
```

### FieldMayBeFinal
Field `sinkOptions` may be 'final'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/starrocks/src/main/java/org/apache/inlong/sort/starrocks/table/sink/StarRocksDynamicTableSink.java`
#### Snippet
```java

    private transient TableSchema flinkSchema;
    private StarRocksSinkOptions sinkOptions;
    private final boolean multipleSink;
    private final String sinkMultipleFormat;
```

### FieldMayBeFinal
Field `flinkSchema` may be 'final'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/starrocks/src/main/java/org/apache/inlong/sort/starrocks/table/sink/StarRocksDynamicTableSink.java`
#### Snippet
```java
public class StarRocksDynamicTableSink implements DynamicTableSink {

    private transient TableSchema flinkSchema;
    private StarRocksSinkOptions sinkOptions;
    private final boolean multipleSink;
```

### FieldMayBeFinal
Field `opAutoProjectionInJson` may be 'final'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/starrocks/src/main/java/org/apache/inlong/sort/starrocks/manager/StarRocksStreamLoadVisitor.java`
#### Snippet
```java
    private final String[] fieldNames;
    private long pos;
    private boolean opAutoProjectionInJson;
    private static final String RESULT_FAILED = "Fail";
    private static final String RESULT_LABEL_EXISTED = "Label Already Exists";
```

### FieldMayBeFinal
Field `rowTransformer` may be 'final'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/starrocks/src/main/java/org/apache/inlong/sort/starrocks/table/sink/StarRocksDynamicSinkFunction.java`
#### Snippet
```java
    private static final String FORMAT_KEY = "format";
    private StarRocksSinkManager sinkManager;
    private StarRocksIRowTransformer<T> rowTransformer;
    private StarRocksSinkOptions sinkOptions;
    private StarRocksISerializer serializer;
```

### FieldMayBeFinal
Field `sinkManager` may be 'final'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/starrocks/src/main/java/org/apache/inlong/sort/starrocks/table/sink/StarRocksDynamicSinkFunction.java`
#### Snippet
```java
    private static final String FORMAT_CSV_VALUE = "csv";
    private static final String FORMAT_KEY = "format";
    private StarRocksSinkManager sinkManager;
    private StarRocksIRowTransformer<T> rowTransformer;
    private StarRocksSinkOptions sinkOptions;
```

### FieldMayBeFinal
Field `inlongAudit` may be 'final'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/debezium/DebeziumSourceFunction.java`
#### Snippet
```java
    private String inlongMetric;

    private String inlongAudit;

    private boolean migrateAll;
```

### FieldMayBeFinal
Field `serializer` may be 'final'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/starrocks/src/main/java/org/apache/inlong/sort/starrocks/table/sink/StarRocksDynamicSinkFunction.java`
#### Snippet
```java
    private StarRocksIRowTransformer<T> rowTransformer;
    private StarRocksSinkOptions sinkOptions;
    private StarRocksISerializer serializer;
    private transient Counter totalInvokeRowsTime;
    private transient Counter totalInvokeRows;
```

### FieldMayBeFinal
Field `inlongMetric` may be 'final'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/debezium/DebeziumSourceFunction.java`
#### Snippet
```java
    private transient Handover handover;

    private String inlongMetric;

    private String inlongAudit;
```

### FieldMayBeFinal
Field `sinkOptions` may be 'final'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/starrocks/src/main/java/org/apache/inlong/sort/starrocks/table/sink/StarRocksDynamicSinkFunction.java`
#### Snippet
```java
    private StarRocksSinkManager sinkManager;
    private StarRocksIRowTransformer<T> rowTransformer;
    private StarRocksSinkOptions sinkOptions;
    private StarRocksISerializer serializer;
    private transient Counter totalInvokeRowsTime;
```

### FieldMayBeFinal
Field `migrateAll` may be 'final'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/debezium/DebeziumSourceFunction.java`
#### Snippet
```java
    private String inlongAudit;

    private boolean migrateAll;

    private SourceTableMetricData sourceMetricData;
```

### FieldMayBeFinal
Field `dirtySinkHelper` may be 'final'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/starrocks/src/main/java/org/apache/inlong/sort/starrocks/table/sink/StarRocksDynamicSinkFunction.java`
#### Snippet
```java
    private transient JsonDynamicSchemaFormat jsonDynamicSchemaFormat;

    private DirtySinkHelper<Object> dirtySinkHelper;

    public StarRocksDynamicSinkFunction(StarRocksSinkOptions sinkOptions,
```

### FieldMayBeFinal
Field `url` may be 'final'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/oracle-cdc/src/main/java/org/apache/inlong/sort/cdc/oracle/source/config/OracleSourceConfig.java`
#### Snippet
```java

    @Nullable
    private String url;

    public OracleSourceConfig(
```

### FieldMayBeFinal
Field `ignoreWriteTables` may be 'final'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/starrocks/src/main/java/org/apache/inlong/sort/starrocks/manager/StarRocksSinkManager.java`
#### Snippet
```java
     * If a table writing throws exception, ignore it when receiving data later again
     */
    private Set<String> ignoreWriteTables = new HashSet<>();

    public void setSinkMetricData(SinkTableMetricData metricData) {
```

### FieldMayBeFinal
Field `updateLookup` may be 'final'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mongodb-cdc/src/main/java/org/apache/inlong/sort/cdc/mongodb/source/config/MongoDBSourceConfigFactory.java`
#### Snippet
```java
    private Integer pollAwaitTimeMillis = POLL_AWAIT_TIME_MILLIS.defaultValue();
    private Integer pollMaxBatchSize = POLL_MAX_BATCH_SIZE.defaultValue();
    private Boolean updateLookup = true;
    private StartupOptions startupOptions = StartupOptions.initial();
    private Integer heartbeatIntervalMillis = HEARTBEAT_INTERVAL_MILLIS.defaultValue();
```

### FieldMayBeFinal
Field `inlongAudit` may be 'final'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/oracle-cdc/src/main/java/org/apache/inlong/sort/cdc/oracle/debezium/DebeziumSourceFunction.java`
#### Snippet
```java
    private String inlongMetric;

    private String inlongAudit;

    private boolean sourceMultipleEnable;
```

### FieldMayBeFinal
Field `inlongMetric` may be 'final'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/oracle-cdc/src/main/java/org/apache/inlong/sort/cdc/oracle/debezium/DebeziumSourceFunction.java`
#### Snippet
```java
    private transient Handover handover;

    private String inlongMetric;

    private String inlongAudit;
```

### FieldMayBeFinal
Field `sourceMultipleEnable` may be 'final'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/oracle-cdc/src/main/java/org/apache/inlong/sort/cdc/oracle/debezium/DebeziumSourceFunction.java`
#### Snippet
```java
    private String inlongAudit;

    private boolean sourceMultipleEnable;

    private SourceTableMetricData sourceMetricData;
```

### FieldMayBeFinal
Field `inlongAudit` may be 'final'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mongodb-cdc/src/main/java/org/apache/inlong/sort/cdc/mongodb/DebeziumSourceFunction.java`
#### Snippet
```java
    private String inlongMetric;

    private String inlongAudit;

    private SourceTableMetricData sourceMetricData;
```

### FieldMayBeFinal
Field `migrateAll` may be 'final'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mongodb-cdc/src/main/java/org/apache/inlong/sort/cdc/mongodb/DebeziumSourceFunction.java`
#### Snippet
```java
    private SourceTableMetricData sourceMetricData;

    private boolean migrateAll;

    private transient ListState<MetricState> metricStateListState;
```

### FieldMayBeFinal
Field `inlongMetric` may be 'final'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mongodb-cdc/src/main/java/org/apache/inlong/sort/cdc/mongodb/DebeziumSourceFunction.java`
#### Snippet
```java
    private transient Handover handover;

    private String inlongMetric;

    private String inlongAudit;
```

### FieldMayBeFinal
Field `serverTimeZone` may be 'final'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/oracle-cdc/src/main/java/org/apache/inlong/sort/cdc/oracle/debezium/table/RowDataDebeziumDeserializeSchema.java`
#### Snippet
```java
    private boolean sourceMultipleEnable;

    private ZoneId serverTimeZone;

    RowDataDebeziumDeserializeSchema(
```

### FieldMayBeFinal
Field `sourceMultipleEnable` may be 'final'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/oracle-cdc/src/main/java/org/apache/inlong/sort/cdc/oracle/debezium/table/RowDataDebeziumDeserializeSchema.java`
#### Snippet
```java
    private final ValueValidator validator;

    private boolean sourceMultipleEnable;

    private ZoneId serverTimeZone;
```

### FieldMayBeFinal
Field `sourceMultipleEnable` may be 'final'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mongodb-cdc/src/main/java/org/apache/inlong/sort/cdc/mongodb/debezium/table/AppendMetadataCollector.java`
#### Snippet
```java
    public transient SourceRecord inputRecord;
    public transient Collector<RowData> outputCollector;
    private boolean sourceMultipleEnable;

    public AppendMetadataCollector(MetadataConverter[] metadataConverters, boolean sourceMultipleEnable) {
```

### FieldMayBeFinal
Field `properties` may be 'final'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/postgres-cdc/src/main/java/org/apache/inlong/sort/cdc/postgres/debezium/internal/DebeziumChangeFetcher.java`
#### Snippet
```java
     * PostgreSQL connector properties
     */
    private Properties properties;

    private PostgreSQLQueryVisitor postgreSQLQueryVisitor;
```

### FieldMayBeFinal
Field `postgreSQLQueryVisitor` may be 'final'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/postgres-cdc/src/main/java/org/apache/inlong/sort/cdc/postgres/debezium/internal/DebeziumChangeFetcher.java`
#### Snippet
```java
    private Properties properties;

    private PostgreSQLQueryVisitor postgreSQLQueryVisitor;

    private Map<String, TableChangeHolder> tableChangeMap = new ConcurrentHashMap<>();
```

### FieldMayBeFinal
Field `tableChangeMap` may be 'final'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/postgres-cdc/src/main/java/org/apache/inlong/sort/cdc/postgres/debezium/internal/DebeziumChangeFetcher.java`
#### Snippet
```java
    private PostgreSQLQueryVisitor postgreSQLQueryVisitor;

    private Map<String, TableChangeHolder> tableChangeMap = new ConcurrentHashMap<>();

    /**
```

### FieldMayBeFinal
Field `auditHostAndPorts` may be 'final'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/sqlserver-cdc/src/main/java/org/apache/inlong/sort/cdc/sqlserver/table/SqlServerTableSource.java`
#### Snippet
```java
    private String inlongMetric;

    private String auditHostAndPorts;

    public SqlServerTableSource(
```

### FieldMayBeFinal
Field `inlongMetric` may be 'final'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/sqlserver-cdc/src/main/java/org/apache/inlong/sort/cdc/sqlserver/table/SqlServerTableSource.java`
#### Snippet
```java
    protected List<String> metadataKeys;

    private String inlongMetric;

    private String auditHostAndPorts;
```

### FieldMayBeFinal
Field `inlongAudit` may be 'final'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/postgres-cdc/src/main/java/org/apache/inlong/sort/cdc/postgres/DebeziumSourceFunction.java`
#### Snippet
```java
    private String inlongMetric;

    private String inlongAudit;

    private SourceTableMetricData sourceMetricData;
```

### FieldMayBeFinal
Field `migrateAll` may be 'final'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/postgres-cdc/src/main/java/org/apache/inlong/sort/cdc/postgres/DebeziumSourceFunction.java`
#### Snippet
```java
    private MetricState metricState;

    private boolean migrateAll;

    // ---------------------------------------------------------------------------------------
```

### FieldMayBeFinal
Field `inlongMetric` may be 'final'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/postgres-cdc/src/main/java/org/apache/inlong/sort/cdc/postgres/DebeziumSourceFunction.java`
#### Snippet
```java
    private transient Handover handover;

    private String inlongMetric;

    private String inlongAudit;
```

### FieldMayBeFinal
Field `auditHostAndPorts` may be 'final'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/sqlserver-cdc/src/main/java/org/apache/inlong/sort/cdc/sqlserver/table/DebeziumSourceFunction.java`
#### Snippet
```java
    private SourceMetricData sourceMetricData;

    private String auditHostAndPorts;

    private transient ListState<MetricState> metricStateListState;
```

### FieldMayBeFinal
Field `inlongMetric` may be 'final'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/sqlserver-cdc/src/main/java/org/apache/inlong/sort/cdc/sqlserver/table/DebeziumSourceFunction.java`
#### Snippet
```java
    private transient Handover handover;

    private String inlongMetric;

    private SourceMetricData sourceMetricData;
```

### FieldMayBeFinal
Field `key` may be 'final'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/transformation/function/JsonGetterFunction.java`
#### Snippet
```java
    private FieldInfo field;
    @JsonProperty("key")
    private StringConstantParam key;

    /**
```

### FieldMayBeFinal
Field `field` may be 'final'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/transformation/function/JsonGetterFunction.java`
#### Snippet
```java

    @JsonProperty("field")
    private FieldInfo field;
    @JsonProperty("key")
    private StringConstantParam key;
```

### FieldMayBeFinal
Field `internalProperties` may be 'final'
in `inlong-sort/sort-formats/format-kv/src/main/java/org/apache/inlong/sort/formats/kv/Kv.java`
#### Snippet
```java
    public static final String FORMAT_TYPE_VALUE = "tdkv";

    private DescriptorProperties internalProperties =
            new DescriptorProperties(true);

```

### FieldMayBeFinal
Field `internalProperties` may be 'final'
in `inlong-sort/sort-formats/format-csv/src/main/java/org/apache/inlong/sort/formats/csv/Csv.java`
#### Snippet
```java
    public static final String FORMAT_TYPE_VALUE = "tdcsv";

    private DescriptorProperties internalProperties =
            new DescriptorProperties(true);

```

### FieldMayBeFinal
Field `encodeDecimalAsPlainNumber` may be 'final'
in `inlong-sort/sort-formats/format-json/src/main/java/org/apache/inlong/sort/formats/json/canal/CanalJsonEnhancedEncodingFormat.java`
#### Snippet
```java
    private final String mapNullKeyLiteral;
    private List<String> metadataKeys;
    private boolean encodeDecimalAsPlainNumber;

    public CanalJsonEnhancedEncodingFormat(
```

### FieldMayBeFinal
Field `internalProperties` may be 'final'
in `inlong-sort/sort-formats/format-inlongmsg-csv/src/main/java/org/apache/inlong/sort/formats/inlongmsgcsv/InLongMsgCsv.java`
#### Snippet
```java
    public static final String FORMAT_TYPE_VALUE = "inlongmsgcsv";

    private DescriptorProperties internalProperties =
            new DescriptorProperties(true);

```

### FieldMayBeFinal
Field `jobTaskPositionMap` may be 'final'
in `inlong-agent/agent-core/src/main/java/org/apache/inlong/agent/core/task/TaskPositionManager.java`
#### Snippet
```java
    private final JobProfileDb jobConfDb;
    private final AgentConfiguration conf;
    private ConcurrentHashMap<String, ConcurrentHashMap<String, Long>> jobTaskPositionMap;

    private TaskPositionManager(AgentManager agentManager) {
```

### FieldMayBeFinal
Field `db` may be 'final'
in `inlong-agent/agent-core/src/main/java/org/apache/inlong/agent/core/job/JobWrapper.java`
#### Snippet
```java
    private boolean isEnd; // Solve the concurrency problem submitted by jobProfile
    private final AtomicInteger index;
    private CommandDb db;

    public JobWrapper(AgentManager manager, Job job) {
```

### FieldMayBeFinal
Field `startIdx` may be 'final'
in `inlong-agent/agent-common/src/main/java/org/apache/inlong/agent/utils/ByteUtil.java`
#### Snippet
```java
    public static class Range {

        private int startIdx;
        private int endIdx;

```

### FieldMayBeFinal
Field `endIdx` may be 'final'
in `inlong-agent/agent-common/src/main/java/org/apache/inlong/agent/utils/ByteUtil.java`
#### Snippet
```java

        private int startIdx;
        private int endIdx;

        /**
```

### FieldMayBeFinal
Field `extraMap` may be 'final'
in `inlong-agent/agent-common/src/main/java/org/apache/inlong/agent/message/PackProxyMessage.java`
#### Snippet
```java
     * extra map used when sending to dataproxy
     */
    private Map<String, String> extraMap = new HashMap<>();
    private volatile long currentCacheTime = System.currentTimeMillis();

```

### FieldMayBeFinal
Field `index` may be 'final'
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/sinks/ProxySink.java`
#### Snippet
```java

    private static final Logger LOGGER = LoggerFactory.getLogger(ProxySink.class);
    private static AtomicLong index = new AtomicLong(0);
    private final ExecutorService executorService = new ThreadPoolExecutor(1, 1,
            0L, TimeUnit.MILLISECONDS,
```

### FieldMayBeFinal
Field `dimensionKeys` may be 'final'
in `inlong-agent/agent-common/src/main/java/org/apache/inlong/agent/metrics/AgentPrometheusMetricListener.java`
#### Snippet
```java
    private Map<String, AtomicLong> metricValueMap = new ConcurrentHashMap<>();
    private Map<String, MetricItemValue> dimensionMetricValueMap = new ConcurrentHashMap<>();
    private List<String> dimensionKeys = new ArrayList<>();

    public AgentPrometheusMetricListener() {
```

### FieldMayBeFinal
Field `dimensionMetricValueMap` may be 'final'
in `inlong-agent/agent-common/src/main/java/org/apache/inlong/agent/metrics/AgentPrometheusMetricListener.java`
#### Snippet
```java
    private AgentMetricItem metricItem;
    private Map<String, AtomicLong> metricValueMap = new ConcurrentHashMap<>();
    private Map<String, MetricItemValue> dimensionMetricValueMap = new ConcurrentHashMap<>();
    private List<String> dimensionKeys = new ArrayList<>();

```

### FieldMayBeFinal
Field `metricItem` may be 'final'
in `inlong-agent/agent-common/src/main/java/org/apache/inlong/agent/metrics/AgentPrometheusMetricListener.java`
#### Snippet
```java
    private static final Logger LOGGER = LoggerFactory.getLogger(AgentPrometheusMetricListener.class);
    protected HTTPServer httpServer;
    private AgentMetricItem metricItem;
    private Map<String, AtomicLong> metricValueMap = new ConcurrentHashMap<>();
    private Map<String, MetricItemValue> dimensionMetricValueMap = new ConcurrentHashMap<>();
```

### FieldMayBeFinal
Field `metricValueMap` may be 'final'
in `inlong-agent/agent-common/src/main/java/org/apache/inlong/agent/metrics/AgentPrometheusMetricListener.java`
#### Snippet
```java
    protected HTTPServer httpServer;
    private AgentMetricItem metricItem;
    private Map<String, AtomicLong> metricValueMap = new ConcurrentHashMap<>();
    private Map<String, MetricItemValue> dimensionMetricValueMap = new ConcurrentHashMap<>();
    private List<String> dimensionKeys = new ArrayList<>();
```

### FieldMayBeFinal
Field `resendQueue` may be 'final'
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/sinks/SenderManager.java`
#### Snippet
```java
    private static final ConcurrentHashMap<String, List<DefaultMessageSender>> SENDER_MAP =
            new ConcurrentHashMap<>();
    private LinkedBlockingQueue<AgentSenderCallback> resendQueue;
    private final ExecutorService resendExecutorService = new ThreadPoolExecutor(1, 1,
            0L, TimeUnit.MILLISECONDS,
```

### FieldMayBeFinal
Field `taskPositionManager` may be 'final'
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/sinks/SenderManager.java`
#### Snippet
```java
    private AgentMetricItemSet metricItemSet;
    private Map<String, String> dimensions;
    private TaskPositionManager taskPositionManager;
    private int ioThreadNum;
    private boolean enableBusyWait;
```

### FieldMayBeFinal
Field `semaphore` may be 'final'
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/sinks/SenderManager.java`
#### Snippet
```java
    private int ioThreadNum;
    private boolean enableBusyWait;
    private Semaphore semaphore;
    private String authSecretId;
    private String authSecretKey;
```

### FieldMayBeFinal
Field `metricItemSet` may be 'final'
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/sinks/SenderManager.java`
#### Snippet
```java

    // metric
    private AgentMetricItemSet metricItemSet;
    private Map<String, String> dimensions;
    private TaskPositionManager taskPositionManager;
```

### FieldMayBeFinal
Field `authSecretKey` may be 'final'
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/sinks/SenderManager.java`
#### Snippet
```java
    private Semaphore semaphore;
    private String authSecretId;
    private String authSecretKey;
    protected int batchFlushInterval;

```

### FieldMayBeFinal
Field `enableBusyWait` may be 'final'
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/sinks/SenderManager.java`
#### Snippet
```java
    private TaskPositionManager taskPositionManager;
    private int ioThreadNum;
    private boolean enableBusyWait;
    private Semaphore semaphore;
    private String authSecretId;
```

### FieldMayBeFinal
Field `dimensions` may be 'final'
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/sinks/SenderManager.java`
#### Snippet
```java
    // metric
    private AgentMetricItemSet metricItemSet;
    private Map<String, String> dimensions;
    private TaskPositionManager taskPositionManager;
    private int ioThreadNum;
```

### FieldMayBeFinal
Field `ioThreadNum` may be 'final'
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/sinks/SenderManager.java`
#### Snippet
```java
    private Map<String, String> dimensions;
    private TaskPositionManager taskPositionManager;
    private int ioThreadNum;
    private boolean enableBusyWait;
    private Semaphore semaphore;
```

### FieldMayBeFinal
Field `authSecretId` may be 'final'
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/sinks/SenderManager.java`
#### Snippet
```java
    private boolean enableBusyWait;
    private Semaphore semaphore;
    private String authSecretId;
    private String authSecretKey;
    protected int batchFlushInterval;
```

### FieldMayBeFinal
Field `originRegex` may be 'final'
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/filter/DateFormatRegex.java`
#### Snippet
```java
    private int minuteOffset = 0;

    private String originRegex;
    private String formattedTime = "";
    private String formattedRegex;
```

### FieldMayBeFinal
Field `snowFlake` may be 'final'
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/message/SequentialID.java`
#### Snippet
```java

    private static SequentialID uniqueSequentialID = null;
    private SnowFlake snowFlake;

    private SequentialID() {
```

### FieldMayBeFinal
Field `metricsIndex` may be 'final'
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/sources/DatabaseSqlSource.java`
#### Snippet
```java
    private static final String DATABASE_SOURCE_TAG_NAME = "AgentDatabaseSourceMetric";

    private static AtomicLong metricsIndex = new AtomicLong(0);

    public DatabaseSqlSource() {
```

### FieldMayBeFinal
Field `startTime` may be 'final'
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/sinks/KafkaSink.java`
#### Snippet
```java
    class AsyncSinkCallback implements Callback {

        private long startTime;
        private BatchProxyMessage batchMsg;

```

### FieldMayBeFinal
Field `batchMsg` may be 'final'
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/sinks/KafkaSink.java`
#### Snippet
```java

        private long startTime;
        private BatchProxyMessage batchMsg;

        public AsyncSinkCallback(long startTime, BatchProxyMessage batchMsg) {
```

### FieldMayBeFinal
Field `metricsIndex` may be 'final'
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/sources/KafkaSource.java`
#### Snippet
```java
    private static final String KAFKA_SESSION_TIMEOUT = "session.timeout.ms";
    private static final Gson gson = new Gson();
    private static AtomicLong metricsIndex = new AtomicLong(0);

    public KafkaSource() {
```

### FieldMayBeFinal
Field `currentTotalReadBytes` may be 'final'
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/sources/reader/KafkaReader.java`
#### Snippet
```java
    private static AtomicLong lastTotalReadRecords = new AtomicLong(0);
    /* total readBytes */
    private static AtomicLong currentTotalReadBytes = new AtomicLong(0);
    private static AtomicLong lastTotalReadBytes = new AtomicLong(0);
    KafkaConsumer<K, V> consumer;
```

### FieldMayBeFinal
Field `currentTotalReadRecords` may be 'final'
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/sources/reader/KafkaReader.java`
#### Snippet
```java
    private static final String JOB_KAFKAJOB_READ_TIMEOUT = "job.kafkaJob.read.timeout";
    /* total readRecords */
    private static AtomicLong currentTotalReadRecords = new AtomicLong(0);
    private static AtomicLong lastTotalReadRecords = new AtomicLong(0);
    /* total readBytes */
```

### FieldMayBeFinal
Field `validators` may be 'final'
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/sources/reader/KafkaReader.java`
#### Snippet
```java
    long flowControlInterval;
    private Iterator<ConsumerRecord<K, V>> iterator;
    private List<Validator> validators = new ArrayList<>();
    private long timeout;
    private long waitTimeout = 1000;
```

### FieldMayBeFinal
Field `topic` may be 'final'
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/sources/reader/KafkaReader.java`
#### Snippet
```java
    private boolean isFinished = false;
    private boolean destroyed = false;
    private String topic;

    /**
```

### FieldMayBeFinal
Field `lastTotalReadBytes` may be 'final'
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/sources/reader/KafkaReader.java`
#### Snippet
```java
    /* total readBytes */
    private static AtomicLong currentTotalReadBytes = new AtomicLong(0);
    private static AtomicLong lastTotalReadBytes = new AtomicLong(0);
    KafkaConsumer<K, V> consumer;
    long lastTimestamp;
```

### FieldMayBeFinal
Field `lastTime` may be 'final'
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/sources/reader/KafkaReader.java`
#### Snippet
```java
    private long timeout;
    private long waitTimeout = 1000;
    private long lastTime = 0;
    private String inlongGroupId;
    private String inlongStreamId;
```

### FieldMayBeFinal
Field `clusterName` may be 'final'
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/fetcher/ManagerFetcher.java`
#### Snippet
```java
    private String localIp;
    private String uuid;
    private String clusterName;

    private CommandDb commandDb;
```

### FieldMayBeFinal
Field `commandDb` may be 'final'
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/fetcher/ManagerFetcher.java`
#### Snippet
```java
    private String clusterName;

    private CommandDb commandDb;

    public ManagerFetcher(AgentManager agentManager) {
```

### FieldMayBeFinal
Field `fileReaderOperator` may be 'final'
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/sources/reader/file/KubernetesMetadataProvider.java`
#### Snippet
```java

    private KubernetesClient client;
    private FileReaderOperator fileReaderOperator;

    KubernetesMetadataProvider(FileReaderOperator fileReaderOperator) {
```

### FieldMayBeFinal
Field `pattern` may be 'final'
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/validator/PatternValidator.java`
#### Snippet
```java
public class PatternValidator implements Validator {

    private String pattern;

    public PatternValidator(String pattern) {
```

### FieldMayBeFinal
Field `prefix` may be 'final'
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/validator/PrefixValidator.java`
#### Snippet
```java
public class PrefixValidator implements Validator {

    private String prefix;

    public PrefixValidator(String prefix) {
```

### FieldMayBeFinal
Field `packToken` may be 'final'
in `inlong-audit/audit-sdk/src/main/java/org/apache/inlong/audit/send/SenderChannel.java`
#### Snippet
```java
    private IpPort ipPort;
    private Channel channel;
    private Semaphore packToken;
    private Bootstrap client;
    private SenderManager senderManager;
```

### FieldMayBeFinal
Field `senderManager` may be 'final'
in `inlong-audit/audit-sdk/src/main/java/org/apache/inlong/audit/send/SenderChannel.java`
#### Snippet
```java
    private Semaphore packToken;
    private Bootstrap client;
    private SenderManager senderManager;

    /**
```

### FieldMayBeFinal
Field `senderManager` may be 'final'
in `inlong-audit/audit-sdk/src/main/java/org/apache/inlong/audit/send/ClientPipelineFactory.java`
#### Snippet
```java
public class ClientPipelineFactory extends ChannelInitializer<SocketChannel> {

    private SenderManager senderManager;

    public ClientPipelineFactory(SenderManager senderManager) {
```

### FieldMayBeFinal
Field `senderManager` may be 'final'
in `inlong-audit/audit-sdk/src/main/java/org/apache/inlong/audit/send/SenderGroup.java`
#### Snippet
```java
    private boolean hasSendError = false;

    private SenderManager senderManager;

    /**
```

### FieldMayBeFinal
Field `totalChannels` may be 'final'
in `inlong-audit/audit-sdk/src/main/java/org/apache/inlong/audit/send/SenderGroup.java`
#### Snippet
```java
    private int mIndex = 0;
    private List<SenderChannel> deleteChannels = new ArrayList<>();
    private ConcurrentHashMap<String, SenderChannel> totalChannels = new ConcurrentHashMap<>();

    private int waitChannelTimes = DEFAULT_WAIT_TIMES;
```

### FieldMayBeFinal
Field `channelGroups` may be 'final'
in `inlong-audit/audit-sdk/src/main/java/org/apache/inlong/audit/send/SenderGroup.java`
#### Snippet
```java
    public static final int RANDOM_MIN = 0;

    private List<LinkedBlockingQueue<SenderChannel>> channelGroups = new ArrayList<>();
    private int mIndex = 0;
    private List<SenderChannel> deleteChannels = new ArrayList<>();
```

### FieldMayBeFinal
Field `deleteChannels` may be 'final'
in `inlong-audit/audit-sdk/src/main/java/org/apache/inlong/audit/send/SenderGroup.java`
#### Snippet
```java
    private List<LinkedBlockingQueue<SenderChannel>> channelGroups = new ArrayList<>();
    private int mIndex = 0;
    private List<SenderChannel> deleteChannels = new ArrayList<>();
    private ConcurrentHashMap<String, SenderChannel> totalChannels = new ConcurrentHashMap<>();

```

### FieldMayBeFinal
Field `waitChannelTimes` may be 'final'
in `inlong-audit/audit-sdk/src/main/java/org/apache/inlong/audit/send/SenderGroup.java`
#### Snippet
```java
    private ConcurrentHashMap<String, SenderChannel> totalChannels = new ConcurrentHashMap<>();

    private int waitChannelTimes = DEFAULT_WAIT_TIMES;
    private int waitChannelIntervalMs = WAIT_INTERVAL;
    private int maxSynchRequest = DEFAULT_SYNCH_REQUESTS;
```

### FieldMayBeFinal
Field `maxSynchRequest` may be 'final'
in `inlong-audit/audit-sdk/src/main/java/org/apache/inlong/audit/send/SenderGroup.java`
#### Snippet
```java
    private int waitChannelTimes = DEFAULT_WAIT_TIMES;
    private int waitChannelIntervalMs = WAIT_INTERVAL;
    private int maxSynchRequest = DEFAULT_SYNCH_REQUESTS;
    private boolean hasSendError = false;

```

### FieldMayBeFinal
Field `waitChannelIntervalMs` may be 'final'
in `inlong-audit/audit-sdk/src/main/java/org/apache/inlong/audit/send/SenderGroup.java`
#### Snippet
```java

    private int waitChannelTimes = DEFAULT_WAIT_TIMES;
    private int waitChannelIntervalMs = WAIT_INTERVAL;
    private int maxSynchRequest = DEFAULT_SYNCH_REQUESTS;
    private boolean hasSendError = false;
```

### FieldMayBeFinal
Field `FILE_PATH` may be 'final'
in `inlong-audit/audit-sdk/src/main/java/org/apache/inlong/audit/util/AuditConfig.java`
#### Snippet
```java

    private static final Logger logger = LoggerFactory.getLogger(AuditConfig.class);
    private static String FILE_PATH = "/data/inlong/audit/";
    private static final int FILE_SIZE = 500 * 1024 * 1024;
    private static final int MAX_CACHE_ROWS = 2000000;
```

### FieldMayBeFinal
Field `disasterFileName` may be 'final'
in `inlong-audit/audit-sdk/src/main/java/org/apache/inlong/audit/util/AuditConfig.java`
#### Snippet
```java
    }

    private String disasterFileName = "disaster.data";
}

```

### FieldMayBeFinal
Field `allTriggerWatches` may be 'final'
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/trigger/DirectoryTrigger.java`
#### Snippet
```java
    private static volatile WatchService watchService;
    private static WatchKeyProviderThread resourceProviderThread = new WatchKeyProviderThread();
    private static ConcurrentHashMap<WatchKey, Set<DirectoryTrigger>> allTriggerWatches =
            new ConcurrentHashMap<>();

```

### FieldMayBeFinal
Field `resourceProviderThread` may be 'final'
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/trigger/DirectoryTrigger.java`
#### Snippet
```java
    private static final Logger LOGGER = LoggerFactory.getLogger(DirectoryTrigger.class);
    private static volatile WatchService watchService;
    private static WatchKeyProviderThread resourceProviderThread = new WatchKeyProviderThread();
    private static ConcurrentHashMap<WatchKey, Set<DirectoryTrigger>> allTriggerWatches =
            new ConcurrentHashMap<>();
```

### FieldMayBeFinal
Field `counter` may be 'final'
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/utils/LogCounter.java`
#### Snippet
```java
public class LogCounter {

    private AtomicInteger counter = new AtomicInteger(0);

    private int start = 10;
```

### FieldMayBeFinal
Field `validators` may be 'final'
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/sources/reader/file/FileReaderOperator.java`
#### Snippet
```java
    private long waitTimeout;
    private long lastTime = 0;
    private List<Validator> validators = new ArrayList<>();
    public boolean firstStored = false;

```

### FieldMayBeFinal
Field `allChannels` may be 'final'
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/source/ServerMessageFactory.java`
#### Snippet
```java
    private AbstractSource source;
    private ChannelProcessor processor;
    private ChannelGroup allChannels;
    private ServiceDecoder serviceDecoder;
    private String messageHandlerName;
```

### FieldMayBeFinal
Field `processor` may be 'final'
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/source/ServerMessageFactory.java`
#### Snippet
```java
    private static final int DEFAULT_READ_IDLE_TIME = 70 * 60 * 1000;
    private AbstractSource source;
    private ChannelProcessor processor;
    private ChannelGroup allChannels;
    private ServiceDecoder serviceDecoder;
```

### FieldMayBeFinal
Field `source` may be 'final'
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/source/ServerMessageFactory.java`
#### Snippet
```java
    private static final Logger LOG = LoggerFactory.getLogger(ServerMessageFactory.class);
    private static final int DEFAULT_READ_IDLE_TIME = 70 * 60 * 1000;
    private AbstractSource source;
    private ChannelProcessor processor;
    private ChannelGroup allChannels;
```

### FieldMayBeFinal
Field `messageHandlerName` may be 'final'
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/source/ServerMessageFactory.java`
#### Snippet
```java
    private ChannelGroup allChannels;
    private ServiceDecoder serviceDecoder;
    private String messageHandlerName;
    private int maxConnections = Integer.MAX_VALUE;
    private int maxMsgLength;
```

### FieldMayBeFinal
Field `serviceDecoder` may be 'final'
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/source/ServerMessageFactory.java`
#### Snippet
```java
    private ChannelProcessor processor;
    private ChannelGroup allChannels;
    private ServiceDecoder serviceDecoder;
    private String messageHandlerName;
    private int maxConnections = Integer.MAX_VALUE;
```

### FieldMayBeFinal
Field `name` may be 'final'
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/source/ServerMessageFactory.java`
#### Snippet
```java
    private int maxConnections = Integer.MAX_VALUE;
    private int maxMsgLength;
    private String name;

    /**
```

### FieldMayBeFinal
Field `maxMsgLength` may be 'final'
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/source/ServerMessageFactory.java`
#### Snippet
```java
    private String messageHandlerName;
    private int maxConnections = Integer.MAX_VALUE;
    private int maxMsgLength;
    private String name;

```

### FieldMayBeFinal
Field `TOPIC` may be 'final'
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/PulsarSink.java`
#### Snippet
```java
     * properties for header info
     */
    private static String TOPIC = "topic";

    /*
```

### FieldMayBeFinal
Field `DISK_IO_RATE_PER_SEC` may be 'final'
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/PulsarSink.java`
#### Snippet
```java
    private static String LOG_EVERY_N_EVENTS = "log_every_n_events";

    private static String DISK_IO_RATE_PER_SEC = "disk_io_rate_per_sec";

    private static final String SINK_THREAD_NUM = "thread_num";
```

### FieldMayBeFinal
Field `currentSuccessSendCnt` may be 'final'
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/PulsarSink.java`
#### Snippet
```java
     * for stat
     */
    private AtomicLong currentSuccessSendCnt = new AtomicLong(0);

    private AtomicLong lastSuccessSendCnt = new AtomicLong(0);
```

### FieldMayBeFinal
Field `LOG_EVERY_N_EVENTS` may be 'final'
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/PulsarSink.java`
#### Snippet
```java
     * properties for stat
     */
    private static String LOG_EVERY_N_EVENTS = "log_every_n_events";

    private static String DISK_IO_RATE_PER_SEC = "disk_io_rate_per_sec";
```

### FieldMayBeFinal
Field `BATCH_SIZE` may be 'final'
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/PulsarSink.java`
#### Snippet
```java
     */
    private static int BAD_EVENT_QUEUE_SIZE = 10000;
    private static int BATCH_SIZE = 10000;
    private static final int DEFAULT_LOG_EVERY_N_EVENTS = 100000;

```

### FieldMayBeFinal
Field `lastSuccessSendCnt` may be 'final'
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/PulsarSink.java`
#### Snippet
```java
    private AtomicLong currentSuccessSendCnt = new AtomicLong(0);

    private AtomicLong lastSuccessSendCnt = new AtomicLong(0);

    private long t1 = System.currentTimeMillis();
```

### FieldMayBeFinal
Field `totalPulsarSuccSendSize` may be 'final'
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/PulsarSink.java`
#### Snippet
```java
    private static AtomicLong totalPulsarSuccSendCnt = new AtomicLong(0);

    private static AtomicLong totalPulsarSuccSendSize = new AtomicLong(0);
    /*
     * for control
```

### FieldMayBeFinal
Field `totalPulsarSuccSendCnt` may be 'final'
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/PulsarSink.java`
#### Snippet
```java
    private long t2 = 0L;

    private static AtomicLong totalPulsarSuccSendCnt = new AtomicLong(0);

    private static AtomicLong totalPulsarSuccSendSize = new AtomicLong(0);
```

### FieldMayBeFinal
Field `EVENT_QUEUE_SIZE` may be 'final'
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/PulsarSink.java`
#### Snippet
```java
    private volatile boolean canTake = false;

    private static int EVENT_QUEUE_SIZE = 1000;

    private int threadNum;
```

### FieldMayBeFinal
Field `BAD_EVENT_QUEUE_SIZE` may be 'final'
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/PulsarSink.java`
#### Snippet
```java
     * default value
     */
    private static int BAD_EVENT_QUEUE_SIZE = 10000;
    private static int BATCH_SIZE = 10000;
    private static final int DEFAULT_LOG_EVERY_N_EVENTS = 100000;
```

### FieldMayBeFinal
Field `scheduledExecutorService` may be 'final'
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/PulsarSink.java`
#### Snippet
```java
    private static final PulsarPerformanceTask pulsarPerformanceTask = new PulsarPerformanceTask();

    private static ScheduledExecutorService scheduledExecutorService = Executors
            .newScheduledThreadPool(1, new HighPriorityThreadFactory("pulsarPerformance-Printer-thread"));

```

### FieldMayBeFinal
Field `DEFAULT_PULSAR_TOKEN_AUTH` may be 'final'
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/pulsar/PulsarClientService.java`
#### Snippet
```java

    private static boolean DEFAULT_PULSAR_ENABLE_TOKEN_AUTH = false;
    private static String DEFAULT_PULSAR_TOKEN_AUTH = "";
    /*
     * for producer
```

### FieldMayBeFinal
Field `MAX_BATCHING_MESSAGES` may be 'final'
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/pulsar/PulsarClientService.java`
#### Snippet
```java
    private static String BLOCK_IF_QUEUE_FULL = "block_if_queue_full";
    private static String MAX_PENDING_MESSAGES = "max_pending_messages";
    private static String MAX_BATCHING_MESSAGES = "max_batching_messages";

    private static int DEFAULT_SEND_TIMEOUT_MILL = 30 * 1000;
```

### FieldMayBeFinal
Field `SEND_TIMEOUT` may be 'final'
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/pulsar/PulsarClientService.java`
#### Snippet
```java
     * properties key pulsar producer
     */
    private static String SEND_TIMEOUT = "send_timeout_ms";
    private static String CLIENT_TIMEOUT = "client_op_timeout_second";
    private static String ENABLE_BATCH = "enable_batch";
```

### FieldMayBeFinal
Field `DEFAULT_MAX_PENDING_MESSAGES` may be 'final'
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/pulsar/PulsarClientService.java`
#### Snippet
```java
    private static boolean DEFAULT_ENABLE_BATCH = true;
    private static boolean DEFAULT_BLOCK_IF_QUEUE_FULL = true;
    private static int DEFAULT_MAX_PENDING_MESSAGES = 10000;
    private static int DEFAULT_MAX_BATCHING_MESSAGES = 1000;

```

### FieldMayBeFinal
Field `MAX_PENDING_MESSAGES` may be 'final'
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/pulsar/PulsarClientService.java`
#### Snippet
```java
    private static String ENABLE_BATCH = "enable_batch";
    private static String BLOCK_IF_QUEUE_FULL = "block_if_queue_full";
    private static String MAX_PENDING_MESSAGES = "max_pending_messages";
    private static String MAX_BATCHING_MESSAGES = "max_batching_messages";

```

### FieldMayBeFinal
Field `DEFAULT_MAX_BATCHING_MESSAGES` may be 'final'
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/pulsar/PulsarClientService.java`
#### Snippet
```java
    private static boolean DEFAULT_BLOCK_IF_QUEUE_FULL = true;
    private static int DEFAULT_MAX_PENDING_MESSAGES = 10000;
    private static int DEFAULT_MAX_BATCHING_MESSAGES = 1000;

    private static boolean DEFAULT_PULSAR_ENABLE_TOKEN_AUTH = false;
```

### FieldMayBeFinal
Field `DEFAULT_CLIENT_TIMEOUT_SECOND` may be 'final'
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/pulsar/PulsarClientService.java`
#### Snippet
```java

    private static int DEFAULT_SEND_TIMEOUT_MILL = 30 * 1000;
    private static int DEFAULT_CLIENT_TIMEOUT_SECOND = 30;
    private static boolean DEFAULT_ENABLE_BATCH = true;
    private static boolean DEFAULT_BLOCK_IF_QUEUE_FULL = true;
```

### FieldMayBeFinal
Field `PULSAR_ENABLE_AUTH_TOKEN` may be 'final'
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/pulsar/PulsarClientService.java`
#### Snippet
```java
    private static String PULSAR_SERVER_URL = "pulsar_server_url";
    private static String PULSAR_ENABLE_AUTH = "enable_token_auth";
    private static String PULSAR_ENABLE_AUTH_TOKEN = "auth_token";
    /*
     * properties key pulsar producer
```

### FieldMayBeFinal
Field `PULSAR_SERVER_URL` may be 'final'
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/pulsar/PulsarClientService.java`
#### Snippet
```java
     * properties key for pulsar client
     */
    private static String PULSAR_SERVER_URL = "pulsar_server_url";
    private static String PULSAR_ENABLE_AUTH = "enable_token_auth";
    private static String PULSAR_ENABLE_AUTH_TOKEN = "auth_token";
```

### FieldMayBeFinal
Field `PULSAR_ENABLE_AUTH` may be 'final'
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/pulsar/PulsarClientService.java`
#### Snippet
```java
     */
    private static String PULSAR_SERVER_URL = "pulsar_server_url";
    private static String PULSAR_ENABLE_AUTH = "enable_token_auth";
    private static String PULSAR_ENABLE_AUTH_TOKEN = "auth_token";
    /*
```

### FieldMayBeFinal
Field `DEFAULT_ENABLE_BATCH` may be 'final'
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/pulsar/PulsarClientService.java`
#### Snippet
```java
    private static int DEFAULT_SEND_TIMEOUT_MILL = 30 * 1000;
    private static int DEFAULT_CLIENT_TIMEOUT_SECOND = 30;
    private static boolean DEFAULT_ENABLE_BATCH = true;
    private static boolean DEFAULT_BLOCK_IF_QUEUE_FULL = true;
    private static int DEFAULT_MAX_PENDING_MESSAGES = 10000;
```

### FieldMayBeFinal
Field `DEFAULT_BLOCK_IF_QUEUE_FULL` may be 'final'
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/pulsar/PulsarClientService.java`
#### Snippet
```java
    private static int DEFAULT_CLIENT_TIMEOUT_SECOND = 30;
    private static boolean DEFAULT_ENABLE_BATCH = true;
    private static boolean DEFAULT_BLOCK_IF_QUEUE_FULL = true;
    private static int DEFAULT_MAX_PENDING_MESSAGES = 10000;
    private static int DEFAULT_MAX_BATCHING_MESSAGES = 1000;
```

### FieldMayBeFinal
Field `DEFAULT_SEND_TIMEOUT_MILL` may be 'final'
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/pulsar/PulsarClientService.java`
#### Snippet
```java
    private static String MAX_BATCHING_MESSAGES = "max_batching_messages";

    private static int DEFAULT_SEND_TIMEOUT_MILL = 30 * 1000;
    private static int DEFAULT_CLIENT_TIMEOUT_SECOND = 30;
    private static boolean DEFAULT_ENABLE_BATCH = true;
```

### FieldMayBeFinal
Field `sendTimeout` may be 'final'
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/pulsar/PulsarClientService.java`
#### Snippet
```java
     * for producer
     */
    private Integer sendTimeout; // in millsec
    private Integer clientOpTimeout;
    private boolean enableBatch = true;
```

### FieldMayBeFinal
Field `ENABLE_BATCH` may be 'final'
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/pulsar/PulsarClientService.java`
#### Snippet
```java
    private static String SEND_TIMEOUT = "send_timeout_ms";
    private static String CLIENT_TIMEOUT = "client_op_timeout_second";
    private static String ENABLE_BATCH = "enable_batch";
    private static String BLOCK_IF_QUEUE_FULL = "block_if_queue_full";
    private static String MAX_PENDING_MESSAGES = "max_pending_messages";
```

### FieldMayBeFinal
Field `DEFAULT_PULSAR_ENABLE_TOKEN_AUTH` may be 'final'
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/pulsar/PulsarClientService.java`
#### Snippet
```java
    private static int DEFAULT_MAX_BATCHING_MESSAGES = 1000;

    private static boolean DEFAULT_PULSAR_ENABLE_TOKEN_AUTH = false;
    private static String DEFAULT_PULSAR_TOKEN_AUTH = "";
    /*
```

### FieldMayBeFinal
Field `BLOCK_IF_QUEUE_FULL` may be 'final'
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/pulsar/PulsarClientService.java`
#### Snippet
```java
    private static String CLIENT_TIMEOUT = "client_op_timeout_second";
    private static String ENABLE_BATCH = "enable_batch";
    private static String BLOCK_IF_QUEUE_FULL = "block_if_queue_full";
    private static String MAX_PENDING_MESSAGES = "max_pending_messages";
    private static String MAX_BATCHING_MESSAGES = "max_batching_messages";
```

### FieldMayBeFinal
Field `CLIENT_TIMEOUT` may be 'final'
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/pulsar/PulsarClientService.java`
#### Snippet
```java
     */
    private static String SEND_TIMEOUT = "send_timeout_ms";
    private static String CLIENT_TIMEOUT = "client_op_timeout_second";
    private static String ENABLE_BATCH = "enable_batch";
    private static String BLOCK_IF_QUEUE_FULL = "block_if_queue_full";
```

### FieldMayBeFinal
Field `clientOpTimeout` may be 'final'
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/pulsar/PulsarClientService.java`
#### Snippet
```java
     */
    private Integer sendTimeout; // in millsec
    private Integer clientOpTimeout;
    private boolean enableBatch = true;
    private boolean blockIfQueueFull = true;
```

### FieldMayBeFinal
Field `RECEIVE_BUFFER_MAX_SIZE` may be 'final'
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/source/SimpleTcpSource.java`
#### Snippet
```java
    private static int SEND_BUFFER_DEFAULT_SIZE = 64 * 1024;

    private static int RECEIVE_BUFFER_MAX_SIZE = 16 * 1024 * 1024;

    private static int SEND_BUFFER_MAX_SIZE = 16 * 1024 * 1024;
```

### FieldMayBeFinal
Field `RECEIVE_BUFFER_DEFAULT_SIZE` may be 'final'
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/source/SimpleTcpSource.java`
#### Snippet
```java
    private static int HIGH_WATER_MARK_DEFAULT_VALUE = 64 * 1024;

    private static int RECEIVE_BUFFER_DEFAULT_SIZE = 64 * 1024;

    private static int SEND_BUFFER_DEFAULT_SIZE = 64 * 1024;
```

### FieldMayBeFinal
Field `HIGH_WATER_MARK_DEFAULT_VALUE` may be 'final'
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/source/SimpleTcpSource.java`
#### Snippet
```java
    private static int BUFFER_SIZE_MUST_THAN = 0;

    private static int HIGH_WATER_MARK_DEFAULT_VALUE = 64 * 1024;

    private static int RECEIVE_BUFFER_DEFAULT_SIZE = 64 * 1024;
```

### FieldMayBeFinal
Field `DEFAULT_MAX_CONNECTIONS` may be 'final'
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/source/SimpleTcpSource.java`
#### Snippet
```java
    private static int DEFAULT_MAX_THREADS = 32;

    private static int DEFAULT_MAX_CONNECTIONS = 5000;

    private static int MIN_MSG_LENGTH = 4;
```

### FieldMayBeFinal
Field `MAX_MSG_LENGTH` may be 'final'
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/source/SimpleTcpSource.java`
#### Snippet
```java
    private static int MIN_MSG_LENGTH = 4;

    private static int MAX_MSG_LENGTH = 1024 * 64;

    private static int BUFFER_SIZE_MUST_THAN = 0;
```

### FieldMayBeFinal
Field `MIN_MSG_LENGTH` may be 'final'
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/source/SimpleTcpSource.java`
#### Snippet
```java
    private static int DEFAULT_MAX_CONNECTIONS = 5000;

    private static int MIN_MSG_LENGTH = 4;

    private static int MAX_MSG_LENGTH = 1024 * 64;
```

### FieldMayBeFinal
Field `nettyChannel` may be 'final'
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/source/SimpleTcpSource.java`
#### Snippet
```java
    protected int acceptorThreads = 1;

    private Channel nettyChannel = null;

    public SimpleTcpSource() {
```

### FieldMayBeFinal
Field `SEND_BUFFER_DEFAULT_SIZE` may be 'final'
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/source/SimpleTcpSource.java`
#### Snippet
```java
    private static int RECEIVE_BUFFER_DEFAULT_SIZE = 64 * 1024;

    private static int SEND_BUFFER_DEFAULT_SIZE = 64 * 1024;

    private static int RECEIVE_BUFFER_MAX_SIZE = 16 * 1024 * 1024;
```

### FieldMayBeFinal
Field `DEFAULT_MAX_THREADS` may be 'final'
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/source/SimpleTcpSource.java`
#### Snippet
```java
    private static String HOST_DEFAULT_VALUE = "0.0.0.0";

    private static int DEFAULT_MAX_THREADS = 32;

    private static int DEFAULT_MAX_CONNECTIONS = 5000;
```

### FieldMayBeFinal
Field `HOST_DEFAULT_VALUE` may be 'final'
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/source/SimpleTcpSource.java`
#### Snippet
```java
    protected boolean customProcessor = false;

    private static String HOST_DEFAULT_VALUE = "0.0.0.0";

    private static int DEFAULT_MAX_THREADS = 32;
```

### FieldMayBeFinal
Field `SEND_BUFFER_MAX_SIZE` may be 'final'
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/source/SimpleTcpSource.java`
#### Snippet
```java
    private static int RECEIVE_BUFFER_MAX_SIZE = 16 * 1024 * 1024;

    private static int SEND_BUFFER_MAX_SIZE = 16 * 1024 * 1024;

    protected boolean enableBusyWait = false;
```

### FieldMayBeFinal
Field `BUFFER_SIZE_MUST_THAN` may be 'final'
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/source/SimpleTcpSource.java`
#### Snippet
```java
    private static int MAX_MSG_LENGTH = 1024 * 64;

    private static int BUFFER_SIZE_MUST_THAN = 0;

    private static int HIGH_WATER_MARK_DEFAULT_VALUE = 64 * 1024;
```

### FieldMayBeFinal
Field `totalKafkaSuccSendCnt` may be 'final'
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/KafkaSink.java`
#### Snippet
```java
    private long t1 = System.currentTimeMillis();
    private long t2 = 0L;
    private static AtomicLong totalKafkaSuccSendCnt = new AtomicLong(0);
    private static AtomicLong totalKafkaSuccSendSize = new AtomicLong(0);

```

### FieldMayBeFinal
Field `lastSuccessSendCnt` may be 'final'
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/KafkaSink.java`
#### Snippet
```java
    // for stas
    private AtomicLong currentSuccessSendCnt = new AtomicLong(0);
    private AtomicLong lastSuccessSendCnt = new AtomicLong(0);
    private long t1 = System.currentTimeMillis();
    private long t2 = 0L;
```

### FieldMayBeFinal
Field `totalKafkaSuccSendSize` may be 'final'
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/KafkaSink.java`
#### Snippet
```java
    private long t2 = 0L;
    private static AtomicLong totalKafkaSuccSendCnt = new AtomicLong(0);
    private static AtomicLong totalKafkaSuccSendSize = new AtomicLong(0);

    private boolean overflow = false;
```

### FieldMayBeFinal
Field `currentSuccessSendCnt` may be 'final'
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/KafkaSink.java`
#### Snippet
```java

    // for stas
    private AtomicLong currentSuccessSendCnt = new AtomicLong(0);
    private AtomicLong lastSuccessSendCnt = new AtomicLong(0);
    private long t1 = System.currentTimeMillis();
```

### FieldMayBeFinal
Field `scheduledExecutorService` may be 'final'
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/KafkaSink.java`
#### Snippet
```java
    private static final Long PRINT_INTERVAL = 30L;
    private static final KafkaPerformanceTask kafkaPerformanceTask = new KafkaPerformanceTask();
    private static ScheduledExecutorService scheduledExecutorService = Executors.newScheduledThreadPool(1,
            new HighPriorityThreadFactory("kafkaPerformance-Printer-thread"));

```

### FieldMayBeFinal
Field `lastSuccessSendCnt` may be 'final'
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/TubeSink.java`
#### Snippet
```java
     */
    private AtomicLong currentSuccessSendCnt = new AtomicLong(0);
    private AtomicLong lastSuccessSendCnt = new AtomicLong(0);
    private long t1 = System.currentTimeMillis();
    private long t2 = 0L;
```

### FieldMayBeFinal
Field `illegalTopicMap` may be 'final'
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/TubeSink.java`
#### Snippet
```java
    private static AtomicLong totalTubeSuccSendSize = new AtomicLong(0);

    private static ConcurrentHashMap<String, Long> illegalTopicMap =
            new ConcurrentHashMap<String, Long>();

```

### FieldMayBeFinal
Field `scheduledExecutorService` may be 'final'
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/TubeSink.java`
#### Snippet
```java
            new ConcurrentHashMap<String, Long>();

    private static ScheduledExecutorService scheduledExecutorService = Executors.newScheduledThreadPool(1,
            new HighPriorityThreadFactory("tubePerformance-Printer-thread"));

```

### FieldMayBeFinal
Field `currentSuccessSendCnt` may be 'final'
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/TubeSink.java`
#### Snippet
```java
     * for stat
     */
    private AtomicLong currentSuccessSendCnt = new AtomicLong(0);
    private AtomicLong lastSuccessSendCnt = new AtomicLong(0);
    private long t1 = System.currentTimeMillis();
```

### FieldMayBeFinal
Field `totalTubeSuccSendCnt` may be 'final'
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/TubeSink.java`
#### Snippet
```java
    private static int retryCnt = defaultRetryCnt;

    private static AtomicLong totalTubeSuccSendCnt = new AtomicLong(0);

    private static AtomicLong totalTubeSuccSendSize = new AtomicLong(0);
```

### FieldMayBeFinal
Field `sendTime` may be 'final'
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/TubeSink.java`
#### Snippet
```java

        private org.apache.inlong.audit.sink.EventStat myEventStat;
        private long sendTime;

        public MyCallback(org.apache.inlong.audit.sink.EventStat eventStat) {
```

### FieldMayBeFinal
Field `myEventStat` may be 'final'
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/TubeSink.java`
#### Snippet
```java
    public class MyCallback implements MessageSentCallback {

        private org.apache.inlong.audit.sink.EventStat myEventStat;
        private long sendTime;

```

### FieldMayBeFinal
Field `totalTubeSuccSendSize` may be 'final'
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/TubeSink.java`
#### Snippet
```java
    private static AtomicLong totalTubeSuccSendCnt = new AtomicLong(0);

    private static AtomicLong totalTubeSuccSendSize = new AtomicLong(0);

    private static ConcurrentHashMap<String, Long> illegalTopicMap =
```

### FieldMayBeFinal
Field `needToCheckChanged` may be 'final'
in `inlong-audit/audit-common/src/main/java/org/apache/inlong/audit/file/ConfigHolder.java`
#### Snippet
```java
    private String filePath;
    private File configFile;
    private boolean needToCheckChanged;

    public ConfigHolder(String fileName, boolean needToCheckChanged) {
```

### FieldMayBeFinal
Field `batchCounter` may be 'final'
in `inlong-audit/audit-store/src/main/java/org/apache/inlong/audit/service/ClickHouseService.java`
#### Snippet
```java
    private LinkedBlockingQueue<ClickHouseDataPo> batchQueue;
    private AtomicBoolean needBatchOutput = new AtomicBoolean(false);
    private AtomicInteger batchCounter = new AtomicInteger(0);
    private AtomicLong lastCheckTime = new AtomicLong(System.currentTimeMillis());
    private Connection conn;
```

### FieldMayBeFinal
Field `chConfig` may be 'final'
in `inlong-audit/audit-store/src/main/java/org/apache/inlong/audit/service/ClickHouseService.java`
#### Snippet
```java
            + "    values (?,?,?,?,?,?,?,?,?,?,?,?,?)";

    private ClickHouseConfig chConfig;

    private ScheduledExecutorService timerService = Executors.newSingleThreadScheduledExecutor();
```

### FieldMayBeFinal
Field `needBatchOutput` may be 'final'
in `inlong-audit/audit-store/src/main/java/org/apache/inlong/audit/service/ClickHouseService.java`
#### Snippet
```java
    private ScheduledExecutorService timerService = Executors.newSingleThreadScheduledExecutor();
    private LinkedBlockingQueue<ClickHouseDataPo> batchQueue;
    private AtomicBoolean needBatchOutput = new AtomicBoolean(false);
    private AtomicInteger batchCounter = new AtomicInteger(0);
    private AtomicLong lastCheckTime = new AtomicLong(System.currentTimeMillis());
```

### FieldMayBeFinal
Field `timerService` may be 'final'
in `inlong-audit/audit-store/src/main/java/org/apache/inlong/audit/service/ClickHouseService.java`
#### Snippet
```java
    private ClickHouseConfig chConfig;

    private ScheduledExecutorService timerService = Executors.newSingleThreadScheduledExecutor();
    private LinkedBlockingQueue<ClickHouseDataPo> batchQueue;
    private AtomicBoolean needBatchOutput = new AtomicBoolean(false);
```

### FieldMayBeFinal
Field `lastCheckTime` may be 'final'
in `inlong-audit/audit-store/src/main/java/org/apache/inlong/audit/service/ClickHouseService.java`
#### Snippet
```java
    private AtomicBoolean needBatchOutput = new AtomicBoolean(false);
    private AtomicInteger batchCounter = new AtomicInteger(0);
    private AtomicLong lastCheckTime = new AtomicLong(System.currentTimeMillis());
    private Connection conn;

```

### FieldMayBeFinal
Field `topic` may be 'final'
in `inlong-audit/audit-store/src/main/java/org/apache/inlong/audit/service/consume/TubeConsume.java`
#### Snippet
```java

        private final PullMessageConsumer pullMessageConsumer;
        private String topic;

        public Fetcher(PullMessageConsumer pullMessageConsumer, String topic) {
```

### FieldMayBeFinal
Field `DEFAULT_CONFIG_PROPERTIES` may be 'final'
in `inlong-audit/audit-common/src/main/java/org/apache/inlong/audit/file/ConfigManager.java`
#### Snippet
```java
    private static ConfigManager instance = null;

    private static String DEFAULT_CONFIG_PROPERTIES = "application.properties";

    static {
```

### FieldMayBeFinal
Field `buffer` may be 'final'
in `inlong-common/src/main/java/org/apache/inlong/common/msg/DataInputBuffer.java`
#### Snippet
```java
    }

    private Buffer buffer;

    /**
```

### FieldMayBeFinal
Field `buffer` may be 'final'
in `inlong-common/src/main/java/org/apache/inlong/common/msg/DataOutputBuffer.java`
#### Snippet
```java
    }

    private Buffer buffer;

    /**
```

### FieldMayBeFinal
Field `timerService` may be 'final'
in `inlong-audit/audit-store/src/main/java/org/apache/inlong/audit/service/ElasticsearchService.java`
#### Snippet
```java
    private static final Logger LOG = LoggerFactory.getLogger(ElasticsearchService.class);

    private static ScheduledExecutorService timerService = Executors.newScheduledThreadPool(1);
    private final Semaphore semaphore = new Semaphore(1);
    private List<ESDataPo> datalist = new ArrayList<>();
```

### FieldMayBeFinal
Field `datalist` may be 'final'
in `inlong-audit/audit-store/src/main/java/org/apache/inlong/audit/service/ElasticsearchService.java`
#### Snippet
```java
    private static ScheduledExecutorService timerService = Executors.newScheduledThreadPool(1);
    private final Semaphore semaphore = new Semaphore(1);
    private List<ESDataPo> datalist = new ArrayList<>();
    @Autowired
    @Qualifier("restClient")
```

### FieldMayBeFinal
Field `machineId` may be 'final'
in `inlong-common/src/main/java/org/apache/inlong/common/util/SnowFlake.java`
#### Snippet
```java
    private static final long TIMESTMP_LEFT = SEQUENCE_BIT + MACHINE_BIT;

    private long machineId;
    private long sequence = 0L;
    private long lastStmp = -1L;
```

### FieldMayBeFinal
Field `listenerList` may be 'final'
in `inlong-common/src/main/java/org/apache/inlong/common/metric/MetricListenerRunnable.java`
#### Snippet
```java

    private String domain;
    private List<MetricListener> listenerList;

    /**
```

### FieldMayBeFinal
Field `domain` may be 'final'
in `inlong-common/src/main/java/org/apache/inlong/common/metric/MetricListenerRunnable.java`
#### Snippet
```java
    public static final Logger LOG = LoggerFactory.getLogger(MetricListenerRunnable.class);

    private String domain;
    private List<MetricListener> listenerList;

```

### FieldMayBeFinal
Field `intervalSec` may be 'final'
in `inlong-common/src/main/java/org/apache/inlong/common/monitor/MonitorIndexExt.java`
#### Snippet
```java
    private ConcurrentHashMap<String, AtomicLong> counterMap =
            new ConcurrentHashMap<String, AtomicLong>();
    private int intervalSec;
    private int maxCnt;

```

### FieldMayBeFinal
Field `counterMap` may be 'final'
in `inlong-common/src/main/java/org/apache/inlong/common/monitor/MonitorIndexExt.java`
#### Snippet
```java
    private IndexCollectThread indexCol;
    private String name;
    private ConcurrentHashMap<String, AtomicLong> counterMap =
            new ConcurrentHashMap<String, AtomicLong>();
    private int intervalSec;
```

### FieldMayBeFinal
Field `name` may be 'final'
in `inlong-common/src/main/java/org/apache/inlong/common/monitor/MonitorIndexExt.java`
#### Snippet
```java

    private IndexCollectThread indexCol;
    private String name;
    private ConcurrentHashMap<String, AtomicLong> counterMap =
            new ConcurrentHashMap<String, AtomicLong>();
```

### FieldMayBeFinal
Field `indexCol` may be 'final'
in `inlong-common/src/main/java/org/apache/inlong/common/monitor/MonitorIndexExt.java`
#### Snippet
```java
    private static final LogCounter logPrinter = new LogCounter(10, 100000, 60 * 1000);

    private IndexCollectThread indexCol;
    private String name;
    private ConcurrentHashMap<String, AtomicLong> counterMap =
```

### FieldMayBeFinal
Field `counter` may be 'final'
in `inlong-common/src/main/java/org/apache/inlong/common/monitor/LogCounter.java`
#### Snippet
```java
public class LogCounter {

    private AtomicInteger counter = new AtomicInteger(0);

    private int start = 10;
```

### FieldMayBeFinal
Field `statExecutor` may be 'final'
in `inlong-common/src/main/java/org/apache/inlong/common/metric/MetricObserver.java`
#### Snippet
```java
    public static final Logger LOG = LoggerFactory.getLogger(MetricObserver.class);
    private static final AtomicBoolean isInited = new AtomicBoolean(false);
    private static ScheduledExecutorService statExecutor = Executors.newScheduledThreadPool(5);

    /**
```

### FieldMayBeFinal
Field `name` may be 'final'
in `inlong-common/src/main/java/org/apache/inlong/common/monitor/StatRunner.java`
#### Snippet
```java
    private static final Logger logger = LoggerFactory.getLogger(StatRunner.class);
    private boolean shutDownFlag = false;
    private String name;
    private CounterGroup counterGroup;
    private CounterGroupExt counterGroupExt;
```

### FieldMayBeFinal
Field `moniterNames` may be 'final'
in `inlong-common/src/main/java/org/apache/inlong/common/monitor/StatRunner.java`
#### Snippet
```java
    private CounterGroupExt counterGroupExt;
    private int intervalSec;
    private Set<String> moniterNames;

    public StatRunner(String name, CounterGroup counterGroup, CounterGroupExt counterExt,
```

### FieldMayBeFinal
Field `intervalSec` may be 'final'
in `inlong-common/src/main/java/org/apache/inlong/common/monitor/StatRunner.java`
#### Snippet
```java
    private CounterGroup counterGroup;
    private CounterGroupExt counterGroupExt;
    private int intervalSec;
    private Set<String> moniterNames;

```

### FieldMayBeFinal
Field `counterGroup` may be 'final'
in `inlong-common/src/main/java/org/apache/inlong/common/monitor/StatRunner.java`
#### Snippet
```java
    private boolean shutDownFlag = false;
    private String name;
    private CounterGroup counterGroup;
    private CounterGroupExt counterGroupExt;
    private int intervalSec;
```

### FieldMayBeFinal
Field `counterGroupExt` may be 'final'
in `inlong-common/src/main/java/org/apache/inlong/common/monitor/StatRunner.java`
#### Snippet
```java
    private String name;
    private CounterGroup counterGroup;
    private CounterGroupExt counterGroupExt;
    private int intervalSec;
    private Set<String> moniterNames;
```

### FieldMayBeFinal
Field `intervalSec` may be 'final'
in `inlong-common/src/main/java/org/apache/inlong/common/monitor/MonitorIndex.java`
#### Snippet
```java
    private String name;
    private ConcurrentHashMap<String, String> counterMap = new ConcurrentHashMap<String, String>();
    private int intervalSec;
    private int maxCnt;

```

### FieldMayBeFinal
Field `indexCol` may be 'final'
in `inlong-common/src/main/java/org/apache/inlong/common/monitor/MonitorIndex.java`
#### Snippet
```java
    private static final AtomicLong recId = new AtomicLong(0);

    private IndexCollectThread indexCol;
    private String name;
    private ConcurrentHashMap<String, String> counterMap = new ConcurrentHashMap<String, String>();
```

### FieldMayBeFinal
Field `name` may be 'final'
in `inlong-common/src/main/java/org/apache/inlong/common/monitor/MonitorIndex.java`
#### Snippet
```java

    private IndexCollectThread indexCol;
    private String name;
    private ConcurrentHashMap<String, String> counterMap = new ConcurrentHashMap<String, String>();
    private int intervalSec;
```

### FieldMayBeFinal
Field `counterMap` may be 'final'
in `inlong-common/src/main/java/org/apache/inlong/common/monitor/MonitorIndex.java`
#### Snippet
```java
    private IndexCollectThread indexCol;
    private String name;
    private ConcurrentHashMap<String, String> counterMap = new ConcurrentHashMap<String, String>();
    private int intervalSec;
    private int maxCnt;
```

### FieldMayBeFinal
Field `targetAddress` may be 'final'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/RpcServiceInvoker.java`
#### Snippet
```java
public class RpcServiceInvoker extends AbstractServiceInvoker {

    private NodeAddrInfo targetAddress;

    public RpcServiceInvoker(ClientFactory clientFactory, Class serviceClass,
```

### FieldMayBeFinal
Field `chainedCallback` may be 'final'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/AbstractServiceInvoker.java`
#### Snippet
```java
    private static class RpcResponseCallback implements Callback {

        private Callback chainedCallback;

        public RpcResponseCallback(Callback chainedCallback) {
```

### FieldMayBeFinal
Field `masterInfo` may be 'final'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/RpcServiceFailoverInvoker.java`
#### Snippet
```java

    private final AtomicInteger retryCounter = new AtomicInteger(0);
    private MasterInfo masterInfo;
    private Client currentClient;
    private int masterNodeCnt;
```

### FieldMayBeFinal
Field `masterNodeCnt` may be 'final'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/RpcServiceFailoverInvoker.java`
#### Snippet
```java
    private MasterInfo masterInfo;
    private Client currentClient;
    private int masterNodeCnt;

    public RpcServiceFailoverInvoker(ClientFactory clientFactory, Class serviceClass,
```

### FieldMayBeFinal
Field `ctx` may be 'final'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/netty/NettyRequestContext.java`
#### Snippet
```java

    private RequestWrapper request;
    private ChannelHandlerContext ctx;
    private long receiveTime;

```

### FieldMayBeFinal
Field `request` may be 'final'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/netty/NettyRequestContext.java`
#### Snippet
```java
            LoggerFactory.getLogger(NettyRequestContext.class);

    private RequestWrapper request;
    private ChannelHandlerContext ctx;
    private long receiveTime;
```

### FieldMayBeFinal
Field `receiveTime` may be 'final'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/netty/NettyRequestContext.java`
#### Snippet
```java
    private RequestWrapper request;
    private ChannelHandlerContext ctx;
    private long receiveTime;

    public NettyRequestContext(RequestWrapper request,
```

### FieldMayBeFinal
Field `lastSizeTime` may be 'final'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/netty/NettyProtocolDecoder.java`
#### Snippet
```java
            new ConcurrentHashMap<>();
    private static AtomicLong lastProtolTime = new AtomicLong(0);
    private static AtomicLong lastSizeTime = new AtomicLong(0);
    private boolean packHeaderRead = false;
    private int listSize;
```

### FieldMayBeFinal
Field `lastProtolTime` may be 'final'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/netty/NettyProtocolDecoder.java`
#### Snippet
```java
    private static final ConcurrentHashMap<String, AtomicLong> errSizeAddrMap =
            new ConcurrentHashMap<>();
    private static AtomicLong lastProtolTime = new AtomicLong(0);
    private static AtomicLong lastSizeTime = new AtomicLong(0);
    private boolean packHeaderRead = false;
```

### FieldMayBeFinal
Field `rpcDataPackList` may be 'final'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/netty/NettyProtocolDecoder.java`
#### Snippet
```java
    private boolean packHeaderRead = false;
    private int listSize;
    private List<RpcDataPack> rpcDataPackList = new ArrayList<>();
    private RpcDataPack dataPack;
    private ByteBuf lastByteBuf;
```

### FieldMayBeFinal
Field `workerIdCounter` may be 'final'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/netty/NettyClientFactory.java`
#### Snippet
```java
    protected AtomicBoolean shutdown = new AtomicBoolean(true);
    private EventLoopGroup eventLoopGroup;
    private AtomicInteger workerIdCounter = new AtomicInteger(0);
    // TSL encryption and need Two Way Authentic
    private boolean enableTLS = false;
```

### FieldMayBeFinal
Field `lastParseTime` may be 'final'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/netty/NettyRpcServer.java`
#### Snippet
```java
    private static final ConcurrentHashMap<String, AtomicLong> errParseAddrMap =
            new ConcurrentHashMap<>();
    private static AtomicLong lastParseTime = new AtomicLong(0);
    private final ConcurrentHashMap<Integer, Protocol> protocols =
            new ConcurrentHashMap<>();
```

### FieldMayBeFinal
Field `workerGroup` may be 'final'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/netty/NettyRpcServer.java`
#### Snippet
```java
    private ServerBootstrap bootstrap;
    private EventLoopGroup acceptorGroup;
    private EventLoopGroup workerGroup;
    private boolean enableBusyWait;
    private AtomicBoolean started = new AtomicBoolean(false);
```

### FieldMayBeFinal
Field `acceptorGroup` may be 'final'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/netty/NettyRpcServer.java`
#### Snippet
```java
            new ConcurrentHashMap<>();
    private ServerBootstrap bootstrap;
    private EventLoopGroup acceptorGroup;
    private EventLoopGroup workerGroup;
    private boolean enableBusyWait;
```

### FieldMayBeFinal
Field `enableBusyWait` may be 'final'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/netty/NettyRpcServer.java`
#### Snippet
```java
    private EventLoopGroup acceptorGroup;
    private EventLoopGroup workerGroup;
    private boolean enableBusyWait;
    private AtomicBoolean started = new AtomicBoolean(false);
    private int protocolType = RpcProtocol.RPC_PROTOCOL_TCP;
```

### FieldMayBeFinal
Field `isOverTLS` may be 'final'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/netty/NettyRpcServer.java`
#### Snippet
```java
    private AtomicBoolean started = new AtomicBoolean(false);
    private int protocolType = RpcProtocol.RPC_PROTOCOL_TCP;
    private boolean isOverTLS;
    private String keyStorePath = "";
    private String keyStorePassword = "";
```

### FieldMayBeFinal
Field `bootstrap` may be 'final'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/netty/NettyRpcServer.java`
#### Snippet
```java
    private final ConcurrentHashMap<Integer, Protocol> protocols =
            new ConcurrentHashMap<>();
    private ServerBootstrap bootstrap;
    private EventLoopGroup acceptorGroup;
    private EventLoopGroup workerGroup;
```

### FieldMayBeFinal
Field `started` may be 'final'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/netty/NettyRpcServer.java`
#### Snippet
```java
    private EventLoopGroup workerGroup;
    private boolean enableBusyWait;
    private AtomicBoolean started = new AtomicBoolean(false);
    private int protocolType = RpcProtocol.RPC_PROTOCOL_TCP;
    private boolean isOverTLS;
```

### FieldMayBeFinal
Field `invoker` may be 'final'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/RpcServiceFactory.java`
#### Snippet
```java

        private T service;
        private AbstractServiceInvoker invoker;

        ServiceHolder(T service, AbstractServiceInvoker invoker) {
```

### FieldMayBeFinal
Field `config` may be 'final'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/RpcServiceFactory.java`
#### Snippet
```java
        private Class clazzType;
        private NodeAddrInfo addressInfo;
        private RpcConfig config;

        public ConnectionNode(Class clazzType,
```

### FieldMayBeFinal
Field `clazzType` may be 'final'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/RpcServiceFactory.java`
#### Snippet
```java
    private static class ConnectionNode {

        private Class clazzType;
        private NodeAddrInfo addressInfo;
        private RpcConfig config;
```

### FieldMayBeFinal
Field `service` may be 'final'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/RpcServiceFactory.java`
#### Snippet
```java
    private static class ServiceHolder<T> implements Shutdownable {

        private T service;
        private AbstractServiceInvoker invoker;

```

### FieldMayBeFinal
Field `addressInfo` may be 'final'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/RpcServiceFactory.java`
#### Snippet
```java

        private Class clazzType;
        private NodeAddrInfo addressInfo;
        private RpcConfig config;

```

### FieldMayBeFinal
Field `trafficName` may be 'final'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/metric/TrafficStatsUnit.java`
#### Snippet
```java

    // the traffic name
    private String trafficName;
    // the message count
    public LongStatsCounter msgCnt;
```

### FieldMayBeFinal
Field `value` may be 'final'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/balance/EventType.java`
#### Snippet
```java
    UNKNOWN(-1, "Unknown operation type");

    private int value;
    private String description;

```

### FieldMayBeFinal
Field `description` may be 'final'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/balance/EventStatus.java`
#### Snippet
```java

    private int value;
    private String description;

    EventStatus(int value, String description) {
```

### FieldMayBeFinal
Field `value` may be 'final'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/balance/EventStatus.java`
#### Snippet
```java
    FAILED(-2, "Process failed");

    private int value;
    private String description;

```

### FieldMayBeFinal
Field `description` may be 'final'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/balance/EventType.java`
#### Snippet
```java

    private int value;
    private String description;

    EventType(int value, String description) {
```

### FieldMayBeFinal
Field `host` may be 'final'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/cluster/NodeAddrInfo.java`
#### Snippet
```java
    private static final long serialVersionUID = -1L;
    private String hostPortStr;
    private String host;
    private int port;

```

### FieldMayBeFinal
Field `hostPortStr` may be 'final'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/cluster/NodeAddrInfo.java`
#### Snippet
```java

    private static final long serialVersionUID = -1L;
    private String hostPortStr;
    private String host;
    private int port;
```

### FieldMayBeFinal
Field `port` may be 'final'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/cluster/NodeAddrInfo.java`
#### Snippet
```java
    private String hostPortStr;
    private String host;
    private int port;

    public NodeAddrInfo(String host, int port) {
```

### FieldMayBeFinal
Field `partitionNum` may be 'final'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/cluster/TopicInfo.java`
#### Snippet
```java
    private String simpleTopicInfo;
    private String simpleValue;
    private int partitionNum;
    private int topicStoreNum = 1;
    private boolean acceptPublish;
```

### FieldMayBeFinal
Field `broker` may be 'final'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/cluster/TopicInfo.java`
#### Snippet
```java

    private static final long serialVersionUID = -2394664452604382172L;
    private BrokerInfo broker;
    private String topic;
    private String simpleTopicInfo;
```

### FieldMayBeFinal
Field `topic` may be 'final'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/cluster/TopicInfo.java`
#### Snippet
```java
    private static final long serialVersionUID = -2394664452604382172L;
    private BrokerInfo broker;
    private String topic;
    private String simpleTopicInfo;
    private String simpleValue;
```

### FieldMayBeFinal
Field `peerInfo` may be 'final'
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/common/ConfirmResult.java`
#### Snippet
```java

    private String topicName = "";
    private PeerInfo peerInfo = new PeerInfo();
    private String confirmContext = "";

```

### FieldMayBeFinal
Field `confirmContext` may be 'final'
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/common/ConfirmResult.java`
#### Snippet
```java
    private String topicName = "";
    private PeerInfo peerInfo = new PeerInfo();
    private String confirmContext = "";

    public ConfirmResult() {
```

### FieldMayBeFinal
Field `peerInfo` may be 'final'
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/common/ConsumeResult.java`
#### Snippet
```java

    private String topicName = "";
    private PeerInfo peerInfo = new PeerInfo();
    private String confirmContext = "";
    private List<Message> messageList = new ArrayList<>();
```

### FieldMayBeFinal
Field `success` may be 'final'
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/PartitionSelectResult.java`
#### Snippet
```java
public class PartitionSelectResult {

    private boolean success;
    private int errCode;
    private String errMsg;
```

### FieldMayBeFinal
Field `partition` may be 'final'
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/FetchContext.java`
#### Snippet
```java
public class FetchContext {

    private Partition partition;
    private long usedToken;
    private boolean lastConsumed = false;
```

### FieldMayBeFinal
Field `usedToken` may be 'final'
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/FetchContext.java`
#### Snippet
```java

    private Partition partition;
    private long usedToken;
    private boolean lastConsumed = false;
    private boolean success = false;
```

### FieldMayBeFinal
Field `description` may be 'final'
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/ConsumePosition.java`
#### Snippet
```java

    private int code;
    private String description;

    ConsumePosition(int code, String description) {
```

### FieldMayBeFinal
Field `code` may be 'final'
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/ConsumePosition.java`
#### Snippet
```java
            "Start from 0 for the first time. Otherwise start from last consume position.");

    private int code;
    private String description;

```

### FieldMayBeFinal
Field `assignedPartMap` may be 'final'
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/ClientSubInfo.java`
#### Snippet
```java
    private String requiredPartition = "";
    private Set<String> subscribedTopics = new HashSet<>();
    private Map<String, Long> assignedPartMap = new HashMap<>();
    private Map<String, Boolean> topicFilterMap = new HashMap<>();

```

### FieldMayBeFinal
Field `topicFilterMap` may be 'final'
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/ClientSubInfo.java`
#### Snippet
```java
    private Set<String> subscribedTopics = new HashSet<>();
    private Map<String, Long> assignedPartMap = new HashMap<>();
    private Map<String, Boolean> topicFilterMap = new HashMap<>();

    public ClientSubInfo() {
```

### FieldMayBeFinal
Field `subscribedTopics` may be 'final'
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/ClientSubInfo.java`
#### Snippet
```java
    private boolean isSelectBig = true;
    private String requiredPartition = "";
    private Set<String> subscribedTopics = new HashSet<>();
    private Map<String, Long> assignedPartMap = new HashMap<>();
    private Map<String, Boolean> topicFilterMap = new HashMap<>();
```

### FieldMayBeFinal
Field `isNotAllocated` may be 'final'
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/ClientSubInfo.java`
#### Snippet
```java
            new ConcurrentHashMap<>();
    private boolean requireBound = false;
    private AtomicBoolean isNotAllocated =
            new AtomicBoolean(true);
    private int sourceCount = -1;
```

### FieldMayBeFinal
Field `messageListener` may be 'final'
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/TopicProcessor.java`
#### Snippet
```java
public class TopicProcessor {

    private MessageListener messageListener;
    private TreeSet<String> filterCondStrs;
    private List<Integer> filterCondCodes = new ArrayList<>();
```

### FieldMayBeFinal
Field `filterCondCodes` may be 'final'
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/TopicProcessor.java`
#### Snippet
```java
    private MessageListener messageListener;
    private TreeSet<String> filterCondStrs;
    private List<Integer> filterCondCodes = new ArrayList<>();

    public TopicProcessor(final MessageListener messageListener,
```

### FieldMayBeFinal
Field `filterCondStrs` may be 'final'
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/TopicProcessor.java`
#### Snippet
```java

    private MessageListener messageListener;
    private TreeSet<String> filterCondStrs;
    private List<Integer> filterCondCodes = new ArrayList<>();

```

### FieldMayBeFinal
Field `tlsConfig` may be 'final'
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/config/TubeClientConfig.java`
#### Snippet
```java
    private String usrPassWord = "";
    // TLS configuration.
    private TLSConfig tlsConfig = new TLSConfig();

    public TubeClientConfig(String masterAddrInfo) {
```

### FieldMayBeFinal
Field `peerInfo` may be 'final'
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/ConsumerResult.java`
#### Snippet
```java

    private String topicName = "";
    private PeerInfo peerInfo = new PeerInfo();
    private String confirmContext = "";
    private List<Message> messageList = new ArrayList<>();
```

### FieldMayBeFinal
Field `partitionKey` may be 'final'
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/ConsumeOffsetInfo.java`
#### Snippet
```java
public class ConsumeOffsetInfo {

    private String partitionKey;
    private long currOffset = TBaseConstants.META_VALUE_UNDEFINED;
    private long maxOffset = TBaseConstants.META_VALUE_UNDEFINED;
```

### FieldMayBeFinal
Field `lastFailureCount` may be 'final'
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/SimplePushMessageConsumer.java`
#### Snippet
```java
    private final BaseMessageConsumer baseConsumer;
    private AtomicLong lastLogPrintTime = new AtomicLong(0);
    private AtomicLong lastFailureCount = new AtomicLong(0);
    private CountDownLatch consumeSync = new CountDownLatch(0);

```

### FieldMayBeFinal
Field `lastLogPrintTime` may be 'final'
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/SimplePushMessageConsumer.java`
#### Snippet
```java
    private final MessageFetchManager fetchManager;
    private final BaseMessageConsumer baseConsumer;
    private AtomicLong lastLogPrintTime = new AtomicLong(0);
    private AtomicLong lastFailureCount = new AtomicLong(0);
    private CountDownLatch consumeSync = new CountDownLatch(0);
```

### FieldMayBeFinal
Field `managerStatus` may be 'final'
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/MessageFetchManager.java`
#### Snippet
```java
    // 0: Stopped
    // 1: Started
    private AtomicInteger managerStatus = new AtomicInteger(-1);
    private Thread[] fetchWorkerPool;

```

### FieldMayBeFinal
Field `sendNumSnapshot` may be 'final'
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/producer/qltystats/BrokerStatsItemSet.java`
#### Snippet
```java
    private AtomicLong receiveNum = new AtomicLong();
    private AtomicLong recSucNum = new AtomicLong();
    private AtomicLong sendNumSnapshot = new AtomicLong();
    private AtomicLong recSucNumSnapshot = new AtomicLong();
    private long dltSendNum = 0;
```

### FieldMayBeFinal
Field `receiveNum` may be 'final'
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/producer/qltystats/BrokerStatsItemSet.java`
#### Snippet
```java

    private AtomicLong sendNum = new AtomicLong();
    private AtomicLong receiveNum = new AtomicLong();
    private AtomicLong recSucNum = new AtomicLong();
    private AtomicLong sendNumSnapshot = new AtomicLong();
```

### FieldMayBeFinal
Field `sendNum` may be 'final'
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/producer/qltystats/BrokerStatsItemSet.java`
#### Snippet
```java
public class BrokerStatsItemSet {

    private AtomicLong sendNum = new AtomicLong();
    private AtomicLong receiveNum = new AtomicLong();
    private AtomicLong recSucNum = new AtomicLong();
```

### FieldMayBeFinal
Field `recSucNumSnapshot` may be 'final'
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/producer/qltystats/BrokerStatsItemSet.java`
#### Snippet
```java
    private AtomicLong recSucNum = new AtomicLong();
    private AtomicLong sendNumSnapshot = new AtomicLong();
    private AtomicLong recSucNumSnapshot = new AtomicLong();
    private long dltSendNum = 0;
    private long dltRecSucNum = 0;
```

### FieldMayBeFinal
Field `recSucNum` may be 'final'
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/producer/qltystats/BrokerStatsItemSet.java`
#### Snippet
```java
    private AtomicLong sendNum = new AtomicLong();
    private AtomicLong receiveNum = new AtomicLong();
    private AtomicLong recSucNum = new AtomicLong();
    private AtomicLong sendNumSnapshot = new AtomicLong();
    private AtomicLong recSucNumSnapshot = new AtomicLong();
```

### FieldMayBeFinal
Field `minDataLimitDlt` may be 'final'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/policies/FlowCtrlRuleHandler.java`
#### Snippet
```java
    private AtomicInteger minZeroCnt =
            new AtomicInteger(Integer.MAX_VALUE);
    private AtomicLong minDataLimitDlt =
            new AtomicLong(Long.MAX_VALUE);
    private AtomicInteger dataLimitStartTime =
```

### FieldMayBeFinal
Field `dataLimitEndTime` may be 'final'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/policies/FlowCtrlRuleHandler.java`
#### Snippet
```java
    private AtomicInteger dataLimitStartTime =
            new AtomicInteger(2500);
    private AtomicInteger dataLimitEndTime =
            new AtomicInteger(TBaseConstants.META_VALUE_UNDEFINED);
    private FlowCtrlItem filterCtrlItem =
```

### FieldMayBeFinal
Field `dataLimitStartTime` may be 'final'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/policies/FlowCtrlRuleHandler.java`
#### Snippet
```java
    private AtomicLong minDataLimitDlt =
            new AtomicLong(Long.MAX_VALUE);
    private AtomicInteger dataLimitStartTime =
            new AtomicInteger(2500);
    private AtomicInteger dataLimitEndTime =
```

### FieldMayBeFinal
Field `qryPriorityId` may be 'final'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/policies/FlowCtrlRuleHandler.java`
#### Snippet
```java
    private AtomicLong flowCtrlId =
            new AtomicLong(TBaseConstants.META_VALUE_UNDEFINED);
    private AtomicInteger qryPriorityId =
            new AtomicInteger(TBaseConstants.META_VALUE_UNDEFINED);
    private String strFlowCtrlInfo;
```

### FieldMayBeFinal
Field `flowCtrlId` may be 'final'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/policies/FlowCtrlRuleHandler.java`
#### Snippet
```java
    private final ReentrantLock writeLock = new ReentrantLock();
    // Flow control ID and string information obtained from the server
    private AtomicLong flowCtrlId =
            new AtomicLong(TBaseConstants.META_VALUE_UNDEFINED);
    private AtomicInteger qryPriorityId =
```

### FieldMayBeFinal
Field `minZeroCnt` may be 'final'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/policies/FlowCtrlRuleHandler.java`
#### Snippet
```java
    // The maximum interval of the flow control extracts the set of values,
    // improving the efficiency of the search return in the range
    private AtomicInteger minZeroCnt =
            new AtomicInteger(Integer.MAX_VALUE);
    private AtomicLong minDataLimitDlt =
```

### FieldMayBeFinal
Field `lastPrintTime` may be 'final'
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/producer/qltystats/DefaultBrokerRcvQltyStats.java`
#### Snippet
```java
    // 1:Stopped
    private final AtomicInteger statusId = new AtomicInteger(-1);
    private long lastPrintTime = System.currentTimeMillis();
    // Total sent request number.
    private final AtomicLong curTotalSentRequestNum = new AtomicLong(0);
```

### FieldMayBeFinal
Field `indexId` may be 'final'
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/RmtDataCache.java`
#### Snippet
```java
    public class TimeoutTask implements TimerTask {

        private String indexId;
        private long createTime = 0L;

```

### FieldMayBeFinal
Field `lastEmptyTopicPrintTime` may be 'final'
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/RmtDataCache.java`
#### Snippet
```java
    // store broker configure info
    private long lastEmptyBrokerPrintTime = 0;
    private long lastEmptyTopicPrintTime = 0;
    private long lastBrokerUpdatedTime = System.currentTimeMillis();
    private final AtomicLong lstBrokerConfigId =
```

### FieldMayBeFinal
Field `payloadFilePath` may be 'final'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/tools/cli/CliProducer.java`
#### Snippet
```java
    private boolean useRandData = true;
    private int msgDataSize = 1000;
    private String payloadFilePath = null;
    private String payloadDelim = null;
    private long rpcTimeoutMs = TBaseConstants.META_VALUE_UNDEFINED;
```

### FieldMayBeFinal
Field `useRandData` may be 'final'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/tools/cli/CliProducer.java`
#### Snippet
```java
    private String masterServers;
    private long msgCount = TBaseConstants.META_VALUE_UNDEFINED;
    private boolean useRandData = true;
    private int msgDataSize = 1000;
    private String payloadFilePath = null;
```

### FieldMayBeFinal
Field `payloadDelim` may be 'final'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/tools/cli/CliProducer.java`
#### Snippet
```java
    private int msgDataSize = 1000;
    private String payloadFilePath = null;
    private String payloadDelim = null;
    private long rpcTimeoutMs = TBaseConstants.META_VALUE_UNDEFINED;
    private boolean reuseConn = false;
```

### FieldMayBeFinal
Field `broker` may be 'final'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/web/WebServer.java`
#### Snippet
```java
    private int port = 8080;
    private org.eclipse.jetty.server.Server srv;
    private TubeBroker broker;

    public WebServer(String hostname, int port, TubeBroker broker) {
```

### FieldMayBeFinal
Field `maxIndexSegmentSize` may be 'final'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/tools/StoreRepairAdmin.java`
#### Snippet
```java
        private final File topicDir;
        private final File indexDir;
        private int maxIndexSegmentSize =
                500000 * DataStoreUtils.STORE_INDEX_HEAD_LEN;
        private SegmentList segments;
```

### FieldMayBeFinal
Field `promMetricService` may be 'final'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/TubeBroker.java`
#### Snippet
```java
            new SimpleClientAuthenticateHandler();
    private MasterService masterService;
    private BrokerPromMetricService promMetricService;
    private boolean requireReportConf = false;
    private boolean isOnline = false;
```

### FieldMayBeFinal
Field `masterService` may be 'final'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/TubeBroker.java`
#### Snippet
```java
    private final ClientAuthenticateHandler clientAuthHandler =
            new SimpleClientAuthenticateHandler();
    private MasterService masterService;
    private BrokerPromMetricService promMetricService;
    private boolean requireReportConf = false;
```

### FieldMayBeFinal
Field `maxReleaseTryCnt` may be 'final'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/TubeBroker.java`
#### Snippet
```java
    private final AtomicBoolean shutdownHooked = new AtomicBoolean(false);
    private final AtomicLong heartbeatErrors = new AtomicLong(0);
    private int maxReleaseTryCnt = 10;

    /**
```

### FieldMayBeFinal
Field `configId` may be 'final'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/metadata/ClusterConfigHolder.java`
#### Snippet
```java
public class ClusterConfigHolder {

    private static AtomicLong configId =
            new AtomicLong(TBaseConstants.META_VALUE_UNDEFINED);
    private static AtomicInteger maxMsgSize =
```

### FieldMayBeFinal
Field `minMemCacheSize` may be 'final'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/metadata/ClusterConfigHolder.java`
#### Snippet
```java
            new AtomicInteger(TBaseConstants.META_MAX_MESSAGE_DATA_SIZE
                    + TBaseConstants.META_MAX_MESSAGE_HEADER_SIZE);
    private static AtomicInteger minMemCacheSize =
            new AtomicInteger(TBaseConstants.META_MIN_MEM_BUFFER_SIZE);

```

### FieldMayBeFinal
Field `maxMsgSize` may be 'final'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/metadata/ClusterConfigHolder.java`
#### Snippet
```java
    private static AtomicLong configId =
            new AtomicLong(TBaseConstants.META_VALUE_UNDEFINED);
    private static AtomicInteger maxMsgSize =
            new AtomicInteger(TBaseConstants.META_MAX_MESSAGE_DATA_SIZE
                    + TBaseConstants.META_MAX_MESSAGE_HEADER_SIZE);
```

### FieldMayBeFinal
Field `started` may be 'final'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/BrokerServiceServer.java`
#### Snippet
```java
            new ConsumerTimeoutListener();
    // status of broker service.
    private AtomicBoolean started = new AtomicBoolean(false);

    public BrokerServiceServer(final TubeBroker tubeBroker,
```

### FieldMayBeFinal
Field `addrRcvFrom` may be 'final'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/nodeinfo/ConsumerNodeInfo.java`
#### Snippet
```java
    private final Set<Integer> filterCondCode = new HashSet<>(10);
    // consumer's address
    private String addrRcvFrom;
    private String rmtAddrInfo;
    private boolean isSupportLimit = false;
```

### FieldMayBeFinal
Field `comparators` may be 'final'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/utils/WritableComparator.java`
#### Snippet
```java
public class WritableComparator {

    private static HashMap<Class, WritableComparator> comparators = new HashMap();

    public static int hashBytes(byte[] bytes, int offset, int length) {
```

### FieldMayBeFinal
Field `value` may be 'final'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/fielddef/WebFieldType.java`
#### Snippet
```java
    DELPOLICY(11, "Delete policy");

    private int value;
    private String desc;

```

### FieldMayBeFinal
Field `desc` may be 'final'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/fielddef/WebFieldType.java`
#### Snippet
```java

    private int value;
    private String desc;

    WebFieldType(int value, String desc) {
```

### FieldMayBeFinal
Field `description` may be 'final'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/statusdef/TopicStsChgType.java`
#### Snippet
```java

    private int code;
    private String description;

    TopicStsChgType(int code, String description) {
```

### FieldMayBeFinal
Field `code` may be 'final'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/statusdef/TopicStsChgType.java`
#### Snippet
```java
    STATUS_CHANGE_REDO_SFDEL(2, "Redo soft delete");

    private int code;
    private String description;

```

### FieldMayBeFinal
Field `code` may be 'final'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/statusdef/EnableStatus.java`
#### Snippet
```java
    STATUS_ENABLE(2, "Enable.");

    private int code;
    private String description;

```

### FieldMayBeFinal
Field `description` may be 'final'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/statusdef/EnableStatus.java`
#### Snippet
```java

    private int code;
    private String description;

    EnableStatus(int code, String description) {
```

### FieldMayBeFinal
Field `description` may be 'final'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/statusdef/TopicStatus.java`
#### Snippet
```java

    private int code;
    private String description;

    TopicStatus(int code, String description) {
```

### FieldMayBeFinal
Field `code` may be 'final'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/statusdef/TopicStatus.java`
#### Snippet
```java
    STATUS_TOPIC_HARD_REMOVE(3, "Hard removed");

    private int code;
    private String description;

```

### FieldMayBeFinal
Field `description` may be 'final'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/statusdef/CleanPolType.java`
#### Snippet
```java

    private int code;
    private String description;

    CleanPolType(int code, String description) {
```

### FieldMayBeFinal
Field `code` may be 'final'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/statusdef/CleanPolType.java`
#### Snippet
```java
    CLEAN_POL_DELETE(0, "Delete");

    private int code;
    private String description;

```

### FieldMayBeFinal
Field `description` may be 'final'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/statusdef/ManageStatus.java`
#### Snippet
```java

    private int code;
    private String description;
    private boolean isAcceptPublish;
    private boolean isAcceptSubscribe;
```

### FieldMayBeFinal
Field `code` may be 'final'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/statusdef/ManageStatus.java`
#### Snippet
```java
    STATUS_MANAGE_OFFLINE(9, "offline", false, false);

    private int code;
    private String description;
    private boolean isAcceptPublish;
```

### FieldMayBeFinal
Field `isAcceptSubscribe` may be 'final'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/statusdef/ManageStatus.java`
#### Snippet
```java
    private String description;
    private boolean isAcceptPublish;
    private boolean isAcceptSubscribe;

    ManageStatus(int code, String description,
```

### FieldMayBeFinal
Field `isAcceptPublish` may be 'final'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/statusdef/ManageStatus.java`
#### Snippet
```java
    private int code;
    private String description;
    private boolean isAcceptPublish;
    private boolean isAcceptSubscribe;

```

### FieldMayBeFinal
Field `quorum` may be 'final'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/zookeeper/ZooKeeperWatcher.java`
#### Snippet
```java
    private String baseZNode;
    // zookeeper quorum
    private String quorum;
    // zookeeper connection
    private RecoverableZooKeeper recoverableZooKeeper;
```

### FieldMayBeFinal
Field `saslLatch` may be 'final'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/zookeeper/ZooKeeperWatcher.java`
#### Snippet
```java
    // Used by ZKUtil:waitForZKConnectionIfAuthenticating to wait for SASL
    // negotiation to complete
    private CountDownLatch saslLatch = new CountDownLatch(1);
    // base znode for this cluster
    private String baseZNode;
```

### FieldMayBeFinal
Field `abortable` may be 'final'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/zookeeper/ZooKeeperWatcher.java`
#### Snippet
```java
    private final Exception constructorCaller;
    // abortable in case of zk failure
    private Abortable abortable;

    // node names
```

### FieldMayBeFinal
Field `auditProxyAddrSet` may be 'final'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/fileconfig/ADConfig.java`
#### Snippet
```java
    private boolean auditEnable = false;
    // audit proxy server addresses
    private HashSet<String> auditProxyAddrSet =
            new HashSet<>(Arrays.asList("127.0.0.1:10081"));
    // file path for audit cache data
```

### FieldMayBeFinal
Field `defMetaDataService` may be 'final'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/MasterStatusCheckFilter.java`
#### Snippet
```java

    private TMaster master;
    private MetaDataService defMetaDataService;

    public MasterStatusCheckFilter(TMaster master) {
```

### FieldMayBeFinal
Field `master` may be 'final'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/MasterStatusCheckFilter.java`
#### Snippet
```java
public class MasterStatusCheckFilter implements Filter {

    private TMaster master;
    private MetaDataService defMetaDataService;

```

### FieldMayBeFinal
Field `quorumServers` may be 'final'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/zookeeper/RecoverableZooKeeper.java`
#### Snippet
```java
    private Watcher watcher;
    private int sessionTimeout;
    private String quorumServers;

    public RecoverableZooKeeper(String quorumServers, int sessionTimeout, Watcher watcher,
```

### FieldMayBeFinal
Field `watcher` may be 'final'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/zookeeper/RecoverableZooKeeper.java`
#### Snippet
```java
    private ZooKeeper zk;
    // An identifier of this process in the cluster
    private Watcher watcher;
    private int sessionTimeout;
    private String quorumServers;
```

### FieldMayBeFinal
Field `sessionTimeout` may be 'final'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/zookeeper/RecoverableZooKeeper.java`
#### Snippet
```java
    // An identifier of this process in the cluster
    private Watcher watcher;
    private int sessionTimeout;
    private String quorumServers;

```

### FieldMayBeFinal
Field `master` may be 'final'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/WebServer.java`
#### Snippet
```java
    private final MasterConfig masterConfig;
    private org.eclipse.jetty.server.Server srv;
    private TMaster master;

    public WebServer(final MasterConfig masterConfig, TMaster master) {
```

### FieldMayBeFinal
Field `master` may be 'final'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/action/screen/config/BrokerDetail.java`
#### Snippet
```java
public class BrokerDetail implements Action {

    private TMaster master;

    public BrokerDetail(TMaster master) {
```

### FieldMayBeFinal
Field `master` may be 'final'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/action/screen/config/TopicDetail.java`
#### Snippet
```java
public class TopicDetail implements Action {

    private TMaster master;

    public TopicDetail(TMaster master) {
```

### FieldMayBeFinal
Field `master` may be 'final'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/action/screen/config/TopicList.java`
#### Snippet
```java
public class TopicList implements Action {

    private TMaster master;

    public TopicList(TMaster master) {
```

### FieldMayBeFinal
Field `master` may be 'final'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/action/screen/consume/Detail.java`
#### Snippet
```java
public class Detail implements Action {

    private TMaster master;

    public Detail(TMaster master) {
```

### FieldMayBeFinal
Field `cachedParams` may be 'final'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/simplemvc/MultipartHttpServletRequest.java`
#### Snippet
```java

    private boolean parsed;
    private HashMap<String, String> cachedParams = new HashMap<>();

    public MultipartHttpServletRequest(HttpServletRequest request) {
```

### FieldMayBeFinal
Field `curCltBalanceParal` may be 'final'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/TMaster.java`
#### Snippet
```java
    private int balanceDelayTimes = 0;
    private AtomicInteger curSvrBalanceParal = new AtomicInteger(0);
    private AtomicInteger curCltBalanceParal = new AtomicInteger(0);
    private Sleeper stopSleeper = new Sleeper(1000, this);
    private SimpleVisitTokenManager visitTokenManager;
```

### FieldMayBeFinal
Field `curSvrBalanceParal` may be 'final'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/TMaster.java`
#### Snippet
```java
    private boolean startupBalance = true;
    private int balanceDelayTimes = 0;
    private AtomicInteger curSvrBalanceParal = new AtomicInteger(0);
    private AtomicInteger curCltBalanceParal = new AtomicInteger(0);
    private Sleeper stopSleeper = new Sleeper(1000, this);
```

### FieldMayBeFinal
Field `idGenerator` may be 'final'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/TMaster.java`
#### Snippet
```java
    private final CertificateMasterHandler serverAuthHandler; // server auth handler
    private AtomicBoolean shutdownHooked = new AtomicBoolean(false);
    private AtomicLong idGenerator = new AtomicLong(0); // id generator
    private volatile boolean stopped = false; // stop flag
    private Thread balancerChore; // balance chore
```

### FieldMayBeFinal
Field `stopSleeper` may be 'final'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/TMaster.java`
#### Snippet
```java
    private AtomicInteger curSvrBalanceParal = new AtomicInteger(0);
    private AtomicInteger curCltBalanceParal = new AtomicInteger(0);
    private Sleeper stopSleeper = new Sleeper(1000, this);
    private SimpleVisitTokenManager visitTokenManager;

```

### FieldMayBeFinal
Field `visitTokenManager` may be 'final'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/TMaster.java`
#### Snippet
```java
    private AtomicInteger curCltBalanceParal = new AtomicInteger(0);
    private Sleeper stopSleeper = new Sleeper(1000, this);
    private SimpleVisitTokenManager visitTokenManager;

    /**
```

### FieldMayBeFinal
Field `shutdownHooked` may be 'final'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/TMaster.java`
#### Snippet
```java
    private final ShutdownHook shutdownHook; // shutdown hook
    private final CertificateMasterHandler serverAuthHandler; // server auth handler
    private AtomicBoolean shutdownHooked = new AtomicBoolean(false);
    private AtomicLong idGenerator = new AtomicLong(0); // id generator
    private volatile boolean stopped = false; // stop flag
```

### FieldMayBeFinal
Field `balanceDelayTimes` may be 'final'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/TMaster.java`
#### Snippet
```java
    private boolean initialized = false;
    private boolean startupBalance = true;
    private int balanceDelayTimes = 0;
    private AtomicInteger curSvrBalanceParal = new AtomicInteger(0);
    private AtomicInteger curCltBalanceParal = new AtomicInteger(0);
```

### FieldMayBeFinal
Field `code` may be 'final'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/metamanage/DataOpErrCode.java`
#### Snippet
```java
    DERR_MASTER_UNKNOWN(599, "Unknown error");

    private int code;
    private String description;

```

### FieldMayBeFinal
Field `description` may be 'final'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/metamanage/DataOpErrCode.java`
#### Snippet
```java

    private int code;
    private String description;

    DataOpErrCode(int code, String description) {
```

### FieldMayBeFinal
Field `clientId` may be 'final'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/nodemanage/nodeconsumer/NodeRebInfo.java`
#### Snippet
```java
public class NodeRebInfo {

    private String clientId;
    private int status;
    private long createTime;
```

### FieldMayBeFinal
Field `waitDuration` may be 'final'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/nodemanage/nodeconsumer/NodeRebInfo.java`
#### Snippet
```java
    private long createTime;
    private int reqType;
    private AtomicInteger waitDuration = new AtomicInteger(0);

    public NodeRebInfo(String clientId,
```

### FieldMayBeFinal
Field `createTime` may be 'final'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/nodemanage/nodeconsumer/NodeRebInfo.java`
#### Snippet
```java
    private String clientId;
    private int status;
    private long createTime;
    private int reqType;
    private AtomicInteger waitDuration = new AtomicInteger(0);
```

### FieldMayBeFinal
Field `reqType` may be 'final'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/nodemanage/nodeconsumer/NodeRebInfo.java`
#### Snippet
```java
    private int status;
    private long createTime;
    private int reqType;
    private AtomicInteger waitDuration = new AtomicInteger(0);

```

### FieldMayBeFinal
Field `forbiddenTime` may be 'final'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/nodemanage/nodebroker/BrokerAbnHolder.java`
#### Snippet
```java
        private ManageStatus befStatus;
        private ManageStatus newStatus;
        private long forbiddenTime;
        private long lastUpdateTime;

```

### FieldMayBeFinal
Field `firstRepTime` may be 'final'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/nodemanage/nodebroker/BrokerAbnHolder.java`
#### Snippet
```java
        private int brokerId;
        private int abnStatus; // 0 normal , -100 read abnormal, -1 write abnormal, -101 r & w abnormal
        private long firstRepTime;
        private long lastRepTime;

```

### FieldMayBeFinal
Field `brokerId` may be 'final'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/nodemanage/nodebroker/BrokerAbnHolder.java`
#### Snippet
```java
    public static class BrokerAbnInfo {

        private int brokerId;
        private int abnStatus; // 0 normal , -100 read abnormal, -1 write abnormal, -101 r & w abnormal
        private long firstRepTime;
```

### FieldMayBeFinal
Field `brokerId` may be 'final'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/nodemanage/nodebroker/BrokerAbnHolder.java`
#### Snippet
```java
    public static class BrokerFbdInfo {

        private int brokerId;
        private ManageStatus befStatus;
        private ManageStatus newStatus;
```

### FieldMayBeFinal
Field `lastBrokerUpdatedTime` may be 'final'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/nodemanage/nodebroker/DefBrokerRunManager.java`
#### Snippet
```java
    private final AtomicLong brokerInfoCheckSum =
            new AtomicLong(System.currentTimeMillis());
    private long lastBrokerUpdatedTime = System.currentTimeMillis();
    private final ConcurrentHashMap<Integer, String> brokersMap =
            new ConcurrentHashMap<>();
```

### FieldMayBeFinal
Field `typeName` may be 'final'
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/enums/TaskTypeEnum.java`
#### Snippet
```java
    private Integer taskType;

    private String typeName;

    TaskTypeEnum(Integer role, String typeName) {
```

### FieldMayBeFinal
Field `taskType` may be 'final'
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/enums/TaskTypeEnum.java`
#### Snippet
```java
    CREATE_TOPIC(1, "createTopicTask");

    private Integer taskType;

    private String typeName;
```

### FieldMayBeFinal
Field `message` may be 'final'
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/enums/ErrorCode.java`
#### Snippet
```java
    private Integer code;

    private String message;

    ErrorCode(Integer code, String message) {
```

### FieldMayBeFinal
Field `code` may be 'final'
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/enums/ErrorCode.java`
#### Snippet
```java
    TASK_EXIST(200, "task already exist");

    private Integer code;

    private String message;
```

### FieldMayBeFinal
Field `code` may be 'final'
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/enums/TaskStatusEnum.java`
#### Snippet
```java
    SUCCESS(3, "success");

    private Integer code;

    private String message;
```

### FieldMayBeFinal
Field `message` may be 'final'
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/enums/TaskStatusEnum.java`
#### Snippet
```java
    private Integer code;

    private String message;

    TaskStatusEnum(Integer code, String message) {
```

### FieldMayBeFinal
Field `httpclient` may be 'final'
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/MasterServiceImpl.java`
#### Snippet
```java
public class MasterServiceImpl implements MasterService {

    private static CloseableHttpClient httpclient = HttpClients.createDefault();
    private static Gson gson = new Gson();

```

### FieldMayBeFinal
Field `gson` may be 'final'
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/MasterServiceImpl.java`
#### Snippet
```java

    private static CloseableHttpClient httpclient = HttpClients.createDefault();
    private static Gson gson = new Gson();

    @Autowired
```

### FieldMayBeFinal
Field `gson` may be 'final'
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/controller/task/TaskController.java`
#### Snippet
```java
    private TaskService taskService;

    private Gson gson = new Gson();

    @RequestMapping(value = "")
```

### FieldMayBeFinal
Field `gson` may be 'final'
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/controller/group/GroupController.java`
#### Snippet
```java
public class GroupController {

    private Gson gson = new Gson();

    @Autowired
```

### FieldMayBeFinal
Field `gson` may be 'final'
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/controller/topic/TopicWebController.java`
#### Snippet
```java
    private NodeService nodeService;

    private Gson gson = new Gson();

    @Autowired
```

### FieldMayBeFinal
Field `properties` may be 'final'
in `inlong-tubemq/tubemq-connectors/tubemq-connector-flink/src/main/java/org/apache/flink/connectors/tubemq/Tubemq.java`
#### Snippet
```java

    @Nonnull
    private Map<String, String> properties;

    public Tubemq() {
```

### FieldMayBeFinal
Field `sourceType` may be 'final'
in `inlong-manager/manager-plugins/src/main/java/org/apache/inlong/manager/plugin/flink/enums/ConnectorJarType.java`
#### Snippet
```java
    ;

    private String sourceType;
    private String connectorType;

```

### FieldMayBeFinal
Field `connectorType` may be 'final'
in `inlong-manager/manager-plugins/src/main/java/org/apache/inlong/manager/plugin/flink/enums/ConnectorJarType.java`
#### Snippet
```java

    private String sourceType;
    private String connectorType;

    ConnectorJarType(String sourceType, String connectorType) {
```

### FieldMayBeFinal
Field `agentTaskCache` may be 'final'
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/source/SourceSnapshotOperator.java`
#### Snippet
```java
     * Cache the task ip and task status, the key is task ip
     */
    private Cache<String, ConcurrentHashMap<Integer, Integer>> agentTaskCache = CacheBuilder.newBuilder()
            .maximumSize(1000).expireAfterWrite(30, TimeUnit.SECONDS).build(
                    new CacheLoader<String, ConcurrentHashMap<Integer, Integer>>() {
```

### FieldMayBeFinal
Field `channel` may be 'final'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/base/SinkRspEvent.java`
#### Snippet
```java
public class SinkRspEvent implements Event {

    private Channel channel;
    private MsgType msgType;
    private Event event;
```

### FieldMayBeFinal
Field `msgType` may be 'final'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/base/SinkRspEvent.java`
#### Snippet
```java

    private Channel channel;
    private MsgType msgType;
    private Event event;

```

### FieldMayBeFinal
Field `event` may be 'final'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/base/SinkRspEvent.java`
#### Snippet
```java
    private Channel channel;
    private MsgType msgType;
    private Event event;

    public SinkRspEvent(Event event, MsgType msgType, Channel channel) {
```

### FieldMayBeFinal
Field `messageHandler` may be 'final'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/http/MessageProcessServlet.java`
#### Snippet
```java
    private static final LogCounter logCounter = new LogCounter(10, 100000, 60 * 1000);

    private MessageHandler messageHandler;

    public MessageProcessServlet(MessageHandler messageHandler) {
```

### FieldMayBeFinal
Field `ackingCount` may be 'final'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/BatchPackProfileCallback.java`
#### Snippet
```java
public class BatchPackProfileCallback {

    private AtomicInteger ackingCount;
    private SourceCallback callback;

```

### FieldMayBeFinal
Field `callback` may be 'final'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/BatchPackProfileCallback.java`
#### Snippet
```java

    private AtomicInteger ackingCount;
    private SourceCallback callback;

    /**
```

### FieldMayBeFinal
Field `zoneProducer` may be 'final'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/MessageQueueZoneWorker.java`
#### Snippet
```java
    private final MessageQueueZoneSinkContext context;

    private MessageQueueZoneProducer zoneProducer;
    private LifecycleState status;

```

### FieldMayBeFinal
Field `handler` may be 'final'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/MessageQueueClusterProducer.java`
#### Snippet
```java
    private LifecycleState state;

    private MessageQueueHandler handler;

    /**
```

### FieldMayBeFinal
Field `cacheClusterSelector` may be 'final'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/MessageQueueZoneProducer.java`
#### Snippet
```java

    private AtomicInteger clusterIndex = new AtomicInteger(0);
    private CacheClusterSelector cacheClusterSelector;

    /**
```

### FieldMayBeFinal
Field `clusterIndex` may be 'final'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/MessageQueueZoneProducer.java`
#### Snippet
```java
    private List<MessageQueueClusterProducer> deletingClusterList = new ArrayList<>();

    private AtomicInteger clusterIndex = new AtomicInteger(0);
    private CacheClusterSelector cacheClusterSelector;

```

### FieldMayBeFinal
Field `dispatchTime` may be 'final'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/BatchPackProfile.java`
#### Snippet
```java
    private long count = 0;
    private long size = 0;
    private long dispatchTime;
    private BatchPackProfileCallback callback;

```

### FieldMayBeFinal
Field `createTime` may be 'final'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/BatchPackProfile.java`
#### Snippet
```java
    private final String uid;
    private List<ProxyEvent> events = new ArrayList<>();
    private long createTime = System.currentTimeMillis();
    private long count = 0;
    private long size = 0;
```

### FieldMayBeFinal
Field `workers` may be 'final'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/MessageQueueZoneSink.java`
#### Snippet
```java
    private Context parentContext;
    private MessageQueueZoneSinkContext context;
    private List<MessageQueueZoneWorker> workers = new ArrayList<>();
    // message group
    private BatchPackManager dispatchManager;
```

### FieldMayBeFinal
Field `dispatchQueue` may be 'final'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/BatchPackManager.java`
#### Snippet
```java
    private final long maxPackCount;
    private final long maxPackSize;
    private BufferQueue<BatchPackProfile> dispatchQueue;
    private ConcurrentHashMap<String, BatchPackProfile> profileCache = new ConcurrentHashMap<>();
    // flag that manager need to output overtime data.
```

### FieldMayBeFinal
Field `outCounter` may be 'final'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/BatchPackManager.java`
#### Snippet
```java
    private AtomicBoolean needOutputOvertimeData = new AtomicBoolean(false);
    private AtomicLong inCounter = new AtomicLong(0);
    private AtomicLong outCounter = new AtomicLong(0);

    /**
```

### FieldMayBeFinal
Field `needOutputOvertimeData` may be 'final'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/BatchPackManager.java`
#### Snippet
```java
    private ConcurrentHashMap<String, BatchPackProfile> profileCache = new ConcurrentHashMap<>();
    // flag that manager need to output overtime data.
    private AtomicBoolean needOutputOvertimeData = new AtomicBoolean(false);
    private AtomicLong inCounter = new AtomicLong(0);
    private AtomicLong outCounter = new AtomicLong(0);
```

### FieldMayBeFinal
Field `profileCache` may be 'final'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/BatchPackManager.java`
#### Snippet
```java
    private final long maxPackSize;
    private BufferQueue<BatchPackProfile> dispatchQueue;
    private ConcurrentHashMap<String, BatchPackProfile> profileCache = new ConcurrentHashMap<>();
    // flag that manager need to output overtime data.
    private AtomicBoolean needOutputOvertimeData = new AtomicBoolean(false);
```

### FieldMayBeFinal
Field `inCounter` may be 'final'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/BatchPackManager.java`
#### Snippet
```java
    // flag that manager need to output overtime data.
    private AtomicBoolean needOutputOvertimeData = new AtomicBoolean(false);
    private AtomicLong inCounter = new AtomicLong(0);
    private AtomicLong outCounter = new AtomicLong(0);

```

### FieldMayBeFinal
Field `leftSemaphore` may be 'final'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/utils/SizeSemaphore.java`
#### Snippet
```java
    private int leftSize = 0;
    private Semaphore sizeSemaphore = null;
    private AtomicInteger leftSemaphore = new AtomicInteger(0);

    /**
```

### FieldMayBeFinal
Field `MASTER_HOST_PORT_LIST` may be 'final'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/tube/TubeHandler.java`
#### Snippet
```java

    public static final Logger LOG = LoggerFactory.getLogger(TubeHandler.class);
    private static String MASTER_HOST_PORT_LIST = "master-host-port-list";
    public static final String KEY_NAMESPACE = "namespace";

```

### FieldMayBeFinal
Field `handlerLocal` may be 'final'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/tube/TubeHandler.java`
#### Snippet
```java
    private MessageProducer producer;
    private Set<String> topicSet = new HashSet<>();
    private ThreadLocal<EventHandler> handlerLocal = new ThreadLocal<>();

    /**
```

### FieldMayBeFinal
Field `topicSet` may be 'final'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/tube/TubeHandler.java`
#### Snippet
```java
    private TubeMultiSessionFactory sessionFactory;
    private MessageProducer producer;
    private Set<String> topicSet = new HashSet<>();
    private ThreadLocal<EventHandler> handlerLocal = new ThreadLocal<>();

```

### FieldMayBeFinal
Field `handlerLocal` may be 'final'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/kafka/KafkaHandler.java`
#### Snippet
```java
    // kafka producer
    private KafkaProducer<String, byte[]> producer;
    private ThreadLocal<EventHandler> handlerLocal = new ThreadLocal<>();

    /**
```

### FieldMayBeFinal
Field `producerMap` may be 'final'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/pulsar/PulsarHandler.java`
#### Snippet
```java
    private ProducerBuilder<byte[]> baseBuilder;

    private ConcurrentHashMap<String, Producer<byte[]>> producerMap = new ConcurrentHashMap<>();

    /**
```

### FieldMayBeFinal
Field `handlerLocal` may be 'final'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/pulsar/PulsarHandler.java`
#### Snippet
```java
    private String tenant;
    private String namespace;
    private ThreadLocal<EventHandler> handlerLocal = new ThreadLocal<>();

    /**
```

### FieldMayBeFinal
Field `UPD_BUFFER_DEFAULT_SIZE` may be 'final'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/SimpleUdpSource.java`
#### Snippet
```java
            .getLogger(SimpleUdpSource.class);

    private static int UPD_BUFFER_DEFAULT_SIZE = 8192;

    private Bootstrap bootstrap;
```

### FieldMayBeFinal
Field `MAX_TOTAL_MEMORY_SIZE` may be 'final'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/ServerMessageFactory.java`
#### Snippet
```java
    private static long MAX_CHANNEL_MEMORY_SIZE = 1024 * 1024;

    private static long MAX_TOTAL_MEMORY_SIZE = 1024 * 1024;

    private static int MSG_LENGTH_LEN = 4;
```

### FieldMayBeFinal
Field `protocolType` may be 'final'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/ServerMessageFactory.java`
#### Snippet
```java
    private ChannelGroup allChannels;

    private String protocolType;

    private ServiceDecoder serviceDecoder;
```

### FieldMayBeFinal
Field `messageHandlerName` may be 'final'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/ServerMessageFactory.java`
#### Snippet
```java
    private ServiceDecoder serviceDecoder;

    private String messageHandlerName;

    private int maxConnections = Integer.MAX_VALUE;
```

### FieldMayBeFinal
Field `source` may be 'final'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/ServerMessageFactory.java`
#### Snippet
```java
    private static int MSG_LENGTH_LEN = 4;

    private BaseSource source;

    private ChannelProcessor processor;
```

### FieldMayBeFinal
Field `name` may be 'final'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/ServerMessageFactory.java`
#### Snippet
```java
    private boolean isCompressed;

    private String name;

    private String topic;
```

### FieldMayBeFinal
Field `TRAFFIC_CLASS_TYPE_96` may be 'final'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/SimpleTcpSource.java`
#### Snippet
```java
    private static int TRAFFIC_CLASS_TYPE_0 = 0;

    private static int TRAFFIC_CLASS_TYPE_96 = 96;

    private static int HIGH_WATER_MARK_DEFAULT_VALUE = 64 * 1024;
```

### FieldMayBeFinal
Field `TRAFFIC_CLASS_TYPE_0` may be 'final'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/SimpleTcpSource.java`
#### Snippet
```java
    private static final Logger logger = LoggerFactory.getLogger(SimpleTcpSource.class);

    private static int TRAFFIC_CLASS_TYPE_0 = 0;

    private static int TRAFFIC_CLASS_TYPE_96 = 96;
```

### FieldMayBeFinal
Field `HIGH_WATER_MARK_DEFAULT_VALUE` may be 'final'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/SimpleTcpSource.java`
#### Snippet
```java
    private static int TRAFFIC_CLASS_TYPE_96 = 96;

    private static int HIGH_WATER_MARK_DEFAULT_VALUE = 64 * 1024;

    private boolean tcpNoDelay = true;
```

### FieldMayBeFinal
Field `attr` may be 'final'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/ServerMessageFactory.java`
#### Snippet
```java
    private String topic;

    private String attr;

    private boolean filterEmptyMsg;
```

### FieldMayBeFinal
Field `processor` may be 'final'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/ServerMessageFactory.java`
#### Snippet
```java
    private BaseSource source;

    private ChannelProcessor processor;

    private ChannelGroup allChannels;
```

### FieldMayBeFinal
Field `maxMsgLength` may be 'final'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/ServerMessageFactory.java`
#### Snippet
```java
    private int maxConnections = Integer.MAX_VALUE;

    private int maxMsgLength;

    private boolean isCompressed;
```

### FieldMayBeFinal
Field `MSG_LENGTH_LEN` may be 'final'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/ServerMessageFactory.java`
#### Snippet
```java
    private static long MAX_TOTAL_MEMORY_SIZE = 1024 * 1024;

    private static int MSG_LENGTH_LEN = 4;

    private BaseSource source;
```

### FieldMayBeFinal
Field `serviceDecoder` may be 'final'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/ServerMessageFactory.java`
#### Snippet
```java
    private String protocolType;

    private ServiceDecoder serviceDecoder;

    private String messageHandlerName;
```

### FieldMayBeFinal
Field `MAX_CHANNEL_MEMORY_SIZE` may be 'final'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/ServerMessageFactory.java`
#### Snippet
```java
    private static final int DEFAULT_READ_IDLE_TIME = 70 * 60 * 1000;

    private static long MAX_CHANNEL_MEMORY_SIZE = 1024 * 1024;

    private static long MAX_TOTAL_MEMORY_SIZE = 1024 * 1024;
```

### FieldMayBeFinal
Field `topic` may be 'final'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/ServerMessageFactory.java`
#### Snippet
```java
    private String name;

    private String topic;

    private String attr;
```

### FieldMayBeFinal
Field `isCompressed` may be 'final'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/ServerMessageFactory.java`
#### Snippet
```java
    private int maxMsgLength;

    private boolean isCompressed;

    private String name;
```

### FieldMayBeFinal
Field `monitorIndexExt` may be 'final'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/ServerMessageFactory.java`
#### Snippet
```java
    private MonitorIndex monitorIndex;

    private MonitorIndexExt monitorIndexExt;

    /**
```

### FieldMayBeFinal
Field `filterEmptyMsg` may be 'final'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/ServerMessageFactory.java`
#### Snippet
```java
    private String attr;

    private boolean filterEmptyMsg;

    private MonitorIndex monitorIndex;
```

### FieldMayBeFinal
Field `monitorIndex` may be 'final'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/ServerMessageFactory.java`
#### Snippet
```java
    private boolean filterEmptyMsg;

    private MonitorIndex monitorIndex;

    private MonitorIndexExt monitorIndexExt;
```

### FieldMayBeFinal
Field `allChannels` may be 'final'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/ServerMessageFactory.java`
#### Snippet
```java
    private ChannelProcessor processor;

    private ChannelGroup allChannels;

    private String protocolType;
```

### FieldMayBeFinal
Field `protocolType` may be 'final'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/tcp/InlongTcpChannelPipelineFactory.java`
#### Snippet
```java
    private SourceContext sourceContext;
    private String messageHandlerName;
    private String protocolType;

    /**
```

### FieldMayBeFinal
Field `sourceContext` may be 'final'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/tcp/InlongTcpChannelPipelineFactory.java`
#### Snippet
```java
    public static final boolean DEFAULT_FAIL_FAST = true;
    private static final int DEFAULT_READ_IDLE_TIME = 70 * 60 * 1000;
    private SourceContext sourceContext;
    private String messageHandlerName;
    private String protocolType;
```

### FieldMayBeFinal
Field `PKG_TIMEOUT_DEFAULT_SEC` may be 'final'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/BaseSource.java`
#### Snippet
```java
    private static int STAT_INTERVAL_MUST_THAN = 0;

    private static int PKG_TIMEOUT_DEFAULT_SEC = 3;

    private static int MSG_MIN_LENGTH = 4;
```

### FieldMayBeFinal
Field `MAX_MSG_DEFAULT_LENGTH` may be 'final'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/BaseSource.java`
#### Snippet
```java
    private static int MSG_MIN_LENGTH = 4;

    private static int MAX_MSG_DEFAULT_LENGTH = 1024 * 64;

    private static int INTERVAL_SEC = 60;
```

### FieldMayBeFinal
Field `HOST_DEFAULT_VALUE` may be 'final'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/BaseSource.java`
#### Snippet
```java
    protected ChannelFuture channelFuture;

    private static String HOST_DEFAULT_VALUE = "0.0.0.0";

    private static int maxMonitorCnt = ConfigConstants.DEF_MONITOR_STAT_CNT;
```

### FieldMayBeFinal
Field `MSG_MIN_LENGTH` may be 'final'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/BaseSource.java`
#### Snippet
```java
    private static int PKG_TIMEOUT_DEFAULT_SEC = 3;

    private static int MSG_MIN_LENGTH = 4;

    private static int MAX_MSG_DEFAULT_LENGTH = 1024 * 64;
```

### FieldMayBeFinal
Field `INTERVAL_SEC` may be 'final'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/BaseSource.java`
#### Snippet
```java
    private static int MAX_MSG_DEFAULT_LENGTH = 1024 * 64;

    private static int INTERVAL_SEC = 60;

    protected static int BUFFER_SIZE_MUST_THAN = 0;
```

### FieldMayBeFinal
Field `DEFAULT_MAX_CONNECTIONS` may be 'final'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/BaseSource.java`
#### Snippet
```java
    private static int maxMonitorCnt = ConfigConstants.DEF_MONITOR_STAT_CNT;

    private static int DEFAULT_MAX_CONNECTIONS = 5000;

    private static int STAT_INTERVAL_MUST_THAN = 0;
```

### FieldMayBeFinal
Field `STAT_INTERVAL_MUST_THAN` may be 'final'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/BaseSource.java`
#### Snippet
```java
    private static int DEFAULT_MAX_CONNECTIONS = 5000;

    private static int STAT_INTERVAL_MUST_THAN = 0;

    private static int PKG_TIMEOUT_DEFAULT_SEC = 3;
```

### FieldMayBeFinal
Field `takeList` may be 'final'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/channel/ProxyTransaction.java`
#### Snippet
```java
    private Semaphore countSemaphore;
    private BufferQueue<ProxyEvent> bufferQueue;
    private List<ProxyEvent> takeList = new ArrayList<>();
    private List<ProxyEvent> putList = new ArrayList<>();

```

### FieldMayBeFinal
Field `putList` may be 'final'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/channel/ProxyTransaction.java`
#### Snippet
```java
    private BufferQueue<ProxyEvent> bufferQueue;
    private List<ProxyEvent> takeList = new ArrayList<>();
    private List<ProxyEvent> putList = new ArrayList<>();

    /**
```

### FieldMayBeFinal
Field `bufferQueue` may be 'final'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/channel/ProxyTransaction.java`
#### Snippet
```java

    private Semaphore countSemaphore;
    private BufferQueue<ProxyEvent> bufferQueue;
    private List<ProxyEvent> takeList = new ArrayList<>();
    private List<ProxyEvent> putList = new ArrayList<>();
```

### FieldMayBeFinal
Field `countSemaphore` may be 'final'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/channel/ProxyTransaction.java`
#### Snippet
```java
    public static final Logger LOG = LoggerFactory.getLogger(ProxyTransaction.class);

    private Semaphore countSemaphore;
    private BufferQueue<ProxyEvent> bufferQueue;
    private List<ProxyEvent> takeList = new ArrayList<>();
```

### FieldMayBeFinal
Field `putCounter` may be 'final'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/channel/BufferQueueChannel.java`
#### Snippet
```java
    protected Timer channelTimer;
    private AtomicLong takeCounter = new AtomicLong(0);
    private AtomicLong putCounter = new AtomicLong(0);

    /**
```

### FieldMayBeFinal
Field `currentTransaction` may be 'final'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/channel/BufferQueueChannel.java`
#### Snippet
```java
    private int maxBufferQueueSizeKb;
    private BufferQueue<ProxyEvent> bufferQueue;
    private ThreadLocal<ProxyTransaction> currentTransaction = new ThreadLocal<ProxyTransaction>();
    protected Timer channelTimer;
    private AtomicLong takeCounter = new AtomicLong(0);
```

### FieldMayBeFinal
Field `takeCounter` may be 'final'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/channel/BufferQueueChannel.java`
#### Snippet
```java
    private ThreadLocal<ProxyTransaction> currentTransaction = new ThreadLocal<ProxyTransaction>();
    protected Timer channelTimer;
    private AtomicLong takeCounter = new AtomicLong(0);
    private AtomicLong putCounter = new AtomicLong(0);

```

### FieldMayBeFinal
Field `sourceContext` may be 'final'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/tcp/InlongTcpChannelHandler.java`
#### Snippet
```java
    public static final int VERSION_1 = 1;

    private SourceContext sourceContext;

    /**
```

### FieldMayBeFinal
Field `source` may be 'final'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source2/InLongMessageFactory.java`
#### Snippet
```java
    public static final boolean DEFAULT_FAIL_FAST = true;

    private BaseSource source;

    /**
```

### FieldMayBeFinal
Field `metricItem` may be 'final'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/metrics/prometheus/PrometheusMetricListener.java`
#### Snippet
```java
    protected HTTPServer httpServer;
    private String metricName;
    private DataProxyMetricItem metricItem;
    private Map<String, AtomicLong> metricValueMap = new ConcurrentHashMap<>();
    private Map<String, MetricItemValue> dimensionMetricValueMap = new ConcurrentHashMap<>();
```

### FieldMayBeFinal
Field `dimensionMetricValueMap` may be 'final'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/metrics/prometheus/PrometheusMetricListener.java`
#### Snippet
```java
    private DataProxyMetricItem metricItem;
    private Map<String, AtomicLong> metricValueMap = new ConcurrentHashMap<>();
    private Map<String, MetricItemValue> dimensionMetricValueMap = new ConcurrentHashMap<>();
    private List<String> dimensionKeys = new ArrayList<>();

```

### FieldMayBeFinal
Field `metricValueMap` may be 'final'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/metrics/prometheus/PrometheusMetricListener.java`
#### Snippet
```java
    private String metricName;
    private DataProxyMetricItem metricItem;
    private Map<String, AtomicLong> metricValueMap = new ConcurrentHashMap<>();
    private Map<String, MetricItemValue> dimensionMetricValueMap = new ConcurrentHashMap<>();
    private List<String> dimensionKeys = new ArrayList<>();
```

### FieldMayBeFinal
Field `dimensionKeys` may be 'final'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/metrics/prometheus/PrometheusMetricListener.java`
#### Snippet
```java
    private Map<String, AtomicLong> metricValueMap = new ConcurrentHashMap<>();
    private Map<String, MetricItemValue> dimensionMetricValueMap = new ConcurrentHashMap<>();
    private List<String> dimensionKeys = new ArrayList<>();

    /**
```

### FieldMayBeFinal
Field `metricName` may be 'final'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/metrics/prometheus/PrometheusMetricListener.java`
#### Snippet
```java
    private static final Logger LOG = LoggerFactory.getLogger(PrometheusMetricListener.class);
    protected HTTPServer httpServer;
    private String metricName;
    private DataProxyMetricItem metricItem;
    private Map<String, AtomicLong> metricValueMap = new ConcurrentHashMap<>();
```

### FieldMayBeFinal
Field `monitorIndexExt` may be 'final'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/ServerMessageHandler.java`
#### Snippet
```java
    private MonitorIndex monitorIndex;

    private MonitorIndexExt monitorIndexExt;

    //
```

### FieldMayBeFinal
Field `source` may be 'final'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/ServerMessageHandler.java`
#### Snippet
```java
            .trimResults().withKeyValueSeparator(AttributeConstants.KEY_VALUE_SEPARATOR);

    private BaseSource source;

    private final ChannelGroup allChannels;
```

### FieldMayBeFinal
Field `monitorIndex` may be 'final'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/ServerMessageHandler.java`
#### Snippet
```java
    private final String protocolType;

    private MonitorIndex monitorIndex;

    private MonitorIndexExt monitorIndexExt;
```

### FieldMayBeFinal
Field `ackingCount` may be 'final'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/dispatch/DispatchProfileCallback.java`
#### Snippet
```java
public class DispatchProfileCallback {

    private AtomicInteger ackingCount;
    private SourceCallback callback;

```

### FieldMayBeFinal
Field `callback` may be 'final'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/dispatch/DispatchProfileCallback.java`
#### Snippet
```java

    private AtomicInteger ackingCount;
    private SourceCallback callback;

    /**
```

### FieldMayBeFinal
Field `dispatchTime` may be 'final'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/dispatch/DispatchProfile.java`
#### Snippet
```java
    private long count = 0;
    private long size = 0;
    private long dispatchTime;
    private DispatchProfileCallback callback;
    private boolean isOrder;
```

### FieldMayBeFinal
Field `createTime` may be 'final'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/dispatch/DispatchProfile.java`
#### Snippet
```java
    private final String uid;
    private List<ProxyEvent> events = new ArrayList<>();
    private long createTime = System.currentTimeMillis();
    private long count = 0;
    private long size = 0;
```

### FieldMayBeFinal
Field `source` may be 'final'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/SimpleMessageHandler.java`
#### Snippet
```java
        }
    };
    private BaseSource source;
    private final ChannelGroup allChannels;
    private int maxConnections = Integer.MAX_VALUE;
```

### FieldMayBeFinal
Field `inCounter` may be 'final'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/dispatch/DispatchManager.java`
#### Snippet
```java
    // flag that manager need to output overtime data.
    private AtomicBoolean needOutputOvertimeData = new AtomicBoolean(false);
    private AtomicLong inCounter = new AtomicLong(0);
    private AtomicLong outCounter = new AtomicLong(0);

```

### FieldMayBeFinal
Field `outCounter` may be 'final'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/dispatch/DispatchManager.java`
#### Snippet
```java
    private AtomicBoolean needOutputOvertimeData = new AtomicBoolean(false);
    private AtomicLong inCounter = new AtomicLong(0);
    private AtomicLong outCounter = new AtomicLong(0);

    /**
```

### FieldMayBeFinal
Field `needOutputOvertimeData` may be 'final'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/dispatch/DispatchManager.java`
#### Snippet
```java
    private ConcurrentHashMap<String, DispatchProfile> profileCache = new ConcurrentHashMap<>();
    // flag that manager need to output overtime data.
    private AtomicBoolean needOutputOvertimeData = new AtomicBoolean(false);
    private AtomicLong inCounter = new AtomicLong(0);
    private AtomicLong outCounter = new AtomicLong(0);
```

### FieldMayBeFinal
Field `profileCache` may be 'final'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/dispatch/DispatchManager.java`
#### Snippet
```java
    private final AtomicInteger sendIndex = new AtomicInteger();

    private ConcurrentHashMap<String, DispatchProfile> profileCache = new ConcurrentHashMap<>();
    // flag that manager need to output overtime data.
    private AtomicBoolean needOutputOvertimeData = new AtomicBoolean(false);
```

### FieldMayBeFinal
Field `leftSemaphore` may be 'final'
in `inlong-sort-standalone/sort-standalone-common/src/main/java/org/apache/inlong/sort/standalone/utils/SizeSemaphore.java`
#### Snippet
```java
    private int leftSize = 0;
    private Semaphore sizeSemaphore = null;
    private AtomicInteger leftSemaphore = new AtomicInteger(0);

    /**
```

### FieldMayBeFinal
Field `objectMapper` may be 'final'
in `inlong-sort-standalone/sort-standalone-common/src/main/java/org/apache/inlong/sort/standalone/config/loader/ManagerSortClusterConfigLoader.java`
#### Snippet
```java
    private Context context;
    private CloseableHttpClient httpClient;
    private ObjectMapper objectMapper = new ObjectMapper();
    private String md5;

```

### FieldMayBeFinal
Field `taskMap` may be 'final'
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/SortCluster.java`
#### Snippet
```java

    private Timer reloadTimer;
    private Map<String, SortTask> taskMap = new ConcurrentHashMap<>();
    private List<SortTask> deletingTasks = new ArrayList<>();
    private AdminTask adminTask;
```

### FieldMayBeFinal
Field `deletingTasks` may be 'final'
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/SortCluster.java`
#### Snippet
```java
    private Timer reloadTimer;
    private Map<String, SortTask> taskMap = new ConcurrentHashMap<>();
    private List<SortTask> deletingTasks = new ArrayList<>();
    private AdminTask adminTask;

```

### FieldMayBeFinal
Field `workers` may be 'final'
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/cls/ClsSink.java`
#### Snippet
```java
    private Context parentContext;
    private ClsSinkContext context;
    private List<ClsChannelWorker> workers = new ArrayList<>();

    /**
```

### FieldMayBeFinal
Field `dateFormat` may be 'final'
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/cls/DefaultEvent2LogItemHandler.java`
#### Snippet
```java

    private static final Logger LOG = InlongLoggerFactory.getLogger(DefaultEvent2LogItemHandler.class);
    private SimpleDateFormat dateFormat = new SimpleDateFormat("yyyy-MM-dd HH:mm:ss.SSS");

    /**
```

### FieldMayBeFinal
Field `FORMAT_REPOSITORY` may be 'final'
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/hive/HdfsIdConfig.java`
#### Snippet
```java
    };
    // format repository
    private static ThreadLocal<Map<String, SimpleDateFormat>> FORMAT_REPOSITORY;

    static {
```

### FieldMayBeFinal
Field `FORMAT_MINUTE` may be 'final'
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/hive/HdfsIdConfig.java`
#### Snippet
```java
        }
    };
    private static ThreadLocal<SimpleDateFormat> FORMAT_MINUTE = new ThreadLocal<SimpleDateFormat>() {

        protected SimpleDateFormat initialValue() {
```

### FieldMayBeFinal
Field `FORMAT_HOUR` may be 'final'
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/hive/HdfsIdConfig.java`
#### Snippet
```java
        }
    };
    private static ThreadLocal<SimpleDateFormat> FORMAT_HOUR = new ThreadLocal<SimpleDateFormat>() {

        protected SimpleDateFormat initialValue() {
```

### FieldMayBeFinal
Field `FORMAT_DAY` may be 'final'
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/hive/HdfsIdConfig.java`
#### Snippet
```java
    public static final long HOUR_MS = 60L * 60 * 1000;
    public static final int SEPARATOR_LENGTH = 1;
    private static ThreadLocal<SimpleDateFormat> FORMAT_DAY = new ThreadLocal<SimpleDateFormat>() {

        protected SimpleDateFormat initialValue() {
```

### FieldMayBeFinal
Field `workers` may be 'final'
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/kafka/KafkaFederationSink.java`
#### Snippet
```java
    private Context parentContext;
    private KafkaFederationSinkContext context;
    private List<KafkaFederationWorker> workers = new ArrayList<>();

    /** init and start workers */
```

### FieldMayBeFinal
Field `handler` may be 'final'
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/kafka/KafkaProducerCluster.java`
#### Snippet
```java
    private final String cacheClusterName;
    private LifecycleState state;
    private IEvent2KafkaRecordHandler handler;

    private KafkaProducer<String, byte[]> producer;
```

### FieldMayBeFinal
Field `partitionCreateMap` may be 'final'
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/hive/PartitionLeaderElectionRunnable.java`
#### Snippet
```java
    public static final Logger LOG = InlongLoggerFactory.getLogger(HiveSinkContext.class);
    private final HiveSinkContext context;
    private Map<String, PartitionCreateRunnable> partitionCreateMap = new ConcurrentHashMap<>();

    /**
```

### FieldMayBeFinal
Field `dispatchQueue` may be 'final'
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/hive/HiveSink.java`
#### Snippet
```java
    // message group
    private DispatchManager dispatchManager;
    private LinkedBlockingQueue<DispatchProfile> dispatchQueue = new LinkedBlockingQueue<>();
    // message file
    private Map<String, HdfsIdFile> hdfsIdFileMap = new ConcurrentHashMap<>();
```

### FieldMayBeFinal
Field `hdfsIdFileMap` may be 'final'
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/hive/HiveSink.java`
#### Snippet
```java
    private LinkedBlockingQueue<DispatchProfile> dispatchQueue = new LinkedBlockingQueue<>();
    // message file
    private Map<String, HdfsIdFile> hdfsIdFileMap = new ConcurrentHashMap<>();
    // scheduled thread pool
    // partition leader election runnable
```

### FieldMayBeFinal
Field `deletingClusterList` may be 'final'
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/kafka/KafkaProducerFederation.java`
#### Snippet
```java

    private List<KafkaProducerCluster> clusterList = new ArrayList<>();
    private List<KafkaProducerCluster> deletingClusterList = new ArrayList<>();

    private AtomicInteger clusterIndex = new AtomicInteger(0);
```

### FieldMayBeFinal
Field `reloadInterval` may be 'final'
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/kafka/KafkaProducerFederation.java`
#### Snippet
```java
    private final KafkaFederationSinkContext context;
    private ScheduledExecutorService pool;
    private long reloadInterval;

    private List<KafkaProducerCluster> clusterList = new ArrayList<>();
```

### FieldMayBeFinal
Field `clusterIndex` may be 'final'
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/kafka/KafkaProducerFederation.java`
#### Snippet
```java
    private List<KafkaProducerCluster> deletingClusterList = new ArrayList<>();

    private AtomicInteger clusterIndex = new AtomicInteger(0);

    /**
```

### FieldMayBeFinal
Field `nodeId` may be 'final'
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/hive/HiveSinkContext.java`
#### Snippet
```java

    private Context parentContext;
    private String nodeId;
    private Map<String, HdfsIdConfig> idConfigMap = new ConcurrentHashMap<>();
    private LinkedBlockingQueue<DispatchProfile> dispatchQueue = new LinkedBlockingQueue<>();
```

### FieldMayBeFinal
Field `partitionCreatePool` may be 'final'
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/hive/HiveSinkContext.java`
#### Snippet
```java
    // scheduled thread pool
    // partition create runnable
    private ExecutorService partitionCreatePool;
    private IEventFormatHandler eventFormatHandler;

```

### FieldMayBeFinal
Field `outputPool` may be 'final'
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/hive/HiveSinkContext.java`
#### Snippet
```java
    private String hivePassword;
    // write file thread pool
    private ExecutorService outputPool;
    // scheduled thread pool
    // partition create runnable
```

### FieldMayBeFinal
Field `workers` may be 'final'
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/pulsar/PulsarFederationSink.java`
#### Snippet
```java

    private PulsarFederationSinkContext context;
    private List<PulsarFederationWorker> workers = new ArrayList<>();
    private Map<String, String> dimensions;

```

### FieldMayBeFinal
Field `deletingClusterList` may be 'final'
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/pulsar/PulsarProducerFederation.java`
#### Snippet
```java

    private List<PulsarProducerCluster> clusterList = new ArrayList<>();
    private List<PulsarProducerCluster> deletingClusterList = new ArrayList<>();

    private AtomicInteger clusterIndex = new AtomicInteger(0);
```

### FieldMayBeFinal
Field `clusterIndex` may be 'final'
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/pulsar/PulsarProducerFederation.java`
#### Snippet
```java
    private List<PulsarProducerCluster> deletingClusterList = new ArrayList<>();

    private AtomicInteger clusterIndex = new AtomicInteger(0);

    /**
```

### FieldMayBeFinal
Field `deletingClients` may be 'final'
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/cls/ClsSinkContext.java`
#### Snippet
```java

    private final Map<String, AsyncProducerClient> clientMap;
    private List<AsyncProducerClient> deletingClients = new ArrayList<>();
    private Context sinkContext;
    private Map<String, ClsIdConfig> idConfigMap = new ConcurrentHashMap<>();
```

### FieldMayBeFinal
Field `clientIdIndex` may be 'final'
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/kafka/PartitionerSelector.java`
#### Snippet
```java
     * clientId and number of producers
     */
    private static Map<String, AtomicInteger> clientIdIndex = new ConcurrentHashMap<>();

    /**
```

### FieldMayBeFinal
Field `producerFederation` may be 'final'
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/pulsar/PulsarFederationWorker.java`
#### Snippet
```java
    private final PulsarFederationSinkContext context;

    private PulsarProducerFederation producerFederation;
    private LifecycleState status;

```

### FieldMayBeFinal
Field `workers` may be 'final'
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/elasticsearch/EsSink.java`
#### Snippet
```java
    private EsSinkContext context;
    // workers
    private List<EsChannelWorker> workers = new ArrayList<>();
    // output
    private EsOutputChannel outputChannel;
```

### FieldMayBeFinal
Field `handler` may be 'final'
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/clickhouse/ClickHouseChannelWorker.java`
#### Snippet
```java
    private final int workerIndex;
    private LifecycleState status;
    private IEventHandler handler;
    private Connection conn;

```

### FieldMayBeFinal
Field `dateFormat` may be 'final'
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/clickhouse/DefaultEventHandler.java`
#### Snippet
```java
    public static final String KEY_EXTINFO = "extinfo";

    private SimpleDateFormat dateFormat = new SimpleDateFormat("yyyy-MM-dd HH:mm:ss");

    /**
```

### FieldMayBeFinal
Field `workers` may be 'final'
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/clickhouse/ClickHouseSink.java`
#### Snippet
```java
    private LinkedBlockingQueue<DispatchProfile> dispatchQueue = new LinkedBlockingQueue<>();
    // workers
    private List<ClickHouseChannelWorker> workers = new ArrayList<>();
    // schedule
    private ScheduledExecutorService scheduledPool;
```

### FieldMayBeFinal
Field `dispatchQueue` may be 'final'
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/clickhouse/ClickHouseSink.java`
#### Snippet
```java
    private ClickHouseSinkContext context;
    private DispatchManager dispatchManager;
    private LinkedBlockingQueue<DispatchProfile> dispatchQueue = new LinkedBlockingQueue<>();
    // workers
    private List<ClickHouseChannelWorker> workers = new ArrayList<>();
```

### FieldMayBeFinal
Field `producerMap` may be 'final'
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/pulsar/PulsarProducerCluster.java`
#### Snippet
```java
    private ProducerBuilder<byte[]> baseBuilder;

    private Map<String, Producer<byte[]>> producerMap = new ConcurrentHashMap<>();

    /**
```

### FieldMayBeFinal
Field `handler` may be 'final'
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/pulsar/PulsarProducerCluster.java`
#### Snippet
```java
    private final String cacheClusterName;
    private LifecycleState state;
    private IEvent2PulsarRecordHandler handler;

    /**
```

### FieldMayBeFinal
Field `FORMAT_HOUR` may be 'final'
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/elasticsearch/EsIdConfig.java`
#### Snippet
```java
        }
    };
    private static ThreadLocal<SimpleDateFormat> FORMAT_HOUR = new ThreadLocal<SimpleDateFormat>() {

        protected SimpleDateFormat initialValue() {
```

### FieldMayBeFinal
Field `FORMAT_MINUTE` may be 'final'
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/elasticsearch/EsIdConfig.java`
#### Snippet
```java
        }
    };
    private static ThreadLocal<SimpleDateFormat> FORMAT_MINUTE = new ThreadLocal<SimpleDateFormat>() {

        protected SimpleDateFormat initialValue() {
```

### FieldMayBeFinal
Field `FORMAT_DAY` may be 'final'
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/elasticsearch/EsIdConfig.java`
#### Snippet
```java
    public static final String REGEX_HOUR = "\\{yyyyMMddHH\\}";
    public static final String REGEX_MINUTE = "\\{yyyyMMddHHmm\\}";
    private static ThreadLocal<SimpleDateFormat> FORMAT_DAY = new ThreadLocal<SimpleDateFormat>() {

        protected SimpleDateFormat initialValue() {
```

### FieldMayBeFinal
Field `handler` may be 'final'
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/elasticsearch/EsChannelWorker.java`
#### Snippet
```java

    private LifecycleState status;
    private IEvent2IndexRequestHandler handler;

    /**
```

### FieldMayBeFinal
Field `dateFormat` may be 'final'
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/elasticsearch/DefaultEvent2IndexRequestHandler.java`
#### Snippet
```java
    public static final String KEY_EXTINFO = "extinfo";

    private SimpleDateFormat dateFormat = new SimpleDateFormat("yyyy-MM-dd HH:mm:ss");
    private AtomicLong esIndexIndex = new AtomicLong(System.currentTimeMillis());

```

### FieldMayBeFinal
Field `esIndexIndex` may be 'final'
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/elasticsearch/DefaultEvent2IndexRequestHandler.java`
#### Snippet
```java

    private SimpleDateFormat dateFormat = new SimpleDateFormat("yyyy-MM-dd HH:mm:ss");
    private AtomicLong esIndexIndex = new AtomicLong(System.currentTimeMillis());

    /**
```

### FieldMayBeFinal
Field `context` may be 'final'
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/elasticsearch/EsCallbackListener.java`
#### Snippet
```java
    public static final Logger LOG = InlongLoggerFactory.getLogger(EsCallbackListener.class);

    private EsSinkContext context;

    /**
```

### FieldMayBeFinal
Field `context` may be 'final'
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/elasticsearch/EsOutputChannel.java`
#### Snippet
```java

    private LifecycleState status;
    private EsSinkContext context;
    private RestHighLevelClient esClient;
    private BulkProcessor bulkProcessor;
```

### FieldMayBeFinal
Field `takeCounter` may be 'final'
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/elasticsearch/EsSinkContext.java`
#### Snippet
```java
    private final BufferQueue<EsIndexRequest> dispatchQueue;
    private AtomicLong offerCounter = new AtomicLong(0);
    private AtomicLong takeCounter = new AtomicLong(0);
    private AtomicLong backCounter = new AtomicLong(0);
    // rest client
```

### FieldMayBeFinal
Field `backCounter` may be 'final'
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/elasticsearch/EsSinkContext.java`
#### Snippet
```java
    private AtomicLong offerCounter = new AtomicLong(0);
    private AtomicLong takeCounter = new AtomicLong(0);
    private AtomicLong backCounter = new AtomicLong(0);
    // rest client
    private String username;
```

### FieldMayBeFinal
Field `offerCounter` may be 'final'
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/elasticsearch/EsSinkContext.java`
#### Snippet
```java
    private Map<String, EsIdConfig> idConfigMap = new ConcurrentHashMap<>();
    private final BufferQueue<EsIndexRequest> dispatchQueue;
    private AtomicLong offerCounter = new AtomicLong(0);
    private AtomicLong takeCounter = new AtomicLong(0);
    private AtomicLong backCounter = new AtomicLong(0);
```

### FieldMayBeFinal
Field `takeList` may be 'final'
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/channel/ProfileTransaction.java`
#### Snippet
```java

    private BufferQueue<ProfileEvent> bufferQueue;
    private List<ProfileEvent> takeList = new ArrayList<>();
    private List<ProfileEvent> putList = new ArrayList<>();

```

### FieldMayBeFinal
Field `bufferQueue` may be 'final'
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/channel/ProfileTransaction.java`
#### Snippet
```java
    public static final Logger LOG = InlongLoggerFactory.getLogger(ProfileTransaction.class);

    private BufferQueue<ProfileEvent> bufferQueue;
    private List<ProfileEvent> takeList = new ArrayList<>();
    private List<ProfileEvent> putList = new ArrayList<>();
```

### FieldMayBeFinal
Field `putList` may be 'final'
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/channel/ProfileTransaction.java`
#### Snippet
```java
    private BufferQueue<ProfileEvent> bufferQueue;
    private List<ProfileEvent> takeList = new ArrayList<>();
    private List<ProfileEvent> putList = new ArrayList<>();

    /**
```

### FieldMayBeFinal
Field `putCounter` may be 'final'
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/channel/BufferQueueChannel.java`
#### Snippet
```java
    protected Timer channelTimer;
    private AtomicLong takeCounter = new AtomicLong(0);
    private AtomicLong putCounter = new AtomicLong(0);

    /**
```

### FieldMayBeFinal
Field `bufferQueue` may be 'final'
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/channel/BufferQueueChannel.java`
#### Snippet
```java
    // global buffer size
    private static SizeSemaphore globalBufferQueueSizeKb;
    private BufferQueue<ProfileEvent> bufferQueue;
    private ThreadLocal<ProfileTransaction> currentTransaction = new ThreadLocal<ProfileTransaction>();
    protected Timer channelTimer;
```

### FieldMayBeFinal
Field `takeCounter` may be 'final'
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/channel/BufferQueueChannel.java`
#### Snippet
```java
    private ThreadLocal<ProfileTransaction> currentTransaction = new ThreadLocal<ProfileTransaction>();
    protected Timer channelTimer;
    private AtomicLong takeCounter = new AtomicLong(0);
    private AtomicLong putCounter = new AtomicLong(0);

```

### FieldMayBeFinal
Field `currentTransaction` may be 'final'
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/channel/BufferQueueChannel.java`
#### Snippet
```java
    private static SizeSemaphore globalBufferQueueSizeKb;
    private BufferQueue<ProfileEvent> bufferQueue;
    private ThreadLocal<ProfileTransaction> currentTransaction = new ThreadLocal<ProfileTransaction>();
    protected Timer channelTimer;
    private AtomicLong takeCounter = new AtomicLong(0);
```

### FieldMayBeFinal
Field `createTime` may be 'final'
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/dispatch/DispatchProfile.java`
#### Snippet
```java
    private final String uid;
    private List<ProfileEvent> events = new ArrayList<>();
    private long createTime = System.currentTimeMillis();
    private long count = 0;
    private long size = 0;
```

### FieldMayBeFinal
Field `sortClients` may be 'final'
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/source/sortsdk/SortSdkSource.java`
#### Snippet
```java

    // {@link SortClient}.
    private List<SortClient> sortClients = new ArrayList<>();

    /**
```

### FieldMayBeFinal
Field `inCounter` may be 'final'
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/dispatch/DispatchManager.java`
#### Snippet
```java
    // flag that manager need to output overtime data.
    private AtomicBoolean needOutputOvertimeData = new AtomicBoolean(false);
    private AtomicLong inCounter = new AtomicLong(0);
    private AtomicLong outCounter = new AtomicLong(0);

```

### FieldMayBeFinal
Field `dispatchQueue` may be 'final'
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/dispatch/DispatchManager.java`
#### Snippet
```java
    private final long dispatchAheadTime;
    private final long dispatchDelayTime;
    private LinkedBlockingQueue<DispatchProfile> dispatchQueue;
    private ConcurrentHashMap<String, DispatchProfile> profileCache = new ConcurrentHashMap<>();
    // flag that manager need to output overtime data.
```

### FieldMayBeFinal
Field `outCounter` may be 'final'
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/dispatch/DispatchManager.java`
#### Snippet
```java
    private AtomicBoolean needOutputOvertimeData = new AtomicBoolean(false);
    private AtomicLong inCounter = new AtomicLong(0);
    private AtomicLong outCounter = new AtomicLong(0);

    /**
```

### FieldMayBeFinal
Field `needOutputOvertimeData` may be 'final'
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/dispatch/DispatchManager.java`
#### Snippet
```java
    private ConcurrentHashMap<String, DispatchProfile> profileCache = new ConcurrentHashMap<>();
    // flag that manager need to output overtime data.
    private AtomicBoolean needOutputOvertimeData = new AtomicBoolean(false);
    private AtomicLong inCounter = new AtomicLong(0);
    private AtomicLong outCounter = new AtomicLong(0);
```

### FieldMayBeFinal
Field `profileCache` may be 'final'
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/dispatch/DispatchManager.java`
#### Snippet
```java
    private final long dispatchDelayTime;
    private LinkedBlockingQueue<DispatchProfile> dispatchQueue;
    private ConcurrentHashMap<String, DispatchProfile> profileCache = new ConcurrentHashMap<>();
    // flag that manager need to output overtime data.
    private AtomicBoolean needOutputOvertimeData = new AtomicBoolean(false);
```

### FieldMayBeFinal
Field `parentContext` may be 'final'
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/clickhouse/ClickHouseSinkContext.java`
#### Snippet
```java
    public static final String KEY_EVENT_HANDLER = "clickHouseEventHandler";

    private Context parentContext;
    private String nodeId;
    private Map<String, ClickHouseIdConfig> idConfigMap = new ConcurrentHashMap<>();
```

### FieldMayBeFinal
Field `metricItem` may be 'final'
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/metrics/prometheus/PrometheusMetricListener.java`
#### Snippet
```java

    private String metricName;
    private SortMetricItem metricItem;
    private Map<String, AtomicLong> metricValueMap = new ConcurrentHashMap<>();
    protected HTTPServer httpServer;
```

### FieldMayBeFinal
Field `metricValueMap` may be 'final'
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/metrics/prometheus/PrometheusMetricListener.java`
#### Snippet
```java
    private String metricName;
    private SortMetricItem metricItem;
    private Map<String, AtomicLong> metricValueMap = new ConcurrentHashMap<>();
    protected HTTPServer httpServer;
    private Map<String, MetricItemValue> dimensionMetricValueMap = new ConcurrentHashMap<>();
```

### FieldMayBeFinal
Field `metricName` may be 'final'
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/metrics/prometheus/PrometheusMetricListener.java`
#### Snippet
```java
    public static final String DEFAULT_DIMENSION_LABEL = "dimension";

    private String metricName;
    private SortMetricItem metricItem;
    private Map<String, AtomicLong> metricValueMap = new ConcurrentHashMap<>();
```

### FieldMayBeFinal
Field `dimensionKeys` may be 'final'
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/metrics/prometheus/PrometheusMetricListener.java`
#### Snippet
```java
    protected HTTPServer httpServer;
    private Map<String, MetricItemValue> dimensionMetricValueMap = new ConcurrentHashMap<>();
    private List<String> dimensionKeys = new ArrayList<>();

    /**
```

### FieldMayBeFinal
Field `dimensionMetricValueMap` may be 'final'
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/metrics/prometheus/PrometheusMetricListener.java`
#### Snippet
```java
    private Map<String, AtomicLong> metricValueMap = new ConcurrentHashMap<>();
    protected HTTPServer httpServer;
    private Map<String, MetricItemValue> dimensionMetricValueMap = new ConcurrentHashMap<>();
    private List<String> dimensionKeys = new ArrayList<>();

```

## RuleId[id=JavaReflectionInvocation]
### JavaReflectionInvocation
Argument is not assignable to 'java.lang.Integer'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/redis/src/main/java/org/apache/inlong/sort/redis/common/handler/RedisMapperHandler.java`
#### Snippet
```java
            if (additionalKey != null && ttl != null) {
                return (RedisMapper) redisMapper.getConstructor(Integer.class, String.class)
                        .newInstance(ttl, additionalKey);
            }
            if (additionalKey != null) {
```

### JavaReflectionInvocation
Argument is not assignable to 'java.lang.Integer'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/redis/src/main/java/org/apache/inlong/sort/redis/common/handler/RedisMapperHandler.java`
#### Snippet
```java
            }
            return (RedisMapper) redisMapper.getConstructor(Integer.class)
                    .newInstance(ttl);
        } catch (Exception e) {
            LOGGER.error("create redis mapper failed", e);
```

### JavaReflectionInvocation
One argument is expected
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/utils/MixUtils.java`
#### Snippet
```java
                        if (ctor != null) {
                            if (strExceptionMsgSet.length == 1) {
                                return (Throwable) ctor.newInstance();
                            } else {
                                if (strExceptionMsgSet[0]
```

## RuleId[id=CaughtExceptionImmediatelyRethrown]
### CaughtExceptionImmediatelyRethrown
Caught exception `e` is immediately rethrown
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/zookeeper/ZKUtil.java`
#### Snippet
```java
            createParentPath(zkw, path);
            ZKUtil.createAndWatch(zkw, path, bytes);
        } catch (final Exception e) {
            throw e;
        }
```

## RuleId[id=InstantiationOfUtilityClass]
### InstantiationOfUtilityClass
Instantiation of utility class `InterceptorBuilderFactory`
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/channel/FailoverChannelProcessor.java`
#### Snippet
```java

        // run through and instantiate all the interceptors specified in the Context
        InterceptorBuilderFactory factory = new InterceptorBuilderFactory();
        for (String interceptorName : interceptorNames) {
            Context interceptorContext =
```

### InstantiationOfUtilityClass
Instantiation of utility class `InterceptorBuilderFactory`
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/channel/FailoverChannelProcessor.java`
#### Snippet
```java

        // run through and instantiate all the interceptors specified in the Context
        InterceptorBuilderFactory factory = new InterceptorBuilderFactory();
        for (String interceptorName : interceptorNames) {
            Context interceptorContext =
```

## RuleId[id=SynchronizationOnLocalVariableOrMethodParameter]
### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on method parameter `tcpChannel`
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/network/TcpChannelGroup.java`
#### Snippet
```java
    private void reconnect(TcpChannel tcpChannel) {
        try {
            synchronized (tcpChannel) {
                Channel oldChannel = tcpChannel.getChannel();
                if (oldChannel != null && oldChannel.isOpen()) {
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on method parameter `counterGroup`
in `inlong-common/src/main/java/org/apache/inlong/common/monitor/CounterGroupExt.java`
#### Snippet
```java

    public synchronized void add(CounterGroupExt counterGroup) {
        synchronized (counterGroup) {
            Iterator ite = counterGroup.getCounters().entrySet().iterator();

```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on method parameter `counterGroup`
in `inlong-common/src/main/java/org/apache/inlong/common/monitor/CounterGroup.java`
#### Snippet
```java

    public synchronized void add(CounterGroup counterGroup) {
        synchronized (counterGroup) {
            Iterator iterator = counterGroup.getCounters().entrySet().iterator();

```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on method parameter `commonProperties`
in `inlong-common/src/main/java/org/apache/inlong/common/metric/MetricObserver.java`
#### Snippet
```java
                        "The given prefix does not end with a period (" + prefix + ")");
        Map<String, String> result = Maps.newHashMap();
        synchronized (commonProperties) {
            Iterator var4 = commonProperties.entrySet().iterator();
            while (var4.hasNext()) {
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `info`
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/offset/offsetstorage/ZkOffsetStorage.java`
#### Snippet
```java
            long newOffset = -1;
            long msgId = -1;
            synchronized (info) {
                if (!info.isModified()) {
                    continue;
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `uniqueId`
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/SimpleClientBalanceConsumer.java`
#### Snippet
```java
                        .append("#").append(partitionKey).toString();
        sBuffer.delete(0, sBuffer.length());
        synchronized (uniqueId) {
            registerPartitions(partition, boostrapOffset, result, sBuffer);
        }
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `uniqueId`
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/SimpleClientBalanceConsumer.java`
#### Snippet
```java
                                .append("#").append(partitionKey).toString();
                sBuffer.delete(0, sBuffer.length());
                synchronized (uniqueId) {
                    partition = clientRmtDataCache.getPartitionByKey(partitionKey);
                    if (partition == null) {
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `eventList`
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/nodemanage/nodeconsumer/ConsumerEventManager.java`
#### Snippet
```java
                        disconnectEventMap.get(consumerId);
                if (eventList != null) {
                    synchronized (eventList) {
                        if (CollectionUtils.isNotEmpty(eventList)) {
                            return true;
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `eventList`
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/nodemanage/nodeconsumer/ConsumerEventManager.java`
#### Snippet
```java
        LinkedList<ConsumerEvent> eventList = currentEventMap.get(consumerId);
        if (eventList != null) {
            synchronized (eventList) {
                if (CollectionUtils.isNotEmpty(eventList)) {
                    event = eventList.removeFirst();
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `eventList`
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/nodemanage/nodeconsumer/ConsumerEventManager.java`
#### Snippet
```java
            }
        }
        synchronized (eventList) {
            return eventList.add(event);
        }
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `eventList`
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/nodemanage/nodeconsumer/ConsumerEventManager.java`
#### Snippet
```java
                    currentEventMap.get(consumerId);
            if (eventList != null) {
                synchronized (eventList) {
                    return eventList.peek();
                }
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `eventList`
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/nodemanage/nodeconsumer/ConsumerEventManager.java`
#### Snippet
```java
            }
        }
        synchronized (eventList) {
            return eventList.add(event);
        }
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `idFile`
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/hive/HiveSink.java`
#### Snippet
```java
        for (String key : overtimePathSet) {
            HdfsIdFile idFile = this.hdfsIdFileMap.remove(key);
            synchronized (idFile) {
                idFile.close();
            }
```

## RuleId[id=SpringJavaAutowiredFieldsWarningInspection]
### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-audit/audit-store/src/main/java/org/apache/inlong/audit/db/DruidConfig.java`
#### Snippet
```java
    @Autowired
    protected StoreConfig storeConfig;
    @Autowired
    private DruidDataSourceProperties properties;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-audit/audit-store/src/main/java/org/apache/inlong/audit/db/DruidConfig.java`
#### Snippet
```java
    private static final Logger LOGGER = LoggerFactory.getLogger(DruidConfig.class);

    @Autowired
    protected StoreConfig storeConfig;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-audit/audit-store/src/main/java/org/apache/inlong/audit/service/AuditMsgConsumerServer.java`
#### Snippet
```java
    @Autowired
    private ElasticsearchService esService;
    @Autowired
    private StoreConfig storeConfig;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-audit/audit-store/src/main/java/org/apache/inlong/audit/service/AuditMsgConsumerServer.java`
#### Snippet
```java

    private static final Logger LOG = LoggerFactory.getLogger(AuditMsgConsumerServer.class);
    @Autowired
    private MessageQueueConfig mqConfig;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-audit/audit-store/src/main/java/org/apache/inlong/audit/service/AuditMsgConsumerServer.java`
#### Snippet
```java
    @Autowired
    private StoreConfig storeConfig;
    @Autowired
    private ClickHouseConfig chConfig;
    // ClickHouseService
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-audit/audit-store/src/main/java/org/apache/inlong/audit/service/AuditMsgConsumerServer.java`
#### Snippet
```java
    @Autowired
    private AuditDataDao auditDataDao;
    @Autowired
    private ElasticsearchService esService;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-audit/audit-store/src/main/java/org/apache/inlong/audit/service/AuditMsgConsumerServer.java`
#### Snippet
```java
    @Autowired
    private MessageQueueConfig mqConfig;
    @Autowired
    private AuditDataDao auditDataDao;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-audit/audit-store/src/main/java/org/apache/inlong/audit/service/ElasticsearchService.java`
#### Snippet
```java
    private final Semaphore semaphore = new Semaphore(1);
    private List<ESDataPo> datalist = new ArrayList<>();
    @Autowired
    @Qualifier("restClient")
    private RestHighLevelClient client;
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-audit/audit-store/src/main/java/org/apache/inlong/audit/service/ElasticsearchService.java`
#### Snippet
```java
    private RestHighLevelClient client;

    @Autowired
    private ElasticsearchConfig esConfig;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/TopicBackendWorker.java`
#### Snippet
```java
    private final ScheduledExecutorService workerExecutor;

    @Autowired
    private TopicRepository topicRepository;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/RegionServiceImpl.java`
#### Snippet
```java
    RegionRepository regionRepository;

    @Autowired
    BrokerRepository brokerRepository;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/RegionServiceImpl.java`
#### Snippet
```java

    public static final String DUPLICATE_ENTRY = "duplicate entry";
    @Autowired
    RegionRepository regionRepository;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/RegionServiceImpl.java`
#### Snippet
```java
    MasterRepository masterRepository;

    @Autowired
    BrokerService brokerService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/RegionServiceImpl.java`
#### Snippet
```java
    BrokerRepository brokerRepository;

    @Autowired
    MasterRepository masterRepository;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/MasterServiceImpl.java`
#### Snippet
```java
    private static Gson gson = new Gson();

    @Autowired
    MasterRepository masterRepository;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/BrokerServiceImpl.java`
#### Snippet
```java
    BrokerRepository brokerRepository;

    @Autowired
    MasterService masterService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/BrokerServiceImpl.java`
#### Snippet
```java
public class BrokerServiceImpl implements BrokerService {

    @Autowired
    BrokerRepository brokerRepository;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/NodeServiceImpl.java`
#### Snippet
```java
    private TopicService topicService;

    @Autowired
    private MasterService masterService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/NodeServiceImpl.java`
#### Snippet
```java
    private final TopicBackendWorker worker;

    @Autowired
    private MasterRepository masterRepository;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/NodeServiceImpl.java`
#### Snippet
```java
    private MasterRepository masterRepository;

    @Autowired
    private TopicService topicService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/ClusterServiceImpl.java`
#### Snippet
```java
    ClusterRepository clusterRepository;

    @Autowired
    NodeService nodeService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/ClusterServiceImpl.java`
#### Snippet
```java
    NodeService nodeService;

    @Autowired
    MasterService masterService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/ClusterServiceImpl.java`
#### Snippet
```java
public class ClusterServiceImpl implements ClusterService {

    @Autowired
    ClusterRepository clusterRepository;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/TaskServiceImpl.java`
#### Snippet
```java
    AddTopicExecutor addTopicExecutor;

    @Autowired
    NodeService nodeService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/TaskServiceImpl.java`
#### Snippet
```java
    TopicTaskRepository topicTaskRepository;

    @Autowired
    ClusterService clusterService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/TaskServiceImpl.java`
#### Snippet
```java
    ClusterService clusterService;

    @Autowired
    AddTopicExecutor addTopicExecutor;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/TaskServiceImpl.java`
#### Snippet
```java
    MasterRepository masterRepository;

    @Autowired
    TopicService topicService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/TaskServiceImpl.java`
#### Snippet
```java
    public static final Integer MAX_RELOAD_TIMES = 500;

    @Autowired
    TopicTaskRepository topicTaskRepository;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/TaskServiceImpl.java`
#### Snippet
```java
    TopicService topicService;

    @Autowired
    MasterService masterService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/TaskServiceImpl.java`
#### Snippet
```java
    NodeService nodeService;

    @Autowired
    MasterRepository masterRepository;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/TopicServiceImpl.java`
#### Snippet
```java
    private int brokerWebPort;

    @Autowired
    private MasterService masterService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/TopicServiceImpl.java`
#### Snippet
```java
    private MasterService masterService;

    @Autowired
    private BrokerService brokerService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/controller/node/NodeController.java`
#### Snippet
```java
    private final Gson gson = new Gson();

    @Autowired
    NodeService nodeService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/controller/node/NodeController.java`
#### Snippet
```java
    MasterRepository masterRepository;

    @Autowired
    MasterService masterService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/controller/node/NodeController.java`
#### Snippet
```java
    NodeService nodeService;

    @Autowired
    MasterRepository masterRepository;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/controller/task/TaskController.java`
#### Snippet
```java
public class TaskController {

    @Autowired
    private ClusterService clusterService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/controller/task/TaskController.java`
#### Snippet
```java
    private ClusterService clusterService;

    @Autowired
    private TaskService taskService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/executors/AddTopicExecutor.java`
#### Snippet
```java
    NodeService nodeService;

    @Autowired
    MasterRepository masterRepository;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/executors/AddTopicExecutor.java`
#### Snippet
```java

    public static final int MAX_CONFIG_TOPIC_NUM = 100;
    @Autowired
    NodeService nodeService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/executors/AddTopicExecutor.java`
#### Snippet
```java
    private int maxRetryTimes = 5000;

    @Autowired
    TopicTaskRepository topicTaskRepository;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/executors/AddTopicExecutor.java`
#### Snippet
```java
    TopicService topicService;

    @Autowired
    MasterService masterService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/executors/AddTopicExecutor.java`
#### Snippet
```java
    TopicTaskRepository topicTaskRepository;

    @Autowired
    TopicService topicService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/controller/group/GroupController.java`
#### Snippet
```java
    private Gson gson = new Gson();

    @Autowired
    private MasterService masterService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/controller/group/GroupController.java`
#### Snippet
```java
    private MasterService masterService;

    @Autowired
    private TopicServiceImpl topicService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/controller/topic/TopicWebController.java`
#### Snippet
```java
    private MasterService masterService;

    @Autowired
    private TopicService topicService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/controller/topic/TopicWebController.java`
#### Snippet
```java
public class TopicWebController {

    @Autowired
    private NodeService nodeService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/controller/topic/TopicWebController.java`
#### Snippet
```java
    private Gson gson = new Gson();

    @Autowired
    private MasterService masterService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/controller/region/RegionController.java`
#### Snippet
```java
    RegionService regionService;

    @Autowired
    ClusterService clusterService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/controller/region/RegionController.java`
#### Snippet
```java
    private final Gson gson = new GsonBuilder().serializeNulls().create();

    @Autowired
    RegionService regionService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/controller/cluster/ClusterController.java`
#### Snippet
```java
    private ClusterService clusterService;

    @Autowired
    private MasterService masterService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/controller/cluster/ClusterController.java`
#### Snippet
```java
    private final TubeMQResult result = new TubeMQResult();

    @Autowired
    private ClusterService clusterService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-web/src/main/java/org/apache/inlong/manager/web/auth/InlongShiroImpl.java`
#### Snippet
```java
    private static final String FILTER_NAME_API = "authAPI";

    @Autowired
    private UserService userService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-web/src/main/java/org/apache/inlong/manager/web/controller/InlongSortController.java`
#### Snippet
```java
public class InlongSortController {

    @Autowired
    private SortService sortService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-web/src/main/java/org/apache/inlong/manager/web/controller/AuditController.java`
#### Snippet
```java

    @Lazy
    @Autowired
    private AuditService auditService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-web/src/main/java/org/apache/inlong/manager/web/controller/WorkflowEventController.java`
#### Snippet
```java
public class WorkflowEventController {

    @Autowired
    private WorkflowEventService workflowEventService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-web/src/main/java/org/apache/inlong/manager/web/controller/StreamSourceController.java`
#### Snippet
```java
public class StreamSourceController {

    @Autowired
    StreamSourceService sourceService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-web/src/main/java/org/apache/inlong/manager/web/controller/StreamSinkController.java`
#### Snippet
```java
public class StreamSinkController {

    @Autowired
    private StreamSinkService sinkService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-web/src/main/java/org/apache/inlong/manager/web/controller/AnnoController.java`
#### Snippet
```java
public class AnnoController {

    @Autowired
    private UserService userService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-web/src/main/java/org/apache/inlong/manager/web/controller/StreamTransformController.java`
#### Snippet
```java
public class StreamTransformController {

    @Autowired
    protected StreamTransformService streamTransformService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-web/src/main/java/org/apache/inlong/manager/web/controller/InlongConsumeController.java`
#### Snippet
```java
    @Autowired
    private InlongConsumeService consumeService;
    @Autowired
    private InlongConsumeProcessService consumeProcessService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-web/src/main/java/org/apache/inlong/manager/web/controller/InlongConsumeController.java`
#### Snippet
```java
public class InlongConsumeController {

    @Autowired
    private InlongConsumeService consumeService;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-web/src/main/java/org/apache/inlong/manager/web/controller/UserController.java`
#### Snippet
```java
public class UserController {

    @Autowired
    UserService userService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-web/src/main/java/org/apache/inlong/manager/web/controller/HeartbeatController.java`
#### Snippet
```java
public class HeartbeatController {

    @Autowired
    private HeartbeatService heartbeatService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-web/src/main/java/org/apache/inlong/manager/web/controller/WorkflowApproverController.java`
#### Snippet
```java
public class WorkflowApproverController {

    @Autowired
    private WorkflowApproverService workflowApproverService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-web/src/main/java/org/apache/inlong/manager/web/controller/openapi/DBCollectorController.java`
#### Snippet
```java
public class DBCollectorController {

    @Autowired
    private DBCollectorTaskService taskService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-web/src/main/java/org/apache/inlong/manager/web/controller/openapi/AuditController.java`
#### Snippet
```java
public class AuditController {

    @Autowired
    private InlongClusterService clusterService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-web/src/main/java/org/apache/inlong/manager/web/controller/InlongClusterController.java`
#### Snippet
```java
public class InlongClusterController {

    @Autowired
    private InlongClusterService clusterService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-web/src/main/java/org/apache/inlong/manager/web/controller/DataNodeController.java`
#### Snippet
```java
public class DataNodeController {

    @Autowired
    private DataNodeService dataNodeService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-web/src/main/java/org/apache/inlong/manager/web/controller/WorkflowController.java`
#### Snippet
```java
public class WorkflowController {

    @Autowired
    private WorkflowService workflowService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-web/src/main/java/org/apache/inlong/manager/web/controller/openapi/SortController.java`
#### Snippet
```java
public class SortController {

    @Autowired
    private SortService sortService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-web/src/main/java/org/apache/inlong/manager/web/controller/openapi/DataProxyController.java`
#### Snippet
```java
public class DataProxyController {

    @Autowired
    @Lazy
    private InlongClusterService clusterService;
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-web/src/main/java/org/apache/inlong/manager/web/controller/openapi/DataProxyController.java`
#### Snippet
```java
    @Lazy
    private InlongClusterService clusterService;
    @Autowired
    private DataProxyConfigRepository dataProxyConfigRepository;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-web/src/main/java/org/apache/inlong/manager/web/controller/openapi/OpenStreamSinkController.java`
#### Snippet
```java
public class OpenStreamSinkController {

    @Autowired
    private StreamSinkService sinkService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-web/src/main/java/org/apache/inlong/manager/web/controller/InlongGroupController.java`
#### Snippet
```java
    @Autowired
    private InlongGroupService groupService;
    @Autowired
    private InlongGroupProcessService groupProcessOperation;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-web/src/main/java/org/apache/inlong/manager/web/controller/InlongGroupController.java`
#### Snippet
```java
public class InlongGroupController {

    @Autowired
    private InlongGroupService groupService;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-web/src/main/java/org/apache/inlong/manager/web/controller/openapi/OpenDataNodeController.java`
#### Snippet
```java
public class OpenDataNodeController {

    @Autowired
    private DataNodeService dataNodeService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-web/src/main/java/org/apache/inlong/manager/web/controller/openapi/OpenStreamTransformController.java`
#### Snippet
```java
public class OpenStreamTransformController {

    @Autowired
    protected StreamTransformService streamTransformService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-web/src/main/java/org/apache/inlong/manager/web/controller/InlongStreamController.java`
#### Snippet
```java
public class InlongStreamController {

    @Autowired
    private ObjectMapper objectMapper;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-web/src/main/java/org/apache/inlong/manager/web/controller/InlongStreamController.java`
#### Snippet
```java
    @Autowired
    private InlongStreamService streamService;
    @Autowired
    private InlongStreamProcessService streamProcessOperation;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-web/src/main/java/org/apache/inlong/manager/web/controller/InlongStreamController.java`
#### Snippet
```java
    private ObjectMapper objectMapper;

    @Autowired
    private InlongStreamService streamService;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-web/src/main/java/org/apache/inlong/manager/web/controller/openapi/OpenInLongStreamController.java`
#### Snippet
```java
public class OpenInLongStreamController {

    @Autowired
    private InlongStreamService streamService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-web/src/main/java/org/apache/inlong/manager/web/controller/openapi/OpenHeartbeatController.java`
#### Snippet
```java
public class OpenHeartbeatController {

    @Autowired
    private HeartbeatService heartbeatService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-web/src/main/java/org/apache/inlong/manager/web/controller/openapi/AgentController.java`
#### Snippet
```java
    @Autowired
    private AgentService agentService;
    @Autowired
    private InlongClusterService clusterService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-web/src/main/java/org/apache/inlong/manager/web/controller/openapi/AgentController.java`
#### Snippet
```java
public class AgentController {

    @Autowired
    private AgentService agentService;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-web/src/main/java/org/apache/inlong/manager/web/controller/openapi/OpenInLongGroupController.java`
#### Snippet
```java
public class OpenInLongGroupController {

    @Autowired
    private InlongGroupService groupService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-web/src/main/java/org/apache/inlong/manager/web/controller/openapi/OpenInLongGroupController.java`
#### Snippet
```java
    private InlongGroupService groupService;

    @Autowired
    private InlongGroupProcessService groupProcessOperation;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-web/src/main/java/org/apache/inlong/manager/web/controller/openapi/OpenStreamSourceController.java`
#### Snippet
```java
public class OpenStreamSourceController {

    @Autowired
    StreamSourceService sourceService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-web/src/main/java/org/apache/inlong/manager/web/controller/openapi/OpenInLongClusterController.java`
#### Snippet
```java
public class OpenInLongClusterController {

    @Autowired
    private InlongClusterService clusterService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/core/impl/RoleServiceImpl.java`
#### Snippet
```java
public class RoleServiceImpl implements RoleService {

    @Autowired
    private UserRoleEntityMapper userRoleEntityMapper;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/core/impl/DBCollectorTaskServiceImpl.java`
#### Snippet
```java
    private static final int RETURN_INVALID_STATE = 3;

    @Autowired
    private DBCollectorDetailTaskMapper detailTaskMapper;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/core/impl/SortConfigLoaderImpl.java`
#### Snippet
```java
    @Autowired
    private StreamSinkEntityMapper streamSinkEntityMapper;
    @Autowired
    private StreamSinkFieldEntityMapper streamSinkFieldEntityMapper;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/core/impl/SortConfigLoaderImpl.java`
#### Snippet
```java
public class SortConfigLoaderImpl implements SortConfigLoader {

    @Autowired
    private InlongClusterEntityMapper clusterEntityMapper;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/core/impl/SortConfigLoaderImpl.java`
#### Snippet
```java
    @Autowired
    private InlongClusterEntityMapper clusterEntityMapper;
    @Autowired
    private StreamSinkEntityMapper streamSinkEntityMapper;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/core/impl/SortConfigLoaderImpl.java`
#### Snippet
```java
    @Autowired
    private InlongStreamExtEntityMapper inlongStreamExtEntityMapper;
    @Autowired
    private InlongStreamEntityMapper inlongStreamEntityMapper;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/core/impl/SortConfigLoaderImpl.java`
#### Snippet
```java
    @Autowired
    private InlongStreamEntityMapper inlongStreamEntityMapper;
    @Autowired
    private DataNodeEntityMapper dataNodeEntityMapper;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/core/impl/SortConfigLoaderImpl.java`
#### Snippet
```java
    @Autowired
    private InlongGroupExtEntityMapper inlongGroupExtEntityMapper;
    @Autowired
    private InlongStreamExtEntityMapper inlongStreamExtEntityMapper;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/core/impl/SortConfigLoaderImpl.java`
#### Snippet
```java
    @Autowired
    private InlongGroupEntityMapper inlongGroupEntityMapper;
    @Autowired
    private InlongGroupExtEntityMapper inlongGroupExtEntityMapper;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/core/impl/SortConfigLoaderImpl.java`
#### Snippet
```java
    @Autowired
    private StreamSinkFieldEntityMapper streamSinkFieldEntityMapper;
    @Autowired
    private InlongGroupEntityMapper inlongGroupEntityMapper;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/core/impl/SortServiceImpl.java`
#### Snippet
```java

    @Lazy
    @Autowired
    private SortSourceService sortSourceService;
    @Lazy
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/core/impl/SortServiceImpl.java`
#### Snippet
```java
    @Autowired
    private SortClusterService sortClusterService;
    @Autowired
    private InlongGroupService groupService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/core/impl/SortServiceImpl.java`
#### Snippet
```java
    private SortSourceService sortSourceService;
    @Lazy
    @Autowired
    private SortClusterService sortClusterService;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/core/impl/WorkflowEventServiceImpl.java`
#### Snippet
```java
    @Autowired
    private WorkflowQueryService queryService;
    @Autowired
    private EventListenerService eventListenerService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/core/impl/WorkflowEventServiceImpl.java`
#### Snippet
```java
public class WorkflowEventServiceImpl implements WorkflowEventService {

    @Autowired
    private WorkflowQueryService queryService;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/node/DataNodeOperateHelper.java`
#### Snippet
```java
    @Autowired
    private DataNodeEntityMapper dataNodeMapper;
    @Autowired
    private DataNodeOperatorFactory operatorFactory;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/node/DataNodeOperateHelper.java`
#### Snippet
```java
public class DataNodeOperateHelper {

    @Autowired
    private DataNodeEntityMapper dataNodeMapper;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/core/impl/WorkflowApproverServiceImpl.java`
#### Snippet
```java
    @Autowired
    private WorkflowApproverEntityMapper approverMapper;
    @Autowired
    private ProcessDefinitionService processDefinitionService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/core/impl/WorkflowApproverServiceImpl.java`
#### Snippet
```java
    private static final Logger LOGGER = LoggerFactory.getLogger(WorkflowApproverServiceImpl.class);

    @Autowired
    private WorkflowApproverEntityMapper approverMapper;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/node/DataNodeOperatorFactory.java`
#### Snippet
```java
public class DataNodeOperatorFactory {

    @Autowired
    private List<DataNodeOperator> dataNodeOperatorList;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/node/ck/ClickHouseDataNodeOperator.java`
#### Snippet
```java
    private static final Logger LOGGER = LoggerFactory.getLogger(ClickHouseDataNodeOperator.class);

    @Autowired
    private ObjectMapper objectMapper;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/node/es/ElasticsearchDataNodeOperator.java`
#### Snippet
```java
    private static final Logger LOGGER = LoggerFactory.getLogger(ElasticsearchDataNodeOperator.class);

    @Autowired
    private ObjectMapper objectMapper;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/node/hive/HiveDataNodeOperator.java`
#### Snippet
```java
    private static final Logger LOGGER = LoggerFactory.getLogger(HiveDataNodeOperator.class);

    @Autowired
    private ObjectMapper objectMapper;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/node/hudi/HudiDataNodeOperator.java`
#### Snippet
```java
    private static final Logger LOGGER = LoggerFactory.getLogger(HudiDataNodeOperator.class);

    @Autowired
    private ObjectMapper objectMapper;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/node/kudu/KuduDataNodeOperator.java`
#### Snippet
```java
    private static final Logger LOGGER = LoggerFactory.getLogger(KuduDataNodeOperator.class);

    @Autowired
    private ObjectMapper objectMapper;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/node/redis/RedisDataNodeOperator.java`
#### Snippet
```java
    private static final Logger LOGGER = LoggerFactory.getLogger(RedisDataNodeOperator.class);

    @Autowired
    private ObjectMapper objectMapper;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/node/iceberg/IcebergDataNodeOperator.java`
#### Snippet
```java
    private static final Logger LOGGER = LoggerFactory.getLogger(IcebergDataNodeOperator.class);

    @Autowired
    private ObjectMapper objectMapper;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/node/mysql/MySQLDataNodeOperator.java`
#### Snippet
```java
    private static final Logger LOGGER = LoggerFactory.getLogger(MySQLDataNodeOperator.class);

    @Autowired
    private ObjectMapper objectMapper;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/node/starrocks/StarRocksDataNodeOperator.java`
#### Snippet
```java
    private static final Logger LOGGER = LoggerFactory.getLogger(StarRocksDataNodeOperator.class);

    @Autowired
    private ObjectMapper objectMapper;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/node/DataNodeServiceImpl.java`
#### Snippet
```java
    private static final Logger LOGGER = LoggerFactory.getLogger(DataNodeServiceImpl.class);

    @Autowired
    private DataNodeEntityMapper dataNodeMapper;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/node/DataNodeServiceImpl.java`
#### Snippet
```java
    @Autowired
    private DataNodeOperatorFactory operatorFactory;
    @Autowired
    private UserService userService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/node/DataNodeServiceImpl.java`
#### Snippet
```java
    @Autowired
    private DataNodeEntityMapper dataNodeMapper;
    @Autowired
    private DataNodeOperatorFactory operatorFactory;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/node/postgresql/PostgreSQLDataNodeOperator.java`
#### Snippet
```java
    private static final Logger LOGGER = LoggerFactory.getLogger(PostgreSQLDataNodeOperator.class);

    @Autowired
    private ObjectMapper objectMapper;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/core/impl/SortClusterServiceImpl.java`
#### Snippet
```java
    private long reloadInterval;

    @Autowired
    private SortConfigLoader sortConfigLoader;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/core/impl/SortClusterServiceImpl.java`
#### Snippet
```java
    @Autowired
    private SinkOperatorFactory sinkOperatorFactory;
    @Autowired
    private DataNodeOperatorFactory dataNodeOperatorFactory;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/core/impl/SortClusterServiceImpl.java`
#### Snippet
```java
    @Autowired
    private SortConfigLoader sortConfigLoader;
    @Autowired
    private SinkOperatorFactory sinkOperatorFactory;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/core/impl/AgentServiceImpl.java`
#### Snippet
```java
    @Autowired
    private InlongGroupEntityMapper groupMapper;
    @Autowired
    private InlongStreamEntityMapper streamMapper;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/core/impl/AgentServiceImpl.java`
#### Snippet
```java
    @Autowired
    private DataSourceCmdConfigEntityMapper sourceCmdConfigMapper;
    @Autowired
    private InlongGroupEntityMapper groupMapper;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/core/impl/AgentServiceImpl.java`
#### Snippet
```java
    @Autowired
    private InlongClusterEntityMapper clusterMapper;
    @Autowired
    private InlongClusterNodeEntityMapper clusterNodeMapper;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/core/impl/AgentServiceImpl.java`
#### Snippet
```java
    @Autowired
    private SourceSnapshotOperator snapshotOperator;
    @Autowired
    private DataSourceCmdConfigEntityMapper sourceCmdConfigMapper;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/core/impl/AgentServiceImpl.java`
#### Snippet
```java
    @Autowired
    private InlongStreamEntityMapper streamMapper;
    @Autowired
    private InlongClusterEntityMapper clusterMapper;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/core/impl/AgentServiceImpl.java`
#### Snippet
```java
    @Autowired
    private StreamSourceEntityMapper sourceMapper;
    @Autowired
    private SourceSnapshotOperator snapshotOperator;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/core/impl/AgentServiceImpl.java`
#### Snippet
```java
    @Value("${source.cleansing.interval:600}")
    private Integer cleanInterval;
    @Autowired
    private StreamSourceEntityMapper sourceMapper;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/sink/SinkOperatorFactory.java`
#### Snippet
```java
public class SinkOperatorFactory {

    @Autowired
    private List<StreamSinkOperator> sinkOperatorList;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/sink/hdfs/HDFSSinkOperator.java`
#### Snippet
```java
    private static final Logger LOGGER = LoggerFactory.getLogger(HDFSSinkOperator.class);

    @Autowired
    private ObjectMapper objectMapper;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/sink/hive/HiveSinkOperator.java`
#### Snippet
```java
    private static final Logger LOGGER = LoggerFactory.getLogger(HiveSinkOperator.class);

    @Autowired
    private ObjectMapper objectMapper;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/sink/es/ElasticsearchSinkOperator.java`
#### Snippet
```java
    private static final String KEY_FIELDS = "fieldNames";

    @Autowired
    private ObjectMapper objectMapper;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/sink/ck/ClickHouseSinkOperator.java`
#### Snippet
```java
    private static final Logger LOGGER = LoggerFactory.getLogger(ClickHouseSinkOperator.class);

    @Autowired
    private ObjectMapper objectMapper;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/sink/hbase/HBaseSinkOperator.java`
#### Snippet
```java
    private static final Logger LOGGER = LoggerFactory.getLogger(HBaseSinkOperator.class);

    @Autowired
    private ObjectMapper objectMapper;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/sink/doris/DorisSinkOperator.java`
#### Snippet
```java
    private static final Logger LOGGER = LoggerFactory.getLogger(DorisSinkOperator.class);

    @Autowired
    private ObjectMapper objectMapper;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/sink/hudi/HudiSinkOperator.java`
#### Snippet
```java
    private static final String CATALOG_TYPE_HIVE = "HIVE";

    @Autowired
    private ObjectMapper objectMapper;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/sink/kudu/KuduSinkOperator.java`
#### Snippet
```java
    private static final Logger LOGGER = LoggerFactory.getLogger(KuduSinkOperator.class);

    @Autowired
    private ObjectMapper objectMapper;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/core/impl/AuditServiceImpl.java`
#### Snippet
```java
    @Autowired
    private AuditEntityMapper auditEntityMapper;
    @Autowired
    private ElasticsearchApi elasticsearchApi;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/core/impl/AuditServiceImpl.java`
#### Snippet
```java
    private String auditQuerySource = AuditQuerySource.MYSQL.name();

    @Autowired
    private AuditBaseEntityMapper auditBaseMapper;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/core/impl/AuditServiceImpl.java`
#### Snippet
```java
    @Autowired
    private StreamSinkEntityMapper sinkEntityMapper;
    @Autowired
    private StreamSourceEntityMapper sourceEntityMapper;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/core/impl/AuditServiceImpl.java`
#### Snippet
```java
    @Autowired
    private ElasticsearchApi elasticsearchApi;
    @Autowired
    private StreamSinkEntityMapper sinkEntityMapper;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/core/impl/AuditServiceImpl.java`
#### Snippet
```java
    @Autowired
    private AuditBaseEntityMapper auditBaseMapper;
    @Autowired
    private AuditEntityMapper auditEntityMapper;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/sink/kafka/KafkaSinkOperator.java`
#### Snippet
```java
    private static final Logger LOGGER = LoggerFactory.getLogger(KafkaSinkOperator.class);

    @Autowired
    private ObjectMapper objectMapper;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/sink/mysql/MySQLSinkOperator.java`
#### Snippet
```java
    private static final Logger LOGGER = LoggerFactory.getLogger(MySQLSinkOperator.class);

    @Autowired
    private ObjectMapper objectMapper;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/sink/oracle/OracleSinkOperator.java`
#### Snippet
```java
    private static final Logger LOGGER = LoggerFactory.getLogger(OracleSinkOperator.class);

    @Autowired
    private ObjectMapper objectMapper;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/sink/sqlserver/SQLServerSinkOperator.java`
#### Snippet
```java
    private static final Logger LOGGER = LoggerFactory.getLogger(SQLServerSinkOperator.class);

    @Autowired
    private ObjectMapper objectMapper;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/sink/iceberg/IcebergSinkOperator.java`
#### Snippet
```java
    private static final String CATALOG_TYPE_HIVE = "HIVE";

    @Autowired
    private ObjectMapper objectMapper;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/sink/redis/RedisSinkOperator.java`
#### Snippet
```java
    private static final int PORT_MAX_VALUE = 65535;

    @Autowired
    private ObjectMapper objectMapper;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/sink/postgresql/TDSQLPostgreSQLSinkOperator.java`
#### Snippet
```java
    @Autowired
    private ObjectMapper objectMapper;
    @Autowired
    private StreamSinkFieldEntityMapper sinkFieldMapper;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/sink/postgresql/TDSQLPostgreSQLSinkOperator.java`
#### Snippet
```java
    private static final Logger LOGGER = LoggerFactory.getLogger(TDSQLPostgreSQLSinkOperator.class);

    @Autowired
    private ObjectMapper objectMapper;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/sink/postgresql/PostgreSQLSinkOperator.java`
#### Snippet
```java
    private static final Logger LOGGER = LoggerFactory.getLogger(PostgreSQLSinkOperator.class);

    @Autowired
    private ObjectMapper objectMapper;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/core/impl/SortSourceServiceImpl.java`
#### Snippet
```java
    private Map<String, Map<String, List<SortSourceStreamSinkInfo>>> streamSinkMap;

    @Autowired
    private SortConfigLoader configLoader;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/task/DataCleansingTask.java`
#### Snippet
```java
    private StreamTransformFieldEntityMapper streamTransformFieldMapper;

    @Autowired
    private WorkflowProcessEntityMapper workflowProcessMapper;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/task/DataCleansingTask.java`
#### Snippet
```java
    @Autowired
    private InlongStreamExtEntityMapper streamExtMapper;
    @Autowired
    private InlongStreamFieldEntityMapper streamFieldMapper;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/task/DataCleansingTask.java`
#### Snippet
```java
    @Autowired
    private WorkflowTaskEntityMapper workflowTaskMapper;
    @Autowired
    private WorkflowEventLogEntityMapper workflowEventLogMapper;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/task/DataCleansingTask.java`
#### Snippet
```java
    private Integer batchSize;

    @Autowired
    private InlongGroupEntityMapper groupMapper;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/task/DataCleansingTask.java`
#### Snippet
```java
    @Autowired
    private StreamTransformEntityMapper streamTransformMapper;
    @Autowired
    private StreamTransformFieldEntityMapper streamTransformFieldMapper;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/task/DataCleansingTask.java`
#### Snippet
```java
    @Autowired
    private StreamSourceFieldEntityMapper streamSourceFieldMapper;
    @Autowired
    private StreamTransformEntityMapper streamTransformMapper;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/task/DataCleansingTask.java`
#### Snippet
```java
    @Autowired
    private StreamSinkEntityMapper streamSinkMapper;
    @Autowired
    private StreamSinkFieldEntityMapper streamSinkFieldMapper;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/task/DataCleansingTask.java`
#### Snippet
```java
    @Autowired
    private InlongGroupExtEntityMapper groupExtMapper;
    @Autowired
    private InlongStreamEntityMapper streamMapper;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/task/DataCleansingTask.java`
#### Snippet
```java
    @Autowired
    private StreamSinkFieldEntityMapper streamSinkFieldMapper;
    @Autowired
    private StreamSourceEntityMapper streamSourceMapper;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/task/DataCleansingTask.java`
#### Snippet
```java
    @Autowired
    private InlongStreamEntityMapper streamMapper;
    @Autowired
    private InlongStreamExtEntityMapper streamExtMapper;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/task/DataCleansingTask.java`
#### Snippet
```java
    @Autowired
    private StreamSourceEntityMapper streamSourceMapper;
    @Autowired
    private StreamSourceFieldEntityMapper streamSourceFieldMapper;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/task/DataCleansingTask.java`
#### Snippet
```java
    @Autowired
    private InlongGroupEntityMapper groupMapper;
    @Autowired
    private InlongGroupExtEntityMapper groupExtMapper;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/task/DataCleansingTask.java`
#### Snippet
```java
    @Autowired
    private WorkflowProcessEntityMapper workflowProcessMapper;
    @Autowired
    private WorkflowTaskEntityMapper workflowTaskMapper;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/task/DataCleansingTask.java`
#### Snippet
```java
    @Autowired
    private InlongStreamFieldEntityMapper streamFieldMapper;
    @Autowired
    private StreamSinkEntityMapper streamSinkMapper;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/sink/greenplum/GreenplumSinkOperator.java`
#### Snippet
```java
    private static final Logger LOGGER = LoggerFactory.getLogger(GreenplumSinkOperator.class);

    @Autowired
    private ObjectMapper objectMapper;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/sink/starrocks/StarRocksSinkOperator.java`
#### Snippet
```java
    private static final Logger LOGGER = LoggerFactory.getLogger(StarRocksSinkOperator.class);

    @Autowired
    private ObjectMapper objectMapper;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/sink/StreamSinkServiceImpl.java`
#### Snippet
```java
    @Autowired
    private StreamSinkEntityMapper sinkMapper;
    @Autowired
    private StreamSinkFieldEntityMapper sinkFieldMapper;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/sink/StreamSinkServiceImpl.java`
#### Snippet
```java
    @Autowired
    private AutowireCapableBeanFactory autowireCapableBeanFactory;
    @Autowired
    private UserService userService;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/sink/StreamSinkServiceImpl.java`
#### Snippet
```java
    @Autowired
    private InlongGroupEntityMapper groupMapper;
    @Autowired
    private StreamSinkEntityMapper sinkMapper;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/sink/StreamSinkServiceImpl.java`
#### Snippet
```java
    @Autowired
    private SinkOperatorFactory operatorFactory;
    @Autowired
    private GroupCheckService groupCheckService;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/sink/StreamSinkServiceImpl.java`
#### Snippet
```java
    @Autowired
    private UserService userService;
    @Autowired
    private ObjectMapper objectMapper;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/sink/StreamSinkServiceImpl.java`
#### Snippet
```java
    private static final int PARSE_FIELD_CSV_MIN_COLUMNS = 2;

    @Autowired
    private SinkOperatorFactory operatorFactory;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/sink/StreamSinkServiceImpl.java`
#### Snippet
```java
    @Autowired
    private GroupCheckService groupCheckService;
    @Autowired
    private InlongStreamEntityMapper streamMapper;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/sink/StreamSinkServiceImpl.java`
#### Snippet
```java
    @Autowired
    private StreamSinkFieldEntityMapper sinkFieldMapper;
    @Autowired
    private AutowireCapableBeanFactory autowireCapableBeanFactory;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/sink/StreamSinkServiceImpl.java`
#### Snippet
```java
    @Autowired
    private InlongStreamEntityMapper streamMapper;
    @Autowired
    private InlongGroupEntityMapper groupMapper;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/group/GroupCheckService.java`
#### Snippet
```java
public class GroupCheckService {

    @Autowired
    private InlongGroupEntityMapper groupMapper;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/group/GroupCheckService.java`
#### Snippet
```java
    @Autowired
    private InlongGroupEntityMapper groupMapper;
    @Autowired
    private UserEntityMapper userMapper;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/group/InlongGroupOperatorFactory.java`
#### Snippet
```java
public class InlongGroupOperatorFactory {

    @Autowired
    private List<InlongGroupOperator> groupOperatorList;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/source/SourceOperatorFactory.java`
#### Snippet
```java
public class SourceOperatorFactory {

    @Autowired
    private List<StreamSourceOperator> sourceOperatorList;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/group/InlongGroupProcessService.java`
#### Snippet
```java
    @Autowired
    private WorkflowService workflowService;
    @Autowired
    private InlongStreamService streamService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/group/InlongGroupProcessService.java`
#### Snippet
```java
            new CallerRunsPolicy());

    @Autowired
    private InlongGroupEntityMapper groupMapper;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/group/InlongGroupProcessService.java`
#### Snippet
```java
    @Autowired
    private InlongGroupService groupService;
    @Autowired
    private WorkflowQueryService workflowQueryService;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/group/InlongGroupProcessService.java`
#### Snippet
```java
    @Autowired
    private WorkflowQueryService workflowQueryService;
    @Autowired
    private WorkflowService workflowService;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/group/InlongGroupProcessService.java`
#### Snippet
```java
    @Autowired
    private InlongGroupEntityMapper groupMapper;
    @Autowired
    private InlongGroupService groupService;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/plugin/PluginService.java`
#### Snippet
```java

    @Getter
    @Autowired
    private List<PluginBinder> pluginBinders;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/source/mqtt/MqttSourceOperator.java`
#### Snippet
```java
public class MqttSourceOperator extends AbstractSourceOperator {

    @Autowired
    private ObjectMapper objectMapper;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/source/hudi/HudiSourceOperator.java`
#### Snippet
```java
public class HudiSourceOperator extends AbstractSourceOperator {

    @Autowired
    private ObjectMapper objectMapper;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/source/file/FileSourceOperator.java`
#### Snippet
```java
public class FileSourceOperator extends AbstractSourceOperator {

    @Autowired
    private ObjectMapper objectMapper;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/source/file/FileSourceOperator.java`
#### Snippet
```java
    private ObjectMapper objectMapper;

    @Autowired
    private StreamSourceEntityMapper sourceMapper;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/source/redis/RedisSourceOperator.java`
#### Snippet
```java
public class RedisSourceOperator extends AbstractSourceOperator {

    @Autowired
    private ObjectMapper objectMapper;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/source/binlog/BinlogSourceOperator.java`
#### Snippet
```java
public class BinlogSourceOperator extends AbstractSourceOperator {

    @Autowired
    private ObjectMapper objectMapper;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/source/kafka/KafkaSourceOperator.java`
#### Snippet
```java
public class KafkaSourceOperator extends AbstractSourceOperator {

    @Autowired
    private ObjectMapper objectMapper;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/source/kafka/KafkaSourceOperator.java`
#### Snippet
```java
    @Autowired
    private ObjectMapper objectMapper;
    @Autowired
    private InlongClusterService clusterService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/source/oracle/OracleSourceOperator.java`
#### Snippet
```java
public class OracleSourceOperator extends AbstractSourceOperator {

    @Autowired
    private ObjectMapper objectMapper;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/source/SourceSnapshotOperator.java`
#### Snippet
```java
            new CallerRunsPolicy());

    @Autowired
    private StreamSourceEntityMapper sourceMapper;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/source/mongodb/MongoDBSourceOperator.java`
#### Snippet
```java
public class MongoDBSourceOperator extends AbstractSourceOperator {

    @Autowired
    private ObjectMapper objectMapper;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/source/sqlserver/SQLServerSourceOperator.java`
#### Snippet
```java
public class SQLServerSourceOperator extends AbstractSourceOperator {

    @Autowired
    private ObjectMapper objectMapper;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/user/UserServiceImpl.java`
#### Snippet
```java
    @Autowired
    private InlongClusterTagEntityMapper clusterTagMapper;
    @Autowired
    private DataNodeEntityMapper dataNodeMapper;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/user/UserServiceImpl.java`
#### Snippet
```java
    @Autowired
    private InlongGroupEntityMapper groupMapper;
    @Autowired
    private InlongClusterEntityMapper clusterMapper;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/user/UserServiceImpl.java`
#### Snippet
```java
    @Autowired
    private DataNodeEntityMapper dataNodeMapper;
    @Autowired
    private InlongConsumeEntityMapper consumeMapper;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/user/UserServiceImpl.java`
#### Snippet
```java
    @Autowired
    private InlongClusterEntityMapper clusterMapper;
    @Autowired
    private InlongClusterTagEntityMapper clusterTagMapper;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/user/UserServiceImpl.java`
#### Snippet
```java
    @Autowired
    private UserEntityMapper userMapper;
    @Autowired
    private InlongGroupEntityMapper groupMapper;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/user/UserServiceImpl.java`
#### Snippet
```java
    private final Map<String, UserLoginLockStatus> loginLockStatusMap = new ConcurrentHashMap<>();

    @Autowired
    private UserEntityMapper userMapper;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/source/tubemq/TubeMQSourceOperator.java`
#### Snippet
```java
public class TubeMQSourceOperator extends AbstractSourceOperator {

    @Autowired
    private ObjectMapper objectMapper;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/source/tubemq/TubeMQSourceOperator.java`
#### Snippet
```java
    @Autowired
    private ObjectMapper objectMapper;
    @Autowired
    private InlongClusterService clusterService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/source/autopush/AutoPushSourceOperator.java`
#### Snippet
```java
    private static final Logger LOGGER = LoggerFactory.getLogger(AutoPushSourceOperator.class);

    @Autowired
    private ObjectMapper objectMapper;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/source/pulsar/PulsarSourceOperator.java`
#### Snippet
```java
    @Autowired
    private ObjectMapper objectMapper;
    @Autowired
    private InlongClusterService clusterService;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/source/pulsar/PulsarSourceOperator.java`
#### Snippet
```java
    @Autowired
    private InlongClusterService clusterService;
    @Autowired
    private StreamSinkEntityMapper sinkMapper;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/source/pulsar/PulsarSourceOperator.java`
#### Snippet
```java
    private static final String AUTH_PARAMS_VALUE = "token:%s";

    @Autowired
    private ObjectMapper objectMapper;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/source/postgresql/PostgreSQLSourceOperator.java`
#### Snippet
```java
public class PostgreSQLSourceOperator extends AbstractSourceOperator {

    @Autowired
    private ObjectMapper objectMapper;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/group/InlongGroupServiceImpl.java`
#### Snippet
```java
    private InlongClusterService clusterService;

    @Autowired
    private InlongGroupOperatorFactory groupOperatorFactory;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/group/InlongGroupServiceImpl.java`
#### Snippet
```java
    @Autowired
    private InlongGroupEntityMapper groupMapper;
    @Autowired
    private InlongGroupExtEntityMapper groupExtMapper;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/group/InlongGroupServiceImpl.java`
#### Snippet
```java
    @Autowired
    private InlongGroupExtEntityMapper groupExtMapper;
    @Autowired
    private InlongStreamService streamService;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/group/InlongGroupServiceImpl.java`
#### Snippet
```java
    @Autowired
    private StreamSourceEntityMapper streamSourceMapper;
    @Autowired
    private InlongClusterService clusterService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/group/InlongGroupServiceImpl.java`
#### Snippet
```java
    private boolean enableZookeeper;

    @Autowired
    private InlongGroupEntityMapper groupMapper;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/group/InlongGroupServiceImpl.java`
#### Snippet
```java
    @Autowired
    private InlongStreamService streamService;
    @Autowired
    private StreamSourceEntityMapper streamSourceMapper;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/group/InlongGroupServiceImpl.java`
#### Snippet
```java
    @Autowired
    private InlongGroupOperatorFactory groupOperatorFactory;
    @Autowired
    private SourceOperatorFactory sourceOperatorFactory;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/group/InlongGroupServiceImpl.java`
#### Snippet
```java
    @Autowired
    private SourceOperatorFactory sourceOperatorFactory;
    @Autowired
    private UserService userService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/cluster/InlongClusterOperatorFactory.java`
#### Snippet
```java
public class InlongClusterOperatorFactory {

    @Autowired
    private List<InlongClusterOperator> clusterOperatorList;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/cluster/ElasticsearchClusterOperator.java`
#### Snippet
```java
public class ElasticsearchClusterOperator extends AbstractClusterOperator {

    @Autowired
    private ObjectMapper mapper;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/cluster/AgentClusterOperator.java`
#### Snippet
```java
    private static final Logger LOGGER = LoggerFactory.getLogger(AgentClusterOperator.class);

    @Autowired
    private ObjectMapper objectMapper;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/cluster/TubeClusterOperator.java`
#### Snippet
```java
    private static final Logger LOGGER = LoggerFactory.getLogger(InlongGroupOperator4NoneMQ.class);

    @Autowired
    private ObjectMapper objectMapper;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/source/StreamSourceServiceImpl.java`
#### Snippet
```java
    @Autowired
    private SourceOperatorFactory operatorFactory;
    @Autowired
    private InlongGroupEntityMapper groupMapper;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/source/StreamSourceServiceImpl.java`
#### Snippet
```java
    private static final Logger LOGGER = LoggerFactory.getLogger(StreamSourceServiceImpl.class);

    @Autowired
    private SourceOperatorFactory operatorFactory;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/source/StreamSourceServiceImpl.java`
#### Snippet
```java
    @Autowired
    private StreamSourceEntityMapper sourceMapper;
    @Autowired
    private StreamSourceFieldEntityMapper sourceFieldMapper;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/source/StreamSourceServiceImpl.java`
#### Snippet
```java
    @Autowired
    private InlongStreamEntityMapper streamMapper;
    @Autowired
    private StreamSourceEntityMapper sourceMapper;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/source/StreamSourceServiceImpl.java`
#### Snippet
```java
    @Autowired
    private InlongGroupEntityMapper groupMapper;
    @Autowired
    private InlongStreamEntityMapper streamMapper;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/source/StreamSourceServiceImpl.java`
#### Snippet
```java
    @Autowired
    private StreamSourceFieldEntityMapper sourceFieldMapper;
    @Autowired
    private GroupCheckService groupCheckService;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/source/StreamSourceServiceImpl.java`
#### Snippet
```java
    @Autowired
    private GroupCheckService groupCheckService;
    @Autowired
    private UserService userService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/cluster/PulsarClusterOperator.java`
#### Snippet
```java
    private static final String SERVICE_URL_PREFIX = "pulsar://";

    @Autowired
    private ObjectMapper objectMapper;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/stream/InlongStreamProcessService.java`
#### Snippet
```java
            new CallerRunsPolicy());

    @Autowired
    private InlongGroupService groupService;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/stream/InlongStreamProcessService.java`
#### Snippet
```java
    @Autowired
    private InlongStreamService streamService;
    @Autowired
    private WorkflowService workflowService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/stream/InlongStreamProcessService.java`
#### Snippet
```java
    @Autowired
    private InlongGroupService groupService;
    @Autowired
    private InlongStreamService streamService;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/cluster/node/AgentClusterNodeOperator.java`
#### Snippet
```java
    private static final Logger LOGGER = LoggerFactory.getLogger(AgentClusterNodeOperator.class);

    @Autowired
    private ObjectMapper objectMapper;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/cluster/node/InlongClusterNodeOperatorFactory.java`
#### Snippet
```java
public class InlongClusterNodeOperatorFactory {

    @Autowired
    private List<InlongClusterNodeOperator> clusterNodeOperatorList;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/consume/InlongConsumeProcessService.java`
#### Snippet
```java
    @Autowired
    private InlongConsumeService consumeService;
    @Autowired
    private WorkflowService workflowService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/consume/InlongConsumeProcessService.java`
#### Snippet
```java
public class InlongConsumeProcessService {

    @Autowired
    private InlongConsumeService consumeService;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/consume/InlongConsumeOperatorFactory.java`
#### Snippet
```java
public class InlongConsumeOperatorFactory {

    @Autowired
    private List<InlongConsumeOperator> consumeOperatorList;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/consume/ConsumeKafkaOperator.java`
#### Snippet
```java
    private static final Logger LOGGER = LoggerFactory.getLogger(ConsumeKafkaOperator.class);

    @Autowired
    private InlongGroupService groupService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/consume/ConsumeTubeMQOperator.java`
#### Snippet
```java
    private static final Logger LOGGER = LoggerFactory.getLogger(ConsumeTubeMQOperator.class);

    @Autowired
    private InlongGroupService groupService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/consume/ConsumePulsarOperator.java`
#### Snippet
```java
    @Autowired
    private InlongStreamEntityMapper streamMapper;
    @Autowired
    private InlongClusterService clusterService;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/consume/ConsumePulsarOperator.java`
#### Snippet
```java
    private static final String PREFIX_RLQ = "rlq";

    @Autowired
    private InlongGroupService groupService;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/consume/ConsumePulsarOperator.java`
#### Snippet
```java
    @Autowired
    private InlongGroupService groupService;
    @Autowired
    private InlongStreamEntityMapper streamMapper;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/consume/ConsumePulsarOperator.java`
#### Snippet
```java
    @Autowired
    private InlongClusterService clusterService;
    @Autowired
    private InlongStreamService streamService;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/consume/ConsumePulsarOperator.java`
#### Snippet
```java
    @Autowired
    private InlongStreamService streamService;
    @Autowired
    private ObjectMapper objectMapper;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/listener/GroupTaskListenerFactory.java`
#### Snippet
```java
    @Autowired
    private QueueResourceListener queueResourceListener;
    @Autowired
    private SinkResourceListener sinkResourceListener;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/listener/GroupTaskListenerFactory.java`
#### Snippet
```java
    @Autowired
    private SourceStopListener sourceStopListener;
    @Autowired
    private SourceRestartListener sourceRestartListener;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/listener/GroupTaskListenerFactory.java`
#### Snippet
```java
    @Autowired
    private SourceRestartListener sourceRestartListener;
    @Autowired
    private SourceDeleteListener sourceDeleteListener;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/listener/GroupTaskListenerFactory.java`
#### Snippet
```java
    @Autowired
    private SinkResourceListener sinkResourceListener;
    @Autowired
    private SortConfigListener sortConfigListener;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/listener/GroupTaskListenerFactory.java`
#### Snippet
```java
    @Autowired
    private SourceDeleteListener sourceDeleteListener;
    @Autowired
    private QueueResourceListener queueResourceListener;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/listener/GroupTaskListenerFactory.java`
#### Snippet
```java
    private List<SortOperateListener> sortOperateListeners;

    @Autowired
    private SourceStopListener sourceStopListener;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/listener/StreamTaskListenerFactory.java`
#### Snippet
```java
    @Autowired
    private StreamSortConfigListener streamSortConfigListener;
    @Autowired
    private StreamSinkResourceListener sinkResourceListener;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/listener/StreamTaskListenerFactory.java`
#### Snippet
```java
    private List<SinkOperateListener> sinkOperateListeners;

    @Autowired
    private StreamQueueResourceListener queueResourceListener;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/listener/StreamTaskListenerFactory.java`
#### Snippet
```java
    @Autowired
    private StreamQueueResourceListener queueResourceListener;
    @Autowired
    private StreamSortConfigListener streamSortConfigListener;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/listener/sink/SinkResourceListener.java`
#### Snippet
```java
    @Autowired
    private InlongStreamService streamService;
    @Autowired
    private InlongGroupService groupService;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/listener/sink/SinkResourceListener.java`
#### Snippet
```java
    @Autowired
    private StreamSinkEntityMapper sinkMapper;
    @Autowired
    private InlongStreamService streamService;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/listener/sink/SinkResourceListener.java`
#### Snippet
```java
    @Autowired
    private InlongGroupService groupService;
    @Autowired
    private SinkResourceOperatorFactory sinkOperatorFactory;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/listener/sink/SinkResourceListener.java`
#### Snippet
```java
public class SinkResourceListener implements SinkOperateListener {

    @Autowired
    private StreamSinkEntityMapper sinkMapper;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/listener/sink/StreamSinkResourceListener.java`
#### Snippet
```java
    @Autowired
    private InlongGroupService groupService;
    @Autowired
    private SinkResourceOperatorFactory resourceOperatorFactory;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/listener/sink/StreamSinkResourceListener.java`
#### Snippet
```java
    @Autowired
    private StreamSinkEntityMapper sinkEntityMapper;
    @Autowired
    private InlongGroupService groupService;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/listener/sink/StreamSinkResourceListener.java`
#### Snippet
```java
public class StreamSinkResourceListener implements SinkOperateListener {

    @Autowired
    private StreamSinkEntityMapper sinkEntityMapper;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/consume/InlongConsumeServiceImpl.java`
#### Snippet
```java
    @Autowired
    private InlongConsumeEntityMapper consumeMapper;
    @Autowired
    private InlongConsumeOperatorFactory consumeOperatorFactory;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/consume/InlongConsumeServiceImpl.java`
#### Snippet
```java
    @Autowired
    private InlongConsumeOperatorFactory consumeOperatorFactory;
    @Autowired
    private UserService userService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/consume/InlongConsumeServiceImpl.java`
#### Snippet
```java
    private static final String AUTO_CREATE_MSG = "auto_create_by_system";

    @Autowired
    private InlongConsumeEntityMapper consumeMapper;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/cluster/KafkaClusterOperator.java`
#### Snippet
```java
    private static final Logger LOGGER = LoggerFactory.getLogger(KafkaClusterOperator.class);

    @Autowired
    private ObjectMapper objectMapper;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/listener/group/UpdateGroupFailedListener.java`
#### Snippet
```java
public class UpdateGroupFailedListener implements ProcessEventListener {

    @Autowired
    private InlongGroupService groupService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/listener/group/UpdateGroupCompleteListener.java`
#### Snippet
```java
public class UpdateGroupCompleteListener implements ProcessEventListener {

    @Autowired
    private InlongGroupService groupService;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/listener/group/UpdateGroupCompleteListener.java`
#### Snippet
```java
    @Autowired
    private InlongGroupService groupService;
    @Autowired
    private StreamSourceService sourceService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/listener/group/InitGroupFailedListener.java`
#### Snippet
```java
public class InitGroupFailedListener implements ProcessEventListener {

    @Autowired
    private InlongGroupService groupService;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/listener/group/InitGroupFailedListener.java`
#### Snippet
```java
    @Autowired
    private InlongGroupService groupService;
    @Autowired
    private InlongStreamService streamService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/listener/group/InitGroupListener.java`
#### Snippet
```java
public class InitGroupListener implements ProcessEventListener {

    @Autowired
    private InlongGroupService groupService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/listener/sort/StreamSortConfigListener.java`
#### Snippet
```java
    @Autowired
    private SortConfigOperatorFactory operatorFactory;
    @Autowired
    private InlongGroupService groupService;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/listener/sort/StreamSortConfigListener.java`
#### Snippet
```java
    @Autowired
    private InlongGroupService groupService;
    @Autowired
    private InlongStreamService streamService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/listener/sort/StreamSortConfigListener.java`
#### Snippet
```java
    private static final Logger LOGGER = LoggerFactory.getLogger(StreamSortConfigListener.class);

    @Autowired
    private SortConfigOperatorFactory operatorFactory;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/listener/group/UpdateGroupListener.java`
#### Snippet
```java
public class UpdateGroupListener implements ProcessEventListener {

    @Autowired
    private InlongGroupService groupService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/listener/group/apply/AfterApprovedTaskListener.java`
#### Snippet
```java
    @Autowired
    private InlongGroupService groupService;
    @Autowired
    private InlongGroupEntityMapper groupMapper;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/listener/group/apply/AfterApprovedTaskListener.java`
#### Snippet
```java
public class AfterApprovedTaskListener implements TaskEventListener {

    @Autowired
    private InlongGroupService groupService;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/listener/group/apply/AfterApprovedTaskListener.java`
#### Snippet
```java
    @Autowired
    private InlongGroupEntityMapper groupMapper;
    @Autowired
    private InlongStreamService streamService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/listener/group/InitGroupCompleteListener.java`
#### Snippet
```java
    @Autowired
    private InlongGroupService groupService;
    @Autowired
    private InlongGroupEntityMapper groupMapper;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/listener/group/InitGroupCompleteListener.java`
#### Snippet
```java
    @Autowired
    private InlongGroupEntityMapper groupMapper;
    @Autowired
    private InlongStreamService streamService;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/listener/group/InitGroupCompleteListener.java`
#### Snippet
```java
public class InitGroupCompleteListener implements ProcessEventListener {

    @Autowired
    private InlongGroupService groupService;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/listener/group/InitGroupCompleteListener.java`
#### Snippet
```java
    @Autowired
    private InlongStreamService streamService;
    @Autowired
    private StreamSourceService sourceService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/listener/group/apply/CancelApplyProcessListener.java`
#### Snippet
```java
public class CancelApplyProcessListener implements ProcessEventListener {

    @Autowired
    private InlongGroupEntityMapper groupMapper;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/listener/group/apply/ApproveApplyProcessListener.java`
#### Snippet
```java
    @Autowired
    private InlongStreamService streamService;
    @Autowired
    private WorkflowService workflowService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/listener/group/apply/RejectApplyProcessListener.java`
#### Snippet
```java
    @Autowired
    private InlongGroupService groupService;
    @Autowired
    private InlongGroupEntityMapper groupMapper;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/listener/group/apply/RejectApplyProcessListener.java`
#### Snippet
```java
public class RejectApplyProcessListener implements ProcessEventListener {

    @Autowired
    private InlongGroupService groupService;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/listener/group/apply/ApproveApplyProcessListener.java`
#### Snippet
```java
public class ApproveApplyProcessListener implements ProcessEventListener {

    @Autowired
    private InlongGroupService groupService;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/listener/sort/SortConfigListener.java`
#### Snippet
```java
    @Autowired
    private InlongGroupService groupService;
    @Autowired
    private InlongStreamService streamService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/listener/sort/SortConfigListener.java`
#### Snippet
```java
    private static final Logger LOGGER = LoggerFactory.getLogger(SortConfigListener.class);

    @Autowired
    private SortConfigOperatorFactory operatorFactory;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/listener/sort/SortConfigListener.java`
#### Snippet
```java
    @Autowired
    private SortConfigOperatorFactory operatorFactory;
    @Autowired
    private InlongGroupService groupService;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/listener/group/apply/ApproveApplyProcessListener.java`
#### Snippet
```java
    @Autowired
    private InlongGroupService groupService;
    @Autowired
    private InlongStreamService streamService;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/listener/queue/StreamQueueResourceListener.java`
#### Snippet
```java
    @Autowired
    private InlongGroupService groupService;
    @Autowired
    private InlongStreamService streamService;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/listener/queue/StreamQueueResourceListener.java`
#### Snippet
```java
    @Autowired
    private InlongStreamService streamService;
    @Autowired
    private QueueResourceOperatorFactory queueOperatorFactory;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/listener/queue/StreamQueueResourceListener.java`
#### Snippet
```java
public class StreamQueueResourceListener implements QueueOperateListener {

    @Autowired
    private InlongGroupService groupService;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/listener/stream/UpdateStreamFailedListener.java`
#### Snippet
```java
public class UpdateStreamFailedListener implements ProcessEventListener {

    @Autowired
    private InlongStreamService streamService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/listener/stream/UpdateStreamListener.java`
#### Snippet
```java
public class UpdateStreamListener implements ProcessEventListener {

    @Autowired
    private InlongStreamService streamService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/listener/stream/InitStreamCompleteListener.java`
#### Snippet
```java
    @Autowired
    private InlongStreamService streamService;
    @Autowired
    private StreamSourceService sourceService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/listener/stream/InitStreamCompleteListener.java`
#### Snippet
```java
public class InitStreamCompleteListener implements ProcessEventListener {

    @Autowired
    private InlongStreamService streamService;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/listener/stream/InitStreamFailedListener.java`
#### Snippet
```java
public class InitStreamFailedListener implements ProcessEventListener {

    @Autowired
    private InlongStreamService streamService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/listener/stream/InitStreamListener.java`
#### Snippet
```java
public class InitStreamListener implements ProcessEventListener {

    @Autowired
    private InlongStreamService streamService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/listener/stream/UpdateStreamCompleteListener.java`
#### Snippet
```java
public class UpdateStreamCompleteListener implements ProcessEventListener {

    @Autowired
    private InlongStreamService streamService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/listener/consume/OperateConsumeTaskListener.java`
#### Snippet
```java
public class OperateConsumeTaskListener implements TaskEventListener {

    @Autowired
    private InlongConsumeService consumeService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/listener/consume/apply/CancelConsumeProcessListener.java`
#### Snippet
```java
public class CancelConsumeProcessListener implements ProcessEventListener {

    @Autowired
    private InlongConsumeEntityMapper consumeMapper;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/listener/consume/apply/RejectConsumeProcessListener.java`
#### Snippet
```java
    private static final Logger LOGGER = LoggerFactory.getLogger(RejectConsumeProcessListener.class);

    @Autowired
    private InlongConsumeEntityMapper consumeMapper;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/listener/source/AbstractSourceOperateListener.java`
#### Snippet
```java
    protected InlongStreamService streamService;

    @Autowired
    protected StreamSourceService streamSourceService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/listener/source/AbstractSourceOperateListener.java`
#### Snippet
```java
public abstract class AbstractSourceOperateListener implements SourceOperateListener {

    @Autowired
    protected InlongStreamService streamService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/listener/queue/QueueResourceListener.java`
#### Snippet
```java
    @Autowired
    private InlongGroupService groupService;
    @Autowired
    private InlongStreamService streamService;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/listener/queue/QueueResourceListener.java`
#### Snippet
```java
            new CallerRunsPolicy());

    @Autowired
    private InlongGroupService groupService;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/listener/queue/QueueResourceListener.java`
#### Snippet
```java
    @Autowired
    private InlongStreamService streamService;
    @Autowired
    private WorkflowService workflowService;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/listener/queue/QueueResourceListener.java`
#### Snippet
```java
    @Autowired
    private WorkflowService workflowService;
    @Autowired
    private QueueResourceOperatorFactory queueOperatorFactory;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sink/SinkResourceOperatorFactory.java`
#### Snippet
```java
public class SinkResourceOperatorFactory {

    @Autowired
    private List<SinkResourceOperator> operatorList;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/listener/consume/apply/ApproveConsumeProcessListener.java`
#### Snippet
```java
public class ApproveConsumeProcessListener implements ProcessEventListener {

    @Autowired
    private InlongGroupEntityMapper groupMapper;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/listener/consume/apply/ApproveConsumeProcessListener.java`
#### Snippet
```java
    @Autowired
    private PulsarOperator pulsarOperator;
    @Autowired
    private TubeMQOperator tubeMQOperator;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/listener/consume/apply/ApproveConsumeProcessListener.java`
#### Snippet
```java
    @Autowired
    private InlongGroupEntityMapper groupMapper;
    @Autowired
    private InlongConsumeEntityMapper consumeMapper;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/listener/consume/apply/ApproveConsumeProcessListener.java`
#### Snippet
```java
    @Autowired
    private InlongConsumeEntityMapper consumeMapper;
    @Autowired
    private InlongClusterService clusterService;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/listener/consume/apply/ApproveConsumeProcessListener.java`
#### Snippet
```java
    @Autowired
    private InlongClusterService clusterService;
    @Autowired
    private PulsarOperator pulsarOperator;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/stream/InlongStreamServiceImpl.java`
#### Snippet
```java
    @Autowired
    private StreamSourceService sourceService;
    @Autowired
    private StreamSinkService sinkService;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/stream/InlongStreamServiceImpl.java`
#### Snippet
```java
    @Autowired
    private InlongStreamEntityMapper streamMapper;
    @Autowired
    private InlongStreamFieldEntityMapper streamFieldMapper;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/stream/InlongStreamServiceImpl.java`
#### Snippet
```java
    @Autowired
    private InlongStreamFieldEntityMapper streamFieldMapper;
    @Autowired
    private InlongStreamExtEntityMapper streamExtMapper;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/stream/InlongStreamServiceImpl.java`
#### Snippet
```java
    @Autowired
    private StreamSinkService sinkService;
    @Autowired
    private ObjectMapper objectMapper;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/stream/InlongStreamServiceImpl.java`
#### Snippet
```java
    @Autowired
    private InlongGroupEntityMapper groupMapper;
    @Autowired
    private StreamSourceService sourceService;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/stream/InlongStreamServiceImpl.java`
#### Snippet
```java
    @Autowired
    private ObjectMapper objectMapper;
    @Autowired
    private UserService userService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/stream/InlongStreamServiceImpl.java`
#### Snippet
```java
    @Autowired
    private InlongStreamExtEntityMapper streamExtMapper;
    @Autowired
    private InlongGroupEntityMapper groupMapper;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/stream/InlongStreamServiceImpl.java`
#### Snippet
```java
    private static final int PARSE_FIELD_CSV_MIN_COLUMNS = 2;

    @Autowired
    private InlongStreamEntityMapper streamMapper;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/cluster/InlongClusterServiceImpl.java`
#### Snippet
```java
    @Autowired
    private InlongGroupEntityMapper groupMapper;
    @Autowired
    private InlongStreamEntityMapper streamMapper;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/cluster/InlongClusterServiceImpl.java`
#### Snippet
```java
    @Autowired
    private InlongClusterEntityMapper clusterMapper;
    @Autowired
    private InlongClusterNodeEntityMapper clusterNodeMapper;
    @Lazy
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/cluster/InlongClusterServiceImpl.java`
#### Snippet
```java
    @Autowired
    private InlongClusterNodeOperatorFactory clusterNodeOperatorFactory;
    @Autowired
    private InlongClusterTagEntityMapper clusterTagMapper;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/cluster/InlongClusterServiceImpl.java`
#### Snippet
```java
    private InlongClusterNodeEntityMapper clusterNodeMapper;
    @Lazy
    @Autowired
    private DataProxyConfigRepository proxyRepository;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/cluster/InlongClusterServiceImpl.java`
#### Snippet
```java
    private static final Gson GSON = new Gson();

    @Autowired
    private UserService userService;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/cluster/InlongClusterServiceImpl.java`
#### Snippet
```java
    @Autowired
    private InlongStreamEntityMapper streamMapper;
    @Autowired
    private InlongClusterOperatorFactory clusterOperatorFactory;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/cluster/InlongClusterServiceImpl.java`
#### Snippet
```java
    @Autowired
    private InlongClusterTagEntityMapper clusterTagMapper;
    @Autowired
    private InlongClusterEntityMapper clusterMapper;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/cluster/InlongClusterServiceImpl.java`
#### Snippet
```java
    @Autowired
    private UserService userService;
    @Autowired
    private InlongGroupEntityMapper groupMapper;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/cluster/InlongClusterServiceImpl.java`
#### Snippet
```java
    @Autowired
    private InlongClusterOperatorFactory clusterOperatorFactory;
    @Autowired
    private InlongClusterNodeOperatorFactory clusterNodeOperatorFactory;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sink/ck/ClickHouseResourceOperator.java`
#### Snippet
```java

    private static final Logger LOGGER = LoggerFactory.getLogger(ClickHouseResourceOperator.class);
    @Autowired
    private StreamSinkService sinkService;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sink/ck/ClickHouseResourceOperator.java`
#### Snippet
```java
    @Autowired
    private StreamSinkFieldEntityMapper clickHouseFieldMapper;
    @Autowired
    private DataNodeOperateHelper dataNodeHelper;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sink/ck/ClickHouseResourceOperator.java`
#### Snippet
```java
    @Autowired
    private StreamSinkService sinkService;
    @Autowired
    private StreamSinkFieldEntityMapper clickHouseFieldMapper;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sink/hive/HiveResourceOperator.java`
#### Snippet
```java
    @Autowired
    private StreamSinkFieldEntityMapper sinkFieldMapper;
    @Autowired
    private DataNodeOperateHelper dataNodeHelper;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sink/hive/HiveResourceOperator.java`
#### Snippet
```java

    private static final Logger LOGGER = LoggerFactory.getLogger(HiveResourceOperator.class);
    @Autowired
    private StreamSinkService sinkService;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sink/hive/HiveResourceOperator.java`
#### Snippet
```java
    @Autowired
    private StreamSinkService sinkService;
    @Autowired
    private StreamSinkFieldEntityMapper sinkFieldMapper;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sink/es/ElasticsearchResourceOperator.java`
#### Snippet
```java

    private static final Logger LOGGER = LoggerFactory.getLogger(ElasticsearchResourceOperator.class);
    @Autowired
    private StreamSinkService sinkService;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sink/es/ElasticsearchResourceOperator.java`
#### Snippet
```java
    @Autowired
    private StreamSinkFieldEntityMapper sinkFieldMapper;
    @Autowired
    private DataNodeOperateHelper dataNodeHelper;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sink/es/ElasticsearchResourceOperator.java`
#### Snippet
```java
    @Autowired
    private StreamSinkService sinkService;
    @Autowired
    private StreamSinkFieldEntityMapper sinkFieldMapper;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sink/es/ElasticsearchApi.java`
#### Snippet
```java
    private static final Logger LOG = LoggerFactory.getLogger(ElasticsearchApi.class);

    @Autowired
    private ElasticsearchConfig esConfig;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sink/hudi/HudiResourceOperator.java`
#### Snippet
```java
    @Autowired
    private StreamSinkFieldEntityMapper sinkFieldMapper;
    @Autowired
    private DataNodeOperateHelper dataNodeHelper;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sink/hudi/HudiResourceOperator.java`
#### Snippet
```java
    private static final String CATALOG_TYPE_HIVE = "HIVE";

    @Autowired
    private StreamSinkService sinkService;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sink/hudi/HudiResourceOperator.java`
#### Snippet
```java
    @Autowired
    private StreamSinkService sinkService;
    @Autowired
    private StreamSinkFieldEntityMapper sinkFieldMapper;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sink/kudu/KuduResourceOperator.java`
#### Snippet
```java
    private static final Logger LOGGER = LoggerFactory.getLogger(KuduResourceOperator.class);

    @Autowired
    private StreamSinkService sinkService;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sink/kudu/KuduResourceOperator.java`
#### Snippet
```java
    @Autowired
    private StreamSinkFieldEntityMapper sinkFieldMapper;
    @Autowired
    private DataNodeOperateHelper dataNodeHelper;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sink/kudu/KuduResourceOperator.java`
#### Snippet
```java
    @Autowired
    private StreamSinkService sinkService;
    @Autowired
    private StreamSinkFieldEntityMapper sinkFieldMapper;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sink/kafka/KafkaResourceOperator.java`
#### Snippet
```java
    private static final Logger LOGGER = LoggerFactory.getLogger(KafkaResourceOperator.class);

    @Autowired
    private StreamSinkService sinkService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sink/hbase/HBaseResourceOperator.java`
#### Snippet
```java
    private static final Logger LOGGER = LoggerFactory.getLogger(HBaseResourceOperator.class);

    @Autowired
    private StreamSinkService sinkService;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sink/hbase/HBaseResourceOperator.java`
#### Snippet
```java
    @Autowired
    private StreamSinkService sinkService;
    @Autowired
    private StreamSinkFieldEntityMapper sinkFieldMapper;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sink/mysql/MySQLResourceOperator.java`
#### Snippet
```java
    private StreamSinkFieldEntityMapper fieldEntityMapper;

    @Autowired
    private DataNodeOperateHelper dataNodeHelper;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sink/mysql/MySQLResourceOperator.java`
#### Snippet
```java
    private static final Logger LOG = LoggerFactory.getLogger(MySQLResourceOperator.class);

    @Autowired
    private StreamSinkService sinkService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sink/mysql/MySQLResourceOperator.java`
#### Snippet
```java
    private StreamSinkService sinkService;

    @Autowired
    private StreamSinkFieldEntityMapper fieldEntityMapper;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sink/iceberg/IcebergResourceOperator.java`
#### Snippet
```java
    @Autowired
    private StreamSinkService sinkService;
    @Autowired
    private StreamSinkFieldEntityMapper sinkFieldMapper;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sink/iceberg/IcebergResourceOperator.java`
#### Snippet
```java
    @Autowired
    private StreamSinkFieldEntityMapper sinkFieldMapper;
    @Autowired
    private DataNodeOperateHelper dataNodeHelper;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sink/iceberg/IcebergResourceOperator.java`
#### Snippet
```java
    private static final String CATALOG_TYPE_HIVE = "HIVE";

    @Autowired
    private StreamSinkService sinkService;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sink/sqlserver/SQLServerResourceOperator.java`
#### Snippet
```java
    private static final Logger LOG = LoggerFactory.getLogger(SQLServerResourceOperator.class);

    @Autowired
    private StreamSinkService sinkService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sink/sqlserver/SQLServerResourceOperator.java`
#### Snippet
```java
    private StreamSinkService sinkService;

    @Autowired
    private StreamSinkFieldEntityMapper fieldEntityMapper;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sink/starrocks/StarRocksResourceOperator.java`
#### Snippet
```java
    private StreamSinkFieldEntityMapper fieldEntityMapper;

    @Autowired
    private DataNodeOperateHelper dataNodeHelper;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sink/starrocks/StarRocksResourceOperator.java`
#### Snippet
```java
    private static final Logger LOG = LoggerFactory.getLogger(MySQLResourceOperator.class);

    @Autowired
    private StreamSinkService sinkService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sink/starrocks/StarRocksResourceOperator.java`
#### Snippet
```java
    private StreamSinkService sinkService;

    @Autowired
    private StreamSinkFieldEntityMapper fieldEntityMapper;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sort/SortConfigOperatorFactory.java`
#### Snippet
```java
public class SortConfigOperatorFactory {

    @Autowired
    private List<SortConfigOperator> operatorList;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/queue/QueueResourceOperatorFactory.java`
#### Snippet
```java
public class QueueResourceOperatorFactory {

    @Autowired
    private List<QueueResourceOperator> operatorList;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sink/postgresql/PostgreSQLResourceOperator.java`
#### Snippet
```java
    private static final String POSTGRESQL_DEFAULT_SCHEMA = "public";

    @Autowired
    private StreamSinkService sinkService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sink/postgresql/PostgreSQLResourceOperator.java`
#### Snippet
```java
    private StreamSinkService sinkService;

    @Autowired
    private StreamSinkFieldEntityMapper postgresFieldMapper;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/queue/kafka/KafkaResourceOperators.java`
#### Snippet
```java
    @Autowired
    private InlongConsumeService consumeService;
    @Autowired
    private InlongClusterService clusterService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/queue/kafka/KafkaResourceOperators.java`
#### Snippet
```java
    @Autowired
    private KafkaOperator kafkaOperator;
    @Autowired
    private InlongStreamService streamService;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/queue/kafka/KafkaResourceOperators.java`
#### Snippet
```java
    @Autowired
    private InlongStreamService streamService;
    @Autowired
    private InlongConsumeService consumeService;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/queue/kafka/KafkaResourceOperators.java`
#### Snippet
```java
    public static final String KAFKA_CONSUMER_GROUP = "%s_%s_consumer_group";

    @Autowired
    private KafkaOperator kafkaOperator;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/workflow/WorkflowDefinitionRegister.java`
#### Snippet
```java
public class WorkflowDefinitionRegister {

    @Autowired
    private ProcessDefinitionService processDefService;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/workflow/WorkflowDefinitionRegister.java`
#### Snippet
```java
    @Autowired
    private ProcessDefinitionService processDefService;
    @Autowired
    private List<WorkflowDefinition> workflowDefinitions;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/queue/tubemq/TubeMQResourceOperator.java`
#### Snippet
```java
    @Autowired
    private InlongClusterService clusterService;
    @Autowired
    private InlongConsumeService consumeService;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/queue/tubemq/TubeMQResourceOperator.java`
#### Snippet
```java
    @Autowired
    private InlongConsumeService consumeService;
    @Autowired
    private TubeMQOperator tubeMQOperator;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/queue/tubemq/TubeMQResourceOperator.java`
#### Snippet
```java
public class TubeMQResourceOperator implements QueueResourceOperator {

    @Autowired
    private InlongClusterService clusterService;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/queue/tubemq/TubeMQOperator.java`
#### Snippet
```java
    private static final String ADD_CONSUMER_PATH = "/webapi.htm?method=admin_add_authorized_consumergroup_info";

    @Autowired
    private RestTemplate restTemplate;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sort/DefaultSortConfigOperator.java`
#### Snippet
```java
    @Autowired
    private StreamSinkService sinkService;
    @Autowired
    private AuditService auditService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sort/DefaultSortConfigOperator.java`
#### Snippet
```java
    @Autowired
    private StreamTransformService transformService;
    @Autowired
    private StreamSinkService sinkService;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sort/DefaultSortConfigOperator.java`
#### Snippet
```java
    @Autowired
    private StreamSourceService sourceService;
    @Autowired
    private StreamTransformService transformService;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sort/DefaultSortConfigOperator.java`
#### Snippet
```java
    private static final ObjectMapper OBJECT_MAPPER = new ObjectMapper();

    @Autowired
    private StreamSourceService sourceService;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/workflow/group/CreateGroupWorkflowDefinition.java`
#### Snippet
```java
    @Autowired
    private InitGroupListener initGroupListener;
    @Autowired
    private InitGroupCompleteListener initGroupCompleteListener;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/workflow/group/CreateGroupWorkflowDefinition.java`
#### Snippet
```java
public class CreateGroupWorkflowDefinition implements WorkflowDefinition {

    @Autowired
    private InitGroupListener initGroupListener;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/workflow/group/CreateGroupWorkflowDefinition.java`
#### Snippet
```java
    @Autowired
    private InitGroupFailedListener initGroupFailedListener;
    @Autowired
    private GroupTaskListenerFactory groupTaskListenerFactory;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/workflow/group/CreateGroupWorkflowDefinition.java`
#### Snippet
```java
    @Autowired
    private InitGroupCompleteListener initGroupCompleteListener;
    @Autowired
    private InitGroupFailedListener initGroupFailedListener;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/workflow/group/DeleteGroupWorkflowDefinition.java`
#### Snippet
```java
    @Autowired
    private UpdateGroupFailedListener updateGroupFailedListener;
    @Autowired
    private GroupTaskListenerFactory groupTaskListenerFactory;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/workflow/group/DeleteGroupWorkflowDefinition.java`
#### Snippet
```java
    @Autowired
    private UpdateGroupListener updateGroupListener;
    @Autowired
    private UpdateGroupCompleteListener updateGroupCompleteListener;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/workflow/group/DeleteGroupWorkflowDefinition.java`
#### Snippet
```java
    @Autowired
    private UpdateGroupCompleteListener updateGroupCompleteListener;
    @Autowired
    private UpdateGroupFailedListener updateGroupFailedListener;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/workflow/group/DeleteGroupWorkflowDefinition.java`
#### Snippet
```java
public class DeleteGroupWorkflowDefinition implements WorkflowDefinition {

    @Autowired
    private UpdateGroupListener updateGroupListener;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/workflow/group/SuspendGroupWorkflowDefinition.java`
#### Snippet
```java
    @Autowired
    private UpdateGroupFailedListener updateGroupFailedListener;
    @Autowired
    private GroupTaskListenerFactory groupTaskListenerFactory;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/workflow/group/SuspendGroupWorkflowDefinition.java`
#### Snippet
```java
    @Autowired
    private UpdateGroupListener updateGroupListener;
    @Autowired
    private UpdateGroupCompleteListener updateGroupCompleteListener;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/workflow/group/SuspendGroupWorkflowDefinition.java`
#### Snippet
```java
public class SuspendGroupWorkflowDefinition implements WorkflowDefinition {

    @Autowired
    private UpdateGroupListener updateGroupListener;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/workflow/group/SuspendGroupWorkflowDefinition.java`
#### Snippet
```java
    @Autowired
    private UpdateGroupCompleteListener updateGroupCompleteListener;
    @Autowired
    private UpdateGroupFailedListener updateGroupFailedListener;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/queue/pulsar/PulsarResourceOperator.java`
#### Snippet
```java
    @Autowired
    private StreamSinkService sinkService;
    @Autowired
    private InlongConsumeService consumeService;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/queue/pulsar/PulsarResourceOperator.java`
#### Snippet
```java
    @Autowired
    private InlongClusterService clusterService;
    @Autowired
    private InlongStreamService streamService;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/queue/pulsar/PulsarResourceOperator.java`
#### Snippet
```java
    @Autowired
    private InlongConsumeService consumeService;
    @Autowired
    private PulsarOperator pulsarOperator;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/queue/pulsar/PulsarResourceOperator.java`
#### Snippet
```java
    public static final String PULSAR_SUBSCRIPTION = "%s_%s_%s_consumer_group";

    @Autowired
    private InlongClusterService clusterService;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/queue/pulsar/PulsarResourceOperator.java`
#### Snippet
```java
    @Autowired
    private InlongStreamService streamService;
    @Autowired
    private StreamSinkService sinkService;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/workflow/group/RestartGroupWorkflowDefinition.java`
#### Snippet
```java
    @Autowired
    private UpdateGroupFailedListener updateGroupFailedListener;
    @Autowired
    private GroupTaskListenerFactory taskListenerFactory;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/workflow/group/RestartGroupWorkflowDefinition.java`
#### Snippet
```java
    @Autowired
    private UpdateGroupListener updateGroupListener;
    @Autowired
    private UpdateGroupCompleteListener updateGroupCompleteListener;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/workflow/group/RestartGroupWorkflowDefinition.java`
#### Snippet
```java
    @Autowired
    private UpdateGroupCompleteListener updateGroupCompleteListener;
    @Autowired
    private UpdateGroupFailedListener updateGroupFailedListener;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/workflow/group/RestartGroupWorkflowDefinition.java`
#### Snippet
```java
public class RestartGroupWorkflowDefinition implements WorkflowDefinition {

    @Autowired
    private UpdateGroupListener updateGroupListener;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/workflow/stream/SuspendStreamWorkflowDefinition.java`
#### Snippet
```java
    @Autowired
    private UpdateStreamFailedListener updateStreamFailedListener;
    @Autowired
    private StreamTaskListenerFactory streamTaskListenerFactory;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/workflow/stream/SuspendStreamWorkflowDefinition.java`
#### Snippet
```java
    @Autowired
    private UpdateStreamCompleteListener updateStreamCompleteListener;
    @Autowired
    private UpdateStreamFailedListener updateStreamFailedListener;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/workflow/stream/SuspendStreamWorkflowDefinition.java`
#### Snippet
```java
    @Autowired
    private UpdateStreamListener updateStreamListener;
    @Autowired
    private UpdateStreamCompleteListener updateStreamCompleteListener;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/workflow/stream/SuspendStreamWorkflowDefinition.java`
#### Snippet
```java
public class SuspendStreamWorkflowDefinition implements WorkflowDefinition {

    @Autowired
    private UpdateStreamListener updateStreamListener;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/workflow/stream/DeleteStreamWorkflowDefinition.java`
#### Snippet
```java
    @Autowired
    private UpdateStreamListener updateStreamListener;
    @Autowired
    private UpdateStreamCompleteListener updateStreamCompleteListener;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/workflow/stream/DeleteStreamWorkflowDefinition.java`
#### Snippet
```java
    @Autowired
    private UpdateStreamCompleteListener updateStreamCompleteListener;
    @Autowired
    private UpdateStreamFailedListener updateStreamFailedListener;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/workflow/stream/DeleteStreamWorkflowDefinition.java`
#### Snippet
```java
    @Autowired
    private UpdateStreamFailedListener updateStreamFailedListener;
    @Autowired
    private StreamTaskListenerFactory streamTaskListenerFactory;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/workflow/stream/DeleteStreamWorkflowDefinition.java`
#### Snippet
```java
public class DeleteStreamWorkflowDefinition implements WorkflowDefinition {

    @Autowired
    private UpdateStreamListener updateStreamListener;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/workflow/stream/CreateStreamWorkflowDefinition.java`
#### Snippet
```java
public class CreateStreamWorkflowDefinition implements WorkflowDefinition {

    @Autowired
    private InitStreamListener initStreamListener;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/workflow/stream/CreateStreamWorkflowDefinition.java`
#### Snippet
```java
    @Autowired
    private InitStreamCompleteListener initStreamCompleteListener;
    @Autowired
    private InitStreamFailedListener initStreamFailedListener;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/workflow/stream/CreateStreamWorkflowDefinition.java`
#### Snippet
```java
    @Autowired
    private InitStreamFailedListener initStreamFailedListener;
    @Autowired
    private StreamTaskListenerFactory streamTaskListenerFactory;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/workflow/stream/CreateStreamWorkflowDefinition.java`
#### Snippet
```java
    @Autowired
    private InitStreamListener initStreamListener;
    @Autowired
    private InitStreamCompleteListener initStreamCompleteListener;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/queue/pulsar/PulsarOperator.java`
#### Snippet
```java
    private static final int DELAY_SECONDS = 5;

    @Autowired
    private ConversionHandle conversionHandle;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/workflow/group/ApplyGroupWorkflowDefinition.java`
#### Snippet
```java
    @Autowired
    private RejectApplyProcessListener rejectApplyProcessListener;
    @Autowired
    private ApproveApplyProcessListener approveApplyProcessListener;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/workflow/group/ApplyGroupWorkflowDefinition.java`
#### Snippet
```java
    @Autowired
    private ApproveApplyProcessListener approveApplyProcessListener;
    @Autowired
    private AfterApprovedTaskListener afterApprovedTaskListener;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/workflow/group/ApplyGroupWorkflowDefinition.java`
#### Snippet
```java
public class ApplyGroupWorkflowDefinition implements WorkflowDefinition {

    @Autowired
    private CancelApplyProcessListener cancelApplyProcessListener;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/workflow/group/ApplyGroupWorkflowDefinition.java`
#### Snippet
```java
    @Autowired
    private AfterApprovedTaskListener afterApprovedTaskListener;
    @Autowired
    private WorkflowApproverService workflowApproverService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/workflow/group/ApplyGroupWorkflowDefinition.java`
#### Snippet
```java
    @Autowired
    private CancelApplyProcessListener cancelApplyProcessListener;
    @Autowired
    private RejectApplyProcessListener rejectApplyProcessListener;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/workflow/consume/ApplyConsumeProcessHandler.java`
#### Snippet
```java
    @Autowired
    private ObjectMapper objectMapper;
    @Autowired
    private ProcessDefinitionService processDefinitionService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/workflow/consume/ApplyConsumeProcessHandler.java`
#### Snippet
```java
public class ApplyConsumeProcessHandler implements ProcessDetailHandler {

    @Autowired
    private ObjectMapper objectMapper;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/workflow/stream/RestartStreamWorkflowDefinition.java`
#### Snippet
```java
    @Autowired
    private UpdateStreamListener updateStreamListener;
    @Autowired
    private UpdateStreamCompleteListener updateStreamCompleteListener;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/workflow/stream/RestartStreamWorkflowDefinition.java`
#### Snippet
```java
    @Autowired
    private UpdateStreamCompleteListener updateStreamCompleteListener;
    @Autowired
    private UpdateStreamFailedListener updateStreamFailedListener;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/workflow/stream/RestartStreamWorkflowDefinition.java`
#### Snippet
```java
    @Autowired
    private UpdateStreamFailedListener updateStreamFailedListener;
    @Autowired
    private StreamTaskListenerFactory streamTaskListenerFactory;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/workflow/stream/RestartStreamWorkflowDefinition.java`
#### Snippet
```java
public class RestartStreamWorkflowDefinition implements WorkflowDefinition {

    @Autowired
    private UpdateStreamListener updateStreamListener;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/operationlog/OperationLogPool.java`
#### Snippet
```java
            new CallerRunsPolicy());

    @Autowired
    private OperationLogEntityMapper operationLogMapper;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/workflow/consume/ApplyConsumeWorkflowDefinition.java`
#### Snippet
```java
    @Autowired
    private WorkflowApproverService workflowApproverService;
    @Autowired
    private ApplyConsumeProcessHandler applyConsumeProcessHandler;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/workflow/consume/ApplyConsumeWorkflowDefinition.java`
#### Snippet
```java
    @Autowired
    private ApproveConsumeProcessListener approveConsumeProcessListener;
    @Autowired
    private OperateConsumeTaskListener operateConsumeTaskListener;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/workflow/consume/ApplyConsumeWorkflowDefinition.java`
#### Snippet
```java
    @Autowired
    private CancelConsumeProcessListener cancelConsumeProcessListener;
    @Autowired
    private RejectConsumeProcessListener rejectConsumeProcessListener;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/workflow/consume/ApplyConsumeWorkflowDefinition.java`
#### Snippet
```java
    private OperateConsumeTaskListener operateConsumeTaskListener;

    @Autowired
    private WorkflowApproverService workflowApproverService;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/workflow/consume/ApplyConsumeWorkflowDefinition.java`
#### Snippet
```java
    @Autowired
    private RejectConsumeProcessListener rejectConsumeProcessListener;
    @Autowired
    private ApproveConsumeProcessListener approveConsumeProcessListener;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/workflow/consume/ApplyConsumeWorkflowDefinition.java`
#### Snippet
```java
public class ApplyConsumeWorkflowDefinition implements WorkflowDefinition {

    @Autowired
    private CancelConsumeProcessListener cancelConsumeProcessListener;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/workflow/consume/ApplyConsumeWorkflowDefinition.java`
#### Snippet
```java
    @Autowired
    private ApplyConsumeProcessHandler applyConsumeProcessHandler;
    @Autowired
    private InlongGroupService groupService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/heartbeat/HeartbeatServiceImpl.java`
#### Snippet
```java
    @Autowired
    private GroupHeartbeatEntityMapper groupHeartbeatMapper;
    @Autowired
    private StreamHeartbeatEntityMapper streamHeartbeatMapper;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/heartbeat/HeartbeatServiceImpl.java`
#### Snippet
```java
    @Autowired
    private ComponentHeartbeatEntityMapper componentHeartbeatMapper;
    @Autowired
    private GroupHeartbeatEntityMapper groupHeartbeatMapper;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/heartbeat/HeartbeatServiceImpl.java`
#### Snippet
```java
    @Lazy
    private HeartbeatManager heartbeatManager;
    @Autowired
    private ComponentHeartbeatEntityMapper componentHeartbeatMapper;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/heartbeat/HeartbeatServiceImpl.java`
#### Snippet
```java
    private static final Gson GSON = new Gson();

    @Autowired
    @Lazy
    private HeartbeatManager heartbeatManager;
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/workflow/WorkflowServiceImpl.java`
#### Snippet
```java
    @Autowired
    private ProcessDefinitionService processDefService;
    @Autowired
    private ProcessService processService;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/workflow/WorkflowServiceImpl.java`
#### Snippet
```java
    @Autowired
    private ProcessService processService;
    @Autowired
    private TaskService taskService;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/workflow/WorkflowServiceImpl.java`
#### Snippet
```java
    private static final Logger LOGGER = LoggerFactory.getLogger(WorkflowServiceImpl.class);

    @Autowired
    private WorkflowQueryService queryService;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/workflow/WorkflowServiceImpl.java`
#### Snippet
```java
    @Autowired
    private TaskService taskService;
    @Autowired
    private ObjectMapper objectMapper;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/workflow/WorkflowServiceImpl.java`
#### Snippet
```java
    @Autowired
    private WorkflowQueryService queryService;
    @Autowired
    private ProcessDefinitionService processDefService;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-workflow/src/main/java/org/apache/inlong/manager/workflow/core/impl/ProcessDefinitionServiceImpl.java`
#### Snippet
```java
public class ProcessDefinitionServiceImpl implements ProcessDefinitionService {

    @Autowired
    private ProcessDefinitionRepository definitionRepository;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/heartbeat/HeartbeatManager.java`
#### Snippet
```java
    @Autowired
    private InlongClusterEntityMapper clusterMapper;
    @Autowired
    private InlongClusterNodeEntityMapper clusterNodeMapper;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/heartbeat/HeartbeatManager.java`
#### Snippet
```java
    @Autowired
    private InlongClusterOperatorFactory clusterOperatorFactory;
    @Autowired
    private InlongClusterEntityMapper clusterMapper;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/heartbeat/HeartbeatManager.java`
#### Snippet
```java
    @Getter
    private LoadingCache<ComponentHeartbeat, ClusterInfo> clusterInfoCache;
    @Autowired
    private InlongClusterOperatorFactory clusterOperatorFactory;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/heartbeat/HeartbeatManager.java`
#### Snippet
```java
    @Autowired
    private InlongClusterNodeEntityMapper clusterNodeMapper;
    @Autowired
    private StreamSourceEntityMapper sourceMapper;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-workflow/src/main/java/org/apache/inlong/manager/workflow/core/impl/TaskServiceImpl.java`
#### Snippet
```java
    @Autowired
    private ProcessorExecutor processorExecutor;
    @Autowired
    private WorkflowContextBuilder workflowContextBuilder;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-workflow/src/main/java/org/apache/inlong/manager/workflow/core/impl/TaskServiceImpl.java`
#### Snippet
```java
public class TaskServiceImpl implements TaskService {

    @Autowired
    private ProcessorExecutor processorExecutor;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-workflow/src/main/java/org/apache/inlong/manager/workflow/core/impl/ProcessorExecutorImpl.java`
#### Snippet
```java
    @Autowired
    private StartEventProcessor startEventProcessor;
    @Autowired
    private EndEventProcessor endEventProcessor;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-workflow/src/main/java/org/apache/inlong/manager/workflow/core/impl/ProcessorExecutorImpl.java`
#### Snippet
```java
    @Autowired
    private UserTaskProcessor userTaskProcessor;
    @Autowired
    private ServiceTaskProcessor serviceTaskProcessor;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-workflow/src/main/java/org/apache/inlong/manager/workflow/core/impl/ProcessorExecutorImpl.java`
#### Snippet
```java
    @Autowired
    private EndEventProcessor endEventProcessor;
    @Autowired
    private UserTaskProcessor userTaskProcessor;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-workflow/src/main/java/org/apache/inlong/manager/workflow/core/impl/ProcessorExecutorImpl.java`
#### Snippet
```java
    private ImmutableMap<Class<? extends Element>, ElementProcessor<? extends Element>> elementProcessor;

    @Autowired
    private StartEventProcessor startEventProcessor;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-workflow/src/main/java/org/apache/inlong/manager/workflow/core/impl/EventListenerServiceImpl.java`
#### Snippet
```java
    @Autowired
    private TaskEventNotifier taskEventNotifier;
    @Autowired
    private WorkflowEventLogEntityMapper eventLogMapper;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-workflow/src/main/java/org/apache/inlong/manager/workflow/core/impl/EventListenerServiceImpl.java`
#### Snippet
```java
    @Autowired
    private ProcessEventNotifier processEventNotifier;
    @Autowired
    private TaskEventNotifier taskEventNotifier;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-workflow/src/main/java/org/apache/inlong/manager/workflow/core/impl/EventListenerServiceImpl.java`
#### Snippet
```java
public class EventListenerServiceImpl implements EventListenerService {

    @Autowired
    private WorkflowContextBuilder workflowContextBuilder;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-workflow/src/main/java/org/apache/inlong/manager/workflow/core/impl/EventListenerServiceImpl.java`
#### Snippet
```java
    @Autowired
    private WorkflowContextBuilder workflowContextBuilder;
    @Autowired
    private ProcessEventNotifier processEventNotifier;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-workflow/src/main/java/org/apache/inlong/manager/workflow/core/impl/ProcessServiceImpl.java`
#### Snippet
```java
    @Autowired
    private WorkflowTaskEntityMapper taskEntityMapper;
    @Autowired
    private WorkflowContextBuilder workflowContextBuilder;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-workflow/src/main/java/org/apache/inlong/manager/workflow/core/impl/ProcessServiceImpl.java`
#### Snippet
```java
public class ProcessServiceImpl implements ProcessService {

    @Autowired
    private ProcessorExecutor processorExecutor;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-workflow/src/main/java/org/apache/inlong/manager/workflow/core/impl/ProcessServiceImpl.java`
#### Snippet
```java
    @Autowired
    private InlongGroupEntityMapper groupMapper;
    @Autowired
    private UserEntityMapper userMapper;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-workflow/src/main/java/org/apache/inlong/manager/workflow/core/impl/ProcessServiceImpl.java`
#### Snippet
```java
    @Autowired
    private WorkflowContextBuilder workflowContextBuilder;
    @Autowired
    private InlongGroupEntityMapper groupMapper;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-workflow/src/main/java/org/apache/inlong/manager/workflow/core/impl/ProcessServiceImpl.java`
#### Snippet
```java
    @Autowired
    private ProcessorExecutor processorExecutor;
    @Autowired
    private WorkflowTaskEntityMapper taskEntityMapper;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/repository/DataProxyConfigRepository.java`
#### Snippet
```java
    private long reloadInterval;

    @Autowired
    private ClusterSetMapper clusterSetMapper;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/repository/DataProxyConfigRepository.java`
#### Snippet
```java
    @Autowired
    private StreamSinkEntityMapper streamSinkMapper;
    @Autowired
    private SortConfigLoader sortConfigLoader;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/repository/DataProxyConfigRepository.java`
#### Snippet
```java
    @Autowired
    private ClusterSetMapper clusterSetMapper;
    @Autowired
    private InlongClusterEntityMapper clusterMapper;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/repository/DataProxyConfigRepository.java`
#### Snippet
```java
    @Autowired
    private InlongClusterEntityMapper clusterMapper;
    @Autowired
    private InlongGroupEntityMapper inlongGroupMapper;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/repository/DataProxyConfigRepository.java`
#### Snippet
```java
    @Autowired
    private InlongGroupEntityMapper inlongGroupMapper;
    @Autowired
    private StreamSinkEntityMapper streamSinkMapper;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-workflow/src/main/java/org/apache/inlong/manager/workflow/core/impl/WorkflowContextBuilderImpl.java`
#### Snippet
```java
    @Autowired
    private ProcessDefinitionRepository definitionRepository;
    @Autowired
    private WorkflowProcessEntityMapper processEntityMapper;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-workflow/src/main/java/org/apache/inlong/manager/workflow/core/impl/WorkflowContextBuilderImpl.java`
#### Snippet
```java
    @Autowired
    private WorkflowProcessEntityMapper processEntityMapper;
    @Autowired
    private WorkflowTaskEntityMapper taskEntityMapper;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-workflow/src/main/java/org/apache/inlong/manager/workflow/core/impl/WorkflowContextBuilderImpl.java`
#### Snippet
```java
public class WorkflowContextBuilderImpl implements WorkflowContextBuilder {

    @Autowired
    private ObjectMapper objectMapper;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/transform/StreamTransformServiceImpl.java`
#### Snippet
```java
    @Autowired
    private InlongGroupEntityMapper groupMapper;
    @Autowired
    protected StreamTransformEntityMapper transformMapper;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/transform/StreamTransformServiceImpl.java`
#### Snippet
```java
    @Autowired
    protected GroupCheckService groupCheckService;
    @Autowired
    protected UserService userService;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/transform/StreamTransformServiceImpl.java`
#### Snippet
```java
    @Autowired
    protected StreamTransformFieldEntityMapper transformFieldMapper;
    @Autowired
    protected GroupCheckService groupCheckService;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/transform/StreamTransformServiceImpl.java`
#### Snippet
```java
    @Autowired
    protected StreamTransformEntityMapper transformMapper;
    @Autowired
    protected StreamTransformFieldEntityMapper transformFieldMapper;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/transform/StreamTransformServiceImpl.java`
#### Snippet
```java
    private static final Logger LOGGER = LoggerFactory.getLogger(StreamTransformServiceImpl.class);

    @Autowired
    private InlongGroupEntityMapper groupMapper;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-workflow/src/main/java/org/apache/inlong/manager/workflow/core/impl/WorkflowContextBuilderImpl.java`
#### Snippet
```java
    @Autowired
    private ObjectMapper objectMapper;
    @Autowired
    private ProcessDefinitionRepository definitionRepository;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-workflow/src/main/java/org/apache/inlong/manager/workflow/event/task/TaskEventNotifier.java`
#### Snippet
```java
public class TaskEventNotifier implements EventListenerNotifier<TaskEvent> {

    @Autowired
    private WorkflowEventLogEntityMapper eventLogMapper;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-workflow/src/main/java/org/apache/inlong/manager/workflow/event/process/ProcessEventNotifier.java`
#### Snippet
```java
public class ProcessEventNotifier implements EventListenerNotifier<ProcessEvent> {

    @Autowired
    private WorkflowEventLogEntityMapper eventLogMapper;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-workflow/src/main/java/org/apache/inlong/manager/workflow/core/impl/WorkflowQueryServiceImpl.java`
#### Snippet
```java
    @Autowired
    private ProcessDefinitionRepository definitionRepository;
    @Autowired
    private WorkflowProcessEntityMapper processEntityMapper;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-workflow/src/main/java/org/apache/inlong/manager/workflow/core/impl/WorkflowQueryServiceImpl.java`
#### Snippet
```java
    @Autowired
    private WorkflowApproverEntityMapper approverMapper;
    @Autowired
    private ObjectMapper objectMapper;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-workflow/src/main/java/org/apache/inlong/manager/workflow/core/impl/WorkflowQueryServiceImpl.java`
#### Snippet
```java
    @Autowired
    private WorkflowTaskEntityMapper taskEntityMapper;
    @Autowired
    private WorkflowEventLogEntityMapper eventLogMapper;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-workflow/src/main/java/org/apache/inlong/manager/workflow/core/impl/WorkflowQueryServiceImpl.java`
#### Snippet
```java
    @Autowired
    private WorkflowProcessEntityMapper processEntityMapper;
    @Autowired
    private WorkflowTaskEntityMapper taskEntityMapper;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-workflow/src/main/java/org/apache/inlong/manager/workflow/core/impl/WorkflowQueryServiceImpl.java`
#### Snippet
```java
    @Autowired
    private WorkflowEventLogEntityMapper eventLogMapper;
    @Autowired
    private WorkflowApproverEntityMapper approverMapper;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-workflow/src/main/java/org/apache/inlong/manager/workflow/core/impl/WorkflowQueryServiceImpl.java`
#### Snippet
```java
public class WorkflowQueryServiceImpl implements WorkflowQueryService {

    @Autowired
    private ProcessDefinitionRepository definitionRepository;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-workflow/src/main/java/org/apache/inlong/manager/workflow/processor/EndEventProcessor.java`
#### Snippet
```java
    @Autowired
    private WorkflowTaskEntityMapper taskEntityMapper;
    @Autowired
    private WorkflowProcessEntityMapper processEntityMapper;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-workflow/src/main/java/org/apache/inlong/manager/workflow/processor/EndEventProcessor.java`
#### Snippet
```java
    @Autowired
    private ProcessEventNotifier processEventNotifier;
    @Autowired
    private WorkflowTaskEntityMapper taskEntityMapper;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-workflow/src/main/java/org/apache/inlong/manager/workflow/processor/EndEventProcessor.java`
#### Snippet
```java
public class EndEventProcessor implements ElementProcessor<EndEvent> {

    @Autowired
    private ProcessEventNotifier processEventNotifier;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-workflow/src/main/java/org/apache/inlong/manager/workflow/processor/StartEventProcessor.java`
#### Snippet
```java
    @Autowired
    private ProcessEventNotifier processEventNotifier;
    @Autowired
    private WorkflowProcessEntityMapper processEntityMapper;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-workflow/src/main/java/org/apache/inlong/manager/workflow/processor/StartEventProcessor.java`
#### Snippet
```java
public class StartEventProcessor extends AbstractNextableElementProcessor<StartEvent> {

    @Autowired
    private ProcessEventNotifier processEventNotifier;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-workflow/src/main/java/org/apache/inlong/manager/workflow/processor/ServiceTaskProcessor.java`
#### Snippet
```java
            TaskStatus.PENDING, TaskStatus.FAILED);

    @Autowired
    private ObjectMapper objectMapper;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-workflow/src/main/java/org/apache/inlong/manager/workflow/processor/ServiceTaskProcessor.java`
#### Snippet
```java
    @Autowired
    private ObjectMapper objectMapper;
    @Autowired
    private WorkflowTaskEntityMapper taskEntityMapper;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-workflow/src/main/java/org/apache/inlong/manager/workflow/processor/ServiceTaskProcessor.java`
#### Snippet
```java
    @Autowired
    private TaskEventNotifier taskEventNotifier;
    @Autowired
    private ProcessEventNotifier processEventNotifier;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-workflow/src/main/java/org/apache/inlong/manager/workflow/processor/ServiceTaskProcessor.java`
#### Snippet
```java
    @Autowired
    private WorkflowTaskEntityMapper taskEntityMapper;
    @Autowired
    private TaskEventNotifier taskEventNotifier;
    @Autowired
```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-workflow/src/main/java/org/apache/inlong/manager/workflow/processor/UserTaskProcessor.java`
#### Snippet
```java
    @Autowired
    private ObjectMapper objectMapper;
    @Autowired
    private TaskEventNotifier taskEventNotifier;

```

### SpringJavaAutowiredFieldsWarningInspection
Field injection is not recommended
in `inlong-manager/manager-workflow/src/main/java/org/apache/inlong/manager/workflow/processor/UserTaskProcessor.java`
#### Snippet
```java
            WorkflowAction.TERMINATE);

    @Autowired
    private ObjectMapper objectMapper;
    @Autowired
```

## RuleId[id=UnnecessaryLocalVariable]
### UnnecessaryLocalVariable
Local variable `str` is redundant
in `inlong-sdk/sort-sdk/src/main/java/org/apache/inlong/sdk/sort/util/Utils.java`
#### Snippet
```java
        NumberFormat format = NumberFormat.getInstance();
        format.setMinimumFractionDigits(fractionDigits);
        String str = format.format(value);
        return str;
    }
```

### UnnecessaryLocalVariable
Local variable `str` is redundant
in `inlong-sdk/sort-sdk/src/main/java/org/apache/inlong/sdk/sort/util/Utils.java`
#### Snippet
```java
        }
        try {
            String str = new String(value, charsetName);
            return str;
        } catch (UnsupportedEncodingException e) {
```

### UnnecessaryLocalVariable
Local variable `resultStr` is redundant
in `inlong-sdk/sort-sdk/src/main/java/org/apache/inlong/sdk/sort/util/StringUtil.java`
#### Snippet
```java
            }

            String resultStr = formatDate(dateStr, inputFormat, format);
            return resultStr;
        }
```

### UnnecessaryLocalVariable
Local variable `snapshot` is redundant
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/metrics/SdkMetricItemSet.java`
#### Snippet
```java
     */
    public List<MetricItem> snapshot() {
        List<MetricItem> snapshot = super.snapshot();
        return snapshot;
    }
```

### UnnecessaryLocalVariable
Local variable `auditFormatTime` is redundant
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/context/SinkContext.java`
#### Snippet
```java
     */
    public long getAuditFormatTime(long msgTime) {
        long auditFormatTime = msgTime - msgTime % auditFormatInterval;
        return auditFormatTime;
    }
```

### UnnecessaryLocalVariable
Local variable `src` is redundant
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/network/ByteArrayToBinaryEncoder.java`
#### Snippet
```java
            return msg;
        } else {
            ChannelBuffer src = (ChannelBuffer) msg;
            return src;
        }
```

### UnnecessaryLocalVariable
Local variable `eventLoopGroup` is redundant
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/utils/EventLoopUtil.java`
#### Snippet
```java
            return new EpollEventLoopGroup(nThreads, threadFactory);
        } else {
            EpollEventLoopGroup eventLoopGroup = new EpollEventLoopGroup(nThreads, threadFactory, () -> {
                return (selectSupplier, hasTasks) -> {
                    return -3;
```

### UnnecessaryLocalVariable
Local variable `object` is redundant
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/codec/ProtocolEncoder.java`
#### Snippet
```java
        ByteBuf buf = null;
        try {
            EncodeObject object = message;
            if (object.getMsgtype() == 3) {
                buf = writeToBuf3(object);
```

### UnnecessaryLocalVariable
Local variable `output` is redundant
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/utils/EncryptUtil.java`
#### Snippet
```java
            // cipher= Cipher.getInstance("RSA", new BouncyCastleProvider());
            cipher.init(Cipher.DECRYPT_MODE, publicKey);
            byte[] output = cipher.doFinal(cipherData);
            return output;
        } catch (NoSuchAlgorithmException e) {
```

### UnnecessaryLocalVariable
Local variable `output` is redundant
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/utils/EncryptUtil.java`
#### Snippet
```java
            // cipher= Cipher.getInstance("RSA", new BouncyCastleProvider());
            cipher.init(Cipher.DECRYPT_MODE, privateKey);
            byte[] output = cipher.doFinal(cipherData);
            return output;
        } catch (NoSuchAlgorithmException e) {
```

### UnnecessaryLocalVariable
Local variable `encodeObject` is redundant
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/ClientHandler.java`
#### Snippet
```java
    public void channelRead0(ChannelHandlerContext ctx, EncodeObject e) {
        try {
            EncodeObject encodeObject = e;
            logger.debug("Channel = {} , msgType = {}", ctx.channel(), encodeObject.getMsgtype());
            if (encodeObject.getMsgtype() != 8) {
```

### UnnecessaryLocalVariable
Local variable `encodeObject` is redundant
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/example/UdpClientExample.java`
#### Snippet
```java
            boolean isGroupIdTransfer, long dt, long seqId, String groupId, String streamId,
            String attr) throws UnsupportedEncodingException {
        EncodeObject encodeObject =
                new EncodeObject(getRandomString(5).getBytes("UTF-8"), msgType,
                        isCompress,
```

### UnnecessaryLocalVariable
Local variable `sourceRecord` is redundant
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/source/reader/external/JdbcSourceFetchTaskContext.java`
#### Snippet
```java
                                    Instant.ofEpochMilli(
                                            value.getInt64(Envelope.FieldName.TIMESTAMP));
                            SourceRecord sourceRecord =
                                    new SourceRecord(
                                            record.sourcePartition(),
```

### UnnecessaryLocalVariable
Local variable `ipPorts` is redundant
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/hudi/src/main/java/org/apache/inlong/sort/hudi/metric/HudiAuditReporter.java`
#### Snippet
```java
            labels.put(key, value);
        });
        String ipPorts = inLongAudit;
        HashSet<String> ipPortList = new HashSet<>();

```

### UnnecessaryLocalVariable
Local variable `jdbcExecOptions` is redundant
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/jdbc/src/main/java/org/apache/inlong/sort/jdbc/internal/JdbcMultiBatchingComm.java`
#### Snippet
```java

    public static JdbcOptions getExecJdbcOptions(JdbcOptions jdbcOptions, String tableIdentifier) {
        JdbcOptions jdbcExecOptions =
                JdbcOptions.builder()
                        .setDBUrl(jdbcOptions.getDbURL() + "/" + getDatabaseNameFromIdentifier(tableIdentifier))
```

### UnnecessaryLocalVariable
Local variable `tubeMQConsumer` is redundant
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/tubemq/src/main/java/org/apache/inlong/sort/tubemq/table/TubeMQTableSource.java`
#### Snippet
```java
                deserialization, metadataConverters, producedTypeInfo, ignoreErrors);

        final FlinkTubeMQConsumer<RowData> tubeMQConsumer = new FlinkTubeMQConsumer(masterAddress, topic, tidSet,
                consumerGroup, tubeMQDeserializer, configuration, sessionKey, innerFormat);
        return tubeMQConsumer;
```

### UnnecessaryLocalVariable
Local variable `data` is redundant
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/mysql/utils/MetaDataUtils.java`
#### Snippet
```java
        Struct messageStruct = (Struct) record.value();
        Struct sourceStruct = messageStruct.getStruct(FieldName.SOURCE);
        GenericRowData data = rowData;
        Map<String, Object> field = (Map<String, Object>) data.getField(0);

```

### UnnecessaryLocalVariable
Local variable `sourceRecord` is redundant
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/mysql/source/utils/RecordUtils.java`
#### Snippet
```java
                                    Instant.ofEpochMilli(
                                            value.getInt64(Envelope.FieldName.TIMESTAMP));
                            SourceRecord sourceRecord =
                                    new SourceRecord(
                                            record.sourcePartition(),
```

### UnnecessaryLocalVariable
Local variable `data` is redundant
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/oracle-cdc/src/main/java/org/apache/inlong/sort/cdc/oracle/table/OracleReadableMetaData.java`
#### Snippet
```java
        long ts = (Long) messageStruct.get(FieldName.TIMESTAMP);
        // actual data
        GenericRowData data = rowData;
        Map<String, Object> field = (Map<String, Object>) data.getField(0);
        List<Map<String, Object>> dataList = new ArrayList<>();
```

### UnnecessaryLocalVariable
Local variable `oracleOffsetContext` is redundant
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/oracle-cdc/src/main/java/org/apache/inlong/sort/cdc/oracle/source/reader/fetch/OracleSourceFetchTaskContext.java`
#### Snippet
```java
                        : oracleSplit.asStreamSplit().getStartingOffset();

        OracleOffsetContext oracleOffsetContext =
                (OracleOffsetContext) loader.load(offset.getOffset());

```

### UnnecessaryLocalVariable
Local variable `data` is redundant
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/postgres-cdc/src/main/java/org/apache/inlong/sort/cdc/postgres/table/PostgreSQLReadableMetaData.java`
#### Snippet
```java
        long ts = (Long) messageStruct.get(FieldName.TIMESTAMP);
        // actual data
        GenericRowData data = rowData;
        Map<String, Object> field = (Map<String, Object>) data.getField(0);
        List<Map<String, Object>> dataList = new ArrayList<>();
```

### UnnecessaryLocalVariable
Local variable `componentType` is redundant
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/postgres-cdc/src/main/java/org/apache/inlong/sort/cdc/postgres/debezium/internal/DebeziumChangeFetcher.java`
#### Snippet
```java
            String typeName = (String) column.get("data_type");
            int jdbcType = getJdbcType(typeName);
            int componentType = jdbcType;
            String charsetName = "utf-8";
            ColumnImpl columnImpl = new ColumnImpl(columnName, position, jdbcType, componentType, typeName, null,
```

### UnnecessaryLocalVariable
Local variable `rhlClient` is redundant
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/elasticsearch-6/src/main/java/org/apache/inlong/sort/elasticsearch6/Elasticsearch6ApiCallBridge.java`
#### Snippet
```java
                RestClient.builder(httpHosts.toArray(new HttpHost[httpHosts.size()]));
        restClientFactory.configureRestClientBuilder(builder);
        RestHighLevelClient rhlClient = new RestHighLevelClient(builder);
        return rhlClient;
    }
```

### UnnecessaryLocalVariable
Local variable `eventLoopGroup` is redundant
in `inlong-audit/audit-sdk/src/main/java/org/apache/inlong/audit/util/EventLoopUtil.java`
#### Snippet
```java
            return new EpollEventLoopGroup(nThreads, threadFactory);
        } else {
            EpollEventLoopGroup eventLoopGroup = new EpollEventLoopGroup(nThreads, threadFactory, () -> {
                return (selectSupplier, hasTasks) -> {
                    return -3;
```

### UnnecessaryLocalVariable
Local variable `eventLoopGroup` is redundant
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/utils/EventLoopUtil.java`
#### Snippet
```java
            return new EpollEventLoopGroup(nThreads, threadFactory);
        } else {
            EpollEventLoopGroup eventLoopGroup = new EpollEventLoopGroup(nThreads, threadFactory, () -> {
                return (selectSupplier, hasTasks) -> {
                    return -3;
```

### UnnecessaryLocalVariable
Local variable `docId` is redundant
in `inlong-audit/audit-store/src/main/java/org/apache/inlong/audit/db/entities/ESDataPo.java`
#### Snippet
```java

    public String getDocId() {
        String docId = ip + dockerId + threadId + sdkTs + packetId + logTs + inlongGroupId + inlongStreamId
                + auditId + count;
        return docId;
```

### UnnecessaryLocalVariable
Local variable `dataPack` is redundant
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/netty/NettyProtocolEncoder.java`
#### Snippet
```java
    @Override
    protected void encode(ChannelHandlerContext chx, RpcDataPack msg, List<Object> out) {
        RpcDataPack dataPack = msg;
        try (ByteArrayOutputStream byteOut = new ByteArrayOutputStream()) {
            byteOut.write(getPackHeader(dataPack).array());
```

### UnnecessaryLocalVariable
Local variable `eventLoopGroup` is redundant
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/netty/EventLoopUtil.java`
#### Snippet
```java
            return new EpollEventLoopGroup(nThreads, threadFactory);
        } else {
            EpollEventLoopGroup eventLoopGroup = new EpollEventLoopGroup(nThreads,
                    threadFactory, () -> {
                        return (selectSupplier, hasTasks) -> {
```

### UnnecessaryLocalVariable
Local variable `children` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/zookeeper/ZKUtil.java`
#### Snippet
```java
            ZooKeeperWatcher zkw, String zNode) throws KeeperException {
        try {
            List<String> children = zkw.getRecoverableZooKeeper().getChildren(zNode,
                    zkw);
            return children;
```

### UnnecessaryLocalVariable
Local variable `revData` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/zookeeper/RecoverableZooKeeper.java`
#### Snippet
```java
        while (true) {
            try {
                byte[] revData = zk.getData(path, watcher, stat);
                return revData;
            } catch (KeeperException e) {
```

### UnnecessaryLocalVariable
Local variable `newData` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/zookeeper/RecoverableZooKeeper.java`
#### Snippet
```java
    public synchronized String create(String path, byte[] data, List<ACL> acl, CreateMode createMode)
            throws KeeperException, InterruptedException {
        byte[] newData = data;
        switch (createMode) {
            case EPHEMERAL:
```

### UnnecessaryLocalVariable
Local variable `newData` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/zookeeper/RecoverableZooKeeper.java`
#### Snippet
```java
            InterruptedException {
        RetryCounter retryCounter = retryCounterFactory.create();
        byte[] newData = data;
        while (true) {
            try {
```

### UnnecessaryLocalVariable
Local variable `revData` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/zookeeper/RecoverableZooKeeper.java`
#### Snippet
```java
        while (true) {
            try {
                byte[] revData = zk.getData(path, watch, stat);
                return revData;
            } catch (KeeperException e) {
```

### UnnecessaryLocalVariable
Local variable `newPath` is redundant
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/zookeeper/RecoverableZooKeeper.java`
#### Snippet
```java
        boolean first = true;
        // String newPath = path + this.identifier;
        String newPath = path;
        while (true) {
            try {
```

### UnnecessaryLocalVariable
Local variable `regionBrokerIdList` is redundant
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/BrokerServiceImpl.java`
#### Snippet
```java
        List<BrokerEntry> brokerEntries = brokerRepository
                .findBrokerEntriesByRegionIdEqualsAndClusterIdEquals(regionId, clusterId);
        List<Long> regionBrokerIdList = brokerEntries.stream().map(BrokerEntry::getBrokerId).collect(
                Collectors.toList());
        return regionBrokerIdList;
```

### UnnecessaryLocalVariable
Local variable `brokerStatusInfo` is redundant
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/NodeServiceImpl.java`
#### Snippet
```java
        String url = TubeConst.SCHEMA + masterEntry.getIp() + ":" + masterEntry.getWebPort()
                + "/" + TubeConst.TUBE_REQUEST_PATH + "?" + ConvertUtils.convertReqToQueryStr(queryReq);
        BrokerStatusInfo brokerStatusInfo = gson.fromJson(masterService.queryMaster(url),
                BrokerStatusInfo.class);
        return brokerStatusInfo;
```

### UnnecessaryLocalVariable
Local variable `existTopicName` is redundant
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/TaskServiceImpl.java`
#### Snippet
```java

    private Set<String> findExistTopics(Long clusterId, Set<String> topicNames) {
        Set<String> existTopicName = topicNames.stream()
                .filter(topicName -> hasAlreadyExistTopicTask(clusterId, topicName, TaskStatusEnum.ADDING
                        .getCode()))
```

### UnnecessaryLocalVariable
Local variable `shiroFilterFactoryBean` is redundant
in `inlong-manager/manager-web/src/main/java/org/apache/inlong/manager/web/auth/ShiroConfig.java`
#### Snippet
```java
    @Bean
    public ShiroFilterFactoryBean shiroFilter(SecurityManager securityManager) {
        ShiroFilterFactoryBean shiroFilterFactoryBean = inlongShiro.getShiroFilter(securityManager);
        return shiroFilterFactoryBean;
    }
```

### UnnecessaryLocalVariable
Local variable `selector` is redundant
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/MessageQueueZoneSinkContext.java`
#### Snippet
```java
            }
            if (selectorObject instanceof CacheClusterSelector) {
                CacheClusterSelector selector = (CacheClusterSelector) selectorObject;
                return selector;
            }
```

### UnnecessaryLocalVariable
Local variable `handler` is redundant
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/common/SinkContext.java`
#### Snippet
```java
            Object handlerObject = handlerClass.getDeclaredConstructor().newInstance();
            if (handlerObject instanceof EventHandler) {
                EventHandler handler = (EventHandler) handlerObject;
                return handler;
            }
```

### UnnecessaryLocalVariable
Local variable `handler` is redundant
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/common/SinkContext.java`
#### Snippet
```java
            Object handlerObject = handlerClass.getDeclaredConstructor().newInstance();
            if (handlerObject instanceof MessageQueueHandler) {
                MessageQueueHandler handler = (MessageQueueHandler) handlerObject;
                return handler;
            }
```

### UnnecessaryLocalVariable
Local variable `eventLoopGroup` is redundant
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/utils/EventLoopUtil.java`
#### Snippet
```java
            return new EpollEventLoopGroup(nThreads, threadFactory);
        } else {
            EpollEventLoopGroup eventLoopGroup = new EpollEventLoopGroup(nThreads, threadFactory, () -> {
                return (selectSupplier, hasTasks) -> {
                    return -3;
```

### UnnecessaryLocalVariable
Local variable `hostIp` is redundant
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/SourceContext.java`
#### Snippet
```java
        Map<String, String> envMap = System.getenv();
        if (envMap.containsKey(INLONG_HOST_IP)) {
            String hostIp = envMap.get(INLONG_HOST_IP);
            return hostIp;
        }
```

### UnnecessaryLocalVariable
Local variable `event` is redundant
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/SimpleMessageHandler.java`
#### Snippet
```java
                commonHeaders.get(AttributeConstants.NODE_IP));
        headers.put(ConfigConstants.MSG_ENCODE_VER, InLongMsgVer.INLONG_V1.getName());
        Event event = EventBuilder.withBody(proxyMessage.getData(), headers);
        return event;
    }
```

### UnnecessaryLocalVariable
Local variable `namePrefix` is redundant
in `inlong-sort-standalone/sort-standalone-common/src/main/java/org/apache/inlong/sort/standalone/utils/InlongLoggerFactory.java`
#### Snippet
```java
            return "Inlong";
        }
        String namePrefix = className.substring(0, index);
        return namePrefix;
    }
```

### UnnecessaryLocalVariable
Local variable `value` is redundant
in `inlong-sort-standalone/sort-standalone-common/src/main/java/org/apache/inlong/sort/standalone/config/holder/CommonPropertiesHolder.java`
#### Snippet
```java
    public static Long getLong(String key) {
        String strValue = get().get(key);
        Long value = (strValue == null) ? null : NumberUtils.toLong(get().get(key));
        return value;
    }
```

### UnnecessaryLocalVariable
Local variable `config` is redundant
in `inlong-sort-standalone/sort-standalone-common/src/main/java/org/apache/inlong/sort/standalone/config/loader/ClassResourceSortClusterConfigLoader.java`
#### Snippet
```java
            confString = confString.substring(index);
            ObjectMapper objectMapper = new ObjectMapper();
            SortClusterConfig config = objectMapper.readValue(confString, SortClusterConfig.class);
            return config;
        } catch (UnsupportedEncodingException e) {
```

### UnnecessaryLocalVariable
Local variable `snapshot` is redundant
in `inlong-sort-standalone/sort-standalone-common/src/main/java/org/apache/inlong/sort/standalone/metrics/SortMetricItemSet.java`
#### Snippet
```java
     */
    public List<MetricItem> snapshot() {
        List<MetricItem> snapshot = super.snapshot();
        return snapshot;
    }
```

### UnnecessaryLocalVariable
Local variable `auditFormatTime` is redundant
in `inlong-sort-standalone/sort-standalone-common/src/main/java/org/apache/inlong/sort/standalone/metrics/SortMetricItem.java`
#### Snippet
```java
     */
    public static long getAuditFormatTime(long msgTime) {
        long auditFormatTime = msgTime - msgTime % CommonPropertiesHolder.getAuditFormatInterval();
        return auditFormatTime;
    }
```

### UnnecessaryLocalVariable
Local variable `dispatchQueue` is redundant
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/SinkContext.java`
#### Snippet
```java
        int maxBufferQueueSizeKb = CommonPropertiesHolder.getInteger(KEY_MAX_BUFFERQUEUE_SIZE_KB,
                DEFAULT_MAX_BUFFERQUEUE_SIZE_KB);
        BufferQueue<U> dispatchQueue = new BufferQueue<>(maxBufferQueueSizeKb);
        return dispatchQueue;
    }
```

### UnnecessaryLocalVariable
Local variable `record` is redundant
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/kafka/DefaultEvent2KafkaRecordHandler.java`
#### Snippet
```java
        outMsg.write(event.getBody());
        byte[] msgContent = outMsg.toByteArray();
        ProducerRecord<String, byte[]> record = new ProducerRecord<>(idConfig.getTopic(), msgContent);
        return record;
    }
```

### UnnecessaryLocalVariable
Local variable `handler` is redundant
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/kafka/KafkaFederationSinkContext.java`
#### Snippet
```java
            Object handlerObject = handlerClass.getDeclaredConstructor().newInstance();
            if (handlerObject instanceof IEvent2KafkaRecordHandler) {
                IEvent2KafkaRecordHandler handler = (IEvent2KafkaRecordHandler) handlerObject;
                return handler;
            }
```

### UnnecessaryLocalVariable
Local variable `msgContent` is redundant
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/pulsar/DefaultEvent2PulsarRecordHandler.java`
#### Snippet
```java
        }
        outMsg.write(event.getBody());
        byte[] msgContent = outMsg.toByteArray();
        return msgContent;
    }
```

### UnnecessaryLocalVariable
Local variable `handler` is redundant
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/pulsar/PulsarFederationSinkContext.java`
#### Snippet
```java
            Object handlerObject = handlerClass.getDeclaredConstructor().newInstance();
            if (handlerObject instanceof IEvent2PulsarRecordHandler) {
                IEvent2PulsarRecordHandler handler = (IEvent2PulsarRecordHandler) handlerObject;
                return handler;
            }
```

### UnnecessaryLocalVariable
Local variable `bulkProcessor` is redundant
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/elasticsearch/EsOutputChannel.java`
#### Snippet
```java
                BiConsumer<BulkRequest, ActionListener<BulkResponse>> consumer = (request, bulkListener) -> esClient
                        .bulkAsync(request, RequestOptions.DEFAULT, bulkListener);
                BulkProcessor bulkProcessor = BulkProcessor.builder(consumer, esListener)
                        .setBulkActions(context.getBulkAction())
                        .setBulkSize(new ByteSizeValue(context.getBulkSizeMb(), ByteSizeUnit.MB))
```

### UnnecessaryLocalVariable
Local variable `handler` is redundant
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/elasticsearch/EsSinkContext.java`
#### Snippet
```java
            Object handlerObject = handlerClass.getDeclaredConstructor().newInstance();
            if (handlerObject instanceof IEvent2IndexRequestHandler) {
                IEvent2IndexRequestHandler handler = (IEvent2IndexRequestHandler) handlerObject;
                return handler;
            }
```

### UnnecessaryLocalVariable
Local variable `handler` is redundant
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/clickhouse/ClickHouseSinkContext.java`
#### Snippet
```java
            Object handlerObject = handlerClass.getDeclaredConstructor().newInstance();
            if (handlerObject instanceof IEventHandler) {
                IEventHandler handler = (IEventHandler) handlerObject;
                return handler;
            }
```

## RuleId[id=ArrayHashCode]
### ArrayHashCode
Array passed to 'Objects.hash()' should be wrapped in 'Arrays.hashcode()'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kafka/src/main/java/org/apache/inlong/sort/kafka/KafkaDynamicSink.java`
#### Snippet
```java
                keyEncodingFormat,
                valueEncodingFormat,
                keyProjection,
                valueProjection,
                keyPrefix,
```

### ArrayHashCode
Array passed to 'Objects.hash()' should be wrapped in 'Arrays.hashcode()'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kafka/src/main/java/org/apache/inlong/sort/kafka/KafkaDynamicSink.java`
#### Snippet
```java
                valueEncodingFormat,
                keyProjection,
                valueProjection,
                keyPrefix,
                topic,
```

### ArrayHashCode
Array passed to 'Objects.hash()' should be wrapped in 'Arrays.hashcode()'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kafka/src/main/java/org/apache/inlong/sort/kafka/table/KafkaDynamicSource.java`
#### Snippet
```java
                keyDecodingFormat,
                valueDecodingFormat,
                keyProjection,
                valueProjection,
                keyPrefix,
```

### ArrayHashCode
Array passed to 'Objects.hash()' should be wrapped in 'Arrays.hashcode()'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kafka/src/main/java/org/apache/inlong/sort/kafka/table/KafkaDynamicSource.java`
#### Snippet
```java
                valueDecodingFormat,
                keyProjection,
                valueProjection,
                keyPrefix,
                topics,
```

## RuleId[id=BusyWait]
### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `inlong-sdk/sdk-common/src/main/java/org/apache/inlong/sdk/commons/admin/AdminTask.java`
#### Snippet
```java
                try {
                    LOG.info("Waiting for channel: " + ch.getName() + " to start. Sleeping for 500 ms");
                    Thread.sleep(500);
                } catch (InterruptedException e) {
                    LOG.error("Interrupted while waiting for channel to start.", e);
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/config/ProxyConfigManager.java`
#### Snippet
```java
                }
                LOGGER.info("sleep time {}", sleepTimeSec);
                Thread.sleep(sleepTimeSec * 1000);
            } catch (Throwable e2) {
                //
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/Sender.java`
#### Snippet
```java
                    }
                    try {
                        Thread.sleep(1000);
                    } catch (InterruptedException e) {
                        // TODO Auto-generated catch block
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `inlong-sort/sort-flink/base/src/main/java/org/apache/inlong/sort/base/dirty/sink/s3/S3Helper.java`
#### Snippet
```java
                }
                try {
                    Thread.sleep(1000L * i);
                } catch (InterruptedException ex) {
                    Thread.currentThread().interrupt();
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/ClientMgr.java`
#### Snippet
```java
                    try {
                        int index = (int) (Math.random() * random.length);
                        Thread.sleep((random[index]) * 1000);
                    } catch (InterruptedException e) {
                        // TODO Auto-generated catch block
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kudu/src/main/java/org/apache/inlong/sort/kudu/source/KuduConsumerTask.java`
#### Snippet
```java
                flush();
                try {
                    Thread.sleep(1);
                } catch (InterruptedException ignored) {
                }
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/jdbc/src/main/java/org/apache/inlong/sort/jdbc/internal/JdbcBatchingOutputFormat.java`
#### Snippet
```java
                }
                try {
                    Thread.sleep(1000 * i);
                } catch (InterruptedException ex) {
                    Thread.currentThread().interrupt();
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kudu/src/main/java/org/apache/inlong/sort/kudu/source/KuduLookupFunction.java`
#### Snippet
```java
                }
                try {
                    Thread.sleep(1000L * retry);
                } catch (InterruptedException e1) {
                    throw new RuntimeException(e1);
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/jdbc/src/main/java/org/apache/inlong/sort/jdbc/internal/JdbcMultiBatchingOutputFormat.java`
#### Snippet
```java
                            updateOneExecutor(true, tableIdentifier);
                            try {
                                Thread.sleep(1000 * retryTimes);
                            } catch (InterruptedException ex) {
                                Thread.currentThread().interrupt();
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kafka/src/main/java/org/apache/inlong/sort/kafka/FlinkKafkaConsumerBase.java`
#### Snippet
```java
                                    if (running && discoveryIntervalMillis != 0) {
                                        try {
                                            Thread.sleep(discoveryIntervalMillis);
                                        } catch (InterruptedException iex) {
                                            // may be interrupted if the consumer was canceled
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/doris/src/main/java/org/apache/inlong/sort/doris/table/DorisDynamicSchemaOutputFormat.java`
#### Snippet
```java
                    LOG.warn("streamload error,switch be: {}",
                            dorisStreamLoad.getLoadUrlStr(tableWithDb[0], tableWithDb[1]), e);
                    Thread.sleep(1000L * i);
                } catch (InterruptedException ex) {
                    Thread.currentThread().interrupt();
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/redis/src/main/java/org/apache/inlong/sort/redis/sink/AbstractRedisSinkFunction.java`
#### Snippet
```java
                try {
                    try {
                        Thread.sleep(timeoutInMillis);
                    } catch (InterruptedException e) {
                        if (running) {
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/pulsar/src/main/java/org/apache/inlong/sort/pulsar/internal/PulsarFetcher.java`
#### Snippet
```java
                    }
                    retries += 1;
                    Thread.sleep(1000);
                }
            }
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/pulsar/src/main/java/org/apache/inlong/sort/pulsar/internal/FlinkPulsarSource.java`
#### Snippet
```java

                            if (running && discoveryIntervalMillis != -1) {
                                Thread.sleep(discoveryIntervalMillis);
                            }
                        }
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/pulsar/src/main/java/org/apache/inlong/sort/pulsar/internal/FlinkPulsarSourceWithoutAdmin.java`
#### Snippet
```java

                            if (running && discoveryIntervalMillis != -1) {
                                Thread.sleep(discoveryIntervalMillis);
                            }
                        }
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/starrocks/src/main/java/org/apache/inlong/sort/starrocks/manager/StarRocksSinkManager.java`
#### Snippet
```java
                }
                try {
                    Thread.sleep(1000L * Math.min(i + 1, 10));
                } catch (InterruptedException ex) {
                    Thread.currentThread().interrupt();
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `inlong-agent/agent-core/src/main/java/org/apache/inlong/agent/core/AgentManager.java`
#### Snippet
```java
                while (true) {
                    try {
                        Thread.sleep(10 * 1000); // 10s check
                        File file = new File(
                                conf.getConfigLocation(AgentConfiguration.DEFAULT_CONFIG_FILE).getFile());
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/PulsarSink.java`
#### Snippet
```java
                    if (PulsarSink.this.overflow) {
                        PulsarSink.this.overflow = false;
                        Thread.currentThread().sleep(10);
                    }
                    if (!resendQueue.isEmpty()) {
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/PulsarSink.java`
#### Snippet
```java
                        } else {
                            try {
                                Thread.sleep(100);
                            } catch (InterruptedException e) {
                                // ignore..
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/KafkaSink.java`
#### Snippet
```java
                    if (KafkaSink.this.overflow) {
                        KafkaSink.this.overflow = false;
                        Thread.sleep(10);
                    }
                    if (!resendQueue.isEmpty()) {
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/KafkaSink.java`
#### Snippet
```java
                        } else {
                            try {
                                Thread.sleep(100);
                            } catch (InterruptedException e) {
                                // ignore..
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/node/Application.java`
#### Snippet
```java
                    logger.info("Waiting for channel: " + ch.getName()
                            + " to start. Sleeping for 500 ms");
                    Thread.sleep(500);
                } catch (InterruptedException e) {
                    logger.error("Interrupted while waiting for channel to start.", e);
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/TubeSink.java`
#### Snippet
```java
                    if (TubeSink.this.overflow) {
                        TubeSink.this.overflow = false;
                        Thread.sleep(10);
                    }
                    if (!resendQueue.isEmpty()) {
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/TubeSink.java`
#### Snippet
```java
                        } else {
                            try {
                                Thread.sleep(100);
                            } catch (InterruptedException e) {
                                // ignore..
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `inlong-audit/audit-store/src/main/java/org/apache/inlong/audit/service/AuditMsgConsumerServer.java`
#### Snippet
```java
                    }
                    LOG.info("MQ config may not be registered yet, wait for 5s and try again");
                    Thread.sleep(INTERVAL_MS);
                }
            }
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `inlong-common/src/main/java/org/apache/inlong/common/monitor/MonitorIndexExt.java`
#### Snippet
```java
            while (!bShutDown) {
                try {
                    Thread.sleep(intervalSec * 1000L);
                    for (String str : counterMap.keySet()) {
                        counterMap.computeIfPresent(str, (s, atomicLong) -> {
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `inlong-common/src/main/java/org/apache/inlong/common/monitor/StatRunner.java`
#### Snippet
```java
        while (!this.shutDownFlag) {
            try {
                Thread.sleep((long) (this.intervalSec * 1000));
                Iterator iterator = this.moniterNames.iterator();

```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `inlong-common/src/main/java/org/apache/inlong/common/monitor/MonitorIndex.java`
#### Snippet
```java
            while (!bShutDown) {
                try {
                    Thread.sleep(intervalSec * 1000L);
                    for (String str : counterMap.keySet()) {
                        counterMap.computeIfPresent(str, (s, s2) -> {
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/daemon/AbstractDaemonService.java`
#### Snippet
```java
        while (!isStopped()) {
            try {
                Thread.sleep(intervalMs);
                loopProcess(strBuff);
            } catch (InterruptedException e) {
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/factory/TubeSingleSessionFactory.java`
#### Snippet
```java
        while (isShutDown.get()) {
            try {
                Thread.sleep(50);
            } catch (Throwable e) {
                break;
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/MessageFetchManager.java`
#### Snippet
```java
            if (haveProcessingThread) {
                try {
                    Thread.sleep(150);
                } catch (InterruptedException e1) {
                    break;
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/RmtDataCache.java`
#### Snippet
```java
            if (needWait) {
                try {
                    Thread.sleep(200);
                } catch (InterruptedException e1) {
                    break;
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/producer/ProducerManager.java`
#### Snippet
```java
                    }
                }
                Thread.sleep(1000);
            } catch (Throwable e) {
                sBuilder.delete(0, sBuilder.length());
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/stats/TrafficStatsService.java`
#### Snippet
```java
            }
            try {
                Thread.sleep(2);
            } catch (InterruptedException e) {
                break;
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/BaseMessageConsumer.java`
#### Snippet
```java
            }
            retryTimesRegister2Broker++;
            Thread.sleep(1000);
        }
        for (Partition partition : unRegPartitions) {
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/BaseMessageConsumer.java`
#### Snippet
```java
                    // Wait for next heartbeat
                    lastHeartbeatTime2Broker = System.currentTimeMillis();
                    Thread.sleep(consumerConfig.getHeartbeatPeriodMs());
                } catch (Throwable e) {
                    clientStatsInfo.bookHB2BrokerException();
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/BaseMessageConsumer.java`
#### Snippet
```java
        do {
            try {
                Thread.sleep(200);
            } catch (InterruptedException e) {
                break;
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/SimpleClientBalanceConsumer.java`
#### Snippet
```java
                    // Wait for next heartbeat
                    lastHeartbeatTime2Broker = System.currentTimeMillis();
                    Thread.sleep(consumerConfig.getHeartbeatPeriodMs());
                } catch (Throwable e) {
                    lastHeartbeatTime2Broker = System.currentTimeMillis();
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/heartbeat/HeartbeatManager.java`
#### Snippet
```java
                            }
                        }
                        Thread.sleep(1000 * 1);
                    } catch (Throwable t) {
                        logger.error(new StringBuilder(256)
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `inlong-tubemq/tubemq-example/src/main/java/org/apache/inlong/tubemq/example/MAMessageProducerExample.java`
#### Snippet
```java
                logger.info("Sending task is running, total = {}, finished = {}",
                        needSentCnt, totalSentCnt.get());
                Thread.sleep(30000);
            }
        } catch (Throwable e) {
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/queue/pulsar/PulsarOperator.java`
#### Snippet
```java
                        res = pulsarAdmin.lookups().lookupPartitionedTopic(fullTopicName);
                        try {
                            Thread.sleep(500);
                        } catch (InterruptedException e) {
                            LOGGER.error("Thread has been interrupted");
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/node/Application.java`
#### Snippet
```java
                try {
                    LOGGER.info("sleeping for 500 ms to wait for channel: {} to start", ch.getName());
                    Thread.sleep(500);
                } catch (InterruptedException e) {
                    LOGGER.error("interrupted while waiting for channel to start: ", e);
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/SortTask.java`
#### Snippet
```java
                try {
                    LOG.info("Waiting for channel: " + ch.getName() + " to start. Sleeping for 500 ms");
                    Thread.sleep(500);
                } catch (InterruptedException e) {
                    LOG.error("Interrupted while waiting for channel to start.", e);
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/pulsar/PulsarFederationWorker.java`
#### Snippet
```java
                    tx.commit();
                    this.context.addSendFailMetric();
                    Thread.sleep(context.getProcessInterval());
                    return;
                }
```

## RuleId[id=RedundantFileCreation]
### RedundantFileCreation
`new File` is redundant
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/utils/TSSLEngineUtil.java`
#### Snippet
```java
            throws Exception {

        InputStream keyStoreStream = new FileInputStream(new File(keyStorePath));
        InputStream trustStoreStream = new FileInputStream(new File(trustStorePath));

```

### RedundantFileCreation
`new File` is redundant
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/utils/TSSLEngineUtil.java`
#### Snippet
```java

        InputStream keyStoreStream = new FileInputStream(new File(keyStorePath));
        InputStream trustStoreStream = new FileInputStream(new File(trustStorePath));

        return createSSLEngine(keyStoreStream, keyStorePassword, trustStoreStream,
```

## RuleId[id=ArraysAsListWithZeroOrOneArgument]
### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `inlong-sdk/sort-sdk/src/main/java/org/apache/inlong/sdk/sort/metrics/SortSdkPrometheusMetricListener.java`
#### Snippet
```java
        CounterMetricFamily totalCounter = new CounterMetricFamily("SortSdkTotal",
                "The metrics of SortSdk.",
                Arrays.asList("dimension"));
        totalCounter.addMetric(Collections.singletonList(M_CONSUME_TIMES), metricItem.consumeTimes.get());
        totalCounter.addMetric(Collections.singletonList(M_CONSUME_SIZE), metricItem.consumeSize.get());
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/util/RecordUtils.java`
#### Snippet
```java
public class RecordUtils {

    private static final List<String> NUMBER_TYPE = Arrays.asList("NUMBER");
    private static final List<String> FLOAT_TYPE = Arrays.asList("FLOAT", "BINARY_FLOAT");
    private static final List<String> DOUBLE_TYPE = Arrays.asList("DOUBLE PRECISION", "BINARY_DOUBLE");
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/tubemq/src/main/java/org/apache/inlong/sort/tubemq/table/TubeMQDynamicTableFactory.java`
#### Snippet
```java
    public static final String IDENTIFIER = "tubemq";

    public static final List<String> INNERFORMATTYPE = Arrays.asList("inlong-msg");

    public static boolean innerFormat = false;
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/mysql/source/reader/MySqlSourceReader.java`
#### Snippet
```java
        } else {
            LOG.info("The meta of binlog split {} has been collected success", splitId);
            this.addSplits(Arrays.asList(binlogSplit));
        }
    }
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` to create an empty List
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mongodb-cdc/src/main/java/org/apache/inlong/sort/cdc/mongodb/debezium/utils/RecordUtils.java`
#### Snippet
```java
                    BsonType.MAX_KEY, BsonType.ARRAY,
                    BsonType.DOCUMENT, BsonType.BINARY);
    private static final List<BsonType> ARRAY_TYPE = Arrays.asList();

    private static final List<BsonType> BINARY_TYPE = Arrays.asList();
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` to create an empty List
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mongodb-cdc/src/main/java/org/apache/inlong/sort/cdc/mongodb/debezium/utils/RecordUtils.java`
#### Snippet
```java
    private static final List<BsonType> BOOL_TYPE = Arrays.asList(BsonType.BOOLEAN);
    private static final List<BsonType> DOUBLE_TYPE = Arrays.asList(BsonType.DOUBLE, BsonType.DECIMAL128);
    private static final List<BsonType> DECIMAL_TYPE = Arrays.asList();

    private static final List<BsonType> TIMESTAMP_TYPE = Arrays
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mongodb-cdc/src/main/java/org/apache/inlong/sort/cdc/mongodb/debezium/utils/RecordUtils.java`
#### Snippet
```java
    public static final String DOCUMENT_TO_FIELD = "to";
    private static final List<BsonType> INT_TYPE = Arrays.asList(BsonType.INT32, BsonType.INT64);
    private static final List<BsonType> BOOL_TYPE = Arrays.asList(BsonType.BOOLEAN);
    private static final List<BsonType> DOUBLE_TYPE = Arrays.asList(BsonType.DOUBLE, BsonType.DECIMAL128);
    private static final List<BsonType> DECIMAL_TYPE = Arrays.asList();
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` to create an empty List
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mongodb-cdc/src/main/java/org/apache/inlong/sort/cdc/mongodb/debezium/utils/RecordUtils.java`
#### Snippet
```java
    private static final List<BsonType> ARRAY_TYPE = Arrays.asList();

    private static final List<BsonType> BINARY_TYPE = Arrays.asList();

    private static final Map<String, Integer> SQL_TYPE_MAP =
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `inlong-agent/agent-common/src/main/java/org/apache/inlong/agent/metrics/AgentPrometheusMetricListener.java`
#### Snippet
```java
        // total
        CounterMetricFamily totalCounter = new CounterMetricFamily("agent", "metrics_of_agent_node_total",
                Arrays.asList(DEFAULT_DIMENSION_LABEL));
        totalCounter.addMetric(Arrays.asList(M_JOB_RUNNING_COUNT), metricItem.jobRunningCount.get());
        totalCounter.addMetric(Arrays.asList(M_JOB_FATAL_COUNT), metricItem.jobFatalCount.get());
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `inlong-agent/agent-common/src/main/java/org/apache/inlong/agent/metrics/AgentPrometheusMetricListener.java`
#### Snippet
```java
        CounterMetricFamily totalCounter = new CounterMetricFamily("agent", "metrics_of_agent_node_total",
                Arrays.asList(DEFAULT_DIMENSION_LABEL));
        totalCounter.addMetric(Arrays.asList(M_JOB_RUNNING_COUNT), metricItem.jobRunningCount.get());
        totalCounter.addMetric(Arrays.asList(M_JOB_FATAL_COUNT), metricItem.jobFatalCount.get());
        totalCounter.addMetric(Arrays.asList(M_TASK_RUNNING_COUNT), metricItem.taskRunningCount.get());
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `inlong-agent/agent-common/src/main/java/org/apache/inlong/agent/metrics/AgentPrometheusMetricListener.java`
#### Snippet
```java
                Arrays.asList(DEFAULT_DIMENSION_LABEL));
        totalCounter.addMetric(Arrays.asList(M_JOB_RUNNING_COUNT), metricItem.jobRunningCount.get());
        totalCounter.addMetric(Arrays.asList(M_JOB_FATAL_COUNT), metricItem.jobFatalCount.get());
        totalCounter.addMetric(Arrays.asList(M_TASK_RUNNING_COUNT), metricItem.taskRunningCount.get());
        totalCounter.addMetric(Arrays.asList(M_TASK_RETRYING_COUNT), metricItem.taskRetryingCount.get());
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `inlong-agent/agent-common/src/main/java/org/apache/inlong/agent/metrics/AgentPrometheusMetricListener.java`
#### Snippet
```java
        totalCounter.addMetric(Arrays.asList(M_JOB_RUNNING_COUNT), metricItem.jobRunningCount.get());
        totalCounter.addMetric(Arrays.asList(M_JOB_FATAL_COUNT), metricItem.jobFatalCount.get());
        totalCounter.addMetric(Arrays.asList(M_TASK_RUNNING_COUNT), metricItem.taskRunningCount.get());
        totalCounter.addMetric(Arrays.asList(M_TASK_RETRYING_COUNT), metricItem.taskRetryingCount.get());
        totalCounter.addMetric(Arrays.asList(M_TASK_FATAL_COUNT), metricItem.taskFatalCount.get());
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `inlong-agent/agent-common/src/main/java/org/apache/inlong/agent/metrics/AgentPrometheusMetricListener.java`
#### Snippet
```java
        totalCounter.addMetric(Arrays.asList(M_JOB_FATAL_COUNT), metricItem.jobFatalCount.get());
        totalCounter.addMetric(Arrays.asList(M_TASK_RUNNING_COUNT), metricItem.taskRunningCount.get());
        totalCounter.addMetric(Arrays.asList(M_TASK_RETRYING_COUNT), metricItem.taskRetryingCount.get());
        totalCounter.addMetric(Arrays.asList(M_TASK_FATAL_COUNT), metricItem.taskFatalCount.get());
        totalCounter.addMetric(Arrays.asList(M_SINK_SUCCESS_COUNT), metricItem.sinkSuccessCount.get());
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `inlong-agent/agent-common/src/main/java/org/apache/inlong/agent/metrics/AgentPrometheusMetricListener.java`
#### Snippet
```java
        totalCounter.addMetric(Arrays.asList(M_TASK_RUNNING_COUNT), metricItem.taskRunningCount.get());
        totalCounter.addMetric(Arrays.asList(M_TASK_RETRYING_COUNT), metricItem.taskRetryingCount.get());
        totalCounter.addMetric(Arrays.asList(M_TASK_FATAL_COUNT), metricItem.taskFatalCount.get());
        totalCounter.addMetric(Arrays.asList(M_SINK_SUCCESS_COUNT), metricItem.sinkSuccessCount.get());
        totalCounter.addMetric(Arrays.asList(M_SINK_FAIL_COUNT), metricItem.sinkFailCount.get());
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `inlong-agent/agent-common/src/main/java/org/apache/inlong/agent/metrics/AgentPrometheusMetricListener.java`
#### Snippet
```java
        totalCounter.addMetric(Arrays.asList(M_TASK_RETRYING_COUNT), metricItem.taskRetryingCount.get());
        totalCounter.addMetric(Arrays.asList(M_TASK_FATAL_COUNT), metricItem.taskFatalCount.get());
        totalCounter.addMetric(Arrays.asList(M_SINK_SUCCESS_COUNT), metricItem.sinkSuccessCount.get());
        totalCounter.addMetric(Arrays.asList(M_SINK_FAIL_COUNT), metricItem.sinkFailCount.get());
        totalCounter.addMetric(Arrays.asList(M_SOURCE_SUCCESS_COUNT), metricItem.sourceSuccessCount.get());
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `inlong-agent/agent-common/src/main/java/org/apache/inlong/agent/metrics/AgentPrometheusMetricListener.java`
#### Snippet
```java
        totalCounter.addMetric(Arrays.asList(M_TASK_FATAL_COUNT), metricItem.taskFatalCount.get());
        totalCounter.addMetric(Arrays.asList(M_SINK_SUCCESS_COUNT), metricItem.sinkSuccessCount.get());
        totalCounter.addMetric(Arrays.asList(M_SINK_FAIL_COUNT), metricItem.sinkFailCount.get());
        totalCounter.addMetric(Arrays.asList(M_SOURCE_SUCCESS_COUNT), metricItem.sourceSuccessCount.get());
        totalCounter.addMetric(Arrays.asList(M_SOURCE_FAIL_COUNT), metricItem.sourceFailCount.get());
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `inlong-agent/agent-common/src/main/java/org/apache/inlong/agent/metrics/AgentPrometheusMetricListener.java`
#### Snippet
```java
        totalCounter.addMetric(Arrays.asList(M_SINK_SUCCESS_COUNT), metricItem.sinkSuccessCount.get());
        totalCounter.addMetric(Arrays.asList(M_SINK_FAIL_COUNT), metricItem.sinkFailCount.get());
        totalCounter.addMetric(Arrays.asList(M_SOURCE_SUCCESS_COUNT), metricItem.sourceSuccessCount.get());
        totalCounter.addMetric(Arrays.asList(M_SOURCE_FAIL_COUNT), metricItem.sourceFailCount.get());
        totalCounter.addMetric(Arrays.asList(M_PLUGIN_READ_COUNT), metricItem.pluginReadCount.get());
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `inlong-agent/agent-common/src/main/java/org/apache/inlong/agent/metrics/AgentPrometheusMetricListener.java`
#### Snippet
```java
        totalCounter.addMetric(Arrays.asList(M_SINK_FAIL_COUNT), metricItem.sinkFailCount.get());
        totalCounter.addMetric(Arrays.asList(M_SOURCE_SUCCESS_COUNT), metricItem.sourceSuccessCount.get());
        totalCounter.addMetric(Arrays.asList(M_SOURCE_FAIL_COUNT), metricItem.sourceFailCount.get());
        totalCounter.addMetric(Arrays.asList(M_PLUGIN_READ_COUNT), metricItem.pluginReadCount.get());
        totalCounter.addMetric(Arrays.asList(M_PLUGIN_SEND_COUNT), metricItem.pluginSendCount.get());
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `inlong-agent/agent-common/src/main/java/org/apache/inlong/agent/metrics/AgentPrometheusMetricListener.java`
#### Snippet
```java
        totalCounter.addMetric(Arrays.asList(M_SOURCE_SUCCESS_COUNT), metricItem.sourceSuccessCount.get());
        totalCounter.addMetric(Arrays.asList(M_SOURCE_FAIL_COUNT), metricItem.sourceFailCount.get());
        totalCounter.addMetric(Arrays.asList(M_PLUGIN_READ_COUNT), metricItem.pluginReadCount.get());
        totalCounter.addMetric(Arrays.asList(M_PLUGIN_SEND_COUNT), metricItem.pluginSendCount.get());
        totalCounter.addMetric(Arrays.asList(M_PLUGIN_READ_FAIL_COUNT), metricItem.pluginReadFailCount.get());
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `inlong-agent/agent-common/src/main/java/org/apache/inlong/agent/metrics/AgentPrometheusMetricListener.java`
#### Snippet
```java
        totalCounter.addMetric(Arrays.asList(M_SOURCE_FAIL_COUNT), metricItem.sourceFailCount.get());
        totalCounter.addMetric(Arrays.asList(M_PLUGIN_READ_COUNT), metricItem.pluginReadCount.get());
        totalCounter.addMetric(Arrays.asList(M_PLUGIN_SEND_COUNT), metricItem.pluginSendCount.get());
        totalCounter.addMetric(Arrays.asList(M_PLUGIN_READ_FAIL_COUNT), metricItem.pluginReadFailCount.get());
        totalCounter.addMetric(Arrays.asList(M_PLUGIN_SEND_FAIL_COUNT), metricItem.pluginSendFailCount.get());
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `inlong-agent/agent-common/src/main/java/org/apache/inlong/agent/metrics/AgentPrometheusMetricListener.java`
#### Snippet
```java
        totalCounter.addMetric(Arrays.asList(M_PLUGIN_READ_COUNT), metricItem.pluginReadCount.get());
        totalCounter.addMetric(Arrays.asList(M_PLUGIN_SEND_COUNT), metricItem.pluginSendCount.get());
        totalCounter.addMetric(Arrays.asList(M_PLUGIN_READ_FAIL_COUNT), metricItem.pluginReadFailCount.get());
        totalCounter.addMetric(Arrays.asList(M_PLUGIN_SEND_FAIL_COUNT), metricItem.pluginSendFailCount.get());
        totalCounter.addMetric(Arrays.asList(M_PLUGIN_READ_SUCCESS_COUNT), metricItem.pluginReadSuccessCount.get());
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `inlong-agent/agent-common/src/main/java/org/apache/inlong/agent/metrics/AgentPrometheusMetricListener.java`
#### Snippet
```java
        totalCounter.addMetric(Arrays.asList(M_PLUGIN_SEND_COUNT), metricItem.pluginSendCount.get());
        totalCounter.addMetric(Arrays.asList(M_PLUGIN_READ_FAIL_COUNT), metricItem.pluginReadFailCount.get());
        totalCounter.addMetric(Arrays.asList(M_PLUGIN_SEND_FAIL_COUNT), metricItem.pluginSendFailCount.get());
        totalCounter.addMetric(Arrays.asList(M_PLUGIN_READ_SUCCESS_COUNT), metricItem.pluginReadSuccessCount.get());
        totalCounter.addMetric(Arrays.asList(M_PLUGIN_SEND_SUCCESS_COUNT), metricItem.pluginSendSuccessCount.get());
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `inlong-agent/agent-common/src/main/java/org/apache/inlong/agent/metrics/AgentPrometheusMetricListener.java`
#### Snippet
```java
        totalCounter.addMetric(Arrays.asList(M_PLUGIN_READ_FAIL_COUNT), metricItem.pluginReadFailCount.get());
        totalCounter.addMetric(Arrays.asList(M_PLUGIN_SEND_FAIL_COUNT), metricItem.pluginSendFailCount.get());
        totalCounter.addMetric(Arrays.asList(M_PLUGIN_READ_SUCCESS_COUNT), metricItem.pluginReadSuccessCount.get());
        totalCounter.addMetric(Arrays.asList(M_PLUGIN_SEND_SUCCESS_COUNT), metricItem.pluginSendSuccessCount.get());
        List<MetricFamilySamples> mfs = new ArrayList<>();
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `inlong-agent/agent-common/src/main/java/org/apache/inlong/agent/metrics/AgentPrometheusMetricListener.java`
#### Snippet
```java
        totalCounter.addMetric(Arrays.asList(M_PLUGIN_SEND_FAIL_COUNT), metricItem.pluginSendFailCount.get());
        totalCounter.addMetric(Arrays.asList(M_PLUGIN_READ_SUCCESS_COUNT), metricItem.pluginReadSuccessCount.get());
        totalCounter.addMetric(Arrays.asList(M_PLUGIN_SEND_SUCCESS_COUNT), metricItem.pluginSendSuccessCount.get());
        List<MetricFamilySamples> mfs = new ArrayList<>();
        mfs.add(totalCounter);
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/stats/prometheus/BrokerPromMetricService.java`
#### Snippet
```java
                        .append("&group=serviceStatus").toString(),
                        "The service status metrics of TubeMQ-Broker node.",
                        Arrays.asList("serviceStatus"));
        strBuff.delete(0, strBuff.length());
        BrokerSrvStatsHolder.snapShort(statsMap);
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/stats/prometheus/BrokerPromMetricService.java`
#### Snippet
```java
        BrokerSrvStatsHolder.snapShort(statsMap);
        for (Map.Entry<String, Long> entry : statsMap.entrySet()) {
            srvStatusCounter.addMetric(Arrays.asList(entry.getKey()), entry.getValue());
        }
        mfs.add(srvStatusCounter);
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/stats/prometheus/BrokerPromMetricService.java`
#### Snippet
```java
                        .append("&group=webAPI").toString(),
                        "The web api call metrics of TubeMQ-Broker node.",
                        Arrays.asList("webAPI"));
        strBuff.delete(0, strBuff.length());
        statsMap.clear();
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/stats/prometheus/BrokerPromMetricService.java`
#### Snippet
```java
        WebCallStatsHolder.snapShort(statsMap);
        for (Map.Entry<String, Long> entry : statsMap.entrySet()) {
            webAPICounter.addMetric(Arrays.asList(entry.getKey()), entry.getValue());
        }
        mfs.add(webAPICounter);
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/stats/prometheus/BrokerPromMetricService.java`
#### Snippet
```java
                        .append("&group=msgStore").toString(),
                        "The message store metrics of TubeMQ-Broker node.",
                        Arrays.asList("msgStore"));
        strBuff.delete(0, strBuff.length());
        // set topic's statistic status
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/fileconfig/ADConfig.java`
#### Snippet
```java
    // audit proxy server addresses
    private HashSet<String> auditProxyAddrSet =
            new HashSet<>(Arrays.asList("127.0.0.1:10081"));
    // file path for audit cache data
    private String auditCacheFilePath = "/data/inlong/audit";
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebAdminFlowRuleHandler.java`
#### Snippet
```java
    private static final List<Integer> allowedPriorityVal = Arrays.asList(1, 2, 3);
    private static final Set<String> rsvGroupNameSet =
            new HashSet<>(Arrays.asList(TServerConstants.TOKEN_DEFAULT_FLOW_CONTROL));

    public WebAdminFlowRuleHandler(TMaster master) {
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/stats/prometheus/MasterPromMetricService.java`
#### Snippet
```java
                        .append("&group=serviceStatus").toString(),
                        "The service status metrics of TubeMQ-Master node.",
                        Arrays.asList("serviceStatus"));
        strBuff.delete(0, strBuff.length());
        MasterSrvStatsHolder.snapShort(statsMap);
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/stats/prometheus/MasterPromMetricService.java`
#### Snippet
```java
        MasterSrvStatsHolder.snapShort(statsMap);
        for (Map.Entry<String, Long> entry : statsMap.entrySet()) {
            srvStatusCounter.addMetric(Arrays.asList(entry.getKey()), entry.getValue());
        }
        mfs.add(srvStatusCounter);
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/stats/prometheus/MasterPromMetricService.java`
#### Snippet
```java
                        .append("&group=webAPI").toString(),
                        "The web api call metrics of TubeMQ-Master node.",
                        Arrays.asList("webAPI"));
        strBuff.delete(0, strBuff.length());
        statsMap.clear();
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/stats/prometheus/MasterPromMetricService.java`
#### Snippet
```java
        WebCallStatsHolder.snapShort(statsMap);
        for (Map.Entry<String, Long> entry : statsMap.entrySet()) {
            webAPICounter.addMetric(Arrays.asList(entry.getKey()), entry.getValue());
        }
        mfs.add(webAPICounter);
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/metrics/prometheus/PrometheusMetricListener.java`
#### Snippet
```java
        CounterMetricFamily totalCounter = new CounterMetricFamily(metricName,
                "The metrics of dataproxy node.",
                Arrays.asList("dimension"));
        totalCounter.addMetric(Arrays.asList(M_READ_SUCCESS_COUNT), metricItem.readSuccessCount.get());
        totalCounter.addMetric(Arrays.asList(M_READ_SUCCESS_SIZE), metricItem.readSuccessSize.get());
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/metrics/prometheus/PrometheusMetricListener.java`
#### Snippet
```java
                "The metrics of dataproxy node.",
                Arrays.asList("dimension"));
        totalCounter.addMetric(Arrays.asList(M_READ_SUCCESS_COUNT), metricItem.readSuccessCount.get());
        totalCounter.addMetric(Arrays.asList(M_READ_SUCCESS_SIZE), metricItem.readSuccessSize.get());
        totalCounter.addMetric(Arrays.asList(M_READ_FAIL_COUNT), metricItem.readFailCount.get());
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/metrics/prometheus/PrometheusMetricListener.java`
#### Snippet
```java
                Arrays.asList("dimension"));
        totalCounter.addMetric(Arrays.asList(M_READ_SUCCESS_COUNT), metricItem.readSuccessCount.get());
        totalCounter.addMetric(Arrays.asList(M_READ_SUCCESS_SIZE), metricItem.readSuccessSize.get());
        totalCounter.addMetric(Arrays.asList(M_READ_FAIL_COUNT), metricItem.readFailCount.get());
        totalCounter.addMetric(Arrays.asList(M_READ_FAIL_SIZE), metricItem.readFailSize.get());
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/metrics/prometheus/PrometheusMetricListener.java`
#### Snippet
```java
        totalCounter.addMetric(Arrays.asList(M_READ_SUCCESS_COUNT), metricItem.readSuccessCount.get());
        totalCounter.addMetric(Arrays.asList(M_READ_SUCCESS_SIZE), metricItem.readSuccessSize.get());
        totalCounter.addMetric(Arrays.asList(M_READ_FAIL_COUNT), metricItem.readFailCount.get());
        totalCounter.addMetric(Arrays.asList(M_READ_FAIL_SIZE), metricItem.readFailSize.get());
        // send
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/metrics/prometheus/PrometheusMetricListener.java`
#### Snippet
```java
        totalCounter.addMetric(Arrays.asList(M_READ_SUCCESS_SIZE), metricItem.readSuccessSize.get());
        totalCounter.addMetric(Arrays.asList(M_READ_FAIL_COUNT), metricItem.readFailCount.get());
        totalCounter.addMetric(Arrays.asList(M_READ_FAIL_SIZE), metricItem.readFailSize.get());
        // send
        totalCounter.addMetric(Arrays.asList(M_SEND_COUNT), metricItem.sendCount.get());
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/metrics/prometheus/PrometheusMetricListener.java`
#### Snippet
```java
        totalCounter.addMetric(Arrays.asList(M_READ_FAIL_SIZE), metricItem.readFailSize.get());
        // send
        totalCounter.addMetric(Arrays.asList(M_SEND_COUNT), metricItem.sendCount.get());
        totalCounter.addMetric(Arrays.asList(M_SEND_SIZE), metricItem.sendSize.get());
        totalCounter.addMetric(Arrays.asList(M_SEND_SUCCESS_COUNT), metricItem.sendSuccessCount.get());
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/metrics/prometheus/PrometheusMetricListener.java`
#### Snippet
```java
        // send
        totalCounter.addMetric(Arrays.asList(M_SEND_COUNT), metricItem.sendCount.get());
        totalCounter.addMetric(Arrays.asList(M_SEND_SIZE), metricItem.sendSize.get());
        totalCounter.addMetric(Arrays.asList(M_SEND_SUCCESS_COUNT), metricItem.sendSuccessCount.get());
        totalCounter.addMetric(Arrays.asList(M_SEND_SUCCESS_SIZE), metricItem.sendSuccessSize.get());
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/metrics/prometheus/PrometheusMetricListener.java`
#### Snippet
```java
        totalCounter.addMetric(Arrays.asList(M_SEND_COUNT), metricItem.sendCount.get());
        totalCounter.addMetric(Arrays.asList(M_SEND_SIZE), metricItem.sendSize.get());
        totalCounter.addMetric(Arrays.asList(M_SEND_SUCCESS_COUNT), metricItem.sendSuccessCount.get());
        totalCounter.addMetric(Arrays.asList(M_SEND_SUCCESS_SIZE), metricItem.sendSuccessSize.get());
        totalCounter.addMetric(Arrays.asList(M_SEND_FAIL_COUNT), metricItem.sendFailCount.get());
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/metrics/prometheus/PrometheusMetricListener.java`
#### Snippet
```java
        totalCounter.addMetric(Arrays.asList(M_SEND_SIZE), metricItem.sendSize.get());
        totalCounter.addMetric(Arrays.asList(M_SEND_SUCCESS_COUNT), metricItem.sendSuccessCount.get());
        totalCounter.addMetric(Arrays.asList(M_SEND_SUCCESS_SIZE), metricItem.sendSuccessSize.get());
        totalCounter.addMetric(Arrays.asList(M_SEND_FAIL_COUNT), metricItem.sendFailCount.get());
        totalCounter.addMetric(Arrays.asList(M_SEND_FAIL_SIZE), metricItem.sendFailSize.get());
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/metrics/prometheus/PrometheusMetricListener.java`
#### Snippet
```java
        totalCounter.addMetric(Arrays.asList(M_SEND_SUCCESS_COUNT), metricItem.sendSuccessCount.get());
        totalCounter.addMetric(Arrays.asList(M_SEND_SUCCESS_SIZE), metricItem.sendSuccessSize.get());
        totalCounter.addMetric(Arrays.asList(M_SEND_FAIL_COUNT), metricItem.sendFailCount.get());
        totalCounter.addMetric(Arrays.asList(M_SEND_FAIL_SIZE), metricItem.sendFailSize.get());
        // duration
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/metrics/prometheus/PrometheusMetricListener.java`
#### Snippet
```java
        totalCounter.addMetric(Arrays.asList(M_SEND_SUCCESS_SIZE), metricItem.sendSuccessSize.get());
        totalCounter.addMetric(Arrays.asList(M_SEND_FAIL_COUNT), metricItem.sendFailCount.get());
        totalCounter.addMetric(Arrays.asList(M_SEND_FAIL_SIZE), metricItem.sendFailSize.get());
        // duration
        totalCounter.addMetric(Arrays.asList(M_SINK_DURATION), metricItem.sinkDuration.get());
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/metrics/prometheus/PrometheusMetricListener.java`
#### Snippet
```java
        totalCounter.addMetric(Arrays.asList(M_SEND_FAIL_SIZE), metricItem.sendFailSize.get());
        // duration
        totalCounter.addMetric(Arrays.asList(M_SINK_DURATION), metricItem.sinkDuration.get());
        totalCounter.addMetric(Arrays.asList(M_NODE_DURATION), metricItem.nodeDuration.get());
        totalCounter.addMetric(Arrays.asList(M_WHOLE_DURATION), metricItem.wholeDuration.get());
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/metrics/prometheus/PrometheusMetricListener.java`
#### Snippet
```java
        // duration
        totalCounter.addMetric(Arrays.asList(M_SINK_DURATION), metricItem.sinkDuration.get());
        totalCounter.addMetric(Arrays.asList(M_NODE_DURATION), metricItem.nodeDuration.get());
        totalCounter.addMetric(Arrays.asList(M_WHOLE_DURATION), metricItem.wholeDuration.get());
        List<MetricFamilySamples> mfs = new ArrayList<>();
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/metrics/prometheus/PrometheusMetricListener.java`
#### Snippet
```java
        totalCounter.addMetric(Arrays.asList(M_SINK_DURATION), metricItem.sinkDuration.get());
        totalCounter.addMetric(Arrays.asList(M_NODE_DURATION), metricItem.nodeDuration.get());
        totalCounter.addMetric(Arrays.asList(M_WHOLE_DURATION), metricItem.wholeDuration.get());
        List<MetricFamilySamples> mfs = new ArrayList<>();
        mfs.add(totalCounter);
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/metrics/prometheus/PrometheusMetricListener.java`
#### Snippet
```java
        CounterMetricFamily totalCounter = new CounterMetricFamily(metricName + "&group=total",
                "The metrics of SortStandalone node.",
                Arrays.asList("dimension"));
        totalCounter.addMetric(Arrays.asList(M_READ_SUCCESS_COUNT), metricItem.readSuccessCount.get());
        totalCounter.addMetric(Arrays.asList(M_READ_SUCCESS_SIZE), metricItem.readSuccessSize.get());
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/metrics/prometheus/PrometheusMetricListener.java`
#### Snippet
```java
                "The metrics of SortStandalone node.",
                Arrays.asList("dimension"));
        totalCounter.addMetric(Arrays.asList(M_READ_SUCCESS_COUNT), metricItem.readSuccessCount.get());
        totalCounter.addMetric(Arrays.asList(M_READ_SUCCESS_SIZE), metricItem.readSuccessSize.get());
        totalCounter.addMetric(Arrays.asList(M_READ_FAIL_COUNT), metricItem.readFailCount.get());
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/metrics/prometheus/PrometheusMetricListener.java`
#### Snippet
```java
                Arrays.asList("dimension"));
        totalCounter.addMetric(Arrays.asList(M_READ_SUCCESS_COUNT), metricItem.readSuccessCount.get());
        totalCounter.addMetric(Arrays.asList(M_READ_SUCCESS_SIZE), metricItem.readSuccessSize.get());
        totalCounter.addMetric(Arrays.asList(M_READ_FAIL_COUNT), metricItem.readFailCount.get());
        totalCounter.addMetric(Arrays.asList(M_READ_FAIL_SIZE), metricItem.readFailSize.get());
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/metrics/prometheus/PrometheusMetricListener.java`
#### Snippet
```java
        totalCounter.addMetric(Arrays.asList(M_READ_SUCCESS_COUNT), metricItem.readSuccessCount.get());
        totalCounter.addMetric(Arrays.asList(M_READ_SUCCESS_SIZE), metricItem.readSuccessSize.get());
        totalCounter.addMetric(Arrays.asList(M_READ_FAIL_COUNT), metricItem.readFailCount.get());
        totalCounter.addMetric(Arrays.asList(M_READ_FAIL_SIZE), metricItem.readFailSize.get());
        //
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/metrics/prometheus/PrometheusMetricListener.java`
#### Snippet
```java
        totalCounter.addMetric(Arrays.asList(M_READ_SUCCESS_SIZE), metricItem.readSuccessSize.get());
        totalCounter.addMetric(Arrays.asList(M_READ_FAIL_COUNT), metricItem.readFailCount.get());
        totalCounter.addMetric(Arrays.asList(M_READ_FAIL_SIZE), metricItem.readFailSize.get());
        //
        totalCounter.addMetric(Arrays.asList(M_SEND_COUNT), metricItem.sendCount.get());
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/metrics/prometheus/PrometheusMetricListener.java`
#### Snippet
```java
        totalCounter.addMetric(Arrays.asList(M_READ_FAIL_SIZE), metricItem.readFailSize.get());
        //
        totalCounter.addMetric(Arrays.asList(M_SEND_COUNT), metricItem.sendCount.get());
        totalCounter.addMetric(Arrays.asList(M_SEND_SIZE), metricItem.sendSize.get());
        //
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/metrics/prometheus/PrometheusMetricListener.java`
#### Snippet
```java
        //
        totalCounter.addMetric(Arrays.asList(M_SEND_COUNT), metricItem.sendCount.get());
        totalCounter.addMetric(Arrays.asList(M_SEND_SIZE), metricItem.sendSize.get());
        //
        totalCounter.addMetric(Arrays.asList(M_SEND_SUCCESS_COUNT), metricItem.sendSuccessCount.get());
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/metrics/prometheus/PrometheusMetricListener.java`
#### Snippet
```java
        totalCounter.addMetric(Arrays.asList(M_SEND_SIZE), metricItem.sendSize.get());
        //
        totalCounter.addMetric(Arrays.asList(M_SEND_SUCCESS_COUNT), metricItem.sendSuccessCount.get());
        totalCounter.addMetric(Arrays.asList(M_SEND_SUCCESS_SIZE), metricItem.sendSuccessSize.get());
        totalCounter.addMetric(Arrays.asList(M_SEND_FAIL_COUNT), metricItem.sendFailCount.get());
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/metrics/prometheus/PrometheusMetricListener.java`
#### Snippet
```java
        //
        totalCounter.addMetric(Arrays.asList(M_SEND_SUCCESS_COUNT), metricItem.sendSuccessCount.get());
        totalCounter.addMetric(Arrays.asList(M_SEND_SUCCESS_SIZE), metricItem.sendSuccessSize.get());
        totalCounter.addMetric(Arrays.asList(M_SEND_FAIL_COUNT), metricItem.sendFailCount.get());
        totalCounter.addMetric(Arrays.asList(M_SEND_FAIL_SIZE), metricItem.sendFailSize.get());
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/metrics/prometheus/PrometheusMetricListener.java`
#### Snippet
```java
        totalCounter.addMetric(Arrays.asList(M_SEND_SUCCESS_COUNT), metricItem.sendSuccessCount.get());
        totalCounter.addMetric(Arrays.asList(M_SEND_SUCCESS_SIZE), metricItem.sendSuccessSize.get());
        totalCounter.addMetric(Arrays.asList(M_SEND_FAIL_COUNT), metricItem.sendFailCount.get());
        totalCounter.addMetric(Arrays.asList(M_SEND_FAIL_SIZE), metricItem.sendFailSize.get());
        //
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/metrics/prometheus/PrometheusMetricListener.java`
#### Snippet
```java
        totalCounter.addMetric(Arrays.asList(M_SEND_SUCCESS_SIZE), metricItem.sendSuccessSize.get());
        totalCounter.addMetric(Arrays.asList(M_SEND_FAIL_COUNT), metricItem.sendFailCount.get());
        totalCounter.addMetric(Arrays.asList(M_SEND_FAIL_SIZE), metricItem.sendFailSize.get());
        //
        totalCounter.addMetric(Arrays.asList(M_SINK_DURATION), metricItem.sinkDuration.get());
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/metrics/prometheus/PrometheusMetricListener.java`
#### Snippet
```java
        totalCounter.addMetric(Arrays.asList(M_SEND_FAIL_SIZE), metricItem.sendFailSize.get());
        //
        totalCounter.addMetric(Arrays.asList(M_SINK_DURATION), metricItem.sinkDuration.get());
        totalCounter.addMetric(Arrays.asList(M_NODE_DURATION), metricItem.nodeDuration.get());
        totalCounter.addMetric(Arrays.asList(M_WHOLE_DURATION), metricItem.wholeDuration.get());
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/metrics/prometheus/PrometheusMetricListener.java`
#### Snippet
```java
        //
        totalCounter.addMetric(Arrays.asList(M_SINK_DURATION), metricItem.sinkDuration.get());
        totalCounter.addMetric(Arrays.asList(M_NODE_DURATION), metricItem.nodeDuration.get());
        totalCounter.addMetric(Arrays.asList(M_WHOLE_DURATION), metricItem.wholeDuration.get());
        List<MetricFamilySamples> mfs = new ArrayList<>();
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/metrics/prometheus/PrometheusMetricListener.java`
#### Snippet
```java
        totalCounter.addMetric(Arrays.asList(M_SINK_DURATION), metricItem.sinkDuration.get());
        totalCounter.addMetric(Arrays.asList(M_NODE_DURATION), metricItem.nodeDuration.get());
        totalCounter.addMetric(Arrays.asList(M_WHOLE_DURATION), metricItem.wholeDuration.get());
        List<MetricFamilySamples> mfs = new ArrayList<>();
        mfs.add(totalCounter);
```

## RuleId[id=IntegerDivisionInFloatingPointContext]
### IntegerDivisionInFloatingPointContext
`maxBrokerCount / inConsumerInfo.getSourceCount()`: integer division in floating-point context
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/paramcheck/PBParameterUtils.java`
#### Snippet
```java
        int maxBrokerCount =
                brokerRunManager.getSubTopicMaxBrokerCount(inConsumerInfo.getTopicSet());
        int curBClientRate = (int) Math.floor(maxBrokerCount / inConsumerInfo.getSourceCount());
        if (curBClientRate > allowRate) {
            int minClientCnt = maxBrokerCount / allowRate;
```

### IntegerDivisionInFloatingPointContext
`maxBrokerCount / newConsumerList.size()`: integer division in floating-point context
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/balance/DefaultLoadBalancer.java`
#### Snippet
```java
                int maxBrokerCount =
                        brokerRunManager.getSubTopicMaxBrokerCount(topicSet);
                int curBClientRate = (int) Math.floor(maxBrokerCount / newConsumerList.size());
                if (curBClientRate > allowRate) {
                    int minClientCnt = maxBrokerCount / allowRate;
```

### IntegerDivisionInFloatingPointContext
`maxBrokerCount / consumerList.size()`: integer division in floating-point context
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/balance/DefaultLoadBalancer.java`
#### Snippet
```java
            int maxBrokerCount =
                    brokerRunManager.getSubTopicMaxBrokerCount(topicSet);
            int curBClientRate = (int) Math.floor(maxBrokerCount / consumerList.size());
            if (curBClientRate > allowRate) {
                int minClientCnt = maxBrokerCount / allowRate;
```

## RuleId[id=PointlessBooleanExpression]
### PointlessBooleanExpression
`offerSuc != true` can be simplified to '!offerSuc'
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/sources/reader/file/FileReaderOperator.java`
#### Snippet
```java
            try {
                boolean offerSuc = queue.offer(line, 1, TimeUnit.SECONDS);
                while (offerSuc != true) {
                    offerSuc = queue.offer(line, 1, TimeUnit.SECONDS);
                }
```

## RuleId[id=UseBulkOperation]
### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `inlong-sdk/sort-sdk/src/main/java/org/apache/inlong/sdk/sort/util/Utils.java`
#### Snippet
```java
        Set<T> result = new HashSet<T>();
        for (T v : values) {
            result.add(v);
        }
        return result;
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `inlong-sdk/sort-sdk/src/main/java/org/apache/inlong/sdk/sort/util/Utils.java`
#### Snippet
```java
        List<T> result = new ArrayList<T>();
        for (T v : values) {
            result.add(v);
        }
        return result;
```

### UseBulkOperation
Iteration can be replaced with bulk 'Map.putAll()' call
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/debezium/internal/FlinkOffsetBackingStore.java`
#### Snippet
```java
                () -> {
                    for (Map.Entry<ByteBuffer, ByteBuffer> entry : values.entrySet()) {
                        data.put(entry.getKey(), entry.getValue());
                    }
                    if (callback != null) {
```

### UseBulkOperation
Iteration can be replaced with bulk 'Map.putAll()' call
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mongodb-cdc/src/main/java/org/apache/inlong/sort/cdc/mongodb/debezium/internal/FlinkOffsetBackingStore.java`
#### Snippet
```java
                () -> {
                    for (Map.Entry<ByteBuffer, ByteBuffer> entry : values.entrySet()) {
                        data.put(entry.getKey(), entry.getValue());
                    }
                    if (callback != null) {
```

### UseBulkOperation
Iteration can be replaced with bulk 'Map.putAll()' call
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/configuration/Configuration.java`
#### Snippet
```java
        synchronized (this.confData) {
            for (Map.Entry<String, Object> entry : this.confData.entrySet()) {
                props.put(entry.getKey(), entry.getValue());
            }
        }
```

### UseBulkOperation
Iteration can be replaced with bulk 'Map.putAll()' call
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/channel/MemoryChannel.java`
#### Snippet
```java
        dimensions.put(KEY_INLONG_STREAM_ID, inlongStreamId);
        dimens.forEach((key, value) -> {
            dimensions.put(key, value);
        });
        return this.metricItemSet.findMetricItem(dimensions);
```

### UseBulkOperation
Iteration can be replaced with bulk 'Map.putAll()' call
in `inlong-audit/audit-common/src/main/java/org/apache/inlong/audit/file/holder/PropertiesConfigHolder.java`
#### Snippet
```java
        if (holder != null) {
            for (Map.Entry<String, String> entry : holder.entrySet()) {
                tmpHolder.put(entry.getKey(), entry.getValue());
            }
        }
```

## RuleId[id=UnstableApiUsage]
### UnstableApiUsage
'com.google.common.hash.Hashing' is marked unstable with @Beta
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/utils/ConsistencyHashUtil.java`
#### Snippet
```java

    public static String hashMurMurHash(String key, int seed) {
        HashFunction hashFunction = Hashing.murmur3_128(seed);
        return hashFunction.hashString(key, StandardCharsets.UTF_8).toString();
    }
```

### UnstableApiUsage
'murmur3_128(int)' is declared in unstable class 'com.google.common.hash.Hashing' marked with @Beta
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/utils/ConsistencyHashUtil.java`
#### Snippet
```java

    public static String hashMurMurHash(String key, int seed) {
        HashFunction hashFunction = Hashing.murmur3_128(seed);
        return hashFunction.hashString(key, StandardCharsets.UTF_8).toString();
    }
```

### UnstableApiUsage
'split(java.lang.CharSequence)' is declared in unstable class 'com.google.common.base.Splitter.MapSplitter' marked with @Beta
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/codec/EncodeObject.java`
#### Snippet
```java
            return;
        }
        Map<String, String> backAttrs = new HashMap<>(MAP_SPLITTER.split(attributes));
        if (backAttrs.containsKey(AttributeConstants.MESSAGE_ID)) {
            this.messageId = backAttrs.get(AttributeConstants.MESSAGE_ID);
```

### UnstableApiUsage
'com.google.common.base.Splitter.MapSplitter' is marked unstable with @Beta
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/codec/EncodeObject.java`
#### Snippet
```java
public class EncodeObject {

    private static final Splitter.MapSplitter MAP_SPLITTER = Splitter.on(AttributeConstants.SEPARATOR).trimResults()
            .withKeyValueSeparator(AttributeConstants.KEY_VALUE_SEPARATOR);

```

### UnstableApiUsage
'withKeyValueSeparator(java.lang.String)' is marked unstable with @Beta
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/codec/EncodeObject.java`
#### Snippet
```java

    private static final Splitter.MapSplitter MAP_SPLITTER = Splitter.on(AttributeConstants.SEPARATOR).trimResults()
            .withKeyValueSeparator(AttributeConstants.KEY_VALUE_SEPARATOR);

    private byte[] bodyBytes;
```

### UnstableApiUsage
'com.google.common.util.concurrent.RateLimiter' is marked unstable with @Beta
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/PulsarSink.java`
#### Snippet
```java
        diskIORatePerSec = context.getLong(DISK_IO_RATE_PER_SEC, 0L);
        if (diskIORatePerSec != 0) {
            diskRateLimiter = RateLimiter.create(diskIORatePerSec);
        }

```

### UnstableApiUsage
'create(double)' is declared in unstable class 'com.google.common.util.concurrent.RateLimiter' marked with @Beta
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/PulsarSink.java`
#### Snippet
```java
        diskIORatePerSec = context.getLong(DISK_IO_RATE_PER_SEC, 0L);
        if (diskIORatePerSec != 0) {
            diskRateLimiter = RateLimiter.create(diskIORatePerSec);
        }

```

### UnstableApiUsage
'acquire(int)' is declared in unstable class 'com.google.common.util.concurrent.RateLimiter' marked with @Beta
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/PulsarSink.java`
#### Snippet
```java
            if (event != null) {
                if (diskRateLimiter != null) {
                    diskRateLimiter.acquire(event.getBody().length);
                }
                if (!eventQueue.offer(event, 3 * 1000, TimeUnit.MILLISECONDS)) {
```

### UnstableApiUsage
'com.google.common.util.concurrent.RateLimiter' is marked unstable with @Beta
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/PulsarSink.java`
#### Snippet
```java
    private long diskIORatePerSec;

    private RateLimiter diskRateLimiter;

    /*
```

### UnstableApiUsage
'com.google.common.util.concurrent.RateLimiter' is marked unstable with @Beta
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/KafkaSink.java`
#### Snippet
```java
    private Integer logEveryNEvents;
    private long diskIORatePerSec;
    private RateLimiter diskRateLimiter;

    // properties for stat
```

### UnstableApiUsage
'com.google.common.util.concurrent.RateLimiter' is marked unstable with @Beta
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/KafkaSink.java`
#### Snippet
```java
        diskIORatePerSec = context.getLong(DISK_IO_RATE_PER_SEC, 0L);
        if (diskIORatePerSec != 0) {
            diskRateLimiter = RateLimiter.create(diskIORatePerSec);
        }

```

### UnstableApiUsage
'create(double)' is declared in unstable class 'com.google.common.util.concurrent.RateLimiter' marked with @Beta
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/KafkaSink.java`
#### Snippet
```java
        diskIORatePerSec = context.getLong(DISK_IO_RATE_PER_SEC, 0L);
        if (diskIORatePerSec != 0) {
            diskRateLimiter = RateLimiter.create(diskIORatePerSec);
        }

```

### UnstableApiUsage
'acquire(int)' is declared in unstable class 'com.google.common.util.concurrent.RateLimiter' marked with @Beta
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/KafkaSink.java`
#### Snippet
```java
            if (event != null) {
                if (diskRateLimiter != null) {
                    diskRateLimiter.acquire(event.getBody().length);
                }
                if (!eventQueue.offer(event, 3 * 1000, TimeUnit.MILLISECONDS)) {
```

### UnstableApiUsage
'com.google.common.util.concurrent.RateLimiter' is marked unstable with @Beta
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/TubeSink.java`
#### Snippet
```java
        diskIORatePerSec = context.getLong("disk-io-rate-per-sec", 0L);
        if (diskIORatePerSec != 0) {
            diskRateLimiter = RateLimiter.create(diskIORatePerSec);
        }

```

### UnstableApiUsage
'create(double)' is declared in unstable class 'com.google.common.util.concurrent.RateLimiter' marked with @Beta
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/TubeSink.java`
#### Snippet
```java
        diskIORatePerSec = context.getLong("disk-io-rate-per-sec", 0L);
        if (diskIORatePerSec != 0) {
            diskRateLimiter = RateLimiter.create(diskIORatePerSec);
        }

```

### UnstableApiUsage
'acquire(int)' is declared in unstable class 'com.google.common.util.concurrent.RateLimiter' marked with @Beta
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/TubeSink.java`
#### Snippet
```java
            if (event != null) {
                if (diskRateLimiter != null) {
                    diskRateLimiter.acquire(event.getBody().length);
                }
                if (!eventQueue.offer(event, 3 * 1000, TimeUnit.MILLISECONDS)) {
```

### UnstableApiUsage
'com.google.common.util.concurrent.RateLimiter' is marked unstable with @Beta
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/TubeSink.java`
#### Snippet
```java
    private LinkedBlockingQueue<Event> eventQueue;
    private long diskIORatePerSec;
    private RateLimiter diskRateLimiter;
    private String masterHostAndPortList;
    private Integer logEveryNEvents;
```

### UnstableApiUsage
'com.google.common.base.Splitter.MapSplitter' is marked unstable with @Beta
in `inlong-audit/audit-common/src/main/java/org/apache/inlong/audit/file/ConfigHolder.java`
#### Snippet
```java
public abstract class ConfigHolder {

    public static final Splitter.MapSplitter MAP_SPLITTER =
            Splitter.on(AttributeConstants.SEPARATOR)
                    .trimResults().withKeyValueSeparator(AttributeConstants.KEY_VALUE_SEPARATOR);
```

### UnstableApiUsage
'withKeyValueSeparator(java.lang.String)' is marked unstable with @Beta
in `inlong-audit/audit-common/src/main/java/org/apache/inlong/audit/file/ConfigHolder.java`
#### Snippet
```java
    public static final Splitter.MapSplitter MAP_SPLITTER =
            Splitter.on(AttributeConstants.SEPARATOR)
                    .trimResults().withKeyValueSeparator(AttributeConstants.KEY_VALUE_SEPARATOR);
    private static final Logger LOG = LoggerFactory.getLogger(ConfigHolder.class);
    private final String fileName;
```

### UnstableApiUsage
'com.google.common.base.Splitter.MapSplitter' is marked unstable with @Beta
in `inlong-common/src/main/java/org/apache/inlong/common/msg/InLongMsg.java`
#### Snippet
```java
            Joiner.on(AttributeConstants.SEPARATOR)
                    .withKeyValueSeparator(AttributeConstants.KEY_VALUE_SEPARATOR);
    private static final Splitter.MapSplitter MAP_SPLITTER =
            Splitter.on(AttributeConstants.SEPARATOR)
                    .trimResults().withKeyValueSeparator(AttributeConstants.KEY_VALUE_SEPARATOR);
```

### UnstableApiUsage
'withKeyValueSeparator(java.lang.String)' is marked unstable with @Beta
in `inlong-common/src/main/java/org/apache/inlong/common/msg/InLongMsg.java`
#### Snippet
```java
    private static final Splitter.MapSplitter MAP_SPLITTER =
            Splitter.on(AttributeConstants.SEPARATOR)
                    .trimResults().withKeyValueSeparator(AttributeConstants.KEY_VALUE_SEPARATOR);

    static class DataBuffer {
```

### UnstableApiUsage
'split(java.lang.CharSequence)' is declared in unstable class 'com.google.common.base.Splitter.MapSplitter' marked with @Beta
in `inlong-common/src/main/java/org/apache/inlong/common/msg/InLongMsg.java`
#### Snippet
```java
            String strAttr = new String(attr);

            commonAttrMap = new HashMap<String, String>(MAP_SPLITTER.split(strAttr));
        }

```

### UnstableApiUsage
'split(java.lang.CharSequence)' is declared in unstable class 'com.google.common.base.Splitter.MapSplitter' marked with @Beta
in `inlong-common/src/main/java/org/apache/inlong/common/msg/InLongMsg.java`
#### Snippet
```java
                String attrBufStr = new String(attrBuf);

                finalAttrMap = new HashMap<String, String>(MAP_SPLITTER.split(attrBufStr));
                finalAttrMap.putAll(commonAttrMap);

```

### UnstableApiUsage
'getType()' is declared in unstable class 'com.google.common.reflect.TypeToken' marked with @Beta
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/controller/cluster/ClusterController.java`
#### Snippet
```java
        List<TopicViewRes> topicViewResList = gson.fromJson(fromJson.toString(),
                new TypeToken<List<TopicViewRes>>() {
                }.getType());
        for (TopicViewRes topicViewRes : topicViewResList) {
            storeCount = storeCount + (int) Math.ceil(topicViewRes.getTotalCfgNumStore());
```

### UnstableApiUsage
'TypeToken()' is declared in unstable class 'com.google.common.reflect.TypeToken' marked with @Beta
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/controller/cluster/ClusterController.java`
#### Snippet
```java
        int partitionCount = 0;
        List<TopicQueryRes> topicQueryResList = gson.fromJson(jsonData.toString(),
                new TypeToken<List<TopicQueryRes>>() {
                }.getType());
        for (TopicQueryRes topicQueryRes : topicQueryResList) {
```

### UnstableApiUsage
'com.google.common.reflect.TypeToken' is marked unstable with @Beta
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/controller/cluster/ClusterController.java`
#### Snippet
```java
        int partitionCount = 0;
        List<TopicQueryRes> topicQueryResList = gson.fromJson(jsonData.toString(),
                new TypeToken<List<TopicQueryRes>>() {
                }.getType());
        for (TopicQueryRes topicQueryRes : topicQueryResList) {
```

### UnstableApiUsage
'TypeToken()' is declared in unstable class 'com.google.common.reflect.TypeToken' marked with @Beta
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/controller/cluster/ClusterController.java`
#### Snippet
```java
        int consumerCount = 0;
        List<ConsumerInfoRes> topicViewResList = gson.fromJson(jsonData.toString(),
                new TypeToken<List<ConsumerInfoRes>>() {
                }.getType());
        for (ConsumerInfoRes consumerInfoRes : topicViewResList) {
```

### UnstableApiUsage
'com.google.common.reflect.TypeToken' is marked unstable with @Beta
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/controller/cluster/ClusterController.java`
#### Snippet
```java
        int consumerCount = 0;
        List<ConsumerInfoRes> topicViewResList = gson.fromJson(jsonData.toString(),
                new TypeToken<List<ConsumerInfoRes>>() {
                }.getType());
        for (ConsumerInfoRes consumerInfoRes : topicViewResList) {
```

### UnstableApiUsage
'getType()' is declared in unstable class 'com.google.common.reflect.TypeToken' marked with @Beta
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/controller/cluster/ClusterController.java`
#### Snippet
```java
        List<ConsumerInfoRes> topicViewResList = gson.fromJson(jsonData.toString(),
                new TypeToken<List<ConsumerInfoRes>>() {
                }.getType());
        for (ConsumerInfoRes consumerInfoRes : topicViewResList) {
            consumerCount = (int) Math.ceil(consumerInfoRes.getConsumerNum());
```

### UnstableApiUsage
'getType()' is declared in unstable class 'com.google.common.reflect.TypeToken' marked with @Beta
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/controller/cluster/ClusterController.java`
#### Snippet
```java
        List<ConsumerGroupInfoRes> groupList = gson.fromJson(jsonData.toString(),
                new TypeToken<List<ConsumerGroupInfoRes>>() {
                }.getType());
        for (ConsumerGroupInfoRes groupInfoRes : groupList) {
            consumerGroupCount = consumerGroupCount + (int) Math.ceil(groupInfoRes.getGroupCount());
```

### UnstableApiUsage
'TypeToken()' is declared in unstable class 'com.google.common.reflect.TypeToken' marked with @Beta
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/controller/cluster/ClusterController.java`
#### Snippet
```java
        int storeCount = 0;
        List<TopicViewRes> topicViewResList = gson.fromJson(fromJson.toString(),
                new TypeToken<List<TopicViewRes>>() {
                }.getType());
        for (TopicViewRes topicViewRes : topicViewResList) {
```

### UnstableApiUsage
'com.google.common.reflect.TypeToken' is marked unstable with @Beta
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/controller/cluster/ClusterController.java`
#### Snippet
```java
        int storeCount = 0;
        List<TopicViewRes> topicViewResList = gson.fromJson(fromJson.toString(),
                new TypeToken<List<TopicViewRes>>() {
                }.getType());
        for (TopicViewRes topicViewRes : topicViewResList) {
```

### UnstableApiUsage
'getType()' is declared in unstable class 'com.google.common.reflect.TypeToken' marked with @Beta
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/controller/cluster/ClusterController.java`
#### Snippet
```java
        List<TopicQueryRes> topicQueryResList = gson.fromJson(jsonData.toString(),
                new TypeToken<List<TopicQueryRes>>() {
                }.getType());
        for (TopicQueryRes topicQueryRes : topicQueryResList) {
            String totalCfgNumPart = topicQueryRes.getTotalCfgNumPart();
```

### UnstableApiUsage
'TypeToken()' is declared in unstable class 'com.google.common.reflect.TypeToken' marked with @Beta
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/controller/cluster/ClusterController.java`
#### Snippet
```java
        JsonArray jsonData = gson.fromJson(data, JsonArray.class);
        List<ConsumerGroupInfoRes> groupList = gson.fromJson(jsonData.toString(),
                new TypeToken<List<ConsumerGroupInfoRes>>() {
                }.getType());
        for (ConsumerGroupInfoRes groupInfoRes : groupList) {
```

### UnstableApiUsage
'com.google.common.reflect.TypeToken' is marked unstable with @Beta
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/controller/cluster/ClusterController.java`
#### Snippet
```java
        JsonArray jsonData = gson.fromJson(data, JsonArray.class);
        List<ConsumerGroupInfoRes> groupList = gson.fromJson(jsonData.toString(),
                new TypeToken<List<ConsumerGroupInfoRes>>() {
                }.getType());
        for (ConsumerGroupInfoRes groupInfoRes : groupList) {
```

### UnstableApiUsage
'split(java.lang.CharSequence)' is declared in unstable class 'com.google.common.base.Splitter.MapSplitter' marked with @Beta
in `inlong-manager/manager-pojo/src/main/java/org/apache/inlong/manager/pojo/sort/standalone/SortSourceClusterInfo.java`
#### Snippet
```java
        if (extTagMap.isEmpty() && StringUtils.isNotBlank(extTag)) {
            try {
                extTagMap = MAP_SPLITTER.split(extTag);
            } catch (Throwable t) {
                LOGGER.error("fail to parse cluster ext tag params", t);
```

### UnstableApiUsage
'com.google.common.base.Splitter.MapSplitter' is marked unstable with @Beta
in `inlong-manager/manager-pojo/src/main/java/org/apache/inlong/manager/pojo/sort/standalone/SortSourceClusterInfo.java`
#### Snippet
```java
    private static final Logger LOGGER = LoggerFactory.getLogger(SortSourceClusterInfo.class);
    private static final Gson GSON = new Gson();
    private static final Splitter.MapSplitter MAP_SPLITTER = Splitter.on("&").trimResults()
            .withKeyValueSeparator("=");
    private static final String KEY_IS_CONSUMABLE = "consumer";
```

### UnstableApiUsage
'withKeyValueSeparator(java.lang.String)' is marked unstable with @Beta
in `inlong-manager/manager-pojo/src/main/java/org/apache/inlong/manager/pojo/sort/standalone/SortSourceClusterInfo.java`
#### Snippet
```java
    private static final Gson GSON = new Gson();
    private static final Splitter.MapSplitter MAP_SPLITTER = Splitter.on("&").trimResults()
            .withKeyValueSeparator("=");
    private static final String KEY_IS_CONSUMABLE = "consumer";

```

### UnstableApiUsage
'withKeyValueSeparator(java.lang.String)' is marked unstable with @Beta
in `inlong-manager/manager-client/src/main/java/org/apache/inlong/manager/client/api/impl/LowLevelInlongClientImpl.java`
#### Snippet
```java
     */
    public LowLevelInlongClientImpl(String serviceUrl, ClientConfiguration configuration) {
        Map<String, String> hostPorts = Splitter.on(URL_SPLITTER).withKeyValueSeparator(HOST_SPLITTER)
                .split(serviceUrl);
        if (MapUtils.isEmpty(hostPorts)) {
```

### UnstableApiUsage
'split(java.lang.CharSequence)' is declared in unstable class 'com.google.common.base.Splitter.MapSplitter' marked with @Beta
in `inlong-manager/manager-client/src/main/java/org/apache/inlong/manager/client/api/impl/LowLevelInlongClientImpl.java`
#### Snippet
```java
    public LowLevelInlongClientImpl(String serviceUrl, ClientConfiguration configuration) {
        Map<String, String> hostPorts = Splitter.on(URL_SPLITTER).withKeyValueSeparator(HOST_SPLITTER)
                .split(serviceUrl);
        if (MapUtils.isEmpty(hostPorts)) {
            throw new IllegalArgumentException(String.format("Unsupported serviceUrl : %s", serviceUrl));
```

### UnstableApiUsage
'withKeyValueSeparator(java.lang.String)' is marked unstable with @Beta
in `inlong-manager/manager-client/src/main/java/org/apache/inlong/manager/client/api/impl/InlongClientImpl.java`
#### Snippet
```java

    public InlongClientImpl(String serviceUrl, ClientConfiguration configuration) {
        Map<String, String> hostPorts = Splitter.on(URL_SPLITTER).withKeyValueSeparator(HOST_SPLITTER)
                .split(serviceUrl);
        if (MapUtils.isEmpty(hostPorts)) {
```

### UnstableApiUsage
'split(java.lang.CharSequence)' is declared in unstable class 'com.google.common.base.Splitter.MapSplitter' marked with @Beta
in `inlong-manager/manager-client/src/main/java/org/apache/inlong/manager/client/api/impl/InlongClientImpl.java`
#### Snippet
```java
    public InlongClientImpl(String serviceUrl, ClientConfiguration configuration) {
        Map<String, String> hostPorts = Splitter.on(URL_SPLITTER).withKeyValueSeparator(HOST_SPLITTER)
                .split(serviceUrl);
        if (MapUtils.isEmpty(hostPorts)) {
            throw new IllegalArgumentException(String.format("Unsupported serviceUrl: %s", serviceUrl));
```

### UnstableApiUsage
'drain(java.util.concurrent.BlockingQueue, java.util.Collection, int, long, java.util.concurrent.TimeUnit)' is marked unstable with @Beta
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/operationlog/OperationLogPool.java`
#### Snippet
```java
            buffer.clear();
            try {
                int size = Queues.drain(OPERATION_POOL, buffer, BUFFER_SIZE, TIMEOUT_SECOND, TimeUnit.SECONDS);
                if (buffer.isEmpty()) {
                    continue;
```

### UnstableApiUsage
'split(java.lang.CharSequence)' is declared in unstable class 'com.google.common.base.Splitter.MapSplitter' marked with @Beta
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/repository/DataProxyConfigRepository.java`
#### Snippet
```java
                continue;
            }
            Map<String, String> tagMap = MAP_SPLITTER.split(cacheCluster.getExtTag());
            String producerTag = tagMap.getOrDefault(CACHE_CLUSTER_PRODUCER_TAG, Boolean.TRUE.toString());
            if (StringUtils.equalsIgnoreCase(producerTag, Boolean.TRUE.toString())) {
```

### UnstableApiUsage
'split(java.lang.CharSequence)' is declared in unstable class 'com.google.common.base.Splitter.MapSplitter' marked with @Beta
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/repository/DataProxyConfigRepository.java`
#### Snippet
```java
            Map<String, List<CacheCluster>> cacheClusterZoneMap = cacheClusterMap.get(clusterTag);
            if (cacheClusterZoneMap != null) {
                Map<String, String> subTagMap = tagCache.computeIfAbsent(extTag, k -> MAP_SPLITTER.split(extTag));
                for (Entry<String, List<CacheCluster>> cacheEntry : cacheClusterZoneMap.entrySet()) {
                    if (cacheEntry.getValue().size() == 0) {
```

### UnstableApiUsage
'split(java.lang.CharSequence)' is declared in unstable class 'com.google.common.base.Splitter.MapSplitter' marked with @Beta
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/repository/DataProxyConfigRepository.java`
#### Snippet
```java
                    }
                    Map<String, String> wholeTagMap = tagCache.computeIfAbsent(cacheEntry.getKey(),
                            k -> MAP_SPLITTER.split(cacheEntry.getKey()));
                    if (isSubTag(wholeTagMap, subTagMap)) {
                        CacheClusterSetObject cacheSet = clusterObj.getCacheClusterSet();
```

### UnstableApiUsage
'com.google.common.base.Splitter.MapSplitter' is marked unstable with @Beta
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/repository/DataProxyConfigRepository.java`
#### Snippet
```java
    public static final String KEY_SINK_NAME = "defaultSinkName";

    public static final Splitter.MapSplitter MAP_SPLITTER = Splitter.on(SEPARATOR).trimResults()
            .withKeyValueSeparator(KEY_VALUE_SEPARATOR);
    public static final String CACHE_CLUSTER_PRODUCER_TAG = "producer";
```

### UnstableApiUsage
'withKeyValueSeparator(java.lang.String)' is marked unstable with @Beta
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/repository/DataProxyConfigRepository.java`
#### Snippet
```java

    public static final Splitter.MapSplitter MAP_SPLITTER = Splitter.on(SEPARATOR).trimResults()
            .withKeyValueSeparator(KEY_VALUE_SEPARATOR);
    public static final String CACHE_CLUSTER_PRODUCER_TAG = "producer";
    public static final String CACHE_CLUSTER_CONSUMER_TAG = "consumer";
```

### UnstableApiUsage
'com.google.common.base.Splitter.MapSplitter' is marked unstable with @Beta
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/config/ConfigHolder.java`
#### Snippet
```java
public abstract class ConfigHolder {

    public static final Splitter.MapSplitter MAP_SPLITTER = Splitter.on(AttrConstants.SEPARATOR)
            .trimResults().withKeyValueSeparator(AttrConstants.KEY_VALUE_SEPARATOR);
    private static final Logger LOG = LoggerFactory.getLogger(ConfigHolder.class);
```

### UnstableApiUsage
'withKeyValueSeparator(java.lang.String)' is marked unstable with @Beta
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/config/ConfigHolder.java`
#### Snippet
```java

    public static final Splitter.MapSplitter MAP_SPLITTER = Splitter.on(AttrConstants.SEPARATOR)
            .trimResults().withKeyValueSeparator(AttrConstants.KEY_VALUE_SEPARATOR);
    private static final Logger LOG = LoggerFactory.getLogger(ConfigHolder.class);
    private final String fileName;
```

### UnstableApiUsage
'split(java.lang.CharSequence)' is declared in unstable class 'com.google.common.base.Splitter.MapSplitter' marked with @Beta
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/config/holder/GroupIdPropertiesHolder.java`
#### Snippet
```java
                if (StringUtils.isNotBlank(entry.getValue())) {
                    tmpStreamIdMappingProperties.put(sArray[0].trim(),
                            MAP_SPLITTER.split(entry.getValue()));
                }
            }
```

### UnstableApiUsage
'split(java.lang.CharSequence)' is declared in unstable class 'com.google.common.base.Splitter.MapSplitter' marked with @Beta
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/config/holder/MxPropertiesHolder.java`
#### Snippet
```java
        try {
            for (Map.Entry<String, String> entry : getHolder().entrySet()) {
                mxPropertiesMaps.put(entry.getKey(), MAP_SPLITTER.split(entry.getValue()));
            }
        } catch (Exception e) {
```

### UnstableApiUsage
'split(java.lang.CharSequence)' is declared in unstable class 'com.google.common.base.Splitter.MapSplitter' marked with @Beta
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/DefaultServiceDecoder.java`
#### Snippet
```java
        String strAttr = new String(attrData, StandardCharsets.UTF_8);
        try {
            commonAttrMap = new HashMap<>(mapSplitter.split(strAttr));
        } catch (Exception e) {
            throw new Exception("Parse commonAttrMap error.commonAttrString is: "
```

### UnstableApiUsage
'split(java.lang.CharSequence)' is declared in unstable class 'com.google.common.base.Splitter.MapSplitter' marked with @Beta
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/DefaultServiceDecoder.java`
#### Snippet
```java
            resultMap.put(ConfigConstants.DECODER_ATTRS, strAttr);
            try {
                commonAttrMap.putAll(mapSplitter.split(strAttr));
            } catch (Exception e) {
                cb.clear();
```

### UnstableApiUsage
'com.google.common.base.Splitter.MapSplitter' is marked unstable with @Beta
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/DefaultServiceDecoder.java`
#### Snippet
```java
            .getLogger(DefaultServiceDecoder.class);

    private static final Splitter.MapSplitter mapSplitter = Splitter
            .on(AttributeConstants.SEPARATOR).trimResults()
            .withKeyValueSeparator(AttributeConstants.KEY_VALUE_SEPARATOR);
```

### UnstableApiUsage
'withKeyValueSeparator(java.lang.String)' is marked unstable with @Beta
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/DefaultServiceDecoder.java`
#### Snippet
```java
    private static final Splitter.MapSplitter mapSplitter = Splitter
            .on(AttributeConstants.SEPARATOR).trimResults()
            .withKeyValueSeparator(AttributeConstants.KEY_VALUE_SEPARATOR);

    /**
```

### UnstableApiUsage
'com.google.common.base.Splitter.MapSplitter' is marked unstable with @Beta
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/ServerMessageHandler.java`
#### Snippet
```java
            .withKeyValueSeparator(AttributeConstants.KEY_VALUE_SEPARATOR);

    private static final Splitter.MapSplitter mapSplitter = Splitter
            .on(AttributeConstants.SEPARATOR)
            .trimResults().withKeyValueSeparator(AttributeConstants.KEY_VALUE_SEPARATOR);
```

### UnstableApiUsage
'withKeyValueSeparator(java.lang.String)' is marked unstable with @Beta
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/ServerMessageHandler.java`
#### Snippet
```java
    private static final Splitter.MapSplitter mapSplitter = Splitter
            .on(AttributeConstants.SEPARATOR)
            .trimResults().withKeyValueSeparator(AttributeConstants.KEY_VALUE_SEPARATOR);

    private BaseSource source;
```

### UnstableApiUsage
'split(java.lang.CharSequence)' is declared in unstable class 'com.google.common.base.Splitter.MapSplitter' marked with @Beta
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/ServerMessageHandler.java`
#### Snippet
```java
                    message.getAttributeMap().putAll(mxValue);
                } else {
                    message.getAttributeMap().putAll(mapSplitter.split(this.defaultMXAttr));
                }
                // get configured topic name
```

### UnstableApiUsage
'split(java.lang.CharSequence)' is declared in unstable class 'com.google.common.base.Splitter.MapSplitter' marked with @Beta
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source2/v0msg/AbsV0MsgCodec.java`
#### Snippet
```java
        if (StringUtils.isNotBlank(this.origAttr)) {
            try {
                this.attrMap.putAll(mapSplitter.split(this.origAttr));
            } catch (Exception e) {
                source.fileMetricEventInc(StatConstants.EVENT_INVALIDATTR);
```

### UnstableApiUsage
'com.google.common.base.Splitter.MapSplitter' is marked unstable with @Beta
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source2/v0msg/AbsV0MsgCodec.java`
#### Snippet
```java

    // string splitter
    protected static final Splitter.MapSplitter mapSplitter = Splitter
            .on(AttributeConstants.SEPARATOR).trimResults()
            .withKeyValueSeparator(AttributeConstants.KEY_VALUE_SEPARATOR);
```

### UnstableApiUsage
'withKeyValueSeparator(java.lang.String)' is marked unstable with @Beta
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source2/v0msg/AbsV0MsgCodec.java`
#### Snippet
```java
    protected static final Splitter.MapSplitter mapSplitter = Splitter
            .on(AttributeConstants.SEPARATOR).trimResults()
            .withKeyValueSeparator(AttributeConstants.KEY_VALUE_SEPARATOR);
    // map joiner
    protected static final Joiner.MapJoiner mapJoiner = Joiner.on(AttributeConstants.SEPARATOR)
```

### UnstableApiUsage
'split(java.lang.CharSequence)' is declared in unstable class 'com.google.common.base.Splitter.MapSplitter' marked with @Beta
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/SimpleMessageHandler.java`
#### Snippet
```java
                message.getAttributeMap().putAll(mxValue);
            } else {
                message.getAttributeMap().putAll(mapSplitter.split(this.defaultMXAttr));
            }
        } else {
```

### UnstableApiUsage
'com.google.common.base.Splitter.MapSplitter' is marked unstable with @Beta
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/SimpleMessageHandler.java`
#### Snippet
```java
    private static final Joiner.MapJoiner mapJoiner = Joiner.on(AttributeConstants.SEPARATOR)
            .withKeyValueSeparator(AttributeConstants.KEY_VALUE_SEPARATOR);
    private static final Splitter.MapSplitter mapSplitter = Splitter
            .on(AttributeConstants.SEPARATOR)
            .trimResults().withKeyValueSeparator(AttributeConstants.KEY_VALUE_SEPARATOR);
```

### UnstableApiUsage
'withKeyValueSeparator(java.lang.String)' is marked unstable with @Beta
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/SimpleMessageHandler.java`
#### Snippet
```java
    private static final Splitter.MapSplitter mapSplitter = Splitter
            .on(AttributeConstants.SEPARATOR)
            .trimResults().withKeyValueSeparator(AttributeConstants.KEY_VALUE_SEPARATOR);

    private static final ThreadLocal<SimpleDateFormat> dateFormator = new ThreadLocal<SimpleDateFormat>() {
```

## RuleId[id=StringEquality]
### StringEquality
String values are compared using `==`, not 'equals()'
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/config/EncryptConfigEntry.java`
#### Snippet
```java
        return (this.userName.equals(info.getUserName()))
                && (this.version.equals(info.getVersion()))
                && (this.pubKey == info.getPubKey());
    }

```

### StringEquality
String values are compared using `==`, not 'equals()'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mongodb-cdc/src/main/java/org/apache/inlong/sort/cdc/mongodb/source/config/MongoDBSourceConfig.java`
#### Snippet
```java
                && splitMetaGroupSize == that.splitMetaGroupSize
                && splitSizeMB == that.splitSizeMB
                && inlongMetric == that.inlongMetric
                && inlongAudit == that.inlongAudit
                && Objects.equals(hosts, that.hosts)
```

### StringEquality
String values are compared using `==`, not 'equals()'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mongodb-cdc/src/main/java/org/apache/inlong/sort/cdc/mongodb/source/config/MongoDBSourceConfig.java`
#### Snippet
```java
                && splitSizeMB == that.splitSizeMB
                && inlongMetric == that.inlongMetric
                && inlongAudit == that.inlongAudit
                && Objects.equals(hosts, that.hosts)
                && Objects.equals(username, that.username)
```

### StringEquality
String values are compared using `==`, not 'equals()'
in `inlong-manager/manager-pojo/src/main/java/org/apache/inlong/manager/pojo/sink/kudu/KuduType.java`
#### Snippet
```java
    public static final KuduType forKuduType(String kuduType) {
        for (KuduType type : values()) {
            if (type.getKuduType() == kuduType) {
                return type;
            }
```

## RuleId[id=LoopStatementsThatDontLoop]
### LoopStatementsThatDontLoop
`while` statement does not loop
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/sources/reader/KafkaReader.java`
#### Snippet
```java
        // cousumer->topic->one partition
        Iterator<TopicPartition> iterator = assignment.iterator();
        while (iterator.hasNext()) {
            TopicPartition topicPartition = iterator.next();
            return topicPartition.topic() + "_" + topicPartition.partition();
```

## RuleId[id=UnnecessaryStringEscape]
### UnnecessaryStringEscape
`\'` is unnecessarily escaped
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/source/SimpleTcpSource.java`
#### Snippet
```java
            maxConnections = context.getInteger(CONNECTIONS, DEFAULT_MAX_CONNECTIONS);
        } catch (NumberFormatException e) {
            logger.warn("BaseSource\'s \"connections\" property must specify an integer value.",
                    context.getString(CONNECTIONS));
        }
```

### UnnecessaryStringEscape
`\'` is unnecessarily escaped
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sink/greenplum/GreenplumSqlBuilder.java`
#### Snippet
```java
        return new StringBuilder()
                .append("SELECT COUNT(1) FROM INFORMATION_SCHEMA.SCHEMATA ")
                .append(" WHERE SCHEMA_NAME = \'")
                .append(schemaName)
                .append("\';")
```

### UnnecessaryStringEscape
`\'` is unnecessarily escaped
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sink/greenplum/GreenplumSqlBuilder.java`
#### Snippet
```java
                .append(" WHERE SCHEMA_NAME = \'")
                .append(schemaName)
                .append("\';")
                .toString();
    }
```

### UnnecessaryStringEscape
`\'` is unnecessarily escaped
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sink/greenplum/GreenplumSqlBuilder.java`
#### Snippet
```java
                .append("\".\"")
                .append(tableInfo.getTableName())
                .append("\" IS \'")
                .append(tableInfo.getComment())
                .append("\';")
```

### UnnecessaryStringEscape
`\'` is unnecessarily escaped
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sink/greenplum/GreenplumSqlBuilder.java`
#### Snippet
```java
                .append("\" IS \'")
                .append(tableInfo.getComment())
                .append("\';")
                .toString();
    }
```

### UnnecessaryStringEscape
`\'` is unnecessarily escaped
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sink/greenplum/GreenplumSqlBuilder.java`
#### Snippet
```java
                        .append("\".\"")
                        .append(columnInfo.getName())
                        .append("\" IS \'")
                        .append(columnInfo.getComment())
                        .append("\' ;");
```

### UnnecessaryStringEscape
`\'` is unnecessarily escaped
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sink/greenplum/GreenplumSqlBuilder.java`
#### Snippet
```java
                        .append("\" IS \'")
                        .append(columnInfo.getComment())
                        .append("\' ;");
                commentList.add(commSql.toString());
            }
```

### UnnecessaryStringEscape
`\'` is unnecessarily escaped
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sink/postgresql/PostgreSQLSqlBuilder.java`
#### Snippet
```java
                        .append("\".\"")
                        .append(columnInfo.getName())
                        .append("\" IS \'")
                        .append(columnInfo.getComment())
                        .append("\' ;");
```

### UnnecessaryStringEscape
`\'` is unnecessarily escaped
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sink/postgresql/PostgreSQLSqlBuilder.java`
#### Snippet
```java
                        .append("\" IS \'")
                        .append(columnInfo.getComment())
                        .append("\' ;");
                commentList.add(commSql.toString());
            }
```

### UnnecessaryStringEscape
`\'` is unnecessarily escaped
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sink/postgresql/PostgreSQLSqlBuilder.java`
#### Snippet
```java
                .append("\".\"")
                .append(tableInfo.getTableName())
                .append("\" IS \'")
                .append(tableInfo.getComment())
                .append("\';")
```

### UnnecessaryStringEscape
`\'` is unnecessarily escaped
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sink/postgresql/PostgreSQLSqlBuilder.java`
#### Snippet
```java
                .append("\" IS \'")
                .append(tableInfo.getComment())
                .append("\';")
                .toString();
    }
```

### UnnecessaryStringEscape
`\'` is unnecessarily escaped
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sink/postgresql/PostgreSQLSqlBuilder.java`
#### Snippet
```java
        return new StringBuilder()
                .append("SELECT COUNT(1) FROM INFORMATION_SCHEMA.SCHEMATA ")
                .append(" WHERE SCHEMA_NAME = \'")
                .append(schemaName)
                .append("\';")
```

### UnnecessaryStringEscape
`\'` is unnecessarily escaped
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sink/postgresql/PostgreSQLSqlBuilder.java`
#### Snippet
```java
                .append(" WHERE SCHEMA_NAME = \'")
                .append(schemaName)
                .append("\';")
                .toString();
    }
```

### UnnecessaryStringEscape
`\'` is unnecessarily escaped
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/BaseSource.java`
#### Snippet
```java
            maxConnections = context.getInteger(CONNECTIONS, DEFAULT_MAX_CONNECTIONS);
        } catch (NumberFormatException e) {
            logger.warn("BaseSource\'s \"connections\" property must specify an integer value.",
                    context.getString(CONNECTIONS));
        }
```

## RuleId[id=RefusedBequest]
### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/util/ParameterTool.java`
#### Snippet
```java

    @Override
    protected Object clone() throws CloneNotSupportedException {
        return new ParameterTool(this.data);
    }
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/configuration/Configuration.java`
#### Snippet
```java

    @Override
    public Configuration clone() {
        Configuration config = new Configuration();
        config.addAll(this);
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/cluster/MasterInfo.java`
#### Snippet
```java

    @Override
    public MasterInfo clone() {
        return new MasterInfo(this.addrMap4Failover, this.firstNodeAddr, this.masterClusterStr);
    }
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/cluster/TopicInfo.java`
#### Snippet
```java

    @Override
    public TopicInfo clone() {
        return new TopicInfo(this.broker, this.topic, this.partitionNum,
                this.topicStoreNum, this.acceptPublish, this.acceptSubscribe);
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/metadata/TopicMetadata.java`
#### Snippet
```java

    @Override
    public TopicMetadata clone() {
        return new TopicMetadata(this.topic, this.unflushThreshold,
                this.unflushInterval, this.unflushDataHold,
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/nodemanage/nodeconsumer/NodeRebInfo.java`
#### Snippet
```java

    @Override
    public NodeRebInfo clone() {
        return new NodeRebInfo(this.clientId,
                this.status, this.createTime,
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `inlong-manager/manager-workflow/src/main/java/org/apache/inlong/manager/workflow/definition/ServiceTask.java`
#### Snippet
```java
    @Override
    @SneakyThrows
    public ServiceTask clone() {
        ServiceTask serviceTask = new ServiceTask();
        serviceTask.setName(this.getName());
```

## RuleId[id=RedundantArrayCreation]
### RedundantArrayCreation
Redundant array creation for calling varargs method
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/utils/InLongDatabaseHistory.java`
#### Snippet
```java

    static {
        ALL_FIELDS = Collect.arrayListOf(FILE_PATH, new Field[0]);
        UTF8 = StandardCharsets.UTF_8;
    }
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/utils/InLongDatabaseHistory.java`
#### Snippet
```java
                                    } catch (IOException var7) {
                                        this.logger.error("Failed to add record to history at {}: {}",
                                                new Object[]{this.path, record, var7});
                                    }
                                } catch (Throwable var8) {
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/utils/InLongDatabaseHistory.java`
#### Snippet
```java
                try {
                    if (!this.storageExists()) {
                        if (path.getParent() != null && !Files.exists(path.getParent(), new LinkOption[0])) {
                            Files.createDirectories(path.getParent());
                        }
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/PulsarSink.java`
#### Snippet
```java
            logger.info("metasink {}, succ put {} events to pulsar,"
                    + " in the past {} millsec",
                    new Object[]{
                            getName(), (nowCnt - oldCnt), (t2 - t1)
                    });
            t1 = t2;
        }
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/TubeSink.java`
#### Snippet
```java
            logger.info("tubesink {}, succ put {} events to tube,"
                    + " in the past {} millsec",
                    new Object[]{
                            getName(), (nowCnt - oldCnt), (t2 - t1)
                    });
            t1 = t2;
        }
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `inlong-common/src/main/java/org/apache/inlong/common/monitor/MonitorIndexExt.java`
#### Snippet
```java
                    for (Map.Entry<String, Long> entrys : counterExt.entrySet()) {
                        logger.info("{}#{}#{}",
                                new Object[]{name, entrys.getKey(), entrys.getValue()});
                    }
                    counterExt.clear();
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `inlong-common/src/main/java/org/apache/inlong/common/monitor/StatRunner.java`
#### Snippet
```java
                    str = (String) iterator.next();
                    cnt = (Long) counters.get(str);
                    logger.info("{}.{}={}", new Object[]{this.name, str, cnt});
                }

```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `inlong-common/src/main/java/org/apache/inlong/common/monitor/StatRunner.java`
#### Snippet
```java
                    Entry<String, Long> entrys = (Entry) iterator.next();
                    logger.info("{}.{}={}",
                            new Object[]{this.name, entrys.getKey(), entrys.getValue()});
                }

```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `inlong-common/src/main/java/org/apache/inlong/common/monitor/MonitorIndex.java`
#### Snippet
```java
                        uuidStr = currentKey + "_" + recId.incrementAndGet();
                        logger.info("{}#{}#{}#{}",
                                new Object[]{name, uuidStr, entrys.getKey(), entrys.getValue()});
                    }
                    counterExt.clear();
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/SimpleUdpSource.java`
#### Snippet
```java
        } catch (Exception e) {
            logger.error("Simple UDP Source error bind host {} port {}, program will exit!",
                    new Object[]{host, port});
            System.exit(-1);
            // throw new FlumeException(e.getMessage());
```

## RuleId[id=DuplicateBranchesInSwitch]
### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `inlong-sdk/sort-sdk/src/main/java/org/apache/inlong/sdk/sort/api/InlongTopicManagerFactory.java`
#### Snippet
```java
        switch (type) {
            case SINGLE_TOPIC:
                return createSingleTopicManager(context, queryConsumeConfig);
            case MULTI_TOPIC:
                return createMultiTopicManager(context, queryConsumeConfig);
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/jdbc/src/main/java/org/apache/inlong/sort/jdbc/converter/AbstractJdbcRowConverter.java`
#### Snippet
```java
                return val -> val instanceof Integer ? ((Integer) val).shortValue() : val;
            case INTEGER:
                return val -> val;
            case BIGINT:
                return val -> val;
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/jdbc/src/main/java/org/apache/inlong/sort/jdbc/converter/AbstractJdbcRowConverter.java`
#### Snippet
```java
                return val -> val;
            case BIGINT:
                return val -> val;
            case DECIMAL:
                final int precision = ((DecimalType) type).getPrecision();
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/mysql/table/OldFieldMetadataConverter.java`
#### Snippet
```java
            case CHAR:
            case VARCHAR:
                return toStringConverter;
            case BINARY:
            case VARBINARY:
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/Metadata.java`
#### Snippet
```java
                break;
            case ORACLE_TYPE:
                metadataType = "MAP<STRING, STRING>";
                break;
            case PK_NAMES:
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/fileconfig/BdbMetaConfig.java`
#### Snippet
```java
        switch (metaLocalSyncPolicy) {
            case 1:
                return Durability.SyncPolicy.SYNC;
            case 2:
                return Durability.SyncPolicy.NO_SYNC;
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/fileconfig/BdbMetaConfig.java`
#### Snippet
```java
        switch (metaReplicaSyncPolicy) {
            case 1:
                return Durability.SyncPolicy.SYNC;
            case 2:
                return Durability.SyncPolicy.NO_SYNC;
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/fileconfig/BdbMetaConfig.java`
#### Snippet
```java
        switch (repReplicaAckPolicy) {
            case 1:
                return Durability.ReplicaAckPolicy.SIMPLE_MAJORITY;
            case 2:
                return Durability.ReplicaAckPolicy.ALL;
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/zookeeper/ZooKeeperWatcher.java`
#### Snippet
```java

            case AuthFailed:
                if (ZKUtil.isSecureZooKeeper()) {
                    // We could not be authenticated, but clients should proceed
                    // anyway.
                    // Only access to znodes that require SASL authentication will
                    // be
                    // denied. The client may never need to access them.
                    saslLatch.countDown();
                }
                break;

```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `inlong-manager/manager-pojo/src/main/java/org/apache/inlong/manager/pojo/sort/util/FilterFunctionUtils.java`
#### Snippet
```java
                        return FilterStrategy.REMOVE;
                    case RETAIN:
                        return FilterStrategy.RETAIN;
                    default:
                        return FilterStrategy.RETAIN;
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `inlong-sort-standalone/sort-standalone-common/src/main/java/org/apache/inlong/sort/standalone/config/holder/AckPolicy.java`
#### Snippet
```java
        switch (value) {
            case 0:
                return COUNT;
            case 1:
                return TOKEN;
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/kafka/DefaultEvent2KafkaRecordHandler.java`
#### Snippet
```java
            case JCE:
            case UNKNOWN:
                break;
            default:
                break;
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/pulsar/DefaultEvent2PulsarRecordHandler.java`
#### Snippet
```java
            case JCE:
            case UNKNOWN:
                break;
            default:
                break;
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/clickhouse/DefaultEventHandler.java`
#### Snippet
```java
                case Types.LONGVARCHAR:
                    // LONGVARCHAR = -1;
                    pstat.setString(i, fieldValue);
                    break;
                // Date
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/pulsar/PulsarProducerCluster.java`
#### Snippet
```java
                return CompressionType.LZ4;
            case "NONE":
                return CompressionType.NONE;
            case "ZLIB":
                return CompressionType.ZLIB;
```

## RuleId[id=NonFinalFieldInEnum]
### NonFinalFieldInEnum
Non-final field `name` in enum 'LoadBalance'
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/LoadBalance.java`
#### Snippet
```java
    WEIGHT_ROBIN("weight robin", 4);

    private String name;
    private int index;

```

### NonFinalFieldInEnum
Non-final field `index` in enum 'LoadBalance'
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/LoadBalance.java`
#### Snippet
```java

    private String name;
    private int index;

    private LoadBalance(String name, int index) {
```

### NonFinalFieldInEnum
Non-final field `value` in enum 'LoaderType'
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/config/LoaderType.java`
#### Snippet
```java
    File("file"), Context("context"), Manager("manager"), Plugin("plugin");

    private String value;

    /**
```

### NonFinalFieldInEnum
Non-final field `value` in enum 'ErrorCode'
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/config/pojo/ErrorCode.java`
#### Snippet
```java
    SUCC(0), NOUPDATE(1), FAIL(-1), REQ_PARAMS_ERROR(-101);

    private int value;

    /**
```

### NonFinalFieldInEnum
Non-final field `description` in enum 'SchemaUpdateExceptionPolicy'
in `inlong-sort/sort-flink/base/src/main/java/org/apache/inlong/sort/base/sink/SchemaUpdateExceptionPolicy.java`
#### Snippet
```java
    THROW_WITH_STOP("Throw exception to stop flink job when meet schema update.");

    private String description;

    SchemaUpdateExceptionPolicy(String description) {
```

### NonFinalFieldInEnum
Non-final field `redisDataType` in enum 'RedisCommand'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/redis/src/main/java/org/apache/inlong/sort/redis/common/mapper/RedisCommand.java`
#### Snippet
```java
     * The {@link RedisDataType} this command belongs to.
     */
    private RedisDataType redisDataType;

    RedisCommand(RedisDataType redisDataType) {
```

### NonFinalFieldInEnum
Non-final field `description` in enum 'EventStatus'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/balance/EventStatus.java`
#### Snippet
```java

    private int value;
    private String description;

    EventStatus(int value, String description) {
```

### NonFinalFieldInEnum
Non-final field `value` in enum 'EventStatus'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/balance/EventStatus.java`
#### Snippet
```java
    FAILED(-2, "Process failed");

    private int value;
    private String description;

```

### NonFinalFieldInEnum
Non-final field `value` in enum 'EventType'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/balance/EventType.java`
#### Snippet
```java
    UNKNOWN(-1, "Unknown operation type");

    private int value;
    private String description;

```

### NonFinalFieldInEnum
Non-final field `description` in enum 'EventType'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/balance/EventType.java`
#### Snippet
```java

    private int value;
    private String description;

    EventType(int value, String description) {
```

### NonFinalFieldInEnum
Non-final field `description` in enum 'ConsumePosition'
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/ConsumePosition.java`
#### Snippet
```java

    private int code;
    private String description;

    ConsumePosition(int code, String description) {
```

### NonFinalFieldInEnum
Non-final field `code` in enum 'ConsumePosition'
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/ConsumePosition.java`
#### Snippet
```java
            "Start from 0 for the first time. Otherwise start from last consume position.");

    private int code;
    private String description;

```

### NonFinalFieldInEnum
Non-final field `value` in enum 'WebFieldType'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/fielddef/WebFieldType.java`
#### Snippet
```java
    DELPOLICY(11, "Delete policy");

    private int value;
    private String desc;

```

### NonFinalFieldInEnum
Non-final field `desc` in enum 'WebFieldType'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/fielddef/WebFieldType.java`
#### Snippet
```java

    private int value;
    private String desc;

    WebFieldType(int value, String desc) {
```

### NonFinalFieldInEnum
Non-final field `description` in enum 'TopicStsChgType'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/statusdef/TopicStsChgType.java`
#### Snippet
```java

    private int code;
    private String description;

    TopicStsChgType(int code, String description) {
```

### NonFinalFieldInEnum
Non-final field `code` in enum 'TopicStsChgType'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/statusdef/TopicStsChgType.java`
#### Snippet
```java
    STATUS_CHANGE_REDO_SFDEL(2, "Redo soft delete");

    private int code;
    private String description;

```

### NonFinalFieldInEnum
Non-final field `code` in enum 'EnableStatus'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/statusdef/EnableStatus.java`
#### Snippet
```java
    STATUS_ENABLE(2, "Enable.");

    private int code;
    private String description;

```

### NonFinalFieldInEnum
Non-final field `description` in enum 'EnableStatus'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/statusdef/EnableStatus.java`
#### Snippet
```java

    private int code;
    private String description;

    EnableStatus(int code, String description) {
```

### NonFinalFieldInEnum
Non-final field `description` in enum 'TopicStatus'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/statusdef/TopicStatus.java`
#### Snippet
```java

    private int code;
    private String description;

    TopicStatus(int code, String description) {
```

### NonFinalFieldInEnum
Non-final field `code` in enum 'TopicStatus'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/statusdef/TopicStatus.java`
#### Snippet
```java
    STATUS_TOPIC_HARD_REMOVE(3, "Hard removed");

    private int code;
    private String description;

```

### NonFinalFieldInEnum
Non-final field `description` in enum 'CleanPolType'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/statusdef/CleanPolType.java`
#### Snippet
```java

    private int code;
    private String description;

    CleanPolType(int code, String description) {
```

### NonFinalFieldInEnum
Non-final field `code` in enum 'CleanPolType'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/statusdef/CleanPolType.java`
#### Snippet
```java
    CLEAN_POL_DELETE(0, "Delete");

    private int code;
    private String description;

```

### NonFinalFieldInEnum
Non-final field `description` in enum 'ManageStatus'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/statusdef/ManageStatus.java`
#### Snippet
```java

    private int code;
    private String description;
    private boolean isAcceptPublish;
    private boolean isAcceptSubscribe;
```

### NonFinalFieldInEnum
Non-final field `code` in enum 'ManageStatus'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/statusdef/ManageStatus.java`
#### Snippet
```java
    STATUS_MANAGE_OFFLINE(9, "offline", false, false);

    private int code;
    private String description;
    private boolean isAcceptPublish;
```

### NonFinalFieldInEnum
Non-final field `isAcceptSubscribe` in enum 'ManageStatus'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/statusdef/ManageStatus.java`
#### Snippet
```java
    private String description;
    private boolean isAcceptPublish;
    private boolean isAcceptSubscribe;

    ManageStatus(int code, String description,
```

### NonFinalFieldInEnum
Non-final field `isAcceptPublish` in enum 'ManageStatus'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/statusdef/ManageStatus.java`
#### Snippet
```java
    private int code;
    private String description;
    private boolean isAcceptPublish;
    private boolean isAcceptSubscribe;

```

### NonFinalFieldInEnum
Non-final field `code` in enum 'DataOpErrCode'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/metamanage/DataOpErrCode.java`
#### Snippet
```java
    DERR_MASTER_UNKNOWN(599, "Unknown error");

    private int code;
    private String description;

```

### NonFinalFieldInEnum
Non-final field `description` in enum 'DataOpErrCode'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/metamanage/DataOpErrCode.java`
#### Snippet
```java

    private int code;
    private String description;

    DataOpErrCode(int code, String description) {
```

### NonFinalFieldInEnum
Non-final field `typeName` in enum 'TaskTypeEnum'
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/enums/TaskTypeEnum.java`
#### Snippet
```java
    private Integer taskType;

    private String typeName;

    TaskTypeEnum(Integer role, String typeName) {
```

### NonFinalFieldInEnum
Non-final field `taskType` in enum 'TaskTypeEnum'
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/enums/TaskTypeEnum.java`
#### Snippet
```java
    CREATE_TOPIC(1, "createTopicTask");

    private Integer taskType;

    private String typeName;
```

### NonFinalFieldInEnum
Non-final field `message` in enum 'ErrorCode'
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/enums/ErrorCode.java`
#### Snippet
```java
    private Integer code;

    private String message;

    ErrorCode(Integer code, String message) {
```

### NonFinalFieldInEnum
Non-final field `code` in enum 'ErrorCode'
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/enums/ErrorCode.java`
#### Snippet
```java
    TASK_EXIST(200, "task already exist");

    private Integer code;

    private String message;
```

### NonFinalFieldInEnum
Non-final field `code` in enum 'TaskStatusEnum'
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/enums/TaskStatusEnum.java`
#### Snippet
```java
    SUCCESS(3, "success");

    private Integer code;

    private String message;
```

### NonFinalFieldInEnum
Non-final field `message` in enum 'TaskStatusEnum'
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/enums/TaskStatusEnum.java`
#### Snippet
```java
    private Integer code;

    private String message;

    TaskStatusEnum(Integer code, String message) {
```

### NonFinalFieldInEnum
Non-final field `status` in enum 'NodeStatus'
in `inlong-manager/manager-common/src/main/java/org/apache/inlong/manager/common/enums/NodeStatus.java`
#### Snippet
```java
    HEARTBEAT_TIMEOUT(2);

    int status;

    NodeStatus(int status) {
```

### NonFinalFieldInEnum
Non-final field `COMPLETED_STATUS` in enum 'TaskStatus'
in `inlong-manager/manager-common/src/main/java/org/apache/inlong/manager/common/enums/TaskStatus.java`
#### Snippet
```java
     * Completed
     */
    public static Set<TaskStatus> COMPLETED_STATUS = ImmutableSet
            .of(APPROVED, REJECTED, CANCELED, TRANSFERRED, TERMINATED, COMPLETED, FAILED, SKIPPED);
}
```

### NonFinalFieldInEnum
Non-final field `status` in enum 'EventStatus'
in `inlong-manager/manager-common/src/main/java/org/apache/inlong/manager/common/enums/EventStatus.java`
#### Snippet
```java
    FAILED(-1);

    int status;

    EventStatus(int status) {
```

### NonFinalFieldInEnum
Non-final field `sourceType` in enum 'ConnectorJarType'
in `inlong-manager/manager-plugins/src/main/java/org/apache/inlong/manager/plugin/flink/enums/ConnectorJarType.java`
#### Snippet
```java
    ;

    private String sourceType;
    private String connectorType;

```

### NonFinalFieldInEnum
Non-final field `connectorType` in enum 'ConnectorJarType'
in `inlong-manager/manager-plugins/src/main/java/org/apache/inlong/manager/plugin/flink/enums/ConnectorJarType.java`
#### Snippet
```java

    private String sourceType;
    private String connectorType;

    ConnectorJarType(String sourceType, String connectorType) {
```

## RuleId[id=RegExpSimplifiable]
### RegExpSimplifiable
`[\\d]` can be simplified to '\\d'
in `inlong-agent/agent-core/src/main/java/org/apache/inlong/agent/core/HeartbeatManager.java`
#### Snippet
```java
    private final String reportSnapshotUrl;
    private final String reportHeartbeatUrl;
    private final Pattern numberPattern = Pattern.compile("^[-+]?[\\d]*$");

    /**
```

### RegExpSimplifiable
`[\\x00]` can be simplified to '\\x00'
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/plugin/PluginClassLoader.java`
#### Snippet
```java
            JarFile pluginJar = new JarFile(jarFile);
            String pluginDef = readPluginDef(pluginJar);
            pluginDef = pluginDef.replaceAll("[\\x00]+", "");
            PluginDefinition definition = YAML_MAPPER.readValue(pluginDef, PluginDefinition.class);

```

## RuleId[id=DuplicateExpressions]
### DuplicateExpressions
Multiple occurrences of `Long.parseLong(validValStr.substring(0, validValStr.length() - 1))`
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/MessageStore.java`
#### Snippet
```java
        try {
            if (validValStr.endsWith("m")) {
                return Long.parseLong(validValStr.substring(0, validValStr.length() - 1)) * 60000;
            } else if (validValStr.endsWith("s")) {
                return Long.parseLong(validValStr.substring(0, validValStr.length() - 1)) * 1000;
```

### DuplicateExpressions
Multiple occurrences of `Long.parseLong(validValStr.substring(0, validValStr.length() - 1))`
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/MessageStore.java`
#### Snippet
```java
                return Long.parseLong(validValStr.substring(0, validValStr.length() - 1)) * 60000;
            } else if (validValStr.endsWith("s")) {
                return Long.parseLong(validValStr.substring(0, validValStr.length() - 1)) * 1000;
            } else if (validValStr.endsWith("h")) {
                return Long.parseLong(validValStr.substring(0, validValStr.length() - 1)) * 3600000;
```

### DuplicateExpressions
Multiple occurrences of `Long.parseLong(validValStr.substring(0, validValStr.length() - 1))`
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/MessageStore.java`
#### Snippet
```java
                return Long.parseLong(validValStr.substring(0, validValStr.length() - 1)) * 1000;
            } else if (validValStr.endsWith("h")) {
                return Long.parseLong(validValStr.substring(0, validValStr.length() - 1)) * 3600000;
            } else {
                return Long.parseLong(validValStr) * 3600000;
```

### DuplicateExpressions
Multiple occurrences of `Long.parseLong(validValStr.substring(0, validValStr.length() - 1))`
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/utils/WebParameterUtils.java`
#### Snippet
```java
        try {
            if (timeUnit.endsWith("s")) {
                validDuration = Long.parseLong(validValStr.substring(0, validValStr.length() - 1)) * 1000;
            } else if (timeUnit.endsWith("m")) {
                validDuration = Long.parseLong(validValStr.substring(0, validValStr.length() - 1)) * 60000;
```

### DuplicateExpressions
Multiple occurrences of `Long.parseLong(validValStr.substring(0, validValStr.length() - 1))`
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/utils/WebParameterUtils.java`
#### Snippet
```java
                validDuration = Long.parseLong(validValStr.substring(0, validValStr.length() - 1)) * 1000;
            } else if (timeUnit.endsWith("m")) {
                validDuration = Long.parseLong(validValStr.substring(0, validValStr.length() - 1)) * 60000;
            } else if (timeUnit.endsWith("h")) {
                validDuration = Long.parseLong(validValStr.substring(0, validValStr.length() - 1)) * 3600000;
```

### DuplicateExpressions
Multiple occurrences of `Long.parseLong(validValStr.substring(0, validValStr.length() - 1))`
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/utils/WebParameterUtils.java`
#### Snippet
```java
                validDuration = Long.parseLong(validValStr.substring(0, validValStr.length() - 1)) * 60000;
            } else if (timeUnit.endsWith("h")) {
                validDuration = Long.parseLong(validValStr.substring(0, validValStr.length() - 1)) * 3600000;
            } else {
                validDuration = Long.parseLong(validValStr) * 3600000;
```

### DuplicateExpressions
Multiple occurrences of `Long.parseLong(validValStr.substring(0, validValStr.length() - 1))`
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/metamanage/metastore/dao/entity/TopicPropGroup.java`
#### Snippet
```java
        String timeUnit = validValStr.substring(validValStr.length() - 1).toLowerCase();
        if (timeUnit.endsWith("s")) {
            validDuration = Long.parseLong(validValStr.substring(0, validValStr.length() - 1)) * 1000;
        } else if (timeUnit.endsWith("m")) {
            validDuration = Long.parseLong(validValStr.substring(0, validValStr.length() - 1)) * 60000;
```

### DuplicateExpressions
Multiple occurrences of `Long.parseLong(validValStr.substring(0, validValStr.length() - 1))`
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/metamanage/metastore/dao/entity/TopicPropGroup.java`
#### Snippet
```java
            validDuration = Long.parseLong(validValStr.substring(0, validValStr.length() - 1)) * 1000;
        } else if (timeUnit.endsWith("m")) {
            validDuration = Long.parseLong(validValStr.substring(0, validValStr.length() - 1)) * 60000;
        } else if (timeUnit.endsWith("h")) {
            validDuration = Long.parseLong(validValStr.substring(0, validValStr.length() - 1)) * 3600000;
```

### DuplicateExpressions
Multiple occurrences of `Long.parseLong(validValStr.substring(0, validValStr.length() - 1))`
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/metamanage/metastore/dao/entity/TopicPropGroup.java`
#### Snippet
```java
            validDuration = Long.parseLong(validValStr.substring(0, validValStr.length() - 1)) * 60000;
        } else if (timeUnit.endsWith("h")) {
            validDuration = Long.parseLong(validValStr.substring(0, validValStr.length() - 1)) * 3600000;
        } else {
            validDuration = Long.parseLong(validValStr) * 3600000;
```

## RuleId[id=StringBufferReplaceableByString]
### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/utils/IpUtils.java`
#### Snippet
```java
     */
    public static String intToIp(int ipInt) {
        return new StringBuilder().append(((ipInt >> 24) & 0xff)).append('.').append((ipInt >> 16) & 0xff).append('.')
                .append((ipInt >> 8) & 0xff).append('.').append((ipInt & 0xff)).toString();
    }
```

### StringBufferReplaceableByString
`StringBuilder subWriterInlongMetric` can be replaced with 'String'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/iceberg/src/main/java/org/apache/inlong/sort/iceberg/sink/multiple/IcebergMultipleStreamWriter.java`
#### Snippet
```java

            if (multipleWriters.get(tableId) == null) {
                StringBuilder subWriterInlongMetric = new StringBuilder(inlongMetric);
                subWriterInlongMetric.append(DELIMITER)
                        .append(Constants.DATABASE_NAME).append("=").append(tableId.namespace().toString())
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/starrocks/src/main/java/org/apache/inlong/sort/starrocks/manager/StarRocksStreamLoadVisitor.java`
#### Snippet
```java
        long tmp = pos + hostList.size();
        for (; pos < tmp; pos++) {
            String host = new StringBuilder("http://").append(hostList.get((int) (pos % hostList.size()))).toString();
            if (tryHttpConnection(host)) {
                return host;
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/starrocks/src/main/java/org/apache/inlong/sort/starrocks/manager/StarRocksStreamLoadVisitor.java`
#### Snippet
```java
            throw new IOException("None of the hosts in `load_url` could be connected.");
        }
        String loadUrl = new StringBuilder(host).append("/api/").append(bufferEntity.getDatabase()).append("/")
                .append(bufferEntity.getTable()).append("/_stream_load").toString();
        LOG.info(String.format("Start to join batch data: label[%s].", bufferEntity.getLabel()));
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/starrocks/src/main/java/org/apache/inlong/sort/starrocks/manager/StarRocksStreamLoadVisitor.java`
#### Snippet
```java
        String auth = username + ":" + password;
        byte[] encodedAuth = Base64.encodeBase64(auth.getBytes(StandardCharsets.UTF_8));
        return new StringBuilder("Basic ").append(new String(encodedAuth)).toString();
    }

```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/starrocks/src/main/java/org/apache/inlong/sort/starrocks/manager/StarRocksStreamLoadVisitor.java`
#### Snippet
```java
            try (CloseableHttpClient httpclient = HttpClients.createDefault()) {
                HttpGet httpGet = new HttpGet(
                        new StringBuilder(host).append("/api/").append(database)
                                .append("/get_load_state?label=").append(label).toString());
                httpGet.setHeader("Authorization",
```

### StringBufferReplaceableByString
`StringBuilder predicate` can be replaced with 'String'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/oracle-cdc/src/main/java/io/debezium/connector/oracle/logminer/LogMinerQueryBuilder.java`
#### Snippet
```java
     */
    private static String buildDdlPredicate() {
        final StringBuilder predicate = new StringBuilder(256);
        predicate.append("(OPERATION_CODE = 5 ");
        predicate.append("AND USERNAME NOT IN ('SYS','SYSTEM') ");
```

### StringBufferReplaceableByString
`StringBuilder beanName` can be replaced with 'String'
in `inlong-agent/agent-common/src/main/java/org/apache/inlong/agent/metrics/AgentPrometheusMetricListener.java`
#### Snippet
```java
        this.metricItem = new AgentMetricItem();
        final MBeanServer mbs = ManagementFactory.getPlatformMBeanServer();
        StringBuilder beanName = new StringBuilder();
        beanName.append(JMX_DOMAIN).append(DOMAIN_SEPARATOR).append("type=AgentPrometheus");
        String strBeanName = beanName.toString();
```

### StringBufferReplaceableByString
`StringBuilder beanName` can be replaced with 'String'
in `inlong-common/src/main/java/org/apache/inlong/common/metric/MetricRegister.java`
#### Snippet
```java
    public static void unregister(MetricItemSet<? extends MetricItem> obj) {
        final MBeanServer mbs = ManagementFactory.getPlatformMBeanServer();
        StringBuilder beanName = new StringBuilder();
        beanName.append(JMX_DOMAIN).append(MetricItemMBean.DOMAIN_SEPARATOR).append("type=")
                .append(MetricUtils.getDomain(obj.getClass())).append(",name=").append(obj.getName());
```

### StringBufferReplaceableByString
`StringBuilder beanName` can be replaced with 'String'
in `inlong-common/src/main/java/org/apache/inlong/common/metric/MetricRegister.java`
#### Snippet
```java
    public static void register(MetricItemSet<? extends MetricItem> obj) {
        final MBeanServer mbs = ManagementFactory.getPlatformMBeanServer();
        StringBuilder beanName = new StringBuilder();
        beanName.append(JMX_DOMAIN).append(MetricItemMBean.DOMAIN_SEPARATOR).append("type=")
                .append(MetricUtils.getDomain(obj.getClass())).append(",name=").append(obj.getName());
```

### StringBufferReplaceableByString
`StringBuilder beanName` can be replaced with 'String'
in `inlong-common/src/main/java/org/apache/inlong/common/metric/MetricRegister.java`
#### Snippet
```java
    public static void unregister(MetricItem obj) {
        final MBeanServer mbs = ManagementFactory.getPlatformMBeanServer();
        StringBuilder beanName = new StringBuilder();
        beanName.append(JMX_DOMAIN).append(MetricItemMBean.DOMAIN_SEPARATOR).append("type=")
                .append(MetricUtils.getDomain(obj.getClass())).append(",").append(obj.getDimensionsKey());
```

### StringBufferReplaceableByString
`StringBuilder beanName` can be replaced with 'String'
in `inlong-common/src/main/java/org/apache/inlong/common/metric/MetricRegister.java`
#### Snippet
```java
    public static void register(MetricItem obj) {
        final MBeanServer mbs = ManagementFactory.getPlatformMBeanServer();
        StringBuilder beanName = new StringBuilder();
        beanName.append(JMX_DOMAIN).append(MetricItemMBean.DOMAIN_SEPARATOR).append("type=")
                .append(MetricUtils.getDomain(obj.getClass())).append(",").append(obj.getDimensionsKey());
```

### StringBufferReplaceableByString
`StringBuilder beanName` can be replaced with 'String'
in `inlong-common/src/main/java/org/apache/inlong/common/metric/MetricListenerRunnable.java`
#### Snippet
```java
    public List<MetricItemValue> getItemValues() throws InstanceNotFoundException, AttributeNotFoundException,
            ReflectionException, MBeanException, MalformedObjectNameException, ClassNotFoundException {
        StringBuilder beanName = new StringBuilder();
        beanName.append(MetricRegister.JMX_DOMAIN).append(MetricItemMBean.DOMAIN_SEPARATOR)
                .append("type=").append(domain)
```

### StringBufferReplaceableByString
`StringBuilder stringBuilder` can be replaced with 'String'
in `inlong-common/src/main/java/org/apache/inlong/common/monitor/MonitorIndex.java`
#### Snippet
```java
                                + (Integer.parseInt(va[3]) + failcnt);
                    } else {
                        StringBuilder stringBuilder = new StringBuilder();
                        value = stringBuilder.append(cnt).append("#").append(packcnt).append("#")
                                .append(packsize).append("#").append(failcnt).toString();
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/RpcServiceInvoker.java`
#### Snippet
```java
                return responseWrapper.getResponseData();
            } else {
                throw MixUtils.unwrapException(new StringBuilder(512)
                        .append(responseWrapper.getErrMsg()).append("#")
                        .append(responseWrapper.getStackTrace()).toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/AbstractServiceInvoker.java`
#### Snippet
```java
                    chainedCallback.handleResult(wrapper.getResponseData());
                } else {
                    String errMsg = new StringBuilder(512)
                            .append(wrapper.getErrMsg() == null ? "" : wrapper.getErrMsg())
                            .append("#").append(wrapper.getStackTrace()).toString();
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/RpcServiceFailoverInvoker.java`
#### Snippet
```java
                        } else {
                            Throwable remote =
                                    MixUtils.unwrapException(new StringBuilder(512)
                                            .append(responseWrapper.getErrMsg()).append("#")
                                            .append(responseWrapper.getStackTrace()).toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/RpcServiceFailoverInvoker.java`
#### Snippet
```java
            } else {
                int index = (currentCounter & Integer.MAX_VALUE) % masterNodeCnt;
                t = new IOException(new StringBuilder(512).append("Connect server ")
                        .append(masterInfo.getNodeHostPortList().get(index)).append(" failure!").toString());
                if (currentCounter == retryCounter.get()) {
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/codec/PbEnDecoder.java`
#### Snippet
```java
                case RpcConstants.RPC_MSG_BROKER_PRODUCER_HEARTBEAT:
                default: {
                    throw new Exception(new StringBuilder(256)
                            .append("Unsupported method ID :")
                            .append(methodId).toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/codec/PbEnDecoder.java`
#### Snippet
```java
                case RpcConstants.RPC_MSG_BROKER_PRODUCER_HEARTBEAT:
                default: {
                    throw new Exception(new StringBuilder(256)
                            .append("Unsupported method ID :")
                            .append(methodId).toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/codec/PbEnDecoder.java`
#### Snippet
```java
        Integer serviceId = rpcServiceMap.get(serviceName);
        if (serviceId == null) {
            throw new Exception(new StringBuilder(256)
                    .append("Unsupported service name ").append(serviceName).toString());
        } else {
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/codec/PbEnDecoder.java`
#### Snippet
```java
        Integer methodId = rpcMethodMap.get(methodName);
        if (methodId == null) {
            throw new Exception(new StringBuilder(256)
                    .append("Unsupported method name ").append(methodName).toString());
        } else {
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/netty/NettyRequestContext.java`
#### Snippet
```java
        if ((System.currentTimeMillis() - receiveTime) >= request.getTimeout()) {
            if (logger.isDebugEnabled()) {
                logger.debug(new StringBuilder(512)
                        .append("Timeout,so give up send response to client.RequestId:")
                        .append(request.getSerialNo()).append(".client:")
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/netty/NettyRequestContext.java`
#### Snippet
```java
                        if (logger.isDebugEnabled()) {
                            if (IOException.class.isAssignableFrom(exception.getClass())) {
                                logger.debug(new StringBuilder(512)
                                        .append("server write response error.")
                                        .append("reason: ")
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/netty/NettyRequestContext.java`
#### Snippet
```java
                                        .append(exception.toString()).toString());
                            } else {
                                logger.debug(new StringBuilder(512)
                                        .append("server write response error.")
                                        .append("reason: ")
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/netty/NettyRequestContext.java`
#### Snippet
```java
                    } catch (Throwable ee) {
                        if (logger.isDebugEnabled()) {
                            logger.debug(new StringBuilder(512)
                                    .append("Exception while creating response ")
                                    .append(ee).toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/netty/NettyRequestContext.java`
#### Snippet
```java
            }
        } catch (IOException e) {
            logger.warn(new StringBuilder(512)
                    .append("Exception while creating response ")
                    .append(e).toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/netty/NettyProtocolDecoder.java`
#### Snippet
```java
                }
            }
            throw new UnknownProtocolException(new StringBuilder(256)
                    .append("Unknown protocol exception for message frame, channel.address = ")
                    .append(channel.remoteAddress().toString()).toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/netty/NettyClientFactory.java`
#### Snippet
```java
                        pipeline.addLast("ssl", new SslHandler(sslEngine));
                    } catch (Throwable t) {
                        logger.error(new StringBuilder(256)
                                .append("Create SSLEngine to connection ")
                                .append(addressInfo.getHostPortStr()).append(" failure!").toString(), t);
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/netty/NettyClientFactory.java`
#### Snippet
```java
                    conf.getInt(RpcConstants.WORKER_COUNT,
                            RpcConstants.CFG_DEFAULT_CLIENT_WORKER_COUNT);
            String threadName = new StringBuilder(256)
                    .append(conf.getString(RpcConstants.WORKER_THREAD_NAME,
                            RpcConstants.CFG_DEFAULT_WORKER_THREAD_NAME))
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/netty/NettyClientFactory.java`
#### Snippet
```java
        if (!future.isDone()) {
            future.cancel(false);
            throw new LocalConnException(new StringBuilder(256).append("Create connection to ")
                    .append(addressInfo.getHostPortStr()).append(" timeout!").toString());
        }
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/netty/NettyClientFactory.java`
#### Snippet
```java
        }
        if (future.isCancelled()) {
            throw new LocalConnException(new StringBuilder(256).append("Create connection to ")
                    .append(addressInfo.getHostPortStr()).append(" cancelled by user!").toString());
        }
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/netty/NettyClientFactory.java`
#### Snippet
```java
        }
        if (!future.isSuccess()) {
            throw new LocalConnException(new StringBuilder(256).append("Create connection to ")
                    .append(addressInfo.getHostPortStr()).append(" error").toString(),
                    future.cause());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/netty/NettyRpcServer.java`
#### Snippet
```java
        this.started.set(true);
        if (isOverTLS) {
            logger.info(new StringBuilder(256)
                    .append("TLS RpcServer started, listen port: ")
                    .append(listenPort).toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/netty/NettyRpcServer.java`
#### Snippet
```java
                    .append(listenPort).toString());
        } else {
            logger.info(new StringBuilder(256)
                    .append("TCP RpcServer started, listen port: ")
                    .append(listenPort).toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/netty/NettyRpcServer.java`
#### Snippet
```java
                }
            } catch (IOException e) {
                logger.warn(new StringBuilder(512)
                        .append("Exception while creating response ")
                        .append(e).toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/netty/NettyRpcServer.java`
#### Snippet
```java
        if (protocol == null) {
            if (ProtocolFactory.getProtocol(protocolType) == null) {
                throw new Exception(new StringBuilder(256)
                        .append("Invalid protocol type ").append(protocolType)
                        .append("! You have to register you new protocol before publish service.").toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/netty/NettyRpcServer.java`
#### Snippet
```java
            }
            if (keyStorePath == null || keyStorePassword == null) {
                throw new Exception(new StringBuilder(512).append("Required parameters: ")
                        .append(RpcConstants.TLS_KEYSTORE_PATH).append(" or ")
                        .append(RpcConstants.TLS_KEYSTORE_PASSWORD).append(" for TLS!").toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/netty/NettyRpcServer.java`
#### Snippet
```java
            if (this.needTwoWayAuthentic) {
                if (trustStorePath == null || trustStorePassword == null) {
                    throw new Exception(new StringBuilder(512).append("Required parameters: ")
                            .append(RpcConstants.TLS_TRUSTSTORE_PATH).append(" or ")
                            .append(RpcConstants.TLS_TRUSTSTORE_PASSWORD).append(" for TLS!").toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/netty/NettyRpcServer.java`
#### Snippet
```java
                        if (curTime - befTime > 180000) {
                            if (lastParseTime.compareAndSet(befTime, System.currentTimeMillis())) {
                                logger.warn(new StringBuilder(512)
                                        .append("[Abnormal Visit] Abnormal Message Content visit list is :")
                                        .append(errParseAddrMap).toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/netty/NettyRpcServer.java`
#### Snippet
```java
                List<ByteBuffer> res =
                        prepareResponse(null, rmtVersion, RPCProtos.ResponseHeader.Status.FATAL,
                                e1.getClass().getName(), new StringBuilder(512)
                                        .append("IPC server unable to read call parameters:")
                                        .append(e1.getMessage()).toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/netty/NettyRpcServer.java`
#### Snippet
```java
                List<ByteBuffer> res =
                        prepareResponse(null, rmtVersion, RPCProtos.ResponseHeader.Status.FATAL,
                                ee.getClass().getName(), new StringBuilder(512)
                                        .append("IPC server handle request error :")
                                        .append(ee.getMessage()).toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/netty/NettyClient.java`
#### Snippet
```java
                        if (!responseWrapper.isSuccess()) {
                            Throwable remote =
                                    MixUtils.unwrapException(new StringBuilder(512)
                                            .append(responseWrapper.getErrMsg()).append("#")
                                            .append(responseWrapper.getStackTrace()).toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/netty/NettyClient.java`
#### Snippet
```java
                this.channel = null;
            }
            logger.info(new StringBuilder(256).append("Client(")
                    .append(clientStr).append(") closed").toString());
        }
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/RpcServiceFactory.java`
#### Snippet
```java

    private String getServiceKey(String targetAddress, String serviceName) {
        return new StringBuilder(256).append(serviceName)
                .append("@").append(targetAddress).toString();
    }
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/RpcServiceFactory.java`
#### Snippet
```java
                    && (System.currentTimeMillis() - curLastPrintTime > 120000)) {
                if (lastLogPrintTime.compareAndSet(curLastPrintTime, System.currentTimeMillis())) {
                    logger.info(new StringBuilder(512)
                            .append("[Remote Address] forbidden list : ")
                            .append(forbiddenAddrMap.toString()).toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/RpcServiceFactory.java`
#### Snippet
```java
                        RpcConstants.CFG_UNAVAILABLE_FORBIDDEN_DURATION_MS);
        connectionManager = new ConnectionManager();
        connectionManager.setName(new StringBuilder(256)
                .append("rpcFactory-Thread-")
                .append(threadIdGen.getAndIncrement()).toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/RpcServiceFactory.java`
#### Snippet
```java

    private String getFailoverServiceKey(MasterInfo masterInfo, String serviceName) {
        return new StringBuilder(256).append(serviceName)
                .append(TokenConstants.GROUP_SEP)
                .append(masterInfo.getMasterClusterStr()).toString();
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/utils/KeyBuilderUtils.java`
#### Snippet
```java

    public static String buildAddressInfo(String brokerIp, int brokerPort) {
        return new StringBuilder(TBaseConstants.BUILDER_DEFAULT_SIZE)
                .append(brokerIp).append(TokenConstants.ATTR_SEP)
                .append(brokerPort).toString();
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/utils/KeyBuilderUtils.java`
#### Snippet
```java

    public static String buildTopicConfRecKey(int brokerId, String topicName) {
        return new StringBuilder(TBaseConstants.BUILDER_DEFAULT_SIZE)
                .append(brokerId).append(TokenConstants.ATTR_SEP)
                .append(topicName).toString();
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/utils/KeyBuilderUtils.java`
#### Snippet
```java

    public static String buildGroupTopicRecKey(String groupName, String topicName) {
        return new StringBuilder(TBaseConstants.BUILDER_DEFAULT_SIZE)
                .append(topicName).append(TokenConstants.ATTR_SEP)
                .append(groupName).toString();
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/Message.java`
#### Snippet
```java
                || valueVal.contains(TokenConstants.TOKEN_MSG_TYPE)
                || valueVal.contains(TokenConstants.TOKEN_MSG_TIME)) {
            throw new IllegalArgumentException(new StringBuilder(512).append("System Headers(")
                    .append(TokenConstants.TOKEN_MSG_TYPE).append(",")
                    .append(TokenConstants.TOKEN_MSG_TIME)
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/Message.java`
#### Snippet
```java
                || (valueVal.contains(TokenConstants.ARRAY_SEP)
                        || valueVal.contains(TokenConstants.EQ))) {
            throw new IllegalArgumentException(new StringBuilder(512).append("(")
                    .append(TokenConstants.ARRAY_SEP).append(",")
                    .append(TokenConstants.EQ).append(
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/utils/ServiceStatusHolder.java`
#### Snippet
```java
            isServiceStopped.set(isStopped);
            if (isStopped) {
                logger.warn(new StringBuilder(256)
                        .append("[Service Status]: global-write stopped by caller ")
                        .append(caller).toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/utils/ServiceStatusHolder.java`
#### Snippet
```java
            isReadStopped.set(isReadStop);
            if (isReadStop) {
                logger.warn(new StringBuilder(256)
                        .append("[Service Status]: global-read stopped by caller ")
                        .append(caller).toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/utils/ServiceStatusHolder.java`
#### Snippet
```java
                    lastReadStatsTime.set(System.currentTimeMillis());
                }
                logger.warn(new StringBuilder(256)
                        .append("[Service Status]: global-read opened by caller ")
                        .append(caller).toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/utils/ServiceStatusHolder.java`
#### Snippet
```java
            isWriteStopped.set(isWriteStop);
            if (isWriteStop) {
                logger.warn(new StringBuilder(256)
                        .append("[Service Status]: global-write stopped by caller ")
                        .append(caller).toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/utils/ServiceStatusHolder.java`
#### Snippet
```java
                    lastWriteStatsTime.set(System.currentTimeMillis());
                }
                logger.warn(new StringBuilder(256)
                        .append("[Service Status]: global-write opened by caller ")
                        .append(caller).toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/config/TLSConfig.java`
#### Snippet
```java
    @Override
    public String toString() {
        return new StringBuilder(512)
                .append("\"TLSConfig\":{\"tlsEnable\":").append(tlsEnable)
                .append(",\"tlsPort\":").append(tlsPort)
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/daemon/AbstractDaemonService.java`
#### Snippet
```java
        }
        if (this.shutdown.compareAndSet(false, true)) {
            logger.info(new StringBuilder(256).append(name)
                    .append("-daemon-thread closing ......").toString());
            try {
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/daemon/AbstractDaemonService.java`
#### Snippet
```java
                //
            }
            logger.info(new StringBuilder(256).append(name)
                    .append("-daemon-thread stopped!").toString());
            return false;
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/utils/AddressUtils.java`
#### Snippet
```java
            return true;
        }
        throw new AddressException(new StringBuilder(256)
                .append("Illegal parameter: not found the ip(").append(currLocalHost)
                .append(") or ip v4 address in local networkInterfaces!").toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/utils/AddressUtils.java`
#### Snippet
```java
            throw new AddressException("Call getIPV4LocalAddress throw exception", e);
        }
        throw new AddressException(new StringBuilder(256)
                .append("Illegal parameter: not found the default ip")
                .append(" in local networkInterfaces!").toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/utils/AddressUtils.java`
#### Snippet
```java

    public static String intToIp(int ipInt) {
        return new StringBuilder(128).append(((ipInt >> 24) & 0xff)).append('.')
                .append((ipInt >> 16) & 0xff).append('.').append((ipInt >> 8) & 0xff).append('.')
                .append((ipInt & 0xff)).toString();
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/utils/AddressUtils.java`
#### Snippet
```java
            return new Tuple2<>(false, fstV4IP);
        } catch (Throwable e) {
            throw new AddressException(new StringBuilder(256)
                    .append("Illegal parameter: ")
                    .append("unable to obtain valid IP from network card ")
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/utils/AddressUtils.java`
#### Snippet
```java
        }
        if (foundNetInter) {
            throw new AddressException(new StringBuilder(256)
                    .append("Illegal parameter: not found valid ip")
                    .append(" in networkInterfaces ").append(defEthName).toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/utils/AddressUtils.java`
#### Snippet
```java
                    .append(" in networkInterfaces ").append(defEthName).toString());
        } else {
            throw new AddressException(new StringBuilder(256)
                    .append("Illegal parameter: ").append(defEthName)
                    .append(" does not exist or is not in a valid state!").toString());
```

### StringBufferReplaceableByString
`StringBuilder sBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/cluster/SubscribeInfo.java`
#### Snippet
```java

    private void builderSubscribeStr() {
        StringBuilder sBuilder = new StringBuilder(256);
        this.fullInfo = sBuilder.append(consumerId).append(TokenConstants.GROUP_SEP)
                .append(group).append(TokenConstants.SEGMENT_SEP)
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/cluster/NodeAddrInfo.java`
#### Snippet
```java
        this.host = host.trim();
        this.port = port;
        this.hostPortStr = new StringBuilder(256).append(host)
                .append(TokenConstants.ATTR_SEP).append(port).toString();
    }
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/aaaclient/SimpleClientAuthenticateHandler.java`
#### Snippet
```java
                new SecureRandom(StringUtils.getBytesUtf8(String.valueOf(timestamp))).nextInt(Integer.MAX_VALUE);
        String signature = TStringUtils.getAuthSignature(userName, usrPassWord, timestamp, nonce);
        return new StringBuilder(512).append(userName)
                .append(TokenConstants.BLANK).append(timestamp)
                .append(TokenConstants.BLANK).append(nonce)
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/common/StatsConfig.java`
#### Snippet
```java
    @Override
    public String toString() {
        return new StringBuilder(512)
                .append("\"StatsConfig\":{\"statsLevel\":\"").append(statsLevel.getName())
                .append("\",\"enableSelfPrint\":").append(enableSelfPrint)
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/config/ConsumerConfig.java`
#### Snippet
```java
        String tmpConsumerGroup = consumerGroup.trim();
        if (tmpConsumerGroup.length() > TBaseConstants.META_MAX_GROUPNAME_LENGTH) {
            throw new IllegalArgumentException(new StringBuilder(512)
                    .append("Illegal parameter: the max length of consumerGroup is ")
                    .append(TBaseConstants.META_MAX_GROUPNAME_LENGTH)
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/config/ConsumerConfig.java`
#### Snippet
```java
        }
        if (!tmpConsumerGroup.matches(TBaseConstants.META_TMP_GROUP_VALUE)) {
            throw new IllegalArgumentException(new StringBuilder(512)
                    .append("Illegal parameter: the value of consumerGroup")
                    .append(" must begin with a letter, ")
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/config/ConsumerConfig.java`
#### Snippet
```java
    @Override
    public String toString() {
        return new StringBuilder(512).append("\"ConsumerConfig\":{")
                .append("\"consumerGroup\":\"").append(this.consumerGroup)
                .append("\",\"maxSubInfoReportIntvlTimes\":").append(this.maxSubInfoReportIntvlTimes)
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/factory/TubeBaseSessionFactory.java`
#### Snippet
```java
            throws TubeClientException {
        if (!tubeClientConfig.getMasterInfo().equals(consumerConfig.getMasterInfo())) {
            throw new TubeClientException(new StringBuilder(512)
                    .append("consumerConfig's masterInfo not equal!")
                    .append(" SessionFactory's masterInfo is ")
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/factory/TubeBaseSessionFactory.java`
#### Snippet
```java
        this.brokerRcvQltyStats =
                new DefaultBrokerRcvQltyStats(this.getRpcServiceFactory(), this.tubeClientConfig);
        logger.info(new StringBuilder(512)
                .append("Created Session Factory, the config is: ")
                .append(tubeClientConfig.toJsonString()).toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/factory/TubeBaseSessionFactory.java`
#### Snippet
```java
            throws TubeClientException {
        if (!tubeClientConfig.getMasterInfo().equals(consumerConfig.getMasterInfo())) {
            throw new TubeClientException(new StringBuilder(512)
                    .append("consumerConfig's masterInfo not equal!")
                    .append(" SessionFactory's masterInfo is ")
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/factory/TubeBaseSessionFactory.java`
#### Snippet
```java
            throws TubeClientException {
        if (!tubeClientConfig.getMasterInfo().equals(consumerConfig.getMasterInfo())) {
            throw new TubeClientException(new StringBuilder(512)
                    .append("consumerConfig's masterInfo not equal!")
                    .append(" SessionFactory's masterInfo is ")
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/producer/RoundRobinPartitionRouter.java`
#### Snippet
```java
    public Partition getPartition(final Message message, final List<Partition> partitions) throws TubeClientException {
        if (partitions == null || partitions.isEmpty()) {
            throw new TubeClientException(new StringBuilder(512)
                    .append("No available partition for topic: ")
                    .append(message.getTopic()).toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/SimplePushMessageConsumer.java`
#### Snippet
```java
    public void pauseConsume() {
        this.consumeSync = new CountDownLatch(1);
        logger.info(new StringBuilder(256)
                .append("[PauseConsume] Consume is paused, consumerId :")
                .append(this.baseConsumer.consumerId).toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/SimplePushMessageConsumer.java`
#### Snippet
```java
                });
            } catch (final RejectedExecutionException e) {
                logger.error(new StringBuilder(512)
                        .append("MessageListener thread poll is busy, topic=")
                        .append(request.getPartition().getTopic())
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/SimplePushMessageConsumer.java`
#### Snippet
```java
    public void resumeConsume() {
        this.consumeSync.countDown();
        logger.info(new StringBuilder(256)
                .append("[ResumeConsume] Consume is resume, consumerId :")
                .append(this.baseConsumer.consumerId).toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/policies/FlowCtrlRuleHandler.java`
#### Snippet
```java
            JsonObject jsonObject) throws Exception {
        if (jsonObject == null || jsonObject.get("type").getAsInt() != 1) {
            throw new Exception(new StringBuilder(512)
                    .append("parse freq_limit rule failure in record(")
                    .append(recordNo).append(") of flowCtrlInfo value!").toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/policies/FlowCtrlRuleHandler.java`
#### Snippet
```java
        }
        if (!jsonObject.has("rule")) {
            throw new Exception(new StringBuilder(512)
                    .append("FIELD rule is required in freq_limit record(")
                    .append(recordNo).append(") of flowCtrlInfo value!").toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/policies/FlowCtrlRuleHandler.java`
#### Snippet
```java
        JsonArray ruleArray = jsonObject.get("rule").getAsJsonArray();
        if (ruleArray == null) {
            throw new Exception(new StringBuilder(512)
                    .append("emtpy rule define in freq_limit record(")
                    .append(recordNo).append(") of flowCtrlInfo value!").toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/policies/FlowCtrlRuleHandler.java`
#### Snippet
```java
            JsonObject ruleObject = ruleArray.get(index).getAsJsonObject();
            if (!ruleObject.has("zeroCnt")) {
                throw new Exception(new StringBuilder(512)
                        .append("FIELD zeroCnt is required in freq_limit item(")
                        .append(itemNo).append(").record(").append(recordNo)
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/policies/FlowCtrlRuleHandler.java`
#### Snippet
```java
            int zeroCnt = ruleObject.get("zeroCnt").getAsInt();
            if (zeroCnt < 1) {
                throw new Exception(new StringBuilder(512)
                        .append("the value of FIELD zeroCnt must be greater than or equal to 1 ")
                        .append("in freq_limit item(").append(itemNo)
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/policies/FlowCtrlRuleHandler.java`
#### Snippet
```java
            }
            if (!ruleObject.has("freqInMs")) {
                throw new Exception(new StringBuilder(512)
                        .append("FIELD freqInMs is required in freq_limit item(")
                        .append(itemNo).append(").record(").append(recordNo)
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/policies/FlowCtrlRuleHandler.java`
#### Snippet
```java
            int freqInMs = ruleObject.get("freqInMs").getAsInt();
            if (freqInMs < 0) {
                throw new Exception(new StringBuilder(512)
                        .append("the value of FIELD freqInMs must be greater than or equal to 0 ")
                        .append("in freq_limit item(").append(itemNo)
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/policies/FlowCtrlRuleHandler.java`
#### Snippet
```java
        }
        if (flowCtrlItems.isEmpty()) {
            throw new Exception(new StringBuilder(512)
                    .append("not found valid rule define in freq_limit record(")
                    .append(recordNo).append(") of flowCtrlInfo value!").toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/policies/FlowCtrlRuleHandler.java`
#### Snippet
```java
            JsonObject jsonObject) throws Exception {
        if (jsonObject == null || jsonObject.get("type").getAsInt() != 3) {
            throw new Exception(new StringBuilder(512)
                    .append("parse low_fetch_limit rule failure in record(")
                    .append(recordNo).append(") of flowCtrlInfo value!").toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/policies/FlowCtrlRuleHandler.java`
#### Snippet
```java
        }
        if (!jsonObject.has("rule")) {
            throw new Exception(new StringBuilder(512)
                    .append("FIELD rule is required in low_fetch_limit record(")
                    .append(recordNo).append(") of flowCtrlInfo value!").toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/policies/FlowCtrlRuleHandler.java`
#### Snippet
```java
        JsonArray ruleArray = jsonObject.get("rule").getAsJsonArray();
        if (ruleArray == null) {
            throw new Exception(new StringBuilder(512)
                    .append("emtpy rule define in low_fetch_limit record(")
                    .append(recordNo).append(") of flowCtrlInfo value!").toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/policies/FlowCtrlRuleHandler.java`
#### Snippet
```java
        }
        if (ruleArray.size() > 1) {
            throw new Exception(new StringBuilder(512)
                    .append("only allow set one rule in low_fetch_limit record(")
                    .append(recordNo).append(") of flowCtrlInfo value!").toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/policies/FlowCtrlRuleHandler.java`
#### Snippet
```java
                    || ruleObject.has("minDataFilterFreqInMs")) {
                if (!ruleObject.has("filterFreqInMs")) {
                    throw new Exception(new StringBuilder(512)
                            .append("FIELD filterFreqInMs is required ")
                            .append("in low_fetch_limit item(").append(itemNo)
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/policies/FlowCtrlRuleHandler.java`
#### Snippet
```java
                filterFreqInMs = ruleObject.get("filterFreqInMs").getAsInt();
                if (filterFreqInMs < 0 || filterFreqInMs > 300000) {
                    throw new Exception(new StringBuilder(512)
                            .append("the value of FIELD filterFreqInMs must in [0, 300000] ")
                            .append("in low_fetch_limit item(").append(itemNo)
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/policies/FlowCtrlRuleHandler.java`
#### Snippet
```java
                }
                if (!ruleObject.has("minDataFilterFreqInMs")) {
                    throw new Exception(new StringBuilder(512)
                            .append("FIELD minDataFilterFreqInMs is required ")
                            .append("in low_fetch_limit item(").append(itemNo)
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/policies/FlowCtrlRuleHandler.java`
#### Snippet
```java
                minDataFilterFreqInMs = ruleObject.get("minDataFilterFreqInMs").getAsInt();
                if (minDataFilterFreqInMs < 0 || minDataFilterFreqInMs > 300000) {
                    throw new Exception(new StringBuilder(512)
                            .append("the value of FIELD minDataFilterFreqInMs must in [0, 300000] ")
                            .append("in low_fetch_limit item(").append(itemNo)
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/policies/FlowCtrlRuleHandler.java`
#### Snippet
```java
                }
                if (minDataFilterFreqInMs < filterFreqInMs) {
                    throw new Exception(new StringBuilder(512)
                            .append("the value of FIELD minDataFilterFreqInMs must be greater ")
                            .append("than or equal to the value of FIELD filterFreqInMs")
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/policies/FlowCtrlRuleHandler.java`
#### Snippet
```java
                normfreqInMs = ruleObject.get("normFreqInMs").getAsInt();
                if (normfreqInMs < 0 || normfreqInMs > 300000) {
                    throw new Exception(new StringBuilder(512)
                            .append("the value of FIELD normFreqInMs must in [0, 300000] ")
                            .append("in low_fetch_limit item(").append(itemNo)
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/policies/FlowCtrlRuleHandler.java`
#### Snippet
```java
        }
        if (flowCtrlItems.isEmpty()) {
            throw new Exception(new StringBuilder(512)
                    .append("not found valid rule define in low_fetch_limit record(")
                    .append(recordNo).append(") of flowCtrlInfo value!").toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/policies/FlowCtrlRuleHandler.java`
#### Snippet
```java
                JsonObject jsonObject = jsonItem.getAsJsonObject();
                if (!jsonObject.has("type")) {
                    throw new Exception(new StringBuilder(512)
                            .append("FIELD type is required in record(")
                            .append(recordNo).append(") of flowCtrlInfo value!").toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/policies/FlowCtrlRuleHandler.java`
#### Snippet
```java
                int typeVal = jsonObject.get("type").getAsInt();
                if (typeVal < 0 || typeVal > 3) {
                    throw new Exception(new StringBuilder(512)
                            .append("the value of FIELD type must in [0,1,3] in record(")
                            .append(recordNo).append(") of flowCtrlInfo value!").toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/policies/FlowCtrlRuleHandler.java`
#### Snippet
```java
            }
        } catch (Throwable e2) {
            throw new Exception(new StringBuilder(512)
                    .append("Parse flowCtrlInfo value failure, ")
                    .append(e2.getMessage()).toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/policies/FlowCtrlRuleHandler.java`
#### Snippet
```java
            int itemNo, int recordNo) throws Exception {
        if (!ruleObject.has(fieldName)) {
            throw new Exception(new StringBuilder(512)
                    .append("FIELD ").append(fieldName).append(" is required ")
                    .append("in data_limit item(").append(itemNo)
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/policies/FlowCtrlRuleHandler.java`
#### Snippet
```java
        String strTimeVal = ruleObject.get(fieldName).getAsString();
        if (TStringUtils.isBlank(strTimeVal)) {
            throw new Exception(new StringBuilder(512)
                    .append("the value of FIELD ").append(fieldName)
                    .append(" is null or blank in data_limit item(").append(itemNo)
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/policies/FlowCtrlRuleHandler.java`
#### Snippet
```java
                || TStringUtils.isBlank(startItems[0])
                || TStringUtils.isBlank(startItems[1])) {
            throw new Exception(new StringBuilder(512)
                    .append("illegal format, the value of FIELD ").append(fieldName)
                    .append(" must be 'aa:bb' and 'aa','bb' must be int value ")
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/policies/FlowCtrlRuleHandler.java`
#### Snippet
```java
            timeHour = Integer.parseInt(startItems[0]);
        } catch (Throwable e2) {
            throw new Exception(new StringBuilder(512)
                    .append("illegal format, the value of FIELD ").append(fieldName)
                    .append(" must be 'aa:bb' and 'aa' must be int value ")
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/policies/FlowCtrlRuleHandler.java`
#### Snippet
```java
            timeMin = Integer.parseInt(startItems[1]);
        } catch (Throwable e2) {
            throw new Exception(new StringBuilder(512)
                    .append("illegal format, the value of FIELD ").append(fieldName)
                    .append(" must be 'aa:bb' and 'bb' must be int value ")
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/policies/FlowCtrlRuleHandler.java`
#### Snippet
```java
        }
        if (timeHour < 0 || timeHour > 24) {
            throw new Exception(new StringBuilder(512)
                    .append("illegal value, the value of FIELD ").append(fieldName)
                    .append("-hour value must in [0,23] in data_limit item(")
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/policies/FlowCtrlRuleHandler.java`
#### Snippet
```java
        }
        if (timeMin < 0 || timeMin > 59) {
            throw new Exception(new StringBuilder(512)
                    .append("illegal value, the value of FIELD ").append(fieldName)
                    .append("-minute value must in [0,59] in data_limit item(")
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/policies/FlowCtrlRuleHandler.java`
#### Snippet
```java
            JsonObject jsonObject) throws Exception {
        if (jsonObject == null || jsonObject.get("type").getAsInt() != 0) {
            throw new Exception(new StringBuilder(512)
                    .append("parse data_limit rule failure in record(")
                    .append(recordNo).append(") of flowCtrlInfo value!").toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/policies/FlowCtrlRuleHandler.java`
#### Snippet
```java
        }
        if (!jsonObject.has("rule")) {
            throw new Exception(new StringBuilder(512)
                    .append("FIELD rule is required in data_limit record(")
                    .append(recordNo).append(") of flowCtrlInfo value!").toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/policies/FlowCtrlRuleHandler.java`
#### Snippet
```java
        JsonArray ruleArray = jsonObject.get("rule").getAsJsonArray();
        if (ruleArray == null) {
            throw new Exception(new StringBuilder(512)
                    .append("emtpy rule define in data_limit record(")
                    .append(recordNo).append(") of flowCtrlInfo value!").toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/policies/FlowCtrlRuleHandler.java`
#### Snippet
```java
            int endTime = validAndGetTimeValue(ruleObject, "end", itemNo, recordNo);
            if (startTime >= endTime) {
                throw new Exception(new StringBuilder(512)
                        .append("the value of FIELD start must lower than the value FIELD end ")
                        .append("in data_limit item(").append(itemNo)
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/policies/FlowCtrlRuleHandler.java`
#### Snippet
```java
            }
            if (!ruleObject.has("dltInM")) {
                throw new Exception(new StringBuilder(512)
                        .append("FIELD dltInM is required in data_limit item(")
                        .append(itemNo).append(").record(").append(recordNo)
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/policies/FlowCtrlRuleHandler.java`
#### Snippet
```java
            long dltVal = ruleObject.get("dltInM").getAsLong();
            if (dltVal <= 20) {
                throw new Exception(new StringBuilder(512)
                        .append("the value of FIELD dltInM must be greater than 20 ")
                        .append("in data_limit item(").append(itemNo)
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/policies/FlowCtrlRuleHandler.java`
#### Snippet
```java
            }
            if (!ruleObject.has("limitInM")) {
                throw new Exception(new StringBuilder(512)
                        .append("FIELD limitInM is required in data_limit item(")
                        .append(itemNo).append(").record(").append(recordNo)
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/policies/FlowCtrlRuleHandler.java`
#### Snippet
```java
            long dataLimitInM = ruleObject.get("limitInM").getAsLong();
            if (dataLimitInM < 0) {
                throw new Exception(new StringBuilder(512)
                        .append("the value of FIELD limitInM must be greater than or equal to 0 ")
                        .append("in data_limit item(").append(itemNo)
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/policies/FlowCtrlRuleHandler.java`
#### Snippet
```java
            dataLimitInM = dataLimitInM * 1024 * 1024;
            if (!ruleObject.has("freqInMs")) {
                throw new Exception(new StringBuilder(512)
                        .append("FIELD freqInMs is required in data_limit item(")
                        .append(itemNo).append(").record(").append(recordNo)
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/policies/FlowCtrlRuleHandler.java`
#### Snippet
```java
            int freqInMs = ruleObject.get("freqInMs").getAsInt();
            if (freqInMs < 200) {
                throw new Exception(new StringBuilder(512)
                        .append("the value of FIELD freqInMs must be greater than or equal to 200 ")
                        .append("in data_limit item(").append(itemNo)
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/policies/FlowCtrlRuleHandler.java`
#### Snippet
```java
        }
        if (flowCtrlItems.isEmpty()) {
            throw new Exception(new StringBuilder(512)
                    .append("not found valid rule define in data_limit record(")
                    .append(recordNo).append(") of flowCtrlInfo value!").toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/producer/SimpleMessageProducer.java`
#### Snippet
```java
        for (String topicItem : topicSet) {
            if (TStringUtils.isBlank(topicItem)) {
                throw new TubeClientException(new StringBuilder(256)
                        .append("Illegal parameter: found blank topic value in topicSet : ")
                        .append(topicSet).toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/producer/SimpleMessageProducer.java`
#### Snippet
```java
                this.partitionRouter.getPartition(message, partList);
        if (partition == null) {
            throw new TubeClientException(new StringBuilder(512)
                    .append("Not found available partition for topic: ")
                    .append(message.getTopic()).toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/producer/SimpleMessageProducer.java`
#### Snippet
```java
        }
        if (this.publishTopicMap.get(message.getTopic()) == null) {
            throw new TubeClientException(new StringBuilder(512)
                    .append("Topic ").append(message.getTopic())
                    .append(" not publish, please publish first!").toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/producer/SimpleMessageProducer.java`
#### Snippet
```java
        }
        if (this.producerManager.getTopicPartition(message.getTopic()) == null) {
            throw new TubeClientException(new StringBuilder(512)
                    .append("Topic ").append(message.getTopic())
                    .append(" not publish, make sure the topic exist or acceptPublish and try later!").toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/producer/SimpleMessageProducer.java`
#### Snippet
```java
                : (message.getData().length + message.getAttribute().length());
        if (msgSize > producerManager.getMaxMsgSize(message.getTopic())) {
            throw new TubeClientException(new StringBuilder(512)
                    .append("Illegal parameter: over max message length for the total size of")
                    .append(" message data and attribute, allowed size is ")
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/tools/CliUtils.java`
#### Snippet
```java
            }
            if (!cli.hasOption(FILEPATH.longOpt)) {
                result.setFailResult(new StringBuilder(512)
                        .append("Please input the configuration file path by ")
                        .append("-").append(FILEPATH.opt).append(" or ")
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/tools/CliUtils.java`
#### Snippet
```java
            String configFilePath = cli.getOptionValue(FILEPATH.longOpt);
            if (TStringUtils.isBlank(configFilePath)) {
                result.setFailResult(new StringBuilder(512)
                        .append(FILEPATH.longOpt).append(" is required!").toString());
                return result.isSuccess();
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/tools/CliUtils.java`
#### Snippet
```java
            result.setSuccResult(configFilePath);
        } catch (Throwable e) {
            result.setFailResult(new StringBuilder(512)
                    .append("Parse configuration file path failure: ")
                    .append(e.toString()).toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/producer/qltystats/DefaultBrokerRcvQltyStats.java`
#### Snippet
```java
        long currentWaitCount = this.curTotalSentRequestNum.get();
        if (currentWaitCount >= this.clientConfig.getSessionMaxAllowedDelayedMsgCount()) {
            throw new TubeClientException(new StringBuilder(512)
                    .append("Current delayed messages over max allowed count, allowed is ")
                    .append(this.clientConfig.getSessionMaxAllowedDelayedMsgCount())
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/RmtDataCache.java`
#### Snippet
```java
            if (isFreeze) {
                partitionFrozenMap.put(partitionKey, System.currentTimeMillis());
                logger.info(new StringBuilder(512)
                        .append("[Freeze Partition] Partition : ")
                        .append(partitionKey).append(" is frozen by caller!").toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/RmtDataCache.java`
#### Snippet
```java
                if (frozenTime != null) {
                    releaseIdlePartition(partitionKey);
                    logger.info(new StringBuilder(512)
                            .append("[UnFreeze Partition] Partition : ")
                            .append(partitionKey).append(" is unFreeze by caller!").toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/RmtDataCache.java`
#### Snippet
```java
            return new PartitionSelectResult(false,
                    TErrCodeConstants.BAD_REQUEST,
                    new StringBuilder(256)
                            .append("Wait partition to consume abnormal : ")
                            .append(e1.getMessage()).toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/RmtDataCache.java`
#### Snippet
```java
            if (frozenTime != null) {
                releaseIdlePartition(partKey);
                logger.info(new StringBuilder(512)
                        .append("[UnFreeze Partition] Partition : ")
                        .append(partKey).append(" is unFreeze by caller-2!").toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/producer/ProducerManager.java`
#### Snippet
```java
                    @Override
                    public Thread newThread(Runnable r) {
                        Thread t = new Thread(r, new StringBuilder(256)
                                .append("Producer-Heartbeat-Thread-")
                                .append(producerId).toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/producer/ProducerManager.java`
#### Snippet
```java
            pidName = pidName.split("@")[0];
        }
        return new StringBuilder(256)
                .append(AddressUtils.getLocalAddress())
                .append("-").append(pidName)
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/tools/StoreRepairAdmin.java`
#### Snippet
```java
                    if (file.isFile() && file.toString().endsWith(fileSuffix)) {
                        if (!file.canRead()) {
                            throw new IOException(new StringBuilder(512)
                                    .append("Could not read DATA file ").append(file).toString());
                        }
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/tools/StoreRepairAdmin.java`
#### Snippet
```java
            }
            segments = new FileSegmentList(accum.toArray(new Segment[accum.size()]));
            logger.info(new StringBuilder(512)
                    .append("[Data Repair] Loaded DATA ")
                    .append(accum.size()).append(" segments from ")
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/tools/StoreRepairAdmin.java`
#### Snippet
```java
        final File storeDir = new File(storePath);
        if (!storeDir.exists()) {
            throw new RuntimeException(new StringBuilder(512)
                    .append("[Data Repair] store path is not existed, path is ")
                    .append(storePath).toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/tools/StoreRepairAdmin.java`
#### Snippet
```java
        }
        if (!storeDir.isDirectory()) {
            throw new RuntimeException(new StringBuilder(512)
                    .append("[Data Repair]  store path is not a directory, path is ")
                    .append(storePath).toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/tools/StoreRepairAdmin.java`
#### Snippet
```java
        final File[] ls = storeDir.listFiles();
        if (topicList == null || topicList.isEmpty()) {
            logger.warn(new StringBuilder(512)
                    .append("[Data Repair] Begin to scan store path: ")
                    .append(storePath).toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/tools/StoreRepairAdmin.java`
#### Snippet
```java
                    .append(storePath).toString());
        } else {
            logger.warn(new StringBuilder(512)
                    .append("[Data Repair] Begin to scan store path: ")
                    .append(storePath).append(",topicList:")
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/tools/StoreRepairAdmin.java`
#### Snippet
```java
            //
        }
        logger.warn(new StringBuilder(512)
                .append("[Data Repair] End to scan data path in ")
                .append((System.currentTimeMillis() - start) / 1000)
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/tools/StoreRepairAdmin.java`
#### Snippet
```java
                final Segment next = segments.get(i + 1);
                if (curr.getStart() + curr.getCachedSize() != next.getStart()) {
                    throw new IllegalStateException(new StringBuilder(512)
                            .append("The following ").append(segTypeStr)
                            .append(" segments don't validate: ")
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/BrokerConfig.java`
#### Snippet
```java
        this.primaryPath = brokerSect.get("primaryPath").trim();
        if (TStringUtils.isBlank(brokerSect.get("hostName"))) {
            throw new IllegalArgumentException(new StringBuilder(256).append("hostName is null or Blank in ")
                    .append(SECT_TOKEN_BROKER).append(" section!").toString());
        }
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/BrokerConfig.java`
#### Snippet
```java
                this.hostName = AddressUtils.getIPV4LocalAddress(this.defEthName);
            } catch (Throwable e) {
                throw new IllegalArgumentException(new StringBuilder(256)
                        .append("Get default broker hostName failure : ")
                        .append(e.getMessage()).toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/BrokerConfig.java`
#### Snippet
```java
        }
        if (TStringUtils.isBlank(brokerSect.get("masterAddressList"))) {
            throw new IllegalArgumentException(new StringBuilder(256)
                    .append("masterAddressList is null or Blank in ")
                    .append(SECT_TOKEN_BROKER).append(" section!").toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/BrokerConfig.java`
#### Snippet
```java
        if (this.visitMasterAuth) {
            if (TStringUtils.isBlank(brokerSect.get("visitName"))) {
                throw new IllegalArgumentException(new StringBuilder(256)
                        .append("visitName is null or Blank in ")
                        .append(SECT_TOKEN_BROKER).append(" section!").toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/BrokerConfig.java`
#### Snippet
```java
            }
            if (TStringUtils.isBlank(brokerSect.get("visitPassword"))) {
                throw new IllegalArgumentException(new StringBuilder(256)
                        .append("visitPassword is null or Blank in ").append(SECT_TOKEN_BROKER)
                        .append(" section!").toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/BrokerConfig.java`
#### Snippet
```java
        if (this.port == this.webPort
                || (tlsConfig.isTlsEnable() && (this.tlsConfig.getTlsPort() == this.webPort))) {
            throw new IllegalArgumentException(new StringBuilder(512)
                    .append("Illegal port value configuration, the value of ")
                    .append("port or tlsPort cannot be the same as the value of webPort!")
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/BrokerConfig.java`
#### Snippet
```java
                    || (tlsConfig.isTlsEnable()
                            && (this.tlsConfig.getTlsPort() == promHttpPort)))) {
                throw new IllegalArgumentException(new StringBuilder(512)
                        .append("Illegal port value configuration, the value of ")
                        .append("port or webPort or tlsPort cannot be the same as the value of promHttpPort!")
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/TubeBroker.java`
#### Snippet
```java
            builder.setBrokerDefaultConfInfo(metadataManager.getBrokerDefMetaConfInfo());
            builder.addAllBrokerTopicSetConfInfo(metadataManager.getTopicMetaConfInfoLst());
            logger.info(new StringBuilder(512)
                    .append("[HeartBeat request] current broker report info: brokerConfId=")
                    .append(metadataManager.getBrokerMetadataConfId())
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/TubeBroker.java`
#### Snippet
```java
        }
        builder.setClsConfig(buildClusterConfig());
        logger.info(new StringBuilder(512)
                .append("[Register request] current broker report info: brokerConfId=")
                .append(metadataManager.getBrokerMetadataConfId())
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/TubeBroker.java`
#### Snippet
```java

    private String generateBrokerClientId() {
        return new StringBuilder(512).append(tubeConfig.getBrokerId()).append(":")
                .append(tubeConfig.getHostName()).append(":")
                .append(tubeConfig.getPort()).append(":")
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/TubeBroker.java`
#### Snippet
```java
        this.brokerServiceServer.start();
        isOnline = true;
        logger.info(new StringBuilder(512)
                .append("Start tube server successfully, broker version=")
                .append(TubeServerVersion.SERVER_VERSION).toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/offset/offsetstorage/OffsetStorageInfo.java`
#### Snippet
```java
    @Override
    public String toString() {
        return new StringBuilder(512).append("OffsetStorageInfo [OffsetStoreKey=")
                .append(topic).append(TokenConstants.HYPHEN).append(brokerId)
                .append(TokenConstants.HYPHEN).append(partitionId)
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/tools/cli/CliMetaDataBRU.java`
#### Snippet
```java
            if (operationType.equalsIgnoreCase("backup")) {
                if (!metaDataDir.mkdirs()) {
                    throw new IOException(new StringBuilder(512)
                            .append("Creates the directory named ")
                            .append(metaDataDir.getAbsolutePath())
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/tools/cli/CliMetaDataBRU.java`
#### Snippet
```java
                }
            } else {
                throw new RuntimeException(new StringBuilder(512)
                        .append("Path ").append(backupAndRecoveryPath)
                        .append(" is not existed!").toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/tools/cli/CliMetaDataBRU.java`
#### Snippet
```java
        }
        if (!metaDataDir.isDirectory() || !metaDataDir.canRead()) {
            throw new RuntimeException(new StringBuilder(512)
                    .append("Path ").append(backupAndRecoveryPath)
                    .append(" is not a readable directory!").toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/offset/offsetstorage/ZkOffsetStorage.java`
#### Snippet
```java
    @Override
    public OffsetStorageInfo loadOffset(String group, String topic, int partitionId) {
        String zkNode = new StringBuilder(512).append(this.consumerZkDir).append("/")
                .append(group).append("/offsets/").append(topic).append("/")
                .append(brokerId).append(TokenConstants.HYPHEN)
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/offset/offsetstorage/ZkOffsetStorage.java`
#### Snippet
```java
        } catch (Throwable e) {
            BrokerSrvStatsHolder.incZKExcCnt();
            logger.error(new StringBuilder(256)
                    .append("[ZkOffsetStorage] Failed to connect ZooKeeper server (")
                    .append(this.zkConfig.getZkServerAddr()).append(") !").toString(), e);
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/offset/offsetstorage/ZkOffsetStorage.java`
#### Snippet
```java
    public Map<String, Set<String>> queryZkAllGroupTopicInfos() {
        // get all booked groups name
        String groupNode = new StringBuilder(512)
                .append(this.consumerZkDir).toString();
        List<String> bookedGroups = ZKUtil.getChildren(this.zkw, groupNode);
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/metadata/TopicMetadata.java`
#### Snippet
```java
    @Override
    public String toString() {
        return new StringBuilder(512).append("TopicMetadata [topic=").append(this.topic)
                .append(", unflushThreshold=").append(this.unflushThreshold)
                .append(", unflushInterval=").append(this.unflushInterval)
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/offset/DefaultOffsetManager.java`
#### Snippet
```java

    private String getOffsetCacheKey(String topic, int partitionId) {
        return new StringBuilder(256).append(topic)
                .append("-").append(partitionId).toString();
    }
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/offset/DefaultOffsetManager.java`
#### Snippet
```java
            long currentOffset = regInfo.getOffset();
            long offsetDelta = indexMaxOffset - currentOffset;
            logger.info(new StringBuilder(512)
                    .append("[Offset Manager] Manual update offset by modifier=")
                    .append(modifier).append(",resetOffset=").append(reSetOffset)
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/offset/DefaultOffsetManager.java`
#### Snippet
```java
        updatedOffset = regInfo.addAndGetOffset(tmpOffset);
        if (logger.isDebugEnabled()) {
            logger.debug(new StringBuilder(512)
                    .append("[Offset Manager] Update offset finished, offset=").append(updatedOffset)
                    .append(",group=").append(group).append(",topic-part=").append(offsetCacheKey).toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/BaseMessageConsumer.java`
#### Snippet
```java
            }
        } catch (Throwable e) {
            logger.error(new StringBuilder(256)
                    .append("flushLastRequest, commit ")
                    .append(partition.getTopic()).append("#")
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/BaseMessageConsumer.java`
#### Snippet
```java
                    @Override
                    public Thread newThread(Runnable r) {
                        Thread t = new Thread(r, new StringBuilder(512)
                                .append("Master-Heartbeat-Thread-")
                                .append(consumerId).toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/BaseMessageConsumer.java`
#### Snippet
```java
                                    AddressUtils.getLocalAddress(), consumerConfig.isTlsEnable());
                } catch (Throwable e) {
                    logger.error(new StringBuilder(512)
                            .append("Disconnect to Broker error! broker:")
                            .append(partResult.getPartition().getBroker().toString()).toString(), e);
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/BaseMessageConsumer.java`
#### Snippet
```java
        if ((filterConds != null) && (!filterConds.isEmpty())) {
            if (filterConds.size() > TBaseConstants.CFG_FLT_MAX_FILTER_ITEM_COUNT) {
                throw new TubeClientException(new StringBuilder(256)
                        .append("Parameter error: Over max allowed filter count, allowed count is ")
                        .append(TBaseConstants.CFG_FLT_MAX_FILTER_ITEM_COUNT).toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/BaseMessageConsumer.java`
#### Snippet
```java
                }
                if (filter.length() > TBaseConstants.CFG_FLT_MAX_FILTER_ITEM_LENGTH) {
                    throw new TubeClientException(new StringBuilder(256)
                            .append("Parameter error: over max allowed filter length, allowed length is ")
                            .append(TBaseConstants.CFG_FLT_MAX_FILTER_ITEM_LENGTH).toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/BaseMessageConsumer.java`
#### Snippet
```java
                            new TopicProcessor(messageListener, filterConds));
            if (oldProcessor != null) {
                throw new TubeClientException(new StringBuilder(256).append("Topic=")
                        .append(topic).append(" has been subscribed").toString());
            }
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/BaseMessageConsumer.java`
#### Snippet
```java
            return this;
        } else {
            throw new TubeClientException(new StringBuilder(256).append("Topic=")
                    .append(topic).append(" has been subscribed").toString());
        }
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/BaseMessageConsumer.java`
#### Snippet
```java
                }
            }
        }, new StringBuilder(512).append("Rebalance-Thread-")
                .append(this.consumerId).toString());
        this.rebalanceThread.setPriority(Thread.MAX_PRIORITY);
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/SimpleClientBalanceConsumer.java`
#### Snippet
```java
                    @Override
                    public Thread newThread(Runnable r) {
                        Thread t = new Thread(r, new StringBuilder(512)
                                .append("Master-Heartbeat-Thread-")
                                .append(consumerId).toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/SimpleClientBalanceConsumer.java`
#### Snippet
```java
            pidName = pidName.split("@")[0];
        }
        return new StringBuilder(256)
                .append(this.consumerConfig.getConsumerGroup())
                .append("_").append(AddressUtils.getLocalAddress())
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/SimpleClientBalanceConsumer.java`
#### Snippet
```java
                                    AddressUtils.getLocalAddress(), consumerConfig.isTlsEnable());
                } catch (Throwable e) {
                    logger.error(new StringBuilder(512)
                            .append("Disconnect to Broker error! broker:")
                            .append(partResult.getPartition().getBroker().toString()).toString(), e);
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/disk/FileSegmentList.java`
#### Snippet
```java
        final Segment startSeg = curViews[minStart];
        if (offset < startSeg.getStart()) {
            throw new ArrayIndexOutOfBoundsException(new StringBuilder(512)
                    .append("Request offsets is ").append(offset)
                    .append(", the start is ").append(startSeg.getStart()).toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/BrokerServiceServer.java`
#### Snippet
```java

    private String getHeartbeatNodeId(String consumerId, String partStr) {
        return new StringBuilder(512).append(consumerId)
                .append(TokenConstants.SEGMENT_SEP).append(partStr).toString();

```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/BrokerServiceServer.java`
#### Snippet
```java

    private String getPartStr(String group, String topic, int partitionId) {
        return new StringBuilder(512).append(group).append(TokenConstants.ATTR_SEP)
                .append(topic).append(TokenConstants.ATTR_SEP).append(partitionId).toString();
    }
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/nodeinfo/ConsumerNodeInfo.java`
#### Snippet
```java
            String[] strInfos = targetStr.split("-");
            if (strInfos.length > 2) {
                this.rmtAddrInfo = new StringBuilder(256)
                        .append(strInfos[0]).append("#").append(strInfos[1]).toString();
            }
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/web/BrokerAdminServlet.java`
#### Snippet
```java

    private String buildQueryID(String group, String topic, int partitionId) {
        return new StringBuilder(512).append(group)
                .append(TokenConstants.ATTR_SEP).append(topic)
                .append(TokenConstants.ATTR_SEP).append(partitionId).toString();
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/web/BrokerAdminServlet.java`
#### Snippet
```java
            if (keyItems.length != 3) {
                result.setFailResult(fieldDef.id,
                        new StringBuilder(512).append("Parameter ")
                                .append(fieldDef.name).append("'s key invalid:")
                                .append(entry.getKey())
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/web/BrokerAdminServlet.java`
#### Snippet
```java
            } catch (NumberFormatException e) {
                result.setFailResult(fieldDef.id,
                        new StringBuilder(512).append("Parameter ")
                                .append(fieldDef.name).append("'s key invalid:")
                                .append(entry.getKey())
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/web/BrokerAdminServlet.java`
#### Snippet
```java
        if (offsetVals.isEmpty()) {
            result.setFailResult(fieldDef.id,
                    new StringBuilder(512).append("Parameter ")
                            .append(fieldDef.name).append("'s value is invalid!").toString());
        } else {
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/web/BrokerAdminServlet.java`
#### Snippet
```java
        if (!bookedGroups.contains(srcGroupName)) {
            WebParameterUtils.buildFailResult(sBuffer,
                    new StringBuilder(512).append("Parameter ")
                            .append(WebFieldDef.SRCGROUPNAME.name).append(": ")
                            .append(srcGroupName)
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/web/BrokerAdminServlet.java`
#### Snippet
```java
                broker.getOffsetManager().getGroupSubInfo(groupName);
        if (subTopicSet == null || subTopicSet.isEmpty()) {
            result.setFailResult(400, new StringBuilder(512)
                    .append("Parameter ").append(groupFldDef.name)
                    .append(": subscribed topic set of ").append(groupName)
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/web/BrokerAdminServlet.java`
#### Snippet
```java
            }
            if (tgtTopicSet.isEmpty()) {
                result.setFailResult(400, new StringBuilder(512)
                        .append("Parameter ").append(groupFldDef.name)
                        .append(": ").append(groupName)
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/web/BrokerAdminServlet.java`
#### Snippet
```java
        Map<String, Set<Integer>> topicPartMap = getTopicPartitions(tgtTopicSet);
        if (topicPartMap.isEmpty()) {
            result.setFailResult(400, new StringBuilder(512)
                    .append("Parameter ").append(groupFldDef.name)
                    .append(": all topics subscribed by the group have been deleted!").toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/MessageStore.java`
#### Snippet
```java
    public void flushMemCacheData() throws IOException {
        if (this.closed.get()) {
            throw new IllegalStateException(new StringBuilder(512)
                    .append("[Data Store] Closed MessageStore for storeKey ")
                    .append(this.storeKey).toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/MessageStore.java`
#### Snippet
```java
    public boolean runClearupPolicy(boolean onlyCheck) {
        if (this.closed.get()) {
            throw new IllegalStateException(new StringBuilder(512)
                    .append("[Data Store] Closed MessageStore for storeKey ")
                    .append(this.storeKey).toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/MessageStore.java`
#### Snippet
```java
            long waitRetryMs) throws IOException {
        if (this.closed.get()) {
            throw new IllegalStateException(new StringBuilder(512)
                    .append("[Data Store] Closed MessageStore for storeKey ")
                    .append(this.storeKey).toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/MessageStore.java`
#### Snippet
```java
    public void refreshUnflushThreshold(TopicMetadata topicMetadata) {
        if (this.closed.get()) {
            throw new IllegalStateException(new StringBuilder(512)
                    .append("[Data Store] Closed MessageStore for storeKey ")
                    .append(this.storeKey).toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/MessageStore.java`
#### Snippet
```java
        // #lizard forgives
        if (this.closed.get()) {
            throw new IllegalStateException(new StringBuilder(512)
                    .append("[Data Store] Closed MessageStore for storeKey ")
                    .append(this.storeKey).toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/MessageStore.java`
#### Snippet
```java
        int memCacheSize = topicMetadata.getMemCacheMsgSize();
        if (memCacheSize < topicMetadata.getMinMemCacheSize()) {
            logger.info(new StringBuilder(512)
                    .append("[Data Store] ").append(getTopic())
                    .append(" writeCacheMaxSize changed, from ")
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/MessageStore.java`
#### Snippet
```java
                flushWriteCacheCondition.awaitNanos(FLUSH_CONDITION_WAIT_DLT_NS);
                if (System.currentTimeMillis() - startTime > 2000) {
                    logger.warn(new StringBuilder(512)
                            .append("[Data Store] StoreKey=").append(storeKey)
                            .append(" Wait Cache flush write too long! wait time is ")
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/MessageStore.java`
#### Snippet
```java
        } catch (InterruptedException e) {
            Thread.currentThread().interrupt();
            throw new IOException(new StringBuilder(512)
                    .append("[Data Store] StoreKey=").append(storeKey)
                    .append(" Interrupted when triggerFlushAndAddMsg process for storekey ")
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/MessageStore.java`
#### Snippet
```java
    public void flushFile() throws IOException {
        if (this.closed.get()) {
            throw new IllegalStateException(new StringBuilder(512)
                    .append("[Data Store] Closed MessageStore for storeKey ")
                    .append(this.storeKey).toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/MessageStore.java`
#### Snippet
```java
    public long getStartOffsetByTimeStamp(long timestamp) {
        if (this.closed.get()) {
            throw new IllegalStateException(new StringBuilder(512)
                    .append("[Data Store] Closed MessageStore for storeKey ")
                    .append(this.storeKey).toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/utils/FileUtil.java`
#### Snippet
```java
        if (!dir.exists()) {
            if (!dir.mkdirs()) {
                throw new RuntimeException(new StringBuilder(512)
                        .append("Create directory failed:")
                        .append(dir.getAbsolutePath()).toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/utils/FileUtil.java`
#### Snippet
```java
        }
        if (!dir.isDirectory()) {
            throw new RuntimeException(new StringBuilder(512)
                    .append("Path is not a directory:")
                    .append(dir.getAbsolutePath()).toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/disk/FileSegment.java`
#### Snippet
```java
                    BrokerSrvStatsHolder.incDiskIOExcCnt();
                }
                logger.error(new StringBuilder(512).append("[File Store] Close ")
                        .append(this.file.getAbsoluteFile().toString())
                        .append("'s ").append(segmentType).append(" file failure").toString(), ee);
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/disk/FileSegment.java`
#### Snippet
```java
                } else {
                    if (recoverResult.getTruncated() > 0) {
                        logger.info(new StringBuilder(512)
                                .append("[File Store] Recover DATA Segment succeeded in ")
                                .append((System.currentTimeMillis() - startMs) / 1000)
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/disk/FileSegment.java`
#### Snippet
```java
                } else {
                    if (recoverResult.getTruncated() > 0) {
                        logger.info(new StringBuilder(512)
                                .append("[File Store] Recover Index Segment succeeded in ")
                                .append((System.currentTimeMillis() - startMs) / 1000)
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/disk/FileSegment.java`
#### Snippet
```java
        }
        try {
            logger.info(new StringBuilder(512)
                    .append("[File Store] delete file ")
                    .append(file.getAbsoluteFile()).toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/utils/RowLock.java`
#### Snippet
```java
        HashedBytes rowKey = lockIds.remove(lockId);
        if (rowKey == null) {
            logger.warn(new StringBuilder(256).append(this.name)
                    .append(" release unknown lockId: ")
                    .append(lockId).toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/utils/RowLock.java`
#### Snippet
```java
        CountDownLatch rowLatch = lockedRows.remove(rowKey);
        if (rowLatch == null) {
            logger.error(new StringBuilder(256).append(this.name)
                    .append(" releases row not locked, lockId: ")
                    .append(lockId).append(" row: ").append(rowKey).toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/utils/RowLock.java`
#### Snippet
```java
                    if (!existingLatch.await(
                            this.rowLockWaitDuration, TimeUnit.MILLISECONDS)) {
                        throw new IOException(new StringBuilder(256)
                                .append("Timed out on getting lock for row=")
                                .append(rowKey).toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/utils/RowLock.java`
#### Snippet
```java
            HashedBytes rowFromLock = lockIds.get(lockId);
            if (!row.equals(rowFromLock)) {
                throw new IOException(new StringBuilder(512)
                        .append("Invalid row lock: LockId: ").append(lockId)
                        .append(" holds the lock for row: ").append(rowFromLock)
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/webbase/WebMethodMapper.java`
#### Snippet
```java
            }
        }
        logger.error(new StringBuilder(512)
                .append("registerWebMethod failure, not found Method by clsMethodName ")
                .append(clsMethodName).append(" in WebHandler class ")
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/MessageStoreManager.java`
#### Snippet
```java
                            }
                        } catch (Throwable ee) {
                            logger.error(new StringBuilder(512)
                                    .append("[Remove Topic] Close removed store failure, storeKey=")
                                    .append(tmpTopic).append("-").append(storeId).toString(), ee);
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/MessageStoreManager.java`
#### Snippet
```java
                                try {
                                    msgStore.close();
                                    logger.info(new StringBuilder(512)
                                            .append("[Store Manager] Close duplicated messageStore ")
                                            .append(msgStore.getStoreKey()).toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/MessageStoreManager.java`
#### Snippet
```java
                        } catch (Throwable e2) {
                            errCnt.incrementAndGet();
                            logger.error(new StringBuilder(512).append("[Store Manager] Loaded ")
                                    .append(subDir.getAbsolutePath())
                                    .append("message store failure:").toString(), e2);
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/MessageStoreManager.java`
#### Snippet
```java
                                subEntry.getValue().close();
                            } catch (final Throwable e) {
                                logger.error(new StringBuilder(512)
                                        .append("[Store Manager] Try to run close  ")
                                        .append(subEntry.getValue().getStoreKey()).append(" failed").toString(), e);
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/MessageStoreManager.java`
#### Snippet
```java
            final File dir = new File(path);
            if (!dir.exists() && !dir.mkdirs()) {
                throw new IOException(new StringBuilder(512)
                        .append("Could not make Log directory ")
                        .append(dir.getAbsolutePath()).toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/MessageStoreManager.java`
#### Snippet
```java
            }
            if (!dir.isDirectory() || !dir.canRead()) {
                throw new IOException(new StringBuilder(512).append("Log path ")
                        .append(dir.getAbsolutePath())
                        .append(" is not a readable directory").toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/disk/MsgFileStore.java`
#### Snippet
```java
        // append message, put in data file first, then index file.
        if (this.closed.get()) {
            throw new IllegalStateException(new StringBuilder(512)
                    .append("Closed MessageStore for storeKey ")
                    .append(this.storeKey).toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/disk/MsgFileStore.java`
#### Snippet
```java
            final Segment next = segments.get(i + 1);
            if (curr.getStart() + curr.getCachedSize() != next.getStart()) {
                throw new IllegalStateException(new StringBuilder(512)
                        .append("The following ").append(segTypeStr)
                        .append(" segments don't validate: ")
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/disk/MsgFileStore.java`
#### Snippet
```java
                if (file.isFile() && file.toString().endsWith(fileSuffix)) {
                    if (!file.canRead()) {
                        throw new IOException(new StringBuilder(512)
                                .append("Could not read ").append(segTypeStr)
                                .append(" file ").append(file).toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/heartbeat/HeartbeatManager.java`
#### Snippet
```java
                        Thread.sleep(1000 * 1);
                    } catch (Throwable t) {
                        logger.error(new StringBuilder(256)
                                .append(businessType).append(" heartbeat scan error!").toString(), t);
                    }
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/heartbeat/HeartbeatManager.java`
#### Snippet
```java
        TimeoutInfo timeoutInfo = consumerRegMap.get(nodeId);
        if (timeoutInfo == null) {
            throw new HeartbeatException(new StringBuilder(512)
                    .append("Invalid node id:").append(nodeId)
                    .append(", you have to append node first!").toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/heartbeat/HeartbeatManager.java`
#### Snippet
```java
        TimeoutInfo timeoutInfo = producerRegMap.get(nodeId);
        if (timeoutInfo == null) {
            throw new HeartbeatException(new StringBuilder(512)
                    .append("Invalid node id:").append(nodeId)
                    .append(", you have to append node first!").toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/fileconfig/ZKConfig.java`
#### Snippet
```java
    @Override
    public String toString() {
        return new StringBuilder(512)
                .append("\"ZKConfig\":{\"zkServerAddr\":\"").append(zkServerAddr)
                .append("\",\"zkNodeRoot\":\"").append(zkNodeRoot)
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/fileconfig/ZKMetaConfig.java`
#### Snippet
```java
    @Override
    public String toString() {
        return new StringBuilder(512)
                .append("\"ZKMetaConfig\":{\"zkServerAddr\":\"").append(getZkServerAddr())
                .append("\",\"zkNodeRoot\":\"").append(getZkNodeRoot())
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/fileconfig/BdbMetaConfig.java`
#### Snippet
```java
    @Override
    public String toString() {
        return new StringBuilder(512)
                .append("\"BdbMetaConfig\":{\"repGroupName\":").append(repGroupName)
                .append("\",\"repNodeName\":\"").append(repNodeName)
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/fileconfig/ADConfig.java`
#### Snippet
```java

    public String toString() {
        return new StringBuilder(512)
                .append("\"ADConfig\":{\"auditEnable\":").append(auditEnable)
                .append(",\"auditProxyAddr\":\"").append(auditProxyAddrSet)
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/fileconfig/PrometheusConfig.java`
#### Snippet
```java

    public String toString() {
        return new StringBuilder(512)
                .append("\"PrometheusConfig\":{\"promEnable\":").append(promEnable)
                .append(",\"promClusterName\":\"").append(promClusterName)
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/fileconfig/AbstractFileConfig.java`
#### Snippet
```java
        if (!configFields.contains(checkItem)) {
            String best = this.findBestMatchField(configFields, checkItem);
            throw new IllegalArgumentException(new StringBuilder(256)
                    .append("Config item ").append(checkItem)
                    .append(" is Required! in section ").append(section)
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/fileconfig/AbstractFileConfig.java`
#### Snippet
```java
        final String value = section.get(key);
        if (TStringUtils.isBlank(value)) {
            throw new IllegalArgumentException(new StringBuilder(256)
                    .append("Blank value for ").append(key).toString());
        } else {
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/fileconfig/AbstractFileConfig.java`
#### Snippet
```java
                return Integer.parseInt(value);
            } catch (NumberFormatException e) {
                throw new IllegalArgumentException(new StringBuilder(256)
                        .append("Translate key(").append(key).append(")'s value ")
                        .append(value).append(" to int failure!").toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/fileconfig/AbstractFileConfig.java`
#### Snippet
```java
        final Profile.Section zkeeperSect = iniConf.get(SECT_TOKEN_ZKEEPER);
        if (zkeeperSect == null) {
            throw new IllegalArgumentException(new StringBuilder(256)
                    .append(SECT_TOKEN_ZKEEPER).append(" configure section is required!").toString());
        }
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/fileconfig/AbstractFileConfig.java`
#### Snippet
```java
        Set<String> configKeySet = zkeeperSect.keySet();
        if (configKeySet.isEmpty()) {
            throw new IllegalArgumentException(new StringBuilder(256)
                    .append("Empty configure item in ").append(SECT_TOKEN_ZKEEPER)
                    .append(" section!").toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/fileconfig/AbstractFileConfig.java`
#### Snippet
```java
        final String value = section.get(key);
        if (TStringUtils.isBlank(value)) {
            throw new IllegalArgumentException(new StringBuilder(256)
                    .append("Blank value for ").append(key).toString());
        } else {
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/fileconfig/AbstractFileConfig.java`
#### Snippet
```java
                return Long.parseLong(value);
            } catch (NumberFormatException e) {
                throw new IllegalArgumentException(new StringBuilder(256)
                        .append("Translate key(").append(key).append(")'s value ")
                        .append(value).append(" to long failure!").toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/fileconfig/AbstractFileConfig.java`
#### Snippet
```java
            final File file = new File(configPath);
            if (!file.exists()) {
                throw new StartupException(new StringBuilder(256).append("File ")
                        .append(configPath).append(" is not exists").toString());
            }
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/fileconfig/AbstractFileConfig.java`
#### Snippet
```java
            this.loadConfigAttributes(iniConf);
        } catch (final IOException e) {
            throw new StartupException(new StringBuilder(256)
                    .append("Parse configuration failed,path=")
                    .append(configPath).toString(), e);
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/fileconfig/AbstractFileConfig.java`
#### Snippet
```java
    @Override
    public String toString() {
        return new StringBuilder(512)
                .append("\"loadFileAttr\":{\"basePath\":\"").append(basePath)
                .append("\",\"configPath\":\"").append(configPath)
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/fileconfig/AbstractFileConfig.java`
#### Snippet
```java
        final String value = section.get(key);
        if (TStringUtils.isBlank(value)) {
            throw new IllegalArgumentException(new StringBuilder(256)
                    .append("Blank value for ").append(key).toString());
        } else {
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/fileconfig/AbstractFileConfig.java`
#### Snippet
```java
                return Boolean.parseBoolean(value);
            } catch (NumberFormatException e) {
                throw new IllegalArgumentException(new StringBuilder(256)
                        .append("Translate key(").append(key).append(")'s value ")
                        .append(value).append(" to boolean failure!").toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/MasterConfig.java`
#### Snippet
```java
        Set<String> configKeySet = repSect.keySet();
        if (configKeySet.isEmpty()) {
            throw new IllegalArgumentException(new StringBuilder(256)
                    .append("Empty configure item in ").append(SECT_TOKEN_META_BDB)
                    .append(" section!").toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/MasterConfig.java`
#### Snippet
```java
        if (this.port == this.webPort
                || (tlsConfig.isTlsEnable() && (this.tlsConfig.getTlsPort() == this.webPort))) {
            throw new IllegalArgumentException(new StringBuilder(512)
                    .append("Illegal field value configuration, the value of ")
                    .append("port or tlsPort cannot be the same as the value of webPort!")
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/MasterConfig.java`
#### Snippet
```java
                    || (tlsConfig.isTlsEnable()
                            && (this.tlsConfig.getTlsPort() == promHttpPort)))) {
                throw new IllegalArgumentException(new StringBuilder(512)
                        .append("Illegal port value configuration, the value of ")
                        .append("port or webPort or tlsPort cannot be the same as the value of promHttpPort!")
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/MasterConfig.java`
#### Snippet
```java
            if (this.port == bdbMetaConfig.getRepNodePort() || (tlsConfig.isTlsEnable()
                    && (this.tlsConfig.getTlsPort() == bdbMetaConfig.getRepNodePort()))) {
                throw new IllegalArgumentException(new StringBuilder(512)
                        .append("Illegal field value configuration, the value of ")
                        .append("port or tlsPort cannot be the same as the value of repNodePort!")
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/MasterConfig.java`
#### Snippet
```java
            }
            if (this.webPort == bdbMetaConfig.getRepNodePort()) {
                throw new IllegalArgumentException(new StringBuilder(512)
                        .append("Illegal field value configuration, the value of ")
                        .append("webPort cannot be the same as the value of repNodePort!")
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/MasterConfig.java`
#### Snippet
```java
            if (this.promConfig.isPromEnable()
                    && this.promConfig.getPromHttpPort() == bdbMetaConfig.getRepNodePort()) {
                throw new IllegalArgumentException(new StringBuilder(512)
                        .append("Illegal field value configuration, the value of ")
                        .append("promHttpPort cannot be the same as the value of repNodePort!")
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/MasterConfig.java`
#### Snippet
```java
        final Profile.Section masterConf = iniConf.get(SECT_TOKEN_MASTER);
        if (masterConf == null) {
            throw new IllegalArgumentException(new StringBuilder(256)
                    .append(SECT_TOKEN_MASTER).append(" configure section is required!").toString());
        }
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/MasterConfig.java`
#### Snippet
```java
        Set<String> configKeySet = masterConf.keySet();
        if (configKeySet.isEmpty()) { /* Should have a least one config item */
            throw new IllegalArgumentException(new StringBuilder(256)
                    .append("Empty configure item in ").append(SECT_TOKEN_MASTER)
                    .append(" section!").toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/MasterConfig.java`
#### Snippet
```java
                this.hostName = AddressUtils.getIPV4LocalAddress();
            } catch (Throwable e) {
                throw new IllegalArgumentException(new StringBuilder(256)
                        .append("Get default master hostName failure : ")
                        .append(e.getMessage()).toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/MasterConfig.java`
#### Snippet
```java
        // web resource path
        if (TStringUtils.isBlank(masterConf.get("webResourcePath"))) {
            throw new IllegalArgumentException(new StringBuilder(256)
                    .append("webResourcePath is null or Blank in ").append(SECT_TOKEN_MASTER)
                    .append(" section!").toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/MasterConfig.java`
#### Snippet
```java
        if (this.needBrokerVisitAuth) {
            if (TStringUtils.isBlank(masterConf.get("visitName"))) {
                throw new IllegalArgumentException(new StringBuilder(256)
                        .append("visitName is null or Blank in ").append(SECT_TOKEN_MASTER)
                        .append(" section!").toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/MasterConfig.java`
#### Snippet
```java
            }
            if (TStringUtils.isBlank(masterConf.get("visitPassword"))) {
                throw new IllegalArgumentException(new StringBuilder(256)
                        .append("visitPassword is null or Blank in ").append(SECT_TOKEN_MASTER)
                        .append(" section!").toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/MasterConfig.java`
#### Snippet
```java
        Set<String> configKeySet = repSect.keySet();
        if (configKeySet.isEmpty()) {
            throw new IllegalArgumentException(new StringBuilder(256)
                    .append("Empty configure item in ").append(SECT_TOKEN_REPLICATION)
                    .append(" section!").toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/MasterConfig.java`
#### Snippet
```java
        final Profile.Section zkeeperSect = iniConf.get(SECT_TOKEN_META_ZK);
        if (zkeeperSect == null) {
            throw new IllegalArgumentException(new StringBuilder(256)
                    .append(SECT_TOKEN_META_ZK).append(" configure section is required!").toString());
        }
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/MasterConfig.java`
#### Snippet
```java
        Set<String> configKeySet = zkeeperSect.keySet();
        if (configKeySet.isEmpty()) {
            throw new IllegalArgumentException(new StringBuilder(256)
                    .append("Empty configure item in ").append(SECT_TOKEN_META_ZK)
                    .append(" section!").toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/MasterConfig.java`
#### Snippet
```java
        if (iniConf.get(SECT_TOKEN_META_BDB) != null
                && iniConf.get(SECT_TOKEN_META_ZK) != null) {
            throw new IllegalArgumentException(new StringBuilder(256)
                    .append("Cannot configure both ").append(SECT_TOKEN_META_BDB).append(" and ")
                    .append(SECT_TOKEN_META_ZK).append(" meta-data sections in the same time")
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/MasterConfig.java`
#### Snippet
```java
            return;
        }
        throw new IllegalArgumentException(new StringBuilder(256)
                .append("Missing necessary meta-data section, please select ")
                .append(SECT_TOKEN_META_ZK).append(" or ").append(SECT_TOKEN_META_BDB)
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/MasterConfig.java`
#### Snippet
```java
        Set<String> configKeySet = bdbSect.keySet();
        if (configKeySet.isEmpty()) {
            throw new IllegalArgumentException(new StringBuilder(256)
                    .append("Empty configure item in ").append(SECT_TOKEN_BDB)
                    .append(" section!").toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/utils/WebParameterUtils.java`
#### Snippet
```java
        if (TStringUtils.isBlank(paramValue)) {
            if (required) {
                result.setFailResult(new StringBuilder(512)
                        .append("Parameter ").append(fieldDef.name)
                        .append(" is missing or value is blank!").toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/utils/WebParameterUtils.java`
#### Snippet
```java
                    TBaseConstants.META_DEFAULT_CHARSET_NAME);
        } catch (UnsupportedEncodingException e) {
            result.setFailResult(new StringBuilder(512)
                    .append("Parameter ").append(fieldDef.name)
                    .append(" decode error, exception is ")
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/utils/WebParameterUtils.java`
#### Snippet
```java
        if (TStringUtils.isBlank(paramValue)) {
            if (required) {
                result.setFailResult(new StringBuilder(512).append("Parameter ")
                        .append(fieldDef.name).append("'s value is blank!").toString());
            } else {
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/utils/WebParameterUtils.java`
#### Snippet
```java
        if (fieldDef.valMaxLen != TBaseConstants.META_VALUE_UNDEFINED) {
            if (paramValue.length() > fieldDef.valMaxLen) {
                result.setFailResult(new StringBuilder(512)
                        .append("Parameter ").append(fieldDef.name)
                        .append("'s length over max allowed length (")
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/utils/WebParameterUtils.java`
#### Snippet
```java
            result.setSuccResult(arrayValue);
        } catch (Throwable e) {
            result.setFailResult(new StringBuilder(512)
                    .append("Parameter ").append(fieldDef.name)
                    .append(" value parse failure, error is ")
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/utils/WebParameterUtils.java`
#### Snippet
```java
        if (TStringUtils.isBlank(paramValue)) {
            if (required) {
                result.setFailResult(new StringBuilder(512)
                        .append("Parameter ").append(fieldDef.name)
                        .append(" is missing or value is blank!").toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/utils/WebParameterUtils.java`
#### Snippet
```java
                    TBaseConstants.META_DEFAULT_CHARSET_NAME);
        } catch (UnsupportedEncodingException e) {
            result.setFailResult(new StringBuilder(512)
                    .append("Parameter ").append(fieldDef.name)
                    .append(" decode error, exception is ")
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/utils/WebParameterUtils.java`
#### Snippet
```java
        if (TStringUtils.isBlank(paramValue)) {
            if (required) {
                result.setFailResult(new StringBuilder(512).append("Parameter ")
                        .append(fieldDef.name).append("'s value is blank!").toString());
            } else {
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/utils/WebParameterUtils.java`
#### Snippet
```java
        if (fieldDef.valMaxLen != TBaseConstants.META_VALUE_UNDEFINED) {
            if (paramValue.length() > fieldDef.valMaxLen) {
                result.setFailResult(new StringBuilder(512)
                        .append("Parameter ").append(fieldDef.name)
                        .append("'s length over max allowed length (")
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/utils/WebParameterUtils.java`
#### Snippet
```java
            result.setSuccResult(manOffsets);
        } catch (Throwable e) {
            result.setFailResult(new StringBuilder(512)
                    .append("Parameter ").append(fieldDef.name)
                    .append(" value parse failure, error is ")
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/utils/WebParameterUtils.java`
#### Snippet
```java
        if (paramValue == null) {
            if (required) {
                throw new Exception(new StringBuilder(512).append("Required ")
                        .append(paramName).append(" parameter").toString());
            }
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/utils/WebParameterUtils.java`
#### Snippet
```java
            if (TStringUtils.isBlank(temParamValue)) {
                if (required) {
                    throw new Exception(new StringBuilder(512)
                            .append("Null or blank value of ").append(paramName)
                            .append(" parameter").toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/utils/WebParameterUtils.java`
#### Snippet
```java
                    TBaseConstants.META_DEFAULT_CHARSET_NAME);
        } catch (UnsupportedEncodingException e) {
            throw new Exception(new StringBuilder(512).append("Decode ")
                    .append(paramName).append("error, exception is ")
                    .append(e.toString()).toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/utils/WebParameterUtils.java`
#### Snippet
```java
        if (TStringUtils.isBlank(decTmpParamVal)) {
            if (required) {
                throw new Exception(new StringBuilder(512)
                        .append("Blank value of ").append(paramName)
                        .append(" parameter").toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/utils/WebParameterUtils.java`
#### Snippet
```java
        if (paramMaxLen != TBaseConstants.META_VALUE_UNDEFINED) {
            if (decTmpParamVal.length() > paramMaxLen) {
                throw new Exception(new StringBuilder(512)
                        .append("the max length of ").append(paramName)
                        .append(" parameter is ").append(paramMaxLen)
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/simplemvc/RequestContext.java`
#### Snippet
```java
        this.resp = resp;
        this.requestPath =
                new StringBuilder(512).append(req.getServletPath())
                        .append(req.getPathInfo() != null ? req.getPathInfo() : "").toString();
        if ("/".equals(this.requestPath)) {
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/simplemvc/RequestContext.java`
#### Snippet
```java
        int lastSlashIndex = requestTarget.lastIndexOf("/");
        if (lastSlashIndex >= 0) {
            requestTarget = new StringBuilder(512)
                    .append(requestTarget.substring(0, lastSlashIndex)).append("/")
                    .append(TStringUtils.toCamelCase(requestTarget.substring(lastSlashIndex + 1))).toString();
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/simplemvc/RequestDispatcher.java`
#### Snippet
```java
     */
    public String getActionKey(String type, String target) {
        return new StringBuilder(256).append(type)
                .append("/").append(target).toString();
    }
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/simplemvc/RequestDispatcher.java`
#### Snippet
```java
            } else {
                if (!this.templates.containsKey(templatePath)) {
                    throw new TemplateNotFoundException(new StringBuilder(256)
                            .append("Invalid ").append(type)
                            .append(" template path:").append(templatePath).toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/simplemvc/RequestDispatcher.java`
#### Snippet
```java
            }
            if (realTemplatePath == null) {
                throw new TemplateNotFoundException(new StringBuilder(256).append("Invalid ")
                        .append(type).append(" template path:").append(templatePath).toString());
            }
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/simplemvc/RequestDispatcher.java`
#### Snippet
```java
     */
    public String getTemplateName(String type, String target) {
        return new StringBuilder(256).append(type)
                .append("/").append(target).append(".vm").toString();
    }
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/simplemvc/WebApiServlet.java`
#### Snippet
```java
                servletContext.getResource(config.getResourcePath());
        if (resourcesURL == null) {
            throw new ServletException(new StringBuilder(256)
                    .append("Invalid resources path:")
                    .append(config.getResourcePath()).toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/simplemvc/WebApiServlet.java`
#### Snippet
```java
        URL templatesURL = servletContext.getResource(config.getTemplatePath());
        if (templatesURL == null) {
            throw new ServletException(new StringBuilder(256)
                    .append("Invalid templates path:")
                    .append(config.getTemplatePath()).toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/simplemvc/WebApiServlet.java`
#### Snippet
```java
                config.setVelocityConfigFilePath(velocityConfigFilePath.getPath());
            } else {
                logger.warn(new StringBuilder(256)
                        .append("Invalid velocity config file path:")
                        .append(config.getVelocityConfigFilePath()).toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/simplemvc/WebApiServlet.java`
#### Snippet
```java
                        config.getServletContext().getResource(this.configFilePath);
                if (configFileURL == null) {
                    throw new ServletException(new StringBuilder(256)
                            .append("can not found config file:")
                            .append(this.configFilePath).toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/simplemvc/conf/ConfigFileParser.java`
#### Snippet
```java

    private String errorMsg(String attr, String elementName) {
        return new StringBuilder(256).append("Attribute '")
                .append(attr).append("' is needed for ").append(elementName).toString();
    }
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/bdbstore/bdbentitys/BdbBlackGroupEntity.java`
#### Snippet
```java
            String attributes, String createUser,
            Date createDate) {
        this.recordKey = new StringBuilder(512).append(topicName)
                .append(TokenConstants.ATTR_SEP).append(consumerGroupName).toString();
        this.topicName = topicName;
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/bdbstore/bdbentitys/BdbBlackGroupEntity.java`
#### Snippet
```java
    public BdbBlackGroupEntity(String topicName, String consumerGroupName,
            String createUser, Date createDate) {
        this.recordKey = new StringBuilder(512).append(topicName)
                .append(TokenConstants.ATTR_SEP).append(consumerGroupName).toString();
        this.topicName = topicName;
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/TMaster.java`
#### Snippet
```java
     */
    private String getConsumerKey(String group, String consumerId) {
        return new StringBuilder(512).append(consumerId)
                .append("@").append(group).toString();
    }
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/TMaster.java`
#### Snippet
```java
        final File dir = new File(bdbEnvPath);
        if (!dir.exists() && !dir.mkdirs()) {
            throw new Exception(new StringBuilder(256)
                    .append("Could not make bdb data directory ")
                    .append(dir.getAbsolutePath()).toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/TMaster.java`
#### Snippet
```java
        }
        if (!dir.isDirectory() || !dir.canRead()) {
            throw new Exception(new StringBuilder(256)
                    .append("bdb data path ")
                    .append(dir.getAbsolutePath())
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/TMaster.java`
#### Snippet
```java
                                        isStartBalance, subGroups, strBuffer);
                            } catch (Throwable e) {
                                logger.warn(new StringBuilder(1024)
                                        .append("[Svr-Balance Status] Error during reset-reb,")
                                        .append("the groups that may be affected are ")
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/TMaster.java`
#### Snippet
```java
                                        isStartBalance, subGroups, strBuffer);
                            } catch (Throwable e) {
                                logger.warn(new StringBuilder(1024)
                                        .append("[Svr-Balance Status] Error during normal-reb,")
                                        .append("the groups that may be affected are ")
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/TMaster.java`
#### Snippet
```java
                    @Override
                    public void onTimeout(final String nodeId, TimeoutInfo nodeInfo) {
                        logger.info(new StringBuilder(512).append("[Producer Timeout] ")
                                .append(nodeId).toString());
                        new ReleaseProducer().run(nodeId, true);
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/TMaster.java`
#### Snippet
```java
                    @Override
                    public void onTimeout(String nodeId, TimeoutInfo nodeInfo) {
                        logger.info(new StringBuilder(512).append("[Consumer Timeout] ")
                                .append(nodeId).toString());
                        new ReleaseConsumer().run(nodeId, true);
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/bdbstore/bdbentitys/BdbGroupFilterCondEntity.java`
#### Snippet
```java
            String attributes, String modifyUser, Date modifyDate) {
        this.recordKey =
                new StringBuilder(512)
                        .append(topicName)
                        .append(TokenConstants.ATTR_SEP)
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/bdbstore/bdbentitys/BdbGroupFilterCondEntity.java`
#### Snippet
```java
            String modifyUser, Date modifyDate) {
        this.recordKey =
                new StringBuilder(512)
                        .append(topicName)
                        .append(TokenConstants.ATTR_SEP)
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/bdbstore/bdbentitys/BdbConsumerGroupEntity.java`
#### Snippet
```java
    public BdbConsumerGroupEntity(String topicName, String consumerGroupName,
            String createUser, Date createDate) {
        this.recordKey = new StringBuilder(512).append(topicName)
                .append(TokenConstants.ATTR_SEP).append(consumerGroupName).toString();
        this.topicName = topicName;
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/bdbstore/bdbentitys/BdbBrokerConfEntity.java`
#### Snippet
```java
     */
    public String getBrokerDefaultConfInfo() {
        return new StringBuilder(512).append(this.getDftNumPartitions())
                .append(TokenConstants.ATTR_SEP).append(this.isAcceptPublish())
                .append(TokenConstants.ATTR_SEP).append(this.isAcceptSubscribe())
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/metamanage/DefaultMetaDataService.java`
#### Snippet
```java
                        }
                    } catch (Throwable e) {
                        logger.error(new StringBuilder(512)
                                .append("BDBGroupStatus Check exception, wait ")
                                .append(bdbMetaConfig.getRepStatusCheckTimeoutMs())
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/metamanage/metastore/impl/bdbimpl/BdbMetaConfigMapperImpl.java`
#### Snippet
```java
                // network connectivity issues, etc. Wait and retry to allow the problem
                // to be resolved.
                logger.error(new StringBuilder(TBaseConstants.BUILDER_DEFAULT_SIZE)
                        .append("[BDB Impl] master could not be established. ")
                        .append("Exception message:").append(unknownMaster.getMessage())
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/metamanage/metastore/impl/bdbimpl/BdbMetaConfigMapperImpl.java`
#### Snippet
```java
                continue;
            } catch (InsufficientLogException insufficientLogEx) {
                logger.error(new StringBuilder(TBaseConstants.BUILDER_DEFAULT_SIZE)
                        .append("[BDB Impl] [Restoring data please wait....] ")
                        .append("Obtains logger files for a Replica from other members of ")
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/metamanage/metastore/impl/bdbimpl/BdbMetaConfigMapperImpl.java`
#### Snippet
```java
            if (!isPrimaryNodeActive()) {
                if ((replicas4Transfer != null) && (!replicas4Transfer.isEmpty())) {
                    logger.info(new StringBuilder(TBaseConstants.BUILDER_DEFAULT_SIZE)
                            .append("[BDB Impl] start transferMaster to replicas: ")
                            .append(replicas4Transfer).toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/metamanage/metastore/impl/bdbimpl/BdbMetaConfigMapperImpl.java`
#### Snippet
```java
        public void doWork(final StateChangeEvent stateChangeEvent) {

            final String currentNode = new StringBuilder(TBaseConstants.BUILDER_DEFAULT_SIZE)
                    .append("GroupName:").append(repConfig.getGroupName())
                    .append(",nodeName:").append(repConfig.getNodeName())
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/metamanage/metastore/impl/bdbimpl/BdbMetaConfigMapperImpl.java`
#### Snippet
```java
        public void stateChange(StateChangeEvent stateChangeEvent) throws RuntimeException {
            if (repConfig != null) {
                logger.warn(new StringBuilder(TBaseConstants.BUILDER_DEFAULT_SIZE)
                        .append("[BDB Impl][").append(repConfig.getGroupName())
                        .append("Receive a group status changed event]... stateChangeEventTime: ")
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/nodemanage/nodebroker/BrokerAbnHolder.java`
#### Snippet
```java
        }
        if (!brokerFbdInfos.isEmpty()) {
            logger.info(new StringBuilder(512)
                    .append("[Broker AutoForbidden] remove forbidden brokers by reason ")
                    .append(reason).append(", release list is ").append(brokerFbdInfos.toString()).toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/nodemanage/nodeconsumer/ConsumerEventManager.java`
#### Snippet
```java
            }
        } else {
            logger.warn(new StringBuilder(512)
                    .append("No group by consumer ")
                    .append(consumerId).toString());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/nodemanage/nodebroker/DefBrokerRunManager.java`
#### Snippet
```java
                    @Override
                    public void onTimeout(final String nodeId, TimeoutInfo nodeInfo) throws Exception {
                        logger.info(new StringBuilder(512).append("[Broker Timeout] ")
                                .append(nodeId).toString());
                        releaseBrokerRunInfo(Integer.parseInt(nodeId),
```

### StringBufferReplaceableByString
`StringBuilder result` can be replaced with 'String'
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sink/ck/ClickHouseSqlBuilder.java`
#### Snippet
```java
    private static String buildCreateColumnsSql(List<ClickHouseFieldInfo> columns) {
        List<String> columnList = getColumnsInfo(columns);
        StringBuilder result = new StringBuilder().append(" (")
                .append(StringUtils.join(columnList, ",")).append(") ");
        return result.toString();
```

### StringBufferReplaceableByString
`StringBuilder sql` can be replaced with 'String'
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sink/ck/ClickHouseSqlBuilder.java`
#### Snippet
```java
        List<String> resultList = new ArrayList<>();
        for (String columnInfo : columnInfoList) {
            StringBuilder sql = new StringBuilder().append("ALTER TABLE ")
                    .append(dbTableName).append(" ADD COLUMN ").append(columnInfo);
            resultList.add(sql.toString());
```

### StringBufferReplaceableByString
`StringBuilder mapping` can be replaced with 'String'
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sink/es/ElasticsearchApi.java`
#### Snippet
```java
        CreateIndexRequest createIndexRequest = new CreateIndexRequest(indexName);
        List<String> fieldList = getMappingInfo(fieldInfos);
        StringBuilder mapping = new StringBuilder().append("{\n      \"properties\" : {\n")
                .append(StringUtils.join(fieldList, ",\n")).append("\n      }\n}");
        createIndexRequest.mapping(mapping.toString(), XContentType.JSON);
```

### StringBufferReplaceableByString
`StringBuilder createSql` can be replaced with 'String'
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sink/oracle/OracleSqlBuilder.java`
#### Snippet
```java
     */
    public static List<String> buildCreateTableSql(final OracleTableInfo table) {
        final StringBuilder createSql = new StringBuilder()
                .append("CREATE TABLE ").append(table.getUserName())
                .append(".\"")
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sink/greenplum/GreenplumSqlBuilder.java`
#### Snippet
```java
     */
    public static String getCheckSchema(final String schemaName) {
        return new StringBuilder()
                .append("SELECT COUNT(1) FROM INFORMATION_SCHEMA.SCHEMATA ")
                .append(" WHERE SCHEMA_NAME = \'")
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sink/greenplum/GreenplumSqlBuilder.java`
#### Snippet
```java
     */
    public static String buildCreateSchema(final String schemaName, final String user) {
        return new StringBuilder()
                .append(" CREATE SCHEMA \"")
                .append(schemaName)
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sink/greenplum/GreenplumSqlBuilder.java`
#### Snippet
```java
     */
    private static String getTableComment(final GreenplumTableInfo tableInfo) {
        return new StringBuilder()
                .append("COMMENT ON TABLE \"")
                .append(tableInfo.getSchemaName())
```

### StringBufferReplaceableByString
`StringBuilder commSql` can be replaced with 'String'
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sink/greenplum/GreenplumSqlBuilder.java`
#### Snippet
```java
        for (GreenplumColumnInfo columnInfo : columns) {
            if (StringUtils.isNotBlank(columnInfo.getComment())) {
                StringBuilder commSql = new StringBuilder();
                commSql.append("COMMENT ON COLUMN \"")
                        .append(schemaName)
```

### StringBufferReplaceableByString
`StringBuilder createSql` can be replaced with 'String'
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sink/greenplum/GreenplumSqlBuilder.java`
#### Snippet
```java
    public static List<String> buildCreateTableSql(final GreenplumTableInfo table) {
        final List<String> sqls = Lists.newArrayList();
        final StringBuilder createSql = new StringBuilder()
                .append("CREATE TABLE ").append(table.getSchemaName())
                .append(".\"")
```

### StringBufferReplaceableByString
`StringBuilder sql` can be replaced with 'String'
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sink/sqlserver/SQLServerSqlBuilder.java`
#### Snippet
```java
    public static List<String> buildCreateTableSql(SQLServerTableInfo table) {
        final List<String> sqls = Lists.newArrayList();
        final StringBuilder sql = new StringBuilder()
                .append("CREATE TABLE ").append(table.getSchemaName())
                .append(".")
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sink/sqlserver/SQLServerSqlBuilder.java`
#### Snippet
```java
            final String columnName,
            String comment) {
        return new StringBuilder()
                .append("EXEC sys.sp_addextendedproperty @name=N'MS_Description',")
                .append(" @value=N'")
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sink/sqlserver/SQLServerSqlBuilder.java`
#### Snippet
```java
     */
    public static String buildCreateSchemaSql(String schemaName) {
        return new StringBuilder()
                .append("CREATE SCHEMA \"")
                .append(schemaName)
```

### StringBufferReplaceableByString
`StringBuilder createSql` can be replaced with 'String'
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sink/postgresql/PostgreSQLSqlBuilder.java`
#### Snippet
```java
    public static List<String> buildCreateTableSql(final PostgreSQLTableInfo table) {
        final List<String> sqls = Lists.newArrayList();
        final StringBuilder createSql = new StringBuilder()
                .append("CREATE TABLE ").append(table.getSchemaName())
                .append(".\"")
```

### StringBufferReplaceableByString
`StringBuilder commSql` can be replaced with 'String'
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sink/postgresql/PostgreSQLSqlBuilder.java`
#### Snippet
```java
        for (PostgreSQLColumnInfo columnInfo : columns) {
            if (StringUtils.isNotBlank(columnInfo.getComment())) {
                StringBuilder commSql = new StringBuilder();
                commSql.append("COMMENT ON COLUMN \"")
                        .append(schemaName)
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sink/postgresql/PostgreSQLSqlBuilder.java`
#### Snippet
```java
     */
    public static String buildCreateSchema(final String schemaName, final String user) {
        return new StringBuilder()
                .append(" CREATE SCHEMA \"")
                .append(schemaName)
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sink/postgresql/PostgreSQLSqlBuilder.java`
#### Snippet
```java
     */
    private static String getTableComment(final PostgreSQLTableInfo tableInfo) {
        return new StringBuilder()
                .append("COMMENT ON TABLE \"")
                .append(tableInfo.getSchemaName())
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sink/postgresql/PostgreSQLSqlBuilder.java`
#### Snippet
```java
     */
    public static String getCheckSchema(final String schemaName) {
        return new StringBuilder()
                .append("SELECT COUNT(1) FROM INFORMATION_SCHEMA.SCHEMATA ")
                .append(" WHERE SCHEMA_NAME = \'")
```

### StringBufferReplaceableByString
`StringBuilder builder` can be replaced with 'String'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/http/MessageFilter.java`
#### Snippet
```java
    private void returnRspPackage(HttpServletResponse resp, String charEncoding,
            int errCode, String errMsg) throws IOException {
        StringBuilder builder =
                new StringBuilder().append("{\"code\":\"").append(errCode)
                        .append("\",\"msg\":\"").append(errMsg).append("\"}");
```

### StringBufferReplaceableByString
`StringBuffer` can be replaced with 'String'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/tcp/InlongTcpSource.java`
#### Snippet
```java
     */
    public ChannelInitializer getChannelInitializerFactory() {
        LOG.info(new StringBuffer("load msgFactory=").append(msgFactoryName)
                .append(" and serviceDecoderName=").append(serviceDecoderName).toString());

```

### StringBufferReplaceableByString
`StringBuffer` can be replaced with 'String'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/BaseSource.java`
#### Snippet
```java
     */
    public ChannelInitializer getChannelInitializerFactory() {
        logger.info(new StringBuffer("load msgFactory=").append(msgFactoryName)
                .append(" and serviceDecoderName=").append(serviceDecoderName).toString());
        ChannelInitializer fac = null;
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/DefaultServiceDecoder.java`
#### Snippet
```java
        String time = String.valueOf(dataTime);
        commonAttrMap.put(AttributeConstants.SEQUENCE_ID,
                new StringBuilder(256).append(strRemoteIP)
                        .append("#").append(time).append("#").append(uniq).toString());
        // dt from sdk
```

### StringBufferReplaceableByString
`StringBuilder beanName` can be replaced with 'String'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/metrics/prometheus/PrometheusMetricListener.java`
#### Snippet
```java
        this.metricItem.clusterId = metricName;
        final MBeanServer mbs = ManagementFactory.getPlatformMBeanServer();
        StringBuilder beanName = new StringBuilder();
        beanName.append(JMX_DOMAIN).append(DOMAIN_SEPARATOR).append("type=DataProxyPrometheus");
        String strBeanName = beanName.toString();
```

### StringBufferReplaceableByString
`StringBuilder sidBuilder` can be replaced with 'String'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/SimpleMessageHandler.java`
#### Snippet
```java
                String sequenceId = commonAttrMap.get(AttributeConstants.SEQUENCE_ID);
                if (StringUtils.isNotEmpty(sequenceId)) {
                    StringBuilder sidBuilder =
                            new StringBuilder().append(topicEntry.getKey())
                                    .append(AttributeConstants.SEPARATOR).append(streamIdEntry.getKey())
```

### StringBufferReplaceableByString
`StringBuilder beanName` can be replaced with 'String'
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/metrics/prometheus/PrometheusMetricListener.java`
#### Snippet
```java
        this.metricItem.clusterId = metricName;
        final MBeanServer mbs = ManagementFactory.getPlatformMBeanServer();
        StringBuilder beanName = new StringBuilder();
        beanName.append(JMX_DOMAIN).append(DOMAIN_SEPARATOR).append("type=SortStandalonePrometheus");
        String strBeanName = beanName.toString();
```

## RuleId[id=UnnecessaryReturn]
### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `inlong-sdk/sort-sdk/src/main/java/org/apache/inlong/sdk/sort/impl/QueryConsumeConfigImpl.java`
#### Snippet
```java
            }
            if (handleSortTaskConfResult(getUrl, managerResponse, managerResponse.getCode())) {
                return;
            }
        } catch (Throwable e) {
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `inlong-sdk/sdk-common/src/main/java/org/apache/inlong/sdk/commons/admin/AdminTask.java`
#### Snippet
```java
        } catch (InterruptedException e) {
            LOG.info("Interrupted while trying to handle configuration event");
            return;
        } finally {
            // If interrupted while trying to lock, we don't own the lock, so must not attempt to unlock
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/config/ProxyConfigManager.java`
#### Snippet
```java
            }
        }
        return;
    }

```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/ClientMgr.java`
#### Snippet
```java
        try {
            if (loadvalue == (-1) || loadCycle == 0) {
                return;
            } else {
                for (Map.Entry<HostInfo, NettyClient> entry : clientMapData.entrySet()) {
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `inlong-sort/sort-formats/format-json/src/main/java/org/apache/inlong/sort/formats/json/canal/CanalJsonDeserializationSchema.java`
#### Snippet
```java
                // "data" field is null and "type" is "CREATE" which means
                // this is a DDL change event, and we should skip it.
                return;
            } else {
                if (!ignoreParseErrors) {
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `inlong-sort/sort-formats/format-json/src/main/java/org/apache/inlong/sort/formats/json/canal/CanalJsonEnhancedDeserializationSchema.java`
#### Snippet
```java
                // "data" field is null and "type" is "CREATE" which means
                // this is a DDL change event, and we should skip it.
                return;
            } else {
                if (!ignoreParseErrors) {
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/channel/MemoryChannel.java`
#### Snippet
```java
        LOGGER.debug("plugin read failed:{}", dimensions.toString());
        Thread.currentThread().interrupt();
        return;
    }

```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/channel/MemoryChannel.java`
#### Snippet
```java
        LOGGER.debug("plugin send failed:{}", dimensions.toString());
        Thread.currentThread().interrupt();
        return;
    }
}
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/sources/reader/PostgreSQLReader.java`
#### Snippet
```java
    @Override
    public void setWaitMillisecond(long millis) {
        return;
    }

```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/sources/reader/PostgreSQLReader.java`
#### Snippet
```java
    @Override
    public void setReadTimeout(long mill) {
        return;
    }

```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/sources/reader/file/KubernetesMetadataProvider.java`
#### Snippet
```java
            }
        });
        return;
    }
}
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `inlong-audit/audit-sdk/src/main/java/org/apache/inlong/audit/util/Config.java`
#### Snippet
```java
        } catch (SocketException ex) {
            localIP = "127.0.0.1";
            return;
        }
    }
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/node/Application.java`
#### Snippet
```java
        } catch (InterruptedException e) {
            logger.info("Interrupted while trying to handle configuration event");
            return;
        } finally {
            // If interrupted while trying to lock, we don't own the lock, so must not attempt to unlock
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/netty/NettyRpcServer.java`
#### Snippet
```java
                    ctx.channel().writeAndFlush(dataPack);
                }
                return;
            }
        }
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/zookeeper/ZKUtil.java`
#### Snippet
```java
                    CreateMode.PERSISTENT);
        } catch (KeeperException.NodeExistsException nee) {
            return;
        } catch (KeeperException.NoNodeException nne) {
            createWithParents(zkw, getParent(zkNode));
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/SortTask.java`
#### Snippet
```java
        } catch (InterruptedException e) {
            LOG.info("Interrupted while trying to handle configuration event");
            return;
        } finally {
            // If interrupted while trying to lock, we don't own the lock, so must not attempt to unlock
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/elasticsearch/EsChannelWorker.java`
#### Snippet
```java
            }
            tx.commit();
            return;
        } catch (Throwable t) {
            LOG.error("Process event failed!" + this.getName(), t);
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/elasticsearch/EsChannelWorker.java`
#### Snippet
```java
                LOG.error("Channel take transaction rollback exception:" + getName(), e);
            }
            return;
        } finally {
            tx.close();
```

## RuleId[id=SimplifiableConditionalExpression]
### SimplifiableConditionalExpression
`proxyCluster.getIsIntranet() == 1 ? true : false` can be simplified to 'proxyCluster.getIsIntranet() == 1'
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/config/ProxyConfigManager.java`
#### Snippet
```java
        boolean isIntranet = true;
        if (ObjectUtils.isNotEmpty(proxyCluster.getIsSwitch())) {
            isIntranet = proxyCluster.getIsIntranet() == 1 ? true : false;
        }
        int isSwitch = 0;
```

### SimplifiableConditionalExpression
`this.tableOptions == null
? false
: this.tableOptions.get(FlinkDynamicTableFactory.WRITE_COMPACT_ENABLE)` can be simplified to 'this.tableOptions!=null \&\& this.tableOptions.get(FlinkDynamicTableFactory.WRITE_COMPACT_ENABLE)'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/iceberg/src/main/java/org/apache/inlong/sort/iceberg/sink/multiple/IcebergSingleFileCommiter.java`
#### Snippet
```java
        this.table = tableLoader.loadTable();

        boolean writeCompactEnabelFromTO = this.tableOptions == null
                ? false
                : this.tableOptions.get(FlinkDynamicTableFactory.WRITE_COMPACT_ENABLE);
```

### SimplifiableConditionalExpression
`iterator != null ? true : false` can be simplified to 'iterator != null'
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/sources/reader/KafkaReader.java`
#### Snippet
```java
        ConsumerRecords<K, V> records = consumer.poll(Duration.ofMillis(fetchDataTimeout));
        iterator = records.iterator();
        return iterator != null ? true : false;
    }

```

## RuleId[id=ManualMinMaxCalculation]
### ManualMinMaxCalculation
Can be replaced with 'Math.min()' call
in `inlong-sort/sort-core/src/main/java/org/apache/inlong/sort/function/EncryptFunction.java`
#### Snippet
```java
            byte[] key = "000000000000000000000000".getBytes(ENCODING);
            byte[] temp = keyStr.getBytes(ENCODING);
            if (key.length > temp.length) {
                System.arraycopy(temp, 0, key, 0, temp.length);
            } else {
```

### ManualMinMaxCalculation
Can be replaced with 'Math.max()' call
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/sources/reader/KafkaReader.java`
#### Snippet
```java
            }
            // calculate sleep time
            long sleepTime = byteLimitSleepTime < recordLimitSleepTime
                    ? recordLimitSleepTime
                    : byteLimitSleepTime;
            if (sleepTime > 0) {
                LOGGER.info("sleep seconds:{}", sleepTime / 1000);
```

### ManualMinMaxCalculation
Can be replaced with 'Math.max()' call
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/config/TubeClientConfig.java`
#### Snippet
```java
        if (rpcReadTimeoutMs >= RpcConstants.CFG_RPC_READ_TIMEOUT_MAX_MS) {
            this.rpcReadTimeoutMs = RpcConstants.CFG_RPC_READ_TIMEOUT_MAX_MS;
        } else if (rpcReadTimeoutMs <= RpcConstants.CFG_RPC_READ_TIMEOUT_MIN_MS) {
            this.rpcReadTimeoutMs = RpcConstants.CFG_RPC_READ_TIMEOUT_MIN_MS;
        } else {
```

## RuleId[id=SillyAssignment]
### SillyAssignment
Variable 'jdbcConnectionPoolFactory' is assigned to itself
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/relational/connection/JdbcConnectionPools.java`
#### Snippet
```java
    public static synchronized JdbcConnectionPools getInstance(JdbcConnectionPoolFactory jdbcConnectionPoolFactory) {
        if (instance == null) {
            jdbcConnectionPoolFactory = jdbcConnectionPoolFactory;
            instance = new JdbcConnectionPools();
        }
```

## RuleId[id=NegativeIntConstantInLongContext]
### NegativeIntConstantInLongContext
Negative int hexadecimal constant in long context
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/utils/WritableComparator.java`
#### Snippet
```java
            i |= bytes[(start + 1 + idx)] & 0xFF;
        }
        return isNegative ? i ^ 0xFFFFFFFF : i;
    }

```

### NegativeIntConstantInLongContext
Negative int hexadecimal constant in long context
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/utils/WritableComparator.java`
#### Snippet
```java

    public static long readLong(byte[] bytes, int start) {
        return ((long) (readInt(bytes, start)) << 32) + ((long) (readInt(bytes, start + 4))) & 0xFFFFFFFF;
    }

```

## RuleId[id=JavadocLinkAsPlainText]
### JavadocLinkAsPlainText
Link specified as plain text
in `inlong-sort/sort-flink/base/src/main/java/org/apache/inlong/sort/base/Constants.java`
#### Snippet
```java
    /**
     * It is used for jdbc url filter for avoiding url attack
     * see also in https://su18.org/post/jdbc-connection-url-attack/
     */
    public static final String AUTO_DESERIALIZE = "autoDeserialize";
```

### JavadocLinkAsPlainText
Link specified as plain text
in `inlong-sort/sort-flink/base/src/main/java/org/apache/inlong/sort/base/util/JdbcUrlUtils.java`
#### Snippet
```java

    /**
     * see https://su18.org/post/jdbc-connection-url-attack/
     * @param url
     * @return url after filtering out the invalid property
```

### JavadocLinkAsPlainText
Link specified as plain text
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/util/RecordUtils.java`
#### Snippet
```java
    /**
     * According column's type, get it's logicalType
     * Refer: https://ververica.github.io/flink-cdc-connectors/master/content/connectors/oracle-cdc.html?from_wecom=1
     * @param column
     * @param struct
```

### JavadocLinkAsPlainText
Link specified as plain text
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/config/JdbcSourceConfigFactory.java`
#### Snippet
```java
     * The session time zone in database server, e.g. "America/Los_Angeles". It controls how the
     * TIMESTAMP type converted to STRING. See more
     * https://debezium.io/documentation/reference/1.5/connectors/mysql.html#mysql-temporal-types
     */
    public JdbcSourceConfigFactory serverTimeZone(String timeZone) {
```

### JavadocLinkAsPlainText
Link specified as plain text
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/jdbc/src/main/java/org/apache/inlong/sort/jdbc/dialect/SqlServerDialect.java`
#### Snippet
```java
    /**
     * SqlServer merge data.
     * https://docs.microsoft.com/en-us/sql/t-sql/statements/merge-transact-sql?view=sql-server-ver15
     */
    @Override
```

### JavadocLinkAsPlainText
Link specified as plain text
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kafka/src/main/java/org/apache/inlong/sort/kafka/FlinkKafkaConsumer.java`
#### Snippet
```java
 *
 * <p>Please refer to Kafka's documentation for the available configuration properties:
 * http://kafka.apache.org/documentation.html#newconsumerconfigs
 */
@PublicEvolving
```

### JavadocLinkAsPlainText
Link specified as plain text
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/iceberg/src/main/java/org/apache/inlong/sort/iceberg/sink/EqualityFieldKeySelector.java`
#### Snippet
```java
 * Create a {@link KeySelector} to shuffle by equality fields, to ensure same equality fields record
 * will be emitted to same writer in order.
 * `EqualityFieldKeySelector` is copied from https://github.com/apache/iceberg/blob/e2bb9ad7e792efca419fa7c4a1afde7c4c44fa01/flink/v1.13/flink/src/main/java/org/apache/iceberg/flink/sink/EqualityFieldKeySelector.java#L36
 */
class EqualityFieldKeySelector implements KeySelector<RowData, Integer> {
```

### JavadocLinkAsPlainText
Link specified as plain text
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/mysql/source/MySqlSourceBuilder.java`
#### Snippet
```java
     * The session time zone in database server, e.g. "America/Los_Angeles". It controls how the
     * TIMESTAMP type in MYSQL converted to STRING. See more
     * https://debezium.io/documentation/reference/1.5/connectors/mysql.html#mysql-temporal-types
     */
    public MySqlSourceBuilder<T> serverTimeZone(String timeZone) {
```

### JavadocLinkAsPlainText
Link specified as plain text
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/mysql/MySqlSource.java`
#### Snippet
```java
         * The session time zone in database server, e.g. "America/Los_Angeles". It controls how the
         * TIMESTAMP type in MYSQL converted to STRING. See more
         * https://debezium.io/documentation/reference/1.5/connectors/mysql.html#mysql-temporal-types
         */
        public Builder<T> serverTimeZone(String timeZone) {
```

### JavadocLinkAsPlainText
Link specified as plain text
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/mysql/source/config/MySqlSourceConfigFactory.java`
#### Snippet
```java
     * The session time zone in database server, e.g. "America/Los_Angeles". It controls how the
     * TIMESTAMP type in MYSQL converted to STRING. See more
     * https://debezium.io/documentation/reference/1.5/connectors/mysql.html#mysql-temporal-types
     */
    public MySqlSourceConfigFactory serverTimeZone(String timeZone) {
```

### JavadocLinkAsPlainText
Link specified as plain text
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/mysql/debezium/dispatcher/SignalEventDispatcher.java`
#### Snippet
```java
 *
 * <p>The watermark signal event is used to describe the start point and end point of a split scan.
 * The Watermark Signal Algorithm is inspired by https://arxiv.org/pdf/2010.12597v1.pdf.</p>
 */
public class SignalEventDispatcher {
```

### JavadocLinkAsPlainText
Link specified as plain text
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/mysql/debezium/task/MySqlSnapshotSplitReadTask.java`
#### Snippet
```java
     * Read JDBC return value and deal special type like time, timestamp.
     *
     * <p>Note https://issues.redhat.com/browse/DBZ-3238 has fixed this issue, please remove this
     * method once we bump Debezium version to 1.6</p>
     */
```

### JavadocLinkAsPlainText
Link specified as plain text
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/mysql/debezium/task/MySqlSnapshotSplitReadTask.java`
#### Snippet
```java
    /**
     * As MySQL connector/J implementation is broken for MySQL type "TIME" we have to use a
     * binary-ish workaround. https://issues.jboss.org/browse/DBZ-342
     */
    private Object readTimeField(ResultSet rs, int fieldNo) throws SQLException {
```

### JavadocLinkAsPlainText
Link specified as plain text
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/debezium/DebeziumSourceFunction.java`
#### Snippet
```java
 *
 * <p>Please refer to Debezium's documentation for the available configuration properties:
 * https://debezium.io/documentation/reference/1.5/development/engine.html#engine-properties</p>
 */
@PublicEvolving
```

### JavadocLinkAsPlainText
Link specified as plain text
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/oracle-cdc/src/main/java/org/apache/inlong/sort/cdc/oracle/source/OracleSourceBuilder.java`
#### Snippet
```java
     * The session time zone in database server, e.g. "America/Los_Angeles". It controls how the
     * TIMESTAMP type in Oracle converted to STRING. See more
     * https://debezium.io/documentation/reference/1.5/connectors/Oracle.html#Oracle-temporal-types
     */
    public OracleSourceBuilder<T> serverTimeZone(String timeZone) {
```

### JavadocLinkAsPlainText
Link specified as plain text
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mongodb-cdc/src/main/java/org/apache/inlong/sort/cdc/mongodb/MongoDBSource.java`
#### Snippet
```java
         * Ampersand (i.e. &) separated MongoDB connection options eg
         * replicaSet=test&connectTimeoutMS=300000
         * https://docs.mongodb.com/manual/reference/connection-string/#std-label-connections-connection-options
         */
        public Builder<T> connectionOptions(String connectionOptions) {
```

### JavadocLinkAsPlainText
Link specified as plain text
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mongodb-cdc/src/main/java/org/apache/inlong/sort/cdc/mongodb/MongoDBSource.java`
#### Snippet
```java
        /**
         * The properties of mongodb kafka connector.
         * https://docs.mongodb.com/kafka-connector/current/kafka-source
         */
        public DebeziumSourceFunction<T> build() {
```

### JavadocLinkAsPlainText
Link specified as plain text
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mongodb-cdc/src/main/java/org/apache/inlong/sort/cdc/mongodb/source/MongoDBSourceBuilder.java`
#### Snippet
```java
     * Ampersand (i.e. &) separated MongoDB connection options eg
     * replicaSet=test&connectTimeoutMS=300000
     * https://docs.mongodb.com/manual/reference/connection-string/#std-label-connections-connection-options
     */
    public MongoDBSourceBuilder<T> connectionOptions(String connectionOptions) {
```

### JavadocLinkAsPlainText
Link specified as plain text
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mongodb-cdc/src/main/java/org/apache/inlong/sort/cdc/mongodb/source/config/MongoDBSourceConfigFactory.java`
#### Snippet
```java
     * Ampersand (i.e. &) separated MongoDB connection options eg
     * replicaSet=test&connectTimeoutMS=300000
     * https://docs.mongodb.com/manual/reference/connection-string/#std-label-connections-connection-options
     */
    public MongoDBSourceConfigFactory connectionOptions(String connectionOptions) {
```

### JavadocLinkAsPlainText
Link specified as plain text
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/oracle-cdc/src/main/java/org/apache/inlong/sort/cdc/oracle/debezium/DebeziumSourceFunction.java`
#### Snippet
```java
 *
 * <p>Please refer to Debezium's documentation for the available configuration properties:
 * https://debezium.io/documentation/reference/1.5/development/engine.html#engine-properties</p>
 */
@PublicEvolving
```

### JavadocLinkAsPlainText
Link specified as plain text
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mongodb-cdc/src/main/java/org/apache/inlong/sort/cdc/mongodb/DebeziumSourceFunction.java`
#### Snippet
```java
 *
 * <p>Please refer to Debezium's documentation for the available configuration properties:
 * https://debezium.io/documentation/reference/1.5/development/engine.html#engine-properties
 */
@PublicEvolving
```

### JavadocLinkAsPlainText
Link specified as plain text
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/postgres-cdc/src/main/java/org/apache/inlong/sort/cdc/postgres/DebeziumSourceFunction.java`
#### Snippet
```java
 *
 * <p>Please refer to Debezium's documentation for the available configuration properties:
 * https://debezium.io/documentation/reference/1.5/development/engine.html#engine-properties</p>
 */
@PublicEvolving
```

### JavadocLinkAsPlainText
Link specified as plain text
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/elasticsearch-6/src/main/java/org/apache/inlong/sort/elasticsearch6/table/Elasticsearch6Configuration.java`
#### Snippet
```java
     *
     * <pre>
     *     connector.hosts = http://host_name:9092;http://host_name:9093
     * </pre>
     */
```

### JavadocLinkAsPlainText
Link specified as plain text
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/sqlserver-cdc/src/main/java/org/apache/inlong/sort/cdc/sqlserver/table/DebeziumSourceFunction.java`
#### Snippet
```java
 *
 * <p>Please refer to Debezium's documentation for the available configuration properties:
 * https://debezium.io/documentation/reference/1.5/development/engine.html#engine-properties
 */
@PublicEvolving
```

### JavadocLinkAsPlainText
Link specified as plain text
in `inlong-agent/agent-common/src/main/java/org/apache/inlong/agent/utils/HttpManager.java`
#### Snippet
```java
     * build base url for manager according to config
     *
     * example - http://127.0.0.1:8080/inlong/manager/openapi
     */
    public static String buildBaseUrl() {
```

### JavadocLinkAsPlainText
Link specified as plain text
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/fetcher/ManagerFetcher.java`
#### Snippet
```java
     * build ip check url for manager according to config
     * <p>
     * example - http://127.0.0.1:8080/inlong/manager/openapi/fileAgent/confirmAgentIp
     */
    private String buildIpCheckUrl(String baseUrl) {
```

### JavadocLinkAsPlainText
Link specified as plain text
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/fetcher/ManagerFetcher.java`
#### Snippet
```java
     * build file collect task url for manager according to config
     * <p>
     * example - http://127.0.0.1:8080/inlong/manager/openapi/fileAgent/getTaskConf
     */
    private String buildFileCollectTaskUrl(String baseUrl) {
```

### JavadocLinkAsPlainText
Link specified as plain text
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/fetcher/ManagerFetcher.java`
#### Snippet
```java
     * build db collector get task url for manager according to config
     * <p>
     * example - http://127.0.0.1:8080/inlong/manager/openapi/dbcollector/getTask
     */
    private String buildDbCollectorGetTaskUrl(String baseUrl) {
```

### JavadocLinkAsPlainText
Link specified as plain text
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/fetcher/ManagerFetcher.java`
#### Snippet
```java
     * build base url for manager according to config
     * <p>
     * example - http://127.0.0.1:8080/inlong/manager/openapi
     */
    private String buildBaseUrl() {
```

### JavadocLinkAsPlainText
Link specified as plain text
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/fetcher/ManagerFetcher.java`
#### Snippet
```java
     * build vip url for manager according to config
     * <p>
     * example - http://127.0.0.1:8080/inlong/manager/openapi/agent/getManagerIpList
     */
    private String buildVipUrl(String baseUrl) {
```

### JavadocLinkAsPlainText
Link specified as plain text
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/utils/HttpUtils.java`
#### Snippet
```java
     *      and there are nodes that do not take effect
     * Call url:
     *    http://127.0.0.1:8080/webapi.htm?method=admin_query_topic_info
     * Request parameters:
     *    topicName=test_1, brokerId=170399798
```

### JavadocLinkAsPlainText
Link specified as plain text
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/utils/IdWorker.java`
#### Snippet
```java

/**
 * reference :https://github.com/twitter/snowflake/blob/master/src/main/scala/com/twitter/service/snowflake/IdWorker.scala
 */
public class IdWorker {
```

### JavadocLinkAsPlainText
Link specified as plain text
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/zookeeper/RecoverableZooKeeper.java`
#### Snippet
```java
 * created is "x-352-109".
 *
 * See "http://wiki.apache.org/hadoop/ZooKeeper/ErrorHandling"
 *
 * Copied from <a href="http://hbase.apache.org">Apache HBase Project</a>
```

## RuleId[id=FieldCanBeLocal]
### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-sdk/sort-sdk/src/main/java/org/apache/inlong/sdk/sort/util/PeriodicTask.java`
#### Snippet
```java

    private final TimeUnit timeUnit;
    private final SortClientConfig config;
    protected Logger logger = LoggerFactory.getLogger(PeriodicTask.class);
    private volatile long runInterval;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-sdk/sort-sdk/src/main/java/org/apache/inlong/sdk/sort/metrics/SortSdkPrometheusMetricListener.java`
#### Snippet
```java
    private SortSdkMetricItem metricItem;
    private Map<String, AtomicLong> metricValueMap = new ConcurrentHashMap<>();
    private List<String> dimensionKeys = new ArrayList<>();

    public SortSdkPrometheusMetricListener() {
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/network/TcpChannelGroup.java`
#### Snippet
```java

    private String bid;
    private int senderThreadNum;
    private ClientBootstrap client = new ClientBootstrap();
    private Set<IpPort> currentIpLists = new HashSet<>();
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/utils/ServiceDiscoveryUtils.java`
#### Snippet
```java
    private static final String GET_MANAGER_IP_LIST_API = "/inlong/manager/openapi/agent/getManagerIpList";
    private static String latestManagerIPList = "";
    private static String arraySed = ",";

    /**
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/example/MyMessageCallBack.java`
#### Snippet
```java
            .getLogger(MyMessageCallBack.class);
    private DefaultMessageSender messageSender = null;
    private Event event = null;

    public MyMessageCallBack() {
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/example/MyMessageCallBack.java`
#### Snippet
```java
    private static final Logger logger = LoggerFactory
            .getLogger(MyMessageCallBack.class);
    private DefaultMessageSender messageSender = null;
    private Event event = null;

```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/example/MyFileCallBack.java`
#### Snippet
```java
    private static final Logger logger = LoggerFactory
            .getLogger(MyFileCallBack.class);
    private DefaultMessageSender messageSender = null;
    private Event event = null;

```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/example/MyFileCallBack.java`
#### Snippet
```java
            .getLogger(MyFileCallBack.class);
    private DefaultMessageSender messageSender = null;
    private Event event = null;

    public MyFileCallBack() {
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kudu/src/main/java/org/apache/inlong/sort/kudu/sink/KuduWriter.java`
#### Snippet
```java

    private final List<Tuple2<Long, Integer>> consumeTimes;
    private final int timePrintingThreshold = 10;
    private int applyNum = 0;

```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kudu/src/main/java/org/apache/inlong/sort/kudu/sink/AbstractKuduSinkFunction.java`
#### Snippet
```java
     * The exception thrown in asynchronous tasks.
     */
    private transient Throwable flushThrowable;

    private SinkMetricData sinkMetricData;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kudu/src/main/java/org/apache/inlong/sort/kudu/table/KuduDynamicTableSink.java`
#### Snippet
```java
     */
    @Nullable
    private String[] keyFieldNames;
    private final boolean ignoreAllChangeLog;

```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/jdbc/src/main/java/org/apache/inlong/sort/jdbc/internal/JdbcBatchingOutputFormat.java`
#### Snippet
```java
    private transient ScheduledFuture<?> scheduledFuture;
    private transient volatile Exception flushException;
    private transient RuntimeContext runtimeContext;

    private transient ListState<MetricState> metricStateListState;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/hbase/src/main/java/org/apache/inlong/sort/hbase/sink/HBaseSinkFunction.java`
#### Snippet
```java
    private transient ScheduledFuture scheduledFuture;
    private transient AtomicLong numPendingRequests;
    private transient RuntimeContext runtimeContext;
    private transient volatile boolean closed = false;
    private Long dataSize = 0L;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/jdbc/src/main/java/org/apache/inlong/sort/jdbc/internal/JdbcMultiBatchingOutputFormat.java`
#### Snippet
```java
    private transient ScheduledFuture<?> scheduledFuture;
    private transient RuntimeContext runtimeContext;
    private transient JsonDynamicSchemaFormat jsonDynamicSchemaFormat;
    private JdbcDmlOptions dmlOptions;
    private JdbcOptions jdbcOptions;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/jdbc/src/main/java/org/apache/inlong/sort/jdbc/internal/JdbcMultiBatchingOutputFormat.java`
#### Snippet
```java
    private transient ScheduledExecutorService scheduler;
    private transient ScheduledFuture<?> scheduledFuture;
    private transient RuntimeContext runtimeContext;
    private transient JsonDynamicSchemaFormat jsonDynamicSchemaFormat;
    private JdbcDmlOptions dmlOptions;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/pulsar/src/main/java/org/apache/inlong/sort/pulsar/table/DynamicPulsarSerializationSchema.java`
#### Snippet
```java
    private int parallelInstanceId;

    private int numParallelInstances;

    private DataType valueDataType;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/pulsar/src/main/java/org/apache/inlong/sort/pulsar/table/DynamicPulsarSerializationSchema.java`
#### Snippet
```java
    private int[] partitions;

    private int parallelInstanceId;

    private int numParallelInstances;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/pulsar/src/main/java/org/apache/inlong/sort/pulsar/internal/ReaderThread.java`
#### Snippet
```java
    protected boolean excludeMessageId = false;
    private boolean failOnDataLoss = true;
    private boolean useEarliestWhenDataLoss = false;
    private final PulsarCollector pulsarCollector;
    private final String subscriptionName;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/pulsar/src/main/java/org/apache/inlong/sort/pulsar/internal/PulsarMetadataReader.java`
#### Snippet
```java
    private final String serverUrl;

    private final ClientConfigurationData clientConf;

    private final String subscriptionName;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/pulsar/src/main/java/org/apache/inlong/sort/pulsar/internal/PulsarMetadataReader.java`
#### Snippet
```java
                    .withDescription("Authentication token for connection.");

    private final String serverUrl;

    private final ClientConfigurationData clientConf;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/pulsar/src/main/java/org/apache/inlong/sort/pulsar/internal/PulsarMetadataReader.java`
#### Snippet
```java
    private Set<TopicRange> seenTopics = new HashSet<>();

    private final boolean useExternalSubscription;

    private final SerializableRange range;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/iceberg/src/main/java/org/apache/inlong/sort/iceberg/FlinkActions.java`
#### Snippet
```java
        private static final long serialVersionUID = 1L;

        private Map<String, String> actionProperties;

        @Override
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/pulsar/src/main/java/org/apache/inlong/sort/pulsar/internal/PulsarFetcher.java`
#### Snippet
```java

    /** The threads that runs the actual reading and hand the records to this fetcher. */
    private Map<TopicRange, ReaderThread<T>> topicToThread;

    /** Failed or not when data loss. **/
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/pulsar/src/main/java/org/apache/inlong/sort/pulsar/internal/PulsarFetcher.java`
#### Snippet
```java
     * pulsar-shipped metrics will be registered.
     */
    private final boolean useMetrics;

    /**
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/pulsar/src/main/java/org/apache/inlong/sort/pulsar/internal/FlinkPulsarSourceWithoutAdmin.java`
#### Snippet
```java

    /** Counter for successful Pulsar offset commits. */
    private transient Counter successfulCommits;

    /** Counter for failed Pulsar offset commits. */
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/pulsar/src/main/java/org/apache/inlong/sort/pulsar/internal/FlinkPulsarSourceWithoutAdmin.java`
#### Snippet
```java

    /** Counter for failed Pulsar offset commits. */
    private transient Counter failedCommits;

    private transient int taskIndex;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/iceberg/src/main/java/org/apache/inlong/sort/iceberg/sink/FlinkSink.java`
#### Snippet
```java
        private DistributionMode distributionMode = null;
        private Integer writeParallelism = null;
        private boolean upsert = false;
        private List<String> equalityFieldColumns = null;
        private String uidPrefix = null;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/iceberg/src/main/java/org/apache/inlong/sort/iceberg/sink/FlinkSink.java`
#### Snippet
```java
        private boolean overwrite = false;
        private boolean appendMode = false;
        private DistributionMode distributionMode = null;
        private Integer writeParallelism = null;
        private boolean upsert = false;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/iceberg/src/main/java/org/apache/inlong/sort/iceberg/sink/multiple/IcebergMultipleStreamWriter.java`
#### Snippet
```java
    private transient MetricState metricState;
    private transient ListState<MetricState> metricStateListState;
    private transient RuntimeContext runtimeContext;

    public IcebergMultipleStreamWriter(
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/mysql/debezium/task/MySqlBinlogSplitReadTask.java`
#### Snippet
```java
    private final MySqlBinlogSplit binlogSplit;
    private final MySqlOffsetContext offsetContext;
    private final EventDispatcherImpl<TableId> eventDispatcher;
    private final SignalEventDispatcher signalEventDispatcher;
    private final ErrorHandler errorHandler;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/oracle-cdc/src/main/java/org/apache/inlong/sort/cdc/oracle/source/OracleSourceBuilder.java`
#### Snippet
```java
    private final OracleSourceConfigFactory configFactory = new OracleSourceConfigFactory();
    private RedoLogOffsetFactory offsetFactory;
    private OracleDialect dialect;
    private DebeziumDeserializationSchema<T> deserializer;

```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/oracle-cdc/src/main/java/org/apache/inlong/sort/cdc/oracle/source/OracleSourceBuilder.java`
#### Snippet
```java

    private final OracleSourceConfigFactory configFactory = new OracleSourceConfigFactory();
    private RedoLogOffsetFactory offsetFactory;
    private OracleDialect dialect;
    private DebeziumDeserializationSchema<T> deserializer;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/oracle-cdc/src/main/java/org/apache/inlong/sort/cdc/oracle/source/OracleDialect.java`
#### Snippet
```java

    private static final long serialVersionUID = 1L;
    private final OracleSourceConfigFactory configFactory;
    private final OracleSourceConfig sourceConfig;
    private transient OracleSchema oracleSchema;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/oracle-cdc/src/main/java/org/apache/inlong/sort/cdc/oracle/source/OracleDialect.java`
#### Snippet
```java
    private static final long serialVersionUID = 1L;
    private final OracleSourceConfigFactory configFactory;
    private final OracleSourceConfig sourceConfig;
    private transient OracleSchema oracleSchema;

```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/starrocks/src/main/java/org/apache/inlong/sort/starrocks/manager/StarRocksSinkManager.java`
#### Snippet
```java

    private final DirtySinkHelper<Object> dirtySinkHelper;
    private String sinkMultipleFormat;

    /**
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/oracle-cdc/src/main/java/org/apache/inlong/sort/cdc/oracle/source/reader/fetch/OracleStreamFetchTask.java`
#### Snippet
```java
    private final StreamSplit split;
    private volatile boolean taskRunning = false;
    private RedoLogSplitReadTask redoLogSplitReadTask;

    public OracleStreamFetchTask(StreamSplit split) {
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/oracle-cdc/src/main/java/io/debezium/connector/oracle/logminer/LogMinerStreamingChangeEventSource.java`
#### Snippet
```java
    private final OracleDatabaseSchema schema;
    private final boolean isRac;
    private final Set<String> racHosts = new HashSet<>();
    private final JdbcConfiguration jdbcConfiguration;
    private final OracleConnectorConfig.LogMiningStrategy strategy;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/oracle-cdc/src/main/java/io/debezium/connector/oracle/logminer/LogMinerStreamingChangeEventSource.java`
#### Snippet
```java
    private final Clock clock;
    private final OracleDatabaseSchema schema;
    private final boolean isRac;
    private final Set<String> racHosts = new HashSet<>();
    private final JdbcConfiguration jdbcConfiguration;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/oracle-cdc/src/main/java/org/apache/inlong/sort/cdc/oracle/source/reader/fetch/OracleSourceFetchTaskContext.java`
#### Snippet
```java
    private SnapshotChangeEventSourceMetrics snapshotChangeEventSourceMetrics;
    private OracleStreamingChangeEventSourceMetrics streamingChangeEventSourceMetrics;
    private TopicSelector<TableId> topicSelector;
    private JdbcSourceEventDispatcher dispatcher;
    private ChangeEventQueue<DataChangeEvent> queue;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/oracle-cdc/src/main/java/org/apache/inlong/sort/cdc/oracle/source/reader/fetch/OracleScanFetchTask.java`
#### Snippet
```java
    private volatile boolean taskRunning = false;

    private OracleSnapshotSplitReadTask snapshotSplitReadTask;

    public OracleScanFetchTask(SnapshotSplit split) {
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mongodb-cdc/src/main/java/org/apache/inlong/sort/cdc/mongodb/source/config/MongoDBSourceConfigFactory.java`
#### Snippet
```java
    private Integer pollAwaitTimeMillis = POLL_AWAIT_TIME_MILLIS.defaultValue();
    private Integer pollMaxBatchSize = POLL_MAX_BATCH_SIZE.defaultValue();
    private Boolean updateLookup = true;
    private StartupOptions startupOptions = StartupOptions.initial();
    private Integer heartbeatIntervalMillis = HEARTBEAT_INTERVAL_MILLIS.defaultValue();
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/postgres-cdc/src/main/java/org/apache/inlong/sort/cdc/postgres/debezium/internal/DebeziumChangeFetcher.java`
#### Snippet
```java
     * PostgreSQL connector properties
     */
    private Properties properties;

    private PostgreSQLQueryVisitor postgreSQLQueryVisitor;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-sort/sort-formats/format-json/src/main/java/org/apache/inlong/sort/formats/json/canal/CanalJsonEnhancedSerializationSchema.java`
#### Snippet
```java
     * row schema that json serializer can parse output row to json format
     */
    private final RowType jsonRowType;
    /**
     * The index in writeableMetadata of {@link WriteableMetadata#TYPE}
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-agent/agent-core/src/main/java/org/apache/inlong/agent/core/job/JobManager.java`
#### Snippet
```java
    private final Map<String, String> dimensions;

    private final AgentConfiguration agentConf;

    /**
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-agent/agent-core/src/main/java/org/apache/inlong/agent/core/task/TaskPositionManager.java`
#### Snippet
```java
    private static final Logger LOGGER = LoggerFactory.getLogger(TaskPositionManager.class);
    private static volatile TaskPositionManager taskPositionManager = null;
    private final AgentManager agentManager;
    private final JobProfileDb jobConfDb;
    private final AgentConfiguration conf;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-agent/agent-core/src/main/java/org/apache/inlong/agent/core/task/TaskManager.java`
#### Snippet
```java
    // task thread pool;
    private final ThreadPoolExecutor runningPool;
    private final AgentManager agentManager;
    private final ConcurrentHashMap<String, TaskWrapper> tasks;
    private final BlockingQueue<TaskWrapper> retryTasks;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-agent/agent-core/src/main/java/org/apache/inlong/agent/core/trigger/TriggerManager.java`
#### Snippet
```java
    private final TriggerProfileDb triggerProfileDB;
    private final ConcurrentHashMap<String, Trigger> triggerMap;
    private final AgentConfiguration conf;
    private final int triggerFetchInterval;
    private final int maxRunningNum;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/sinks/SenderManager.java`
#### Snippet
```java
    // metric
    private AgentMetricItemSet metricItemSet;
    private Map<String, String> dimensions;
    private TaskPositionManager taskPositionManager;
    private int ioThreadNum;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/sinks/PulsarSink.java`
#### Snippet
```java
    private int clientIoThreads;

    private int sendQueueSize;
    private Semaphore sendQueueSemaphore; // limit the count of batchProxyMessage waiting to be sent
    private LinkedBlockingQueue<BatchProxyMessage> pulsarSendQueue;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/sources/reader/PostgreSQLReader.java`
#### Snippet
```java
    private String snapshotMode;
    private String instanceId;
    private String offset;
    private String specificOffsetFile;
    private String specificOffsetPos;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/fetcher/ManagerFetcher.java`
#### Snippet
```java
    private static final int MAX_RETRY = 2;
    private final String managerVipUrl;
    private final String baseManagerUrl;
    private final String managerTaskUrl;
    private final String managerIpsCheckUrl;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/fetcher/ManagerFetcher.java`
#### Snippet
```java
    private final String managerDbCollectorTaskUrl;
    private final AgentConfiguration conf;
    private final String uniqId;
    private final AgentManager agentManager;
    private final HttpManager httpManager;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/sources/reader/MqttReader.java`
#### Snippet
```java
    private int qos;
    private boolean cleanSession = false;
    private boolean automaticReconnect = true;
    private JobProfile jobProfile;
    private String instanceId;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/sources/reader/MqttReader.java`
#### Snippet
```java
    private String serverURI;
    private String userName;
    private String password;
    private String topic;
    private int qos;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/sources/reader/MqttReader.java`
#### Snippet
```java

    private String serverURI;
    private String userName;
    private String password;
    private String topic;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/sources/reader/MqttReader.java`
#### Snippet
```java
    private String instanceId;
    private String clientId;
    private int mqttVersion = MqttConnectOptions.MQTT_VERSION_DEFAULT;

    private LinkedBlockingQueue<DefaultMessage> mqttMessagesQueue;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/sources/reader/MqttReader.java`
#### Snippet
```java
    private String topic;
    private int qos;
    private boolean cleanSession = false;
    private boolean automaticReconnect = true;
    private JobProfile jobProfile;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/sources/reader/MqttReader.java`
#### Snippet
```java
    private boolean cleanSession = false;
    private boolean automaticReconnect = true;
    private JobProfile jobProfile;
    private String instanceId;
    private String clientId;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/sources/reader/OracleReader.java`
#### Snippet
```java
    private String offsetStoreFileName;
    private String snapshotMode;
    private String offset;
    private String specificOffsetFile;
    private String specificOffsetPos;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/sources/reader/SQLServerReader.java`
#### Snippet
```java
    private String offsetStoreFileName;
    private String snapshotMode;
    private String offset;
    private String specificOffsetFile;
    private String specificOffsetPos;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-audit/audit-sdk/src/main/java/org/apache/inlong/audit/send/SenderGroup.java`
#### Snippet
```java
    private int waitChannelTimes = DEFAULT_WAIT_TIMES;
    private int waitChannelIntervalMs = WAIT_INTERVAL;
    private int maxSynchRequest = DEFAULT_SYNCH_REQUESTS;
    private boolean hasSendError = false;

```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-audit/audit-sdk/src/main/java/org/apache/inlong/audit/send/SenderGroup.java`
#### Snippet
```java

    private int waitChannelTimes = DEFAULT_WAIT_TIMES;
    private int waitChannelIntervalMs = WAIT_INTERVAL;
    private int maxSynchRequest = DEFAULT_SYNCH_REQUESTS;
    private boolean hasSendError = false;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-audit/audit-sdk/src/main/java/org/apache/inlong/audit/util/AuditConfig.java`
#### Snippet
```java
    }

    private String disasterFileName = "disaster.data";
}

```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/trigger/DirectoryTrigger.java`
#### Snippet
```java
    private Set<PathPattern> pathPatterns = new HashSet<>();
    private TriggerProfile profile;
    private int interval;

    private static void initWatchService() {
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/sources/reader/file/FileReaderOperator.java`
#### Snippet
```java
    public String fileKey = null;
    private long timeout;
    private long waitTimeout;
    private long lastTime = 0;
    private List<Validator> validators = new ArrayList<>();
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/PulsarSink.java`
#### Snippet
```java
     * properties for header info
     */
    private static String TOPIC = "topic";

    /*
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/PulsarSink.java`
#### Snippet
```java
    private static int EVENT_QUEUE_SIZE = 1000;

    private int threadNum;

    /*
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/PulsarSink.java`
#### Snippet
```java
    private Integer logEveryNEvents;

    private long diskIORatePerSec;

    private RateLimiter diskRateLimiter;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/PulsarSink.java`
#### Snippet
```java
    private static String LOG_EVERY_N_EVENTS = "log_every_n_events";

    private static String DISK_IO_RATE_PER_SEC = "disk_io_rate_per_sec";

    private static final String SINK_THREAD_NUM = "thread_num";
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/PulsarSink.java`
#### Snippet
```java
     * properties for stat
     */
    private static String LOG_EVERY_N_EVENTS = "log_every_n_events";

    private static String DISK_IO_RATE_PER_SEC = "disk_io_rate_per_sec";
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/PulsarSink.java`
#### Snippet
```java
     */
    private static int BAD_EVENT_QUEUE_SIZE = 10000;
    private static int BATCH_SIZE = 10000;
    private static final int DEFAULT_LOG_EVERY_N_EVENTS = 100000;

```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/PulsarSink.java`
#### Snippet
```java
    private volatile boolean canTake = false;

    private static int EVENT_QUEUE_SIZE = 1000;

    private int threadNum;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/PulsarSink.java`
#### Snippet
```java
    private long t1 = System.currentTimeMillis();

    private long t2 = 0L;

    private static AtomicLong totalPulsarSuccSendCnt = new AtomicLong(0);
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/PulsarSink.java`
#### Snippet
```java
     * default value
     */
    private static int BAD_EVENT_QUEUE_SIZE = 10000;
    private static int BATCH_SIZE = 10000;
    private static final int DEFAULT_LOG_EVERY_N_EVENTS = 100000;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/pulsar/PulsarClientService.java`
#### Snippet
```java

    private static boolean DEFAULT_PULSAR_ENABLE_TOKEN_AUTH = false;
    private static String DEFAULT_PULSAR_TOKEN_AUTH = "";
    /*
     * for producer
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/pulsar/PulsarClientService.java`
#### Snippet
```java
    private static String BLOCK_IF_QUEUE_FULL = "block_if_queue_full";
    private static String MAX_PENDING_MESSAGES = "max_pending_messages";
    private static String MAX_BATCHING_MESSAGES = "max_batching_messages";

    private static int DEFAULT_SEND_TIMEOUT_MILL = 30 * 1000;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/pulsar/PulsarClientService.java`
#### Snippet
```java
     * properties key pulsar producer
     */
    private static String SEND_TIMEOUT = "send_timeout_ms";
    private static String CLIENT_TIMEOUT = "client_op_timeout_second";
    private static String ENABLE_BATCH = "enable_batch";
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/pulsar/PulsarClientService.java`
#### Snippet
```java
    private static boolean DEFAULT_ENABLE_BATCH = true;
    private static boolean DEFAULT_BLOCK_IF_QUEUE_FULL = true;
    private static int DEFAULT_MAX_PENDING_MESSAGES = 10000;
    private static int DEFAULT_MAX_BATCHING_MESSAGES = 1000;

```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/pulsar/PulsarClientService.java`
#### Snippet
```java
    private static String ENABLE_BATCH = "enable_batch";
    private static String BLOCK_IF_QUEUE_FULL = "block_if_queue_full";
    private static String MAX_PENDING_MESSAGES = "max_pending_messages";
    private static String MAX_BATCHING_MESSAGES = "max_batching_messages";

```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/pulsar/PulsarClientService.java`
#### Snippet
```java
    private static boolean DEFAULT_BLOCK_IF_QUEUE_FULL = true;
    private static int DEFAULT_MAX_PENDING_MESSAGES = 10000;
    private static int DEFAULT_MAX_BATCHING_MESSAGES = 1000;

    private static boolean DEFAULT_PULSAR_ENABLE_TOKEN_AUTH = false;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/pulsar/PulsarClientService.java`
#### Snippet
```java

    private static int DEFAULT_SEND_TIMEOUT_MILL = 30 * 1000;
    private static int DEFAULT_CLIENT_TIMEOUT_SECOND = 30;
    private static boolean DEFAULT_ENABLE_BATCH = true;
    private static boolean DEFAULT_BLOCK_IF_QUEUE_FULL = true;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/pulsar/PulsarClientService.java`
#### Snippet
```java
    private static String PULSAR_SERVER_URL = "pulsar_server_url";
    private static String PULSAR_ENABLE_AUTH = "enable_token_auth";
    private static String PULSAR_ENABLE_AUTH_TOKEN = "auth_token";
    /*
     * properties key pulsar producer
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/pulsar/PulsarClientService.java`
#### Snippet
```java
     */
    private static String PULSAR_SERVER_URL = "pulsar_server_url";
    private static String PULSAR_ENABLE_AUTH = "enable_token_auth";
    private static String PULSAR_ENABLE_AUTH_TOKEN = "auth_token";
    /*
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/pulsar/PulsarClientService.java`
#### Snippet
```java
    private static int DEFAULT_SEND_TIMEOUT_MILL = 30 * 1000;
    private static int DEFAULT_CLIENT_TIMEOUT_SECOND = 30;
    private static boolean DEFAULT_ENABLE_BATCH = true;
    private static boolean DEFAULT_BLOCK_IF_QUEUE_FULL = true;
    private static int DEFAULT_MAX_PENDING_MESSAGES = 10000;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/pulsar/PulsarClientService.java`
#### Snippet
```java
    private static int DEFAULT_CLIENT_TIMEOUT_SECOND = 30;
    private static boolean DEFAULT_ENABLE_BATCH = true;
    private static boolean DEFAULT_BLOCK_IF_QUEUE_FULL = true;
    private static int DEFAULT_MAX_PENDING_MESSAGES = 10000;
    private static int DEFAULT_MAX_BATCHING_MESSAGES = 1000;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/pulsar/PulsarClientService.java`
#### Snippet
```java
    private static String MAX_BATCHING_MESSAGES = "max_batching_messages";

    private static int DEFAULT_SEND_TIMEOUT_MILL = 30 * 1000;
    private static int DEFAULT_CLIENT_TIMEOUT_SECOND = 30;
    private static boolean DEFAULT_ENABLE_BATCH = true;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/pulsar/PulsarClientService.java`
#### Snippet
```java
    private static String SEND_TIMEOUT = "send_timeout_ms";
    private static String CLIENT_TIMEOUT = "client_op_timeout_second";
    private static String ENABLE_BATCH = "enable_batch";
    private static String BLOCK_IF_QUEUE_FULL = "block_if_queue_full";
    private static String MAX_PENDING_MESSAGES = "max_pending_messages";
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/pulsar/PulsarClientService.java`
#### Snippet
```java
    private static int DEFAULT_MAX_BATCHING_MESSAGES = 1000;

    private static boolean DEFAULT_PULSAR_ENABLE_TOKEN_AUTH = false;
    private static String DEFAULT_PULSAR_TOKEN_AUTH = "";
    /*
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/pulsar/PulsarClientService.java`
#### Snippet
```java
    private static String CLIENT_TIMEOUT = "client_op_timeout_second";
    private static String ENABLE_BATCH = "enable_batch";
    private static String BLOCK_IF_QUEUE_FULL = "block_if_queue_full";
    private static String MAX_PENDING_MESSAGES = "max_pending_messages";
    private static String MAX_BATCHING_MESSAGES = "max_batching_messages";
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/pulsar/PulsarClientService.java`
#### Snippet
```java
     */
    private static String SEND_TIMEOUT = "send_timeout_ms";
    private static String CLIENT_TIMEOUT = "client_op_timeout_second";
    private static String ENABLE_BATCH = "enable_batch";
    private static String BLOCK_IF_QUEUE_FULL = "block_if_queue_full";
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/source/SimpleTcpSource.java`
#### Snippet
```java
    private static int SEND_BUFFER_DEFAULT_SIZE = 64 * 1024;

    private static int RECEIVE_BUFFER_MAX_SIZE = 16 * 1024 * 1024;

    private static int SEND_BUFFER_MAX_SIZE = 16 * 1024 * 1024;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/source/SimpleTcpSource.java`
#### Snippet
```java
    private static int HIGH_WATER_MARK_DEFAULT_VALUE = 64 * 1024;

    private static int RECEIVE_BUFFER_DEFAULT_SIZE = 64 * 1024;

    private static int SEND_BUFFER_DEFAULT_SIZE = 64 * 1024;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/source/SimpleTcpSource.java`
#### Snippet
```java
    private static int BUFFER_SIZE_MUST_THAN = 0;

    private static int HIGH_WATER_MARK_DEFAULT_VALUE = 64 * 1024;

    private static int RECEIVE_BUFFER_DEFAULT_SIZE = 64 * 1024;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/source/SimpleTcpSource.java`
#### Snippet
```java
    protected Context context;

    private ServerBootstrap bootstrap = null;
    protected EventLoopGroup acceptorGroup;
    protected EventLoopGroup workerGroup;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/source/SimpleTcpSource.java`
#### Snippet
```java
    private static int DEFAULT_MAX_THREADS = 32;

    private static int DEFAULT_MAX_CONNECTIONS = 5000;

    private static int MIN_MSG_LENGTH = 4;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/source/SimpleTcpSource.java`
#### Snippet
```java
    private static int MIN_MSG_LENGTH = 4;

    private static int MAX_MSG_LENGTH = 1024 * 64;

    private static int BUFFER_SIZE_MUST_THAN = 0;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/source/SimpleTcpSource.java`
#### Snippet
```java
    private static int DEFAULT_MAX_CONNECTIONS = 5000;

    private static int MIN_MSG_LENGTH = 4;

    private static int MAX_MSG_LENGTH = 1024 * 64;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/source/SimpleTcpSource.java`
#### Snippet
```java
    private static int RECEIVE_BUFFER_DEFAULT_SIZE = 64 * 1024;

    private static int SEND_BUFFER_DEFAULT_SIZE = 64 * 1024;

    private static int RECEIVE_BUFFER_MAX_SIZE = 16 * 1024 * 1024;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/source/SimpleTcpSource.java`
#### Snippet
```java
    private static String HOST_DEFAULT_VALUE = "0.0.0.0";

    private static int DEFAULT_MAX_THREADS = 32;

    private static int DEFAULT_MAX_CONNECTIONS = 5000;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/source/SimpleTcpSource.java`
#### Snippet
```java
    protected boolean customProcessor = false;

    private static String HOST_DEFAULT_VALUE = "0.0.0.0";

    private static int DEFAULT_MAX_THREADS = 32;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/source/SimpleTcpSource.java`
#### Snippet
```java
    private static int RECEIVE_BUFFER_MAX_SIZE = 16 * 1024 * 1024;

    private static int SEND_BUFFER_MAX_SIZE = 16 * 1024 * 1024;

    protected boolean enableBusyWait = false;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/source/SimpleTcpSource.java`
#### Snippet
```java
    private static int MAX_MSG_LENGTH = 1024 * 64;

    private static int BUFFER_SIZE_MUST_THAN = 0;

    private static int HIGH_WATER_MARK_DEFAULT_VALUE = 64 * 1024;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/KafkaSink.java`
#### Snippet
```java
    private volatile boolean canSend = false;
    private volatile boolean canTake = false;
    private int threadNum;
    private Thread[] sinkThreadPool;

```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/KafkaSink.java`
#### Snippet
```java
    private AtomicLong lastSuccessSendCnt = new AtomicLong(0);
    private long t1 = System.currentTimeMillis();
    private long t2 = 0L;
    private static AtomicLong totalKafkaSuccSendCnt = new AtomicLong(0);
    private static AtomicLong totalKafkaSuccSendSize = new AtomicLong(0);
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/KafkaSink.java`
#### Snippet
```java
    // for log
    private Integer logEveryNEvents;
    private long diskIORatePerSec;
    private RateLimiter diskRateLimiter;

```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/KafkaSink.java`
#### Snippet
```java
    private boolean overflow = false;

    private String localIp = "127.0.0.1";

    static {
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/TubeSink.java`
#### Snippet
```java
    private String masterHostAndPortList;
    private Integer logEveryNEvents;
    private Integer sendTimeout;
    private int threadNum;
    private Thread[] sinkThreadPool;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/TubeSink.java`
#### Snippet
```java
    private AtomicLong lastSuccessSendCnt = new AtomicLong(0);
    private long t1 = System.currentTimeMillis();
    private long t2 = 0L;

    private String localIp = "127.0.0.1";
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/TubeSink.java`
#### Snippet
```java
    private Integer logEveryNEvents;
    private Integer sendTimeout;
    private int threadNum;
    private Thread[] sinkThreadPool;
    private long linkMaxAllowedDelayedMsgCount;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/TubeSink.java`
#### Snippet
```java
    private static final String RETRY_CNT = "retry-currentSuccSendedCnt";

    private static int retryCnt = defaultRetryCnt;

    private static AtomicLong totalTubeSuccSendCnt = new AtomicLong(0);
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/TubeSink.java`
#### Snippet
```java
    private long sessionMaxAllowedDelayedMsgCount;
    private long nettyWriteBufferHighWaterMark;
    private int recoverthreadcount;
    private boolean overflow = false;
    /*
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/TubeSink.java`
#### Snippet
```java
    private LinkedBlockingQueue<EventStat> resendQueue;
    private LinkedBlockingQueue<Event> eventQueue;
    private long diskIORatePerSec;
    private RateLimiter diskRateLimiter;
    private String masterHostAndPortList;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/TubeSink.java`
#### Snippet
```java

        private org.apache.inlong.audit.sink.EventStat myEventStat;
        private long sendTime;

        public MyCallback(org.apache.inlong.audit.sink.EventStat eventStat) {
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-audit/audit-store/src/main/java/org/apache/inlong/audit/service/consume/TubeConsume.java`
#### Snippet
```java
    private static final Logger LOG = LoggerFactory.getLogger(TubeConsume.class);
    private PullMessageConsumer pullConsumer;
    private TubeMultiSessionFactory sessionFactory;
    private String masterUrl;
    private String topic;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-audit/audit-store/src/main/java/org/apache/inlong/audit/service/consume/TubeConsume.java`
#### Snippet
```java
    private String masterUrl;
    private String topic;
    private int fetchThreadCnt = 4;

    /**
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/benchemark/RpcService4BenchmarkServer.java`
#### Snippet
```java
    private final RpcServiceFactory rpcServiceFactory =
            new RpcServiceFactory();
    private SimpleService simpleService;

    public static void main(String[] args) throws Exception {
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/netty/NettyRpcServer.java`
#### Snippet
```java
    private ServerBootstrap bootstrap;
    private EventLoopGroup acceptorGroup;
    private EventLoopGroup workerGroup;
    private boolean enableBusyWait;
    private AtomicBoolean started = new AtomicBoolean(false);
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/netty/NettyRpcServer.java`
#### Snippet
```java
            new ConcurrentHashMap<>();
    private ServerBootstrap bootstrap;
    private EventLoopGroup acceptorGroup;
    private EventLoopGroup workerGroup;
    private boolean enableBusyWait;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/netty/NettyRpcServer.java`
#### Snippet
```java
    private EventLoopGroup acceptorGroup;
    private EventLoopGroup workerGroup;
    private boolean enableBusyWait;
    private AtomicBoolean started = new AtomicBoolean(false);
    private int protocolType = RpcProtocol.RPC_PROTOCOL_TCP;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/benchemark/RcpService4BenchmarkClient.java`
#### Snippet
```java

    private final ExecutorService executorService = Executors.newCachedThreadPool();
    private final String targetHost;
    private final int targetPort;
    private final RpcServiceFactory rpcServiceFactory;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/benchemark/RcpService4BenchmarkClient.java`
#### Snippet
```java
    private final int targetPort;
    private final RpcServiceFactory rpcServiceFactory;
    private final NettyClientFactory clientFactory = new NettyClientFactory();
    private final SimpleService simpleService;
    private int threadNum = 10;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/benchemark/RcpService4BenchmarkClient.java`
#### Snippet
```java
    private final String targetHost;
    private final int targetPort;
    private final RpcServiceFactory rpcServiceFactory;
    private final NettyClientFactory clientFactory = new NettyClientFactory();
    private final SimpleService simpleService;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/benchemark/RcpService4BenchmarkClient.java`
#### Snippet
```java
    private final ExecutorService executorService = Executors.newCachedThreadPool();
    private final String targetHost;
    private final int targetPort;
    private final RpcServiceFactory rpcServiceFactory;
    private final NettyClientFactory clientFactory = new NettyClientFactory();
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/utils/ServiceStatusHolder.java`
#### Snippet
```java
    private static int allowedReadIOExcptCnt = 10;
    private static int allowedWriteIOExcptCnt = 10;
    private static long statsDurationMs = 120000;
    private static final AtomicLong curReadIOExcptCnt = new AtomicLong(0);
    private static final AtomicLong lastReadStatsTime =
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/common/ConfirmResult.java`
#### Snippet
```java
    private String topicName = "";
    private PeerInfo peerInfo = new PeerInfo();
    private String confirmContext = "";

    public ConfirmResult() {
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/policies/FlowCtrlRuleHandler.java`
#### Snippet
```java
public class FlowCtrlRuleHandler {

    private final boolean isDefaultHandler;
    private final String flowCtrlName;
    private static final Logger logger =
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/policies/FlowCtrlRuleHandler.java`
#### Snippet
```java
            new FlowCtrlItem(3, TBaseConstants.META_VALUE_UNDEFINED,
                    TBaseConstants.META_VALUE_UNDEFINED, TBaseConstants.META_VALUE_UNDEFINED);
    private long lastUpdateTime =
            System.currentTimeMillis();
    // Decoded flow control rules
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/RmtDataCache.java`
#### Snippet
```java
    private long lastEmptyBrokerPrintTime = 0;
    private long lastEmptyTopicPrintTime = 0;
    private long lastBrokerUpdatedTime = System.currentTimeMillis();
    private final AtomicLong lstBrokerConfigId =
            new AtomicLong(TBaseConstants.META_VALUE_UNDEFINED);
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/producer/ProducerManager.java`
#### Snippet
```java
    private final ConcurrentHashMap<String, AtomicInteger> publishTopics =
            new ConcurrentHashMap<>();
    private final RpcConfig rpcConfig = new RpcConfig();
    private final ScheduledExecutorService heartbeatService;
    private final AtomicLong visitToken =
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/producer/ProducerManager.java`
#### Snippet
```java
    private final TubeClientConfig tubeClientConfig;
    private final InnerSessionFactory sessionFactory;
    private final RpcServiceFactory rpcServiceFactory;
    private final ConcurrentHashMap<String, AtomicInteger> publishTopics =
            new ConcurrentHashMap<>();
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/producer/ProducerManager.java`
#### Snippet
```java
    private final int producerAddrId;
    private final TubeClientConfig tubeClientConfig;
    private final InnerSessionFactory sessionFactory;
    private final RpcServiceFactory rpcServiceFactory;
    private final ConcurrentHashMap<String, AtomicInteger> publishTopics =
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/tools/cli/CliProducer.java`
#### Snippet
```java
    private boolean syncProduction = false;
    private boolean withoutDelay = false;
    private boolean isStarted = false;
    private ExecutorService sendExecutorService = null;

```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/web/WebServer.java`
#### Snippet
```java
public class WebServer implements Server {

    private String hostname = "0.0.0.0";
    private int port = 8080;
    private org.eclipse.jetty.server.Server srv;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/tools/cli/CliConsumer.java`
#### Snippet
```java
    private boolean isPushConsume = false;

    private boolean isStarted = false;

    public CliConsumer() {
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/tools/StoreRepairAdmin.java`
#### Snippet
```java
        private final File topicDir;
        private final File indexDir;
        private int maxIndexSegmentSize =
                500000 * DataStoreUtils.STORE_INDEX_HEAD_LEN;
        private SegmentList segments;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/tools/StoreRepairAdmin.java`
#### Snippet
```java

        private final String topic;
        private final int storeId;
        private final String basePath;
        private final String topicKey;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/tools/StoreRepairAdmin.java`
#### Snippet
```java
        private final String topic;
        private final int storeId;
        private final String basePath;
        private final String topicKey;
        private final String storePath;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/tools/StoreRepairAdmin.java`
#### Snippet
```java
    private static class IndexRepairStore implements Closeable {

        private final String topic;
        private final int storeId;
        private final String basePath;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/TubeBroker.java`
#### Snippet
```java
            new SimpleClientAuthenticateHandler();
    private MasterService masterService;
    private BrokerPromMetricService promMetricService;
    private boolean requireReportConf = false;
    private boolean isOnline = false;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/stats/prometheus/BrokerPromMetricService.java`
#### Snippet
```java
    private TubeBroker tubeBroker;
    private PrometheusConfig promConfig = new PrometheusConfig();
    private HTTPServer httpServer;
    private volatile boolean started = false;

```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/tools/cli/CliMetaDataBRU.java`
#### Snippet
```java
    private String authToken = null;
    private String backupAndRecoveryPath = "./meta_backup";
    private File metaDataDir = null;
    private MasterInfo masterInfo;

```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/tools/cli/CliMetaDataBRU.java`
#### Snippet
```java
    private static final String storeFileNameCsmCtrl = "groupConsumeConfig";
    // cli parameters
    private String masterServers = null;
    private String operationType = null;
    private String authToken = null;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/offset/offsetstorage/ZkOffsetStorage.java`
#### Snippet
```java
    private final String tubeZkRoot;
    private final String consumerZkDir;
    private final boolean isBroker;
    private final int brokerId;
    private final String strBrokerId;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/offset/offsetstorage/ZkOffsetStorage.java`
#### Snippet
```java
    }

    private final String tubeZkRoot;
    private final String consumerZkDir;
    private final boolean isBroker;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/BrokerServiceServer.java`
#### Snippet
```java
    private final CertificateBrokerHandler serverAuthHandler;
    // consumer timeout listener.
    private final ConsumerTimeoutListener consumerListener =
            new ConsumerTimeoutListener();
    // status of broker service.
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/nodeinfo/ConsumerNodeInfo.java`
#### Snippet
```java
    // partition string format
    private final String partStr;
    private final MessageStoreManager storeManager;
    // consumer id
    private String consumerId;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/nodeinfo/ConsumerNodeInfo.java`
#### Snippet
```java
    private final AtomicInteger qryPriorityId =
            new AtomicInteger(TBaseConstants.META_VALUE_UNDEFINED);
    private long createTime = System.currentTimeMillis();

    /**
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/statusdef/CleanPolType.java`
#### Snippet
```java

    private int code;
    private String description;

    CleanPolType(int code, String description) {
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/action/screen/config/BrokerDetail.java`
#### Snippet
```java
public class BrokerDetail implements Action {

    private TMaster master;

    public BrokerDetail(TMaster master) {
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/action/screen/config/TopicDetail.java`
#### Snippet
```java
public class TopicDetail implements Action {

    private TMaster master;

    public TopicDetail(TMaster master) {
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/action/screen/config/TopicList.java`
#### Snippet
```java
public class TopicList implements Action {

    private TMaster master;

    public TopicList(TMaster master) {
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/action/screen/consume/Detail.java`
#### Snippet
```java
public class Detail implements Action {

    private TMaster master;

    public Detail(TMaster master) {
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/simplemvc/WebApiServlet.java`
#### Snippet
```java
    private MultipartConfigElement multipartConfig;
    private String configFilePath;
    private File configFile;
    private WebConfig config;
    private RequestDispatcher dispatcher;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/stats/prometheus/MasterPromMetricService.java`
#### Snippet
```java
            LoggerFactory.getLogger(MasterPromMetricService.class);
    private PrometheusConfig promConfig = new PrometheusConfig();
    private HTTPServer httpServer;
    private volatile boolean started = false;

```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/utils/SimpleVisitTokenManager.java`
#### Snippet
```java
    private static final Logger logger = LoggerFactory.getLogger(SimpleVisitTokenManager.class);

    private final MasterConfig masterConfig;
    private final AtomicLong validVisitAuthorized = new AtomicLong(0);
    private final AtomicLong freshVisitAuthorized = new AtomicLong(0);
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/TMaster.java`
#### Snippet
```java
    private final NodeAddrInfo masterAddInfo; // master address info
    private final HeartbeatManager heartbeatManager; // heartbeat manager
    private final MasterPromMetricService promMetricService;
    private final ShutdownHook shutdownHook; // shutdown hook
    private final CertificateMasterHandler serverAuthHandler; // server auth handler
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/metamanage/DefaultMetaDataService.java`
#### Snippet
```java
    private volatile boolean isStarted = false;
    private volatile boolean isStopped = false;
    private long serviceStartTime = System.currentTimeMillis();

    public DefaultMetaDataService(TMaster tMaster) {
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-tubemq/tubemq-example/src/main/java/org/apache/inlong/tubemq/example/MessageProducerExample.java`
#### Snippet
```java
            new MsgSendReceiveStats(true);

    private static MessageSessionFactory sessionFactory;
    private static MessageProducer messageProducer;

```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-tubemq/tubemq-example/src/main/java/org/apache/inlong/tubemq/example/MessageProducerExample.java`
#### Snippet
```java

    private static MessageSessionFactory sessionFactory;
    private static MessageProducer messageProducer;

    /**
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-tubemq/tubemq-example/src/main/java/org/apache/inlong/tubemq/example/MessagePushConsumerExample.java`
#### Snippet
```java
    private static final MsgSendReceiveStats msgRcvStats =
            new MsgSendReceiveStats(false);
    private static MessageSessionFactory sessionFactory;
    private static final Map<String, PushMessageConsumer> consumerMap = new HashMap<>();

```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-tubemq/tubemq-example/src/main/java/org/apache/inlong/tubemq/example/MessagePullConsumerExample.java`
#### Snippet
```java
            new MsgSendReceiveStats(false);
    private static PullMessageConsumer pullConsumer;
    private static MessageSessionFactory sessionFactory;

    /**
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-tubemq/tubemq-example/src/main/java/org/apache/inlong/tubemq/example/MAMessageProducerExample.java`
#### Snippet
```java
            new HashMap<>();
    private static final AtomicLong totalSentCnt = new AtomicLong(0);
    private static ExecutorService sendExecutorService;

    /**
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-tubemq/tubemq-example/src/main/java/org/apache/inlong/tubemq/example/ClientBalanceConsumerExample.java`
#### Snippet
```java
            new MsgSendReceiveStats(false);
    private static ClientBalanceConsumer consumer;
    private static MessageSessionFactory messageSessionFactory;
    // 0. Map of partitionKey and last success offset
    // You can persist the information and use it when restarting or
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/MessageQueueClusterProducer.java`
#### Snippet
```java
    private final String workerName;
    private final CacheClusterConfig config;
    private final MessageQueueZoneSinkContext sinkContext;
    private final String cacheClusterName;
    private LifecycleState state;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/http/SimpleHttpSource.java`
#### Snippet
```java
    private volatile Server srv;
    private Map<String, String> subProps;
    private MessageHandler messageHandler;

    // SSL configuration variable
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/MessageQueueZoneSink.java`
#### Snippet
```java
    private ScheduledExecutorService scheduledPool;

    private MessageQueueZoneProducer zoneProducer;

    /**
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/MessageQueueZoneSink.java`
#### Snippet
```java
    // message group
    private BatchPackManager dispatchManager;
    private BufferQueue<BatchPackProfile> dispatchQueue;
    // scheduled thread pool
    // reload
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/MessageQueueZoneSink.java`
#### Snippet
```java
    // reload
    // dispatch
    private ScheduledExecutorService scheduledPool;

    private MessageQueueZoneProducer zoneProducer;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/tube/TubeHandler.java`
#### Snippet
```java

    // parameter
    private String masterHostAndPortList;
    private long linkMaxAllowedDelayedMsgCount;
    private long sessionWarnDelayedMsgCount;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/tube/TubeHandler.java`
#### Snippet
```java

    public static final Logger LOG = LoggerFactory.getLogger(TubeHandler.class);
    private static String MASTER_HOST_PORT_LIST = "master-host-port-list";
    public static final String KEY_NAMESPACE = "namespace";

```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/tube/TubeHandler.java`
#### Snippet
```java
    private long sessionWarnDelayedMsgCount;
    private long sessionMaxAllowedDelayedMsgCount;
    private long nettyWriteBufferHighWaterMark;
    // tube producer
    private TubeMultiSessionFactory sessionFactory;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/tube/TubeHandler.java`
#### Snippet
```java
    private long linkMaxAllowedDelayedMsgCount;
    private long sessionWarnDelayedMsgCount;
    private long sessionMaxAllowedDelayedMsgCount;
    private long nettyWriteBufferHighWaterMark;
    // tube producer
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/tube/TubeHandler.java`
#### Snippet
```java
    // parameter
    private String masterHostAndPortList;
    private long linkMaxAllowedDelayedMsgCount;
    private long sessionWarnDelayedMsgCount;
    private long sessionMaxAllowedDelayedMsgCount;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/tube/TubeHandler.java`
#### Snippet
```java
    private String masterHostAndPortList;
    private long linkMaxAllowedDelayedMsgCount;
    private long sessionWarnDelayedMsgCount;
    private long sessionMaxAllowedDelayedMsgCount;
    private long nettyWriteBufferHighWaterMark;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/config/RemoteConfigManager.java`
#### Snippet
```java

    private long reloadInterval;
    private Timer reloadTimer;

    private CloseableHttpClient httpClient;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/SimpleUdpSource.java`
#### Snippet
```java
    private static int UPD_BUFFER_DEFAULT_SIZE = 8192;

    private Bootstrap bootstrap;

    public SimpleUdpSource() {
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/ServerMessageFactory.java`
#### Snippet
```java
    private static long MAX_TOTAL_MEMORY_SIZE = 1024 * 1024;

    private static int MSG_LENGTH_LEN = 4;

    private BaseSource source;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/SimpleTcpSource.java`
#### Snippet
```java
    protected String topic;

    private ServerBootstrap bootstrap;

    public SimpleTcpSource() {
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/SimpleTcpSource.java`
#### Snippet
```java
    private int highWaterMark;

    private int trafficClass;

    protected String topic;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/SimpleTcpSource.java`
#### Snippet
```java
    private static int TRAFFIC_CLASS_TYPE_0 = 0;

    private static int TRAFFIC_CLASS_TYPE_96 = 96;

    private static int HIGH_WATER_MARK_DEFAULT_VALUE = 64 * 1024;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/SimpleTcpSource.java`
#### Snippet
```java
    private static final Logger logger = LoggerFactory.getLogger(SimpleTcpSource.class);

    private static int TRAFFIC_CLASS_TYPE_0 = 0;

    private static int TRAFFIC_CLASS_TYPE_96 = 96;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/SimpleTcpSource.java`
#### Snippet
```java
    private static int TRAFFIC_CLASS_TYPE_96 = 96;

    private static int HIGH_WATER_MARK_DEFAULT_VALUE = 64 * 1024;

    private boolean tcpNoDelay = true;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/BaseSource.java`
#### Snippet
```java
    private static int STAT_INTERVAL_MUST_THAN = 0;

    private static int PKG_TIMEOUT_DEFAULT_SEC = 3;

    private static int MSG_MIN_LENGTH = 4;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/BaseSource.java`
#### Snippet
```java
    private static int MSG_MIN_LENGTH = 4;

    private static int MAX_MSG_DEFAULT_LENGTH = 1024 * 64;

    private static int INTERVAL_SEC = 60;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/BaseSource.java`
#### Snippet
```java
    protected boolean filterEmptyMsg;

    private int statIntervalSec;

    protected int pkgTimeoutSec;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/BaseSource.java`
#### Snippet
```java
    protected ChannelFuture channelFuture;

    private static String HOST_DEFAULT_VALUE = "0.0.0.0";

    private static int maxMonitorCnt = ConfigConstants.DEF_MONITOR_STAT_CNT;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/BaseSource.java`
#### Snippet
```java
    private static int PKG_TIMEOUT_DEFAULT_SEC = 3;

    private static int MSG_MIN_LENGTH = 4;

    private static int MAX_MSG_DEFAULT_LENGTH = 1024 * 64;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/BaseSource.java`
#### Snippet
```java
    private static int maxMonitorCnt = ConfigConstants.DEF_MONITOR_STAT_CNT;

    private static int DEFAULT_MAX_CONNECTIONS = 5000;

    private static int STAT_INTERVAL_MUST_THAN = 0;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/BaseSource.java`
#### Snippet
```java
    private static int DEFAULT_MAX_CONNECTIONS = 5000;

    private static int STAT_INTERVAL_MUST_THAN = 0;

    private static int PKG_TIMEOUT_DEFAULT_SEC = 3;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/channel/BufferQueueChannel.java`
#### Snippet
```java
    private int maxBufferQueueCount;
    private Semaphore countSemaphore;
    private int maxBufferQueueSizeKb;
    private BufferQueue<ProxyEvent> bufferQueue;
    private ThreadLocal<ProxyTransaction> currentTransaction = new ThreadLocal<ProxyTransaction>();
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source2/SimpleUdpSource.java`
#### Snippet
```java
            .getLogger(SimpleUdpSource.class);

    private Bootstrap bootstrap;

    public SimpleUdpSource() {
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source2/SimpleTcpSource.java`
#### Snippet
```java
    private static final Logger logger = LoggerFactory.getLogger(SimpleTcpSource.class);

    private ServerBootstrap bootstrap;
    private boolean tcpNoDelay;
    private boolean tcpKeepAlive;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/SimpleMessageHandler.java`
#### Snippet
```java
    private final String defaultTopic;
    private String defaultMXAttr = "m=3";
    private final String protocolType;
    private final DataProxyMetricItemSet metricItemSet;

```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source2/v0msg/CodecBinMsg.java`
#### Snippet
```java
    private boolean isOrderOrProxy = false;
    private boolean indexMsg = false;
    private boolean fileCheckMsg = false;
    private boolean needTraceMsg = false;

```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-sort-standalone/sort-standalone-common/src/main/java/org/apache/inlong/sort/standalone/config/holder/SortClusterConfigHolder.java`
#### Snippet
```java

    private long reloadInterval;
    private Timer reloadTimer;
    private SortClusterConfigLoader loader;
    private SortClusterConfig config;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/pulsar/PulsarFederationSink.java`
#### Snippet
```java
    private PulsarFederationSinkContext context;
    private List<PulsarFederationWorker> workers = new ArrayList<>();
    private Map<String, String> dimensions;

    /**
```

### FieldCanBeLocal
Field can be converted to a local variable
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/elasticsearch/EsSink.java`
#### Snippet
```java

    private Context parentContext;
    private BufferQueue<EsIndexRequest> dispatchQueue;
    private EsSinkContext context;
    // workers
```

## RuleId[id=DefaultAnnotationParam]
### DefaultAnnotationParam
Redundant default parameter value assignment
in `inlong-manager/manager-web/src/main/java/org/apache/inlong/manager/web/controller/InlongClusterController.java`
#### Snippet
```java
            @ApiImplicitParam(name = "inlongGroupId", dataTypeClass = String.class, required = true),
            @ApiImplicitParam(name = "clusterType", dataTypeClass = String.class, required = true),
            @ApiImplicitParam(name = "protocolType", dataTypeClass = String.class, required = false)
    })
    @OperationLog(operation = OperationType.GET)
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `inlong-manager/manager-web/src/main/java/org/apache/inlong/manager/web/controller/openapi/OpenInLongClusterController.java`
#### Snippet
```java
            @ApiImplicitParam(name = "inlongGroupId", dataTypeClass = String.class, required = true),
            @ApiImplicitParam(name = "clusterType", dataTypeClass = String.class, required = true),
            @ApiImplicitParam(name = "protocolType", dataTypeClass = String.class, required = false)
    })
    @OperationLog(operation = OperationType.GET)
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/plugin/PluginService.java`
#### Snippet
```java
@Service
@Slf4j
@Order(Ordered.LOWEST_PRECEDENCE)
public class PluginService implements InitializingBean {

```

## RuleId[id=MalformedFormatString]
### MalformedFormatString
Too many arguments for format string (found: 1, expected: 0)
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/iceberg/src/main/java/org/apache/inlong/sort/iceberg/sink/multiple/IcebergSingleStreamWriter.java`
#### Snippet
```java
            this.metricStateListState = context.getOperatorStateStore().getUnionListState(
                    new ListStateDescriptor<>(
                            String.format(INLONG_METRIC_STATE_NAME, fullTableName),
                            TypeInformation.of(new TypeHint<MetricState>() {
                            })));
```

## RuleId[id=ThrowFromFinallyBlock]
### ThrowFromFinallyBlock
`throw` inside 'finally' block
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/mysql/MySqlValidator.java`
#### Snippet
```java
                    connection.close();
                } catch (SQLException e) {
                    throw new FlinkRuntimeException("Closing connection error", e);
                }
            }
```

## RuleId[id=IfStatementWithIdenticalBranches]
### IfStatementWithIdenticalBranches
Common parts with variables can be extracted from 'if'
in `inlong-sdk/sort-sdk/src/main/java/org/apache/inlong/sdk/sort/api/SortClientConfig.java`
#### Snippet
```java
        for (InLongTopic topic : totalTopics) {
            int topicHashId = 0;
            if (this.consumerSubsetType == ConsumerSubsetType.CLUSTER) {
                String hashString = topic.getInLongCluster().getClusterId();
                topicHashId = Math.abs(hashString.hashCode()) % this.consumerSubsetSize;
```

### IfStatementWithIdenticalBranches
Common part can be extracted from 'if'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/iceberg/src/main/java/org/apache/inlong/sort/iceberg/sink/multiple/SchemaChangeUtils.java`
#### Snippet
```java
        List<TableChange> tableChanges = new ArrayList<>();
        while (ni < newFields.size()) {
            if (oi < oldFields.size() && oldFields.get(oi).equals(newFields.get(ni))) {
                oi++;
                ni++;
```

### IfStatementWithIdenticalBranches
Common part can be extracted from 'if'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/elasticsearch-base/src/main/java/org/apache/inlong/sort/elasticsearch/table/ElasticsearchSinkFunctionBase.java`
#### Snippet
```java
            case INSERT:
            case UPDATE_AFTER:
                if (key != null) {
                    request = requestFactory.createUpdateRequest(index, docType, key, contentType, document);
                    if (addRouting(request, element, document)) {
```

### IfStatementWithIdenticalBranches
Common part can be extracted from 'if'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/elasticsearch-base/src/main/java/org/apache/inlong/sort/elasticsearch/table/MultipleElasticsearchSinkFunctionBase.java`
#### Snippet
```java
            case INSERT:
            case UPDATE_AFTER:
                if (key != null) {
                    request = requestFactory.createUpdateRequest(index, docType, key, contentType, document);
                    if (addRouting(request, element, document)) {
```

### IfStatementWithIdenticalBranches
Common part can be extracted removing branch
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/TubeSink.java`
#### Snippet
```java

    private MessageProducer getProducer(String topic) throws TubeClientException {
        if (producerMap.containsKey(topic)) {
            return producerMap.get(topic);
        } else {
```

### IfStatementWithIdenticalBranches
Common part can be extracted from 'if'
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/SimpleClientBalanceConsumer.java`
#### Snippet
```java
                                .consumerCommitC2B(createBrokerCommitRequest(curPartition, isConsumed),
                                        AddressUtils.getLocalAddress(), getConsumerConfig().isTlsEnable());
                if (response == null) {
                    result.setFailResult(TErrCodeConstants.CONNECT_RETURN_NULL,
                            sBuilder.append("Confirm ").append(confirmContext)
```

### IfStatementWithIdenticalBranches
Common part can be extracted from 'if'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/BrokerServiceServer.java`
#### Snippet
```java
                    storeManager.getMessages(dataStore, topicName, partitionId, msgCount, filterCondSet);
            BrokerSrvStatsHolder.updGetMsgLatency(System.currentTimeMillis() - startTime);
            if ((getMessageResult.transferedMessageList == null)
                    || (getMessageResult.transferedMessageList.isEmpty())) {
                sb.append("{\"result\":false,\"errCode\":401,\"errMsg\":\"")
```

### IfStatementWithIdenticalBranches
Common part can be extracted from 'if'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/web/BrokerAdminServlet.java`
#### Snippet
```java
        OffsetService offsetService = broker.getOffsetManager();
        sBuffer.append("{\"result\":true,\"errCode\":0,\"errMsg\":\"Success!\",\"dataSet\":[");
        if (withDivide) {
            // query in-memory group name set
            Set<String> onlineGroups = offsetService.getInMemoryGroups();
```

### IfStatementWithIdenticalBranches
'if' statement can be collapsed
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/aaaserver/SimpleCertificateBrokerHandler.java`
#### Snippet
```java
    public boolean validConsumeAuthorizeInfo(String userName, String groupName, String topicName,
            Set<String> msgTypeLst, boolean isRegister, String clientIp, ProcessResult result) {
        if (!enableConsumeAuthorize) {
            result.setSuccResult(new CertifiedInfo());
            return result.isSuccess();
```

### IfStatementWithIdenticalBranches
'if' statement can be collapsed
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/aaaserver/SimpleCertificateBrokerHandler.java`
#### Snippet
```java
    public boolean validProduceAuthorizeInfo(String userName, String topicName,
            String msgType, String clientIp, ProcessResult result) {
        if (!enableProduceAuthorize) {
            result.setSuccResult(new CertifiedInfo());
            return result.isSuccess();
```

### IfStatementWithIdenticalBranches
'if' statement can be collapsed
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/aaaserver/SimpleCertificateMasterHandler.java`
#### Snippet
```java
    public boolean validProducerAuthorizeInfo(String userName, Set<String> topics,
            String clientIp, ProcessResult result) {
        if (!masterConfig.isStartProduceAuthorize()) {
            result.setSuccResult(new CertifiedInfo());
            return result.isSuccess();
```

### IfStatementWithIdenticalBranches
'if' statement can be collapsed
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/aaaserver/SimpleCertificateMasterHandler.java`
#### Snippet
```java
    public boolean validConsumerAuthorizeInfo(String userName, String groupName, Set<String> topics,
            Map<String, TreeSet<String>> topicConds, String clientIp, ProcessResult result) {
        if (!masterConfig.isStartProduceAuthorize()) {
            result.setSuccResult(new CertifiedInfo());
            return result.isSuccess();
```

### IfStatementWithIdenticalBranches
Common part can be extracted from 'if'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebBrokerConfHandler.java`
#### Snippet
```java
            }
            ManageStatus mngStatus = (ManageStatus) result.getRetData();
            if (isAddOp) {
                // get brokerIp and brokerId field
                if (!getBrokerIpAndIdParamValue(brokerObject, sBuffer, result)) {
```

### IfStatementWithIdenticalBranches
Common part can be extracted from 'if'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/balance/DefaultLoadBalancer.java`
#### Snippet
```java
                // calculate if current consumer partition need to release or add
                for (List<Partition> entry : partitions.values()) {
                    if (entry.size() > numToOffload) {
                        int condition = numToOffload;
                        for (int i = 0; i < condition; i++) {
```

### IfStatementWithIdenticalBranches
Common parts with variables can be extracted from 'if'
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/channel/BufferQueueChannel.java`
#### Snippet
```java
        ProfileTransaction transaction = currentTransaction.get();
        Preconditions.checkState(transaction != null, "No transaction exists for this thread");
        if (event instanceof ProfileEvent) {
            ProfileEvent profile = (ProfileEvent) event;
            transaction.doPut(profile);
```

## RuleId[id=SlowListContainsAll]
### SlowListContainsAll
Call to 'list.containsAll(collection)' may have poor performance
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/BrokerServiceImpl.java`
#### Snippet
```java
        List<Long> regionBrokerIdList = brokerEntries.stream().map(BrokerEntry::getBrokerId).collect(
                Collectors.toList());
        return regionBrokerIdList.containsAll(brokerIdList);
    }

```

## RuleId[id=InfiniteLoopStatement]
### InfiniteLoopStatement
`while` statement cannot complete without throwing an exception
in `inlong-sdk/sort-sdk/src/main/java/org/apache/inlong/sdk/sort/fetcher/kafka/KafkaSingleTopicFetcher.java`
#### Snippet
```java
        public void run() {
            boolean hasPermit;
            while (true) {
                hasPermit = false;
                try {
```

### InfiniteLoopStatement
`while` statement cannot complete without throwing an exception
in `inlong-sdk/sort-sdk/src/main/java/org/apache/inlong/sdk/sort/fetcher/kafka/KafkaMultiTopicsFetcher.java`
#### Snippet
```java
        public void run() {
            boolean hasPermit;
            while (true) {
                hasPermit = false;
                try {
```

### InfiniteLoopStatement
`while` statement cannot complete without throwing an exception
in `inlong-agent/agent-core/src/main/java/org/apache/inlong/agent/core/AgentManager.java`
#### Snippet
```java
            @Override
            public void run() {
                while (true) {
                    try {
                        Thread.sleep(10 * 1000); // 10s check
```

### InfiniteLoopStatement
`while` statement cannot complete without throwing an exception
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/trigger/DirectoryTrigger.java`
#### Snippet
```java
        public void run() {
            Thread.currentThread().setName("Directory watch checker");
            while (true) {
                try {
                    TimeUnit.SECONDS.sleep(AgentConstants.DEFAULT_TRIGGER_CHECK_INTERVAL);
```

### InfiniteLoopStatement
`while` statement cannot complete without throwing an exception
in `inlong-audit/audit-store/src/main/java/org/apache/inlong/audit/service/consume/KafkaConsume.java`
#### Snippet
```java
        @Override
        public void run() {
            while (true) {
                try {
                    // Set the waiting time of the consumer to 100ms
```

## RuleId[id=Lombok]
### Lombok
Class contains required fields, you have to force NoArgsConstructor.
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/load/IcebergLoadNode.java`
#### Snippet
```java
@JsonTypeName("icebergLoad")
@Data
@NoArgsConstructor
@EqualsAndHashCode(callSuper = true)
public class IcebergLoadNode extends LoadNode implements InlongMetric, Serializable {
```

### Lombok
Class contains required fields, you have to force NoArgsConstructor.
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/load/KuduLoadNode.java`
#### Snippet
```java
@JsonTypeName("kuduLoad")
@Data
@NoArgsConstructor
@EqualsAndHashCode(callSuper = true)
public class KuduLoadNode extends LoadNode implements InlongMetric, Serializable {
```

### Lombok
Class contains required fields, you have to force NoArgsConstructor.
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/load/DorisLoadNode.java`
#### Snippet
```java
@JsonInclude(Include.NON_NULL)
@Data
@NoArgsConstructor
public class DorisLoadNode extends LoadNode implements InlongMetric, Serializable {

```

### Lombok
Class contains required fields, you have to force NoArgsConstructor.
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/load/ClickHouseLoadNode.java`
#### Snippet
```java
@JsonTypeName("clickHouseLoad")
@Data
@NoArgsConstructor
public class ClickHouseLoadNode extends LoadNode implements InlongMetric, Serializable {

```

### Lombok
Class contains required fields, you have to force NoArgsConstructor.
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/load/ElasticsearchLoadNode.java`
#### Snippet
```java
@JsonTypeName("elasticsearchLoadNode")
@Data
@NoArgsConstructor
public class ElasticsearchLoadNode extends LoadNode implements InlongMetric, Serializable {

```

### Lombok
Class contains required fields, you have to force NoArgsConstructor.
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/load/StarRocksLoadNode.java`
#### Snippet
```java
@JsonInclude(Include.NON_NULL)
@Data
@NoArgsConstructor
public class StarRocksLoadNode extends LoadNode implements InlongMetric, Serializable {

```

### Lombok
Class contains required fields, you have to force NoArgsConstructor.
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/load/HiveLoadNode.java`
#### Snippet
```java
@JsonTypeName("hiveLoad")
@Data
@NoArgsConstructor
public class HiveLoadNode extends LoadNode implements InlongMetric, Serializable {

```

### Lombok
Class contains required fields, you have to force NoArgsConstructor.
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/load/FileSystemLoadNode.java`
#### Snippet
```java
@JsonTypeName("fileSystemLoad")
@Data
@NoArgsConstructor
public class FileSystemLoadNode extends LoadNode implements InlongMetric, Serializable {

```

### Lombok
Class contains required fields, you have to force NoArgsConstructor.
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/load/HudiLoadNode.java`
#### Snippet
```java
@JsonTypeName("hudiLoad")
@Data
@NoArgsConstructor
@EqualsAndHashCode(callSuper = true)
public class HudiLoadNode extends LoadNode implements InlongMetric, Serializable {
```

### Lombok
Class contains required fields, you have to force NoArgsConstructor.
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/node/load/KafkaLoadNode.java`
#### Snippet
```java
@JsonInclude(Include.NON_NULL)
@Data
@NoArgsConstructor
public class KafkaLoadNode extends LoadNode implements InlongMetric, Metadata, Serializable {

```

### Lombok
Generating equals/hashCode implementation but without a call to superclass, even though this class does not extend java.lang.Object. If this is intentional, add '(callSuper=false)' to your type.
in `inlong-manager/manager-pojo/src/main/java/org/apache/inlong/manager/pojo/sink/ck/ClickHouseFieldInfo.java`
#### Snippet
```java
 * ClickHouse field info.
 */
@Data
@NoArgsConstructor
@AllArgsConstructor
```

### Lombok
Generating equals/hashCode implementation but without a call to superclass, even though this class does not extend java.lang.Object. If this is intentional, add '(callSuper=false)' to your type.
in `inlong-manager/manager-pojo/src/main/java/org/apache/inlong/manager/pojo/sink/es/ElasticsearchFieldInfo.java`
#### Snippet
```java
import javax.validation.constraints.NotNull;

@Data
@NoArgsConstructor
@AllArgsConstructor
```

### Lombok
Generating equals/hashCode implementation but without a call to superclass, even though this class does not extend java.lang.Object. If this is intentional, add '(callSuper=false)' to your type.
in `inlong-manager/manager-pojo/src/main/java/org/apache/inlong/manager/pojo/sink/kudu/KuduColumnInfo.java`
#### Snippet
```java
 * Kudu column info
 */
@Data
@NoArgsConstructor
@AllArgsConstructor
```

### Lombok
Generating equals/hashCode implementation but without a call to superclass, even though this class does not extend java.lang.Object. If this is intentional, add '(callSuper=false)' to your type.
in `inlong-manager/manager-pojo/src/main/java/org/apache/inlong/manager/pojo/sink/starrocks/StarRocksColumnInfo.java`
#### Snippet
```java
 * StarRocks column info.
 */
@Data
@NoArgsConstructor
@AllArgsConstructor
```

### Lombok
Generating equals/hashCode implementation but without a call to superclass, even though this class does not extend java.lang.Object. If this is intentional, add '(callSuper=false)' to your type.
in `inlong-manager/manager-pojo/src/main/java/org/apache/inlong/manager/pojo/group/kafka/InlongKafkaDTO.java`
#### Snippet
```java
 * Inlong group info for Kafka
 */
@Data
@Builder
@NoArgsConstructor
```

### Lombok
Generating equals/hashCode implementation but without a call to superclass, even though this class does not extend java.lang.Object. If this is intentional, add '(callSuper=false)' to your type.
in `inlong-manager/manager-pojo/src/main/java/org/apache/inlong/manager/pojo/group/tubemq/InlongTubeMQDTO.java`
#### Snippet
```java
 * Inlong group info for TubeMQ
 */
@Data
@NoArgsConstructor
@ApiModel("Inlong group info for TubeMQ")
```

### Lombok
Generating equals/hashCode implementation but without a call to superclass, even though this class does not extend java.lang.Object. If this is intentional, add '(callSuper=false)' to your type.
in `inlong-manager/manager-pojo/src/main/java/org/apache/inlong/manager/pojo/consume/kafka/ConsumeKafkaDTO.java`
#### Snippet
```java
 * Inlong group info of Kafka
 */
@Data
@Builder
@AllArgsConstructor
```

### Lombok
Generating equals/hashCode implementation but without a call to superclass, even though this class does not extend java.lang.Object. If this is intentional, add '(callSuper=false)' to your type.
in `inlong-manager/manager-pojo/src/main/java/org/apache/inlong/manager/pojo/consume/pulsar/ConsumePulsarDTO.java`
#### Snippet
```java
 * Inlong group dto of Pulsar
 */
@Data
@Builder
@NoArgsConstructor
```

### Lombok
Generating equals/hashCode implementation but without a call to superclass, even though this class does not extend java.lang.Object. If this is intentional, add '(callSuper=false)' to your type.
in `inlong-manager/manager-pojo/src/main/java/org/apache/inlong/manager/pojo/consume/tubemq/ConsumeTubeMQDTO.java`
#### Snippet
```java
 * Inlong group info of TubeMQ
 */
@Data
@Builder
@AllArgsConstructor
```

### Lombok
Generating equals/hashCode implementation but without a call to superclass, even though this class does not extend java.lang.Object. If this is intentional, add '(callSuper=false)' to your type.
in `inlong-manager/manager-pojo/src/main/java/org/apache/inlong/manager/pojo/heartbeat/HeartbeatReportRequest.java`
#### Snippet
```java
 * Heartbeat report request
 */
@Data
@ApiModel("Request of heartbeat report")
public class HeartbeatReportRequest extends HeartbeatMsg {
```

## RuleId[id=ConditionalExpressionWithIdenticalBranches]
### ConditionalExpressionWithIdenticalBranches
Conditional expression `isLongName ? "serialId" : "serialId"` with identical branches
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/metamanage/metastore/dao/entity/BaseEntity.java`
#### Snippet
```java
        }
        if (serialId.get() != TBaseConstants.META_VALUE_UNDEFINED) {
            paramMap.put((isLongName ? "serialId" : "serialId"),
                    String.valueOf(serialId.get()));
        }
```

## RuleId[id=PointlessNullCheck]
### PointlessNullCheck
Unnecessary 'null' check before 'equalsIgnoreCase()' call
in `inlong-sort/sort-flink/base/src/main/java/org/apache/inlong/sort/base/format/JsonToRowDataConverters.java`
#### Snippet
```java
            String boolStr = jsonNode.asText().trim();
            // Compatible with tinyint data and bool conversion
            return (boolStr != null) && ("true".equalsIgnoreCase(boolStr) || "1".equalsIgnoreCase(boolStr));
        }
    }
```

### PointlessNullCheck
Unnecessary 'null' check before 'isNotBlank()' call
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/trigger/PathPattern.java`
#### Snippet
```java
        this.subDirs = new HashSet<>();
        this.blackList = blackList;
        if (offset != null && StringUtils.isNotBlank(offset)) {
            this.whiteList = whiteList.stream()
                    .map(whiteRegex -> DateFormatRegex.ofRegex(whiteRegex).withOffset(offset))
```

### PointlessNullCheck
Unnecessary 'null' check before 'equals()' call
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/service/tube/TubeHttpBrokerInfoList.java`
#### Snippet
```java

        private boolean isIdle() {
            return subStatus != null && IDLE.equals(subStatus);
        }

```

## RuleId[id=InfiniteRecursion]
### InfiniteRecursion
Method `deserialize()` recurses infinitely, and can only end by throwing an exception
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/oracle-cdc/src/main/java/org/apache/inlong/sort/cdc/oracle/debezium/table/RowDataDebeziumDeserializeSchema.java`
#### Snippet
```java

    @Override
    public void deserialize(SourceRecord record, Collector<RowData> out) throws Exception {
        deserialize(record, out);
    }
```

## RuleId[id=NullArgumentToVariableArgMethod]
### NullArgumentToVariableArgMethod
Confusing argument `new String[]{inLongTopic.getInLongCluster().getClusterId(), inLo...`, unclear if a varargs or non-varargs call is desired
in `inlong-sdk/sort-sdk/src/main/java/org/apache/inlong/sdk/sort/manager/InlongSingleTopicManager.java`
#### Snippet
```java
                    tubeFactories.put(inLongTopic.getInLongCluster().getClusterId(), tubeConsumerCreator);
                    LOGGER.debug("create tube client succ {} {} {}",
                            new String[]{inLongTopic.getInLongCluster().getClusterId(),
                                    inLongTopic.getInLongCluster().getBootstraps(),
                                    inLongTopic.getInLongCluster().getToken()});
                } catch (Exception e) {
                    LOGGER.error("create tube client error {}", inLongTopic);
```

### NullArgumentToVariableArgMethod
Confusing argument `new String[]{topic.getInLongCluster().getClusterId(), topic.getI...`, unclear if a varargs or non-varargs call is desired
in `inlong-sdk/sort-sdk/src/main/java/org/apache/inlong/sdk/sort/manager/InlongSingleTopicManager.java`
#### Snippet
```java
                    pulsarClients.put(topic.getInLongCluster().getClusterId(), pulsarClient);
                    LOGGER.debug("create pulsar client succ {}",
                            new String[]{topic.getInLongCluster().getClusterId(),
                                    topic.getInLongCluster().getBootstraps(),
                                    topic.getInLongCluster().getToken()});
                } catch (Exception e) {
                    LOGGER.error("create pulsar client error {}", topic);
```

## RuleId[id=ConstantValue]
### ConstantValue
Condition `httpPost != null` is always `false`
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/config/FileProxyClusterConfigLoader.java`
#### Snippet
```java
            return null;
        } finally {
            if (httpPost != null) {
                httpPost.releaseConnection();
            }
```

### ConstantValue
Condition `!(o instanceof IpPort)` is always `false`
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/network/IpPort.java`
#### Snippet
```java
        }

        if (!(o instanceof IpPort)) {
            return false;
        }
```

### ConstantValue
Condition `oldChannel != null` is always `true`
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/network/TcpChannelGroup.java`
#### Snippet
```java
                tcpChannel.setChannel(newChannel);
                newChannel.setAttachment(oldChannel.getAttachment());
                if (oldChannel != null) {
                    oldChannel.disconnect();
                    oldChannel.close();
```

### ConstantValue
Value `jb` is always 'null'
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/utils/ServiceDiscoveryUtils.java`
#### Snippet
```java
                if (jb == null) {
                    log.warn("ServiceDiscovery updated manager ip failed, returnStr = {} jb is "
                            + "null ", returnStr, jb);
                    return null;
                }
```

### ConstantValue
Condition `count < sentCount` is always `true`
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/example/UdpClientExample.java`
#### Snippet
```java
        try {
            int count = 0;
            while (count < sentCount) {
                if (count % 1000 == 0) {
                    long seqId = idGenerator.getNextInt();
```

### ConstantValue
Condition `count % 1000 == 0` is always `true`
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/example/UdpClientExample.java`
#### Snippet
```java
            int count = 0;
            while (count < sentCount) {
                if (count % 1000 == 0) {
                    long seqId = idGenerator.getNextInt();
                    long dt = System.currentTimeMillis() / 1000;
```

### ConstantValue
Value `pattern` is always 'null'
in `inlong-sort/sort-flink/base/src/main/java/org/apache/inlong/sort/base/util/PatternReplaceUtils.java`
#### Snippet
```java
    public static String replace(String pattern, Map<String, String> params) {
        if (pattern == null) {
            return pattern;
        }
        Matcher matcher = REGEX_PATTERN.matcher(pattern);
```

### ConstantValue
Condition `boolStr != null` is always `true`
in `inlong-sort/sort-flink/base/src/main/java/org/apache/inlong/sort/base/format/JsonToRowDataConverters.java`
#### Snippet
```java
            String boolStr = jsonNode.asText().trim();
            // Compatible with tinyint data and bool conversion
            return (boolStr != null) && ("true".equalsIgnoreCase(boolStr) || "1".equalsIgnoreCase(boolStr));
        }
    }
```

### ConstantValue
Condition `s <= 0` is always `true`
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/util/RecordUtils.java`
#### Snippet
```java
                return new DecimalType(p, s);
            }
            if (s <= 0 && p - s > 38) {
                return new VarCharType(Integer.MAX_VALUE);
            }
```

### ConstantValue
Condition `null != tableException` is always `true` when reached
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/jdbc/src/main/java/org/apache/inlong/sort/jdbc/internal/JdbcMultiBatchingOutputFormat.java`
#### Snippet
```java
                        }
                    }
                    if (!flushFlag && null != tableException) {
                        LOG.info("Put tableIdentifier:{} exception:{}",
                                tableIdentifier, tableException.getMessage());
```

### ConstantValue
Value `specificOffset` is always 'null'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/oracle-cdc/src/main/java/org/apache/inlong/sort/cdc/oracle/OracleSource.java`
#### Snippet
```java

            return new DebeziumSourceFunction<>(
                    deserializer, props, specificOffset, new OracleValidator(props),
                    inlongMetric, inlongAudit, sourceMultipleEnable);
        }
```

### ConstantValue
Value `pkColumnDoesNotExists` is always 'false'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/postgres-cdc/src/main/java/org/apache/inlong/sort/cdc/postgres/debezium/internal/TableEditorImpl.java`
#### Snippet
```java
                                    + "but a matching column is not defined in table \"" + tableId() + "\"!");
                }
                return pkColumnDoesNotExists;
            });
        }
```

### ConstantValue
Value `key` is always 'null'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/elasticsearch-base/src/main/java/org/apache/inlong/sort/elasticsearch/table/ElasticsearchSinkFunctionBase.java`
#### Snippet
```java
                    }
                } else {
                    request = requestFactory.createIndexRequest(index, docType, key, contentType, document);
                    if (addRouting(request, element, document)) {
                        indexer.add(request);
```

### ConstantValue
Value `key` is always 'null'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/elasticsearch-base/src/main/java/org/apache/inlong/sort/elasticsearch/table/MultipleElasticsearchSinkFunctionBase.java`
#### Snippet
```java
                    }
                } else {
                    request = requestFactory.createIndexRequest(index, docType, key, contentType, document);
                    if (addRouting(request, element, document)) {
                        indexer.add(request);
```

### ConstantValue
Value `profile` is always 'null'
in `inlong-agent/agent-common/src/main/java/org/apache/inlong/agent/pojo/DbCollectorTaskResult.java`
#### Snippet
```java
        JobProfile profile = null;
        if (data == null) {
            return profile;
        }
        profile = SqlJobProfileDto.convertToJobProfile(data);
```

### ConstantValue
Condition `historyWriter != null` is always `true`
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/utils/InLongDatabaseHistory.java`
#### Snippet
```java
                                    }
                                } catch (Throwable var8) {
                                    if (historyWriter != null) {
                                        try {
                                            historyWriter.close();
```

### ConstantValue
Condition `historyWriter != null` is always `true`
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/utils/InLongDatabaseHistory.java`
#### Snippet
```java
                                }

                                if (historyWriter != null) {
                                    historyWriter.close();
                                }
```

### ConstantValue
Condition `historyWriter != null` is always `true`
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/utils/InLongDatabaseHistory.java`
#### Snippet
```java
                            }

                            if (historyWriter != null) {
                                historyWriter.close();
                            }
```

### ConstantValue
Condition `Objects.nonNull(topicList)` is always `true`
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/sources/MqttSource.java`
#### Snippet
```java
        final List<Reader> result = new ArrayList<>();
        String[] topicList = topics.split(CommonConstants.COMMA);
        if (Objects.nonNull(topicList)) {
            Arrays.stream(topicList).forEach(topic -> {
                MqttReader mqttReader = new MqttReader(topic);
```

### ConstantValue
Condition `iterator != null` is always `true`
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/sources/reader/KafkaReader.java`
#### Snippet
```java
        ConsumerRecords<K, V> records = consumer.poll(Duration.ofMillis(fetchDataTimeout));
        iterator = records.iterator();
        return iterator != null ? true : false;
    }

```

### ConstantValue
Condition `!(o instanceof IpPort)` is always `false`
in `inlong-audit/audit-sdk/src/main/java/org/apache/inlong/audit/util/IpPort.java`
#### Snippet
```java
        }

        if (!(o instanceof IpPort)) {
            return false;
        }
```

### ConstantValue
Condition `ip != null && ip.equals(ctp.port) && port == ctp.port` is always `false`
in `inlong-audit/audit-sdk/src/main/java/org/apache/inlong/audit/util/IpPort.java`
#### Snippet
```java
        try {
            IpPort ctp = (IpPort) o;
            if (ip != null && ip.equals(ctp.port) && port == ctp.port) {
                return true;
            }
```

### ConstantValue
Condition `ip.equals(ctp.port)` is always `false`
in `inlong-audit/audit-sdk/src/main/java/org/apache/inlong/audit/util/IpPort.java`
#### Snippet
```java
        try {
            IpPort ctp = (IpPort) o;
            if (ip != null && ip.equals(ctp.port) && port == ctp.port) {
                return true;
            }
```

### ConstantValue
Value `decrementFlag` is always 'false'
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/PulsarSink.java`
#### Snippet
```java
                        }
                    }
                    resendEvent(eventStat, decrementFlag);
                }
            }
```

### ConstantValue
Value `decrementFlag` is always 'false'
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/KafkaSink.java`
#### Snippet
```java
                        }
                    }
                    resendEvent(eventStat, decrementFlag);
                }
            }
```

### ConstantValue
Value `decrementFlag` is always 'false'
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/TubeSink.java`
#### Snippet
```java
                        }
                    }
                    resendEvent(es, decrementFlag);
                }
            }
```

### ConstantValue
Condition `!maskedThisCharacter` is always `true`
in `inlong-common/src/main/java/org/apache/inlong/common/util/MaskDataUtils.java`
#### Snippet
```java
                maskedThisCharacter = false;
            }
            if (!maskedThisCharacter) {
                while (pos < length
                        && !(Character.isWhitespace(stringBuilder.charAt(pos))
```

### ConstantValue
Condition `listenerObject == null` is always `false`
in `inlong-common/src/main/java/org/apache/inlong/common/metric/MetricObserver.java`
#### Snippet
```java
                Class<?> listenerClass = ClassUtils.getClass(listenerType);
                Object listenerObject = listenerClass.getDeclaredConstructor().newInstance();
                if (listenerObject == null || !(listenerObject instanceof MetricListener)) {
                    LOG.error("{} is not instance of MetricListener.", listenerType);
                    continue;
```

### ConstantValue
Condition `StandbyException.class.isAssignableFrom(remote.getClass())` is always `false` when reached
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/RpcServiceFailoverInvoker.java`
#### Snippet
```java
                                            .append(responseWrapper.getStackTrace()).toString());
                            if ((IOException.class.isAssignableFrom(remote.getClass()))
                                    || (StandbyException.class.isAssignableFrom(remote.getClass()))) {
                                if (currentCounter == retryCounter.get()) {
                                    getNextClient(true);
```

### ConstantValue
Condition `clazz != null` is always `true`
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/utils/MixUtils.java`
#### Snippet
```java
                if (!TStringUtils.isBlank(strExceptionMsgSet[0])) {
                    Class clazz = Class.forName(strExceptionMsgSet[0]);
                    if (clazz != null) {
                        Constructor<?> ctor = clazz.getConstructor(String.class);
                        if (ctor != null) {
```

### ConstantValue
Condition `ctor != null` is always `true`
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/utils/MixUtils.java`
#### Snippet
```java
                    if (clazz != null) {
                        Constructor<?> ctor = clazz.getConstructor(String.class);
                        if (ctor != null) {
                            if (strExceptionMsgSet.length == 1) {
                                return (Throwable) ctor.newInstance();
```

### ConstantValue
Result of `flag & 0xFFFFFFFE` is always '0'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/utils/MessageFlagUtils.java`
#### Snippet
```java
        int flag = 0;
        if (message != null && message.getAttribute() != null) {
            flag = flag & 0xFFFFFFFE | 1;
        }
        return flag;
```

### ConstantValue
Value `strRemoteIP` is always 'null'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/utils/AddressUtils.java`
#### Snippet
```java
        String strRemoteIP = null;
        if (channel == null) {
            return strRemoteIP;
        }
        SocketAddress remoteSocketAddress = channel.remoteAddress();
```

### ConstantValue
Condition `curSegment != null` is always `true`
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/tools/StoreRepairAdmin.java`
#### Snippet
```java
                    logger.error("Create Index file error ", ee);
                } finally {
                    if (curSegment != null) {
                        curSegment.relViewRef();
                    }
```

### ConstantValue
Condition is always false
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/disk/FileSegment.java`
#### Snippet
```java
                }
                next = itemNext;
            } while (false);
            if (next >= 0) {
                validBytes = next;
```

### ConstantValue
Condition is always false
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/disk/FileSegment.java`
#### Snippet
```java
                }
                next = itemNext;
            } while (false);
            if (next >= 0) {
                validBytes = next;
```

### ConstantValue
Condition `ee instanceof IOException` is always `false`
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/disk/FileSegment.java`
#### Snippet
```java
            this.file.delete();
        } catch (Throwable ee) {
            if (ee instanceof IOException) {
                ServiceStatusHolder.addReadIOErrCnt();
                BrokerSrvStatsHolder.incDiskIOExcCnt();
```

### ConstantValue
Value `createParents` is always 'true'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/zookeeper/ZKUtil.java`
#### Snippet
```java
            }
            String parentDir = path.substring(0, path.lastIndexOf('/'));
            createPersistent(zkw, parentDir, createParents);
            createPersistent(zkw, path, createParents);
        }
```

### ConstantValue
Value `createParents` is always 'true'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/zookeeper/ZKUtil.java`
#### Snippet
```java
            String parentDir = path.substring(0, path.lastIndexOf('/'));
            createPersistent(zkw, parentDir, createParents);
            createPersistent(zkw, path, createParents);
        }
    }
```

### ConstantValue
Value `createUsr` is always 'null'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/utils/WebParameterUtils.java`
#### Snippet
```java
            if (!WebParameterUtils.getStringParamValue(paramCntr, WebFieldDef.CREATEUSER,
                    defOpEntity == null,
                    (defOpEntity == null ? createUsr : defOpEntity.getCreateUser()),
                    sBuffer, result)) {
                return result.isSuccess();
```

### ConstantValue
Value `isAddOp` is always 'true'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebBrokerConfHandler.java`
#### Snippet
```java
                mngStatus = ManageStatus.STATUS_MANAGE_APPLY;
            }
            retInfo.add(defMetaDataService.addOrUpdBrokerConfig(isAddOp, opEntity,
                    brokerIdAndIpTuple.getF0(), brokerIdAndIpTuple.getF1(), brokerPort,
                    brokerTlsPort, brokerWebPort, regionId, groupId,
```

### ConstantValue
Value `isAddOp` is always 'false'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebBrokerConfHandler.java`
#### Snippet
```java
            Set<Integer> brokerIdSet = (Set<Integer>) result.getRetData();
            for (Integer brokerId : brokerIdSet) {
                retInfo.add(defMetaDataService.addOrUpdBrokerConfig(isAddOp, opEntity,
                        brokerId, "", brokerPort, brokerTlsPort, brokerWebPort,
                        regionId, groupId, mngStatus, brokerProps, sBuffer, result));
```

### ConstantValue
Value `consumerGroupName` is always 'null'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/bdbstore/bdbentitys/BdbBlackGroupEntity.java`
#### Snippet
```java
            Date createDate) {
        this.recordKey = new StringBuilder(512).append(topicName)
                .append(TokenConstants.ATTR_SEP).append(consumerGroupName).toString();
        this.topicName = topicName;
        this.consumerGroupName = groupName;
```

### ConstantValue
Value `outClientConfig` is always 'null'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/TMaster.java`
#### Snippet
```java
        ClientMaster.ApprovedClientConfig.Builder outClientConfig = null;
        if (inClientConfig == null) {
            return outClientConfig;
        }
        outClientConfig = ClientMaster.ApprovedClientConfig.newBuilder();
```

### ConstantValue
Value `printHeader` is always 'false'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/TMaster.java`
#### Snippet
```java
        }
        sBuffer.append(type).append(info).append(", ");
        return printHeader;
    }

```

### ConstantValue
Value `enableAuthCtrl` is always 'true'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebTopicDeployHandler.java`
#### Snippet
```java
                    .append(",\"authData\":{");
            if (enableAuthCtrl) {
                sBuffer.append("\"enableAuthControl\":").append(enableAuthCtrl)
                        .append(",\"createUser\":\"").append(ctrlEntity.getModifyUser())
                        .append("\",\"createDate\":\"").append(ctrlEntity.getModifyDateStr())
```

### ConstantValue
Value `isAddOp` is always 'true'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/metamanage/DefaultMetaDataService.java`
#### Snippet
```java
        if (isAddOp) {
            for (TopicCtrlEntity entity : entityMap.values()) {
                metaConfigMapper.addOrUpdTopicCtrlConf(isAddOp, entity, strBuff, result);
                retInfo.add(new TopicProcessResult(0, entity.getTopicName(), result));
            }
```

### ConstantValue
Value `isAddOp` is always 'false'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/metamanage/DefaultMetaDataService.java`
#### Snippet
```java
                    }
                }
                metaConfigMapper.addOrUpdTopicCtrlConf(isAddOp, entity, strBuff, result);
                retInfo.add(new TopicProcessResult(0, entity.getTopicName(), result));
            }
```

### ConstantValue
Value `isChanged` is always 'false'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/nodemanage/nodebroker/BrokerTopicInfoView.java`
#### Snippet
```java
        Tuple2<Boolean, Boolean> retResult = new Tuple2<>();
        if (topicInfoMap == null || topicInfoMap.isEmpty()) {
            retResult.setF0AndF1(isChanged, isFastSync);
            return retResult;
        }
```

### ConstantValue
Value `isFastSync` is always 'true'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/nodemanage/nodebroker/BrokerTopicInfoView.java`
#### Snippet
```java
        Tuple2<Boolean, Boolean> retResult = new Tuple2<>();
        if (topicInfoMap == null || topicInfoMap.isEmpty()) {
            retResult.setF0AndF1(isChanged, isFastSync);
            return retResult;
        }
```

### ConstantValue
Value `changed` is always 'false'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/nodemanage/nodebroker/BrokerTopicInfoView.java`
#### Snippet
```java
        ConcurrentHashSet<String> topicSet = brokerIdIndexMap.get(brokerId);
        if (topicSet == null || topicSet.isEmpty()) {
            return changed;
        }
        for (String topic : delTopicSet) {
```

### ConstantValue
Condition `masterIp != null` is always `true`
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/controller/cluster/ClusterController.java`
#### Snippet
```java
            return result;
        }
        if (masterIp != null) {
            result = queryClusterByMasterIp(masterIp);
            return result;
```

### ConstantValue
Condition `this.clusterClient == null` is always `true`
in `inlong-manager/manager-client/src/main/java/org/apache/inlong/manager/client/api/impl/InlongClusterImpl.java`
#### Snippet
```java

    public InlongClusterImpl(InlongClientImpl inlongClient) {
        if (this.clusterClient == null) {
            ClientFactory clientFactory = ClientUtils.getClientFactory(inlongClient.getConfiguration());
            this.clusterClient = clientFactory.getClusterClient();
```

### ConstantValue
Value `latestFinkVersion` is always 'null'
in `inlong-manager/manager-plugins/src/main/java/org/apache/inlong/manager/plugin/util/FlinkUtils.java`
#### Snippet
```java
            }
        }
        return latestFinkVersion;
    }

```

### ConstantValue
Value `isStream` is always 'false'
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sort/DefaultSortConfigOperator.java`
#### Snippet
```java

        if (LOGGER.isDebugEnabled()) {
            LOGGER.debug("success to build sort config, isStream={}, dataflow={}", isStream, dataflow);
        }
    }
```

### ConstantValue
Condition `producerTopic == null` is always `false`
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/pulsar/PulsarHandler.java`
#### Snippet
```java
            // topic
            String producerTopic = this.getProducerTopic(baseTopic, idConfig);
            if (producerTopic == null) {
                sinkContext.addSendResultMetric(event, clusterName, event.getUid(), false, 0);
                sinkContext.getDispatchQueue().release(event.getSize());
```

### ConstantValue
Value `remoteChannel` is always 'null'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/utils/MessageUtils.java`
#### Snippet
```java
            if (logCounter.shouldPrint()) {
                if (remoteChannel == null) {
                    logger.warn("remoteChannel == null, discard it!", remoteChannel);
                } else {
                    logger.warn("Unknown msgType message from {}, discard it!", remoteChannel);
```

### ConstantValue
Condition `backAttr != null` is always `true`
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/SimpleMessageHandler.java`
#### Snippet
```java
                byte[] backBody = null;

                if (backAttr != null && !new String(backAttr, StandardCharsets.UTF_8).isEmpty()) {
                    if (MsgType.MSG_ORIGINAL_RETURN.equals(msgType)) {

```

## RuleId[id=OptionalGetWithoutIsPresent]
### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `inlong-sdk/sort-sdk/src/main/java/org/apache/inlong/sdk/sort/fetcher/kafka/KafkaTopicFetcherBuilderImpl.java`
#### Snippet
```java

    private TopicFetcher subscribeMultiTopic() {
        InLongTopic firstTopic = topics.stream().findFirst().get();
        interceptor = Optional.ofNullable(interceptor).orElse(new MsgTimeInterceptor());
        interceptor.configure(firstTopic);
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `inlong-sdk/sort-sdk/src/main/java/org/apache/inlong/sdk/sort/fetcher/pulsar/PulsarTopicFetcherBuilderImpl.java`
#### Snippet
```java

    private TopicFetcher subscribeMultiTopic() {
        InLongTopic firstTopic = topics.stream().findFirst().get();
        interceptor = Optional.ofNullable(interceptor).orElse(new MsgTimeInterceptor());
        interceptor.configure(firstTopic);
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `inlong-sdk/sort-sdk/src/main/java/org/apache/inlong/sdk/sort/fetcher/kafka/KafkaMultiTopicsFetcher.java`
#### Snippet
```java
        try {
            this.consumer = createKafkaConsumer();
            InLongTopic topic = onlineTopics.values().stream().findFirst().get();
            this.seeker = SeekerFactory.createKafkaSeeker(consumer, topic);
            this.listener = new AckOffsetOnRebalance(topic.getInLongCluster().getClusterId(), seeker,
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `inlong-sdk/sort-sdk/src/main/java/org/apache/inlong/sdk/sort/fetcher/kafka/KafkaMultiTopicsFetcher.java`
#### Snippet
```java
                // update
                onlineTopics = newTopics.stream().collect(Collectors.toMap(InLongTopic::getTopic, t -> t));
                InLongTopic topic = onlineTopics.values().stream().findFirst().get();
                seeker = SeekerFactory.createKafkaSeeker(consumer, topic);
                Optional.ofNullable(interceptor).ifPresent(i -> i.configure(topic));
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `inlong-sdk/sort-sdk/src/main/java/org/apache/inlong/sdk/sort/manager/InlongMultiTopicManager.java`
#### Snippet
```java
            return;
        }
        InLongTopic topic = topics.stream().findFirst().get();
        LOGGER.info("create new pulsar multi topic consumer for bootstrap {}, size is {}",
                topic.getInLongCluster().getBootstraps(), consumerSize);
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `inlong-sdk/sort-sdk/src/main/java/org/apache/inlong/sdk/sort/manager/InlongMultiTopicManager.java`
#### Snippet
```java
            return;
        }
        String bootstraps = topics.stream().findFirst().get().getInLongCluster().getBootstraps();
        TopicFetcherBuilder builder = TopicFetcherBuilder.newKafkaBuilder()
                .bootstrapServers(bootstraps)
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `inlong-sdk/sort-sdk/src/main/java/org/apache/inlong/sdk/sort/manager/InlongMultiTopicManager.java`
#### Snippet
```java
            return;
        }
        InLongTopic topic = topics.stream().findFirst().get();
        LOGGER.info("create new tube multi topic consumer for bootstrap {}, size is {}",
                topic.getInLongCluster().getBootstraps(), consumerSize);
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `inlong-sdk/sort-sdk/src/main/java/org/apache/inlong/sdk/sort/fetcher/pulsar/PulsarMultiTopicsFetcher.java`
#### Snippet
```java
        }
        this.currentConsumer = new PulsarConsumer(newConsumer);
        InLongTopic firstTopic = onlineTopics.values().stream().findFirst().get();
        this.seeker = SeekerFactory.createPulsarSeeker(newConsumer, firstTopic);
        String threadName = String.format("sort_sdk_pulsar_multi_topic_fetch_thread_%s_%s",
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `inlong-sdk/sort-sdk/src/main/java/org/apache/inlong/sdk/sort/fetcher/pulsar/PulsarMultiTopicsFetcher.java`
#### Snippet
```java
        }
        PulsarConsumer newConsumerWrapper = new PulsarConsumer(newConsumer);
        InLongTopic firstTopic = newTopics.stream().findFirst().get();
        final Seeker newSeeker = SeekerFactory.createPulsarSeeker(newConsumer, firstTopic);
        // save
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/jdbc/src/main/java/org/apache/inlong/sort/jdbc/table/AbstractJdbcDialect.java`
#### Snippet
```java
    public void validate(TableSchema schema) throws ValidationException {
        for (int i = 0; i < schema.getFieldCount(); i++) {
            DataType dt = schema.getFieldDataType(i).get();
            String fieldName = schema.getFieldName(i).get();

```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/jdbc/src/main/java/org/apache/inlong/sort/jdbc/table/AbstractJdbcDialect.java`
#### Snippet
```java
        for (int i = 0; i < schema.getFieldCount(); i++) {
            DataType dt = schema.getFieldDataType(i).get();
            String fieldName = schema.getFieldName(i).get();

            // TODO: We can't convert VARBINARY(n) data type to
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/hive/src/main/java/org/apache/inlong/sort/hive/HiveTableSink.java`
#### Snippet
```java
        LogicalType[] formatTypes = new LogicalType[formatFieldCount];
        for (int i = 0; i < formatFieldCount; i++) {
            formatNames[i] = tableSchema.getFieldName(i).get();
            formatTypes[i] = tableSchema.getFieldDataType(i).get().getLogicalType();
        }
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/hive/src/main/java/org/apache/inlong/sort/hive/HiveTableSink.java`
#### Snippet
```java
        for (int i = 0; i < formatFieldCount; i++) {
            formatNames[i] = tableSchema.getFieldName(i).get();
            formatTypes[i] = tableSchema.getFieldDataType(i).get().getLogicalType();
        }
        RowType formatType = RowType.of(formatTypes, formatNames);
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/jdbc/src/main/java/org/apache/inlong/sort/jdbc/table/JdbcDynamicTableFactory.java`
#### Snippet
```java
                        .setDBUrl(url)
                        .setTableName(readableConfig.get(TABLE_NAME))
                        .setDialect(jdbcDialect.get())
                        .setParallelism(
                                readableConfig
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kafka/src/main/java/org/apache/inlong/sort/kafka/table/UpsertKafkaDynamicTableFactory.java`
#### Snippet
```java
        TableSchema schema = catalogTable.getSchema();
        // primary key should validated earlier
        List<String> keyFields = schema.getPrimaryKey().get().getColumns();
        DataType physicalDataType = schema.toPhysicalRowDataType();

```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/redis/src/main/java/org/apache/inlong/sort/redis/common/schema/impl/AbstractRedisSchema.java`
#### Snippet
```java
    private void validateRowKey(ResolvedSchema resolvedSchema) {
        Optional<Column> column = resolvedSchema.getColumn(0);
        checkState(column.get().getDataType().getConversionClass() == String.class,
                "The rowKey (first element) must be String");
    }
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kafka/src/main/java/org/apache/inlong/sort/kafka/FlinkKafkaProducer.java`
#### Snippet
```java
            Collection<FlinkKafkaProducer.KafkaTransactionState> handledTransactions) {
        cleanUpUserContext(handledTransactions);
        resetAvailableTransactionalIdsPool(getUserContext().get().transactionalIds);
        LOG.info("Recovered transactionalIds {}", getUserContext().get().transactionalIds);
    }
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/pulsar/src/main/java/org/apache/inlong/sort/pulsar/table/UpsertPulsarDynamicTableFactory.java`
#### Snippet
```java
        TableSchema schema = catalogTable.getSchema();
        // primary key should validated earlier
        List<String> keyFields = schema.getPrimaryKey().get().getColumns();
        DataType physicalDataType = schema.toPhysicalRowDataType();

```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/elasticsearch-base/src/main/java/org/apache/inlong/sort/elasticsearch/table/ElasticsearchValidationUtils.java`
#### Snippet
```java
                                                        LogicalType logicalType =
                                                                schema.getFieldDataType(fieldName)
                                                                        .get()
                                                                        .getLogicalType();
                                                        if (hasRoot(
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/trigger/PathPattern.java`
#### Snippet
```java
    public String getSuitTime() {
        // todo: Adapt to datetime in the case of multiple regex
        return whiteList.stream().findAny().get().getFormattedTime();
    }
}
```

## RuleId[id=NonAtomicOperationOnVolatileField]
### NonAtomicOperationOnVolatileField
Non-atomic operation on volatile field `asyncException`
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kafka/src/main/java/org/apache/inlong/sort/kafka/FlinkKafkaProducer.java`
#### Snippet
```java
            super.close();
        } catch (Exception e) {
            asyncException = ExceptionUtils.firstOrSuppressed(e, asyncException);
        } finally {
            // We may have to close producer of the current transaction in case some exception was
```

## RuleId[id=StringConcatenationInsideStringBufferAppend]
### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `inlong-sdk/sort-sdk/src/main/java/org/apache/inlong/sdk/sort/util/Utils.java`
#### Snippet
```java
        try {
            StringBuilder sb = new StringBuilder();
            sb.append(exception.getClass().getSimpleName() + ",");
            sb.append(exception.getMessage());

```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `inlong-sdk/sort-sdk/src/main/java/org/apache/inlong/sdk/sort/util/Utils.java`
#### Snippet
```java
            Throwable cause = exception.getCause();
            if (cause != null) {
                sb.append("," + cause.getMessage());
            }
            return sb.toString();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/mysql/source/utils/StatementUtils.java`
#### Snippet
```java
        StringBuilder sql = new StringBuilder();
        for (Iterator<String> fieldNamesIt = pkRowType.getFieldNames().iterator(); fieldNamesIt.hasNext();) {
            sql.append("MAX(" + fieldNamesIt.next() + ")");
            if (fieldNamesIt.hasNext()) {
                sql.append(" , ");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/oracle-cdc/src/main/java/org/apache/inlong/sort/cdc/oracle/source/utils/OracleUtils.java`
#### Snippet
```java
        StringBuilder sql = new StringBuilder();
        for (Iterator<String> fieldNamesIt = pkRowType.getFieldNames().iterator(); fieldNamesIt.hasNext();) {
            sql.append("MAX(" + fieldNamesIt.next() + ")");
            if (fieldNamesIt.hasNext()) {
                sql.append(" , ");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/sources/reader/file/FileReaderOperator.java`
#### Snippet
```java
            Pattern pattern = Pattern.compile(jobConf.get(JOB_FILE_LINE_END_PATTERN));
            resultLines = lines.stream().flatMap(line -> {
                sb.append(line + System.lineSeparator());
                String data = sb.toString();
                Matcher matcher = pattern.matcher(data);
```

## RuleId[id=OptionalIsPresent]
### OptionalIsPresent
Can be replaced with single expression in functional style
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/mysql/source/utils/StatementUtils.java`
#### Snippet
```java
        sql.append(projection).append(" FROM ");
        sql.append(quotedTableIdString(tableId));
        if (condition.isPresent()) {
            sql.append(" WHERE ").append(condition.get());
        }
```

### OptionalIsPresent
Can be replaced with single expression in functional style
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/mysql/source/utils/StatementUtils.java`
#### Snippet
```java
            sql.append(" WHERE ").append(condition.get());
        }
        if (orderBy.isPresent()) {
            sql.append(" ORDER BY ").append(orderBy.get());
        }
```

### OptionalIsPresent
Can be replaced with single expression in functional style
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/mysql/source/utils/StatementUtils.java`
#### Snippet
```java
        sql.append(" FROM ");
        sql.append(quotedTableIdString(tableId));
        if (condition.isPresent()) {
            sql.append(" WHERE ").append(condition.get());
        }
```

### OptionalIsPresent
Can be replaced with single expression in functional style
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/oracle-cdc/src/main/java/org/apache/inlong/sort/cdc/oracle/source/utils/OracleUtils.java`
#### Snippet
```java
        sql.append(" FROM ");
        sql.append(quoteSchemaAndTable(tableId));
        if (condition.isPresent()) {
            sql.append(" WHERE ").append(condition.get());
        }
```

### OptionalIsPresent
Can be replaced with single expression in functional style
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/oracle-cdc/src/main/java/org/apache/inlong/sort/cdc/oracle/source/utils/OracleUtils.java`
#### Snippet
```java
        sql.append(projection).append(" FROM ");
        sql.append(quoteSchemaAndTable(tableId));
        if (condition.isPresent()) {
            sql.append(" WHERE ").append(condition.get());
        }
```

### OptionalIsPresent
Can be replaced with single expression in functional style
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/oracle-cdc/src/main/java/org/apache/inlong/sort/cdc/oracle/source/utils/OracleUtils.java`
#### Snippet
```java
            sql.append(" WHERE ").append(condition.get());
        }
        if (orderBy.isPresent()) {
            sql.append(" ORDER BY ").append(orderBy.get());
        }
```

## RuleId[id=RedundantLengthCheck]
### RedundantLengthCheck
Redundant array length check
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/sources/KafkaSource.java`
#### Snippet
```java
                        new TopicPartition(partitionInfo.topic(), partitionInfo.partition())));
                // if get offset,consume from offset; if not,consume from 0
                if (partitionOffsets != null && partitionOffsets.length > 0) {
                    for (String partitionOffset : partitionOffsets) {
                        if (partitionOffset.contains(JOB_KAFKA_PARTITION_OFFSET_DELIMITER)
```

## RuleId[id=UNCHECKED_WARNING]
### UNCHECKED_WARNING
Unchecked assignment: 'java.util.ArrayList' to 'java.util.List'
in `inlong-sdk/sdk-common/src/main/java/org/apache/inlong/sdk/commons/protocol/EventUtils.java`
#### Snippet
```java
        // decode
        MessageObjs msgObjs = MessageObjs.parseFrom(srcBytes);
        List<ProxyEvent> events = new ArrayList<>(msgObjs.getMsgsList().size());
        String inlongGroupId = header.getInlongGroupId();
        String inlongStreamId = header.getInlongStreamId();
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.ArrayList' to 'java.util.List'
in `inlong-sdk/sdk-common/src/main/java/org/apache/inlong/sdk/commons/protocol/EventUtils.java`
#### Snippet
```java
        // decode
        MessageObjs msgObjs = MessageObjs.parseFrom(srcBytes);
        List<SortEvent> events = new ArrayList<>(msgObjs.getMsgsList().size());
        for (MessageObj msgObj : msgObjs.getMsgsList()) {
            SortEvent event = new SortEvent(inlongGroupId, inlongStreamId, msgObj);
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.lang.InheritableThreadLocal' to 'java.lang.ThreadLocal'
in `inlong-sort/sort-flink/base/src/main/java/org/apache/inlong/sort/base/security/FlinkSecurityManager.java`
#### Snippet
```java
    FlinkSecurityManager(UserSystemExitMode userSystemExitMode, boolean haltOnSystemExit,
            SecurityManager originalSecurityManager) {
        this.monitorUserSystemExit = new InheritableThreadLocal();
        this.userSystemExitMode = (UserSystemExitMode) Preconditions.checkNotNull(userSystemExitMode);
        this.haltOnSystemExit = haltOnSystemExit;
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Iterator' to 'java.util.Iterator'
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/source/meta/split/ChangeEventRecords.java`
#### Snippet
```java
            Set<String> finishedSnapshotSplits) {
        this.splitId = splitId;
        this.recordsForSplit = recordsForSplit;
        this.finishedSnapshotSplits = finishedSnapshotSplits;
    }
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Map' to 'java.util.Map'
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/source/meta/offset/OffsetDeserializerSerializer.java`
#### Snippet
```java
        public Offset deserialize(byte[] bytes) throws IOException {
            ObjectMapper objectMapper = new ObjectMapper();
            Map<String, String> offset = objectMapper.readValue(bytes, Map.class);

            return factory.newOffset(offset);
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.List' to 'java.util.List'
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/source/assigner/StreamSplitAssigner.java`
#### Snippet
```java
    @Override
    public List<FinishedSnapshotSplitInfo> getFinishedSplitInfos() {
        return Collections.EMPTY_LIST;
    }

```

### UNCHECKED_WARNING
Unchecked call to 'displayCurrentOffset(C)' as a member of raw type 'org.apache.inlong.sort.cdc.base.dialect.DataSourceDialect'
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/source/assigner/StreamSplitAssigner.java`
#### Snippet
```java
        return new StreamSplit(
                STREAM_SPLIT_ID,
                dialect.displayCurrentOffset(sourceConfig),
                offsetFactory.createNoStoppingOffset(),
                new ArrayList<>(),
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/hudi/src/main/java/org/apache/inlong/sort/hudi/metric/HudiMetricsConfig.java`
#### Snippet
```java
            Class<?> defaultValueClass = defaultValue.getClass();
            if (o.getClass().isAssignableFrom(defaultValueClass)) {
                return (T) o;
            } else {
                String property = String.valueOf(o);
```

### UNCHECKED_WARNING
Unchecked cast: 'capture' to 'T'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/hudi/src/main/java/org/apache/inlong/sort/hudi/metric/HudiMetricsConfig.java`
#### Snippet
```java
                String property = String.valueOf(o);
                try {
                    return (T) parseValue(defaultValueClass, property);
                } catch (InvocationTargetException | IllegalAccessException e) {
                    throw new RuntimeException("Can not properly parse value:'" + property +
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/hudi/src/main/java/org/apache/inlong/sort/hudi/metric/HudiMetricsConfig.java`
#### Snippet
```java
                throw new UnsupportedOperationException();
            }
            return (T) valueOfMethod.invoke(null, property);
        }
    }
```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.flink.configuration.ConfigOption' to 'org.apache.flink.configuration.ConfigOption'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/jdbc/src/main/java/org/apache/inlong/sort/jdbc/table/JdbcDynamicTableFactory.java`
#### Snippet
```java
        int presentCount = 0;
        for (ConfigOption configOption : configOptions) {
            if (config.getOptional(configOption).isPresent()) {
                presentCount++;
            }
```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.inlong.sort.base.dirty.DirtySinkHelper' to 'org.apache.inlong.sort.base.dirty.DirtySinkHelper'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/jdbc/src/main/java/org/apache/inlong/sort/jdbc/internal/JdbcBatchingOutputFormat.java`
#### Snippet
```java
        final JdbcRowConverter converter = (JdbcRowConverter) rowConverter.get(executor);
        TableMetricStatementExecutor newExecutor =
                new TableMetricStatementExecutor(stmtFactory, converter, dirtySinkHelper, sinkMetricData);
        // for TableBufferedStatementExecutor, replace the executor
        if (exec instanceof TableBufferedStatementExecutor) {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.function.Function'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/jdbc/src/main/java/org/apache/inlong/sort/jdbc/internal/JdbcBatchingOutputFormat.java`
#### Snippet
```java
            Field transform = TableBufferedStatementExecutor.class.getDeclaredField("valueTransform");
            transform.setAccessible(true);
            Function<RowData, RowData> valueTransform = (Function<RowData, RowData>) transform.get(exec);
            newExecutor.setValueTransform(valueTransform);
            return (JdbcExec) newExecutor;
```

### UNCHECKED_WARNING
Unchecked cast: 'org.apache.inlong.sort.jdbc.internal.TableMetricStatementExecutor' to 'JdbcExec'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/jdbc/src/main/java/org/apache/inlong/sort/jdbc/internal/JdbcBatchingOutputFormat.java`
#### Snippet
```java
            Function<RowData, RowData> valueTransform = (Function<RowData, RowData>) transform.get(exec);
            newExecutor.setValueTransform(valueTransform);
            return (JdbcExec) newExecutor;
        }
        // replace the sub-executor that generates flinkSQL for executors such as
```

### UNCHECKED_WARNING
Unchecked cast: 'org.apache.flink.connector.jdbc.internal.executor.JdbcBatchStatementExecutor' to 'JdbcExec'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/jdbc/src/main/java/org/apache/inlong/sort/jdbc/internal/JdbcMultiBatchingOutputFormat.java`
#### Snippet
```java
                    .withKeyFields(pkNameList.toArray(new String[pkNameList.size()]))
                    .build();
            statementExecutorFactory = ctx -> (JdbcExec) JdbcMultiBatchingComm.createBufferReduceExecutor(
                    createDmlOptions, ctx, rowDataTypeInfo, logicalTypes);

        } else {
```

### UNCHECKED_WARNING
Unchecked cast: 'org.apache.flink.connector.jdbc.internal.executor.JdbcBatchStatementExecutor' to 'JdbcExec'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/jdbc/src/main/java/org/apache/inlong/sort/jdbc/internal/JdbcMultiBatchingOutputFormat.java`
#### Snippet
```java
                    .getInsertIntoStatement(JdbcMultiBatchingComm.getTableNameFromIdentifier(tableIdentifier),
                            filedNames);
            statementExecutorFactory = ctx -> (JdbcExec) JdbcMultiBatchingComm.createSimpleBufferedExecutor(
                    ctx,
                    dmlOptions.getDialect(),
                    filedNames,
                    logicalTypes,
                    sql,
                    rowDataTypeInfo);
        }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'JdbcExec'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/jdbc/src/main/java/org/apache/inlong/sort/jdbc/internal/JdbcMultiBatchingOutputFormat.java`
#### Snippet
```java
            subExecutor = TableBufferReducedStatementExecutor.class.getDeclaredField("upsertExecutor");
            subExecutor.setAccessible(true);
            executor = (JdbcExec) subExecutor.get(executor);
        } else if (executor instanceof TableBufferedStatementExecutor) {
            subExecutor = TableBufferedStatementExecutor.class.getDeclaredField("statementExecutor");
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'JdbcExec'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/jdbc/src/main/java/org/apache/inlong/sort/jdbc/internal/JdbcMultiBatchingOutputFormat.java`
#### Snippet
```java
            subExecutor = TableBufferedStatementExecutor.class.getDeclaredField("statementExecutor");
            subExecutor.setAccessible(true);
            executor = (JdbcExec) subExecutor.get(executor);
        }
        Field metricField = TableMetricStatementExecutor.class.getDeclaredField("metric");
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.function.Function'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/jdbc/src/main/java/org/apache/inlong/sort/jdbc/internal/JdbcMultiBatchingOutputFormat.java`
#### Snippet
```java
            subExecutor = TableBufferedStatementExecutor.class.getDeclaredField("valueTransform");
            subExecutor.setAccessible(true);
            Function<RowData, RowData> valueTransform = (Function<RowData, RowData>) subExecutor.get(exec);
            newExecutor.setValueTransform(valueTransform);
            return (JdbcExec) newExecutor;
```

### UNCHECKED_WARNING
Unchecked cast: 'org.apache.inlong.sort.jdbc.internal.TableMetricStatementExecutor' to 'JdbcExec'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/jdbc/src/main/java/org/apache/inlong/sort/jdbc/internal/JdbcMultiBatchingOutputFormat.java`
#### Snippet
```java
            Function<RowData, RowData> valueTransform = (Function<RowData, RowData>) subExecutor.get(exec);
            newExecutor.setValueTransform(valueTransform);
            return (JdbcExec) newExecutor;
        }
        // replace the sub-executor that generates flinkSQL for executors such as
```

### UNCHECKED_WARNING
Unchecked cast: 'org.apache.flink.table.data.GenericRowData' to 'JdbcIn'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/jdbc/src/main/java/org/apache/inlong/sort/jdbc/internal/JdbcMultiBatchingOutputFormat.java`
#### Snippet
```java
                for (GenericRowData record : tableIdRecordList) {
                    totalDataSize = totalDataSize + record.toString().getBytes(StandardCharsets.UTF_8).length;
                    jdbcStatementExecutor.addToBatch((JdbcIn) record);
                }
                if (dirtySinkHelper.getDirtySink() != null) {
```

### UNCHECKED_WARNING
Unchecked cast: 'org.apache.flink.table.data.GenericRowData' to 'JdbcIn'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/jdbc/src/main/java/org/apache/inlong/sort/jdbc/internal/JdbcMultiBatchingOutputFormat.java`
#### Snippet
```java
                        try {
                            jdbcStatementExecutor = getOrCreateStatementExecutor(tableIdentifier);
                            jdbcStatementExecutor.addToBatch((JdbcIn) record);
                            jdbcStatementExecutor.executeBatch();
                            Long totalDataSize =
```

### UNCHECKED_WARNING
Unchecked call to 'DorisDynamicSchemaOutputFormat(DorisOptions, DorisReadOptions, DorisExecutionOptions, String, LogicalType\[\], String\[\], String, ...)' as a member of raw type 'org.apache.inlong.sort.doris.table.DorisDynamicSchemaOutputFormat'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/doris/src/main/java/org/apache/inlong/sort/doris/table/DorisDynamicSchemaOutputFormat.java`
#### Snippet
```java
                        .map(DataType::getLogicalType).toArray(LogicalType[]::new);
            }
            return new DorisDynamicSchemaOutputFormat(
                    optionsBuilder.setTableIdentifier(tableIdentifier).build(),
                    readOptions,
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/doris/src/main/java/org/apache/inlong/sort/doris/table/DorisDynamicSchemaOutputFormat.java`
#### Snippet
```java
                Object data = jsonFormat ? valueMap : value.toString();
                List mapData = batchMap.getOrDefault(tableIdentifier, new ArrayList<String>());
                mapData.add(data);
                batchMap.putIfAbsent(tableIdentifier, mapData);
            } catch (Exception e) {
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/doris/src/main/java/org/apache/inlong/sort/doris/table/DorisDynamicSchemaOutputFormat.java`
#### Snippet
```java
            batchBytes += ((String) row).getBytes(StandardCharsets.UTF_8).length;
            List mapData = batchMap.getOrDefault(tableIdentifier, new ArrayList<String>());
            mapData.add(row);
            batchMap.putIfAbsent(tableIdentifier, mapData);
        } else {
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Map' to 'java.util.Map'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/doris/src/main/java/org/apache/inlong/sort/doris/table/DorisDynamicSchemaOutputFormat.java`
#### Snippet
```java
        Map<String, String> rawData;
        if (dirtyData instanceof Map) {
            rawData = (Map) dirtyData;
        } else {
            rawData = OBJECT_MAPPER.readValue(OBJECT_MAPPER.writeValueAsString(dirtyData), Map.class);
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Map' to 'java.util.Map'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/doris/src/main/java/org/apache/inlong/sort/doris/table/DorisDynamicSchemaOutputFormat.java`
#### Snippet
```java
            rawData = (Map) dirtyData;
        } else {
            rawData = OBJECT_MAPPER.readValue(OBJECT_MAPPER.writeValueAsString(dirtyData), Map.class);
        }
        String label = rawData.remove(DIRTY_LABEL);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Map'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/doris/src/main/java/org/apache/inlong/sort/doris/table/DorisDynamicSchemaOutputFormat.java`
#### Snippet
```java
            for (Object item : values) {
                if (item instanceof Map) {
                    Map<String, String> map = (Map<String, String>) item;
                    Set<String> fieldNameSet = new LinkedHashSet<>(map.keySet());
                    if (columns == null) {
```

### UNCHECKED_WARNING
Unchecked call to 'getSerializationSchema(Context, EncodingFormat\>)' as a member of raw type 'org.apache.inlong.sort.redis.common.schema.RedisSchema'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/redis/src/main/java/org/apache/inlong/sort/redis/sink/RedisDynamicTableSink.java`
#### Snippet
```java

        SerializationSchema serializationSchema =
                mappingMode == DYNAMIC ? null : redisSchema.getSerializationSchema(context, format);
        StateEncoder stateEncoder = redisSchema.getStateEncoder();

```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.flink.api.common.serialization.SerializationSchema' to 'org.apache.flink.api.common.serialization.SerializationSchema'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/redis/src/main/java/org/apache/inlong/sort/redis/sink/RedisDynamicTableSink.java`
#### Snippet
```java
            case HASH:
                redisSinkFunction = new RedisHashSinkFunction(
                        serializationSchema,
                        stateEncoder,
                        batchSize,
```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.inlong.sort.redis.common.schema.StateEncoder' to 'org.apache.inlong.sort.redis.common.schema.StateEncoder\>'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/redis/src/main/java/org/apache/inlong/sort/redis/sink/RedisDynamicTableSink.java`
#### Snippet
```java
                redisSinkFunction = new RedisHashSinkFunction(
                        serializationSchema,
                        stateEncoder,
                        batchSize,
                        flushInterval,
```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.flink.api.common.serialization.SerializationSchema' to 'org.apache.flink.api.common.serialization.SerializationSchema'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/redis/src/main/java/org/apache/inlong/sort/redis/sink/RedisDynamicTableSink.java`
#### Snippet
```java
            case PLAIN:
                redisSinkFunction = new RedisPlainSinkFunction(
                        serializationSchema,
                        stateEncoder,
                        batchSize,
```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.inlong.sort.redis.common.schema.StateEncoder' to 'org.apache.inlong.sort.redis.common.schema.StateEncoder\>'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/redis/src/main/java/org/apache/inlong/sort/redis/sink/RedisDynamicTableSink.java`
#### Snippet
```java
                redisSinkFunction = new RedisPlainSinkFunction(
                        serializationSchema,
                        stateEncoder,
                        batchSize,
                        flushInterval,
```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.flink.api.common.serialization.SerializationSchema' to 'org.apache.flink.api.common.serialization.SerializationSchema'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/redis/src/main/java/org/apache/inlong/sort/redis/sink/RedisDynamicTableSink.java`
#### Snippet
```java
            case BITMAP:
                redisSinkFunction = new RedisBitmapSinkFunction(
                        serializationSchema,
                        stateEncoder,
                        batchSize,
```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.inlong.sort.redis.common.schema.StateEncoder' to 'org.apache.inlong.sort.redis.common.schema.StateEncoder\>'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/redis/src/main/java/org/apache/inlong/sort/redis/sink/RedisDynamicTableSink.java`
#### Snippet
```java
                redisSinkFunction = new RedisBitmapSinkFunction(
                        serializationSchema,
                        stateEncoder,
                        batchSize,
                        flushInterval,
```

### UNCHECKED_WARNING
Unchecked call to 'getConstructor(Class...)' as a member of raw type 'java.lang.Class'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/redis/src/main/java/org/apache/inlong/sort/redis/common/handler/RedisMapperHandler.java`
#### Snippet
```java
            }
            if (additionalKey != null && ttl != null) {
                return (RedisMapper) redisMapper.getConstructor(Integer.class, String.class)
                        .newInstance(ttl, additionalKey);
            }
```

### UNCHECKED_WARNING
Unchecked call to 'getConstructor(Class...)' as a member of raw type 'java.lang.Class'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/redis/src/main/java/org/apache/inlong/sort/redis/common/handler/RedisMapperHandler.java`
#### Snippet
```java
            }
            if (additionalKey != null) {
                return (RedisMapper) redisMapper.getConstructor(String.class)
                        .newInstance(additionalKey);
            }
```

### UNCHECKED_WARNING
Unchecked call to 'getConstructor(Class...)' as a member of raw type 'java.lang.Class'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/redis/src/main/java/org/apache/inlong/sort/redis/common/handler/RedisMapperHandler.java`
#### Snippet
```java
                        .newInstance(additionalKey);
            }
            return (RedisMapper) redisMapper.getConstructor(Integer.class)
                    .newInstance(ttl);
        } catch (Exception e) {
```

### UNCHECKED_WARNING
Unchecked cast: 'org.apache.flink.streaming.connectors.kafka.KafkaSerializationSchema' to 'org.apache.flink.streaming.connectors.kafka.KafkaContextAware'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kafka/src/main/java/org/apache/inlong/sort/kafka/FlinkKafkaProducer.java`
#### Snippet
```java

        if (kafkaSchema instanceof KafkaContextAware) {
            KafkaContextAware<IN> contextAwareSchema = (KafkaContextAware<IN>) kafkaSchema;
            contextAwareSchema.setParallelInstanceId(ctx.getIndexOfThisSubtask());
            contextAwareSchema.setNumParallelInstances(ctx.getNumberOfParallelSubtasks());
```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.flink.streaming.util.serialization.ThreadSafeDeserializationSchema' to 'org.apache.flink.api.common.serialization.DeserializationSchema'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/pulsar/src/main/java/org/apache/inlong/sort/pulsar/table/DynamicPulsarDeserializationSchema.java`
#### Snippet
```java
                    "Key must be set in upsert mode for deserialization schema.");
        }
        this.keyDeserialization = ThreadSafeDeserializationSchema.of(keyDeserialization);
        this.valueDeserialization = ThreadSafeDeserializationSchema.of(valueDeserialization);
        this.hasMetadata = hasMetadata;
```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.flink.streaming.util.serialization.ThreadSafeDeserializationSchema' to 'org.apache.flink.api.common.serialization.DeserializationSchema'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/pulsar/src/main/java/org/apache/inlong/sort/pulsar/table/DynamicPulsarDeserializationSchema.java`
#### Snippet
```java
        }
        this.keyDeserialization = ThreadSafeDeserializationSchema.of(keyDeserialization);
        this.valueDeserialization = ThreadSafeDeserializationSchema.of(valueDeserialization);
        this.hasMetadata = hasMetadata;
        this.outputCollector = new OutputProjectionCollector(
```

### UNCHECKED_WARNING
Unchecked overriding: return type requires unchecked conversion. Found 'org.apache.inlong.sort.pulsar.table.DynamicPulsarDeserializationSchema.SimpleCollector', required 'org.apache.inlong.sort.pulsar.table.DynamicPulsarDeserializationSchema.SimpleCollector'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/pulsar/src/main/java/org/apache/inlong/sort/pulsar/table/DynamicPulsarDeserializationSchema.java`
#### Snippet
```java

                @Override
                public SimpleCollector initialValue() {
                    return new SimpleCollector();
                }
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Map' to 'java.util.Map'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/pulsar/src/main/java/org/apache/inlong/sort/pulsar/table/UpsertPulsarDynamicTableFactory.java`
#### Snippet
```java
        PulsarTableOptions.StartupOptions startupOptions = new PulsarTableOptions.StartupOptions();
        startupOptions.startupMode = StartupMode.EARLIEST;
        startupOptions.specificOffsets = Collections.EMPTY_MAP;

        String adminUrl = tableOptions.get(ADMIN_URL);
```

### UNCHECKED_WARNING
Unchecked call to 'FlinkPulsarSource(String, String, ClientConfigurationData, PulsarDeserializationSchema, Properties, String, String, ...)' as a member of raw type 'org.apache.inlong.sort.pulsar.internal.FlinkPulsarSource'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/pulsar/src/main/java/org/apache/inlong/sort/pulsar/table/PulsarDynamicTableSource.java`
#### Snippet
```java
            PulsarDeserializationSchema<RowData> deserializationSchema) {
        FlinkPulsarSource source =
                new FlinkPulsarSource(
                        adminUrl,
                        serviceUrl,
```

### UNCHECKED_WARNING
Unchecked call to 'assignTimestampsAndWatermarks(WatermarkStrategy)' as a member of raw type 'org.apache.inlong.sort.pulsar.internal.FlinkPulsarSource'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/pulsar/src/main/java/org/apache/inlong/sort/pulsar/table/PulsarDynamicTableSource.java`
#### Snippet
```java

        if (watermarkStrategy != null) {
            source.assignTimestampsAndWatermarks(watermarkStrategy);
        }

```

### UNCHECKED_WARNING
Unchecked call to 'setStartFromSpecificOffsets(Map)' as a member of raw type 'org.apache.inlong.sort.pulsar.internal.FlinkPulsarSource'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/pulsar/src/main/java/org/apache/inlong/sort/pulsar/table/PulsarDynamicTableSource.java`
#### Snippet
```java
                break;
            case SPECIFIC_OFFSETS:
                source.setStartFromSpecificOffsets(startupOptions.specificOffsets);
                break;
            case EXTERNAL_SUBSCRIPTION:
```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.inlong.sort.pulsar.internal.FlinkPulsarSource' to 'org.apache.flink.streaming.api.functions.source.SourceFunction'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/pulsar/src/main/java/org/apache/inlong/sort/pulsar/table/PulsarDynamicTableSource.java`
#### Snippet
```java
                source.setStartFromSubscription(startupOptions.externalSubscriptionName, subscriptionPosition);
        }
        return source;
    }

```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.flink.streaming.connectors.pulsar.internal.PulsarTopicState' to 'org.apache.flink.streaming.connectors.pulsar.internal.PulsarTopicState'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/pulsar/src/main/java/org/apache/inlong/sort/pulsar/internal/ReaderThread.java`
#### Snippet
```java
            String subscriptionName) {
        this.owner = owner;
        this.state = state;
        this.clientConf = clientConf;
        this.readerConf = readerConf;
```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.inlong.sort.tubemq.FlinkTubeMQConsumer' to 'org.apache.inlong.sort.tubemq.FlinkTubeMQConsumer'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/tubemq/src/main/java/org/apache/inlong/sort/tubemq/table/TubeMQTableSource.java`
#### Snippet
```java
                deserialization, metadataConverters, producedTypeInfo, ignoreErrors);

        final FlinkTubeMQConsumer<RowData> tubeMQConsumer = new FlinkTubeMQConsumer(masterAddress, topic, tidSet,
                consumerGroup, tubeMQDeserializer, configuration, sessionKey, innerFormat);
        return tubeMQConsumer;
```

### UNCHECKED_WARNING
Unchecked call to 'FlinkTubeMQConsumer(String, String, TreeSet, String, DeserializationSchema, Configuration, String, ...)' as a member of raw type 'org.apache.inlong.sort.tubemq.FlinkTubeMQConsumer'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/tubemq/src/main/java/org/apache/inlong/sort/tubemq/table/TubeMQTableSource.java`
#### Snippet
```java
                deserialization, metadataConverters, producedTypeInfo, ignoreErrors);

        final FlinkTubeMQConsumer<RowData> tubeMQConsumer = new FlinkTubeMQConsumer(masterAddress, topic, tidSet,
                consumerGroup, tubeMQDeserializer, configuration, sessionKey, innerFormat);
        return tubeMQConsumer;
```

### UNCHECKED_WARNING
Unchecked cast: 'org.apache.flink.api.common.functions.util.ListCollector' to 'org.apache.flink.util.Collector'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/tubemq/src/main/java/org/apache/inlong/sort/tubemq/FlinkTubeMQConsumer.java`
#### Snippet
```java
                ListCollector<RowData> out = new ListCollector<>(rowDataList);
                for (Message message : messageList) {
                    deserializationSchema.deserialize(message.getData(), (Collector<T>) out);
                }
                rowDataList.forEach(data -> records.add((T) data));
```

### UNCHECKED_WARNING
Unchecked cast: 'org.apache.flink.table.data.RowData' to 'T'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/tubemq/src/main/java/org/apache/inlong/sort/tubemq/FlinkTubeMQConsumer.java`
#### Snippet
```java
                    deserializationSchema.deserialize(message.getData(), (Collector<T>) out);
                }
                rowDataList.forEach(data -> records.add((T) data));
            }
        }
```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.flink.streaming.connectors.pulsar.internal.PulsarTopicState' to 'org.apache.flink.streaming.connectors.pulsar.internal.PulsarTopicState'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/pulsar/src/main/java/org/apache/inlong/sort/pulsar/internal/PulsarFetcher.java`
#### Snippet
```java
            case NO_TIMESTAMPS_WATERMARKS: {
                for (Map.Entry<TopicRange, MessageId> partitionEntry : partitionsToInitialOffsets.entrySet()) {
                    PulsarTopicState<T> state = new PulsarTopicState(partitionEntry.getKey());
                    state.setOffset(partitionEntry.getValue());
                    partitionStates.add(state);
```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.flink.streaming.connectors.pulsar.internal.PulsarTopicState' to 'org.apache.flink.streaming.connectors.pulsar.internal.PulsarTopicState'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/pulsar/src/main/java/org/apache/inlong/sort/pulsar/internal/PulsarFetcher.java`
#### Snippet
```java
                    final TopicRange topicRange = partitionEntry.getKey();

                    PulsarTopicState<T> state = new PulsarTopicState(partitionEntry.getKey());
                    WatermarkStrategy<T> deserializedWatermarkStrategy = watermarkStrategy.deserializeValue(
                            userCodeClassLoader);
```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.flink.streaming.connectors.pulsar.internal.PulsarTopicState' to 'org.apache.flink.streaming.connectors.pulsar.internal.PulsarTopicState'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/pulsar/src/main/java/org/apache/inlong/sort/pulsar/internal/PulsarFetcher.java`
#### Snippet
```java
            // the ordering is crucial here; first register the state holder, then
            // push it to the partitions queue to be read
            subscribedPartitionStates.add(state);
            unassignedPartitionsQueue.add(state);
        }
```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.flink.streaming.connectors.pulsar.internal.PulsarTopicState' to 'org.apache.flink.streaming.connectors.pulsar.internal.PulsarTopicState'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/pulsar/src/main/java/org/apache/inlong/sort/pulsar/internal/PulsarFetcher.java`
#### Snippet
```java
            // push it to the partitions queue to be read
            subscribedPartitionStates.add(state);
            unassignedPartitionsQueue.add(state);
        }
    }
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Map' to 'java.util.Map\>'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/pulsar/src/main/java/org/apache/inlong/sort/pulsar/internal/PulsarFetcher.java`
#### Snippet
```java
                    }
                    topicToThread.putAll(
                            createAndStartReaderThread(ImmutableList.of(topicForBlocking), exceptionProxy));
                }
            }
```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.pulsar.shade.com.google.common.collect.ImmutableList' to 'java.util.List\>'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/pulsar/src/main/java/org/apache/inlong/sort/pulsar/internal/PulsarFetcher.java`
#### Snippet
```java
                    }
                    topicToThread.putAll(
                            createAndStartReaderThread(ImmutableList.of(topicForBlocking), exceptionProxy));
                }
            }
```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.flink.streaming.connectors.pulsar.internal.PulsarTopicState' to 'org.apache.flink.streaming.connectors.pulsar.internal.PulsarTopicState'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/pulsar/src/main/java/org/apache/inlong/sort/pulsar/internal/PulsarFetcher.java`
#### Snippet
```java
                    }
                    topicToThread.putAll(
                            createAndStartReaderThread(ImmutableList.of(topicForBlocking), exceptionProxy));
                }
            }
```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.flink.streaming.connectors.pulsar.internal.PoisonState' to 'org.apache.flink.streaming.connectors.pulsar.internal.PulsarTopicState'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/pulsar/src/main/java/org/apache/inlong/sort/pulsar/internal/PulsarFetcher.java`
#### Snippet
```java

        // make sure the main thread wakes up soon
        unassignedPartitionsQueue.addIfOpen(poisonState);
    }

```

### UNCHECKED_WARNING
Unchecked call to 'write(T)' as a member of raw type 'org.apache.inlong.sort.iceberg.sink.trick.BaseTaskWriter.BaseRollingWriter'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/iceberg/src/main/java/org/apache/inlong/sort/iceberg/sink/trick/BaseTaskWriter.java`
#### Snippet
```java
            }

            dataWriter.write(row);
        }

```

### UNCHECKED_WARNING
Unchecked call to 'write(T)' as a member of raw type 'org.apache.inlong.sort.iceberg.sink.trick.BaseTaskWriter.BaseRollingWriter'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/iceberg/src/main/java/org/apache/inlong/sort/iceberg/sink/trick/BaseTaskWriter.java`
#### Snippet
```java
        public void delete(T row) throws IOException {
            if (!internalPosDelete(structProjection.wrap(asStructLike(row)))) {
                eqDeleteWriter.write(row);
            }
        }
```

### UNCHECKED_WARNING
Unchecked call to 'write(T)' as a member of raw type 'org.apache.inlong.sort.iceberg.sink.trick.BaseTaskWriter.BaseRollingWriter'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/iceberg/src/main/java/org/apache/inlong/sort/iceberg/sink/trick/BaseTaskWriter.java`
#### Snippet
```java
        public void deleteKey(T key) throws IOException {
            if (!internalPosDelete(asStructLikeKey(key))) {
                eqDeleteWriter.write(key);
            }
        }
```

### UNCHECKED_WARNING
Unchecked call to 'IcebergProcessOperator(IcebergProcessFunction)' as a member of raw type 'org.apache.inlong.sort.iceberg.sink.multiple.IcebergProcessOperator'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/iceberg/src/main/java/org/apache/inlong/sort/iceberg/sink/FlinkSink.java`
#### Snippet
```java

            IcebergProcessOperator streamWriter =
                    new IcebergProcessOperator(new IcebergMultipleStreamWriter(
                            appendMode, catalogLoader, inlongMetric, auditHostAndPorts,
                            multipleSinkOption, dirtyOptions, dirtySink));
```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.flink.streaming.api.datastream.SingleOutputStreamOperator' to 'org.apache.flink.streaming.api.datastream.SingleOutputStreamOperator'. Reason: 'routeStream .transform(operatorName(ICEBERG_MULTIPLE_STREAM_WRITER_NAME), TypeInformation.of(IcebergProcessOperator.class), streamWriter)' has raw type, so result of setParallelism is erased
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/iceberg/src/main/java/org/apache/inlong/sort/iceberg/sink/FlinkSink.java`
#### Snippet
```java
                            appendMode, catalogLoader, inlongMetric, auditHostAndPorts,
                            multipleSinkOption, dirtyOptions, dirtySink));
            SingleOutputStreamOperator<MultipleWriteResult> writerStream = routeStream
                    .transform(operatorName(ICEBERG_MULTIPLE_STREAM_WRITER_NAME),
                            TypeInformation.of(IcebergProcessOperator.class),
                            streamWriter)
                    .setParallelism(parallelism);
            if (uidPrefix != null) {
                writerStream = writerStream.uid(uidPrefix + "-writer");
```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.inlong.sort.iceberg.sink.multiple.IcebergProcessOperator' to 'org.apache.flink.streaming.api.operators.OneInputStreamOperator'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/iceberg/src/main/java/org/apache/inlong/sort/iceberg/sink/FlinkSink.java`
#### Snippet
```java
                    .transform(operatorName(ICEBERG_MULTIPLE_STREAM_WRITER_NAME),
                            TypeInformation.of(IcebergProcessOperator.class),
                            streamWriter)
                    .setParallelism(parallelism);
            if (uidPrefix != null) {
```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.flink.streaming.api.datastream.SingleOutputStreamOperator' to 'org.apache.flink.streaming.api.datastream.SingleOutputStreamOperator'. Reason: 'input .map(new RateLimitMapFunction(tableOptions.get(WRITE_RATE_LIMIT))) .name("rate_limit")' has raw type, so result of setParallelism is erased
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/iceberg/src/main/java/org/apache/inlong/sort/iceberg/sink/FlinkSink.java`
#### Snippet
```java

            int parallelism = writeParallelism == null ? input.getParallelism() : writeParallelism;
            SingleOutputStreamOperator<T> inputWithRateLimit = input
                    .map(new RateLimitMapFunction(tableOptions.get(WRITE_RATE_LIMIT)))
                    .name("rate_limit")
                    .setParallelism(parallelism);

            if (uidPrefix != null) {
```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.inlong.sort.iceberg.sink.RateLimitMapFunction' to 'org.apache.flink.api.common.functions.MapFunction'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/iceberg/src/main/java/org/apache/inlong/sort/iceberg/sink/FlinkSink.java`
#### Snippet
```java
            int parallelism = writeParallelism == null ? input.getParallelism() : writeParallelism;
            SingleOutputStreamOperator<T> inputWithRateLimit = input
                    .map(new RateLimitMapFunction(tableOptions.get(WRITE_RATE_LIMIT)))
                    .name("rate_limit")
                    .setParallelism(parallelism);
```

### UNCHECKED_WARNING
Unchecked cast: 'org.apache.inlong.sort.iceberg.sink.collections.KVBuffer,org.apache.flink.table.data.RowData\>' to 'org.apache.inlong.sort.iceberg.sink.collections.KVBuffer,org.apache.flink.table.data.RowData\>'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/iceberg/src/main/java/org/apache/inlong/sort/iceberg/sink/collections/PartitionGroupBuffer.java`
#### Snippet
```java
            allPartition.add(partitionKey.toPath());

            return ((KVBuffer<Tuple2<String, Long>, RowData>) buffer).put(
                    new Tuple2<>(partitionKey.toPath(), number.incrementAndGet()), row);
        }
```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.flink.api.java.typeutils.TupleTypeInfo' to 'org.apache.flink.api.common.typeinfo.TypeInformation\>'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/iceberg/src/main/java/org/apache/inlong/sort/iceberg/sink/collections/PartitionGroupBuffer.java`
#### Snippet
```java
            PartitionKey partitionKey,
            BufferType type) {
        TypeInformation<Tuple2<String, Long>> keyTypeInfo = new TupleTypeInfo(
                BasicTypeInfo.STRING_TYPE_INFO,
                BasicTypeInfo.LONG_TYPE_INFO);
```

### UNCHECKED_WARNING
Unchecked call to 'TupleTypeInfo(TypeInformation...)' as a member of raw type 'org.apache.flink.api.java.typeutils.TupleTypeInfo'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/iceberg/src/main/java/org/apache/inlong/sort/iceberg/sink/collections/PartitionGroupBuffer.java`
#### Snippet
```java
            PartitionKey partitionKey,
            BufferType type) {
        TypeInformation<Tuple2<String, Long>> keyTypeInfo = new TupleTypeInfo(
                BasicTypeInfo.STRING_TYPE_INFO,
                BasicTypeInfo.LONG_TYPE_INFO);
```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.flink.api.java.typeutils.TupleTypeInfo' to 'org.apache.flink.api.common.typeinfo.TypeInformation\>'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/iceberg/src/main/java/org/apache/inlong/sort/iceberg/sink/collections/PartitionGroupBuffer.java`
#### Snippet
```java
        KVBuffer<Tuple2<String, RowData>, RowData> buffer = null;
        if (BufferType.ROCKSDB.equals(type)) {
            TypeInformation<Tuple2<String, RowData>> keyTypeInfo = new TupleTypeInfo(
                    BasicTypeInfo.STRING_TYPE_INFO,
                    InternalTypeInfo.of(FlinkSchemaUtil.convert(deleteSchema)));
```

### UNCHECKED_WARNING
Unchecked call to 'TupleTypeInfo(TypeInformation...)' as a member of raw type 'org.apache.flink.api.java.typeutils.TupleTypeInfo'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/iceberg/src/main/java/org/apache/inlong/sort/iceberg/sink/collections/PartitionGroupBuffer.java`
#### Snippet
```java
        KVBuffer<Tuple2<String, RowData>, RowData> buffer = null;
        if (BufferType.ROCKSDB.equals(type)) {
            TypeInformation<Tuple2<String, RowData>> keyTypeInfo = new TupleTypeInfo(
                    BasicTypeInfo.STRING_TYPE_INFO,
                    InternalTypeInfo.of(FlinkSchemaUtil.convert(deleteSchema)));
```

### UNCHECKED_WARNING
Unchecked cast: 'org.apache.inlong.sort.iceberg.sink.collections.KVBuffer,org.apache.flink.table.data.RowData\>' to 'org.apache.inlong.sort.iceberg.sink.collections.KVBuffer,org.apache.flink.table.data.RowData\>'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/iceberg/src/main/java/org/apache/inlong/sort/iceberg/sink/collections/PartitionGroupBuffer.java`
#### Snippet
```java

            if (RowDataUtil.isAccumulateMsg(row)) {
                return ((KVBuffer<Tuple2<String, RowData>, RowData>) buffer).put(
                        new Tuple2<>(partitionKey.toPath(), primaryKey), row);
            } else {
```

### UNCHECKED_WARNING
Unchecked cast: 'org.apache.inlong.sort.iceberg.sink.collections.KVBuffer,org.apache.flink.table.data.RowData\>' to 'org.apache.inlong.sort.iceberg.sink.collections.KVBuffer,org.apache.flink.table.data.RowData\>'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/iceberg/src/main/java/org/apache/inlong/sort/iceberg/sink/collections/PartitionGroupBuffer.java`
#### Snippet
```java
                        new Tuple2<>(partitionKey.toPath(), primaryKey), row);
            } else {
                return ((KVBuffer<Tuple2<String, RowData>, RowData>) buffer).remove(
                        new Tuple2<>(partitionKey.toPath(), primaryKey));
            }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Map'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/mysql/utils/MetaDataUtils.java`
#### Snippet
```java
        Struct sourceStruct = messageStruct.getStruct(FieldName.SOURCE);
        GenericRowData data = rowData;
        Map<String, Object> field = (Map<String, Object>) data.getField(0);

        Source source = Source.builder().db(getMetaData(record, AbstractSourceInfo.DATABASE_NAME_KEY))
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Map'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/mysql/utils/MetaDataUtils.java`
#### Snippet
```java
        long opTs = (Long) sourceStruct.get(AbstractSourceInfo.TIMESTAMP_KEY);
        // actual data
        Map<String, Object> field = (Map<String, Object>) rowData.getField(0);
        List<Map<String, Object>> dataList = new ArrayList<>();

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Iterator' to 'java.util.Iterator'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/mysql/source/split/MySqlRecords.java`
#### Snippet
```java
            Set<String> finishedSnapshotSplits) {
        this.splitId = splitId;
        this.recordsForSplit = recordsForSplit;
        this.finishedSnapshotSplits = finishedSnapshotSplits;
    }
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Map' to 'java.util.Map'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/mysql/source/offset/BinlogOffsetSerializer.java`
#### Snippet
```java
    public BinlogOffset deserialize(byte[] bytes) throws IOException {
        ObjectMapper objectMapper = new ObjectMapper();
        Map<String, String> offset = objectMapper.readValue(bytes, Map.class);
        return new BinlogOffset(offset);
    }
```

### UNCHECKED_WARNING
Unchecked call to 'compareTo(T)' as a member of raw type 'java.lang.Comparable'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/mysql/source/utils/RecordUtils.java`
#### Snippet
```java
    private static int compareObjects(Object o1, Object o2) {
        if (o1 instanceof Comparable && o1.getClass().equals(o2.getClass())) {
            return ((Comparable) o1).compareTo(o2);
        } else {
            return o1.toString().compareTo(o2.toString());
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.List' to 'java.util.List'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/mysql/source/assigners/MySqlBinlogSplitAssigner.java`
#### Snippet
```java
    @Override
    public List<FinishedSnapshotSplitInfo> getFinishedSplitInfos() {
        return Collections.EMPTY_LIST;
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Map'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/oracle-cdc/src/main/java/org/apache/inlong/sort/cdc/oracle/table/OracleReadableMetaData.java`
#### Snippet
```java
        // actual data
        GenericRowData data = rowData;
        Map<String, Object> field = (Map<String, Object>) data.getField(0);
        List<Map<String, Object>> dataList = new ArrayList<>();
        dataList.add(field);
```

### UNCHECKED_WARNING
Unchecked call to 'load(Map)' as a member of raw type 'io.debezium.pipeline.spi.OffsetContext.Loader'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/oracle-cdc/src/main/java/org/apache/inlong/sort/cdc/oracle/source/reader/fetch/OracleSourceFetchTaskContext.java`
#### Snippet
```java

        OracleOffsetContext oracleOffsetContext =
                (OracleOffsetContext) loader.load(offset.getOffset());

        return oracleOffsetContext;
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Map'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mongodb-cdc/src/main/java/org/apache/inlong/sort/cdc/mongodb/source/utils/MetaDataUtils.java`
#### Snippet
```java
        Struct sourceStruct = messageStruct.getStruct(Envelope.FieldName.SOURCE);
        GenericRowData data = (GenericRowData) rowData;
        Map<String, Object> field = (Map<String, Object>) data.getField(0);
        Map<String, String> mongoDbType = (Map<String, String>) data.getField(1);

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Map'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mongodb-cdc/src/main/java/org/apache/inlong/sort/cdc/mongodb/source/utils/MetaDataUtils.java`
#### Snippet
```java
        GenericRowData data = (GenericRowData) rowData;
        Map<String, Object> field = (Map<String, Object>) data.getField(0);
        Map<String, String> mongoDbType = (Map<String, String>) data.getField(1);

        String database = getMetaData(record, MongoDBEnvelope.NAMESPACE_DATABASE_FIELD);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Map'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mongodb-cdc/src/main/java/org/apache/inlong/sort/cdc/mongodb/source/utils/MetaDataUtils.java`
#### Snippet
```java
        Struct messageStruct = (Struct) record.value();
        GenericRowData data = (GenericRowData) rowData;
        Map<String, Object> field = (Map<String, Object>) data.getField(0);
        Map<String, String> mongoDbType = (Map<String, String>) data.getField(1);

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Map'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mongodb-cdc/src/main/java/org/apache/inlong/sort/cdc/mongodb/source/utils/MetaDataUtils.java`
#### Snippet
```java
        GenericRowData data = (GenericRowData) rowData;
        Map<String, Object> field = (Map<String, Object>) data.getField(0);
        Map<String, String> mongoDbType = (Map<String, String>) data.getField(1);

        String database = getMetaData(record, MongoDBEnvelope.NAMESPACE_DATABASE_FIELD);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Map'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mongodb-cdc/src/main/java/org/apache/inlong/sort/cdc/mongodb/source/utils/MetaDataUtils.java`
#### Snippet
```java
        }
        GenericRowData data = (GenericRowData) rowData;
        Map<String, String> mongoDbType = (Map<String, String>) data.getField(1);
        Map<String, Integer> sqlType = new LinkedHashMap<>();
        mongoDbType.forEach((name, value) -> sqlType.put(name, RecordUtils.getSqlType(value)));
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Map'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/postgres-cdc/src/main/java/org/apache/inlong/sort/cdc/postgres/table/PostgreSQLReadableMetaData.java`
#### Snippet
```java
            Struct sourceStruct = messageStruct.getStruct(FieldName.SOURCE);
            GenericRowData data = (GenericRowData) rowData;
            Map<String, Object> field = (Map<String, Object>) data.getField(0);

            Source source = Source.builder().db(getMetaData(record, AbstractSourceInfo.DATABASE_NAME_KEY))
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Map'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/postgres-cdc/src/main/java/org/apache/inlong/sort/cdc/postgres/table/PostgreSQLReadableMetaData.java`
#### Snippet
```java
        // actual data
        GenericRowData data = rowData;
        Map<String, Object> field = (Map<String, Object>) data.getField(0);
        List<Map<String, Object>> dataList = new ArrayList<>();
        dataList.add(field);
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Map' to 'java.util.Map'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/util/ParameterTool.java`
#### Snippet
```java
        Properties props = new Properties();
        props.load(inputStream);
        return fromMap((Map) props);
    }

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Map' to 'java.util.Map'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/util/ParameterTool.java`
#### Snippet
```java
     */
    public static ParameterTool fromSystemProperties() {
        return fromMap((Map) System.getProperties());
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Map'
in `inlong-sort/sort-formats/format-json/src/main/java/org/apache/inlong/sort/formats/json/canal/CanalJsonDecodingFormat.java`
#### Snippet
```java
                    @Override
                    public Object convert(Object in) {
                        return new GenericMapData((Map<String, Integer>) in);
                    }
                }),
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.HashMap' to 'java.util.Map'
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/utils/DatabaseHistoryUtil.java`
#### Snippet
```java

    private static final Map<String, Collection<SchemaRecord>> HISTORY = new HashMap();
    private static final Map<String, Boolean> HISTORY_CLEANUP_STATUS = new HashMap();

    private DatabaseHistoryUtil() {
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.HashMap' to 'java.util.Map\>'
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/utils/DatabaseHistoryUtil.java`
#### Snippet
```java
public class DatabaseHistoryUtil {

    private static final Map<String, Collection<SchemaRecord>> HISTORY = new HashMap();
    private static final Map<String, Boolean> HISTORY_CLEANUP_STATUS = new HashMap();

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Collection' to 'java.util.Collection'
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/utils/DatabaseHistoryUtil.java`
#### Snippet
```java
                        engineName));
            } else {
                return (Collection) HISTORY.getOrDefault(engineName, Collections.emptyList());
            }
        }
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.concurrent.CompletableFuture' to 'java.util.concurrent.CompletableFuture'. Reason: 'producer.newMessage().eventTime(batchMsg.getDataTime()) .value(message.buildArray())' has raw type, so result of sendAsync is erased
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/sinks/PulsarSink.java`
#### Snippet
```java
        sinkMetric.pluginSendCount.addAndGet(batchMsg.getMsgCnt());
        if (asyncSend) {
            CompletableFuture<MessageId> future = producer.newMessage().eventTime(batchMsg.getDataTime())
                    .value(message.buildArray()).sendAsync();
            future.whenCompleteAsync((m, t) -> {
                if (t != null) {
```

### UNCHECKED_WARNING
Unchecked call to 'value(T)' as a member of raw type 'org.apache.pulsar.client.api.TypedMessageBuilder'
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/sinks/PulsarSink.java`
#### Snippet
```java
        sinkMetric.pluginSendCount.addAndGet(batchMsg.getMsgCnt());
        if (asyncSend) {
            CompletableFuture<MessageId> future = producer.newMessage().eventTime(batchMsg.getDataTime())
                    .value(message.buildArray()).sendAsync();
            future.whenCompleteAsync((m, t) -> {
                if (t != null) {
```

### UNCHECKED_WARNING
Unchecked call to 'value(T)' as a member of raw type 'org.apache.pulsar.client.api.TypedMessageBuilder'
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/sinks/PulsarSink.java`
#### Snippet
```java
        } else {
            try {
                producer.newMessage().eventTime(batchMsg.getDataTime()).value(message.buildArray()).send();
                sendQueueSemaphore.release();
                updateSuccessSendMetrics(batchMsg);
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Map' to 'java.util.Map'
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/sources/KafkaSource.java`
#### Snippet
```java

        Properties props = new Properties();
        Map<String, String> map = gson.fromJson(conf.toJsonStr(), Map.class);
        props.put(JOB_KAFKA_BOOTSTRAP_SERVERS.replace(JOB_KAFKAJOB_PARAM_PREFIX, StringUtils.EMPTY),
                map.get(JOB_KAFKA_BOOTSTRAP_SERVERS));
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Collection' to 'java.lang.Iterable'
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/utils/InLongDatabaseHistory.java`
#### Snippet
```java
        Logger var10002 = this.logger;
        Objects.requireNonNull(var10002);
        if (!config.validateAndRecord(var10001, var10002::error)) {
            throw new ConnectException("Error configuring an instance of " + this.getClass().getSimpleName()
                    + "; check the logs for details");
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.concurrent.ConcurrentLinkedQueue' to 'java.util.concurrent.ConcurrentLinkedQueue'
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/utils/InLongDatabaseHistory.java`
#### Snippet
```java
    private ConcurrentLinkedQueue<SchemaRecord> getRegisteredHistoryRecord(String instanceName) {
        Collection<SchemaRecord> historyRecords = DatabaseHistoryUtil.retrieveHistory(instanceName);
        return new ConcurrentLinkedQueue(historyRecords);
    }

```

### UNCHECKED_WARNING
Unchecked call to 'ConcurrentLinkedQueue(Collection)' as a member of raw type 'java.util.concurrent.ConcurrentLinkedQueue'
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/utils/InLongDatabaseHistory.java`
#### Snippet
```java
    private ConcurrentLinkedQueue<SchemaRecord> getRegisteredHistoryRecord(String instanceName) {
        Collection<SchemaRecord> historyRecords = DatabaseHistoryUtil.retrieveHistory(instanceName);
        return new ConcurrentLinkedQueue(historyRecords);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.concurrent.ConcurrentHashMap'
in `inlong-audit/audit-sdk/src/main/java/org/apache/inlong/audit/send/SenderManager.java`
#### Snippet
```java
            ObjectInputStream objectStream = new ObjectInputStream(inputStream);
            ConcurrentHashMap<Long, AuditData> fileData =
                    (ConcurrentHashMap<Long, AuditData>) objectStream.readObject();
            for (Map.Entry<Long, AuditData> entry : fileData.entrySet()) {
                if (this.dataMap.size() < (auditConfig.getMaxCacheRow() / 2)) {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Class\>' to 'java.lang.Class'
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/source/ServerMessageFactory.java`
#### Snippet
```java
            try {
                Class<? extends ChannelInboundHandlerAdapter> clazz =
                        (Class<? extends ChannelInboundHandlerAdapter>) Class.forName(messageHandlerName);

                Constructor<?> ctor =
```

### UNCHECKED_WARNING
Unchecked call to 'exceptionally(Function)' as a member of raw type 'java.util.concurrent.CompletableFuture'
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/pulsar/PulsarClientService.java`
#### Snippet
```java

        logger.debug("producer send msg!");
        producer.newMessage().properties(proMap).value(event.getBody())
                .sendAsync().thenAccept((msgId) -> {
                    sendMessageCallBack.handleMessageSendSuccess((MessageIdImpl) msgId, es);

                }).exceptionally((e) -> {
                    sendMessageCallBack.handleMessageSendException(es, e);
                    return null;
```

### UNCHECKED_WARNING
Unchecked call to 'properties(Map)' as a member of raw type 'org.apache.pulsar.client.api.TypedMessageBuilder'
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/pulsar/PulsarClientService.java`
#### Snippet
```java

        logger.debug("producer send msg!");
        producer.newMessage().properties(proMap).value(event.getBody())
                .sendAsync().thenAccept((msgId) -> {
                    sendMessageCallBack.handleMessageSendSuccess((MessageIdImpl) msgId, es);
```

### UNCHECKED_WARNING
Unchecked call to 'thenAccept(Consumer)' as a member of raw type 'java.util.concurrent.CompletableFuture'
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/pulsar/PulsarClientService.java`
#### Snippet
```java

        logger.debug("producer send msg!");
        producer.newMessage().properties(proMap).value(event.getBody())
                .sendAsync().thenAccept((msgId) -> {
                    sendMessageCallBack.handleMessageSendSuccess((MessageIdImpl) msgId, es);

```

### UNCHECKED_WARNING
Unchecked call to 'value(T)' as a member of raw type 'org.apache.pulsar.client.api.TypedMessageBuilder'
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/pulsar/PulsarClientService.java`
#### Snippet
```java

        logger.debug("producer send msg!");
        producer.newMessage().properties(proMap).value(event.getBody())
                .sendAsync().thenAccept((msgId) -> {
                    sendMessageCallBack.handleMessageSendSuccess((MessageIdImpl) msgId, es);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Class\>' to 'java.lang.Class'
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/source/SimpleTcpSource.java`
#### Snippet
```java
                    (ServiceDecoder) Class.forName(serviceDecoderName).newInstance();
            Class<? extends ChannelInitializer> clazz =
                    (Class<? extends ChannelInitializer>) Class.forName(msgFactoryName);
            Constructor ctor =
                    clazz.getConstructor(AbstractSource.class, ChannelGroup.class,
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Map.Entry' to 'java.util.Map.Entry'
in `inlong-common/src/main/java/org/apache/inlong/common/monitor/CounterGroupExt.java`
#### Snippet
```java

            while (ite.hasNext()) {
                Entry<String, AtomicLong> entry = (Entry) ite.next();
                this.addAndGet((String) entry.getKey(), ((AtomicLong) entry.getValue()).get());
            }
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Map.Entry' to 'java.util.Map.Entry'
in `inlong-common/src/main/java/org/apache/inlong/common/monitor/CounterGroup.java`
#### Snippet
```java

            while (iterator.hasNext()) {
                Entry<String, AtomicLong> entry = (Entry) iterator.next();
                this.addAndGet((String) entry.getKey(), ((AtomicLong) entry.getValue()).get());
            }
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.HashMap' to 'java.util.HashMap'
in `inlong-common/src/main/java/org/apache/inlong/common/monitor/CounterGroupExt.java`
#### Snippet
```java

    private String name;
    private HashMap<String, AtomicLong> counters = new HashMap();
    private int maxCnt = 100000;
    private static final Logger logger = LoggerFactory.getLogger(CounterGroupExt.class);
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.HashMap' to 'java.util.HashMap'
in `inlong-common/src/main/java/org/apache/inlong/common/monitor/CounterGroup.java`
#### Snippet
```java

    private String name;
    private HashMap<String, AtomicLong> counters = new HashMap();

    public CounterGroup() {
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Map.Entry' to 'java.util.Map.Entry'
in `inlong-common/src/main/java/org/apache/inlong/common/metric/MetricObserver.java`
#### Snippet
```java
            Iterator var4 = commonProperties.entrySet().iterator();
            while (var4.hasNext()) {
                Entry<String, String> entry = (Entry) var4.next();
                String key = (String) entry.getKey();
                if (key.startsWith(prefix)) {
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.HashMap' to 'java.util.HashMap'
in `inlong-common/src/main/java/org/apache/inlong/common/monitor/StatRunner.java`
#### Snippet
```java
     */
    public void run() {
        HashMap<String, Long> counters = new HashMap();
        HashMap counterExt = new HashMap();

```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.HashMap'
in `inlong-common/src/main/java/org/apache/inlong/common/monitor/StatRunner.java`
#### Snippet
```java

                        str = (String) ite.next();
                        counterExt.put(str, this.counterGroupExt.get(str));
                    }
                }
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Map.Entry' to 'java.util.Map.Entry'
in `inlong-common/src/main/java/org/apache/inlong/common/monitor/StatRunner.java`
#### Snippet
```java

                while (iterator.hasNext()) {
                    Entry<String, Long> entrys = (Entry) iterator.next();
                    logger.info("{}.{}={}",
                            new Object[]{this.name, entrys.getKey(), entrys.getValue()});
```

### UNCHECKED_WARNING
Unchecked call to 'getConstructor(Class...)' as a member of raw type 'java.lang.Class'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/utils/MixUtils.java`
#### Snippet
```java
                    Class clazz = Class.forName(strExceptionMsgSet[0]);
                    if (clazz != null) {
                        Constructor<?> ctor = clazz.getConstructor(String.class);
                        if (ctor != null) {
                            if (strExceptionMsgSet.length == 1) {
```

### UNCHECKED_WARNING
Unchecked call to 'handleResult(T)' as a member of raw type 'org.apache.inlong.tubemq.corerpc.client.Callback'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/netty/NettyClient.java`
#### Snippet
```java
                            }
                        }
                        callback.handleResult(responseWrapper);
                    } catch (Throwable ee) {
                        responseWrapper =
```

### UNCHECKED_WARNING
Unchecked call to 'handleResult(T)' as a member of raw type 'org.apache.inlong.tubemq.corerpc.client.Callback'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/netty/NettyClient.java`
#### Snippet
```java
                            NettyClient.this.close();
                        }
                        callback.handleResult(responseWrapper);
                    }
                } else {
```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.inlong.tubemq.corerpc.client.Callback' to 'org.apache.inlong.tubemq.corerpc.client.Callback'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/netty/NettyClient.java`
#### Snippet
```java
        rpcBodyRequest.writeDelimitedTo(bbo);
        RpcDataPack pack = new RpcDataPack(request.getSerialNo(), bbo.getBufferList());
        CallFuture<ResponseWrapper> future = new CallFuture<ResponseWrapper>(callback);
        requests.put(request.getSerialNo(), future);
        if (callback == null) {
```

### UNCHECKED_WARNING
Unchecked call to 'ServiceHolder(T, AbstractServiceInvoker)' as a member of raw type 'org.apache.inlong.tubemq.corerpc.RpcServiceFactory.ServiceHolder'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/RpcServiceFactory.java`
#### Snippet
```java
                                continue;
                            }
                            servicesCache.putIfAbsent(serviceKey, new ServiceHolder(service, invoker));
                            updateTime.put(serviceKey, System.currentTimeMillis());
                            brokerQueue.remove(serviceKey);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'. Reason: 'h' has raw type, so result of getService is erased
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/RpcServiceFactory.java`
#### Snippet
```java
        ServiceHolder h = servicesCache.get(serviceKey);
        if (h != null) {
            return (T) h.getService();
        }
        RpcServiceFailoverInvoker invoker =
```

### UNCHECKED_WARNING
Unchecked call to 'ServiceHolder(T, AbstractServiceInvoker)' as a member of raw type 'org.apache.inlong.tubemq.corerpc.RpcServiceFactory.ServiceHolder'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/RpcServiceFactory.java`
#### Snippet
```java
        Object service =
                Proxy.newProxyInstance(clazz.getClassLoader(), new Class[]{clazz}, invoker);
        servicesCache.put(serviceKey, new ServiceHolder(service, invoker));
        return (T) service;
    }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/RpcServiceFactory.java`
#### Snippet
```java
                Proxy.newProxyInstance(clazz.getClassLoader(), new Class[]{clazz}, invoker);
        servicesCache.put(serviceKey, new ServiceHolder(service, invoker));
        return (T) service;
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'. Reason: 'h' has raw type, so result of getService is erased
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/RpcServiceFactory.java`
#### Snippet
```java
        if (h != null) {
            updateTime.put(serviceKey, System.currentTimeMillis());
            return (T) h.getService();
        }
        RpcServiceInvoker invoker =
```

### UNCHECKED_WARNING
Unchecked call to 'ServiceHolder(T, AbstractServiceInvoker)' as a member of raw type 'org.apache.inlong.tubemq.corerpc.RpcServiceFactory.ServiceHolder'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/RpcServiceFactory.java`
#### Snippet
```java
        Object service =
                Proxy.newProxyInstance(clazz.getClassLoader(), new Class[]{clazz}, invoker);
        servicesCache.put(serviceKey, new ServiceHolder(service, invoker));
        updateTime.put(serviceKey, System.currentTimeMillis());
        return (T) service;
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/RpcServiceFactory.java`
#### Snippet
```java
        servicesCache.put(serviceKey, new ServiceHolder(service, invoker));
        updateTime.put(serviceKey, System.currentTimeMillis());
        return (T) service;
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'. Reason: 'h' has raw type, so result of getService is erased
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/RpcServiceFactory.java`
#### Snippet
```java
        ServiceHolder h = servicesCache.get(serviceKey);
        if (h != null) {
            return (T) h.getService();
        } else {
            if (!isRemoteAddrForbidden(brokerInfo.getBrokerAddr())) {
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.lang.Class' to 'java.lang.Class'
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/producer/SimpleMessageProducer.java`
#### Snippet
```java
        int count = 0;
        while (count++ < partList.size()) {
            if (rpcServiceFactory.getOrCreateService(clazz, brokerInfo, rpcConfig) != null) {
                break;
            }
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.concurrent.ConcurrentHashMap' to 'java.util.concurrent.ConcurrentHashMap'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/tools/cli/CliConsumer.java`
#### Snippet
```java
    private static final AtomicLong TOTAL_COUNTER = new AtomicLong(0);
    private static final ConcurrentHashMap<String, AtomicLong> TOPIC_COUNT_MAP =
            new ConcurrentHashMap();
    private long startTime = System.currentTimeMillis();
    // sent data content
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Map'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/tools/cli/CliMetaDataBRU.java`
#### Snippet
```java
        logger.info("[Recovery Topic Deploy] begin ");
        Map<String, TopicDeployEntity> storedTopicDeployMap =
                (Map<String, TopicDeployEntity>) readObjectFromFile(
                        backupAndRecoveryPath, storeFileNameTopicDeploy);
        if (storedTopicDeployMap == null) {
            logger.error(strBuff.append("  read configure file ")
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Map'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/tools/cli/CliMetaDataBRU.java`
#### Snippet
```java
        logger.info("[Backup Topic Deploy] verify configurations");
        Map<String, TopicDeployEntity> storedTopicDeployMap =
                (Map<String, TopicDeployEntity>) readObjectFromFile(
                        backupAndRecoveryPath, storeFileNameTopicDeploy);
        if (storedTopicDeployMap == null) {
            logger.error(strBuff.append("  read configure file ")
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Map'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/tools/cli/CliMetaDataBRU.java`
#### Snippet
```java
        logger.info("[Recovery Broker Conf] begin ");
        Map<String, BrokerConfEntity> storedBrokerConfigMap =
                (Map<String, BrokerConfEntity>) readObjectFromFile(
                        backupAndRecoveryPath, storeFileNameBrokerConf);
        if (storedBrokerConfigMap == null) {
            logger.error(strBuff.append("  read configure file ")
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Map'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/tools/cli/CliMetaDataBRU.java`
#### Snippet
```java
        logger.info("[Backup Cluster Conf] verify configurations ");
        Map<String, ClusterSettingEntity> storedSettingMap =
                (Map<String, ClusterSettingEntity>) readObjectFromFile(
                        backupAndRecoveryPath, storeFileNameClusterConf);
        if (storedSettingMap == null) {
            logger.error(strBuff.append("  read configure file ")
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Map'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/tools/cli/CliMetaDataBRU.java`
#### Snippet
```java
        logger.info("[Backup Csm Ctrl] verify configurations");
        Map<String, GroupConsumeCtrlEntity> storedGroupCsmMap =
                (Map<String, GroupConsumeCtrlEntity>) readObjectFromFile(
                        backupAndRecoveryPath, storeFileNameCsmCtrl);
        if (storedGroupCsmMap == null) {
            logger.error(strBuff.append("  read configure file ")
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Map'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/tools/cli/CliMetaDataBRU.java`
#### Snippet
```java
        logger.info("[Recovery Topic Ctrl] begin ");
        Map<String, TopicCtrlEntity> storedTopicCtrlMap =
                (Map<String, TopicCtrlEntity>) readObjectFromFile(
                        backupAndRecoveryPath, storeFileNameTopicCtrl);
        if (storedTopicCtrlMap == null) {
            logger.error(strBuff.append("  read configure file ")
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Map'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/tools/cli/CliMetaDataBRU.java`
#### Snippet
```java
        logger.info("[Recovery Cluster Conf] begin ");
        Map<String, ClusterSettingEntity> storedSettingMap =
                (Map<String, ClusterSettingEntity>) readObjectFromFile(
                        backupAndRecoveryPath, storeFileNameClusterConf);
        if (storedSettingMap == null) {
            logger.error(strBuff.append("  read configure file ")
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Map'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/tools/cli/CliMetaDataBRU.java`
#### Snippet
```java
        logger.info("[Recovery Csm Ctrl] begin ");
        Map<String, GroupConsumeCtrlEntity> storedGroupCsmMap =
                (Map<String, GroupConsumeCtrlEntity>) readObjectFromFile(
                        backupAndRecoveryPath, storeFileNameCsmCtrl);
        if (storedGroupCsmMap == null) {
            logger.error(strBuff.append("  read configure file ")
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Map'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/tools/cli/CliMetaDataBRU.java`
#### Snippet
```java
        logger.info("[Recovery Group Ctrl] begin ");
        Map<String, GroupResCtrlEntity> storedGroupCtrlMap =
                (Map<String, GroupResCtrlEntity>) readObjectFromFile(
                        backupAndRecoveryPath, storeFileNameGroupCtrl);
        if (storedGroupCtrlMap == null) {
            logger.error(strBuff.append("  read configure file ")
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Map'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/tools/cli/CliMetaDataBRU.java`
#### Snippet
```java
        logger.info("[Backup Group Ctrl] verify configurations");
        Map<String, GroupResCtrlEntity> storedGroupCtrlMap =
                (Map<String, GroupResCtrlEntity>) readObjectFromFile(
                        backupAndRecoveryPath, storeFileNameGroupCtrl);
        if (storedGroupCtrlMap == null) {
            logger.error(strBuff.append("  read configure file ")
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Map'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/tools/cli/CliMetaDataBRU.java`
#### Snippet
```java
        logger.info("[Backup Topic Ctrl] verify configurations");
        Map<String, TopicCtrlEntity> storedTopicCtrlMap =
                (Map<String, TopicCtrlEntity>) readObjectFromFile(
                        backupAndRecoveryPath, storeFileNameTopicCtrl);
        if (storedTopicCtrlMap == null) {
            logger.error(strBuff.append("  read configure file ")
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Map'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/tools/cli/CliMetaDataBRU.java`
#### Snippet
```java
        logger.info("[Backup Broker Conf] verify configurations");
        Map<String, BrokerConfEntity> storedBrokerConfigMap =
                (Map<String, BrokerConfEntity>) readObjectFromFile(
                        backupAndRecoveryPath, storeFileNameBrokerConf);
        if (storedBrokerConfigMap == null) {
            logger.error(strBuff.append("  read configure file ")
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Map\>'
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/SimpleClientBalanceConsumer.java`
#### Snippet
```java
        // store consume target information
        Map<String, TreeSet<String>> consumeTargetMap =
                (Map<String, TreeSet<String>>) result.getRetData();
        this.consumeSubInfo.storeConsumeTarget(consumeTargetMap);
        if (!startMasterAndBrokerThreads(result, sBuffer)) {
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.HashMap' to 'java.util.HashMap'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/utils/WritableComparator.java`
#### Snippet
```java
public class WritableComparator {

    private static HashMap<Class, WritableComparator> comparators = new HashMap();

    public static int hashBytes(byte[] bytes, int offset, int length) {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/web/BrokerAdminServlet.java`
#### Snippet
```java
            return;
        }
        Set<String> filterCondStrSet = (Set<String>) result.getRetData();
        broker.getBrokerServiceServer()
                .getMessageSnapshot(topicName, partitionId, msgCount, filterCondStrSet, sBuffer);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/web/BrokerAdminServlet.java`
#### Snippet
```java
            return;
        }
        Set<String> groupNameSet = (Set<String>) result.getRetData();
        // get online partition-client map
        int totalCnt = 0;
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/web/BrokerAdminServlet.java`
#### Snippet
```java
        }
        // get target consume group name
        Set<String> topicSet = (Set<String>) result.getRetData();
        // get topic's publish info
        Map<String, Map<Integer, TopicPubStoreInfo>> topicStorePubInfoMap =
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.List\>'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/web/BrokerAdminServlet.java`
#### Snippet
```java
        }
        List<Tuple3<String, Integer, Long>> resetOffsets =
                (List<Tuple3<String, Integer, Long>>) result.getRetData();
        if (resetOffsets.isEmpty()) {
            WebParameterUtils.buildFailResult(sBuffer, "Not found history offset value!");
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/web/BrokerAdminServlet.java`
#### Snippet
```java
                return;
            }
            Set<String> topicNameSet = (Set<String>) result.getRetData();
            // set topic's statistic status
            Map<String, ConcurrentHashMap<Integer, MessageStore>> msgTopicStores =
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/web/BrokerAdminServlet.java`
#### Snippet
```java
            return;
        }
        final Set<String> groupNameSet = (Set<String>) result.getRetData();
        // get modify user
        if (!WebParameterUtils.getStringParamValue(req,
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/web/BrokerAdminServlet.java`
#### Snippet
```java
        }
        // get target consume group name
        Set<String> topicNameSet = (Set<String>) result.getRetData();
        // get set mode
        if (!WebParameterUtils.getBooleanParamValue(req,
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Map\>\>'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/web/BrokerAdminServlet.java`
#### Snippet
```java
        }
        Map<String, Map<String, Set<Integer>>> groupTopicPartMap =
                (Map<String, Map<String, Set<Integer>>>) result.getRetData();
        broker.getOffsetManager().deleteGroupOffset(
                onlyMemory, groupTopicPartMap, modifier);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Map\>'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/web/BrokerAdminServlet.java`
#### Snippet
```java
            if (validAndGetTopicPartInfo(group, WebFieldDef.GROUPNAME, topicSet, result)) {
                Map<String, Set<Integer>> topicPartMap =
                        (Map<String, Set<Integer>>) result.getRetData();
                groupTopicPartMap.put(group, topicPartMap);
            }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/web/BrokerAdminServlet.java`
#### Snippet
```java
            return;
        }
        final Set<String> groupNameSet = (Set<String>) result.getRetData();
        // get the maximum record number of the group to be queried
        if (!WebParameterUtils.getIntParamValue(req,
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/web/BrokerAdminServlet.java`
#### Snippet
```java
            return;
        }
        Set<String> inGroupNameSet = (Set<String>) result.getRetData();
        // get the topic set to be queried
        if (!WebParameterUtils.getStringParamValue(req,
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/web/BrokerAdminServlet.java`
#### Snippet
```java
        }
        // get target consume group name
        Set<String> topicSet = (Set<String>) result.getRetData();
        // filter invalid groups
        Set<String> qryGroupNameSet = new HashSet<>();
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/web/BrokerAdminServlet.java`
#### Snippet
```java
            return;
        }
        Set<String> topicNameSet = (Set<String>) result.getRetData();
        sBuilder.append("{\"result\":true,\"errCode\":0,\"errMsg\":\"Success!\",\"dataSet\":[");
        Map<String, ConcurrentHashMap<Integer, MessageStore>> messageTopicStores =
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/web/BrokerAdminServlet.java`
#### Snippet
```java
            return;
        }
        final Set<String> groupNameSet = (Set<String>) result.getRetData();
        // get set mode
        if (!WebParameterUtils.getBooleanParamValue(req,
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Map'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/web/BrokerAdminServlet.java`
#### Snippet
```java
            }
            Map<String, Long> manOffsets =
                    (Map<String, Long>) result.getRetData();
            // valid and transfer offset format
            if (!validManOffsetResetInfo(WebFieldDef.OFFSETJSON, manOffsets, result)) {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.List\>'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/web/BrokerAdminServlet.java`
#### Snippet
```java
            }
            resetOffsets =
                    (List<Tuple3<String, Integer, Long>>) result.getRetData();
        } else {
            // get the topic set to be set
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/web/BrokerAdminServlet.java`
#### Snippet
```java
                return;
            }
            Set<String> topicSet = (Set<String>) result.getRetData();
            // transfer offset format
            resetOffsets = buildOffsetResetInfo(topicSet);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/web/BrokerAdminServlet.java`
#### Snippet
```java
            return;
        }
        Set<String> topicNameSet = (Set<String>) result.getRetData();
        if (!WebParameterUtils.getBooleanParamValue(req,
                WebFieldDef.NEEDREFRESH, false, false, sBuffer, result)) {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/web/BrokerAdminServlet.java`
#### Snippet
```java
            return;
        }
        Set<String> srcTopicNameSet = (Set<String>) result.getRetData();
        // valid topic and get topic's partitionIds
        if (!validAndGetTopicPartInfo(srcGroupName,
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Map\>'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/web/BrokerAdminServlet.java`
#### Snippet
```java
        }
        final Map<String, Set<Integer>> topicPartMap =
                (Map<String, Set<Integer>>) result.getRetData();
        // get target consume group name
        if (!WebParameterUtils.getStringParamValue(req,
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/web/BrokerAdminServlet.java`
#### Snippet
```java
            return;
        }
        Set<String> tgtGroupNameSet = (Set<String>) result.getRetData();
        // get modify user
        if (!WebParameterUtils.getStringParamValue(req,
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/web/BrokerAdminServlet.java`
#### Snippet
```java
            return;
        }
        Set<String> groupNameSet = (Set<String>) result.getRetData();
        strBuff.append("{\"result\":true,\"errCode\":0,\"errMsg\":\"Success!\",\"dataSet\":[");
        Map<String, ConsumerNodeInfo> map =
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Map\>'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/web/BrokerAdminServlet.java`
#### Snippet
```java
            if (validAndGetTopicPartInfo(group, groupFldDef, topicSet, result)) {
                Map<String, Set<Integer>> topicPartMap =
                        (Map<String, Set<Integer>>) result.getRetData();
                // get topic's publish info
                Map<String, Map<Integer, TopicPubStoreInfo>> topicStorePubInfoMap =
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.concurrent.ConcurrentHashMap' to 'java.util.concurrent.ConcurrentHashMap'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/webbase/WebCallStatsHolder.java`
#### Snippet
```java
        // Simple Statistics Based on Methods
        protected final ConcurrentHashMap<String, SimpleHistogram> methodStatsMap =
                new ConcurrentHashMap();

        public WebCallStatsItemSet() {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Map'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/MessageStoreManager.java`
#### Snippet
```java
            @Override
            public void propertyChange(final PropertyChangeEvent evt) {
                Map<String, TopicMetadata> oldTopicConfigMap = (Map<String, TopicMetadata>) evt.getOldValue();
                Map<String, TopicMetadata> newTopicConfigMap = (Map<String, TopicMetadata>) evt.getNewValue();
                MessageStoreManager.this.refreshMessageStoresHoldVals(oldTopicConfigMap, newTopicConfigMap);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Map'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/MessageStoreManager.java`
#### Snippet
```java
            public void propertyChange(final PropertyChangeEvent evt) {
                Map<String, TopicMetadata> oldTopicConfigMap = (Map<String, TopicMetadata>) evt.getOldValue();
                Map<String, TopicMetadata> newTopicConfigMap = (Map<String, TopicMetadata>) evt.getNewValue();
                MessageStoreManager.this.refreshMessageStoresHoldVals(oldTopicConfigMap, newTopicConfigMap);

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.ArrayList' to 'java.util.List'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/action/screen/config/BrokerList.java`
#### Snippet
```java
        BrokerRunManager brokerRunManager = master.getBrokerRunManager();
        List<BrokerInfo> brokerInfoList =
                new ArrayList(brokerRunManager.getBrokerInfoMap(null).values());
        // *************************************************************************************
        for (int i = 0; i < 95; i++) {
```

### UNCHECKED_WARNING
Unchecked call to 'ArrayList(Collection)' as a member of raw type 'java.util.ArrayList'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/action/screen/config/BrokerList.java`
#### Snippet
```java
        BrokerRunManager brokerRunManager = master.getBrokerRunManager();
        List<BrokerInfo> brokerInfoList =
                new ArrayList(brokerRunManager.getBrokerInfoMap(null).values());
        // *************************************************************************************
        for (int i = 0; i < 95; i++) {
```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.Map'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/common/BaseResult.java`
#### Snippet
```java
                this.tempParam = new HashMap(5);
            }
            this.tempParam.put(String.valueOf(key), val);
        }
        return this;
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebAdminFlowRuleHandler.java`
#### Snippet
```java
            return sBuffer;
        }
        final Set<String> groupNameSet = (Set<String>) result.getRetData();
        // get and valid qryPriorityId info
        if (!WebParameterUtils.getQryPriorityIdParameter(req,
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebAdminFlowRuleHandler.java`
#### Snippet
```java
            return sBuffer;
        }
        final Set<String> groupNameSet = (Set<String>) result.getRetData();
        // get and valid qryPriorityId info
        if (!WebParameterUtils.getQryPriorityIdParameter(req,
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebAdminFlowRuleHandler.java`
#### Snippet
```java
            return sBuffer;
        }
        Set<String> groupNameSet = (Set<String>) result.getRetData();
        // add or modify records
        GroupResCtrlEntity ctrlEntity;
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.List\>'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebAdminTopicAuthHandler.java`
#### Snippet
```java
        }
        List<Map<String, String>> deployJsonArray =
                (List<Map<String, String>>) result.getRetData();
        TopicCtrlEntity itemConf;
        Map<String, TopicCtrlEntity> addRecordMap = new HashMap<>();
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebAdminTopicAuthHandler.java`
#### Snippet
```java
            return sBuffer;
        }
        Set<String> topicNameSet = (Set<String>) result.getRetData();
        // delete records
        TopicCtrlEntity ctrlEntity;
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Map'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebAdminTopicAuthHandler.java`
#### Snippet
```java
        }
        Map<String, TopicCtrlEntity> addRecordMap =
                (Map<String, TopicCtrlEntity>) result.getRetData();
        List<TopicProcessResult> retInfo = new ArrayList<>();
        for (TopicCtrlEntity topicCtrlInfo : addRecordMap.values()) {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebAdminTopicAuthHandler.java`
#### Snippet
```java
            return sBuffer;
        }
        Set<String> topicNameSet = (Set<String>) result.getRetData();
        // query matched records
        Map<String, TopicCtrlEntity> topicCtrlMap =
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebAdminTopicAuthHandler.java`
#### Snippet
```java
            return sBuffer;
        }
        Set<String> topicNameSet = (Set<String>) result.getRetData();
        // get authCtrlStatus info
        if (!WebParameterUtils.getEnableStatusValue(req, WebFieldDef.ISENABLE,
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Map'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebGroupConsumeCtrlHandler.java`
#### Snippet
```java
        }
        Map<String, GroupConsumeCtrlEntity> batchAddInfoMap =
                (Map<String, GroupConsumeCtrlEntity>) result.getRetData();
        // add group resource record
        GroupProcessResult addResult;
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.List\>'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebGroupConsumeCtrlHandler.java`
#### Snippet
```java
        }
        List<Map<String, String>> filterJsonArray =
                (List<Map<String, String>>) result.getRetData();
        // parse groupCsmJsonSet field info
        GroupConsumeCtrlEntity itemConf;
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebGroupConsumeCtrlHandler.java`
#### Snippet
```java
            return sBuffer;
        }
        Set<String> groupNameSet = (Set<String>) result.getRetData();
        // check and get topicName field
        if (!WebParameterUtils.getStringParamValue(req,
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebMasterInfoHandler.java`
#### Snippet
```java
            return sBuffer;
        }
        Set<Integer> brokerIds = (Set<Integer>) result.getRetData();
        // check and get topicName field
        if (!WebParameterUtils.getStringParamValue(req,
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebMasterInfoHandler.java`
#### Snippet
```java
            return sBuffer;
        }
        Set<String> topicNameSet = (Set<String>) result.getRetData();
        // query topic configure info
        Map<String, List<TopicDeployEntity>> topicConfMap =
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebMasterInfoHandler.java`
#### Snippet
```java
            return strBuff;
        }
        Set<String> topicNameSet = (Set<String>) result.getRetData();
        Set<String> targetTopicSet;
        // query topic-group map info
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebGroupConsumeCtrlHandler.java`
#### Snippet
```java
            return sBuffer;
        }
        Set<String> topicNameSet = (Set<String>) result.getRetData();
        // execute delete operation
        List<GroupProcessResult> retInfo = new ArrayList<>();
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebGroupConsumeCtrlHandler.java`
#### Snippet
```java
            return strBuff;
        }
        final Set<String> groupSet = (Set<String>) result.getRetData();
        // check and get topicName field
        if (!WebParameterUtils.getStringParamValue(req,
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebGroupConsumeCtrlHandler.java`
#### Snippet
```java
            return strBuff;
        }
        final Set<String> topicNameSet = (Set<String>) result.getRetData();
        // get consumeEnable info
        if (!WebParameterUtils.getEnableStatusValue(req,
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebGroupConsumeCtrlHandler.java`
#### Snippet
```java
            return strBuff;
        }
        Set<String> filterCondSet = (Set<String>) result.getRetData();
        // query matched records
        qryEntity.updModifyInfo(qryEntity.getDataVerId(),
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebGroupConsumeCtrlHandler.java`
#### Snippet
```java
            return sBuffer;
        }
        final Set<String> topicNameSet = (Set<String>) result.getRetData();
        // get groupName field
        if (!WebParameterUtils.getStringParamValue(req,
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebGroupConsumeCtrlHandler.java`
#### Snippet
```java
            return sBuffer;
        }
        final Set<String> groupNameSet = (Set<String>) result.getRetData();
        // get consumeEnable info
        if (!WebParameterUtils.getEnableStatusValue(req,
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Map'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebGroupConsumeCtrlHandler.java`
#### Snippet
```java
        }
        Map<String, GroupConsumeCtrlEntity> batchAddInfoMap =
                (Map<String, GroupConsumeCtrlEntity>) result.getRetData();
        // delete group consume control records
        List<GroupProcessResult> retInfo = new ArrayList<>();
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebTopicCtrlHandler.java`
#### Snippet
```java
            return sBuffer;
        }
        Set<String> topicNameSet = (Set<String>) result.getRetData();
        // delete records
        List<TopicProcessResult> retInfo = new ArrayList<>();
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebTopicCtrlHandler.java`
#### Snippet
```java
            return sBuffer;
        }
        Set<String> topicNameSet = (Set<String>) result.getRetData();
        // query matched records
        Map<String, TopicCtrlEntity> topicCtrlMap =
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebTopicCtrlHandler.java`
#### Snippet
```java
            return sBuffer;
        }
        Set<String> topicNameSet = (Set<String>) result.getRetData();
        // get topicNameId info
        int topicNameId = TBaseConstants.META_VALUE_UNDEFINED;
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Map'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebTopicCtrlHandler.java`
#### Snippet
```java
        }
        Map<String, TopicCtrlEntity> addRecordMap =
                (Map<String, TopicCtrlEntity>) result.getRetData();
        List<TopicProcessResult> retInfo =
                defMetaDataService.addOrUpdTopicCtrlConf(isAddOp, addRecordMap, sBuffer, result);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.List\>'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebTopicCtrlHandler.java`
#### Snippet
```java
        }
        List<Map<String, String>> ctrlJsonArray =
                (List<Map<String, String>>) result.getRetData();
        // get default max message size
        ClusterSettingEntity defClusterSetting =
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebGroupResCtrlHandler.java`
#### Snippet
```java
            return sBuffer;
        }
        final Set<String> batchGroupNames = (Set<String>) result.getRetData();
        // get resCheckStatus info
        if (!WebParameterUtils.getEnableStatusValue(req, WebFieldDef.RESCHECKENABLE,
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebGroupResCtrlHandler.java`
#### Snippet
```java
            return strBuff;
        }
        final Set<String> inGroupSet = (Set<String>) result.getRetData();
        // get resCheckStatus info
        if (!WebParameterUtils.getEnableStatusValue(req,
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.List\>'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebGroupResCtrlHandler.java`
#### Snippet
```java
        }
        List<Map<String, String>> ctrlJsonArray =
                (List<Map<String, String>>) result.getRetData();
        // get default qryPriorityId
        ClusterSettingEntity defClusterSetting =
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebGroupResCtrlHandler.java`
#### Snippet
```java
            return sBuffer;
        }
        Set<String> batchGroupNames = (Set<String>) result.getRetData();
        // delete group resource record
        List<GroupProcessResult> retInfo = new ArrayList<>();
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Map'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebGroupResCtrlHandler.java`
#### Snippet
```java
        }
        Map<String, GroupResCtrlEntity> addRecordMap =
                (Map<String, GroupResCtrlEntity>) result.getRetData();
        // add or update group resource record
        List<GroupProcessResult> retInfo = new ArrayList<>();
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Map' to 'java.util.Map'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/simplemvc/VelocityTemplateEngine.java`
#### Snippet
```java
        Template t = engine.getTemplate(templateName);
        if (t != null) {
            t.merge(new VelocityContext(context.getMap()), writer);
        }
    }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/utils/WebParameterUtils.java`
#### Snippet
```java
        if (fieldDef.isCompFieldType()) {
            Set<Integer> tgtValueSet = new HashSet<>();
            Set<String> valItemSet = (Set<String>) result.getRetData();
            if (valItemSet.isEmpty()) {
                if (hasDefVal) {
```

### UNCHECKED_WARNING
Unchecked cast: 'T' to 'java.util.Map'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/utils/WebParameterUtils.java`
#### Snippet
```java
        if (paramCntr instanceof Map) {
            Map<String, String> keyValueMap =
                    (Map<String, String>) paramCntr;
            paramValue = keyValueMap.get(WebFieldDef.FLOWCTRLSET.name);
            if (paramValue == null) {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/utils/WebParameterUtils.java`
#### Snippet
```java
            return result.isSuccess();
        }
        Set<String> filterCondSet = (Set<String>) result.getRetData();
        if (filterCondSet.isEmpty()) {
            if (transBlank) {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/utils/WebParameterUtils.java`
#### Snippet
```java
            // translate filter condition item with "''"
            TreeSet<String> newFilterCondSet = new TreeSet<>();
            Set<String> filterCondSet = (Set<String>) result.getRetData();
            if (!filterCondSet.isEmpty()) {
                for (String filterCond : filterCondSet) {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/utils/WebParameterUtils.java`
#### Snippet
```java
            return result.isSuccess();
        }
        Set<String> topicNameSet = (Set<String>) result.getRetData();
        Set<String> existedTopicSet =
                defMetaDataService.getDeployedTopicSet();
```

### UNCHECKED_WARNING
Unchecked cast: 'T' to 'java.util.Map'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/utils/WebParameterUtils.java`
#### Snippet
```java
        if (paramCntr instanceof Map) {
            Map<String, String> keyValueMap =
                    (Map<String, String>) paramCntr;
            paramValue = keyValueMap.get(fieldDef.name);
            if (paramValue == null) {
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Set' to 'java.util.Set'. Reason: 'map' has raw type, so result of keySet is erased
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/simplemvc/MappedContext.java`
#### Snippet
```java
    @Override
    public Set<String> keySet() {
        return map.keySet();
    }

```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.Map'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/simplemvc/MappedContext.java`
#### Snippet
```java
    @Override
    public void put(String key, Object value) {
        map.put(key, value);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebOtherInfoHandler.java`
#### Snippet
```java
            return sBuffer;
        }
        Set<String> inGroupNameSet = (Set<String>) result.getRetData();
        if (inGroupNameSet.isEmpty()) {
            if (!WebParameterUtils.getStringParamValue(req,
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebOtherInfoHandler.java`
#### Snippet
```java
                return sBuffer;
            }
            inGroupNameSet = (Set<String>) result.getRetData();
        }
        if (!WebParameterUtils.getStringParamValue(req,
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebOtherInfoHandler.java`
#### Snippet
```java
            return sBuffer;
        }
        Set<String> topicNameSet = (Set<String>) result.getRetData();
        TopicPSInfoManager topicPSInfoManager = master.getTopicPSInfoManager();
        Set<String> queryGroupSet =
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.HashMap'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebBrokerConfHandler.java`
#### Snippet
```java
        }
        Map<Integer, BrokerConfEntity> addedRecordMap =
                (HashMap<Integer, BrokerConfEntity>) result.getRetData();
        // add record and process result
        List<BrokerProcessResult> retInfo = new ArrayList<>();
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.apache.inlong.tubemq.corebase.utils.Tuple2'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebBrokerConfHandler.java`
#### Snippet
```java
            }
            Tuple2<Integer, String> brokerIdAndIpTuple =
                    (Tuple2<Integer, String>) result.getRetData();
            // set the initial status
            if (mngStatus == ManageStatus.STATUS_MANAGE_UNDEFINED) {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebBrokerConfHandler.java`
#### Snippet
```java
                return sBuffer;
            }
            Set<Integer> brokerIdSet = (Set<Integer>) result.getRetData();
            for (Integer brokerId : brokerIdSet) {
                retInfo.add(defMetaDataService.addOrUpdBrokerConfig(isAddOp, opEntity,
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebBrokerConfHandler.java`
#### Snippet
```java
            return sBuffer;
        }
        Set<Integer> brokerIds = (Set<Integer>) result.getRetData();
        List<BrokerProcessResult> retInfo = new ArrayList<>();
        for (Integer brokerId : brokerIds) {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebBrokerConfHandler.java`
#### Snippet
```java
            return sBuffer;
        }
        Set<Integer> brokerIds = (Set<Integer>) result.getRetData();
        List<BrokerProcessResult> retInfo = new ArrayList<>();
        for (Integer brokerId : brokerIds) {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebBrokerConfHandler.java`
#### Snippet
```java
            return sBuffer;
        }
        final Set<Integer> brokerIds = (Set<Integer>) result.getRetData();
        // get brokerIp info
        if (!WebParameterUtils.getStringParamValue(req,
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebBrokerConfHandler.java`
#### Snippet
```java
            return sBuffer;
        }
        final Set<String> brokerIpSet = (Set<String>) result.getRetData();
        // get brokerPort field
        if (!WebParameterUtils.getIntParamValue(req, WebFieldDef.BROKERPORT,
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebBrokerConfHandler.java`
#### Snippet
```java
            return sBuffer;
        }
        Set<String> topicNameSet = (Set<String>) result.getRetData();
        // get isInclude info
        if (!WebParameterUtils.getBooleanParamValue(req,
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebBrokerConfHandler.java`
#### Snippet
```java
            return sBuffer;
        }
        Set<Integer> brokerIds = (Set<Integer>) result.getRetData();
        // get and valid broker read or write status info
        if (!getAcceptReadAndWriteParamValue(req, sBuffer, result)) {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.apache.inlong.tubemq.corebase.utils.Tuple2'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebBrokerConfHandler.java`
#### Snippet
```java
        }
        Tuple2<Boolean, Boolean> rdWtTpl =
                (Tuple2<Boolean, Boolean>) result.getRetData();
        // change broker status
        BrokerConfEntity curEntry;
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.List\>'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebBrokerConfHandler.java`
#### Snippet
```java
        }
        List<Map<String, String>> brokerJsonArray =
                (List<Map<String, String>>) result.getRetData();
        // check and get cluster default setting info
        ClusterSettingEntity defClusterSetting =
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.apache.inlong.tubemq.corebase.utils.Tuple2'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebBrokerConfHandler.java`
#### Snippet
```java
                }
                Tuple2<Integer, String> brokerIdAndIpTuple =
                        (Tuple2<Integer, String>) result.getRetData();
                // set the initial status
                if (mngStatus == ManageStatus.STATUS_MANAGE_UNDEFINED) {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebBrokerConfHandler.java`
#### Snippet
```java
            return sBuffer;
        }
        Set<Integer> brokerIds = (Set<Integer>) result.getRetData();
        // check and get relReason field
        if (!WebParameterUtils.getStringParamValue(req, WebFieldDef.RELREASON,
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebBrokerConfHandler.java`
#### Snippet
```java
            return sBuffer;
        }
        Set<Integer> brokerIds = (Set<Integer>) result.getRetData();
        List<BrokerProcessResult> retInfo = new ArrayList<>();
        for (Integer brokerId : brokerIds) {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebBrokerConfHandler.java`
#### Snippet
```java
            return sBuffer;
        }
        final Set<Integer> brokerIds = (Set<Integer>) result.getRetData();
        // get brokerIp info
        if (!WebParameterUtils.getStringParamValue(req,
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebBrokerConfHandler.java`
#### Snippet
```java
            return sBuffer;
        }
        final Set<String> brokerIpSet = (Set<String>) result.getRetData();
        // get withDetail info
        if (!WebParameterUtils.getBooleanParamValue(req,
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebBrokerConfHandler.java`
#### Snippet
```java
            return sBuffer;
        }
        Set<Integer> brokerIds = (Set<Integer>) result.getRetData();
        List<BrokerProcessResult> retInfo = new ArrayList<>();
        for (Integer brokerId : brokerIds) {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/TMaster.java`
#### Snippet
```java
            return builder.build();
        }
        final Set<String> reqTopicSet = (Set<String>) result.getRetData();
        final Map<String, TreeSet<String>> reqTopicConditions =
                DataConverterUtil.convertTopicConditions(request.getTopicConditionList());
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Map'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/TMaster.java`
#### Snippet
```java
            return builder.build();
        }
        Map<String, Long> requiredPartMap = (Map<String, Long>) result.getRetData();
        String sessionKey = request.hasSessionKey() ? request.getSessionKey() : "";
        long sessionTime = request.hasSessionTime()
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/TMaster.java`
#### Snippet
```java
            return builder.build();
        }
        final Set<String> transTopicSet = (Set<String>) result.getRetData();
        if (!request.hasBrokerCheckSum()) {
            builder.setErrCode(TErrCodeConstants.BAD_REQUEST);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/TMaster.java`
#### Snippet
```java
            return builder.build();
        }
        final Set<String> reqTopicSet = (Set<String>) result.getRetData();
        final Map<String, TreeSet<String>> reqTopicConditions =
                DataConverterUtil.convertTopicConditions(request.getTopicConditionList());
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/TMaster.java`
#### Snippet
```java
            return builder.build();
        }
        final Set<String> transTopicSet = (Set<String>) result.getRetData();
        if (!request.hasBrokerCheckSum()) {
            builder.setErrCode(TErrCodeConstants.BAD_REQUEST);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebTopicDeployHandler.java`
#### Snippet
```java
            return sBuffer;
        }
        Set<String> topicNameSet = (Set<String>) result.getRetData();
        // check and get brokerId info
        if (!WebParameterUtils.getIntParamValue(req,
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebTopicDeployHandler.java`
#### Snippet
```java
            return sBuffer;
        }
        Set<Integer> brokerIdSet = (Set<Integer>) result.getRetData();
        // modify record status
        List<TopicProcessResult> retInfo = new ArrayList<>();
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebTopicDeployHandler.java`
#### Snippet
```java
            return sBuffer;
        }
        final Set<String> topicNameSet = (Set<String>) result.getRetData();
        // check and get brokerId field
        if (!WebParameterUtils.getIntParamValue(req,
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebTopicDeployHandler.java`
#### Snippet
```java
            return sBuffer;
        }
        final Set<Integer> brokerIdSet = (Set<Integer>) result.getRetData();
        // get brokerPort field
        if (!WebParameterUtils.getIntParamValue(req, WebFieldDef.BROKERPORT,
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebTopicDeployHandler.java`
#### Snippet
```java
            return sBuffer;
        }
        Set<String> topicNameSet = (Set<String>) result.getRetData();
        if (!WebParameterUtils.getBooleanParamValue(req,
                WebFieldDef.WITHIP, false, false, sBuffer, result)) {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebTopicDeployHandler.java`
#### Snippet
```java
            return sBuffer;
        }
        final Set<String> topicNameSet = (Set<String>) result.getRetData();
        // check and get brokerId info
        if (!WebParameterUtils.getIntParamValue(req,
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebTopicDeployHandler.java`
#### Snippet
```java
            return sBuffer;
        }
        final Set<Integer> brokerIdSet = (Set<Integer>) result.getRetData();
        // get and valid TopicPropGroup info
        if (!WebParameterUtils.getTopicPropInfo(req, null, sBuffer, result)) {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebTopicDeployHandler.java`
#### Snippet
```java
            return sBuffer;
        }
        Set<Integer> brokerIds = (Set<Integer>) result.getRetData();
        // check and get topicName field
        if (!WebParameterUtils.getStringParamValue(req,
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebTopicDeployHandler.java`
#### Snippet
```java
            return sBuffer;
        }
        Set<String> topicNameSet = (Set<String>) result.getRetData();
        // query topic configure info
        Map<String, List<TopicDeployEntity>> topicConfMap =
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Map'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebTopicDeployHandler.java`
#### Snippet
```java
        }
        Map<String, TopicDeployEntity> addRecordMap =
                (Map<String, TopicDeployEntity>) result.getRetData();
        List<TopicProcessResult> retInfo = new ArrayList<>();
        for (TopicDeployEntity topicDeployInfo : addRecordMap.values()) {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebTopicDeployHandler.java`
#### Snippet
```java
            return sBuffer;
        }
        final Set<String> topicNameSet = (Set<String>) result.getRetData();
        // check and get brokerId field
        if (!WebParameterUtils.getIntParamValue(req,
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebTopicDeployHandler.java`
#### Snippet
```java
            return sBuffer;
        }
        final Set<Integer> brokerIdSet = (Set<Integer>) result.getRetData();
        // get brokerPort field
        if (!WebParameterUtils.getIntParamValue(req, WebFieldDef.BROKERPORT,
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.List\>'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebTopicDeployHandler.java`
#### Snippet
```java
        }
        List<Map<String, String>> deployJsonArray =
                (List<Map<String, String>>) result.getRetData();
        TopicDeployEntity itemConf;
        Map<String, TopicDeployEntity> addRecordMap = new HashMap<>();
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebTopicDeployHandler.java`
#### Snippet
```java
            return sBuffer;
        }
        Set<Integer> brokerIds = (Set<Integer>) result.getRetData();
        Map<Integer, Set<String>> brokerTopicConfigMap =
                defMetaDataService.getBrokerTopicConfigInfo(brokerIds);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebTopicDeployHandler.java`
#### Snippet
```java
            return sBuffer;
        }
        Set<String> topicNameSet = (Set<String>) result.getRetData();
        // check and get brokerId field
        if (!WebParameterUtils.getIntParamValue(req,
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebTopicDeployHandler.java`
#### Snippet
```java
            return sBuffer;
        }
        Set<Integer> brokerIdSet = (Set<Integer>) result.getRetData();
        Map<Integer, List<TopicDeployEntity>> queryResult =
                defMetaDataService.getTopicDeployInfoMap(topicNameSet, brokerIdSet);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebAdminGroupCtrlHandler.java`
#### Snippet
```java
            return sBuffer;
        }
        Set<String> groupNameSet = (Set<String>) result.getRetData();
        // get group list
        if (!WebParameterUtils.getIntParamValue(req,
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebAdminGroupCtrlHandler.java`
#### Snippet
```java
            return sBuffer;
        }
        Set<String> groupNameSet = (Set<String>) result.getRetData();
        // check and get topicName field
        if (!WebParameterUtils.getStringParamValue(req,
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebAdminGroupCtrlHandler.java`
#### Snippet
```java
            return sBuffer;
        }
        Set<String> topicNameSet = (Set<String>) result.getRetData();
        // only query disable consume group
        entity.setConsumeEnable(EnableStatus.STATUS_DISABLE);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Map'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebAdminGroupCtrlHandler.java`
#### Snippet
```java
        }
        Map<String, GroupConsumeCtrlEntity> addRecordMap =
                (Map<String, GroupConsumeCtrlEntity>) result.getRetData();
        // add or update and build result
        GroupConsumeCtrlEntity curEntity;
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Map'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebAdminGroupCtrlHandler.java`
#### Snippet
```java
        }
        Map<String, GroupConsumeCtrlEntity> addRecordMap =
                (Map<String, GroupConsumeCtrlEntity>) result.getRetData();
        // add or update and build result
        GroupConsumeCtrlEntity ctrlEntity;
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebAdminGroupCtrlHandler.java`
#### Snippet
```java
            return sBuffer;
        }
        Set<String> groupNameSet = (Set<String>) result.getRetData();
        // check and get topicName field
        if (!WebParameterUtils.getStringParamValue(req,
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebAdminGroupCtrlHandler.java`
#### Snippet
```java
            return sBuffer;
        }
        Set<String> topicNameSet = (Set<String>) result.getRetData();
        qryEntity.setConsumeEnable(EnableStatus.STATUS_ENABLE);
        Map<String, List<GroupConsumeCtrlEntity>> qryResultMap =
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebAdminGroupCtrlHandler.java`
#### Snippet
```java
            return sBuffer;
        }
        final Set<String> groupNameSet = (Set<String>) result.getRetData();
        // check and get topicName field
        if (!WebParameterUtils.getStringParamValue(req,
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebAdminGroupCtrlHandler.java`
#### Snippet
```java
            return sBuffer;
        }
        final Set<String> topicNameSet = (Set<String>) result.getRetData();
        // check and get condStatus field
        if (!getCondStatusParamValue(req, false, null, sBuffer, result)) {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebAdminGroupCtrlHandler.java`
#### Snippet
```java
            return sBuffer;
        }
        Set<String> filterCondSet = (Set<String>) result.getRetData();
        qryEntity.updModifyInfo(qryEntity.getDataVerId(),
                null, null, filterEnable, null);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebAdminGroupCtrlHandler.java`
#### Snippet
```java
            return sBuffer;
        }
        Set<String> consumerIdSet = (Set<String>) result.getRetData();
        ConsumerInfoHolder consumerInfoHolder =
                master.getConsumerHolder();
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.List\>'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebAdminGroupCtrlHandler.java`
#### Snippet
```java
        }
        List<Map<String, String>> groupJsonArray =
                (List<Map<String, String>>) result.getRetData();
        GroupConsumeCtrlEntity itemEntity;
        Map<String, GroupConsumeCtrlEntity> addRecordMap = new HashMap<>();
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebAdminGroupCtrlHandler.java`
#### Snippet
```java
            return sBuffer;
        }
        final Set<String> groupNameSet = (Set<String>) result.getRetData();
        // process offset setting
        ConsumerInfoHolder consumerHolder = master.getConsumerHolder();
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebAdminGroupCtrlHandler.java`
#### Snippet
```java
            return sBuffer;
        }
        Set<String> topicNameSet = (Set<String>) result.getRetData();
        // get group list
        if (!WebParameterUtils.getStringParamValue(req,
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebAdminGroupCtrlHandler.java`
#### Snippet
```java
            return sBuffer;
        }
        Set<String> groupNameSet = (Set<String>) result.getRetData();
        List<GroupProcessResult> retInfoList = new ArrayList<>();
        if (groupNameSet.isEmpty()) {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebAdminGroupCtrlHandler.java`
#### Snippet
```java
            return sBuffer;
        }
        Set<String> groupNameSet = (Set<String>) result.getRetData();
        // check and get topicName field
        if (!WebParameterUtils.getAndValidTopicNameInfo(req,
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebAdminGroupCtrlHandler.java`
#### Snippet
```java
            return sBuffer;
        }
        Set<String> topicNameSet = (Set<String>) result.getRetData();
        // add black list records
        GroupConsumeCtrlEntity ctrlEntity;
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Map'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebAdminGroupCtrlHandler.java`
#### Snippet
```java
        }
        Map<String, GroupConsumeCtrlEntity> addRecordMap =
                (Map<String, GroupConsumeCtrlEntity>) result.getRetData();
        // add or update and build result
        GroupConsumeCtrlEntity ctrlEntity;
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebAdminGroupCtrlHandler.java`
#### Snippet
```java
            return sBuffer;
        }
        Set<String> groupNameSet = (Set<String>) result.getRetData();
        // check and get topicName field
        if (!WebParameterUtils.getAndValidTopicNameInfo(req,
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebAdminGroupCtrlHandler.java`
#### Snippet
```java
            return sBuffer;
        }
        Set<String> topicNameSet = (Set<String>) result.getRetData();
        List<GroupProcessResult> retInfoList = new ArrayList<>();
        // add allowed consume records
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Map'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebAdminGroupCtrlHandler.java`
#### Snippet
```java
        }
        Map<String, GroupConsumeCtrlEntity> addRecordMap =
                (Map<String, GroupConsumeCtrlEntity>) result.getRetData();
        // add or update and build result
        GroupConsumeCtrlEntity ctrlEntity;
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.List\>'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebAdminGroupCtrlHandler.java`
#### Snippet
```java
        }
        List<Map<String, String>> groupJsonArray =
                (List<Map<String, String>>) result.getRetData();
        GroupResCtrlEntity itemEntity;
        Map<String, String> itemValueMap;
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebAdminGroupCtrlHandler.java`
#### Snippet
```java
            return sBuffer;
        }
        Set<String> groupNameSet = (Set<String>) result.getRetData();
        // get resCheckStatus info
        if (!WebParameterUtils.getEnableStatusValue(req, WebFieldDef.RESCHECKENABLE,
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebAdminGroupCtrlHandler.java`
#### Snippet
```java
            return sBuffer;
        }
        Set<String> topicNameSet = (Set<String>) result.getRetData();
        // get group list
        if (!WebParameterUtils.getStringParamValue(req,
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebAdminGroupCtrlHandler.java`
#### Snippet
```java
            return sBuffer;
        }
        Set<String> groupNameSet = (Set<String>) result.getRetData();
        // add allowed consume records
        List<GroupProcessResult> retInfoList = new ArrayList<>();
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.List\>'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebAdminGroupCtrlHandler.java`
#### Snippet
```java
        }
        List<Map<String, String>> groupJsonArray =
                (List<Map<String, String>>) result.getRetData();
        GroupConsumeCtrlEntity itemEntity;
        Map<String, GroupConsumeCtrlEntity> addRecordMap = new HashMap<>();
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Map'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebAdminGroupCtrlHandler.java`
#### Snippet
```java
        }
        Map<String, GroupConsumeCtrlEntity> addRecordMap =
                (Map<String, GroupConsumeCtrlEntity>) result.getRetData();
        // add or update and build result
        GroupConsumeCtrlEntity ctrlEntity;
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Map'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebAdminGroupCtrlHandler.java`
#### Snippet
```java
        }
        Map<String, GroupResCtrlEntity> addRecordMap =
                (Map<String, GroupResCtrlEntity>) result.getRetData();
        // add or update and build result
        List<GroupProcessResult> retInfoList = new ArrayList<>();
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebAdminGroupCtrlHandler.java`
#### Snippet
```java
            return sBuffer;
        }
        Set<String> topicNameSet = (Set<String>) result.getRetData();
        // get group list
        if (!WebParameterUtils.getStringParamValue(req,
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebAdminGroupCtrlHandler.java`
#### Snippet
```java
            return sBuffer;
        }
        Set<String> groupNameSet = (Set<String>) result.getRetData();
        List<GroupProcessResult> retInfoList = new ArrayList<>();
        if (groupNameSet.isEmpty()) {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebAdminGroupCtrlHandler.java`
#### Snippet
```java
            return sBuffer;
        }
        final Set<String> groupNameSet = (Set<String>) result.getRetData();
        // check and get topicName field
        if (!WebParameterUtils.getAndValidTopicNameInfo(req,
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebAdminGroupCtrlHandler.java`
#### Snippet
```java
            return sBuffer;
        }
        Set<String> topicNameSet = (Set<String>) result.getRetData();
        // check and get condStatus field
        if (!getCondStatusParamValue(req, false,
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Set'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebAdminGroupCtrlHandler.java`
#### Snippet
```java
            return sBuffer;
        }
        Set<String> groupNameSet = (Set<String>) result.getRetData();
        // add or update group control record
        List<GroupProcessResult> retInfoList = new ArrayList<>();
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Map' to 'java.util.Map'
in `inlong-manager/manager-pojo/src/main/java/org/apache/inlong/manager/pojo/sort/standalone/SortSourceStreamInfo.java`
#### Snippet
```java
        if (extParamsMap.isEmpty() && StringUtils.isNotBlank(extParams)) {
            try {
                extParamsMap = GSON.fromJson(extParams, Map.class);
            } catch (Throwable t) {
                LOGGER.error("fail to parse group ext params", t);
```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.inlong.manager.common.tool.excel.tuple.ImmutableQuartet' to 'org.apache.inlong.manager.common.tool.excel.tuple.ImmutableQuartet'
in `inlong-manager/manager-common/src/main/java/org/apache/inlong/manager/common/tool/excel/tuple/ImmutableQuartet.java`
#### Snippet
```java
     */
    public static <L, M, R, S> ImmutableQuartet<L, M, R, S> nullTriple() {
        return NULL;
    }

```

### UNCHECKED_WARNING
Unchecked call to 'validate(T)' as a member of raw type 'org.apache.inlong.manager.common.tool.excel.validator.ExcelCellValidator'
in `inlong-manager/manager-common/src/main/java/org/apache/inlong/manager/common/tool/excel/ExcelTool.java`
#### Snippet
```java
            Object value) {
        ExcelCellValidator cellValidator = fieldMeta.getCellValidator();
        if (cellValidator != null && !cellValidator.validate(value)) {
            return Optional.of(cellValidator.getInvalidTip());
        } else {
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.HashMap' to 'java.util.Map'
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/node/AbstractDataNodeOperator.java`
#### Snippet
```java
    @Override
    public Map<String, String> parse2SinkParams(DataNodeInfo info) {
        return JsonUtils.parseObject(info.getExtParams(), HashMap.class);
    }

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.HashMap' to 'java.util.Map'
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/core/impl/AgentServiceImpl.java`
#### Snippet
```java
                    clusterInfo.setToken(cluster.getToken());
                    clusterInfo.setMqType(cluster.getType());
                    clusterInfo.setParams(JsonUtils.parseObject(cluster.getExtParams(), HashMap.class));
                    mqSet.add(clusterInfo);
                }
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.HashMap' to 'java.util.Map'
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/sink/AbstractSinkOperator.java`
#### Snippet
```java
        Map<String, String> param;
        try {
            param = JsonUtils.parseObject(streamSink.getExtParams(), HashMap.class);
            // put group and stream info
            assert param != null;
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Map' to 'java.util.Map'
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/cluster/InlongClusterServiceImpl.java`
#### Snippet
```java
            clusterInfo.setToken(cluster.getToken());
            clusterInfo.setMqType(cluster.getType());
            Map<String, String> configParams = GSON.fromJson(cluster.getExtParams(), Map.class);
            clusterInfo.setParams(configParams);
            mqSet.add(clusterInfo);
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Map' to 'java.util.Map'
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/cluster/InlongClusterServiceImpl.java`
#### Snippet
```java
            info.setUrl(entity.getUrl());
            info.setMqType(entity.getType());
            info.setParams(GSON.fromJson(entity.getExtParams(), Map.class));
            mqInfoList.add(info);
        }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Map'
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sink/es/ElasticsearchApi.java`
#### Snippet
```java
        List<ElasticsearchFieldInfo> notExistFieldInfos = new ArrayList<>(fieldInfos);
        Map<String, MappingMetaData> mapping = getFields(indexName);
        Map<String, Object> filedMap = (Map<String, Object>) mapping.get(indexName).getSourceAsMap().get(FIELD_KEY);
        for (String key : filedMap.keySet()) {
            for (ElasticsearchFieldInfo field : notExistFieldInfos) {
```

### UNCHECKED_WARNING
Unchecked call to 'create(T, WorkflowContext)' as a member of raw type 'org.apache.inlong.manager.workflow.processor.ElementProcessor'
in `inlong-manager/manager-workflow/src/main/java/org/apache/inlong/manager/workflow/core/impl/ProcessorExecutorImpl.java`
#### Snippet
```java
        context.setCurrentElement(element);

        if (!processor.create(element, context)) {
            return;
        }
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.List' to 'java.util.List'. Reason: 'processor' has raw type, so result of next is erased
in `inlong-manager/manager-workflow/src/main/java/org/apache/inlong/manager/workflow/core/impl/ProcessorExecutorImpl.java`
#### Snippet
```java
            return;
        }
        List<Element> nextElements = processor.next(element, context);
        for (Element next : nextElements) {
            executeStart(next, context);
```

### UNCHECKED_WARNING
Unchecked call to 'next(T, WorkflowContext)' as a member of raw type 'org.apache.inlong.manager.workflow.processor.ElementProcessor'
in `inlong-manager/manager-workflow/src/main/java/org/apache/inlong/manager/workflow/core/impl/ProcessorExecutorImpl.java`
#### Snippet
```java
            return;
        }
        List<Element> nextElements = processor.next(element, context);
        for (Element next : nextElements) {
            executeStart(next, context);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Class\>' to 'java.lang.Class'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/config/CommonConfigHolder.java`
#### Snippet
```java
        try {
            Class<? extends IManagerIpListParser> ipListParserClass =
                    (Class<? extends IManagerIpListParser>) Class.forName(this.managerType);
            this.ipListParser = ipListParserClass.getDeclaredConstructor().newInstance();
            this.ipListParser.setCommonProperties(this.props);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Class\>' to 'java.lang.Class'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/ServerMessageFactory.java`
#### Snippet
```java
            try {
                Class<? extends ChannelInboundHandlerAdapter> clazz =
                        (Class<? extends ChannelInboundHandlerAdapter>) Class
                                .forName(messageHandlerName);

                Constructor<?> ctor = clazz.getConstructor(
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Class\>' to 'java.lang.Class'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/tcp/InlongTcpChannelPipelineFactory.java`
#### Snippet
```java
            try {
                Class<? extends ChannelInboundHandlerAdapter> clazz =
                        (Class<? extends ChannelInboundHandlerAdapter>) Class
                                .forName(messageHandlerName);

                Constructor<?> ctor = clazz.getConstructor(SourceContext.class);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Class\>' to 'java.lang.Class'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/BaseSource.java`
#### Snippet
```java
            ServiceDecoder serviceDecoder = (ServiceDecoder) Class.forName(serviceDecoderName).newInstance();
            Class<? extends ChannelInitializer> clazz =
                    (Class<? extends ChannelInitializer>) Class.forName(msgFactoryName);
            Constructor ctor = clazz.getConstructor(BaseSource.class, ChannelGroup.class,
                    String.class, ServiceDecoder.class, String.class, Integer.class,
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Class\>' to 'java.lang.Class'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/tcp/InlongTcpSource.java`
#### Snippet
```java
        ChannelInitializer fac = null;
        try {
            Class<? extends ChannelInitializer> clazz = (Class<? extends ChannelInitializer>) Class
                    .forName(msgFactoryName);

            Constructor ctor = clazz.getConstructor(SourceContext.class, String.class);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Class\>' to 'java.lang.Class'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source2/InLongMessageFactory.java`
#### Snippet
```java
            try {
                Class<? extends ChannelInboundHandlerAdapter> clazz =
                        (Class<? extends ChannelInboundHandlerAdapter>) Class.forName(source.getMessageHandlerName());
                Constructor<?> ctor = clazz.getConstructor(BaseSource.class);
                ChannelInboundHandlerAdapter messageHandler =
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Map'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/ServerMessageHandler.java`
#### Snippet
```java
            // get attribute data from parsed result
            Map<String, String> commonAttrMap =
                    (Map<String, String>) resultMap.get(ConfigConstants.COMMON_ATTR_MAP);
            if (commonAttrMap == null) {
                commonAttrMap = new HashMap<>();
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.List'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/ServerMessageHandler.java`
#### Snippet
```java
            // check message's groupId, streamId, topic
            List<ProxyMessage> msgList =
                    (List<ProxyMessage>) resultMap.get(ConfigConstants.MSG_LIST);
            if (msgList == null) {
                commonAttrMap.put(AttributeConstants.MESSAGE_PROCESS_ERRCODE,
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Class\>' to 'java.lang.Class'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source2/BaseSource.java`
#### Snippet
```java
        try {
            Class<? extends ChannelInitializer> clazz =
                    (Class<? extends ChannelInitializer>) Class.forName(msgFactoryName);
            Constructor ctor = clazz.getConstructor(BaseSource.class);
            logger.info("Using channel processor:{}", getChannelProcessor().getClass().getName());
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Map'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/SimpleMessageHandler.java`
#### Snippet
```java
            }

            Map<String, String> commonAttrMap = (Map<String, String>) resultMap.get(ConfigConstants.COMMON_ATTR_MAP);
            if (commonAttrMap == null) {
                commonAttrMap = new HashMap<String, String>();
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.List'
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/SimpleMessageHandler.java`
#### Snippet
```java
            }

            List<ProxyMessage> msgList = (List<ProxyMessage>) resultMap.get(ConfigConstants.MSG_LIST);
            if (msgList != null
                    && !commonAttrMap.containsKey(ConfigConstants.FILE_CHECK_DATA)
```

## RuleId[id=DataFlowIssue]
### DataFlowIssue
Method invocation `offset` may produce `NullPointerException`
in `inlong-sdk/sort-sdk/src/main/java/org/apache/inlong/sdk/sort/fetcher/kafka/KafkaSeeker.java`
#### Snippet
```java
            endOffsetsTopicPartitions.add(tp);
        }
        long expected = offsetAndTimestamp.offset();
        long last = consumer.position(tp);
        LOGGER.info("for tp {}, expected offset is {}, last offset is {}", tp, expected, last);
```

### DataFlowIssue
Argument `oldDispatchProfile` might be null
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/dispatch/DispatchManager.java`
#### Snippet
```java
                    sdkEvent.getInlongStreamId(), dispatchTime);
            DispatchProfile oldDispatchProfile = this.profileCache.put(dispatchKey, newDispatchProfile);
            this.dispatchQueue.offer(oldDispatchProfile);
            newDispatchProfile.addEvent(event, maxPackCount, maxPackSize);
        }
```

### DataFlowIssue
Argument `attrBytes` might be null
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/codec/ProtocolDecoder.java`
#### Snippet
```java
                buffer.readBytes(attrBytes);
            }
            EncodeObject object = new EncodeObject(bodyBytes, new String(attrBytes, StandardCharsets.UTF_8));
            object.setMsgtype(5);
            out.add(object);
```

### DataFlowIssue
Argument `attrBytes` might be null
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/codec/ProtocolDecoder.java`
#### Snippet
```java
                buffer.readBytes(attrBytes);
            }
            EncodeObject object = new EncodeObject(new String(attrBytes, StandardCharsets.UTF_8));
            object.setMessageId(String.valueOf(seqId));

```

### DataFlowIssue
Method invocation `getAttachment` may produce `NullPointerException`
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/network/TcpChannelGroup.java`
#### Snippet
```java
                Channel newChannel = future.getChannel();
                tcpChannel.setChannel(newChannel);
                newChannel.setAttachment(oldChannel.getAttachment());
                if (oldChannel != null) {
                    oldChannel.disconnect();
```

### DataFlowIssue
Dereference of `body` may produce `NullPointerException`
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/codec/ProtocolEncoder.java`
#### Snippet
```java
                msgType |= FLAG_ALLOW_COMPRESS;
            }
            totalLength = totalLength + body.length + endAttr.getBytes("utf8").length;
            buf = ByteBufAllocator.DEFAULT.buffer(4 + totalLength);
            buf.writeInt(totalLength);
```

### DataFlowIssue
Dereference of `body` may produce `NullPointerException`
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/codec/ProtocolEncoder.java`
#### Snippet
```java
                    msgType |= FLAG_ALLOW_ENCRYPT;
                }
                totalLength = totalLength + body.length + msgAttrs.getBytes("utf8").length;
                buf = ByteBufAllocator.DEFAULT.buffer(4 + totalLength);
                buf.writeInt(totalLength);
```

### DataFlowIssue
Dereference of `body` may produce `NullPointerException`
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/codec/ProtocolEncoder.java`
#### Snippet
```java
                    msgType |= FLAG_ALLOW_ENCRYPT;
                }
                totalLength = totalLength + body.length + msgAttrs.getBytes("utf8").length;
                buf = ByteBufAllocator.DEFAULT.buffer(4 + totalLength);
                buf.writeInt(totalLength);
```

### DataFlowIssue
Method invocation `init` may produce `NullPointerException`
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/utils/EncryptUtil.java`
#### Snippet
```java
        }

        kg.init(AES_KEY_SIZE);

        SecretKey secretKey = kg.generateKey();
```

### DataFlowIssue
Dereference of `body` may produce `NullPointerException`
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/example/UdpClientExample.java`
#### Snippet
```java
                    msgType |= FLAG_ALLOW_COMPRESS;
                }
                totalLength = totalLength + body.length
                        + endAttr.getBytes("utf8").length;
                buf = ByteBufAllocator.DEFAULT.buffer(4 + totalLength);
```

### DataFlowIssue
Argument `build3DesKey(key)` might be null
in `inlong-sort/sort-core/src/main/java/org/apache/inlong/sort/function/EncryptFunction.java`
#### Snippet
```java
    public static String encrypt3DES(String data, String key) {
        try {
            SecretKey desKey = new SecretKeySpec(build3DesKey(key),
                    KEY_ALGORITHM_3DES);
            Cipher cipher = Cipher.getInstance(CIPHER_ALGORITHM_3DES);
```

### DataFlowIssue
Variable is already assigned to this value
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/config/ProxyConfigManager.java`
#### Snippet
```java
                        this.userEncryConfigEntry = encryptEntry;
                    } else {
                        encryptEntry = this.userEncryConfigEntry;
                    }
                }
```

### DataFlowIssue
Casting `o` to `StreamSplit` will produce `ClassCastException` for any non-null value
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/source/meta/split/StreamSplit.java`
#### Snippet
```java
            return false;
        }
        StreamSplit that = (StreamSplit) o;
        return totalFinishedSplitSize == that.totalFinishedSplitSize
                && Objects.equals(startingOffset, that.startingOffset)
```

### DataFlowIssue
Method invocation `pollSplitRecords` may produce `NullPointerException`
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/source/reader/IncrementalSourceSplitReader.java`
#### Snippet
```java
        Iterator<SourceRecords> dataIt = null;
        try {
            dataIt = currentFetcher.pollSplitRecords();
        } catch (InterruptedException e) {
            LOG.warn("fetch data failed.", e);
```

### DataFlowIssue
Method invocation `get` may produce `NullPointerException`
in `inlong-sort/sort-core/src/main/java/org/apache/inlong/sort/parser/impl/FlinkSqlParser.java`
#### Snippet
```java
    private String getFilterPrimaryKey(Node node) {
        if (node instanceof MongoExtractNode) {
            if (null != node.getProperties().get(SOURCE_MULTIPLE_ENABLE_KEY)
                    && node.getProperties().get(SOURCE_MULTIPLE_ENABLE_KEY).equals("true")) {
                return node.getPrimaryKey();
```

### DataFlowIssue
Variable is already assigned to this value
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/relational/connection/JdbcConnectionPools.java`
#### Snippet
```java
    public static synchronized JdbcConnectionPools getInstance(JdbcConnectionPoolFactory jdbcConnectionPoolFactory) {
        if (instance == null) {
            jdbcConnectionPoolFactory = jdbcConnectionPoolFactory;
            instance = new JdbcConnectionPools();
        }
```

### DataFlowIssue
Argument `record.getTableChangeDoc()` might be null
in `inlong-sort/sort-flink/cdc-base/src/main/java/org/apache/inlong/sort/cdc/base/debezium/internal/FlinkDatabaseSchemaHistory.java`
#### Snippet
```java
            TableChange tableChange =
                    FlinkJsonTableChangeSerializer.fromDocument(
                            record.getTableChangeDoc(), useCatalogBeforeSchema);
            schema.overwriteTable(tableChange.getTable());
        }
```

### DataFlowIssue
Method invocation `toString` may produce `NullPointerException`
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/hive/src/main/java/org/apache/inlong/sort/hive/HiveRowPartitionComputer.java`
#### Snippet
```java
            Object field = in.getField(index);
            String partitionValue =
                    field != null ? partColConversions[i].toHiveObject(field).toString() : null;
            if (StringUtils.isEmpty(partitionValue)) {
                partitionValue = defaultPartValue;
```

### DataFlowIssue
Method invocation `toString` may produce `NullPointerException`
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/hive/src/main/java/org/apache/inlong/sort/hive/HiveRowDataPartitionComputer.java`
#### Snippet
```java
            Object field = partitionConverters[i].toExternal(in, partitionIndexes[i]);
            String partitionValue =
                    field != null ? hiveObjectConversions[i].toHiveObject(field).toString() : null;
            if (StringUtils.isEmpty(partitionValue)) {
                partitionValue = defaultPartValue;
```

### DataFlowIssue
Argument `rowType` might be null
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/jdbc/src/main/java/org/apache/inlong/sort/jdbc/internal/JdbcMultiBatchingOutputFormat.java`
#### Snippet
```java
                JsonNode physicalData = jsonDynamicSchemaFormat.getPhysicalData(rootNode);
                List<Map<String, String>> physicalDataList = jsonDynamicSchemaFormat.jsonNode2Map(physicalData);
                record = generateRecord(rowType, physicalDataList.get(0));
                List<RowKind> rowKinds = jsonDynamicSchemaFormat
                        .opType2RowKind(jsonDynamicSchemaFormat.getOpType(rootNode));
```

### DataFlowIssue
Method invocation `addToBatch` may produce `NullPointerException`
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/jdbc/src/main/java/org/apache/inlong/sort/jdbc/internal/JdbcMultiBatchingOutputFormat.java`
#### Snippet
```java
                for (GenericRowData record : tableIdRecordList) {
                    totalDataSize = totalDataSize + record.toString().getBytes(StandardCharsets.UTF_8).length;
                    jdbcStatementExecutor.addToBatch((JdbcIn) record);
                }
                if (dirtySinkHelper.getDirtySink() != null) {
```

### DataFlowIssue
Method invocation `executeBatch` may produce `NullPointerException`
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/jdbc/src/main/java/org/apache/inlong/sort/jdbc/internal/JdbcMultiBatchingOutputFormat.java`
#### Snippet
```java
                    fillDirtyData(jdbcStatementExecutor, tableIdentifier);
                }
                jdbcStatementExecutor.executeBatch();
                flushFlag = true;
                if (dirtySinkHelper.getDirtySink() == null) {
```

### DataFlowIssue
Method invocation `addToBatch` may produce `NullPointerException`
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/jdbc/src/main/java/org/apache/inlong/sort/jdbc/internal/JdbcMultiBatchingOutputFormat.java`
#### Snippet
```java
                        try {
                            jdbcStatementExecutor = getOrCreateStatementExecutor(tableIdentifier);
                            jdbcStatementExecutor.addToBatch((JdbcIn) record);
                            jdbcStatementExecutor.executeBatch();
                            Long totalDataSize =
```

### DataFlowIssue
Argument `tableOptions.getOptional(SINK_MULTIPLE_PARTITION_PATTERN) .orEls...` might be null
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kafka/src/main/java/org/apache/inlong/sort/kafka/table/KafkaDynamicTableFactory.java`
#### Snippet
```java
                    new InLongFixedPartitionPartitioner<>(tableOptions.getOptional(PATTERN_PARTITION_MAP)
                            .orElse(null),
                            tableOptions.getOptional(SINK_MULTIPLE_PARTITION_PATTERN)
                                    .orElse(null));
            inLongFixedPartitionPartitioner.setSinkMultipleFormat(tableOptions.getOptional(SINK_MULTIPLE_FORMAT)
                    .orElse(null));
```

### DataFlowIssue
Dereference of `currentTransaction()` may produce `NullPointerException`
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kafka/src/main/java/org/apache/inlong/sort/kafka/FlinkKafkaProducer.java`
#### Snippet
```java
            if (currentTransaction() != null) {
                try {
                    currentTransaction().producer.close(Duration.ofSeconds(0));
                } catch (Throwable t) {
                    LOG.warn("Error closing producer.", t);
```

### DataFlowIssue
Argument `topics` might be null
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/pulsar/src/main/java/org/apache/inlong/sort/pulsar/table/PulsarDynamicTableFactory.java`
#### Snippet
```java
        String adminUrl = tableOptions.get(ADMIN_URL);
        String serverUrl = tableOptions.get(SERVICE_URL);
        return createPulsarTableSink(tableOptions, topics, adminUrl, serverUrl, keyEncodingFormat, valueEncodingFormat,
                properties, physicalDataType, keyProjection, valueProjection, keyPrefix, context);
    }
```

### DataFlowIssue
Method invocation `refreshFieldId` may produce `NullPointerException`
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/iceberg/src/main/java/org/apache/inlong/sort/iceberg/sink/multiple/DynamicSchemaHandleOperator.java`
#### Snippet
```java
                AtomicLong rowSize = new AtomicLong();
                RecordWithSchema recordWithSchema = queue.poll()
                        .refreshFieldId(latestSchema)
                        .refreshRowData((jsonNode, schema1) -> {
                            try {
```

### DataFlowIssue
Method invocation `getOriginalData` may produce `NullPointerException`
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/iceberg/src/main/java/org/apache/inlong/sort/iceberg/sink/multiple/DynamicSchemaHandleOperator.java`
#### Snippet
```java
                        .getSchemaUpdatePolicy()) {
                    RecordWithSchema recordWithSchema = queue.poll();
                    handleDirtyDataOfLogWithIgnore(recordWithSchema.getOriginalData(), dataSchema, tableId,
                            new NotSupportedException(
                                    String.format("SchemaUpdatePolicy %s does not support schema dynamic update!",
```

### DataFlowIssue
Method invocation `toBigDecimal` may produce `NullPointerException`
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/mysql/table/OldFieldMetadataConverter.java`
#### Snippet
```java
                }
                return DecimalData.fromBigDecimal(bigDecimal, precision, scale)
                        .toBigDecimal()
                        .toString();
            }
```

### DataFlowIssue
Method invocation `pollSplitRecords` may produce `NullPointerException`
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/mysql/source/reader/MySqlSplitReader.java`
#### Snippet
```java
        Iterator<SourceRecord> dataIt;
        try {
            dataIt = currentReader.pollSplitRecords();
        } catch (InterruptedException e) {
            LOG.warn("fetch data failed.", e);
```

### DataFlowIssue
Casting `value` to `GenericRowData` may produce `ClassCastException`
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/starrocks/src/main/java/org/apache/inlong/sort/starrocks/table/sink/StarRocksDynamicSinkFunction.java`
#### Snippet
```java

        if (multipleSink) {
            GenericRowData rowData = (GenericRowData) value;
            if (jsonDynamicSchemaFormat == null) {
                jsonDynamicSchemaFormat = (JsonDynamicSchemaFormat) DynamicSchemaFormatFactory.getFormat(
```

### DataFlowIssue
Method invocation `getField` may produce `NullPointerException`
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/starrocks/src/main/java/org/apache/inlong/sort/starrocks/table/sink/StarRocksDynamicSinkFunction.java`
#### Snippet
```java
                        this.sinkMultipleFormat);
            }
            JsonNode rootNode = jsonDynamicSchemaFormat.deserialize((byte[]) rowData.getField(0));
            boolean isDDL = jsonDynamicSchemaFormat.extractDDLFlag(rootNode);
            if (isDDL) {
```

### DataFlowIssue
Method invocation `factoryIdentifier` may produce `NullPointerException`
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/filesystem/src/main/java/org/apache/inlong/sort/filesystem/FileSystemTableSink.java`
#### Snippet
```java
            @Override
            public ReadableConfig getFormatOptions() {
                return formatOptions(formatFactory.factoryIdentifier());
            }

```

### DataFlowIssue
Argument `record.getTableChangeDoc()` might be null
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/oracle-cdc/src/main/java/org/apache/inlong/sort/cdc/oracle/debezium/internal/FlinkDatabaseSchemaHistory.java`
#### Snippet
```java
            TableChange tableChange =
                    FlinkJsonTableChangeSerializer.fromDocument(
                            record.getTableChangeDoc(), useCatalogBeforeSchema);
            schema.overwriteTable(tableChange.getTable());
        }
```

### DataFlowIssue
Passing a non-null argument to `Optional`
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mongodb-cdc/src/main/java/org/apache/inlong/sort/cdc/mongodb/table/MongoDBTableSource.java`
#### Snippet
```java
            Optional.ofNullable(inlongMetric).ifPresent(builder::inlongMetric);
            Optional.ofNullable(inlongAudit).ifPresent(builder::inlongAudit);
            Optional.ofNullable(sourceMultipleEnable).ifPresent(builder::migrateAll);
            DebeziumSourceFunction<RowData> sourceFunction = builder.build();

```

### DataFlowIssue
Array access `snapshotSplit.getSplitStart()[1]` may produce `NullPointerException`
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mongodb-cdc/src/main/java/org/apache/inlong/sort/cdc/mongodb/source/reader/fetch/MongoDBScanFetchTask.java`
#### Snippet
```java
                    collection
                            .find()
                            .min((BsonDocument) snapshotSplit.getSplitStart()[1])
                            .max((BsonDocument) snapshotSplit.getSplitEnd()[1])
                            .hint((BsonDocument) snapshotSplit.getSplitStart()[0])
```

### DataFlowIssue
Array access `snapshotSplit.getSplitEnd()[1]` may produce `NullPointerException`
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mongodb-cdc/src/main/java/org/apache/inlong/sort/cdc/mongodb/source/reader/fetch/MongoDBScanFetchTask.java`
#### Snippet
```java
                            .find()
                            .min((BsonDocument) snapshotSplit.getSplitStart()[1])
                            .max((BsonDocument) snapshotSplit.getSplitEnd()[1])
                            .hint((BsonDocument) snapshotSplit.getSplitStart()[0])
                            .batchSize(sourceConfig.getBatchSize())
```

### DataFlowIssue
Argument `record.getTableChangeDoc()` might be null
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mongodb-cdc/src/main/java/org/apache/inlong/sort/cdc/mongodb/debezium/internal/FlinkDatabaseSchemaHistory.java`
#### Snippet
```java
            TableChange tableChange =
                    FlinkJsonTableChangeSerializer.fromDocument(
                            record.getTableChangeDoc(), useCatalogBeforeSchema);
            schema.overwriteTable(tableChange.getTable());
        }
```

### DataFlowIssue
Method invocation `getTypeRoot` may produce `NullPointerException`
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mongodb-cdc/src/main/java/org/apache/inlong/sort/cdc/mongodb/debezium/table/MongoDBConnectorDeserializationSchema.java`
#### Snippet
```java
                    try {
                        LogicalType logicalType = RecordUtils.convertLogicType(value);
                        dataType.put(key, logicalType.getTypeRoot().name());
                        Object fieldValue = convertField(createConverter(logicalType), value);
                        if (fieldValue instanceof DecimalData) {
```

### DataFlowIssue
Method invocation `getTable` may produce `NullPointerException`
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/postgres-cdc/src/main/java/org/apache/inlong/sort/cdc/postgres/table/PostgreSQLReadableMetaData.java`
#### Snippet
```java

                    Map<StringData, Integer> postgresType = new HashMap<>();
                    final Table table = tableSchema.getTable();
                    table.columns().forEach(column -> {
                        postgresType.put(StringData.fromString(column.name()), column.jdbcType());
```

### DataFlowIssue
Method invocation `toString` may produce `NullPointerException`
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/elasticsearch-base/src/main/java/org/apache/inlong/sort/elasticsearch/table/KeyExtractor.java`
#### Snippet
```java
            default:
                RowData.FieldGetter fieldGetter = RowData.createFieldGetter(type, index);
                return (row) -> fieldGetter.getFieldOrNull(row).toString();
        }
    }
```

### DataFlowIssue
Method invocation `invokeDirty` may produce `NullPointerException`
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/elasticsearch-base/src/main/java/org/apache/inlong/sort/elasticsearch/table/MultipleElasticsearchSinkFunctionBase.java`
#### Snippet
```java
                        rowData.toString().getBytes(StandardCharsets.UTF_8).length);
            } else {
                sinkMetricData.invokeDirty(1, rowData.toString().getBytes(StandardCharsets.UTF_8).length);
            }
        }
```

### DataFlowIssue
Variable is already assigned to this value
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/sinks/SenderManager.java`
#### Snippet
```java
                suc = true;
            } catch (Exception exception) {
                suc = false;
                if (retry > maxSenderRetry) {
                    LOGGER.warn("max retry reached, retry count is {}, sleep and send again", retry);
```

### DataFlowIssue
Method invocation `getKey` may produce `NullPointerException`
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/sources/reader/PostgreSQLReader.java`
#### Snippet
```java
        Pair<String, String> message = postgreSQLMessageQueue.poll();
        Map<String, String> header = new HashMap<>(DEFAULT_MAP_CAPACITY);
        header.put(PROXY_KEY_DATA, message.getKey());
        return new DefaultMessage(message.getValue().getBytes(StandardCharsets.UTF_8), header);
    }
```

### DataFlowIssue
Method invocation `getKey` may produce `NullPointerException`
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/sources/reader/BinlogReader.java`
#### Snippet
```java
        Pair<String, String> message = binlogMessagesQueue.poll();
        Map<String, String> header = new HashMap<>(DEFAULT_MAP_CAPACITY);
        header.put(PROXY_KEY_DATA, message.getKey());
        return new DefaultMessage(message.getValue().getBytes(StandardCharsets.UTF_8), header);
    }
```

### DataFlowIssue
Argument `file.listFiles()` might be null
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/trigger/PathPattern.java`
#### Snippet
```java
                suitableFiles.add(file);
            } else if (file.isDirectory()) {
                Stream.of(file.listFiles()).forEach(subFile -> walkSuitableFiles(suitableFiles, subFile, maxNum));
            }
        }
```

### DataFlowIssue
Method invocation `getBytes` may produce `NullPointerException`
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/sources/reader/RedisReader.java`
#### Snippet
```java
        if (!redisMessageQueue.isEmpty()) {
            readerMetric.pluginReadCount.incrementAndGet();
            return new DefaultMessage(redisMessageQueue.poll().getBytes());
        } else {
            return null;
```

### DataFlowIssue
Method invocation `isSuccess` may produce `NullPointerException`
in `inlong-audit/audit-sdk/src/main/java/org/apache/inlong/audit/send/SenderGroup.java`
#### Snippet
```java
                dataBuf.release();
            }
            return new SenderResult(channel.getIpPort().ip, channel.getIpPort().port, t.isSuccess());
        } catch (Throwable ex) {
            logger.error(ex.getMessage());
```

### DataFlowIssue
Argument `topic` might be null
in `inlong-audit/audit-proxy/src/main/java/org/apache/inlong/audit/sink/TubeSink.java`
#### Snippet
```java
                        if (message != null && (message.contains("No available queue for topic")
                                || message.contains("The brokers of topic are all forbidden"))) {
                            illegalTopicMap.put(topic, System.currentTimeMillis() + 60 * 1000);
                            logger.info("IllegalTopicMap.put " + topic);
                            continue;
```

### DataFlowIssue
Dereference of `mqInfoList` may produce `NullPointerException`
in `inlong-audit/audit-store/src/main/java/org/apache/inlong/audit/service/AuditMsgConsumerServer.java`
#### Snippet
```java
        List<InsertData> insertServiceList = this.getInsertServiceList();

        for (MQInfo mqInfo : mqInfoList) {
            if (mqConfig.isPulsar() && MQType.PULSAR.equals(mqInfo.getMqType())) {
                mqConfig.setPulsarServerUrl(mqInfo.getUrl());
```

### DataFlowIssue
Method invocation `start` may produce `NullPointerException`
in `inlong-audit/audit-store/src/main/java/org/apache/inlong/audit/service/AuditMsgConsumerServer.java`
#### Snippet
```java
            ckService.start();
        }
        mqConsume.start();
    }

```

### DataFlowIssue
Argument `domainMap.getOrDefault(MetricListener.KEY_SNAPSHOT_INTERVAL, "60000")` might be null
in `inlong-common/src/main/java/org/apache/inlong/common/metric/MetricObserver.java`
#### Snippet
```java
            // get snapshot interval
            long snapshotInterval = Long.parseLong(
                    domainMap.getOrDefault(MetricListener.KEY_SNAPSHOT_INTERVAL, "60000"));
            LOG.info("begin to register domain:{}, MetricListeners:{}, snapshotInterval:{}",
                    domain, listenerList, snapshotInterval);
```

### DataFlowIssue
Method invocation `hasNext` may produce `NullPointerException`
in `inlong-common/src/main/java/org/apache/inlong/common/msg/InLongMsg.java`
#### Snippet
```java
        Iterator<ByteBuffer> it = getIteratorBuffer(data);
        setCheckMode(false);
        while (it.hasNext()) {
            res = this.addMsg(attr, it.next());
        }
```

### DataFlowIssue
Variable is already assigned to this value
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/policies/FlowCtrlRuleHandler.java`
#### Snippet
```java
                    case 0:
                    default:
                        typeVal = 0;
                        flowCtrlItemList = parseDataLimit(recordNo, typeVal, jsonObject);
                        break;
```

### DataFlowIssue
Variable is already assigned to this value
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/BaseMessageConsumer.java`
#### Snippet
```java
            }
            default: {
                readStatus = TBaseConstants.CONSUME_MODEL_READ_NORMAL;
            }
        }
```

### DataFlowIssue
Switch label `TErrCodeConstants.SUCCESS` is unreachable
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/BaseMessageConsumer.java`
#### Snippet
```java
            // Process the response based on the return code
            switch (msgRspB2C.getErrCode()) {
                case TErrCodeConstants.SUCCESS: {
                    int msgSize = 0;
                    int msgCount = 0;
```

### DataFlowIssue
Switch label `TErrCodeConstants.HB_NO_NODE` is unreachable
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/BaseMessageConsumer.java`
#### Snippet
```java
                    break;
                }
                case TErrCodeConstants.HB_NO_NODE:
                case TErrCodeConstants.CERTIFICATE_FAILURE:
                case TErrCodeConstants.DUPLICATE_PARTITION: {
```

### DataFlowIssue
Switch label `TErrCodeConstants.CERTIFICATE_FAILURE` is unreachable
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/BaseMessageConsumer.java`
#### Snippet
```java
                }
                case TErrCodeConstants.HB_NO_NODE:
                case TErrCodeConstants.CERTIFICATE_FAILURE:
                case TErrCodeConstants.DUPLICATE_PARTITION: {
                    // Release the partitions when meeting these error codes
```

### DataFlowIssue
Switch label `TErrCodeConstants.DUPLICATE_PARTITION` is unreachable
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/BaseMessageConsumer.java`
#### Snippet
```java
                case TErrCodeConstants.HB_NO_NODE:
                case TErrCodeConstants.CERTIFICATE_FAILURE:
                case TErrCodeConstants.DUPLICATE_PARTITION: {
                    // Release the partitions when meeting these error codes
                    removePartition(partition);
```

### DataFlowIssue
Switch label `TErrCodeConstants.SERVER_CONSUME_SPEED_LIMIT` is unreachable
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/BaseMessageConsumer.java`
#### Snippet
```java
                    break;
                }
                case TErrCodeConstants.SERVER_CONSUME_SPEED_LIMIT: {
                    // Process with server side speed limit
                    long defDltTime =
```

### DataFlowIssue
Switch label `TErrCodeConstants.NOT_FOUND` is unreachable
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/BaseMessageConsumer.java`
#### Snippet
```java
                    break;
                }
                case TErrCodeConstants.NOT_FOUND:
                case TErrCodeConstants.FORBIDDEN:
                case TErrCodeConstants.SERVICE_UNAVAILABLE:
```

### DataFlowIssue
Switch label `TErrCodeConstants.FORBIDDEN` is unreachable
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/BaseMessageConsumer.java`
#### Snippet
```java
                }
                case TErrCodeConstants.NOT_FOUND:
                case TErrCodeConstants.FORBIDDEN:
                case TErrCodeConstants.SERVICE_UNAVAILABLE:
                case TErrCodeConstants.MOVED:
```

### DataFlowIssue
Switch label `TErrCodeConstants.SERVICE_UNAVAILABLE` is unreachable
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/BaseMessageConsumer.java`
#### Snippet
```java
                case TErrCodeConstants.NOT_FOUND:
                case TErrCodeConstants.FORBIDDEN:
                case TErrCodeConstants.SERVICE_UNAVAILABLE:
                case TErrCodeConstants.MOVED:
                default: {
```

### DataFlowIssue
Switch label `TErrCodeConstants.MOVED` is unreachable
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/BaseMessageConsumer.java`
#### Snippet
```java
                case TErrCodeConstants.FORBIDDEN:
                case TErrCodeConstants.SERVICE_UNAVAILABLE:
                case TErrCodeConstants.MOVED:
                default: {
                    // Slow down the request based on the limitation configuration when meet these errors
```

### DataFlowIssue
Switch label `TErrCodeConstants.FORBIDDEN` is unreachable
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/BaseMessageConsumer.java`
#### Snippet
```java
                    long limitDlt = 300;
                    switch (msgRspB2C.getErrCode()) {
                        case TErrCodeConstants.FORBIDDEN: {
                            limitDlt = 2000;
                            break;
```

### DataFlowIssue
Switch label `TErrCodeConstants.SERVICE_UNAVAILABLE` is unreachable
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/BaseMessageConsumer.java`
#### Snippet
```java
                            break;
                        }
                        case TErrCodeConstants.SERVICE_UNAVAILABLE: {
                            limitDlt = 300;
                            break;
```

### DataFlowIssue
Switch label `TErrCodeConstants.MOVED` is unreachable
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/BaseMessageConsumer.java`
#### Snippet
```java
                            break;
                        }
                        case TErrCodeConstants.MOVED: {
                            limitDlt = 200;
                            break;
```

### DataFlowIssue
Switch label `TErrCodeConstants.NOT_FOUND` is unreachable
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/BaseMessageConsumer.java`
#### Snippet
```java
                            break;
                        }
                        case TErrCodeConstants.NOT_FOUND: {
                            limitDlt = consumerConfig.getMsgNotFoundWaitPeriodMs();
                            break;
```

### DataFlowIssue
Variable is already assigned to this value
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/SimpleClientBalanceConsumer.java`
#### Snippet
```java
            }
            default: {
                readStatus = TBaseConstants.CONSUME_MODEL_READ_NORMAL;
            }
        }
```

### DataFlowIssue
Switch label `TErrCodeConstants.SUCCESS` is unreachable
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/SimpleClientBalanceConsumer.java`
#### Snippet
```java
            // Process the response based on the return code
            switch (msgRspB2C.getErrCode()) {
                case TErrCodeConstants.SUCCESS: {
                    int msgSize = 0;
                    int msgCount = 0;
```

### DataFlowIssue
Switch label `TErrCodeConstants.HB_NO_NODE` is unreachable
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/SimpleClientBalanceConsumer.java`
#### Snippet
```java
                    break;
                }
                case TErrCodeConstants.HB_NO_NODE:
                case TErrCodeConstants.CERTIFICATE_FAILURE:
                case TErrCodeConstants.DUPLICATE_PARTITION: {
```

### DataFlowIssue
Switch label `TErrCodeConstants.CERTIFICATE_FAILURE` is unreachable
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/SimpleClientBalanceConsumer.java`
#### Snippet
```java
                }
                case TErrCodeConstants.HB_NO_NODE:
                case TErrCodeConstants.CERTIFICATE_FAILURE:
                case TErrCodeConstants.DUPLICATE_PARTITION: {
                    // Release the partitions when meeting these error codes
```

### DataFlowIssue
Switch label `TErrCodeConstants.DUPLICATE_PARTITION` is unreachable
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/SimpleClientBalanceConsumer.java`
#### Snippet
```java
                case TErrCodeConstants.HB_NO_NODE:
                case TErrCodeConstants.CERTIFICATE_FAILURE:
                case TErrCodeConstants.DUPLICATE_PARTITION: {
                    // Release the partitions when meeting these error codes
                    clientRmtDataCache.removePartition(partition);
```

### DataFlowIssue
Switch label `TErrCodeConstants.SERVER_CONSUME_SPEED_LIMIT` is unreachable
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/SimpleClientBalanceConsumer.java`
#### Snippet
```java
                    break;
                }
                case TErrCodeConstants.SERVER_CONSUME_SPEED_LIMIT: {
                    // Process with server side speed limit
                    long defDltTime =
```

### DataFlowIssue
Switch label `TErrCodeConstants.NOT_FOUND` is unreachable
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/SimpleClientBalanceConsumer.java`
#### Snippet
```java
                    break;
                }
                case TErrCodeConstants.NOT_FOUND:
                case TErrCodeConstants.FORBIDDEN:
                case TErrCodeConstants.SERVICE_UNAVAILABLE:
```

### DataFlowIssue
Switch label `TErrCodeConstants.FORBIDDEN` is unreachable
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/SimpleClientBalanceConsumer.java`
#### Snippet
```java
                }
                case TErrCodeConstants.NOT_FOUND:
                case TErrCodeConstants.FORBIDDEN:
                case TErrCodeConstants.SERVICE_UNAVAILABLE:
                case TErrCodeConstants.MOVED:
```

### DataFlowIssue
Switch label `TErrCodeConstants.SERVICE_UNAVAILABLE` is unreachable
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/SimpleClientBalanceConsumer.java`
#### Snippet
```java
                case TErrCodeConstants.NOT_FOUND:
                case TErrCodeConstants.FORBIDDEN:
                case TErrCodeConstants.SERVICE_UNAVAILABLE:
                case TErrCodeConstants.MOVED:
                default: {
```

### DataFlowIssue
Switch label `TErrCodeConstants.MOVED` is unreachable
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/SimpleClientBalanceConsumer.java`
#### Snippet
```java
                case TErrCodeConstants.FORBIDDEN:
                case TErrCodeConstants.SERVICE_UNAVAILABLE:
                case TErrCodeConstants.MOVED:
                default: {
                    // Slow down the request based on the limitation configuration when meet these errors
```

### DataFlowIssue
Switch label `TErrCodeConstants.FORBIDDEN` is unreachable
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/SimpleClientBalanceConsumer.java`
#### Snippet
```java
                    long limitDlt = 300;
                    switch (msgRspB2C.getErrCode()) {
                        case TErrCodeConstants.FORBIDDEN: {
                            limitDlt = 2000;
                            break;
```

### DataFlowIssue
Switch label `TErrCodeConstants.SERVICE_UNAVAILABLE` is unreachable
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/SimpleClientBalanceConsumer.java`
#### Snippet
```java
                            break;
                        }
                        case TErrCodeConstants.SERVICE_UNAVAILABLE: {
                            limitDlt = 300;
                            break;
```

### DataFlowIssue
Switch label `TErrCodeConstants.MOVED` is unreachable
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/SimpleClientBalanceConsumer.java`
#### Snippet
```java
                            break;
                        }
                        case TErrCodeConstants.MOVED: {
                            limitDlt = 200;
                            break;
```

### DataFlowIssue
Switch label `TErrCodeConstants.NOT_FOUND` is unreachable
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/SimpleClientBalanceConsumer.java`
#### Snippet
```java
                            break;
                        }
                        case TErrCodeConstants.NOT_FOUND: {
                            limitDlt = consumerConfig.getMsgNotFoundWaitPeriodMs();
                            break;
```

### DataFlowIssue
Variable is already assigned to this value
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/utils/WebParameterUtils.java`
#### Snippet
```java
                isAcceptSubscribe = true;
            } else if (manageStatus == TStatusConstants.STATUS_MANAGE_ONLINE_NOT_WRITE) {
                isAcceptPublish = false;
                isAcceptSubscribe = true;
            } else if (manageStatus == TStatusConstants.STATUS_MANAGE_ONLINE_NOT_READ) {
```

### DataFlowIssue
Variable is already assigned to this value
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/utils/WebParameterUtils.java`
#### Snippet
```java
            } else if (manageStatus == TStatusConstants.STATUS_MANAGE_ONLINE_NOT_READ) {
                isAcceptPublish = true;
                isAcceptSubscribe = false;
            }
        }
```

### DataFlowIssue
Method invocation `decryptPassword` may produce `NullPointerException`
in `inlong-manager/manager-pojo/src/main/java/org/apache/inlong/manager/pojo/sink/es/ElasticsearchSinkDTO.java`
#### Snippet
```java
    public static ElasticsearchSinkDTO getFromJson(@NotNull String extParams) {
        try {
            return JsonUtils.parseObject(extParams, ElasticsearchSinkDTO.class).decryptPassword();
        } catch (Exception e) {
            throw new BusinessException(ErrorCodeEnum.SINK_INFO_INCORRECT,
```

### DataFlowIssue
Method invocation `decryptPassword` may produce `NullPointerException`
in `inlong-manager/manager-pojo/src/main/java/org/apache/inlong/manager/pojo/sink/hive/HiveSinkDTO.java`
#### Snippet
```java
    public static HiveSinkDTO getFromJson(@NotNull String extParams) {
        try {
            return JsonUtils.parseObject(extParams, HiveSinkDTO.class).decryptPassword();
        } catch (Exception e) {
            throw new BusinessException(ErrorCodeEnum.SINK_INFO_INCORRECT,
```

### DataFlowIssue
Method invocation `decryptPassword` may produce `NullPointerException`
in `inlong-manager/manager-pojo/src/main/java/org/apache/inlong/manager/pojo/sink/doris/DorisSinkDTO.java`
#### Snippet
```java
    public static DorisSinkDTO getFromJson(@NotNull String extParams) {
        try {
            return JsonUtils.parseObject(extParams, DorisSinkDTO.class).decryptPassword();
        } catch (Exception e) {
            throw new BusinessException(ErrorCodeEnum.SINK_INFO_INCORRECT,
```

### DataFlowIssue
Method invocation `decryptPassword` may produce `NullPointerException`
in `inlong-manager/manager-pojo/src/main/java/org/apache/inlong/manager/pojo/sink/postgresql/PostgreSQLSinkDTO.java`
#### Snippet
```java
    public static PostgreSQLSinkDTO getFromJson(@NotNull String extParams) {
        try {
            return JsonUtils.parseObject(extParams, PostgreSQLSinkDTO.class).decryptPassword();
        } catch (Exception e) {
            throw new BusinessException(ErrorCodeEnum.SINK_INFO_INCORRECT,
```

### DataFlowIssue
Method invocation `getPath` may produce `NullPointerException`
in `inlong-manager/manager-common/src/main/java/org/apache/inlong/manager/common/util/AESUtils.java`
#### Snippet
```java
    private static Properties getApplicationProperties() throws IOException {
        Properties properties = new Properties();
        String path = Thread.currentThread().getContextClassLoader().getResource("").getPath() + CONFIG_FILE;
        try (InputStream inputStream = new BufferedInputStream(Files.newInputStream(Paths.get(path)))) {
            properties.load(inputStream);
```

### DataFlowIssue
Method invocation `getPath` may produce `NullPointerException`
in `inlong-manager/manager-plugins/src/main/java/org/apache/inlong/manager/plugin/flink/FlinkOperation.java`
#### Snippet
```java
        if (properties == null) {
            properties = new Properties();
            String path = Thread.currentThread().getContextClassLoader().getResource("").getPath() + CONFIG_FILE;
            try (InputStream inputStream = new BufferedInputStream(Files.newInputStream(Paths.get(path)))) {
                properties.load(inputStream);
```

### DataFlowIssue
Dereference of `clusterMode` may produce `NullPointerException`
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/node/redis/RedisDataNodeOperator.java`
#### Snippet
```java
        RedisClusterMode clusterMode = RedisClusterMode.of(redisDataNodeRequest.getClusterMode());

        switch (clusterMode) {
            case STANDALONE:
                String host = redisDataNodeRequest.getHost();
```

### DataFlowIssue
Dereference of `redisClusterMode` may produce `NullPointerException`
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/sink/redis/RedisSinkOperator.java`
#### Snippet
```java
                "Redis ClusterMode must in one of " + Arrays.toString(RedisClusterMode.values()) + " !");

        switch (redisClusterMode) {
            case CLUSTER:
                String clusterNodes = sinkRequest.getClusterNodes();
```

### DataFlowIssue
Method invocation `getInlongGroupId` will produce `NullPointerException`
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/source/StreamSourceServiceImpl.java`
#### Snippet
```java
        if (groupEntity == null) {
            throw new BusinessException(ErrorCodeEnum.GROUP_NOT_FOUND,
                    String.format("InlongGroup does not exist with InlongGroupId=%s", groupEntity.getInlongGroupId()));
        }
        // only the person in charges can query
```

### DataFlowIssue
Method invocation `getPath` may produce `NullPointerException`
in `inlong-manager/manager-client-tools/src/main/java/org/apache/inlong/manager/client/cli/util/ClientUtils.java`
#### Snippet
```java
    private static void initClientConfiguration() {
        Properties properties = new Properties();
        String path = Thread.currentThread().getContextClassLoader().getResource("").getPath() + CONFIG_FILE;
        try (InputStream inputStream = new BufferedInputStream(Files.newInputStream(Paths.get(path)))) {
            properties.load(inputStream);
```

### DataFlowIssue
Method invocation `getSize` may produce `NullPointerException`
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/sink/mq/BatchPackManager.java`
#### Snippet
```java
                    event.getInlongStreamId(), dispatchTime);
            BatchPackProfile oldDispatchProfile = this.profileCache.put(dispatchKey, newDispatchProfile);
            this.dispatchQueue.acquire(oldDispatchProfile.getSize());
            this.dispatchQueue.offer(oldDispatchProfile);
            outCounter.addAndGet(dispatchProfile.getCount());
```

### DataFlowIssue
Argument `getClass().getClassLoader().getResource(fileName)` might be null
in `inlong-sort-standalone/sort-standalone-common/src/main/java/org/apache/inlong/sort/standalone/config/loader/ClassResourceSortClusterConfigLoader.java`
#### Snippet
```java
                fileName = context.getString(SortClusterConfigType.KEY_FILE, SortClusterConfigType.DEFAULT_FILE);
            }
            String confString = IOUtils.toString(getClass().getClassLoader().getResource(fileName),
                    Charset.defaultCharset());
            int index = confString.indexOf('{');
```

### DataFlowIssue
Argument `getClass().getClassLoader().getResource(fileName)` might be null
in `inlong-sort-standalone/sort-standalone-common/src/main/java/org/apache/inlong/sort/standalone/config/loader/ClassResourceQueryConsumeConfig.java`
#### Snippet
```java
        String fileName = sortTaskId + ".conf";
        try {
            String confString = IOUtils.toString(getClass().getClassLoader().getResource(fileName),
                    Charset.defaultCharset());
            ObjectMapper objectMapper = new ObjectMapper();
```

### DataFlowIssue
Method invocation `openStream` may produce `NullPointerException`
in `inlong-sort-standalone/sort-standalone-common/src/main/java/org/apache/inlong/sort/standalone/config/loader/ClassResourceCommonPropertiesLoader.java`
#### Snippet
```java
    protected Map<String, String> loadProperties(String fileName) {
        Map<String, String> result = new ConcurrentHashMap<>();
        try (InputStream inStream = getClass().getClassLoader().getResource(fileName).openStream()) {
            Properties props = new Properties();
            props.load(inStream);
```

### DataFlowIssue
Variable is already assigned to this value
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/hive/PartitionCreateRunnable.java`
#### Snippet
```java
            }
            this.concatInFiles2OuttmpFile(idFile, concatInFiles, fs);
            outputFileSize = 0;
            concatInFiles.clear();
        }
```

### DataFlowIssue
Variable is already assigned to this value
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/hive/PartitionCreateRunnable.java`
#### Snippet
```java
        if (concatInFiles.size() > 0) {
            this.concatInFiles2OuttmpFile(idFile, concatInFiles, fs);
            outputFileSize = 0;
            concatInFiles.clear();
        }
```

### DataFlowIssue
Method invocation `getSinkParams` may produce `NullPointerException`
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/hive/HiveSinkContext.java`
#### Snippet
```java
            }
            this.sortTaskConfig = newSortTaskConfig;
            this.parentContext = new Context(this.sortTaskConfig.getSinkParams());
            // parse the config of id and topic
            Map<String, HdfsIdConfig> newIdConfigMap = new ConcurrentHashMap<>();
```

### DataFlowIssue
Method invocation `getSinkParams` may produce `NullPointerException`
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/pulsar/PulsarFederationSinkContext.java`
#### Snippet
```java
            }
            this.sortTaskConfig = newSortTaskConfig;
            this.producerContext = new Context(this.sortTaskConfig.getSinkParams());
            // parse the config of id and topic
            Map<String, PulsarIdConfig> newIdConfigMap = new ConcurrentHashMap<>();
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/source/sortsdk/SubscribeFetchResult.java`
#### Snippet
```java
        this.headers.put(Constants.HEADER_KEY_MSG_TIME, String.valueOf(message.getRecTime()));
        // TODO to fix here
        this.headers.putAll(null);
        this.body = null;
    }
```

### DataFlowIssue
Method invocation `getSinkParams` may produce `NullPointerException`
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/elasticsearch/EsSinkContext.java`
#### Snippet
```java
                    new ObjectMapper().writeValueAsString(newSortTaskConfig));
            this.sortTaskConfig = newSortTaskConfig;
            this.sinkContext = new Context(this.sortTaskConfig.getSinkParams());
            // parse the config of id and topic
            Map<String, EsIdConfig> newIdConfigMap = new ConcurrentHashMap<>();
```

### DataFlowIssue
Argument `oldDispatchProfile` might be null
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/dispatch/DispatchManager.java`
#### Snippet
```java
            long curTime = System.currentTimeMillis();
            this.checkAndResetDispatchTime(dispatchProfile, curTime);
            this.dispatchQueue.offer(oldDispatchProfile);
            outCounter.addAndGet(dispatchProfile.getCount());
            newDispatchProfile.addEvent(event, maxPackCount, maxPackSize);
```

### DataFlowIssue
Method invocation `getIdParams` may produce `NullPointerException`
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/clickhouse/ClickHouseSinkContext.java`
#### Snippet
```java
            // parse the config of id and topic
            Map<String, ClickHouseIdConfig> newIdConfigMap = new ConcurrentHashMap<>();
            List<Map<String, String>> idList = newSortTaskConfig.getIdParams();
            ObjectMapper objectMapper = new ObjectMapper();
            for (Map<String, String> idParam : idList) {
```

## RuleId[id=SpringBootApplicationProperties]
### SpringBootApplicationProperties
Deprecated configuration property 'spring.datasource.schema'
in `inlong-audit/audit-store/src/test/resources/application-test.properties`
#### Snippet
```java
spring.datasource.type=com.alibaba.druid.pool.DruidDataSource
spring.datasource.druid.driver-class-name=org.h2.Driver
spring.datasource.schema=classpath:sql/apache_inlong_audit.sql
spring.datasource.druid.url=jdbc:h2:mem:test;MODE=MYSQL;DB_CLOSE_DELAY=-1;IGNORECASE=TRUE;
spring.datasource.druid.username=root
```

### SpringBootApplicationProperties
Cannot resolve configuration property 'audit.config.file.check.enable'
in `inlong-audit/audit-store/src/test/resources/application-test.properties`
#### Snippet
```java

# audit config
audit.config.file.check.enable=false
audit.config.manager.server.url=http://127.0.0.1:8000

```

### SpringBootApplicationProperties
Cannot resolve configuration property 'audit.config.manager.server.url'
in `inlong-audit/audit-store/src/test/resources/application-test.properties`
#### Snippet
```java
# audit config
audit.config.file.check.enable=false
audit.config.manager.server.url=http://127.0.0.1:8000

# store.server: elasticsearch / mysql
```

### SpringBootApplicationProperties
Cannot resolve configuration property 'server.host'
in `inlong-manager/manager-web/src/test/resources/application.properties`
#### Snippet
```java
#

server.host=127.0.0.1
server.port=8083
default.admin.user=admin
```

### SpringBootApplicationProperties
Cannot resolve configuration property 'default.admin.user'
in `inlong-manager/manager-web/src/test/resources/application.properties`
#### Snippet
```java
server.host=127.0.0.1
server.port=8083
default.admin.user=admin
default.admin.password=inlong
server.servlet.context-path=/inlong/manager
```

### SpringBootApplicationProperties
Cannot resolve configuration property 'default.admin.password'
in `inlong-manager/manager-web/src/test/resources/application.properties`
#### Snippet
```java
server.port=8083
default.admin.user=admin
default.admin.password=inlong
server.servlet.context-path=/inlong/manager
spring.application.name=InlongManagerMain
```

### SpringBootApplicationProperties
Cannot resolve configuration property 'inlong.auth.type'
in `inlong-manager/manager-web/src/test/resources/application.properties`
#### Snippet
```java

# Configure auth plugin
inlong.auth.type=default

# Encryption config, the suffix of value must be the same as the version.
```

### SpringBootApplicationProperties
Cannot resolve configuration property 'inlong.encrypt.key.value1'
in `inlong-manager/manager-web/src/test/resources/application.properties`
#### Snippet
```java
# Encryption config, the suffix of value must be the same as the version.
inlong.encrypt.version=1
inlong.encrypt.key.value1="I!N@L#O$N%G^"

# clients (e.g. agent and dataproxy) must be authenticated by secretId and secretKey if turned on
```

### SpringBootApplicationProperties
Cannot resolve configuration property 'server.host'
in `inlong-manager/manager-web/src/main/resources/application.properties`
#### Snippet
```java
# under the License.

server.host=127.0.0.1
server.port=8083
default.admin.user=admin
```

### SpringBootApplicationProperties
Cannot resolve configuration property 'default.admin.user'
in `inlong-manager/manager-web/src/main/resources/application.properties`
#### Snippet
```java
server.host=127.0.0.1
server.port=8083
default.admin.user=admin
default.admin.password=inlong
server.servlet.context-path=/inlong/manager
```

### SpringBootApplicationProperties
Cannot resolve configuration property 'default.admin.password'
in `inlong-manager/manager-web/src/main/resources/application.properties`
#### Snippet
```java
server.port=8083
default.admin.user=admin
default.admin.password=inlong
server.servlet.context-path=/inlong/manager
spring.application.name=InlongManagerMain
```

### SpringBootApplicationProperties
Cannot resolve configuration property 'inlong.auth.type'
in `inlong-manager/manager-web/src/main/resources/application.properties`
#### Snippet
```java

# Configure auth plugin
inlong.auth.type=default

# Encryption config, the suffix of value must be the same as the version.
```

### SpringBootApplicationProperties
Cannot resolve configuration property 'inlong.encrypt.key.value1'
in `inlong-manager/manager-web/src/main/resources/application.properties`
#### Snippet
```java
# Encryption config, the suffix of value must be the same as the version.
inlong.encrypt.version=1
inlong.encrypt.key.value1="I!N@L#O$N%G^"

# Clients (e.g. agent and dataproxy) must be authenticated by secretId and secretKey if turned on
```

### SpringBootApplicationProperties
Cannot resolve configuration property 'es.index.search.port'
in `inlong-manager/manager-web/src/main/resources/application-test.properties`
#### Snippet
```java
es.index.search.hostname=127.0.0.1
# Elasticsearch port
es.index.search.port=9200
# Elasticsearch support authentication flag
es.auth.enable=false
```

### SpringBootApplicationProperties
Cannot resolve configuration property 'es.index.search.port'
in `inlong-manager/manager-web/src/main/resources/application-dev.properties`
#### Snippet
```java
es.index.search.hostname=127.0.0.1
# Elasticsearch port
es.index.search.port=9200
# Elasticsearch support authentication flag
es.auth.enable=false
```

### SpringBootApplicationProperties
Cannot resolve configuration property 'es.index.search.port'
in `inlong-manager/manager-web/src/main/resources/application-prod.properties`
#### Snippet
```java
es.index.search.hostname=127.0.0.1
# Elasticsearch port
es.index.search.port=9200
# Elasticsearch support authentication flag
es.auth.enable=false
```

### SpringBootApplicationProperties
Cannot resolve configuration property 'audit.query.source'
in `inlong-manager/manager-test/src/main/resources/application-unit-test.properties`
#### Snippet
```java
# Audit configuration
# Audit query source that decide what data source to query, currently only supports [MYSQL|ELASTICSEARCH|CLICKHOUSE]
audit.query.source=MYSQL

# Elasticsearch config
```

### SpringBootApplicationProperties
Cannot resolve configuration property 'es.index.search.hostname'
in `inlong-manager/manager-test/src/main/resources/application-unit-test.properties`
#### Snippet
```java
# Elasticsearch config
# Elasticsearch host split by coma if more than one host, such as 'host1,host2'
es.index.search.hostname=127.0.0.1
# Elasticsearch port
es.index.search.port=9200
```

### SpringBootApplicationProperties
Cannot resolve configuration property 'es.index.search.port'
in `inlong-manager/manager-test/src/main/resources/application-unit-test.properties`
#### Snippet
```java
es.index.search.hostname=127.0.0.1
# Elasticsearch port
es.index.search.port=9200
# Elasticsearch support authentication flag
es.auth.enable=false
```

### SpringBootApplicationProperties
Cannot resolve configuration property 'es.auth.enable'
in `inlong-manager/manager-test/src/main/resources/application-unit-test.properties`
#### Snippet
```java
es.index.search.port=9200
# Elasticsearch support authentication flag
es.auth.enable=false
# Elasticsearch user of authentication info
es.auth.user=admin
```

### SpringBootApplicationProperties
Cannot resolve configuration property 'es.auth.user'
in `inlong-manager/manager-test/src/main/resources/application-unit-test.properties`
#### Snippet
```java
es.auth.enable=false
# Elasticsearch user of authentication info
es.auth.user=admin
# Elasticsearch password of authentication info
es.auth.password=inlong
```

### SpringBootApplicationProperties
Cannot resolve configuration property 'es.auth.password'
in `inlong-manager/manager-test/src/main/resources/application-unit-test.properties`
#### Snippet
```java
es.auth.user=admin
# Elasticsearch password of authentication info
es.auth.password=inlong

# ClickHouse config
```

### SpringBootApplicationProperties
Cannot resolve configuration property 'audit.ck.jdbcUrl'
in `inlong-manager/manager-test/src/main/resources/application-unit-test.properties`
#### Snippet
```java
# ClickHouse config
# ClickHouse jdbcUrl
audit.ck.jdbcUrl=jdbc:clickhouse://127.0.0.1:8123/apache_inlong_audit
# ClickHouse username
audit.ck.username=default
```

### SpringBootApplicationProperties
Cannot resolve configuration property 'audit.ck.username'
in `inlong-manager/manager-test/src/main/resources/application-unit-test.properties`
#### Snippet
```java
audit.ck.jdbcUrl=jdbc:clickhouse://127.0.0.1:8123/apache_inlong_audit
# ClickHouse username
audit.ck.username=default
# ClickHouse password
audit.ck.password=
```

### SpringBootApplicationProperties
Cannot resolve configuration property 'audit.ck.password'
in `inlong-manager/manager-test/src/main/resources/application-unit-test.properties`
#### Snippet
```java
audit.ck.username=default
# ClickHouse password
audit.ck.password=

# Configure mybatis
```

### SpringBootApplicationProperties
Cannot resolve configuration property 'mybatis.mapper-locations'
in `inlong-manager/manager-test/src/main/resources/application-unit-test.properties`
#### Snippet
```java

# Configure mybatis
mybatis.mapper-locations=classpath:mappers/*.xml
# Global mapping, no need to write the full path of the entity class in the xml file
mybatis.type-aliases-package=org.apache.inlong.manager.dao.entity
```

### SpringBootApplicationProperties
Cannot resolve configuration property 'mybatis.type-aliases-package'
in `inlong-manager/manager-test/src/main/resources/application-unit-test.properties`
#### Snippet
```java
mybatis.mapper-locations=classpath:mappers/*.xml
# Global mapping, no need to write the full path of the entity class in the xml file
mybatis.type-aliases-package=org.apache.inlong.manager.dao.entity
# Turn on camel case mapping
mybatis.configuration.map-underscore-to-camel-case=true
```

### SpringBootApplicationProperties
Cannot resolve configuration property 'mybatis.configuration.map-underscore-to-camel-case'
in `inlong-manager/manager-test/src/main/resources/application-unit-test.properties`
#### Snippet
```java
mybatis.type-aliases-package=org.apache.inlong.manager.dao.entity
# Turn on camel case mapping
mybatis.configuration.map-underscore-to-camel-case=true
# Configure http client
common.http-client.maxTotal=5000
```

### SpringBootApplicationProperties
Cannot resolve configuration property 'common.http-client.maxTotal'
in `inlong-manager/manager-test/src/main/resources/application-unit-test.properties`
#### Snippet
```java
mybatis.configuration.map-underscore-to-camel-case=true
# Configure http client
common.http-client.maxTotal=5000
common.http-client.defaultMaxPerRoute=2000
common.http-client.validateAfterInactivity=5000
```

### SpringBootApplicationProperties
Cannot resolve configuration property 'common.http-client.defaultMaxPerRoute'
in `inlong-manager/manager-test/src/main/resources/application-unit-test.properties`
#### Snippet
```java
# Configure http client
common.http-client.maxTotal=5000
common.http-client.defaultMaxPerRoute=2000
common.http-client.validateAfterInactivity=5000
common.http-client.connectionTimeout=3000
```

### SpringBootApplicationProperties
Cannot resolve configuration property 'common.http-client.validateAfterInactivity'
in `inlong-manager/manager-test/src/main/resources/application-unit-test.properties`
#### Snippet
```java
common.http-client.maxTotal=5000
common.http-client.defaultMaxPerRoute=2000
common.http-client.validateAfterInactivity=5000
common.http-client.connectionTimeout=3000
common.http-client.readTimeout=10000
```

### SpringBootApplicationProperties
Cannot resolve configuration property 'common.http-client.connectionTimeout'
in `inlong-manager/manager-test/src/main/resources/application-unit-test.properties`
#### Snippet
```java
common.http-client.defaultMaxPerRoute=2000
common.http-client.validateAfterInactivity=5000
common.http-client.connectionTimeout=3000
common.http-client.readTimeout=10000
common.http-client.connectionRequestTimeout=3000
```

### SpringBootApplicationProperties
Cannot resolve configuration property 'common.http-client.readTimeout'
in `inlong-manager/manager-test/src/main/resources/application-unit-test.properties`
#### Snippet
```java
common.http-client.validateAfterInactivity=5000
common.http-client.connectionTimeout=3000
common.http-client.readTimeout=10000
common.http-client.connectionRequestTimeout=3000

```

### SpringBootApplicationProperties
Cannot resolve configuration property 'common.http-client.connectionRequestTimeout'
in `inlong-manager/manager-test/src/main/resources/application-unit-test.properties`
#### Snippet
```java
common.http-client.connectionTimeout=3000
common.http-client.readTimeout=10000
common.http-client.connectionRequestTimeout=3000

```

### SpringBootApplicationProperties
Cannot resolve configuration property 'inlong.encrypt.key.value1'
in `inlong-manager/manager-common/src/test/resources/application.properties`
#### Snippet
```java
# Encryption config, the suffix of value must be the same as the version.
inlong.encrypt.version=1
inlong.encrypt.key.value1="I!N@L#O$N%G^"

```

### SpringBootApplicationProperties
Cannot resolve configuration property 'inlong.encrypt.key.value1'
in `inlong-manager/manager-service/src/test/resources/application.properties`
#### Snippet
```java
# Encryption config, the suffix of value must be the same as the version.
inlong.encrypt.version=1
inlong.encrypt.key.value1="I!N@L#O$N%G^"

```

## RuleId[id=DeprecatedIsStillUsed]
### DeprecatedIsStillUsed
Deprecated member 'NEXT_TRANSACTIONAL_ID_HINT_DESCRIPTOR' is still used
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kafka/src/main/java/org/apache/inlong/sort/kafka/FlinkKafkaProducer.java`
#### Snippet
```java
     */
    @Deprecated
    private static final ListStateDescriptor<FlinkKafkaProducer.NextTransactionalIdHint> NEXT_TRANSACTIONAL_ID_HINT_DESCRIPTOR =
            new ListStateDescriptor<>(
                    "next-transactional-id-hint",
```

### DeprecatedIsStillUsed
Deprecated member 'Builder' is still used
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/mysql/MySqlSource.java`
#### Snippet
```java
     */
    @Deprecated
    public static class Builder<T> {

        private int port = 3306; // default 3306 port
```

### DeprecatedIsStillUsed
Deprecated member 'MySqlSource' is still used
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/mysql/MySqlSource.java`
#### Snippet
```java
 */
@Deprecated
public class MySqlSource {

    private static final String DATABASE_SERVER_NAME = "mysql_binlog_source";
```

### DeprecatedIsStillUsed
Deprecated member 'includeMetadata' is still used
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/protocol/deserialization/CanalDeserializationInfo.java`
#### Snippet
```java
    @Deprecated
    @JsonProperty("include_metadata")
    private final boolean includeMetadata;

    @JsonCreator
```

### DeprecatedIsStillUsed
Deprecated member 'hasDeprecatedKeys' is still used
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/configuration/ConfigOption.java`
#### Snippet
```java
     */
    @Deprecated
    public boolean hasDeprecatedKeys() {
        return fallbackKeys != EMPTY && Arrays.stream(fallbackKeys)
                .anyMatch(FallbackKey::isDeprecated);
```

### DeprecatedIsStillUsed
Deprecated member 'deprecatedKeys' is still used
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/configuration/ConfigOption.java`
#### Snippet
```java
     */
    @Deprecated
    public Iterable<String> deprecatedKeys() {
        return fallbackKeys == EMPTY ? Collections.<String>emptyList()
                : Arrays.stream(fallbackKeys)
```

### DeprecatedIsStillUsed
Deprecated member 'OP_TYPE' is still used
in `inlong-sort/sort-formats/format-json/src/main/java/org/apache/inlong/sort/formats/json/canal/CanalJsonEnhancedDecodingFormat.java`
#### Snippet
```java
         */
        @Deprecated
        OP_TYPE(
                "op-type",
                DataTypes.STRING().nullable(),
```

### DeprecatedIsStillUsed
Deprecated member 'OP_TYPE' is still used
in `inlong-sort/sort-formats/format-json/src/main/java/org/apache/inlong/sort/formats/json/canal/CanalJsonEnhancedEncodingFormat.java`
#### Snippet
```java
         */
        @Deprecated
        OP_TYPE(
                "op-type",
                DataTypes.STRING().nullable(),
```

### DeprecatedIsStillUsed
Deprecated member 'getRestartJobs' is still used
in `inlong-agent/agent-common/src/main/java/org/apache/inlong/agent/db/JobProfileDb.java`
#### Snippet
```java
     */
    @Deprecated
    public List<JobProfile> getRestartJobs() {
        List<JobProfile> jobsByState = getJobsByState(StateSearchKey.ACCEPTED);
        jobsByState.addAll(getJobsByState(StateSearchKey.RUNNING));
```

### DeprecatedIsStillUsed
Deprecated member 'm' is still used
in `inlong-common/src/main/java/org/apache/inlong/common/pojo/dataproxy/DataProxyTopicInfo.java`
#### Snippet
```java
     */
    @Deprecated
    private String m;

    public DataProxyTopicInfo() {
```

### DeprecatedIsStillUsed
Deprecated member 'DATA_BYTES' is still used
in `inlong-common/src/main/java/org/apache/inlong/common/enums/MetaField.java`
#### Snippet
```java
     * @deprecated please use DATA_BYTES_DEBEZIUM \ DATA_CANAL_BYTES
     */
    DATA_BYTES,

    /**
```

### DeprecatedIsStillUsed
Deprecated member 'deleteWhen' is still used
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/metadata/BrokerDefMetadata.java`
#### Snippet
```java
    private String dataPath;
    @Deprecated
    private String deleteWhen = "0 0 6,18 * * ?";
    // expire policy.
    private String deletePolicy = "delete,168h";
```

### DeprecatedIsStillUsed
Deprecated member 'deleteWhen' is still used
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/metadata/TopicMetadata.java`
#### Snippet
```java
    private String dataPath;
    @Deprecated
    private String deleteWhen = "0 0 6,18 * * ?";
    // expire policy.
    private String deletePolicy = "delete,168h";
```

### DeprecatedIsStillUsed
Deprecated member 'COMPSCONSUMEGROUP' is still used
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/fielddef/WebFieldDef.java`
#### Snippet
```java

    @Deprecated
    COMPSCONSUMEGROUP(30, "consumeGroup", "group", WebFieldType.COMPSTRING,
            "Group name", TBaseConstants.META_MAX_GROUPNAME_LENGTH,
            RegexDef.TMP_GROUP),
```

### DeprecatedIsStillUsed
Deprecated member 'CONSUMEGROUP' is still used
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/fielddef/WebFieldDef.java`
#### Snippet
```java
            "Client ID", TBaseConstants.META_MAX_CLIENT_ID_LENGTH),
    @Deprecated
    CONSUMEGROUP(29, "consumeGroup", "group", WebFieldType.STRING,
            "Group name", TBaseConstants.META_MAX_GROUPNAME_LENGTH,
            RegexDef.TMP_GROUP),
```

### DeprecatedIsStillUsed
Deprecated member 'WebAdminFlowRuleHandler' is still used
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebAdminFlowRuleHandler.java`
#### Snippet
```java

@Deprecated
public class WebAdminFlowRuleHandler extends AbstractWebHandler {

    private static final String blankFlowCtrlRules = "[]";
```

### DeprecatedIsStillUsed
Deprecated member 'WebAdminTopicAuthHandler' is still used
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebAdminTopicAuthHandler.java`
#### Snippet
```java

@Deprecated
public class WebAdminTopicAuthHandler extends AbstractWebHandler {

    public WebAdminTopicAuthHandler(TMaster master) {
```

### DeprecatedIsStillUsed
Deprecated member 'WebAdminGroupCtrlHandler' is still used
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebAdminGroupCtrlHandler.java`
#### Snippet
```java

@Deprecated
public class WebAdminGroupCtrlHandler extends AbstractWebHandler {

    private static final Logger logger =
```

### DeprecatedIsStillUsed
Deprecated member 'DBCollectorDetailTaskMapper' is still used
in `inlong-manager/manager-dao/src/main/java/org/apache/inlong/manager/dao/mapper/DBCollectorDetailTaskMapper.java`
#### Snippet
```java
@Deprecated
@Repository
public interface DBCollectorDetailTaskMapper {

    DBCollectorDetailTaskEntity selectOneByState(int state);
```

## RuleId[id=RedundantCollectionOperation]
### RedundantCollectionOperation
Unnecessary 'containsKey()' check
in `inlong-agent/agent-common/src/main/java/org/apache/inlong/agent/conf/AbstractConfiguration.java`
#### Snippet
```java

        // del
        if (configStorage.containsKey(key)) {
            configStorage.remove(key);
        }
```

## RuleId[id=SwitchStatementWithTooFewBranches]
### SwitchStatementWithTooFewBranches
'switch' statement has only 'default' case
in `inlong-sort/sort-flink/base/src/main/java/org/apache/inlong/sort/base/metric/SourceMetricData.java`
#### Snippet
```java
        ThreadSafeCounter recordsInCounter = new ThreadSafeCounter();
        ThreadSafeCounter bytesInCounter = new ThreadSafeCounter();
        switch (option.getRegisteredMetric()) {
            default:
                recordsInCounter.inc(option.getInitRecords());
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/controller/node/NodeController.java`
#### Snippet
```java
    @RequestMapping(value = "/master")
    public @ResponseBody TubeMQResult masterMethodProxy(@RequestParam String method, @RequestBody String req) {
        switch (method) {
            case TubeConst.MODIFY:
                return nodeService.modifyMasterNode(gson.fromJson(req, MasterDto.class));
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `inlong-tubemq/tubemq-manager/src/main/java/org/apache/inlong/tubemq/manager/controller/task/TaskController.java`
#### Snippet
```java
    public @ResponseBody TubeMQResult taskMethodProxy(@RequestParam String method,
            @RequestBody String req) {
        switch (method) {
            case TubeConst.ADD_TOPIC_TASK:
                return addTask(gson.fromJson(req, BatchAddTopicTaskReq.class));
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/core/impl/SortSourceServiceImpl.java`
#### Snippet
```java
            SortSourceClusterInfo cluster,
            boolean isBackupTag) {
        switch (cluster.getType()) {
            case ClusterType.PULSAR:
                return parsePulsarZone(sinks, cluster, isBackupTag);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/source/tcp/InlongTcpChannelHandler.java`
#### Snippet
```java
            // read version
            int version = cb.readShort();
            switch (version) {
                case VERSION_1:
                    // decode version 1
```

## RuleId[id=MismatchedJavadocCode]
### MismatchedJavadocCode
Method is specified to return 'null' but it's annotated as @NotNull
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/zookeeper/ZKUtil.java`
#### Snippet
```java
     * specified zNode are created or deleted.
     *
     * Returns null if the specified node does not exist. Otherwise returns a list
     * of children of the specified node. If the node exists but it has no
     * children, an empty list will be returned.
```

### MismatchedJavadocCode
Method is specified to return 'true' but its return type is not boolean
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/zookeeper/ZKUtil.java`
#### Snippet
```java
     * Check if the specified node exists. Sets no watches.
     * <p/>
     * Returns true if node exists, false if not. Returns an exception if there is an unexpected
     * zookeeper exception.
     *
```

### MismatchedJavadocCode
Method is specified to return 'true' but its return type is not boolean
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/metamanage/MetaDataService.java`
#### Snippet
```java
     * @param strBuff   the print info string buffer
     * @param result    the process result return
     * @return true if success otherwise false
     */
    GroupProcessResult insertGroupCtrlConf(GroupResCtrlEntity entity,
```

### MismatchedJavadocCode
Method is specified to return 'true' but its return type is not boolean
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/metamanage/MetaDataService.java`
#### Snippet
```java
     * @param strBuff        the print information string buffer
     * @param result         the process result return
     * @return true if success otherwise false
     */
    GroupProcessResult addOrUpdGroupCtrlConf(boolean isAddOp, BaseEntity opEntity,
```

### MismatchedJavadocCode
Method is specified to return 'true' but its return type is not boolean
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/metamanage/MetaDataService.java`
#### Snippet
```java
     * @param strBuff    the print information string buffer
     * @param result     the process result return
     * @return true if success otherwise false
     */
    BrokerProcessResult reloadBrokerConfInfo(BaseEntity opEntity, int brokerId,
```

### MismatchedJavadocCode
Method is specified to return 'true' but its return type is not boolean
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/metamanage/MetaDataService.java`
#### Snippet
```java
     * @param strBuff   the print info string buffer
     * @param result    the process result return
     * @return true if success otherwise false
     */
    GroupProcessResult delGroupResCtrlConf(String operator, String groupName,
```

### MismatchedJavadocCode
Method is specified to return 'true' but its return type is not boolean
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/metamanage/MetaDataService.java`
#### Snippet
```java
     * @param strBuff   the print information string buffer
     * @param result     the process result return
     * @return true if success otherwise false
     */
    BrokerProcessResult addOrUpdBrokerConfig(boolean isAddOp, BrokerConfEntity entity,
```

### MismatchedJavadocCode
Method is specified to return 'true' but its return type is not boolean
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/metamanage/MetaDataService.java`
#### Snippet
```java
     * @param strBuff    the print info string buffer
     * @param result     the process result return
     * @return true if success otherwise false
     */
    List<TopicProcessResult> addOrUpdTopicCtrlConf(boolean isAddOp,
```

### MismatchedJavadocCode
Method is specified to return 'true' but its return type is not boolean
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/metamanage/MetaDataService.java`
#### Snippet
```java
     * @param strBuff        the print information string buffer
     * @param result         the process result return
     * @return true if success otherwise false
     */
    GroupProcessResult addOrUpdConsumeCtrlInfo(boolean isAddOp, BaseEntity opEntity,
```

### MismatchedJavadocCode
Method is specified to return 'true' but its return type is not boolean
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/metamanage/MetaDataService.java`
#### Snippet
```java
     * @param strBuff  the print info string buffer
     * @param result     the process result return
     * @return true if success otherwise false
     */
    TopicProcessResult insertTopicCtrlConf(BaseEntity opEntity, String topicName,
```

### MismatchedJavadocCode
Method is specified to return 'true' but its return type is not boolean
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/metamanage/MetaDataService.java`
#### Snippet
```java
     * @param strBuff   the print information string buffer
     * @param result    the process result return
     * @return true if success otherwise false
     */
    BrokerProcessResult delBrokerConfInfo(String operator, boolean rsvData,
```

### MismatchedJavadocCode
Method is specified to return 'true' but its return type is not boolean
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/metamanage/MetaDataService.java`
#### Snippet
```java
     * @param strBuff  the print info string buffer
     * @param result   the process result return
     * @return true if success otherwise false
     */
    TopicProcessResult addOrUpdTopicDeployInfo(boolean isAddOp, TopicDeployEntity deployEntity,
```

### MismatchedJavadocCode
Method is specified to return 'true' but its return type is not boolean
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/metamanage/MetaDataService.java`
#### Snippet
```java
     * @param strBuff         the string buffer
     * @param result          the process result
     * @return                true if success otherwise false
     */
    TopicProcessResult addOrUpdTopicDeployInfo(boolean isAddOp, BaseEntity opEntity,
```

### MismatchedJavadocCode
Method is specified to return 'true' but its return type is not boolean
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/metamanage/MetaDataService.java`
#### Snippet
```java
     * @param strBuff  the print info string buffer
     * @param result     the process result return
     * @return true if success otherwise false
     */
    TopicProcessResult insertTopicCtrlConf(TopicCtrlEntity entity,
```

### MismatchedJavadocCode
Method is specified to return 'true' but its return type is not boolean
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/metamanage/MetaDataService.java`
#### Snippet
```java
     * @param strBuff        the print information string buffer
     * @param result         the process result return
     * @return true if success otherwise false
     */
    GroupProcessResult insertGroupCtrlConf(BaseEntity opEntity, String groupName,
```

### MismatchedJavadocCode
Method is specified to return 'true' but its return type is not boolean
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/metamanage/MetaDataService.java`
#### Snippet
```java
     * @param strBuff   the print info string buffer
     * @param result    the process result return
     * @return true if success otherwise false
     */
    GroupProcessResult addOrUpdConsumeCtrlInfo(boolean isAddOp, GroupConsumeCtrlEntity entity,
```

### MismatchedJavadocCode
Method is specified to return 'true' but its return type is not boolean
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/metamanage/MetaDataService.java`
#### Snippet
```java
     * @param strBuff       the print information string buffer
     * @param result        the process result return
     * @return true if success otherwise false
     */
    BrokerProcessResult changeBrokerConfStatus(BaseEntity opEntity,
```

### MismatchedJavadocCode
Method is specified to return 'true' but its return type is not boolean
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/metamanage/MetaDataService.java`
#### Snippet
```java
     * @param strBuff          the print info string buffer
     * @param result           the process result return
     * @return true if success otherwise false
     */
    GroupProcessResult insertGroupCtrlConf(BaseEntity opEntity, String groupName,
```

### MismatchedJavadocCode
Method is specified to return 'true' but its return type is not boolean
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/metamanage/MetaDataService.java`
#### Snippet
```java
     * @param strBuff          the print info string buffer
     * @param result           the process result return
     * @return true if success otherwise false
     */
    List<TopicProcessResult> addOrUpdTopicCtrlConf(boolean isAddOp, BaseEntity opEntity,
```

### MismatchedJavadocCode
Method is specified to return 'true' but its return type is not boolean
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/metamanage/MetaDataService.java`
#### Snippet
```java
     * @param strBuff    the print info string buffer
     * @param result     the process result return
     * @return true if success otherwise false
     */
    TopicProcessResult updTopicDeployStatusInfo(BaseEntity opEntity, int brokerId,
```

### MismatchedJavadocCode
Method is specified to return 'true' but its return type is not boolean
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/metamanage/MetaDataService.java`
#### Snippet
```java
     * @param strBuff  the print info string buffer
     * @param result     the process result return
     * @return true if success otherwise false
     */
    GroupProcessResult addOrUpdGroupCtrlConf(boolean isAddOp, GroupResCtrlEntity entity,
```

## RuleId[id=CatchMayIgnoreException]
### CatchMayIgnoreException
'catch' parameter named `ignored` is used
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/Utils.java`
#### Snippet
```java
            socket.connect(InetAddress.getByName("8.8.8.8"), 10002);
            ip = socket.getLocalAddress().getHostAddress();
        } catch (Exception ignored) {
            logger.warn("getLocalIp ", ignored);
        }
```

### CatchMayIgnoreException
'catch' parameter named `ignored` is used
in `inlong-audit/audit-common/src/main/java/org/apache/inlong/audit/file/ConfigManager.java`
#### Snippet
```java
                    sleepTimeInMs = Long.parseLong(sleepTimeInMsStr);
                }
            } catch (Exception ignored) {
                LOG.info("ignored Exception ", ignored);
            }
```

## RuleId[id=IOStreamConstructor]
### IOStreamConstructor
'OutputStream' can be constructed using 'Files.newOutputStream()'
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/utils/ServiceDiscoveryUtils.java`
#### Snippet
```java

        try (BufferedWriter writer = new BufferedWriter(
                new OutputStreamWriter(new FileOutputStream(localPath), StandardCharsets.UTF_8))) {
            writer.write(storeString);
            writer.flush();
```

### IOStreamConstructor
'InputStream' can be constructed using 'Files.newInputStream()'
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/utils/EncryptUtil.java`
#### Snippet
```java
            }

            DataInputStream dis = new DataInputStream(new BufferedInputStream(new FileInputStream(path)));
            byte[] content = new byte[len];
            while (dis.read(content) != -1) {
```

### IOStreamConstructor
'InputStream' can be constructed using 'Files.newInputStream()'
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/utils/EncryptUtil.java`
#### Snippet
```java
            }

            DataInputStream dis = new DataInputStream(new BufferedInputStream(new FileInputStream(path)));
            byte[] content = new byte[len];
            while (dis.read(content) != -1) {
```

### IOStreamConstructor
'OutputStream' can be constructed using 'Files.newOutputStream()'
in `inlong-sort/sort-common/src/main/java/org/apache/inlong/sort/util/ParameterTool.java`
#### Snippet
```java
        final Properties defaultProps = new Properties();
        defaultProps.putAll(this.defaultData);
        try (final OutputStream out = new FileOutputStream(file)) {
            defaultProps.store(out,
                    "Default file created by Flink's ParameterUtil.createPropertiesFile()");
```

### IOStreamConstructor
'InputStream' can be constructed using 'Files.newInputStream()'
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/sources/reader/file/FileReaderOperator.java`
#### Snippet
```java
        BufferedReader reader = null;
        try {
            reader = new BufferedReader(new InputStreamReader(new FileInputStream(file),
                    StandardCharsets.UTF_8));
            int count = 0;
```

### IOStreamConstructor
'InputStream' can be constructed using 'Files.newInputStream()'
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/sources/reader/file/FileReaderOperator.java`
#### Snippet
```java
            String line = null;
            List<String> lines = new ArrayList<>();
            reader = new BufferedReader(new InputStreamReader(new FileInputStream(file),
                    StandardCharsets.UTF_8));
            int count = 0;
```

### IOStreamConstructor
'InputStream' can be constructed using 'Files.newInputStream()'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/utils/TSSLEngineUtil.java`
#### Snippet
```java
            throws Exception {

        InputStream keyStoreStream = new FileInputStream(new File(keyStorePath));
        InputStream trustStoreStream = new FileInputStream(new File(trustStorePath));

```

### IOStreamConstructor
'InputStream' can be constructed using 'Files.newInputStream()'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/utils/TSSLEngineUtil.java`
#### Snippet
```java

        InputStream keyStoreStream = new FileInputStream(new File(keyStorePath));
        InputStream trustStoreStream = new FileInputStream(new File(trustStorePath));

        return createSSLEngine(keyStoreStream, keyStorePassword, trustStoreStream,
```

### IOStreamConstructor
'InputStream' can be constructed using 'Files.newInputStream()'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/simplemvc/conf/ConfigFileParser.java`
#### Snippet
```java
        SAXReader reader = new SAXReader();
        WebConfig config = new WebConfig();
        InputStream in = new FileInputStream(configFile);
        try {
            Document document = reader.read(in);
```

## RuleId[id=CollectionAddAllCanBeReplacedWithConstructor]
### CollectionAddAllCanBeReplacedWithConstructor
'addAll()' call can be replaced with parametrized constructor call
in `inlong-sdk/sort-sdk/src/main/java/org/apache/inlong/sdk/sort/fetcher/tube/TubeSingleTopicFetcher.java`
#### Snippet
```java
                            context.addConsumeFilter(topic, -1, filterSize);

                            msgs.addAll(deserialize);
                            handleAndCallbackMsg(new MessageRecord(topic.getTopicKey(), msgs,
                                    message.getConfirmContext(), System.currentTimeMillis()));
```

### CollectionAddAllCanBeReplacedWithConstructor
'putAll()' call can be replaced with parametrized constructor call
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/pb/config/ProxyClusterConfigHolder.java`
#### Snippet
```java
            }
            Map<String, ProxyClusterResult> newProxyMap = new ConcurrentHashMap<>();
            newProxyMap.putAll(instance.proxyClusterMap);
            newProxyMap.put(result.getClusterId(), result);
            instance.proxyClusterMap = newProxyMap;
```

### CollectionAddAllCanBeReplacedWithConstructor
'putAll()' call can be replaced with parametrized constructor call
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/starrocks/src/main/java/org/apache/inlong/sort/starrocks/manager/StarRocksSinkManager.java`
#### Snippet
```java
    public Map<String, SinkBufferEntity> getBufferedBatchMap() {
        Map<String, SinkBufferEntity> clone = new HashMap<>();
        clone.putAll(bufferMap);
        return clone;
    }
```

### CollectionAddAllCanBeReplacedWithConstructor
'addAll()' call can be replaced with parametrized constructor call
in `inlong-dataproxy/dataproxy-source/src/main/java/org/apache/inlong/dataproxy/config/loader/ContextCacheClusterConfigLoader.java`
#### Snippet
```java
        String[] clusterNameArray = StringUtils.split(clusterNames);
        Set<String> clusterNameSet = new HashSet<>();
        clusterNameSet.addAll(Arrays.asList(clusterNameArray));
        //
        List<CacheClusterConfig> configList = new ArrayList<>(clusterNameSet.size());
```

### CollectionAddAllCanBeReplacedWithConstructor
'putAll()' call can be replaced with parametrized constructor call
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/source/sortsdk/SortSdkSource.java`
#### Snippet
```java
        Map<String, String> sortSdkParams = new HashMap<>();
        Map<String, String> commonParams = CommonPropertiesHolder.getContext().getSubProperties(SORT_SDK_PREFIX);
        sortSdkParams.putAll(commonParams);
        SortTaskConfig taskConfig = SortClusterConfigHolder.getTaskConfig(taskName);
        if (taskConfig != null) {
```

## RuleId[id=UnnecessaryToStringCall]
### UnnecessaryToStringCall
Unnecessary `toString()` call
in `inlong-sdk/sdk-common/src/main/java/org/apache/inlong/sdk/commons/admin/AdminServiceRegister.java`
#### Snippet
```java
        try {
            ObjectName objName = new ObjectName(beanName);
            System.out.println(objName.toString());
            System.out.println(objName.getCanonicalKeyPropertyListString());
            System.out.println(objName.getCanonicalName());
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/HttpProxySender.java`
#### Snippet
```java
                    logger.error("manager is null, please check it!");
                }
                logger.info("get new proxy list " + hostList.toString());
            } catch (InterruptedException ignored) {
                // ignore it.
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/config/ProxyConfigManager.java`
#### Snippet
```java
    private void compareProxyList(ProxyConfigEntry proxyEntry) {
        if (proxyEntry != null) {
            LOGGER.info("{}", proxyEntry.toString());
            if (proxyEntry.getSize() != 0) {
                /* Initialize the current proxy information list first. */
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/config/ProxyConfigManager.java`
#### Snippet
```java
                LOGGER.info("ProxyConf update!");
            } catch (Throwable e) {
                LOGGER.error("Refresh proxy ip list runs into exception {}, {}", e.toString(), e.getStackTrace());
                e.printStackTrace();
            }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/ClientMgr.java`
#### Snippet
```java
            }
        } catch (Exception e) {
            logger.error("{} , {}", e.toString(), e.getStackTrace());
        }
    }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/ClientMgr.java`
#### Snippet
```java

        } catch (Exception e) {
            logger.error("replaceBadConnection exception {}, {}", e.toString(), e.getStackTrace());
        } finally {
            writeUnlock();
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/ClientMgr.java`
#### Snippet
```java
        } catch (Throwable e) {
            logger.error("sendHeartBeat to " + hostInfo.getReferenceName()
                    + " exception {}, {}", e.toString(), e.getStackTrace());
        }
    }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `inlong-sort/sort-flink/base/src/main/java/org/apache/inlong/sort/base/dirty/sink/s3/S3DirtySink.java`
#### Snippet
```java
            if (!s3Options.ignoreSideOutputErrors()) {
                throw new RuntimeException(String.format("Add batch to identifier:%s failed, the dirty data: %s.",
                        dirtyData.getIdentifier(), dirtyData.toString()), e);
            }
            LOGGER.warn("Add batch to identifier:{} failed "
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/jdbc/src/main/java/org/apache/inlong/sort/jdbc/table/AbstractJdbcDialect.java`
#### Snippet
```java
                        String.format(
                                "The %s dialect doesn't support type: %s.",
                                dialectName(), dt.toString()));
            }

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/hive/src/main/java/org/apache/inlong/sort/hive/HiveTableSink.java`
#### Snippet
```java
            OutputFileConfig.OutputFileConfigBuilder fileNamingBuilder =
                    OutputFileConfig.builder()
                            .withPartPrefix("part-" + UUID.randomUUID().toString())
                            .withPartSuffix(extension == null ? "" : extension);

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/pulsar/src/main/java/org/apache/inlong/sort/pulsar/internal/ReaderThread.java`
#### Snippet
```java
                    close();
                } catch (Throwable e) {
                    log.error("Error while closing Pulsar reader " + e.toString());
                }
            }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/pulsar/src/main/java/org/apache/inlong/sort/pulsar/internal/FlinkPulsarSource.java`
#### Snippet
```java
            return externalSubscriptionName;
        } else {
            return "flink-pulsar-" + uuid.toString();
        }
    }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/pulsar/src/main/java/org/apache/inlong/sort/pulsar/internal/FlinkPulsarSourceWithoutAdmin.java`
#### Snippet
```java
            return externalSubscriptionName;
        } else {
            return "flink-pulsar-" + uuid.toString();
        }
    }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/mysql/source/config/ServerIdRange.java`
#### Snippet
```java
                                    + "make the number of server id larger than "
                                    + "the source parallelism.",
                            subTaskId, this.toString()));
        }
        return startServerId + subTaskId;
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/mysql/source/config/MySqlSourceConfigFactory.java`
#### Snippet
```java
                "database.history", EmbeddedFlinkDatabaseHistory.class.getCanonicalName());
        props.setProperty(
                "database.history.instance.name", UUID.randomUUID().toString() + "_" + subtaskId);
        props.setProperty("database.history.skip.unparseable.ddl", String.valueOf(true));
        props.setProperty("database.history.refer.ddl", String.valueOf(true));
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/filesystem/src/main/java/org/apache/inlong/sort/filesystem/FileSystemTableSink.java`
#### Snippet
```java
                        tableOptions.get(SINK_ROLLING_POLICY_ROLLOVER_INTERVAL).toMillis());

        String randomPrefix = "part-" + UUID.randomUUID().toString();
        OutputFileConfig.OutputFileConfigBuilder fileNamingBuilder = OutputFileConfig.builder();
        fileNamingBuilder =
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/filesystem/src/main/java/org/apache/inlong/sort/filesystem/FileSystemTableSink.java`
#### Snippet
```java
        builder.setOutputFileConfig(
                OutputFileConfig.builder()
                        .withPartPrefix("part-" + UUID.randomUUID().toString())
                        .build());
        return inputStream
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/oracle-cdc/src/main/java/io/debezium/connector/oracle/logminer/LogMinerStreamingChangeEventSource.java`
#### Snippet
```java
                        endScn,
                        offsetContext.getScn());
                LOGGER.info("Transactional buffer dump: {}", transactionalBuffer.toString());
                LOGGER.info("Streaming metrics dump: {}", streamingMetrics.toString());
            }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/channel/MemoryChannel.java`
#### Snippet
```java
        AgentMetricItem metricItem = getMetricItem(dimensions);
        metricItem.pluginReadFailCount.incrementAndGet();
        LOGGER.debug("plugin read failed:{}", dimensions.toString());
        Thread.currentThread().interrupt();
        return;
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `inlong-agent/agent-plugins/src/main/java/org/apache/inlong/agent/plugin/channel/MemoryChannel.java`
#### Snippet
```java
        metricItem.pluginSendFailCount.incrementAndGet();
        metricItem.pluginSendCount.incrementAndGet();
        LOGGER.debug("plugin send failed:{}", dimensions.toString());
        Thread.currentThread().interrupt();
        return;
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/netty/NettyRequestContext.java`
#### Snippet
```java
                                        .append("reason: ")
                                        .append(future.channel().toString())
                                        .append(exception.toString()).toString());
                            } else {
                                logger.debug(new StringBuilder(512)
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/netty/NettyProtocolDecoder.java`
#### Snippet
```java
                                + "list is : {}",
                                inParamValue, allowTokenVal,
                                errProtolAddrMap.toString());
                        errProtolAddrMap.clear();
                    }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/netty/NettyProtocolDecoder.java`
#### Snippet
```java
                if (curTime - befTime > 180000) {
                    if (lastSizeTime.compareAndSet(befTime, System.currentTimeMillis())) {
                        logger.warn("[Abnormal Visit] Abnormal BodySize visit list is :" + errSizeAddrMap.toString());
                        errSizeAddrMap.clear();
                    }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corerpc/RpcServiceFactory.java`
#### Snippet
```java
                    logger.info(new StringBuilder(512)
                            .append("[Remote Address] forbidden list : ")
                            .append(forbiddenAddrMap.toString()).toString());
                }
            }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/cluster/Partition.java`
#### Snippet
```java
                .append(TokenConstants.ATTR_SEP).append(this.partitionId).toString();
        sBuilder.delete(0, sBuilder.length());
        this.partitionFullStr = sBuilder.append(this.broker.toString())
                .append(TokenConstants.SEGMENT_SEP).append(this.topic)
                .append(TokenConstants.ATTR_SEP).append(this.partitionId).toString();
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/ConsumerSamplePrint.java`
#### Snippet
```java
                    logger.error(sBuilder
                            .append("[heartbeat failed] heartbeat to broker error 2 is ")
                            .append(e.toString()).toString());
                    sBuilder.delete(0, sBuilder.length());
                }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/config/TubeClientConfig.java`
#### Snippet
```java
                .append(",\"unAvailableFbdDurationMs\":").append(this.unAvailableFbdDurationMs)
                .append(",\"enableUserAuthentic\":").append(this.enableUserAuthentic)
                .append(",").append(this.statsConfig.toString())
                .append(",\"usrName\":\"").append(this.usrName)
                .append("\",\"usrPassWord\":\"").append(this.usrPassWord)
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/tools/CliUtils.java`
#### Snippet
```java
            result.setFailResult(new StringBuilder(512)
                    .append("Parse configuration file path failure: ")
                    .append(e.toString()).toString());
        }
        return result.isSuccess();
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/producer/qltystats/DefaultBrokerRcvQltyStats.java`
#### Snippet
```java
                + TokenConstants.ATTR_SEP + ",linkMaxAllowedDelayedMsgCount:"
                + this.clientConfig.getLinkMaxAllowedDelayedMsgCount() + TokenConstants.ATTR_SEP
                + ",brokerStats:" + this.brokerStats.toString() + TokenConstants.ATTR_SEP
                + ",brokerForbiddenMap:" + this.brokerForbiddenMap.toString();
    }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/producer/qltystats/DefaultBrokerRcvQltyStats.java`
#### Snippet
```java
                + this.clientConfig.getLinkMaxAllowedDelayedMsgCount() + TokenConstants.ATTR_SEP
                + ",brokerStats:" + this.brokerStats.toString() + TokenConstants.ATTR_SEP
                + ",brokerForbiddenMap:" + this.brokerForbiddenMap.toString();
    }

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/producer/qltystats/DefaultBrokerRcvQltyStats.java`
#### Snippet
```java
            if (!brokerForbiddenMap.isEmpty()) {
                logger.info(sBuilder.append("[status check]: current response quality respForbiddenMap is ")
                        .append(brokerForbiddenMap.toString()).toString());
                sBuilder.delete(0, sBuilder.length());
            }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/producer/qltystats/DefaultBrokerRcvQltyStats.java`
#### Snippet
```java
                if (!brokerForbiddenMap.isEmpty()) {
                    logger.info(sBuilder.append("End statistic 1: forbidden Broker Set is ")
                            .append(brokerForbiddenMap.toString()).toString());
                    sBuilder.delete(0, sBuilder.length());
                }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/producer/qltystats/DefaultBrokerRcvQltyStats.java`
#### Snippet
```java
                if (!brokerForbiddenMap.isEmpty()) {
                    logger.info(sBuilder.append("End statistic 2: forbidden Broker Set is ")
                            .append(brokerForbiddenMap.toString()).toString());
                    sBuilder.delete(0, sBuilder.length());
                }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/producer/qltystats/DefaultBrokerRcvQltyStats.java`
#### Snippet
```java
            if (!brokerForbiddenMap.isEmpty()) {
                logger.info(sBuilder.append("End statistic 3: forbidden Broker Set is ")
                        .append(brokerForbiddenMap.toString()).toString());
                sBuilder.delete(0, sBuilder.length());
            }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/RmtDataCache.java`
#### Snippet
```java
                                && !indexPartition.remove(partition.getPartitionKey())) {
                            logger.info(sBuilder.append("[Process Interrupt] Partition : ")
                                    .append(partition.toString())
                                    .append(", data in processing, canceled").toString());
                            sBuilder.delete(0, sBuilder.length());
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/RmtDataCache.java`
#### Snippet
```java
                    && !indexPartition.remove(partitionKey)) {
                logger.info(sBuffer.append("[Process Interrupt] Partition : ")
                        .append(partitionExt.toString())
                        .append(", data in processing, canceled").toString());
                sBuffer.delete(0, sBuffer.length());
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/producer/ProducerManager.java`
#### Snippet
```java
            logger.info(sBuilder.append("[Publish begin 2] publish topicSet ")
                    .append(topicSet).append(", address = ")
                    .append(this.toString()).toString());
            sBuilder.delete(0, sBuilder.length());
            boolean hasNewTopic = false;
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/producer/ProducerManager.java`
#### Snippet
```java
                    logger.error("Heartbeat failed,retry later.Reason:{}",
                            strBuff.append(e.getClass().getSimpleName())
                                    .append("#").append(e.getMessage()).toString());
                    strBuff.delete(0, strBuff.length());
                }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/producer/ProducerManager.java`
#### Snippet
```java
            logger.info(sBuilder.append("[Publish begin 1] publish topic ")
                    .append(topic).append(", address = ")
                    .append(this.toString()).toString());
            sBuilder.delete(0, sBuilder.length());
            AtomicInteger curPubCnt = this.publishTopics.get(topic);
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/tools/StoreRepairAdmin.java`
#### Snippet
```java
                    .append("[Data Repair] Begin to scan store path: ")
                    .append(storePath).append(",topicList:")
                    .append(topicList.toString()).toString());
        }
        int count = 0;
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/utils/DiskSamplePrint.java`
#### Snippet
```java
                        logger.error(sBuilder
                                .append("[File Store] Append message in file failed 2 ")
                                .append(e.toString()).toString());
                        sBuilder.delete(0, sBuilder.length());
                    }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/utils/DiskSamplePrint.java`
#### Snippet
```java
                    logger.error(sBuilder
                            .append("[File Store] Append message in file failed 4 is ")
                            .append(e.toString()).toString());
                    sBuilder.delete(0, sBuilder.length());
                }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/utils/DiskSamplePrint.java`
#### Snippet
```java
                                .append("[File Store] Get message failure for IOException 2, storeKey=")
                                .append(storeKey).append(", partitionId=").append(partitionId)
                                .append(", error = ").append(e.toString()).toString());
                    }
                    sBuilder.delete(0, sBuilder.length());
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/utils/DiskSamplePrint.java`
#### Snippet
```java
                            .append("[File Store] Get message failure for Exception 4, storeKey=")
                            .append(storeKey).append(", partitionId=").append(partitionId)
                            .append(", error = ").append(e.toString()).toString());
                }
                sBuilder.delete(0, sBuilder.length());
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/utils/BrokerSamplePrint.java`
#### Snippet
```java
                        logger.error(sBuilder
                                .append("[heartbeat failed] heartbeat to master exception 2 is ")
                                .append(e.toString()).toString());
                        sBuilder.delete(0, sBuilder.length());
                    }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/utils/BrokerSamplePrint.java`
#### Snippet
```java
                    logger.error(sBuilder
                            .append("[heartbeat failed] heartbeat to master exception 4 is ")
                            .append(e.toString()).toString());
                    sBuilder.delete(0, sBuilder.length());
                }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/tools/cli/CliMetaDataBRU.java`
#### Snippet
```java
            if (strBuff.length() > maxDataLength
                    || count % TServerConstants.CFG_BATCH_BROKER_OPERATE_MAX_COUNT == 0) {
                inParamMap.put("brokerJsonSet", "[" + strBuff.toString() + "]");
                strBuff.delete(0, strBuff.length());
                inParamMap.put("createUser", curOperator);
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/tools/cli/CliMetaDataBRU.java`
#### Snippet
```java
        }
        if (strBuff.length() > 0) {
            inParamMap.put("brokerJsonSet", "[" + strBuff.toString() + "]");
            strBuff.delete(0, strBuff.length());
            inParamMap.put("createUser", curOperator);
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/tools/cli/CliMetaDataBRU.java`
#### Snippet
```java
            if (strBuff.length() > maxDataLength
                    || count % TServerConstants.CFG_BATCH_BROKER_OPERATE_MAX_COUNT == 0) {
                inParamMap.put("groupCsmJsonSet", "[" + strBuff.toString() + "]");
                strBuff.delete(0, strBuff.length());
                inParamMap.put("createUser", curOperator);
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/tools/cli/CliMetaDataBRU.java`
#### Snippet
```java
        }
        if (strBuff.length() > 0) {
            inParamMap.put("groupCsmJsonSet", "[" + strBuff.toString() + "]");
            strBuff.delete(0, strBuff.length());
            inParamMap.put("createUser", curOperator);
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/tools/cli/CliMetaDataBRU.java`
#### Snippet
```java
            if (strBuff.length() > maxDataLength
                    || count % TServerConstants.CFG_BATCH_BROKER_OPERATE_MAX_COUNT == 0) {
                inParamMap.put("topicJsonSet", "[" + strBuff.toString() + "]");
                strBuff.delete(0, strBuff.length());
                inParamMap.put("createUser", curOperator);
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/tools/cli/CliMetaDataBRU.java`
#### Snippet
```java
        }
        if (strBuff.length() > 0) {
            inParamMap.put("topicJsonSet", "[" + strBuff.toString() + "]");
            strBuff.delete(0, strBuff.length());
            inParamMap.put("createUser", curOperator);
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/tools/cli/CliMetaDataBRU.java`
#### Snippet
```java
            if (strBuff.length() > maxDataLength
                    || count % TServerConstants.CFG_BATCH_BROKER_OPERATE_MAX_COUNT == 0) {
                inParamMap.put("topicJsonSet", "[" + strBuff.toString() + "]");
                strBuff.delete(0, strBuff.length());
                inParamMap.put("createUser", curOperator);
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/tools/cli/CliMetaDataBRU.java`
#### Snippet
```java
        }
        if (strBuff.length() > 0) {
            inParamMap.put("topicJsonSet", "[" + strBuff.toString() + "]");
            strBuff.delete(0, strBuff.length());
            inParamMap.put("createUser", curOperator);
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/tools/cli/CliMetaDataBRU.java`
#### Snippet
```java
            if (strBuff.length() > maxDataLength
                    || count % TServerConstants.CFG_BATCH_BROKER_OPERATE_MAX_COUNT == 0) {
                inParamMap.put("topicCtrlJsonSet", "[" + strBuff.toString() + "]");
                strBuff.delete(0, strBuff.length());
                inParamMap.put("createUser", curOperator);
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/tools/cli/CliMetaDataBRU.java`
#### Snippet
```java
        }
        if (strBuff.length() > 0) {
            inParamMap.put("topicCtrlJsonSet", "[" + strBuff.toString() + "]");
            strBuff.delete(0, strBuff.length());
            inParamMap.put("createUser", curOperator);
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/tools/cli/CliMetaDataBRU.java`
#### Snippet
```java
        inParamMap.clear();
        entity.toWebJsonStr(strBuff, true, true);
        inParamMap.put("topicCtrlJsonSet", "[" + strBuff.toString() + "]");
        strBuff.delete(0, strBuff.length());
        inParamMap.put("createUser", curOperator);
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/BaseMessageConsumer.java`
#### Snippet
```java
                            logger.info(strBuffer.append("Registered partition: consumer is ")
                                    .append(consumerId).append(", partition is:")
                                    .append(partition.toString()).toString());
                            strBuffer.delete(0, strBuffer.length());
                        } else {
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/BaseMessageConsumer.java`
#### Snippet
```java
                                logger.warn(strBuffer.append("register2broker error! ")
                                        .append(retryTimesRegister2Broker).append(" register ")
                                        .append(partition.toString()).append(" return null!")
                                        .toString());
                            } else {
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/BaseMessageConsumer.java`
#### Snippet
```java
                                    logger.warn(strBuffer.append("register2broker error! ")
                                            .append(retryTimesRegister2Broker).append(" register ")
                                            .append(partition.toString()).append(" return ")
                                            .append(responseB2C.getErrMsg()).toString());
                                }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/BaseMessageConsumer.java`
#### Snippet
```java
                        logger.warn(strBuffer.append("register2broker error1 ! ")
                                .append(retryTimesRegister2Broker).append(" ")
                                .append(partition.toString()).toString(), e);
                        strBuffer.delete(0, strBuffer.length());
                    } catch (Throwable ee) {
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/BaseMessageConsumer.java`
#### Snippet
```java
            taskContext.setFailProcessResult(400, strBuffer
                    .append("Get message error, reason is ")
                    .append(ee.toString()).toString());
            strBuffer.delete(0, strBuffer.length());
            return taskContext;
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/BaseMessageConsumer.java`
#### Snippet
```java
                    strBuffer.append("Get message failed,topic=")
                            .append(topic).append(",partition=").append(partition)
                            .append(", throw info is ").append(ee.toString()).toString());
            strBuffer.delete(0, strBuffer.length());
        }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/BaseMessageConsumer.java`
#### Snippet
```java
                                                logger.warn(strBuffer
                                                        .append("[heart2broker error] partition:")
                                                        .append(failPartition.toString())
                                                        .append(", errorCode=")
                                                        .append(errorCode).toString());
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/SimpleClientBalanceConsumer.java`
#### Snippet
```java
                        sBuilder.append("Confirm ").append(confirmContext)
                                .append("'s offset failed, exception is ")
                                .append(e.toString()).toString());
                sBuilder.delete(0, sBuilder.length());
                return result.isSuccess();
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/SimpleClientBalanceConsumer.java`
#### Snippet
```java
            logger.info(sBuffer.append("Unregister partition: consumer is ")
                    .append(consumerId).append(", partition=")
                    .append(partition.toString()).append(", isLastPackConsumed=")
                    .append(isLastConsumed).toString());
        } catch (Throwable e) {
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/SimpleClientBalanceConsumer.java`
#### Snippet
```java
                                        logger.warn(sBuffer
                                                .append("[heart2broker error] partition:")
                                                .append(failPartition.toString())
                                                .append(", errorCode=")
                                                .append(errorCode).toString());
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/SimpleClientBalanceConsumer.java`
#### Snippet
```java
                            logger.info(sBuffer.append("[Admin Reset] consumer is ")
                                    .append(consumerId).append(", partition=")
                                    .append(partition.toString()).append(", consume from max=")
                                    .append(currOffset).toString());
                            sBuffer.delete(0, sBuffer.length());
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/SimpleClientBalanceConsumer.java`
#### Snippet
```java
                    } catch (Throwable e) {
                        sBuffer.delete(0, sBuffer.length());
                        logger.info(sBuffer.append("register ").append(partition.toString())
                                .append(" throw exception ").append(e.toString()).toString());
                        sBuffer.delete(0, sBuffer.length());
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/SimpleClientBalanceConsumer.java`
#### Snippet
```java
                        sBuffer.delete(0, sBuffer.length());
                        logger.info(sBuffer.append("register ").append(partition.toString())
                                .append(" throw exception ").append(e.toString()).toString());
                        sBuffer.delete(0, sBuffer.length());
                    }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/SimpleClientBalanceConsumer.java`
#### Snippet
```java
                clientStatsInfo.bookReg2Broker(true);
                result.setFailResult(TErrCodeConstants.CONNECT_RETURN_NULL,
                        sBuffer.append(" register ").append(partition.toString())
                                .append(" return null!").toString());
                return result.isSuccess();
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/SimpleClientBalanceConsumer.java`
#### Snippet
```java
                logger.info(sBuffer.append("Registered partition: consumer is ")
                        .append(consumerId).append(", partition=")
                        .append(partition.toString()).append(", boostrapOffset=")
                        .append(boostrapOffset).toString());
                sBuffer.delete(0, sBuffer.length());
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/SimpleClientBalanceConsumer.java`
#### Snippet
```java
                } else {
                    result.setFailResult(response.getErrCode(),
                            sBuffer.append(" register ").append(partition.toString())
                                    .append(" return ").append(response.getErrMsg()).toString());
                }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/SimpleClientBalanceConsumer.java`
#### Snippet
```java
            result.setFailResult(TErrCodeConstants.UNSPECIFIED_ABNORMAL,
                    sBuffer.append("register ").append(partition.toString())
                            .append(" throw exception ").append(e.toString()).toString());
            sBuffer.delete(0, sBuffer.length());
            return result.isSuccess();
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/SimpleClientBalanceConsumer.java`
#### Snippet
```java
            taskContext.setFailProcessResult(400, sBuffer
                    .append("Get message error, reason is ")
                    .append(ee.toString()).toString());
            sBuffer.delete(0, sBuffer.length());
            return taskContext;
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/SimpleClientBalanceConsumer.java`
#### Snippet
```java
                    .append("Get message failed,topic=")
                    .append(topic).append(",partition=").append(partition)
                    .append(", throw info is ").append(ee.toString()).toString());
            sBuffer.delete(0, sBuffer.length());
        }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/consumer/SimpleClientBalanceConsumer.java`
#### Snippet
```java
            }
            logger.warn(sBuffer.append("register ")
                    .append(partition.toString()).append(" failure(")
                    .append(retryTimesRegister2Broker).append("), return ")
                    .append(result.getErrMsg()).toString());
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/utils/HttpUtils.java`
#### Snippet
```java
        // process result
        if (jsonRes != null) {
            System.out.println("query result is " + jsonRes.toString());
        }
    }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/broker/msgstore/disk/FileSegment.java`
#### Snippet
```java
                }
                logger.error(new StringBuilder(512).append("[File Store] Close ")
                        .append(this.file.getAbsoluteFile().toString())
                        .append("'s ").append(segmentType).append(" file failure").toString(), ee);
            }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/zookeeper/ZooKeeperWatcher.java`
#### Snippet
```java
     */
    public String prefix(final String str) {
        return this.toString() + " " + str;
    }

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/action/screen/Master.java`
#### Snippet
```java
            ProducerInfo producer = master.getProducerHolder().getProducerInfo(producerId);
            if (producer != null) {
                sBuilder.append(producer.toString());
            } else {
                sBuilder.append("No such producer!");
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/utils/WebParameterUtils.java`
#### Snippet
```java
            result.setFailResult(sBuffer.append("Parse parameter ")
                    .append(WebFieldDef.FLOWCTRLSET.name)
                    .append(" failure: '").append(e.toString()).toString());
            sBuffer.delete(0, sBuffer.length());
            return 0;
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/utils/WebParameterUtils.java`
#### Snippet
```java
                    .append("Parameter ").append(fieldDef.name)
                    .append(" decode error, exception is ")
                    .append(e.toString()).toString());
        }
        if (TStringUtils.isBlank(paramValue)) {
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/utils/WebParameterUtils.java`
#### Snippet
```java
                    .append("Parameter ").append(fieldDef.name)
                    .append(" decode error, exception is ")
                    .append(e.toString()).toString());
        }
        if (TStringUtils.isBlank(paramValue)) {
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/utils/WebParameterUtils.java`
#### Snippet
```java
            throw new Exception(new StringBuilder(512).append("Decode ")
                    .append(paramName).append("error, exception is ")
                    .append(e.toString()).toString());
        }
        if (TStringUtils.isBlank(decTmpParamVal)) {
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/utils/MetaConfigSamplePrint.java`
#### Snippet
```java
                        logger.error(sBuilder.append("[MetaConfig Error] Connect to node:[")
                                .append(hostName).append(",").append(nodeName)
                                .append("] IOException error is ").append(e.toString()).toString());
                    }
                    sBuilder.delete(0, sBuilder.length());
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/utils/MetaConfigSamplePrint.java`
#### Snippet
```java
                                .append(hostName).append(",").append(nodeName)
                                .append("] ServiceConnectFailedException error is ")
                                .append(e.toString()).toString());
                    }
                    sBuilder.delete(0, sBuilder.length());
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebBrokerConfHandler.java`
#### Snippet
```java
                                .append(" by ").append(WebFieldDef.BROKERIP.name)
                                .append(" error !, exception is :")
                                .append(e.toString()).toString());
                sBuffer.delete(0, sBuffer.length());
                return result.isSuccess();
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebAdminGroupCtrlHandler.java`
#### Snippet
```java
        logger.info(sBuffer.append("[Re-balance] Add rebalance consumer: group=")
                .append(groupName).append(", consumerIds=")
                .append(consumerIdSet.toString())
                .append(", reJoinWait=").append(reJoinWait)
                .append(", creator=").append(opEntity.getModifyUser()).toString());
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/handler/WebAdminGroupCtrlHandler.java`
#### Snippet
```java
            logger.info(sBuffer.append("[Admin reset] ").append(opEntity.getModifyUser())
                    .append(" set client-balance group consume from max offset, group set = ")
                    .append(filtedGroupSet.toString()).toString());
            sBuffer.delete(0, sBuffer.length());
            WebParameterUtils.buildSuccessResult(sBuffer);
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/nodemanage/nodebroker/BrokerAbnHolder.java`
#### Snippet
```java
            logger.info(new StringBuilder(512)
                    .append("[Broker AutoForbidden] remove forbidden brokers by reason ")
                    .append(reason).append(", release list is ").append(brokerFbdInfos.toString()).toString());
        }
    }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sink/es/ElasticsearchApi.java`
#### Snippet
```java
            StringBuilder mapping = new StringBuilder().append("{\n      \"properties\" : {\n")
                    .append(StringUtils.join(fieldList, ",\n")).append("\n      }\n}");
            System.out.println(mapping.toString());
            PutMappingRequest indexRequest = new PutMappingRequest(indexName)
                    .source(mapping.toString(), XContentType.JSON);
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sink/es/ElasticsearchApi.java`
#### Snippet
```java
            AcknowledgedResponse acknowledgedResponse = getEsClient().indices()
                    .putMapping(indexRequest, RequestOptions.DEFAULT);
            LOG.info("put mapping: {} result: {}", mapping.toString(), acknowledgedResponse.toString());
        }
    }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/hive/PartitionCreateRunnable.java`
#### Snippet
```java
                LOG.info("error PartitionCreateRunnable id:{},fileStatus:{},getModificationTime:{},"
                        + "fileArchiveDelayTime:{}", idConfig.getInlongGroupId(),
                        filePath.toString(),
                        fileStatus.getModificationTime(), fileArchiveDelayTime);
                LOG.info("inlongGroupId:{},partition:{} can not archived in path:{}.", idConfig.getInlongGroupId(),
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `inlong-sort-standalone/sort-standalone-source/src/main/java/org/apache/inlong/sort/standalone/sink/hive/PartitionCreateRunnable.java`
#### Snippet
```java
            LOG.info("ok PartitionCreateRunnable id:{},fileStatus:{},getModificationTime:{},"
                    + "fileArchiveDelayTime:{}", idConfig.getInlongGroupId(),
                    filePath.toString(),
                    fileStatus.getModificationTime(), fileArchiveDelayTime);
        }
```

## RuleId[id=InnerClassMayBeStatic]
### InnerClassMayBeStatic
Inner class `SnapshotSplitChangeEventSourceContextImpl` may be 'static'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/mysql/debezium/reader/SnapshotSplitReader.java`
#### Snippet
```java
     * watermark for each {@link MySqlSnapshotSplit}.
     */
    public class SnapshotSplitChangeEventSourceContextImpl
            implements
                ChangeEventSource.ChangeEventSourceContext {
```

### InnerClassMayBeStatic
Inner class `SnapshotSplitChangeEventSourceContext` may be 'static'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/oracle-cdc/src/main/java/org/apache/inlong/sort/cdc/oracle/source/reader/fetch/OracleScanFetchTask.java`
#### Snippet
```java
     * watermark for each {@link SnapshotSplit}.
     */
    public class SnapshotSplitChangeEventSourceContext
            implements
                ChangeEventSource.ChangeEventSourceContext {
```

### InnerClassMayBeStatic
Inner class `TableChangeHolder` may be 'static'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/postgres-cdc/src/main/java/org/apache/inlong/sort/cdc/postgres/debezium/internal/DebeziumChangeFetcher.java`
#### Snippet
```java
    }

    private class TableChangeHolder {

        public TableChange tableChange;
```

### InnerClassMayBeStatic
Inner class `DefaultSendCallback` may be 'static'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/tools/cli/CliProducer.java`
#### Snippet
```java
    }

    private class DefaultSendCallback implements MessageSentCallback {

        @Override
```

### InnerClassMayBeStatic
Inner class `BrokerComparator` may be 'static'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/action/screen/config/BrokerList.java`
#### Snippet
```java
    }

    public class BrokerComparator implements Comparator<BrokerInfo> {

        @Override
```

### InnerClassMayBeStatic
Inner class `BrokerComparator` may be 'static'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/action/screen/cluster/ClusterManager.java`
#### Snippet
```java
    }

    public class BrokerComparator implements Comparator<BrokerInfo> {

        @Override
```

## RuleId[id=SuspiciousMethodCalls]
### SuspiciousMethodCalls
Suspicious call to 'Map.containsKey()'
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/utils/MapBackedSet.java`
#### Snippet
```java
    @Override
    public boolean contains(Object o) {
        return map.containsKey(o);
    }

```

### SuspiciousMethodCalls
Suspicious call to 'ConcurrentHashMap.contains()'
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/network/Sender.java`
#### Snippet
```java
        Channel channel = client.getChannel();
        // used by async send
        if (callbacks.contains(channel) && MapUtils.isNotEmpty(callbacks.get(channel))) {
            return false;
        }
```

### SuspiciousMethodCalls
'Set' may not contain objects of type 'ListStateDescriptor'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/kafka/src/main/java/org/apache/inlong/sort/kafka/FlinkKafkaProducer.java`
#### Snippet
```java
        if (context.getOperatorStateStore()
                .getRegisteredStateNames()
                .contains(NEXT_TRANSACTIONAL_ID_HINT_DESCRIPTOR)) {
            migrateNextTransactionalIdHindState(context);
        }
```

### SuspiciousMethodCalls
Suspicious call to 'ConcurrentHashMap.containsKey()'
in `inlong-tubemq/tubemq-core/src/main/java/org/apache/inlong/tubemq/corebase/utils/ConcurrentHashSet.java`
#### Snippet
```java
    @Override
    public boolean contains(Object item) {
        return keyValMap.containsKey(item);
    }

```

### SuspiciousMethodCalls
'ConcurrentHashMap\>' may not contain values of type 'ConcurrentHashSet'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/metamanage/metastore/impl/AbsConsumeCtrlMapperImpl.java`
#### Snippet
```java
            keySet.remove(recordKey);
            if (keySet.isEmpty()) {
                topic2RecordCache.remove(curEntity.getTopicName(), new ConcurrentHashSet<>());
            }
        }
```

### SuspiciousMethodCalls
'ConcurrentHashMap\>' may not contain values of type 'ConcurrentHashSet'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/metamanage/metastore/impl/AbsConsumeCtrlMapperImpl.java`
#### Snippet
```java
            keySet.remove(recordKey);
            if (keySet.isEmpty()) {
                group2RecordCache.remove(curEntity.getGroupName(), new ConcurrentHashSet<>());
            }
        }
```

### SuspiciousMethodCalls
'ConcurrentHashMap\>' may not contain values of type 'ConcurrentHashSet'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/metamanage/metastore/impl/AbsTopicDeployMapperImpl.java`
#### Snippet
```java
            keySet.remove(recordKey);
            if (keySet.isEmpty()) {
                topicName2RecordCache.remove(curEntity.getTopicName(), new ConcurrentHashSet<>());
            }
        }
```

### SuspiciousMethodCalls
'ConcurrentHashMap\>' may not contain values of type 'ConcurrentHashSet'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/metamanage/metastore/impl/AbsTopicDeployMapperImpl.java`
#### Snippet
```java
            keySet.remove(recordKey);
            if (keySet.isEmpty()) {
                brokerId2RecordCache.remove(curEntity.getBrokerId(), new ConcurrentHashSet<>());
            }
        }
```

### SuspiciousMethodCalls
'ConcurrentHashMap\>' may not contain values of type 'ConcurrentHashSet'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/metamanage/metastore/impl/AbsTopicDeployMapperImpl.java`
#### Snippet
```java
            keySet.remove(curEntity.getTopicName());
            if (keySet.isEmpty()) {
                brokerId2TopicNameCache.remove(curEntity.getBrokerId(), new ConcurrentHashSet<>());
            }
        }
```

## RuleId[id=SynchronizeOnNonFinalField]
### SynchronizeOnNonFinalField
Synchronization on a non-final field `this.lastUpdateTime`
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/config/EncryptConfigEntry.java`
#### Snippet
```java
            encryptInfo = null;
        }
        synchronized (this.lastUpdateTime) {
            if (visitTime == this.lastUpdateTime.get()) {
                RSAPublicKey rsaKey = EncryptUtil.loadPublicKeyByText(pubKey);
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `lock`
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/redis/src/main/java/org/apache/inlong/sort/redis/sink/AbstractRedisSinkFunction.java`
#### Snippet
```java
        List<OUT> redisOutputs = serialize(in);
        sendMetrics(in.toString().getBytes());
        synchronized (lock) {
            rows.addAll(redisOutputs);
            if (forceFlush || rows.size() >= batchSize) {
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `lock`
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/redis/src/main/java/org/apache/inlong/sort/redis/sink/AbstractRedisSinkFunction.java`
#### Snippet
```java

    private void closeClient() {
        synchronized (lock) {
            if (redisCommandsContainer != null) {
                flush();
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `lock`
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/redis/src/main/java/org/apache/inlong/sort/redis/sink/AbstractRedisSinkFunction.java`
#### Snippet
```java
    public void snapshotState(FunctionSnapshotContext functionSnapshotContext) throws Exception {
        LOG.info("redis start snapshotState, id: {}", functionSnapshotContext.getCheckpointId());
        synchronized (lock) {
            listState.clear();
            listState.addAll(rows);
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `lock`
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/redis/src/main/java/org/apache/inlong/sort/redis/sink/AbstractRedisSinkFunction.java`
#### Snippet
```java

    private void flush() {
        synchronized (lock) {
            try {
                if (rows != null && rows.size() > 0) {
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `lock`
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/mysql/source/assigners/MySqlSnapshotSplitAssigner.java`
#### Snippet
```java
                                                .map(MySqlSnapshotSplit::toSchemaLessSnapshotSplit)
                                                .collect(Collectors.toList());
                                synchronized (lock) {
                                    remainingSplits.addAll(schemaLessSnapshotSplits);
                                    remainingTables.remove(nextTable);
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `lock`
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/mysql-cdc/src/main/java/org/apache/inlong/sort/cdc/mysql/source/assigners/MySqlSnapshotSplitAssigner.java`
#### Snippet
```java
    @Override
    public Optional<MySqlSplit> getNext() {
        synchronized (lock) {
            if (!remainingSplits.isEmpty()) {
                // return remaining splits firstly
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `client`
in `inlong-audit/audit-sdk/src/main/java/org/apache/inlong/audit/send/SenderChannel.java`
#### Snippet
```java
            }

            synchronized (client) {
                ChannelFuture future = client.connect(this.ipPort.addr).sync();
                this.channel = future.channel();
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `this.counterGroup`
in `inlong-common/src/main/java/org/apache/inlong/common/monitor/StatRunner.java`
#### Snippet
```java
                    str = (String) iterator.next();
                    cnt = 0L;
                    synchronized (this.counterGroup) {
                        cnt = this.counterGroup.get(str);
                        this.counterGroup.set(str, 0L);
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `this.counterGroupExt`
in `inlong-common/src/main/java/org/apache/inlong/common/monitor/StatRunner.java`
#### Snippet
```java

                counters.clear();
                synchronized (this.counterGroupExt) {
                    Iterator ite = this.counterGroupExt.getCounters().keySet().iterator();

```

## RuleId[id=ConditionCoveredByFurtherCondition]
### ConditionCoveredByFurtherCondition
Condition 'other == null' covered by subsequent condition '!(other instanceof HostInfo)'
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/config/HostInfo.java`
#### Snippet
```java
    @Override
    public boolean equals(Object other) {
        if (other == null || !(other instanceof HostInfo)) {
            return false;
        }
```

### ConditionCoveredByFurtherCondition
Condition 'other == null' covered by subsequent condition '!(other instanceof EncryptConfigEntry)'
in `inlong-sdk/dataproxy-sdk/src/main/java/org/apache/inlong/sdk/dataproxy/config/EncryptConfigEntry.java`
#### Snippet
```java
    @Override
    public boolean equals(Object other) {
        if (other == null || !(other instanceof EncryptConfigEntry)) {
            return false;
        }
```

### ConditionCoveredByFurtherCondition
Condition 'errorUrl.isEmpty()' covered by subsequent condition '!errorUrl.startsWith(...)'
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/starrocks/src/main/java/org/apache/inlong/sort/starrocks/manager/StarRocksStreamLoadVisitor.java`
#### Snippet
```java

    private String getErrorLog(String errorUrl) {
        if (errorUrl == null || errorUrl.isEmpty() || !errorUrl.startsWith("http")) {
            return null;
        }
```

### ConditionCoveredByFurtherCondition
Condition 'confFilterStr.length() == 2' covered by subsequent condition 'confFilterStr.equals(...)'
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/common/utils/WebParameterUtils.java`
#### Snippet
```java
        }
        if (confFilterStr == null
                || (confFilterStr.length() == 2
                        && confFilterStr.equals(TServerConstants.BLANK_FILTER_ITEM_STR))) {
            return false;
```

## RuleId[id=RegExpSingleCharAlternation]
### RegExpSingleCharAlternation
Single character alternation in RegExp
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/web/simplemvc/RequestDispatcher.java`
#### Snippet
```java
        List<File> actionClassFileList = getFileList(url.getFile(), ".class");
        for (File f : actionClassFileList) {
            String pathStr = f.getPath().replaceAll("/|\\\\", ".");
            Class clazz = Class.forName(
                    pathStr.substring(pathStr.indexOf(packageName), pathStr.length() - 6));
```

### RegExpSingleCharAlternation
Single character alternation in RegExp
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/resource/sink/redis/RedisResourceClient.java`
#### Snippet
```java
    private static final String REDIS_TEST_VALUE = "__inLong_test_value__";
    private static final String HOST_PORT_SEPARATOR = ":";
    private static final String NODE_LINE_SEPARATOR = ",|;";

    /**
```

## RuleId[id=SpringTransactionalMethodCallsInspection]
### SpringTransactionalMethodCallsInspection
@Transactional self-invocation (in effect, a method within the target object calling another method of the target object) does not lead to an actual transaction at runtime
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/core/impl/SortClusterServiceImpl.java`
#### Snippet
```java
        try {
            this.reloadInterval = DEFAULT_HEARTBEAT_INTERVAL_MS;
            reload();
            setReloadTimer();
        } catch (Throwable t) {
```

### SpringTransactionalMethodCallsInspection
@Transactional self-invocation (in effect, a method within the target object calling another method of the target object) does not lead to an actual transaction at runtime
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/core/impl/AuditServiceImpl.java`
#### Snippet
```java
        LOGGER.info("init audit base item cache map for {}", AuditServiceImpl.class.getSimpleName());
        try {
            refreshBaseItemCache();
        } catch (Throwable t) {
            LOGGER.error("initialize audit base item cache error", t);
```

### SpringTransactionalMethodCallsInspection
@Transactional self-invocation (in effect, a method within the target object calling another method of the target object) does not lead to an actual transaction at runtime
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/core/impl/SortSourceServiceImpl.java`
#### Snippet
```java
        LOGGER.info("create repository for " + SortSourceServiceImpl.class.getSimpleName());
        try {
            reload();
            setReloadTimer();
        } catch (Throwable t) {
```

### SpringTransactionalMethodCallsInspection
@Transactional self-invocation (in effect, a method within the target object calling another method of the target object) does not lead to an actual transaction at runtime
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/stream/InlongStreamServiceImpl.java`
#### Snippet
```java
        // add record
        streamMapper.insertSelective(streamEntity);
        saveField(request.getInlongGroupId(), request.getInlongStreamId(), request.getFieldList());
        List<InlongStreamExtInfo> extList = request.getExtList();
        if (CollectionUtils.isNotEmpty(extList)) {
```

### SpringTransactionalMethodCallsInspection
@Transactional self-invocation (in effect, a method within the target object calling another method of the target object) does not lead to an actual transaction at runtime
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/stream/InlongStreamServiceImpl.java`
#### Snippet
```java
        List<InlongStreamExtInfo> extList = request.getExtList();
        if (CollectionUtils.isNotEmpty(extList)) {
            saveOrUpdateExt(request.getInlongGroupId(), request.getInlongStreamId(), extList);
        }
        return streamEntity.getId();
```

### SpringTransactionalMethodCallsInspection
@Transactional self-invocation (in effect, a method within the target object calling another method of the target object) does not lead to an actual transaction at runtime
in `inlong-manager/manager-service/src/main/java/org/apache/inlong/manager/service/repository/DataProxyConfigRepository.java`
#### Snippet
```java
        try {
            this.reloadInterval = DEFAULT_HEARTBEAT_INTERVAL_MS;
            reload();
            setReloadTimer();
        } catch (Throwable t) {
```

## RuleId[id=UnnecessaryContinue]
### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/metamanage/metastore/impl/bdbimpl/BdbMetaConfigMapperImpl.java`
#### Snippet
```java
                        .append(" Will retry after 5 seconds.").toString());
                Thread.sleep(5 * 1000);
                continue;
            } catch (InsufficientLogException insufficientLogEx) {
                logger.error(new StringBuilder(TBaseConstants.BUILDER_DEFAULT_SIZE)
```

### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/metamanage/metastore/impl/bdbimpl/BdbMetaConfigMapperImpl.java`
#### Snippet
```java
                masterNodeInfo.setNodeStatus(-1);
                metaSamplePrint.printExceptionCaught(e, node.getHostName(), node.getName());
                continue;
            } catch (ServiceDispatcher.ServiceConnectFailedException e) {
                masterNodeInfo.setNodeStatus(-2);
```

### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/metamanage/metastore/impl/bdbimpl/BdbMetaConfigMapperImpl.java`
#### Snippet
```java
                masterNodeInfo.setNodeStatus(-2);
                metaSamplePrint.printExceptionCaught(e, node.getHostName(), node.getName());
                continue;
            } catch (Throwable ee) {
                masterNodeInfo.setNodeStatus(-3);
```

### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/metamanage/metastore/impl/bdbimpl/BdbMetaConfigMapperImpl.java`
#### Snippet
```java
                masterNodeInfo.setNodeStatus(-3);
                metaSamplePrint.printExceptionCaught(ee, node.getHostName(), node.getName());
                continue;
            }
        }
```

## RuleId[id=CopyConstructorMissesField]
### CopyConstructorMissesField
Copy constructor does not copy field 'partStatusMap'
in `inlong-tubemq/tubemq-client/src/main/java/org/apache/inlong/tubemq/client/common/QueryMetaResult.java`
#### Snippet
```java
    }

    public QueryMetaResult(QueryMetaResult other) {
        super(other);
    }
```

### CopyConstructorMissesField
Copy constructor does not copy 4 fields
in `inlong-tubemq/tubemq-server/src/main/java/org/apache/inlong/tubemq/server/master/metamanage/metastore/dao/entity/BaseEntity.java`
#### Snippet
```java
     * @param other  the BaseEntity initial object
     */
    public BaseEntity(BaseEntity other) {
        this.dataVersionId = other.dataVersionId;
        setCreateInfo(other.createUser, other.createDate);
```

## RuleId[id=EqualsWhichDoesntCheckParameterClass]
### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `inlong-sort/sort-flink/sort-flink-v1.13/sort-connectors/redis/src/main/java/org/apache/inlong/sort/redis/common/mapper/row/RowRedisMapper.java`
#### Snippet
```java

    @Override
    public boolean equals(Object obj) {
        RedisCommand redisCommand = ((RowRedisMapper) obj).redisCommand;
        return this.redisCommand == redisCommand;
```

## RuleId[id=CastCanBeRemovedNarrowingVariableType]
### CastCanBeRemovedNarrowingVariableType
Cast may be removed by changing the type of 'itemT' to 'MetricItem'
in `inlong-common/src/main/java/org/apache/inlong/common/metric/MetricListenerRunnable.java`
#### Snippet
```java
                for (Object itemT : items) {
                    if (itemT instanceof MetricItem) {
                        MetricItem item = (MetricItem) itemT;
                        String dimensionsKey = item.getDimensionsKey();
                        Map<String, String> dimensions = item.getDimensions();
```

