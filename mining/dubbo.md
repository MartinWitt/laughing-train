# dubbo 
 
# Bad smells
I found 3211 bad smells with 82 repairable:
| ruleID | number | fixable |
| --- | --- | --- |
| JavadocDeclaration | 837 | false |
| JavadocReference | 325 | false |
| Deprecation | 310 | false |
| UNCHECKED_WARNING | 272 | false |
| FieldMayBeFinal | 209 | false |
| DeprecatedIsStillUsed | 116 | false |
| DuplicatedCode | 110 | false |
| DataFlowIssue | 105 | false |
| ConstantValue | 63 | false |
| CatchMayIgnoreException | 51 | false |
| UnusedAssignment | 45 | false |
| CommentedOutCode | 43 | false |
| NullableProblems | 41 | false |
| TrivialIf | 39 | false |
| RegExpUnnecessaryNonCapturingGroup | 37 | false |
| FieldCanBeLocal | 36 | false |
| IgnoreResultOfCall | 34 | false |
| UnnecessaryLocalVariable | 31 | true |
| RegExpRedundantEscape | 29 | false |
| SpringBootApplicationYaml | 22 | false |
| RedundantCast | 21 | false |
| RedundantMethodOverride | 18 | false |
| ArraysAsListWithZeroOrOneArgument | 18 | false |
| JavadocLinkAsPlainText | 18 | false |
| UnnecessaryToStringCall | 18 | true |
| LongLiteralsEndingWithLowercaseL | 17 | false |
| EmptyStatementBody | 16 | false |
| SynchronizationOnLocalVariableOrMethodParameter | 15 | false |
| StringBufferReplaceableByString | 15 | false |
| StringConcatenationInsideStringBufferAppend | 15 | false |
| DanglingJavadoc | 14 | false |
| AutoCloseableResource | 13 | false |
| UseBulkOperation | 13 | false |
| RedundantLengthCheck | 13 | false |
| SimplifyStreamApiCallChains | 12 | false |
| PointlessArithmeticExpression | 11 | false |
| BusyWait | 11 | false |
| RegExpSimplifiable | 8 | false |
| CollectionAddAllCanBeReplacedWithConstructor | 8 | false |
| CollectionContainsUrl | 7 | false |
| UnnecessaryReturn | 7 | true |
| OptionalGetWithoutIsPresent | 7 | false |
| PointlessBitwiseExpression | 6 | false |
| ManualMinMaxCalculation | 6 | false |
| DefaultAnnotationParam | 6 | false |
| TypeParameterExtendsObject | 6 | false |
| InnerClassMayBeStatic | 6 | true |
| SuspiciousMethodCalls | 6 | false |
| ToArrayCallWithZeroLengthArrayArgument | 5 | true |
| TrivialStringConcatenation | 5 | false |
| RedundantArrayCreation | 5 | true |
| NonFinalFieldInEnum | 5 | false |
| SimplifiableConditionalExpression | 5 | false |
| IOStreamConstructor | 5 | false |
| UnnecessaryContinue | 5 | false |
| FinalStaticMethod | 4 | false |
| ManualArrayToCollectionCopy | 4 | false |
| CaughtExceptionImmediatelyRethrown | 4 | false |
| UnnecessaryStringEscape | 4 | true |
| EqualsWhichDoesntCheckParameterClass | 4 | false |
| RedundantTypeArguments | 3 | false |
| UnnecessarySemicolon | 3 | false |
| UnnecessaryCallToStringValueOf | 3 | false |
| CharsetObjectCanBeUsed | 3 | false |
| InfiniteLoopStatement | 3 | false |
| SynchronizeOnNonFinalField | 3 | false |
| UnnecessaryModifier | 2 | true |
| EmptyFinallyBlock | 2 | false |
| DuplicateCondition | 2 | false |
| ReturnFromFinallyBlock | 2 | false |
| MismatchedCollectionQueryUpdate | 2 | false |
| TypeParameterHidesVisibleType | 2 | false |
| ProtectedMemberInFinalClass | 2 | true |
| ComparatorResultComparison | 2 | false |
| OptionalUsedAsFieldOrParameterType | 2 | false |
| NonStrictComparisonCanBeEquality | 2 | true |
| EmptyTryBlock | 2 | false |
| RefusedBequest | 2 | false |
| DuplicateExpressions | 2 | false |
| ThrowFromFinallyBlock | 2 | false |
| IfStatementWithIdenticalBranches | 2 | false |
| MismatchedArrayReadWrite | 1 | false |
| CStyleArrayDeclaration | 1 | false |
| AccessStaticViaInstance | 1 | false |
| IntegerMultiplicationImplicitCastToLong | 1 | false |
| OptionalAssignedToNull | 1 | false |
| StringConcatenationInLoops | 1 | false |
| StringEqualsCharSequence | 1 | false |
| StringEquality | 1 | false |
| SuspiciousSystemArraycopy | 1 | false |
| RedundantClassCall | 1 | false |
| ManualArrayCopy | 1 | false |
| CollectionAddedToSelf | 1 | false |
| RedundantStringFormatCall | 1 | false |
| FuseStreamOperations | 1 | false |
| PointlessNullCheck | 1 | false |
| NullArgumentToVariableArgMethod | 1 | false |
| OptionalIsPresent | 1 | false |
| RedundantOperationOnEmptyContainer | 1 | false |
| RedundantCollectionOperation | 1 | false |
| ConditionCoveredByFurtherCondition | 1 | false |
| CopyConstructorMissesField | 1 | false |
| ThrowableNotThrown | 1 | false |
## RuleId[id=ToArrayCallWithZeroLengthArrayArgument]
### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new String\[collection.size()\]'
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/DataParseUtils.java`
#### Snippet
```java

    public static String[] toStringArray(Collection<String> collection) {
        return collection == null ? null : collection.toArray(new String[collection.size()]);
    }
}
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new ChannelHandler\[channelHandlers.size()\]'
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/netty/NettyServer.java`
#### Snippet
```java
                }

                channelPipeline.addLast(channelHandlers.toArray(new ChannelHandler[channelHandlers.size()]));

                List<ChannelHandler> unSharedHandlers = unSharedHandlerCallBack.getUnSharedHandlers(url);
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new String\[attributeNamesToIgnore.size()\]'
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/AnnotationUtils.java`
#### Snippet
```java
            }
            // extends the ignored list
            actualIgnoreAttributeNames = attributeNamesToIgnore.toArray(new String[attributeNamesToIgnore.size()]);
        }

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Class\[groups.size()\]'
in `dubbo-filter/dubbo-filter-validation/src/main/java/org/apache/dubbo/validation/support/jvalidation/JValidator.java`
#### Snippet
```java

        // convert list to array
        Class<?>[] classgroups = groups.toArray(new Class[groups.size()]);

        Object parameterBean = getMethodParameterBean(clazz, method, arguments);
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Class\[groups.size()\]'
in `dubbo-filter/dubbo-filter-validation/src/main/java/org/apache/dubbo/validation/support/jvalidation/JValidatorNew.java`
#### Snippet
```java

        // convert list to array
        Class<?>[] classgroups = groups.toArray(new Class[groups.size()]);

        Object parameterBean = getMethodParameterBean(clazz, method, arguments);
```

## RuleId[id=UnnecessaryModifier]
### UnnecessaryModifier
Modifier `static` is redundant for inner interfaces
in `dubbo-common/src/main/java/org/apache/dubbo/common/bytecode/ClassGenerator.java`
#### Snippet
```java
    }

    public static interface DC {

    } // dynamic class tag interface.
```

### UnnecessaryModifier
Modifier `static` is redundant for inner interfaces
in `dubbo-common/src/main/java/org/apache/dubbo/common/bytecode/Mixin.java`
#### Snippet
```java
    abstract public Object newInstance(Object[] ds);

    public static interface MixinAware {
        void setMixinInstance(Object instance);
    }
```

## RuleId[id=MismatchedArrayReadWrite]
### MismatchedArrayReadWrite
Contents of array `objects` are read, but never written to
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/RestRPCInvocationUtil.java`
#### Snippet
```java
        parseContext.setRequest(servletRequest);

        Object[] objects = new Object[restMethodMetadata.getArgInfos().size()];
        parseContext.setArgs(Arrays.asList(objects));
        parseContext.setArgInfos(restMethodMetadata.getArgInfos());
```

## RuleId[id=PointlessArithmeticExpression]
### PointlessArithmeticExpression
`off + 0` can be replaced with 'off'
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/Bytes.java`
#### Snippet
```java
        b[off + 2] = (byte) (v >>> 40);
        b[off + 1] = (byte) (v >>> 48);
        b[off + 0] = (byte) (v >>> 56);
    }

```

### PointlessArithmeticExpression
`off + 0` can be replaced with 'off'
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/Bytes.java`
#### Snippet
```java
        b[off + 2] = (byte) (i >>> 8);
        b[off + 1] = (byte) (i >>> 16);
        b[off + 0] = (byte) (i >>> 24);
    }

```

### PointlessArithmeticExpression
`off + 0` can be replaced with 'off'
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/Bytes.java`
#### Snippet
```java
        b[off + 2] = (byte) (j >>> 40);
        b[off + 1] = (byte) (j >>> 48);
        b[off + 0] = (byte) (j >>> 56);
    }

```

### PointlessArithmeticExpression
`off + 0` can be replaced with 'off'
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/Bytes.java`
#### Snippet
```java
        b[off + 2] = (byte) (v >>> 8);
        b[off + 1] = (byte) (v >>> 16);
        b[off + 0] = (byte) (v >>> 24);
    }

```

### PointlessArithmeticExpression
`off + 0` can be replaced with 'off'
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/Bytes.java`
#### Snippet
```java
                ((b[off + 2] & 0xFF) << 8) +
                ((b[off + 1] & 0xFF) << 16) +
                ((b[off + 0]) << 24);
        return Float.intBitsToFloat(i);
    }
```

### PointlessArithmeticExpression
`off + 0` can be replaced with 'off'
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/Bytes.java`
#### Snippet
```java
    public static short bytes2short(byte[] b, int off) {
        return (short) (((b[off + 1] & 0xFF) << 0) +
                ((b[off + 0]) << 8));
    }

```

### PointlessArithmeticExpression
`off + 0` can be replaced with 'off'
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/Bytes.java`
#### Snippet
```java
                ((b[off + 2] & 0xFFL) << 40) +
                ((b[off + 1] & 0xFFL) << 48) +
                (((long) b[off + 0]) << 56);
        return Double.longBitsToDouble(j);
    }
```

### PointlessArithmeticExpression
`off + 0` can be replaced with 'off'
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/Bytes.java`
#### Snippet
```java
                ((b[off + 2] & 0xFFL) << 40) +
                ((b[off + 1] & 0xFFL) << 48) +
                (((long) b[off + 0]) << 56);
    }

```

### PointlessArithmeticExpression
`off + 0` can be replaced with 'off'
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/Bytes.java`
#### Snippet
```java
                ((b[off + 2] & 0xFF) << 8) +
                ((b[off + 1] & 0xFF) << 16) +
                ((b[off + 0]) << 24);
    }

```

### PointlessArithmeticExpression
`off + 0` can be replaced with 'off'
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/Bytes.java`
#### Snippet
```java
    public static void short2bytes(short v, byte[] b, int off) {
        b[off + 1] = (byte) v;
        b[off + 0] = (byte) (v >>> 8);
    }

```

### PointlessArithmeticExpression
`1 * 1024` can be replaced with '1024'
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/Constants.java`
#### Snippet
```java
    int MAX_BUFFER_SIZE = 16 * 1024;

    int MIN_BUFFER_SIZE = 1 * 1024;

    String IDLE_TIMEOUT_KEY = "idle.timeout";
```

## RuleId[id=EmptyStatementBody]
### EmptyStatementBody
`if` statement has empty body
in `dubbo-remoting/dubbo-remoting-netty4/src/main/java/org/apache/dubbo/remoting/transport/netty4/NettyConfigOperator.java`
#### Snippet
```java

        // todo distinguish between client and server channel
        if( isClientSide(channel)){
            //todo config client channel handler
        }else {
```

### EmptyStatementBody
`if` statement has empty body
in `dubbo-remoting/dubbo-remoting-netty4/src/main/java/org/apache/dubbo/remoting/transport/netty4/NettyClient.java`
#### Snippet
```java
        } finally {
            // just add new valid channel to NettyChannel's cache
            if (!isConnected()) {
                //future.cancel(true);
            }
```

### EmptyStatementBody
`if` statement has empty body
in `dubbo-remoting/dubbo-remoting-netty4/src/main/java/org/apache/dubbo/remoting/transport/netty4/NettyClient.java`
#### Snippet
```java
        } finally {
            // just add new valid channel to NettyChannel's cache
            if (!isConnected()) {
                //future.cancel(true);
            }
```

### EmptyStatementBody
`if` statement has empty body
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/DubboTestChecker.java`
#### Snippet
```java
    public void executionStarted(TestIdentifier testIdentifier) {
        TestSource testSource = testIdentifier.getSource().orElse(null);
        if (testSource instanceof ClassSource) {
//            ClassSource source = (ClassSource) testSource;
//            log("Run test class: " + source.getClassName());
```

### EmptyStatementBody
`if` statement has empty body
in `dubbo-registry/dubbo-registry-zookeeper/src/main/java/org/apache/dubbo/registry/zookeeper/ZookeeperRegistry.java`
#### Snippet
```java
                logger.warn(REGISTRY_ZOOKEEPER_EXCEPTION, "", "", "Url of this instance will be deleted from registry soon. " +
                    "Dubbo client will try to re-register once a new session is created.");
            } else if (state == StateListener.SUSPENDED) {

            } else if (state == StateListener.CONNECTED) {
```

### EmptyStatementBody
`if` statement has empty body
in `dubbo-registry/dubbo-registry-zookeeper/src/main/java/org/apache/dubbo/registry/zookeeper/ZookeeperRegistry.java`
#### Snippet
```java
            } else if (state == StateListener.SUSPENDED) {

            } else if (state == StateListener.CONNECTED) {

            }
```

### EmptyStatementBody
`if` statement has empty body
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/FrameworkServiceRepository.java`
#### Snippet
```java
        String key = providerModel.getServiceKey();
        ProviderModel previous = providers.putIfAbsent(key, providerModel);
        if (previous != null && previous != providerModel) {
            // TODO callback service multi instances
            // throw new IllegalStateException("Register duplicate provider for key: " + key);
```

### EmptyStatementBody
`if` statement has empty body
in `dubbo-common/src/main/java/org/apache/dubbo/common/URLStrParser.java`
#### Snippet
```java
        int hostEndIdx = lastIndexOf(decodedBody, ':', starIdx, endIdx);
        if (hostEndIdx > 0 && hostEndIdx < decodedBody.length() - 1) {
            if (lastIndexOf(decodedBody, '%', starIdx, endIdx) > hostEndIdx) {
                // ipv6 address with scope id
                // e.g. fe80:0:0:0:894:aeec:f37d:23e1%en0
```

### EmptyStatementBody
`if` statement has empty body
in `dubbo-common/src/main/java/org/apache/dubbo/common/url/component/URLAddress.java`
#### Snippet
```java
        int i = decodeStr.lastIndexOf(':');
        if (i >= 0 && i < decodeStr.length() - 1) {
            if (decodeStr.lastIndexOf('%') > i) {
                // ipv6 address with scope id
                // e.g. fe80:0:0:0:894:aeec:f37d:23e1%en0
```

### EmptyStatementBody
`if` statement has empty body
in `dubbo-common/src/main/java/org/apache/dubbo/common/url/component/URLAddress.java`
#### Snippet
```java
        i = decodeStr.lastIndexOf(':');
        if (i >= 0 && i < decodeStr.length() - 1) {
            if (decodeStr.lastIndexOf('%') > i) {
                // ipv6 address with scope id
                // e.g. fe80:0:0:0:894:aeec:f37d:23e1%en0
```

### EmptyStatementBody
`if` statement has empty body
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/UrlUtils.java`
#### Snippet
```java
        i = url.lastIndexOf(':');
        if (i >= 0 && i < url.length() - 1) {
            if (url.lastIndexOf('%') > i) {
                // ipv6 address with scope id
                // e.g. fe80:0:0:0:894:aeec:f37d:23e1%en0
```

### EmptyStatementBody
`if` statement has empty body
in `dubbo-common/src/main/java/org/apache/dubbo/config/AbstractConfig.java`
#### Snippet
```java

                    invokeSetParameters(newMap, this);
                } else if (isNestedSetter(this, method)) {
                    // not support
                }
```

### EmptyStatementBody
`if` statement has empty body
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/ReferenceAnnotationBeanPostProcessor.java`
#### Snippet
```java
                    propertyValue.setOptional(true);
                }
            } else if (isAnnotatedReferenceBean(beanDefinition)) {
                // extract beanClass from java-config bean method generic return type: ReferenceBean<DemoService>
                //Class beanClass = getBeanFactory().getType(beanName);
```

### EmptyStatementBody
`if` statement has empty body
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/schema/DubboBeanDefinitionParser.java`
#### Snippet
```java
        beanDefinition.setAttribute(BEAN_NAME, beanName);

        if (ProtocolConfig.class.equals(beanClass)) {
//            for (String name : parserContext.getRegistry().getBeanDefinitionNames()) {
//                BeanDefinition definition = parserContext.getRegistry().getBeanDefinition(name);
```

### EmptyStatementBody
`else` statement has empty body
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcServiceContext.java`
#### Snippet
```java
                    }
                    return CompletableFuture.completedFuture(o);
                } else {
                    // The service has a normal sync method signature, should get future from RpcContext.
                }
```

### EmptyStatementBody
`else` statement has empty body
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/RpcContext.java`
#### Snippet
```java
                    f.run();
                    return f;
                } else {

                }
```

## RuleId[id=EmptyFinallyBlock]
### EmptyFinallyBlock
Empty `finally` block
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/filter/FilterChainBuilder.java`
#### Snippet
```java
                }
                throw e;
            } finally {

            }
```

### EmptyFinallyBlock
Empty `finally` block
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/filter/FilterChainBuilder.java`
#### Snippet
```java
                }
                throw e;
            } finally {

            }
```

## RuleId[id=CStyleArrayDeclaration]
### CStyleArrayDeclaration
C-style array declaration of local variable `str`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/MD5Utils.java`
#### Snippet
```java

        int j = md5.length;
        char str[] = new char[j * 2];
        int k = 0;
        for (int i = 0; i < j; i++) {
```

## RuleId[id=CommentedOutCode]
### CommentedOutCode
Commented out code (5 lines)
in `dubbo-demo/dubbo-demo-xml/dubbo-demo-xml-provider/src/main/java/org/apache/dubbo/demo/provider/DemoServiceImpl.java`
#### Snippet
```java
    public CompletableFuture<String> sayHelloAsync(String name) {
        CompletableFuture<String> cf = CompletableFuture.supplyAsync(() -> {
//            try {
//                Thread.sleep(1000);
//            } catch (InterruptedException e) {
```

### CommentedOutCode
Commented out code (8 lines)
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/bootstrap/builders/ServiceBuilder.java`
#### Snippet
```java
    }

//    @Override
//    public ServiceBuilder<U> mock(String mock) {
//        throw new IllegalArgumentException("mock doesn't support on provider side");
```

### CommentedOutCode
Commented out code (21 lines)
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/deploy/DefaultApplicationDeployer.java`
#### Snippet
```java

        // Ignore checking new module after start
//        executorRepository.getSharedExecutor().submit(() -> {
//            try {
//                while (isStarting()) {
```

### CommentedOutCode
Commented out code (2 lines)
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/DubboTestChecker.java`
#### Snippet
```java
        TestSource testSource = testIdentifier.getSource().orElse(null);
        if (testSource instanceof ClassSource) {
//            ClassSource source = (ClassSource) testSource;
//            log("Run test class: " + source.getClassName());
        } else if (testSource instanceof MethodSource) {
```

### CommentedOutCode
Commented out code (2 lines)
in `dubbo-xds/src/main/java/org/apache/dubbo/registry/xds/util/NodeBuilder.java`
#### Snippet
```java

    public static Node build() {
//        String podName = System.getenv("metadata.name");
//        String podNamespace = System.getenv("metadata.namespace");

```

### CommentedOutCode
Commented out code (4 lines)
in `dubbo-xds/src/main/java/org/apache/dubbo/registry/xds/util/bootstrap/BootstrapperImpl.java`
#### Snippet
```java
            ChannelCredentials channelCredentials =
                parseChannelCredentials(JsonUtil.checkObjectList(rawChannelCredsList), serverUri);
//            if (channelCredentials == null) {
//                throw new XdsInitializationException(
//                    "Server " + serverUri + ": no supported channel credentials found");
```

### CommentedOutCode
Commented out code (6 lines)
in `dubbo-demo/dubbo-demo-xml/dubbo-demo-jaxrs-rest-provider/src/main/java/org/apache/dubbo/demo/rest/api/RestProvider.java`
#### Snippet
```java
        context.refresh();

//        SpringControllerService springControllerService = context.getBean(SpringControllerService.class);
//        ServiceConfig<SpringControllerService> serviceConfig = new ServiceConfig<>();
//        serviceConfig.setInterface(SpringControllerService.class);
```

### CommentedOutCode
Commented out code (2 lines)
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/rest/RequestMetadata.java`
#### Snippet
```java
            addAll(httpHeaders, headers);
            // Handles "Content-Type" and "Accept" headers if present
//            mediaTypes(httpHeaders, HttpHeaders.CONTENT_TYPE, this.consumes);
//            mediaTypes(httpHeaders, HttpHeaders.ACCEPT, this.produces);
            this.headers.putAll(httpHeaders);
```

### CommentedOutCode
Commented out code (7 lines)
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/rest/RequestMetadata.java`
#### Snippet
```java
    }

//    public List<MediaType> getConsumeMediaTypes() {
//        return toMediaTypes(consumes);
//    }
```

### CommentedOutCode
Commented out code (2 lines)
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/report/MetadataReportInstance.java`
#### Snippet
```java
        url = url.addParameterIfAbsent(REGISTRY_LOCAL_FILE_CACHE_ENABLED, String.valueOf(applicationModel.getCurrentConfig().getEnableFileCache()));
        String relatedRegistryId = isEmpty(config.getRegistry()) ? (isEmpty(config.getId()) ? DEFAULT_KEY : config.getId()) : config.getRegistry();
//        RegistryConfig registryConfig = applicationModel.getConfigManager().getRegistry(relatedRegistryId)
//                .orElseThrow(() -> new IllegalStateException("Registry id " + relatedRegistryId + " does not exist."));
        MetadataReport metadataReport = metadataReportFactory.getMetadataReport(url);
```

### CommentedOutCode
Commented out code (5 lines)
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/netty/NettyHttpResponse.java`
#### Snippet
```java
    @SuppressWarnings({"rawtypes", "unchecked"})
    public static void transformHeaders(NettyHttpResponse nettyResponse, io.netty.handler.codec.http.HttpResponse response) {
//        if (nettyResponse.isKeepAlive()) {
//            response.headers().set(HttpHeaders.Names.CONNECTION, HttpHeaders.Values.KEEP_ALIVE);
//        } else {
```

### CommentedOutCode
Commented out code (2 lines)
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/annotation/consumer/inercept/RequestHeaderIntercept.java`
#### Snippet
```java
        }

//        URL url = connectionCreateContext.getUrl();


```

### CommentedOutCode
Commented out code (5 lines)
in `dubbo-test/dubbo-test-common/src/main/java/org/apache/dubbo/test/common/impl/DemoServiceImpl.java`
#### Snippet
```java
    public CompletableFuture<String> sayHelloAsync(String name) {
        CompletableFuture<String> cf = CompletableFuture.supplyAsync(() -> {
//            try {
//                Thread.sleep(1000);
//            } catch (InterruptedException e) {
```

### CommentedOutCode
Commented out code (3 lines)
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ConsumerMethodModel.java`
#### Snippet
```java
    }

//    public ConcurrentMap<String, Object> getAttributeMap() {
//        return attributeMap;
//    }
```

### CommentedOutCode
Commented out code (2 lines)
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ConsumerMethodModel.java`
#### Snippet
```java
public class ConsumerMethodModel {
    private final Method method;
    //    private final boolean isCallBack;
//    private final boolean isFuture;
    private final String[] parameterTypes;
```

### CommentedOutCode
Commented out code (8 lines)
in `dubbo-common/src/main/java/org/apache/dubbo/common/url/component/PathURLAddress.java`
#### Snippet
```java
            buf.append("://");
        }
//
//        if (StringUtils.isNotEmpty(username)) {
//            buf.append(username);
```

### CommentedOutCode
Commented out code (7 lines)
in `dubbo-common/src/main/java/org/apache/dubbo/common/url/component/URLParam.java`
#### Snippet
```java

            return value;
//            if (StringUtils.isEmpty(value)) {
//                // Forward compatible, make sure key dynamic increment can work.
//                // In that case, some values which are proceed before increment will set in EXTRA_PARAMS.
```

### CommentedOutCode
Commented out code (2 lines)
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/UrlUtils.java`
#### Snippet
```java
                    String group = params.get(GROUP_KEY);
                    String version = params.get(VERSION_KEY);
                    //params.remove("group");
                    //params.remove("version");
                    String name = serviceName;
```

### CommentedOutCode
Commented out code (2 lines)
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/UrlUtils.java`
#### Snippet
```java
                            String group = params.get(GROUP_KEY);
                            String version = params.get(VERSION_KEY);
                            // params.remove("group");
                            // params.remove("version");
                            String name = serviceName;
```

### CommentedOutCode
Commented out code (2 lines)
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/UrlUtils.java`
#### Snippet
```java
                String group = params.get(GROUP_KEY);
                String version = params.get(VERSION_KEY);
                //params.remove("group");
                //params.remove("version");
                String name = serviceName;
```

### CommentedOutCode
Commented out code (4 lines)
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/UrlUtils.java`
#### Snippet
```java
            password = defaultPassword;
        }
        /*if (u.isAnyHost() || u.isLocalHost()) {
            changed = true;
            host = NetUtils.getLocalHost();
```

### CommentedOutCode
Commented out code (3 lines)
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/MethodUtils.java`
#### Snippet
```java
    }

//    static List<Method> getOverriderMethods(Class<?> implementationClass, Class<?>... superTypes) {

//
```

### CommentedOutCode
Commented out code (3 lines)
in `dubbo-common/src/main/java/org/apache/dubbo/config/RegistryConfig.java`
#### Snippet
```java

    public void setTransporter(String transporter) {
        /*if(transporter != null && transporter.length() > 0 && ! this.getExtensionLoader(Transporter.class).hasExtension(transporter)){
            throw new IllegalStateException("No such transporter type : " + transporter);
        }*/
```

### CommentedOutCode
Commented out code (3 lines)
in `dubbo-common/src/main/java/org/apache/dubbo/config/RegistryConfig.java`
#### Snippet
```java

    public void setServer(String server) {
        /*if(server != null && server.length() > 0 && ! this.getExtensionLoader(Transporter.class).hasExtension(server)){
            throw new IllegalStateException("No such server type : " + server);
        }*/
```

### CommentedOutCode
Commented out code (3 lines)
in `dubbo-common/src/main/java/org/apache/dubbo/config/RegistryConfig.java`
#### Snippet
```java

    public void setClient(String client) {
        /*if(client != null && client.length() > 0 && ! this.getExtensionLoader(Transporter.class).hasExtension(client)){
            throw new IllegalStateException("No such client type : " + client);
        }*/
```

### CommentedOutCode
Commented out code (3 lines)
in `dubbo-common/src/main/java/org/apache/dubbo/config/AbstractConfig.java`
#### Snippet
```java
    protected void postProcessAfterScopeModelChanged(ScopeModel oldScopeModel, ScopeModel newScopeModel) {
        // remove this config from old ConfigManager
//        if (oldScopeModel != null && oldScopeModel instanceof ApplicationModel) {
//           ((ApplicationModel)oldScopeModel).getApplicationConfigManager().removeConfig(this);
//        }
```

### CommentedOutCode
Commented out code (3 lines)
in `dubbo-common/src/main/java/org/apache/dubbo/config/MethodConfig.java`
#### Snippet
```java
        this.name = name;
        // FIXME, add id strategy in ConfigManager
//        if (StringUtils.isEmpty(id)) {
//            id = name;
//        }
```

### CommentedOutCode
Commented out code (198 lines)
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/FileSystemServiceDiscovery.java`
#### Snippet
```java
 * limitations under the License.
 */
//package org.apache.dubbo.registry.client;
//
//import com.alibaba.fastjson.JSON;
```

### CommentedOutCode
Commented out code (3 lines)
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/ServiceDiscoveryRegistry.java`
#### Snippet
```java
                if (CollectionUtils.isEmpty(mappingByUrl)) {
                    logger.info("No interface-apps mapping found in local cache, stop subscribing, will automatically wait for mapping listener callback: " + url);
//                if (check) {
//                    throw new IllegalStateException("Should has at least one way to know which services this interface belongs to, subscription url: " + url);
//                }
```

### CommentedOutCode
Commented out code (2 lines)
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/schema/DubboBeanDefinitionParser.java`
#### Snippet
```java
        //https://github.com/apache/dubbo/pull/6079
        //https://github.com/apache/dubbo/issues/6035
//        Environment environment = parserContext.getReaderContext().getEnvironment();
//        return environment.resolvePlaceholders(attributeValue);
        return attributeValue;
```

### CommentedOutCode
Commented out code (10 lines)
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/schema/DubboBeanDefinitionParser.java`
#### Snippet
```java

        if (ProtocolConfig.class.equals(beanClass)) {
//            for (String name : parserContext.getRegistry().getBeanDefinitionNames()) {
//                BeanDefinition definition = parserContext.getRegistry().getBeanDefinition(name);
//                PropertyValue property = definition.getPropertyValues().getPropertyValue("protocol");
```

### CommentedOutCode
Commented out code (4 lines)
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/reference/ReferenceBeanBuilder.java`
#### Snippet
```java
    }

//    public ReferenceBeanBuilder setRouter(String router) {
//        attributes.put(ReferenceAttributes.ROUTER, router);
//        return this;
```

### CommentedOutCode
Commented out code (10 lines)
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/reference/ReferenceBeanBuilder.java`
#### Snippet
```java
    }

//    @Deprecated
//    public ReferenceBeanBuilder setApplication(ApplicationConfig application) {
//        attributes.put(ReferenceAttributes.APPLICATION, application);
```

### CommentedOutCode
Commented out code (27 lines)
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/reference/ReferenceBeanBuilder.java`
#### Snippet
```java
    }

//    public ReferenceBeanBuilder setAuth(Boolean auth) {
//        attributes.put(ReferenceAttributes.AUTH, auth);
//        return this;
```

### CommentedOutCode
Commented out code (2 lines)
in `dubbo-plugin/dubbo-qos/src/main/java/org/apache/dubbo/qos/command/impl/ShutdownTelnet.java`
#### Snippet
```java
        }
        // TODO change to ApplicationDeployer.destroy() or ApplicationModel.destroy()
//        DubboShutdownHook.getDubboShutdownHook().unregister();
//        DubboShutdownHook.getDubboShutdownHook().doDestroy();
        long end = System.currentTimeMillis();
```

### CommentedOutCode
Commented out code (4 lines)
in `dubbo-remoting/dubbo-remoting-netty/src/main/java/org/apache/dubbo/remoting/transport/netty/NettyServer.java`
#### Snippet
```java
                NettyCodecAdapter adapter = new NettyCodecAdapter(getCodec(), getUrl(), NettyServer.this);
                ChannelPipeline pipeline = Channels.pipeline();
                /*int idleTimeout = getIdleTimeout();
                if (idleTimeout > 10000) {
                    pipeline.addLast("timer", new IdleStateHandler(timer, idleTimeout / 1000, 0, 0));
```

### CommentedOutCode
Commented out code (5 lines)
in `dubbo-remoting/dubbo-remoting-netty/src/main/java/org/apache/dubbo/remoting/transport/netty/NettyClient.java`
#### Snippet
```java
    @Override
    protected void doClose() throws Throwable {
        /*try {
            bootstrap.releaseExternalResources();
        } catch (Throwable t) {
```

### CommentedOutCode
Commented out code (4 lines)
in `dubbo-remoting/dubbo-remoting-netty/src/main/java/org/apache/dubbo/remoting/transport/netty/NettyPortUnificationServer.java`
#### Snippet
```java
                NettyCodecAdapter adapter = new NettyCodecAdapter(getCodec(), getUrl(), NettyPortUnificationServer.this);
                ChannelPipeline pipeline = Channels.pipeline();
                /*int idleTimeout = getIdleTimeout();
                if (idleTimeout > 10000) {
                    pipeline.addLast("timer", new IdleStateHandler(timer, idleTimeout / 1000, 0, 0));
```

### CommentedOutCode
Commented out code (4 lines)
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/FutureAdapter.java`
#### Snippet
```java
    @Override
    public boolean cancel(boolean mayInterruptIfRunning) {
//        Invocation invocation = invocationSoftReference.get();
//        if (invocation != null) {
//            invocation.getInvoker().invoke(cancel);
```

### CommentedOutCode
Commented out code (4 lines)
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/common/URL.java`
#### Snippet
```java
    }

//    public List<URL> getBackupUrls() {
//        List<org.apache.dubbo.common.URL> res = super.getBackupUrls();
//        return res.stream().map(url -> new URL(url)).collect(Collectors.toList());
```

### CommentedOutCode
Commented out code (6 lines)
in `dubbo-rpc/dubbo-rpc-dubbo/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/ChannelWrappedInvoker.java`
#### Snippet
```java
    @Override
    public void destroy() {
//        super.destroy();
//        try {
//            channel.close();
```

### CommentedOutCode
Commented out code (4 lines)
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/support/AbstractClusterInvoker.java`
#### Snippet
```java

        // binding attachments into invocation.
//        Map<String, Object> contextAttachments = RpcContext.getClientAttachment().getObjectAttachments();
//        if (contextAttachments != null && contextAttachments.size() != 0) {
//            ((RpcInvocation) invocation).addObjectAttachmentsIfAbsent(contextAttachments);
```

### CommentedOutCode
Commented out code (3 lines)
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/telnet/support/TelnetUtils.java`
#### Snippet
```java
                        buf.append(rowbuf.toString());
                        rowbuf = new StringBuilder();
//                        for(int m = 0;m < maxcountbefore && maxcountbefore < totalWidth ; m++){
//                            rowbuf.append(" ");
//                        }
```

## RuleId[id=RegExpRedundantEscape]
### RegExpRedundantEscape
Redundant character escape `\\,` in RegExp
in `dubbo-common/src/main/java/org/apache/dubbo/common/ProtocolServiceKey.java`
#### Snippet
```java
                    // 4.3. if rule protocol contains ',', split and match each
                    if (rule.getProtocol().contains(CommonConstants.COMMA_SEPARATOR)) {
                        String[] protocols = rule.getProtocol().split("\\" +CommonConstants.COMMA_SEPARATOR, -1);
                        boolean match = false;
                        for (String protocol : protocols) {
```

### RegExpRedundantEscape
Redundant character escape `\\,` in RegExp
in `dubbo-common/src/main/java/org/apache/dubbo/common/ServiceKey.java`
#### Snippet
```java
                    // 2.3. if rule version contains ',', split and match each
                    if (rule.getVersion().contains(CommonConstants.COMMA_SEPARATOR)) {
                        String[] versions = rule.getVersion().split("\\" +CommonConstants.COMMA_SEPARATOR, -1);
                        boolean match = false;
                        for (String version : versions) {
```

### RegExpRedundantEscape
Redundant character escape `\\,` in RegExp
in `dubbo-common/src/main/java/org/apache/dubbo/common/ServiceKey.java`
#### Snippet
```java
                    // 3.3. if rule group contains ',', split and match each
                    if (rule.getGroup().contains(CommonConstants.COMMA_SEPARATOR)) {
                        String[] groups = rule.getGroup().split("\\" +CommonConstants.COMMA_SEPARATOR, -1);
                        boolean match = false;
                        for (String group : groups) {
```

### RegExpRedundantEscape
Redundant character escape `\\.` in RegExp
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ConfigUtils.java`
#### Snippet
```java
    private static final ErrorTypeAwareLogger logger = LoggerFactory.getErrorTypeAwareLogger(ConfigUtils.class);
    private static final Pattern VARIABLE_PATTERN = Pattern.compile(
        "\\$\\s*\\{?\\s*([\\._0-9a-zA-Z]+)\\s*\\}?");
    private static int PID = -1;

```

### RegExpRedundantEscape
Redundant character escape `\\}` in RegExp
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ConfigUtils.java`
#### Snippet
```java
    private static final ErrorTypeAwareLogger logger = LoggerFactory.getErrorTypeAwareLogger(ConfigUtils.class);
    private static final Pattern VARIABLE_PATTERN = Pattern.compile(
        "\\$\\s*\\{?\\s*([\\._0-9a-zA-Z]+)\\s*\\}?");
    private static int PID = -1;

```

### RegExpRedundantEscape
Redundant character escape `\\.` in RegExp
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/StringUtils.java`
#### Snippet
```java
    private static final Pattern KVP_PATTERN = Pattern.compile("([_.a-zA-Z0-9][-_.a-zA-Z0-9]*)[=](.*)"); //key value pair pattern.
    private static final Pattern NUM_PATTERN = Pattern.compile("^\\d+$");
    private static final Pattern PARAMETERS_PATTERN = Pattern.compile("^\\[((\\s*\\{\\s*[\\w_\\-\\.]+\\s*:\\s*.+?\\s*\\}\\s*,?\\s*)+)\\s*\\]$");
    private static final Pattern PAIR_PARAMETERS_PATTERN = Pattern.compile("^\\{\\s*([\\w-_\\.]+)\\s*:\\s*(.+)\\s*\\}$");
    private static final int PAD_LIMIT = 8192;
```

### RegExpRedundantEscape
Redundant character escape `\\}` in RegExp
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/StringUtils.java`
#### Snippet
```java
    private static final Pattern KVP_PATTERN = Pattern.compile("([_.a-zA-Z0-9][-_.a-zA-Z0-9]*)[=](.*)"); //key value pair pattern.
    private static final Pattern NUM_PATTERN = Pattern.compile("^\\d+$");
    private static final Pattern PARAMETERS_PATTERN = Pattern.compile("^\\[((\\s*\\{\\s*[\\w_\\-\\.]+\\s*:\\s*.+?\\s*\\}\\s*,?\\s*)+)\\s*\\]$");
    private static final Pattern PAIR_PARAMETERS_PATTERN = Pattern.compile("^\\{\\s*([\\w-_\\.]+)\\s*:\\s*(.+)\\s*\\}$");
    private static final int PAD_LIMIT = 8192;
```

### RegExpRedundantEscape
Redundant character escape `\\]` in RegExp
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/StringUtils.java`
#### Snippet
```java
    private static final Pattern KVP_PATTERN = Pattern.compile("([_.a-zA-Z0-9][-_.a-zA-Z0-9]*)[=](.*)"); //key value pair pattern.
    private static final Pattern NUM_PATTERN = Pattern.compile("^\\d+$");
    private static final Pattern PARAMETERS_PATTERN = Pattern.compile("^\\[((\\s*\\{\\s*[\\w_\\-\\.]+\\s*:\\s*.+?\\s*\\}\\s*,?\\s*)+)\\s*\\]$");
    private static final Pattern PAIR_PARAMETERS_PATTERN = Pattern.compile("^\\{\\s*([\\w-_\\.]+)\\s*:\\s*(.+)\\s*\\}$");
    private static final int PAD_LIMIT = 8192;
```

### RegExpRedundantEscape
Redundant character escape `\\.` in RegExp
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/StringUtils.java`
#### Snippet
```java
    private static final Pattern NUM_PATTERN = Pattern.compile("^\\d+$");
    private static final Pattern PARAMETERS_PATTERN = Pattern.compile("^\\[((\\s*\\{\\s*[\\w_\\-\\.]+\\s*:\\s*.+?\\s*\\}\\s*,?\\s*)+)\\s*\\]$");
    private static final Pattern PAIR_PARAMETERS_PATTERN = Pattern.compile("^\\{\\s*([\\w-_\\.]+)\\s*:\\s*(.+)\\s*\\}$");
    private static final int PAD_LIMIT = 8192;
    private static final byte[] HEX2B;
```

### RegExpRedundantEscape
Redundant character escape `\\}` in RegExp
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/StringUtils.java`
#### Snippet
```java
    private static final Pattern NUM_PATTERN = Pattern.compile("^\\d+$");
    private static final Pattern PARAMETERS_PATTERN = Pattern.compile("^\\[((\\s*\\{\\s*[\\w_\\-\\.]+\\s*:\\s*.+?\\s*\\}\\s*,?\\s*)+)\\s*\\]$");
    private static final Pattern PAIR_PARAMETERS_PATTERN = Pattern.compile("^\\{\\s*([\\w-_\\.]+)\\s*:\\s*(.+)\\s*\\}$");
    private static final int PAD_LIMIT = 8192;
    private static final byte[] HEX2B;
```

### RegExpRedundantEscape
Redundant character escape `\\:` in RegExp
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/NetUtils.java`
#### Snippet
```java
    private static final int MAX_PORT = 65535;

    private static final Pattern ADDRESS_PATTERN = Pattern.compile("^\\d{1,3}(\\.\\d{1,3}){3}\\:\\d{1,5}$");
    private static final Pattern LOCAL_IP_PATTERN = Pattern.compile("127(\\.\\d{1,3}){3}$");
    private static final Pattern IP_PATTERN = Pattern.compile("\\d{1,3}(\\.\\d{1,3}){3,5}$");
```

### RegExpRedundantEscape
Redundant character escape `\/` in RegExp
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static final String ARRAY_DESC = "(?:\\[+(?:(?:[VZBCDFIJS])|" + CLASS_DESC + "))";

    public static final String DESC_REGEX = "(?:(?:[VZBCDFIJS])|" + CLASS_DESC + "|" + ARRAY_DESC + ")";

    public static final Pattern DESC_PATTERN = Pattern.compile(DESC_REGEX);
```

### RegExpRedundantEscape
Redundant character escape `\/` in RegExp
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static final String ARRAY_DESC = "(?:\\[+(?:(?:[VZBCDFIJS])|" + CLASS_DESC + "))";

    public static final String DESC_REGEX = "(?:(?:[VZBCDFIJS])|" + CLASS_DESC + "|" + ARRAY_DESC + ")";

    public static final Pattern DESC_PATTERN = Pattern.compile(DESC_REGEX);
```

### RegExpRedundantEscape
Redundant character escape `\/` in RegExp
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static final String CLASS_DESC = "(?:L" + JAVA_IDENT_REGEX + "(?:\\/" + JAVA_IDENT_REGEX + ")*;)";

    public static final String ARRAY_DESC = "(?:\\[+(?:(?:[VZBCDFIJS])|" + CLASS_DESC + "))";

    public static final String DESC_REGEX = "(?:(?:[VZBCDFIJS])|" + CLASS_DESC + "|" + ARRAY_DESC + ")";
```

### RegExpRedundantEscape
Redundant character escape `\/` in RegExp
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static final Pattern METHOD_DESC_PATTERN = Pattern.compile(METHOD_DESC_REGEX);

    public static final Pattern GETTER_METHOD_DESC_PATTERN = Pattern.compile("get([A-Z][_a-zA-Z0-9]*)\\(\\)(" + DESC_REGEX + ")");

    public static final Pattern SETTER_METHOD_DESC_PATTERN = Pattern.compile("set([A-Z][_a-zA-Z0-9]*)\\((" + DESC_REGEX + ")\\)V");
```

### RegExpRedundantEscape
Redundant character escape `\/` in RegExp
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static final Pattern GETTER_METHOD_DESC_PATTERN = Pattern.compile("get([A-Z][_a-zA-Z0-9]*)\\(\\)(" + DESC_REGEX + ")");

    public static final Pattern SETTER_METHOD_DESC_PATTERN = Pattern.compile("set([A-Z][_a-zA-Z0-9]*)\\((" + DESC_REGEX + ")\\)V");

    public static final Pattern IS_HAS_CAN_METHOD_DESC_PATTERN = Pattern.compile("(?:is|has|can)([A-Z][_a-zA-Z0-9]*)\\(\\)Z");
```

### RegExpRedundantEscape
Redundant character escape `\\/` in RegExp
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static final String JAVA_NAME_REGEX = "(?:" + JAVA_IDENT_REGEX + "(?:\\." + JAVA_IDENT_REGEX + ")*)";

    public static final String CLASS_DESC = "(?:L" + JAVA_IDENT_REGEX + "(?:\\/" + JAVA_IDENT_REGEX + ")*;)";

    public static final String ARRAY_DESC = "(?:\\[+(?:(?:[VZBCDFIJS])|" + CLASS_DESC + "))";
```

### RegExpRedundantEscape
Redundant character escape `\/` in RegExp
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static final Pattern DESC_PATTERN = Pattern.compile(DESC_REGEX);

    public static final String METHOD_DESC_REGEX = "(?:(" + JAVA_IDENT_REGEX + ")?\\((" + DESC_REGEX + "*)\\)(" + DESC_REGEX + ")?)";

    public static final Pattern METHOD_DESC_PATTERN = Pattern.compile(METHOD_DESC_REGEX);
```

### RegExpRedundantEscape
Redundant character escape `\/` in RegExp
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static final Pattern DESC_PATTERN = Pattern.compile(DESC_REGEX);

    public static final String METHOD_DESC_REGEX = "(?:(" + JAVA_IDENT_REGEX + ")?\\((" + DESC_REGEX + "*)\\)(" + DESC_REGEX + ")?)";

    public static final Pattern METHOD_DESC_PATTERN = Pattern.compile(METHOD_DESC_REGEX);
```

### RegExpRedundantEscape
Redundant character escape `\\.` in RegExp
in `dubbo-common/src/main/java/org/apache/dubbo/common/compiler/support/AbstractCompiler.java`
#### Snippet
```java
public abstract class AbstractCompiler implements Compiler {

    private static final Pattern PACKAGE_PATTERN = Pattern.compile("package\\s+([$_a-zA-Z][$_a-zA-Z0-9\\.]*);");

    private static final Pattern CLASS_PATTERN = Pattern.compile("class\\s+([$_a-zA-Z][$_a-zA-Z0-9]*)\\s+");
```

### RegExpRedundantEscape
Redundant character escape `\\.` in RegExp
in `dubbo-common/src/main/java/org/apache/dubbo/common/compiler/support/JavassistCompiler.java`
#### Snippet
```java
    private static final Pattern IMPORT_PATTERN = Pattern.compile("import\\s+([\\w\\.\\*]+);\n");

    private static final Pattern EXTENDS_PATTERN = Pattern.compile("\\s+extends\\s+([\\w\\.]+)[^\\{]*\\{\n");

    private static final Pattern IMPLEMENTS_PATTERN = Pattern.compile("\\s+implements\\s+([\\w\\.]+)\\s*\\{\n");
```

### RegExpRedundantEscape
Redundant character escape `\\{` in RegExp
in `dubbo-common/src/main/java/org/apache/dubbo/common/compiler/support/JavassistCompiler.java`
#### Snippet
```java
    private static final Pattern IMPORT_PATTERN = Pattern.compile("import\\s+([\\w\\.\\*]+);\n");

    private static final Pattern EXTENDS_PATTERN = Pattern.compile("\\s+extends\\s+([\\w\\.]+)[^\\{]*\\{\n");

    private static final Pattern IMPLEMENTS_PATTERN = Pattern.compile("\\s+implements\\s+([\\w\\.]+)\\s*\\{\n");
```

### RegExpRedundantEscape
Redundant character escape `\\.` in RegExp
in `dubbo-common/src/main/java/org/apache/dubbo/common/compiler/support/JavassistCompiler.java`
#### Snippet
```java
    private static final Pattern EXTENDS_PATTERN = Pattern.compile("\\s+extends\\s+([\\w\\.]+)[^\\{]*\\{\n");

    private static final Pattern IMPLEMENTS_PATTERN = Pattern.compile("\\s+implements\\s+([\\w\\.]+)\\s*\\{\n");

    private static final Pattern METHODS_PATTERN = Pattern.compile("\n(private|public|protected)\\s+");
```

### RegExpRedundantEscape
Redundant character escape `\\,` in RegExp
in `dubbo-common/src/main/java/org/apache/dubbo/common/compiler/support/JavassistCompiler.java`
#### Snippet
```java
        matcher = IMPLEMENTS_PATTERN.matcher(source);
        if (matcher.find()) {
            String[] ifaces = matcher.group(1).trim().split("\\,");
            Arrays.stream(ifaces).forEach(i -> builder.addInterface(i.trim()));
        }
```

### RegExpRedundantEscape
Redundant character escape `\\.` in RegExp
in `dubbo-common/src/main/java/org/apache/dubbo/common/compiler/support/JavassistCompiler.java`
#### Snippet
```java
public class JavassistCompiler extends AbstractCompiler {

    private static final Pattern IMPORT_PATTERN = Pattern.compile("import\\s+([\\w\\.\\*]+);\n");

    private static final Pattern EXTENDS_PATTERN = Pattern.compile("\\s+extends\\s+([\\w\\.]+)[^\\{]*\\{\n");
```

### RegExpRedundantEscape
Redundant character escape `\\*` in RegExp
in `dubbo-common/src/main/java/org/apache/dubbo/common/compiler/support/JavassistCompiler.java`
#### Snippet
```java
public class JavassistCompiler extends AbstractCompiler {

    private static final Pattern IMPORT_PATTERN = Pattern.compile("import\\s+([\\w\\.\\*]+);\n");

    private static final Pattern EXTENDS_PATTERN = Pattern.compile("\\s+extends\\s+([\\w\\.]+)[^\\{]*\\{\n");
```

### RegExpRedundantEscape
Redundant character escape `\\:` in RegExp
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/schema/DubboBeanDefinitionParser.java`
#### Snippet
```java

    private static final Logger logger = LoggerFactory.getLogger(DubboBeanDefinitionParser.class);
    private static final Pattern GROUP_AND_VERSION = Pattern.compile("^[\\-.0-9_a-zA-Z]+(\\:[\\-.0-9_a-zA-Z]+)?$");
    private static final String ONRETURN = "onreturn";
    private static final String ONTHROW = "onthrow";
```

### RegExpRedundantEscape
Redundant character escape `\\]` in RegExp
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/condition/matcher/argument/ArgumentConditionMatcher.java`
#### Snippet
```java
public class ArgumentConditionMatcher extends AbstractConditionMatcher {
    private static final ErrorTypeAwareLogger logger = LoggerFactory.getErrorTypeAwareLogger(ArgumentConditionMatcher.class);
    private static final Pattern ARGUMENTS_PATTERN = Pattern.compile("arguments\\[([0-9]+)\\]");

    public ArgumentConditionMatcher(String key, ModuleModel model) {
```

### RegExpRedundantEscape
Redundant character escape `\\]` in RegExp
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/condition/matcher/attachment/AttachmentConditionMatcher.java`
#### Snippet
```java
public class AttachmentConditionMatcher extends AbstractConditionMatcher {
    private static final ErrorTypeAwareLogger logger = LoggerFactory.getErrorTypeAwareLogger(AttachmentConditionMatcher.class);
    private static final Pattern ATTACHMENTS_PATTERN = Pattern.compile("attachments\\[(.+)\\]");

    public AttachmentConditionMatcher(String key, ModuleModel model) {
```

## RuleId[id=SpringBootApplicationYaml]
### SpringBootApplicationYaml
Cannot resolve configuration property 'dubbo.application.name'
in `dubbo-demo/dubbo-demo-spring-boot/dubbo-demo-spring-boot-consumer/src/main/resources/application.yml`
#### Snippet
```java
dubbo:
  application:
    name: ${spring.application.name}
  protocol:
    name: dubbo
```

### SpringBootApplicationYaml
Cannot resolve configuration property 'dubbo.protocol.name'
in `dubbo-demo/dubbo-demo-spring-boot/dubbo-demo-spring-boot-consumer/src/main/resources/application.yml`
#### Snippet
```java
    name: ${spring.application.name}
  protocol:
    name: dubbo
    port: -1
  registry:
```

### SpringBootApplicationYaml
Cannot resolve configuration property 'dubbo.protocol.port'
in `dubbo-demo/dubbo-demo-spring-boot/dubbo-demo-spring-boot-consumer/src/main/resources/application.yml`
#### Snippet
```java
  protocol:
    name: dubbo
    port: -1
  registry:
    id: zk-registry
```

### SpringBootApplicationYaml
Cannot resolve configuration property 'dubbo.registry.id'
in `dubbo-demo/dubbo-demo-spring-boot/dubbo-demo-spring-boot-consumer/src/main/resources/application.yml`
#### Snippet
```java
    port: -1
  registry:
    id: zk-registry
    address: zookeeper://127.0.0.1:2181
  config-center:
```

### SpringBootApplicationYaml
Cannot resolve configuration property 'dubbo.registry.address'
in `dubbo-demo/dubbo-demo-spring-boot/dubbo-demo-spring-boot-consumer/src/main/resources/application.yml`
#### Snippet
```java
  registry:
    id: zk-registry
    address: zookeeper://127.0.0.1:2181
  config-center:
    address: zookeeper://127.0.0.1:2181
```

### SpringBootApplicationYaml
Cannot resolve configuration property 'dubbo.config-center.address'
in `dubbo-demo/dubbo-demo-spring-boot/dubbo-demo-spring-boot-consumer/src/main/resources/application.yml`
#### Snippet
```java
    address: zookeeper://127.0.0.1:2181
  config-center:
    address: zookeeper://127.0.0.1:2181
  metadata-report:
    address: zookeeper://127.0.0.1:2181
```

### SpringBootApplicationYaml
Cannot resolve configuration property 'dubbo.metadata-report.address'
in `dubbo-demo/dubbo-demo-spring-boot/dubbo-demo-spring-boot-consumer/src/main/resources/application.yml`
#### Snippet
```java
    address: zookeeper://127.0.0.1:2181
  metadata-report:
    address: zookeeper://127.0.0.1:2181

logging:
```

### SpringBootApplicationYaml
Cannot resolve configuration property 'logging.pattern.level'
in `dubbo-demo/dubbo-demo-spring-boot/dubbo-demo-spring-boot-consumer/src/main/resources/application.yml`
#### Snippet
```java
logging:
  pattern:
    level: '%5p [${spring.application.name:},%X{traceId:-},%X{spanId:-}]'


```

### SpringBootApplicationYaml
Cannot resolve configuration property 'dubbo.application.name'
in `dubbo-demo/dubbo-demo-spring-boot/dubbo-demo-spring-boot-provider/src/main/resources/application.yml`
#### Snippet
```java
dubbo:
  application:
    name: ${spring.application.name}
  protocol:
    name: dubbo
```

### SpringBootApplicationYaml
Cannot resolve configuration property 'dubbo.protocol.name'
in `dubbo-demo/dubbo-demo-spring-boot/dubbo-demo-spring-boot-provider/src/main/resources/application.yml`
#### Snippet
```java
    name: ${spring.application.name}
  protocol:
    name: dubbo
    port: -1
  registry:
```

### SpringBootApplicationYaml
Cannot resolve configuration property 'dubbo.protocol.port'
in `dubbo-demo/dubbo-demo-spring-boot/dubbo-demo-spring-boot-provider/src/main/resources/application.yml`
#### Snippet
```java
  protocol:
    name: dubbo
    port: -1
  registry:
    id: zk-registry
```

### SpringBootApplicationYaml
Cannot resolve configuration property 'dubbo.registry.id'
in `dubbo-demo/dubbo-demo-spring-boot/dubbo-demo-spring-boot-provider/src/main/resources/application.yml`
#### Snippet
```java
    port: -1
  registry:
    id: zk-registry
    address: zookeeper://127.0.0.1:2181
  config-center:
```

### SpringBootApplicationYaml
Cannot resolve configuration property 'dubbo.registry.address'
in `dubbo-demo/dubbo-demo-spring-boot/dubbo-demo-spring-boot-provider/src/main/resources/application.yml`
#### Snippet
```java
  registry:
    id: zk-registry
    address: zookeeper://127.0.0.1:2181
  config-center:
    address: zookeeper://127.0.0.1:2181
```

### SpringBootApplicationYaml
Cannot resolve configuration property 'dubbo.config-center.address'
in `dubbo-demo/dubbo-demo-spring-boot/dubbo-demo-spring-boot-provider/src/main/resources/application.yml`
#### Snippet
```java
    address: zookeeper://127.0.0.1:2181
  config-center:
    address: zookeeper://127.0.0.1:2181
  metadata-report:
    address: zookeeper://127.0.0.1:2181
```

### SpringBootApplicationYaml
Cannot resolve configuration property 'dubbo.metadata-report.address'
in `dubbo-demo/dubbo-demo-spring-boot/dubbo-demo-spring-boot-provider/src/main/resources/application.yml`
#### Snippet
```java
    address: zookeeper://127.0.0.1:2181
  metadata-report:
    address: zookeeper://127.0.0.1:2181
  metrics:
    protocol: prometheus
```

### SpringBootApplicationYaml
Cannot resolve configuration property 'dubbo.metrics.protocol'
in `dubbo-demo/dubbo-demo-spring-boot/dubbo-demo-spring-boot-provider/src/main/resources/application.yml`
#### Snippet
```java
    address: zookeeper://127.0.0.1:2181
  metrics:
    protocol: prometheus
    enable-jvm: true
    enable-registry: true
```

### SpringBootApplicationYaml
Cannot resolve configuration property 'dubbo.metrics.enable-jvm'
in `dubbo-demo/dubbo-demo-spring-boot/dubbo-demo-spring-boot-provider/src/main/resources/application.yml`
#### Snippet
```java
  metrics:
    protocol: prometheus
    enable-jvm: true
    enable-registry: true
    aggregation:
```

### SpringBootApplicationYaml
Cannot resolve configuration property 'dubbo.metrics.enable-registry'
in `dubbo-demo/dubbo-demo-spring-boot/dubbo-demo-spring-boot-provider/src/main/resources/application.yml`
#### Snippet
```java
    protocol: prometheus
    enable-jvm: true
    enable-registry: true
    aggregation:
      enabled: true
```

### SpringBootApplicationYaml
Cannot resolve configuration property 'dubbo.metrics.aggregation.enabled'
in `dubbo-demo/dubbo-demo-spring-boot/dubbo-demo-spring-boot-provider/src/main/resources/application.yml`
#### Snippet
```java
    enable-registry: true
    aggregation:
      enabled: true
    prometheus:
      exporter:
```

### SpringBootApplicationYaml
Cannot resolve configuration property 'dubbo.metrics.prometheus.exporter.enabled'
in `dubbo-demo/dubbo-demo-spring-boot/dubbo-demo-spring-boot-provider/src/main/resources/application.yml`
#### Snippet
```java
    prometheus:
      exporter:
        enabled: true
    enable-metadata: true
logging:
```

### SpringBootApplicationYaml
Cannot resolve configuration property 'dubbo.metrics.enable-metadata'
in `dubbo-demo/dubbo-demo-spring-boot/dubbo-demo-spring-boot-provider/src/main/resources/application.yml`
#### Snippet
```java
      exporter:
        enabled: true
    enable-metadata: true
logging:
  pattern:
```

### SpringBootApplicationYaml
Cannot resolve configuration property 'logging.pattern.level'
in `dubbo-demo/dubbo-demo-spring-boot/dubbo-demo-spring-boot-provider/src/main/resources/application.yml`
#### Snippet
```java
logging:
  pattern:
    level: '%5p [${spring.application.name:},%X{traceId:-},%X{spanId:-}]'


```

## RuleId[id=Deprecation]
### Deprecation
'getContext()' is deprecated
in `dubbo-demo/dubbo-demo-spring-boot/dubbo-demo-spring-boot-provider/src/main/java/org/apache/dubbo/springboot/demo/provider/DemoServiceImpl.java`
#### Snippet
```java
    @Override
    public String sayHello(String name) {
        logger.info("Hello " + name + ", request from consumer: " + RpcContext.getContext().getRemoteAddress());
        return "Hello " + name;

```

### Deprecation
'getExtensionLoader(java.lang.Class)' is deprecated
in `dubbo-container/dubbo-container-api/src/main/java/org/apache/dubbo/container/Main.java`
#### Snippet
```java
    private static final ErrorTypeAwareLogger logger = LoggerFactory.getErrorTypeAwareLogger(Main.class);

    private static final ExtensionLoader<Container> LOADER = ExtensionLoader.getExtensionLoader(Container.class);

    private static final ReentrantLock LOCK = new ReentrantLock();
```

### Deprecation
'getExportBackground()' is deprecated
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/deploy/DefaultModuleDeployer.java`
#### Snippet
```java
        return moduleModel.getConfigManager().getProviders()
            .stream()
            .map(ProviderConfig::getExportBackground)
            .anyMatch(k -> k != null && k);
    }
```

### Deprecation
'getReferBackground()' is deprecated
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/deploy/DefaultModuleDeployer.java`
#### Snippet
```java
        return moduleModel.getConfigManager().getConsumers()
            .stream()
            .map(ConsumerConfig::getReferBackground)
            .anyMatch(k -> k != null && k);
    }
```

### Deprecation
'setApplication(org.apache.dubbo.config.ApplicationConfig)' is deprecated
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/bootstrap/builders/InternalServiceConfigBuilder.java`
#### Snippet
```java
        ServiceConfig<T> serviceConfig = new ServiceConfig<>();
        serviceConfig.setScopeModel(applicationModel.getInternalModule());
        serviceConfig.setApplication(applicationConfig);

        RegistryConfig registryConfig = new RegistryConfig("N/A");
```

### Deprecation
'org.apache.dubbo.config.bootstrap.DubboBootstrapStartStopListener' is deprecated
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/bootstrap/DubboBootstrap.java`
#### Snippet
```java

    private void notifyStarted(ApplicationModel applicationModel) {
        ExtensionLoader<DubboBootstrapStartStopListener> exts = applicationModel.getExtensionLoader(DubboBootstrapStartStopListener.class);
        exts.getSupportedExtensionInstances().forEach(ext -> ext.onStart(DubboBootstrap.this));
    }
```

### Deprecation
'org.apache.dubbo.config.bootstrap.DubboBootstrapStartStopListener' is deprecated
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/bootstrap/DubboBootstrap.java`
#### Snippet
```java

    private void notifyStarted(ApplicationModel applicationModel) {
        ExtensionLoader<DubboBootstrapStartStopListener> exts = applicationModel.getExtensionLoader(DubboBootstrapStartStopListener.class);
        exts.getSupportedExtensionInstances().forEach(ext -> ext.onStart(DubboBootstrap.this));
    }
```

### Deprecation
'org.apache.dubbo.config.bootstrap.DubboBootstrapStartStopListener' is deprecated
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/bootstrap/DubboBootstrap.java`
#### Snippet
```java

    private void notifyStopped(ApplicationModel applicationModel) {
        ExtensionLoader<DubboBootstrapStartStopListener> exts = applicationModel.getExtensionLoader(DubboBootstrapStartStopListener.class);
        exts.getSupportedExtensionInstances().forEach(ext -> ext.onStop(DubboBootstrap.this));
        executeMutually(() -> {
```

### Deprecation
'org.apache.dubbo.config.bootstrap.DubboBootstrapStartStopListener' is deprecated
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/bootstrap/DubboBootstrap.java`
#### Snippet
```java

    private void notifyStopped(ApplicationModel applicationModel) {
        ExtensionLoader<DubboBootstrapStartStopListener> exts = applicationModel.getExtensionLoader(DubboBootstrapStartStopListener.class);
        exts.getSupportedExtensionInstances().forEach(ext -> ext.onStop(DubboBootstrap.this));
        executeMutually(() -> {
```

### Deprecation
'setPort(java.lang.Integer)' is deprecated
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/bootstrap/builders/ProviderBuilder.java`
#### Snippet
```java

        provider.setHost(host);
        provider.setPort(port);
        provider.setContextpath(contextpath);
        provider.setThreadpool(threadpool);
```

### Deprecation
'SHUTDOWN_WAIT_SECONDS_KEY' is deprecated
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/utils/ConfigValidationUtils.java`
#### Snippet
```java
            System.setProperty(SHUTDOWN_WAIT_KEY, wait.trim());
        } else {
            wait = configuration.getProperty(SHUTDOWN_WAIT_SECONDS_KEY);
            if (wait != null && wait.trim().length() > 0) {
                System.setProperty(SHUTDOWN_WAIT_SECONDS_KEY, wait.trim());
```

### Deprecation
'SHUTDOWN_WAIT_SECONDS_KEY' is deprecated
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/utils/ConfigValidationUtils.java`
#### Snippet
```java
            wait = configuration.getProperty(SHUTDOWN_WAIT_SECONDS_KEY);
            if (wait != null && wait.trim().length() > 0) {
                System.setProperty(SHUTDOWN_WAIT_SECONDS_KEY, wait.trim());
            }
        }
```

### Deprecation
'SHUTDOWN_WAIT_SECONDS_KEY' is deprecated
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/utils/ConfigValidationUtils.java`
#### Snippet
```java
import static org.apache.dubbo.common.constants.CommonConstants.REMOVE_VALUE_PREFIX;
import static org.apache.dubbo.common.constants.CommonConstants.SHUTDOWN_WAIT_KEY;
import static org.apache.dubbo.common.constants.CommonConstants.SHUTDOWN_WAIT_SECONDS_KEY;
import static org.apache.dubbo.common.constants.CommonConstants.THREADPOOL_KEY;
import static org.apache.dubbo.common.constants.CommonConstants.USERNAME_KEY;
```

### Deprecation
'getDispather()' is deprecated
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/utils/ConfigValidationUtils.java`
#### Snippet
```java
            checkExtension(config.getScopeModel(), Exchanger.class, EXCHANGER_KEY, config.getExchanger());
            checkExtension(config.getScopeModel(), Dispatcher.class, DISPATCHER_KEY, config.getDispatcher());
            checkExtension(config.getScopeModel(), Dispatcher.class, "dispather", config.getDispather());
            checkExtension(config.getScopeModel(), ThreadPool.class, THREADPOOL_KEY, config.getThreadpool());
        }
```

### Deprecation
'getLocal()' is deprecated
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/utils/ConfigValidationUtils.java`
#### Snippet
```java

    public static void validateAbstractInterfaceConfig(AbstractInterfaceConfig config) {
        checkName(LOCAL_KEY, config.getLocal());
        checkName("stub", config.getStub());
        checkMultiName("owner", config.getOwner());
```

### Deprecation
'setHighestPriority(java.lang.Boolean)' is deprecated
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/bootstrap/builders/ConfigCenterBuilder.java`
#### Snippet
```java
        configCenter.setPassword(password);
        configCenter.setTimeout(timeout);
        configCenter.setHighestPriority(highestPriority);
        configCenter.setCheck(check);
        configCenter.setConfigFile(configFile);
```

### Deprecation
'setReliable(java.lang.Boolean)' is deprecated
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/bootstrap/builders/MethodBuilder.java`
#### Snippet
```java
        methodConfig.setServiceId(serviceId);
        methodConfig.setSticky(sticky);
        methodConfig.setReliable(reliable);
        methodConfig.setStat(stat);
        methodConfig.setRetry(retry);
```

### Deprecation
'setStat(java.lang.Integer)' is deprecated
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/bootstrap/builders/MethodBuilder.java`
#### Snippet
```java
        methodConfig.setSticky(sticky);
        methodConfig.setReliable(reliable);
        methodConfig.setStat(stat);
        methodConfig.setRetry(retry);

```

### Deprecation
'setRetry(java.lang.Boolean)' is deprecated
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/bootstrap/builders/MethodBuilder.java`
#### Snippet
```java
        methodConfig.setReliable(reliable);
        methodConfig.setStat(stat);
        methodConfig.setRetry(retry);

        return methodConfig;
```

### Deprecation
'setInjvm(java.lang.Boolean)' is deprecated
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/bootstrap/builders/AbstractReferenceBuilder.java`
#### Snippet
```java
        }
        if (injvm != null) {
            instance.setInjvm(injvm);
        }
        if (lazy != null) {
```

### Deprecation
'org.apache.dubbo.config.annotation.Service' is deprecated
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/ServiceConfig.java`
#### Snippet
```java
    }

    public ServiceConfig(Service service) {
        super(service);
    }
```

### Deprecation
'appendMetricsCompatible(java.util.Map)' is deprecated
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/ServiceConfig.java`
#### Snippet
```java
        AbstractConfig.appendParameters(map, protocolConfig);
        AbstractConfig.appendParameters(map, this);
        appendMetricsCompatible(map);

        // append params with method configs,
```

### Deprecation
'org.apache.dubbo.config.annotation.Service' is deprecated
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/ServiceConfig.java`
#### Snippet
```java
    }

    public ServiceConfig(ModuleModel moduleModel, Service service) {
        super(moduleModel, service);
    }
```

### Deprecation
'setConfig(org.apache.dubbo.config.AbstractInterfaceConfig)' is deprecated
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/ServiceConfig.java`
#### Snippet
```java

        // Compatible with dependencies on ServiceModel#getServiceConfig(), and will be removed in a future version
        providerModel.setConfig(this);

        providerModel.setDestroyRunner(getDestroyRunner());
```

### Deprecation
'setHighestPriority(java.lang.Boolean)' is deprecated
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/deploy/DefaultApplicationDeployer.java`
#### Snippet
```java
            cc.setTimeout(registryConfig.getTimeout().longValue());
        }
        cc.setHighestPriority(false);
        return cc;
    }
```

### Deprecation
'setLocal(java.lang.String)' is deprecated
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/bootstrap/builders/AbstractInterfaceBuilder.java`
#### Snippet
```java

        if (!StringUtils.isEmpty(local)) {
            instance.setLocal(local);
        }
        if (!StringUtils.isEmpty(stub)) {
```

### Deprecation
'setMonitor(org.apache.dubbo.config.MonitorConfig)' is deprecated
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/bootstrap/builders/AbstractInterfaceBuilder.java`
#### Snippet
```java
        }
        if (monitor != null) {
            instance.setMonitor(monitor);
        }
        if (!StringUtils.isEmpty(proxy)) {
```

### Deprecation
'setApplication(org.apache.dubbo.config.ApplicationConfig)' is deprecated
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/bootstrap/builders/AbstractInterfaceBuilder.java`
#### Snippet
```java
        }
        if (application != null) {
            instance.setApplication(application);
        }
        if (module != null) {
```

### Deprecation
'setModule(org.apache.dubbo.config.ModuleConfig)' is deprecated
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/bootstrap/builders/AbstractInterfaceBuilder.java`
#### Snippet
```java
        }
        if (module != null) {
            instance.setModule(module);
        }
        if (registries != null) {
```

### Deprecation
'setMetadataReportConfig(org.apache.dubbo.config.MetadataReportConfig)' is deprecated
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/bootstrap/builders/AbstractInterfaceBuilder.java`
#### Snippet
```java
        }
        if (metadataReportConfig != null) {
            instance.setMetadataReportConfig(metadataReportConfig);
        }
        if (configCenter != null) {
```

### Deprecation
'setConfigCenter(org.apache.dubbo.config.ConfigCenterConfig)' is deprecated
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/bootstrap/builders/AbstractInterfaceBuilder.java`
#### Snippet
```java
        }
        if (configCenter != null) {
            instance.setConfigCenter(configCenter);
        }
        if (callbacks != null) {
```

### Deprecation
'setWait(java.lang.Integer)' is deprecated
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/bootstrap/builders/RegistryBuilder.java`
#### Snippet
```java
        registry.setUsername(username);
        registry.setVersion(version);
        registry.setWait(wait);
        registry.setUseAsConfigCenter(useAsConfigCenter);
        registry.setUseAsMetadataCenter(useAsMetadataCenter);
```

### Deprecation
'org.apache.dubbo.config.annotation.Reference' is deprecated
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/ReferenceConfig.java`
#### Snippet
```java
    }

    public ReferenceConfig(ModuleModel moduleModel, Reference reference) {
        super(moduleModel, reference);
    }
```

### Deprecation
'setConfig(org.apache.dubbo.config.AbstractInterfaceConfig)' is deprecated
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/ReferenceConfig.java`
#### Snippet
```java

            // Compatible with dependencies on ServiceModel#getReferenceConfig() , and will be removed in a future version.
            consumerModel.setConfig(this);

            repository.registerConsumer(consumerModel);
```

### Deprecation
'isInjvm()' is deprecated
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/ReferenceConfig.java`
#### Snippet
```java
                u = u.setScopeModel(getScopeModel());
                u = u.setServiceModel(consumerModel);
                if (isInjvm() != null && isInjvm()) {
                    u = u.addParameter(LOCAL_PROTOCOL, true);
                }
```

### Deprecation
'isInjvm()' is deprecated
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/ReferenceConfig.java`
#### Snippet
```java
                u = u.setScopeModel(getScopeModel());
                u = u.setServiceModel(consumerModel);
                if (isInjvm() != null && isInjvm()) {
                    u = u.addParameter(LOCAL_PROTOCOL, true);
                }
```

### Deprecation
'isInjvm()' is deprecated
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/ReferenceConfig.java`
#### Snippet
```java
    protected boolean shouldJvmRefer(Map<String, String> map) {
        boolean isJvmRefer;
        if (isInjvm() == null) {
            // if an url is specified, don't do local reference
            if (StringUtils.isNotEmpty(url)) {
```

### Deprecation
'isInjvm()' is deprecated
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/ReferenceConfig.java`
#### Snippet
```java
            }
        } else {
            isJvmRefer = isInjvm();
        }
        return isJvmRefer;
```

### Deprecation
'org.apache.dubbo.config.annotation.Reference' is deprecated
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/ReferenceConfig.java`
#### Snippet
```java
    }

    public ReferenceConfig(Reference reference) {
        super(reference);
    }
```

### Deprecation
'appendMetricsCompatible(java.util.Map)' is deprecated
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/ReferenceConfig.java`
#### Snippet
```java
        AbstractConfig.appendParameters(map, consumer);
        AbstractConfig.appendParameters(map, this);
        appendMetricsCompatible(map);

        String hostToRegistry = ConfigUtils.getSystemProperty(DUBBO_IP_TO_REGISTRY);
```

### Deprecation
'DEFAULT_MULTIPLE_CONFIG_PROPERTY_VALUE' is deprecated
in `dubbo-spring-boot/dubbo-spring-boot-compatible/autoconfigure/src/main/java/org/apache/dubbo/spring/boot/autoconfigure/DubboConfigurationProperties.java`
#### Snippet
```java
         * Indicates multiple properties binding from externalized configuration or not.
         */
        private boolean multiple = DEFAULT_MULTIPLE_CONFIG_PROPERTY_VALUE;

        /**
```

### Deprecation
'DEFAULT_MULTIPLE_CONFIG_PROPERTY_VALUE' is deprecated
in `dubbo-spring-boot/dubbo-spring-boot-compatible/autoconfigure/src/main/java/org/apache/dubbo/spring/boot/autoconfigure/DubboConfigurationProperties.java`
#### Snippet
```java
import java.util.Set;

import static org.apache.dubbo.spring.boot.util.DubboUtils.DEFAULT_MULTIPLE_CONFIG_PROPERTY_VALUE;
import static org.apache.dubbo.spring.boot.util.DubboUtils.DEFAULT_OVERRIDE_CONFIG_PROPERTY_VALUE;
import static org.apache.dubbo.spring.boot.util.DubboUtils.DUBBO_PREFIX;
```

### Deprecation
'org.apache.dubbo.remoting.Codec' is deprecated
in `dubbo-remoting/dubbo-remoting-netty4/src/main/java/org/apache/dubbo/remoting/transport/netty4/NettyConfigOperator.java`
#### Snippet
```java
        if (url.getOrDefaultFrameworkModel().getExtensionLoader(Codec2.class).hasExtension(codecName)) {
            codec2 = url.getOrDefaultFrameworkModel().getExtensionLoader(Codec2.class).getExtension(codecName);
        } else if(url.getOrDefaultFrameworkModel().getExtensionLoader(Codec.class).hasExtension(codecName)){
            codec2 = new CodecAdapter(url.getOrDefaultFrameworkModel().getExtensionLoader(Codec.class)
                .getExtension(codecName));
```

### Deprecation
'org.apache.dubbo.remoting.Codec' is deprecated
in `dubbo-remoting/dubbo-remoting-netty4/src/main/java/org/apache/dubbo/remoting/transport/netty4/NettyConfigOperator.java`
#### Snippet
```java
            codec2 = url.getOrDefaultFrameworkModel().getExtensionLoader(Codec2.class).getExtension(codecName);
        } else if(url.getOrDefaultFrameworkModel().getExtensionLoader(Codec.class).hasExtension(codecName)){
            codec2 = new CodecAdapter(url.getOrDefaultFrameworkModel().getExtensionLoader(Codec.class)
                .getExtension(codecName));
        }else {
```

### Deprecation
'org.apache.dubbo.remoting.Codec' is deprecated
in `dubbo-remoting/dubbo-remoting-netty4/src/main/java/org/apache/dubbo/remoting/transport/netty4/NettyChannel.java`
#### Snippet
```java
        if (frameworkModel.getExtensionLoader(Codec2.class).hasExtension(codecName)) {
            return frameworkModel.getExtensionLoader(Codec2.class).getExtension(codecName);
        } else if(frameworkModel.getExtensionLoader(Codec.class).hasExtension(codecName)){
            return new CodecAdapter(frameworkModel.getExtensionLoader(Codec.class)
                .getExtension(codecName));
```

### Deprecation
'org.apache.dubbo.remoting.Codec' is deprecated
in `dubbo-remoting/dubbo-remoting-netty4/src/main/java/org/apache/dubbo/remoting/transport/netty4/NettyChannel.java`
#### Snippet
```java
            return frameworkModel.getExtensionLoader(Codec2.class).getExtension(codecName);
        } else if(frameworkModel.getExtensionLoader(Codec.class).hasExtension(codecName)){
            return new CodecAdapter(frameworkModel.getExtensionLoader(Codec.class)
                .getExtension(codecName));
        }else {
```

### Deprecation
'getContext()' is deprecated
in `dubbo-demo/dubbo-demo-native/dubbo-demo-native-provider/src/main/java/org/apache/dubbo/demo/graalvm/provider/DemoServiceImpl.java`
#### Snippet
```java
    @Override
    public String sayHello(String name) {
        System.out.println("Hello " + name + ", request from consumer: " + RpcContext.getContext().getRemoteAddress());
        return "Hello " + name ;
    }
```

### Deprecation
'EXACT_MATCH' is deprecated
in `dubbo-xds/src/main/java/org/apache/dubbo/rpc/cluster/router/xds/RdsVirtualHostListener.java`
#### Snippet
```java
            matcher.setInverted(headerMatcher.getInvertMatch());
            switch (headerMatcher.getHeaderMatchSpecifierCase()) {
                case EXACT_MATCH:
                    matcher.setExactValue(headerMatcher.getExactMatch());
                    headerMatchers.add(matcher);
```

### Deprecation
'getExactMatch()' is deprecated
in `dubbo-xds/src/main/java/org/apache/dubbo/rpc/cluster/router/xds/RdsVirtualHostListener.java`
#### Snippet
```java
            switch (headerMatcher.getHeaderMatchSpecifierCase()) {
                case EXACT_MATCH:
                    matcher.setExactValue(headerMatcher.getExactMatch());
                    headerMatchers.add(matcher);
                    break;
```

### Deprecation
'SAFE_REGEX_MATCH' is deprecated
in `dubbo-xds/src/main/java/org/apache/dubbo/rpc/cluster/router/xds/RdsVirtualHostListener.java`
#### Snippet
```java
                    headerMatchers.add(matcher);
                    break;
                case SAFE_REGEX_MATCH:
                    matcher.setRegex(headerMatcher.getSafeRegexMatch().getRegex());
                    headerMatchers.add(matcher);
```

### Deprecation
'getSafeRegexMatch()' is deprecated
in `dubbo-xds/src/main/java/org/apache/dubbo/rpc/cluster/router/xds/RdsVirtualHostListener.java`
#### Snippet
```java
                    break;
                case SAFE_REGEX_MATCH:
                    matcher.setRegex(headerMatcher.getSafeRegexMatch().getRegex());
                    headerMatchers.add(matcher);
                    break;
```

### Deprecation
'PREFIX_MATCH' is deprecated
in `dubbo-xds/src/main/java/org/apache/dubbo/rpc/cluster/router/xds/RdsVirtualHostListener.java`
#### Snippet
```java
                    headerMatchers.add(matcher);
                    break;
                case PREFIX_MATCH:
                    matcher.setPrefix(headerMatcher.getPrefixMatch());
                    headerMatchers.add(matcher);
```

### Deprecation
'getPrefixMatch()' is deprecated
in `dubbo-xds/src/main/java/org/apache/dubbo/rpc/cluster/router/xds/RdsVirtualHostListener.java`
#### Snippet
```java
                    break;
                case PREFIX_MATCH:
                    matcher.setPrefix(headerMatcher.getPrefixMatch());
                    headerMatchers.add(matcher);
                    break;
```

### Deprecation
'SUFFIX_MATCH' is deprecated
in `dubbo-xds/src/main/java/org/apache/dubbo/rpc/cluster/router/xds/RdsVirtualHostListener.java`
#### Snippet
```java
                    headerMatchers.add(matcher);
                    break;
                case SUFFIX_MATCH:
                    matcher.setSuffix(headerMatcher.getSuffixMatch());
                    headerMatchers.add(matcher);
```

### Deprecation
'getSuffixMatch()' is deprecated
in `dubbo-xds/src/main/java/org/apache/dubbo/rpc/cluster/router/xds/RdsVirtualHostListener.java`
#### Snippet
```java
                    break;
                case SUFFIX_MATCH:
                    matcher.setSuffix(headerMatcher.getSuffixMatch());
                    headerMatchers.add(matcher);
                    break;
```

### Deprecation
'attachHeaders(T, io.grpc.Metadata)' is deprecated
in `dubbo-xds/src/main/java/org/apache/dubbo/registry/xds/istio/IstioCitadelCertificateSigner.java`
#### Snippet
```java
        IstioCertificateServiceGrpc.IstioCertificateServiceStub stub = IstioCertificateServiceGrpc.newStub(channel);

        stub = MetadataUtils.attachHeaders(stub, header);

        CountDownLatch countDownLatch = new CountDownLatch(1);
```

### Deprecation
'org.apache.dubbo.config.annotation.Service' is deprecated
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/rest/AbstractServiceRestMetadataResolver.java`
#### Snippet
```java

    protected final boolean isServiceAnnotationPresent(Class<?> serviceType) {
        return isAnyAnnotationPresent(serviceType, DubboService.class, Service.class,
            com.alibaba.dubbo.config.annotation.Service.class);
    }
```

### Deprecation
'com.alibaba.dubbo.config.annotation.Service' is deprecated
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/rest/AbstractServiceRestMetadataResolver.java`
#### Snippet
```java
    protected final boolean isServiceAnnotationPresent(Class<?> serviceType) {
        return isAnyAnnotationPresent(serviceType, DubboService.class, Service.class,
            com.alibaba.dubbo.config.annotation.Service.class);
    }

```

### Deprecation
'RpcInvocation()' is deprecated
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/RestRPCInvocationUtil.java`
#### Snippet
```java
     */
    public static RpcInvocation createBaseRpcInvocation(RequestFacade request, RestMethodMetadata restMethodMetadata) {
        RpcInvocation rpcInvocation = new RpcInvocation();

        rpcInvocation.setParameterTypes(restMethodMetadata.getReflectMethod().getParameterTypes());
```

### Deprecation
'isKeepAlive(io.netty.handler.codec.http.HttpMessage)' is deprecated
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/netty/RestHttpRequestDecoder.java`
#### Snippet
```java
    @Override
    protected void decode(ChannelHandlerContext ctx, io.netty.handler.codec.http.FullHttpRequest request, List<Object> out) throws Exception {
        boolean keepAlive = HttpHeaders.isKeepAlive(request);

        NettyHttpResponse nettyHttpResponse = new NettyHttpResponse(ctx, keepAlive);
```

### Deprecation
'io.netty.handler.codec.http.HttpHeaders.Names' is deprecated
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/netty/NettyHttpResponse.java`
#### Snippet
```java
        DefaultFullHttpResponse res = new DefaultFullHttpResponse(HTTP_1_1, HttpResponseStatus.valueOf(getStatus()));
        if (method == null || !method.equals(HttpMethod.HEAD)) {
            res.headers().add(Names.CONTENT_LENGTH, EMPTY_CONTENT_LENGTH);
        }
        transformResponseHeaders(res);
```

### Deprecation
'setTransferEncodingChunked(io.netty.handler.codec.http.HttpMessage)' is deprecated
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/netty/NettyHttpResponse.java`
#### Snippet
```java
        committed = true;
        DefaultHttpResponse response = getDefaultHttpResponse();
        HttpHeaders.setTransferEncodingChunked(response);
        ctx.write(response);
    }
```

### Deprecation
'org.apache.dubbo.config.annotation.Service' is deprecated
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/integration/swagger/DubboSwaggerApiListingResource.java`
#### Snippet
```java
import javax.ws.rs.core.UriInfo;

@Service
public class DubboSwaggerApiListingResource extends BaseApiListingResource implements DubboSwaggerService {

```

### Deprecation
'reset()' is deprecated
in `dubbo-test/dubbo-test-spring/src/main/java/org/apache/dubbo/test/spring/SpringAnnotationBeanTest.java`
#### Snippet
```java
    @AfterAll
    public static void afterAll(){
        DubboBootstrap.reset();
    }

```

### Deprecation
'reset()' is deprecated
in `dubbo-test/dubbo-test-spring/src/main/java/org/apache/dubbo/test/spring/SpringAnnotationBeanTest.java`
#### Snippet
```java
    @BeforeAll
    public static void beforeAll() {
        DubboBootstrap.reset();
    }

```

### Deprecation
'reset()' is deprecated
in `dubbo-test/dubbo-test-spring/src/main/java/org/apache/dubbo/test/spring/SpringXmlConfigTest.java`
#### Snippet
```java
    @BeforeAll
    public static void beforeAll() {
        DubboBootstrap.reset();
    }

```

### Deprecation
'reset()' is deprecated
in `dubbo-test/dubbo-test-spring/src/main/java/org/apache/dubbo/test/spring/SpringXmlConfigTest.java`
#### Snippet
```java
    @AfterAll
    public static void afterAll(){
        DubboBootstrap.reset();
        providerContext.close();
    }
```

### Deprecation
'reset()' is deprecated
in `dubbo-test/dubbo-test-spring/src/main/java/org/apache/dubbo/test/spring/SpringJavaConfigBeanTest.java`
#### Snippet
```java
    @BeforeEach
    public void beforeEach() {
        DubboBootstrap.reset();
    }

```

### Deprecation
'reset()' is deprecated
in `dubbo-test/dubbo-test-spring/src/main/java/org/apache/dubbo/test/spring/SpringJavaConfigBeanTest.java`
#### Snippet
```java
    @BeforeAll
    public static void beforeAll() {
        DubboBootstrap.reset();
    }

```

### Deprecation
'reset()' is deprecated
in `dubbo-test/dubbo-test-spring/src/main/java/org/apache/dubbo/test/spring/SpringJavaConfigBeanTest.java`
#### Snippet
```java
    @AfterAll
    public static void afterAll(){
        DubboBootstrap.reset();
    }

```

### Deprecation
'org.apache.dubbo.rpc.model.ConsumerMethodModel' is deprecated
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ConsumerModel.java`
#### Snippet
```java
     * @return method model
     */
    public ConsumerMethodModel getMethodModel(Method method) {
        return methodModels.get(method);
    }
```

### Deprecation
'org.apache.dubbo.rpc.model.ConsumerMethodModel' is deprecated
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ConsumerModel.java`
#### Snippet
```java
     * @return
     */
    public ConsumerMethodModel getMethodModel(String method, String[] argsType) {
        Optional<ConsumerMethodModel> consumerMethodModel = methodModels.entrySet().stream()
            .filter(entry -> entry.getKey().getName().equals(method))
```

### Deprecation
'org.apache.dubbo.rpc.model.ConsumerMethodModel' is deprecated
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ConsumerModel.java`
#### Snippet
```java
     */
    public ConsumerMethodModel getMethodModel(String method, String[] argsType) {
        Optional<ConsumerMethodModel> consumerMethodModel = methodModels.entrySet().stream()
            .filter(entry -> entry.getKey().getName().equals(method))
            .map(Map.Entry::getValue).filter(methodModel -> Arrays.equals(argsType, methodModel.getParameterTypes()))
```

### Deprecation
'org.apache.dubbo.rpc.model.ConsumerMethodModel' is deprecated
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ConsumerModel.java`
#### Snippet
```java
     * @return method model list
     */
    public List<ConsumerMethodModel> getAllMethodModels() {
        return new ArrayList<>(methodModels.values());
    }
```

### Deprecation
'org.apache.dubbo.rpc.model.ConsumerMethodModel' is deprecated
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ConsumerModel.java`
#### Snippet
```java
     * @return method model
     */
    public ConsumerMethodModel getMethodModel(String method) {
        Optional<Map.Entry<Method, ConsumerMethodModel>> consumerMethodModelEntry = methodModels.entrySet().stream().filter(entry -> entry.getKey().getName().equals(method)).findFirst();
        return consumerMethodModelEntry.map(Map.Entry::getValue).orElse(null);
```

### Deprecation
'org.apache.dubbo.rpc.model.ConsumerMethodModel' is deprecated
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ConsumerModel.java`
#### Snippet
```java
     */
    public ConsumerMethodModel getMethodModel(String method) {
        Optional<Map.Entry<Method, ConsumerMethodModel>> consumerMethodModelEntry = methodModels.entrySet().stream().filter(entry -> entry.getKey().getName().equals(method)).findFirst();
        return consumerMethodModelEntry.map(Map.Entry::getValue).orElse(null);
    }
```

### Deprecation
'org.apache.dubbo.rpc.model.ConsumerMethodModel' is deprecated
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ConsumerModel.java`
#### Snippet
```java
        for (Class<?> interfaceClass : interfaceList) {
            for (Method method : interfaceClass.getMethods()) {
                methodModels.put(method, new ConsumerMethodModel(method));
            }
        }
```

### Deprecation
'org.apache.dubbo.rpc.model.ConsumerMethodModel' is deprecated
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ConsumerModel.java`
#### Snippet
```java

    private final Map<String, AsyncMethodInfo> methodConfigs;
    private Map<Method, ConsumerMethodModel> methodModels = new HashMap<>();

    /**
```

### Deprecation
'org.apache.dubbo.rpc.model.ProviderMethodModel' is deprecated
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ProviderModel.java`
#### Snippet
```java
public class ProviderModel extends ServiceModel {
    private final List<RegisterStatedURL> urls;
    private final Map<String, List<ProviderMethodModel>> methods = new HashMap<>();

    /**
```

### Deprecation
'org.apache.dubbo.rpc.model.ProviderMethodModel' is deprecated
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ProviderModel.java`
#### Snippet
```java
    }

    public ProviderMethodModel getMethodModel(String methodName, String[] argTypes) {
        List<ProviderMethodModel> methodModels = methods.get(methodName);
        if (methodModels != null) {
```

### Deprecation
'org.apache.dubbo.rpc.model.ProviderMethodModel' is deprecated
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ProviderModel.java`
#### Snippet
```java

    public ProviderMethodModel getMethodModel(String methodName, String[] argTypes) {
        List<ProviderMethodModel> methodModels = methods.get(methodName);
        if (methodModels != null) {
            for (ProviderMethodModel methodModel : methodModels) {
```

### Deprecation
'org.apache.dubbo.rpc.model.ProviderMethodModel' is deprecated
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ProviderModel.java`
#### Snippet
```java
        List<ProviderMethodModel> methodModels = methods.get(methodName);
        if (methodModels != null) {
            for (ProviderMethodModel methodModel : methodModels) {
                if (Arrays.equals(argTypes, methodModel.getMethodArgTypes())) {
                    return methodModel;
```

### Deprecation
'org.apache.dubbo.rpc.model.ProviderMethodModel' is deprecated
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ProviderModel.java`
#### Snippet
```java
            method.setAccessible(true);

            List<ProviderMethodModel> methodModels = methods.computeIfAbsent(method.getName(), k -> new ArrayList<>());
            methodModels.add(new ProviderMethodModel(method));
        }
```

### Deprecation
'org.apache.dubbo.rpc.model.ProviderMethodModel' is deprecated
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ProviderModel.java`
#### Snippet
```java

            List<ProviderMethodModel> methodModels = methods.computeIfAbsent(method.getName(), k -> new ArrayList<>());
            methodModels.add(new ProviderMethodModel(method));
        }
    }
```

### Deprecation
'org.apache.dubbo.rpc.model.ProviderMethodModel' is deprecated
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ProviderModel.java`
#### Snippet
```java
    }

    public List<ProviderMethodModel> getMethodModelList(String methodName) {
        List<ProviderMethodModel> resultList = methods.get(methodName);
        return resultList == null ? Collections.emptyList() : resultList;
```

### Deprecation
'org.apache.dubbo.rpc.model.ProviderMethodModel' is deprecated
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ProviderModel.java`
#### Snippet
```java

    public List<ProviderMethodModel> getMethodModelList(String methodName) {
        List<ProviderMethodModel> resultList = methods.get(methodName);
        return resultList == null ? Collections.emptyList() : resultList;
    }
```

### Deprecation
'org.apache.dubbo.rpc.model.ProviderMethodModel' is deprecated
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ProviderModel.java`
#### Snippet
```java
    }

    public List<ProviderMethodModel> getAllMethodModels() {
        List<ProviderMethodModel> result = new ArrayList<ProviderMethodModel>();
        for (List<ProviderMethodModel> models : methods.values()) {
```

### Deprecation
'org.apache.dubbo.rpc.model.ProviderMethodModel' is deprecated
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ProviderModel.java`
#### Snippet
```java

    public List<ProviderMethodModel> getAllMethodModels() {
        List<ProviderMethodModel> result = new ArrayList<ProviderMethodModel>();
        for (List<ProviderMethodModel> models : methods.values()) {
            result.addAll(models);
```

### Deprecation
'org.apache.dubbo.rpc.model.ProviderMethodModel' is deprecated
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ProviderModel.java`
#### Snippet
```java

    public List<ProviderMethodModel> getAllMethodModels() {
        List<ProviderMethodModel> result = new ArrayList<ProviderMethodModel>();
        for (List<ProviderMethodModel> models : methods.values()) {
            result.addAll(models);
```

### Deprecation
'org.apache.dubbo.rpc.model.ProviderMethodModel' is deprecated
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ProviderModel.java`
#### Snippet
```java
    public List<ProviderMethodModel> getAllMethodModels() {
        List<ProviderMethodModel> result = new ArrayList<ProviderMethodModel>();
        for (List<ProviderMethodModel> models : methods.values()) {
            result.addAll(models);
        }
```

### Deprecation
'org.apache.dubbo.config.annotation.Service' is deprecated
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ServiceAnnotationResolver.java`
#### Snippet
```java
     * @since 2.7.9
     */
    public static List<Class<? extends Annotation>> SERVICE_ANNOTATION_CLASSES = unmodifiableList(asList(DubboService.class, Service.class, com.alibaba.dubbo.config.annotation.Service.class));

    private final Annotation serviceAnnotation;
```

### Deprecation
'com.alibaba.dubbo.config.annotation.Service' is deprecated
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ServiceAnnotationResolver.java`
#### Snippet
```java
     * @since 2.7.9
     */
    public static List<Class<? extends Annotation>> SERVICE_ANNOTATION_CLASSES = unmodifiableList(asList(DubboService.class, Service.class, com.alibaba.dubbo.config.annotation.Service.class));

    private final Annotation serviceAnnotation;
```

### Deprecation
'org.apache.dubbo.config.annotation.Service' is deprecated
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ServiceAnnotationResolver.java`
#### Snippet
```java

/**
 * The resolver class for {@link Service @Service}
 *
 * @see Service
```

### Deprecation
'org.apache.dubbo.config.annotation.Service' is deprecated
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ServiceAnnotationResolver.java`
#### Snippet
```java
 * The resolver class for {@link Service @Service}
 *
 * @see Service
 * @see com.alibaba.dubbo.config.annotation.Service
 * @since 2.7.6
```

### Deprecation
'com.alibaba.dubbo.config.annotation.Service' is deprecated
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ServiceAnnotationResolver.java`
#### Snippet
```java
 *
 * @see Service
 * @see com.alibaba.dubbo.config.annotation.Service
 * @since 2.7.6
 */
```

### Deprecation
Overrides deprecated method in 'org.apache.dubbo.common.config.Environment'
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/ModuleEnvironment.java`
#### Snippet
```java
    @Override
    @DisableInject
    public void setLocalMigrationRule(String localMigrationRule) {
        applicationDelegate.setLocalMigrationRule(localMigrationRule);
    }
```

### Deprecation
'setLocalMigrationRule(java.lang.String)' is deprecated
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/ModuleEnvironment.java`
#### Snippet
```java
    @DisableInject
    public void setLocalMigrationRule(String localMigrationRule) {
        applicationDelegate.setLocalMigrationRule(localMigrationRule);
    }

```

### Deprecation
'isPrimitive(java.lang.Class)' is deprecated
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/MethodUtils.java`
#### Snippet
```java
            return false;
        }
        if (!ClassUtils.isPrimitive(method.getReturnType())) {
            return false;
        }
```

### Deprecation
'isPrimitive(java.lang.Class)' is deprecated
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/MethodUtils.java`
#### Snippet
```java
                && Modifier.isPublic(method.getModifiers())
                && method.getParameterTypes().length == 0
                && ClassUtils.isPrimitive(method.getReturnType());
    }

```

### Deprecation
'isPrimitive(java.lang.Class)' is deprecated
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/MethodUtils.java`
#### Snippet
```java
                && Modifier.isPublic(method.getModifiers())
                && method.getParameterCount() == 1
                && ClassUtils.isPrimitive(method.getParameterTypes()[0]);
    }

```

### Deprecation
'SHUTDOWN_WAIT_SECONDS_KEY' is deprecated
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/ConfigurationUtils.java`
#### Snippet
```java
import static org.apache.dubbo.common.constants.CommonConstants.DEFAULT_SERVER_SHUTDOWN_TIMEOUT;
import static org.apache.dubbo.common.constants.CommonConstants.SHUTDOWN_WAIT_KEY;
import static org.apache.dubbo.common.constants.CommonConstants.SHUTDOWN_WAIT_SECONDS_KEY;
import static org.apache.dubbo.common.constants.LoggerCodeConstants.COMMON_UNEXPECTED_EXCEPTION;

```

### Deprecation
'getExportThreadNum()' is deprecated
in `dubbo-common/src/main/java/org/apache/dubbo/common/threadpool/manager/DefaultExecutorRepository.java`
#### Snippet
```java
            threadNum = moduleModel.getConfigManager().getProviders()
                .stream()
                .map(ProviderConfig::getExportThreadNum)
                .filter(k -> k != null && k > 0)
                .findAny().orElse(null);
```

### Deprecation
'com.alibaba.dubbo.common.extension.Activate' is deprecated
in `dubbo-common/src/main/java/org/apache/dubbo/common/extension/ExtensionLoader.java`
#### Snippet
```java
        if (activate instanceof Activate) {
            onClass = ((Activate) activate).onClass();
        } else if (activate instanceof com.alibaba.dubbo.common.extension.Activate) {
            onClass = ((com.alibaba.dubbo.common.extension.Activate) activate).onClass();
        }
```

### Deprecation
'com.alibaba.dubbo.common.extension.Activate' is deprecated
in `dubbo-common/src/main/java/org/apache/dubbo/common/extension/ExtensionLoader.java`
#### Snippet
```java
            onClass = ((Activate) activate).onClass();
        } else if (activate instanceof com.alibaba.dubbo.common.extension.Activate) {
            onClass = ((com.alibaba.dubbo.common.extension.Activate) activate).onClass();
        }

```

### Deprecation
'getReferAsync()' is deprecated
in `dubbo-common/src/main/java/org/apache/dubbo/config/ReferenceConfigBase.java`
#### Snippet
```java

    public Boolean shouldReferAsync() {
        Boolean shouldReferAsync = getReferAsync();
        if (shouldReferAsync == null) {
            shouldReferAsync = consumer != null && consumer.getReferAsync() != null && consumer.getReferAsync();
```

### Deprecation
'getReferAsync()' is deprecated
in `dubbo-common/src/main/java/org/apache/dubbo/config/ReferenceConfigBase.java`
#### Snippet
```java
        Boolean shouldReferAsync = getReferAsync();
        if (shouldReferAsync == null) {
            shouldReferAsync = consumer != null && consumer.getReferAsync() != null && consumer.getReferAsync();
        }

```

### Deprecation
'getReferAsync()' is deprecated
in `dubbo-common/src/main/java/org/apache/dubbo/config/ReferenceConfigBase.java`
#### Snippet
```java
        Boolean shouldReferAsync = getReferAsync();
        if (shouldReferAsync == null) {
            shouldReferAsync = consumer != null && consumer.getReferAsync() != null && consumer.getReferAsync();
        }

```

### Deprecation
'org.apache.dubbo.config.annotation.Reference' is deprecated
in `dubbo-common/src/main/java/org/apache/dubbo/config/ReferenceConfigBase.java`
#### Snippet
```java
    }

    public ReferenceConfigBase(Reference reference) {
        serviceMetadata = new ServiceMetadata();
        serviceMetadata.addAttribute(ORIGIN_CONFIG, this);
```

### Deprecation
'org.apache.dubbo.config.annotation.Reference' is deprecated
in `dubbo-common/src/main/java/org/apache/dubbo/config/ReferenceConfigBase.java`
#### Snippet
```java
        serviceMetadata = new ServiceMetadata();
        serviceMetadata.addAttribute(ORIGIN_CONFIG, this);
        appendAnnotation(Reference.class, reference);
        setMethods(MethodConfig.constructMethodConfig(reference.methods()));
    }
```

### Deprecation
'constructMethodConfig(org.apache.dubbo.config.annotation.Method\[\])' is deprecated
in `dubbo-common/src/main/java/org/apache/dubbo/config/ReferenceConfigBase.java`
#### Snippet
```java
        serviceMetadata.addAttribute(ORIGIN_CONFIG, this);
        appendAnnotation(Reference.class, reference);
        setMethods(MethodConfig.constructMethodConfig(reference.methods()));
    }

```

### Deprecation
'org.apache.dubbo.config.annotation.Reference' is deprecated
in `dubbo-common/src/main/java/org/apache/dubbo/config/ReferenceConfigBase.java`
#### Snippet
```java
    }

    public ReferenceConfigBase(ModuleModel moduleModel, Reference reference) {
        super(moduleModel);
        serviceMetadata = new ServiceMetadata();
```

### Deprecation
'org.apache.dubbo.config.annotation.Reference' is deprecated
in `dubbo-common/src/main/java/org/apache/dubbo/config/ReferenceConfigBase.java`
#### Snippet
```java
        serviceMetadata = new ServiceMetadata();
        serviceMetadata.addAttribute(ORIGIN_CONFIG, this);
        appendAnnotation(Reference.class, reference);
        setMethods(MethodConfig.constructMethodConfig(reference.methods()));
    }
```

### Deprecation
'constructMethodConfig(org.apache.dubbo.config.annotation.Method\[\])' is deprecated
in `dubbo-common/src/main/java/org/apache/dubbo/config/ReferenceConfigBase.java`
#### Snippet
```java
        serviceMetadata.addAttribute(ORIGIN_CONFIG, this);
        appendAnnotation(Reference.class, reference);
        setMethods(MethodConfig.constructMethodConfig(reference.methods()));
    }

```

### Deprecation
'org.apache.dubbo.config.annotation.Service' is deprecated
in `dubbo-common/src/main/java/org/apache/dubbo/config/ServiceConfigBase.java`
#### Snippet
```java
    }

    public ServiceConfigBase(Service service) {
        serviceMetadata = new ServiceMetadata();
        serviceMetadata.addAttribute("ORIGIN_CONFIG", this);
```

### Deprecation
'org.apache.dubbo.config.annotation.Service' is deprecated
in `dubbo-common/src/main/java/org/apache/dubbo/config/ServiceConfigBase.java`
#### Snippet
```java
        serviceMetadata = new ServiceMetadata();
        serviceMetadata.addAttribute("ORIGIN_CONFIG", this);
        appendAnnotation(Service.class, service);
        setMethods(MethodConfig.constructMethodConfig(service.methods()));
    }
```

### Deprecation
'constructMethodConfig(org.apache.dubbo.config.annotation.Method\[\])' is deprecated
in `dubbo-common/src/main/java/org/apache/dubbo/config/ServiceConfigBase.java`
#### Snippet
```java
        serviceMetadata.addAttribute("ORIGIN_CONFIG", this);
        appendAnnotation(Service.class, service);
        setMethods(MethodConfig.constructMethodConfig(service.methods()));
    }

```

### Deprecation
'getExportAsync()' is deprecated
in `dubbo-common/src/main/java/org/apache/dubbo/config/ServiceConfigBase.java`
#### Snippet
```java

    public Boolean shouldExportAsync() {
        Boolean shouldExportAsync = getExportAsync();
        if (shouldExportAsync == null) {
            shouldExportAsync = provider != null && provider.getExportAsync() != null && provider.getExportAsync();
```

### Deprecation
'getExportAsync()' is deprecated
in `dubbo-common/src/main/java/org/apache/dubbo/config/ServiceConfigBase.java`
#### Snippet
```java
        Boolean shouldExportAsync = getExportAsync();
        if (shouldExportAsync == null) {
            shouldExportAsync = provider != null && provider.getExportAsync() != null && provider.getExportAsync();
        }

```

### Deprecation
'getExportAsync()' is deprecated
in `dubbo-common/src/main/java/org/apache/dubbo/config/ServiceConfigBase.java`
#### Snippet
```java
        Boolean shouldExportAsync = getExportAsync();
        if (shouldExportAsync == null) {
            shouldExportAsync = provider != null && provider.getExportAsync() != null && provider.getExportAsync();
        }

```

### Deprecation
'setConfigCenter(org.apache.dubbo.config.ConfigCenterConfig)' is deprecated
in `dubbo-common/src/main/java/org/apache/dubbo/config/ServiceConfigBase.java`
#### Snippet
```java
            }
            if (configCenter == null) {
                setConfigCenter(provider.getConfigCenter());
            }
        }
```

### Deprecation
'getConfigCenter()' is deprecated
in `dubbo-common/src/main/java/org/apache/dubbo/config/ServiceConfigBase.java`
#### Snippet
```java
            }
            if (configCenter == null) {
                setConfigCenter(provider.getConfigCenter());
            }
        }
```

### Deprecation
'org.apache.dubbo.config.annotation.Service' is deprecated
in `dubbo-common/src/main/java/org/apache/dubbo/config/ServiceConfigBase.java`
#### Snippet
```java
    }

    public ServiceConfigBase(ModuleModel moduleModel, Service service) {
        super(moduleModel);
        serviceMetadata = new ServiceMetadata();
```

### Deprecation
'org.apache.dubbo.config.annotation.Service' is deprecated
in `dubbo-common/src/main/java/org/apache/dubbo/config/ServiceConfigBase.java`
#### Snippet
```java
        serviceMetadata = new ServiceMetadata();
        serviceMetadata.addAttribute("ORIGIN_CONFIG", this);
        appendAnnotation(Service.class, service);
        setMethods(MethodConfig.constructMethodConfig(service.methods()));
    }
```

### Deprecation
'constructMethodConfig(org.apache.dubbo.config.annotation.Method\[\])' is deprecated
in `dubbo-common/src/main/java/org/apache/dubbo/config/ServiceConfigBase.java`
#### Snippet
```java
        serviceMetadata.addAttribute("ORIGIN_CONFIG", this);
        appendAnnotation(Service.class, service);
        setMethods(MethodConfig.constructMethodConfig(service.methods()));
    }

```

### Deprecation
'findMethodByMethodSignature(java.lang.Class, java.lang.String, java.lang.String\[\])' is deprecated
in `dubbo-common/src/main/java/org/apache/dubbo/config/AbstractConfig.java`
#### Snippet
```java
import static org.apache.dubbo.common.constants.LoggerCodeConstants.COMMON_UNEXPECTED_EXCEPTION;
import static org.apache.dubbo.common.utils.ClassUtils.isSimpleType;
import static org.apache.dubbo.common.utils.ReflectUtils.findMethodByMethodSignature;
import static org.apache.dubbo.config.Constants.PARAMETERS;

```

### Deprecation
'findMethodByMethodSignature(java.lang.Class, java.lang.String, java.lang.String\[\])' is deprecated
in `dubbo-common/src/main/java/org/apache/dubbo/config/AbstractConfig.java`
#### Snippet
```java
        try {

            Method method = findMethodByMethodSignature(c, "getParameters", null);
            if (method != null && isParametersGetter(method)) {
                return (Map<String, String>) method.invoke(o);
```

### Deprecation
'findMethodByMethodSignature(java.lang.Class, java.lang.String, java.lang.String\[\])' is deprecated
in `dubbo-common/src/main/java/org/apache/dubbo/config/AbstractConfig.java`
#### Snippet
```java
    private static void invokeSetParameters(Class c, Object o, Map map) {
        try {
            Method method = findMethodByMethodSignature(c, "setParameters", new String[]{Map.class.getName()});
            if (method != null && isParametersSetter(method)) {
                method.invoke(o, map);
```

### Deprecation
'findMethodByMethodName(java.lang.Class, java.lang.String)' is deprecated
in `dubbo-common/src/main/java/org/apache/dubbo/config/MethodConfig.java`
#### Snippet
```java
    private java.lang.reflect.Method getMethodByName(Class<?> clazz, String methodName) {
        try {
            return ReflectUtils.findMethodByMethodName(clazz, methodName);
        } catch (Exception e) {
            throw new IllegalStateException(e);
```

### Deprecation
'org.apache.dubbo.config.annotation.Service' is deprecated
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/ServiceBean.java`
#### Snippet
```java
    private static final long serialVersionUID = 213195494150089726L;

    private final transient Service service;

    private transient ApplicationContext applicationContext;
```

### Deprecation
'org.apache.dubbo.config.annotation.Service' is deprecated
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/ServiceBean.java`
#### Snippet
```java
    }

    public ServiceBean(Service service) {
        super(service);
        this.service = service;
```

### Deprecation
'org.apache.dubbo.config.annotation.Service' is deprecated
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/ServiceBean.java`
#### Snippet
```java

    /**
     * Gets associated {@link Service}
     *
     * @return associated {@link Service}
```

### Deprecation
'org.apache.dubbo.config.annotation.Service' is deprecated
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/ServiceBean.java`
#### Snippet
```java
     * Gets associated {@link Service}
     *
     * @return associated {@link Service}
     */
    public Service getService() {
```

### Deprecation
'org.apache.dubbo.config.annotation.Service' is deprecated
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/ServiceBean.java`
#### Snippet
```java
     * @return associated {@link Service}
     */
    public Service getService() {
        return service;
    }
```

### Deprecation
'org.apache.dubbo.config.annotation.Service' is deprecated
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/ServiceBean.java`
#### Snippet
```java
    }

    public ServiceBean(ModuleModel moduleModel, Service service) {
        super(moduleModel, service);
        this.service = service;
```

### Deprecation
'org.apache.dubbo.config.annotation.Service' is deprecated
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/DubboAnnotationUtils.java`
#### Snippet
```java
     * Note: the service interface class maybe not found locally if is a generic service.
     *
     * @param attributes             annotation attributes of {@link Service @Service}
     * @param defaultInterfaceClass the default class of interface
     * @return the interface name if found
```

### Deprecation
'com.alibaba.dubbo.rpc.service.GenericService' is deprecated
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/DubboAnnotationUtils.java`
#### Snippet
```java
        if (StringUtils.hasText(interfaceClassName)) {
            if (GenericService.class.getName().equals(interfaceClassName) ||
                com.alibaba.dubbo.rpc.service.GenericService.class.getName().equals(interfaceClassName)) {
                throw new IllegalStateException("@Service interfaceName() cannot be GenericService: " + interfaceClassName);
            }
```

### Deprecation
'org.apache.dubbo.config.annotation.Reference' is deprecated
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/ServiceBeanNameBuilder.java`
#### Snippet
```java
    }

    public static ServiceBeanNameBuilder create(Reference reference, Class<?> interfaceClass, Environment environment) {
        return create(getAnnotationAttributes(reference, false, false), interfaceClass, environment);
    }
```

### Deprecation
'org.apache.dubbo.config.annotation.Service' is deprecated
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/ServiceBeanNameBuilder.java`
#### Snippet
```java
    }

    public static ServiceBeanNameBuilder create(Service service, Class<?> interfaceClass, Environment environment) {
        return create(getAnnotationAttributes(service, false, false), interfaceClass, environment);
    }
```

### Deprecation
'org.apache.dubbo.config.annotation.Service' is deprecated
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/ServiceBeanNameBuilder.java`
#### Snippet
```java

/**
 * Dubbo {@link Service @Service} Bean Builder
 *
 * @see Service
```

### Deprecation
'org.apache.dubbo.config.annotation.Service' is deprecated
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/ServiceBeanNameBuilder.java`
#### Snippet
```java
 * Dubbo {@link Service @Service} Bean Builder
 *
 * @see Service
 * @see Reference
 * @see ServiceBean
```

### Deprecation
'org.apache.dubbo.config.annotation.Reference' is deprecated
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/ServiceBeanNameBuilder.java`
#### Snippet
```java
 *
 * @see Service
 * @see Reference
 * @see ServiceBean
 * @see ReferenceBean
```

### Deprecation
'constructMethodConfig(org.apache.dubbo.config.annotation.Method\[\])' is deprecated
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/ServiceAnnotationPostProcessor.java`
#### Snippet
```java
            return Collections.EMPTY_LIST;
        }
        return MethodConfig.constructMethodConfig((Method[]) methodsAnnotation);
    }

```

### Deprecation
'org.apache.dubbo.config.annotation.Service' is deprecated
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/ServiceAnnotationPostProcessor.java`
#### Snippet
```java
            DubboService.class,
            // @since 2.7.0 the substitute @com.alibaba.dubbo.config.annotation.Service
            Service.class,
            // @since 2.7.3 Add the compatibility for legacy Dubbo's @Service , the issue : https://github.com/apache/dubbo/issues/4330
            com.alibaba.dubbo.config.annotation.Service.class
```

### Deprecation
'com.alibaba.dubbo.config.annotation.Service' is deprecated
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/ServiceAnnotationPostProcessor.java`
#### Snippet
```java
            Service.class,
            // @since 2.7.3 Add the compatibility for legacy Dubbo's @Service , the issue : https://github.com/apache/dubbo/issues/4330
            com.alibaba.dubbo.config.annotation.Service.class
    );

```

### Deprecation
'org.apache.dubbo.config.annotation.Service' is deprecated
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/ServiceAnnotationPostProcessor.java`
#### Snippet
```java

/**
 * A {@link BeanFactoryPostProcessor} used for processing of {@link Service @Service} annotated classes and annotated bean in java config classes.
 * It's also the infrastructure class of XML {@link BeanDefinitionParser} on &lt;dubbo:annotation /&gt;
 *
```

### Deprecation
'org.apache.dubbo.config.annotation.Service' is deprecated
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/ServiceAnnotationPostProcessor.java`
#### Snippet
```java
    /**
     * Finds a {@link Set} of {@link BeanDefinitionHolder BeanDefinitionHolders} whose bean type annotated
     * {@link Service} Annotation.
     *
     * @param scanner       {@link ClassPathBeanDefinitionScanner}
```

### Deprecation
'org.apache.dubbo.config.annotation.Service' is deprecated
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/ServiceAnnotationPostProcessor.java`
#### Snippet
```java
     *
     * @param serviceAnnotationAttributes
     * @param serviceInterface              the class of interface annotated {@link Service}
     * @return ServiceBean@interfaceClassName#annotatedServiceBeanName
     * @since 2.7.3
```

### Deprecation
'org.apache.dubbo.config.annotation.Service' is deprecated
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/ServiceAnnotationPostProcessor.java`
#### Snippet
```java

    /**
     * Scan and registers service beans whose classes was annotated {@link Service}
     *
     * @param packagesToScan The base packages to scan
```

### Deprecation
'org.apache.dubbo.config.annotation.Service' is deprecated
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/ServiceAnnotationPostProcessor.java`
#### Snippet
```java

    /**
     * Registers {@link ServiceBean} from new annotated {@link Service} {@link BeanDefinition}
     *
     * @param beanDefinitionHolder
```

### Deprecation
'org.apache.dubbo.config.annotation.Service' is deprecated
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/context/annotation/DubboComponentScan.java`
#### Snippet
```java
/**
 * Dubbo Component Scan {@link Annotation},scans the classpath for annotated components that will be auto-registered as
 * Spring beans. Dubbo-provided {@link Service} and {@link Reference}.
 *
 * @see Service
```

### Deprecation
'org.apache.dubbo.config.annotation.Reference' is deprecated
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/context/annotation/DubboComponentScan.java`
#### Snippet
```java
/**
 * Dubbo Component Scan {@link Annotation},scans the classpath for annotated components that will be auto-registered as
 * Spring beans. Dubbo-provided {@link Service} and {@link Reference}.
 *
 * @see Service
```

### Deprecation
'org.apache.dubbo.config.annotation.Service' is deprecated
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/context/annotation/DubboComponentScan.java`
#### Snippet
```java
 * Spring beans. Dubbo-provided {@link Service} and {@link Reference}.
 *
 * @see Service
 * @see Reference
 * @since 2.5.7
```

### Deprecation
'org.apache.dubbo.config.annotation.Reference' is deprecated
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/context/annotation/DubboComponentScan.java`
#### Snippet
```java
 *
 * @see Service
 * @see Reference
 * @since 2.5.7
 */
```

### Deprecation
'org.apache.dubbo.config.annotation.Service' is deprecated
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/context/annotation/DubboComponentScanRegistrar.java`
#### Snippet
```java
 * Dubbo {@link DubboComponentScan} Bean Registrar
 *
 * @see Service
 * @see DubboComponentScan
 * @see ImportBeanDefinitionRegistrar
```

### Deprecation
'org.apache.dubbo.config.annotation.Reference' is deprecated
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/ReferenceAnnotationBeanPostProcessor.java`
#### Snippet
```java
 *
 * @see DubboReference
 * @see Reference
 * @see com.alibaba.dubbo.config.annotation.Reference
 * @since 2.5.7
```

### Deprecation
'com.alibaba.dubbo.config.annotation.Reference' is deprecated
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/ReferenceAnnotationBeanPostProcessor.java`
#### Snippet
```java
 * @see DubboReference
 * @see Reference
 * @see com.alibaba.dubbo.config.annotation.Reference
 * @since 2.5.7
 */
```

### Deprecation
'getAnnotationType()' is deprecated
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/ReferenceAnnotationBeanPostProcessor.java`
#### Snippet
```java
            throw ex;
        } catch (Throwable ex) {
            throw new BeanCreationException(beanName, "Injection of @" + getAnnotationType().getSimpleName()
                + " dependencies is failed", ex);
        }
```

### Deprecation
'com.alibaba.dubbo.config.annotation.Reference' is deprecated
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/ReferenceAnnotationBeanPostProcessor.java`
#### Snippet
```java

    /**
     * {@link com.alibaba.dubbo.config.annotation.Reference @com.alibaba.dubbo.config.annotation.Reference} has been supported since 2.7.3
     * <p>
     * {@link DubboReference @DubboReference} has been supported since 2.7.7
```

### Deprecation
'org.apache.dubbo.config.annotation.Reference' is deprecated
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/ReferenceAnnotationBeanPostProcessor.java`
#### Snippet
```java
     */
    public ReferenceAnnotationBeanPostProcessor() {
        super(DubboReference.class, Reference.class, com.alibaba.dubbo.config.annotation.Reference.class);
    }

```

### Deprecation
'com.alibaba.dubbo.config.annotation.Reference' is deprecated
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/ReferenceAnnotationBeanPostProcessor.java`
#### Snippet
```java
     */
    public ReferenceAnnotationBeanPostProcessor() {
        super(DubboReference.class, Reference.class, com.alibaba.dubbo.config.annotation.Reference.class);
    }

```

### Deprecation
'setMonitor(org.apache.dubbo.config.MonitorConfig)' is deprecated
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/reference/ReferenceCreator.java`
#### Snippet
```java
        if (StringUtils.hasText(monitorConfigId)) {
            MonitorConfig monitorConfig = getConfig(monitorConfigId, MonitorConfig.class);
            configBean.setMonitor(monitorConfig);
        }
    }
```

### Deprecation
'setModule(org.apache.dubbo.config.ModuleConfig)' is deprecated
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/reference/ReferenceCreator.java`
#### Snippet
```java
        if (StringUtils.hasText(moduleConfigId)) {
            ModuleConfig moduleConfig = getConfig(moduleConfigId, ModuleConfig.class);
            configBean.setModule(moduleConfig);
        }
    }
```

### Deprecation
'getDubboProtocol()' is deprecated
in `dubbo-plugin/dubbo-qos/src/main/java/org/apache/dubbo/qos/legacy/ChangeTelnetHandler.java`
#### Snippet
```java
        } else {
            boolean found = false;
            for (Exporter<?> exporter : DubboProtocol.getDubboProtocol().getExporters()) {
                if (message.equals(exporter.getInvoker().getInterface().getSimpleName())
                        || message.equals(exporter.getInvoker().getInterface().getName())
```

### Deprecation
'getDubboProtocol()' is deprecated
in `dubbo-plugin/dubbo-qos/src/main/java/org/apache/dubbo/qos/legacy/TraceTelnetHandler.java`
#### Snippet
```java
        }
        Invoker<?> invoker = null;
        for (Exporter<?> exporter : DubboProtocol.getDubboProtocol().getExporters()) {
            if (service.equals(exporter.getInvoker().getInterface().getSimpleName())
                    || service.equals(exporter.getInvoker().getInterface().getName())
```

### Deprecation
'getAndRemove()' is deprecated
in `dubbo-plugin/dubbo-qos/src/main/java/org/apache/dubbo/qos/command/impl/ChangeTelnet.java`
#### Snippet
```java
        StringBuilder buf = new StringBuilder();
        if ("/".equals(message) || "..".equals(message)) {
            String service = channel.attr(SERVICE_KEY).getAndRemove();
            buf.append("Cancelled default service ").append(service).append('.');
        } else {
```

### Deprecation
'remove()' is deprecated
in `dubbo-plugin/dubbo-qos/src/main/java/org/apache/dubbo/qos/command/impl/InvokeTelnet.java`
#### Snippet
```java
    private boolean isInvokedSelectCommand(Channel channel) {
        if (channel.attr(SelectTelnet.SELECT_KEY).get() != null) {
            channel.attr(SelectTelnet.SELECT_KEY).remove();
            return true;
        }
```

### Deprecation
'setCompatibleFuture(java.util.concurrent.CompletableFuture)' is deprecated
in `dubbo-rpc/dubbo-rpc-injvm/src/main/java/org/apache/dubbo/rpc/protocol/injvm/InjvmInvoker.java`
#### Snippet
```java
            }, executor);
            // save for 2.6.x compatibility, for example, TraceFilter in Zipkin uses com.alibaba.xxx.FutureAdapter
            FutureContext.getContext().setCompatibleFuture(appResponseFuture);
            AsyncRpcResult result = new AsyncRpcResult(appResponseFuture, copiedInvocation);
            result.setExecutor(executor);
```

### Deprecation
'remove(java.lang.String)' is deprecated
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcContext.java`
#### Snippet
```java
        RpcContextAttachment rpcContextAttachment = RpcContext.getServerContext();
        for(String key : rpcContextAttachment.attachments.keySet()) {
            rpcContextAttachment.remove(key);
        }
    }
```

### Deprecation
Overrides deprecated method in 'org.apache.dubbo.rpc.RpcContext'
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcContextAttachment.java`
#### Snippet
```java
    @Override
    @Deprecated
    public Object get(String key) {
        return getAttachment(key);
    }
```

### Deprecation
Overrides deprecated method in 'org.apache.dubbo.rpc.RpcContext'
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcContextAttachment.java`
#### Snippet
```java
    @Override
    @Deprecated
    public RpcContextAttachment remove(String key) {
        return removeAttachment(key);
    }
```

### Deprecation
Overrides deprecated method in 'org.apache.dubbo.rpc.RpcContext'
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcContextAttachment.java`
#### Snippet
```java
    @Override
    @Deprecated
    public RpcContextAttachment set(String key, Object value) {
        return setAttachment(key, value);
    }
```

### Deprecation
Overrides deprecated method in 'org.apache.dubbo.rpc.RpcContext'
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcContextAttachment.java`
#### Snippet
```java
    @Override
    @Deprecated
    public Map<String, Object> get() {
        return getObjectAttachments();
    }
```

### Deprecation
Overrides deprecated method in 'org.apache.dubbo.rpc.RpcContext'
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcContextAttachment.java`
#### Snippet
```java
    @Override
    @Deprecated
    public Map<String, String> getAttachments() {
        return new AttachmentsAdapter.ObjectToStringMap(this.getObjectAttachments());
    }
```

### Deprecation
Overrides deprecated method in 'org.apache.dubbo.rpc.RpcContextAttachment'
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcServerContextAttachment.java`
#### Snippet
```java

    @Override
    public Map<String, Object> get() {
        return getObjectAttachments();
    }
```

### Deprecation
Overrides deprecated method in 'org.apache.dubbo.rpc.RpcContextAttachment'
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcServerContextAttachment.java`
#### Snippet
```java

    @Override
    public Object get(String key) {
        return getAttachment(key);
    }
```

### Deprecation
Overrides deprecated method in 'org.apache.dubbo.rpc.RpcContextAttachment'
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcServerContextAttachment.java`
#### Snippet
```java

    @Override
    public RpcContextAttachment remove(String key) {
        return removeAttachment(key);
    }
```

### Deprecation
Overrides deprecated method in 'org.apache.dubbo.rpc.RpcContextAttachment'
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcServerContextAttachment.java`
#### Snippet
```java

    @Override
    public RpcContextAttachment set(String key, Object value) {
        return setAttachment(key, value);
    }
```

### Deprecation
Overrides deprecated method in 'org.apache.dubbo.rpc.RpcContextAttachment'
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcServerContextAttachment.java`
#### Snippet
```java

    @Override
    public Map<String, String> getAttachments() {
        return new AttachmentsAdapter.ObjectToStringMap(new ObjectAttachmentMap(this));
    }
```

### Deprecation
'org.apache.dubbo.rpc.model.ConsumerMethodModel' is deprecated
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/AsyncRpcResult.java`
#### Snippet
```java

    private static Result createDefaultValue(Invocation invocation) {
        ConsumerMethodModel method = (ConsumerMethodModel) invocation.get(Constants.METHOD_MODEL);
        return method != null ? new AppResponse(defaultReturn(method.getReturnClass())) : new AppResponse();
    }
```

### Deprecation
'org.apache.dubbo.rpc.model.ConsumerMethodModel' is deprecated
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/AsyncRpcResult.java`
#### Snippet
```java

    private static Result createDefaultValue(Invocation invocation) {
        ConsumerMethodModel method = (ConsumerMethodModel) invocation.get(Constants.METHOD_MODEL);
        return method != null ? new AppResponse(defaultReturn(method.getReturnClass())) : new AppResponse();
    }
```

### Deprecation
Overrides deprecated method in 'org.apache.dubbo.rpc.RpcContext'
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcServiceContext.java`
#### Snippet
```java
    @Override
    @Deprecated
    public boolean isClientSide() {
        return isConsumerSide();
    }
```

### Deprecation
Overrides deprecated method in 'org.apache.dubbo.rpc.RpcContext'
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcServiceContext.java`
#### Snippet
```java
    @Override
    @Deprecated
    public boolean isServerSide() {
        return isProviderSide();
    }
```

### Deprecation
Overrides deprecated method in 'org.apache.dubbo.rpc.RpcContext'
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcServiceContext.java`
#### Snippet
```java
    @Deprecated
    @SuppressWarnings({"unchecked", "rawtypes"})
    public List<Invoker<?>> getInvokers() {
        return invokers == null && invoker != null ? (List) Arrays.asList(invoker) : invokers;
    }
```

### Deprecation
Overrides deprecated method in 'org.apache.dubbo.rpc.RpcContext'
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcServiceContext.java`
#### Snippet
```java
    @Override
    @Deprecated
    public Invoker<?> getInvoker() {
        return invoker;
    }
```

### Deprecation
Overrides deprecated method in 'org.apache.dubbo.rpc.RpcContext'
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcServiceContext.java`
#### Snippet
```java
    @Override
    @Deprecated
    public Invocation getInvocation() {
        return invocation;
    }
```

### Deprecation
'com.alibaba.dubbo.rpc.service.GenericService' is deprecated
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/proxy/AbstractProxyFactory.java`
#### Snippet
```java

            if (GenericService.class.equals(invoker.getInterface()) || !GenericService.class.isAssignableFrom(invoker.getInterface())) {
                interfaces.add(com.alibaba.dubbo.rpc.service.GenericService.class);
            }
        }
```

### Deprecation
'com.alibaba.dubbo.rpc.service.GenericException' is deprecated
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/filter/GenericFilter.java`
#### Snippet
```java
                if (appException instanceof GenericException) {
                    GenericException tmp = (GenericException) appException;
                    appException = new com.alibaba.dubbo.rpc.service.GenericException(tmp.getExceptionClass(), tmp.getExceptionMessage());
                }
                if (!(appException instanceof com.alibaba.dubbo.rpc.service.GenericException)) {
```

### Deprecation
'com.alibaba.dubbo.rpc.service.GenericException' is deprecated
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/filter/GenericFilter.java`
#### Snippet
```java
                    appException = new com.alibaba.dubbo.rpc.service.GenericException(tmp.getExceptionClass(), tmp.getExceptionMessage());
                }
                if (!(appException instanceof com.alibaba.dubbo.rpc.service.GenericException)) {
                    appException = new com.alibaba.dubbo.rpc.service.GenericException(appException);
                }
```

### Deprecation
'com.alibaba.dubbo.rpc.service.GenericException' is deprecated
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/filter/GenericFilter.java`
#### Snippet
```java
                }
                if (!(appException instanceof com.alibaba.dubbo.rpc.service.GenericException)) {
                    appException = new com.alibaba.dubbo.rpc.service.GenericException(appException);
                }
                appResponse.setException(appException);
```

### Deprecation
'findMethodByMethodSignature(java.lang.Class, java.lang.String, java.lang.String\[\])' is deprecated
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/filter/GenericFilter.java`
#### Snippet
```java
            Object[] args = (Object[]) inv.getArguments()[2];
            try {
                Method method = ReflectUtils.findMethodByMethodSignature(invoker.getInterface(), name, types);
                Class<?>[] params = method.getParameterTypes();
                if (args == null) {
```

### Deprecation
'com.alibaba.dubbo.rpc.service.GenericException' is deprecated
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/filter/GenericImplFilter.java`
#### Snippet
```java
                    throw new RpcException(e.getMessage(), e);
                }
            } else if (appResponse.getException() instanceof com.alibaba.dubbo.rpc.service.GenericException) {
                com.alibaba.dubbo.rpc.service.GenericException exception = (com.alibaba.dubbo.rpc.service.GenericException) appResponse.getException();
                try {
```

### Deprecation
'com.alibaba.dubbo.rpc.service.GenericException' is deprecated
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/filter/GenericImplFilter.java`
#### Snippet
```java
                }
            } else if (appResponse.getException() instanceof com.alibaba.dubbo.rpc.service.GenericException) {
                com.alibaba.dubbo.rpc.service.GenericException exception = (com.alibaba.dubbo.rpc.service.GenericException) appResponse.getException();
                try {
                    String className = exception.getExceptionClass();
```

### Deprecation
'com.alibaba.dubbo.rpc.service.GenericException' is deprecated
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/filter/GenericImplFilter.java`
#### Snippet
```java
                }
            } else if (appResponse.getException() instanceof com.alibaba.dubbo.rpc.service.GenericException) {
                com.alibaba.dubbo.rpc.service.GenericException exception = (com.alibaba.dubbo.rpc.service.GenericException) appResponse.getException();
                try {
                    String className = exception.getExceptionClass();
```

### Deprecation
'org.apache.dubbo.common.Parameters' is deprecated
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/protocol/AbstractProxyProtocol.java`
#### Snippet
```java

        @Override
        public void reset(Parameters parameters) {

        }
```

### Deprecation
'com.alibaba.dubbo.rpc.Invoker' is deprecated
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/RpcInvocation.java`
#### Snippet
```java
    private Map<String, String> attachments;

    private transient Invoker<?> invoker;

    public RpcInvocation() {
```

### Deprecation
'com.alibaba.dubbo.rpc.Invocation' is deprecated
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/RpcInvocation.java`
#### Snippet
```java
import com.alibaba.dubbo.common.URL;

public class RpcInvocation implements Invocation, Serializable {

    private static final long serialVersionUID = -4355285085441097045L;
```

### Deprecation
'com.alibaba.dubbo.rpc.Invoker' is deprecated
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/RpcInvocation.java`
#### Snippet
```java
    }

    public void setInvoker(Invoker<?> invoker) {
        this.invoker = invoker;
    }
```

### Deprecation
'com.alibaba.dubbo.rpc.Invoker' is deprecated
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/RpcInvocation.java`
#### Snippet
```java
    }

    public Invoker<?> getInvoker() {
        return invoker;
    }
```

### Deprecation
'com.alibaba.dubbo.rpc.Invocation' is deprecated
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/RpcInvocation.java`
#### Snippet
```java
    }

    public RpcInvocation(Invocation invocation, Invoker<?> invoker) {
        this(invocation.getMethodName(), invocation.getParameterTypes(),
                invocation.getArguments(), new HashMap<String, String>(invocation.getAttachments()),
```

### Deprecation
'com.alibaba.dubbo.rpc.Invoker' is deprecated
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/RpcInvocation.java`
#### Snippet
```java
    }

    public RpcInvocation(Invocation invocation, Invoker<?> invoker) {
        this(invocation.getMethodName(), invocation.getParameterTypes(),
                invocation.getArguments(), new HashMap<String, String>(invocation.getAttachments()),
```

### Deprecation
'com.alibaba.dubbo.common.URL' is deprecated
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/RpcInvocation.java`
#### Snippet
```java
                invocation.getInvoker());
        if (invoker != null) {
            URL url = invoker.getUrl();
            setAttachment(Constants.PATH_KEY, url.getPath());
            if (url.hasParameter(Constants.INTERFACE_KEY)) {
```

### Deprecation
'com.alibaba.dubbo.common.Constants' is deprecated
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/RpcInvocation.java`
#### Snippet
```java
        if (invoker != null) {
            URL url = invoker.getUrl();
            setAttachment(Constants.PATH_KEY, url.getPath());
            if (url.hasParameter(Constants.INTERFACE_KEY)) {
                setAttachment(Constants.INTERFACE_KEY, url.getParameter(Constants.INTERFACE_KEY));
```

### Deprecation
'com.alibaba.dubbo.common.Constants' is deprecated
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/RpcInvocation.java`
#### Snippet
```java
            URL url = invoker.getUrl();
            setAttachment(Constants.PATH_KEY, url.getPath());
            if (url.hasParameter(Constants.INTERFACE_KEY)) {
                setAttachment(Constants.INTERFACE_KEY, url.getParameter(Constants.INTERFACE_KEY));
            }
```

### Deprecation
'com.alibaba.dubbo.common.Constants' is deprecated
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/RpcInvocation.java`
#### Snippet
```java
            setAttachment(Constants.PATH_KEY, url.getPath());
            if (url.hasParameter(Constants.INTERFACE_KEY)) {
                setAttachment(Constants.INTERFACE_KEY, url.getParameter(Constants.INTERFACE_KEY));
            }
            if (url.hasParameter(Constants.GROUP_KEY)) {
```

### Deprecation
'com.alibaba.dubbo.common.Constants' is deprecated
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/RpcInvocation.java`
#### Snippet
```java
            setAttachment(Constants.PATH_KEY, url.getPath());
            if (url.hasParameter(Constants.INTERFACE_KEY)) {
                setAttachment(Constants.INTERFACE_KEY, url.getParameter(Constants.INTERFACE_KEY));
            }
            if (url.hasParameter(Constants.GROUP_KEY)) {
```

### Deprecation
'com.alibaba.dubbo.common.Constants' is deprecated
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/RpcInvocation.java`
#### Snippet
```java
                setAttachment(Constants.INTERFACE_KEY, url.getParameter(Constants.INTERFACE_KEY));
            }
            if (url.hasParameter(Constants.GROUP_KEY)) {
                setAttachment(Constants.GROUP_KEY, url.getParameter(Constants.GROUP_KEY));
            }
```

### Deprecation
'com.alibaba.dubbo.common.Constants' is deprecated
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/RpcInvocation.java`
#### Snippet
```java
            }
            if (url.hasParameter(Constants.GROUP_KEY)) {
                setAttachment(Constants.GROUP_KEY, url.getParameter(Constants.GROUP_KEY));
            }
            if (url.hasParameter(Constants.VERSION_KEY)) {
```

### Deprecation
'com.alibaba.dubbo.common.Constants' is deprecated
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/RpcInvocation.java`
#### Snippet
```java
            }
            if (url.hasParameter(Constants.GROUP_KEY)) {
                setAttachment(Constants.GROUP_KEY, url.getParameter(Constants.GROUP_KEY));
            }
            if (url.hasParameter(Constants.VERSION_KEY)) {
```

### Deprecation
'com.alibaba.dubbo.common.Constants' is deprecated
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/RpcInvocation.java`
#### Snippet
```java
                setAttachment(Constants.GROUP_KEY, url.getParameter(Constants.GROUP_KEY));
            }
            if (url.hasParameter(Constants.VERSION_KEY)) {
                setAttachment(Constants.VERSION_KEY, url.getParameter(Constants.VERSION_KEY, "0.0.0"));
            }
```

### Deprecation
'com.alibaba.dubbo.common.Constants' is deprecated
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/RpcInvocation.java`
#### Snippet
```java
            }
            if (url.hasParameter(Constants.VERSION_KEY)) {
                setAttachment(Constants.VERSION_KEY, url.getParameter(Constants.VERSION_KEY, "0.0.0"));
            }
            if (url.hasParameter(Constants.TIMEOUT_KEY)) {
```

### Deprecation
'com.alibaba.dubbo.common.Constants' is deprecated
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/RpcInvocation.java`
#### Snippet
```java
            }
            if (url.hasParameter(Constants.VERSION_KEY)) {
                setAttachment(Constants.VERSION_KEY, url.getParameter(Constants.VERSION_KEY, "0.0.0"));
            }
            if (url.hasParameter(Constants.TIMEOUT_KEY)) {
```

### Deprecation
'com.alibaba.dubbo.common.Constants' is deprecated
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/RpcInvocation.java`
#### Snippet
```java
                setAttachment(Constants.VERSION_KEY, url.getParameter(Constants.VERSION_KEY, "0.0.0"));
            }
            if (url.hasParameter(Constants.TIMEOUT_KEY)) {
                setAttachment(Constants.TIMEOUT_KEY, url.getParameter(Constants.TIMEOUT_KEY));
            }
```

### Deprecation
'com.alibaba.dubbo.common.Constants' is deprecated
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/RpcInvocation.java`
#### Snippet
```java
            }
            if (url.hasParameter(Constants.TIMEOUT_KEY)) {
                setAttachment(Constants.TIMEOUT_KEY, url.getParameter(Constants.TIMEOUT_KEY));
            }
            if (url.hasParameter(Constants.TOKEN_KEY)) {
```

### Deprecation
'com.alibaba.dubbo.common.Constants' is deprecated
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/RpcInvocation.java`
#### Snippet
```java
            }
            if (url.hasParameter(Constants.TIMEOUT_KEY)) {
                setAttachment(Constants.TIMEOUT_KEY, url.getParameter(Constants.TIMEOUT_KEY));
            }
            if (url.hasParameter(Constants.TOKEN_KEY)) {
```

### Deprecation
'com.alibaba.dubbo.common.Constants' is deprecated
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/RpcInvocation.java`
#### Snippet
```java
                setAttachment(Constants.TIMEOUT_KEY, url.getParameter(Constants.TIMEOUT_KEY));
            }
            if (url.hasParameter(Constants.TOKEN_KEY)) {
                setAttachment(Constants.TOKEN_KEY, url.getParameter(Constants.TOKEN_KEY));
            }
```

### Deprecation
'com.alibaba.dubbo.common.Constants' is deprecated
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/RpcInvocation.java`
#### Snippet
```java
            }
            if (url.hasParameter(Constants.TOKEN_KEY)) {
                setAttachment(Constants.TOKEN_KEY, url.getParameter(Constants.TOKEN_KEY));
            }
            if (url.hasParameter(Constants.APPLICATION_KEY)) {
```

### Deprecation
'com.alibaba.dubbo.common.Constants' is deprecated
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/RpcInvocation.java`
#### Snippet
```java
            }
            if (url.hasParameter(Constants.TOKEN_KEY)) {
                setAttachment(Constants.TOKEN_KEY, url.getParameter(Constants.TOKEN_KEY));
            }
            if (url.hasParameter(Constants.APPLICATION_KEY)) {
```

### Deprecation
'com.alibaba.dubbo.common.Constants' is deprecated
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/RpcInvocation.java`
#### Snippet
```java
                setAttachment(Constants.TOKEN_KEY, url.getParameter(Constants.TOKEN_KEY));
            }
            if (url.hasParameter(Constants.APPLICATION_KEY)) {
                setAttachment(Constants.APPLICATION_KEY, url.getParameter(Constants.APPLICATION_KEY));
            }
```

### Deprecation
'com.alibaba.dubbo.common.Constants' is deprecated
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/RpcInvocation.java`
#### Snippet
```java
            }
            if (url.hasParameter(Constants.APPLICATION_KEY)) {
                setAttachment(Constants.APPLICATION_KEY, url.getParameter(Constants.APPLICATION_KEY));
            }
        }
```

### Deprecation
'com.alibaba.dubbo.common.Constants' is deprecated
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/RpcInvocation.java`
#### Snippet
```java
            }
            if (url.hasParameter(Constants.APPLICATION_KEY)) {
                setAttachment(Constants.APPLICATION_KEY, url.getParameter(Constants.APPLICATION_KEY));
            }
        }
```

### Deprecation
'com.alibaba.dubbo.rpc.Invocation' is deprecated
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/RpcInvocation.java`
#### Snippet
```java
    }

    public RpcInvocation(Invocation invocation) {
        this(invocation.getMethodName(), invocation.getParameterTypes(),
                invocation.getArguments(), invocation.getAttachments(), invocation.getInvoker());
```

### Deprecation
'com.alibaba.dubbo.rpc.Invoker' is deprecated
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/RpcInvocation.java`
#### Snippet
```java
    }

    public RpcInvocation(String methodName, Class<?>[] parameterTypes, Object[] arguments, Map<String, String> attachments, Invoker<?> invoker) {
        this.methodName = methodName;
        this.parameterTypes = parameterTypes == null ? new Class<?>[0] : parameterTypes;
```

### Deprecation
'com.alibaba.dubbo.rpc.Invocation' is deprecated
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/support/RpcUtils.java`
#### Snippet
```java

    // TODO why not get return type when initialize Invocation?
    public static Type[] getReturnTypes(Invocation invocation) {
        return org.apache.dubbo.rpc.support.RpcUtils.getReturnTypes(invocation);
    }
```

### Deprecation
'com.alibaba.dubbo.common.URL' is deprecated
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/support/RpcUtils.java`
#### Snippet
```java
    }

    public static boolean isOneway(URL url, Invocation inv) {
        return org.apache.dubbo.rpc.support.RpcUtils.isOneway(url.getOriginalURL(), inv);
    }
```

### Deprecation
'com.alibaba.dubbo.rpc.Invocation' is deprecated
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/support/RpcUtils.java`
#### Snippet
```java
    }

    public static boolean isOneway(URL url, Invocation inv) {
        return org.apache.dubbo.rpc.support.RpcUtils.isOneway(url.getOriginalURL(), inv);
    }
```

### Deprecation
'com.alibaba.dubbo.rpc.Invocation' is deprecated
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/support/RpcUtils.java`
#### Snippet
```java
    }

    public static Object[] getArguments(Invocation invocation) {
        return org.apache.dubbo.rpc.support.RpcUtils.getArguments(invocation);
    }
```

### Deprecation
'com.alibaba.dubbo.common.URL' is deprecated
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/support/RpcUtils.java`
#### Snippet
```java
     * @param inv
     */
    public static void attachInvocationIdIfAsync(URL url, Invocation inv) {
        org.apache.dubbo.rpc.support.RpcUtils.attachInvocationIdIfAsync(url.getOriginalURL(), inv);
    }
```

### Deprecation
'com.alibaba.dubbo.rpc.Invocation' is deprecated
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/support/RpcUtils.java`
#### Snippet
```java
     * @param inv
     */
    public static void attachInvocationIdIfAsync(URL url, Invocation inv) {
        org.apache.dubbo.rpc.support.RpcUtils.attachInvocationIdIfAsync(url.getOriginalURL(), inv);
    }
```

### Deprecation
'com.alibaba.dubbo.rpc.Invocation' is deprecated
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/support/RpcUtils.java`
#### Snippet
```java


    public static Class<?> getReturnType(Invocation invocation) {
        return org.apache.dubbo.rpc.support.RpcUtils.getReturnType(invocation);
    }
```

### Deprecation
'com.alibaba.dubbo.rpc.Invocation' is deprecated
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/support/RpcUtils.java`
#### Snippet
```java
    }

    public static Long getInvocationId(Invocation inv) {
        return org.apache.dubbo.rpc.support.RpcUtils.getInvocationId(inv);
    }
```

### Deprecation
'com.alibaba.dubbo.rpc.Invocation' is deprecated
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/support/RpcUtils.java`
#### Snippet
```java


    public static String getMethodName(Invocation invocation) {
        return org.apache.dubbo.rpc.support.RpcUtils.getMethodName(invocation);
    }
```

### Deprecation
'com.alibaba.dubbo.rpc.Invocation' is deprecated
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/support/RpcUtils.java`
#### Snippet
```java
    }

    public static Class<?>[] getParameterTypes(Invocation invocation) {
        return org.apache.dubbo.rpc.support.RpcUtils.getParameterTypes(invocation);
    }
```

### Deprecation
'com.alibaba.dubbo.rpc.Invocation' is deprecated
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/support/RpcUtils.java`
#### Snippet
```java
    }

    public static boolean isReturnTypeFuture(Invocation inv) {
        return org.apache.dubbo.rpc.support.RpcUtils.isReturnTypeFuture(inv);
    }
```

### Deprecation
'com.alibaba.dubbo.common.URL' is deprecated
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/support/RpcUtils.java`
#### Snippet
```java
    }

    public static boolean isAsync(URL url, Invocation inv) {
        return org.apache.dubbo.rpc.support.RpcUtils.isAsync(url.getOriginalURL(), inv);
    }
```

### Deprecation
'com.alibaba.dubbo.rpc.Invocation' is deprecated
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/support/RpcUtils.java`
#### Snippet
```java
    }

    public static boolean isAsync(URL url, Invocation inv) {
        return org.apache.dubbo.rpc.support.RpcUtils.isAsync(url.getOriginalURL(), inv);
    }
```

### Deprecation
Overrides deprecated method in 'org.apache.dubbo.rpc.cluster.Router'
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/cluster/Router.java`
#### Snippet
```java
    // Add since 2.7.0
    @Override
    default <T> List<Invoker<T>> route(List<Invoker<T>> invokers, URL url, Invocation invocation) throws RpcException {
        List<com.alibaba.dubbo.rpc.Invoker<T>> invs = invokers.stream().map(invoker -> new com.alibaba.dubbo.rpc.Invoker.CompatibleInvoker<T>(invoker)).
                collect(Collectors.toList());
```

### Deprecation
Overrides deprecated method in 'org.apache.dubbo.config.ProviderConfig'
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/config/ProviderConfig.java`
#### Snippet
```java

    @Override
    public void setProtocol(String protocol) {
        setProtocol(new com.alibaba.dubbo.config.ProtocolConfig(protocol));
    }
```

### Deprecation
'com.alibaba.dubbo.common.URL' is deprecated
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/container/page/PageHandler.java`
#### Snippet
```java
     * @return the page.
     */
    Page handle(URL url);

}
```

### Deprecation
Overrides deprecated method in 'org.apache.dubbo.common.URL'
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/common/DelegateURL.java`
#### Snippet
```java
    @Override
    @Deprecated
    public boolean getBooleanParameter(String key) {
        return apacheUrl.getBooleanParameter(key);
    }
```

### Deprecation
Overrides deprecated method in 'org.apache.dubbo.common.URL'
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/common/DelegateURL.java`
#### Snippet
```java
    @Override
    @Deprecated
    public boolean getBooleanParameter(String key, boolean defaultValue) {
        return apacheUrl.getBooleanParameter(key, defaultValue);
    }
```

### Deprecation
Overrides deprecated method in 'org.apache.dubbo.common.URL'
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/common/DelegateURL.java`
#### Snippet
```java
    @Override
    @Deprecated
    public boolean getMethodBooleanParameter(String method, String key) {
        return apacheUrl.getMethodBooleanParameter(method, key);
    }
```

### Deprecation
Overrides deprecated method in 'org.apache.dubbo.common.URL'
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/common/DelegateURL.java`
#### Snippet
```java
    @Override
    @Deprecated
    public int getIntParameter(String key, int defaultValue) {
        return apacheUrl.getIntParameter(key, defaultValue);
    }
```

### Deprecation
Overrides deprecated method in 'org.apache.dubbo.common.URL'
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/common/DelegateURL.java`
#### Snippet
```java
    @Override
    @Deprecated
    public int getIntParameter(String key) {
        return apacheUrl.getIntParameter(key);
    }
```

### Deprecation
Overrides deprecated method in 'org.apache.dubbo.common.URL'
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/common/DelegateURL.java`
#### Snippet
```java
    @Override
    @Deprecated
    public int getMethodPositiveIntParameter(String method, String key, int defaultValue) {
        return apacheUrl.getMethodPositiveIntParameter(method, key, defaultValue);
    }
```

### Deprecation
Overrides deprecated method in 'org.apache.dubbo.common.URL'
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/common/DelegateURL.java`
#### Snippet
```java
    @Override
    @Deprecated
    public boolean getMethodBooleanParameter(String method, String key, boolean defaultValue) {
        return apacheUrl.getMethodBooleanParameter(method, key, defaultValue);
    }
```

### Deprecation
Overrides deprecated method in 'org.apache.dubbo.common.URL'
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/common/DelegateURL.java`
#### Snippet
```java
    @Override
    @Deprecated
    public int getPositiveIntParameter(String key, int defaultValue) {
        return apacheUrl.getPositiveIntParameter(key, defaultValue);
    }
```

### Deprecation
Overrides deprecated method in 'org.apache.dubbo.common.URL'
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/common/DelegateURL.java`
#### Snippet
```java
    @Override
    @Deprecated
    public int getMethodIntParameter(String method, String key) {
        return apacheUrl.getMethodIntParameter(method, key);
    }
```

### Deprecation
Overrides deprecated method in 'org.apache.dubbo.common.URL'
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/common/DelegateURL.java`
#### Snippet
```java
    @Override
    @Deprecated
    public String getServiceName() {
        return apacheUrl.getServiceName();
    }
```

### Deprecation
Overrides deprecated method in 'org.apache.dubbo.common.URL'
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/common/DelegateURL.java`
#### Snippet
```java
    @Override
    @Deprecated
    public int getMethodIntParameter(String method, String key, int defaultValue) {
        return apacheUrl.getMethodIntParameter(method, key, defaultValue);
    }
```

### Deprecation
'com.alibaba.dubbo.common.URL' is deprecated
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/container/page/pages/HomePageHandler.java`
#### Snippet
```java

    @Override
    public Page handle(URL url) {
        List<List<String>> rows = new ArrayList<List<String>>();
        for (PageHandler handler : PageServlet.getInstance().getMenus()) {
```

### Deprecation
'getExtensionLoader(java.lang.Class)' is deprecated
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/container/page/pages/HomePageHandler.java`
#### Snippet
```java
        List<List<String>> rows = new ArrayList<List<String>>();
        for (PageHandler handler : PageServlet.getInstance().getMenus()) {
            String uri = ExtensionLoader.getExtensionLoader(PageHandler.class).getExtensionName(handler);
            Menu menu = handler.getClass().getAnnotation(Menu.class);
            List<String> row = new ArrayList<String>();
```

### Deprecation
'com.alibaba.dubbo.common.URL' is deprecated
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/container/page/pages/StatusPageHandler.java`
#### Snippet
```java

    @Override
    public Page handle(URL url) {
        List<List<String>> rows = new ArrayList<List<String>>();
        Set<String> names = ExtensionLoader.getExtensionLoader(StatusChecker.class).getSupportedExtensions();
```

### Deprecation
'getExtensionLoader(java.lang.Class)' is deprecated
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/container/page/pages/StatusPageHandler.java`
#### Snippet
```java
    public Page handle(URL url) {
        List<List<String>> rows = new ArrayList<List<String>>();
        Set<String> names = ExtensionLoader.getExtensionLoader(StatusChecker.class).getSupportedExtensions();
        Map<String, Status> statuses = new HashMap<String, Status>();
        for (String name : names) {
```

### Deprecation
'com.alibaba.dubbo.common.status.StatusChecker' is deprecated
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/container/page/pages/StatusPageHandler.java`
#### Snippet
```java
    public Page handle(URL url) {
        List<List<String>> rows = new ArrayList<List<String>>();
        Set<String> names = ExtensionLoader.getExtensionLoader(StatusChecker.class).getSupportedExtensions();
        Map<String, Status> statuses = new HashMap<String, Status>();
        for (String name : names) {
```

### Deprecation
'com.alibaba.dubbo.common.status.StatusChecker' is deprecated
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/container/page/pages/StatusPageHandler.java`
#### Snippet
```java
        Map<String, Status> statuses = new HashMap<String, Status>();
        for (String name : names) {
            StatusChecker checker = ExtensionLoader.getExtensionLoader(StatusChecker.class).getExtension(name);
            List<String> row = new ArrayList<String>();
            row.add(name);
```

### Deprecation
'getExtensionLoader(java.lang.Class)' is deprecated
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/container/page/pages/StatusPageHandler.java`
#### Snippet
```java
        Map<String, Status> statuses = new HashMap<String, Status>();
        for (String name : names) {
            StatusChecker checker = ExtensionLoader.getExtensionLoader(StatusChecker.class).getExtension(name);
            List<String> row = new ArrayList<String>();
            row.add(name);
```

### Deprecation
'com.alibaba.dubbo.common.status.StatusChecker' is deprecated
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/container/page/pages/StatusPageHandler.java`
#### Snippet
```java
        Map<String, Status> statuses = new HashMap<String, Status>();
        for (String name : names) {
            StatusChecker checker = ExtensionLoader.getExtensionLoader(StatusChecker.class).getExtension(name);
            List<String> row = new ArrayList<String>();
            row.add(name);
```

### Deprecation
'com.alibaba.dubbo.common.URL' is deprecated
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/container/page/pages/LogPageHandler.java`
#### Snippet
```java

    @Override
    public Page handle(URL url) {
        long size = 0;
        String content = "";
```

### Deprecation
'com.alibaba.dubbo.common.URL' is deprecated
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/container/page/pages/SystemPageHandler.java`
#### Snippet
```java

    @Override
    public Page handle(URL url) {
        List<List<String>> rows = new ArrayList<List<String>>();
        List<String> row;
```

### Deprecation
'com.alibaba.dubbo.common.Constants' is deprecated
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/container/page/ResourceFilter.java`
#### Snippet
```java
        String config = filterConfig.getInitParameter("resources");
        if (config != null && config.length() > 0) {
            String[] configs = Constants.COMMA_SPLIT_PATTERN.split(config);
            for (String c : configs) {
                if (c != null && c.length() > 0) {
```

### Deprecation
'getExtensionLoader(java.lang.Class)' is deprecated
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/container/page/PageServlet.java`
#### Snippet
```java
        writer.println("    <tr>");
        for (PageHandler handler : menus) {
            String uri = ExtensionLoader.getExtensionLoader(PageHandler.class).getExtensionName(handler);
            Menu menu = handler.getClass().getAnnotation(Menu.class);
            writer.println("        <th><a href=\"" + uri + ".html\">" + menu.name() + "</a></th>");
```

### Deprecation
'getExtensionLoader(java.lang.Class)' is deprecated
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/container/page/PageServlet.java`
#### Snippet
```java
                return;
            }
            ExtensionLoader<PageHandler> pageHandlerLoader = ExtensionLoader.getExtensionLoader(PageHandler.class);
            PageHandler pageHandler = pageHandlerLoader.hasExtension(uri) ? pageHandlerLoader.getExtension(uri) : null;
            if (isHtml) {
```

### Deprecation
'com.alibaba.dubbo.common.URL' is deprecated
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/container/page/PageServlet.java`
#### Snippet
```java
                try {
                    String query = request.getQueryString();
                    page = pageHandler.handle(URL.valueOf(request.getRequestURL().toString()
                            + (query == null || query.length() == 0 ? "" : "?" + query)));
                } catch (Throwable t) {
```

### Deprecation
'getExtensionLoader(java.lang.Class)' is deprecated
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/container/page/PageServlet.java`
#### Snippet
```java
                        String nav = page.getNavigation();
                        if (nav == null || nav.length() == 0) {
                            nav = ExtensionLoader.getExtensionLoader(PageHandler.class).getExtensionName(pageHandler);
                            nav = nav.substring(0, 1).toUpperCase() + nav.substring(1);
                        }
```

### Deprecation
'com.alibaba.dubbo.common.Constants' is deprecated
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/container/page/PageServlet.java`
#### Snippet
```java
        Collection<String> names;
        if (config != null && config.length() > 0) {
            names = Arrays.asList(Constants.COMMA_SPLIT_PATTERN.split(config));
        } else {
            names = ExtensionLoader.getExtensionLoader(PageHandler.class).getSupportedExtensions();
```

### Deprecation
'getExtensionLoader(java.lang.Class)' is deprecated
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/container/page/PageServlet.java`
#### Snippet
```java
            names = Arrays.asList(Constants.COMMA_SPLIT_PATTERN.split(config));
        } else {
            names = ExtensionLoader.getExtensionLoader(PageHandler.class).getSupportedExtensions();
        }
        for (String name : names) {
```

### Deprecation
'getExtensionLoader(java.lang.Class)' is deprecated
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/container/page/PageServlet.java`
#### Snippet
```java
        }
        for (String name : names) {
            PageHandler handler = ExtensionLoader.getExtensionLoader(PageHandler.class).getExtension(name);
            pages.put(ExtensionLoader.getExtensionLoader(PageHandler.class).getExtensionName(handler), handler);
            Menu menu = handler.getClass().getAnnotation(Menu.class);
```

### Deprecation
'getExtensionLoader(java.lang.Class)' is deprecated
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/container/page/PageServlet.java`
#### Snippet
```java
        for (String name : names) {
            PageHandler handler = ExtensionLoader.getExtensionLoader(PageHandler.class).getExtension(name);
            pages.put(ExtensionLoader.getExtensionLoader(PageHandler.class).getExtensionName(handler), handler);
            Menu menu = handler.getClass().getAnnotation(Menu.class);
            if (menu != null) {
```

### Deprecation
'org.apache.dubbo.remoting.Codec' is deprecated
in `dubbo-rpc/dubbo-rpc-dubbo/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/DecodeableRpcResult.java`
#### Snippet
```java
import static org.apache.dubbo.rpc.Constants.SERIALIZATION_SECURITY_CHECK_KEY;

public class DecodeableRpcResult extends AppResponse implements Codec, Decodeable {

    private static final ErrorTypeAwareLogger log = LoggerFactory.getErrorTypeAwareLogger(DecodeableRpcResult.class);
```

### Deprecation
'RpcInvocation()' is deprecated
in `dubbo-rpc/dubbo-rpc-dubbo/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/DecodeableRpcInvocation.java`
#### Snippet
```java
    private static final boolean CHECK_SERIALIZATION = Boolean.parseBoolean(System.getProperty(SERIALIZATION_SECURITY_CHECK_KEY, "true"));

    public DecodeableRpcInvocation(FrameworkModel frameworkModel, Channel channel, Request request, InputStream is, byte id) {
        this.frameworkModel = frameworkModel;
        Assert.notNull(channel, "channel == null");
```

### Deprecation
'getAttachment(java.lang.String)' is deprecated
in `dubbo-rpc/dubbo-rpc-dubbo/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/DecodeableRpcInvocation.java`
#### Snippet
```java

        setArguments(args);
        String targetServiceName = buildKey(getAttachment(PATH_KEY),
            getAttachment(GROUP_KEY),
            getAttachment(VERSION_KEY));
```

### Deprecation
'getAttachment(java.lang.String)' is deprecated
in `dubbo-rpc/dubbo-rpc-dubbo/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/DecodeableRpcInvocation.java`
#### Snippet
```java
        setArguments(args);
        String targetServiceName = buildKey(getAttachment(PATH_KEY),
            getAttachment(GROUP_KEY),
            getAttachment(VERSION_KEY));
        setTargetServiceUniqueName(targetServiceName);
```

### Deprecation
'getAttachment(java.lang.String)' is deprecated
in `dubbo-rpc/dubbo-rpc-dubbo/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/DecodeableRpcInvocation.java`
#### Snippet
```java
        String targetServiceName = buildKey(getAttachment(PATH_KEY),
            getAttachment(GROUP_KEY),
            getAttachment(VERSION_KEY));
        setTargetServiceUniqueName(targetServiceName);
    }
```

### Deprecation
'org.apache.dubbo.remoting.Codec' is deprecated
in `dubbo-rpc/dubbo-rpc-dubbo/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/DecodeableRpcInvocation.java`
#### Snippet
```java
import static org.apache.dubbo.rpc.Constants.SERIALIZATION_SECURITY_CHECK_KEY;

public class DecodeableRpcInvocation extends RpcInvocation implements Codec, Decodeable {

    protected static final ErrorTypeAwareLogger log = LoggerFactory.getErrorTypeAwareLogger(DecodeableRpcInvocation.class);
```

### Deprecation
'getAttachment(java.lang.String)' is deprecated
in `dubbo-rpc/dubbo-rpc-dubbo/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/CallbackServiceCodec.java`
#### Snippet
```java
            case CallbackServiceCodec.CALLBACK_CREATE:
                try {
                    return referOrDestroyCallbackService(channel, url, pts[paraIndex], inv, Integer.parseInt(inv.getAttachment(INV_ATT_CALLBACK_KEY + paraIndex)), true);
                } catch (Exception e) {
                    logger.error(PROTOCOL_FAILED_DESTROY_INVOKER, "", "", e.getMessage(), e);
```

### Deprecation
'getAttachment(java.lang.String)' is deprecated
in `dubbo-rpc/dubbo-rpc-dubbo/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/CallbackServiceCodec.java`
#### Snippet
```java
            case CallbackServiceCodec.CALLBACK_DESTROY:
                try {
                    return referOrDestroyCallbackService(channel, url, pts[paraIndex], inv, Integer.parseInt(inv.getAttachment(INV_ATT_CALLBACK_KEY + paraIndex)), false);
                } catch (Exception e) {
                    throw new IOException(StringUtils.toString(e));
```

### Deprecation
'getAttachment(java.lang.String)' is deprecated
in `dubbo-rpc/dubbo-rpc-dubbo/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/DubboCodec.java`
#### Snippet
```java
        out.writeUTF(version);
        // https://github.com/apache/dubbo/issues/6138
        String serviceName = inv.getAttachment(INTERFACE_KEY);
        if (serviceName == null) {
            serviceName = inv.getAttachment(PATH_KEY);
```

### Deprecation
'getAttachment(java.lang.String)' is deprecated
in `dubbo-rpc/dubbo-rpc-dubbo/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/DubboCodec.java`
#### Snippet
```java
        String serviceName = inv.getAttachment(INTERFACE_KEY);
        if (serviceName == null) {
            serviceName = inv.getAttachment(PATH_KEY);
        }
        out.writeUTF(serviceName);
```

### Deprecation
'getAttachment(java.lang.String)' is deprecated
in `dubbo-rpc/dubbo-rpc-dubbo/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/DubboCodec.java`
#### Snippet
```java
        }
        out.writeUTF(serviceName);
        out.writeUTF(inv.getAttachment(VERSION_KEY));

        out.writeUTF(inv.getMethodName());
```

### Deprecation
'getDubboProtocol()' is deprecated
in `dubbo-rpc/dubbo-rpc-dubbo/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/status/ServerStatusChecker.java`
#### Snippet
```java
    @Override
    public Status check() {
        List<ProtocolServer> servers = DubboProtocol.getDubboProtocol().getServers();
        if (servers == null || servers.isEmpty()) {
            return new Status(Status.Level.UNKNOWN);
```

### Deprecation
'setCompatibleFuture(java.util.concurrent.CompletableFuture)' is deprecated
in `dubbo-rpc/dubbo-rpc-dubbo/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/DubboInvoker.java`
#### Snippet
```java
                    currentClient.request(request, timeout, executor).thenApply(AppResponse.class::cast);
                // save for 2.6.x compatibility, for example, TraceFilter in Zipkin uses com.alibaba.xxx.FutureAdapter
                FutureContext.getContext().setCompatibleFuture(appResponseFuture);
                AsyncRpcResult result = new AsyncRpcResult(appResponseFuture, inv);
                result.setExecutor(executor);
```

### Deprecation
'setCompatibleFuture(java.util.concurrent.CompletableFuture)' is deprecated
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/TripleInvoker.java`
#### Snippet
```java
        }
        result = new AsyncRpcResult(future, invocation);
        FutureContext.getContext().setCompatibleFuture(future);

        result.setExecutor(callbackExecutor);
```

### Deprecation
'org.apache.dubbo.rpc.protocol.tri.transport.WriteQueue' is deprecated
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/stream/TripleClientStream.java`
#### Snippet
```java

/**
 * ClientStream is an abstraction for bi-directional messaging. It maintains a {@link WriteQueue} to
 * write Http2Frame to remote. A {@link H2TransportListener} receives Http2Frame from remote.
 * Instead of maintaining state, this class depends on upper layer or transport layer's states.
```

### Deprecation
'addAttachmentsIfAbsent(java.util.Map)' is deprecated
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/filter/support/ConsumerContextFilter.java`
#### Snippet
```java
            if (CollectionUtils.isNotEmptyMap(contextAttachments)) {
                /**
                 * invocation.addAttachmentsIfAbsent(context){@link RpcInvocation#addAttachmentsIfAbsent(Map)}should not be used here,
                 * because the {@link RpcContext#setAttachment(String, String)} is passed in the Filter when the call is triggered
                 * by the built-in retry mechanism of the Dubbo. The attachment to update RpcContext will no longer work, which is
```

### Deprecation
'org.apache.dubbo.rpc.ListenableFilter' is deprecated
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/filter/FilterChainBuilder.java`
#### Snippet
```java
            } catch (Exception e) {
                InvocationProfilerUtils.releaseDetailProfiler(invocation);
                if (filter instanceof ListenableFilter) {
                    ListenableFilter listenableFilter = ((ListenableFilter) filter);
                    try {
```

### Deprecation
'org.apache.dubbo.rpc.ListenableFilter' is deprecated
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/filter/FilterChainBuilder.java`
#### Snippet
```java
                InvocationProfilerUtils.releaseDetailProfiler(invocation);
                if (filter instanceof ListenableFilter) {
                    ListenableFilter listenableFilter = ((ListenableFilter) filter);
                    try {
                        Filter.Listener listener = listenableFilter.listener(invocation);
```

### Deprecation
'org.apache.dubbo.rpc.ListenableFilter' is deprecated
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/filter/FilterChainBuilder.java`
#### Snippet
```java
                InvocationProfilerUtils.releaseDetailProfiler(invocation);
                if (filter instanceof ListenableFilter) {
                    ListenableFilter listenableFilter = ((ListenableFilter) filter);
                    try {
                        Filter.Listener listener = listenableFilter.listener(invocation);
```

### Deprecation
'org.apache.dubbo.rpc.ListenableFilter' is deprecated
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/filter/FilterChainBuilder.java`
#### Snippet
```java
            } catch (Exception e) {
                InvocationProfilerUtils.releaseDetailProfiler(invocation);
                if (filter instanceof ListenableFilter) {
                    ListenableFilter listenableFilter = ((ListenableFilter) filter);
                    try {
```

### Deprecation
'org.apache.dubbo.rpc.ListenableFilter' is deprecated
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/filter/FilterChainBuilder.java`
#### Snippet
```java
                InvocationProfilerUtils.releaseDetailProfiler(invocation);
                if (filter instanceof ListenableFilter) {
                    ListenableFilter listenableFilter = ((ListenableFilter) filter);
                    try {
                        Filter.Listener listener = listenableFilter.listener(invocation);
```

### Deprecation
'org.apache.dubbo.rpc.ListenableFilter' is deprecated
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/filter/FilterChainBuilder.java`
#### Snippet
```java
                InvocationProfilerUtils.releaseDetailProfiler(invocation);
                if (filter instanceof ListenableFilter) {
                    ListenableFilter listenableFilter = ((ListenableFilter) filter);
                    try {
                        Filter.Listener listener = listenableFilter.listener(invocation);
```

### Deprecation
'org.apache.dubbo.rpc.ListenableFilter' is deprecated
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/filter/FilterChainBuilder.java`
#### Snippet
```java
            return asyncResult.whenCompleteWithContext((r, t) -> {
                InvocationProfilerUtils.releaseDetailProfiler(invocation);
                if (filter instanceof ListenableFilter) {
                    ListenableFilter listenableFilter = ((ListenableFilter) filter);
                    Filter.Listener listener = listenableFilter.listener(invocation);
```

### Deprecation
'org.apache.dubbo.rpc.ListenableFilter' is deprecated
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/filter/FilterChainBuilder.java`
#### Snippet
```java
                InvocationProfilerUtils.releaseDetailProfiler(invocation);
                if (filter instanceof ListenableFilter) {
                    ListenableFilter listenableFilter = ((ListenableFilter) filter);
                    Filter.Listener listener = listenableFilter.listener(invocation);
                    try {
```

### Deprecation
'org.apache.dubbo.rpc.ListenableFilter' is deprecated
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/filter/FilterChainBuilder.java`
#### Snippet
```java
                InvocationProfilerUtils.releaseDetailProfiler(invocation);
                if (filter instanceof ListenableFilter) {
                    ListenableFilter listenableFilter = ((ListenableFilter) filter);
                    Filter.Listener listener = listenableFilter.listener(invocation);
                    try {
```

### Deprecation
'org.apache.dubbo.rpc.ListenableFilter' is deprecated
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/filter/FilterChainBuilder.java`
#### Snippet
```java
                    try {
                        InvocationProfilerUtils.releaseDetailProfiler(invocation);
                        if (filter instanceof ListenableFilter) {
                            ListenableFilter listenableFilter = ((ListenableFilter) filter);
                            Filter.Listener listener = listenableFilter.listener(invocation);
```

### Deprecation
'org.apache.dubbo.rpc.ListenableFilter' is deprecated
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/filter/FilterChainBuilder.java`
#### Snippet
```java
                        InvocationProfilerUtils.releaseDetailProfiler(invocation);
                        if (filter instanceof ListenableFilter) {
                            ListenableFilter listenableFilter = ((ListenableFilter) filter);
                            Filter.Listener listener = listenableFilter.listener(invocation);
                            try {
```

### Deprecation
'org.apache.dubbo.rpc.ListenableFilter' is deprecated
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/filter/FilterChainBuilder.java`
#### Snippet
```java
                        InvocationProfilerUtils.releaseDetailProfiler(invocation);
                        if (filter instanceof ListenableFilter) {
                            ListenableFilter listenableFilter = ((ListenableFilter) filter);
                            Filter.Listener listener = listenableFilter.listener(invocation);
                            try {
```

### Deprecation
'getContext()' is deprecated
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/script/ScriptStateRouter.java`
#### Snippet
```java
            } catch (ScriptException e) {
                logger.error(CLUSTER_SCRIPT_EXCEPTION, "Scriptrouter exec script error", "", "Script route error, rule has been ignored. rule: " + rule + ", method:" +
                    invocation.getMethodName() + ", url: " + RpcContext.getContext().getUrl(), e);
                return invokers;
            }
```

### Deprecation
'getInvoker()' is deprecated
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/support/AbstractClusterInvoker.java`
#### Snippet
```java
    private Invoker<T> setContext(Invoker<T> invoker, URL consumerUrl) {
        RpcServiceContext context = RpcContext.getServiceContext();
        Invoker<?> originInvoker = context.getInvoker();
        context.setInvoker(invoker)
            .setConsumerUrl(null != consumerUrl ? consumerUrl : RpcContext.getServiceContext().getConsumerUrl());
```

### Deprecation
'request(java.lang.Object)' is deprecated
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/RemotingException.java`
#### Snippet
```java
 * @see org.apache.dubbo.remoting.exchange.support.DefaultFuture#get()
 * @see org.apache.dubbo.remoting.Channel#send(Object, boolean)
 * @see org.apache.dubbo.remoting.exchange.ExchangeChannel#request(Object)
 * @see org.apache.dubbo.remoting.exchange.ExchangeChannel#request(Object, int)
 * @see org.apache.dubbo.remoting.Transporter#bind(org.apache.dubbo.common.URL, ChannelHandler)
```

### Deprecation
'request(java.lang.Object, int)' is deprecated
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/RemotingException.java`
#### Snippet
```java
 * @see org.apache.dubbo.remoting.Channel#send(Object, boolean)
 * @see org.apache.dubbo.remoting.exchange.ExchangeChannel#request(Object)
 * @see org.apache.dubbo.remoting.exchange.ExchangeChannel#request(Object, int)
 * @see org.apache.dubbo.remoting.Transporter#bind(org.apache.dubbo.common.URL, ChannelHandler)
 * @see org.apache.dubbo.remoting.Transporter#connect(org.apache.dubbo.common.URL, ChannelHandler)
```

### Deprecation
'org.apache.dubbo.remoting.transport.codec.TransportCodec' is deprecated
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/telnet/codec/TelnetCodec.java`
#### Snippet
```java
 * TelnetCodec
 */
public class TelnetCodec extends TransportCodec {

    private static final ErrorTypeAwareLogger logger = LoggerFactory.getErrorTypeAwareLogger(TelnetCodec.class);
```

### Deprecation
'request(java.lang.Object)' is deprecated
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/exchange/support/header/HeaderExchangeClient.java`
#### Snippet
```java
    @Override
    public CompletableFuture<Object> request(Object request) throws RemotingException {
        return channel.request(request);
    }

```

### Deprecation
'request(java.lang.Object, int)' is deprecated
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/exchange/support/header/HeaderExchangeClient.java`
#### Snippet
```java
    @Override
    public CompletableFuture<Object> request(Object request, int timeout) throws RemotingException {
        return channel.request(request, timeout);
    }

```

### Deprecation
'org.apache.dubbo.remoting.Codec' is deprecated
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/transport/AbstractEndpoint.java`
#### Snippet
```java
        if (frameworkModel.getExtensionLoader(Codec2.class).hasExtension(codecName)) {
            return frameworkModel.getExtensionLoader(Codec2.class).getExtension(codecName);
        } else if (frameworkModel.getExtensionLoader(Codec.class).hasExtension(codecName)) {
            return new CodecAdapter(frameworkModel.getExtensionLoader(Codec.class)
                .getExtension(codecName));
```

### Deprecation
'org.apache.dubbo.remoting.Codec' is deprecated
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/transport/AbstractEndpoint.java`
#### Snippet
```java
            return frameworkModel.getExtensionLoader(Codec2.class).getExtension(codecName);
        } else if (frameworkModel.getExtensionLoader(Codec.class).hasExtension(codecName)) {
            return new CodecAdapter(frameworkModel.getExtensionLoader(Codec.class)
                .getExtension(codecName));
        } else {
```

### Deprecation
'org.apache.dubbo.remoting.Codec' is deprecated
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/transport/codec/CodecAdapter.java`
#### Snippet
```java
    private Codec codec;

    public CodecAdapter(Codec codec) {
        Assert.notNull(codec, "codec == null");
        this.codec = codec;
```

### Deprecation
'org.apache.dubbo.remoting.Codec' is deprecated
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/transport/codec/CodecAdapter.java`
#### Snippet
```java
    }

    public Codec getCodec() {
        return codec;
    }
```

### Deprecation
'org.apache.dubbo.remoting.Codec' is deprecated
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/transport/codec/CodecAdapter.java`
#### Snippet
```java
        Object result = codec.decode(channel, is);
        buffer.readerIndex(savedReaderIndex + is.position());
        return result == Codec.NEED_MORE_INPUT ? DecodeResult.NEED_MORE_INPUT : result;
    }

```

### Deprecation
'org.apache.dubbo.remoting.Codec' is deprecated
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/transport/codec/CodecAdapter.java`
#### Snippet
```java
public class CodecAdapter implements Codec2 {

    private Codec codec;

    public CodecAdapter(Codec codec) {
```

### Deprecation
'org.apache.dubbo.common.Parameters' is deprecated
in `dubbo-remoting/dubbo-remoting-http/src/main/java/org/apache/dubbo/remoting/http/support/AbstractHttpServer.java`
#### Snippet
```java

    @Override
    public void reset(Parameters parameters) {

    }
```

## RuleId[id=FinalStaticMethod]
### FinalStaticMethod
'static' method declared `final`
in `dubbo-plugin/dubbo-qos/src/main/java/org/apache/dubbo/qos/command/decoder/TelnetCommandDecoder.java`
#### Snippet
```java

public class TelnetCommandDecoder {
    public static final CommandContext decode(String str) {
        CommandContext commandContext = null;
        if (!StringUtils.isBlank(str)) {
```

### FinalStaticMethod
'static' method declared `final`
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/rest/AbstractServiceRestMetadataResolver.java`
#### Snippet
```java
                                            ExecutableElement method, Set<String> consumes);

    protected static final void put(String name, Object value) {
        Map<String, Object> cache = getCache();
        cache.put(name, value);
```

### FinalStaticMethod
'static' method declared `final`
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/rest/AbstractServiceRestMetadataResolver.java`
#### Snippet
```java
    }

    protected static final <V> V computeIfAbsent(String name, Function<? super String, ? extends V> mappingFunction) {
        return (V) getCache().computeIfAbsent(name, mappingFunction);
    }
```

### FinalStaticMethod
'static' method declared `final`
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/rest/AbstractServiceRestMetadataResolver.java`
#### Snippet
```java
    }

    protected static final <T> T get(String name) throws ClassCastException {
        Map<String, Object> cache = getCache();
        return (T) cache.get(name);
```

## RuleId[id=TrivialStringConcatenation]
### TrivialStringConcatenation
Empty string used in concatenation
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ScopeModel.java`
#### Snippet
```java
            return parentInternalId + "." + childIndex;
        } else {
            return "" + childIndex;
        }
    }
```

### TrivialStringConcatenation
Empty string used in concatenation
in `dubbo-common/src/main/java/org/apache/dubbo/config/ReferenceConfigBase.java`
#### Snippet
```java
            if (logger.isWarnEnabled()) {
                if (resolveFile != null) {
                    logger.warn(COMMON_UNEXPECTED_EXCEPTION, "", "", "Using default dubbo resolve file " + resolveFile + " replace " + interfaceName + "" + resolve + " to p2p invoke remote service.");
                } else {
                    logger.warn(COMMON_UNEXPECTED_EXCEPTION, "", "", "Using -D" + interfaceName + "=" + resolve + " to p2p invoke remote service.");
```

### TrivialStringConcatenation
Empty string used in concatenation
in `dubbo-plugin/dubbo-qos/src/main/java/org/apache/dubbo/qos/command/impl/BaseOnline.java`
#### Snippet
```java
        String servicePattern = ".*";
        if (ArrayUtils.isNotEmpty(args)) {
            servicePattern = "" + args[0];
        }

```

### TrivialStringConcatenation
Empty string used in concatenation
in `dubbo-metrics/dubbo-metrics-api/src/main/java/org/apache/dubbo/metrics/model/ThreadPoolRejectMetric.java`
#### Snippet
```java
        Map<String, String> tags = new HashMap<>();
        tags.put(TAG_IP, getLocalHost());
        tags.put(TAG_PID, ConfigUtils.getPid()+"");
        tags.put(TAG_HOSTNAME, getLocalHostName());
        tags.put(TAG_APPLICATION_NAME, applicationName);
```

### TrivialStringConcatenation
Empty string used in concatenation
in `dubbo-metrics/dubbo-metrics-api/src/main/java/org/apache/dubbo/metrics/model/ThreadPoolMetric.java`
#### Snippet
```java
        Map<String, String> tags = new HashMap<>();
        tags.put(TAG_IP, getLocalHost());
        tags.put(TAG_PID, ConfigUtils.getPid()+"");
        tags.put(TAG_HOSTNAME, getLocalHostName());
        tags.put(TAG_APPLICATION_NAME, applicationName);
```

## RuleId[id=RedundantTypeArguments]
### RedundantTypeArguments
Explicit type arguments can be inferred
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/netty/NettyServer.java`
#### Snippet
```java
     */
    public void setChannelOptions(final Map<ChannelOption, Object> channelOptions) {
        this.channelOptions = channelOptions == null ? Collections.<ChannelOption, Object>emptyMap() : channelOptions;
    }

```

### RedundantTypeArguments
Explicit type arguments can be inferred
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/netty/NettyServer.java`
#### Snippet
```java
     */
    public void setChildChannelOptions(final Map<ChannelOption, Object> channelOptions) {
        this.childChannelOptions = channelOptions == null ? Collections.<ChannelOption, Object>emptyMap() : channelOptions;
    }

```

### RedundantTypeArguments
Explicit type arguments can be inferred
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/netty/NettyServer.java`
#### Snippet
```java
     */
    public void setChannelHandlers(final List<ChannelHandler> channelHandlers) {
        this.channelHandlers = channelHandlers == null ? Collections.<ChannelHandler>emptyList() : channelHandlers;
    }

```

## RuleId[id=NullableProblems]
### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `dubbo-native/src/main/java/org/apache/dubbo/aot/generate/BasicJsonWriter.java`
#### Snippet
```java

        @Override
        public void write(char[] chars, int offset, int length) {
            try {
                if (this.prependIndent) {
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `dubbo-remoting/dubbo-remoting-netty4/src/main/java/org/apache/dubbo/remoting/transport/netty4/NettyBackedChannelBuffer.java`
#### Snippet
```java

    @Override
    public int compareTo(ChannelBuffer o) {
        return ChannelBuffers.compare(this, o);
    }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/netty/ChunkOutputStream.java`
#### Snippet
```java

    @Override
    public void write(byte[] b, int off, int len) throws IOException {
        int dataLengthLeftToWrite = len;
        int dataToWriteOffset = off;
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/UnsafeStringWriter.java`
#### Snippet
```java

    @Override
    public void write(String str) {
        mBuffer.append(str);
    }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/UnsafeStringWriter.java`
#### Snippet
```java

    @Override
    public void write(char[] cs) throws IOException {
        mBuffer.append(cs, 0, cs.length);
    }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/UnsafeStringWriter.java`
#### Snippet
```java

    @Override
    public void write(char[] cs, int off, int len) throws IOException {
        if ((off < 0) || (off > cs.length) || (len < 0) ||
                ((off + len) > cs.length) || ((off + len) < 0)) {
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/UnsafeStringWriter.java`
#### Snippet
```java

    @Override
    public void write(String str, int off, int len) {
        mBuffer.append(str, off, off + len);
    }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/UnsafeByteArrayOutputStream.java`
#### Snippet
```java

    @Override
    public void write(byte[] b, int off, int len) {
        if ((off < 0) || (off > b.length) || (len < 0) || ((off + len) > b.length) || ((off + len) < 0)) {
            throw new IndexOutOfBoundsException();
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/UnsafeStringReader.java`
#### Snippet
```java

    @Override
    public int read(char[] cs, int off, int len) throws IOException {
        ensureOpen();
        if ((off < 0) || (off > cs.length) || (len < 0) ||
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/UnsafeByteArrayInputStream.java`
#### Snippet
```java

    @Override
    public int read(byte[] b, int off, int len) {
        if (b == null) {
            throw new NullPointerException();
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/StreamUtils.java`
#### Snippet
```java

            @Override
            public int read(byte[] b, int off, int len) throws IOException {
                if (b == null) {
                    throw new NullPointerException();
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/NamedThreadFactory.java`
#### Snippet
```java

    @Override
    public Thread newThread(Runnable runnable) {
        String name = mPrefix + mThreadNum.getAndIncrement();
        Thread ret = new Thread(mGroup, runnable, name, 0);
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `dubbo-common/src/main/java/org/apache/dubbo/common/beanutil/JavaBeanDescriptor.java`
#### Snippet
```java

    @Override
    public Iterator<Map.Entry<Object, Object>> iterator() {
        return properties.entrySet().iterator();
    }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `dubbo-common/src/main/java/org/apache/dubbo/common/threadpool/MemorySafeLinkedBlockingQueue.java`
#### Snippet
```java

    @Override
    public boolean offer(final E e) {
        if (!hasRemainedMemory()) {
            rejector.reject(e, this);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `dubbo-common/src/main/java/org/apache/dubbo/common/threadpool/MemoryLimitedLinkedBlockingQueue.java`
#### Snippet
```java

    @Override
    public boolean offer(E e) {
        return memoryLimiter.acquire(e) && super.offer(e);
    }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `dubbo-common/src/main/java/org/apache/dubbo/common/threadpool/ThreadlessExecutor.java`
#### Snippet
```java

    @Override
    public boolean awaitTermination(long timeout, TimeUnit unit) {
        return false;
    }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `dubbo-common/src/main/java/org/apache/dubbo/common/threadpool/ThreadlessExecutor.java`
#### Snippet
```java
     */
    @Override
    public void execute(Runnable runnable) {
        RunnableWrapper run = new RunnableWrapper(runnable);
        queue.add(run);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `dubbo-common/src/main/java/org/apache/dubbo/common/threadpool/serial/SerializingExecutor.java`
#### Snippet
```java
     */
    @Override
    public void execute(Runnable r) {
        runQueue.add(r);
        schedule(r);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `dubbo-common/src/main/java/org/apache/dubbo/common/threadpool/support/eager/TaskQueue.java`
#### Snippet
```java

    @Override
    public boolean offer(Runnable runnable) {
        if (executor == null) {
            throw new RejectedExecutionException("The task queue does not have executor!");
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `dubbo-common/src/main/java/org/apache/dubbo/common/threadpool/support/eager/EagerThreadPoolExecutor.java`
#### Snippet
```java

    @Override
    public void execute(Runnable command) {
        if (command == null) {
            throw new NullPointerException();
```

### NullableProblems
Not annotated parameter overrides @NonNullApi parameter
in `dubbo-metrics/dubbo-metrics-api/src/main/java/org/apache/dubbo/metrics/observation/DubboServerObservationConvention.java`
#### Snippet
```java
public interface DubboServerObservationConvention extends ObservationConvention<DubboServerContext> {
    @Override
    default boolean supportsContext(Observation.Context context) {
        return context instanceof DubboServerContext;
    }
```

### NullableProblems
Not annotated method overrides method annotated with @NonNullApi
in `dubbo-metrics/dubbo-metrics-api/src/main/java/org/apache/dubbo/metrics/observation/DefaultDubboServerObservationConvention.java`
#### Snippet
```java

    @Override
    public KeyValues getLowCardinalityKeyValues(DubboServerContext context) {
        return super.getLowCardinalityKeyValues(context.getInvocation());
    }
```

### NullableProblems
Not annotated method overrides method annotated with @NonNullApi
in `dubbo-metrics/dubbo-metrics-api/src/main/java/org/apache/dubbo/metrics/observation/DubboObservationDocumentation.java`
#### Snippet
```java

        @Override
        public KeyName[] getLowCardinalityKeyNames() {
            return LowCardinalityKeyNames.values();
        }
```

### NullableProblems
Not annotated method overrides method annotated with @NonNullApi
in `dubbo-metrics/dubbo-metrics-api/src/main/java/org/apache/dubbo/metrics/observation/DubboObservationDocumentation.java`
#### Snippet
```java

        @Override
        public KeyName[] getLowCardinalityKeyNames() {
            return LowCardinalityKeyNames.values();
        }
```

### NullableProblems
Not annotated parameter overrides @NonNullApi parameter
in `dubbo-metrics/dubbo-metrics-api/src/main/java/org/apache/dubbo/metrics/observation/DubboClientObservationConvention.java`
#### Snippet
```java
public interface DubboClientObservationConvention extends ObservationConvention<DubboClientContext> {
    @Override
    default boolean supportsContext(Observation.Context context) {
        return context instanceof DubboClientContext;
    }
```

### NullableProblems
Not annotated method overrides method annotated with @NonNullApi
in `dubbo-metrics/dubbo-metrics-api/src/main/java/org/apache/dubbo/metrics/observation/DefaultDubboClientObservationConvention.java`
#### Snippet
```java

    @Override
    public KeyValues getLowCardinalityKeyValues(DubboClientContext context) {
        KeyValues keyValues = super.getLowCardinalityKeyValues(context.getInvocation());
        return withRemoteHostPort(keyValues, context);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `dubbo-remoting/dubbo-remoting-netty/src/main/java/org/apache/dubbo/remoting/transport/netty/NettyBackedChannelBuffer.java`
#### Snippet
```java

    @Override
    public int compareTo(ChannelBuffer o) {
        return ChannelBuffers.compare(this, o);
    }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/RpcContext.java`
#### Snippet
```java

                @Override
                public T get(long timeout, TimeUnit unit)
                        throws InterruptedException, ExecutionException,
                        TimeoutException {
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/protocol/dubbo/FutureAdapter.java`
#### Snippet
```java
    @Override
    @SuppressWarnings("unchecked")
    public V get(long timeout, TimeUnit unit) throws InterruptedException, ExecutionException, TimeoutException {
        try {
            return (V) (((Result) future.get(timeout, unit)).recreate());
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/Router.java`
#### Snippet
```java

    @Override
    default int compareTo(Router o) {
        if (o == null) {
            throw new IllegalArgumentException();
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/Configurator.java`
#### Snippet
```java
     */
    @Override
    default int compareTo(Configurator o) {
        if (o == null) {
            return -1;
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/state/BitList.java`
#### Snippet
```java
    @Override
    @SuppressWarnings("unchecked")
    public boolean addAll(Collection<? extends E> c) {
        if (c instanceof BitList) {
            rootSet.or(((BitList<? extends E>) c).rootSet);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/buffer/ChannelBufferInputStream.java`
#### Snippet
```java

    @Override
    public int read(byte[] b, int off, int len) throws IOException {
        int available = available();
        if (available == 0) {
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/buffer/ChannelBufferOutputStream.java`
#### Snippet
```java

    @Override
    public void write(byte[] b, int off, int len) throws IOException {
        if (len == 0) {
            return;
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/buffer/ChannelBufferOutputStream.java`
#### Snippet
```java

    @Override
    public void write(byte[] b) throws IOException {
        buffer.writeBytes(b);
    }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/buffer/AbstractChannelBuffer.java`
#### Snippet
```java

    @Override
    public int compareTo(ChannelBuffer that) {
        return ChannelBuffers.compare(this, that);
    }
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/exchange/support/MultiMessage.java`
#### Snippet
```java

    @Override
    public Iterator iterator() {
        return messages.iterator();
    }
```

### NullableProblems
Not annotated parameter overrides @EverythingIsNonNull parameter
in `dubbo-remoting/dubbo-remoting-http/src/main/java/org/apache/dubbo/remoting/http/restclient/OKHttpRestClient.java`
#### Snippet
```java

            @Override
            public void onResponse(Call call, Response response) throws IOException {
                future.complete(new RestResult() {
                    @Override
```

### NullableProblems
Not annotated parameter overrides @EverythingIsNonNull parameter
in `dubbo-remoting/dubbo-remoting-http/src/main/java/org/apache/dubbo/remoting/http/restclient/OKHttpRestClient.java`
#### Snippet
```java

            @Override
            public void onResponse(Call call, Response response) throws IOException {
                future.complete(new RestResult() {
                    @Override
```

### NullableProblems
Not annotated parameter overrides @EverythingIsNonNull parameter
in `dubbo-remoting/dubbo-remoting-http/src/main/java/org/apache/dubbo/remoting/http/restclient/OKHttpRestClient.java`
#### Snippet
```java
        okHttpClient.newCall(builder.build()).enqueue(new Callback() {
            @Override
            public void onFailure(Call call, IOException e) {
                future.completeExceptionally(e);
            }
```

### NullableProblems
Not annotated parameter overrides @EverythingIsNonNull parameter
in `dubbo-remoting/dubbo-remoting-http/src/main/java/org/apache/dubbo/remoting/http/restclient/OKHttpRestClient.java`
#### Snippet
```java
        okHttpClient.newCall(builder.build()).enqueue(new Callback() {
            @Override
            public void onFailure(Call call, IOException e) {
                future.completeExceptionally(e);
            }
```

## RuleId[id=DuplicateCondition]
### DuplicateCondition
Duplicate condition `mask[i].equals(ipAddress[i])`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/NetUtils.java`
#### Snippet
```java

        for (int i = 0; i < mask.length; i++) {
            if ("*".equals(mask[i]) || mask[i].equals(ipAddress[i])) {
                continue;
            } else if (mask[i].contains("-")) {
```

### DuplicateCondition
Duplicate condition `mask[i].equals(ipAddress[i])`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/NetUtils.java`
#### Snippet
```java
            } else if ("0".equals(ipAddress[i]) && ("0".equals(mask[i]) || "00".equals(mask[i]) || "000".equals(mask[i]) || "0000".equals(mask[i]))) {
                continue;
            } else if (!mask[i].equals(ipAddress[i])) {
                return false;
            }
```

## RuleId[id=IgnoreResultOfCall]
### IgnoreResultOfCall
Result of `InputStream.read()` is ignored
in `dubbo-demo/dubbo-demo-xml/dubbo-demo-xml-provider/src/main/java/org/apache/dubbo/demo/provider/Application.java`
#### Snippet
```java
        ClassPathXmlApplicationContext context = new ClassPathXmlApplicationContext("spring/dubbo-provider.xml");
        context.start();
        System.in.read();
    }
}
```

### IgnoreResultOfCall
Result of `File.mkdirs()` is ignored
in `dubbo-native/src/main/java/org/apache/dubbo/aot/generate/NativeConfigurationWriter.java`
#### Snippet
```java
            outputDirectory = outputDirectory.resolve(this.groupId).resolve(this.artifactId);
        }
        outputDirectory.toFile().mkdirs();
        File file = outputDirectory.resolve(filename).toFile();
        file.createNewFile();
```

### IgnoreResultOfCall
Result of `File.createNewFile()` is ignored
in `dubbo-native/src/main/java/org/apache/dubbo/aot/generate/NativeConfigurationWriter.java`
#### Snippet
```java
        outputDirectory.toFile().mkdirs();
        File file = outputDirectory.resolve(filename).toFile();
        file.createNewFile();
        return file;
    }
```

### IgnoreResultOfCall
Result of `InetAddress.getByName()` is ignored
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/utils/ConfigValidationUtils.java`
#### Snippet
```java
            // if the value start with "[" and end with "]", check whether it is IPV6
            try {
                InetAddress.getByName(value);
                return;
            } catch (UnknownHostException e) {
```

### IgnoreResultOfCall
Result of `InputStream.read()` is ignored
in `dubbo-demo/dubbo-demo-native/dubbo-demo-native-provider/src/main/java/org/apache/dubbo/demo/graalvm/provider/Application.java`
#### Snippet
```java
        System.setProperty("dubbo.json-framework.prefer", "fastjson");
        startWithBootstrap();
        System.in.read();
    }

```

### IgnoreResultOfCall
Result of `ExecutorService.awaitTermination()` is ignored
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/registrycenter/context/ZookeeperWindowsContext.java`
#### Snippet
```java
        }
        try {
            DEFAULT_EXECUTOR_SERVICE.awaitTermination(5, TimeUnit.SECONDS);
        } catch (InterruptedException ex) {
            Thread.currentThread().interrupt();
```

### IgnoreResultOfCall
Result of `File.mkdirs()` is ignored
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/DubboTestChecker.java`
#### Snippet
```java
            this.reportFile = new File(reportFilePath);
            if (reportFile.isDirectory()) {
                reportFile.mkdirs();
                reportFile = new File(reportFile, defaultReportFileName);
            }
```

### IgnoreResultOfCall
Result of `File.mkdirs()` is ignored
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/registrycenter/initializer/UnpackZookeeperInitializer.java`
#### Snippet
```java
                File parent = curFile.getParentFile();
                if (!parent.exists()) {
                    parent.mkdirs();
                }
                try (FileOutputStream outputStream = new FileOutputStream(curFile)) {
```

### IgnoreResultOfCall
Result of `File.renameTo()` is ignored
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/registrycenter/initializer/UnpackZookeeperInitializer.java`
#### Snippet
```java
            File sourceFile = parentPath.toFile().listFiles()[0];
            File targetFile = Paths.get(parentPath.toString(), context.getUnpackedDirectory()).toFile();
            sourceFile.renameTo(targetFile);
            if (!Files.exists(targetFile.toPath()) || !targetFile.isDirectory()) {
                throw new IllegalStateException(String.format("Failed to rename the directory. source directory: %s, target directory: %s",
```

### IgnoreResultOfCall
Result of `File.setExecutable()` is ignored
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/registrycenter/initializer/UnpackZookeeperInitializer.java`
#### Snippet
```java
            // update file permission
            for (File file : zookeeperBin.toFile().listFiles()) {
                file.setExecutable(true, false);
                file.setReadable(true, false);
                file.setWritable(false, false);
```

### IgnoreResultOfCall
Result of `File.setReadable()` is ignored
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/registrycenter/initializer/UnpackZookeeperInitializer.java`
#### Snippet
```java
            for (File file : zookeeperBin.toFile().listFiles()) {
                file.setExecutable(true, false);
                file.setReadable(true, false);
                file.setWritable(false, false);
            }
```

### IgnoreResultOfCall
Result of `File.setWritable()` is ignored
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/registrycenter/initializer/UnpackZookeeperInitializer.java`
#### Snippet
```java
                file.setExecutable(true, false);
                file.setReadable(true, false);
                file.setWritable(false, false);
            }
        }
```

### IgnoreResultOfCall
Result of `InputStream.read()` is ignored
in `dubbo-demo/dubbo-demo-annotation/dubbo-demo-annotation-provider/src/main/java/org/apache/dubbo/demo/provider/Application.java`
#### Snippet
```java
        AnnotationConfigApplicationContext context = new AnnotationConfigApplicationContext(ProviderConfiguration.class);
        context.start();
        System.in.read();
    }

```

### IgnoreResultOfCall
Result of `InputStream.read()` is ignored
in `dubbo-demo/dubbo-demo-xml/dubbo-demo-jaxrs-rest-provider/src/main/java/org/apache/dubbo/demo/rest/api/RestProvider.java`
#### Snippet
```java
        System.out.println("dubbo service started");

        System.in.read();
    }

```

### IgnoreResultOfCall
Result of `InputStream.read()` is ignored
in `dubbo-demo/dubbo-demo-xml/dubbo-demo-spring-mvc-rest-provider/src/main/java/org/apache/dubbo/demo/rest/api/SpringMvcRestProvider.java`
#### Snippet
```java
        System.out.println("spring mvc rest provider started");

        System.in.read();
    }

```

### IgnoreResultOfCall
Result of `File.delete()` is ignored
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/report/support/AbstractMetadataReport.java`
#### Snippet
```java
            // if this file exists, firstly delete it.
            if (!initialized.getAndSet(true) && file.exists()) {
                file.delete();
            }
        }
```

### IgnoreResultOfCall
Result of `File.createNewFile()` is ignored
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/report/support/AbstractMetadataReport.java`
#### Snippet
```java
            File lockfile = new File(file.getAbsolutePath() + ".lock");
            if (!lockfile.exists()) {
                lockfile.createNewFile();
            }
            try (RandomAccessFile raf = new RandomAccessFile(lockfile, "rw");
```

### IgnoreResultOfCall
Result of `File.createNewFile()` is ignored
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/report/support/AbstractMetadataReport.java`
#### Snippet
```java
                try {
                    if (!file.exists()) {
                        file.createNewFile();
                    }

```

### IgnoreResultOfCall
Result of `InputStream.skip()` is ignored
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/StreamUtils.java`
#### Snippet
```java
                }

                is.skip(len);
                mPosition += len;
                return len;
```

### IgnoreResultOfCall
Result of `InputStream.read()` is ignored
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/StreamUtils.java`
#### Snippet
```java
                }

                is.read(b, off, len);
                mPosition += len;
                return len;
```

### IgnoreResultOfCall
Result of `InputStream.skip()` is ignored
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/StreamUtils.java`
#### Snippet
```java
    public static void skipUnusedStream(InputStream is) throws IOException {
        if (is.available() > 0) {
            is.skip(is.available());
        }
    }
```

### IgnoreResultOfCall
Result of `File.createNewFile()` is ignored
in `dubbo-common/src/main/java/org/apache/dubbo/common/cache/FileCacheStoreFactory.java`
#### Snippet
```java
        FileLock dirLock;
        try {
            lockFile.createNewFile();
            if (!lockFile.exists()) {
                throw new AssertionError("Failed to create lock file " + lockFile);
```

### IgnoreResultOfCall
Result of `ExecutorService.awaitTermination()` is ignored
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ExecutorUtil.java`
#### Snippet
```java
        }
        try {
            es.awaitTermination(timeout, TimeUnit.MILLISECONDS);
        } catch (InterruptedException ex) {
            Thread.currentThread().interrupt();
```

### IgnoreResultOfCall
Result of `File.createNewFile()` is ignored
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/support/AbstractRegistry.java`
#### Snippet
```java
            lockfile = new File(file.getAbsolutePath() + ".lock");
            if (!lockfile.exists()) {
                lockfile.createNewFile();
            }

```

### IgnoreResultOfCall
Result of `File.createNewFile()` is ignored
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/support/AbstractRegistry.java`
#### Snippet
```java
                try {
                    if (!file.exists()) {
                        file.createNewFile();
                    }

```

### IgnoreResultOfCall
Result of `LineNumberReader.skip()` is ignored
in `dubbo-plugin/dubbo-qos/src/main/java/org/apache/dubbo/qos/command/impl/DefaultMetricsReporterCmd.java`
#### Snippet
```java
        LineNumberReader lnr = new LineNumberReader(new CharArrayReader(content.toCharArray()));
        try {
            lnr.skip(Long.MAX_VALUE);
            lnr.close();
        } catch (IOException ignore) {
```

### IgnoreResultOfCall
Result of `InputStream.read()` is ignored
in `dubbo-demo/dubbo-demo-triple/src/main/java/org/apache/dubbo/demo/consumer/ApiWrapperConsumer.java`
#### Snippet
```java
        // 4MB response
        System.out.println("Reply length:" + reply.length() + " cost:" + (System.currentTimeMillis() - st));
        System.in.read();
    }
}
```

### IgnoreResultOfCall
Result of `InputStream.read()` is ignored
in `dubbo-demo/dubbo-demo-triple/src/main/java/org/apache/dubbo/demo/consumer/ApiConsumer.java`
#### Snippet
```java
            t.printStackTrace();
        }
        System.in.read();
    }
}
```

### IgnoreResultOfCall
Result of `File.renameTo()` is ignored
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/filter/AccessLogFilter.java`
#### Snippet
```java
            if (!now.equals(last)) {
                File archive = new File(file.getAbsolutePath() + "." + now);
                file.renameTo(archive);
            }
        }
```

### IgnoreResultOfCall
Result of `File.mkdirs()` is ignored
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/filter/AccessLogFilter.java`
#### Snippet
```java
        File dir = file.getParentFile();
        if (null != dir && !dir.exists()) {
            dir.mkdirs();
        }
    }
```

### IgnoreResultOfCall
Result of `LineNumberReader.skip()` is ignored
in `dubbo-metrics/dubbo-metrics-prometheus/src/main/java/org/apache/dubbo/metrics/prometheus/PrometheusMetricsReporterCmd.java`
#### Snippet
```java
        LineNumberReader lnr = new LineNumberReader(new CharArrayReader(content.toCharArray()));
        try {
            lnr.skip(Long.MAX_VALUE);
            lnr.close();
        } catch (IOException ignore) {
```

### IgnoreResultOfCall
Result of `InputStream.read()` is ignored
in `dubbo-rpc/dubbo-rpc-dubbo/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/DubboCodec.java`
#### Snippet
```java
        if (is.available() > 0) {
            byte[] result = new byte[is.available()];
            is.read(result);
            return result;
        }
```

### IgnoreResultOfCall
Result of `BlockingQueue.offer()` is ignored
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/support/ForkingClusterInvoker.java`
#### Snippet
```java
                }, executor).whenComplete((v, t) -> {
                    if (t == null) {
                        ref.offer(v);
                    } else {
                        int value = count.incrementAndGet();
```

### IgnoreResultOfCall
Result of `BlockingQueue.offer()` is ignored
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/support/ForkingClusterInvoker.java`
#### Snippet
```java
                        int value = count.incrementAndGet();
                        if (value >= selected.size()) {
                            ref.offer(t);
                        }
                    }
```

## RuleId[id=AccessStaticViaInstance]
### AccessStaticViaInstance
Static member 'org.apache.dubbo.common.resource.GlobalResourcesRepository.globalReusedDisposables' accessed via instance reference
in `dubbo-common/src/main/java/org/apache/dubbo/common/resource/GlobalResourcesRepository.java`
#### Snippet
```java
            synchronized (GlobalResourcesRepository.class) {
                if (globalReusedDisposables.contains(disposable)) {
                    this.globalReusedDisposables.remove(disposable);
                }
            }
```

## RuleId[id=RedundantMethodOverride]
### RedundantMethodOverride
Method `getServiceKey()` is identical to its super method
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ServiceMetadata.java`
#### Snippet
```java

    @Override
    public String getServiceKey() {
        return serviceKey;
    }
```

### RedundantMethodOverride
Method `close()` is identical to its super method
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/UnsafeByteArrayOutputStream.java`
#### Snippet
```java

    @Override
    public void close() throws IOException {
    }
}
```

### RedundantMethodOverride
Method `close()` is identical to its super method
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/UnsafeByteArrayInputStream.java`
#### Snippet
```java

    @Override
    public void close() throws IOException {
    }

```

### RedundantMethodOverride
Method `getScopeModel()` is identical to its super method
in `dubbo-common/src/main/java/org/apache/dubbo/common/url/component/DubboServiceAddressURL.java`
#### Snippet
```java

    @Override
    public ScopeModel getScopeModel() {
        return consumerURL.getScopeModel();
    }
```

### RedundantMethodOverride
Method `getServiceModel()` is identical to its super method
in `dubbo-common/src/main/java/org/apache/dubbo/common/url/component/DubboServiceAddressURL.java`
#### Snippet
```java

    @Override
    public ServiceModel getServiceModel() {
        return consumerURL.getServiceModel();
    }
```

### RedundantMethodOverride
Method `getVersion()` is identical to its super method
in `dubbo-common/src/main/java/org/apache/dubbo/common/url/component/ServiceAddressURL.java`
#### Snippet
```java

    @Override
    public String getVersion() {
        return super.getParameter(VERSION_KEY);
    }
```

### RedundantMethodOverride
Method `getGroup()` is identical to its super method
in `dubbo-common/src/main/java/org/apache/dubbo/common/url/component/ServiceAddressURL.java`
#### Snippet
```java

    @Override
    public String getGroup() {
        return super.getParameter(GROUP_KEY);
    }
```

### RedundantMethodOverride
Method `isConfigured()` is identical to its super method
in `dubbo-common/src/main/java/org/apache/dubbo/common/logger/log4j2/Log4j2LoggerAdapter.java`
#### Snippet
```java

    @Override
    public boolean isConfigured() {
        return true;
    }
```

### RedundantMethodOverride
Method `getPriority()` is identical to its super method
in `dubbo-common/src/main/java/org/apache/dubbo/common/convert/StringToIntegerConverter.java`
#### Snippet
```java

    @Override
    public int getPriority() {
        return NORMAL_PRIORITY;
    }
```

### RedundantMethodOverride
Method `getInternalProperty()` is identical to its super method
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/configcenter/file/FileSystemDynamicConfiguration.java`
#### Snippet
```java

    @Override
    public Object getInternalProperty(String key) {
        return null;
    }
```

### RedundantMethodOverride
Method `getPriority()` is identical to its super method
in `dubbo-common/src/main/java/org/apache/dubbo/common/extension/DubboLoadingStrategy.java`
#### Snippet
```java

    @Override
    public int getPriority() {
        return NORMAL_PRIORITY;
    }
```

### RedundantMethodOverride
Method `setListener()` is identical to its super method
in `dubbo-common/src/main/java/org/apache/dubbo/config/AbstractServiceConfig.java`
#### Snippet
```java

    @Override
    public void setListener(String listener) {
        this.listener = listener;
    }
```

### RedundantMethodOverride
Method `isServiceDiscovery()` is identical to its super method
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/integration/RegistryDirectory.java`
#### Snippet
```java

    @Override
    public boolean isServiceDiscovery() {
        return false;
    }
```

### RedundantMethodOverride
Method `getUrl()` is identical to its super method
in `dubbo-registry/dubbo-registry-nacos/src/main/java/org/apache/dubbo/registry/nacos/NacosServiceDiscovery.java`
#### Snippet
```java

    @Override
    public URL getUrl() {
        return registryURL;
    }
```

### RedundantMethodOverride
Method `getServers()` is identical to its super method
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/Protocol.java`
#### Snippet
```java

    @Override
    default List<ProtocolServer> getServers() {
        return Collections.emptyList();
    }
```

### RedundantMethodOverride
Method `getServiceName()` is identical to its super method
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/Invocation.java`
#### Snippet
```java

        @Override
        public String getServiceName() {
            return null;
        }
```

### RedundantMethodOverride
Method `isEnable()` is identical to its super method
in `dubbo-kubernetes/src/main/java/org/apache/dubbo/registry/kubernetes/NopKubernetesMeshEnvListener.java`
#### Snippet
```java

    @Override
    public boolean isEnable() {
        return false;
    }
```

### RedundantMethodOverride
Method `getUrl()` is identical to its super method
in `dubbo-registry/dubbo-registry-multicast/src/main/java/org/apache/dubbo/registry/multicast/MulticastServiceDiscovery.java`
#### Snippet
```java

    @Override
    public URL getUrl() {
        return registryURL;
    }
```

## RuleId[id=IntegerMultiplicationImplicitCastToLong]
### IntegerMultiplicationImplicitCastToLong
timeout \* 2: integer multiplication implicitly cast to long
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/AdaptiveMetrics.java`
#### Snippet
```java

        //If the time more than 2 times, mandatory selected
        if (System.currentTimeMillis() - metrics.pickTime > timeout * 2) {
            return 0;
        }
```

## RuleId[id=OptionalAssignedToNull]
### OptionalAssignedToNull
Null is used for 'Optional' type in declaration
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/configcenter/file/FileSystemDynamicConfiguration.java`
#### Snippet
```java

    private static Optional<WatchService> newWatchService() {
        Optional<WatchService> watchService = null;
        FileSystem fileSystem = FileSystems.getDefault();
        try {
```

## RuleId[id=ReturnFromFinallyBlock]
### ReturnFromFinallyBlock
'return' inside 'finally' block
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/NetUtils.java`
#### Snippet
```java
                } finally {
                    if (matched) {
                        return true;
                    }
                    if (networkInterfaceDisplayName.equals(trimIgnoredInterface)) {
```

### ReturnFromFinallyBlock
'return' inside 'finally' block
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/NetUtils.java`
#### Snippet
```java
                    }
                    if (networkInterfaceDisplayName.equals(trimIgnoredInterface)) {
                        return true;
                    }
                }
```

## RuleId[id=MismatchedCollectionQueryUpdate]
### MismatchedCollectionQueryUpdate
Contents of collection `globalPermittedSerializationIds` are updated, but never queried
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/protocol/PermittedSerializationKeeper.java`
#### Snippet
```java
public class PermittedSerializationKeeper {
    private final ConcurrentMap<String, Set<Byte>> serviceToSerializationId = new ConcurrentHashMap<>();
    private final Set<Byte> globalPermittedSerializationIds = new ConcurrentHashSet<>();

    public void registerService(URL url) {
```

### MismatchedCollectionQueryUpdate
Contents of collection `ID_SERIALIZATIONNAME_MAP` are updated, but never queried
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/transport/CodecSupport.java`
#### Snippet
```java
    private static final ErrorTypeAwareLogger logger = LoggerFactory.getErrorTypeAwareLogger(CodecSupport.class);
    private static Map<Byte, Serialization> ID_SERIALIZATION_MAP = new HashMap<Byte, Serialization>();
    private static Map<Byte, String> ID_SERIALIZATIONNAME_MAP = new HashMap<Byte, String>();
    private static Map<String, Byte> SERIALIZATIONNAME_ID_MAP = new HashMap<String, Byte>();
    // Cache null object serialize results, for heartbeat request/response serialize use.
```

## RuleId[id=PointlessBitwiseExpression]
### PointlessBitwiseExpression
`(b[off + 3] & 0xFF) << 0` can be replaced with '(b\[off + 3\] \& 0xFF)'
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/Bytes.java`
#### Snippet
```java
     */
    public static float bytes2float(byte[] b, int off) {
        int i = ((b[off + 3] & 0xFF) << 0) +
                ((b[off + 2] & 0xFF) << 8) +
                ((b[off + 1] & 0xFF) << 16) +
```

### PointlessBitwiseExpression
`(b[off + 1] & 0xFF) << 0` can be replaced with '(b\[off + 1\] \& 0xFF)'
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/Bytes.java`
#### Snippet
```java
     */
    public static short bytes2short(byte[] b, int off) {
        return (short) (((b[off + 1] & 0xFF) << 0) +
                ((b[off + 0]) << 8));
    }
```

### PointlessBitwiseExpression
`(b[off + 7] & 0xFFL) << 0` can be replaced with '(b\[off + 7\] \& 0xFFL)'
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/Bytes.java`
#### Snippet
```java
     */
    public static double bytes2double(byte[] b, int off) {
        long j = ((b[off + 7] & 0xFFL) << 0) +
                ((b[off + 6] & 0xFFL) << 8) +
                ((b[off + 5] & 0xFFL) << 16) +
```

### PointlessBitwiseExpression
`(b[off + 7] & 0xFFL) << 0` can be replaced with '(b\[off + 7\] \& 0xFFL)'
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/Bytes.java`
#### Snippet
```java
     */
    public static long bytes2long(byte[] b, int off) {
        return ((b[off + 7] & 0xFFL) << 0) +
                ((b[off + 6] & 0xFFL) << 8) +
                ((b[off + 5] & 0xFFL) << 16) +
```

### PointlessBitwiseExpression
`(b[off + 3] & 0xFF) << 0` can be replaced with '(b\[off + 3\] \& 0xFF)'
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/Bytes.java`
#### Snippet
```java
     */
    public static int bytes2int(byte[] b, int off) {
        return ((b[off + 3] & 0xFF) << 0) +
                ((b[off + 2] & 0xFF) << 8) +
                ((b[off + 1] & 0xFF) << 16) +
```

### PointlessBitwiseExpression
`1 << 0` can be replaced with '1'
in `dubbo-plugin/dubbo-qos/src/main/java/org/apache/dubbo/qos/textui/TTable.java`
#### Snippet
```java
         * border outer top
         */
        public static final int BORDER_OUTER_TOP = 1 << 0;

        /**
```

## RuleId[id=StringConcatenationInLoops]
### StringConcatenationInLoops
String concatenation `+` in loop
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/rest/PathUtil.java`
#### Snippet
```java
            }

            pat = pat + string + SEPARATOR;
        }

```

## RuleId[id=UnusedAssignment]
### UnusedAssignment
Variable `protocol` initializer `""` is redundant
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/bootstrap/builders/InternalServiceConfigBuilder.java`
#### Snippet
```java
     */
    private String getRelatedOrDefaultProtocol() {
        String protocol = "";
        // <dubbo:consumer/>
        List<ModuleModel> moduleModels = applicationModel.getPubModuleModels();
```

### UnusedAssignment
Variable `metricsReporter` initializer `null` is redundant
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/deploy/DefaultApplicationDeployer.java`
#### Snippet
```java
        collector.setThreadpoolCollectEnabled(Optional.ofNullable(metricsConfig.getEnableThreadpool()).orElse(true));
        MetricsReporterFactory metricsReporterFactory = getExtensionLoader(MetricsReporterFactory.class).getAdaptiveExtension();
        MetricsReporter metricsReporter = null;
        try {
            metricsReporter = metricsReporterFactory.createMetricsReporter(metricsConfig.toUrl());
```

### UnusedAssignment
Variable `beanMetadata` initializer `null` is redundant
in `dubbo-spring-boot/dubbo-spring-boot-compatible/actuator/src/main/java/org/apache/dubbo/spring/boot/actuate/endpoint/metadata/DubboReferencesMetadata.java`
#### Snippet
```java
            ReferenceConfig referenceConfig = referenceBean.getReferenceConfig();

            Map<String, Object> beanMetadata = null;
            if (referenceConfig != null) {
                beanMetadata = resolveBeanMetadata(referenceConfig);
```

### UnusedAssignment
Variable `mediaType` initializer `MediaType.TEXT_PLAIN` is redundant
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/RestInvoker.java`
#### Snippet
```java
                    try {
                        int responseCode = r.getResponseCode();
                        MediaType mediaType = MediaType.TEXT_PLAIN;

                        if (responseCode == 404) {
```

### UnusedAssignment
Variable `springMultiValueMapImplClass` initializer `null` is redundant
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/MultiValueCreator.java`
#### Snippet
```java
    private final static String JAVAX_MultiValueMap = "javax.ws.rs.core.MultivaluedMap";

    private static Class springMultiValueMapImplClass = null;
    private static Class springMultiValueMapClass = null;
    private static Method springMultiValueMapAdd = null;
```

### UnusedAssignment
Variable `jaxrsMultiValueMapClass` initializer `null` is redundant
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/MultiValueCreator.java`
#### Snippet
```java

    private static Class jaxrsMultiValueMapImplClass = null;
    private static Class jaxrsMultiValueMapClass = null;

    private static Method jaxrsMultiValueMapAdd = null;
```

### UnusedAssignment
Variable `jaxrsMultiValueMapAdd` initializer `null` is redundant
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/MultiValueCreator.java`
#### Snippet
```java
    private static Class jaxrsMultiValueMapClass = null;

    private static Method jaxrsMultiValueMapAdd = null;

    static {
```

### UnusedAssignment
Variable `springMultiValueMapClass` initializer `null` is redundant
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/MultiValueCreator.java`
#### Snippet
```java

    private static Class springMultiValueMapImplClass = null;
    private static Class springMultiValueMapClass = null;
    private static Method springMultiValueMapAdd = null;

```

### UnusedAssignment
Variable `springMultiValueMapAdd` initializer `null` is redundant
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/MultiValueCreator.java`
#### Snippet
```java
    private static Class springMultiValueMapImplClass = null;
    private static Class springMultiValueMapClass = null;
    private static Method springMultiValueMapAdd = null;

    private static Class jaxrsMultiValueMapImplClass = null;
```

### UnusedAssignment
Variable `jaxrsMultiValueMapImplClass` initializer `null` is redundant
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/MultiValueCreator.java`
#### Snippet
```java
    private static Method springMultiValueMapAdd = null;

    private static Class jaxrsMultiValueMapImplClass = null;
    private static Class jaxrsMultiValueMapClass = null;

```

### UnusedAssignment
The value `serviceConfig.getInterfaceClass()` assigned to `cla` is never used
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ModuleServiceRepository.java`
#### Snippet
```java
        if (serviceConfig != null) {
            classLoader = serviceConfig.getInterfaceClassLoader();
            cla = serviceConfig.getInterfaceClass();
        }
        ProviderModel providerModel = new ProviderModel(serviceKey, serviceInstance, serviceModel,
```

### UnusedAssignment
Variable `mMark` initializer `0` is redundant
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/UnsafeByteArrayInputStream.java`
#### Snippet
```java
    protected byte[] mData;

    protected int mPosition, mLimit, mMark = 0;

    public UnsafeByteArrayInputStream(byte[] buf) {
```

### UnusedAssignment
The value changed at `r++` is never used
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/Bytes.java`
#### Snippet
```java

        if (rem == 2) {
            int c1 = indexOf(code, str.charAt(r++)), c2 = indexOf(code, str.charAt(r++));

            b[w++] = (byte) ((c1 << 2) | (c2 >> 4));
```

### UnusedAssignment
The value changed at `w++` is never used
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/Bytes.java`
#### Snippet
```java
            int c1 = indexOf(code, str.charAt(r++)), c2 = indexOf(code, str.charAt(r++));

            b[w++] = (byte) ((c1 << 2) | (c2 >> 4));
        } else if (rem == 3) {
            int c1 = indexOf(code, str.charAt(r++)), c2 = indexOf(code, str.charAt(r++)), c3 = indexOf(code, str.charAt(r++));
```

### UnusedAssignment
The value changed at `r++` is never used
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/Bytes.java`
#### Snippet
```java
            b[w++] = (byte) ((c1 << 2) | (c2 >> 4));
        } else if (rem == 3) {
            int c1 = indexOf(code, str.charAt(r++)), c2 = indexOf(code, str.charAt(r++)), c3 = indexOf(code, str.charAt(r++));

            b[w++] = (byte) ((c1 << 2) | (c2 >> 4));
```

### UnusedAssignment
The value changed at `w++` is never used
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/Bytes.java`
#### Snippet
```java

            b[w++] = (byte) ((c1 << 2) | (c2 >> 4));
            b[w++] = (byte) ((c2 << 4) | (c3 >> 2));
        }
        return b;
```

### UnusedAssignment
The value changed at `r++` is never used
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/Bytes.java`
#### Snippet
```java

        if (rem == 2) {
            int c1 = t[str.charAt(r++)], c2 = t[str.charAt(r++)];

            b[w++] = (byte) ((c1 << 2) | (c2 >> 4));
```

### UnusedAssignment
The value changed at `w++` is never used
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/Bytes.java`
#### Snippet
```java
            int c1 = t[str.charAt(r++)], c2 = t[str.charAt(r++)];

            b[w++] = (byte) ((c1 << 2) | (c2 >> 4));
        } else if (rem == 3) {
            int c1 = t[str.charAt(r++)], c2 = t[str.charAt(r++)], c3 = t[str.charAt(r++)];
```

### UnusedAssignment
The value changed at `r++` is never used
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/Bytes.java`
#### Snippet
```java
            b[w++] = (byte) ((c1 << 2) | (c2 >> 4));
        } else if (rem == 3) {
            int c1 = t[str.charAt(r++)], c2 = t[str.charAt(r++)], c3 = t[str.charAt(r++)];

            b[w++] = (byte) ((c1 << 2) | (c2 >> 4));
```

### UnusedAssignment
The value changed at `w++` is never used
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/Bytes.java`
#### Snippet
```java

            b[w++] = (byte) ((c1 << 2) | (c2 >> 4));
            b[w++] = (byte) ((c2 << 4) | (c3 >> 2));
        }
        return b;
```

### UnusedAssignment
The value changed at `r++` is never used
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/Bytes.java`
#### Snippet
```java

        if (rem == 1) {
            int b1 = bs[r++] & MASK8;
            cs[w++] = code[b1 >> 2];
            cs[w++] = code[(b1 << 4) & MASK6];
```

### UnusedAssignment
The value changed at `w++` is never used
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/Bytes.java`
#### Snippet
```java
            if (pad) {
                cs[w++] = code[64];
                cs[w++] = code[64];
            }
        } else if (rem == 2) {
```

### UnusedAssignment
The value changed at `r++` is never used
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/Bytes.java`
#### Snippet
```java
            }
        } else if (rem == 2) {
            int b1 = bs[r++] & MASK8, b2 = bs[r++] & MASK8;
            cs[w++] = code[b1 >> 2];
            cs[w++] = code[(b1 << 4) & MASK6 | (b2 >> 4)];
```

### UnusedAssignment
The value changed at `w++` is never used
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/Bytes.java`
#### Snippet
```java
            cs[w++] = code[(b2 << 2) & MASK6];
            if (pad) {
                cs[w++] = code[64];
            }
        }
```

### UnusedAssignment
Variable `value` initializer `null` is redundant
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/PojoUtils.java`
#### Snippet
```java
            }
            String methodName = method.getName();
            Object value = null;
            if (methodName.length() > 3 && methodName.startsWith("get")) {
                value = map.get(methodName.substring(3, 4).toLowerCase() + methodName.substring(4));
```

### UnusedAssignment
Variable `value` initializer `null` is redundant
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/MethodUtils.java`
#### Snippet
```java
        Class[] parameterTypes = resolveTypes(methodParameters);
        Method method = findMethod(type, methodName, parameterTypes);
        T value = null;

        if (method == null) {
```

### UnusedAssignment
Variable `localAddress` initializer `null` is redundant
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/NetUtils.java`
#### Snippet
```java

    private static InetAddress getLocalAddress0() {
        InetAddress localAddress = null;

        // @since 2.7.6, choose the {@link NetworkInterface} first
```

### UnusedAssignment
Variable `watchService` initializer `null` is redundant
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/configcenter/file/FileSystemDynamicConfiguration.java`
#### Snippet
```java

    private static Optional<WatchService> newWatchService() {
        Optional<WatchService> watchService = null;
        FileSystem fileSystem = FileSystems.getDefault();
        try {
```

### UnusedAssignment
Variable `beanInfo` initializer `null` is redundant
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static <T> T getProperty(Object bean, String methodName) {
        Class<?> beanClass = bean.getClass();
        BeanInfo beanInfo = null;
        T propertyValue = null;

```

### UnusedAssignment
The value `StringUtils.join(parameterTypes)` assigned to `signature` is never used
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
        String signature = clazz.getName() + "." + methodName;
        if (parameterTypes != null && parameterTypes.length > 0) {
            signature += StringUtils.join(parameterTypes);
        }
        Method method;
```

### UnusedAssignment
Variable `result` initializer `null` is redundant
in `dubbo-common/src/main/java/org/apache/dubbo/common/function/ThrowableFunction.java`
#### Snippet
```java
     */
    default R execute(T t) throws RuntimeException {
        R result = null;
        try {
            result = apply(t);
```

### UnusedAssignment
Variable `v` initializer `null` is redundant
in `dubbo-common/src/main/java/org/apache/dubbo/common/threadlocal/InternalThreadLocal.java`
#### Snippet
```java

    private V initialize(InternalThreadLocalMap threadLocalMap) {
        V v = null;
        try {
            v = initialValue();
```

### UnusedAssignment
Variable `enabled` initializer `true` is redundant
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/integration/RegistryDirectory.java`
#### Snippet
```java
            if (invoker == null) { // Not in the cache, refer again
                try {
                    boolean enabled = true;
                    if (url.hasParameter(DISABLED_KEY)) {
                        enabled = !url.getParameter(DISABLED_KEY, false);
```

### UnusedAssignment
Variable `consumerConfig` initializer `null` is redundant
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/reference/ReferenceCreator.java`
#### Snippet
```java

    private void configureConsumerConfig(ReferenceConfig<?> referenceBean) {
        ConsumerConfig consumerConfig = null;
        Object consumer = AnnotationUtils.getAttribute(attributes, "consumer");
        if (consumer != null) {
```

### UnusedAssignment
Variable `referenceProps` initializer `null` is redundant
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/reference/ReferenceBeanSupport.java`
#### Snippet
```java

    public static Map<String, Object> getReferenceAttributes(BeanDefinition beanDefinition) {
        Map<String, Object> referenceProps = null;
        if (beanDefinition.hasAttribute(Constants.REFERENCE_PROPS)) {
            referenceProps = (Map<String, Object>) beanDefinition.getAttribute(Constants.REFERENCE_PROPS);
```

### UnusedAssignment
Variable `ctClass` initializer `null` is redundant
in `dubbo-filter/dubbo-filter-validation/src/main/java/org/apache/dubbo/validation/support/jvalidation/JValidator.java`
#### Snippet
```java
        ClassPool pool = ClassGenerator.getClassPool(clazz.getClassLoader());
        synchronized (parameterClassName.intern()) {
            CtClass ctClass = null;
            try {
                ctClass = pool.getCtClass(parameterClassName);
```

### UnusedAssignment
Variable `ctClass` initializer `null` is redundant
in `dubbo-filter/dubbo-filter-validation/src/main/java/org/apache/dubbo/validation/support/jvalidation/JValidatorNew.java`
#### Snippet
```java
        ClassPool pool = ClassGenerator.getClassPool(clazz.getClassLoader());
        synchronized (parameterClassName.intern()) {
            CtClass ctClass = null;
            try {
                ctClass = pool.getCtClass(parameterClassName);
```

### UnusedAssignment
Variable `n` initializer `0` is redundant
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/container/page/ResourceFilter.java`
#### Snippet
```java
            ByteArrayOutputStream output = new ByteArrayOutputStream();
            byte[] buffer = new byte[8192];
            int n = 0;
            while (-1 != (n = input.read(buffer))) {
                output.write(buffer, 0, n);
```

### UnusedAssignment
Variable `url` initializer `null` is redundant
in `dubbo-rpc/dubbo-rpc-dubbo/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/CallbackServiceCodec.java`
#### Snippet
```java
        // if it's a callback, create proxy on client side, callback interface on client side can be invoked through channel
        // need get URL from channel and env when decode
        URL url = null;
        try {
            url = dubboProtocol.getInvoker(channel, inv).getUrl();
```

### UnusedAssignment
Variable `size` initializer `0` is redundant
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/telnet/support/command/LogTelnetHandler.java`
#### Snippet
```java
    @Override
    public String telnet(Channel channel, String message) {
        long size = 0;
        File file = LoggerFactory.getFile();
        StringBuilder buf = new StringBuilder();
```

### UnusedAssignment
The value `0` assigned to `size` is never used
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/telnet/support/command/LogTelnetHandler.java`
#### Snippet
```java
                    }
                } else {
                    size = 0;
                    buf.append("\r\nMESSAGE: log file not exists or log appender is console .");
                }
```

### UnusedAssignment
Variable `values` initializer `null` is redundant
in `dubbo-remoting/dubbo-remoting-http/src/main/java/org/apache/dubbo/remoting/http/RequestTemplate.java`
#### Snippet
```java
        }

        Collection<String> values = null;
        if (!maps.containsKey(key)) {
            values = new HashSet<>();
```

### UnusedAssignment
Variable `contextPath` initializer `""` is redundant
in `dubbo-remoting/dubbo-remoting-http/src/main/java/org/apache/dubbo/remoting/http/RequestTemplate.java`
#### Snippet
```java
    private String protocol = "http://";
    private final Invocation invocation;
    private String contextPath = "";
    private Class<?> bodyType;

```

### UnusedAssignment
Variable `httpRequest` initializer `null` is redundant
in `dubbo-remoting/dubbo-remoting-http/src/main/java/org/apache/dubbo/remoting/http/restclient/HttpClientRestClient.java`
#### Snippet
```java
    public CompletableFuture<RestResult> send(RequestTemplate requestTemplate) {

        HttpRequestBase httpRequest = null;
        String httpMethod = requestTemplate.getHttpMethod();

```

### UnusedAssignment
Variable `httpUriRequest` initializer `null` is redundant
in `dubbo-remoting/dubbo-remoting-http/src/main/java/org/apache/dubbo/remoting/http/restclient/HttpClientRestClient.java`
#### Snippet
```java
    protected HttpRequestBase createHttpUriRequest(String httpMethod, RequestTemplate requestTemplate) {
        String uri = requestTemplate.getURL();
        HttpRequestBase httpUriRequest = null;
        if (HttpGet.METHOD_NAME.equals(httpMethod)) {
            httpUriRequest = new HttpGet(uri);
```

## RuleId[id=StringEqualsCharSequence]
### StringEqualsCharSequence
`String.equals()` called with 'CharSequence' argument
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/util/MemberUtils.java`
#### Snippet
```java
        for (int i = 0; i < size; i++) {
            VariableElement parameter = parameters.get(i);
            if (!Objects.equals(parameter.asType().toString(), parameterTypes[i])) {
                return false;
            }
```

## RuleId[id=JavadocReference]
### JavadocReference
Cannot resolve symbol `org.springframework.boot.actuate.autoconfigure.tracing.MicrometerTracingAutoConfiguration`
in `dubbo-spring-boot/dubbo-spring-boot-observability-starters/autoconfigure/src/main/java/org/apache/dubbo/spring/boot/observability/autoconfigure/DubboMicrometerTracingAutoConfiguration.java`
#### Snippet
```java

/**
 * copy from {@link org.springframework.boot.actuate.autoconfigure.tracing.MicrometerTracingAutoConfiguration}
 * this class is available starting from Boot 3.0. It's not available if you're using Boot < 3.0
 */
```

### JavadocReference
Cannot resolve symbol `org.springframework.boot.actuate.autoconfigure.observation.ObservationHandlerGrouping`
in `dubbo-spring-boot/dubbo-spring-boot-observability-starters/autoconfigure/src/main/java/org/apache/dubbo/spring/boot/observability/autoconfigure/ObservationHandlerGrouping.java`
#### Snippet
```java
/**
 * Groups {@link ObservationHandler ObservationHandlers} by type.
 * copy from {@link org.springframework.boot.actuate.autoconfigure.observation.ObservationHandlerGrouping}
 * this class is available starting from Boot 3.0. It's not available if you're using Boot < 3.0
 *
```

### JavadocReference
Cannot resolve symbol `EnableAutoConfiguration`
in `dubbo-spring-boot/dubbo-spring-boot-observability-starters/autoconfigure/src/main/java/org/apache/dubbo/spring/boot/observability/autoconfigure/exporter/zipkin/ZipkinAutoConfiguration.java`
#### Snippet
```java

/**
 * {@link EnableAutoConfiguration Auto-configuration} for Zipkin.
 * <p>
 * It uses imports on {@link ZipkinConfigurations} to guarantee the correct configuration ordering.
```

### JavadocReference
Cannot resolve symbol `WebClient.Builder`
in `dubbo-spring-boot/dubbo-spring-boot-observability-starters/autoconfigure/src/main/java/org/apache/dubbo/spring/boot/observability/autoconfigure/exporter/zipkin/customizer/ZipkinWebClientBuilderCustomizer.java`
#### Snippet
```java
/**
 * Callback interface that can be implemented by beans wishing to customize the
 * {@link WebClient.Builder} used to send spans to Zipkin.
 *
 * @since 3.2.0
```

### JavadocReference
Cannot resolve symbol `RestTemplateBuilder`
in `dubbo-spring-boot/dubbo-spring-boot-observability-starters/autoconfigure/src/main/java/org/apache/dubbo/spring/boot/observability/autoconfigure/exporter/zipkin/customizer/ZipkinRestTemplateBuilderCustomizer.java`
#### Snippet
```java
/**
 * Callback interface that can be implemented by beans wishing to customize the
 * {@link RestTemplateBuilder} used to send spans to Zipkin.
 *
 * @since 3.2.0
```

### JavadocReference
Symbol `ApplicationShutdownHooks` is inaccessible from here
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/DubboShutdownHook.java`
#### Snippet
```java
 * The shutdown hook thread to do the cleanup stuff.
 * This is a singleton in order to ensure there is only one shutdown hook registered.
 * Because {@link ApplicationShutdownHooks} use {@link java.util.IdentityHashMap}
 * to store the shutdown hooks.
 */
```

### JavadocReference
Cannot resolve symbol `Endpoint`
in `dubbo-spring-boot/dubbo-spring-boot-actuator/src/main/java/org/apache/dubbo/spring/boot/actuate/endpoint/DubboReferencesMetadataEndpoint.java`
#### Snippet
```java

/**
 * {@link DubboReference} Metadata {@link Endpoint}
 *
 * @since 2.7.0
```

### JavadocReference
Cannot resolve symbol `Endpoint`
in `dubbo-spring-boot/dubbo-spring-boot-actuator/src/main/java/org/apache/dubbo/spring/boot/actuate/endpoint/DubboMetadataEndpoint.java`
#### Snippet
```java

/**
 * Actuator {@link Endpoint} to expose Dubbo Meta Data
 *
 * @see Endpoint
```

### JavadocReference
Cannot resolve symbol `Endpoint`
in `dubbo-spring-boot/dubbo-spring-boot-actuator/src/main/java/org/apache/dubbo/spring/boot/actuate/endpoint/DubboMetadataEndpoint.java`
#### Snippet
```java
 * Actuator {@link Endpoint} to expose Dubbo Meta Data
 *
 * @see Endpoint
 * @since 2.7.0
 */
```

### JavadocReference
Cannot resolve symbol `Endpoint`
in `dubbo-spring-boot/dubbo-spring-boot-actuator/src/main/java/org/apache/dubbo/spring/boot/actuate/endpoint/DubboServicesMetadataEndpoint.java`
#### Snippet
```java

/**
 * {@link DubboService} Metadata {@link Endpoint}
 *
 * @since 2.7.0
```

### JavadocReference
Cannot resolve symbol `Endpoint`
in `dubbo-spring-boot/dubbo-spring-boot-actuator/src/main/java/org/apache/dubbo/spring/boot/actuate/endpoint/DubboPropertiesMetadataEndpoint.java`
#### Snippet
```java

/**
 * Dubbo Properties {@link Endpoint}
 *
 * @since 2.7.0
```

### JavadocReference
Cannot resolve symbol `Endpoint`
in `dubbo-spring-boot/dubbo-spring-boot-actuator/src/main/java/org/apache/dubbo/spring/boot/actuate/endpoint/DubboConfigsMetadataEndpoint.java`
#### Snippet
```java

/**
 * Dubbo Configs Metadata {@link Endpoint}
 *
 * @since 2.7.0
```

### JavadocReference
Cannot resolve symbol `Endpoint`
in `dubbo-spring-boot/dubbo-spring-boot-actuator/src/main/java/org/apache/dubbo/spring/boot/actuate/endpoint/condition/CompatibleConditionalOnEnabledEndpoint.java`
#### Snippet
```java
    /**
     * The endpoint type that should be checked. Inferred when the return type of the
     * {@code @Bean} method is either an {@link Endpoint @Endpoint} or an
     * {@link EndpointExtension @EndpointExtension}.
     *
```

### JavadocReference
Cannot resolve symbol `EndpointExtension`
in `dubbo-spring-boot/dubbo-spring-boot-actuator/src/main/java/org/apache/dubbo/spring/boot/actuate/endpoint/condition/CompatibleConditionalOnEnabledEndpoint.java`
#### Snippet
```java
     * The endpoint type that should be checked. Inferred when the return type of the
     * {@code @Bean} method is either an {@link Endpoint @Endpoint} or an
     * {@link EndpointExtension @EndpointExtension}.
     *
     * @return the endpoint type to check
```

### JavadocReference
Cannot resolve symbol `Conditional`
in `dubbo-spring-boot/dubbo-spring-boot-actuator/src/main/java/org/apache/dubbo/spring/boot/actuate/endpoint/condition/CompatibleConditionalOnEnabledEndpoint.java`
#### Snippet
```java

/**
 * {@link Conditional} that checks whether or not an endpoint is enabled, which is compatible with
 * org.springframework.boot.actuate.autoconfigure.endpoint.condition.ConditionalOnEnabledEndpoint ([2.0.x, 2.2.x])
 * org.springframework.boot.actuate.autoconfigure.endpoint.condition.ConditionalOnAvailableEndpoint
```

### JavadocReference
Cannot resolve symbol `Conditional`
in `dubbo-spring-boot/dubbo-spring-boot-actuator/src/main/java/org/apache/dubbo/spring/boot/actuate/endpoint/condition/CompatibleOnEnabledEndpointCondition.java`
#### Snippet
```java

/**
 * {@link Conditional} that checks whether or not an endpoint is enabled, which is compatible with
 * org.springframework.boot.actuate.autoconfigure.endpoint.condition.OnEnabledEndpointCondition
 * and org.springframework.boot.actuate.autoconfigure.endpoint.condition.OnAvailableEndpointCondition
```

### JavadocReference
Cannot resolve symbol `Endpoint`
in `dubbo-spring-boot/dubbo-spring-boot-actuator/src/main/java/org/apache/dubbo/spring/boot/actuate/autoconfigure/DubboEndpointAnnotationAutoConfiguration.java`
#### Snippet
```java

/**
 * Dubbo {@link Endpoint @Endpoint} Auto-{@link Configuration} for Spring Boot Actuator 2.0
 *
 * @see Endpoint
```

### JavadocReference
Cannot resolve symbol `Configuration`
in `dubbo-spring-boot/dubbo-spring-boot-actuator/src/main/java/org/apache/dubbo/spring/boot/actuate/autoconfigure/DubboEndpointAnnotationAutoConfiguration.java`
#### Snippet
```java

/**
 * Dubbo {@link Endpoint @Endpoint} Auto-{@link Configuration} for Spring Boot Actuator 2.0
 *
 * @see Endpoint
```

### JavadocReference
Cannot resolve symbol `Endpoint`
in `dubbo-spring-boot/dubbo-spring-boot-actuator/src/main/java/org/apache/dubbo/spring/boot/actuate/autoconfigure/DubboEndpointAnnotationAutoConfiguration.java`
#### Snippet
```java
 * Dubbo {@link Endpoint @Endpoint} Auto-{@link Configuration} for Spring Boot Actuator 2.0
 *
 * @see Endpoint
 * @see Configuration
 * @since 2.7.0
```

### JavadocReference
Cannot resolve symbol `Configuration`
in `dubbo-spring-boot/dubbo-spring-boot-actuator/src/main/java/org/apache/dubbo/spring/boot/actuate/autoconfigure/DubboEndpointAnnotationAutoConfiguration.java`
#### Snippet
```java
 *
 * @see Endpoint
 * @see Configuration
 * @since 2.7.0
 */
```

### JavadocReference
Cannot resolve symbol `Endpoint`
in `dubbo-spring-boot/dubbo-spring-boot-compatible/actuator/src/main/java/org/apache/dubbo/spring/boot/actuate/endpoint/DubboEndpoint.java`
#### Snippet
```java

/**
 * Actuator {@link Endpoint} to expose Dubbo Meta Data
 *
 * @see Endpoint
```

### JavadocReference
Cannot resolve symbol `Endpoint`
in `dubbo-spring-boot/dubbo-spring-boot-compatible/actuator/src/main/java/org/apache/dubbo/spring/boot/actuate/endpoint/DubboEndpoint.java`
#### Snippet
```java
 * Actuator {@link Endpoint} to expose Dubbo Meta Data
 *
 * @see Endpoint
 * @since 2.7.0
 */
```

### JavadocReference
Cannot resolve symbol `HealthIndicator`
in `dubbo-spring-boot/dubbo-spring-boot-compatible/actuator/src/main/java/org/apache/dubbo/spring/boot/actuate/health/DubboHealthIndicatorProperties.java`
#### Snippet
```java

/**
 * Dubbo {@link HealthIndicator} Properties
 *
 * @see HealthIndicator
```

### JavadocReference
Cannot resolve symbol `HealthIndicator`
in `dubbo-spring-boot/dubbo-spring-boot-compatible/actuator/src/main/java/org/apache/dubbo/spring/boot/actuate/health/DubboHealthIndicatorProperties.java`
#### Snippet
```java
 * Dubbo {@link HealthIndicator} Properties
 *
 * @see HealthIndicator
 * @since 2.7.0
 */
```

### JavadocReference
Cannot resolve symbol `MvcEndpoint`
in `dubbo-spring-boot/dubbo-spring-boot-compatible/actuator/src/main/java/org/apache/dubbo/spring/boot/actuate/endpoint/mvc/DubboMvcEndpoint.java`
#### Snippet
```java

/**
 * {@link MvcEndpoint} to expose Dubbo Metadata
 *
 * @see MvcEndpoint
```

### JavadocReference
Cannot resolve symbol `MvcEndpoint`
in `dubbo-spring-boot/dubbo-spring-boot-compatible/actuator/src/main/java/org/apache/dubbo/spring/boot/actuate/endpoint/mvc/DubboMvcEndpoint.java`
#### Snippet
```java
 * {@link MvcEndpoint} to expose Dubbo Metadata
 *
 * @see MvcEndpoint
 * @since 2.7.0
 */
```

### JavadocReference
Cannot resolve symbol `HealthIndicator`
in `dubbo-spring-boot/dubbo-spring-boot-compatible/actuator/src/main/java/org/apache/dubbo/spring/boot/actuate/autoconfigure/DubboHealthIndicatorAutoConfiguration.java`
#### Snippet
```java
 * Dubbo {@link DubboHealthIndicator} Auto Configuration
 *
 * @see HealthIndicator
 * @since 2.7.0
 */
```

### JavadocReference
Cannot resolve symbol `Endpoint`
in `dubbo-spring-boot/dubbo-spring-boot-compatible/actuator/src/main/java/org/apache/dubbo/spring/boot/actuate/autoconfigure/DubboEndpointAutoConfiguration.java`
#### Snippet
```java

/**
 * Dubbo {@link Endpoint} Auto Configuration is compatible with Spring Boot Actuator 1.x
 *
 * @since 2.7.0
```

### JavadocReference
Cannot resolve symbol `Configuration`
in `dubbo-spring-boot/dubbo-spring-boot-compatible/actuator/src/main/java/org/apache/dubbo/spring/boot/actuate/autoconfigure/DubboEndpointMetadataAutoConfiguration.java`
#### Snippet
```java

/**
 * Dubbo Endpoints Metadata Auto-{@link Configuration}
 */
@ConditionalOnClass(name = {
```

### JavadocReference
Cannot resolve symbol `MvcEndpoint`
in `dubbo-spring-boot/dubbo-spring-boot-compatible/actuator/src/main/java/org/apache/dubbo/spring/boot/actuate/autoconfigure/DubboMvcEndpointManagementContextConfiguration.java`
#### Snippet
```java

/**
 * Dubbo {@link MvcEndpoint} {@link ManagementContextConfiguration} for Spring Boot 1.x
 *
 * @since 2.7.0
```

### JavadocReference
Cannot resolve symbol `ManagementContextConfiguration`
in `dubbo-spring-boot/dubbo-spring-boot-compatible/actuator/src/main/java/org/apache/dubbo/spring/boot/actuate/autoconfigure/DubboMvcEndpointManagementContextConfiguration.java`
#### Snippet
```java

/**
 * Dubbo {@link MvcEndpoint} {@link ManagementContextConfiguration} for Spring Boot 1.x
 *
 * @since 2.7.0
```

### JavadocReference
Cannot resolve symbol `HealthIndicator`
in `dubbo-spring-boot/dubbo-spring-boot-compatible/actuator/src/main/java/org/apache/dubbo/spring/boot/actuate/health/DubboHealthIndicator.java`
#### Snippet
```java

/**
 * Dubbo {@link HealthIndicator}
 *
 * @see HealthIndicator
```

### JavadocReference
Cannot resolve symbol `HealthIndicator`
in `dubbo-spring-boot/dubbo-spring-boot-compatible/actuator/src/main/java/org/apache/dubbo/spring/boot/actuate/health/DubboHealthIndicator.java`
#### Snippet
```java
 * Dubbo {@link HealthIndicator}
 *
 * @see HealthIndicator
 * @since 2.7.0
 */
```

### JavadocReference
Cannot resolve symbol `ApplicationContextInitializer`
in `dubbo-spring-boot/dubbo-spring-boot-compatible/autoconfigure/src/main/java/org/apache/dubbo/spring/boot/context/DubboApplicationContextInitializer.java`
#### Snippet
```java

/**
 * Dubbo {@link ApplicationContextInitializer} implementation
 *
 * @see ApplicationContextInitializer
```

### JavadocReference
Cannot resolve symbol `ApplicationContextInitializer`
in `dubbo-spring-boot/dubbo-spring-boot-compatible/autoconfigure/src/main/java/org/apache/dubbo/spring/boot/context/DubboApplicationContextInitializer.java`
#### Snippet
```java
 * Dubbo {@link ApplicationContextInitializer} implementation
 *
 * @see ApplicationContextInitializer
 * @since 2.7.1
 */
```

### JavadocReference
Cannot resolve symbol `CommonAnnotationBeanPostProcessor`
in `dubbo-spring-boot/dubbo-spring-boot-compatible/autoconfigure/src/main/java/org/apache/dubbo/spring/boot/beans/factory/config/ServiceBeanIdConflictProcessor.java`
#### Snippet
```java

    /**
     * Keep the order being higher than {@link CommonAnnotationBeanPostProcessor#getOrder()} that is
     * {@link Ordered#LOWEST_PRECEDENCE}
     *
```

### JavadocReference
Cannot resolve symbol `getOrder()`
in `dubbo-spring-boot/dubbo-spring-boot-compatible/autoconfigure/src/main/java/org/apache/dubbo/spring/boot/beans/factory/config/ServiceBeanIdConflictProcessor.java`
#### Snippet
```java

    /**
     * Keep the order being higher than {@link CommonAnnotationBeanPostProcessor#getOrder()} that is
     * {@link Ordered#LOWEST_PRECEDENCE}
     *
```

### JavadocReference
Cannot resolve symbol `Ordered`
in `dubbo-spring-boot/dubbo-spring-boot-compatible/autoconfigure/src/main/java/org/apache/dubbo/spring/boot/beans/factory/config/ServiceBeanIdConflictProcessor.java`
#### Snippet
```java
    /**
     * Keep the order being higher than {@link CommonAnnotationBeanPostProcessor#getOrder()} that is
     * {@link Ordered#LOWEST_PRECEDENCE}
     *
     * @return {@link Ordered#LOWEST_PRECEDENCE} +1
```

### JavadocReference
Cannot resolve symbol `LOWEST_PRECEDENCE`
in `dubbo-spring-boot/dubbo-spring-boot-compatible/autoconfigure/src/main/java/org/apache/dubbo/spring/boot/beans/factory/config/ServiceBeanIdConflictProcessor.java`
#### Snippet
```java
    /**
     * Keep the order being higher than {@link CommonAnnotationBeanPostProcessor#getOrder()} that is
     * {@link Ordered#LOWEST_PRECEDENCE}
     *
     * @return {@link Ordered#LOWEST_PRECEDENCE} +1
```

### JavadocReference
Cannot resolve symbol `Ordered`
in `dubbo-spring-boot/dubbo-spring-boot-compatible/autoconfigure/src/main/java/org/apache/dubbo/spring/boot/beans/factory/config/ServiceBeanIdConflictProcessor.java`
#### Snippet
```java
     * {@link Ordered#LOWEST_PRECEDENCE}
     *
     * @return {@link Ordered#LOWEST_PRECEDENCE} +1
     */
    @Override
```

### JavadocReference
Cannot resolve symbol `LOWEST_PRECEDENCE`
in `dubbo-spring-boot/dubbo-spring-boot-compatible/autoconfigure/src/main/java/org/apache/dubbo/spring/boot/beans/factory/config/ServiceBeanIdConflictProcessor.java`
#### Snippet
```java
     * {@link Ordered#LOWEST_PRECEDENCE}
     *
     * @return {@link Ordered#LOWEST_PRECEDENCE} +1
     */
    @Override
```

### JavadocReference
Cannot resolve symbol `CommonAnnotationBeanPostProcessor`
in `dubbo-spring-boot/dubbo-spring-boot-compatible/autoconfigure/src/main/java/org/apache/dubbo/spring/boot/beans/factory/config/ServiceBeanIdConflictProcessor.java`
#### Snippet
```java
 * Current implementation is a temporary resolution, and will be removed in the future.
 *
 * @see CommonAnnotationBeanPostProcessor
 * @since 2.7.7
 * @deprecated
```

### JavadocReference
Cannot resolve symbol `EnvironmentPostProcessor`
in `dubbo-spring-boot/dubbo-spring-boot-compatible/autoconfigure/src/main/java/org/apache/dubbo/spring/boot/env/DubboDefaultPropertiesEnvironmentPostProcessor.java`
#### Snippet
```java

/**
 * The lowest precedence {@link EnvironmentPostProcessor} processes
 * {@link SpringApplication#setDefaultProperties(Properties) Spring Boot default properties} for Dubbo
 * as late as possible before {@link ConfigurableApplicationContext#refresh() application context refresh}.
```

### JavadocReference
Cannot resolve symbol `SpringApplication`
in `dubbo-spring-boot/dubbo-spring-boot-compatible/autoconfigure/src/main/java/org/apache/dubbo/spring/boot/env/DubboDefaultPropertiesEnvironmentPostProcessor.java`
#### Snippet
```java
/**
 * The lowest precedence {@link EnvironmentPostProcessor} processes
 * {@link SpringApplication#setDefaultProperties(Properties) Spring Boot default properties} for Dubbo
 * as late as possible before {@link ConfigurableApplicationContext#refresh() application context refresh}.
 */
```

### JavadocReference
Cannot resolve symbol `setDefaultProperties(Properties)`
in `dubbo-spring-boot/dubbo-spring-boot-compatible/autoconfigure/src/main/java/org/apache/dubbo/spring/boot/env/DubboDefaultPropertiesEnvironmentPostProcessor.java`
#### Snippet
```java
/**
 * The lowest precedence {@link EnvironmentPostProcessor} processes
 * {@link SpringApplication#setDefaultProperties(Properties) Spring Boot default properties} for Dubbo
 * as late as possible before {@link ConfigurableApplicationContext#refresh() application context refresh}.
 */
```

### JavadocReference
Cannot resolve symbol `ConfigurableApplicationContext`
in `dubbo-spring-boot/dubbo-spring-boot-compatible/autoconfigure/src/main/java/org/apache/dubbo/spring/boot/env/DubboDefaultPropertiesEnvironmentPostProcessor.java`
#### Snippet
```java
 * The lowest precedence {@link EnvironmentPostProcessor} processes
 * {@link SpringApplication#setDefaultProperties(Properties) Spring Boot default properties} for Dubbo
 * as late as possible before {@link ConfigurableApplicationContext#refresh() application context refresh}.
 */
public class DubboDefaultPropertiesEnvironmentPostProcessor implements EnvironmentPostProcessor, Ordered {
```

### JavadocReference
Cannot resolve symbol `refresh()`
in `dubbo-spring-boot/dubbo-spring-boot-compatible/autoconfigure/src/main/java/org/apache/dubbo/spring/boot/env/DubboDefaultPropertiesEnvironmentPostProcessor.java`
#### Snippet
```java
 * The lowest precedence {@link EnvironmentPostProcessor} processes
 * {@link SpringApplication#setDefaultProperties(Properties) Spring Boot default properties} for Dubbo
 * as late as possible before {@link ConfigurableApplicationContext#refresh() application context refresh}.
 */
public class DubboDefaultPropertiesEnvironmentPostProcessor implements EnvironmentPostProcessor, Ordered {
```

### JavadocReference
Cannot resolve symbol `MutablePropertySources`
in `dubbo-spring-boot/dubbo-spring-boot-compatible/autoconfigure/src/main/java/org/apache/dubbo/spring/boot/env/DubboDefaultPropertiesEnvironmentPostProcessor.java`
#### Snippet
```java
     * Copy from BusEnvironmentPostProcessor#addOrReplace(MutablePropertySources, Map)
     *
     * @param propertySources {@link MutablePropertySources}
     * @param map             Default Dubbo Properties
     */
```

### JavadocReference
Cannot resolve symbol `PropertySource`
in `dubbo-spring-boot/dubbo-spring-boot-compatible/autoconfigure/src/main/java/org/apache/dubbo/spring/boot/env/DubboDefaultPropertiesEnvironmentPostProcessor.java`
#### Snippet
```java

    /**
     * The name of default {@link PropertySource} defined in SpringApplication#configurePropertySources method.
     */
    public static final String PROPERTY_SOURCE_NAME = "defaultProperties";
```

### JavadocReference
Cannot resolve symbol `ApplicationListener`
in `dubbo-spring-boot/dubbo-spring-boot-compatible/autoconfigure/src/main/java/org/apache/dubbo/spring/boot/context/event/WelcomeLogoApplicationListener.java`
#### Snippet
```java

/**
 * Dubbo Welcome Logo {@link ApplicationListener}
 *
 * @see ApplicationListener
```

### JavadocReference
Cannot resolve symbol `ApplicationListener`
in `dubbo-spring-boot/dubbo-spring-boot-compatible/autoconfigure/src/main/java/org/apache/dubbo/spring/boot/context/event/WelcomeLogoApplicationListener.java`
#### Snippet
```java
 * Dubbo Welcome Logo {@link ApplicationListener}
 *
 * @see ApplicationListener
 * @since 2.7.0
 */
```

### JavadocReference
Cannot resolve symbol `ApplicationListener`
in `dubbo-spring-boot/dubbo-spring-boot-compatible/autoconfigure/src/main/java/org/apache/dubbo/spring/boot/context/event/DubboConfigBeanDefinitionConflictApplicationListener.java`
#### Snippet
```java

/**
 * The {@link ApplicationListener} class for Dubbo Config {@link BeanDefinition Bean Definition} to resolve conflict
 *
 * @see BeanDefinition
```

### JavadocReference
Cannot resolve symbol `BeanDefinition`
in `dubbo-spring-boot/dubbo-spring-boot-compatible/autoconfigure/src/main/java/org/apache/dubbo/spring/boot/context/event/DubboConfigBeanDefinitionConflictApplicationListener.java`
#### Snippet
```java

/**
 * The {@link ApplicationListener} class for Dubbo Config {@link BeanDefinition Bean Definition} to resolve conflict
 *
 * @see BeanDefinition
```

### JavadocReference
Cannot resolve symbol `BeanDefinition`
in `dubbo-spring-boot/dubbo-spring-boot-compatible/autoconfigure/src/main/java/org/apache/dubbo/spring/boot/context/event/DubboConfigBeanDefinitionConflictApplicationListener.java`
#### Snippet
```java
 * The {@link ApplicationListener} class for Dubbo Config {@link BeanDefinition Bean Definition} to resolve conflict
 *
 * @see BeanDefinition
 * @see ApplicationListener
 * @since 2.7.5
```

### JavadocReference
Cannot resolve symbol `ApplicationListener`
in `dubbo-spring-boot/dubbo-spring-boot-compatible/autoconfigure/src/main/java/org/apache/dubbo/spring/boot/context/event/DubboConfigBeanDefinitionConflictApplicationListener.java`
#### Snippet
```java
 *
 * @see BeanDefinition
 * @see ApplicationListener
 * @since 2.7.5
 */
```

### JavadocReference
Cannot resolve symbol `BeanDefinitionRegistry`
in `dubbo-spring-boot/dubbo-spring-boot-compatible/autoconfigure/src/main/java/org/apache/dubbo/spring/boot/context/event/DubboConfigBeanDefinitionConflictApplicationListener.java`
#### Snippet
```java
     * Resolve the unique {@link ApplicationConfig} Bean
     *
     * @param registry    {@link BeanDefinitionRegistry} instance
     * @param beanFactory {@link ConfigurableListableBeanFactory} instance
     * @see EnableDubboConfig
```

### JavadocReference
Cannot resolve symbol `ConfigurableListableBeanFactory`
in `dubbo-spring-boot/dubbo-spring-boot-compatible/autoconfigure/src/main/java/org/apache/dubbo/spring/boot/context/event/DubboConfigBeanDefinitionConflictApplicationListener.java`
#### Snippet
```java
     *
     * @param registry    {@link BeanDefinitionRegistry} instance
     * @param beanFactory {@link ConfigurableListableBeanFactory} instance
     * @see EnableDubboConfig
     */
```

### JavadocReference
Cannot resolve symbol `Configuration`
in `dubbo-spring-boot/dubbo-spring-boot-compatible/autoconfigure/src/main/java/org/apache/dubbo/spring/boot/autoconfigure/DubboListenerAutoConfiguration.java`
#### Snippet
```java

/**
 * Dubbo Listener Auto-{@link Configuration}
 *
 * @since 3.0.4
```

### JavadocReference
Cannot resolve symbol `ApplicationListener`
in `dubbo-spring-boot/dubbo-spring-boot-compatible/autoconfigure/src/main/java/org/apache/dubbo/spring/boot/context/event/AwaitingNonWebApplicationListener.java`
#### Snippet
```java

/**
 * Awaiting Non-Web Spring Boot {@link ApplicationListener}
 *
 * @since 2.7.0
```

### JavadocReference
Cannot resolve symbol `Environment`
in `dubbo-spring-boot/dubbo-spring-boot-compatible/autoconfigure/src/main/java/org/apache/dubbo/spring/boot/autoconfigure/DubboRelaxedBindingAutoConfiguration.java`
#### Snippet
```java
     * The bean is used to scan the packages of Dubbo Service classes
     *
     * @param environment {@link Environment} instance
     * @return non-null {@link Set}
     * @since 2.7.8
```

### JavadocReference
Cannot resolve symbol `Configuration`
in `dubbo-spring-boot/dubbo-spring-boot-compatible/autoconfigure/src/main/java/org/apache/dubbo/spring/boot/autoconfigure/DubboRelaxedBindingAutoConfiguration.java`
#### Snippet
```java

/**
 * Dubbo Relaxed Binding Auto-{@link Configuration} for Spring Boot 1.x
 */
@ConditionalOnProperty(prefix = DUBBO_PREFIX, name = "enabled", matchIfMissing = true)
```

### JavadocReference
Cannot resolve symbol `Configuration`
in `dubbo-spring-boot/dubbo-spring-boot-compatible/autoconfigure/src/main/java/org/apache/dubbo/spring/boot/autoconfigure/DubboAutoConfiguration.java`
#### Snippet
```java

/**
 * Dubbo Auto {@link Configuration}
 *
 * @see DubboReference
```

### JavadocReference
Cannot resolve symbol `ContextIdApplicationContextInitializer`
in `dubbo-spring-boot/dubbo-spring-boot-compatible/autoconfigure/src/main/java/org/apache/dubbo/spring/boot/util/DubboUtils.java`
#### Snippet
```java
     * The property name of Spring Application
     *
     * @see ContextIdApplicationContextInitializer
     * @since 2.7.1
     */
```

### JavadocReference
Cannot resolve symbol `PropertyResolver`
in `dubbo-spring-boot/dubbo-spring-boot-compatible/autoconfigure/src/main/java/org/apache/dubbo/spring/boot/util/DubboUtils.java`
#### Snippet
```java

    /**
     * The bean name of {@link PropertyResolver} for {@link ServiceAnnotationPostProcessor}'s base-packages
     *
     * @deprecated 2.7.8 It will be remove in the future, please use {@link #BASE_PACKAGES_BEAN_NAME}
```

### JavadocReference
Cannot resolve symbol `Environment`
in `dubbo-spring-boot/dubbo-spring-boot-autoconfigure/src/main/java/org/apache/dubbo/spring/boot/autoconfigure/DubboRelaxedBinding2AutoConfiguration.java`
#### Snippet
```java
     * The bean is used to scan the packages of Dubbo Service classes
     *
     * @param environment {@link Environment} instance
     * @return non-null {@link Set}
     * @since 2.7.8
```

### JavadocReference
Cannot resolve symbol `Configuration`
in `dubbo-spring-boot/dubbo-spring-boot-autoconfigure/src/main/java/org/apache/dubbo/spring/boot/autoconfigure/DubboRelaxedBinding2AutoConfiguration.java`
#### Snippet
```java

/**
 * Dubbo Relaxed Binding Auto-{@link Configuration} for Spring Boot 2.0
 *
 * @see DubboRelaxedBindingAutoConfiguration
```

### JavadocReference
Cannot resolve symbol `ConfigurationProperties`
in `dubbo-spring-boot/dubbo-spring-boot-compatible/autoconfigure/src/main/java/org/apache/dubbo/spring/boot/autoconfigure/DubboConfigurationProperties.java`
#### Snippet
```java

/**
 * Dubbo {@link ConfigurationProperties Config Properties} only used to generate JSON metadata(non-public class)
 *
 * @see ConfigKeys
```

### JavadocReference
Cannot resolve symbol `TestPlan`
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/AbstractRegistryCenterTestExecutionListener.java`
#### Snippet
```java
/**
 * The abstract implementation provides the basic methods. <p></p>
 * {@link #needRegistryCenter(TestPlan)}: checks if current {@link TestPlan} need registry center.
 */
public abstract class AbstractRegistryCenterTestExecutionListener implements TestExecutionListener {
```

### JavadocReference
Cannot resolve symbol `TestPlan`
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/AbstractRegistryCenterTestExecutionListener.java`
#### Snippet
```java
/**
 * The abstract implementation provides the basic methods. <p></p>
 * {@link #needRegistryCenter(TestPlan)}: checks if current {@link TestPlan} need registry center.
 */
public abstract class AbstractRegistryCenterTestExecutionListener implements TestExecutionListener {
```

### JavadocReference
Cannot resolve symbol `TestIdentifier`
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/AbstractRegistryCenterTestExecutionListener.java`
#### Snippet
```java

    /**
     * Checks if current {@link TestIdentifier} need registry center.
     */
    public boolean needRegistryCenter(TestIdentifier testIdentifier) {
```

### JavadocReference
Cannot resolve symbol `TestIdentifier`
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/AbstractRegistryCenterTestExecutionListener.java`
#### Snippet
```java

    /**
     * Checks if the current {@link TestIdentifier} need embedded zookeeper.
     */
    private boolean supportEmbeddedZookeeper(TestIdentifier testIdentifier) {
```

### JavadocReference
Cannot resolve symbol `TestPlan`
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/AbstractRegistryCenterTestExecutionListener.java`
#### Snippet
```java

    /**
     * Checks if current {@link TestPlan} need registry center.
     */
    public boolean needRegistryCenter(TestPlan testPlan) {
```

### JavadocReference
Cannot resolve symbol `org.apache.dubbo.test.check.registrycenter.Initializer.Context`
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/registrycenter/Processor.java`
#### Snippet
```java

/**
 * Define the processor to execute {@link Process} with the {@link org.apache.dubbo.test.check.registrycenter.Initializer.Context}
 */
public interface Processor {
```

### JavadocReference
Cannot resolve symbol `ExecuteWatchdog`
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/registrycenter/context/ZookeeperWindowsContext.java`
#### Snippet
```java

    /**
     * Returns the {@link ExecuteWatchdog}.
     */
    public ExecuteWatchdog getWatchdog() {
```

### JavadocReference
Cannot resolve symbol `ExecuteWatchdog`
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/registrycenter/context/ZookeeperWindowsContext.java`
#### Snippet
```java

    /**
     * Define the default {@link ExecuteWatchdog} for terminating all registered zookeeper processes.
     */
    private final ExecuteWatchdog WATCHDOG = new ExecuteWatchdog(ExecuteWatchdog.INFINITE_TIMEOUT);
```

### JavadocReference
Cannot resolve symbol `updateObserve(long, Set)`
in `dubbo-xds/src/main/java/org/apache/dubbo/registry/xds/util/protocol/XdsProtocol.java`
#### Snippet
```java
     * @param resourceNames specified resource name
     * @param consumer      resource notifier, will be called when resource updated
     * @return requestId, used when resourceNames update with {@link XdsProtocol#updateObserve(long, Set)}
     */
    void observeResource(Set<String> resourceNames, Consumer<Map<String, T>> consumer, boolean isReConnect);
```

### JavadocReference
Cannot resolve symbol `CuratorWatcher`
in `dubbo-registry/dubbo-registry-zookeeper/src/main/java/org/apache/dubbo/registry/zookeeper/ZookeeperServiceDiscoveryChangeWatcher.java`
#### Snippet
```java

/**
 * Zookeeper {@link ServiceDiscovery} Change {@link CuratorWatcher watcher} only interests in
 * {@link Watcher.Event.EventType#NodeChildrenChanged} and {@link Watcher.Event.EventType#NodeDataChanged} event types,
 * which will multicast a {@link ServiceInstancesChangedEvent} when the service node has been changed.
```

### JavadocReference
Cannot resolve symbol `Watcher.Event.EventType`
in `dubbo-registry/dubbo-registry-zookeeper/src/main/java/org/apache/dubbo/registry/zookeeper/ZookeeperServiceDiscoveryChangeWatcher.java`
#### Snippet
```java
/**
 * Zookeeper {@link ServiceDiscovery} Change {@link CuratorWatcher watcher} only interests in
 * {@link Watcher.Event.EventType#NodeChildrenChanged} and {@link Watcher.Event.EventType#NodeDataChanged} event types,
 * which will multicast a {@link ServiceInstancesChangedEvent} when the service node has been changed.
 *
```

### JavadocReference
Cannot resolve symbol `NodeChildrenChanged`
in `dubbo-registry/dubbo-registry-zookeeper/src/main/java/org/apache/dubbo/registry/zookeeper/ZookeeperServiceDiscoveryChangeWatcher.java`
#### Snippet
```java
/**
 * Zookeeper {@link ServiceDiscovery} Change {@link CuratorWatcher watcher} only interests in
 * {@link Watcher.Event.EventType#NodeChildrenChanged} and {@link Watcher.Event.EventType#NodeDataChanged} event types,
 * which will multicast a {@link ServiceInstancesChangedEvent} when the service node has been changed.
 *
```

### JavadocReference
Cannot resolve symbol `Watcher.Event.EventType`
in `dubbo-registry/dubbo-registry-zookeeper/src/main/java/org/apache/dubbo/registry/zookeeper/ZookeeperServiceDiscoveryChangeWatcher.java`
#### Snippet
```java
/**
 * Zookeeper {@link ServiceDiscovery} Change {@link CuratorWatcher watcher} only interests in
 * {@link Watcher.Event.EventType#NodeChildrenChanged} and {@link Watcher.Event.EventType#NodeDataChanged} event types,
 * which will multicast a {@link ServiceInstancesChangedEvent} when the service node has been changed.
 *
```

### JavadocReference
Cannot resolve symbol `NodeDataChanged`
in `dubbo-registry/dubbo-registry-zookeeper/src/main/java/org/apache/dubbo/registry/zookeeper/ZookeeperServiceDiscoveryChangeWatcher.java`
#### Snippet
```java
/**
 * Zookeeper {@link ServiceDiscovery} Change {@link CuratorWatcher watcher} only interests in
 * {@link Watcher.Event.EventType#NodeChildrenChanged} and {@link Watcher.Event.EventType#NodeDataChanged} event types,
 * which will multicast a {@link ServiceInstancesChangedEvent} when the service node has been changed.
 *
```

### JavadocReference
Cannot resolve symbol `CuratorFramework`
in `dubbo-registry/dubbo-registry-zookeeper/src/main/java/org/apache/dubbo/registry/zookeeper/util/CuratorFrameworkParams.java`
#### Snippet
```java

/**
 * The enumeration for the parameters  of {@link CuratorFramework}
 *
 * @see CuratorFramework
```

### JavadocReference
Cannot resolve symbol `CuratorFramework`
in `dubbo-registry/dubbo-registry-zookeeper/src/main/java/org/apache/dubbo/registry/zookeeper/util/CuratorFrameworkParams.java`
#### Snippet
```java
 * The enumeration for the parameters  of {@link CuratorFramework}
 *
 * @see CuratorFramework
 * @since 2.7.5
 */
```

### JavadocReference
Cannot resolve symbol `CuratorWatcher`
in `dubbo-registry/dubbo-registry-zookeeper/src/main/java/org/apache/dubbo/registry/zookeeper/ZookeeperServiceDiscovery.java`
#### Snippet
```java

    /**
     * The Key is watched Zookeeper path, the value is an instance of {@link CuratorWatcher}
     */
    private final Map<String, ZookeeperServiceDiscoveryChangeWatcher> watcherCaches = new ConcurrentHashMap<>();
```

### JavadocReference
Cannot resolve symbol `newModel()`
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/FrameworkModel.java`
#### Snippet
```java

    /**
     * Use {@link FrameworkModel#newModel()} to create a new model
     */
    public FrameworkModel() {
```

### JavadocReference
Symbol `org.apache.dubbo.configcenter.support.zookeeper.ZookeeperDynamicConfiguration` is inaccessible from here
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/configcenter/TreePathDynamicConfiguration.java`
#### Snippet
```java
 * <ul>
 *     <li>{@link FileSystemDynamicConfiguration "file"}</li>
 *     <li>{@link org.apache.dubbo.configcenter.support.zookeeper.ZookeeperDynamicConfiguration "zookeeper"}</li>
 *     <li>{@link org.apache.dubbo.configcenter.consul.ConsulDynamicConfiguration "consul"}</li>
 * </ul>
```

### JavadocReference
Cannot resolve symbol `org.apache.dubbo.configcenter.consul.ConsulDynamicConfiguration`
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/configcenter/TreePathDynamicConfiguration.java`
#### Snippet
```java
 *     <li>{@link FileSystemDynamicConfiguration "file"}</li>
 *     <li>{@link org.apache.dubbo.configcenter.support.zookeeper.ZookeeperDynamicConfiguration "zookeeper"}</li>
 *     <li>{@link org.apache.dubbo.configcenter.consul.ConsulDynamicConfiguration "consul"}</li>
 * </ul>
 *
```

### JavadocReference
Cannot resolve symbol `groupDirectory(String)`
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/configcenter/file/FileSystemDynamicConfiguration.java`
#### Snippet
```java

    /**
     * The {@link Set} of {@link #groupDirectory(String) directories} that may be processing,
     * <p>
     * if {@link #isBasedPoolingWatchService()} is <code>false</code>, this properties will be
```

### JavadocReference
Cannot resolve symbol `sun.nio.fs.PollingWatchService`
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/configcenter/file/FileSystemDynamicConfiguration.java`
#### Snippet
```java

    /**
     * It's whether the implementation of {@link WatchService} is based on {@linkplain sun.nio.fs.PollingWatchService}
     * or not.
     * <p>
```

### JavadocReference
Cannot resolve symbol `sun.nio.fs.PollingWatchService`
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/configcenter/file/FileSystemDynamicConfiguration.java`
#### Snippet
```java

    /**
     * The class name of {@linkplain sun.nio.fs.PollingWatchService}
     */
    private static final String POLLING_WATCH_SERVICE_CLASS_NAME = "sun.nio.fs.PollingWatchService";
```

### JavadocReference
Cannot resolve symbol `sun.nio.fs.PollingWatchService`
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/configcenter/file/FileSystemDynamicConfiguration.java`
#### Snippet
```java

    /**
     * Detect the argument of {@link WatchService} is based on {@linkplain sun.nio.fs.PollingWatchService}
     * or not.
     * <p>
```

### JavadocReference
Cannot resolve symbol `sun.nio.fs.PollingWatchService`
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/configcenter/file/FileSystemDynamicConfiguration.java`
#### Snippet
```java
     * <p>
     * Some platforms do not provide the native implementation of {@link WatchService}, just use
     * {@linkplain sun.nio.fs.PollingWatchService} in periodic poll file modifications.
     *
     * @param watchService the instance of {@link WatchService}
```

### JavadocReference
Cannot resolve symbol `NotFoundException`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
     * @param c class.
     * @return desc.
     * @throws NotFoundException
     */
    public static String getDesc(final CtClass c) throws NotFoundException {
```

### JavadocReference
Cannot resolve symbol `NotFoundException`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
     * @param cs class array.
     * @return desc.
     * @throws NotFoundException
     */
    public static String getDesc(final Class<?>[] cs) {
```

### JavadocReference
Cannot resolve symbol `NotFoundException`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
     * @param c class.
     * @return desc.
     * @throws NotFoundException
     */
    public static String getDesc(Class<?> c) {
```

### JavadocReference
Cannot resolve symbol `org.apache.commons.lang3.concurrent.AtomicSafeInitializer`
in `dubbo-common/src/main/java/org/apache/dubbo/common/concurrent/CallableSafeInitializer.java`
#### Snippet
```java
 * {@code Callable} object.
 * </p>
 * @see org.apache.commons.lang3.concurrent.AtomicSafeInitializer
 */
public class CallableSafeInitializer<T> {
```

### JavadocReference
Symbol `org.apache.dubbo.config.spring.context.DubboDeployApplicationListener` is inaccessible from here
in `dubbo-common/src/main/java/org/apache/dubbo/config/ModuleConfig.java`
#### Snippet
```java
     * If start in background, do not await finish on Spring ContextRefreshedEvent.
     *
     * @see org.apache.dubbo.config.spring.context.DubboDeployApplicationListener
     */
    private Boolean background;
```

### JavadocReference
Symbol `org.apache.dubbo.config.spring.context.DubboDeployApplicationListener` is inaccessible from here
in `dubbo-common/src/main/java/org/apache/dubbo/config/ModuleConfig.java`
#### Snippet
```java
     * If start in backgound, do not await finish on Spring ContextRefreshedEvent.
     *
     * @see org.apache.dubbo.config.spring.context.DubboDeployApplicationListener
     */
    public void setBackground(Boolean background) {
```

### JavadocReference
Symbol `org.apache.dubbo.spring.boot.autoconfigure.DubboConfigurationProperties` is inaccessible from here
in `dubbo-common/src/main/java/org/apache/dubbo/config/ConfigKeys.java`
#### Snippet
```java
/**
 * External config keys list
 * @see org.apache.dubbo.spring.boot.autoconfigure.DubboConfigurationProperties
 */
public interface ConfigKeys {
```

### JavadocReference
Symbol `configMode` is inaccessible from here
in `dubbo-common/src/main/java/org/apache/dubbo/config/ConfigKeys.java`
#### Snippet
```java
     * Change dubbo config mode, available values from {@link ConfigMode}. Default value is {@link ConfigMode#STRICT}.
     * @see ConfigMode
     * @see ConfigManager#configMode
     */
    String DUBBO_CONFIG_MODE = "dubbo.config.mode";
```

### JavadocReference
Symbol `org.apache.dubbo.config.spring.context.annotation.EnableDubbo` is inaccessible from here
in `dubbo-common/src/main/java/org/apache/dubbo/config/ConfigKeys.java`
#### Snippet
```java
    /**
     * The basePackages to scan , the multiple-value is delimited by comma
     * @see org.apache.dubbo.config.spring.context.annotation.EnableDubbo#scanBasePackages()
     */
    String DUBBO_SCAN_BASE_PACKAGES = "dubbo.scan.base-packages";
```

### JavadocReference
Symbol `scanBasePackages()` is inaccessible from here
in `dubbo-common/src/main/java/org/apache/dubbo/config/ConfigKeys.java`
#### Snippet
```java
    /**
     * The basePackages to scan , the multiple-value is delimited by comma
     * @see org.apache.dubbo.config.spring.context.annotation.EnableDubbo#scanBasePackages()
     */
    String DUBBO_SCAN_BASE_PACKAGES = "dubbo.scan.base-packages";
```

### JavadocReference
Symbol `referAsync` is inaccessible from here
in `dubbo-common/src/main/java/org/apache/dubbo/config/AbstractReferenceConfig.java`
#### Snippet
```java
     * Weather the reference is referred asynchronously
     *
     * @see ModuleConfig#referAsync
     * @deprecated
     */
```

### JavadocReference
Symbol `uniqueConfigTypes` is inaccessible from here
in `dubbo-common/src/main/java/org/apache/dubbo/config/context/ConfigMode.java`
#### Snippet
```java
/**
 * Config processing mode for unique config type, e.g. ApplicationConfig, ModuleConfig, MonitorConfig, SslConfig, MetricsConfig
 * @see ConfigManager#uniqueConfigTypes
 */
public enum ConfigMode {
```

### JavadocReference
Symbol `org.apache.dubbo.rpc.Constants` is inaccessible from here
in `dubbo-common/src/main/java/org/apache/dubbo/config/annotation/DubboService.java`
#### Snippet
```java
    /**
     * the scope for referring/exporting a service, if it's local, it means searching in current JVM only.
     * @see org.apache.dubbo.rpc.Constants#SCOPE_LOCAL
     * @see org.apache.dubbo.rpc.Constants#SCOPE_REMOTE
     */
```

### JavadocReference
Symbol `SCOPE_LOCAL` is inaccessible from here
in `dubbo-common/src/main/java/org/apache/dubbo/config/annotation/DubboService.java`
#### Snippet
```java
    /**
     * the scope for referring/exporting a service, if it's local, it means searching in current JVM only.
     * @see org.apache.dubbo.rpc.Constants#SCOPE_LOCAL
     * @see org.apache.dubbo.rpc.Constants#SCOPE_REMOTE
     */
```

### JavadocReference
Symbol `org.apache.dubbo.rpc.Constants` is inaccessible from here
in `dubbo-common/src/main/java/org/apache/dubbo/config/annotation/DubboService.java`
#### Snippet
```java
     * the scope for referring/exporting a service, if it's local, it means searching in current JVM only.
     * @see org.apache.dubbo.rpc.Constants#SCOPE_LOCAL
     * @see org.apache.dubbo.rpc.Constants#SCOPE_REMOTE
     */
    String scope() default "";
```

### JavadocReference
Symbol `SCOPE_REMOTE` is inaccessible from here
in `dubbo-common/src/main/java/org/apache/dubbo/config/annotation/DubboService.java`
#### Snippet
```java
     * the scope for referring/exporting a service, if it's local, it means searching in current JVM only.
     * @see org.apache.dubbo.rpc.Constants#SCOPE_LOCAL
     * @see org.apache.dubbo.rpc.Constants#SCOPE_REMOTE
     */
    String scope() default "";
```

### JavadocReference
Cannot resolve symbol `ExporterListener`
in `dubbo-common/src/main/java/org/apache/dubbo/config/annotation/DubboService.java`
#### Snippet
```java
     * Listeners for service exporting and unexporting
     *
     * @see ExporterListener
     */
    String[] listener() default {};
```

### JavadocReference
Cannot resolve symbol `Filter`
in `dubbo-common/src/main/java/org/apache/dubbo/config/annotation/DubboService.java`
#### Snippet
```java
     * Filters for service invocation
     *
     * @see Filter
     */
    String[] filter() default {};
```

### JavadocReference
Cannot resolve symbol `Filter`
in `dubbo-common/src/main/java/org/apache/dubbo/config/annotation/Service.java`
#### Snippet
```java
     * Filters for service invocation
     *
     * @see Filter
     */
    String[] filter() default {};
```

### JavadocReference
Cannot resolve symbol `ExporterListener`
in `dubbo-common/src/main/java/org/apache/dubbo/config/annotation/Service.java`
#### Snippet
```java
     * Listeners for service exporting and unexporting
     *
     * @see ExporterListener
     */
    String[] listener() default {};
```

### JavadocReference
Symbol `providerNamespace` is inaccessible from here
in `dubbo-common/src/main/java/org/apache/dubbo/config/annotation/DubboReference.java`
#### Snippet
```java
    /**
     * assign the namespace that provider belong to
     * @see AbstractReferenceConfig#providerNamespace
     * @since 3.1.1
     */
```

### JavadocReference
Symbol `unloadClusterRelated` is inaccessible from here
in `dubbo-common/src/main/java/org/apache/dubbo/config/annotation/DubboReference.java`
#### Snippet
```java
     * unload Cluster related in mesh mode
     *
     * @see ReferenceConfigBase#unloadClusterRelated
     * @since 3.1.0
     */
```

### JavadocReference
Symbol `org.apache.dubbo.config.spring.reference.ReferenceBeanBuilder` is inaccessible from here
in `dubbo-common/src/main/java/org/apache/dubbo/config/annotation/DubboReference.java`
#### Snippet
```java
 * </pre>
 *
 * @see org.apache.dubbo.config.spring.reference.ReferenceBeanBuilder
 * @see org.apache.dubbo.config.spring.ReferenceBean
 * @since 2.7.7
```

### JavadocReference
Symbol `org.apache.dubbo.config.spring.ReferenceBean` is inaccessible from here
in `dubbo-common/src/main/java/org/apache/dubbo/config/annotation/DubboReference.java`
#### Snippet
```java
 *
 * @see org.apache.dubbo.config.spring.reference.ReferenceBeanBuilder
 * @see org.apache.dubbo.config.spring.ReferenceBean
 * @since 2.7.7
 */
```

### JavadocReference
Symbol `providerPort` is inaccessible from here
in `dubbo-common/src/main/java/org/apache/dubbo/config/annotation/DubboReference.java`
#### Snippet
```java
     * The service port of the provider
     *
     * @see AbstractReferenceConfig#providerPort
     * @since 3.1.0
     */
```

### JavadocReference
Symbol `org.apache.dubbo.rpc.Constants` is inaccessible from here
in `dubbo-common/src/main/java/org/apache/dubbo/config/annotation/DubboReference.java`
#### Snippet
```java
     * the scope for referring/exporting a service, if it's local, it means searching in current JVM only.
     *
     * @see org.apache.dubbo.rpc.Constants#SCOPE_LOCAL
     * @see org.apache.dubbo.rpc.Constants#SCOPE_REMOTE
     */
```

### JavadocReference
Symbol `SCOPE_LOCAL` is inaccessible from here
in `dubbo-common/src/main/java/org/apache/dubbo/config/annotation/DubboReference.java`
#### Snippet
```java
     * the scope for referring/exporting a service, if it's local, it means searching in current JVM only.
     *
     * @see org.apache.dubbo.rpc.Constants#SCOPE_LOCAL
     * @see org.apache.dubbo.rpc.Constants#SCOPE_REMOTE
     */
```

### JavadocReference
Symbol `org.apache.dubbo.rpc.Constants` is inaccessible from here
in `dubbo-common/src/main/java/org/apache/dubbo/config/annotation/DubboReference.java`
#### Snippet
```java
     *
     * @see org.apache.dubbo.rpc.Constants#SCOPE_LOCAL
     * @see org.apache.dubbo.rpc.Constants#SCOPE_REMOTE
     */
    String scope() default "";
```

### JavadocReference
Symbol `SCOPE_REMOTE` is inaccessible from here
in `dubbo-common/src/main/java/org/apache/dubbo/config/annotation/DubboReference.java`
#### Snippet
```java
     *
     * @see org.apache.dubbo.rpc.Constants#SCOPE_LOCAL
     * @see org.apache.dubbo.rpc.Constants#SCOPE_REMOTE
     */
    String scope() default "";
```

### JavadocReference
Symbol `exportAsync` is inaccessible from here
in `dubbo-common/src/main/java/org/apache/dubbo/config/AbstractServiceConfig.java`
#### Snippet
```java
     * Weather the service is export asynchronously
     * @deprecated
     * @see ModuleConfig#exportAsync
     */
    @Deprecated
```

### JavadocReference
Cannot resolve symbol `toString()`
in `dubbo-common/src/main/java/org/apache/dubbo/metadata/definition/model/TypeDefinition.java`
#### Snippet
```java
    /**
     * Replacing <code>", "</code> to <code>","</code> will not change the semantic of
     * {@link ParameterizedType#toString()}
     *
     * @param type
```

### JavadocReference
Cannot resolve symbol `org.springframework.core.AttributeAccessor`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/Constants.java`
#### Snippet
```java
    /**
     * The name of an attribute that can be
     * {@link org.springframework.core.AttributeAccessor#setAttribute set} on a
     * {@link org.springframework.beans.factory.config.BeanDefinition} so that
     * factory beans can signal their object type when it can't be deduced from
```

### JavadocReference
Cannot resolve symbol `setAttribute`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/Constants.java`
#### Snippet
```java
    /**
     * The name of an attribute that can be
     * {@link org.springframework.core.AttributeAccessor#setAttribute set} on a
     * {@link org.springframework.beans.factory.config.BeanDefinition} so that
     * factory beans can signal their object type when it can't be deduced from
```

### JavadocReference
Cannot resolve symbol `org.springframework.beans.factory.config.BeanDefinition`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/Constants.java`
#### Snippet
```java
     * The name of an attribute that can be
     * {@link org.springframework.core.AttributeAccessor#setAttribute set} on a
     * {@link org.springframework.beans.factory.config.BeanDefinition} so that
     * factory beans can signal their object type when it can't be deduced from
     * the factory bean class.
```

### JavadocReference
Cannot resolve symbol `BeanPostProcessor`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/GenericBeanPostProcessorAdapter.java`
#### Snippet
```java

/**
 * Generic {@link BeanPostProcessor} Adapter
 *
 * @see BeanPostProcessor
```

### JavadocReference
Cannot resolve symbol `BeanPostProcessor`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/GenericBeanPostProcessorAdapter.java`
#### Snippet
```java
 * Generic {@link BeanPostProcessor} Adapter
 *
 * @see BeanPostProcessor
 */
@SuppressWarnings("unchecked")
```

### JavadocReference
Cannot resolve symbol `BeanPostProcessor`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/GenericBeanPostProcessorAdapter.java`
#### Snippet
```java
     * @param beanName Bean Name
     * @return Bean Object
     * @see BeanPostProcessor#postProcessAfterInitialization(Object, String)
     */
    protected T doPostProcessAfterInitialization(T bean, String beanName) throws BeansException {
```

### JavadocReference
Cannot resolve symbol `postProcessAfterInitialization(Object, String)`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/GenericBeanPostProcessorAdapter.java`
#### Snippet
```java
     * @param beanName Bean Name
     * @return Bean Object
     * @see BeanPostProcessor#postProcessAfterInitialization(Object, String)
     */
    protected T doPostProcessAfterInitialization(T bean, String beanName) throws BeansException {
```

### JavadocReference
Cannot resolve symbol `PropertySources`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/PropertySourcesUtils.java`
#### Snippet
```java

/**
 * {@link PropertySources} Utilities
 *
 * @see PropertySources
```

### JavadocReference
Cannot resolve symbol `BeansException`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/GenericBeanPostProcessorAdapter.java`
#### Snippet
```java
     * @param bean     Bean Object
     * @param beanName Bean Name
     * @throws BeansException  in case of errors
     */
    protected void processBeforeInitialization(T bean, String beanName) throws BeansException {
```

### JavadocReference
Cannot resolve symbol `BeansException`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/GenericBeanPostProcessorAdapter.java`
#### Snippet
```java
     * @param bean     Bean Object
     * @param beanName Bean Name
     * @throws BeansException  in case of errors
     */
    protected void processAfterInitialization(T bean, String beanName) throws BeansException {
```

### JavadocReference
Cannot resolve symbol `BeanPostProcessor`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/GenericBeanPostProcessorAdapter.java`
#### Snippet
```java
     * @param beanName Bean Name
     * @return Bean Object
     * @see BeanPostProcessor#postProcessBeforeInitialization(Object, String)
     */
    protected T doPostProcessBeforeInitialization(T bean, String beanName) throws BeansException {
```

### JavadocReference
Cannot resolve symbol `postProcessBeforeInitialization(Object, String)`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/GenericBeanPostProcessorAdapter.java`
#### Snippet
```java
     * @param beanName Bean Name
     * @return Bean Object
     * @see BeanPostProcessor#postProcessBeforeInitialization(Object, String)
     */
    protected T doPostProcessBeforeInitialization(T bean, String beanName) throws BeansException {
```

### JavadocReference
Cannot resolve symbol `PropertySources`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/PropertySourcesUtils.java`
#### Snippet
```java
 * {@link PropertySources} Utilities
 *
 * @see PropertySources
 */
public abstract class PropertySourcesUtils {
```

### JavadocReference
Cannot resolve symbol `PropertySources`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/PropertySourcesUtils.java`
#### Snippet
```java
     * Get prefixed {@link Properties}
     *
     * @param propertySources {@link PropertySources}
     * @param prefix          the prefix of property name
     * @return Map
```

### JavadocReference
Cannot resolve symbol `PropertySource`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/PropertySourcesUtils.java`
#### Snippet
```java
     * Get Sub {@link Properties}
     *
     * @param propertySources {@link PropertySource} Iterable
     * @param prefix          the prefix of property name
     * @return Map
```

### JavadocReference
Cannot resolve symbol `PropertySource`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/PropertySourcesUtils.java`
#### Snippet
```java

    /**
     * Get the property names as the array from the specified {@link PropertySource} instance.
     *
     * @param propertySource {@link PropertySource} instance
```

### JavadocReference
Cannot resolve symbol `PropertySource`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/PropertySourcesUtils.java`
#### Snippet
```java
     * Get the property names as the array from the specified {@link PropertySource} instance.
     *
     * @param propertySource {@link PropertySource} instance
     * @return non-null
     */
```

### JavadocReference
Cannot resolve symbol `PropertySources`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/PropertySourcesUtils.java`
#### Snippet
```java
     * Get prefixed {@link Properties}
     *
     * @param propertySources  {@link PropertySources}
     * @param propertyResolver {@link PropertyResolver} to resolve the placeholder if present
     * @param prefix           the prefix of property name
```

### JavadocReference
Cannot resolve symbol `PropertyResolver`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/PropertySourcesUtils.java`
#### Snippet
```java
     *
     * @param propertySources  {@link PropertySources}
     * @param propertyResolver {@link PropertyResolver} to resolve the placeholder if present
     * @param prefix           the prefix of property name
     * @return Map
```

### JavadocReference
Cannot resolve symbol `ConfigurableEnvironment`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/PropertySourcesUtils.java`
#### Snippet
```java
     * Get Sub {@link Properties}
     *
     * @param environment {@link ConfigurableEnvironment}
     * @param prefix      the prefix of property name
     * @return Map
```

### JavadocReference
Cannot resolve symbol `AliasRegistry`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/BeanRegistrar.java`
#### Snippet
```java

    /**
     * Detect the alias is present or not in the given bean name from {@link AliasRegistry}
     *
     * @param registry {@link AliasRegistry}
```

### JavadocReference
Cannot resolve symbol `AliasRegistry`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/BeanRegistrar.java`
#### Snippet
```java
     * Detect the alias is present or not in the given bean name from {@link AliasRegistry}
     *
     * @param registry {@link AliasRegistry}
     * @param beanName the bean name
     * @param alias    alias to test
```

### JavadocReference
Cannot resolve symbol `org.springframework.context.support.PostProcessorRegistrationDelegate`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/DubboBeanUtils.java`
#### Snippet
```java
     * @param registry
     * @see DubboInfraBeanRegisterPostProcessor
     * @see org.springframework.context.support.PostProcessorRegistrationDelegate#invokeBeanFactoryPostProcessors(org.springframework.beans.factory.config.ConfigurableListableBeanFactory, java.util.List)
     */
    static void registerPlaceholderConfigurerBeanIfNotExists(ConfigurableListableBeanFactory beanFactory, BeanDefinitionRegistry registry) {
```

### JavadocReference
Cannot resolve symbol `invokeBeanFactoryPostProcessors(org.springframework.beans.factory.config.ConfigurableListableBeanFactory, java.util.List)`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/DubboBeanUtils.java`
#### Snippet
```java
     * @param registry
     * @see DubboInfraBeanRegisterPostProcessor
     * @see org.springframework.context.support.PostProcessorRegistrationDelegate#invokeBeanFactoryPostProcessors(org.springframework.beans.factory.config.ConfigurableListableBeanFactory, java.util.List)
     */
    static void registerPlaceholderConfigurerBeanIfNotExists(ConfigurableListableBeanFactory beanFactory, BeanDefinitionRegistry registry) {
```

### JavadocReference
Cannot resolve symbol `org.springframework.beans.factory.config.ConfigurableListableBeanFactory`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/DubboBeanUtils.java`
#### Snippet
```java
     * @param registry
     * @see DubboInfraBeanRegisterPostProcessor
     * @see org.springframework.context.support.PostProcessorRegistrationDelegate#invokeBeanFactoryPostProcessors(org.springframework.beans.factory.config.ConfigurableListableBeanFactory, java.util.List)
     */
    static void registerPlaceholderConfigurerBeanIfNotExists(ConfigurableListableBeanFactory beanFactory, BeanDefinitionRegistry registry) {
```

### JavadocReference
Cannot resolve symbol `BeanDefinitionRegistry`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/DubboBeanUtils.java`
#### Snippet
```java
     * Register Infrastructure Bean
     *
     * @param beanDefinitionRegistry {@link BeanDefinitionRegistry}
     * @param beanType               the type of bean
     * @param beanName               the name of bean
```

### JavadocReference
Cannot resolve symbol `BeanDefinitionRegistry`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/DubboBeanUtils.java`
#### Snippet
```java
     * Register the common beans
     *
     * @param registry {@link BeanDefinitionRegistry}
     * @see ReferenceAnnotationBeanPostProcessor
     * @see DubboConfigDefaultPropertyValueBeanPostProcessor
```

### JavadocReference
Cannot resolve symbol `BeanMetadataElement`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/config/ConfigurableSourceBeanMetadataElement.java`
#### Snippet
```java

    /**
     * Set the source into the specified {@link BeanMetadataElement}
     *
     * @param beanMetadataElement {@link BeanMetadataElement} instance
```

### JavadocReference
Cannot resolve symbol `BeanMetadataElement`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/config/ConfigurableSourceBeanMetadataElement.java`
#### Snippet
```java
     * Set the source into the specified {@link BeanMetadataElement}
     *
     * @param beanMetadataElement {@link BeanMetadataElement} instance
     */
    default void setSource(BeanMetadataElement beanMetadataElement) {
```

### JavadocReference
Cannot resolve symbol `BeanMetadataAttributeAccessor`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/config/ConfigurableSourceBeanMetadataElement.java`
#### Snippet
```java

/**
 * Configurable the {@link BeanMetadataAttributeAccessor#setSource(Object) source} for {@link BeanMetadataElement}
 *
 * @since 2.7.5
```

### JavadocReference
Cannot resolve symbol `setSource(Object)`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/config/ConfigurableSourceBeanMetadataElement.java`
#### Snippet
```java

/**
 * Configurable the {@link BeanMetadataAttributeAccessor#setSource(Object) source} for {@link BeanMetadataElement}
 *
 * @since 2.7.5
```

### JavadocReference
Cannot resolve symbol `BeanMetadataElement`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/config/ConfigurableSourceBeanMetadataElement.java`
#### Snippet
```java

/**
 * Configurable the {@link BeanMetadataAttributeAccessor#setSource(Object) source} for {@link BeanMetadataElement}
 *
 * @since 2.7.5
```

### JavadocReference
Symbol `configReferenceBean` is inaccessible from here
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/ReferenceBean.java`
#### Snippet
```java
     * The ReferenceBean is also a FactoryBean.
     * <br/>
     * (This has already been resolved by decorating the BeanDefinition: {@link DubboBeanDefinitionParser#configReferenceBean})
     *
     * <p/>
```

### JavadocReference
Symbol `initReferenceBean(ReferenceBean)` is inaccessible from here
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/ReferenceBean.java`
#### Snippet
```java
     *
     * @see DubboConfigBeanInitializer
     * @see ReferenceBeanManager#initReferenceBean(ReferenceBean)
     * @see DubboBeanDefinitionParser#configReferenceBean
     */
```

### JavadocReference
Symbol `configReferenceBean` is inaccessible from here
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/ReferenceBean.java`
#### Snippet
```java
     * @see DubboConfigBeanInitializer
     * @see ReferenceBeanManager#initReferenceBean(ReferenceBean)
     * @see DubboBeanDefinitionParser#configReferenceBean
     */
    @Override
```

### JavadocReference
Cannot resolve symbol `Environment`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/EnvironmentUtils.java`
#### Snippet
```java

/**
 * The utilities class for {@link Environment}
 *
 * @see Environment
```

### JavadocReference
Cannot resolve symbol `Environment`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/EnvironmentUtils.java`
#### Snippet
```java
 * The utilities class for {@link Environment}
 *
 * @see Environment
 * @since 2.7.0
 */
```

### JavadocReference
Cannot resolve symbol `ConfigurableEnvironment`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/EnvironmentUtils.java`
#### Snippet
```java

    /**
     * Filters Dubbo Properties from {@link ConfigurableEnvironment}
     *
     * @param environment {@link ConfigurableEnvironment}
```

### JavadocReference
Cannot resolve symbol `ConfigurableEnvironment`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/EnvironmentUtils.java`
#### Snippet
```java
     * Filters Dubbo Properties from {@link ConfigurableEnvironment}
     *
     * @param environment {@link ConfigurableEnvironment}
     * @return Read-only SortedMap
     */
```

### JavadocReference
Cannot resolve symbol `ConfigurableEnvironment`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/EnvironmentUtils.java`
#### Snippet
```java

    /**
     * Extras The properties from {@link ConfigurableEnvironment}
     *
     * @param environment {@link ConfigurableEnvironment}
```

### JavadocReference
Cannot resolve symbol `ConfigurableEnvironment`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/EnvironmentUtils.java`
#### Snippet
```java
     * Extras The properties from {@link ConfigurableEnvironment}
     *
     * @param environment {@link ConfigurableEnvironment}
     * @return Read-only Map
     */
```

### JavadocReference
Cannot resolve symbol `InitDestroyAnnotationBeanPostProcessor`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/config/DubboConfigDefaultPropertyValueBeanPostProcessor.java`
#### Snippet
```java

    /**
     * @return Higher than {@link InitDestroyAnnotationBeanPostProcessor#getOrder()}
     * @see InitDestroyAnnotationBeanPostProcessor
     * @see CommonAnnotationBeanPostProcessor
```

### JavadocReference
Cannot resolve symbol `getOrder()`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/config/DubboConfigDefaultPropertyValueBeanPostProcessor.java`
#### Snippet
```java

    /**
     * @return Higher than {@link InitDestroyAnnotationBeanPostProcessor#getOrder()}
     * @see InitDestroyAnnotationBeanPostProcessor
     * @see CommonAnnotationBeanPostProcessor
```

### JavadocReference
Cannot resolve symbol `InitDestroyAnnotationBeanPostProcessor`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/config/DubboConfigDefaultPropertyValueBeanPostProcessor.java`
#### Snippet
```java
    /**
     * @return Higher than {@link InitDestroyAnnotationBeanPostProcessor#getOrder()}
     * @see InitDestroyAnnotationBeanPostProcessor
     * @see CommonAnnotationBeanPostProcessor
     * @see PostConstruct
```

### JavadocReference
Cannot resolve symbol `CommonAnnotationBeanPostProcessor`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/config/DubboConfigDefaultPropertyValueBeanPostProcessor.java`
#### Snippet
```java
     * @return Higher than {@link InitDestroyAnnotationBeanPostProcessor#getOrder()}
     * @see InitDestroyAnnotationBeanPostProcessor
     * @see CommonAnnotationBeanPostProcessor
     * @see PostConstruct
     */
```

### JavadocReference
Cannot resolve symbol `BeanPostProcessor`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/config/DubboConfigDefaultPropertyValueBeanPostProcessor.java`
#### Snippet
```java

/**
 * The {@link BeanPostProcessor} class for the default property value of {@link AbstractConfig Dubbo's Config Beans}
 *
 * @since 2.7.6
```

### JavadocReference
Cannot resolve symbol `PropertyValues`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/AnnotationPropertyValuesAdapter.java`
#### Snippet
```java

/**
 * {@link Annotation} {@link PropertyValues} Adapter
 *
 * @see Annotation
```

### JavadocReference
Cannot resolve symbol `PropertyValues`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/AnnotationPropertyValuesAdapter.java`
#### Snippet
```java
 *
 * @see Annotation
 * @see PropertyValues
 * @since 2.5.11
 */
```

### JavadocReference
Cannot resolve symbol `org.springframework.core.annotation.AnnotationUtils`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/DubboAnnotationUtils.java`
#### Snippet
```java
 * Dubbo Annotation Utilities Class
 *
 * @see org.springframework.core.annotation.AnnotationUtils
 * @since 2.5.11
 */
```

### JavadocReference
Cannot resolve symbol `org.springframework.core.type.AnnotationMetadata`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/AnnotationUtils.java`
#### Snippet
```java
     * @param annotationType         the {@link Class tyoe} pf {@link Annotation annotation}
     * @param classValuesAsString    whether to turn Class references into Strings (for
     *                               compatibility with {@link org.springframework.core.type.AnnotationMetadata} or to
     *                               preserve them as Class references
     * @param nestedAnnotationsAsMap whether to turn nested Annotation instances into
```

### JavadocReference
Cannot resolve symbol `AnnotationAttributes`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/AnnotationUtils.java`
#### Snippet
```java
     *                               preserve them as Class references
     * @param nestedAnnotationsAsMap whether to turn nested Annotation instances into
     *                               {@link AnnotationAttributes} maps (for compatibility with
     *                               {@link org.springframework.core.type.AnnotationMetadata} or to preserve them as
     *                               Annotation instances
```

### JavadocReference
Cannot resolve symbol `org.springframework.core.type.AnnotationMetadata`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/AnnotationUtils.java`
#### Snippet
```java
     * @param nestedAnnotationsAsMap whether to turn nested Annotation instances into
     *                               {@link AnnotationAttributes} maps (for compatibility with
     *                               {@link org.springframework.core.type.AnnotationMetadata} or to preserve them as
     *                               Annotation instances
     * @return If current version of Spring Framework is below 4.2, return <code>null</code>
```

### JavadocReference
Cannot resolve symbol `AnnotationAttributes`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/AnnotationUtils.java`
#### Snippet
```java
     * Get the attribute value the will
     *
     * @param attributes    {@link Map the annotation attributes} or {@link AnnotationAttributes}
     * @param attributeName the name of attribute
     * @param required      the required attribute or not
```

### JavadocReference
Cannot resolve symbol `AnnotationAttributes`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/AnnotationUtils.java`
#### Snippet
```java

    /**
     * Get the {@link AnnotationAttributes}
     *
     * @param annotation           specified {@link Annotation}
```

### JavadocReference
Cannot resolve symbol `PropertyResolver`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/AnnotationUtils.java`
#### Snippet
```java
     *
     * @param annotation           specified {@link Annotation}
     * @param propertyResolver     {@link PropertyResolver} instance, e.g {@link Environment}
     * @param ignoreDefaultValue   whether ignore default value or not
     * @param ignoreAttributeNames the attribute names of annotation should be ignored
```

### JavadocReference
Cannot resolve symbol `Environment`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/AnnotationUtils.java`
#### Snippet
```java
     *
     * @param annotation           specified {@link Annotation}
     * @param propertyResolver     {@link PropertyResolver} instance, e.g {@link Environment}
     * @param ignoreDefaultValue   whether ignore default value or not
     * @param ignoreAttributeNames the attribute names of annotation should be ignored
```

### JavadocReference
Cannot resolve symbol `PropertyResolver`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/AnnotationUtils.java`
#### Snippet
```java
     * @param ignoreAttributeNames the attribute names of annotation should be ignored
     * @return non-null
     * @see #getAttributes(Annotation, PropertyResolver, boolean, String...)
     * @see #getAnnotationAttributes(AnnotatedElement, Class, PropertyResolver, boolean, String...)
     */
```

### JavadocReference
Cannot resolve symbol `getAnnotationAttributes(AnnotatedElement, Class, PropertyResolver, boolean, String...)`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/AnnotationUtils.java`
#### Snippet
```java
     * @return non-null
     * @see #getAttributes(Annotation, PropertyResolver, boolean, String...)
     * @see #getAnnotationAttributes(AnnotatedElement, Class, PropertyResolver, boolean, String...)
     */
    public static AnnotationAttributes getAnnotationAttributes(Annotation annotation, PropertyResolver propertyResolver,
```

### JavadocReference
Cannot resolve symbol `PropertyResolver`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/AnnotationUtils.java`
#### Snippet
```java
     * @return non-null
     * @see #getAttributes(Annotation, PropertyResolver, boolean, String...)
     * @see #getAnnotationAttributes(AnnotatedElement, Class, PropertyResolver, boolean, String...)
     */
    public static AnnotationAttributes getAnnotationAttributes(Annotation annotation, PropertyResolver propertyResolver,
```

### JavadocReference
Cannot resolve symbol `AnnotationAttributes`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/AnnotationUtils.java`
#### Snippet
```java

    /**
     * Get the {@link AnnotationAttributes}, if the argument <code>tryMergedAnnotation</code> is <code>true</code>,
     * the {@link AnnotationAttributes} will be got from
     * {@link #tryGetMergedAnnotationAttributes(AnnotatedElement, Class, PropertyResolver, boolean, String...) merged annotation} first,
```

### JavadocReference
Cannot resolve symbol `AnnotationAttributes`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/AnnotationUtils.java`
#### Snippet
```java
    /**
     * Get the {@link AnnotationAttributes}, if the argument <code>tryMergedAnnotation</code> is <code>true</code>,
     * the {@link AnnotationAttributes} will be got from
     * {@link #tryGetMergedAnnotationAttributes(AnnotatedElement, Class, PropertyResolver, boolean, String...) merged annotation} first,
     * if failed, and then to get from
```

### JavadocReference
Cannot resolve symbol `BeanDefinitionRegistry`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/schema/DubboNamespaceHandler.java`
#### Snippet
```java
     * Register the processors for the Spring Annotation-Driven features
     *
     * @param registry {@link BeanDefinitionRegistry}
     * @see AnnotationConfigUtils
     * @since 2.7.5
```

### JavadocReference
Cannot resolve symbol `AnnotationConfigUtils`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/schema/DubboNamespaceHandler.java`
#### Snippet
```java
     *
     * @param registry {@link BeanDefinitionRegistry}
     * @see AnnotationConfigUtils
     * @since 2.7.5
     */
```

### JavadocReference
Cannot resolve symbol `NamespaceHandlerSupport`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/schema/DubboNamespaceHandler.java`
#### Snippet
```java

    /**
     * Override {@link NamespaceHandlerSupport#parse(Element, ParserContext)} method
     *
     * @param element       {@link Element}
```

### JavadocReference
Cannot resolve symbol `parse(Element, ParserContext)`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/schema/DubboNamespaceHandler.java`
#### Snippet
```java

    /**
     * Override {@link NamespaceHandlerSupport#parse(Element, ParserContext)} method
     *
     * @param element       {@link Element}
```

### JavadocReference
Cannot resolve symbol `tryGetMergedAnnotationAttributes(AnnotatedElement, Class, PropertyResolver, boolean, String...)`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/AnnotationUtils.java`
#### Snippet
```java
     * Get the {@link AnnotationAttributes}, if the argument <code>tryMergedAnnotation</code> is <code>true</code>,
     * the {@link AnnotationAttributes} will be got from
     * {@link #tryGetMergedAnnotationAttributes(AnnotatedElement, Class, PropertyResolver, boolean, String...) merged annotation} first,
     * if failed, and then to get from
     * {@link #getAnnotationAttributes(AnnotatedElement, Class, PropertyResolver, boolean, boolean, String...) normal one}
```

### JavadocReference
Cannot resolve symbol `PropertyResolver`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/AnnotationUtils.java`
#### Snippet
```java
     * Get the {@link AnnotationAttributes}, if the argument <code>tryMergedAnnotation</code> is <code>true</code>,
     * the {@link AnnotationAttributes} will be got from
     * {@link #tryGetMergedAnnotationAttributes(AnnotatedElement, Class, PropertyResolver, boolean, String...) merged annotation} first,
     * if failed, and then to get from
     * {@link #getAnnotationAttributes(AnnotatedElement, Class, PropertyResolver, boolean, boolean, String...) normal one}
```

### JavadocReference
Cannot resolve symbol `PropertyResolver`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/AnnotationUtils.java`
#### Snippet
```java
     * {@link #tryGetMergedAnnotationAttributes(AnnotatedElement, Class, PropertyResolver, boolean, String...) merged annotation} first,
     * if failed, and then to get from
     * {@link #getAnnotationAttributes(AnnotatedElement, Class, PropertyResolver, boolean, boolean, String...) normal one}
     *
     * @param annotatedElement     {@link AnnotatedElement the annotated element}
```

### JavadocReference
Cannot resolve symbol `PropertyResolver`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/AnnotationUtils.java`
#### Snippet
```java
     * @param annotatedElement     {@link AnnotatedElement the annotated element}
     * @param annotationType       the {@link Class tyoe} pf {@link Annotation annotation}
     * @param propertyResolver     {@link PropertyResolver} instance, e.g {@link Environment}
     * @param ignoreDefaultValue   whether ignore default value or not
     * @param tryMergedAnnotation  whether try merged annotation or not
```

### JavadocReference
Cannot resolve symbol `Environment`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/AnnotationUtils.java`
#### Snippet
```java
     * @param annotatedElement     {@link AnnotatedElement the annotated element}
     * @param annotationType       the {@link Class tyoe} pf {@link Annotation annotation}
     * @param propertyResolver     {@link PropertyResolver} instance, e.g {@link Environment}
     * @param ignoreDefaultValue   whether ignore default value or not
     * @param tryMergedAnnotation  whether try merged annotation or not
```

### JavadocReference
Cannot resolve symbol `AnnotationAttributes`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/AnnotationUtils.java`
#### Snippet
```java

    /**
     * Get the {@link AnnotationAttributes}
     *
     * @param annotatedElement     {@link AnnotatedElement the annotated element}
```

### JavadocReference
Cannot resolve symbol `PropertyResolver`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/AnnotationUtils.java`
#### Snippet
```java
     * @param annotatedElement     {@link AnnotatedElement the annotated element}
     * @param annotationType       the {@link Class tyoe} pf {@link Annotation annotation}
     * @param propertyResolver     {@link PropertyResolver} instance, e.g {@link Environment}
     * @param ignoreDefaultValue   whether ignore default value or not
     * @param ignoreAttributeNames the attribute names of annotation should be ignored
```

### JavadocReference
Cannot resolve symbol `Environment`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/AnnotationUtils.java`
#### Snippet
```java
     * @param annotatedElement     {@link AnnotatedElement the annotated element}
     * @param annotationType       the {@link Class tyoe} pf {@link Annotation annotation}
     * @param propertyResolver     {@link PropertyResolver} instance, e.g {@link Environment}
     * @param ignoreDefaultValue   whether ignore default value or not
     * @param ignoreAttributeNames the attribute names of annotation should be ignored
```

### JavadocReference
Cannot resolve symbol `AnnotationAttributes`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/AnnotationUtils.java`
#### Snippet
```java

    /**
     * Get the {@link AnnotationAttributes}
     *
     * @param annotation           specified {@link Annotation}
```

### JavadocReference
Cannot resolve symbol `PropertyResolver`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/AnnotationUtils.java`
#### Snippet
```java
     * @param ignoreAttributeNames the attribute names of annotation should be ignored
     * @return non-null
     * @see #getAnnotationAttributes(Annotation, PropertyResolver, boolean, String...)
     */
    public static AnnotationAttributes getAnnotationAttributes(Annotation annotation, boolean ignoreDefaultValue,
```

### JavadocReference
Cannot resolve symbol `AnnotationAttributes`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/AnnotationUtils.java`
#### Snippet
```java

    /**
     * Try to get {@link AnnotationAttributes the annotation attributes} after merging and resolving the placeholders
     *
     * @param annotatedElement       {@link AnnotatedElement the annotated element}
```

### JavadocReference
Cannot resolve symbol `PropertyResolver`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/AnnotationUtils.java`
#### Snippet
```java
     * @param annotatedElement       {@link AnnotatedElement the annotated element}
     * @param annotationType         the {@link Class tyoe} pf {@link Annotation annotation}
     * @param propertyResolver       {@link PropertyResolver} instance, e.g {@link Environment}
     * @param classValuesAsString    whether to turn Class references into Strings (for
     *                               compatibility with {@link org.springframework.core.type.AnnotationMetadata} or to
```

### JavadocReference
Cannot resolve symbol `Environment`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/AnnotationUtils.java`
#### Snippet
```java
     * @param annotatedElement       {@link AnnotatedElement the annotated element}
     * @param annotationType         the {@link Class tyoe} pf {@link Annotation annotation}
     * @param propertyResolver       {@link PropertyResolver} instance, e.g {@link Environment}
     * @param classValuesAsString    whether to turn Class references into Strings (for
     *                               compatibility with {@link org.springframework.core.type.AnnotationMetadata} or to
```

### JavadocReference
Cannot resolve symbol `org.springframework.core.type.AnnotationMetadata`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/AnnotationUtils.java`
#### Snippet
```java
     * @param propertyResolver       {@link PropertyResolver} instance, e.g {@link Environment}
     * @param classValuesAsString    whether to turn Class references into Strings (for
     *                               compatibility with {@link org.springframework.core.type.AnnotationMetadata} or to
     *                               preserve them as Class references
     * @param nestedAnnotationsAsMap whether to turn nested Annotation instances into
```

### JavadocReference
Cannot resolve symbol `ParserContext`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/schema/DubboNamespaceHandler.java`
#### Snippet
```java

    /**
     * Override {@link NamespaceHandlerSupport#parse(Element, ParserContext)} method
     *
     * @param element       {@link Element}
```

### JavadocReference
Cannot resolve symbol `ParserContext`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/schema/DubboNamespaceHandler.java`
#### Snippet
```java
     *
     * @param element       {@link Element}
     * @param parserContext {@link ParserContext}
     * @return
     * @since 2.7.5
```

### JavadocReference
Cannot resolve symbol `AnnotationAttributes`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/AnnotationUtils.java`
#### Snippet
```java
     *                               preserve them as Class references
     * @param nestedAnnotationsAsMap whether to turn nested Annotation instances into
     *                               {@link AnnotationAttributes} maps (for compatibility with
     *                               {@link org.springframework.core.type.AnnotationMetadata} or to preserve them as
     *                               Annotation instances
```

### JavadocReference
Cannot resolve symbol `org.springframework.core.type.AnnotationMetadata`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/AnnotationUtils.java`
#### Snippet
```java
     * @param nestedAnnotationsAsMap whether to turn nested Annotation instances into
     *                               {@link AnnotationAttributes} maps (for compatibility with
     *                               {@link org.springframework.core.type.AnnotationMetadata} or to preserve them as
     *                               Annotation instances
     * @param ignoreDefaultValue     whether ignore default value or not
```

### JavadocReference
Cannot resolve symbol `PropertyResolver`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/AnnotationUtils.java`
#### Snippet
```java
     *
     * @param annotation           specified {@link Annotation}
     * @param propertyResolver     {@link PropertyResolver} instance, e.g {@link Environment}
     * @param ignoreDefaultValue   whether ignore default value or not
     * @param ignoreAttributeNames the attribute names of annotation should be ignored
```

### JavadocReference
Cannot resolve symbol `Environment`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/AnnotationUtils.java`
#### Snippet
```java
     *
     * @param annotation           specified {@link Annotation}
     * @param propertyResolver     {@link PropertyResolver} instance, e.g {@link Environment}
     * @param ignoreDefaultValue   whether ignore default value or not
     * @param ignoreAttributeNames the attribute names of annotation should be ignored
```

### JavadocReference
Cannot resolve symbol `PropertyResolver`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/AnnotationUtils.java`
#### Snippet
```java
    /**
     * @param annotation             specified {@link Annotation}
     * @param propertyResolver       {@link PropertyResolver} instance, e.g {@link Environment}
     * @param classValuesAsString    whether to turn Class references into Strings (for
     *                               compatibility with {@link org.springframework.core.type.AnnotationMetadata} or to
```

### JavadocReference
Cannot resolve symbol `Environment`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/AnnotationUtils.java`
#### Snippet
```java
    /**
     * @param annotation             specified {@link Annotation}
     * @param propertyResolver       {@link PropertyResolver} instance, e.g {@link Environment}
     * @param classValuesAsString    whether to turn Class references into Strings (for
     *                               compatibility with {@link org.springframework.core.type.AnnotationMetadata} or to
```

### JavadocReference
Cannot resolve symbol `org.springframework.core.type.AnnotationMetadata`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/AnnotationUtils.java`
#### Snippet
```java
     * @param propertyResolver       {@link PropertyResolver} instance, e.g {@link Environment}
     * @param classValuesAsString    whether to turn Class references into Strings (for
     *                               compatibility with {@link org.springframework.core.type.AnnotationMetadata} or to
     *                               preserve them as Class references
     * @param nestedAnnotationsAsMap whether to turn nested Annotation instances into
```

### JavadocReference
Cannot resolve symbol `AnnotationAttributes`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/AnnotationUtils.java`
#### Snippet
```java
     *                               preserve them as Class references
     * @param nestedAnnotationsAsMap whether to turn nested Annotation instances into
     *                               {@link AnnotationAttributes} maps (for compatibility with
     *                               {@link org.springframework.core.type.AnnotationMetadata} or to preserve them as
     *                               Annotation instances
```

### JavadocReference
Cannot resolve symbol `org.springframework.core.type.AnnotationMetadata`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/AnnotationUtils.java`
#### Snippet
```java
     * @param nestedAnnotationsAsMap whether to turn nested Annotation instances into
     *                               {@link AnnotationAttributes} maps (for compatibility with
     *                               {@link org.springframework.core.type.AnnotationMetadata} or to preserve them as
     *                               Annotation instances
     * @param ignoreDefaultValue     whether ignore default value or not
```

### JavadocReference
Cannot resolve symbol `AnnotationAttributes`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/AnnotationUtils.java`
#### Snippet
```java
     * Get the required attribute value
     *
     * @param attributes    {@link Map the annotation attributes} or {@link AnnotationAttributes}
     * @param attributeName the name of attribute
     * @param <T>           the type of attribute value
```

### JavadocReference
Cannot resolve symbol `AnnotationAttributes`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/AnnotationUtils.java`
#### Snippet
```java
     * Get the attribute value with default value
     *
     * @param attributes    {@link Map the annotation attributes} or {@link AnnotationAttributes}
     * @param attributeName the name of attribute
     * @param defaultValue  the default value of attribute
```

### JavadocReference
Cannot resolve symbol `AnnotationAttributes`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/AnnotationUtils.java`
#### Snippet
```java

    /**
     * Get the {@link AnnotationAttributes}
     *
     * @param annotation             specified {@link Annotation}
```

### JavadocReference
Cannot resolve symbol `PropertyResolver`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/AnnotationUtils.java`
#### Snippet
```java
     *
     * @param annotation             specified {@link Annotation}
     * @param propertyResolver       {@link PropertyResolver} instance, e.g {@link Environment}
     * @param classValuesAsString    whether to turn Class references into Strings (for
     *                               compatibility with {@link org.springframework.core.type.AnnotationMetadata} or to
```

### JavadocReference
Cannot resolve symbol `Environment`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/AnnotationUtils.java`
#### Snippet
```java
     *
     * @param annotation             specified {@link Annotation}
     * @param propertyResolver       {@link PropertyResolver} instance, e.g {@link Environment}
     * @param classValuesAsString    whether to turn Class references into Strings (for
     *                               compatibility with {@link org.springframework.core.type.AnnotationMetadata} or to
```

### JavadocReference
Cannot resolve symbol `org.springframework.core.type.AnnotationMetadata`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/AnnotationUtils.java`
#### Snippet
```java
     * @param propertyResolver       {@link PropertyResolver} instance, e.g {@link Environment}
     * @param classValuesAsString    whether to turn Class references into Strings (for
     *                               compatibility with {@link org.springframework.core.type.AnnotationMetadata} or to
     *                               preserve them as Class references
     * @param nestedAnnotationsAsMap whether to turn nested Annotation instances into
```

### JavadocReference
Cannot resolve symbol `AnnotationAttributes`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/AnnotationUtils.java`
#### Snippet
```java
     *                               preserve them as Class references
     * @param nestedAnnotationsAsMap whether to turn nested Annotation instances into
     *                               {@link AnnotationAttributes} maps (for compatibility with
     *                               {@link org.springframework.core.type.AnnotationMetadata} or to preserve them as
     *                               Annotation instances
```

### JavadocReference
Cannot resolve symbol `org.springframework.core.type.AnnotationMetadata`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/AnnotationUtils.java`
#### Snippet
```java
     * @param nestedAnnotationsAsMap whether to turn nested Annotation instances into
     *                               {@link AnnotationAttributes} maps (for compatibility with
     *                               {@link org.springframework.core.type.AnnotationMetadata} or to preserve them as
     *                               Annotation instances
     * @param ignoreAttributeNames   the attribute names of annotation should be ignored
```

### JavadocReference
Cannot resolve symbol `PropertyResolver`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/AnnotationUtils.java`
#### Snippet
```java
     * @param ignoreDefaultValue     whether ignore default value or not
     * @return non-null
     * @see #getAttributes(Annotation, PropertyResolver, boolean, String...)
     * @see #getAnnotationAttributes(AnnotatedElement, Class, PropertyResolver, boolean, String...)
     */
```

### JavadocReference
Cannot resolve symbol `getAnnotationAttributes(AnnotatedElement, Class, PropertyResolver, boolean, String...)`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/AnnotationUtils.java`
#### Snippet
```java
     * @return non-null
     * @see #getAttributes(Annotation, PropertyResolver, boolean, String...)
     * @see #getAnnotationAttributes(AnnotatedElement, Class, PropertyResolver, boolean, String...)
     */
    public static AnnotationAttributes getAnnotationAttributes(Annotation annotation,
```

### JavadocReference
Cannot resolve symbol `PropertyResolver`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/AnnotationUtils.java`
#### Snippet
```java
     * @return non-null
     * @see #getAttributes(Annotation, PropertyResolver, boolean, String...)
     * @see #getAnnotationAttributes(AnnotatedElement, Class, PropertyResolver, boolean, String...)
     */
    public static AnnotationAttributes getAnnotationAttributes(Annotation annotation,
```

### JavadocReference
Cannot resolve symbol `AnnotationAttributes`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/AnnotationUtils.java`
#### Snippet
```java

    /**
     * Get the {@link AnnotationAttributes}, if the argument <code>tryMergedAnnotation</code> is <code>true</code>,
     * the {@link AnnotationAttributes} will be got from
     * {@link #tryGetMergedAnnotationAttributes(AnnotatedElement, Class, PropertyResolver, boolean, String...) merged annotation} first,
```

### JavadocReference
Cannot resolve symbol `AnnotationAttributes`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/AnnotationUtils.java`
#### Snippet
```java
    /**
     * Get the {@link AnnotationAttributes}, if the argument <code>tryMergedAnnotation</code> is <code>true</code>,
     * the {@link AnnotationAttributes} will be got from
     * {@link #tryGetMergedAnnotationAttributes(AnnotatedElement, Class, PropertyResolver, boolean, String...) merged annotation} first,
     * if failed, and then to get from
```

### JavadocReference
Cannot resolve symbol `tryGetMergedAnnotationAttributes(AnnotatedElement, Class, PropertyResolver, boolean, String...)`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/AnnotationUtils.java`
#### Snippet
```java
     * Get the {@link AnnotationAttributes}, if the argument <code>tryMergedAnnotation</code> is <code>true</code>,
     * the {@link AnnotationAttributes} will be got from
     * {@link #tryGetMergedAnnotationAttributes(AnnotatedElement, Class, PropertyResolver, boolean, String...) merged annotation} first,
     * if failed, and then to get from
     * {@link #getAnnotationAttributes(AnnotatedElement, Class, PropertyResolver, boolean, boolean, String...) normal one}
```

### JavadocReference
Cannot resolve symbol `PropertyResolver`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/AnnotationUtils.java`
#### Snippet
```java
     * Get the {@link AnnotationAttributes}, if the argument <code>tryMergedAnnotation</code> is <code>true</code>,
     * the {@link AnnotationAttributes} will be got from
     * {@link #tryGetMergedAnnotationAttributes(AnnotatedElement, Class, PropertyResolver, boolean, String...) merged annotation} first,
     * if failed, and then to get from
     * {@link #getAnnotationAttributes(AnnotatedElement, Class, PropertyResolver, boolean, boolean, String...) normal one}
```

### JavadocReference
Cannot resolve symbol `PropertyResolver`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/AnnotationUtils.java`
#### Snippet
```java
     * {@link #tryGetMergedAnnotationAttributes(AnnotatedElement, Class, PropertyResolver, boolean, String...) merged annotation} first,
     * if failed, and then to get from
     * {@link #getAnnotationAttributes(AnnotatedElement, Class, PropertyResolver, boolean, boolean, String...) normal one}
     *
     * @param annotatedElement       {@link AnnotatedElement the annotated element}
```

### JavadocReference
Cannot resolve symbol `PropertyResolver`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/AnnotationUtils.java`
#### Snippet
```java
     * @param annotatedElement       {@link AnnotatedElement the annotated element}
     * @param annotationType         the {@link Class tyoe} pf {@link Annotation annotation}
     * @param propertyResolver       {@link PropertyResolver} instance, e.g {@link Environment}
     * @param classValuesAsString    whether to turn Class references into Strings (for
     *                               compatibility with {@link org.springframework.core.type.AnnotationMetadata} or to
```

### JavadocReference
Cannot resolve symbol `Environment`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/AnnotationUtils.java`
#### Snippet
```java
     * @param annotatedElement       {@link AnnotatedElement the annotated element}
     * @param annotationType         the {@link Class tyoe} pf {@link Annotation annotation}
     * @param propertyResolver       {@link PropertyResolver} instance, e.g {@link Environment}
     * @param classValuesAsString    whether to turn Class references into Strings (for
     *                               compatibility with {@link org.springframework.core.type.AnnotationMetadata} or to
```

### JavadocReference
Cannot resolve symbol `org.springframework.core.type.AnnotationMetadata`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/AnnotationUtils.java`
#### Snippet
```java
     * @param propertyResolver       {@link PropertyResolver} instance, e.g {@link Environment}
     * @param classValuesAsString    whether to turn Class references into Strings (for
     *                               compatibility with {@link org.springframework.core.type.AnnotationMetadata} or to
     *                               preserve them as Class references
     * @param nestedAnnotationsAsMap whether to turn nested Annotation instances into
```

### JavadocReference
Cannot resolve symbol `AnnotationAttributes`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/AnnotationUtils.java`
#### Snippet
```java
     *                               preserve them as Class references
     * @param nestedAnnotationsAsMap whether to turn nested Annotation instances into
     *                               {@link AnnotationAttributes} maps (for compatibility with
     *                               {@link org.springframework.core.type.AnnotationMetadata} or to preserve them as
     *                               Annotation instances
```

### JavadocReference
Cannot resolve symbol `org.springframework.core.type.AnnotationMetadata`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/AnnotationUtils.java`
#### Snippet
```java
     * @param nestedAnnotationsAsMap whether to turn nested Annotation instances into
     *                               {@link AnnotationAttributes} maps (for compatibility with
     *                               {@link org.springframework.core.type.AnnotationMetadata} or to preserve them as
     *                               Annotation instances
     * @param ignoreDefaultValue     whether ignore default value or not
```

### JavadocReference
Cannot resolve symbol `AnnotationAttributes`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/AnnotationUtils.java`
#### Snippet
```java
     * Get the attribute value
     *
     * @param attributes    {@link Map the annotation attributes} or {@link AnnotationAttributes}
     * @param attributeName the name of attribute
     * @param <T>           the type of attribute value
```

### JavadocReference
Cannot resolve symbol `PropertyResolver`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/AnnotationUtils.java`
#### Snippet
```java
     *
     * @param annotationAttributes the attributes of specified {@link Annotation}
     * @param propertyResolver     {@link PropertyResolver} instance, e.g {@link Environment}
     * @param ignoreAttributeNames the attribute names of annotation should be ignored
     * @return non-null
```

### JavadocReference
Cannot resolve symbol `Environment`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/AnnotationUtils.java`
#### Snippet
```java
     *
     * @param annotationAttributes the attributes of specified {@link Annotation}
     * @param propertyResolver     {@link PropertyResolver} instance, e.g {@link Environment}
     * @param ignoreAttributeNames the attribute names of annotation should be ignored
     * @return non-null
```

### JavadocReference
Cannot resolve symbol `BeanFactoryPostProcessor`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/ServiceAnnotationPostProcessor.java`
#### Snippet
```java

/**
 * A {@link BeanFactoryPostProcessor} used for processing of {@link Service @Service} annotated classes and annotated bean in java config classes.
 * It's also the infrastructure class of XML {@link BeanDefinitionParser} on &lt;dubbo:annotation /&gt;
 *
```

### JavadocReference
Cannot resolve symbol `BeanDefinitionParser`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/ServiceAnnotationPostProcessor.java`
#### Snippet
```java
/**
 * A {@link BeanFactoryPostProcessor} used for processing of {@link Service @Service} annotated classes and annotated bean in java config classes.
 * It's also the infrastructure class of XML {@link BeanDefinitionParser} on &lt;dubbo:annotation /&gt;
 *
 *
```

### JavadocReference
Cannot resolve symbol `BeanDefinitionRegistryPostProcessor`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/ServiceAnnotationPostProcessor.java`
#### Snippet
```java
 *
 * @see AnnotationBeanDefinitionParser
 * @see BeanDefinitionRegistryPostProcessor
 * @since 2.7.7
 */
```

### JavadocReference
Cannot resolve symbol `BeanDefinitionHolder`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/ServiceAnnotationPostProcessor.java`
#### Snippet
```java

    /**
     * Finds a {@link Set} of {@link BeanDefinitionHolder BeanDefinitionHolders} whose bean type annotated
     * {@link Service} Annotation.
     *
```

### JavadocReference
Cannot resolve symbol `InjectionMetadata.InjectedElement`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/AbstractAnnotationBeanPostProcessor.java`
#### Snippet
```java

    /**
     * Finds {@link InjectionMetadata.InjectedElement} Metadata from annotated fields
     *
     * @param beanClass The {@link Class} of Bean
```

### JavadocReference
Cannot resolve symbol `AnnotationAttributes`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/AbstractAnnotationBeanPostProcessor.java`
#### Snippet
```java
     * </ul>
     *
     * @param attributes      {@link AnnotationAttributes the annotation attributes}
     * @param bean            Current bean that will be injected
     * @param beanName        Current bean name that will be injected
```

### JavadocReference
Cannot resolve symbol `BeanPostProcessor`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/AbstractAnnotationBeanPostProcessor.java`
#### Snippet
```java

/**
 * Abstract common {@link BeanPostProcessor} implementation for customized annotation that annotated injected-object.
 */
@SuppressWarnings("unchecked")
```

### JavadocReference
Cannot resolve symbol `InjectionMetadata.InjectedElement`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/AbstractAnnotationBeanPostProcessor.java`
#### Snippet
```java

    /**
     * {@link Annotation Annotated} {@link Method} {@link InjectionMetadata.InjectedElement}
     */
    protected class AnnotatedInjectElement extends InjectionMetadata.InjectedElement {
```

### JavadocReference
Cannot resolve symbol `InjectionMetadata.InjectedElement`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/AbstractAnnotationBeanPostProcessor.java`
#### Snippet
```java

    /**
     * Finds {@link InjectionMetadata.InjectedElement} Metadata from annotated methods
     *
     * @param beanClass The {@link Class} of Bean
```

### JavadocReference
Cannot resolve symbol `AnnotationAttributes`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/AbstractAnnotationBeanPostProcessor.java`
#### Snippet
```java

    /**
     * Get injected-object from specified {@link AnnotationAttributes annotation attributes} and Bean Class
     *
     * @param attributes      {@link AnnotationAttributes the annotation attributes}
```

### JavadocReference
Cannot resolve symbol `BeanFactoryPostProcessor`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/context/DubboSpringInitCustomizer.java`
#### Snippet
```java
    /**
     * <p>Customize dubbo spring initialization on bean registry processing phase.</p>
     * <p>You can register a {@link BeanFactoryPostProcessor} or {@link BeanPostProcessor} for custom processing.</p>
     * <p>Or change the bind module model via {@link DubboSpringInitContext#setModuleModel(ModuleModel)}.</p>
     *
```

### JavadocReference
Cannot resolve symbol `BeanPostProcessor`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/context/DubboSpringInitCustomizer.java`
#### Snippet
```java
    /**
     * <p>Customize dubbo spring initialization on bean registry processing phase.</p>
     * <p>You can register a {@link BeanFactoryPostProcessor} or {@link BeanPostProcessor} for custom processing.</p>
     * <p>Or change the bind module model via {@link DubboSpringInitContext#setModuleModel(ModuleModel)}.</p>
     *
```

### JavadocReference
Cannot resolve symbol `AnnotationAttributes`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/AbstractAnnotationBeanPostProcessor.java`
#### Snippet
```java
     * Get injected-object from specified {@link AnnotationAttributes annotation attributes} and Bean Class
     *
     * @param attributes      {@link AnnotationAttributes the annotation attributes}
     * @param bean            Current bean that will be injected
     * @param beanName        Current bean name that will be injected
```

### JavadocReference
Cannot resolve symbol `BeanFactoryPostProcessor`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/context/DubboSpringInitCustomizer.java`
#### Snippet
```java
     * <p>1. The bean factory may be not ready yet when triggered by parsing dubbo xml definition.</p>
     * <p>2. Some bean definitions may be not registered at this moment. If you plan to process all bean definitions,
     * it is recommended to register a custom {@link BeanFactoryPostProcessor} to do so.</p>
     *
     * @param context
```

### JavadocReference
Cannot resolve symbol `InjectionMetadata`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/AbstractAnnotationBeanPostProcessor.java`
#### Snippet
```java

    /**
     * {@link Annotation Annotated} {@link InjectionMetadata} implementation
     */
    protected static class AnnotatedInjectionMetadata extends InjectionMetadata {
```

### JavadocReference
Cannot resolve symbol `ClassPathBeanDefinitionScanner`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/ServiceAnnotationPostProcessor.java`
#### Snippet
```java
     * {@link Service} Annotation.
     *
     * @param scanner       {@link ClassPathBeanDefinitionScanner}
     * @param packageToScan pachage to scan
     * @param registry      {@link BeanDefinitionRegistry}
```

### JavadocReference
Cannot resolve symbol `BeanDefinitionRegistry`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/ServiceAnnotationPostProcessor.java`
#### Snippet
```java
     * @param scanner       {@link ClassPathBeanDefinitionScanner}
     * @param packageToScan pachage to scan
     * @param registry      {@link BeanDefinitionRegistry}
     * @return non-null
     * @since 2.5.8
```

### JavadocReference
Cannot resolve symbol `AbstractBeanDefinition`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/ServiceAnnotationPostProcessor.java`
#### Snippet
```java

    /**
     * Build the {@link AbstractBeanDefinition Bean Definition}
     *
     *
```

### JavadocReference
Cannot resolve symbol `ConfigurationClassPostProcessor`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/ServiceAnnotationPostProcessor.java`
#### Snippet
```java
    /**
     * It'd be better to use BeanNameGenerator instance that should reference
     * {@link ConfigurationClassPostProcessor#componentScanBeanNameGenerator},
     * thus it maybe a potential problem on bean name generation.
     *
```

### JavadocReference
Cannot resolve symbol `componentScanBeanNameGenerator`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/ServiceAnnotationPostProcessor.java`
#### Snippet
```java
    /**
     * It'd be better to use BeanNameGenerator instance that should reference
     * {@link ConfigurationClassPostProcessor#componentScanBeanNameGenerator},
     * thus it maybe a potential problem on bean name generation.
     *
```

### JavadocReference
Cannot resolve symbol `BeanDefinitionRegistry`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/ServiceAnnotationPostProcessor.java`
#### Snippet
```java
     * thus it maybe a potential problem on bean name generation.
     *
     * @param registry {@link BeanDefinitionRegistry}
     * @return {@link BeanNameGenerator} instance
     * @see SingletonBeanRegistry
```

### JavadocReference
Cannot resolve symbol `BeanNameGenerator`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/ServiceAnnotationPostProcessor.java`
#### Snippet
```java
     *
     * @param registry {@link BeanDefinitionRegistry}
     * @return {@link BeanNameGenerator} instance
     * @see SingletonBeanRegistry
     * @see AnnotationConfigUtils#CONFIGURATION_BEAN_NAME_GENERATOR
```

### JavadocReference
Cannot resolve symbol `SingletonBeanRegistry`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/ServiceAnnotationPostProcessor.java`
#### Snippet
```java
     * @param registry {@link BeanDefinitionRegistry}
     * @return {@link BeanNameGenerator} instance
     * @see SingletonBeanRegistry
     * @see AnnotationConfigUtils#CONFIGURATION_BEAN_NAME_GENERATOR
     * @see ConfigurationClassPostProcessor#processConfigBeanDefinitions
```

### JavadocReference
Cannot resolve symbol `AnnotationConfigUtils`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/ServiceAnnotationPostProcessor.java`
#### Snippet
```java
     * @return {@link BeanNameGenerator} instance
     * @see SingletonBeanRegistry
     * @see AnnotationConfigUtils#CONFIGURATION_BEAN_NAME_GENERATOR
     * @see ConfigurationClassPostProcessor#processConfigBeanDefinitions
     * @since 2.5.8
```

### JavadocReference
Cannot resolve symbol `CONFIGURATION_BEAN_NAME_GENERATOR`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/ServiceAnnotationPostProcessor.java`
#### Snippet
```java
     * @return {@link BeanNameGenerator} instance
     * @see SingletonBeanRegistry
     * @see AnnotationConfigUtils#CONFIGURATION_BEAN_NAME_GENERATOR
     * @see ConfigurationClassPostProcessor#processConfigBeanDefinitions
     * @since 2.5.8
```

### JavadocReference
Cannot resolve symbol `ConfigurationClassPostProcessor`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/ServiceAnnotationPostProcessor.java`
#### Snippet
```java
     * @see SingletonBeanRegistry
     * @see AnnotationConfigUtils#CONFIGURATION_BEAN_NAME_GENERATOR
     * @see ConfigurationClassPostProcessor#processConfigBeanDefinitions
     * @since 2.5.8
     */
```

### JavadocReference
Cannot resolve symbol `processConfigBeanDefinitions`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/ServiceAnnotationPostProcessor.java`
#### Snippet
```java
     * @see SingletonBeanRegistry
     * @see AnnotationConfigUtils#CONFIGURATION_BEAN_NAME_GENERATOR
     * @see ConfigurationClassPostProcessor#processConfigBeanDefinitions
     * @since 2.5.8
     */
```

### JavadocReference
Cannot resolve symbol `BeanDefinitionRegistry`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/ServiceAnnotationPostProcessor.java`
#### Snippet
```java
     *
     * @param packagesToScan The base packages to scan
     * @param registry       {@link BeanDefinitionRegistry}
     */
    private void scanServiceBeans(Set<String> packagesToScan, BeanDefinitionRegistry registry) {
```

### JavadocReference
Cannot resolve symbol `BeanDefinition`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/ServiceAnnotationPostProcessor.java`
#### Snippet
```java

    /**
     * Registers {@link ServiceBean} from new annotated {@link Service} {@link BeanDefinition}
     *
     * @param beanDefinitionHolder
```

### JavadocReference
Cannot resolve symbol `BeanDefinition`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/ServiceAnnotationPostProcessor.java`
#### Snippet
```java
     * @param beanDefinitionHolder
     * @see ServiceBean
     * @see BeanDefinition
     */
    private void processScannedBeanDefinition(BeanDefinitionHolder beanDefinitionHolder) {
```

### JavadocReference
Cannot resolve symbol `ApplicationContext`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/context/DubboBootstrapApplicationListener.java`
#### Snippet
```java

    /**
     * Is original {@link ApplicationContext} as the event source
     *
     * @param event {@link ApplicationEvent}
```

### JavadocReference
Cannot resolve symbol `ApplicationEvent`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/context/DubboBootstrapApplicationListener.java`
#### Snippet
```java
     * Is original {@link ApplicationContext} as the event source
     *
     * @param event {@link ApplicationEvent}
     * @return if original, return <code>true</code>, or <code>false</code>
     */
```

### JavadocReference
Cannot resolve symbol `ApplicationListener`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/context/DubboBootstrapApplicationListener.java`
#### Snippet
```java

/**
 * The {@link ApplicationListener} for {@link DubboBootstrap}'s lifecycle when the {@link ContextRefreshedEvent}
 * and {@link ContextClosedEvent} raised
 *
```

### JavadocReference
Cannot resolve symbol `ContextRefreshedEvent`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/context/DubboBootstrapApplicationListener.java`
#### Snippet
```java

/**
 * The {@link ApplicationListener} for {@link DubboBootstrap}'s lifecycle when the {@link ContextRefreshedEvent}
 * and {@link ContextClosedEvent} raised
 *
```

### JavadocReference
Cannot resolve symbol `ContextClosedEvent`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/context/DubboBootstrapApplicationListener.java`
#### Snippet
```java
/**
 * The {@link ApplicationListener} for {@link DubboBootstrap}'s lifecycle when the {@link ContextRefreshedEvent}
 * and {@link ContextClosedEvent} raised
 *
 * @since 2.7.5
```

### JavadocReference
Cannot resolve symbol `org.springframework.context.ApplicationEvent`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/context/event/DubboBootstrapStatedEvent.java`
#### Snippet
```java

/**
 * A {@link org.springframework.context.ApplicationEvent} after {@link org.apache.dubbo.config.bootstrap.DubboBootstrap#start()} success
 *
 * @see org.springframework.context.ApplicationEvent
```

### JavadocReference
Cannot resolve symbol `org.springframework.context.ApplicationEvent`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/context/event/DubboBootstrapStatedEvent.java`
#### Snippet
```java
 * A {@link org.springframework.context.ApplicationEvent} after {@link org.apache.dubbo.config.bootstrap.DubboBootstrap#start()} success
 *
 * @see org.springframework.context.ApplicationEvent
 * @see org.springframework.context.ApplicationListener
 * @see org.apache.dubbo.config.bootstrap.DubboBootstrap
```

### JavadocReference
Cannot resolve symbol `org.springframework.context.ApplicationListener`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/context/event/DubboBootstrapStatedEvent.java`
#### Snippet
```java
 *
 * @see org.springframework.context.ApplicationEvent
 * @see org.springframework.context.ApplicationListener
 * @see org.apache.dubbo.config.bootstrap.DubboBootstrap
 * @since 2.7.9
```

### JavadocReference
Cannot resolve symbol `ApplicationEvent`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/context/event/ServiceBeanExportedEvent.java`
#### Snippet
```java

/**
 * A {@link ApplicationEvent} after {@link ServiceBean} {@link ServiceBean#export() export} invocation
 *
 * @see ApplicationEvent
```

### JavadocReference
Cannot resolve symbol `ApplicationEvent`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/context/event/ServiceBeanExportedEvent.java`
#### Snippet
```java
 * A {@link ApplicationEvent} after {@link ServiceBean} {@link ServiceBean#export() export} invocation
 *
 * @see ApplicationEvent
 * @see ApplicationListener
 * @see ServiceBean
```

### JavadocReference
Cannot resolve symbol `ApplicationListener`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/context/event/ServiceBeanExportedEvent.java`
#### Snippet
```java
 *
 * @see ApplicationEvent
 * @see ApplicationListener
 * @see ServiceBean
 * @since 2.6.5
```

### JavadocReference
Cannot resolve symbol `ApplicationEvent`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/context/event/DubboConfigInitEvent.java`
#### Snippet
```java

/**
 * An {@link ApplicationEvent} to trigger init {@link DubboConfigBeanInitializer}.
 *
 */
```

### JavadocReference
Cannot resolve symbol `org.springframework.context.ApplicationEvent`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/context/event/DubboBootstrapStopedEvent.java`
#### Snippet
```java

/**
 * A {@link org.springframework.context.ApplicationEvent} after {@link org.apache.dubbo.config.bootstrap.DubboBootstrap#stop()} success
 *
 * @see org.springframework.context.ApplicationEvent
```

### JavadocReference
Cannot resolve symbol `org.springframework.context.ApplicationEvent`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/context/event/DubboBootstrapStopedEvent.java`
#### Snippet
```java
 * A {@link org.springframework.context.ApplicationEvent} after {@link org.apache.dubbo.config.bootstrap.DubboBootstrap#stop()} success
 *
 * @see org.springframework.context.ApplicationEvent
 * @see org.springframework.context.ApplicationListener
 * @see org.apache.dubbo.config.bootstrap.DubboBootstrap
```

### JavadocReference
Cannot resolve symbol `org.springframework.context.ApplicationListener`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/context/event/DubboBootstrapStopedEvent.java`
#### Snippet
```java
 *
 * @see org.springframework.context.ApplicationEvent
 * @see org.springframework.context.ApplicationListener
 * @see org.apache.dubbo.config.bootstrap.DubboBootstrap
 * @since 2.7.9
```

### JavadocReference
Cannot resolve symbol `ApplicationContext`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/context/config/DubboConfigBeanCustomizer.java`
#### Snippet
```java
 * after {@link DubboConfigBinder#bind(String, AbstractConfig) its binding}.
 * <p>
 * If There are multiple {@link DubboConfigBeanCustomizer} beans in the Spring {@link ApplicationContext context}, they
 * are executed orderly, thus the subclass should be aware to implement the {@link #getOrder()} method.
 *
```

### JavadocReference
Cannot resolve symbol `getOrder()`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/context/config/DubboConfigBeanCustomizer.java`
#### Snippet
```java
 * <p>
 * If There are multiple {@link DubboConfigBeanCustomizer} beans in the Spring {@link ApplicationContext context}, they
 * are executed orderly, thus the subclass should be aware to implement the {@link #getOrder()} method.
 *
 * @see DubboConfigBinder#bind(String, AbstractConfig)
```

### JavadocReference
Cannot resolve symbol `org.springframework.context.support.PostProcessorRegistrationDelegate`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/context/DubboInfraBeanRegisterPostProcessor.java`
#### Snippet
```java
 * in order to enable the registered BeanFactoryPostProcessor bean to be loaded and executed.
 *
 * @see org.springframework.context.support.PostProcessorRegistrationDelegate#invokeBeanFactoryPostProcessors(
 *org.springframework.beans.factory.config.ConfigurableListableBeanFactory, java.util.List)
 */
```

### JavadocReference
Cannot resolve symbol `invokeBeanFactoryPostProcessors(
*org.springframework.beans.factory.config.ConfigurableListableBeanFactory, java.util.List)`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/context/DubboInfraBeanRegisterPostProcessor.java`
#### Snippet
```java
 * in order to enable the registered BeanFactoryPostProcessor bean to be loaded and executed.
 *
 * @see org.springframework.context.support.PostProcessorRegistrationDelegate#invokeBeanFactoryPostProcessors(
 *org.springframework.beans.factory.config.ConfigurableListableBeanFactory, java.util.List)
 */
```

### JavadocReference
Cannot resolve symbol `org.springframework.beans.factory.config.ConfigurableListableBeanFactory`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/context/DubboInfraBeanRegisterPostProcessor.java`
#### Snippet
```java
 *
 * @see org.springframework.context.support.PostProcessorRegistrationDelegate#invokeBeanFactoryPostProcessors(
 *org.springframework.beans.factory.config.ConfigurableListableBeanFactory, java.util.List)
 */
public class DubboInfraBeanRegisterPostProcessor implements BeanDefinitionRegistryPostProcessor, ApplicationContextAware {
```

### JavadocReference
Cannot resolve symbol `ImportBeanDefinitionRegistrar`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/context/annotation/DubboConfigConfigurationRegistrar.java`
#### Snippet
```java

/**
 * Dubbo {@link AbstractConfig Config} {@link ImportBeanDefinitionRegistrar register}, which order can be configured
 *
 * @see EnableDubboConfig
```

### JavadocReference
Cannot resolve symbol `Ordered`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/context/annotation/DubboConfigConfigurationRegistrar.java`
#### Snippet
```java
 * @see EnableDubboConfig
 * @see DubboConfigConfiguration
 * @see Ordered
 * @since 2.5.8
 */
```

### JavadocReference
Cannot resolve symbol `ImportBeanDefinitionRegistrar`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/context/annotation/DubboComponentScanRegistrar.java`
#### Snippet
```java
 * @see Service
 * @see DubboComponentScan
 * @see ImportBeanDefinitionRegistrar
 * @see ServiceAnnotationPostProcessor
 * @see ReferenceAnnotationBeanPostProcessor
```

### JavadocReference
Cannot resolve symbol `BeanDefinitionRegistry`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/context/annotation/DubboComponentScanRegistrar.java`
#### Snippet
```java
     *
     * @param packagesToScan packages to scan without resolving placeholders
     * @param registry       {@link BeanDefinitionRegistry}
     * @since 2.5.8
     */
```

### JavadocReference
Cannot resolve symbol `ClassPathBeanDefinitionScanner`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/context/annotation/DubboClassPathBeanDefinitionScanner.java`
#### Snippet
```java

/**
 * Dubbo {@link ClassPathBeanDefinitionScanner} that exposes some methods to be public.
 *
 * @see #doScan(String...)
```

### JavadocReference
Cannot resolve symbol `doScan(String...)`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/context/annotation/DubboClassPathBeanDefinitionScanner.java`
#### Snippet
```java
 * Dubbo {@link ClassPathBeanDefinitionScanner} that exposes some methods to be public.
 *
 * @see #doScan(String...)
 * @see #registerDefaultFilters()
 * @since 2.5.7
```

### JavadocReference
Cannot resolve symbol `registerDefaultFilters()`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/context/annotation/DubboClassPathBeanDefinitionScanner.java`
#### Snippet
```java
 *
 * @see #doScan(String...)
 * @see #registerDefaultFilters()
 * @since 2.5.7
 */
```

### JavadocReference
Cannot resolve symbol `BeanFactoryPostProcessor`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/ReferenceAnnotationBeanPostProcessor.java`
#### Snippet
```java
 * <p>
 * Step 1:
 * The purpose of implementing {@link BeanFactoryPostProcessor} is to scan the registration reference bean definition earlier,
 * so that it can be shared with the xml bean configuration.
 * </p>
```

### JavadocReference
Cannot resolve symbol `org.springframework.beans.factory.config.InstantiationAwareBeanPostProcessor`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/ReferenceAnnotationBeanPostProcessor.java`
#### Snippet
```java
 * <p>
 * Step 2:
 * By implementing {@link org.springframework.beans.factory.config.InstantiationAwareBeanPostProcessor},
 * inject the reference bean instance into the fields and setter methods which annotated with {@link DubboReference}.
 * </p>
```

### JavadocReference
Cannot resolve symbol `PropertyValues`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/ReferenceAnnotationBeanPostProcessor.java`
#### Snippet
```java

    /**
     * Alternatives to the {@link #postProcessPropertyValues(PropertyValues, PropertyDescriptor[], Object, String)}.
     * @see #postProcessPropertyValues
     */
```

### JavadocReference
Cannot resolve symbol `PropertyValues`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/ReferenceAnnotationBeanPostProcessor.java`
#### Snippet
```java

    /**
     * Alternatives to the {@link #postProcessProperties(PropertyValues, Object, String)}, that removed as of Spring
     * Framework 6.0.0, and in favor of {@link #postProcessProperties(PropertyValues, Object, String)}.
     * <p>In order to be compatible with the lower version of Spring, it is still retained.
```

### JavadocReference
Cannot resolve symbol `PropertyValues`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/ReferenceAnnotationBeanPostProcessor.java`
#### Snippet
```java
    /**
     * Alternatives to the {@link #postProcessProperties(PropertyValues, Object, String)}, that removed as of Spring
     * Framework 6.0.0, and in favor of {@link #postProcessProperties(PropertyValues, Object, String)}.
     * <p>In order to be compatible with the lower version of Spring, it is still retained.
     * @see #postProcessProperties
```

### JavadocReference
Cannot resolve symbol `PropertySource`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/context/properties/AbstractDubboConfigBinder.java`
#### Snippet
```java

    /**
     * Get multiple {@link PropertySource propertySources}
     *
     * @return multiple {@link PropertySource propertySources}
```

### JavadocReference
Cannot resolve symbol `PropertySource`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/context/properties/AbstractDubboConfigBinder.java`
#### Snippet
```java
     * Get multiple {@link PropertySource propertySources}
     *
     * @return multiple {@link PropertySource propertySources}
     */
    protected Iterable<PropertySource<?>> getPropertySources() {
```

### JavadocReference
Cannot resolve symbol `EnvironmentAware`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/context/properties/DubboConfigBinder.java`
#### Snippet
```java
 *
 * @see AbstractConfig
 * @see EnvironmentAware
 * @since 2.5.11
 */
```

### JavadocReference
Cannot resolve symbol `Configuration`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/context/annotation/DubboConfigConfiguration.java`
#### Snippet
```java

/**
 * Dubbo {@link AbstractConfig Config} {@link Configuration}
 *
 * @revised 2.7.5
```

### JavadocReference
Cannot resolve symbol `Configuration`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/context/annotation/DubboConfigConfiguration.java`
#### Snippet
```java
 *
 * @revised 2.7.5
 * @see Configuration
 * @see EnableConfigurationBeanBindings
 * @see EnableConfigurationBeanBinding
```

### JavadocReference
Cannot resolve symbol `DataBinder`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/context/properties/DefaultDubboConfigBinder.java`
#### Snippet
```java

/**
 * Default {@link DubboConfigBinder} implementation based on Spring {@link DataBinder}
 */
public class DefaultDubboConfigBinder extends AbstractDubboConfigBinder {
```

### JavadocReference
Cannot resolve symbol `AbstractKryoFactory`
in `dubbo-serialization/dubbo-serialization-api/src/main/java/org/apache/dubbo/common/serialize/support/SerializableClassRegistry.java`
#### Snippet
```java
/**
 * Provide a unified serialization registry, this class used for {@code dubbo-serialization-fst}
 * and {@code dubbo-serialization-kryo}, it will register some classes at startup time (for example {@link AbstractKryoFactory#create})
 */
public abstract class SerializableClassRegistry {
```

### JavadocReference
Cannot resolve symbol `create`
in `dubbo-serialization/dubbo-serialization-api/src/main/java/org/apache/dubbo/common/serialize/support/SerializableClassRegistry.java`
#### Snippet
```java
/**
 * Provide a unified serialization registry, this class used for {@code dubbo-serialization-fst}
 * and {@code dubbo-serialization-kryo}, it will register some classes at startup time (for example {@link AbstractKryoFactory#create})
 */
public abstract class SerializableClassRegistry {
```

### JavadocReference
Symbol `org.apache.dubbo.rpc.cluster.loadbalance.LeastActiveLoadBalance` is inaccessible from here
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcStatus.java`
#### Snippet
```java
 * @see org.apache.dubbo.rpc.filter.ActiveLimitFilter
 * @see org.apache.dubbo.rpc.filter.ExecuteLimitFilter
 * @see org.apache.dubbo.rpc.cluster.loadbalance.LeastActiveLoadBalance
 */
public class RpcStatus {
```

### JavadocReference
Cannot resolve symbol `Constants.LAZY_REQUEST_WITH_WARNING_KEY`
in `dubbo-rpc/dubbo-rpc-dubbo/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/LazyConnectExchangeClient.java`
#### Snippet
```java

    /**
     * If {@link Constants.LAZY_REQUEST_WITH_WARNING_KEY} is configured, then warn once every 5000 invocations.
     */
    private void warning() {
```

### JavadocReference
Cannot resolve symbol `HealthCheckResponse.ServingStatus`
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/service/HealthStatusManager.java`
#### Snippet
```java
     *                running with. It can also be an empty String {@code ""} per the gRPC
     *                specification.
     * @param status  is one of the values {@link HealthCheckResponse.ServingStatus#SERVING}, {@link
     *                HealthCheckResponse.ServingStatus#NOT_SERVING} and {@link
     *                HealthCheckResponse.ServingStatus#UNKNOWN}.
```

### JavadocReference
Cannot resolve symbol `SERVING`
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/service/HealthStatusManager.java`
#### Snippet
```java
     *                running with. It can also be an empty String {@code ""} per the gRPC
     *                specification.
     * @param status  is one of the values {@link HealthCheckResponse.ServingStatus#SERVING}, {@link
     *                HealthCheckResponse.ServingStatus#NOT_SERVING} and {@link
     *                HealthCheckResponse.ServingStatus#UNKNOWN}.
```

### JavadocReference
Cannot resolve symbol `HealthCheckResponse.ServingStatus`
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/service/HealthStatusManager.java`
#### Snippet
```java
     *                specification.
     * @param status  is one of the values {@link HealthCheckResponse.ServingStatus#SERVING}, {@link
     *                HealthCheckResponse.ServingStatus#NOT_SERVING} and {@link
     *                HealthCheckResponse.ServingStatus#UNKNOWN}.
     */
```

### JavadocReference
Cannot resolve symbol `NOT_SERVING`
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/service/HealthStatusManager.java`
#### Snippet
```java
     *                specification.
     * @param status  is one of the values {@link HealthCheckResponse.ServingStatus#SERVING}, {@link
     *                HealthCheckResponse.ServingStatus#NOT_SERVING} and {@link
     *                HealthCheckResponse.ServingStatus#UNKNOWN}.
     */
```

### JavadocReference
Cannot resolve symbol `HealthCheckResponse.ServingStatus`
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/service/HealthStatusManager.java`
#### Snippet
```java
     * @param status  is one of the values {@link HealthCheckResponse.ServingStatus#SERVING}, {@link
     *                HealthCheckResponse.ServingStatus#NOT_SERVING} and {@link
     *                HealthCheckResponse.ServingStatus#UNKNOWN}.
     */
    public void setStatus(String service, HealthCheckResponse.ServingStatus status) {
```

### JavadocReference
Cannot resolve symbol `UNKNOWN`
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/service/HealthStatusManager.java`
#### Snippet
```java
     * @param status  is one of the values {@link HealthCheckResponse.ServingStatus#SERVING}, {@link
     *                HealthCheckResponse.ServingStatus#NOT_SERVING} and {@link
     *                HealthCheckResponse.ServingStatus#UNKNOWN}.
     */
    public void setStatus(String service, HealthCheckResponse.ServingStatus status) {
```

### JavadocReference
Cannot resolve symbol `join(Directory)`
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/RouterFactory.java`
#### Snippet
```java
 * <a href="http://en.wikipedia.org/wiki/Routing">Routing</a>
 *
 * @see org.apache.dubbo.rpc.cluster.Cluster#join(Directory)
 * @see org.apache.dubbo.rpc.cluster.Directory#list(org.apache.dubbo.rpc.Invocation)
 * <p>
```

### JavadocReference
Cannot resolve symbol `join(Directory)`
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/Directory.java`
#### Snippet
```java
 * <a href="http://en.wikipedia.org/wiki/Directory_service">Directory Service</a>
 *
 * @see org.apache.dubbo.rpc.cluster.Cluster#join(Directory)
 */
public interface Directory<T> extends Node {
```

### JavadocReference
Cannot resolve symbol `join(Directory)`
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/LoadBalance.java`
#### Snippet
```java
 * <a href="http://en.wikipedia.org/wiki/Load_balancing_(computing)">Load-Balancing</a>
 *
 * @see org.apache.dubbo.rpc.cluster.Cluster#join(Directory)
 */
@SPI(RandomLoadBalance.NAME)
```

### JavadocReference
Cannot resolve symbol `ArtifactsFilter`
in `dubbo-maven-plugin/src/main/java/org/apache/dubbo/maven/plugin/aot/IncludeFilter.java`
#### Snippet
```java

/**
 * An {@link ArtifactsFilter} that filters out any artifact not matching an
 * {@link Include}.
 *
```

### JavadocReference
Cannot resolve symbol `org.apache.maven.shared.artifact.filter.collection.ArtifactsFilter`
in `dubbo-maven-plugin/src/main/java/org/apache/dubbo/maven/plugin/aot/MatchingGroupIdFilter.java`
#### Snippet
```java

/**
 * An {@link org.apache.maven.shared.artifact.filter.collection.ArtifactsFilter
 * ArtifactsFilter} that filters by matching groupId.
 *
```

### JavadocReference
Cannot resolve symbol `org.apache.maven.shared.artifact.filter.collection.GroupIdFilter`
in `dubbo-maven-plugin/src/main/java/org/apache/dubbo/maven/plugin/aot/MatchingGroupIdFilter.java`
#### Snippet
```java
 *
 * Preferred over the
 * {@link org.apache.maven.shared.artifact.filter.collection.GroupIdFilter} due to that
 * classes use of {@link String#startsWith} to match on prefix.
 *
```

### JavadocReference
Cannot resolve symbol `ArtifactFilter`
in `dubbo-maven-plugin/src/main/java/org/apache/dubbo/maven/plugin/aot/AbstractDependencyFilterMojo.java`
#### Snippet
```java

    /**
     * {@link ArtifactFilter} to exclude test scope dependencies.
     */
    protected static class ExcludeTestScopeArtifactFilter extends AbstractArtifactFeatureFilter {
```

### JavadocReference
Cannot resolve symbol `Artifact`
in `dubbo-maven-plugin/src/main/java/org/apache/dubbo/maven/plugin/aot/DependencyFilter.java`
#### Snippet
```java

	/**
	 * Check if the specified {@link Artifact} matches the
	 * specified {@link org.apache.dubbo.maven.plugin.aot.FilterableDependency}. Returns
	 * {@code true} if it should be excluded
```

### JavadocReference
Cannot resolve symbol `Artifact`
in `dubbo-maven-plugin/src/main/java/org/apache/dubbo/maven/plugin/aot/DependencyFilter.java`
#### Snippet
```java
	 * specified {@link org.apache.dubbo.maven.plugin.aot.FilterableDependency}. Returns
	 * {@code true} if it should be excluded
	 * @param artifact the Maven {@link Artifact}
	 * @param dependency the {@link org.apache.dubbo.maven.plugin.aot.FilterableDependency}
	 * @return {@code true} if the artifact matches the dependency
```

### JavadocReference
Cannot resolve symbol `ArtifactsFilter`
in `dubbo-maven-plugin/src/main/java/org/apache/dubbo/maven/plugin/aot/DependencyFilter.java`
#### Snippet
```java

/**
 * Base class for {@link ArtifactsFilter} based on a {@link org.apache.dubbo.maven.plugin.aot.FilterableDependency} list.
 *
 * @author Stephane Nicoll
```

## RuleId[id=ManualArrayToCollectionCopy]
### ManualArrayToCollectionCopy
Manual array to collection copy
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/ReflectUtils.java`
#### Snippet
```java

    private static void filterConstructMethod(Set<Constructor<?>> methods, Constructor<?>[] declaredMethods) {
        for (Constructor<?> constructor : declaredMethods) {
            methods.add(constructor);
        }
```

### ManualArrayToCollectionCopy
Manual array to collection copy
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/message/MediaTypeMatcher.java`
#### Snippet
```java
        List<MediaType> mediaTypeList = getDefaultList();

        for (MediaType mediaType : mediaTypes) {

            mediaTypeList.add(mediaType);
```

### ManualArrayToCollectionCopy
Manual array to collection copy
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/CollectionUtils.java`
#### Snippet
```java

        Set<T> elements = new LinkedHashSet<>(size, loadFactor);
        for (int i = 0; i < size; i++) {
            elements.add(values[i]);
        }
```

### ManualArrayToCollectionCopy
Manual array to collection copy
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/MethodUtils.java`
#### Snippet
```java
            Method[] methods = publicOnly ? classToSearch.getMethods() : classToSearch.getDeclaredMethods();
            // Add the declared methods or public methods
            for (Method method : methods) {
                allMethods.add(method);
            }
```

## RuleId[id=UnnecessarySemicolon]
### UnnecessarySemicolon
Unnecessary semicolon `;`
in `dubbo-native/src/main/java/org/apache/dubbo/aot/generate/MemberCategory.java`
#### Snippet
```java
	 * via a call to {@link Class#getDeclaredClasses}.
	 */
	DECLARED_CLASSES;

}
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/event/listener/ServiceInstancesChangedListener.java`
#### Snippet
```java
import static org.apache.dubbo.registry.client.metadata.ServiceInstanceMetadataUtils.getExportedServicesRevision;

;

/**
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/InvokeMode.java`
#### Snippet
```java
public enum InvokeMode {

    SYNC, ASYNC, FUTURE;

}
```

## RuleId[id=SimplifyStreamApiCallChains]
### SimplifyStreamApiCallChains
''stream().forEach()'' can be replaced with 'forEach()'' (may change semantics)
in `dubbo-registry/dubbo-registry-zookeeper/src/main/java/org/apache/dubbo/registry/zookeeper/ZookeeperRegistry.java`
#### Snippet
```java
            if (ANY_VALUE.equals(url.getServiceInterface())) {
                String root = toRootPath();
                childListeners.stream().forEach(childListener -> zkClient.removeChildListener(root, childListener));
            } else {
                for (String path : toCategoriesPath(url)) {
```

### SimplifyStreamApiCallChains
''stream().forEach()'' can be replaced with 'forEach()'' (may change semantics)
in `dubbo-registry/dubbo-registry-zookeeper/src/main/java/org/apache/dubbo/registry/zookeeper/ZookeeperRegistry.java`
#### Snippet
```java
            } else {
                for (String path : toCategoriesPath(url)) {
                    childListeners.stream().forEach(childListener -> zkClient.removeChildListener(path, childListener));
                }
            }
```

### SimplifyStreamApiCallChains
''stream().forEach()'' can be replaced with 'forEach()'' (may change semantics)
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/rest/AbstractServiceRestMetadataResolver.java`
#### Snippet
```java

    private void putServiceMethodToMap(Map<Method, Method> serviceMethodsMap, List<Method> declaredServiceMethods) {
        declaredServiceMethods.stream().forEach(method -> {

            // filter static private default
```

### SimplifyStreamApiCallChains
''stream().forEach()'' can be replaced with 'forEach()'' (may change semantics)
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/PathAndInvokerMapper.java`
#### Snippet
```java
    public void addPathAndInvoker(Map<PathMatcher, RestMethodMetadata> metadataMap, Invoker invoker) {

        metadataMap.entrySet().stream().forEach(entry -> {
            PathMatcher pathMatcher = entry.getKey();
            if (pathMatcher.hasPathVariable()) {
```

### SimplifyStreamApiCallChains
''stream().forEach()'' can be replaced with 'forEach()'' (may change semantics)
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/NettyHttpRestServer.java`
#### Snippet
```java
        Map<PathMatcher, RestMethodMetadata> pathToServiceMapContainPathVariable =
            serviceRestMetadata.getPathContainPathVariableToServiceMap();
        pathToServiceMapContainPathVariable.keySet().stream().forEach(pathAndInvokerMapper::removePath);

        Map<PathMatcher, RestMethodMetadata> pathToServiceMapUnContainPathVariable =
```

### SimplifyStreamApiCallChains
''stream().forEach()'' can be replaced with 'forEach()'' (may change semantics)
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/NettyHttpRestServer.java`
#### Snippet
```java
        Map<PathMatcher, RestMethodMetadata> pathToServiceMapUnContainPathVariable =
            serviceRestMetadata.getPathUnContainPathVariableToServiceMap();
        pathToServiceMapUnContainPathVariable.keySet().stream().forEach(pathAndInvokerMapper::removePath);

    }
```

### SimplifyStreamApiCallChains
''stream().forEach()'' can be replaced with 'forEach()'' (may change semantics)
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/HttpHeaderUtil.java`
#### Snippet
```java
        Map<String, List<String>> attachments = createAttachments(RpcContext.getServerContext().getObjectAttachments());

        attachments.entrySet().stream().forEach(attachment -> {
            nettyHttpResponse.getOutputHeaders().put(appendPrefixToAttachRealHeader(attachment.getKey()), attachment.getValue());
        });
```

### SimplifyStreamApiCallChains
''stream().forEach()'' can be replaced with 'forEach()'' (may change semantics)
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/HttpHeaderUtil.java`
#### Snippet
```java
        }

        headers.entrySet().stream().forEach(entry -> {
            String header = entry.getKey();
            if (isRestAttachHeader(header)) {
```

### SimplifyStreamApiCallChains
''stream().forEach()'' can be replaced with 'forEach()'' (may change semantics)
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/message/codec/MultiValueCodec.java`
#### Snippet
```java
            Object emptyObject = ReflectUtils.getEmptyObject(targetType);

            beanPropertyFields.entrySet().stream().forEach(entry -> {
                try {
                    List values = (List) valuesMap.get(entry.getKey());
```

### SimplifyStreamApiCallChains
''stream().forEach()'' can be replaced with 'forEach()'' (may change semantics)
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/annotation/param/parse/consumer/FormConsumerParamParser.java`
#### Snippet
```java
            Set<String> allFieldNames = ReflectUtils.getAllFieldNames(value.getClass());

            allFieldNames.stream().forEach(entry -> {

                    Object fieldValue = ReflectUtils.getFieldValue(value, entry);
```

### SimplifyStreamApiCallChains
The 'filter().findFirst().isPresent()' chain can be replaced with 'anyMatch()'
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/util/ServiceAnnotationUtils.java`
#### Snippet
```java
    static boolean isServiceAnnotationPresent(TypeElement annotatedType) {
        return SUPPORTED_ANNOTATION_TYPES.stream()
                .filter(type -> isAnnotationPresent(annotatedType, type))
                .findFirst()
                .isPresent();
    }

```

### SimplifyStreamApiCallChains
''stream().forEach()'' can be replaced with 'forEach()'' (may change semantics)
in `dubbo-metrics/dubbo-metrics-default/src/main/java/org/apache/dubbo/metrics/collector/sample/ThreadRejectMetricsCountSampler.java`
#### Snippet
```java
    public List<MetricSample> sample() {
        List<MetricSample> metricSamples = new ArrayList<>();
        metricNames.stream().forEach(name->collect(metricSamples,name));
        return metricSamples;
    }
```

## RuleId[id=TypeParameterHidesVisibleType]
### TypeParameterHidesVisibleType
Type parameter `T` hides type parameter 'T'
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/tag/TagStateRouter.java`
#### Snippet
```java
    }

    private <T> BitList<Invoker<T>> filterInvoker(BitList<Invoker<T>> invokers, Predicate<Invoker<T>> predicate) {
        if (invokers.stream().allMatch(predicate)) {
            return invokers;
```

### TypeParameterHidesVisibleType
Type parameter `T` hides type parameter 'T'
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/tag/TagStateRouter.java`
#### Snippet
```java
     * @return
     */
    private <T> BitList<Invoker<T>> filterUsingStaticTag(BitList<Invoker<T>> invokers, URL url, Invocation invocation) {
        BitList<Invoker<T>> result;
        // Dynamic param
```

## RuleId[id=UnnecessaryCallToStringValueOf]
### UnnecessaryCallToStringValueOf
Unnecessary `String.valueOf()` call
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/container/page/pages/SystemPageHandler.java`
#### Snippet
```java
        row = new ArrayList<String>();
        row.add("CPU");
        row.add(System.getProperty("os.arch", "") + ", " + String.valueOf(Runtime.getRuntime().availableProcessors()) + " cores");
        rows.add(row);

```

### UnnecessaryCallToStringValueOf
Unnecessary `String.valueOf()` call
in `dubbo-compiler/src/main/java/org/apache/dubbo/gen/AbstractGenerator.java`
#### Snippet
```java
        public String methodNameCamelCase() {
            String mn = methodName.replace("_", "");
            return String.valueOf(Character.toLowerCase(mn.charAt(0))) + mn.substring(1);
        }
    }
```

### UnnecessaryCallToStringValueOf
Unnecessary `String.valueOf()` call
in `dubbo-compiler/src/main/java/org/apache/dubbo/gen/AbstractGenerator.java`
#### Snippet
```java
        public String methodNamePascalCase() {
            String mn = methodName.replace("_", "");
            return String.valueOf(Character.toUpperCase(mn.charAt(0))) + mn.substring(1);
        }

```

## RuleId[id=DuplicatedCode]
### DuplicatedCode
Duplicated code
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/filter/FilterChainBuilder.java`
#### Snippet
```java
            } catch (Exception e) {
                InvocationProfilerUtils.releaseDetailProfiler(invocation);
                if (filter instanceof ListenableFilter) {
                    ListenableFilter listenableFilter = ((ListenableFilter) filter);
                    try {
                        Filter.Listener listener = listenableFilter.listener(invocation);
                        if (listener != null) {
                            listener.onError(e, originalInvoker, invocation);
                        }
                    } finally {
                        listenableFilter.removeListener(invocation);
                    }
                } else if (filter instanceof FILTER.Listener) {
                    FILTER.Listener listener = (FILTER.Listener) filter;
                    listener.onError(e, originalInvoker, invocation);
                }
                throw e;
            } finally {
```

### DuplicatedCode
Duplicated code
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/filter/FilterChainBuilder.java`
#### Snippet
```java
                InvocationProfilerUtils.releaseDetailProfiler(invocation);
                if (filter instanceof ListenableFilter) {
                    ListenableFilter listenableFilter = ((ListenableFilter) filter);
                    Filter.Listener listener = listenableFilter.listener(invocation);
                    try {
                        if (listener != null) {
                            if (t == null) {
                                listener.onResponse(r, originalInvoker, invocation);
                            } else {
                                listener.onError(t, originalInvoker, invocation);
                            }
                        }
                    } finally {
                        listenableFilter.removeListener(invocation);
                    }
                } else if (filter instanceof FILTER.Listener) {
                    FILTER.Listener listener = (FILTER.Listener) filter;
                    if (t == null) {
                        listener.onResponse(r, originalInvoker, invocation);
                    } else {
                        listener.onError(t, originalInvoker, invocation);
                    }
                }
```

### DuplicatedCode
Duplicated code
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/loadbalance/LeastActiveLoadBalance.java`
#### Snippet
```java
        if (leastCount == 1) {
            // If we got exactly one invoker having the least active value, return this invoker directly.
            return invokers.get(leastIndexes[0]);
        }
        if (!sameWeight && totalWeight > 0) {
            // If (not every invoker has the same weight & at least one invoker's weight>0), select randomly based on 
            // totalWeight.
            int offsetWeight = ThreadLocalRandom.current().nextInt(totalWeight);
            // Return a invoker based on the random value.
            for (int i = 0; i < leastCount; i++) {
                int leastIndex = leastIndexes[i];
                offsetWeight -= weights[leastIndex];
                if (offsetWeight < 0) {
                    return invokers.get(leastIndex);
                }
            }
        }
        // If all invokers have the same weight value or totalWeight=0, return evenly.
        return invokers.get(leastIndexes[ThreadLocalRandom.current().nextInt(leastCount)]);
```

### DuplicatedCode
Duplicated code
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/condition/config/ProviderAppStateRouter.java`
#### Snippet
```java
        synchronized (this) {
            if (!providerApplication.equals(application)) {
                if (StringUtils.isNotEmpty(application)) {
                    this.getRuleRepository().removeListener(application + RULE_SUFFIX, this);
                }
                String key = providerApplication + RULE_SUFFIX;
                this.getRuleRepository().addListener(key, this);
                application = providerApplication;
                String rawRule = this.getRuleRepository().getRule(key, DynamicConfiguration.DEFAULT_GROUP);
                if (StringUtils.isNotEmpty(rawRule)) {
                    this.process(new ConfigChangedEvent(key, DynamicConfiguration.DEFAULT_GROUP, rawRule));
                }
            }
        }
```

### DuplicatedCode
Duplicated code
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/tag/TagStateRouter.java`
#### Snippet
```java
                if (StringUtils.isNotEmpty(application)) {
                    this.getRuleRepository().removeListener(application + RULE_SUFFIX, this);
                }
                String key = providerApplication + RULE_SUFFIX;
                this.getRuleRepository().addListener(key, this);
                application = providerApplication;
                String rawRule = this.getRuleRepository().getRule(key, DynamicConfiguration.DEFAULT_GROUP);
                if (StringUtils.isNotEmpty(rawRule)) {
                    this.process(new ConfigChangedEvent(key, DynamicConfiguration.DEFAULT_GROUP, rawRule));
                }
```

### DuplicatedCode
Duplicated code
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/support/merger/DefaultProviderURLMergeProcessor.java`
#### Snippet
```java
                String remoteFilter = remoteMap.get(REFERENCE_FILTER_KEY);
                String localFilter = copyOfLocalMap.get(REFERENCE_FILTER_KEY);
                if (remoteFilter != null && remoteFilter.length() > 0
                        && localFilter != null && localFilter.length() > 0) {
                    map.put(REFERENCE_FILTER_KEY, remoteFilter + "," + localFilter);
                }
```

### DuplicatedCode
Duplicated code
in `dubbo-common/src/main/java/org/apache/dubbo/common/URL.java`
#### Snippet
```java
        if (pairs == null || pairs.length == 0) {
            return this;
        }
        if (pairs.length % 2 != 0) {
            throw new IllegalArgumentException("Map pairs can not be odd number.");
        }
        Map<String, String> map = new HashMap<>();
        int len = pairs.length / 2;
        for (int i = 0; i < len; i++) {
            map.put(pairs[2 * i], pairs[2 * i + 1]);
        }
        return addParameters(map);
```

### DuplicatedCode
Duplicated code
in `dubbo-common/src/main/java/org/apache/dubbo/common/URLBuilder.java`
#### Snippet
```java
        String protocol = url.getProtocol();
        String username = url.getUsername();
        String password = url.getPassword();
        String host = url.getHost();
        int port = url.getPort();
        String path = url.getPath();
        Map<String, String> parameters = new HashMap<>(url.getParameters());
```

### DuplicatedCode
Duplicated code
in `dubbo-common/src/main/java/org/apache/dubbo/common/URLStrParser.java`
#### Snippet
```java
        int len = str.length();
        if (from >= len) {
            return Collections.emptyMap();
        }

        TempBuf tempBuf = DECODE_TEMP_BUF.get();
        Map<String, String> params = new HashMap<>();
        int nameStart = from;
        int valueStart = -1;
        int i;
```

### DuplicatedCode
Duplicated code
in `dubbo-common/src/main/java/org/apache/dubbo/common/bytecode/ClassGenerator.java`
#### Snippet
```java
        sb.append(')');
        if (ArrayUtils.isNotEmpty(ets)) {
            sb.append(" throws ");
            for (int i = 0; i < ets.length; i++) {
                if (i > 0) {
                    sb.append(',');
                }
                sb.append(ReflectUtils.getName(ets[i]));
            }
        }
        sb.append('{').append(body).append('}');
```

### DuplicatedCode
Duplicated code
in `dubbo-common/src/main/java/org/apache/dubbo/common/bytecode/Wrapper.java`
#### Snippet
```java
            if ("getClass".equals(mn)) {
                return instance.getClass();
            }
            if ("hashCode".equals(mn)) {
                return instance.hashCode();
            }
            if ("toString".equals(mn)) {
                return instance.toString();
            }
            if ("equals".equals(mn)) {
                if (args.length == 1) {
                    return instance.equals(args[0]);
                }
                throw new IllegalArgumentException("Invoke method [" + mn + "] argument number error.");
            }
```

### DuplicatedCode
Duplicated code
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/Environment.java`
#### Snippet
```java
        if (defaultDynamicGlobalConfiguration == null) {
            if (defaultDynamicConfiguration == null) {
                if (logger.isWarnEnabled()) {
                    logger.warn(COMMON_UNEXPECTED_EXCEPTION, "", "", "dynamicConfiguration is null , return globalConfiguration.");
                }
                return getConfiguration();
            }
            defaultDynamicGlobalConfiguration = new CompositeConfiguration();
            defaultDynamicGlobalConfiguration.addConfiguration(defaultDynamicConfiguration);
            defaultDynamicGlobalConfiguration.addConfiguration(getConfiguration());
        }
        return defaultDynamicGlobalConfiguration;
```

### DuplicatedCode
Duplicated code
in `dubbo-common/src/main/java/org/apache/dubbo/common/extension/ExtensionLoader.java`
#### Snippet
```java
            List<String> newContentList = new ArrayList<>();

            try (BufferedReader reader = new BufferedReader(
                new InputStreamReader(resourceURL.openStream(), StandardCharsets.UTF_8))) {
                String line;
                while ((line = reader.readLine()) != null) {
                    final int ci = line.indexOf('#');
                    if (ci >= 0) {
                        line = line.substring(0, ci);
                    }
                    line = line.trim();
                    if (line.length() > 0) {
                        newContentList.add(line);
                    }
                }
            } catch (IOException e) {
                throw new RuntimeException(e.getMessage(), e);
            }
            return newContentList;
```

### DuplicatedCode
Duplicated code
in `dubbo-common/src/main/java/org/apache/dubbo/common/extension/support/ActivateComparator.java`
#### Snippet
```java
        if (o1 == null && o2 == null) {
            return 0;
        }
        if (o1 == null) {
            return -1;
        }
        if (o2 == null) {
            return 1;
        }
        if (o1.equals(o2)) {
            return 0;
        }
```

### DuplicatedCode
Duplicated code
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/Bytes.java`
#### Snippet
```java
        b[off + 7] = (byte) v;
        b[off + 6] = (byte) (v >>> 8);
        b[off + 5] = (byte) (v >>> 16);
        b[off + 4] = (byte) (v >>> 24);
        b[off + 3] = (byte) (v >>> 32);
        b[off + 2] = (byte) (v >>> 40);
        b[off + 1] = (byte) (v >>> 48);
        b[off + 0] = (byte) (v >>> 56);
```

### DuplicatedCode
Duplicated code
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/Bytes.java`
#### Snippet
```java
        return ((b[off + 7] & 0xFFL) << 0) +
                ((b[off + 6] & 0xFFL) << 8) +
                ((b[off + 5] & 0xFFL) << 16) +
                ((b[off + 4] & 0xFFL) << 24) +
                ((b[off + 3] & 0xFFL) << 32) +
                ((b[off + 2] & 0xFFL) << 40) +
                ((b[off + 1] & 0xFFL) << 48) +
                (((long) b[off + 0]) << 56);
```

### DuplicatedCode
Duplicated code
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/Bytes.java`
#### Snippet
```java
        if (off < 0) {
            throw new IndexOutOfBoundsException("base642bytes: offset < 0, offset is " + off);
        }
        if (len < 0) {
            throw new IndexOutOfBoundsException("base642bytes: length < 0, length is " + len);
        }
        if (len == 0) {
            return new byte[0];
        }
        if (off + len > str.length()) {
            throw new IndexOutOfBoundsException("base642bytes: offset + length > string length.");
        }
```

### DuplicatedCode
Duplicated code
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/Bytes.java`
#### Snippet
```java
            if (str.charAt(off + len - 2) == pc) {
                size -= 2;
                --num;
                rem = 2;
            } else if (str.charAt(off + len - 1) == pc) {
                size--;
                --num;
                rem = 3;
            }
```

### DuplicatedCode
Duplicated code
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/StreamUtils.java`
#### Snippet
```java
                if (b == null) {
                    throw new NullPointerException();
                }

                if (off < 0 || len < 0 || len > b.length - off) {
                    throw new IndexOutOfBoundsException();
                }

                if (mPosition >= mLimit) {
                    return -1;
                }

                if (mPosition + len > mLimit) {
                    len = mLimit - mPosition;
                }

                if (len <= 0) {
                    return 0;
                }
```

### DuplicatedCode
Duplicated code
in `dubbo-common/src/main/java/org/apache/dubbo/common/logger/LoggerFactory.java`
#### Snippet
```java
        Map<Class<? extends LoggerAdapter>, String> candidates = new HashMap<>();
        candidates.put(Log4jLoggerAdapter.class, "log4j");
        candidates.put(Slf4jLoggerAdapter.class, "slf4j");
        candidates.put(Log4j2LoggerAdapter.class, "log4j2");
        candidates.put(JclLoggerAdapter.class, "jcl");
        candidates.put(JdkLoggerAdapter.class, "jdk");
```

### DuplicatedCode
Duplicated code
in `dubbo-common/src/main/java/org/apache/dubbo/common/reference/ReferenceCountedResource.java`
#### Snippet
```java
        long remainingCount = COUNTER_UPDATER.decrementAndGet(this);

        if (remainingCount == 0) {
            destroy();
            return true;
        } else if (remainingCount <= -1) {
            logger.warn(PROTOCOL_ERROR_CLOSE_CLIENT, "", "", "This instance has been destroyed");
            return false;
        } else {
            return false;
        }
```

### DuplicatedCode
Duplicated code
in `dubbo-common/src/main/java/org/apache/dubbo/common/threadpool/manager/DefaultExecutorRepository.java`
#### Snippet
```java
        ExecutorService executor = executors.get(executorCacheKey);
        if (executor != null && (executor.isShutdown() || executor.isTerminated())) {
            executors.remove(executorCacheKey);
            // Does not re-create a shutdown executor, use SHARED_EXECUTOR for downgrade.
            executor = null;
            logger.info("Executor for " + url + " is shutdown.");
        }
        if (executor == null) {
            return frameworkExecutorRepository.getSharedExecutor();
        } else {
            return executor;
        }
```

### DuplicatedCode
Duplicated code
in `dubbo-common/src/main/java/org/apache/dubbo/common/threadpool/support/cached/CachedThreadPool.java`
#### Snippet
```java
        String name = url.getParameter(THREAD_NAME_KEY, (String) url.getAttribute(THREAD_NAME_KEY, DEFAULT_THREAD_NAME));
        int cores = url.getParameter(CORE_THREADS_KEY, DEFAULT_CORE_THREADS);
        int threads = url.getParameter(THREADS_KEY, Integer.MAX_VALUE);
        int queues = url.getParameter(QUEUES_KEY, DEFAULT_QUEUES);
        int alive = url.getParameter(ALIVE_KEY, DEFAULT_ALIVE);
```

### DuplicatedCode
Duplicated code
in `dubbo-common/src/main/java/org/apache/dubbo/common/url/component/ServiceAddressURL.java`
#### Snippet
```java
        if (GROUP_KEY.equals(key)) {
            return getGroup();
        } else if (VERSION_KEY.equals(key)) {
            return getVersion();
        } else if (APPLICATION_KEY.equals(key)) {
            return getRemoteApplication();
        } else if (SIDE_KEY.equals(key)) {
            return getSide();
        } else if (CATEGORY_KEY.equals(key)) {
            return getCategory();
        }
```

### DuplicatedCode
Duplicated code
in `dubbo-common/src/main/java/org/apache/dubbo/common/url/component/URLAddress.java`
#### Snippet
```java
        if (i >= 0) {
            if (i == 0) {
                throw new IllegalStateException("url missing protocol: \"" + decodeStr + "\"");
            }
            protocol = decodeStr.substring(0, i);
            decodeStr = decodeStr.substring(i + 3);
        } else {
            // case: file:/path/to/file.txt
            i = decodeStr.indexOf(":/");
            if (i >= 0) {
                if (i == 0) {
                    throw new IllegalStateException("url missing protocol: \"" + decodeStr + "\"");
                }
                protocol = decodeStr.substring(0, i);
                decodeStr = decodeStr.substring(i + 1);
            }
        }

        i = decodeStr.indexOf('/');
        if (i >= 0) {
            path = decodeStr.substring(i + 1);
            decodeStr = decodeStr.substring(0, i);
        }
        i = decodeStr.lastIndexOf('@');
        if (i >= 0) {
            username = decodeStr.substring(0, i);
            int j = username.indexOf(':');
            if (j >= 0) {
                password = username.substring(j + 1);
                username = username.substring(0, j);
            }
            decodeStr = decodeStr.substring(i + 1);
        }
        i = decodeStr.lastIndexOf(':');
        if (i >= 0 && i < decodeStr.length() - 1) {
            if (decodeStr.lastIndexOf('%') > i) {
                // ipv6 address with scope id
                // e.g. fe80:0:0:0:894:aeec:f37d:23e1%en0
                // see https://howdoesinternetwork.com/2013/ipv6-zone-id
                // ignore
            } else {
                port = Integer.parseInt(decodeStr.substring(i + 1));
                host = decodeStr.substring(0, i);
            }
        }
```

### DuplicatedCode
Duplicated code
in `dubbo-common/src/main/java/org/apache/dubbo/common/url/component/URLParam.java`
#### Snippet
```java
        BitSet keyBit = new BitSet(capacity);
        Map<Integer, Integer> valueMap = new HashMap<>(capacity);
        Map<String, String> extraParam = new HashMap<>(capacity);
        Map<String, Map<String, String>> methodParameters = new HashMap<>(capacity);
```

### DuplicatedCode
Duplicated code
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ClassUtils.java`
#### Snippet
```java
        if (tClass == Object.class) {
            return OBJECT_METHODS;
        }
        Method[] methods = Arrays.stream(tClass.getMethods())
            .collect(Collectors.toList())
            .toArray(new Method[]{});
        List<String> mns = new ArrayList<>(); // method names.
        boolean hasMethod = hasMethods(methods);
```

### DuplicatedCode
Duplicated code
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/DefaultSerializeClassChecker.java`
#### Snippet
```java
            if (ch == '$') {
                ch = '.';
            }
            hash ^= ch;
            hash *= MAGIC_PRIME;

            if (Arrays.binarySearch(disAllowPrefixes, hash) >= 0) {
                String msg = "[Serialization Security] Serialized class " + className + " is in disallow list. " +
                    "Current mode is `WARN`, will disallow to deserialize it by default. " +
                    "Please add it into security/serialize.allowlist or follow FAQ to configure it.";
                if (serializeSecurityManager.getWarnedClasses().add(className)) {
                    logger.error(PROTOCOL_UNTRUSTED_SERIALIZE_CLASS, "", "", msg);
                }

                throw new IllegalArgumentException(msg);
            }
```

### DuplicatedCode
Duplicated code
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
        Class<?>[] parameterTypes = m.getParameterTypes();
        for (int i = 0; i < parameterTypes.length; i++) {
            if (i > 0) {
                ret.append(',');
            }
            ret.append(getName(parameterTypes[i]));
        }
        ret.append(')');
        return ret.toString();
```

### DuplicatedCode
Duplicated code
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
        for (int i = 0; i < parameterTypes.length; i++) {
            if (i > 0) {
                ret.append(',');
            }
            ret.append(getName(parameterTypes[i]));
        }
        ret.append(')');
        return ret.toString();
```

### DuplicatedCode
Duplicated code
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
        if (c.isPrimitive()) {
            String t = c.getName();
            if ("void".equals(t)) {
                ret.append(JVM_VOID);
            } else if ("boolean".equals(t)) {
                ret.append(JVM_BOOLEAN);
            } else if ("byte".equals(t)) {
                ret.append(JVM_BYTE);
            } else if ("char".equals(t)) {
                ret.append(JVM_CHAR);
            } else if ("double".equals(t)) {
                ret.append(JVM_DOUBLE);
            } else if ("float".equals(t)) {
                ret.append(JVM_FLOAT);
            } else if ("int".equals(t)) {
                ret.append(JVM_INT);
            } else if ("long".equals(t)) {
                ret.append(JVM_LONG);
            } else if ("short".equals(t)) {
                ret.append(JVM_SHORT);
            }
        } else {
            ret.append('L');
            ret.append(c.getName().replace('.', '/'));
            ret.append(';');
        }
```

### DuplicatedCode
Duplicated code
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
        Class<?>[] parameterTypes = m.getParameterTypes();
        for (int i = 0; i < parameterTypes.length; i++) {
            ret.append(getDesc(parameterTypes[i]));
        }
        ret.append(')').append(getDesc(m.getReturnType()));
        return ret.toString();
```

### DuplicatedCode
Duplicated code
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/StringUtils.java`
#### Snippet
```java
        if (ArrayUtils.isEmpty(array)) {
            return EMPTY_STRING;
        }
        StringBuilder sb = new StringBuilder();
        for (int i = 0; i < array.length; i++) {
            if (i > 0) {
                sb.append(split);
            }
            sb.append(array[i]);
        }
        return sb.toString();
```

### DuplicatedCode
Duplicated code
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/UrlUtils.java`
#### Snippet
```java
                for (Map.Entry<String, String> entry2 : serviceUrls.entrySet()) {
                    String serviceUrl = entry2.getKey();
                    String serviceQuery = entry2.getValue();
                    Map<String, String> params = StringUtils.parseQueryString(serviceQuery);
                    String group = params.get(GROUP_KEY);
                    String version = params.get(VERSION_KEY);
                    //params.remove("group");
                    //params.remove("version");
                    String name = serviceName;
                    if (StringUtils.isNotEmpty(group)) {
                        name = group + "/" + name;
                    }
                    if (StringUtils.isNotEmpty(version)) {
                        name = name + ":" + version;
                    }
                    Map<String, String> newUrls = newRegister.computeIfAbsent(name, k -> new HashMap<>());
                    newUrls.put(serviceUrl, StringUtils.toQueryString(params));
                }
```

### DuplicatedCode
Duplicated code
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/UrlUtils.java`
#### Snippet
```java
                Map<String, String> params = StringUtils.parseQueryString(serviceQuery);
                String group = params.get(GROUP_KEY);
                String version = params.get(VERSION_KEY);
                //params.remove("group");
                //params.remove("version");
                String name = serviceName;
                if (StringUtils.isNotEmpty(group)) {
                    name = group + "/" + name;
                }
                if (StringUtils.isNotEmpty(version)) {
                    name = name + ":" + version;
                }
```

### DuplicatedCode
Duplicated code
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/UrlUtils.java`
#### Snippet
```java
                Map<String, String> params = StringUtils.parseQueryString(serviceQuery);
                String name = serviceName;
                int i = name.indexOf('/');
                if (i >= 0) {
                    params.put(GROUP_KEY, name.substring(0, i));
                    name = name.substring(i + 1);
                }
                i = name.lastIndexOf(':');
                if (i >= 0) {
                    params.put(VERSION_KEY, name.substring(i + 1));
                    name = name.substring(0, i);
                }
```

### DuplicatedCode
Duplicated code
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/UrlUtils.java`
#### Snippet
```java
                    String name = serviceName;
                    int i = name.indexOf('/');
                    if (i >= 0) {
                        params.put(GROUP_KEY, name.substring(0, i));
                        name = name.substring(i + 1);
                    }
                    i = name.lastIndexOf(':');
                    if (i >= 0) {
                        params.put(VERSION_KEY, name.substring(i + 1));
                        name = name.substring(0, i);
                    }
```

### DuplicatedCode
Duplicated code
in `dubbo-common/src/main/java/org/apache/dubbo/config/ConfigCenterConfig.java`
#### Snippet
```java
        this.address = address;
        if (address != null) {
            try {
                URL url = URL.valueOf(address);
                updatePropertyIfAbsent(this::getUsername, this::setUsername, url.getUsername());
                updatePropertyIfAbsent(this::getPassword, this::setPassword, url.getPassword());
                updatePropertyIfAbsent(this::getProtocol, this::setProtocol, url.getProtocol());
                updatePropertyIfAbsent(this::getPort, this::setPort, url.getPort());

                Map<String, String> params = url.getParameters();
                if (CollectionUtils.isNotEmptyMap(params)) {
                    params.remove(BACKUP_KEY);
                }
                updateParameters(params);
            } catch (Exception ignored) {
            }
        }
```

### DuplicatedCode
Duplicated code
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ApplicationModel.java`
#### Snippet
```java
            if (deployer != null) {
                deployer.postDestroy();
            }

            // 7. destroy other resources (e.g. ZookeeperTransporter )
            notifyDestroy();

            if (environment != null) {
                environment.destroy();
                environment = null;
            }
            if (configManager != null) {
                configManager.destroy();
                configManager = null;
            }
            if (serviceRepository != null) {
                serviceRepository.destroy();
                serviceRepository = null;
            }

            // 8. destroy framework if none application
            frameworkModel.tryDestroy();
```

### DuplicatedCode
Duplicated code
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ReflectionServiceDescriptor.java`
#### Snippet
```java
        List<MethodDescriptor> methodModels = methods.get(methodName);
        if (CollectionUtils.isNotEmpty(methodModels)) {
            for (MethodDescriptor descriptor : methodModels) {
                if (Arrays.equals(paramTypes, descriptor.getParameterClasses())) {
                    return descriptor;
                }
            }
        }
        return null;
```

### DuplicatedCode
Duplicated code
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/config/ServiceConfig.java`
#### Snippet
```java
        protocol.setServer(provider.getServer());
        protocol.setClient(provider.getClient());
        protocol.setCodec(provider.getCodec());
        protocol.setHost(provider.getHost());
        protocol.setPort(provider.getPort());
        protocol.setPath(provider.getPath());
        protocol.setPayload(provider.getPayload());
        protocol.setThreads(provider.getThreads());
        protocol.setParameters(provider.getParameters());
        return protocol;
```

### DuplicatedCode
Duplicated code
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/container/page/PageServlet.java`
#### Snippet
```java
                        writer.println("<table>");
                        writer.println("<thead>");
                        writer.println("    <tr>");
                        writer.println("        <th>Error</th>");
                        writer.println("    </tr>");
                        writer.println("</thead>");
                        writer.println("<tbody>");
                        writer.println("    <tr>");
                        writer.println("        <td>");
```

### DuplicatedCode
Duplicated code
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/container/page/pages/LogPageHandler.java`
#### Snippet
```java
                FileChannel channel = fis.getChannel();
                size = channel.size();
                ByteBuffer bb;
                if (size <= SHOW_LOG_LENGTH) {
                    bb = ByteBuffer.allocate((int) size);
                    channel.read(bb, 0);
                } else {
                    int pos = (int) (size - SHOW_LOG_LENGTH);
                    bb = ByteBuffer.allocate(SHOW_LOG_LENGTH);
                    channel.read(bb, pos);
                }
                bb.flip();
```

### DuplicatedCode
Duplicated code
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/ReferenceConfig.java`
#### Snippet
```java
            String revision = Version.getVersion(interfaceClass, version);
            if (StringUtils.isNotEmpty(revision)) {
                map.put(REVISION_KEY, revision);
            }

            String[] methods = methods(interfaceClass);
            if (methods.length == 0) {
                logger.warn(CONFIG_NO_METHOD_FOUND, "", "", "No method found in service interface: " + interfaceClass.getName());
                map.put(METHODS_KEY, ANY_VALUE);
            } else {
                List<String> copyOfMethods = new ArrayList<>(Arrays.asList(methods));
                copyOfMethods.sort(Comparator.naturalOrder());
                map.put(METHODS_KEY, String.join(COMMA_SEPARATOR, copyOfMethods));
            }
```

### DuplicatedCode
Duplicated code
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/ReferenceConfig.java`
#### Snippet
```java
                AbstractConfig.appendParameters(map, methodConfig, methodConfig.getName());
                String retryKey = methodConfig.getName() + ".retry";
                if (map.containsKey(retryKey)) {
                    String retryValue = map.remove(retryKey);
                    if ("false".equals(retryValue)) {
                        map.put(methodConfig.getName() + ".retries", "0");
                    }
                }
```

### DuplicatedCode
Duplicated code
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/deploy/DefaultApplicationDeployer.java`
#### Snippet
```java
                ModuleServiceRepository serviceRepository = moduleModel.getServiceRepository();
                List<ProviderModel> exportedServices = serviceRepository.getExportedServices();
                for (ProviderModel exportedService : exportedServices) {
                    List<ProviderModel.RegisterStatedURL> statedUrls = exportedService.getStatedUrl();
                    for (ProviderModel.RegisterStatedURL statedURL : statedUrls) {
                        if (statedURL.isRegistered()) {
                            doOffline(statedURL);
                        }
                    }
                }
```

### DuplicatedCode
Duplicated code
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/ReferenceAnnotationBeanPostProcessor.java`
#### Snippet
```java
        Map<InjectionMetadata.InjectedElement, ReferenceBean<?>> map = new HashMap<>();
        for (Map.Entry<InjectionMetadata.InjectedElement, String> entry : injectedFieldReferenceBeanCache.entrySet()) {
            map.put(entry.getKey(), referenceBeanManager.getById(entry.getValue()));
        }
        return Collections.unmodifiableMap(map);
```

### DuplicatedCode
Duplicated code
in `dubbo-configcenter/dubbo-configcenter-nacos/src/main/java/org/apache/dubbo/configcenter/support/nacos/NacosDynamicConfiguration.java`
#### Snippet
```java
int retryTimes = url.getPositiveParameter(NACOS_RETRY_KEY, 10);
        int sleepMsBetweenRetries = url.getPositiveParameter(NACOS_RETRY_WAIT_KEY, 1000);
        boolean check = url.getParameter(NACOS_CHECK_KEY, true);
        ConfigService tmpConfigServices = null;
        try {
            for (int i = 0; i < retryTimes + 1; i++) {
                tmpConfigServices = NacosFactory.createConfigService(nacosProperties);
                if (!check || (UP.equals(tmpConfigServices.getServerStatus()) && testConfigService(tmpConfigServices))) {
                    break;
                } else {
                    logger.warn(LoggerCodeConstants.CONFIG_ERROR_NACOS, "", "",
                        "Failed to connect to nacos config server. " +
                            (i < retryTimes ? "Dubbo will try to retry in " + sleepMsBetweenRetries + ". " : "Exceed retry max times.") +
                            "Try times: " + (i + 1));
                }
                tmpConfigServices.shutDown();
                tmpConfigServices = null;
                Thread.sleep(sleepMsBetweenRetries);
            }
        } catch (NacosException e) {
            logger.error(CONFIG_ERROR_NACOS, "", "", e.getErrMsg(), e);
            throw new IllegalStateException(e);
        } catch (InterruptedException e) {
            logger.error(INTERNAL_INTERRUPTED, "", "", "Interrupted when creating nacos config service client.", e);
            Thread.currentThread().interrupt();
            throw new IllegalStateException(e);
        }

        if (tmpConfigServices == null) {
            logger.error(CONFIG_ERROR_NACOS, "", "", "Failed to create nacos config service client. Reason: server status check failed.");
            throw new IllegalStateException("Failed to create nacos config service client. Reason: server status check failed.");
        }

        return new NacosConfigServiceWrapper(tmpConfigServices);
```

### DuplicatedCode
Duplicated code
in `dubbo-configcenter/dubbo-configcenter-nacos/src/main/java/org/apache/dubbo/configcenter/support/nacos/NacosDynamicConfiguration.java`
#### Snippet
```java
        StringBuilder serverAddrBuilder =
            new StringBuilder(url.getHost()) // Host
                .append(':')
                .append(url.getPort()); // Port

        // Append backup parameter as other servers
        String backup = url.getParameter(BACKUP_KEY);
        if (backup != null) {
            serverAddrBuilder.append(',').append(backup);
        }
        String serverAddr = serverAddrBuilder.toString();
        properties.put(SERVER_ADDR, serverAddr);
```

### DuplicatedCode
Duplicated code
in `dubbo-configcenter/dubbo-configcenter-nacos/src/main/java/org/apache/dubbo/configcenter/support/nacos/NacosDynamicConfiguration.java`
#### Snippet
```java
        Map<String, String> parameters = url.getParameters(of(PropertyKeyConst.class));
        // Put all parameters
        properties.putAll(parameters);
        if (StringUtils.isNotEmpty(url.getUsername())) {
            properties.put(USERNAME, url.getUsername());
        }
        if (StringUtils.isNotEmpty(url.getPassword())) {
            properties.put(PASSWORD, url.getPassword());
        }
```

### DuplicatedCode
Duplicated code
in `dubbo-configcenter/dubbo-configcenter-nacos/src/main/java/org/apache/dubbo/configcenter/support/nacos/NacosDynamicConfiguration.java`
#### Snippet
```java
            String oldValue = cacheData.get(dataId);
            ConfigChangedEvent event = new ConfigChangedEvent(dataId, group, configInfo, getChangeType(configInfo, oldValue));
            if (configInfo == null) {
                cacheData.remove(dataId);
            } else {
                cacheData.put(dataId, configInfo);
            }
            listeners.forEach(listener -> listener.process(event));
```

### DuplicatedCode
Duplicated code
in `dubbo-demo/dubbo-demo-xml/dubbo-demo-jaxrs-rest-consumer/src/main/java/org/apache/dubbo/demo/rest/api/RestConsumer.java`
#### Snippet
```java
        String hello = jaxRsRestDemoService.sayHello("hello");
        assertEquals("Hello, hello", hello);
        Integer result = jaxRsRestDemoService.primitiveInt(1, 2);
        Long resultLong = jaxRsRestDemoService.primitiveLong(1, 2l);
        long resultByte = jaxRsRestDemoService.primitiveByte((byte) 1, 2l);
        long resultShort = jaxRsRestDemoService.primitiveShort((short) 1, 2l, 1);

        assertEquals(result, 3);
        assertEquals(resultShort, 3l);
        assertEquals(resultLong, 3l);
        assertEquals(resultByte, 3l);

        assertEquals(Long.valueOf(1l), jaxRsRestDemoService.testFormBody(1l));
```

### DuplicatedCode
Duplicated code
in `dubbo-filter/dubbo-filter-validation/src/main/java/org/apache/dubbo/validation/support/jvalidation/JValidator.java`
#### Snippet
```java
        if (!hasConstraintParameter(method)) {
            return null;
        }
        try {
            String parameterClassName = generateMethodParameterClassName(clazz, method);
            Class<?> parameterClass;
            try {
                parameterClass = Class.forName(parameterClassName, true, clazz.getClassLoader());
            } catch (ClassNotFoundException e) {
                parameterClass = generateMethodParameterClass(clazz, method, parameterClassName);
            }
            Object parameterBean = parameterClass.getDeclaredConstructor().newInstance();
            for (int i = 0; i < args.length; i++) {
                Field field = parameterClass.getField(method.getName() + "Argument" + i);
                field.set(parameterBean, args[i]);
            }
            return parameterBean;
        } catch (Throwable e) {
            logger.warn(CONFIG_FILTER_VALIDATION_EXCEPTION, "", "", e.getMessage(), e);
            return null;
        }
```

### DuplicatedCode
Duplicated code
in `dubbo-filter/dubbo-filter-validation/src/main/java/org/apache/dubbo/validation/support/jvalidation/JValidator.java`
#### Snippet
```java
ClassPool pool = ClassGenerator.getClassPool(clazz.getClassLoader());
        synchronized (parameterClassName.intern()) {
            CtClass ctClass = null;
            try {
                ctClass = pool.getCtClass(parameterClassName);
            } catch (NotFoundException ignore) {
            }

            if (null == ctClass) {
                ctClass = pool.makeClass(parameterClassName);
                ClassFile classFile = ctClass.getClassFile();
                classFile.setVersionToJava5();
                ctClass.addConstructor(CtNewConstructor.defaultConstructor(pool.getCtClass(parameterClassName)));
                // parameter fields
                Class<?>[] parameterTypes = method.getParameterTypes();
                Annotation[][] parameterAnnotations = method.getParameterAnnotations();
                for (int i = 0; i < parameterTypes.length; i++) {
                    Class<?> type = parameterTypes[i];
                    Annotation[] annotations = parameterAnnotations[i];
                    AnnotationsAttribute attribute = new AnnotationsAttribute(classFile.getConstPool(), AnnotationsAttribute.visibleTag);
                    for (Annotation annotation : annotations) {
                        if (annotation.annotationType().isAnnotationPresent(Constraint.class)) {
                            javassist.bytecode.annotation.Annotation ja = new javassist.bytecode.annotation.Annotation(
                                classFile.getConstPool(), pool.getCtClass(annotation.annotationType().getName()));
                            Method[] members = annotation.annotationType().getMethods();
                            for (Method member : members) {
                                if (Modifier.isPublic(member.getModifiers())
                                    && member.getParameterTypes().length == 0
                                    && member.getDeclaringClass() == annotation.annotationType()) {
                                    Object value = member.invoke(annotation);
                                    if (null != value) {
                                        MemberValue memberValue = createMemberValue(
                                            classFile.getConstPool(), pool.get(member.getReturnType().getName()), value);
                                        ja.addMemberValue(member.getName(), memberValue);
                                    }
                                }
                            }
                            attribute.addAnnotation(ja);
                        }
                    }
                    String fieldName = method.getName() + "Argument" + i;
                    CtField ctField = CtField.make("public " + type.getCanonicalName() + " " + fieldName + ";", pool.getCtClass(parameterClassName));
                    ctField.getFieldInfo().addAttribute(attribute);
                    ctClass.addField(ctField);
                }
                return ctClass.toClass(clazz.getClassLoader(), null);
            } else {
                return Class.forName(parameterClassName, true, clazz.getClassLoader());
            }
        }
```

### DuplicatedCode
Duplicated code
in `dubbo-filter/dubbo-filter-validation/src/main/java/org/apache/dubbo/validation/support/jvalidation/JValidator.java`
#### Snippet
```java
        StringBuilder builder = new StringBuilder().append(clazz.getName())
            .append('_')
            .append(toUpperMethoName(method.getName()))
            .append("Parameter");

        Class<?>[] parameterTypes = method.getParameterTypes();
        for (Class<?> parameterType : parameterTypes) {
            builder.append('_').append(parameterType.getName());
        }

        return builder.toString();
```

### DuplicatedCode
Duplicated code
in `dubbo-filter/dubbo-filter-validation/src/main/java/org/apache/dubbo/validation/support/jvalidation/JValidator.java`
#### Snippet
```java
MemberValue memberValue = javassist.bytecode.annotation.Annotation.createMemberValue(cp, type);
        if (memberValue instanceof BooleanMemberValue) {
            ((BooleanMemberValue) memberValue).setValue((Boolean) value);
        } else if (memberValue instanceof ByteMemberValue) {
            ((ByteMemberValue) memberValue).setValue((Byte) value);
        } else if (memberValue instanceof CharMemberValue) {
            ((CharMemberValue) memberValue).setValue((Character) value);
        } else if (memberValue instanceof ShortMemberValue) {
            ((ShortMemberValue) memberValue).setValue((Short) value);
        } else if (memberValue instanceof IntegerMemberValue) {
            ((IntegerMemberValue) memberValue).setValue((Integer) value);
        } else if (memberValue instanceof LongMemberValue) {
            ((LongMemberValue) memberValue).setValue((Long) value);
        } else if (memberValue instanceof FloatMemberValue) {
            ((FloatMemberValue) memberValue).setValue((Float) value);
        } else if (memberValue instanceof DoubleMemberValue) {
            ((DoubleMemberValue) memberValue).setValue((Double) value);
        } else if (memberValue instanceof ClassMemberValue) {
            ((ClassMemberValue) memberValue).setValue(((Class<?>) value).getName());
        } else if (memberValue instanceof StringMemberValue) {
            ((StringMemberValue) memberValue).setValue((String) value);
        } else if (memberValue instanceof EnumMemberValue) {
            ((EnumMemberValue) memberValue).setValue(((Enum<?>) value).name());
        }
        /* else if (memberValue instanceof AnnotationMemberValue) */
        else if (memberValue instanceof ArrayMemberValue) {
            CtClass arrayType = type.getComponentType();
            int len = Array.getLength(value);
            MemberValue[] members = new MemberValue[len];
            for (int i = 0; i < len; i++) {
                members[i] = createMemberValue(cp, arrayType, Array.get(value, i));
            }
            ((ArrayMemberValue) memberValue).setValue(members);
        }
        return memberValue;
```

### DuplicatedCode
Duplicated code
in `dubbo-filter/dubbo-filter-validation/src/main/java/org/apache/dubbo/validation/support/jvalidation/JValidator.java`
#### Snippet
```java
        List<Class<?>> groups = new ArrayList<>();
        Class<?> methodClass = methodClass(methodName);
        if (methodClass != null) {
            groups.add(methodClass);
        }
        Set<ConstraintViolation<?>> violations = new HashSet<>();
        Method method = clazz.getMethod(methodName, parameterTypes);
        Class<?>[] methodClasses;
        if (method.isAnnotationPresent(MethodValidated.class)) {
            methodClasses = method.getAnnotation(MethodValidated.class).value();
            groups.addAll(Arrays.asList(methodClasses));
        }
        // add into default group
        groups.add(0, Default.class);
        groups.add(1, clazz);

        // convert list to array
        Class<?>[] classgroups = groups.toArray(new Class[groups.size()]);

        Object parameterBean = getMethodParameterBean(clazz, method, arguments);
        if (parameterBean != null) {
            violations.addAll(validator.validate(parameterBean, classgroups));
        }

        for (Object arg : arguments) {
            validate(violations, arg, classgroups);
        }

        if (!violations.isEmpty()) {
            logger.info("Failed to validate service: " + clazz.getName() + ", method: " + methodName + ", cause: " + violations);
            throw new ConstraintViolationException("Failed to validate service: " + clazz.getName() + ", method: " + methodName + ", cause: " + violations, violations);
        }
```

### DuplicatedCode
Duplicated code
in `dubbo-filter/dubbo-filter-validation/src/main/java/org/apache/dubbo/validation/support/jvalidation/JValidator.java`
#### Snippet
```java
        Class<?> methodClass = null;
        String methodClassName = clazz.getName() + "$" + toUpperMethoName(methodName);
        Class<?> cached = methodClassMap.get(methodClassName);
        if (cached != null) {
            return cached == clazz ? null : cached;
        }
        try {
            methodClass = Class.forName(methodClassName, false, Thread.currentThread().getContextClassLoader());
            methodClassMap.put(methodClassName, methodClass);
        } catch (ClassNotFoundException e) {
            methodClassMap.put(methodClassName, clazz);
        }
        return methodClass;
```

### DuplicatedCode
Duplicated code
in `dubbo-filter/dubbo-filter-validation/src/main/java/org/apache/dubbo/validation/support/jvalidation/JValidator.java`
#### Snippet
```java
        if (arg != null && !ReflectUtils.isPrimitives(arg.getClass())) {
            if (arg instanceof Object[]) {
                for (Object item : (Object[]) arg) {
                    validate(violations, item, groups);
                }
            } else if (arg instanceof Collection) {
                for (Object item : (Collection<?>) arg) {
                    validate(violations, item, groups);
                }
            } else if (arg instanceof Map) {
                for (Map.Entry<?, ?> entry : ((Map<?, ?>) arg).entrySet()) {
                    validate(violations, entry.getKey(), groups);
                    validate(violations, entry.getValue(), groups);
                }
            } else {
                violations.addAll(validator.validate(arg, groups));
            }
        }
```

### DuplicatedCode
Duplicated code
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/report/support/AbstractMetadataReport.java`
#### Snippet
```java
                        tmpProperties = new Properties();
                        Set<Map.Entry<Object, Object>> entries = properties.entrySet();
                        for (Map.Entry<Object, Object> entry : entries) {
                            tmpProperties.setProperty((String) entry.getKey(), (String) entry.getValue());
                        }
```

### DuplicatedCode
Duplicated code
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/rest/AbstractServiceRestMetadataResolver.java`
#### Snippet
```java
        Set<String> produces = new LinkedHashSet<>();
        processProduces(serviceMethod, serviceType, serviceInterfaceClass, produces);

        // process consumes
        Set<String> consumes = new LinkedHashSet<>();
        processConsumes(serviceMethod, serviceType, serviceInterfaceClass, consumes);

        // Initialize RequestMetadata
        RequestMetadata request = metadata.getRequest();
        request.setPath(requestPath);
        request.appendContextPathFromUrl(serviceRestMetadata.getContextPathFromUrl());
        request.setMethod(requestMethod);
        request.setProduces(produces);
        request.setConsumes(consumes);
```

### DuplicatedCode
Duplicated code
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/rest/jaxrs/DefaultValueParameterProcessor.java`
#### Snippet
```java
        OUTTER:
        for (Map.Entry<String, List<String>> entry : source.entrySet()) {
            List<String> values = entry.getValue();
            int size = values.size();
            for (int i = 0; i < size; i++) {
                String value = values.get(i);
                if (placeholderValue.equals(value)) {
                    values.set(i, defaultValue);
                    break OUTTER;
                }
            }
        }
```

### DuplicatedCode
Duplicated code
in `dubbo-metrics/dubbo-metrics-api/src/main/java/org/apache/dubbo/metrics/model/ThreadPoolMetric.java`
#### Snippet
```java
        Map<String, String> tags = new HashMap<>();
        tags.put(TAG_IP, getLocalHost());
        tags.put(TAG_PID, ConfigUtils.getPid()+"");
        tags.put(TAG_HOSTNAME, getLocalHostName());
        tags.put(TAG_APPLICATION_NAME, applicationName);
        tags.put(TAG_THREAD_NAME, threadPoolName);
        return tags;
```

### DuplicatedCode
Duplicated code
in `dubbo-plugin/dubbo-qos/src/main/java/org/apache/dubbo/qos/command/impl/GetAddress.java`
#### Snippet
```java
            .ifPresent(invokers -> {
                List<String> invokerUrls = new LinkedList<>();
                plainOutput.append("All Invokers: \n");
                for (org.apache.dubbo.rpc.Invoker<?> invoker : invokers) {
                    invokerUrls.add(invoker.getUrl().toFullString());
                    plainOutput.append(invoker.getUrl().toFullString()).append("\n");
                }
                plainOutput.append("\n");
                appMap.put("All", invokerUrls);
            });
```

### DuplicatedCode
Duplicated code
in `dubbo-plugin/dubbo-qos/src/main/java/org/apache/dubbo/qos/command/impl/GetAddress.java`
#### Snippet
```java
            .ifPresent(invokers -> {
                List<String> invokerUrls = new LinkedList<>();
                plainOutput.append("Valid Invokers: \n");
                for (org.apache.dubbo.rpc.Invoker<?> invoker : invokers) {
                    invokerUrls.add(invoker.getUrl().toFullString());
                    plainOutput.append(invoker.getUrl().toFullString()).append("\n");
                }
                plainOutput.append("\n");
                appMap.put("Valid", invokerUrls);
            });
```

### DuplicatedCode
Duplicated code
in `dubbo-plugin/dubbo-qos/src/main/java/org/apache/dubbo/qos/command/impl/GetAddress.java`
#### Snippet
```java
            .ifPresent(invokers -> {
                List<String> invokerUrls = new LinkedList<>();
                plainOutput.append("Disabled Invokers: \n");
                for (org.apache.dubbo.rpc.Invoker<?> invoker : invokers) {
                    invokerUrls.add(invoker.getUrl().toFullString());
                    plainOutput.append(invoker.getUrl().toFullString()).append("\n");
                }
                plainOutput.append("\n");
                appMap.put("Disabled", invokerUrls);
            });
```

### DuplicatedCode
Duplicated code
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/telnet/support/command/LogTelnetHandler.java`
#### Snippet
```java
                                size = filechannel.size();
                                ByteBuffer bb;
                                if (size <= showLogLength) {
                                    bb = ByteBuffer.allocate((int) size);
                                    filechannel.read(bb, 0);
                                } else {
                                    int pos = (int) (size - showLogLength);
                                    bb = ByteBuffer.allocate(showLogLength);
                                    filechannel.read(bb, pos);
                                }
                                bb.flip();
                                String content = new String(bb.array()).replace("<", "&lt;")
                                        .replace(">", "&gt;").replace("\n", "<br/><br/>");
```

### DuplicatedCode
Duplicated code
in `dubbo-plugin/dubbo-security/src/main/java/org/apache/dubbo/security/cert/DubboCertManager.java`
#### Snippet
```java
        PemObject pemObject = new PemObject(type, content);
        StringWriter str = new StringWriter();
        JcaPEMWriter jcaPEMWriter = new JcaPEMWriter(str);
        jcaPEMWriter.writeObject(pemObject);
        jcaPEMWriter.close();
        str.close();
        return str.toString();
```

### DuplicatedCode
Duplicated code
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/InstanceAddressURL.java`
#### Snippet
```java
        if (VERSION_KEY.equals(key)) {
            return getVersion();
        } else if (GROUP_KEY.equals(key)) {
            return getGroup();
        } else if (INTERFACE_KEY.equals(key)) {
            return getServiceInterface();
        } else if (REMOTE_APPLICATION_KEY.equals(key)) {
            return instance.getServiceName();
        } else if (SIDE_KEY.equals(key)) {
            return getSide();
        }
```

### DuplicatedCode
Duplicated code
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/InstanceAddressURL.java`
#### Snippet
```java
        Map<String, String> instanceParams = getInstance().getAllParams();
        Map<String, String> metadataParams = (metadataInfo == null ? new HashMap<>() : metadataInfo.getParameters(protocolServiceKey));
        int i = instanceParams == null ? 0 : instanceParams.size();
        int j = metadataParams == null ? 0 : metadataParams.size();
        Map<String, String> params = new HashMap<>((int) ((i + j) / 0.75) + 1);
        if (instanceParams != null) {
            params.putAll(instanceParams);
        }
        if (metadataParams != null) {
            params.putAll(metadataParams);
        }
```

### DuplicatedCode
Duplicated code
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/OverrideInstanceAddressURL.java`
#### Snippet
```java
        Map<String, String> overrideParameters = overrideParams.getParameters();
        Map<String, String> result = new HashMap<>((int) (parameters.size() + overrideParameters.size() / 0.75f) + 1);
        result.putAll(parameters);
        result.putAll(overrideParameters);
        return result;
```

### DuplicatedCode
Duplicated code
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/ServiceDiscoveryRegistryDirectory.java`
#### Snippet
```java
        ExtensionLoader<AddressListener> addressListenerExtensionLoader = getUrl().getOrDefaultModuleModel().getExtensionLoader(AddressListener.class);
        List<AddressListener> supportedListeners = addressListenerExtensionLoader.getActivateExtension(getUrl(), (String[]) null);
        if (supportedListeners != null && !supportedListeners.isEmpty()) {
            for (AddressListener addressListener : supportedListeners) {
                instanceUrls = addressListener.notify(instanceUrls, getConsumerUrl(), this);
            }
        }

        refreshOverrideAndInvoker(instanceUrls);
```

### DuplicatedCode
Duplicated code
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/ServiceDiscoveryRegistryDirectory.java`
#### Snippet
```java
        if (CollectionUtils.isNotEmpty(configurators)) {
            if (url instanceof DubboServiceAddressURL) {
                DubboServiceAddressURL interfaceAddressURL = (DubboServiceAddressURL) url;
                URL overriddenURL = interfaceAddressURL.getOverrideURL();
                if (overriddenURL == null) {
                    String appName = interfaceAddressURL.getApplication();
                    String side = interfaceAddressURL.getSide();
                    overriddenURL = URLBuilder.from(interfaceAddressURL)
                        .clearParameters()
                        .addParameter(APPLICATION_KEY, appName)
                        .addParameter(SIDE_KEY, side).build();
                }
                for (Configurator configurator : configurators) {
                    overriddenURL = configurator.configure(overriddenURL);
                }
                url = new DubboServiceAddressURL(
                    interfaceAddressURL.getUrlAddress(),
                    interfaceAddressURL.getUrlParam(),
                    interfaceAddressURL.getConsumerURL(),
                    (ServiceConfigURL) overriddenURL);
            } else {
                for (Configurator configurator : configurators) {
                    url = configurator.configure(url);
                }
            }
        }
        return url;
```

### DuplicatedCode
Duplicated code
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/ServiceDiscoveryRegistryDirectory.java`
#### Snippet
```java
            List<Invoker<T>> newInvokers = Collections.unmodifiableList(new ArrayList<>(newUrlInvokerMap.values()));
            BitList<Invoker<T>> finalInvokers = multiGroup ? new BitList<>(toMergeInvokerList(newInvokers)) : new BitList<>(newInvokers);
            // pre-route and build cache
            refreshRouter(finalInvokers.clone(), () -> this.setInvokers(finalInvokers));
            this.urlInvokerMap = newUrlInvokerMap;
```

### DuplicatedCode
Duplicated code
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/ServiceDiscoveryRegistryDirectory.java`
#### Snippet
```java
        List<Invoker<T>> mergedInvokers = new ArrayList<>();
        Map<String, List<Invoker<T>>> groupMap = new HashMap<>();
        for (Invoker<T> invoker : invokers) {
            String group = invoker.getUrl().getGroup("");
            groupMap.computeIfAbsent(group, k -> new ArrayList<>());
            groupMap.get(group).add(invoker);
        }

        if (groupMap.size() == 1) {
            mergedInvokers.addAll(groupMap.values().iterator().next());
        } else if (groupMap.size() > 1) {
            for (List<Invoker<T>> groupList : groupMap.values()) {
                StaticDirectory<T> staticDirectory = new StaticDirectory<>(groupList);
                staticDirectory.buildRouterChain();
                mergedInvokers.add(cluster.join(staticDirectory, false));
            }
        } else {
            mergedInvokers = invokers;
        }
        return mergedInvokers;
```

### DuplicatedCode
Duplicated code
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/ServiceDiscoveryRegistryDirectory.java`
#### Snippet
```java
            for (Invoker<T> invoker : new ArrayList<>(localUrlInvokerMap.values())) {
                try {
                    invoker.destroy();
                } catch (Throwable t) {
                    logger.warn(PROTOCOL_FAILED_DESTROY_INVOKER, "", "", "Failed to destroy service " + serviceKey + " to provider " + invoker.getUrl(), t);
                }
            }
            localUrlInvokerMap.clear();
```

### DuplicatedCode
Duplicated code
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/metadata/MetadataServiceDelegation.java`
#### Snippet
```java
            if (serviceURLs == null) {
                continue;
            }
            for (Map.Entry<String, SortedSet<URL>> entry : serviceURLs.entrySet()) {
                SortedSet<URL> urls = entry.getValue();
                if (urls != null) {
                    for (URL url : urls) {
                        if (!MetadataService.class.getName().equals(url.getServiceInterface())) {
                            bizURLs.add(url);
                        }
                    }
                }
            }
```

### DuplicatedCode
Duplicated code
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/migration/model/SubMigrationRule.java`
#### Snippet
```java
        Object step = map.get(MIGRATION_RULE_STEP_KEY);
        if (step != null) {
            interfaceMigrationRule.setStep(MigrationStep.valueOf(step.toString()));
        }

        Object threshold = map.get(MIGRATION_RULE_THRESHOLD_KEY);
        if (threshold != null) {
            interfaceMigrationRule.setThreshold(Float.valueOf(threshold.toString()));
        }

        Object proportion = map.get(MIGRATION_RULE_PROPORTION_KEY);
        if (proportion != null) {
            interfaceMigrationRule.setProportion(Integer.valueOf(proportion.toString()));
        }

        Object delay = map.get(MIGRATION_RULE_DELAY_KEY);
        if (delay != null) {
            interfaceMigrationRule.setDelay(Integer.valueOf(delay.toString()));
        }

        Object force = map.get(MIGRATION_RULE_FORCE_KEY);
        if (force != null) {
            interfaceMigrationRule.setForce(Boolean.valueOf(force.toString()));
        }
```

### DuplicatedCode
Duplicated code
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/migration/model/MigrationRule.java`
#### Snippet
```java
        if (step != null) {
            migrationRule.setStep(MigrationStep.valueOf(step.toString()));
        }

        Object threshold = map.get(MIGRATION_RULE_THRESHOLD_KEY);
        if (threshold != null) {
            migrationRule.setThreshold(Float.valueOf(threshold.toString()));
        }

        Object proportion = map.get(MIGRATION_RULE_PROPORTION_KEY);
        if (proportion != null) {
            migrationRule.setProportion(Integer.valueOf(proportion.toString()));
        }

        Object delay = map.get(MIGRATION_RULE_DELAY_KEY);
        if (delay != null) {
            migrationRule.setDelay(Integer.valueOf(delay.toString()));
        }

        Object force = map.get(MIGRATION_RULE_FORCE_KEY);
        if (force != null) {
            migrationRule.setForce(Boolean.valueOf(force.toString()));
        }
```

### DuplicatedCode
Duplicated code
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/integration/RegistryProtocol.java`
#### Snippet
```java
        String providerUrlKey = getProviderUrlKey(originInvoker);
        String registryUrlKey = getRegistryUrlKey(originInvoker);
        Map<String, ExporterChangeableWrapper<?>> registryMap = bounds.get(providerUrlKey);
        if (registryMap == null) {
            logger.warn(INTERNAL_ERROR, "error state, exporterMap can not be null", "", "error state, exporterMap can not be null", new IllegalStateException("error state, exporterMap can not be null"));
            return;
        }
```

### DuplicatedCode
Duplicated code
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/support/FailbackRegistry.java`
#### Snippet
```java
            for (Map.Entry<URL, Set<NotifyListener>> entry : recoverSubscribed.entrySet()) {
                URL url = entry.getKey();
                for (NotifyListener listener : entry.getValue()) {
                    // First remove other tasks to ensure that addFailedSubscribed can succeed.
                    removeFailedSubscribed(url, listener);
                    addFailedSubscribed(url, listener);
                }
            }
```

### DuplicatedCode
Duplicated code
in `dubbo-registry/dubbo-registry-nacos/src/main/java/org/apache/dubbo/registry/nacos/NacosNamingServiceWrapper.java`
#### Snippet
```java
            } catch (NacosException e) {
                le = e;
                logger.warn(LoggerCodeConstants.REGISTRY_NACOS_EXCEPTION, "", "",
                    "Failed to request nacos naming server. " +
                        (times < retryTimes ? "Dubbo will try to retry in " + sleepMsBetweenRetries + ". " : "Exceed retry max times.") +
                        "Try times: " + (times + 1), e);
                if (times < retryTimes) {
                    try {
                        Thread.sleep(sleepMsBetweenRetries);
                    } catch (InterruptedException ex) {
                        logger.warn(LoggerCodeConstants.INTERNAL_INTERRUPTED, "", "", "Interrupted when waiting to retry.", ex);
                        Thread.currentThread().interrupt();
                    }
                }
            }
```

### DuplicatedCode
Duplicated code
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/telnet/support/TelnetUtils.java`
#### Snippet
```java
        buf.append('+');
        for (int j = 0; j < widths.length; j++) {
            for (int k = 0; k < widths[j] + 2; k++) {
                buf.append('-');
            }
            buf.append('+');
        }
        buf.append("\r\n");
```

### DuplicatedCode
Duplicated code
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/transport/AbstractEndpoint.java`
#### Snippet
```java
        String codecName = url.getParameter(Constants.CODEC_KEY);
        if (StringUtils.isEmpty(codecName)) {
            // codec extension name must stay the same with protocol name
            codecName = url.getProtocol();
        }
        FrameworkModel frameworkModel = getFrameworkModel(url.getScopeModel());
        if (frameworkModel.getExtensionLoader(Codec2.class).hasExtension(codecName)) {
            return frameworkModel.getExtensionLoader(Codec2.class).getExtension(codecName);
        } else if (frameworkModel.getExtensionLoader(Codec.class).hasExtension(codecName)) {
            return new CodecAdapter(frameworkModel.getExtensionLoader(Codec.class)
                .getExtension(codecName));
        } else {
            return frameworkModel.getExtensionLoader(Codec2.class).getExtension("default");
        }
```

### DuplicatedCode
Duplicated code
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/transport/dispatcher/all/AllChannelHandler.java`
#### Snippet
```java
        ExecutorService executor = getPreferredExecutorService(message);
        try {
            executor.execute(new ChannelEventRunnable(channel, handler, ChannelState.RECEIVED, message));
        } catch (Throwable t) {
            if(message instanceof Request && t instanceof RejectedExecutionException){
                sendFeedback(channel, (Request) message, t);
                return;
            }
            throw new ExecutionException(message, channel, getClass() + " error when process received event .", t);
        }
```

### DuplicatedCode
Duplicated code
in `dubbo-remoting/dubbo-remoting-netty/src/main/java/org/apache/dubbo/remoting/transport/netty/NettyChannel.java`
#### Snippet
```java
        try {
            super.close();
        } catch (Exception e) {
            logger.warn(TRANSPORT_FAILED_CLOSE, "", "", e.getMessage(), e);
        }
        try {
            removeChannelIfDisconnected(channel);
        } catch (Exception e) {
            logger.warn(TRANSPORT_FAILED_CLOSE, "", "", e.getMessage(), e);
        }
        try {
            attributes.clear();
        } catch (Exception e) {
            logger.warn(TRANSPORT_FAILED_CLOSE, "", "", e.getMessage(), e);
        }
        try {
            if (logger.isInfoEnabled()) {
                logger.info("Close netty channel " + channel);
            }
            channel.close();
        } catch (Exception e) {
            logger.warn(TRANSPORT_FAILED_CLOSE, "", "", e.getMessage(), e);
        }
```

### DuplicatedCode
Duplicated code
in `dubbo-remoting/dubbo-remoting-netty/src/main/java/org/apache/dubbo/remoting/transport/netty/NettyClient.java`
#### Snippet
```java
                try {
                    // Close old channel
                    Channel oldChannel = NettyClient.this.channel; // copy reference
                    if (oldChannel != null) {
                        try {
                            if (logger.isInfoEnabled()) {
                                logger.info("Close old netty channel " + oldChannel + " on create new netty channel " + newChannel);
                            }
                            oldChannel.close();
                        } finally {
                            NettyChannel.removeChannelIfDisconnected(oldChannel);
                        }
                    }
                } finally {
                    if (NettyClient.this.isClosed()) {
                        try {
                            if (logger.isInfoEnabled()) {
                                logger.info("Close new netty channel " + newChannel + ", because the client closed.");
                            }
                            newChannel.close();
                        } finally {
                            NettyClient.this.channel = null;
                            NettyChannel.removeChannelIfDisconnected(newChannel);
                        }
                    } else {
                        NettyClient.this.channel = newChannel;
                    }
                }
```

### DuplicatedCode
Duplicated code
in `dubbo-remoting/dubbo-remoting-netty/src/main/java/org/apache/dubbo/remoting/transport/netty/NettyPortUnificationServer.java`
#### Snippet
```java
        NettyHelper.setNettyLoggerFactory();
        ExecutorService boss = Executors.newCachedThreadPool(new NamedThreadFactory(EVENT_LOOP_BOSS_POOL_NAME, true));
        ExecutorService worker = Executors.newCachedThreadPool(new NamedThreadFactory(EVENT_LOOP_WORKER_POOL_NAME, true));
        ChannelFactory channelFactory = new NioServerSocketChannelFactory(boss, worker, getUrl().getPositiveParameter(IO_THREADS_KEY, Constants.DEFAULT_IO_THREADS));
        bootstrap = new ServerBootstrap(channelFactory);

        final NettyHandler nettyHandler = new NettyHandler(getUrl(), this);
        dubboChannels = nettyHandler.getChannels();
        // https://issues.jboss.org/browse/NETTY-365
        // https://issues.jboss.org/browse/NETTY-379
        // final Timer timer = new HashedWheelTimer(new NamedThreadFactory("NettyIdleTimer", true));
        bootstrap.setOption("child.tcpNoDelay", true);
        bootstrap.setOption("backlog", getUrl().getPositiveParameter(BACKLOG_KEY, Constants.DEFAULT_BACKLOG));
```

### DuplicatedCode
Duplicated code
in `dubbo-remoting/dubbo-remoting-netty/src/main/java/org/apache/dubbo/remoting/transport/netty/NettyPortUnificationServer.java`
#### Snippet
```java
        String bindIp = getUrl().getParameter(Constants.BIND_IP_KEY, getUrl().getHost());
        int bindPort = getUrl().getParameter(Constants.BIND_PORT_KEY, getUrl().getPort());
        if (getUrl().getParameter(ANYHOST_KEY, false) || NetUtils.isInvalidLocalHost(bindIp)) {
            bindIp = ANYHOST_VALUE;
        }
        InetSocketAddress bindAddress = new InetSocketAddress(bindIp, bindPort);
```

### DuplicatedCode
Duplicated code
in `dubbo-remoting/dubbo-remoting-netty4/src/main/java/org/apache/dubbo/remoting/transport/netty4/NettyServer.java`
#### Snippet
```java
        try {
            if (channel != null) {
                // unbind.
                channel.close();
            }
        } catch (Throwable e) {
            logger.warn(TRANSPORT_FAILED_CLOSE, "", "", e.getMessage(), e);
        }
        try {
            Collection<Channel> channels = getChannels();
            if (CollectionUtils.isNotEmpty(channels)) {
                for (Channel channel : channels) {
                    try {
                        channel.close();
                    } catch (Throwable e) {
                        logger.warn(TRANSPORT_FAILED_CLOSE, "", "", e.getMessage(), e);
                    }
                }
            }
        } catch (Throwable e) {
            logger.warn(TRANSPORT_FAILED_CLOSE, "", "", e.getMessage(), e);
        }
```

### DuplicatedCode
Duplicated code
in `dubbo-remoting/dubbo-remoting-netty/src/main/java/org/apache/dubbo/remoting/transport/netty/NettyPortUnificationServer.java`
#### Snippet
```java
        try {
            Collection<Channel> channels = getChannels();
            if (CollectionUtils.isNotEmpty(channels)) {
                for (Channel channel : channels) {
                    try {
                        channel.close();
                    } catch (Throwable e) {
                        logger.warn(TRANSPORT_FAILED_CLOSE, "", "", e.getMessage(), e);
                    }
                }
            }
        } catch (Throwable e) {
            logger.warn(TRANSPORT_FAILED_CLOSE, "", "", e.getMessage(), e);
        }
        for (WireProtocol protocol : getProtocols()) {
            protocol.close();
        }
```

### DuplicatedCode
Duplicated code
in `dubbo-remoting/dubbo-remoting-netty/src/main/java/org/apache/dubbo/remoting/transport/netty/NettyPortUnificationServer.java`
#### Snippet
```java
        try {
            if (bootstrap != null) {
                // release external resource.
                bootstrap.releaseExternalResources();
            }
        } catch (Throwable e) {
            logger.warn(TRANSPORT_FAILED_CLOSE, "", "", e.getMessage(), e);
        }
        try {
            if (dubboChannels != null) {
                dubboChannels.clear();
            }
        } catch (Throwable e) {
            logger.warn(TRANSPORT_FAILED_CLOSE, "", "", e.getMessage(), e);
        }
```

### DuplicatedCode
Duplicated code
in `dubbo-remoting/dubbo-remoting-netty4/src/main/java/org/apache/dubbo/remoting/transport/netty4/logging/MessageFormatter.java`
#### Snippet
```java
        sbuf.append('[');
        final int len = a.length;
        for (int i = 0; i < len; i++) {
            sbuf.append(a[i]);
            if (i != len - 1) {
                sbuf.append(", ");
            }
        }
        sbuf.append(']');
```

### DuplicatedCode
Duplicated code
in `dubbo-remoting/dubbo-remoting-netty4/src/main/java/org/apache/dubbo/remoting/transport/netty4/ssl/SslContexts.java`
#### Snippet
```java
SslContextBuilder sslClientContextBuilder;
        InputStream serverKeyCertChainPathStream = null;
        InputStream serverPrivateKeyPathStream = null;
        InputStream serverTrustCertStream = null;
        try {
            serverKeyCertChainPathStream = providerConnectionConfig.getKeyCertChainInputStream();
            serverPrivateKeyPathStream = providerConnectionConfig.getPrivateKeyInputStream();
            serverTrustCertStream = providerConnectionConfig.getTrustCertInputStream();
            String password = providerConnectionConfig.getPassword();
            if (password != null) {
                sslClientContextBuilder = SslContextBuilder.forServer(serverKeyCertChainPathStream,
                    serverPrivateKeyPathStream, password);
            } else {
                sslClientContextBuilder = SslContextBuilder.forServer(serverKeyCertChainPathStream,
                    serverPrivateKeyPathStream);
            }

            if (serverTrustCertStream != null) {
                sslClientContextBuilder.trustManager(serverTrustCertStream);
                if (providerConnectionConfig.getAuthPolicy() == AuthPolicy.CLIENT_AUTH) {
                    sslClientContextBuilder.clientAuth(ClientAuth.REQUIRE);
                } else {
                    sslClientContextBuilder.clientAuth(ClientAuth.OPTIONAL);
                }
            }
        } catch (Exception e) {
            throw new IllegalArgumentException("Could not find certificate file or the certificate is invalid.", e);
        } finally {
            safeCloseStream(serverTrustCertStream);
            safeCloseStream(serverKeyCertChainPathStream);
            safeCloseStream(serverPrivateKeyPathStream);
        }
        try {
            return sslClientContextBuilder.sslProvider(findSslProvider()).build();
        } catch (SSLException e) {
            throw new IllegalStateException("Build SslSession failed.", e);
        }
```

### DuplicatedCode
Duplicated code
in `dubbo-remoting/dubbo-remoting-netty4/src/main/java/org/apache/dubbo/remoting/transport/netty4/ssl/SslContexts.java`
#### Snippet
```java
CertManager certManager = url.getOrDefaultFrameworkModel().getBeanFactory().getBean(CertManager.class);
        Cert consumerConnectionConfig = certManager.getConsumerConnectionConfig(url);
        if (consumerConnectionConfig == null) {
            return null;
        }

        SslContextBuilder builder = SslContextBuilder.forClient();
        InputStream clientTrustCertCollectionPath = null;
        InputStream clientCertChainFilePath = null;
        InputStream clientPrivateKeyFilePath = null;
        try {
            clientTrustCertCollectionPath = consumerConnectionConfig.getTrustCertInputStream();
            if (clientTrustCertCollectionPath != null) {
                builder.trustManager(clientTrustCertCollectionPath);
            }

            clientCertChainFilePath = consumerConnectionConfig.getKeyCertChainInputStream();
            clientPrivateKeyFilePath = consumerConnectionConfig.getPrivateKeyInputStream();
            if (clientCertChainFilePath != null && clientPrivateKeyFilePath != null) {
                String password = consumerConnectionConfig.getPassword();
                if (password != null) {
                    builder.keyManager(clientCertChainFilePath, clientPrivateKeyFilePath, password);
                } else {
                    builder.keyManager(clientCertChainFilePath, clientPrivateKeyFilePath);
                }
            }
        } catch (Exception e) {
            throw new IllegalArgumentException("Could not find certificate file or find invalid certificate.", e);
        } finally {
            safeCloseStream(clientTrustCertCollectionPath);
            safeCloseStream(clientCertChainFilePath);
            safeCloseStream(clientPrivateKeyFilePath);
        }
        try {
            return builder.sslProvider(findSslProvider()).build();
        } catch (SSLException e) {
            throw new IllegalStateException("Build SslSession failed.", e);
        }
```

### DuplicatedCode
Duplicated code
in `dubbo-remoting/dubbo-remoting-netty4/src/main/java/org/apache/dubbo/remoting/transport/netty4/ssl/SslServerTlsHandler.java`
#### Snippet
```java
        if (evt instanceof SslHandshakeCompletionEvent) {
            SslHandshakeCompletionEvent handshakeEvent = (SslHandshakeCompletionEvent) evt;
            if (handshakeEvent.isSuccess()) {
                SSLSession session = ctx.pipeline().get(SslHandler.class).engine().getSession();
                logger.info("TLS negotiation succeed with: " + session.getPeerHost());
                // Remove after handshake success.
                ctx.pipeline().remove(this);
            } else {
                logger.error(INTERNAL_ERROR, "", "", "TLS negotiation failed when trying to accept new connection.", handshakeEvent.cause());
                ctx.close();
            }
        }
        super.userEventTriggered(ctx, evt);
```

### DuplicatedCode
Duplicated code
in `dubbo-remoting/dubbo-remoting-netty4/src/main/java/org/apache/dubbo/remoting/transport/netty4/ssl/SslServerTlsHandler.java`
#### Snippet
```java
        if (byteBuf.readableBytes() < 5) {
            return;
        }

        if (sslDetected) {
            return;
        }

        CertManager certManager = url.getOrDefaultFrameworkModel().getBeanFactory().getBean(CertManager.class);
        ProviderCert providerConnectionConfig = certManager.getProviderConnectionConfig(url, channelHandlerContext.channel().remoteAddress());

        if (providerConnectionConfig == null) {
            ChannelPipeline p = channelHandlerContext.pipeline();
            p.remove(this);
            return;
        }

        if (isSsl(byteBuf)) {
            SslContext sslContext = SslContexts.buildServerSslContext(providerConnectionConfig);
            enableSsl(channelHandlerContext, sslContext);
            return;
        }

        if (providerConnectionConfig.getAuthPolicy() == AuthPolicy.NONE) {
            ChannelPipeline p = channelHandlerContext.pipeline();
            p.remove(this);
        }

        logger.error(INTERNAL_ERROR, "", "", "TLS negotiation failed when trying to accept new connection.");
        channelHandlerContext.close();
```

### DuplicatedCode
Duplicated code
in `dubbo-remoting/dubbo-remoting-zookeeper-curator5/src/main/java/org/apache/dubbo/remoting/zookeeper/curator5/Curator5ZookeeperClient.java`
#### Snippet
```java
        try {
            client.create().forPath(path);
        } catch (NodeExistsException e) {
            if (!faultTolerant) {
                logger.warn(REGISTRY_ZOOKEEPER_EXCEPTION, "", "", "ZNode " + path + " already exists.", e);
                throw new IllegalStateException(e.getMessage(), e);
            }
        } catch (Exception e) {
            throw new IllegalStateException(e.getMessage(), e);
        }
```

### DuplicatedCode
Duplicated code
in `dubbo-remoting/dubbo-remoting-zookeeper-curator5/src/main/java/org/apache/dubbo/remoting/zookeeper/curator5/Curator5ZookeeperClient.java`
#### Snippet
```java
        try {
            client.create().withMode(CreateMode.EPHEMERAL).forPath(path);
        } catch (NodeExistsException e) {
            if (faultTolerant) {
                logger.info("ZNode " + path + " already exists, since we will only try to recreate a node on a session expiration" +
                    ", this duplication might be caused by a delete delay from the zk server, which means the old expired session" +
                    " may still holds this ZNode and the server just hasn't got time to do the deletion. In this case, " +
                    "we can just try to delete and create again.");
                deletePath(path);
                createEphemeral(path, true);
            } else {
                logger.warn(REGISTRY_ZOOKEEPER_EXCEPTION, "", "", "ZNode " + path + " already exists.", e);
                throw new IllegalStateException(e.getMessage(), e);
            }
        } catch (Exception e) {
            throw new IllegalStateException(e.getMessage(), e);
        }
```

### DuplicatedCode
Duplicated code
in `dubbo-remoting/dubbo-remoting-zookeeper-curator5/src/main/java/org/apache/dubbo/remoting/zookeeper/curator5/Curator5ZookeeperClient.java`
#### Snippet
```java
        byte[] dataBytes = data.getBytes(CHARSET);
        try {
            client.create().forPath(path, dataBytes);
        } catch (NodeExistsException e) {
            if (faultTolerant) {
                logger.info("ZNode " + path + " already exists. Will be override with new data.");
                try {
                    client.setData().forPath(path, dataBytes);
                } catch (Exception e1) {
                    throw new IllegalStateException(e.getMessage(), e1);
                }
            } else {
                logger.warn(REGISTRY_ZOOKEEPER_EXCEPTION, "", "", "ZNode " + path + " already exists.", e);
                throw new IllegalStateException(e.getMessage(), e);
            }
        } catch (Exception e) {
            throw new IllegalStateException(e.getMessage(), e);
        }
```

### DuplicatedCode
Duplicated code
in `dubbo-remoting/dubbo-remoting-zookeeper-curator5/src/main/java/org/apache/dubbo/remoting/zookeeper/curator5/Curator5ZookeeperClient.java`
#### Snippet
```java
        byte[] dataBytes = data.getBytes(CHARSET);
        try {
            client.create().withMode(CreateMode.EPHEMERAL).forPath(path, dataBytes);
        } catch (NodeExistsException e) {
            if (faultTolerant) {
                logger.info("ZNode " + path + " already exists, since we will only try to recreate a node on a session expiration" +
                    ", this duplication might be caused by a delete delay from the zk server, which means the old expired session" +
                    " may still holds this ZNode and the server just hasn't got time to do the deletion. In this case, " +
                    "we can just try to delete and create again.");
                deletePath(path);
                createEphemeral(path, data, true);
            } else {
                logger.warn(REGISTRY_ZOOKEEPER_EXCEPTION, "", "", "ZNode " + path + " already exists.", e);
                throw new IllegalStateException(e.getMessage(), e);
            }
        } catch (Exception e) {
            throw new IllegalStateException(e.getMessage(), e);
        }
```

### DuplicatedCode
Duplicated code
in `dubbo-remoting/dubbo-remoting-zookeeper-curator5/src/main/java/org/apache/dubbo/remoting/zookeeper/curator5/Curator5ZookeeperClient.java`
#### Snippet
```java
        try {
            byte[] dataBytes = client.getData().forPath(path);
            return (dataBytes == null || dataBytes.length == 0) ? null : new String(dataBytes, CHARSET);
        } catch (NoNodeException e) {
            // ignore NoNode Exception.
        } catch (Exception e) {
            throw new IllegalStateException(e.getMessage(), e);
        }
        return null;
```

### DuplicatedCode
Duplicated code
in `dubbo-remoting/dubbo-remoting-zookeeper-curator5/src/main/java/org/apache/dubbo/remoting/zookeeper/curator5/Curator5ZookeeperClient.java`
#### Snippet
```java
        String content;
        Stat stat;
        try {
            stat = new Stat();
            byte[] dataBytes = client.getData().storingStatIn(stat).forPath(path);
            content = (dataBytes == null || dataBytes.length == 0) ? null : new String(dataBytes, CHARSET);
        } catch (NoNodeException e) {
            return new ConfigItem();
        } catch (Exception e) {
            throw new IllegalStateException(e.getMessage(), e);
        }
        return new ConfigItem(content, stat);
```

### DuplicatedCode
Duplicated code
in `dubbo-remoting/dubbo-remoting-zookeeper-curator5/src/main/java/org/apache/dubbo/remoting/zookeeper/curator5/Curator5ZookeeperClient.java`
#### Snippet
```java
        try {
            NodeCache nodeCache = new NodeCache(client, path);
            if (nodeCacheMap.putIfAbsent(path, nodeCache) != null) {
                return;
            }
            if (executor == null) {
                nodeCache.getListenable().addListener(nodeCacheListener);
            } else {
                nodeCache.getListenable().addListener(nodeCacheListener, executor);
            }

            nodeCache.start();
        } catch (Exception e) {
            throw new IllegalStateException("Add nodeCache listener for path:" + path, e);
        }
```

### DuplicatedCode
Duplicated code
in `dubbo-remoting/dubbo-remoting-zookeeper-curator5/src/main/java/org/apache/dubbo/remoting/zookeeper/curator5/Curator5ZookeeperClient.java`
#### Snippet
```java
            ChildData childData = nodeCacheMap.get(path).getCurrentData();
            String content = null;
            EventType eventType;
            if (childData == null) {
                eventType = EventType.NodeDeleted;
            } else if (childData.getStat().getVersion() == 0) {
                content = new String(childData.getData(), CHARSET);
                eventType = EventType.NodeCreated;
            } else {
                content = new String(childData.getData(), CHARSET);
                eventType = EventType.NodeDataChanged;
            }
            dataListener.dataChanged(path, content, eventType);
```

### DuplicatedCode
Duplicated code
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcInvocation.java`
#### Snippet
```java
        try {
            attachmentLock.lock();
            if (attachments == null) {
                return;
            }
            if (this.attachments == null) {
                this.attachments = new HashMap<>();
            }
            this.attachments.putAll(attachments);
        } finally {
            attachmentLock.unlock();
        }
```

### DuplicatedCode
Duplicated code
in `dubbo-rpc/dubbo-rpc-dubbo/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/filter/FutureFilter.java`
#### Snippet
```java
        if (rParaTypes.length > 1) {
            if (rParaTypes.length == 2 && rParaTypes[1].isAssignableFrom(Object[].class)) {
                params = new Object[2];
                params[0] = result;
                params[1] = args;
            } else {
                params = new Object[args.length + 1];
                params[0] = result;
                System.arraycopy(args, 0, params, 1, args.length);
            }
        } else {
            params = new Object[]{result};
        }
```

### DuplicatedCode
Duplicated code
in `dubbo-rpc/dubbo-rpc-dubbo/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/pu/DubboDetector.java`
#### Snippet
```java
        int bytesRead = min(in.readableBytes(), prefaceLen);

        if (bytesRead ==0 || !ChannelBuffers.prefixEquals(in,  Preface,  bytesRead)) {
            return Result.UNRECOGNIZED;
        }
        if (bytesRead == prefaceLen) {
            return Result.RECOGNIZED;
        }

        return Result.NEED_MORE_DATA;
```

### DuplicatedCode
Duplicated code
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/TripleCustomerProtocolWapper.java`
#### Snippet
```java
            int totalSize = 0;

            int serializeTypeTag = makeTag(1, 2);
            byte[] serializeTypeTagBytes = varIntEncode(serializeTypeTag);
            byte[] serializeTypeBytes = serializeType.getBytes(StandardCharsets.UTF_8);
            byte[] serializeTypeLengthVarIntEncodeBytes = varIntEncode(serializeTypeBytes.length);
            totalSize += serializeTypeTagBytes.length
                + serializeTypeLengthVarIntEncodeBytes.length
                + serializeTypeBytes.length;
```

### DuplicatedCode
Duplicated code
in `dubbo-serialization/dubbo-serialization-fastjson2/src/main/java/org/apache/dubbo/common/serialize/fastjson2/FastJson2ObjectInput.java`
#### Snippet
```java
        updateClassLoaderIfNeed();
        int length = readLength();
        byte[] bytes = new byte[length];
        int read = is.read(bytes, 0, length);
        if (read != length) {
            throw new IllegalArgumentException("deserialize failed. expected read length: " + length + " but actual read: " + read);
        }
        Fastjson2SecurityManager.Handler securityFilter = fastjson2SecurityManager.getSecurityFilter();
        T result;
        if (securityFilter.isCheckSerializable()) {
            result = JSONB.parseObject(bytes, cls, securityFilter,
                JSONReader.Feature.UseDefaultConstructorAsPossible,
                JSONReader.Feature.ErrorOnNoneSerializable,
                JSONReader.Feature.IgnoreAutoTypeNotMatch,
                JSONReader.Feature.UseNativeObject,
                JSONReader.Feature.FieldBased);
        } else {
            result = JSONB.parseObject(bytes, cls, securityFilter,
                JSONReader.Feature.UseDefaultConstructorAsPossible,
                JSONReader.Feature.UseNativeObject,
                JSONReader.Feature.IgnoreAutoTypeNotMatch,
                JSONReader.Feature.FieldBased);
        }
        if (result != null && cls != null && !ClassUtils.isMatch(result.getClass(), cls)) {
            throw new IllegalArgumentException("deserialize failed. expected class: " + cls + " but actual class: " + result.getClass());
        }
        return result;
```

## RuleId[id=CollectionContainsUrl]
### CollectionContainsUrl
Set `urls` may contain URL objects
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/SerializeSecurityConfigurator.java`
#### Snippet
```java

    private void loadBlocked(ClassLoader classLoader) {
        Set<URL> urls = ClassLoaderResourceLoader.loadResources(SERIALIZE_BLOCKED_LIST_FILE_PATH, classLoader);
        for (URL u : urls) {
            try {
```

### CollectionContainsUrl
Set `urls` may contain URL objects
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/SerializeSecurityConfigurator.java`
#### Snippet
```java

    private void loadAllow(ClassLoader classLoader) {
        Set<URL> urls = ClassLoaderResourceLoader.loadResources(SERIALIZE_ALLOW_LIST_FILE_PATH, classLoader);
        for (URL u : urls) {
            try {
```

### CollectionContainsUrl
Set `urls` may contain URL objects
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ConfigUtils.java`
#### Snippet
```java
            classLoadersToLoad.add(ClassUtils.getClassLoader());
            classLoadersToLoad.addAll(classLoaders);
            for (Set<URL> urls : ClassLoaderResourceLoader.loadResources(fileName, classLoadersToLoad).values()) {
                for (URL url : urls) {
                    InputStream is = url.openStream();
```

### CollectionContainsUrl
Set `set` may contain URL objects
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ConfigUtils.java`
#### Snippet
```java
        }

        Set<java.net.URL> set = null;
        try {
            List<ClassLoader> classLoadersToLoad = new LinkedList<>();
```

### CollectionContainsUrl
Set `set` may contain URL objects
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ClassLoaderResourceLoader.java`
#### Snippet
```java
        Map<String, Set<URL>> urlCache = classLoaderCache.get(currentClassLoader);
        if (!urlCache.containsKey(fileName)) {
            Set<URL> set = new LinkedHashSet<>();
            Enumeration<URL> urls;
            try {
```

### CollectionContainsUrl
Map `urlListMap` may contain URL objects
in `dubbo-common/src/main/java/org/apache/dubbo/common/extension/ExtensionLoader.java`
#### Snippet
```java

    private List<String> getResourceContent(java.net.URL resourceURL) throws IOException {
        Map<java.net.URL, List<String>> urlListMap = urlListMapCache.get();
        if (urlListMap == null) {
            synchronized (ExtensionLoader.class) {
```

### CollectionContainsUrl
Set `urls` may contain URL objects
in `dubbo-common/src/main/java/org/apache/dubbo/common/extension/ExtensionLoader.java`
#### Snippet
```java

    private void loadFromClass(Map<String, Class<?>> extensionClasses, boolean overridden,
                               Set<java.net.URL> urls, ClassLoader classLoader,
                               String[] includedPackages, String[] excludedPackages,
                               String[] onlyExtensionClassLoaderPackages) {
```

## RuleId[id=ProtectedMemberInFinalClass]
### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `dubbo-spring-boot/dubbo-spring-boot-autoconfigure/src/main/java/org/apache/dubbo/spring/boot/autoconfigure/DubboRelaxedBinding2AutoConfiguration.java`
#### Snippet
```java
        ConfigurableEnvironment propertyResolver = new AbstractEnvironment() {
            @Override
            protected void customizePropertySources(MutablePropertySources propertySources) {
                Map<String, Object> dubboScanProperties = PropertySourcesUtils.getSubProperties(environment.getPropertySources(), DUBBO_SCAN_PREFIX);
                propertySources.addLast(new MapPropertySource("dubboScanProperties", dubboScanProperties));
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/StringUtils.java`
#### Snippet
```java
    }

    protected static boolean isSnakeCase(String str) {
        return str.contains("_") || str.equals(str.toLowerCase()) || str.equals(str.toUpperCase());
    }
```

## RuleId[id=DanglingJavadoc]
### DanglingJavadoc
Dangling Javadoc comment
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/ServiceConfig.java`
#### Snippet
```java
                return;
            }
            /**
             * Because executor is not a string type, it cannot be attached to the url parameter, so it is added to URL#attributes
             * and obtained it in IsolationExecutorRepository#createExecutor method
```

### DanglingJavadoc
Dangling Javadoc comment
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/ServiceConfig.java`
#### Snippet
```java
        }

        /**
         * Here the token value configured by the provider is used to assign the value to ServiceConfig#token
         */
```

### DanglingJavadoc
Dangling Javadoc comment
in `dubbo-spring-boot/dubbo-spring-boot-compatible/autoconfigure/src/main/java/org/apache/dubbo/spring/boot/context/event/WelcomeLogoApplicationListener.java`
#### Snippet
```java
        }

        /**
         * Gets Logger After LoggingSystem configuration ready
         * @see LoggingApplicationListener
```

### DanglingJavadoc
Dangling Javadoc comment
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/MetadataInfo.java`
#### Snippet
```java

            ServiceInfo serviceInfo = (ServiceInfo) obj;
            /**
             * Equals to Objects.equals(this.getMatchKey(), serviceInfo.getMatchKey()), but match key will not get initialized
             * on json deserialization.
```

### DanglingJavadoc
Dangling Javadoc comment
in `dubbo-common/src/main/java/org/apache/dubbo/common/extension/ExtensionLoader.java`
#### Snippet
```java
                    continue;
                }
                /**
                 * Check {@link DisableInject} to see if we need auto-injection for this property
                 */
```

### DanglingJavadoc
Dangling Javadoc comment
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/migration/model/MigrationRule.java`
#### Snippet
```java
        }

        /**
         * FIXME, it's really hard to follow setting default values here.
         */
```

### DanglingJavadoc
Dangling Javadoc comment
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/schema/AnnotationBeanDefinitionParser.java`
#### Snippet
```java
        builder.setRole(BeanDefinition.ROLE_INFRASTRUCTURE);

        /**
         * @since 2.7.6 Register the common beans
         * @since 2.7.8 comment this code line, and migrated to
```

### DanglingJavadoc
Dangling Javadoc comment
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/schema/DubboBeanDefinitionParser.java`
#### Snippet
```java
                        beanDefinition.setLazyInit(true);
                    } else if ("provider".equals(property) || "registry".equals(property) || ("protocol".equals(property) && AbstractServiceConfig.class.isAssignableFrom(beanClass))) {
                        /**
                         * For 'provider' 'protocol' 'registry', keep literal value (should be id/name) and set the value to 'registryIds' 'providerIds' protocolIds'
                         * The following process should make sure each id refers to the corresponding instance, here's how to find the instance for different use cases:
```

### DanglingJavadoc
Dangling Javadoc comment
in `dubbo-registry/dubbo-registry-nacos/src/main/java/org/apache/dubbo/registry/nacos/NacosRegistry.java`
#### Snippet
```java
            if (isServiceNamesWithCompatibleMode(url)) {

                /**
                 * Get all instances with serviceNames to avoid instance overwrite and but with empty instance mentioned
                 * in https://github.com/apache/dubbo/issues/5885 and https://github.com/apache/dubbo/issues/5899
```

### DanglingJavadoc
Dangling Javadoc comment
in `dubbo-registry/dubbo-registry-nacos/src/main/java/org/apache/dubbo/registry/nacos/NacosRegistry.java`
#### Snippet
```java
                String serviceName = getServiceName(url);
                Instance instance = createInstance(url);
                /**
                 *  namingService.registerInstance with {@link org.apache.dubbo.registry.support.AbstractRegistry#registryUrl}
                 *  default {@link DEFAULT_GROUP}
```

### DanglingJavadoc
Dangling Javadoc comment
in `dubbo-filter/dubbo-filter-cache/src/main/java/org/apache/dubbo/cache/support/expiring/ExpiringCache.java`
#### Snippet
```java
import java.util.Map;

/**
 * ExpiringCache - With the characteristic of expiration time.
 */
```

### DanglingJavadoc
Dangling Javadoc comment
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/filter/GenericImplFilter.java`
#### Snippet
```java
            RpcInvocation invocation2 = new RpcInvocation(invocation);

            /**
             * Mark this invocation as a generic impl call, this value will be removed automatically before passing on the wire.
             * See {@link RpcUtils#sieveUnnecessaryAttachments(Invocation)}
```

### DanglingJavadoc
Dangling Javadoc comment
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/filter/support/ConsumerContextFilter.java`
#### Snippet
```java
            Map<String, Object> contextAttachments = RpcContext.getClientAttachment().getObjectAttachments();
            if (CollectionUtils.isNotEmptyMap(contextAttachments)) {
                /**
                 * invocation.addAttachmentsIfAbsent(context){@link RpcInvocation#addAttachmentsIfAbsent(Map)}should not be used here,
                 * because the {@link RpcContext#setAttachment(String, String)} is passed in the Filter when the call is triggered
```

### DanglingJavadoc
Dangling Javadoc comment
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/transport/AbstractClient.java`
#### Snippet
```java
        ExecutorRepository executorRepository = ExecutorRepository.getInstance(url.getOrDefaultApplicationModel());

        /**
         * Consumer's executor is shared globally, provider ip doesn't need to be part of the thread name.
         *
```

## RuleId[id=ComparatorResultComparison]
### ComparatorResultComparison
Comparison of compare method result with specific constant
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/CIDRUtils.java`
#### Snippet
```java
        int te = target.compareTo(end);

        return (st == -1 || st == 0) && (te == -1 || te == 0);
    }
}
```

### ComparatorResultComparison
Comparison of compare method result with specific constant
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/CIDRUtils.java`
#### Snippet
```java
        int te = target.compareTo(end);

        return (st == -1 || st == 0) && (te == -1 || te == 0);
    }
}
```

## RuleId[id=TrivialIf]
### TrivialIf
`if` statement can be simplified
in `dubbo-registry/dubbo-registry-multiple/src/main/java/org/apache/dubbo/registry/multiple/MultipleRegistry.java`
#### Snippet
```java
            }
        }
        if (!available) {
            return false;
        }
```

### TrivialIf
`if` statement can be simplified
in `dubbo-monitor/dubbo-monitor-default/src/main/java/org/apache/dubbo/monitor/dubbo/Statistics.java`
#### Snippet
```java
        }
        if (version == null) {
            if (other.version != null) {
                return false;
            }
```

### TrivialIf
`if` statement can be simplified
in `dubbo-monitor/dubbo-monitor-default/src/main/java/org/apache/dubbo/monitor/dubbo/Statistics.java`
#### Snippet
```java
                return false;
            }
        } else if (!version.equals(other.version)) {
            return false;
        }
```

### TrivialIf
`if` statement can be simplified
in `dubbo-remoting/dubbo-remoting-netty4/src/main/java/org/apache/dubbo/remoting/transport/netty4/NettyChannel.java`
#### Snippet
```java
        NettyChannel other = (NettyChannel) obj;
        if (channel == null) {
            if (other.channel != null) {
                return false;
            }
```

### TrivialIf
`if` statement can be simplified
in `dubbo-remoting/dubbo-remoting-netty4/src/main/java/org/apache/dubbo/remoting/transport/netty4/NettyChannel.java`
#### Snippet
```java
                return false;
            }
        } else if (!channel.equals(other.channel)) {
            return false;
        }
```

### TrivialIf
`if` statement can be simplified
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/rest/PathMatcher.java`
#### Snippet
```java
        boolean lengthCondition = pathSplit.length() >= 3 || pathToCompare.length() >= 3;

        if (!lengthCondition) {
            return false;
        }
```

### TrivialIf
`if` statement can be simplified
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/MetadataInfo.java`
#### Snippet
```java
        if (serviceInfo == null) {
            serviceInfo = getNoProtocolServiceInfo(serviceKey);
            if (serviceInfo == null) {
                return null;
            }
```

### TrivialIf
`if` statement can be simplified
in `dubbo-metadata/dubbo-metadata-definition-protobuf/src/main/java/org/apache/dubbo/metadata/definition/protobuf/ProtobufTypeBuilder.java`
#### Snippet
```java
        String methodName = methodTemp.getName();
        Class[] parameters = methodTemp.getParameterTypes();
        if (methodName.startsWith("putAll") && parameters.length == 1 && Map.class.isAssignableFrom(parameters[0])) {
            return true;
        }
```

### TrivialIf
`if` statement can be simplified
in `dubbo-metadata/dubbo-metadata-definition-protobuf/src/main/java/org/apache/dubbo/metadata/definition/protobuf/ProtobufTypeBuilder.java`
#### Snippet
```java

        // if field name end with List, should skip
        if (!List.class.isAssignableFrom(type)) {
            return false;
        }
```

### TrivialIf
`if` statement can be simplified
in `dubbo-metadata/dubbo-metadata-definition-protobuf/src/main/java/org/apache/dubbo/metadata/definition/protobuf/ProtobufTypeBuilder.java`
#### Snippet
```java
        // skip setXXXValue(int value)
        // parse setXXX(SomeEnum value)
        if (methodName.endsWith("Value") && types[0] == int.class) {
            return false;
        }
```

### TrivialIf
`if` statement can be simplified
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/HttpHeaderUtil.java`
#### Snippet
```java
    public static boolean isRestAttachHeader(String header) {

        if (StringUtils.isEmpty(header) || !header.startsWith(RestHeaderEnum.REST_HEADER_PREFIX.getHeader())) {
            return false;
        }
```

### TrivialIf
`if` statement can be simplified
in `dubbo-common/src/main/java/org/apache/dubbo/common/ProtocolServiceKey.java`
#### Snippet
```java
                            }
                        }
                        if (!match) {
                            return false;
                        }
```

### TrivialIf
`if` statement can be simplified
in `dubbo-common/src/main/java/org/apache/dubbo/common/ProtocolServiceKey.java`
#### Snippet
```java
                    }
                    // 4.3. if rule protocol is not contains ',', match directly
                    else if (!Objects.equals(rule.getProtocol(), target.getProtocol())) {
                        return false;
                    }
```

### TrivialIf
`if` statement can be simplified
in `dubbo-common/src/main/java/org/apache/dubbo/common/ServiceKey.java`
#### Snippet
```java
                            }
                        }
                        if (!match) {
                            return false;
                        }
```

### TrivialIf
`if` statement can be simplified
in `dubbo-common/src/main/java/org/apache/dubbo/common/ServiceKey.java`
#### Snippet
```java
                    }
                    // 3.4. if rule group is not contains ',', match directly
                    else if (!Objects.equals(rule.getGroup(), target.getGroup())) {
                        return false;
                    }
```

### TrivialIf
`if` statement can be simplified
in `dubbo-common/src/main/java/org/apache/dubbo/common/Version.java`
#### Snippet
```java
        }
        int iVersion = getIntVersion(version);
        if (iVersion >= LOWEST_VERSION_FOR_RESPONSE_ATTACHMENT && iVersion <= HIGHEST_PROTOCOL_VERSION) {
            return true;
        }
```

### TrivialIf
`if` statement can be simplified
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/Log.java`
#### Snippet
```java
        }
        if (logThread == null) {
            if (other.logThread != null) {
                return false;
            }
```

### TrivialIf
`if` statement can be simplified
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/Log.java`
#### Snippet
```java
                return false;
            }
        } else if (!logThread.equals(other.logThread)) {
            return false;
        }
```

### TrivialIf
`if` statement can be simplified
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ExecutorUtil.java`
#### Snippet
```java
    public static boolean isTerminated(Executor executor) {
        if (executor instanceof ExecutorService) {
            if (((ExecutorService) executor).isTerminated()) {
                return true;
            }
```

### TrivialIf
`if` statement can be simplified
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ClassUtils.java`
#### Snippet
```java
     */
    public static boolean isTypeMatch(Class<?> type, String value) {
        if ((type == boolean.class || type == Boolean.class)
            && !("true".equals(value) || "false".equals(value))) {
            return false;
```

### TrivialIf
`if` statement can be simplified
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/LogUtil.java`
#### Snippet
```java

    public static boolean checkNoError() {
        if (findLevel(Level.ERROR) == 0) {
            return true;
        } else {
```

### TrivialIf
`if` statement can be simplified
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/StringUtils.java`
#### Snippet
```java

    private static boolean isWord(char ch) {
        if ((ch >= 'A' && ch <= 'Z') ||
            (ch >= 'a' && ch <= 'z') ||
            (ch >= '0' && ch <= '9')) {
```

### TrivialIf
`if` statement can be simplified
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/MethodUtils.java`
#### Snippet
```java

        // Method comparison: The return type of overrider must be inherit from the overridden's
        if (!overridden.getReturnType().isAssignableFrom(overrider.getReturnType())) {
            return false;
        }
```

### TrivialIf
`if` statement can be simplified
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/MethodUtils.java`
#### Snippet
```java
            return false;
        }
        if (!ClassUtils.isPrimitive(method.getReturnType())) {
            return false;
        }
```

### TrivialIf
`if` statement can be simplified
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/NetUtils.java`
#### Snippet
```java
        if ((ip.charAt(0) == '[' && ip.indexOf(']') > 2)) {
            return true;
        } else if (ip.indexOf(":") != ip.lastIndexOf(":")) {
            return true;
        } else {
```

### TrivialIf
`if` statement can be simplified
in `dubbo-common/src/main/java/org/apache/dubbo/common/convert/multiple/StringToArrayConverter.java`
#### Snippet
```java

    public boolean accept(Class<String> type, Class<?> multiValueType) {
        if (multiValueType != null && multiValueType.isArray()) {
            return true;
        }
```

### TrivialIf
`if` statement can be simplified
in `dubbo-common/src/main/java/org/apache/dubbo/config/context/AbstractConfigManager.java`
#### Snippet
```java
     */
    protected <T extends AbstractConfig> boolean isRequired(Class<T> clazz) {
        if (clazz == RegistryConfig.class ||
                clazz == MetadataReportConfig.class ||
                clazz == MonitorConfig.class ||
```

### TrivialIf
`if` statement can be simplified
in `dubbo-common/src/main/java/org/apache/dubbo/config/context/AbstractConfigManager.java`
#### Snippet
```java
     */
    protected <T extends AbstractConfig> boolean isNeedValidation(T config) {
        if (config instanceof MetadataReportConfig) {
            return false;
        }
```

### TrivialIf
`if` statement can be simplified
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/AbstractAnnotationBeanPostProcessor.java`
#### Snippet
```java
            }
            //IGNORE Spring CGLIB enhanced class
            if (targetClass.isAssignableFrom(clazz) && clazz.getName().contains("$$EnhancerBySpringCGLIB$$")) {
                return false;
            }
```

### TrivialIf
`if` statement can be simplified
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/ReferenceAnnotationBeanPostProcessor.java`
#### Snippet
```java
            AnnotatedBeanDefinition annotatedBeanDefinition = (AnnotatedBeanDefinition) beanDefinition;
            String beanClassName = SpringCompatUtils.getFactoryMethodReturnType(annotatedBeanDefinition);
            if (beanClassName != null && ReferenceBean.class.getName().equals(beanClassName)) {
                return true;
            }
```

### TrivialIf
`if` statement can be simplified
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/extension/SpringExtensionInjector.java`
#### Snippet
```java

        T bean = getOptionalBean(context, name, type);
        if (bean != null) {
            return bean;
        }
```

### TrivialIf
`if` statement can be simplified
in `dubbo-registry/dubbo-registry-nacos/src/main/java/org/apache/dubbo/registry/nacos/NacosServiceName.java`
#### Snippet
```java
        }

        if (!StringUtils.isEquals(this.group, concreteServiceName.group) &&
                !matchRange(this.group, concreteServiceName.group)) {
            return false;
```

### TrivialIf
`if` statement can be simplified
in `dubbo-rpc/dubbo-rpc-injvm/src/main/java/org/apache/dubbo/rpc/protocol/injvm/InjvmProtocol.java`
#### Snippet
```java
            // Broadcast cluster means that multiple machines will be called,
            // which is not converted to injvm protocol at this time.
            if (BROADCAST_CLUSTER.equalsIgnoreCase(url.getParameter(CLUSTER_KEY))) {
                return false;
            }
```

### TrivialIf
`if` statement can be simplified
in `dubbo-remoting/dubbo-remoting-netty/src/main/java/org/apache/dubbo/remoting/transport/netty/NettyChannel.java`
#### Snippet
```java
        NettyChannel other = (NettyChannel) obj;
        if (channel == null) {
            if (other.channel != null) {
                return false;
            }
```

### TrivialIf
`if` statement can be simplified
in `dubbo-remoting/dubbo-remoting-netty/src/main/java/org/apache/dubbo/remoting/transport/netty/NettyChannel.java`
#### Snippet
```java
                return false;
            }
        } else if (!channel.equals(other.channel)) {
            return false;
        }
```

### TrivialIf
`if` statement can be simplified
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/support/RpcUtils.java`
#### Snippet
```java
            // add invocationid in async operation by default
            return isAsync(url, invocation);
        } else if (Boolean.TRUE.toString().equalsIgnoreCase(value)) {
            return true;
        } else {
```

### TrivialIf
`if` statement can be simplified
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/TriHttp2RemoteFlowController.java`
#### Snippet
```java

        final boolean isOverFlowControl() {
            if(connectionState.windowSize() == 0){
                return true;
            }else {
```

### TrivialIf
`if` statement can be simplified
in `dubbo-remoting/dubbo-remoting-http/src/main/java/org/apache/dubbo/remoting/http/restclient/URLConnectionRestClient.java`
#### Snippet
```java
        connection.setDoInput(true);

        if ("GET".equals(httpMethod)) {
            connection.setInstanceFollowRedirects(true);
        } else {
```

### TrivialIf
`if` statement can be simplified
in `dubbo-remoting/dubbo-remoting-http/src/main/java/org/apache/dubbo/remoting/http/restclient/URLConnectionRestClient.java`
#### Snippet
```java


        if ("POST".equals(httpMethod) || "PUT".equals(httpMethod) ||
            "PATCH".equals(httpMethod) || "DELETE".equals(httpMethod)) {
            connection.setDoOutput(true);
```

## RuleId[id=OptionalUsedAsFieldOrParameterType]
### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'watchService'
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/configcenter/file/FileSystemDynamicConfiguration.java`
#### Snippet
```java
        });

    private static final Optional<WatchService> watchService;

    /**
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'watchService'
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/configcenter/file/FileSystemDynamicConfiguration.java`
#### Snippet
```java
     * @return if based, return <code>true</code>, or <code>false</code>
     */
    private static boolean detectPoolingBasedWatchService(Optional<WatchService> watchService) {
        String className = watchService.map(Object::getClass).map(Class::getName).orElse(null);
        return POLLING_WATCH_SERVICE_CLASS_NAME.equals(className);
```

## RuleId[id=NonStrictComparisonCanBeEquality]
### NonStrictComparisonCanBeEquality
Can be replaced with equality
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/DataParseUtils.java`
#### Snippet
```java
                        token = token.trim();
                    }
                } while (ignoreEmptyTokens && token.length() <= 0);

                tokens.add(token);
```

### NonStrictComparisonCanBeEquality
Can be replaced with equality
in `dubbo-common/src/main/java/org/apache/dubbo/common/extension/AdaptiveClassCodeGenerator.java`
#### Snippet
```java
        }

        if (getterReturnUrl.size() <= 0) {
            // getter method not found, throw
            throw new IllegalStateException("Failed to create adaptive class for interface " + type.getName()
```

## RuleId[id=CharsetObjectCanBeUsed]
### CharsetObjectCanBeUsed
charset can be used instead
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/telnet/codec/TelnetCodec.java`
#### Snippet
```java
            return "";
        }
        return new String(copy, 0, index, charset.name()).trim();
    }

```

### CharsetObjectCanBeUsed
getCharset(channel) can be used instead
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/telnet/codec/TelnetCodec.java`
#### Snippet
```java
                message = message + "\r\n";
            }
            byte[] msgData = ((String) message).getBytes(getCharset(channel).name());
            buffer.writeBytes(msgData);
        } else {
```

### CharsetObjectCanBeUsed
getCharset(channel) can be used instead
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/telnet/codec/TelnetCodec.java`
#### Snippet
```java
            try {
                boolean isDoubleChar = message.length >= 3 && message[message.length - 3] < 0; // double byte char
                channel.send(new String(isDoubleChar ? new byte[]{32, 32, 8, 8} : new byte[]{32, 8}, getCharset(channel).name()));
            } catch (RemotingException e) {
                throw new IOException(StringUtils.toString(e));
```

## RuleId[id=AutoCloseableResource]
### AutoCloseableResource
'IndentingWriter' used without 'try'-with-resources statement
in `dubbo-native/src/main/java/org/apache/dubbo/aot/generate/BasicJsonWriter.java`
#### Snippet
```java
         */
        public IndentingWriter indented(Runnable runnable) {
            indent();
            runnable.run();
            return outdent();
```

### AutoCloseableResource
'ReferenceCountedClient' used without 'try'-with-resources statement
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/RestProtocol.java`
#### Snippet
```java
            ReferenceCountedClient<?> referenceCountedClient = clients.get(url.getAddress());
            if (referenceCountedClient != null && referenceCountedClient.release()) {
                clients.remove(url.getAddress());
            }
        } catch (Exception e) {
```

### AutoCloseableResource
'RandomAccessFile' used without 'try'-with-resources statement
in `dubbo-common/src/main/java/org/apache/dubbo/common/cache/FileCacheStoreFactory.java`
#### Snippet
```java
                throw new AssertionError("Failed to create lock file " + lockFile);
            }
            FileChannel lockFileChannel = new RandomAccessFile(lockFile, "rw").getChannel();
            dirLock = lockFileChannel.tryLock();
        } catch (OverlappingFileLockException ofle) {
```

### AutoCloseableResource
'InputStream' used without 'try'-with-resources statement
in `dubbo-native-plugin/src/main/java/org/apache/dubbo/maven/plugin/DubboNativeCodeGeneratorMojo.java`
#### Snippet
```java

        Arrays.stream(nativeFiles).forEach(nativeFile -> {
            InputStream is = DubboNativeCodeGeneratorMojo.class.getClassLoader().getResourceAsStream(nativeFile);
            project.getResources().stream().findFirst().ifPresent(resource -> {
                String directory = resource.getDirectory();
```

### AutoCloseableResource
'ValidatorFactory' used without 'try'-with-resources statement
in `dubbo-filter/dubbo-filter-validation/src/main/java/org/apache/dubbo/validation/support/jvalidation/JValidatorNew.java`
#### Snippet
```java
        ValidatorFactory factory;
        if (jvalidation != null && jvalidation.length() > 0) {
            factory = Validation.byProvider((Class) ReflectUtils.forName(jvalidation)).configure().buildValidatorFactory();
        } else {
            factory = Validation.buildDefaultValidatorFactory();
```

### AutoCloseableResource
'ValidatorFactory' used without 'try'-with-resources statement
in `dubbo-filter/dubbo-filter-validation/src/main/java/org/apache/dubbo/validation/support/jvalidation/JValidatorNew.java`
#### Snippet
```java
            factory = Validation.byProvider((Class) ReflectUtils.forName(jvalidation)).configure().buildValidatorFactory();
        } else {
            factory = Validation.buildDefaultValidatorFactory();
        }
        this.validator = factory.getValidator();
```

### AutoCloseableResource
'Unloaded' used without 'try'-with-resources statement
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/proxy/bytebuddy/ByteBuddyProxy.java`
#### Snippet
```java
            .method(methodMatcher)
            .intercept(MethodDelegation.to(new ByteBuddyInterceptor(handler)))
            .make()
            .load(cl)
            .getLoaded();
```

### AutoCloseableResource
'JvmGcMetrics' used without 'try'-with-resources statement
in `dubbo-metrics/dubbo-metrics-default/src/main/java/org/apache/dubbo/metrics/report/AbstractMetricsReporter.java`
#### Snippet
```java
            // Do not change JvmGcMetrics to try-with-resources as the JvmGcMetrics will not be available after (auto-)closing.
            // See https://github.com/micrometer-metrics/micrometer/issues/1492
            JvmGcMetrics jvmGcMetrics = new JvmGcMetrics(extraTags);
            jvmGcMetrics.bindTo(compositeRegistry);
            Runtime.getRuntime().addShutdownHook(new Thread(jvmGcMetrics::close));
```

### AutoCloseableResource
'FileInputStream' used without 'try'-with-resources statement
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/container/page/pages/LogPageHandler.java`
#### Snippet
```java
        if (file != null && file.exists()) {
            try {
                FileInputStream fis = new FileInputStream(file);
                FileChannel channel = fis.getChannel();
                size = channel.size();
```

### AutoCloseableResource
'SharedIndexInformer' used without 'try'-with-resources statement
in `dubbo-kubernetes/src/main/java/org/apache/dubbo/registry/kubernetes/KubernetesServiceDiscovery.java`
#### Snippet
```java
                                if (PODS_INFORMER.containsKey(serviceName)) {
                                    PODS_INFORMER.get(serviceName).close();
                                    PODS_INFORMER.remove(serviceName);
                                }
                                watchPods(listener, serviceName);
```

### AutoCloseableResource
'Http2ConnectionEncoder' used without 'try'-with-resources statement
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/TripleHttp2Protocol.java`
#### Snippet
```java
            .frameLogger(CLIENT_LOGGER)
            .build();
        codec.connection().local().flowController().frameWriter(codec.encoder().frameWriter());
        final Http2MultiplexHandler handler = new Http2MultiplexHandler(
            new TripleClientHandler(frameworkModel));
```

### AutoCloseableResource
'Http2ConnectionEncoder' used without 'try'-with-resources statement
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/TripleHttp2Protocol.java`
#### Snippet
```java
            .build();
        ExecutorSupport executorSupport = ExecutorRepository.getInstance(url.getOrDefaultApplicationModel()).getExecutorSupport(url);
        codec.connection().local().flowController().frameWriter(codec.encoder().frameWriter());
        TripleWriteQueue writeQueue = new TripleWriteQueue();
        final Http2MultiplexHandler handler = new Http2MultiplexHandler(
```

### AutoCloseableResource
'ByteArrayOutputStream' used without 'try'-with-resources statement
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/compressor/Bzip2.java`
#### Snippet
```java
        }

        ByteArrayOutputStream out = new ByteArrayOutputStream();
        ByteArrayInputStream in = new ByteArrayInputStream(payloadByteArr);
        try {
```

## RuleId[id=RedundantCast]
### RedundantCast
Casting `request` to `HttpContent` is redundant
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/request/NettyRequestFacade.java`
#### Snippet
```java

    protected void parseBody() {
        ByteBuf byteBuf = ((HttpContent) request).content();

        if (byteBuf.readableBytes() > 0) {
```

### RedundantCast
Casting `null` to `MetricsKey` is redundant
in `dubbo-metrics/dubbo-metrics-registry/src/main/java/org/apache/dubbo/metrics/registry/event/RegistryEvent.java`
#### Snippet
```java

    public static RegistryEvent toNotifyEvent(ApplicationModel applicationModel) {
        return new RegistryEvent(applicationModel, new TypeWrapper(MetricsLevel.APP, MetricsKey.NOTIFY_METRIC_REQUESTS, MetricsKey.NOTIFY_METRIC_NUM_LAST, (MetricsKey) null)) {
            @Override
            public void customAfterPost(Object postResult) {
```

### RedundantCast
Casting `getCause()` to `GenericException` is redundant
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/service/GenericException.java`
#### Snippet
```java
    public StackTraceElement[] getStackTrace() {
        if(this.useCause) {
            return ((GenericException)getCause()).getStackTrace();
        }
        return super.getStackTrace();
```

### RedundantCast
Casting `null` to `Map` is redundant
in `dubbo-common/src/main/java/org/apache/dubbo/common/URL.java`
#### Snippet
```java
            }
        }
        return newMap.isEmpty() ? new ServiceConfigURL(url.getProtocol(), url.getUsername(), url.getPassword(), url.getHost(), url.getPort(), url.getPath(), (Map<String, String>) null, url.getAttributes())
            : new ServiceConfigURL(url.getProtocol(), url.getUsername(), url.getPassword(), url.getHost(), url.getPort(), url.getPath(), newMap, url.getAttributes());
    }
```

### RedundantCast
Casting `get()` to `long` is redundant
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/AtomicPositiveInteger.java`
#### Snippet
```java
    @Override
    public long longValue() {
        return (long) get();
    }

```

### RedundantCast
Casting `get()` to `double` is redundant
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/AtomicPositiveInteger.java`
#### Snippet
```java
    @Override
    public double doubleValue() {
        return (double) get();
    }

```

### RedundantCast
Casting `PRIMITIVE_TYPE_NAME_MAP.get(...)` to `Class` is redundant
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ClassUtils.java`
#### Snippet
```java
        if (name != null && name.length() <= 8) {
            // Could be a primitive - likely.
            result = (Class<?>) PRIMITIVE_TYPE_NAME_MAP.get(name);
        }
        return result;
```

### RedundantCast
Casting `result` to `Collection` is redundant
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/StringUtils.java`
#### Snippet
```java
            }

            return toStringArray((Collection) result);
        }
    }
```

### RedundantCast
Casting `collection.toArray(...)` to `String[]` is redundant
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/StringUtils.java`
#### Snippet
```java

    public static String[] toStringArray(Collection<String> collection) {
        return (String[]) collection.toArray(new String[0]);
    }

```

### RedundantCast
Casting `null` to `String` is redundant
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/StringUtils.java`
#### Snippet
```java

    public static String[] delimitedListToStringArray(String str, String delimiter) {
        return delimitedListToStringArray(str, delimiter, (String) null);
    }

```

### RedundantCast
Casting `activate` to `Activate` is redundant
in `dubbo-common/src/main/java/org/apache/dubbo/common/extension/ExtensionLoader.java`
#### Snippet
```java

        if (activate instanceof Activate) {
            onClass = ((Activate) activate).onClass();
        } else if (activate instanceof com.alibaba.dubbo.common.extension.Activate) {
            onClass = ((com.alibaba.dubbo.common.extension.Activate) activate).onClass();
```

### RedundantCast
Casting `annotatedClass` to `TypeElement` is redundant
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/util/ServiceAnnotationUtils.java`
#### Snippet
```java
        if (interfaceClass == null) {
            // last, get the interface class from first one
            interfaceClass = ((TypeElement) annotatedClass).getInterfaces().get(0);
        }

```

### RedundantCast
Casting `getAttribute(...)` to `T` is redundant
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/util/AnnotationUtils.java`
#### Snippet
```java

    static <T> T getValue(AnnotationMirror annotation) {
        return (T) getAttribute(annotation, "value");
    }
}
```

### RedundantCast
Casting `value` to `Object` is redundant
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcContextAttachment.java`
#### Snippet
```java
    @Override
    public RpcContextAttachment setAttachment(String key, String value) {
        return setObjectAttachment(key, (Object) value);
    }

```

### RedundantCast
Casting `getContext().getFuture()` to `Future` is redundant
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/RpcContext.java`
#### Snippet
```java
            };
        }
        return ((Future<T>) getContext().getFuture());
    }

```

### RedundantCast
Casting `attachments.get(...)` to `String` is redundant
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/RpcInvocation.java`
#### Snippet
```java
            return defaultValue;
        }
        String value = (String) attachments.get(key);
        if (value == null || value.length() == 0) {
            return defaultValue;
```

### RedundantCast
Casting `attachments.get(...)` to `String` is redundant
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/RpcInvocation.java`
#### Snippet
```java
            return null;
        }
        return (String) attachments.get(key);
    }

```

### RedundantCast
Casting `appResponse.getValue()` to `V` is redundant
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/protocol/dubbo/FutureAdapter.java`
#### Snippet
```java
                        callback.caught(appResponse.getException());
                    } else {
                        callback.done((V) appResponse.getValue());
                    }
                }
```

### RedundantCast
Casting `null` to `String` is redundant
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/container/page/Page.java`
#### Snippet
```java

    public Page(String navigation) {
        this(navigation, (String) null, (String[]) null, (List<List<String>>) null);
    }

```

### RedundantCast
Casting `null` to `List`> is redundant
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/container/page/Page.java`
#### Snippet
```java

    public Page(String navigation) {
        this(navigation, (String) null, (String[]) null, (List<List<String>>) null);
    }

```

### RedundantCast
Casting `stream.getProperty(...)` to `FlowState` is redundant
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/TriHttp2RemoteFlowController.java`
#### Snippet
```java

    private FlowState state(Http2Stream stream) {
        return (FlowState) stream.getProperty(stateKey);
    }

```

## RuleId[id=JavadocDeclaration]
### JavadocDeclaration
Wrong tag `Consumers`
in `dubbo-demo/dubbo-demo-interface/src/main/java/org/apache/dubbo/demo/rest/api/JaxRsRestDemoService.java`
#### Snippet
```java
/**
 *
 * @Consumers & @Produces can be not used ,we will make sure the content-type of request by arg type
 *  but the Request method  is forbidden disappear
 *  parameters which annotation are not present , it is from the body (jaxrs anntation is diffrent from spring web from param(only request param can  ignore anntation))
```

### JavadocDeclaration
`@param url` tag description is missing
in `dubbo-monitor/dubbo-monitor-api/src/main/java/org/apache/dubbo/monitor/MonitorFactory.java`
#### Snippet
```java
     * Create monitor.
     *
     * @param url
     * @return monitor
     */
```

### JavadocDeclaration
`@param query` tag description is missing
in `dubbo-monitor/dubbo-monitor-api/src/main/java/org/apache/dubbo/monitor/MonitorService.java`
#### Snippet
```java
     * 1.4 date=2012-07-03: specify the date to collect the data, today is the default value
     *
     * @param query
     * @return statistics
     */
```

### JavadocDeclaration
`@param statistics` tag description is missing
in `dubbo-monitor/dubbo-monitor-api/src/main/java/org/apache/dubbo/monitor/MonitorService.java`
#### Snippet
```java
     * 1.3 success,failure,elapsed: record success count, failure count, and total cost for success invocations, average cost (total cost/success calls)
     *
     * @param statistics
     */
    void collect(URL statistics);
```

### JavadocDeclaration
`@param sc` tag description is missing
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/ServiceListener.java`
#### Snippet
```java
    /**
     * Callback when ServiceConfig is unexported
     * @param sc
     */
    void unexported(ServiceConfig sc);
```

### JavadocDeclaration
`@param sc` tag description is missing
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/ServiceListener.java`
#### Snippet
```java
    /**
     * Callback when ServiceConfig is exported
     * @param sc
     */
    void exported(ServiceConfig sc);
```

### JavadocDeclaration
`@param invoker` tag description is missing
in `dubbo-monitor/dubbo-monitor-api/src/main/java/org/apache/dubbo/monitor/support/MonitorFilter.java`
#### Snippet
```java
     * Create statistics url
     *
     * @param invoker
     * @param invocation
     * @param result
```

### JavadocDeclaration
`@param invocation` tag description is missing
in `dubbo-monitor/dubbo-monitor-api/src/main/java/org/apache/dubbo/monitor/support/MonitorFilter.java`
#### Snippet
```java
     *
     * @param invoker
     * @param invocation
     * @param result
     * @param remoteHost
```

### JavadocDeclaration
`@param result` tag description is missing
in `dubbo-monitor/dubbo-monitor-api/src/main/java/org/apache/dubbo/monitor/support/MonitorFilter.java`
#### Snippet
```java
     * @param invoker
     * @param invocation
     * @param result
     * @param remoteHost
     * @param start
```

### JavadocDeclaration
`@param remoteHost` tag description is missing
in `dubbo-monitor/dubbo-monitor-api/src/main/java/org/apache/dubbo/monitor/support/MonitorFilter.java`
#### Snippet
```java
     * @param invocation
     * @param result
     * @param remoteHost
     * @param start
     * @param error
```

### JavadocDeclaration
`@param start` tag description is missing
in `dubbo-monitor/dubbo-monitor-api/src/main/java/org/apache/dubbo/monitor/support/MonitorFilter.java`
#### Snippet
```java
     * @param result
     * @param remoteHost
     * @param start
     * @param error
     * @return
```

### JavadocDeclaration
`@param error` tag description is missing
in `dubbo-monitor/dubbo-monitor-api/src/main/java/org/apache/dubbo/monitor/support/MonitorFilter.java`
#### Snippet
```java
     * @param remoteHost
     * @param start
     * @param error
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-monitor/dubbo-monitor-api/src/main/java/org/apache/dubbo/monitor/support/MonitorFilter.java`
#### Snippet
```java
     * @param start
     * @param error
     * @return
     */
    private URL createStatisticsUrl(Invoker<?> invoker, Invocation invocation, Result result, String remoteHost, long start, boolean error) {
```

### JavadocDeclaration
`@param invoker` tag description is missing
in `dubbo-monitor/dubbo-monitor-api/src/main/java/org/apache/dubbo/monitor/support/MonitorFilter.java`
#### Snippet
```java
     * The collector logic, it will be handled by the default monitor
     *
     * @param invoker
     * @param invocation
     * @param result     the invocation result
```

### JavadocDeclaration
`@param invocation` tag description is missing
in `dubbo-monitor/dubbo-monitor-api/src/main/java/org/apache/dubbo/monitor/support/MonitorFilter.java`
#### Snippet
```java
     *
     * @param invoker
     * @param invocation
     * @param result     the invocation result
     * @param remoteHost the remote host address
```

### JavadocDeclaration
`@param invoker` tag description is missing
in `dubbo-monitor/dubbo-monitor-api/src/main/java/org/apache/dubbo/monitor/support/MonitorFilter.java`
#### Snippet
```java
     * concurrent counter
     *
     * @param invoker
     * @param invocation
     * @return
```

### JavadocDeclaration
`@param invocation` tag description is missing
in `dubbo-monitor/dubbo-monitor-api/src/main/java/org/apache/dubbo/monitor/support/MonitorFilter.java`
#### Snippet
```java
     *
     * @param invoker
     * @param invocation
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-monitor/dubbo-monitor-api/src/main/java/org/apache/dubbo/monitor/support/MonitorFilter.java`
#### Snippet
```java
     * @param invoker
     * @param invocation
     * @return
     */
    private AtomicInteger getConcurrent(Invoker<?> invoker, Invocation invocation) {
```

### JavadocDeclaration
`@throws` tag description is missing
in `dubbo-monitor/dubbo-monitor-api/src/main/java/org/apache/dubbo/monitor/support/MonitorFilter.java`
#### Snippet
```java
     * @param invocation invocation.
     * @return {@link Result} the invoke result
     * @throws RpcException
     */
    @Override
```

### JavadocDeclaration
Wrong tag `scene`
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/bootstrap/DubboBootstrapStartStopListener.java`
#### Snippet
```java
 * call on DubboBootstrap start or stop.
 *
 * @scene 2.7.9
 * @see DubboBootstrap
 */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/bootstrap/builders/InternalServiceConfigBuilder.java`
#### Snippet
```java
     * Get other configured protocol from environment in priority order. If get nothing, use default dubbo.
     *
     * @return
     */
    private String getRelatedOrDefaultProtocol() {
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/bootstrap/DubboBootstrap.java`
#### Snippet
```java
     * Stop dubbo application
     *
     * @return
     * @throws IllegalStateException
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/bootstrap/DubboBootstrap.java`
#### Snippet
```java
     *
     * @return
     * @throws IllegalStateException
     */
    public DubboBootstrap stop() throws IllegalStateException {
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/bootstrap/DubboBootstrap.java`
#### Snippet
```java
     *
     * @param wait If true, wait for startup to complete, or else no waiting.
     * @return
     */
    public DubboBootstrap start(boolean wait) {
```

### JavadocDeclaration
`@param injvm` tag description is missing
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/bootstrap/builders/AbstractReferenceBuilder.java`
#### Snippet
```java

    /**
     * @param injvm
     * @see AbstractInterfaceBuilder#scope(String)
     * @deprecated instead, use the parameter <b>scope</b> to judge if it's in jvm, scope=local
```

### JavadocDeclaration
`@param protocolConfig` tag description is missing
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/ServiceConfig.java`
#### Snippet
```java
     * /etc/hosts -> default network address -> first available network address
     *
     * @param protocolConfig
     * @param map
     * @return
```

### JavadocDeclaration
`@param map` tag description is missing
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/ServiceConfig.java`
#### Snippet
```java
     *
     * @param protocolConfig
     * @param map
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/ServiceConfig.java`
#### Snippet
```java
     * @param protocolConfig
     * @param map
     * @return
     */
    private static String findConfiguredHosts(ProtocolConfig protocolConfig,
```

### JavadocDeclaration
`@param protocolConfig` tag description is missing
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/ServiceConfig.java`
#### Snippet
```java
     * -> protocol default port
     *
     * @param protocolConfig
     * @param name
     * @return
```

### JavadocDeclaration
`@param name` tag description is missing
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/ServiceConfig.java`
#### Snippet
```java
     *
     * @param protocolConfig
     * @param name
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/ServiceConfig.java`
#### Snippet
```java
     * @param protocolConfig
     * @param name
     * @return
     */
    private static synchronized Integer findConfiguredPort(ProtocolConfig protocolConfig,
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/ServiceConfig.java`
#### Snippet
```java
     * Determine if it is injvm
     *
     * @return
     */
    private boolean isOnlyInJvm() {
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/deploy/DefaultApplicationDeployer.java`
#### Snippet
```java
     * @param centerType           the type name of center
     * @param extensionClass       an extension class of a center infrastructure
     * @return
     * @since 2.7.8
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/deploy/DefaultApplicationDeployer.java`
#### Snippet
```java
     * Start the bootstrap
     *
     * @return
     */
    @Override
```

### JavadocDeclaration
`@param local` tag description is missing
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/bootstrap/builders/AbstractInterfaceBuilder.java`
#### Snippet
```java

    /**
     * @param local
     * @see AbstractInterfaceBuilder#stub(String)
     * @deprecated Replace to <code>stub(String)</code>
```

### JavadocDeclaration
`@param local` tag description is missing
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/bootstrap/builders/AbstractInterfaceBuilder.java`
#### Snippet
```java

    /**
     * @param local
     * @see AbstractInterfaceBuilder#stub(Boolean)
     * @deprecated Replace to <code>stub(Boolean)</code>
```

### JavadocDeclaration
`@param dispather` tag description is missing
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/bootstrap/builders/ProtocolBuilder.java`
#### Snippet
```java

    /**
     * @param dispather
     * @return ProtocolBuilder
     * @see ProtocolBuilder#dispatcher(String)
```

### JavadocDeclaration
`@param path` tag description is missing
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/bootstrap/builders/ProtocolBuilder.java`
#### Snippet
```java

    /**
     * @param path
     * @return ProtocolBuilder
     * @see ProtocolBuilder#contextpath(String)
```

### JavadocDeclaration
`@param transport` tag description is missing
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/bootstrap/builders/RegistryBuilder.java`
#### Snippet
```java

    /**
     * @param transport
     * @see #transporter(String)
     * @deprecated
```

### JavadocDeclaration
`@param wait` tag description is missing
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/bootstrap/builders/RegistryBuilder.java`
#### Snippet
```java

    /**
     * @param wait
     * @see ProviderBuilder#wait(Integer)
     * @deprecated
```

### JavadocDeclaration
`@param applicationContext` tag description is missing
in `dubbo-spring-boot/dubbo-spring-boot-compatible/autoconfigure/src/main/java/org/apache/dubbo/spring/boot/context/event/AwaitingNonWebApplicationListener.java`
#### Snippet
```java

    /**
     * @param applicationContext
     * @since 2.7.8
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/ReferenceConfig.java`
#### Snippet
```java
     * just for test
     *
     * @return
     */
    @Deprecated
```

### JavadocDeclaration
`@throws` tag description is missing
in `dubbo-remoting/dubbo-remoting-netty4/src/main/java/org/apache/dubbo/remoting/transport/netty4/NettyServer.java`
#### Snippet
```java
     * Init and start netty server
     *
     * @throws Throwable
     */
    @Override
```

### JavadocDeclaration
`@param url` tag description is missing
in `dubbo-remoting/dubbo-remoting-netty4/src/main/java/org/apache/dubbo/remoting/transport/netty4/NettyChannel.java`
#### Snippet
```java
     *
     * @param ch      netty channel
     * @param url
     * @param handler dubbo handler that contain netty's handler
     * @return
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-remoting/dubbo-remoting-netty4/src/main/java/org/apache/dubbo/remoting/transport/netty4/NettyChannel.java`
#### Snippet
```java
     * @param url
     * @param handler dubbo handler that contain netty's handler
     * @return
     */
    static NettyChannel getOrAddChannel(Channel ch, URL url, ChannelHandler handler) {
```

### JavadocDeclaration
`@param url` tag description is missing
in `dubbo-remoting/dubbo-remoting-netty4/src/main/java/org/apache/dubbo/remoting/transport/netty4/NettyChannel.java`
#### Snippet
```java
     *
     * @param channel netty channel
     * @param url
     * @param handler dubbo handler that contain netty handler
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `dubbo-remoting/dubbo-remoting-netty4/src/main/java/org/apache/dubbo/remoting/transport/netty4/NettyClient.java`
#### Snippet
```java
     * Init bootstrap
     *
     * @throws Throwable
     */
    @Override
```

### JavadocDeclaration
Javadoc pointing to itself
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/registrycenter/Config.java`
#### Snippet
```java
     * <h3>How to use</h3>
     * <pre>
     * System.getProperty({@link #getConnectionAddressKey()})
     * </pre>
     */
```

### JavadocDeclaration
Javadoc pointing to itself
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/registrycenter/Config.java`
#### Snippet
```java
     * <h3>How to use</h3>
     * <pre>
     * System.getProperty({@link #getConnectionAddressKey2()})
     * </pre>
     */
```

### JavadocDeclaration
Javadoc pointing to itself
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/registrycenter/Config.java`
#### Snippet
```java
     * <h3>How to use</h3>
     * <pre>
     * System.getProperty({@link #getConnectionAddressKey1()})
     * </pre>
     */
```

### JavadocDeclaration
Javadoc pointing to itself
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/registrycenter/config/ZookeeperRegistryCenterConfig.java`
#### Snippet
```java
     * <h3>How to use</h3>
     * <pre>
     * System.getProperty({@link ZookeeperRegistryCenterConfig#getConnectionAddressKey2()})
     * </pre>
     */
```

### JavadocDeclaration
Javadoc pointing to itself
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/registrycenter/config/ZookeeperRegistryCenterConfig.java`
#### Snippet
```java
     * <h3>How to use</h3>
     * <pre>
     * System.getProperty({@link ZookeeperRegistryCenterConfig#getConnectionAddressKey1()})
     * </pre>
     */
```

### JavadocDeclaration
Javadoc pointing to itself
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/registrycenter/config/ZookeeperRegistryCenterConfig.java`
#### Snippet
```java
     * <h3>How to use</h3>
     * <pre>
     * System.getProperty({@link ZookeeperRegistryCenterConfig#getConnectionAddressKey()})
     * </pre>
     */
```

### JavadocDeclaration
Javadoc pointing to itself
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/registrycenter/ZookeeperRegistryCenter.java`
#### Snippet
```java

    /**
     * The {@link #INITIALIZED} for flagging the {@link #startup()} method is called or not.
     */
    private static final AtomicBoolean INITIALIZED = new AtomicBoolean(false);
```

### JavadocDeclaration
Javadoc pointing to itself
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/registrycenter/initializer/ZookeeperInitializer.java`
#### Snippet
```java

    /**
     * The {@link #INITIALIZED} for checking the {@link #initialize(Context)} method is called or not.
     */
    private final AtomicBoolean INITIALIZED = new AtomicBoolean(false);
```

### JavadocDeclaration
Tag `return` is not allowed here
in `dubbo-xds/src/main/java/org/apache/dubbo/registry/xds/util/protocol/XdsProtocol.java`
#### Snippet
```java
     * @param resourceNames specified resource name
     * @param consumer      resource notifier, will be called when resource updated
     * @return requestId, used when resourceNames update with {@link XdsProtocol#updateObserve(long, Set)}
     */
    void observeResource(Set<String> resourceNames, Consumer<Map<String, T>> consumer, boolean isReConnect);
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/MetadataService.java`
#### Snippet
```java
     * Interface definition.
     *
     * @return
     */
    default String getServiceDefinition(String interfaceName, String version, String group) {
```

### JavadocDeclaration
`@param classes` tag description is missing
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/rest/ParamType.java`
#### Snippet
```java
     * exclude null types
     *
     * @param classes
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/rest/ParamType.java`
#### Snippet
```java
     *
     * @param classes
     * @return
     */
    private static List<Class> addSupportTypes(Class... classes) {
```

### JavadocDeclaration
`@param rawPath` tag description is missing
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/rest/PathUtil.java`
#### Snippet
```java
     * generate real path from  rawPath according to argInfo and method args
     *
     * @param rawPath
     * @param argInfos
     * @param args
```

### JavadocDeclaration
`@param argInfos` tag description is missing
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/rest/PathUtil.java`
#### Snippet
```java
     *
     * @param rawPath
     * @param argInfos
     * @param args
     * @return
```

### JavadocDeclaration
`@param args` tag description is missing
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/rest/PathUtil.java`
#### Snippet
```java
     * @param rawPath
     * @param argInfos
     * @param args
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/rest/PathUtil.java`
#### Snippet
```java
     * @param argInfos
     * @param args
     * @return
     */
    public static String resolvePathVariable(String rawPath, List<ArgInfo> argInfos, List<Object> args) {
```

### JavadocDeclaration
`@param rawPath` tag description is missing
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/rest/PathUtil.java`
#### Snippet
```java
     * parse pathVariable index from url by annotation info
     *
     * @param rawPath
     * @param argInfos
     */
```

### JavadocDeclaration
`@param argInfos` tag description is missing
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/rest/PathUtil.java`
#### Snippet
```java
     *
     * @param rawPath
     * @param argInfos
     */
    public static void setArgInfoSplitIndex(String rawPath, List<ArgInfo> argInfos) {
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/AbstractServiceNameMapping.java`
#### Snippet
```java
     * Get the service names from the specified Dubbo service interface, group, version and protocol
     *
     * @return
     */
    abstract public Set<String> getAndListen(URL url, MappingListener mappingListener);
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/AbstractServiceNameMapping.java`
#### Snippet
```java
     * Get the service names from the specified Dubbo service interface, group, version and protocol
     *
     * @return
     */
    abstract public Set<String> get(URL url);
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/rest/ServiceRestMetadataResolver.java`
#### Snippet
```java
     *
     * @param serviceType Dubbo Service interface or type
     * @return
     */
    ServiceRestMetadata resolve(Class<?> serviceType);
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-remoting/dubbo-remoting-zookeeper-curator5/src/main/java/org/apache/dubbo/remoting/zookeeper/curator5/Curator5ZookeeperClient.java`
#### Snippet
```java
     * just for unit test
     *
     * @return
     */
    CuratorFramework getClient() {
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/report/identifier/KeyTypeEnum.java`
#### Snippet
```java
     * @param one    one
     * @param others the others
     * @return
     * @since 2.7.8
     */
```

### JavadocDeclaration
`@param metadataMap` tag description is missing
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/PathAndInvokerMapper.java`
#### Snippet
```java
     * deploy path metadata
     *
     * @param metadataMap
     * @param invoker
     */
```

### JavadocDeclaration
`@param invoker` tag description is missing
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/PathAndInvokerMapper.java`
#### Snippet
```java
     *
     * @param metadataMap
     * @param invoker
     */
    public void addPathAndInvoker(Map<PathMatcher, RestMethodMetadata> metadataMap, Invoker invoker) {
```

### JavadocDeclaration
`@param path` tag description is missing
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/PathAndInvokerMapper.java`
#### Snippet
```java
     * acquire metadata & invoker by service info
     *
     * @param path
     * @param version
     * @param group
```

### JavadocDeclaration
`@param version` tag description is missing
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/PathAndInvokerMapper.java`
#### Snippet
```java
     *
     * @param path
     * @param version
     * @param group
     * @param port
```

### JavadocDeclaration
`@param group` tag description is missing
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/PathAndInvokerMapper.java`
#### Snippet
```java
     * @param path
     * @param version
     * @param group
     * @param port
     * @return
```

### JavadocDeclaration
`@param port` tag description is missing
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/PathAndInvokerMapper.java`
#### Snippet
```java
     * @param version
     * @param group
     * @param port
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/PathAndInvokerMapper.java`
#### Snippet
```java
     * @param group
     * @param port
     * @return
     */
    public InvokerAndRestMethodMetadataPair getRestMethodMetadata(String path, String version, String group, Integer port,String method) {
```

### JavadocDeclaration
`@param pathMatcher` tag description is missing
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/PathAndInvokerMapper.java`
#### Snippet
```java
     * undeploy path metadata
     *
     * @param pathMatcher
     */
    public void removePath(PathMatcher pathMatcher) {
```

### JavadocDeclaration
`@param invocation` tag description is missing
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/RestInvoker.java`
#### Snippet
```java
     * create intercept context
     *
     * @param invocation
     * @param serviceRestMetadata
     * @param restMethodMetadata
```

### JavadocDeclaration
`@param serviceRestMetadata` tag description is missing
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/RestInvoker.java`
#### Snippet
```java
     *
     * @param invocation
     * @param serviceRestMetadata
     * @param restMethodMetadata
     * @param requestTemplate
```

### JavadocDeclaration
`@param restMethodMetadata` tag description is missing
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/RestInvoker.java`
#### Snippet
```java
     * @param invocation
     * @param serviceRestMetadata
     * @param restMethodMetadata
     * @param requestTemplate
     * @return
```

### JavadocDeclaration
`@param requestTemplate` tag description is missing
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/RestInvoker.java`
#### Snippet
```java
     * @param serviceRestMetadata
     * @param restMethodMetadata
     * @param requestTemplate
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/RestInvoker.java`
#### Snippet
```java
     * @param restMethodMetadata
     * @param requestTemplate
     * @return
     */
    private HttpConnectionCreateContext creatHttpConnectionCreateContext(Invocation invocation, ServiceRestMetadata serviceRestMetadata, RestMethodMetadata restMethodMetadata, RequestTemplate requestTemplate) {
```

### JavadocDeclaration
`@param methodName` tag description is missing
in `dubbo-metadata/dubbo-metadata-definition-protobuf/src/main/java/org/apache/dubbo/metadata/definition/protobuf/ProtobufTypeBuilder.java`
#### Snippet
```java
     * ex： getXXXList()<br/>
     *
     * @param methodName
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-metadata/dubbo-metadata-definition-protobuf/src/main/java/org/apache/dubbo/metadata/definition/protobuf/ProtobufTypeBuilder.java`
#### Snippet
```java
     *
     * @param methodName
     * @return
     */
    private String generateListFieldName(String methodName) {
```

### JavadocDeclaration
`@param fieldName` tag description is missing
in `dubbo-metadata/dubbo-metadata-definition-protobuf/src/main/java/org/apache/dubbo/metadata/definition/protobuf/ProtobufTypeBuilder.java`
#### Snippet
```java
     * Bytes is a primitive type in Proto, transform to ByteString.class in java<br/>
     *
     * @param fieldName
     * @param typeName
     * @return
```

### JavadocDeclaration
`@param typeName` tag description is missing
in `dubbo-metadata/dubbo-metadata-definition-protobuf/src/main/java/org/apache/dubbo/metadata/definition/protobuf/ProtobufTypeBuilder.java`
#### Snippet
```java
     *
     * @param fieldName
     * @param typeName
     * @return
     */
```

### JavadocDeclaration
Tag `return` is not allowed here
in `dubbo-metadata/dubbo-metadata-definition-protobuf/src/main/java/org/apache/dubbo/metadata/definition/protobuf/ProtobufTypeBuilder.java`
#### Snippet
```java
     * @param fieldName
     * @param typeName
     * @return
     */
    private void validateMapType(String fieldName, String typeName) {
```

### JavadocDeclaration
`@param methodName` tag description is missing
in `dubbo-metadata/dubbo-metadata-definition-protobuf/src/main/java/org/apache/dubbo/metadata/definition/protobuf/ProtobufTypeBuilder.java`
#### Snippet
```java
     * ex: putAllXXX();<br/>
     *
     * @param methodName
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-metadata/dubbo-metadata-definition-protobuf/src/main/java/org/apache/dubbo/metadata/definition/protobuf/ProtobufTypeBuilder.java`
#### Snippet
```java
     *
     * @param methodName
     * @return
     */
    private String generateMapFieldName(String methodName) {
```

### JavadocDeclaration
`@param methodTemp` tag description is missing
in `dubbo-metadata/dubbo-metadata-definition-protobuf/src/main/java/org/apache/dubbo/metadata/definition/protobuf/ProtobufTypeBuilder.java`
#### Snippet
```java
     * generated setting method: putAllCards(java.util.Map<String, string> values) </br>
     *
     * @param methodTemp
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-metadata/dubbo-metadata-definition-protobuf/src/main/java/org/apache/dubbo/metadata/definition/protobuf/ProtobufTypeBuilder.java`
#### Snippet
```java
     *
     * @param methodTemp
     * @return
     */
    private boolean isMapPropertySettingMethod(Method methodTemp) {
```

### JavadocDeclaration
`@param method` tag description is missing
in `dubbo-metadata/dubbo-metadata-definition-protobuf/src/main/java/org/apache/dubbo/metadata/definition/protobuf/ProtobufTypeBuilder.java`
#### Snippet
```java
     * generated getting method：List<String> getNamesList()
     *
     * @param method
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-metadata/dubbo-metadata-definition-protobuf/src/main/java/org/apache/dubbo/metadata/definition/protobuf/ProtobufTypeBuilder.java`
#### Snippet
```java
     *
     * @param method
     * @return
     */
    boolean isListPropertyGettingMethod(Method method) {
```

### JavadocDeclaration
`@param methodName` tag description is missing
in `dubbo-metadata/dubbo-metadata-definition-protobuf/src/main/java/org/apache/dubbo/metadata/definition/protobuf/ProtobufTypeBuilder.java`
#### Snippet
```java
     * ex:setXXX();<br/>
     *
     * @param methodName
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-metadata/dubbo-metadata-definition-protobuf/src/main/java/org/apache/dubbo/metadata/definition/protobuf/ProtobufTypeBuilder.java`
#### Snippet
```java
     *
     * @param methodName
     * @return
     */
    private String generateSimpleFiledName(String methodName) {
```

### JavadocDeclaration
`@param method` tag description is missing
in `dubbo-metadata/dubbo-metadata-definition-protobuf/src/main/java/org/apache/dubbo/metadata/definition/protobuf/ProtobufTypeBuilder.java`
#### Snippet
```java
     * generated setting method ex: setNameValue(String name);
     *
     * @param method
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-metadata/dubbo-metadata-definition-protobuf/src/main/java/org/apache/dubbo/metadata/definition/protobuf/ProtobufTypeBuilder.java`
#### Snippet
```java
     *
     * @param method
     * @return
     */
    private boolean isSimplePropertySettingMethod(Method method) {
```

### JavadocDeclaration
`@param request` tag description is missing
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/RestRPCInvocationUtil.java`
#### Snippet
```java
     * build RpcInvocation
     *
     * @param request
     * @param restMethodMetadata
     * @return
```

### JavadocDeclaration
`@param restMethodMetadata` tag description is missing
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/RestRPCInvocationUtil.java`
#### Snippet
```java
     *
     * @param request
     * @param restMethodMetadata
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/RestRPCInvocationUtil.java`
#### Snippet
```java
     * @param request
     * @param restMethodMetadata
     * @return
     */
    public static RpcInvocation createBaseRpcInvocation(RequestFacade request, RestMethodMetadata restMethodMetadata) {
```

### JavadocDeclaration
`@param contentTypes` tag description is missing
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/MediaTypeUtil.java`
#### Snippet
```java
     * return first match , if any multiple content-type  ,acquire mediaType by targetClass type .if contentTypes is empty
     *
     * @param contentTypes
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/MediaTypeUtil.java`
#### Snippet
```java
     *
     * @param contentTypes
     * @return
     */
    public static MediaType convertMediaType(Class<?> targetType, String... contentTypes) {
```

### JavadocDeclaration
`@param request` tag description is missing
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/RestRPCInvocationUtil.java`
#### Snippet
```java
     * create parseMethodArgs context
     *
     * @param request
     * @param servletRequest
     * @param servletResponse
```

### JavadocDeclaration
`@param servletRequest` tag description is missing
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/RestRPCInvocationUtil.java`
#### Snippet
```java
     *
     * @param request
     * @param servletRequest
     * @param servletResponse
     * @param restMethodMetadata
```

### JavadocDeclaration
`@param servletResponse` tag description is missing
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/RestRPCInvocationUtil.java`
#### Snippet
```java
     * @param request
     * @param servletRequest
     * @param servletResponse
     * @param restMethodMetadata
     * @return
```

### JavadocDeclaration
`@param restMethodMetadata` tag description is missing
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/RestRPCInvocationUtil.java`
#### Snippet
```java
     * @param servletRequest
     * @param servletResponse
     * @param restMethodMetadata
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/RestRPCInvocationUtil.java`
#### Snippet
```java
     * @param servletResponse
     * @param restMethodMetadata
     * @return
     */
    private static ProviderParseContext createParseContext(RequestFacade request, Object servletRequest, Object servletResponse, RestMethodMetadata restMethodMetadata) {
```

### JavadocDeclaration
`@param request` tag description is missing
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/RestRPCInvocationUtil.java`
#### Snippet
```java
     * get  path mapping
     *
     * @param request
     * @param pathAndInvokerMapper
     * @return
```

### JavadocDeclaration
`@param pathAndInvokerMapper` tag description is missing
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/RestRPCInvocationUtil.java`
#### Snippet
```java
     *
     * @param request
     * @param pathAndInvokerMapper
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/RestRPCInvocationUtil.java`
#### Snippet
```java
     * @param request
     * @param pathAndInvokerMapper
     * @return
     */
    public static InvokerAndRestMethodMetadataPair getRestMethodMetadata(RequestFacade request, PathAndInvokerMapper pathAndInvokerMapper) {
```

### JavadocDeclaration
`@param rpcInvocation` tag description is missing
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/RestRPCInvocationUtil.java`
#### Snippet
```java
     * service method real args parse
     *
     * @param rpcInvocation
     * @param request
     * @param servletRequest
```

### JavadocDeclaration
`@param request` tag description is missing
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/RestRPCInvocationUtil.java`
#### Snippet
```java
     *
     * @param rpcInvocation
     * @param request
     * @param servletRequest
     * @param servletResponse
```

### JavadocDeclaration
`@param servletRequest` tag description is missing
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/RestRPCInvocationUtil.java`
#### Snippet
```java
     * @param rpcInvocation
     * @param request
     * @param servletRequest
     * @param servletResponse
     * @param restMethodMetadata
```

### JavadocDeclaration
`@param servletResponse` tag description is missing
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/RestRPCInvocationUtil.java`
#### Snippet
```java
     * @param request
     * @param servletRequest
     * @param servletResponse
     * @param restMethodMetadata
     */
```

### JavadocDeclaration
`@param restMethodMetadata` tag description is missing
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/RestRPCInvocationUtil.java`
#### Snippet
```java
     * @param servletRequest
     * @param servletResponse
     * @param restMethodMetadata
     */
    public static void parseMethodArgs(RpcInvocation rpcInvocation, RequestFacade request, Object servletRequest,
```

### JavadocDeclaration
`@param url` tag description is missing
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/NettyHttpRestServer.java`
#### Snippet
```java
     * create child channel options map
     *
     * @param url
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/NettyHttpRestServer.java`
#### Snippet
```java
     *
     * @param url
     * @return
     */
    protected Map<ChannelOption, Object> getChildChannelOptionMap(URL url) {
```

### JavadocDeclaration
`@param url` tag description is missing
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/NettyHttpRestServer.java`
#### Snippet
```java
     * create channel options map
     *
     * @param url
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/NettyHttpRestServer.java`
#### Snippet
```java
     *
     * @param url
     * @return
     */
    protected Map<ChannelOption, Object> getChannelOptionMap(URL url) {
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/NettyHttpRestServer.java`
#### Snippet
```java
     * for triple override
     *
     * @return
     */
    protected NettyServer getNettyServer() {
```

### JavadocDeclaration
`@param url` tag description is missing
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/NettyHttpRestServer.java`
#### Snippet
```java
     * create channel handler
     *
     * @param url
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/NettyHttpRestServer.java`
#### Snippet
```java
     *
     * @param url
     * @return
     */
    protected List<ChannelHandler> getChannelHandlers(URL url) {
```

### JavadocDeclaration
`@param object` tag description is missing
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/DataParseUtils.java`
#### Snippet
```java
     * content-type text
     *
     * @param object
     * @param outputStream
     * @throws IOException
```

### JavadocDeclaration
`@param outputStream` tag description is missing
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/DataParseUtils.java`
#### Snippet
```java
     *
     * @param object
     * @param outputStream
     * @throws IOException
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/DataParseUtils.java`
#### Snippet
```java
     * @param object
     * @param outputStream
     * @throws IOException
     */
    public static void writeTextContent(Object object, OutputStream outputStream) throws IOException {
```

### JavadocDeclaration
`@param object` tag description is missing
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/DataParseUtils.java`
#### Snippet
```java
     * content-type json
     *
     * @param object
     * @param outputStream
     * @throws Exception
```

### JavadocDeclaration
`@param outputStream` tag description is missing
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/DataParseUtils.java`
#### Snippet
```java
     *
     * @param object
     * @param outputStream
     * @throws Exception
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/DataParseUtils.java`
#### Snippet
```java
     * @param object
     * @param outputStream
     * @throws Exception
     */
    public static void writeJsonContent(Object object, OutputStream outputStream) throws Exception {
```

### JavadocDeclaration
`@param formData` tag description is missing
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/DataParseUtils.java`
#### Snippet
```java
     * content-type form
     *
     * @param formData
     * @param outputStream
     * @throws Exception
```

### JavadocDeclaration
`@param outputStream` tag description is missing
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/DataParseUtils.java`
#### Snippet
```java
     *
     * @param formData
     * @param outputStream
     * @throws Exception
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/DataParseUtils.java`
#### Snippet
```java
     * @param formData
     * @param outputStream
     * @throws Exception
     */
    public static void writeFormContent(Map formData, OutputStream outputStream) throws Exception {
```

### JavadocDeclaration
`@param url` tag description is missing
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/RestProtocol.java`
#### Snippet
```java
     * create rest ReferenceCountedClient
     *
     * @param url
     * @return
     * @throws RpcException
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/RestProtocol.java`
#### Snippet
```java
     *
     * @param url
     * @return
     * @throws RpcException
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/RestProtocol.java`
#### Snippet
```java
     * @param url
     * @return
     * @throws RpcException
     */
    private ReferenceCountedClient<? extends RestClient> createReferenceCountedClient(URL url) throws RpcException {
```

### JavadocDeclaration
`@param header` tag description is missing
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/HttpHeaderUtil.java`
#### Snippet
```java
     * for substring attachment prefix
     *
     * @param header
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/HttpHeaderUtil.java`
#### Snippet
```java
     *
     * @param header
     * @return
     */
    public static String subRestAttachRealHeaderPrefix(String header) {
```

### JavadocDeclaration
`@param attachmentMap` tag description is missing
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/HttpHeaderUtil.java`
#### Snippet
```java
     * convert attachment to Map<String, List<String>>
     *
     * @param attachmentMap
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/HttpHeaderUtil.java`
#### Snippet
```java
     *
     * @param attachmentMap
     * @return
     */
    public static Map<String, List<String>> createAttachments(Map<String, Object> attachmentMap) {
```

### JavadocDeclaration
`@param header` tag description is missing
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/HttpHeaderUtil.java`
#### Snippet
```java
     * append prefix to rest header  distinguish from normal header
     *
     * @param header
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/HttpHeaderUtil.java`
#### Snippet
```java
     *
     * @param header
     * @return
     */
    public static String appendPrefixToAttachRealHeader(String header) {
```

### JavadocDeclaration
`@param nettyHttpResponse` tag description is missing
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/HttpHeaderUtil.java`
#### Snippet
```java
     * add  provider attachment to response
     *
     * @param nettyHttpResponse
     */
    public static void addResponseAttachments(NettyHttpResponse nettyHttpResponse) {
```

### JavadocDeclaration
`@param requestTemplate` tag description is missing
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/HttpHeaderUtil.java`
#### Snippet
```java
     * add consumer attachment to request
     *
     * @param requestTemplate
     * @param attachmentMap
     */
```

### JavadocDeclaration
`@param attachmentMap` tag description is missing
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/HttpHeaderUtil.java`
#### Snippet
```java
     *
     * @param requestTemplate
     * @param attachmentMap
     */
    public static void addRequestAttachments(RequestTemplate requestTemplate, Map<String, Object> attachmentMap) {
```

### JavadocDeclaration
`@param rpcInvocation` tag description is missing
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/HttpHeaderUtil.java`
#### Snippet
```java
    /**
     *  parse request attribute
     * @param rpcInvocation
     * @param request
     */
```

### JavadocDeclaration
`@param request` tag description is missing
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/HttpHeaderUtil.java`
#### Snippet
```java
     *  parse request attribute
     * @param rpcInvocation
     * @param request
     */
    public static void parseRequestAttribute(RpcInvocation rpcInvocation, RequestFacade request) {
```

### JavadocDeclaration
`@param rpcInvocation` tag description is missing
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/HttpHeaderUtil.java`
#### Snippet
```java
    /**
     *  parse request
     * @param rpcInvocation
     * @param request
     */
```

### JavadocDeclaration
`@param request` tag description is missing
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/HttpHeaderUtil.java`
#### Snippet
```java
     *  parse request
     * @param rpcInvocation
     * @param request
     */
    public static void parseRequest(RpcInvocation rpcInvocation, RequestFacade request) {
```

### JavadocDeclaration
`@param appResponse` tag description is missing
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/HttpHeaderUtil.java`
#### Snippet
```java
    /**
     *  parse rest response header to appResponse attribute & attachment
     * @param appResponse
     * @param restResult
     */
```

### JavadocDeclaration
`@param restResult` tag description is missing
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/HttpHeaderUtil.java`
#### Snippet
```java
     *  parse rest response header to appResponse attribute & attachment
     * @param appResponse
     * @param restResult
     */
    public static void parseResponseHeader(AppResponse appResponse, RestResult restResult) {
```

### JavadocDeclaration
`@param header` tag description is missing
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/HttpHeaderUtil.java`
#### Snippet
```java
     * for judge rest header or rest attachment
     *
     * @param header
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/HttpHeaderUtil.java`
#### Snippet
```java
     *
     * @param header
     * @return
     */
    public static boolean isRestAttachHeader(String header) {
```

### JavadocDeclaration
`@param rpcInvocation` tag description is missing
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/HttpHeaderUtil.java`
#### Snippet
```java
     * parse rest request header  attachment & header
     *
     * @param rpcInvocation
     * @param requestFacade
     */
```

### JavadocDeclaration
`@param requestFacade` tag description is missing
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/HttpHeaderUtil.java`
#### Snippet
```java
     *
     * @param rpcInvocation
     * @param requestFacade
     */
    public static void parseRequestHeader(RpcInvocation rpcInvocation, RequestFacade requestFacade) {
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/report/support/AbstractMetadataReport.java`
#### Snippet
```java
     * between 2:00 am to 6:00 am, the time is random.
     *
     * @return
     */
    long calculateStartTime() {
```

### JavadocDeclaration
`@param targetType` tag description is missing
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/message/HttpMessageCodec.java`
#### Snippet
```java
    /**
     *  class type support judge
     * @param targetType
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/message/HttpMessageCodec.java`
#### Snippet
```java
     *  class type support judge
     * @param targetType
     * @return
     */
    boolean typeSupport(Class<?> targetType);
```

### JavadocDeclaration
`@param mediaType` tag description is missing
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/message/HttpMessageCodec.java`
#### Snippet
```java
    /**
     *  content-type support judge
     * @param mediaType
     * @param targetType
     * @return
```

### JavadocDeclaration
`@param targetType` tag description is missing
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/message/HttpMessageCodec.java`
#### Snippet
```java
     *  content-type support judge
     * @param mediaType
     * @param targetType
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/message/HttpMessageCodec.java`
#### Snippet
```java
     * @param mediaType
     * @param targetType
     * @return
     */
    boolean contentTypeSupport(MediaType mediaType, Class<?> targetType);
```

### JavadocDeclaration
`@param mediaType` tag description is missing
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/message/HttpMessageCodecManager.java`
#### Snippet
```java
     * if content-type is null or  all ,will judge media type by class type
     *
     * @param mediaType
     * @param bodyType
     * @param httpMessageCodec
```

### JavadocDeclaration
`@param bodyType` tag description is missing
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/message/HttpMessageCodecManager.java`
#### Snippet
```java
     *
     * @param mediaType
     * @param bodyType
     * @param httpMessageCodec
     * @return
```

### JavadocDeclaration
`@param httpMessageCodec` tag description is missing
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/message/HttpMessageCodecManager.java`
#### Snippet
```java
     * @param mediaType
     * @param bodyType
     * @param httpMessageCodec
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/message/HttpMessageCodecManager.java`
#### Snippet
```java
     * @param bodyType
     * @param httpMessageCodec
     * @return
     */
    private static boolean typeJudge(MediaType mediaType, Class<?> bodyType, HttpMessageCodec httpMessageCodec) {
```

### JavadocDeclaration
`@param nettyHttpResponse` tag description is missing
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/handler/NettyHttpHandler.java`
#### Snippet
```java
     * write return value by accept
     *
     * @param nettyHttpResponse
     * @param request
     * @param invoker
```

### JavadocDeclaration
`@param request` tag description is missing
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/handler/NettyHttpHandler.java`
#### Snippet
```java
     *
     * @param nettyHttpResponse
     * @param request
     * @param invoker
     * @param value
```

### JavadocDeclaration
`@param invoker` tag description is missing
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/handler/NettyHttpHandler.java`
#### Snippet
```java
     * @param nettyHttpResponse
     * @param request
     * @param invoker
     * @param value
     * @param returnType
```

### JavadocDeclaration
`@param value` tag description is missing
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/handler/NettyHttpHandler.java`
#### Snippet
```java
     * @param request
     * @param invoker
     * @param value
     * @param returnType
     * @throws Exception
```

### JavadocDeclaration
`@param returnType` tag description is missing
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/handler/NettyHttpHandler.java`
#### Snippet
```java
     * @param invoker
     * @param value
     * @param returnType
     * @throws Exception
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/handler/NettyHttpHandler.java`
#### Snippet
```java
     * @param value
     * @param returnType
     * @throws Exception
     */
    private void writeResult(NettyHttpResponse nettyHttpResponse, RequestFacade request, Invoker invoker, Object value, Class returnType) throws Exception {
```

### JavadocDeclaration
`@param request` tag description is missing
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/handler/NettyHttpHandler.java`
#### Snippet
```java
     * return first match , if any multiple content-type
     *
     * @param request
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/handler/NettyHttpHandler.java`
#### Snippet
```java
     *
     * @param request
     * @return
     */
    private MediaType getAcceptMediaType(RequestFacade request, Class<?> returnType) {
```

### JavadocDeclaration
`@param requestFacade` tag description is missing
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/handler/NettyHttpHandler.java`
#### Snippet
```java
     * accept can not support will throw UnSupportAcceptException
     *
     * @param requestFacade
     */
    private void acceptSupportJudge(RequestFacade requestFacade, Class<?> returnType) {
```

### JavadocDeclaration
`@param methodName` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ServiceDescriptor.java`
#### Snippet
```java
     * Does not use Optional as return type to avoid potential performance decrease.
     *
     * @param methodName
     * @param params
     * @return
```

### JavadocDeclaration
`@param params` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ServiceDescriptor.java`
#### Snippet
```java
     *
     * @param methodName
     * @param params
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ServiceDescriptor.java`
#### Snippet
```java
     * @param methodName
     * @param params
     * @return
     */
    MethodDescriptor getMethod(String methodName, String params);
```

### JavadocDeclaration
`@param methodName` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ServiceDescriptor.java`
#### Snippet
```java
     * Does not use Optional as return type to avoid potential performance decrease.
     *
     * @param methodName
     * @param paramTypes
     * @return methodDescriptor
```

### JavadocDeclaration
`@param paramTypes` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ServiceDescriptor.java`
#### Snippet
```java
     *
     * @param methodName
     * @param paramTypes
     * @return methodDescriptor
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-configcenter/dubbo-configcenter-nacos/src/main/java/org/apache/dubbo/configcenter/support/nacos/NacosDynamicConfiguration.java`
#### Snippet
```java
     * @param key   property key the native listener will listen on
     * @param group to distinguish different set of properties
     * @return
     */
    private NacosConfigListener createTargetListener(String key, String group) {
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ConsumerModel.java`
#### Snippet
```java
     * @param method   methodName
     * @param argsType method arguments type
     * @return
     */
    public ConsumerMethodModel getMethodModel(String method, String[] argsType) {
```

### JavadocDeclaration
`@param scopeModel` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ScopeModelAware.java`
#### Snippet
```java
    /**
     * Override this method if you need get the scope model (maybe one of FrameworkModel/ApplicationModel/ModuleModel).
     * @param scopeModel
     */
    default void setScopeModel(ScopeModel scopeModel) {
```

### JavadocDeclaration
`@param moduleModel` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ScopeModelAware.java`
#### Snippet
```java
    /**
     * Override this method if you just need module model
     * @param moduleModel
     */
    default void setModuleModel(ModuleModel moduleModel) {
```

### JavadocDeclaration
`@param applicationModel` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ScopeModelAware.java`
#### Snippet
```java
    /**
     * Override this method if you just need application model
     * @param applicationModel
     */
    default void setApplicationModel(ApplicationModel applicationModel) {
```

### JavadocDeclaration
`@param frameworkModel` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ScopeModelAware.java`
#### Snippet
```java
    /**
     * Override this method if you just need framework model
     * @param frameworkModel
     */
    default void setFrameworkModel(FrameworkModel frameworkModel) {
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/FrameworkModel.java`
#### Snippet
```java
    /**
     * Get or create default application model
     * @return
     */
    public ApplicationModel defaultApplication() {
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/FrameworkModel.java`
#### Snippet
```java
    /**
     * Get all framework model instances
     * @return
     */
    public static List<FrameworkModel> getAllInstances() {
```

### JavadocDeclaration
`@param methodName` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ReflectionServiceDescriptor.java`
#### Snippet
```java
     * Does not use Optional as return type to avoid potential performance decrease.
     *
     * @param methodName
     * @param paramTypes
     * @return
```

### JavadocDeclaration
`@param paramTypes` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ReflectionServiceDescriptor.java`
#### Snippet
```java
     *
     * @param methodName
     * @param paramTypes
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ReflectionServiceDescriptor.java`
#### Snippet
```java
     * @param methodName
     * @param paramTypes
     * @return
     */
    public MethodDescriptor getMethod(String methodName, Class<?>[] paramTypes) {
```

### JavadocDeclaration
`@param methodName` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ReflectionServiceDescriptor.java`
#### Snippet
```java
     * Does not use Optional as return type to avoid potential performance decrease.
     *
     * @param methodName
     * @param params
     * @return
```

### JavadocDeclaration
`@param params` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ReflectionServiceDescriptor.java`
#### Snippet
```java
     *
     * @param methodName
     * @param params
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ReflectionServiceDescriptor.java`
#### Snippet
```java
     * @param methodName
     * @param params
     * @return
     */
    public MethodDescriptor getMethod(String methodName, String params) {
```

### JavadocDeclaration
`@param path` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ModuleServiceRepository.java`
#### Snippet
```java
     * 3. path's default value is the name of the interface.
     *
     * @param path
     * @param interfaceClass
     * @return
```

### JavadocDeclaration
`@param interfaceClass` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ModuleServiceRepository.java`
#### Snippet
```java
     *
     * @param path
     * @param interfaceClass
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ModuleServiceRepository.java`
#### Snippet
```java
     * @param path
     * @param interfaceClass
     * @return
     */
    public ServiceDescriptor registerService(String path, Class<?> interfaceClass) {
```

### JavadocDeclaration
`@param methodName` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/StubServiceDescriptor.java`
#### Snippet
```java
     * Does not use Optional as return type to avoid potential performance decrease.
     *
     * @param methodName
     * @param params
     * @return
```

### JavadocDeclaration
`@param params` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/StubServiceDescriptor.java`
#### Snippet
```java
     *
     * @param methodName
     * @param params
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/StubServiceDescriptor.java`
#### Snippet
```java
     * @param methodName
     * @param params
     * @return
     */
    public MethodDescriptor getMethod(String methodName, String params) {
```

### JavadocDeclaration
`@param methodName` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/StubServiceDescriptor.java`
#### Snippet
```java
     * Does not use Optional as return type to avoid potential performance decrease.
     *
     * @param methodName
     * @param paramTypes
     * @return
```

### JavadocDeclaration
`@param paramTypes` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/StubServiceDescriptor.java`
#### Snippet
```java
     *
     * @param methodName
     * @param paramTypes
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/StubServiceDescriptor.java`
#### Snippet
```java
     * @param methodName
     * @param paramTypes
     * @return
     */
    public MethodDescriptor getMethod(String methodName, Class<?>[] paramTypes) {
```

### JavadocDeclaration
Wrong tag `export`
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/service/EchoService.java`
#### Snippet
```java
/**
 * Echo service.
 * @export
 */
public interface EchoService {
```

### JavadocDeclaration
Wrong tag `export`
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/service/GenericService.java`
#### Snippet
```java
 * Generic service interface
 *
 * @export
 */
public interface GenericService {
```

### JavadocDeclaration
`@param url` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/Resetable.java`
#### Snippet
```java
     * reset.
     *
     * @param url
     */
    void reset(URL url);
```

### JavadocDeclaration
Wrong tag `export`
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/service/GenericException.java`
#### Snippet
```java
 * GenericException
 *
 * @export
 */
public class GenericException extends RuntimeException {
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ServiceModel.java`
#### Snippet
```java
    /**
     * ServiceModel should be decoupled from AbstractInterfaceConfig and removed in a future version
     * @return
     */
    @Deprecated
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ServiceModel.java`
#### Snippet
```java
    /**
     * ServiceModel should be decoupled from AbstractInterfaceConfig and removed in a future version
     * @return
     */
    @Deprecated
```

### JavadocDeclaration
`@param displayKey` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/BaseServiceMetadata.java`
#### Snippet
```java
     * revert of org.apache.dubbo.common.ServiceDescriptor#getDisplayServiceKey()
     *
     * @param displayKey
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/BaseServiceMetadata.java`
#### Snippet
```java
     *
     * @param displayKey
     * @return
     */
    public static BaseServiceMetadata revertDisplayServiceKey(String displayKey) {
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/BaseServiceMetadata.java`
#### Snippet
```java
     * Format : interface:version
     *
     * @return
     */
    public String getDisplayServiceKey() {
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/Version.java`
#### Snippet
```java
     *
     * @param version, the protocol version.
     * @return
     */
    public static boolean isSupportResponseAttachment(String version) {
```

### JavadocDeclaration
`@param obj` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/url/component/DubboServiceAddressURL.java`
#### Snippet
```java
     * It's only meaningful for comparing two AddressURLs related to the same consumerURL.
     *
     * @param obj
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/url/component/DubboServiceAddressURL.java`
#### Snippet
```java
     *
     * @param obj
     * @return
     */
    @Override
```

### JavadocDeclaration
`@param obj` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/url/component/ServiceAddressURL.java`
#### Snippet
```java
     * It's only meaningful for comparing two address urls related to the same consumerURL.
     *
     * @param obj
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/url/component/ServiceAddressURL.java`
#### Snippet
```java
     *
     * @param obj
     * @return
     */
    @Override
```

### JavadocDeclaration
`@param n` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/url/component/param/FixedParamValue.java`
#### Snippet
```java
    /**
     * DEFAULT value will be returned if n = 0
     * @param n
     */
    @Override
```

### JavadocDeclaration
`@throws` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/Bytes.java`
#### Snippet
```java
     * @param bytes compressed byte array.
     * @return byte uncompressed array.
     * @throws IOException
     */
    public static byte[] unzip(byte[] bytes) throws IOException {
```

### JavadocDeclaration
`@throws` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/Bytes.java`
#### Snippet
```java
     * @param bytes source.
     * @return compressed byte array.
     * @throws IOException
     */
    public static byte[] zip(byte[] bytes) throws IOException {
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/infra/InfraAdapter.java`
#### Snippet
```java
     *
     * @param params application name or hostname are most likely to be used as input params.
     * @return
     */
    Map<String, String> getExtraAttributes(Map<String, String> params);
```

### JavadocDeclaration
`@param key` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/infra/InfraAdapter.java`
#### Snippet
```java

    /**
     * @param key
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/infra/InfraAdapter.java`
#### Snippet
```java
    /**
     * @param key
     * @return
     */
    String getAttribute(String key);
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/URL.java`
#### Snippet
```java
     * The format of return value is '{group}/{path/interfaceName}:{version}'
     *
     * @return
     */
    public String getPathKey() {
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/URL.java`
#### Snippet
```java
     * Format : interface:version
     *
     * @return
     */
    public String getDisplayServiceKey() {
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/URL.java`
#### Snippet
```java
     * The format is "{interface}:[version]:[group]"
     *
     * @return
     */
    public String getColonSeparatedKey() {
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/URL.java`
#### Snippet
```java
     * @param url,     url string
     * @param encoded, encoded or decoded
     * @return
     */
    public static URL valueOf(String url, boolean encoded) {
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/URL.java`
#### Snippet
```java
     *
     * @param url, decoded url string
     * @return
     */
    public static URL valueOf(String url) {
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/URL.java`
#### Snippet
```java
     * The format of return value is '{group}/{interfaceName}:{version}'
     *
     * @return
     */
    public String getServiceKey() {
```

### JavadocDeclaration
`@param index` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/Stack.java`
#### Snippet
```java
     * remove.
     *
     * @param index
     * @return element
     */
```

### JavadocDeclaration
`@param ele` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/Stack.java`
#### Snippet
```java
     * push.
     *
     * @param ele
     */
    public void push(E ele) {
```

### JavadocDeclaration
`@param input` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/MD5Utils.java`
#### Snippet
```java
     * Calculation md5 value of specify string
     *
     * @param input
     */
    public String getMd5(String input) {
```

### JavadocDeclaration
`@param resourceLocation` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/IOUtils.java`
#### Snippet
```java
     * use like spring code
     *
     * @param resourceLocation
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/IOUtils.java`
#### Snippet
```java
     *
     * @param resourceLocation
     * @return
     */
    public static URL getURL(String resourceLocation) throws FileNotFoundException {
```

### JavadocDeclaration
`@param annotation` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/AnnotationUtils.java`
#### Snippet
```java
    /**
     * Get attributes of annotation
     * @param annotation
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/AnnotationUtils.java`
#### Snippet
```java
     * Get attributes of annotation
     * @param annotation
     * @return
     */
    static Map<String, Object> getAttributes(Annotation annotation, boolean filterDefaultValue) {
```

### JavadocDeclaration
`@param annotation` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/AnnotationUtils.java`
#### Snippet
```java
    /**
     * Filter default value of Annotation type
     * @param annotation
     * @param attributes
     * @return
```

### JavadocDeclaration
`@param attributes` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/AnnotationUtils.java`
#### Snippet
```java
     * Filter default value of Annotation type
     * @param annotation
     * @param attributes
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/AnnotationUtils.java`
#### Snippet
```java
     * @param annotation
     * @param attributes
     * @return
     */
    static Map<String, Object> filterDefaultValues(Annotation annotation, Map<String, Object> attributes) {
```

### JavadocDeclaration
`@param attributes` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/AnnotationUtils.java`
#### Snippet
```java
     * Filter default value of Annotation type
     * @param annotationType annotation type from {@link Annotation#annotationType()}
     * @param attributes
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/AnnotationUtils.java`
#### Snippet
```java
     * @param annotationType annotation type from {@link Annotation#annotationType()}
     * @param attributes
     * @return
     */
    static Map<String, Object> filterDefaultValues(Class<? extends Annotation> annotationType, Map<String, Object> attributes) {
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ClassUtils.java`
#### Snippet
```java
     *
     * @param type the type to test
     * @return
     * @deprecated as 2.7.6, use {@link Class#isPrimitive()} plus {@link #isSimpleType(Class)} instead
     */
```

### JavadocDeclaration
`@param clazz` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ClassUtils.java`
#### Snippet
```java
     * get class loader
     *
     * @param clazz
     * @return class loader
     */
```

### JavadocDeclaration
`@param type` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ClassUtils.java`
#### Snippet
```java
     * We only check boolean value at this moment.
     *
     * @param type
     * @param value
     * @return
```

### JavadocDeclaration
`@param value` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ClassUtils.java`
#### Snippet
```java
     *
     * @param type
     * @param value
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ClassUtils.java`
#### Snippet
```java
     * @param type
     * @param value
     * @return
     */
    public static boolean isTypeMatch(Class<?> type, String value) {
```

### JavadocDeclaration
`@param type` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ClassHelper.java`
#### Snippet
```java
     * We only check boolean value at this moment.
     *
     * @param type
     * @param value
     * @return
```

### JavadocDeclaration
`@param value` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ClassHelper.java`
#### Snippet
```java
     *
     * @param type
     * @param value
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ClassHelper.java`
#### Snippet
```java
     * @param type
     * @param value
     * @return
     */
    public static boolean isTypeMatch(Class<?> type, String value) {
```

### JavadocDeclaration
`@param clazz` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ClassHelper.java`
#### Snippet
```java
     * get class loader
     *
     * @param clazz
     * @return class loader
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/Page.java`
#### Snippet
```java
     * Returns whether the page has data at all.
     *
     * @return
     */
    default boolean hasData() {
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ConfigUtils.java`
#### Snippet
```java
     * It is not recommended using this method to modify dubbo properties.
     *
     * @return
     */
    public static Properties getProperties(Set<ClassLoader> classLoaders) {
```

### JavadocDeclaration
`@param fileName` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ConfigUtils.java`
#### Snippet
```java
     * check if the fileName can be found in filesystem
     *
     * @param fileName
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ConfigUtils.java`
#### Snippet
```java
     *
     * @param fileName
     * @return
     */
    private static boolean checkFileNameExist(String fileName) {
```

### JavadocDeclaration
`@param snakeName` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/StringUtils.java`
#### Snippet
```java
     * NOTE: Return itself if it's not a snake case.
     *
     * @param snakeName
     * @param split
     * @return
```

### JavadocDeclaration
`@param split` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/StringUtils.java`
#### Snippet
```java
     *
     * @param snakeName
     * @param split
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/StringUtils.java`
#### Snippet
```java
     * @param snakeName
     * @param split
     * @return
     */
    public static String snakeToSplitName(String snakeName, String split) {
```

### JavadocDeclaration
`@param str` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/StringUtils.java`
#### Snippet
```java
     * Convert camelCase or snake_case/SNAKE_CASE to kebab-case
     *
     * @param str
     * @param split
     * @return
```

### JavadocDeclaration
`@param split` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/StringUtils.java`
#### Snippet
```java
     *
     * @param str
     * @param split
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/StringUtils.java`
#### Snippet
```java
     * @param str
     * @param split
     * @return
     */
    public static String convertToSplitName(String str, String split) {
```

### JavadocDeclaration
`@param str` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/StringUtils.java`
#### Snippet
```java
     * Test str whether starts with the prefix ignore case.
     *
     * @param str
     * @param prefix
     * @return
```

### JavadocDeclaration
`@param prefix` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/StringUtils.java`
#### Snippet
```java
     *
     * @param str
     * @param prefix
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/StringUtils.java`
#### Snippet
```java
     * @param str
     * @param prefix
     * @return
     */
    public static boolean startsWithIgnoreCase(String str, String prefix) {
```

### JavadocDeclaration
`@param params` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/StringUtils.java`
#### Snippet
```java
     * Encode parameters map to string, like '[{a:b},{c:d}]'
     *
     * @param params
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/StringUtils.java`
#### Snippet
```java
     *
     * @param params
     * @return
     */
    public static String encodeParameters(Map<String, String> params) {
```

### JavadocDeclaration
`@param msg` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/StringUtils.java`
#### Snippet
```java

    /**
     * @param msg
     * @param e
     * @return string
```

### JavadocDeclaration
`@param e` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/StringUtils.java`
#### Snippet
```java
    /**
     * @param msg
     * @param e
     * @return string
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/StringUtils.java`
#### Snippet
```java
     *
     * @param rawParameters format like '[{a:b},{c:d}]'
     * @return
     */
    public static Map<String, String> parseParameters(String rawParameters) {
```

### JavadocDeclaration
`@param e` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/StringUtils.java`
#### Snippet
```java

    /**
     * @param e
     * @return string
     */
```

### JavadocDeclaration
`@param values` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/StringUtils.java`
#### Snippet
```java

    /**
     * @param values
     * @param value
     * @return contains
```

### JavadocDeclaration
`@param value` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/StringUtils.java`
#### Snippet
```java
    /**
     * @param values
     * @param value
     * @return contains
     */
```

### JavadocDeclaration
`@param cs` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/StringUtils.java`
#### Snippet
```java
     * Check the cs String whether contains non whitespace characters.
     *
     * @param cs
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/StringUtils.java`
#### Snippet
```java
     *
     * @param cs
     * @return
     */
    public static boolean hasText(CharSequence cs) {
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/OrderedPropertiesProvider.java`
#### Snippet
```java
     * order
     *
     * @return
     */
    int priority();
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/OrderedPropertiesProvider.java`
#### Snippet
```java
     * load the properties
     *
     * @return
     */
    Properties initProperties();
```

### JavadocDeclaration
`@param parameterType` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/PojoUtils.java`
#### Snippet
```java
     * return init value
     *
     * @param parameterType
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/PojoUtils.java`
#### Snippet
```java
     *
     * @param parameterType
     * @return
     */
    private static Object getDefaultValue(Class<?> parameterType) {
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/Environment.java`
#### Snippet
```java
     * Get global configuration as map list
     *
     * @return
     */
    public List<Map<String, String>> getConfigurationMaps() {
```

### JavadocDeclaration
`@param config` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/Environment.java`
#### Snippet
```java
     * This method helps us t filter out the most priority values from various configuration sources.
     *
     * @param config
     * @param prefix
     * @return
```

### JavadocDeclaration
`@param prefix` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/Environment.java`
#### Snippet
```java
     *
     * @param config
     * @param prefix
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/Environment.java`
#### Snippet
```java
     * @param config
     * @param prefix
     * @return
     */
    public Configuration getPrefixedConfiguration(AbstractConfig config, String prefix) {
```

### JavadocDeclaration
`@param map` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/Environment.java`
#### Snippet
```java
     * Merge target map properties into app configuration
     *
     * @param map
     */
    public void updateAppConfigMap(Map<String, String> map) {
```

### JavadocDeclaration
`@param config` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/Environment.java`
#### Snippet
```java
     * Get configuration map list for target instance
     *
     * @param config
     * @param prefix
     * @return
```

### JavadocDeclaration
`@param prefix` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/Environment.java`
#### Snippet
```java
     *
     * @param config
     * @param prefix
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/Environment.java`
#### Snippet
```java
     * @param config
     * @param prefix
     * @return
     */
    public List<Map<String, String>> getConfigurationMaps(AbstractConfig config, String prefix) {
```

### JavadocDeclaration
Wrong tag `jls`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/MethodUtils.java`
#### Snippet
```java
     * @return {@code true} if and only if the first method overrides
     * the second
     * @jls 8.4.8 Inheritance, Overriding, and Hiding
     * @jls 9.4.1 Inheritance and Overriding
     * @see Elements#overrides(ExecutableElement, ExecutableElement, TypeElement)
```

### JavadocDeclaration
Wrong tag `jls`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/MethodUtils.java`
#### Snippet
```java
     * the second
     * @jls 8.4.8 Inheritance, Overriding, and Hiding
     * @jls 9.4.1 Inheritance and Overriding
     * @see Elements#overrides(ExecutableElement, ExecutableElement, TypeElement)
     */
```

### JavadocDeclaration
`@param hostName` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/NetUtils.java`
#### Snippet
```java

    /**
     * @param hostName
     * @return ip address or hostName if UnknownHostException
     */
```

### JavadocDeclaration
`@param pattern` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/NetUtils.java`
#### Snippet
```java

    /**
     * @param pattern
     * @param host
     * @param port
```

### JavadocDeclaration
`@param host` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/NetUtils.java`
#### Snippet
```java
    /**
     * @param pattern
     * @param host
     * @param port
     * @return
```

### JavadocDeclaration
`@param port` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/NetUtils.java`
#### Snippet
```java
     * @param pattern
     * @param host
     * @param port
     * @return
     * @throws UnknownHostException
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/NetUtils.java`
#### Snippet
```java
     * @param host
     * @param port
     * @return
     * @throws UnknownHostException
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/NetUtils.java`
#### Snippet
```java
     * @param port
     * @return
     * @throws UnknownHostException
     */
    public static boolean matchIpRange(String pattern, String host, int port) throws UnknownHostException {
```

### JavadocDeclaration
`@throws` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/configcenter/AbstractDynamicConfiguration.java`
#### Snippet
```java
     * @param group the group
     * @return If successful, return <code>true</code>, or <code>false</code>
     * @throws Exception
     * @since 2.7.8
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/deploy/ApplicationDeployer.java`
#### Snippet
```java
    /**
     * Starts the component.
     * @return
     */
    Future start() throws IllegalStateException;
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/deploy/Deployer.java`
#### Snippet
```java
    /**
     * Starts the component.
     * @return
     */
    Future start() throws IllegalStateException;
```

### JavadocDeclaration
`@throws` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/configcenter/DynamicConfiguration.java`
#### Snippet
```java
     * Close the configuration
     *
     * @throws Exception
     * @since 2.7.5
     */
```

### JavadocDeclaration
`@param key` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/configcenter/DynamicConfiguration.java`
#### Snippet
```java
     * get configItem which contains content and stat info.
     *
     * @param key
     * @param group
     * @return
```

### JavadocDeclaration
`@param group` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/configcenter/DynamicConfiguration.java`
#### Snippet
```java
     *
     * @param key
     * @param group
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/configcenter/DynamicConfiguration.java`
#### Snippet
```java
     * @param key
     * @param group
     * @return
     */
    default ConfigItem getConfigItem(String key, String group) {
```

### JavadocDeclaration
Wrong tag `revision`
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/configcenter/DynamicConfiguration.java`
#### Snippet
```java
     * This method are mostly used to get a compound config file, such as a complete dubbo.properties file.
     *
     * @revision 2.7.4
     */
    default String getProperties(String key, String group, long timeout) throws IllegalStateException {
```

### JavadocDeclaration
`@param key` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/configcenter/DynamicConfiguration.java`
#### Snippet
```java
     * publish config mapped to this given key and given group with stat.
     *
     * @param key
     * @param group
     * @param content
```

### JavadocDeclaration
`@param group` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/configcenter/DynamicConfiguration.java`
#### Snippet
```java
     *
     * @param key
     * @param group
     * @param content
     * @param ticket
```

### JavadocDeclaration
`@param content` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/configcenter/DynamicConfiguration.java`
#### Snippet
```java
     * @param key
     * @param group
     * @param content
     * @param ticket
     * @return
```

### JavadocDeclaration
`@param ticket` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/configcenter/DynamicConfiguration.java`
#### Snippet
```java
     * @param group
     * @param content
     * @param ticket
     * @return
     * @throws UnsupportedOperationException
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/configcenter/DynamicConfiguration.java`
#### Snippet
```java
     * @param content
     * @param ticket
     * @return
     * @throws UnsupportedOperationException
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/configcenter/DynamicConfiguration.java`
#### Snippet
```java
     * @param ticket
     * @return
     * @throws UnsupportedOperationException
     */
    default boolean publishConfigCas(String key, String group, String content, Object ticket) throws UnsupportedOperationException {
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/configcenter/DynamicConfiguration.java`
#### Snippet
```java
     * The format is '{interfaceName}:[version]:[group]'
     *
     * @return
     */
    static String getRuleKey(URL url) {
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/ConfigurationUtils.java`
#### Snippet
```java
     * Used to get properties from the jvm
     *
     * @return
     */
    public static Configuration getSystemConfiguration(ScopeModel scopeModel) {
```

### JavadocDeclaration
`@param configMaps` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/ConfigurationUtils.java`
#### Snippet
```java
     * </pre>
     *
     * @param configMaps
     * @param prefix
     * @return
```

### JavadocDeclaration
`@param prefix` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/ConfigurationUtils.java`
#### Snippet
```java
     *
     * @param configMaps
     * @param prefix
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/ConfigurationUtils.java`
#### Snippet
```java
     * @param configMaps
     * @param prefix
     * @return
     */
    public static <V extends Object> Set<String> getSubIds(Collection<Map<String, V>> configMaps, String prefix) {
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/ConfigurationUtils.java`
#### Snippet
```java
     * Also see {@link Environment#getConfiguration()}
     *
     * @return
     */

```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/ConfigurationUtils.java`
#### Snippet
```java
     * Used to get properties from the os environment
     *
     * @return
     */
    public static Configuration getEnvConfiguration(ScopeModel scopeModel) {
```

### JavadocDeclaration
`@param configMaps` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/ConfigurationUtils.java`
#### Snippet
```java
     * </pre>
     *
     * @param configMaps
     * @param prefix
     * @param <V>
```

### JavadocDeclaration
`@param prefix` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/ConfigurationUtils.java`
#### Snippet
```java
     *
     * @param configMaps
     * @param prefix
     * @param <V>
     * @return
```

### JavadocDeclaration
`@param ` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/ConfigurationUtils.java`
#### Snippet
```java
     * @param configMaps
     * @param prefix
     * @param <V>
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/ConfigurationUtils.java`
#### Snippet
```java
     * @param prefix
     * @param <V>
     * @return
     */
    public static <V extends Object> Map<String, V> getSubProperties(Collection<Map<String, V>> configMaps, String prefix) {
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/ConfigurationUtils.java`
#### Snippet
```java
     * Server shutdown wait timeout mills
     *
     * @return
     */
    @SuppressWarnings("deprecation")
```

### JavadocDeclaration
`@throws` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/context/Lifecycle.java`
#### Snippet
```java
     * Destroy the component
     *
     * @throws IllegalStateException
     */
    void destroy() throws IllegalStateException;
```

### JavadocDeclaration
Tag `return` is not allowed here
in `dubbo-common/src/main/java/org/apache/dubbo/common/context/Lifecycle.java`
#### Snippet
```java
     * Initialize the component before {@link #start() start}
     *
     * @return current {@link Lifecycle}
     * @throws IllegalStateException
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/context/Lifecycle.java`
#### Snippet
```java
     *
     * @return current {@link Lifecycle}
     * @throws IllegalStateException
     */
    void initialize() throws IllegalStateException;
```

### JavadocDeclaration
Tag `return` is not allowed here
in `dubbo-common/src/main/java/org/apache/dubbo/common/context/Lifecycle.java`
#### Snippet
```java
     * Start the component
     *
     * @return current {@link Lifecycle}
     * @throws IllegalStateException
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/context/Lifecycle.java`
#### Snippet
```java
     *
     * @return current {@link Lifecycle}
     * @throws IllegalStateException
     */
    void start() throws IllegalStateException;
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/convert/ConverterUtil.java`
#### Snippet
```java
     * @param sourceType the source type
     * @param targetType the target type
     * @return
     * @see ExtensionLoader#getSupportedExtensionInstances()
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/convert/multiple/MultiValueConverter.java`
#### Snippet
```java
     * @param multiValueType the multi-value type
     * @param elementType    the element type
     * @return
     */
    Object convert(S source, Class<?> multiValueType, Class<?> elementType);
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/configcenter/file/FileSystemDynamicConfiguration.java`
#### Snippet
```java
     * @param function       the customized {@link Function function} with {@link File}
     * @param <V>            the computed value
     * @return
     */
    protected <V> V delay(String configFilePath, ThrowableFunction<File, V> function) {
```

### JavadocDeclaration
`@throws` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
     * @param c class.
     * @return desc.
     * @throws NotFoundException
     */
    public static String getDesc(final CtClass c) throws NotFoundException {
```

### JavadocDeclaration
`@throws` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
     * @param methodName Method signature, e.g.: method1(int, String). It is allowed to provide method name only, e.g.: method2
     * @return target method
     * @throws NoSuchMethodException
     * @throws ClassNotFoundException
     * @throws IllegalStateException  when multiple methods are found (overridden method when parameter info is not provided)
```

### JavadocDeclaration
`@throws` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
     * @return target method
     * @throws NoSuchMethodException
     * @throws ClassNotFoundException
     * @throws IllegalStateException  when multiple methods are found (overridden method when parameter info is not provided)
     * @deprecated Recommend {@link MethodUtils#findMethod(Class, String, Class[])}
```

### JavadocDeclaration
`@param type` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    /**
     * Get all field names of target type
     * @param type
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
     * Get all field names of target type
     * @param type
     * @return
     */
    public static Set<String> getAllFieldNames(Class<?> type) {
```

### JavadocDeclaration
`@param beanClass` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    /**
     * Check target bean class whether has specify method
     * @param beanClass
     * @param methodName
     * @return
```

### JavadocDeclaration
`@param methodName` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
     * Check target bean class whether has specify method
     * @param beanClass
     * @param methodName
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
     * @param beanClass
     * @param methodName
     * @return
     */
    public static boolean hasMethod(Class<?> beanClass, String methodName) {
```

### JavadocDeclaration
`@throws` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
     * @param desc desc.
     * @return Class instance.
     * @throws ClassNotFoundException
     */
    private static Class<?> desc2class(ClassLoader cl, String desc) throws ClassNotFoundException {
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
     * @param methodName the name of getter
     * @param <T>        the type of property value
     * @return
     * @since 2.7.5
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
     * @param cs class array.
     * @return desc.
     * @throws NotFoundException
     */
    public static String getDesc(final Class<?>[] cs) {
```

### JavadocDeclaration
`@throws` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
     * @param methodName Method signature, e.g.: method1(int, String). It is allowed to provide method name only, e.g.: method2
     * @return target method
     * @throws NoSuchMethodException
     * @throws ClassNotFoundException
     * @throws IllegalStateException  when multiple methods are found (overridden method when parameter info is not provided)
```

### JavadocDeclaration
`@throws` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
     * @return target method
     * @throws NoSuchMethodException
     * @throws ClassNotFoundException
     * @throws IllegalStateException  when multiple methods are found (overridden method when parameter info is not provided)
     * @deprecated Recommend {@link MethodUtils#findMethod(Class, String, Class[])}
```

### JavadocDeclaration
`@throws` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
     * @param desc desc.
     * @return Class[] class array.
     * @throws ClassNotFoundException
     */
    private static Class<?>[] desc2classArray(ClassLoader cl, String desc) throws ClassNotFoundException {
```

### JavadocDeclaration
`@throws` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
     * @param desc desc.
     * @return Class instance.
     * @throws ClassNotFoundException
     */
    public static Class<?> desc2class(String desc) throws ClassNotFoundException {
```

### JavadocDeclaration
`@throws` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
     * @param c class.
     * @return desc.
     * @throws NotFoundException
     */
    public static String getDesc(Class<?> c) {
```

### JavadocDeclaration
`@throws` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
     * @param desc desc.
     * @return Class class array.
     * @throws ClassNotFoundException
     */
    public static Class<?>[] desc2classArray(String desc) throws ClassNotFoundException {
```

### JavadocDeclaration
`@param disposable` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/resource/GlobalResourcesRepository.java`
#### Snippet
```java
     * Register a one-off disposable, the disposable is removed automatically on first shutdown.
     *
     * @param disposable
     */
    public void registerDisposable(Disposable disposable) {
```

### JavadocDeclaration
`@param disposable` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/resource/GlobalResourcesRepository.java`
#### Snippet
```java
     * Note: the global disposable should be registered in static code, it's reusable and will not be removed when dubbo shutdown.
     *
     * @param disposable
     */
    public static void registerGlobalDisposable(Disposable disposable) {
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/extension/Wrapper.java`
#### Snippet
```java
     * absolute ordering, optional
     * ascending order, smaller values will be in the front of the list.
     * @return
     */
    int order() default 0;
```

### JavadocDeclaration
`@param types` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/bytecode/Wrapper.java`
#### Snippet
```java
     * @param instance instance.
     * @param mn       method name.
     * @param types
     * @param args     argument array.
     * @return return value.
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/extension/AdaptiveClassCodeGenerator.java`
#### Snippet
```java

    /**
     * @return
     */
    private String generateScopeModelAssignment() {
```

### JavadocDeclaration
`@param runnable` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/threadpool/ThreadlessExecutor.java`
#### Snippet
```java
     * Otherwise, submit to shared callback executor directly.
     *
     * @param runnable
     */
    @Override
```

### JavadocDeclaration
`@param url` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/threadpool/manager/ExecutorRepository.java`
#### Snippet
```java
     * When the Client or Server starts for the first time, generate a new threadpool according to the parameters specified.
     *
     * @param url
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/threadpool/manager/ExecutorRepository.java`
#### Snippet
```java
     *
     * @param url
     * @return
     */
    ExecutorService createExecutorIfAbsent(URL url);
```

### JavadocDeclaration
`@param url` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/threadpool/manager/ExecutorRepository.java`
#### Snippet
```java
     * Modify some of the threadpool's properties according to the url, for example, coreSize, maxSize, ...
     *
     * @param url
     * @param executor
     */
```

### JavadocDeclaration
`@param executor` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/threadpool/manager/ExecutorRepository.java`
#### Snippet
```java
     *
     * @param url
     * @param executor
     */
    void updateThreadpool(URL url, ExecutorService executor);
```

### JavadocDeclaration
`@param url` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/threadpool/manager/ExecutorRepository.java`
#### Snippet
```java
     * Be careful,The semantics of this method are getOrDefaultExecutor
     *
     * @param url
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/threadpool/manager/ExecutorRepository.java`
#### Snippet
```java
     *
     * @param url
     * @return
     */
    ExecutorService getExecutor(URL url);
```

### JavadocDeclaration
`@param msg` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/threadpool/support/AbortPolicyWithReport.java`
#### Snippet
```java
     * dispatch ThreadPoolExhaustedEvent
     *
     * @param msg
     */
    public void dispatchThreadPoolExhaustedEvent(String msg) {
```

### JavadocDeclaration
Wrong tag `export`
in `dubbo-common/src/main/java/org/apache/dubbo/config/ArgumentConfig.java`
#### Snippet
```java
 * The method arguments configuration
 *
 * @export
 */
public class ArgumentConfig implements Serializable {
```

### JavadocDeclaration
Wrong tag `export`
in `dubbo-common/src/main/java/org/apache/dubbo/config/AbstractMethodConfig.java`
#### Snippet
```java
 * AbstractMethodConfig
 *
 * @export
 */
public abstract class AbstractMethodConfig extends AbstractConfig {
```

### JavadocDeclaration
`@param url` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/threadpool/manager/DefaultExecutorRepository.java`
#### Snippet
```java
     * Get called when the server or client instance initiating.
     *
     * @param url
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/threadpool/manager/DefaultExecutorRepository.java`
#### Snippet
```java
     *
     * @param url
     * @return
     */
    @Override
```

### JavadocDeclaration
`@param url` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/threadpool/manager/DefaultExecutorRepository.java`
#### Snippet
```java
     * Return the executor key based on the type (internal or biz) of the current service.
     *
     * @param url
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/threadpool/manager/DefaultExecutorRepository.java`
#### Snippet
```java
     *
     * @param url
     * @return
     */
    private String getExecutorKey(URL url) {
```

### JavadocDeclaration
Wrong tag `export`
in `dubbo-common/src/main/java/org/apache/dubbo/config/ApplicationConfig.java`
#### Snippet
```java
 * The application info
 *
 * @export
 */
public class ApplicationConfig extends AbstractConfig {
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/config/ApplicationConfig.java`
#### Snippet
```java
     * The format is the same as the springboot, including: getQosEnableCompatible(), getQosPortCompatible(), getQosAcceptForeignIpCompatible().
     *
     * @return
     */
    @Parameter(key = QOS_ENABLE_COMPATIBLE, excluded = true, attribute = false)
```

### JavadocDeclaration
Wrong tag `export`
in `dubbo-common/src/main/java/org/apache/dubbo/config/MetadataReportConfig.java`
#### Snippet
```java
 * MetadataReportConfig
 *
 * @export
 */
public class MetadataReportConfig extends AbstractConfig {
```

### JavadocDeclaration
Wrong tag `export`
in `dubbo-common/src/main/java/org/apache/dubbo/config/ModuleConfig.java`
#### Snippet
```java
 * The module info
 *
 * @export
 */
public class ModuleConfig extends AbstractConfig {
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/common/extension/ExtensionLoader.java`
#### Snippet
```java
     * application startup very slow
     *
     * @return
     */
    private static Map<String, String> getSpecialSPILoadingStrategyMap() {
```

### JavadocDeclaration
Wrong tag `export`
in `dubbo-common/src/main/java/org/apache/dubbo/config/MonitorConfig.java`
#### Snippet
```java
 * MonitorConfig
 *
 * @export
 */
public class MonitorConfig extends AbstractConfig {
```

### JavadocDeclaration
Wrong tag `export`
in `dubbo-common/src/main/java/org/apache/dubbo/config/ConsumerConfig.java`
#### Snippet
```java
 * The service consumer default configuration
 *
 * @export
 */
public class ConsumerConfig extends AbstractReferenceConfig {
```

### JavadocDeclaration
`@param transport` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/config/RegistryConfig.java`
#### Snippet
```java

    /**
     * @param transport
     * @see #setTransporter(String)
     * @deprecated
```

### JavadocDeclaration
Wrong tag `export`
in `dubbo-common/src/main/java/org/apache/dubbo/config/RegistryConfig.java`
#### Snippet
```java
 * RegistryConfig
 *
 * @export
 */
public class RegistryConfig extends AbstractConfig {
```

### JavadocDeclaration
`@param wait` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/config/RegistryConfig.java`
#### Snippet
```java

    /**
     * @param wait
     * @see org.apache.dubbo.config.ProviderConfig#setWait(Integer)
     * @deprecated
```

### JavadocDeclaration
Wrong tag `export`
in `dubbo-common/src/main/java/org/apache/dubbo/config/AbstractReferenceConfig.java`
#### Snippet
```java
 * AbstractConsumerConfig
 *
 * @export
 * @see ReferenceConfigBase
 */
```

### JavadocDeclaration
Wrong tag `export`
in `dubbo-common/src/main/java/org/apache/dubbo/config/ProtocolConfig.java`
#### Snippet
```java
 * ProtocolConfig
 *
 * @export
 */
public class ProtocolConfig extends AbstractConfig {
```

### JavadocDeclaration
`@param injvm` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/config/AbstractReferenceConfig.java`
#### Snippet
```java

    /**
     * @param injvm
     * @deprecated instead, use the parameter <b>scope</b> to judge if it's in jvm, scope=local
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/config/AbstractReferenceConfig.java`
#### Snippet
```java

    /**
     * @return
     * @deprecated instead, use the parameter <b>scope</> to judge if it's in jvm, scope=local
     */
```

### JavadocDeclaration
Wrong tag `export`
in `dubbo-common/src/main/java/org/apache/dubbo/config/ProviderConfig.java`
#### Snippet
```java
 * The service provider default configuration
 *
 * @export
 * @see org.apache.dubbo.config.ProtocolConfig
 * @see ServiceConfigBase
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/config/ReferenceConfigBase.java`
#### Snippet
```java
     * The actual service type of remote provider.
     *
     * @return
     */
    public Class<?> getServiceInterfaceClass() {
```

### JavadocDeclaration
`@param generic` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/config/ReferenceConfigBase.java`
#### Snippet
```java
     * Determine the interface of the proxy class
     *
     * @param generic
     * @param interfaceName
     * @return
```

### JavadocDeclaration
`@param interfaceName` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/config/ReferenceConfigBase.java`
#### Snippet
```java
     *
     * @param generic
     * @param interfaceName
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/config/ReferenceConfigBase.java`
#### Snippet
```java
     * @param generic
     * @param interfaceName
     * @return
     */
    public static Class<?> determineInterfaceClass(String generic, String interfaceName) {
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/config/ReferenceConfigBase.java`
#### Snippet
```java
     * The proxy interface class is used to create proxy instance.
     *
     * @return
     */
    public Class<?> getInterfaceClass() {
```

### JavadocDeclaration
Wrong tag `export`
in `dubbo-common/src/main/java/org/apache/dubbo/config/ReferenceConfigBase.java`
#### Snippet
```java
 * ReferenceConfig
 *
 * @export
 */
public abstract class ReferenceConfigBase<T> extends AbstractReferenceConfig {
```

### JavadocDeclaration
`@param interfaceClass` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/config/ServiceConfigBase.java`
#### Snippet
```java

    /**
     * @param interfaceClass
     * @see #setInterface(Class)
     * @deprecated
```

### JavadocDeclaration
Wrong tag `export`
in `dubbo-common/src/main/java/org/apache/dubbo/config/ServiceConfigBase.java`
#### Snippet
```java
 * ServiceConfig
 *
 * @export
 */
public abstract class ServiceConfigBase<T> extends AbstractServiceConfig {
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/config/annotation/DubboService.java`
#### Snippet
```java
     * methods support
     *
     * @return
     */
    Method[] methods() default {};
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/config/annotation/DubboService.java`
#### Snippet
```java
    /**
     * bean name of service executor(thread pool), used for thread pool isolation between services
     * @return
     */
    String executor() default "";
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/config/annotation/Service.java`
#### Snippet
```java
     * methods support
     *
     * @return
     */
    Method[] methods() default {};
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/config/annotation/Reference.java`
#### Snippet
```java
     * methods support
     *
     * @return
     */
    Method[] methods() default {};
```

### JavadocDeclaration
`@param oldScopeModel` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/config/AbstractConfig.java`
#### Snippet
```java
     * </pre>
     *
     * @param oldScopeModel
     * @param newScopeModel
     */
```

### JavadocDeclaration
`@param newScopeModel` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/config/AbstractConfig.java`
#### Snippet
```java
     *
     * @param oldScopeModel
     * @param newScopeModel
     */
    protected void postProcessAfterScopeModelChanged(ScopeModel oldScopeModel, ScopeModel newScopeModel) {
```

### JavadocDeclaration
Wrong tag `revised`
in `dubbo-common/src/main/java/org/apache/dubbo/config/AbstractConfig.java`
#### Snippet
```java
     * @param prefix     the prefix
     * @return the parameters whose raw key will replace "-" to "."
     * @revised 2.7.8 "private" to be "protected"
     */
    protected static Map<String, String> convert(Map<String, String> parameters, String prefix) {
```

### JavadocDeclaration
`@param parameters` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/config/AbstractConfig.java`
#### Snippet
```java
     * Put attributes of specify 'config' into 'parameters' argument
     *
     * @param parameters
     * @param config
     */
```

### JavadocDeclaration
`@param config` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/config/AbstractConfig.java`
#### Snippet
```java
     *
     * @param parameters
     * @param config
     */
    public static void appendAttributes(Map<String, String> parameters, Object config) {
```

### JavadocDeclaration
Wrong tag `export`
in `dubbo-common/src/main/java/org/apache/dubbo/config/AbstractConfig.java`
#### Snippet
```java
 * Utility methods and public methods for parsing configuration
 *
 * @export
 */
public abstract class AbstractConfig implements Serializable {
```

### JavadocDeclaration
`@param annotationClass` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/config/AbstractConfig.java`
#### Snippet
```java
     * Copy attributes from annotation
     *
     * @param annotationClass
     * @param annotation
     */
```

### JavadocDeclaration
`@param annotation` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/config/AbstractConfig.java`
#### Snippet
```java
     *
     * @param annotationClass
     * @param annotation
     */
    protected void appendAnnotation(Class<?> annotationClass, Object annotation) {
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/config/AbstractConfig.java`
#### Snippet
```java
     * compute attributed getter methods, subclass can override this method to add/remove attributed methods
     *
     * @return
     */
    protected List<Method> computeAttributedMethods() {
```

### JavadocDeclaration
`@param application` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/config/context/ConfigManager.java`
#### Snippet
```java
     * Set application config
     *
     * @param application
     * @return current application config instance
     */
```

### JavadocDeclaration
Tag `return` is not allowed here
in `dubbo-common/src/main/java/org/apache/dubbo/config/context/ConfigManager.java`
#### Snippet
```java
     *
     * @param application
     * @return current application config instance
     */
    @DisableInject
```

### JavadocDeclaration
Wrong tag `ProvidedBy("dubbo-samples-xds-provider")`
in `dubbo-common/src/main/java/org/apache/dubbo/config/annotation/ProvidedBy.java`
#### Snippet
```java
 * Class-level annotation used for declaring Dubbo interface.
 * Example:
 * @ProvidedBy("dubbo-samples-xds-provider")
 * public interface GreetingService {
 *     String sayHello(String name);
```

### JavadocDeclaration
`@param config` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/config/context/ModuleConfigManager.java`
#### Snippet
```java
     * check duplicated ReferenceConfig/ServiceConfig
     *
     * @param config
     */
    private AbstractInterfaceConfig findDuplicatedInterfaceConfig(AbstractInterfaceConfig config) {
```

### JavadocDeclaration
Wrong tag `Component("annotatedConsumer")`
in `dubbo-common/src/main/java/org/apache/dubbo/config/annotation/ProvidedBy.java`
#### Snippet
```java
 * }
 *
 * @Component("annotatedConsumer")
 * public class GreetingServiceConsumer {
 *     @DubboReference(version = "1.0.0")
```

### JavadocDeclaration
Wrong tag `DubboReference(version`
in `dubbo-common/src/main/java/org/apache/dubbo/config/annotation/ProvidedBy.java`
#### Snippet
```java
 * @Component("annotatedConsumer")
 * public class GreetingServiceConsumer {
 *     @DubboReference(version = "1.0.0")
 *     private GreetingService greetingService;
 * }
```

### JavadocDeclaration
`@param clazz` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/metadata/definition/util/ClassUtils.java`
#### Snippet
```java
     * Get the code source file or class path of the Class passed in.
     *
     * @param clazz
     * @return Jar file name or class path.
     */
```

### JavadocDeclaration
Wrong tag `export`
in `dubbo-common/src/main/java/org/apache/dubbo/config/AbstractServiceConfig.java`
#### Snippet
```java
 * AbstractServiceConfig
 *
 * @export
 */
public abstract class AbstractServiceConfig extends AbstractInterfaceConfig {
```

### JavadocDeclaration
`@param type` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/metadata/definition/model/TypeDefinition.java`
#### Snippet
```java
     * {@link ParameterizedType#toString()}
     *
     * @param type
     * @return formatted type
     * @see sun.reflect.generics.reflectiveObjects.ParameterizedTypeImpl
```

### JavadocDeclaration
`@param configType` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/config/context/AbstractConfigManager.java`
#### Snippet
```java
     * Get config by id
     *
     * @param configType
     * @param id
     * @return
```

### JavadocDeclaration
`@param id` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/config/context/AbstractConfigManager.java`
#### Snippet
```java
     *
     * @param configType
     * @param id
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/config/context/AbstractConfigManager.java`
#### Snippet
```java
     * @param configType
     * @param id
     * @return
     */
    protected <C extends AbstractConfig> C getConfigById(String configType, String id) {
```

### JavadocDeclaration
`@param clazz` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/config/context/AbstractConfigManager.java`
#### Snippet
```java
     * The configuration that does not affect the main process is not necessary.
     *
     * @param clazz
     * @param <T>
     * @return
```

### JavadocDeclaration
`@param ` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/config/context/AbstractConfigManager.java`
#### Snippet
```java
     *
     * @param clazz
     * @param <T>
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/config/context/AbstractConfigManager.java`
#### Snippet
```java
     * @param clazz
     * @param <T>
     * @return
     */
    protected <T extends AbstractConfig> boolean isRequired(Class<T> clazz) {
```

### JavadocDeclaration
`@param config` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/config/context/AbstractConfigManager.java`
#### Snippet
```java
     * Add config
     *
     * @param config
     * @param configsMap
     * @return the existing equivalent config or the new adding config
```

### JavadocDeclaration
`@param configsMap` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/config/context/AbstractConfigManager.java`
#### Snippet
```java
     *
     * @param config
     * @param configsMap
     * @return the existing equivalent config or the new adding config
     * @throws IllegalStateException
```

### JavadocDeclaration
`@throws` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/config/context/AbstractConfigManager.java`
#### Snippet
```java
     * @param configsMap
     * @return the existing equivalent config or the new adding config
     * @throws IllegalStateException
     */
    private <C extends AbstractConfig> C addIfAbsent(C config, Map<String, C> configsMap)
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/config/context/AbstractConfigManager.java`
#### Snippet
```java
     *
     * @param config the config instance to remove.
     * @return
     */
    public boolean removeConfig(AbstractConfig config) {
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/config/context/AbstractConfigManager.java`
#### Snippet
```java
     * @param cls      Config type
     * @param idOrName the id or name of the config
     * @return
     */
    public <T extends AbstractConfig> Optional<T> getConfig(Class<T> cls, String idOrName) {
```

### JavadocDeclaration
`@param cls` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/config/context/AbstractConfigManager.java`
#### Snippet
```java
     * Get config by name if existed
     *
     * @param cls
     * @param name
     * @return
```

### JavadocDeclaration
`@param name` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/config/context/AbstractConfigManager.java`
#### Snippet
```java
     *
     * @param cls
     * @param name
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/config/context/AbstractConfigManager.java`
#### Snippet
```java
     * @param cls
     * @param name
     * @return
     */
    protected <C extends AbstractConfig> C getConfigByName(Class<? extends C> cls, String name) {
```

### JavadocDeclaration
`@param config` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/config/context/AbstractConfigManager.java`
#### Snippet
```java
     * The component configuration that does not affect the main process does not need to be verified.
     *
     * @param config
     * @param <T>
     * @return
```

### JavadocDeclaration
`@param ` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/config/context/AbstractConfigManager.java`
#### Snippet
```java
     *
     * @param config
     * @param <T>
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/config/context/AbstractConfigManager.java`
#### Snippet
```java
     * @param config
     * @param <T>
     * @return
     */
    protected <T extends AbstractConfig> boolean isNeedValidation(T config) {
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/config/MethodConfig.java`
#### Snippet
```java
     * Get method prefixes
     *
     * @return
     */
    @Override
```

### JavadocDeclaration
`@param methods` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/config/MethodConfig.java`
#### Snippet
```java
     * TODO remove constructMethodConfig
     *
     * @param methods
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/config/MethodConfig.java`
#### Snippet
```java
     *
     * @param methods
     * @return
     */
    @Deprecated
```

### JavadocDeclaration
Wrong tag `export`
in `dubbo-common/src/main/java/org/apache/dubbo/config/MethodConfig.java`
#### Snippet
```java
 * The method configuration
 *
 * @export
 */
public class MethodConfig extends AbstractMethodConfig {
```

### JavadocDeclaration
Illegal character
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/RegistryService.java`
#### Snippet
```java
     *
     * @param url Query condition, is not allowed to be empty, e.g. consumer://10.20.153.10/org.apache.dubbo.foo.BarService?version=1.0.0&application=kylin
     * @return The registered information list, which may be empty, the meaning is the same as the parameters of {@link org.apache.dubbo.registry.NotifyListener#notify(List<URL>)}.
     * @see org.apache.dubbo.registry.NotifyListener#notify(List)
     */
```

### JavadocDeclaration
Illegal character
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/RegistryService.java`
#### Snippet
```java
     *
     * @param url Query condition, is not allowed to be empty, e.g. consumer://10.20.153.10/org.apache.dubbo.foo.BarService?version=1.0.0&application=kylin
     * @return The registered information list, which may be empty, the meaning is the same as the parameters of {@link org.apache.dubbo.registry.NotifyListener#notify(List<URL>)}.
     * @see org.apache.dubbo.registry.NotifyListener#notify(List)
     */
```

### JavadocDeclaration
`@param listener` tag description is missing
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/ServiceDiscovery.java`
#### Snippet
```java
     * unsubscribe to instance change event.
     *
     * @param listener
     * @throws IllegalArgumentException
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/ServiceDiscovery.java`
#### Snippet
```java
     *
     * @param listener
     * @throws IllegalArgumentException
     */
    default void removeServiceInstancesChangedListener(ServiceInstancesChangedListener listener)
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/config/AbstractInterfaceConfig.java`
#### Snippet
```java
     * rest protocol allow the service is implement class
     *
     * @return
     */
    protected boolean canSkipInterfaceCheck() {
```

### JavadocDeclaration
`@param application` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/config/AbstractInterfaceConfig.java`
#### Snippet
```java

    /**
     * @param application
     * @deprecated Use {@link org.apache.dubbo.config.AbstractConfig#setScopeModel(ScopeModel)}
     */
```

### JavadocDeclaration
Wrong tag `export`
in `dubbo-common/src/main/java/org/apache/dubbo/config/AbstractInterfaceConfig.java`
#### Snippet
```java
 * AbstractDefaultConfig
 *
 * @export
 */
public abstract class AbstractInterfaceConfig extends AbstractMethodConfig {
```

### JavadocDeclaration
`@param local` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/config/AbstractInterfaceConfig.java`
#### Snippet
```java

    /**
     * @param local
     * @deprecated Replace to <code>setStub(String)</code>
     */
```

### JavadocDeclaration
`@param module` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/config/AbstractInterfaceConfig.java`
#### Snippet
```java

    /**
     * @param module
     * @deprecated Use {@link org.apache.dubbo.config.AbstractConfig#setScopeModel(ScopeModel)}
     */
```

### JavadocDeclaration
`@param local` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/config/AbstractInterfaceConfig.java`
#### Snippet
```java

    /**
     * @param local
     * @deprecated Replace to <code>setStub(Boolean)</code>
     */
```

### JavadocDeclaration
`@param interfaceClass` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/config/AbstractInterfaceConfig.java`
#### Snippet
```java
     * To obtain the method list in the port, use reflection when in native mode and javassist otherwise.
     *
     * @param interfaceClass
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-common/src/main/java/org/apache/dubbo/config/AbstractInterfaceConfig.java`
#### Snippet
```java
     *
     * @param interfaceClass
     * @return
     */
    protected String[] methods(Class<?> interfaceClass) {
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/ServiceDiscoveryRegistry.java`
#### Snippet
```java
     *
     * @param registryURL the {@link URL} to connect the registry
     * @return
     */
    private ServiceDiscovery getServiceDiscovery(URL registryURL) {
```

### JavadocDeclaration
`@param method` tag description is missing
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/InstanceAddressURL.java`
#### Snippet
```java
     * method parameter only exists in ServiceInfo
     *
     * @param method
     * @param key
     * @return
```

### JavadocDeclaration
`@param key` tag description is missing
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/InstanceAddressURL.java`
#### Snippet
```java
     *
     * @param method
     * @param key
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/InstanceAddressURL.java`
#### Snippet
```java
     * @param method
     * @param key
     * @return
     */
    @Override
```

### JavadocDeclaration
`@param key` tag description is missing
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/InstanceAddressURL.java`
#### Snippet
```java
     * Gets method level value of the specified key.
     *
     * @param key
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/InstanceAddressURL.java`
#### Snippet
```java
     *
     * @param key
     * @return
     */
    @Override
```

### JavadocDeclaration
`@param method` tag description is missing
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/InstanceAddressURL.java`
#### Snippet
```java
     * method parameter only exists in ServiceInfo
     *
     * @param method
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/InstanceAddressURL.java`
#### Snippet
```java
     *
     * @param method
     * @return
     */
    @Override
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/InstanceAddressURL.java`
#### Snippet
```java
     * Avoid calling this method in RPC call.
     *
     * @return
     */
    @Override
```

### JavadocDeclaration
`@param method` tag description is missing
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/InstanceAddressURL.java`
#### Snippet
```java
     * method parameter only exists in ServiceInfo
     *
     * @param method
     * @param key
     * @return
```

### JavadocDeclaration
`@param key` tag description is missing
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/InstanceAddressURL.java`
#### Snippet
```java
     *
     * @param method
     * @param key
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/InstanceAddressURL.java`
#### Snippet
```java
     * @param method
     * @param key
     * @return
     */
    @Override
```

### JavadocDeclaration
`@param oldUrlInvokerMap` tag description is missing
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/ServiceDiscoveryRegistryDirectory.java`
#### Snippet
```java
     * If set attribute of url: refer.autodestroy=false, the invokers will only increase without decreasing,there may be a refer leak
     *
     * @param oldUrlInvokerMap
     * @param newUrlInvokerMap
     */
```

### JavadocDeclaration
`@param newUrlInvokerMap` tag description is missing
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/ServiceDiscoveryRegistryDirectory.java`
#### Snippet
```java
     *
     * @param oldUrlInvokerMap
     * @param newUrlInvokerMap
     */
    private void destroyUnusedInvokers(Map<ProtocolServiceKeyWithAddress, Invoker<T>> oldUrlInvokerMap, Map<ProtocolServiceKeyWithAddress, Invoker<T>> newUrlInvokerMap) {
```

### JavadocDeclaration
`@param urls` tag description is missing
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/ServiceDiscoveryRegistryDirectory.java`
#### Snippet
```java
     *
     * @param oldUrlInvokerMap it might be modified during the process.
     * @param urls
     * @return invokers
     */
```

### JavadocDeclaration
`@param event` tag description is missing
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/event/listener/ServiceInstancesChangedListener.java`
#### Snippet
```java

    /**
     * @param event
     */
    private synchronized void doOnEvent(ServiceInstancesChangedEvent event) {
```

### JavadocDeclaration
`@param url` tag description is missing
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/integration/RegistryProtocolListener.java`
#### Snippet
```java
     * @param registryProtocol RegistryProtocol instance
     * @param invoker          invoker
     * @param url
     * @see RegistryProtocol#refer(Class, URL)
     */
```

### JavadocDeclaration
Wrong tag `MigrationRule`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/migration/MigrationRuleListener.java`
#### Snippet
```java

/**
 * Listens to {@MigrationRule} from Config Center.
 * <p>
 * - Migration rule is of consumer application scope.
```

### JavadocDeclaration
`@param applicationEventPublisher` tag description is missing
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/ServiceBean.java`
#### Snippet
```java

    /**
     * @param applicationEventPublisher
     * @since 2.6.5
     */
```

### JavadocDeclaration
Wrong tag `export`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/ServiceBean.java`
#### Snippet
```java
 * ServiceFactoryBean
 *
 * @export
 */
public class ServiceBean<T> extends ServiceConfig<T> implements InitializingBean, DisposableBean,
```

### JavadocDeclaration
`@param factoryMethodMetadata` tag description is missing
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/SpringCompatUtils.java`
#### Snippet
```java
     * </pre>
     *
     * @param factoryMethodMetadata
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/SpringCompatUtils.java`
#### Snippet
```java
     *
     * @param factoryMethodMetadata
     * @return
     */
    public static Class getGenericTypeOfReturnType(MethodMetadata factoryMethodMetadata) {
```

### JavadocDeclaration
`@param beanFactory` tag description is missing
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/DubboBeanUtils.java`
#### Snippet
```java
     * in order to enable the registered BeanFactoryPostProcessor bean to be loaded and executed.
     *
     * @param beanFactory
     * @param registry
     * @see DubboInfraBeanRegisterPostProcessor
```

### JavadocDeclaration
`@param registry` tag description is missing
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/DubboBeanUtils.java`
#### Snippet
```java
     *
     * @param beanFactory
     * @param registry
     * @see DubboInfraBeanRegisterPostProcessor
     * @see org.springframework.context.support.PostProcessorRegistrationDelegate#invokeBeanFactoryPostProcessors(org.springframework.beans.factory.config.ConfigurableListableBeanFactory, java.util.List)
```

### JavadocDeclaration
`@param urls` tag description is missing
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/integration/RegistryDirectory.java`
#### Snippet
```java
     *
     * @param oldUrlInvokerMap it might be modified during the process.
     * @param urls
     * @return invokers
     */
```

### JavadocDeclaration
`@param urls` tag description is missing
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/integration/RegistryDirectory.java`
#### Snippet
```java

    /**
     * @param urls
     * @return null : no routers ,do nothing
     * else :routers list
```

### JavadocDeclaration
`@param providerUrl` tag description is missing
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/integration/RegistryDirectory.java`
#### Snippet
```java
     * Merge url parameters. the order is: override > -D >Consumer > Provider
     *
     * @param providerUrl
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/integration/RegistryDirectory.java`
#### Snippet
```java
     *
     * @param providerUrl
     * @return
     */
    private URL mergeUrl(URL providerUrl) {
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/ReferenceBean.java`
#### Snippet
```java
    /**
     * The interface of this ReferenceBean, for injection purpose
     * @return
     */
    public Class<?> getInterfaceClass() {
```

### JavadocDeclaration
`@param attributes` tag description is missing
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/AnnotationPropertyValuesAdapter.java`
#### Snippet
```java

    /**
     * @param attributes
     * @param propertyResolver
     * @param ignoreAttributeNames
```

### JavadocDeclaration
`@param propertyResolver` tag description is missing
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/AnnotationPropertyValuesAdapter.java`
#### Snippet
```java
    /**
     * @param attributes
     * @param propertyResolver
     * @param ignoreAttributeNames
     * @since 2.7.3
```

### JavadocDeclaration
`@param ignoreAttributeNames` tag description is missing
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/AnnotationPropertyValuesAdapter.java`
#### Snippet
```java
     * @param attributes
     * @param propertyResolver
     * @param ignoreAttributeNames
     * @since 2.7.3
     */
```

### JavadocDeclaration
`@param testPkg` tag description is missing
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/ServicePackagesHolder.java`
#### Snippet
```java
    /**
     * Whether test package is sub package of parent package
     * @param testPkg
     * @param parent
     * @return
```

### JavadocDeclaration
`@param parent` tag description is missing
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/ServicePackagesHolder.java`
#### Snippet
```java
     * Whether test package is sub package of parent package
     * @param testPkg
     * @param parent
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/ServicePackagesHolder.java`
#### Snippet
```java
     * @param testPkg
     * @param parent
     * @return
     */
    private boolean isSubPackage(String testPkg, String parent) {
```

### JavadocDeclaration
`@param attributes` tag description is missing
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/ServiceBeanNameBuilder.java`
#### Snippet
```java

    /**
     * @param attributes
     * @param defaultInterfaceClass
     * @param environment
```

### JavadocDeclaration
`@param defaultInterfaceClass` tag description is missing
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/ServiceBeanNameBuilder.java`
#### Snippet
```java
    /**
     * @param attributes
     * @param defaultInterfaceClass
     * @param environment
     * @return
```

### JavadocDeclaration
`@param parameters` tag description is missing
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/DubboAnnotationUtils.java`
#### Snippet
```java
     * </p>
     *
     * @param parameters
     * @return
     */
```

### JavadocDeclaration
`@param environment` tag description is missing
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/ServiceBeanNameBuilder.java`
#### Snippet
```java
     * @param attributes
     * @param defaultInterfaceClass
     * @param environment
     * @return
     * @since 2.7.3
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/DubboAnnotationUtils.java`
#### Snippet
```java
     *
     * @param parameters
     * @return
     */
    public static Map<String, String> convertParameters(String[] parameters) {
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/ServiceBeanNameBuilder.java`
#### Snippet
```java
     * @param defaultInterfaceClass
     * @param environment
     * @return
     * @since 2.7.3
     */
```

### JavadocDeclaration
`@param element` tag description is missing
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/schema/AnnotationBeanDefinitionParser.java`
#### Snippet
```java
     * </prev>
     *
     * @param element
     * @param parserContext
     * @param builder
```

### JavadocDeclaration
`@param parserContext` tag description is missing
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/schema/AnnotationBeanDefinitionParser.java`
#### Snippet
```java
     *
     * @param element
     * @param parserContext
     * @param builder
     */
```

### JavadocDeclaration
`@param builder` tag description is missing
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/schema/AnnotationBeanDefinitionParser.java`
#### Snippet
```java
     * @param element
     * @param parserContext
     * @param builder
     */
    @Override
```

### JavadocDeclaration
Wrong tag `export`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/schema/DubboNamespaceHandler.java`
#### Snippet
```java
 * DubboNamespaceHandler
 *
 * @export
 */
public class DubboNamespaceHandler extends NamespaceHandlerSupport implements ConfigurableSourceBeanMetadataElement {
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/schema/DubboNamespaceHandler.java`
#### Snippet
```java
     * @param element       {@link Element}
     * @param parserContext {@link ParserContext}
     * @return
     * @since 2.7.5
     */
```

### JavadocDeclaration
Javadoc pointing to itself
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/AnnotationUtils.java`
#### Snippet
```java
     * {@link #tryGetMergedAnnotationAttributes(AnnotatedElement, Class, PropertyResolver, boolean, String...) merged annotation} first,
     * if failed, and then to get from
     * {@link #getAnnotationAttributes(AnnotatedElement, Class, PropertyResolver, boolean, boolean, String...) normal one}
     *
     * @param annotatedElement     {@link AnnotatedElement the annotated element}
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/AnnotationUtils.java`
#### Snippet
```java
     * @param ignoreDefaultValue     whether ignore default value or not
     * @param ignoreAttributeNames   the attribute names of annotation should be ignored
     * @return
     */
    public static Map<String, Object> getAttributes(Annotation annotation,
```

### JavadocDeclaration
`@param keepRunningOnSpringClosed` tag description is missing
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/context/DubboSpringInitContext.java`
#### Snippet
```java
    /**
     * Keep Dubbo running when spring is stopped
     * @param keepRunningOnSpringClosed
     */
    public void setKeepRunningOnSpringClosed(boolean keepRunningOnSpringClosed) {
```

### JavadocDeclaration
`@param moduleModel` tag description is missing
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/context/DubboSpringInitContext.java`
#### Snippet
```java
     * Change the binding ModuleModel, the ModuleModel and DubboBootstrap must be matched.
     *
     * @param moduleModel
     */
    public void setModuleModel(ModuleModel moduleModel) {
```

### JavadocDeclaration
Wrong tag `scene`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/context/DubboBootstrapStartStopListenerSpringAdapter.java`
#### Snippet
```java
 * convcert Dubbo bootstrap event to spring environment.
 *
 * @scene 2.7.9
 */
@Deprecated
```

### JavadocDeclaration
`@param context` tag description is missing
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/context/DubboSpringInitCustomizer.java`
#### Snippet
```java
     * it is recommended to register a custom {@link BeanFactoryPostProcessor} to do so.</p>
     *
     * @param context
     */
    void customize(DubboSpringInitContext context);
```

### JavadocDeclaration
`@param metadata` tag description is missing
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/AbstractAnnotationBeanPostProcessor.java`
#### Snippet
```java
     * Prepare injection data after found injection elements
     *
     * @param metadata
     * @throws Exception
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/AbstractAnnotationBeanPostProcessor.java`
#### Snippet
```java
     *
     * @param metadata
     * @throws Exception
     */
    protected void prepareInjection(AnnotatedInjectionMetadata metadata) throws Exception {
```

### JavadocDeclaration
`@param refServiceBeanName` tag description is missing
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/ServiceAnnotationPostProcessor.java`
#### Snippet
```java
     * }
     * </pre>
     * @param refServiceBeanName
     * @param refServiceBeanDefinition
     * @param attributes
```

### JavadocDeclaration
`@param refServiceBeanDefinition` tag description is missing
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/ServiceAnnotationPostProcessor.java`
#### Snippet
```java
     * </pre>
     * @param refServiceBeanName
     * @param refServiceBeanDefinition
     * @param attributes
     */
```

### JavadocDeclaration
`@param attributes` tag description is missing
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/ServiceAnnotationPostProcessor.java`
#### Snippet
```java
     * @param refServiceBeanName
     * @param refServiceBeanDefinition
     * @param attributes
     */
    private void processAnnotatedBeanDefinition(String refServiceBeanName, AnnotatedBeanDefinition refServiceBeanDefinition, Map<String, Object> attributes) {
```

### JavadocDeclaration
`@param serviceAnnotationAttributes` tag description is missing
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/ServiceAnnotationPostProcessor.java`
#### Snippet
```java
     *
     *
     * @param serviceAnnotationAttributes
     * @param serviceInterface
     * @param refServiceBeanName
```

### JavadocDeclaration
`@param serviceInterface` tag description is missing
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/ServiceAnnotationPostProcessor.java`
#### Snippet
```java
     *
     * @param serviceAnnotationAttributes
     * @param serviceInterface
     * @param refServiceBeanName
     * @return
```

### JavadocDeclaration
`@param refServiceBeanName` tag description is missing
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/ServiceAnnotationPostProcessor.java`
#### Snippet
```java
     * @param serviceAnnotationAttributes
     * @param serviceInterface
     * @param refServiceBeanName
     * @return
     * @since 2.7.3
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/ServiceAnnotationPostProcessor.java`
#### Snippet
```java
     * @param serviceInterface
     * @param refServiceBeanName
     * @return
     * @since 2.7.3
     */
```

### JavadocDeclaration
`@param serviceAnnotationAttributes` tag description is missing
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/ServiceAnnotationPostProcessor.java`
#### Snippet
```java
     * Generates the bean name of {@link ServiceBean}
     *
     * @param serviceAnnotationAttributes
     * @param serviceInterface              the class of interface annotated {@link Service}
     * @return ServiceBean@interfaceClassName#annotatedServiceBeanName
```

### JavadocDeclaration
`@param beanDefinitionHolder` tag description is missing
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/ServiceAnnotationPostProcessor.java`
#### Snippet
```java
     * Registers {@link ServiceBean} from new annotated {@link Service} {@link BeanDefinition}
     *
     * @param beanDefinitionHolder
     * @see ServiceBean
     * @see BeanDefinition
```

### JavadocDeclaration
`@param beanName` tag description is missing
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/ReferenceAnnotationBeanPostProcessor.java`
#### Snippet
```java
     * </pre>
     *
     * @param beanName
     * @param beanDefinition
     */
```

### JavadocDeclaration
`@param beanDefinition` tag description is missing
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/ReferenceAnnotationBeanPostProcessor.java`
#### Snippet
```java
     *
     * @param beanName
     * @param beanDefinition
     */
    private void processReferenceAnnotatedBeanDefinition(String beanName, AnnotatedBeanDefinition beanDefinition) {
```

### JavadocDeclaration
Wrong tag `revised`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/context/annotation/EnableDubboConfig.java`
#### Snippet
```java
     *
     * @return the default value is <code>true</code>
     * @revised 2.5.9
     */
    boolean multiple() default true;
```

### JavadocDeclaration
`@param prefix` tag description is missing
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/context/properties/DubboConfigBinder.java`
#### Snippet
```java
     * Bind the properties to Dubbo Config Object under specified prefix.
     *
     * @param prefix
     * @param dubboConfig
     */
```

### JavadocDeclaration
`@param dubboConfig` tag description is missing
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/context/properties/DubboConfigBinder.java`
#### Snippet
```java
     *
     * @param prefix
     * @param dubboConfig
     */
    <C extends AbstractConfig> void bind(String prefix, C dubboConfig);
```

### JavadocDeclaration
Wrong tag `revised`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/context/annotation/DubboConfigConfiguration.java`
#### Snippet
```java
 * Dubbo {@link AbstractConfig Config} {@link Configuration}
 *
 * @revised 2.7.5
 * @see Configuration
 * @see EnableConfigurationBeanBindings
```

### JavadocDeclaration
Wrong tag `export`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/schema/DubboBeanDefinitionParser.java`
#### Snippet
```java
 * AbstractBeanDefinitionParser
 *
 * @export
 */
public class DubboBeanDefinitionParser implements BeanDefinitionParser {
```

### JavadocDeclaration
`@param providerUrl` tag description is missing
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/integration/RegistryProtocol.java`
#### Snippet
```java
         * Get existing configuration rule and override provider url before exporting.
         *
         * @param providerUrl
         * @param <T>
         * @return
```

### JavadocDeclaration
`@param ` tag description is missing
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/integration/RegistryProtocol.java`
#### Snippet
```java
         *
         * @param providerUrl
         * @param <T>
         * @return
         */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/integration/RegistryProtocol.java`
#### Snippet
```java
         * @param providerUrl
         * @param <T>
         * @return
         */
        private <T> URL overrideUrl(URL providerUrl) {
```

### JavadocDeclaration
`@param registryUrl` tag description is missing
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/integration/RegistryProtocol.java`
#### Snippet
```java
     * Get an instance of registry based on the address of invoker
     *
     * @param registryUrl
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/integration/RegistryProtocol.java`
#### Snippet
```java
     *
     * @param registryUrl
     * @return
     */
    protected Registry getRegistry(final URL registryUrl) {
```

### JavadocDeclaration
`@param originInvoker` tag description is missing
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/integration/RegistryProtocol.java`
#### Snippet
```java
     * Get the address of the providerUrl through the url of the invoker
     *
     * @param originInvoker
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/integration/RegistryProtocol.java`
#### Snippet
```java
     *
     * @param originInvoker
     * @return
     */
    private URL getProviderUrl(final Invoker<?> originInvoker) {
```

### JavadocDeclaration
`@param providerUrl` tag description is missing
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/integration/RegistryProtocol.java`
#### Snippet
```java
     * Return the url that is registered to the registry and filter the url parameter once
     *
     * @param providerUrl
     * @return url to registry.
     */
```

### JavadocDeclaration
`@param originInvoker` tag description is missing
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/integration/RegistryProtocol.java`
#### Snippet
```java
     * Get the key cached in bounds by invoker
     *
     * @param originInvoker
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/integration/RegistryProtocol.java`
#### Snippet
```java
     *
     * @param originInvoker
     * @return
     */
    private String getProviderUrlKey(final Invoker<?> originInvoker) {
```

### JavadocDeclaration
`@param invoker` tag description is missing
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/integration/RegistryProtocol.java`
#### Snippet
```java

        /**
         * @param invoker
         * @param url     invoker.getUrl return this value
         */
```

### JavadocDeclaration
`@param originInvoker` tag description is missing
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/integration/RegistryProtocol.java`
#### Snippet
```java
     * Reexport the invoker of the modified url
     *
     * @param originInvoker
     * @param newInvokerUrl
     * @param <T>
```

### JavadocDeclaration
`@param newInvokerUrl` tag description is missing
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/integration/RegistryProtocol.java`
#### Snippet
```java
     *
     * @param originInvoker
     * @param newInvokerUrl
     * @param <T>
     */
```

### JavadocDeclaration
`@param ` tag description is missing
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/integration/RegistryProtocol.java`
#### Snippet
```java
     * @param originInvoker
     * @param newInvokerUrl
     * @param <T>
     */
    @SuppressWarnings("unchecked")
```

### JavadocDeclaration
`@param injvm` tag description is missing
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/reference/ReferenceBeanBuilder.java`
#### Snippet
```java

    /**
     * @param injvm
     * @deprecated instead, use the parameter <b>scope</b> to judge if it's in jvm, scope=local
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/reference/ReferenceBeanManager.java`
#### Snippet
```java
     * Initialize all reference beans, call at Dubbo starting
     *
     * @throws Exception
     */
    public void prepareReferenceBeans() throws Exception {
```

### JavadocDeclaration
`@param referenceBean` tag description is missing
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/reference/ReferenceBeanManager.java`
#### Snippet
```java
     * NOTE: This method should only call after all dubbo config beans and all property resolvers is loaded.
     *
     * @param referenceBean
     * @throws Exception
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/reference/ReferenceBeanManager.java`
#### Snippet
```java
     *
     * @param referenceBean
     * @throws Exception
     */
    private synchronized void initReferenceBean(ReferenceBean referenceBean) throws Exception {
```

### JavadocDeclaration
`@throws` tag description is missing
in `dubbo-filter/dubbo-filter-validation/src/main/java/org/apache/dubbo/validation/support/jvalidation/JValidator.java`
#### Snippet
```java
     * @param parameterClassName generated parameterClassName
     * @return Class<?> generated methodParameterClass
     * @throws Exception
     */
    private static Class<?> generateMethodParameterClass(Class<?> clazz, Method method, String parameterClassName)
```

### JavadocDeclaration
`@param group` tag description is missing
in `dubbo-configcenter/dubbo-configcenter-apollo/src/main/java/org/apache/dubbo/configcenter/support/apollo/ApolloDynamicConfiguration.java`
#### Snippet
```java
     *
     * @param key     default value is 'dubbo.properties', currently useless for Apollo.
     * @param group
     * @param timeout
     * @return
```

### JavadocDeclaration
`@param timeout` tag description is missing
in `dubbo-configcenter/dubbo-configcenter-apollo/src/main/java/org/apache/dubbo/configcenter/support/apollo/ApolloDynamicConfiguration.java`
#### Snippet
```java
     * @param key     default value is 'dubbo.properties', currently useless for Apollo.
     * @param group
     * @param timeout
     * @return
     * @throws IllegalStateException
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-configcenter/dubbo-configcenter-apollo/src/main/java/org/apache/dubbo/configcenter/support/apollo/ApolloDynamicConfiguration.java`
#### Snippet
```java
     * @param group
     * @param timeout
     * @return
     * @throws IllegalStateException
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `dubbo-configcenter/dubbo-configcenter-apollo/src/main/java/org/apache/dubbo/configcenter/support/apollo/ApolloDynamicConfiguration.java`
#### Snippet
```java
     * @param timeout
     * @return
     * @throws IllegalStateException
     */
    @Override
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-configcenter/dubbo-configcenter-apollo/src/main/java/org/apache/dubbo/configcenter/support/apollo/ApolloDynamicConfiguration.java`
#### Snippet
```java
     * @param key   property key the native listener will listen on
     * @param group to distinguish different set of properties
     * @return
     */
    private ApolloListener createTargetListener(String key, String group) {
```

### JavadocDeclaration
`@throws` tag description is missing
in `dubbo-serialization/dubbo-serialization-api/src/main/java/org/apache/dubbo/common/serialize/DataInput.java`
#### Snippet
```java
     *
     * @return integer.
     * @throws IOException
     */
    int readInt() throws IOException;
```

### JavadocDeclaration
`@throws` tag description is missing
in `dubbo-serialization/dubbo-serialization-api/src/main/java/org/apache/dubbo/common/serialize/DataInput.java`
#### Snippet
```java
     *
     * @return short.
     * @throws IOException
     */
    short readShort() throws IOException;
```

### JavadocDeclaration
`@throws` tag description is missing
in `dubbo-serialization/dubbo-serialization-api/src/main/java/org/apache/dubbo/common/serialize/DataInput.java`
#### Snippet
```java
     *
     * @return float.
     * @throws IOException
     */
    float readFloat() throws IOException;
```

### JavadocDeclaration
`@throws` tag description is missing
in `dubbo-serialization/dubbo-serialization-api/src/main/java/org/apache/dubbo/common/serialize/DataInput.java`
#### Snippet
```java
     *
     * @return byte array.
     * @throws IOException
     */
    byte[] readBytes() throws IOException;
```

### JavadocDeclaration
`@throws` tag description is missing
in `dubbo-serialization/dubbo-serialization-api/src/main/java/org/apache/dubbo/common/serialize/DataInput.java`
#### Snippet
```java
     *
     * @return string.
     * @throws IOException
     */
    String readUTF() throws IOException;
```

### JavadocDeclaration
`@throws` tag description is missing
in `dubbo-serialization/dubbo-serialization-api/src/main/java/org/apache/dubbo/common/serialize/DataInput.java`
#### Snippet
```java
     *
     * @return byte value.
     * @throws IOException
     */
    byte readByte() throws IOException;
```

### JavadocDeclaration
`@throws` tag description is missing
in `dubbo-serialization/dubbo-serialization-api/src/main/java/org/apache/dubbo/common/serialize/DataInput.java`
#### Snippet
```java
     *
     * @return double.
     * @throws IOException
     */
    double readDouble() throws IOException;
```

### JavadocDeclaration
`@throws` tag description is missing
in `dubbo-serialization/dubbo-serialization-api/src/main/java/org/apache/dubbo/common/serialize/DataInput.java`
#### Snippet
```java
     *
     * @return boolean.
     * @throws IOException
     */
    boolean readBool() throws IOException;
```

### JavadocDeclaration
`@throws` tag description is missing
in `dubbo-serialization/dubbo-serialization-api/src/main/java/org/apache/dubbo/common/serialize/DataInput.java`
#### Snippet
```java
     *
     * @return long.
     * @throws IOException
     */
    long readLong() throws IOException;
```

### JavadocDeclaration
`@throws` tag description is missing
in `dubbo-filter/dubbo-filter-validation/src/main/java/org/apache/dubbo/validation/support/jvalidation/JValidatorNew.java`
#### Snippet
```java
     * @param parameterClassName generated parameterClassName
     * @return Class<?> generated methodParameterClass
     * @throws Exception
     */
    private static Class<?> generateMethodParameterClass(Class<?> clazz, Method method, String parameterClassName)
```

### JavadocDeclaration
`@throws` tag description is missing
in `dubbo-serialization/dubbo-serialization-api/src/main/java/org/apache/dubbo/common/serialize/Serialization.java`
#### Snippet
```java
     * @param input the underlying input stream
     * @return deserializer
     * @throws IOException
     */
    @Adaptive
```

### JavadocDeclaration
`@throws` tag description is missing
in `dubbo-serialization/dubbo-serialization-api/src/main/java/org/apache/dubbo/common/serialize/Serialization.java`
#### Snippet
```java
     * @param output the underlying output stream
     * @return serializer
     * @throws IOException
     */
    @Adaptive
```

### JavadocDeclaration
`@throws` tag description is missing
in `dubbo-serialization/dubbo-serialization-api/src/main/java/org/apache/dubbo/common/serialize/DataOutput.java`
#### Snippet
```java
     *
     * @param v value.
     * @throws IOException
     */
    void writeBool(boolean v) throws IOException;
```

### JavadocDeclaration
`@throws` tag description is missing
in `dubbo-serialization/dubbo-serialization-api/src/main/java/org/apache/dubbo/common/serialize/DataOutput.java`
#### Snippet
```java
     *
     * @param v value.
     * @throws IOException
     */
    void writeFloat(float v) throws IOException;
```

### JavadocDeclaration
`@throws` tag description is missing
in `dubbo-serialization/dubbo-serialization-api/src/main/java/org/apache/dubbo/common/serialize/DataOutput.java`
#### Snippet
```java
     *
     * @param v value.
     * @throws IOException
     */
    void writeInt(int v) throws IOException;
```

### JavadocDeclaration
`@throws` tag description is missing
in `dubbo-serialization/dubbo-serialization-api/src/main/java/org/apache/dubbo/common/serialize/DataOutput.java`
#### Snippet
```java
     *
     * @param v value.
     * @throws IOException
     */
    void writeByte(byte v) throws IOException;
```

### JavadocDeclaration
`@throws` tag description is missing
in `dubbo-serialization/dubbo-serialization-api/src/main/java/org/apache/dubbo/common/serialize/DataOutput.java`
#### Snippet
```java
     *
     * @param v value.
     * @throws IOException
     */
    void writeDouble(double v) throws IOException;
```

### JavadocDeclaration
`@throws` tag description is missing
in `dubbo-serialization/dubbo-serialization-api/src/main/java/org/apache/dubbo/common/serialize/DataOutput.java`
#### Snippet
```java
     *
     * @param v value.
     * @throws IOException
     */
    void writeLong(long v) throws IOException;
```

### JavadocDeclaration
`@throws` tag description is missing
in `dubbo-serialization/dubbo-serialization-api/src/main/java/org/apache/dubbo/common/serialize/DataOutput.java`
#### Snippet
```java
     *
     * @param v value.
     * @throws IOException
     */
    void writeUTF(String v) throws IOException;
```

### JavadocDeclaration
`@throws` tag description is missing
in `dubbo-serialization/dubbo-serialization-api/src/main/java/org/apache/dubbo/common/serialize/DataOutput.java`
#### Snippet
```java
     * @param off the start offset in the data.
     * @param len the number of bytes that are written.
     * @throws IOException
     */
    void writeBytes(byte[] v, int off, int len) throws IOException;
```

### JavadocDeclaration
`@throws` tag description is missing
in `dubbo-serialization/dubbo-serialization-api/src/main/java/org/apache/dubbo/common/serialize/DataOutput.java`
#### Snippet
```java
     * Flush buffer.
     *
     * @throws IOException
     */
    void flushBuffer() throws IOException;
```

### JavadocDeclaration
`@throws` tag description is missing
in `dubbo-serialization/dubbo-serialization-api/src/main/java/org/apache/dubbo/common/serialize/DataOutput.java`
#### Snippet
```java
     *
     * @param v value.
     * @throws IOException
     */
    void writeBytes(byte[] v) throws IOException;
```

### JavadocDeclaration
`@throws` tag description is missing
in `dubbo-serialization/dubbo-serialization-api/src/main/java/org/apache/dubbo/common/serialize/DataOutput.java`
#### Snippet
```java
     *
     * @param v value.
     * @throws IOException
     */
    void writeShort(short v) throws IOException;
```

### JavadocDeclaration
`@param url` tag description is missing
in `dubbo-registry/dubbo-registry-nacos/src/main/java/org/apache/dubbo/registry/nacos/NacosServiceName.java`
#### Snippet
```java
     * Build an instance of {@link NacosServiceName}
     *
     * @param url
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-registry/dubbo-registry-nacos/src/main/java/org/apache/dubbo/registry/nacos/NacosServiceName.java`
#### Snippet
```java
     *
     * @param url
     * @return
     */
    public static NacosServiceName valueOf(URL url) {
```

### JavadocDeclaration
`@throws` tag description is missing
in `dubbo-plugin/dubbo-qos/src/main/java/org/apache/dubbo/qos/command/CommandExecutor.java`
#### Snippet
```java
     * @param commandContext command context
     * @return command execution result
     * @throws NoSuchCommandException
     */
    String execute(CommandContext commandContext) throws NoSuchCommandException, PermissionDenyException;
```

### JavadocDeclaration
`@param serviceName` tag description is missing
in `dubbo-registry/dubbo-registry-nacos/src/main/java/org/apache/dubbo/registry/nacos/NacosRegistry.java`
#### Snippet
```java
     * Verify whether it is a dubbo service
     *
     * @param serviceName
     * @return
     * @since 2.7.12
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-registry/dubbo-registry-nacos/src/main/java/org/apache/dubbo/registry/nacos/NacosRegistry.java`
#### Snippet
```java
     *
     * @param serviceName
     * @return
     * @since 2.7.12
     */
```

### JavadocDeclaration
`@param url` tag description is missing
in `dubbo-registry/dubbo-registry-nacos/src/main/java/org/apache/dubbo/registry/nacos/NacosRegistry.java`
#### Snippet
```java
     * to fix bug with https://github.com/apache/dubbo/issues/5442
     *
     * @param url
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-registry/dubbo-registry-nacos/src/main/java/org/apache/dubbo/registry/nacos/NacosRegistry.java`
#### Snippet
```java
     *
     * @param url
     * @return
     */
    private boolean isServiceNamesWithCompatibleMode(final URL url) {
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-configcenter/dubbo-configcenter-zookeeper/src/main/java/org/apache/dubbo/configcenter/support/zookeeper/ZookeeperDynamicConfiguration.java`
#### Snippet
```java
    /**
     * @param key e.g., {service}.configurators, {service}.tagrouters, {group}.dubbo.properties
     * @return
     */
    @Override
```

### JavadocDeclaration
Wrong tag `Params`
in `dubbo-metrics/dubbo-metrics-api/src/main/java/org/apache/dubbo/metrics/collector/ApplicationMetricsCollector.java`
#### Snippet
```java
 * registration center, configuration center and other scenarios
 *
 * @Params <T>  metrics type
 */
public interface ApplicationMetricsCollector<E extends TimeCounterEvent> extends MetricsCollector<E> {
```

### JavadocDeclaration
`@param url` tag description is missing
in `dubbo-filter/dubbo-filter-cache/src/main/java/org/apache/dubbo/cache/CacheFactory.java`
#### Snippet
```java
     * CacheFactory implementation class needs to implement this return underlying cache instance for method against
     * url and invocation.
     * @param url
     * @param invocation
     * @return Instance of Cache containing cached value against method url and invocation.
```

### JavadocDeclaration
`@param invocation` tag description is missing
in `dubbo-filter/dubbo-filter-cache/src/main/java/org/apache/dubbo/cache/CacheFactory.java`
#### Snippet
```java
     * url and invocation.
     * @param url
     * @param invocation
     * @return Instance of Cache containing cached value against method url and invocation.
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `dubbo-filter/dubbo-filter-cache/src/main/java/org/apache/dubbo/cache/filter/CacheFilter.java`
#### Snippet
```java
     * @param invocation invocation.
     * @return Cache returned value if found by the underlying cache store. If cache miss it will call target method.
     * @throws RpcException
     */
    @Override
```

### JavadocDeclaration
`@param url` tag description is missing
in `dubbo-filter/dubbo-filter-cache/src/main/java/org/apache/dubbo/cache/support/threadlocal/ThreadLocalCache.java`
#### Snippet
```java
     * Taken URL as an argument to create an instance of ThreadLocalCache. In this version of implementation constructor
     * argument is not getting used in the scope of this class.
     * @param url
     */
    public ThreadLocalCache(URL url) {
```

### JavadocDeclaration
`@param annotations` tag description is missing
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/rest/ServiceRestMetadataResolver.java`
#### Snippet
```java
     * @param processingEnvironment {@link ProcessingEnvironment}
     * @param serviceType           Dubbo service type or interface
     * @param annotations
     * @return non-null
     */
```

### JavadocDeclaration
Wrong tag `Service`
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/rest/ServiceRestMetadataAnnotationProcessor.java`
#### Snippet
```java
 * The {@link Processor} class to generate the metadata of REST from the classes that are annotated by Dubbo's
 *
 * @Service
 * @see Processor
 * @since 2.7.6
```

### JavadocDeclaration
`@param serviceType` tag description is missing
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/rest/AbstractServiceRestMetadataResolver.java`
#### Snippet
```java
     *
     * @param processingEnv        {@link ProcessingEnvironment}
     * @param serviceType
     * @param serviceInterfaceType
     * @param serviceMethod
```

### JavadocDeclaration
`@param serviceInterfaceType` tag description is missing
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/rest/AbstractServiceRestMetadataResolver.java`
#### Snippet
```java
     * @param processingEnv        {@link ProcessingEnvironment}
     * @param serviceType
     * @param serviceInterfaceType
     * @param serviceMethod
     * @return <code>null</code> if can't be found
```

### JavadocDeclaration
`@param serviceMethod` tag description is missing
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/rest/AbstractServiceRestMetadataResolver.java`
#### Snippet
```java
     * @param serviceType
     * @param serviceInterfaceType
     * @param serviceMethod
     * @return <code>null</code> if can't be found
     */
```

### JavadocDeclaration
`@param exporter` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/ExporterListener.java`
#### Snippet
```java
     * The exporter exported.
     *
     * @param exporter
     * @throws RpcException
     * @see org.apache.dubbo.rpc.Protocol#export(Invoker)
```

### JavadocDeclaration
`@throws` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/ExporterListener.java`
#### Snippet
```java
     *
     * @param exporter
     * @throws RpcException
     * @see org.apache.dubbo.rpc.Protocol#export(Invoker)
     */
```

### JavadocDeclaration
`@param exporter` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/ExporterListener.java`
#### Snippet
```java
     * The exporter unexported.
     *
     * @param exporter
     * @throws RpcException
     * @see org.apache.dubbo.rpc.Exporter#unexport()
```

### JavadocDeclaration
`@throws` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/ExporterListener.java`
#### Snippet
```java
     *
     * @param exporter
     * @throws RpcException
     * @see org.apache.dubbo.rpc.Exporter#unexport()
     */
```

### JavadocDeclaration
Illegal character
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/FutureContext.java`
#### Snippet
```java

/**
 * Used for async call scenario. But if the method you are calling has a {@link CompletableFuture<?>} signature
 * you do not need to use this class since you will get a Future response directly.
 * <p>
```

### JavadocDeclaration
`@param ` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/FutureContext.java`
#### Snippet
```java
     * get future.
     *
     * @param <T>
     * @return future
     */
```

### JavadocDeclaration
`@param future` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/FutureContext.java`
#### Snippet
```java
     * set future.
     *
     * @param future
     */
    public void setFuture(CompletableFuture<?> future) {
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-remoting/dubbo-remoting-zookeeper/src/main/java/org/apache/dubbo/remoting/zookeeper/curator/CuratorZookeeperClient.java`
#### Snippet
```java
     * just for unit test
     *
     * @return
     */
    CuratorFramework getClient() {
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/Protocol.java`
#### Snippet
```java
     * Get all servers serving this protocol
     *
     * @return
     */
    default List<ProtocolServer> getServers() {
```

### JavadocDeclaration
`@param invoker` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/InvokerListener.java`
#### Snippet
```java
     * The invoker destroyed.
     *
     * @param invoker
     * @see org.apache.dubbo.rpc.Invoker#destroy()
     */
```

### JavadocDeclaration
`@param invoker` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/InvokerListener.java`
#### Snippet
```java
     * The invoker referred
     *
     * @param invoker
     * @throws RpcException
     * @see org.apache.dubbo.rpc.Protocol#refer(Class, org.apache.dubbo.common.URL)
```

### JavadocDeclaration
`@throws` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/InvokerListener.java`
#### Snippet
```java
     *
     * @param invoker
     * @throws RpcException
     * @see org.apache.dubbo.rpc.Protocol#refer(Class, org.apache.dubbo.common.URL)
     */
```

### JavadocDeclaration
`@param invocation` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/Invoker.java`
#### Snippet
```java
     * invoke.
     *
     * @param invocation
     * @return result
     * @throws RpcException
```

### JavadocDeclaration
`@throws` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/Invoker.java`
#### Snippet
```java
     * @param invocation
     * @return result
     * @throws RpcException
     */
    Result invoke(Invocation invocation) throws RpcException;
```

### JavadocDeclaration
`@param map` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/Result.java`
#### Snippet
```java
     * Replace the existing attachments with the specified param.
     *
     * @param map
     */
    @Experimental("Experiment api for supporting Object transmission")
```

### JavadocDeclaration
`@param map` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/Result.java`
#### Snippet
```java
     * Replace the existing attachments with the specified param.
     *
     * @param map
     */
    void setAttachments(Map<String, String> map);
```

### JavadocDeclaration
Javadoc pointing to itself
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/Result.java`
#### Snippet
```java
     * <p>
     * Just as the method name implies, this method will guarantee the callback being triggered under the same context as when the call was started,
     * see implementation in {@link Result#whenCompleteWithContext(BiConsumer)}
     *
     * @param fn
```

### JavadocDeclaration
`@param fn` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/Result.java`
#### Snippet
```java
     * see implementation in {@link Result#whenCompleteWithContext(BiConsumer)}
     *
     * @param fn
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/Result.java`
#### Snippet
```java
     *
     * @param fn
     * @return
     */
    Result whenCompleteWithContext(BiConsumer<Result, Throwable> fn);
```

### JavadocDeclaration
Wrong tag value
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/Result.java`
#### Snippet
```java
     *
     * @return result.
     * @throws if has exception throw it.
     */
    Object recreate() throws Throwable;
```

### JavadocDeclaration
`@param map` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/Result.java`
#### Snippet
```java
     * Add the specified map to existing attachments in this instance.
     *
     * @param map
     */
    void addAttachments(Map<String, String> map);
```

### JavadocDeclaration
`@param map` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/Result.java`
#### Snippet
```java
     * Add the specified map to existing attachments in this instance.
     *
     * @param map
     */
    @Experimental("Experiment api for supporting Object transmission")
```

### JavadocDeclaration
`@param invoker` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/ProxyFactory.java`
#### Snippet
```java
     * create proxy.
     *
     * @param invoker
     * @return proxy
     */
```

### JavadocDeclaration
`@param ` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/ProxyFactory.java`
#### Snippet
```java
     * create invoker.
     *
     * @param <T>
     * @param proxy
     * @param type
```

### JavadocDeclaration
`@param proxy` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/ProxyFactory.java`
#### Snippet
```java
     *
     * @param <T>
     * @param proxy
     * @param type
     * @param url
```

### JavadocDeclaration
`@param type` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/ProxyFactory.java`
#### Snippet
```java
     * @param <T>
     * @param proxy
     * @param type
     * @param url
     * @return invoker
```

### JavadocDeclaration
`@param url` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/ProxyFactory.java`
#### Snippet
```java
     * @param proxy
     * @param type
     * @param url
     * @return invoker
     */
```

### JavadocDeclaration
`@param invoker` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/ProxyFactory.java`
#### Snippet
```java
     * create proxy.
     *
     * @param invoker
     * @return proxy
     */
```

### JavadocDeclaration
Tag `serial` is not allowed here
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/Invocation.java`
#### Snippet
```java
     *
     * @return method name.
     * @serial
     */
    String getMethodName();
```

### JavadocDeclaration
Tag `serial` is not allowed here
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/Invocation.java`
#### Snippet
```java
     *
     * @return attachments.
     * @serial
     */
    Map<String, String> getAttachments();
```

### JavadocDeclaration
Tag `serial` is not allowed here
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/Invocation.java`
#### Snippet
```java
     *
     * @return attachment value.
     * @serial
     */
    String getAttachment(String key, String defaultValue);
```

### JavadocDeclaration
Tag `serial` is not allowed here
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/Invocation.java`
#### Snippet
```java
     *
     * @return arguments.
     * @serial
     */
    Object[] getArguments();
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/Invocation.java`
#### Snippet
```java
     * get the interface name
     *
     * @return
     */
    String getServiceName();
```

### JavadocDeclaration
Tag `serial` is not allowed here
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/Invocation.java`
#### Snippet
```java
     *
     * @return parameter types.
     * @serial
     */
    Class<?>[] getParameterTypes();
```

### JavadocDeclaration
Wrong tag `transient`
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/Invocation.java`
#### Snippet
```java
     *
     * @return invoker.
     * @transient
     */
    Invoker<?> getInvoker();
```

### JavadocDeclaration
Tag `serial` is not allowed here
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/Invocation.java`
#### Snippet
```java
     *
     * @return attachment value.
     * @serial
     */
    String getAttachment(String key);
```

### JavadocDeclaration
`@param key` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcContext.java`
#### Snippet
```java
     * remove value.
     *
     * @param key
     * @return value
     */
```

### JavadocDeclaration
`@param runnable` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcContext.java`
#### Snippet
```java
     * one way async call, send request only, and result is not required
     *
     * @param runnable
     */
    public void asyncCall(Runnable runnable) {
```

### JavadocDeclaration
`@param key` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcContext.java`
#### Snippet
```java
     * set value.
     *
     * @param key
     * @param value
     * @return context
```

### JavadocDeclaration
`@param value` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcContext.java`
#### Snippet
```java
     *
     * @param key
     * @param value
     * @return context
     */
```

### JavadocDeclaration
`@param attachment` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcContext.java`
#### Snippet
```java
     * set attachments
     *
     * @param attachment
     * @return context
     */
```

### JavadocDeclaration
`@param key` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcContext.java`
#### Snippet
```java
     * get attachment.
     *
     * @param key
     * @return attachment
     */
```

### JavadocDeclaration
`@param ` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcContext.java`
#### Snippet
```java
     * get future.
     *
     * @param <T>
     * @return future
     */
```

### JavadocDeclaration
`@param attachment` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcContext.java`
#### Snippet
```java
     * set attachments
     *
     * @param attachment
     * @return context
     */
```

### JavadocDeclaration
`@param host` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcContext.java`
#### Snippet
```java
     * set remote address.
     *
     * @param host
     * @param port
     * @return context
```

### JavadocDeclaration
`@param port` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcContext.java`
#### Snippet
```java
     *
     * @param host
     * @param port
     * @return context
     */
```

### JavadocDeclaration
`@param key` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcContext.java`
#### Snippet
```java
     * set attachment.
     *
     * @param key
     * @param value
     * @return context
```

### JavadocDeclaration
`@param value` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcContext.java`
#### Snippet
```java
     *
     * @param key
     * @param value
     * @return context
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcContext.java`
#### Snippet
```java

    /**
     * @return
     * @throws IllegalStateException
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcContext.java`
#### Snippet
```java
    /**
     * @return
     * @throws IllegalStateException
     */
    @SuppressWarnings("unchecked")
```

### JavadocDeclaration
Wrong tag `export`
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcContext.java`
#### Snippet
```java
 * The reason why using `ServiceContext` is to make API compatible with previous.
 *
 * @export
 * @see org.apache.dubbo.rpc.filter.ContextFilter
 */
```

### JavadocDeclaration
`@param key` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcContext.java`
#### Snippet
```java
     * remove attachment.
     *
     * @param key
     * @return context
     */
```

### JavadocDeclaration
`@param host` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcContext.java`
#### Snippet
```java
     * set local address.
     *
     * @param host
     * @param port
     * @return context
```

### JavadocDeclaration
`@param port` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcContext.java`
#### Snippet
```java
     *
     * @param host
     * @param port
     * @return context
     */
```

### JavadocDeclaration
`@param key` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcContext.java`
#### Snippet
```java
     * get value.
     *
     * @param key
     * @return value
     */
```

### JavadocDeclaration
`@param ` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcContext.java`
#### Snippet
```java
     * get CompletableFuture.
     *
     * @param <T>
     * @return future
     */
```

### JavadocDeclaration
`@param address` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcContext.java`
#### Snippet
```java
     * set local address.
     *
     * @param address
     * @return context
     */
```

### JavadocDeclaration
`@param future` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcContext.java`
#### Snippet
```java
     * set future.
     *
     * @param future
     */
    public void setFuture(CompletableFuture<?> future) {
```

### JavadocDeclaration
Tag `serial` is not allowed here
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcContext.java`
#### Snippet
```java
     * get parameter types.
     *
     * @serial
     */
    public Class<?>[] getParameterTypes() {
```

### JavadocDeclaration
`@param key` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcContext.java`
#### Snippet
```java
     * also see {@link #getObjectAttachment(String)}.
     *
     * @param key
     * @return attachment
     */
```

### JavadocDeclaration
`@param address` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcContext.java`
#### Snippet
```java
     * set remote address.
     *
     * @param address
     * @return context
     */
```

### JavadocDeclaration
`@param callable` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcContext.java`
#### Snippet
```java
     * Async invocation. Timeout will be handled even if <code>Future.get()</code> is not called.
     *
     * @param callable
     * @return get the return result from <code>future.get()</code>
     */
```

### JavadocDeclaration
`@param key` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcContextAttachment.java`
#### Snippet
```java
     * get value.
     *
     * @param key
     * @return value
     */
```

### JavadocDeclaration
`@param key` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcContextAttachment.java`
#### Snippet
```java
     * remove value.
     *
     * @param key
     * @return value
     */
```

### JavadocDeclaration
`@param key` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcContextAttachment.java`
#### Snippet
```java
     * remove attachment.
     *
     * @param key
     * @return context
     */
```

### JavadocDeclaration
`@param key` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcContextAttachment.java`
#### Snippet
```java
     * set value.
     *
     * @param key
     * @param value
     * @return context
```

### JavadocDeclaration
`@param value` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcContextAttachment.java`
#### Snippet
```java
     *
     * @param key
     * @param value
     * @return context
     */
```

### JavadocDeclaration
`@param key` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcContextAttachment.java`
#### Snippet
```java
     * get attachment.
     *
     * @param key
     * @return attachment
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcContextAttachment.java`
#### Snippet
```java

    /**
     * @return
     * @throws IllegalStateException
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcContextAttachment.java`
#### Snippet
```java
    /**
     * @return
     * @throws IllegalStateException
     */
    @SuppressWarnings("unchecked")
```

### JavadocDeclaration
`@param attachment` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcContextAttachment.java`
#### Snippet
```java
     * set attachments
     *
     * @param attachment
     * @return context
     */
```

### JavadocDeclaration
`@param key` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcContextAttachment.java`
#### Snippet
```java
     * also see {@link #getObjectAttachment(String)}.
     *
     * @param key
     * @return attachment
     */
```

### JavadocDeclaration
`@param key` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcContextAttachment.java`
#### Snippet
```java
     * set attachment.
     *
     * @param key
     * @param value
     * @return context
```

### JavadocDeclaration
`@param value` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcContextAttachment.java`
#### Snippet
```java
     *
     * @param key
     * @param value
     * @return context
     */
```

### JavadocDeclaration
`@param attachment` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcContextAttachment.java`
#### Snippet
```java
     * set attachments
     *
     * @param attachment
     * @return context
     */
```

### JavadocDeclaration
`@param url` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcStatus.java`
#### Snippet
```java

    /**
     * @param url
     */
    public static boolean beginCount(URL url, String methodName, int max) {
```

### JavadocDeclaration
`@param key` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcStatus.java`
#### Snippet
```java
     * get value.
     *
     * @param key
     * @return value
     */
```

### JavadocDeclaration
`@param url` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcStatus.java`
#### Snippet
```java

    /**
     * @param url
     */
    public static void removeStatus(URL url) {
```

### JavadocDeclaration
`@param url` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcStatus.java`
#### Snippet
```java

    /**
     * @param url
     * @param methodName
     * @return status
```

### JavadocDeclaration
`@param methodName` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcStatus.java`
#### Snippet
```java
    /**
     * @param url
     * @param methodName
     * @return status
     */
```

### JavadocDeclaration
`@param url` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcStatus.java`
#### Snippet
```java

    /**
     * @param url
     */
    public static void removeStatus(URL url, String methodName) {
```

### JavadocDeclaration
`@param key` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcStatus.java`
#### Snippet
```java
     * set value.
     *
     * @param key
     * @param value
     */
```

### JavadocDeclaration
`@param value` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcStatus.java`
#### Snippet
```java
     *
     * @param key
     * @param value
     */
    public void set(String key, Object value) {
```

### JavadocDeclaration
`@param url` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcStatus.java`
#### Snippet
```java

    /**
     * @param url
     * @return status
     */
```

### JavadocDeclaration
`@param url` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcStatus.java`
#### Snippet
```java

    /**
     * @param url
     * @param elapsed
     * @param succeeded
```

### JavadocDeclaration
`@param elapsed` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcStatus.java`
#### Snippet
```java
    /**
     * @param url
     * @param elapsed
     * @param succeeded
     */
```

### JavadocDeclaration
`@param succeeded` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcStatus.java`
#### Snippet
```java
     * @param url
     * @param elapsed
     * @param succeeded
     */
    public static void endCount(URL url, String methodName, long elapsed, boolean succeeded) {
```

### JavadocDeclaration
`@param value` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/AsyncRpcResult.java`
#### Snippet
```java
     * The result is you may lose the value you expected to set.
     *
     * @param value
     */
    @Override
```

### JavadocDeclaration
Javadoc pointing to itself
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/AsyncRpcResult.java`
#### Snippet
```java

    /**
     * Notice the return type of {@link #getValue} is the actual type of the RPC method, not {@link AppResponse}
     *
     * @return
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/AsyncRpcResult.java`
#### Snippet
```java
     * Notice the return type of {@link #getValue} is the actual type of the RPC method, not {@link AppResponse}
     *
     * @return
     */
    @Override
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/AsyncRpcResult.java`
#### Snippet
```java
     * 2. if no value returns after timeout, throw TimeoutException.
     *
     * @return
     * @throws InterruptedException
     * @throws ExecutionException
```

### JavadocDeclaration
`@throws` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/AsyncRpcResult.java`
#### Snippet
```java
     *
     * @return
     * @throws InterruptedException
     * @throws ExecutionException
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/AsyncRpcResult.java`
#### Snippet
```java
     * @return
     * @throws InterruptedException
     * @throws ExecutionException
     */
    @Override
```

### JavadocDeclaration
`@param address` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcServiceContext.java`
#### Snippet
```java
     * set remote address.
     *
     * @param address
     * @return context
     */
```

### JavadocDeclaration
`@param ` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcServiceContext.java`
#### Snippet
```java
     * get CompletableFuture.
     *
     * @param <T>
     * @return future
     */
```

### JavadocDeclaration
`@param host` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcServiceContext.java`
#### Snippet
```java
     * set remote address.
     *
     * @param host
     * @param port
     * @return context
```

### JavadocDeclaration
`@param port` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcServiceContext.java`
#### Snippet
```java
     *
     * @param host
     * @param port
     * @return context
     */
```

### JavadocDeclaration
`@param future` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcServiceContext.java`
#### Snippet
```java
     * set future.
     *
     * @param future
     */
    @Override
```

### JavadocDeclaration
`@param host` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcServiceContext.java`
#### Snippet
```java
     * set local address.
     *
     * @param host
     * @param port
     * @return context
```

### JavadocDeclaration
`@param port` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcServiceContext.java`
#### Snippet
```java
     *
     * @param host
     * @param port
     * @return context
     */
```

### JavadocDeclaration
`@param callable` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcServiceContext.java`
#### Snippet
```java
     * Async invocation. Timeout will be handled even if <code>Future.get()</code> is not called.
     *
     * @param callable
     * @return get the return result from <code>future.get()</code>
     */
```

### JavadocDeclaration
Tag `serial` is not allowed here
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcServiceContext.java`
#### Snippet
```java
     * get parameter types.
     *
     * @serial
     */
    @Override
```

### JavadocDeclaration
`@param needCopy` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcServiceContext.java`
#### Snippet
```java
     * Also see {@link RpcContextAttachment#copyOf(boolean)}
     *
     * @param needCopy
     * @return a shallow copy of RpcServiceContext
     */
```

### JavadocDeclaration
`@param runnable` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcServiceContext.java`
#### Snippet
```java
     * one way async call, send request only, and result is not required
     *
     * @param runnable
     */
    @Override
```

### JavadocDeclaration
`@param ` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcServiceContext.java`
#### Snippet
```java
     * get future.
     *
     * @param <T>
     * @return future
     */
```

### JavadocDeclaration
`@param address` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcServiceContext.java`
#### Snippet
```java
     * set local address.
     *
     * @param address
     * @return context
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/filter/AccessLogFilter.java`
#### Snippet
```java
     * @param inv     Invocation service method.
     * @return Result from service method.
     * @throws RpcException
     */
    @Override
```

### JavadocDeclaration
`@param remotePort` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/support/AccessLogData.java`
#### Snippet
```java
     * Set caller remote port.
     *
     * @param remotePort
     */
    private void setRemotePort(Integer remotePort) {
```

### JavadocDeclaration
`@param remoteHost` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/support/AccessLogData.java`
#### Snippet
```java
     * Set caller remote host
     *
     * @param remoteHost
     */
    private void setRemoteHost(String remoteHost) {
```

### JavadocDeclaration
`@param types` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/support/AccessLogData.java`
#### Snippet
```java
     * Set invocation's method's input parameter's types
     *
     * @param types
     */
    public void setTypes(Class[] types) {
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/support/AccessLogData.java`
#### Snippet
```java
     * Return gthe service of access log entry
     *
     * @return
     */
    public String getServiceName() {
```

### JavadocDeclaration
`@param localHost` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/support/AccessLogData.java`
#### Snippet
```java
     * Set local host
     *
     * @param localHost
     */
    private void setLocalHost(String localHost) {
```

### JavadocDeclaration
`@param serviceName` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/support/AccessLogData.java`
#### Snippet
```java
     * Add service name.
     *
     * @param serviceName
     */
    public void setServiceName(String serviceName) {
```

### JavadocDeclaration
`@param outTime` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/support/AccessLogData.java`
#### Snippet
```java
     * Set the out date. As an argument it accept date string.
     *
     * @param outTime
     */
    public void setOutTime(Date outTime) {
```

### JavadocDeclaration
`@param version` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/support/AccessLogData.java`
#### Snippet
```java
     * Add version information.
     *
     * @param version
     */
    public void setVersion(String version) {
```

### JavadocDeclaration
`@param group` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/support/AccessLogData.java`
#### Snippet
```java
     * Add group name
     *
     * @param group
     */
    public void setGroup(String group) {
```

### JavadocDeclaration
`@param arguments` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/support/AccessLogData.java`
#### Snippet
```java
     * Sets invocation arguments
     *
     * @param arguments
     */
    public void setArguments(Object[] arguments) {
```

### JavadocDeclaration
`@param methodName` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/support/AccessLogData.java`
#### Snippet
```java
     * Set target method name.
     *
     * @param methodName
     */
    public void setMethodName(String methodName) {
```

### JavadocDeclaration
`@param key` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/support/AccessLogData.java`
#### Snippet
```java
     * Return value of key
     *
     * @param key
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/support/AccessLogData.java`
#### Snippet
```java
     *
     * @param key
     * @return
     */
    private Object get(String key) {
```

### JavadocDeclaration
`@param invocationTime` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/support/AccessLogData.java`
#### Snippet
```java
     * Set the invocation date. As an argument it accept date string.
     *
     * @param invocationTime
     */
    public void setInvocationTime(Date invocationTime) {
```

### JavadocDeclaration
`@param localPort` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/support/AccessLogData.java`
#### Snippet
```java
     * Set local port of exported service
     *
     * @param localPort
     */
    private void setLocalPort(Integer localPort) {
```

### JavadocDeclaration
`@param url` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/support/RpcUtils.java`
#### Snippet
```java
     * Idempotent operation: invocation id will be added in async operation by default
     *
     * @param url
     * @param inv
     */
```

### JavadocDeclaration
`@param inv` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/support/RpcUtils.java`
#### Snippet
```java
     *
     * @param url
     * @param inv
     */
    public static void attachInvocationIdIfAsync(URL url, Invocation inv) {
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/protocol/AbstractProxyProtocol.java`
#### Snippet
```java

        /**
         * @return
         */
        @Override
```

### JavadocDeclaration
`@param callable` tag description is missing
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/RpcContext.java`
#### Snippet
```java
     * Async invocation. Timeout will be handled even if <code>Future.get()</code> is not called.
     *
     * @param callable
     * @return get the return result from <code>future.get()</code>
     */
```

### JavadocDeclaration
`@param key` tag description is missing
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/RpcContext.java`
#### Snippet
```java
     * set value.
     *
     * @param key
     * @param value
     * @return context
```

### JavadocDeclaration
`@param value` tag description is missing
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/RpcContext.java`
#### Snippet
```java
     *
     * @param key
     * @param value
     * @return context
     */
```

### JavadocDeclaration
`@param runnable` tag description is missing
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/RpcContext.java`
#### Snippet
```java
     * one way async call, send request only, and result is not required
     *
     * @param runnable
     */
    public void asyncCall(Runnable runnable) {
```

### JavadocDeclaration
`@param url` tag description is missing
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/support/RpcUtils.java`
#### Snippet
```java
     * Idempotent operation: invocation id will be added in async operation by default
     *
     * @param url
     * @param inv
     */
```

### JavadocDeclaration
`@param inv` tag description is missing
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/support/RpcUtils.java`
#### Snippet
```java
     *
     * @param url
     * @param inv
     */
    public static void attachInvocationIdIfAsync(URL url, Invocation inv) {
```

### JavadocDeclaration
`@param response` tag description is missing
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/remoting/exchange/ResponseCallback.java`
#### Snippet
```java
     * done.
     *
     * @param response
     */
    void done(Object response);
```

### JavadocDeclaration
`@param exception` tag description is missing
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/remoting/exchange/ResponseCallback.java`
#### Snippet
```java
     * caught exception.
     *
     * @param exception
     */
    void caught(Throwable exception);
```

### JavadocDeclaration
`@param callback` tag description is missing
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/remoting/exchange/ResponseFuture.java`
#### Snippet
```java
     * set callback.
     *
     * @param callback
     */
    void setCallback(ResponseCallback callback);
```

### JavadocDeclaration
`@param url` tag description is missing
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/container/page/PageHandler.java`
#### Snippet
```java
     * Handle the page.
     *
     * @param url
     * @return the page.
     */
```

### JavadocDeclaration
Tag `return` is not allowed here
in `dubbo-rpc/dubbo-rpc-dubbo/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/ReferenceCountExchangeClient.java`
#### Snippet
```java
     * the client will "resurrect".
     *
     * @return
     */
    private void replaceWithLazyClient() {
```

### JavadocDeclaration
`@param channel` tag description is missing
in `dubbo-rpc/dubbo-rpc-dubbo/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/CallbackServiceCodec.java`
#### Snippet
```java
     * export or unexport callback service on client side
     *
     * @param channel
     * @param url
     * @param clazz
```

### JavadocDeclaration
`@param url` tag description is missing
in `dubbo-rpc/dubbo-rpc-dubbo/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/CallbackServiceCodec.java`
#### Snippet
```java
     *
     * @param channel
     * @param url
     * @param clazz
     * @param inst
```

### JavadocDeclaration
`@param clazz` tag description is missing
in `dubbo-rpc/dubbo-rpc-dubbo/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/CallbackServiceCodec.java`
#### Snippet
```java
     * @param channel
     * @param url
     * @param clazz
     * @param inst
     * @param export
```

### JavadocDeclaration
`@param inst` tag description is missing
in `dubbo-rpc/dubbo-rpc-dubbo/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/CallbackServiceCodec.java`
#### Snippet
```java
     * @param url
     * @param clazz
     * @param inst
     * @param export
     * @throws IOException
```

### JavadocDeclaration
`@param export` tag description is missing
in `dubbo-rpc/dubbo-rpc-dubbo/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/CallbackServiceCodec.java`
#### Snippet
```java
     * @param clazz
     * @param inst
     * @param export
     * @throws IOException
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `dubbo-rpc/dubbo-rpc-dubbo/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/CallbackServiceCodec.java`
#### Snippet
```java
     * @param inst
     * @param export
     * @throws IOException
     */
    @SuppressWarnings({"unchecked", "rawtypes"})
```

### JavadocDeclaration
`@param url` tag description is missing
in `dubbo-rpc/dubbo-rpc-dubbo/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/CallbackServiceCodec.java`
#### Snippet
```java
     * refer or destroy callback service on server side
     *
     * @param url
     */
    @SuppressWarnings("unchecked")
```

### JavadocDeclaration
`@param referenceCountExchangeClients` tag description is missing
in `dubbo-rpc/dubbo-rpc-dubbo/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/SharedClientsProvider.java`
#### Snippet
```java
     * Check if the client list is all available
     *
     * @param referenceCountExchangeClients
     * @return true-available，false-unavailable
     */
```

### JavadocDeclaration
`@param client` tag description is missing
in `dubbo-rpc/dubbo-rpc-dubbo/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/SharedClientsProvider.java`
#### Snippet
```java
     * close ReferenceCountExchangeClient
     *
     * @param client
     */
    private void closeReferenceCountExchangeClient(ReferenceCountExchangeClient client) {
```

### JavadocDeclaration
`@param referenceCountExchangeClients` tag description is missing
in `dubbo-rpc/dubbo-rpc-dubbo/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/SharedClientsProvider.java`
#### Snippet
```java
     * Increase the reference Count if we create new invoker shares same connection, the connection will be closed without any reference.
     *
     * @param referenceCountExchangeClients
     */
    private void batchClientRefIncr(List<ReferenceCountExchangeClient> referenceCountExchangeClients) {
```

### JavadocDeclaration
`@param invocation` tag description is missing
in `dubbo-rpc/dubbo-rpc-dubbo/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/DubboProtocol.java`
#### Snippet
```java
     * only log body in debugger mode for size & security consideration.
     *
     * @param invocation
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-rpc/dubbo-rpc-dubbo/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/DubboProtocol.java`
#### Snippet
```java
     *
     * @param invocation
     * @return
     */
    private Invocation getInvocationWithoutData(Invocation invocation) {
```

### JavadocDeclaration
`@param url` tag description is missing
in `dubbo-rpc/dubbo-rpc-dubbo/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/DubboProtocol.java`
#### Snippet
```java
     * Bulk build client
     *
     * @param url
     * @param connectNum
     * @return
```

### JavadocDeclaration
`@param connectNum` tag description is missing
in `dubbo-rpc/dubbo-rpc-dubbo/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/DubboProtocol.java`
#### Snippet
```java
     *
     * @param url
     * @param connectNum
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-rpc/dubbo-rpc-dubbo/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/DubboProtocol.java`
#### Snippet
```java
     * @param url
     * @param connectNum
     * @return
     */
    private List<ReferenceCountExchangeClient> buildReferenceCountExchangeClientList(URL url, int connectNum) {
```

### JavadocDeclaration
`@param url` tag description is missing
in `dubbo-rpc/dubbo-rpc-dubbo/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/DubboProtocol.java`
#### Snippet
```java
     * Build a single client
     *
     * @param url
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-rpc/dubbo-rpc-dubbo/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/DubboProtocol.java`
#### Snippet
```java
     *
     * @param url
     * @return
     */
    private ReferenceCountExchangeClient buildReferenceCountExchangeClient(URL url) {
```

### JavadocDeclaration
`@param url` tag description is missing
in `dubbo-rpc/dubbo-rpc-dubbo/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/DubboProtocol.java`
#### Snippet
```java
     * Get shared connection
     *
     * @param url
     * @param connectNum connectNum must be greater than or equal to 1
     */
```

### JavadocDeclaration
`@param channel` tag description is missing
in `dubbo-rpc/dubbo-rpc-dubbo/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/DubboProtocol.java`
#### Snippet
```java
             * we can choose to use a common service to carry onConnect event if there's no easy way to get the specific
             * service this connection is binding to.
             * @param channel
             * @param url
             * @param methodKey
```

### JavadocDeclaration
`@param url` tag description is missing
in `dubbo-rpc/dubbo-rpc-dubbo/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/DubboProtocol.java`
#### Snippet
```java
             * service this connection is binding to.
             * @param channel
             * @param url
             * @param methodKey
             * @return
```

### JavadocDeclaration
`@param methodKey` tag description is missing
in `dubbo-rpc/dubbo-rpc-dubbo/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/DubboProtocol.java`
#### Snippet
```java
             * @param channel
             * @param url
             * @param methodKey
             * @return
             */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-rpc/dubbo-rpc-dubbo/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/DubboProtocol.java`
#### Snippet
```java
             * @param url
             * @param methodKey
             * @return
             */
            private Invocation createInvocation(Channel channel, URL url, String methodKey) {
```

### JavadocDeclaration
`@param url` tag description is missing
in `dubbo-rpc/dubbo-rpc-dubbo/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/DubboProtocol.java`
#### Snippet
```java
     * Create new connection
     *
     * @param url
     */
    private ExchangeClient initClient(URL url) {
```

### JavadocDeclaration
`@param url` tag description is missing
in `dubbo-plugin/dubbo-auth/src/main/java/org/apache/dubbo/auth/spi/AccessKeyStorage.java`
#### Snippet
```java
     * get AccessKeyPair of this request
     *
     * @param url
     * @param invocation
     * @return
```

### JavadocDeclaration
`@param invocation` tag description is missing
in `dubbo-plugin/dubbo-auth/src/main/java/org/apache/dubbo/auth/spi/AccessKeyStorage.java`
#### Snippet
```java
     *
     * @param url
     * @param invocation
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-plugin/dubbo-auth/src/main/java/org/apache/dubbo/auth/spi/AccessKeyStorage.java`
#### Snippet
```java
     * @param url
     * @param invocation
     * @return
     */
    AccessKeyPair getAccessKey(URL url, Invocation invocation);
```

### JavadocDeclaration
`@param invocation` tag description is missing
in `dubbo-plugin/dubbo-auth/src/main/java/org/apache/dubbo/auth/spi/Authenticator.java`
#### Snippet
```java
    /**
     * verify the signature of the request is valid or not
     * @param invocation
     * @param url
     * @throws RpcAuthenticationException when failed to authenticate current invocation
```

### JavadocDeclaration
`@param url` tag description is missing
in `dubbo-plugin/dubbo-auth/src/main/java/org/apache/dubbo/auth/spi/Authenticator.java`
#### Snippet
```java
     * verify the signature of the request is valid or not
     * @param invocation
     * @param url
     * @throws RpcAuthenticationException when failed to authenticate current invocation
     */
```

### JavadocDeclaration
`@param invocation` tag description is missing
in `dubbo-plugin/dubbo-auth/src/main/java/org/apache/dubbo/auth/spi/Authenticator.java`
#### Snippet
```java
     * give a sign to request
     *
     * @param invocation
     * @param url
     */
```

### JavadocDeclaration
`@param url` tag description is missing
in `dubbo-plugin/dubbo-auth/src/main/java/org/apache/dubbo/auth/spi/Authenticator.java`
#### Snippet
```java
     *
     * @param invocation
     * @param url
     */
    void sign(Invocation invocation, URL url);
```

### JavadocDeclaration
`@throws` tag description is missing
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/Router.java`
#### Snippet
```java
     * @param invocation invocation
     * @return routed invokers
     * @throws RpcException
     */
    @Deprecated
```

### JavadocDeclaration
`@throws` tag description is missing
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/Router.java`
#### Snippet
```java
     * @param needToPrintMessage whether to print router state. Such as `use router branch a`.
     * @return state with route result
     * @throws RpcException
     */
    default <T> RouterResult<Invoker<T>> route(List<Invoker<T>> invokers, URL url, Invocation invocation,
```

### JavadocDeclaration
`@param ` tag description is missing
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/Cluster.java`
#### Snippet
```java
     * Merge the directory invokers to a virtual invoker.
     *
     * @param <T>
     * @param directory
     * @return cluster invoker
```

### JavadocDeclaration
`@param directory` tag description is missing
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/Cluster.java`
#### Snippet
```java
     *
     * @param <T>
     * @param directory
     * @return cluster invoker
     * @throws RpcException
```

### JavadocDeclaration
`@throws` tag description is missing
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/Cluster.java`
#### Snippet
```java
     * @param directory
     * @return cluster invoker
     * @throws RpcException
     */
    @Adaptive
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/ProviderURLMergeProcessor.java`
#### Snippet
```java
     * @param remoteUrl          providerUrl
     * @param localParametersMap consumer url parameters
     * @return
     */
    URL mergeUrl(URL remoteUrl, Map<String, String> localParametersMap);
```

### JavadocDeclaration
`@param mergerCls` tag description is missing
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/merger/MergerFactory.java`
#### Snippet
```java
    /**
     * get merger's actual type argument (same as return type)
     * @param mergerCls
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/merger/MergerFactory.java`
#### Snippet
```java
     * get merger's actual type argument (same as return type)
     * @param mergerCls
     * @return
     */
    private Class<?> getActualTypeArgument(Class<? extends Merger> mergerCls) {
```

### JavadocDeclaration
`@param invokers` tag description is missing
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/tag/TagStateRouter.java`
#### Snippet
```java
     * TODO, to guarantee consistent behavior of interoperability between 2.6- and 2.7+, this method should has the same logic with the TagRouter in 2.6.x.
     *
     * @param invokers
     * @param url
     * @param invocation
```

### JavadocDeclaration
`@param url` tag description is missing
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/tag/TagStateRouter.java`
#### Snippet
```java
     *
     * @param invokers
     * @param url
     * @param invocation
     * @param <T>
```

### JavadocDeclaration
`@param invocation` tag description is missing
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/tag/TagStateRouter.java`
#### Snippet
```java
     * @param invokers
     * @param url
     * @param invocation
     * @param <T>
     * @return
```

### JavadocDeclaration
`@param ` tag description is missing
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/tag/TagStateRouter.java`
#### Snippet
```java
     * @param url
     * @param invocation
     * @param <T>
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/tag/TagStateRouter.java`
#### Snippet
```java
     * @param invocation
     * @param <T>
     * @return
     */
    private <T> BitList<Invoker<T>> filterUsingStaticTag(BitList<Invoker<T>> invokers, URL url, Invocation invocation) {
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/condition/matcher/ConditionMatcher.java`
#### Snippet
```java
     * mismatch patterns extracted from then condition
     *
     * @return
     */
    Set<String> getMismatches();
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/condition/matcher/ConditionMatcher.java`
#### Snippet
```java
     * match patterns extracted from when condition
     *
     * @return
     */
    Set<String> getMatches();
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/support/AbstractClusterInvoker.java`
#### Snippet
```java
     * In this scenario, please use the invokeWithContextAsync method.
     *
     * @return
     */
    protected Result invokeWithContextAsync(Invoker<T> invoker, Invocation invocation, URL consumerUrl) {
```

### JavadocDeclaration
`@param invocation` tag description is missing
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/support/wrapper/MockClusterInvoker.java`
#### Snippet
```java
     * if directory.list() returns more than one mock invoker, only one of them will be used.
     *
     * @param invocation
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/support/wrapper/MockClusterInvoker.java`
#### Snippet
```java
     *
     * @param invocation
     * @return
     */
    private List<Invoker<T>> selectMockInvoker(Invocation invocation) {
```

### JavadocDeclaration
`@param clusterInvoker` tag description is missing
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/interceptor/ClusterInterceptor.java`
#### Snippet
```java
     * executed before and after invoke.
     *
     * @param clusterInvoker
     * @param invocation
     * @return
```

### JavadocDeclaration
`@param invocation` tag description is missing
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/interceptor/ClusterInterceptor.java`
#### Snippet
```java
     *
     * @param clusterInvoker
     * @param invocation
     * @return
     * @throws RpcException
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/interceptor/ClusterInterceptor.java`
#### Snippet
```java
     * @param clusterInvoker
     * @param invocation
     * @return
     * @throws RpcException
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/interceptor/ClusterInterceptor.java`
#### Snippet
```java
     * @param invocation
     * @return
     * @throws RpcException
     */
    default Result intercept(AbstractClusterInvoker<?> clusterInvoker, Invocation invocation) throws RpcException {
```

### JavadocDeclaration
`@param ` tag description is missing
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/loadbalance/RandomLoadBalance.java`
#### Snippet
```java
     * @param url        URL
     * @param invocation Invocation
     * @param <T>
     * @return The selected invoker
     */
```

### JavadocDeclaration
`@param invokers` tag description is missing
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/loadbalance/RoundRobinLoadBalance.java`
#### Snippet
```java
     * <b>for unit test only</b>
     *
     * @param invokers
     * @param invocation
     * @return
```

### JavadocDeclaration
`@param invocation` tag description is missing
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/loadbalance/RoundRobinLoadBalance.java`
#### Snippet
```java
     *
     * @param invokers
     * @param invocation
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/loadbalance/RoundRobinLoadBalance.java`
#### Snippet
```java
     * @param invokers
     * @param invocation
     * @return
     */
    protected <T> Collection<String> getInvokerAddrList(List<Invoker<T>> invokers, Invocation invocation) {
```

### JavadocDeclaration
`@param consumerUrl` tag description is missing
in `dubbo-cluster/src/main/java/org/apache/dubbo/registry/AddressListener.java`
#### Snippet
```java
     *
     * @param addresses         provider address list
     * @param consumerUrl
     * @param registryDirectory
     */
```

### JavadocDeclaration
`@param registryDirectory` tag description is missing
in `dubbo-cluster/src/main/java/org/apache/dubbo/registry/AddressListener.java`
#### Snippet
```java
     * @param addresses         provider address list
     * @param consumerUrl
     * @param registryDirectory
     */
    List<URL> notify(List<URL> addresses, URL consumerUrl, Directory registryDirectory);
```

### JavadocDeclaration
`@param remoteAddress` tag description is missing
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/RemotingServer.java`
#### Snippet
```java
     * get channel.
     *
     * @param remoteAddress
     * @return channel
     */
```

### JavadocDeclaration
`@param message` tag description is missing
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/Endpoint.java`
#### Snippet
```java
     * send message.
     *
     * @param message
     * @throws RemotingException
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/Endpoint.java`
#### Snippet
```java
     *
     * @param message
     * @throws RemotingException
     */
    void send(Object message) throws RemotingException;
```

### JavadocDeclaration
`@param message` tag description is missing
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/Endpoint.java`
#### Snippet
```java
     * send message.
     *
     * @param message
     * @param sent    already sent to socket?
     */
```

### JavadocDeclaration
Wrong tag `export`
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/TimeoutException.java`
#### Snippet
```java
 * TimeoutException. (API, Prototype, ThreadSafe)
 *
 * @export
 * @see org.apache.dubbo.remoting.exchange.support.DefaultFuture#get()
 */
```

### JavadocDeclaration
`@param handler` tag description is missing
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/Dispatcher.java`
#### Snippet
```java
     * dispatch the message to threadpool.
     *
     * @param handler
     * @param url
     * @return channel handler
```

### JavadocDeclaration
`@param url` tag description is missing
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/Dispatcher.java`
#### Snippet
```java
     *
     * @param handler
     * @param url
     * @return channel handler
     */
```

### JavadocDeclaration
`@param handler` tag description is missing
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/Transporter.java`
#### Snippet
```java
     *
     * @param url     server url
     * @param handler
     * @return client
     * @throws RemotingException
```

### JavadocDeclaration
`@throws` tag description is missing
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/Transporter.java`
#### Snippet
```java
     * @param handler
     * @return client
     * @throws RemotingException
     * @see org.apache.dubbo.remoting.Transporters#connect(URL, ChannelHandler...)
     */
```

### JavadocDeclaration
`@param handler` tag description is missing
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/Transporter.java`
#### Snippet
```java
     *
     * @param url     server url
     * @param handler
     * @return server
     * @throws RemotingException
```

### JavadocDeclaration
`@throws` tag description is missing
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/Transporter.java`
#### Snippet
```java
     * @param handler
     * @return server
     * @throws RemotingException
     * @see org.apache.dubbo.remoting.Transporters#bind(URL, ChannelHandler...)
     */
```

### JavadocDeclaration
Wrong tag `export`
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/ExecutionException.java`
#### Snippet
```java
 * ReceiveException
 *
 * @export
 */
public class ExecutionException extends RemotingException {
```

### JavadocDeclaration
Wrong tag `export`
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/RemotingException.java`
#### Snippet
```java
 * RemotingException. (API, Prototype, ThreadSafe)
 *
 * @export
 * @see org.apache.dubbo.remoting.exchange.support.DefaultFuture#get()
 * @see org.apache.dubbo.remoting.Channel#send(Object, boolean)
```

### JavadocDeclaration
`@param message` tag description is missing
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/utils/PayloadDropper.java`
#### Snippet
```java
     * only log body in debugger mode for size & security consideration.
     *
     * @param message
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/utils/PayloadDropper.java`
#### Snippet
```java
     *
     * @param message
     * @return
     */
    public static Object getRequestWithoutData(Object message) {
```

### JavadocDeclaration
`@param channel` tag description is missing
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/telnet/TelnetHandler.java`
#### Snippet
```java
     * telnet.
     *
     * @param channel
     * @param message
     */
```

### JavadocDeclaration
`@param message` tag description is missing
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/telnet/TelnetHandler.java`
#### Snippet
```java
     *
     * @param channel
     * @param message
     */
    String telnet(Channel channel, String message) throws RemotingException;
```

### JavadocDeclaration
`@param channel` tag description is missing
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/exchange/ExchangeHandler.java`
#### Snippet
```java
     * reply.
     *
     * @param channel
     * @param request
     * @return response
```

### JavadocDeclaration
`@param request` tag description is missing
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/exchange/ExchangeHandler.java`
#### Snippet
```java
     *
     * @param channel
     * @param request
     * @return response
     * @throws RemotingException
```

### JavadocDeclaration
`@throws` tag description is missing
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/exchange/ExchangeHandler.java`
#### Snippet
```java
     * @param request
     * @return response
     * @throws RemotingException
     */
    CompletableFuture<Object> reply(ExchangeChannel channel, Object request) throws RemotingException;
```

### JavadocDeclaration
Javadoc pointing to itself
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/buffer/ChannelBuffer.java`
#### Snippet
```java
     * Marks the current {@code writerIndex} in this buffer.  You can reposition
     * the current {@code writerIndex} to the marked {@code writerIndex} by
     * calling {@link #resetWriterIndex()}. The initial value of the marked
     * {@code writerIndex} is {@code 0}.
     */
```

### JavadocDeclaration
Javadoc pointing to itself
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/buffer/ChannelBuffer.java`
#### Snippet
```java
     * </pre>
     * <p/>
     * By contrast, {@link #setIndex(int, int)} guarantees that it never throws
     * an {@link IndexOutOfBoundsException} as long as the specified indexes
     * meet basic constraints, regardless what the current index values of the
```

### JavadocDeclaration
`@param remoteAddress` tag description is missing
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/exchange/ExchangeServer.java`
#### Snippet
```java
     * get channel.
     *
     * @param remoteAddress
     * @return channel
     */
```

### JavadocDeclaration
`@param timeout` tag description is missing
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/exchange/ExchangeChannel.java`
#### Snippet
```java
     * graceful close.
     *
     * @param timeout
     */
    @Override
```

### JavadocDeclaration
`@param request` tag description is missing
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/exchange/ExchangeChannel.java`
#### Snippet
```java
     * send request.
     *
     * @param request
     * @return response future
     * @throws RemotingException
```

### JavadocDeclaration
`@throws` tag description is missing
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/exchange/ExchangeChannel.java`
#### Snippet
```java
     * @param request
     * @return response future
     * @throws RemotingException
     */
    CompletableFuture<Object> request(Object request, ExecutorService executor) throws RemotingException;
```

### JavadocDeclaration
`@param request` tag description is missing
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/exchange/ExchangeChannel.java`
#### Snippet
```java
     * send request.
     *
     * @param request
     * @param timeout
     * @return response future
```

### JavadocDeclaration
`@param timeout` tag description is missing
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/exchange/ExchangeChannel.java`
#### Snippet
```java
     *
     * @param request
     * @param timeout
     * @return response future
     * @throws RemotingException
```

### JavadocDeclaration
`@throws` tag description is missing
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/exchange/ExchangeChannel.java`
#### Snippet
```java
     * @param timeout
     * @return response future
     * @throws RemotingException
     */
    @Deprecated
```

### JavadocDeclaration
`@param request` tag description is missing
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/exchange/ExchangeChannel.java`
#### Snippet
```java
     * send request.
     *
     * @param request
     * @param timeout
     * @return response future
```

### JavadocDeclaration
`@param timeout` tag description is missing
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/exchange/ExchangeChannel.java`
#### Snippet
```java
     *
     * @param request
     * @param timeout
     * @return response future
     * @throws RemotingException
```

### JavadocDeclaration
`@throws` tag description is missing
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/exchange/ExchangeChannel.java`
#### Snippet
```java
     * @param timeout
     * @return response future
     * @throws RemotingException
     */
    CompletableFuture<Object> request(Object request, int timeout, ExecutorService executor) throws RemotingException;
```

### JavadocDeclaration
`@param request` tag description is missing
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/exchange/ExchangeChannel.java`
#### Snippet
```java
     * send request.
     *
     * @param request
     * @return response future
     * @throws RemotingException
```

### JavadocDeclaration
`@throws` tag description is missing
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/exchange/ExchangeChannel.java`
#### Snippet
```java
     * @param request
     * @return response future
     * @throws RemotingException
     */
    @Deprecated
```

### JavadocDeclaration
`@param url` tag description is missing
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/exchange/Exchanger.java`
#### Snippet
```java
     * bind.
     *
     * @param url
     * @param handler
     * @return message server
```

### JavadocDeclaration
`@param handler` tag description is missing
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/exchange/Exchanger.java`
#### Snippet
```java
     *
     * @param url
     * @param handler
     * @return message server
     */
```

### JavadocDeclaration
`@param url` tag description is missing
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/exchange/Exchanger.java`
#### Snippet
```java
     * connect.
     *
     * @param url
     * @param handler
     * @return message channel
```

### JavadocDeclaration
`@param handler` tag description is missing
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/exchange/Exchanger.java`
#### Snippet
```java
     *
     * @param url
     * @param handler
     * @return message channel
     */
```

### JavadocDeclaration
`@param channel` tag description is missing
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/exchange/support/Replier.java`
#### Snippet
```java
     * reply.
     *
     * @param channel
     * @param request
     * @return response
```

### JavadocDeclaration
`@param request` tag description is missing
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/exchange/support/Replier.java`
#### Snippet
```java
     *
     * @param channel
     * @param request
     * @return response
     * @throws RemotingException
```

### JavadocDeclaration
`@throws` tag description is missing
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/exchange/support/Replier.java`
#### Snippet
```java
     * @param request
     * @return response
     * @throws RemotingException
     */
    Object reply(ExchangeChannel channel, T request) throws RemotingException;
```

### JavadocDeclaration
`@param is` tag description is missing
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/transport/CodecSupport.java`
#### Snippet
```java
     * Read all payload to byte[]
     *
     * @param is
     * @return
     * @throws IOException
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/transport/CodecSupport.java`
#### Snippet
```java
     *
     * @param is
     * @return
     * @throws IOException
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/transport/CodecSupport.java`
#### Snippet
```java
     * @param is
     * @return
     * @throws IOException
     */
    public static byte[] getPayload(InputStream is) throws IOException {
```

### JavadocDeclaration
`@param payload` tag description is missing
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/transport/CodecSupport.java`
#### Snippet
```java
     * Check if payload is null object serialize result byte[] of serialization
     *
     * @param payload
     * @param proto
     * @return
```

### JavadocDeclaration
`@param proto` tag description is missing
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/transport/CodecSupport.java`
#### Snippet
```java
     *
     * @param payload
     * @param proto
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/transport/CodecSupport.java`
#### Snippet
```java
     * @param payload
     * @param proto
     * @return
     */
    public static boolean isHeartBeat(byte[] payload, byte proto) {
```

### JavadocDeclaration
`@throws` tag description is missing
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/transport/AbstractClient.java`
#### Snippet
```java
     * Close client.
     *
     * @throws Throwable
     */
    protected abstract void doClose() throws Throwable;
```

### JavadocDeclaration
`@throws` tag description is missing
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/transport/AbstractClient.java`
#### Snippet
```java
     * Open client.
     *
     * @throws Throwable
     */
    protected abstract void doOpen() throws Throwable;
```

### JavadocDeclaration
`@throws` tag description is missing
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/transport/AbstractClient.java`
#### Snippet
```java
     * Connect to server.
     *
     * @throws Throwable
     */
    protected abstract void doConnect() throws Throwable;
```

### JavadocDeclaration
`@throws` tag description is missing
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/transport/AbstractClient.java`
#### Snippet
```java
     * disConnect to server.
     *
     * @throws Throwable
     */
    protected abstract void doDisConnect() throws Throwable;
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/transport/dispatcher/WrappedChannelHandler.java`
#### Snippet
```java
    /**
     * @param msg  msg is the network message body, executorSupport.getExecutor needs it, and gets important information from it to get executor
     * @return
     */
    public ExecutorService getSharedExecutorService(Object msg) {
```

### JavadocDeclaration
`@param msg` tag description is missing
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/transport/dispatcher/WrappedChannelHandler.java`
#### Snippet
```java
     * 2. Use shared executor to execute the callback.
     *
     * @param msg
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/transport/dispatcher/WrappedChannelHandler.java`
#### Snippet
```java
     *
     * @param msg
     * @return
     */
    public ExecutorService getPreferredExecutorService(Object msg) {
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/transport/dispatcher/WrappedChannelHandler.java`
#### Snippet
```java
     * get the shared executor for current Server or Client
     *
     * @return
     */
    public ExecutorService getSharedExecutorService() {
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/zookeeper/AbstractZookeeperTransporter.java`
#### Snippet
```java
     * for unit test
     *
     * @return
     */
    public Map<String, ZookeeperClient> getZookeeperClientMap() {
```

### JavadocDeclaration
`@param addressList` tag description is missing
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/zookeeper/AbstractZookeeperTransporter.java`
#### Snippet
```java
     * write address-ZookeeperClient relationship to Map
     *
     * @param addressList
     * @param zookeeperClient
     */
```

### JavadocDeclaration
`@param zookeeperClient` tag description is missing
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/zookeeper/AbstractZookeeperTransporter.java`
#### Snippet
```java
     *
     * @param addressList
     * @param zookeeperClient
     */
    void writeToClientMap(List<String> addressList, ZookeeperClient zookeeperClient) {
```

### JavadocDeclaration
`@param url` tag description is missing
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/zookeeper/AbstractZookeeperTransporter.java`
#### Snippet
```java
     * Make sure the connection is connected.
     *
     * @param url
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/zookeeper/AbstractZookeeperTransporter.java`
#### Snippet
```java
     *
     * @param url
     * @return
     */
    @Override
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/zookeeper/AbstractZookeeperTransporter.java`
#### Snippet
```java
     *            The url in AbstractZookeeperTransporter#connect parameter is rewritten by this one.
     *            such as: zookeeper://127.0.0.1:2181/org.apache.dubbo.remoting.zookeeper.ZookeeperTransporter
     * @return
     */
    protected abstract ZookeeperClient createZookeeperClient(URL url);
```

### JavadocDeclaration
`@param url` tag description is missing
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/zookeeper/AbstractZookeeperTransporter.java`
#### Snippet
```java
     * redefine the url for zookeeper. just keep protocol, username, password, host, port, and individual parameter.
     *
     * @param url
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/zookeeper/AbstractZookeeperTransporter.java`
#### Snippet
```java
     *
     * @param url
     * @return
     */
    URL toClientURL(URL url) {
```

### JavadocDeclaration
`@param addressList` tag description is missing
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/zookeeper/AbstractZookeeperTransporter.java`
#### Snippet
```java
     * It is not private method for unit test.
     *
     * @param addressList
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/zookeeper/AbstractZookeeperTransporter.java`
#### Snippet
```java
     *
     * @param addressList
     * @return
     */
    public ZookeeperClient fetchAndUpdateZookeeperClientCache(List<String> addressList) {
```

### JavadocDeclaration
`@param message` tag description is missing
in `dubbo-remoting/dubbo-remoting-http/src/main/java/org/apache/dubbo/remoting/http/RestClient.java`
#### Snippet
```java
     * send message.
     *
     * @param message
     * @throws RemotingException
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `dubbo-remoting/dubbo-remoting-http/src/main/java/org/apache/dubbo/remoting/http/RestClient.java`
#### Snippet
```java
     *
     * @param message
     * @throws RemotingException
     */
    CompletableFuture<RestResult> send(RequestTemplate message);
```

### JavadocDeclaration
RemotingException is not declared to be thrown by method send
in `dubbo-remoting/dubbo-remoting-http/src/main/java/org/apache/dubbo/remoting/http/RestClient.java`
#### Snippet
```java
     *
     * @param message
     * @throws RemotingException
     */
    CompletableFuture<RestResult> send(RequestTemplate message);
```

### JavadocDeclaration
`@throws` tag description is missing
in `dubbo-remoting/dubbo-remoting-http/src/main/java/org/apache/dubbo/remoting/http/HttpHandler.java`
#### Snippet
```java
     * @param request  request.
     * @param response response.
     * @throws IOException
     */
    void handle(REQUEST request, RESPONSE response) throws IOException;
```

## RuleId[id=FieldMayBeFinal]
### FieldMayBeFinal
Field `applicationModel` may be 'final'
in `dubbo-metadata/dubbo-metadata-report-zookeeper/src/main/java/org/apache/dubbo/metadata/store/zookeeper/ZookeeperMetadataReportFactory.java`
#### Snippet
```java
    private ZookeeperTransporter zookeeperTransporter;

    private ApplicationModel applicationModel;

    public ZookeeperMetadataReportFactory(ApplicationModel applicationModel) {
```

### FieldMayBeFinal
Field `listeners` may be 'final'
in `dubbo-metadata/dubbo-metadata-report-zookeeper/src/main/java/org/apache/dubbo/metadata/store/zookeeper/ZookeeperMetadataReport.java`
#### Snippet
```java
        private String serviceKey;
        private String path;
        private Set<MappingListener> listeners;

        public MappingDataListener(String serviceKey, String path) {
```

### FieldMayBeFinal
Field `path` may be 'final'
in `dubbo-metadata/dubbo-metadata-report-zookeeper/src/main/java/org/apache/dubbo/metadata/store/zookeeper/ZookeeperMetadataReport.java`
#### Snippet
```java

        private String serviceKey;
        private String path;
        private Set<MappingListener> listeners;

```

### FieldMayBeFinal
Field `casListenerMap` may be 'final'
in `dubbo-metadata/dubbo-metadata-report-zookeeper/src/main/java/org/apache/dubbo/metadata/store/zookeeper/ZookeeperMetadataReport.java`
#### Snippet
```java
    ZookeeperClient zkClient;

    private ConcurrentMap<String, MappingDataListener> casListenerMap = new ConcurrentHashMap<>();


```

### FieldMayBeFinal
Field `serviceKey` may be 'final'
in `dubbo-metadata/dubbo-metadata-report-zookeeper/src/main/java/org/apache/dubbo/metadata/store/zookeeper/ZookeeperMetadataReport.java`
#### Snippet
```java
    private static class MappingDataListener implements DataListener {

        private String serviceKey;
        private String path;
        private Set<MappingListener> listeners;
```

### FieldMayBeFinal
Field `registryURL` may be 'final'
in `dubbo-registry/dubbo-registry-multiple/src/main/java/org/apache/dubbo/registry/multiple/MultipleServiceDiscovery.java`
#### Snippet
```java
    private static final String SERVICE = "service";
    private final Map<String, ServiceDiscovery> serviceDiscoveries = new ConcurrentHashMap<>();
    private URL registryURL;
    private String applicationName;
    private volatile boolean isDestroy;
```

### FieldMayBeFinal
Field `applicationName` may be 'final'
in `dubbo-registry/dubbo-registry-multiple/src/main/java/org/apache/dubbo/registry/multiple/MultipleServiceDiscovery.java`
#### Snippet
```java
    private final Map<String, ServiceDiscovery> serviceDiscoveries = new ConcurrentHashMap<>();
    private URL registryURL;
    private String applicationName;
    private volatile boolean isDestroy;

```

### FieldMayBeFinal
Field `bootstrap` may be 'final'
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/bootstrap/DubboBootstrap.java`
#### Snippet
```java
    public class Module {
        private ModuleModel moduleModel;
        private DubboBootstrap bootstrap;

        public Module(ModuleModel moduleModel) {
```

### FieldMayBeFinal
Field `moduleModel` may be 'final'
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/bootstrap/DubboBootstrap.java`
#### Snippet
```java

    public class Module {
        private ModuleModel moduleModel;
        private DubboBootstrap bootstrap;

```

### FieldMayBeFinal
Field `logger` may be 'final'
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/utils/ConfigValidationUtils.java`
#### Snippet
```java

public class ConfigValidationUtils {
    private static ErrorTypeAwareLogger logger = LoggerFactory.getErrorTypeAwareLogger(ConfigValidationUtils.class);
    /**
     * The maximum length of a <b>parameter's value</b>
```

### FieldMayBeFinal
Field `initialized` may be 'final'
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/ServiceConfig.java`
#### Snippet
```java
    private transient volatile boolean unexported;

    private transient volatile AtomicBoolean initialized = new AtomicBoolean(false);

    /**
```

### FieldMayBeFinal
Field `conflictedBeanNames` may be 'final'
in `dubbo-spring-boot/dubbo-spring-boot-compatible/autoconfigure/src/main/java/org/apache/dubbo/spring/boot/beans/factory/config/ServiceBeanIdConflictProcessor.java`
#### Snippet
```java
     * Holds the bean names of {@link ServiceBean} or {@link ServiceConfig}.
     */
    private Set<String> conflictedBeanNames = new HashSet<>();

    @Override
```

### FieldMayBeFinal
Field `interfaceNamesToBeanNames` may be 'final'
in `dubbo-spring-boot/dubbo-spring-boot-compatible/autoconfigure/src/main/java/org/apache/dubbo/spring/boot/beans/factory/config/ServiceBeanIdConflictProcessor.java`
#### Snippet
```java
     * The value is bean names of {@link ServiceBean} or {@link ServiceConfig}.
     */
    private Map<String, String> interfaceNamesToBeanNames = new HashMap<>();

    /**
```

### FieldMayBeFinal
Field `processed` may be 'final'
in `dubbo-spring-boot/dubbo-spring-boot-compatible/autoconfigure/src/main/java/org/apache/dubbo/spring/boot/context/event/WelcomeLogoApplicationListener.java`
#### Snippet
```java
public class WelcomeLogoApplicationListener implements ApplicationListener<ApplicationEnvironmentPreparedEvent> {

    private static AtomicBoolean processed = new AtomicBoolean(false);

    @Override
```

### FieldMayBeFinal
Field `handler` may be 'final'
in `dubbo-remoting/dubbo-remoting-netty4/src/main/java/org/apache/dubbo/remoting/transport/netty4/NettyConfigOperator.java`
#### Snippet
```java

    private final Channel channel;
    private ChannelHandler handler;

    public NettyConfigOperator(NettyChannel channel, ChannelHandler handler) {
```

### FieldMayBeFinal
Field `enableEmbeddedZookeeper` may be 'final'
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/AbstractRegistryCenterTestExecutionListener.java`
#### Snippet
```java
     * Use embedded zookeeper or not.
     */
    private static boolean enableEmbeddedZookeeper;

    static {
```

### FieldMayBeFinal
Field `config` may be 'final'
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/registrycenter/context/ZookeeperContext.java`
#### Snippet
```java
     * The config of zookeeper.
     */
    private ZookeeperConfig config = new ZookeeperConfig();

    /**
```

### FieldMayBeFinal
Field `processIds` may be 'final'
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/registrycenter/context/ZookeeperWindowsContext.java`
#### Snippet
```java
     * The map to store the pair of clientPort and PID.
     */
    private Map<Integer, Integer> processIds = new HashMap<>();

    /**
```

### FieldMayBeFinal
Field `context` may be 'final'
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/registrycenter/ZookeeperRegistryCenter.java`
#### Snippet
```java
     * The global context of zookeeper.
     */
    private ZookeeperContext context;

    /**
```

### FieldMayBeFinal
Field `os` may be 'final'
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/registrycenter/ZookeeperRegistryCenter.java`
#### Snippet
```java
     * The OS type.
     */
    private static OS os = getOS();

    /**
```

### FieldMayBeFinal
Field `initializers` may be 'final'
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/registrycenter/ZookeeperRegistryCenter.java`
#### Snippet
```java
     * All of {@link ZookeeperInitializer} instances.
     */
    private List<Initializer> initializers;

    /**
```

### FieldMayBeFinal
Field `processors` may be 'final'
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/registrycenter/ZookeeperRegistryCenter.java`
#### Snippet
```java
     * To store all processor instances.
     */
    private Map<OS, Map<Command, Processor>> processors = new HashMap<>();

    /**
```

### FieldMayBeFinal
Field `unclosedThreadsOfTestMap` may be 'final'
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/DubboTestChecker.java`
#### Snippet
```java
    private Map<Thread, StackTraceElement[]> unclosedThreadMap = new ConcurrentHashMap<>();
    // test class name -> thread list
    private Map<String, List<Thread>> unclosedThreadsOfTestMap = new ConcurrentHashMap<>();
    private String identifier;
    private PrintWriter reportWriter;
```

### FieldMayBeFinal
Field `unclosedThreadMap` may be 'final'
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/DubboTestChecker.java`
#### Snippet
```java
     * thread -> stacktrace
     */
    private Map<Thread, StackTraceElement[]> unclosedThreadMap = new ConcurrentHashMap<>();
    // test class name -> thread list
    private Map<String, List<Thread>> unclosedThreadsOfTestMap = new ConcurrentHashMap<>();
```

### FieldMayBeFinal
Field `adsObserver` may be 'final'
in `dubbo-xds/src/main/java/org/apache/dubbo/registry/xds/util/AdsObserver.java`
#### Snippet
```java

    private static class ResponseObserver implements StreamObserver<DiscoveryResponse> {
        private AdsObserver adsObserver;

        public ResponseObserver(AdsObserver adsObserver) {
```

### FieldMayBeFinal
Field `endpoints` may be 'final'
in `dubbo-xds/src/main/java/org/apache/dubbo/registry/xds/util/protocol/message/EndpointResult.java`
#### Snippet
```java

public class EndpointResult {
    private Set<Endpoint> endpoints;

    public EndpointResult() {
```

### FieldMayBeFinal
Field `virtualHostMap` may be 'final'
in `dubbo-xds/src/main/java/org/apache/dubbo/registry/xds/util/protocol/message/RouteResult.java`
#### Snippet
```java
public class RouteResult {
    private final Map<String, Set<String>> domainMap;
    private Map<String, VirtualHost> virtualHostMap;


```

### FieldMayBeFinal
Field `url` may be 'final'
in `dubbo-xds/src/main/java/org/apache/dubbo/registry/xds/util/XdsChannel.java`
#### Snippet
```java
    private static final String USE_AGENT = "use-agent";

    private URL url;

    private static final String SECURE = "secure";
```

### FieldMayBeFinal
Field `caAddr` may be 'final'
in `dubbo-xds/src/main/java/org/apache/dubbo/registry/xds/istio/IstioEnv.java`
#### Snippet
```java
    private String podName;

    private String caAddr;

    private String jwtPolicy;
```

### FieldMayBeFinal
Field `pilotCertProvider` may be 'final'
in `dubbo-xds/src/main/java/org/apache/dubbo/registry/xds/istio/IstioEnv.java`
#### Snippet
```java
    private String istioMetaClusterId;

    private String pilotCertProvider;

    private IstioEnv() {
```

### FieldMayBeFinal
Field `istioMetaClusterId` may be 'final'
in `dubbo-xds/src/main/java/org/apache/dubbo/registry/xds/istio/IstioEnv.java`
#### Snippet
```java
    private float secretGracePeriodRatio;

    private String istioMetaClusterId;

    private String pilotCertProvider;
```

### FieldMayBeFinal
Field `eccSigAlg` may be 'final'
in `dubbo-xds/src/main/java/org/apache/dubbo/registry/xds/istio/IstioEnv.java`
#### Snippet
```java
    private int rasKeySize;

    private String eccSigAlg;

    private int secretTTL;
```

### FieldMayBeFinal
Field `secretTTL` may be 'final'
in `dubbo-xds/src/main/java/org/apache/dubbo/registry/xds/istio/IstioEnv.java`
#### Snippet
```java
    private String eccSigAlg;

    private int secretTTL;

    private float secretGracePeriodRatio;
```

### FieldMayBeFinal
Field `rasKeySize` may be 'final'
in `dubbo-xds/src/main/java/org/apache/dubbo/registry/xds/istio/IstioEnv.java`
#### Snippet
```java
    private String workloadNameSpace;

    private int rasKeySize;

    private String eccSigAlg;
```

### FieldMayBeFinal
Field `jwtPolicy` may be 'final'
in `dubbo-xds/src/main/java/org/apache/dubbo/registry/xds/istio/IstioEnv.java`
#### Snippet
```java
    private String caAddr;

    private String jwtPolicy;

    private String trustDomain;
```

### FieldMayBeFinal
Field `workloadNameSpace` may be 'final'
in `dubbo-xds/src/main/java/org/apache/dubbo/registry/xds/istio/IstioEnv.java`
#### Snippet
```java
    private String trustDomain;

    private String workloadNameSpace;

    private int rasKeySize;
```

### FieldMayBeFinal
Field `trustDomain` may be 'final'
in `dubbo-xds/src/main/java/org/apache/dubbo/registry/xds/istio/IstioEnv.java`
#### Snippet
```java
    private String jwtPolicy;

    private String trustDomain;

    private String workloadNameSpace;
```

### FieldMayBeFinal
Field `secretGracePeriodRatio` may be 'final'
in `dubbo-xds/src/main/java/org/apache/dubbo/registry/xds/istio/IstioEnv.java`
#### Snippet
```java
    private int secretTTL;

    private float secretGracePeriodRatio;

    private String istioMetaClusterId;
```

### FieldMayBeFinal
Field `podName` may be 'final'
in `dubbo-xds/src/main/java/org/apache/dubbo/registry/xds/istio/IstioEnv.java`
#### Snippet
```java
    private static final IstioEnv INSTANCE = new IstioEnv();

    private String podName;

    private String caAddr;
```

### FieldMayBeFinal
Field `md5Utils` may be 'final'
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/RevisionResolver.java`
#### Snippet
```java
    public static final String EMPTY_REVISION = "0";

    private static MD5Utils md5Utils = new MD5Utils();

    public static String calRevision(String metadata) {
```

### FieldMayBeFinal
Field `parameterTypes` may be 'final'
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/ParameterTypesComparator.java`
#### Snippet
```java
public class ParameterTypesComparator {

    private Class[] parameterTypes;


```

### FieldMayBeFinal
Field `annotationClasses` may be 'final'
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/rest/ParamType.java`
#### Snippet
```java

    EMPTY(addSupportTypes());
    private List<Class> annotationClasses;


```

### FieldMayBeFinal
Field `timeout` may be 'final'
in `dubbo-remoting/dubbo-remoting-zookeeper-curator5/src/main/java/org/apache/dubbo/remoting/zookeeper/curator5/Curator5ZookeeperClient.java`
#### Snippet
```java

        private long lastSessionId;
        private int timeout;
        private int sessionExpireMs;

```

### FieldMayBeFinal
Field `sessionExpireMs` may be 'final'
in `dubbo-remoting/dubbo-remoting-zookeeper-curator5/src/main/java/org/apache/dubbo/remoting/zookeeper/curator5/Curator5ZookeeperClient.java`
#### Snippet
```java
        private long lastSessionId;
        private int timeout;
        private int sessionExpireMs;

        public CuratorConnectionStateListener(URL url) {
```

### FieldMayBeFinal
Field `nodeCacheMap` may be 'final'
in `dubbo-remoting/dubbo-remoting-zookeeper-curator5/src/main/java/org/apache/dubbo/remoting/zookeeper/curator5/Curator5ZookeeperClient.java`
#### Snippet
```java
    private static final Charset CHARSET = StandardCharsets.UTF_8;
    private final CuratorFramework client;
    private static Map<String, NodeCache> nodeCacheMap = new ConcurrentHashMap<>();

    public Curator5ZookeeperClient(URL url) {
```

### FieldMayBeFinal
Field `pathToServiceMapUnContainPathVariable` may be 'final'
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/rest/ServiceRestMetadata.java`
#### Snippet
```java

    private Map<PathMatcher, RestMethodMetadata> pathToServiceMapContainPathVariable = new HashMap<>();
    private Map<PathMatcher, RestMethodMetadata> pathToServiceMapUnContainPathVariable = new HashMap<>();
    private Map<String, Map<ParameterTypesComparator, RestMethodMetadata>> methodToServiceMap = new HashMap<>();

```

### FieldMayBeFinal
Field `pathToServiceMapContainPathVariable` may be 'final'
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/rest/ServiceRestMetadata.java`
#### Snippet
```java
    private Class codeStyle;

    private Map<PathMatcher, RestMethodMetadata> pathToServiceMapContainPathVariable = new HashMap<>();
    private Map<PathMatcher, RestMethodMetadata> pathToServiceMapUnContainPathVariable = new HashMap<>();
    private Map<String, Map<ParameterTypesComparator, RestMethodMetadata>> methodToServiceMap = new HashMap<>();
```

### FieldMayBeFinal
Field `params` may be 'final'
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/rest/RequestMetadata.java`
#### Snippet
```java
    private String path;

    private Map<String, List<String>> params = new LinkedHashMap<>();

    private Map<String, List<String>> headers = new LinkedHashMap<>();
```

### FieldMayBeFinal
Field `headers` may be 'final'
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/rest/RequestMetadata.java`
#### Snippet
```java
    private Map<String, List<String>> params = new LinkedHashMap<>();

    private Map<String, List<String>> headers = new LinkedHashMap<>();

    private Set<String> consumes = new LinkedHashSet<>();
```

### FieldMayBeFinal
Field `init` may be 'final'
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/report/MetadataReportInstance.java`
#### Snippet
```java
public class MetadataReportInstance implements Disposable {

    private AtomicBoolean init = new AtomicBoolean(false);
    private String metadataType;

```

### FieldMayBeFinal
Field `client` may be 'final'
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/ReferenceCountedClient.java`
#### Snippet
```java
    private RestClientFactory clientFactory;

    private T client;

    public ReferenceCountedClient(T client, ConcurrentMap<String, ReferenceCountedClient<? extends RestClient>> clients, RestClientFactory clientFactory, URL url) {
```

### FieldMayBeFinal
Field `url` may be 'final'
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/ReferenceCountedClient.java`
#### Snippet
```java

    private ConcurrentMap<String, ReferenceCountedClient<? extends RestClient>> clients;
    private URL url;
    private RestClientFactory clientFactory;

```

### FieldMayBeFinal
Field `clients` may be 'final'
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/ReferenceCountedClient.java`
#### Snippet
```java
    private static final ErrorTypeAwareLogger logger = LoggerFactory.getErrorTypeAwareLogger(ReferenceCountedClient.class);

    private ConcurrentMap<String, ReferenceCountedClient<? extends RestClient>> clients;
    private URL url;
    private RestClientFactory clientFactory;
```

### FieldMayBeFinal
Field `clientFactory` may be 'final'
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/ReferenceCountedClient.java`
#### Snippet
```java
    private ConcurrentMap<String, ReferenceCountedClient<? extends RestClient>> clients;
    private URL url;
    private RestClientFactory clientFactory;

    private T client;
```

### FieldMayBeFinal
Field `server` may be 'final'
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/NettyHttpRestServer.java`
#### Snippet
```java
    private final PathAndInvokerMapper pathAndInvokerMapper = new PathAndInvokerMapper();
    private final ExceptionMapper exceptionMapper = new ExceptionMapper();
    private NettyServer server = getNettyServer();

    /**
```

### FieldMayBeFinal
Field `initialized` may be 'final'
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/report/support/AbstractMetadataReport.java`
#### Snippet
```java
    // Local disk cache file
    File file;
    private AtomicBoolean initialized = new AtomicBoolean(false);
    public MetadataReportRetry metadataReportRetry;
    private ScheduledExecutorService reportTimerScheduler;
```

### FieldMayBeFinal
Field `version` may be 'final'
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/report/support/AbstractMetadataReport.java`
#### Snippet
```java

    private class SaveProperties implements Runnable {
        private long version;

        private SaveProperties(long version) {
```

### FieldMayBeFinal
Field `mediaTypes` may be 'final'
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/message/MediaTypeMatcher.java`
#### Snippet
```java
    ;

    private List<MediaType> mediaTypes;


```

### FieldMayBeFinal
Field `keepAlive` may be 'final'
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/netty/NettyHttpResponse.java`
#### Snippet
```java
    private final ChannelHandlerContext ctx;
    private boolean committed;
    private boolean keepAlive;
    private HttpMethod method;

```

### FieldMayBeFinal
Field `method` may be 'final'
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/netty/NettyHttpResponse.java`
#### Snippet
```java
    private boolean committed;
    private boolean keepAlive;
    private HttpMethod method;

    public NettyHttpResponse(final ChannelHandlerContext ctx, final boolean keepAlive) {
```

### FieldMayBeFinal
Field `outputHeaders` may be 'final'
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/netty/NettyHttpResponse.java`
#### Snippet
```java
    private int status = 200;
    private OutputStream os;
    private Map<String, List<String>> outputHeaders;
    private final ChannelHandlerContext ctx;
    private boolean committed;
```

### FieldMayBeFinal
Field `requestTemplate` may be 'final'
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/annotation/param/parse/consumer/ConsumerParseContext.java`
#### Snippet
```java

public class ConsumerParseContext extends BaseParseContext {
    private RequestTemplate requestTemplate;

    public ConsumerParseContext(RequestTemplate requestTemplate) {
```

### FieldMayBeFinal
Field `context` may be 'final'
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/request/NettyRequestFacade.java`
#### Snippet
```java


    private ChannelHandlerContext context;


```

### FieldMayBeFinal
Field `requestFacade` may be 'final'
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/annotation/param/parse/provider/ProviderParseContext.java`
#### Snippet
```java


    private RequestFacade requestFacade;
    private Object response;
    private Object request;
```

### FieldMayBeFinal
Field `mClassLoader` may be 'final'
in `dubbo-serialization/dubbo-serialization-jdk/src/main/java/org/apache/dubbo/common/serialize/java/CompactedObjectInputStream.java`
#### Snippet
```java
 */
public class CompactedObjectInputStream extends ObjectInputStream {
    private ClassLoader mClassLoader;

    public CompactedObjectInputStream(InputStream in) throws IOException {
```

### FieldMayBeFinal
Field `map` may be 'final'
in `dubbo-test/dubbo-test-common/src/main/java/org/apache/dubbo/test/common/SysProps.java`
#### Snippet
```java
public class SysProps {

    private static Map<String, String> map = new LinkedHashMap<String, String>();

    public static void reset() {
```

### FieldMayBeFinal
Field `applicationModel` may be 'final'
in `dubbo-configcenter/dubbo-configcenter-nacos/src/main/java/org/apache/dubbo/configcenter/support/nacos/NacosDynamicConfigurationFactory.java`
#### Snippet
```java
public class NacosDynamicConfigurationFactory extends AbstractDynamicConfigurationFactory {

    private ApplicationModel applicationModel;
    
    public NacosDynamicConfigurationFactory(ApplicationModel applicationModel) {
```

### FieldMayBeFinal
Field `contexts` may be 'final'
in `dubbo-test/dubbo-test-spring/src/main/java/org/apache/dubbo/test/spring/context/MockSpringInitCustomizer.java`
#### Snippet
```java
public class MockSpringInitCustomizer implements DubboSpringInitCustomizer {

    private List<DubboSpringInitContext> contexts = new ArrayList<>();

    @Override
```

### FieldMayBeFinal
Field `timeout` may be 'final'
in `dubbo-metadata/dubbo-metadata-report-redis/src/main/java/org/apache/dubbo/metadata/store/redis/RedisMetadataReport.java`
#### Snippet
```java
    protected JedisPool pool;
    private Set<HostAndPort> jedisClusterNodes;
    private int timeout;
    private String password;

```

### FieldMayBeFinal
Field `password` may be 'final'
in `dubbo-metadata/dubbo-metadata-report-redis/src/main/java/org/apache/dubbo/metadata/store/redis/RedisMetadataReport.java`
#### Snippet
```java
    private Set<HostAndPort> jedisClusterNodes;
    private int timeout;
    private String password;


```

### FieldMayBeFinal
Field `listeners` may be 'final'
in `dubbo-configcenter/dubbo-configcenter-nacos/src/main/java/org/apache/dubbo/configcenter/support/nacos/NacosDynamicConfiguration.java`
#### Snippet
```java
    public class NacosConfigListener extends AbstractSharedListener {

        private Set<ConfigurationListener> listeners = new CopyOnWriteArraySet<>();
        /**
         * cache data to store old value
```

### FieldMayBeFinal
Field `applicationModel` may be 'final'
in `dubbo-configcenter/dubbo-configcenter-nacos/src/main/java/org/apache/dubbo/configcenter/support/nacos/NacosDynamicConfiguration.java`
#### Snippet
```java
    private final NacosConfigServiceWrapper configService;

    private ApplicationModel applicationModel;

    /**
```

### FieldMayBeFinal
Field `cacheData` may be 'final'
in `dubbo-configcenter/dubbo-configcenter-nacos/src/main/java/org/apache/dubbo/configcenter/support/nacos/NacosDynamicConfiguration.java`
#### Snippet
```java
         * cache data to store old value
         */
        private Map<String, String> cacheData = new ConcurrentHashMap<>();

        @Override
```

### FieldMayBeFinal
Field `methodModels` may be 'final'
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ConsumerModel.java`
#### Snippet
```java

    private final Map<String, AsyncMethodInfo> methodConfigs;
    private Map<Method, ConsumerMethodModel> methodModels = new HashMap<>();

    /**
```

### FieldMayBeFinal
Field `scopeModel` may be 'final'
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ScopeModelAwareExtensionProcessor.java`
#### Snippet
```java

public class ScopeModelAwareExtensionProcessor implements ExtensionPostProcessor, ScopeModelAccessor {
    private ScopeModel scopeModel;
    private FrameworkModel frameworkModel;
    private ApplicationModel applicationModel;
```

### FieldMayBeFinal
Field `mLimit` may be 'final'
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/UnsafeStringReader.java`
#### Snippet
```java
    private String mString;

    private int mPosition, mLimit, mMark;

    public UnsafeStringReader(String str) {
```

### FieldMayBeFinal
Field `username` may be 'final'
in `dubbo-common/src/main/java/org/apache/dubbo/common/url/component/PathURLAddress.java`
#### Snippet
```java
public class PathURLAddress extends URLAddress {
    private String protocol;
    private String username;
    private String password;
    private String path;
```

### FieldMayBeFinal
Field `password` may be 'final'
in `dubbo-common/src/main/java/org/apache/dubbo/common/url/component/PathURLAddress.java`
#### Snippet
```java
    private String protocol;
    private String username;
    private String password;
    private String path;

```

### FieldMayBeFinal
Field `path` may be 'final'
in `dubbo-common/src/main/java/org/apache/dubbo/common/url/component/PathURLAddress.java`
#### Snippet
```java
    private String username;
    private String password;
    private String path;

    private transient String address;
```

### FieldMayBeFinal
Field `protocol` may be 'final'
in `dubbo-common/src/main/java/org/apache/dubbo/common/url/component/PathURLAddress.java`
#### Snippet
```java

public class PathURLAddress extends URLAddress {
    private String protocol;
    private String username;
    private String password;
```

### FieldMayBeFinal
Field `registeredClasses` may be 'final'
in `dubbo-common/src/main/java/org/apache/dubbo/common/beans/factory/ScopeBeanFactory.java`
#### Snippet
```java
    private InstantiationStrategy instantiationStrategy;
    private final AtomicBoolean destroyed = new AtomicBoolean();
    private List<Class<?>> registeredClasses = new ArrayList<>();

    public ScopeBeanFactory(ScopeBeanFactory parent, ExtensionAccessor extensionAccessor) {
```

### FieldMayBeFinal
Field `value` may be 'final'
in `dubbo-common/src/main/java/org/apache/dubbo/common/url/component/URLParam.java`
#### Snippet
```java
        public static class Node implements Map.Entry<String, String> {
            private final String key;
            private String value;

            public Node(String key, String value) {
```

### FieldMayBeFinal
Field `inetAddress` may be 'final'
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/CIDRUtils.java`
#### Snippet
```java
    private final String cidr;

    private InetAddress inetAddress;
    private InetAddress startAddress;
    private InetAddress endAddress;
```

### FieldMayBeFinal
Field `dynamicConfigurations` may be 'final'
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/configcenter/AbstractDynamicConfigurationFactory.java`
#### Snippet
```java
public abstract class AbstractDynamicConfigurationFactory implements DynamicConfigurationFactory {

    private volatile ConcurrentHashMap<String, DynamicConfiguration> dynamicConfigurations = new ConcurrentHashMap<>();

    @Override
```

### FieldMayBeFinal
Field `USED_PORT` may be 'final'
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/NetUtils.java`
#### Snippet
```java
     * the set used only on the synchronized method.
     */
    private static BitSet USED_PORT = new BitSet(65536);

    public static int getRandomPort() {
```

### FieldMayBeFinal
Field `scopeModel` may be 'final'
in `dubbo-common/src/main/java/org/apache/dubbo/common/deploy/AbstractDeployer.java`
#### Snippet
```java
    protected List<DeployListener<E>> listeners = new CopyOnWriteArrayList<>();

    private E scopeModel;

    public AbstractDeployer(E scopeModel) {
```

### FieldMayBeFinal
Field `converterUtil` may be 'final'
in `dubbo-common/src/main/java/org/apache/dubbo/common/convert/multiple/StringToArrayConverter.java`
#### Snippet
```java
 */
public class StringToArrayConverter implements StringToMultiValueConverter {
    private ConverterUtil converterUtil;

    public StringToArrayConverter(FrameworkModel frameworkModel) {
```

### FieldMayBeFinal
Field `converterUtil` may be 'final'
in `dubbo-common/src/main/java/org/apache/dubbo/common/convert/multiple/StringToIterableConverter.java`
#### Snippet
```java
 */
public abstract class StringToIterableConverter<T extends Iterable> implements StringToMultiValueConverter {
    private ConverterUtil converterUtil;

    public StringToIterableConverter(FrameworkModel frameworkModel) {
```

### FieldMayBeFinal
Field `hasRegisteredShutdownHook` may be 'final'
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/configcenter/file/FileSystemDynamicConfiguration.java`
#### Snippet
```java
    private final Map<File, List<ConfigurationListener>> listenersRepository;
    private ScopeModel scopeModel;
    private AtomicBoolean hasRegisteredShutdownHook = new AtomicBoolean();

    public FileSystemDynamicConfiguration() {
```

### FieldMayBeFinal
Field `primitiveDefaults` may be 'final'
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static final Pattern IS_HAS_CAN_METHOD_DESC_PATTERN = Pattern.compile("(?:is|has|can)([A-Z][_a-zA-Z0-9]*)\\(\\)Z");

    private static Map<Class<?>, Object> primitiveDefaults = new HashMap<>();

    static {
```

### FieldMayBeFinal
Field `WRAPPER_CLASS_COUNTER` may be 'final'
in `dubbo-common/src/main/java/org/apache/dubbo/common/bytecode/Wrapper.java`
#### Snippet
```java
        }
    };
    private static AtomicLong WRAPPER_CLASS_COUNTER = new AtomicLong(0);

    /**
```

### FieldMayBeFinal
Field `registryFactory` may be 'final'
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/RegistryFactoryWrapper.java`
#### Snippet
```java

public class RegistryFactoryWrapper implements RegistryFactory {
    private RegistryFactory registryFactory;

    public RegistryFactoryWrapper(RegistryFactory registryFactory) {
```

### FieldMayBeFinal
Field `scheduler` may be 'final'
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/RegistryNotifier.java`
#### Snippet
```java
    private final AtomicInteger executeTime = new AtomicInteger(0);

    private ScheduledExecutorService scheduler;

    public RegistryNotifier(URL registryUrl, long delayTime) {
```

### FieldMayBeFinal
Field `delayTime` may be 'final'
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/RegistryNotifier.java`
#### Snippet
```java

    private Object rawAddresses;
    private long delayTime;

    // should delay notify or not
```

### FieldMayBeFinal
Field `runnable` may be 'final'
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/retry/ReExportTask.java`
#### Snippet
```java
    private static final String NAME = "retry re-export";

    private Runnable runnable;

    public ReExportTask(Runnable runnable, URL oldUrl, FailbackRegistry registry) {
```

### FieldMayBeFinal
Field `instanceAddressURL` may be 'final'
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/DefaultServiceInstance.java`
#### Snippet
```java
    private transient List<Endpoint> endpoints;
    private transient ApplicationModel applicationModel;
    private transient Map<String, InstanceAddressURL> instanceAddressURL = new ConcurrentHashMap<>();

    public DefaultServiceInstance() {
```

### FieldMayBeFinal
Field `failureRecordTime` may be 'final'
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/event/RetryServiceInstancesChangedEvent.java`
#### Snippet
```java
public class RetryServiceInstancesChangedEvent extends ServiceInstancesChangedEvent {

    private volatile long failureRecordTime;

    public RetryServiceInstancesChangedEvent(String serviceName) {
```

### FieldMayBeFinal
Field `listener` may be 'final'
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/ServiceDiscoveryRegistry.java`
#### Snippet
```java
        private final URL url;
        private Set<String> oldApps;
        private NotifyListener listener;
        private volatile boolean stopped;

```

### FieldMayBeFinal
Field `metadataServicePort` may be 'final'
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/metadata/StandardMetadataServiceURLBuilder.java`
#### Snippet
```java

    private ApplicationModel applicationModel;
    private Integer metadataServicePort;

    public StandardMetadataServiceURLBuilder(ApplicationModel applicationModel) {
```

### FieldMayBeFinal
Field `applicationModel` may be 'final'
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/metadata/StandardMetadataServiceURLBuilder.java`
#### Snippet
```java
    public static final String NAME = "standard";

    private ApplicationModel applicationModel;
    private Integer metadataServicePort;

```

### FieldMayBeFinal
Field `instanceMetadataChangedListenerMap` may be 'final'
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/metadata/MetadataServiceDelegation.java`
#### Snippet
```java
    private final ApplicationModel applicationModel;
    private final RegistryManager registryManager;
    private ConcurrentMap<String, InstanceMetadataChangedListener> instanceMetadataChangedListenerMap = new ConcurrentHashMap<>();
    private URL url;
    // works only for DNS service discovery
```

### FieldMayBeFinal
Field `applicationModel` may be 'final'
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/status/RegistryStatusChecker.java`
#### Snippet
```java
public class RegistryStatusChecker implements StatusChecker {

    private ApplicationModel applicationModel;

    public RegistryStatusChecker(ApplicationModel applicationModel) {
```

### FieldMayBeFinal
Field `applicationModel` may be 'final'
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/support/RegistryManager.java`
#### Snippet
```java
    private static final ErrorTypeAwareLogger LOGGER = LoggerFactory.getErrorTypeAwareLogger(RegistryManager.class);

    private ApplicationModel applicationModel;

    /**
```

### FieldMayBeFinal
Field `cacheRemovalTaskIntervalInMillis` may be 'final'
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/support/CacheableFailbackRegistry.java`
#### Snippet
```java

    private ScheduledExecutorService cacheRemovalScheduler;
    private int cacheRemovalTaskIntervalInMillis;
    private int cacheClearWaitingThresholdInMillis;

```

### FieldMayBeFinal
Field `cacheRemovalScheduler` may be 'final'
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/support/CacheableFailbackRegistry.java`
#### Snippet
```java
    protected ConcurrentMap<String, URLParam> stringParam = new ConcurrentHashMap<>();

    private ScheduledExecutorService cacheRemovalScheduler;
    private int cacheRemovalTaskIntervalInMillis;
    private int cacheClearWaitingThresholdInMillis;
```

### FieldMayBeFinal
Field `cacheClearWaitingThresholdInMillis` may be 'final'
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/support/CacheableFailbackRegistry.java`
#### Snippet
```java
    private ScheduledExecutorService cacheRemovalScheduler;
    private int cacheRemovalTaskIntervalInMillis;
    private int cacheClearWaitingThresholdInMillis;

    private Map<ServiceAddressURL, Long> waitForRemove = new ConcurrentHashMap<>();
```

### FieldMayBeFinal
Field `VARIABLE_KEYS` may be 'final'
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/support/CacheableFailbackRegistry.java`
#### Snippet
```java
    private static final ErrorTypeAwareLogger logger = LoggerFactory.getErrorTypeAwareLogger(CacheableFailbackRegistry.class);

    private static String[] VARIABLE_KEYS = new String[]{ENCODED_TIMESTAMP_KEY, ENCODED_PID_KEY};

    protected ConcurrentMap<String, URLAddress> stringAddress = new ConcurrentHashMap<>();
```

### FieldMayBeFinal
Field `semaphore` may be 'final'
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/support/CacheableFailbackRegistry.java`
#### Snippet
```java

    private Map<ServiceAddressURL, Long> waitForRemove = new ConcurrentHashMap<>();
    private Semaphore semaphore = new Semaphore(1);

    private final Map<String, String> extraParameters;
```

### FieldMayBeFinal
Field `waitForRemove` may be 'final'
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/support/CacheableFailbackRegistry.java`
#### Snippet
```java
    private int cacheClearWaitingThresholdInMillis;

    private Map<ServiceAddressURL, Long> waitForRemove = new ConcurrentHashMap<>();
    private Semaphore semaphore = new Semaphore(1);

```

### FieldMayBeFinal
Field `licensedStr` may be 'final'
in `dubbo-native-plugin/src/main/java/org/apache/dubbo/maven/plugin/CodeGenerator.java`
#### Snippet
```java


    private static String licensedStr = "/*\n" +
        " * Licensed to the Apache Software Foundation (ASF) under one or more\n" +
        " * contributor license agreements.  See the NOTICE file distributed with\n" +
```

### FieldMayBeFinal
Field `directory` may be 'final'
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/integration/RegistryDirectory.java`
#### Snippet
```java

    private static class ReferenceConfigurationListener extends AbstractConfiguratorListener {
        private RegistryDirectory directory;
        private URL url;

```

### FieldMayBeFinal
Field `url` may be 'final'
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/integration/RegistryDirectory.java`
#### Snippet
```java
    private static class ReferenceConfigurationListener extends AbstractConfiguratorListener {
        private RegistryDirectory directory;
        private URL url;

        ReferenceConfigurationListener(ModuleModel moduleModel, RegistryDirectory directory, URL url) {
```

### FieldMayBeFinal
Field `sources` may be 'final'
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/ReferenceBean.java`
#### Snippet
```java

    // Registration sources of this reference, may be xml file or annotation location
    private List<Map<String,Object>> sources = new ArrayList<>();

    public ReferenceBean() {
```

### FieldMayBeFinal
Field `targetClass` may be 'final'
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/AbstractAnnotationBeanPostProcessor.java`
#### Snippet
```java
    protected static class AnnotatedInjectionMetadata extends InjectionMetadata {

        private Class<?> targetClass;
        private final Collection<AbstractAnnotationBeanPostProcessor.AnnotatedFieldElement> fieldElements;

```

### FieldMayBeFinal
Field `order` may be 'final'
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/AbstractAnnotationBeanPostProcessor.java`
#### Snippet
```java
    private ClassLoader classLoader;

    private int order = Ordered.LOWEST_PRECEDENCE;

    /**
```

### FieldMayBeFinal
Field `beanPropsCache` may be 'final'
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/schema/DubboBeanDefinitionParser.java`
#### Snippet
```java
    private static boolean resolvePlaceholdersEnabled = true;
    private final Class<?> beanClass;
    private static Map<String, Map<String, Class>> beanPropsCache = new HashMap<>();

    public DubboBeanDefinitionParser(Class<?> beanClass) {
```

### FieldMayBeFinal
Field `exporter` may be 'final'
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/integration/RegistryProtocol.java`
#### Snippet
```java
    private static class DestroyableExporter<T> implements Exporter<T> {

        private Exporter<T> exporter;

        public DestroyableExporter(Exporter<T> exporter) {
```

### FieldMayBeFinal
Field `reExportFailedTasks` may be 'final'
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/integration/RegistryProtocol.java`
#### Snippet
```java
    protected ProxyFactory proxyFactory;

    private ConcurrentMap<URL, ReExportTask> reExportFailedTasks = new ConcurrentHashMap<>();
    private HashedWheelTimer retryTimer = new HashedWheelTimer(new NamedThreadFactory("DubboReexportTimer", true), DEFAULT_REGISTRY_RETRY_PERIOD, TimeUnit.MILLISECONDS, 128);
    private FrameworkModel frameworkModel;
```

### FieldMayBeFinal
Field `notifyListener` may be 'final'
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/integration/RegistryProtocol.java`
#### Snippet
```java
    private class ServiceConfigurationListener extends AbstractConfiguratorListener {
        private URL providerUrl;
        private OverrideListener notifyListener;

        private final ModuleModel moduleModel;
```

### FieldMayBeFinal
Field `providerUrl` may be 'final'
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/integration/RegistryProtocol.java`
#### Snippet
```java

    private class ServiceConfigurationListener extends AbstractConfiguratorListener {
        private URL providerUrl;
        private OverrideListener notifyListener;

```

### FieldMayBeFinal
Field `retryTimer` may be 'final'
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/integration/RegistryProtocol.java`
#### Snippet
```java

    private ConcurrentMap<URL, ReExportTask> reExportFailedTasks = new ConcurrentHashMap<>();
    private HashedWheelTimer retryTimer = new HashedWheelTimer(new NamedThreadFactory("DubboReexportTimer", true), DEFAULT_REGISTRY_RETRY_PERIOD, TimeUnit.MILLISECONDS, 128);
    private FrameworkModel frameworkModel;

```

### FieldMayBeFinal
Field `attributes` may be 'final'
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/reference/ReferenceBeanBuilder.java`
#### Snippet
```java
 */
public class ReferenceBeanBuilder {
    private Map<String, Object> attributes = new HashMap<>();

    public <T> ReferenceBean<T> build() {
```

### FieldMayBeFinal
Field `referenceKeyMap` may be 'final'
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/reference/ReferenceBeanManager.java`
#### Snippet
```java

    //reference key -> reference bean names
    private ConcurrentMap<String, List<String>> referenceKeyMap = new ConcurrentHashMap<>();

    // reference alias -> reference bean name
```

### FieldMayBeFinal
Field `referenceConfigMap` may be 'final'
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/reference/ReferenceBeanManager.java`
#### Snippet
```java

    //reference key -> ReferenceConfig instance
    private ConcurrentMap<String, ReferenceConfig> referenceConfigMap = new ConcurrentHashMap<>();

    private ApplicationContext applicationContext;
```

### FieldMayBeFinal
Field `referenceBeanMap` may be 'final'
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/reference/ReferenceBeanManager.java`
#### Snippet
```java

    //reference bean name -> ReferenceBean
    private ConcurrentMap<String, ReferenceBean> referenceBeanMap = new ConcurrentHashMap<>();

    //reference key -> ReferenceConfig instance
```

### FieldMayBeFinal
Field `referenceAliasMap` may be 'final'
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/reference/ReferenceBeanManager.java`
#### Snippet
```java

    // reference alias -> reference bean name
    private ConcurrentMap<String, String> referenceAliasMap = new ConcurrentHashMap<>();

    //reference bean name -> ReferenceBean
```

### FieldMayBeFinal
Field `applicationModel` may be 'final'
in `dubbo-configcenter/dubbo-configcenter-apollo/src/main/java/org/apache/dubbo/configcenter/support/apollo/ApolloDynamicConfigurationFactory.java`
#### Snippet
```java
public class ApolloDynamicConfigurationFactory extends AbstractDynamicConfigurationFactory {

    private ApplicationModel applicationModel;
    
    public ApolloDynamicConfigurationFactory(ApplicationModel applicationModel) {
```

### FieldMayBeFinal
Field `listeners` may be 'final'
in `dubbo-configcenter/dubbo-configcenter-apollo/src/main/java/org/apache/dubbo/configcenter/support/apollo/ApolloDynamicConfiguration.java`
#### Snippet
```java
    public class ApolloListener implements ConfigChangeListener {

        private Set<ConfigurationListener> listeners = new CopyOnWriteArraySet<>();

        ApolloListener() {
```

### FieldMayBeFinal
Field `frameworkModel` may be 'final'
in `dubbo-plugin/dubbo-qos/src/main/java/org/apache/dubbo/qos/server/Server.java`
#### Snippet
```java
    private EventLoopGroup worker;

    private FrameworkModel frameworkModel;

    public Server(FrameworkModel frameworkModel) {
```

### FieldMayBeFinal
Field `started` may be 'final'
in `dubbo-plugin/dubbo-qos/src/main/java/org/apache/dubbo/qos/server/Server.java`
#### Snippet
```java
    private String welcome;

    private AtomicBoolean started = new AtomicBoolean();

    /**
```

### FieldMayBeFinal
Field `key` may be 'final'
in `dubbo-configcenter/dubbo-configcenter-zookeeper/src/main/java/org/apache/dubbo/configcenter/support/zookeeper/ZookeeperDataListener.java`
#### Snippet
```java

    private String path;
    private String key;
    private String group;
    private Set<ConfigurationListener> listeners;
```

### FieldMayBeFinal
Field `path` may be 'final'
in `dubbo-configcenter/dubbo-configcenter-zookeeper/src/main/java/org/apache/dubbo/configcenter/support/zookeeper/ZookeeperDataListener.java`
#### Snippet
```java
public class ZookeeperDataListener implements DataListener {

    private String path;
    private String key;
    private String group;
```

### FieldMayBeFinal
Field `listeners` may be 'final'
in `dubbo-configcenter/dubbo-configcenter-zookeeper/src/main/java/org/apache/dubbo/configcenter/support/zookeeper/ZookeeperDataListener.java`
#### Snippet
```java
    private String key;
    private String group;
    private Set<ConfigurationListener> listeners;
    private ApplicationModel applicationModel;

```

### FieldMayBeFinal
Field `group` may be 'final'
in `dubbo-configcenter/dubbo-configcenter-zookeeper/src/main/java/org/apache/dubbo/configcenter/support/zookeeper/ZookeeperDataListener.java`
#### Snippet
```java
    private String path;
    private String key;
    private String group;
    private Set<ConfigurationListener> listeners;
    private ApplicationModel applicationModel;
```

### FieldMayBeFinal
Field `applicationModel` may be 'final'
in `dubbo-configcenter/dubbo-configcenter-zookeeper/src/main/java/org/apache/dubbo/configcenter/support/zookeeper/ZookeeperDataListener.java`
#### Snippet
```java
    private String group;
    private Set<ConfigurationListener> listeners;
    private ApplicationModel applicationModel;

    public ZookeeperDataListener(String path, String key, String group, ApplicationModel applicationModel) {
```

### FieldMayBeFinal
Field `description` may be 'final'
in `dubbo-metrics/dubbo-metrics-api/src/main/java/org/apache/dubbo/metrics/model/key/MetricsKey.java`
#### Snippet
```java

    private String name;
    private String description;

    public final String getName() {
```

### FieldMayBeFinal
Field `name` may be 'final'
in `dubbo-metrics/dubbo-metrics-api/src/main/java/org/apache/dubbo/metrics/model/key/MetricsKey.java`
#### Snippet
```java
    ;

    private String name;
    private String description;

```

### FieldMayBeFinal
Field `key` may be 'final'
in `dubbo-filter/dubbo-filter-cache/src/main/java/org/apache/dubbo/cache/support/expiring/ExpiringMap.java`
#### Snippet
```java
     */
    private class ExpiryObject {
        private K key;
        private V value;
        private AtomicLong lastAccessTime;
```

### FieldMayBeFinal
Field `lastAccessTime` may be 'final'
in `dubbo-filter/dubbo-filter-cache/src/main/java/org/apache/dubbo/cache/support/expiring/ExpiringMap.java`
#### Snippet
```java
        private K key;
        private V value;
        private AtomicLong lastAccessTime;

        ExpiryObject(K key, V value, long lastAccessTime) {
```

### FieldMayBeFinal
Field `value` may be 'final'
in `dubbo-filter/dubbo-filter-cache/src/main/java/org/apache/dubbo/cache/support/expiring/ExpiringMap.java`
#### Snippet
```java
    private class ExpiryObject {
        private K key;
        private V value;
        private AtomicLong lastAccessTime;

```

### FieldMayBeFinal
Field `serviceDefinitions` may be 'final'
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/ServiceDefinitionMetadataAnnotationProcessor.java`
#### Snippet
```java
public class ServiceDefinitionMetadataAnnotationProcessor extends AbstractServiceAnnotationProcessor {

    private List<ServiceDefinition> serviceDefinitions = new LinkedList<>();

    @Override
```

### FieldMayBeFinal
Field `serviceRestMetadata` may be 'final'
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/rest/ServiceRestMetadataAnnotationProcessor.java`
#### Snippet
```java
    private ServiceRestMetadataStorage serviceRestMetadataWriter;

    private Set<ServiceRestMetadata> serviceRestMetadata = new LinkedHashSet<>();

    @Override
```

### FieldMayBeFinal
Field `unmergedWeight` may be 'final'
in `dubbo-metrics/dubbo-metrics-api/src/main/java/org/apache/dubbo/metrics/aggregate/DubboMergingDigest.java`
#### Snippet
```java

    // sum_i tempWeight[i]
    private AtomicInteger unmergedWeight = new AtomicInteger(0);

    // this is the index of the next temporary centroid
```

### FieldMayBeFinal
Field `logger` may be 'final'
in `dubbo-remoting/dubbo-remoting-netty/src/main/java/org/apache/dubbo/remoting/transport/netty/NettyHelper.java`
#### Snippet
```java

        public static final String LOGGER_CAUSE_STRING = "unknown error in remoting-netty module";
        private ErrorTypeAwareLogger logger;

        DubboLogger(ErrorTypeAwareLogger logger) {
```

### FieldMayBeFinal
Field `sessionExpireMs` may be 'final'
in `dubbo-remoting/dubbo-remoting-zookeeper/src/main/java/org/apache/dubbo/remoting/zookeeper/curator/CuratorZookeeperClient.java`
#### Snippet
```java
        private long lastSessionId;
        private int timeout;
        private int sessionExpireMs;

        public CuratorConnectionStateListener(URL url) {
```

### FieldMayBeFinal
Field `timeout` may be 'final'
in `dubbo-remoting/dubbo-remoting-zookeeper/src/main/java/org/apache/dubbo/remoting/zookeeper/curator/CuratorZookeeperClient.java`
#### Snippet
```java
        private final long UNKNOWN_SESSION_ID = -1L;
        private long lastSessionId;
        private int timeout;
        private int sessionExpireMs;

```

### FieldMayBeFinal
Field `invokedInvokers` may be 'final'
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcInvocation.java`
#### Snippet
```java
    private transient InvokeMode invokeMode;

    private transient List<Invoker<?>> invokedInvokers = new LinkedList<>();

    /**
```

### FieldMayBeFinal
Field `attributes` may be 'final'
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/protocol/AbstractProxyProtocol.java`
#### Snippet
```java
        private RemotingServer server;
        private String address;
        private Map<String, Object> attributes = new ConcurrentHashMap<>();

        public ProxyProtocolServer(RemotingServer server) {
```

### FieldMayBeFinal
Field `server` may be 'final'
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/protocol/AbstractProxyProtocol.java`
#### Snippet
```java
    protected class ProxyProtocolServer implements ProtocolServer {

        private RemotingServer server;
        private String address;
        private Map<String, Object> attributes = new ConcurrentHashMap<>();
```

### FieldMayBeFinal
Field `delegate` may be 'final'
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/Exporter.java`
#### Snippet
```java
    class CompatibleExporter<T> implements Exporter<T> {

        private org.apache.dubbo.rpc.Exporter<T> delegate;

        public CompatibleExporter(org.apache.dubbo.rpc.Exporter<T> delegate) {
```

### FieldMayBeFinal
Field `delegate` may be 'final'
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/Invocation.java`
#### Snippet
```java
    class CompatibleInvocation implements Invocation {

        private org.apache.dubbo.rpc.Invocation delegate;

        public CompatibleInvocation(org.apache.dubbo.rpc.Invocation invocation) {
```

### FieldMayBeFinal
Field `delegate` may be 'final'
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/Result.java`
#### Snippet
```java

    class CompatibleResult implements Result {
        private org.apache.dubbo.rpc.Result delegate;

        public CompatibleResult(org.apache.dubbo.rpc.Result result) {
```

### FieldMayBeFinal
Field `invoker` may be 'final'
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/Invoker.java`
#### Snippet
```java
    class CompatibleInvoker<T> implements Invoker<T> {

        private org.apache.dubbo.rpc.Invoker<T> invoker;

        public CompatibleInvoker(org.apache.dubbo.rpc.Invoker<T> invoker) {
```

### FieldMayBeFinal
Field `newRpcContext` may be 'final'
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/RpcContext.java`
#### Snippet
```java
    }

    private org.apache.dubbo.rpc.RpcContext newRpcContext;

    public RpcContext(org.apache.dubbo.rpc.RpcContext newRpcContext) {
```

### FieldMayBeFinal
Field `future` may be 'final'
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/protocol/dubbo/FutureAdapter.java`
#### Snippet
```java
public class FutureAdapter<V> implements Future<V> {

    private CompletableFuture<Object> future;

    public FutureAdapter(CompletableFuture<Object> future) {
```

### FieldMayBeFinal
Field `listener` may be 'final'
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/registry/NotifyListener.java`
#### Snippet
```java
    class CompatibleNotifyListener implements NotifyListener {

        private org.apache.dubbo.registry.NotifyListener listener;

        public CompatibleNotifyListener(org.apache.dubbo.registry.NotifyListener listener) {
```

### FieldMayBeFinal
Field `listener` may be 'final'
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/registry/NotifyListener.java`
#### Snippet
```java
    class ReverseCompatibleNotifyListener implements org.apache.dubbo.registry.NotifyListener {

        private NotifyListener listener;

        public ReverseCompatibleNotifyListener(NotifyListener listener) {
```

### FieldMayBeFinal
Field `failbackRegistry` may be 'final'
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/registry/support/FailbackRegistry.java`
#### Snippet
```java
public abstract class FailbackRegistry implements org.apache.dubbo.registry.Registry, Registry {

    private CompatibleFailbackRegistry failbackRegistry;

    public FailbackRegistry(URL url) {
```

### FieldMayBeFinal
Field `compatibleFailbackRegistry` may be 'final'
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/registry/support/FailbackRegistry.java`
#### Snippet
```java
    static class CompatibleFailbackRegistry extends org.apache.dubbo.registry.support.FailbackRegistry {

        private FailbackRegistry compatibleFailbackRegistry;

        public CompatibleFailbackRegistry(org.apache.dubbo.common.URL url, FailbackRegistry compatibleFailbackRegistry) {
```

### FieldMayBeFinal
Field `configService` may be 'final'
in `dubbo-metadata/dubbo-metadata-report-nacos/src/main/java/org/apache/dubbo/metadata/store/nacos/NacosConfigServiceWrapper.java`
#### Snippet
```java
    private static final long DEFAULT_TIMEOUT = 3000L;

    private ConfigService configService;

    public NacosConfigServiceWrapper(ConfigService configService) {
```

### FieldMayBeFinal
Field `casListenerMap` may be 'final'
in `dubbo-metadata/dubbo-metadata-report-nacos/src/main/java/org/apache/dubbo/metadata/store/nacos/NacosMetadataReport.java`
#### Snippet
```java
    private Map<String, NacosConfigListener> watchListenerMap = new ConcurrentHashMap<>();

    private Map<String, MappingDataListener> casListenerMap = new ConcurrentHashMap<>();

    private MD5Utils md5Utils = new MD5Utils();
```

### FieldMayBeFinal
Field `groupId` may be 'final'
in `dubbo-metadata/dubbo-metadata-report-nacos/src/main/java/org/apache/dubbo/metadata/store/nacos/NacosMetadataReport.java`
#### Snippet
```java
        private String dataId;

        private String groupId;

        private String serviceKey;
```

### FieldMayBeFinal
Field `dataId` may be 'final'
in `dubbo-metadata/dubbo-metadata-report-nacos/src/main/java/org/apache/dubbo/metadata/store/nacos/NacosMetadataReport.java`
#### Snippet
```java
    static class MappingDataListener implements ConfigurationListener {

        private String dataId;

        private String groupId;
```

### FieldMayBeFinal
Field `group` may be 'final'
in `dubbo-metadata/dubbo-metadata-report-nacos/src/main/java/org/apache/dubbo/metadata/store/nacos/NacosMetadataReport.java`
#### Snippet
```java
     * The group used to store metadata in Nacos
     */
    private String group;

    private Map<String, NacosConfigListener> watchListenerMap = new ConcurrentHashMap<>();
```

### FieldMayBeFinal
Field `listeners` may be 'final'
in `dubbo-metadata/dubbo-metadata-report-nacos/src/main/java/org/apache/dubbo/metadata/store/nacos/NacosMetadataReport.java`
#### Snippet
```java
        private String serviceKey;

        private Set<MappingListener> listeners;

        public MappingDataListener(String dataId, String groupId) {
```

### FieldMayBeFinal
Field `cacheData` may be 'final'
in `dubbo-metadata/dubbo-metadata-report-nacos/src/main/java/org/apache/dubbo/metadata/store/nacos/NacosMetadataReport.java`
#### Snippet
```java
         * cache data to store old value
         */
        private Map<String, String> cacheData = new ConcurrentHashMap<>();

        @Override
```

### FieldMayBeFinal
Field `listeners` may be 'final'
in `dubbo-metadata/dubbo-metadata-report-nacos/src/main/java/org/apache/dubbo/metadata/store/nacos/NacosMetadataReport.java`
#### Snippet
```java
    public class NacosConfigListener extends AbstractSharedListener {

        private Set<ConfigurationListener> listeners = new CopyOnWriteArraySet<>();
        /**
         * cache data to store old value
```

### FieldMayBeFinal
Field `watchListenerMap` may be 'final'
in `dubbo-metadata/dubbo-metadata-report-nacos/src/main/java/org/apache/dubbo/metadata/store/nacos/NacosMetadataReport.java`
#### Snippet
```java
    private String group;

    private Map<String, NacosConfigListener> watchListenerMap = new ConcurrentHashMap<>();

    private Map<String, MappingDataListener> casListenerMap = new ConcurrentHashMap<>();
```

### FieldMayBeFinal
Field `md5Utils` may be 'final'
in `dubbo-metadata/dubbo-metadata-report-nacos/src/main/java/org/apache/dubbo/metadata/store/nacos/NacosMetadataReport.java`
#### Snippet
```java
    private Map<String, MappingDataListener> casListenerMap = new ConcurrentHashMap<>();

    private MD5Utils md5Utils = new MD5Utils();

    private static final String NACOS_RETRY_KEY = "nacos.retry";
```

### FieldMayBeFinal
Field `configService` may be 'final'
in `dubbo-metadata/dubbo-metadata-report-nacos/src/main/java/org/apache/dubbo/metadata/store/nacos/NacosMetadataReport.java`
#### Snippet
```java
public class NacosMetadataReport extends AbstractMetadataReport {

    private NacosConfigServiceWrapper configService;

    /**
```

### FieldMayBeFinal
Field `serviceKey` may be 'final'
in `dubbo-metadata/dubbo-metadata-report-nacos/src/main/java/org/apache/dubbo/metadata/store/nacos/NacosMetadataReport.java`
#### Snippet
```java
        private String groupId;

        private String serviceKey;

        private Set<MappingListener> listeners;
```

### FieldMayBeFinal
Field `abstractRegistry` may be 'final'
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/registry/support/AbstractRegistry.java`
#### Snippet
```java
public abstract class AbstractRegistry implements Registry {

    private CompatibleAbstractRegistry abstractRegistry;

    public AbstractRegistry(com.alibaba.dubbo.common.URL url) {
```

### FieldMayBeFinal
Field `config` may be 'final'
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/service/TriBuiltinService.java`
#### Snippet
```java
    private ReflectionV1AlphaService reflectionServiceV1Alpha;
    private HealthStatusManager healthStatusManager;
    private Configuration config = ConfigurationUtils.getGlobalConfiguration(
        ApplicationModel.defaultModel());

```

### FieldMayBeFinal
Field `frameworkModel` may be 'final'
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/service/TriBuiltinService.java`
#### Snippet
```java
    private Health healthService;

    private FrameworkModel frameworkModel;

    private ReflectionV1AlphaService reflectionServiceV1Alpha;
```

### FieldMayBeFinal
Field `routerMap` may be 'final'
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/CacheableRouterFactory.java`
#### Snippet
```java
 */
public abstract class CacheableRouterFactory implements RouterFactory {
    private ConcurrentMap<String, Router> routerMap = new ConcurrentHashMap<>();

    @Override
```

### FieldMayBeFinal
Field `mainChain` may be 'final'
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/RouterChain.java`
#### Snippet
```java
    private static final ErrorTypeAwareLogger logger = LoggerFactory.getErrorTypeAwareLogger(RouterChain.class);

    private volatile SingleRouterChain<T> mainChain;
    private volatile SingleRouterChain<T> backupChain;
    private volatile SingleRouterChain<T> currentChain;
```

### FieldMayBeFinal
Field `backupChain` may be 'final'
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/RouterChain.java`
#### Snippet
```java

    private volatile SingleRouterChain<T> mainChain;
    private volatile SingleRouterChain<T> backupChain;
    private volatile SingleRouterChain<T> currentChain;

```

### FieldMayBeFinal
Field `supportedSelectors` may be 'final'
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/filter/support/ConsumerContextFilter.java`
#### Snippet
```java
public class ConsumerContextFilter implements ClusterFilter, ClusterFilter.Listener {

    private Set<PenetrateAttachmentSelector> supportedSelectors;

    public ConsumerContextFilter(ApplicationModel applicationModel) {
```

### FieldMayBeFinal
Field `MERGER_CACHE` may be 'final'
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/merger/MergerFactory.java`
#### Snippet
```java
    private static final ErrorTypeAwareLogger logger = LoggerFactory.getErrorTypeAwareLogger(MergerFactory.class);

    private ConcurrentMap<Class<?>, Merger<?>> MERGER_CACHE = new ConcurrentHashMap<Class<?>, Merger<?>>();
    private ScopeModel scopeModel;

```

### FieldMayBeFinal
Field `originalInvoker` may be 'final'
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/filter/FilterChainBuilder.java`
#### Snippet
```java
    class ClusterCallbackRegistrationInvoker<T, FILTER extends BaseFilter> extends CallbackRegistrationInvoker<T, FILTER>
        implements ClusterInvoker<T> {
        private ClusterInvoker<T> originalInvoker;

        public ClusterCallbackRegistrationInvoker(ClusterInvoker<T> originalInvoker, Invoker<T> filterInvoker, List<FILTER> filters) {
```

### FieldMayBeFinal
Field `accessControlContext` may be 'final'
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/script/ScriptStateRouter.java`
#### Snippet
```java
    private CompiledScript function;

    private AccessControlContext accessControlContext;

    {
```

### FieldMayBeFinal
Field `bitList` may be 'final'
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/state/BitList.java`
#### Snippet
```java

    public static class BitListIterator<E> implements ListIterator<E> {
        private BitList<E> bitList;
        private int index;
        private ListIterator<E> tailListIterator;
```

### FieldMayBeFinal
Field `consumerUrl` may be 'final'
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/support/FailbackClusterInvoker.java`
#### Snippet
```java
        private final long tick;
        private Invoker<T> lastInvoker;
        private URL consumerUrl;

        /**
```

### FieldMayBeFinal
Field `interceptorInvoker` may be 'final'
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/support/wrapper/AbstractCluster.java`
#### Snippet
```java

    static class InvocationInterceptorInvoker<T> extends AbstractClusterInvoker<T> {
        private ClusterInvoker<T> interceptorInvoker;

        public InvocationInterceptorInvoker(AbstractClusterInvoker<T> invoker, List<InvocationInterceptorBuilder> builders) {
```

### FieldMayBeFinal
Field `attachmentKey` may be 'final'
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/loadbalance/AdaptiveLoadBalance.java`
#### Snippet
```java

    //default key
    private String attachmentKey = "mem,load";

    private final AdaptiveMetrics adaptiveMetrics;
```

### FieldMayBeFinal
Field `applicationModel` may be 'final'
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/directory/AbstractDirectory.java`
#### Snippet
```java
    private final int reconnectTaskPeriod;

    private ApplicationModel applicationModel;

    public AbstractDirectory(URL url) {
```

### FieldMayBeFinal
Field `localAddress` may be 'final'
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/RemotingException.java`
#### Snippet
```java
    private static final long serialVersionUID = -3160452149606778709L;

    private InetSocketAddress localAddress;

    private InetSocketAddress remoteAddress;
```

### FieldMayBeFinal
Field `remoteAddress` may be 'final'
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/RemotingException.java`
#### Snippet
```java
    private InetSocketAddress localAddress;

    private InetSocketAddress remoteAddress;

    public RemotingException(Channel channel, String msg) {
```

### FieldMayBeFinal
Field `frameworkModel` may be 'final'
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/api/connection/SingleProtocolConnectionManager.java`
#### Snippet
```java
    private final ConcurrentMap<String, AbstractConnectionClient> connections = new ConcurrentHashMap<>(16);

    private FrameworkModel frameworkModel;

    public SingleProtocolConnectionManager(FrameworkModel frameworkModel) {
```

### FieldMayBeFinal
Field `logger` may be 'final'
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/utils/PayloadDropper.java`
#### Snippet
```java

public class PayloadDropper {
    private static Logger logger = LoggerFactory.getLogger(PayloadDropper.class);

    /**
```

### FieldMayBeFinal
Field `frameworkModel` may be 'final'
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/api/connection/MultiplexProtocolConnectionManager.java`
#### Snippet
```java
    private final ConcurrentMap<String, ConnectionManager> protocols = new ConcurrentHashMap<>(1);

    private FrameworkModel frameworkModel;

    public MultiplexProtocolConnectionManager(FrameworkModel frameworkModel) {
```

### FieldMayBeFinal
Field `processedTable` may be 'final'
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/telnet/support/command/HelpTelnetHandler.java`
#### Snippet
```java
    private static final String MAIN_HELP = "mainHelp";

    private static Map<String, String> processedTable = new WeakHashMap<>();

    public HelpTelnetHandler(FrameworkModel frameworkModel) {
```

### FieldMayBeFinal
Field `ID_SERIALIZATION_MAP` may be 'final'
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/transport/CodecSupport.java`
#### Snippet
```java
public class CodecSupport {
    private static final ErrorTypeAwareLogger logger = LoggerFactory.getErrorTypeAwareLogger(CodecSupport.class);
    private static Map<Byte, Serialization> ID_SERIALIZATION_MAP = new HashMap<Byte, Serialization>();
    private static Map<Byte, String> ID_SERIALIZATIONNAME_MAP = new HashMap<Byte, String>();
    private static Map<String, Byte> SERIALIZATIONNAME_ID_MAP = new HashMap<String, Byte>();
```

### FieldMayBeFinal
Field `ID_NULLBYTES_MAP` may be 'final'
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/transport/CodecSupport.java`
#### Snippet
```java
    private static Map<String, Byte> SERIALIZATIONNAME_ID_MAP = new HashMap<String, Byte>();
    // Cache null object serialize results, for heartbeat request/response serialize use.
    private static ConcurrentMap<Byte, byte[]> ID_NULLBYTES_MAP = new ConcurrentHashMap<>();

    private static final ThreadLocal<byte[]> TL_BUFFER = ThreadLocal.withInitial(() -> new byte[1024]);
```

### FieldMayBeFinal
Field `ID_SERIALIZATIONNAME_MAP` may be 'final'
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/transport/CodecSupport.java`
#### Snippet
```java
    private static final ErrorTypeAwareLogger logger = LoggerFactory.getErrorTypeAwareLogger(CodecSupport.class);
    private static Map<Byte, Serialization> ID_SERIALIZATION_MAP = new HashMap<Byte, Serialization>();
    private static Map<Byte, String> ID_SERIALIZATIONNAME_MAP = new HashMap<Byte, String>();
    private static Map<String, Byte> SERIALIZATIONNAME_ID_MAP = new HashMap<String, Byte>();
    // Cache null object serialize results, for heartbeat request/response serialize use.
```

### FieldMayBeFinal
Field `SERIALIZATIONNAME_ID_MAP` may be 'final'
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/transport/CodecSupport.java`
#### Snippet
```java
    private static Map<Byte, Serialization> ID_SERIALIZATION_MAP = new HashMap<Byte, Serialization>();
    private static Map<Byte, String> ID_SERIALIZATIONNAME_MAP = new HashMap<Byte, String>();
    private static Map<String, Byte> SERIALIZATIONNAME_ID_MAP = new HashMap<String, Byte>();
    // Cache null object serialize results, for heartbeat request/response serialize use.
    private static ConcurrentMap<Byte, byte[]> ID_NULLBYTES_MAP = new ConcurrentHashMap<>();
```

### FieldMayBeFinal
Field `executors` may be 'final'
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/transport/AbstractServer.java`
#### Snippet
```java
public abstract class AbstractServer extends AbstractEndpoint implements RemotingServer {
    private static final ErrorTypeAwareLogger logger = LoggerFactory.getErrorTypeAwareLogger(AbstractServer.class);
    private Set<ExecutorService> executors = new ConcurrentHashSet<>();
    private InetSocketAddress localAddress;
    private InetSocketAddress bindAddress;
```

### FieldMayBeFinal
Field `bindAddress` may be 'final'
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/transport/AbstractServer.java`
#### Snippet
```java
    private Set<ExecutorService> executors = new ConcurrentHashSet<>();
    private InetSocketAddress localAddress;
    private InetSocketAddress bindAddress;
    private int accepts;

```

### FieldMayBeFinal
Field `localAddress` may be 'final'
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/transport/AbstractServer.java`
#### Snippet
```java
    private static final ErrorTypeAwareLogger logger = LoggerFactory.getErrorTypeAwareLogger(AbstractServer.class);
    private Set<ExecutorService> executors = new ConcurrentHashSet<>();
    private InetSocketAddress localAddress;
    private InetSocketAddress bindAddress;
    private int accepts;
```

### FieldMayBeFinal
Field `executorRepository` may be 'final'
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/transport/AbstractServer.java`
#### Snippet
```java
    private int accepts;

    private ExecutorRepository executorRepository;

    public AbstractServer(URL url, ChannelHandler handler) throws RemotingException {
```

### FieldMayBeFinal
Field `codec` may be 'final'
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/transport/codec/CodecAdapter.java`
#### Snippet
```java
public class CodecAdapter implements Codec2 {

    private Codec codec;

    public CodecAdapter(Codec codec) {
```

### FieldMayBeFinal
Field `HTTP_CLIENT_CONNECTION_MANAGER_CLOSE_IDLE_TIME_S` may be 'final'
in `dubbo-remoting/dubbo-remoting-http/src/main/java/org/apache/dubbo/remoting/http/config/HttpClientConfig.java`
#### Snippet
```java
    private int HTTPCLIENT_KEEP_ALIVE_DURATION = 30 * 1000;
    private int HTTP_CLIENT_CONNECTION_MANAGER_CLOSE_WAIT_TIME_MS = 1000;
    private int HTTP_CLIENT_CONNECTION_MANAGER_CLOSE_IDLE_TIME_S = 30;


```

### FieldMayBeFinal
Field `HTTPCLIENT_KEEP_ALIVE_DURATION` may be 'final'
in `dubbo-remoting/dubbo-remoting-http/src/main/java/org/apache/dubbo/remoting/http/config/HttpClientConfig.java`
#### Snippet
```java
    private int HTTP_CLIENT_CONNECTION_MANAGER_MAX_PER_ROUTE = 20;
    private int HTTP_CLIENT_CONNECTION_MANAGER_MAX_TOTAL = 20;
    private int HTTPCLIENT_KEEP_ALIVE_DURATION = 30 * 1000;
    private int HTTP_CLIENT_CONNECTION_MANAGER_CLOSE_WAIT_TIME_MS = 1000;
    private int HTTP_CLIENT_CONNECTION_MANAGER_CLOSE_IDLE_TIME_S = 30;
```

### FieldMayBeFinal
Field `HTTP_CLIENT_CONNECTION_MANAGER_MAX_PER_ROUTE` may be 'final'
in `dubbo-remoting/dubbo-remoting-http/src/main/java/org/apache/dubbo/remoting/http/config/HttpClientConfig.java`
#### Snippet
```java
    private int chunkLength = 8196;

    private int HTTP_CLIENT_CONNECTION_MANAGER_MAX_PER_ROUTE = 20;
    private int HTTP_CLIENT_CONNECTION_MANAGER_MAX_TOTAL = 20;
    private int HTTPCLIENT_KEEP_ALIVE_DURATION = 30 * 1000;
```

### FieldMayBeFinal
Field `chunkLength` may be 'final'
in `dubbo-remoting/dubbo-remoting-http/src/main/java/org/apache/dubbo/remoting/http/config/HttpClientConfig.java`
#### Snippet
```java
    private int writeTimeout = 6 * 1000;
    private int connectTimeout = 6 * 1000;
    private int chunkLength = 8196;

    private int HTTP_CLIENT_CONNECTION_MANAGER_MAX_PER_ROUTE = 20;
```

### FieldMayBeFinal
Field `HTTP_CLIENT_CONNECTION_MANAGER_MAX_TOTAL` may be 'final'
in `dubbo-remoting/dubbo-remoting-http/src/main/java/org/apache/dubbo/remoting/http/config/HttpClientConfig.java`
#### Snippet
```java

    private int HTTP_CLIENT_CONNECTION_MANAGER_MAX_PER_ROUTE = 20;
    private int HTTP_CLIENT_CONNECTION_MANAGER_MAX_TOTAL = 20;
    private int HTTPCLIENT_KEEP_ALIVE_DURATION = 30 * 1000;
    private int HTTP_CLIENT_CONNECTION_MANAGER_CLOSE_WAIT_TIME_MS = 1000;
```

### FieldMayBeFinal
Field `HTTP_CLIENT_CONNECTION_MANAGER_CLOSE_WAIT_TIME_MS` may be 'final'
in `dubbo-remoting/dubbo-remoting-http/src/main/java/org/apache/dubbo/remoting/http/config/HttpClientConfig.java`
#### Snippet
```java
    private int HTTP_CLIENT_CONNECTION_MANAGER_MAX_TOTAL = 20;
    private int HTTPCLIENT_KEEP_ALIVE_DURATION = 30 * 1000;
    private int HTTP_CLIENT_CONNECTION_MANAGER_CLOSE_WAIT_TIME_MS = 1000;
    private int HTTP_CLIENT_CONNECTION_MANAGER_CLOSE_IDLE_TIME_S = 30;

```

### FieldMayBeFinal
Field `server` may be 'final'
in `dubbo-remoting/dubbo-remoting-http/src/main/java/org/apache/dubbo/remoting/http/jetty/JettyHttpServer.java`
#### Snippet
```java
    private static final ErrorTypeAwareLogger logger = LoggerFactory.getErrorTypeAwareLogger(JettyHttpServer.class);

    private Server server;

    private URL url;
```

### FieldMayBeFinal
Field `url` may be 'final'
in `dubbo-remoting/dubbo-remoting-http/src/main/java/org/apache/dubbo/remoting/http/jetty/JettyHttpServer.java`
#### Snippet
```java
    private Server server;

    private URL url;

    public JettyHttpServer(URL url, final HttpHandler handler) {
```

## RuleId[id=CaughtExceptionImmediatelyRethrown]
### CaughtExceptionImmediatelyRethrown
Caught exception `e` is immediately rethrown
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/RestInvoker.java`
#### Snippet
```java
            });
            return asyncRpcResult;
        } catch (RpcException e) {
            throw e;
        }
```

### CaughtExceptionImmediatelyRethrown
Caught exception `e` is immediately rethrown
in `dubbo-common/src/main/java/org/apache/dubbo/common/bytecode/ClassGenerator.java`
#### Snippet
```java
                throw t;
            }
        } catch (RuntimeException e) {
            throw e;
        } catch (NotFoundException | CannotCompileException e) {
```

### CaughtExceptionImmediatelyRethrown
Caught exception `ex` is immediately rethrown
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/ReferenceAnnotationBeanPostProcessor.java`
#### Snippet
```java
            prepareInjection(metadata);
            metadata.inject(bean, beanName, pvs);
        } catch (BeansException ex) {
            throw ex;
        } catch (Throwable ex) {
```

### CaughtExceptionImmediatelyRethrown
Caught exception `e` is immediately rethrown
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/ReferenceAnnotationBeanPostProcessor.java`
#### Snippet
```java
                try {
                    prepareInjection(metadata);
                } catch (BeansException e) {
                    throw e;
                } catch (Exception e) {
```

## RuleId[id=SynchronizationOnLocalVariableOrMethodParameter]
### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `serviceDescriptors`
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ModuleServiceRepository.java`
#### Snippet
```java
        List<ServiceDescriptor> serviceDescriptors = ConcurrentHashMapUtils.computeIfAbsent(services, interfaceClazz.getName(),
            k -> new CopyOnWriteArrayList<>());
        synchronized (serviceDescriptors) {
            Optional<ServiceDescriptor> previous = serviceDescriptors.stream()
                .filter(s -> s.getServiceInterfaceClass().equals(interfaceClazz)).findFirst();
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `serviceDescriptors`
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ModuleServiceRepository.java`
#### Snippet
```java
            List<ServiceDescriptor> serviceDescriptors = ConcurrentHashMapUtils.computeIfAbsent(services, path,
                _k -> new CopyOnWriteArrayList<>());
            synchronized (serviceDescriptors) {
                Optional<ServiceDescriptor> previous = serviceDescriptors.stream()
                    .filter(s -> s.getServiceInterfaceClass().equals(serviceDescriptor.getServiceInterfaceClass())).findFirst();
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on method parameter `type`
in `dubbo-common/src/main/java/org/apache/dubbo/common/beans/factory/ScopeBeanFactory.java`
#### Snippet
```java
        if (bean == null) {
            // lock by type
            synchronized (type) {
                bean = getBean(name, type);
                if (bean == null) {
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on method parameter `type`
in `dubbo-common/src/main/java/org/apache/dubbo/common/beans/factory/ScopeBeanFactory.java`
#### Snippet
```java
        if (bean == null) {
            // lock by type
            synchronized (type) {
                bean = getBean(name, type);
                if (bean == null) {
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `configMap`
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/ConfigurationUtils.java`
#### Snippet
```java
        for (Map<String, V> configMap : configMaps) {
            Map<String, V> copy;
            synchronized (configMap) {
                copy = new HashMap<>(configMap);
            }
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on method parameter `configMap`
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/ConfigurationUtils.java`
#### Snippet
```java
        }
        Map<String, V> copy;
        synchronized (configMap) {
            copy = new HashMap<>(configMap);
        }
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on method parameter `configMap`
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/ConfigurationUtils.java`
#### Snippet
```java
        if (CollectionUtils.isNotEmptyMap(configMap)) {
            Map<String, V> copy;
            synchronized (configMap) {
                copy = new HashMap<>(configMap);
            }
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on method parameter `mutex`
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/configcenter/file/FileSystemDynamicConfiguration.java`
#### Snippet
```java
    private <V> V executeMutually(final Object mutex, Callable<V> callable) {
        V value = null;
        synchronized (mutex) {
            try {
                value = callable.call();
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `holder`
in `dubbo-common/src/main/java/org/apache/dubbo/common/extension/ExtensionLoader.java`
#### Snippet
```java
        Object instance = holder.get();
        if (instance == null) {
            synchronized (holder) {
                instance = holder.get();
                if (instance == null) {
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `configsMap`
in `dubbo-common/src/main/java/org/apache/dubbo/config/context/AbstractConfigManager.java`
#### Snippet
```java

        // lock by config type
        synchronized (configsMap) {
            return (T) addIfAbsent(config, configsMap);
        }
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `configs`
in `dubbo-common/src/main/java/org/apache/dubbo/config/context/AbstractConfigManager.java`
#### Snippet
```java
        if (CollectionUtils.isNotEmptyMap(configs)) {
            // lock by config type
            synchronized (configs) {
                return removeIfAbsent(config, configs);
            }
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on method parameter `rpcStatus`
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/filter/ActiveLimitFilter.java`
#### Snippet
```java
    private void notifyFinish(final RpcStatus rpcStatus, int max) {
        if (max > 0) {
            synchronized (rpcStatus) {
                rpcStatus.notifyAll();
            }
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `rpcStatus`
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/filter/ActiveLimitFilter.java`
#### Snippet
```java
            long start = System.currentTimeMillis();
            long remain = timeout;
            synchronized (rpcStatus) {
                while (!RpcStatus.beginCount(url, methodName, max)) {
                    try {
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `listener`
in `dubbo-registry/dubbo-registry-multicast/src/main/java/org/apache/dubbo/registry/multicast/MulticastRegistry.java`
#### Snippet
```java
                for (final NotifyListener listener : entry.getValue()) {
                    notify(key, listener, list);
                    synchronized (listener) {
                        listener.notify();
                    }
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on method parameter `listener`
in `dubbo-registry/dubbo-registry-multicast/src/main/java/org/apache/dubbo/registry/multicast/MulticastRegistry.java`
#### Snippet
```java
        }
        multicast(SUBSCRIBE + " " + url.toFullString());
        synchronized (listener) {
            try {
                listener.wait(url.getParameter(TIMEOUT_KEY, DEFAULT_TIMEOUT));
```

## RuleId[id=UnnecessaryLocalVariable]
### UnnecessaryLocalVariable
Local variable `cf` is redundant
in `dubbo-demo/dubbo-demo-xml/dubbo-demo-xml-provider/src/main/java/org/apache/dubbo/demo/provider/DemoServiceImpl.java`
#### Snippet
```java
    @Override
    public CompletableFuture<String> sayHelloAsync(String name) {
        CompletableFuture<String> cf = CompletableFuture.supplyAsync(() -> {
//            try {
//                Thread.sleep(1000);
```

### UnnecessaryLocalVariable
Local variable `spiClasses` is redundant
in `dubbo-native/src/main/java/org/apache/dubbo/aot/generate/ClassSourceScanner.java`
#### Snippet
```java
    public List<Class<?>> spiClassesWithAdaptive() {
        Map<String, Class<?>> allClasses = getClasses();
        List<Class<?>> spiClasses = new ArrayList<>(allClasses.values()).stream().filter(it -> {
            if (null == it) {
                return false;
```

### UnnecessaryLocalVariable
Local variable `consumerConfig` is redundant
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/bootstrap/DubboBootstrap.java`
#### Snippet
```java
        ConsumerBuilder builder = createConsumerBuilder(id);
        builderConsumer.accept(builder);
        ConsumerConfig consumerConfig = builder.build();
        return consumerConfig;
    }
```

### UnnecessaryLocalVariable
Local variable `referenceConfig` is redundant
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/bootstrap/DubboBootstrap.java`
#### Snippet
```java
        ReferenceBuilder builder = createReferenceBuilder(id);
        consumerBuilder.accept(builder);
        ReferenceConfig referenceConfig = builder.build();
        return referenceConfig;
    }
```

### UnnecessaryLocalVariable
Local variable `providerConfig` is redundant
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/bootstrap/DubboBootstrap.java`
#### Snippet
```java
        ProviderBuilder builder = createProviderBuilder(id);
        builderConsumer.accept(builder);
        ProviderConfig providerConfig = builder.build();
        return providerConfig;
    }
```

### UnnecessaryLocalVariable
Local variable `serviceConfig` is redundant
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/bootstrap/DubboBootstrap.java`
#### Snippet
```java
        ServiceBuilder builder = createServiceBuilder(id);
        consumerBuilder.accept(builder);
        ServiceConfig serviceConfig = builder.build();
        return serviceConfig;
    }
```

### UnnecessaryLocalVariable
Local variable `metricsInvoker` is redundant
in `dubbo-monitor/dubbo-monitor-default/src/main/java/org/apache/dubbo/monitor/dubbo/MetricsFilter.java`
#### Snippet
```java

    private Invoker<MetricsService> initMetricsInvoker() {
        Invoker<MetricsService> metricsInvoker = new Invoker<MetricsService>() {
            @Override
            public Class<MetricsService> getInterface() {
```

### UnnecessaryLocalVariable
Local variable `supportedExtensionInstances` is redundant
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/rest/AbstractServiceRestMetadataResolver.java`
#### Snippet
```java

    private static Set<NoAnnotatedParameterRequestTagProcessor> loadNoAnnotatedMethodParameterProcessors(ApplicationModel applicationModel) {
        Set<NoAnnotatedParameterRequestTagProcessor> supportedExtensionInstances = applicationModel.getExtensionLoader(NoAnnotatedParameterRequestTagProcessor.class)
            .getSupportedExtensionInstances();

```

### UnnecessaryLocalVariable
Local variable `channelHandlers` is redundant
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/NettyHttpRestServer.java`
#### Snippet
```java
     */
    protected List<ChannelHandler> getChannelHandlers(URL url) {
        List<ChannelHandler> channelHandlers = new ArrayList<>();

        return channelHandlers;
```

### UnnecessaryLocalVariable
Local variable `names` is redundant
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/ReflectUtils.java`
#### Snippet
```java
    public static Class findClass(ClassLoader classLoader, String... name) throws ClassNotFoundException {

        String[] names = name;

        Class tmp;
```

### UnnecessaryLocalVariable
Local variable `defaultList` is redundant
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/message/MediaTypeMatcher.java`
#### Snippet
```java
    private static List<MediaType> getDefaultList() {

        List<MediaType> defaultList = new ArrayList<>();
        return defaultList;
    }
```

### UnnecessaryLocalVariable
Local variable `mediaType` is redundant
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/handler/NettyHttpHandler.java`
#### Snippet
```java
    private MediaType getAcceptMediaType(RequestFacade request, Class<?> returnType) {
        String accept = request.getHeader(RestHeaderEnum.ACCEPT.getHeader());
        MediaType mediaType = MediaTypeUtil.convertMediaType(returnType, accept);
        return mediaType;
    }
```

### UnnecessaryLocalVariable
Local variable `resolve` is redundant
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/annotation/metadata/MetadataResolver.java`
#### Snippet
```java
                ServiceRestMetadata serviceRestMetadata = new ServiceRestMetadata(url.getServiceInterface(), url.getVersion(), url.getGroup(), false);
                serviceRestMetadata.setContextPathFromUrl(contextPathFromUrl);
                ServiceRestMetadata resolve = serviceRestMetadataResolver.resolve(serviceImpl, serviceRestMetadata);
                return resolve;
            }
```

### UnnecessaryLocalVariable
Local variable `resolve` is redundant
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/annotation/metadata/MetadataResolver.java`
#### Snippet
```java
                ServiceRestMetadata serviceRestMetadata = new ServiceRestMetadata(url.getServiceInterface(), url.getVersion(), url.getGroup(), true);
                serviceRestMetadata.setContextPathFromUrl(contextPathFromUrl);
                ServiceRestMetadata resolve = serviceRestMetadataResolver.resolve(targetClass, serviceRestMetadata);
                return resolve;
            }
```

### UnnecessaryLocalVariable
Local variable `cf` is redundant
in `dubbo-test/dubbo-test-common/src/main/java/org/apache/dubbo/test/common/impl/DemoServiceImpl.java`
#### Snippet
```java
    @Override
    public CompletableFuture<String> sayHelloAsync(String name) {
        CompletableFuture<String> cf = CompletableFuture.supplyAsync(() -> {
//            try {
//                Thread.sleep(1000);
```

### UnnecessaryLocalVariable
Local variable `future` is redundant
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ExecutorUtil.java`
#### Snippet
```java

    public static void cancelScheduledFuture(ScheduledFuture<?> scheduledFuture) {
        ScheduledFuture<?> future = scheduledFuture;
        if (future != null && !future.isCancelled()) {
            future.cancel(true);
```

### UnnecessaryLocalVariable
Local variable `ret` is redundant
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
     */
    public static Class<?>[] desc2classArray(String desc) throws ClassNotFoundException {
        Class<?>[] ret = desc2classArray(ClassUtils.getClassLoader(), desc);
        return ret;
    }
```

### UnnecessaryLocalVariable
Local variable `contentList` is redundant
in `dubbo-common/src/main/java/org/apache/dubbo/common/extension/ExtensionLoader.java`
#### Snippet
```java
        }

        List<String> contentList = urlListMap.computeIfAbsent(resourceURL, key -> {
            List<String> newContentList = new ArrayList<>();

```

### UnnecessaryLocalVariable
Local variable `finalInterfaceClass` is redundant
in `dubbo-common/src/main/java/org/apache/dubbo/config/AbstractInterfaceConfig.java`
#### Snippet
```java
                boolean ignoreInvalidMethodConfig = Boolean.parseBoolean(ignoreInvalidMethodConfigVal.toString());

                Class<?> finalInterfaceClass = interfaceClass;
                List<MethodConfig> validMethodConfigs = methodConfigs.stream().filter(methodConfig -> {
                    methodConfig.setParentPrefix(preferredPrefix);
```

### UnnecessaryLocalVariable
Local variable `urls` is redundant
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/support/AbstractRegistry.java`
#### Snippet
```java
        Map<String, List<URL>> categoryNotified = notified.get(url);
        if (CollectionUtils.isNotEmptyMap(categoryNotified)) {
            List<URL> urls = categoryNotified.values().stream().flatMap(Collection::stream).collect(Collectors.toList());
            return urls;
        }
```

### UnnecessaryLocalVariable
Local variable `originalEventSource` is redundant
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/context/DubboBootstrapApplicationListener.java`
#### Snippet
```java
    private boolean isOriginalEventSource(ApplicationEvent event) {

        boolean originalEventSource = nullSafeEquals(getApplicationContext(), event.getSource());
        return originalEventSource;
    }
```

### UnnecessaryLocalVariable
Local variable `interfaceClass` is redundant
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/ReferenceAnnotationBeanPostProcessor.java`
#### Snippet
```java
            }

            Class interfaceClass = beanClass;

            // set attribute instead of property values
```

### UnnecessaryLocalVariable
Local variable `interfaceClass` is redundant
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/ReferenceAnnotationBeanPostProcessor.java`
#### Snippet
```java
        }

        Class interfaceClass = injectedType;

        // TODO Only register one reference bean for same (group, interface, version)
```

### UnnecessaryLocalVariable
Local variable `attributeValue` is redundant
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/schema/DubboBeanDefinitionParser.java`
#### Snippet
```java

    private static String resolveAttribute(Element element, String attributeName, ParserContext parserContext) {
        String attributeValue = element.getAttribute(attributeName);
        // Early resolve place holder may be wrong ( Before PropertySourcesPlaceholderConfigurer/PropertyPlaceholderConfigurer )
        //https://github.com/apache/dubbo/pull/6079
```

### UnnecessaryLocalVariable
Local variable `key` is redundant
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/integration/RegistryProtocol.java`
#### Snippet
```java
    private String getProviderUrlKey(final Invoker<?> originInvoker) {
        URL providerUrl = getProviderUrl(originInvoker);
        String key = providerUrl.removeParameters(DYNAMIC_KEY, ENABLED_KEY).toFullString();
        return key;
    }
```

### UnnecessaryLocalVariable
Local variable `key` is redundant
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/integration/RegistryProtocol.java`
#### Snippet
```java
    private String getRegistryUrlKey(final Invoker<?> originInvoker) {
        URL registryUrl = getRegistryUrl(originInvoker);
        String key = registryUrl.removeParameters(DYNAMIC_KEY, ENABLED_KEY).toFullString();
        return key;
    }
```

### UnnecessaryLocalVariable
Local variable `typeDefinition` is redundant
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/builder/TypeDefinitionBuilder.java`
#### Snippet
```java
     */
    static TypeDefinition build(ProcessingEnvironment processingEnv, Element element, Map<String, TypeDefinition> typeCache) {
        TypeDefinition typeDefinition = build(processingEnv, element.asType(), typeCache);
        // Comment this code for the compatibility
        // typeDefinition.set$ref(element.toString());
```

### UnnecessaryLocalVariable
Local variable `typeDefinition` is redundant
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/builder/PrimitiveTypeDefinitionBuilder.java`
#### Snippet
```java
    @Override
    public TypeDefinition build(ProcessingEnvironment processingEnv, PrimitiveType type, Map<String, TypeDefinition> typeCache) {
        TypeDefinition typeDefinition = new TypeDefinition(type.toString());
        return typeDefinition;
    }
```

### UnnecessaryLocalVariable
Local variable `td` is redundant
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/builder/SimpleTypeDefinitionBuilder.java`
#### Snippet
```java
    @Override
    public TypeDefinition build(ProcessingEnvironment processingEnv, DeclaredType type, Map<String, TypeDefinition> typeCache) {
        TypeDefinition td = new TypeDefinition(type.toString());
        return td;
    }
```

### UnnecessaryLocalVariable
Local variable `finalAccessLogKey` is redundant
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/filter/AccessLogFilter.java`
#### Snippet
```java
            return invoker.invoke(inv);
        } finally {
            String finalAccessLogKey = accessLogKey;
            optionalAccessLogData.ifPresent(logData -> {
                logData.setOutTime(new Date());
```

### UnnecessaryLocalVariable
Local variable `client` is redundant
in `dubbo-remoting/dubbo-remoting-http/src/main/java/org/apache/dubbo/remoting/http/restclient/OKHttpRestClient.java`
#### Snippet
```java

    public OkHttpClient createHttpClient(HttpClientConfig httpClientConfig) {
        OkHttpClient client = new OkHttpClient.Builder().
            readTimeout(httpClientConfig.getReadTimeout(), TimeUnit.SECONDS).
            writeTimeout(httpClientConfig.getWriteTimeout(), TimeUnit.SECONDS).
```

## RuleId[id=BusyWait]
### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `dubbo-demo/dubbo-demo-generic-call/src/main/java/org/apache/dubbo/demo/consumer/GenericApplication.java`
#### Snippet
```java
                    new Object[]{param});
                System.out.println(genericInvokeResult);
                Thread.sleep(1000);
            } catch (Exception e) {
                e.printStackTrace();
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `dubbo-demo/dubbo-demo-xml/dubbo-demo-xml-consumer/src/main/java/org/apache/dubbo/demo/consumer/Application.java`
#### Snippet
```java
                }
                try {
                    Thread.sleep(1000);
                } catch (InterruptedException e) {
                }
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `dubbo-demo/dubbo-demo-xml/dubbo-demo-xml-consumer/src/main/java/org/apache/dubbo/demo/consumer/Application.java`
#### Snippet
```java
                }
                try {
                    Thread.sleep(1000);
                } catch (InterruptedException e) {
                }
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `dubbo-demo/dubbo-demo-xml/dubbo-demo-xml-consumer/src/main/java/org/apache/dubbo/demo/consumer/Application.java`
#### Snippet
```java
            }

            Thread.sleep(5000);
        }
    }
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `dubbo-common/src/main/java/org/apache/dubbo/common/timer/HashedWheelTimer.java`
#### Snippet
```java

                try {
                    Thread.sleep(sleepTimeMs);
                } catch (InterruptedException ignored) {
                    if (WORKER_STATE_UPDATER.get(HashedWheelTimer.this) == WORKER_STATE_SHUTDOWN) {
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/migration/MigrationRuleListener.java`
#### Snippet
```java
                        rule = ruleQueue.take();
                        if (StringUtils.isEmpty(rule)) {
                            Thread.sleep(1000);
                        }
                    } catch (InterruptedException e) {
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `dubbo-filter/dubbo-filter-cache/src/main/java/org/apache/dubbo/cache/support/expiring/ExpiringMap.java`
#### Snippet
```java
                processExpires();
                try {
                    Thread.sleep(expirationIntervalMillis);
                } catch (InterruptedException e) {
                    running = false;
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `dubbo-metrics/dubbo-metrics-api/src/main/java/org/apache/dubbo/metrics/aggregate/DubboMergingDigest.java`
#### Snippet
```java
        while (merging) {
            try {
                Thread.sleep(10);
            } catch (InterruptedException e) {
                Thread.currentThread().interrupt();
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/exchange/support/header/HeaderExchangeServer.java`
#### Snippet
```java
                && System.currentTimeMillis() - start < (long) timeout) {
                try {
                    Thread.sleep(10);
                } catch (InterruptedException e) {
                    logger.warn(TRANSPORT_FAILED_CLOSE, "", "", e.getMessage(), e);
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/exchange/support/header/HeaderExchangeChannel.java`
#### Snippet
```java
                && System.currentTimeMillis() - start < timeout) {
                try {
                    Thread.sleep(10);
                } catch (InterruptedException e) {
                    logger.warn(TRANSPORT_FAILED_CLOSE, "", "", e.getMessage(), e);
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `dubbo-maven-plugin/src/main/java/org/apache/dubbo/maven/plugin/aot/RunProcess.java`
#### Snippet
```java
			}
			try {
				Thread.sleep(500);
			}
			catch (InterruptedException ex) {
```

## RuleId[id=ArraysAsListWithZeroOrOneArgument]
### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `dubbo-spring-boot/dubbo-spring-boot-autoconfigure/src/main/java/org/apache/dubbo/spring/boot/autoconfigure/BinderDubboConfigBinder.java`
#### Snippet
```java
                     boolean ignoreInvalidFields, Object configurationBean) {

        Iterable<PropertySource<?>> propertySources = asList(new MapPropertySource("internal", configurationProperties));

        // Converts ConfigurationPropertySources
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `dubbo-demo/dubbo-demo-xml/dubbo-demo-xml-consumer/src/main/java/org/apache/dubbo/demo/consumer/Application.java`
#### Snippet
```java

                    MultivaluedHashMap multivaluedHashMap = new MultivaluedHashMap();
                    multivaluedHashMap.put("1", Arrays.asList("1"));
                    multivaluedHashMap.put("2", Arrays.asList("2"));
                    MultivaluedMap form2 = restDemoService.testForm2(multivaluedHashMap);
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `dubbo-demo/dubbo-demo-xml/dubbo-demo-xml-consumer/src/main/java/org/apache/dubbo/demo/consumer/Application.java`
#### Snippet
```java
                    MultivaluedHashMap multivaluedHashMap = new MultivaluedHashMap();
                    multivaluedHashMap.put("1", Arrays.asList("1"));
                    multivaluedHashMap.put("2", Arrays.asList("2"));
                    MultivaluedMap form2 = restDemoService.testForm2(multivaluedHashMap);
                    System.out.println(form2);
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `dubbo-demo/dubbo-demo-xml/dubbo-demo-jaxrs-rest-consumer/src/main/java/org/apache/dubbo/demo/rest/api/RestConsumer.java`
#### Snippet
```java

        MultivaluedMapImpl<String, String> forms = new MultivaluedMapImpl<>();
        forms.put("form", Arrays.asList("F1"));

        assertEquals(Arrays.asList("F1"), jaxRsRestDemoService.testMapForm(forms));
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `dubbo-demo/dubbo-demo-xml/dubbo-demo-jaxrs-rest-consumer/src/main/java/org/apache/dubbo/demo/rest/api/RestConsumer.java`
#### Snippet
```java
        forms.put("form", Arrays.asList("F1"));

        assertEquals(Arrays.asList("F1"), jaxRsRestDemoService.testMapForm(forms));
        assertEquals(User.getInstance(), jaxRsRestDemoService.testJavaBeanBody(User.getInstance()));
    }
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `dubbo-demo/dubbo-demo-xml/dubbo-demo-jaxrs-rest-provider/src/main/java/org/apache/dubbo/demo/rest/api/impl/HttpRequestAndResponseRPCContextServiceImpl.java`
#### Snippet
```java
        Map<String, List<String>> outputHeaders = ((NettyHttpResponse) response).getOutputHeaders();
        String responseKey = "response";
        outputHeaders.put(responseKey, Arrays.asList(hello));


```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/annotation/param/parse/consumer/FormConsumerParamParser.java`
#### Snippet
```java
        Map<String, List<String>> tmp = new HashMap<>();
        if (DataParseUtils.isTextType(value.getClass())) {
            tmp.put(argInfo.getAnnotationNameAttribute(), Arrays.asList(String.valueOf(value)));
            requestTemplate.body(tmp, Map.class);
        } else if (value instanceof Map) {
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/annotation/param/parse/consumer/FormConsumerParamParser.java`
#### Snippet
```java

                    Object fieldValue = ReflectUtils.getFieldValue(value, entry);
                    tmp.put(String.valueOf(entry), Arrays.asList(String.valueOf(fieldValue)));
                }
            );
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `dubbo-metadata/dubbo-metadata-report-redis/src/main/java/org/apache/dubbo/metadata/store/redis/RedisMetadataReport.java`
#### Snippet
```java
            return Collections.emptyList();
        }
        return new ArrayList<>(Arrays.asList(URL.decode(content)));
    }

```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `dubbo-common/src/main/java/org/apache/dubbo/config/ProviderConfig.java`
#### Snippet
```java
    @Deprecated
    public void setProtocol(String protocol) {
        this.protocols = new ArrayList<>(Arrays.asList(new ProtocolConfig(protocol)));
    }

```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `dubbo-common/src/main/java/org/apache/dubbo/config/AbstractServiceConfig.java`
#### Snippet
```java

    public void setProtocol(ProtocolConfig protocol) {
        setProtocols(new ArrayList<>(Arrays.asList(protocol)));
    }

```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/reference/ReferenceBeanBuilder.java`
#### Snippet
```java

    public ReferenceBeanBuilder setRegistry(RegistryConfig registry) {
        setRegistries(Arrays.asList(registry));
        return this;
    }
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `dubbo-demo/dubbo-demo-xml/dubbo-demo-spring-mvc-rest-consumer/src/main/java/org/apache/dubbo/demo/rest/api/SpringMvcRestConsumer.java`
#### Snippet
```java

        MultiValueMap<String, String> forms = new LinkedMultiValueMap<>();
        forms.put("form", Arrays.asList("F1"));

        assertEquals(Arrays.asList("F1"), springRestDemoService.testMapForm(forms));
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `dubbo-demo/dubbo-demo-xml/dubbo-demo-spring-mvc-rest-consumer/src/main/java/org/apache/dubbo/demo/rest/api/SpringMvcRestConsumer.java`
#### Snippet
```java
        forms.put("form", Arrays.asList("F1"));

        assertEquals(Arrays.asList("F1"), springRestDemoService.testMapForm(forms));
        assertEquals(User.getInstance(), springRestDemoService.testJavaBeanBody(User.getInstance()));
    }
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `dubbo-registry/dubbo-registry-nacos/src/main/java/org/apache/dubbo/registry/nacos/NacosRegistry.java`
#### Snippet
```java
    private List<String> getCategories(URL url) {
        return ANY_VALUE.equals(url.getServiceInterface()) ?
            ALL_SUPPORTED_CATEGORIES : Arrays.asList(DEFAULT_CATEGORY);
    }

```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcServiceContext.java`
#### Snippet
```java
    @SuppressWarnings({"unchecked", "rawtypes"})
    public List<Invoker<?>> getInvokers() {
        return invokers == null && invoker != null ? (List) Arrays.asList(invoker) : invokers;
    }

```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcServiceContext.java`
#### Snippet
```java
    @Override
    public List<URL> getUrls() {
        return urls == null && url != null ? Arrays.asList(url) : urls;
    }

```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `dubbo-metadata/dubbo-metadata-report-nacos/src/main/java/org/apache/dubbo/metadata/store/nacos/NacosMetadataReport.java`
#### Snippet
```java
            return Collections.emptyList();
        }
        return new ArrayList<String>(Arrays.asList(URL.decode(content)));
    }

```

## RuleId[id=UseBulkOperation]
### UseBulkOperation
Iteration can be replaced with bulk 'Map.putAll()' call
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/AbstractCacheManager.java`
#### Snippet
```java
        try {
            for (Map.Entry<String, V> entry : cache.entrySet()) {
                copyMap.put(entry.getKey(), entry.getValue());
            }
        } finally {
```

### UseBulkOperation
Iteration can be replaced with bulk 'Map.putAll()' call
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/AbstractCacheManager.java`
#### Snippet
```java
    public void update(Map<String, V> newCache) {
        for (Map.Entry<String, V> entry : newCache.entrySet()) {
            cache.put(entry.getKey(), entry.getValue());
        }
    }
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/rest/jaxrs/JAXRSServiceRestMetadataResolver.java`
#### Snippet
```java
        String[] value = getValue(annotation);
        if (value != null) {
            Stream.of(value).forEach(result::add);
        }
    }
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/rest/jaxrs/JAXRSServiceRestMetadataResolver.java`
#### Snippet
```java
        String[] value = getValue(annotation);
        if (value != null) {
            Stream.of(value).forEach(result::add);
        }
    }
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/rest/springmvc/SpringMvcServiceRestMetadataResolver.java`
#### Snippet
```java

        if (isNotEmpty(mediaTypes)) {
            of(mediaTypes).forEach(mediaTypesSet::add);
        }
    }
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/rest/springmvc/SpringMvcServiceRestMetadataResolver.java`
#### Snippet
```java

        if (isNotEmpty(mediaTypes)) {
            of(mediaTypes).forEach(mediaTypesSet::add);
        }
    }
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/ReflectUtils.java`
#### Snippet
```java
    private static void filterConstructMethod(Set<Constructor<?>> methods, Constructor<?>[] declaredMethods) {
        for (Constructor<?> constructor : declaredMethods) {
            methods.add(constructor);
        }

```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/message/MediaTypeMatcher.java`
#### Snippet
```java
        for (MediaType mediaType : mediaTypes) {

            mediaTypeList.add(mediaType);
        }
        return mediaTypeList;
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/MethodUtils.java`
#### Snippet
```java
            // Add the declared methods or public methods
            for (Method method : methods) {
                allMethods.add(method);
            }
        }
```

### UseBulkOperation
Iteration can be replaced with bulk 'Map.putAll()' call
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/ServiceDiscoveryRegistryDirectory.java`
#### Snippet
```java
                // the initial capacity should be set greater than the maximum number of entries divided by the load factor to avoid resizing.
                oldUrlInvokerMap = new LinkedHashMap<>(Math.round(1 + localUrlInvokerMap.size() / DEFAULT_HASHMAP_LOAD_FACTOR));
                localUrlInvokerMap.forEach(oldUrlInvokerMap::put);
            }
            Map<ProtocolServiceKeyWithAddress, Invoker<T>> newUrlInvokerMap = toInvokers(oldUrlInvokerMap, invokerUrls);// Translate url list to Invoker map
```

### UseBulkOperation
Iteration can be replaced with bulk 'Map.putAll()' call
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/integration/RegistryDirectory.java`
#### Snippet
```java
                // the initial capacity should be set greater than the maximum number of entries divided by the load factor to avoid resizing.
                oldUrlInvokerMap = new LinkedHashMap<>(Math.round(1 + localUrlInvokerMap.size() / DEFAULT_HASHMAP_LOAD_FACTOR));
                localUrlInvokerMap.forEach(oldUrlInvokerMap::put);
            }
            Map<URL, Invoker<T>> newUrlInvokerMap = toInvokers(oldUrlInvokerMap, invokerUrls);// Translate url list to Invoker map
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/rest/jaxrs/JAXRSServiceRestMetadataResolver.java`
#### Snippet
```java
        String[] value = getValue(annotation);
        if (value != null) {
            Stream.of(value).forEach(result::add);
        }
    }
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/rest/springmvc/SpringMvcServiceRestMetadataResolver.java`
#### Snippet
```java

        if (isNotEmpty(mediaTypes)) {
            of(mediaTypes).forEach(mediaTypesSet::add);
        }
    }
```

## RuleId[id=StringEquality]
### StringEquality
String values are compared using `==`, not 'equals()'
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/DefaultServiceInstance.java`
#### Snippet
```java
            }
            if (entry.getValue() == null) {
                equals = equals && (entry.getValue() == that.getMetadata().get(entry.getKey()));
            } else {
                equals = equals && entry.getValue().equals(that.getMetadata().get(entry.getKey()));
```

## RuleId[id=EmptyTryBlock]
### EmptyTryBlock
Empty `try` block
in `dubbo-registry/dubbo-registry-multicast/src/main/java/org/apache/dubbo/registry/multicast/MulticastRegistry.java`
#### Snippet
```java
            return false;
        }
        try (Socket socket = new Socket(url.getHost(), url.getPort())) {
        } catch (Throwable e) {
            try {
```

### EmptyTryBlock
Empty `try` block
in `dubbo-registry/dubbo-registry-multicast/src/main/java/org/apache/dubbo/registry/multicast/MulticastRegistry.java`
#### Snippet
```java
            } catch (Throwable e2) {
            }
            try (Socket socket2 = new Socket(url.getHost(), url.getPort())) {
            } catch (Throwable e2) {
                return true;
```

## RuleId[id=SuspiciousSystemArraycopy]
### SuspiciousSystemArraycopy
`result` is not of an array type
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/merger/ArrayMerger.java`
#### Snippet
```java
        for (Object[] array : items) {
            if (array != null) {
                System.arraycopy(array, 0, result, index, array.length);
                index += array.length;
            }
```

## RuleId[id=RedundantClassCall]
### RedundantClassCall
Redundant call to `cast()`
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/NumberUtils.java`
#### Snippet
```java
            return Short.toString(number.shortValue()).getBytes();
        } else if (number instanceof BigDecimal) {
            return BigDecimal.class.cast(number).toString().getBytes();
        }

```

## RuleId[id=UnnecessaryStringEscape]
### UnnecessaryStringEscape
`\'` is unnecessarily escaped
in `dubbo-common/src/main/java/org/apache/dubbo/common/compiler/support/ClassUtils.java`
#### Snippet
```java
                    def = "false";
                } else if (type == char.class) {
                    def = "\'\\0\'";
                } else if (type == byte.class
                        || type == short.class
```

### UnnecessaryStringEscape
`\'` is unnecessarily escaped
in `dubbo-common/src/main/java/org/apache/dubbo/common/compiler/support/ClassUtils.java`
#### Snippet
```java
                    def = "false";
                } else if (type == char.class) {
                    def = "\'\\0\'";
                } else if (type == byte.class
                        || type == short.class
```

### UnnecessaryStringEscape
`\'` is unnecessarily escaped
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/support/MockInvoker.java`
#### Snippet
```java
            value = false;
        } else if (mock.length() >= 2 && (mock.startsWith("\"") && mock.endsWith("\"")
            || mock.startsWith("\'") && mock.endsWith("\'"))) {
            value = mock.subSequence(1, mock.length() - 1);
        } else if (returnTypes != null && returnTypes.length > 0 && returnTypes[0] == String.class) {
```

### UnnecessaryStringEscape
`\'` is unnecessarily escaped
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/support/MockInvoker.java`
#### Snippet
```java
            value = false;
        } else if (mock.length() >= 2 && (mock.startsWith("\"") && mock.endsWith("\"")
            || mock.startsWith("\'") && mock.endsWith("\'"))) {
            value = mock.subSequence(1, mock.length() - 1);
        } else if (returnTypes != null && returnTypes.length > 0 && returnTypes[0] == String.class) {
```

## RuleId[id=ManualArrayCopy]
### ManualArrayCopy
Manual array copy
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/RouterSnapshotSwitcher.java`
#### Snippet
```java
    public String[] cloneSnapshot() {
        String[] clonedSnapshot = new String[MAX_LENGTH];
        for (int i = 0; i < MAX_LENGTH; i++) {
            clonedSnapshot[i] = recentSnapshot[i];
        }
```

## RuleId[id=RefusedBequest]
### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/MetadataInfo.java`
#### Snippet
```java

    @Override
    public synchronized MetadataInfo clone() {
        return new MetadataInfo(app, revision, services, initiated, extendParams, instanceParams, updated, subscribedServiceURLs, exportedServiceURLs, loader);
    }
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/state/BitList.java`
#### Snippet
```java

    @Override
    public BitList<E> clone() {
        return new BitList<>(originList, (BitSet) rootSet.clone(), tailList == null ? null : new LinkedList<>(tailList));
    }
```

## RuleId[id=LongLiteralsEndingWithLowercaseL]
### LongLiteralsEndingWithLowercaseL
'long' literal `1l` ends with lowercase 'l'
in `dubbo-demo/dubbo-demo-interface/src/main/java/po/User.java`
#### Snippet
```java

    public static User getInstance() {
        return new User(1l, "dubbo-rest");
    }

```

### LongLiteralsEndingWithLowercaseL
'long' literal `2l` ends with lowercase 'l'
in `dubbo-demo/dubbo-demo-xml/dubbo-demo-jaxrs-rest-consumer/src/main/java/org/apache/dubbo/demo/rest/api/RestConsumer.java`
#### Snippet
```java
        assertEquals("Hello, hello", hello);
        Integer result = jaxRsRestDemoService.primitiveInt(1, 2);
        Long resultLong = jaxRsRestDemoService.primitiveLong(1, 2l);
        long resultByte = jaxRsRestDemoService.primitiveByte((byte) 1, 2l);
        long resultShort = jaxRsRestDemoService.primitiveShort((short) 1, 2l, 1);
```

### LongLiteralsEndingWithLowercaseL
'long' literal `2l` ends with lowercase 'l'
in `dubbo-demo/dubbo-demo-xml/dubbo-demo-jaxrs-rest-consumer/src/main/java/org/apache/dubbo/demo/rest/api/RestConsumer.java`
#### Snippet
```java
        Integer result = jaxRsRestDemoService.primitiveInt(1, 2);
        Long resultLong = jaxRsRestDemoService.primitiveLong(1, 2l);
        long resultByte = jaxRsRestDemoService.primitiveByte((byte) 1, 2l);
        long resultShort = jaxRsRestDemoService.primitiveShort((short) 1, 2l, 1);

```

### LongLiteralsEndingWithLowercaseL
'long' literal `2l` ends with lowercase 'l'
in `dubbo-demo/dubbo-demo-xml/dubbo-demo-jaxrs-rest-consumer/src/main/java/org/apache/dubbo/demo/rest/api/RestConsumer.java`
#### Snippet
```java
        Long resultLong = jaxRsRestDemoService.primitiveLong(1, 2l);
        long resultByte = jaxRsRestDemoService.primitiveByte((byte) 1, 2l);
        long resultShort = jaxRsRestDemoService.primitiveShort((short) 1, 2l, 1);

        assertEquals(result, 3);
```

### LongLiteralsEndingWithLowercaseL
'long' literal `3l` ends with lowercase 'l'
in `dubbo-demo/dubbo-demo-xml/dubbo-demo-jaxrs-rest-consumer/src/main/java/org/apache/dubbo/demo/rest/api/RestConsumer.java`
#### Snippet
```java

        assertEquals(result, 3);
        assertEquals(resultShort, 3l);
        assertEquals(resultLong, 3l);
        assertEquals(resultByte, 3l);
```

### LongLiteralsEndingWithLowercaseL
'long' literal `3l` ends with lowercase 'l'
in `dubbo-demo/dubbo-demo-xml/dubbo-demo-jaxrs-rest-consumer/src/main/java/org/apache/dubbo/demo/rest/api/RestConsumer.java`
#### Snippet
```java
        assertEquals(result, 3);
        assertEquals(resultShort, 3l);
        assertEquals(resultLong, 3l);
        assertEquals(resultByte, 3l);

```

### LongLiteralsEndingWithLowercaseL
'long' literal `3l` ends with lowercase 'l'
in `dubbo-demo/dubbo-demo-xml/dubbo-demo-jaxrs-rest-consumer/src/main/java/org/apache/dubbo/demo/rest/api/RestConsumer.java`
#### Snippet
```java
        assertEquals(resultShort, 3l);
        assertEquals(resultLong, 3l);
        assertEquals(resultByte, 3l);

        assertEquals(Long.valueOf(1l), jaxRsRestDemoService.testFormBody(1l));
```

### LongLiteralsEndingWithLowercaseL
'long' literal `1l` ends with lowercase 'l'
in `dubbo-demo/dubbo-demo-xml/dubbo-demo-jaxrs-rest-consumer/src/main/java/org/apache/dubbo/demo/rest/api/RestConsumer.java`
#### Snippet
```java
        assertEquals(resultByte, 3l);

        assertEquals(Long.valueOf(1l), jaxRsRestDemoService.testFormBody(1l));

        MultivaluedMapImpl<String, String> forms = new MultivaluedMapImpl<>();
```

### LongLiteralsEndingWithLowercaseL
'long' literal `1l` ends with lowercase 'l'
in `dubbo-demo/dubbo-demo-xml/dubbo-demo-jaxrs-rest-consumer/src/main/java/org/apache/dubbo/demo/rest/api/RestConsumer.java`
#### Snippet
```java
        assertEquals(resultByte, 3l);

        assertEquals(Long.valueOf(1l), jaxRsRestDemoService.testFormBody(1l));

        MultivaluedMapImpl<String, String> forms = new MultivaluedMapImpl<>();
```

### LongLiteralsEndingWithLowercaseL
'long' literal `2l` ends with lowercase 'l'
in `dubbo-demo/dubbo-demo-xml/dubbo-demo-spring-mvc-rest-consumer/src/main/java/org/apache/dubbo/demo/rest/api/SpringMvcRestConsumer.java`
#### Snippet
```java
        assertEquals("Hello, hello", hello);
        Integer result = springRestDemoService.primitiveInt(1, 2);
        Long resultLong = springRestDemoService.primitiveLong(1, 2l);
        long resultByte = springRestDemoService.primitiveByte((byte) 1, 2l);
        long resultShort = springRestDemoService.primitiveShort((short) 1, 2l, 1);
```

### LongLiteralsEndingWithLowercaseL
'long' literal `2l` ends with lowercase 'l'
in `dubbo-demo/dubbo-demo-xml/dubbo-demo-spring-mvc-rest-consumer/src/main/java/org/apache/dubbo/demo/rest/api/SpringMvcRestConsumer.java`
#### Snippet
```java
        Integer result = springRestDemoService.primitiveInt(1, 2);
        Long resultLong = springRestDemoService.primitiveLong(1, 2l);
        long resultByte = springRestDemoService.primitiveByte((byte) 1, 2l);
        long resultShort = springRestDemoService.primitiveShort((short) 1, 2l, 1);

```

### LongLiteralsEndingWithLowercaseL
'long' literal `2l` ends with lowercase 'l'
in `dubbo-demo/dubbo-demo-xml/dubbo-demo-spring-mvc-rest-consumer/src/main/java/org/apache/dubbo/demo/rest/api/SpringMvcRestConsumer.java`
#### Snippet
```java
        Long resultLong = springRestDemoService.primitiveLong(1, 2l);
        long resultByte = springRestDemoService.primitiveByte((byte) 1, 2l);
        long resultShort = springRestDemoService.primitiveShort((short) 1, 2l, 1);

        assertEquals(result, 3);
```

### LongLiteralsEndingWithLowercaseL
'long' literal `3l` ends with lowercase 'l'
in `dubbo-demo/dubbo-demo-xml/dubbo-demo-spring-mvc-rest-consumer/src/main/java/org/apache/dubbo/demo/rest/api/SpringMvcRestConsumer.java`
#### Snippet
```java

        assertEquals(result, 3);
        assertEquals(resultShort, 3l);
        assertEquals(resultLong, 3l);
        assertEquals(resultByte, 3l);
```

### LongLiteralsEndingWithLowercaseL
'long' literal `3l` ends with lowercase 'l'
in `dubbo-demo/dubbo-demo-xml/dubbo-demo-spring-mvc-rest-consumer/src/main/java/org/apache/dubbo/demo/rest/api/SpringMvcRestConsumer.java`
#### Snippet
```java
        assertEquals(result, 3);
        assertEquals(resultShort, 3l);
        assertEquals(resultLong, 3l);
        assertEquals(resultByte, 3l);

```

### LongLiteralsEndingWithLowercaseL
'long' literal `3l` ends with lowercase 'l'
in `dubbo-demo/dubbo-demo-xml/dubbo-demo-spring-mvc-rest-consumer/src/main/java/org/apache/dubbo/demo/rest/api/SpringMvcRestConsumer.java`
#### Snippet
```java
        assertEquals(resultShort, 3l);
        assertEquals(resultLong, 3l);
        assertEquals(resultByte, 3l);

        assertEquals(Long.valueOf(1l), springRestDemoService.testFormBody(1l));
```

### LongLiteralsEndingWithLowercaseL
'long' literal `1l` ends with lowercase 'l'
in `dubbo-demo/dubbo-demo-xml/dubbo-demo-spring-mvc-rest-consumer/src/main/java/org/apache/dubbo/demo/rest/api/SpringMvcRestConsumer.java`
#### Snippet
```java
        assertEquals(resultByte, 3l);

        assertEquals(Long.valueOf(1l), springRestDemoService.testFormBody(1l));

        MultiValueMap<String, String> forms = new LinkedMultiValueMap<>();
```

### LongLiteralsEndingWithLowercaseL
'long' literal `1l` ends with lowercase 'l'
in `dubbo-demo/dubbo-demo-xml/dubbo-demo-spring-mvc-rest-consumer/src/main/java/org/apache/dubbo/demo/rest/api/SpringMvcRestConsumer.java`
#### Snippet
```java
        assertEquals(resultByte, 3l);

        assertEquals(Long.valueOf(1l), springRestDemoService.testFormBody(1l));

        MultiValueMap<String, String> forms = new LinkedMultiValueMap<>();
```

## RuleId[id=RedundantArrayCreation]
### RedundantArrayCreation
Redundant array creation for calling varargs method
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
        Constructor<?> targetConstructor;
        try {
            targetConstructor = clazz.getConstructor(new Class<?>[]{paramType});
        } catch (NoSuchMethodException e) {
            targetConstructor = null;
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/status/SpringStatusChecker.java`
#### Snippet
```java
            while (cls != null && method == null) {
                try {
                    method = cls.getDeclaredMethod("getConfigLocations", new Class<?>[0]);
                } catch (NoSuchMethodException t) {
                    cls = cls.getSuperclass();
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/schema/DubboBeanDefinitionParser.java`
#### Snippet
```java
                Method getter = null;
                try {
                    getter = beanClass.getMethod("get" + name.substring(3), new Class<?>[0]);
                } catch (NoSuchMethodException e) {
                    try {
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/schema/DubboBeanDefinitionParser.java`
#### Snippet
```java
                } catch (NoSuchMethodException e) {
                    try {
                        getter = beanClass.getMethod("is" + name.substring(3), new Class<?>[0]);
                    } catch (NoSuchMethodException e2) {
                        // ignore, there is no need any log here since some class implement the interface: EnvironmentAware,
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/container/page/Page.java`
#### Snippet
```java
    public Page(String navigation, String title,
                String column, String row) {
        this(navigation, title, column == null ? null : Arrays.asList(new String[]{column}), row == null ? null : stringToList(row));
    }

```

## RuleId[id=NonFinalFieldInEnum]
### NonFinalFieldInEnum
Non-final field `annotationClasses` in enum 'ParamType'
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/rest/ParamType.java`
#### Snippet
```java

    EMPTY(addSupportTypes());
    private List<Class> annotationClasses;


```

### NonFinalFieldInEnum
Non-final field `value` in enum 'MediaType'
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/rest/media/MediaType.java`
#### Snippet
```java
    }

    public String value;

    public static String getAllContentType() {
```

### NonFinalFieldInEnum
Non-final field `mediaTypes` in enum 'MediaTypeMatcher'
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/message/MediaTypeMatcher.java`
#### Snippet
```java
    ;

    private List<MediaType> mediaTypes;


```

### NonFinalFieldInEnum
Non-final field `description` in enum 'MetricsKey'
in `dubbo-metrics/dubbo-metrics-api/src/main/java/org/apache/dubbo/metrics/model/key/MetricsKey.java`
#### Snippet
```java

    private String name;
    private String description;

    public final String getName() {
```

### NonFinalFieldInEnum
Non-final field `name` in enum 'MetricsKey'
in `dubbo-metrics/dubbo-metrics-api/src/main/java/org/apache/dubbo/metrics/model/key/MetricsKey.java`
#### Snippet
```java
    ;

    private String name;
    private String description;

```

## RuleId[id=RegExpSimplifiable]
### RegExpSimplifiable
`[,]` can be simplified to ','
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/utils/ConfigValidationUtils.java`
#### Snippet
```java
        checkMultiName(property, value);
        if (isNotEmpty(value)) {
            String[] values = value.split("\\s*[,]+\\s*");
            for (String v : values) {
                if (v.startsWith(REMOVE_VALUE_PREFIX)) {
```

### RegExpSimplifiable
`[=]` can be simplified to '='
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/StringUtils.java`
#### Snippet
```java

    private static final ErrorTypeAwareLogger logger = LoggerFactory.getErrorTypeAwareLogger(StringUtils.class);
    private static final Pattern KVP_PATTERN = Pattern.compile("([_.a-zA-Z0-9][-_.a-zA-Z0-9]*)[=](.*)"); //key value pair pattern.
    private static final Pattern NUM_PATTERN = Pattern.compile("^\\d+$");
    private static final Pattern PARAMETERS_PATTERN = Pattern.compile("^\\[((\\s*\\{\\s*[\\w_\\-\\.]+\\s*:\\s*.+?\\s*\\}\\s*,?\\s*)+)\\s*\\]$");
```

### RegExpSimplifiable
`[-]` can be simplified to '-'
in `dubbo-common/src/main/java/org/apache/dubbo/common/logger/support/FailsafeErrorTypeAwareLogger.java`
#### Snippet
```java
        }

        String[] segments = trimmedString.split("[-]");

        int[] errorCodeSegments = new int[2];
```

### RegExpSimplifiable
`[,]` can be simplified to ','
in `dubbo-common/src/main/java/org/apache/dubbo/common/extension/ExtensionLoader.java`
#### Snippet
```java
        ExtensionLoader.class);

    private static final Pattern NAME_SEPARATOR         = Pattern.compile("\\s*[,]+\\s*");
    private static final String  SPECIAL_SPI_PROPERTIES = "special_spi.properties";

```

### RegExpSimplifiable
`[;]` can be simplified to ';'
in `dubbo-common/src/main/java/org/apache/dubbo/common/constants/CommonConstants.java`
#### Snippet
```java
    String SEMICOLON_SEPARATOR = ";";

    Pattern SEMICOLON_SPLIT_PATTERN = Pattern.compile("\\s*[;]+\\s*");

    Pattern EQUAL_SPLIT_PATTERN = Pattern.compile("\\s*[=]+\\s*");
```

### RegExpSimplifiable
`[=]` can be simplified to '='
in `dubbo-common/src/main/java/org/apache/dubbo/common/constants/CommonConstants.java`
#### Snippet
```java
    Pattern SEMICOLON_SPLIT_PATTERN = Pattern.compile("\\s*[;]+\\s*");

    Pattern EQUAL_SPLIT_PATTERN = Pattern.compile("\\s*[=]+\\s*");

    String DEFAULT_PROXY = "javassist";
```

### RegExpSimplifiable
`[,]` can be simplified to ','
in `dubbo-common/src/main/java/org/apache/dubbo/common/constants/CommonConstants.java`
#### Snippet
```java
    String DOT_SEPARATOR = ".";

    Pattern COMMA_SPLIT_PATTERN = Pattern.compile("\\s*[,]+\\s*");

    String PATH_SEPARATOR = "/";
```

### RegExpSimplifiable
`[\\\\]` can be simplified to '\\\\'
in `dubbo-plugin/dubbo-qos/src/main/java/org/apache/dubbo/qos/command/decoder/TelnetCommandDecoder.java`
#### Snippet
```java
        if (!StringUtils.isBlank(str)) {
            str = str.trim();
            String[] array = str.split("(?<![\\\\]) ");
            if (array.length > 0) {
                String[] targetArgs = new String[array.length - 1];
```

## RuleId[id=DuplicateExpressions]
### DuplicateExpressions
Multiple occurrences of `Paths.get(zookeeperConf.toString(), "log4j.properties")`
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/registrycenter/initializer/ConfigZookeeperInitializer.java`
#### Snippet
```java
        }

        File log4j = Paths.get(zookeeperConf.toString(), "log4j.properties").toFile();
        try {
            // use Files.newInputStream instead of new FileInputStream
```

### DuplicateExpressions
Multiple occurrences of `Paths.get(zookeeperConf.toString(), "log4j.properties")`
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/registrycenter/initializer/ConfigZookeeperInitializer.java`
#### Snippet
```java
            FileOutputStream oFile = null;
            try {
                oFile = new FileOutputStream(Paths.get(zookeeperConf.toString(), "log4j.properties").toFile());
                properties.store(oFile, "");
            } finally {
```

## RuleId[id=StringBufferReplaceableByString]
### StringBufferReplaceableByString
`StringBuilder bannerTextBuilder` can be replaced with 'String'
in `dubbo-spring-boot/dubbo-spring-boot-compatible/autoconfigure/src/main/java/org/apache/dubbo/spring/boot/context/event/WelcomeLogoApplicationListener.java`
#### Snippet
```java
    String buildBannerText() {

        StringBuilder bannerTextBuilder = new StringBuilder();

        bannerTextBuilder
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/rest/ServiceRestMetadata.java`
#### Snippet
```java
    @Override
    public String toString() {
        final StringBuilder sb = new StringBuilder("ServiceRestMetadata{");
        sb.append("serviceInterface='").append(serviceInterface).append('\'');
        sb.append(", version='").append(version).append('\'');
```

### StringBufferReplaceableByString
`StringBuilder serviceNameBuilder` can be replaced with 'String'
in `dubbo-common/src/main/java/org/apache/dubbo/common/BaseServiceMetadata.java`
#### Snippet
```java
     */
    public String getDisplayServiceKey() {
        StringBuilder serviceNameBuilder = new StringBuilder();
        serviceNameBuilder.append(serviceInterfaceName);
        serviceNameBuilder.append(COLON_SEPARATOR).append(version);
```

### StringBufferReplaceableByString
`StringBuilder code` can be replaced with 'String'
in `dubbo-common/src/main/java/org/apache/dubbo/common/extension/AdaptiveClassCodeGenerator.java`
#### Snippet
```java
    private String generateGetUrlNullCheck(int index, Class<?> type, String method) {
        // Null point check
        StringBuilder code = new StringBuilder();
        code.append(String.format("if (arg%d == null) throw new IllegalArgumentException(\"%s argument == null\");\n",
                index, type.getName()));
```

### StringBufferReplaceableByString
`StringBuilder builder` can be replaced with 'String'
in `dubbo-common/src/main/java/org/apache/dubbo/common/extension/AdaptiveClassCodeGenerator.java`
#### Snippet
```java
     */
    private String generateImports() {
        StringBuilder builder = new StringBuilder();
        builder.append(String.format(CODE_IMPORTS, ScopeModel.class.getName()));
        builder.append(String.format(CODE_IMPORTS, ScopeModelUtil.class.getName()));
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `dubbo-registry/dubbo-registry-nacos/src/main/java/org/apache/dubbo/registry/nacos/NacosServiceName.java`
#### Snippet
```java

    private String toValue() {
        return new StringBuilder(category)
                .append(NAME_SEPARATOR).append(serviceInterface)
                .append(NAME_SEPARATOR).append(version)
```

### StringBufferReplaceableByString
`StringBuilder result` can be replaced with 'String'
in `dubbo-plugin/dubbo-qos/src/main/java/org/apache/dubbo/qos/command/impl/Ls.java`
#### Snippet
```java
    @Override
    public String execute(CommandContext commandContext, String[] args) {
        StringBuilder result = new StringBuilder();
        result.append(listProvider());
        result.append(listConsumer());
```

### StringBufferReplaceableByString
`StringBuilder versionDescBuilder` can be replaced with 'String'
in `dubbo-plugin/dubbo-qos/src/main/java/org/apache/dubbo/qos/command/impl/Version.java`
#### Snippet
```java
    @Override
    public String execute(CommandContext commandContext, String[] args) {
        StringBuilder versionDescBuilder = new StringBuilder();
        versionDescBuilder.append("dubbo version \"");
        versionDescBuilder.append(org.apache.dubbo.common.Version.getVersion());
```

### StringBufferReplaceableByString
`StringBuilder buf` can be replaced with 'String'
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/TimeoutCountDown.java`
#### Snippet
```java
        long remainingMillis = timeRemaining(TimeUnit.MILLISECONDS);

        StringBuilder buf = new StringBuilder();
        buf.append("Total timeout value - ");
        buf.append(timeoutMillis);
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/filter/ProfilerServerFilter.java`
#### Snippet
```java
            OperatingSystemMXBean operatingSystemMXBean = ManagementFactory.getOperatingSystemMXBean();

            StringBuilder sb = new StringBuilder(64);
            sb.append("curTime:").append(System.currentTimeMillis());
            sb.append(COMMA_SEPARATOR).append("load:").append(operatingSystemMXBean.getSystemLoadAverage() * 100 / operatingSystemMXBean.getAvailableProcessors() );
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/filter/AdaptiveLoadBalanceFilter.java`
#### Snippet
```java

    private String buildServiceKey(Invocation invocation){
        StringBuilder sb = new StringBuilder(128);
        sb.append(invocation.getInvoker().getUrl().getAddress()).append(":").append(invocation.getProtocolServiceKey());
        return sb.toString();
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/loadbalance/AdaptiveLoadBalance.java`
#### Snippet
```java
    private String buildServiceKey(Invoker<?> invoker,Invocation invocation){
        URL url = invoker.getUrl();
        StringBuilder sb = new StringBuilder(128);
        sb.append(url.getAddress()).append(":").append(invocation.getProtocolServiceKey());
        return sb.toString();
```

### StringBufferReplaceableByString
`StringBuilder buf` can be replaced with 'String'
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/telnet/support/command/HelpTelnetHandler.java`
#### Snippet
```java
        TelnetHandler handler = extensionLoader.getExtension(message);
        Help help = handler.getClass().getAnnotation(Help.class);
        StringBuilder buf = new StringBuilder();
        buf.append("Command:\r\n    ");
        buf.append(message + " " + help.parameter().replace("\r\n", " ").replace("\n", " "));
```

### StringBufferReplaceableByString
`StringBuilder stringBuilder` can be replaced with 'String'
in `dubbo-remoting/dubbo-remoting-http/src/main/java/org/apache/dubbo/remoting/http/RequestTemplate.java`
#### Snippet
```java

    public String getUri() {
        StringBuilder stringBuilder = new StringBuilder(getContextPath() + path);
        return stringBuilder.append(getQueryString()).toString();
    }
```

### StringBufferReplaceableByString
`StringBuilder stringBuilder` can be replaced with 'String'
in `dubbo-remoting/dubbo-remoting-http/src/main/java/org/apache/dubbo/remoting/http/RequestTemplate.java`
#### Snippet
```java

    public String getURL() {
        StringBuilder stringBuilder = new StringBuilder(getProtocol() + address);

        stringBuilder.append(getUri());
```

## RuleId[id=UnnecessaryReturn]
### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/utils/ConfigValidationUtils.java`
#### Snippet
```java
    public static void validateMetricsConfig(MetricsConfig metricsConfig) {
        if (metricsConfig == null) {
            return;
        }
    }
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/utils/ConfigValidationUtils.java`
#### Snippet
```java
    public static void validateSslConfig(SslConfig sslConfig) {
        if (sslConfig == null) {
            return;
        }
    }
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/utils/ConfigValidationUtils.java`
#### Snippet
```java
    public static void validateConsumerConfig(ConsumerConfig config) {
        if (config == null) {
            return;
        }
    }
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/utils/ConfigValidationUtils.java`
#### Snippet
```java
    public static void validateTracingConfig(TracingConfig tracingConfig) {
        if (tracingConfig == null) {
            return;
        }
    }
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `dubbo-demo/dubbo-demo-xml/dubbo-demo-jaxrs-rest-consumer/src/main/java/org/apache/dubbo/demo/rest/api/RestConsumer.java`
#### Snippet
```java

        if (equal) {
            return;
        } else {
            throw new RuntimeException();
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `dubbo-demo/dubbo-demo-xml/dubbo-demo-spring-mvc-rest-consumer/src/main/java/org/apache/dubbo/demo/rest/api/SpringMvcRestConsumer.java`
#### Snippet
```java

        if (equal) {
            return;
        } else {
            throw new RuntimeException();
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/filter/GenericFilter.java`
#### Snippet
```java
                }
            } else if (ProtocolUtils.isGenericReturnRawResult(generic)) {
                return;
            } else {
                appResponse.setValue(PojoUtils.generalize(appResponse.getValue()));
```

## RuleId[id=SimplifiableConditionalExpression]
### SimplifiableConditionalExpression
`Boolean.TRUE.equals(application.get().getEnableFileCache()) ? true : false` can be simplified to 'Boolean.TRUE.equals(application.get().getEnableFileCache())'
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/AbstractServiceNameMapping.java`
#### Snippet
```java
        Optional<ApplicationConfig> application = applicationModel.getApplicationConfigManager().getApplication();
        if (application.isPresent()) {
            enableFileCache = Boolean.TRUE.equals(application.get().getEnableFileCache()) ? true : false;
        }
        this.mappingCacheManager = new MappingCacheManager(enableFileCache,
```

### SimplifiableConditionalExpression
`this.needCompareMethod ? Objects.equals(httpMethod, that.httpMethod) : true` can be simplified to '!this.needCompareMethod \|\| Objects.equals(httpMethod, that.httpMethod)'
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/rest/PathMatcher.java`
#### Snippet
```java
        return pathEqual(that)
            && Objects.equals(version, that.version)
            && (this.needCompareMethod ? Objects.equals(httpMethod, that.httpMethod) : true)
            && Objects.equals(group, that.group) && Objects.equals(port, that.port);
    }
```

### SimplifiableConditionalExpression
`v == null ? false : v.booleanValue()` can be simplified to 'v!=null \&\& v.booleanValue()'
in `dubbo-common/src/main/java/org/apache/dubbo/common/compiler/support/ClassUtils.java`
#### Snippet
```java

    public static boolean unboxed(Boolean v) {
        return v == null ? false : v.booleanValue();
    }

```

### SimplifiableConditionalExpression
`export == null ? true : export` can be simplified to 'export == null \|\| export'
in `dubbo-common/src/main/java/org/apache/dubbo/config/ServiceConfigBase.java`
#### Snippet
```java
        Boolean export = getExport();
        // default value is true
        return export == null ? true : export;
    }

```

### SimplifiableConditionalExpression
`member == null || kind == null ? false : kind.equals(member.getKind())` can be simplified to 'member!=null \&\& kind!=null \&\& kind.equals(member.getKind())'
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/util/MemberUtils.java`
#### Snippet
```java

    static boolean matches(Element member, ElementKind kind) {
        return member == null || kind == null ? false : kind.equals(member.getKind());
    }

```

## RuleId[id=ManualMinMaxCalculation]
### ManualMinMaxCalculation
Can be replaced with 'Math.max()' call
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ConfigUtils.java`
#### Snippet
```java
            // add default extension
            int i = names.indexOf(DEFAULT_KEY);
            if (i > 0) {
                names.addAll(i, defaults);
            } else {
```

### ManualMinMaxCalculation
Can be replaced with 'Math.max()' call
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/StringUtils.java`
#### Snippet
```java
        final int replLength = searchString.length();
        int increase = replacement.length() - replLength;
        increase = increase < 0 ? 0 : increase;
        increase *= max < 0 ? 16 : max > 64 ? 64 : max;
        final StringBuilder buf = new StringBuilder(text.length() + increase);
```

### ManualMinMaxCalculation
Can be replaced with 'Math.min()' call
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/StringUtils.java`
#### Snippet
```java
        int increase = replacement.length() - replLength;
        increase = increase < 0 ? 0 : increase;
        increase *= max < 0 ? 16 : max > 64 ? 64 : max;
        final StringBuilder buf = new StringBuilder(text.length() + increase);
        while (end != INDEX_NOT_FOUND) {
```

### ManualMinMaxCalculation
Can be replaced with 'Math.max()' call
in `dubbo-remoting/dubbo-remoting-netty/src/main/java/org/apache/dubbo/remoting/transport/netty/NettyCodecAdapter.java`
#### Snippet
```java
                    int size = buffer.readableBytes() + input.readableBytes();
                    message = org.apache.dubbo.remoting.buffer.ChannelBuffers.dynamicBuffer(
                            size > bufferSize ? size : bufferSize);
                    message.writeBytes(buffer, buffer.readableBytes());
                    message.writeBytes(input.toByteBuffer());
```

### ManualMinMaxCalculation
Can be replaced with 'Math.min()' call
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/cluster/loadbalance/AbstractLoadBalance.java`
#### Snippet
```java
    static int calculateWarmupWeight(int uptime, int warmup, int weight) {
        int ww = (int) ( (float) uptime / ( (float) warmup / (float) weight ) );
        return ww < 1 ? 1 : (ww > weight ? weight : ww);
    }
}
```

### ManualMinMaxCalculation
Can be replaced with 'Math.min()' call
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/telnet/support/TelnetUtils.java`
#### Snippet
```java
            for (int j = 0; j < widths.length; j++) {
                widths[j] = Math.max(widths[j], row.get(j).length());
                totalWidth = (totalWidth + widths[j]) > maxwidth ? maxwidth : (totalWidth + widths[j]);
                if (j < widths.length - 1) {
                    countbefore = countbefore + widths[j];
```

## RuleId[id=JavadocLinkAsPlainText]
### JavadocLinkAsPlainText
Link specified as plain text
in `dubbo-spring-boot/dubbo-spring-boot-compatible/autoconfigure/src/main/java/org/apache/dubbo/spring/boot/env/DubboDefaultPropertiesEnvironmentPostProcessor.java`
#### Snippet
```java
    /**
     * The property name of "spring.main.allow-bean-definition-overriding".
     * Please refer to: https://github.com/spring-projects/spring-boot/wiki/Spring-Boot-2.1-Release-Notes#bean-overriding
     */
    public static final String ALLOW_BEAN_DEFINITION_OVERRIDING_PROPERTY = "spring.main.allow-bean-definition-overriding";
```

### JavadocLinkAsPlainText
Link specified as plain text
in `dubbo-common/src/main/java/org/apache/dubbo/common/URL.java`
#### Snippet
```java

    /**
     * refer to https://datatracker.ietf.org/doc/html/rfc3986
     *
     * @return authority
```

### JavadocLinkAsPlainText
Link specified as plain text
in `dubbo-common/src/main/java/org/apache/dubbo/common/URL.java`
#### Snippet
```java

    /**
     * refer to https://datatracker.ietf.org/doc/html/rfc3986
     *
     * @return user information
```

### JavadocLinkAsPlainText
Link specified as plain text
in `dubbo-common/src/main/java/org/apache/dubbo/common/URL.java`
#### Snippet
```java
 * url example:
 * <ul>
 * <li>http://www.facebook.com/friends?param1=value1&amp;param2=value2
 * <li>http://username:password@10.20.130.230:8080/list?version=1.0.0
 * <li>ftp://username:password@192.168.1.7:21/1/read.txt
```

### JavadocLinkAsPlainText
Link specified as plain text
in `dubbo-common/src/main/java/org/apache/dubbo/common/URL.java`
#### Snippet
```java
 * <ul>
 * <li>http://www.facebook.com/friends?param1=value1&amp;param2=value2
 * <li>http://username:password@10.20.130.230:8080/list?version=1.0.0
 * <li>ftp://username:password@192.168.1.7:21/1/read.txt
 * <li>registry://192.168.1.7:9090/org.apache.dubbo.service1?param1=value1&amp;param2=value2
```

### JavadocLinkAsPlainText
Link specified as plain text
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ExecutorUtil.java`
#### Snippet
```java
    /**
     * Use the shutdown pattern from:
     * https://docs.oracle.com/javase/8/docs/api/java/util/concurrent/ExecutorService.html
     *
     * @param executor the Executor to shutdown
```

### JavadocLinkAsPlainText
Link specified as plain text
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/CIDRUtils.java`
#### Snippet
```java
 * both IPv4 and IPv6.
 * <p>
 * From https://github.com/edazdarevic/CIDRUtils/blob/master/CIDRUtils.java
 */
public class CIDRUtils {
```

### JavadocLinkAsPlainText
Link specified as plain text
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/metadata/SpringCloudMetadataServiceURLBuilder.java`
#### Snippet
```java

/**
 * Supporting interaction with Dubbo Spring Cloud at https://github.com/alibaba/spring-cloud-alibaba
 * Dubbo Spring Cloud is a Dubbo extension that favours a per instance registry model and exposes metadata service.
 *
```

### JavadocLinkAsPlainText
Link specified as plain text
in `dubbo-configcenter/dubbo-configcenter-apollo/src/main/java/org/apache/dubbo/configcenter/support/apollo/ApolloDynamicConfiguration.java`
#### Snippet
```java

/**
 * Apollo implementation, https://github.com/ctripcorp/apollo
 * <p>
 * Apollo will be used for management of both governance rules and .properties files, by default, these two different
```

### JavadocLinkAsPlainText
Link specified as plain text
in `dubbo-configcenter/dubbo-configcenter-apollo/src/main/java/org/apache/dubbo/configcenter/support/apollo/ApolloDynamicConfiguration.java`
#### Snippet
```java
 * 'group=dubbo' is for properties files.
 * <p>
 * Please see http://dubbo.apache.org/zh-cn/docs/user/configuration/config-center.html for details.
 */
public class ApolloDynamicConfiguration implements DynamicConfiguration {
```

### JavadocLinkAsPlainText
Link specified as plain text
in `dubbo-serialization/dubbo-serialization-api/src/main/java/org/apache/dubbo/common/serialize/ObjectOutput.java`
#### Snippet
```java
     * should be highly independent and portable, easy to cross platforms and languages, for example, like the http headers,
     * restricting the content of headers / attachments to Ascii strings and uses ISO_8859_1 to encode them.
     * https://tools.ietf.org/html/rfc7540#section-8.1.2
     */
    default void writeThrowable(Throwable obj) throws IOException {
```

### JavadocLinkAsPlainText
Link specified as plain text
in `dubbo-serialization/dubbo-serialization-api/src/main/java/org/apache/dubbo/common/serialize/ObjectInput.java`
#### Snippet
```java
     * should be highly independent and portable, easy to cross platforms and languages, for example, like the http headers,
     * restricting the content of headers / attachments to Ascii strings and uses ISO_8859_1 to encode them.
     * https://tools.ietf.org/html/rfc7540#section-8.1.2
     */
    default Throwable readThrowable() throws IOException, ClassNotFoundException {
```

### JavadocLinkAsPlainText
Link specified as plain text
in `dubbo-registry/dubbo-registry-nacos/src/main/java/org/apache/dubbo/registry/nacos/NacosRegistry.java`
#### Snippet
```java
    /**
     * Since 2.7.6 the legacy service name will be added to serviceNames
     * to fix bug with https://github.com/apache/dubbo/issues/5442
     *
     * @param url
```

### JavadocLinkAsPlainText
Link specified as plain text
in `dubbo-registry/dubbo-registry-nacos/src/main/java/org/apache/dubbo/registry/nacos/NacosNamingServiceWrapper.java`
#### Snippet
```java

    /**
     * see https://github.com/apache/dubbo/issues/7129
     * nacos service name just support `0-9a-zA-Z-._:`, grpc interface is inner interface, need compatible.
     */
```

### JavadocLinkAsPlainText
Link specified as plain text
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/TriRpcStatus.java`
#### Snippet
```java

/**
 * See https://github.com/grpc/grpc/blob/master/doc/statuscodes.md
 */

```

### JavadocLinkAsPlainText
Link specified as plain text
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/service/ReflectionV1AlphaService.java`
#### Snippet
```java
 * Provides a reflection service for Protobuf service for service test and dynamic gateway.
 *
 * @link https://github.com/grpc/grpc/blob/master/doc/server-reflection.md
 */
public class ReflectionV1AlphaService extends DubboServerReflectionTriple.ServerReflectionImplBase {
```

### JavadocLinkAsPlainText
Link specified as plain text
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/compressor/Snappy.java`
#### Snippet
```java
 * snappy compressor, Provide high-speed compression speed and reasonable compression ratio
 *
 * @link https://github.com/google/snappy
 */
public class Snappy implements Compressor, DeCompressor {
```

### JavadocLinkAsPlainText
Link specified as plain text
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/compressor/Bzip2.java`
#### Snippet
```java
 * bzip2 compressor, faster compression efficiency
 *
 * @link https://commons.apache.org/proper/commons-compress/
 */
public class Bzip2 implements Compressor, DeCompressor {
```

## RuleId[id=FieldCanBeLocal]
### FieldCanBeLocal
Field can be converted to a local variable
in `dubbo-metadata/dubbo-metadata-report-zookeeper/src/main/java/org/apache/dubbo/metadata/store/zookeeper/ZookeeperMetadataReportFactory.java`
#### Snippet
```java
    private ZookeeperTransporter zookeeperTransporter;

    private ApplicationModel applicationModel;

    public ZookeeperMetadataReportFactory(ApplicationModel applicationModel) {
```

### FieldCanBeLocal
Field can be converted to a local variable
in `dubbo-registry/dubbo-registry-multiple/src/main/java/org/apache/dubbo/registry/multiple/MultipleServiceDiscovery.java`
#### Snippet
```java
    private final Map<String, ServiceDiscovery> serviceDiscoveries = new ConcurrentHashMap<>();
    private URL registryURL;
    private String applicationName;
    private volatile boolean isDestroy;

```

### FieldCanBeLocal
Field can be converted to a local variable
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/bootstrap/DubboBootstrap.java`
#### Snippet
```java
    private final ExecutorRepository executorRepository;

    private final Environment environment;

    private final ApplicationModel applicationModel;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/bootstrap/DubboBootstrap.java`
#### Snippet
```java
    private final Condition condition = lock.newCondition();

    private final ExecutorRepository executorRepository;

    private final Environment environment;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `dubbo-monitor/dubbo-monitor-default/src/main/java/org/apache/dubbo/monitor/dubbo/DubboMonitor.java`
#### Snippet
```java
     * The timer for sending statistics
     */
    private final ScheduledExecutorService scheduledExecutorService;

    /**
```

### FieldCanBeLocal
Field can be converted to a local variable
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/DubboTestChecker.java`
#### Snippet
```java
     * Check report file
     */
    private File reportFile;

    /**
```

### FieldCanBeLocal
Field can be converted to a local variable
in `dubbo-xds/src/main/java/org/apache/dubbo/registry/xds/util/protocol/AbstractProtocol.java`
#### Snippet
```java
    protected final Node node;

    private final int checkInterval;

    protected final ReentrantReadWriteLock lock = new ReentrantReadWriteLock();
```

### FieldCanBeLocal
Field can be converted to a local variable
in `dubbo-registry/dubbo-registry-zookeeper/src/main/java/org/apache/dubbo/registry/zookeeper/ZookeeperServiceDiscovery.java`
#### Snippet
```java
    private final CuratorFramework curatorFramework;

    private final String rootPath;

    private final org.apache.curator.x.discovery.ServiceDiscovery<ZookeeperInstance> serviceDiscovery;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `dubbo-remoting/dubbo-remoting-zookeeper-curator5/src/main/java/org/apache/dubbo/remoting/zookeeper/curator5/Curator5ZookeeperClient.java`
#### Snippet
```java

    private class CuratorConnectionStateListener implements ConnectionStateListener {
        private final long UNKNOWN_SESSION_ID = -1L;

        private long lastSessionId;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `dubbo-remoting/dubbo-remoting-zookeeper-curator5/src/main/java/org/apache/dubbo/remoting/zookeeper/curator5/Curator5ZookeeperClient.java`
#### Snippet
```java
    static class NodeCacheListenerImpl implements NodeCacheListener {

        private CuratorFramework client;

        private volatile DataListener dataListener;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/report/support/AbstractMetadataReport.java`
#### Snippet
```java
    // Local disk cache file
    File file;
    private AtomicBoolean initialized = new AtomicBoolean(false);
    public MetadataReportRetry metadataReportRetry;
    private ScheduledExecutorService reportTimerScheduler;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/netty/NettyServer.java`
#### Snippet
```java

    private EventLoopGroup eventLoopGroup;
    private EventLoopGroup workerLoopGroup;
    private int ioWorkerCount = Runtime.getRuntime().availableProcessors() * 2;

```

### FieldCanBeLocal
Field can be converted to a local variable
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/StubMethodDescriptor.java`
#### Snippet
```java
public class StubMethodDescriptor implements MethodDescriptor, PackableMethod {
    private static final Logger logger = LoggerFactory.getLogger(StubMethodDescriptor.class);
    private final ServiceDescriptor serviceDescriptor;
    private final ConcurrentMap<String, Object> attributeMap = new ConcurrentHashMap<>();
    private final String methodName;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/CIDRUtils.java`
#### Snippet
```java
 */
public class CIDRUtils {
    private final String cidr;

    private InetAddress inetAddress;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/InmemoryConfiguration.java`
#### Snippet
```java
public class InmemoryConfiguration implements Configuration {

    private String name;

    // stores the configuration key-value pairs
```

### FieldCanBeLocal
Field can be converted to a local variable
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/support/RegistryManager.java`
#### Snippet
```java
    private static final ErrorTypeAwareLogger LOGGER = LoggerFactory.getErrorTypeAwareLogger(RegistryManager.class);

    private ApplicationModel applicationModel;

    /**
```

### FieldCanBeLocal
Field can be converted to a local variable
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/context/DubboConfigBeanInitializer.java`
#### Snippet
```java
    private final AtomicBoolean initialized = new AtomicBoolean(false);
    private ConfigurableListableBeanFactory beanFactory;
    private ReferenceBeanManager referenceBeanManager;

    @Autowired
```

### FieldCanBeLocal
Field can be converted to a local variable
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/integration/RegistryProtocol.java`
#### Snippet
```java
    private class ExporterChangeableWrapper<T> implements Exporter<T> {

        private final ScheduledExecutorService executor;

        private final Invoker<T> originInvoker;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/integration/RegistryProtocol.java`
#### Snippet
```java

    private class ServiceConfigurationListener extends AbstractConfiguratorListener {
        private URL providerUrl;
        private OverrideListener notifyListener;

```

### FieldCanBeLocal
Field can be converted to a local variable
in `dubbo-plugin/dubbo-qos/src/main/java/org/apache/dubbo/qos/pu/TelnetDetector.java`
#### Snippet
```java

    private final FrameworkModel frameworkModel;
    private final int MaxSize = 2048;
    private final ChannelBuffer AytPreface = new HeapChannelBuffer(new byte[]{(byte) 0xff, (byte) 0xf6});

```

### FieldCanBeLocal
Field can be converted to a local variable
in `dubbo-plugin/dubbo-qos/src/main/java/org/apache/dubbo/qos/probe/impl/ProviderReadinessProbe.java`
#### Snippet
```java
@Activate
public class ProviderReadinessProbe implements ReadinessProbe {
    private final FrameworkModel frameworkModel;
    private final FrameworkServiceRepository serviceRepository;

```

### FieldCanBeLocal
Field can be converted to a local variable
in `dubbo-remoting/dubbo-remoting-zookeeper/src/main/java/org/apache/dubbo/remoting/zookeeper/curator/CuratorZookeeperClient.java`
#### Snippet
```java

    private class CuratorConnectionStateListener implements ConnectionStateListener {
        private final long UNKNOWN_SESSION_ID = -1L;
        private long lastSessionId;
        private int timeout;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `dubbo-rpc/dubbo-rpc-dubbo/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/ChannelWrappedInvoker.java`
#### Snippet
```java
class ChannelWrappedInvoker<T> extends AbstractInvoker<T> {

    private final Channel channel;
    private final String serviceKey;
    private final ExchangeClient currentClient;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `dubbo-rpc/dubbo-rpc-dubbo/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/CallbackServiceCodec.java`
#### Snippet
```java
    private final ProxyFactory proxyFactory;
    private final Protocol protocolSPI;
    private final FrameworkModel frameworkModel;
    private final DubboProtocol dubboProtocol;

```

### FieldCanBeLocal
Field can be converted to a local variable
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/DeadlineFuture.java`
#### Snippet
```java
    private final String serviceName;
    private final String methodName;
    private final String address;
    private final int timeout;
    private final long start = System.currentTimeMillis();
```

### FieldCanBeLocal
Field can be converted to a local variable
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/ReflectionPackableMethod.java`
#### Snippet
```java
    private final boolean needWrapper;

    private final Collection<String> allSerialize;

    @Override
```

### FieldCanBeLocal
Field can be converted to a local variable
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/TriHttp2RemoteFlowController.java`
#### Snippet
```java
    private final StreamByteDistributor streamByteDistributor;
    private final FlowState connectionState;
    private final Configuration config;
    private int initialWindowSize;
    private WritabilityMonitor monitor;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/service/TriBuiltinService.java`
#### Snippet
```java
    private PathResolver pathResolver;

    private Health healthService;

    private FrameworkModel frameworkModel;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/service/TriBuiltinService.java`
#### Snippet
```java
    private FrameworkModel frameworkModel;

    private ReflectionV1AlphaService reflectionServiceV1Alpha;
    private HealthStatusManager healthStatusManager;
    private Configuration config = ConfigurationUtils.getGlobalConfiguration(
```

### FieldCanBeLocal
Field can be converted to a local variable
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/transport/TripleClientHandler.java`
#### Snippet
```java
public class TripleClientHandler extends ChannelDuplexHandler {

    private final FrameworkModel frameworkModel;

    private static final String CONNECTION_HANDLER_NAME = "connectionHandler";
```

### FieldCanBeLocal
Field can be converted to a local variable
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/transport/TripleHttp2FrameServerHandler.java`
#### Snippet
```java
    private final PathResolver pathResolver;
    private final ExecutorSupport executorSupport;
    private final String acceptEncoding;
    private final TripleServerStream tripleServerStream;

```

### FieldCanBeLocal
Field can be converted to a local variable
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/transport/TripleHttp2FrameServerHandler.java`
#### Snippet
```java
    private static final ErrorTypeAwareLogger LOGGER = LoggerFactory.getErrorTypeAwareLogger(
        TripleHttp2FrameServerHandler.class);
    private final PathResolver pathResolver;
    private final ExecutorSupport executorSupport;
    private final String acceptEncoding;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/loadbalance/AdaptiveLoadBalance.java`
#### Snippet
```java

    //default key
    private String attachmentKey = "mem,load";

    private final AdaptiveMetrics adaptiveMetrics;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/loadbalance/ConsistentHashLoadBalance.java`
#### Snippet
```java
        private final TreeMap<Long, Invoker<T>> virtualInvokers;

        private final int replicaNumber;

        private final int identityHashCode;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `dubbo-remoting/dubbo-remoting-http/src/main/java/org/apache/dubbo/remoting/http/config/HttpClientConfig.java`
#### Snippet
```java
    private int writeTimeout = 6 * 1000;
    private int connectTimeout = 6 * 1000;
    private int chunkLength = 8196;

    private int HTTP_CLIENT_CONNECTION_MANAGER_MAX_PER_ROUTE = 20;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `dubbo-remoting/dubbo-remoting-http/src/main/java/org/apache/dubbo/remoting/http/restclient/OKHttpRestClient.java`
#### Snippet
```java
public class OKHttpRestClient implements RestClient {
    private final OkHttpClient okHttpClient;
    private final HttpClientConfig httpClientConfig;

    public OKHttpRestClient(HttpClientConfig clientConfig) {
```

## RuleId[id=CollectionAddedToSelf]
### CollectionAddedToSelf
'putAll()' called on collection `attributes` with itself as argument
in `dubbo-common/src/main/java/org/apache/dubbo/common/URL.java`
#### Snippet
```java
    public URL addAttributes(Map<String, Object> attributes) {
        if (attributes != null) {
            attributes.putAll(attributes);
        }
        return this;
```

## RuleId[id=DefaultAnnotationParam]
### DefaultAnnotationParam
Redundant default parameter value assignment
in `dubbo-common/src/main/java/org/apache/dubbo/config/MethodConfig.java`
#### Snippet
```java
    }

    @Parameter(key = ON_INVOKE_METHOD_PARAMETER_KEY, excluded = true, attribute = true)
    public String getOninvokeMethod() {
        return oninvokeMethod;
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `dubbo-common/src/main/java/org/apache/dubbo/config/MethodConfig.java`
#### Snippet
```java
    }

    @Parameter(key = ON_THROW_INSTANCE_PARAMETER_KEY, excluded = true, attribute = true)
    public Object getOnthrow() {
        return onthrow;
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `dubbo-common/src/main/java/org/apache/dubbo/config/MethodConfig.java`
#### Snippet
```java
    }

    @Parameter(key = ON_INVOKE_INSTANCE_PARAMETER_KEY, excluded = true, attribute = true)
    public Object getOninvoke() {
        return oninvoke;
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `dubbo-common/src/main/java/org/apache/dubbo/config/MethodConfig.java`
#### Snippet
```java
    }

    @Parameter(key = ON_RETURN_METHOD_PARAMETER_KEY, excluded = true, attribute = true)
    public String getOnreturnMethod() {
        return onreturnMethod;
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `dubbo-common/src/main/java/org/apache/dubbo/config/MethodConfig.java`
#### Snippet
```java
    }

    @Parameter(key = ON_THROW_METHOD_PARAMETER_KEY, excluded = true, attribute = true)
    public String getOnthrowMethod() {
        return onthrowMethod;
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `dubbo-common/src/main/java/org/apache/dubbo/config/MethodConfig.java`
#### Snippet
```java
    }

    @Parameter(key = ON_RETURN_INSTANCE_PARAMETER_KEY, excluded = true, attribute = true)
    public Object getOnreturn() {
        return onreturn;
```

## RuleId[id=ThrowFromFinallyBlock]
### ThrowFromFinallyBlock
`throw` inside 'finally' block
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/registrycenter/initializer/ConfigZookeeperInitializer.java`
#### Snippet
```java
                    oFile.close();
                } catch (IOException e) {
                    throw new DubboTestException("Failed to close file", e);
                }
            }
```

### ThrowFromFinallyBlock
`throw` inside 'finally' block
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/registrycenter/initializer/ConfigZookeeperInitializer.java`
#### Snippet
```java
                    oFile.close();
                } catch (IOException e) {
                    throw new DubboTestException("Failed to close file", e);
                }
            }
```

## RuleId[id=IfStatementWithIdenticalBranches]
### IfStatementWithIdenticalBranches
Common part can be extracted from 'if'
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/deploy/DefaultApplicationDeployer.java`
#### Snippet
```java

    private void overrideMetadataReportConfig(MetadataReportConfig metadataConfigToOverride, MetadataReportConfig metadataReportConfig) {
        if (metadataReportConfig.getId() == null) {
            Collection<MetadataReportConfig> metadataReportConfigs = configManager.getMetadataConfigs();
            if (CollectionUtils.isNotEmpty(metadataReportConfigs)) {
```

### IfStatementWithIdenticalBranches
Common part can be extracted removing branch
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ArrayUtils.java`
#### Snippet
```java

    public static int indexOf(String[] array, String valueToFind, int startIndex) {
        if (isEmpty(array) || valueToFind == null) {
            return -1;
        } else {
```

## RuleId[id=InfiniteLoopStatement]
### InfiniteLoopStatement
`while` statement cannot complete without throwing an exception
in `dubbo-demo/dubbo-demo-generic-call/src/main/java/org/apache/dubbo/demo/consumer/GenericApplication.java`
#### Snippet
```java
        // generic invoke
        GenericService genericService = bootstrap.getCache().get(reference);
        while (true) {
            try {
                Object genericInvokeResult = genericService.$invoke("sayHello", new String[]{String.class.getName()},
```

### InfiniteLoopStatement
`while` statement cannot complete without throwing an exception
in `dubbo-demo/dubbo-demo-xml/dubbo-demo-xml-consumer/src/main/java/org/apache/dubbo/demo/consumer/Application.java`
#### Snippet
```java
        }).start();

        while (true) {
            try {
                CompletableFuture<String> hello = demoService.sayHelloAsync("world");
```

### InfiniteLoopStatement
`while` statement cannot complete without throwing an exception
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/migration/MigrationRuleListener.java`
#### Snippet
```java
        if (executorSubmit.compareAndSet(false, true)) {
            ruleMigrationFuture = ruleManageExecutor.submit(() -> {
                while (true) {
                    String rule = "";
                    try {
```

## RuleId[id=RedundantStringFormatCall]
### RedundantStringFormatCall
Redundant call to `format()`
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/registrycenter/initializer/UnpackZookeeperInitializer.java`
#### Snippet
```java
            }
        } catch (IOException e) {
            throw new DubboTestException(String.format("Failed to unpack the zookeeper binary file"), e);
        }
    }
```

## RuleId[id=FuseStreamOperations]
### FuseStreamOperations
Stream may be extended replacing 'sort'
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/DubboTestChecker.java`
#### Snippet
```java
            ))
            .filter(thread -> !unclosedThreadMap.containsKey(thread))
            .collect(Collectors.toList());
        unclosedThreads.sort(Comparator.comparing(Thread::getName));
        if (unclosedThreads.size() > 0) {
```

## RuleId[id=PointlessNullCheck]
### PointlessNullCheck
Unnecessary 'null' check before 'equals()' call
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/ReferenceAnnotationBeanPostProcessor.java`
#### Snippet
```java
            AnnotatedBeanDefinition annotatedBeanDefinition = (AnnotatedBeanDefinition) beanDefinition;
            String beanClassName = SpringCompatUtils.getFactoryMethodReturnType(annotatedBeanDefinition);
            if (beanClassName != null && ReferenceBean.class.getName().equals(beanClassName)) {
                return true;
            }
```

## RuleId[id=NullArgumentToVariableArgMethod]
### NullArgumentToVariableArgMethod
Confusing argument `null`, unclear if a varargs or non-varargs call is desired
in `dubbo-metadata/dubbo-metadata-definition-protobuf/src/main/java/org/apache/dubbo/metadata/definition/protobuf/ProtobufTypeBuilder.java`
#### Snippet
```java
    private GeneratedMessageV3.Builder getMessageBuilder(Class<?> requestType) throws Exception {
        Method method = requestType.getMethod("newBuilder");
        return (GeneratedMessageV3.Builder) method.invoke(null, null);
    }

```

## RuleId[id=TypeParameterExtendsObject]
### TypeParameterExtendsObject
Type parameter `V` explicitly extends 'java.lang.Object'
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/ConfigurationUtils.java`
#### Snippet
```java
     * @return
     */
    public static <V extends Object> Set<String> getSubIds(Collection<Map<String, V>> configMaps, String prefix) {
        if (!prefix.endsWith(".")) {
            prefix += ".";
```

### TypeParameterExtendsObject
Type parameter `V` explicitly extends 'java.lang.Object'
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/ConfigurationUtils.java`
#### Snippet
```java
    }

    public static <V extends Object> boolean hasSubProperties(Map<String, V> configMap, String prefix) {
        if (!prefix.endsWith(".")) {
            prefix += ".";
```

### TypeParameterExtendsObject
Type parameter `V` explicitly extends 'java.lang.Object'
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/ConfigurationUtils.java`
#### Snippet
```java
    }

    public static <V extends Object> boolean hasSubProperties(Collection<Map<String, V>> configMaps, String prefix) {
        if (!prefix.endsWith(".")) {
            prefix += ".";
```

### TypeParameterExtendsObject
Type parameter `V` explicitly extends 'java.lang.Object'
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/ConfigurationUtils.java`
#### Snippet
```java
     * @return
     */
    public static <V extends Object> Map<String, V> getSubProperties(Collection<Map<String, V>> configMaps, String prefix) {
        Map<String, V> map = new LinkedHashMap<>();
        for (Map<String, V> configMap : configMaps) {
```

### TypeParameterExtendsObject
Type parameter `V` explicitly extends 'java.lang.Object'
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/ConfigurationUtils.java`
#### Snippet
```java
    }

    public static <V extends Object> Map<String, V> getSubProperties(Map<String, V> configMap, String prefix) {
        return getSubProperties(configMap, prefix, null);
    }
```

### TypeParameterExtendsObject
Type parameter `V` explicitly extends 'java.lang.Object'
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/ConfigurationUtils.java`
#### Snippet
```java
    }

    private static <V extends Object> Map<String, V> getSubProperties(Map<String, V> configMap, String prefix, Map<String, V> resultMap) {
        if (!prefix.endsWith(".")) {
            prefix += ".";
```

## RuleId[id=OptionalGetWithoutIsPresent]
### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/metadata/ConfigurableMetadataServiceExporter.java`
#### Snippet
```java

    private ApplicationConfig getApplicationConfig() {
        return applicationModel.getApplicationConfigManager().getApplication().get();
    }

```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `dubbo-test/dubbo-test-spring/src/main/java/org/apache/dubbo/test/spring/SpringJavaConfigBeanTest.java`
#### Snippet
```java
            ApplicationModel applicationModel = consumerContext.getBean(ApplicationModel.class);
            ConfigManager configManager = consumerContext.getBean(ConfigManager.class);
            ApplicationConfig application = configManager.getApplication().get();
            Assertions.assertEquals(false, application.getQosEnable());
            Assertions.assertEquals("Tom", application.getOwner());
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `dubbo-test/dubbo-test-spring/src/main/java/org/apache/dubbo/test/spring/SpringJavaConfigBeanTest.java`
#### Snippet
```java
            Assertions.assertEquals("Tom", application.getOwner());

            RegistryConfig registry = configManager.getRegistry(MY_REGISTRY_ID).get();
            Assertions.assertEquals(registryAddress, registry.getAddress());

```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `dubbo-test/dubbo-test-spring/src/main/java/org/apache/dubbo/test/spring/SpringJavaConfigBeanTest.java`
#### Snippet
```java

            ModuleConfigManager moduleConfigManager = applicationModel.getDefaultModule().getConfigManager();
            ConsumerConfig consumerConfig = moduleConfigManager.getDefaultConsumer().get();
            Assertions.assertEquals(1000, consumerConfig.getTimeout());
            Assertions.assertEquals("demo", consumerConfig.getGroup());
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/configcenter/file/FileSystemDynamicConfiguration.java`
#### Snippet
```java
                    if (configDirectory != null) {
                        // Register the configDirectory
                        configDirectory.toPath().register(watchService.get(), INTEREST_PATH_KINDS, MODIFIERS);
                    }
                });
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
                        }
                        return null;
                    }).get();
        } catch (Exception e) {

```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `dubbo-metrics/dubbo-metrics-api/src/main/java/org/apache/dubbo/metrics/data/RtStatComposite.java`
#### Snippet
```java
        AtomicLongContainer avgContainer = new AtomicLongContainer(new MetricsKeyWrapper(MetricsKey.METRIC_RT_AVG, placeValue), (k, v) -> v.incrementAndGet());
        avgContainer.setValueSupplier(applicationName -> {
            LongContainer<? extends Number> totalContainer = rtStats.stream().filter(longContainer -> longContainer.isKeyWrapper(MetricsKey.METRIC_RT_SUM, placeValue.getType())).findFirst().get();
            AtomicLong totalRtTimes = avgContainer.get(applicationName);
            AtomicLong totalRtSum = (AtomicLong) totalContainer.get(applicationName);
```

## RuleId[id=ConstantValue]
### ConstantValue
Condition `values != null` is always `true`
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/registrycenter/processor/FindPidWindowsProcessor.java`
#### Snippet
```java
                //   TCP    127.0.0.1:56723        127.0.0.1:2182         ESTABLISHED     1980
                //   TCP    [::]:2182              [::]:0                 LISTENING       4020
                if (values != null && values.length > 0) {
                    for (int i = 0; i < values.length; i++) {
                        List<String> segments = Arrays.stream(values[i].trim().split(" "))
```

### ConstantValue
Condition `segments != null` is always `true`
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/registrycenter/processor/FindPidWindowsProcessor.java`
#### Snippet
```java
                        // ESTABLISHED
                        // 4020
                        if (segments != null && segments.size() == 5) {
                            if (this.check(segments.get(1), clientPort)) {
                                int pid = Integer.valueOf(segments.get(segments.size() - 1).trim());
```

### ConstantValue
Condition `l < argArray.length - 1` is always `false`
in `dubbo-remoting/dubbo-remoting-netty4/src/main/java/org/apache/dubbo/remoting/transport/netty4/logging/MessageFormatter.java`
#### Snippet
```java
        // append the characters following the last {} pair.
        sbuf.append(messagePattern.substring(i));
        if (l < argArray.length - 1) {
            return new FormattingTuple(sbuf.toString(), argArray, throwableCandidate);
        } else {
```

### ConstantValue
Value `testSource` is always 'null'
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/DubboTestChecker.java`
#### Snippet
```java
            testClassName = methodSource.getClassName();
            //log("Finish test method: " + methodSource.getClassName() + "#" + methodSource.getMethodName());
        } else if (testSource instanceof ClassSource) {
            if (forceDestroyDubboAfterClass) {
                // make sure destroy dubbo engine
```

### ConstantValue
Value `testSource` is always 'null'
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/DubboTestChecker.java`
#### Snippet
```java
//            ClassSource source = (ClassSource) testSource;
//            log("Run test class: " + source.getClassName());
        } else if (testSource instanceof MethodSource) {
            MethodSource source = (MethodSource) testSource;
            log("Run test method: " + source.getClassName() + "#" + source.getMethodName());
```

### ConstantValue
Condition `targetPath.toFile() != null` is always `true`
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/registrycenter/initializer/UnpackZookeeperInitializer.java`
#### Snippet
```java
            String.valueOf(clientPort));
        // check if it's unpacked.
        if (targetPath.toFile() != null && targetPath.toFile().isDirectory()) {
            logger.info(String.format("The file has been unpacked, target path:%s", targetPath.toString()));
            return;
```

### ConstantValue
Value `value` is always 'null'
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/MetadataInfo.java`
#### Snippet
```java
            String value = null;
            if (map == null) {
                return value;
            }

```

### ConstantValue
Condition `responseCode < 400` is always `true`
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/RestInvoker.java`
#### Snippet
```java
                        } else if (responseCode >= 500) {
                            responseFuture.completeExceptionally(new RemoteServerInternalException(r.getMessage()));
                        } else if (responseCode < 400) {
                            mediaType = MediaTypeUtil.convertMediaType(restMethodMetadata.getReflectMethod().getReturnType(), r.getContentType());
                            Object value = HttpMessageCodecManager.httpMessageDecode(r.getBody(),
```

### ConstantValue
Condition `objectClass == boolean.class` is always `false` when reached
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/DataParseUtils.java`
#### Snippet
```java
        Class<?> objectClass = object.getClass();

        if (objectClass == Boolean.class || objectClass == boolean.class) {
            return object.toString().getBytes();
        }
```

### ConstantValue
Condition `targetType != null` is always `true`
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/DataParseUtils.java`
#### Snippet
```java
        }

        if (targetType != null && targetType.isPrimitive()) {
            return NumberUtils.parseNumber(value, targetType);
        }
```

### ConstantValue
Condition `reportCacheExecutor != null` is always `true`
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/report/support/AbstractMetadataReport.java`
#### Snippet
```java
    @Override
    public void destroy() {
        if (reportCacheExecutor != null) {
            reportCacheExecutor.shutdown();
        }
```

### ConstantValue
Condition `parameterTypes != null` is always `true`
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ConsumerMethodModel.java`
#### Snippet
```java
        this.parameterTypes = this.createParamSignature(parameterClasses);
        this.methodName = method.getName();
        this.generic = methodName.equals($INVOKE) && parameterTypes != null && parameterTypes.length == 3;
    }

```

### ConstantValue
Condition `b2` is always `true` when reached
in `dubbo-common/src/main/java/org/apache/dubbo/common/lang/Prioritized.java`
#### Snippet
```java
        } else if (b2 && !b1) { // two is Prioritized, one is not
            return 1;
        } else if (b1 && b2) {  //  one and two both are Prioritized
            return ((Prioritized) one).compareTo((Prioritized) two);
        } else {                // no different
```

### ConstantValue
Condition `Float.isNaN(evictionFactor)` is always `false` when reached
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/LFUCache.java`
#### Snippet
```java
        }
        boolean factorInRange = evictionFactor <= 1 && evictionFactor > 0;
        if (!factorInRange || Float.isNaN(evictionFactor)) {
            throw new IllegalArgumentException("Illegal eviction factor value:"
                    + evictionFactor);
```

### ConstantValue
Condition `type != null` is always `true`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/PojoUtils.java`
#### Snippet
```java
                return dest;
            } else {
                Class<?> ctype = (type != null && type.isArray() ? type.getComponentType() : pojo.getClass().getComponentType());
                int len = Array.getLength(pojo);
                Object dest = Array.newInstance(ctype, len);
```

### ConstantValue
Condition `type != null` is always `true` when reached
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/PojoUtils.java`
#### Snippet
```java
        }

        if (pojo instanceof Map<?, ?> && type != null) {
            Object className = ((Map<Object, Object>) pojo).get("class");
            if (className instanceof String) {
```

### ConstantValue
Condition `dynamicConfiguration == null` is always `false`
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/ModuleEnvironment.java`
#### Snippet
```java
        }
        if (dynamicGlobalConfiguration == null) {
            if (dynamicConfiguration == null) {
                if (logger.isWarnEnabled()) {
                    logger.warn(COMMON_UNEXPECTED_EXCEPTION, "", "", "dynamicConfiguration is null , return globalConfiguration.");
```

### ConstantValue
Condition `!(t instanceof CannotCompileException)` is always `false`
in `dubbo-common/src/main/java/org/apache/dubbo/common/compiler/support/JavassistCompiler.java`
#### Snippet
```java
            return cp.toClass(cls, neighbor, classLoader, JavassistCompiler.class.getProtectionDomain());
        } catch (Throwable t) {
            if (!(t instanceof CannotCompileException)) {
                return cp.toClass(cls, classLoader, JavassistCompiler.class.getProtectionDomain());
            }
```

### ConstantValue
Condition `!(t instanceof CannotCompileException)` is always `false`
in `dubbo-common/src/main/java/org/apache/dubbo/common/bytecode/ClassGenerator.java`
#### Snippet
```java
                return mPool.toClass(mCtc, neighborClass, loader, pd);
            } catch (Throwable t) {
                if (!(t instanceof CannotCompileException)) {
                    return mPool.toClass(mCtc, loader, pd);
                }
```

### ConstantValue
Condition `field != null` is always `true`
in `dubbo-common/src/main/java/org/apache/dubbo/common/beanutil/JavaBeanSerializeUtil.java`
#### Snippet
```java
                    try {
                        Field field = result.getClass().getField(property);
                        if (field != null) {
                            field.set(result, value);
                        }
```

### ConstantValue
Value `defaultExtName` is always 'null'
in `dubbo-common/src/main/java/org/apache/dubbo/common/extension/AdaptiveClassCodeGenerator.java`
#### Snippet
```java
                    if (!CommonConstants.PROTOCOL_KEY.equals(value[i])) {
                        if (hasInvocation) {
                            getNameCode = String.format("url.getMethodParameter(methodName, \"%s\", \"%s\")", value[i], defaultExtName);
                        } else {
                            getNameCode = String.format("url.getParameter(\"%s\")", value[i]);
```

### ConstantValue
Condition `activate instanceof Activate` is always `true`
in `dubbo-common/src/main/java/org/apache/dubbo/common/extension/ExtensionLoader.java`
#### Snippet
```java
        String[] onClass = null;

        if (activate instanceof Activate) {
            onClass = ((Activate) activate).onClass();
        } else if (activate instanceof com.alibaba.dubbo.common.extension.Activate) {
```

### ConstantValue
Condition `key != null` is always `true` when reached
in `dubbo-common/src/main/java/org/apache/dubbo/config/context/AbstractConfigManager.java`
#### Snippet
```java
                    throw new IllegalStateException("load config failed, id: " + id + ", type:" + cls.getSimpleName());
                } finally {
                    if (addDefaultNameConfig && key != null) {
                        properties.remove(key);
                    }
```

### ConstantValue
Value `other.extendParams` is always 'null'
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/DefaultServiceInstance.java`
#### Snippet
```java
        this.metadata = new HashMap<>(other.metadata);
        this.applicationModel = other.applicationModel;
        this.extendParams = other.extendParams != null ? new HashMap<>(other.extendParams) : other.extendParams;
        this.endpoints = other.endpoints != null ? new ArrayList<>(other.endpoints) : other.endpoints;
    }
```

### ConstantValue
Value `other.endpoints` is always 'null'
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/DefaultServiceInstance.java`
#### Snippet
```java
        this.applicationModel = other.applicationModel;
        this.extendParams = other.extendParams != null ? new HashMap<>(other.extendParams) : other.extendParams;
        this.endpoints = other.endpoints != null ? new ArrayList<>(other.endpoints) : other.endpoints;
    }

```

### ConstantValue
Result of `entry.getValue()` is always 'null'
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/DefaultServiceInstance.java`
#### Snippet
```java
            }
            if (entry.getValue() == null) {
                equals = equals && (entry.getValue() == that.getMetadata().get(entry.getKey()));
            } else {
                equals = equals && entry.getValue().equals(that.getMetadata().get(entry.getKey()));
```

### ConstantValue
Value `event` is always 'null'
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/context/DubboBootstrapApplicationListener.java`
#### Snippet
```java
        if (event instanceof ContextRefreshedEvent) {
            onContextRefreshedEvent((ContextRefreshedEvent) event);
        } else if (event instanceof ContextClosedEvent) {
            onContextClosedEvent((ContextClosedEvent) event);
        }
```

### ConstantValue
Value `registry` is always 'null'
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/context/DubboSpringInitializer.java`
#### Snippet
```java
        if (registry instanceof ConfigurableListableBeanFactory) {
            beanFactory = (ConfigurableListableBeanFactory) registry;
        } else if (registry instanceof GenericApplicationContext) {
            GenericApplicationContext genericApplicationContext = (GenericApplicationContext) registry;
            beanFactory = genericApplicationContext.getBeanFactory();
```

### ConstantValue
Condition `nodeList == null` is always `false`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/schema/DubboBeanDefinitionParser.java`
#### Snippet
```java
    private static void parseNested(Element element, ParserContext parserContext, Class<?> beanClass, boolean registered, String tag, String property, String ref, BeanDefinition beanDefinition) {
        NodeList nodeList = element.getChildNodes();
        if (nodeList == null) {
            return;
        }
```

### ConstantValue
Condition `subDefinition != null` is always `true`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/schema/DubboBeanDefinitionParser.java`
#### Snippet
```java
                }
                RootBeanDefinition subDefinition = parse((Element) node, parserContext, beanClass, registered);
                if (subDefinition != null) {
                    if (StringUtils.isNotEmpty(ref)) {
                        subDefinition.getPropertyValues().addPropertyValue(property, new RuntimeBeanReference(ref));
```

### ConstantValue
Value `memberValue` is always 'null'
in `dubbo-filter/dubbo-filter-validation/src/main/java/org/apache/dubbo/validation/support/jvalidation/JValidator.java`
#### Snippet
```java
        if (memberValue instanceof BooleanMemberValue) {
            ((BooleanMemberValue) memberValue).setValue((Boolean) value);
        } else if (memberValue instanceof ByteMemberValue) {
            ((ByteMemberValue) memberValue).setValue((Byte) value);
        } else if (memberValue instanceof CharMemberValue) {
```

### ConstantValue
Value `memberValue` is always 'null'
in `dubbo-filter/dubbo-filter-validation/src/main/java/org/apache/dubbo/validation/support/jvalidation/JValidator.java`
#### Snippet
```java
        } else if (memberValue instanceof ByteMemberValue) {
            ((ByteMemberValue) memberValue).setValue((Byte) value);
        } else if (memberValue instanceof CharMemberValue) {
            ((CharMemberValue) memberValue).setValue((Character) value);
        } else if (memberValue instanceof ShortMemberValue) {
```

### ConstantValue
Value `memberValue` is always 'null'
in `dubbo-filter/dubbo-filter-validation/src/main/java/org/apache/dubbo/validation/support/jvalidation/JValidator.java`
#### Snippet
```java
        } else if (memberValue instanceof CharMemberValue) {
            ((CharMemberValue) memberValue).setValue((Character) value);
        } else if (memberValue instanceof ShortMemberValue) {
            ((ShortMemberValue) memberValue).setValue((Short) value);
        } else if (memberValue instanceof IntegerMemberValue) {
```

### ConstantValue
Value `memberValue` is always 'null'
in `dubbo-filter/dubbo-filter-validation/src/main/java/org/apache/dubbo/validation/support/jvalidation/JValidator.java`
#### Snippet
```java
        } else if (memberValue instanceof ShortMemberValue) {
            ((ShortMemberValue) memberValue).setValue((Short) value);
        } else if (memberValue instanceof IntegerMemberValue) {
            ((IntegerMemberValue) memberValue).setValue((Integer) value);
        } else if (memberValue instanceof LongMemberValue) {
```

### ConstantValue
Value `memberValue` is always 'null'
in `dubbo-filter/dubbo-filter-validation/src/main/java/org/apache/dubbo/validation/support/jvalidation/JValidator.java`
#### Snippet
```java
        } else if (memberValue instanceof IntegerMemberValue) {
            ((IntegerMemberValue) memberValue).setValue((Integer) value);
        } else if (memberValue instanceof LongMemberValue) {
            ((LongMemberValue) memberValue).setValue((Long) value);
        } else if (memberValue instanceof FloatMemberValue) {
```

### ConstantValue
Value `memberValue` is always 'null'
in `dubbo-filter/dubbo-filter-validation/src/main/java/org/apache/dubbo/validation/support/jvalidation/JValidator.java`
#### Snippet
```java
        } else if (memberValue instanceof LongMemberValue) {
            ((LongMemberValue) memberValue).setValue((Long) value);
        } else if (memberValue instanceof FloatMemberValue) {
            ((FloatMemberValue) memberValue).setValue((Float) value);
        } else if (memberValue instanceof DoubleMemberValue) {
```

### ConstantValue
Value `memberValue` is always 'null'
in `dubbo-filter/dubbo-filter-validation/src/main/java/org/apache/dubbo/validation/support/jvalidation/JValidator.java`
#### Snippet
```java
        } else if (memberValue instanceof FloatMemberValue) {
            ((FloatMemberValue) memberValue).setValue((Float) value);
        } else if (memberValue instanceof DoubleMemberValue) {
            ((DoubleMemberValue) memberValue).setValue((Double) value);
        } else if (memberValue instanceof ClassMemberValue) {
```

### ConstantValue
Value `memberValue` is always 'null'
in `dubbo-filter/dubbo-filter-validation/src/main/java/org/apache/dubbo/validation/support/jvalidation/JValidator.java`
#### Snippet
```java
        } else if (memberValue instanceof DoubleMemberValue) {
            ((DoubleMemberValue) memberValue).setValue((Double) value);
        } else if (memberValue instanceof ClassMemberValue) {
            ((ClassMemberValue) memberValue).setValue(((Class<?>) value).getName());
        } else if (memberValue instanceof StringMemberValue) {
```

### ConstantValue
Value `memberValue` is always 'null'
in `dubbo-filter/dubbo-filter-validation/src/main/java/org/apache/dubbo/validation/support/jvalidation/JValidator.java`
#### Snippet
```java
        } else if (memberValue instanceof ClassMemberValue) {
            ((ClassMemberValue) memberValue).setValue(((Class<?>) value).getName());
        } else if (memberValue instanceof StringMemberValue) {
            ((StringMemberValue) memberValue).setValue((String) value);
        } else if (memberValue instanceof EnumMemberValue) {
```

### ConstantValue
Value `memberValue` is always 'null'
in `dubbo-filter/dubbo-filter-validation/src/main/java/org/apache/dubbo/validation/support/jvalidation/JValidator.java`
#### Snippet
```java
        } else if (memberValue instanceof StringMemberValue) {
            ((StringMemberValue) memberValue).setValue((String) value);
        } else if (memberValue instanceof EnumMemberValue) {
            ((EnumMemberValue) memberValue).setValue(((Enum<?>) value).name());
        }
```

### ConstantValue
Value `memberValue` is always 'null'
in `dubbo-filter/dubbo-filter-validation/src/main/java/org/apache/dubbo/validation/support/jvalidation/JValidator.java`
#### Snippet
```java
        }
        /* else if (memberValue instanceof AnnotationMemberValue) */
        else if (memberValue instanceof ArrayMemberValue) {
            CtClass arrayType = type.getComponentType();
            int len = Array.getLength(value);
```

### ConstantValue
Condition `parameterAnnotations != null` is always `true`
in `dubbo-filter/dubbo-filter-validation/src/main/java/org/apache/dubbo/validation/support/jvalidation/JValidator.java`
#### Snippet
```java
    private static boolean hasConstraintParameter(Method method) {
        Annotation[][] parameterAnnotations = method.getParameterAnnotations();
        if (parameterAnnotations != null && parameterAnnotations.length > 0) {
            for (Annotation[] annotations : parameterAnnotations) {
                for (Annotation annotation : annotations) {
```

### ConstantValue
Value `memberValue` is always 'null'
in `dubbo-filter/dubbo-filter-validation/src/main/java/org/apache/dubbo/validation/support/jvalidation/JValidatorNew.java`
#### Snippet
```java
        if (memberValue instanceof BooleanMemberValue) {
            ((BooleanMemberValue) memberValue).setValue((Boolean) value);
        } else if (memberValue instanceof ByteMemberValue) {
            ((ByteMemberValue) memberValue).setValue((Byte) value);
        } else if (memberValue instanceof CharMemberValue) {
```

### ConstantValue
Value `memberValue` is always 'null'
in `dubbo-filter/dubbo-filter-validation/src/main/java/org/apache/dubbo/validation/support/jvalidation/JValidatorNew.java`
#### Snippet
```java
        } else if (memberValue instanceof ByteMemberValue) {
            ((ByteMemberValue) memberValue).setValue((Byte) value);
        } else if (memberValue instanceof CharMemberValue) {
            ((CharMemberValue) memberValue).setValue((Character) value);
        } else if (memberValue instanceof ShortMemberValue) {
```

### ConstantValue
Value `memberValue` is always 'null'
in `dubbo-filter/dubbo-filter-validation/src/main/java/org/apache/dubbo/validation/support/jvalidation/JValidatorNew.java`
#### Snippet
```java
        } else if (memberValue instanceof CharMemberValue) {
            ((CharMemberValue) memberValue).setValue((Character) value);
        } else if (memberValue instanceof ShortMemberValue) {
            ((ShortMemberValue) memberValue).setValue((Short) value);
        } else if (memberValue instanceof IntegerMemberValue) {
```

### ConstantValue
Value `memberValue` is always 'null'
in `dubbo-filter/dubbo-filter-validation/src/main/java/org/apache/dubbo/validation/support/jvalidation/JValidatorNew.java`
#### Snippet
```java
        } else if (memberValue instanceof ShortMemberValue) {
            ((ShortMemberValue) memberValue).setValue((Short) value);
        } else if (memberValue instanceof IntegerMemberValue) {
            ((IntegerMemberValue) memberValue).setValue((Integer) value);
        } else if (memberValue instanceof LongMemberValue) {
```

### ConstantValue
Value `memberValue` is always 'null'
in `dubbo-filter/dubbo-filter-validation/src/main/java/org/apache/dubbo/validation/support/jvalidation/JValidatorNew.java`
#### Snippet
```java
        } else if (memberValue instanceof IntegerMemberValue) {
            ((IntegerMemberValue) memberValue).setValue((Integer) value);
        } else if (memberValue instanceof LongMemberValue) {
            ((LongMemberValue) memberValue).setValue((Long) value);
        } else if (memberValue instanceof FloatMemberValue) {
```

### ConstantValue
Value `memberValue` is always 'null'
in `dubbo-filter/dubbo-filter-validation/src/main/java/org/apache/dubbo/validation/support/jvalidation/JValidatorNew.java`
#### Snippet
```java
        } else if (memberValue instanceof LongMemberValue) {
            ((LongMemberValue) memberValue).setValue((Long) value);
        } else if (memberValue instanceof FloatMemberValue) {
            ((FloatMemberValue) memberValue).setValue((Float) value);
        } else if (memberValue instanceof DoubleMemberValue) {
```

### ConstantValue
Value `memberValue` is always 'null'
in `dubbo-filter/dubbo-filter-validation/src/main/java/org/apache/dubbo/validation/support/jvalidation/JValidatorNew.java`
#### Snippet
```java
        } else if (memberValue instanceof FloatMemberValue) {
            ((FloatMemberValue) memberValue).setValue((Float) value);
        } else if (memberValue instanceof DoubleMemberValue) {
            ((DoubleMemberValue) memberValue).setValue((Double) value);
        } else if (memberValue instanceof ClassMemberValue) {
```

### ConstantValue
Value `memberValue` is always 'null'
in `dubbo-filter/dubbo-filter-validation/src/main/java/org/apache/dubbo/validation/support/jvalidation/JValidatorNew.java`
#### Snippet
```java
        } else if (memberValue instanceof DoubleMemberValue) {
            ((DoubleMemberValue) memberValue).setValue((Double) value);
        } else if (memberValue instanceof ClassMemberValue) {
            ((ClassMemberValue) memberValue).setValue(((Class<?>) value).getName());
        } else if (memberValue instanceof StringMemberValue) {
```

### ConstantValue
Value `memberValue` is always 'null'
in `dubbo-filter/dubbo-filter-validation/src/main/java/org/apache/dubbo/validation/support/jvalidation/JValidatorNew.java`
#### Snippet
```java
        } else if (memberValue instanceof ClassMemberValue) {
            ((ClassMemberValue) memberValue).setValue(((Class<?>) value).getName());
        } else if (memberValue instanceof StringMemberValue) {
            ((StringMemberValue) memberValue).setValue((String) value);
        } else if (memberValue instanceof EnumMemberValue) {
```

### ConstantValue
Value `memberValue` is always 'null'
in `dubbo-filter/dubbo-filter-validation/src/main/java/org/apache/dubbo/validation/support/jvalidation/JValidatorNew.java`
#### Snippet
```java
        } else if (memberValue instanceof StringMemberValue) {
            ((StringMemberValue) memberValue).setValue((String) value);
        } else if (memberValue instanceof EnumMemberValue) {
            ((EnumMemberValue) memberValue).setValue(((Enum<?>) value).name());
        }
```

### ConstantValue
Value `memberValue` is always 'null'
in `dubbo-filter/dubbo-filter-validation/src/main/java/org/apache/dubbo/validation/support/jvalidation/JValidatorNew.java`
#### Snippet
```java
        }
        /* else if (memberValue instanceof AnnotationMemberValue) */
        else if (memberValue instanceof ArrayMemberValue) {
            CtClass arrayType = type.getComponentType();
            int len = Array.getLength(value);
```

### ConstantValue
Condition `exception instanceof AccessDeniedException` is always `false` when reached
in `dubbo-plugin/dubbo-spring-security/src/main/java/org/apache/dubbo/spring/security/filter/AuthenticationExceptionTranslatorFilter.java`
#### Snippet
```java

        return result.hasException()
            && (exception instanceof AuthenticationException || exception instanceof AccessDeniedException);

    }
```

### ConstantValue
Value `exception` is always 'null'
in `dubbo-plugin/dubbo-spring-security/src/main/java/org/apache/dubbo/spring/security/filter/AuthenticationExceptionTranslatorFilter.java`
#### Snippet
```java

        return result.hasException()
            && (exception instanceof AuthenticationException || exception instanceof AccessDeniedException);

    }
```

### ConstantValue
Value `value` is always 'null'
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/filter/GenericImplFilter.java`
#### Snippet
```java
                    if (ProtocolUtils.isBeanGenericSerialization(generic)) {
                        if (value == null) {
                            appResponse.setValue(value);
                        } else if (value instanceof JavaBeanDescriptor) {
                            appResponse.setValue(JavaBeanSerializeUtil.deserialize((JavaBeanDescriptor) value));
```

### ConstantValue
Condition `lastException != null` is always `true`
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/filter/GenericImplFilter.java`
#### Snippet
```java
                        }
                        appResponse.setException(targetException);
                    } else if (lastException != null) {
                        throw lastException;
                    }
```

### ConstantValue
Value `mock` is always 'null'
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/support/MockInvoker.java`
#### Snippet
```java
    public static String normalizeMock(String mock) {
        if (mock == null) {
            return mock;
        }

```

### ConstantValue
Condition `meter.getId() == null` is always `false`
in `dubbo-metrics/dubbo-metrics-default/src/main/java/org/apache/dubbo/metrics/report/DefaultMetricsReporter.java`
#### Snippet
```java
        StringBuilder sb = new StringBuilder();
        meterRegistry.getMeters().stream().filter(meter -> {
            if (meter == null || meter.getId() == null || meter.getId().getName() == null) {
                return false;
            }
```

### ConstantValue
Condition `meter.getId().getName() == null` is always `false`
in `dubbo-metrics/dubbo-metrics-default/src/main/java/org/apache/dubbo/metrics/report/DefaultMetricsReporter.java`
#### Snippet
```java
        StringBuilder sb = new StringBuilder();
        meterRegistry.getMeters().stream().filter(meter -> {
            if (meter == null || meter.getId() == null || meter.getId().getName() == null) {
                return false;
            }
```

### ConstantValue
Method reference result is always 'true'
in `dubbo-metrics/dubbo-metrics-default/src/main/java/org/apache/dubbo/metrics/collector/sample/ThreadPoolMetricsSampler.java`
#### Snippet
```java

    public void addExecutors(String name, ExecutorService executorService) {
        Optional.ofNullable(executorService).filter(Objects::nonNull).filter(e -> e instanceof ThreadPoolExecutor)
            .map(e -> (ThreadPoolExecutor) e)
            .ifPresent(threadPoolExecutor -> sampleThreadPoolExecutor.put(name, threadPoolExecutor));
```

### ConstantValue
Condition `!mismatches.isEmpty()` is always `true` when reached
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/condition/matcher/AbstractConditionMatcher.java`
#### Snippet
```java
        }

        if (!matches.isEmpty() && !mismatches.isEmpty()) {
            //when both mismatches and matches contain the same value, then using mismatches first
            for (String mismatch : mismatches) {
```

### ConstantValue
Condition `t instanceof Error` is always `true`
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/exchange/codec/ExchangeCodec.java`
#### Snippet
```java
            } else if (t instanceof RuntimeException) {
                throw (RuntimeException) t;
            } else if (t instanceof Error) {
                throw (Error) t;
            } else {
```

## RuleId[id=StringConcatenationInsideStringBufferAppend]
### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `dubbo-monitor/dubbo-monitor-default/src/main/java/org/apache/dubbo/monitor/dubbo/MetricsFilter.java`
#### Snippet
```java

        for (int i = 0; i < argTypes.length; i++) {
            method.append((i == 0 ? "" : ", ") + argTypes[i].getSimpleName());
        }
        method.append(')');
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/rest/media/MediaType.java`
#### Snippet
```java

        for (MediaType mediaType : values) {
            stringBuilder.append(mediaType.value + " ");
        }
        return stringBuilder.toString();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/handler/NettyHttpHandler.java`
#### Snippet
```java
        StringBuilder stringBuilder = new StringBuilder("\n");
        for (StackTraceElement traceElement : stackTrace) {
            stringBuilder.append("\tat " + traceElement).append("\n");
        }

```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/JVMUtil.java`
#### Snippet
```java
                threadInfo.getThreadState());
        if (threadInfo.getLockName() != null) {
            sb.append(" on " + threadInfo.getLockName());
        }
        if (threadInfo.getLockOwnerName() != null) {
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/JVMUtil.java`
#### Snippet
```java
        }
        if (threadInfo.getLockOwnerName() != null) {
            sb.append(" owned by \"" + threadInfo.getLockOwnerName() +
                    "\" Id=" + threadInfo.getLockOwnerId());
        }
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/JVMUtil.java`
#### Snippet
```java
        LockInfo[] locks = threadInfo.getLockedSynchronizers();
        if (locks.length > 0) {
            sb.append("\n\tNumber of locked synchronizers = " + locks.length);
            sb.append('\n');
            for (LockInfo li : locks) {
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/JVMUtil.java`
#### Snippet
```java
            sb.append('\n');
            for (LockInfo li : locks) {
                sb.append("\t- " + li);
                sb.append('\n');
            }
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `dubbo-plugin/dubbo-qos/src/main/java/org/apache/dubbo/qos/command/impl/Ls.java`
#### Snippet
```java
    public String listConsumer() {
        StringBuilder stringBuilder = new StringBuilder();
        stringBuilder.append("As Consumer side:" + System.lineSeparator());
        Collection<ConsumerModel> consumerModelList = frameworkModel.getServiceRepository().allConsumerModels();

```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `dubbo-plugin/dubbo-qos/src/main/java/org/apache/dubbo/qos/command/impl/Ls.java`
#### Snippet
```java
    public String listProvider() {
        StringBuilder stringBuilder = new StringBuilder();
        stringBuilder.append("As Provider side:" + System.lineSeparator());
        Collection<ProviderModel> providerModelList = frameworkModel.getServiceRepository().allProviderModels();

```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/telnet/support/command/LogTelnetHandler.java`
#### Snippet
```java
                                String content = new String(bb.array()).replace("<", "&lt;")
                                        .replace(">", "&gt;").replace("\n", "<br/><br/>");
                                buf.append("\r\ncontent:" + content);

                                buf.append("\r\nmodified:" + (new SimpleDateFormat("yyyy-MM-dd HH:mm:ss")
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/telnet/support/command/LogTelnetHandler.java`
#### Snippet
```java
                                buf.append("\r\ncontent:" + content);

                                buf.append("\r\nmodified:" + (new SimpleDateFormat("yyyy-MM-dd HH:mm:ss")
                                        .format(new Date(file.lastModified()))));
                                buf.append("\r\nsize:" + size + "\r\n");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/telnet/support/command/LogTelnetHandler.java`
#### Snippet
```java
                                buf.append("\r\nmodified:" + (new SimpleDateFormat("yyyy-MM-dd HH:mm:ss")
                                        .format(new Date(file.lastModified()))));
                                buf.append("\r\nsize:" + size + "\r\n");
                            }
                        }
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/telnet/support/command/LogTelnetHandler.java`
#### Snippet
```java
            }
        }
        buf.append("\r\nCURRENT LOG LEVEL:" + LoggerFactory.getLevel())
                .append("\r\nCURRENT LOG APPENDER:" + (file == null ? "console" : file.getAbsolutePath()));
        return buf.toString();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/telnet/support/command/LogTelnetHandler.java`
#### Snippet
```java
        }
        buf.append("\r\nCURRENT LOG LEVEL:" + LoggerFactory.getLevel())
                .append("\r\nCURRENT LOG APPENDER:" + (file == null ? "console" : file.getAbsolutePath()));
        return buf.toString();
    }
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/telnet/support/command/HelpTelnetHandler.java`
#### Snippet
```java
        StringBuilder buf = new StringBuilder();
        buf.append("Command:\r\n    ");
        buf.append(message + " " + help.parameter().replace("\r\n", " ").replace("\n", " "));
        buf.append("\r\nSummary:\r\n    ");
        buf.append(help.summary().replace("\r\n", " ").replace("\n", " "));
```

## RuleId[id=OptionalIsPresent]
### OptionalIsPresent
Can be replaced with single expression in functional style
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ApplicationModel.java`
#### Snippet
```java
    public String tryGetApplicationName() {
        Optional<ApplicationConfig> appCfgOptional = getApplicationConfigManager().getApplication();
        return appCfgOptional.isPresent() ? appCfgOptional.get().getName() : null;
    }

```

## RuleId[id=RedundantOperationOnEmptyContainer]
### RedundantOperationOnEmptyContainer
Map `outputHeaders` is always empty
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/netty/NettyHttpResponse.java`
#### Snippet
```java
        }
        outputHeaders.clear();
        outputHeaders.clear();
    }

```

## RuleId[id=RedundantLengthCheck]
### RedundantLengthCheck
Redundant array length check
in `dubbo-common/src/main/java/org/apache/dubbo/common/URLBuilder.java`
#### Snippet
```java
    @Override
    public URLBuilder removeParameters(String... keys) {
        if (keys == null || keys.length == 0) {
            return this;
        }
```

### RedundantLengthCheck
Redundant array length check
in `dubbo-common/src/main/java/org/apache/dubbo/common/URL.java`
#### Snippet
```java
        urls.add(this);
        String[] backups = getParameter(RemotingConstants.BACKUP_KEY, new String[0]);
        if (backups != null && backups.length > 0) {
            for (String backup : backups) {
                urls.add(this.setAddress(backup));
```

### RedundantLengthCheck
Redundant array length check
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ClassUtils.java`
#### Snippet
```java

    public static boolean hasMethods(Method[] methods) {
        if (methods == null || methods.length == 0) {
            return false;
        }
```

### RedundantLengthCheck
Redundant array length check
in `dubbo-common/src/main/java/org/apache/dubbo/common/compiler/support/ClassUtils.java`
#### Snippet
```java
    public static <K, V> Map<K, V> toMap(Map.Entry<K, V>[] entries) {
        Map<K, V> map = new HashMap<K, V>();
        if (entries != null && entries.length > 0) {
            for (Map.Entry<K, V> entry : entries) {
                map.put(entry.getKey(), entry.getValue());
```

### RedundantLengthCheck
Redundant array length check
in `dubbo-common/src/main/java/org/apache/dubbo/common/compiler/support/ClassUtils.java`
#### Snippet
```java
            return classForName(className);
        } catch (ClassNotFoundException e) {
            if (packages != null && packages.length > 0) {
                for (String pkg : packages) {
                    try {
```

### RedundantLengthCheck
Redundant array length check
in `dubbo-common/src/main/java/org/apache/dubbo/common/extension/support/ActivateComparator.java`
#### Snippet
```java

    private Class<?> findSpi(Class<?> clazz) {
        if (clazz.getInterfaces().length == 0) {
            return null;
        }
```

### RedundantLengthCheck
Redundant array length check
in `dubbo-common/src/main/java/org/apache/dubbo/metadata/definition/util/JaketConfigurationUtils.java`
#### Snippet
```java
        String canonicalName = clazz.getCanonicalName();

        if (closedTypes != null && closedTypes.length > 0) {
            for (String type : closedTypes) {
                if (canonicalName.startsWith(type)) {
```

### RedundantLengthCheck
Redundant array length check
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/metadata/MetadataServiceNameMapping.java`
#### Snippet
```java
                    if (StringUtils.isNotEmpty(oldConfigContent)) {
                        String[] oldAppNames = oldConfigContent.split(",");
                        if (oldAppNames.length > 0) {
                            for (String oldAppName : oldAppNames) {
                                if (oldAppName.equals(appName)) {
```

### RedundantLengthCheck
Redundant array length check
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/status/SpringStatusChecker.java`
#### Snippet
```java
                }
                String[] configs = (String[]) method.invoke(applicationContext, new Object[0]);
                if (configs != null && configs.length > 0) {
                    for (String config : configs) {
                        if (buf.length() > 0) {
```

### RedundantLengthCheck
Redundant array length check
in `dubbo-filter/dubbo-filter-validation/src/main/java/org/apache/dubbo/validation/support/jvalidation/JValidator.java`
#### Snippet
```java
    private static boolean hasConstraintParameter(Method method) {
        Annotation[][] parameterAnnotations = method.getParameterAnnotations();
        if (parameterAnnotations != null && parameterAnnotations.length > 0) {
            for (Annotation[] annotations : parameterAnnotations) {
                for (Annotation annotation : annotations) {
```

### RedundantLengthCheck
Redundant array length check
in `dubbo-filter/dubbo-filter-validation/src/main/java/org/apache/dubbo/validation/support/jvalidation/JValidatorNew.java`
#### Snippet
```java
    private static boolean hasConstraintParameter(Method method) {
        Annotation[][] parameterAnnotations = method.getParameterAnnotations();
        if (parameterAnnotations.length > 0) {
            for (Annotation[] annotations : parameterAnnotations) {
                for (Annotation annotation : annotations) {
```

### RedundantLengthCheck
Redundant array length check
in `dubbo-plugin/dubbo-qos/src/main/java/org/apache/dubbo/qos/command/impl/PortTelnet.java`
#### Snippet
```java
        String port = null;
        boolean detail = false;
        if (args.length > 0) {
            for (String part : args) {
                if ("-l".equals(part)) {
```

### RedundantLengthCheck
Redundant array length check
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/ReflectionPackableMethod.java`
#### Snippet
```java
        while (clazz != Object.class && clazz != null) {
            Class<?>[] interfaces = clazz.getInterfaces();
            if (interfaces.length > 0) {
                for (Class<?> clazzInterface : interfaces) {
                    if (PROTOBUF_MESSAGE_CLASS_NAME.equalsIgnoreCase(clazzInterface.getName())) {
```

## RuleId[id=UNCHECKED_WARNING]
### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Class\>' to 'java.lang.Class'
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/utils/CompositeReferenceCache.java`
#### Snippet
```java
        T proxy = null;
        if (singleton) {
            proxy = get(key, (Class<T>) type);
        } else {
            logger.warn(CONFIG_API_WRONG_USE, "the api method is being used incorrectly", "", "Using non-singleton ReferenceConfig and ReferenceCache at the same time may cause memory leak. " +
```

### UNCHECKED_WARNING
Unchecked call to 'complete(T)' as a member of raw type 'java.util.concurrent.CompletableFuture'
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/deploy/DefaultModuleDeployer.java`
#### Snippet
```java
    private void completeStartFuture(boolean value) {
        if (startFuture != null && !startFuture.isDone()) {
            startFuture.complete(value);
        }
        if (exportFuture != null && !exportFuture.isDone()) {
```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.dubbo.config.bootstrap.builders.ReferenceBuilder' to 'org.apache.dubbo.config.bootstrap.builders.ReferenceBuilder'
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/bootstrap/DubboBootstrap.java`
#### Snippet
```java
    private <S> ReferenceConfig createReferenceConfig(String id, Consumer<ReferenceBuilder<S>> consumerBuilder) {
        ReferenceBuilder builder = createReferenceBuilder(id);
        consumerBuilder.accept(builder);
        ReferenceConfig referenceConfig = builder.build();
        return referenceConfig;
```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.dubbo.config.bootstrap.builders.ServiceBuilder' to 'org.apache.dubbo.config.bootstrap.builders.ServiceBuilder'
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/bootstrap/DubboBootstrap.java`
#### Snippet
```java
    private <S> ServiceConfig createServiceConfig(String id, Consumer<ServiceBuilder<S>> consumerBuilder) {
        ServiceBuilder builder = createServiceBuilder(id);
        consumerBuilder.accept(builder);
        ServiceConfig serviceConfig = builder.build();
        return serviceConfig;
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.concurrent.CompletableFuture' to 'java.util.concurrent.CompletableFuture'
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/deploy/DefaultApplicationDeployer.java`
#### Snippet
```java
        }
        setStarting();
        startFuture = new CompletableFuture();
        if (logger.isInfoEnabled()) {
            logger.info(getIdentifier() + " is starting.");
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.SortedMap' to 'java.util.SortedMap'
in `dubbo-spring-boot/dubbo-spring-boot-compatible/actuator/src/main/java/org/apache/dubbo/spring/boot/actuate/endpoint/metadata/DubboPropertiesMetadata.java`
#### Snippet
```java

    public SortedMap<String, Object> properties() {
        return (SortedMap) filterDubboProperties(environment);
    }
}
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Map' to 'java.util.Map'
in `dubbo-monitor/dubbo-monitor-default/src/main/java/org/apache/dubbo/monitor/dubbo/MetricsFilter.java`
#### Snippet
```java

                MetricsCollector collector = MetricsCollectorFactory.createNew(
                    CollectLevel.NORMAL, Collections.EMPTY_MAP, rateFactor, durationFactor, null);

                for (Map.Entry<MetricName, FastCompass> entry : fastCompasses.entrySet()) {
```

### UNCHECKED_WARNING
Unchecked call to 'addAll(Collection)' as a member of raw type 'java.util.List'
in `dubbo-monitor/dubbo-monitor-default/src/main/java/org/apache/dubbo/monitor/dubbo/MetricsFilter.java`
#### Snippet
```java

                List res = collector.build();
                res.addAll(getThreadPoolMessage());
                return AsyncRpcResult.newDefaultAsyncResult(JsonUtils.toJson(res), invocation);
            }
```

### UNCHECKED_WARNING
Unchecked generics array creation for varargs parameter
in `dubbo-spring-boot/dubbo-spring-boot-compatible/autoconfigure/src/main/java/org/apache/dubbo/spring/boot/context/event/AwaitingNonWebApplicationListener.java`
#### Snippet
```java

    private static final Class<? extends ApplicationEvent>[] SUPPORTED_APPLICATION_EVENTS =
        of(ApplicationReadyEvent.class, ContextClosedEvent.class);

    private final AtomicBoolean awaited = new AtomicBoolean(false);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Map'
in `dubbo-xds/src/main/java/org/apache/dubbo/registry/xds/util/bootstrap/BootstrapperImpl.java`
#### Snippet
```java
        Map<String, ?> rawBootstrap;
        try {
            rawBootstrap = (Map<String, ?>) JsonParser.parse(fileContent);
        } catch (IOException e) {
            throw new XdsInitializationException("Failed to parse JSON", e);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.util.function.Function' to 'java.util.function.Function'
in `dubbo-registry/dubbo-registry-zookeeper/src/main/java/org/apache/dubbo/registry/zookeeper/util/CuratorFrameworkParams.java`
#### Snippet
```java
        this.name = name;
        this.defaultValue = defaultValue;
        this.converter = (Function<String, Object>) converter;
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `dubbo-registry/dubbo-registry-zookeeper/src/main/java/org/apache/dubbo/registry/zookeeper/util/CuratorFrameworkParams.java`
#### Snippet
```java
        String param = url.getParameter(name);
        Object value = param != null ? converter.apply(param) : defaultValue;
        return (T) value;
    }
}
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.LinkedHashSet' to 'java.util.Set'
in `dubbo-registry/dubbo-registry-zookeeper/src/main/java/org/apache/dubbo/registry/zookeeper/ZookeeperServiceDiscovery.java`
#### Snippet
```java
    @Override
    public Set<String> getServices() {
        return doInServiceDiscovery(s -> new LinkedHashSet<>(s.queryForNames()));
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.List'
in `dubbo-registry/dubbo-registry-zookeeper/src/main/java/org/apache/dubbo/registry/zookeeper/ZookeeperRegistry.java`
#### Snippet
```java

        protected void doNotify(String path, Object rawAddresses) {
            ZookeeperRegistry.this.notify(consumerUrl, listener, ZookeeperRegistry.this.toUrlsWithEmpty(consumerUrl, path, (List<String>) rawAddresses));
        }
    }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Class\>' to 'java.lang.Class'
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/rest/RestMetadataConstants.java`
#### Snippet
```java
         * @since 2.7.9
         */
        Class<? extends Annotation> CONTROLLER_ANNOTATION_CLASS = (Class<? extends Annotation>) resolveClass(CONTROLLER_ANNOTATION_CLASS_NAME, getClassLoader());

        /**
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Class\>' to 'java.lang.Class'
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/rest/RestMetadataConstants.java`
#### Snippet
```java
         * @since 2.7.9
         */
        Class<? extends Annotation> REQUEST_MAPPING_ANNOTATION_CLASS = (Class<? extends Annotation>) resolveClass(REQUEST_MAPPING_ANNOTATION_CLASS_NAME, getClassLoader());

        /**
```

### UNCHECKED_WARNING
Unchecked generics array creation for varargs parameter
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/rest/AbstractServiceRestMetadataResolver.java`
#### Snippet
```java

    protected final boolean isImplementedInterface(Class<?> serviceType) {
        return !getAllInterfaces(serviceType).isEmpty();
    }

```

### UNCHECKED_WARNING
Unchecked generics array creation for varargs parameter
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/rest/AbstractServiceRestMetadataResolver.java`
#### Snippet
```java

    protected final boolean isServiceAnnotationPresent(Class<?> serviceType) {
        return isAnyAnnotationPresent(serviceType, DubboService.class, Service.class,
            com.alibaba.dubbo.config.annotation.Service.class);
    }
```

### UNCHECKED_WARNING
Unchecked generics array creation for varargs parameter
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/rest/AbstractServiceRestMetadataResolver.java`
#### Snippet
```java
        Map<Method, Method> serviceMethodsMap = new LinkedHashMap<>();
        // exclude the public methods declared in java.lang.Object.class
        List<Method> declaredServiceMethods = new ArrayList<>(getAllMethods(serviceInterfaceClass, excludedDeclaredClass(Object.class)));

        // controller class
```

### UNCHECKED_WARNING
Unchecked generics array creation for varargs parameter
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/rest/AbstractServiceRestMetadataResolver.java`
#### Snippet
```java
        }

        List<Method> serviceMethods = new ArrayList<>(getAllMethods(serviceType, excludedDeclaredClass(Object.class)));


```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.dubbo.rpc.protocol.rest.ReferenceCountedClient' to 'org.apache.dubbo.rpc.protocol.rest.ReferenceCountedClient'
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/ReferenceCountedClient.java`
#### Snippet
```java
                    if (referenceCountedClient.isDestroyed()) {
                        RestClient restClient = clientFactory.createRestClient(url);
                        clients.put(url.getAddress(), new ReferenceCountedClient(restClient, clients, clientFactory, url));
                        return (T) restClient;
                    } else {
```

### UNCHECKED_WARNING
Unchecked call to 'ReferenceCountedClient(T, ConcurrentMap\>, RestClientFactory, URL)' as a member of raw type 'org.apache.dubbo.rpc.protocol.rest.ReferenceCountedClient'
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/ReferenceCountedClient.java`
#### Snippet
```java
                    if (referenceCountedClient.isDestroyed()) {
                        RestClient restClient = clientFactory.createRestClient(url);
                        clients.put(url.getAddress(), new ReferenceCountedClient(restClient, clients, clientFactory, url));
                        return (T) restClient;
                    } else {
```

### UNCHECKED_WARNING
Unchecked cast: 'org.apache.dubbo.remoting.http.RestClient' to 'T'
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/ReferenceCountedClient.java`
#### Snippet
```java
                        RestClient restClient = clientFactory.createRestClient(url);
                        clients.put(url.getAddress(), new ReferenceCountedClient(restClient, clients, clientFactory, url));
                        return (T) restClient;
                    } else {
                        return (T) referenceCountedClient.client;
```

### UNCHECKED_WARNING
Unchecked cast: 'capture' to 'T'
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/ReferenceCountedClient.java`
#### Snippet
```java
                        return (T) restClient;
                    } else {
                        return (T) referenceCountedClient.client;
                    }
                }
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.lang.Class' to 'java.lang.Class'
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/RestInvoker.java`
#### Snippet
```java
                       Set<HttpConnectionPreBuildIntercept> httpConnectionPreBuildIntercepts,
                       ServiceRestMetadata serviceRestMetadata) {
        super(type, url);
        this.serviceRestMetadata = serviceRestMetadata;
        this.referenceCountedClient = referenceCountedClient;
```

### UNCHECKED_WARNING
Unchecked call to 'getDeclaredConstructor(Class...)' as a member of raw type 'java.lang.Class'
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/MultiValueCreator.java`
#### Snippet
```java
        try {
            if (typeJudge(springMultiValueMapClass, targetType)) {
                return springMultiValueMapImplClass.getDeclaredConstructor().newInstance();
            } else if (typeJudge(jaxrsMultiValueMapClass, targetType)) {
                return jaxrsMultiValueMapImplClass.getDeclaredConstructor().newInstance();
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.ArrayList' to 'java.util.Collection'
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/DataParseUtils.java`
#### Snippet
```java
                do {
                    if (!st.hasMoreTokens()) {
                        return toStringArray(tokens);
                    }

```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.ArrayList'
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/DataParseUtils.java`
#### Snippet
```java
                } while (ignoreEmptyTokens && token.length() <= 0);

                tokens.add(token);
            }
        }
```

### UNCHECKED_WARNING
Unchecked call to 'forEach(BiConsumer)' as a member of raw type 'java.util.Map'
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/DataParseUtils.java`
#### Snippet
```java
    public static String serializeForm(Map formData, Charset charset) {
        StringBuilder builder = new StringBuilder();
        formData.forEach((name, values) -> {
            if (name == null) {

```

### UNCHECKED_WARNING
Unchecked call to 'forEach(Consumer)' as a member of raw type 'java.lang.Iterable'
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/DataParseUtils.java`
#### Snippet
```java
                return;
            }
            ((List) values).forEach(value -> {
                try {
                    if (builder.length() != 0) {
```

### UNCHECKED_WARNING
Unchecked call to 'getDeclaredConstructor(Class...)' as a member of raw type 'java.lang.Class'
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/MultiValueCreator.java`
#### Snippet
```java
                return springMultiValueMapImplClass.getDeclaredConstructor().newInstance();
            } else if (typeJudge(jaxrsMultiValueMapClass, targetType)) {
                return jaxrsMultiValueMapImplClass.getDeclaredConstructor().newInstance();
            }
        } catch (Exception e) {
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Enumeration' to 'java.util.Enumeration'. Reason: 'requestFacade' has raw type, so result of getHeaderNames is erased
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/HttpHeaderUtil.java`
#### Snippet
```java
    public static void parseRequestHeader(RpcInvocation rpcInvocation, RequestFacade requestFacade) {

        Enumeration<String> headerNames = requestFacade.getHeaderNames();

        while (headerNames.hasMoreElements()) {
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Map' to 'java.util.Map'
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/report/support/AbstractMetadataReport.java`
#### Snippet
```java
                this.storeProviderMetadata(item.getKey(), (FullServiceDefinition) item.getValue());
            } else if (CONSUMER_SIDE.equals(item.getKey().getSide())) {
                this.storeConsumerMetadata(item.getKey(), (Map) item.getValue());
            }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Byte' to 'T'
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/NumberUtils.java`
#### Snippet
```java

        if (Byte.class == targetClass || byte.class == targetClass) {
            return (T) (isHexNumber(trimmed) ? Byte.decode(trimmed) : Byte.valueOf(trimmed));
        } else if (Short.class == targetClass || short.class == targetClass) {
            return (T) (isHexNumber(trimmed) ? Short.decode(trimmed) : Short.valueOf(trimmed));
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Short' to 'T'
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/NumberUtils.java`
#### Snippet
```java
            return (T) (isHexNumber(trimmed) ? Byte.decode(trimmed) : Byte.valueOf(trimmed));
        } else if (Short.class == targetClass || short.class == targetClass) {
            return (T) (isHexNumber(trimmed) ? Short.decode(trimmed) : Short.valueOf(trimmed));
        } else if (Integer.class == targetClass || int.class == targetClass) {
            return (T) (isHexNumber(trimmed) ? Integer.decode(trimmed) : Integer.valueOf(trimmed));
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Integer' to 'T'
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/NumberUtils.java`
#### Snippet
```java
            return (T) (isHexNumber(trimmed) ? Short.decode(trimmed) : Short.valueOf(trimmed));
        } else if (Integer.class == targetClass || int.class == targetClass) {
            return (T) (isHexNumber(trimmed) ? Integer.decode(trimmed) : Integer.valueOf(trimmed));
        } else if (Long.class == targetClass || long.class == targetClass) {
            return (T) (isHexNumber(trimmed) ? Long.decode(trimmed) : Long.valueOf(trimmed));
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Long' to 'T'
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/NumberUtils.java`
#### Snippet
```java
            return (T) (isHexNumber(trimmed) ? Integer.decode(trimmed) : Integer.valueOf(trimmed));
        } else if (Long.class == targetClass || long.class == targetClass) {
            return (T) (isHexNumber(trimmed) ? Long.decode(trimmed) : Long.valueOf(trimmed));
        } else if (BigInteger.class == targetClass) {
            return (T) (isHexNumber(trimmed) ? decodeBigInteger(trimmed) : new BigInteger(trimmed));
```

### UNCHECKED_WARNING
Unchecked cast: 'java.math.BigInteger' to 'T'
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/NumberUtils.java`
#### Snippet
```java
            return (T) (isHexNumber(trimmed) ? Long.decode(trimmed) : Long.valueOf(trimmed));
        } else if (BigInteger.class == targetClass) {
            return (T) (isHexNumber(trimmed) ? decodeBigInteger(trimmed) : new BigInteger(trimmed));
        } else if (Float.class == targetClass || float.class == targetClass) {
            return (T) Float.valueOf(trimmed);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Float' to 'T'
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/NumberUtils.java`
#### Snippet
```java
            return (T) (isHexNumber(trimmed) ? decodeBigInteger(trimmed) : new BigInteger(trimmed));
        } else if (Float.class == targetClass || float.class == targetClass) {
            return (T) Float.valueOf(trimmed);
        } else if (Double.class == targetClass || double.class == targetClass) {
            return (T) Double.valueOf(trimmed);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Double' to 'T'
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/NumberUtils.java`
#### Snippet
```java
            return (T) Float.valueOf(trimmed);
        } else if (Double.class == targetClass || double.class == targetClass) {
            return (T) Double.valueOf(trimmed);
        } else if (BigDecimal.class == targetClass || Number.class == targetClass) {
            return (T) new BigDecimal(trimmed);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.math.BigDecimal' to 'T'
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/NumberUtils.java`
#### Snippet
```java
            return (T) Double.valueOf(trimmed);
        } else if (BigDecimal.class == targetClass || Number.class == targetClass) {
            return (T) new BigDecimal(trimmed);
        } else {
            throw new IllegalArgumentException(
```

### UNCHECKED_WARNING
Unchecked call to 'contentTypeSupport(MediaType, Class)' as a member of raw type 'org.apache.dubbo.rpc.protocol.rest.message.HttpMessageCodec'
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/message/HttpMessageCodecManager.java`
#### Snippet
```java

        for (HttpMessageCodec httpMessageCodec : httpMessageCodecs) {
            if (httpMessageCodec.contentTypeSupport(mediaType, type) || typeJudge(mediaType, type, httpMessageCodec)) {
                return httpMessageCodec.decode(body, type);
            }
```

### UNCHECKED_WARNING
Unchecked call to 'decode(InputStream, Class)' as a member of raw type 'org.apache.dubbo.rpc.protocol.rest.message.HttpMessageDecode'
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/message/HttpMessageCodecManager.java`
#### Snippet
```java
        for (HttpMessageCodec httpMessageCodec : httpMessageCodecs) {
            if (httpMessageCodec.contentTypeSupport(mediaType, type) || typeJudge(mediaType, type, httpMessageCodec)) {
                return httpMessageCodec.decode(body, type);
            }
        }
```

### UNCHECKED_WARNING
Unchecked call to 'contentTypeSupport(MediaType, Class)' as a member of raw type 'org.apache.dubbo.rpc.protocol.rest.message.HttpMessageCodec'
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/message/HttpMessageCodecManager.java`
#### Snippet
```java
        if (unSerializedBody == null) {
            for (HttpMessageCodec httpMessageCodec : httpMessageCodecs) {
                if (httpMessageCodec.contentTypeSupport(mediaType, bodyType) || typeJudge(mediaType, bodyType, httpMessageCodec)) {
                    return MessageCodecResultPair.pair(false, httpMessageCodec.contentType());
                }
```

### UNCHECKED_WARNING
Unchecked call to 'contentTypeSupport(MediaType, Class)' as a member of raw type 'org.apache.dubbo.rpc.protocol.rest.message.HttpMessageCodec'
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/message/HttpMessageCodecManager.java`
#### Snippet
```java

        for (HttpMessageCodec httpMessageCodec : httpMessageCodecs) {
            if (httpMessageCodec.contentTypeSupport(mediaType, bodyType) || typeJudge(mediaType, bodyType, httpMessageCodec)) {
                httpMessageCodec.encode(outputStream, unSerializedBody, url);
                return MessageCodecResultPair.pair(true, httpMessageCodec.contentType());
```

### UNCHECKED_WARNING
Unchecked call to 'encode(OutputStream, Object, URL)' as a member of raw type 'org.apache.dubbo.rpc.protocol.rest.message.HttpMessageEncode'
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/message/HttpMessageCodecManager.java`
#### Snippet
```java
        for (HttpMessageCodec httpMessageCodec : httpMessageCodecs) {
            if (httpMessageCodec.contentTypeSupport(mediaType, bodyType) || typeJudge(mediaType, bodyType, httpMessageCodec)) {
                httpMessageCodec.encode(outputStream, unSerializedBody, url);
                return MessageCodecResultPair.pair(true, httpMessageCodec.contentType());
            }
```

### UNCHECKED_WARNING
Unchecked call to 'typeSupport(Class)' as a member of raw type 'org.apache.dubbo.rpc.protocol.rest.message.HttpMessageCodec'
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/message/HttpMessageCodecManager.java`
#### Snippet
```java
    private static boolean typeJudge(MediaType mediaType, Class<?> bodyType, HttpMessageCodec httpMessageCodec) {
        return (MediaType.ALL_VALUE.equals(mediaType) || mediaType == null)
            && bodyType != null && httpMessageCodec.typeSupport(bodyType);
    }

```

### UNCHECKED_WARNING
Unchecked call to 'typeSupport(Class)' as a member of raw type 'org.apache.dubbo.rpc.protocol.rest.message.HttpMessageCodec'
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/message/HttpMessageCodecManager.java`
#### Snippet
```java
        for (HttpMessageCodec httpMessageCodec : httpMessageCodecs) {

            if (httpMessageCodec.typeSupport(type)) {
                return httpMessageCodec.contentType();
            }
```

### UNCHECKED_WARNING
Unchecked assignment: 'io.netty.channel.ChannelOption' to 'io.netty.channel.ChannelOption'
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/netty/NettyServer.java`
#### Snippet
```java

        for (Map.Entry<ChannelOption, Object> entry : channelOptions.entrySet()) {
            bootstrap.option(entry.getKey(), entry.getValue());
        }

```

### UNCHECKED_WARNING
Unchecked assignment: 'io.netty.channel.ChannelOption' to 'io.netty.channel.ChannelOption'
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/netty/NettyServer.java`
#### Snippet
```java

        for (Map.Entry<ChannelOption, Object> entry : childChannelOptions.entrySet()) {
            bootstrap.childOption(entry.getKey(), entry.getValue());
        }

```

### UNCHECKED_WARNING
Unchecked call to 'ArrayList(Collection)' as a member of raw type 'java.util.ArrayList'
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/message/codec/MultiValueCodec.java`
#### Snippet
```java
            // only fetch  first
            Set set = valuesMap.keySet();
            ArrayList arrayList = new ArrayList<>(set);
            Object key = arrayList.get(0);
            Object value = valuesMap.get(key);
```

### UNCHECKED_WARNING
Unchecked call to 'parse(T, ArgInfo)' as a member of raw type 'org.apache.dubbo.rpc.protocol.rest.annotation.ParamParser'
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/annotation/ParamParserManager.java`
#### Snippet
```java
            for (ParamParser paramParser : providerParamParsers) {

                paramParser.parse(parseContext, args.get(i));
            }
        }
```

### UNCHECKED_WARNING
Unchecked call to 'result(E)' as a member of raw type 'org.apache.dubbo.rpc.protocol.rest.exception.mapper.ExceptionHandler'
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/exception/mapper/ExceptionMapper.java`
#### Snippet
```java
            return throwable;
        }
        return exceptionHandler.result((Throwable) throwable);
    }

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Enumeration' to 'java.util.Enumeration'. Reason: 'request' has raw type, so result of getParameterNames is erased
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/annotation/param/parse/provider/ParamProviderParamParser.java`
#### Snippet
```java

            Map<String, String> paramMap = new LinkedHashMap<>();
            Enumeration<String> parameterNames = request.getParameterNames();

            while (parameterNames.hasMoreElements()) {
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Enumeration' to 'java.util.Enumeration'. Reason: 'request' has raw type, so result of getHeaderNames is erased
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/annotation/param/parse/provider/HeaderProviderParamParser.java`
#### Snippet
```java

            Map<String, String> headerMap = new LinkedHashMap<>();
            Enumeration<String> headerNames = request.getHeaderNames();

            while (headerNames.hasMoreElements()) {
```

### UNCHECKED_WARNING
Unchecked call to 'onDestroy(T)' as a member of raw type 'org.apache.dubbo.rpc.model.ScopeModelDestroyListener'
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ScopeModel.java`
#### Snippet
```java
        for (ScopeModelDestroyListener destroyListener : destroyListeners) {
            if (destroyListener.isProtocol()) {
                destroyListener.onDestroy(this);
            }
        }
```

### UNCHECKED_WARNING
Unchecked call to 'onAddClassLoader(T, ClassLoader)' as a member of raw type 'org.apache.dubbo.rpc.model.ScopeClassLoaderListener'
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ScopeModel.java`
#### Snippet
```java
    protected void notifyClassLoaderAdd(ClassLoader classLoader) {
        for (ScopeClassLoaderListener classLoaderListener : classLoaderListeners) {
            classLoaderListener.onAddClassLoader(this, classLoader);
        }
    }
```

### UNCHECKED_WARNING
Unchecked call to 'onRemoveClassLoader(T, ClassLoader)' as a member of raw type 'org.apache.dubbo.rpc.model.ScopeClassLoaderListener'
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ScopeModel.java`
#### Snippet
```java
    protected void notifyClassLoaderDestroy(ClassLoader classLoader) {
        for (ScopeClassLoaderListener classLoaderListener : classLoaderListeners) {
            classLoaderListener.onRemoveClassLoader(this, classLoader);
        }
    }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ScopeModel.java`
#### Snippet
```java

    public <T> T getAttribute(String key, Class<T> type) {
        return (T) attributes.get(key);
    }

```

### UNCHECKED_WARNING
Unchecked call to 'onDestroy(T)' as a member of raw type 'org.apache.dubbo.rpc.model.ScopeModelDestroyListener'
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ScopeModel.java`
#### Snippet
```java
    protected void notifyDestroy() {
        for (ScopeModelDestroyListener destroyListener : destroyListeners) {
            destroyListener.onDestroy(this);
        }
    }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.concurrent.CompletableFuture'
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/service/GenericService.java`
#### Snippet
```java
        Object object = $invoke(method, parameterTypes, args);
        if (object instanceof CompletableFuture) {
            return (CompletableFuture<Object>) object;
        }
        return CompletableFuture.completedFuture(object);
```

### UNCHECKED_WARNING
Unchecked cast: 'org.apache.dubbo.common.url.component.DubboServiceAddressURL' to 'T'
in `dubbo-common/src/main/java/org/apache/dubbo/common/url/component/DubboServiceAddressURL.java`
#### Snippet
```java
    @Override
    protected <T extends URL> T newURL(URLAddress urlAddress, URLParam urlParam) {
        return (T) new DubboServiceAddressURL(urlAddress, urlParam, this.consumerURL, this.overrideURL);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.apache.dubbo.common.url.component.ServiceConfigURL' to 'T'
in `dubbo-common/src/main/java/org/apache/dubbo/common/url/component/ServiceConfigURL.java`
#### Snippet
```java
    @Override
    protected <T extends URL> T newURL(URLAddress urlAddress, URLParam urlParam) {
        return (T) new ServiceConfigURL(urlAddress, urlParam, attributes);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.apache.dubbo.common.url.component.ServiceConfigURL' to 'T'
in `dubbo-common/src/main/java/org/apache/dubbo/common/URL.java`
#### Snippet
```java

    protected <T extends URL> T newURL(URLAddress urlAddress, URLParam urlParam) {
        return (T) new ServiceConfigURL(urlAddress, urlParam, attributes);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.util.Map,capture\>' to 'java.util.Map'
in `dubbo-common/src/main/java/org/apache/dubbo/common/url/component/URLParam.java`
#### Snippet
```java
        @Override
        public void putAll(Map<? extends String, ? extends String> m) {
            urlParam = urlParam.addParameters((Map<String, String>) m);
        }

```

### UNCHECKED_WARNING
Unchecked cast: 'capture' to 'A'
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/AnnotationUtils.java`
#### Snippet
```java
            return null;
        }
        return (A) annotatedElement.getAnnotation(annotationType);
    }

```

### UNCHECKED_WARNING
Unchecked generics array creation for varargs parameter
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/TypeUtils.java`
#### Snippet
```java
        allTypes.add(rawClass);
        // Add all super classes
        allTypes.addAll(getAllSuperClasses(rawClass, NON_OBJECT_TYPE_FILTER));

        List<ParameterizedType> allGenericSuperClasses = allTypes
```

### UNCHECKED_WARNING
Unchecked generics array creation for varargs parameter
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/TypeUtils.java`
#### Snippet
```java
        allTypes.add(rawClass);
        // Add all super classes
        allTypes.addAll(getAllSuperClasses(rawClass, NON_OBJECT_TYPE_FILTER));
        // Add all super interfaces
        allTypes.addAll(getAllInterfaces(rawClass));
```

### UNCHECKED_WARNING
Unchecked generics array creation for varargs parameter
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/TypeUtils.java`
#### Snippet
```java
        allTypes.addAll(getAllSuperClasses(rawClass, NON_OBJECT_TYPE_FILTER));
        // Add all super interfaces
        allTypes.addAll(getAllInterfaces(rawClass));

        List<ParameterizedType> allGenericInterfaces = allTypes
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Class\>' to 'java.lang.Class'
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/TypeUtils.java`
#### Snippet
```java

    static <T> Class<T> findActualTypeArgument(Type type, Class<?> interfaceClass, int index) {
        return (Class<T>) findActualTypeArguments(type, interfaceClass).get(index);
    }

```

### UNCHECKED_WARNING
Unchecked generics array creation for varargs parameter
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/TypeUtils.java`
#### Snippet
```java
        List<Class<?>> actualTypeArguments = new ArrayList<>();

        getAllGenericTypes(type, t -> isAssignableFrom(interfaceClass, getRawClass(t)))
                .forEach(parameterizedType -> {
                    Class<?> rawClass = getRawClass(parameterizedType);
```

### UNCHECKED_WARNING
Unchecked generics array creation for varargs parameter
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ClassUtils.java`
#### Snippet
```java

            // add all super classes to waitResolve
            getAllSuperClasses(clazz)
                .stream()
                .filter(resolved::add)
```

### UNCHECKED_WARNING
Unchecked generics array creation for varargs parameter
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/FieldUtils.java`
#### Snippet
```java
            return field;
        }
        for (Class superType : getAllInheritedTypes(declaredClass)) {
            field = getDeclaredField(superType, fieldName);
            if (field != null) {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/FieldUtils.java`
#### Snippet
```java
            field.setAccessible(accessible);
        }
        return (T) previousValue;
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/FieldUtils.java`
#### Snippet
```java
            field.setAccessible(accessible);
        }
        return (T) value;
    }

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.ArrayList' to 'java.util.List'
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/StringUtils.java`
#### Snippet
```java
            return new String[]{str};
        } else {
            List<String> result = new ArrayList();
            int pos;
            if ("".equals(delimiter)) {
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Collection' to 'java.util.Collection'
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/StringUtils.java`
#### Snippet
```java
            }

            return toStringArray((Collection) result);
        }
    }
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.LinkedHashSet' to 'java.util.Set'
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/StringUtils.java`
#### Snippet
```java

        if (!trimElements) { // Do not require to trim the elements
            return new LinkedHashSet(values);
        }

```

### UNCHECKED_WARNING
Unchecked call to 'LinkedHashSet(Collection)' as a member of raw type 'java.util.LinkedHashSet'
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/StringUtils.java`
#### Snippet
```java

        if (!trimElements) { // Do not require to trim the elements
            return new LinkedHashSet(values);
        }

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Map' to 'java.util.Map'
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/PropertiesConfiguration.java`
#### Snippet
```java

    public Map<String, String> getProperties() {
        return (Map) properties;
    }
}
```

### UNCHECKED_WARNING
Unchecked method 'valueOf(Class, String)' invocation
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/PojoUtils.java`
#### Snippet
```java

        if (type != null && type.isEnum() && pojo.getClass() == String.class) {
            return Enum.valueOf((Class<Enum>) type, (String) pojo);
        }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Class\>' to 'java.lang.Class'
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/PojoUtils.java`
#### Snippet
```java

        if (type != null && type.isEnum() && pojo.getClass() == String.class) {
            return Enum.valueOf((Class<Enum>) type, (String) pojo);
        }

```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Collection'
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/PojoUtils.java`
#### Snippet
```java
                    Object obj = Array.get(pojo, i);
                    Object value = realize1(obj, ctype, null, mapGeneric, history);
                    dest.add(value);
                }
                return dest;
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Collection'
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/PojoUtils.java`
#### Snippet
```java
            if (type.isArray()) {
                Class<?> ctype = type.getComponentType();
                Collection<Object> src = (Collection<Object>) pojo;
                int len = src.size();
                Object dest = Array.newInstance(ctype, len);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Collection'
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/PojoUtils.java`
#### Snippet
```java
                return dest;
            } else {
                Collection<Object> src = (Collection<Object>) pojo;
                int len = src.size();
                Collection<Object> dest = createCollection(type, len);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Map'
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/PojoUtils.java`
#### Snippet
```java

        if (pojo instanceof Map<?, ?> && type != null) {
            Object className = ((Map<Object, Object>) pojo).get("class");
            if (className instanceof String) {
                if (!CLASS_NOT_FOUND_CACHE.containsKey(className)) {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Map'
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/PojoUtils.java`
#### Snippet
```java
            // special logic for enum
            if (type.isEnum()) {
                Object name = ((Map<Object, Object>) pojo).get("name");
                if (name != null) {
                    if (!(name instanceof String)) {
```

### UNCHECKED_WARNING
Unchecked method 'valueOf(Class, String)' invocation
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/PojoUtils.java`
#### Snippet
```java
                        throw new IllegalArgumentException("`name` filed should be string!");
                    } else {
                        return Enum.valueOf((Class<Enum>) type, (String) name);
                    }
                }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Class\>' to 'java.lang.Class'
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/PojoUtils.java`
#### Snippet
```java
                        throw new IllegalArgumentException("`name` filed should be string!");
                    } else {
                        return Enum.valueOf((Class<Enum>) type, (String) name);
                    }
                }
```

### UNCHECKED_WARNING
Unchecked cast: 'capture' to 'java.util.Map'
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/PojoUtils.java`
#### Snippet
```java
            if (!type.isInterface() && !type.isAssignableFrom(pojo.getClass())) {
                try {
                    map = (Map<Object, Object>) type.getDeclaredConstructor().newInstance();
                    Map<Object, Object> mapPojo = (Map<Object, Object>) pojo;
                    map.putAll(mapPojo);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Map'
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/PojoUtils.java`
#### Snippet
```java
                try {
                    map = (Map<Object, Object>) type.getDeclaredConstructor().newInstance();
                    Map<Object, Object> mapPojo = (Map<Object, Object>) pojo;
                    map.putAll(mapPojo);
                    if (GENERIC_WITH_CLZ) {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Map'
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/PojoUtils.java`
#### Snippet
```java
                } catch (Exception e) {
                    //ignore error
                    map = (Map<Object, Object>) pojo;
                }
            } else {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Map'
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/PojoUtils.java`
#### Snippet
```java
                }
            } else {
                map = (Map<Object, Object>) pojo;
            }

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Map' to 'java.util.Map'
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/PojoUtils.java`
#### Snippet
```java
                    && !typeKeyType.getTypeName().equals(mapKeyType.getTypeName());
                if (typeMismatch) {
                    result = createMap(new HashMap(0));
                } else {
                    result = createMap(map);
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Map' to 'java.util.Map'
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/PojoUtils.java`
#### Snippet
```java
                    result = createMap(new HashMap(0));
                } else {
                    result = createMap(map);
                }

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Map' to 'java.util.Map'
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/SystemConfiguration.java`
#### Snippet
```java

    public Map<String, String> getProperties() {
        return (Map) System.getProperties();
    }
}
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Map' to 'java.util.Map'
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/OrderedPropertiesConfiguration.java`
#### Snippet
```java

    public Map<String, String> getProperties() {
        return (Map) properties;
    }
}
```

### UNCHECKED_WARNING
Unchecked call to 'getDeclaredMethod(String, Class...)' as a member of raw type 'java.lang.Class'
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/MethodUtils.java`
#### Snippet
```java
        try {
            if (type != null && isNotEmpty(methodName)) {
                method = type.getDeclaredMethod(methodName, parameterTypes);
            }
        } catch (NoSuchMethodException e) {
```

### UNCHECKED_WARNING
Unchecked generics array creation for varargs parameter
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/MethodUtils.java`
#### Snippet
```java
        Class<?> declaringClass = overrider.getDeclaringClass();
        Method overriddenMethod = null;
        for (Class<?> inheritedType : getAllInheritedTypes(declaringClass)) {
            overriddenMethod = findOverriddenMethod(overrider, inheritedType);
            if (overriddenMethod != null) {
```

### UNCHECKED_WARNING
Unchecked generics array creation for varargs parameter
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/MethodUtils.java`
#### Snippet
```java
     */
    static Method findOverriddenMethod(Method overrider, Class<?> declaringClass) {
        List<Method> matchedMethods = getAllMethods(declaringClass, method -> overrides(overrider, method));
        return matchedMethods.isEmpty() ? null : matchedMethods.get(0);
    }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/MethodUtils.java`
#### Snippet
```java
                method.setAccessible(true);
            }
            value = (T) method.invoke(object, methodParameters);
            method.setAccessible(isAccessible);
        } catch (Exception e) {
```

### UNCHECKED_WARNING
Unchecked generics array creation for varargs parameter
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/MethodUtils.java`
#### Snippet
```java
        // If the super classes are resolved, all them into declaredClasses
        if (includeInheritedTypes) {
            declaredClasses.addAll(getAllInheritedTypes(declaringClass));
        }

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Enumeration' to 'java.util.Enumeration'
in `dubbo-common/src/main/java/org/apache/dubbo/common/logger/log4j/Log4jLoggerAdapter.java`
#### Snippet
```java
            org.apache.log4j.Logger logger = LogManager.getRootLogger();
            if (logger != null) {
                Enumeration<Appender> appenders = logger.getAllAppenders();
                if (appenders != null) {
                    while (appenders.hasMoreElements()) {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'. Reason: 'converter' has raw type, so result of convert is erased
in `dubbo-common/src/main/java/org/apache/dubbo/common/convert/ConverterUtil.java`
#### Snippet
```java
        Converter converter = getConverter(source.getClass(), targetType);
        if (converter != null) {
            return (T) converter.convert(source);
        }
        return null;
```

### UNCHECKED_WARNING
Unchecked call to 'convert(S)' as a member of raw type 'org.apache.dubbo.common.convert.Converter'
in `dubbo-common/src/main/java/org/apache/dubbo/common/convert/ConverterUtil.java`
#### Snippet
```java
        Converter converter = getConverter(source.getClass(), targetType);
        if (converter != null) {
            return (T) converter.convert(source);
        }
        return null;
```

### UNCHECKED_WARNING
Unchecked call to 'accept(Class, Class)' as a member of raw type 'org.apache.dubbo.common.convert.Converter'
in `dubbo-common/src/main/java/org/apache/dubbo/common/convert/ConverterUtil.java`
#### Snippet
```java
            .getSupportedExtensionInstances()
            .stream()
            .filter(converter -> converter.accept(sourceType, targetType))
            .collect(Collectors.toList()));

```

### UNCHECKED_WARNING
Unchecked call to 'convert(S)' as a member of raw type 'org.apache.dubbo.common.convert.Converter'
in `dubbo-common/src/main/java/org/apache/dubbo/common/convert/multiple/StringToArrayConverter.java`
#### Snippet
```java

        for (int i = 0; i < size; i++) {
            Array.set(array, i, converter.convert(segments[i]));
        }

```

### UNCHECKED_WARNING
Unchecked generics array creation for varargs parameter
in `dubbo-common/src/main/java/org/apache/dubbo/common/convert/multiple/StringToIterableConverter.java`
#### Snippet
```java
    @Override
    public final int getPriority() {
        int level = getAllInterfaces(getSupportedType(), type ->
            isAssignableFrom(Iterable.class, type)).size();
        return MIN_PRIORITY - level;
```

### UNCHECKED_WARNING
Unchecked call to 'convert(S)' as a member of raw type 'org.apache.dubbo.common.convert.Converter'
in `dubbo-common/src/main/java/org/apache/dubbo/common/convert/multiple/StringToIterableConverter.java`
#### Snippet
```java
                for (int i = 0; i < size; i++) {
                    String segment = segments[i];
                    Object element = converter.convert(segment);
                    collection.add(element);
                }
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Collection'
in `dubbo-common/src/main/java/org/apache/dubbo/common/convert/multiple/StringToIterableConverter.java`
#### Snippet
```java
                    String segment = segments[i];
                    Object element = converter.convert(segment);
                    collection.add(element);
                }
                return collection;
```

### UNCHECKED_WARNING
Unchecked call to 'accept(Class, Class)' as a member of raw type 'org.apache.dubbo.common.convert.multiple.MultiValueConverter'
in `dubbo-common/src/main/java/org/apache/dubbo/common/convert/multiple/MultiValueConverter.java`
#### Snippet
```java
                .getSupportedExtensionInstances()
                .stream()
                .filter(converter -> converter.accept(sourceType, targetType))
                .findFirst()
                .orElse(null);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'. Reason: 'converter' has raw type, so result of convert is erased
in `dubbo-common/src/main/java/org/apache/dubbo/common/convert/multiple/MultiValueConverter.java`
#### Snippet
```java
        MultiValueConverter converter = find(sourceType, multiValueType);
        if (converter != null) {
            return (T) converter.convert(source, multiValueType, elementType);
        }
        return null;
```

### UNCHECKED_WARNING
Unchecked call to 'convert(S, Class, Class)' as a member of raw type 'org.apache.dubbo.common.convert.multiple.MultiValueConverter'
in `dubbo-common/src/main/java/org/apache/dubbo/common/convert/multiple/MultiValueConverter.java`
#### Snippet
```java
        MultiValueConverter converter = find(sourceType, multiValueType);
        if (converter != null) {
            return (T) converter.convert(source, multiValueType, elementType);
        }
        return null;
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.concurrent.SynchronousQueue' to 'java.util.concurrent.BlockingQueue'
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/configcenter/file/FileSystemDynamicConfiguration.java`
#### Snippet
```java
        return new ThreadPoolExecutor(THREAD_POOL_SIZE, THREAD_POOL_SIZE,
            0L, MILLISECONDS,
            new SynchronousQueue(),
            new NamedThreadFactory("dubbo-config-center-watch-events-loop", true));
    }
```

### UNCHECKED_WARNING
Unchecked generics array creation for varargs parameter
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/configcenter/file/FileSystemDynamicConfiguration.java`
#### Snippet
```java
    public static final String DEFAULT_CONFIG_CENTER_ENCODING = "UTF-8";

    private static final WatchEvent.Kind[] INTEREST_PATH_KINDS = of(ENTRY_CREATE, ENTRY_DELETE, ENTRY_MODIFY);

    /**
```

### UNCHECKED_WARNING
Unchecked generics array creation for varargs parameter
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
        }

        Set<Class<?>> allSuperClasses = ClassUtils.getAllSuperClasses(type);
        for (Class<?> aClass : allSuperClasses) {
            for (Field field : aClass.getDeclaredFields()) {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
        try {
            beanInfo = Introspector.getBeanInfo(beanClass);
            propertyValue = (T) Stream.of(beanInfo.getMethodDescriptors())
                    .filter(methodDescriptor -> methodName.equals(methodDescriptor.getName()))
                    .findFirst()
                    .map(method -> {
                        try {
                            return method.getMethod().invoke(bean);
                        } catch (Exception e) {
                            //ignore
                        }
                        return null;
                    }).get();
        } catch (Exception e) {

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Class\>' to 'java.lang.Class'
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java

        if (matchType.isAssignableFrom(sourceClass)) {
            hierarchicalTypes.add((Class<T>) sourceClass);
        }

```

### UNCHECKED_WARNING
Unchecked call to 'getDeclaredConstructor(Class...)' as a member of raw type 'java.lang.Class'
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static boolean checkZeroArgConstructor(Class clazz) {
        try {
            clazz.getDeclaredConstructor();
            return true;
        } catch (NoSuchMethodException e) {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
            Field field = obj.getClass().getDeclaredField(fieldName);
            field.setAccessible(true);
            return (T) field.get(obj);
        } catch (Exception e) {
            throw new RuntimeException(e.getMessage(), e);
```

### UNCHECKED_WARNING
Unchecked call to 'getMethod(String, Class...)' as a member of raw type 'java.lang.Class'
in `dubbo-common/src/main/java/org/apache/dubbo/common/beanutil/JavaBeanSerializeUtil.java`
#### Snippet
```java

    private static Method getEnumValueOfMethod(Class cl) throws NoSuchMethodException {
        return cl.getMethod("valueOf", Class.class, String.class);
    }

```

### UNCHECKED_WARNING
Unchecked call to 'forEach(BiConsumer)' as a member of raw type 'java.util.Map'
in `dubbo-common/src/main/java/org/apache/dubbo/common/beanutil/JavaBeanSerializeUtil.java`
#### Snippet
```java
        } else if (obj instanceof Map) {
            Map map = (Map) obj;
            map.forEach((key, value) -> {
                Object keyDescriptor = key == null ? null : createDescriptorIfAbsent(key, accessor, cache);
                Object valueDescriptor = value == null ? null : createDescriptorIfAbsent(value, accessor, cache);
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Collection'
in `dubbo-common/src/main/java/org/apache/dubbo/common/beanutil/JavaBeanSerializeUtil.java`
#### Snippet
```java
                    deserializeInternal(item, itemDescriptor, loader, cache);
                }
                collection.add(item);
            }
        } else if (beanDescriptor.isMapType()) {
```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.Map'
in `dubbo-common/src/main/java/org/apache/dubbo/common/beanutil/JavaBeanSerializeUtil.java`
#### Snippet
```java
                    deserializeInternal(value, valueDescriptor, loader, cache);
                }
                map.put(key, value);
            }
        } else if (beanDescriptor.isBeanType()) {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `dubbo-common/src/main/java/org/apache/dubbo/common/threadlocal/InternalThreadLocal.java`
#### Snippet
```java
        Object v = threadLocalMap.indexedVariable(index);
        if (v != InternalThreadLocalMap.UNSET) {
            return (V) v;
        }

```

### UNCHECKED_WARNING
Unchecked call to 'putAll(Map)' as a member of raw type 'java.util.Map'
in `dubbo-common/src/main/java/org/apache/dubbo/common/extension/ExtensionLoader.java`
#### Snippet
```java
        Properties properties = loadProperties(ExtensionLoader.class.getClassLoader(),
            SPECIAL_SPI_PROPERTIES);
        map.putAll(properties);
        return map;
    }
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Map' to 'java.util.Map'
in `dubbo-common/src/main/java/org/apache/dubbo/common/extension/ExtensionLoader.java`
#### Snippet
```java
            SPECIAL_SPI_PROPERTIES);
        map.putAll(properties);
        return map;
    }

```

### UNCHECKED_WARNING
Unchecked generics array creation for varargs parameter
in `dubbo-common/src/main/java/org/apache/dubbo/config/AbstractConfig.java`
#### Snippet
```java
        }

        List<Method> methods = MethodUtils.getMethods(this.getClass(), method -> method.getDeclaringClass() != Object.class);
        for (Method method : methods) {
            try {
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Map' to 'java.util.Map'
in `dubbo-common/src/main/java/org/apache/dubbo/config/AbstractConfig.java`
#### Snippet
```java
                    Map<String, String> oldMap = null;
                    if (oldOne instanceof Map) {
                        oldMap = (Map) oldOne;
                    }

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Map' to 'java.util.Map'
in `dubbo-common/src/main/java/org/apache/dubbo/config/AbstractConfig.java`
#### Snippet
```java
                    Map<String, String> newMap = null;
                    if (newResult instanceof Map) {
                        newMap = (Map) newResult;
                    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Map'
in `dubbo-common/src/main/java/org/apache/dubbo/config/AbstractConfig.java`
#### Snippet
```java
            Method method = findMethodByMethodSignature(c, "getParameters", null);
            if (method != null && isParametersGetter(method)) {
                return (Map<String, String>) method.invoke(o);
            }
        } catch (Throwable t) {
```

### UNCHECKED_WARNING
Unchecked generics array creation for varargs parameter
in `dubbo-common/src/main/java/org/apache/dubbo/config/AbstractConfig.java`
#### Snippet
```java

        // loop methods, get override value and set the new value back to method
        List<Method> methods = MethodUtils.getMethods(obj.getClass(), method -> method.getDeclaringClass() != Object.class);
        for (Method method : methods) {
            if (MethodUtils.isSetter(method)) {
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Map' to 'java.util.Map'
in `dubbo-common/src/main/java/org/apache/dubbo/config/AbstractConfig.java`
#### Snippet
```java
                    Object oldOne = getterMethod.invoke(this);
                    if (oldOne instanceof Map) {
                        oldMap = (Map) oldOne;
                    }
                } catch (Exception ignore) {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Map'
in `dubbo-common/src/main/java/org/apache/dubbo/config/AbstractConfig.java`
#### Snippet
```java
                    }
                } else if (isParametersGetter(method)) {
                    Map<String, String> map = (Map<String, String>) method.invoke(config);
                    map = convert(map, prefix);
                    if (asParameters) {
```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.dubbo.config.ReferenceConfigBase' to 'org.apache.dubbo.config.ReferenceConfigBase'
in `dubbo-common/src/main/java/org/apache/dubbo/config/context/ModuleConfigManager.java`
#### Snippet
```java

    public <T> ReferenceConfigBase<T> getReference(String id) {
        return getConfig(ReferenceConfigBase.class, id).orElse(null);
    }

```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.dubbo.config.ServiceConfigBase' to 'org.apache.dubbo.config.ServiceConfigBase'
in `dubbo-common/src/main/java/org/apache/dubbo/config/context/ModuleConfigManager.java`
#### Snippet
```java

    public <T> ServiceConfigBase<T> getService(String id) {
        return getConfig(ServiceConfigBase.class, id).orElse(null);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.apache.dubbo.config.AbstractInterfaceConfig' to 'C'
in `dubbo-common/src/main/java/org/apache/dubbo/config/context/ModuleConfigManager.java`
#### Snippet
```java
        // special check service and reference config by unique service name, speed up the processing of large number of instances
        if (config instanceof ReferenceConfigBase || config instanceof ServiceConfigBase) {
            C existedConfig = (C) findDuplicatedInterfaceConfig((AbstractInterfaceConfig) config);
            if (existedConfig != null) {
                return Optional.of(existedConfig);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.util.Collection' to 'java.util.Collection'
in `dubbo-common/src/main/java/org/apache/dubbo/config/context/AbstractConfigManager.java`
#### Snippet
```java

    protected <C extends AbstractConfig> Collection<C> getConfigs(String configType) {
        return (Collection<C>) getConfigsMap(configType).values();
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.apache.dubbo.config.AbstractConfig' to 'C'
in `dubbo-common/src/main/java/org/apache/dubbo/config/context/AbstractConfigManager.java`
#### Snippet
```java
     */
    protected <C extends AbstractConfig> C getConfigById(String configType, String id) {
        return (C) getConfigsMap(configType).get(id);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.util.Collection' to 'java.util.Collection'
in `dubbo-common/src/main/java/org/apache/dubbo/config/context/AbstractConfigManager.java`
#### Snippet
```java

    public <C extends AbstractConfig> Collection<C> getConfigs(Class<C> configType) {
        return (Collection<C>) getConfigsMap(getTagName(configType)).values();
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.apache.dubbo.config.AbstractConfig' to 'C'
in `dubbo-common/src/main/java/org/apache/dubbo/config/context/AbstractConfigManager.java`
#### Snippet
```java
                    " instances, please remove redundant configs. instances: " + configsMap.values());
        }
        return (C) configsMap.values().iterator().next();
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.apache.dubbo.config.AbstractConfig' to 'T'
in `dubbo-common/src/main/java/org/apache/dubbo/config/context/AbstractConfigManager.java`
#### Snippet
```java
            for (AbstractConfig value : configsMap.values()) {
                if (value.equals(config)) {
                    return (T) value;
                }
            }
```

### UNCHECKED_WARNING
Unchecked cast: 'org.apache.dubbo.config.AbstractConfig' to 'T'
in `dubbo-common/src/main/java/org/apache/dubbo/config/context/AbstractConfigManager.java`
#### Snippet
```java
        // lock by config type
        synchronized (configsMap) {
            return (T) addIfAbsent(config, configsMap);
        }
    }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.util.Map' to 'java.util.Map'
in `dubbo-common/src/main/java/org/apache/dubbo/config/context/AbstractConfigManager.java`
#### Snippet
```java

    protected <C extends AbstractConfig> Map<String, C> getConfigsMap(String configType) {
        return (Map<String, C>) configsCache.getOrDefault(configType, emptyMap());
    }

```

### UNCHECKED_WARNING
Unchecked generics array creation for varargs parameter
in `dubbo-common/src/main/java/org/apache/dubbo/config/MethodConfig.java`
#### Snippet
```java
            String prefix = argument.getIndex() + ".";
            Environment environment = getScopeModel().getModelEnvironment();
            List<java.lang.reflect.Method> methods = MethodUtils.getMethods(argument.getClass(),
                method -> method.getDeclaringClass() != Object.class);
            for (java.lang.reflect.Method method : methods) {
```

### UNCHECKED_WARNING
Unchecked overriding: return type requires unchecked conversion. Found 'org.apache.dubbo.registry.client.OverrideInstanceAddressURL', required 'T'
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/OverrideInstanceAddressURL.java`
#### Snippet
```java
    }
    @Override
    protected OverrideInstanceAddressURL newURL(URLAddress urlAddress, URLParam urlParam) {
        return new OverrideInstanceAddressURL(originUrl, overrideParams);
    }
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Map' to 'java.util.Map'
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/metadata/StandardMetadataServiceURLBuilder.java`
#### Snippet
```java
        Map<String, String> metadata = serviceInstance.getMetadata();
        String param = metadata.get(METADATA_SERVICE_URL_PARAMS_PROPERTY_NAME);
        return isBlank(param) ? emptyMap() : (Map) JsonUtils.toJavaObject(param, Map.class);
    }
}
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.List'
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/event/listener/ServiceInstancesChangedListener.java`
#### Snippet
```java

            List<ProtocolServiceKeyWithUrls> list = newServiceUrls.computeIfAbsent(serviceInfo.getPath(), k -> new LinkedList<>());
            list.add(new ProtocolServiceKeyWithUrls(serviceInfo.getProtocolServiceKey(), (List<URL>) urls));
        }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Map'
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/ConfigCenterBean.java`
#### Snippet
```java
        try {
            if (rawProperties instanceof Map) {
                externalProperties.putAll((Map<String, String>) rawProperties);
            } else if (rawProperties instanceof String) {
                externalProperties.putAll(ConfigurationUtils.parseProperties((String) rawProperties));
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Map'
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/ConfigCenterBean.java`
#### Snippet
```java
                    Object source = propertySource.getSource();
                    if (source instanceof Map) {
                        ((Map<String, Object>) source).forEach((k, v) -> {
                            externalProperties.put(k, (String) v);
                        });
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/SpringCompatUtils.java`
#### Snippet
```java
            return (T) typedString.getValue();
        }
        return (T) val;
    }

```

### UNCHECKED_WARNING
Unchecked call to 'refreshOverrideAndInvoker(List)' as a member of raw type 'org.apache.dubbo.registry.integration.RegistryDirectory'
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/integration/RegistryDirectory.java`
#### Snippet
```java
        @Override
        protected void notifyOverrides() {
            listeners.forEach(listener -> listener.refreshOverrideAndInvoker(Collections.emptyList()));
        }
    }
```

### UNCHECKED_WARNING
Unchecked call to 'refreshOverrideAndInvoker(List)' as a member of raw type 'org.apache.dubbo.registry.integration.RegistryDirectory'
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/integration/RegistryDirectory.java`
#### Snippet
```java
        protected void notifyOverrides() {
            // to notify configurator/router changes
            directory.refreshOverrideAndInvoker(Collections.emptyList());
        }
    }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/ReferenceBean.java`
#### Snippet
```java
            createLazyProxy();
        }
        return (T) lazyProxy;
    }

```

### UNCHECKED_WARNING
Unchecked generics array creation for varargs parameter
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/config/DubboConfigDefaultPropertyValueBeanPostProcessor.java`
#### Snippet
```java
            Method setterMethod = propertyDescriptor.getWriteMethod();
            if (setterMethod != null) { // the getter and setter methods are present
                if (Arrays.equals(ObjectUtils.of(String.class), setterMethod.getParameterTypes())) { // the param type is String
                    // set bean name to the value of the property
                    invokeMethod(setterMethod, bean, beanName);
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.List' to 'java.util.List'
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/ServiceAnnotationPostProcessor.java`
#### Snippet
```java
        builder.addPropertyValue("parameters", DubboAnnotationUtils.convertParameters((String[]) serviceAnnotationAttributes.get("parameters")));
        // Add methods parameters
        List<MethodConfig> methodConfigs = convertMethodConfigs(serviceAnnotationAttributes.get("methods"));
        if (!methodConfigs.isEmpty()) {
            builder.addPropertyValue("methods", methodConfigs);
```

### UNCHECKED_WARNING
Unchecked generics array creation for varargs parameter
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/context/config/NamePropertyDefaultValueDubboConfigBeanCustomizer.java`
#### Snippet
```java
            Method setNameMethod = propertyDescriptor.getWriteMethod();
            if (setNameMethod != null) { // "setName" and "getName" methods are present
                if (Arrays.equals(ObjectUtils.of(String.class), setNameMethod.getParameterTypes())) { // the param type is String
                    // set bean name to the value of the "name" property
                    ReflectionUtils.invokeMethod(setNameMethod, dubboConfigBean, beanName);
```

### UNCHECKED_WARNING
Unchecked generics array creation for varargs parameter
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/ReferenceAnnotationBeanPostProcessor.java`
#### Snippet
```java
     */
    public ReferenceAnnotationBeanPostProcessor() {
        super(DubboReference.class, Reference.class, com.alibaba.dubbo.config.annotation.Reference.class);
    }

```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.dubbo.rpc.Invoker' to 'org.apache.dubbo.rpc.Invoker'
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/integration/RegistryProtocol.java`
#### Snippet
```java
            if (!newUrl.equals(currentUrl)) {
                if (newUrl.getParameter(Constants.NEED_REEXPORT, true)) {
                    RegistryProtocol.this.reExport(originInvoker, newUrl);
                }
                logger.info("exported provider url changed, origin url: " + originUrl +
```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.dubbo.config.spring.ReferenceBean' to 'org.apache.dubbo.config.spring.ReferenceBean'
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/reference/ReferenceBeanBuilder.java`
#### Snippet
```java

    public <T> ReferenceBean<T> build() {
        return new ReferenceBean(attributes);
    }

```

### UNCHECKED_WARNING
Unchecked call to 'ReferenceBean(Map)' as a member of raw type 'org.apache.dubbo.config.spring.ReferenceBean'
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/reference/ReferenceBeanBuilder.java`
#### Snippet
```java

    public <T> ReferenceBean<T> build() {
        return new ReferenceBean(attributes);
    }

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.List' to 'java.util.List'
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/reference/ReferenceBeanManager.java`
#### Snippet
```java

    public List<String> getBeanNamesByKey(String key) {
        return Collections.unmodifiableList(referenceKeyMap.getOrDefault(key, Collections.EMPTY_LIST));
    }

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.List' to 'java.util.List'
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/reference/ReferenceBeanManager.java`
#### Snippet
```java

    public List<String> getBeanNamesByKey(String key) {
        return Collections.unmodifiableList(referenceKeyMap.getOrDefault(key, Collections.EMPTY_LIST));
    }

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Map' to 'java.util.Map'. Reason: 'referenceBean' has raw type, so result of getReferenceProps is erased
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/reference/ReferenceBeanSupport.java`
#### Snippet
```java

    public static Map<String, Object> getReferenceAttributes(ReferenceBean referenceBean) {
        Map<String, Object> referenceProps = referenceBean.getReferenceProps();
        if (referenceProps == null) {
            MutablePropertyValues propertyValues = referenceBean.getPropertyValues();
```

### UNCHECKED_WARNING
Unchecked call to 'getAnnotation(Class)' as a member of raw type 'java.lang.Class'
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/reference/ReferenceBeanSupport.java`
#### Snippet
```java
        ProvidedBy providedbBy = null;
        if (defaultInterfaceClass != null) {
            providedbBy = (ProvidedBy) defaultInterfaceClass.getAnnotation(ProvidedBy.class);
        }
        if (providedbBy != null && providedbBy.name() != null && providedbBy.name().length > 0) {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Map'
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/reference/ReferenceBeanSupport.java`
#### Snippet
```java
            return Arrays.toString(newArray);
        } else if (obj instanceof Map) {
            Map<String, Object> map = (Map<String, Object>) obj;
            TreeMap newMap = new TreeMap();
            for (Map.Entry<String, Object> entry : map.entrySet()) {
```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.TreeMap'
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/reference/ReferenceBeanSupport.java`
#### Snippet
```java
            TreeMap newMap = new TreeMap();
            for (Map.Entry<String, Object> entry : map.entrySet()) {
                newMap.put(entry.getKey(), convertToString(entry.getKey(), entry.getValue()));
            }
            return String.valueOf(newMap);
```

### UNCHECKED_WARNING
Unchecked cast: 'org.apache.dubbo.rpc.protocol.tri.observer.CallStreamObserver\>' to 'org.apache.dubbo.rpc.protocol.tri.observer.CallStreamObserver'
in `dubbo-plugin/dubbo-reactive/src/main/java/org/apache/dubbo/reactive/calls/ReactorClientCalls.java`
#### Snippet
```java
            ClientTripleReactorSubscriber<TRequest> clientSubscriber = requestFlux.subscribeWith(new ClientTripleReactorSubscriber<>());
            ClientTripleReactorPublisher<TResponse> clientPublisher = new ClientTripleReactorPublisher<>(
                s -> clientSubscriber.subscribe((CallStreamObserver<TRequest>) s),
                clientSubscriber::cancel);
            return Flux.from(clientPublisher).doOnSubscribe(dummy ->
```

### UNCHECKED_WARNING
Unchecked cast: 'org.apache.dubbo.rpc.protocol.tri.observer.CallStreamObserver\>' to 'org.apache.dubbo.rpc.protocol.tri.observer.CallStreamObserver'
in `dubbo-plugin/dubbo-reactive/src/main/java/org/apache/dubbo/reactive/calls/ReactorClientCalls.java`
#### Snippet
```java
            ClientTripleReactorSubscriber<TRequest> clientSubscriber = requestFlux.subscribeWith(new ClientTripleReactorSubscriber<>());
            ClientTripleReactorPublisher<TResponse> clientPublisher = new ClientTripleReactorPublisher<>(
                s -> clientSubscriber.subscribe((CallStreamObserver<TRequest>) s),
                clientSubscriber::cancel);
            return Mono.from(clientPublisher).doOnSubscribe(dummy ->
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Map' to 'java.util.Map'
in `dubbo-serialization/dubbo-serialization-api/src/main/java/org/apache/dubbo/common/serialize/ObjectInput.java`
#### Snippet
```java

    default Map<String, Object> readAttachments() throws IOException, ClassNotFoundException {
        return readObject(Map.class);
    }
}
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.List'
in `dubbo-registry/dubbo-registry-nacos/src/main/java/org/apache/dubbo/registry/nacos/NacosRegistry.java`
#### Snippet
```java
                @Override
                protected void doNotify(Object rawAddresses) {
                    List<Instance> instances = (List<Instance>) rawAddresses;
                    NacosRegistry.this.notifySubscriber(consumerUrl, serviceName, listener, instances);
                }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Map\>'
in `dubbo-plugin/dubbo-qos/src/main/java/org/apache/dubbo/qos/command/impl/GetRouterSnapshot.java`
#### Snippet
```java
                    Map<Registry, MigrationInvoker<?>> invokerMap;
                    if (object instanceof Map) {
                        invokerMap = (Map<Registry, MigrationInvoker<?>>) object;
                        for (Map.Entry<Registry, MigrationInvoker<?>> invokerEntry : invokerMap.entrySet()) {
                            Directory<?> directory = invokerEntry.getValue().getDirectory();
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Map\>'
in `dubbo-plugin/dubbo-qos/src/main/java/org/apache/dubbo/qos/command/util/ServiceCheckUtils.java`
#### Snippet
```java
        List<String> nums = new LinkedList<>();
        if (object instanceof Map) {
            invokerMap = (Map<Registry, MigrationInvoker<?>>) object;
            for (Map.Entry<Registry, MigrationInvoker<?>> entry : invokerMap.entrySet()) {
                URL registryUrl = entry.getKey().getUrl();
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Map\>'
in `dubbo-plugin/dubbo-qos/src/main/java/org/apache/dubbo/qos/command/impl/GetAddress.java`
#### Snippet
```java
        Map<Registry, MigrationInvoker<?>> invokerMap;
        if (object instanceof Map) {
            invokerMap = (Map<Registry, MigrationInvoker<?>>) object;
            for (Map.Entry<Registry, MigrationInvoker<?>> entry : invokerMap.entrySet()) {
                appendInvokers(plainOutput, consumerMap, entry);
```

### UNCHECKED_WARNING
Unchecked method 'createCache(String, C)' invocation
in `dubbo-filter/dubbo-filter-cache/src/main/java/org/apache/dubbo/cache/support/jcache/JCache.java`
#### Snippet
```java
                                .setManagementEnabled(true)
                                .setStatisticsEnabled(true);
                cache = cacheManager.createCache(key, config);
            } catch (CacheException e) {
                // concurrent cache initialization
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/rest/AbstractServiceRestMetadataResolver.java`
#### Snippet
```java

    protected static final <V> V computeIfAbsent(String name, Function<? super String, ? extends V> mappingFunction) {
        return (V) getCache().computeIfAbsent(name, mappingFunction);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/rest/AbstractServiceRestMetadataResolver.java`
#### Snippet
```java
    protected static final <T> T get(String name) throws ClassCastException {
        Map<String, Object> cache = getCache();
        return (T) cache.get(name);
    }

```

### UNCHECKED_WARNING
Unchecked generics array creation for varargs parameter
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/util/FieldUtils.java`
#### Snippet
```java

    static List<VariableElement> getNonStaticFields(TypeMirror type) {
        return getDeclaredFields(type, FieldUtils::isNonStaticField);
    }

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.List' to 'java.util.List'
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/util/FieldUtils.java`
#### Snippet
```java

    static List<VariableElement> getAllDeclaredFields(TypeMirror type) {
        return getAllDeclaredFields(type, EMPTY_ARRAY);
    }

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.function.Predicate\[\]' to 'java.util.function.Predicate...'
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/util/FieldUtils.java`
#### Snippet
```java

    static List<VariableElement> getAllDeclaredFields(TypeMirror type) {
        return getAllDeclaredFields(type, EMPTY_ARRAY);
    }

```

### UNCHECKED_WARNING
Unchecked generics array creation for varargs parameter
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/util/FieldUtils.java`
#### Snippet
```java

    static List<VariableElement> getAllNonStaticFields(TypeMirror type) {
        return getAllDeclaredFields(type, FieldUtils::isNonStaticField);
    }

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.List' to 'java.util.List'
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/util/FieldUtils.java`
#### Snippet
```java

    static List<VariableElement> getAllDeclaredFields(Element element) {
        return getAllDeclaredFields(element, EMPTY_ARRAY);
    }

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.function.Predicate\[\]' to 'java.util.function.Predicate...'
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/util/FieldUtils.java`
#### Snippet
```java

    static List<VariableElement> getAllDeclaredFields(Element element) {
        return getAllDeclaredFields(element, EMPTY_ARRAY);
    }

```

### UNCHECKED_WARNING
Unchecked generics array creation for varargs parameter
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/util/FieldUtils.java`
#### Snippet
```java

    static VariableElement findField(TypeMirror type, String fieldName) {
        return filterFirst(getAllDeclaredFields(type, field -> equals(field, fieldName)));
    }

```

### UNCHECKED_WARNING
Unchecked generics array creation for varargs parameter
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/util/FieldUtils.java`
#### Snippet
```java

    static VariableElement findField(TypeMirror type, String fieldName) {
        return filterFirst(getAllDeclaredFields(type, field -> equals(field, fieldName)));
    }

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.List' to 'java.util.List'
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/util/FieldUtils.java`
#### Snippet
```java

    static List<VariableElement> getDeclaredFields(TypeMirror type) {
        return getDeclaredFields(type, EMPTY_ARRAY);
    }

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.function.Predicate\[\]' to 'java.util.function.Predicate...'
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/util/FieldUtils.java`
#### Snippet
```java

    static List<VariableElement> getDeclaredFields(TypeMirror type) {
        return getDeclaredFields(type, EMPTY_ARRAY);
    }

```

### UNCHECKED_WARNING
Unchecked generics array creation for varargs parameter
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/util/FieldUtils.java`
#### Snippet
```java

    static VariableElement getDeclaredField(TypeMirror type, String fieldName) {
        return filterFirst(getDeclaredFields(type, field -> fieldName.equals(field.getSimpleName().toString())));
    }

```

### UNCHECKED_WARNING
Unchecked generics array creation for varargs parameter
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/util/FieldUtils.java`
#### Snippet
```java

    static VariableElement getDeclaredField(TypeMirror type, String fieldName) {
        return filterFirst(getDeclaredFields(type, field -> fieldName.equals(field.getSimpleName().toString())));
    }

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.List' to 'java.util.List'
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/util/FieldUtils.java`
#### Snippet
```java

    static List<VariableElement> getDeclaredFields(Element element) {
        return getDeclaredFields(element, EMPTY_ARRAY);
    }

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.function.Predicate\[\]' to 'java.util.function.Predicate...'
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/util/FieldUtils.java`
#### Snippet
```java

    static List<VariableElement> getDeclaredFields(Element element) {
        return getDeclaredFields(element, EMPTY_ARRAY);
    }

```

### UNCHECKED_WARNING
Unchecked generics array creation for varargs parameter
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/rest/springmvc/SpringMvcServiceRestMetadataResolver.java`
#### Snippet
```java
    private AnnotationMirror getMappingAnnotation(Element element) {
        return computeIfAbsent(valueOf(element), key ->
            filterFirst(getAllAnnotations(element), annotation -> {
                DeclaredType annotationType = annotation.getAnnotationType();
                // try "@RequestMapping" first
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.List' to 'java.util.List'
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/util/AnnotationUtils.java`
#### Snippet
```java

    static List<AnnotationMirror> getAllAnnotations(Element element) {
        return getAllAnnotations(element, EMPTY_ARRAY);
    }

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.function.Predicate\[\]' to 'java.util.function.Predicate...'
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/util/AnnotationUtils.java`
#### Snippet
```java

    static List<AnnotationMirror> getAllAnnotations(Element element) {
        return getAllAnnotations(element, EMPTY_ARRAY);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.util.List\>' to 'java.util.List'
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/util/AnnotationUtils.java`
#### Snippet
```java
                        .flatMap(Collection::stream)
                        .collect(Collectors.toList()) :
                element == null ? emptyList() : (List<AnnotationMirror>) element.getAnnotationMirrors();

        return filterAll(allAnnotations, annotationFilters);
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.List' to 'java.util.List'
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/util/AnnotationUtils.java`
#### Snippet
```java

    static List<AnnotationMirror> getAllAnnotations(ProcessingEnvironment processingEnv, Type annotatedType) {
        return getAllAnnotations(processingEnv, annotatedType, EMPTY_ARRAY);
    }

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.function.Predicate\[\]' to 'java.util.function.Predicate...'
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/util/AnnotationUtils.java`
#### Snippet
```java

    static List<AnnotationMirror> getAllAnnotations(ProcessingEnvironment processingEnv, Type annotatedType) {
        return getAllAnnotations(processingEnv, annotatedType, EMPTY_ARRAY);
    }

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.List' to 'java.util.List'
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/util/AnnotationUtils.java`
#### Snippet
```java

    static List<AnnotationMirror> getAnnotations(AnnotatedConstruct annotatedConstruct) {
        return getAnnotations(annotatedConstruct, EMPTY_ARRAY);
    }

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.function.Predicate\[\]' to 'java.util.function.Predicate...'
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/util/AnnotationUtils.java`
#### Snippet
```java

    static List<AnnotationMirror> getAnnotations(AnnotatedConstruct annotatedConstruct) {
        return getAnnotations(annotatedConstruct, EMPTY_ARRAY);
    }

```

### UNCHECKED_WARNING
Unchecked generics array creation for varargs parameter
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/util/AnnotationUtils.java`
#### Snippet
```java

    static List<AnnotationMirror> getAllAnnotations(Element element, CharSequence annotationClassName) {
        return getAllAnnotations(element, annotation -> isSameType(annotation.getAnnotationType(), annotationClassName));
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.List'
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/util/AnnotationUtils.java`
#### Snippet
```java
                    String componentType = arrayType.getComponentType().toString();
                    ClassLoader classLoader = AnnotationUtils.class.getClassLoader();
                    List<AnnotationValue> values = (List<AnnotationValue>) value.getValue();
                    int size = values.size();
                    try {
```

### UNCHECKED_WARNING
Unchecked method 'valueOf(Class, String)' invocation
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/util/AnnotationUtils.java`
#### Snippet
```java
                            Object element = values.get(i).getValue();
                            if (isEnum) {
                                element = valueOf(componentClass, element.toString());
                            }
                            Array.set(array, i, element);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/util/AnnotationUtils.java`
#### Snippet
```java
                            Array.set(array, i, element);
                        }
                        annotationValue = (T) array;
                    } catch (ClassNotFoundException e) {
                        throw new RuntimeException(e);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/util/AnnotationUtils.java`
#### Snippet
```java
                    }
                } else {
                    annotationValue = (T) value.getValue();
                }
                break;
```

### UNCHECKED_WARNING
Unchecked generics array creation for varargs parameter
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/util/AnnotationUtils.java`
#### Snippet
```java

    static AnnotationMirror findAnnotation(Element element, CharSequence annotationClassName) {
        return filterFirst(getAllAnnotations(element, annotation -> isSameType(annotation.getAnnotationType(), annotationClassName)));
    }

```

### UNCHECKED_WARNING
Unchecked generics array creation for varargs parameter
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/util/AnnotationUtils.java`
#### Snippet
```java

    static AnnotationMirror findAnnotation(Element element, CharSequence annotationClassName) {
        return filterFirst(getAllAnnotations(element, annotation -> isSameType(annotation.getAnnotationType(), annotationClassName)));
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.util.List\>' to 'java.util.List'
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/util/AnnotationUtils.java`
#### Snippet
```java
        return actualAnnotatedConstruct == null ?
                emptyList() :
                filterAll((List<AnnotationMirror>) actualAnnotatedConstruct.getAnnotationMirrors(), annotationFilters);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/util/AnnotationUtils.java`
#### Snippet
```java

    static <T> T getValue(AnnotationMirror annotation) {
        return (T) getAttribute(annotation, "value");
    }
}
```

### UNCHECKED_WARNING
Unchecked generics array creation for varargs parameter
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/util/AnnotationUtils.java`
#### Snippet
```java
    static List<AnnotationMirror> getAnnotations(AnnotatedConstruct annotatedConstruct,
                                                 CharSequence annotationClassName) {
        return getAnnotations(annotatedConstruct,
                annotation -> isSameType(annotation.getAnnotationType(), annotationClassName));
    }
```

### UNCHECKED_WARNING
Unchecked call to 'build(ProcessingEnvironment, T, Map)' as a member of raw type 'org.apache.dubbo.metadata.annotation.processing.builder.TypeBuilder'
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/builder/TypeDefinitionBuilder.java`
#### Snippet
```java
                .findFirst()
                .map(builder -> {
                    return builder.build(processingEnv, type, typeCache);
                    // typeDefinition.setTypeBuilderName(builder.getClass().getName());
                }).orElse(null);
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.List' to 'java.util.List'
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/util/MethodUtils.java`
#### Snippet
```java

    static List<ExecutableElement> getAllDeclaredMethods(TypeElement type) {
        return getAllDeclaredMethods(type, EMPTY_ARRAY);
    }

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.function.Predicate\[\]' to 'java.util.function.Predicate...'
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/util/MethodUtils.java`
#### Snippet
```java

    static List<ExecutableElement> getAllDeclaredMethods(TypeElement type) {
        return getAllDeclaredMethods(type, EMPTY_ARRAY);
    }

```

### UNCHECKED_WARNING
Unchecked generics array creation for varargs parameter
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/util/MethodUtils.java`
#### Snippet
```java

    static ExecutableElement findMethod(TypeMirror type, String methodName, CharSequence... parameterTypes) {
        return filterFirst(getAllDeclaredMethods(type),
                method -> methodName.equals(method.getSimpleName().toString()),
                method -> matchParameterTypes(method.getParameters(), parameterTypes)
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.List' to 'java.util.List'
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/util/MethodUtils.java`
#### Snippet
```java

    static List<ExecutableElement> getAllDeclaredMethods(TypeMirror type) {
        return getAllDeclaredMethods(type, EMPTY_ARRAY);
    }

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.function.Predicate\[\]' to 'java.util.function.Predicate...'
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/util/MethodUtils.java`
#### Snippet
```java

    static List<ExecutableElement> getAllDeclaredMethods(TypeMirror type) {
        return getAllDeclaredMethods(type, EMPTY_ARRAY);
    }

```

### UNCHECKED_WARNING
Unchecked generics array creation for varargs parameter
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/util/MethodUtils.java`
#### Snippet
```java
        return getHierarchicalTypes(type, excludedTypes)
                .stream()
                .map(t -> getDeclaredMethods(t))
                .flatMap(Collection::stream)
                .collect(Collectors.toList());
```

### UNCHECKED_WARNING
Unchecked generics array creation for varargs parameter
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/util/MethodUtils.java`
#### Snippet
```java
                                               ExecutableElement declaringMethod) {
        Elements elements = processingEnv.getElementUtils();
        return filterFirst(getAllDeclaredMethods(type), method -> elements.overrides(method, declaringMethod, type));
    }

```

### UNCHECKED_WARNING
Unchecked generics array creation for varargs parameter
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/builder/EnumTypeDefinitionBuilder.java`
#### Snippet
```java
    public TypeDefinition build(ProcessingEnvironment processingEnv, DeclaredType type, Map<String, TypeDefinition> typeCache) {
        TypeDefinition typeDefinition = new TypeDefinition(type.toString());
        getDeclaredFields(type, FieldUtils::isEnumMemberField)
                .stream()
                .map(Element::getSimpleName)
```

### UNCHECKED_WARNING
Unchecked generics array creation for varargs parameter
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/util/TypeUtils.java`
#### Snippet
```java
        }
        Set<TypeMirror> allInterfaces = new LinkedHashSet<>();
        getInterfaces(type).forEach(i -> {
            // Add current type's interfaces
            allInterfaces.add(i);
```

### UNCHECKED_WARNING
Unchecked generics array creation for varargs parameter
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/util/TypeUtils.java`
#### Snippet
```java
            allInterfaces.add(i);
            // Add
            allInterfaces.addAll(getAllInterfaces(i));
        });
        // Add all super types' interfaces
```

### UNCHECKED_WARNING
Unchecked generics array creation for varargs parameter
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/util/TypeUtils.java`
#### Snippet
```java
        });
        // Add all super types' interfaces
        getAllSuperTypes(type).forEach(superType -> allInterfaces.addAll(getAllInterfaces(superType)));
        return filterAll(allInterfaces, interfaceFilters);
    }
```

### UNCHECKED_WARNING
Unchecked generics array creation for varargs parameter
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/util/TypeUtils.java`
#### Snippet
```java

        if (includeSuperInterfaces) {
            hierarchicalTypes.addAll(getAllInterfaces(type));
        }

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Set' to 'java.util.Set'
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/util/TypeUtils.java`
#### Snippet
```java

    static Set<TypeElement> getAllSuperTypes(TypeElement type) {
        return getAllSuperTypes(type, EMPTY_ARRAY);
    }

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.function.Predicate\[\]' to 'java.util.function.Predicate...'
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/util/TypeUtils.java`
#### Snippet
```java

    static Set<TypeElement> getAllSuperTypes(TypeElement type) {
        return getAllSuperTypes(type, EMPTY_ARRAY);
    }

```

### UNCHECKED_WARNING
Unchecked generics array creation for varargs parameter
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/util/TypeUtils.java`
#### Snippet
```java
    static Set<DeclaredType> getHierarchicalTypes(TypeMirror type, CharSequence... excludedTypeNames) {
        Set<String> typeNames = of(excludedTypeNames).map(CharSequence::toString).collect(toSet());
        return getHierarchicalTypes(type, t -> !typeNames.contains(t.toString()));
    }

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Set' to 'java.util.Set'
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/util/TypeUtils.java`
#### Snippet
```java

    static Set<DeclaredType> getAllSuperTypes(TypeMirror type) {
        return getAllSuperTypes(type, EMPTY_ARRAY);
    }

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.function.Predicate\[\]' to 'java.util.function.Predicate...'
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/util/TypeUtils.java`
#### Snippet
```java

    static Set<DeclaredType> getAllSuperTypes(TypeMirror type) {
        return getAllSuperTypes(type, EMPTY_ARRAY);
    }

```

### UNCHECKED_WARNING
Unchecked generics array creation for varargs parameter
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/util/TypeUtils.java`
#### Snippet
```java

    static Set<TypeElement> getAllInterfaces(TypeElement type, Predicate<TypeElement>... interfaceFilters) {
        return type == null ? emptySet() : filterAll(ofTypeElements(getAllInterfaces(type.asType())), interfaceFilters);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.util.List\>' to 'java.util.List'
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/util/TypeUtils.java`
#### Snippet
```java

    static List<TypeMirror> getInterfaces(TypeElement type, Predicate<TypeMirror>... interfaceFilters) {
        return type == null ? emptyList() : filterAll((List<TypeMirror>) ofTypeElement(type).getInterfaces(), interfaceFilters);
    }

```

### UNCHECKED_WARNING
Unchecked generics array creation for varargs parameter
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/util/TypeUtils.java`
#### Snippet
```java
                                                  boolean includeSuperTypes,
                                                  boolean includeSuperInterfaces) {
        return ofDeclaredTypes(getHierarchicalTypes(ofTypeElement(type),
                includeSelf,
                includeSuperTypes,
```

### UNCHECKED_WARNING
Unchecked generics array creation for varargs parameter
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/util/TypeUtils.java`
#### Snippet
```java

    static Set<TypeElement> getHierarchicalTypes(TypeElement type) {
        return getHierarchicalTypes(type, true, true, true);
    }

```

### UNCHECKED_WARNING
Unchecked generics array creation for varargs parameter
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/util/TypeUtils.java`
#### Snippet
```java

    static TypeMirror findInterface(TypeMirror type, CharSequence interfaceClassName) {
        return filterFirst(getAllInterfaces(type), t -> isSameType(t, interfaceClassName));
    }

```

### UNCHECKED_WARNING
Unchecked generics array creation for varargs parameter
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/util/TypeUtils.java`
#### Snippet
```java

    static TypeMirror findInterface(TypeMirror type, CharSequence interfaceClassName) {
        return filterFirst(getAllInterfaces(type), t -> isSameType(t, interfaceClassName));
    }

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Set' to 'java.util.Set'
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/util/TypeUtils.java`
#### Snippet
```java

    static Set<DeclaredType> getHierarchicalTypes(TypeMirror type) {
        return getHierarchicalTypes(type, EMPTY_ARRAY);
    }

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.function.Predicate\[\]' to 'java.util.function.Predicate...'
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/util/TypeUtils.java`
#### Snippet
```java

    static Set<DeclaredType> getHierarchicalTypes(TypeMirror type) {
        return getHierarchicalTypes(type, EMPTY_ARRAY);
    }

```

### UNCHECKED_WARNING
Unchecked call to 'FutureAdapter(CompletableFuture)' as a member of raw type 'org.apache.dubbo.rpc.protocol.dubbo.FutureAdapter'
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/FutureContext.java`
#### Snippet
```java
        this.compatibleFuture = compatibleFuture;
        if (compatibleFuture != null) {
            this.setFuture(new FutureAdapter(compatibleFuture));
        }
    }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.apache.dubbo.common.stream.StreamObserver'
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/stub/BiStreamMethodHandler.java`
#### Snippet
```java
    @Override
    public CompletableFuture<StreamObserver<T>> invoke(Object[] arguments) {
        StreamObserver<R> responseObserver = (StreamObserver<R>) arguments[0];
        StreamObserver<T> requestObserver = func.apply(responseObserver);
        return CompletableFuture.completedFuture(requestObserver);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/stub/ServerStreamMethodHandler.java`
#### Snippet
```java
    @Override
    public CompletableFuture<?> invoke(Object[] arguments) {
        T request = (T) arguments[0];
        StreamObserver<R> responseObserver = (StreamObserver<R>) arguments[1];
        func.accept(request, responseObserver);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.apache.dubbo.common.stream.StreamObserver'
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/stub/ServerStreamMethodHandler.java`
#### Snippet
```java
    public CompletableFuture<?> invoke(Object[] arguments) {
        T request = (T) arguments[0];
        StreamObserver<R> responseObserver = (StreamObserver<R>) arguments[1];
        func.accept(request, responseObserver);
        return CompletableFuture.completedFuture(null);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/stub/UnaryStubMethodHandler.java`
#### Snippet
```java
    @Override
    public CompletableFuture<R> invoke(Object[] arguments) {
        T request = (T) arguments[0];
        CompletableFuture<R> future = new CompletableFuture<>();
        StreamObserver<R> responseObserver = new FutureToObserverAdaptor<>(future);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/stub/StubSuppliers.java`
#### Snippet
```java
            }
        }
        return (T) STUB_SUPPLIERS.get(interfaceName).apply(invoker);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.concurrent.CompletableFuture'
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/proxy/AbstractProxyInvoker.java`
#### Snippet
```java
        if (value instanceof CompletableFuture) {
            invocation.put(PROVIDER_ASYNC_KEY, Boolean.TRUE);
            return (CompletableFuture<Object>) value;
        } else if (RpcContext.getServerAttachment().isAsyncStarted()) {
            invocation.put(PROVIDER_ASYNC_KEY, Boolean.TRUE);
```

### UNCHECKED_WARNING
Unchecked cast: 'capture' to 'T'
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/proxy/wrapper/StubProxyFactoryWrapper.java`
#### Snippet
```java
                    try {
                        Constructor<?> constructor = ReflectUtils.findConstructor(stubClass, serviceType);
                        proxy = (T) constructor.newInstance(new Object[]{proxy});
                        //export stub service
                        URLBuilder urlBuilder = URLBuilder.from(url);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/FutureAdapter.java`
#### Snippet
```java
                    this.completeExceptionally(appResponse.getException());
                } else {
                    this.complete((V) appResponse.getValue());
                }
            }
```

### UNCHECKED_WARNING
Unchecked call to 'CompatibleInvoker(Invoker)' as a member of raw type 'com.alibaba.dubbo.rpc.Invoker.CompatibleInvoker'
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/Invocation.java`
#### Snippet
```java
        @Override
        public Invoker<?> getInvoker() {
            return new Invoker.CompatibleInvoker(delegate.getInvoker());
        }

```

### UNCHECKED_WARNING
Unchecked assignment: 'com.alibaba.dubbo.rpc.protocol.dubbo.FutureAdapter' to 'java.util.concurrent.Future'
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/RpcContext.java`
#### Snippet
```java
            return null;
        }
        return new FutureAdapter(completableFuture);
    }

```

### UNCHECKED_WARNING
Unchecked call to 'FutureAdapter(CompletableFuture)' as a member of raw type 'com.alibaba.dubbo.rpc.protocol.dubbo.FutureAdapter'
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/RpcContext.java`
#### Snippet
```java
            return null;
        }
        return new FutureAdapter(completableFuture);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/protocol/dubbo/FutureAdapter.java`
#### Snippet
```java
                        callback.caught(appResponse.getException());
                    } else {
                        callback.done((V) appResponse.getValue());
                    }
                }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'R'
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/stub/StubInvocationUtil.java`
#### Snippet
```java
        try {
            Object res = unaryCall(invoker, method, request);
            responseObserver.onNext((R) res);
        } catch (Exception e) {
            responseObserver.onError(e);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.apache.dubbo.common.stream.StreamObserver'
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/stub/StubInvocationUtil.java`
#### Snippet
```java
    public static <T, R> StreamObserver<T> biOrClientStreamCall(Invoker<?> invoker,
        MethodDescriptor method, StreamObserver<R> responseObserver) {
        return (StreamObserver<T>) call(invoker, method, new Object[]{responseObserver});
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'R'
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/stub/StubInvocationUtil.java`
#### Snippet
```java
    public static <T, R> R unaryCall(Invoker<?> invoker, MethodDescriptor methodDescriptor,
        T request) {
        return (R) call(invoker, methodDescriptor, new Object[]{request});
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'com.google.protobuf.Parser\>' to 'com.google.protobuf.Parser'
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/SingleProtobufUtils.java`
#### Snippet
```java
        SingleMessageMarshaller(T defaultInstance) {
            this.defaultInstance = defaultInstance;
            this.parser = (Parser<T>) defaultInstance.getParserForType();
        }

```

### UNCHECKED_WARNING
Unchecked cast: 'capture' to 'T'
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/SingleProtobufUtils.java`
#### Snippet
```java
        }
        try {
            return (T) getMarshaller(clz).parse(in);
        } catch (InvalidProtocolBufferException e) {
            throw new IOException(e);
```

### UNCHECKED_WARNING
Unchecked cast: 'com.google.protobuf.Message' to 'T'
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/SingleProtobufUtils.java`
#### Snippet
```java

        SingleMessageMarshaller(Class<T> clz) {
            this.defaultInstance = (T) defaultInst(clz);
            this.parser = (Parser<T>) defaultInstance.getParserForType();
        }
```

### UNCHECKED_WARNING
Unchecked cast: 'com.google.protobuf.Parser\>' to 'com.google.protobuf.Parser'
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/SingleProtobufUtils.java`
#### Snippet
```java
        SingleMessageMarshaller(Class<T> clz) {
            this.defaultInstance = (T) defaultInst(clz);
            this.parser = (Parser<T>) defaultInstance.getParserForType();
        }

```

### UNCHECKED_WARNING
Unchecked call to 'SingleMessageMarshaller(Class)' as a member of raw type 'org.apache.dubbo.rpc.protocol.tri.SingleProtobufUtils.SingleMessageMarshaller'
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/SingleProtobufUtils.java`
#### Snippet
```java

    private static SingleMessageMarshaller<?> getMarshaller(Class<?> clz) {
        return ConcurrentHashMapUtils.computeIfAbsent(MARSHALLER_CACHE, clz, k -> new SingleMessageMarshaller(k));
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.apache.dubbo.common.stream.StreamObserver'
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/TripleInvoker.java`
#### Snippet
```java
                                      ClientCall call) {
        RequestMetadata request = createRequest(methodDescriptor, invocation, null);
        StreamObserver<Object> responseObserver = (StreamObserver<Object>) invocation.getArguments()[1];
        final StreamObserver<Object> requestObserver = streamCall(call, request, responseObserver);
        requestObserver.onNext(invocation.getArguments()[0]);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.apache.dubbo.common.stream.StreamObserver'
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/TripleInvoker.java`
#### Snippet
```java
        final AsyncRpcResult result;
        RequestMetadata request = createRequest(methodDescriptor, invocation, null);
        StreamObserver<Object> responseObserver = (StreamObserver<Object>) invocation.getArguments()[0];
        final StreamObserver<Object> requestObserver = streamCall(call, request, responseObserver);
        result = new AsyncRpcResult(
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.apache.dubbo.common.stream.StreamObserver'
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/call/BiStreamServerCallListener.java`
#### Snippet
```java
    @Override
    public void onReturn(Object value) {
        this.requestObserver = (StreamObserver<Object>) value;
    }

```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.dubbo.rpc.cluster.Merger' to 'org.apache.dubbo.rpc.cluster.Merger'
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/merger/MergerFactory.java`
#### Snippet
```java
            merger = ArrayMerger.INSTANCE;
        }
        return merger;
    }

```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.dubbo.rpc.cluster.router.state.StateRouter' to 'org.apache.dubbo.rpc.cluster.router.state.StateRouter'
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/state/CacheableStateRouterFactory.java`
#### Snippet
```java
    @Override
    public <T> StateRouter<T> getRouter(Class<T> interfaceClass, URL url) {
        return ConcurrentHashMapUtils.computeIfAbsent(routerMap, url.getServiceKey(), k -> createRouter(interfaceClass, url));
    }

```

### UNCHECKED_WARNING
Unchecked call to 'BitList(List)' as a member of raw type 'org.apache.dubbo.rpc.cluster.router.state.BitList'
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/state/BitList.java`
#### Snippet
```java
    private final BitSet rootSet;
    private volatile List<E> originList;
    private final static BitList emptyList = new BitList(Collections.emptyList());
    private volatile List<E> tailList = null;

```

### UNCHECKED_WARNING
Unchecked cast: 'org.apache.dubbo.rpc.Invoker\>' to 'org.apache.dubbo.rpc.Invoker'
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/support/AbstractClusterInvoker.java`
#### Snippet
```java
        context.setInvoker(invoker)
            .setConsumerUrl(null != consumerUrl ? consumerUrl : RpcContext.getServiceContext().getConsumerUrl());
        return (Invoker<T>) originInvoker;
    }

```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.dubbo.rpc.Invoker' to 'org.apache.dubbo.rpc.Invoker'
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/support/wrapper/ScopeClusterInvoker.java`
#### Snippet
```java
                    List<Invoker<?>> invokers = new ArrayList<>();
                    invokers.add(invoker);
                    injvmInvoker = Cluster.getCluster(url.getScopeModel(), Cluster.DEFAULT, false).join(new StaticDirectory(url, invokers), true);
                }
            }
```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.dubbo.rpc.cluster.directory.StaticDirectory' to 'org.apache.dubbo.rpc.cluster.Directory'
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/support/wrapper/ScopeClusterInvoker.java`
#### Snippet
```java
                    List<Invoker<?>> invokers = new ArrayList<>();
                    invokers.add(invoker);
                    injvmInvoker = Cluster.getCluster(url.getScopeModel(), Cluster.DEFAULT, false).join(new StaticDirectory(url, invokers), true);
                }
            }
```

### UNCHECKED_WARNING
Unchecked call to 'StaticDirectory(URL, List\>)' as a member of raw type 'org.apache.dubbo.rpc.cluster.directory.StaticDirectory'
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/support/wrapper/ScopeClusterInvoker.java`
#### Snippet
```java
                    List<Invoker<?>> invokers = new ArrayList<>();
                    invokers.add(invoker);
                    injvmInvoker = Cluster.getCluster(url.getScopeModel(), Cluster.DEFAULT, false).join(new StaticDirectory(url, invokers), true);
                }
            }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Map'
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/directory/AbstractDirectory.java`
#### Snippet
```java
        Object referParams = url.getAttribute(REFER_KEY);
        if (referParams instanceof Map) {
            queryMap = (Map<String, String>) referParams;
            this.consumerUrl = (URL) url.getAttribute(CONSUMER_URL_KEY);
        } else {
```

### UNCHECKED_WARNING
Unchecked call to 'addAll(Collection)' as a member of raw type 'java.util.List'
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/exchange/support/MultiMessage.java`
#### Snippet
```java

    public void addMessages(Collection collection) {
        messages.addAll(collection);
    }

```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.List'
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/exchange/support/MultiMessage.java`
#### Snippet
```java

    public void addMessage(Object msg) {
        messages.add(msg);
    }

```

### UNCHECKED_WARNING
Unchecked call to 'handle(REQUEST, RESPONSE)' as a member of raw type 'org.apache.dubbo.remoting.http.HttpHandler'
in `dubbo-remoting/dubbo-remoting-http/src/main/java/org/apache/dubbo/remoting/http/servlet/DispatcherServlet.java`
#### Snippet
```java
            response.sendError(HttpServletResponse.SC_NOT_FOUND, "Service not found.");
        } else {
            handler.handle(request, response);
        }
    }
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.LinkedHashSet' to 'java.util.Set'
in `dubbo-maven-plugin/src/main/java/org/apache/dubbo/maven/plugin/aot/AbstractDependencyFilterMojo.java`
#### Snippet
```java
        throws MojoExecutionException {
        try {
            Set<Artifact> filtered = new LinkedHashSet<>(dependencies);
            filtered.retainAll(getFilters(additionalFilters).filter(dependencies));
            return filtered;
```

### UNCHECKED_WARNING
Unchecked call to 'LinkedHashSet(Collection)' as a member of raw type 'java.util.LinkedHashSet'
in `dubbo-maven-plugin/src/main/java/org/apache/dubbo/maven/plugin/aot/AbstractDependencyFilterMojo.java`
#### Snippet
```java
        throws MojoExecutionException {
        try {
            Set<Artifact> filtered = new LinkedHashSet<>(dependencies);
            filtered.retainAll(getFilters(additionalFilters).filter(dependencies));
            return filtered;
```

## RuleId[id=DataFlowIssue]
### DataFlowIssue
Method invocation `addListener` may produce `NullPointerException`
in `dubbo-metadata/dubbo-metadata-report-zookeeper/src/main/java/org/apache/dubbo/metadata/store/zookeeper/ZookeeperMetadataReport.java`
#### Snippet
```java
            return newMappingListener;
        });
        mappingDataListener.addListener(listener);
        return getAppNames(zkClient.getContent(path));
    }
```

### DataFlowIssue
Method invocation `add` may produce `NullPointerException`
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/utils/SimpleReferenceCache.java`
#### Snippet
```java
        if (proxy == null) {
            List<ReferenceConfigBase<?>> referencesOfType = ConcurrentHashMapUtils.computeIfAbsent(referenceTypeMap, type, _t -> Collections.synchronizedList(new ArrayList<>()));
            referencesOfType.add(rc);
            List<ReferenceConfigBase<?>> referenceConfigList = ConcurrentHashMapUtils.computeIfAbsent(referenceKeyMap, key, _k -> Collections.synchronizedList(new ArrayList<>()));
            referenceConfigList.add(rc);
```

### DataFlowIssue
Method invocation `add` may produce `NullPointerException`
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/utils/SimpleReferenceCache.java`
#### Snippet
```java
            referencesOfType.add(rc);
            List<ReferenceConfigBase<?>> referenceConfigList = ConcurrentHashMapUtils.computeIfAbsent(referenceKeyMap, key, _k -> Collections.synchronizedList(new ArrayList<>()));
            referenceConfigList.add(rc);
            proxy = rc.get();
        }
```

### DataFlowIssue
Method invocation `getDesc` may produce `NullPointerException`
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/deploy/DefaultApplicationDeployer.java`
#### Snippet
```java
                        }
                    }
                    onFailed(getIdentifier() + " found failed module: " + errorModule.getDesc(), error);
                    break;
                case PENDING:
```

### DataFlowIssue
Method invocation `get` may produce `NullPointerException`
in `dubbo-monitor/dubbo-monitor-default/src/main/java/org/apache/dubbo/monitor/dubbo/DubboMonitor.java`
#### Snippet
```java
        StatisticsItem update = new StatisticsItem();
        do {
            current = reference.get();
            if (current == null) {
                update.setItems(success, failure, input, output, elapsed, concurrent, input, output, elapsed, concurrent);
```

### DataFlowIssue
Method invocation `getRemoteAddress` may produce `NullPointerException`
in `dubbo-remoting/dubbo-remoting-netty4/src/main/java/org/apache/dubbo/remoting/transport/netty4/NettyServerHandler.java`
#### Snippet
```java

        if (logger.isInfoEnabled()) {
            logger.info("The connection of " + channel.getRemoteAddress() + " -> " + channel.getLocalAddress() + " is established.");
        }
    }
```

### DataFlowIssue
Variable is already assigned to this value
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/registrycenter/config/ZookeeperConfig.java`
#### Snippet
```java
                port1 = Integer.parseInt(portConfig1);
            } catch (NumberFormatException e) {
                port1 = DEFAULT_CLIENT_PORT_1;
            }
        }
```

### DataFlowIssue
Variable is already assigned to this value
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/registrycenter/config/ZookeeperConfig.java`
#### Snippet
```java
                port2 = Integer.parseInt(portConfig2);
            } catch (NumberFormatException e) {
                port2 = DEFAULT_CLIENT_PORT_2;
            }
        }
```

### DataFlowIssue
Dereference of `parentPath.toFile().listFiles()` may produce `NullPointerException`
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/registrycenter/initializer/UnpackZookeeperInitializer.java`
#### Snippet
```java
            if (!Files.exists(parentPath) ||
                !parentPath.toFile().isDirectory() ||
                parentPath.toFile().listFiles().length != 1) {
                throw new IllegalStateException("There is something wrong in unpacked file!");
            }
```

### DataFlowIssue
Array access `parentPath.toFile().listFiles()[0]` may produce `NullPointerException`
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/registrycenter/initializer/UnpackZookeeperInitializer.java`
#### Snippet
```java
            }
            // rename directory
            File sourceFile = parentPath.toFile().listFiles()[0];
            File targetFile = Paths.get(parentPath.toString(), context.getUnpackedDirectory()).toFile();
            sourceFile.renameTo(targetFile);
```

### DataFlowIssue
Dereference of `zookeeperBin.toFile().listFiles()` may produce `NullPointerException`
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/registrycenter/initializer/UnpackZookeeperInitializer.java`
#### Snippet
```java
            Path zookeeperBin = Paths.get(targetFile.toString(), "bin");
            // update file permission
            for (File file : zookeeperBin.toFile().listFiles()) {
                file.setExecutable(true, false);
                file.setReadable(true, false);
```

### DataFlowIssue
Method invocation `close` may produce `NullPointerException`
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/registrycenter/initializer/ConfigZookeeperInitializer.java`
#### Snippet
```java
            } finally {
                try {
                    oFile.close();
                } catch (IOException e) {
                    throw new DubboTestException("Failed to close file", e);
```

### DataFlowIssue
Method invocation `close` may produce `NullPointerException`
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/registrycenter/initializer/ConfigZookeeperInitializer.java`
#### Snippet
```java
            } finally {
                try {
                    oFile.close();
                } catch (IOException e) {
                    throw new DubboTestException("Failed to close file", e);
```

### DataFlowIssue
Method invocation `add` may produce `NullPointerException`
in `dubbo-xds/src/main/java/org/apache/dubbo/rpc/cluster/router/xds/RdsRouteRuleManager.java`
#### Snippet
```java
            doSubscribeRds(domain);
        }
        listeners.add(listener);

        if (ROUTE_DATA_CACHE.containsKey(domain)) {
```

### DataFlowIssue
Method invocation `add` may produce `NullPointerException`
in `dubbo-xds/src/main/java/org/apache/dubbo/rpc/cluster/router/xds/EdsEndpointManager.java`
#### Snippet
```java
            doSubscribeEds(cluster);
        }
        listeners.add(listener);

        if (ENDPOINT_DATA_CACHE.containsKey(cluster)) {
```

### DataFlowIssue
Argument `valueMap` might be null
in `dubbo-xds/src/main/java/org/apache/dubbo/registry/xds/util/bootstrap/BootstrapperImpl.java`
#### Snippet
```java
                Map<String, ?> valueMap = JsonUtil.getObject(certProvidersBlob, name);
                String pluginName =
                    checkForNull(JsonUtil.getString(valueMap, "plugin_name"), "plugin_name");
                Map<String, ?> config = checkForNull(JsonUtil.getObject(valueMap, "config"), "config");
                CertificateProviderInfoImpl certificateProviderInfo =
```

### DataFlowIssue
Method invocation `add` may produce `NullPointerException`
in `dubbo-xds/src/main/java/org/apache/dubbo/registry/xds/util/protocol/AbstractProtocol.java`
#### Snippet
```java
                writeLock.lock();
                ConcurrentHashMapUtils.computeIfAbsent((ConcurrentHashMap<Set<String>, List<Consumer<Map<String, T>>>>)consumerObserveMap,consumerObserveResourceNames, key -> new ArrayList<>())
                    .add(futureConsumer);
            } finally {
                writeLock.unlock();
```

### DataFlowIssue
Argument `privateKey` might be null
in `dubbo-xds/src/main/java/org/apache/dubbo/registry/xds/istio/IstioCitadelCertificateSigner.java`
#### Snippet
```java
        }

        String privateKeyPem = generatePrivatePemKey(privateKey);
        CertPair certPair = new CertPair(privateKeyPem, publicKeyBuilder.toString(), System.currentTimeMillis(), expireTime);

```

### DataFlowIssue
Dereference of `annotations` may produce `NullPointerException`
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/rest/AbstractServiceRestMetadataResolver.java`
#### Snippet
```java
        }

        for (Annotation annotation : annotations) {
            String annotationType = annotation.annotationType().getName();
            parameterProcessorsMap.getOrDefault(annotationType, emptyList())
```

### DataFlowIssue
Method invocation `deploy` may produce `NullPointerException`
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/RestProtocol.java`
#### Snippet
```java


        server.deploy(serviceRestMetadata, invoker);

        exporter = new AbstractExporter<T>(invoker) {
```

### DataFlowIssue
Method invocation `retain` may produce `NullPointerException`
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/RestProtocol.java`
#### Snippet
```java
            }
        }
        refClient.retain();


```

### DataFlowIssue
Method invocation `getContexts` may produce `NullPointerException`
in `dubbo-test/dubbo-test-spring/src/main/java/org/apache/dubbo/test/spring/context/MockSpringInitCustomizer.java`
#### Snippet
```java
        // check applicationContext
        boolean foundInitContext = false;
        List<DubboSpringInitContext> contexts = mockCustomizer.getContexts();
        for (DubboSpringInitContext initializationContext : contexts) {
            if (initializationContext.getRegistry() == applicationContext.getBeanFactory()) {
```

### DataFlowIssue
Condition `referProxy instanceof DemoService` is redundant and can be replaced with a null check
in `dubbo-test/dubbo-test-spring/src/main/java/org/apache/dubbo/test/spring/SpringJavaConfigBeanTest.java`
#### Snippet
```java

            DemoService referProxy = (DemoService) referenceConfig.get();
            Assertions.assertTrue( referProxy instanceof DemoService);
            String result = referProxy.sayHello("dubbo");
            Assertions.assertEquals("Hello dubbo", result);
```

### DataFlowIssue
Method invocation `addListener` may produce `NullPointerException`
in `dubbo-configcenter/dubbo-configcenter-nacos/src/main/java/org/apache/dubbo/configcenter/support/nacos/NacosDynamicConfiguration.java`
#### Snippet
```java
        String listenerKey = buildListenerKey(key, group);
        NacosConfigListener nacosConfigListener = ConcurrentHashMapUtils.computeIfAbsent(watchListenerMap, listenerKey, k -> createTargetListener(key, group));
        nacosConfigListener.addListener(listener);
        try {
            configService.addListener(key, group, nacosConfigListener);
```

### DataFlowIssue
Method invocation `add` may produce `NullPointerException`
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/FrameworkServiceRepository.java`
#### Snippet
```java
        }
        String keyWithoutGroup = keyWithoutGroup(key);
        ConcurrentHashMapUtils.computeIfAbsent(providersWithoutGroup, keyWithoutGroup, (k) -> new CopyOnWriteArrayList<>()).add(providerModel);
    }

```

### DataFlowIssue
Dereference of `serviceDescriptors` may produce `NullPointerException`
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ModuleServiceRepository.java`
#### Snippet
```java
        List<ServiceDescriptor> serviceDescriptors = ConcurrentHashMapUtils.computeIfAbsent(services, interfaceClazz.getName(),
            k -> new CopyOnWriteArrayList<>());
        synchronized (serviceDescriptors) {
            Optional<ServiceDescriptor> previous = serviceDescriptors.stream()
                .filter(s -> s.getServiceInterfaceClass().equals(interfaceClazz)).findFirst();
```

### DataFlowIssue
Method invocation `add` may produce `NullPointerException`
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ModuleServiceRepository.java`
#### Snippet
```java

    public void registerConsumer(ConsumerModel consumerModel) {
        ConcurrentHashMapUtils.computeIfAbsent(consumers, consumerModel.getServiceKey(), (serviceKey) -> new CopyOnWriteArrayList<>()).add(consumerModel);
    }

```

### DataFlowIssue
Method invocation `addAll` may produce `NullPointerException`
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ModuleServiceRepository.java`
#### Snippet
```java
        List<ConsumerModel> consumerModel = this.consumers.get(serviceKey);
        consumerModel.forEach(c -> c.setServiceKey(newServiceKey));
        ConcurrentHashMapUtils.computeIfAbsent(this.consumers, newServiceKey, (k) -> new CopyOnWriteArrayList<>()).addAll(consumerModel);
        this.consumers.remove(serviceKey);
    }
```

### DataFlowIssue
Dereference of `serviceDescriptors` may produce `NullPointerException`
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ModuleServiceRepository.java`
#### Snippet
```java
            List<ServiceDescriptor> serviceDescriptors = ConcurrentHashMapUtils.computeIfAbsent(services, path,
                _k -> new CopyOnWriteArrayList<>());
            synchronized (serviceDescriptors) {
                Optional<ServiceDescriptor> previous = serviceDescriptors.stream()
                    .filter(s -> s.getServiceInterfaceClass().equals(serviceDescriptor.getServiceInterfaceClass())).findFirst();
```

### DataFlowIssue
Method invocation `put` may produce `NullPointerException`
in `dubbo-common/src/main/java/org/apache/dubbo/common/url/component/ServiceConfigURL.java`
#### Snippet
```java
    private void updateCachedNumber(String method, String key, Number n) {
        Map<String, Number> keyNumber = ConcurrentHashMapUtils.computeIfAbsent(getMethodNumbers(), method, m -> new HashMap<>());
        keyNumber.put(key, n);
    }

```

### DataFlowIssue
Method invocation `put` may produce `NullPointerException`
in `dubbo-common/src/main/java/org/apache/dubbo/common/store/support/SimpleDataStore.java`
#### Snippet
```java
    public void put(String componentName, String key, Object value) {
        Map<String, Object> componentData = ConcurrentHashMapUtils.computeIfAbsent(data, componentName, k -> new ConcurrentHashMap<>());
        componentData.put(key, value);
    }

```

### DataFlowIssue
Method invocation `incrementAndGet` may produce `NullPointerException`
in `dubbo-common/src/main/java/org/apache/dubbo/common/beans/factory/ScopeBeanFactory.java`
#### Snippet
```java

    private int getNextId(Class<?> beanClass) {
        return ConcurrentHashMapUtils.computeIfAbsent(beanNameIdCounterMap, beanClass, key -> new AtomicInteger()).incrementAndGet();
    }

```

### DataFlowIssue
Dereference of `urlParam.VALUE` may produce `NullPointerException`
in `dubbo-common/src/main/java/org/apache/dubbo/common/url/component/URLParam.java`
#### Snippet
```java
        @Override
        public Set<String> keySet() {
            Set<String> set = new LinkedHashSet<>((int) ((urlParam.VALUE.length + urlParam.EXTRA_PARAMS.size()) / 0.75) + 1);
            for (int i = urlParam.KEY.nextSetBit(0); i >= 0; i = urlParam.KEY.nextSetBit(i + 1)) {
                set.add(DynamicParamTable.getKey(i));
```

### DataFlowIssue
Method invocation `size` may produce `NullPointerException`
in `dubbo-common/src/main/java/org/apache/dubbo/common/url/component/URLParam.java`
#### Snippet
```java
        @Override
        public Set<String> keySet() {
            Set<String> set = new LinkedHashSet<>((int) ((urlParam.VALUE.length + urlParam.EXTRA_PARAMS.size()) / 0.75) + 1);
            for (int i = urlParam.KEY.nextSetBit(0); i >= 0; i = urlParam.KEY.nextSetBit(i + 1)) {
                set.add(DynamicParamTable.getKey(i));
```

### DataFlowIssue
Method invocation `nextSetBit` may produce `NullPointerException`
in `dubbo-common/src/main/java/org/apache/dubbo/common/url/component/URLParam.java`
#### Snippet
```java
        public Set<String> keySet() {
            Set<String> set = new LinkedHashSet<>((int) ((urlParam.VALUE.length + urlParam.EXTRA_PARAMS.size()) / 0.75) + 1);
            for (int i = urlParam.KEY.nextSetBit(0); i >= 0; i = urlParam.KEY.nextSetBit(i + 1)) {
                set.add(DynamicParamTable.getKey(i));
            }
```

### DataFlowIssue
Method invocation `cardinality` may produce `NullPointerException`
in `dubbo-common/src/main/java/org/apache/dubbo/common/url/component/URLParam.java`
#### Snippet
```java
        @Override
        public Set<Entry<String, String>> entrySet() {
            Set<Entry<String, String>> set = new LinkedHashSet<>((int) ((urlParam.KEY.cardinality() + urlParam.EXTRA_PARAMS.size()) / 0.75) + 1);
            for (int i = urlParam.KEY.nextSetBit(0); i >= 0; i = urlParam.KEY.nextSetBit(i + 1)) {
                String value;
```

### DataFlowIssue
Method invocation `size` may produce `NullPointerException`
in `dubbo-common/src/main/java/org/apache/dubbo/common/url/component/URLParam.java`
#### Snippet
```java
        @Override
        public Set<Entry<String, String>> entrySet() {
            Set<Entry<String, String>> set = new LinkedHashSet<>((int) ((urlParam.KEY.cardinality() + urlParam.EXTRA_PARAMS.size()) / 0.75) + 1);
            for (int i = urlParam.KEY.nextSetBit(0); i >= 0; i = urlParam.KEY.nextSetBit(i + 1)) {
                String value;
```

### DataFlowIssue
Method invocation `cardinality` may produce `NullPointerException`
in `dubbo-common/src/main/java/org/apache/dubbo/common/url/component/URLParam.java`
#### Snippet
```java
        @Override
        public int size() {
            return urlParam.KEY.cardinality() + urlParam.EXTRA_PARAMS.size();
        }

```

### DataFlowIssue
Method invocation `size` may produce `NullPointerException`
in `dubbo-common/src/main/java/org/apache/dubbo/common/url/component/URLParam.java`
#### Snippet
```java
        @Override
        public int size() {
            return urlParam.KEY.cardinality() + urlParam.EXTRA_PARAMS.size();
        }

```

### DataFlowIssue
Dereference of `urlParam.VALUE` may produce `NullPointerException`
in `dubbo-common/src/main/java/org/apache/dubbo/common/url/component/URLParam.java`
#### Snippet
```java
        @Override
        public Collection<String> values() {
            Set<String> set = new LinkedHashSet<>((int) ((urlParam.VALUE.length + urlParam.EXTRA_PARAMS.size()) / 0.75) + 1);
            for (int i = urlParam.KEY.nextSetBit(0); i >= 0; i = urlParam.KEY.nextSetBit(i + 1)) {
                String value;
```

### DataFlowIssue
Method invocation `size` may produce `NullPointerException`
in `dubbo-common/src/main/java/org/apache/dubbo/common/url/component/URLParam.java`
#### Snippet
```java
        @Override
        public Collection<String> values() {
            Set<String> set = new LinkedHashSet<>((int) ((urlParam.VALUE.length + urlParam.EXTRA_PARAMS.size()) / 0.75) + 1);
            for (int i = urlParam.KEY.nextSetBit(0); i >= 0; i = urlParam.KEY.nextSetBit(i + 1)) {
                String value;
```

### DataFlowIssue
Method invocation `nextSetBit` may produce `NullPointerException`
in `dubbo-common/src/main/java/org/apache/dubbo/common/url/component/URLParam.java`
#### Snippet
```java
        public Collection<String> values() {
            Set<String> set = new LinkedHashSet<>((int) ((urlParam.VALUE.length + urlParam.EXTRA_PARAMS.size()) / 0.75) + 1);
            for (int i = urlParam.KEY.nextSetBit(0); i >= 0; i = urlParam.KEY.nextSetBit(i + 1)) {
                String value;
                int offset = urlParam.keyIndexToOffset(i);
```

### DataFlowIssue
Method invocation `getSuperclass` may produce `NullPointerException`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/TypeUtils.java`
#### Snippet
```java
                        }
                    }
                    Class<?> superClass = rawClass.getSuperclass();
                    if (superClass != null) {
                        actualTypeArguments.addAll(findActualTypeArguments(superClass, interfaceClass));
```

### DataFlowIssue
Method invocation `getTypeParameters` may produce `NullPointerException`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/PojoUtils.java`
#### Snippet
```java
        Map<String, Type> mapGeneric = new HashMap<>(8);
        mapGeneric.putAll(mapParent);
        TypeVariable<? extends Class<?>>[] typeParameters = type.getTypeParameters();
        if (genericType instanceof ParameterizedType && typeParameters.length > 0) {
            ParameterizedType parameterizedType = (ParameterizedType) genericType;
```

### DataFlowIssue
Passing a non-null argument to `Optional`
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/ModuleEnvironment.java`
#### Snippet
```java
            return applicationDelegate.getDynamicConfiguration();
        }
        return Optional.ofNullable(dynamicConfiguration);
    }

```

### DataFlowIssue
Method invocation `size` may produce `NullPointerException`
in `dubbo-common/src/main/java/org/apache/dubbo/common/convert/ConverterUtil.java`
#### Snippet
```java
            .collect(Collectors.toList()));

        return converters.size() > 0 ? converters.get(0) : null;
    }

```

### DataFlowIssue
Method invocation `exists` may produce `NullPointerException`
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/configcenter/file/FileSystemDynamicConfiguration.java`
#### Snippet
```java
        }

        if (directoryPath == null || !rootDirectory.exists()) { // If the directory does not exist
            rootDirectory = new File(DEFAULT_CONFIG_CENTER_DIR_PATH);
        }
```

### DataFlowIssue
Argument `getRootDirectory().listFiles()` might be null
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/configcenter/file/FileSystemDynamicConfiguration.java`
#### Snippet
```java

    public Set<String> getConfigGroups() {
        return Stream.of(getRootDirectory().listFiles())
            .filter(File::isDirectory)
            .map(File::getName)
```

### DataFlowIssue
Method invocation `isShutdown` may produce `NullPointerException`
in `dubbo-common/src/main/java/org/apache/dubbo/common/threadpool/manager/DefaultExecutorRepository.java`
#### Snippet
```java
        ExecutorService executor = ConcurrentHashMapUtils.computeIfAbsent(executors, executorCacheKey, k -> createExecutor(finalUrl));
        // If executor has been shut down, create a new one
        if (executor.isShutdown() || executor.isTerminated()) {
            executors.remove(executorCacheKey);
            executor = createExecutor(url);
```

### DataFlowIssue
Method invocation `remove` may produce `NullPointerException`
in `dubbo-common/src/main/java/org/apache/dubbo/common/threadpool/manager/DefaultExecutorRepository.java`
#### Snippet
```java
        // If executor has been shut down, create a new one
        if (executor.isShutdown() || executor.isTerminated()) {
            executors.remove(executorCacheKey);
            executor = createExecutor(url);
            executors.put(executorCacheKey, executor);
```

### DataFlowIssue
Method invocation `put` may produce `NullPointerException`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/migration/DefaultMigrationAddressComparator.java`
#### Snippet
```java

        if (!newInvoker.hasProxyInvokers()) {
            migrationData.put(OLD_ADDRESS_SIZE, getAddressSize(oldInvoker));
            migrationData.put(NEW_ADDRESS_SIZE, -1);
            logger.info("No " + getInvokerType(newInvoker) + " address available, stop compare.");
```

### DataFlowIssue
Method invocation `put` may produce `NullPointerException`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/migration/DefaultMigrationAddressComparator.java`
#### Snippet
```java
        }
        if (!oldInvoker.hasProxyInvokers()) {
            migrationData.put(OLD_ADDRESS_SIZE, -1);
            migrationData.put(NEW_ADDRESS_SIZE, getAddressSize(newInvoker));
            logger.info("No " + getInvokerType(oldInvoker) + " address available, stop compare.");
```

### DataFlowIssue
Method invocation `put` may produce `NullPointerException`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/migration/DefaultMigrationAddressComparator.java`
#### Snippet
```java
        int oldAddressSize = getAddressSize(oldInvoker);

        migrationData.put(OLD_ADDRESS_SIZE, oldAddressSize);
        migrationData.put(NEW_ADDRESS_SIZE, newAddressSize);

```

### DataFlowIssue
Method invocation `doMigrate` may produce `NullPointerException`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/migration/MigrationRuleListener.java`
#### Snippet
```java
        });

        migrationRuleHandler.doMigrate(rule);
    }

```

### DataFlowIssue
Method invocation `getUrl` may produce `NullPointerException`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/integration/DynamicDirectory.java`
#### Snippet
```java
            // 1-8: Failed to unregister / unsubscribe url on destroy.
            logger.warn(REGISTRY_FAILED_DESTROY_UNREGISTER_URL, "", "",
                "unexpected error when unsubscribe service " + serviceKey + " from registry: " + registry.getUrl(), t);
        }

```

### DataFlowIssue
Argument `is` might be null
in `dubbo-native-plugin/src/main/java/org/apache/dubbo/maven/plugin/DubboNativeCodeGeneratorMojo.java`
#### Snippet
```java
                    File file = new File(directory + File.separator + nativeFile);
                    if (!file.exists()) {
                        FileUtils.copyInputStreamToFile(is, file);
                        log.info("Copy native config file:" + file);
                    } else {
```

### DataFlowIssue
Method invocation `getConfigManager` may produce `NullPointerException`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/ServiceBean.java`
#### Snippet
```java
        //register service bean
        ModuleModel moduleModel = DubboBeanUtils.getModuleModel(applicationContext);
        moduleModel.getConfigManager().addService(this);
        moduleModel.getDeployer().setPending();
    }
```

### DataFlowIssue
Method invocation `setTimestamp` may produce `NullPointerException`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/support/CacheableFailbackRegistry.java`
#### Snippet
```java
        // PathURLAddress if it's using dubbo protocol.
        URLAddress address = ConcurrentHashMapUtils.computeIfAbsent(stringAddress, rawAddress, k -> URLAddress.parse(k, getDefaultURLProtocol(), isEncoded));
        address.setTimestamp(System.currentTimeMillis());

        URLParam param = ConcurrentHashMapUtils.computeIfAbsent(stringParam, rawParams, k -> URLParam.parse(k, isEncoded, extraParameters));
```

### DataFlowIssue
Method invocation `setTimestamp` may produce `NullPointerException`
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/support/CacheableFailbackRegistry.java`
#### Snippet
```java

        URLParam param = ConcurrentHashMapUtils.computeIfAbsent(stringParam, rawParams, k -> URLParam.parse(k, isEncoded, extraParameters));
        param.setTimestamp(System.currentTimeMillis());

        // create service URL using cached address, param, and consumer URL.
```

### DataFlowIssue
Method invocation `getFile` may produce `NullPointerException`
in `dubbo-native-plugin/src/main/java/org/apache/dubbo/maven/plugin/CodeGenerator.java`
#### Snippet
```java
    public static void main(String[] args) {
        URL r = Thread.currentThread().getContextClassLoader().getResource("");
        String targetClassPath = new File(r.getFile()).getAbsolutePath();
        String p = Paths.get(targetClassPath).getParent().getParent().toString() + File.separator + "src" + File.separator + "main" + File.separator + "java";
        execute(p, new SystemStreamLog());
```

### DataFlowIssue
Method invocation `getName` will produce `NullPointerException`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/EnvironmentUtils.java`
#### Snippet
```java
            }
        } else {
            map.put(root + source.getName(), source);
        }
    }
```

### DataFlowIssue
Method invocation `isInterface` may produce `NullPointerException`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/DubboAnnotationUtils.java`
#### Snippet
```java

        Assert.notNull(interfaceClass, "@Service interfaceClass() or interfaceName() or interface class must be present!");
        Assert.isTrue(interfaceClass.isInterface(), "The annotated type must be an interface!");
        return interfaceClass.getName();
    }
```

### DataFlowIssue
Variable is already assigned to this value
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/util/AnnotationUtils.java`
#### Snippet
```java
                    values[i] = resolvePlaceholders(values[i], propertyResolver);
                }
                attributeValue = values;
            }
            actualAttributes.put(attributeName, attributeValue);
```

### DataFlowIssue
Method invocation `getApplicationModel` may produce `NullPointerException`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/context/DubboBootstrapApplicationListener.java`
#### Snippet
```java
        this.applicationContext = applicationContext;
        moduleModel = DubboBeanUtils.getModuleModel(applicationContext);
        this.setBootstrap(DubboBootstrap.getInstance(moduleModel.getApplicationModel()));
        if (shouldInitConfigBeans) {
            checkCallStackAndInit();
```

### DataFlowIssue
Method invocation `getClass` will produce `NullPointerException`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/context/DubboSpringInitializer.java`
#### Snippet
```java
            beanFactory = genericApplicationContext.getBeanFactory();
        } else {
            throw new IllegalStateException("Can not find Spring BeanFactory from registry: " + registry.getClass().getName());
        }
        return beanFactory;
```

### DataFlowIssue
Argument `applicationModel` might be null
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/context/DubboInfraBeanRegisterPostProcessor.java`
#### Snippet
```java

        // Initialize SpringExtensionInjector
        SpringExtensionInjector.get(applicationModel).init(applicationContext);
        SpringExtensionInjector.get(moduleModel).init(applicationContext);
        DubboBeanUtils.getInitializationContext(beanFactory).setApplicationContext(applicationContext);
```

### DataFlowIssue
Argument `moduleModel` might be null
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/context/DubboInfraBeanRegisterPostProcessor.java`
#### Snippet
```java
        // Initialize SpringExtensionInjector
        SpringExtensionInjector.get(applicationModel).init(applicationContext);
        SpringExtensionInjector.get(moduleModel).init(applicationContext);
        DubboBeanUtils.getInitializationContext(beanFactory).setApplicationContext(applicationContext);

```

### DataFlowIssue
Method invocation `setApplicationContext` may produce `NullPointerException`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/context/DubboInfraBeanRegisterPostProcessor.java`
#### Snippet
```java
        SpringExtensionInjector.get(applicationModel).init(applicationContext);
        SpringExtensionInjector.get(moduleModel).init(applicationContext);
        DubboBeanUtils.getInitializationContext(beanFactory).setApplicationContext(applicationContext);

        // Initialize dubbo Environment before ConfigManager
```

### DataFlowIssue
Method invocation `getDeclaringClassName` may produce `NullPointerException`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/ReferenceAnnotationBeanPostProcessor.java`
#### Snippet
```java

        if (beanClass == null) {
            String beanMethodSignature = factoryMethodMetadata.getDeclaringClassName() + "#" + factoryMethodMetadata.getMethodName() + "()";
            throw new BeanCreationException("The ReferenceBean is missing necessary generic type, which returned by the @Bean method of Java-config class. " +
                "The generic type of the returned ReferenceBean must be specified as the referenced interface type, " +
```

### DataFlowIssue
Method invocation `isAnnotated` may produce `NullPointerException`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/ReferenceAnnotationBeanPostProcessor.java`
#### Snippet
```java
        // try all dubbo reference annotation types
        for (Class<? extends Annotation> annotationType : getAnnotationTypes()) {
            if (factoryMethodMetadata.isAnnotated(annotationType.getName())) {
                // Since Spring 5.2
                // return factoryMethodMetadata.getAnnotations().get(annotationType).filterDefaultValues().asMap();
```

### DataFlowIssue
Method invocation `contains` may produce `NullPointerException`
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/reference/ReferenceBeanManager.java`
#### Snippet
```java
    public void registerReferenceKeyAndBeanName(String referenceKey, String referenceBeanNameOrAlias) {
        List<String> list = ConcurrentHashMapUtils.computeIfAbsent(referenceKeyMap, referenceKey, (key) -> new ArrayList<>());
        if (!list.contains(referenceBeanNameOrAlias)) {
            list.add(referenceBeanNameOrAlias);
            // register bean name as alias
```

### DataFlowIssue
Method invocation `equals` may produce `NullPointerException`
in `dubbo-plugin/dubbo-qos/src/main/java/org/apache/dubbo/qos/legacy/TraceTelnetHandler.java`
#### Snippet
```java
        Invoker<?> invoker = null;
        for (Exporter<?> exporter : DubboProtocol.getDubboProtocol().getExporters()) {
            if (service.equals(exporter.getInvoker().getInterface().getSimpleName())
                    || service.equals(exporter.getInvoker().getInterface().getName())
                    || service.equals(exporter.getInvoker().getUrl().getPath())) {
```

### DataFlowIssue
Argument `times` might be null
in `dubbo-plugin/dubbo-qos/src/main/java/org/apache/dubbo/qos/command/impl/CountTelnet.java`
#### Snippet
```java
            return "Illegal times " + times + ", must be integer.";
        }
        final int t = Integer.parseInt(times);
        Invoker<?> invoker = null;
        for (Exporter<?> exporter : dubboProtocol.getExporters()) {
```

### DataFlowIssue
Method invocation `lock` may produce `NullPointerException`
in `dubbo-registry/dubbo-registry-nacos/src/main/java/org/apache/dubbo/registry/nacos/NacosNamingServiceWrapper.java`
#### Snippet
```java

        try {
            instancesInfo.lock();
            if (!instancesInfo.isValid() || instancesInfo.getInstances().isEmpty()) {
                throw new IllegalArgumentException(serviceName + " has not been registered to nacos.");
```

### DataFlowIssue
Method invocation `lock` may produce `NullPointerException`
in `dubbo-registry/dubbo-registry-nacos/src/main/java/org/apache/dubbo/registry/nacos/NacosNamingServiceWrapper.java`
#### Snippet
```java

        try {
            instancesInfo.lock();
            if (!instancesInfo.isValid()) {
                registerInstance(serviceName, group, instance);
```

### DataFlowIssue
Method invocation `subscribe` may produce `NullPointerException`
in `dubbo-registry/dubbo-registry-nacos/src/main/java/org/apache/dubbo/registry/nacos/NacosNamingServiceWrapper.java`
#### Snippet
```java
        SubscribeInfo subscribeInfo = new SubscribeInfo(nacosServiceName, group, eventListener);
        NamingService namingService = ConcurrentHashMapUtils.computeIfAbsent(subscribeStatus, subscribeInfo, info -> nacosConnectionManager.getNamingService());
        accept(() -> namingService.subscribe(nacosServiceName, group, eventListener));
    }

```

### DataFlowIssue
Method invocation `lock` may produce `NullPointerException`
in `dubbo-registry/dubbo-registry-nacos/src/main/java/org/apache/dubbo/registry/nacos/NacosNamingServiceWrapper.java`
#### Snippet
```java

        try {
            instancesInfo.lock();
            Optional<InstanceInfo> optional = instancesInfo.getInstances()
                .stream()
```

### DataFlowIssue
Method invocation `lock` may produce `NullPointerException`
in `dubbo-registry/dubbo-registry-nacos/src/main/java/org/apache/dubbo/registry/nacos/NacosNamingServiceWrapper.java`
#### Snippet
```java

        try {
            instancesInfo.lock();

            List<Instance> instances = instancesInfo.getInstances()
```

### DataFlowIssue
Method invocation `addListener` may produce `NullPointerException`
in `dubbo-configcenter/dubbo-configcenter-zookeeper/src/main/java/org/apache/dubbo/configcenter/support/zookeeper/CacheListener.java`
#### Snippet
```java
        ZookeeperDataListener zookeeperDataListener = ConcurrentHashMapUtils.computeIfAbsent(pathKeyListeners, pathKey,
            _pathKey -> new ZookeeperDataListener(_pathKey, key, group, applicationModel));
        zookeeperDataListener.addListener(configurationListener);
        return zookeeperDataListener;
    }
```

### DataFlowIssue
Condition `exception instanceof AuthenticationException` is redundant and can be replaced with a null check
in `dubbo-plugin/dubbo-spring-security/src/main/java/org/apache/dubbo/spring/security/filter/AuthenticationExceptionTranslatorFilter.java`
#### Snippet
```java

        return result.hasException()
            && (exception instanceof AuthenticationException || exception instanceof AccessDeniedException);

    }
```

### DataFlowIssue
Method invocation `getInterfaces` may produce `NullPointerException`
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/util/TypeUtils.java`
#### Snippet
```java

    static List<TypeMirror> getInterfaces(TypeElement type, Predicate<TypeMirror>... interfaceFilters) {
        return type == null ? emptyList() : filterAll((List<TypeMirror>) ofTypeElement(type).getInterfaces(), interfaceFilters);
    }

```

### DataFlowIssue
Method invocation `getRemoteAddress` may produce `NullPointerException`
in `dubbo-remoting/dubbo-remoting-netty/src/main/java/org/apache/dubbo/remoting/transport/netty/NettyHandler.java`
#### Snippet
```java

        if (logger.isInfoEnabled()) {
            logger.info("The connection between " + channel.getRemoteAddress() + " and " + channel.getLocalAddress() + " is established");
        }
    }
```

### DataFlowIssue
Method invocation `size` may produce `NullPointerException`
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/filter/AccessLogFilter.java`
#### Snippet
```java
        Queue<AccessLogData> logQueue = ConcurrentHashMapUtils.computeIfAbsent(logEntries, accessLog, k -> new ConcurrentLinkedQueue<>());

        if (logQueue.size() < LOG_MAX_BUFFER) {
            logQueue.add(accessLogData);
        } else {
```

### DataFlowIssue
Method invocation `add` may produce `NullPointerException`
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/protocol/PermittedSerializationKeeper.java`
#### Snippet
```java
            Byte id = CodecSupport.getIDByName(serialization);
            if (id != null) {
                set.add(id);
                globalPermittedSerializationIds.add(id);
            }
```

### DataFlowIssue
Method invocation `getName` may produce `NullPointerException`
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/support/MockInvoker.java`
#### Snippet
```java
        }
        if (mockClass == null || !serviceType.isAssignableFrom(mockClass)) {
            throw new IllegalStateException("The mock class " + mockClass.getName() +
                " not implement interface " + serviceType.getName());
        }
```

### DataFlowIssue
Method invocation `record` may produce `NullPointerException`
in `dubbo-metrics/dubbo-metrics-default/src/main/java/org/apache/dubbo/metrics/collector/HistogramMetricsCollector.java`
#### Snippet
```java

            Timer timer = ConcurrentHashMapUtils.computeIfAbsent(rt, metric, k -> metricRegister.register(sample));
            timer.record(responseTime, TimeUnit.MILLISECONDS);
        }
    }
```

### DataFlowIssue
Method invocation `getTags` may produce `NullPointerException`
in `dubbo-metrics/dubbo-metrics-default/src/main/java/org/apache/dubbo/metrics/collector/sample/ThreadPoolMetricsSampler.java`
#### Snippet
```java
        ThreadPoolMetric threadPoolMetric = ConcurrentHashMapUtils.computeIfAbsent(threadPoolMetricMap, name,
            v -> new ThreadPoolMetric(collector.getApplicationName(), name, executor));
        list.add(new GaugeMetricSample<>(MetricsKey.THREAD_POOL_CORE_SIZE, threadPoolMetric.getTags(), THREAD_POOL, threadPoolMetric, ThreadPoolMetric::getCorePoolSize));
        list.add(new GaugeMetricSample<>(MetricsKey.THREAD_POOL_LARGEST_SIZE, threadPoolMetric.getTags(), THREAD_POOL, threadPoolMetric, ThreadPoolMetric::getLargestPoolSize));
        list.add(new GaugeMetricSample<>(MetricsKey.THREAD_POOL_MAX_SIZE, threadPoolMetric.getTags(), THREAD_POOL, threadPoolMetric, ThreadPoolMetric::getMaximumPoolSize));
```

### DataFlowIssue
Method invocation `increment` may produce `NullPointerException`
in `dubbo-metrics/dubbo-metrics-default/src/main/java/org/apache/dubbo/metrics/collector/AggregateMetricsCollector.java`
#### Snippet
```java
        MethodMetric metric = calcWindowCounter(event, MetricsKey.METRIC_REQUESTS);
        TimeWindowCounter qpsCounter = ConcurrentHashMapUtils.computeIfAbsent(qps, metric, methodMetric -> new TimeWindowCounter(bucketNum, timeWindowSeconds));
        qpsCounter.increment();
    }

```

### DataFlowIssue
Method invocation `add` may produce `NullPointerException`
in `dubbo-metrics/dubbo-metrics-default/src/main/java/org/apache/dubbo/metrics/collector/AggregateMetricsCollector.java`
#### Snippet
```java
        long responseTime = event.getTimePair().calc();
        TimeWindowQuantile quantile = ConcurrentHashMapUtils.computeIfAbsent(rt, metric, k -> new TimeWindowQuantile(DEFAULT_COMPRESSION, bucketNum, timeWindowSeconds));
        quantile.add(responseTime);

        TimeWindowAggregator timeWindowAggregator = ConcurrentHashMapUtils.computeIfAbsent(rtAgr, metric, methodMetric -> new TimeWindowAggregator(bucketNum, timeWindowSeconds));
```

### DataFlowIssue
Method invocation `add` may produce `NullPointerException`
in `dubbo-metrics/dubbo-metrics-default/src/main/java/org/apache/dubbo/metrics/collector/AggregateMetricsCollector.java`
#### Snippet
```java

        TimeWindowAggregator timeWindowAggregator = ConcurrentHashMapUtils.computeIfAbsent(rtAgr, metric, methodMetric -> new TimeWindowAggregator(bucketNum, timeWindowSeconds));
        timeWindowAggregator.add(responseTime);
    }

```

### DataFlowIssue
Method invocation `increment` may produce `NullPointerException`
in `dubbo-metrics/dubbo-metrics-default/src/main/java/org/apache/dubbo/metrics/collector/AggregateMetricsCollector.java`
#### Snippet
```java

        TimeWindowCounter windowCounter = ConcurrentHashMapUtils.computeIfAbsent(counter, metric, methodMetric -> new TimeWindowCounter(bucketNum, timeWindowSeconds));
        windowCounter.increment();
        return metric;
    }
```

### DataFlowIssue
Argument `url` might be null
in `dubbo-rpc/dubbo-rpc-dubbo/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/CallbackServiceCodec.java`
#### Snippet
```java
            // one channel can have multiple callback instances, no need to re-export for different instance.
            if (!channel.hasAttribute(cacheKey)) {
                if (!isInstancesOverLimit(channel, url, clazz.getName(), instid, false)) {
                    ModuleModel moduleModel;
                    if (inv.getServiceModel() == null) {
```

### DataFlowIssue
Method invocation `getServiceModel` may produce `NullPointerException`
in `dubbo-rpc/dubbo-rpc-dubbo/src/main/java/org/apache/dubbo/rpc/protocol/dubbo/CallbackServiceCodec.java`
#### Snippet
```java
                if (!isInstancesOverLimit(channel, url, clazz.getName(), instid, false)) {
                    ModuleModel moduleModel;
                    if (inv.getServiceModel() == null) {
                        //TODO should get scope model from url?
                        moduleModel = ApplicationModel.defaultModel().getDefaultModule();
```

### DataFlowIssue
Casting `invocation` to `RpcInvocation` may produce `ClassCastException`
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/filter/support/ConsumerContextFilter.java`
#### Snippet
```java
                    Map<String, Object> selected = supportedSelector.select(invocation, RpcContext.getClientAttachment(), RpcContext.getServerAttachment());
                    if (CollectionUtils.isNotEmptyMap(selected)) {
                        ((RpcInvocation) invocation).addObjectAttachments(selected);
                    }
                }
```

### DataFlowIssue
Casting `invocation` to `RpcInvocation` may produce `ClassCastException`
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/filter/support/ConsumerContextFilter.java`
#### Snippet
```java
                }
            } else {
                ((RpcInvocation) invocation).addObjectAttachments(RpcContext.getServerAttachment().getObjectAttachments());
            }
            Map<String, Object> contextAttachments = RpcContext.getClientAttachment().getObjectAttachments();
```

### DataFlowIssue
Casting `invocation` to `RpcInvocation` may produce `ClassCastException`
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/filter/support/ConsumerContextFilter.java`
#### Snippet
```java
                 * a mistake in most cases (for example, through Filter to RpcContext output traceId and spanId and other information).
                 */
                ((RpcInvocation) invocation).addObjectAttachments(contextAttachments);
            }

```

### DataFlowIssue
Method invocation `add` may produce `NullPointerException`
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/mesh/util/MeshRuleDispatcher.java`
#### Snippet
```java
        }
        ConcurrentHashMapUtils.computeIfAbsent(listenerMap, listener.ruleSuffix(), (k) -> new ConcurrentHashSet<>())
            .add(listener);
    }

```

### DataFlowIssue
Method invocation `getMethod` may produce `NullPointerException`
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/support/MergeableClusterInvoker.java`
#### Snippet
```java
            Method method;
            try {
                method = returnType.getMethod(merger, returnType);
            } catch (NoSuchMethodException | NullPointerException e) {
                throw new RpcException("Can not merge result because missing method [ " + merger + " ] in class [ " +
```

### DataFlowIssue
Method invocation `getCode` may produce `NullPointerException`
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/support/FailoverClusterInvoker.java`
#### Snippet
```java
            }
        }
        throw new RpcException(le.getCode(), "Failed to invoke the method "
                + methodName + " in the service " + getInterface().getName()
                + ". Tried " + len + " times of the providers " + providers
```

### DataFlowIssue
Method invocation `getEstimateResponse` may produce `NullPointerException`
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/loadbalance/ShortestResponseLoadBalance.java`
#### Snippet
```java

            // Calculate the estimated response time from the product of active connections and succeeded average elapsed time.
            long estimateResponse = slideWindowData.getEstimateResponse();
            int afterWarmup = getWeight(invoker, invocation);
            weights[i] = afterWarmup;
```

### DataFlowIssue
Method invocation `getWeight` may produce `NullPointerException`
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/loadbalance/RoundRobinLoadBalance.java`
#### Snippet
```java
            });

            if (weight != weightedRoundRobin.getWeight()) {
                //weight changed
                weightedRoundRobin.setWeight(weight);
```

### DataFlowIssue
Method invocation `size` may produce `NullPointerException`
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/loadbalance/RoundRobinLoadBalance.java`
#### Snippet
```java
            totalWeight += weight;
        }
        if (invokers.size() != map.size()) {
            map.entrySet().removeIf(item -> now - item.getValue().getLastUpdate() > RECYCLE_PERIOD);
        }
```

### DataFlowIssue
Method invocation `connect` may produce `NullPointerException`
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/api/connection/MultiplexProtocolConnectionManager.java`
#### Snippet
```java
    public AbstractConnectionClient connect(URL url, ChannelHandler handler) {
        final ConnectionManager manager = ConcurrentHashMapUtils.computeIfAbsent(protocols, url.getProtocol(), this::createSingleProtocolConnectionManager);
        return manager.connect(url, handler);
    }

```

### DataFlowIssue
Variable is already assigned to this value
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/telnet/support/command/LogTelnetHandler.java`
#### Snippet
```java
                    }
                } else {
                    size = 0;
                    buf.append("\r\nMESSAGE: log file not exists or log appender is console .");
                }
```

### DataFlowIssue
Argument `getBody()` might be null
in `dubbo-remoting/dubbo-remoting-http/src/main/java/org/apache/dubbo/remoting/http/restclient/OKHttpRestClient.java`
#### Snippet
```java
                    @Override
                    public String getMessage() throws IOException {
                        return appendErrorMessage(response.message(), new String(getBody()));
                    }
                });
```

## RuleId[id=DeprecatedIsStillUsed]
### DeprecatedIsStillUsed
Deprecated member 'DubboBootstrapStartStopListener' is still used
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/bootstrap/DubboBootstrapStartStopListener.java`
#### Snippet
```java
@SPI
@Deprecated
public interface DubboBootstrapStartStopListener {

    void onStart(DubboBootstrap bootstrap);
```

### DeprecatedIsStillUsed
Deprecated member 'dispather' is still used
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/bootstrap/builders/ProtocolBuilder.java`
#### Snippet
```java
     */
    @Deprecated
    public ProtocolBuilder dispather(String dispather) {
        this.dispatcher = dispather;
        return getThis();
```

### DeprecatedIsStillUsed
Deprecated member 'MetricsFilter' is still used
in `dubbo-monitor/dubbo-monitor-default/src/main/java/org/apache/dubbo/monitor/dubbo/MetricsFilter.java`
#### Snippet
```java
 */
@Deprecated
public class MetricsFilter implements Filter, ExtensionAccessorAware, ScopeModelAware {

    private static final ErrorTypeAwareLogger logger = LoggerFactory.getErrorTypeAwareLogger(MetricsFilter.class);
```

### DeprecatedIsStillUsed
Deprecated member 'MULTIPLE_CONFIG_PROPERTY_NAME' is still used
in `dubbo-spring-boot/dubbo-spring-boot-compatible/autoconfigure/src/main/java/org/apache/dubbo/spring/boot/util/DubboUtils.java`
#### Snippet
```java
     */
    @Deprecated
    public static final String MULTIPLE_CONFIG_PROPERTY_NAME = "multiple";

    /**
```

### DeprecatedIsStillUsed
Deprecated member 'DEFAULT_MULTIPLE_CONFIG_PROPERTY_VALUE' is still used
in `dubbo-spring-boot/dubbo-spring-boot-compatible/autoconfigure/src/main/java/org/apache/dubbo/spring/boot/util/DubboUtils.java`
#### Snippet
```java
     */
    @Deprecated
    public static final boolean DEFAULT_MULTIPLE_CONFIG_PROPERTY_VALUE = true;

    /**
```

### DeprecatedIsStillUsed
Deprecated member 'MetricsService' is still used
in `dubbo-monitor/dubbo-monitor-api/src/main/java/org/apache/dubbo/monitor/MetricsService.java`
#### Snippet
```java
 */
@Deprecated
public interface MetricsService {
    String getMetricsByGroup(String group);
}
```

### DeprecatedIsStillUsed
Deprecated member 'getServices' is still used
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/ReferenceConfig.java`
#### Snippet
```java
    @Deprecated
    @Parameter(key = SUBSCRIBED_SERVICE_NAMES_KEY)
    public String getServices() {
        return services;
    }
```

### DeprecatedIsStillUsed
Deprecated member 'getSubscribedServices' is still used
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/ReferenceConfig.java`
#### Snippet
```java
    @Deprecated
    @Parameter(excluded = true)
    public Set<String> getSubscribedServices() {
        return splitToSet(getServices(), COMMA_SEPARATOR_CHAR);
    }
```

### DeprecatedIsStillUsed
Deprecated member 'getSubscribeApplications' is still used
in `dubbo-xds/src/main/java/org/apache/dubbo/rpc/cluster/router/xds/XdsRouter.java`
#### Snippet
```java

    @Deprecated
    Set<String> getSubscribeApplications() {
        return subscribeApplications;
    }
```

### DeprecatedIsStillUsed
Deprecated member 'getXdsRouteRuleMap' is still used
in `dubbo-xds/src/main/java/org/apache/dubbo/rpc/cluster/router/xds/XdsRouter.java`
#### Snippet
```java
     */
    @Deprecated
    ConcurrentHashMap<String, List<XdsRouteRule>> getXdsRouteRuleMap() {
        return xdsRouteRuleMap;
    }
```

### DeprecatedIsStillUsed
Deprecated member 'XdsRouter' is still used
in `dubbo-xds/src/main/java/org/apache/dubbo/rpc/cluster/router/xds/XdsRouter.java`
#### Snippet
```java
     * @deprecated only for uts
     */
    protected XdsRouter(URL url, RdsRouteRuleManager rdsRouteRuleManager, EdsEndpointManager edsEndpointManager, boolean isEnable) {
        super(url);
        this.isEnable = isEnable;
```

### DeprecatedIsStillUsed
Deprecated member 'getDestinationSubsetMap' is still used
in `dubbo-xds/src/main/java/org/apache/dubbo/rpc/cluster/router/xds/XdsRouter.java`
#### Snippet
```java
     */
    @Deprecated
    ConcurrentHashMap<String, DestinationSubset<T>> getDestinationSubsetMap() {
        return destinationSubsetMap;
    }
```

### DeprecatedIsStillUsed
Deprecated member 'getInvokerList' is still used
in `dubbo-xds/src/main/java/org/apache/dubbo/rpc/cluster/router/xds/XdsRouter.java`
#### Snippet
```java
     */
    @Deprecated
    BitList<Invoker<T>> getInvokerList() {
        return currentInvokeList;
    }
```

### DeprecatedIsStillUsed
Deprecated member 'getMetadataReportRetry' is still used
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/report/support/AbstractMetadataReport.java`
#### Snippet
```java
     */
    @Deprecated
    protected MetadataReportRetry getMetadataReportRetry() {
        return metadataReportRetry;
    }
```

### DeprecatedIsStillUsed
Deprecated member 'getRetryExecutor' is still used
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/report/support/AbstractMetadataReport.java`
#### Snippet
```java
         */
        @Deprecated
        ScheduledExecutorService getRetryExecutor() {
            return retryExecutor;
        }
```

### DeprecatedIsStillUsed
Deprecated member 'getReportCacheExecutor' is still used
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/report/support/AbstractMetadataReport.java`
#### Snippet
```java
     */
    @Deprecated
    protected ExecutorService getReportCacheExecutor() {
        return reportCacheExecutor;
    }
```

### DeprecatedIsStillUsed
Deprecated member 'ConsumerMethodModel' is still used
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ConsumerMethodModel.java`
#### Snippet
```java
 */
@Deprecated
public class ConsumerMethodModel {
    private final Method method;
    //    private final boolean isCallBack;
```

### DeprecatedIsStillUsed
Deprecated member 'ProviderMethodModel' is still used
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ProviderMethodModel.java`
#### Snippet
```java
 */
@Deprecated
public class ProviderMethodModel {
    private final Method method;
    private final String methodName;
```

### DeprecatedIsStillUsed
Deprecated member 'setModuleEnvironment' is still used
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ModuleModel.java`
#### Snippet
```java
     */
    @Deprecated
    public void setModuleEnvironment(ModuleEnvironment moduleEnvironment) {
        this.moduleEnvironment = moduleEnvironment;
    }
```

### DeprecatedIsStillUsed
Deprecated member 'setConfig' is still used
in `dubbo-common/src/main/java/org/apache/dubbo/rpc/model/ServiceModel.java`
#### Snippet
```java

    @Deprecated
    public void setConfig(AbstractInterfaceConfig config) {
        this.config = config;
    }
```

### DeprecatedIsStillUsed
Deprecated member 'setSupportGson' is still used
in `dubbo-common/src/main/java/org/apache/dubbo/common/json/GsonUtils.java`
#### Snippet
```java
     */
    @Deprecated
    protected static void setSupportGson(Boolean supportGson) {
        GsonUtils.supportGson = supportGson;
    }
```

### DeprecatedIsStillUsed
Deprecated member 'getCacheMap' is still used
in `dubbo-common/src/main/java/org/apache/dubbo/common/cache/FileCacheStoreFactory.java`
#### Snippet
```java
     */
    @Deprecated
    static Map<String, FileCacheStore> getCacheMap() {
        return cacheMap;
    }
```

### DeprecatedIsStillUsed
Deprecated member 'getCacheFilePath' is still used
in `dubbo-common/src/main/java/org/apache/dubbo/common/cache/FileCacheStore.java`
#### Snippet
```java
     */
    @Deprecated
    protected String getCacheFilePath() {
        return cacheFilePath;
    }
```

### DeprecatedIsStillUsed
Deprecated member 'setJson' is still used
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/JsonUtils.java`
#### Snippet
```java
     */
    @Deprecated
    protected static void setJson(JSON json) {
        JsonUtils.json = json;
    }
```

### DeprecatedIsStillUsed
Deprecated member 'loadMigrationRule' is still used
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/Environment.java`
#### Snippet
```java
     */
    @Deprecated
    private void loadMigrationRule() {
        if (Boolean.parseBoolean(System.getProperty(CommonConstants.DUBBO_MIGRATION_FILE_ENABLE, "false"))) {
            String path = System.getProperty(CommonConstants.DUBBO_MIGRATION_KEY);
```

### DeprecatedIsStillUsed
Deprecated member 'setLocalMigrationRule' is still used
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/Environment.java`
#### Snippet
```java
    @Deprecated
    @DisableInject
    public void setLocalMigrationRule(String localMigrationRule) {
        this.localMigrationRule = localMigrationRule;
    }
```

### DeprecatedIsStillUsed
Deprecated member 'NopDynamicConfigurationFactory' is still used
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/configcenter/nop/NopDynamicConfigurationFactory.java`
#### Snippet
```java
 */
@Deprecated
public class NopDynamicConfigurationFactory extends AbstractDynamicConfigurationFactory {

    @Override
```

### DeprecatedIsStillUsed
Deprecated member 'NopDynamicConfiguration' is still used
in `dubbo-common/src/main/java/org/apache/dubbo/common/config/configcenter/nop/NopDynamicConfiguration.java`
#### Snippet
```java
 */
@Deprecated
public class NopDynamicConfiguration implements DynamicConfiguration {

    public NopDynamicConfiguration(URL url) {
```

### DeprecatedIsStillUsed
Deprecated member 'findMethodByMethodName' is still used
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
     */
    @Deprecated
    public static Method findMethodByMethodName(Class<?> clazz, String methodName)
            throws NoSuchMethodException, ClassNotFoundException {
        return findMethodByMethodSignature(clazz, methodName, null);
```

### DeprecatedIsStillUsed
Deprecated member 'findMethodByMethodSignature' is still used
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
     */
    @Deprecated
    public static Method findMethodByMethodSignature(Class<?> clazz, String methodName, String[] parameterTypes)
            throws NoSuchMethodException, ClassNotFoundException {
        String signature = clazz.getName() + "." + methodName;
```

### DeprecatedIsStillUsed
Deprecated member 'ExtensionFactory' is still used
in `dubbo-common/src/main/java/org/apache/dubbo/common/extension/ExtensionFactory.java`
#### Snippet
```java
@Deprecated
@SPI(scope = ExtensionScope.FRAMEWORK)
public interface ExtensionFactory extends ExtensionInjector {

    @Override
```

### DeprecatedIsStillUsed
Deprecated member 'nextScheduledExecutor' is still used
in `dubbo-common/src/main/java/org/apache/dubbo/common/threadpool/manager/ExecutorRepository.java`
#### Snippet
```java
     */
    @Deprecated
    ScheduledExecutorService nextScheduledExecutor();

    /**
```

### DeprecatedIsStillUsed
Deprecated member 'port' is still used
in `dubbo-common/src/main/java/org/apache/dubbo/config/MetricsConfig.java`
#### Snippet
```java
     */
    @Deprecated
    private String port;

    /**
```

### DeprecatedIsStillUsed
Deprecated member 'replaceExtension' is still used
in `dubbo-common/src/main/java/org/apache/dubbo/common/extension/ExtensionLoader.java`
#### Snippet
```java
     */
    @Deprecated
    public void replaceExtension(String name, Class<?> clazz) {
        checkDestroyed();
        getExtensionClasses(); // load classes
```

### DeprecatedIsStillUsed
Deprecated member 'setHighestPriority' is still used
in `dubbo-common/src/main/java/org/apache/dubbo/config/ConfigCenterConfig.java`
#### Snippet
```java

    @Deprecated
    public void setHighestPriority(Boolean highestPriority) {
        this.highestPriority = highestPriority;
    }
```

### DeprecatedIsStillUsed
Deprecated member 'isHighestPriority' is still used
in `dubbo-common/src/main/java/org/apache/dubbo/config/ConfigCenterConfig.java`
#### Snippet
```java
    @Deprecated
    @Parameter(key = CONFIG_ENABLE_KEY)
    public Boolean isHighestPriority() {
        return highestPriority;
    }
```

### DeprecatedIsStillUsed
Deprecated member 'getReferBackground' is still used
in `dubbo-common/src/main/java/org/apache/dubbo/config/ConsumerConfig.java`
#### Snippet
```java
    @Deprecated
    @Parameter(key = REFER_BACKGROUND_KEY, excluded = true)
    public Boolean getReferBackground() {
        return referBackground;
    }
```

### DeprecatedIsStillUsed
Deprecated member 'setReferBackground' is still used
in `dubbo-common/src/main/java/org/apache/dubbo/config/ConsumerConfig.java`
#### Snippet
```java
     */
    @Deprecated
    public void setReferBackground(Boolean referBackground) {
        this.referBackground = referBackground;
    }
```

### DeprecatedIsStillUsed
Deprecated member 'getTransport' is still used
in `dubbo-common/src/main/java/org/apache/dubbo/config/RegistryConfig.java`
#### Snippet
```java
    @Deprecated
    @Parameter(excluded = true, attribute = false)
    public String getTransport() {
        return getTransporter();
    }
```

### DeprecatedIsStillUsed
Deprecated member 'setReferAsync' is still used
in `dubbo-common/src/main/java/org/apache/dubbo/config/AbstractReferenceConfig.java`
#### Snippet
```java

    @Deprecated
    public void setReferAsync(Boolean referAsync) {
        this.referAsync = referAsync;
    }
```

### DeprecatedIsStillUsed
Deprecated member 'getReferAsync' is still used
in `dubbo-common/src/main/java/org/apache/dubbo/config/AbstractReferenceConfig.java`
#### Snippet
```java
    @Deprecated
    @Parameter(key = REFER_ASYNC_KEY)
    public Boolean getReferAsync() {
        return referAsync;
    }
```

### DeprecatedIsStillUsed
Deprecated member 'isInjvm' is still used
in `dubbo-common/src/main/java/org/apache/dubbo/config/AbstractReferenceConfig.java`
#### Snippet
```java
     */
    @Deprecated
    public Boolean isInjvm() {
        return injvm;
    }
```

### DeprecatedIsStillUsed
Deprecated member 'getDispather' is still used
in `dubbo-common/src/main/java/org/apache/dubbo/config/ProtocolConfig.java`
#### Snippet
```java
    @Deprecated
    @Parameter(excluded = true, attribute = false)
    public String getDispather() {
        return getDispatcher();
    }
```

### DeprecatedIsStillUsed
Deprecated member 'getExportBackground' is still used
in `dubbo-common/src/main/java/org/apache/dubbo/config/ProviderConfig.java`
#### Snippet
```java
    @Deprecated
    @Parameter(key = EXPORT_BACKGROUND_KEY, excluded = true)
    public Boolean getExportBackground() {
        return exportBackground;
    }
```

### DeprecatedIsStillUsed
Deprecated member 'getExportThreadNum' is still used
in `dubbo-common/src/main/java/org/apache/dubbo/config/ProviderConfig.java`
#### Snippet
```java
    @Deprecated
    @Parameter(key = EXPORT_THREAD_NUM_KEY, excluded = true)
    public Integer getExportThreadNum() {
        return exportThreadNum;
    }
```

### DeprecatedIsStillUsed
Deprecated member 'setInterfaceClass' is still used
in `dubbo-common/src/main/java/org/apache/dubbo/config/ServiceConfigBase.java`
#### Snippet
```java
     * @deprecated
     */
    public void setInterfaceClass(Class<?> interfaceClass) {
        setInterface(interfaceClass);
    }
```

### DeprecatedIsStillUsed
Deprecated member 'parameters' is still used
in `dubbo-common/src/main/java/org/apache/dubbo/metadata/definition/model/MethodDefinition.java`
#### Snippet
```java
     */
    @Deprecated
    private List<TypeDefinition> parameters;

    private List<String> annotations;
```

### DeprecatedIsStillUsed
Deprecated member 'getExportAsync' is still used
in `dubbo-common/src/main/java/org/apache/dubbo/config/AbstractServiceConfig.java`
#### Snippet
```java
    @Deprecated
    @Parameter(key = EXPORT_ASYNC_KEY)
    public Boolean getExportAsync() {
        return exportAsync;
    }
```

### DeprecatedIsStillUsed
Deprecated member 'setExportAsync' is still used
in `dubbo-common/src/main/java/org/apache/dubbo/config/AbstractServiceConfig.java`
#### Snippet
```java

    @Deprecated
    public void setExportAsync(Boolean exportAsync) {
        this.exportAsync = exportAsync;
    }
```

### DeprecatedIsStillUsed
Deprecated member 'isReliable' is still used
in `dubbo-common/src/main/java/org/apache/dubbo/config/MethodConfig.java`
#### Snippet
```java

    @Deprecated
    public Boolean isReliable() {
        return reliable;
    }
```

### DeprecatedIsStillUsed
Deprecated member 'setStat' is still used
in `dubbo-common/src/main/java/org/apache/dubbo/config/MethodConfig.java`
#### Snippet
```java

    @Deprecated
    public void setStat(Integer stat) {
        this.stat = stat;
    }
```

### DeprecatedIsStillUsed
Deprecated member 'constructMethodConfig' is still used
in `dubbo-common/src/main/java/org/apache/dubbo/config/MethodConfig.java`
#### Snippet
```java
     */
    @Deprecated
    public static List<MethodConfig> constructMethodConfig(Method[] methods) {
        if (methods != null && methods.length != 0) {
            List<MethodConfig> methodConfigs = new ArrayList<>(methods.length);
```

### DeprecatedIsStillUsed
Deprecated member 'setReliable' is still used
in `dubbo-common/src/main/java/org/apache/dubbo/config/MethodConfig.java`
#### Snippet
```java

    @Deprecated
    public void setReliable(Boolean reliable) {
        this.reliable = reliable;
    }
```

### DeprecatedIsStillUsed
Deprecated member 'setRetry' is still used
in `dubbo-common/src/main/java/org/apache/dubbo/config/MethodConfig.java`
#### Snippet
```java

    @Deprecated
    public void setRetry(Boolean retry) {
        this.retry = retry;
    }
```

### DeprecatedIsStillUsed
Deprecated member 'isRetry' is still used
in `dubbo-common/src/main/java/org/apache/dubbo/config/MethodConfig.java`
#### Snippet
```java

    @Deprecated
    public Boolean isRetry() {
        return retry;
    }
```

### DeprecatedIsStillUsed
Deprecated member 'SHUTDOWN_WAIT_SECONDS_KEY' is still used
in `dubbo-common/src/main/java/org/apache/dubbo/common/constants/CommonConstants.java`
#### Snippet
```java

    @Deprecated
    String SHUTDOWN_WAIT_SECONDS_KEY = "dubbo.service.shutdown.wait.seconds";
    String SHUTDOWN_WAIT_KEY = "dubbo.service.shutdown.wait";
    String DUBBO_PROTOCOL = "dubbo";
```

### DeprecatedIsStillUsed
Deprecated member 'setMetadataReportConfig' is still used
in `dubbo-common/src/main/java/org/apache/dubbo/config/AbstractInterfaceConfig.java`
#### Snippet
```java
     */
    @Deprecated
    public void setMetadataReportConfig(MetadataReportConfig metadataReportConfig) {
        this.metadataReportConfig = metadataReportConfig;
        if (metadataReportConfig != null) {
```

### DeprecatedIsStillUsed
Deprecated member 'setConfigCenter' is still used
in `dubbo-common/src/main/java/org/apache/dubbo/config/AbstractInterfaceConfig.java`
#### Snippet
```java
     */
    @Deprecated
    public void setConfigCenter(ConfigCenterConfig configCenter) {
        this.configCenter = configCenter;
        if (configCenter != null) {
```

### DeprecatedIsStillUsed
Deprecated member 'setLocal' is still used
in `dubbo-common/src/main/java/org/apache/dubbo/config/AbstractInterfaceConfig.java`
#### Snippet
```java
     */
    @Deprecated
    public void setLocal(String local) {
        this.local = local;
    }
```

### DeprecatedIsStillUsed
Deprecated member 'appendMetricsCompatible' is still used
in `dubbo-common/src/main/java/org/apache/dubbo/config/AbstractInterfaceConfig.java`
#### Snippet
```java
     */
    @Deprecated
    protected void appendMetricsCompatible(Map<String, String> map) {
        MetricsConfig metricsConfig = getConfigManager().getMetrics().orElse(null);
        if (metricsConfig != null) {
```

### DeprecatedIsStillUsed
Deprecated member 'getConfigCenter' is still used
in `dubbo-common/src/main/java/org/apache/dubbo/config/AbstractInterfaceConfig.java`
#### Snippet
```java
     */
    @Deprecated
    public ConfigCenterConfig getConfigCenter() {
        if (configCenter != null) {
            return configCenter;
```

### DeprecatedIsStillUsed
Deprecated member 'getMetadataReportConfig' is still used
in `dubbo-common/src/main/java/org/apache/dubbo/config/AbstractInterfaceConfig.java`
#### Snippet
```java
     */
    @Deprecated
    public MetadataReportConfig getMetadataReportConfig() {
        if (metadataReportConfig != null) {
            return metadataReportConfig;
```

### DeprecatedIsStillUsed
Deprecated member 'setLocal' is still used
in `dubbo-common/src/main/java/org/apache/dubbo/config/AbstractInterfaceConfig.java`
#### Snippet
```java
     */
    @Deprecated
    public void setLocal(Boolean local) {
        if (local == null) {
            setLocal((String) null);
```

### DeprecatedIsStillUsed
Deprecated member 'getLocal' is still used
in `dubbo-common/src/main/java/org/apache/dubbo/config/AbstractInterfaceConfig.java`
#### Snippet
```java
     */
    @Deprecated
    public String getLocal() {
        return local;
    }
```

### DeprecatedIsStillUsed
Deprecated member 'getSemaphore' is still used
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/support/CacheableFailbackRegistry.java`
#### Snippet
```java
     */
    @Deprecated
    protected Semaphore getSemaphore() {
        return semaphore;
    }
```

### DeprecatedIsStillUsed
Deprecated member 'getAnnotationType' is still used
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/beans/factory/annotation/AbstractAnnotationBeanPostProcessor.java`
#### Snippet
```java
     */
    @Deprecated
    public final Class<? extends Annotation> getAnnotationType() {
        return annotationTypes[0];
    }
```

### DeprecatedIsStillUsed
Deprecated member 'getRegisterStatus' is still used
in `dubbo-registry/dubbo-registry-nacos/src/main/java/org/apache/dubbo/registry/nacos/NacosNamingServiceWrapper.java`
#### Snippet
```java
     */
    @Deprecated
    protected Map<InstanceId, InstancesInfo> getRegisterStatus() {
        return registerStatus;
    }
```

### DeprecatedIsStillUsed
Deprecated member 'NacosNamingServiceWrapper' is still used
in `dubbo-registry/dubbo-registry-nacos/src/main/java/org/apache/dubbo/registry/nacos/NacosNamingServiceWrapper.java`
#### Snippet
```java
     */
    @Deprecated
    protected NacosNamingServiceWrapper(NacosConnectionManager nacosConnectionManager, boolean isSupportBatchRegister, int retryTimes, int sleepMsBetweenRetries) {
        this.nacosConnectionManager = nacosConnectionManager;
        this.isSupportBatchRegister = isSupportBatchRegister;
```

### DeprecatedIsStillUsed
Deprecated member 'NacosConnectionManager' is still used
in `dubbo-registry/dubbo-registry-nacos/src/main/java/org/apache/dubbo/registry/nacos/NacosConnectionManager.java`
#### Snippet
```java
     */
    @Deprecated
    protected NacosConnectionManager(NamingService namingService) {
        this.connectionURL = null;
        this.retryTimes = 0;
```

### DeprecatedIsStillUsed
Deprecated member 'SERVICE_ANNOTATION_TYPE' is still used
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/util/ServiceAnnotationUtils.java`
#### Snippet
```java
     */
    @Deprecated
    String SERVICE_ANNOTATION_TYPE = "org.apache.dubbo.config.annotation.Service";

    /**
```

### DeprecatedIsStillUsed
Deprecated member 'LEGACY_SERVICE_ANNOTATION_TYPE' is still used
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/util/ServiceAnnotationUtils.java`
#### Snippet
```java
     */
    @Deprecated
    String LEGACY_SERVICE_ANNOTATION_TYPE = "com.alibaba.dubbo.config.annotation.Service";

    /**
```

### DeprecatedIsStillUsed
Deprecated member 'setCompatibleFuture' is still used
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/FutureContext.java`
#### Snippet
```java
     */
    @Deprecated
    public void setCompatibleFuture(CompletableFuture<?> compatibleFuture) {
        this.compatibleFuture = compatibleFuture;
        if (compatibleFuture != null) {
```

### DeprecatedIsStillUsed
Deprecated member 'getCompatibleCompletableFuture' is still used
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/FutureContext.java`
#### Snippet
```java
    @Deprecated
    @SuppressWarnings("unchecked")
    public <T> CompletableFuture<T> getCompatibleCompletableFuture() {
        try {
            return (CompletableFuture<T>) compatibleFuture;
```

### DeprecatedIsStillUsed
Deprecated member 'ListenableFilter' is still used
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/ListenableFilter.java`
#### Snippet
```java
 */
@Deprecated
public abstract class ListenableFilter implements Filter {

    protected Listener listener = null;
```

### DeprecatedIsStillUsed
Deprecated member 'invokers' is still used
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcServiceContext.java`
#### Snippet
```java

    @Deprecated
    private List<Invoker<?>> invokers;
    @Deprecated
    private Invoker<?> invoker;
```

### DeprecatedIsStillUsed
Deprecated member 'invocation' is still used
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcServiceContext.java`
#### Snippet
```java
    private Invoker<?> invoker;
    @Deprecated
    private Invocation invocation;

    // now we don't use the 'values' map to hold these objects
```

### DeprecatedIsStillUsed
Deprecated member 'invoker' is still used
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/RpcServiceContext.java`
#### Snippet
```java
    private List<Invoker<?>> invokers;
    @Deprecated
    private Invoker<?> invoker;
    @Deprecated
    private Invocation invocation;
```

### DeprecatedIsStillUsed
Deprecated member 'GenericException' is still used
in `dubbo-rpc/dubbo-rpc-api/src/main/java/com/alibaba/dubbo/rpc/service/GenericException.java`
#### Snippet
```java

@Deprecated
public class GenericException extends RuntimeException {

    private static final long serialVersionUID = -1182299763306599962L;
```

### DeprecatedIsStillUsed
Deprecated member 'protocolBindingRefer' is still used
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/protocol/AbstractProtocol.java`
#### Snippet
```java

    @Deprecated
    protected abstract <T> Invoker<T> protocolBindingRefer(Class<T> type, URL url) throws RpcException;

    public Map<String, Exporter<?>> getExporterMap() {
```

### DeprecatedIsStillUsed
Deprecated member 'Result' is still used
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/Result.java`
#### Snippet
```java

@Deprecated
public interface Result extends org.apache.dubbo.rpc.Result {

    @Override
```

### DeprecatedIsStillUsed
Deprecated member 'RpcContext' is still used
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/RpcContext.java`
#### Snippet
```java

@Deprecated
public class RpcContext {

    public static RpcContext getContext() {
```

### DeprecatedIsStillUsed
Deprecated member 'CacheFactory' is still used
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/cache/CacheFactory.java`
#### Snippet
```java

@Deprecated
public interface CacheFactory extends org.apache.dubbo.cache.CacheFactory {

    Cache getCache(URL url, Invocation invocation);
```

### DeprecatedIsStillUsed
Deprecated member 'AbstractCacheFactory' is still used
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/cache/support/AbstractCacheFactory.java`
#### Snippet
```java

@Deprecated
public abstract class AbstractCacheFactory implements CacheFactory {

    private final ConcurrentMap<String, Cache> caches = new ConcurrentHashMap<String, Cache>();
```

### DeprecatedIsStillUsed
Deprecated member 'Router' is still used
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/cluster/Router.java`
#### Snippet
```java

@Deprecated
public interface Router extends org.apache.dubbo.rpc.cluster.Router{

    @Override
```

### DeprecatedIsStillUsed
Deprecated member 'StatusChecker' is still used
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/common/status/StatusChecker.java`
#### Snippet
```java

@Deprecated
public interface StatusChecker extends org.apache.dubbo.common.status.StatusChecker {

    @Override
```

### DeprecatedIsStillUsed
Deprecated member 'ExtensionFactory' is still used
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/common/extension/ExtensionFactory.java`
#### Snippet
```java
@Deprecated
@SPI(scope = ExtensionScope.FRAMEWORK)
public interface ExtensionFactory extends org.apache.dubbo.common.extension.ExtensionFactory {
}

```

### DeprecatedIsStillUsed
Deprecated member 'Serialization' is still used
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/common/serialize/Serialization.java`
#### Snippet
```java

@Deprecated
public interface Serialization extends org.apache.dubbo.common.serialize.Serialization {

    ObjectOutput serialize(URL url, OutputStream output) throws IOException;
```

### DeprecatedIsStillUsed
Deprecated member 'ObjectInput' is still used
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/common/serialize/ObjectInput.java`
#### Snippet
```java

@Deprecated
public interface ObjectInput extends org.apache.dubbo.common.serialize.ObjectInput {
}

```

### DeprecatedIsStillUsed
Deprecated member 'RpcResult' is still used
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/RpcResult.java`
#### Snippet
```java

@Deprecated
public class RpcResult extends AppResponse implements com.alibaba.dubbo.rpc.Result {

    public RpcResult() {
```

### DeprecatedIsStillUsed
Deprecated member 'ConsumerConfig' is still used
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/config/ConsumerConfig.java`
#### Snippet
```java

@Deprecated
public class ConsumerConfig extends org.apache.dubbo.config.ConsumerConfig {

    public void setApplication(com.alibaba.dubbo.config.ApplicationConfig application) {
```

### DeprecatedIsStillUsed
Deprecated member 'Cache' is still used
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/cache/Cache.java`
#### Snippet
```java

@Deprecated
public interface Cache extends org.apache.dubbo.cache.Cache {
}

```

### DeprecatedIsStillUsed
Deprecated member 'ObjectOutput' is still used
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/common/serialize/ObjectOutput.java`
#### Snippet
```java

@Deprecated
public interface ObjectOutput extends org.apache.dubbo.common.serialize.ObjectOutput {
}

```

### DeprecatedIsStillUsed
Deprecated member 'ReferenceConfig' is still used
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/config/ReferenceConfig.java`
#### Snippet
```java

@Deprecated
public class ReferenceConfig<T> extends org.apache.dubbo.config.ReferenceConfig<T> {

    public ReferenceConfig() {
```

### DeprecatedIsStillUsed
Deprecated member 'ProtocolConfig' is still used
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/config/ProtocolConfig.java`
#### Snippet
```java

@Deprecated
public class ProtocolConfig extends org.apache.dubbo.config.ProtocolConfig {

    public ProtocolConfig() {
```

### DeprecatedIsStillUsed
Deprecated member 'MethodConfig' is still used
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/config/MethodConfig.java`
#### Snippet
```java

@Deprecated
public class MethodConfig extends org.apache.dubbo.config.MethodConfig {
    public void addArgument(com.alibaba.dubbo.config.ArgumentConfig argumentConfig) {
        super.addArgument(argumentConfig);
```

### DeprecatedIsStillUsed
Deprecated member 'ArgumentConfig' is still used
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/config/ArgumentConfig.java`
#### Snippet
```java

@Deprecated
public class ArgumentConfig extends org.apache.dubbo.config.ArgumentConfig {
}

```

### DeprecatedIsStillUsed
Deprecated member 'Filter' is still used
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/rpc/Filter.java`
#### Snippet
```java

@Deprecated
public interface Filter extends org.apache.dubbo.rpc.Filter {

    Result invoke(Invoker<?> invoker, Invocation invocation) throws RpcException;
```

### DeprecatedIsStillUsed
Deprecated member 'ProviderConfig' is still used
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/config/ProviderConfig.java`
#### Snippet
```java

@Deprecated
public class ProviderConfig extends org.apache.dubbo.config.ProviderConfig {
    public void setApplication(com.alibaba.dubbo.config.ApplicationConfig application) {
        super.setApplication(application);
```

### DeprecatedIsStillUsed
Deprecated member 'ServiceConfig' is still used
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/config/ServiceConfig.java`
#### Snippet
```java

@Deprecated
public class ServiceConfig<T> extends org.apache.dubbo.config.ServiceConfig<T> {

    public ServiceConfig() {
```

### DeprecatedIsStillUsed
Deprecated member 'getPushJobExecutor' is still used
in `dubbo-metrics/dubbo-metrics-prometheus/src/main/java/org/apache/dubbo/metrics/prometheus/PrometheusMetricsReporter.java`
#### Snippet
```java
     */
    @Deprecated
    public ScheduledExecutorService getPushJobExecutor() {
        return pushJobExecutor;
    }
```

### DeprecatedIsStillUsed
Deprecated member 'getPrometheusRegistry' is still used
in `dubbo-metrics/dubbo-metrics-prometheus/src/main/java/org/apache/dubbo/metrics/prometheus/PrometheusMetricsReporter.java`
#### Snippet
```java
     */
    @Deprecated
    public PrometheusMeterRegistry getPrometheusRegistry() {
        return prometheusRegistry;
    }
```

### DeprecatedIsStillUsed
Deprecated member 'setCurrentHostname' is still used
in `dubbo-kubernetes/src/main/java/org/apache/dubbo/registry/kubernetes/KubernetesServiceDiscovery.java`
#### Snippet
```java
     */
    @Deprecated
    public void setCurrentHostname(String currentHostname) {
        this.currentHostname = currentHostname;
    }
```

### DeprecatedIsStillUsed
Deprecated member 'setKubernetesClient' is still used
in `dubbo-kubernetes/src/main/java/org/apache/dubbo/registry/kubernetes/KubernetesServiceDiscovery.java`
#### Snippet
```java
     */
    @Deprecated
    public void setKubernetesClient(KubernetesClient kubernetesClient) {
        this.kubernetesClient = kubernetesClient;
    }
```

### DeprecatedIsStillUsed
Deprecated member 'WriteQueue' is still used
in `dubbo-rpc/dubbo-rpc-triple/src/main/java/org/apache/dubbo/rpc/protocol/tri/transport/WriteQueue.java`
#### Snippet
```java

@Deprecated
public class WriteQueue {

    static final int DEQUE_CHUNK_SIZE = 128;
```

### DeprecatedIsStillUsed
Deprecated member 'setHeadStateRouter' is still used
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/SingleRouterChain.java`
#### Snippet
```java
     */
    @Deprecated
    public void setHeadStateRouter(StateRouter<T> headStateRouter) {
        this.headStateRouter = headStateRouter;
    }
```

### DeprecatedIsStillUsed
Deprecated member 'getStateRouters' is still used
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/RouterChain.java`
#### Snippet
```java

    @Deprecated
    public List<StateRouter<T>> getStateRouters() {
        return currentChain.getStateRouters();
    }
```

### DeprecatedIsStillUsed
Deprecated member 'getListenerMap' is still used
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/mesh/util/MeshRuleDispatcher.java`
#### Snippet
```java
     */
    @Deprecated
    public Map<String, Set<MeshRuleListener>> getListenerMap() {
        return listenerMap;
    }
```

### DeprecatedIsStillUsed
Deprecated member 'getAppRuleListeners' is still used
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/mesh/route/MeshRuleManager.java`
#### Snippet
```java
     */
    @Deprecated
    public ConcurrentHashMap<String, MeshAppRuleListener> getAppRuleListeners() {
        return APP_RULE_LISTENERS;
    }
```

### DeprecatedIsStillUsed
Deprecated member 'getMeshRuleCache' is still used
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/mesh/route/MeshRuleRouter.java`
#### Snippet
```java
     */
    @Deprecated
    public MeshRuleCache<T> getMeshRuleCache() {
        return meshRuleCache;
    }
```

### DeprecatedIsStillUsed
Deprecated member 'getInvokerList' is still used
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/mesh/route/MeshRuleRouter.java`
#### Snippet
```java
     */
    @Deprecated
    public BitList<Invoker<T>> getInvokerList() {
        return invokerList;
    }
```

### DeprecatedIsStillUsed
Deprecated member 'getRemoteAppName' is still used
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/mesh/route/MeshRuleRouter.java`
#### Snippet
```java
     */
    @Deprecated
    public Set<String> getRemoteAppName() {
        return remoteAppName;
    }
```

### DeprecatedIsStillUsed
Deprecated member 'getMeshRuleDispatcher' is still used
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/mesh/route/MeshAppRuleListener.java`
#### Snippet
```java
     */
    @Deprecated
    public MeshRuleDispatcher getMeshRuleDispatcher() {
        return meshRuleDispatcher;
    }
```

### DeprecatedIsStillUsed
Deprecated member 'build27xCompatibleClusterInterceptors' is still used
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/support/wrapper/AbstractCluster.java`
#### Snippet
```java

    @Deprecated
    private <T> ClusterInvoker<T> build27xCompatibleClusterInterceptors(AbstractClusterInvoker<T> clusterInvoker, AbstractClusterInvoker<T> last) {
        List<ClusterInterceptor> interceptors = ScopeModelUtil.getApplicationModel(clusterInvoker.getUrl().getScopeModel()).getExtensionLoader(ClusterInterceptor.class).getActivateExtensions();

```

### DeprecatedIsStillUsed
Deprecated member 'configureDeprecated' is still used
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/configurator/AbstractConfigurator.java`
#### Snippet
```java

    @Deprecated
    private URL configureDeprecated(URL url) {
        // If override url has port, means it is a provider address. We want to control a specific provider with this override url, it may take effect on the specific provider instance or on consumers holding this provider instance.
        if (configuratorUrl.getPort() != 0) {
```

### DeprecatedIsStillUsed
Deprecated member 'setHeartbeat' is still used
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/exchange/Response.java`
#### Snippet
```java

    @Deprecated
    public void setHeartbeat(boolean isHeartbeat) {
        if (isHeartbeat) {
            setEvent(HEARTBEAT_EVENT);
```

### DeprecatedIsStillUsed
Deprecated member 'TransportCodec' is still used
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/transport/codec/TransportCodec.java`
#### Snippet
```java
 */
@Deprecated
public class TransportCodec extends AbstractCodec {

    @Override
```

## RuleId[id=RedundantCollectionOperation]
### RedundantCollectionOperation
Unnecessary 'contains()' check
in `dubbo-common/src/main/java/org/apache/dubbo/common/resource/GlobalResourcesRepository.java`
#### Snippet
```java
        if (oneoffDisposables.contains(disposable)) {
            synchronized (this) {
                if (oneoffDisposables.contains(disposable)) {
                    oneoffDisposables.remove(disposable);
                }
```

## RuleId[id=CatchMayIgnoreException]
### CatchMayIgnoreException
Empty `catch` block
in `dubbo-native/src/main/java/org/apache/dubbo/aot/generate/ClassSourceScanner.java`
#### Snippet
```java
                        extensionClasses.put(clazz, getClasses().get(clazz));
                    }
                } catch (Throwable t) {
                }
            }
```

### CatchMayIgnoreException
Empty `catch` block
in `dubbo-native/src/main/java/org/apache/dubbo/aot/generate/ClassSourceScanner.java`
#### Snippet
```java
                }
            }
        } catch (Throwable t) {
        }

```

### CatchMayIgnoreException
'catch' parameter named `ignored` is used
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/deploy/DefaultApplicationDeployer.java`
#### Snippet
```java
            try {
                serviceDiscovery.destroy();
            } catch (Throwable ignored) {
                logger.warn(CONFIG_FAILED_EXECUTE_DESTROY, "", "", ignored.getMessage(), ignored);
            }
```

### CatchMayIgnoreException
Empty `catch` block
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/DubboTestChecker.java`
#### Snippet
```java
        try {
            Thread.sleep(waitMs);
        } catch (InterruptedException e) {
        }

```

### CatchMayIgnoreException
Empty `catch` block
in `dubbo-demo/dubbo-demo-xml/dubbo-demo-xml-consumer/src/main/java/org/apache/dubbo/demo/consumer/Application.java`
#### Snippet
```java
                try {
                    Thread.sleep(1000);
                } catch (InterruptedException e) {
                }
            }
```

### CatchMayIgnoreException
Empty `catch` block
in `dubbo-demo/dubbo-demo-xml/dubbo-demo-xml-consumer/src/main/java/org/apache/dubbo/demo/consumer/Application.java`
#### Snippet
```java
                try {
                    Thread.sleep(1000);
                } catch (InterruptedException e) {
                }
            }
```

### CatchMayIgnoreException
'catch' parameter named `ignored` is used
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/report/support/AbstractMetadataReportFactory.java`
#### Snippet
```java
                try {
                    metadataReport.destroy();
                } catch (Throwable ignored) {
                    // ignored
                    logger.warn(COMMON_UNEXPECTED_EXCEPTION, "", "", ignored.getMessage(), ignored);
```

### CatchMayIgnoreException
Empty `catch` block
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/ReflectUtils.java`
#### Snippet
```java
        try {
            return invoke(object, method, params);
        } catch (Exception e) {

        }
```

### CatchMayIgnoreException
Empty `catch` block
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/ReflectUtils.java`
#### Snippet
```java
        try {
            return findClass(classLoader, name);
        } catch (Exception e) {

        }
```

### CatchMayIgnoreException
Empty `catch` block
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/ReflectUtils.java`
#### Snippet
```java
        try {
            return findClass(name, classLoader);
        } catch (Throwable e) {

        }
```

### CatchMayIgnoreException
Empty `catch` block
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/ReflectUtils.java`
#### Snippet
```java
        try {
            filterConstructMethod(methods, clazz.getDeclaredConstructors());
        } catch (Exception e) {
        }

```

### CatchMayIgnoreException
Empty `catch` block
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/ReflectUtils.java`
#### Snippet
```java
        try {
            filterConstructMethod(methods, clazz.getConstructors());
        } catch (Exception e) {

        }
```

### CatchMayIgnoreException
Empty `catch` block
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/ReflectUtils.java`
#### Snippet
```java
            filterMethod(name, methods, clazz.getDeclaredMethods());

        } catch (Exception e) {

        }
```

### CatchMayIgnoreException
Empty `catch` block
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/ReflectUtils.java`
#### Snippet
```java
        try {
            filterMethod(name, methods, clazz.getMethods());
        } catch (Exception e) {

        }
```

### CatchMayIgnoreException
Empty `catch` block
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/message/codec/MultiValueCodec.java`
#### Snippet
```java
                    String value = values == null ? null : String.valueOf(values.get(0));
                    entry.getValue().set(emptyObject, DataParseUtils.stringTypeConvert(entry.getValue().getType(), value));
                } catch (IllegalAccessException e) {

                }
```

### CatchMayIgnoreException
Empty `catch` block
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/request/NettyRequestFacade.java`
#### Snippet
```java
            try {
                body = IOUtils.toByteArray(new ByteBufInputStream(byteBuf));
            } catch (IOException e) {

            }
```

### CatchMayIgnoreException
Empty `catch` block
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ConfigUtils.java`
#### Snippet
```java
                        try {
                            input.close();
                        } catch (Throwable t) {
                        }
                    }
```

### CatchMayIgnoreException
Empty `catch` block
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/PojoUtils.java`
#### Snippet
```java
                    result.setAccessible(true);
                }
            } catch (NoSuchFieldException e) {
            }
        }
```

### CatchMayIgnoreException
Empty `catch` block
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/MethodUtils.java`
#### Snippet
```java
                method = type.getDeclaredMethod(methodName, parameterTypes);
            }
        } catch (NoSuchMethodException e) {
        }
        return method;
```

### CatchMayIgnoreException
Empty `catch` block
in `dubbo-common/src/main/java/org/apache/dubbo/common/logger/support/FailsafeLogger.java`
#### Snippet
```java
        try {
            logger.warn(appendContextMessage(msg));
        } catch (Throwable t) {
        }
    }
```

### CatchMayIgnoreException
Empty `catch` block
in `dubbo-common/src/main/java/org/apache/dubbo/common/logger/support/FailsafeLogger.java`
#### Snippet
```java
        try {
            logger.error(e);
        } catch (Throwable t) {
        }
    }
```

### CatchMayIgnoreException
Empty `catch` block
in `dubbo-common/src/main/java/org/apache/dubbo/common/logger/support/FailsafeLogger.java`
#### Snippet
```java
        try {
            logger.debug(appendContextMessage(msg));
        } catch (Throwable t) {
        }
    }
```

### CatchMayIgnoreException
Empty `catch` block
in `dubbo-common/src/main/java/org/apache/dubbo/common/logger/support/FailsafeLogger.java`
#### Snippet
```java
        try {
            logger.warn(e);
        } catch (Throwable t) {
        }
    }
```

### CatchMayIgnoreException
Empty `catch` block
in `dubbo-common/src/main/java/org/apache/dubbo/common/logger/support/FailsafeLogger.java`
#### Snippet
```java
        try {
            logger.warn(appendContextMessage(msg), e);
        } catch (Throwable t) {
        }
    }
```

### CatchMayIgnoreException
Empty `catch` block
in `dubbo-common/src/main/java/org/apache/dubbo/common/logger/support/FailsafeLogger.java`
#### Snippet
```java
        try {
            logger.info(e);
        } catch (Throwable t) {
        }
    }
```

### CatchMayIgnoreException
Empty `catch` block
in `dubbo-common/src/main/java/org/apache/dubbo/common/logger/support/FailsafeLogger.java`
#### Snippet
```java
        try {
            logger.error(appendContextMessage(msg), e);
        } catch (Throwable t) {
        }
    }
```

### CatchMayIgnoreException
Empty `catch` block
in `dubbo-common/src/main/java/org/apache/dubbo/common/logger/support/FailsafeLogger.java`
#### Snippet
```java
        try {
            logger.trace(appendContextMessage(msg));
        } catch (Throwable t) {
        }
    }
```

### CatchMayIgnoreException
Empty `catch` block
in `dubbo-common/src/main/java/org/apache/dubbo/common/logger/support/FailsafeLogger.java`
#### Snippet
```java
        try {
            logger.trace(e);
        } catch (Throwable t) {
        }
    }
```

### CatchMayIgnoreException
Empty `catch` block
in `dubbo-common/src/main/java/org/apache/dubbo/common/logger/support/FailsafeLogger.java`
#### Snippet
```java
        try {
            logger.info(appendContextMessage(msg));
        } catch (Throwable t) {
        }
    }
```

### CatchMayIgnoreException
Empty `catch` block
in `dubbo-common/src/main/java/org/apache/dubbo/common/logger/support/FailsafeLogger.java`
#### Snippet
```java
        try {
            logger.error(appendContextMessage(msg));
        } catch (Throwable t) {
        }
    }
```

### CatchMayIgnoreException
Empty `catch` block
in `dubbo-common/src/main/java/org/apache/dubbo/common/logger/support/FailsafeLogger.java`
#### Snippet
```java
        try {
            logger.debug(e);
        } catch (Throwable t) {
        }
    }
```

### CatchMayIgnoreException
Empty `catch` block
in `dubbo-common/src/main/java/org/apache/dubbo/common/logger/support/FailsafeLogger.java`
#### Snippet
```java
        try {
            logger.debug(appendContextMessage(msg), e);
        } catch (Throwable t) {
        }
    }
```

### CatchMayIgnoreException
Empty `catch` block
in `dubbo-common/src/main/java/org/apache/dubbo/common/logger/support/FailsafeLogger.java`
#### Snippet
```java
        try {
            logger.info(appendContextMessage(msg), e);
        } catch (Throwable t) {
        }
    }
```

### CatchMayIgnoreException
Empty `catch` block
in `dubbo-common/src/main/java/org/apache/dubbo/common/logger/support/FailsafeLogger.java`
#### Snippet
```java
        try {
            logger.trace(appendContextMessage(msg), e);
        } catch (Throwable t) {
        }
    }
```

### CatchMayIgnoreException
Empty `catch` block
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
                    .findFirst();
            return descriptor.isPresent();
        } catch (Exception e) {

        }
```

### CatchMayIgnoreException
Empty `catch` block
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
                        return null;
                    }).get();
        } catch (Exception e) {

        }
```

### CatchMayIgnoreException
'catch' parameter named `ignored` is used
in `dubbo-common/src/main/java/org/apache/dubbo/common/threadpool/manager/DefaultExecutorRepository.java`
#### Snippet
```java
                        try {
                            ExecutorUtil.shutdownNow(executor, 100);
                        } catch (Throwable ignored) {
                            // ignored
                            logger.warn(COMMON_UNEXPECTED_EXECUTORS_SHUTDOWN, "", "", ignored.getMessage(), ignored);
```

### CatchMayIgnoreException
'catch' parameter named `ignored` is used
in `dubbo-common/src/main/java/org/apache/dubbo/common/threadpool/manager/DefaultExecutorRepository.java`
#### Snippet
```java
                try {
                    serviceReferExecutor.shutdown();
                } catch (Throwable ignored) {
                    logger.warn(COMMON_UNEXPECTED_EXECUTORS_SHUTDOWN, "", "", ignored.getMessage(), ignored);
                }
```

### CatchMayIgnoreException
'catch' parameter named `ignored` is used
in `dubbo-common/src/main/java/org/apache/dubbo/common/threadpool/manager/DefaultExecutorRepository.java`
#### Snippet
```java
                try {
                    serviceExportExecutor.shutdown();
                } catch (Throwable ignored) {
                    // ignored
                    logger.warn(COMMON_UNEXPECTED_EXECUTORS_SHUTDOWN, "", "", ignored.getMessage(), ignored);
```

### CatchMayIgnoreException
Empty `catch` block
in `dubbo-common/src/main/java/org/apache/dubbo/common/extension/ExtensionLoader.java`
#### Snippet
```java
            ClassUtils.forName(oldType);
            loadDirectoryInternal(extensionClasses, strategy, oldType);
        } catch (ClassNotFoundException classNotFoundException) {

        }
```

### CatchMayIgnoreException
Empty `catch` block
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/AbstractServiceDiscovery.java`
#### Snippet
```java
                    try {
                        Thread.sleep(1000);
                    } catch (InterruptedException e) {
                    }
                }
```

### CatchMayIgnoreException
Empty `catch` block
in `dubbo-native-plugin/src/main/java/org/apache/dubbo/maven/plugin/CodeGenerator.java`
#### Snippet
```java
            try {
                return Class.forName(it);
            } catch (Throwable e) {
            }
            return null;
```

### CatchMayIgnoreException
Empty `catch` block
in `dubbo-plugin/dubbo-spring-security/src/main/java/org/apache/dubbo/spring/security/jackson/ObjectMapperCodec.java`
#### Snippet
```java
                mapper.registerModule(objectMapperModule);

            } catch (Throwable ex) {
            }
        }
```

### CatchMayIgnoreException
Empty `catch` block
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/ClassPathMetadataStorage.java`
#### Snippet
```java
            try {
                return fileObject.openReader(false);
            } catch (IOException e) {
            }
            return null;
```

### CatchMayIgnoreException
Empty `catch` block
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/container/page/pages/LogPageHandler.java`
#### Snippet
```java
                modified = new SimpleDateFormat("yyyy-MM-dd HH:mm:ss")
                        .format(new Date(file.lastModified()));
            } catch (IOException e) {
            }
        }
```

### CatchMayIgnoreException
Empty `catch` block
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/container/page/pages/LogPageHandler.java`
#### Snippet
```java
                }
            }
        } catch (Throwable t) {
        }
    }
```

### CatchMayIgnoreException
Empty `catch` block
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/container/page/ResourceFilter.java`
#### Snippet
```java
                    return new URL(path).openStream();
                }
            } catch (IOException e) {
            }
        }
```

### CatchMayIgnoreException
Empty `catch` block
in `dubbo-registry/dubbo-registry-multicast/src/main/java/org/apache/dubbo/registry/multicast/MulticastRegistry.java`
#### Snippet
```java
            try {
                Thread.sleep(100);
            } catch (Throwable e2) {
            }
            try (Socket socket2 = new Socket(url.getHost(), url.getPort())) {
```

### CatchMayIgnoreException
Empty `catch` block
in `dubbo-registry/dubbo-registry-multicast/src/main/java/org/apache/dubbo/registry/multicast/MulticastRegistry.java`
#### Snippet
```java
            try {
                listener.wait(url.getParameter(TIMEOUT_KEY, DEFAULT_TIMEOUT));
            } catch (InterruptedException e) {
            }
        }
```

### CatchMayIgnoreException
Empty `catch` block
in `dubbo-remoting/dubbo-remoting-http/src/main/java/org/apache/dubbo/remoting/http/restclient/URLConnectionRestClient.java`
#### Snippet
```java
                try {
                    out.close();
                } catch (IOException suppressed) {
                }
            }
```

### CatchMayIgnoreException
Empty `catch` block
in `dubbo-remoting/dubbo-remoting-http/src/main/java/org/apache/dubbo/remoting/http/restclient/HttpClientRestClient.java`
#### Snippet
```java
        try {
            closeableHttpClient.close();
        } catch (IOException e) {

        }
```

## RuleId[id=IOStreamConstructor]
### IOStreamConstructor
'InputStream' can be constructed using 'Files.newInputStream()'
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/report/support/AbstractMetadataReport.java`
#### Snippet
```java
    void loadProperties() {
        if (file != null && file.exists()) {
            try (InputStream in = new FileInputStream(file)) {
                properties.load(in);
                if (logger.isInfoEnabled()) {
```

### IOStreamConstructor
'InputStream' can be constructed using 'Files.newInputStream()'
in `dubbo-common/src/main/java/org/apache/dubbo/common/io/Bytes.java`
#### Snippet
```java
     */
    public static byte[] getMD5(File file) throws IOException {
        InputStream is = new FileInputStream(file);
        try {
            return getMD5(is);
```

### IOStreamConstructor
'OutputStream' can be constructed using 'Files.newOutputStream()'
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/IOUtils.java`
#### Snippet
```java
            throw new IOException("File is null.");
        }
        writeLines(new FileOutputStream(file), lines);
    }

```

### IOStreamConstructor
'InputStream' can be constructed using 'Files.newInputStream()'
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/IOUtils.java`
#### Snippet
```java
        }

        return readLines(new FileInputStream(file));
    }

```

### IOStreamConstructor
'InputStream' can be constructed using 'Files.newInputStream()'
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/container/page/ResourceFilter.java`
#### Snippet
```java
            try {
                if (isFile(path)) {
                    return new FileInputStream(path);
                } else if (path.startsWith(CLASSPATH_PREFIX)) {
                    return Thread.currentThread().getContextClassLoader().getResourceAsStream(path.substring(CLASSPATH_PREFIX.length()));
```

## RuleId[id=CollectionAddAllCanBeReplacedWithConstructor]
### CollectionAddAllCanBeReplacedWithConstructor
'addAll()' call can be replaced with parametrized constructor call
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/AnnotationUtils.java`
#### Snippet
```java
        List<Annotation> metaAnnotations = getMetaAnnotations(annotationType);

        allMetaAnnotations.addAll(metaAnnotations);

        for (Annotation metaAnnotation : metaAnnotations) {
```

### CollectionAddAllCanBeReplacedWithConstructor
'putAll()' call can be replaced with parametrized constructor call
in `dubbo-common/src/main/java/org/apache/dubbo/config/MetadataReportConfig.java`
#### Snippet
```java
        // Issue : https://github.com/apache/dubbo/issues/6491
        // Append the parameters from address
        map.putAll(url.getParameters());
        // Append or overrides the properties as parameters
        appendParameters(map, this);
```

### CollectionAddAllCanBeReplacedWithConstructor
'putAll()' call can be replaced with parametrized constructor call
in `dubbo-common/src/main/java/org/apache/dubbo/common/extension/ExtensionLoader.java`
#### Snippet
```java
        Properties properties = loadProperties(ExtensionLoader.class.getClassLoader(),
            SPECIAL_SPI_PROPERTIES);
        map.putAll(properties);
        return map;
    }
```

### CollectionAddAllCanBeReplacedWithConstructor
'addAll()' call can be replaced with parametrized constructor call
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/integration/RegistryDirectory.java`
#### Snippet
```java
            } else {
                localCachedInvokerUrls = new HashSet<>();
                localCachedInvokerUrls.addAll(invokerUrls);//Cached invoker urls, convenient for comparison
                this.cachedInvokerUrls = localCachedInvokerUrls;
            }
```

### CollectionAddAllCanBeReplacedWithConstructor
'addAll()' call can be replaced with parametrized constructor call
in `dubbo-config/dubbo-config-spring/src/main/java/org/apache/dubbo/config/spring/context/annotation/DubboComponentScanRegistrar.java`
#### Snippet
```java
        // basePackages
        String[] basePackages = attributes.getStringArray(basePackagesName);
        packagesToScan.addAll(Arrays.asList(basePackages));
        // basePackageClasses
        Class<?>[] basePackageClasses = attributes.getClassArray(basePackageClassesName);
```

### CollectionAddAllCanBeReplacedWithConstructor
'addAll()' call can be replaced with parametrized constructor call
in `dubbo-registry/dubbo-registry-nacos/src/main/java/org/apache/dubbo/registry/nacos/NacosRegistry.java`
#### Snippet
```java
        try {
            Set<String> serviceNames = new LinkedHashSet<>();
            serviceNames.addAll(namingService.getServicesOfServer(1, Integer.MAX_VALUE,
                getUrl().getGroup(Constants.DEFAULT_GROUP)).getData()
                .stream()
```

### CollectionAddAllCanBeReplacedWithConstructor
'addAll()' call can be replaced with parametrized constructor call
in `dubbo-registry/dubbo-registry-nacos/src/main/java/org/apache/dubbo/registry/nacos/NacosRegistry.java`
#### Snippet
```java
                for (String serviceName : serviceNames) {
                    List<Instance> instances = new LinkedList<>();
                    instances.addAll(namingService.getAllInstances(serviceName
                        , getUrl().getGroup(Constants.DEFAULT_GROUP)));
                    String serviceInterface = serviceName;
```

### CollectionAddAllCanBeReplacedWithConstructor
'addAll()' call can be replaced with parametrized constructor call
in `dubbo-registry/dubbo-registry-nacos/src/main/java/org/apache/dubbo/registry/nacos/NacosRegistry.java`
#### Snippet
```java
            List<String> firstPageData = listView.getData();
            // Append first page into list
            serviceNames.addAll(firstPageData);
            // the total count
            int count = listView.getCount();
```

## RuleId[id=UnnecessaryToStringCall]
### UnnecessaryToStringCall
Unnecessary `toString()` call
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/deploy/DefaultModuleDeployer.java`
#### Snippet
```java
            exportFuture.get();
        } catch (Throwable e) {
            logger.warn(CONFIG_FAILED_EXPORT_SERVICE, "", "", getIdentifier() + " export services occurred an exception: " + e.toString());
        } finally {
            logger.info(getIdentifier() + " export services finished.");
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/deploy/DefaultModuleDeployer.java`
#### Snippet
```java
            referFuture.get();
        } catch (Throwable e) {
            logger.warn(CONFIG_FAILED_REFER_SERVICE, "", "", getIdentifier() + " refer services occurred an exception: " + e.toString());
        } finally {
            logger.info(getIdentifier() + " refer services finished.");
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/registrycenter/initializer/UnpackZookeeperInitializer.java`
#### Snippet
```java
        // check if it's unpacked.
        if (targetPath.toFile() != null && targetPath.toFile().isDirectory()) {
            logger.info(String.format("The file has been unpacked, target path:%s", targetPath.toString()));
            return;
        }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/registrycenter/initializer/UnpackZookeeperInitializer.java`
#### Snippet
```java
            if (!Files.exists(targetFile.toPath()) || !targetFile.isDirectory()) {
                throw new IllegalStateException(String.format("Failed to rename the directory. source directory: %s, target directory: %s",
                    sourceFile.toPath().toString(),
                    targetFile.toPath().toString()));
            }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/registrycenter/initializer/UnpackZookeeperInitializer.java`
#### Snippet
```java
                throw new IllegalStateException(String.format("Failed to rename the directory. source directory: %s, target directory: %s",
                    sourceFile.toPath().toString(),
                    targetFile.toPath().toString()));
            }
            // get the bin path
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `dubbo-test/dubbo-test-check/src/main/java/org/apache/dubbo/test/check/registrycenter/initializer/DownloadZookeeperInitializer.java`
#### Snippet
```java
            logger.info("It is beginning to download the zookeeper binary archive, it will take several minutes..." +
                "\nThe zookeeper binary archive file will be download from " + zookeeperBinaryUrl + "," +
                "\nwhich will be saved in " + temporaryFilePath.toString() + "," +
                "\nalso it will be renamed to 'apache-zookeeper-bin.tar.gz' and moved into " + context.getSourceFile() + ".\n");
            this.download(zookeeperBinaryUrl, temporaryFilePath);
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `dubbo-registry/dubbo-registry-zookeeper/src/main/java/org/apache/dubbo/registry/zookeeper/ZookeeperServiceDiscovery.java`
#### Snippet
```java
            serviceDiscovery.updateService(newInstance);
        } catch (Exception e) {
            throw new RpcException(REGISTRY_EXCEPTION, "Failed register instance " + newServiceInstance.toString(), e);
        }
    }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `dubbo-metadata/dubbo-metadata-definition-protobuf/src/main/java/org/apache/dubbo/metadata/definition/protobuf/ProtobufTypeBuilder.java`
#### Snippet
```java
        if (!matcher.matches()) {
            throw new IllegalArgumentException("Map protobuf property " + fieldName + "of Type " +
                    typeName + " can't be parsed.The type name should mathch[" + MAP_PATTERN.toString() + "].");
        }
    }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/UrlUtils.java`
#### Snippet
```java
                    backup.append(addresses[i]);
                }
                url += URL_PARAM_STARTING_SYMBOL + RemotingConstants.BACKUP_KEY + "=" + backup.toString();
            }
        }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
            while (c.isArray());

            return c.getName() + sb.toString();
        }
        return c.getName();
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `dubbo-common/src/main/java/org/apache/dubbo/common/extension/ExtensionLoader.java`
#### Snippet
```java
                        createAdaptiveInstanceError = t;
                        throw new IllegalStateException(
                            "Failed to create adaptive instance: " + t.toString(), t);
                    }
                }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/InstanceAddressURL.java`
#### Snippet
```java
        String protocolServiceKey = getProtocolServiceKey();
        if (StringUtils.isNotEmpty(protocolServiceKey)) {
            return instance.toString() + ", " + metadataInfo.getServiceString(protocolServiceKey);
        }

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/integration/RegistryDirectory.java`
#### Snippet
```java
                    new IllegalStateException(
                        "urls to invokers error. invokerUrls.size :" +
                            invokerUrls.size() + ", invoker.size :0. urls :" + invokerUrls.toString()));

                return;
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `dubbo-registry/dubbo-registry-nacos/src/main/java/org/apache/dubbo/registry/nacos/NacosServiceDiscovery.java`
#### Snippet
```java
            });
        } catch (Exception e) {
            throw new RpcException(REGISTRY_EXCEPTION, "Failed register instance " + newServiceInstance.toString(), e);
        }
    }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/AbstractServiceAnnotationProcessor.java`
#### Snippet
```java
        methodSignatureBuilder.append(method.getReturnType())
                .append(' ')
                .append(method.toString());

        return methodSignatureBuilder.toString();
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/SingleRouterChain.java`
#### Snippet
```java
                    + " from registry " + url.getAddress()
                    + " on the consumer " + NetUtils.getLocalHost()
                    + " using the dubbo version " + Version.getVersion() + ". Router snapshot is below: \n" + snapshotNode.toString();
                if (routerSnapshotSwitcher.isEnable()) {
                    routerSnapshotSwitcher.setSnapshot(message);
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/SingleRouterChain.java`
#### Snippet
```java
                    + " from registry " + url.getAddress()
                    + " on the consumer " + NetUtils.getLocalHost()
                    + " using the dubbo version " + Version.getVersion() + " is below: \n" + snapshotNode.toString();
                if (routerSnapshotSwitcher.isEnable()) {
                    routerSnapshotSwitcher.setSnapshot(message);
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `dubbo-remoting/dubbo-remoting-api/src/main/java/org/apache/dubbo/remoting/telnet/support/TelnetUtils.java`
#### Snippet
```java

                    if (rowbuf.length() >= totalWidth) {
                        buf.append(rowbuf.toString());
                        rowbuf = new StringBuilder();
//                        for(int m = 0;m < maxcountbefore && maxcountbefore < totalWidth ; m++){
```

## RuleId[id=InnerClassMayBeStatic]
### InnerClassMayBeStatic
Inner class `ReferenceConfigurationListener` may be 'static'
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/ServiceDiscoveryRegistryDirectory.java`
#### Snippet
```java
    }

    private class ReferenceConfigurationListener extends AbstractConfiguratorListener {
        private final ServiceDiscoveryRegistryDirectory<?> directory;
        private final URL url;
```

### InnerClassMayBeStatic
Inner class `Border` may be 'static'
in `dubbo-plugin/dubbo-qos/src/main/java/org/apache/dubbo/qos/textui/TTable.java`
#### Snippet
```java
     * border style
     */
    public class Border {

        private int borders = BORDER_OUTER | BORDER_INNER;
```

### InnerClassMayBeStatic
Inner class `RemotingServerAdapter` may be 'static'
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/protocol/AbstractProxyProtocol.java`
#### Snippet
```java
    }

    protected abstract class RemotingServerAdapter implements RemotingServer {

        public abstract Object getDelegateServer();
```

### InnerClassMayBeStatic
Inner class `ProxyProtocolServer` may be 'static'
in `dubbo-rpc/dubbo-rpc-api/src/main/java/org/apache/dubbo/rpc/protocol/AbstractProxyProtocol.java`
#### Snippet
```java
    protected abstract <T> Runnable doExport(T impl, Class<T> type, URL url) throws RpcException;

    protected class ProxyProtocolServer implements ProtocolServer {

        private RemotingServer server;
```

### InnerClassMayBeStatic
Inner class `NacosConfigListener` may be 'static'
in `dubbo-metadata/dubbo-metadata-report-nacos/src/main/java/org/apache/dubbo/metadata/store/nacos/NacosMetadataReport.java`
#### Snippet
```java
    }

    public class NacosConfigListener extends AbstractSharedListener {

        private Set<ConfigurationListener> listeners = new CopyOnWriteArraySet<>();
```

### InnerClassMayBeStatic
Inner class `ServiceContext` may be 'static'
in `dubbo-compiler/src/main/java/org/apache/dubbo/gen/AbstractGenerator.java`
#### Snippet
```java
     * Template class for proto Service objects.
     */
    private class ServiceContext {

        // CHECKSTYLE DISABLE VisibilityModifier FOR 8 LINES
```

## RuleId[id=SuspiciousMethodCalls]
### SuspiciousMethodCalls
Suspicious call to 'Collection.contains()'
in `dubbo-common/src/main/java/org/apache/dubbo/common/url/component/URLParam.java`
#### Snippet
```java
        @Override
        public boolean containsValue(Object value) {
            return values().contains(value);
        }

```

### SuspiciousMethodCalls
'Collection' may not contain objects of type 'capture of ?'
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/CollectionUtils.java`
#### Snippet
```java

        try {
            return one.containsAll(another);
        } catch (ClassCastException | NullPointerException unused) {
            return false;
```

### SuspiciousMethodCalls
Suspicious call to 'Map.containsKey()'
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ConcurrentHashSet.java`
#### Snippet
```java
    @Override
    public boolean contains(Object o) {
        return map.containsKey(o);
    }

```

### SuspiciousMethodCalls
'List' may not contain objects of type 'capture of ? extends Element'
in `dubbo-metadata/dubbo-metadata-processor/src/main/java/org/apache/dubbo/metadata/annotation/processing/AbstractServiceAnnotationProcessor.java`
#### Snippet
```java
    protected List<? extends Element> getActualMembers(TypeElement type) {
        List<? extends Element> members = new LinkedList<>(elements.getAllMembers(type));
        members.removeAll(objectMembers);
        return members;
    }
```

### SuspiciousMethodCalls
Suspicious call to 'List.indexOf()'
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/state/BitList.java`
#### Snippet
```java
    @Override
    public boolean contains(Object o) {
        int idx = originList.indexOf(o);
        return (idx >= 0 && rootSet.get(idx)) || (CollectionUtils.isNotEmpty(tailList) && tailList.contains(o));
    }
```

### SuspiciousMethodCalls
Suspicious call to 'List.indexOf()'
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/router/state/BitList.java`
#### Snippet
```java
    @Override
    public boolean remove(Object o) {
        int idx = originList.indexOf(o);
        if (idx > -1 && rootSet.get(idx)) {
            rootSet.set(idx, false);
```

## RuleId[id=SynchronizeOnNonFinalField]
### SynchronizeOnNonFinalField
Synchronization on a non-final field `mdInst`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/MD5Utils.java`
#### Snippet
```java
        byte[] md5;
        // MessageDigest instance is NOT thread-safe
        synchronized (mdInst) {
            mdInst.update(input.getBytes(UTF_8));
            md5 = mdInst.digest();
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `this.validInvokers`
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/directory/AbstractDirectory.java`
#### Snippet
```java

    private boolean addValidInvoker(Invoker<T> invoker) {
        synchronized (this.validInvokers) {
            return this.validInvokers.add(invoker);
        }
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `this.validInvokers`
in `dubbo-cluster/src/main/java/org/apache/dubbo/rpc/cluster/directory/AbstractDirectory.java`
#### Snippet
```java

    private boolean removeValidInvoker(Invoker<T> invoker) {
        synchronized (this.validInvokers) {
            return this.validInvokers.remove(invoker);
        }
```

## RuleId[id=ConditionCoveredByFurtherCondition]
### ConditionCoveredByFurtherCondition
Condition 'exception instanceof AuthenticationException' covered by subsequent condition 'exception instanceof AccessDeniedException'
in `dubbo-plugin/dubbo-spring-security/src/main/java/org/apache/dubbo/spring/security/filter/AuthenticationExceptionTranslatorFilter.java`
#### Snippet
```java

        return result.hasException()
            && (exception instanceof AuthenticationException || exception instanceof AccessDeniedException);

    }
```

## RuleId[id=UnnecessaryContinue]
### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/rest/PathMatcher.java`
#### Snippet
```java
            boolean equals = thisPathSplits[i].equals(pathSplits[i]);
            if (equals) {
                continue;
            } else {
                if (placeHoldCompare(pathSplits[i], thisPathSplits[i])) {
```

### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `dubbo-metadata/dubbo-metadata-api/src/main/java/org/apache/dubbo/metadata/rest/PathMatcher.java`
#### Snippet
```java
            } else {
                if (placeHoldCompare(pathSplits[i], thisPathSplits[i])) {
                    continue;
                } else {
                    return false;
```

### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `dubbo-rpc/dubbo-rpc-rest/src/main/java/org/apache/dubbo/rpc/protocol/rest/util/ReflectUtils.java`
#### Snippet
```java
            tmp = findClassAndTryCatch(s, classLoader);
            if (tmp == null) {
                continue;
            } else {
                return tmp;
```

### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/NetUtils.java`
#### Snippet
```java
        for (int i = 0; i < mask.length; i++) {
            if ("*".equals(mask[i]) || mask[i].equals(ipAddress[i])) {
                continue;
            } else if (mask[i].contains("-")) {
                String[] rangeNumStrs = StringUtils.split(mask[i], '-');
```

### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/NetUtils.java`
#### Snippet
```java
                }
            } else if ("0".equals(ipAddress[i]) && ("0".equals(mask[i]) || "00".equals(mask[i]) || "000".equals(mask[i]) || "0000".equals(mask[i]))) {
                continue;
            } else if (!mask[i].equals(ipAddress[i])) {
                return false;
```

## RuleId[id=CopyConstructorMissesField]
### CopyConstructorMissesField
Copy constructor does not copy field 'rawAddress'
in `dubbo-registry/dubbo-registry-api/src/main/java/org/apache/dubbo/registry/client/DefaultServiceInstance.java`
#### Snippet
```java
    }

    public DefaultServiceInstance(DefaultServiceInstance other) {
        this.serviceName = other.serviceName;
        this.host = other.host;
```

## RuleId[id=RegExpUnnecessaryNonCapturingGroup]
### RegExpUnnecessaryNonCapturingGroup
Unnecessary non-capturing group `(?:[_$a-zA-Z][_$a-zA-Z0-9]*)`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static final Class<?>[] EMPTY_CLASS_ARRAY = new Class<?>[0];

    public static final String JAVA_IDENT_REGEX = "(?:[_$a-zA-Z][_$a-zA-Z0-9]*)";

    public static final String JAVA_NAME_REGEX = "(?:" + JAVA_IDENT_REGEX + "(?:\\." + JAVA_IDENT_REGEX + ")*)";
```

### RegExpUnnecessaryNonCapturingGroup
Unnecessary non-capturing group `(?:(?:[VZBCDFIJS])|(?:L(?:[_$a-zA-Z][_$a-zA-Z0-9]*)(?:\/(?:[_$a-zA-Z][_$a-zA-Z0-9]*))*;)|(?:\[+(?:(?:[VZBCDFIJS])|(?:L(?:[_$a-zA-Z][_$a-zA-Z0-9]*)(?:\/(?:[_$a-zA-Z][_$a-zA-Z0-9]*))*;))))`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static final String ARRAY_DESC = "(?:\\[+(?:(?:[VZBCDFIJS])|" + CLASS_DESC + "))";

    public static final String DESC_REGEX = "(?:(?:[VZBCDFIJS])|" + CLASS_DESC + "|" + ARRAY_DESC + ")";

    public static final Pattern DESC_PATTERN = Pattern.compile(DESC_REGEX);
```

### RegExpUnnecessaryNonCapturingGroup
Unnecessary non-capturing group `(?:[VZBCDFIJS])`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static final String ARRAY_DESC = "(?:\\[+(?:(?:[VZBCDFIJS])|" + CLASS_DESC + "))";

    public static final String DESC_REGEX = "(?:(?:[VZBCDFIJS])|" + CLASS_DESC + "|" + ARRAY_DESC + ")";

    public static final Pattern DESC_PATTERN = Pattern.compile(DESC_REGEX);
```

### RegExpUnnecessaryNonCapturingGroup
Unnecessary non-capturing group `(?:L(?:[_$a-zA-Z][_$a-zA-Z0-9]*)(?:\/(?:[_$a-zA-Z][_$a-zA-Z0-9]*))*;)`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static final String ARRAY_DESC = "(?:\\[+(?:(?:[VZBCDFIJS])|" + CLASS_DESC + "))";

    public static final String DESC_REGEX = "(?:(?:[VZBCDFIJS])|" + CLASS_DESC + "|" + ARRAY_DESC + ")";

    public static final Pattern DESC_PATTERN = Pattern.compile(DESC_REGEX);
```

### RegExpUnnecessaryNonCapturingGroup
Unnecessary non-capturing group `(?:[_$a-zA-Z][_$a-zA-Z0-9]*)`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static final String ARRAY_DESC = "(?:\\[+(?:(?:[VZBCDFIJS])|" + CLASS_DESC + "))";

    public static final String DESC_REGEX = "(?:(?:[VZBCDFIJS])|" + CLASS_DESC + "|" + ARRAY_DESC + ")";

    public static final Pattern DESC_PATTERN = Pattern.compile(DESC_REGEX);
```

### RegExpUnnecessaryNonCapturingGroup
Unnecessary non-capturing group `(?:\[+(?:(?:[VZBCDFIJS])|(?:L(?:[_$a-zA-Z][_$a-zA-Z0-9]*)(?:\/(?:[_$a-zA-Z][_$a-zA-Z0-9]*))*;)))`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static final String ARRAY_DESC = "(?:\\[+(?:(?:[VZBCDFIJS])|" + CLASS_DESC + "))";

    public static final String DESC_REGEX = "(?:(?:[VZBCDFIJS])|" + CLASS_DESC + "|" + ARRAY_DESC + ")";

    public static final Pattern DESC_PATTERN = Pattern.compile(DESC_REGEX);
```

### RegExpUnnecessaryNonCapturingGroup
Unnecessary non-capturing group `(?:[VZBCDFIJS])`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static final String ARRAY_DESC = "(?:\\[+(?:(?:[VZBCDFIJS])|" + CLASS_DESC + "))";

    public static final String DESC_REGEX = "(?:(?:[VZBCDFIJS])|" + CLASS_DESC + "|" + ARRAY_DESC + ")";

    public static final Pattern DESC_PATTERN = Pattern.compile(DESC_REGEX);
```

### RegExpUnnecessaryNonCapturingGroup
Unnecessary non-capturing group `(?:L(?:[_$a-zA-Z][_$a-zA-Z0-9]*)(?:\/(?:[_$a-zA-Z][_$a-zA-Z0-9]*))*;)`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static final String ARRAY_DESC = "(?:\\[+(?:(?:[VZBCDFIJS])|" + CLASS_DESC + "))";

    public static final String DESC_REGEX = "(?:(?:[VZBCDFIJS])|" + CLASS_DESC + "|" + ARRAY_DESC + ")";

    public static final Pattern DESC_PATTERN = Pattern.compile(DESC_REGEX);
```

### RegExpUnnecessaryNonCapturingGroup
Unnecessary non-capturing group `(?:[_$a-zA-Z][_$a-zA-Z0-9]*)`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static final String ARRAY_DESC = "(?:\\[+(?:(?:[VZBCDFIJS])|" + CLASS_DESC + "))";

    public static final String DESC_REGEX = "(?:(?:[VZBCDFIJS])|" + CLASS_DESC + "|" + ARRAY_DESC + ")";

    public static final Pattern DESC_PATTERN = Pattern.compile(DESC_REGEX);
```

### RegExpUnnecessaryNonCapturingGroup
Unnecessary non-capturing group `(?:\\[+(?:(?:[VZBCDFIJS])|(?:L(?:[_$a-zA-Z][_$a-zA-Z0-9]*)(?:\/(?:[_$a-zA-Z][_$a-zA-Z0-9]*))*;)))`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static final String CLASS_DESC = "(?:L" + JAVA_IDENT_REGEX + "(?:\\/" + JAVA_IDENT_REGEX + ")*;)";

    public static final String ARRAY_DESC = "(?:\\[+(?:(?:[VZBCDFIJS])|" + CLASS_DESC + "))";

    public static final String DESC_REGEX = "(?:(?:[VZBCDFIJS])|" + CLASS_DESC + "|" + ARRAY_DESC + ")";
```

### RegExpUnnecessaryNonCapturingGroup
Unnecessary non-capturing group `(?:[VZBCDFIJS])`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static final String CLASS_DESC = "(?:L" + JAVA_IDENT_REGEX + "(?:\\/" + JAVA_IDENT_REGEX + ")*;)";

    public static final String ARRAY_DESC = "(?:\\[+(?:(?:[VZBCDFIJS])|" + CLASS_DESC + "))";

    public static final String DESC_REGEX = "(?:(?:[VZBCDFIJS])|" + CLASS_DESC + "|" + ARRAY_DESC + ")";
```

### RegExpUnnecessaryNonCapturingGroup
Unnecessary non-capturing group `(?:L(?:[_$a-zA-Z][_$a-zA-Z0-9]*)(?:\/(?:[_$a-zA-Z][_$a-zA-Z0-9]*))*;)`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static final String CLASS_DESC = "(?:L" + JAVA_IDENT_REGEX + "(?:\\/" + JAVA_IDENT_REGEX + ")*;)";

    public static final String ARRAY_DESC = "(?:\\[+(?:(?:[VZBCDFIJS])|" + CLASS_DESC + "))";

    public static final String DESC_REGEX = "(?:(?:[VZBCDFIJS])|" + CLASS_DESC + "|" + ARRAY_DESC + ")";
```

### RegExpUnnecessaryNonCapturingGroup
Unnecessary non-capturing group `(?:[_$a-zA-Z][_$a-zA-Z0-9]*)`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static final String CLASS_DESC = "(?:L" + JAVA_IDENT_REGEX + "(?:\\/" + JAVA_IDENT_REGEX + ")*;)";

    public static final String ARRAY_DESC = "(?:\\[+(?:(?:[VZBCDFIJS])|" + CLASS_DESC + "))";

    public static final String DESC_REGEX = "(?:(?:[VZBCDFIJS])|" + CLASS_DESC + "|" + ARRAY_DESC + ")";
```

### RegExpUnnecessaryNonCapturingGroup
Unnecessary non-capturing group `(?:(?:[VZBCDFIJS])|(?:L(?:[_$a-zA-Z][_$a-zA-Z0-9]*)(?:\/(?:[_$a-zA-Z][_$a-zA-Z0-9]*))*;)|(?:\[+(?:(?:[VZBCDFIJS])|(?:L(?:[_$a-zA-Z][_$a-zA-Z0-9]*)(?:\/(?:[_$a-zA-Z][_$a-zA-Z0-9]*))*;))))`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static final Pattern METHOD_DESC_PATTERN = Pattern.compile(METHOD_DESC_REGEX);

    public static final Pattern GETTER_METHOD_DESC_PATTERN = Pattern.compile("get([A-Z][_a-zA-Z0-9]*)\\(\\)(" + DESC_REGEX + ")");

    public static final Pattern SETTER_METHOD_DESC_PATTERN = Pattern.compile("set([A-Z][_a-zA-Z0-9]*)\\((" + DESC_REGEX + ")\\)V");
```

### RegExpUnnecessaryNonCapturingGroup
Unnecessary non-capturing group `(?:[VZBCDFIJS])`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static final Pattern METHOD_DESC_PATTERN = Pattern.compile(METHOD_DESC_REGEX);

    public static final Pattern GETTER_METHOD_DESC_PATTERN = Pattern.compile("get([A-Z][_a-zA-Z0-9]*)\\(\\)(" + DESC_REGEX + ")");

    public static final Pattern SETTER_METHOD_DESC_PATTERN = Pattern.compile("set([A-Z][_a-zA-Z0-9]*)\\((" + DESC_REGEX + ")\\)V");
```

### RegExpUnnecessaryNonCapturingGroup
Unnecessary non-capturing group `(?:L(?:[_$a-zA-Z][_$a-zA-Z0-9]*)(?:\/(?:[_$a-zA-Z][_$a-zA-Z0-9]*))*;)`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static final Pattern METHOD_DESC_PATTERN = Pattern.compile(METHOD_DESC_REGEX);

    public static final Pattern GETTER_METHOD_DESC_PATTERN = Pattern.compile("get([A-Z][_a-zA-Z0-9]*)\\(\\)(" + DESC_REGEX + ")");

    public static final Pattern SETTER_METHOD_DESC_PATTERN = Pattern.compile("set([A-Z][_a-zA-Z0-9]*)\\((" + DESC_REGEX + ")\\)V");
```

### RegExpUnnecessaryNonCapturingGroup
Unnecessary non-capturing group `(?:[_$a-zA-Z][_$a-zA-Z0-9]*)`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static final Pattern METHOD_DESC_PATTERN = Pattern.compile(METHOD_DESC_REGEX);

    public static final Pattern GETTER_METHOD_DESC_PATTERN = Pattern.compile("get([A-Z][_a-zA-Z0-9]*)\\(\\)(" + DESC_REGEX + ")");

    public static final Pattern SETTER_METHOD_DESC_PATTERN = Pattern.compile("set([A-Z][_a-zA-Z0-9]*)\\((" + DESC_REGEX + ")\\)V");
```

### RegExpUnnecessaryNonCapturingGroup
Unnecessary non-capturing group `(?:\[+(?:(?:[VZBCDFIJS])|(?:L(?:[_$a-zA-Z][_$a-zA-Z0-9]*)(?:\/(?:[_$a-zA-Z][_$a-zA-Z0-9]*))*;)))`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static final Pattern METHOD_DESC_PATTERN = Pattern.compile(METHOD_DESC_REGEX);

    public static final Pattern GETTER_METHOD_DESC_PATTERN = Pattern.compile("get([A-Z][_a-zA-Z0-9]*)\\(\\)(" + DESC_REGEX + ")");

    public static final Pattern SETTER_METHOD_DESC_PATTERN = Pattern.compile("set([A-Z][_a-zA-Z0-9]*)\\((" + DESC_REGEX + ")\\)V");
```

### RegExpUnnecessaryNonCapturingGroup
Unnecessary non-capturing group `(?:(?:[VZBCDFIJS])|(?:L(?:[_$a-zA-Z][_$a-zA-Z0-9]*)(?:\/(?:[_$a-zA-Z][_$a-zA-Z0-9]*))*;)|(?:\[+(?:(?:[VZBCDFIJS])|(?:L(?:[_$a-zA-Z][_$a-zA-Z0-9]*)(?:\/(?:[_$a-zA-Z][_$a-zA-Z0-9]*))*;))))`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static final Pattern GETTER_METHOD_DESC_PATTERN = Pattern.compile("get([A-Z][_a-zA-Z0-9]*)\\(\\)(" + DESC_REGEX + ")");

    public static final Pattern SETTER_METHOD_DESC_PATTERN = Pattern.compile("set([A-Z][_a-zA-Z0-9]*)\\((" + DESC_REGEX + ")\\)V");

    public static final Pattern IS_HAS_CAN_METHOD_DESC_PATTERN = Pattern.compile("(?:is|has|can)([A-Z][_a-zA-Z0-9]*)\\(\\)Z");
```

### RegExpUnnecessaryNonCapturingGroup
Unnecessary non-capturing group `(?:[VZBCDFIJS])`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static final Pattern GETTER_METHOD_DESC_PATTERN = Pattern.compile("get([A-Z][_a-zA-Z0-9]*)\\(\\)(" + DESC_REGEX + ")");

    public static final Pattern SETTER_METHOD_DESC_PATTERN = Pattern.compile("set([A-Z][_a-zA-Z0-9]*)\\((" + DESC_REGEX + ")\\)V");

    public static final Pattern IS_HAS_CAN_METHOD_DESC_PATTERN = Pattern.compile("(?:is|has|can)([A-Z][_a-zA-Z0-9]*)\\(\\)Z");
```

### RegExpUnnecessaryNonCapturingGroup
Unnecessary non-capturing group `(?:L(?:[_$a-zA-Z][_$a-zA-Z0-9]*)(?:\/(?:[_$a-zA-Z][_$a-zA-Z0-9]*))*;)`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static final Pattern GETTER_METHOD_DESC_PATTERN = Pattern.compile("get([A-Z][_a-zA-Z0-9]*)\\(\\)(" + DESC_REGEX + ")");

    public static final Pattern SETTER_METHOD_DESC_PATTERN = Pattern.compile("set([A-Z][_a-zA-Z0-9]*)\\((" + DESC_REGEX + ")\\)V");

    public static final Pattern IS_HAS_CAN_METHOD_DESC_PATTERN = Pattern.compile("(?:is|has|can)([A-Z][_a-zA-Z0-9]*)\\(\\)Z");
```

### RegExpUnnecessaryNonCapturingGroup
Unnecessary non-capturing group `(?:[_$a-zA-Z][_$a-zA-Z0-9]*)`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static final Pattern GETTER_METHOD_DESC_PATTERN = Pattern.compile("get([A-Z][_a-zA-Z0-9]*)\\(\\)(" + DESC_REGEX + ")");

    public static final Pattern SETTER_METHOD_DESC_PATTERN = Pattern.compile("set([A-Z][_a-zA-Z0-9]*)\\((" + DESC_REGEX + ")\\)V");

    public static final Pattern IS_HAS_CAN_METHOD_DESC_PATTERN = Pattern.compile("(?:is|has|can)([A-Z][_a-zA-Z0-9]*)\\(\\)Z");
```

### RegExpUnnecessaryNonCapturingGroup
Unnecessary non-capturing group `(?:\[+(?:(?:[VZBCDFIJS])|(?:L(?:[_$a-zA-Z][_$a-zA-Z0-9]*)(?:\/(?:[_$a-zA-Z][_$a-zA-Z0-9]*))*;)))`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static final Pattern GETTER_METHOD_DESC_PATTERN = Pattern.compile("get([A-Z][_a-zA-Z0-9]*)\\(\\)(" + DESC_REGEX + ")");

    public static final Pattern SETTER_METHOD_DESC_PATTERN = Pattern.compile("set([A-Z][_a-zA-Z0-9]*)\\((" + DESC_REGEX + ")\\)V");

    public static final Pattern IS_HAS_CAN_METHOD_DESC_PATTERN = Pattern.compile("(?:is|has|can)([A-Z][_a-zA-Z0-9]*)\\(\\)Z");
```

### RegExpUnnecessaryNonCapturingGroup
Unnecessary non-capturing group `(?:L(?:[_$a-zA-Z][_$a-zA-Z0-9]*)(?:\\/(?:[_$a-zA-Z][_$a-zA-Z0-9]*))*;)`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static final String JAVA_NAME_REGEX = "(?:" + JAVA_IDENT_REGEX + "(?:\\." + JAVA_IDENT_REGEX + ")*)";

    public static final String CLASS_DESC = "(?:L" + JAVA_IDENT_REGEX + "(?:\\/" + JAVA_IDENT_REGEX + ")*;)";

    public static final String ARRAY_DESC = "(?:\\[+(?:(?:[VZBCDFIJS])|" + CLASS_DESC + "))";
```

### RegExpUnnecessaryNonCapturingGroup
Unnecessary non-capturing group `(?:[_$a-zA-Z][_$a-zA-Z0-9]*)`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static final String JAVA_NAME_REGEX = "(?:" + JAVA_IDENT_REGEX + "(?:\\." + JAVA_IDENT_REGEX + ")*)";

    public static final String CLASS_DESC = "(?:L" + JAVA_IDENT_REGEX + "(?:\\/" + JAVA_IDENT_REGEX + ")*;)";

    public static final String ARRAY_DESC = "(?:\\[+(?:(?:[VZBCDFIJS])|" + CLASS_DESC + "))";
```

### RegExpUnnecessaryNonCapturingGroup
Unnecessary non-capturing group `(?:[_$a-zA-Z][_$a-zA-Z0-9]*)`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static final String JAVA_NAME_REGEX = "(?:" + JAVA_IDENT_REGEX + "(?:\\." + JAVA_IDENT_REGEX + ")*)";

    public static final String CLASS_DESC = "(?:L" + JAVA_IDENT_REGEX + "(?:\\/" + JAVA_IDENT_REGEX + ")*;)";

    public static final String ARRAY_DESC = "(?:\\[+(?:(?:[VZBCDFIJS])|" + CLASS_DESC + "))";
```

### RegExpUnnecessaryNonCapturingGroup
Unnecessary non-capturing group `(?:((?:[_$a-zA-Z][_$a-zA-Z0-9]*))?\\(((?:(?:[VZBCDFIJS])|(?:L(?:[_$a-zA-Z][_$a-zA-Z0-9]*)(?:\/(?:[_$a-zA-Z][_$a-zA-Z0-9]*))*;)|(?:\[+(?:(?:[VZBCDFIJS])|(?:L(?:[_$a-zA-Z][_$a-zA-Z0-9]*)(?:\/(?:[_$a-zA-Z][_$a-zA-Z0-9]*))*;))))*)\\)((?:(?:[VZBCDFIJS])|(?:L(?:[_$a-zA-Z][_$a-zA-Z0-9]*)(?:\/(?:[_$a-zA-Z][_$a-zA-Z0-9]*))*;)|(?:\[+(?:(?:[VZBCDFIJS])|(?:L(?:[_$a-zA-Z][_$a-zA-Z0-9]*)(?:\/(?:[_$a-zA-Z][_$a-zA-Z0-9]*))*;)))))?)`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static final Pattern DESC_PATTERN = Pattern.compile(DESC_REGEX);

    public static final String METHOD_DESC_REGEX = "(?:(" + JAVA_IDENT_REGEX + ")?\\((" + DESC_REGEX + "*)\\)(" + DESC_REGEX + ")?)";

    public static final Pattern METHOD_DESC_PATTERN = Pattern.compile(METHOD_DESC_REGEX);
```

### RegExpUnnecessaryNonCapturingGroup
Unnecessary non-capturing group `(?:[_$a-zA-Z][_$a-zA-Z0-9]*)`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static final Pattern DESC_PATTERN = Pattern.compile(DESC_REGEX);

    public static final String METHOD_DESC_REGEX = "(?:(" + JAVA_IDENT_REGEX + ")?\\((" + DESC_REGEX + "*)\\)(" + DESC_REGEX + ")?)";

    public static final Pattern METHOD_DESC_PATTERN = Pattern.compile(METHOD_DESC_REGEX);
```

### RegExpUnnecessaryNonCapturingGroup
Unnecessary non-capturing group `(?:[VZBCDFIJS])`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static final Pattern DESC_PATTERN = Pattern.compile(DESC_REGEX);

    public static final String METHOD_DESC_REGEX = "(?:(" + JAVA_IDENT_REGEX + ")?\\((" + DESC_REGEX + "*)\\)(" + DESC_REGEX + ")?)";

    public static final Pattern METHOD_DESC_PATTERN = Pattern.compile(METHOD_DESC_REGEX);
```

### RegExpUnnecessaryNonCapturingGroup
Unnecessary non-capturing group `(?:L(?:[_$a-zA-Z][_$a-zA-Z0-9]*)(?:\/(?:[_$a-zA-Z][_$a-zA-Z0-9]*))*;)`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static final Pattern DESC_PATTERN = Pattern.compile(DESC_REGEX);

    public static final String METHOD_DESC_REGEX = "(?:(" + JAVA_IDENT_REGEX + ")?\\((" + DESC_REGEX + "*)\\)(" + DESC_REGEX + ")?)";

    public static final Pattern METHOD_DESC_PATTERN = Pattern.compile(METHOD_DESC_REGEX);
```

### RegExpUnnecessaryNonCapturingGroup
Unnecessary non-capturing group `(?:[_$a-zA-Z][_$a-zA-Z0-9]*)`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static final Pattern DESC_PATTERN = Pattern.compile(DESC_REGEX);

    public static final String METHOD_DESC_REGEX = "(?:(" + JAVA_IDENT_REGEX + ")?\\((" + DESC_REGEX + "*)\\)(" + DESC_REGEX + ")?)";

    public static final Pattern METHOD_DESC_PATTERN = Pattern.compile(METHOD_DESC_REGEX);
```

### RegExpUnnecessaryNonCapturingGroup
Unnecessary non-capturing group `(?:\[+(?:(?:[VZBCDFIJS])|(?:L(?:[_$a-zA-Z][_$a-zA-Z0-9]*)(?:\/(?:[_$a-zA-Z][_$a-zA-Z0-9]*))*;)))`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static final Pattern DESC_PATTERN = Pattern.compile(DESC_REGEX);

    public static final String METHOD_DESC_REGEX = "(?:(" + JAVA_IDENT_REGEX + ")?\\((" + DESC_REGEX + "*)\\)(" + DESC_REGEX + ")?)";

    public static final Pattern METHOD_DESC_PATTERN = Pattern.compile(METHOD_DESC_REGEX);
```

### RegExpUnnecessaryNonCapturingGroup
Unnecessary non-capturing group `(?:(?:[VZBCDFIJS])|(?:L(?:[_$a-zA-Z][_$a-zA-Z0-9]*)(?:\/(?:[_$a-zA-Z][_$a-zA-Z0-9]*))*;)|(?:\[+(?:(?:[VZBCDFIJS])|(?:L(?:[_$a-zA-Z][_$a-zA-Z0-9]*)(?:\/(?:[_$a-zA-Z][_$a-zA-Z0-9]*))*;))))`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static final Pattern DESC_PATTERN = Pattern.compile(DESC_REGEX);

    public static final String METHOD_DESC_REGEX = "(?:(" + JAVA_IDENT_REGEX + ")?\\((" + DESC_REGEX + "*)\\)(" + DESC_REGEX + ")?)";

    public static final Pattern METHOD_DESC_PATTERN = Pattern.compile(METHOD_DESC_REGEX);
```

### RegExpUnnecessaryNonCapturingGroup
Unnecessary non-capturing group `(?:[VZBCDFIJS])`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static final Pattern DESC_PATTERN = Pattern.compile(DESC_REGEX);

    public static final String METHOD_DESC_REGEX = "(?:(" + JAVA_IDENT_REGEX + ")?\\((" + DESC_REGEX + "*)\\)(" + DESC_REGEX + ")?)";

    public static final Pattern METHOD_DESC_PATTERN = Pattern.compile(METHOD_DESC_REGEX);
```

### RegExpUnnecessaryNonCapturingGroup
Unnecessary non-capturing group `(?:L(?:[_$a-zA-Z][_$a-zA-Z0-9]*)(?:\/(?:[_$a-zA-Z][_$a-zA-Z0-9]*))*;)`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static final Pattern DESC_PATTERN = Pattern.compile(DESC_REGEX);

    public static final String METHOD_DESC_REGEX = "(?:(" + JAVA_IDENT_REGEX + ")?\\((" + DESC_REGEX + "*)\\)(" + DESC_REGEX + ")?)";

    public static final Pattern METHOD_DESC_PATTERN = Pattern.compile(METHOD_DESC_REGEX);
```

### RegExpUnnecessaryNonCapturingGroup
Unnecessary non-capturing group `(?:[_$a-zA-Z][_$a-zA-Z0-9]*)`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static final Pattern DESC_PATTERN = Pattern.compile(DESC_REGEX);

    public static final String METHOD_DESC_REGEX = "(?:(" + JAVA_IDENT_REGEX + ")?\\((" + DESC_REGEX + "*)\\)(" + DESC_REGEX + ")?)";

    public static final Pattern METHOD_DESC_PATTERN = Pattern.compile(METHOD_DESC_REGEX);
```

### RegExpUnnecessaryNonCapturingGroup
Unnecessary non-capturing group `(?:\[+(?:(?:[VZBCDFIJS])|(?:L(?:[_$a-zA-Z][_$a-zA-Z0-9]*)(?:\/(?:[_$a-zA-Z][_$a-zA-Z0-9]*))*;)))`
in `dubbo-common/src/main/java/org/apache/dubbo/common/utils/ReflectUtils.java`
#### Snippet
```java
    public static final Pattern DESC_PATTERN = Pattern.compile(DESC_REGEX);

    public static final String METHOD_DESC_REGEX = "(?:(" + JAVA_IDENT_REGEX + ")?\\((" + DESC_REGEX + "*)\\)(" + DESC_REGEX + ")?)";

    public static final Pattern METHOD_DESC_PATTERN = Pattern.compile(METHOD_DESC_REGEX);
```

## RuleId[id=ThrowableNotThrown]
### ThrowableNotThrown
Result of `getThrowable()` not thrown
in `dubbo-config/dubbo-config-api/src/main/java/org/apache/dubbo/config/utils/ConfigValidationUtils.java`
#### Snippet
```java
                try {
                    //Check whether the mock value is legal
                    MockInvoker.getThrowable(normalizedMock);
                } catch (Exception e) {
                    throw new IllegalStateException("Illegal mock throw in <dubbo:service/reference ... " +
```

## RuleId[id=EqualsWhichDoesntCheckParameterClass]
### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `dubbo-common/src/main/java/org/apache/dubbo/common/Parameters.java`
#### Snippet
```java

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
```

### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `dubbo-filter/dubbo-filter-cache/src/main/java/org/apache/dubbo/cache/support/expiring/ExpiringMap.java`
#### Snippet
```java

        @Override
        public boolean equals(Object obj) {
            if (this == obj) {
                return true;
```

### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `dubbo-filter/dubbo-filter-cache/src/main/java/org/apache/dubbo/cache/support/expiring/ExpiringMap.java`
#### Snippet
```java

    @Override
    public boolean equals(Object obj) {
        if (this == obj) {
            return true;
```

### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `dubbo-compatible/src/main/java/com/alibaba/dubbo/common/DelegateURL.java`
#### Snippet
```java

    @Override
    public boolean equals(Object obj) {
        return apacheUrl.equals(obj);
    }
```

